/**
 * TabBar 组件常量配置
 * TabBar component constants
 */

/** 滚动步长（像素） */
export const SCROLL_STEP = 200;

/** 初始延迟检测时间（毫秒） */
export const INITIAL_DELAYS = [0, 100, 300] as const;

/** 默认高度（像素） */
export const DEFAULT_HEIGHT = 40;

/** 默认内边距（像素） */
export const DEFAULT_PADDING = 16;

/** 最小宽度（像素） */
export const MIN_WIDTH = 32;

/** 滚动容差（像素） */
export const SCROLL_TOLERANCE = 1;

/** 下拉菜单最高层级 */
export const DROPDOWN_MAX_ZINDEX = 1000;
