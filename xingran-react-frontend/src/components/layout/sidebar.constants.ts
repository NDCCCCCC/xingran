/**
 * Sidebar 组件常量配置
 * Sidebar component constants
 */

/** 默认侧边栏宽度（像素） */
export const DEFAULT_SIDEBAR_WIDTH = 280;

/** Header高度（像素） */
export const HEADER_HEIGHT = 64;

/** 收缩按钮位置（像素） */
export const COLLAPSE_BUTTON_LEFT = 16;

/** 菜单基础字体大小（像素） */
export const MENU_FONT_SIZE = 14;

/** 导航触发延迟时间（毫秒） */
export const NAVIGATION_DELAY = 100;
