/**
 * Phase 83 CR-01 trial placeholder — proves the PR diff coverage gate pathspec
 * mirrors vitest coverage.exclude: src/test/** stays out of the diff
 * denominator, so future harness files cannot trip GOV-04. Never imported.
 */
export function trialPlaceholder(): number {
  return 0;
}
