# legacy-peer-deps 仍需保留（2026-08-20 核实）。
#
# 项目实际运行 eslint 9.39.2（并非早期注释所称的 eslint 10）。
# eslint-plugin-react@7.37.5（截至 2026-08 仍是 npm 上最新发布版）
# 声明的 peer 上限为 "^3 || ^4 || ^5 || ^6 || ^7 || ^8 || ^9.7"，
# 而 9.39.2 > 9.7，不在其范围内 → npm 严格 peer 解析会报 ERESOLVE。
# 因此必须安装时使用 --legacy-peer-deps。本地与 CI 的 `npm ci` 需
# 同一标志以保证可复现。
#
# 注意：legacy-peer-deps=true 会让 npm 不再自动安装 peer dependency。
# 任何被代码直接 import 的包都必须显式写进 package.json（否则干净
# 安装会缺失）。ESLint import/no-extraneous-dependencies 已启用以防
# 此类问题（参见 2026-08 @testing-library/dom CI 故障）。
#
# 移除条件：eslint-plugin-react 发布 peer 范围覆盖 eslint 9.39+（或
# 项目升级到的版本）的新版后，删除本文件并在干净环境验证 `npm ci`
# 无 ERESOLVE。dependabot 会在该包发布新版时自动提 PR。
legacy-peer-deps=true
