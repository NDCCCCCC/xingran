package operations

import (
	"testing"
)

func TestBuildingService_Create(t *testing.T) {
	// 这里需要 mock DB 或者使用测试数据库
	// 暂时跳过，实际实现时需要添加
	t.Skip("需要 mock DB 或测试数据库")
}

func TestBuildingService_CheckCodeUnique(t *testing.T) {
	t.Skip("需要 mock DB 或测试数据库")
}
