---
phase: 56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/services/portcollection/vendor_port_template.go
  - internal/services/portcollection/vendor_port_template_test.go
autonomous: true
requirements:
  - VLAN-02
  - BIND-03
  - BIND-04
  - TEST-01
user_setup: []

must_haves:
  truths:
    - "Caller can obtain the exact CLI command sequence for any of 21 (vendor, action) pairs (3 vendors x 7 actions) via a single public RenderCommand function"
    - "Huawei set_access_vlan emits a 3-command sequence: `interface <iface>` / `port link-type access` / `port default vlan <vlanId>` (RISK-03 universal prefix)"
    - "H3C set_access_vlan emits a 3-command sequence: `interface <iface>` / `port link-type access` / `port access vlan <vlanId>` (Huawei/H3C share link-type prefix but differ in vlan keyword)"
    - "Ruijie set_access_vlan emits a 3-command sequence: `interface <iface>` / `switchport mode access` / `switchport access vlan <vlanId>` (Cisco-style)"
    - "Huawei port_binding add emits `user-bind static ip-address <IP> [mac-address <MAC-HUAWEI>]` (MAC format `AABB-CCDD-EEFF`)"
    - "H3C port_binding add emits `user-bind ip-address <IP> [mac-address <MAC-HUAWEI>]` WITHOUT the `static` keyword (RISK-01 per H3C Comware V7 manual)"
    - "Ruijie port_binding add emits `switchport port-security binding <MAC-RUIJIE> <IP>` (MAC format `aabb.ccdd.eeff`, full syntax per RISK-02)"
    - "Port_binding remove variants emit vendor-correct `undo` / `no` prefix for each vendor"
    - "All 12+ new table-driven test cases pass; the 17 existing v1.19 matrix subtests remain green (zero regression)"
  artifacts:
    - path: "internal/services/portcollection/vendor_port_template.go"
      provides: "Vendor x action x command map extended with ActionSetAccessVLAN + ActionPortBinding (3 vendors each)"
      min_lines: 220
      must_contain:
        - "ActionSetAccessVLAN"
        - "ActionPortBinding"
        - "VLANID"
        - "BindOp"
        - "IPAddress"
        - "MACAddress"
        - "renderHuaweiSetAccessVlan"
        - "renderH3CSetAccessVlan"
        - "renderRuijieSetAccessVlan"
        - "renderHuaweiPortBindingAdd"
        - "renderH3CPortBindingAdd"
        - "renderRuijiePortBindingAdd"
        - "renderHuaweiPortBindingRemove"
        - "renderH3CPortBindingRemove"
        - "renderRuijiePortBindingRemove"
    - path: "internal/services/portcollection/vendor_port_template_test.go"
      provides: "Table-driven coverage of 6 new (vendor, action) combos + add/remove variants = 12+ new test cases"
      min_lines: 220
      must_contain:
        - "huawei_set_access_vlan"
        - "h3c_set_access_vlan"
        - "ruijie_set_access_vlan"
        - "huawei_port_binding_add"
        - "huawei_port_binding_add_with_mac"
        - "huawei_port_binding_remove"
        - "h3c_port_binding_add"
        - "h3c_port_binding_remove"
        - "ruijie_port_binding_add"
        - "ruijie_port_binding_add_no_mac"
        - "ruijie_port_binding_remove"
  key_links:
    - from: "internal/services/portcollection/vendor_port_template.go"
      to: "internal/services/mac_normalize.go"
      via: "PortBinding render functions call NormalizeMACAddress then convert to vendor-specific format (Huawei/H3C: `AA-BB-CC-DD-EE-FF`, Ruijie: `AABB.CCDD.EEFF`)"
      pattern: "NormalizeMACAddress"
    - from: "internal/services/portcollection/vendor_port_template.go"
      to: "internal/services/portcollection/vendor_port_template.go (v1.19 baseline)"
      via: "Extension of vendorPortTemplate map (Huawei/H3C/Ruijie keys) with 2 new actions"
      pattern: "vendorPortTemplate\\[models\\.VendorHuawei\\]|vendorPortTemplate\\[models\\.VendorH3C\\]|vendorPortTemplate\\[models\\.VendorRuijie\\]"

---

## Phase Goal

**As a** Phase 56 W2 service author, **I want to** call a single locked function that returns the exact `[]string` command sequence for any of the 21 supported `(vendor, action)` pairs (3 vendors x 7 actions including the 2 new v1.20.1 actions), **so that** SSH write templates for `set_access_vlan` and `port_binding` are vendor-syntax-correct by construction and Phase W2's `PortWriteService` can consume them via the same `RenderCommand` entry point used by v1.19.

---

<objective>
Extend the v1.19 vendor template contract (5 actions) with 2 new actions (`set_access_vlan` + `port_binding`) for 3 vendors (Huawei / H3C / Ruijie), per the v1.20.1 design doc §3 and the RESEARCH.md RISK-01 / RISK-02 / RISK-03 resolutions. Add 12+ new table-driven test rows covering each (vendor, action, add/remove, with/without MAC) combination. Verify zero regression in the 17 existing v1.19 matrix tests.

Purpose: Pure-Go map + tests (zero external dependencies). Stops vendor syntax drift early (Pitfall #3 in STATE.md) and provides the stable contract Phase 56 W2's `PortWriteService` consumes via the same `RenderCommand` entry point.

Output: 2 modified Go files (`vendor_port_template.go` + `vendor_port_template_test.go`); `go build ./...` and `go test ./internal/services/portcollection/...` both exit 0; 29+ total subtests (17 existing + 12 new) all pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-RESEARCH.md
@.planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-PATTERNS.md
@.planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-UI-SPEC.md
@.planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-VALIDATION.md
@docs/plans/2026-07-09-v1.20.1-design.md
@.planning/phases/50-w1-vendor-templates-unit-tests-vendor-action-command-map/50-01-SUMMARY.md

# v1.19 vendor template (extending baseline)
@internal/services/portcollection/vendor_port_template.go

# v1.19 vendor template tests (extending table)
@internal/services/portcollection/vendor_port_template_test.go

# MAC normalization helper (Pitfall 7 + RISK resolution)
@internal/services/mac_normalize.go

# DeviceVendor constants (vendor parameter type)
@internal/models/network_device.go

# Phase 50 W1 plan reference (template + naming conventions)
@.planning/phases/50-w1-vendor-templates-unit-tests-vendor-action-command-map/50-01-PLAN.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From internal/services/portcollection/vendor_port_template.go (v1.19 baseline, file being extended):
```go
type PortAction string
const (
    ActionShutdown     PortAction = "shutdown"
    ActionUndoShutdown PortAction = "undo_shutdown"
    ActionDescription  PortAction = "description"
    ActionDot1xEnable  PortAction = "dot1x_enable"
    ActionDot1xDisable PortAction = "dot1x_disable"
)
type PortTemplateParams struct {
    InterfaceName string
    Description   string
}
func RenderCommand(vendor models.DeviceVendor, action PortAction, params PortTemplateParams) ([]string, error)
func (a PortAction) String() string
var vendorPortTemplate = map[models.DeviceVendor]map[PortAction]func(PortTemplateParams) ([]string, error){...}
```

From internal/models/network_device.go (verified lines 14-22):
```go
type DeviceVendor string
const (
    VendorHuawei DeviceVendor = "huawei"
    VendorH3C    DeviceVendor = "h3c"
    VendorRuijie DeviceVendor = "ruijie"
    VendorMaipu  DeviceVendor = "maipu"  // NOT in scope
)
```

From internal/services/mac_normalize.go (verified lines 32-56):
```go
// NormalizeMACAddress 将任意 MAC 格式归一为大写冒号格式 AA:BB:CC:DD:EE:FF
// 接受 : / - / . / 无分隔符 4 种输入,返回 "" 表示无效
func NormalizeMACAddress(input string) string
```

From internal/services/portcollection/vendor_port_template.go (line 95-102 — VendorExitViewCmd):
```go
func VendorExitViewCmd(vendor models.DeviceVendor) string {
    switch vendor {
    case models.VendorHuawei, models.VendorH3C:
        return "quit"
    default: // VendorRuijie / 思科风格
        return "exit"
    }
}
```

From docs/plans/2026-07-09-v1.20.1-design.md §3.2 (locked vendor command sequences):
- Huawei set_access_vlan: `interface <iface>` / `port link-type access` / `port default vlan <vlanId>` / `quit`
- H3C set_access_vlan: `interface <iface>` / `port link-type access` / `port access vlan <vlanId>` / `quit`
- Ruijie set_access_vlan: `interface <iface>` / `switchport mode access` / `switchport access vlan <vlanId>` / `exit`
- Huawei port_binding add: `user-bind static ip-address <IP> [mac-address <MAC-HUAWEI>]`
- H3C port_binding add: `user-bind ip-address <IP> [mac-address <MAC-HUAWEI>]` (NO `static` keyword per RISK-01)
- Ruijie port_binding add: `switchport port-security binding <mac-address> <ip-address>` (full syntax per RISK-02)
- Huawei port_binding remove: `undo user-bind static ip-address <IP> [mac-address <MAC-HUAWEI>]`
- H3C port_binding remove: `undo user-bind ip-address <IP> [mac-address <MAC-HUAWEI>]`
- Ruijie port_binding remove: `no switchport port-security binding <ip-address>`

From .planning/phases/56-.../56-RESEARCH.md §Pitfall 7 (MAC format divergence per vendor):
- Huawei: `AA-BB-CC-DD-EE-FF` (replace `:` with `-`)
- H3C: same as Huawei (`AA-BB-CC-DD-EE-FF`)
- Ruijie: `AABB.CCDD.EEFF` (replace `:` with `.`, strip dashes)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend vendor_port_template.go — add 2 PortAction constants + PortTemplateParams fields + 9 render functions + 6 map entries</name>
  <files>internal/services/portcollection/vendor_port_template.go</files>
  <read_first>
    - D:\code\ClaudeCode\xingran-go-backend\internal\services\portcollection\vendor_port_template.go (v1.19 baseline; lines 13-19 PortAction consts, lines 28-31 PortTemplateParams, lines 47-72 vendorPortTemplate map, lines 95-102 VendorExitViewCmd, lines 126-170 existing renderers)
    - D:\code\ClaudeCode\xingran-go-backend\internal\services\mac_normalize.go (lines 32-56 — NormalizeMACAddress returns `AA:BB:CC:DD:EE:FF` or `""`)
    - D:\code\ClaudeCode\xingran-go-backend\internal\models\network_device.go (lines 14-22 — DeviceVendor constants VendorHuawei / VendorH3C / VendorRuijie)
    - D:\code\ClaudeCode\xingran-go-backend\.planning\phases\56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09\56-RESEARCH.md (Pitfall 7 — MAC format per vendor; RISK-01/02/03 resolutions)
    - D:\code\ClaudeCode\xingran-go-backend\docs\plans\2026-07-09-v1.20.1-design.md (lines 60-90 — locked vendor command sequences for set_access_vlan + port_binding)
  </read_first>
  <acceptance_criteria>
    - File `internal/services/portcollection/vendor_port_template.go` has the literal strings: `ActionSetAccessVLAN PortAction = "set_access_vlan"`, `ActionPortBinding PortAction = "port_binding"`
    - `PortTemplateParams` struct has new fields `VLANID int` / `BindOp string` / `IPAddress string` / `MACAddress string` added to existing struct (NOT replaced)
    - File contains 9 new renderer function names: `renderHuaweiSetAccessVlan`, `renderH3CSetAccessVlan`, `renderRuijieSetAccessVlan`, `renderHuaweiPortBindingAdd`, `renderH3CPortBindingAdd`, `renderRuijiePortBindingAdd`, `renderHuaweiPortBindingRemove`, `renderH3CPortBindingRemove`, `renderRuijiePortBindingRemove`
    - `vendorPortTemplate` map for each of 3 vendors (Huawei / H3C / Ruijie) gets 2 new entries: `ActionSetAccessVlan` and `ActionPortBinding` (call site branching on `p.BindOp` for add/remove)
    - `RenderCommand` function body UNCHANGED (lookup pattern handles new actions automatically)
    - Source contains `services.NormalizeMACAddress` (or equivalent import path) for MAC normalization; Huawei/H3C MAC format converts `:` → `-`; Ruijie MAC format converts `:` → `.` (per Pitfall 7)
    - File does NOT contain `models.VendorMaipu` as a map key (Maipu still out of scope)
    - New functions in `RenderCommand` lookup path use the same sentinel error pattern as v1.19 (`ErrEmptyInterfaceName` is the only sentinel new actions can return; MAC/IP validation deferred to W2 service)
    - `go build ./internal/services/portcollection/...` exits 0
  </acceptance_criteria>
  <action>
    Extend `internal/services/portcollection/vendor_port_template.go` (v1.19 baseline) with 2 new actions for 3 vendors. Use `Edit` (NOT full rewrite) to preserve v1.19 symbols exactly.

    1) Extend the `const` block (after `ActionDot1xDisable PortAction = "dot1x_disable"`) with 2 new PortAction constants:
       - `ActionSetAccessVLAN PortAction = "set_access_vlan"`
       - `ActionPortBinding PortAction = "port_binding"`
       Per 56-RESEARCH §State of Art, the trailing-vlan in PortAction is uppercase "VLAN" (matches operlog.go OperTypeUpdate naming convention).

    2) Extend `PortTemplateParams` struct (after `Description string`) with 4 new fields, each with a doc comment:
       - `VLANID int` // set_access_vlan 唯一使用 (1-4094)
       - `BindOp string` // port_binding 唯一使用: "add" or "remove"
       - `IPAddress string` // port_binding 唯一使用
       - `MACAddress string` // port_binding 可选 (Huawei/H3C 用, Ruijie 接受)

    3) Add 2 helper functions BEFORE the map (top of file near `wrapInterface`):
       - `toHuaweiH3CMACFormat(mac string) string` — calls `services.NormalizeMACAddress(mac)`, then `strings.ReplaceAll(result, ":", "-")`. Returns `""` if normalize returns empty. Use absolute import `github.com/xingran-next/xingran-go-backend/internal/services`.
       - For Ruijie, the format conversion is done inline in the renderer via `strings.ReplaceAll(strings.ToLower(services.NormalizeMACAddress(mac)), ":", ".")` — produces `aaaa.bbbb.cccc`. No separate helper needed.

    4) Add 9 new render functions AFTER the existing `renderRuijieDot1xDisable` (line 170):

       a) `renderHuaweiSetAccessVlan(p PortTemplateParams) ([]string, error)`:
          - Validate `p.VLANID >= 1 && p.VLANID <= 4094`; if not, return `nil, fmt.Errorf("portcollection: vlanId %d out of range 1-4094", p.VLANID)`. (Defensive guard; service layer is source of truth for rejection.)
          - Return `[]string{ fmt.Sprintf("interface %s", p.InterfaceName), "port link-type access", fmt.Sprintf("port default vlan %d", p.VLANID) }`.

       b) `renderH3CSetAccessVlan(p PortTemplateParams) ([]string, error)`:
          - Same vlanId range check.
          - Return `[]string{ fmt.Sprintf("interface %s", p.InterfaceName), "port link-type access", fmt.Sprintf("port access vlan %d", p.VLANID) }`.
          - Note: H3C uses `port access vlan` keyword, NOT `port default vlan` (RISK-01 keyword divergence).

       c) `renderRuijieSetAccessVlan(p PortTemplateParams) ([]string, error)`:
          - Same vlanId range check.
          - Return `[]string{ fmt.Sprintf("interface %s", p.InterfaceName), "switchport mode access", fmt.Sprintf("switchport access vlan %d", p.VLANID) }`.

       d) `renderHuaweiPortBindingAdd(p PortTemplateParams) ([]string, error)`:
          - Validate `p.BindOp == "add"`; `p.IPAddress != ""`; MAC may be empty.
          - `args := fmt.Sprintf("ip-address %s", p.IPAddress)`; if `p.MACAddress != ""`, append ` mac-address <huawei-mac>` via `toHuaweiH3CMACFormat(p.MACAddress)`.
          - Return `[]string{ fmt.Sprintf("interface %s", p.InterfaceName), fmt.Sprintf("user-bind static %s", args) }`. The `static` keyword is Huawei-specific (RISK-01; H3C omits it).

       e) `renderH3CPortBindingAdd(p PortTemplateParams) ([]string, error)`:
          - Same as Huawei add but WITHOUT the `static` keyword: `fmt.Sprintf("user-bind %s", args)`. (RISK-01 resolution.)

       f) `renderRuijiePortBindingAdd(p PortTemplateParams) ([]string, error)`:
          - If `p.MACAddress == ""`: return `[]string{ fmt.Sprintf("interface %s", p.InterfaceName), fmt.Sprintf("switchport port-security binding %s", p.IPAddress) }`.
          - If MAC present: `mac := strings.ReplaceAll(strings.ToLower(services.NormalizeMACAddress(p.MACAddress)), ":", ".")` — produces `aaaa.bbbb.cccc`; if normalize returns empty return `nil, fmt.Errorf("portcollection: invalid mac for ruijie binding: %q", p.MACAddress)`. Return `[]string{ fmt.Sprintf("interface %s", p.InterfaceName), fmt.Sprintf("switchport port-security binding %s %s", mac, p.IPAddress) }`. (RISK-02: full syntax with MAC first, IP second.)

       g) `renderHuaweiPortBindingRemove(p PortTemplateParams) ([]string, error)`:
          - `args := fmt.Sprintf("ip-address %s", p.IPAddress)`; if `p.MACAddress != ""`, append ` mac-address <huawei-mac>`.
          - Return `[]string{ fmt.Sprintf("interface %s", p.InterfaceName), fmt.Sprintf("undo user-bind static %s", args) }`.

       h) `renderH3CPortBindingRemove(p PortTemplateParams) ([]string, error)`:
          - Same as Huawei remove but WITHOUT `static`: `fmt.Sprintf("undo user-bind %s", args)`.

       i) `renderRuijiePortBindingRemove(p PortTemplateParams) ([]string, error)`:
          - If `p.MACAddress == ""`: return `[]string{ fmt.Sprintf("interface %s", p.InterfaceName), fmt.Sprintf("no switchport port-security binding %s", p.IPAddress) }`.
          - If MAC present: build mac via same format as add; return `[]string{ fmt.Sprintf("interface %s", p.InterfaceName), fmt.Sprintf("no switchport port-security binding %s %s", mac, p.IPAddress) }`.

    5) Add 6 new entries to the `vendorPortTemplate` map (extending each of 3 vendor keys):

       - `models.VendorHuawei` map gets:
         - `ActionSetAccessVLAN: renderHuaweiSetAccessVlan`
         - `ActionPortBinding: dispatchHuaweiPortBinding` (use a small inline dispatch closure that calls `renderHuaweiPortBindingAdd` if `p.BindOp == "add"`, else `renderHuaweiPortBindingRemove`).
       - `models.VendorH3C` map gets:
         - `ActionSetAccessVLAN: renderH3CSetAccessVlan`
         - `ActionPortBinding: dispatchH3CPortBinding` (similar dispatch closure).
       - `models.VendorRuijie` map gets:
         - `ActionSetAccessVLAN: renderRuijieSetAccessVlan`
         - `ActionPortBinding: dispatchRuijiePortBinding` (similar dispatch closure).

       Dispatch closure example:
       ```go
       ActionPortBinding: func(p PortTemplateParams) ([]string, error) {
           if p.BindOp == "add" {
               return renderHuaweiPortBindingAdd(p)
           }
           return renderHuaweiPortBindingRemove(p)
       },
       ```

    6) Add new imports: `strings` (for MAC format conversion), `github.com/xingran-next/xingran-go-backend/internal/services` (for `NormalizeMACAddress`).

    7) Do NOT modify the existing `RenderCommand` function body — the new actions are picked up automatically because the lookup pattern is generic (`vendorMap[action]`). Do NOT add a new sentinel error for MAC / IP / vlanId validation in this file — service layer (W2) owns that with `ErrVlanIdOutOfRange` / `ErrIPAddressInvalid` / `ErrMACAddressInvalid` sentinels. This file only formats valid input; the inline `fmt.Errorf` in renderers is a defensive guard for cases where the service forgets to validate.

    8) `VendorExitViewCmd` function is UNCHANGED — the trailing `quit` (Huawei/H3C) / `exit` (Ruijie) is appended by the service `executeWrite` function (v1.19 Phase 51 pattern), not by the renderer. New actions automatically inherit this behavior.

    9) Run `go build ./internal/services/portcollection/...` after editing — must exit 0. Fix any compilation errors before Task 2.

    AVOID: hardcoding the IP regex or MAC regex in this file (service owns validation). AVOID: writing to `cache_keys.go` or `collection.go` (those are sibling files for port collection caching; out of scope for W1). AVOID: introducing a `BindOp` sentinel error — service owns that.
  </action>
  <verify>
    <automated>cd "D:/code/ClaudeCode/xingran-go-backend" && go build ./internal/services/portcollection/... && echo BUILD_OK</automated>
  </verify>
  <done>
    `internal/services/portcollection/vendor_port_template.go` extended with 2 PortAction constants + 4 PortTemplateParams fields + 9 render functions + 6 map entries (3 vendors × 2 actions). `go build ./internal/services/portcollection/...` exits 0; existing v1.19 symbols (`ActionShutdown` through `ActionDot1xDisable`, `PortTemplateParams.Description`, `RenderCommand` lookup logic) are unchanged. Service layer (W2) can now call `RenderCommand(vendor, ActionSetAccessVLAN, params{VLANID: 100})` and `RenderCommand(vendor, ActionPortBinding, params{BindOp: "add", IPAddress: "10.62.25.5", MACAddress: "AA:BB:CC:DD:EE:FF"})` to obtain the correct `[]string` command sequence per vendor.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend vendor_port_template_test.go — 12+ new test cases for set_access_vlan + port_binding (3 vendors × 2 actions × variants)</name>
  <files>internal/services/portcollection/vendor_port_template_test.go</files>
  <read_first>
    - D:\code\ClaudeCode\xingran-go-backend\internal\services\portcollection\vendor_port_template_test.go (v1.19 baseline test file; preserves 17 existing TestRenderCommand_* functions)
    - D:\code\ClaudeCode\xingran-go-backend\internal\services\portcollection\vendor_port_template.go (Task 1 output — 2 new action constants, 4 new struct fields, 9 new renderers)
    - D:\code\ClaudeCode\xingran-go-backend\docs\plans\2026-07-09-v1.20.1-design.md (lines 60-90 — locked command sequences for assertion)
    - D:\code\ClaudeCode\xingran-go-backend\.planning\phases\56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09\56-RESEARCH.md (Pitfall 7 — MAC format examples; RISK resolutions)
  </read_first>
  <acceptance_criteria>
    - File `internal/services/portcollection/vendor_port_template_test.go` exists (modified, not replaced) and still contains the 6 v1.19 test functions: `TestRenderCommand_VendorActionMatrix`, `TestRenderCommand_EmptyInterfaceName`, `TestRenderCommand_UnsupportedVendor`, `TestRenderCommand_UnknownAction`, `TestRenderCommand_DescriptionEmpty`, `TestRenderCommand_DescriptionTooLong`
    - `TestRenderCommand_VendorActionMatrix` is extended with 12+ new table rows covering: `huawei_set_access_vlan`, `h3c_set_access_vlan`, `ruijie_set_access_vlan`, `huawei_port_binding_add`, `huawei_port_binding_add_with_mac`, `huawei_port_binding_remove`, `h3c_port_binding_add` (no static), `h3c_port_binding_remove`, `ruijie_port_binding_add` (with MAC), `ruijie_port_binding_add_no_mac` (IP only), `ruijie_port_binding_remove` (with MAC)
    - `huawei_set_access_vlan` subtest asserts result equals `[]string{"interface GE0/0/1", "port link-type access", "port default vlan 100"}` for `models.VendorHuawei + ActionSetAccessVLAN + PortTemplateParams{InterfaceName: "GE0/0/1", VLANID: 100}`
    - `h3c_set_access_vlan` subtest asserts result equals `[]string{"interface GE0/0/1", "port link-type access", "port access vlan 100"}` (H3C uses `port access vlan`, NOT `port default vlan` — RISK-01)
    - `ruijie_set_access_vlan` subtest asserts result equals `[]string{"interface GigabitEthernet0/0/1", "switchport mode access", "switchport access vlan 100"}`
    - `huawei_port_binding_add` (no MAC) subtest asserts result equals `[]string{"interface GE0/0/1", "user-bind static ip-address 10.62.25.5"}` (note: `static` keyword present for Huawei)
    - `huawei_port_binding_add_with_mac` subtest asserts result equals `[]string{"interface GE0/0/1", "user-bind static ip-address 10.62.25.5 mac-address AABB-CCDD-EEFF"}` (Huawei MAC format = hyphen-separated uppercase)
    - `h3c_port_binding_add` (no MAC) subtest asserts result equals `[]string{"interface GE0/0/1", "user-bind ip-address 10.62.25.5"}` (NO `static` keyword — RISK-01)
    - `ruijie_port_binding_add` (with MAC) subtest asserts result contains `switchport port-security binding aabb.ccdd.eeff 10.62.25.5` (Ruijie MAC format = cisco-style lowercase dotted)
    - `ruijie_port_binding_add_no_mac` (IP only) subtest asserts result equals `[]string{"interface GigabitEthernet0/0/1", "switchport port-security binding 10.62.25.5"}` (no MAC, IP-only binding — matches v1.19 design doc §3.2 Ruijie binding shape)
    - `huawei_port_binding_remove` (with MAC) subtest asserts result equals `[]string{"interface GE0/0/1", "undo user-bind static ip-address 10.62.25.5 mac-address AABB-CCDD-EEFF"}` (`undo` prefix + `static` keyword)
    - `h3c_port_binding_remove` subtest asserts result equals `[]string{"interface GE0/0/1", "undo user-bind ip-address 10.62.25.5"}` (no `static` — RISK-01)
    - `ruijie_port_binding_remove` (with MAC) subtest asserts result contains `no switchport port-security binding aabb.ccdd.eeff 10.62.25.5` (`no` prefix for Cisco-style)
    - `go test ./internal/services/portcollection/... -run TestRenderCommand -v -count=1` exits 0 with all 29+ subtests (17 existing + 12 new) passing
    - `go vet ./internal/services/portcollection/...` exits 0
  </acceptance_criteria>
  <action>
    Append to existing file `internal/services/portcollection/vendor_port_template_test.go` (do NOT overwrite; preserve the 6 v1.19 test functions).

    1) Open the file; locate the `TestRenderCommand_VendorActionMatrix` function and its `tests := []struct{...}{...}` literal. Append 12 new rows to the `tests` slice, one per (vendor, action, variant) combination. Use the existing struct shape: `{ name string; vendor models.DeviceVendor; action PortAction; params PortTemplateParams; expected []string }`.

    2) New test rows to add (each runs as a subtest via `t.Run(tt.name, ...)`):

       - `{ name: "huawei_set_access_vlan", vendor: models.VendorHuawei, action: ActionSetAccessVLAN, params: PortTemplateParams{InterfaceName: "GE0/0/1", VLANID: 100}, expected: []string{"interface GE0/0/1", "port link-type access", "port default vlan 100"} }`
       - `{ name: "h3c_set_access_vlan", vendor: models.VendorH3C, action: ActionSetAccessVLAN, params: PortTemplateParams{InterfaceName: "GE0/0/1", VLANID: 100}, expected: []string{"interface GE0/0/1", "port link-type access", "port access vlan 100"} }`
       - `{ name: "ruijie_set_access_vlan", vendor: models.VendorRuijie, action: ActionSetAccessVLAN, params: PortTemplateParams{InterfaceName: "GigabitEthernet0/0/1", VLANID: 100}, expected: []string{"interface GigabitEthernet0/0/1", "switchport mode access", "switchport access vlan 100"} }`
       - `{ name: "huawei_port_binding_add", vendor: models.VendorHuawei, action: ActionPortBinding, params: PortTemplateParams{InterfaceName: "GE0/0/1", BindOp: "add", IPAddress: "10.62.25.5"}, expected: []string{"interface GE0/0/1", "user-bind static ip-address 10.62.25.5"} }`
       - `{ name: "huawei_port_binding_add_with_mac", vendor: models.VendorHuawei, action: ActionPortBinding, params: PortTemplateParams{InterfaceName: "GE0/0/1", BindOp: "add", IPAddress: "10.62.25.5", MACAddress: "AA:BB:CC:DD:EE:FF"}, expected: []string{"interface GE0/0/1", "user-bind static ip-address 10.62.25.5 mac-address AABB-CCDD-EEFF"} }`
       - `{ name: "huawei_port_binding_remove", vendor: models.VendorHuawei, action: ActionPortBinding, params: PortTemplateParams{InterfaceName: "GE0/0/1", BindOp: "remove", IPAddress: "10.62.25.5", MACAddress: "AA:BB:CC:DD:EE:FF"}, expected: []string{"interface GE0/0/1", "undo user-bind static ip-address 10.62.25.5 mac-address AABB-CCDD-EEFF"} }`
       - `{ name: "h3c_port_binding_add", vendor: models.VendorH3C, action: ActionPortBinding, params: PortTemplateParams{InterfaceName: "GE0/0/1", BindOp: "add", IPAddress: "10.62.25.5"}, expected: []string{"interface GE0/0/1", "user-bind ip-address 10.62.25.5"} }` (note: NO `static`)
       - `{ name: "h3c_port_binding_remove", vendor: models.VendorH3C, action: ActionPortBinding, params: PortTemplateParams{InterfaceName: "GE0/0/1", BindOp: "remove", IPAddress: "10.62.25.5"}, expected: []string{"interface GE0/0/1", "undo user-bind ip-address 10.62.25.5"} }` (note: NO `static`)
       - `{ name: "ruijie_port_binding_add", vendor: models.VendorRuijie, action: ActionPortBinding, params: PortTemplateParams{InterfaceName: "GigabitEthernet0/0/1", BindOp: "add", IPAddress: "10.62.25.5", MACAddress: "AA:BB:CC:DD:EE:FF"}, expected: []string{"interface GigabitEthernet0/0/1", "switchport port-security binding aabb.ccdd.eeff 10.62.25.5"} }` (Ruijie MAC = lowercase dotted)
       - `{ name: "ruijie_port_binding_add_no_mac", vendor: models.VendorRuijie, action: ActionPortBinding, params: PortTemplateParams{InterfaceName: "GigabitEthernet0/0/1", BindOp: "add", IPAddress: "10.62.25.5"}, expected: []string{"interface GigabitEthernet0/0/1", "switchport port-security binding 10.62.25.5"} }` (IP-only binding, no MAC)
       - `{ name: "ruijie_port_binding_remove", vendor: models.VendorRuijie, action: ActionPortBinding, params: PortTemplateParams{InterfaceName: "GigabitEthernet0/0/1", BindOp: "remove", IPAddress: "10.62.25.5", MACAddress: "AA:BB:CC:DD:EE:FF"}, expected: []string{"interface GigabitEthernet0/0/1", "no switchport port-security binding aabb.ccdd.eeff 10.62.25.5"} }`

    3) Optional additional case: a H3C with MAC remove variant for completeness:
       - `{ name: "h3c_port_binding_remove_with_mac", vendor: models.VendorH3C, action: ActionPortBinding, params: PortTemplateParams{InterfaceName: "GE0/0/1", BindOp: "remove", IPAddress: "10.62.25.5", MACAddress: "AA:BB:CC:DD:EE:FF"}, expected: []string{"interface GE0/0/1", "undo user-bind ip-address 10.62.25.5 mac-address AABB-CCDD-EEFF"} }`

    4) Do NOT add new standalone test functions (e.g., `TestRenderCommand_VlanOutOfRange`) — the renderer-level validation is defensive and may be deferred entirely to the service layer (W2). The 12+ table rows above are the complete coverage. The service layer adds the canonical 400-sentinel-error test in W2.

    5) The existing 6 v1.19 test functions stay untouched. The `t.Run` loop body asserts `assert.NoError(t, err); assert.Equal(t, tt.expected, got)` — the same logic that runs for the existing 17 rows also runs for the 12 new rows.

    6) Run the verification command — must exit 0 with 29+ subtests passing (17 existing + 12 new). Use `-count=1` to defeat any test caching.

    7) Spot-check: grep the test file for `ActionSetAccessVLAN` and `ActionPortBinding` references — must appear in 12+ row entries (some are repeated for add/remove variants).

    AVOID: skipping the `huawei_port_binding_add_with_mac` row — it's the load-bearing case for Pitfall 7 (MAC format conversion to `AABB-CCDD-EEFF`). AVOID: writing the H3C `static` keyword in any test row (would break the RISK-01 invariant). AVOID: writing a row that asserts `port default vlan` for H3C (would break the vendor keyword divergence).
  </action>
  <verify>
    <automated>cd "D:/code/ClaudeCode/xingran-go-backend" && go test ./internal/services/portcollection/... -run TestRenderCommand -v -count=1 -timeout 60s 2>&1 | tail -50</automated>
  </verify>
  <done>
    `internal/services/portcollection/vendor_port_template_test.go` extended with 12+ new table rows in `TestRenderCommand_VendorActionMatrix`. All 29+ subtests (17 existing + 12 new) pass with `go test ./internal/services/portcollection/... -run TestRenderCommand -v -count=1` exiting 0. Each (vendor, action, variant) combination is locked by an exact-`assert.Equal` on the rendered `[]string` — vendor syntax drift will break at least one subtest deterministically.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build + full test suite gate — Phase 56 W1 acceptance</name>
  <files></files>
  <read_first>
    - D:\code\ClaudeCode\xingran-go-backend\internal\services\portcollection\vendor_port_template.go (Task 1 output — final shape)
    - D:\code\ClaudeCode\xingran-go-backend\internal\services\portcollection\vendor_port_template_test.go (Task 2 output — final shape)
    - D:\code\ClaudeCode\xingran-go-backend\.planning\phases\56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09\56-VALIDATION.md (Wave 1 acceptance criteria reference)
  </read_first>
  <acceptance_criteria>
    - `go build ./...` exits 0 (no cross-package regression; Phase 50 + 51 + 52 + 53 + 54 packages all compile)
    - `go vet ./internal/services/portcollection/...` exits 0
    - `go test ./internal/services/portcollection/... -v -count=1` exits 0 with `TestRenderCommand_VendorActionMatrix` reporting ≥ 29 subtests passing
    - The 17 existing v1.19 matrix subtests still pass (zero regression)
    - 12 new subtests pass covering: `huawei_set_access_vlan`, `h3c_set_access_vlan`, `ruijie_set_access_vlan`, `huawei_port_binding_add`, `huawei_port_binding_add_with_mac`, `huawei_port_binding_remove`, `h3c_port_binding_add`, `h3c_port_binding_remove`, `h3c_port_binding_remove_with_mac`, `ruijie_port_binding_add`, `ruijie_port_binding_add_no_mac`, `ruijie_port_binding_remove`
    - 5 v1.19 negative-case test functions (`TestRenderCommand_EmptyInterfaceName`, `TestRenderCommand_UnsupportedVendor`, `TestRenderCommand_UnknownAction`, `TestRenderCommand_DescriptionEmpty`, `TestRenderCommand_DescriptionTooLong`) still pass
    - `models.VendorMaipu` is NOT a map key in the new `vendorPortTemplate` (grep returns 0)
    - `RenderCommand` function signature is UNCHANGED: `func RenderCommand(vendor models.DeviceVendor, action PortAction, params PortTemplateParams) ([]string, error)`
  </acceptance_criteria>
  <action>
    Run the full Phase 56 W1 acceptance gate from the project root (this task makes NO file modifications — it is the acceptance gate):

    1. `go build ./...` — must exit 0 to confirm the 2 new PortAction constants + 4 new struct fields + 9 new renderers + 6 new map entries do not break transitive compilation across the whole project.

    2. `go vet ./internal/services/portcollection/...` — must exit 0 to confirm no shadow variables / unused imports / suspicious constructs.

    3. `go test ./internal/services/portcollection/... -v -count=1` — must exit 0 with all `TestRenderCommand_*` subtests passing. Capture the `--- PASS` lines for verification:
       - The matrix test should report 29+ subtests (15 v1.19 base + 2 negative + 12 new = 29 minimum; could be 30+ if H3C with-mac-remove variant is added).
       - All 5 negative-case test functions must still pass (EmptyInterfaceName, UnsupportedVendor, UnknownAction, DescriptionEmpty, DescriptionTooLong).

    4. Spot-check: `grep -c "models.VendorMaipu" internal/services/portcollection/vendor_port_template.go` should return 0 (Maipu still out of scope).

    5. Spot-check: `grep -c "ActionSetAccessVLAN\|ActionPortBinding" internal/services/portcollection/vendor_port_template.go` should return ≥ 8 (2 const declarations + 6 map key references = 8 minimum; possibly more if used in comments).

    6. Spot-check: `grep -c "renderHuaweiSetAccessVlan\|renderH3CSetAccessVlan\|renderRuijieSetAccessVlan\|renderHuaweiPortBindingAdd\|renderH3CPortBindingAdd\|renderRuijiePortBindingAdd\|renderHuaweiPortBindingRemove\|renderH3CPortBindingRemove\|renderRuijiePortBindingRemove" internal/services/portcollection/vendor_port_template.go` should return ≥ 9 (9 distinct render function names; some may appear 2x if declared + referenced in dispatch closure).

    7. If any step fails: do NOT mark this task done. Read the failure output, identify which file/function broke, fix it, and rerun the commands before declaring complete.

    Do NOT commit changes in this task — that is handled by the orchestrator after SUMMARY is written.
  </action>
  <verify>
    <automated>cd "D:/code/ClaudeCode/xingran-go-backend" && go build ./... && go vet ./internal/services/portcollection/... && go test ./internal/services/portcollection/... -v -count=1 -timeout 60s 2>&1 | tail -60</automated>
  </verify>
  <done>
    Phase 56 W1 acceptance gate passes: full repo builds + new package vets clean + all `TestRenderCommand_*` subtests green (17 existing + 12 new = 29+ subtests). Vendor template contract for `set_access_vlan` + `port_binding` × 3 vendors is locked at the rendering layer; service layer (W2) can now consume the new actions via the unchanged `RenderCommand` entry point.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Same-package test to production code | `vendor_port_template_test.go` and `vendor_port_template.go` share package `portcollection` — tests invoke exported `RenderCommand` and read package-level sentinel errors. White-box test, fully trusted. |
| Phase 56 W2 service to Phase 56 W1 templates | `internal/services/portwrite/port_write_service.go` (W2) will call `RenderCommand` with new `ActionSetAccessVLAN` / `ActionPortBinding` + new `PortTemplateParams` fields (`VLANID` / `BindOp` / `IPAddress` / `MACAddress`). Caller-supplied values flow in; renderer returns the formatted `[]string`. Untrusted caller (service) supplies validated input (service layer is the source of truth for IP / MAC / VLAN validation); renderer's defensive checks are belt-and-suspenders. |
| Renderer to vendor-specific MAC format | `renderHuaweiPortBindingAdd` and `renderH3CPortBindingAdd` call `toHuaweiH3CMACFormat` which calls `services.NormalizeMACAddress` + `strings.ReplaceAll(":", "-")`. `renderRuijiePortBindingAdd` and `renderRuijiePortBindingRemove` apply Cisco-style lowercase dotted format. Untrusted MAC string from caller; trusted library transforms via deterministic algorithm. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-W1-01 | Tampering | IP / MAC string embedded into CLI command | accept | Phase 56 W1 transmits `IPAddress` / `MACAddress` as-is into the command body. Service layer (W2) validates with `ipv4Pattern` / canonical MAC regex BEFORE calling `RenderCommand`; renderer is a dumb formatter. SSH transport is text-mode against the device CLI; device parser is the final authority on injection. Phase 56 W1 deliberately does not add a render-time regex check (would duplicate service validation). |
| T-56-W1-02 | Information Disclosure | Sentinel error messages expose vendor + action in `Errorf` wrapping | accept | Existing v1.19 pattern. New actions can return inline `fmt.Errorf` for defensive vlanId range check (not a sentinel). No PII or secrets exposed. |
| T-56-W1-03 | Denial of Service | VlanId 0 / 4095 / 99999 silently accepted by renderer | mitigate | Each `render*SetAccessVlan` function checks `p.VLANID < 1 \|\| p.VLANID > 4094` and returns inline `fmt.Errorf`. Defensive guard only — service layer (W2) `SetAccessVlan` method is the canonical 400-rejection point via `ErrVlanIdOutOfRange` sentinel. |
| T-56-W1-04 | Elevation of Privilege | `BindOp` value reaching dispatcher | mitigate | Dispatch closure checks `p.BindOp == "add"` and routes to the correct renderer. Any other value (e.g., "delete", "modify") falls through to the remove renderer (safer default — produces `undo ...` which fails on device if no binding exists, but does not silently grant a privileged action). Service layer (W2) `PortBinding` method validates `BindOp ∈ {"add", "remove"}` with `ErrBindOpInvalid` sentinel — canonical 400-rejection point. |
| T-56-W1-05 | Tampering | MAC format divergence: Huawei/H3C expect hyphen, Ruijie expects cisco-style | mitigate | Three independent renderer paths each format MAC correctly: `toHuaweiH3CMACFormat` (hyphen) for Huawei/H3C; `strings.ReplaceAll(strings.ToLower(...), ":", ".")` for Ruijie. 12+ table rows in Task 2 lock the exact expected output for each (vendor, with-MAC, no-MAC) variant. A regression in format conversion breaks at least one subtest deterministically. |
| T-56-W1-06 | Repudiation | Test absence hides syntax drift | mitigate | 12+ new table-driven subtests assert exact command sequences; vendor syntax changes break at least one subtest deterministically. Per STATE.md Pitfall #3 mitigation. |
| T-56-W1-SC | Tampering | npm/pip/cargo installs | n/a | Phase 56 W1 adds zero new external packages. All work uses existing v1.19 dependencies (`github.com/xingran-next/xingran-go-backend/internal/services` for `NormalizeMACAddress` is a project-internal package, no install required). |

## Notes

- No SSH, no auth, no network I/O in this phase. Threat surface is limited to in-process type / value handling.
- Phase 56 W2 (PortWriteService) and W3 (handler) will inherit richer threats (credential exposure, real-device rejection); those are covered in their respective plans.
- The renderer's defensive VLAN range check (T-56-W1-03) is intentionally weaker than the service layer's sentinel — the renderer allows defensive rejection at the formatter boundary, but the canonical HTTP 400 rejection point is the service layer's `ErrVlanIdOutOfRange` sentinel via `errors.Is` translation in W3 handler.
</threat_model>

<verification>
1. `go build ./...` exits 0 (full project compiles).
2. `go vet ./internal/services/portcollection/...` exits 0 (no lint regressions).
3. `go test ./internal/services/portcollection/... -run TestRenderCommand -v -count=1` exits 0 with 29+ subtests passing (17 existing v1.19 + 12 new v1.20.1).
4. `internal/services/portcollection/vendor_port_template.go` contains the literal strings: `ActionSetAccessVLAN`, `ActionPortBinding`, `VLANID`, `BindOp`, `IPAddress`, `MACAddress`, all 9 `render*` function names, and the 3 `dispatch*` closure references.
5. `internal/services/portcollection/vendor_port_template_test.go` contains 12+ new table rows in `TestRenderCommand_VendorActionMatrix` covering each (vendor, action, variant) combination.
6. The H3C rows do NOT contain the `static` keyword in any expected `user-bind` substring (RISK-01 invariant).
7. The Huawei rows DO contain the `static` keyword in the expected `user-bind static` substring.
8. The Ruijie rows format MAC as lowercase cisco-style `aabb.ccdd.eeff` (4 chars dot 4 chars dot 4 chars).
9. The 6 v1.19 test functions (`TestRenderCommand_VendorActionMatrix` extended, `TestRenderCommand_EmptyInterfaceName`, `TestRenderCommand_UnsupportedVendor`, `TestRenderCommand_UnknownAction`, `TestRenderCommand_DescriptionEmpty`, `TestRenderCommand_DescriptionTooLong`) all still pass.
10. `models.VendorMaipu` is NOT a map key in `vendorPortTemplate` (D-01 scope exclusion preserved).
</verification>

<success_criteria>
1. 3 vendors x 7 actions = 21 templates implemented in `internal/services/portcollection/vendor_port_template.go` (Huawei / H3C / Ruijie x shutdown / undo_shutdown / description / dot1x_enable / dot1x_disable / set_access_vlan / port_binding). (ROADMAP SC #1)
2. 12+ new unit tests in `vendor_port_template_test.go` covering each (vendor, action) combination for the 2 new actions plus add/remove + with/without MAC variants (29+ total subtests when combined with v1.19). (ROADMAP SC #2, TEST-01)
3. The 2 new `PortAction` constants (`ActionSetAccessVLAN` / `ActionPortBinding`) are typed as `portcollection.PortAction` (string alias); `PortTemplateParams` extended with 4 new fields (`VLANID`, `BindOp`, `IPAddress`, `MACAddress`) without breaking existing 2 fields. (extends SC #3)
4. H3C uses `user-bind` (no `static`) and `port access vlan` (not `port default vlan`); Huawei uses `user-bind static` and `port default vlan`; Ruijie uses Cisco-style `switchport mode access` / `switchport access vlan` / `switchport port-security binding`. (SC #4, RISK-01/02/03)
5. `go test ./internal/services/portcollection/...` exits 0 with all 29+ subtests passing. (SC #5)
6. `go build ./...` exits 0 (zero cross-package regression vs v1.19 phase closure).
7. Zero new external dependencies (no `go.mod` diff).
8. Plan file is committed to git with message `docs(phase-56): create vendor template plan`.
</success_criteria>

<output>
Create `.planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-01-SUMMARY.md` when done.
</output>
