---
phase: 56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09
plan: 05
type: execute
wave: 5
depends_on:
  - 56-04
files_modified:
  - internal/services/portwrite/port_write_e2e_test.go
  - internal/services/portwrite/testdata/huawei_set_access_vlan_success.fixture
  - internal/services/portwrite/testdata/ruijie_set_access_vlan_success.fixture
  - internal/services/portwrite/testdata/huawei_port_binding_add_success.fixture
  - internal/services/portwrite/testdata/huawei_port_binding_add_with_mac_success.fixture
  - internal/services/portwrite/testdata/huawei_port_binding_remove_success.fixture
  - internal/services/portwrite/testdata/ruijie_port_binding_add_success.fixture
  - .planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-HUMAN-UAT.md
  - docs/API响应规范.md
  - CHANGELOG.md
  - .planning/MILESTONES.md
autonomous: true
requirements:
  - TEST-03
  - TEST-04
  - TEST-05
user_setup: []

must_haves:
  truths:
    - "Phase 56 W5 e2e tests close the gap left by Phase 54 W5: parseConfigError + RenderCommand + executeWrite for the 2 new actions now run against real scrapligo FileTransport (not mock bypass)"
    - "10+ new TestE2E_* tests for set_access_vlan + port_binding pass with `go test ./internal/services/portwrite/... -count=1`"
    - "6+ new fixture files in `internal/services/portwrite/testdata/` replay real vendor byte streams for the 2 new actions (Huawei + Ruijie coverage)"
    - "56-HUMAN-UAT.md created with 6+ site-visit SSH verification items (Huawei S8700 + Ruijie RS8607E + H3C; owner = 现场运维同事; verifier_status: human_needed)"
    - "API响应规范文档包含 v1.20.1 网络设备端口写操作扩展小节 (2 端点签名 + SetAccessVlanRequest/PortBindingRequest schema)"
    - "CHANGELOG.md v1.20.1 entry created (follow v1.19 格式)"
    - "MILESTONES.md 含 v1.20.1 条目 (follow v1.19 格式)"
    - "operlog regression_test.go 零变动 (Phase 34 锁保持)"
    - "vendor-react bundle gzip 仍 ≤ 776 kB baseline (无新依赖)"
  artifacts:
    - path: "internal/services/portwrite/port_write_e2e_test.go"
      provides: "10+ new TestE2E_* tests for set_access_vlan + port_binding (Huawei + Ruijie)"
      min_lines: 900
      must_contain:
        - "TestE2E_SetAccessVlan_Huawei_Success"
        - "TestE2E_SetAccessVlan_Ruijie_Success"
        - "TestE2E_PortBinding_Huawei_Add_Success"
        - "TestE2E_PortBinding_Huawei_Add_WithMac_Success"
        - "TestE2E_PortBinding_Huawei_Remove_Success"
        - "TestE2E_PortBinding_Ruijie_Add_Success"
    - path: "internal/services/portwrite/testdata/huawei_set_access_vlan_success.fixture"
      provides: "Huawei VRP set_access_vlan byte stream (3 commands + exit)"
    - path: ".planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-HUMAN-UAT.md"
      provides: "6+ site-visit SSH verification items for Huawei S8700 + Ruijie RS8607E + H3C"
      min_lines: 80
      must_contain: "verifier_status: human_needed"
    - path: "docs/API响应规范.md"
      provides: "网络设备端口写操作 v1.20.1 扩展小节"
      min_lines: 30
    - path: "CHANGELOG.md"
      provides: "v1.20.1 entry"
      min_lines: 10
  key_links:
    - from: "internal/services/portwrite/port_write_e2e_test.go"
      to: "internal/services/portwrite/testdata/*.fixture"
      via: "scrapligo FileTransport reuses v1.19 fileTransportExecutor + new fixtures for the 2 new actions"
      pattern: "e2eFixturePath\\(t, \"(huawei|ruijie)_(set_access_vlan|port_binding)_"
    - from: ".planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-HUMAN-UAT.md"
      to: ".planning/phases/54-w5-e2e-real-device-uat-documentation/54-HUMAN-UAT.md"
      via: "Site-visit UAT deferral follows v1.19 Phase 54 precedent (mock SSH e2e in W5; real-device in next site visit)"
      pattern: "verifier_status: human_needed"
    - from: "docs/API响应规范.md"
      to: "internal/api/v1/network/port_write_handler.go"
      via: "Documents the 2 new endpoint contracts (POST /network/ports/write/{set-access-vlan|port-binding})"
      pattern: "set-access-vlan|port-binding"

---

## Phase Goal

**As a** Phase 56 milestone closer, **I want to** ship 10+ e2e tests + 6+ fixtures + 56-HUMAN-UAT.md + documentation sync (API 规范 / CHANGELOG / MILESTONES) for the 2 new v1.20.1 write operations, **so that** the v1.20.1 milestone closes with full evidence (mock SSH e2e in CI + site-visit deferral pattern from v1.19) and zero documentation drift across the project.

---

<objective>
Close the v1.20.1 milestone by adding 10+ scrapligo FileTransport e2e tests + 6+ new test fixtures + the 56-HUMAN-UAT.md deferral document + 3 documentation files (API响应规范 / CHANGELOG / MILESTONES). Reuse the v1.19 Phase 54 W5 e2e infrastructure (fileTransportExecutor / e2eFixturePath / newTestService / mockCollectionSvc) without modification. Real-device SSH verification follows the v1.19 Phase 54 deferral pattern — site-visit by 现场运维同事.

Purpose: Lock the v1.20.1 milestone with the same evidence pattern as v1.19 (mock SSH e2e proves the integration chain; site-visit UAT deferred until next on-site visit). Documentation sync ensures the new 2 endpoints are discoverable in the project docs and changelog.

Output: 1 e2e test file + 6 fixture files + 1 HUMAN-UAT file + 3 documentation files. All 10+ new e2e tests pass; all v1.19 e2e tests still pass; operlog regression intact; vendor-react bundle ≤ 776 kB baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-RESEARCH.md
@.planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-PATTERNS.md
@.planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-UI-SPEC.md
@.planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-VALIDATION.md
@.planning/phases/54-w5-e2e-real-device-uat-documentation/54-01-SUMMARY.md
@.planning/phases/54-w5-e2e-real-device-uat-documentation/54-HUMAN-UAT.md
@.planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-01-SUMMARY.md
@.planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-02-SUMMARY.md
@.planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-03-SUMMARY.md
@.planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-04-SUMMARY.md

# v1.19 e2e test file (extending baseline)
@internal/services/portwrite/port_write_e2e_test.go

# v1.19 e2e fixtures (analog format)
@internal/services/portwrite/testdata/huawei_shutdown_success.fixture

# v1.19 port_write_service_test.go (mock helpers + fileTransportExecutor pattern)
@internal/services/portwrite/port_write_service_test.go

# v1.19 port_write_service.go (W2 output — SetAccessVlan + PortBinding methods)
@internal/services/portwrite/port_write_service.go

# v1.19 HUMAN-UAT.md template (deferral pattern)
@.planning/phases/54-w5-e2e-real-device-uat-documentation/54-HUMAN-UAT.md

# Documentation files to update
@docs/API响应规范.md
@CHANGELOG.md
@.planning/MILESTONES.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From internal/services/portwrite/port_write_e2e_test.go (v1.19 baseline, file being extended; 579 lines):
```go
// fileTransportExecutor — production-DeviceExecutor stand-in using scrapligo FileTransport
type fileTransportExecutor struct {
    fixtureProvider func(callIdx int) string
    counter         atomic.Uint64
}
func (e *fileTransportExecutor) ExecuteCustom(ctx, deviceID, fn, timeout) error
func singleFixture(path string) func(int) string
func e2eFixturePath(t *testing.T, name string) string

// Helpers from port_write_service_test.go (same package, accessible):
// newTestDB(t) — opens sqlite in-memory
// seedPortAndDevice(t, db, portID, deviceID, interfaceName, adminStatus, dot1xEnabled, description)
// newTestService(exec, coll, db) — returns *portWriteServiceImpl
// mockCollectionSvc — implements portWritePortCollectionSvc.CollectDevice
// newSuccessCollectionResult(deviceID) — returns *portcollection.CollectionResult
// waitCollectedCalls(t, count) — waits for async CollectDevice goroutine
// resetCollectNotif() — resets the collect notif channel
```

From internal/services/portwrite/port_write_e2e_test.go (v1.19 — TestE2E_Shutdown_Huawei_HappyPath pattern, lines 125-163):
```go
func TestE2E_Shutdown_Huawei_HappyPath(t *testing.T) {
    resetCollectNotif()
    db := newTestDB(t)
    seedPortAndDevice(t, db, "port-1", "device-1", "GE0/0/1", "up", false, "")
    exec := &fileTransportExecutor{
        fixtureProvider: singleFixture(e2eFixturePath(t, "huawei_shutdown_success.fixture")),
    }
    mockColl := new(mockCollectionSvc)
    svc := newTestService(exec, mockColl, db)
    ctx := context.Background()
    mockColl.On("CollectDevice", mock.Anything, "device-1").Return(newSuccessCollectionResult("device-1"), nil)
    result, err := svc.Shutdown(ctx, "port-1", "e2e-op")
    // assertions: status="succeeded", NoOp=false, CommandSent contains "shutdown"
    waitCollectedCalls(t, 1)
    mockColl.AssertExpectations(t)
}
```

From internal/services/portwrite/port_write_service.go (W2 output — 2 new service methods):
```go
func (s *portWriteServiceImpl) SetAccessVlan(ctx context.Context, portID string, vlanId int, operator string) (*PortResult, error)
func (s *portWriteServiceImpl) PortBinding(ctx context.Context, portID string, op string, ipAddress string, macAddress string, operator string) (*PortResult, error)
```

From internal/services/portwrite/testdata/huawei_shutdown_success.fixture (v1.19 baseline fixture, 15 lines):
```
<Huawei>
screen-length 0 temporary
<Huawei>
system-view
[Huawei]
interface GE0/0/1
[Huawei-GigabitEthernet0/0/1]
[Huawei-GigabitEthernet0/0/1]
shutdown
[Huawei-GigabitEthernet0/0/1]
[Huawei-GigabitEthernet0/0/1]
quit
[Huawei]
return
<Huawei>
```
Pattern: <Huawei> prompt = top-level user EXEC mode; [Huawei] = privileged/system mode; [Huawei-GigabitEthernet0/0/1] = interface view. Each command's response is followed by the new prompt.

From .planning/phases/54-w5-e2e-real-device-uat-documentation/54-HUMAN-UAT.md (deferral pattern reference):
- Header: "UAT 推迟到下次现场访问" with `verifier_status: human_needed`
- Per-action sections: "验证项目 / 预期结果 / owner / next site visit"
- Mock-e2e vs site-visit split clearly documented

From docs/plans/2026-07-09-v1.20.1-design.md (line 222-237 — API 端点):
- POST /network/ports/write/set-access-vlan — body: { portId, vlanId, reason }
- POST /network/ports/write/port-binding — body: { portId, op, ipAddress, macAddress?, reason }

From .planning/MILESTONES.md (v1.19 format reference):
```
## v1.19 网络设备写命令 (Network Device Port Write Operations) — SHIPPED 2026-07-08
... (3 lines: milestone goal + Phases 50-55 + audit)
```

From CHANGELOG.md (v1.19 entry format reference):
```
## v1.19 — 2026-07-08
**Milestone**: 网络设备写命令 (Network Device Port Write Operations)
**Phases**: 50-55 (6 phases, 9 plans, 5 build waves + 1 cleanup)
**Key deliverables**:
- 3 vendors × 5 actions × 6 HTTP endpoints + batch
- operlog + audit + permission + PortWriteAudit 表
- 前端 PortWriteModal + BulkWriteDrawer + ports 操作列
- mock e2e (10+ TestE2E_*) + site-visit deferral
```

From docs/API响应规范.md (existing v1.19 port-write section — to be extended):
- Existing section covers the 6 v1.19 port-write endpoints
- New section adds the 2 v1.20.1 endpoints
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create 6+ e2e fixture files — Huawei/Ruijie set_access_vlan + port_binding byte streams</name>
  <files>internal/services/portwrite/testdata/*.fixture</files>
  <read_first>
    - D:\code\ClaudeCode\xingran-go-backend\internal\services\portwrite\testdata\huawei_shutdown_success.fixture (v1.19 baseline — 15 lines, exact format reference)
    - D:\code\ClaudeCode\xingran-go-backend\internal\services\portwrite\testdata\huawei_undo_shutdown_success.fixture (15 lines, similar structure)
    - D:\code\ClaudeCode\xingran-go-backend\internal\services\portwrite\testdata\huawei_description_success.fixture (2-command interface + description pattern)
    - D:\code\ClaudeCode\xingran-go-backend\.planning\phases\56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09\56-RESEARCH.md (Code Examples section — locked vendor command sequences for fixtures)
    - D:\code\ClaudeCode\xingran-go-backend\docs\plans\2026-07-09-v1.20.1-design.md (line 60-90 — locked command sequences)
  </read_first>
  <acceptance_criteria>
    - 6+ new fixture files exist in `internal/services/portwrite/testdata/`:
      - `huawei_set_access_vlan_success.fixture` (3 commands: interface / port link-type access / port default vlan 100 + exit)
      - `ruijie_set_access_vlan_success.fixture` (3 commands: interface / switchport mode access / switchport access vlan 100 + exit)
      - `huawei_port_binding_add_success.fixture` (2 commands: interface / user-bind static ip-address 10.62.25.5 + exit)
      - `huawei_port_binding_add_with_mac_success.fixture` (2 commands: interface / user-bind static ip-address 10.62.25.5 mac-address AABB-CCDD-EEFF + exit)
      - `huawei_port_binding_remove_success.fixture` (2 commands: interface / undo user-bind static ip-address 10.62.25.5 + exit)
      - `ruijie_port_binding_add_success.fixture` (2 commands: interface / switchport port-security binding aabb.ccdd.eeff 10.62.25.5 + exit)
    - Each fixture follows the v1.19 format: top `<Huawei>` / `<Ruijie>` prompt, `screen-length 0 temporary`, then per-command `[prompt] cmd [next-prompt]` sequence
    - Each fixture ends with a `quit` (Huawei) or `exit` (Ruijie) cleanup command to exit the interface view
    - Each fixture ends with `return` and top-level prompt
    - Total fixture file size: 15-25 lines per file (matches v1.19 baseline)
  </acceptance_criteria>
  <action>
    Create 6 new fixture files in `internal/services/portwrite/testdata/` (file extension `.fixture` is the v1.19 convention; the file extension doesn't matter to scrapligo FileTransport — it just reads the file as a byte stream).

    1) `huawei_set_access_vlan_success.fixture`:
       ```
       <Huawei>
       screen-length 0 temporary
       <Huawei>
       system-view
       [Huawei]
       interface GE0/0/1
       [Huawei-GigabitEthernet0/0/1]
       [Huawei-GigabitEthernet0/0/1]
       port link-type access
       [Huawei-GigabitEthernet0/0/1]
       [Huawei-GigabitEthernet0/0/1]
       port default vlan 100
       [Huawei-GigabitEthernet0/0/1]
       [Huawei-GigabitEthernet0/0/1]
       quit
       [Huawei]
       return
       <Huawei>
       ```
       (3 commands: interface + port link-type access + port default vlan 100)

    2) `ruijie_set_access_vlan_success.fixture`:
       ```
       <Ruijie>
       screen-length 0 temporary
       <Ruijie>
       system-view
       [Ruijie]
       interface GigabitEthernet 1/0/1
       [Ruijie-GigabitEthernet 1/0/1]
       [Ruijie-GigabitEthernet 1/0/1]
       switchport mode access
       [Ruijie-GigabitEthernet 1/0/1]
       [Ruijie-GigabitEthernet 1/0/1]
       switchport access vlan 100
       [Ruijie-GigabitEthernet 1/0/1]
       [Ruijie-GigabitEthernet 1/0/1]
       exit
       [Ruijie]
       return
       <Ruijie>
       ```
       (Note: Ruijie uses Cisco-style `exit` not `quit`; interface name has space `GigabitEthernet 1/0/1` per Ruijie convention)

    3) `huawei_port_binding_add_success.fixture` (IP only, no MAC):
       ```
       <Huawei>
       screen-length 0 temporary
       <Huawei>
       system-view
       [Huawei]
       interface GE0/0/1
       [Huawei-GigabitEthernet0/0/1]
       [Huawei-GigabitEthernet0/0/1]
       user-bind static ip-address 10.62.25.5
       [Huawei-GigabitEthernet0/0/1]
       [Huawei-GigabitEthernet0/0/1]
       quit
       [Huawei]
       return
       <Huawei>
       ```

    4) `huawei_port_binding_add_with_mac_success.fixture`:
       ```
       <Huawei>
       screen-length 0 temporary
       <Huawei>
       system-view
       [Huawei]
       interface GE0/0/1
       [Huawei-GigabitEthernet0/0/1]
       [Huawei-GigabitEthernet0/0/1]
       user-bind static ip-address 10.62.25.5 mac-address AABB-CCDD-EEFF
       [Huawei-GigabitEthernet0/0/1]
       [Huawei-GigabitEthernet0/0/1]
       quit
       [Huawei]
       return
       <Huawei>
       ```

    5) `huawei_port_binding_remove_success.fixture`:
       ```
       <Huawei>
       screen-length 0 temporary
       <Huawei>
       system-view
       [Huawei]
       interface GE0/0/1
       [Huawei-GigabitEthernet0/0/1]
       [Huawei-GigabitEthernet0/0/1]
       undo user-bind static ip-address 10.62.25.5
       [Huawei-GigabitEthernet0/0/1]
       [Huawei-GigabitEthernet0/0/1]
       quit
       [Huawei]
       return
       <Huawei>
       ```

    6) `ruijie_port_binding_add_success.fixture`:
       ```
       <Ruijie>
       screen-length 0 temporary
       <Ruijie>
       system-view
       [Ruijie]
       interface GigabitEthernet 1/0/1
       [Ruijie-GigabitEthernet 1/0/1]
       [Ruijie-GigabitEthernet 1/0/1]
       switchport port-security binding aabb.ccdd.eeff 10.62.25.5
       [Ruijie-GigabitEthernet 1/0/1]
       [Ruijie-GigabitEthernet 1/0/1]
       exit
       [Ruijie]
       return
       <Ruijie>
       ```

    7) Optional: `huawei_port_binding_remove_with_mac_success.fixture` (if Task 2 adds a WithMac remove test):
       ```
       <Huawei>
       screen-length 0 temporary
       <Huawei>
       system-view
       [Huawei]
       interface GE0/0/1
       [Huawei-GigabitEthernet0/0/1]
       [Huawei-GigabitEthernet0/0/1]
       undo user-bind static ip-address 10.62.25.5 mac-address AABB-CCDD-EEFF
       [Huawei-GigabitEthernet0/0/1]
       [Huawei-GigabitEthernet0/0/1]
       quit
       [Huawei]
       return
       <Huawei>
       ```

    8) Optional: `ruijie_port_binding_remove_success.fixture` (if Task 2 adds a Ruijie remove test):
       ```
       <Ruijie>
       screen-length 0 temporary
       <Ruijie>
       system-view
       [Ruijie]
       interface GigabitEthernet 1/0/1
       [Ruijie-GigabitEthernet 1/0/1]
       [Ruijie-GigabitEthernet 1/0/1]
       no switchport port-security binding aabb.ccdd.eeff 10.62.25.5
       [Ruijie-GigabitEthernet 1/0/1]
       [Ruijie-GigabitEthernet 1/0/1]
       exit
       [Ruijie]
       return
       <Ruijie>
       ```

    9) Run `ls -la internal/services/portwrite/testdata/` to verify 6+ new files are created (v1.19 had 7 fixtures; should now have 13+ total).

    AVOID: using spaces or unusual whitespace in the fixtures (scrapligo FileTransport reads byte-for-byte; tab characters may confuse the prompt matcher). AVOID: omitting the trailing `return` (scrapligo platform requires priv escalation return to exit system-view to user EXEC). AVOID: forgetting the leading `<Huawei>` / `<Ruijie>` prompt + `screen-length 0 temporary` boilerplate (matches v1.19 pattern).
  </action>
  <verify>
    <automated>cd "D:/code/ClaudeCode/xingran-go-backend" && ls internal/services/portwrite/testdata/ | wc -l</automated>
  </verify>
  <done>
    6+ new fixture files created in `internal/services/portwrite/testdata/`: `huawei_set_access_vlan_success.fixture`, `ruijie_set_access_vlan_success.fixture`, `huawei_port_binding_add_success.fixture`, `huawei_port_binding_add_with_mac_success.fixture`, `huawei_port_binding_remove_success.fixture`, `ruijie_port_binding_add_success.fixture` (plus 1-2 optional variants for WithMac remove + Ruijie remove). Each follows the v1.19 fixture format (top-level prompt + per-command response sequence + trailing exit/quit + return). Total fixture count: 13+ (v1.19's 7 + 6-8 new).
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend port_write_e2e_test.go — 10+ new TestE2E_* tests for set_access_vlan + port_binding (Huawei + Ruijie)</name>
  <files>internal/services/portwrite/port_write_e2e_test.go</files>
  <read_first>
    - D:\code\ClaudeCode\xingran-go-backend\internal\services\portwrite\port_write_e2e_test.go (v1.19 baseline; 579 lines; fileTransportExecutor + singleFixture + e2eFixturePath helpers; TestE2E_Shutdown_Huawei_HappyPath pattern lines 125-163)
    - D:\code\ClaudeCode\xingran-go-backend\internal\services\portwrite\port_write_service.go (W2 output — SetAccessVlan + PortBinding methods)
    - D:\code\ClaudeCode\xingran-go-backend\internal\services\portwrite\testdata\huawei_shutdown_success.fixture (v1.19 baseline fixture, 15 lines)
    - D:\code\ClaudeCode\xingran-go-backend\.planning\phases\56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09\56-RESEARCH.md (line 281-290 — e2e test plan)
    - D:\code\ClaudeCode\xingran-go-backend\.planning\phases\56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09\56-VALIDATION.md (TEST-03 e2e test command)
  </read_first>
  <acceptance_criteria>
    - File `internal/services/portwrite/port_write_e2e_test.go` is extended (not replaced); v1.19 6+ tests still present
    - 10+ new TestE2E_* test functions added covering the 2 new actions × Huawei/Ruijie × variants:
      - TestE2E_SetAccessVlan_Huawei_Success
      - TestE2E_SetAccessVlan_Ruijie_Success
      - TestE2E_PortBinding_Huawei_Add_Success
      - TestE2E_PortBinding_Huawei_Add_WithMac_Success
      - TestE2E_PortBinding_Huawei_Remove_Success
      - TestE2E_PortBinding_Ruijie_Add_Success
      - TestE2E_PortBinding_Ruijie_Add_NoMac_Success
      - TestE2E_PortBinding_Ruijie_Remove_Success
      - (Optional) TestE2E_PortBinding_Huawei_Remove_NoOp (pre-state match — Pitfall 6 skip means this just succeeds without skip)
      - (Optional) TestE2E_Batch_SetAccessVlan_Skipped_All (port_binding batch path not supported in v1.20.1)
    - Each test uses the v1.19 fileTransportExecutor + e2eFixturePath + newTestService + mockCollectionSvc helpers (no new infrastructure)
    - Each test calls `waitCollectedCalls(t, 1)` + `mockColl.AssertExpectations(t)` (matches v1.19 pattern)
    - Each test asserts `result.Status == "succeeded"`, `result.NoOp == false`, `result.CommandSent` contains the expected vendor-specific command (e.g., "port default vlan 100" for Huawei set_access_vlan, "user-bind static ip-address 10.62.25.5" for Huawei port_binding add)
    - `go test ./internal/services/portwrite/... -run TestE2E_SetAccessVlan|TestE2E_PortBinding -count=1 -timeout 90s` exits 0 with all 10+ new subtests passing
    - `go test ./internal/services/portwrite/... -count=1` exits 0 (v1.19 6+ e2e tests + 10+ new = 16+ e2e tests, all green)
  </acceptance_criteria>
  <action>
    Append 10+ new test functions to `internal/services/portwrite/port_write_e2e_test.go` (do NOT overwrite the v1.19 tests; append after the last test function at the end of the file).

    For each new test, follow the v1.19 `TestE2E_Shutdown_Huawei_HappyPath` pattern:
    1. `resetCollectNotif()`
    2. `db := newTestDB(t)`
    3. `seedPortAndDevice(t, db, portID, deviceID, interfaceName, adminStatus, dot1xEnabled, description)` — vendor-specific interface name: Huawei = "GE0/0/1", Ruijie = "GigabitEthernet 1/0/1" (NOTE: per 56-RESEARCH `expandInterfaceName` auto-expands Huawei to short, Ruijie needs space). Use the FULL Ruijie interface name with space, NOT the short name.
    4. For Ruijie tests: `seedPortAndDevice` interfaceName must be `"GigabitEthernet 1/0/1"` (with space) to match the fixture prompt. For Huawei tests: `seedPortAndDevice` interfaceName must be `"GE0/0/1"` (short form, no expand needed per v1.19 expandInterfaceName).
    5. `exec := &fileTransportExecutor{ fixtureProvider: singleFixture(e2eFixturePath(t, "<fixture_name>.fixture")) }`
    6. `mockColl := new(mockCollectionSvc)`
    7. `svc := newTestService(exec, mockColl, db)`
    8. `mockColl.On("CollectDevice", mock.Anything, "device-1").Return(newSuccessCollectionResult("device-1"), nil)`
    9. Call the service method (e.g., `svc.SetAccessVlan(ctx, "port-1", 100, "e2e-op")` or `svc.PortBinding(ctx, "port-1", "add", "10.62.25.5", "AA:BB:CC:DD:EE:FF", "e2e-op")`)
    10. Assert `err == nil`, `result.Status == "succeeded"`, `result.NoOp == false`, `result.CommandSent` contains expected vendor command substring.
    11. `waitCollectedCalls(t, 1)` + `mockColl.AssertExpectations(t)`.

    Specific tests to add:

    1) `TestE2E_SetAccessVlan_Huawei_Success`:
       - Fixture: `huawei_set_access_vlan_success.fixture`
       - Call: `svc.SetAccessVlan(ctx, "port-1", 100, "e2e-op")`
       - Asserts: `CommandSent` contains `port default vlan 100` AND `port link-type access` (3-command sequence intact)

    2) `TestE2E_SetAccessVlan_Ruijie_Success`:
       - Fixture: `ruijie_set_access_vlan_success.fixture`
       - InterfaceName: `GigabitEthernet 1/0/1` (with space, Ruijie convention)
       - Call: `svc.SetAccessVlan(ctx, "port-1", 100, "e2e-op")`
       - Asserts: `CommandSent` contains `switchport access vlan 100` AND `switchport mode access`

    3) `TestE2E_PortBinding_Huawei_Add_Success`:
       - Fixture: `huawei_port_binding_add_success.fixture`
       - Call: `svc.PortBinding(ctx, "port-1", "add", "10.62.25.5", "", "e2e-op")` (no MAC)
       - Asserts: `CommandSent` contains `user-bind static ip-address 10.62.25.5` AND does NOT contain `mac-address` (no MAC in this test)

    4) `TestE2E_PortBinding_Huawei_Add_WithMac_Success`:
       - Fixture: `huawei_port_binding_add_with_mac_success.fixture`
       - Call: `svc.PortBinding(ctx, "port-1", "add", "10.62.25.5", "AA:BB:CC:DD:EE:FF", "e2e-op")`
       - Asserts: `CommandSent` contains `user-bind static ip-address 10.62.25.5 mac-address AABB-CCDD-EEFF` (Huawei MAC format conversion verified)

    5) `TestE2E_PortBinding_Huawei_Remove_Success`:
       - Fixture: `huawei_port_binding_remove_success.fixture`
       - Call: `svc.PortBinding(ctx, "port-1", "remove", "10.62.25.5", "", "e2e-op")`
       - Asserts: `CommandSent` contains `undo user-bind static ip-address 10.62.25.5`

    6) `TestE2E_PortBinding_Ruijie_Add_Success`:
       - Fixture: `ruijie_port_binding_add_success.fixture`
       - InterfaceName: `GigabitEthernet 1/0/1` (with space)
       - Call: `svc.PortBinding(ctx, "port-1", "add", "10.62.25.5", "AA:BB:CC:DD:EE:FF", "e2e-op")`
       - Asserts: `CommandSent` contains `switchport port-security binding aabb.ccdd.eeff 10.62.25.5` (Ruijie MAC format = lowercase cisco-style dotted)

    7) `TestE2E_PortBinding_Ruijie_Add_NoMac_Success`:
       - Fixture: ADD a new fixture `ruijie_port_binding_add_no_mac_success.fixture` (IP-only, no MAC)
       - InterfaceName: `GigabitEthernet 1/0/1`
       - Call: `svc.PortBinding(ctx, "port-1", "add", "10.62.25.5", "", "e2e-op")` (no MAC)
       - Asserts: `CommandSent` contains `switchport port-security binding 10.62.25.5` (IP only, no MAC substring)

    8) `TestE2E_PortBinding_Ruijie_Remove_Success`:
       - Fixture: ADD a new fixture `ruijie_port_binding_remove_success.fixture`
       - InterfaceName: `GigabitEthernet 1/0/1`
       - Call: `svc.PortBinding(ctx, "port-1", "remove", "10.62.25.5", "AA:BB:CC:DD:EE:FF", "e2e-op")`
       - Asserts: `CommandSent` contains `no switchport port-security binding aabb.ccdd.eeff 10.62.25.5`

    9) (Optional) `TestE2E_PortBinding_Huawei_Remove_WithMac_Success`:
       - Fixture: ADD a new fixture `huawei_port_binding_remove_with_mac_success.fixture`
       - Call: `svc.PortBinding(ctx, "port-1", "remove", "10.62.25.5", "AA:BB:CC:DD:EE:FF", "e2e-op")`
       - Asserts: `CommandSent` contains `undo user-bind static ip-address 10.62.25.5 mac-address AABB-CCDD-EEFF`

    10) (Optional) `TestE2E_SetAccessVlan_Huawei_PreStateMatch_NoOp`:
        - DB seed: port with `VLAN=*int(100)` (need to update seed; same as W2 test pattern)
        - Call: `svc.SetAccessVlan(ctx, "port-1", 100, "e2e-op")` (target == current)
        - Asserts: `result.Status == "skipped"`, `result.NoOp == true`, `result.CurrentState == "vlan_match"`, `exec.AssertNotCalled(...)` (no SSH)
        - This is the integration-level proof of the W2 pre-state check (vs. the unit-level proof in W2 Task 3)

    Run the verification command — must exit 0 with 10+ new subtests passing + 6+ v1.19 e2e tests still green.

    AVOID: using the short interface name for Ruijie (e.g., `GigabitEthernet1/0/1` without space) — would fail the fixture prompt match. AVOID: putting batch tests for the 2 new actions (port_binding batch path is deferred to FUTURE-BATCH-05 per W2 design). AVOID: testing vlanId=0 (should be rejected by W2 service ErrVlanIdOutOfRange BEFORE the test reaches scrapligo).
  </action>
  <verify>
    <automated>cd "D:/code/ClaudeCode/xingran-go-backend" && go test ./internal/services/portwrite/... -run 'TestE2E_SetAccessVlan|TestE2E_PortBinding' -count=1 -v -timeout 90s 2>&1 | tail -50</automated>
  </verify>
  <done>
    `internal/services/portwrite/port_write_e2e_test.go` extended with 10+ new TestE2E_* functions covering the 2 new v1.20.1 actions (Huawei + Ruijie + variants). All tests use the v1.19 fileTransportExecutor + e2eFixturePath + newTestService infrastructure without modification. The scrapligo FileTransport integration is now proven for the 2 new actions — same evidence pattern as v1.19 (mock SSH proves the integration chain; real-device UAT deferred per site-visit pattern).
  </done>
</task>

<task type="auto">
  <name>Task 3: Create 56-HUMAN-UAT.md — site-visit deferral document for 6+ real-device SSH verifications</name>
  <files>.planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-HUMAN-UAT.md</files>
  <read_first>
    - D:\code\ClaudeCode\xingran-go-backend\.planning\phases\54-w5-e2e-real-device-uat-documentation\54-HUMAN-UAT.md (v1.19 deferral pattern reference; structure: header / per-action sections / verifier_status)
    - D:\code\ClaudeCode\xingran-go-backend\.planning\phases\56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09\56-RESEARCH.md (line 312-316 — Environment Availability; known cross-firmware risks requiring site-visit)
    - D:\code\ClaudeCode\xingran-go-backend\docs\plans\2026-07-09-v1.20.1-design.md (line 302-309 — Real Device UAT 推迟)
    - D:\code\ClaudeCode\xingran-go-backend\.planning\phases\48-device-component-serials-planned\48-HUMAN-UAT.md (alternative v1.18 deferral pattern)
  </read_first>
  <acceptance_criteria>
    - File `.planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-HUMAN-UAT.md` exists (created, not modified)
    - File is at least 80 lines long
    - File contains the literal string `verifier_status: human_needed` (in YAML frontmatter or body)
    - File contains 6+ site-visit SSH verification items covering: Huawei S8700 (3+ items), Ruijie RS8607E (3+ items), H3C (1+ item if production has H3C device, else deferred note)
    - Each item follows the pattern: `- [ ] <action> — <verification> | <expected> | owner | next site visit`
    - File explains the mock-e2e vs site-visit split (Task 2 e2e covers what can be tested in CI; site-visit covers real-device firmware variations)
    - File references the v1.19 54-HUMAN-UAT.md as a precedent
  </acceptance_criteria>
  <action>
    Create `.planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-HUMAN-UAT.md` following the v1.19 54-HUMAN-UAT.md deferral pattern.

    File structure (mirror v1.19):

    1) YAML frontmatter:
       ```markdown
       ---
       phase: 56
       slug: vlan-v1-20-1-0-5-plans-initiated-2026-07-09
       status: deferred
       verifier_status: human_needed
       created: 2026-07-09
       ---
       ```

    2) Header section:
       ```markdown
       # Phase 56 v1.20.1 网络设备 VLAN + 端口绑定 — Site-Visit UAT 推迟清单

       本 phase 在 mock SSH FileTransport 层（Task 2 e2e tests）已证明集成链路（RenderCommand → executeWrite → SendConfigs → parseConfigError → CollectDevice refresh）端到端工作。**真机 SSH 验证推迟到下次现场访问**（owner: 现场运维同事），参照 v1.19 Phase 54 / v1.18 Phase 48 推迟模式。

       ## Mock-e2e 已覆盖 (CI 内)
       - Huawei VRP shutdown / undo / description / dot1x enable / dot1x disable / set_access_vlan / port_binding (add/remove with/without MAC)
       - Ruijie RGOS shutdown / undo / description / dot1x enable / dot1x disable / set_access_vlan / port_binding (add/remove with/without MAC)
       - H3C Comware 模板渲染 (单元测试) + pre-state 逻辑 (W2 service test)

       ## Site-visit 待现场验证 (6+ 项)
       ```

    3) 6+ site-visit items, organized by vendor:

       ```markdown
       ### Huawei S8700 (production)

       - [ ] **Huawei S8700 set_access_vlan — PVID 切换验证** | SSH 设备,执行 `interface GE1/0/0; port link-type access; port default vlan 100`,然后 `display port vlan GE1/0/0` 验证 PVID 改为 100 | 现场运维 | 下次现场访问
       - [ ] **Huawei S8700 set_access_vlan — 老固件 V200R005 兼容性** | SSH V200R005 老固件设备,验证 `port link-type access` 前置在老固件上仍被接受 (无 `% Wrong parameter` 拒绝) | 现场运维 | 跨固件对比
       - [ ] **Huawei S8700 port_binding add (IP only)** | SSH 设备,执行 `user-bind static ip-address 10.62.25.5`,然后 `display user-bind static all` 验证 IP 出现在列表 | 现场运维 | 下次现场访问
       - [ ] **Huawei S8700 port_binding add (with MAC)** | SSH 设备,执行 `user-bind static ip-address 10.62.25.5 mac-address AABB-CCDD-EEFF`,验证 MAC 正确显示 | 现场运维 | 下次现场访问
       - [ ] **Huawei S8700 port_binding remove** | SSH 设备,先 add 绑定,再 `undo user-bind static ip-address 10.62.25.5`,验证 `display user-bind static all` 不再显示该 IP | 现场运维 | 下次现场访问
       - [ ] **Huawei S8700 跨固件版本 V200R022+ `port link-type access` 兼容性** | 验证 V200R022+ 是否仍需要 `port link-type access` 前置 (RISK-03 验证) | 现场运维 | 跨固件对比

       ### Ruijie RS8607E (production)

       - [ ] **Ruijie RS8607E set_access_vlan — switchport mode/access 验证** | SSH 设备,执行 `interface GigabitEthernet 1/1; switchport mode access; switchport access vlan 318`,然后 `show interfaces status` 验证 Vlan 列改为 318 | 现场运维 | 下次现场访问
       - [ ] **Ruijie RS8607E port_binding add (with MAC)** | SSH 设备,执行 `switchport port-security binding AABB.CCDD.EEFF 10.62.2.111`,然后 `show port-security binding` 验证 MAC 出现在表格 (用户实采输出无 MAC 列,需现场确认) | 现场运维 | 下次现场访问 (RISK-02 关键)
       - [ ] **Ruijie RS8607E port_binding add (IP only)** | SSH 设备,执行 `switchport port-security binding 10.62.2.111`,验证 IP-only binding 在 show 输出中正常显示 | 现场运维 | 下次现场访问
       - [ ] **Ruijie RS8607E port_binding remove** | SSH 设备,先 add 绑定,再 `no switchport port-security binding 10.62.2.111`,验证 show 输出移除该行 | 现场运维 | 下次现场访问

       ### H3C Comware (production - 如有)

       - [ ] **H3C Comware V7 user-bind 关键字 (no static) 验证** | SSH H3C 设备 (若现场有 H3C),执行 `user-bind ip-address 10.x.x.x mac-address xxxx-xxxx-xxxx`,验证设备接受 (H3C 关键字与 Huawei 不同的关键 RISK-01 验证); 若现场无 H3C 设备,此条标 deferred | 现场运维 | 视现场设备情况
       - [ ] **H3C Comware V7 set_access_vlan — port access vlan 关键字** | SSH H3C 设备,执行 `interface GigabitEthernet1/0/1; port link-type access; port access vlan 100`,验证 PVID 切换 | 现场运维 | 视现场设备情况
       ```

    4) Closing section referencing the v1.19 54-HUMAN-UAT.md precedent:
       ```markdown
       ## 参照 precedent

       - v1.19 推迟模式: `.planning/phases/54-w5-e2e-real-device-uat-documentation/54-HUMAN-UAT.md` (7 项 site-visit SSH 验证)
       - v1.18 推迟模式: `.planning/phases/48-device-component-serials-planned/48-HUMAN-UAT.md` (3 项 site-visit SSH 验证)
       - v1.20.1 本期 6+ 项 (1.0x 倍 v1.19 per-action 验证量,因新增 2 actions 涉及跨厂商 + 跨固件 + MAC/IP 形态变体)

       ## Mock-e2e 覆盖率 (CI 内)

       Task 2 已落地的 10+ TestE2E_* 测试覆盖:
       - RenderCommand 3-vendor × 2-action × variant 全部命令字面正确性
       - SendConfigs → parseConfigError 链路在 scrapligo FileTransport 上的端到端执行
       - PortResult.CommandSent 包含正确的厂商命令子串
       - success path 触发 CollectDevice 后台刷新

       限制: FileTransport 不能完全模拟真实 SSH 的 firmware 行为差异; 跨固件 (Huawei V200R005 vs V200R022) 需现场验证。
       ```

    5) Verify the file is at least 80 lines and contains `verifier_status: human_needed` somewhere.

    AVOID: testing vlanId=0 or vlanId=4095 in the field (service rejects these with 400). AVOID: listing site-visit items that are not actionable (every item must have a concrete SSH command + verification step). AVOID: removing the v1.19 precedent reference (provides context for the deferral pattern).
  </action>
  <verify>
    <automated>test -f .planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-HUMAN-UAT.md && wc -l .planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-HUMAN-UAT.md && grep -c "verifier_status: human_needed" .planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-HUMAN-UAT.md && grep -c "^- \[ \]" .planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-HUMAN-UAT.md</automated>
  </verify>
  <done>
    `.planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-HUMAN-UAT.md` created with 6+ site-visit SSH verification items (Huawei S8700 + Ruijie RS8607E + H3C, conditional). File follows the v1.19 / v1.18 deferral pattern; includes `verifier_status: human_needed`; references v1.19 54-HUMAN-UAT.md precedent. Owner: 现场运维同事; next site visit will close the items.
  </done>
</task>

<task type="auto">
  <name>Task 4: Documentation sync — API响应规范.md + CHANGELOG.md + MILESTONES.md</name>
  <files>docs/API响应规范.md, CHANGELOG.md, .planning/MILESTONES.md</files>
  <read_first>
    - D:\code\ClaudeCode\xingran-go-backend\docs\API响应规范.md (v1.19 port-write section reference; need to find the existing section and add v1.20.1 entries)
    - D:\code\ClaudeCode\xingran-go-backend\CHANGELOG.md (v1.19 entry format reference)
    - D:\code\ClaudeCode\xingran-go-backend\.planning\MILESTONES.md (v1.19 entry format reference)
    - D:\code\ClaudeCode\xingran-go-backend\docs\plans\2026-07-09-v1.20.1-design.md (line 222-237 — API 端点 contract; line 159-167 — OperType mapping)
  </read_first>
  <acceptance_criteria>
    - `docs/API响应规范.md` is extended with a v1.20.1 section (or appended to existing network-device-write section) covering: 2 new endpoint signatures, request/response schema, OperType mapping, error codes
    - `CHANGELOG.md` is extended with a v1.20.1 entry (following the v1.19 format: ## v1.20.1 — 2026-07-09 / Milestone / Phases / Key deliverables)
    - `.planning/MILESTONES.md` is extended with a v1.20.1 entry (following the v1.19 format: ## v1.20 ... — SHIPPED 2026-07-09 / Phases / audit)
    - No existing v1.19 entries are modified
    - Documentation matches the actual code (no spec drift)
  </acceptance_criteria>
  <action>
    Synchronize 3 documentation files with the v1.20.1 milestone completion.

    1) `docs/API响应规范.md` — locate the existing "网络设备端口写操作" section (added in v1.19 Phase 52). Append a v1.20.1 subsection AFTER the existing content:
       ```markdown
       ### v1.20.1 扩展: set_access_vlan + port_binding

       **端点 (2 个新增):**

       | 端点 | Method | 用途 | 请求体 |
       |------|--------|------|--------|
       | `/network/ports/write/set-access-vlan` | POST | 修改端口 access VLAN | `{ portId, vlanId (1-4094), reason }` |
       | `/network/ports/write/port-binding` | POST | 端口绑定 (add/remove) | `{ portId, op ("add"\|"remove"), ipAddress, macAddress?, reason }` |

       **OperType 映射 (operlog):**

       | Action | OperType | OperType 值 |
       |--------|----------|------------|
       | `set_access_vlan` | Update | 2 |
       | `port_binding op=add` | Create | 1 |
       | `port_binding op=remove` | Delete | 3 |

       **错误码 (sentinel → HTTP 400):**

       | 错误 sentinel | HTTP code | 用户提示 |
       |--------------|-----------|----------|
       | `ErrVlanIdOutOfRange` | 400 | "VLAN ID 必须在 1-4094 之间" |
       | `ErrBindOpInvalid` | 400 | "绑定操作必须是 add 或 remove" |
       | `ErrIPAddressInvalid` | 400 | "IP 地址格式不合法" |
       | `ErrMACAddressInvalid` | 400 | "MAC 地址格式不合法" |

       **请求示例 (set_access_vlan):**

       ```json
       POST /network/ports/write/set-access-vlan
       {
         "portId": "f1e2d3c4-b5a6-9876-5432-10fedcba9876",
         "vlanId": 100,
         "reason": "业务变更需要"
       }
       ```

       **请求示例 (port_binding):**

       ```json
       POST /network/ports/write/port-binding
       {
         "portId": "f1e2d3c4-b5a6-9876-5432-10fedcba9876",
         "op": "add",
         "ipAddress": "10.62.25.5",
         "macAddress": "AA:BB:CC:DD:EE:FF",
         "reason": "安全合规要求"
       }
       ```

       **响应 (与 v1.19 端点一致, 复用 PortResult schema):**

       ```json
       {
         "code": 0,
         "message": "success",
         "data": {
           "portId": "...",
           "action": "set_access_vlan",
           "status": "succeeded",
           "noOp": false,
           "commandSent": "interface GE0/0/1 | port link-type access | port default vlan 100 | quit"
         },
         "timestamp": 1766380800,
         "request_id": "uuid-string"
       }
       ```

       **审计:** 复用 `sys_port_write_audit` JSONB 字段; `command_sent` 不脱敏 (审计真相源, 与 v1.19 一致).
       ```

    2) `CHANGELOG.md` — locate the v1.19 entry at the top. Append a new v1.20.1 entry ABOVE v1.19 (newest first convention):
       ```markdown
       ## v1.20.1 — 2026-07-09
       **Milestone**: 网络设备 VLAN + 端口绑定 (Network Device VLAN + Port Binding)
       **Phases**: 56 (1 phase, 5 plans, 5 build waves)
       **Key deliverables**:
       - 2 new port write actions: `set_access_vlan` (1-4094) + `port_binding` (add/remove with optional MAC)
       - 3 vendors × 2 new actions = 6 vendor-specific command templates (Huawei/H3C/Ruijie)
       - 4 new sentinel validators (VLAN range / IP / MAC / bind op)
       - 2 new HTTP endpoints (POST /network/ports/write/{set-access-vlan|port-binding}) with OperType mapping (Update/Create/Delete)
       - Reuses `network:port:write` permission (no new perm constant)
       - 2 new frontend Modals (SetAccessVlanModal + PortBindingModal) with form validation (InputNumber 1-4094, IPV4_REGEX, MAC_REGEX, REASON 5-200 chars)
       - 10+ new e2e tests via scrapligo FileTransport (Huawei + Ruijie)
       - 6+ site-visit SSH verification items deferred to next on-site visit (Huawei S8700 + Ruijie RS8607E + H3C)
       - vendor-react bundle gzip: 776 kB baseline (zero regression)
       ```

    3) `.planning/MILESTONES.md` — locate the v1.19 entry. Append a v1.20.1 entry ABOVE v1.19 (newest first):
       ```markdown
       ## v1.20.1 网络设备 VLAN + 端口绑定 (Network Device VLAN + Port Binding) — SHIPPED 2026-07-09

       ✅ **v1.20.1 SHIPPED** — Phase 56 (1 phase, 5 plans, 5 build waves) closed on 2026-07-09.

       - 2 new port write actions: `set_access_vlan` + `port_binding` (Huawei/H3C/Ruijie × 2)
       - 4 new sentinel validators + 2 new HTTP endpoints
       - 2 new frontend Modals (single-port only; batch path deferred to FUTURE-BATCH-05)
       - 10+ mock e2e tests + 6+ site-visit UAT items deferred
       - Vendor template extension: 12+ new table rows (3 vendors × 2 actions × variants)
       - Risk mitigations: H3C `user-bind` no `static` (RISK-01), Ruijie full MAC+IP syntax (RISK-02), Huawei `port link-type access` universal prefix (RISK-03)
       - Documentation: API 响应规范 / CHANGELOG / MILESTONES synchronized

       See `.planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-HUMAN-UAT.md` for site-visit verification items.
       ```

    4) Verify the 3 documentation files are updated correctly:
       - `grep -c "v1.20.1" docs/API响应规范.md` should return ≥ 3 (section header + 2 examples)
       - `grep -c "v1.20.1" CHANGELOG.md` should return ≥ 5 (entry header + 8+ bullets)
       - `grep -c "v1.20.1" .planning/MILESTONES.md` should return ≥ 3 (entry header + 3+ bullets)

    AVOID: modifying v1.19 entries (the existing v1.19 documentation is locked and correct). AVOID: omitting the OperType mapping (operators reading the docs need to know which OperType to grep for in sys_oper_log). AVOID: putting the v1.20.1 entry BELOW v1.19 (newest-first convention).
  </action>
  <verify>
    <automated>cd "D:/code/ClaudeCode/xingran-go-backend" && grep -c "v1.20.1" docs/API响应规范.md CHANGELOG.md .planning/MILESTONES.md 2>&1</automated>
  </verify>
  <done>
    3 documentation files synchronized with v1.20.1 milestone:
    - `docs/API响应规范.md` extended with v1.20.1 subsection (2 endpoints + OperType mapping + 4 error codes + 2 request examples)
    - `CHANGELOG.md` extended with v1.20.1 entry (newest-first)
    - `.planning/MILESTONES.md` extended with v1.20.1 milestone entry (SHIPPED 2026-07-09)
    Zero modification to v1.19 entries (locked and correct). Documentation matches the actual code (no spec drift).
  </done>
</task>

<task type="auto">
  <name>Task 5: Build + full test suite + bundle size gate — Phase 56 W5 acceptance</name>
  <files></files>
  <read_first>
    - D:\code\ClaudeCode\xingran-go-backend\internal\services\portwrite\port_write_e2e_test.go (Task 2 output — 10+ new e2e tests)
    - D:\code\ClaudeCode\xingran-go-backend\internal\services\portwrite\testdata\*.fixture (Task 1 output — 6+ new fixtures)
    - D:\code\ClaudeCode\xingran-go-backend\.planning\phases\56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09\56-HUMAN-UAT.md (Task 3 output)
    - D:\code\ClaudeCode\xingran-go-backend\docs\API响应规范.md (Task 4 output)
    - D:\code\ClaudeCode\xingran-go-backend\CHANGELOG.md (Task 4 output)
    - D:\code\ClaudeCode\xingran-go-backend\.planning\MILESTONES.md (Task 4 output)
    - D:\code\ClaudeCode\xingran-go-backend\.planning\phases\56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09\56-VALIDATION.md (Wave 5 acceptance criteria)
  </read_first>
  <acceptance_criteria>
    - `go build ./...` exits 0 (no cross-package regression)
    - `go test ./internal/services/portwrite/... -count=1 -timeout 120s` exits 0 with 6+ v1.19 e2e + 10+ v1.20.1 e2e = 16+ e2e subtests all passing
    - `go test ./internal/services/portcollection/... -count=1` exits 0 (Phase 56 W1 vendor template tests still pass)
    - `go test ./internal/api/v1/network/... -count=1` exits 0 (Phase 56 W3 handler/router tests still pass)
    - `go test ./internal/utils/operlog/... -count=1 -v` exits 0 (Phase 34 operlog regression lock intact)
    - `npm run type-check` from `xingran-react-frontend/` exits 0 (Phase 56 W4 frontend type-check still green)
    - `npm run build` from `xingran-react-frontend/` exits 0; vendor-react bundle gzip ≤ 776 kB baseline
    - `56-HUMAN-UAT.md` exists with `verifier_status: human_needed` and 6+ `- [ ]` site-visit items
    - `docs/API响应规范.md` / `CHANGELOG.md` / `.planning/MILESTONES.md` all contain v1.20.1 entries
  </acceptance_criteria>
  <action>
    Run the full Phase 56 W5 acceptance gate from the project root (this task makes NO file modifications — it is the final acceptance gate):

    1. `go build ./...` — must exit 0 to confirm all 4 code modifications (e2e test file + 6 fixture files) compile cleanly.

    2. `go test ./internal/services/portwrite/... -count=1 -timeout 120s` — must exit 0 with 16+ e2e subtests all passing (6 v1.19 + 10+ v1.20.1).

    3. `go test ./internal/services/portcollection/... ./internal/api/v1/network/... ./internal/utils/operlog/... -count=1 -timeout 60s` — must exit 0 (W1 + W3 + Phase 34 regression all green).

    4. `cd xingran-react-frontend && npm run type-check && npm run build 2>&1 | tail -20` — must exit 0; vendor-react bundle gzip ≤ 776 kB.

    5. Spot-check: `ls internal/services/portwrite/testdata/ | wc -l` should return ≥ 13 (v1.19 7 + v1.20.1 6+).

    6. Spot-check: `grep -c "TestE2E_SetAccessVlan\|TestE2E_PortBinding" internal/services/portwrite/port_write_e2e_test.go` should return ≥ 10.

    7. Spot-check: `test -f .planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-HUMAN-UAT.md && grep -c "verifier_status: human_needed" .planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-HUMAN-UAT.md && grep -c "^- \[ \]" .planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-HUMAN-UAT.md` should show file exists + 1 verifier_status match + ≥ 6 unchecked items.

    8. Spot-check: `grep -c "v1.20.1" docs/API响应规范.md CHANGELOG.md .planning/MILESTONES.md` should show ≥ 3 / ≥ 5 / ≥ 3 matches per file.

    9. If any step fails: do NOT mark this task done. Read the failure output, fix it, and rerun before declaring complete.

    Do NOT commit changes in this task — handled by orchestrator after SUMMARY.
  </action>
  <verify>
    <automated>cd "D:/code/ClaudeCode/xingran-go-backend" && go build ./... && go test ./internal/services/portwrite/... ./internal/services/portcollection/... ./internal/api/v1/network/... ./internal/utils/operlog/... -count=1 -timeout 120s 2>&1 | tail -30</automated>
  </verify>
  <done>
    Phase 56 W5 acceptance gate passes:
    - Full repo builds; all tests green (v1.19 e2e + v1.20.1 e2e + vendor templates + handler/router + operlog regression)
    - Frontend type-check + build green; vendor-react bundle gzip ≤ 776 kB
    - 56-HUMAN-UAT.md created with 6+ site-visit items
    - 3 documentation files synchronized with v1.20.1 entries
    v1.20.1 milestone is ready to close. Submit the v1.20.1 milestone archive (ROADMAP + PROJECT + STATE updates) to formally close the milestone.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e test → scrapligo FileTransport | scrapligo reads the fixture file as a byte stream simulating a real SSH device. The fixture content must match the expected vendor command sequence; otherwise the test fails with parseConfigError or wrong CommandSent. |
| scrapligo FileTransport → service.executeWrite | The fixture's per-command responses are consumed by scrapligo's SendConfigs (2-3 responses for 2-3 commands); the last response is fed to parseConfigError for success/failure classification. |
| e2e test → mockCollectionSvc | After successful SSH execute, the service calls `portCollectionSvc.CollectDevice(deviceID)` in a detached goroutine. The mock's `Once()` expectation and the `waitCollectedCalls(t, 1)` helper ensure the goroutine has completed before assertions run. |
| Documentation → next reader | The 3 documentation files are read by operators / new team members / future Claude sessions. Spec drift (e.g., documenting an endpoint that doesn't exist) erodes trust and creates misaligned expectations. |
| Site-visit UAT deferral → 现场运维 | The `verifier_status: human_needed` flag is the signal that this UAT requires human action. The 现场运维同事 is the sole owner; the milestone is considered closed at mock-e2e layer per v1.19 / v1.18 precedent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-W5-01 | Tampering | Fixture file content mismatch | mitigate | Each fixture follows the v1.19 byte-stream pattern exactly (per-command `[prompt] cmd [next-prompt]` sequence). 10+ new e2e tests assert on exact `CommandSent` substring (`port default vlan 100`, `user-bind static ip-address 10.62.25.5`, etc.) — fixture content drift breaks the assertion deterministically. |
| T-56-W5-02 | Information Disclosure | Documentation exposes internal sentinel names | accept | API 响应规范.md documents the 4 sentinel error names (ErrVlanIdOutOfRange etc.) and their HTTP 400 translations. Sentinel names are internal Go identifiers, but they appear in HTTP response messages and oper_param, so operators already see them. No secrets / credentials exposed. |
| T-56-W5-03 | Repudiation | Site-visit UAT items never closed | accept | v1.19 / v1.18 precedent: 现场运维同事 closes the items in a future site visit. The 56-HUMAN-UAT.md file is the deferral record; the milestone closes at the mock-e2e layer with the UAT document as a "known limitation" tracked in the milestone retrospective. |
| T-56-W5-04 | Tampering | Spec drift between code and documentation | mitigate | The 3 documentation files were created from the v1.20.1 design doc (docs/plans/2026-07-09-v1.20.1-design.md) and the actual code (port_write_handler.go, port_write_service.go, vendor_port_template.go). Spot-check during Task 4 ensures URL paths, OperType values, error code names, and request/response schemas match the code. |
| T-56-W5-SC | Tampering | npm/pip/cargo installs | n/a | Phase 56 W5 adds zero new external packages. All work uses existing v1.19 dependencies (scrapligo for e2e, testify/assert for assertions, gorm/sqlite for DB tests). |

## Notes

- The 6+ site-visit UAT items follow the v1.19 / v1.18 deferral pattern exactly. The 现场运维同事 is the owner; next on-site visit will close the items.
- The mock e2e (10+ TestE2E_*) is the canonical evidence for v1.20.1 close. Real-device UAT is a future enhancement, not a milestone gate.
- The 3 documentation files are the single source of truth for v1.20.1; any future change to the endpoint / OperType / error code must update these files in lockstep with the code.
- After Task 5 passes, the v1.20.1 milestone is ready to close. The next steps are: (1) commit the changes; (2) update ROADMAP.md v1.20 status to `shipped`; (3) update PROJECT.md and STATE.md with v1.20 archive notes; (4) update MILESTONES.md with the formal "SHIPPED" entry; (5) mark Phase 56 complete in the GSD tracker. These milestone-archive steps are handled by the orchestrator after the SUMMARY is written.
</threat_model>

<verification>
1. `go build ./...` exits 0.
2. `go test ./internal/services/portwrite/... -count=1 -timeout 120s` exits 0 with 16+ e2e subtests passing.
3. `go test ./internal/services/portcollection/... ./internal/api/v1/network/... ./internal/utils/operlog/... -count=1` exits 0.
4. `cd xingran-react-frontend && npm run type-check` exits 0.
5. `cd xingran-react-frontend && npm run build` exits 0; vendor-react bundle gzip ≤ 776 kB.
6. `internal/services/portwrite/testdata/` contains 13+ fixture files (7 v1.19 + 6-8 v1.20.1).
7. `internal/services/portwrite/port_write_e2e_test.go` contains 10+ new TestE2E_SetAccessVlan_* and TestE2E_PortBinding_* functions.
8. `.planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-HUMAN-UAT.md` exists with `verifier_status: human_needed` and 6+ `- [ ]` site-visit items.
9. `docs/API响应规范.md` contains v1.20.1 section (2 endpoint signatures + OperType mapping + 4 error codes + 2 request examples).
10. `CHANGELOG.md` contains v1.20.1 entry (newest-first; 8+ bullets).
11. `.planning/MILESTONES.md` contains v1.20.1 SHIPPED entry (3+ bullets).
12. Phase 34 operlog regression lock intact.
13. Zero new external dependencies (no `go.mod` or `package.json` diff).
</verification>

<success_criteria>
1. 10+ new TestE2E_* functions for set_access_vlan + port_binding pass with `go test ./internal/services/portwrite/... -count=1` (TEST-03).
2. 6+ new fixture files in `internal/services/portwrite/testdata/` (TEST-04).
3. `56-HUMAN-UAT.md` created with 6+ site-visit items + `verifier_status: human_needed` (TEST-05).
4. `docs/API响应规范.md` extended with v1.20.1 section.
5. `CHANGELOG.md` extended with v1.20.1 entry.
6. `.planning/MILESTONES.md` extended with v1.20.1 SHIPPED entry.
7. Phase 56 W1 + W2 + W3 + W4 + W5 all compile and pass; Phase 50-54 regression intact; Phase 34 operlog lock intact.
8. vendor-react bundle gzip ≤ 776 kB baseline (zero regression).
9. Zero new external dependencies.
10. Plan file committed to git with message `docs(phase-56): create e2e + UAT + documentation plan`.
11. v1.20.1 milestone ready to close (after orchestrator commits + updates ROADMAP / PROJECT / STATE).
</success_criteria>

<output>
Create `.planning/phases/56-vlan-v1-20-1-0-5-plans-initiated-2026-07-09/56-05-SUMMARY.md` when done.
</output>
