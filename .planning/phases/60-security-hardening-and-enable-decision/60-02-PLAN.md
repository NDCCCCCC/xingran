---
phase: 60-security-hardening-and-enable-decision
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/models/api_key.go
  - internal/services/system/apikey_service.go
  - internal/services/system/apikey_service_test.go
  - docs/operations/sql/2026-08-13-drop-idx-api-keys-key.sql
  - .planning/notes/260813-sec01-hash-migration.md
  - .planning/notes/260813-sec02-redundant-index-removal.md
autonomous: true
requirements:
  - SEC-01
  - SEC-02

must_haves:
  truths:
    - "调用 CreateAPIKey 后 DB 中存储的是 SM3(key+salt) 的 hex 哈希,不是明文 key,且同次请求响应里仍含明文 key 一次性返回"
    - "ValidateAPIKey 在收到与 DB 行 KeyHash 不匹配的明文时返回错误;匹配时返回 apiKey 对象(走 KeyPrefix 缩窄 + sm3 比对路径)"
    - "ListAPIKey 按 Keyword 过滤时跨 SQLite/PostgreSQL 一致命中 Name 或 KeyPrefix,不再依赖 dialect 分支"
    - "执行 docs/operations/sql/2026-08-13-drop-idx-api-keys-key.sql 后,PG 的 pg_indexes 与 SQLite 的 sqlite_master 都不再含 idx_api_keys_key"
  artifacts:
    - path: "internal/models/api_key.go"
      provides: "APIKey schema: KeyHash(64 hex, uniqueIndex) + Salt(32 hex) + KeyPrefix(12 chars) 三列替换原 Key 列(D-06)"
      contains: "KeyHash.*size:64.*uniqueIndex"
    - path: "internal/services/system/apikey_service.go"
      provides: "ValidateAPIKey 走 KeyPrefix 缩窄 + SM3 哈希比对 + 恒定时间;CreateAPIKey 生成 Salt 后算 SM3 存哈希;ListAPIKeys 删 dialect 分支(per D-05/D-07/D-08/D-09)"
      contains: "func hashAPIKey"
    - path: "internal/services/system/apikey_service_test.go"
      provides: "按新 schema 重写 setupTestDB 的 sys_api_keys DDL + createTestAPIKey helper + TestCreateAPIKey/TestValidateAPIKey/TestListAPIKeys 等子测试断言"
      contains: "key_hash TEXT NOT NULL UNIQUE"
    - path: "docs/operations/sql/2026-08-13-drop-idx-api-keys-key.sql"
      provides: "SEC-02 手动运维 SQL(DROP INDEX IF EXISTS idx_api_keys_key)"
      contains: "DROP INDEX IF EXISTS idx_api_keys_key"
    - path: ".planning/notes/260813-sec01-hash-migration.md"
      provides: "SEC-01 决策记录(5 维度:D-05 存储方案 / D-06 Schema / D-07 List 搜索 / D-08 迁移路径 / D-09 创建流程)"
      contains: "## 存储方案"
    - path: ".planning/notes/260813-sec02-redundant-index-removal.md"
      provides: "SEC-02 决策记录 + 验证查询(PG pg_indexes + SQLite sqlite_master 双片段)"
      contains: "## 验证查询"
  key_links:
    - from: "internal/services/system/apikey_service.go:CreateAPIKey"
      to: "internal/models/api_key.go:KeyHash field"
      via: "apiKey.KeyHash = hashAPIKey(key, salt) → DB 写入 hash + 明文 key 通过 *string 返回"
      pattern: "KeyHash:\\s*hashAPIKey"
    - from: "internal/services/system/apikey_service.go:ValidateAPIKey"
      to: "internal/models/api_key.go:KeyHash/Salt/KeyPrefix"
      via: "KeyPrefix 缩窄候选 + sm3 比对 + subtle.ConstantTimeCompare"
      pattern: "key_prefix\\s*=\\s*\\?"
    - from: "internal/services/system/apikey_service_test.go:setupTestDB"
      to: "internal/models/api_key.go:new schema"
      via: "CREATE TABLE sys_api_keys DDL 含 key_hash/salt/key_prefix 三列"
      pattern: "CREATE TABLE IF NOT EXISTS sys_api_keys"
    - from: "docs/operations/sql/2026-08-13-drop-idx-api-keys-key.sql"
      to: "PostgreSQL pg_indexes / SQLite sqlite_master"
      via: "DROP INDEX IF EXISTS 让索引名从 system catalog 消失"
      pattern: "DROP INDEX IF EXISTS"
---

<objective>
将 API Key 存储从明文 WHERE key = ? 改为 SM3 单向哈希存储(SEC-01),同时通过手动运维 SQL(D-10)收敛 migration_085 残留的冗余索引 idx_api_keys_key(SEC-02),并以 5+1 维度决策记录固化上述偏离推荐的用户选择(不可逆哈希 vs SM4 对称加密;手动 SQL vs Go migration)。两类改造都基于"用户确认无活跃 API key"前提,无需双读兼容期 / 回填脚本。

Purpose: SEC-01 解决 P2-c「API Key 明文存储」根因缺陷 — DB 泄漏后无法还原明文 key;SEC-02 解决 P3「冗余索引」性能债。两项硬化直接锁住 API Key 的 data-at-rest 与 schema 整洁,使 Phase 61 资源权限矩阵启用时不会遇到核心凭据层缺陷。

Output: 1 个 model schema 变更 + 1 个 service 三函数重写 + 1 个现有 test 文件改造 + 1 个 SQL 运维文件 + 2 个 notes 决策记录。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-security-hardening-and-enable-decision/60-CONTEXT.md
@.planning/phases/60-security-hardening-and-enable-decision/60-PATTERNS.md
@.planning/phases/60-security-hardening-and-enable-decision/60-VALIDATION.md
@.planning/phases/60-security-hardening-and-enable-decision/60-DISCUSSION-LOG.md
@internal/models/base.go
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (SEC-01): schema 变更 + service 三函数重写 + apikey_service_test.go 按新 schema 重写 + 5 维度决策记录 notes</name>
  <files>internal/models/api_key.go, internal/services/system/apikey_service.go, internal/services/system/apikey_service_test.go, .planning/notes/260813-sec01-hash-migration.md</files>
  <read_first>
- internal/models/api_key.go(全文件;line 8-23 APIKey struct 当前形态,line 11 是 `Key string` 字段,line 26-28 TableName 不变)
- internal/models/base.go(line 11-19 BaseModel 字段,确定 gorm tag 风格)
- internal/services/system/apikey_service.go(全文件 586 行;重点:line 53-66 常量定义、line 79-99 isValidKeyFormat 复用、line 118-126 generateKey 复用、line 128-161 ValidateAPIKey 改造点、line 164-222 CreateAPIKey 改造点、line 224-289 ListAPIKeys 改造点 line 238-247 dialect 分支需删除、line 3-15 import 块)
- internal/services/system/apikey_service_test.go(全文件 1323 行;重点 line 33-187 setupTestDB 的 sys_api_keys DDL line 82-103 旧 Key 列需改、line 209-251 createTestAPIKey helper 改用 hashAPIKey+固定 Salt、line 273-399 TestCreateAPIKey 子测试断言 key_hash/salt/key_prefix、line 401-503 TestValidateAPIKey 子测试改用 apiKey.Key 明文,line 506+ TestListAPIKeys 用 Name/KeyPrefix 双字段命中测试)
- internal/core/security/password.go(line 14 既有 sm3 import 路径、line 79-82 sm3.New() + h.Write + h.Sum 原语、line 104-106 salt := make([]byte, pm.config.SaltLength); rand.Read 原语、line 152 subtle.ConstantTimeCompare 恒定时间比对原语)
- .planning/phases/60-security-hardening-and-enable-decision/60-CONTEXT.md(读 D-05 存储方案/D-06 Schema/D-07 List 搜索/D-08 无迁移路径/D-09 一次性明文返回;Claude's Discretion: SM3 格式无前缀、Salt 16 字节、新列在 BaseModel 后 Name 紧邻)
- .planning/phases/60-security-hardening-and-enable-decision/60-PATTERNS.md(读 ## SEC-01 DDL 改造 checklist + ## 既有 Analog 形态 line 196-291)
- .planning/codebase/TESTING.md(无 gomock + 需 DB 真实连接约定)
- .planning/notes/260616-rpa-heartbeat-log-flooding.md(notes 文件格式参考)
  </read_first>
  <action>
按 D-05/D-06/D-07/D-08/D-09 五条锁定决策落地 SEC-01 全栈改造(模型 → service 三函数 → 测试)。

**api_key.go schema 改动(per D-06)**:
1. 移除 `Key string` 字段(原 line 11;`gorm:"size:100;uniqueIndex;not null" json:"key"`)
2. 在 BaseModel 之后、Name 紧邻位置**新增**三个字段(根据 CONTEXT.md Claude's Discretion):
   - `KeyHash string` 标签 `gorm:"size:64;uniqueIndex;not null" json:"-"`(SM3 hex 64 字符,对 DB 唯一但 API 不暴露)
   - `Salt string` 标签 `gorm:"size:32;not null" json:"-"`(16 字节 hex 32 字符,API 不暴露)
   - `KeyPrefix string` 标签 `gorm:"size:12;index;not null" json:"keyPrefix"`(明文前 12 字符,用于 List 搜索;普通 index,非 unique 因为多 key 可重复)
3. **不**改 `TableName()`(line 26-28 sys_api_keys 保留)
4. **不**改 `User *User` gorm foreignKey 关联
5. 注:Phase 60 选单次 SM3 不复用 HashPassword 的 `$sm3$iterations$salt$hash` 格式(避免与密码哈希格式混淆;Claude's Discretion)

**apikey_service.go 改动(per D-05/D-07/D-08/D-09)**:
1. import 块(line 3-15)新增两条:
   - `"crypto/subtle"`(恒定时间比对)
   - `"github.com/tjfoc/gmsm/sm3"`(SM3 hash 原语,既有依赖 go.mod)
   - 既有 `"crypto/rand"` + `"encoding/hex"` 留
2. **新增**顶层 helper 函数(line ~127 位置,在 `generateKey` 之后):
   - `func hashAPIKey(key, salt string) string`:`h := sm3.New(); h.Write([]byte(key)); h.Write([]byte(salt)); return hex.EncodeToString(h.Sum(nil))`(per PATTERNS.md line 612-619;**不**复用 HashPassword)
   - `func generateSalt() (string, error)`:16 字节 random + hex.EncodeToString(per PATTERNS.md line 636-647;**不**复用 PasswordManager.GenerateSalt)
3. **重写** `ValidateAPIKey`(line 128-161,D-08 改造):
   - line 137-140 `Where("key = ? AND is_active = ?", keyStr, true)` 改为 KeyPrefix 缩窄:`Where("key_prefix = ? AND is_active = ?", keyStr[:12], true)` 取候选列表
   - 循环候选:`computedHash := hashAPIKey(keyStr, candidate.Salt)`;`if subtle.ConstantTimeCompare([]byte(computedHash), []byte(candidate.KeyHash)) == 1 { return &candidate }`(per PATTERNS.md line 207-227)
   - 循环结束 → `apperrors.Wrap(nil, apperrors.CodeUnauthorized, "密钥不存在或已禁用")`(沿用既有 line 144 error wrapping)
4. **重写** `CreateAPIKey`(line 164-222,D-09 改造):
   - 在 line 204 `apiKey := models.APIKey{...}` 构造前,新增:`key, err := generateKey(); salt, err := generateSalt(); keyHash := hashAPIKey(key, salt)`(per PATTERNS.md line 230-260)
   - 替换 `Key: key` 为:`KeyHash: keyHash, Salt: salt, KeyPrefix: key[:12],`(per D-06)
   - 既有 `generateKey()` 已存在且返回 `rec_+64hex=68 字符`,**不动**
   - 不动 line 221 `return &key, nil`(明文一次性返回契约保持)
5. **改造** `ListAPIKeys`(line 224-289,D-07 改造):
   - 删 `isSQLite := s.db.Dialector.Name() == "sqlite"`(line 238,本函数专用)
   - 删 line 241-246 `if isSQLite { ... substr ... } else { ... LEFT ... }` dialect 分支
   - 替换为 `query = query.Where("name LIKE ? OR key_prefix LIKE ?", keyword, keyword)`(per PATTERNS.md line 285-291)
   - **不**动 line 251-258 `if params.Scope != nil` SQLite 用 `json_each` / PG 用 `scopes @> ?` 的 dialect 分支(本 phase 范围限定仅修 KeyPrefix 这一项)
6. **不动** GetAPIKey / UpdateAPIKey / DeleteAPIKey / ToggleAPIKeyStatus / ListUsageLogs / GetUsageLogSummary(这些函数不读 Key 列)
7. **不动** isValidKeyFormat / isKeyExpired / validateScopes / generateKey 既有 helpers
8. **不动** error wrapping 模式(沿用 apperrors.Wrap + apperrors.DatabaseError)

**apikey_service_test.go 改造(per D-06 schema + D-09 创建 + D-08 验证,D-07 搜索)**:
此文件**已存在 1323 行**,Phase 60 是「按新 schema 重写」而非「新增」。改造 checklist:
1. `setupTestDB`(line 33-187)的 `sys_api_keys` DDL(line 82-103):
   - 移除 `key TEXT NOT NULL UNIQUE` 列
   - 新增 `key_hash TEXT NOT NULL UNIQUE` + `salt TEXT NOT NULL` + `key_prefix TEXT NOT NULL`
   - **不动**其他列(id/created_at/updated_at/deleted_at/created_by/updated_by/version/name/user_id/expires_at/last_used_at/is_active/scopes/ip_whitelist/description/inherit_perms)
   - **不动** sys_api_key_usage_logs / sys_role / sys_role_menu / sys_role_dept / sys_user_role / sys_user_post DDL
2. `createTestAPIKey`(line 209-251)helper 重构:
   - 现有 line 219 `key := fmt.Sprintf("rec_%016x%048x", time.Now().UnixNano(), seq)` 保留(明文 68 字符)
   - 现有 line 221-231 `apiKey := &models.APIKey{... Key: key ...}` 改为:`KeyHash: hashAPIKey(key, fixedSalt), Salt: fixedSalt, KeyPrefix: key[:12]`(fixedSalt 用 32 hex 字符串字面量,例:`"0123456789abcdef0123456789abcdef"`)
   - 现有 line 237-242 is_active 处理保留(raw SQL 写 0/1)
   - 增加 `return apiKey` 让测试拿到 KeyHash/Salt/KeyPrefix 完整对象(测试断言用)
3. `TestCreateAPIKey`(line 273-399)子测试断言改造:
   - "正常创建" 子测试 line 302:`db.Where("key = ?", *key).First(&apiKey)` 改为 `db.Where("key_hash = ?", apiKey.KeyHash).First(&apiKey)`(因 service 不再返回 DB 对象,只能通过 key_hash 反查)
   - "正常创建" 断言增强: `len(apiKey.KeyHash) == 64` + `len(apiKey.Salt) == 32` + `apiKey.KeyPrefix == (*key)[:12]`
   - "密钥数量限制" 子测试(line 326-358)重构:`for i := 0; i < MaxKeysPerUser; i++ { key := "...格式填充..."; apiKey := &models.APIKey{ KeyHash: hashAPIKey(key, fixedSalt), Salt: fixedSalt, KeyPrefix: key[:12], ... } }`(按新字段构造)
   - "密钥格式正确性" 子测试(line 372-398)保留 `assert.Equal(t, 68, len(*key))` 等明文返回格式断言
4. `TestValidateAPIKey`(line 401-503)子测试断言改造:
   - "有效密钥" 子测试:`apiKey := createTestAPIKey(...)` + `validated, err := service.ValidateAPIKey(ctx, apiKey.Key)`(`apiKey.Key` 已不存在 → 改为 `validated, err := service.ValidateAPIKey(ctx, plaintextKey)` 其中 plaintextKey 在 createTestAPIKey 内部 helper 暴露或通过 salt+keyprefix 还原)
   - "密钥不存在" 子测试已有(line 447-454),保持
   - **方案:** 在 `createTestAPIKey` 内同步生成并返回明文 plaintext key,signature 改为 `func createTestAPIKey(t *testing.T, db *gorm.DB, userID string, isActive bool) (*models.APIKey, string)` 双返回(明文 + 对象),让所有测试断言一致
5. `TestListAPIKeys`(line 506+)调整 "关键词搜索" 子测试:
   - 现有构造 key1 时 `key1.Name = "Active Key"` 命中 LIKE %Active%;新增:`key1.KeyPrefix = "<含 Active 子串>..."` 让 KeyPrefix LIKE %Active% 也命中
   - "关键词搜索" 子测试(line 544-560)断言保留 `assert.Equal(t, int64(1), result.Total)` 但需要 ListAPIKeys 返回 DB 行,KeyPrefix 命中 1 条即 1
6. "密钥脱敏" 子测试(`TestGetAPIKey` 内,如果存在 `result.Key == 68 字符`断言)改为: `assert.Equal(t, 12, len(result.KeyPrefix))` + `assert.Empty(t, result.KeyHash)`(API 不暴露 KeyHash)
7. 其他子测试("用户不存在错误" / "无效作用域" / "无效格式_*" / "密钥已禁用" / "密钥已过期" / "最后使用时间更新" / "状态筛选" / "作用域筛选" / "分页功能" 等)保留逻辑,只更新 `apiKey.Key` 为明文局部变量
8. **不动** cleanupTestData / createTestUser / testTrackedXX 变量
9. 测试运行命令: `go test -v ./internal/services/system/...` 必须全绿

**SEC-01 决策记录 notes**:
新建 `.planning/notes/260813-sec01-hash-migration.md`,5 维度段落 = D-05..D-09 一一对应:
- `## 1. 存储方案`(D-05):SM3 单次哈希 vs SM4 对称加密 vs PBKDF2 决策表 + 为什么选 SM3(256-bit 熵无字典攻击;不可逆;与密码哈希国密栈一致;不依赖 SM4_KEY 保护)
- `## 2. Schema 变更`(D-06):Key 列移除 + KeyHash/Salt/KeyPrefix 三列 gorm tag 表(json tag `KeyHash:"-",` Salt:"-",` KeyPrefix:"keyPrefix"`)
- `## 3. List 搜索`(D-07):删除 dialect 分支 + KeyPrefix LIKE 双字段命中;为什么 KeyPrefix 而非 KeyHash(前缀可搜;哈希不可搜)
- `## 4. 迁移路径`(D-08):无迁移前提("现在没有使用中的 api key")+ 直接切换理由;若未来有活跃 key 需双读期的扩展路径
- `## 5. 创建流程`(D-09):generateKey → generateSalt → hashAPIKey → 存三列 → 返回明文 *string;为什么一次性返回(管理员秘钥管理 UX 标准);轮换走"重新创建"

文件名前缀 `260813-` 沿用既有 notes 命名;格式参考 `.planning/notes/260616-rpa-heartbeat-log-flooding.md`。
  </action>
  <verify>
<automated>cd D:/code/ClaudeCode/guoguo && go build ./...</automated>
<automated>cd D:/code/ClaudeCode/guoguo && go test -v -run "TestCreateAPIKey|TestValidateAPIKey|TestListAPIKeys|TestGetAPIKey" ./internal/services/system/...</automated>
<automated>cd D:/code/ClaudeCode/guoguo && grep -n "KeyHash\|Salt\|KeyPrefix" internal/models/api_key.go | head -10</automated>
<automated>cd D:/code/ClaudeCode/guoguo && grep -n "func hashAPIKey\|func generateSalt" internal/services/system/apikey_service.go</automated>
<automated>cd D:/code/ClaudeCode/guoguo && grep -n "subtle.ConstantTimeCompare\|sm3.New" internal/services/system/apikey_service.go</automated>
<automated>cd D:/code/ClaudeCode/guoguo && grep -n "key_hash TEXT NOT NULL UNIQUE\|salt TEXT NOT NULL\|key_prefix TEXT NOT NULL" internal/services/system/apikey_service_test.go</automated>
<automated>cd D:/code/ClaudeCode/guoguo && go test ./internal/services/system/...</automated>
</verify>
  <acceptance_criteria>
- `internal/models/api_key.go` **不**含 `Key string` 字段(grep 无 Key string 字面量剩余)
- `internal/models/api_key.go` 含 `KeyHash string` + `Salt string` + `KeyPrefix string` 三字段,gorm tag 分别为 `size:64;uniqueIndex;not null` + `size:32;not null` + `size:12;index;not null`
- `internal/models/api_key.go` KeyHash/Salt 的 `json:"-"` 隐藏,KeyPrefix 的 `json:"keyPrefix"` 暴露
- `internal/services/system/apikey_service.go` 顶层含 `func hashAPIKey(key, salt string) string`(用 sm3.New)与 `func generateSalt() (string, error)`(用 crypto/rand.Read 16 字节 + hex)
- `internal/services/system/apikey_service.go` ValidateAPIKey 用 KeyPrefix 缩窄 + sm3 比对 + subtle.ConstantTimeCompare,不再含 `Where("key = ?"` 字面量
- `internal/services/system/apikey_service.go` CreateAPIKey 构造 `apiKey := models.APIKey{KeyHash: hashAPIKey(...), Salt: ..., KeyPrefix: ...}`,不再含 `Key: key` 字面量
- `internal/services/system/apikey_service.go` ListAPIKeys 删 `isSQLite := s.db.Dialector.Name() == "sqlite"` 本地变量与 dialect 分支;keyword 搜索改为 `name LIKE ? OR key_prefix LIKE ?`(单行 Where);scope 搜索的 dialect 分支**保留**(本 phase 范围限定)
- `internal/services/system/apikey_service_test.go` `setupTestDB` 的 `sys_api_keys` DDL 含 `key_hash TEXT NOT NULL UNIQUE` + `salt TEXT NOT NULL` + `key_prefix TEXT NOT NULL` 三列,**不**含 `key TEXT NOT NULL UNIQUE`
- `internal/services/system/apikey_service_test.go` `createTestAPIKey` helper 用新字段构造,不再设 `Key: key`
- `go test -v ./internal/services/system/...` 退出码 0(全绿,无 schema 字段冲突)
- `.planning/notes/260813-sec01-hash-migration.md` 文件存在,含 5 个 H2 段落标题分别为 `存储方案` / `Schema 变更` / `List 搜索` / `迁移路径` / `创建流程`
- notes 文档引用 D-05..D-09 决策 ID 与源码行号
- `go build ./...` 退出码 0
- `go test ./...` 全套测试无回归(既有 Phase 57/59 测试不破)
  </acceptance_criteria>
  <done>api_key.go schema 三列替换;apikey_service.go ValidateAPIKey/CreateAPIKey/ListAPIKeys 改造 + hashAPIKey/generateSalt helper;apikey_service_test.go 按新 schema 重写;5 维度决策 notes 落地;go build + go test 全绿</done>
</task>

<task type="auto">
  <name>Task 2 (SEC-02): 手动运维 SQL 移除冗余索引 idx_api_keys_key + 决策记录 notes 含 PG/SQLite 双片段验证查询</name>
  <files>docs/operations/sql/2026-08-13-drop-idx-api-keys-key.sql, .planning/notes/260813-sec02-redundant-index-removal.md</files>
  <read_first>
- internal/core/db/migrations/archive/020_drop_mac_unique_index.sql(全文件;参考既有 DROP INDEX IF EXISTS 注释 + 风险说明段风格)
- internal/core/db/migrations/archive/legacy-2026-06-15/027_cleanup_duplicate_indexes.sql(参考 DROP CONSTRAINT + DROP INDEX 双语句模式)
- internal/core/db/migrations/archive/applied/migration_085_api_keys.go(只读;确认 `idx_api_keys_key` 索引创建位置;**注意** 此文件带 `//go:build archive_skip` 不再编译执行,但 SchemaMigrationRunner 历史跑过该文件)
- .planning/phases/60-security-hardening-and-enable-decision/60-CONTEXT.md(读 D-10 手动 SQL + 文档 + 验证查询,**不**写 Go migration)
- .planning/notes/260616-rpa-heartbeat-log-flooding.md(notes 文件格式参考)
  </read_first>
  <action>
按 D-10 锁定决策交付 SEC-02: 手动 SQL + 决策记录,**不**新建 `internal/core/db/migrations/` Go migration。

**SQL 文件新建**(per D-10):
路径: `D:/code/ClaudeCode/guoguo/docs/operations/sql/2026-08-13-drop-idx-api-keys-key.sql`
注释模板参考 `archive/020_drop_mac_unique_index.sql`(7 行注释 + 单条 DROP INDEX 语句),本文件内容:
```sql
-- Phase 60 / SEC-02: 移除 migration_085 遗留的冗余索引 idx_api_keys_key
-- 触发原因: sys_api_keys.key 列的 uniqueIndex 与 idx_api_keys_key(由 migration_085 创建)功能重复,GORM AutoMigrate 自动生成 sys_api_keys_key_hash_key(uniqueIndex),两者等价
-- 风险: 无 — uniqueIndex 保留,索引扫描性能不变,索引覆盖查询不变
-- 回滚: CREATE INDEX idx_api_keys_key ON sys_api_keys(key);
-- 幂等: IF EXISTS 关键字保证重复跑安全
-- 验证查询: 见 .planning/notes/260813-sec02-redundant-index-removal.md "验证查询" 段(PG pg_indexes + SQLite sqlite_master 双片段)
-- 执行模式: 手动运维,生产 DB 在运维窗口执行;不进 Go migration runner

DROP INDEX IF EXISTS idx_api_keys_key;
```
关键约束:
- **单语句** `DROP INDEX IF EXISTS idx_api_keys_key;`(沿用 archive/020 风格,不写 transaction wrapper)
- 文件顶 7 行注释段:原因 / 风险 / 回滚 / 幂等 / 验证引用 / 执行模式
- **不**新建 `internal/core/db/migrations/migration_086_*.go`(用户决策 D-10)

**SEC-02 决策记录 notes**:
新建 `.planning/notes/260813-sec02-redundant-index-removal.md`,与其它 notes 一致的 260813- 前缀 + 中文 H2 段落。结构按 VALIDATION.md SC#3 锚点:
- `## 1. 为什么`(D-10): 手动 SQL + notes + 验证查询 vs 新建 Go migration 决策表;为什么选手动 SQL(用户偏离推荐:决策快路径 + 运维可控 + 偏离 migration_085 archive_skip 模式更清晰)
- `## 2. 怎么跑 SQL`:执行命令 + 运维窗口选择;文件位置(`docs/operations/sql/2026-08-13-drop-idx-api-keys-key.sql`)
- `## 3. 验证查询`(per VALIDATION.md SC#3 + Claude's Discretion 双 dialect 双片段):
  - PostgreSQL 片段:
    ```sql
    SELECT indexname FROM pg_indexes WHERE tablename = 'sys_api_keys' AND indexname = 'idx_api_keys_key';
    -- 预期: 0 行(DROP 成功);非 0 行 = DROP 失败
    ```
  - SQLite 片段:
    ```sql
    SELECT name FROM sqlite_master WHERE type = 'index' AND tbl_name = 'sys_api_keys' AND name = 'idx_api_keys_key';
    -- 预期: 空(DROP 成功);非空 = DROP 失败
    ```
- `## 4. AutoMigrate 行为说明`: Phase 60 后 GORM AutoMigrate 启动时 key 列变 KeyHash/Salt/KeyPrefix(三列) + 唯一索引名变 `sys_api_keys_key_hash_key`(自动生成),`idx_api_keys_key` 与新 uniqueIndex 一旦存在即冗余;运维修剪窗口与代码部署可解耦
- 引用 D-10 决策 ID 与对照文件 `migration_085_api_keys.go` (带 archive_skip)
  </action>
  <verify>
<automated>ls -la D:/code/ClaudeCode/guoguo/docs/operations/sql/2026-08-13-drop-idx-api-keys-key.sql</automated>
<automated>grep -c "DROP INDEX IF EXISTS idx_api_keys_key" D:/code/ClaudeCode/guoguo/docs/operations/sql/2026-08-13-drop-idx-api-keys-key.sql</automated>
<automated>ls -la D:/code/ClaudeCode/guoguo/.planning/notes/260813-sec02-redundant-index-removal.md</automated>
<automated>grep -c "^## " D:/code/ClaudeCode/guoguo/.planning/notes/260813-sec02-redundant-index-removal.md</automated>
<automated>grep -n "pg_indexes\|sqlite_master" D:/code/ClaudeCode/guoguo/.planning/notes/260813-sec02-redundant-index-removal.md</automated>
</verify>
  <acceptance_criteria>
- `docs/operations/sql/2026-08-13-drop-idx-api-keys-key.sql` 文件存在
- SQL 文件含单条 `DROP INDEX IF EXISTS idx_api_keys_key;` 语句(grep -c 输出至少 1)
- SQL 文件 7 行注释段齐全(原因 / 风险 / 回滚 / 幂等 / 验证引用 / 执行模式)
- **不**新建 `internal/core/db/migrations/migration_086_*.go`(用户决策 D-10)
- `.planning/notes/260813-sec02-redundant-index-removal.md` 文件存在,含至少 4 个 H2 段落
- notes 文档含 PostgreSQL `pg_indexes` 验证片段 + SQLite `sqlite_master` 验证片段(双 dialect 片段,CLAUDE.md Claude's Discretion)
- notes 文档引用 D-10 决策 ID 与 migration_085 文件路径
  </acceptance_criteria>
  <done>SQL 文件就位(DROP INDEX IF EXISTS);notes 文档 4 段落齐全 + PG/SQLite 验证片段;无 Go migration 新建</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MultiAuth → APIKeyService.ValidateAPIKey | 明文 key 跨越此边界,与 DB hash 比对必须恒定时间,防侧信道 |
| DB at-rest | APIKey 行 KeyHash + Salt + KeyPrefix 落库后,DB 文件泄漏即 hash 暴露 |
| 手动运维窗口(生产 DBA) | 执行 SQL 的人决定 DROP 索引是否成功;验证查询监督结果 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-S1 | Spoofing | ValidateAPIKey (apikey_service.go:128-161) | mitigate | SM3(key+salt) + subtle.ConstantTimeCompare(D-05/D-08);不再有明文 `key = ?` 查询路径 |
| T-60-S2 | Info Disclosure | sys_api_keys 表 | mitigate | Key 列移除,DB 落盘仅 KeyHash/Salt/KeyPrefix(D-06);泄露后不可还原明文 |
| T-60-S3 | Tampering | ValidateAPIKey SQL 比较 | mitigate | `subtle.ConstantTimeCompare(hashBytes, comparisonHash) == 1` 恒定时间比对(password.go:152 原语复用),防止侧信道时序攻击 |
| T-60-S4 | Tampering | idx_api_keys_key 冗余索引 | mitigate | SEC-02 移除冗余索引,DROP INDEX IF EXISTS 幂等;uniqueIndex 保留,查询覆盖不变 |
| T-60-S5 | DoS | idx_api_keys_key 与 uniqueIndex 重复写放大 | mitigate | SEC-02 收敛唯一索引数 → INSERT/UPDATE 写路径仅维护一个索引 → 写吞吐性能恢复 |
| T-60-S6 | EoP | APIKey.Key plaintext 字段残留 | mitigate | Phase 60 重写后 grep 无 `Key string` 字段 + 无 `db.Where("key = ?"` 字面量(强制 schema 升级);handler 对 KeyPrefix 也只读不写(Create/Update request 无 key 字段) |
| T-60-S7 | Repudiation | CreateAPIKey operlog.RecordWithBody 路径 | mitigate | CLAUDE.md 强制 operlog 写于 success path 末尾;请求体本就不含 key 字段(operlog 关键词脱敏无变化) |
| T-60-S8 | Tampering | 手动 SQL 误执行(无回滚) | mitigate | notes 含回滚 SQL `CREATE INDEX idx_api_keys_key ON sys_api_keys(key);` 模板;DBA 跑 SQL 前先备份 schema |
| T-60-SC | Tampering | go install / go build | mitigate | `go build ./...` + `go test ./...` 退出码 0(SEC-01 测试全绿) |
</threat_model>

<verification>
[Overall phase-60-02 checks]
- go build ./... exit code 0
- go test -v ./internal/services/system/... exit code 0(SEC-01 三函数测试全绿)
- go test ./... exit code 0(既有 Phase 57/59 测试无回归)
- grep "Key string" internal/models/api_key.go → 0 matches
- grep "key = ?" internal/services/system/apikey_service.go → 0 matches(明文 WHERE 子句消除)
- grep "func hashAPIKey\|func generateSalt" internal/services/system/apikey_service.go → 2 matches
- grep "key_hash TEXT NOT NULL UNIQUE\|salt TEXT NOT NULL\|key_prefix TEXT NOT NULL" internal/services/system/apikey_service_test.go → 3 matches
- ls docs/operations/sql/2026-08-13-drop-idx-api-keys-key.sql exit code 0
- ls .planning/notes/260813-sec01-hash-migration.md exit code 0
- ls .planning/notes/260813-sec02-redundant-index-removal.md exit code 0
- grep "pg_indexes\|sqlite_master" .planning/notes/260813-sec02-redundant-index-removal.md → 2 matches
- ls internal/core/db/migrations/migration_086* 2>/dev/null → 0 results(D-10 强制不写 Go migration)
</verification>

<success_criteria>
[Phase 60 / 60-02 完成判据]

1. SC#2 / SEC-01 决策记录 + 哈希迁移: `.planning/notes/260813-sec01-hash-migration.md` 存在 + 5 维度段落齐全;`go test -v ./internal/services/system/...` 全绿(含 TestCreateAPIKey/TestValidateAPIKey/TestListAPIKeys)
2. SC#3 / SEC-02 索引收敛: `docs/operations/sql/2026-08-13-drop-idx-api-keys-key.sql` 存在 + notes 含 PG `pg_indexes` + SQLite `sqlite_master` 验证片段;**不**新建 Go migration
3. SEC-01 schema 升级生效: `internal/models/api_key.go` 含 KeyHash(64 uniqueIndex)/Salt(32)/KeyPrefix(12 index)三字段;`internal/services/system/apikey_service.go` ValidateAPIKey 走 SM3 比对 + CreateAPIKey 算哈希存三列 + ListAPIKeys 删 dialect 分支用 KeyPrefix LIKE
4. SEC-01 测试改造覆盖: `internal/services/system/apikey_service_test.go` 按新 schema 重写 DDL + createTestAPIKey helper + TestCreateAPIKey/TestValidateAPIKey/TestListAPIKeys 子测试断言
5. 既有测试无回归: `go test ./...` 全套退出码 0
</success_criteria>

<output>
Create `.planning/phases/60-security-hardening-and-enable-decision/60-02-SUMMARY.md` when done
</output>
