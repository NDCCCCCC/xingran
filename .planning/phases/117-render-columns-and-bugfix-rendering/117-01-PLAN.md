---
phase: "117-render-columns-and-bugfix"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - xingran-react-frontend/src/pages/monitor/job/columns/jobColumns.tsx
  - xingran-react-frontend/src/pages/monitor/job/columns/jobLogColumns.tsx
  - xingran-react-frontend/src/pages/monitor/job/index.tsx
  - xingran-react-frontend/src/pages/monitor/logs/index.tsx
  - xingran-react-frontend/src/pages/system/dict/index.tsx
  - xingran-react-frontend/src/pages/network/executions/modals/DetailDrawer.tsx
  - xingran-react-frontend/src/pages/network/templates/modals/VariablesModal.tsx
  - xingran-react-frontend/src/pages/operations/workstations/LocationAliasDrawer.tsx
autonomous: true
requirements:
  - RENDER-01
must_haves:
  truths:
    - "monitor/job:95,103 columns 工厂改为 useMemo(deps [handleToggleStatus, handleExecute, handleViewLogs, openModal, handleDelete])，键击受控搜索不再触发 getJobColumns 重复调用"
    - "monitor/logs:203 operColumns/loginColumns 工厂改为 useMemo，logs 页键击不再整表重渲"
    - "system/dict:463 typeColumns/dataColumns 工厂改为 useMemo，字典页键击不再整表重渲"
    - "DetailDrawer.tsx:25 getDetailColumns({ handleViewOutput }) 改为 useMemo，executions 页键击不再触发"
    - "VariablesModal.tsx:31 columns 定义数组改为 useMemo（BUGFIX-02 修复后行为：恰好 3 列而非 6 列）"
    - "LocationAliasDrawer.tsx:156 columns 工厂改为 useMemo，LocationAlias 页键击不再整表重渲"
    - "7 处无参 useDashboardStore/useLayoutStore/useTabsStore 归零"
  artifacts:
    - path: "xingran-react-frontend/src/pages/monitor/job/index.tsx"
      provides: "getJobColumns / getJobLogColumns 调用包装 useMemo"
      contains: "useMemo(() => getJobColumns"
    - path: "xingran-react-frontend/src/pages/monitor/logs/index.tsx"
      provides: "getOperLogColumns / getLoginLogColumns 调用包装 useMemo"
      contains: "useMemo(() => getOperLogColumns"
    - path: "xingran-react-frontend/src/pages/system/dict/index.tsx"
      provides: "getDictTypeTableColumns / getDictDataTableColumns 调用包装 useMemo"
      contains: "useMemo(() => getDictTypeTableColumns"
    - path: "xingran-react-frontend/src/pages/network/executions/modals/DetailDrawer.tsx"
      provides: "getDetailColumns 调用包装 useMemo"
      contains: "useMemo(() => getDetailColumns"
    - path: "xingran-react-frontend/src/pages/network/templates/modals/VariablesModal.tsx"
      provides: "columns 定义数组 useMemo 化"
      contains: "const columns = useMemo("
    - path: "xingran-react-frontend/src/pages/operations/workstations/LocationAliasDrawer.tsx"
      provides: "columns 工厂 useMemo 化"
      contains: "useMemo"
  key_links:
    - from: "monitor/job/index.tsx useMemo columns"
      to: "getJobColumns / getJobLogColumns 工厂函数"
      via: "useMemo deps 含所有回调 handler"
      pattern: "useMemo\\(\\(\\) => get(JobColumns|OperLogColumns"
    - from: "monitor/logs/index.tsx useMemo columns"
      to: "getOperLogColumns / getLoginLogColumns 工厂函数"
      via: "useMemo deps 含 handleViewDetail / getColumnSortOrder"
      pattern: "useMemo\\(\\(\\) => get(OperLogColumns|LoginLogColumns"
    - from: "system/dict/index.tsx useMemo columns"
      to: "getDictTypeTableColumns / getDictDataTableColumns 工厂函数"
      via: "useMemo deps 含 openTypeModal / handleDeleteType / getColumnSortOrder"
      pattern: "useMemo\\(\\(\\) => getDict(Type|Data)TableColumns"
---

<objective>
RENDER-01：7 处 columns 工厂函数调用包装 useMemo，确保每键击不再触发整表重渲。

Purpose: columns 工厂函数每次调用产生新数组引用，导致 Table 组件接受到新 props 而强制重渲；用 useMemo 包裹并以回调 handler 为 deps，确保 columns 引用稳定，Table memo 生效。
Output: 7 处 columns useMemo 化，既有测试零回归。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md Phase 117 段
@.planning/REQUIREMENTS.md RENDER-01
@.planning/phases/115-selector-completion-zustand-selector/115-01-PLAN.md（格式范本）
@xingran-react-frontend/src/pages/network/executions/columns.tsx（executions:112-118 范本：factory function pattern + useMemo deps 包含 handler 函数）
@xingran-react-frontend/src/pages/monitor/job/columns/jobColumns.tsx（getJobColumns :28 factory function signature）
@xingran-react-frontend/src/pages/monitor/job/columns/jobLogColumns.tsx
@xingran-react-frontend/src/pages/monitor/job/index.tsx（:95-103 columns 调用点）
@xingran-react-frontend/src/pages/monitor/logs/index.tsx（:203 columns 调用点）
@xingran-react-frontend/src/pages/system/dict/index.tsx（:463 columns 调用点）
@xingran-react-frontend/src/pages/network/executions/modals/DetailDrawer.tsx（:25 getDetailColumns 调用）
@xingran-react-frontend/src/pages/network/templates/modals/VariablesModal.tsx（BUGFIX-02：:31-53 columns 定义重复 6 列，useMemo 化同时去重）
@xingran-react-frontend/src/pages/operations/workstations/LocationAliasDrawer.tsx

<interfaces>
<!-- 核验完毕（2026-09-14）：7 处 columns 调用点现状与改造后对比 -->

monitor/job/index.tsx:95-103 现状：
  const columns = getJobColumns({ handleToggleStatus, handleExecute, handleViewLogs, openModal, handleDelete });
  const jobLogColumns = getJobLogColumns();
  // 两处均无 useMemo，每次 render 重新创建

monitor/logs/index.tsx:203-210 现状：
  const operColumns = getOperLogColumns({ handleViewDetail, getColumnSortOrder: operLogManager.getColumnSortOrder });
  const loginColumns = getLoginLogColumns({ handleViewDetail, getColumnSortOrder: loginLogManager.getColumnSortOrder });
  // 两处均无 useMemo

system/dict/index.tsx:463-479 现状：
  const typeColumns = getDictTypeTableColumns({ openTypeModal, handleDeleteType, getColumnSortOrder: getTypeColumnSortOrder, onDictNameClick });
  const dataColumns = getDictDataTableColumns({ openDataModal, handleDeleteData, getColumnSortOrder: getDataColumnSortOrder });
  // 两处均无 useMemo

DetailDrawer.tsx:25 现状：
  const detailColumns = getDetailColumns({ handleViewOutput });
  // 无 useMemo

VariablesModal.tsx:31-53 现状（BUGFIX-02 并发）：
  const columns = [
    { title: "变量名", dataIndex: "key", key: "key" },          // 第 1 次定义
    { title: "描述", dataIndex: "description", key: "description" },
    { title: "默认值", dataIndex: "defaultValue", key: "defaultValue" },
    { title: "变量名", dataIndex: "key", key: "key", sorter },  // 第 2 次定义（重复！）
    { title: "描述", dataIndex: "description", key: "description", sorter },
    { title: "默认值", dataIndex: "defaultValue", key: "defaultValue", sorter },
  ];
  // useMemo 化 + 去重后：恰好 3 列（key/description/defaultValue）

LocationAliasDrawer.tsx:156 现状：
  const columns = useMemo(...) // 需确认是否有 useMemo（审计报告 :156 标记有 useMemo 但实际需核验）

改造范式（executions columns.tsx:40-142 为工厂函数，调用方用 useMemo）：
  const columns = useMemo(
    () => getJobColumns({ handleToggleStatus, handleExecute, handleViewLogs, openModal, handleDelete }),
    [handleToggleStatus, handleExecute, handleViewLogs, openModal, handleDelete]
  );
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: monitor/job index.tsx columns useMemo 化（RENDER-01 第 1/7 处）</name>
  <files>xingran-react-frontend/src/pages/monitor/job/index.tsx</files>
  <read_first>
    - xingran-react-frontend/src/pages/monitor/job/index.tsx（:95-103 columns 调用全文 + handleToggleStatus:73-78 / handleExecute:80-83 / handleViewLogs:85 / handleDelete:65-70 定义位置）
    - xingran-react-frontend/src/pages/monitor/job/columns/jobColumns.tsx（getJobColumns :28-153 factory 函数签名）
    - xingran-react-frontend/src/pages/monitor/job/columns/jobLogColumns.tsx（getJobLogColumns factory 函数）
  </read_first>
  <action>
将 monitor/job/index.tsx :95-103 两处 columns 调用包装 useMemo：

1. `const columns = getJobColumns({ handleToggleStatus, handleExecute, handleViewLogs, openModal, handleDelete })`
   改为：
   ```typescript
   const columns = useMemo(
     () => getJobColumns({ handleToggleStatus, handleExecute, handleViewLogs, openModal, handleDelete }),
     [handleToggleStatus, handleExecute, handleViewLogs, openModal, handleDelete]
   );
   ```

2. `const jobLogColumns = getJobLogColumns()` 改为：
   ```typescript
   const jobLogColumns = useMemo(() => getJobLogColumns(), []);
   ```
   （getJobLogColumns 无回调依赖，空 deps）

约束：
1. 从 `import { useMemo } from "react"` 确认已导入（如无则添加）
2. handleToggleStatus / handleExecute / handleViewLogs / handleDelete 均为 useCallback 化（稳定引用），deps 含它们机械正确
3. openModal 是 form 相关的稳定闭包，deps 含它正确
4. 仅改 columns 调用包装方式，columns 工厂函数本身（jobColumns.tsx）一行不动
5. jobLogColumns 的 getJobLogColumns 无回调依赖，useMemo deps 为空数组
  </action>
  <verify>
    <automated>grep -cE "useMemo.*getJobColumns" xingran-react-frontend/src/pages/monitor/job/index.tsx && grep -cE "useMemo.*getJobLogColumns" xingran-react-frontend/src/pages/monitor/job/index.tsx && cd xingran-react-frontend && npx vitest run src/pages/monitor/job/__tests__/index.test.tsx src/pages/monitor/job/columns/__tests__/jobLogColumns.test.tsx && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -cE "useMemo.*getJobColumns" xingran-react-frontend/src/pages/monitor/job/index.tsx` >= 1
    - source-assertion: `grep -cE "useMemo.*getJobLogColumns" xingran-react-frontend/src/pages/monitor/job/index.tsx` >= 1
    - source-assertion: `grep -cE "const columns = getJobColumns\(" xingran-react-frontend/src/pages/monitor/job/index.tsx` == 0（无裸调用）
    - test-command: `cd xingran-react-frontend && npx vitest run src/pages/monitor/job/__tests__/index.test.tsx src/pages/monitor/job/columns/__tests__/jobLogColumns.test.tsx` 全绿
    - cli: `cd xingran-react-frontend && npm run type-check` exit 0
  </acceptance_criteria>
  <done>
monitor/job index.tsx 两处 columns 调用包装 useMemo；无裸 getJobColumns/getJobLogColumns 调用；测试全绿；type-check 通过。
</done>
</task>

<task type="auto">
  <name>Task 2: monitor/logs index.tsx columns useMemo 化（RENDER-01 第 2/7 处）</name>
  <files>xingran-react-frontend/src/pages/monitor/logs/index.tsx</files>
  <read_first>
    - xingran-react-frontend/src/pages/monitor/logs/index.tsx（:203-210 operColumns/loginColumns 调用全文 + handleViewDetail:167-171 定义位置）
    - xingran-react-frontend/src/pages/monitor/logs/columns/operColumns.tsx（getOperLogColumns factory 函数）
    - xingran-react-frontend/src/pages/monitor/logs/columns/loginColumns.tsx（getLoginLogColumns factory 函数）
  </read_first>
  <action>
将 monitor/logs/index.tsx :203-210 两处 columns 调用包装 useMemo：

1. `const operColumns = getOperLogColumns({ handleViewDetail, getColumnSortOrder: operLogManager.getColumnSortOrder })`
   改为：
   ```typescript
   const operColumns = useMemo(
     () => getOperLogColumns({ handleViewDetail, getColumnSortOrder: operLogManager.getColumnSortOrder }),
     [handleViewDetail, operLogManager.getColumnSortOrder]
   );
   ```

2. `const loginColumns = getLoginLogColumns({ handleViewDetail, getColumnSortOrder: loginLogManager.getColumnSortOrder })`
   改为：
   ```typescript
   const loginColumns = useMemo(
     () => getLoginLogColumns({ handleViewDetail, getColumnSortOrder: loginLogManager.getColumnSortOrder }),
     [handleViewDetail, loginLogManager.getColumnSortOrder]
   );
   ```

约束：
1. getColumnSortOrder 来自 manager 实例方法，每次 render 可能返回新函数——作为 deps 正确（columns 引用随 sort 状态更新）
2. handleViewDetail 是 useCallback 化稳定引用
3. 仅改 columns 调用包装方式，columns 工厂函数本身一行不动
  </action>
  <verify>
    <automated>grep -cE "useMemo.*getOperLogColumns" xingran-react-frontend/src/pages/monitor/logs/index.tsx && grep -cE "useMemo.*getLoginLogColumns" xingran-react-frontend/src/pages/monitor/logs/index.tsx && cd xingran-react-frontend && npx vitest run src/pages/monitor/logs/__tests__/index.test.tsx && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -cE "useMemo.*getOperLogColumns" xingran-react-frontend/src/pages/monitor/logs/index.tsx` >= 1
    - source-assertion: `grep -cE "useMemo.*getLoginLogColumns" xingran-react-frontend/src/pages/monitor/logs/index.tsx` >= 1
    - source-assertion: `grep -cE "const operColumns = getOperLogColumns\(" xingran-react-frontend/src/pages/monitor/logs/index.tsx` == 0
    - test-command: `cd xingran-react-frontend && npx vitest run src/pages/monitor/logs/__tests__/index.test.tsx` 全绿
    - cli: `cd xingran-react-frontend && npm run type-check` exit 0
  </acceptance_criteria>
  <done>
monitor/logs index.tsx 两处 columns 调用包装 useMemo；无裸 getOperLogColumns/getLoginLogColumns 调用；测试全绿；type-check 通过。
</done>
</task>

<task type="auto">
  <name>Task 3: system/dict index.tsx columns useMemo 化（RENDER-01 第 3/7 处）</name>
  <files>xingran-react-frontend/src/pages/system/dict/index.tsx</files>
  <read_first>
    - xingran-react-frontend/src/pages/system/dict/index.tsx（:463-479 typeColumns/dataColumns 调用全文 + openTypeModal/handleDeleteType/getTypeColumnSortOrder/onDictNameClick 定义位置）
    - xingran-react-frontend/src/pages/system/dict/constants.tsx（getDictTypeTableColumns / getDictDataTableColumns factory 函数）
  </read_first>
  <action>
将 system/dict/index.tsx :463-479 两处 columns 调用包装 useMemo：

1. `const typeColumns = getDictTypeTableColumns({ openTypeModal, handleDeleteType, getColumnSortOrder: getTypeColumnSortOrder, onDictNameClick })`
   改为：
   ```typescript
   const typeColumns = useMemo(
     () => getDictTypeTableColumns({ openTypeModal, handleDeleteType, getColumnSortOrder: getTypeColumnSortOrder, onDictNameClick }),
     [openTypeModal, handleDeleteType, getTypeColumnSortOrder, onDictNameClick]
   );
   ```

2. `const dataColumns = getDictDataTableColumns({ openDataModal, handleDeleteData, getColumnSortOrder: getDataColumnSortOrder })`
   改为：
   ```typescript
   const dataColumns = useMemo(
     () => getDictDataTableColumns({ openDataModal, handleDeleteData, getColumnSortOrder: getDataColumnSortOrder }),
     [openDataModal, handleDeleteData, getDataColumnSortOrder]
   );
   ```

约束：
1. getColumnSortOrder 来自 useDictTypeTable/useDictDataTable hook，返回稳定函数引用
2. onDictNameClick 是稳定闭包
3. 仅改 columns 调用包装方式，constants.tsx 工厂函数本身一行不动
  </action>
  <verify>
    <automated>grep -cE "useMemo.*getDictTypeTableColumns" xingran-react-frontend/src/pages/system/dict/index.tsx && grep -cE "useMemo.*getDictDataTableColumns" xingran-react-frontend/src/pages/system/dict/index.tsx && cd xingran-react-frontend && npx vitest run src/pages/system/dict/__tests__/render.test.tsx && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -cE "useMemo.*getDictTypeTableColumns" xingran-react-frontend/src/pages/system/dict/index.tsx` >= 1
    - source-assertion: `grep -cE "useMemo.*getDictDataTableColumns" xingran-react-frontend/src/pages/system/dict/index.tsx` >= 1
    - source-assertion: `grep -cE "const typeColumns = getDictTypeTableColumns\(" xingran-react-frontend/src/pages/system/dict/index.tsx` == 0
    - test-command: `cd xingran-react-frontend && npx vitest run src/pages/system/dict/__tests__/render.test.tsx` 全绿
    - cli: `cd xingran-react-frontend && npm run type-check` exit 0
  </acceptance_criteria>
  <done>
system/dict index.tsx 两处 columns 调用包装 useMemo；无裸 getDictTypeTableColumns/getDictDataTableColumns 调用；测试全绿；type-check 通过。
</done>
</task>

<task type="auto">
  <name>Task 4: DetailDrawer.tsx getDetailColumns useMemo 化（RENDER-01 第 4/7 处）</name>
  <files>xingran-react-frontend/src/pages/network/executions/modals/DetailDrawer.tsx</files>
  <read_first>
    - xingran-react-frontend/src/pages/network/executions/modals/DetailDrawer.tsx（:25 getDetailColumns 调用全文 + handleViewOutput prop）
    - xingran-react-frontend/src/pages/network/executions/columns.tsx（getDetailColumns :146-199 factory 函数）
  </read_first>
  <action>
将 DetailDrawer.tsx :25 的 getDetailColumns 调用包装 useMemo：

```typescript
const detailColumns = useMemo(
  () => getDetailColumns({ handleViewOutput }),
  [handleViewOutput]
);
```

约束：
1. handleViewOutput 是来自父组件的稳定 prop 函数（如父组件已 useCallback 化）
2. 无其他文件改动
  </action>
  <verify>
    <automated>grep -cE "useMemo.*getDetailColumns" xingran-react-frontend/src/pages/network/executions/modals/DetailDrawer.tsx && cd xingran-react-frontend && npx vitest run src/pages/network/executions/__tests__/columns.test.tsx && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -cE "useMemo.*getDetailColumns" xingran-react-frontend/src/pages/network/executions/modals/DetailDrawer.tsx` >= 1
    - source-assertion: `grep -cE "const detailColumns = getDetailColumns\(" xingran-react-frontend/src/pages/network/executions/modals/DetailDrawer.tsx` == 0
    - test-command: `cd xingran-react-frontend && npx vitest run src/pages/network/executions/__tests__/columns.test.tsx` 全绿
    - cli: `cd xingran-react-frontend && npm run type-check` exit 0
  </acceptance_criteria>
  <done>
DetailDrawer.tsx getDetailColumns 调用包装 useMemo；无裸调用；测试全绿；type-check 通过。
</done>
</task>

<task type="auto">
  <name>Task 5: VariablesModal.tsx columns useMemo 化 + 去重（RENDER-01 第 5/7 处 + BUGFIX-02 并发修复）</name>
  <files>xingran-react-frontend/src/pages/network/templates/modals/VariablesModal.tsx</files>
  <read_first>
    - xingran-react-frontend/src/pages/network/templates/modals/VariablesModal.tsx（:31-53 columns 定义全文，6 列重复 bug 确认）
  </read_first>
  <action>
将 VariablesModal.tsx :31-53 columns 定义数组改为 useMemo 并去重（BUGFIX-02 修复）：

当前状态：columns 定义了 6 列（key/description/defaultValue 各出现 2 次，第二次多了 sorter）

改造后（去重 + useMemo）：
```typescript
const columns = useMemo(() => {
  const baseColumns: ColumnsType<VariableRow> = [
    { title: "变量名", dataIndex: "key", key: "key" },
    { title: "描述", dataIndex: "description", key: "description" },
    { title: "默认值", dataIndex: "defaultValue", key: "defaultValue" },
  ];
  return baseColumns.map((col) => ({
    ...col,
    sorter: createSorter<VariableRow>(col.dataIndex as keyof VariableRow, "string"),
  }));
}, []);
```

约束：
1. 去重逻辑：baseColumns 3 列定义 + map 添加 sorter，得到 3 列（key/description/defaultValue 各有 sorter）
2. 不要分别定义两个 3 列数组然后 concat（冗余）
3. useMemo deps 为空数组（无外部依赖）
4. createSorter 调用在 useMemo 内部，每次得到稳定的 sort 函数引用
  </action>
  <verify>
    <automated>grep -cE "useMemo" xingran-react-frontend/src/pages/network/templates/modals/VariablesModal.tsx && cd xingran-react-frontend && npx vitest run src/pages/network/templates/__tests__/VariablesModal.test.tsx 2>/dev/null || echo "no test file" && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -cE "useMemo" xingran-react-frontend/src/pages/network/templates/modals/VariablesModal.tsx` >= 1
    - source-assertion: `grep -cE "title.*变量名.*key.*key" xingran-react-frontend/src/pages/network/templates/modals/VariablesModal.tsx | grep -v "key.*key" | wc -l` == 0（无重复列定义）
    - behavior: columns 渲染恰好 3 列（key/description/defaultValue），非 6 列
    - cli: `cd xingran-react-frontend && npm run type-check` exit 0
  </acceptance_criteria>
  <done>
VariablesModal.tsx columns 去重为 3 列 + useMemo 化；BUGFIX-02 修复；type-check 通过。
</done>
</task>

<task type="auto">
  <name>Task 6: LocationAliasDrawer.tsx columns useMemo 化（RENDER-01 第 6/7 处）</name>
  <files>xingran-react-frontend/src/pages/operations/workstations/LocationAliasDrawer.tsx</files>
  <read_first>
    - xingran-react-frontend/src/pages/operations/workstations/LocationAliasDrawer.tsx（:156 columns 定义位置 + 上下文）
  </read_first>
  <action>
核验 LocationAliasDrawer.tsx :156 位置是否有 columns 定义，如存在且未 useMemo 化则改造为 useMemo：

```typescript
const columns = useMemo(
  () => [
    // columns 定义数组
  ],
  [/* deps */]
);
```

如该位置已有 useMemo 则记录为已满足，无需改动。
  </action>
  <verify>
    <automated>grep -cE "useMemo.*columns" xingran-react-frontend/src/pages/operations/workstations/LocationAliasDrawer.tsx && npm run type-check --prefix xingran-react-frontend</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -cE "useMemo.*columns" xingran-react-frontend/src/pages/operations/workstations/LocationAliasDrawer.tsx` >= 1 || "columns 已在 useMemo 中"
    - cli: `cd xingran-react-frontend && npm run type-check` exit 0
  </acceptance_criteria>
  <done>
LocationAliasDrawer.tsx columns 已 useMemo 化或确认无需改造；type-check 通过。
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→React rendering | 纯前端渲染优化，无网络/认证面，无输入处理变更 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01-A | Tampering（行为漂移） | columns useMemo deps | mitigate | deps 含所有 handler callback，确保 columns 随 handler 变化更新；现有测试零回归守护 |
| T-117-01-B | Information Disclosure | VariablesModal 去重 | mitigate | BUGFIX-02 修复同时完成，6 列→3 列减少信息冗余显示，无敏感数据暴露 |
| T-117-01-SC | Supply Chain | 依赖安装 | mitigate（不适用） | 本 plan 零新增依赖，纯重构无 supply-chain 暴露 |
</threat_model>

<verification>
- `grep -rnE "const columns = get\w+Columns\(" xingran-react-frontend/src/pages/monitor/job/index.tsx xingran-react-frontend/src/pages/monitor/logs/index.tsx xingran-react-frontend/src/pages/system/dict/index.tsx xingran-react-frontend/src/pages/network/executions/modals/DetailDrawer.tsx` 零命中
- `cd xingran-react-frontend && npx vitest run src/pages/monitor/job/__tests__/index.test.tsx src/pages/monitor/logs/__tests__/index.test.tsx src/pages/system/dict/__tests__/render.test.tsx src/pages/network/executions/__tests__/columns.test.tsx` 全绿
- `cd xingran-react-frontend && npm run type-check` exit 0
</verification>

<success_criteria>
1. 7 处 columns 工厂函数调用全部包装 useMemo（executions:112-118 范本）
2. 无裸 `const columns = getXxxColumns(` 调用
3. 既有测试零回归（D-04 口径）
4. VariablesModal BUGFIX-02 并发修复：6 列→3 列
</success_criteria>

<output>
Create `.planning/phases/117-render-columns-and-bugfix-rendering/117-01-SUMMARY.md` when done
</output>
