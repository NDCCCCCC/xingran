---
context: phase
phase: 58-route-contract-alignment
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-08-13T08:37:15.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] **CONSTRAINT**: Backend 启动 hang 在 `createRequestEncryptionToggleConfig` 之后(GORM session 在 Supabase Session pooler 多次写后失活)。`:9000 LISTENING` 未达成 → Phase 58 SC#1-SC#4 runtime test 无法进行。
- [ ] **CONSTRAINT**: AutoMigrate 注册列表缺漏 3 个 model (`models.APIKey{}`, `models.APIKeyUsageLog{}`, `models.Config{}`)。即使绕过 hang,新部署/clean DB 上 sys_api_keys + sys_config 永远不会被建 — P0 bug。
- [ ] **CONSTRAINT**: `migration_085_api_keys.go` 被 `//go:build archive_skip` 排除编译。已被 `SKIP_AUTOMIGRATE` + inline DDL 取代,需决定保留还是恢复。
- [ ] **CONSTRAINT**: 凭证安全 — Upstash token + Supabase password 永远只写 `.env` (gitignored),`configs/config.yaml` 仅 env var 名 + 占位。严禁回显到 chat。
- [ ] **CONSTRAINT**: Phase 58 frontend-only changes 不变量已成立 (commit 1978935 + 6a4c772); dev 环境改动 (TLS code + configs + .env) 必须作为独立 quick task, **禁止合并到 Phase 58 任何 task**。

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| 临时 dev 改动混入 Phase 58 commit | 会破坏 Phase 58 "backend zero changes" 验证语义 | blocking | dev 改动走独立 quick task,commit prefix `chore(dev):` |
| 凭证泄露到 git | Upstash token / Supabase password 写入 git 跟踪文件 | blocking | `.env` gitignored + `configs/config.yaml` 占位 |
| Phase 58 SUMMARY.md 提前生成 | Task 3 未 resolve 时生成 58-01-SUMMARY.md 引入 false completion | advisory | 等 human-verify + :9000 跑通后再生成 |

<current_state>

Phase 58 执行被 **dev env Supabase pooler 阻塞**。Task 1+2 frontend-only 已 commit (1978935 + 6a4c772),后端 `:9000 LISTENING` 始终未达成。本轮 session 通过直接 DDL + 旁路脚本完成了 **DB schema + seed 修复** (见 completed_work),但 backend 启动后半段仍 hang。

```
进度: [██████░░░░] 60%
DB 现状: ✅ 全模块 schema + admin + 36 menus + 7 sys_config + 11 cron
Backend: ❌ :9000 未 LISTENING (hang at createRequestEncryptionToggleConfig)
```

</current_state>

<completed_work>

**Task 1 + 2 (Phase 58 frontend):**
- Task 1: apikey.ts three CRUD ops align to POST routes — commit `1978935`
- Task 2: types/apikey.ts + index.tsx field names snake→camelCase — commit `6a4c772`

**Task 3 (本 session 完成的 dev env / DB 修复):**
- ✅ DB schema 补漏: 直接 pgx DDL 建 `sys_api_keys` + `sys_api_key_usage_logs` + `sys_config`
- ✅ DB seed: admin user (SM3 hash admin123) + 36 menus + 2 roles + user-role link + 11 cron jobs + 7 sys_config + AD auth config
- ✅ AutoMigrate 注册列表补 `&models.APIKey{}` + `&models.APIKeyUsageLog{}` + `&models.Config{}` (`database.go:346-348`)
- ✅ 加 `SKIP_AUTOMIGRATE=true` 旁路开关 + `BootstrapMissingTables()` inline DDL 方法
- ✅ Build 成功 (`xingran-backend.exe` 127MB)

</completed_work>

<remaining_work>

**Phase 58:**
- Task 3 human-verify (SC#1-SC#4 + 排序): 需 backend `:9000 LISTENING` → Supabase pooler hang 未解

**Phase 60:** 完成 (commits 6324e45 + 6891936 + c7d3144)
**Phase 61:** 待启动 (AUTH-04 资源级权限矩阵 + QUAL-03 限流生产调优,unconditional per Phase 60 AUTH-03=启用)

**未 commit 改动:**
- `internal/core/db/database.go`: AutoMigrate 列表补漏 + BootstrapMissingTables + SKIP_AUTOMIGRATE 分支
- `internal/core/core.go`: initDBAndData SKIP_AUTOMIGRATE 分支调用
- `pkg/logger/logger.go`: fileMirrorHook + Fatalf 修复
- `internal/core/db/database.go`: `PrepareStmt: false` (前几轮已加)
- `scripts/dbbootstrap/main.go`: pgx 直连 DDL 临时脚本 (新增)
- `scripts/seedmain/main.go`: 调 init_data seed 临时脚本 (新增)
- `xingran-react-frontend/src/components/layout/InnovativeLayout.tsx` (M, pre-existing)
- `xingran-react-frontend/src/pages/login/index.tsx` (M, pre-existing)

</remaining_work>

<decisions_made>

- **CONTRACT-01** 选 Option A (前端改 POST 对齐 v1.6 Router Pattern) — 后端零改动
- **CONTRACT-02** 改前端 types+index.tsx 字段名 camelCase 对齐后端 json tag
- **Dev DB 选 Supabase (hosted Postgres)** — 用户偏好; SQLite 被否决 (jsonb 不兼容)
- **Dev Cache 选 Upstash Redis (TLS-required)** — 用户偏好
- **Supabase pooler 选 Session mode (5432)** — Transaction mode (6543) 有 EAUTHTIMEOUT 风险
- **`SKIP_AUTOMIGRATE` + `BootstrapMissingTables` 作为 dev 工具** — 临时绕过 Supabase pooler 上 GORM AutoMigrate hang
- **不动 AutoMigrate model 列表也算 P0 bug 修复** — 新部署/clean DB 需要 `models.APIKey/Config` 自动建表

</decisions_made>

<blockers>

- **🔴 BLOCKER-1 (technical)**: Backend 启动 hang 在 `createRequestEncryptionToggleConfig` 之后。GORM session 在 Supabase Session pooler 上多次写操作后失活,`pg_stat_activity` 中 backend 连接消失但 Go 进程仍在。**workaround**: 切 Transaction pooler (6543) + 加连接 keepalive/retry,或 init_data 拆批
- **🟡 BLOCKER-2 (advisory)**: cleanupOldConstraints 内部若表存在会发 ALTER TABLE DROP CONSTRAINT (~20 条),在 pooler 上也可能 hang。AutoMigrate 之前的 hang 实际上是它还是 AutoMigrate 本体未确认

</blockers>

## Required Reading (in order)

1. `.planning/phases/58-route-contract-alignment/58-01-PLAN.md` — Phase 58 计划 (§4 human-verify 验收项)
2. `.planning/STATE.md` — v1.21 milestone 状态, Phase 60 已完成
3. `.planning/HANDOFF.json` — 机器可读 state 快照
4. `internal/core/db/database.go:293-426` — AutoMigrate 当前注册列表 + BootstrapMissingTables + SKIP 分支
5. `internal/core/db/init_data.go:13-65` — initData 顺序, `createRequestEncryptionToggleConfig` (line 55) 是 hang 候选点
6. `internal/core/core.go:258-291` — `initDBAndData` 的 SKIP_AUTOMIGRATE 分支

## Critical Anti-Patterns (do NOT repeat these)

- **ANTI-PATTERN**: dev env 改动混入 Phase 58 commits → 破坏 backend-zero-changes 不变量 → **MITIGATION**: 走独立 quick task + commit prefix `chore(dev):`
- **ANTI-PATTERN**: 凭证回显到 chat 或 git 跟踪文件 → **MITIGATION**: `.env` gitignored, `configs/config.yaml` 仅 env var 名 + 占位
- **ANTI-PATTERN**: 临时 fix 没 commit 进独立 task → 下次 session 找不到 → **MITIGATION**: SKIP_AUTOMIGRATE 等 dev 改动必须 commit 进独立 quick task
- **ANTI-PATTERN**: AutoMigrate 注册列表继续缺漏 model → **MITIGATION**: 在 GSD planning step 加 lint 校验所有 model.TableName() 在 AutoMigrate list 内

## Infrastructure State

- **分支**: `docs/phase-59-context` (Phase 58 工作分支)
- **DB**: Supabase Session pooler `aws-0-ap-southeast-1.pooler.supabase.com:5432` 凭证在 `.env` (gitignored)
- **Cache**: Upstash Redis `more-anchovy-179877.upstash.io:6379` TLS required
- **Backend binary**: `xingran-backend.exe` 已 build,127MB;跑过但未到 :9000
- **DB 现状**: 75 表 + sys_api_keys + sys_api_key_usage_logs + sys_config = 78 表;admin 用户 + 36 menus + 全部 seed 完成
- **临时脚本**: `scripts/dbbootstrap/` + `scripts/seedmain/` (建议保留作 dev 工具或 commit 后删除)

<context>

mental state: 后端 hang 调试进入死胡同,直接 DDL 旁路救活了 DB schema + seed,但 backend 启动后半段仍 hang,Phase 58 SC#1-SC#4 runtime test 受阻。Phase 60 已完成,Phase 61 待启动。当前 session 把 dev infra (SKIP_AUTOMIGRATE + BootstrapMissingTables + AutoMigrate 列表补漏 + 直接 DDL)落地了,但因 Supabase pooler hang 仍未完全验证 :9000。下次 session 优先:①决定 commit/revert 临时改动 ② 解决 backend hang (尝试 Transaction pooler) ③ 验证 Phase 58 SC#1-SC#4。

context 已 critical (77%),本次暂停。

</context>

<next_action>

Start with:
1. 读本文件 + STATE.md + 58-01-PLAN.md §4 human-verify
2. **决策点**: 当前未 commit 改动 (database.go SKIP_AUTOMIGRATE 分支 + AutoMigrate 列表补漏 + BootstrapMissingTables + logger fileMirrorHook + PrepareStmt:false) 是否作为 `chore(dev): add Supabase pooler compat layer` commit?
3. 试 `DB_PORT=6543` Transaction pooler (在 .env 里改) + build + 启动 backend,看是否绕过 createRequestEncryptionToggleConfig hang
4. 若 :9000 LISTENING 达成 → 跑 Phase 58 SC#1-SC#4 human-verify
5. 若仍 hang → 决定: (a) 接受 dev env 半成品,Phase 58 SUMMARY.md 标注 "runtime test blocked by external Supabase pooler issue"; (b) 删除 dev 改动回滚干净; (c) 升级 Supabase pooler plan 解决外部阻塞
6. 生成 58-01-SUMMARY.md 关闭 Phase 58
7. 启动 Phase 61 规划 (AUTH-04 + QUAL-03)

</next_action>