---
phase: 104-handler-wire
plan: "04"
type: execute
wave: 4
depends_on:
  - "104-01"
  - "104-02"
files_modified:
  - internal/api/v1/operations/building_handler_test.go
  - internal/api/v1/operations/asset_handler_test.go
  - internal/api/v1/monitor/oper_log_handler_test.go
  - internal/api/v1/monitor/login_log_handler_test.go
autonomous: true
requirements:
  - WIRE-01
  - HANDLER-01
  - HANDLER-02
user_setup: []

must_haves:
  truths:
    - "Handler tests assert (HTTPStatus, code, message) triple per D-104-10"
    - "Binding error tests assert code=1001, HTTPStatus=400"
    - "Service error tests assert code=1500, HTTPStatus=500"
  artifacts:
    - path: internal/api/v1/operations/*_test.go
      provides: Handler tests upgraded with new code assertion values
  key_links:
    - from: "*_handler_test.go"
      to: pkg/response/handler_helpers.go
      via: HTTPStatus + code assertions
      pattern: "Equal.*code.*1001|Equal.*code.*1500"
---

<objective>
Upgrade 200+ handler tests to assert new Response.code values per D-104-10. After wire contract change: binding errors → code=1001, service errors → code=1500. Also upgrade monitor handler tests.
</objective>

<context>
@internal/api/v1/operations/building_handler_test.go
@internal/api/v1/operations/asset_handler_test.go
@internal/api/v1/monitor/oper_log_handler_test.go
@internal/api/v1/monitor/login_log_handler_test.go
@pkg/errors/codes.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade operations handler tests with new code assertion values</name>
  <files>internal/api/v1/operations/building_handler_test.go,internal/api/v1/operations/asset_handler_test.go</files>
  <action>
Upgrade handler test assertions from old code values to new business code values per D-104-10 and the wire contract change.

**Code value mapping (D-104-10):**
- Binding error / param error: `code: 1001`, `HTTPStatus: 400`
- Service error (server error): `code: 1500`, `HTTPStatus: 500`
- Not found: `code: 1010`, `HTTPStatus: 404`

**Pattern for operations handler tests:**
In each test file, find assertion patterns like:
- `assert.Equal(t, 400, resp.Code)` → `assert.Equal(t, 1001, resp.Code)` for binding errors
- `assert.Equal(t, 500, resp.Code)` → `assert.Equal(t, 1500, resp.Code)` for service errors
- HTTPStatus assertions should already be correct (400/500/404) — verify and update if needed

**Key files to check:**
- building_handler_test.go — update code assertions in binding error and service error test cases
- asset_handler_test.go — same pattern

Run `go test ./internal/api/v1/operations/... -count=1` after changes to verify.
</action>
  <verify>
    <automated>go test ./internal/api/v1/operations/... -count=1 2>&1 | tail -20</automated>
  </verify>
  <done>Operations handler tests upgraded with new code assertion values (1001/1500/1010); tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade monitor handler tests with new code assertion values</name>
  <files>internal/api/v1/monitor/oper_log_handler_test.go,internal/api/v1/monitor/login_log_handler_test.go</files>
  <action>
Upgrade monitor handler test assertions with new business code values per D-104-10.

**Same code value mapping:**
- Binding error: `code: 1001`, `HTTPStatus: 400`
- Service error: `code: 1500`, `HTTPStatus: 500`
- Not found: `code: 1010`, `HTTPStatus: 404`

In each monitor handler test file:
- Find all `assert.Equal(t, http.StatusBadRequest, statusCode)` and add/verify `assert.Equal(t, 1001, resp.Code)`
- Find all `assert.Equal(t, http.StatusInternalServerError, statusCode)` and add/verify `assert.Equal(t, 1500, resp.Code)`
- For page response tests, ensure the code is 0 for success cases
</action>
  <verify>
    <automated>go test ./internal/api/v1/monitor/... -count=1 2>&1 | tail -20</automated>
  </verify>
  <done>Monitor handler tests upgraded; tests pass</done>
</task>

</tasks>

<verification>
go test ./internal/api/v1/operations/... -count=1
go test ./internal/api/v1/monitor/... -count=1
</verification>

<success_criteria>
- Handler tests assert (HTTPStatus, code, message) triple per D-104-10
- All operations and monitor handler tests pass with new code values
- go build ./... passes
</success_criteria>

<output>
Create `.planning/phases/104-handler-wire/104-04-SUMMARY.md` when done
</output>
