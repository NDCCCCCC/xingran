---
phase: 104-handler-wire
plan: "03"
type: execute
wave: 3
depends_on:
  - "104-01"
files_modified:
  - internal/api/v1/monitor/log_handler.go
  - internal/api/v1/monitor/oper_log_handler.go
  - internal/api/v1/monitor/login_log_handler.go
  - internal/api/v1/monitor/oper_log_router.go
  - internal/api/v1/monitor/login_log_router.go
autonomous: true
requirements:
  - HANDLER-02
user_setup: []

must_haves:
  truths:
    - "MonitorLogHandler[T] generic struct consolidates 5 duplicate methods from oper/login_log handlers"
    - "OperLog.Clean and LoginLog.Clean methods remain in their original files (non-generic)"
    - "LoginLog.UnlockUser remains untouched (Phase 107 TODO-03)"
  artifacts:
    - path: internal/api/v1/monitor/log_handler.go
      provides: Generic MonitorLogHandler[T] with 5 consolidated methods
      min_lines: 80
  key_links:
    - from: monitor/oper_log_handler.go
      to: monitor/log_handler.go
      via: NewMonitorLogHandler[models.OperLog]
      pattern: "MonitorLogHandler"
    - from: monitor/login_log_handler.go
      to: monitor/log_handler.go
      via: NewMonitorLogHandler[models.LoginLog]
      pattern: "MonitorLogHandler"
---

<objective>
Create generic `MonitorLogHandler[T]` in monitor/log_handler.go; migrate OperLogHandler and LoginLogHandler to use it for the 5 duplicate methods (List/GetByID/Delete/BatchDelete/GetByUsername). Retain Clean methods and UnlockUser in their original files per D-104-9.
</objective>

<context>
@internal/api/v1/monitor/oper_log_handler.go
@internal/api/v1/monitor/login_log_handler.go
@internal/api/v1/monitor/oper_log_router.go
@internal/api/v1/monitor/login_log_router.go
@internal/services/monitor/oper_log_service.go
@internal/services/monitor/login_log_service.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create monitor/log_handler.go with generic MonitorLogHandler[T]</name>
  <files>internal/api/v1/monitor/log_handler.go</files>
  <action>
Create a new file `internal/api/v1/monitor/log_handler.go` containing the generic `MonitorLogHandler[T]` struct.

**Interface constraint** — both OperLogService and LoginLogService implement these 5 methods identically:
```go
type MonitorLogService interface {
    List(ctx context.Context, params interface{}) (*PageResult, error)
    GetByID(ctx context.Context, id string) (interface{}, error)
    Delete(ctx context.Context, id string) error
    BatchDelete(ctx context.Context, ids []string) error
    GetByUsername(ctx context.Context, username string) (interface{}, error)
}
```

**MonitorLogHandler[T] struct:**
```go
type MonitorLogHandler[T any] struct {
    service     MonitorLogService
    core        *core.Core
    moduleName  string  // "操作日志" or "登录日志"
}

func NewMonitorLogHandler[T any](svc MonitorLogService, moduleName string) *MonitorLogHandler[T] {
    return &MonitorLogHandler[T]{service: svc, moduleName: moduleName}
}
```

**5 generic methods** (List/GetByID/Delete/BatchDelete/GetByUsername):
- Each method uses `response.HandleJSONBinding` and `response.HandleServiceError` from pkg
- Operlog calls `operlog.Record` with `moduleName` after successful mutations
- LoginLog does NOT call operlog.Record (per existing behavior)

**Non-generic methods kept in original files** per D-104-9:
- `OperLogHandler.Clean` — synchronous audit transaction + post-clean verification
- `LoginLogHandler.Clean` — simple async operlog.Record
- `LoginLogHandler.UnlockUser` — Phase 107 TODO-03 placeholder, untouched
</action>
  <verify>
    <automated>go build ./internal/api/v1/monitor/... 2>&1 | head -20</automated>
  </verify>
  <done>MonitorLogHandler[T] created in log_handler.go; go build passes</done>
</task>

<task type="auto">
  <name>Task 2: Refactor oper_log_handler.go to use MonitorLogHandler[models.OperLog]</name>
  <files>internal/api/v1/monitor/oper_log_handler.go,internal/api/v1/monitor/oper_log_router.go</files>
  <action>
Refactor oper_log_handler.go to use MonitorLogHandler[models.OperLog] for the 5 duplicate methods.

**In oper_log_handler.go:**
- Keep type definition and request structs (OperLogHandler, OperLogListRequest)
- Keep `NewOperLogHandler` constructor (returns *MonitorLogHandler[models.OperLog])
- Keep `WithCore` method
- Keep `Clean` method (non-generic per D-104-9)
- Remove the 5 duplicate methods (List/GetByID/Delete/BatchDelete/GetByUsername) — they are now on MonitorLogHandler[T]

**In oper_log_router.go:**
- Change `operLogHandler := monitor.NewOperLogHandler(operLogService)` to use the new constructor
- Ensure `WithCore` is called after construction for operlog.Record in Clean method
</action>
  <verify>
    <automated>go build ./internal/api/v1/monitor/... 2>&1 | head -20</automated>
  </verify>
  <done>OperLogHandler refactored to use MonitorLogHandler[models.OperLog]; Clean method retained; 5 methods migrated to generic</done>
</task>

<task type="auto">
  <name>Task 3: Refactor login_log_handler.go to use MonitorLogHandler[models.LoginLog]</name>
  <files>internal/api/v1/monitor/login_log_handler.go,internal/api/v1/monitor/login_log_router.go</files>
  <action>
Refactor login_log_handler.go to use MonitorLogHandler[models.LoginLog] for the 5 duplicate methods.

**In login_log_handler.go:**
- Keep type definition and request structs (LoginLogHandler, LoginLogListRequest)
- Keep `NewLoginLogHandler` constructor (returns *MonitorLogHandler[models.LoginLog])
- Keep `WithCore` method
- Keep `Clean` method (non-generic per D-104-9)
- Keep `UnlockUser` method (Phase 107 TODO-03, untouched per D-104-9)
- Remove the 5 duplicate methods (List/GetByID/Delete/BatchDelete/GetByUsername)

**In login_log_router.go:**
- Change `loginLogHandler := monitor.NewLoginLogHandler(loginLogService)` to use the new constructor
- Ensure `WithCore` is called after construction for operlog.Record in Clean method
</action>
  <verify>
    <automated>go build ./internal/api/v1/monitor/... 2>&1 | head -20</automated>
  </verify>
  <done>LoginLogHandler refactored to use MonitorLogHandler[models.LoginLog]; Clean and UnlockUser methods retained; 5 methods migrated to generic</done>
</task>

</tasks>

<verification>
go build ./internal/api/v1/monitor/...
go test ./internal/api/v1/monitor/... -count=1
grep -c "func (h \\*MonitorLogHandler" internal/api/v1/monitor/log_handler.go  # should be 5
</verification>

<success_criteria>
- MonitorLogHandler[T] in log_handler.go implements 5 generic methods
- OperLogHandler uses MonitorLogHandler[models.OperLog]; Clean method remains in oper_log_handler.go
- LoginLogHandler uses MonitorLogHandler[models.LoginLog]; Clean and UnlockUser methods remain in login_log_handler.go
- go build ./internal/api/v1/monitor/... passes
- go test passes
</success_criteria>

<output>
Create `.planning/phases/104-handler-wire/104-03-SUMMARY.md` when done
</output>
