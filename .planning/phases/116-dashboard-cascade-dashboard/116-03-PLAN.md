---
phase: "116-dashboard-cascade-dashboard"
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - xingran-react-frontend/src/components/dashboard/layout/DashboardGrid.tsx
autonomous: true
requirements:
  - DASH-04
must_haves:
  truths:
    - "DashboardGrid.tsx :42 移除 useWindowSize 订阅，改用 useState 初始化 containerWidth（clientWidth 初始化一次，mount 后 resize 由 ResizeObserver 处理）"
    - "DashboardGrid.tsx :49 gridProps 对象（layouts 包装/containerPadding/handleLayoutChange）useMemo 化，deps 收敛为 [widgets, containerWidth, layoutConfig, isEditable, isMobile]"
    - "useWindowSize 从 DashboardGrid.tsx 的 import 语句中移除（删除 useWindowSize 导入）"
    - "DashboardGrid.css 和 DashboardGrid.tsx 同名 CSS Module 零改动"
  artifacts:
    - path: "xingran-react-frontend/src/components/dashboard/layout/DashboardGrid.tsx"
      provides: "DashboardGrid 稳定化：DASH-04"
      contains: "useMemo.*gridProps"
    - path: "xingran-react-frontend/src/components/dashboard/layout/DashboardGrid.tsx"
      provides: "containerWidth useState 初始化一次"
      contains: "useState.*clientWidth"
  key_links:
    - from: "DashboardGrid.tsx containerWidth"
      to: "ResizeObserver callback"
      via: "setContainerWidth(entry.contentRect.width)"
      pattern: "ResizeObserver"
    - from: "DashboardGrid.tsx gridProps"
      to: "layouts / containerPadding / handleLayoutChange"
      via: "useMemo deps [widgets, containerWidth, layoutConfig, isEditable, isMobile]"
      pattern: "useMemo.*gridProps"
---

<objective>
DASH-04：DashboardGrid.tsx :42,49 移除 useWindowSize 订阅 + gridProps useMemo 化。移除 useWindowSize 依赖后 containerWidth 初始化一次（window.innerWidth），mount 后 resize 由 ResizeObserver 处理；gridProps 三件套（layouts 包装/containerPadding/handleLayoutChange）useMemo 化使拖拽期间回调引用稳定。

Purpose: 消除 useWindowSize 整店订阅造成的窗口尺寸变化级联重渲；gridProps useMemo 化确保 layouts/handleLayoutChange 引用在依赖未变时稳定，拖拽回调不引发无关 widget 重渲。
Output: DashboardGrid.tsx 单一文件改动，测试零回归。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-dashboard-cascade-dashboard/116-CONTEXT.md
@.planning/ROADMAP.md Phase 116 段
@.planning/REQUIREMENTS.md DASH-04
@.planning/phases/116-dashboard-cascade-dashboard/116-CONTEXT.md Grey Areas 段

<interfaces>
<!-- 核验完毕（2026-09-14）：DashboardGrid.tsx 现状与改造目标 -->

DashboardGrid.tsx :42,49 现状：
const windowSize = useWindowSize();  // 整店订阅 windowSize
const viewMode = useDashboardStore((s) => s.viewMode);  // 已逐字段 selector
...
const containerWidth = useState<number>(windowSize.width);  // 初始化一次
const layouts = useMemo(...);  // 已有 useMemo，但 gridProps 本身没有
...
const gridProps: ExtendedResponsiveProps = {  // :178-199
  className: "layout",
  width: containerWidth,
  layouts: { lg: layouts },
  breakpoints,
  cols,
  rowHeight: layoutConfig.rowHeight,
  margin: layoutConfig.margin,
  containerPadding: [16, 16],  // 常量，应提出
  isDraggable: isEditable && layoutConfig.draggable && !isMobile,
  isResizable: isEditable && layoutConfig.resizable && !isMobile,
  onLayoutChange: handleLayoutChange,  // callback，需稳定
  onDragStart: handleDragStart,
  onDragStop: handleDragStop,
  onResizeStart: handleResizeStart,
  onResizeStop: handleResizeStop,
  compactType: "vertical",
  preventCollision: true,
  draggableHandle: ".widget-drag-handle",
  useCSSTransforms: true,
  children,
};

改造后目标：
1. 删除 `const windowSize = useWindowSize();` 整店订阅
2. containerWidth useState 初始化：`useState<number>(() => typeof window !== 'undefined' ? window.innerWidth : 1200)`（SSR 安全）
3. 删除 useWindowSize 导入
4. gridProps 整体 useMemo 化，deps：[widgets, containerWidth, layoutConfig, isEditable, isMobile]

关键：containerPadding: [16, 16] 是常量对象，每次渲染都创建新引用，应提取为 useMemo 外部常量或 useMemo 本身 deps 中不含它所以也会稳定。

layouts useMemo（:112-143）已存在且 deps 为 [widgets, isMobile, getColumns]——handleLayoutChange 回调引用稳定性由 gridProps useMemo 提供。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DashboardGrid 移除 useWindowSize 订阅 + gridProps useMemo 化</name>
  <files>xingran-react-frontend/src/components/dashboard/layout/DashboardGrid.tsx</files>
  <read_first>
    - xingran-react-frontend/src/components/dashboard/layout/DashboardGrid.tsx（:8 import useWindowSize、:42 useWindowSize 调用、:49 containerWidth useState、:178-199 gridProps、:112-143 layouts useMemo）
    - xingran-react-frontend/src/hooks/useWindowSize.ts（确认无其他使用方，或确认 dashboard-system 外使用方不在本 plan 范围）
    - xingran-react-frontend/src/components/dashboard/layout/DashboardGrid.css（确认无 CSS 依赖）
  </read_first>
  <action>
对 DashboardGrid.tsx 做以下改造：

1. 删除 useWindowSize import（:10）：
   - 删除 `import { useWindowSize } from "@/hooks/useWindowSize";`

2. 删除 useWindowSize 调用（:42）：
   - 删除 `const windowSize = useWindowSize();`

3. 改造 containerWidth useState 初始化（:49）：
   - 旧：`useState<number>(windowSize.width)`（依赖 windowSize）
   - 新：`useState<number>(() => typeof window !== 'undefined' ? window.innerWidth : 1200)`（初始化一次，mount 后 resize 由 ResizeObserver 处理）

4. 提取 containerPadding 为常量（避免每次渲染重新创建对象）：
   - 在组件内、useMemo 定义前添加：`const CONTAINER_PADDING: [number, number] = [16, 16];`

5. gridProps useMemo 化（将 :178-199 的内联对象改为 useMemo）：
   ```typescript
   const gridProps = useMemo<ExtendedResponsiveProps>(() => ({
     className: "layout",
     width: containerWidth,
     layouts: { lg: layouts },
     breakpoints,
     cols,
     rowHeight: layoutConfig.rowHeight,
     margin: layoutConfig.margin,
     containerPadding: CONTAINER_PADDING,
     isDraggable: isEditable && layoutConfig.draggable && !isMobile,
     isResizable: isEditable && layoutConfig.resizable && !isMobile,
     onLayoutChange: handleLayoutChange,
     onDragStart: handleDragStart,
     onDragStop: handleDragStop,
     onResizeStart: handleResizeStart,
     onResizeStop: handleResizeStop,
     compactType: "vertical",
     preventCollision: true,
     draggableHandle: ".widget-drag-handle",
     useCSSTransforms: true,
     children,
   }), [containerWidth, layouts, layoutConfig, isEditable, isMobile, handleLayoutChange, handleDragStart, handleDragStop, handleResizeStart, handleResizeStop, children]);
   ```
   - deps 包含 handleLayoutChange 等回调（这些回调自身通过 useCallback 包装引用稳定，见下步）

6. 验证回调引用稳定性（handleLayoutChange / handleDragStart / handleDragStop / handleResizeStart / handleResizeStop 已在文件内通过 useCallback 包装，deps 均为空或稳定值，引用稳定）

约束：
1. 删除 useWindowSize 后，containerWidth 仅通过 ResizeObserver 在 mount 后更新（与 Grey Areas 设计一致）
2. gridProps useMemo 后，layouts/widget 依赖未变时不重建对象，拖拽回调引用稳定
3. handleLayoutChange 依赖不变（仅依赖 currentLayout），useCallback 包装确保稳定
4. CSS 文件零改动（DashboardGrid.css）
5. 不删除 useWindowSize hook 本身（其他模块可能使用，不在本 phase 范围内）
6. 不修改 Responsive 组件的 JSX 传参方式（`<Responsive {...gridProps}>` 保持不变）
  </action>
  <verify>
    <automated>grep -c "useWindowSize" xingran-react-frontend/src/components/dashboard/layout/DashboardGrid.tsx && grep -c "useMemo.*gridProps" xingran-react-frontend/src/components/dashboard/layout/DashboardGrid.tsx</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -c "useWindowSize" xingran-react-frontend/src/components/dashboard/layout/DashboardGrid.tsx` == 0
    - source-assertion: `grep -c "useMemo.*gridProps" xingran-react-frontend/src/components/dashboard/layout/DashboardGrid.tsx` == 1
    - source-assertion: `grep -c "useState.*window\\.innerWidth" xingran-react-frontend/src/components/dashboard/layout/DashboardGrid.tsx` == 1
    - cli: cd xingran-react-frontend && npm run type-check exit 0
    - cli: cd xingran-react-frontend && npm run lint -- --max-warnings 0 xingran-react-frontend/src/components/dashboard/layout/DashboardGrid.tsx exit 0
  </acceptance_criteria>
  <done>
DashboardGrid 移除 useWindowSize 订阅，containerWidth 初始化一次，gridProps useMemo 化；type-check + lint 通过。
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→browser API | window.innerWidth 初始化一次（SSR 安全），后续 resize 由 ResizeObserver 处理，无 useWindowSize 整店订阅 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-03-A | Availability（功能降级） | containerWidth 初始化 | mitigate | useState 初始化使用 window.innerWidth（SSR 保护：typeof window check），ResizeObserver 处理后续 resize；trade-off：mount 时窗口 resize 不感知（符合 Grey Areas 定案） |
| T-116-03-B | DoS（无限重渲） | gridProps 重建 | mitigate | useMemo deps 包含所有依赖项，layouts/widget 不变时 gridProps 引用稳定，拖拽回调不引发级联 |
| T-116-03-C | Tampering（功能破坏） | useWindowSize 删除 | mitigate | 仅删除 DashboardGrid.tsx 中的 useWindowSize 导入和调用，不删除 hook 本身（其他使用方不在本 phase 范围） |
| T-116-03-SC | Supply Chain | 依赖安装 | mitigate（不适用） | 本 plan 零新增依赖 |
</threat_model>

<verification>
- `grep -c "useWindowSize" xingran-react-frontend/src/components/dashboard/layout/DashboardGrid.tsx` == 0
- `grep -c "useMemo<ExtendedResponsiveProps>" xingran-react-frontend/src/components/dashboard/layout/DashboardGrid.tsx` == 1
- `cd xingran-react-frontend && npm run type-check` exit 0
- `cd xingran-react-frontend && npm run lint -- --max-warnings 0` exit 0
- `cd xingran-react-frontend && npx vitest run xingran-react-frontend/src/pages/dashboard-system/__tests__/` 全绿（如果测试存在）
</verification>

<success_criteria>
1. DashboardGrid.tsx 移除 useWindowSize 整店订阅，containerWidth 初始化一次（DASH-04）
2. gridProps 对象 useMemo 化，拖拽回调引用稳定
3. DashboardGrid.css 零改动
4. type-check + lint 零回归
</success_criteria>

<output>
Create `.planning/phases/116-dashboard-cascade-dashboard/116-03-SUMMARY.md` when done
</output>
