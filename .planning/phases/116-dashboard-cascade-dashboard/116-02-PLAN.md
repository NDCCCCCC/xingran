---
phase: "116-dashboard-cascade-dashboard"
plan: "02"
type: execute
wave: 2
depends_on:
  - "116-01-PLAN.md"
files_modified:
  - xingran-react-frontend/src/store/dashboardStore.ts
autonomous: true
requirements:
  - DASH-02
must_haves:
  truths:
    - "widget 数据 L1 缓存从响应式 widgetDataCache state 移出至模块级 Map（noticeStore P1-M4 先例）——dashboardStore widgetDataCache state 字段删除，模块级 widgetDataCache Map 替代"
    - "getCachedWidgetData / cacheWidgetData / clearWidgetCache / updateWidgetData 四个 action 改为对模块级 Map 的操作，不触发任何 Zustand 响应式订阅"
    - "useWidgetData 改造后通过 getState().getCachedWidgetData 读取模块级 Map，行为与改造前完全一致"
    - "persist partialize 仍包含 viewMode / showGridLines / showWidgetBorders，widget 数据不持久化（符合原设计意图）"
  artifacts:
    - path: "xingran-react-frontend/src/store/dashboardStore.ts"
      provides: "widget L1 缓存移出响应式 state：DASH-02"
      contains: "const widgetDataCache"
    - path: "xingran-react-frontend/src/hooks/useWidgetData.ts"
      provides: "useWidgetData 依赖 Plan 01 改造后的 getState().getCachedWidgetData"
      contains: "useDashboardStore.getState().getCachedWidgetData"
  key_links:
    - from: "useWidgetData.ts fetchWidgetData"
      to: "模块级 widgetDataCache Map"
      via: "useDashboardStore.getState().getCachedWidgetData(widget.id)"
      pattern: "useDashboardStore\\.getState\\(\\)"
    - from: "dashboardStore actions"
      to: "模块级 widgetDataCache Map"
      via: "直接对 Map 读写，无 set() 触发响应式"
      pattern: "widgetDataCache\\.(get|set|delete|clear)"
---

<objective>
DASH-02：widget 数据 L1 缓存从 dashboardStore 响应式 state（widgetDataCache Map）移出至模块级 Map，参考 noticeStore P1-M4 先例。getCachedWidgetData / cacheWidgetData / clearWidgetCache / updateWidgetData 四个 action 改为操作模块级 Map，不触发 Zustay 响应式订阅。useWidgetData 已在 Plan 01 中改为 getState().getCachedWidgetData 调用，本 plan 仅改 store 内部实现。

Purpose: widget 数据写入模块级 Map 后，轮询刷新不触发 dashboardStore 任何订阅（widgetDataCache 退出响应式 state 后，cacheWidgetData 的 set() 调用不再触发任何组件重渲）；配合 Plan 01 selector 化，真正实现"单个 widget 数据更新只重渲该 widget"。
Output: dashboardStore.ts 改动，persist 配置调整，既有测试零回归。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-dashboard-cascade-dashboard/116-CONTEXT.md
@.planning/ROADMAP.md Phase 116 段
@.planning/REQUIREMENTS.md DASH-02
@.planning/phases/116-dashboard-cascade-dashboard/116-01-PLAN.md（DASH-01 已将 useWidgetData 改造为 getState 内部调用，本 plan 仅改 store 内部）

<interfaces>
<!-- 核验完毕（2026-09-14）：dashboardStore widgetDataCache 相关代码段 -->

dashboardStore.ts 现状：
- widgetDataCache: Map<string, {data: unknown, timestamp: number}> 声明在 state 接口（:70-76）和 initialState（:143）
- cacheWidgetData（:405-413）：set((state) => { newCache = new Map(state.widgetDataCache); newCache.set(...); return { widgetDataCache: newCache } })
- getCachedWidgetData（:416-427）：get().widgetDataCache.get() 读取 + 过期检查
- clearWidgetCache（:429-438）：set((state) => { newCache = new Map(state.widgetDataCache); ... })
- updateWidgetData（:477-485）：set((state) => { newCache = new Map(state.widgetDataCache); newCache.set(...); return { widgetDataCache: newCache } })

noticeStore P1-M4 模式（:8-11 模块级 Set）：
const rpaProgressListeners = new Set<RPAProgressCallback>();

注意：noticeStore P1-M4 是 Set，本 plan 是 Map（widgetDataCache 天然需要 key-value），实现完全兼容该模式。

改造后 dashboardStore.ts 结构：
- 删除 DashboardState interface 中的 widgetDataCache 字段
- 删除 initialState 中的 widgetDataCache: new Map() 初始化
- 在 store 创建之前（模块级）声明：const widgetDataCache = new Map<string, {data: unknown, timestamp: number}>();
- cacheWidgetData：直接 widgetDataCache.set(widgetId, {data, timestamp: Date.now()})，无 set() 调用
- getCachedWidgetData：直接 widgetDataCache.get(widgetId) 读取，无 get() 穿透 state 读取
- clearWidgetCache：直接 widgetDataCache.delete() / widgetDataCache.clear()
- updateWidgetData：直接 widgetDataCache.set(widgetId, {data, timestamp: Date.now()})
- persist partialize 维持不变（widgetDataCache 本就不在 persist 范围）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: widget L1 缓存移出响应式 state，改为模块级 Map</name>
  <files>xingran-react-frontend/src/store/dashboardStore.ts</files>
  <read_first>
    - xingran-react-frontend/src/store/dashboardStore.ts（DashboardState interface :54-87、initialState :127-154、cacheWidgetData :405-413、getCachedWidgetData :416-427、clearWidgetCache :429-438、updateWidgetData :477-485、persist config :492-501）
    - xingran-react-frontend/src/store/noticeStore.ts（:8-11 P1-M4 模块级 Set 模式，参考其位置与声明方式）
    - xingran-react-frontend/src/hooks/useWidgetData.ts（确认 Plan 01 改造后的调用路径：useDashboardStore.getState().getCachedWidgetData）
  </read_first>
  <action>
对 dashboardStore.ts 做以下改造：

1. 删除 DashboardState interface 中的 widgetDataCache 字段（:70-76 从 interface 中移除）

2. 删除 initialState 中的 widgetDataCache: new Map() 初始化（:143 从 initialState 中移除）

3. 在 store 创建之前（文件顶部，import 之后），声明模块级 Map：
   ```typescript
   // 模块级 widget L1 缓存（不进入响应式 state，参考 noticeStore P1-M4）
   const widgetDataCache = new Map<string, { data: unknown; timestamp: number }>();
   ```
   放置位置：在 import 语句之后，export const useDashboardStore 之前。

4. 改造 cacheWidgetData action（:405-413）：
   - 旧：set((state) => { newCache = new Map(state.widgetDataCache); ...; return { widgetDataCache: newCache } })
   - 新：直接 widgetDataCache.set(widgetId, { data, timestamp: Date.now() });（无 set() 调用）
   - 返回 undefined（Zustand action 可不返回 state）

5. 改造 getCachedWidgetData action（:416-427）：
   - 旧：从 get().widgetDataCache.get() 读取
   - 新：从模块级 widgetDataCache.get() 读取
   - 过期逻辑不变（Date.now() - cached.timestamp > cacheExpiry）

6. 改造 clearWidgetCache action（:429-438）：
   - 旧：set() 创建 new Map 操作
   - 新：直接 widgetDataCache.delete(widgetId) / widgetDataCache.clear()

7. 改造 updateWidgetData action（:477-485）：
   - 旧：set() 创建 new Map 操作
   - 新：直接 widgetDataCache.set(widgetId, { data, timestamp: Date.now() })

8. persist partialize 配置（:492-501）检查：
   - 确认 widgetDataCache 不在 partialize 中（widget 数据本就不应持久化）
   - 保持 viewMode / showGridLines / showWidgetBorders 三字段持久化

约束：
1. 模块级 Map 与 Zustand store 完全解耦——widget 数据写入/读取不触发任何组件订阅
2. getCachedWidgetData 仍返回 null | unknown（接口契约不变）
3. cacheWidgetData / clearWidgetCache / updateWidgetData 仍为 store action（Zustand action 规范：第一个参数是 set，第二个是 get）
4. 禁止在 action 内部调用 set() 修改响应式 state（违反会导致订阅触发）
5. DashboardStore 类型定义中 widgetDataCache 字段删除后，getCachedWidgetData 内部 get().x 调用需改为直接读模块级 Map
6. 如后续需要类型断言（Map.get 返回 undefined 而非 null），保持返回 null 语义
  </action>
  <verify>
    <automated>grep -c "widgetDataCache: Map" xingran-react-frontend/src/store/dashboardStore.ts && grep -c "^  const widgetDataCache = new Map" xingran-react-frontend/src/store/dashboardStore.ts</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -c "^  const widgetDataCache = new Map" xingran-react-frontend/src/store/dashboardStore.ts` == 1（模块级声明恰好 1 个）
    - source-assertion: `grep -c "widgetDataCache: Map" xingran-react-frontend/src/store/dashboardStore.ts` == 0（state interface 中无此字段）
    - source-assertion: `grep -c "set.*widgetDataCache" xingran-react-frontend/src/store/dashboardStore.ts` == 0（无 set() 调用）
    - source-assertion: `grep -c "new Map(state.widgetDataCache)" xingran-react-frontend/src/store/dashboardStore.ts` == 0
    - cli: cd xingran-react-frontend && npm run type-check exit 0
  </acceptance_criteria>
  <done>
widget L1 缓存移出响应式 state；模块级 widgetDataCache Map 替代响应式 state；所有缓存 action 改为直接操作 Map，不触发 Zustand 订阅；type-check 通过。
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Zustand store | 纯前端内部状态重构，不触网络/认证/输入处理 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-02-A | Information Loss（数据丢失） | 模块级 Map SSR | mitigate | 模块级 Map 在 browser 环境运行，dashboard 是 SPA 页面无 SSR 问题；注意 fast-refresh 会重置（符合 widget 数据语义） |
| T-116-02-B | Tampering（行为漂移） | cacheWidgetData 返回值 | mitigate | cacheWidgetData 改造后无返回值，Zustand action 规范允许；调用方 useWidgetData 不依赖返回值 |
| T-116-02-C | Consistency（数据不一致） | getCachedWidgetData 过期检查 | mitigate | 过期检查逻辑（5min TTL）原样保留，clearWidgetCache 调用也原样改造为 Map.delete/clear |
| T-116-02-SC | Supply Chain | 依赖安装 | mitigate（不适用） | 本 plan 零新增依赖，纯 Zustand 内部重构 |
</threat_model>

<verification>
- `grep -c "^  const widgetDataCache = new Map" xingran-react-frontend/src/store/dashboardStore.ts` == 1
- `grep -c "widgetDataCache: Map" xingran-react-frontend/src/store/dashboardStore.ts` == 0（state interface 清干净）
- `grep -c "set.*widgetDataCache" xingran-react-frontend/src/store/dashboardStore.ts` == 0（无 set 触发响应式）
- `cd xingran-react-frontend && npx vitest run src/store/dashboardStore.test.ts` 全绿（如果存在）
- `cd xingran-react-frontend && npm run type-check` exit 0
</verification>

<success_criteria>
1. widgetDataCache 从 DashboardState interface 删除，模块级 Map 替代（DASH-02）
2. cacheWidgetData / getCachedWidgetData / clearWidgetCache / updateWidgetData 四个 action 不再调用 set() 触发 Zustand 响应式订阅
3. useWidgetData 通过 getState().getCachedWidgetData 读取模块级 Map，行为与改造前完全一致
4. persist partialize 仍包含 viewMode / showGridLines / showWidgetBorders（widget 数据不持久化）
5. type-check + 既有测试零回归
</success_criteria>

<output>
Create `.planning/phases/116-dashboard-cascade-dashboard/116-02-SUMMARY.md` when done
</output>
