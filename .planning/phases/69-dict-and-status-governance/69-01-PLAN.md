---
phase: 69-dict-and-status-governance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/models/dict.go
  - internal/models/log.go
  - internal/models/vdi.go
  - internal/models/notice_enhanced.go
  - internal/models/dashboard.go
  - internal/models/operations/infopoint.go
  - internal/models/status_constants_test.go
  - scripts/check-status-literals.sh
  - internal/services/system/dict_service.go
  - internal/services/system/post_service.go
  - internal/services/system/role_service.go
  - internal/services/system/user_service.go
  - internal/services/system/widget_data_fetcher.go
autonomous: true
requirements: [DICT-01]

must_haves:
  truths:
    - "DICT-01: `internal/models` 是状态常量唯一真相源——不新建 internal/constants 平行包；缺失实体常量（DictStatus / Notice / VDIServer / InfoPoint / OperLog 成败）按 base.go 风格补齐"
    - "DICT-01: `internal/models/status_constants_test.go` 以 AST 双向断言锁住 watched 常量家族的值与数量（改值/删常量/家族内偷加常量都会 fail），`go test ./internal/models/ -run TestStatusConstants` 全绿"
    - "DICT-01: `scripts/check-status-literals.sh` 守护脚本以 ratchet 白名单运行退出码 0——白名单外文件出现裸 status 字面量（含 map/JSON 形态）、或白名单内文件命中数超基线即非零退出"
    - "DICT-01: 批 1 完成——internal/services/system/ 的 dict/post/role/user/widget_data_fetcher service 全部裸 `status = 0/1`（含 CASE WHEN）与 `Status: 0/1` 字面量替换为 models 常量引用，守护白名单移除批 1 文件，`go build ./...` + `go test ./internal/services/system/` 全绿"
  artifacts:
    - path: "internal/models/status_constants_test.go"
      provides: "models 状态常量家族锁值 regression test（operlog regression_test 模式改造）"
      contains: "TestStatusConstants"
    - path: "scripts/check-status-literals.sh"
      provides: "裸 status 字面量防回归守护（grep + ratchet 白名单）"
      contains: "geocoding_service.go"
    - path: "internal/models/dict.go"
      provides: "DictType/DictData status 常量（当前缺失）"
      contains: "DictStatusNormal"
  key_links:
    - from: "internal/services/system/dict_service.go"
      to: "internal/models/dict.go (DictStatus 常量)"
      via: "CASE WHEN 聚合替换 fmt.Sprintf + int(models.DictStatusNormal)"
      pattern: "models\\.DictStatus"
    - from: "internal/models/status_constants_test.go"
      to: "internal/models/*.go + internal/models/operations/*.go"
      via: "go/ast parser.ParseFile 扫描包目录，前缀过滤 watched 家族"
      pattern: "parser\\.ParseFile"
    - from: "scripts/check-status-literals.sh"
      to: "internal/api/v1/ + internal/services/ 源码"
      via: "grep 模式扫描 + 嵌入式白名单比对"
      pattern: "status = [0-9]"
---

# Phase 69 Plan 01: 后端常量真相源收口 — Wave 0 锁值测试 + 守护脚本 + 批 1 替换

**Status:** READY FOR EXECUTION
**Created:** 2026-08-19（2026-08-19 revision：files_modified 移除 base.go（只读母版）；守护脚本增加第四条 map/JSON 命中模式）
**Source:** ROADMAP Phase 69（DICT-01）+ 69-RESEARCH.md A1/A2/A3 + 69-PATTERNS.md
**决策依据:** orchestrator 规划要点 1 —— models 层已有 11 组常量（86 个枚举 type），欠账在消费端；不新建 internal/constants（避免第二真相源）

<objective>

建立 DICT-01 的三层地基并完成第一批替换：(1) 补齐 models 层缺失实体的状态常量；(2) `status_constants_test.go` 以 operlog regression_test 模式锁值（防后人改值/删常量）；(3) `scripts/check-status-literals.sh` ratchet 白名单守护脚本（防新增裸字面量回归，含 map/JSON 形态）；(4) 批 1 —— `internal/services/system/` 6 个 service 文件的裸 0/1 字面量替换为常量引用。

Purpose: 本 phase 所有后续替换（69-03/69-04/69-05 批 2-4）依赖本 plan 交付的常量集、锁值测试与守护脚本；没有锁值测试，常量值可被静默改动；没有守护脚本，替换成果无法防回归。69-02（migration_208 字典 seed）同样依赖本 plan 的 DictStatus 常量家族。
Output: 2 个新文件（status_constants_test.go / check-status-literals.sh）+ models 常量补齐（4-6 处 model 文件）+ 批 1 的 6 个 service 文件替换，3-4 个独立 commit，每 commit 边界 `go build ./...` 常绿。

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/69-dict-and-status-governance/69-RESEARCH.md
@.planning/phases/69-dict-and-status-governance/69-PATTERNS.md

<interfaces>
<!-- 关键现状契约（已从代码库提取，executor 无需再探索） -->

`internal/models/base.go:44-135` 常量定义风格（补缺常量必须照抄此风格——type + const 块 + 行尾中文 label；本 plan 只读 base.go 作母版，不修改该文件）:

```go
// UserStatus 用户状态枚举
type UserStatus int

const (
	UserStatusEnabled  UserStatus = 0 // 启用
	UserStatusDisabled UserStatus = 1 // 禁用
)

// RoleStatus 角色状态枚举
type RoleStatus int

const (
	RoleStatusEnabled  RoleStatus = 0 // 正常
	RoleStatusDisabled RoleStatus = 1 // 停用
)
```

已确认存在的常量家族（PATTERNS 核实，**不要重定义**）:
- base.go: Gender(0/1/2 男/女/保密)、UserStatus、RoleStatus、DataScope、MenuType、VisibleType(VisibleShow=1/VisibleHidden=0 反转例外)、MenuStatus、DeptStatus、PostStatus、ConfigType(Y/N)、ConfigIsSystem(0/1)
- internal/models/operations/building.go:11-12 (BuildingStatus)、operations/dedicated_line.go:11-14 (LineStatus Normal=0/Fault=1/Disabled=2)
- internal/models/operations.go (package models 旧版无注释): FloorStatus:3-7、RoomStatus:28-32、RoomDeviceStatus:49-54(Normal=0/Fault=1/Scrap=2)
- internal/models/operations/server_room.go:7-13 (package operations 子包版 RoomStatus 带注释)
- internal/models/workstation.go:4-18 (WorkstationType/WorkstationStatus)、network_device.go (DeviceType 5 值/DeviceStatus 3 态)、config_execution.go:19-27 (ExecutionStatus 0-4)、knowledge.go:16-17 (KnowledgeArticleStatus Draft=0/Published=1)、notice_enhanced.go:23-26 (PublishStatus Draft=0/Published=1/Scheduled=2/Withdrawn=3)、log.go:51-56 (JobStatusNormal=0 正常/JobStatusPause=1 暂停)

**CRITICAL 双定义陷阱（批 2 起生效，此处只做登记）**: RoomStatus/FloorStatus 等在 `internal/models/operations.go`（package models）与 `internal/models/operations/*.go`（package operations）双定义。引用时按 model struct 实际所在包，禁止新造第三份。

`internal/utils/operlog/regression_test.go` 锁值三件套（T2 测试的改造母版）:
- :45-71 期望值 map（全量枚举，缺失/新增都 fail）
- :76-105 双向断言（expected→actual 查缺失与改值；actual→expected 查偷加）
- :229-282 readOperTypeConsts：parser.ParseFile + 遍历 *ast.GenDecl{Tok: token.CONST} + 名称前缀过滤。改造点：从单文件改为 filepath.Glob 扫 `internal/models/*.go` + `internal/models/operations/*.go` 两个目录（排除 _test.go），前缀过滤改为「watched 家族名集合」而非单一前缀。

批 1 替换目标态范本 `internal/services/system/department_cache_impl.go:65`:
```go
query = query.Where("status = ?", models.DeptStatusNormal)
```

批 1 CASE WHEN 聚合现状（dict_service.go:52-53 为例，post_service.go:66-67 / role_service.go:55-56 同型）:
```go
"COALESCE(SUM(CASE WHEN status = 0 THEN 1 ELSE 0 END), 0) AS active",
"COALESCE(SUM(CASE WHEN status = 1 THEN 1 ELSE 0 END), 0) AS inactive",
```
替换为 fmt.Sprintf("COALESCE(SUM(CASE WHEN status = %d THEN 1 ELSE 0 END), 0) AS active", int(models.DictStatusNormal)) 形态（无法参数化处，RESEARCH Pitfall 5）。

语义簇对照表（RESEARCH A2，全 phase 替换依据）:

| 簇 | 语义 | 例 | 目标常量 | 严禁 |
|----|------|----|---------|------|
| A 通用启停 | 0=正常/启用, 1=停用 | dept/role/post/dict/user/building/floor/workstation/server_room/notice/job/vdi_server | 各实体 Status 常量 | — |
| B 任务状态机 | 0=待执行..4=取消 | config_execution/device_discovery/command_dispatch | ExecutionStatus* | 套 Enabled/Stop |
| C 成功/失败 | 0=成功, 1=失败 | oper_log/job_log/fix_suggestion_monitor | 查/补 log.go 常量 | — |
| D 多态(3+值) | 0/1/2 | dedicated_line/account_pool/device | LineStatus/DutyPool/DeviceStatus 逐实体 | 一刀切 |
| E 语义反转 | 1=正向 | knowledge/notice 已发布 | KnowledgeArticleStatusPublished / PublishStatusPublished | 套 1=停用 |
| F 外部契约 | 第三方返回码 | geocoding_service.go:332 百度 | 不替换，白名单永久豁免 | — |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>T1: 补齐 models 缺失实体常量 + status_constants_test.go 锁值测试</name>
  <files>internal/models/dict.go, internal/models/log.go, internal/models/vdi.go, internal/models/notice_enhanced.go, internal/models/dashboard.go, internal/models/operations/infopoint.go, internal/models/status_constants_test.go</files>
  <read_first>
  - internal/models/base.go（44-135 常量风格母版 + 既有 11 组家族清单——只读，不改）
  - internal/utils/operlog/regression_test.go（全文——锁值测试改造母版：期望 map / 双向断言 / AST 读取器三段）
  - internal/models/log.go（OperLog / Logininfor / JobLog 的 Status 字段语义与注释）
  - internal/models/dict.go（DictType/DictData 的 Status 字段——确认无常量后补）
  - internal/models/vdi.go、internal/models/notice_enhanced.go（Notice 实体 Status 字段；PublishStatus 已存在勿动）
  - internal/models/operations/infopoint.go、internal/models/dashboard.go（批 1 widget_data_fetcher 与后续批次涉及实体的常量现状）
  </read_first>
  <action>
  第一步 常量补齐（逐文件核对后仅在缺失时新增，已有则跳过；一律按 base.go 风格：type 定义 + const 块 + 行尾中文 label 注释，放在对应 model struct 之前）：
  1. internal/models/dict.go：新增 DictStatus 家族（DictStatusNormal = 0 // 正常、DictStatusDisabled = 1 // 停用）——批 1 的 dict_service.go 替换与 69-02 migration_208 seed 必需。
  2. internal/models/log.go：读 OperLog 与 Logininfor 的 Status 语义（成功/失败，簇 C），若无常量则新增 OperLogStatusSuccess = 0 / OperLogStatusFailure = 1（命名以实际语义为准，簇 C 供 69-05 批 4 的 oper_log_handler.go:190 使用）；JobLog 状态同理核对，缺则补。
  3. internal/models/vdi.go：核对 VDIServer 实体 status（簇 A），缺则补 VDIServerStatusNormal/Stopped（或按文件内既有命名风格）。
  4. internal/models/notice_enhanced.go：核对 Notice 实体自身启停 Status（区别于已存在的 PublishStatus），缺则补 NoticeStatusNormal/Stopped。
  5. internal/models/operations/infopoint.go：核对 InfoPoint status（簇 A），缺则补。
  6. internal/models/dashboard.go：核对 widget status 语义（批 1 的 widget_data_fetcher.go 消费），缺则按语义补。
  第二步 写 internal/models/status_constants_test.go（改造 operlog regression_test 三件套）：
  - readStatusConsts 函数：filepath.Glob 扫 internal/models/*.go 与 internal/models/operations/*.go（跳过 _test.go），parser.ParseFile 后收集 *ast.GenDecl{Tok: token.CONST} 下带 watched 家族前缀的常量名→值 map。
  - watched 家族前缀集合：UserStatus、RoleStatus、MenuStatus、DeptStatus、PostStatus、DictStatus、Visible、Gender、ConfigType、ConfigIsSystem、ExecutionStatus、KnowledgeArticleStatus、PublishStatus、NoticeStatus、JobStatus、OperLogStatus、LineStatus、WorkstationType、WorkstationStatus、DeviceType、DeviceStatus、DutyPoolStatus、BuildingStatus、FloorStatus、RoomStatus、RoomDeviceStatus、VDIServerStatus、InfoPointStatus（含本任务新增的家族；DutyPoolStatus/DutyPool 相关常量若在 models/duty.go 用其他命名，以实际文件为准登记进 watched 集合）。批 3（69-04）批内补缺的 ADAccountStatus*/RPACredentialStatus* 等新家族由该 plan 执行时同步登记进此集合与 expectedStatusValues——本任务预留该机制并在文件头注释写明「后续批次新增家族必须同步登记，漏登记即锁值覆盖出现静默缺口」。
  - expectedStatusValues map：对 watched 家族逐常量登记期望值（值以读到的 model 文件为准锁定，登记后即冻结）。至少必须覆盖并锁死：UserStatusEnabled=0、UserStatusDisabled=1、RoleStatusEnabled=0、RoleStatusDisabled=1、VisibleShow=1、VisibleHidden=0（反转例外）、KnowledgeArticleStatusPublished=1（E 簇反转）、PublishStatusDraft=0/Published=1/Scheduled=2/Withdrawn=3、ExecutionStatus 全家族 0-4（B 簇状态机）、LineStatusNormal=0/Fault=1/Disabled=2（D 簇）、JobStatusNormal=0/JobStatusPause=1、GenderMale=0/Female=1/Secret=2、DictStatusNormal=0/Disabled=1，以及本任务新增的全部常量。
  - TestStatusConstantsStability：operlog 式双向断言——expected 中缺失的常量报 "removing is a breaking change"；值不等报 "renumbering would corrupt status semantics"；actual 中多出的 watched 常量报 "add it to expectedStatusValues"。
  - 文件头注释照 operlog regression_test.go:3-23 风格写明「锁什么、为什么锁、哪些语义簇是反转/状态机不可重排」。
  注意：本任务只补常量与测试，不改任何 service/handler；替换在 T3 与 69-03/69-04/69-05 进行。
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/models/ -run TestStatusConstants -v</automated>
  </verify>
  <acceptance_criteria>
  - `go build ./...` 退出码 0
  - `go test ./internal/models/ -run TestStatusConstants` PASS，且测试输出覆盖 UserStatus/RoleStatus/Visible/KnowledgeArticleStatus/ExecutionStatus/LineStatus/DictStatus 家族
  - `grep -c "DictStatusNormal" internal/models/dict.go` ≥ 1
  - status_constants_test.go 内存在 parser.ParseFile 调用与 filepath.Glob 对 internal/models 与 internal/models/operations 两个目录的扫描
  - 人为把 expectedStatusValues 中任一值改错时测试 fail（executor 自测一次后改回）
  - 不存在新建 internal/constants 目录（`ls internal/constants` 失败为正确状态）
  - `git diff --stat internal/models/base.go` 为空（base.go 只读母版，不修改）
  </acceptance_criteria>
  <done>models 层 watched 家族常量齐备且被 AST 测试锁值；DictStatus/OperLog 成败等批 1 与 69-02/69-03/69-04/69-05 依赖的常量全部存在；go build + go test ./internal/models/ 全绿。Commit: feat(models): add missing status constants and AST-locked regression test per DICT-01</done>
</task>

<task type="auto">
  <name>T2: scripts/check-status-literals.sh 守护脚本（ratchet 白名单）</name>
  <files>scripts/check-status-literals.sh</files>
  <read_first>
  - scripts/deploy/setup-server.sh（仓内 bash 脚本头部风格：shebang / set 选项 / 用途注释）
  - internal/services/operations/geocoding_service.go:332 附近（F 簇永久豁免对象——百度 API 返回码比较）
  - internal/services/operations/excel_service.go:1975 与 :2029（map/JSON 形态命中实例：`"status":     0, // 正常`——前三条模式抓不到、第四条模式的目标形态）
  - 69-RESEARCH.md A1 表（四条 grep 模式的口径来源）
  </read_first>
  <action>
  新建 scripts/check-status-literals.sh（bash）：
  1. 头部：shebang + set -euo pipefail + 注释说明用途（防止 internal/api/v1 与 internal/services 下新增裸 status 数字字面量；白名单为 ratchet——每完成一批替换即删除对应条目，最终只剩 F 簇）+ 用法（bash scripts/check-status-literals.sh 检查；--baseline 打印当前每文件命中数供粘贴回白名单）。
  2. 扫描范围：internal/api/v1 与 internal/services 下 --include="*.go"，排除 *_test.go，排除纯注释行（行首 // 开头，剥前导空白后判断）。
  3. 四条命中模式（对齐 RESEARCH A1 口径，含 checker 补正的第四条）：(a) raw SQL 字符串 status = [0-9]；(b) 结构体字面量 Status: *[0-9]（不锚定开头——PublishStatus: 1、JobStatus: 0 等同应命中）；(c) 比较 Status [=!]= *[0-9]；(d) map/JSON 形态 "status": 后跟空白与数字（grep -E '"status":[[:space:]]*[0-9]'——excel_service.go:1975/:2029 的 `"status":     0,` 即此形态，实测 2 处逃逸于前三模式，批 2（69-03）替换）。
  4. 白名单：脚本内嵌入 ALLOWED 列表，每行格式 相对路径=命中数（四条模式的命中数合并计入同一文件条目）。永久豁免条目 internal/services/operations/geocoding_service.go（行尾注释「F 簇：百度 API 返回码契约，不迁移」）。
  5. 判定逻辑：文件有命中但不在白名单 → fail；命中数大于白名单登记数 → fail；小于登记数 → 打印提示「ratchet down: 可把白名单条目降到实际数/删除」但退出码 0。任何 fail 输出违规文件与行内容并 exit 1。
  6. 生成初始基线：先实现 --baseline 模式（输出 path=count 行），运行一次把当前全部命中文件（2026-08-19 实测约 33 文件 / raw SQL 命中约 97 处 + 结构体 22 处 + map/JSON 形态 2 处（excel_service.go:1975/:2029），以实际输出为准）粘贴进 ALLOWED，geocoding_service.go 条目附 F 簇注释。
  7. 可选加分项（不强制）：在脚本头注释写明后续可挂 Phase 63 CI 的方式，但本任务不改任何 CI 配置。
  </action>
  <verify>
    <automated>bash scripts/check-status-literals.sh; echo "exit=$?"; bash scripts/check-status-literals.sh --baseline | wc -l</automated>
  </verify>
  <acceptance_criteria>
  - `bash scripts/check-status-literals.sh` 退出码 0（基线与当前源码一致）
  - `bash scripts/check-status-literals.sh --baseline` 输出 ≥ 30 行（当前欠账文件全量入基线）且输出含 internal/services/operations/excel_service.go（第四条模式生效的证据）
  - 白名单含 internal/services/operations/geocoding_service.go 条目且带 F 簇注释
  - 临时在某 service 文件（如 internal/services/system/post_service.go 尾部）加一行含 status = 1 的字符串后重跑脚本退出码非 0 且输出该文件（自测后撤销）——注意 post_service.go 本身在白名单内，自测应观察到「命中数超基线」同样 fail，两种路径都验证
  - 第四条模式自测：临时在任一 service 文件加一行 `"status": 1,` map 字面量后重跑脚本非零退出（自测后撤销）
  - 脚本排除 *_test.go：向任一 _test.go 加命中行不改变退出码
  </acceptance_criteria>
  <done>守护脚本以当前全量欠账（含 map/JSON 形态）为基线退出码 0；白名单外新增/白名单内超基线均非零退出；F 簇永久豁免登记在案。Commit: chore(scripts): add status-literal ratchet guard per DICT-01</done>
</task>

<task type="auto">
  <name>T3: DICT-01 批 1 — internal/services/system/ 六文件字面量替换</name>
  <files>internal/services/system/dict_service.go, internal/services/system/post_service.go, internal/services/system/role_service.go, internal/services/system/user_service.go, internal/services/system/widget_data_fetcher.go</files>
  <read_first>
  - internal/services/system/dict_service.go:52-53,231-232（CASE WHEN 聚合现状）
  - internal/services/system/post_service.go:66-67、role_service.go:55-56（同型 CASE WHEN）
  - internal/services/system/user_service.go、widget_data_fetcher.go（grep status = [0-9] / Status: *[0-9] 定位全部命中行再读上下文）
  - internal/services/system/department_cache_impl.go:65（替换目标态范本）
  - internal/models/base.go 与 T1 新增常量（替换引用来源；每个文件替换前先读对应 model 常量的中文注释确认语义簇）
  </read_first>
  <action>
  按 T2 守护脚本 --baseline 中 services/system 目录的全部条目逐文件替换（至少覆盖上述 5 文件；若基线显示 services/system 下还有其他文件命中，一并处理）：
  1. dict_service.go：4 处 CASE WHEN（:52-53、:231-232）——status 语义是 sys_dict_type/sys_dict_data 的启停（簇 A），用 T1 新增的 models.DictStatusNormal/DictStatusDisabled 替换，形态为 fmt.Sprintf 且 int() 包裹。
  2. post_service.go / role_service.go：CASE WHEN 聚合分别用 models.PostStatusEnabled/Disabled 与 models.RoleStatusEnabled/Disabled；post_service.go:33-34 的行尾注释（// status = 0）是说明性注释，保留不动（守护脚本已排除注释行）。
  3. user_service.go：命中处按上下文语义用 models.UserStatusEnabled/Disabled；Where 条件一律参数化（Where("status = ?", models.UserStatusEnabled)），不用 Sprintf。
  4. widget_data_fetcher.go：先读命中行上下文确认语义簇——若为通用启停用对应常量，若为 dashboard 专属语义则用 T1 在 dashboard.go 补的常量；语义不明时在 SUMMARY 记录并选最保守映射，不得猜 E/D 簇。
  5. 全文件通例：raw Where 用参数化；CASE WHEN 用 Sprintf + int()；替换后删除行尾原有的 0=正常 之类裸值注释（常量自带语义）；不修改任何逻辑分支、返回值、SQL 结构。
  6. 完成后更新守护白名单：删除 services/system 下已清零文件的条目；重跑脚本退出码 0。
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/services/system/ && bash scripts/check-status-literals.sh && grep -rn 'status = [01]' internal/services/system/ --include="*.go" | grep -v "_test.go" | grep -vc '^\S*:[0-9]*:\s*//'</automated>
  </verify>
  <acceptance_criteria>
  - `go build ./...` 与 `go test ./internal/services/system/` 退出码 0（既有 dict_statistics_test 等不回归）
  - `bash scripts/check-status-literals.sh` 退出码 0 且输出中 services/system 目录条目已从白名单移除（geocoding 保留）
  - grep 非注释 status = 0/1 在 internal/services/system/ 下命中数为 0
  - dict_service.go 内出现 models.DictStatusNormal 与 models.DictStatusDisabled 引用
  - diff 中无逻辑改动（仅常量引用替换 + import 增加 + CASE WHEN 字符串改 Sprintf）
  </acceptance_criteria>
  <done>批 1 清零：services/system 六文件裸字面量全部常量化，守护白名单收缩，构建与包测试全绿。Commit: refactor(system): replace raw status literals with models constants (batch 1) per DICT-01</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| models 常量定义 → services/api 消费 | 常量值一旦被下游 raw SQL 引用，改值即数据语义损坏（锁值测试即为此边界守卫） |
| 守护脚本 → CI/开发者本地 | 脚本输出决定 commit 是否放行；白名单本身是被审对象 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | Tampering | models 状态常量被后人改值/重排（E 簇反转与 B 簇状态机尤其致命） | mitigate | T1: status_constants_test.go AST 双向锁值，改值/删常量/偷加常量即 test fail |
| T-69-02 | Tampering | 替换后新代码重新引入裸字面量（回归；map/JSON 形态绕过字符串/结构体模式） | mitigate | T2: check-status-literals.sh 四模式 ratchet 白名单（含 "status": 数字形态），白名单外新增/超基线非零退出 |
| T-69-03 | Tampering | 字符串拼接 Where 扩散（注入风格习惯） | mitigate | T3: Where 一律参数化；仅 CASE WHEN 无法参数化处用 Sprintf+int(常量)（RESEARCH Pitfall 5） |
| T-69-04 | Denial of Service | 守护脚本误报阻断正常开发 | accept | 白名单 ratchet 机制已把既有欠账合法化；仅「新增/增长」报错，方向单调 |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | 本 plan 零依赖安装（纯 Go 标准库 testing/go/ast + bash/grep），Package Legitimacy Gate 不适用 |
</threat_model>

<verification>

**每任务边界：** T1/T3 `go build ./...` + 相关包 go test；T2 脚本自测双路径（白名单外新增 fail / 白名单内超基线 fail）+ 第四条模式自测。

**Plan 终态：**
1. `go test ./internal/models/ -run TestStatusConstants -v` 全绿且输出覆盖反转/状态机家族
2. `bash scripts/check-status-literals.sh` 退出码 0，白名单不含 services/system 条目
3. `go build ./...` + `go test ./internal/services/system/` 全绿
4. `go test ./...` 全绿（全仓回归，防常量替换波及其他包）

</verification>

<success_criteria>

（ROADMAP Phase 69 SC#1 的 DICT-01 基建部分）

1. models 层常量齐备且为唯一真相源（DictStatus/OperLog 成败/VDIServer/Notice/InfoPoint 补齐），无 internal/constants 平行包 —— 由 T1 交付
2. status_constants_test.go 锁值测试落地并被 `go test ./internal/models/ -run TestStatusConstants` 命中 —— 由 T1 交付
3. check-status-literals.sh 守护脚本（四模式，含 map/JSON 形态）以全量欠账为基线常绿运行，F 簇永久豁免 —— 由 T2 交付
4. 批 1（services/system）裸字面量清零，守护白名单收缩，构建/测试全绿 —— 由 T3 交付

</success_criteria>

<output>
Create `.planning/phases/69-dict-and-status-governance/69-01-SUMMARY.md` when done，必须包含：
- 3 个 task × commit hash 对照表
- 新增常量完整清单（家族名 / 常量名 / 值 / 所在 model 文件）——69-03/69-04/69-05 批 2-4、69-02 seed 与 69-08 DICT-04 直接引用
- status_constants_test.go 的 watched 家族集合与 expected 条目数（以及「后续批次新增家族须同步登记」的机制说明）
- 守护脚本四条命中模式与基线快照（替换前文件数/命中数 vs 替换后）与白名单剩余条目
- 批 1 各文件替换处数与语义簇标注（尤其 widget_data_fetcher 的语义确认结论）
</output>
