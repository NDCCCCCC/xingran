---
phase: 69-dict-and-status-governance
plan: 04
type: execute
wave: 2
depends_on: ["69-02"]
files_modified:
  - xingran-react-frontend/src/constants/status.ts
  - xingran-react-frontend/src/constants/status.test.ts
  - xingran-react-frontend/src/pages/system/user/constants.ts
  - xingran-react-frontend/src/pages/system/user/index.tsx
  - xingran-react-frontend/src/pages/system/user/utils.tsx
  - xingran-react-frontend/src/pages/system/role/constants.ts
  - xingran-react-frontend/src/pages/system/dict/constants.tsx
  - xingran-react-frontend/src/pages/system/dept/constants.tsx
  - xingran-react-frontend/src/pages/system/menu/constants.tsx
  - xingran-react-frontend/src/pages/operations/workstations/constants.tsx
  - xingran-react-frontend/src/pages/operations/workstations/index.tsx
  - xingran-react-frontend/src/pages/operations/workstations/modals/EditModal.tsx
  - xingran-react-frontend/src/pages/operations/floors/constants.ts
  - xingran-react-frontend/src/pages/duty/holidays/constants.tsx
  - xingran-react-frontend/src/pages/duty/holidays/index.tsx
  - xingran-react-frontend/src/pages/duty/holidays/modals/EditModal.tsx
  - xingran-react-frontend/src/pages/duty/holidays/modals/BatchModal.tsx
  - xingran-react-frontend/src/pages/network/devices/constants.ts
  - xingran-react-frontend/src/pages/network/devices/index.tsx
  - CLAUDE.md
autonomous: false
requirements: [DICT-03, DICT-04]

must_haves:
  truths:
    - "DICT-03: `src/constants/status.ts` 落地——ENABLE_DISABLE（0=启用/1=禁用，对齐 models.UserStatus）与 NORMAL_STOP（0=正常/1=停用，对齐 models.RoleStatus/DeptStatus/PostStatus）两组 options + tag 配置单一拷贝，status.test.ts 断言值/label/键一致性全绿"
    - "DICT-03: 7 个 A 簇 status 定义文件改为引用共享模块（本地导出名不变、页面文件零改动），user/role/dict 三份「启用/禁用 vs 正常/停用」漂移拷贝收敛为单一模块；menu 的 VISIBLE（1=可见反转）保留独立定义不被误统一"
    - "DICT-03: user/workstations/duty holidays/network devices 四页 type 类下拉迁移 useDict（dedicated-lines 三件套：Option 渲染 + dictLabel 回退 + isDefault 默认值），配 `?? 原静态数组` 兜底——字典管理页改 label 后下拉随之变化，字典接口异常时下拉仍有静态选项"
    - "DICT-03: status 0/1 下拉不进字典（安全决策：status 是代码分支语义不宜运营可配，走共享常量而非 sys_dict）"
    - "DICT-04: CLAUDE.md Status Value Convention 段删除 6 行模块值表格，改写为指向 internal/models 常量（base.go + status_constants_test.go 锁值）与 sys_dict 真相源的引用 + Menu visible 例外（models.VisibleShow/VisibleHidden）"
  artifacts:
    - path: "xingran-react-frontend/src/constants/status.ts"
      provides: "通用启停 options/tag 共享常量（两套 label 语义组）"
      contains: "ENABLE_DISABLE_OPTIONS"
    - path: "xingran-react-frontend/src/constants/status.test.ts"
      provides: "共享常量值/label/键一致性断言（vitest）"
      contains: "NORMAL_STOP_OPTIONS"
    - path: "CLAUDE.md"
      provides: "Status Value Convention 指针化改写（不再承载值语义本体）"
      contains: "internal/models"
  key_links:
    - from: "src/pages/system/user/constants.ts 等 7 文件"
      to: "src/constants/status.ts"
      via: "本地 STATUS_OPTIONS 导出改为共享模块别名引用（页面 import 不变）"
      pattern: "from \"@/constants/status\""
    - from: "src/pages/{system/user,operations/workstations,duty/holidays,network/devices}"
      to: "useDict(\"sys_user_sex\"|\"ops_workstation_type\"|\"duty_holiday_type\"|\"network_device_type\")"
      via: "React Query hook + 静态 fallback + dictLabel 回退 + isDefault 默认"
      pattern: "useDict\\("
    - from: "CLAUDE.md (Status Value Convention)"
      to: "internal/models/base.go + internal/models/status_constants_test.go + sys_dict"
      via: "文档指针（删除独立值表格）"
      pattern: "status_constants_test"
---

# Phase 69 Plan 04: 前端 DICT-03（status 共享常量 + useDict 分批迁移）+ DICT-04（CLAUDE.md 指针化）

**Status:** READY FOR EXECUTION
**Created:** 2026-08-19
**Source:** ROADMAP Phase 69（DICT-03/DICT-04）+ 69-RESEARCH.md A5 + 69-PATTERNS.md
**决策依据:** orchestrator 规划要点 3/4 + Q2/Q4 —— type 类走 useDict、status 类走共享常量模块（不进字典）；useDict 无内置 fallback 须页面级静态兜底；DICT-04 删除 CLAUDE.md 独立值表格（漂移源头）

<objective>

前端两线收敛：(1) status 0/1 下拉统一到 `src/constants/status.ts` 共享模块（消除 user「启用/禁用」/ role「正常/停用」/ dict「启用/禁用」三份漂移拷贝）；(2) 四页 type 类下拉迁移 useDict 消费 69-02 seed 的字典（user 性别 / 工位类型 / 假日类型 / 网络设备类型），配静态 fallback；(3) CLAUDE.md Status Value Convention 改为真相源指针。终局人工 checkpoint 验证「字典改值 → 下拉变化」与 4 个既有 useDict 页恢复。

Purpose: 前端 OPTIONS 硬编码是三处真相源拷贝中离用户最近的一处；本 plan 让「字典管理页改 label → 消费页下拉变化」成为现实（DICT-03 SC 核心验收），并让 CLAUDE.md 不再手工维护第三份值表。
Output: 2 个新文件 + 约 17 个前端文件修改 + CLAUDE.md 改写；type-check / lint / test 全绿；1 个 human-verify checkpoint。

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/69-dict-and-status-governance/69-RESEARCH.md
@.planning/phases/69-dict-and-status-governance/69-PATTERNS.md
@.planning/phases/69-dict-and-status-governance/69-02-SUMMARY.md

<interfaces>
<!-- 关键现状契约（已从代码库提取，executor 无需再探索） -->

useDict hook（src/hooks/useDict.ts，零改动）: `useDict(dictType: string)` 返回 React Query 结果，data 为 DictItem[]；DictItem 字段 dictValue: string / dictLabel / isDefault?: boolean / listClass；无内置静态 fallback（:53 result.data?.list ?? []），消费方必须自带兜底。

迁移成品范本（dedicated-lines/index.tsx，Wave 5 留下，唯一完整三件套）:
```typescript
// :91-93 hook 消费
const { data: lineTypeDict = [] } = useDict("ops_dedicated_line_type");
// :241-244 isDefault 默认值
const defaultType = lineTypeDict.find((d) => d.isDefault)?.dictValue || lineTypeDict[0]?.dictValue;
// :253-261 dictLabel 回退
const getLineTypeText = (type: string) => {
  const dictItem = lineTypeDict.find((d) => d.dictValue === type);
  return dictItem?.dictLabel || type;
};
// :593-596 Option 渲染
{lineTypeDict.map((d) => (<Option key={d.dictValue} value={d.dictValue}>{d.dictLabel}</Option>))}
```
本 plan 迁移页在此之上加一层兜底（RESEARCH Pitfall 4 / SC#3 明确要求）：下拉 options = 字典 data 非空时渲染字典项，否则回退渲染原静态 OPTIONS——原 OPTIONS 导出一律保留降级为 fallback，不删除。

status.ts 内容范本（迁移源头的单一拷贝，user/constants.ts:11-27 现状）:
```typescript
export const STATUS_OPTIONS: SelectOption[] = [
  { label: "启用", value: 0 },
  { label: "禁用", value: 1 },
];
export const STATUS_TAG_CONFIG: Record<number, { text: string; color: string }> = {
  0: { text: "启用", color: "success" },
  1: { text: "禁用", color: "error" },
};
```
status.ts 设计（对齐后端 base.go 常量注释，两套 label 语义组、零 UX 变化）:
- ENABLE_DISABLE_OPTIONS（0=启用/1=禁用）+ ENABLE_DISABLE_TAG_CONFIG —— 注释 `// 对齐 models.UserStatusEnabled=0(启用) / UserStatusDisabled=1(禁用), internal/models/base.go`，消费方 user/dict
- NORMAL_STOP_OPTIONS（0=正常/1=停用）+ NORMAL_STOP_TAG_CONFIG —— 注释 `// 对齐 models.RoleStatus/DeptStatus/PostStatus: 0=正常, 1=停用, internal/models/base.go`，消费方 role/dept/menu(status)/workstations/floors
- 自带导出类型 StatusOption（label: string; value: number），不反向 import 页面类型（避免循环依赖）

7 个 status 迁移文件与语义组映射:
| 文件 | 现状 label | 改为 |
|------|-----------|------|
| src/pages/system/user/constants.ts | 启用/禁用 | ENABLE_DISABLE 组别名 |
| src/pages/system/dict/constants.tsx | 启用/禁用 | ENABLE_DISABLE 组别名 |
| src/pages/system/role/constants.ts | 正常/停用 | NORMAL_STOP 组别名 |
| src/pages/system/dept/constants.tsx | 正常/停用 | NORMAL_STOP 组别名 |
| src/pages/system/menu/constants.tsx | status=正常/停用；VISIBLE=1显示/0隐藏(反转) | 仅 STATUS 改 NORMAL_STOP；**VISIBLE 相关导出原样保留独立定义**（VisibleShow=1 反转例外，严禁统一） |
| src/pages/operations/workstations/constants.tsx | 以文件实际为准（models.WorkstationStatus 注释为准选组） | NORMAL_STOP 组别名（若现状 label 与组不符，以模型注释对齐并在 SUMMARY 记录 UX 文案变化） |
| src/pages/operations/floors/constants.ts | 正常/停用（FloorStatusNormal/Stopped） | NORMAL_STOP 组别名 |

本地文件改造形态（导出名不变 → 页面 index.tsx/modals 零改动）: 文件内 STATUS_OPTIONS 定义替换为 `export const STATUS_OPTIONS = ENABLE_DISABLE_OPTIONS;` 一行 + import；STATUS_TAG_CONFIG 同理。（menu 的 VISIBLE_OPTIONS 等保持原定义不动。）

4 个 useDict 迁移目标（dictType 均已由 69-02 seed）:
| 页面 | 迁移项 | dictType | 涉及文件 |
|------|--------|----------|---------|
| 系统用户 | GENDER_OPTIONS 下拉与 label 渲染 | sys_user_sex | user/index.tsx（搜索表单+编辑弹窗 Select）、user/utils.tsx（性别 label 渲染函数改为接收 dict 或保留静态 map 兜底）、user/constants.ts（保留 GENDER_OPTIONS 为 fallback） |
| 工位管理 | TYPE_OPTIONS（工位类型） | ops_workstation_type | workstations/index.tsx、modals/EditModal.tsx、constants.tsx（保留 fallback） |
| 假日管理 | HOLIDAY_TYPE_OPTIONS | duty_holiday_type | duty/holidays/index.tsx、modals/EditModal.tsx、modals/BatchModal.tsx、constants.tsx（保留 fallback）；columns.tsx/utils.tsx 的 Tag 渲染保留静态映射兜底（非 hook 上下文） |
| 网络设备 | DEVICE_TYPE_OPTIONS | network_device_type | network/devices/index.tsx、constants.ts（保留 fallback）；VENDOR_OPTIONS 不迁（无字典组，保持现状） |

工作区护栏: `src/pages/settings/index.tsx` 与 `src/pages/system/settings-page/index.tsx` 存在与本 phase 无关的未提交改动（default_theme 删除遗留）——本 plan 任何任务不得触碰这两个文件。

测试范本: src/components/network/port-write/__tests__/constants.test.ts（vitest 纯常量自洽测试：describe/it + expect，断言 label 非空/value 唯一/数值锁定）。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>T1: src/constants/status.ts + status.test.ts + 7 个 A 簇 status 定义文件改共享引用</name>
  <files>xingran-react-frontend/src/constants/status.ts, xingran-react-frontend/src/constants/status.test.ts, xingran-react-frontend/src/pages/system/user/constants.ts, xingran-react-frontend/src/pages/system/role/constants.ts, xingran-react-frontend/src/pages/system/dict/constants.tsx, xingran-react-frontend/src/pages/system/dept/constants.tsx, xingran-react-frontend/src/pages/system/menu/constants.tsx, xingran-react-frontend/src/pages/operations/workstations/constants.tsx, xingran-react-frontend/src/pages/operations/floors/constants.ts</files>
  <read_first>
  - xingran-react-frontend/src/pages/system/user/constants.ts:11-27（内容范本 + 迁移源）
  - xingran-react-frontend/src/pages/system/role/constants.ts:17-20、src/pages/system/dict/constants.tsx:10-20（漂移实证三份拷贝）
  - xingran-react-frontend/src/pages/system/menu/constants.tsx 全文（识别 STATUS 与 VISIBLE 两组导出——VISIBLE 是反转例外必须保留）
  - xingran-react-frontend/src/pages/operations/workstations/constants.tsx、src/pages/operations/floors/constants.ts（现状 label 与导出名）
  - internal/models/base.go UserStatus/RoleStatus/DeptStatus/PostStatus 注释（label 对齐基准）
  - xingran-react-frontend/src/components/network/port-write/__tests__/constants.test.ts（测试范本）
  - xingran-react-frontend/src/constants/ 目录既有文件（buttonStyles.tsx 等——位置与导出风格先例）
  </read_first>
  <action>
  1. 新建 src/constants/status.ts：按 interfaces 设计导出 StatusOption 类型、ENABLE_DISABLE_OPTIONS、ENABLE_DISABLE_TAG_CONFIG、NORMAL_STOP_OPTIONS、NORMAL_STOP_TAG_CONFIG；每组常量上方注释对齐后端 models 常量名（internal/models/base.go）；tag 配置的 color 沿用现状（success/error）。
  2. 新建 src/constants/status.test.ts（port-write constants.test.ts 风格）：断言 两组 options 各覆盖 value 0/1 且唯一、label 与 base.go 注释一致（"启用"/"禁用" 与 "正常"/"停用" 字面锁定）、两组 tag 配置键集合与对应 options 的 value 集合一致、tag text 与 options label 一致——锁死后端常量注释对齐关系。
  3. 7 个文件逐一改造：删除本地 STATUS_OPTIONS/STATUS_TAG_CONFIG 字面定义，改为 import 共享模块对应组并按 interfaces 映射表 re-export（导出名与类型签名不变）；menu/constants.tsx 只动 STATUS 相关导出，VISIBLE 相关原样保留；workstations 若现状 label 与 NORMAL_STOP 不符，以 models.WorkstationStatus 注释为准对齐并在 SUMMARY 记录。
  4. 各文件其余导出（GENDER_OPTIONS/TYPE_OPTIONS 等）本任务不动（T2 处理）；页面 index.tsx/modals 零改动（导出名不变）。
  5. 门禁：npm run type-check && npx vitest run src/constants/status.test.ts && npm run lint。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npm run type-check && npx vitest run src/constants/status.test.ts && grep -c "VISIBLE" src/pages/system/menu/constants.tsx</automated>
  </verify>
  <acceptance_criteria>
  - `npm run type-check` 退出码 0；`npx vitest run src/constants/status.test.ts` 全绿
  - `grep -rl "from \"@/constants/status\"" src/pages/` 命中 7 个文件（user/role/dict/dept/menu/workstations/floors constants）
  - `grep -c "VISIBLE" src/pages/system/menu/constants.tsx` ≥ 1（VISIBLE 导出未被误统一/删除）
  - status.ts 内不存在 "sys_user_status" 等字典化 status 痕迹（status 不进字典，Q2 决策的源码断言）
  - user/role/dict 三文件的 STATUS_OPTIONS label 分别为 启用/禁用、正常/停用、启用/禁用（与迁移前一致，零 UX 变化）
  </acceptance_criteria>
  <done>共享 status 常量模块与测试落地，7 文件漂移拷贝收敛，VISIBLE 反转例外完好，type-check/lint/vitest 全绿。Commit: refactor(frontend): unify status options into shared constants module per DICT-03</done>
</task>

<task type="auto">
  <name>T2: 四页 type 下拉迁移 useDict（sys_user_sex / ops_workstation_type / duty_holiday_type / network_device_type）</name>
  <files>xingran-react-frontend/src/pages/system/user/index.tsx, xingran-react-frontend/src/pages/system/user/utils.tsx, xingran-react-frontend/src/pages/system/user/constants.ts, xingran-react-frontend/src/pages/operations/workstations/index.tsx, xingran-react-frontend/src/pages/operations/workstations/modals/EditModal.tsx, xingran-react-frontend/src/pages/operations/workstations/constants.tsx, xingran-react-frontend/src/pages/duty/holidays/index.tsx, xingran-react-frontend/src/pages/duty/holidays/modals/EditModal.tsx, xingran-react-frontend/src/pages/duty/holidays/modals/BatchModal.tsx, xingran-react-frontend/src/pages/duty/holidays/constants.tsx, xingran-react-frontend/src/pages/network/devices/index.tsx, xingran-react-frontend/src/pages/network/devices/constants.ts</files>
  <read_first>
  - xingran-react-frontend/src/pages/operations/dedicated-lines/index.tsx:91-96,241-266,593-609（三件套唯一完整范本——先通读再动手）
  - xingran-react-frontend/src/hooks/useDict.ts（返回结构与 DictItem 类型）
  - 上述 12 个待改文件各自的 OPTIONS 消费点（grep 该页 OPTIONS 常量名定位 Select/渲染处）
  - 69-02-SUMMARY.md 的 11 组 dictType 清单（确认四个目标组的值与 isDefault 项——下拉 value 类型必须与后端字段类型匹配：sys_user_sex 的 dictValue 是 "0"/"1"/"2" 字符串，而 user 页性别字段是 number，Option value 需做 String(gender) 对齐或后端字段核对，逐页确认）
  </read_first>
  <action>
  对四页逐页套用 dedicated-lines 三件套 + 静态兜底（每页一个子 commit 或合并为一个 commit，executor 视 diff 大小定，但四页必须同批完成）：
  1. 通用改造形态：组件内 useDict(目标 dictType) 取 data；下拉 options 渲染 = data 非空时 data.map 渲染 Option（key/value 用 dictValue、children 用 dictLabel），data 为空时回退渲染原静态 OPTIONS（import 保留，不删除原导出——Pitfall 4）；表格/文本处的 label 渲染用 dictLabel || 静态 map 回退；新增/编辑表单默认值优先 data 中 isDefault 项的 dictValue。
  2. user 页：GENDER_OPTIONS → useDict("sys_user_sex")；index.tsx 的搜索与编辑处 Select 迁移；utils.tsx 的性别 label 函数改为优先查字典入参或保留静态映射兜底（非 hook 上下文允许静态）；注意性别字段 number 与 dictValue string 的类型对齐（表单提交值不改变现有 API 契约——若现有提交 number 则 Option value 保持 number，渲染 key 用 dictValue）。
  3. workstations 页：TYPE_OPTIONS → useDict("ops_workstation_type")；index.tsx 搜索/表格与 modals/EditModal.tsx 表单 Select 迁移（EditModal 是独立组件，hook 在弹窗组件内调用）。
  4. duty/holidays 页：HOLIDAY_TYPE_OPTIONS → useDict("duty_holiday_type")；index.tsx、EditModal、BatchModal 的类型 Select 迁移；columns.tsx/utils.tsx 的 Tag 渲染保留静态映射兜底（明确不迁，SUMMARY 记录）。
  5. network/devices 页：DEVICE_TYPE_OPTIONS → useDict("network_device_type")；index.tsx Select 与设备类型 label 渲染迁移；VENDOR_OPTIONS 保持现状（无字典组）。
  6. 不改 useDict.ts 本体、不改任何 API 层、不动 settings 相关文件（工作区护栏）；useEffect 依赖遵守 CLAUDE.md 稳定性规范（hook 返回的 data 引用来自 React Query 缓存，直接用）。
  7. 门禁：npm run type-check && npm run lint && npm run test。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npm run type-check && npm run lint && npm run test && grep -rn "useDict(" src/pages/system/user/index.tsx src/pages/operations/workstations/index.tsx src/pages/duty/holidays/index.tsx src/pages/network/devices/index.tsx | wc -l</automated>
  </verify>
  <acceptance_criteria>
  - `npm run type-check` / `npm run lint` / `npm run test` 全绿
  - grep useDict( 在四个 index.tsx 合计命中 ≥ 4（每页至少一处）
  - 四页的原 OPTIONS 常量导出仍存在（grep GENDER_OPTIONS/TYPE_OPTIONS/HOLIDAY_TYPE_OPTIONS/DEVICE_TYPE_OPTIONS 在对应 constants 文件命中 ≥ 1——fallback 未被删除）
  - 每个迁移 Select 处存在字典空态回退分支（源码断言：渲染处同时存在 dict 变量与静态 OPTIONS 引用）
  - 表单提交值类型与迁移前一致（无 API 契约变化——type-check 通过 + 人工 diff 复核）
  </acceptance_criteria>
  <done>四页 type 下拉全部走字典 + 静态兜底，字典管理页改 label 即可改变下拉文案（checkpoint 验收），type-check/lint/test 全绿。Commit: refactor(frontend): migrate type options to useDict with static fallback per DICT-03</done>
</task>

<task type="auto">
  <name>T3: DICT-04 — CLAUDE.md Status Value Convention 指针化改写</name>
  <files>CLAUDE.md</files>
  <read_first>
  - CLAUDE.md 的 `### Status Value Convention (IMPORTANT)` 段全文（改写对象——通用规则 + Menu visible 例外 + 6 行模块值表格）
  - 69-01-SUMMARY.md 新增常量清单与 status_constants_test.go 说明（指针指向的真实目标）
  - 69-02-SUMMARY.md 的 11 组 dictType 清单（sys_dict 真相源描述素材）
  </read_first>
  <action>
  1. 改写 `### Status Value Convention (IMPORTANT)` 段：保留通用规则一句（0 = enabled/normal/visible, 1 = disabled/stopped/hidden）与 Menu Visibility 例外句（1 = visible, 0 = hidden），随后替换 6 行模块值表格为指针段，要点：状态语义唯一真相源是 internal/models 具名常量（internal/models/base.go 及各 model 文件，例 models.UserStatusEnabled = 0、models.VisibleShow = 1）；常量值由 internal/models/status_constants_test.go 回归测试锁定，改值即测试失败；type/category 类运营可维护枚举的真相源是 sys_dict_type / sys_dict_data（字典管理页维护，seed 见 internal/core/db/migrations/migration_208_dict_seed.go）；前端通用启停选项共享 xingran-react-frontend/src/constants/status.ts；status 0/1 不入字典（代码分支语义，管理员可配值会破坏逻辑）。
  2. 删除原 6 行表格（| Module | Field | ... 全部行）。
  3. `## Common Gotchas` 段落引用的是通用规则而非表格，不动；CLAUDE.md 其余段落零改动。
  4. 门禁：grep 验证表格残留清零 + 指针关键词存在；go build / 前端门禁不受文档影响（无需重跑，本任务只动 CLAUDE.md）。
  </action>
  <verify>
    <automated>grep -A 20 "Status Value Convention" CLAUDE.md | grep -c "| User | status |"; grep -A 20 "Status Value Convention" CLAUDE.md | grep -c "internal/models"</automated>
  </verify>
  <acceptance_criteria>
  - `grep -A 20 "Status Value Convention" CLAUDE.md | grep -c "| User | status |"` 输出 0（值表格删除）
  - 同段 grep internal/models 命中 ≥ 1、grep status_constants_test 命中 ≥ 1、grep sys_dict 命中 ≥ 1、grep constants/status.ts 命中 ≥ 1（四类指针齐全）
  - Menu visible 例外句保留（grep -i "visible" 命中且语义为 1 = visible）
  - CLAUDE.md 其余段落 diff 为零（git diff --stat CLAUDE.md 只含该段改动）
  </acceptance_criteria>
  <done>CLAUDE.md 不再承载值语义本体，成为指向 models 常量 + sys_dict + 前端共享模块的指针；漂移源头（第三份手工拷贝）消除。Commit: docs(claude): rewrite Status Value Convention as source-of-truth pointers per DICT-04</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>T4: Checkpoint — 字典链路端到端人工验证（DICT-02 目检 + DICT-03 SC#3 验收）</name>
  <files>无文件改动——纯人工验证 checkpoint，验证结果记录进 SUMMARY</files>
  <action>启动后端（sqlite dev 库）与前端 dev server，按 how-to-verify 五步逐项人工验证字典链路端到端行为；全部通过回复 approved，任一步失败则描述问题交回 executor 修复后复验。</action>
  <what-built>DICT-02 字典 seed（69-02）+ 前端四页 useDict 迁移 + status 共享常量 + CLAUDE.md 指针化——字典链路端到端首次打通：字典管理页的修改能实时反映到消费页下拉。</what-built>
  <how-to-verify>
  前置：后端已启动（sqlite dev 库，确认启动日志含 migration_208/字典 seed 成功）+ 前端 npm run dev（http://localhost:4000）+ 用 admin 账号登录。
  1. 字典管理页可见性（DICT-02 目检）：系统管理 → 字典管理，确认出现 69-02 的 11 组字典类型（network_device_type / ops_dedicated_line_type / ops_isp / ops_info_point_type / asset_reconciliation_* 4 组 / ops_workstation_type / sys_user_sex / duty_holiday_type），每组 data 行数与 69-02-SUMMARY 记录一致。
  2. 既有 useDict 页恢复（dev 库字典曾为空）：打开 专线管理（dedicated-lines）→ 线路类型/ISP 下拉有选项（迁移前为空）；信息点管理 → 类型下拉有值；资产对账 exceptions 页 → 冲突类型下拉有值。
  3. 字典改值 → 下拉变化（SC#3 核心验收）：字典管理 → ops_workstation_type → 把「固定工位」label 改为「固定工位(测试)」保存 → 打开 工位管理 页刷新 → 类型下拉显示新 label。
  4. 四页迁移验证：系统用户（性别下拉 3 项）、工位管理（类型下拉）、假日管理（假日类型下拉）、网络设备（设备类型下拉 5 项）均正常出选项；任选一页 DevTools Network 面板把 /system/dicts/data/list 请求断网/改失败后刷新，下拉仍显示静态兜底选项（fallback 生效）。
  5. status 共享常量回归：系统用户列表状态列仍显示 启用/禁用、角色管理仍显示 正常/停用（零 UX 变化）；菜单管理显示/隐藏筛选行为不变（VISIBLE 反转未破坏）。
  </how-to-verify>
  <resume-signal>回复 "approved" 或逐条描述问题（如某页下拉空白/label 未变化）</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sys_dict 字典值（管理员可配）→ 前端下拉/表格渲染 | dictValue/dictLabel 仅作展示与筛选传参；status 类代码分支语义不进此通道 |
| 字典 API（/system/dicts/data/list）→ 页面可用性 | 接口异常/空数据不得导致下拉空白（fallback 边界） |
| CLAUDE.md 文档 → 开发者/AI 行为 | 文档不再是值真相源，错误表格即误导源头（本 plan 消除） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-13 | Tampering | 字典值注入业务逻辑（status 若进字典，管理员可配出代码未识别值） | mitigate | Q2 决策落地：status 走 src/constants/status.ts 共享常量不进字典（T1 acceptance 源码断言无 sys_user_status 痕迹）；dictType 仅 type/性别/假日类 |
| T-69-14 | Denial of Service | 迁移后字典接口异常 → 下拉空白不可用 | mitigate | T2: 每个迁移 Select 强制字典空态回退静态 OPTIONS（acceptance 断言 fallback 分支存在）；原 OPTIONS 导出保留降级不删除 |
| T-69-15 | Tampering | Menu VISIBLE 反转语义被 status 统一误伤（1=可见 ≠ 1=停用） | mitigate | T1: menu/constants.tsx 仅改 STATUS 导出，VISIBLE 原样保留；acceptance grep VISIBLE 命中；checkpoint 第 5 步行为验证 |
| T-69-16 | Tampering | 迁移改变表单提交值类型（number ↔ string）破坏 API 契约 | mitigate | T2: 提交值类型与迁移前一致约束 + type-check 门 + 人工 diff 复核 |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | 零依赖安装（React Query/vitest 均仓内既有），Package Legitimacy Gate 不适用 |
</threat_model>

<verification>

**每任务边界：** T1/T2 `npm run type-check` + vitest 相关文件 + lint；T3 grep 断言；checkpoint 人工五步。

**Plan 终态：**
1. `npm run type-check && npm run lint && npm run test` 全绿
2. `npx vitest run src/constants/status.test.ts` 全绿
3. grep: 7 文件 import @/constants/status；4 页 useDict；CLAUDE.md 值表格 0 残留 + 四类指针齐全
4. checkpoint 五步人工验证通过（字典改值 → 下拉变化 / 既有 useDict 页恢复 / fallback 生效 / status 零 UX 变化）

</verification>

<success_criteria>

（ROADMAP Phase 69 SC#3 + SC#4 全部）

1. 前端硬编码 options 分批迁移：status 0/1 走共享常量模块（7 文件收敛、三份漂移消除、VISIBLE 例外完好）；type 类四页走 useDict 且保留静态 fallback —— 由 T1/T2 交付
2. 已迁移下拉在字典管理改值后选项随之变化 —— 由 T2 实现 + checkpoint 第 3 步验收
3. CLAUDE.md Status Value Convention 改为指向 models 常量 + sys_dict 真相源的引用，不再作为第三份手工拷贝 —— 由 T3 交付
4. status 不进字典的安全设计落地（Q2 决策）—— 由 T1 源码断言 + threat model T-69-13 背书

</success_criteria>

<output>
Create `.planning/phases/69-dict-and-status-governance/69-04-SUMMARY.md` when done，必须包含：
- 3 个 task × commit hash 对照表 + checkpoint 验收结论（五步逐条记录）
- status.ts 两组常量与后端 models 常量对齐表
- 四页迁移台账（页面 / dictType / 涉及文件 / fallback 方式 / 提交值类型结论）
- 7 文件 status 收敛映射表（含 workstations label 对齐决策）
- CLAUDE.md 改写前后对照段
- 明确不迁清单及理由（VENDOR_OPTIONS、columns/utils 静态渲染、其余 OPTIONS 组——后续批次候选）
</output>
