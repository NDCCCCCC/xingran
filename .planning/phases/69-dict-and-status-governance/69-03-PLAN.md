---
phase: 69-dict-and-status-governance
plan: 03
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - internal/services/operations/building_service.go
  - internal/services/operations/floor_service.go
  - internal/services/operations/workstation_service.go
  - internal/services/operations/workstation_device_service.go
  - internal/services/operations/server_room_service.go
  - internal/services/operations/room_device_service.go
  - internal/services/operations/infopoint_service.go
  - internal/services/operations/dedicated_line_service.go
  - internal/services/operations/asset_service.go
  - internal/api/v1/operations/excel_handler.go
  - internal/services/vdi/vm_service_impl.go
  - internal/services/addomain/account_pool.go
  - internal/services/notice_service.go
  - internal/services/knowledge_service.go
  - internal/services/rpa/credential_service.go
  - internal/services/scheduler/job_service.go
  - internal/services/scheduler/job_log_service.go
  - internal/services/workorder/base.go
  - internal/services/workorder/reconciliation_template.go
  - internal/services/duty_pool_service.go
  - internal/services/device_discovery_service.go
  - internal/services/config_execution_service.go
  - internal/services/command_dispatch_service.go
  - internal/services/monitor/server_service.go
  - internal/services/asset/fix_suggestion_monitor.go
  - internal/api/v1/system/notice_handler.go
  - internal/api/v1/monitor/oper_log_handler.go
  - scripts/check-status-literals.sh
autonomous: true
requirements: [DICT-01]

must_haves:
  truths:
    - "DICT-01: 批 2-4 完成——operations/vdi/addomain/notice/knowledge/rpa/scheduler/workorder/duty_pool/device_discovery/config_execution/command_dispatch/monitor 全部 service + api/v1 三个 handler 的裸 status 字面量清零，守护脚本白名单只剩 F 簇 geocoding_service.go"
    - "DICT-01: E 簇反转零误替——knowledge/notice 的 status=1 替换为 KnowledgeArticleStatusPublished / PublishStatusPublished 而非任何 Disabled/Stopped 常量；B 簇状态机用 ExecutionStatus* 系列，diff 中 knowledge/notice/config_execution 不出现 Enabled/Stop 字样套用"
    - "DICT-01: RoomStatus 双包定义陷阱规避——operations 批替换按 model struct 实际所在包引用（子包 operations.RoomStatusNormal 或 models.RoomStatusNormal），不新造第三份定义"
    - "DICT-01: `go build ./...` + `go test ./...` 全绿，行为零变化（纯常量替换，无逻辑分支/SQL 结构改动），F 簇 geocoding_service.go:332 加注释豁免不替换"
  artifacts:
    - path: "internal/services/operations/workstation_device_service.go"
      provides: "11 处结构体字面量 Status: 0/1 全部常量化（批 2 主战场）"
      contains: "models."
    - path: "internal/services/config_execution_service.go"
      provides: "半迁移收口——raw SQL CASE WHEN 补齐到与 struct 赋值同等常量化（B 簇状态机）"
      contains: "ExecutionStatus"
    - path: "scripts/check-status-literals.sh"
      provides: "白名单 ratchet 至终态（只剩 F 簇永久豁免条目）"
      contains: "geocoding_service.go"
  key_links:
    - from: "internal/services/operations/*_service.go"
      to: "internal/models/operations/ + internal/models/operations.go 常量"
      via: "按 model struct 实际所在包 import 引用（双定义陷阱规避）"
      pattern: "operations\\.(RoomStatus|BuildingStatus|LineStatus)|(models)\\.(FloorStatus|RoomDeviceStatus)"
    - from: "internal/services/{knowledge_service,notice_service}.go"
      to: "internal/models/{knowledge,notice_enhanced}.go"
      via: "E 簇反转常量替换 status = 1 字面量"
      pattern: "KnowledgeArticleStatusPublished|PublishStatusPublished"
    - from: "scripts/check-status-literals.sh 白名单"
      to: "批 2-4 全部替换文件"
      via: "每批完成即删除对应条目（ratchet 单调收缩）"
      pattern: "ALLOWED"
---

# Phase 69 Plan 03: DICT-01 批 2-4 — 按语义簇替换全部剩余裸字面量

**Status:** READY FOR EXECUTION
**Created:** 2026-08-19
**Source:** ROADMAP Phase 69（DICT-01）+ 69-RESEARCH.md A1/A2 批次表 + 69-PATTERNS.md
**决策依据:** orchestrator 规划要点 1 —— 按语义簇 A-F 分批替换，禁止一刀切；范本 department_cache_impl.go:65；CASE WHEN 用 Sprintf+int()

<objective>

完成 DICT-01 剩余三个批次的字面量替换（批 2 operations / 批 3 vdi+addomain+notice+knowledge+rpa+scheduler / 批 4 workorder+duty+discovery+execution+dispatch+monitor+三 handler），每批独立 commit、`go build ./...` 常绿、守护白名单单调收缩，最终白名单只剩 F 簇 geocoding_service.go。

Purpose: 69-01 已交付常量集、锁值测试与守护脚本；本 plan 是欠账主体（2026-08-19 基线约 33 文件 / raw SQL 97 处 + 结构体 22 处，批 1 后剩约 27 文件）的清偿。
Output: 约 27 个 service/handler 文件常量化 + 守护白名单收缩至 F 簇，3 个批次 commit；`go test ./...` 全绿。

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/69-dict-and-status-governance/69-RESEARCH.md
@.planning/phases/69-dict-and-status-governance/69-PATTERNS.md
@.planning/phases/69-dict-and-status-governance/69-01-SUMMARY.md

<interfaces>
<!-- 关键现状契约（已从代码库提取，executor 无需再探索） -->

语义簇替换对照表（RESEARCH A2 + PATTERNS 簇表，每个批次任务替换前必须对照）:

| 簇 | 语义 | 涉及文件（批） | 目标常量 | 严禁 |
|----|------|---------------|---------|------|
| A 通用启停 | 0=正常/启用, 1=停用 | operations 7 service、vdi_server、notice、job、ad_config（2/3） | BuildingStatus/LineStatus/WorkstationStatus/RoomStatus/FloorStatus/InfoPoint 常量、NoticeStatus、JobStatusNormal/Pause、VDIServer 常量 | — |
| B 任务状态机 | 0=待执行,1=执行中,2=成功,3=失败,4=取消 | config_execution/device_discovery/command_dispatch（4） | models.ExecutionStatus*（config_execution.go:19-27） | 套 Enabled/Stop |
| C 成功/失败 | 0=成功, 1=失败 | oper_log_handler.go:190、job_log_service、fix_suggestion_monitor.go:181（Status: 0 // 0=成功） | 69-01 T1 在 log.go 补的成败常量 | — |
| D 多态(3+值) | 0/1/2 | dedicated_line（0=正常/1=故障/2=停用）、account_pool（0=可用/1=停用/2=熔断，account_pool.go）、DeviceStatus | LineStatusNormal/Fault/Disabled、AD 账号池常量（addomain 包内已有状态机常量，读文件确认命名） | 一刀切 |
| E 语义反转 | 1=正向 | knowledge_service（status=1 已发布）、notice 发布态 | KnowledgeArticleStatusPublished、PublishStatusPublished | **套 1=停用** |
| F 外部契约 | 百度 API 返回码 | geocoding_service.go:332 | 不替换，加注释「F 簇:百度 API 返回码契约,不迁移（见 scripts/check-status-literals.sh 白名单）」 | 替换 |

替换形态双模板:
- Where 参数化（范本 department_cache_impl.go:65）: Where("status = ?", models.DeptStatusNormal)
- CASE WHEN 聚合 Sprintf（范本 duty_pool_service.go:37-41 before → after）: fmt.Sprintf("SUM(CASE WHEN status = %d THEN 1 ELSE 0 END) AS enabled", int(models.DutyPoolStatusEnabled))

半迁移先例（批 4 安全参照）: config_execution_service.go——struct 赋值已用常量（:136 Status: models.ExecutionStatusPending），同文件 raw SQL 仍字面量（:46-49 "SUM(CASE WHEN status = 0 THEN 1 ELSE 0 END) AS pending"）。批 4 把这类文件补齐到自洽。

结构体字面量批 2 主战场: workstation_device_service.go 11 处 Status: 0/1 → 对应 model 常量（参照 user_sync_service.go:584 的 Status: models.UserStatusEnabled 写法）。

CRITICAL 双包定义陷阱（批 2 必读）: RoomStatus 等在 internal/models/operations.go（package models，旧无注释）与 internal/models/operations/server_room.go（package operations，新带注释）双定义。excel_config.go 经 import operations 子包引用子包版。批 2 替换规则：读 service 文件操作的 model struct 定义位置——OpsServerRoom 在子包 operations 则用 operations.RoomStatusNormal；旧 ServerRoom/RoomDevice/Floor 在 models 包则用 models.RoomStatusNormal 等。禁止新造第三份、禁止混引。

批次文件清单权威来源: `bash scripts/check-status-literals.sh --baseline`（69-01 T2 交付，批 1 后剩余条目即本 plan 工作面；下列目录分组为 RESEARCH 预估，以脚本实际输出为准，多不漏）:
- 批 2: internal/services/operations/（building/floor/workstation/workstation_device/server_room/room_device/infopoint/dedicated_line service 各若干 + asset_service.go 6 处）+ internal/api/v1/operations/excel_handler.go 2 处
- 批 3: internal/services/vdi/vm_service_impl.go（7 处）、internal/services/addomain/account_pool.go（3 处）+ addomain 其余、internal/services/notice_service.go、internal/services/knowledge_service.go（4 处）、internal/services/rpa/credential_service.go、internal/services/scheduler/{job_service,job_log_service}.go
- 批 4: internal/services/workorder/{base,reconciliation_template}.go（4+4 处）、internal/services/duty_pool_service.go（4 处 CASE WHEN）、internal/services/device_discovery_service.go（4 处）、internal/services/config_execution_service.go、internal/services/command_dispatch_service.go（2 处）、internal/services/monitor/server_service.go、internal/services/asset/fix_suggestion_monitor.go、internal/api/v1/system/notice_handler.go、internal/api/v1/monitor/oper_log_handler.go
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>T1: 批 2 — internal/services/operations/ + excel_handler（簇 A + D 专线三态）</name>
  <files>internal/services/operations/building_service.go, internal/services/operations/floor_service.go, internal/services/operations/workstation_service.go, internal/services/operations/workstation_device_service.go, internal/services/operations/server_room_service.go, internal/services/operations/room_device_service.go, internal/services/operations/infopoint_service.go, internal/services/operations/dedicated_line_service.go, internal/services/operations/asset_service.go, internal/api/v1/operations/excel_handler.go</files>
  <read_first>
  - scripts/check-status-literals.sh（先跑 --baseline 拿 operations 目录精确条目清单）
  - internal/models/operations.go 全文（旧版 FloorStatus/RoomStatus/RoomDeviceStatus 定义位置与所属包）
  - internal/models/operations/ 子包 building.go / server_room.go / dedicated_line.go / infopoint.go（新版常量与 struct 实际所在包——双定义陷阱判定依据）
  - internal/models/workstation.go（WorkstationStatus/WorkstationType）
  - internal/services/operations/workstation_device_service.go（11 处 Status: 0/1 集中区，替换前逐处读上下文）
  - internal/services/operations/excel_config.go:146,265（该模块常量引用先例——operations 子包 import 方式）
  - internal/services/system/department_cache_impl.go:65（目标态范本）
  </read_first>
  <action>
  1. 跑 bash scripts/check-status-literals.sh --baseline，取 internal/services/operations/ 与 internal/api/v1/operations/ 全部条目（interfaces 预估清单之外若还有命中文件，一并纳入本批）。
  2. 逐文件处理，每文件先确认操作的 model struct 定义所在包再选常量引用（双定义陷阱）：OpsServerRoom → operations.RoomStatusNormal/Stopped（子包）；旧 ServerRoom/RoomDevice/Floor → models.RoomStatusNormal / models.RoomDeviceStatus* / models.FloorStatus*（models 包）；Building → operations.BuildingStatus*（building.go 子包）；dedicated_line 三态 → operations.LineStatusNormal/Fault/Disabled（D 簇，禁止把 1 当停用、2 当停用之外的值乱映射）；InfoPoint → 69-01 补的常量。
  3. workstation_device_service.go 的 11 处结构体字面量：Status: 0/1 → 对应实体常量（设备/工位关联实体的 status 语义按其 model 文件注释；若语义为 workstation 启停用 models.WorkstationStatus*，为设备状态用相应 Device 常量——逐处读上下文判定，SUMMARY 记录判定）。
  4. asset_service.go 6 处 + excel_handler.go 2 处同法；excel_handler 的 status 过滤是导入数据启停语义（簇 A）。
  5. Where 参数化 / CASE WHEN Sprintf+int()；不修改逻辑与 SQL 结构；删除被替换行的裸值行尾注释（0=正常 之类）。
  6. 完成后从守护白名单删除本批全部条目，重跑脚本退出码 0。
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/services/operations/ ./internal/api/v1/... && bash scripts/check-status-literals.sh</automated>
  </verify>
  <acceptance_criteria>
  - `go build ./...` 与 `go test ./internal/services/operations/` 退出码 0
  - 守护脚本退出码 0 且白名单无 internal/services/operations/ 与 internal/api/v1/operations/ 条目
  - grep -rn 'Status: *[01]\b' internal/services/operations/workstation_device_service.go 命中 0（注释行除外）
  - diff 中 dedicated_line 相关替换只出现 LineStatusNormal/Fault/Disabled 三常量；无新增常量定义（不造第三份）
  - excel_config.go:146,265 零改动（本 phase 不改导入链路，Q3 决策）
  </acceptance_criteria>
  <done>批 2 清零：operations 模块 + excel_handler 裸字面量全部常量化，双包引用判定记录进 SUMMARY，白名单收缩，构建与包测试全绿。Commit: refactor(operations): replace raw status literals with models constants (batch 2) per DICT-01</done>
</task>

<task type="auto">
  <name>T2: 批 3 — vdi/addomain/notice/knowledge/rpa/scheduler（簇 A + D 账号池 + E 反转）</name>
  <files>internal/services/vdi/vm_service_impl.go, internal/services/addomain/account_pool.go, internal/services/notice_service.go, internal/services/knowledge_service.go, internal/services/rpa/credential_service.go, internal/services/scheduler/job_service.go, internal/services/scheduler/job_log_service.go</files>
  <read_first>
  - scripts/check-status-literals.sh --baseline（vdi/addomain/notice/knowledge/rpa/scheduler 目录精确条目）
  - internal/models/knowledge.go:16-17 与 internal/models/notice_enhanced.go:23-26（E 簇反转常量——status=1 是「已发布」，绝不能套停用常量）
  - internal/models/log.go JobStatus/JobLog 常量（job_service/job_log_service 替换依据；job_log 成败语义若 69-01 补了常量则用之）
  - internal/services/addomain/account_pool.go（三态状态机常量在包内还是 models——读文件确认既有命名，CLAUDE.md Phase 36 记载 0=可用/1=停用/2=熔断）
  - internal/models/vdi.go（VDIServer 常量，69-01 补）、internal/models/notice_enhanced.go Notice Status（NoticeStatus，69-01 补）
  - 69-01-SUMMARY.md 新增常量清单（引用来源）
  </read_first>
  <action>
  1. --baseline 取批 3 全部条目（含 addomain 目录下 account_pool 之外的命中文件）。
  2. vm_service_impl.go 7 处：按上下文区分——VM 实体启停（簇 A，VDIServer/VM 常量）与任务状态（簇 B）分别映射，逐处读 model 注释。
  3. account_pool.go 3 处：AD 账号池三态（0=可用/1=停用/2=熔断）用包内/models 既有状态机常量（读文件确认命名后引用；若确无具名常量，在 addomain 包内按 base.go 风格补 ADAccountStatus* 三常量并在 SUMMARY 登记——这是唯一允许的批内补缺）。
  4. knowledge_service.go 4 处：E 簇——status = 0 是草稿、status = 1 是已发布，替换为 models.KnowledgeArticleStatusDraft / models.KnowledgeArticleStatusPublished；notice_service.go 同理区分 Notice 启停（NoticeStatus*）与发布态（PublishStatus*）两类字段，按 SQL 上下文的字段名（status vs publish_status）判定，拿不准的字段保持字面量并在 SUMMARY 标记待人工复核（宁缺勿错）。
  5. scheduler 两文件：job_service 启停用 models.JobStatusNormal/JobStatusPause（注意注释语义是 正常/暂停，非启用/停用）；job_log_service 按成败语义用 69-01 补的常量。
  6. rpa/credential_service.go：凭据启停（簇 A）——rpa model 常量核对（models/rpa.go），缺则按 69-01 同风格补并登记。
  7. 白名单删除本批条目；脚本退出码 0。
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/services/... ./internal/api/v1/... && bash scripts/check-status-literals.sh</automated>
  </verify>
  <acceptance_criteria>
  - `go build ./...` + 受影响包 go test 退出码 0
  - knowledge_service.go 的 diff 只出现 KnowledgeArticleStatusDraft/Published，不出现 Disabled/Stopped 套用（E 簇零误替的源码断言）
  - notice_service.go 每处替换在 SUMMARY 有字段语义判定记录（status=启停 vs publish_status=发布态）
  - 守护脚本退出码 0，白名单无 vdi/addomain/notice/knowledge/rpa/scheduler 条目
  - account_pool.go 若补了 ADAccountStatus* 常量，值 0/1/2 与 CLAUDE.md Phase 36 状态机记载一致且已登记 SUMMARY
  </acceptance_criteria>
  <done>批 3 清零：E 簇反转与 D 簇三态全部正确映射，白名单收缩，构建与包测试全绿。Commit: refactor(services): replace raw status literals with models constants (batch 3) per DICT-01</done>
</task>

<task type="auto">
  <name>T3: 批 4 — workorder/duty/discovery/execution/dispatch/monitor + 三 handler + F 簇收口（簇 B/C CASE WHEN）</name>
  <files>internal/services/workorder/base.go, internal/services/workorder/reconciliation_template.go, internal/services/duty_pool_service.go, internal/services/device_discovery_service.go, internal/services/config_execution_service.go, internal/services/command_dispatch_service.go, internal/services/monitor/server_service.go, internal/services/asset/fix_suggestion_monitor.go, internal/api/v1/system/notice_handler.go, internal/api/v1/monitor/oper_log_handler.go, internal/services/operations/geocoding_service.go</files>
  <read_first>
  - scripts/check-status-literals.sh --baseline（批 4 全部剩余条目——此批后白名单应只剩 geocoding）
  - internal/models/config_execution.go:19-27（ExecutionStatus 0-4 全家族——B 簇状态机唯一合法映射）
  - internal/services/config_execution_service.go:46-49,136,151-159（半迁移现状——struct 已用常量、SQL 未替换）
  - internal/models/duty.go（DutyPoolStatus 常量名与值）
  - internal/models/workorder.go（WorkOrderStatus 0-4 及类型常量）
  - internal/models/log.go（OperLog/Logininfor 成败常量，69-01 补）与 oper_log_handler.go:190 上下文
  - internal/services/operations/geocoding_service.go:332（F 簇豁免注释落点）
  </read_first>
  <action>
  1. --baseline 取全部剩余条目（workorder/duty_pool/device_discovery/config_execution/command_dispatch/monitor/asset fix_suggestion + api/v1 两 handler + 散点文件，interfaces 预估之外的全部纳入）。
  2. config_execution_service.go：raw SQL CASE WHEN（:46-49 等）按 struct 已用常量的同族映射补齐——status = 0/1/2/3/4 分别 Sprintf models.ExecutionStatusPending/Running/Success/Failed/Cancelled（以 config_execution.go 常量实际命名为准），AS 别名保持不变。
  3. device_discovery_service.go / command_dispatch_service.go：同属 B 簇状态机（若其模型用独立 Status 家族则读各自 model 文件映射，禁止跨实体套用 ExecutionStatus）。
  4. duty_pool_service.go:37-41 CASE WHEN：SUM(CASE WHEN status = 0 → models.DutyPoolStatus 对应常量（读 duty.go 确认命名，如 DutyPoolStatusEnabled/Disabled），Sprintf+int() 形态。
  5. workorder/base.go 4 处 + reconciliation_template.go 4 处：WorkOrderStatus 0-4 状态机映射（models/workorder.go 常量）。
  6. monitor/server_service.go（服务器监控状态语义按其 model 注释）、asset/fix_suggestion_monitor.go:181 的 Status: 0 注释 0=成功（簇 C——用成败常量替换并保留语义注释改写为常量引用）。
  7. oper_log_handler.go:190 与 notice_handler.go：分别按成败（簇 C）与 notice 启停/发布态（批 3 同法判定）替换。
  8. F 簇收口：geocoding_service.go:332 的 Status == 0（百度 API 返回码）不替换，在该行上方加单行注释「// F 簇:百度地图 API 返回码契约,不迁移到 models 常量(见 scripts/check-status-literals.sh 白名单)」。
  9. 白名单删除本批全部条目——终态白名单只剩 geocoding_service.go=F 簇一条；脚本退出码 0。运行全量 `go test ./...`。
  </action>
  <verify>
    <automated>go build ./... && go test ./... && bash scripts/check-status-literals.sh && bash scripts/check-status-literals.sh --baseline | wc -l</automated>
  </verify>
  <acceptance_criteria>
  - `go build ./...` 与 `go test ./...` 全绿
  - `bash scripts/check-status-literals.sh` 退出码 0；`--baseline` 输出行数 = 1（仅 geocoding_service.go）
  - config_execution_service.go 的 CASE WHEN 替换后 AS pending/running 等别名与替换前一致（grep 断言别名保留）
  - geocoding_service.go:332 保持字面量且上方有 F 簇注释（grep 'F 簇' 命中 1 行）
  - 全部 diff 无逻辑/SQL 结构改动（仅常量替换 + import + Sprintf 改造）
  </acceptance_criteria>
  <done>批 4 清零：DICT-01 替换面全部完成，守护白名单终态只剩 F 簇永久豁免，全仓构建与测试全绿。Commit: refactor(services): replace remaining status literals with constants and finalize guard allowlist (batch 4) per DICT-01</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| models 常量 → raw SQL 字符串（CASE WHEN Sprintf） | 状态机值写入聚合 SQL；簇映射错误即统计口径损坏 |
| 双包常量定义（models vs models/operations） → service 引用 | 引错包仍可编译（同名不同值域时语义漂移） |
| 守护白名单 → 替换进度 | 白名单是唯一进度账本；漏删条目即掩盖欠账 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-09 | Tampering | E 簇反转误替（knowledge/notice status=1 已发布被套停用常量）→ 已发布内容不可见 | mitigate | T2: 替换前读 model 常量注释；acceptance 源码断言 diff 只出现 KnowledgeArticleStatusDraft/Published；拿不准的字段宁保持字面量并标记待复核 |
| T-69-10 | Tampering | B 簇状态机误替（CASE WHEN status=1 执行中被套 Stop）→ 统计口径损坏 | mitigate | T3: config_execution 以同文件 struct 已用常量为映射基准（半迁移自洽化），AS 别名 grep 断言不变 |
| T-69-11 | Tampering | 双包同名常量引错包（RoomStatus 双定义） | mitigate | T1: 按 model struct 实际所在包 import；禁止新造第三份；SUMMARY 记录每文件判定 |
| T-69-12 | Repudiation | 替换引入行为变化但无测试覆盖 | accept(受控) | 纯常量等值替换 + 每批包级 go test + 全仓 go test ./... 终态门；替换面为查询条件/聚合，行为等值由常量值锁值测试背书 |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | 零依赖安装，Package Legitimacy Gate 不适用 |
</threat_model>

<verification>

**每任务边界：** 每批 `go build ./...` + 受影响包 go test + 守护脚本退出码 0 + 白名单条目删除。

**Plan 终态（DICT-01 替换面全部完成）：**
1. `bash scripts/check-status-literals.sh` 退出码 0，`--baseline` 输出仅 1 行（geocoding_service.go）
2. `go build ./...` + `go test ./...` 全绿
3. `go test ./internal/models/ -run TestStatusConstants` 仍绿（锁值未破坏）
4. 抽样 grep：internal/services/ + internal/api/v1/ 非注释 `status = [01]` 命中数 = 1（仅 geocoding F 簇）

</verification>

<success_criteria>

（ROADMAP Phase 69 SC#1 的 DICT-01 替换部分——「handler/service 层原先的裸 0/1 字面量替换为常量引用」）

1. 批 2 operations + excel_handler 清零（含 workstation_device_service 11 处结构体字面量、dedicated_line 三态、双包陷阱规避）—— 由 T1 交付
2. 批 3 vdi/addomain/notice/knowledge/rpa/scheduler 清零（E 簇反转 + 账号池三态正确映射）—— 由 T2 交付
3. 批 4 workorder/duty/discovery/execution/dispatch/monitor + 三 handler 清零，F 簇注释豁免，守护白名单终态 1 条 —— 由 T3 交付
4. 全仓 `go build ./...` / `go test ./...` 全绿，行为零变化 —— 三任务共同交付

</success_criteria>

<output>
Create `.planning/phases/69-dict-and-status-governance/69-03-SUMMARY.md` when done，必须包含：
- 3 批 × commit hash 对照表与每批文件数/替换处数（守护脚本基线前后快照佐证）
- 语义簇映射台账：每文件 → 簇 → 所用常量（尤其 E 簇/D 簇/双包判定/批内补缺常量）
- 白名单终态快照（只剩 geocoding 条目）
- 「保持字面量并标记待人工复核」的条目清单（若 T2 有保守跳过项，逐条列出字段与原因）
- go test ./... 全绿证据
</output>
