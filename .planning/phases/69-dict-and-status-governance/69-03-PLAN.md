---
phase: 69-dict-and-status-governance
plan: 03
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - internal/services/operations/building_service.go
  - internal/services/operations/floor_service.go
  - internal/services/operations/workstation_service.go
  - internal/services/operations/workstation_device_service.go
  - internal/services/operations/server_room_service.go
  - internal/services/operations/room_device_service.go
  - internal/services/operations/infopoint_service.go
  - internal/services/operations/dedicated_line_service.go
  - internal/services/operations/asset_service.go
  - internal/services/operations/excel_service.go
  - internal/api/v1/operations/excel_handler.go
  - scripts/check-status-literals.sh
autonomous: true
requirements: [DICT-01]

must_haves:
  truths:
    - "DICT-01: 批 2 完成——internal/services/operations/ 全部 service（building/floor/workstation/workstation_device/server_room/room_device/infopoint/dedicated_line/asset/excel）+ api/v1/operations/excel_handler 的裸 status 字面量清零（含 excel_service.go:1975/:2029 的 map/JSON 形态 `\"status\": 0`——守护脚本第四条模式的命中对象），守护白名单移除对应条目，`go build ./...` + `go test ./internal/services/operations/` 全绿"
    - "DICT-01: RoomStatus 双包定义陷阱规避——operations 批替换按 model struct 实际所在包引用（子包 operations.RoomStatusNormal 或 models.RoomStatusNormal），不新造第三份定义"
    - "DICT-01: 行为零变化——纯常量替换，无逻辑分支/SQL 结构改动；excel_config.go:146,265 零改动（Q3 决策：本 phase 只保证 seed 值一致，不改导入链路）"
  artifacts:
    - path: "internal/services/operations/workstation_device_service.go"
      provides: "11 处结构体字面量 Status: 0/1 全部常量化（批 2 主战场）"
      contains: "models."
    - path: "internal/services/operations/excel_service.go"
      provides: "map/JSON 形态 "status": 0 两处（:1975/:2029）常量化——第四条守护模式的清偿对象"
      contains: "Status"
    - path: "scripts/check-status-literals.sh"
      provides: "白名单 ratchet：批 2 条目删除（批 3/批 4 计划与本 plan 串行共享此白名单文件）"
      contains: "geocoding_service.go"
  key_links:
    - from: "internal/services/operations/*_service.go"
      to: "internal/models/operations/ + internal/models/operations.go 常量"
      via: "按 model struct 实际所在包 import 引用（双定义陷阱规避）"
      pattern: "operations\\.(RoomStatus|BuildingStatus|LineStatus)|(models)\\.(FloorStatus|RoomDeviceStatus)"
    - from: "scripts/check-status-literals.sh 白名单"
      to: "批 2 全部替换文件"
      via: "批完成即删除对应条目（ratchet 单调收缩）"
      pattern: "ALLOWED"
---

# Phase 69 Plan 03: DICT-01 批 2 — operations 模块 + excel 链路字面量替换（簇 A + D 专线三态）

**Status:** READY FOR EXECUTION
**Created:** 2026-08-19（2026-08-19 revision：由原 69-03（批 2-4 合体）拆出批 2 为独立 plan；纳入 checker 实测逃逸的 excel_service.go map/JSON 2 处）
**Source:** ROADMAP Phase 69（DICT-01）+ 69-RESEARCH.md A1/A2 批次表 + 69-PATTERNS.md
**决策依据:** orchestrator 规划要点 1 —— 按语义簇 A-F 分批替换，禁止一刀切；范本 department_cache_impl.go:65；CASE WHEN 用 Sprintf+int()；单批单 plan 以控制语义判断风险面

<objective>

完成 DICT-01 批 2：internal/services/operations/ 全部命中 service + api/v1/operations/excel_handler + excel_service 的 map/JSON 形态 2 处，全部裸 status 字面量替换为 models 常量引用；独立 commit、`go build ./...` 常绿、守护白名单移除批 2 条目。

Purpose: 69-01 已交付常量集、锁值测试与四模式守护脚本；本 plan 是欠账清偿的第一批（operations 模块 + excel 链路，约 11 文件），含双包定义陷阱与 11 处结构体字面量两处最高风险点。批 3（69-04）/批 4（69-05）与本 plan 串行执行（共享守护白名单文件 scripts/check-status-literals.sh）。
Output: 约 11 个 service/handler 文件常量化 + 守护白名单收缩；1 个批次 commit；`go build ./...` + `go test ./internal/services/operations/` 全绿。

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/69-dict-and-status-governance/69-RESEARCH.md
@.planning/phases/69-dict-and-status-governance/69-PATTERNS.md
@.planning/phases/69-dict-and-status-governance/69-01-SUMMARY.md

<interfaces>
<!-- 关键现状契约（已从代码库提取，executor 无需再探索） -->

语义簇替换对照表（RESEARCH A2 + PATTERNS 簇表，批 2 涉及簇 A 与 D）:

| 簇 | 语义 | 涉及文件（批 2） | 目标常量 | 严禁 |
|----|------|---------------|---------|------|
| A 通用启停 | 0=正常/启用, 1=停用 | operations 7 service + asset_service + excel_handler + excel_service map 2 处 | BuildingStatus/WorkstationStatus/RoomStatus/FloorStatus/InfoPoint 常量（69-01 补齐） | — |
| D 多态(3+值) | 0/1/2 | dedicated_line（0=正常/1=故障/2=停用） | operations.LineStatusNormal/Fault/Disabled | 把 1 当停用、2 之外乱映射的一刀切 |
| F 外部契约 | 百度 API 返回码 | geocoding_service.go:332（同目录但**不在本批替换面**） | 不替换（批 4 加豁免注释） | 替换 |

替换形态双模板:
- Where 参数化（范本 department_cache_impl.go:65）: Where("status = ?", models.DeptStatusNormal)
- CASE WHEN 聚合 Sprintf: fmt.Sprintf("SUM(CASE WHEN status = %d THEN 1 ELSE 0 END) AS enabled", int(models.DutyPoolStatusEnabled))

结构体字面量批 2 主战场: workstation_device_service.go 11 处 Status: 0/1 → 对应 model 常量（参照 user_sync_service.go:584 的 Status: models.UserStatusEnabled 写法）。

map/JSON 形态命中（checker 实测逃逸、69-01 T2 第四条模式已纳入守护）: excel_service.go:1975 与 :2029 两处 `"status":     0, // 正常`——map 字面量键值形态，前三条模式抓不到；本批按上下文实体语义替换为常量值。

CRITICAL 双包定义陷阱（批 2 必读）: RoomStatus 等在 internal/models/operations.go（package models，旧无注释）与 internal/models/operations/server_room.go（package operations，新带注释）双定义。excel_config.go 经 import operations 子包引用子包版。批 2 替换规则：读 service 文件操作的 model struct 定义位置——OpsServerRoom 在子包 operations 则用 operations.RoomStatusNormal；旧 ServerRoom/RoomDevice/Floor 在 models 包则用 models.RoomStatusNormal 等。禁止新造第三份、禁止混引。

批次文件清单权威来源: `bash scripts/check-status-literals.sh --baseline`（69-01 T2 交付，批 1 后剩余条目中取 internal/services/operations/ 与 internal/api/v1/operations/ 全部条目；下列目录分组为 RESEARCH 预估，以脚本实际输出为准，多不漏）:
- building/floor/workstation/workstation_device/server_room/room_device/infopoint/dedicated_line service 各若干 + asset_service.go 6 处 + excel_service.go map 形态 2 处 + excel_handler.go 2 处
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>T1: 批 2 — internal/services/operations/ + excel_handler + excel_service map 形态（簇 A + D 专线三态）</name>
  <files>internal/services/operations/building_service.go, internal/services/operations/floor_service.go, internal/services/operations/workstation_service.go, internal/services/operations/workstation_device_service.go, internal/services/operations/server_room_service.go, internal/services/operations/room_device_service.go, internal/services/operations/infopoint_service.go, internal/services/operations/dedicated_line_service.go, internal/services/operations/asset_service.go, internal/services/operations/excel_service.go, internal/api/v1/operations/excel_handler.go, scripts/check-status-literals.sh</files>
  <read_first>
  - scripts/check-status-literals.sh（先跑 --baseline 拿 operations 目录精确条目清单，确认 excel_service.go 的 map 形态命中已计入）
  - internal/models/operations.go 全文（旧版 FloorStatus/RoomStatus/RoomDeviceStatus 定义位置与所属包）
  - internal/models/operations/ 子包 building.go / server_room.go / dedicated_line.go / infopoint.go（新版常量与 struct 实际所在包——双定义陷阱判定依据）
  - internal/models/workstation.go（WorkstationStatus/WorkstationType）
  - internal/services/operations/workstation_device_service.go（11 处 Status: 0/1 集中区，替换前逐处读上下文）
  - internal/services/operations/excel_service.go:1975,:2029 上下文（map 键 "status" 对应哪个实体的默认状态——模板示例/upsert 默认值）
  - internal/services/operations/excel_config.go:146,265（该模块常量引用先例——operations 子包 import 方式；本 plan 不改此文件）
  - internal/services/system/department_cache_impl.go:65（目标态范本）
  - 69-01-SUMMARY.md 新增常量清单（InfoPointStatus 等批 2 引用来源）
  </read_first>
  <action>
  1. 跑 bash scripts/check-status-literals.sh --baseline，取 internal/services/operations/ 与 internal/api/v1/operations/ 全部条目（interfaces 预估清单之外若还有命中文件，一并纳入本批）。
  2. 逐文件处理，每文件先确认操作的 model struct 定义所在包再选常量引用（双定义陷阱）：OpsServerRoom → operations.RoomStatusNormal/Stopped（子包）；旧 ServerRoom/RoomDevice/Floor → models.RoomStatusNormal / models.RoomDeviceStatus* / models.FloorStatus*（models 包）；Building → operations.BuildingStatus*（building.go 子包）；dedicated_line 三态 → operations.LineStatusNormal/Fault/Disabled（D 簇，禁止把 1 当停用、2 当停用之外的值乱映射）；InfoPoint → 69-01 补的常量。
  3. workstation_device_service.go 的 11 处结构体字面量：Status: 0/1 → 对应实体常量（设备/工位关联实体的 status 语义按其 model 文件注释；若语义为 workstation 启停用 models.WorkstationStatus*，为设备状态用相应 Device 常量——逐处读上下文判定，SUMMARY 记录判定）。
  4. asset_service.go 6 处 + excel_handler.go 2 处同法；excel_handler 的 status 过滤是导入数据启停语义（簇 A）。
  5. excel_service.go :1975 与 :2029 两处 map 字面量 `"status": 0, // 正常`：读上下文确认该 map 是哪条导入链路的默认状态（building/floor/workstation 模板行或 upsert 默认——按实际实体选常量），值替换为对应 models 常量（map value 为 int，直接放常量即可；若常量类型非 int 用 int() 包裹）。
  6. Where 参数化 / CASE WHEN Sprintf+int()；不修改逻辑与 SQL 结构；删除被替换行的裸值行尾注释（0=正常 之类）。
  7. 完成后从守护白名单删除本批全部条目（geocoding_service.go 属 F 簇豁免条目，保留），重跑脚本退出码 0。
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/services/operations/ ./internal/api/v1/... && bash scripts/check-status-literals.sh</automated>
  </verify>
  <acceptance_criteria>
  - `go build ./...` 与 `go test ./internal/services/operations/` 退出码 0
  - 守护脚本退出码 0 且白名单无 internal/services/operations/ 与 internal/api/v1/operations/ 条目（geocoding 保留）
  - grep -rn 'Status: *[01]\b' internal/services/operations/workstation_device_service.go 命中 0（注释行除外）
  - grep -En '"status":[[:space:]]*[0-9]' internal/services/operations/excel_service.go 命中 0（数字锚定口径，与守护模式 (d) 一致；键名 `"status":` 在值常量化后保留属预期，不作 0 命中要求）
  - diff 中 dedicated_line 相关替换只出现 LineStatusNormal/Fault/Disabled 三常量；无新增常量定义（不造第三份）
  - excel_config.go:146,265 零改动（本 phase 不改导入链路，Q3 决策）
  </acceptance_criteria>
  <done>批 2 清零：operations 模块 + excel_handler + excel_service map 形态全部常量化，双包引用判定记录进 SUMMARY，白名单收缩，构建与包测试全绿。Commit: refactor(operations): replace raw status literals with models constants (batch 2) per DICT-01</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| models 常量 → raw SQL 字符串（CASE WHEN Sprintf） | 状态机值写入聚合 SQL；簇映射错误即统计口径损坏 |
| 双包常量定义（models vs models/operations） → service 引用 | 引错包仍可编译（同名不同值域时语义漂移） |
| 守护白名单 → 替换进度 | 白名单是唯一进度账本；漏删条目即掩盖欠账 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-11 | Tampering | 双包同名常量引错包（RoomStatus 双定义） | mitigate | T1: 按 model struct 实际所在包 import；禁止新造第三份；SUMMARY 记录每文件判定 |
| T-69-12 | Repudiation | 替换引入行为变化但无测试覆盖 | accept(受控) | 纯常量等值替换 + 包级 go test + `go test ./...` 终态门（69-05）；替换面为查询条件/聚合，行为等值由常量值锁值测试背书 |
| T-69-17 | Tampering | map/JSON 形态字面量绕过守护（前三模式盲区） | mitigate | 69-01 T2 第四条模式 `"status":\s*[0-9]` 已纳入守护；本批 excel_service.go 2 处清偿后 grep 断言命中为 0 |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | 零依赖安装，Package Legitimacy Gate 不适用 |
</threat_model>

<verification>

**每任务边界：** `go build ./...` + 受影响包 go test + 守护脚本退出码 0 + 白名单条目删除。

**Plan 终态（批 2 完成）：**
1. `bash scripts/check-status-literals.sh` 退出码 0，白名单不含 operations/api-v1-operations 条目
2. `go build ./...` + `go test ./internal/services/operations/` 全绿
3. `go test ./internal/models/ -run TestStatusConstants` 仍绿（锁值未破坏）

</verification>

<success_criteria>

（ROADMAP Phase 69 SC#1 的 DICT-01 替换部分——批 2）

1. operations 9 service + excel_handler + excel_service map 形态 2 处清零（含 workstation_device_service 11 处结构体字面量、dedicated_line 三态、双包陷阱规避）—— 由 T1 交付
2. 守护白名单对应条目收缩，第四条模式命中清偿 —— 由 T1 交付
3. 行为零变化（excel_config 不动，Q3 决策）—— 由 T1 diff 约束交付

</success_criteria>

<output>
Create `.planning/phases/69-dict-and-status-governance/69-03-SUMMARY.md` when done，必须包含：
- 1 批 × commit hash 对照表与文件数/替换处数（守护脚本基线前后快照佐证）
- 语义簇映射台账：每文件 → 簇 → 所用常量（尤其 D 簇/双包判定/excel_service map 形态 2 处的实体判定）
- workstation_device_service 11 处逐处判定记录（实体 → 常量）
- 白名单快照（批 2 条目删除后剩余条目——批 3/批 4 的工作面）
</output>
