---
phase: 69-dict-and-status-governance
plan: 07
type: execute
wave: 3
depends_on: ["69-02", "69-06"]
files_modified:
  - xingran-react-frontend/src/pages/system/user/index.tsx
  - xingran-react-frontend/src/pages/system/user/utils.tsx
  - xingran-react-frontend/src/pages/system/user/constants.ts
  - xingran-react-frontend/src/pages/operations/workstations/index.tsx
  - xingran-react-frontend/src/pages/operations/workstations/modals/EditModal.tsx
  - xingran-react-frontend/src/pages/operations/workstations/constants.tsx
  - xingran-react-frontend/src/pages/duty/holidays/index.tsx
  - xingran-react-frontend/src/pages/duty/holidays/modals/EditModal.tsx
  - xingran-react-frontend/src/pages/duty/holidays/modals/BatchModal.tsx
  - xingran-react-frontend/src/pages/duty/holidays/constants.tsx
  - xingran-react-frontend/src/pages/network/devices/index.tsx
  - xingran-react-frontend/src/pages/network/devices/constants.ts
autonomous: true
requirements: [DICT-03]

must_haves:
  truths:
    - "DICT-03: user/workstations/duty holidays/network devices 四页 type 类下拉迁移 useDict（dedicated-lines 三件套：Option 渲染 + dictLabel 回退 + isDefault 默认值），配 `?? 原静态数组` 兜底——字典管理页改 label 后下拉随之变化，字典接口异常时下拉仍有静态选项"
  artifacts:
    - path: "xingran-react-frontend/src/pages/system/user/index.tsx"
      provides: "性别下拉 useDict(sys_user_sex) 迁移 + 静态兜底"
      contains: "useDict("
    - path: "xingran-react-frontend/src/pages/operations/workstations/index.tsx"
      provides: "工位类型下拉 useDict(ops_workstation_type) 迁移 + 静态兜底"
      contains: "useDict("
    - path: "xingran-react-frontend/src/pages/duty/holidays/index.tsx"
      provides: "假日类型下拉 useDict(duty_holiday_type) 迁移 + 静态兜底"
      contains: "useDict("
    - path: "xingran-react-frontend/src/pages/network/devices/index.tsx"
      provides: "设备类型下拉 useDict(network_device_type) 迁移 + 静态兜底"
      contains: "useDict("
  key_links:
    - from: "src/pages/{system/user,operations/workstations,duty/holidays,network/devices}"
      to: "useDict(\"sys_user_sex\"|\"ops_workstation_type\"|\"duty_holiday_type\"|\"network_device_type\")"
      via: "React Query hook + 静态 fallback + dictLabel 回退 + isDefault 默认"
      pattern: "useDict\\("
    - from: "四页消费的 dictType"
      to: "migration_208 seed 的 11 组字典数据（69-02 交付）"
      via: "POST /system/dicts/data/list 返回 dictValue/dictLabel/isDefault"
      pattern: "sys_user_sex|ops_workstation_type|duty_holiday_type|network_device_type"
---

# Phase 69 Plan 07: DICT-03 前端字典线 — 四页 type 下拉迁移 useDict（sys_user_sex / ops_workstation_type / duty_holiday_type / network_device_type）

**Status:** READY FOR EXECUTION
**Created:** 2026-08-19（2026-08-19 revision：由原 69-04（前端双线 + DICT-04 合体）拆出 useDict 迁移线为独立 plan；任务按页两两拆分；verify 统一 npx vitest run 避免 watch 模式挂起）
**Source:** ROADMAP Phase 69（DICT-03）+ 69-RESEARCH.md A5 + 69-PATTERNS.md
**决策依据:** orchestrator 规划要点 3 + Q4 —— type 类走 useDict；依赖 69-02（seed 数据——四个 dictType 均由 migration_208 提供）与 69-06（constants 文件 STATUS 收敛先行，避免同文件冲突）

<objective>

四页 type 类下拉迁移 useDict 消费 69-02 seed 的字典（user 性别 / 工位类型 / 假日类型 / 网络设备类型），每处配静态 fallback（字典空态/接口异常时回退原静态 OPTIONS，原导出不删除）——让「字典管理页改 label → 消费页下拉变化」成为现实（DICT-03 SC 核心验收，checkpoint 在 69-08 验证）。

Purpose: useDict hook 与字典 API 基建完好（零改动），欠的只是消费方迁移；69-02 落地 seed 后四页 dictType 数据齐备，本 plan 完成消费侧接线。
Output: 12 个前端文件修改（4 组 constants 保留 fallback + 8 个页面/组件迁移）；type-check / lint / vitest 全绿；每页对或合并 1-2 个 commit。

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/69-dict-and-status-governance/69-RESEARCH.md
@.planning/phases/69-dict-and-status-governance/69-PATTERNS.md
@.planning/phases/69-dict-and-status-governance/69-02-SUMMARY.md
@.planning/phases/69-dict-and-status-governance/69-06-SUMMARY.md

<interfaces>
<!-- 关键现状契约（已从代码库提取，executor 无需再探索） -->

useDict hook（src/hooks/useDict.ts，零改动）: `useDict(dictType: string)` 返回 React Query 结果，data 为 DictItem[]；DictItem 字段 dictValue: string / dictLabel / isDefault?: boolean / listClass；无内置静态 fallback（:53 result.data?.list ?? []），消费方必须自带兜底。

迁移成品范本（dedicated-lines/index.tsx，Wave 5 留下，唯一完整三件套）:
```typescript
// :91-93 hook 消费
const { data: lineTypeDict = [] } = useDict("ops_dedicated_line_type");
// :241-244 isDefault 默认值
const defaultType = lineTypeDict.find((d) => d.isDefault)?.dictValue || lineTypeDict[0]?.dictValue;
// :253-261 dictLabel 回退
const getLineTypeText = (type: string) => {
  const dictItem = lineTypeDict.find((d) => d.dictValue === type);
  return dictItem?.dictLabel || type;
};
// :593-596 Option 渲染
{lineTypeDict.map((d) => (<Option key={d.dictValue} value={d.dictValue}>{d.dictLabel}</Option>))}
```
本 plan 迁移页在此之上加一层兜底（RESEARCH Pitfall 4 / SC#3 明确要求）：下拉 options = 字典 data 非空时渲染字典项，否则回退渲染原静态 OPTIONS——原 OPTIONS 导出一律保留降级为 fallback，不删除。

4 个 useDict 迁移目标（dictType 均已由 69-02 seed，值与 isDefault 见 69-02-SUMMARY）:
| 页面 | 迁移项 | dictType | 涉及文件 |
|------|--------|----------|---------|
| 系统用户 | GENDER_OPTIONS 下拉与 label 渲染 | sys_user_sex | user/index.tsx（搜索表单+编辑弹窗 Select）、user/utils.tsx（性别 label 渲染函数改为接收 dict 或保留静态 map 兜底）、user/constants.ts（保留 GENDER_OPTIONS 为 fallback） |
| 工位管理 | TYPE_OPTIONS（工位类型） | ops_workstation_type | workstations/index.tsx、modals/EditModal.tsx、constants.tsx（保留 fallback） |
| 假日管理 | HOLIDAY_TYPE_OPTIONS | duty_holiday_type | duty/holidays/index.tsx、modals/EditModal.tsx、modals/BatchModal.tsx、constants.tsx（保留 fallback）；columns.tsx/utils.tsx 的 Tag 渲染保留静态映射兜底（非 hook 上下文） |
| 网络设备 | DEVICE_TYPE_OPTIONS | network_device_type | network/devices/index.tsx、constants.ts（保留 fallback）；VENDOR_OPTIONS 不迁（无字典组，保持现状） |

依赖契约: 四页 constants 文件的 STATUS_OPTIONS 已由 69-06 改为共享模块引用——本 plan 触碰这些文件时 STATUS 相关导出不动；工作区护栏（`src/pages/settings/index.tsx` 与 `src/pages/system/settings-page/index.tsx` 未提交改动）任何任务不得触碰。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>T1: user 页 + workstations 页 type 下拉迁移（sys_user_sex / ops_workstation_type）</name>
  <files>xingran-react-frontend/src/pages/system/user/index.tsx, xingran-react-frontend/src/pages/system/user/utils.tsx, xingran-react-frontend/src/pages/system/user/constants.ts, xingran-react-frontend/src/pages/operations/workstations/index.tsx, xingran-react-frontend/src/pages/operations/workstations/modals/EditModal.tsx, xingran-react-frontend/src/pages/operations/workstations/constants.tsx</files>
  <read_first>
  - xingran-react-frontend/src/pages/operations/dedicated-lines/index.tsx:91-96,241-266,593-609（三件套唯一完整范本——先通读再动手）
  - xingran-react-frontend/src/hooks/useDict.ts（返回结构与 DictItem 类型）
  - 本任务 6 个待改文件各自的 OPTIONS 消费点（grep GENDER_OPTIONS / TYPE_OPTIONS 定位 Select/渲染处）
  - 69-02-SUMMARY.md 的 11 组 dictType 清单（确认 sys_user_sex / ops_workstation_type 的值与 isDefault 项——下拉 value 类型必须与后端字段类型匹配：sys_user_sex 的 dictValue 是 "0"/"1"/"2" 字符串，而 user 页性别字段是 number，Option value 需做 String(gender) 对齐或后端字段核对，逐处确认）
  </read_first>
  <action>
  对两页套用 dedicated-lines 三件套 + 静态兜底：
  1. 通用改造形态：组件内 useDict(目标 dictType) 取 data；下拉 options 渲染 = data 非空时 data.map 渲染 Option（key/value 用 dictValue、children 用 dictLabel），data 为空时回退渲染原静态 OPTIONS（import 保留，不删除原导出——Pitfall 4）；表格/文本处的 label 渲染用 dictLabel || 静态 map 回退；新增/编辑表单默认值优先 data 中 isDefault 项的 dictValue。
  2. user 页：GENDER_OPTIONS → useDict("sys_user_sex")；index.tsx 的搜索与编辑处 Select 迁移；utils.tsx 的性别 label 函数改为优先查字典入参或保留静态映射兜底（非 hook 上下文允许静态）；注意性别字段 number 与 dictValue string 的类型对齐（表单提交值不改变现有 API 契约——若现有提交 number 则 Option value 保持 number，渲染 key 用 dictValue）。
  3. workstations 页：TYPE_OPTIONS → useDict("ops_workstation_type")；index.tsx 搜索/表格与 modals/EditModal.tsx 表单 Select 迁移（EditModal 是独立组件，hook 在弹窗组件内调用）。
  4. 两页 constants 文件的 STATUS_OPTIONS（69-06 已改共享引用）不动，只确保 GENDER_OPTIONS/TYPE_OPTIONS 导出保留作 fallback。
  5. 不改 useDict.ts 本体、不改任何 API 层、不动 settings 相关文件（工作区护栏）；useEffect 依赖遵守 CLAUDE.md 稳定性规范（hook 返回的 data 引用来自 React Query 缓存，直接用）。
  6. 门禁：npm run type-check && npm run lint && npx vitest run。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npm run type-check && npm run lint && npx vitest run && grep -c "useDict(" src/pages/system/user/index.tsx src/pages/operations/workstations/index.tsx</automated>
  </verify>
  <acceptance_criteria>
  - `npm run type-check` / `npm run lint` / `npx vitest run` 全绿（run 模式，非 watch）
  - grep useDict( 在 user/index.tsx 与 workstations/index.tsx 各命中 ≥ 1
  - GENDER_OPTIONS / TYPE_OPTIONS 常量导出仍存在（fallback 未被删除）
  - 每个迁移 Select 处存在字典空态回退分支（源码断言：渲染处同时存在 dict 变量与静态 OPTIONS 引用）
  - 表单提交值类型与迁移前一致（无 API 契约变化——type-check 通过 + 人工 diff 复核）
  </acceptance_criteria>
  <done>user 页性别与 workstations 页工位类型下拉走字典 + 静态兜底，type-check/lint/vitest 全绿。Commit: refactor(frontend): migrate user/workstations type options to useDict with static fallback per DICT-03</done>
</task>

<task type="auto">
  <name>T2: duty/holidays 页 + network/devices 页 type 下拉迁移（duty_holiday_type / network_device_type）</name>
  <files>xingran-react-frontend/src/pages/duty/holidays/index.tsx, xingran-react-frontend/src/pages/duty/holidays/modals/EditModal.tsx, xingran-react-frontend/src/pages/duty/holidays/modals/BatchModal.tsx, xingran-react-frontend/src/pages/duty/holidays/constants.tsx, xingran-react-frontend/src/pages/network/devices/index.tsx, xingran-react-frontend/src/pages/network/devices/constants.ts</files>
  <read_first>
  - xingran-react-frontend/src/pages/operations/dedicated-lines/index.tsx:91-96,241-266,593-609（三件套范本）
  - xingran-react-frontend/src/hooks/useDict.ts（返回结构与 DictItem 类型）
  - 本任务 6 个待改文件各自的 OPTIONS 消费点（grep HOLIDAY_TYPE_OPTIONS / DEVICE_TYPE_OPTIONS 定位 Select/渲染处）
  - 69-02-SUMMARY.md 的 dictType 清单（duty_holiday_type / network_device_type 的值与 isDefault 项；dictValue 均为字符串码，确认页面字段类型对齐）
  </read_first>
  <action>
  对两页套用 dedicated-lines 三件套 + 静态兜底（与 T1 同一改造形态）：
  1. duty/holidays 页：HOLIDAY_TYPE_OPTIONS → useDict("duty_holiday_type")；index.tsx、EditModal、BatchModal 的类型 Select 迁移；columns.tsx/utils.tsx 的 Tag 渲染保留静态映射兜底（明确不迁，SUMMARY 记录）。
  2. network/devices 页：DEVICE_TYPE_OPTIONS → useDict("network_device_type")；index.tsx Select 与设备类型 label 渲染迁移；VENDOR_OPTIONS 保持现状（无字典组）。
  3. 两页 constants 文件的原 OPTIONS 导出保留作 fallback；STATUS 相关导出（若存在）不动。
  4. 不改 useDict.ts 本体、不改任何 API 层、不动 settings 相关文件（工作区护栏）；useEffect 依赖遵守 CLAUDE.md 稳定性规范。
  5. 门禁：npm run type-check && npm run lint && npx vitest run。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npm run type-check && npm run lint && npx vitest run && grep -c "useDict(" src/pages/duty/holidays/index.tsx src/pages/network/devices/index.tsx</automated>
  </verify>
  <acceptance_criteria>
  - `npm run type-check` / `npm run lint` / `npx vitest run` 全绿（run 模式，非 watch）
  - grep useDict( 在 holidays/index.tsx 与 devices/index.tsx 各命中 ≥ 1
  - HOLIDAY_TYPE_OPTIONS / DEVICE_TYPE_OPTIONS 常量导出仍存在（fallback 未被删除）
  - 每个迁移 Select 处存在字典空态回退分支（源码断言）
  - VENDOR_OPTIONS 与 columns/utils 静态渲染未被改动（明确不迁清单生效）
  </acceptance_criteria>
  <done>holidays 页假日类型与 devices 页设备类型下拉走字典 + 静态兜底，四页迁移全部完成，type-check/lint/vitest 全绿。Commit: refactor(frontend): migrate holidays/devices type options to useDict with static fallback per DICT-03</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sys_dict 字典值（管理员可配）→ 前端下拉/表格渲染 | dictValue/dictLabel 仅作展示与筛选传参；status 类代码分支语义不进此通道（Q2） |
| 字典 API（/system/dicts/data/list）→ 页面可用性 | 接口异常/空数据不得导致下拉空白（fallback 边界） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-14 | Denial of Service | 迁移后字典接口异常 → 下拉空白不可用 | mitigate | T1/T2: 每个迁移 Select 强制字典空态回退静态 OPTIONS（acceptance 断言 fallback 分支存在）；原 OPTIONS 导出保留降级不删除 |
| T-69-16 | Tampering | 迁移改变表单提交值类型（number ↔ string）破坏 API 契约 | mitigate | T1/T2: 提交值类型与迁移前一致约束 + type-check 门 + 人工 diff 复核 |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | 零依赖安装（React Query/vitest 均仓内既有），Package Legitimacy Gate 不适用 |
</threat_model>

<verification>

**每任务边界：** T1/T2 `npm run type-check` + lint + `npx vitest run`（run 模式，严禁裸 npm run test 的 watch 形态）+ grep useDict 断言。

**Plan 终态：**
1. `npm run type-check && npm run lint && npx vitest run` 全绿
2. grep: 4 页 index.tsx 各有 useDict(；四组原 OPTIONS 常量导出保留（fallback 完好）
3. 每个迁移 Select 处字典空态回退分支存在（源码断言）

</verification>

<success_criteria>

（ROADMAP Phase 69 SC#3 的 useDict 迁移部分）

1. type 类四页走 useDict 且保留静态 fallback —— 由 T1/T2 交付
2. 已迁移下拉在字典管理改值后选项随之变化 —— 由 T1/T2 实现 + 69-08 checkpoint 第 3 步验收

</success_criteria>

<output>
Create `.planning/phases/69-dict-and-status-governance/69-07-SUMMARY.md` when done，必须包含：
- 2 个 task × commit hash 对照表
- 四页迁移台账（页面 / dictType / 涉及文件 / fallback 方式 / 提交值类型结论）
- 明确不迁清单及理由（VENDOR_OPTIONS、columns/utils 静态渲染、其余 OPTIONS 组——后续批次候选）
</output>
