---
phase: 115-selector-completion-zustand-selector
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - xingran-react-frontend/src/router/RouteGuard.tsx
  - xingran-react-frontend/src/router/DynamicRoutes.tsx
  - xingran-react-frontend/src/pages/operations/building-spaces-3d/index.tsx
  - xingran-react-frontend/src/pages/operations/building-spaces-3d/components/BuildingView3D.tsx
  - xingran-react-frontend/src/pages/operations/building-spaces-3d/components/FloorView3D.tsx
  - xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMap.tsx
  - xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMapGL.tsx
autonomous: true
requirements:
  - SELECTOR-03
  - SELECTOR-04
must_haves:
  truths:
    - "RouteGuard 仅订阅 s.permissions（别名 userPermissions 保留），41 行权限判断逻辑与文件头 UX-only 注释逐字不变——SELECTOR-03 / V4 守护项"
    - "DynamicRoutes 订阅 5 个独立 selector（allMenus/fetchAll/permissions + isAuthenticated/initialized），effect 依赖不变，菜单加载时序与 getState/setState 静态方法一行不动（Phase 118 DATA-02 前向兼容）——SELECTOR-03"
    - "3D 页 5 处 visualizationStore 整店订阅全部为字段级 selector（index:36 / BuildingView3D:59 / FloorView3D:58-59 / HubeiMap:78 / HubeiMapGL:77）——SELECTOR-04"
    - "menuStore 的 loading/lastFetchTime/error 高频写入不再触发 RouteGuard/DynamicRoutes 重渲（本 phase 实质重渲收益主锚点）"
  artifacts:
    - path: "xingran-react-frontend/src/router/RouteGuard.tsx"
      provides: "permissions 单字段 selector 订阅"
      contains: "useMenuStore((s) => s.permissions)"
    - path: "xingran-react-frontend/src/router/DynamicRoutes.tsx"
      provides: "menuStore 3 字段 + authStore 2 字段独立 selector 订阅"
      contains: "useMenuStore((s) => s.allMenus)"
    - path: "xingran-react-frontend/src/pages/operations/building-spaces-3d/components/FloorView3D.tsx"
      provides: "4 个独立 selector（selectedFloor/selectedBuilding/navigateToBuilding/navigateToMap）"
      contains: "useVisualizationStore((s) => s.selectedFloor)"
  key_links:
    - from: "xingran-react-frontend/src/router/RouteGuard.tsx"
      to: "useMenuStore permissions 单字段订阅"
      via: "useMenuStore((s) => s.permissions)"
      pattern: "useMenuStore\\(\\(s\\) => s\\.permissions\\)"
    - from: "xingran-react-frontend/src/router/DynamicRoutes.tsx"
      to: "menuStore/authStore 逐字段订阅"
      via: "5 个独立 selector，effect 依赖 [isAuthenticated, initialized, allMenus.length, fetchAll] 不变"
      pattern: "use(Auth|Menu)Store\\(\\(s\\) =>"
    - from: "xingran-react-frontend/src/pages/operations/building-spaces-3d/（5 文件）"
      to: "useVisualizationStore 逐字段订阅（共 11 个 selector）"
      via: "每字段一次 useVisualizationStore((s) => s.field)"
      pattern: "useVisualizationStore\\(\\(s\\) =>"
---

<objective>
SELECTOR-03/04：路由层（RouteGuard.tsx:33 + DynamicRoutes.tsx:105-106）与 3D 页 5 处 visualizationStore 整店订阅改为逐字段 selector 订阅（D-05 范本风格）。

Purpose: 菜单刷新过程（fetchAll 置 loading → 写 lastFetchTime → 置 error）不再重渲整棵已认证路由树——这是本 phase 实质重渲收益的主锚点（RESEARCH Pitfall 4）；3D 页 camera/filters 等高频 store 写入只重渲真正消费该字段的组件。Phase 118 (DATA-02) 与本 plan 同触 DynamicRoutes.tsx，本 plan 只改订阅形态、不动数据流。
Output: 2 个路由文件 + 5 个 3D 页文件订阅形态改造，RouteGuard 权限逻辑逐字保留，既有测试/lint/type-check 零回归。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/115-selector-completion-zustand-selector/115-CONTEXT.md
@.planning/phases/115-selector-completion-zustand-selector/115-RESEARCH.md
@.planning/phases/115-selector-completion-zustand-selector/115-PATTERNS.md

<interfaces>
<!-- PATTERNS.md 已实读核验全部行号（2026-09-14，Phase 114 漂移已修正）；逐文件现状与目标形态： -->

RouteGuard.tsx:33 现状 `const { permissions: userPermissions } = useMenuStore();` → 目标 `const userPermissions = useMenuStore((s) => s.permissions);`；41 行 `permissions.some((p) => userPermissions.includes(p))` 逐字保留。

DynamicRoutes.tsx:105-106 现状 `const { allMenus, fetchAll, permissions } = useMenuStore();` + `const { isAuthenticated, initialized } = useAuthStore();` → 5 个独立 selector；effect（131-137）依赖 `[isAuthenticated, initialized, allMenus.length, fetchAll]` 不变；117-125 行 useAuthStore.getState()/setState(...) 静态方法不改造；151 行 routeConfigManager.initialize 一行不动。

3D 页 5 处（visualizationStore，共 11 个 selector）：index.tsx:36 → viewLevel（1）；BuildingView3D.tsx:59 → selectedBuilding + navigateToMap（2）；FloorView3D.tsx:58-59 → selectedFloor + selectedBuilding + navigateToBuilding + navigateToMap（4，selectedFloor 在 loadWorkstations 的 useCallback 依赖 :79 [selectedFloor] 中直接消费，引用语义不变）；HubeiMap.tsx:78 → clearSelection + navigateToBuilding（2）；HubeiMapGL.tsx:77 → clearSelection + navigateToBuilding（2）。

4 个 3D 相关测试文件全部用真实 store（setState 注入），零 mock 改动。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 路由层 selector 化——RouteGuard:33 + DynamicRoutes:105-106（SELECTOR-03，含 V4 守护项）</name>
  <files>xingran-react-frontend/src/router/RouteGuard.tsx, xingran-react-frontend/src/router/DynamicRoutes.tsx</files>
  <read_first>
    - xingran-react-frontend/src/router/RouteGuard.tsx（全文：33 行订阅、41 行权限判断、文件头"这不是安全边界"注释）
    - xingran-react-frontend/src/router/DynamicRoutes.tsx（全文：105-106 订阅、117-125 getState/setState、131-137 fetchAll effect、151 routeConfigManager.initialize）
    - xingran-react-frontend/src/components/layout/shared/useRouteTabs.ts（D-05 范本 44-48 行：跨两 store 逐字段订阅形态）
    - .planning/phases/115-selector-completion-zustand-selector/115-PATTERNS.md（Pattern Assignments → RouteGuard 段 + DynamicRoutes 段：目标形态 + 本文件 3 条纪律）
    - .planning/phases/115-selector-completion-zustand-selector/115-RESEARCH.md（Security Domain V4 守护项 + Anti-Patterns"不要订阅 s.allMenus.length"）
  </read_first>
  <action>
RouteGuard.tsx：33 行改为 `const userPermissions = useMenuStore((s) => s.permissions);`（保留 userPermissions 别名）。41 行权限判断 `permissions.some((p) => userPermissions.includes(p))` 与文件头 UX-only 注释逐字保留——selector 化只换数据来源形态，不改变权限数据（同一 permissions 数组引用）。

DynamicRoutes.tsx：105-106 两行解构替换为 5 个独立 const selector——useMenuStore 取 allMenus / fetchAll / permissions，useAuthStore 取 isAuthenticated / initialized。纪律（ROADMAP 跨 phase 冲突约束）：
1. 只改订阅形态——fetchAll 调用处 effect（131-137）与 routeConfigManager.initialize（151）一行不动（Phase 118 DATA-02 依赖本文件菜单加载时序与 TTLMenuCache 语义）。
2. effect 依赖 `[isAuthenticated, initialized, allMenus.length, fetchAll]` 不变；订阅 `s.allMenus`（数组引用），禁止改成 `s => s.allMenus.length`（RESEARCH Anti-Patterns 明示）。
3. 117-125 行 `useAuthStore.getState()` / `useAuthStore.setState(...)` 是 store 静态方法（非 hook 订阅），原样保留。
禁止对象 selector 与 useShallow（项目 0 使用，逐字段方案）。
  </action>
  <verify>
    <automated>! grep -qE "use(Menu|Auth)Store\(\)" xingran-react-frontend/src/router/RouteGuard.tsx xingran-react-frontend/src/router/DynamicRoutes.tsx && grep -qF "permissions.some((p) => userPermissions.includes(p))" xingran-react-frontend/src/router/RouteGuard.tsx && test "$(grep -cE 'use(Menu|Auth)Store\(\(s\) =>' xingran-react-frontend/src/router/DynamicRoutes.tsx)" -eq 5 && cd xingran-react-frontend && npx vitest run src/router/__tests__/routeGuard-lastpath.test.tsx && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - behavior: RouteGuard 权限门控行为不变（同一 permissions 引用来源）；DynamicRoutes 菜单加载时序、InitializingFallback 门控、getState/setState 静态写入行为全部不变
    - source-assertion: RouteGuard.tsx 与 DynamicRoutes.tsx 内 `useMenuStore()` / `useAuthStore()` 无参调用 == 0
    - source-assertion: RouteGuard.tsx 含逐字 `permissions.some((p) => userPermissions.includes(p))`（grep -qF 命中）
    - source-assertion: DynamicRoutes.tsx 内 `use(Menu|Auth)Store((s) =>` 计数 == 5
    - test-command: `cd xingran-react-frontend && npx vitest run src/router/__tests__/routeGuard-lastpath.test.tsx` 全绿
    - cli: `cd xingran-react-frontend && npm run type-check` exit 0
  </acceptance_criteria>
  <done>
RouteGuard 仅订阅 permissions 且权限逻辑/注释逐字保留；DynamicRoutes 5 个独立 selector、数据流零改动；路由层无参调用归零；routeGuard 测试全绿；type-check 通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: 3D 页 5 处 visualizationStore 逐字段 selector 化（SELECTOR-04）</name>
  <files>xingran-react-frontend/src/pages/operations/building-spaces-3d/index.tsx, xingran-react-frontend/src/pages/operations/building-spaces-3d/components/BuildingView3D.tsx, xingran-react-frontend/src/pages/operations/building-spaces-3d/components/FloorView3D.tsx, xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMap.tsx, xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMapGL.tsx</files>
  <read_first>
    - 上述 5 个待改文件（各处订阅行 + 周边消费上下文，尤其 FloorView3D :58-59 订阅与 :79 useCallback 依赖 [selectedFloor]）
    - .planning/phases/115-selector-completion-zustand-selector/115-PATTERNS.md（Pattern Assignments → SELECTOR-04 段：5 文件逐处现状/目标字段清单对照表）
    - .planning/phases/115-selector-completion-zustand-selector/115-RESEARCH.md（Pitfall 5：FloorView3D 一次解构 4 字段必须拆 4 个独立调用）
    - xingran-react-frontend/src/components/layout/shared/useRouteTabs.ts（D-05 范本）
  </read_first>
  <action>
按 PATTERNS 对照表逐文件替换（行号为 2026-09-14 实读核验值）：
1. index.tsx:36 → `const viewLevel = useVisualizationStore((s) => s.viewLevel);`（1 个 selector）。
2. BuildingView3D.tsx:59 → selectedBuilding 与 navigateToMap 两个独立 selector（1 state + 1 action）。
3. FloorView3D.tsx:58-59 → 拆 4 个独立 selector：selectedFloor / selectedBuilding / navigateToBuilding / navigateToMap（RESEARCH Pitfall 5：不得合并为对象 selector）；selectedFloor 在 loadWorkstations 的 useCallback 依赖（:79 [selectedFloor]）中直接消费，保持引用语义不变。
4. HubeiMap.tsx:78 → clearSelection 与 navigateToBuilding 两个独立 selector（RESEARCH Code Examples 已给对照）。
5. HubeiMapGL.tsx:77 → 同 HubeiMap。

统一约束：action 引用直接 `s => s.action`，禁止 useMemo/useCallback 包裹（zustand 5.0.15 action 身份稳定，RESEARCH Don't Hand-Roll）；禁止对象 selector 与 useShallow；每个文件只动订阅行，渲染逻辑/事件处理（Phase 114 刚落地的 useMemo filter 与 effect cleanup）一行不动。
  </action>
  <verify>
    <automated>! grep -rqE "useVisualizationStore\(\)" xingran-react-frontend/src/pages/operations/building-spaces-3d/ && test "$(grep -rcE 'useVisualizationStore\(\(s\) =>' xingran-react-frontend/src/pages/operations/building-spaces-3d/index.tsx xingran-react-frontend/src/pages/operations/building-spaces-3d/components/BuildingView3D.tsx xingran-react-frontend/src/pages/operations/building-spaces-3d/components/FloorView3D.tsx xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMap.tsx xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMapGL.tsx | awk -F: '{s+=$NF} END {print s}')" -eq 11 && cd xingran-react-frontend && npx vitest run src/pages/operations/building-spaces-3d/ src/store/visualizationStore.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - behavior: 5 个组件消费的 visualizationStore 字段引用语义不变（selectedFloor 在 useCallback 依赖中仍正确触发 loadWorkstations 重建）；聚类/filter useMemo/effect cleanup 等 Phase 114 成果零回退
    - source-assertion: building-spaces-3d/ 目录内 `useVisualizationStore()` 无参调用 == 0
    - source-assertion: 5 文件 `useVisualizationStore((s) =>` 计数合计 == 11（1+2+4+2+2）
    - test-command: `cd xingran-react-frontend && npx vitest run src/pages/operations/building-spaces-3d/ src/store/visualizationStore.test.ts` 全绿（4 个相关测试全用真实 store，零 mock 改动）
    - cli: `cd xingran-react-frontend && npm run type-check` exit 0
  </acceptance_criteria>
  <done>
3D 页 5 处整店订阅全部为字段级 selector（共 11 个）；目录内无参调用归零；3D 簇测试全绿；type-check 通过；Phase 114 改造成果零回退。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端权限门控（RouteGuard） | UX 层路由门控——非安全边界（文件头注释明示，后端 API 独立校验）；本 plan 触及其数据来源形态，权限判断逻辑必须逐字保留 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-02-A | Elevation of Privilege | RouteGuard.tsx:41 权限判断 | mitigate | `permissions.some((p) => userPermissions.includes(p))` 逐字保留（verify 用 grep -qF 锁定该行）；文件头"这不是安全边界——后端 API 必须独立校验"注释语义不变；selector 化不改变 permissions 数据来源（同一数组引用） |
| T-115-02-B | Tampering（行为漂移） | DynamicRoutes.tsx 菜单加载时序 | mitigate | 只改订阅形态的硬纪律：fetchAll effect（131-137）/ routeConfigManager.initialize（151）/ getState/setState 静态方法（117-125）一行不动；routeGuard-lastpath.test.tsx + type-check 守护；Phase 118 DATA-02 前向兼容 |
| T-115-02-C | DoS（无限重渲） | 新增 selector | mitigate | 逐字段单值 selector（禁对象 selector/无 useShallow 字面量返回）；订阅 s.allMenus 数组引用而非 .length 派生 |
| T-115-SC | Tampering | 依赖安装 | mitigate（不适用） | 本 plan 零新增依赖（RESEARCH Package Legitimacy Audit：本 phase 不安装任何包），无 supply-chain 暴露，无 [ASSUMED]/[SUS] 包 |
</threat_model>

<verification>
- `grep -rnE "use(Menu|Auth|Visualization)Store\(\)" xingran-react-frontend/src/router/ xingran-react-frontend/src/pages/operations/building-spaces-3d/` 零命中
- RouteGuard.tsx 41 行权限判断逐字在位（grep -qF）
- `cd xingran-react-frontend && npx vitest run src/router/__tests__/routeGuard-lastpath.test.tsx src/pages/operations/building-spaces-3d/ src/store/visualizationStore.test.ts` 全绿
- `cd xingran-react-frontend && npm run lint && npm run type-check` 通过（D-02 七 gate 相关项）
- 判红前注意 RESEARCH Pitfall 3：冷缓存首跑假失败须隔离重跑确认
</verification>

<success_criteria>
1. RouteGuard 仅订阅 s.permissions，权限判断与 UX-only 注释逐字保留（SELECTOR-03 + V4 守护项）
2. DynamicRoutes 5 个独立 selector，effect 依赖与菜单加载时序零改动（SELECTOR-03，Phase 118 前向兼容）
3. 3D 页 5 处共 11 个字段级 selector，FloorView3D 拆 4 个独立调用（SELECTOR-04）
4. 路由层 + 3D 目录无参调用 grep 归零；既有测试全绿 + lint/type-check 零回归（D-04 口径）
</success_criteria>

<output>
Create `.planning/phases/115-selector-completion-zustand-selector/115-02-SUMMARY.md` when done
</output>
