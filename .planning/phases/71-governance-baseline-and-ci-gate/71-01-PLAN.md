---
phase: 71-governance-baseline-and-ci-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .coverage-threshold
  - .github/scripts/check-coverage.sh
  - .github/workflows/ci.yml
  - .planning/coverage-baseline.md
autonomous: true
requirements:
  - GOV-01
  - GOV-02
  - GOV-04

must_haves:
  truths:
    - "PR fails when weighted coverage < .coverage-threshold (12.8)"
    - "ci.yml backend job produces coverage.out + coverage.html artifacts retained 30 days"
    - ".planning/coverage-baseline.md has starting line + Phase 71 after line with per-package table"
    - "go test ./pkg/cache/... passes 15/15 (TestAsyncRetryWorker_Enqueue guard)"
    - "go test ./internal/... ./pkg/... ./cmd/... exits 0 (no failures)"
  artifacts:
    - path: ".coverage-threshold"
      provides: "single-value gate threshold (12.8) read by check-coverage.sh"
      contains: "12.8\\n"
    - path: ".github/scripts/check-coverage.sh"
      provides: "bash + awk weighted-average gate (exit 1 if avg < threshold)"
      exports: ["exit 0 (pass)", "exit 1 (gate fail)", "exit 2 (usage)"]
    - path: ".github/workflows/ci.yml"
      provides: "backend job with -coverprofile + Coverage HTML + Coverage gate + Upload artifact steps"
      contains: "actions/upload-artifact@v4"
    - path: ".planning/coverage-baseline.md"
      provides: "Phase 71 starting + post-phase snapshot table with per-package detail"
      contains: "## Phase 71 后"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: ".coverage-threshold"
      via: "bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold"
      pattern: "check-coverage.sh coverage.out .coverage-threshold"
    - from: ".github/scripts/check-coverage.sh"
      to: ".coverage-threshold"
      via: "THRESHOLD=$(cat .coverage-threshold)"
      pattern: "cat .coverage-threshold"
    - from: ".github/workflows/ci.yml"
      to: "actions/upload-artifact@v4"
      via: "Upload coverage artifact step + if: always()"
      pattern: "actions/upload-artifact@v4"
---

<objective>
Phase 71 ships the backend CI coverage gate so that test-coverage backsliding becomes impossible: `ci.yml` runs `go test -coverprofile=coverage.out -covermode=atomic -count=1`, a bash+awk script enforces the weighted-average threshold read from `.coverage-threshold`, and the per-phase actual numbers are snapshotted into `.planning/coverage-baseline.md`. The phase also re-verifies the previously fixed `pkg/cache` flaky test (`5ead742`) and the full-package test suite exit 0, satisfying SC#1-SC#5 without modifying any business logic.

Purpose: This is engineering infrastructure (D-01..D-04 locked, no business tests added). The gate initialises at 12.8% (the 2026-08-20 baseline from quick-260820-bcs); subsequent phases (72/73/74) ratchet it upward manually per D-04.

Output: 4 files (3 NEW + 1 MODIFY) — `.coverage-threshold`, `.github/scripts/check-coverage.sh`, `.planning/coverage-baseline.md`, and an extended `.github/workflows/ci.yml` — plus the push that turns on the gate for every PR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-governance-baseline-and-ci-gate/71-CONTEXT.md
@.planning/phases/71-governance-baseline-and-ci-gate/71-RESEARCH.md
@.planning/phases/71-governance-baseline-and-ci-gate/71-VALIDATION.md
@.planning/phases/71-governance-baseline-and-ci-gate/71-PATTERNS.md
@.github/workflows/ci.yml
@.github/workflows/deploy.yml
@scripts/check-status-literals.sh
@.planning/quick/260820-backend-test-coverage-scan/per-package-coverage.txt
@.planning/quick/260820-backend-test-coverage-scan/SUMMARY.md
</context>

<interfaces>
<!-- Key contracts/values the executor must use verbatim. -->

From `.planning/phases/71-governance-baseline-and-ci-gate/71-RESEARCH.md` (Awk Aggregation Formula Validation, lines 97-117):

The awk aggregator that `check-coverage.sh` must embed. It reads `coverage.out`, peels off the `file:line.col,line.col` prefix via `split($1, parts, ":")`, walks path segments via `split(parts[1], seg, "/")`, excludes paths starting with `scripts/`, `tests/scripts/`, `node_modules/`, aggregates per-package stmts and covered into associative arrays, prints per-package rows via `printf "%-50s %8d %8d %6.2f%%\n"`, and emits a trailing `PACKAGE %8d %8d %6.2f%%\n` summary line.

From `.planning/phases/71-governance-baseline-and-ci-gate/71-RESEARCH.md` (CI Workflow Changes, lines 159-189):

The four-step backend job extension pattern. Test step adds `-coverprofile=coverage.out -covermode=atomic -count=1` (no `-race` per D-01 Claude's Discretion); Coverage HTML uses `go tool cover -html=coverage.out -o coverage.html` with `if: always()`; Coverage gate invokes `bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold`; Upload uses `actions/upload-artifact@v4` with `retention-days: 30` and `if: always()`. Step order invariant: Test → Coverage HTML → Coverage gate → Upload.

From `.planning/phases/71-governance-baseline-and-ci-gate/71-RESEARCH.md` (Coverage-baseline.md Schema, lines 234-291):

`coverage-baseline.md` is a Markdown file with a snapshot table per phase (columns: date / phase_label / weighted_avg / total_stmts / total_covered / 0pct_pkg_count / commit / phase_executor / ratchet_from / ratchet_to) plus a per-package fenced block containing the 76-line table from `per-package-coverage.txt`.

From `scripts/check-status-literals.sh` lines 30-126:

The bash ratchet pattern `check-coverage.sh` must mirror: `#!/usr/bin/env bash` shebang, docstring header, `set -euo pipefail`, `ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"` path anchor, `collect`/`check`/`case "${1:-}"` structure, exit-code convention (`0` pass, `1` fail, `2` usage).

From `.planning/quick/260820-backend-test-coverage-scan/per-package-coverage.txt` (76 lines):

The exact per-package text to inline into `coverage-baseline.md` starting + Phase 71 后 sections. Lines 1-74 list per-package stmts/covered/percent; lines 75-76 are the trailing `PACKAGE` summary + dashes.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write .coverage-threshold file (12.8 baseline)</name>
  <files>.coverage-threshold</files>
  <read_first>
    - .planning/phases/71-governance-baseline-and-ci-gate/71-CONTEXT.md (D-01 + Claude's Discretion — pure number, single line, LF-terminated)
    - .planning/phases/71-governance-baseline-and-ci-gate/71-RESEARCH.md (Threshold File Format section, lines 205-225 — confirms 12.8 not 12.8%, no comments, no JSON/YAML, one decimal precision)
    - .planning/quick/260820-backend-test-coverage-scan/SUMMARY.md (line 18 — confirms 12.8% is the canonical baseline number)
  </read_first>
  <action>
    Create the file `.coverage-threshold` at the repo root (sibling of `go.mod`, `.gitignore`, `.env`). The file's sole content is the literal string `12.8` followed by a single LF newline byte. Do NOT include a trailing percent sign (12.8% is wrong), do NOT include any comment lines, do NOT wrap in JSON or YAML, do NOT include a UTF-8 BOM. The file must be readable by `cat .coverage-threshold | tr -d '\n'` producing exactly the string `12.8`. Place it with exactly the bytes: ASCII 1, 2, ., 8, newline — five bytes total.

    Rationale (do not encode in the file): per D-01, this file is the single source of truth for the gate threshold; future phase ratchets (Phase 72/73/74) only edit this file (never `check-coverage.sh` or `ci.yml`).
  </action>
  <acceptance_criteria>
    - `cat .coverage-threshold` outputs `12.8` (no trailing newline visible, byte 5 is LF confirmed via `wc -c .coverage-threshold` = 5)
    - `xxd .coverage-threshold` shows bytes `31 32 2e 38 0a` (ASCII 1, 2, ., 8, LF) — no CR, no BOM
    - `grep -c '.' .coverage-threshold` = 1 (single line, no comments)
    - `file .coverage-threshold` does NOT report "with BOM" or "UTF-8 Unicode (with BOM) text"
  </acceptance_criteria>
  <verify>
    <automated>test -f .coverage-threshold && cat .coverage-threshold && echo "---" && wc -c .coverage-threshold && echo "---" && xxd .coverage-threshold</automated>
  </verify>
  <done>
    File `.coverage-threshold` exists at the repo root, contains exactly five bytes (31 32 2e 38 0a), is readable as `12.8` by `cat`, and will be parsed cleanly by `$(cat .coverage-threshold)` inside `check-coverage.sh`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write .github/scripts/check-coverage.sh with awk aggregator + threshold compare</name>
  <files>.github/scripts/check-coverage.sh</files>
  <read_first>
    - .planning/phases/71-governance-baseline-and-ci-gate/71-PATTERNS.md (lines 38-148 — the structural pattern to mirror from check-status-literals.sh, plus the verbatim awk block from RESEARCH.md lines 97-117)
    - .planning/phases/71-governance-baseline-and-ci-gate/71-RESEARCH.md (Awk Aggregation Formula Validation section lines 92-148 — the canonical awk script that must be embedded in this file; Threshold File Format lines 205-225 — confirms THRESHOLD=$(cat .coverage-threshold) read pattern)
    - .planning/quick/260820-backend-test-coverage-scan/SUMMARY.md (lines 30-47 — original awk formula to preserve identical semantics)
    - scripts/check-status-literals.sh (lines 1-126 — shebang, docstring header, set -euo pipefail, ROOT anchor, collect/check/case structure, exit-code convention 0/1/2)
  </read_first>
  <action>
    Create `.github/scripts/check-coverage.sh` as an executable bash script. The script structure:

    1. Shebang `#!/usr/bin/env bash` + a docstring header (model on `check-status-literals.sh` lines 1-29) explaining Phase 71 purpose, D-01 origin (bash + awk, zero deps), exclusions (`scripts/`, `tests/scripts/`, `node_modules/`), threshold file location (`.coverage-threshold`), CI hookup (the `bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold` invocation from ci.yml), and exact exit-code semantics.

    2. `set -euo pipefail` (mirror line 30 of check-status-literals.sh).

    3. `ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"` — note the `../..` because the script lives under `.github/scripts/`, so `ROOT` must climb two directories to reach the repo root.

    4. Argument parsing: `PROFILE="${1:-}"`, `THRESHOLD_FILE="${2:-}"`. If either is empty, print usage to stderr and `exit 2`.

    5. Threshold read: `THRESHOLD="$(cat "$THRESHOLD_FILE")"` — fail-safe if file missing (the `set -e` will trip on `cat` of a missing file).

    6. Existence check: `if [ ! -f "$PROFILE" ]; then echo "coverage profile $PROFILE missing — was Test step skipped?" >&2; exit 0; fi` — gate stays pass when no profile exists, so HTML step / Upload step still run via `if: always()`.

    7. The awk aggregator embedded via heredoc: copy the formula verbatim from RESEARCH.md lines 97-117 — `NR > 1` parsing the file:line prefix via `split($1, parts, ":")` then `split(parts[1], seg, "/")` and joining `seg[4..n-1]` with `/`; excluding `scripts/`, `tests/scripts/`, `node_modules/`; aggregating per-package stmts and covered into two associative arrays; END block iterating all packages with `printf "%-50s %8d %8d %6.2f%%\n"` (matching per-package-coverage.txt column widths) and a trailing `printf "PACKAGE %8d %8d %6.2f%%\n"` line.

    8. Threshold comparison: invoke `awk` with `-v threshold="$THRESHOLD"` and append a post-END check that captures the PACKAGE line, parses the trailing `pct`, and prints `PASS` or `FAIL: weighted avg X.X% < threshold Y.Y%` to stderr; exit code `0` on PASS, `1` on FAIL. The check must coerce both sides to float with `+ 0` (avoids BWK awk integer division).

    9. On FAIL, also dump the per-package table to stdout (already produced by the aggregator above) so CI logs show which packages are dragging the weighted average.

    10. `chmod +x .github/scripts/check-coverage.sh` after writing.

    The script must be a single file under ~120 lines. No third-party dependencies. The 12.8% baseline must verify against `quick-260820-bcs` per-package-coverage.txt on the local coverage profile (covered in Task 3).
  </action>
  <acceptance_criteria>
    - `bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold` (with a valid coverage profile) prints per-package rows + final `PACKAGE <stmts> <covered> <pct>%` summary line + exits 0 when avg >= threshold
    - `bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold` exits 1 when weighted avg < threshold, and the FAIL message references both the actual avg and the threshold value
    - `bash .github/scripts/check-coverage.sh` with no args exits 2 (usage error)
    - `bash .github/scripts/check-coverage.sh /nonexistent .coverage-threshold` exits 0 (no profile = skip, don't fail — must coexist with `if: always()` on downstream steps)
    - Output per-package lines match the column widths of `.planning/quick/260820-backend-test-coverage-scan/per-package-coverage.txt` exactly (`%-50s %8d %8d %6.2f%%`)
    - File mode bits include execute: `ls -l .github/scripts/check-coverage.sh` shows `-rwxr-xr-x`
    - First line is `#!/usr/bin/env bash` and second line begins with `# check-coverage.sh`
  </acceptance_criteria>
  <verify>
    <automated>ls -l .github/scripts/check-coverage.sh && head -10 .github/scripts/check-coverage.sh && echo "--- syntax check ---" && bash -n .github/scripts/check-coverage.sh && echo OK</automated>
  </verify>
  <done>
    `.github/scripts/check-coverage.sh` exists, is executable, has a docstring header, uses `set -euo pipefail`, embeds the awk aggregator from RESEARCH.md, reads `.coverage-threshold` via `cat`, prints per-package rows in `per-package-coverage.txt` column format, and exits 0 (pass) / 1 (fail) / 2 (usage). Validated by the local smoke test in Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Local smoke test — generate coverage.out, verify exit 0/1 behaviour</name>
  <files>.coverage-threshold, .github/scripts/check-coverage.sh, /tmp/cov-phase71.out</files>
  <read_first>
    - .coverage-threshold (Task 1 output — current threshold value 12.8)
    - .github/scripts/check-coverage.sh (Task 2 output — script under test)
    - .planning/quick/260820-backend-test-coverage-scan/per-package-coverage.txt (reference 76-row table — smoke test result must match this within rounding tolerance)
    - .planning/quick/260820-backend-test-coverage-scan/SUMMARY.md (line 18 — 12.8% reference number)
    - .planning/phases/71-governance-baseline-and-ci-gate/71-RESEARCH.md (Gotchas & Risks section lines 365-432 — -count=1 must be present; -covermode=atomic required for concurrent tests; -race must NOT be present)
  </read_first>
  <action>
    Run three local validations on the executor's machine (Bash + Git Bash on Windows is acceptable; awk must be gawk or BWK with float semantics):

    1. Generate a fresh coverage profile — invoke `go test -timeout 15m -count=1 -coverprofile=/tmp/cov-phase71.out -covermode=atomic ./internal/... ./pkg/... ./cmd/...` from the repo root. The `-count=1` flag is mandatory (prevents test-result caching); `-covermode=atomic` is mandatory (correct for concurrent tests per Go docs); `-race` must NOT be present (would 5-10x test runtime per RESEARCH.md line 380-384). Capture exit code — must be 0 (no failures).

    2. Validate gate passes at the current threshold — invoke `bash .github/scripts/check-coverage.sh /tmp/cov-phase71.out .coverage-threshold`. Capture stdout (must contain per-package rows + final `PACKAGE <stmts> <covered> <pct>%` summary line) and exit code (must be 0). Verify the printed `PACKAGE` line's `<stmts>` is ~43652, `<covered>` is ~5589, `<pct>` is `12.80` or `12.81` (matches quick-260820-bcs baseline within rounding). If the numbers diverge from 43652 / 5589 / 12.80, log the divergence in the task summary but proceed (a small drift is acceptable — the exact reproduction is only meaningful if no business code changed since the 2026-08-20 scan).

    3. Validate gate fails when threshold is artificially raised — `cp .coverage-threshold .coverage-threshold.bak` then `printf '99.9\n' > .coverage-threshold`. Re-invoke `bash .github/scripts/check-coverage.sh /tmp/cov-phase71.out .coverage-threshold`. Exit code MUST be 1 (not 0). Stderr MUST contain a `FAIL:` message naming both the actual weighted avg and the threshold (`99.9`). Then restore: `mv .coverage-threshold.bak .coverage-threshold` and re-invoke once more to confirm exit 0 again. If the file restoration fails for any reason, leave the threshold at 12.8 (the correct value) and abort with a loud error message.

    These three local validations gate the CI extension in Task 4. If any fails, do NOT proceed to Task 4 — fix the script first.
  </action>
  <acceptance_criteria>
    - `go test -timeout 15m -count=1 -coverprofile=/tmp/cov-phase71.out -covermode=atomic ./internal/... ./pkg/... ./cmd/...` exits 0
    - `bash .github/scripts/check-coverage.sh /tmp/cov-phase71.out .coverage-threshold` exits 0, prints per-package rows matching `per-package-coverage.txt` column widths, final `PACKAGE` line shows ~43652 stmts and ~5589 covered
    - With `.coverage-threshold` temporarily set to `99.9`, the script exits 1 and stderr contains both the actual weighted avg and `99.9`
    - After restoring `.coverage-threshold` to `12.8`, re-invoking the script again exits 0 (proves restoration succeeded)
    - `/tmp/cov-phase71.out` exists and is non-empty (`ls -la /tmp/cov-phase71.out` shows size > 1MB; Go atomic coverage profile of 74 packages is ~3MB)
    - `.coverage-threshold` is exactly `12.8\n` after the test completes (verify with `cat` + `xxd`)
  </acceptance_criteria>
  <verify>
    <automated>go test -timeout 15m -count=1 -coverprofile=/tmp/cov-phase71.out -covermode=atomic ./internal/... ./pkg/... ./cmd/... && echo "--- gate pass test ---" && bash .github/scripts/check-coverage.sh /tmp/cov-phase71.out .coverage-threshold | tail -3 && echo "--- gate fail test ---" && cp .coverage-threshold .coverage-threshold.bak && printf '99.9\n' > .coverage-threshold && (bash .github/scripts/check-coverage.sh /tmp/cov-phase71.out .coverage-threshold; echo "exit=$?"); mv .coverage-threshold.bak .coverage-threshold && echo "--- restored ---" && cat .coverage-threshold && bash .github/scripts/check-coverage.sh /tmp/cov-phase71.out .coverage-threshold > /dev/null && echo "restored gate exit 0 OK"</automated>
  </verify>
  <done>
    Local smoke test green: gate passes at 12.8% with the existing weighted average, gate fails at 99.9%, and restoration leaves `.coverage-threshold` at exactly `12.8` with the gate still passing. The local coverage profile `/tmp/cov-phase71.out` exists for use as the basis for the Phase 71 后 snapshot in Task 7.
  </done>
</task>

<task type="auto">
  <name>Task 4: Extend .github/workflows/ci.yml backend job with coverage steps</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (current state — Test step at lines 50-57 to be modified; do not modify Lint step at lines 41-48, concurrency at lines 19-21, permissions at line 23, or frontend job at lines 59-108)
    - .planning/phases/71-governance-baseline-and-ci-gate/71-RESEARCH.md (CI Workflow Changes section lines 150-201 — exact step definitions + if: always() rationale + step-order invariant; Gotchas #9 lines 418-423 — coverage.out existence check before HTML step)
    - .github/workflows/deploy.yml (lines 138-152 — upload-artifact@v7 precedent in this repo; D-02 mandates @v4 not @v7 for coverage)
  </read_first>
  <action>
    Extend `.github/workflows/ci.yml` by replacing the existing `Test` step (lines 50-57) and appending three new steps. The complete diff to apply:

    1. Replace the current `Test` step (lines 50-57, which contains `run: go test -timeout 15m -count=1 ./internal/... ./pkg/... ./cmd/...`) with a multiline `run:` that adds `-coverprofile=coverage.out -covermode=atomic` while keeping `-timeout 15m -count=1` and the same package list. Add a comment line above it: `# Phase 71 GOV-01: -coverprofile + -covermode=atomic (count=1 already present)`.

    2. Immediately after the modified Test step, append a new step:
       - name: Coverage HTML report
       if: always()
       run: go tool cover -html=coverage.out -o coverage.html
       with a comment above explaining `if: always()` keeps this step running even when the gate fails.

    3. Append a new step:
       - name: Coverage gate
       run: bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold
       with a comment above: `# GOV-02: weighted-average gate (D-01)`. No `if:` field — this step's failure must propagate.

    4. Append the final new step:
       - name: Upload coverage artifact
       if: always()
       uses: actions/upload-artifact@v4
       with:
         name: backend-coverage
         path: |
           coverage.out
           coverage.html
         retention-days: 30
       with a comment above explaining `actions/upload-artifact@v4` + 30-day retention (D-02).

    DO NOT modify: the concurrency group (lines 19-21), the `permissions: contents: read` block (line 23), the Lint step (lines 41-48), or the entire frontend job (lines 59-108). DO NOT add `-race` to the Test command (D-01 Claude's Discretion forbids it — would 5-10x test runtime).

    Validate the resulting YAML with `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` and confirm 0 exit. The expected diff stat is exactly 4 step blocks: 1 modified (Test) + 3 added (HTML / gate / Upload).
  </action>
  <acceptance_criteria>
    - `git diff .github/workflows/ci.yml` shows 4 hunks: Test step flags change, Coverage HTML step addition, Coverage gate step addition, Upload artifact step addition
    - The new Test step `run:` value contains the substring `-coverprofile=coverage.out -covermode=atomic` (D-01 mandatory flags)
    - Coverage HTML step has `if: always()` (D-02 / Gotcha #9)
    - Coverage gate step invokes `bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold` (D-01 contract)
    - Upload step uses `actions/upload-artifact@v4` (D-02 mandates v4 not v7) with `retention-days: 30` and `if: always()`
    - YAML syntax valid: `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0
    - Lint step (lines 41-48) untouched: `git diff .github/workflows/ci.yml | grep -E "golangci/golangci-lint-action"` shows no change lines
    - Concurrency group untouched: `git diff .github/workflows/ci.yml | grep -E "concurrency|cancel-in-progress"` shows no change lines
    - Frontend job untouched: `git diff .github/workflows/ci.yml | grep -E "frontend:"` shows no change lines (the only frontend touch would be from a wrongly placed hunk)
  </acceptance_criteria>
  <verify>
    <automated>git diff .github/workflows/ci.yml | head -60 && echo "--- YAML validity ---" && python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo "OK yaml" && echo "--- upload-artifact version ---" && grep -E "actions/upload-artifact@v[0-9]+" .github/workflows/ci.yml && echo "--- coverage.out / coverage.html references ---" && grep -E "coverage\.(out|html)" .github/workflows/ci.yml</automated>
  </verify>
  <done>
    `.github/workflows/ci.yml` backend job contains exactly 4 changes: Test step adds `-coverprofile=coverage.out -covermode=atomic -count=1` flags; Coverage HTML step (`if: always()`); Coverage gate step (invokes `check-coverage.sh`); Upload artifact step (`actions/upload-artifact@v4`, `retention-days: 30`, `if: always()`). Lint step, concurrency, permissions, frontend job remain untouched. YAML is valid. Ready to push for CI verification in Task 8.
  </done>
</task>

<task type="auto">
  <name>Task 5: SC#4 verification — pkg/cache TestAsyncRetryWorker_Enqueue passes (5ead742 regression guard)</name>
  <files>pkg/cache/retry_test.go</files>
  <read_first>
    - pkg/cache/retry_test.go (the test file containing TestAsyncRetryWorker_Enqueue — verify it still asserts the post-fix behavior, not the original racy assertion)
    - .planning/STATE.md (line 144 — quick-260820-far entry: "竞态断言改非负 + 新增 TestAsyncRetryWorker_QueueSemantics"; commit `5ead742` is the flaky fix)
    - .planning/quick/260820-fix-async-retry-worker-flaky-test/SUMMARY.md (if exists — documents the exact fix)
  </read_first>
  <action>
    Run the SC#4 verification per Phase 71 SUCCESS CRITERIA #4 and the 71-VALIDATION.md Per-Task Verification Map row 71-01-08.

    Invoke `go test -v -count=1 ./pkg/cache/...` from the repo root. The `-v` flag surfaces individual test names so the executor can confirm `TestAsyncRetryWorker_Enqueue` runs and passes (not just "all 15 passed" — must explicitly see `--- PASS: TestAsyncRetryWorker_Enqueue` in the output). The `-count=1` flag prevents Go's test-result cache from masking a real failure if the test source hasn't changed since the last run.

    Capture the full output. Expected: 15 tests pass (including `TestAsyncRetryWorker_Enqueue` and `TestAsyncRetryWorker_QueueSemantics` per quick-260820-far). Exit code must be 0. If `TestAsyncRetryWorker_Enqueue` is missing or fails, abort with a loud error and reference the `5ead742` fix — this means the flaky test has regressed and must be re-fixed before Phase 71 can ship.

    Do NOT modify `retry_test.go` in this task. This is pure verification.
  </action>
  <acceptance_criteria>
    - `go test -v -count=1 ./pkg/cache/...` exits 0
    - Output contains `--- PASS: TestAsyncRetryWorker_Enqueue` (explicit, not just "ok" line)
    - Output contains `--- PASS: TestAsyncRetryWorker_QueueSemantics` (the post-fix non-racy test added in 5ead742)
    - Total PASS count is 15 (or matches the current file's test count if upstream added more)
    - Output contains `FAIL` zero times
    - `pkg/cache` line in output ends with `ok` (not `FAIL`)
  </acceptance_criteria>
  <verify>
    <automated>go test -v -count=1 ./pkg/cache/... 2>&1 | tail -25 && echo "--- AsyncRetryWorker test count ---" && go test -v -count=1 ./pkg/cache/... 2>&1 | grep -E "PASS:|FAIL:" | grep -c AsyncRetryWorker && echo "async tests listed"</automated>
  </verify>
  <done>
    SC#4 verified: `pkg/cache` test suite (15 tests including `TestAsyncRetryWorker_Enqueue` and `TestAsyncRetryWorker_QueueSemantics`) passes cleanly. The `5ead742` flaky test fix is intact. Phase 71 SC#4 is satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 6: SC#5 verification — full-package test suite exits 0 (no failures)</name>
  <read_first>
    - .github/workflows/ci.yml (line 57 — canonical Test step command; matches RESEARCH.md's "Full suite command")
    - .planning/quick/260820-backend-test-coverage-scan/SUMMARY.md (lines 32, 49 — confirms this is the standard full-package command; known pre-scan test failure was pkg/cache.TestAsyncRetryWorker_Enqueue already fixed in 5ead742)
  </read_first>
  <action>
    Run the SC#5 verification per Phase 71 SUCCESS CRITERIA #5 and the 71-VALIDATION.md Per-Task Verification Map row 71-01-09.

    Invoke `go test -timeout 15m -count=1 ./internal/... ./pkg/... ./cmd/...` from the repo root. The command matches the canonical ci.yml Test step (line 57) and the 71-RESEARCH.md Full suite command exactly. The `-timeout 15m` flag guards against the network/ports 3 timeout advisory from Phase 63 advisory backlog (real failures within timeout budget indicate actual regressions; timeouts indicate environment issues and should be flagged separately). The `-count=1` flag forces fresh runs (no cache).

    Capture exit code (must be 0) and the trailing output summary. If exit is non-zero, parse the output for `FAIL` markers and abort with the failure list — do not silently proceed. Known pre-Phase-71 failures per quick-260820-bcs scan were `pkg/cache.TestAsyncRetryWorker_Enqueue` (fixed in 5ead742, verified in Task 5) and potentially `login_encryption_test.go:235` + `pkg/errors/errors_test.go` + operations subtree (pre-existing from Phase 51-54 window per PROJECT.md line 171 — Phase 71 does not fix these; if they surface here, that is a regression and must be investigated before commit).

    Do NOT modify any test file in this task. This is pure verification.
  </action>
  <acceptance_criteria>
    - `go test -timeout 15m -count=1 ./internal/... ./pkg/... ./cmd/...` exits 0
    - Output contains `FAIL` zero times
    - All packages end with `ok` lines (no `--- FAIL` anywhere)
    - Total packages reported matches the package count from Phase 63 ci.yml baseline (74 business packages + helper sub-packages; count may have grown slightly since Phase 63)
    - No `panic:` lines in output
    - Wall time < 15 minutes (the -timeout 15m per-package ceiling — total wall time should be ~5-10 minutes based on Phase 63 historical data)
  </acceptance_criteria>
  <verify>
    <automated>go test -timeout 15m -count=1 ./internal/... ./pkg/... ./cmd/... 2>&1 | tee /tmp/full-test-phase71.log | tail -10 && echo "--- fail grep ---" && grep -c "^FAIL" /tmp/full-test-phase71.log && echo "--- panic grep ---" && grep -c "^panic" /tmp/full-test-phase71.log</automated>
  </verify>
  <done>
    SC#5 verified: full backend test suite (`./internal/... ./pkg/... ./cmd/...`) exits 0 with no failures, no panics, and within the 15-minute wall-clock budget. Phase 71 SC#5 is satisfied. Combined with Task 5 (SC#4), both flaky-test-related SC are green.
  </done>
</task>

<task type="auto">
  <name>Task 7: Write .planning/coverage-baseline.md with starting + Phase 71 after snapshot rows + per-package detail blocks</name>
  <files>.planning/coverage-baseline.md</files>
  <read_first>
    - .planning/phases/71-governance-baseline-and-ci-gate/71-RESEARCH.md (Coverage-baseline.md Schema section lines 234-291 — column schema + snapshot row format + per-package block format + regression checklist pattern)
    - .planning/phases/71-governance-baseline-and-ci-gate/71-PATTERNS.md (lines 215-266 — .coverage-baseline.md doc + Markdown tracking table pattern + per-package inline fenced block pattern)
    - .planning/quick/260820-backend-test-coverage-scan/per-package-coverage.txt (76 lines — the exact text to inline into both the starting + Phase 71 后 per-package blocks)
    - .planning/quick/260820-backend-test-coverage-scan/SUMMARY.md (line 18 — 12.8% baseline; line 19 — 33 zero-coverage packages)
    - /tmp/cov-phase71.out (Task 3 output — the fresh local profile; regenerate via `bash .github/scripts/check-coverage.sh /tmp/cov-phase71.out /tmp/.dummy-threshold > /tmp/phase71-per-pkg.txt` if the Phase 71 后 per-package block numbers diverge from the starting block; expect ~equal since Phase 71 makes no business changes)
  </read_first>
  <action>
    Create the file `.planning/coverage-baseline.md` at `.planning/coverage-baseline.md` (one directory above the existing quick scan SUMMARY.md, NOT inside it — per D-03 "新建独立文件，不污染 quick scan SUMMARY.md"). No frontmatter YAML block (unlike quick tasks, this file is a phase artifact, not a quick task report).

    The file structure:

    1. H1 header — `# Coverage Baseline: v1.26 后端测试覆盖率优秀`.

    2. Provenance block (3 lines, mirrors SUMMARY.md TL;DR):
       - `**起点来源:** \`.planning/quick/260820-backend-test-coverage-scan/SUMMARY.md\` (2026-08-20 纯只读扫描, 不可回填)`
       - `**测量口径:** 74 业务包加权平均, 排除 scripts/migrations/cmd main/internal/docs (43652 stmts 口径)`
       - `**生成方式:** \`bash .github/scripts/check-coverage.sh coverage.out\` (本地 + CI 同公式)`

    3. Horizontal rule, then `## 起点 (v1.26 启动前)` heading.

    4. Starting snapshot table with the column schema from RESEARCH.md lines 242-246: `date | phase_label | weighted_avg | total_stmts | total_covered | 0pct_pkg_count | commit | phase_executor | ratchet_from | ratchet_to`. Single row: `| 2026-08-20 | 起点 | 12.8 | 43652 | 5589 | 33 | 5ead742 | n/a | n/a | n/a |`.

    5. Per-package (起点) subsection: a Markdown heading `### Per-package (起点)` followed by a fenced code block containing the verbatim 76 lines from `.planning/quick/260820-backend-test-coverage-scan/per-package-coverage.txt`. Do NOT reformat columns; copy the lines byte-for-byte (column widths already match the awk output format per `printf "%-50s %8d %8d %6.2f%%\n"`).

    6. Horizontal rule, then `## Phase 71 后` heading.

    7. Phase 71 后 snapshot table with the same column schema. Single row: `| 2026-08-20 | Phase 71 后 | 12.8 | <new_total_stmts> | <new_total_covered> | 33 | TBD | gsd-execute-phase 71 | n/a | n/a |`. The `weighted_avg` should equal `12.8` (no business changes in Phase 71). `<new_total_stmts>` and `<new_total_covered>` come from running `bash .github/scripts/check-coverage.sh /tmp/cov-phase71.out /dev/null` (use /dev/null as a dummy threshold file; the script's cat will fail, but `THRESHOLD=12.8` if you point at `.coverage-threshold`); better: run with `/tmp/.dummy-threshold` containing `0` so the gate always passes and you get the full per-package table on stdout. Numbers will equal 43652 / 5589 within rounding if no business changes (which is the contract).

    8. Per-package (Phase 71 后) subsection: a Markdown heading `### Per-package (Phase 71 后)` followed by a fenced code block containing the per-package output from running `bash .github/scripts/check-coverage.sh /tmp/cov-phase71.out /tmp/.dummy-threshold` — pipe stdout into the file via `bash ... > /tmp/phase71-per-pkg.txt` then read the contents.

    9. Per-package 倒退检查 (本行) subsection: a Markdown checklist mirroring the pattern from RESEARCH.md lines 287-291:
       - `- [x] 无新增 0% 包 (起点 33 → Phase 71 后 33)`
       - `- [x] 无 per-package 倒退 (Phase 71 不改业务, 不改测试)`

    10. Ratchet note (per D-04) at the end: a Markdown blockquote explaining that this Phase 71 后 snapshot's `commit` field is `TBD` until Task 8 amends the commit; subsequent phases (72/73/74) will append new sections following the same schema.

    The `commit` field for Phase 71 后 is intentionally left as `TBD` because it must be the SHA of the commit Task 8 will create. Task 8 will replace `TBD` with the actual short SHA via Edit + amend.
  </action>
  <acceptance_criteria>
    - File `.planning/coverage-baseline.md` exists with size between 4KB and 12KB (76-line block × 2 ≈ 4.5KB + headers + tables ≈ 6KB total)
    - `cat .planning/coverage-baseline.md` contains the literal section heading `## 起点 (v1.26 启动前)` AND `## Phase 71 后`
    - Contains exactly two `### Per-package` subsections (起点 + Phase 71 后), each followed by a fenced code block
    - The starting per-package fenced block contains the line `pkg/normalize` at the top AND `cmd/agent` near the bottom AND the trailing `PACKAGE` summary line — verifiable via grep
    - The Phase 71 后 row in the snapshot table has `phase_label = Phase 71 后`, `commit = TBD` (will be amended in Task 8)
    - The `weighted_avg` value in the Phase 71 后 row equals `12.8` (since Phase 71 makes no business changes, the weighted average must not drift; any drift > 0.01 indicates a regression or measurement anomaly and must abort)
    - The 0% 包 count column in Phase 71 后 row equals `33` (same as 起点 — Phase 71 doesn't add new packages)
    - File does NOT contain a YAML frontmatter block (unlike quick tasks)
    - `quick/260820-backend-test-coverage-scan/SUMMARY.md` is UNCHANGED — `git diff .planning/quick/260820-backend-test-coverage-scan/SUMMARY.md` returns empty
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/coverage-baseline.md && wc -c .planning/coverage-baseline.md && grep -c "^## " .planning/coverage-baseline.md && echo "--- sections ---" && grep "^##" .planning/coverage-baseline.md && echo "--- quick scan untouched ---" && git diff --stat .planning/quick/260820-backend-test-coverage-scan/SUMMARY.md</automated>
  </verify>
  <done>
    `.planning/coverage-baseline.md` exists at `.planning/coverage-baseline.md` with the starting + Phase 71 后 snapshot tables, two 76-line per-package detail blocks (inline fenced), regression checklist, and ratchet note. The `commit` column in the Phase 71 后 row holds `TBD` pending Task 8 amend. `quick/260820-backend-test-coverage-scan/SUMMARY.md` is untouched. Phase 71 GOV-04 artefact is in place.
  </done>
</task>

<task type="auto">
  <name>Task 8: Commit + push + gh run watch (CI three green) + amend baseline.md commit SHA</name>
  <files>.coverage-threshold, .github/scripts/check-coverage.sh, .github/workflows/ci.yml, .planning/coverage-baseline.md</files>
  <read_first>
    - .planning/STATE.md (line 143 — commit `fdefdefd` is the latest; verify the working tree is otherwise clean before adding Phase 71 files)
    - .planning/notes/ (if any pre-existing notes — verify no conflicts with current changes)
    - .planning/phases/71-governance-baseline-and-ci-gate/71-CONTEXT.md (D-04 — manual ratchet, no CI auto-bump; commit message must indicate Phase 71 origin)
    - .github/workflows/ci.yml (Task 4 output — the file being pushed; verify diff is as expected before commit)
    - .planning/coverage-baseline.md (Task 7 output — the file being pushed)
    - Memory note `push-watch-ci.md` (per user memory — use `gh run watch` to monitor CI in background)
  </read_first>
  <action>
    Five steps in order:

    1. Replace `TBD` with the planned commit placeholder strategy — in `.planning/coverage-baseline.md` Phase 71 后 row, the `commit` field is `TBD`. Do NOT pre-fill it (the value is the SHA of the commit you're about to make, which doesn't exist yet). Instead, after the commit is made, run `git rev-parse --short HEAD` and use Edit to replace `TBD` with the short SHA. Then amend the commit OR push the amended version.

    2. Stage the 4 files — `git add .coverage-threshold .github/scripts/check-coverage.sh .github/workflows/ci.yml .planning/coverage-baseline.md`. Run `git status` to verify exactly these 4 files are staged (no stray workspace artifacts like `temp_*.go` or `test_*.go` per CLAUDE.md "Temporary Files Cleanup" — abort if found). If `M internal/services/system/asset_columns_schema.json` (the pre-existing Phase 68 leftover per STATE.md line 200) is dirty, do NOT touch it; just confirm it's not staged.

    3. Commit — `git commit -m "ci(71): backend coverage gate + baseline snapshot

    GOV-01: -coverprofile=coverage.out -covermode=atomic -count=1 in Test step
    GOV-02: bash + awk weighted-average gate reading .coverage-threshold (12.8)
    GOV-04: .planning/coverage-baseline.md with starting + Phase 71 后 snapshot

    Phase 71 ships 0 business changes. Gate fails PRs whose weighted avg
    drops below 12.8%; ratchet is manual via .coverage-threshold edits
    in Phase 72/73/74 execute plans (per D-04).

    Coverage artifact uploaded 30 days via actions/upload-artifact@v4
    for local debugging on gate failure."`. Verify the commit with `git log -1 --stat` showing all 4 files (3 NEW + 1 modified for ci.yml).

    4. Push — `git push origin main`. Capture the push output for the upstream branch ref. If push is rejected (e.g., branch protection), do NOT force-push; report the rejection.

    5. Watch CI — immediately after push, invoke `gh run watch --exit-status` in the background (per memory `push-watch-ci.md`). Capture the run ID and exit status. The CI must show three green jobs:
       - `backend` (golangci-lint pass + go test pass + Coverage gate pass)
       - `frontend` (lint + type-check + vitest + build pass)
       - the artifact `backend-coverage` is downloadable from the PR page

       If backend fails on the Coverage gate (which would be unexpected since 12.8 >= 12.8), abort and investigate. If frontend fails, that's pre-existing and out of Phase 71 scope — note but do not block Phase 71 ship. If backend fails on lint or test, those are pre-existing issues — note but the Coverage gate is what Phase 71 specifically tests.

       After CI is green, run `git rev-parse --short HEAD` and Edit `.planning/coverage-baseline.md` Phase 71 后 row to replace `TBD` with the short SHA. Then `git add .planning/coverage-baseline.md && git commit --amend --no-edit` (amend the same commit so the SHA on the snapshot row matches the SHA that produced the snapshot).

       Then `git push --force-with-lease origin main` (safer than `--force`) to push the amended commit.

    Do NOT mark Phase 71 complete until all three CI jobs are green AND the amended commit is pushed. If CI fails after the amend (because the amend changed `coverage.out`... wait, it doesn't — only `.planning/coverage-baseline.md` changed), restart the watch loop.
  </action>
  <acceptance_criteria>
    - `git status` after the final amend shows clean working tree
    - `git log -1 --stat` shows all 4 files in the latest commit: `.coverage-threshold` (new), `.github/scripts/check-coverage.sh` (new), `.github/workflows/ci.yml` (modified), `.planning/coverage-baseline.md` (new)
    - The commit message contains the strings `GOV-01`, `GOV-02`, `GOV-04`, `Phase 71`, `coverage gate`, `12.8` (verifying the intent statement)
    - `.planning/coverage-baseline.md` Phase 71 后 row's `commit` column is the actual short SHA of HEAD (not `TBD`), matching `git rev-parse --short HEAD`
    - `git push --force-with-lease origin main` succeeded (pushed commit is reachable from `origin/main`)
    - `gh run watch` for the run triggered by the push exited 0 (or the equivalent exit-status check passed)
    - `gh api repos/:owner/:repo/actions/runs/<run-id>/artifacts` lists `backend-coverage` artifact (or the artifact appears in the GitHub UI PR page)
    - The three GitHub Actions jobs all reported success: backend (lint + test + gate), frontend (lint + type-check + test + build)
    - `deploy.yml` did NOT trigger (because `workflow_run` gate on ci.yml success is intact — gate fail or ci.yml pass both still allow deploy, but if ci.yml was green, deploy would have triggered; absence of deploy run is acceptable since this is a documentation-only push to `main` per repo policy, but if deploy DID trigger and succeed, that's also fine)
  </acceptance_criteria>
  <verify>
    <automated>git status && echo "--- log ---" && git log -1 --stat && echo "--- coverage-baseline commit field ---" && grep -E "^\| 2026-08-20 \| Phase 71" .planning/coverage-baseline.md && echo "--- pushed ---" && git log --oneline -3 origin/main && echo "--- gh runs (latest 3) ---" && gh run list --limit 3 --json status,conclusion,name,headBranch 2>&1 | head -30</automated>
  </verify>
  <done>
    Phase 71 is shipped: 4 files committed (3 NEW + 1 modified), pushed to `main`, CI shows three green jobs (backend: lint + test + Coverage gate pass; frontend: lint + type-check + test + build pass), `backend-coverage` artifact is downloadable from the PR page, and `.planning/coverage-baseline.md` references the actual commit SHA in its Phase 71 后 row. The coverage gate is now live for every PR — backsliding below 12.8% is impossible without overriding the threshold file (which is itself a reviewable git diff per D-04).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR author → CI runner | Untrusted PR code runs `go test` and produces `coverage.out`; the gate decides pass/fail |
| Local executor → `.coverage-threshold` | The threshold file is edited by humans (D-04); integrity enforced by git history + PR review |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01 | Tampering | `.coverage-threshold` | mitigate | Single source of truth at repo root, all edits visible as PR diffs (D-04); manual ratchet prevents silent threshold changes; commit history makes every bump auditable |
| T-71-02 | Tampering | `check-coverage.sh` | mitigate | Script lives under `.github/scripts/` (only maintainers edit); awk formula is identical to quick-260820-bcs (verified 12.8%); local smoke test (Task 3) confirms exit 0/1 behaviour before push |
| T-71-03 | Information Disclosure | `backend-coverage` artifact (coverage.out + coverage.html) | accept | Coverage profile is non-sensitive (test instrumentation, no business secrets); 30-day retention is a CI cost decision not a privacy one |
| T-71-04 | Elevation of Privilege | Coverage gate fail → `if: always()` artifact upload | accept | Fail-state artifact upload is intentional for developer debugging; gate fail cannot bypass security checks (lint + test still gate deploy via workflow_run) |
| T-71-05 | Denial of Service | CI resource exhaustion via large coverage artifacts | mitigate | `actions/upload-artifact@v4` has a 1GB per-repo cap; estimated 150 artifacts × 8MB = 1.2GB approaching cap; RESEARCH §Gotchas #11 flags this for Phase 74+ re-evaluation |
| T-71-06 | Repudiation | Phase 71 threshold changes | mitigate | Every `.coverage-threshold` edit is a separate git commit; git log + commit message includes ratchet before/after numbers (per D-04 + 71-RESEARCH §Ratchet Workflow) |
| T-71-SC | Tampering | npm/pip/cargo installs | n/a | Phase 71 is engineering infrastructure, not package installation — no new dependencies introduced (D-01 zero-dep mandate) |

## Notes

- This threat model is intentionally thin because Phase 71 is engineering infrastructure (CI shell + workflow YAML), not business code. The "security boundary" is PR-author → CI-runner, and the existing `permissions: contents: read` block + step-level `actions/upload-artifact@v4` are the primary controls.
- No new network exposure introduced (gate runs entirely in-process on the CI runner).
- No new data exposure — coverage profile is generated from existing test code and business code that's already in the repo.
</threat_model>

<verification>
[Overall phase checks]

- [ ] `.coverage-threshold` exists at repo root, exactly 5 bytes (verified by `wc -c` and `xxd`)
- [ ] `.github/scripts/check-coverage.sh` exists, is executable (`-rwxr-xr-x`), bash syntax-clean (`bash -n`)
- [ ] Local smoke test (Task 3) green: gate passes at 12.8%, fails at 99.9%, restores cleanly
- [ ] `.github/workflows/ci.yml` YAML valid (`python -c "import yaml; yaml.safe_load(...)"`)
- [ ] `ci.yml` backend job has 4 changes in correct order: Lint → Test (with -coverprofile) → Coverage HTML → Coverage gate → Upload artifact
- [ ] `actions/upload-artifact@v4` literal appears with `retention-days: 30` and `if: always()`
- [ ] `go test ./pkg/cache/...` passes 15/15 including `TestAsyncRetryWorker_Enqueue` (SC#4)
- [ ] `go test ./internal/... ./pkg/... ./cmd/...` exits 0 (SC#5)
- [ ] `.planning/coverage-baseline.md` exists with two snapshot rows + two 76-row per-package detail blocks
- [ ] 4 files committed in a single commit with intent-stating message
- [ ] Pushed to `origin/main`
- [ ] CI shows three green jobs (backend + frontend + artifact present)
- [ ] No business code modified — `git diff <previous-HEAD>..HEAD -- '*.go'` returns only ci.yml + the 4 new files, no business logic touched
</verification>

<success_criteria>
[Measurable completion — derived from ROADMAP §Phase 71 §Success Criteria]

- [ ] **SC#1**: `ci.yml` backend job contains `-coverprofile=coverage.out -covermode=atomic -count=1` and produces `coverage.out` + `coverage.html` artifacts uploaded via `actions/upload-artifact@v4` with `retention-days: 30`. Verified by `git diff` + `gh run watch`.
- [ ] **SC#2**: CI introduces a weighted-average gate. Threshold = 12.8 (from `.coverage-threshold`). Below-threshold PR fails. Ratchet mechanism: future phases (72/73/74) edit `.coverage-threshold` + append a `.planning/coverage-baseline.md` row in the same atomic commit. Verified by local smoke test (Task 3: bump to 99.9 → exit 1) + CI run.
- [ ] **SC#3**: `.planning/coverage-baseline.md` contains both a starting row (from quick-260820-bcs, commit `5ead742`) and a Phase 71 后 row (from this push's local coverage profile), each followed by a 76-line per-package table inline. Verified by `cat` + grep.
- [ ] **SC#4**: `go test -v -count=1 ./pkg/cache/...` exits 0 with `--- PASS: TestAsyncRetryWorker_Enqueue` explicitly in output (5ead742 guard). Verified in Task 5.
- [ ] **SC#5**: `go test -timeout 15m -count=1 ./internal/... ./pkg/... ./cmd/...` exits 0 with no failures, no panics. Verified in Task 6.

**Requirements coverage:**
- GOV-01 (CI -coverprofile + profile artifact): Task 4 + Task 8 CI verification
- GOV-02 (CI threshold gate + ratchet): Tasks 1 + 2 + 3 + 4 + 8
- GOV-04 (Baseline snapshot): Task 7 + Task 8 amend

**Decisions honored:**
- D-01 (bash + awk + `.coverage-threshold`): Tasks 1 + 2
- D-02 (actions/upload-artifact@v4 + 30 days): Task 4
- D-03 (`.planning/coverage-baseline.md` separate from quick scan): Task 7
- D-04 (manual ratchet): plan-level (Phase 72/73/74 execute plans will bump)

All 5 SC, 3 requirements, 4 D-locked decisions covered. Phase 71 is shippable when all 8 tasks complete.
</success_criteria>

<output>
Create `.planning/phases/71-governance-baseline-and-ci-gate/71-01-SUMMARY.md` when done
</output>