---
phase: 74-p2-finalize-and-diff-coverage
plan: 11
type: execute
wave: 5
depends_on:
  - 74-01
  - 74-02
  - 74-03
  - 74-04
  - 74-05
  - 74-06
  - 74-07
  - 74-08
  - 74-09
  - 74-10
files_modified:
  - .coverage-threshold
  - .planning/coverage-baseline.md
  - .github/scripts/check-coverage.sh
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/74-p2-finalize-and-diff-coverage/74-11-SUMMARY.md
  - .planning/phases/74-p2-finalize-and-diff-coverage/74-MILESTONE-AUDIT.md
autonomous: true
requirements:
  - GOV-03
  - SCALE-01
  - SCALE-02
  - SCALE-03
user_setup: []

must_haves:
  truths:
    - "Phase 74 weighted avg ≥70% (from 25.9% in Phase 73) per SCALE-03"
    - "Phase 74 0%-pkg count ≤5 (from 22 in Phase 73) per SC-b"
    - "All 10 P2 big-block packages ≥70% per D-15 (api/v1/{operations,asset,network} + services/{rpa,vdi} + internal/{core,device,utils} + agent/server + scheduler)"
    - "All 8 P1 packages still ≥70% per D-04/D-10 strict (no regression)"
    - "check-coverage.sh extended with section 4 p2_package_check (10 P2 packages ≥70% floor, exit 5)"
    - "GOV-03 PR diff coverage ≥80% gate active in CI per D-14"
    - "milestone SC-a/b/c/d/e all 5 achieved and documented in audit report"
  artifacts:
    - path: ".coverage-threshold"
      provides: "New threshold ≥70% (ratcheted from 25.9)"
      contains: "70"
    - path: ".planning/coverage-baseline.md"
      provides: "Phase 74 row appended with weighted_avg + 10 P2 packages per-package % table + 8 P1 packages preserved + 0%-pkg count"
      contains: "Phase 74 后"
    - path: ".github/scripts/check-coverage.sh"
      provides: "Extended coverage gate with section 4 p2_package_check (10 P2 packages ≥70% floor, exit 5)"
      contains: "p2_package_check"
    - path: ".planning/STATE.md"
      provides: "Updated to mark Phase 74 SHIPPED with milestone SC status"
      contains: "Phase 74 SHIPPED"
    - path: ".planning/ROADMAP.md"
      provides: "Updated to mark Phase 74 SHIPPED + v1.26 milestone complete"
      contains: "v1.26 SHIPPED"
    - path: ".planning/phases/74-p2-finalize-and-diff-coverage/74-11-SUMMARY.md"
      provides: "Final phase summary with full coverage delta + per-package table + commit SHA + milestone SC status"
      contains: "coverage ratchet"
    - path: ".planning/phases/74-p2-finalize-and-diff-coverage/74-MILESTONE-AUDIT.md"
      provides: "Milestone v1.26 audit report verifying SC-a/b/c/d/e all 5 achieved"
      contains: "SC-a"
  key_links:
    - from: ".github/scripts/check-coverage.sh"
      to: "coverage.out"
      via: "awk parses atomic profile + iterates P1+P2 package lists"
      pattern: "awk.*coverage\\.out"
    - from: ".planning/coverage-baseline.md"
      to: ".coverage-threshold"
      via: "ratchet note documents atomic commit bumping both files together per D-04"
      pattern: "atomic ratchet"
    - from: ".github/workflows/ci.yml"
      to: ".github/scripts/check-coverage.sh"
      via: "Coverage gate step runs the bash script"
      pattern: "bash \\.github/scripts/check-coverage\\.sh"
---

<objective>
Phase 74 final ratchet + milestone audit. Mirrors Phase 73-05 / Phase 72-13 ratchet structure:

1. Capture actual coverage (Task 1)
2. Extend check-coverage.sh with section 4 p2_package_check (10 P2 packages ≥70% floor, exit 5) (Task 2)
3. Atomic ratchet commit with 6 files + milestone audit report (Tasks 3 + 4)
4. CI verification + final SUMMARY closeout (Task 5)

This is the closing plan for v1.26 milestone. After Task 5, v1.26 is SHIPPED and milestone SC-a/b/c/d/e are all 5 verified.

Purpose: Without this final plan, Phase 74 work has no enforcement. The gate must check 8 P1 + 10 P2 + weighted avg + diff coverage - all 4 enforcement layers active simultaneously.

Output: 1 atomic commit with 6 files (per D-07 + D-11) + 1 milestone audit report (separate file).
</objective>

<execution_context>
execute@$HOME/.claude/get-shit-done/workflows/execute-plan.md
execute@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
context@.planning/phases/74-p2-finalize-and-diff-coverage/74-CONTEXT.md
context@.planning/phases/73-p1-pending/73-05-PLAN.md (Phase 73 ratchet structure mirror)
context@.planning/phases/72-p0-core-supplement/72-13-PLAN.md (Phase 72 ratchet structure)
context@.planning/phases/74-p2-finalize-and-diff-coverage/74-{01..10}-SUMMARY.md (per-plan coverage data)
context@.planning/REQUIREMENTS.md (milestone SC + 4 requirements)
context@.planning/coverage-baseline.md (Phase 73 row - current 25.9%)

# Source of truth
Read these files first:
- .github/scripts/check-coverage.sh (current Phase 71+73 version - extend it for P2 floor)
- .coverage-threshold (current 25.9)
- .planning/coverage-baseline.md (Phase 73 row schema)
- .planning/phases/74-p2-finalize-and-diff-coverage/74-{01..10}-SUMMARY.md (per-plan coverage)
- .planning/STATE.md (current Phase 73 SHIPPED state to update)
- .planning/ROADMAP.md (Phase 74 entry to mark SHIPPED + v1.26 milestone complete)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture actual coverage measurement (pre-ratchet)</name>
  <files>.planning/phases/74-p2-finalize-and-diff-coverage/74-11-SUMMARY.md</files>
  <read_first>.planning/coverage-baseline.md (current state - Phase 73 row), .planning/phases/74-p2-finalize-and-diff-coverage/74-{01..10}-SUMMARY.md (per-plan coverage numbers)</read_first>
  <action>Run full backend test coverage and capture per-package %:

Commands:
- cd /path/to/repo && go test -timeout 15m -count=1 -coverprofile=coverage.out ./internal/... ./pkg/... ./cmd/... 2>&1 | tail -5
- bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold 2>&1 | tail -20
- go tool cover -func=coverage.out > cover-func.txt

Capture via awk:
- Weighted average: bash check-coverage.sh output (current 25.9% should be ≥70% after Phase 74 work)
- Total stmts: awk '/^total:/ {print $2}' cover-func.txt
- Total covered: awk '/^total:/ {print $3}' cover-func.txt
- 0%-pkg count: grep "0.0%" per-package output of check-coverage.sh

Verify ALL 10 P2 targets individually pass ≥70%:
- SCALE-01 api/v1/operations (1285)
- SCALE-01 api/v1/asset (420)
- SCALE-01 api/v1/network (1971)
- SCALE-01 services/rpa (1865)
- SCALE-01 services/vdi (1127)
- SCALE-01 internal/core (754)
- SCALE-01 internal/device (1249)
- SCALE-01 internal/utils (531)
- SCALE-01 internal/agent/server (616)
- SCALE-01 services/scheduler (167)

Verify ALL 8 P1 targets still ≥70% (D-04 strict, no regression):
- api/v1/duty 83.0%, api/v1/knowledge 84.2%, api/v1/rpa 79.2%, api/v1/vdi 76.2%
- services/duty 95.6%, services/knowledge 95.3%, services/network 92.1%, services/monitor 95.3%

If any target <70%, ESCALATE — do NOT proceed with ratchet. Identify which plan(s) underdelivered, add gap-closure plan 74-12 ad-hoc if needed.
If weighted avg <70%, similar escalation.
If 0%-pkg count >5, identify remaining 0%-pkg, escalate.

Save raw output to a temp file for use in Task 3.
  </action>
  <verify>bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold 2>&1 | tail -20</verify>
  <acceptance_criteria>All 10 P2 packages ≥70%, all 8 P1 packages ≥70% (D-04/D-10 strict), weighted avg ≥70%, 0%-pkg count ≤5. Raw measurement saved to temp file.</acceptance_criteria>
  <done>Pre-ratchet coverage measured; all targets met; ready for ratchet commit.</done>
</task>

<task type="auto">
  <name>Task 2: Extend check-coverage.sh with section 4 p2_package_check (10 P2 packages ≥70% floor, exit 5)</name>
  <files>.github/scripts/check-coverage.sh</files>
  <read_first>.github/scripts/check-coverage.sh (current Phase 73 version - extend it for P2 floor), .planning/phases/74-p2-finalize-and-diff-coverage/74-CONTEXT.md (D-15 P2 packages list)</read_first>
  <action>Extend .github/scripts/check-coverage.sh to validate a 4th layer: P2 per-package ≥70% floor (10 packages).

Add section 4 after section 3 p1_package_check. The implementation mirrors section 3 exactly (same awk logic, same floor comparison, same PASS/FAIL output format) — only the PACKAGES list and exit code differ.

P2_PACKAGES (10 entries, per D-15):
- internal/api/v1/operations
- internal/api/v1/asset
- internal/api/v1/network
- internal/services/rpa
- internal/services/vdi
- internal/core
- internal/device
- internal/utils
- internal/agent/server
- internal/services/scheduler

P2_FLOOR="70.0" (same as P1)

Exit code 5 for P2 package failures (distinct from exit 1 weighted avg, exit 4 P1 floor).

Preserve backward compatibility:
- Sections 1-2 weighted-avg gate (unchanged)
- Section 3 P1 floor (unchanged - 8 P1 packages continue ≥70% per D-04/D-10 strict)
- New section 4 P2 floor (additive, does not affect existing PASS/FAIL semantics)
  </action>
  <verify>bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold 2>&1 | tail -30</verify>
  <acceptance_criteria>check-coverage.sh extended with section 4 p2_package_check (10 P2 packages ≥70% floor, exit 5). All 4 layers (weighted-avg + P1 floor + P2 floor + P1 strict from D-04) coexist. Existing sections 1-3 unchanged (backward compat). Script remains bash + awk only (D-06).</acceptance_criteria>
  <done>check-coverage.sh extended for P2 floor; all 4 enforcement layers active.</done>
</task>

<task type="auto">
  <name>Task 3: Atomic ratchet commit (6 files) + milestone audit report</name>
  <files>
.coverage-threshold
.planning/coverage-baseline.md
.github/scripts/check-coverage.sh
.planning/STATE.md
.planning/ROADMAP.md
.planning/phases/74-p2-finalize-and-diff-coverage/74-11-SUMMARY.md
.planning/phases/74-p2-finalize-and-diff-coverage/74-MILESTONE-AUDIT.md
  </files>
  <read_first>.coverage-threshold (current 25.9), .planning/coverage-baseline.md (Phase 73 row schema), .planning/phases/74-p2-finalize-and-diff-coverage/74-{01..10}-SUMMARY.md (per-plan data), .planning/STATE.md (current state to update), .planning/ROADMAP.md (Phase 74 entry to mark SHIPPED + v1.26 milestone complete)</read_first>
  <action>Atomic ratchet per D-07 — single commit modifying 6 files. Per D-05 + D-11, STATE.md + ROADMAP.md updates go in THIS ratchet commit (not a follow-up). Add 74-MILESTONE-AUDIT.md as 7th file (separate from the 6 ratchet files - it's a report, not part of gate enforcement).

Concrete commands (no placeholders):

1. .coverage-threshold: write new value from Task 1 measurement (≥70%, e.g. 70.5)

2. .planning/coverage-baseline.md: append Phase 74 后 section:
   - weighted_avg: bash check-coverage.sh output (≥70%)
   - total_stmts: 43652 (unchanged)
   - total_covered: awk '/^total:/ {print $3}' cover-func.txt
   - 0pct_pkg_count: ≤5
   - commit: git rev-parse HEAD (after this ratchet commit)
   - 10 P2 packages per-package % table (one row per package)
   - 8 P1 packages per-package % table (preserved, no regression)
   - Per-package 倒退检查 checklist (zero 0%-pkg addition, all targets met)

3. .github/scripts/check-coverage.sh: extended per Task 2 (already staged for this commit)

4. .planning/STATE.md: update progress table row for Phase 74 to SHIPPED with commit SHA + total_plans: 32 (4 phases × avg 8 plans) + completed_plans: 32 + percent: 100 + status: "v1.26 milestone SHIPPED"

5. .planning/ROADMAP.md: update Phase 74 entry status from Pending to SHIPPED with commit SHA + date; update v1.26 milestone status from IN PROGRESS to SHIPPED; update Progress table (Phase 74 SHIPPED); update last_updated

6. .planning/phases/74-p2-finalize-and-diff-coverage/74-11-SUMMARY.md: write phase-end summary with:
   - Coverage delta (25.9% → NEW_VALUE% ≥70%)
   - Per-SCALE target achieved status (SCALE-01/02/03 all ✓)
   - 10 P2 packages per-package % table (all ≥70%)
   - 8 P1 packages per-package % table (no regression per D-04)
   - 0%-pkg count delta (22 → ≤5)
   - Commit SHA (this ratchet commit)
   - Phase executor model
   - Test count total across all 11 plans (74-01..10)
   - Files modified total

7. .planning/phases/74-p2-finalize-and-diff-coverage/74-MILESTONE-AUDIT.md: milestone v1.26 audit report with:
   - SC-a weighted avg ≥70%: ✓ with actual value
   - SC-b 0%-pkg count ≤5: ✓ with actual count
   - SC-c CI coverage threshold gate active: ✓ (.coverage-threshold bumped to ≥70%)
   - SC-d full go test zero failures: ✓ (no regression)
   - SC-e PR diff coverage ≥80% gate: ✓ (per Plan 74-10 D-14)
   - Per-requirement traceability: GOV-03 ✓ + SCALE-01 ✓ + SCALE-02 ✓ + SCALE-03 ✓
   - Per-phase SUMMARY link table
   - Final commit SHA list

After writing all 6+1 files:
- git add .coverage-threshold .planning/coverage-baseline.md .github/scripts/check-coverage.sh .planning/STATE.md .planning/ROADMAP.md .planning/phases/74-p2-finalize-and-diff-coverage/74-11-SUMMARY.md .planning/phases/74-p2-finalize-and-diff-coverage/74-MILESTONE-AUDIT.md
- git commit -m "docs(74): coverage ratchet 25.9% → $(awk '/weighted avg/ {print $NF}' coverage.out)%

- v1.26 milestone SHIPPED (all 5 SC met: SC-a/b/c/d/e)
- 10 P2 packages ≥70% per D-15 + 8 P1 packages preserved per D-04/D-10
- check-coverage.sh extended with section 4 p2_package_check (exit 5)
- GOV-03 PR diff coverage ≥80% gate active per D-14
- 11 plans executed (74-01..11), 0%-pkg count 22→≤5

Co-Authored-By: Claude <noreply@anthropic.com>"

If git push fails because branch is protected (Phase 71 had this issue — see cfec2c4), adapt: create a new commit instead of amend. Document in SUMMARY.

CRITICAL: All 6 file modifications MUST be in a single commit (D-07 atomic ratchet). The 7th file (MILESTONE-AUDIT.md) is part of the same commit (it's a report, but linked to the ratchet commit's content).

Push deferred to orchestrator/user per Phase 73-05 deviation pattern.
  </action>
  <verify>git log --oneline -3 && cat .coverage-threshold && grep -c "Phase 74 后" .planning/coverage-baseline.md</verify>
  <acceptance_criteria>Single commit contains all 6+1 files: .coverage-threshold + coverage-baseline.md + check-coverage.sh + STATE.md + ROADMAP.md + 74-11-SUMMARY.md + 74-MILESTONE-AUDIT.md. .coverage-threshold value ≥70% (ratchet UP only). coverage-baseline.md has Phase 74 后 section with full per-package + 10 P2 + 8 P1 tables. STATE.md + ROADMAP.md marked Phase 74 SHIPPED + v1.26 milestone SHIPPED. 74-MILESTONE-AUDIT.md has all 5 SC verified. Commit message starts with docs(74): coverage ratchet. git log --oneline -1 shows ratchet commit.</acceptance_criteria>
  <done>Phase 74 atomic ratchet committed; coverage-baseline.md updated; CI gate extended with section 4; STATE.md + ROADMAP.md marked SHIPPED; milestone audit report written.</done>
</task>

<task type="auto">
  <name>Task 4: CI verification + final SUMMARY closeout</name>
  <files>.planning/phases/74-p2-finalize-and-diff-coverage/74-11-SUMMARY.md</files>
  <read_first>.github/workflows/ci.yml (Coverage gate step + coverage-diff job from Plan 74-10), .planning/phases/74-p2-finalize-and-diff-coverage/74-11-SUMMARY.md (current draft)</read_first>
  <action>Per D-11: STATE.md and ROADMAP.md updates were already shipped in the ratchet commit (Task 3) so all docs land together per D-07. This task focuses on CI verification + SUMMARY closeout.

Verify CI gate will pass with new ratchet value:

Commands:
- git push origin main 2>&1 | tail -5
- gh run watch --repo OWNER/REPO --exit-status 2>&1 | tail -20

Or local validation only:
- go test -timeout 15m -count=1 -coverprofile=c.out ./internal/... ./pkg/... ./cmd/...
- bash .github/scripts/check-coverage.sh c.out .coverage-threshold

Both should:
- Exit 0 on weighted-avg (≥70%)
- Exit 0 on P1 floor (8/8 packages ≥70%)
- Exit 0 on P2 floor (10/10 packages ≥70%)
- Exit 0 on PR diff coverage (if test PR created)

If CI fails:
- Diagnose the failing step
- Fix the underlying issue
- Re-push

If CI passes:
- Capture CI run URL for SUMMARY
- Finalize 74-11-SUMMARY.md with:
  - Coverage delta (25.9% → NEW_VALUE%)
  - Per-SCALE target status table (SCALE-01..03 ✓)
  - 10 P2 packages per-package % table
  - 8 P1 packages per-package % table (no regression)
  - 0%-pkg count delta (22 → ≤5)
  - CI run URL
  - Final commit SHA (ratchet commit)
  - Files modified total (across all 11 plans)
  - Total test count
  - Milestone SC status (5/5 ✓)
  - Per-requirement traceability (GOV-03 + SCALE-01/02/03 ✓)

No further commits needed — STATE.md + ROADMAP.md already SHIPPED in ratchet commit (Task 3) per D-11.
  </action>
  <verify>bash .github/scripts/check-coverage.sh c.out .coverage-threshold 2>&1 | tail -10</verify>
  <acceptance_criteria>check-coverage.sh exits 0 (all 4 layers pass: weighted-avg + P1 + P2 + diff). Output shows PASS for all 3 layer checks. 74-11-SUMMARY.md finalized with all sections. All commits pushed to origin/main (or documented why deferred). Milestone audit report links all 5 SC to evidence.</acceptance_criteria>
  <done>Phase 74 complete; v1.26 milestone SHIPPED; all 5 SC verified; CI green with new ratchet + diff coverage; all documentation shipped in atomic ratchet commit (Task 3).</done>
</task>

</tasks>

<verification>
- bash .github/scripts/check-coverage.sh c.out .coverage-threshold exits 0
- All 4 enforcement layers PASS: weighted-avg (≥70%) + P1 floor (8/8) + P2 floor (10/10) + diff coverage (≥80% per D-14)
- .coverage-threshold value ≥70% (ratchet UP from 25.9)
- .planning/coverage-baseline.md has new Phase 74 后 row
- All 10 P2 targets ≥70% per D-15 (SCALE-01 met)
- All 8 P1 packages ≥70% per D-04/D-10 strict (no regression)
- 0%-pkg count ≤5 (SC-b met)
- All 5 milestone SC (SC-a/b/c/d/e) verified in audit report
- Atomic ratchet commit with 6+1 files (D-07 + D-11): .coverage-threshold + coverage-baseline.md + check-coverage.sh + STATE.md + ROADMAP.md + 74-11-SUMMARY.md + 74-MILESTONE-AUDIT.md
- GOV-03 PR diff coverage ≥80% gate active in CI per Plan 74-10 + D-14
</verification>

<success_criteria>
- [ ] Phase 74 SC#1: PR diff coverage ≥80% gate active in CI (GOV-03 met via Plan 74-10)
- [ ] Phase 74 SC#2: api/v1/{operations, asset, network} ≥70% each (1285 / 420 / 1971 stmts)
- [ ] Phase 74 SC#3: services/{rpa, vdi} ≥70% each (1865 / 1127 stmts)
- [ ] Phase 74 SC#4: internal/{core, device, utils} + agent/server + scheduler ≥70% (D-15 floor)
- [ ] Phase 74 SC#5: pkg/{response, query, time, logger, captcha} + internal/pkg/* ≥70% (SCALE-02)
- [ ] Phase 74 SC#6: Weighted avg ≥70% (25.9% → ≥70%, SCALE-03 / SC-a)
- [ ] Phase 74 SC#7: 0%-pkg count ≤5 (22 → ≤5, SC-b)
- [ ] Phase 74 SC#8: All 5 milestone SC (SC-a/b/c/d/e) verified in audit report
- [ ] All 10 P2 packages ≥70% per D-15
- [ ] All 8 P1 packages ≥70% per D-04/D-10 strict (no regression)
- [ ] check-coverage.sh extended with section 4 p2_package_check (10 P2 packages, exit 5)
- [ ] Atomic commit with 6+1 files (D-07 + D-11)
- [ ] Prior Task 0 batch commit collects all per-module test files (resolves W2 ambiguity)
- [ ] 74-11-SUMMARY.md + 74-MILESTONE-AUDIT.md complete
- [ ] v1.26 milestone SHIPPED status recorded in STATE.md + ROADMAP.md
</success_criteria>

<output>
Create .planning/phases/74-p2-finalize-and-diff-coverage/74-11-SUMMARY.md (final phase summary) and .planning/phases/74-p2-finalize-and-diff-coverage/74-MILESTONE-AUDIT.md (milestone audit report) when done.
</output>