---
phase: 74-p2-finalize-and-diff-coverage
plan: 07
type: execute
wave: 2
depends_on: []
files_modified:
  - internal/services/operations/*_test.go
  - internal/services/system/*_test.go
  - internal/services/asset/*_test.go
autonomous: true
requirements:
  - SCALE-01
user_setup: []

must_haves:
  truths:
    - "internal/services/operations per-package ≥60% (3714 stmts, was 22.5%)"
    - "internal/services/system per-package ≥75% (3483 stmts, was 53.5%)"
    - "internal/services/asset per-package ≥70% (1354 stmts, was 40.5%)"
    - "All *_test.go files are test-only (D-12)"
  artifacts:
    - path: "internal/services/operations/*_service_test.go"
      provides: "Operations sub-service tests (building, floor, workstation, excel, geocoding, etc.)"
      min_lines: 1000
    - path: "internal/services/system/*_cache_impl_test.go (already exists) + new *_service_test.go"
      provides: "System sub-service incremental tests (user, role, dept, etc.)"
      min_lines: 800
    - path: "internal/services/asset/*_service_test.go"
      provides: "Asset sub-service tests"
      min_lines: 400
  key_links:
    - from: "internal/services/operations/building_service_test.go"
      to: "internal/services/operations/building_service.go"
      via: "real DB via glebarez sqlite + mock geocoding/excel deps"
      pattern: "gorm.Open.*sqlite"
---

<objective>
Lift the 3 largest medium-coverage service packages: services/operations (22.5%→60%), services/system (53.5%→75%), services/asset (40.5%→70%). These are NOT in the strict D-15 P2 per-package floor list, but lifting them is essential for SCALE-03 weighted average ≥70% goal.

Purpose: Combined these contribute ~+2541 covered stmts (operations +1393 + system +749 + asset +399). Without this lift, the weighted average cannot reach 70%.

Output: ~15-20 *_service_test.go files across 3 packages; per-file ≥75%; D-12 honored.
</objective>

<execution_context>
execute@$HOME/.claude/get-shit-done/workflows/execute-plan.md
execute@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
context@.planning/phases/74-p2-finalize-and-diff-coverage/74-CONTEXT.md
context@.planning/phases/73-p1-pending/73-03-SUMMARY.md (service cache_impl pattern)
context@.planning/phases/73-p1-pending/73-04-SUMMARY.md (mixed DB + mock)
context@.planning/REQUIREMENTS.md (SCALE-01)
context@.planning/coverage-baseline.md (Phase 73 row - ops 22.5%, sys 53.5%, asset 40.5%)

# Source of truth
Read these files first:
- internal/services/operations/ (large package - identify sub-services)
- internal/services/system/ (Phase 72 has cache_impl_test.go files already - identify gaps)
- internal/services/asset/ (asset sub-services)
- .planning/phases/72-p0-core-supplement/72-04..12-PLAN.md (system sub-module test plan templates)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate gap sub-services + design batch plan</name>
  <files>internal/services/{operations,system,asset}/*_service_test.go (15+ new)</files>
  <read_first>internal/services/operations/ (list all), internal/services/system/ (list all), internal/services/asset/ (list all), .planning/quick/260820-backend-test-coverage-scan/per-package-coverage.txt (current per-file)</read_first>
  <action>1. Per-file coverage scan each of the 3 packages to identify 0%/low-coverage files. 2. Plan in 2 batches: batch 1 (highest stmts) + batch 2 (remaining). 3. Mock strategy: Phase 73-03 portwrite pure mock for service-with-cache-provider; Phase 73-04 mixed DB+mock for service-with-external-deps. 4. Per-file target ≥75%.</action>
  <verify>go test -cover -count=1 -coverprofile=/tmp/ops.out ./internal/services/operations/ && go tool cover -func=/tmp/ops.out | grep -v "100.0%" | head -20</verify>
  <done>Gap files enumerated; batch plan documented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement batch 1 - operations + system sub-service tests (~8 files)</name>
  <files>internal/services/operations/{building,floor,workstation,serverroom,excel,geocoding}_service_test.go, internal/services/system/{user,role,dept}_service_test.go (new gaps)</files>
  <read_first>internal/services/operations/building_service.go, excel_service.go, geocoding_service.go, internal/services/system/user_service.go, role_service.go, dept_service.go, .planning/phases/73-p1-pending/73-04-SUMMARY.md</read_first>
  <action>Implement 8 sub-service test files for highest-impact operations + system sub-services. excel + geocoding are complex (mock external Baidu API for geocoding per CLAUDE.md). Real glebarez sqlite. Each ≥200 lines, ≥8 tests, ≥75% per-file. D-12 honored. Target: operations ≥40%, system ≥65% after batch 1.</action>
  <verify>go test -cover -count=1 ./internal/services/operations/ ./internal/services/system/ 2>&1 | tail -5</verify>
  <acceptance_criteria>8 test files passing, ≥75% per-file, operations ≥40%, system ≥65%, git diff shows only test files (D-12).</acceptance_criteria>
  <done>Batch 1 complete; operations ≥40%, system ≥65%.</done>
</task>

<task type="auto">
  <name>Task 3: Implement batch 2 - remaining sub-services + asset tests (~7 files)</name>
  <files>internal/services/operations/{info_point,dedicated_line,asset_location,duty_station}_service_test.go, internal/services/system/{menu,dict,post,config}_service_test.go (new gaps), internal/services/asset/{asset,component,reconciliation}_service_test.go</files>
  <read_first>internal/services/operations/info_point_service.go, etc., internal/services/system/menu_service.go, internal/services/asset/asset_service.go, .planning/phases/73-p1-pending/73-04-SUMMARY.md</read_first>
  <action>Implement 7+ remaining sub-service tests. Each ≥150 lines, ≥8 tests, ≥75% per-file. Target: operations ≥60%, system ≥75%, asset ≥70%. If <targets, identify uncovered methods and add tests. D-12 honored throughout.</action>
  <verify>go test -cover -count=1 ./internal/services/operations/ ./internal/services/system/ ./internal/services/asset/ 2>&1 | tail -5</verify>
  <acceptance_criteria>All batch 2 test files passing, operations ≥60%, system ≥75%, asset ≥70%, git diff shows only test files (D-12).</acceptance_criteria>
  <done>All 15+ test files complete; operations ≥60%, system ≥75%, asset ≥70%.</done>
</task>

<task type="auto">
  <name>Task 4: Atomic commit + SUMMARY</name>
  <files>.planning/phases/74-p2-finalize-and-diff-coverage/74-07-SUMMARY.md</files>
  <read_first>.planning/phases/72-p0-core-supplement/72-13-PLAN.md (atomic commit pattern)</read_first>
  <action>1. Stage all test files. 2. Verify git diff --cached --stat shows only *_test.go. 3. Commit: git commit -m 'test(74-07): add operations+system+asset service tests (X files, ops 22.5%→60%, sys 53.5%→75%, asset 40.5%→70%, D-12 honored)'. 4. Do NOT push. 5. Capture final coverage. 6. Write 74-07-SUMMARY.md.</action>
  <verify>git log --oneline -2 && go test -cover -count=1 ./internal/services/operations/ ./internal/services/system/ ./internal/services/asset/ 2>&1 | tail -10</verify>
  <acceptance_criteria>Atomic commit with all test files, operations ≥60%, system ≥75%, asset ≥70%, 74-07-SUMMARY.md complete, D-12 honored.</acceptance_criteria>
  <done>74-07 atomic commit complete; 3 service packages lifted; SUMMARY written.</done>
</task>

</tasks>

<verification>go test ./internal/services/operations/ ./internal/services/system/ ./internal/services/asset/ exits 0. operations ≥60% (was 22.5%), system ≥75% (was 53.5%), asset ≥70% (was 40.5%). git diff --stat shows only *_test.go additions (D-12). Atomic commit with 15+ test files. 74-07-SUMMARY.md complete.</verification>

<success_criteria>internal/services/operations per-package ≥60% (3714 stmts). internal/services/system per-package ≥75% (3483 stmts). internal/services/asset per-package ≥70% (1354 stmts). D-12 honored (zero business code changes). Atomic commit containing all test files. 74-07-SUMMARY.md complete.</success_criteria>

<output>Create .planning/phases/74-p2-finalize-and-diff-coverage/74-07-SUMMARY.md when done.</output>