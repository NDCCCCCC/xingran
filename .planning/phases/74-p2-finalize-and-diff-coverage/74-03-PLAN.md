---
phase: 74-p2-finalize-and-diff-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/v1/network/*_test.go
autonomous: true
requirements:
  - SCALE-01
user_setup: []

must_haves:
  truths:
    - "internal/api/v1/network package coverage ≥70% (1971 stmts baseline - largest P2 handler)"
    - "Network device CRUD operations covered"
    - "VLAN + port binding + topology endpoints tested"
    - "All *_test.go files are test-only (D-12)"
  artifacts:
    - path: "internal/api/v1/network/network_device_handler_test.go"
      provides: "Network device CRUD handler tests"
      min_lines: 400
    - path: "internal/api/v1/network/vlan_handler_test.go"
      provides: "VLAN configuration handler tests"
      min_lines: 300
    - path: "internal/api/v1/network/port_binding_handler_test.go"
      provides: "Port binding handler tests"
      min_lines: 300
    - path: "internal/api/v1/network/router_test.go"
      provides: "SetupNetworkRouter smoke test"
      min_lines: 100
  key_links:
    - from: "internal/api/v1/network/network_device_handler_test.go"
      to: "internal/services/network/"
      via: "handler delegates to network service (Phase 73-03 mock pattern)"
      pattern: "network.NewNetworkDeviceService"
---

<objective>
Add comprehensive handler tests for internal/api/v1/network package (1971 stmts, currently 7.6% - largest P2 handler package). Push per-package coverage to ≥70% per Phase 74 D-15 P2 floor. Addresses SCALE-01 - network handler covers device CRUD, VLAN, port binding, topology endpoints.

Purpose: Network handler is the largest P2 handler at 1971 stmts (Phase 56 / v1.20 VLAN + port binding). Closing it lifts the weighted average significantly (estimated +1222 covered stmts).

Output: ~4-6 *_handler_test.go files + 1 router_test.go; per-file ≥75%; D-12 honored.
</objective>

<execution_context>
execute@$HOME/.claude/get-shit-done/workflows/execute-plan.md
execute@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
context@.planning/phases/74-p2-finalize-and-diff-coverage/74-CONTEXT.md
context@.planning/phases/73-p1-pending/73-02-PLAN.md (handler complex test pattern - rpa+vdi)
context@.planning/REQUIREMENTS.md (SCALE-01)
context@.planning/coverage-baseline.md (Phase 73 row - network at 7.6%)

# Source of truth
Read these files first:
- internal/api/v1/network/ (list all .go files)
- internal/services/network/ (network service interfaces)
- .planning/phases/73-p1-pending/73-02-SUMMARY.md (complex handler pattern)
- .planning/phases/73-p1-pending/73-03-SUMMARY.md (network service test pattern reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate network handler modules + design test strategy</name>
  <files>internal/api/v1/network/*_handler_test.go (4+ new), internal/api/v1/network/router_test.go (new)</files>
  <read_first>internal/api/v1/network/ (glob all .go files), internal/services/network/ (service interfaces), .planning/phases/73-p1-pending/73-02-SUMMARY.md (complex handler pattern)</read_first>
  <action>Enumerate network handler methods. Expected sub-handlers: network_device, vlan, port_binding, topology, port_collection. Mock strategy (Phase 73-02 rpa pattern): mockXxxService per sub-handler, separate test files. File split: 4-5 *_handler_test.go + 1 router_test.go. Per-file target ≥75%.</action>
  <verify>ls internal/api/v1/network/ | head -30</verify>
  <done>Test strategy: 5-6 test files mapped to 5 sub-handlers + 1 router.</done>
</task>

<task type="auto">
  <name>Task 2: Implement network_device + vlan + port_binding handler tests (3 files)</name>
  <files>internal/api/v1/network/network_device_handler_test.go, internal/api/v1/network/vlan_handler_test.go, internal/api/v1/network/port_binding_handler_test.go</files>
  <read_first>internal/api/v1/network/network_device_handler.go, vlan_handler.go, port_binding_handler.go, .planning/phases/73-p1-pending/73-02-SUMMARY.md</read_first>
  <action>Implement 3 largest sub-handler test files. Each ≥300 lines, ≥10 tests, ≥75% per-file. mockNetworkDeviceService etc. per Phase 73-02 pattern. D-12 honored.</action>
  <verify>go test -cover -count=1 ./internal/api/v1/network/ 2>&1 | tail -5</verify>
  <acceptance_criteria>3 test files passing, each ≥300 lines ≥10 tests ≥75%, mock pattern matches Phase 73-02, git diff shows only test files (D-12).</acceptance_criteria>
  <done>3 large network handler test files complete; per-file coverage ≥75%.</done>
</task>

<task type="auto">
  <name>Task 3: Implement topology + port_collection + router tests</name>
  <files>internal/api/v1/network/topology_handler_test.go, internal/api/v1/network/port_collection_handler_test.go, internal/api/v1/network/router_test.go</files>
  <read_first>internal/api/v1/network/topology_handler.go, port_collection_handler.go, network_router.go, .planning/phases/73-p1-pending/73-02-SUMMARY.md (router smoke test pattern)</read_first>
  <action>1. Implement 2 remaining sub-handler test files (topology, port_collection) + 1 router_test.go. 2. Run full coverage: target ≥70%. 3. If <70%, identify uncovered methods and add tests.</action>
  <verify>go test -cover -count=1 ./internal/api/v1/network/ 2>&1 | tail -5</verify>
  <done>All network test files complete; per-package network ≥70%.</done>
</task>

<task type="auto">
  <name>Task 4: Atomic commit + SUMMARY</name>
  <files>.planning/phases/74-p2-finalize-and-diff-coverage/74-03-SUMMARY.md</files>
  <read_first>.planning/phases/72-p0-core-supplement/72-13-PLAN.md (atomic commit pattern)</read_first>
  <action>1. Stage all test files. 2. Verify git diff --cached --stat shows only *_test.go. 3. Commit: git commit -m 'test(74-03): add network handler tests (X files, ≥70% per-package, D-12 honored)'. 4. Do NOT push. 5. Write 74-03-SUMMARY.md.</action>
  <verify>git log --oneline -2 && go test -cover -count=1 ./internal/api/v1/network/ 2>&1 | tail -3</verify>
  <acceptance_criteria>Single atomic commit with all test files, per-package network ≥70%, 74-03-SUMMARY.md complete, D-12 honored.</acceptance_criteria>
  <done>74-03 atomic commit complete; per-package network ≥70%; SUMMARY written.</done>
</task>

</tasks>

<verification>go test ./internal/api/v1/network/ exits 0. Per-package coverage ≥70% per D-15 P2 floor. git diff --stat shows only *_test.go additions (D-12). Atomic commit with 4-6 test files. 74-03-SUMMARY.md complete.</verification>

<success_criteria>internal/api/v1/network per-package ≥70% (1971 stmts - largest P2 handler). Per-file coverage ≥75% (4-6 files). D-12 honored (zero business code changes). Atomic commit containing all test files. 74-03-SUMMARY.md complete.</success_criteria>

<output>Create .planning/phases/74-p2-finalize-and-diff-coverage/74-03-SUMMARY.md when done.</output>