---
phase: 74-p2-finalize-and-diff-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/v1/asset/*_test.go
autonomous: true
requirements:
  - SCALE-01
user_setup: []

must_haves:
  truths:
    - "internal/api/v1/asset package coverage ≥70% (420 stmts baseline)"
    - "All *_test.go files added are test-only (no business code changes per D-12)"
    - "Asset CRUD operations covered (create/update/delete/get/list)"
    - "Component collection + reconciliation endpoints tested"
  artifacts:
    - path: "internal/api/v1/asset/asset_handler_test.go"
      provides: "Asset CRUD handler tests"
      min_lines: 400
    - path: "internal/api/v1/asset/router_test.go"
      provides: "SetupAssetRouter smoke test"
      min_lines: 80
  key_links:
    - from: "internal/api/v1/asset/asset_handler_test.go"
      to: "internal/api/v1/asset/asset_handler.go"
      via: "go test invokes handler methods via httptest"
      pattern: "httptest.NewRecorder"
    - from: "internal/api/v1/asset/asset_handler.go"
      to: "internal/services/asset/"
      via: "handler delegates to asset service (Phase 73-01 mock pattern)"
      pattern: "asset.NewAssetService"
---

<objective>
Add handler tests for internal/api/v1/asset package (420 stmts, currently 8.3%), pushing per-package coverage to ≥70% per Phase 74 D-15 P2 floor. Addresses SCALE-01 P2 big block - asset management is mid-sized but relatively self-contained.

Purpose: Asset is a Phase 13 module (v1.13 SHIPPED 2026-06-08), and its handler coverage at 8.3% is incomplete. Lifting it ensures SCALE-01 contribution met (~+260 covered stmts).

Output: ~3-4 *_handler_test.go files + 1 router_test.go; 1 atomic commit per file pattern; D-12 honored.
</objective>

<execution_context>
execute@$HOME/.claude/get-shit-done/workflows/execute-plan.md
execute@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
context@.planning/phases/74-p2-finalize-and-diff-coverage/74-CONTEXT.md
context@.planning/phases/73-p1-pending/73-01-PLAN.md (handler simple test pattern)
context@.planning/REQUIREMENTS.md (SCALE-01)
context@.planning/coverage-baseline.md (Phase 73 row - asset at 8.3%)

# Source of truth
Read these files first:
- internal/api/v1/asset/ (list all .go files)
- internal/services/asset/ (asset service interfaces)
- .planning/phases/73-p1-pending/73-01-SUMMARY.md (mock pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate asset handler modules + design test strategy</name>
  <files>internal/api/v1/asset/asset_handler_test.go (new), internal/api/v1/asset/component_handler_test.go (new), internal/api/v1/asset/reconciliation_handler_test.go (new), internal/api/v1/asset/asset_router_test.go (new)</files>
  <read_first>internal/api/v1/asset/ (glob all .go files), internal/services/asset/ (interface definitions), .planning/phases/73-p1-pending/73-01-SUMMARY.md (mock service pattern)</read_first>
  <action>Enumerate asset handler methods via ls internal/api/v1/asset/ and grep. Plan test strategy: 1. Expected sub-handlers: asset, component, reconciliation. 2. For each: exported methods, DTO structs, service deps, router registration. 3. Mock strategy (Phase 73-01 pattern). 4. File split: 3 *_handler_test.go + 1 router_test.go (4 total). 5. Per-file target ≥75%.</action>
  <verify>ls internal/api/v1/asset/ | head -20</verify>
  <done>Test strategy: 4 test files mapped to 3 sub-handlers + 1 router.</done>
</task>

<task type="auto">
  <name>Task 2: Implement asset + component + reconciliation handler tests (3 files)</name>
  <files>internal/api/v1/asset/asset_handler_test.go, internal/api/v1/asset/component_handler_test.go, internal/api/v1/asset/reconciliation_handler_test.go</files>
  <read_first>internal/api/v1/asset/asset_handler.go, component_handler.go, reconciliation_handler.go, .planning/phases/73-p1-pending/73-01-SUMMARY.md</read_first>
  <action>Implement 3 sub-handler test files. Each ≥150 lines, ≥8 tests, ≥75% per-file. mockXxxService per Phase 73-01 pattern. D-12 honored. Use httptest pattern.</action>
  <verify>go test -cover -count=1 ./internal/api/v1/asset/ 2>&1 | tail -5</verify>
  <acceptance_criteria>3 test files created passing, each ≥150 lines ≥8 tests ≥75%, mock pattern matches Phase 73-01, git diff shows only test files (D-12).</acceptance_criteria>
  <done>3 asset handler test files complete; per-file coverage ≥75%.</done>
</task>

<task type="auto">
  <name>Task 3: Implement router test + verify per-package coverage</name>
  <files>internal/api/v1/asset/asset_router_test.go</files>
  <read_first>internal/api/v1/asset/asset_router.go (SetupAssetRouter), .planning/phases/73-p1-pending/73-02-SUMMARY.md (router smoke test pattern)</read_first>
  <action>1. Create asset_router_test.go: smoke test for SetupAssetRouter. 2. Run full package coverage. Target: ≥70%. 3. If <70%, identify uncovered exported methods and add additional tests.</action>
  <verify>go test -cover -count=1 ./internal/api/v1/asset/ 2>&1 | tail -5</verify>
  <done>Router test complete; per-package asset ≥70%.</done>
</task>

<task type="auto">
  <name>Task 4: Atomic commit + SUMMARY</name>
  <files>.planning/phases/74-p2-finalize-and-diff-coverage/74-02-SUMMARY.md</files>
  <read_first>.planning/phases/72-p0-core-supplement/72-13-PLAN.md (atomic commit pattern)</read_first>
  <action>1. Stage all 4 test files. 2. Verify git diff --cached --stat shows only *_test.go. 3. Commit: git commit -m 'test(74-02): add asset handler tests (4 files, ≥70% per-package, D-12 honored)'. 4. Do NOT push. 5. Capture final coverage. 6. Write 74-02-SUMMARY.md.</action>
  <verify>git log --oneline -2 && go test -cover -count=1 ./internal/api/v1/asset/ 2>&1 | tail -3</verify>
  <acceptance_criteria>Single atomic commit with 4 test files, D-12 honored, 74-02-SUMMARY.md complete, per-package asset ≥70%.</acceptance_criteria>
  <done>74-02 atomic commit complete; per-package asset ≥70%; SUMMARY written.</done>
</task>

</tasks>

<verification>go test ./internal/api/v1/asset/ exits 0. Per-package coverage ≥70% per D-15 P2 floor. git diff --stat shows only *_test.go additions (D-12). Atomic commit with 4 test files. 74-02-SUMMARY.md complete.</verification>

<success_criteria>internal/api/v1/asset per-package ≥70% (420 stmts). Per-file coverage ≥75% (4 files). D-12 honored (zero business code changes). Atomic commit containing all 4 test files. 74-02-SUMMARY.md complete.</success_criteria>

<output>Create .planning/phases/74-p2-finalize-and-diff-coverage/74-02-SUMMARY.md when done.</output>