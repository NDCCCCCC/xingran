---
phase: 74-p2-finalize-and-diff-coverage
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - internal/services/rpa/*_test.go
autonomous: true
requirements:
  - SCALE-01
user_setup: []

must_haves:
  truths:
    - "internal/services/rpa package coverage ≥70% (1865 stmts baseline - largest P2 service)"
    - "RPA worker / execution / credential / AI / flow sub-services all tested"
    - "All *_test.go files are test-only (D-12)"
  artifacts:
    - path: "internal/services/rpa/*_service_test.go"
      provides: "RPA sub-service tests covering worker, execution, credential, ai, flow"
      min_lines: 1200
    - path: "internal/services/rpa/router_test.go (if services package has router)"
      provides: "Service-level smoke tests"
      min_lines: 100
  key_links:
    - from: "internal/services/rpa/*_service_test.go"
      to: "internal/services/rpa/*_service.go"
      via: "go test invokes service methods via glebarez sqlite in-memory"
      pattern: "gorm.Open.*sqlite"
---

<objective>
Add comprehensive service tests for internal/services/rpa package (1865 stmts, currently 1.1% - largest P2 service package). Push per-package coverage to ≥70% per Phase 74 D-15 P2 floor. Addresses SCALE-01 - rpa is the largest service-side gap.

Purpose: RPA services were deferred from Phase 73 due to complexity (5 sub-services). Closing them lifts weighted average significantly (estimated +1287 covered stmts).

Output: ~5 *_service_test.go files; per-file ≥75%; D-12 honored.

NOTE: rpa is complex - uses both DB and external dependencies (Scrapli textFSM templates for command parsing). Mock external deps per Phase 72 portwrite pattern (mock external interface, real DB via glebarez sqlite).
</objective>

<execution_context>
execute@$HOME/.claude/get-shit-done/workflows/execute-plan.md
execute@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
context@.planning/phases/74-p2-finalize-and-diff-coverage/74-CONTEXT.md
context@.planning/phases/73-p1-pending/73-03-SUMMARY.md (service test pattern - portwrite pure mock)
context@.planning/phases/73-p1-pending/73-04-SUMMARY.md (service mixed strategy - DB + mock)
context@.planning/REQUIREMENTS.md (SCALE-01)
context@.planning/coverage-baseline.md (Phase 73 row - services/rpa at 1.1%)

# Source of truth
Read these files first:
- internal/services/rpa/ (list all .go files - identify sub-services)
- internal/models/rpa/ (RPA domain models)
- .planning/phases/73-p1-pending/73-02-SUMMARY.md (RPA handler test pattern reference)
- .planning/phases/73-p1-pending/73-04-SUMMARY.md (mixed DB + mock pattern for services with external deps)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate RPA sub-services + design test strategy</name>
  <files>internal/services/rpa/*_service_test.go (5 new)</files>
  <read_first>internal/services/rpa/ (glob all .go files), .planning/phases/73-p1-pending/73-04-SUMMARY.md (mixed DB + mock pattern)</read_first>
  <action>Enumerate rpa sub-services. Expected: worker, execution, credential, ai, flow. Verify count. 1. For each: identify exported methods, DB models used (sys_rpa_*, etc.), external deps (Scrapli textFSM, scheduler). 2. Test strategy per Phase 73-04 mixed approach - real DB via glebarez sqlite for DB paths, mock external deps (Scrapli client, AI client). 4. Plan file split: 5 *_service_test.go files. 5. Per-file target ≥75%.</action>
  <verify>ls internal/services/rpa/ | head -30</verify>
  <done>Test strategy: 5 test files mapped to 5 sub-services; mock strategy documented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement worker + credential service tests (2 files)</name>
  <files>internal/services/rpa/worker_service_test.go, internal/services/rpa/credential_service_test.go</files>
  <read_first>internal/services/rpa/worker_service.go, credential_service.go, .planning/phases/73-p1-pending/73-03-SUMMARY.md (portwrite mock pattern), .planning/phases/73-p1-pending/73-04-SUMMARY.md (mixed DB+mock)</read_first>
  <action>Implement 2 highest-priority sub-service tests. worker: PK NULL bug fix (commit f0d0a1f) MUST be regression-tested per .planning/debug/resolved/. credential: encryption/decryption with SM4 path per Phase 36 ad_account pattern. Each ≥250 lines, ≥10 tests, ≥75% per-file. Use real glebarez sqlite + mock external deps. D-12 honored.</action>
  <verify>go test -cover -count=1 ./internal/services/rpa/ 2>&1 | tail -5</verify>
  <acceptance_criteria>2 test files passing, ≥75% per-file, git diff shows only test files (D-12), PK NULL regression test present.</acceptance_criteria>
  <done>worker + credential test files complete; per-file coverage ≥75%.</done>
</task>

<task type="auto">
  <name>Task 3: Implement execution + ai + flow service tests (3 files)</name>
  <files>internal/services/rpa/execution_service_test.go, internal/services/rpa/ai_service_test.go, internal/services/rpa/flow_service_test.go</files>
  <read_first>internal/services/rpa/execution_service.go, ai_service.go, flow_service.go, .planning/phases/73-p1-pending/73-04-SUMMARY.md</read_first>
  <action>Implement 3 remaining sub-service tests. execution: test command execution paths with mock Scrapli client. ai: test AI prompt/response paths with mock AI client. flow: test workflow DAG execution with real DB. Each ≥200 lines, ≥8 tests, ≥75% per-file. Real glebarez sqlite + mock external. D-12 honored. Per-package target ≥70%.</action>
  <verify>go test -cover -count=1 ./internal/services/rpa/ 2>&1 | tail -5</verify>
  <acceptance_criteria>3 test files passing, ≥75% per-file, per-package rpa ≥70% per D-15, git diff shows only test files (D-12).</acceptance_criteria>
  <done>All 5 rpa test files complete; per-package rpa ≥70%.</done>
</task>

<task type="auto">
  <name>Task 4: Atomic commit + SUMMARY</name>
  <files>.planning/phases/74-p2-finalize-and-diff-coverage/74-05-SUMMARY.md</files>
  <read_first>.planning/phases/72-p0-core-supplement/72-13-PLAN.md (atomic commit pattern)</read_first>
  <action>1. Stage all 5 test files. 2. Verify git diff --cached --stat shows only *_test.go. 3. Commit: git commit -m 'test(74-05): add rpa service tests (5 files, 1.1%→70%, D-12 honored, PK NULL regression locked)'. 4. Do NOT push. 5. Capture final coverage. 6. Write 74-05-SUMMARY.md.</action>
  <verify>git log --oneline -2 && go test -cover -count=1 ./internal/services/rpa/ 2>&1 | tail -3</verify>
  <acceptance_criteria>Atomic commit with 5 test files, per-package rpa ≥70% per D-15, 74-05-SUMMARY.md complete, D-12 honored, PK NULL regression test verified.</acceptance_criteria>
  <done>74-05 atomic commit complete; per-package rpa ≥70%; SUMMARY written.</done>
</task>

</tasks>

<verification>go test ./internal/services/rpa/ exits 0. Per-package coverage ≥70% per D-15 P2 floor (was 1.1% in Phase 73). git diff --stat shows only *_test.go additions (D-12). Atomic commit with 5 test files. 74-05-SUMMARY.md complete. PK NULL regression test present (commit f0d0a1f).</verification>

<success_criteria>internal/services/rpa per-package ≥70% (1865 stmts - largest P2 service). Per-file coverage ≥75% (5 files). D-12 honored (zero business code changes). RPA worker PK NULL regression tested. Atomic commit containing all test files. 74-05-SUMMARY.md complete.</success_criteria>

<output>Create .planning/phases/74-p2-finalize-and-diff-coverage/74-05-SUMMARY.md when done.</output>