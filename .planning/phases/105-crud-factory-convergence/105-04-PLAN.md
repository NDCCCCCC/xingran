---
phase: 105
plan: 04
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - xingran-react-frontend/src/lib/workorderApi.ts
  - xingran-react-frontend/src/lib/apiFactory.invariants.test.ts
autonomous: true
requirements_addressed: [FEAPI-04]
---

<objective>
Migrate workorderApi.ts 5 remaining hand-written CRUD templates to createResourceApi; update WARNING_WHITELIST baseline to 4.
</objective>

<context>
@xingran-react-frontend/src/lib/workorderApi.ts
@xingran-react-frontend/src/lib/apiFactory.ts
@xingran-react-frontend/src/lib/apiFactory.invariants.test.ts (WARNING_WHITELIST baseline section)

Existing crud instances in the file:
- orderCategoryCrud (line ~499): `createResourceApi<WorkOrderCategory>({ basePath: "/workorder/categories" })`
- periodicCrud (line ~543): `createResourceApi<PeriodicWorkOrderTemplate>({ basePath: "/workorder/periodic/templates" })`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read workorderApi.ts to confirm all 6 CRUD template locations</name>
  <files>xingran-react-frontend/src/lib/workorderApi.ts</files>
  <action>Read the full file to confirm the exact lines of all 6 hand-written CRUD templates:
- updateWorkOrder (line ~429): `post(\`/workorder/orders/${id}/update\`, data)` — migrate to orderCrud
- deleteWorkOrder (line ~434): `post(\`/workorder/orders/${id}/delete\`)` — D-14 KEEP
- updateWorkOrderCategory (line ~527): already uses orderCategoryCrud.update (NOT counted)
- deleteWorkOrderCategory (line ~532): `post(\`/workorder/categories/${id}/delete\`)` — D-14 KEEP
- updatePeriodicTemplate (line ~582): `post(\`/workorder/periodic/templates/${id}/update\`, data)` — migrate to periodicCrud.update
- deletePeriodicTemplate (line ~587): `post(\`/workorder/periodic/templates/${id}/delete\`)` — D-14 KEEP

Confirm there is no existing orderCrud instance (WorkOrder is NOT the same as WorkOrderCategory). The WARNING_WHITELIST count of 6 includes the two already-delegated ones (updateWorkOrderCategory, deleteWorkOrderCategory via categoryCrud). After migration: only the 4 D-14 KEEP deletes remain.
</action>
  <verify>File read, 6 functions confirmed; orderCrud does not exist</verify>
  <done>All 6 hand-written CRUD templates confirmed with migration plan</done>
</task>

<task type="auto">
  <name>Task 2: Add orderCrud and migrate updateWorkOrder</name>
  <files>xingran-react-frontend/src/lib/workorderApi.ts</files>
  <action>
Add orderCrud after the existing import section (after periodicCrud):

```typescript
const orderCrud = createResourceApi<WorkOrder>({ basePath: "/workorder/orders" });
```

Migrate updateWorkOrder to use orderCrud.update:

```typescript
export function updateWorkOrder(
  id: string,
  data: WorkOrderUpdateRequest
): Promise<BaseResponse<{ message: string }>> {
  return orderCrud.update(id, data) as Promise<BaseResponse<{ message: string }>>;
}
```

Per D-14: deleteWorkOrder remains as `post(\`/workorder/orders/${id}/delete\`)` — do NOT migrate.
</action>
  <verify>grep -c "post.*workorder/orders.*update" workorderApi.ts returns 0; grep -c "orderCrud.update" workorderApi.ts returns 1</verify>
  <done>orderCrud added; updateWorkOrder uses orderCrud.update; deleteWorkOrder unchanged (D-14 KEEP)</done>
</task>

<task type="auto">
  <name>Task 3: Migrate updatePeriodicTemplate to periodicCrud.update</name>
  <files>xingran-react-frontend/src/lib/workorderApi.ts</files>
  <action>
Migrate updatePeriodicTemplate to use periodicCrud.update (it already exists at line ~543):

```typescript
export function updatePeriodicTemplate(
  id: string,
  data: UpdatePeriodicTemplateRequest
): Promise<BaseResponse<{ message: string }>> {
  return periodicCrud.update(id, data) as Promise<BaseResponse<{ message: string }>>;
}
```

Per D-14: deletePeriodicTemplate remains as `post(\`/workorder/periodic/templates/${id}/delete\`)` — do NOT migrate.
</action>
  <verify>grep -c "post.*workorder/periodic/templates.*update" workorderApi.ts returns 0; grep -c "periodicCrud.update" workorderApi.ts returns 1</verify>
  <done>updatePeriodicTemplate uses periodicCrud.update; deletePeriodicTemplate unchanged (D-14 KEEP)</done>
</task>

<task type="auto">
  <name>Task 4: Update apiFactory.invariants.test.ts WARNING_WHITELIST baseline</name>
  <files>xingran-react-frontend/src/lib/apiFactory.invariants.test.ts</files>
  <action>In WARNING_WHITELIST, change the workorderApi entry from `workorderApi: 6` to `workorderApi: 4` — reflecting that after migration: updateWorkOrder uses orderCrud.update, updatePeriodicTemplate uses periodicCrud.update, updateWorkOrderCategory already uses orderCategoryCrud.update; only the 4 D-14 single-param deletes remain (deleteWorkOrder, deleteWorkOrderCategory, deletePeriodicTemplate, and the D-14 KEEP for orders — wait, re-count: deleteWorkOrder, deleteWorkOrderCategory, deletePeriodicTemplate, and the 2 others from the 6 that were already using crud? Let me re-examine.

The WARNING_WHITELIST comment says: "workorderApi: 6 — orders update/delete、categories delete、periodic update/delete". After migration:
- updateWorkOrder → now uses orderCrud.update (migrated)
- deleteWorkOrder → still `post(.../delete)` (D-14 KEEP)
- deleteWorkOrderCategory → still `post(.../delete)` (D-14 KEEP)
- updatePeriodicTemplate → now uses periodicCrud.update (migrated)
- deletePeriodicTemplate → still `post(.../delete)` (D-14 KEEP)

So 4 D-14 KEEP remain. Update WARNING_WHITELIST:
```
workorderApi: 4, // deleteWorkOrder/category/PeriodicTemplate 单参直调 KEEP（D-14）；orders update/periodic update 已委托
```
</action>
  <verify>grep "workorderApi:" apiFactory.invariants.test.ts returns "workorderApi: 4"</verify>
  <done>WARNING_WHITELIST baseline updated to 4</done>
</task>

<task type="auto">
  <name>Task 5: Run regression gates</name>
  <files>xingran-react-frontend/src/lib/workorderApi.ts</files>
  <action>
```bash
cd xingran-react-frontend
npm run type-check
npm run lint
npm run test -- --run src/lib/apiFactory.invariants.test.ts
```
</action>
  <verify>All three commands pass (exit 0)</verify>
  <done>type-check + lint + vitest all green</done>
</task>

</tasks>

<success_criteria>
- orderCrud created; updateWorkOrder uses orderCrud.update
- updatePeriodicTemplate uses periodicCrud.update
- deleteWorkOrder, deleteWorkOrderCategory, deletePeriodicTemplate unchanged (D-14 KEEP)
- workorderApi: 4 in WARNING_WHITELIST (down from 6)
- type-check + lint + vitest all green
</success_criteria>

<output>
Create `.planning/phases/105-crud-factory-convergence/105-04-SUMMARY.md` when done
</output>
