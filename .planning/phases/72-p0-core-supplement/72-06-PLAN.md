---
phase: 72-p0-core-supplement
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - internal/api/v1/system/notice_handler_test.go
  - internal/api/v1/system/notice_user_handler_test.go
  - internal/services/system/notice_service_test.go
  - internal/services/system/notice_cache_impl_test.go
autonomous: true
requirements:
  - CORE-04
  - CORE-06
user_setup: []

must_haves:
  truths:
    - "Per-sub-package: notice_handler.go + notice_user_handler.go collectively have ≥70% coverage in api/v1/system"
    - "Per-sub-package: notice_service.go + notice_cache_impl.go collectively have ≥70% coverage in services/system"
    - "Notice handler tests use real SM4 cipher + real NoticeService (per D-03)"
    - "Notice user endpoint tests cover 'my notices' / 'mark read' / 'unread count' paths"
  artifacts:
    - path: "internal/api/v1/system/notice_handler_test.go"
      provides: "Tests for notice CRUD handler"
      contains: "package system"
    - path: "internal/api/v1/system/notice_user_handler_test.go"
      provides: "Tests for user-facing notice endpoints"
      contains: "package system"
    - path: "internal/services/system/notice_service_test.go"
      provides: "Tests for notice service CRUD + publish"
      contains: "package system"
    - path: "internal/services/system/notice_cache_impl_test.go"
      provides: "Tests for notice cache wrapper"
      contains: "package system"
  key_links:
    - from: "internal/api/v1/system/notice_*_test.go"
      to: "internal/services/system/notice_service.go"
      via: "real NoticeService call"
      pattern: "noticeService\\.(Create|Update|Delete|GetByID|List|GetMyNotices|MarkRead)"
    - from: "internal/services/system/notice_cache_impl_test.go"
      to: "pkg/cache"
      via: "Cache mock for invalidation"
      pattern: "cache\\.(Set|Delete|GetOrSet)"
---

<objective>
Add tests for `notice` sub-module in both api/v1/system (CORE-04) and services/system (CORE-06) bringing per-sub-module coverage to ≥70%.

Purpose: Notice module (公告通知) is large (notice_handler.go 620 lines + notice_user_handler.go 344 = 964 handler lines; notice_service.go 128 + notice_cache_impl.go). It serves both admin (publish) and user (read/ack) flows.

Output: 2 new handler test files + 2 new service test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-p0-core-supplement/72-CONTEXT.md
@.planning/REQUIREMENTS.md

Read these source files first:
- internal/api/v1/system/ad_account_handler_test.go (D-01 lightweight pattern)
- internal/api/v1/system/notice_handler.go (620 lines; admin CRUD)
- internal/api/v1/system/notice_user_handler.go (344 lines; user-facing)
- internal/api/v1/system/notice_router.go + notice_user_router.go (constructor signatures)
- internal/services/system/notice_service.go (128 lines; service impl)
- internal/services/system/notice_cache_impl.go (cache wrapper)
- internal/models/sys_notice.go (notice model)
- internal/models/sys_notice_user.go (notice-user read state)
- internal/models/system/requests/notice.go (request DTOs)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map notice handler methods + design fixtures</name>
  <files>internal/api/v1/system/notice_handler_test.go</files>
  <read_first>
    - internal/api/v1/system/notice_handler.go (full file)
    - internal/api/v1/system/notice_user_handler.go (full file)
    - internal/api/v1/system/notice_router.go (constructor signatures)
    - internal/api/v1/system/notice_user_router.go (constructor signatures)
    - internal/services/system/notice_service.go (full file)
    - internal/services/system/notice_cache_impl.go (full file)
  </read_first>
  <action>
Read both notice handler files. Enumerate methods:
- notice_handler.go (admin): List / GetByID / Create / Update / Delete / Publish / Revoke
- notice_user_handler.go (user): GetMyNotices / GetUnreadCount / MarkRead / MarkAllRead / GetNoticeDetail

For each method, identify:
- Request struct (e.g. `notice.ListRequest`)
- Service method called (e.g. `noticeService.List`)
- Response shape

Design shared helpers in notice_handler_test.go:
- `setupNoticeTestDB(t *testing.T) *gorm.DB` — glebarez sqlite in-memory + CREATE TABLE for `sys_notice` (id TEXT PK, title TEXT, content TEXT, type INTEGER, status INTEGER, publisher_id TEXT, created_at DATETIME) and `sys_notice_user` (id TEXT PK, notice_id TEXT, user_id TEXT, read_status INTEGER, read_at DATETIME)
- `setupRealSM4Cipher(t *testing.T)` — per D-03
- `setupNoticeHandler(t *testing.T, db *gorm.DB) (*NoticeHandler, *NoticeUserHandler)` — real NoticeServiceImpl

Output: comment block with method enumeration + fixture design. Stub file compiles.
  </action>
  <verify>
    <automated>go build ./internal/api/v1/system/... 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Comment block enumerates ≥10 handler methods total
    - setupNoticeTestDB DDL matches sys_notice + sys_notice_user schema
    - Stub notice_handler_test.go compiles
  </acceptance_criteria>
  <done>
Method enumeration + fixture design complete; stub compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement handler tests for admin + user notice flows</name>
  <files>internal/api/v1/system/notice_handler_test.go
internal/api/v1/system/notice_user_handler_test.go</files>
  <read_first>
    - internal/api/v1/system/notice_handler.go (full file)
    - internal/api/v1/system/notice_user_handler.go (full file)
    - internal/services/system/notice_service.go (full file)
    - pkg/response/response.go (response format)
  </read_first>
  <action>
Per D-01 lightweight pattern with REAL SM4 cipher + REAL NoticeService:

For notice_handler.go (admin):
- TC1/TC2/TC3 per method: success / validation_error / not_found (where applicable)
- Cover: List (with filter by type/status), GetByID, Create, Update, Delete, Publish, Revoke
- For Publish: assert status transitions from draft (0) to published (1)
- For Revoke: assert status transitions to revoked (2)

For notice_user_handler.go (user):
- TC1/TC2 per method: success / unauthorized (missing user_id context)
- Cover: GetMyNotices, GetUnreadCount, MarkRead, MarkAllRead, GetNoticeDetail
- Use gin context with `c.Set("user_id", uuid)` to simulate authenticated user

All tests use `httptest.NewRecorder()` + `gin.CreateTestContext()` + real router chain.

CRITICAL: Real SM4 cipher per D-03 — do NOT reuse mockSM4Cipher stub from ad_account_handler_test.go.
  </action>
  <verify>
    <automated>go test -count=1 -cover -run "TestNotice" ./internal/api/v1/system/... 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Both handler test files compile and pass
    - `go test -count=1 -cover -run "TestNotice" ./internal/api/v1/system/...` exits 0
    - Real SM4 cipher used (no mockSM4Cipher import)
    - At least 25 test functions across the 2 files
    - User-facing endpoints verify context user_id extraction
  </acceptance_criteria>
  <done>
Notice handler tests complete (admin + user flows).
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement service tests for notice service + cache impl</name>
  <files>internal/services/system/notice_service_test.go
internal/services/system/notice_cache_impl_test.go</files>
  <read_first>
    - internal/services/system/notice_service.go (full file)
    - internal/services/system/notice_cache_impl.go (full file)
    - internal/services/portwrite/port_write_service_test.go (D-01 service pattern reference)
  </read_first>
  <action>
For notice_service.go (128 lines, small):
- Use glebarez sqlite in-memory + real NoticeServiceImpl
- Cover: Create / GetByID / Update / Delete / List / Publish / Revoke / GetMyNotices / MarkRead / MarkAllRead / GetUnreadCount
- 2 test cases per method (success + error)
- Notice service has no `apperrors` based errors likely — verify by reading file

For notice_cache_impl.go (cache wrapper):
- Use `mockCache` (function-field stub) for cache invalidation tests
- 5-8 test cases: cache miss → DB → cache populated; cache hit; cache.Set called after Create/Update/Delete; cache.Delete called after status change
- Compile-time assertion: `var _ CacheProvider = (*noticeCacheImpl)(nil)`

Per D-01 service pattern: testify/mock.Mock embedded for mock types, interface assertions at top of file, table-driven TC1/TC2 naming.
  </action>
  <verify>
    <automated>go test -count=1 -cover -run "TestNotice" ./internal/services/system/... 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Both service test files compile and pass
    - `go test -count=1 -cover -run "TestNotice" ./internal/services/system/...` exits 0
    - At least 15 test functions across the 2 files
    - Cache wrapper invalidation logic verified
  </acceptance_criteria>
  <done>
Notice service tests complete.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify per-sub-module notice coverage ≥70% + write SUMMARY</name>
  <files>.planning/phases/72-p0-core-supplement/72-06-SUMMARY.md</files>
  <read_first>
    - .planning/phases/72-p0-core-supplement/72-CONTEXT.md
  </read_first>
  <action>
Verify per-sub-module notice coverage:
```
go test -coverprofile=c.out -count=1 ./internal/api/v1/system/ ./internal/services/system/
go tool cover -func=c.out | grep -E "(\bnotice_handler\.go|\bnotice_user_handler\.go|\bnotice_service\.go|\bnotice_cache_impl\.go)"
```

Calculate weighted average across the 4 notice-related files. Target ≥70.0%.

If < 70%:
- Identify uncovered branches via `go tool cover -html=c.out -o coverage.html`
- Add missing test cases (likely: edge cases for status transitions, empty user_id contexts)
- Re-run

Clean up artifacts. Write `72-06-SUMMARY.md`:
- Per-file coverage breakdown
- Test count
- Commit message draft: `test(72-06): CORE-04+06 notice sub-module tests → ≥70%`

Do NOT commit yet.
  </action>
  <verify>
    <automated>go test -cover -count=1 ./internal/api/v1/system/ ./internal/services/system/ 2>&1 | grep -E "^(ok|FAIL|coverage:)"</automated>
  </verify>
  <acceptance_criteria>
    - Per-file notice coverage: weighted average ≥70% across notice_handler.go + notice_user_handler.go + notice_service.go + notice_cache_impl.go
    - `72-06-SUMMARY.md` exists with breakdown
  </acceptance_criteria>
  <done>
Notice sub-module per-sub-package ≥70%; SUMMARY documented.
  </done>
</task>

</tasks>

<verification>
- Per-sub-module notice coverage: weighted average ≥70% across 4 notice-related files
- Real SM4 cipher used (D-03)
- Zero business code changes (D-08)
</verification>

<success_criteria>
- [ ] CORE-04 notice sub-module ≥70% per-sub-module
- [ ] CORE-06 notice sub-module ≥70% per-sub-module
- [ ] All 4 notice-related files have ≥2 test functions each
- [ ] Real SM4 cipher used (D-03)
- [ ] No new mock framework (D-06)
</success_criteria>

<output>
Create `.planning/phases/72-p0-core-supplement/72-06-SUMMARY.md` when done.
</output>
