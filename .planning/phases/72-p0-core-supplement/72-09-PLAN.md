---
phase: 72-p0-core-supplement
plan: 09
type: execute
wave: 3
depends_on: []
files_modified:
  - internal/api/v1/system/dict_handler_test.go
  - internal/api/v1/system/post_handler_test.go
  - internal/services/system/dict_service_test.go
  - internal/services/system/dict_cache_impl_test.go
  - internal/services/system/post_service_test.go
  - internal/services/system/post_cache_impl_test.go
autonomous: true
requirements:
  - CORE-04
  - CORE-06
user_setup: []

must_haves:
  truths:
    - "Per-sub-package: dict_handler.go ≥70% coverage in api/v1/system"
    - "Per-sub-package: dict_service.go + dict_cache_impl.go ≥70% coverage in services/system (existing dict_statistics_test.go preserved)"
    - "Per-sub-package: post_handler.go ≥70% coverage in api/v1/system"
    - "Per-sub-package: post_service.go + post_cache_impl.go ≥70% coverage in services/system (existing post_statistics_test.go preserved)"
    - "Dict tests cover sys_dict_type + sys_dict_data (type / value / status / sort)"
    - "Post tests cover sys_post CRUD (name / code / sort / status)"
  artifacts:
    - path: "internal/api/v1/system/dict_handler_test.go"
      provides: "Tests for dict type + data CRUD handler"
      contains: "package system"
    - path: "internal/api/v1/system/post_handler_test.go"
      provides: "Tests for post CRUD handler"
      contains: "package system"
    - path: "internal/services/system/dict_service_test.go"
      provides: "Tests for dict service (extends existing dict_statistics_test)"
      contains: "package system"
    - path: "internal/services/system/dict_cache_impl_test.go"
      provides: "Tests for dict cache wrapper"
      contains: "package system"
    - path: "internal/services/system/post_service_test.go"
      provides: "Tests for post service (extends existing post_statistics_test)"
      contains: "package system"
    - path: "internal/services/system/post_cache_impl_test.go"
      provides: "Tests for post cache wrapper"
      contains: "package system"
  key_links:
    - from: "internal/api/v1/system/dict_handler_test.go"
      to: "internal/services/system/dict_service.go"
      via: "real DictService call"
      pattern: "dictService\\.(CreateType|ListTypes|GetDataByType|...)"
    - from: "internal/api/v1/system/post_handler_test.go"
      to: "internal/services/system/post_service.go"
      via: "real PostService call"
      pattern: "postService\\.(Create|Update|Delete|GetByID|List|...)"
---

<objective>
Add tests for `dict` + `post` sub-modules in both api/v1/system (CORE-04) and services/system (CORE-06) bringing per-sub-module coverage to ≥70%.

Purpose: Dict (字典) + Post (岗位) are config-like tables used everywhere for status/type enums. dict_handler.go 491 lines; post_handler.go 295 lines. Service side: dict_service.go 382; post_service.go 246. Existing dict_statistics_test.go + post_statistics_test.go cover stats methods — this plan fills CRUD gap.

Output: 2 new handler test files + 4 new/extended service test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-p0-core-supplement/72-CONTEXT.md
@.planning/REQUIREMENTS.md

Read these source files first:
- internal/api/v1/system/ad_account_handler_test.go (D-01 lightweight pattern)
- internal/api/v1/system/dict_handler.go (491 lines; covers type + data)
- internal/api/v1/system/dict_router.go (constructor signatures)
- internal/api/v1/system/post_handler.go (295 lines)
- internal/api/v1/system/post_router.go (constructor signatures)
- internal/services/system/dict_service.go (382 lines)
- internal/services/system/dict_cache_impl.go (cache wrapper)
- internal/services/system/dict_statistics_test.go (EXISTING — preserve)
- internal/services/system/post_service.go (246 lines)
- internal/services/system/post_cache_impl.go (cache wrapper)
- internal/services/system/post_statistics_test.go (EXISTING — preserve)
- internal/models/sys_dict_type.go and sys_dict_data.go
- internal/models/sys_post.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map dict + post methods + design fixtures</name>
  <files>internal/api/v1/system/dict_handler_test.go</files>
  <read_first>
    - internal/api/v1/system/dict_handler.go (full file)
    - internal/api/v1/system/post_handler.go (full file)
    - internal/services/system/dict_service.go (full file)
    - internal/services/system/post_service.go (full file)
    - internal/services/system/dict_statistics_test.go (EXISTING — preserve)
    - internal/services/system/post_statistics_test.go (EXISTING — preserve)
  </read_first>
  <action>
Read all 4 source files. Enumerate methods:

dict_handler.go: ListTypes / GetType / CreateType / UpdateType / DeleteType / ListData / GetData / CreateData / UpdateData / DeleteData / GetDataByType
post_handler.go: List / GetByID / Create / Update / Delete / UpdateStatus / GetAll

For each method, identify request/response shape.

Design shared helpers:
- `setupDictTestDB(t *testing.T) *gorm.DB` — glebarez sqlite + CREATE TABLE `sys_dict_type` (id TEXT PK, dict_type TEXT UNIQUE, dict_name TEXT, status INTEGER, created_at DATETIME) + CREATE TABLE `sys_dict_data` (id TEXT PK, dict_type TEXT, dict_label TEXT, dict_value TEXT, status INTEGER, sort INTEGER, created_at DATETIME)
- `setupPostTestDB(t *testing.T) *gorm.DB` — glebarez sqlite + CREATE TABLE `sys_post` (id TEXT PK, post_code TEXT UNIQUE, post_name TEXT, sort INTEGER, status INTEGER [0=normal, 1=stopped], remark TEXT, created_at DATETIME)
- `setupRealSM4Cipher(t)` — per D-03
- `setupDictHandler(t)` and `setupPostHandler(t)` — real service impls

CRITICAL post status convention: per CLAUDE.md `0=normal, 1=stopped`. Use `models.PostStatusNormal = 0` / `models.PostStatusStopped = 1` constants, NOT bare 0/1 literals.

CRITICAL dict status: similar 0=normal, 1=stopped convention. Use constants.

Output: comment block with method enumeration + fixture design. Stub files compile.
  </action>
  <verify>
    <automated>go build ./internal/api/v1/system/... ./internal/services/system/... 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Comment block enumerates ≥15 handler methods total
    - Both test DBs create correct schema
    - Status uses constants per CLAUDE.md (not bare 0/1)
    - Stub files compile
  </acceptance_criteria>
  <done>
Method enumeration + fixture design complete; stubs compile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement dict + post handler tests</name>
  <files>internal/api/v1/system/dict_handler_test.go
internal/api/v1/system/post_handler_test.go</files>
  <read_first>
    - internal/api/v1/system/dict_handler.go (full file)
    - internal/api/v1/system/post_handler.go (full file)
    - internal/services/system/dict_service.go (full file)
    - internal/services/system/post_service.go (full file)
  </read_first>
  <action>
Per D-01 lightweight pattern with REAL services + glebarez sqlite:

For dict_handler.go:
- TC1/TC2 per method: success / error
- Special cases:
  - `CreateType`: assert UNIQUE constraint violation on duplicate dict_type (returns 400/409)
  - `GetDataByType`: seed multiple sys_dict_data with same dict_type, assert all returned
  - `DeleteType`: assert cascade or error when sys_dict_data exists for the type (verify by reading dict_service.go DeleteType logic)

For post_handler.go:
- TC1/TC2 per method: success / error
- Special cases:
  - `Create`: assert UNIQUE constraint violation on duplicate post_code
  - `UpdateStatus`: assert status transitions between `models.PostStatusNormal` (0) and `models.PostStatusStopped` (1)
  - `GetAll`: assert returns all posts regardless of status (used by user form dropdowns)

All tests use `httptest.NewRecorder()` + `gin.CreateTestContext()` + real services. Real SM4 cipher per D-03.
  </action>
  <verify>
    <automated>go test -count=1 -cover -run "TestDict|TestPost" ./internal/api/v1/system/... 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Both handler test files compile and pass
    - `go test -count=1 -cover -run "TestDict|TestPost" ./internal/api/v1/system/...` exits 0
    - At least 25 test functions across 2 files
    - Status convention uses constants per CLAUDE.md
  </acceptance_criteria>
  <done>
Dict + post handler tests complete.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement dict + post service tests (preserve existing statistics tests)</name>
  <files>internal/services/system/dict_service_test.go
internal/services/system/dict_cache_impl_test.go
internal/services/system/post_service_test.go
internal/services/system/post_cache_impl_test.go</files>
  <read_first>
    - internal/services/system/dict_service.go (full file)
    - internal/services/system/dict_cache_impl.go (full file)
    - internal/services/system/post_service.go (full file)
    - internal/services/system/post_cache_impl.go (full file)
    - internal/services/system/dict_statistics_test.go (EXISTING — preserve)
    - internal/services/system/post_statistics_test.go (EXISTING — preserve)
    - internal/services/portwrite/port_write_service_test.go (D-01 service pattern reference)
  </read_first>
  <action>
Per D-01 service pattern:

For dict_service.go (382 lines):
- Use glebarez sqlite in-memory + real DictServiceImpl
- Cover ALL exported methods except those already tested in dict_statistics_test.go (Statistics functions)
- 2-3 test cases per method
- Cache invalidation: use mockCache (function-field stub)

For dict_cache_impl.go:
- Compile-time interface assertion: `var _ CacheProvider = (*dictCacheImpl)(nil)`
- 5-8 test cases: cache miss/hit/invalidation after CreateType/UpdateType/DeleteType/CreateData/UpdateData/DeleteData

For post_service.go (246 lines):
- Use glebarez sqlite in-memory + real PostServiceImpl
- Cover all exported methods except Statistics (already in post_statistics_test.go)
- 2-3 test cases per method

For post_cache_impl.go:
- Compile-time interface assertion
- 5-8 test cases: cache miss/hit/invalidation after Create/Update/Delete

CRITICAL: Do NOT modify existing dict_statistics_test.go or post_statistics_test.go (D-08).
  </action>
  <verify>
    <automated>go test -count=1 -cover -run "TestDict|TestPost" ./internal/services/system/... 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All 4 service test files compile and pass
    - `go test -count=1 -cover -run "TestDict|TestPost" ./internal/services/system/...` exits 0
    - At least 30 test functions across 4 files
    - Existing dict_statistics_test.go + post_statistics_test.go preserved
    - Cache invalidation verified
  </acceptance_criteria>
  <done>
Dict + post service tests complete.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify per-sub-module dict + post coverage ≥70% + write SUMMARY</name>
  <files>.planning/phases/72-p0-core-supplement/72-09-SUMMARY.md</files>
  <read_first>
    - .planning/phases/72-p0-core-supplement/72-CONTEXT.md
  </read_first>
  <action>
Verify per-sub-module coverage for dict + post:
```
go test -coverprofile=c.out -count=1 ./internal/api/v1/system/ ./internal/services/system/
go tool cover -func=c.out | grep -E "(\bdict_handler\.go|\bpost_handler\.go|\bdict_service\.go|\bdict_cache_impl\.go|\bpost_service\.go|\bpost_cache_impl\.go)"
```

Calculate weighted average per sub-module:
- dict sub-module: dict_handler.go + dict_service.go + dict_cache_impl.go ≥70%
- post sub-module: post_handler.go + post_service.go + post_cache_impl.go ≥70%

If < 70%:
- Inspect uncovered branches via `go tool cover -html=c.out -o coverage.html`
- Add missing test cases
- Re-run

Clean up artifacts. Write `72-09-SUMMARY.md`:
- Per-file coverage breakdown for dict + post
- Test count
- Commit message draft: `test(72-09): CORE-04+06 dict+post sub-module tests → ≥70%`

Do NOT commit yet.
  </action>
  <verify>
    <automated>go test -cover -count=1 ./internal/api/v1/system/ ./internal/services/system/ 2>&1 | grep -E "^(ok|FAIL|coverage:)"</automated>
  </verify>
  <acceptance_criteria>
    - dict sub-module weighted avg ≥70% across dict_handler.go + dict_service.go + dict_cache_impl.go
    - post sub-module weighted avg ≥70% across post_handler.go + post_service.go + post_cache_impl.go
    - `72-09-SUMMARY.md` exists with breakdown
  </acceptance_criteria>
  <done>
Dict + post sub-modules per-sub-package ≥70%; SUMMARY documented.
  </done>
</task>

</tasks>

<verification>
- Per-sub-module coverage: dict + post each ≥70% weighted average
- Existing dict_statistics_test.go + post_statistics_test.go preserved (D-08)
- Status convention uses constants per CLAUDE.md
- Real SM4 cipher used (D-03)
</verification>

<success_criteria>
- [ ] CORE-04 dict + post sub-modules ≥70% per-sub-module
- [ ] CORE-06 dict + post sub-modules ≥70% per-sub-module
- [ ] All exported dict + post methods have ≥2 test cases
- [ ] Status uses constants (not bare 0/1)
- [ ] No new mock framework (D-06)
</success_criteria>

<output>
Create `.planning/phases/72-p0-core-supplement/72-09-SUMMARY.md` when done.
</output>
