---
phase: 72-p0-core-supplement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/v1/workorder/workorder_handler_test.go
autonomous: true
requirements:
  - CORE-01
user_setup: []

must_haves:
  truths:
    - "`go test -cover -count=1 ./internal/api/v1/workorder/...` exits 0 with coverage >= 70%"
    - "All handler method groups (基础操作 / 分类 / 评论 / 评分 / 周期 / 模板 / 统计 / 缓存) have at least one test case"
    - "Service errors propagate through `responseHelpers.HandleServiceError` (assert via mocked service error)"
    - "DB-level assertions use `glebarez/sqlite` in-memory; no live Postgres / Redis dependency"
  artifacts:
    - path: "internal/api/v1/workorder/workorder_handler_test.go"
      provides: "Table-driven handler tests for CORE-01 sub-package"
      contains: "package workorder"
  key_links:
    - from: "internal/api/v1/workorder/workorder_handler_test.go"
      to: "internal/services/workorder"
      via: "real service call through WorkOrderCacheService interface"
      pattern: "service\\.(GetList|GetStatusStatistics|GetMyPending|...)"
    - from: "internal/api/v1/workorder/workorder_handler_test.go"
      to: "internal/api/v1/workorder/workorder_handler.go"
      via: "handler method invocation (h.List(c), h.GetStatusStatistics(c), ...)"
      pattern: "h\\.\\w+\\(c\\)"
---

<objective>
Add handler tests for `internal/api/v1/workorder` (CORE-01, 297 stmts, 0.0% → ≥70%).

Purpose: Phase 72 P0 first delivery — workorder is a critical audit path (工单管理). Coverage gate currently 12.8% baseline; this plan pushes workorder to ≥70% per-sub-package.

Output: One `*_test.go` file in `internal/api/v1/workorder/` covering all 8 handler method groups with table-driven TC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-p0-core-supplement/72-CONTEXT.md
@.planning/phases/72-p0-core-supplement/72-DISCUSSION-LOG.md
@.planning/REQUIREMENTS.md
@.planning/quick/260820-backend-test-coverage-scan/per-package-coverage.txt

Read these source files first (D-01 范本 + 目标):
- internal/api/v1/system/ad_account_handler_test.go — D-01 lightweight handler pattern (READ this directly for actual code structure)
- internal/api/v1/workorder/workorder_handler.go — target (already loaded)
- internal/api/v1/workorder/workorder_router.go — handler constructor signature
- internal/services/workorder/service.go — WorkOrderCacheService interface methods (List/GetStatusStatistics/GetMyPending/...)
- internal/services/workorder/workorder_cache_impl.go — service constructor
- pkg/response/response.go — `response.Success` / `response.Error` / `HandleServiceError`
- pkg/response/responseHelpers.go — HandleJSONBinding + HandleServiceError
- internal/utils/operlog/operlog.go — Record(c, ...) signature (Phase 34 全模块)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map handler methods → service interface, design test fixture</name>
  <files>internal/api/v1/workorder/workorder_handler_test.go</files>
  <read_first>
    - internal/api/v1/workorder/workorder_handler.go
    - internal/api/v1/workorder/workorder_router.go
    - internal/services/workorder/service.go
    - internal/services/workorder/workorder_cache_impl.go
    - internal/api/v1/system/ad_account_handler_test.go (D-01 lightweight pattern — READ THIS for actual code structure)
  </read_first>
  <action>
Enumerate all exported handler methods on `*WorkOrderHandler` from `internal/api/v1/workorder/workorder_handler.go`. Group them into 8 categories per D-02 (基础操作 / 状态 / 待办 / 分类 / 评论 / 评分 / 周期 / 模板 / 统计 / 缓存). For each method, identify:
- request struct (e.g. `workorder.ListRequest`)
- service method called (e.g. `service.GetList`)
- response shape (PageResponse / 自定义 struct)

Design test fixture (no code yet, just structure):
- `setupTestDB(t *testing.T) *gorm.DB` — glebarez sqlite in-memory + CREATE TABLE for `sys_work_order`, `sys_work_order_comment`, etc. (use DDL matching production column types per `internal/models/operations/workorder.go` — primary key TEXT UUID, status INTEGER, created_at DATETIME)
- `setupTestHandler(t *testing.T, db *gorm.DB) (*WorkOrderHandler, *mockWorkOrderService)` — instantiates a mock WorkOrderCacheService (lightweight stub, NOT testify/mock — keep consistent with D-01 ad_account_handler pattern; READ ad_account_handler_test.go for the exact mock-with-function-fields idiom)
- `mockWorkOrderService` — implements `workorder.WorkOrderCacheService` interface with function-field pattern (each method maps to a `func(...)` field that the test sets). Mirror the lightweight stub style used in ad_account_handler_test.go.

For each handler method, identify 1 happy path + 1 error path (total ~16-20 test cases). Do NOT cover 0% stmts that require real Redis (cache layer) — use the cache impl's no-cache path by passing nil cache directly (no NoopCacheProvider — see 72-02 fix W4 for the same pattern; pkg/cache/noop.go does NOT exist as of 2026-08-21).

Output the fixture design as a comment block at top of the test file (no executable code yet).
  </action>
  <verify>
    <automated>go build ./internal/api/v1/workorder/... 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Comment block at top of `workorder_handler_test.go` enumerates ≥10 handler methods grouped by category
    - Each method entry names the service interface method and request struct
    - `setupTestDB` DDL matches production column types (TEXT primary key, INTEGER status, DATETIME timestamps)
    - `mockWorkOrderService` declared as stub with function-field pattern matching WorkOrderCacheService interface
    - `go build ./internal/api/v1/workorder/...` exits 0 (no syntax errors in stub file)
  </acceptance_criteria>
  <done>
Fixture design comment + skeleton stub compiles. No test execution yet.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement handler tests (table-driven TC)</name>
  <files>internal/api/v1/workorder/workorder_handler_test.go</files>
  <read_first>
    - internal/api/v1/workorder/workorder_handler.go (handler method bodies)
    - internal/services/workorder/service.go (interface contracts)
    - pkg/response/response.go (response.Success / response.Error return shape)
    - internal/models/operations/workorder.go (request/response struct definitions)
    - internal/api/v1/system/ad_account_handler_test.go (READ THIS — actual D-01 pattern reference for mock service with function fields, gin.CreateTestContext + httptest.NewRecorder idiom, JSON unmarshal assertions)
  </read_first>
  <action>
For each handler method identified in Task 1, write 2 test cases (happy + error) following D-01 naming:
- `TC1: <method>_success_returns_200` — assert `w.Code == 200` + `response.Success` payload unmarshaled
- `TC2: <method>_service_error_returns_500` — mock service returns `errors.New("...")`, assert `responseHelpers.HandleServiceError` propagates as 5xx

Test invocation idiom (per D-01 — see ad_account_handler_test.go for actual code):
- Use `httptest.NewRecorder()` + `gin.CreateTestContext()` to obtain the response recorder and gin context.
- Invoke the handler method directly (e.g. `h.List(c)`) on the constructed context.
- Assert `w.Code == 200` on success and parse JSON body via `json.Unmarshal(w.Body.Bytes(), &resp)` to verify payload fields.

Mock service setup pattern (per D-01 function-field idiom — see ad_account_handler_test.go):
- Declare a struct `mockWorkOrderService` with one function-field per interface method (e.g. `GetListFunc func(ctx context.Context, req *workorder.ListRequest) ([]*models.WorkOrder, int64, error)`).
- In each test, populate only the function fields the handler under test will invoke; leave others nil (they will not be reached).
- Compile-time interface assertion at file top: `var _ workorder.WorkOrderCacheService = (*mockWorkOrderService)(nil)`.
- Construct the handler with `NewWorkOrderHandler(mock)` (or the equivalent constructor in workorder_handler.go).

Seed test data into sqlite (one row per test case where relevant). Use `uuid.NewString()` for IDs. Assert response body via JSON unmarshal into a struct.

Cover at minimum these methods:
- `List`, `GetStatusStatistics`, `GetMyPending`, `Get`, `Create`, `Update`, `Delete`, `UpdateStatus`
- Plus per-category: assignment, comment, rating, periodic, reconciliation template, category methods (grep handler for all `func (h *WorkOrderHandler)` to enumerate)

Write each test as `TestHandler_<method>_<scenario>` with table-driven subtests where multiple inputs need testing.

Skip:
- Methods that internally call `operlog.Record` and require `*core.Core` (just leave `h.core = nil`, the operlog helper handles nil gracefully — verify with `grep -A5 "func Record" internal/utils/operlog/operlog.go`)
- Methods requiring live Redis cache (skip cache-specific assertions; test the underlying handler logic only)
  </action>
  <verify>
    <automated>go test -count=1 -cover ./internal/api/v1/workorder/... 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `go test -count=1 -cover ./internal/api/v1/workorder/...` exits 0
    - Coverage output shows `coverage: ≥70.0% of statements` for the workorder package
    - At least 16 test functions present (each handler method × 2 scenarios)
    - Table-driven subtests use `t.Run(tt.name, ...)` pattern with `TC1`/`TC2` naming per D-01
    - No `testify/mock` import — only `testify/assert` and `testify/require`
    - All assertions use concrete values (`assert.Equal`, `assert.NoError`), no `assert.True(true)` stubs
  </acceptance_criteria>
  <done>
All workorder handler methods have ≥2 test cases each; coverage ≥70%.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify per-sub-package coverage + write SUMMARY</name>
  <files>.planning/phases/72-p0-core-supplement/72-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/72-p0-core-supplement/72-CONTEXT.md (SC checklist)
    - .planning/templates/phase-summary.md (SUMMARY template)
  </read_first>
  <action>
Run full coverage command and capture per-file %:

Commands:
- go test -coverprofile=c.out -count=1 ./internal/api/v1/workorder/
- go tool cover -func=c.out | grep -E "(workorder_handler|total)"

Verify the total line shows ≥70.0%.

If coverage < 70%:
- Identify uncovered stmts via `go tool cover -html=c.out -o coverage.html` + open in browser
- Add additional test cases for uncovered branches (most likely: error paths not yet covered, switch defaults, validation edge cases)
- Re-run until ≥70%

If still < 70% after 3 attempts with exhaustive test cases:
- Document remaining gap in `72-01-SUMMARY.md` (do NOT lower the bar — flag for Phase 73 to address)
- Do NOT mark plan as done — escalate via Phase 73 planning

Once coverage ≥70% confirmed:
- Clean up `c.out` and `coverage.html` (do NOT commit these)
- Write `72-01-SUMMARY.md` with: files modified, coverage achieved, test count, key invariants verified, commit message draft `test(72-01): CORE-01 workorder handler tests 0% → ≥70%`

Do NOT commit yet — Task 0 in plan 72-13 handles the batch commit of all per-plan test files (per W2 fix).
  </action>
  <verify>
    <automated>go test -cover -count=1 ./internal/api/v1/workorder/... 2>&1 | grep -E "^(ok|FAIL|coverage:)"</automated>
  </verify>
  <acceptance_criteria>
    - `go test -cover -count=1 ./internal/api/v1/workorder/...` exits 0
    - Output contains `coverage: 70.X%` (X ≥ 0) of statements
    - `72-01-SUMMARY.md` exists with coverage %, file list, and commit message draft
    - No `c.out` or `coverage.html` artifacts committed
  </acceptance_criteria>
  <done>
CORE-01 target achieved (workorder package ≥70%); SUMMARY documented.
  </done>
</task>

</tasks>

<verification>
- `go test -cover -count=1 ./internal/api/v1/workorder/...` exits 0 with coverage ≥70%
- `go tool cover -func=c.out | tail -1` shows `total` line ≥70.0%
- Per-method coverage: each of the 8 method categories (List/Get/Create/Update/Delete + 4 others) ≥50% individually
</verification>

<success_criteria>
- [ ] CORE-01 SC#1: `internal/api/v1/workorder` coverage ≥70% (297 stmts)
- [ ] All handler methods have at least happy-path + error-path coverage
- [ ] No new mock framework introduced (D-06)
- [ ] Zero business code changes (D-08)
</success_criteria>

<output>
Create `.planning/phases/72-p0-core-supplement/72-01-SUMMARY.md` when done.
</output>