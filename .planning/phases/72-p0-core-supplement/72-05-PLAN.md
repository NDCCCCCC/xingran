---
phase: 72-p0-core-supplement
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - internal/api/v1/system/user_handler_test.go
  - internal/api/v1/system/user_import_handler_test.go
  - internal/api/v1/system/user_unlock_handler_test.go
  - internal/services/system/user_service_test.go
  - internal/services/system/user_cache_impl_test.go
  - internal/services/system/user_sync_service_test.go
autonomous: true
requirements:
  - CORE-04
  - CORE-06
user_setup: []

must_haves:
  truths:
    - "Per-sub-package: `internal/api/v1/system` user-related files (user_handler.go + user_import_handler.go + user_unlock_handler.go) collectively have ≥70% coverage"
    - "Per-sub-package: `internal/services/system` user-related files (user_service.go + user_cache_impl.go + user_sync_service.go) collectively have ≥70% coverage"
    - "Existing user_*_test.go files (user_list_recursive_test, user_list_status_test, user_statistics_test, user_sync_classify_test, user_sync_service_test, widget_data_fetcher_test) preserved unchanged"
    - "New tests fill the gap to reach ≥70% per sub-module"
  artifacts:
    - path: "internal/api/v1/system/user_handler_test.go"
      provides: "Tests for main user CRUD handler"
      contains: "package system"
    - path: "internal/api/v1/system/user_import_handler_test.go"
      provides: "Tests for Excel import user handler"
      contains: "package system"
    - path: "internal/api/v1/system/user_unlock_handler_test.go"
      provides: "Tests for user unlock handler"
      contains: "package system"
    - path: "internal/services/system/user_service_test.go"
      provides: "Tests for main user service CRUD"
      contains: "package system"
    - path: "internal/services/system/user_cache_impl_test.go"
      provides: "Tests for user cache wrapper"
      contains: "package system"
    - path: "internal/services/system/user_sync_service_test.go"
      provides: "Tests for user sync service (extends existing)"
      contains: "package system"
  key_links:
    - from: "internal/api/v1/system/user_*_test.go"
      to: "internal/services/system/user_service.go"
      via: "real UserService call + mock for uncovered methods"
      pattern: "userService\\.(Create|Update|Delete|GetByID|List|Statistics|BatchDelete|UpdateStatus|ResetPassword)"
    - from: "internal/services/system/user_cache_impl_test.go"
      to: "pkg/cache"
      via: "Cache interface mock"
      pattern: "cache\\.(Get|Set|Delete|GetOrSet)"
---

<objective>
Add tests for `user` sub-module across both api/v1/system (CORE-04) and services/system (CORE-06), bringing per-sub-module coverage to ≥70%.

Purpose: User is the largest sub-module (user_handler.go 515 lines + user_import_handler.go 254 + user_unlock_handler.go 59 + user_service.go 666 + user_sync_service.go 797 + user_cache_impl.go + handler_test.go existing). Many tests already exist; this plan fills the gap.

Output: 3 new handler test files + 3 new/extended service test files. Existing tests preserved per D-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-p0-core-supplement/72-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/quick/260820-backend-test-coverage-scan/per-package-coverage.txt

Read these source files first:
- internal/api/v1/system/ad_account_handler_test.go (D-01 lightweight handler pattern)
- internal/api/v1/system/user_handler.go (515 lines; main CRUD)
- internal/api/v1/system/user_import_handler.go (254 lines; Excel import)
- internal/api/v1/system/user_unlock_handler.go (59 lines; unlock)
- internal/api/v1/system/user_handler_test.go (EXISTING — 辅助 helper, preserve)
- internal/api/v1/system/user_router.go (constructor signatures)
- internal/services/system/user_service.go (UserService interface; already loaded)
- internal/services/system/user_sync_service.go (sync logic; 797 lines)
- internal/services/system/user_cache_impl.go (cache wrapper)
- internal/services/system/user_list_recursive_test.go (EXISTING — preserve)
- internal/services/system/user_list_status_test.go (EXISTING — preserve)
- internal/services/system/user_statistics_test.go (EXISTING — preserve)
- internal/services/system/user_sync_classify_test.go (EXISTING — preserve)
- internal/services/system/user_sync_service_test.go (EXISTING — preserve)
- internal/services/system/widget_data_fetcher_test.go (EXISTING — preserve)
- internal/models/system/requests/user.go (request DTOs)
- internal/models/user.go (User model fields)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish baseline user coverage + gap analysis</name>
  <files>internal/api/v1/system/user_handler_test.go</files>
  <read_first>
    - internal/services/system/user_list_recursive_test.go
    - internal/services/system/user_list_status_test.go
    - internal/services/system/user_statistics_test.go
    - internal/services/system/user_sync_classify_test.go
    - internal/services/system/user_sync_service_test.go
    - internal/services/system/widget_data_fetcher_test.go
  </read_first>
  <action>
Run baseline coverage for the user sub-module:
```
go test -coverprofile=c.out -count=1 ./internal/services/system/ ./internal/api/v1/system/
go tool cover -func=c.out | grep -E "(user_|UserService|UserHandler|UserCache|UserSync)"
```

Identify per-file coverage. Group files into "covered" (≥70% already) vs "needs more tests". Files with no test (user_handler.go, user_import_handler.go, user_unlock_handler.go, user_cache_impl.go) need new test files.

Files likely needing more coverage:
- `internal/api/v1/system/user_handler.go` — main CRUD handler; existing `user_handler_test.go` only has helpers
- `internal/api/v1/system/user_import_handler.go` — Excel import; no tests
- `internal/api/v1/system/user_unlock_handler.go` — unlock; no tests
- `internal/services/system/user_service.go` — main CRUD; some coverage from existing tests
- `internal/services/system/user_cache_impl.go` — cache wrapper; no tests
- `internal/services/system/user_sync_service.go` — 797 lines; existing test partial

Output: comment in user_handler_test.go header with per-file baseline % and test target list. Do NOT modify existing test files (D-08).
  </action>
  <verify>
    <automated>go test -coverprofile=c.out -count=1 ./internal/services/system/ ./internal/api/v1/system/ 2>&1 | tail -5 && go tool cover -func=c.out | grep -E "user_" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Baseline per-file % captured for all user-related files
    - Identified which files need new tests vs need only additions
    - No existing test files modified
  </acceptance_criteria>
  <done>
Baseline established; gap analysis documented in test file header.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement handler tests for user CRUD + import + unlock</name>
  <files>internal/api/v1/system/user_handler_test.go
internal/api/v1/system/user_import_handler_test.go
internal/api/v1/system/user_unlock_handler_test.go</files>
  <read_first>
    - internal/api/v1/system/user_handler.go (full file)
    - internal/api/v1/system/user_import_handler.go (full file)
    - internal/api/v1/system/user_unlock_handler.go (full file)
    - internal/api/v1/system/user_router.go (constructor signatures)
    - internal/api/v1/system/ad_account_handler_test.go (D-01 pattern reference)
    - internal/services/system/user_service.go (UserService interface — already loaded)
  </read_first>
  <action>
Per D-01 lightweight handler pattern with REAL SM4 cipher + REAL UserService (not mocked):

Fixture helpers (shared in user_handler_test.go or new `helpers_user_test.go`):
- `setupUserTestDB(t *testing.T) *gorm.DB` — glebarez sqlite in-memory + CREATE TABLE `sys_user` matching production schema
- `setupUserTestHandler(t *testing.T, db *gorm.DB) (*UserHandler, *UserService, *noopCache)` — real UserServiceImpl(db, realSM4Cipher, noopCache)
- `setupRealSM4Cipher(t *testing.T)` — initialize SM4 cipher from env `SM4_KEY` or generated test key
- `setupJWTFactory(t *testing.T)` — generate JWT token for test user

For user_handler.go (CRUD: Create/Update/Delete/GetByID/List/Statistics/BatchDelete/UpdateStatus/ResetPassword):
- 2-3 test cases per method
- `TC1: <method>_success_returns_200_with_correct_user`
- `TC2: <method>_validation_error_returns_400`
- `TC3: <method>_not_found_returns_404` (where applicable)

For user_import_handler.go (Excel import via multipart upload):
- 2 test cases: TC1 valid Excel imports rows; TC2 invalid Excel returns 400
- Use `httptest.NewRequest` with multipart form + `bytes.NewReader([]byte{...})` for fake Excel bytes

For user_unlock_handler.go (unlock by username):
- 2 test cases: TC1 success; TC2 user not found

All tests use `httptest.NewRecorder()` + `gin.CreateTestContext()` with real router chain (auth middleware skipped — pass `gin.SetMode(gin.TestMode)` and a test router that mounts just the handler routes).

CRITICAL: Real SM4 cipher per D-03 — do NOT reuse mockSM4Cipher stub.
  </action>
  <verify>
    <automated>go test -count=1 -cover -run "TestUser" ./internal/api/v1/system/... 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All 3 test files compile
    - `go test -count=1 -cover -run "TestUser" ./internal/api/v1/system/...` exits 0
    - Real SM4 cipher used (no `mockSM4Cipher` import)
    - At least 20 test functions across the 3 files
    - Existing user_handler_test.go (helper file) preserved
  </acceptance_criteria>
  <done>
User handler tests complete (CRUD + import + unlock).
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement service tests for user service + cache impl + sync</name>
  <files>internal/services/system/user_service_test.go
internal/services/system/user_cache_impl_test.go
internal/services/system/user_sync_service_test.go</files>
  <read_first>
    - internal/services/system/user_service.go (full file)
    - internal/services/system/user_cache_impl.go (full file)
    - internal/services/system/user_sync_service.go (full file)
    - internal/services/portwrite/port_write_service_test.go (D-01 service pattern reference)
    - internal/services/system/user_sync_service_test.go (EXISTING — preserve)
  </read_first>
  <action>
For user_service.go — extend coverage beyond existing tests:
- Use glebarez sqlite in-memory + real UserServiceImpl
- Cover: Create / Update / Delete / GetByID / List (with filters) / Statistics / BatchDelete / UpdateStatus / ResetPassword
- For each, 2 test cases (success + error)

For user_cache_impl.go (cache wrapper for user_service methods):
- Use `mockCache` (function-field stub) for cache invalidation tests
- 5-8 test cases covering: cache miss → DB → cache populated; cache hit → DB not touched; cache.Set called after mutation; cache.Delete called after delete
- Compile-time assertion: `var _ CacheProvider = (*userCacheImpl)(nil)`

For user_sync_service.go (sync logic, 797 lines):
- Cover remaining methods not in existing `user_sync_service_test.go`
- Use mocks for any external API calls (LDAP/AD/3rd-party)
- Cover: classify user (already in user_sync_classify_test.go — preserve), build sync plan, execute sync, handle sync errors, mark synced

Do NOT modify existing test files (D-08).

All test files use D-01 service pattern:
- testify/mock.Mock embedded for mock types
- Interface assertions at top of file
- Table-driven TC1/TC2 naming
  </action>
  <verify>
    <automated>go test -count=1 -cover ./internal/services/system/ 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All 3 test files compile
    - `go test -count=1 -cover ./internal/services/system/` exits 0
    - Existing test files preserved (verify by checking git status shows no modifications to user_list_*, user_statistics_*, user_sync_*, widget_data_fetcher_*)
    - At least 25 new test functions added
  </acceptance_criteria>
  <done>
User service tests complete; coverage for user sub-module files ≥70%.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify per-sub-module user coverage ≥70% + write SUMMARY</name>
  <files>.planning/phases/72-p0-core-supplement/72-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/72-p0-core-supplement/72-CONTEXT.md
  </read_first>
  <action>
Verify per-sub-module coverage for user:
```
go test -coverprofile=c.out -count=1 ./internal/services/system/ ./internal/api/v1/system/
go tool cover -func=c.out | grep -E "(\buser_handler\.go|\buser_import_handler\.go|\buser_unlock_handler\.go|\buser_service\.go|\buser_cache_impl\.go|\buser_sync_service\.go)" 
```

Calculate weighted average across the 6 user-related files. Target ≥70.0%.

If < 70%:
- Inspect per-file uncovered lines
- Add more test cases (likely: edge cases like empty passwords, status transitions, multi-user batch ops)
- Re-run

Clean up `c.out`. Write `72-05-SUMMARY.md`:
- Per-file coverage breakdown for user sub-module
- Total test count
- Commit message draft: `test(72-05): CORE-04+06 user sub-module tests → ≥70%`

Do NOT commit yet.
  </action>
  <verify>
    <automated>go test -cover -count=1 ./internal/api/v1/system/ ./internal/services/system/ 2>&1 | grep -E "^(ok|FAIL|coverage:)"</automated>
  </verify>
  <acceptance_criteria>
    - Per-file user coverage: weighted average ≥70% across user_handler.go + user_import_handler.go + user_unlock_handler.go + user_service.go + user_cache_impl.go + user_sync_service.go
    - `72-05-SUMMARY.md` exists with per-file breakdown
  </acceptance_criteria>
  <done>
User sub-module per-sub-package ≥70%; SUMMARY documented.
  </done>
</task>

</tasks>

<verification>
- Per-sub-module user coverage: weighted average ≥70% across user_handler.go + user_import_handler.go + user_unlock_handler.go + user_service.go + user_cache_impl.go + user_sync_service.go
- All existing user test files preserved (D-08: 0 business code changes)
- Real SM4 cipher used in handler tests per D-03
</verification>

<success_criteria>
- [ ] CORE-04 user sub-module ≥70% per-sub-module
- [ ] CORE-06 user sub-module ≥70% per-sub-module
- [ ] All existing user_*_test.go files preserved
- [ ] Real SM4 cipher used (D-03)
- [ ] No new mock framework (D-06)
</success_criteria>

<output>
Create `.planning/phases/72-p0-core-supplement/72-05-SUMMARY.md` when done.
</output>
