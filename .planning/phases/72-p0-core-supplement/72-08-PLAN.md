---
phase: 72-p0-core-supplement
plan: 08
type: execute
wave: 3
depends_on: []
files_modified:
  - internal/api/v1/system/menu_handler_test.go
  - internal/api/v1/system/fix_menu_handler_test.go
  - internal/api/v1/system/department_handler_test.go
  - internal/services/system/menu_service_test.go
  - internal/services/system/menu_cache_impl_test.go
  - internal/services/system/department_service_test.go
  - internal/services/system/department_cache_impl_test.go
autonomous: true
requirements:
  - CORE-04
  - CORE-06
user_setup: []

must_haves:
  truths:
    - "Per-sub-package: menu_handler.go + fix_menu_handler.go + department_handler.go collectively have ≥70% coverage in api/v1/system"
    - "Per-sub-package: menu_service.go + menu_cache_impl.go + department_service.go + department_cache_impl.go collectively have ≥70% coverage in services/system"
    - "Menu tests cover tree-building (parent-child recursion) and visibility convention (1=visible per CLAUDE.md)"
    - "Department tests cover tree queries (recursive subtree) and user assignment"
  artifacts:
    - path: "internal/api/v1/system/menu_handler_test.go"
      provides: "Tests for menu CRUD handler"
      contains: "package system"
    - path: "internal/api/v1/system/fix_menu_handler_test.go"
      provides: "Tests for fix_menu handler (admin repair)"
      contains: "package system"
    - path: "internal/api/v1/system/department_handler_test.go"
      provides: "Tests for department CRUD handler"
      contains: "package system"
    - path: "internal/services/system/menu_service_test.go"
      provides: "Tests for menu service + tree building"
      contains: "package system"
    - path: "internal/services/system/menu_cache_impl_test.go"
      provides: "Tests for menu cache wrapper"
      contains: "package system"
    - path: "internal/services/system/department_service_test.go"
      provides: "Tests for department service + tree queries"
      contains: "package system"
    - path: "internal/services/system/department_cache_impl_test.go"
      provides: "Tests for department cache wrapper"
      contains: "package system"
  key_links:
    - from: "internal/api/v1/system/menu_*_test.go"
      to: "internal/services/system/menu_service.go"
      via: "real MenuService call"
      pattern: "menuService\\.(Create|Update|Delete|GetByID|GetTree|...)"
    - from: "internal/services/system/menu_service_test.go"
      to: "internal/models/sys_menu.go"
      via: "glebarez sqlite in-memory + tree DDL"
      pattern: "sys_menu.*parent_id"
---

<objective>
Add tests for `menu` + `dept` sub-modules in both api/v1/system (CORE-04) and services/system (CORE-06) bringing per-sub-module coverage to ≥70%.

Purpose: Menu (菜单管理) and Department (部门管理) are RBAC core — every request flow checks user permissions against these. menu_handler.go 378 + fix_menu_handler.go 109 = 487; department_handler.go 339. Service side: menu_service.go 603 + department_service.go 499.

Output: 3 new handler test files + 4 new service test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-p0-core-supplement/72-CONTEXT.md
@.planning/REQUIREMENTS.md

Read these source files first:
- internal/api/v1/system/ad_account_handler_test.go (D-01 lightweight pattern)
- internal/api/v1/system/menu_handler.go (378 lines)
- internal/api/v1/system/fix_menu_handler.go (109 lines)
- internal/api/v1/system/menu_router.go (constructor signatures)
- internal/api/v1/system/department_handler.go (339 lines)
- internal/api/v1/system/department_router.go (constructor signatures)
- internal/services/system/menu_service.go (603 lines)
- internal/services/system/menu_cache_impl.go (cache wrapper)
- internal/services/system/department_service.go (499 lines)
- internal/services/system/department_cache_impl.go (cache wrapper)
- internal/models/sys_menu.go (menu model — note visibility: 1=visible, 0=hidden per CLAUDE.md)
- internal/models/sys_dept.go (dept model — tree via parent_id)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map menu + dept handler/service methods, design fixtures</name>
  <files>internal/api/v1/system/menu_handler_test.go</files>
  <read_first>
    - internal/api/v1/system/menu_handler.go (full file)
    - internal/api/v1/system/fix_menu_handler.go (full file)
    - internal/api/v1/system/department_handler.go (full file)
    - internal/services/system/menu_service.go (full file)
    - internal/services/system/department_service.go (full file)
  </read_first>
  <action>
Read all 5 source files. Enumerate methods:

menu_handler.go: List / GetTree / GetByID / Create / Update / Delete / ChangeVisible / AssignRoles / GetMenuByRole
fix_menu_handler.go: FixMenuTree / RepairOrphanNodes / RebuildMenuCache
department_handler.go: List / GetTree / GetByID / Create / Update / Delete / GetUsers / AssignUsers

For each method, identify request/response shape.

Design shared helpers in menu_handler_test.go:
- `setupMenuTestDB(t *testing.T) *gorm.DB` — glebarez sqlite + CREATE TABLE `sys_menu` (id TEXT PK, parent_id TEXT, name TEXT, path TEXT, type INTEGER, visible INTEGER [1=show, 0=hide per CLAUDE.md], permission TEXT, created_at DATETIME) + CREATE TABLE `sys_role_menu` (role_id TEXT, menu_id TEXT, PK(role_id, menu_id)) + CREATE TABLE `sys_user_role`
- `setupDeptTestDB(t *testing.T) *gorm.DB` — glebarez sqlite + CREATE TABLE `sys_dept` (id TEXT PK, parent_id TEXT, dept_name TEXT, status INTEGER, sort INTEGER, created_at DATETIME) + CREATE TABLE `sys_user` (for AssignUsers)
- `setupRealSM4Cipher(t)` — per D-03
- `setupMenuHandler(t)` and `setupDeptHandler(t)` — real service impls

CRITICAL menu visibility: per CLAUDE.md `1=visible, 0=hidden` (boolean semantics). Use `models.VisibleShow = 1` / `models.VisibleHidden = 0` constants, NOT bare 0/1 literals.

Output: comment block with method enumeration + fixture design. Stub files compile.
  </action>
  <verify>
    <automated>go build ./internal/api/v1/system/... ./internal/services/system/... 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Comment block enumerates ≥20 handler methods total
    - Both test DBs create correct schema with proper parent_id FK pattern
    - Visibility uses `models.VisibleShow` / `models.VisibleHidden` constants (per CLAUDE.md)
    - Stub files compile
  </acceptance_criteria>
  <done>
Method enumeration + fixture design complete; stubs compile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement menu + dept handler tests</name>
  <files>internal/api/v1/system/menu_handler_test.go
internal/api/v1/system/fix_menu_handler_test.go
internal/api/v1/system/department_handler_test.go</files>
  <read_first>
    - internal/api/v1/system/menu_handler.go (full file)
    - internal/api/v1/system/fix_menu_handler.go (full file)
    - internal/api/v1/system/department_handler.go (full file)
    - internal/services/system/menu_service.go (full file)
    - internal/services/system/department_service.go (full file)
  </read_first>
  <action>
Per D-01 lightweight pattern with REAL services + glebarez sqlite:

For menu_handler.go:
- TC1/TC2 per method: success / error
- Special cases:
  - `GetTree`: seed 3-level parent-child chain (root → child → grandchild), assert tree structure
  - `ChangeVisible`: assert visibility toggles between `models.VisibleShow` (1) and `models.VisibleHidden` (0)
  - `AssignRoles`: seed sys_role_menu rows, assert correct role-menu bindings

For fix_menu_handler.go:
- TC1: `FixMenuTree` with seeded orphan node (parent_id points to nonexistent parent) — assert fix logic
- TC2: `RepairOrphanNodes` — assert orphans are detected and reassigned
- TC3: `RebuildMenuCache` — assert cache populated after rebuild

For department_handler.go:
- TC1/TC2 per method: success / error
- Special cases:
  - `GetTree`: seed 3-level dept tree, assert tree structure
  - `GetUsers`: seed users with dept_id, assert correct users returned
  - `AssignUsers`: assert user-dept binding updated

All tests use `httptest.NewRecorder()` + `gin.CreateTestContext()` + real services. Real SM4 cipher per D-03.
  </action>
  <verify>
    <automated>go test -count=1 -cover -run "TestMenu|TestFixMenu|TestDept|TestDepartment" ./internal/api/v1/system/... 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All 3 handler test files compile and pass
    - `go test -count=1 -cover -run "TestMenu|TestFixMenu|TestDept|TestDepartment" ./internal/api/v1/system/...` exits 0
    - At least 30 test functions across 3 files
    - Tree-building logic verified (parent-child recursion)
    - Visibility convention uses constants per CLAUDE.md
  </acceptance_criteria>
  <done>
Menu + dept handler tests complete.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement menu + dept service tests</name>
  <files>internal/services/system/menu_service_test.go
internal/services/system/menu_cache_impl_test.go
internal/services/system/department_service_test.go
internal/services/system/department_cache_impl_test.go</files>
  <read_first>
    - internal/services/system/menu_service.go (full file)
    - internal/services/system/menu_cache_impl.go (full file)
    - internal/services/system/department_service.go (full file)
    - internal/services/system/department_cache_impl.go (full file)
    - internal/services/portwrite/port_write_service_test.go (D-01 service pattern reference)
  </read_first>
  <action>
Per D-01 service pattern:

For menu_service.go (603 lines):
- Use glebarez sqlite in-memory + real MenuServiceImpl
- Cover all exported methods
- 2-3 test cases per method (success / error / edge cases)
- Tree building: seed 3-level chain, assert recursion produces correct tree
- Permission filtering: seed menus with different visibility, assert visible-only returned by default
- Cache invalidation: use mockCache (function-field stub) to verify cache.Delete/Set called after mutation

For menu_cache_impl.go:
- Compile-time interface assertion: `var _ CacheProvider = (*menuCacheImpl)(nil)`
- 5-8 test cases: cache miss → DB → cache populated; cache hit; cache.Set called after Create/Update/Delete; cache.Delete called after Delete

For department_service.go (499 lines):
- Use glebarez sqlite in-memory + real DepartmentServiceImpl
- Cover all exported methods
- Tree queries: seed 3-level chain, assert recursive subtree queries return correct descendants
- User assignment: seed users, assert AssignUsers correctly updates sys_user.dept_id

For department_cache_impl.go:
- Compile-time interface assertion
- 5-8 test cases: cache miss/hit/invalidation after mutations
  </action>
  <verify>
    <automated>go test -count=1 -cover -run "TestMenu|TestDept|TestDepartment" ./internal/services/system/... 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All 4 service test files compile and pass
    - `go test -count=1 -cover -run "TestMenu|TestDept|TestDepartment" ./internal/services/system/...` exits 0
    - At least 30 test functions across 4 files
    - Tree-building logic verified
    - Cache invalidation verified
  </acceptance_criteria>
  <done>
Menu + dept service tests complete.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify per-sub-module menu + dept coverage ≥70% + write SUMMARY</name>
  <files>.planning/phases/72-p0-core-supplement/72-08-SUMMARY.md</files>
  <read_first>
    - .planning/phases/72-p0-core-supplement/72-CONTEXT.md
  </read_first>
  <action>
Verify per-sub-module coverage for menu + dept:
```
go test -coverprofile=c.out -count=1 ./internal/api/v1/system/ ./internal/services/system/
go tool cover -func=c.out | grep -E "(\bmenu_handler\.go|\bfix_menu_handler\.go|\bdepartment_handler\.go|\bmenu_service\.go|\bmenu_cache_impl\.go|\bdepartment_service\.go|\bdepartment_cache_impl\.go)"
```

Calculate weighted average per sub-module:
- menu sub-module: menu_handler.go + fix_menu_handler.go + menu_service.go + menu_cache_impl.go ≥70%
- dept sub-module: department_handler.go + department_service.go + department_cache_impl.go ≥70%

If < 70%:
- Inspect uncovered branches via `go tool cover -html=c.out -o coverage.html`
- Add missing test cases
- Re-run

Clean up artifacts. Write `72-08-SUMMARY.md`:
- Per-file coverage breakdown for menu + dept
- Test count
- Commit message draft: `test(72-08): CORE-04+06 menu+dept sub-module tests → ≥70%`

Do NOT commit yet.
  </action>
  <verify>
    <automated>go test -cover -count=1 ./internal/api/v1/system/ ./internal/services/system/ 2>&1 | grep -E "^(ok|FAIL|coverage:)"</automated>
  </verify>
  <acceptance_criteria>
    - menu sub-module weighted avg ≥70% across menu_handler.go + fix_menu_handler.go + menu_service.go + menu_cache_impl.go
    - dept sub-module weighted avg ≥70% across department_handler.go + department_service.go + department_cache_impl.go
    - `72-08-SUMMARY.md` exists with breakdown
  </acceptance_criteria>
  <done>
Menu + dept sub-modules per-sub-package ≥70%; SUMMARY documented.
  </done>
</task>

</tasks>

<verification>
- Per-sub-module coverage: menu + dept each ≥70% weighted average
- Tree recursion verified via real glebarez sqlite (not mocks)
- Visibility convention uses constants per CLAUDE.md
- Real SM4 cipher used (D-03)
</verification>

<success_criteria>
- [ ] CORE-04 menu + dept sub-modules ≥70% per-sub-module
- [ ] CORE-06 menu + dept sub-modules ≥70% per-sub-module
- [ ] All exported menu + dept methods have ≥2 test cases
- [ ] Visibility uses `models.VisibleShow` / `models.VisibleHidden` constants
- [ ] No new mock framework (D-06)
</success_criteria>

<output>
Create `.planning/phases/72-p0-core-supplement/72-08-SUMMARY.md` when done.
</output>
