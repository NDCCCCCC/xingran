---
phase: 72-p0-core-supplement
plan: 11
type: execute
wave: 3
depends_on: []
files_modified:
  - internal/api/v1/system/settings_handler_test.go
  - internal/api/v1/system/apikey_handler_test.go
  - internal/api/v1/system/profile_handler_test.go
  - internal/api/v1/system/file_handler_test.go
  - internal/services/system/settings_service_test.go
  - internal/services/system/settings_cache_impl_test.go
  - internal/services/system/apikey_service_test.go
  - internal/services/system/profile_service_test.go
  - internal/services/system/file_service_test.go
autonomous: true
requirements:
  - CORE-04
  - CORE-06
user_setup: []

must_haves:
  truths:
    - "Per-sub-package: settings_handler.go + apikey_handler.go + profile_handler.go + file_handler.go each ≥70% coverage in api/v1/system"
    - "Per-sub-package: settings_service.go + settings_cache_impl.go + apikey_service.go + profile_service.go + file_service.go each ≥70% coverage in services/system"
    - "Existing apikey_service_test.go + settings_service_test.go preserved (do NOT modify)"
    - "Settings covers theme/layoutStore preference CRUD"
    - "APIKey covers key generation, signature validation, expiry, revocation"
    - "Profile covers current user info + password change"
    - "File covers upload + download + delete + list"
  artifacts:
    - path: "internal/api/v1/system/settings_handler_test.go"
      provides: "Tests for user settings CRUD handler"
      contains: "package system"
    - path: "internal/api/v1/system/apikey_handler_test.go"
      provides: "Tests for API key management handler"
      contains: "package system"
    - path: "internal/api/v1/system/profile_handler_test.go"
      provides: "Tests for user profile handler"
      contains: "package system"
    - path: "internal/api/v1/system/file_handler_test.go"
      provides: "Tests for file upload/download handler"
      contains: "package system"
    - path: "internal/services/system/settings_service_test.go"
      provides: "Tests for settings service (extends existing settings_service_test)"
      contains: "package system"
    - path: "internal/services/system/settings_cache_impl_test.go"
      provides: "Tests for settings cache wrapper"
      contains: "package system"
    - path: "internal/services/system/apikey_service_test.go"
      provides: "Tests for apikey service (extends existing apikey_service_test)"
      contains: "package system"
    - path: "internal/services/system/profile_service_test.go"
      provides: "Tests for profile service"
      contains: "package system"
    - path: "internal/services/system/file_service_test.go"
      provides: "Tests for file service"
      contains: "package system"
  key_links:
    - from: "internal/api/v1/system/settings_handler_test.go"
      to: "internal/services/system/settings_service.go"
      via: "real SettingsService call"
      pattern: "settingsService\\.(Get|Update|Reset)"
    - from: "internal/api/v1/system/apikey_handler_test.go"
      to: "internal/services/system/apikey_service.go"
      via: "real APIKeyService call"
      pattern: "apikeyService\\.(Generate|Revoke|Validate|List)"
---

<objective>
Add tests for `settings` + `apikey` + `profile` + `file` sub-modules in both api/v1/system (CORE-04) and services/system (CORE-06) bringing per-sub-module coverage to ≥70%.

Purpose: These 4 sub-modules are individually small but collectively significant:
- settings: 91 handler + 172 service = 263 lines (existing settings_service_test covers some)
- apikey: 336 handler + 629 service = 965 lines (existing apikey_service_test covers some)
- profile: 209 handler + 227 service = 436 lines
- file: 217 handler + 433 service = 650 lines

Output: 4 new handler test files + 5 new/extended service test files (9 total).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-p0-core-supplement/72-CONTEXT.md
@.planning/REQUIREMENTS.md

Read these source files first:
- internal/api/v1/system/ad_account_handler_test.go (D-01 lightweight pattern)
- internal/api/v1/system/settings_handler.go (91 lines; small)
- internal/api/v1/system/apikey_handler.go (336 lines)
- internal/api/v1/system/profile_handler.go (209 lines)
- internal/api/v1/system/file_handler.go (217 lines)
- internal/api/v1/system/*_router.go (constructor signatures for each)
- internal/services/system/settings_service.go (172 lines)
- internal/services/system/settings_cache_impl.go (cache wrapper)
- internal/services/system/settings_service_test.go (EXISTING — preserve)
- internal/services/system/apikey_service.go (629 lines)
- internal/services/system/apikey_service_test.go (EXISTING — preserve)
- internal/services/system/profile_service.go (227 lines)
- internal/services/system/file_service.go (433 lines)
- internal/models/sys_settings.go / sys_apikey.go / sys_file.go (table schemas)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map 4 sub-module methods + design fixtures</name>
  <files>internal/api/v1/system/settings_handler_test.go</files>
  <read_first>
    - internal/api/v1/system/settings_handler.go (full file)
    - internal/api/v1/system/apikey_handler.go (full file)
    - internal/api/v1/system/profile_handler.go (full file)
    - internal/api/v1/system/file_handler.go (full file)
    - internal/services/system/settings_service.go (full file)
    - internal/services/system/apikey_service.go (full file)
    - internal/services/system/profile_service.go (full file)
    - internal/services/system/file_service.go (full file)
    - internal/services/system/settings_service_test.go (EXISTING — preserve)
    - internal/services/system/apikey_service_test.go (EXISTING — preserve)
  </read_first>
  <action>
Read all 8 source files. Enumerate methods per sub-module:

settings_handler.go: GetSettings / UpdateSettings / ResetSettings
apikey_handler.go: List / Create / Generate / Revoke / Update / Delete / GetByID / Validate
profile_handler.go: GetProfile / UpdateProfile / UpdatePassword / UploadAvatar / GetPermissions
file_handler.go: Upload / Download / Delete / List / GetByID / Preview

For each method, identify request/response shape.

Design shared helpers in settings_handler_test.go (since settings is smallest):
- `setupSettingsTestDB(t)` — glebarez sqlite + CREATE TABLE `sys_user_settings` (id TEXT PK, user_id TEXT, theme TEXT, layout TEXT, density TEXT, lang TEXT, created_at DATETIME, UNIQUE(user_id))
- `setupApikeyTestDB(t)` — glebarez sqlite + CREATE TABLE `sys_apikey` (id TEXT PK, name TEXT, key_id TEXT UNIQUE, key_secret_hash TEXT, status INTEGER, expires_at DATETIME, created_at DATETIME)
- `setupProfileTestDB(t)` — glebarez sqlite + CREATE TABLE `sys_user` (reuse from user sub-module schema) + CREATE TABLE `sys_file` for avatar
- `setupFileTestDB(t)` — glebarez sqlite + CREATE TABLE `sys_file` (id TEXT PK, user_id TEXT, original_name TEXT, storage_path TEXT, size INTEGER, mime_type TEXT, status INTEGER, created_at DATETIME)
- `setupRealSM4Cipher(t)` — per D-03
- 4 setupHandler functions for the 4 sub-modules

CRITICAL: APIKey status uses `0=active, 1=revoked` per CLAUDE.md convention. Use constants.

Output: comment block with method enumeration + fixture design. Stub files compile.
  </action>
  <verify>
    <automated>go build ./internal/api/v1/system/... ./internal/services/system/... 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Comment block enumerates ≥25 handler methods total across 4 sub-modules
    - All 4 test DBs create correct schema
    - Stub files compile
  </acceptance_criteria>
  <done>
Method enumeration + fixture design complete; stubs compile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement settings + apikey handler tests</name>
  <files>internal/api/v1/system/settings_handler_test.go
internal/api/v1/system/apikey_handler_test.go</files>
  <read_first>
    - internal/api/v1/system/settings_handler.go (full file)
    - internal/api/v1/system/apikey_handler.go (full file)
    - internal/services/system/settings_service.go (full file)
    - internal/services/system/apikey_service.go (full file)
    - pkg/response/response.go
  </read_first>
  <action>
Per D-01 lightweight pattern with REAL services + glebarez sqlite:

For settings_handler.go (3 methods, 91 lines):
- TC1/TC2 per method: success / error
- `UpdateSettings`: assert all preference fields persist
- `ResetSettings`: assert defaults restored

For apikey_handler.go (8 methods, 336 lines):
- TC1/TC2 per method: success / error
- Special cases:
  - `Create` / `Generate`: assert key_id + key_secret returned in response, secret hashed in DB
  - `Validate`: seed apikey with valid signature, assert validate succeeds; seed with expired, assert fails
  - `Revoke`: assert status transitions from active (0) to revoked (1) using constants

All tests use `httptest.NewRecorder()` + `gin.CreateTestContext()` + real services. Real SM4 cipher per D-03.
  </action>
  <verify>
    <automated>go test -count=1 -cover -run "TestSettings|TestApikey|TestAPIKey" ./internal/api/v1/system/... 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Both handler test files compile and pass
    - `go test -count=1 -cover -run "TestSettings|TestApikey|TestAPIKey" ./internal/api/v1/system/...` exits 0
    - At least 20 test functions across 2 files
    - APIKey signature validation + revocation paths covered
  </acceptance_criteria>
  <done>
Settings + apikey handler tests complete.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement profile + file handler tests</name>
  <files>internal/api/v1/system/profile_handler_test.go
internal/api/v1/system/file_handler_test.go</files>
  <read_first>
    - internal/api/v1/system/profile_handler.go (full file)
    - internal/api/v1/system/file_handler.go (full file)
    - internal/services/system/profile_service.go (full file)
    - internal/services/system/file_service.go (full file)
  </read_first>
  <action>
Per D-01 lightweight pattern with REAL services + glebarez sqlite:

For profile_handler.go (5 methods, 209 lines):
- TC1/TC2 per method: success / error
- Special cases:
  - `GetProfile`: seed user, assert correct profile returned (uses context user_id)
  - `UpdateProfile`: assert partial updates (only changed fields)
  - `UpdatePassword`: assert password hashed via PasswordManager, old password verified
  - `UploadAvatar`: mock multipart upload, assert file record created and avatar_url updated
  - `GetPermissions`: seed role-menu bindings, assert user's permissions returned

For file_handler.go (6 methods, 217 lines):
- TC1/TC2 per method: success / error
- Special cases:
  - `Upload`: multipart upload, assert file metadata persisted, storage path returned
  - `Download`: seed file row, assert response streams file content
  - `Delete`: assert file row soft-deleted (deleted_at set)
  - `Preview`: assert preview thumbnail URL returned (may be no-op for non-image files)
  - `List`: pagination + filter by mime_type/user_id

All tests use `httptest.NewRecorder()` + `gin.CreateTestContext()` + real services. Real SM4 cipher per D-03.
  </action>
  <verify>
    <automated>go test -count=1 -cover -run "TestProfile|TestFile" ./internal/api/v1/system/... 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Both handler test files compile and pass
    - `go test -count=1 -cover -run "TestProfile|TestFile" ./internal/api/v1/system/...` exits 0
    - At least 20 test functions across 2 files
    - File upload multipart form tested
    - Password hashing verified (use real or stub PasswordManager)
  </acceptance_criteria>
  <done>
Profile + file handler tests complete.
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement settings + apikey + profile + file service tests (preserve existing tests)</name>
  <files>internal/services/system/settings_service_test.go
internal/services/system/settings_cache_impl_test.go
internal/services/system/apikey_service_test.go
internal/services/system/profile_service_test.go
internal/services/system/file_service_test.go</files>
  <read_first>
    - internal/services/system/settings_service.go (full file)
    - internal/services/system/settings_cache_impl.go (full file)
    - internal/services/system/apikey_service.go (full file)
    - internal/services/system/profile_service.go (full file)
    - internal/services/system/file_service.go (full file)
    - internal/services/system/settings_service_test.go (EXISTING — preserve)
    - internal/services/system/apikey_service_test.go (EXISTING — preserve)
    - internal/services/portwrite/port_write_service_test.go (D-01 service pattern reference)
  </read_first>
  <action>
Per D-01 service pattern:

For settings_service.go (172 lines):
- Cover methods not in existing settings_service_test.go
- 2-3 test cases per method

For settings_cache_impl.go:
- Compile-time interface assertion: `var _ CacheProvider = (*settingsCacheImpl)(nil)`
- 5-8 test cases: cache miss/hit/invalidation

For apikey_service.go (629 lines):
- Cover methods not in existing apikey_service_test.go
- 2-3 test cases per method
- Hash verification: assert key_secret_hash matches PasswordManager.HashPassword

For profile_service.go (227 lines):
- Cover all exported methods
- 2-3 test cases per method
- Password change: verify old password check + new password hash

For file_service.go (433 lines):
- Cover all exported methods
- 2-3 test cases per method
- Storage path generation: assert path format
- File existence check: use os.CreateTemp + cleanup

CRITICAL: Do NOT modify existing settings_service_test.go or apikey_service_test.go (D-08).
  </action>
  <verify>
    <automated>go test -count=1 -cover -run "TestSettings|TestApikey|TestAPIKey|TestProfile|TestFile" ./internal/services/system/... 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All 5 service test files compile and pass
    - `go test -count=1 -cover -run "TestSettings|TestApikey|TestAPIKey|TestProfile|TestFile" ./internal/services/system/...` exits 0
    - At least 30 test functions across 5 files
    - Existing settings_service_test.go + apikey_service_test.go preserved
  </acceptance_criteria>
  <done>
Settings + apikey + profile + file service tests complete.
  </done>
</task>

<task type="auto">
  <name>Task 5: Verify per-sub-module coverage ≥70% for all 4 sub-modules + write SUMMARY</name>
  <files>.planning/phases/72-p0-core-supplement/72-11-SUMMARY.md</files>
  <read_first>
    - .planning/phases/72-p0-core-supplement/72-CONTEXT.md
  </read_first>
  <action>
Verify per-sub-module coverage for all 4 sub-modules:
```
go test -coverprofile=c.out -count=1 ./internal/api/v1/system/ ./internal/services/system/
go tool cover -func=c.out | grep -E "(\bsettings_handler\.go|\bapikey_handler\.go|\bprofile_handler\.go|\bfile_handler\.go|\bsettings_service\.go|\bsettings_cache_impl\.go|\bapikey_service\.go|\bprofile_service\.go|\bfile_service\.go)"
```

Calculate weighted average per sub-module:
- settings sub-module: settings_handler.go + settings_service.go + settings_cache_impl.go ≥70%
- apikey sub-module: apikey_handler.go + apikey_service.go ≥70%
- profile sub-module: profile_handler.go + profile_service.go ≥70%
- file sub-module: file_handler.go + file_service.go ≥70%

If any sub-module < 70%:
- Inspect uncovered branches via `go tool cover -html=c.out -o coverage.html`
- Add missing test cases
- Re-run

Clean up artifacts. Write `72-11-SUMMARY.md`:
- Per-file coverage breakdown for 4 sub-modules
- Test count
- Commit message draft: `test(72-11): CORE-04+06 settings+apikey+profile+file sub-module tests → ≥70%`

Do NOT commit yet.
  </action>
  <verify>
    <automated>go test -cover -count=1 ./internal/api/v1/system/ ./internal/services/system/ 2>&1 | grep -E "^(ok|FAIL|coverage:)"</automated>
  </verify>
  <acceptance_criteria>
    - settings sub-module weighted avg ≥70%
    - apikey sub-module weighted avg ≥70%
    - profile sub-module weighted avg ≥70%
    - file sub-module weighted avg ≥70%
    - `72-11-SUMMARY.md` exists with breakdown
  </acceptance_criteria>
  <done>
All 4 small sub-modules per-sub-package ≥70%; SUMMARY documented.
  </done>
</task>

</tasks>

<verification>
- Per-sub-module coverage: settings + apikey + profile + file each ≥70% weighted average
- Existing settings_service_test.go + apikey_service_test.go preserved (D-08)
- File upload tested with multipart form
- Real SM4 cipher used (D-03)
</verification>

<success_criteria>
- [ ] CORE-04 settings + apikey + profile + file sub-modules ≥70% per-sub-module
- [ ] CORE-06 settings + apikey + profile + file sub-modules ≥70% per-sub-module
- [ ] All exported methods on the 4 sub-modules have ≥2 test cases
- [ ] No new mock framework (D-06)
</success_criteria>

<output>
Create `.planning/phases/72-p0-core-supplement/72-11-SUMMARY.md` when done.
</output>
