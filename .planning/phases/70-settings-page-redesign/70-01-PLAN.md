---
phase: 70-settings-page-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/v1/system/default_theme_handler.go        # 删除（既有未提交）
  - internal/services/system/default_theme_service.go       # 删除（既有未提交）
  - internal/services/system/internal/api/v1/system/default_theme_handler.go # 删除（误置嵌套副本）
  - xingran-react-frontend/src/lib/defaultThemeApi.ts      # 删除（既有未提交）
  - xingran-react-frontend/src/pages/system/settings/default-theme.tsx # 删除（既有未提交）
  - internal/api/v1/system/settings_router.go               # 修改（既有未提交）
  - internal/services/system/settings_service.go            # 修改（既有未提交）
  - internal/services/system/settings_cache_impl.go         # 修改（既有未提交）
  - xingran-react-frontend/src/pages/system/settings-page/index.tsx # 修改（既有未提交）
  - xingran-react-frontend/src/pages/system/settings/index.tsx      # 修改（既有未提交）
  - xingran-react-frontend/src/pages/settings/index.tsx             # 修改（既有未提交）
autonomous: false
requirements: [D-10]
must_haves:
  truths:
    - "D-10 落地：工作区既有的 default-theme 清理改动（13 文件，+42/-717）作为 Phase 70 首个执行任务原子提交入库，asset_columns_schema.json 时间戳再生与 .planning/ 规划产物等搭车噪音被排除在提交之外"
    - "提交前 go build ./... 与前端 tsc --noEmit 双绿复核通过（研究期已验证，执行时复跑确认）"
    - "提交后 git status 中 src/ 与 internal/ 下业务代码零未提交残留（.planning/、.planning/sketch/ 归档文件除外）"
  artifacts:
    - path: internal/api/v1/system/default_theme_handler.go
      provides: "文件已删除（default-theme handler 清零）"
    - path: internal/services/system/internal/api/v1/system/default_theme_handler.go
      provides: "误置嵌套副本目录已删除"
    - path: xingran-react-frontend/src/pages/system/settings/default-theme.tsx
      provides: "默认主题 Tab 页已删除"
  key_links:
    - from: "git HEAD（D-10 原子提交）"
      to: "70-RESEARCH.md「D-10 未提交改动清单」13 文件"
      via: "git show --stat HEAD"
      pattern: "default_theme|defaultThemeApi|default-theme"
---

<objective>
吸收工作区已完成的 default-theme 清理改动，作为 Phase 70 的首个原子提交（D-10）。

Purpose: 工作区当前是脏的（default-theme 入口删除的前后端 -717 行改动未提交）。这批改动正是 phase 目标「清理多主题残留」的一部分，必须先原子提交落库，后续任何重构改动才能在干净基线上开展——尤其 `settings-page/index.tsx`、`settings/index.tsx`（barrel）、`pages/settings/index.tsx` 三个文件既在本提交清单内、又是后续 plan 70-05/70-06 的重写对象。
Output: 一个原子 commit（仅含 13 个清单文件），工作区业务代码归零。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-settings-page-redesign/70-CONTEXT.md
@.planning/phases/70-settings-page-redesign/70-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 复核 D-10 清单、双绿验证并精确 stage</name>
  <files>（无文件修改——git stage 操作）</files>
  <read_first>
  - .planning/phases/70-settings-page-redesign/70-RESEARCH.md（「D-10 未提交改动清单」节：13 文件表 + 两个搭车噪音说明）
  - git status --porcelain 与 git diff --stat 的实时输出（以执行时实测为准，研究期清单为核对基准）
  </read_first>
  <action>
  1. 运行 `git status --porcelain` 与 `git diff --stat`，与 RESEARCH「D-10 未提交改动清单」逐项核对：5 个删除（default_theme_handler.go / default_theme_service.go / 误置嵌套副本 default_theme_handler.go / defaultThemeApi.ts / default-theme.tsx）+ 8 个修改（settings_router.go / settings_service.go / settings_cache_impl.go / settings-page/index.tsx / settings barrel index.tsx / pages/settings/index.tsx 等）。执行时清单以 git 实测为准，语义等价（default-theme 清理相关）即纳入。
  2. 排除以下搭车噪音，不得 stage：`internal/services/system/asset_columns_schema.json`（仅 __generated__ 时间戳再生）、`.planning/` 全部内容（ROADMAP/STATE/phase 目录/sketch webp）、`users-list-resp.network-response`、`.planning/config.json`（若再现）。
  3. 复跑双绿验证：`go build ./...` 与 `cd xingran-react-frontend && npx tsc --noEmit -p tsconfig.app.json`，两者必须 exit 0（研究期已验证通过，此处复核未被后续会话破坏）。
  4. 用 `git add` 精确加入清单文件（删除文件用 `git add` 或 `git rm --cached` 均 git add -A 于指定路径），生成 staged 集合。
  </action>
  <verify>
    <automated>git diff --cached --stat | tail -3 显示 13 files changed（±清单口径），且 git diff --cached --name-only | grep -c -E "asset_columns_schema|\.planning|network-response" == 0，且 go build ./... exit 0，且 cd xingran-react-frontend && npx tsc --noEmit -p tsconfig.app.json exit 0</automated>
  </verify>
  <acceptance_criteria>
  - `git diff --cached --name-only` 输出的文件集合与 RESEARCH D-10 清单一致（13 文件，允许执行时等价口径），噪音文件零出现
  - `go build ./...` 零输出通过；`npx tsc --noEmit -p tsconfig.app.json` 零输出通过
  - staged diff 合计约 +42/-717（±少量，若工作区在研究期后有额外等价清理）
  </acceptance_criteria>
  <done>清单文件全部 staged，双绿验证通过，噪音文件确认排除</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 用户过目 D-10 提交范围</name>
  <what-built>default-theme 清理改动的原子提交范围（staged 13 文件 + 双绿验证结果）。依据 CLAUDE.md git 纪律（提交前显式确认）与 70-RESEARCH「建议 planner 设 checkpoint 让用户过目提交范围」。</what-built>
  <how-to-verify>
  1. 向用户展示 `git diff --cached --stat` 全文与 `git diff --cached --name-only` 清单
  2. 展示 `go build ./...` 与 `npx tsc --noEmit -p tsconfig.app.json` 的通过输出
  3. 说明排除项：asset_columns_schema.json（时间戳再生）、.planning/ 规划产物、network-response 调试文件
  4. 拟用 commit message：`chore(70-01): absorb default-theme cleanup as atomic phase-entry commit (D-10)`
  </how-to-verify>
  <resume-signal>回复 "approved" 提交，或指出需调整的文件</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 执行原子提交并验证工作区干净度</name>
  <files>（无文件修改——git commit 操作）</files>
  <read_first>
  - Task 1/2 的 staged 状态与用户批准结果
  </read_first>
  <action>
  1. `git commit -m "chore(70-01): absorb default-theme cleanup as atomic phase-entry commit (D-10)"`（按用户在 Task 2 批准的最终 message）。
  2. 提交后运行 `git status --porcelain`，确认 `xingran-react-frontend/src/` 与 `internal/` 下无未提交业务文件残留；`.planning/` 未跟踪内容不影响后续 plan（属规划产物，随各 plan 提交）。
  3. `git show --stat HEAD | head -20` 记录提交概要到 SUMMARY。
  </action>
  <verify>
    <automated>git show --stat HEAD --name-only | grep -c -E "default_theme|defaultThemeApi|default-theme" >= 5 且 git status --porcelain -- xingran-react-frontend/src internal | wc -l == 0</automated>
  </verify>
  <acceptance_criteria>
  - HEAD 提交包含全部 default-theme 相关删除/修改文件
  - `git status --porcelain -- xingran-react-frontend/src internal` 输出为空（业务工作区干净）
  - 提交是 Phase 70 的首个执行提交（其后才是 70-02 起的重构提交）
  </acceptance_criteria>
  <done>D-10 原子提交入库，业务工作区干净，后续 plan 可在干净基线上开工</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新增边界 | 本 plan 仅提交既有已验证改动（后端 handler/service 删除 + 前端入口清理），不引入新的输入面或数据流 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-0101 | Tampering | D-10 提交范围 | mitigate | 提交前用户 checkpoint 过目 staged 清单；噪音/规划产物文件排除（accept：删除的 default-theme 端点原本就无消费方，研究期 grep 已验证） |
| T-70-SC | Tampering | npm/pip/cargo installs | accept | 本 plan 零包安装（纯 git 操作），legitimacy gate N/A |
</threat_model>

<verification>
- `go build ./...` 通过（提交前后各一次）
- `npx tsc --noEmit -p tsconfig.app.json` 通过
- `git show --stat HEAD` 与 RESEARCH D-10 清单一致
- `git status --porcelain -- xingran-react-frontend/src internal` 为空
</verification>

<success_criteria>
- D-10 原子提交完成且是本 phase 首个执行提交
- 双绿验证通过，噪音文件未混入
- 工作区业务代码干净，70-02+ 可开工
</success_criteria>

<output>
Create `.planning/phases/70-settings-page-redesign/70-01-SUMMARY.md` when done
</output>
