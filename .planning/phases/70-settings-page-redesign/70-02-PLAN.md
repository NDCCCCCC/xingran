---
phase: 70-settings-page-redesign
plan: 02
type: execute
wave: 2
depends_on: ["70-01"]
files_modified:
  - xingran-react-frontend/src/index.css
  - xingran-react-frontend/src/design-system/components/SettingsShell.tsx
  - xingran-react-frontend/src/pages/system/settings/__tests__/SettingsShell.test.tsx
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05]
must_haves:
  truths:
    - "D-01 落地：SettingsShell 提供左侧分类导航白卡 + 右侧内容白卡的布局骨架（桌面 ≥lg：Sider 220px 竖排分类 + Content），衬奶油画布形成双层纸感"
    - "D-02 落地：SettingsCategory.maxWidth 契约就位——表格/网格类分类缺省撑满容器，表单类分类由调用方传 maxWidth（760px）限宽"
    - "D-03 落地：激活分类由 URL ?cat= 参数驱动（useSearchParams 唯一真相源），非法/缺失值回退 defaultCat，切换用 replace:true 不污染 history 栈、不产生新 RouteTab"
    - "D-04 落地：<lg 断点（Grid.useBreakpoint）时左导航降级为顶部 Segmented block 控件、内容区全宽；≥lg 保持左右结构"
    - "D-05 落地：SettingsShell 为两页共用的单一骨架组件（归属 design-system/components/，与 DensitySwitcher 同级），系统设置与用户设置后续 plan 各自实例化，不合并菜单入口"
  artifacts:
    - path: xingran-react-frontend/src/design-system/components/SettingsShell.tsx
      provides: "共用设置页骨架（分类注册表驱动 + URL 参数 + 断点降级）"
      exports: ["SettingsShell", "SettingsCategory", "SettingsShellProps"]
      min_lines: 70
    - path: xingran-react-frontend/src/index.css
      provides: "Phase 70 全部新样式类（.xr-settings-* / .xr-setting-row / .xr-appearance-* / .xr-captcha-*），后续 plan 只消费不新增"
      contains: "xr-settings-nav-item"
    - path: xingran-react-frontend/src/pages/system/settings/__tests__/SettingsShell.test.tsx
      provides: "D-03/D-04 Wave 0 单测（cat 驱动/非法回退/replace 语义/断点降级）"
      min_lines: 90
  key_links:
    - from: xingran-react-frontend/src/design-system/components/SettingsShell.tsx
      to: URL ?cat= 参数
      via: "react-router-dom useSearchParams + setSearchParams({cat}, {replace:true})"
      pattern: "useSearchParams|replace: true"
    - from: xingran-react-frontend/src/design-system/components/SettingsShell.tsx
      to: xingran-react-frontend/src/index.css
      via: "className 消费 .xr-settings-nav / .xr-settings-nav-item / .xr-settings-content"
      pattern: "xr-settings-(nav|nav-item|content)"
    - from: xingran-react-frontend/src/pages/system/settings/__tests__/SettingsShell.test.tsx
      to: xingran-react-frontend/src/design-system/components/SettingsShell.tsx
      via: "vi.mock antd Grid.useBreakpoint 驱动 Sider/Segmented 分支断言"
      pattern: "useBreakpoint|Segmented|aria-current"
---

<objective>
建立 Phase 70 的两个共用契约层：(1) index.css 的全部 `.xr-*` 新类段（样式契约，后续 plan 只引用类名不再改 CSS）；(2) SettingsShell 共用骨架组件 + Wave 0 单测（组件契约，D-01/D-03/D-04/D-05 的载体）。

Purpose: 接口优先——样式类与骨架组件先于三个分类子页与用户设置页落地，后续 4 个 plan 在零 CSS 冲突、零接口摸索的前提下并行施工。
Output: index.css 新段、SettingsShell.tsx、SettingsShell.test.tsx。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-settings-page-redesign/70-PATTERNS.md
@.planning/phases/70-settings-page-redesign/70-UI-SPEC.md

<interfaces>
<!-- 本 plan 创建的契约，下游 70-03~70-07 直接消费 -->

SettingsShell.tsx 将导出：
```typescript
export interface SettingsCategory {
  key: string;          // = URL ?cat= 参数值
  label: string;
  icon: ReactNode;
  content: ReactNode;
  maxWidth?: number;    // 表单类限宽（760）；表格/网格类缺省撑满（D-02）
}
export interface SettingsShellProps {
  categories: SettingsCategory[];  // 调用方模块级常量（CLAUDE.md useEffect/引用稳定纪律）
  defaultCat: string;
  paramName?: string;   // 缺省 "cat"
}
export function SettingsShell(props: SettingsShellProps): JSX.Element
```

index.css 将提供（后续 plan 仅在 TSX 里写 className，不再改 CSS）：
.xr-settings-sider / .xr-settings-nav / .xr-settings-nav-item(-active) /
.xr-settings-content / .xr-setting-row(-label/-desc) /
.xr-appearance-cards / .xr-appearance-card(-selected) / .xr-appearance-preview-light / .xr-appearance-preview-dark /
.xr-captcha-grid / .xr-captcha-card（含图片区与卡脚）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: index.css 追加 Phase 70 样式契约段</name>
  <files>xingran-react-frontend/src/index.css</files>
  <read_first>
  - xingran-react-frontend/src/index.css:5099-5270（v16 段：.stat-card 白卡样板 :5106-5114、.xr-dept-panel 透明 Sider 覆写 :5243-5251、selected 品牌绿语汇 :5259-5266；新段插入位置 = .xr-dept-panel 段之后、分页器段之前）
  - xingran-react-frontend/src/index.css:41（--theme-brand-alpha-10）、:62/:68/:69（--sidebar-bg #14532d / --sidebar-text-active #e0e0b0 / --sidebar-accent #c09058）、:107-113（--theme-spacing-*）
  - .planning/phases/70-settings-page-redesign/70-UI-SPEC.md（L-1/L-4/IC-2 规格 + Spacing Scale 表）
  </read_first>
  <action>
  在 v16 段之后追加带节注释的新段（如 `/* ===== Phase 70: Settings 双层纸感骨架与分类页 ===== */`），实现以下类，全部只引用 var(--theme-*) / var(--sidebar-*)，禁止手写 hex（QA-02 lint 门 check-hardcoded-colors.mjs 拦截）：
  1. `.xr-settings-sider`：`background: transparent !important`（防全局侧栏深绿污染，参照 .dept-list-sider :5249-5251 写法）。
  2. `.xr-settings-nav`：白卡双层纸感语汇（同 .stat-card :5106-5114）——`background: var(--theme-bg-surface); border: 1px solid var(--theme-border-primary); border-radius: var(--theme-radius-xl); padding: 12px`。
  3. `.xr-settings-nav-item`：button 元素样式（width:100%; text-align:left; border:none; background:transparent; cursor:pointer; display:flex; align-items:center; gap:8px; padding:8px 12px; border-radius: var(--theme-radius-md); color: var(--theme-text-primary); font-size:14px）；图标色 var(--theme-text-secondary)（用 `.xr-settings-nav-item .anticon` 选择器）；hover 背景 var(--theme-primary-50)；active 态（选择器 `[aria-current="true"]`，类名 .xr-settings-nav-item-active 同步保留）：`background: var(--theme-brand-alpha-10); color: var(--theme-primary); font-weight: 600`（checker 提示：底色引用令牌而非手写 rgba）。
  4. `.xr-settings-content`：白卡同语汇 + `padding: var(--theme-spacing-xl)`（20px）。
  5. `.xr-setting-row` / `.xr-setting-row-label` / `.xr-setting-row-desc`：行式设置项（flex; justify-content:space-between; align-items:center; padding:16px 0）；行分割用 `.xr-setting-row + .xr-setting-row { border-top: 1px solid var(--theme-border-primary) }`（首行自然无分割线）；label 14px/400 主文字；desc `font-size:12px; color: var(--theme-text-secondary); margin-top:2px`。
  6. `.xr-appearance-cards`：`display:flex; gap:12px`。`.xr-appearance-card`：`width:160px; background: var(--theme-bg-surface); border:1px solid var(--theme-border-primary); border-radius: var(--theme-radius-lg); cursor:pointer; text-align:center`；标签行 `padding-top:8px; font-size:12px`。预览区基类高 90px、圆角 var(--theme-radius-lg)、overflow:hidden，内部用 flex 布局模拟迷你界面：`.xr-appearance-preview-light` = 底 var(--theme-bg-surface) + 左侧 28px 宽侧栏块 var(--sidebar-bg) + 右侧两条 4px 高文本条 var(--theme-border-primary)；`.xr-appearance-preview-dark` = 底 var(--sidebar-bg) + 文本条 var(--sidebar-text-active)（注意：PATTERNS 事实 5(b) 已裁定取 --sidebar-text-active #e0e0b0，不取铜金 --sidebar-accent——UI-SPEC IC-2 注释笔误）。`.xr-appearance-card-selected`：`border:2px solid var(--theme-primary); box-shadow: 0 0 0 3px var(--theme-brand-alpha-15, rgba(21,96,49,0.15))`——若 QA-02 拦截该 rgba 写法，改用与 .xr-dept-panel selected 同款的令牌写法规避（优先找既有 alpha 令牌，无则复用 var(--theme-brand-alpha-10)）；选中卡标签 font-weight:600 + color: var(--theme-primary)。
  7. `.xr-captcha-grid`：`display:grid; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); gap:12px`（对齐 .stat-cards :5100-5105 auto-fill 思路）。`.xr-captcha-card`：白卡语汇、**不带左色条**（图片即视觉主体，UI-SPEC L-3 裁定）；图片区 `aspect-ratio:4/3; object-fit:cover; border-radius`（上圆角下直角或内缩均可，令牌取值）；卡脚行：文件名 ellipsis + mono 12.5px（var(--theme-font-mono)）、两行操作区 padding 8-12px。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npm run lint && grep -c "xr-settings-nav-item\|xr-setting-row\|xr-appearance-card\|xr-captcha-grid" src/index.css >= 4 且 sed -n "/Phase 70: Settings/,/^\/\* =/p" src/index.css | grep -v "^\/\*" | grep -c "#[0-9a-fA-F]\{3,8\}" == 0</automated>
  </verify>
  <acceptance_criteria>
  - src/index.css 存在节注释标记的 Phase 70 新段，包含全部 12 个类名（.xr-settings-sider/-nav/-nav-item/-content、.xr-setting-row 及 label/desc、.xr-appearance-cards/-card/-preview-light/-preview-dark/-selected、.xr-captcha-grid/-card）
  - 新段内除节注释外零手写 hex 色值（awk 起始标记后扫描为 0 命中）
  - `npm run lint` 通过（含 check-hardcoded-colors.mjs）
  - active 态底色引用 var(--theme-brand-alpha-10)，非手写 rgba(21,96,49,0.1)
  </acceptance_criteria>
  <done>Phase 70 样式契约段落位，后续 plan 仅消费类名</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SettingsShell 共用骨架组件</name>
  <files>xingran-react-frontend/src/design-system/components/SettingsShell.tsx</files>
  <read_first>
  - xingran-react-frontend/src/pages/system/user/index.tsx:49,528,554-565（const { Sider, Content } = Layout 解构、透明背景 Layout/Sider 分栏先例、不加 PageTitle 的既有注释惯例）
  - xingran-react-frontend/src/pages/network/mac/index.tsx:46-52（Grid.useBreakpoint + useSearchParams 组合先例）
  - xingran-react-frontend/src/design-system/components/DensitySwitcher.tsx（design-system/components 目录惯例 + Segmented 消费先例）
  - .planning/phases/70-settings-page-redesign/70-UI-SPEC.md L-1（结构规格全文）
  </read_first>
  <behavior>
  - 纯渲染组件：接收 categories/defaultCat/paramName，无内部 useEffect（规避 CLAUDE.md 依赖稳定性风险）
  - activeCat = categories 中存在 key 匹配的原始参数值，否则 defaultCat（非法值回退）
  - setCat 使用 setSearchParams({ [paramName]: key }, { replace: true })
  - isDesktop = !!Grid.useBreakpoint().lg
  </behavior>
  <action>
  在 `src/design-system/components/SettingsShell.tsx` 实现（接口签名见 context 的 interfaces 块，逐字一致）：
  1. `useSearchParams` 取参数（paramName 缺省 "cat"）；`const raw = searchParams.get(paramName)`；`const activeCat = categories.some(c => c.key === raw) ? raw! : defaultCat`；`const setCat = (key: string) => setSearchParams({ [paramName]: key }, { replace: true })`。
  2. 桌面分支（isDesktop）：`Layout`（display:flex, alignItems:stretch, background:transparent）+ `Sider width={220} className="xr-settings-sider"` + `Content`。Sider 内 `<nav className="xr-settings-nav">` 渲染分类项；Content 内 `<div className="xr-settings-content">`，当激活分类有 maxWidth 时内层再包 `style={{ maxWidth: c.maxWidth }}` 容器（D-02 混合宽度）。注意：user 页 Sider 源码 width={250} 是部门树先例，本组件取 220（UI-SPEC L-1 裁定，勿照抄 250/300）。
  3. 分类项为 button 元素：`<button type="button" className={active ? "xr-settings-nav-item xr-settings-nav-item-active" : "xr-settings-nav-item"} aria-current={active ? "true" : undefined} onClick={() => setCat(c.key)}>` 内渲染 icon + label span（可达性契约，UI-SPEC L-1）。
  4. 窄屏分支（!isDesktop）：`<Segmented block value={activeCat} onChange={(v) => setCat(v as string)} options={categories.map(c => ({ label: c.label, value: c.key, icon: c.icon }))} />` + 内容区全宽（同一 .xr-settings-content 容器，无 maxWidth 包裹时同样处理）。
  5. 组件不拥有分类注册表——注册表由页面入口以模块级常量传入（保证 categories.test.ts 可对页面注册表做纯数据断言，且引用稳定）。
  6. Tab key = location.pathname（src/components/layout/shared/useRouteTabs.ts:97-99），searchParams 变更不产生新 RouteTab——无需为本组件做任何 tabsStore 适配（Pitfall 3 已验证，写进组件头注释）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npm run type-check && grep -c "export function SettingsShell\|export interface SettingsCategory" src/design-system/components/SettingsShell.tsx == 2</automated>
  </verify>
  <acceptance_criteria>
  - SettingsShell.tsx 导出 SettingsShell / SettingsCategory / SettingsShellProps 三者，type-check 通过
  - 源码含 `replace: true`、`Grid.useBreakpoint`、`width={220}`、`aria-current`，不含 Antd `Tabs`（页面级分类切换禁用 Tabs）
  - 组件内零 useEffect、零 sessionStorage/localStorage 读写（D-03：URL 为唯一真相源）
  </acceptance_criteria>
  <done>SettingsShell 组件就位，签名与 interfaces 契约一致</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SettingsShell Wave 0 单测</name>
  <files>xingran-react-frontend/src/pages/system/settings/__tests__/SettingsShell.test.tsx</files>
  <read_first>
  - xingran-react-frontend/src/pages/network/ports/__tests__/index.test.tsx（页面级 vitest 标准形态：ResizeObserver stub :21-29、MemoryRouter+antd App Wrapper :155-161、vi.mock 直通 selector :33-37、importActual 部分替换 :145-151）
  - xingran-react-frontend/src/design-system/components/SettingsShell.tsx（Task 2 产物）
  - .planning/phases/70-settings-page-redesign/70-VALIDATION.md（Per-Task Verification Map 的 D-03/D-04 行）
  </read_first>
  <behavior>
  - 用例 1（cat 驱动）：initialEntries 含 ?cat=api 时渲染 api 分类的内容标记
  - 用例 2（非法回退）：?cat=invalid 回退 defaultCat 对应内容，不抛错
  - 用例 3（replace 语义）：点击分类项后 URL 变为目标 cat 且不产生新 history 条目（断言 setSearchParams 调用参数含 replace:true，或 location.search 变化 + history.length 不变）
  - 用例 4（断点降级）：mockBreakpoint {lg:true} 时存在 .xr-settings-nav 且无 .ant-segmented；{lg:false} 时反之
  - 用例 5（可达性）：激活分类项带 aria-current="true"
  </behavior>
  <action>
  按 ports 测试三件套搭建：ResizeObserver polyfill（antd v6 依赖，jsdom 缺失）、`Wrapper = MemoryRouter + antd App`、测试专用 categories 夹具（3 项，每项 content 渲染可断言文本如 "content-email"）。断点 mock 用 `vi.mock("antd", async (importOriginal) => { const actual = await importOriginal(); return { ...actual, Grid: { ...actual.Grid, useBreakpoint: () => mockBreakpoint } } })`，用例内切换 `mockBreakpoint = { lg: true } / { lg: false }` 驱动 Sider/Segmented 分支。MemoryRouter `initialEntries={["/t?cat=api"]}` 注入初始 cat。五个用例按 behavior 块编写；测试文件名必须是 SettingsShell.test.tsx（不以 index/detail 命名，不受 componentLoader glob 影响）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npx vitest run src/pages/system/settings/__tests__/SettingsShell.test.tsx && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
  - `npx vitest run src/pages/system/settings/__tests__/SettingsShell.test.tsx` 全绿（≥5 用例：cat 驱动 / 非法回退 / replace 语义 / 断点双向 / aria-current）
  - 测试通过 `vi.mock` 的 Grid.useBreakpoint 控制，不依赖真实窗口宽度
  - type-check 通过
  </acceptance_criteria>
  <done>D-03/D-04 的 Wave 0 测试闭环，SettingsShell 行为被测试锁定</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL ?cat= → 组件状态 | searchParams 为不可信输入（用户可任意改 URL） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-0201 | Tampering | ?cat= 参数注入 | mitigate | activeCat 经 categories.some(key 匹配) 白名单校验，非法值回退 defaultCat（用例 2 锁定）；参数仅用于本地渲染选择，不进 SQL/路径 |
| T-70-SC | Tampering | npm installs | accept | 零新增依赖（antd/react-router-dom 既有），legitimacy gate N/A |
</threat_model>

<verification>
- `npm run lint`（Task 1 后）+ `npm run type-check`（Task 2/3 后）
- `npx vitest run src/pages/system/settings/__tests__/SettingsShell.test.tsx` 全绿
- Wave 2 汇合门：`npm run build && npm run lint && npm run test -- run`（与 70-03 同 wave，由后完成者执行）
</verification>

<success_criteria>
- 12 个新 CSS 类全部就位且零手写 hex（lint 门通过）
- SettingsShell 三导出齐全，桌面/窄屏双分支 + URL 驱动激活态
- ≥5 个单测锁定 D-03/D-04 行为
</success_criteria>

<output>
Create `.planning/phases/70-settings-page-redesign/70-02-SUMMARY.md` when done
</output>
