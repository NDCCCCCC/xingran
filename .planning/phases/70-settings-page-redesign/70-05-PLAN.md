---
phase: 70-settings-page-redesign
plan: 05
type: execute
wave: 3
depends_on: ["70-02"]
files_modified:
  - xingran-react-frontend/src/pages/settings/index.tsx
  - xingran-react-frontend/src/pages/settings/__tests__/index.test.tsx
autonomous: true
requirements: [D-02, D-05, D-06]
must_haves:
  truths:
    - "D-06 落地：用户设置页采用行式设置项——每行 label + 描述 + 右对齐控件（密度 Select 宽 160 / 侧栏折叠 Switch / 分页大小 Select 宽 160），界面/布局/数据三分组白卡包裹；明暗模式用分段卡片选择器（浅色/深色预览块，role=radio + aria-checked，非 Radio.Button）"
    - "D-05 落地（用户设置实例）：用户设置页与系统设置页共用同一 SettingsShell 组件——左导航 = 界面(appearance,默认)/布局(layout)/数据(data) 三分类，右内容限宽 760px（D-02 表单类限宽）"
    - "保存交互按 IC-1 即改即存裁定：每行控件 onChange 直接调用 settingsStore.updateTheme / updateLayout / updateDataPageSize + message.success(已保存)，移除底部「保存/重置」按钮与 Form 批量提交结构；失败 message.error(保存设置失败，请重试) 且受控控件天然回滚（updatePreferences 先服务端后本地提交）"
  artifacts:
    - path: xingran-react-frontend/src/pages/settings/index.tsx
      provides: "用户设置页（SettingsShell 实例 + 行式设置项 + 即改即存）"
      exports: ["default（页面组件）", "userSettingsCategories"]
      min_lines: 140
    - path: xingran-react-frontend/src/pages/settings/__tests__/index.test.tsx
      provides: "D-06 Wave 0 单测（行式控件 onChange → store action）"
      min_lines: 70
  key_links:
    - from: xingran-react-frontend/src/pages/settings/index.tsx
      to: xingran-react-frontend/src/store/settingsStore.ts
      via: "updateTheme({mode}) / updateLayout(...) / updateDataPageSize(v) 单字段即改即存"
      pattern: "updateTheme|updateLayout|updateDataPageSize"
    - from: xingran-react-frontend/src/pages/settings/index.tsx
      to: xingran-react-frontend/src/design-system/components/SettingsShell.tsx
      via: "SettingsShell categories={userSettingsCategories} defaultCat=\"appearance\""
      pattern: "SettingsShell"
---

<objective>
把用户设置页从「单 Card 垂直 Form」重构为与系统设置同构的 SettingsShell 实例：三分组行式设置项 + 明暗分段卡片选择器 + 即改即存（IC-1），并以单测锁定行式控件与 store action 的接线（D-06）。

Purpose: D-05/D-06 的用户设置侧落地；两页同构后设置类页面语汇统一。
Output: pages/settings/index.tsx 重写 + __tests__/index.test.tsx。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-settings-page-redesign/70-PATTERNS.md
@.planning/phases/70-settings-page-redesign/70-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 用户设置页行式化 + 即改即存 + 分段卡片选择器</name>
  <files>xingran-react-frontend/src/pages/settings/index.tsx</files>
  <read_first>
  - xingran-react-frontend/src/pages/settings/index.tsx（全文；保留段 :25-36 initialize 懒加载 effect、:91-95 密度选项值、:117-122 分页选项值；删除段 :39-53 handleSave/handleReset、:62-131 Form/Divider/Radio.Button/保存重置按钮）
  - xingran-react-frontend/src/store/settingsStore.ts:94-157（updateTheme/updateLayout/updateDataPageSize 单字段 actions + updatePreferences 先服务端后本地的失败回滚事实）——注意 :207-214 updateLayout 是浅合并
  - xingran-react-frontend/src/design-system/components/DensitySwitcher.tsx:19-38（模块级选项常量结构先例）
  - xingran-react-frontend/src/design-system/components/SettingsShell.tsx + src/index.css 的 .xr-setting-row / .xr-appearance-*（70-02 交付的契约）
  - .planning/phases/70-settings-page-redesign/70-UI-SPEC.md（L-4 行清单 + IC-1 + IC-2 全文）
  </read_first>
  <action>
  保持默认导出与目录名不变（`src/pages/settings/`，D-11 裁定），主体重写：
  1. **模块级注册表** `export const userSettingsCategories: SettingsCategory[]`：appearance（BgColorsOutlined「界面设置」，默认）/ layout（LayoutOutlined「布局设置」）/ data（DatabaseOutlined「数据设置」），三类均 `maxWidth: 760`（D-02 表单限宽）。页面 default export 渲染 `<SettingsShell categories={userSettingsCategories} defaultCat="appearance" />`。
  2. **三分组卡片**（Antd Card，title 16px/600：界面设置/布局设置/数据设置），组内设置行用 `.xr-setting-row`（label + `.xr-setting-row-desc` + 右对齐控件）：
     - 明暗模式｜选择界面的颜色模式，深色模式适合夜间使用｜分段卡片选择器（下述）
     - 密度模式｜控制列表与表单的紧凑程度｜Select width 160，options 紧凑=compact/舒适=comfortable/宽松=spacious（原值，THEME-03 不回归）
     - 默认折叠侧边栏｜折叠侧边栏以获得更大的内容区域｜Switch
     - 默认分页大小｜列表页面默认每页显示的数据条数｜Select width 160，options 10/20/50/100（条/页，原值）
  3. **明暗分段卡片选择器（IC-2）**：`.xr-appearance-cards` 容器 + 两张 `.xr-appearance-card`（button 元素，`role="radio"` + `aria-checked`，容器 onKeyDown 处理 ←/→ 键盘切换）；浅色卡预览 `.xr-appearance-preview-light`、深色卡 `.xr-appearance-preview-dark`（CSS 契约已含）；选中卡加 `.xr-appearance-card-selected`。点击 → `handleUpdate(() => updateTheme({ mode: "light" | "dark" }))`。**不使用 Radio.Button**（D-06 明确）。
  4. **即改即存（IC-1）**：统一包装 `const handleUpdate = async (fn: () => Promise<void>) => { try { await fn(); message.success("已保存"); } catch { message.error("保存设置失败，请重试"); } }`；各行 onChange 直接调 store 单字段 action。**浅合并陷阱**：`updateLayout` 对 layout 是浅合并——侧栏折叠必须传完整 sidebar 对象：`updateLayout({ sidebar: { ...preferences.layout.sidebar, collapsed: v } })`（从当前 preferences 展开，否则 width/collapsedWidth 丢失）；密度传 `updateLayout({ density: v })`；分页传 `updateDataPageSize(v)`。受控控件值全部取自 store preferences（失败时服务端未提交、本地 state 未变 → 控件天然回滚，无需额外回滚代码）。
  5. **保留 initialize 懒加载**：`useEffect(() => { if (!initialized) useSettingsStore.getState().initialize(); }, [initialized])` 原样（依赖为原始布尔值，稳定）。
  6. **删除**：Form 实例与批量提交、handleSave/handleReset、底部「保存设置/重置」按钮、Divider、Radio.Button 明暗切换（即改即存后「上次保存」即当前态，重置失去语义——IC-1 裁定）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npm run type-check && grep -c "SettingsShell\|userSettingsCategories" src/pages/settings/index.tsx >= 2 且 grep -c "updateTheme\|updateLayout\|updateDataPageSize" src/pages/settings/index.tsx >= 3 且 grep -c "Radio.Button\|handleReset" src/pages/settings/index.tsx == 0 且 grep -c "sidebar: { \\.\\.\\." src/pages/settings/index.tsx >= 1</automated>
  </verify>
  <acceptance_criteria>
  - 页面为 SettingsShell 实例，userSettingsCategories 导出且三类均含 maxWidth: 760
  - 密度选项值为 compact/comfortable/spacious、分页选项 10/20/50/100（与 settingsStore 权威字段一致）
  - Radio.Button / handleReset / Form 批量提交零命中；即改即存三 action 全部接线
  - sidebar 折叠调用带完整 sidebar 展开对象（浅合并防护）
  - 分段卡片为 button + role="radio" + aria-checked，含键盘 ←/→ 切换
  - type-check 通过
  </acceptance_criteria>
  <done>用户设置页与系统设置同构，行式即改即存落位</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 行式设置项 → store action 接线单测</name>
  <files>xingran-react-frontend/src/pages/settings/__tests__/index.test.tsx</files>
  <read_first>
  - xingran-react-frontend/src/pages/system/settings/__tests__/SettingsShell.test.tsx（70-02 三件套范式）
  - xingran-react-frontend/src/pages/settings/index.tsx（Task 1 产物）
  - xingran-react-frontend/src/store/settingsStore.ts（被 mock 的 store 面与 preferences 结构）
  - .planning/phases/70-settings-page-redesign/70-VALIDATION.md（Per-Task Map 的 D-06 行）
  </read_first>
  <behavior>
  - 用例 1：mock preferences.layout.density = "comfortable" → 切换密度 Select 到 "compact" → spy 断言 updateLayout 被调且参数含 { density: "compact" }
  - 用例 2：切换侧栏折叠 Switch → updateLayout 参数含完整 sidebar 对象（含既有 width/collapsedWidth 字段）且 collapsed = 新值
  - 用例 3：切换分页大小 → updateDataPageSize(50)（数字参数）
  - 用例 4：点击深色模式卡 → updateTheme({ mode: "dark" })；aria-checked 落在选中卡
  </behavior>
  <action>
  按 SettingsShell.test.tsx 三件套搭建（ResizeObserver + MemoryRouter + antd App）。`vi.mock("@/store/settingsStore", ...)` 直通可控 preferences（含 layout.sidebar 完整字段）+ vi.fn spies（updateTheme/updateLayout/updateDataPageSize）+ initialized: true。渲染页面（含 mock Grid.useBreakpoint 固定 lg 桌面分支，或窄屏 Segmented 分支二选一，桌面优先）后用 userEvent/fireEvent 触发行式控件，按 behavior 断言 spy 参数。文件名 index.test.tsx（非 index.tsx，不受 componentLoader glob 影响）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npx vitest run src/pages/settings/__tests__/index.test.tsx && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
  - vitest 该文件全绿（≥4 用例：密度 / 侧栏完整对象 / 分页数字 / 明暗模式卡）
  - sidebar 断言验证了浅合并防护（参数含 width 等既有字段）
  - type-check 通过
  </acceptance_criteria>
  <done>D-06 行式控件与 store 接线被单测锁定</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 行式控件 → PUT /system/settings/preferences | 偏好写入走既有 configService（鉴权与 SM2+SM4 传输不变）；本 plan 不改请求契约 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-70-0501 | Tampering | 偏好写入频率 | accept | 即改即存为低频用户操作（IC-1 已评估无频率风险）；失败路径有 message.error + 受控回滚，不产生静默丢失 |
| T-70-0502 | DoS（自我） | updateLayout 浅合并误用 | mitigate | 完整 sidebar 展开对象为显式任务约束 + 单测用例 2 锁定（错写会丢 width/collapsedWidth 并被测试捕获） |
| T-70-SC | Tampering | npm installs | accept | 零新增依赖，legitimacy gate N/A |
</threat_model>

<verification>
- `npx vitest run src/pages/settings/__tests__/index.test.tsx` 全绿
- `npm run type-check && npm run lint`
- grep 门：Radio.Button / handleReset 零命中，三 action 接线齐全
- Wave 3 汇合门：`npm run build && npm run lint && npm run test -- run`
</verification>

<success_criteria>
- 用户设置页 = SettingsShell 用户实例（3 分类 × 760px 限宽）
- 4 行设置项 + 分段卡片选择器全部即改即存接线
- Form 批量提交结构清零，density/pageSize 选项原值保留（THEME-03 不回归）
</success_criteria>

<output>
Create `.planning/phases/70-settings-page-redesign/70-05-SUMMARY.md` when done
</output>
