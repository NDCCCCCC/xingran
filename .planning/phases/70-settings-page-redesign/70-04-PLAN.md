---
phase: 70-settings-page-redesign
plan: 04
type: execute
wave: 3
depends_on: ["70-02"]
files_modified:
  - xingran-react-frontend/src/pages/system/settings/captcha-background.tsx
  - xingran-react-frontend/src/pages/system/settings/__tests__/captcha-background.test.tsx
autonomous: true
requirements: [D-08, D-09, D-12]
must_haves:
  truths:
    - "D-08 落地：验证码背景图页改造为图片网格墙——.xr-captcha-grid 网格 + .xr-captcha-card 缩略图卡片（Image 预览 + 上传/启停/编辑/删除卡脚操作）+ 4 张统计卡（总数量/启用/禁用/总使用次数）+ 紧凑筛选工具栏，卡片语汇与 v16 统计卡一致，不再使用 Table 列表"
    - "D-12 落地（本 plan 分量）：captcha 页死状态 previewVisible/_setPreviewImage 与隐藏 Image 元素随重构消除；统计数值色 var(--theme-success, #3f8600) / var(--theme-error, #cf1322) 旧 fallback 写法改为纯 var(--theme-success) 等令牌引用；形状/难度/启用 Tag 迁移为 xr-tag 家族"
    - "captcha status 语义反转锁定：1=启用 / 0=禁用（后端契约例外，统计卡计数与启停按钮文案按此取数，统计卡与网格墙徽标两处一致，不按全局 0=启用 惯例「纠正」）"
  artifacts:
    - path: xingran-react-frontend/src/pages/system/settings/captcha-background.tsx
      provides: "验证码背景图图片网格墙分类页"
      min_lines: 300
    - path: xingran-react-frontend/src/pages/system/settings/__tests__/captcha-background.test.tsx
      provides: "status 反转语义 + 启停操作文案单测"
      min_lines: 60
  key_links:
    - from: xingran-react-frontend/src/pages/system/settings/captcha-background.tsx
      to: "@/services/captcha captchaService"
      via: "list / getCaptchaBackgroundStatistics / upload / update / delete / toggleStatus / preloadCache 既有调用保持不变"
      pattern: "getCaptchaBackgroundStatistics|toggleStatus|preloadCache"
    - from: xingran-react-frontend/src/pages/system/settings/captcha-background.tsx
      to: xingran-react-frontend/src/index.css
      via: "className 消费 .xr-captcha-grid / .xr-captcha-card（70-02 已交付）"
      pattern: "xr-captcha-(grid|card)"
    - from: xingran-react-frontend/src/pages/system/settings/captcha-background.tsx
      to: Image 预览
      via: "Antd Image preview={{ src: record.previewUrl }}（现页 :259-267 同 API，不自建灯箱）"
      pattern: "previewUrl"
---

<objective>
把验证码背景图页从「统计卡 + 搜索 + Table」改造为图片网格墙（D-08）：4 张统计卡 + 紧凑筛选工具栏 + 缩略图卡片网格；同步消除该页死状态与旧 fallback 色写法（D-12 分量），并锁定 captcha status 1=启用 的反转语义。

Purpose: 图片资产管理语义用网格墙比表格更贴切；status 反转是本 phase 最易错点，用单测锁死。
Output: captcha-background.tsx 网格墙版 + captcha-background.test.tsx。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-settings-page-redesign/70-PATTERNS.md
@.planning/phases/70-settings-page-redesign/70-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: captcha-background.tsx 改造为图片网格墙</name>
  <files>xingran-react-frontend/src/pages/system/settings/captcha-background.tsx</files>
  <read_first>
  - xingran-react-frontend/src/pages/system/settings/captcha-background.tsx（全文；保留段 :110-117 统计端点、:236-250 上传约束、:259-267 Image 预览、:311/:331/:624 status 语义、:206-213/:486-488 预加载缓存；删除段 :74-75 死状态、:364-407 旧统计卡、Table 与 :638-650 隐藏 Image）
  - xingran-react-frontend/src/pages/system/user/index.tsx:531-552（stat-cards 范本）
  - xingran-react-frontend/src/index.css 的 .xr-captcha-grid / .xr-captcha-card（70-02 交付的样式契约）
  - .planning/phases/70-settings-page-redesign/70-UI-SPEC.md（L-3 全文 + Copywriting 表）
  </read_first>
  <action>
  保持默认导出与文件名不变，主体重写为「统计卡行 + 工具栏卡 + 网格墙」：
  1. **统计卡 4 张**（数据源 `captchaService.getCaptchaBackgroundStatistics()`，返回 {totalCount, enabledCount, disabledCount, totalUsage?}）：「总数量」（默认绿条）/「启用数量」（sc-green）/「禁用数量」（sc-gray）/「总使用次数」（sc-gold，铜金第 1 处）。**D-12 第 5 项同点修复**：数值色改纯 `var(--theme-success)` / `var(--theme-error)` 等令牌引用（删除 `#3f8600` / `#cf1322` fallback）。
  2. **工具栏卡**（紧凑形态）：文件名搜索 Input + 拼图形状 Select + 难度 Select + 状态 Select（allowClear；注意选项 label 按 1=启用/0=禁用 标注）+ 右侧按钮组：「预加载缓存」（default，保留现状功能）+「上传背景图」（`type="primary" icon={<UploadOutlined />}`）。分页保留（`usePagination` + list API 契约不变）。
  3. **网格墙**：容器 `div.xr-captcha-grid`；每项 `.xr-captcha-card`——图片区 `aspect-ratio 4/3` + `<Image src={record.previewUrl} preview={{ src: record.previewUrl }} style={{ objectFit: "cover" }} />`（现页同 API，不自建灯箱）；卡脚行 1 = 文件名（ellipsis，mono 12.5px）+ 状态徽标（`status === 1` → `xr-tag-green`「启用」；否则 `xr-tag`「禁用」）；卡脚行 2 = `.xr-row-ops` 文字链接——启停（**文案按当前 status 取反**：status=1 显示「禁用」、status=0 显示「启用」）+「编辑」+「删除」（`.xr-op-danger`）。
  4. **形状/难度 Tag 迁移（D-12 第 6 项）**：形状 → `xr-tag` 中性；难度 → `xr-tag-gold`（卡内出现处随卡片信息区保留，若原 Table 列的形状/难度信息在卡片上无空间呈现，归入编辑 Modal 或卡脚 title 提示——以不丢信息为准，planner 裁量落入卡脚行 1 的 title 属性）。
  5. **空状态**：跨整行空卡（`grid-column: 1 / -1` 白卡）：PictureOutlined 48px `var(--theme-text-secondary)` + 标题「暂无背景图」（16px/600）+ 正文「上传第一张验证码背景图，用于登录页拼图验证码」+「上传背景图」primary 按钮（触发与工具栏相同的上传 Modal）。
  6. **删除确认升级**：`Modal.confirm({ title: "删除背景图？", content: "删除后不可恢复，启用中的拼图验证码将不再使用该背景。", okText: "删除", okButtonProps: { danger: true }, cancelText: "取消" })`。启停切换与邮箱测试发送不弹确认（维持现状）。
  7. **上传/编辑 Modal 原样保留（D-09）**：仅图片 + <2MB + maxCount 1 + beforeUpload return false 手动上传约束不变。
  8. **删除死状态（D-12 第 4 项）**：`previewVisible` / `_setPreviewImage`（:74-75）与隐藏 Image 元素（:638-650）整体不搬运；旧 Row/Col/Statistic 统计卡与 Table 结构全部替换。
  9. 错误文案「加载背景图列表失败，请刷新重试」。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npm run type-check && npm run lint && grep -c "xr-captcha-grid\|xr-captcha-card" src/pages/system/settings/captcha-background.tsx >= 2 且 grep -c "#3f8600\|#cf1322\|previewVisible" src/pages/system/settings/captcha-background.tsx == 0 且 grep -c "getCaptchaBackgroundStatistics" src/pages/system/settings/captcha-background.tsx >= 1</automated>
  </verify>
  <acceptance_criteria>
  - 组件消费 .xr-captcha-grid / .xr-captcha-card（70-02 契约），无 Table 列表结构
  - `#3f8600` / `#cf1322` / previewVisible 在本文件零命中（死状态与 fallback 清零）
  - status === 1 →「启用」徽标分支存在；启停操作文案为取反动作
  - 4 张统计卡数据来自 getCaptchaBackgroundStatistics；上传约束（<2MB / maxCount 1）原样
  - type-check + lint 通过
  </acceptance_criteria>
  <done>网格墙落位，status 反转语义在统计卡与卡脚徽标两处一致</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 网格墙 status 反转语义单测</name>
  <files>xingran-react-frontend/src/pages/system/settings/__tests__/captcha-background.test.tsx</files>
  <read_first>
  - xingran-react-frontend/src/pages/system/settings/__tests__/SettingsShell.test.tsx（70-02 交付的三件套范式：ResizeObserver stub / MemoryRouter+App Wrapper / vi.mock）
  - xingran-react-frontend/src/pages/system/settings/captcha-background.tsx（Task 1 产物）
  - xingran-react-frontend/src/services/captcha（被 mock 的 API 面：list/getCaptchaBackgroundStatistics 返回结构）
  - .planning/phases/70-settings-page-redesign/70-VALIDATION.md（Per-Task Map 的 D-08 行）
  </read_first>
  <behavior>
  - 用例 1：list 返回 status=1 的 fixture → 卡片渲染「启用」徽标（xr-tag-green 语义文本）
  - 用例 2：list 返回 status=0 的 fixture → 渲染「禁用」徽标
  - 用例 3：status=1 的卡片操作行提供「禁用」动作（取反），status=0 提供「启用」动作
  - 用例 4：statistics fixture（totalCount/enabledCount/disabledCount/totalUsage）渲染 4 张统计卡数值
  </behavior>
  <action>
  按 SettingsShell.test.tsx 同款三件套搭建（ResizeObserver polyfill + MemoryRouter + antd App Wrapper）。`vi.mock("@/services/captcha", ...)` 直通可控 fixture：list 返回含 status 1 与 0 各一条的记录（带 previewUrl），getCaptchaBackgroundStatistics 返回固定数值。渲染页面后按 behavior 断言（文本断言「启用」/「禁用」徽标与操作链接；统计数值断言）。文件名 captcha-background.test.tsx（非 index/detail，不受 componentLoader glob 影响）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npx vitest run src/pages/system/settings/__tests__/captcha-background.test.tsx && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
  - vitest 该文件全绿（≥4 用例：启用徽标 / 禁用徽标 / 取反操作 / 统计卡数值）
  - 断言基于 status 原始值（1/0）→ 显示文本映射，锁定反转语义（IC-4）
  - type-check 通过
  </acceptance_criteria>
  <done>captcha 1=启用 反转语义被单测锁定（本 phase 最易错点闭环）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| previewUrl → Image src | 图片 URL 来自后端返回（可信域内）；上传文件经既有约束校验 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-70-0401 | Tampering | 上传文件 | mitigate | 既有上传约束逐字保留：仅 image/* + <2MB + maxCount 1 + beforeUpload return false 手动上传（accept 上传约束不因重构放松） |
| T-70-0402 | Repudiation | 删除/启停 | mitigate | 删除走具名确认弹窗（danger 按钮）；启停维持后端 operlog 留痕 |
| T-70-SC | Tampering | npm installs | accept | 零新增依赖（Antd Image 内置预览，不自建灯箱），legitimacy gate N/A |
</threat_model>

<verification>
- `npx vitest run src/pages/system/settings/__tests__/captcha-background.test.tsx` 全绿
- `npm run type-check && npm run lint`
- grep 门：fallback 色与死状态零命中（见 Task 1 acceptance）
- Wave 3 汇合门：`npm run build && npm run lint && npm run test -- run`
</verification>

<success_criteria>
- 网格墙三段式（统计 4 卡 + 紧凑工具栏 + 卡片网格）落位
- status 1=启用 在统计卡与徽标两处一致并被单测锁定
- 死状态 / fallback 色 / Table 结构清零，上传与 Modal 契约不变
</success_criteria>

<output>
Create `.planning/phases/70-settings-page-redesign/70-04-SUMMARY.md` when done
</output>
