---
phase: 102-mechanical-constants
plan: 02
type: execute
wave: 2
depends_on: ["102-01"]
files_modified:
  - internal/services/system/cache_keys.go
  - internal/services/system/cache_keys_102_test.go
  - pkg/constants/cache.go
  - pkg/constants/cache_102_test.go
autonomous: true
requirements: [CACHE-02]
user_setup: []

must_haves:
  truths:
    - "CACHE-02 全部 10 模块的键前缀/plain 键完成注册：8 模块入 cache_keys.go、根包 2 格式入 pkg/constants/cache.go，常量值逐字符等于原内联字面量"
    - "失效 pattern 全部由前缀常量 + \":*\" 派生 helper 产出，未新增任何独立 pattern 常量（D-102-4）"
    - "等价快照测试期望列 = 原字面量快照，含参数化 helper 的完整输出断言（如 GetDutyMonthlyKey(2026, 9) == \"duty:monthly:2026:9\"），防 helper 拼接漂移"
  artifacts:
    - path: "internal/services/system/cache_keys.go"
      provides: "notice/settings/duty/workorder/knowledge/network/widget/rpa 8 模块常量 + GetXxxKey/GetXxxPattern helpers（前缀常量 + helper 形态，D-102-1/D-102-3）"
      contains: "CacheKeyRpaSelectorBest"
    - path: "internal/services/system/cache_keys_102_test.go"
      provides: "TestCacheKeyEquivalence 快照表 + helper 输出断言（期望列 = 原字面量）"
      contains: "TestCacheKeyEquivalence"
    - path: "pkg/constants/cache.go"
      provides: "UserEndpointsKeyFormat / MacVendorKeyFormat 根包 2 格式（import 环豁免落点，D-102-2 同机理）"
      contains: "user_endpoints:%s"
    - path: "pkg/constants/cache_102_test.go"
      provides: "TestRootServiceCacheKeyEquivalence（Plan 01 文件追加）"
      contains: "TestRootServiceCacheKeyEquivalence"
  key_links:
    - from: "internal/services/system/cache_keys.go"
      to: "RESEARCH §CACHE-02 清单字面量"
      via: "常量值 == 原字面量逐字符快照"
      pattern: "CacheKey\\w+ = \"\\w+:"
    - from: "internal/services/system/cache_keys_102_test.go"
      to: "internal/services/system/cache_keys.go"
      via: "快照断言 + GetXxxKey 完整输出断言（期望列为字面量）"
      pattern: "duty:monthly:2026:9"
---

<objective>
CACHE-02 第一步：将 10 模块全部键格式注册进两个宿主并交付等价快照测试（守护①）。本 plan 只注册、不替换调用点（调用点替换是 Plan 102-03）。

Purpose: 先立真相源与守护，Plan 03 的机械替换就有唯一目标态可对照；接口先行消除执行器探索成本。
Output: cache_keys.go 8 模块注册表 + pkg/constants 根包 2 格式 + 两个等价快照测试。

## D-102-1 落点二分修订（唯一必要字面修订，向用户显式披露）

- **修订内容**: D-102-1 字面为「业务模块键全集中注册进 cache_keys.go」；实际 **8 模块照 D-102-1 进 cache_keys.go**，但 services 根包 2 文件（api_endpoint_service.go、mac_history_query_service.go）的键**改落 pkg/constants/cache.go**。
- **实证依据**: internal/services/system（adapter.go:7 等）import 了根包 → 根包 import system = Go 语言级 import 环；`go list -deps ./internal/services` 不含任何 services 子包（RESEARCH §关键约束实证）。pkg/constants 是 D-102-2 已锁定的 captcha 落点，同机理同文件，先例一致；「集中注册、单一真相源」精神不变。
- **影响**: 仅 2 个键格式（`user_endpoints:%s`、`mac:vendor:%s`）注册文件不同；常量值、形态、调用点替换方式完全不变。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-mechanical-constants/102-CONTEXT.md
@.planning/phases/102-mechanical-constants/102-RESEARCH.md
@.planning/phases/102-mechanical-constants/102-PATTERNS.md

@sections: 102-RESEARCH.md §关键约束（import 环实证）/ §CACHE-02 清单（33 格式/46 调用点）
@sections: 102-PATTERNS.md §A1（前缀常量 + GetXxxKey helper 形态与 BuildPattern 缺陷）/ §A2（pkg/constants 注册形态）/ §B3（调用点三形态——Plan 03 消费本注册表）
</context>

<interfaces>
<!-- 本 plan 的注册表 = Plan 03 的接口契约；常量与 helper 命名逐字固定，Plan 03 按名引用，禁止另起名 -->
<!-- 形态纪律（D-102-3/D-102-4）: 键前缀为常量；参数化键走 GetXxxKey helper（体内 fmt.Sprintf，同 GetDictDataByTypeKey 模式）；
     失效 pattern 不新增独立常量，一律 GetXxxPattern helper 前缀派生（"%s:*" 拼接），禁用 CacheKeyManager.BuildPattern（其 * 直接追加无 :，产出 notice:my_notices:{uid}* 与现值差一分隔符） -->

**notice**（Plan 03 调用文件与其同包，裸引用常量名）:
- CacheKeyNoticeMyNotices = "notice:my_notices"
  - GetNoticeMyNoticesKey(userID string, page, pageSize int) → "notice:my_notices:{uid}:page:{p}:size:{s}"
  - GetNoticeMyNoticesStatusKey(userID string, page, pageSize int, status string) → "notice:my_notices:{uid}:page:{p}:size:{s}:status:{s}"
  - GetNoticeMyNoticesPattern(userID string) → "notice:my_notices:{uid}:*"
- CacheKeyNoticeUnreadCount = "notice:unread_count"; GetNoticeUnreadCountKey(userID string) → "notice:unread_count:{uid}"
- CacheKeyNoticeDetail = "notice:detail"; GetNoticeDetailKey(noticeID string) → "notice:detail:{nid}"
- GetNoticeAllPattern() → "notice:*"

**settings**（同包）:
- CacheKeySettingsUser = "settings:user"; GetSettingsUserKey(userID string) → "settings:user:{uid}"

**duty**（子包，systemServices 别名已 import）:
- CacheKeyDutyToday = "duty:today"（plain 键，无 helper）
- CacheKeyDutyMonthly = "duty:monthly"; GetDutyMonthlyKey(year, month int) → "duty:monthly:{y}:{m}"
- CacheKeyDutyHolidays = "duty:holidays"; GetDutyHolidaysKey(year int) → "duty:holidays:{y}"
- GetDutyAllPattern() → "duty:*"; GetDutyHolidaysPattern() → "duty:holidays:*"

**workorder**（子包）:
- CacheKeyWorkorderMyPending = "workorder:my_pending"
  - GetWorkorderMyPendingKey(userID string) → "workorder:my_pending:{uid}"
  - GetWorkorderMyPendingLimitKey(userID string, limit int) → "workorder:my_pending:{uid}:limit:{n}"
- CacheKeyWorkorderStatistics = "workorder:statistics"（plain）
- CacheKeyWorkorderDetail = "workorder:detail"; GetWorkorderDetailKey(id string) → "workorder:detail:{id}"
- GetWorkorderAllPattern() → "workorder:*"

**knowledge**（子包）:
- CacheKeyKbArticle = "kb:article"; GetKbArticleKey(id string) → "kb:article:{id}"
- CacheKeyKbCategoryTree = "kb:category:tree"（plain）
- CacheKeyKbCategoryParent = "kb:category:parent"; GetKbCategoryParentKey(parentID string) → "kb:category:parent:{pid}"
- CacheKeyKbTagsAll = "kb:tags:all"（plain）
- GetKbCategoryPattern() → "kb:category:*"; GetKbArticlePattern() → "kb:article:*"

**network**（子包）:
- CacheKeyNetworkDeviceStatistics = "network_device:statistics"（plain）
- CacheKeyNetworkDeviceDept = "network_device:dept"; GetNetworkDeviceDeptKey(deptID string) → "network_device:dept:{did}"
- CacheKeyNetworkDeviceCredential = "network_device:credential"; GetNetworkDeviceCredentialKey(credentialID string) → "network_device:credential:{cid}"
- CacheKeyNetworkDeviceDetail = "network_device:detail"; GetNetworkDeviceDetailKey(deviceID string) → "network_device:detail:{did}"
- GetNetworkDevicePattern() → "network_device:*"

**widget**（同包）:
- CacheKeyWidgetData = "widget:data"
  - GetWidgetDataKey(widgetID string) → "widget:data:{wid}"
  - GetWidgetDataParamsKey(widgetID string, paramsHash []byte) → "widget:data:{wid}:{hex}"（体内保留 %x 动词：fmt.Sprintf("%s:%s:%x", CacheKeyWidgetData, widgetID, paramsHash)，调用方传 paramsHash[:8] 不变）

**rpa**（子包，selector_learner.go 本相只注册键、不迁闭包）:
- CacheKeyRpaSelectorBest = "rpa:selector:best"; GetRpaSelectorBestKey(pageURL, elementID string) → "rpa:selector:best:{url}:{el}"

**根包 2 格式 → pkg/constants/cache.go**（Sprintf 格式常量形态，同 captcha 先例）:
- UserEndpointsKeyFormat = "user_endpoints:%s"
- MacVendorKeyFormat = "mac:vendor:%s"
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cache_keys.go 注册 8 模块键常量与 helper</name>
  <files>internal/services/system/cache_keys.go</files>
  <read_first>
    internal/services/system/cache_keys.go（全文 375 行——:79-129 既有 const 块风格、:296-368 GetXxxKey helper 形态与注释规范、:282-292 BuildModulePattern/BuildInvalidatePattern 前缀派生先例、:34-41 BuildPattern 缺陷勿用）
    internal/services/system/notice_cache_impl.go :200-215（buildMyNoticesKey 原貌——注册值须与调用点字面量逐字符一致）
    internal/services/duty/duty_cache_impl.go :130-150（调用点原貌对照）
  </read_first>
  <behavior>
    - 注册表 20 常量值 == RESEARCH §CACHE-02 清单字面量（逐字符）
    - pattern 全部派生：GetNoticeAllPattern() == "notice:*"、GetDutyHolidaysPattern() == "duty:holidays:*"、GetNetworkDevicePattern() == "network_device:*"
    - 注册文件内零独立 pattern 常量（grep 无 `Pattern = ` 形态常量定义）
    - GetWidgetDataParamsKey("w1", []byte{0x1a, 0x2b}) == "widget:data:w1:1a2b"（%x 动词原样保留）
  </behavior>
  <action>
    先写 internal/services/system/cache_keys_102_test.go（package system，testify require）：TestCacheKeyEquivalence 快照表覆盖全部 20 常量（want 列 = 原字面量字符串字面量，got 列 = system 包内裸常量名，禁 constants.Xxx 自引用）+ helper 输出断言至少每 helper 一条（样例取值自定，如 GetDutyMonthlyKey(2026, 9) == "duty:monthly:2026:9"、GetNoticeMyNoticesKey("u1", 2, 10) == "notice:my_notices:u1:page:2:size:10"、GetNoticeMyNoticesStatusKey("u1", 2, 10, "0") == "notice:my_notices:u1:page:2:size:10:status:0"、GetWidgetDataParamsKey("w1", []byte{0x1a, 0x2b}) == "widget:data:w1:1a2b"、全部 pattern helper 断言派生输出）。此时常量不存在 → 编译失败即 RED。
    再向 cache_keys.go 追加独立 const 块（风格同 :79-129：模块分组注释 + 每常量单行键样例注释）与 helper 区（形态同 :296-368：注释含「参数/返回：完整键样例」行）。常量与 helper 命名/签名/键值严格按本 plan interfaces 块，逐字一致，禁止简化或改名。跑测试转 GREEN。BuildPattern 勿用；EscapeCacheKeyValue（:373）不触碰。
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/services/system/ -run TestCacheKeyEquivalence -count=1</automated>
  </verify>
  <acceptance_criteria>
    - go test ./internal/services/system/ -run TestCacheKeyEquivalence -count=1 退出码 0
    - cache_keys.go contains `CacheKeyNoticeMyNotices = "notice:my_notices"`、`CacheKeyRpaSelectorBest = "rpa:selector:best"`（20 常量全部在位，值同 interfaces 块）
    - grep cache_keys.go 独立 pattern 常量定义（`Pattern\s*=\s*"`）计数 0（pattern 只能由 helper 派生产出）
    - cache_keys_102_test.go 内 helper 输出断言 ≥ 15 条（20 helper 逐条断言）
    - go vet ./internal/services/system/ 通过
  </acceptance_criteria>
  <done>8 模块 20 常量 + 全部 helper 注册完成且快照/输出断言绿；无独立 pattern 常量</done>
</task>

<task type="auto">
  <name>Task 2: pkg/constants 追加根包 2 格式 + 对应快照测试</name>
  <files>pkg/constants/cache.go, pkg/constants/cache_102_test.go</files>
  <read_first>
    pkg/constants/cache.go（Plan 01 后现状——const 块含 6 captcha 常量）
    pkg/constants/cache_102_test.go（Plan 01 后现状——TestCaptchaCacheKeyEquivalence）
    internal/services/api_endpoint_service.go :55-70（调用点字面量原貌）
    internal/services/mac_history_query_service.go :250-260（同上）
  </read_first>
  <action>
    向 pkg/constants/cache.go const 块追加 2 个常量：UserEndpointsKeyFormat = "user_endpoints:%s"、MacVendorKeyFormat = "mac:vendor:%s"（每常量单行注释，注明根包 import 环豁免落点依据 RESEARCH §关键约束）。向 pkg/constants/cache_102_test.go 追加 TestRootServiceCacheKeyEquivalence：2 行快照（want = "user_endpoints:%s" / "mac:vendor:%s" 字面量，got = 对应常量）。本任务不触碰 services 根包任何文件（调用点替换是 Plan 03 Task 3）。
  </action>
  <verify>
    <automated>go test ./pkg/constants/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - pkg/constants/cache.go contains `UserEndpointsKeyFormat = "user_endpoints:%s"` 与 `MacVendorKeyFormat = "mac:vendor:%s"`
    - go test ./pkg/constants/ -count=1 退出码 0（含 Plan 01 的 TestCaptchaCacheKeyEquivalence 与本任务 TestRootServiceCacheKeyEquivalence）
    - services 根包文件零改动（git diff --stat 不含 internal/services/*.go）
  </acceptance_criteria>
  <done>根包 2 格式注册 + 快照测试绿；pkg/constants 全套等价测试绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 应用进程 → Redis | 键注册表是全模块缓存读写/失效的契约面；注册值漂移会使失效 pattern 脱靶（Tampering 面） |
| 注册表 → 调用点（Plan 03） | 注册与消费分离，命名不一致会让 Plan 03 编译失败或拼错键（流程面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-03 | Tampering | 注册常量值漂移 | mitigate | Task 1/2 等价快照测试逐字符锁值，期望列 = 原字面量 |
| T-102-04 | Tampering | pattern 与键分离漂移 | mitigate | D-102-4 前缀派生：pattern helper 由键前缀常量生成，零独立 pattern 常量；acceptance_criteria grep 硬验 |
| T-102-05 | Evasion | 白名单/豁免被滥用 | mitigate | 本 plan 不设任何豁免项；BuildPattern 缺陷在 plan 中显式点名禁用 |
| T-102-SC | Tampering | npm/pip/cargo 安装 | accept | 零新包安装，供应链面无变化 |
</threat_model>

<verification>
- go test ./internal/services/system/ -run TestCacheKeyEquivalence -count=1 绿（31 格式快照 + helper 输出锁）
- go test ./pkg/constants/ -count=1 绿（captcha 6 + 根包 2 格式快照）
- grep cache_keys.go 无独立 pattern 常量定义（D-102-4 达成）
- go build ./... 退出码 0；go vet ./internal/services/system/ 通过
</verification>

<success_criteria>
CACHE-02 注册面交付完成：cache_keys.go 8 模块 20 常量 + 全部 GetXxxKey/GetXxxPattern helper、pkg/constants 根包 2 格式；两宿主等价快照测试绿；pattern 全部前缀派生；对 D-102-1 的唯一字面修订（根包 2 格式落 pkg/constants）已在本 plan objective 显式披露并附实证。
</success_criteria>

<output>
Create `.planning/phases/102-mechanical-constants/102-02-SUMMARY.md` when done
</output>
