---
phase: 102-mechanical-constants
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/scheduler/cron.go
  - internal/scheduler/vdi_sync_tasks.go
  - internal/scheduler/workorder_tasks.go
  - internal/scheduler/reconciliation_tasks.go
  - internal/scheduler/mac_history_tasks.go
  - internal/scheduler/mac_history_matview_tasks.go
  - internal/services/scheduler/job_service.go
  - internal/services/workorder/base.go
  - internal/models/status_constants_test.go
autonomous: true
requirements: [STATUS-01]
user_setup: []

must_haves:
  truths:
    - "RESEARCH 清单 21 处 status 位点 + 执行中新暴露 2 处（reconciliation_tasks.go 双方言 raw SQL）全部引用 models 具名常量，零新增常量（InfoPointStatusNormal 既有）"
    - "status_constants_test.go 值锁全程绿；AST 使用点扫描守护（D-102-6，7 种形态）在位且绿，白名单仅 geocoding_service.go 与 sqlite_reconciliation_views.go 两条"
    - "SP-6 缺口补齐：WorkOrderStatus 族登记进值锁 watched 前缀与期望表（本 plan 使 base.go 引用该族，须随 plan 锁定）"
  artifacts:
    - path: "internal/models/status_constants_test.go"
      provides: "TestNoStatusLiteralUsage AST 使用点扫描（7 形态硬失败 + geocoding/sqlite 视图 DDL 白名单 + migrations 全树排除 + 内存片段自测）"
      contains: "TestNoStatusLiteralUsage"
  key_links:
    - from: "internal/scheduler/cron.go"
      to: "internal/models/log.go"
      via: "Status 字段与 status SQL 参数引用 JobStatusNormal/JobLogStatusSuccess 等具名常量（值锁在位）"
      pattern: "models\\.Job(Status|LogStatus)\\w*"
---

<objective>
STATUS-01：后端业务代码全部 status 数字字面量（RESEARCH 21 位点 + 新暴露 2 处）替换为 models 具名常量引用，并交付 D-102-6 AST 使用点扫描守护。全部行为等价，零新增常量。

Purpose: status 0/1 语义靠命名常量承载，字面量是静默换值事故的入口；AST 使用点扫描让「使用点必须引常量」成为红线。
Output: 9 个业务文件按 RESEARCH §STATUS 清单 + 新暴露 2 处逐处替换（含 cron.go:832 raw SQL 占位符化）+ status_constants_test.go 扫描扩展 + WorkOrderStatus 值锁补登记。

**新暴露位点说明（D-102-6 全后端口径「执行中新暴露位点按同规则顺带清」）**：RESEARCH 清单遗漏 reconciliation_tasks.go:386 与 :398（双方言分支 raw SQL 内嵌 ip.status = 0，与 cron.go:832 同形态）——检查确认 models.InfoPointStatusNormal 已存在（internal/models/operations/infopoint.go:20，值 0，且 InfoPointStatus 前缀已入 status_constants_test.go watched 表，零新增零同步）。sqlite_reconciliation_views.go:224 为视图 DDL 静态 SQL 无法参数化，按 D-102-6 白名单登记。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-mechanical-constants/102-CONTEXT.md
@.planning/phases/102-mechanical-constants/102-RESEARCH.md
@.planning/phases/102-mechanical-constants/102-PATTERNS.md

@sections: 102-RESEARCH.md §STATUS 清单（21 位点映射表）/ §D-102-6 扫描器 6 形态 + 排除豁免面 / §Pattern 4（raw SQL 占位符）
@sections: 102-PATTERNS.md §C1-C7（各形态 before/after 摘录与常量坐标）/ §E3（status 扫描扩展宿主手法）/ §SP-5（check-status-literals.sh 共存）/ §SP-6（值锁缺口）
</context>

<interfaces>
<!-- 既有常量坐标（常量全部已存在，零新增。值锁状态须区分——JobStatus / JobLogStatus / DutyStatus / VDIServerStatus / InfoPointStatus 族：已入 status_constants_test.go watched 表；WorkOrderStatus 族：**待本 plan Task 3① 补登记（SP-6）**，当前不在 watched 表内）：
internal/models/log.go :71-72  JobStatusNormal = 0 / JobStatusPause = 1（命名是 Pause，不是 Stopped——Pitfall 3）
internal/models/log.go :94-95  JobLogStatusSuccess = 0 / JobLogStatusFailure = 1
internal/models/duty.go :25    DutyStatusNormal = 0
internal/models/vdi.go :51-52  VDIServerStatusNormal = 0 / VDIServerStatusStopped = 1
internal/models/workorder.go :17-21  WorkOrderStatusPending = 0 / WorkOrderStatusProcessing = 1（typed 常量，[]int 场景需 int() 转换；watched 表待本 plan Task 3 补登记）
internal/models/operations/infopoint.go :20  InfoPointStatusNormal = 0（子包，InfoPointStatus 前缀已在 watched 表）

转换口径（SP-3）：
- 赋给 typed 结构体字段（Status JobStatus）→ 直接引用常量，无转换
- 与 int 变量比较（job_service.go :332 status 为 int 入参）→ status == int(models.JobStatusNormal)
- []int 切片实参（base.go :183/:191）→ []int{int(models.WorkOrderStatusPending), int(models.WorkOrderStatusProcessing)}（A2 零风险形态，两处统一）

import 惯例：
- internal/scheduler 与 internal/services/{scheduler,workorder} 均已 import internal/models（models 别名）
- reconciliation_tasks.go 需新增 operationsmodels "github.com/xingran-next/xingran-go-backend/internal/models/operations"（仓内既有别名惯例，见 door_handler.go:7）
</interfaces>

<tasks>
<task type="auto">
  <name>Task 1: internal/scheduler 6 文件 14 位点替换（含 raw SQL 占位符化）</name>
  <files>internal/scheduler/cron.go, internal/scheduler/vdi_sync_tasks.go, internal/scheduler/workorder_tasks.go, internal/scheduler/reconciliation_tasks.go, internal/scheduler/mac_history_tasks.go, internal/scheduler/mac_history_matview_tasks.go</files>
  <read_first>
    internal/scheduler/cron.go :40-70 与 :230-240 与 :400-440 与 :825-840（6 位点窗）
    internal/scheduler/vdi_sync_tasks.go :45-90（2 位点）
    internal/scheduler/workorder_tasks.go :190-200 与 :320-335 与 :445-455（3 位点；:327 既有常量引用先例）
    internal/scheduler/reconciliation_tasks.go :190-200（Status: 0 位点 + :195 MisfirePolicy）与 :377-406（双方言 raw SQL 全貌）
    internal/scheduler/mac_history_tasks.go :120-130（1 位点）
    internal/scheduler/mac_history_matview_tasks.go :50-60（1 位点）
    internal/models/log.go :60-100（常量坐标）
  </read_first>
  <action>
    执行前逐文件 grep -n 复核行号，按 RESEARCH §STATUS 清单表逐处替换（行号为 2026-09-07 快照）：
    cron.go —— :43 Status: 0 改 models.JobLogStatusSuccess；:62 jobLog.Status = 1 改 models.JobLogStatusFailure；:235 Where("status = ?", 0) 改传 models.JobStatusNormal；:407 Update("status", 0) 改 models.JobStatusNormal；:435 Update("status", 1) 改 models.JobStatusPause（不是 Stopped——models/log.go:72 实名 Pause）；:832 raw SQL 改 Where("ds.schedule_date = ? AND ds.status = ?", date, models.DutyStatusNormal) 占位符形态，禁字符串拼接。
    vdi_sync_tasks.go —— :48 Where("status = ?", 0) 改 models.VDIServerStatusNormal；:85 server.Status != 0 改 models.VDIServerStatusNormal。
    workorder_tasks.go —— :195 Where(...status = ?, today, 0) 的 0 改 models.DutyStatusNormal（占位符参数，既有注释可保留）；:328 与 :451 Status: 0 改 models.JobStatusNormal。
    reconciliation_tasks.go —— :196 Status: 0 改 models.JobStatusNormal。
    mac_history_tasks.go :127 与 mac_history_matview_tasks.go :56 —— Status: 0 改 models.JobStatusNormal。
    顺带清（RESEARCH Open Question #1 采纳，1 行）：reconciliation_tasks.go :195 MisfirePolicy: 1 改 models.MisfirePolicyImmediately——替换前先读 internal/models/log.go 确认该常量值 == 1；若值不符则不动并在 SUMMARY 记录。
    复合字面量内其余字段（同结构体的 MisfirePolicy 等既有常量行）勿动。
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/scheduler/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "Status: 0[,}]\|status = ?, 0)\|Update(\"status\", 0)\|Update(\"status\", 1)\|ds.status = 0" 六文件计数 0
    - grep -c "models.JobStatusPause" internal/scheduler/cron.go == 1（:435 唯一位点，防误写 Stopped）
    - cron.go contains ds.status = ?（占位符化证据）且不含 ds.status = %d（禁拼接证据）
    - go test ./internal/scheduler/ 0 失败
  </acceptance_criteria>
  <done>scheduler 6 文件 14 位点全部引用具名常量；raw SQL 占位符化；包测试全绿</done>
</task>

<task type="auto">
  <name>Task 2: services 层替换 + 新暴露 raw SQL 位点（InfoPointStatusNormal）</name>
  <files>internal/services/scheduler/job_service.go, internal/services/workorder/base.go, internal/scheduler/reconciliation_tasks.go</files>
  <read_first>
    internal/services/scheduler/job_service.go :318-344（:324 status int 本质 + :332 替换点）
    internal/services/workorder/base.go :175-200（:183 与 :191 孪生 IN 切片）
    internal/scheduler/reconciliation_tasks.go :377-410（双方言 query 构造 + :406 Raw 调用）
    internal/models/operations/infopoint.go :16-22（InfoPointStatusNormal 坐标）
  </read_first>
  <action>
    job_service.go —— :332 if status == 0 改 if status == int(models.JobStatusNormal)（status 为函数 int 入参，最小 diff 仅比较处加转换；:324 的 models.JobStatus(status) 转换先例行勿动）。
    base.go —— :183 与 :191 两处 Where("status IN ?", []int{0, 1}) 整切片表达式替换为 Where("status IN ?", []int{int(models.WorkOrderStatusPending), int(models.WorkOrderStatusProcessing)})（两处同改、同形态统一；:183 行尾注释保留）。
    reconciliation_tasks.go 新暴露位点（D-102-6 全后端口径顺带清）——文件 import 块新增 operationsmodels "github.com/xingran-next/xingran-go-backend/internal/models/operations"；:386（PG 分支）与 :398（sqlite 分支）两处 SQL 串内 ip.status = 0 均改 ip.status = ?；:406 db.Raw(query) 改 db.Raw(query, operationsmodels.InfoPointStatusNormal)（两分支共用一次 Raw 调用一个占位符实参）。禁字符串拼接（Pattern 4）。
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/services/scheduler/ ./internal/services/workorder/ ./internal/scheduler/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "status IN ?, \[\]int{0, 1}" internal/services/workorder/base.go 计数 0；grep -c "int(models.WorkOrderStatusPending)" internal/services/workorder/base.go == 2
    - grep -n "if status == 0" internal/services/scheduler/job_service.go 计数 0
    - grep -n "ip.status = 0" internal/scheduler/reconciliation_tasks.go 计数 0（含注释行不计——正文 SQL 双分支已参数化）；grep -c "operationsmodels.InfoPointStatusNormal" internal/scheduler/reconciliation_tasks.go == 1
    - go test 三包 0 失败
  </acceptance_criteria>
  <done>services 层 4 处 + 新暴露 2 处替换完成；WorkOrderStatus/InfoPointStatus 引用走具名常量；测试全绿</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TestNoStatusLiteralUsage 扫描守护 + WorkOrderStatus 值锁补登记</name>
  <files>internal/models/status_constants_test.go</files>
  <read_first>
    internal/models/status_constants_test.go（全文 363 行——:63-80 watchedStatusPrefixes、:86-200 expectedStatusValues、:214-230 双向断言纪律、:289-348 readStatusConsts AST 读取器）
    internal/services/system/cache_invariants_92_test.go :33-81 与 :121-130（扫描骨架：白名单 map / parser+Inspect / runtime.Caller 相对定位）
    internal/services/operations/geocoding_service.go :325-340（白名单位点原貌）
    internal/core/db/sqlite_reconciliation_views.go :215-230（白名单位点原貌）
  </read_first>
  <behavior>
    - 扫描器自测（内存片段 parser.ParseFile(fset, "snippet.go", src, 0) 逐形态断言命中 >= 1）：Status: 0 复合字面量键 / x.Status = 1 赋值 / x.Status != 0 与 status == 0 二元比较 / Where("status = ?", 0) 与 Update("status", 0) 调用实参 / Where(... []int{0, 1}) status IN 切片 / SQL 串内嵌 Where("... status = 0 ...") 第 7 形态
    - 反例自测：Status: models.JobStatusNormal 与 Where("status = ?", models.JobStatusNormal) 命中 0
    - 全后端扫描（internal/ 业务代码）零硬失败失败项；白名单 2 条（geocoding_service.go 与 sqlite_reconciliation_views.go）各附原因
  </behavior>
  <action>
    同文件扩展 status_constants_test.go（D-102-6 明文）：
    ① WorkOrderStatus 值锁补登记（SP-6 处置）：watchedStatusPrefixes 追加 "WorkOrderStatus"，expectedStatusValues 追加该族 5 值（Pending=0/Processing=1/Completed=2/Closed=3/Rejected=4，坐标 workorder.go:17-21）——先改测试表再被 Plan 内 Task 2 的 base.go 引用行为锁值。
    ② 新增 TestNoStatusLiteralUsage：扫描 internal/ 全后端业务代码（排除 internal/core/db/migrations/ 整树——含 archive 与未归档 migration_175，迁移历史禁改禁扫；排除 internal/models/ 全部常量定义面；排除 _test.go；排除 vendored/生成代码若有）。7 种 AST 形态匹配（RESEARCH 6 形态 + 第 7 形态：AST 提取的字符串字面量值匹配正则 status = 数字（不区分大小写、容忍空白）——覆盖 raw SQL 内嵌字面量形态，第 7 形态仅对 GORM Where/Raw/Update 实参位与 SQL 构造赋值右侧的字符串字面量生效，注释天然不在 AST 字面量内故 excel_handler.go:192 类注释不命中）。
    ③ 白名单 map（文件名 → 豁免原因）：geocoding_service.go = "百度地图 API 返回码（非 DB status，F 簇契约，audit 定性，与 scripts/check-status-literals.sh 白名单同源）"；sqlite_reconciliation_views.go = "sqlite 视图 DDL 静态 SQL 无法参数化（值与 PG migration_175 视图契约一致，禁改）"。命中调和：白名单文件跳过，非白名单命中 t.Errorf 硬失败。
    ④ 空转防护与卫生提示（invariants_92 同款）：扫描文件数为 0 则 t.Errorf；白名单条目附 Logf 提示零残留目标。
    SP-5 处置：scripts/check-status-literals.sh 既有 grep 棘轮不动（并存无冲突；其扫描面不含 internal/scheduler，本相替换不影响其结果）。
  </action>
  <verify>
    <automated>go test ./internal/models/ -run "TestNoStatusLiteralUsage|TestStatusConstants" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - status_constants_test.go contains TestNoStatusLiteralUsage 且 contains "WorkOrderStatus"（watched 表）
    - go test ./internal/models/ -count=1 退出码 0（值锁含 WorkOrderStatus 5 值 + 使用点扫描全绿）
    - grep -c "geocoding_service.go" status_constants_test.go >= 1 且 grep -c "sqlite_reconciliation_views.go" status_constants_test.go >= 1（白名单登记证据）
    - 扫描器自测覆盖 7 形态 + 反例（测试内内存片段断言在位）
  </acceptance_criteria>
  <done>使用点扫描守护在位且全后端绿；白名单 2 条附原因；WorkOrderStatus 值锁补齐；既有值锁测试不倒退</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 业务代码 → DB | status 字面量替换若走字符串拼接（Sprintf 进 SQL）会引入注入面模式先例（Tampering/SQLi 面） |
| 常量真相源 → 业务代码 | models 常量值被静默改动会翻转全系统 0/1 语义（Tampering 面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-09 | Tampering | SQL 替换引入拼接 | mitigate | 占位符形态强制（Pattern 4）；acceptance grep 禁 ds.status = %d 形态；go vet 随套件 |
| T-102-10 | Tampering | 误写不存在常量（JobStatusStopped）或新增常量破坏值锁 | mitigate | 零新增纪律 + status_constants_test.go 值锁（执行中全程绿）；Plan 措辞不声称 WorkOrderStatus 受既有锁保护而是随本 plan 补登记 |
| T-102-11 | Evasion | 扫描白名单堆放豁免 | mitigate | 白名单 2 条均附显式原因；migrations 全树走排除面而非白名单（结构区别：排除=定义域外，白名单=域内豁免） |
| T-102-SC | Tampering | npm/pip/cargo 安装 | accept | 零新包安装，供应链面无变化 |
</threat_model>

<verification>
- go test ./internal/models/ -count=1 绿（值锁含 WorkOrderStatus + 使用点扫描）
- go test ./internal/scheduler/ ./internal/services/scheduler/ ./internal/services/workorder/ -count=1 0 失败
- go build ./... 退出码 0
- grep 六个 scheduler 文件 + job_service.go + base.go 零 status 数字字面量残留（白名单外）
</verification>

<success_criteria>
STATUS-01 交付完成：全部 status 位点（RESEARCH 21 + 新暴露 2）引用 models 具名常量、零新增常量；AST 使用点扫描守护在位且全后端绿（白名单 geocoding + sqlite 视图 DDL 两条）；WorkOrderStatus 值锁补齐；models 值锁全程绿；行为等价（scheduler/workorder 既有测试网零失败）。
</success_criteria>

<output>
Create `.planning/phases/102-mechanical-constants/102-04-SUMMARY.md` when done
</output>
