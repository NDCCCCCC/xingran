---
phase: 102-mechanical-constants
plan: 03
type: execute
wave: 3
depends_on: ["102-02"]
files_modified:
  - internal/services/system/notice_cache_impl.go
  - internal/services/system/settings_cache_impl.go
  - internal/services/system/widget_data_fetcher.go
  - internal/services/duty/duty_cache_impl.go
  - internal/services/workorder/workorder_cache_impl.go
  - internal/services/knowledge/knowledge_cache_impl.go
  - internal/services/network/cache_impl.go
  - internal/services/rpa/selector_learner.go
  - internal/services/api_endpoint_service.go
  - internal/services/mac_history_query_service.go
  - internal/services/system/cache_keys_102_test.go
autonomous: true
requirements: [CACHE-02]
user_setup: []

must_haves:
  truths:
    - "10 模块 46 调用点全部引用具名常量/helper；12 收敛文件零内联键字面量"
    - "失效 pattern 调用点全走 GetXxxPattern 派生 helper；BuildPattern 未被引入"
    - "内联扫描守护（D-102-5②）在位且绿；白名单零条目"
    - "rpa selector_learner.go 仅 getCacheKey 键构造替换；闭包位点未动（Phase 103 前置保持）"
  artifacts:
    - path: "internal/services/system/cache_keys_102_test.go"
      provides: "TestCacheKeyInlineResidue 内联键扫描守护（fmt.Sprintf 首参为含字母+冒号的字符串字面量硬失败；12 文件窄扫；runtime.Caller 相对定位）"
      contains: "TestCacheKeyInlineResidue"
  key_links:
    - from: "internal/services/duty/duty_cache_impl.go"
      to: "internal/services/system/cache_keys.go"
      via: "systemServices.CacheKeyDutyToday / GetDutyMonthlyKey（既有 import 别名）"
      pattern: "systemServices\\.(CacheKey|Get)"
---

<objective>
CACHE-02 第二步：10 模块 46 个调用点按 Plan 02 注册表机械替换为常量/helper 引用，并交付内联键扫描守护（D-102-5②，窄扫 12 文件），12 个收敛文件内联键字面量清零。行为等价——键值/TTL/缓存调用形态三不变。

Purpose: 消灭内联键字面量这一键拼写分叉源头；扫描守护使 12 文件成为红线，回潮即红。
Output: 10 个调用文件替换 + cache_keys_102_test.go 追加 TestCacheKeyInlineResidue。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-mechanical-constants/102-CONTEXT.md
@.planning/phases/102-mechanical-constants/102-RESEARCH.md
@.planning/phases/102-mechanical-constants/102-PATTERNS.md

@sections: 102-PATTERNS.md §B3（调用点三形态 + notice/widget/根包特殊形态摘录）/ §E1（invariants_92 骨架五件套）/ §SP-1（最小 diff 纪律）
@sections: 102-RESEARCH.md §CACHE-02 清单（46 调用点行号 + 33 格式全集）/ §Pitfall 1（import 环）/ §Pitfall 6（扫描面窄口径）
</context>

<interfaces>
<!-- Plan 02 注册表（cache_keys.go 常量与 helper）是本 plan 唯一接口，命名逐字按 Plan 02 interfaces 块。执行器先读 Plan 02 SUMMARY 与 cache_keys.go 注册区。
import 惯例：
- system 同包 3 文件（notice/settings/widget）：同包裸引用常量与 helper 名，无需 import
- duty/workorder/knowledge/network cache_impl：既有 import 在位，别名固定 systemServices（PATTERNS 实核 duty :12）
- rpa/selector_learner.go：需新增 systemServices "github.com/xingran-next/xingran-go-backend/internal/services/system"（rpa 到 system 无环，RESEARCH go list 实证）
- api_endpoint_service.go：需新增 "github.com/xingran-next/xingran-go-backend/pkg/constants"
- mac_history_query_service.go :17：已 import pkg/constants，零成本
</interfaces>

<tasks>
<task type="auto">
  <name>Task 1: system 同包 3 文件替换（notice/settings/widget）</name>
  <files>internal/services/system/notice_cache_impl.go, internal/services/system/settings_cache_impl.go, internal/services/system/widget_data_fetcher.go</files>
  <read_first>
    internal/services/system/notice_cache_impl.go :200-310（buildMyNoticesKey 2 格式 + 失效方法全集）
    internal/services/system/settings_cache_impl.go :35-70（settings:user 三处）
    internal/services/system/widget_data_fetcher.go :55-70（buildWidgetCacheKey，%x 动词形态）
    internal/services/system/cache_keys.go（Plan 02 注册表——常量与 helper 名单）
  </read_first>
  <action>
    同包裸引用。执行前逐文件 grep -n 复核行号（RESEARCH §CACHE-02 notice :208,:210,:240,:289,:296 两处,:302；settings :41,:53,:64；widget :61,:65），按字面量对照注册表映射、不按行号盲改：
    notice —— :208 与 :210 的两个 Sprintf 整表达式改 GetNoticeMyNoticesKey(userID, page, pageSize) 与 GetNoticeMyNoticesStatusKey(userID, page, pageSize, *status) 调用（buildMyNoticesKey 保留为本地 wrapper，注释保留）；:240 改 GetNoticeUnreadCountKey(userID)；:289 改 GetNoticeDetailKey(noticeID)；:296 两处 InvalidatePattern 实参改 GetNoticeMyNoticesPattern(userID) 与 GetNoticeUnreadCountKey(userID)；:302 改 GetNoticeAllPattern()。
    settings —— :41/:53/:64 三处改 GetSettingsUserKey(userID)。
    widget —— :61 改 GetWidgetDataKey(widgetID)；:65 改 GetWidgetDataParamsKey(widgetID, paramsHash[:8])（buildWidgetCacheKey 体内 2 个 Sprintf 整体改 helper 调用；sha256/json 逻辑行不动，%x 动词已内含于 helper）。
    纪律（SP-1）：每处只改键实参；getExpiration(configKey, default) TTL 行与 base.GetOrSetJSON / Invalidate / InvalidatePattern 调用形态零改动。
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/services/system/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - grep -n 内联键字面量（notice: / settings:user / widget:data 前缀的 Sprintf 首参字面量）三文件计数 0
    - notice_cache_impl.go contains GetNoticeMyNoticesStatusKey 调用（:210 形态收敛证据）
    - widget_data_fetcher.go contains GetWidgetDataParamsKey 调用（%x 形态收敛证据）
    - git diff 无 getExpiration / time.Duration 行变更
    - go test ./internal/services/system/ 0 失败
  </acceptance_criteria>
  <done>同包 3 文件全部替换完成，system 包测试全绿</done>
</task>

<task type="auto">
  <name>Task 2: 子包 5 文件替换（duty/workorder/knowledge/network/rpa）</name>
  <files>internal/services/duty/duty_cache_impl.go, internal/services/workorder/workorder_cache_impl.go, internal/services/knowledge/knowledge_cache_impl.go, internal/services/network/cache_impl.go, internal/services/rpa/selector_learner.go</files>
  <read_first>
    internal/services/duty/duty_cache_impl.go :130-150 与 :210-220 与 :270-305（8 位点）
    internal/services/workorder/workorder_cache_impl.go :210-275（6 位点）
    internal/services/knowledge/knowledge_cache_impl.go :85-95 与 :125-135 与 :175-185 与 :220-250（8 位点）
    internal/services/network/cache_impl.go :260-325（8 位点，含失效 pattern）
    internal/services/rpa/selector_learner.go :355-365（getCacheKey 单点）
    internal/services/system/cache_keys.go（Plan 02 注册表）
  </read_first>
  <action>
    子包经既有 systemServices 别名引用；selector_learner.go 先新增 systemServices import（见 interfaces）。执行前逐文件 grep -n 复核行号，按字面量对照注册表映射：
    duty —— :137 "duty:today" 改 systemServices.CacheKeyDutyToday；:144 Sprintf 改 GetDutyMonthlyKey(year, month)；:215 改 GetDutyHolidaysKey(year)；:273 keys 切片字面量改 systemServices.CacheKeyDutyToday；:280/:287/:294/:301 失效位按字面量映射到 GetDutyAllPattern() / GetDutyHolidaysPattern() 等。
    workorder —— :215 Sprintf 改 GetWorkorderMyPendingLimitKey(userID, limit)；:257 改 GetWorkorderMyPendingKey(userID)；:241 改 CacheKeyWorkorderStatistics；:250 改 GetWorkorderDetailKey(id)；:264/:271 失效位改 GetWorkorderAllPattern()。
    knowledge —— :91/:129/:180 等 kb:article 位点改 GetKbArticleKey(id)；kb:category:tree plain 位点改 CacheKeyKbCategoryTree；kb:category:parent 改 GetKbCategoryParentKey(parentID)；kb:tags:all 改 CacheKeyKbTagsAll；kb:category:* 与 kb:article:* 失效位改 GetKbCategoryPattern() / GetKbArticlePattern()。
    network —— :268 改 CacheKeyNetworkDeviceStatistics；:275 改 GetNetworkDeviceDeptKey(deptID)；:282 改 GetNetworkDeviceCredentialKey(credentialID)；:291 改 GetNetworkDeviceDetailKey(deviceID)；:319 等失效位改 GetNetworkDevicePattern()。
    rpa —— getCacheKey（:362）体改 return systemServices.GetRpaSelectorBestKey(pageURL, elementID)；:169-174 与 :226 闭包位点勿动（Phase 103 CONV-03 前置保持）。
    纪律：禁 BuildPattern；禁动 selector_learner 闭包位点；TTL/configKey 行零改动。
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/services/duty/ ./internal/services/workorder/ ./internal/services/knowledge/ ./internal/services/network/ ./internal/services/rpa/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "fmt.Sprintf(\"duty:\|fmt.Sprintf(\"workorder:\|fmt.Sprintf(\"kb:\|fmt.Sprintf(\"network_device:\|fmt.Sprintf(\"rpa:selector" 五文件计数 0
    - duty_cache_impl.go contains systemServices.CacheKeyDutyToday；selector_learner.go contains GetRpaSelectorBestKey
    - git diff selector_learner.go 仅 getCacheKey 函数体与 import 块变化（:169-174/:226 零 diff）
    - git diff 五文件无 getExpiration / time.Duration 行变更
    - go test 五包 0 失败
  </acceptance_criteria>
  <done>子包 5 文件替换完成，五包测试全绿，rpa 闭包位点未动</done>
</task>

<task type="auto">
  <name>Task 3: 根包 2 文件替换 + TestCacheKeyInlineResidue 内联扫描守护</name>
  <files>internal/services/api_endpoint_service.go, internal/services/mac_history_query_service.go, internal/services/system/cache_keys_102_test.go</files>
  <read_first>
    internal/services/api_endpoint_service.go :55-70 与 :185-200（:63/:194 user_endpoints 两处；import 块现状）
    internal/services/mac_history_query_service.go :250-260（:256 mac:vendor 一处；:17 已 import constants）
    internal/services/system/cache_invariants_92_test.go（全文 203 行——AST 扫描骨架五件套：白名单 map / parser+ast.Inspect / runtime.Caller 相对定位 :121-130 / 硬档调和 :151-191 / 空转防护 :155-157）
    internal/services/system/cache_keys_102_test.go（Plan 02 交付的快照测试——本任务追加扫描测试）
  </read_first>
  <action>
    根包 2 文件（D-102-2 同机理落 pkg/constants，import 环豁免）：api_endpoint_service.go :63 与 :194 的 Sprintf 首参改 constants.UserEndpointsKeyFormat（该文件需新增 constants import）；mac_history_query_service.go :256 改 constants.MacVendorKeyFormat（import 已在位）。
    再向 cache_keys_102_test.go 追加 TestCacheKeyInlineResidue（骨架复制 invariants_92）：AST 扫描 fmt.Sprintf 调用，首参为字符串字面量且值同时含 ":" 与至少一个英文字母即命中（键格式形态；"%s:%d" 等纯动词串无字母天然不命中——captcha_background 派生式不受影响）；runtime.Caller 相对推导仓库根（禁本地绝对路径，v1.27 测试纪律）；固定 12 文件清单（D-102-7 窄扫）：internal/core/captcha.go、internal/core/captcha_background.go、notice/settings/widget 同包 3 文件、duty/workorder/knowledge/network cache_impl 4 文件、rpa/selector_learner.go、api_endpoint_service.go、mac_history_query_service.go；白名单 map 初始零条目（豁免须显式登记并附原因）；目录/文件未命中时 t.Errorf 空转防护；命中即 Errorf 硬失败。
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/services/system/ -run "TestCacheKeyInlineResidue|TestCacheKeyEquivalence" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "user_endpoints:%s" internal/services/api_endpoint_service.go 计数 0；grep -n "mac:vendor:%s" internal/services/mac_history_query_service.go 计数 0
    - grep -c "constants.UserEndpointsKeyFormat" internal/services/api_endpoint_service.go == 2；grep -c "constants.MacVendorKeyFormat" internal/services/mac_history_query_service.go == 1
    - cache_keys_102_test.go contains TestCacheKeyInlineResidue 且 runtime.Caller 定位手法在位
    - go test ./internal/services/system/ -run TestCacheKeyInlineResidue -count=1 退出码 0（12 文件全部干净）
    - go test ./... 全量绿（本 plan 收尾门）
  </acceptance_criteria>
  <done>根包 2 文件替换完成；内联扫描守护在位且 12 文件全绿；全仓测试 0 失败</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 应用进程 → Redis | 调用点替换若引入键值漂移 → 存量键孤儿化 + 失效 pattern 脱靶（Tampering 面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-06 | Tampering | 替换时键值走样 | mitigate | 只允许键实参 token 变化；等价快照 + TestCacheKeyInlineResidue 双守护；diff 审查禁 getExpiration/TTL 行变更 |
| T-102-07 | Tampering | 误用 BuildPattern 派生 pattern（缺 : 分隔符） | mitigate | plan 显式禁用；acceptance grep 验证 pattern 位点全走 GetXxxPattern |
| T-102-08 | Evasion | 扫描白名单堆放豁免 | mitigate | 白名单初始零条目；豁免须显式登记并附原因（invariants_92 纪律） |
| T-102-SC | Tampering | npm/pip/cargo 安装 | accept | 零新包安装，供应链面无变化 |
</threat_model>

<verification>
- go test ./internal/services/system/ -run TestCacheKeyInlineResidue -count=1 绿（12 文件内联清零守护）
- go test ./internal/services/system/ ./internal/services/duty/ ./internal/services/workorder/ ./internal/services/knowledge/ ./internal/services/network/ ./internal/services/rpa/ -count=1 0 失败
- go build ./... 退出码 0
- grep 全部 12 收敛文件零内联键格式字面量
</verification>

<success_criteria>
CACHE-02 全部交付完成：46 调用点引用具名常量/helper、12 文件内联键字面量清零、扫描守护在位且绿、pattern 全部前缀派生、rpa 闭包位点未动（Phase 103 CONV-01/03 前置保持）。
</success_criteria>

<output>
Create `.planning/phases/102-mechanical-constants/102-03-SUMMARY.md` when done
</output>
