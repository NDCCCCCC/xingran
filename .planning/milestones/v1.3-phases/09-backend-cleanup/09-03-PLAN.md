---
phase: 09-backend-cleanup
plan: 03
type: execute
wave: 2
depends_on: [09-00, 09-01, 09-02]
files_modified:
  - internal/api/v1/ws_notice_handler.go
  - internal/scheduler/cron.go
  - internal/api/v1/auth.go
autonomous: false
requirements:
  - CODE-02c
user_setup: []
must_haves:
  truths:
    - "WebSocket CheckOrigin 验证逻辑已增强并测试通过"
    - "GlobalDeviceMonitorService 并发安全已实现并测试通过"
    - "登录错误日志已添加并测试通过"
    - "Wave 0 测试从 Skip 转为 Pass (TDD GREEN 阶段)"
  artifacts:
    - path: "internal/api/v1/ws_notice_handler.go"
      provides: "WebSocket 安全验证"
      contains: "CheckOrigin.*applogger.Warnf"
    - path: "internal/scheduler/cron.go"
      provides: "设备监控服务并发访问"
      contains: "RWMutex.*GetDeviceMonitorService"
    - path: "internal/api/v1/auth.go"
      provides: "登录错误日志"
      contains: "applogger.Errorf.*记录登录日志失败"
  key_links:
    - from: "Wave 0 测试文件"
      to: "安全修复实现"
      via: "TDD GREEN 阶段"
      pattern: "测试先行 → 实现通过"
    - from: "internal/api/v1/ws_notice_handler.go"
      to: "CheckOrigin"
      via: "WebSocket 升级验证"
      pattern: "CheckOrigin.*func"
    - from: "internal/scheduler/cron.go"
      to: "GlobalDeviceMonitorService"
      via: "全局变量访问"
      pattern: "sync.RWMutex"
    - from: "internal/api/v1/auth.go"
      to: "recordLoginLog"
      via: "登录日志记录"
      pattern: "func recordLoginLog"
---

<objective>
实现三个安全问题修复（TDD GREEN 阶段），使 Wave 0 创建的测试从 Skip 转为 Pass。增强 WebSocket CheckOrigin 验证、实现 GlobalDeviceMonitorService 并发安全、添加登录错误日志。

Purpose: 修复已知安全漏洞，验证测试先行开发（TDD）方法，提升系统安全性和可观测性
Output: 三个安全问题修复，对应测试从 Skip 转为 Pass
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-backend-cleanup/09-CONTEXT.md
@.planning/phases/09-backend-cleanup/09-RESEARCH.md
@.planning/phases/09-backend-cleanup/09-PATTERNS.md
@.planning/phases/09-backend-cleanup/09-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@internal/api/v1/ws_notice_handler.go
@internal/scheduler/cron.go
@internal/api/v1/auth.go
@.planning/phases/09-backend-cleanup/09-00-PLAN.md
@.planning/phases/08-snmp-panic/08-CONTEXT.md
@internal/api/v1/ws_notice_handler_test.go
@internal/scheduler/cron_test.go
@internal/api/v1/auth_test.go
@internal/core/security/password_test.go
@internal/scheduler/ad_sync_tasks_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 WebSocket CheckOrigin 增强（GREEN 阶段）</name>
  <files>internal/api/v1/ws_notice_handler.go</files>
  <read_first>
    1. internal/api/v1/ws_notice_handler.go - 完整阅读 CheckOrigin 实现
    2. .planning/phases/09-backend-cleanup/09-00-PLAN.md - 查看测试期望行为
    3. internal/api/v1/ws_notice_handler_test.go - 查看测试用例
    4. .planning/phases/09-backend-cleanup/09-RESEARCH.md - WebSocket CheckOrigin 安全问题
  </read_first>
  <behavior>
    实现后应满足以下行为（来自 Wave 0 测试）：
    - 通配符 "*" 允许所有来源并记录警告日志
    - 同源请求（Origin 与 Host 匹配）被允许
    - localhost (127.0.0.1) 被允许
    - 白名单匹配被允许
    - 白名单不匹配被拒绝并记录安全日志
    - 空 Origin 被允许（非浏览器客户端）
  </behavior>
  <action>
    实现 WebSocket CheckOrigin 验证逻辑增强（使 Wave 0 测试通过）：

    1. 读取当前的 CheckOrigin 实现（行 24-54）

    2. 参考测试用例，实现安全增强：
       ```go
       CheckOrigin: func(r *http.Request) bool {
           if allowAll {
               // 生产环境警告
               applogger.Warnf("WebSocket CheckOrigin 允许所有来源（开发模式）")
               return true
           }

           origin := r.Header.Get("Origin")
           if origin == "" {
               return true // 非浏览器客户端无Origin头
           }

           // 允许同源请求
           host := r.Header.Get("Host")
           if host == "" {
               host = r.Host
           }
           if strings.HasPrefix(origin, "http://"+host) || strings.HasPrefix(origin, "https://"+host) {
               return true
           }

           // 允许 localhost（开发环境）
           if strings.HasPrefix(origin, "http://localhost") || strings.HasPrefix(origin, "http://127.0.0.1") {
               return true
           }

           // 检查白名单
           for _, allowed := range allowedOrigins {
               if origin == allowed || strings.HasPrefix(origin, allowed) {
                   return true
               }
           }

           // 记录拒绝的来源（安全审计）
           applogger.Warnf("WebSocket 连接被拒绝: origin=%s, client_ip=%s",
               origin, r.RemoteAddr)
           return false
       }
       ```

    3. 修改要点：
       - 添加 allowAll 模式的警告日志
       - 在拒绝连接时记录 origin 和 client_ip
       - 使用 Warnf 级别记录安全事件

    4. 确保 applogger 包已导入

    5. **TDD 循环**：实现后运行测试，移除测试中的 Skip 标记：
       ```bash
       go test -v -run TestWebSocketUpgrader_CheckOrigin ./internal/api/v1/
       ```
  </action>
  <verify>
    <automated>go test -run TestWebSocketUpgrader_CheckOrigin ./internal/api/v1/... 2>&1 | grep -E "(PASS|FAIL)" | head -1</automated>
  </verify>
  <done>
    WebSocket CheckOrigin 验证已增强，所有相关测试从 Skip 转为 Pass
  </done>
  <acceptance_criteria>
    - CheckOrigin 函数实现完成，包含所有验证逻辑
    - 拒绝连接时记录 origin 和 client_ip 到日志
    - allowAll 模式有警告日志
    - Wave 0 测试全部通过（无 Skip）
    - go build 通过，无编译错误
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 GlobalDeviceMonitorService 并发安全（GREEN 阶段）</name>
  <files>internal/scheduler/cron.go</files>
  <read_first>
    1. internal/scheduler/cron.go - 查看 GlobalDeviceMonitorService 实现
    2. .planning/phases/09-backend-cleanup/09-00-PLAN.md - 查看测试期望行为
    3. internal/scheduler/cron_test.go - 查看并发测试用例
    4. .planning/phases/08-snmp-panic/08-CONTEXT.md - RWMutex 并发安全模式参考
  </read_first>
  <behavior>
    实现后应满足以下行为（来自 Wave 0 测试）：
    - 并发读写不发生数据竞争（-race 检测通过）
    - 并发调用 GetDeviceMonitorService() 返回正确值
    - 并发调用 SetDeviceMonitorService() 不导致 panic
    - 获取 nil 服务返回 nil 而非 panic
  </behavior>
  <action>
    实现 GlobalDeviceMonitorService 并发安全（使 Wave 0 测试通过）：

    1. 读取 GlobalDeviceMonitorService 实现（行 899-1035）

    2. 验证/实现 RWMutex 保护：
       ```go
       var (
           GlobalDeviceMonitorService        DeviceMonitorService
           GlobalDeviceMonitorServiceMu      sync.RWMutex
       )

       func SetDeviceMonitorService(service DeviceMonitorService) {
           GlobalDeviceMonitorServiceMu.Lock()
           defer GlobalDeviceMonitorServiceMu.Unlock()
           GlobalDeviceMonitorService = service
       }

       func GetDeviceMonitorService() DeviceMonitorService {
           GlobalDeviceMonitorServiceMu.RLock()
           defer GlobalDeviceMonitorServiceMu.RUnlock()
           return GlobalDeviceMonitorService
       }
       ```

    3. 如果实现不完整，添加：
       - 添加 GlobalDeviceMonitorServiceMu sync.RWMutex 变量
       - 在 SetDeviceMonitorService() 中添加 Lock/Unlock
       - 在 GetDeviceMonitorService() 中添加 RLock/RUnlock

    4. **TDD 循环**：实现后运行并发测试：
       ```bash
       # 先运行普通测试
       go test -v -run TestGlobalDeviceMonitorService ./internal/scheduler/

       # 再运行竞态检测
       go test -race -run TestGlobalDeviceMonitorService_ConcurrentAccess ./internal/scheduler/
       ```
  </action>
  <verify>
    <automated>go test -race -run TestGlobalDeviceMonitorService_ConcurrentAccess ./internal/scheduler/... 2>&1 | grep -c "WARNING: DATA RACE" || echo "0"</automated>
  </verify>
  <done>
    GlobalDeviceMonitorService 并发安全已实现，所有并发测试从 Skip 转为 Pass，无数据竞争
  </done>
  <acceptance_criteria>
    - GlobalDeviceMonitorServiceMu 已定义
    - GetDeviceMonitorService() 使用 RLock()/RUnlock()
    - SetDeviceMonitorService() 使用 Lock()/Unlock()
    - Wave 0 并发测试通过
    - go test -race 无数据竞争警告
    - go build 通过，无编译错误
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现登录错误日志（GREEN 阶段）</name>
  <files>internal/api/v1/auth.go</files>
  <read_first>
    1. internal/api/v1/auth.go - 查看 recordLoginLog 函数实现
    2. .planning/phases/09-backend-cleanup/09-00-PLAN.md - 查看测试期望行为
    3. internal/api/v1/auth_test.go - 查看错误日志测试
    4. .planning/phases/09-backend-cleanup/09-RESEARCH.md - 错误日志添加模式
  </read_first>
  <behavior>
    实现后应满足以下行为（来自 Wave 0 测试）：
    - DB 错误时记录 Errorf 级别日志
    - 错误日志包含 username, clientIP, status 上下文
    - 异步执行不阻塞登录流程
    - 函数不会在 DB 错误时 panic
  </behavior>
  <action>
    实现登录错误日志记录（使 Wave 0 测试通过）：

    1. 读取 recordLoginLog 函数实现（行 409-432）

    2. 增强错误日志：
       ```go
       func recordLoginLog(c *gin.Context, core *core.Core, username string, status int, msg string) {
           clientIP := utils.GetClientIP(c)  // 在开头捕获
           userAgent := c.Request.UserAgent()
           // ... 省略代码 ...

           go func() {
               if err := core.DB.GetDB().Create(&loginLog).Error; err != nil {
                   applogger.Errorf("记录登录日志失败 (user: %s, ip: %s, status: %d): %v",
                       username, clientIP, status, err)
               }
           }()
       }
       ```

    3. 修改要点：
       - 将 Warnf 改为 Errorf（关键操作失败）
       - 添加上下文信息（username, clientIP, status）
       - 保持 goroutine 异步执行
       - 确保 clientIP 在函数开头捕获

    4. **TDD 循环**：实现后运行测试：
       ```bash
       go test -v -run TestRecordLoginLog_ErrorLogging ./internal/api/v1/
       ```
       注意：由于测试使用 mock/ Skip，主要验证不 panic 和日志级别正确
  </action>
  <verify>
    <automated>grep -A 3 "记录登录日志失败" internal/api/v1/auth.go | grep -c "Errorf"</automated>
  </verify>
  <done>
    登录错误日志已添加，使用 Errorf 级别和详细上下文，测试从 Skip 转为 Pass
  </done>
  <acceptance_criteria>
    - recordLoginLog 函数包含 applogger.Errorf
    - 错误日志包含 username, clientIP, status
    - clientIP 在函数开头捕获
    - Wave 0 测试通过
    - go build 通过，无编译错误
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>三个安全问题已修复，Wave 0 测试从 Skip 全部转为 Pass</what-built>
  <how-to-verify>
    1. 验证 Wave 0 测试状态：
       ```bash
       # WebSocket CheckOrigin 测试
       go test -v -run TestWebSocketUpgrader_CheckOrigin ./internal/api/v1/

       # 并发安全测试
       go test -v -run TestGlobalDeviceMonitorService ./internal/scheduler/

       # 错误日志测试
       go test -v -run TestRecordLoginLog_ErrorLogging ./internal/api/v1/
       ```
       预期：所有测试 PASS，无 SKIP

    2. 验证 WebSocket CheckOrigin 安全日志：
       ```bash
       # 启动服务
       .\xingran-backend.exe

       # 从不同域名尝试 WebSocket 连接
       # 检查日志中是否有 "WebSocket 连接被拒绝" 的警告
       ```

    3. 验证并发安全：
       ```bash
       # 运行竞态检测
       go test -race ./internal/scheduler/...
       ```
       预期：无 DATA RACE 警告

    4. 验证登录错误日志：
       ```bash
       # 登录时故意触发错误（如停止数据库）
       # 检查日志中是否有 "记录登录日志失败" 的 Errorf 日志
       ```

    5. 运行完整测试套件：
       ```bash
       go test -race ./...
       ```
  </how-to-verify>
  <resume-signal>输入 "approved" 确认所有安全修复正常工作，或描述发现的问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → WebSocket | CheckOrigin 验证防止 CSRF 攻击 |
| Concurrent → GlobalDeviceMonitorService | RWMutex 防止竞态条件 |
| Auth → Database | 错误日志记录失败事件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | Spoofing | WebSocket CheckOrigin | mitigate | 增强 Origin 验证，添加安全日志（Task 1） |
| T-9-02 | Tampering | GlobalDeviceMonitorService | mitigate | RWMutex 保护并发访问（Task 2） |
| T-9-03 | Information Disclosure | 登录错误日志 | mitigate | 添加 Errorf 级别日志，包含上下文（Task 3） |

**Notes:**
- 三个安全修复都有明确的威胁模型
- 所有修复都在 Wave 0 有测试覆盖
- 使用 TDD 方法：测试先行（Wave 0），实现后测试通过（本计划 GREEN 阶段）
- 用户决策 D-05（编写单元测试验证安全修复）已通过 Wave 0 + 本计划实现
</threat_model>

<verification>
## 整体验证标准

1. **WebSocket CheckOrigin 验证**：
   - Wave 0 单元测试覆盖所有分支并 PASS
   - 安全日志正确记录拒绝事件
   - 生产环境不允许 "*" 通配符（或记录警告）

2. **GlobalDeviceMonitorService 并发安全**：
   - RWMutex 正确保护全局变量
   - Wave 0 并发测试通过
   - -race 检测无数据竞争
   - nil 安全性验证通过

3. **登录错误日志**：
   - Errorf 级别日志正确添加
   - 包含必要的上下文信息
   - 异步执行不阻塞登录流程
   - Wave 0 测试通过

4. **TDD 合规性**：
   - 所有测试在 Wave 0 创建（RED 阶段）
   - 实现后测试从 Skip 转为 Pass（GREEN 阶段）
   - 满足用户决策 D-05 要求
</verification>

<success_criteria>
1. WebSocket CheckOrigin 验证逻辑已增强，包含安全日志
2. GlobalDeviceMonitorService 并发安全已实现
3. 登录错误日志已添加，使用 Errorf 级别
4. 所有 Wave 0 测试从 Skip 转为 Pass
5. go test -race ./... 通过，无数据竞争
6. 用户决策 D-05（编写单元测试验证安全修复）已满足
</success_criteria>

<output>
After completion, create `.planning/phases/09-backend-cleanup/09-03-SUMMARY.md`
</output>
