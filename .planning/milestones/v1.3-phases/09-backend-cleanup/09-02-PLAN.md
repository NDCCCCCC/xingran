---
phase: 09-backend-cleanup
plan: 02
type: execute
wave: 2
depends_on: [09-01A]
files_modified:
  - internal/core/core.go
autonomous: true
requirements:
  - CODE-02b
user_setup: []
must_haves:
  truths:
    - "Core 结构中 12 个死字段已删除或保留理由明确"
    - "RPAScalingService 已提取为接口或确认保留原因"
    - "所有字段的外部引用已验证并清理"
    - "Core 初始化功能正常"
    - "构建和测试通过"
  artifacts:
    - path: "internal/core/core.go"
      provides: "Core 结构定义"
      min_lines: 80
      contains: "type Core struct"
    - path: ".planning/phases/09-backend-cleanup/09-02-SUMMARY.md"
      provides: "字段处理报告"
      contains: "RPAScalingService|字段处理结果"
  key_links:
    - from: "internal/api/router.go"
      to: "core.TokenBlacklistService"
      via: "middleware 引用"
      pattern: "core\\.TokenBlacklistService"
    - from: "internal/api/v1/auth.go"
      to: "core.CaptchaService"
      via: "验证码服务引用"
      pattern: "core\\.CaptchaService"
    - from: "internal/api/v1/captcha_handler.go"
      to: "core.CaptchaService"
      via: "验证码生成引用"
      pattern: "core\\.CaptchaService"
---

<objective>
清理 Core 结构中的 12 个死字段，按依赖关系分组渐进式删除。特别处理 RPAScalingService：提取为接口或明确保留理由。每步验证构建通过，确保 Core 结构清晰，依赖明确。

Purpose: 简化 Core 结构，移除未使用的字段，处理 RPAScalingService 接口问题，提升代码可维护性
Output: 清理后的 Core 结构，RPAScalingService 已处理，删除可安全删除的死字段
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-backend-cleanup/09-CONTEXT.md
@.planning/phases/09-backend-cleanup/09-RESEARCH.md
@.planning/phases/09-backend-cleanup/09-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@internal/core/core.go
@internal/api/router.go
@internal/api/v1/auth.go
@internal/api/v1/captcha_handler.go
@internal/api/v1/captcha_background_handler.go
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 分析 Core 字段使用并处理 RPAScalingService</name>
  <files>internal/core/core.go</files>
  <read_first>
    1. internal/core/core.go - 完整阅读 Core 结构定义和所有方法
    2. .planning/phases/09-backend-cleanup/09-RESEARCH.md - Core 字段分组分析
    3. .planning/REQUIREMENTS.md - CODE-02b 需求定义
    4. .planning/phases/09-backend-cleanup/09-PATTERNS.md - 渐进式删除模式
  </read_first>
  <action>
    分析 Core 结构中所有字段的使用情况，特别处理 RPAScalingService：

    1. 完整读取 internal/core/core.go 文件（行 1-400+）
       - 记录所有字段定义
       - 记录所有方法中对字段的引用
       - 识别哪些字段仅在 Init() 方法中使用

    2. **特别处理 RPAScalingService**（响应 CODE-02b 需求）：
       ```bash
       # 查找 RPAScalingService 的使用位置
       grep -rn "RPAScalingService\|rpaScalingService" --include="*.go" internal/
       ```

       根据查找结果决定处理方式：
       - **情况 A：未使用** → 直接删除字段和初始化代码
       - **情况 B：外部使用** → 检查是否应为接口，定义接口：
         ```go
         // 在 internal/services/rpa/ 或合适位置创建接口
         type RPAScalingService interface {
             ScaleUp(ctx context.Context, count int) error
             ScaleDown(ctx context.Context, count int) error
             GetStatus(ctx context.Context) (*ScalingStatus, error)
         }

         // Core 中使用接口类型
         RPAScalingService RPAScalingService  // 接口，非具体类型
         ```
       - **情况 C：仅内部使用** → 转为局部变量或保留并添加说明注释

    3. 检查其他 11 个字段的外部引用：
       - DeviceManager *device.Manager
       - DeviceDiscoveryService *services.DeviceDiscoveryService
       - DeviceInfoCollectionService *services.DeviceInfoCollectionService
       - DeviceMonitorService *services.DeviceMonitorService
       - MetricsCacheService *MetricsCacheService
       - CaptchaService *CaptchaService
       - CaptchaBackgroundService *CaptchaBackgroundService
       - OperLogService services.OperLogService
       - TokenBlacklistService services.TokenBlacklistService
       - APIMetadata *config.APIMetadataConfig
       - APIEndpointService *services.APIEndpointService

    4. 对每个字段运行 grep 检查外部引用：
       ```bash
       grep -rn "core\.FieldName" --include="*.go" internal/ | grep -v "^internal/core/core.go"
       ```

    5. 根据使用情况将字段分组：
       - **组 A：完全未使用**（可安全删除）
       - **组 B：仅在内部使用**（可转为局部变量）
       - **组 C：外部使用**（需要保留或重构）

    6. 输出分组结果和删除计划，包含 RPAScalingService 处理决定
  </action>
  <verify>
    <automated>grep -rn "RPAScalingService\|core\\.DeviceManager" --include="*.go" internal/ | grep -v "^internal/core/core.go" | wc -l</automated>
  </verify>
  <done>
    完成 Core 字段分析，RPAScalingService 已处理（接口提取/删除/保留），其他字段分组完成
  </done>
  <acceptance_criteria>
    - Core 结构完整读取并理解
    - RPAScalingService 处理决定已输出到 SUMMARY.md，包含：
      - 选择的情况（A/B/C 之一）
      - 选择理由（未使用/外部使用/仅内部使用）
      - 如选择 B（提取接口）：包含接口定义草案
      - 如选择 C（保留）：包含详细保留理由和用途说明
    - 每个其他字段都经过 grep 外部引用检查
    - 字段分组完成（组 A/B/C 明确，每组包含具体字段列表）
    - 删除顺序已确定
    - SUMMARY.md 包含字段处理结果表（字段名 | 组别 | 处理方式 | 理由）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 删除或转换组 A 和组 B 字段</name>
  <files>internal/core/core.go</files>
  <read_first>
    1. internal/core/core.go - 定位要删除的字段
    2. Task 1 的分析结果 - 确认组 A 和组 B 字段列表
    3. .planning/phases/09-backend-cleanup/09-PATTERNS.md - 缓存预热服务转换模式
  </read_first>
  <action>
    删除或转换组 A（完全未使用）和组 B（仅在内部使用）的字段：

    **组 A：完全未使用的字段 - 直接删除**
    1. 根据 Task 1 分析，确定组 A 字段（可能包括）：
       - DeviceManager *device.Manager（如果确认无引用）
       - 其他完全未使用的字段

    2. 编辑 internal/core/core.go：
       - 找到字段定义（约在行 52-74）
       - 删除整行字段定义
       - 删除相关注释

    3. 如果字段在 Init() 方法中被赋值，同时删除：
       - 删除字段初始化代码
       - 删除相关服务创建代码
       - 删除相关 import（如果无其他用途）

    **组 B：仅在内部使用的字段 - 转为局部变量**
    1. 根据 Task 1 分析，确定组 B 字段（可能包括）：
       - MetricsCacheService *MetricsCacheService
       - 其他仅用于缓存预热的字段

    2. 转换模式（缓存预热服务）：
       ```go
       // 删除字段定义

       // 在 Init() 方法中创建为局部变量
       func (c *Core) Init() error {
           // ... 其他初始化 ...

           // 创建缓存预热服务（局部变量）
           metricsCacheService := NewMetricsCacheService(c.Cache)

           // 传递给 warmUpCache
           c.warmUpCache(context.Background(), metricsCacheService)
       }

       // 修改 warmUpCache 方法签名
       func (c *Core) warmUpCache(ctx context.Context, metricsCacheSvc *MetricsCacheService) {
           // 使用参数而非字段
       }
       ```

    3. 保存文件后立即验证构建
  </action>
  <verify>
    <automated>go build ./... 2>&1 | grep -c "error\|undefined" || echo "0"</automated>
  </verify>
  <done>
    组 A 和组 B 字段已删除或转换，go build ./... 通过
  </done>
  <acceptance_criteria>
    - Core 结构中组 A 字段已删除
    - Core 结构中组 B 字段已转为局部变量
    - Init() 和 warmUpCache() 方法相应调整
    - go build ./... 成功，无编译错误
    - 无 undefined 引用错误
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 处理组 C 字段并清理 import</name>
  <files>internal/core/core.go,internal/api/router.go,internal/api/v1/auth.go</files>
  <read_first>
    1. internal/core/core.go - 定位组 C 字段
    2. Task 1 的分析结果 - 确认组 C 字段和引用位置
    3. .planning/phases/09-backend-cleanup/09-RESEARCH.md - 字段使用分析
  </read_first>
  <action>
    处理组 C（外部使用）的字段，并清理不再需要的 import：

    **组 C 字段处理**
    1. 根据 Task 1 分析，确定组 C 字段（可能包括）：
       - CaptchaService *CaptchaService（被 auth.go 使用）
       - TokenBlacklistService services.TokenBlacklistService（被 middleware 使用）
       - 其他被外部引用的字段

    2. 对每个字段，确认使用情况：
       - 如果确实被外部使用 → 保留字段，添加注释说明用途
       - 如果使用可以优化 → 标记为待优化（未来阶段）
       - 示例注释：
         ```go
         // CaptchaService 验证码服务，被 auth.go 和 captcha_handler.go 引用
         CaptchaService *CaptchaService

         // TokenBlacklistService Token 黑名单服务，被 middleware 引用
         TokenBlacklistService services.TokenBlacklistService
         ```

    **清理 import**
    1. 检查 internal/core/core.go 的 import 块（行 1-30）

    2. 识别不再需要的 import（基于 Task 2 删除的字段）：
       - device 包（如果 DeviceManager 删除）
       - services.DeviceDiscoveryService（如果删除）
       - services.DeviceInfoCollectionService（如果删除）
       - services.DeviceMonitorService（如果删除）
       - 其他已删除字段相关的 import

    3. 验证 import 是否仍被使用：
       - 搜索 import 的使用位置
       - 如果仅在类型注释中使用，可以删除
       - 如果仍在代码中使用，保留 import

    4. 删除不再需要的 import

    5. **处理 RPAScalingService**（如果之前未处理）：
       - 如果提取为接口 → 更新类型声明
       - 如果删除 → 清理相关代码
       - 如果保留 → 添加详细注释说明原因

    6. 保存文件后立即验证构建
  </action>
  <verify>
    <automated>go build ./... 2>&1 | grep -c "imported and not used\|undefined" || echo "0"</automated>
  </verify>
  <done>
    组 C 字段处理完成，import 清理完成，go build ./... 通过
  </done>
  <acceptance_criteria>
    - 每个组 C 字段都有明确保留理由（注释）
    - 无 "imported and not used" 错误
    - 所有必需的 import 都保留
    - RPAScalingService 已处理（接口/删除/保留+注释）
    - go build ./... 成功
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Core 结构已清理，12 个死字段已删除/转换/保留，RPAScalingService 已处理</what-built>
  <how-to-verify>
    1. 查看清理后的 Core 结构：
       ```bash
       grep -A 30 "type Core struct" internal/core/core.go
       ```

    2. 确认字段变化：
       - 对比清理前后的字段数量
       - 确认死字段已删除
       - 确认必要字段保留并有注释

    3. 验证 RPAScalingService 处理：
       ```bash
       grep -n "RPAScalingService" internal/core/core.go
       ```
       - 如果存在：检查是否有接口定义或详细注释
       - 如果不存在：确认已安全删除

    4. 验证系统功能：
       ```bash
       # 启动服务
       .\xingran-backend.exe

       # 检查日志，确认 Core 初始化成功
       # 检查关键功能（登录、验证码等）正常
       ```

    5. 如果发现问题：
       - 记录错误信息
       - 分析是否与字段删除相关
       - 必要时恢复某些字段
  </how-to-verify>
  <resume-signal>输入 "approved" 确认清理成功，或描述发现的问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Core → Services | Core 字段删除可能影响服务初始化顺序 |
| RPAScalingService → 接口 | 类型变更可能影响外部引用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-04 | Tampering | Core 结构修改 | mitigate | 渐进式删除 + 构建验证，每步都验证 |
| T-9-05 | Denial of Service | Core 初始化失败 | mitigate | 保留必要字段，测试验证初始化流程 |
| T-9-06 | Information Disclosure | 字段删除导致配置泄露 | accept | 无敏感信息影响 |
| T-9-07 | Spoofing | RPAScalingService 接口类型 | mitigate | 接口定义清晰，实现验证 |

**Notes:**
- 主要风险是删除字段导致初始化失败
- RPAScalingService 处理需要特别小心，确保类型正确
- 所有修改都可从 git 历史恢复
</threat_model>

<verification>
## 整体验证标准

1. **Core 结构验证**：
   - 死字段已删除或转为局部变量
   - 必要字段保留并有注释说明
   - RPAScalingService 已处理（接口/删除/保留+理由）

2. **CODE-02b 需求覆盖**：
   - 12 个死字段已处理
   - RPAScalingService 已提取为接口或明确保留理由
   - Init() 方法已简化

3. **构建验证**：
   - `go build ./...` 成功
   - 无编译错误

4. **功能验证**：
   - Core 初始化正常
   - 服务依赖正确
   - 系统功能无回归
</verification>

<success_criteria>
1. Core 结构中 12 个死字段已删除/转换/保留
2. RPAScalingService 已处理（接口提取或明确保留理由）
3. 保留的字段有明确使用理由（注释）
4. go build ./... 构建通过
5. go test ./... 测试通过
6. Core 初始化流程正常
7. 系统功能无回归
8. CODE-02b 需求完全覆盖
</success_criteria>

<output>
After completion, create `.planning/phases/09-backend-cleanup/09-02-SUMMARY.md` with RPAScalingService handling details
</output>
