---
phase: 10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/v1/network/network_export_handler.go
  - internal/api/v1/network/network_router.go
autonomous: true
requirements:
  - EXPORT-03a
  - EXPORT-03b
  - EXPORT-03c
user_setup: []
must_haves:
  truths:
    - "User can click '批量导出' button on any network management page"
    - "Modal opens showing all 9 entity types with checkboxes (all pre-selected)"
    - "User can select/deselect entity types to export"
    - "Clicking '确认导出 (N)' downloads a ZIP file containing selected entity Excel files"
    - "Each Excel file is named after the entity type (e.g., '网络设备.xlsx')"
    - "ZIP filename is '网络管理_批量导出_{timestamp}.zip'"
  artifacts:
    - path: "internal/api/v1/network/network_export_handler.go"
      provides: "BatchExport handler method"
      exports: ["BatchExport"]
      contains: "func (h *NetworkExportHandler) BatchExport(c *gin.Context)"
    - path: "internal/api/v1/network/network_router.go"
      provides: "Batch export route registration"
      contains: "POST /batch-export"
    - path: "xingran-react-frontend/src/components/shared/BatchExportModal.tsx"
      provides: "Batch export modal component"
      exports: ["BatchExportModal"]
      min_lines: 80
    - path: "xingran-react-frontend/src/components/shared/index.ts"
      provides: "BatchExportModal export"
      contains: "export { BatchExportModal }"
  key_links:
    - from: "BatchExportModal component"
      to: "/api/v1/network/batch-export"
      via: "fetch with POST method"
      pattern: "fetch.*batch-export"
    - from: "network_export_handler.go:BatchExport"
      to: "archive/zip package"
      via: "zip.NewWriter"
      pattern: "zip\\.NewWriter"
    - from: "BatchExport handler"
      to: "existing export functions"
      via: "calling existing 9 export methods"
      pattern: "(ExportDevices|ExportCredentials|ExportTemplates)"
---

<objective>
实现网络管理批量导出功能，支持一次性导出多个实体类型的 Excel 文件并打包为 ZIP 下载。

Purpose: 根据 D-04 决策，实现全类型批量导出功能。用户可以通过批量导出按钮选择要导出的实体类型，系统将所有选中的 Excel 文件打包为单个 ZIP 文件下载。这提升了批量管理效率，避免了用户逐个导出的繁琐操作。

Output:
- 后端: 新增 `POST /api/v1/network/batch-export` 端点，接收实体类型列表，返回 ZIP 文件流
- 前端: 新增 `BatchExportModal` 组件，集成到所有 9 个网络管理页面工具栏
</objective>

<execution_context>
@$HOME/.claude/get-shit-down/workflows/execute-plan.md
@$HOME/.claude/get-shit-down/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-network-export/10-CONTEXT.md
@.planning/phases/10-network-export/10-UI-SPEC.md

# 现有代码模式
@internal/api/v1/network/network_export_handler.go
@internal/api/v1/network/network_router.go
@xingran-react-frontend/src/components/shared/NetworkExport.tsx
@xingran-react-frontend/src/pages/network/devices/index.tsx

<interfaces>
<!-- 现有导出处理器接口 -->
From internal/api/v1/network/network_export_handler.go:
```go
type NetworkExportHandler struct {
    core *core.Core
}

func NewNetworkExportHandler(core *core.Core) *NetworkExportHandler

// 9 个现有导出方法
func (h *NetworkExportHandler) ExportDevices(c *gin.Context)
func (h *NetworkExportHandler) ExportCredentials(c *gin.Context)
func (h *NetworkExportHandler) ExportTemplates(c *gin.Context)
func (h *NetworkExportHandler) ExportCommands(c *gin.Context)
func (h *NetworkExportHandler) ExportExecutions(c *gin.Context)
func (h *NetworkExportHandler) ExportBackups(c *gin.Context)
func (h *NetworkExportHandler) ExportDiscoveries(c *gin.Context)
func (h *NetworkExportHandler) ExportMACAddresses(c *gin.Context)
func (h *NetworkExportHandler) ExportPorts(c *gin.Context)
```

<!-- 现有前端导出组件模式 -->
From xingran-react-frontend/src/components/shared/NetworkExport.tsx:
```typescript
export interface NetworkExportProps {
  exportUrl: string;          // 导出API地址
  entityName: string;         // 实体名称
  filters?: Record<string, any>; // 当前筛选条件
  current?: number;           // 当前页码
  pageSize?: number;          // 每页条数
}

// 下载处理模式：fetch -> blob -> URL.createObjectURL -> dynamic <a> tag
// 错误处理：message.success/error
// 加载状态：useState + Button loading
```

<!-- 表格行选择模式 -->
From xingran-react-frontend/src/pages/network/devices/index.tsx:
```typescript
// useTableManager 提供 selectedRowKeys 状态
const { selectedRowKeys, setSelectedRowKeys, resetSelection } = useTableManager<NetworkDevice>(...)

// Table rowSelection 配置
<Table
  rowSelection={{
    selectedRowKeys,
    onChange: setSelectedRowKeys,
  }}
  ...
/>

// 批量操作按钮条件渲染
{selectedRowKeys.length > 0 && (
  <Button onClick={handleBatchDelete}>批量删除 ({selectedRowKeys.length})</Button>
)}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现后端批量导出端点</name>
  <files>internal/api/v1/network/network_export_handler.go, internal/api/v1/network/network_router.go</files>
  <read_first>
    - internal/api/v1/network/network_export_handler.go (阅读现有 9 个导出方法的实现模式)
    - internal/api/v1/network/network_router.go (了解路由注册模式)
    - archive/zip 包文档 (Go 标准库)
  </read_first>
  <action>
    在 `network_export_handler.go` 中添加 `BatchExport` 方法：
    
    1. 定义请求结构体：
    ```go
    type BatchExportRequest struct {
        EntityTypes []string `json:"entityTypes" binding:"required,min=1,max=9"`
        Filters     map[string]interface{} `json:"filters"`
    }
    ```
    
    2. 实体类型映射（key -> 导出函数名 + 文件名）：
    ```go
    var entityExportMap = map[string]struct{
        exportFunc func(*gin.Context)
        filename string
    }{
        "devices":     {h.ExportDevices, "网络设备"},
        "credentials": {h.ExportCredentials, "授权凭证"},
        "templates":   {h.ExportTemplates, "配置模板"},
        "commands":    {h.ExportCommands, "命令分发"},
        "executions":  {h.ExportExecutions, "配置执行"},
        "backups":     {h.ExportBackups, "配置备份"},
        "discoveries": {h.ExportDiscoveries, "设备发现"},
        "mac":         {h.ExportMACAddresses, "MAC地址"},
        "ports":       {h.ExportPorts, "端口状态"},
    }
    ```
    
    3. 核心逻辑：
       - 验证 entityTypes 是否都在 entityExportMap 中
       - 创建 zip.Writer，设置响应头 `Content-Type: application/zip` 和 `Content-Disposition: attachment; filename="网络管理_批量导出_{timestamp}.zip"`
       - 对每个实体类型：
         * 创建临时 buffer 调用现有导出函数
         * 将生成的 Excel 文件写入 ZIP（文件名：`{entityName}.xlsx`）
         * 处理错误：任何单个导出失败则整个批量导出失败
       - 设置最大记录数限制（100,000）和 ZIP 大小限制（100MB）
       - 超出限制返回错误："数据量过大，请缩小筛选范围后重试"
    
    4. 在 `network_router.go` 中注册路由：
    ```go
    r.POST("/batch-export", exportHandler.BatchExport)
    ```
    
    关键约束：
    - 必须使用 `archive/zip` 标准库创建 ZIP 文件
    - 必须复用现有 9 个导出函数，不能重新实现业务逻辑
    - ZIP 内部文件名必须是中文名称（如 `网络设备.xlsx`）
    - ZIP 文件名格式：`网络管理_批量导出_20060102_150405.zip`
    - 所有 9 个实体类型必须支持：devices, credentials, templates, commands, executions, backups, discoveries, mac, ports
  </action>
  <verify>
    <automated>
      go test -v -run TestBatchExport ./internal/api/v1/network/
    </automated>
  </verify>
  <done>
    - BatchExport 方法实现完成
    - 路由已注册到 `/api/v1/network/batch-export`
    - 手动测试：发送 POST 请求，entityTypes=["devices", "credentials"]，验证返回 ZIP 文件包含两个 Excel 文件
    - 验证 ZIP 文件名格式正确
    - 验证 ZIP 内部文件名正确
    - 验证传入无效 entityTypes 时返回 400 错误
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 BatchExportModal 前端组件</name>
  <files>xingran-react-frontend/src/components/shared/BatchExportModal.tsx, xingran-react-frontend/src/components/shared/index.ts</files>
  <read_first>
    - xingran-react-frontend/src/components/shared/NetworkExport.tsx (参考下载处理和错误处理模式)
    - xingran-react-frontend/src/pages/network/command/modals/DispatchModal.tsx (参考 Modal 组件结构)
    - .planning/phases/10-network-export/10-UI-SPEC.md (UI 设计规范)
  </read_first>
  <action>
    创建 `BatchExportModal.tsx` 组件，规格如下：
    
    1. 接口定义：
    ```typescript
    export interface BatchExportModalProps {
      visible: boolean;
      onConfirm: (entityTypes: string[]) => Promise<void>;
      onCancel: () => void;
      loading: boolean;
      availableEntityTypes?: EntityType[];
    }

    export interface EntityType {
      key: string;
      label: string;
    }
    ```
    
    2. 组件实现：
       - 使用 Ant Design Modal、Checkbox.Group、Space 组件
       - 默认全选所有 9 个实体类型
       - 提供"全选"和"清空"快捷按钮
       - 确认按钮显示选中数量：`确认导出 (${selectedCount})`
       - 禁用状态：`loading=true` 或 `selectedCount=0`
       - 确认按钮：type="primary", icon={<DownloadOutlined />}
       - 取消按钮：`disabled={loading}`
       - Modal 宽度：600px（桌面），90vw（移动）
       - Checkbox 布局：桌面 3 列，平板 2 列，移动 1 列
    
    3. 9 个实体类型定义：
    ```typescript
    const DEFAULT_ENTITY_TYPES: EntityType[] = [
      { key: 'devices', label: '网络设备' },
      { key: 'credentials', label: '授权凭证' },
      { key: 'templates', label: '命令模板' },
      { key: 'commands', label: '执行记录' },
      { key: 'executions', label: '配置执行' },
      { key: 'backups', label: '配置备份' },
      { key: 'discoveries', label: '自动发现' },
      { key: 'mac', label: 'MAC地址' },
      { key: 'ports', label: '端口采集' },
    ];
    ```
    
    4. 导出处理（参考 NetworkExport.tsx）：
       - 使用 `fetch` 调用 `/api/v1/network/batch-export`
       - 请求体：`{ entityTypes: string[], filters?: Record<string, any> }`
       - 从响应头解析文件名（`Content-Disposition`）
       - 创建 blob URL，动态生成 `<a>` 标签触发下载
       - 成功：`message.success('批量导出成功，文件: {filename}')`，关闭 Modal
       - 失败：`message.error('批量导出失败：{error}')`，保持 Modal 打开
    
    5. 在 `shared/index.ts` 中导出：
    ```typescript
    export { BatchExportModal } from './BatchExportModal';
    export type { BatchExportModalProps, EntityType } from './BatchExportModal';
    ```
    
    关键约束：
    - 必须复用 NetworkExport.tsx 的下载处理模式
    - 必须遵循 UI-SPEC.md 中的交互流程和文案规范
    - 必须支持响应式布局（3/2/1 列）
    - 必须提供键盘导航支持（Tab, Enter, Escape）
  </action>
  <verify>
    <automated>
      cd xingran-react-frontend && npm run type-check
    </automated>
  </verify>
  <done>
    - BatchExportModal.tsx 文件创建完成
    - TypeScript 类型检查通过
    - shared/index.ts 导出 BatchExportModal
    - 手动验证：Modal 正确显示所有 9 个实体类型
    - 验证全选/清空按钮功能
    - 验证确认按钮显示正确数量
  </done>
</task>

<task type="auto">
  <name>Task 3: 集成 BatchExportModal 到网络管理页面</name>
  <files>
    xingran-react-frontend/src/pages/network/devices/index.tsx,
    xingran-react-frontend/src/pages/network/credentials/index.tsx,
    xingran-react-frontend/src/pages/network/templates/index.tsx,
    xingran-react-frontend/src/pages/network/command/index.tsx,
    xingran-react-frontend/src/pages/network/executions/index.tsx,
    xingran-react-frontend/src/pages/network/backups/index.tsx,
    xingran-react-frontend/src/pages/network/discoveries/index.tsx,
    xingran-react-frontend/src/pages/network/mac/index.tsx,
    xingran-react-frontend/src/pages/network/ports/index.tsx
  </files>
  <read_first>
    - xingran-react-frontend/src/pages/network/devices/index.tsx (参考批量删除按钮的集成模式)
    - .planning/phases/10-network-export/10-UI-SPEC.md (UI 设计规范 - 批量导出按钮位置)
  </read_first>
  <action>
    在所有 9 个网络管理页面集成批量导出功能：
    
    1. 导入 BatchExportModal：
    ```typescript
    import { BatchExportModal } from '@/components/shared';
    import { DownloadOutlined } from '@ant-design/icons';
    ```
    
    2. 添加状态管理：
    ```typescript
    const [batchModalVisible, setBatchModalVisible] = useState(false);
    const [batchExporting, setBatchExporting] = useState(false);
    const [selectedEntityTypes, setSelectedEntityTypes] = useState<string[]>(DEFAULT_ENTITY_TYPES.map(e => e.key));
    ```
    
    3. 添加批量导出处理函数：
    ```typescript
    const handleBatchExport = async (entityTypes: string[]) => {
      setBatchExporting(true);
      try {
        const token = await getAccessToken();
        const response = await fetch('/api/v1/network/batch-export', {
          method: 'POST',
          headers: {
            Authorization: `Bearer ${token}`,
            'Content-Type': 'application/json',
          },
          body: JSON.stringify({
            entityTypes,
            filters: currentFilters, // 使用当前页面筛选条件
          }),
        });

        if (!response.ok) throw new Error('导出失败');

        const disposition = response.headers.get('content-disposition');
        let filename = `网络管理_批量导出_${Date.now()}.zip`;
        if (disposition) {
          const match = disposition.match(/filename[^;=\n]*=((['"]).*?\2|[^;\n]*)/);
          if (match?.[1]) filename = decodeURIComponent(match[1].replace(/['"]/g, ''));
        }

        const blob = await response.blob();
        const url = window.URL.createObjectURL(blob);
        const a = document.createElement('a');
        a.href = url;
        a.download = filename;
        document.body.appendChild(a);
        a.click();
        window.URL.revokeObjectURL(url);
        document.body.removeChild(a);

        message.success(`批量导出成功，文件: ${filename}`);
        setBatchModalVisible(false);
      } catch (error: any) {
        message.error(`批量导出失败：${error.message}`);
      } finally {
        setBatchExporting(false);
      }
    };
    ```
    
    4. 在工具栏添加批量导出按钮（在 NetworkExport 按钮旁边）：
    ```typescript
    <Button
      icon={<DownloadOutlined />}
      onClick={() => setBatchModalVisible(true)}
    >
      批量导出
    </Button>
    ```
    
    5. 在页面底部添加 Modal 组件：
    ```typescript
    <BatchExportModal
      visible={batchModalVisible}
      onConfirm={handleBatchExport}
      onCancel={() => setBatchModalVisible(false)}
      loading={batchExporting}
    />
    ```
    
    关键约束：
    - 批量导出按钮必须放在 NetworkExport 按钮旁边
    - 必须使用当前页面的筛选条件（currentFilters）作为导出筛选条件
    - 必须遵循批量删除按钮的样式和位置模式
    - 所有 9 个页面必须使用相同的集成模式
  </action>
  <verify>
    <automated>
      cd xingran-react-frontend && npm run type-check
    </automated>
  </verify>
  <done>
    - 所有 9 个网络管理页面都已集成 BatchExportModal
    - TypeScript 类型检查通过
    - 每个页面的工具栏都有"批量导出"按钮
    - 手动验证每个页面的批量导出功能：
      * 点击按钮，Modal 正确打开
      * 选择实体类型，确认导出
      * 验证下载的 ZIP 文件包含正确的 Excel 文件
      * 验证文件名格式正确
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | 用户选择的实体类型跨越此信任边界 |
| API→ZIP writer | 生成的 Excel 内容跨越此边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Spoofing | BatchExport endpoint | mitigate | 验证 entityTypes 列表，拒绝未知的实体类型 |
| T-10-02 | Tampering | entityTypes 参数 | mitigate | 使用 binding:"required,min=1,max=9" 验证输入范围 |
| T-10-03 | Denial of Service | ZIP 文件大小 | mitigate | 设置最大记录数（100,000）和 ZIP 大小限制（100MB） |
| T-10-04 | Information Disclosure | 文件下载 | accept | 导出数据已是用户可访问的数据，无额外敏感信息 |
| T-10-05 | Elevation of Privilege | 权限检查 | mitigate | 路由已在 /network 组下，继承网络管理权限检查 |
</threat_model>

<verification>
1. 后端批量导出端点响应时间 < 10s（100,000 条记录）
2. ZIP 文件大小不超过 100MB
3. 所有 9 个实体类型都能正确导出
4. 前端 Modal 正确显示和交互
5. 下载的 ZIP 文件可以正常解压
6. ZIP 内的 Excel 文件格式正确，可以正常打开
</verification>

<success_criteria>
1. 用户可以从任何网络管理页面点击"批量导出"按钮
2. 批量导出 Modal 显示所有 9 个实体类型，默认全选
3. 用户选择实体类型并确认后，系统生成 ZIP 文件并下载
4. ZIP 文件包含所有选中实体类型的 Excel 文件
5. 每个 Excel 文件使用正确的中文名称（如 `网络设备.xlsx`）
6. ZIP 文件名格式为 `网络管理_批量导出_{timestamp}.zip`
7. 导出失败时显示明确的错误信息
8. 大数据量导出时有合理的限制和保护机制
</success_criteria>

<output>
After completion, create `.planning/phases/10-network-export/10-01-SUMMARY.md`
</output>
