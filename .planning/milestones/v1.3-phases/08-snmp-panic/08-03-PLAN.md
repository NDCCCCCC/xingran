---
phase: 08-snmp-panic
plan: 03
type: tdd
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - internal/device/snmp_client.go
  - internal/device/snmp_client_test.go
autonomous: true
requirements:
  - SNMP-01c
must_haves:
  truths:
    - 调用者可以验证连接是否真正就绪
    - 连接未就绪时操作返回明确错误
    - 超时机制防止无限等待
  artifacts:
    - path: internal/device/snmp_client.go
      provides: 连接就绪性验证
      exports: ["WaitForReady"]
    - path: internal/device/snmp_client_test.go
      provides: 单元测试
      contains: "TestSNMPClient_WaitForReady"
  key_links:
    - from: snmp_client.go
      to: scrapli_wrapper.go
      via: WaitForReady pattern
      pattern: WaitForReady.*context\.Context
    - from: snmp_client_test.go
      to: snmp_client.go
      via: test coverage
      pattern: func Test.*WaitForReady
---

<objective>
添加 WaitForReady() 方法验证连接已完全就绪，防止在传输层未完成初始化时执行操作。实现超时机制，避免无限等待。使用 TDD 方法确保功能正确性。

Purpose: 防止在连接未完全就绪时执行 SNMP 操作导致 panic
Output: 连接就绪性验证机制和单元测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-snmp-panic/08-CONTEXT.md
@.planning/phases/08-snmp-panic/08-PATTERNS.md

@internal/device/snmp_client.go
@internal/device/scrapli_wrapper.go
@internal/device/connection_pool.go
@internal/services/operations/rate_limiter_test.go
</context>

<interfaces>
<!-- 从 scrapli_wrapper.go 提取的 WaitForReady 模式 -->

WaitForReady pattern (scrapli_wrapper.go:384-406):
```go
// WaitForReady 等待连接就绪（带超时）
func (w *ScrapliWrapper) WaitForReady(timeout time.Duration) error {
    ctx, cancel := context.WithTimeout(context.Background(), timeout)
    defer cancel()

    ticker := time.NewTicker(50 * time.Millisecond)
    defer ticker.Stop()

    for {
        select {
        case <-ctx.Done():
            return fmt.Errorf("等待连接就绪超时")
        case <-w.initDone:
            // 初始化完成，检查最终状态
            if w.getState() == StateReady {
                return nil
            }
            return fmt.Errorf("连接初始化失败")
        case <-ticker.C:
            // 轮询检查状态
            if w.getState() == StateReady {
                return nil
            }
        }
    }
}
```

Current SNMPClient structure (需要修改):
```go
type SNMPClient struct {
    client *gosnmp.GoSNMP
    mu     sync.RWMutex // 添加自 Plan 08-02
}
```

Test pattern (rate_limiter_test.go):
```go
func TestRateLimiter_Allow(t *testing.T) {
    maxTokens := 3
    refillInterval := 100 * time.Millisecond
    limiter := NewRateLimiter(maxTokens, refillInterval)

    // 前三次请求应该被允许
    for i := 0; i < maxTokens; i++ {
        if !limiter.Allow() {
            t.Errorf("请求 %d 应该被允许", i+1)
        }
    }
    // ...
}
```
</interfaces>

<feature>
<name>WaitForReady Connection Validation</name>
<files>
internal/device/snmp_client.go
internal/device/snmp_client_test.go
</files>
<behavior>
**RED Phase - 定义测试用例：**

1. **TestSNMPClient_WaitForReady_ImmediateSuccess**: 连接已就绪时立即返回 nil
   - Setup: 创建 SNMPClient，调用 Connect() 成功
   - Action: 调用 WaitForReady(5 * time.Second)
   - Expect: 返回 nil

2. **TestSNMPClient_WaitForReady_NotConnected**: 未连接时返回错误
   - Setup: 创建 SNMPClient，不调用 Connect()
   - Action: 调用 WaitForReady(1 * time.Second)
   - Expect: 返回错误，错误信息包含 "未连接" 或 "not connected"

3. **TestSNMPClient_WaitForReady_Timeout**: 超时机制验证
   - Setup: 创建 SNMPClient，模拟初始化中状态
   - Action: 调用 WaitForReady(100 * time.Millisecond)
   - Expect: 在 100ms 内返回，返回超时错误

4. **TestSNMPClient_WaitForReady_Concurrent**: 并发调用安全性
   - Setup: 创建已连接的 SNMPClient
   - Action: 10 个 goroutine 并发调用 WaitForReady()
   - Expect: 所有调用都成功返回，无死锁或 panic

5. **TestSNMPClient_Get_WithWaitForReady**: 集成测试
   - Setup: 创建 SNMPClient，调用 Connect()
   - Action: 先调用 WaitForReady()，再调用 Get()
   - Expect: 两个调用都成功
</behavior>
<implementation>
**GREEN Phase - 实现功能：**

1. 在 SNMPClient 结构体中添加 ready 字段和 readyMu 互斥锁：
```go
type SNMPClient struct {
    client  *gosnmp.GoSNMP
    mu      sync.RWMutex
    ready   bool          // 连接是否就绪
    readyMu sync.Mutex    // 保护 ready 字段
}
```

2. 在 Connect() 方法中，连接成功后设置 ready = true：
```go
func (c *SNMPClient) Connect() error {
    c.mu.Lock()
    defer c.mu.Unlock()

    err := c.client.Connect()
    if err != nil {
        return fmt.Errorf("SNMP连接失败: %w", err)
    }

    // 验证连接真正可用（发送测试请求）
    // 注意：gosnmp.Connect() 只是建立连接，不验证可用性
    // 这里简化处理，假设 Connect() 成功就标记为就绪
    c.setReady(true)
    return nil
}
```

3. 在 Close() 方法中设置 ready = false：
```go
func (c *SNMPClient) Close() error {
    c.mu.Lock()
    defer c.mu.Unlock()

    if c.client != nil {
        c.client.Conn.Close()
    }
    c.setReady(false)
    return nil
}
```

4. 添加辅助方法：
```go
// setReady 设置就绪状态（线程安全）
func (c *SNMPClient) setReady(ready bool) {
    c.readyMu.Lock()
    defer c.readyMu.Unlock()
    c.ready = ready
}

// isReady 检查是否就绪（线程安全）
func (c *SNMPClient) isReady() bool {
    c.readyMu.RLock()
    defer c.readyMu.RUnlock()
    return c.ready
}
```

5. 实现 WaitForReady() 方法：
```go
// WaitForReady 等待连接就绪（带超时）
// timeout: 超时时间，建议 5-10 秒
// 返回 nil 表示连接已就绪，返回 error 表示超时或连接失败
func (c *SNMPClient) WaitForReady(timeout time.Duration) error {
    ctx, cancel := context.WithTimeout(context.Background(), timeout)
    defer cancel()

    ticker := time.NewTicker(50 * time.Millisecond)
    defer ticker.Stop()

    for {
        select {
        case <-ctx.Done():
            return fmt.Errorf("等待连接就绪超时")

        case <-ticker.C:
            if c.isReady() {
                return nil
            }
        }
    }
}
```

**REFACTOR Phase - 优化实现（可选）：**

1. 如果需要更精确的就绪验证，可以在 Connect() 中发送测试 SNMP 请求
2. 考虑添加 context.Context 参数支持取消操作
3. 优化轮询间隔（根据实际情况调整）
</implementation>
</feature>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: 使用 TDD 实现 WaitForReady 方法</name>
  <files>internal/device/snmp_client_test.go, internal/device/snmp_client.go</files>
  <read_first>
    - internal/device/snmp_client.go (当前实现)
    - internal/device/scrapli_wrapper.go (WaitForReady 参考实现，行 384-406)
    - internal/services/operations/rate_limiter_test.go (测试模式参考)
  </read_first>
  <behavior>
按照 TDD 的 RED → GREEN → REFACTOR 循环：

**RED Phase (1-2 次提交):**
1. 创建 internal/device/snmp_client_test.go
2. 编写 5 个测试用例（全部预期失败）：
   - TestSNMPClient_WaitForReady_ImmediateSuccess
   - TestSNMPClient_WaitForReady_NotConnected
   - TestSNMPClient_WaitForReady_Timeout
   - TestSNMPClient_WaitForReady_Concurrent
   - TestSNMPClient_Get_WithWaitForReady
3. 运行测试确认全部失败
4. 提交：test(08-03): add failing tests for WaitForReady

**GREEN Phase (1-2 次提交):**
1. 在 SNMPClient 添加 ready 和 readyMu 字段
2. 实现 setReady() 和 isReady() 辅助方法
3. 修改 Connect() 和 Close() 设置 ready 状态
4. 实现 WaitForReady() 方法
5. 运行测试确认全部通过
6. 提交：feat(08-03): implement WaitForReady method

**REFACTOR Phase (如果需要):**
1. 优化代码结构
2. 改进错误消息
3. 运行测试确保仍然通过
4. 提交：refactor(08-03): optimize WaitForReady implementation
  </behavior>
  <action>
**阶段 1: RED - 编写失败的测试**

创建 internal/device/snmp_client_test.go：

```go
package device

import (
    "context"
    "sync"
    "testing"
    "time"
)

// TestSNMPClient_WaitForReady_ImmediateSuccess 测试连接已就绪时立即返回
func TestSNMPClient_WaitForReady_ImmediateSuccess(t *testing.T) {
    config := &SNMPClientConfig{
        Target:    "127.0.0.1", // 使用本地地址（不需要真实设备）
        Port:      161,
        Community: "public",
        Version:   SNMPVersion2c,
        Timeout:   1 * time.Second,
    }

    client := NewSNMPClient(config)

    // 模拟连接成功（手动设置 ready 状态）
    client.setReady(true)

    // 应该立即返回 nil
    err := client.WaitForReady(5 * time.Second)
    if err != nil {
        t.Errorf("WaitForReady 应该成功，返回错误: %v", err)
    }
}

// TestSNMPClient_WaitForReady_NotConnected 测试未连接时返回错误
func TestSNMPClient_WaitForReady_NotConnected(t *testing.T) {
    config := &SNMPClientConfig{
        Target:    "127.0.0.1",
        Port:      161,
        Community: "public",
        Version:   SNMPVersion2c,
        Timeout:   1 * time.Second,
    }

    client := NewSNMPClient(config)
    // 不调用 Connect()，保持未连接状态

    // 应该返回超时错误
    err := client.WaitForReady(500 * time.Millisecond)
    if err == nil {
        t.Error("WaitForReady 应该返回错误")
    }
}

// TestSNMPClient_WaitForReady_Timeout 测试超时机制
func TestSNMPClient_WaitForReady_Timeout(t *testing.T) {
    config := &SNMPClientConfig{
        Target:    "127.0.0.1",
        Port:      161,
        Community: "public",
        Version:   SNMPVersion2c,
        Timeout:   1 * time.Second,
    }

    client := NewSNMPClient(config)
    // 保持未就绪状态

    start := time.Now()
    err := client.WaitForReady(200 * time.Millisecond)
    elapsed := time.Since(start)

    if err == nil {
        t.Error("WaitForReady 应该返回超时错误")
    }
    if elapsed < 200*time.Millisecond {
        t.Errorf("WaitForReady 应该等待至少 200ms，实际等待: %v", elapsed)
    }
    if elapsed > 300*time.Millisecond {
        t.Errorf("WaitForReady 等待时间过长: %v", elapsed)
    }
}

// TestSNMPClient_WaitForReady_Concurrent 测试并发调用安全性
func TestSNMPClient_WaitForReady_Concurrent(t *testing.T) {
    config := &SNMPClientConfig{
        Target:    "127.0.0.1",
        Port:      161,
        Community: "public",
        Version:   SNMPVersion2c,
        Timeout:   1 * time.Second,
    }

    client := NewSNMPClient(config)
    client.setReady(true) // 标记为就绪

    const numGoroutines = 10
    var wg sync.WaitGroup
    errors := make(chan error, numGoroutines)

    for i := 0; i < numGoroutines; i++ {
        wg.Add(1)
        go func() {
            defer wg.Done()
            if err := client.WaitForReady(1 * time.Second); err != nil {
                errors <- err
            }
        }()
    }

    wg.Wait()
    close(errors)

    // 检查是否有错误
    for err := range errors {
        t.Errorf("并发调用 WaitForReady 失败: %v", err)
    }
}

// TestSNMPClient_Get_WithWaitForReady 测试集成场景
func TestSNMPClient_Get_WithWaitForReady(t *testing.T) {
    config := &SNMPClientConfig{
        Target:    "127.0.0.1",
        Port:      161,
        Community: "public",
        Version:   SNMPVersion2c,
        Timeout:   1 * time.Second,
    }

    client := NewSNMPClient(config)

    // 先等待就绪（会超时，因为没有真实设备）
    err := client.WaitForReady(100 * time.Millisecond)
    if err == nil {
        t.Error("WaitForReady 应该超时（无真实设备）")
    }

    // 即使超时，也不应该 panic 或崩溃
    t.Log("WaitForReady 正确处理了超时场景")
}
```

**阶段 2: GREEN - 实现功能**

修改 internal/device/snmp_client.go：

1. 在结构体中添加字段：
```go
type SNMPClient struct {
    client  *gosnmp.GoSNMP
    mu      sync.RWMutex
    ready   bool          // 连接是否就绪
    readyMu sync.Mutex    // 保护 ready 字段
}
```

2. 添加辅助方法：
```go
// setReady 设置就绪状态（线程安全）
func (c *SNMPClient) setReady(ready bool) {
    c.readyMu.Lock()
    defer c.readyMu.Unlock()
    c.ready = ready
}

// isReady 检查是否就绪（线程安全）
func (c *SNMPClient) isReady() bool {
    c.readyMu.RLock()
    defer c.readyMu.RUnlock()
    return c.ready
}
```

3. 修改 Connect() 方法（在现有代码基础上添加）：
```go
func (c *SNMPClient) Connect() error {
    c.mu.Lock()
    defer c.mu.Unlock()

    err := c.connectLocked()
    if err != nil {
        return err
    }

    // 标记为就绪
    c.setReady(true)
    return nil
}
```

4. 修改 Close() 方法（在现有代码基础上添加）：
```go
func (c *SNMPClient) Close() error {
    c.mu.Lock()
    defer c.mu.Unlock()

    err := c.closeLocked()
    c.setReady(false) // 标记为未就绪
    return err
}
```

5. 添加 WaitForReady() 方法：
```go
// WaitForReady 等待连接就绪（带超时）
func (c *SNMPClient) WaitForReady(timeout time.Duration) error {
    ctx, cancel := context.WithTimeout(context.Background(), timeout)
    defer cancel()

    ticker := time.NewTicker(50 * time.Millisecond)
    defer ticker.Stop()

    for {
        select {
        case <-ctx.Done():
            return fmt.Errorf("等待连接就绪超时")

        case <-ticker.C:
            if c.isReady() {
                return nil
            }
        }
    }
}
```

6. 添加必要的 import（如果尚未导入）：
```go
import (
    // ... 现有 import
    "context"
)
```

**阶段 3: REFACTOR - 优化（可选）**

如果测试通过但代码可以改进：
- 优化轮询间隔
- 改进错误消息
- 添加更多上下文信息到日志
  </action>
  <verify>
    <automated>
cd internal/device && go test -v -run TestSNMPClient_WaitForReady
go test ./internal/device/... -cover
go build ./internal/device/...
    </automated>
  </verify>
  <done>
    - **RED**: 5 个测试用例编写完成并失败
    - **GREEN**: WaitForReady() 方法实现完成，所有测试通过
    - 测试覆盖正常场景和边界情况
    - 并发安全性验证通过
    - 超时机制正常工作
    - 代码编译通过，无语法错误
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 连接初始化 → 操作执行 | 传输层可能未完全初始化 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | DoS | 超时机制 | mitigate | WaitForReady 带超时，防止无限等待阻塞操作 |
| T-08-06 | Tampering | 就绪状态 | mitigate | 使用 RWMutex 保护 ready 字段，防止状态不一致 |
</threat_model>

<verification>
1. 运行所有测试用例，确认全部通过
2. 检查测试覆盖率：go test -cover ./internal/device/...
3. 验证并发测试无死锁或 panic
4. 确认超时机制正常工作
5. 代码编译通过
</verification>

<success_criteria>
1. WaitForReady() 方法实现完成并导出
2. 所有 5 个测试用例通过
3. 连接就绪性验证正常工作
4. 超时机制防止无限等待
5. 并发调用安全性得到验证
6. 测试覆盖率 ≥ 80%
7. 代码编译通过，无语法或逻辑错误
</success_criteria>

<output>
After completion, create `.planning/phases/08-snmp-panic/08-03-SUMMARY.md`
</output>
