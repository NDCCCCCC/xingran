---
phase: 23-ad-group-sync
plan: 14
type: execute
wave: 4
depends_on: [23-11]
files_modified:
  - internal/services/addomain/member_sync_service.go
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "人员部门变动时自动移出旧组、加入新组"
    - "每个成员只能属于一个部门组（排他性）"
    - "系统定期检查并清理多组成员关系"
  artifacts:
    - path: "internal/services/addomain/member_sync_service.go"
      provides: "MemberSyncService扩展，添加变更处理和排他性检查"
      contains: "HandleDeptChange|EnsureExclusiveMembership"
  key_links:
    - from: "HandleDeptChange"
      to: "旧部门组和新部门组"
      via: "RemoveGroupMember + SyncDeptMembers"
      pattern: "RemoveGroupMember.*SyncDeptMembers"
    - from: "EnsureExclusiveMembership"
      to: "所有部门组"
      via: "遍历组成员并验证部门归属"
      pattern: "currentMembers.*userDeptID.*RemoveGroupMember"
---

# Plan 23-14: Implement Change Handling and Exclusive Membership

## Objective
Extend MemberSyncService with change handling (HandleDeptChange) and exclusive membership enforcement (EnsureExclusiveMembership). This plan completes the member sync functionality by handling department transitions and ensuring users belong to only one department group.

**Purpose**: Maintain data integrity when users change departments and enforce business rule that each user belongs to exactly one department group.

**Output**: Extended MemberSyncService with HandleDeptChange and EnsureExclusiveMembership methods.

## Context
@.planning/phases/23-ad-group-sync/23-CONTEXT.md
@.planning/phases/23-ad-group-sync/23-VERIFICATION.md
@internal/services/addomain/member_sync_service.go (existing MemberSyncService from Plan 23-11)
@internal/services/addomain/dept_group_mapping_service.go (DeptGroupMappingService)
@internal/services/addomain/ldap_client.go (AddGroupMember, RemoveGroupMember methods)
@internal/services/addomain/utils.go (decryptPassword function)

## Tasks

### Task 1: Add HandleDeptChange method to MemberSyncService

<read_first>
- internal/services/addomain/member_sync_service.go (existing SyncDeptMembers method for reference)
- internal/services/addomain/ldap_client.go (RemoveGroupMember method)
- internal/services/addomain/dept_group_mapping_service.go (GetMappingByDept method)
- internal/services/addomain/utils.go (decryptPassword function - line 50)
</read_first>

<action>
Add the HandleDeptChange method to `internal/services/addomain/member_sync_service.go` (after SyncAllMembers method):

```go
// HandleDeptChange 处理部门变更（移出旧组，加入新组）
func (s *memberSyncService) HandleDeptChange(ctx context.Context, userID, oldDeptID, newDeptID string) error {
	// 1. 获取用户信息
	var user models.User
	if err := s.db.WithContext(ctx).Where("id = ?", userID).First(&user).Error; err != nil {
		return fmt.Errorf("用户不存在")
	}

	if user.ADDN == nil || *user.ADDN == "" {
		applogger.Infof("[成员同步] 用户无AD账号，跳过: %s", user.Username)
		return nil
	}

	userDN := *user.ADDN

	// 2. 从旧部门组中移除
	if oldDeptID != "" && oldDeptID != newDeptID {
		mappingService := NewDeptGroupMappingService(s.db)
		oldMapping, err := mappingService.GetMappingByDept(ctx, oldDeptID)
		if err == nil && oldMapping.SyncEnabled {
			var config models.ADConfig
			if err := s.db.WithContext(ctx).Where("id = ?", oldMapping.ADConfigID).First(&config).Error; err == nil {
				// 验证配置完整性
				if config.ServerAddress != "" && config.ServerPort != 0 && config.BaseDN != "" {
					config.AdminPassword = decryptPassword(config.AdminPassword)
					client := NewLDAPClient(&config)
					if err := client.Connect(); err == nil {
						defer client.Close()
						if err := client.RemoveGroupMember(oldMapping.GroupDN, userDN); err != nil {
							applogger.Warnf("[成员同步] 移除用户失败 [%s from %s]: %v", user.Username, oldMapping.GroupName, err)
						} else {
							applogger.Infof("[成员同步] 移除用户: %s from %s", user.Username, oldMapping.GroupName)
						}
					}
				}
			}
		}
	}

	// 3. 加入新部门组
	if newDeptID != "" {
		_, err := s.SyncDeptMembers(ctx, newDeptID)
		if err != nil {
			applogger.Errorf("[成员同步] 加入新部门组失败 [%s]: %v", user.Username, err)
			return err
		}
	}

	return nil
}
```

Update the MemberSyncService interface to include HandleDeptChange:
```go
type MemberSyncService interface {
	// SyncDeptMembers 同步指定部门的成员到对应的AD组
	SyncDeptMembers(ctx context.Context, deptID string) (*MemberSyncResult, error)
	
	// SyncAllMembers 同步所有有映射的部门的成员
	SyncAllMembers(ctx context.Context, configID string) (*BatchMemberSyncResult, error)
	
	// HandleDeptChange 处理部门变更（移出旧组，加入新组）
	HandleDeptChange(ctx context.Context, userID, oldDeptID, newDeptID string) error
}
```

HandleDeptChange design rationale:
1. **Graceful degradation**: If user has no AD account, skip processing
2. **Config validation**: Checks ServerAddress, ServerPort, BaseDN before LDAP connection
3. **Non-blocking removal**: Logs warning if removal fails, continues with new dept sync
4. **Reuses SyncDeptMembers**: Leverages existing sync logic for adding to new group
5. **Password decryption**: Uses decryptPassword from utils.go before LDAP operations
</action>

<verify>
<automated>
grep -n "func.*HandleDeptChange" internal/services/addomain/member_sync_service.go
grep -n "HandleDeptChange.*context.Context" internal/services/addomain/member_sync_service.go
</automated>
</verify>

<done>
HandleDeptChange method exists with proper LDAP config validation and error handling
</done>

### Task 2: Add EnsureExclusiveMembership method to MemberSyncService

<read_first>
- internal/services/addomain/member_sync_service.go (existing getCurrentGroupMembers method)
- internal/services/addomain/ldap_client.go (RemoveGroupMember method)
- internal/services/addomain/utils.go (decryptPassword function - line 50)
</read_first>

<action>
Add the EnsureExclusiveMembership method to `internal/services/addomain/member_sync_service.go` (after HandleDeptChange method):

```go
// ExclusiveMembershipResult 排他性成员关系结果
type ExclusiveMembershipResult struct {
	TotalUsers      int `json:"totalUsers"`
	RemovedCount    int `json:"removedCount"`
	ProcessedGroups int `json:"processedGroups"`
}

// EnsureExclusiveMembership 确保成员只属于一个部门组
func (s *memberSyncService) EnsureExclusiveMembership(ctx context.Context, configID string) (*ExclusiveMembershipResult, error) {
	result := &ExclusiveMembershipResult{}

	// 1. 获取所有用户及其部门
	var users []struct {
		UserID string
		DeptID *string
		ADDN   *string
	}

	err := s.db.WithContext(ctx).
		Model(&models.User{}).
		Select("id, dept_id, ad_dn").
		Where("ad_dn IS NOT NULL AND ad_dn != '' AND status = ? AND deleted_at IS NULL", models.UserStatusNormal).
		Scan(&users).Error
	if err != nil {
		return nil, fmt.Errorf("查询用户列表失败: %w", err)
	}

	result.TotalUsers = len(users)

	// 2. 获取所有映射
	mappingService := NewDeptGroupMappingService(s.db)
	listReq := &ListMappingsRequest{
		ADConfigID:    &configID,
		MappingStatus: (*models.MappingStatus)(&[]string{"active"}[0]),
		Current:       1,
		PageSize:      1000,
	}

	listResp, err := mappingService.ListMappings(ctx, listReq)
	if err != nil {
		return nil, fmt.Errorf("查询映射列表失败: %w", err)
	}

	// 构建部门到组映射的快速查找表
	deptToGroup := make(map[string]string) // deptID -> groupDN
	for _, mapping := range listResp.List {
		deptToGroup[mapping.DeptID] = mapping.GroupDN
	}

	// 3. 连接LDAP（用于修改组成员）
	var config models.ADConfig
	if err := s.db.WithContext(ctx).Where("id = ?", configID).First(&config).Error; err != nil {
		return nil, fmt.Errorf("AD配置不存在")
	}

	// 验证配置完整性
	if config.ServerAddress == "" || config.ServerPort == 0 || config.BaseDN == "" {
		return nil, fmt.Errorf("AD配置不完整：缺少服务器地址、端口或BaseDN")
	}

	config.AdminPassword = decryptPassword(config.AdminPassword)
	client := NewLDAPClient(&config)
	if err := client.Connect(); err != nil {
		return nil, fmt.Errorf("连接AD服务器失败: %w", err)
	}
	defer client.Close()

	// 4. 对每个组，移除不属于对应部门的成员
	result.ProcessedGroups = len(listResp.List)

	for _, mapping := range listResp.List {
		// 获取组当前的成员（从LDAP）
		currentMembers, err := s.getCurrentGroupMembers(ctx, mapping.GroupDN)
		if err != nil {
			applogger.Warnf("[排他性检查] 获取组成员失败 [%s]: %v", mapping.GroupName, err)
			continue
		}

		// 移除不属于该部门的成员
		for userDN := range currentMembers {
			// 查找用户所属部门
			userDeptID := ""
			for _, u := range users {
				if u.ADDN != nil && *u.ADDN == userDN && u.DeptID != nil {
					userDeptID = *u.DeptID
					break
				}
			}

			// 如果用户不属于该部门，移除
			if userDeptID != mapping.DeptID {
				if err := client.RemoveGroupMember(mapping.GroupDN, userDN); err != nil {
					applogger.Errorf("[排他性检查] 移除成员失败 [%s from %s]: %v", userDN, mapping.GroupName, err)
					continue
				}
				result.RemovedCount++
				applogger.Infof("[排他性检查] 移除成员: %s from %s (不属于部门)", userDN, mapping.GroupName)
			}
		}
	}

	applogger.Infof("[排他性检查] 完成: 总用户=%d, 处理组=%d, 移除成员=%d",
		result.TotalUsers, result.ProcessedGroups, result.RemovedCount)

	return result, nil
}
```

Update the MemberSyncService interface to include EnsureExclusiveMembership:
```go
type MemberSyncService interface {
	// SyncDeptMembers 同步指定部门的成员到对应的AD组
	SyncDeptMembers(ctx context.Context, deptID string) (*MemberSyncResult, error)
	
	// SyncAllMembers 同步所有有映射的部门的成员
	SyncAllMembers(ctx context.Context, configID string) (*BatchMemberSyncResult, error)
	
	// HandleDeptChange 处理部门变更（移出旧组，加入新组）
	HandleDeptChange(ctx context.Context, userID, oldDeptID, newDeptID string) error
	
	// EnsureExclusiveMembership 确保成员只属于一个部门组
	EnsureExclusiveMembership(ctx context.Context, configID string) (*ExclusiveMembershipResult, error)
}
```

EnsureExclusiveMembership design rationale:
1. **Batch processing**: Processes all groups in a single LDAP connection for efficiency
2. **Config validation**: Validates ServerAddress, ServerPort, BaseDN before connecting
3. **Lookup optimization**: Builds deptToGroup map for O(1) lookups
4. **Error tolerance**: Continues processing other groups if one group fails
5. **Audit logging**: Logs each removal for traceability
6. **Password decryption**: Uses decryptPassword from utils.go before LDAP connection
</action>

<verify>
<automated>
grep -n "func.*EnsureExclusiveMembership" internal/services/addomain/member_sync_service.go
grep -n "ExclusiveMembershipResult" internal/services/addomain/member_sync_service.go
grep -n "ServerAddress.*ServerPort.*BaseDN" internal/services/addomain/member_sync_service.go
</automated>
</verify>

<done>
EnsureExclusiveMembership method exists with proper LDAP config validation and error handling
</done>

## Threat Model

### Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Department change API → Service | User dept_id update triggers HandleDeptChange |
| Service → LDAP | Service removes/adds group memberships based on dept changes |
| Scheduler → Service | Periodic exclusive membership check runs via cron |

### STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-14-01 | S | Unauthorized dept changes | mitigate | Add permission check in user update handler (require admin role) |
| T-23-14-02 | T | Inconsistent state during dept change | mitigate | Use database transactions for dept updates, implement retry logic |
| T-23-14-03 | R | Data loss if exclusive membership over-cleans | mitigate | EnsureExclusiveMembership only removes users who don't belong to dept, logs all removals for recovery |
| T-23-14-04 | D | Privilege escalation via group hopping | mitigate | HandleDeptChange validates both old and new dept mappings before changes |

## Verification

1. **Service compilation**: Run `go build ./internal/services/addomain/` to verify syntax
2. **Interface completeness**: Verify MemberSyncService interface includes all four methods
3. **Integration test**: Test HandleDeptChange with a user changing departments

## Success Criteria

- [ ] HandleDeptChange removes user from old group before adding to new group
- [ ] HandleDeptChange skips users without AD accounts
- [ ] EnsureExclusiveMembership enforces one-group-per-user policy
- [ ] Both methods validate LDAP config before connection
- [ ] Both methods use decryptPassword from utils.go
- [ ] Error handling allows partial failures without blocking entire operation
- [ ] No compilation errors after changes

## Output

After completion, create `.planning/phases/23-ad-group-sync/23-14-SUMMARY.md` documenting:
- HandleDeptChange implementation details
- EnsureExclusiveMembership implementation details
- LDAP config validation patterns
- Error handling strategies
- Next steps for testing and API integration
