---
phase: 20-ad-ou-dept-mapping
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - internal/services/addomain/dept_sync_service.go
  - internal/services/addomain/sync_result.go
  - internal/api/v1/system/ad_dept_sync_handler.go
  - internal/api/v1/system/ad_dept_sync_router.go
  - configs/config.yaml
autonomous: true
requirements:
  - PHASE-20-DEPT-SYNC
  - PHASE-20-SCHEDULED-JOB
  - PHASE-20-API-ENDPOINTS
user_setup: []
must_haves:
  truths:
    - "DeptToADSyncService能递归同步部门树到AD OU结构"
    - "定时任务每天凌晨2点自动触发部门同步"
    - "提供手动触发同步的API端点"
    - "同步状态可查询（成功/失败/进行中）"
    - "同步过程有详细的日志记录和错误处理"
  artifacts:
    - path: "internal/services/addomain/dept_sync_service.go"
      provides: "部门到AD同步核心服务"
      exports: ["SyncDeptStructureToAD", "syncDeptTree", "getRootDepartments"]
    - path: "internal/services/addomain/sync_result.go"
      provides: "同步结果数据结构"
      min_lines: 30
    - path: "internal/api/v1/system/ad_dept_sync_handler.go"
      provides: "部门同步HTTP处理器"
      exports: ["SyncDeptToAD", "GetDeptSyncStatus", "TriggerDeptSync"]
    - path: "internal/api/v1/system/ad_dept_sync_router.go"
      provides: "部门同步路由注册"
      exports: ["SetupADDeptSyncRouter"]
    - path: "configs/config.yaml"
      provides: "定时任务配置"
      contains: "scheduler.jobs.*dept_to_ad_sync*"
  key_links:
    - from: "internal/services/addomain/dept_sync_service.go"
      to: "internal/services/addomain/dept_ou_mapper.go"
      via: "DeptOUmapper服务调用"
      pattern: "mapper\\.UpsertMapping"
    - from: "internal/services/addomain/dept_sync_service.go"
      to: "internal/services/addomain/ldap_client.go"
      via: "LDAP客户端调用"
      pattern: "ldap\\.CreateOU"
    - from: "internal/api/v1/system/ad_dept_sync_handler.go"
      to: "internal/services/addomain/dept_sync_service.go"
      via: "服务层调用"
      pattern: "service\\.SyncDeptStructureToAD"
    - from: "internal/scheduler/"
      to: "internal/services/addomain/dept_sync_service.go"
      via: "定时任务注册"
      pattern: "scheduler\\.RegisterJob"
---

<objective>
**Objective:** 实现部门结构到AD域控的定时同步功能

**Purpose:** 将系统部门树（sys_dept）递归同步到AD域控OU结构，保持层级关系一致。提供定时任务（每天凌晨2点）和手动触发两种同步方式，支持同步状态查询和详细日志记录。

**Output:**
- 部门到AD同步服务（DeptToADSyncService）
- 同步结果数据结构（SyncResult、SyncStats）
- HTTP API端点（触发同步、查询状态）
- 定时任务配置和注册

**Key Deliverables:**
1. DeptToADSyncService实现递归部门树同步逻辑
2. 同步状态跟踪（pending/synced/failed）和错误处理
3. API端点：POST /system/ad/sync/dept-to-ad、GET /system/ad/sync/dept-status/:configId、POST /system/ad/sync/dept-trigger
4. 定时任务配置：每天凌晨2点执行
5. 详细日志记录（同步部门数、成功/失败统计、错误信息）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-ad-ou-dept-mapping/CONTEXT.md
@.planning/phases/20-ad-ou-dept-mapping/RESEARCH.md
@.planning/spikes/ou-dept-mapping-corrected.md
@.planning/phases/20-ad-ou-dept-mapping/20-01-SUMMARY.md

# 现有服务和模式
@internal/services/addomain/dept_ou_mapper.go
@internal/services/addomain/ldap_client.go
@internal/models/dept.go
@internal/scheduler/
@internal/api/v1/system/
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建同步结果数据结构</name>
  <files>internal/services/addomain/sync_result.go</files>
  <action>
    在`internal/services/addomain/`目录下创建`sync_result.go`：

```go
package addomain

import "time"

// SyncResult 同步结果
type SyncResult struct {
    StartTime     time.Time     `json:"startTime"`
    EndTime       time.Time     `json:"endTime"`
    Duration      time.Duration `json:"duration"`
    TotalDepts    int           `json:"totalDepts"`
    SuccessDepts  int           `json:"successDepts"`
    FailedDepts   int           `json:"failedDepts"`
    SkippedDepts  int           `json:"skippedDepts"`
    Errors        []SyncError   `json:"errors,omitempty"`
    ADConfigID    string        `json:"adConfigId"`
    Status        string        `json:"status"` // running/completed/failed
}

// SyncError 同步错误
type SyncError struct {
    DeptID   string `json:"deptId"`
    DeptName string `json:"deptName"`
    Error    string `json:"error"`
}

// SyncStats 同步统计
type SyncStats struct {
    LastSyncTime  *time.Time `json:"lastSyncTime,omitempty"`
    LastSyncStatus string    `json:"lastSyncStatus"` // pending/synced/failed
    TotalDepts    int        `json:"totalDepts"`
    SyncedDepts   int        `json:"syncedDepts"`
    FailedDepts   int        `json:"failedDepts"`
    NextSyncTime  *time.Time `json:"nextSyncTime,omitempty"`
}
```

确保：
1. 使用time.Duration表示同步耗时
2. SyncError包含部门标识和错误信息
3. SyncStats用于查询接口返回同步状态概览
4. 所有字段使用JSON标签，使用camelCase命名
  </action>
  <verify>
    <automated>go build ./internal/services/addomain/...</automated>
  </verify>
  <done>
    同步结果数据结构创建完成，包含完整的统计信息和错误详情，编译无错误。
  </done>
</task>

<task type="auto">
  <name>Task 2: 实现部门到AD同步服务</name>
  <files>internal/services/addomain/dept_sync_service.go</files>
  <action>
    在`internal/services/addomain/`目录下创建`dept_sync_service.go`：

```go
package addomain

import (
    "context"
    "fmt"
    "time"

    "github.com/xingran-next/xingran-go-backend/internal/applogger"
    "github.com/xingran-next/xingran-go-backend/internal/models"
    "gorm.io/gorm"
)

type DeptToADSyncService struct {
    db     *gorm.DB
    ldap   *LDAPClient
    mapper *DeptOUmapper
}

func NewDeptToADSyncService(db *gorm.DB, ldapClient *LDAPClient, mapper *DeptOUmapper) *DeptToADSyncService {
    return &DeptToADSyncService{
        db:     db,
        ldap:   ldapClient,
        mapper: mapper,
    }
}

// SyncDeptStructureToAD 同步部门结构到AD OU
func (s *DeptToADSyncService) SyncDeptStructureToAD(ctx context.Context, adConfigID string) (*SyncResult, error) {
    result := &SyncResult{
        StartTime:  time.Now(),
        ADConfigID: adConfigID,
        Status:     "running",
    }

    // 1. 获取AD配置
    var adConfig models.ADConfig
    if err := s.db.WithContext(ctx).Where("id = ?", adConfigID).First(&adConfig).Error; err != nil {
        result.Status = "failed"
        result.EndTime = time.Now()
        result.Duration = result.EndTime.Sub(result.StartTime)
        return nil, fmt.Errorf("获取AD配置失败: %w", err)
    }

    // 2. 连接LDAP
    ldapClient := NewLDAPClient(&adConfig)
    if err := ldapClient.Connect(); err != nil {
        result.Status = "failed"
        result.EndTime = time.Now()
        result.Duration = result.EndTime.Sub(result.StartTime)
        return nil, fmt.Errorf("连接AD失败: %w", err)
    }
    defer ldapClient.Close()

    // 3. 获取根部门（parentId为空的部门）
    rootDepts, err := s.getRootDepartments(ctx)
    if err != nil {
        result.Status = "failed"
        result.EndTime = time.Now()
        result.Duration = result.EndTime.Sub(result.StartTime)
        return nil, fmt.Errorf("获取根部门失败: %w", err)
    }

    result.TotalDepts = s.countTotalDepts(rootDepts)

    // 4. 递归同步部门树到AD OU
    for _, dept := range rootDepts {
        if err := s.syncDeptTree(ctx, ldapClient, &adConfig, dept, adConfig.BaseDN, result); err != nil {
            applogger.Errorf("同步部门树失败: %v", err)
            result.Status = "failed"
            break
        }
    }

    // 5. 更新结果
    result.EndTime = time.Now()
    result.Duration = result.EndTime.Sub(result.StartTime)
    if result.Status == "running" {
        result.Status = "completed"
    }

    applogger.Infof("部门同步完成: 总数=%d, 成功=%d, 失败=%d, 跳过=%d, 耗时=%dms",
        result.TotalDepts, result.SuccessDepts, result.FailedDepts, result.SkippedDepts, result.Duration.Milliseconds())

    return result, nil
}

// syncDeptTree 递归同步部门树
func (s *DeptToADSyncService) syncDeptTree(ctx context.Context, ldapClient *LDAPClient, config *models.ADConfig, dept *models.Department, parentOUDN string, result *SyncResult) error {
    // 1. 构建当前部门的OU DN
    ouDN := fmt.Sprintf("OU=%s,%s", dept.DeptName, parentOUDN)

    // 2. 在AD中创建OU（如果不存在）
    if err := ldapClient.CreateOU(ouDN, dept.DeptName); err != nil {
        result.FailedDepts++
        result.Errors = append(result.Errors, SyncError{
            DeptID:   dept.ID,
            DeptName: dept.DeptName,
            Error:    err.Error(),
        })
        return fmt.Errorf("创建OU失败 [%s]: %w", dept.DeptName, err)
    }

    // 3. 更新映射关系
    mapping := &models.DeptOUMapping{
        DeptID:     dept.ID,
        ADConfigID: config.ID,
        OUDN:       ouDN,
        OUName:     dept.DeptName,
        ParentOUDN: &parentOUDN,
        SyncEnabled: true,
        SyncStatus: "synced",
    }

    now := time.Now()
    mapping.LastSyncAt = &now

    if err := s.mapper.UpsertMapping(ctx, mapping); err != nil {
        applogger.Warnf("更新映射关系失败 [%s]: %v", dept.DeptName, err)
        // 不中断同步流程，继续处理子部门
    } else {
        result.SuccessDepts++
    }

    // 4. 递归处理子部门
    if len(dept.Children) > 0 {
        for _, child := range dept.Children {
            if err := s.syncDeptTree(ctx, ldapClient, config, child, ouDN, result); err != nil {
                applogger.Errorf("同步子部门失败 [%s]: %v", child.DeptName, err)
                // 继续处理其他子部门
            }
        }
    }

    return nil
}

// getRootDepartments 获取根部门（parentId为空的部门）
func (s *DeptToADSyncService) getRootDepartments(ctx context.Context) ([]*models.Department, error) {
    var depts []*models.Department
    err := s.db.WithContext(ctx).
        Preload("Children.Children.Children"). // 预加载3层子部门
        Where("parent_id IS NULL OR parent_id = ''").
        Where("status = 0"). // 0=正常
        Find(&depts).Error
    return depts, err
}

// countTotalDepts 递归计算部门总数
func (s *DeptToADSyncService) countTotalDepts(depts []*models.Department) int {
    count := len(depts)
    for _, dept := range depts {
        if len(dept.Children) > 0 {
            count += s.countTotalDepts(dept.Children)
        }
    }
    return count
}
```

确保：
1. 使用GORM Preload预加载子部门，避免N+1查询问题
2. 递归同步逻辑处理3层以上部门结构
3. 错误处理：单个部门失败不中断整个同步流程
4. 详细日志记录（applogger.Infof、applogger.Errorf）
5. 同步结果统计准确（总数、成功、失败、跳过）
6. 映射表更新使用UpsertMapping保证幂等性
  </action>
  <verify>
    <automated>go build ./internal/services/addomain/...</automated>
  </verify>
  <done>
    部门到AD同步服务实现完成，支持递归同步部门树、错误处理和结果统计，编译无错误。
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建API处理器和路由</name>
  <files>
    internal/api/v1/system/ad_dept_sync_handler.go
    internal/api/v1/system/ad_dept_sync_router.go
  </files>
  <action>
    **ad_dept_sync_handler.go:**
```go
package system

import (
    "net/http"

    "github.com/gin-gonic/gin"
    "github.com/xingran-next/xingran-go-backend/internal/applogger"
    "github.com/xingran-next/xingran-go-backend/internal/services/addomain"
    "github.com/xingran-next/xingran-go-backend/pkg/response"
)

type ADDeptSyncHandler struct {
    syncService *addomain.DeptToADSyncService
}

func NewADDeptSyncHandler(syncService *addomain.DeptToADSyncService) *ADDeptSyncHandler {
    return &ADDeptSyncHandler{syncService: syncService}
}

// SyncDeptToAD 同步部门结构到AD
func (h *ADDeptSyncHandler) SyncDeptToAD(c *gin.Context) {
    var req struct {
        ADConfigID string `json:"adConfigId" binding:"required"`
    }

    if err := c.ShouldBindJSON(&req); err != nil {
        response.Error(c, http.StatusBadRequest, "请求参数错误")
        return
    }

    result, err := h.syncService.SyncDeptStructureToAD(c.Request.Context(), req.ADConfigID)
    if err != nil {
        applogger.Errorf("部门同步失败: %v", err)
        response.Error(c, http.StatusInternalServerError, err.Error())
        return
    }

    response.Success(c, result)
}

// GetDeptSyncStatus 查询部门同步状态
func (h *ADDeptSyncHandler) GetDeptSyncStatus(c *gin.Context) {
    configID := c.Param("configId")

    // TODO: 实现从缓存或数据库查询同步状态
    // 当前先返回空状态
    stats := &addomain.SyncStats{
        LastSyncStatus: "pending",
    }

    response.Success(c, stats)
}

// TriggerDeptSync 手动触发部门同步
func (h *ADDeptSyncHandler) TriggerDeptSync(c *gin.Context) {
    var req struct {
        ADConfigID string `json:"adConfigId" binding:"required"`
    }

    if err := c.ShouldBindJSON(&req); err != nil {
        response.Error(c, http.StatusBadRequest, "请求参数错误")
        return
    }

    // 异步执行同步，避免长时间阻塞HTTP请求
    go func() {
        _, err := h.syncService.SyncDeptStructureToAD(c.Request.Context(), req.ADConfigID)
        if err != nil {
            applogger.Errorf("手动触发部门同步失败: %v", err)
        }
    }()

    response.Success(c, gin.H{
        "message": "部门同步已启动",
    })
}
```

**ad_dept_sync_router.go:**
```go
package system

import (
    "github.com/gin-gonic/gin"
    "github.com/xingran-next/xingran-go-backend/internal/core"
)

func SetupADDeptSyncRouter(r *gin.RouterGroup, core *core.Core) {
    mapper := addomain.NewDeptOUmapper(core.GetDB())
    syncService := addomain.NewDeptToADSyncService(core.GetDB(), nil, mapper)
    handler := NewADDeptSyncHandler(syncService)

    // 同步部门结构到AD
    r.POST("/sync/dept-to-ad", handler.SyncDeptToAD)

    // 查看同步状态
    r.GET("/sync/dept-status/:configId", handler.GetDeptSyncStatus)

    // 手动触发同步
    r.POST("/sync/dept-trigger", handler.TriggerDeptSync)
}
```

确保：
1. 使用response.Success/Error统一响应格式
2. 手动触发使用goroutine异步执行
3. 错误日志记录到applogger
4. 路由路径与API规范一致
5. 导入必要的包
  </action>
  <verify>
    <automated>go build ./internal/api/v1/system/...</automated>
  </verify>
  <done>
    API处理器和路由创建完成，支持同步触发、状态查询和手动触发功能，编译无错误。
  </done>
</task>

<task type="auto">
  <name>Task 4: 配置定时任务和注册路由</name>
  <files>
    configs/config.yaml
    internal/api/router.go
    internal/scheduler/jobs.go
  </files>
  <action>
    **1. 更新config.yaml:**
```yaml
scheduler:
  jobs:
    - name: "部门结构同步到AD域控"
      cron: "0 0 2 * * *"  # 每天凌晨2点执行
      type: "dept_to_ad_sync"
      enabled: true
      description: "将系统部门结构同步到AD域控OU"
```

**2. 在router.go中注册路由:**
找到`SetupSystemRouter`函数，添加：
```go
// AD部门同步路由
system.SetupADDeptSyncRouter(systemGroup, core)
```

**3. 在scheduler/jobs.go中注册定时任务:**
```go
// RegisterDeptSyncJob 注册部门同步定时任务
func RegisterDeptSyncJob(scheduler *Scheduler, db *gorm.DB) {
    mapper := addomain.NewDeptOUmapper(db)
    syncService := addomain.NewDeptToADSyncService(db, nil, mapper)

    scheduler.RegisterJob("dept_to_ad_sync", func() error {
        ctx := context.Background()
        // 获取默认AD配置ID
        var adConfig models.ADConfig
        if err := db.Where("enabled = ?", true).First(&adConfig).Error; err != nil {
            applogger.Errorf("获取AD配置失败: %v", err)
            return err
        }

        _, err := syncService.SyncDeptStructureToAD(ctx, adConfig.ID)
        return err
    })

    applogger.Info("部门同步定时任务注册成功")
}
```

**4. 在main.go中调用RegisterDeptSyncJob:**
找到`scheduler.RegisterXXXJob`调用序列，添加：
```go
addomain.RegisterDeptSyncJob(scheduler, core.GetDB())
```

确保：
1. 定时任务Cron表达式正确（秒 分 时 日 月 周）
2. 定时任务注册在应用启动时执行
3. 路由注册在正确的RouterGroup下
4. 错误处理和日志记录完整
5. 导入必要的包
  </action>
  <verify>
    <automated>go build ./cmd/main.go && grep -r "dept_to_ad_sync" configs/</automated>
  </verify>
  <done>
    定时任务配置完成，路由注册成功，应用启动时自动注册部门同步任务，每天凌晨2点执行。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin API → Sync Service | 管理员通过HTTP API触发部门同步 |
| Scheduler Job → Sync Service | 定时任务自动触发部门同步 |
| Sync Service → AD Domain Controller | LDAP连接创建/更新OU结构 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Spoofing | 手动触发API | mitigate | 需要管理员权限认证，JWT验证 |
| T-20-08 | Tampering | 同步数据 | mitigate | 使用事务保证映射表更新一致性，AD操作失败回滚 |
| T-20-09 | Repudiation | 同步操作 | mitigate | 详细记录同步日志（开始时间、结束时间、操作结果、错误信息） |
| T-20-10 | Denial of Service | 频繁触发同步 | mitigate | 定时任务限制频率（每天一次），手动触发API增加速率限制 |
| T-20-11 | Elevation of Privilege | LDAP操作权限 | mitigate | 使用最小权限AD服务账号，仅授予OU创建/更新权限 |
</threat_model>

<verification>
## 整体验证

1. **服务层验证**
   - 单元测试DeptToADSyncService的递归同步逻辑
   - 测试错误处理（单个部门失败不中断整体同步）
   - 验证映射表更新正确性

2. **API验证**
   - 测试POST /system/ad/sync/dept-to-ad端点
   - 验证请求参数校验（adConfigId必填）
   - 测试GET /system/ad/sync/dept-status/:configId端点
   - 测试POST /system/ad/sync/dept-trigger异步执行

3. **定时任务验证**
   - 手动触发定时任务验证同步功能
   - 检查日志输出是否正确
   - 验证Cron表达式解析正确

4. **集成验证**
   - 端到端测试：部门树 → AD OU同步
   - 验证映射表数据正确性
   - 测试同步状态查询功能

5. **性能验证**
   - 测试100个部门同步耗时 < 10秒
   - 测试递归深度处理（3层以上部门结构）
</verification>

<success_criteria>
1. ✅ 部门到AD同步服务实现完整，支持递归同步
2. ✅ 定时任务每天凌晨2点自动执行
3. ✅ API端点响应正确，参数校验有效
4. ✅ 同步状态可查询，错误信息详细
5. ✅ 映射表数据与AD OU结构一致
6. ✅ 日志记录完整，包含统计信息
7. ✅ 所有代码编译通过，无语法错误
</success_criteria>

<output>
After completion, create `.planning/phases/20-ad-ou-dept-mapping/20-02-SUMMARY.md`
</output>
