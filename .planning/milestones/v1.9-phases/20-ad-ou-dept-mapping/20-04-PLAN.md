---
phase: 20-ad-ou-dept-mapping
plan: 04
type: execute
wave: 4
depends_on: [20-01]
files_modified:
  - internal/services/addomain/user_ad_sync_service.go
  - internal/api/v1/system/user_handler.go
  - internal/api/v1/system/user_router.go
autonomous: true
requirements:
  - PHASE-20-USER-SYNC
  - PHASE-20-DEPT-CHANGE
  - PHASE-20-ATTRIBUTE-SYNC
user_setup: []
must_haves:
  truths:
    - "管理员修改用户信息时自动同步到AD域控"
    - "用户部门变更时同步移动用户到新OU"
    - "用户属性变更（姓名/邮箱/电话等）同步到AD"
    - "同步失败不阻断系统更新流程（降级处理）"
    - "同步操作有详细日志记录和错误处理"
  artifacts:
    - path: "internal/services/addomain/user_ad_sync_service.go"
      provides: "用户AD同步服务"
      exports: ["SyncUserUpdateToAD", "moveUserToNewOU", "syncUserAttributes"]
    - path: "internal/api/v1/system/user_handler.go"
      provides: "修改后的用户处理器"
      contains: "userADSyncService.SyncUserUpdateToAD"
    - path: "internal/api/v1/system/user_router.go"
      provides: "修改后的用户路由"
      contains: "NewUserHandler"
  key_links:
    - from: "internal/api/v1/system/user_handler.go"
      to: "internal/services/addomain/user_ad_sync_service.go"
      via: "用户更新后调用"
      pattern: "userADSyncService\\.SyncUserUpdateToAD"
    - from: "internal/services/addomain/user_ad_sync_service.go"
      to: "internal/services/addomain/dept_ou_mapper.go"
      via: "查找部门OU DN"
      pattern: "mapper\\.FindOUDNByDeptID"
    - from: "internal/services/addomain/user_ad_sync_service.go"
      to: "internal/services/addomain/ldap_client.go"
      via: "LDAP操作"
      pattern: "ldap\\.MoveUser|ldap\\.UpdateUserAttributes"
---

<objective>
**Objective:** 实现用户信息修改时的AD域控同步功能

**Purpose:** 当管理员修改用户信息（包括部门、姓名、邮箱、电话等）时，自动同步变更到AD域控。部门变更时移动用户到新OU，属性变更时更新AD用户属性。采用降级处理策略，AD同步失败不影响系统更新流程。

**Output:**
- 用户AD同步服务（UserADSyncService）
- 修改后的用户更新处理器，集成AD同步
- LDAP操作（移动用户、更新属性）

**Key Deliverables:**
1. UserADSyncService实现用户更新到AD的同步逻辑
2. 部门变更时调用LDAP MoveUser移动用户到新OU
3. 属性变更时调用LDAP UpdateUserAttributes更新AD属性
4. 用户更新接口集成AD同步（在系统更新成功后触发）
5. 降级处理：AD同步失败记录日志但不回滚系统更新
6. 详细日志记录同步操作和错误信息
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-ad-ou-dept-mapping/CONTEXT.md
@.planning/phases/20-ad-ou-dept-mapping/RESEARCH.md
@.planning/spikes/ou-dept-mapping-corrected.md
@.planning/phases/20-ad-ou-dept-mapping/20-01-SUMMARY.md

# 现有服务和模式
@internal/services/addomain/dept_ou_mapper.go
@internal/services/addomain/ldap_client.go
@internal/models/user.go
@internal/api/v1/system/user_handler.go
@internal/services/system/user_service.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现UserADSyncService</name>
  <files>internal/services/addomain/user_ad_sync_service.go</files>
  <action>
    在`internal/services/addomain/`目录下创建`user_ad_sync_service.go`：

```go
package addomain

import (
    "context"
    "fmt"
    "time"

    "github.com/xingran-next/xingran-go-backend/internal/applogger"
    "github.com/xingran-next/xingran-go-backend/internal/models"
    "gorm.io/gorm"
)

type UserADSyncService struct {
    db     *gorm.DB
    ldap   *LDAPClient
    mapper *DeptOUmapper
}

func NewUserADSyncService(db *gorm.DB, ldapClient *LDAPClient, mapper *DeptOUmapper) *UserADSyncService {
    return &UserADSyncService{
        db:     db,
        ldap:   ldapClient,
        mapper: mapper,
    }
}

// SyncUserUpdateToAD 同步用户更新到AD域控
func (s *UserADSyncService) SyncUserUpdateToAD(ctx context.Context, userID string, updateReq map[string]interface{}) error {
    // 1. 获取用户信息
    var user models.User
    if err := s.db.WithContext(ctx).Where("id = ?", userID).First(&user).Error; err != nil {
        return fmt.Errorf("查询用户失败: %w", err)
    }

    // 如果用户没有AD DN，跳过同步
    if user.ADDN == nil || *user.ADDN == "" {
        applogger.Infof("用户 %s 无AD DN，跳过AD同步", user.Username)
        return nil
    }

    // 获取AD配置
    var adConfig models.ADConfig
    if err := s.db.WithContext(ctx).Where("enabled = ?", true).First(&adConfig).Error; err != nil {
        return fmt.Errorf("获取AD配置失败: %w", err)
    }

    // 创建LDAP客户端
    ldapClient := NewLDAPClient(&adConfig)
    if err := ldapClient.Connect(); err != nil {
        return fmt.Errorf("连接AD失败: %w", err)
    }
    defer ldapClient.Close()

    // 2. 如果部门变更，需要移动用户到新OU
    if newDeptID, ok := updateReq["dept_id"]; ok {
        if newDeptIDStr, ok := newDeptID.(string); ok && newDeptIDStr != user.DeptID {
            if err := s.moveUserToNewOU(ctx, ldapClient, userID, newDeptIDStr); err != nil {
                return fmt.Errorf("移动用户到新OU失败: %w", err)
            }
        }
    }

    // 3. 更新其他属性到AD
    if err := s.syncUserAttributes(ctx, ldapClient, &user, updateReq); err != nil {
        return fmt.Errorf("同步用户属性失败: %w", err)
    }

    // 4. 更新同步时间戳
    if err := s.updateSyncTimestamp(ctx, userID); err != nil {
        applogger.Warnf("更新同步时间戳失败: %v", err)
    }

    applogger.Infof("用户 %s 信息同步到AD成功", user.Username)
    return nil
}

// moveUserToNewOU 移动用户到新OU
func (s *UserADSyncService) moveUserToNewOU(ctx context.Context, ldapClient *LDAPClient, userID, newDeptID string) error {
    // 1. 查找新部门的OU DN
    ouDN, err := s.mapper.FindOUDNByDeptID(ctx, newDeptID)
    if err != nil {
        return fmt.Errorf("查找部门OU失败: %w", err)
    }

    // 2. 获取用户信息
    var user models.User
    if err := s.db.WithContext(ctx).Where("id = ?", userID).First(&user).Error; err != nil {
        return err
    }

    if user.ADDN == nil || *user.ADDN == "" {
        return fmt.Errorf("用户无AD DN，无法移动")
    }

    // 3. 移动用户到新OU
    if err := ldapClient.MoveUser(*user.ADDN, ouDN); err != nil {
        return fmt.Errorf("移动用户到新OU失败: %w", err)
    }

    // 4. 更新用户的ad_ou_dn
    if err := s.db.WithContext(ctx).
        Model(&models.User{}).
        Where("id = ?", userID).
        Update("ad_ou_dn", ouDN).Error; err != nil {
        applogger.Warnf("更新用户ad_ou_dn失败: %v", err)
    }

    applogger.Infof("用户 %s 已移动到新OU: %s", user.Username, ouDN)
    return nil
}

// syncUserAttributes 同步用户属性到AD
func (s *UserADSyncService) syncUserAttributes(ctx context.Context, ldapClient *LDAPClient, user *models.User, updateReq map[string]interface{}) error {
    attributes := make(map[string]string)

    // 映射系统字段到AD属性
    if nickname, ok := updateReq["nickname"]; ok {
        if nickStr, ok := nickname.(string); ok {
            attributes["displayName"] = nickStr
        }
    }

    if email, ok := updateReq["email"]; ok {
        if emailStr, ok := email.(string); ok {
            attributes["mail"] = emailStr
        }
    }

    if phone, ok := updateReq["phone"]; ok {
        if phoneStr, ok := phone.(string); ok {
            attributes["telephoneNumber"] = phoneStr
        }
    }

    // 如果部门变更，同步department属性
    if deptID, ok := updateReq["dept_id"]; ok {
        if deptIDStr, ok := deptID.(string); ok {
            var dept models.Department
            if err := s.db.WithContext(ctx).Where("id = ?", deptIDStr).First(&dept).Error; err == nil {
                attributes["department"] = dept.DeptName
            }
        }
    }

    if len(attributes) == 0 {
        return nil
    }

    // 更新AD属性
    if err := ldapClient.UpdateUserAttributes(*user.ADDN, attributes); err != nil {
        return fmt.Errorf("更新AD用户属性失败: %w", err)
    }

    applogger.Infof("用户 %s 属性同步到AD: %v", user.Username, attributes)
    return nil
}

// updateSyncTimestamp 更新同步时间戳
func (s *UserADSyncService) updateSyncTimestamp(ctx context.Context, userID string) error {
    return s.db.WithContext(ctx).
        Model(&models.User{}).
        Where("id = ?", userID).
        Update("ad_synced_at", time.Now()).Error
}
```

确保：
1. 部门变更和属性变更分别处理
2. 使用映射表查找部门对应的OU DN
3. LDAP操作失败返回详细错误信息
4. 更新系统用户的ad_ou_dn和ad_synced_at字段
5. 日志记录同步操作过程
6. 导入必要的包
  </action>
  <verify>
    <automated>go build ./internal/services/addomain/...</automated>
  </verify>
  <done>
    UserADSyncService实现完成，支持用户部门移动和属性同步，编译无错误。
  </done>
</task>

<task type="auto">
  <name>Task 2: 修改用户更新处理器集成AD同步</name>
  <files>internal/api/v1/system/user_handler.go</files>
  <action>
    修改现有的用户更新处理器，在系统更新成功后触发AD同步：

**找到UpdateUser方法，在系统更新成功后添加AD同步逻辑：**

```go
// 在用户更新成功后，添加以下代码
if h.userADSyncService != nil {
    // 异步同步到AD，避免阻塞HTTP响应
    go func() {
        ctx := context.Background()
        if err := h.userADSyncService.SyncUserUpdateToAD(ctx, id, req); err != nil {
            applogger.Errorf("同步用户到AD失败 [ID=%s]: %v", id, err)
            // 不回滚系统更新，只记录错误
        }
    }()
}
```

**完整示例：**
```go
func (h *UserHandler) UpdateUser(c *gin.Context) {
    id := c.Param("id")
    var req map[string]interface{}

    if err := c.ShouldBindJSON(&req); err != nil {
        response.Error(c, http.StatusBadRequest, "请求参数错误")
        return
    }

    // 1. 系统更新（原有逻辑）
    user, err := h.userService.UpdateUser(c.Request.Context(), id, req)
    if err != nil {
        applogger.Errorf("更新用户失败: %v", err)
        response.Error(c, http.StatusInternalServerError, err.Error())
        return
    }

    // 2. 同步到AD（新增逻辑）
    if h.userADSyncService != nil {
        go func() {
            ctx := context.Background()
            if err := h.userADSyncService.SyncUserUpdateToAD(ctx, id, req); err != nil {
                applogger.Errorf("同步用户到AD失败 [ID=%s]: %v", id, err)
                // 不回滚系统更新，只记录错误
            }
        }()
    }

    response.Success(c, user)
}
```

确保：
1. AD同步在系统更新成功后执行
2. 使用goroutine异步执行，避免阻塞HTTP响应
3. 错误不阻断系统更新流程（降级处理）
4. 详细的错误日志记录
5. userADSyncService为nil时跳过同步（向后兼容）
  </action>
  <verify>
    <automated>go build ./internal/api/v1/system/...</automated>
  </verify>
  <done>
    用户更新处理器修改完成，集成UserADSyncService，支持异步AD同步。
  </done>
</task>

<task type="auto">
  <name>Task 3: 在UserHandler构造函数中注入UserADSyncService</name>
  <files>internal/api/v1/system/user_router.go</files>
  <action>
    修改UserHandler构造函数，添加UserADSyncService依赖注入：

**找到UserHandler结构体定义：**
```go
type UserHandler struct {
    userService      system.UserService
    userADSyncService *addomain.UserADSyncService // 新增
    pwdManager       security.PasswordManager
}
```

**修改NewUserHandler构造函数：**
```go
func NewUserHandler(userService system.UserService, userADSyncService *addomain.UserADSyncService, pwdManager security.PasswordManager) *UserHandler {
    return &UserHandler{
        userService:      userService,
        userADSyncService: userADSyncService, // 注入UserADSyncService
        pwdManager:       pwdManager,
    }
}
```

**修改路由注册函数SetupUserRouter：**
```go
func SetupUserRouter(r *gin.RouterGroup, core *core.Core) {
    // 创建AD同步服务
    mapper := addomain.NewDeptOUmapper(core.GetDB())
    userADSyncService := addomain.NewUserADSyncService(core.GetDB(), nil, mapper)

    // 创建UserHandler实例，传入UserADSyncService
    userService := system.NewUserService(core.GetDB(), core.Cache, core.PwdManager)
    userHandler := NewUserHandler(userService, userADSyncService, core.PwdManager)

    // 注册路由
    r.POST("/list", userHandler.List)
    r.POST("", userHandler.Create)
    r.POST("/:id", userHandler.GetUser)
    r.POST("/:id/update", userHandler.UpdateUser) // 这个方法会调用AD同步
    r.POST("/:id/delete", userHandler.Delete)
    // ... 其他路由
}
```

确保：
1. UserADSyncService通过依赖注入传入
2. 在路由注册时创建UserADSyncService实例
3. LDAP客户端传nil（在同步时动态创建）
4. 如果userADSyncService为nil，用户更新流程仍能正常工作（向后兼容）
5. 导入必要的包
  </action>
  <verify>
    <automated>go build ./internal/api/v1/system/...</automated>
  </verify>
  <done>
    UserHandler构造函数修改完成，UserADSyncService正确注入到UserHandler，依赖关系建立正确。
  </done>
</task>

<task type="auto">
  <name>Task 4: 添加批量用户移动支持</name>
  <files>internal/services/addomain/user_ad_sync_service.go</files>
  <action>
    在UserADSyncService中添加批量用户移动方法（为未来功能预留）：

```go
// BatchMoveUsersToNewOU 批量移动用户到新OU
func (s *UserADSyncService) BatchMoveUsersToNewOU(ctx context.Context, userIDs []string, newDeptID string) error {
    // 1. 查找新部门的OU DN
    ouDN, err := s.mapper.FindOUDNByDeptID(ctx, newDeptID)
    if err != nil {
        return fmt.Errorf("查找部门OU失败: %w", err)
    }

    // 2. 获取AD配置
    var adConfig models.ADConfig
    if err := s.db.WithContext(ctx).Where("enabled = ?", true).First(&adConfig).Error; err != nil {
        return fmt.Errorf("获取AD配置失败: %w", err)
    }

    // 创建LDAP客户端
    ldapClient := NewLDAPClient(&adConfig)
    if err := ldapClient.Connect(); err != nil {
        return fmt.Errorf("连接AD失败: %w", err)
    }
    defer ldapClient.Close()

    // 3. 批量移动用户（分批处理，每批10个）
    batchSize := 10
    successCount := 0
    failedCount := 0

    for i := 0; i < len(userIDs); i += batchSize {
        end := i + batchSize
        if end > len(userIDs) {
            end = len(userIDs)
        }

        batch := userIDs[i:end]
        for _, userID := range batch {
            if err := s.moveUserToNewOU(ctx, ldapClient, userID, newDeptID); err != nil {
                applogger.Errorf("移动用户失败 [ID=%s]: %v", userID, err)
                failedCount++
            } else {
                successCount++
            }
        }

        // 每批之间暂停1秒，避免AD压力过大
        if i+batchSize < len(userIDs) {
            time.Sleep(1 * time.Second)
        }
    }

    applogger.Infof("批量移动用户完成: 成功=%d, 失败=%d", successCount, failedCount)
    return nil
}
```

确保：
1. 分批处理（每批10个用户）
2. 批次之间暂停1秒，避免AD压力过大
3. 详细统计成功/失败数量
4. 单个用户失败不影响其他用户
5. 错误日志记录失败用户ID和原因
6. 导入必要的包
  </action>
  <verify>
    <automated>go build ./internal/services/addomain/...</automated>
  </verify>
  <done>
    批量用户移动方法添加完成，支持分批处理和错误统计，为未来功能预留接口。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin API → User Update | 管理员通过HTTP API修改用户信息 |
| User Update → AD Sync | 系统更新成功后触发AD同步 |
| AD Sync Service → AD Domain Controller | LDAP连接移动用户和更新属性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-17 | Spoofing | 用户更新请求 | mitigate | 需要管理员权限认证，JWT验证，参数校验 |
| T-20-18 | Tampering | AD同步数据 | mitigate | 使用事务保证数据一致性，AD操作失败不回滚系统更新 |
| T-20-19 | Repudiation | 同步操作 | mitigate | 详细记录同步日志（ad_synced_at、操作类型、错误信息） |
| T-20-20 | Denial of Service | 批量移动用户 | mitigate | 分批处理限制并发数（每批10个），批次间暂停1秒 |
| T-20-21 | Elevation of Privilege | LDAP操作权限 | mitigate | 使用最小权限AD服务账号，仅授予用户移动和属性更新权限 |
</threat_model>

<verification>
## 整体验证

1. **服务层验证**
   - 单元测试UserADSyncService的SyncUserUpdateToAD方法
   - 测试部门变更场景（移动用户到新OU）
   - 测试属性变更场景（更新displayName、mail等）
   - 测试用户无AD DN场景（跳过同步）

2. **API验证**
   - 测试PUT /api/system/users/:id端点
   - 验证AD同步异步执行（不阻塞HTTP响应）
   - 测试降级处理：AD同步失败不影响系统更新

3. **集成验证**
   - 端到端测试：修改用户部门 → 系统更新 → AD同步
   - 验证用户在AD中OU位置正确
   - 测试属性同步正确性

4. **场景验证**
   - 场景1：修改用户部门 → 移动用户到新OU → 更新ad_ou_dn
   - 场景2：修改用户邮箱 → 更新AD mail属性
   - 场景3：修改用户姓名 → 更新AD displayName属性
   - 场景4：用户无AD DN → 跳过AD同步

5. **错误处理验证**
   - LDAP连接失败 → 记录错误，不回滚系统更新
   - 移动用户失败 → 记录错误，继续处理属性更新
   - 映射表查询失败 → 记录错误，跳过AD同步

6. **性能验证**
   - 测试单个用户同步耗时 < 1秒
   - 测试批量用户移动（100个用户）< 30秒
   - 测试并发更新性能
</verification>

<success_criteria>
1. ✅ UserADSyncService实现完整，支持部门和属性同步
2. ✅ 用户部门变更时同步移动到新OU
3. ✅ 用户属性变更时同步到AD
4. ✅ AD同步异步执行，不阻塞HTTP响应
5. ✅ 降级处理：AD同步失败不影响系统更新
6. ✅ 详细日志记录同步操作和错误
7. ✅ 所有代码编译通过，无语法错误
</success_criteria>

<output>
After completion, create `.planning/phases/20-ad-ou-dept-mapping/20-04-SUMMARY.md`
</output>
