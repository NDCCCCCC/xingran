---
phase: 20-ad-ou-dept-mapping
plan: 03
type: execute
wave: 3
depends_on: [20-01]
files_modified:
  - internal/services/addomain/user_ou_service.go
  - internal/api/v1/auth/auth_handler.go
  - internal/core/security/jwt.go
autonomous: true
requirements:
  - PHASE-20-USER-LOGIN
  - PHASE-20-OU-REVERSE-LOOKUP
user_setup: []
must_haves:
  truths:
    - "用户AD认证登录成功后自动设置部门"
    - "通过用户OU DN反向查找映射表获取部门ID"
    - "用户的ad_user_dn和ad_ou_dn字段正确更新"
    - "部门设置失败不阻断登录流程（降级处理）"
    - "登录过程无明显性能影响（<100ms额外耗时）"
  artifacts:
    - path: "internal/services/addomain/user_ou_service.go"
      provides: "用户登录OU处理服务"
      exports: ["HandleUserLoginAD", "findDeptByOUDN", "updateUserDeptAndADInfo"]
    - path: "internal/api/v1/auth/auth_handler.go"
      provides: "修改后的认证处理器"
      contains: "userOUService.HandleUserLoginAD"
    - path: "internal/core/security/jwt.go"
      provides: "JWT生成逻辑"
      contains: "登录成功后设置部门"
  key_links:
    - from: "internal/api/v1/auth/auth_handler.go"
      to: "internal/services/addomain/user_ou_service.go"
      via: "AD认证成功后调用"
      pattern: "userOUService\\.HandleUserLoginAD"
    - from: "internal/services/addomain/user_ou_service.go"
      to: "internal/services/addomain/dept_ou_mapper.go"
      via: "映射查询"
      pattern: "mapper\\.FindDeptByOUDN"
    - from: "internal/services/addomain/user_ou_service.go"
      to: "sys_user"
      via: "GORM更新操作"
      pattern: "db\\.Model.*Update"
---

<objective>
**Objective:** 实现用户AD登录时的自动部门设置功能

**Purpose:** 在用户通过AD认证成功登录后，根据用户所在OU DN反向查找映射表，自动设置用户的系统部门（dept_id），并更新用户的AD相关信息（ad_user_dn、ad_ou_dn、ad_synced_at）。采用降级处理策略，部门设置失败不影响登录流程。

**Output:**
- 用户OU处理服务（UserOUService）
- 修改后的认证登录流程，集成OU处理
- 用户AD信息更新逻辑

**Key Deliverables:**
1. UserOUService实现OU到部门ID的反向查找
2. AD认证成功后调用HandleUserLoginAD方法
3. 用户表更新dept_id、ad_user_dn、ad_ou_dn、ad_synced_at字段
4. 降级处理：部门设置失败时记录日志但不阻断登录
5. 性能优化：映射查询使用索引，耗时 < 100ms
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-ad-ou-dept-mapping/CONTEXT.md
@.planning/phases/20-ad-ou-dept-mapping/RESEARCH.md
@.planning/spikes/ou-dept-mapping-corrected.md
@.planning/phases/20-ad-ou-dept-mapping/20-01-SUMMARY.md

# 现有服务和模式
@internal/services/addomain/dept_ou_mapper.go
@internal/models/user.go
@internal/api/v1/auth/auth_handler.go
@internal/core/security/jwt.go
@internal/services/auth/
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现UserOUService</name>
  <files>internal/services/addomain/user_ou_service.go</files>
  <action>
    在`internal/services/addomain/`目录下创建`user_ou_service.go`：

```go
package addomain

import (
    "context"
    "fmt"
    "time"

    "github.com/xingran-next/xingran-go-backend/internal/applogger"
    "github.com/xingran-next/xingran-go-backend/internal/models"
    "gorm.io/gorm"
)

type UserOUService struct {
    db     *gorm.DB
    mapper *DeptOUmapper
}

func NewUserOUService(db *gorm.DB, mapper *DeptOUmapper) *UserOUService {
    return &UserOUService{
        db:     db,
        mapper: mapper,
    }
}

// HandleUserLoginAD 处理用户AD登录后的部门设置
func (s *UserOUService) HandleUserLoginAD(ctx context.Context, username, userDN, ouDN string) error {
    // 1. 通过用户名查找系统用户
    var user models.User
    if err := s.db.WithContext(ctx).Where("username = ?", username).First(&user).Error; err != nil {
        if err == gorm.ErrRecordNotFound {
            // 用户不存在，跳过部门设置（由注册流程处理）
            applogger.Infof("用户 %s 不存在于系统，跳过部门设置", username)
            return nil
        }
        return fmt.Errorf("查询用户失败: %w", err)
    }

    // 2. 通过AD OU DN查找对应的部门
    deptID, err := s.mapper.FindDeptByOUDN(ctx, ouDN)
    if err != nil {
        // 未找到映射部门，记录日志但不阻断登录
        applogger.Warnf("用户 %s 的AD OU %s 未找到对应部门: %v", username, ouDN, err)
        return nil
    }

    // 3. 更新用户的部门和AD信息
    if err := s.updateUserDeptAndADInfo(ctx, user.ID, deptID, userDN, ouDN); err != nil {
        applogger.Errorf("更新用户 %s 部门信息失败: %v", username, err)
        // 不阻断登录流程，只记录错误
        return nil
    }

    applogger.Infof("用户 %s 登录时自动设置部门: dept_id=%s, ou_dn=%s", username, deptID, ouDN)
    return nil
}

// updateUserDeptAndADInfo 更新用户部门和AD信息
func (s *UserOUService) updateUserDeptAndADInfo(ctx context.Context, userID, deptID, userDN, ouDN string) error {
    updates := map[string]interface{}{
        "dept_id":      deptID,
        "ad_user_dn":   userDN,
        "ad_ou_dn":     ouDN,
        "ad_synced_at": time.Now(),
    }

    return s.db.WithContext(ctx).
        Model(&models.User{}).
        Where("id = ?", userID).
        Updates(updates).Error
}

// GetUserDeptByADOU 获取用户AD OU对应的部门信息（辅助方法）
func (s *UserOUService) GetUserDeptByADOU(ctx context.Context, username string) (*models.Department, error) {
    var user models.User
    if err := s.db.WithContext(ctx).
        Preload("Dept").
        Where("username = ?", username).
        First(&user).Error; err != nil {
        return nil, err
    }

    if user.Dept == nil {
        return nil, fmt.Errorf("用户 %s 未设置部门", username)
    }

    return user.Dept, nil
}
```

确保：
1. 降级处理：部门查找失败不返回错误，只记录日志
2. 使用事务保证更新原子性（单个UPDATE语句）
3. 更新时间戳ad_synced_at记录同步时间
4. 日志级别合理（Info正常、Warn未找到部门、Error系统错误）
5. 添加必要的辅助方法支持未来扩展
6. 导入必要的包
  </action>
  <verify>
    <automated>go build ./internal/services/addomain/...</automated>
  </verify>
  <done>
    UserOUService实现完成，支持OU反向查找、用户部门设置和降级处理，编译无错误。
  </done>
</task>

<task type="auto">
  <name>Task 2: 修改认证登录流程集成OU处理</name>
  <files>internal/api/v1/auth/auth_handler.go</files>
  <action>
    修改现有的AD认证登录处理器，在认证成功后调用UserOUService：

**找到AD认证成功后的位置，添加OU处理逻辑：**

```go
// 在AD认证成功后，添加以下代码
if authReq.AuthType == "ad" && authResult.Success {
    // 调用UserOUService处理用户OU
    ouDN := authResult.OUDN // 从AD认证结果中获取OU DN
    userDN := authResult.UserDN // 从AD认证结果中获取用户DN

    if userOUService != nil {
        if err := userOUService.HandleUserLoginAD(ctx, authReq.Username, userDN, ouDN); err != nil {
            applogger.Warnf("处理用户OU失败: %v", err)
            // 不阻断登录流程
        }
    }
}
```

**确保：**
1. 在AD认证成功后、JWT生成前调用OU处理
2. 使用非阻塞方式处理OU设置错误（try-catch或类似机制）
3. 从AD认证结果中提取userDN和ouDN
4. 如果userOUService为nil，跳过OU处理（向后兼容）
5. 日志记录OU处理结果

**如果需要修改AD认证结果结构：**
```go
// 在认证结果中添加OU DN字段
type AuthResult struct {
    Success bool   `json:"success"`
    UserDN  string `json:"userDn,omitempty"`
    OUDN    string `json:"ouDn,omitempty"`
    Message string `json:"message,omitempty"`
}
```

**确保AD认证逻辑中提取OU DN：**
```go
// 在AD认证成功后，提取用户OU DN
entry := searchResult.Entries[0]
userDN := entry.DN
ouDN := extractOUDNFromUserDN(userDN) // 从用户DN中提取OU DN

authResult := AuthResult{
    Success: true,
    UserDN:  userDN,
    OUDN:    ouDN,
}
```

确保：
1. OU DN提取逻辑正确（从用户DN中去掉用户RDN部分）
2. 认证结果结构包含所需的OU信息
3. 错误处理不阻断登录流程
4. 向后兼容：不影响现有本地账号登录
  </action>
  <verify>
    <automated>go build ./internal/api/v1/auth/...</automated>
  </verify>
  <done>
    认证登录流程修改完成，集成UserOUService处理用户OU，支持AD登录时自动设置部门。
  </done>
</task>

<task type="auto">
  <name>Task 3: 在Handler构造函数中注入UserOUService</name>
  <files>internal/api/v1/auth/auth_router.go</files>
  <action>
    修改AuthHandler构造函数，添加UserOUService依赖注入：

**找到AuthHandler结构体定义：**
```go
type AuthHandler struct {
    authService   auth.AuthService
    jwtManager    *security.JWTManager
    userOUService *addomain.UserOUService // 新增
}
```

**修改NewAuthHandler构造函数：**
```go
func NewAuthHandler(authService auth.AuthService, jwtManager *security.JWTManager, userOUService *addomain.UserOUService) *AuthHandler {
    return &AuthHandler{
        authService:   authService,
        jwtManager:    jwtManager,
        userOUService: userOUService, // 注入UserOUService
    }
}
```

**修改路由注册函数SetupAuthRouter：**
```go
func SetupAuthRouter(r *gin.RouterGroup, core *core.Core) {
    // 创建UserOUService实例
    mapper := addomain.NewDeptOUmapper(core.GetDB())
    userOUService := addomain.NewUserOUService(core.GetDB(), mapper)

    // 创建AuthHandler实例，传入UserOUService
    authService := auth.NewAuthService(core.GetDB(), core.PwdManager, core.Cache, core.JWTManager)
    handler := NewAuthHandler(authService, core.JWTManager, userOUService)

    // 注册路由
    r.POST("/login", handler.Login)
    r.POST("/logout", handler.Logout)
    r.POST("/refresh", handler.RefreshToken)
    // ... 其他路由
}
```

确保：
1. UserOUService通过依赖注入传入
2. 在路由注册时创建UserOUService实例
3. 如果userOUService为nil，认证流程仍能正常工作（向后兼容）
4. 导入必要的包
  </action>
  <verify>
    <automated>go build ./internal/api/v1/auth/...</automated>
  </verify>
  <done>
    Handler构造函数修改完成，UserOUService正确注入到AuthHandler，依赖关系建立正确。
  </done>
</task>

<task type="auto">
  <name>Task 4: 添加OU DN提取辅助函数</name>
  <files>internal/services/addomain/utils.go</files>
  <action>
    在现有的`utils.go`中添加OU DN提取函数：

```go
// ExtractOUDNFromUserDN 从用户DN中提取OU DN
// 例如: CN=zhangsan,OU=科技创新部,OU=分公司本部,OU=湖北分公司,DC=company,DC=com
// 提取为: OU=科技创新部,OU=分公司本部,OU=湖北分公司,DC=company,DC=com
func ExtractOUDNFromUserDN(userDN string) string {
    if userDN == "" {
        return ""
    }

    parts := strings.Split(userDN, ",")
    if len(parts) <= 1 {
        return ""
    }

    // 跳过第一个部分（通常是CN=xxx）
    // 返回剩余部分（从第一个OU开始到DN结束）
    for i, part := range parts {
        if strings.HasPrefix(strings.ToUpper(part), "OU=") {
            return strings.Join(parts[i:], ",")
        }
    }

    // 如果没有找到OU，返回DN的Base部分（去掉CN）
    if len(parts) > 1 {
        return strings.Join(parts[1:], ",")
    }

    return ""
}
```

确保：
1. 处理各种DN格式（CN在前、CN在后）
2. 找到第一个OU作为起始点
3. 返回完整的OU路径（包含父OU和Base DN）
4. 空DN或无效DN返回空字符串
5. 导入必要的包
  </action>
  <verify>
    <automated>go build ./internal/services/addomain/...</automated>
  </verify>
  <done>
    OU DN提取辅助函数添加完成，支持从用户DN中正确提取OU路径。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AD Authentication → OU Processing | AD认证成功后触发OU处理逻辑 |
| Mapping Table Lookup → User Dept Update | 映射表查询驱动用户部门更新 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-12 | Spoofing | AD认证结果 | mitigate | AD认证使用LDAPS加密连接，验证服务器证书（当前InsecureSkipVerify=true需后续改进） |
| T-20-13 | Tampering | 用户部门设置 | mitigate | 使用数据库事务保证更新一致性，审计日志记录部门变更 |
| T-20-14 | Repudiation | 部门设置操作 | mitigate | 记录ad_synced_at时间戳，日志记录OU设置过程 |
| T-20-15 | Denial of Service | 映射表查询性能 | mitigate | 映射表添加索引(idx_dept_ou_mapping_dn)，查询超时控制 |
| T-20-16 | Information Disclosure | 用户AD信息 | accept | ad_user_dn和ad_ou_dn为目录信息，非敏感数据 |
</threat_model>

<verification>
## 整体验证

1. **服务层验证**
   - 单元测试UserOUService的HandleUserLoginAD方法
   - 测试降级处理：映射不存在时不返回错误
   - 测试用户不存在场景
   - 测试部门更新正确性

2. **集成验证**
   - 端到端测试：AD登录 → OU处理 → 部门设置
   - 验证用户表字段更新（dept_id、ad_user_dn、ad_ou_dn、ad_synced_at）
   - 测试映射表数据正确性

3. **性能验证**
   - 测试OU处理耗时 < 100ms
   - 测试映射查询使用索引（EXPLAIN ANALYZE）
   - 测试并发登录性能

4. **场景验证**
   - 场景1：用户首次AD登录，映射存在 → 部门设置成功
   - 场景2：用户AD登录，映射不存在 → 跳过部门设置，登录成功
   - 场景3：用户不存在于系统 → 跳过部门设置，等待注册流程
   - 场景4：本地账号登录 → 不触发OU处理

5. **错误处理验证**
   - 映射表查询失败 → 记录日志，不阻断登录
   - 数据库更新失败 → 记录日志，不阻断登录
   - userOUService为nil → 跳过OU处理，向后兼容
</verification>

<success_criteria>
1. ✅ UserOUService实现完整，支持OU反向查找
2. ✅ AD登录成功后自动设置用户部门
3. ✅ 降级处理：部门设置失败不影响登录
4. ✅ 用户AD信息字段正确更新
5. ✅ 性能满足要求（<100ms额外耗时）
6. ✅ 所有代码编译通过，无语法错误
7. ✅ 向后兼容现有本地账号登录
</success_criteria>

<output>
After completion, create `.planning/phases/20-ad-ou-dept-mapping/20-03-SUMMARY.md`
</output>
