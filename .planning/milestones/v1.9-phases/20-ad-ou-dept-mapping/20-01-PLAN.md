---
phase: 20-ad-ou-dept-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/core/db/migrations/085_create_dept_ou_mapping_table.sql
  - internal/core/db/migrations/086_add_user_ad_fields.sql
  - internal/models/dept_ou_mapping.go
  - internal/services/addomain/dept_ou_mapper.go
  - internal/services/addomain/ldap_client.go
autonomous: true
requirements:
  - PHASE-20-DATA-MODEL
  - PHASE-20-MAPPER-SERVICE
  - PHASE-20-LDAP-EXTENSIONS
user_setup: []
must_haves:
  truths:
    - "数据库迁移脚本创建成功并包含完整的表结构和索引"
    - "DeptOUMapping模型定义完整，包含所有必需字段和关联"
    - "DeptOUmapper服务实现基本的CRUD操作和映射查询功能"
    - "LDAP客户端扩展支持CreateOU、OUExists、MoveUser、UpdateUserAttributes操作"
    - "所有数据库约束和外键关系正确建立"
  artifacts:
    - path: "internal/core/db/migrations/085_create_dept_ou_mapping_table.sql"
      provides: "部门-OU映射表结构"
      contains: "CREATE TABLE sys_dept_ou_mapping"
    - path: "internal/core/db/migrations/086_add_user_ad_fields.sql"
      provides: "用户表AD相关字段扩展"
      contains: "ALTER TABLE sys_user ADD COLUMN"
    - path: "internal/models/dept_ou_mapping.go"
      provides: "部门-OU映射数据模型"
      min_lines: 40
    - path: "internal/services/addomain/dept_ou_mapper.go"
      provides: "映射查询服务"
      exports: ["FindDeptByOUDN", "FindOUDNByDeptID", "UpsertMapping"]
    - path: "internal/services/addomain/ldap_client.go"
      provides: "扩展的LDAP客户端"
      exports: ["CreateOU", "OUExists", "MoveUser", "UpdateUserAttributes"]
  key_links:
    - from: "internal/models/dept_ou_mapping.go"
      to: "sys_dept_ou_mapping"
      via: "GORM TableName()方法"
      pattern: "func \\(DeptOUMapping\\) TableName\\(\\)"
    - from: "internal/services/addomain/dept_ou_mapper.go"
      to: "internal/models/dept_ou_mapping.go"
      via: "GORM模型引用"
      pattern: "models\\.DeptOUMapping"
    - from: "internal/services/addomain/ldap_client.go"
      to: "ldap.Conn"
      via: "go-ldap/v3客户端连接"
      pattern: "ldap\\.NewAddRequest|ldap\\.NewModifyRequest"
---

<objective>
**Objective:** 创建部门-OU映射的数据模型和基础服务设施

**Purpose:** 建立系统部门与AD域控OU映射的基础数据结构，包括数据库表设计、GORM模型定义、映射查询服务和LDAP客户端扩展，为后续同步功能提供数据层和服务层支持。

**Output:**
- 数据库迁移脚本（映射表创建 + 用户表扩展）
- GORM模型定义（DeptOUMapping）
- 映射查询服务（DeptOUmapper）
- LDAP客户端功能扩展（OU和用户操作）

**Key Deliverables:**
1. `sys_dept_ou_mapping`表及其索引和约束
2. `sys_user`表AD字段扩展（ad_user_dn, ad_ou_dn, ad_synced_at）
3. DeptOUMapping模型，包含完整的字段定义和关联关系
4. DeptOUmapper服务，提供双向映射查询和Upsert功能
5. LDAP客户端新增CreateOU、OUExists、MoveUser、UpdateUserAttributes方法
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-ad-ou-dept-mapping/CONTEXT.md
@.planning/phases/20-ad-ou-dept-mapping/RESEARCH.md
@.planning/spikes/ou-dept-mapping-corrected.md

# 现有模型和模式
@internal/models/dept.go
@internal/models/user.go
@internal/services/addomain/ldap_client.go
@internal/services/addomain/config.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建数据库迁移脚本</name>
  <files>
    internal/core/db/migrations/085_create_dept_ou_mapping_table.sql
    internal/core/db/migrations/086_add_user_ad_fields.sql
  </files>
  <action>
    创建两个迁移脚本：

**085_create_dept_ou_mapping_table.sql:**
- 创建`sys_dept_ou_mapping`表，包含字段：
  - id (UUID主键，默认gen_random_uuid())
  - dept_id (UUID外键 → sys_dept.id)
  - ad_config_id (UUID外键 → sys_ad_config.id)
  - ou_dn (VARCHAR(500)，非空)
  - ou_name (VARCHAR(255)，非空)
  - parent_ou_dn (VARCHAR(500)，可空)
  - sync_enabled (BOOLEAN，默认true)
  - sync_status (VARCHAR(20)，默认'pending')
  - last_sync_at (TIMESTAMP，可空)
  - created_at, updated_at (TIMESTAMP，非空)
- 添加唯一约束：UNIQUE(dept_id, ad_config_id)
- 添加索引：idx_dept_ou_mapping_dn (ou_dn), idx_dept_ou_mapping_dept (dept_id)
- 添加表注释

**086_add_user_ad_fields.sql:**
- 为`sys_user`表添加字段（如果不存在）：
  - ad_user_dn (VARCHAR(500)，可空) - AD用户完整DN
  - ad_ou_dn (VARCHAR(500)，可空) - 用户所在OU的DN
  - ad_synced_at (TIMESTAMP，可空) - 最后AD同步时间
- 使用`ALTER TABLE ... ADD COLUMN IF NOT EXISTS`语法确保幂等性

参考Spike文档中的表结构定义，确保字段类型、约束和索引完全一致。
  </action>
  <verify>
    <automated>go run ./cmd/main.go --migrate-only 2>&1 | grep -E "(085|086)"</automated>
  </verify>
  <done>
    迁移脚本创建完成，包含完整的表结构、约束、索引和外键关系，并通过PostgreSQL语法检查。
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建DeptOUMapping模型</name>
  <files>internal/models/dept_ou_mapping.go</files>
  <action>
    在`internal/models/`目录下创建`dept_ou_mapping.go`：

```go
package models

import "time"

type DeptOUMapping struct {
    ID         string     `gorm:"primaryKey;type:uuid" json:"id"`
    DeptID     string     `gorm:"type:uuid;not null;uniqueIndex:uni_dept_ou_mapping,priority:0" json:"deptId"`
    ADConfigID string     `gorm:"type:uuid;not null;uniqueIndex:uni_dept_ou_mapping,priority:1" json:"adConfigId"`
    OUDN       string     `gorm:"size:500;not null;index:idx_dept_ou_mapping_dn" json:"ouDn"`
    OUName     string     `gorm:"size:255;not null" json:"ouName"`
    ParentOUDN *string    `gorm:"size:500" json:"parentOuDn,omitempty"`
    SyncEnabled bool      `gorm:"default:true" json:"syncEnabled"`
    SyncStatus string     `gorm:"size:20;default:pending" json:"syncStatus"` // pending/synced/failed
    LastSyncAt *time.Time `json:"lastSyncAt,omitempty"`
    CreatedAt  time.Time  `json:"createdAt"`
    UpdatedAt  time.Time  `json:"updatedAt"`

    // 关联（不持久化）
    Dept     *Department `gorm:"foreignKey:DeptID" json:"dept,omitempty"`
    ADConfig *ADConfig   `gorm:"foreignKey:ADConfigID" json:"adConfig,omitempty"`
}

func (DeptOUMapping) TableName() string {
    return "sys_dept_ou_mapping"
}
```

确保：
1. 使用正确的GORM标签（primaryKey、type、uniqueIndex、foreignKey等）
2. JSON字段使用camelCase命名
3. SyncStatus默认值为"pending"
4. 关联字段使用指针避免循环引用问题
5. 导入必要的包（time）
  </action>
  <verify>
    <automated>go build ./internal/models/...</automated>
  </verify>
  <done>
    DeptOUMapping模型创建完成，包含完整的字段定义、GORM标签和关联关系，编译无错误。
  </done>
</task>

<task type="auto">
  <name>Task 3: 实现DeptOUmapper服务</name>
  <files>internal/services/addomain/dept_ou_mapper.go</files>
  <action>
    在`internal/services/addomain/`目录下创建`dept_ou_mapper.go`：

```go
package addomain

import (
    "context"
    "fmt"

    "github.com/xingran-next/xingran-go-backend/internal/models"
    "gorm.io/gorm"
    "gorm.io/gorm/clause"
)

type DeptOUmapper struct {
    db *gorm.DB
}

func NewDeptOUmapper(db *gorm.DB) *DeptOUmapper {
    return &DeptOUmapper{db: db}
}

// FindDeptByOUDN 通过OU DN查找部门ID
func (m *DeptOUmapper) FindDeptByOUDN(ctx context.Context, ouDN string) (string, error) {
    var mapping models.DeptOUMapping
    err := m.db.WithContext(ctx).
        Where("ou_dn = ? AND sync_enabled = ?", ouDN, true).
        First(&mapping).Error
    if err != nil {
        return "", fmt.Errorf("未找到OU DN %s 对应的部门: %w", ouDN, err)
    }
    return mapping.DeptID, nil
}

// FindOUDNByDeptID 通过部门ID查找OU DN
func (m *DeptOUmapper) FindOUDNByDeptID(ctx context.Context, deptID string) (string, error) {
    var mapping models.DeptOUMapping
    err := m.db.WithContext(ctx).
        Where("dept_id = ? AND sync_enabled = ?", deptID, true).
        First(&mapping).Error
    if err != nil {
        return "", fmt.Errorf("未找到部门 %s 对应的OU DN: %w", deptID, err)
    }
    return mapping.OUDN, nil
}

// UpsertMapping 创建或更新映射关系
func (m *DeptOUmapper) UpsertMapping(ctx context.Context, mapping *models.DeptOUMapping) error {
    return m.db.WithContext(ctx).
        Clauses(clause.OnConflict{
            Columns:   []clause.Column{{Name: "dept_id"}, {Name: "ad_config_id"}},
            DoUpdates: clause.AssignmentColumns([]string{"ou_dn", "ou_name", "parent_ou_dn", "sync_status", "last_sync_at", "updated_at"}),
        }).
        Create(mapping).Error
}

// GetMappingByDept 获取部门的映射关系
func (m *DeptOUmapper) GetMappingByDept(ctx context.Context, deptID string) (*models.DeptOUMapping, error) {
    var mapping models.DeptOUMapping
    err := m.db.WithContext(ctx).
        Where("dept_id = ?", deptID).
        First(&mapping).Error
    if err != nil {
        return nil, err
    }
    return &mapping, nil
}
```

确保：
1. 使用GORM的WithContext支持context传递
2. UpsertMapping使用OnConflict处理幂等性
3. 错误信息包含足够的上下文（OU DN、部门ID）
4. 符合现有代码库的错误处理模式
5. 添加必要的导入（gorm.io/gorm、gorm.io/gorm/clause）
  </action>
  <verify>
    <automated>go build ./internal/services/addomain/...</automated>
  </verify>
  <done>
    DeptOUmapper服务实现完成，包含双向映射查询、Upsert和按部门查询功能，编译无错误。
  </done>
</task>

<task type="auto">
  <name>Task 4: 扩展LDAP客户端（OU和用户操作）</name>
  <files>internal/services/addomain/ldap_client.go</files>
  <action>
    在现有`ldap_client.go`中添加以下方法：

**CreateOU方法:**
```go
// CreateOU 在AD中创建OU
func (c *LDAPClient) CreateOU(ouDN, ouName string) error {
    // 检查OU是否已存在
    exists, err := c.OUExists(ouDN)
    if err != nil {
        return fmt.Errorf("检查OU存在性失败: %w", err)
    }
    if exists {
        return nil // 已存在，跳过
    }

    // 创建新OU
    addRequest := ldap.NewAddRequest(ouDN, nil)
    addRequest.Attribute("objectClass", []string{"organizationalUnit"})
    addRequest.Attribute("ou", []string{ouName})
    addRequest.Attribute("description", []string{fmt.Sprintf("同步自系统部门: %s", ouName)})

    if err := c.conn.Add(addRequest); err != nil {
        return fmt.Errorf("创建OU失败 %s: %w", ouDN, err)
    }

    return nil
}
```

**OUExists方法:**
```go
// OUExists 检查OU是否存在
func (c *LDAPClient) OUExists(ouDN string) (bool, error) {
    searchRequest := ldap.NewSearchRequest(
        ouDN,
        ldap.ScopeBaseObject,
        ldap.NeverDerefAliases,
        0, 0, false,
        "(objectClass=*)",
        []string{"dn"},
        nil,
    )

    _, err := c.conn.Search(searchRequest)
    if err != nil {
        if ldap.IsErrorWithCode(err, ldap.LDAPResultNoSuchObject) {
            return false, nil
        }
        return false, fmt.Errorf("搜索OU失败: %w", err)
    }
    return true, nil
}
```

**MoveUser方法:**
```go
// MoveUser 移动用户到新OU
func (c *LDAPClient) MoveUser(userDN, newParentOUDN string) error {
    // 提取用户的RDN（相对标识名）
    rdn := c.extractRDN(userDN)

    // LDAP modify DN操作可以移动条目
    modifyDNRequest := ldap.NewModifyDNRequest(
        userDN,
        rdn,
        true, // deleteOldRDN
        newParentOUDN,
    )

    if err := c.conn.ModifyDN(modifyDNRequest); err != nil {
        return fmt.Errorf("移动用户失败 %s -> %s: %w", userDN, newParentOUDN, err)
    }

    return nil
}

// extractRDN 从DN中提取相对标识名
func (c *LDAPClient) extractRDN(dn string) string {
    parts := strings.Split(dn, ",")
    if len(parts) > 0 {
        return parts[0]
    }
    return dn
}
```

**UpdateUserAttributes方法:**
```go
// UpdateUserAttributes 更新用户属性
func (c *LDAPClient) UpdateUserAttributes(userDN string, attributes map[string]string) error {
    if len(attributes) == 0 {
        return nil
    }

    modifyRequest := ldap.NewModifyRequest(userDN, nil)

    for attr, value := range attributes {
        modifyRequest.Replace(attr, []string{value})
    }

    if err := c.conn.Modify(modifyRequest); err != nil {
        return fmt.Errorf("更新用户属性失败 %s: %w", userDN, err)
    }

    return nil
}
```

确保：
1. 所有方法都有适当的错误处理和日志记录
2. CreateOU幂等性：OU存在时跳过而非报错
3. MoveUser使用LDAP ModifyDN操作（标准移动条目方法）
4. UpdateUserAttributes支持批量属性更新
5. 保持与现有ldap_client.go代码风格一致（错误处理、日志记录等）
6. 导入必要的包（fmt、strings）
  </action>
  <verify>
    <automated>go build ./internal/services/addomain/...</automated>
  </verify>
  <done>
    LDAP客户端扩展完成，支持OU创建/检查、用户移动和属性更新操作，所有方法编译无错误。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go backend → AD Domain Controller | LDAP连接传输部门结构和用户数据 |
| Admin → Mapping Table | 管理员可手动配置部门-OU映射关系 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Spoofing | LDAP连接 | mitigate | 使用LDAPS/LDAP+StartTLS加密连接，验证AD服务器证书（当前InsecureSkipVerify=true需后续改进） |
| T-20-02 | Tampering | Mapping表数据 | mitigate | 数据库外键约束防止无效引用，审计日志记录所有映射变更 |
| T-20-03 | Repudiation | 同步操作 | mitigate | 详细记录同步日志（last_sync_at、sync_status），操作者追溯 |
| T-20-04 | Information Disclosure | OU DN泄露 | accept | OU DN为非敏感目录结构信息，正常业务使用 |
| T-20-05 | Denial of Service | 大量LDAP操作 | mitigate | 连接池管理、操作超时控制、分批处理限制并发 |
| T-20-06 | Elevation of Privilege | AD权限滥用 | mitigate | 使用最小权限AD服务账号，仅授予创建OU和移动用户权限 |
</threat_model>

<verification>
## 整体验证

1. **数据库迁移验证**
   - 运行应用启动，迁移脚本自动执行
   - 检查PostgreSQL中表结构：`\d sys_dept_ou_mapping`
   - 验证索引和约束创建成功

2. **模型验证**
   - 单元测试DeptOUMapping模型的TableName()方法
   - 测试GORM标签正确性（使用GORM AutoMigrate）

3. **服务验证**
   - 单元测试DeptOUmapper的四个方法
   - 测试UpsertMapping的幂等性（多次调用相同参数）

4. **LDAP客户端验证**
   - 使用测试AD服务器验证CreateOU、OUExists功能
   - 测试MoveUser、UpdateUserAttributes操作
   - 验证错误处理（不存在的DN、权限不足等）

5. **集成验证**
   - 创建测试数据验证映射表CRUD操作
   - 测试LDAP连接和基本操作
</verification>

<success_criteria>
1. ✅ 数据库迁移脚本执行成功，表结构和约束正确创建
2. ✅ DeptOUMapping模型编译通过，GORM标签和关联关系正确
3. ✅ DeptOUmapper服务实现完整，支持双向映射查询和Upsert
4. ✅ LDAP客户端扩展功能完整，支持OU和用户操作
5. ✅ 所有代码通过编译，无语法错误
6. ✅ 单元测试覆盖核心映射查询功能
</success_criteria>

<output>
After completion, create `.planning/phases/20-ad-ou-dept-mapping/20-01-SUMMARY.md`
</output>
