---
phase: 19
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - internal/core/security/authenticator.go
  - internal/core/security/local_authenticator.go
  - internal/core/security/ad_authenticator.go
  - internal/core/security/hybrid_authenticator.go
  - internal/core/security/auth_strategy_factory.go
autonomous: true
requirements: [AUTH-01, AUTH-02]
user_setup: []
must_haves:
  truths:
    - 策略模式认证系统已实现，支持三种认证方式（本地/AD/混合）
    - 认证器接口定义清晰，所有认证器实现相同接口
    - 本地认证器使用SM3密码验证，查询sys_user表
    - AD认证器使用go-ldap/v3连接域控，执行LDAP绑定验证
    - 混合认证器实现降级逻辑（本地失败→尝试AD）
    - 认证策略工厂根据模式字符串动态选择认证器
  artifacts:
    - path: internal/core/security/authenticator.go
      provides: 认证器接口定义
      exports: ["Authenticator", "AuthRequest", "AuthResult"]
      min_lines: 50
    - path: internal/core/security/local_authenticator.go
      provides: 本地认证实现
      exports: ["LocalAuthenticator", "NewLocalAuthenticator"]
      min_lines: 80
    - path: internal/core/security/ad_authenticator.go
      provides: AD域控认证实现
      exports: ["ADAuthenticator", "NewADAuthenticator"]
      min_lines: 120
    - path: internal/core/security/hybrid_authenticator.go
      provides: 混合认证实现
      exports: ["HybridAuthenticator", "NewHybridAuthenticator"]
      min_lines: 80
    - path: internal/core/security/auth_strategy_factory.go
      provides: 认证策略工厂
      exports: ["AuthStrategyFactory", "NewAuthStrategyFactory"]
      min_lines: 80
  key_links:
    - from: internal/api/v1/auth.go
      to: internal/core/security/auth_strategy_factory.go
      via: import和依赖注入
      pattern: AuthStrategyFactory
    - from: internal/core/security/ad_authenticator.go
      to: internal/services/addomain/ldap_client.go
      via: 复用现有LDAP客户端
      pattern: addomain\.LDAPClient
    - from: internal/core/security/auth_strategy_factory.go
      to: internal/core/security/authenticator.go
      via: 接口实现
      pattern: Authenticator
---

<objective>
实现策略模式认证系统，支持本地、AD域控和混合三种认证方式

Purpose: 通过策略模式重构认证系统，实现运行时可切换的认证方式，为AD域控账号登录提供核心认证能力
Output: 完整的策略模式认证系统，包含三种认证器实现和工厂类
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/19-ad-auth-login/19-RESEARCH.md
@.planning/phases/19-ad-auth-login/19-02-PLAN.md

@internal/core/security/password.go
@internal/api/v1/auth.go
@internal/services/addomain/ldap_client.go
@internal/services/addomain/service.go

# 现有认证模式
From internal/api/v1/auth.go (lines 84-150):
- 当前使用硬编码的本地认证逻辑
- 直接查询sys_user表
- 使用SM3密码验证（security.NewPasswordManager）
- 用户状态检查（UserStatusEnabled）

# 现有LDAP客户端
From internal/services/addomain/ldap_client.go:
- 提供LDAP连接管理（Connect, Close方法）
- 支持LDAPS/LDAP+StartTLS/普通LDAP
- 使用go-ldap/v3库
- 已实现连接池和重试逻辑
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建认证器接口定义</name>
  <files>internal/core/security/authenticator.go</files>
  <read_first>internal/core/security/password.go, internal/models/user.go</read_first>
  <action>
    创建认证器接口和核心数据结构：

    1. 创建`internal/core/security/authenticator.go`:
       ```go
       package security

       import "context"

       // Authenticator 认证器接口
       type Authenticator interface {
           // Authenticate 执行认证，返回用户信息或错误
           Authenticate(ctx context.Context, req *AuthRequest) (*AuthResult, error)

           // Name 返回认证器名称
           Name() string
       }

       // AuthRequest 认证请求
       type AuthRequest struct {
           Username string
           Password string
           IP       string // 客户端IP，用于日志记录
       }

       // AuthResult 认证结果
       type AuthResult struct {
           User       *UserResult // 用户信息
           AuthSource string      // "local" or "ad"
           ADUserInfo *ADUserInfo // AD用户信息（用于自动同步）
           NeedsSync  bool        // 是否需要同步用户信息
       }

       // UserResult 用户信息（简化版，避免循环依赖）
       type UserResult struct {
           ID       string
           Username string
           Nickname *string
           Email    *string
           Phone    *string
           Status   int
           DeptID   *string
           Roles    []string
       }

       // ADUserInfo AD用户信息
       type ADUserInfo struct {
           UserDN     string
           Username   string
           DisplayName string
           Email      string
           Phone      string
           Mobile     string
           Title      string
           Department string
       }

       // 认证错误定义
       var (
           ErrUserNotFound       = errors.New("用户不存在")
           ErrInvalidCredentials = errors.New("用户名或密码错误")
           ErrUserDisabled       = errors.New("用户已被禁用")
           ErrADConfigNotFound   = errors.New("AD配置不存在")
           ErrADConnectionFailed = errors.New("AD连接失败")
       )
     ```

    关键设计点：
    - 接口简单清晰，只有Authenticate和Name两个方法
    - AuthResult包含AD用户信息，支持自动同步
    - NeedsSync标志控制是否需要同步到sys_user
    - 定义标准错误类型，便于错误处理
  </action>
  <verify>
    <automated>go build ./internal/core/security</automated>
  </verify>
  <done>认证器接口已定义，包含所有必要的数据结构和错误类型</done>
</task>

<task type="auto">
  <name>Task 2: 实现本地认证器</name>
  <files>internal/core/security/local_authenticator.go</files>
  <read_first>internal/core/security/authenticator.go, internal/core/security/password.go, internal/api/v1/auth.go</read_first>
  <action>
    实现本地认证器，复用现有认证逻辑：

    1. 创建`internal/core/security/local_authenticator.go`:
       ```go
       package security

       import (
           "context"
           "errors"
           "gorm.io/gorm"
           "github.com/xingran-next/xingran-go-backend/internal/models"
       )

       // LocalAuthenticator 本地认证器
       type LocalAuthenticator struct {
           db         *gorm.DB
           pwdManager *PasswordManager
       }

       // NewLocalAuthenticator 创建本地认证器
       func NewLocalAuthenticator(db *gorm.DB, pwdMgr *PasswordManager) *LocalAuthenticator {
           return &LocalAuthenticator{
               db:         db,
               pwdManager: pwdMgr,
           }
       }

       // Authenticate 实现本地认证
       func (a *LocalAuthenticator) Authenticate(ctx context.Context, req *AuthRequest) (*AuthResult, error) {
           var user models.User
           if err := a.db.WithContext(ctx).Where("username = ?", req.Username).First(&user).Error; err != nil {
               if errors.Is(err, gorm.ErrRecordNotFound) {
                   return nil, ErrUserNotFound
               }
               return nil, err
           }

           // 检查用户状态
           if user.Status != models.UserStatusEnabled {
               return nil, ErrUserDisabled
           }

           // 验证密码（使用SM3）
           if ok, err := a.pwdManager.VerifyPassword(req.Password, user.Password); err != nil {
               return nil, err
           } else if !ok {
               return nil, ErrInvalidCredentials
           }

           // 转换为UserResult
           userResult := &UserResult{
               ID:       user.ID,
               Username: user.Username,
               Nickname: user.Nickname,
               Email:    user.Email,
               Phone:    user.Phone,
               Status:   int(user.Status),
               DeptID:   user.DeptID,
               Roles:    user.Roles, // 假设已加载
           }

           return &AuthResult{
               User:       userResult,
               AuthSource: "local",
               NeedsSync:  false,
           }, nil
       }

       func (a *LocalAuthenticator) Name() string {
           return "local"
       }
     ```

    复用现有auth.go的认证逻辑，保持一致性
  </action>
  <verify>
    <automated>go test ./internal/core/security -run TestLocalAuthenticator -v</automated>
  </verify>
  <done>本地认证器已实现，通过单元测试验证正确性</done>
</task>

<task type="auto">
  <name>Task 3: 实现AD认证器</name>
  <files>internal/core/security/ad_authenticator.go</files>
  <read_first>internal/core/security/authenticator.go, internal/services/addomain/ldap_client.go, internal/services/addomain/service.go</read_first>
  <action>
    实现AD域控认证器，复用现有LDAP客户端：

    1. 创建`internal/core/security/ad_authenticator.go`:
       ```go
       package security

       import (
           "context"
           "fmt"
           "github.com/go-ldap/ldap/v3"
           "github.com/xingran-next/xingran-go-backend/internal/models"
           "github.com/xingran-next/xingran-go-backend/internal/services/addomain"
       )

       // ADAuthenticator AD认证器
       type ADAuthenticator struct {
           adDomainService *addomain.ADDomainService
           configID        string // AD配置ID
       }

       // NewADAuthenticator 创建AD认证器
       func NewADAuthenticator(adSvc *addomain.ADDomainService, configID string) *ADAuthenticator {
           return &ADAuthenticator{
               adDomainService: adSvc,
               configID:        configID,
           }
       }

       // Authenticate 实现AD认证
       func (a *ADAuthenticator) Authenticate(ctx context.Context, req *AuthRequest) (*AuthResult, error) {
           // 1. 获取AD配置
           var config models.ADConfig
           if err := a.adDomainService.GetDB().Where("id = ? AND status = 0", a.configID).First(&config).Error; err != nil {
               if err == gorm.ErrRecordNotFound {
                   return nil, ErrADConfigNotFound
               }
               return nil, fmt.Errorf("获取AD配置失败: %w", err)
           }

           // 2. 创建LDAP连接
           ldapClient := addomain.NewLDAPClient(&config)
           if err := ldapClient.Connect(); err != nil {
               return nil, fmt.Errorf("%w: %v", ErrADConnectionFailed, err)
           }
           defer ldapClient.Close()

           // 3. 构造用户DN并尝试绑定验证
           // 方案A: 直接使用UPN格式绑定 (username@domain.com)
           userUPN := fmt.Sprintf("%s@%s", req.Username, config.DomainName)

           conn := ldapClient.GetConn()
           if err := conn.Bind(userUPN, req.Password); err != nil {
               return nil, ErrInvalidCredentials
           }

           // 4. 搜索用户信息（用于同步）
           searchRequest := ldap.NewSearchRequest(
               config.BaseDN,
               ldap.ScopeWholeSubtree,
               ldap.NeverDerefAliases,
               0, 0, false,
               fmt.Sprintf("(sAMAccountName=%s)", req.Username),
               []string{"dn", "cn", "displayName", "mail", "telephoneNumber", "mobile", "title", "department"},
               nil,
           )

           sr, err := conn.Search(searchRequest)
           if err != nil {
               return nil, fmt.Errorf("查询AD用户失败: %w", err)
           }

           if len(sr.Entries) == 0 {
               return nil, ErrUserNotFound
           }

           entry := sr.Entries[0]

           // 5. 构造认证结果（包含AD用户信息）
           adUserInfo := &ADUserInfo{
               UserDN:      entry.DN,
               Username:    req.Username,
               DisplayName: entry.GetAttributeValue("displayName"),
               Email:       entry.GetAttributeValue("mail"),
               Phone:       entry.GetAttributeValue("telephoneNumber"),
               Mobile:      entry.GetAttributeValue("mobile"),
               Title:       entry.GetAttributeValue("title"),
               Department:  entry.GetAttributeValue("department"),
           }

           return &AuthResult{
               User:       nil, // AD认证可能没有sys_user记录
               AuthSource: "ad",
               ADUserInfo: adUserInfo,
               NeedsSync:  true, // 标记需要同步到sys_user
           }, nil
       }

       func (a *ADAuthenticator) Name() string {
           return "ad"
       }
     ```

    关键实现点：
    - 复用现有addomain.LDAPClient连接管理
    - 使用UPN格式绑定（username@domain.com）
    - 成功绑定后搜索用户详细信息
    - 返回ADUserInfo供后续同步使用
  </action>
  <verify>
    <automated>go test ./internal/core/security -run TestADAuthenticator -v</automated>
  </verify>
  <done>AD认证器已实现，通过单元测试验证LDAP连接和认证逻辑</done>
</task>

<task type="auto">
  <name>Task 4: 实现混合认证器</name>
  <files>internal/core/security/hybrid_authenticator.go</files>
  <read_first>internal/core/security/authenticator.go, internal/core/security/local_authenticator.go, internal/core/security/ad_authenticator.go</read_first>
  <action>
    实现混合认证器，提供降级逻辑：

    1. 创建`internal/core/security/hybrid_authenticator.go`:
       ```go
       package security

       import "context"

       // HybridAuthenticator 混合认证器
       type HybridAuthenticator struct {
           localAuth *LocalAuthenticator
       adAuth    *ADAuthenticator
       }

       // NewHybridAuthenticator 创建混合认证器
       func NewHybridAuthenticator(local *LocalAuthenticator, ad *ADAuthenticator) *HybridAuthenticator {
           return &HybridAuthenticator{
               localAuth: local,
               adAuth:    ad,
           }
       }

       // Authenticate 实现混合认证（降级逻辑）
       func (h *HybridAuthenticator) Authenticate(ctx context.Context, req *AuthRequest) (*AuthResult, error) {
           // 1. 先尝试本地认证
           result, err := h.localAuth.Authenticate(ctx, req)
           if err == nil {
               // 本地认证成功，直接返回
               return result, nil
           }

           // 2. 本地认证失败，记录日志（但不返回错误）
           // TODO: 添加日志记录本地认证失败原因

           // 3. 尝试AD认证
           adResult, err := h.adAuth.Authenticate(ctx, req)
           if err != nil {
               // AD认证也失败，返回原始的本地认证错误（更通用）
               return nil, err
           }

           // 4. AD认证成功，确保标记需要同步
           adResult.NeedsSync = true
           return adResult, nil
       }

       func (h *HybridAuthenticator) Name() string {
           return "hybrid"
       }
     ```

    关键设计点：
    - 优先使用本地认证（性能更好）
    - 本地失败后自动降级到AD认证
    - AD认证成功时强制设置NeedsSync=true
    - 错误处理返回AD认证错误（更具体）
  </action>
  <verify>
    <automated>go test ./internal/core/security -run TestHybridAuthenticator -v</automated>
  </verify>
  <done>混合认证器已实现，通过单元测试验证降级逻辑正确性</done>
</task>

<task type="auto">
  <name>Task 5: 实现认证策略工厂</name>
  <files>internal/core/security/auth_strategy_factory.go</files>
  <read_first>internal/core/security/authenticator.go, internal/core/security/local_authenticator.go, internal/core/security/ad_authenticator.go, internal/core/security/hybrid_authenticator.go</read_first>
  <action>
    实现认证策略工厂，支持动态选择认证器：

    1. 创建`internal/core/security/auth_strategy_factory.go`:
       ```go
       package security

       import (
           "context"
           "errors"
           "gorm.io/gorm"
           "github.com/xingran-next/xingran-go-backend/internal/models"
           "github.com/xingran-next/xingran-go-backend/internal/services/addomain"
       )

       // AuthStrategyFactory 认证策略工厂
       type AuthStrategyFactory struct {
       db              *gorm.DB
       pwdManager      *PasswordManager
       adDomainService *addomain.ADDomainService
       }

       // NewAuthStrategyFactory 创建认证策略工厂
       func NewAuthStrategyFactory(db *gorm.DB, pwdMgr *PasswordManager, adSvc *addomain.ADDomainService) *AuthStrategyFactory {
           return &AuthStrategyFactory{
               db:              db,
               pwdManager:      pwdMgr,
               adDomainService: adSvc,
           }
       }

       // GetAuthenticator 根据认证模式获取认证器
       func (f *AuthStrategyFactory) GetAuthenticator(mode string) (Authenticator, error) {
           switch mode {
           case "local":
               return NewLocalAuthenticator(f.db, f.pwdManager), nil

           case "ad":
               // 从配置读取默认AD配置ID
               configID, err := f.getADConfigID()
               if err != nil {
                   return nil, err
               }
               return NewADAuthenticator(f.adDomainService, configID), nil

           case "hybrid":
               local := NewLocalAuthenticator(f.db, f.pwdManager)
               configID, err := f.getADConfigID()
               if err != nil {
                   return nil, err
               }
               ad := NewADAuthenticator(f.adDomainService, configID)
               return NewHybridAuthenticator(local, ad), nil

           default:
               return nil, errors.New("不支持的认证模式: " + mode)
           }
       }

       // GetDefaultAuthMode 获取默认认证模式
       func (f *AuthStrategyFactory) GetDefaultAuthMode(ctx context.Context) (string, error) {
           // 从sys_config表读取配置
           var config models.Config
           err := f.db.WithContext(ctx).
               Where("config_key = ?", "sys.auth.default.mode").
               First(&config).Error

           if err != nil {
               if errors.Is(err, gorm.ErrRecordNotFound) {
                   // 配置不存在，返回默认值
                   return "local", nil
               }
               return "", err
           }

           // 验证配置值是否有效
           mode := config.ConfigValue
           if mode != "local" && mode != "ad" && mode != "hybrid" {
               return "local", nil // 无效值，返回默认
           }

           return mode, nil
       }

       // getADConfigID 获取AD配置ID
       func (f *AuthStrategyFactory) getADConfigID() (string, error) {
           var config models.Config
           err := f.db.Where("config_key = ?", "sys.auth.ad.config_id").First(&config).Error

           if err != nil {
               if errors.Is(err, gorm.ErrRecordNotFound) {
                   // 配置不存在，尝试使用第一个启用的AD配置
                   var adConfig models.ADConfig
                   err := f.db.Where("status = 0").First(&adConfig).Error
                   if err != nil {
                       return "", errors.New("没有找到可用的AD配置")
                   }
                   return adConfig.ID, nil
               }
               return "", err
           }

           return config.ConfigValue, nil
       }
     ```

    关键设计点：
    - 支持三种认证模式：local/ad/hybrid
    - 从sys_config表读取默认模式和AD配置ID
    - 提供降级逻辑（配置不存在时使用第一个启用的AD配置）
    - 运行时动态选择认证器，无需重启服务
  </action>
  <verify>
    <automated>go test ./internal/core/security -run TestAuthStrategyFactory -v</automated>
  </verify>
  <done>认证策略工厂已实现，支持动态选择认证器和配置读取</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端请求 → 认证器 | 用户凭证和认证模式从前端传入 |
| 认证器 → AD域控 | LDAP连接需要验证AD服务器证书 |
| 认证器 → 数据库 | 查询sys_user表和sys_config表 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Spoofing | AD认证器 | mitigate | LDAP绑定验证确保用户身份真实性 |
| T-19-02 | Tampering | 认证模式参数 | mitigate | 验证mode参数，只允许local/ad/hybrid |
| T-19-03 | Information Disclosure | 错误信息泄露 | mitigate | 统一错误返回，避免泄露用户存在性 |
| T-19-04 | Denial of Service | LDAP连接耗尽 | mitigate | 使用连接池，设置连接超时 |
| T-19-05 | Elevation of Privilege | 混合认证降级 | accept | 降级到AD认证后仍需正确验证凭证 |
</threat_model>

<verification>
## Wave 2 验证标准

- [ ] 所有认证器接口已实现并通过编译
- [ ] 本地认证器单元测试通过（4个测试场景）
- [ ] AD认证器单元测试通过（5个测试场景）
- [ ] 混合认证器单元测试通过（3个测试场景）
- [ ] 认证策略工厂单元测试通过
- [ ] 代码审查：认证逻辑安全性检查
- [ ] 集成测试：认证器与现有登录接口集成验证
</verification>

<success_criteria>
## Wave 2 完成标准

1. **策略模式实现完整**: 5个核心文件全部实现，接口定义清晰
2. **三种认证方式可用**: 本地认证使用SM3验证，AD认证使用LDAP绑定，混合认证提供降级逻辑
3. **工厂模式可扩展**: 认证策略工厂支持动态选择认证器，易于添加新的认证方式
4. **测试覆盖充分**: 每个认证器都有对应的单元测试，测试通过率100%
5. **代码质量**: 复用现有LDAP客户端和密码管理器，代码符合项目规范
</success_criteria>

<output>
After completion, create `.planning/phases/19-ad-auth-login/19-02-SUMMARY.md`
</output>
