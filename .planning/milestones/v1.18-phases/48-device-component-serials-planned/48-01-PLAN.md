---
phase: 48-device-component-serials-planned
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/models/asset.go
  - internal/models/reconciliation.go
  - internal/core/db/migrations/migration_201_phase48_component_columns.go
  - internal/core/db/database.go
  - internal/services/operations/asset_service.go
  - internal/services/operations/asset_listfilter_test.go
autonomous: true
requirements:
  - REQ-48-01
  - REQ-48-07
user_setup: []

must_haves:
  truths:
    - "ops_asset 表存在 parent_asset_id / source_device_id / component_type / component_slot 4 列(启动迁移可验证)"
    - "sys_data_reconciliation 表存在 recon_category varchar(32) 列(默认 NULL)"
    - "原 partial unique index uniq_recon_asset_type_open 被 DROP 后,新建 uniq_recon_asset_type_cat_open(asset_id, conflict_type, recon_category) WHERE resolved_at IS NULL AND deleted_at IS NULL"
    - "字典 asset_reconciliation_recon_category 存在,dict_data 至少含 component_serial 一条"
    - "Asset GORM 模型含 ParentAssetID / SourceDeviceID / ComponentType / ComponentSlot 4 个 *string 字段"
    - "SysDataReconciliation GORM 模型含 ReconCategory *string 字段"
    - "assetService.List() 默认 WHERE component_type IS NULL,组件行不出现在常规列表"
    - "assetService.Statistics() SELECT 加 AND component_type IS NULL,组件行不计入统计"
  artifacts:
    - path: "internal/core/db/migrations/migration_201_phase48_component_columns.go"
      provides: "ops_asset 4 新列 + sys_data_reconciliation.recon_category 列 + DROP/CREATE partial unique + dict seed"
      contains: "Migrate201Phase48ComponentColumns"
    - path: "internal/models/asset.go"
      provides: "Asset struct 扩展 4 列"
      contains: "ParentAssetID"
    - path: "internal/models/reconciliation.go"
      provides: "SysDataReconciliation 加 ReconCategory 字段"
      contains: "ReconCategory"
    - path: "internal/services/operations/asset_listfilter_test.go"
      provides: "List/Statistics 排除 component_type IS NOT NULL 的单元测试"
      contains: "TestAssetListExcludesComponents"
  key_links:
    - from: "internal/core/db/database.go"
      to: "migration_201_phase48_component_columns.go"
      via: "Migrate201 调用注册"
      pattern: "Migrate201Phase48ComponentColumns"
    - from: "internal/services/operations/asset_service.go"
      to: "ops_asset.component_type"
      via: "List/Statistics WHERE 子句"
      pattern: "component_type IS NULL"
---

<objective>
Phase 48 Wave 1 — 数据模型 + Schema 迁移 + 资产列表过滤底座。仅落地 DB 列与默认过滤,不写任何运行时采集代码。Wave 2/3 依赖本 Wave 的 schema 就位。

Purpose: 落地 D-01 / D-05 / D-06 / D-07 全部 schema 与 service 层默认过滤,让后续 Wave 的采集器与对账 emitter 有地方写入,且现有资产管理 UI 不被组件行污染。
Output: 1 个 migration_NNN 文件、2 个 model 扩展、1 个 service 过滤修改、1 个新测试文件、database.go 注册新迁移。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-device-component-serials-planned/48-CONTEXT.md
@.planning/phases/48-device-component-serials-planned/48-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- 关键签名/约束,executor 无需再次探索代码库 -->

From internal/models/asset.go (现有尾部,扩展点):
```go
// 状态字段 (遵循 0=正常, 1=停用 惯例)
Status int `gorm:"default:0;column:status" json:"status"`
} // ← 在此之前插入 4 个 *string 字段
func (Asset) TableName() string { return "ops_asset" }
```

From internal/models/reconciliation.go (扩展点):
```go
type SysDataReconciliation struct {
    BaseModel
    AssetID string `gorm:"type:uuid;not null;column:asset_id;..."`
    ConflictType string `gorm:"size:2;not null;column:conflict_type;..."` // ← size:2 不可改
    // ← 在 ConflictType 后插入 ReconCategory *string
```

From internal/services/operations/asset_service.go (两个修改点):
```go
// List(): query := s.db.WithContext(ctx).Table("ops_asset") 之后
//         query = s.applyFilters(query, params) 之前/之内加 WHERE
// Statistics(): Select(...) 加 AND component_type IS NULL
func (s *assetService) List(ctx, params) (*PageResult, error)
func (s *assetService) Statistics(ctx) (*AssetStatisticsResult, error)
```

From internal/core/db/database.go:570 (注册点):
```go
if err := migrations.Migrate200FixSuggestionConfigSeeds(d.DB); err != nil { ... }
// ← 在此 if 块之后插入 Migrate201Phase48ComponentColumns 注册
```

From migration_168 (模式参考 — DO $$ + isPostgreSQL):
- partial unique index 必须用 `CREATE UNIQUE INDEX ... WHERE` 而不是 `ADD CONSTRAINT`
- DROP IF EXISTS + CREATE IF NOT EXISTS 幂等模式
- 仅 PostgreSQL 执行 schema 扩展,SQLite 跳过(`if !isPostgreSQL(db) { return nil }`)

From migration_169 (dict seed 模式参考):
- count-then-insert 幂等:先 `Count WHERE dict_type = ?`,==0 才 insert dict_type + dict_data
- models.DictType / models.DictData 结构

ops_asset 现有约束(不可改):
- devicesn VARCHAR(200) NOT NULL UNIQUE (uni_ops_asset_devicesn)
- id UUID PRIMARY KEY DEFAULT gen_random_uuid()
- deleted_at TIMESTAMP(soft delete)

sys_data_reconciliation 现有约束(不可改):
- conflict_type VARCHAR(2) NOT NULL — size:2 锁死,A-F 字典值
- partial unique uniq_recon_asset_type_open (asset_id, conflict_type) WHERE resolved_at IS NULL AND deleted_at IS NULL — 本 Wave 必须 DROP 重建
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 migration_201 + model 扩展 + database.go 注册</name>
  <files>internal/core/db/migrations/migration_201_phase48_component_columns.go, internal/core/db/database.go, internal/models/asset.go, internal/models/reconciliation.go</files>
  <read_first>
    - internal/models/asset.go (扩展点在 Status 字段前)
    - internal/models/reconciliation.go (扩展点在 ConflictType 之后)
    - internal/core/db/migrations/migration_168_reconciliation_tables.go (DO $$ + isPostgreSQL + partial unique index 模式)
    - internal/core/db/migrations/migration_169_reconciliation_dicts_configs.go (dict seed count-then-insert 模式)
    - internal/core/db/database.go (line 565-575,Migrate200 之后插入 Migrate201)
    - internal/models/base.go (BaseModel 字段集)
  </read_first>
  <behavior>
    - 启动后 migration_201 在 PostgreSQL 上 ALTER ops_asset ADD 4 列(parent_asset_id / source_device_id / component_type / component_slot,均 VARCHAR(64)/(32)/(64)/(64) DEFAULT NULL,均加普通 index)
    - 启动后 migration_201 在 PostgreSQL 上 ALTER sys_data_reconciliation ADD COLUMN recon_category VARCHAR(32) DEFAULT NULL,加 index idx_recon_category
    - 启动后 migration_201 DROP INDEX IF EXISTS uniq_recon_asset_type_open,CREATE UNIQUE INDEX IF NOT EXISTS uniq_recon_asset_type_cat_open ON sys_data_reconciliation (asset_id, conflict_type, recon_category) WHERE resolved_at IS NULL AND deleted_at IS NULL
    - 启动后 dict_type 'asset_reconciliation_recon_category' 存在;dict_data 至少有 dict_label='组件序列号' dict_value='component_serial' 一条
    - SQLite 方言下 migration_201 仅做 model AutoMigrate(4 列 + recon_category),跳过 partial unique index 重建,返回 nil
    - 二次启动幂等:重复执行不报错,无重复 dict 数据
    - Asset GORM 模型新增 ParentAssetID/SourceDeviceID/ComponentType/ComponentSlot 4 个 *string 字段,带 gorm tag(size/index/column)
    - SysDataReconciliation GORM 模型新增 ReconCategory *string 字段,gorm tag size:32;column:recon_category;index:idx_recon_category
    - go build ./... 退出码 0
  </behavior>
  <action>
创建 internal/core/db/migrations/migration_201_phase48_component_columns.go,导出函数 Migrate201Phase48ComponentColumns(db *gorm.DB) error。

实现顺序(每步独立日志,失败 return fmt.Errorf 包裹 %w):
1. isPostgreSQL(db) 判断,SQLite 分支:仅 db.AutoMigrate(&models.Asset{}, &models.SysDataReconciliation{}) 后 return nil(由 GORM tag 自动建 4+1 列;SQLite 不支持 partial unique index)。
2. PostgreSQL 分支:用 ALTER TABLE ... ADD COLUMN IF NOT EXISTS 显式加列(不用 AutoMigrate,避免触碰 deleted_at 等已存在列引发不必要的 ALTER TYPE — 参考 xingran-gorm-sql-constraint-naming-conflict 教训):
   - ops_asset: parent_asset_id VARCHAR(64), source_device_id VARCHAR(64), component_type VARCHAR(32), component_slot VARCHAR(64)
   - sys_data_reconciliation: recon_category VARCHAR(32)
   4 个新 ops_asset 列各 CREATE INDEX IF NOT EXISTS idx_ops_asset_<col> ON ops_asset(<col>) WHERE deleted_at IS NULL;recon_category 单独 CREATE INDEX IF NOT EXISTS idx_recon_category ON sys_data_reconciliation(recon_category) WHERE deleted_at IS NULL。
3. 索引切换(D-06 核心):
   - DROP INDEX IF EXISTS uniq_recon_asset_type_open;
   - 用 DO $$ BEGIN IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname='uniq_recon_asset_type_cat_open') THEN CREATE UNIQUE INDEX uniq_recon_asset_type_cat_open ON sys_data_reconciliation (asset_id, conflict_type, recon_category) WHERE resolved_at IS NULL AND deleted_at IS NULL; END IF; END$$;
4. 字典 seed(参考 migration_169 seedReconciliationDictTypes 的 count-then-insert 模式,但本任务只 seed 1 个 dict_type):
   - dict_type 'asset_reconciliation_recon_category' / dict_name '资产对账业务分类'
   - 2 条 dict_data:component_serial / '组件序列号'(默认),future_expansion / '预留扩展'(非默认)
   - 使用 models.DictType{} / models.DictData{} 结构体插入,不要裸 SQL(参考 migration_169 的写法,StatusConvention 0=启用)

修改 internal/models/asset.go:在 Status 字段声明之前插入(顺序按 CONTEXT §specifics 骨架):
  ParentAssetID  *string `gorm:"size:64;index;column:parent_asset_id" json:"parentAssetId,omitempty"`
  SourceDeviceID *string `gorm:"size:64;index;column:source_device_id" json:"sourceDeviceId,omitempty"`
  ComponentType  *string `gorm:"size:32;index;column:component_type" json:"componentType,omitempty"`
  ComponentSlot  *string `gorm:"size:64;column:component_slot" json:"componentSlot,omitempty"`

修改 internal/models/reconciliation.go:在 ConflictType 字段之后插入:
  // ReconCategory Phase 48 sibling 列(D-06):业务类型分类,与 conflict_type(A-F)正交
  ReconCategory *string `gorm:"size:32;column:recon_category;index:idx_recon_category,priority:1" json:"reconCategory,omitempty"`

修改 internal/core/db/database.go:在 Migrate200 的 if 块之后(line ~573)插入:
  if err := migrations.Migrate201Phase48ComponentColumns(d.DB); err != nil {
      applogger.Errorf("Phase 48 组件序列号 schema 迁移失败: %v", err)
  }
注意:不要用 panic/fatal,沿用相邻迁移的 log 错误模式。
  </action>
  <verify>
    <automated>cd D:/code/ClaudeCode/xingran-go-backend && go build ./... && go vet ./internal/core/db/migrations/... ./internal/models/... ./internal/services/operations/...</automated>
  </verify>
  <done>
    migration_201 文件存在且 Migrate201Phase48ComponentColumns 函数导出;database.go 含 Migrate201Phase48ComponentColumns 调用注册;Asset 模型含 4 个新字段;SysDataReconciliation 模型含 ReconCategory;go build ./... 退出码 0;二次执行迁移幂等(通过 IF NOT EXISTS / DO $$ 块保证)。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: asset_service List/Statistics 加 component_type IS NULL 默认过滤 + 单元测试</name>
  <files>internal/services/operations/asset_service.go, internal/services/operations/asset_listfilter_test.go</files>
  <read_first>
    - internal/services/operations/asset_service.go (List 在 line 179-213;applyFilters 在 221-268;Statistics 在 60-75)
    - internal/services/operations/asset_statistics_test.go (现有 Statistics 测试模式参考)
    - internal/services/operations/building_service_test.go (sqlite in-memory test setup 模式)
    - internal/models/asset.go (Task 1 完成后的 4 新列)
  </read_first>
  <behavior>
    - List():查询自动加 WHERE component_type IS NULL(组件行被排除),即便 params 不传任何筛选
    - Statistics():COUNT/SUM 自动加 AND component_type IS NULL,total/normal/stopped/nbf 都不含组件行
    - 用户传入 params["includeComponents"]=true(测试用)可关闭该过滤,但生产路径默认开启(本任务可不实现 includeComponents flag,只要测试能注入 component_type IS NOT NULL 行验证被排除即可)
    - TestAssetListExcludesComponents:插入 1 行 component_type IS NULL + 1 行 component_type='card';调 List(ctx, map{});断言 list 长度=1 且 list[0].ComponentType 为 nil
    - TestAssetStatisticsExcludesComponents:同上数据;调 Statistics;断言 Total==1
    - TestAssetListFilterDoesNotBreakExistingFilters:验证 component_type IS NULL 与 devicesn LIKE 等现有筛选并存
  </behavior>
  <action>
先写测试 internal/services/operations/asset_listfilter_test.go(RED):
  - 用 sqlite in-memory `gorm.Open(sqlite.Open(":memory:"))` + db.AutoMigrate(&models.Asset{}) 建表(参考 building_service_test.go 的 setupAssetDB 风格)
  - 3 个测试函数:TestAssetListExcludesComponents / TestAssetStatisticsExcludesComponents / TestAssetListFilterDoesNotBreakExistingFilters
  - 每个 setup 插入 1 主设备(component_type=NULL) + 1 组件(component_type='card', devicesn='CARD-SN-1')
  - List 测试断言返回 list 仅含主设备;Statistics 断言 Total==1;组合筛选测试断言两个条件 AND 起作用

再改 internal/services/operations/asset_service.go(GREEN):
  - List() 在 `query := s.db.WithContext(ctx).Table("ops_asset")` 之后立即 `query = query.Where("component_type IS NULL")` (放在 applyFilters 之前,作为不可关闭的硬性默认 — D-07 要求)
  - Statistics() 在 Select 之前的 Model(&models.Asset{}) 之后 `.Where("component_type IS NULL")`
  - 不动 applyFilters 的其他分支,不动 GetByDeviceSN(对账要用它查所有 SN,包括组件)、Create、Update
  </action>
  <verify>
    <automated>cd D:/code/ClaudeCode/xingran-go-backend && go test ./internal/services/operations/ -run "TestAssetListExcludesComponents|TestAssetStatisticsExcludesComponents|TestAssetListFilterDoesNotBreakExistingFilters" -v</automated>
  </verify>
  <done>
    asset_service.go List/Statistics 默认 WHERE component_type IS NULL;3 个新测试全 PASS;现有 operations 包测试不回归(go test ./internal/services/operations/ 全绿)。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron→DB | 内部 cron 写入 ops_asset 新列,无外部输入跨越 |
| user→DB | Excel 导入 ops_asset 仍走 GetByDeviceSN + Create,不触碰 4 新列(留 NULL) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Tampering | migration_201 DROP/CREATE partial unique | mitigate | 用 IF EXISTS / IF NOT EXISTS + DO $$ 块保证幂等;不破坏现有 124 条 D 异常(它们 conflict_type 不变,recon_category=NULL 仍满足新约束) |
| T-48-02 | DoS | dict seed 重复插入导致主键冲突 | mitigate | count-then-insert 模式,迁移二次运行 0 行写入 |
| T-48-03 | Information Disclosure | component_type IS NULL 过滤意外隐藏合法资产 | accept | 主设备 Excel 导入永远写 NULL,D-07 锁定;若手动 SQL 注入 component_type='card' 到主设备属操作错误,非系统责任 |
| T-48-SC | Tampering | 无新 npm/pip/cargo 安装 | N/A | 本 plan 无包安装 |
</threat_model>

<verification>
- `go build ./...` 退出码 0
- `go test ./internal/services/operations/ -v` 全绿(含 3 个新测试)
- `go vet ./internal/core/db/migrations/... ./internal/models/...` 无 warning
- 启动后端服务,日志含 "Migration 201:" 字样且无 ERROR(用户手动验证)
- psql 查询 `\d ops_asset` 含 4 新列;`\d sys_data_reconciliation` 含 recon_category(用户手动验证)
- psql 查询 `SELECT indexname FROM pg_indexes WHERE tablename='sys_data_reconciliation'` 含 uniq_recon_asset_type_cat_open,不含 uniq_recon_asset_type_open(用户手动验证)
</verification>

<success_criteria>
- Wave 1 schema 全部就位(4+1 列 + 1 个新 partial unique index + 1 个新 dict)
- 资产列表/统计默认排除组件行,现有 UI 行为不变
- 不引入任何运行时采集逻辑,Wave 2 依赖的全部 DB schema 已就位
- 二次启动迁移幂等
</success_criteria>

<output>
Create `.planning/phases/48-device-component-serials-planned/48-01-SUMMARY.md` when done
</output>
