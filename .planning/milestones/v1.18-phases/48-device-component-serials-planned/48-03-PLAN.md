---
phase: 48-device-component-serials-planned
plan: 03
type: execute
wave: 3
depends_on:
  - 48-01
  - 48-02
files_modified:
  - internal/services/component_collector/ops_asset_writer.go
  - internal/services/component_collector/ops_asset_writer_test.go
  - internal/services/component_collector/reconciliation_emitter.go
  - internal/services/component_collector/reconciliation_emitter_test.go
  - internal/services/component_collector/pipeline.go
  - internal/services/component_collector/pipeline_test.go
  - internal/services/device_info_collection_service.go
  - internal/api/v1/operations/asset_component_handler.go
  - internal/api/router.go
  - internal/utils/operlog/operlog.go
  - xingran-react-frontend/src/lib/opsApi.ts
  - xingran-react-frontend/src/pages/operations/assets/components/ComponentListTab.tsx
  - xingran-react-frontend/src/pages/operations/assets/index.tsx
autonomous: false
requirements:
  - REQ-48-05
  - REQ-48-06
  - REQ-48-08
  - REQ-48-10
  - REQ-48-11
user_setup: []

must_haves:
  truths:
    - "采集到的 ComponentSet 经 OpsAssetWriter 后,匹配上的组件 SN UPDATE ops_asset 的 parent_asset_id / source_device_id / component_type / component_slot 4 列"
    - "采集到的组件 SN 在 ops_asset 中无匹配行 → 写入 sys_data_reconciliation 一条 open 异常,conflict_type=F, recon_category=component_serial"
    - "父交换机不在 ops_asset 时,组件 parent_asset_id 留 NULL(D-04),source_device_id 仍写,不产生交换机侧异常"
    - "每个组件 UPDATE 后调用 operlog.RecordBackground(..., '资产管理', OperTypeSync, 'system-cron') 记录(D-13)"
    - "组件采集 collectComponentInfo 失败时仅 applogger.Warnf,不调 operlog(D-13 范围仅限 UPDATE 路径;applogger.Warnf 是故障信令),不阻塞 chassis 更新(D-14)"
    - "collectComponentInfo 内部按设备 Vendor 走两步流水线(D-10):先 ParseInterfaceStatus/ParseInterfacesStatus 拿 up 接口列表 → 再 ParseInterfaceTransceiver/ParseTransceiverDDM 仅采 up 接口光模块"
    - "DeviceInfoCollectionService.processTask 在 updateDeviceInfo 之后调用 component pipeline(Wave 3 接入)"
    - "新增 API GET /ops/asset/components?parentAssetId=:id 在 internal/api/router.go:691-697 asset 组内内联注册(符合现有 `assets := ops.Group(\"/asset\")` 模式,无 SetupAssetRouter 函数),复用 679-685 行 ops:asset:list 组级权限"
    - "前端资产详情页新增「从属组件清单」Tab,展示组件 ComponentType/Slot/DeviceSN/Model/Source 字段表格"
    - "组件异常列表可在现有对账看板用 recon_category=component_serial 单独筛选"
  artifacts:
    - path: "internal/services/component_collector/ops_asset_writer.go"
      provides: "OpsAssetWriter UPDATE-only 实现(D-02/D-03/D-04/D-13)"
      contains: "type OpsAssetWriter"
    - path: "internal/services/component_collector/reconciliation_emitter.go"
      provides: "组件异常 sys_data_reconciliation INSERT(D-06)"
      contains: "type ReconciliationEmitter"
    - path: "internal/services/component_collector/pipeline.go"
      provides: "Pipeline.Run 编排 Collect→Write→EmitAnomaly 主流程"
      contains: "type Pipeline"
    - path: "internal/services/device_info_collection_service.go"
      provides: "processTask 内 CollectComponentInfo hook(D-14),含 D-10 两步流水线编排"
      contains: "CollectComponentInfo"
    - path: "internal/api/v1/operations/asset_component_handler.go"
      provides: "GET /ops/asset/components 端点"
      contains: "ListComponentComponents"
    - path: "xingran-react-frontend/src/pages/operations/assets/components/ComponentListTab.tsx"
      provides: "前端从属组件清单 Tab"
      contains: "ComponentListTab"
  key_links:
    - from: "internal/services/component_collector/pipeline.go"
      to: "internal/services/operations/asset_service.go"
      via: "AssetService.GetByDeviceSN 调用"
      pattern: "GetByDeviceSN"
    - from: "internal/services/component_collector/ops_asset_writer.go"
      to: "ops_asset"
      via: "UPDATE parent_asset_id/source_device_id/component_type/component_slot WHERE id"
      pattern: "Updates\\(map\\[string\\]interface"
    - from: "internal/services/component_collector/reconciliation_emitter.go"
      to: "sys_data_reconciliation"
      via: "INSERT conflict_type=F + recon_category=component_serial"
      pattern: "ConflictType.*F"
    - from: "internal/services/component_collector/ops_asset_writer.go"
      to: "internal/utils/operlog"
      via: "operlog.RecordBackground 调用(无 gin.Context 路径)"
      pattern: "operlog\\.RecordBackground\\(.*OperTypeSync"
    - from: "internal/services/device_info_collection_service.go"
      to: "internal/services/component_collector/pipeline.go"
      via: "processTask 内 Pipeline.Run(ctx, device, info) 调用"
      pattern: "Pipeline\\.Run"
    - from: "internal/api/router.go"
      to: "internal/api/v1/operations/asset_component_handler.go"
      via: "router.go:691-697 asset 组内内联注册 assets.GET(\"/components\", assetComponentHandler.ListComponents)"
      pattern: "assets\\.GET\\(\"/components\""
---

<objective>
Phase 48 Wave 3 — Pipeline 接入 + 对账异常 + operlog + 前端展示。把 Wave 2 的 ComponentSet 接到现有 DeviceInfoCollectionService cron + ops_asset UPDATE-only 写入路径 + sys_data_reconciliation 异常 emitter + operlog + 前端组件清单 Tab。

Purpose: 落地 D-02/D-03/D-04(写入策略)/D-06(对账异常)/D-07(前端展示)/D-10(up 接口流水线编排)/D-12(stack 接入)/D-13(operlog,UPDATE 路径专用)/D-14(cron 接入,失败仅 log 不阻塞)。Wave 1 schema + Wave 2 collector 在此被组装为端到端流水线。
Output: 3 个新 service 文件(ops_asset_writer / reconciliation_emitter / pipeline)+ device_info_collection_service.go hook + 1 个新 handler/router(确定性挂载)+ 3 个前端文件 + operlog helper 扩展。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/48-device-component-serials-planned/48-CONTEXT.md
@.planning/phases/48-device-component-serials-planned/48-RESEARCH.md
@.planning/phases/48-device-component-serials-planned/48-VALIDATION.md
@.planning/phases/48-device-component-serials-planned/48-01-SUMMARY.md
@.planning/phases/48-device-component-serials-planned/48-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- AssetService lookup (existing, 48-01 已确认 unchanged) -->
From internal/services/operations/asset_service.go:
```go
// 返回 (nil, nil) on not-found; (nil, err) on DB error; (*Asset, nil) on hit
func (s *assetService) GetByDeviceSN(ctx context.Context, deviceSN string) (*models.Asset, error)
// List 默认 WHERE component_type IS NULL(48-01 后)
// 新增需求:ListComponentComponents 需要绕过默认过滤
```

<!-- NetworkDevice model (for source_device_id reference) -->
From internal/models/network_device.go:
```go
type NetworkDevice struct { ...; ID string; DeviceName string; Vendor DeviceVendor; SerialNumber string; ... }
```

<!-- operlog Record signature (existing) -->
From internal/utils/operlog/operlog.go:
```go
func Record(c *gin.Context, operLogSvc Recorder, db *gorm.DB, module string, operType int, opts ...RecordOption)
// 在 success path 末尾、response.Success 之前调用
// 但 cron 路径无 *gin.Context! → 需用 operlog.WithOperParam(...) opts 构造,或新增 RecordBackground helper(本 plan 选第二种)
```

<!-- DeviceInfoCollectionService hook point (existing, line 211-249) -->
From internal/services/device_info_collection_service.go:
```go
func (s *DeviceInfoCollectionService) processTask(ctx context.Context, deviceID string) {
    // ... CollectDeviceInfo + updateDeviceInfo + markTaskSuccess ...
    s.updateDeviceInfo(&device, info)
    // ⮕ Phase 48 hook:在此处插入 s.collectComponentInfo(ctx, &device, info)
    s.markTaskSuccess(&task, info)
}
```

<!-- SysDataReconciliation INSERT shape (existing model) -->
From internal/models/reconciliation.go:
```go
// 必填:AssetID, ConflictType, Severity, RawSnapshot, DetectedAt
// 可选:AssetIP, ExceptionRuleID, AppliedActions, ReconCategory(48-01 新增)
// partial unique index uniq_recon_asset_type_cat_open 保证 (asset_id, conflict_type, recon_category) WHERE open 唯一
// → 重复 emit 不会产生重复行,只会保留首条
```

<!-- Asset model 4 new cols (48-01 added) -->
From internal/models/asset.go:
```go
ParentAssetID  *string `gorm:"size:64;index;column:parent_asset_id"`
SourceDeviceID *string `gorm:"size:64;index;column:source_device_id"`
ComponentType  *string `gorm:"size:32;index;column:component_type"`
ComponentSlot  *string `gorm:"size:64;column:component_slot"`
```

<!-- CONSTRAINT: cron 路径无 gin.Context, operlog 调用方式不同 -->
per RESEARCH §Code Examples #4 (handler reference) + Pitfall 4 (asset lookup nil nil):
- cron worker 无 *gin.Context,operlog.Record 第一个参数为 nil 时需走 WithOperParam 显式注入
- 或新增 operlog.RecordBackground(operLogSvc, db, module, operType, operatorName, params...) helper
- 选第二种(本 plan 任务里加 helper)

<!-- CONSTRAINT: D-04 父交换机不在 ops_asset 时不报异常 -->
组件侧正常 UPDATE/emit;父交换机 ops_asset 行缺失是另一层问题,不混入组件对账。

<!-- CONSTRAINT: pipeline 失败不阻塞 chassis 更新 + operlog 不对称(WARNING 6) -->
processTask 内 collectComponentInfo 失败时只 applogger.Warnf,不调 operlog。D-13 范围明确仅限 UPDATE 路径(success path 末尾);故障信令用 applogger.Warnf,不引入对称 operlog 失败记录(避免审计噪声 + operlog 表语义混淆)。在代码注释中明示此不对称:"// 故障不记 operlog: D-13 范围仅限 UPDATE 路径,失败用 applogger.Warnf 信令"。

<!-- CONSTRAINT: D-10 两步流水线在 collectComponentInfo 内编排 -->
collectComponentInfo 必须按 device.Vendor 编排两步:
- 华为:先 SendCommand("display interface status") → collector.ParseInterfaceStatus 拿 up 接口 → 再 SendCommand("display interface transceiver") → collector.ParseInterfaceTransceiver(raw, upInterfaces)
- 锐捷:先 SendCommand("show interfaces status") → collector.ParseInterfacesStatus 拿 up 接口 → 再 SendCommand("show interface transceiver") → collector.ParseTransceiverDDM(raw, upInterfaces)
不直接调单步 transceiver 解析(否则违反 D-10)。

<!-- CONSTRAINT: 路由挂载点(基于实际代码现状,非编造) -->
asset 端点当前在 internal/api/router.go:679-704 内联挂载(`assets := ops.Group("/asset")` 模式),**不存在 SetupAssetRouter 函数**(整个 internal/api/ grep 0 命中)。本任务采用与现有代码一致的内联挂载:在 router.go:691(search-by-serial)之后、697(`:id` 通配)之前注册 `assets.GET("/components", ...)`,避免 `:id` 吞噬。files_modified 含 router.go。不新建 asset_router.go。

<!-- Frontend API client (existing) -->
From xingran-react-frontend/src/lib/opsApi.ts:
```typescript
// 现有 assetApi 风格,新增 componentApi
export const componentApi = {
  list: (parentAssetId: string) => get('/ops/asset/components', { parentAssetId })
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OpsAssetWriter (UPDATE-only + parent 缺失降级) + ReconciliationEmitter + Pipeline + operlog.RecordBackground + 单元测试</name>
  <files>internal/services/component_collector/ops_asset_writer.go, internal/services/component_collector/ops_asset_writer_test.go, internal/services/component_collector/reconciliation_emitter.go, internal/services/component_collector/reconciliation_emitter_test.go, internal/services/component_collector/pipeline.go, internal/services/component_collector/pipeline_test.go, internal/utils/operlog/operlog.go</files>
  <read_first>
    - internal/services/operations/asset_service.go (GetByDeviceSN 返回 nil,nil 模式,Pitfall 4)
    - internal/models/asset.go (48-01 加的 4 列)
    - internal/models/reconciliation.go (SysDataReconciliation 必填字段 + 48-01 加的 ReconCategory)
    - internal/utils/operlog/operlog.go (Record 签名 + RecordOptions 模式 + regression_test.go 锁定的 API)
    - internal/utils/operlog/regression_test.go(确保新增 RecordBackground 不破坏锁定的 25 常量 + 11 关键词)
    - .planning/phases/48-device-component-serials-planned/48-02-SUMMARY.md (ComponentSet / Component 数据结构)
    - .planning/phases/48-device-component-serials-planned/48-RESEARCH.md §Code Examples #2/#3/#4 (asset lookup / operlog / 单元测试断言)
    - .planning/phases/48-device-component-serials-planned/48-RESEARCH.md §Common Pitfalls #4 (asset nil,nil 处理)
  </read_first>
  <behavior>
    - OpsAssetWriter.Write(ctx, db, assetSvc, deviceID, parentAssetID, components []Component, operLogSvc) WriteResult:
      对每个 component.SerialNumber:
      - 调 assetSvc.GetByDeviceSN(ctx, sn)
      - 命中(*Asset, nil):UPDATE ops_asset SET parent_asset_id=?, source_device_id=?, component_type=?, component_slot=? WHERE id=?;hitCount++
      - 未命中(nil, nil):emit anomaly,missCount++
      - DB 错误(nil, err):return err(整个 pipeline 停)
    - 父交换机 parentAssetID == ""(D-04 场景)时:组件 UPDATE 时 parent_asset_id 字段写 NULL(其他 3 列仍写),不 emit 异常
    - 每个 hit UPDATE 后调 operlog.RecordBackground(operLogSvc, db, "资产管理", OperTypeSync, "system-cron", map[string]interface{}{...})
    - ReconciliationEmitter.Emit(ctx, db, assetID, conflictType, reconCategory, rawSnapshot) 仅 INSERT sys_data_reconciliation 行;partial unique 索引保证重复 emit 幂等(只保留首条),INSERT ... ON CONFLICT DO NOTHING 兜底
    - Pipeline.Run(ctx, db, device *NetworkDevice, set *ComponentSet, assetSvc, operLogSvc) error:
      1. 查父交换机 ops_asset:assetSvc.GetByDeviceSN(ctx, device.SerialNumber)
      2. parentAssetID = parent.ID 若命中 else ""(D-04)
      3. 调 OpsAssetWriter.Write(全部组件)
      4. 不返回错误除非 DB 错误
    - operlog.go 新增 RecordBackground(operLogSvc Recorder, db *gorm.DB, module string, operType int, operatorName string, params map[string]interface{}) — 与 Record 同逻辑但 operatorName 显式注入而非从 gin.Context 提取
    - TestOpsAssetWriterHitUpdatesFourColumns:asset 行存在 → UPDATE 后 4 列非空,hitCount=1
    - TestOpsAssetWriterMissEmitsAnomaly:asset 不存在 → sys_data_reconciliation 多 1 行,conflict_type=F, recon_category=component_serial, missCount=1
    - TestOpsAssetWriterParentMissingDoesNotEmitSwitchAnomaly:parentAssetID="" 时仍 UPDATE 组件,parent_asset_id 列=NULL,无交换机侧异常
    - TestOpsAssetWriterRecordsOperlog:每次 hit UPDATE 后 operlog spy 收到 1 次调用,module="资产管理", operType=14
    - TestReconciliationEmitterIdempotent:同 assetID+conflict_type+recon_category 二次 emit 不报错且表仅 1 行
    - TestPipelineEndToEnd:用 sqlite 内存 DB,mock asset 行,mock ComponentSet,跑完 Pipeline 后断言 ops_asset 4 列被写 + sys_data_reconciliation 有正确条数
    - operlog regression_test.go 不回归(25 常量 + 11 关键词 + Record 5参签名不变)
  </behavior>
  <action>
A. 先扩 internal/utils/operlog/operlog.go(轻微改动,新增 helper):
  - 在已有 Record 函数之后新增 RecordBackground(operLogSvc Recorder, db *gorm.DB, module string, operType int, operatorName string, params map[string]interface{}):
    - 内部构造 operLogSvc.RecordAsync 调用所需参数:method="BACKGROUND", requestMethod="CRON", operUrl="", operIP=nil, operParam=json.Marshal(params) 若非 nil, jsonResult=nil, errorMsg=nil, status=0(成功), costTime=0
    - operatorName / operatorNickname 都设为 operatorName(如 "system-cron")
    - deptName=nil
    - panic-safety defer 包裹(沿用 Record 现有模式)
  - 不修改现有 Record / RecordWithBody 签名(回归保护 regression_test.go 锁定的 25 常量 + 11 关键词 + Record 5参签名不受影响)

B. 创建 internal/services/component_collector/ops_asset_writer.go:
  - type OpsAssetWriter struct { db *gorm.DB; assetSvc operations.AssetService; operLogSvc operlog.Recorder }
  - func NewOpsAssetWriter(db, assetSvc, operLogSvc) *OpsAssetWriter
  - type WriteResult struct { HitCount, MissCount int; EmittedAnomalies []string }
  - func (w *OpsAssetWriter) Write(ctx, deviceID string, parentAssetID string, components []component_collector.Component) (WriteResult, error)
  - 实现按 behavior 描述,注意:
    - GetByDeviceSN 返回 (nil, nil) 是正常 not-found,不要 return err
    - UPDATE 用 db.Model(&models.Asset{}).Where("id = ?", asset.ID).Updates(map[string]interface{}{...}) 显式 map 写法,确保 4 个 *string 字段都能写 NULL(用 *string 等价值)
    - parent_asset_id 列:若 parentAssetID=="" 则 nil,否则 &parentAssetID
    - operLogSvc 可能为 nil(test spy),写法上做 nil-check 跳过

C. 创建 internal/services/component_collector/reconciliation_emitter.go:
  - type ReconciliationEmitter struct { db *gorm.DB }
  - func (e *ReconciliationEmitter) Emit(ctx, assetID, conflictType, reconCategory string, rawSnapshot json.RawMessage) error
  - assetID 必须是合法 UUID;组件侧异常 assetID 用组件自身 ops_asset.id(若组件本身没 ops_asset 行 — 这种情况其实是 miss 不应 emit;实际 emit 场景是"父交换机 ops_asset 存在但子组件 ops_asset 缺失" → assetID 取父交换机的 id 让对账定位到交换机)
  - INSERT 用 raw GORM:db.Create(&models.SysDataReconciliation{...});若 error 是 PG unique violation(SQLSTATE 23505)则 log 跳过(partial unique 兜底)
  - severity:固定 "medium"(per RESEARCH Open Question 2 推荐)
  - DetectedAt: time.Now()

D. 创建 internal/services/component_collector/pipeline.go:
  - type Pipeline struct { db *gorm.DB; assetSvc operations.AssetService; collector component_collector.EntityCollector; ... }
  - func (p *Pipeline) Run(ctx, device *models.NetworkDevice, set *component_collector.ComponentSet) error:
    1. parentAssetID := ""
    2. if device.SerialNumber != "" { parent, err := p.assetSvc.GetByDeviceSN(ctx, device.SerialNumber); if err != nil { return err }; if parent != nil { parentAssetID = parent.ID } }
    3. writer := NewOpsAssetWriter(p.db, p.assetSvc, p.operLogSvc)
    4. _, err := writer.Write(ctx, device.ID, parentAssetID, set.Components)
    5. return err
  - chassis 行(ComponentType=chassis)自身不入 components 列表写入(它是父,不是子),由 device.SerialNumber 路径处理

E. 创建 3 个 _test.go 文件覆盖 behavior(RED first — Wave 0 即 RED-first TDD 流程,不单独建 48-00-PLAN.md):
  - 用 sqlite in-memory + gorm.Open,AutoMigrate ops_asset / sys_data_reconciliation
  - operlog Recorder 用 spy struct(实现 RecordAsync 方法记录调用次数)
  - 写完测试再确认实现(GREEN)
  - 单独跑 go test ./internal/utils/operlog/ -v 确保 regression_test.go 仍 PASS

注意:
  - 不要直接 import operations 包(component_collector 应只依赖 operations.AssetService 接口,通过参数注入避免循环依赖)
  - parentAssetID 类型用 string 而非 *string(空串=不存在,UUID=存在)
  - 组件 UPDATE 用 .Updates(map) 而非 .Save(asset)(避免覆盖其他字段)
  </action>
  <verify>
    <automated>cd D:/code/ClaudeCode/xingran-go-backend && go test ./internal/services/component_collector/ -v -run "TestOpsAssetWriter|TestReconciliationEmitter|TestPipeline" && go test ./internal/utils/operlog/ -v</automated>
  </verify>
  <done>
    OpsAssetWriter 实现 UPDATE-only + D-04 父缺失降级 + operlog.RecordBackground 调用;ReconciliationEmitter 幂等 emit + conflict_type=F + recon_category=component_serial;Pipeline.Run 端到端跑通;operlog.go 新增 RecordBackground helper 不破坏现有 Record + regression_test.go 不回归;6 个新测试全 PASS;go build ./... 退出码 0。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DeviceInfoCollectionService cron hook(D-10 两步流水线编排 + D-14 失败仅 warn)+ 后端 list 端点(确定性挂载)+ 前端组件清单 Tab</name>
  <files>internal/services/device_info_collection_service.go, internal/api/v1/operations/asset_component_handler.go, internal/api/router.go, internal/utils/operlog/operlog.go, xingran-react-frontend/src/lib/opsApi.ts, xingran-react-frontend/src/pages/operations/assets/components/ComponentListTab.tsx, xingran-react-frontend/src/pages/operations/assets/index.tsx</files>
  <read_first>
    - internal/services/device_info_collection_service.go (line 211-249 processTask hook point + line 252-300 CollectDeviceInfo + 290-300 getCommandsByVendor)
    - internal/api/v1/operations/asset_handler.go (现有 handler 结构 + WithCore 模式)
    - internal/api/router.go (现有 679-704 行 asset 组内联挂载风格 `assets := ops.Group("/asset")` — 本任务在此处加一行 GET /components,不新建 asset_router.go)
    - xingran-react-frontend/src/lib/opsApi.ts (assetApi 现有结构)
    - xingran-react-frontend/src/pages/operations/assets/index.tsx (现有资产列表页结构,找 Tab 切换或 Drawer 嵌入位置)
    - xingran-react-frontend/src/pages/operations/assets/columnsSchema.ts (column schema 风格参考)
    - xingran-react-frontend/src/types/operations.ts (现有 Asset TypeScript 类型)
    - .planning/phases/48-device-component-serials-planned/48-02-SUMMARY.md (HuaweiCliCollector / RuijieCliCollector Parse* 方法签名 — D-10 两步流水线编排依据)
  </read_first>
  <behavior>
    - DeviceInfoCollectionService.processTask 在 s.updateDeviceInfo(&device, info) 之后调用 s.collectComponentInfo(ctx, &device, info)
    - collectComponentInfo 失败只 applogger.Warnf,**不调 operlog(D-13 范围仅限 UPDATE 路径;applogger.Warnf 是故障信令)**,不影响 markTaskSuccess(D-14)
    - collectComponentInfo 内部按 device.Vendor 走 D-10 两步流水线:
      1. 实例化 EntityCollector(SNMP)走 ENTITY-MIB 主路径(板卡/电源/风扇/引擎/chassis)
      2. CLI 补充路径(按 device.Vendor):
         - 华为:SendCommand("display interface status") → HuaweiCliCollector.ParseInterfaceStatus 拿 up 接口列表 → SendCommand("display interface transceiver") → HuaweiCliCollector.ParseInterfaceTransceiver(raw, upInterfaces)
         - 锐捷:SendCommand("show interfaces status") → RuijieCliCollector.ParseInterfacesStatus 拿 up 接口列表 → SendCommand("show interface transceiver") → RuijieCliCollector.ParseTransceiverDDM(raw, upInterfaces)
      3. 合并 ComponentSet,调 Pipeline.Run(ctx, &device, &set)
      4. 失败 log 后 return nil
    - 后端 GET /ops/asset/components?parentAssetId=<uuid> **在 internal/api/router.go:691-697 asset 组内内联注册**(符合现有代码模式,无 SetupAssetRouter 函数)
    - 响应格式 {code:0, data: {list: Asset[], total: N}}
    - 复用 ops:asset:list 权限(per MEMORY xingran-perm-namespace-split-readonly-page 教训)
    - 前端 opsApi.ts 新增 componentApi.list(parentAssetId)
    - 前端 ComponentListTab.tsx:Antd Table,列:组件类型(componentType Tag)/ 槽位(componentSlot)/ 序列号(devicesn)/ 型号(deviceModelName)/ 来源(sourceDeviceId 显示)
    - 资产详情页(或资产列表行 expandable)新增「从属组件」Tab/Drawer,渲染 ComponentListTab
    - 现有资产列表(主设备)行为不变(component_type IS NULL 过滤仍生效)
    - TestCollectComponentInfoHuaweiTwoStepPipeline:mock device.Vendor=Huawei,断言 collectComponentInfo 先发 status 命令再发 transceiver 命令(命令序列 spy 验证)
    - TestCollectComponentInfoRuijieTwoStepPipeline:同上,锐捷
    - TestCollectComponentInfoFailureDoesNotBlock:collectComponentInfo 返回 err,断言 processTask 仍 markTaskSuccess(D-14)
    - TestListComponents:GET /ops/asset/components?parentAssetId=<uuid> 返回该 parent 下所有 component_type IS NOT NULL 行
  </behavior>
  <action>
A. 改 internal/services/device_info_collection_service.go:
  - 在 processTask 函数体内,s.updateDeviceInfo(&device, info) 之后、s.markTaskSuccess 之前,插入:
    if err := s.collectComponentInfo(ctx, &device, info); err != nil {
        // 故障不记 operlog: D-13 范围仅限 UPDATE 路径,失败用 applogger.Warnf 信令
        // (不引入对称 operlog 失败记录,避免审计噪声 + operlog 表语义混淆)
        applogger.Warnf("组件采集失败 [设备=%s]: %v", device.DeviceName, err)
        // 不 return,不 markTaskFailed(D-14)
    }
  - 新增方法 func (s *DeviceInfoCollectionService) collectComponentInfo(ctx context.Context, device *models.NetworkDevice, info *DeviceInfo) error:
    - 内部组装 SNMP client(用 device.IP / 凭据 SNMPCommunities[0])
    - 实例化 EntityCollector / Pipeline(需要 db + assetSvc + operLogSvc,这些 s 已有引用或新增字段)
    - **D-10 两步流水线编排**:
      - 实体采集(SNMP 主路径):EntityCollector.Collect → ComponentSet set1
      - CLI 补充(按 device.Vendor):
        - Vendor==Huawei: SendCommand("display interface status") → HuaweiCliCollector.ParseInterfaceStatus(raw) → upInterfaces; SendCommand("display interface transceiver") → HuaweiCliCollector.ParseInterfaceTransceiver(raw, upInterfaces) → set2
        - Vendor==Ruijie: SendCommand("show interfaces status") → RuijieCliCollector.ParseInterfacesStatus(raw) → upInterfaces; SendCommand("show interface transceiver") → RuijieCliCollector.ParseTransceiverDDM(raw, upInterfaces) → set2
      - 合并:set1.Components = append(set1.Components, set2...)
      - 调 Pipeline.Run(ctx, device, &set1)
    - 注意:DeviceInfoCollectionService 当前可能未持有 operations.AssetService / operlog.Recorder 实例,需要扩展 struct 字段 + 构造函数参数 — 检查 NewDeviceInfoCollectionService 签名,添加新依赖
    - 凭据获取:device.CredentialID → 查 sys_auth_credential → 取 SNMPCommunities[0](参考 device_info_collection_service.go 现有凭据获取模式)
    - SNMP 不通时 return err(log warn 即可,不阻塞主流程)
    - SSH 命令发送复用 device.ExecuteOnDevice / connection_pool 现有 API(参考 device_info_collection_service.go 现有调用)

B. 创建 internal/api/v1/operations/asset_component_handler.go:
  - type AssetComponentHandler struct { assetSvc operations.AssetService; core *core.Core }
  - func (h *AssetComponentHandler) ListComponents(c *gin.Context):
    - parentAssetId := c.Query("parentAssetId")
    - 校验 UUID 格式(用 constants.UuidPattern)
    - 直接查 db.Table("ops_asset").Where("parent_asset_id = ? AND deleted_at IS NULL", parentAssetId).Order("component_type, component_slot").Find(&list)
    - 注意:绕过 assetSvc.List 的 component_type IS NULL 默认过滤(48-01 加的),用 raw query
    - response.Success(c, map[string]interface{}{"list": list, "total": len(list)})

C. 改 internal/api/router.go(asset 端点当前在此文件 679-704 行内联挂载,**无 SetupAssetRouter 函数**,采用内联挂载符合现有模式):
  - 在 router.go:697 `assets.POST("/:id", assetHandler.GetByID)` 附近(assetHandler 实例化之后),新增 handler 实例化 + 路由注册:
    ```go
    assetComponentHandler := operations.NewAssetComponentHandler(assetService).WithCore(core)
    assets.GET("/components", assetComponentHandler.ListComponents)
    ```
  - **必须放在 `assets.GET("/search-by-serial/:serial", ...)`(691 行)之后、`assets.POST("/:id", ...)`(697 行)之前**,避免 `:id` 通配吞噬 `/components`(参考 690 行注释"更具体的路由要放在前面")
  - 权限:复用现有 679-685 行 `assets.Use(middleware.RequirePermissions([...ops:asset:list...]))` 组级 middleware,无需额外加 perm
  - **internal/api/router.go 必须列入 files_modified**(本方案就是要改它)
  - **不新建 asset_router.go 文件**(现有代码无此抽取,保持一致)

D. 改 xingran-react-frontend/src/lib/opsApi.ts:
  - 在现有 assetApi 之后新增:
    export const componentApi = {
      list: (parentAssetId: string) => get('/ops/asset/components', { parentAssetId }),
    };

E. 创建 xingran-react-frontend/src/pages/operations/assets/components/ComponentListTab.tsx:
  - props: { parentAssetId: string }
  - useEffect + componentApi.list(parentAssetId) 加载(注意 useMemo 稳定化参数防无限请求循环 — per CLAUDE.md useEffect 警告)
  - Antd Table:columns = [{title:'类型', dataIndex:'componentType', render: t => <Tag>{COMPONENT_TYPE_LABELS[t]||t}</Tag>}, {title:'槽位', dataIndex:'componentSlot'}, {title:'序列号', dataIndex:'devicesn'}, {title:'型号', dataIndex:'deviceModelName'}, {title:'采集来源设备ID', dataIndex:'sourceDeviceId', render: truncateUuid}]
  - 空数据展示 "无可查询的从属组件"
  - loading state 用 Antd Table 自带 spin

F. 改 xingran-react-frontend/src/pages/operations/assets/index.tsx:
  - 在现有列表行操作列加「查看组件」按钮,或行 expandable.expandedRowRender 渲染 <ComponentListTab parentAssetId={record.id} />
  - 选 expandable(更符合"从属组件清单"语义,无需新 Modal/Drawer)
  - 仅对主设备行(componentType undefined/null)显示展开;组件行不展开(per data shape,实际上列表本来就只有主设备,过滤已生效)

G. 创建 collectComponentInfo / ListComponents 的测试(RED first — Wave 0 即 RED-first TDD 流程,不单独建 48-00-PLAN.md):
  - 用 mock SSH executor + 命令序列 spy 验证 D-10 两步流水线
  - 用 sqlite + httptest 验证 ListComponents 端点

注意(踩坑):
  - 不要在 collectComponentInfo 内 panic 或 return 非 nil 错给上层 markTaskFailed(D-14 明确)
  - 故障路径不调 operlog(WARNING 6,D-13 仅 UPDATE 路径);代码注释要明示
  - 后端 GET 端点权限复用 ops:asset:list,不新增 perm namespace
  - router.go:691-697 asset 组内注册 /components 端点(内联,符合现有模式)
  - 前端 useEffect deps 必须是 primitive(parentAssetId 字符串),不可传 object
  - opsApi.ts 的 get 函数签名:确认第二个参数是 query object 还是 data;参考现有 assetApi.list 写法
  </action>
  <verify>
    <automated>cd D:/code/ClaudeCode/xingran-go-backend && go build ./... && go test ./internal/services/... -run "TestCollectComponentInfo|TestListComponents" -v 2>&1 | head -50; cd xingran-react-frontend && npm run type-check 2>&1 | tail -10</automated>
  </verify>
  <done>
    processTask 内 collectComponentInfo hook 存在并按 device.Vendor 编排 D-10 两步流水线(华为/锐捷都有 status → transceiver 序列);失败仅 applogger.Warnf(不 operlog,不阻塞 chassis 更新);GET /ops/asset/components 在 router.go:691-697 asset 组内联注册(符合现有模式,无 SetupAssetRouter);前端 ComponentListTab 渲染 Antd Table;资产列表行可展开看组件;go build ./... 退出码 0;前端 npm run type-check 退出码 0;现有 operations / device_info_collection 测试不回归。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT — 真机验证推迟声明 + 现有资产系统回归</name>
  <files>.planning/STATE.md</files>
  <action>
人工 UAT 关卡。Phase 48 Wave 1-3 全部代码已落地:ops_asset 4 新列 + recon_category + dict seed;SNMP+CLI 收集器解析 35 fixtures(29 huawei + 6 ruijie)全绿(含 D-10 up 接口过滤专项测试);pipeline UPDATE-only 写入 + operlog.RecordBackground + 对账异常 emit;cron hook 接入(D-10 两步流水线编排 + 失败仅 warn);前端组件清单 Tab。本地(sqlite + PostgreSQL)单元测试全绿,go build / npm type-check 全绿。

人工 UAT 关卡。执行步骤:
1. 启动 xingran-backend.exe 观察日志(含 "Migration 201: Phase 48" 字样,无 ERROR)
2. psql 验证 schema:`\d ops_asset` 含 4 新列;`\d sys_data_reconciliation` 含 recon_category;`SELECT indexname FROM pg_indexes WHERE tablename='sys_data_reconciliation'` 含 uniq_recon_asset_type_cat_open 不含 uniq_recon_asset_type_open;`SELECT * FROM sys_dict_data WHERE dict_type='asset_reconciliation_recon_category'` 至少 1 行
3. 前端资产列表页回归(主设备列表/统计数字与 Phase 48 前一致;行可展开调 GET /ops/asset/components)
4. 现有对账看板回归(A-F 异常仍可筛,recon_category 字典可见)
5. 在 STATE.md 追加 Phase 48 真机 UAT deferred 声明(RESEARCH §Environment Availability 显式声明本环境无华为 S8700 / 锐捷 RS8607E,真机 UAT 推迟到下次现场访问)
  </action>
  <verify>
    <automated>cd D:/code/ClaudeCode/xingran-go-backend && grep -q "Phase 48 真机 UAT" .planning/STATE.md && echo "UAT_DEFERRED_DECLARED" || echo "MANUAL_VERIFY_REQUIRED"</automated>
  </verify>
  <done>
用户回 "approved" 或描述发现的问题;若仅真机部分未验证也可 approve(声明推迟到现场)。STATE.md 含 "Phase 48 真机 UAT" 字样;psql schema 验证通过;前端资产列表/对账看板无回归。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron→SNMP/SSH device | 内部 cron 向设备发 SNMP/SSH 跨边界 |
| device→collector→DB | 设备回显经解析器写入 ops_asset / sys_data_reconciliation |
| user→API→DB | 用户读 /ops/asset/components,跨认证边界(已有 Auth middleware) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-08 | Tampering | 组件 SN 字符串注入 SQL | mitigate | 所有 UPDATE/INSERT 用 GORM parameterized;OpsAssetWriter.Updates(map) 不拼 SQL |
| T-48-09 | Spoofing | 假 SNMP community 被设备接受 | accept | 复用现有 sys_auth_credential 凭据管理;无新增认证路径 |
| T-48-10 | Repudiation | 组件 UPDATE 无审计 | mitigate | operlog.RecordBackground 每次写都记,operatorName="system-cron" 可追溯 |
| T-48-11 | DoS | cron 对全部设备同时跑 SNMP 引发风暴 | mitigate | 复用现有 5-worker pool + 队列;EntityCollector 单 GET bounded by index set;失败 log warn 不重试不阻塞 |
| T-48-12 | Elevation | /ops/asset/components 端点权限缺失 | mitigate | 复用 router.go:679-685 asset 组已有的 ops:asset:list RequirePermissions middleware(per MEMORY namespace-split 教训);不新建独立 perm |
| T-48-13 | Information Disclosure | 组件 SN 是不是敏感信息 | accept | SN 是资产标识,资产系统本就存;无新增暴露面 |
| T-48-14 | Information Disclosure | D-10 up 过滤漏判导致 down 接口 transceiver 误采 | mitigate | collectComponentInfo 按 Vendor 强制两步流水线编排 + 两个 TestCollectComponentInfoTwoStepPipeline 测试断言命令序列 |
| T-48-16 | Repudiation | 失败路径无审计(WARNING 6) | accept | D-13 范围仅限 UPDATE 路径;失败用 applogger.Warnf 信令(应用日志层,非审计层);代码注释明示此不对称设计 |
| T-48-SC | Tampering | 无新 npm/pip/cargo 安装 | N/A | 本 plan 无包安装 |
</threat_model>

<verification>
- `go build ./...` 退出码 0
- `go test ./...` 全绿(含 48-01/48-02/48-03 新增测试)
- `go test ./internal/utils/operlog/` 全绿(回归保护 regression_test.go PASS)
- `cd xingran-react-frontend && npm run type-check` 退出码 0
- `cd xingran-react-frontend && npm run lint` 不引入新 warning(只允许 pre-existing)
- 启动后端,日志含 Migration 201 + DeviceInfoCollectionService 启动 + 5 worker pool
- psql 验证 4 + 1 列 + 1 partial unique index + dict seed 全部就位
- 前端资产列表行可展开,展开后调 /ops/asset/components
- 现有对账看板仍可筛选 A-F
- 真机 UAT 推迟到现场(RESEARCH 已声明)
- D-10 两步流水线在 collectComponentInfo 内显式编排(华为/锐捷都有专项测试)
- 失败路径仅 applogger.Warnf,不调 operlog(WARNING 6 设计明示)
- GET /ops/asset/components 在 router.go:691-697 asset 组内联注册(符合现有代码模式)
</verification>

<success_criteria>
- Pipeline 端到端跑通:Collect(含 D-10 两步流水线)→ UPDATE ops_asset → emit anomaly → operlog.RecordBackground
- 父交换机缺失(D-04)和组件未注册(D-02/D-06)两种异常路径都被覆盖
- cron 失败不阻塞 chassis 更新(D-14),失败仅 applogger.Warnf 不 operlog(D-13 范围明示)
- 前端组件清单 Tab 可用,且不污染现有资产列表(D-07)
- GET /ops/asset/components 确定性挂载在 router.go:691-697 asset 组内联(基于实际代码现状,非编造)
- 现有对账看板无回归
- 全部 go test / npm type-check / npm lint 通过
- operlog regression_test.go 不回归
- Phase 48 Wave 1-3 完整 ship(真机 UAT 推迟到现场声明)
</success_criteria>

<output>
Create `.planning/phases/48-device-component-serials-planned/48-03-SUMMARY.md` when done
</output>
