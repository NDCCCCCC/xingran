---
phase: 48-device-component-serials-planned
plan: 02
type: execute
wave: 2
depends_on:
  - 48-01
files_modified:
  - internal/device/snmp_entity_mib.go
  - internal/services/component_collector/component_set.go
  - internal/services/component_collector/snmp_entity_collector.go
  - internal/services/component_collector/cli_huawei_collector.go
  - internal/services/component_collector/cli_ruijie_collector.go
  - internal/services/component_collector/cmd_dispatcher.go
  - internal/services/component_collector/owner_resolver.go
  - internal/services/component_collector/snmp_entity_collector_test.go
  - internal/services/component_collector/cli_huawei_collector_test.go
  - internal/services/component_collector/cli_ruijie_collector_test.go
  - internal/services/component_collector/owner_resolver_test.go
  - internal/services/component_collector/fixtures_loader.go
  - templates/ruijie_os_show_version_modules.textfsm
  - templates/huawei_vrp_display_device_esn.textfsm
  - templates/huawei_vrp_display_interface_transceiver.textfsm
  - templates/huawei_vrp_display_interface_status.textfsm
  - templates/ruijie_os_show_interfaces_status.textfsm
  - templates/ruijie_os_show_interfaces_transceiver_ddm.textfsm
autonomous: true
requirements:
  - REQ-48-02
  - REQ-48-03
  - REQ-48-04
  - REQ-48-09
user_setup: []

must_haves:
  truths:
    - "ComponentSet / Component struct 定义就位,可被 Wave 3 pipeline 直接消费"
    - "SNMP ENTITY-MIB 单 GET 路径在 mock SNMP client 上能解析华为 S8700 / 锐捷 RS8607E 的真机 fixture 输出"
    - "锐捷 ENTITY-MIB 中 temprature* 噪声节点(Class=8 且 Name 前缀 temprature,D-11)按规则 `Class==8 && strings.HasPrefix(Name,\"temprature\")` 全部剔除,真电源(power0/1/2 Class=8 但不以 temprature 开头)保留(精确数字 352 来自现场实测,本环境无 SNMP ENTITY-MIB 真机 fixture,测试用 synthetic fixture 验证规则而非数字)"
    - "华为 S8700 class=module(5) 占位行(SN 空)被丢弃,class=fan(9) 真实体被保留"
    - "华为 CLI 光模块采集器先 ParseInterfaceStatus 过滤 up 接口,再解析 transceiver(丢掉非 up 接口的 transceiver 行)— D-10 落地"
    - "锐捷 CLI 光模块采集器走两步流水线: show interfaces status 拿 up 接口列表 → show interface transceiver 仅对该列表解析 — D-10 落地"
    - "锐捷 CLI 解析 show version modules 一次性输出 chassis SN + 7 个 module slot SN"
    - "锐捷 CLI 使用 _ddm.textfsm 解析 Vendor Serial Number + DDM Bias/Tx/Rx Power(WARNING 4 死代码风险消除)"
    - "CLI 收集器解析 templates/samples/ 全部 fixture 文件(TextFSM 解析成功率 100% 对样本而言)"
    - "fixtures_loader.go glob templates/samples/*.txt 计数等于实际文件数(本环境当前 35 个:29 huawei + 6 ruijie,计数由 glob 动态断言 `CountFixtures() > 0` 不写死)"
    - "OwnerResolver 能根据 entPhysicalContainedIn 把组件归到正确 chassis(class=3 + ContainedIn=0)"
    - "OwnerResolver 在 stack 场景(多个 ContainedIn=0 chassis)能识别 canonical root"
  artifacts:
    - path: "internal/services/component_collector/component_set.go"
      provides: "ComponentSet{Chassis, Components} + Component{ComponentType, Slot, SerialNumber, Model, Source, Raw} 类型定义"
      contains: "type ComponentSet"
    - path: "internal/services/component_collector/snmp_entity_collector.go"
      provides: "ENTITY-MIB 单 GET 收集器 + 锐捷 temprature* 过滤 + 华为 dual-class 去重"
      contains: "EntityCollector"
    - path: "internal/services/component_collector/cli_huawei_collector.go"
      provides: "华为 display device esn + display interface status(up 过滤) + display interface transceiver TextFSM 解析 — D-10"
      contains: "HuaweiCliCollector"
    - path: "internal/services/component_collector/cli_ruijie_collector.go"
      provides: "锐捷 show version modules + show interfaces status(up 过滤) + show interface transceiver DDM TextFSM 解析 — D-10"
      contains: "RuijieCliCollector"
    - path: "internal/services/component_collector/owner_resolver.go"
      provides: "entPhysicalContainedIn 树重建 + stack canonical root 选取(D-12)"
      contains: "ResolveOwnership"
    - path: "templates/ruijie_os_show_version_modules.textfsm"
      provides: "锐捷 show version Module information 段解析(Slot/Role/ModuleType/HW/Boot/SW/SN)"
      contains: "Module information"
    - path: "templates/huawei_vrp_display_device_esn.textfsm"
      provides: "华为 display device esn 单行 chassis SN"
      contains: "ESN of chassis"
    - path: "templates/huawei_vrp_display_interface_transceiver.textfsm"
      provides: "华为 display interface transceiver 块解析(Interface + Manu. Serial Number)"
      contains: "Manu. Serial Number"
    - path: "templates/huawei_vrp_display_interface_status.textfsm"
      provides: "华为 display interface status(up/down 接口清单,D-10 过滤前置)"
      contains: "Interface"
    - path: "templates/ruijie_os_show_interfaces_status.textfsm"
      provides: "锐捷 show interfaces status(up/down 接口清单,D-10 过滤前置)"
      contains: "Interface"
    - path: "templates/ruijie_os_show_interfaces_transceiver_ddm.textfsm"
      provides: "锐捷 show interface transceiver 解析(Vendor Serial Number + DDM Bias/Tx/Rx Power)"
      contains: "Vendor Serial Number"
  key_links:
    - from: "internal/services/component_collector/snmp_entity_collector.go"
      to: "internal/device/snmp_client.go"
      via: "SNMPClient.Get 调用"
      pattern: "snmpClient\\.Get\\(fmt\\.Sprintf"
    - from: "internal/services/component_collector/cli_huawei_collector.go"
      to: "internal/templates/textfsm.go"
      via: "TextFSM ParsePattern + Parse"
      pattern: "textfsm\\.Parse"
    - from: "internal/services/component_collector/cli_huawei_collector.go"
      to: "internal/services/component_collector/cli_huawei_collector.go"
      via: "ParseInterfaceStatus → ParseInterfaceTransceiver(D-10 up 过滤)"
      pattern: "ParseInterfaceStatus"
---

<objective>
Phase 48 Wave 2 — 采集器实现。仅产生 ComponentSet 数据结构,不写 ops_asset,不调对账。Wave 3 才把 ComponentSet 接入 pipeline。

Purpose: 落地 D-08(SNMP 单 GET)/ D-09(CLI 补充)/ D-10(只采 up 接口光模块)/ D-11(锐捷 temprature* 过滤)/ D-12(stack 归属)的纯解析逻辑。所有逻辑用 templates/samples/ 35 个真机样本(29 huawei + 6 ruijie)做 fixture-driven 测试,保证不需要真实设备即可 UAT。
Output: 新建 internal/services/component_collector/ 包(核心文件)+ 6 个新 TextFSM 模板(2 个 status 模板用于 D-10 up 过滤前置)+ internal/device/snmp_entity_mib.go 高层 helper。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/48-device-component-serials-planned/48-CONTEXT.md
@.planning/phases/48-device-component-serials-planned/48-RESEARCH.md
@.planning/phases/48-device-component-serials-planned/48-01-SUMMARY.md
@.planning/phases/48-device-component-serials-planned/48-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- SNMP client API (existing, no change) -->
From internal/device/snmp_client.go:
```go
type SNMPClient struct { ... }
func NewSNMPClient(config *SNMPClientConfig) *SNMPClient
func (c *SNMPClient) Get(oid string) (result interface{}, err error)  // 单 OID
func (c *SNMPClient) Walk(oid string, callback func(oid string, value interface{}) bool) error
func (c *SNMPClient) Close() error
// parseSNMPValue 把 gosnmp.SnmpPDU 转 string/int64(包内私有,不可外部调用)
```

<!-- TextFSM parser API (homegrown) -->
From internal/templates/textfsm.go:
```go
// 解析模板文件 + 文本输入,返回 []map[string]string
// 模板语法:Value <name> <regex> / Start / State / -> Record / -> Continue
// 详见 templates/huawei_vrp_display_device.textfsm 既有写法
```

<!-- Existing template to mirror (Ruijie show version chassis-only — needs MODULES variant) -->
From templates/ruijie_os_show_version.textfsm:仅抓 System serial number / System software version 等顶层字段,不抓 Slot N 行。本 plan 新建 _modules.textfsm 抓 Module information 段。

<!-- TextFSM sample input (real) -->
From templates/samples/ruijie_10_62_63_21_show_version.txt:
```
System serial number    : G1HLC0R000096
Module information:
  Slot M1 : M8600E-CM
    Hardware version    : 1.03
    Boot version        : 1.2.25
    Software version    : S8600E_RGOS 11.0(4)B8P3
    Serial number       : G1HLC0R000096
  Slot 1 : M8600E-24GT20SFP4XS-ED
    Serial number       : G1P7286000129
  ...
```

From templates/samples/huawei_10_62_25_253_display_interface_transceiver.txt:
```
 10GE5/0/4 transceiver information:
 Common information:
   Transceiver Type                      :1000BASE_SX
 Manufacture information:
   Manu. Serial Number                   :8000012000082
   Manufacturing Date                    :2025-2-20
```

From templates/samples/huawei_10_62_25_253_display_esn.txt:
```
ESN of chassis 1: 102599861597
```

<!-- AuthCredential field (existing) -->
From internal/models/auth_credential.go:
```go
type AuthCredential struct {
    BaseModel
    SNMPCommunities pq.StringArray `gorm:"type:text[]" json:"snmpCommunities,omitempty"`
    SNMPVersion     SNMPVersion    `gorm:"size:10;default:v2c" json:"snmpVersion"`
}
// snmp_community 单数列已被 database.go line 734 DROP,不可回退读取
```

<!-- CONSTRAINT: SNMP 必须单 GET,不能 BulkWalk -->
Per D-08 + RESEARCH Pattern 1: 华为 S8700 拒绝 GetBulk。先用一次 Walk on entPhysicalClass 拿到 entity index 集合,然后对每个 index 单 GET 拉 .11/.13/.5/.4/.7 五个属性。

<!-- CONSTRAINT: temprature* 过滤规则 -->
Per D-11: 锐捷 ENTITY-MIB powerSupply(8) class 下大量 temprature* 节点是伪实体。过滤条件 = `Class==8 && strings.HasPrefix(Name, "temprature")`。

<!-- CONSTRAINT: 华为 dual-class 去重 -->
Per Pitfall 2: 同一引擎被报为 module(5,SN 空) + fan(9,SN 实),优先保留 SN 非空者。

<!-- CONSTRAINT: D-10 光模块只采 up 接口 — 跨 T2b/T2c 强制 -->
Per D-10 + Pitfall 5: CLI 光模块采集必须先解析接口状态过滤 up 接口,再对 up 子集解析 transceiver。否则对未插模块的空口会进入交互提示或返回不可解析输出。该约束在 T2b(华为)、T2c(锐捷)两处都必须显式实现并有专项测试断言。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 component_collector 包 + ComponentSet 类型 + OwnerResolver + 测试</name>
  <files>internal/services/component_collector/component_set.go, internal/services/component_collector/owner_resolver.go, internal/services/component_collector/owner_resolver_test.go</files>
  <read_first>
    - internal/services/portcollection/parser.go (vendor dispatcher 模式参考)
    - .planning/phases/48-device-component-serials-planned/48-RESEARCH.md (Pattern 1 SNMP / Pitfall 2 dual-class / Pitfall 7 stack)
    - .planning/phases/48-device-component-serials-planned/48-CONTEXT.md §specifics (ComponentSet / Component 骨架已锁定)
  </read_first>
  <behavior>
    - ComponentSet{Chassis *Component, Components []Component} 可被 json 序列化
    - Component 含字段:ComponentType, Slot, SerialNumber, Model, Source, Raw
    - ComponentType 枚举字符串常量:chassis / card / engine / power / fan / transceiver
    - Source 枚举:snmp / cli-huawei / cli-ruijie
    - OwnerResolver.ResolveOwnership(entities []EntityRow) []OwnedComponent 给定扁平 ENTITY-MIB 行列表,返回每个组件归属的 chassis index;无 stack 时所有组件归 ContainedIn 链回溯到的 root(class=3 + ContainedIn=0)
    - stack 场景(多个 ContainedIn=0 行):选取第一个 ContainedIn=0 的 chassis 为 canonical root,其余 chassis 行被打上 StackMember=true 但 component 归属到 canonical root(D-12)
    - TestOwnerResolverSingleChassis:1 chassis + 5 children → 5 OwnedComponent 全归 chassis index=1
    - TestOwnerResolverStackMode:2 chassis(ContainedIn=0)+ 各自 children → 全部 children 归第一个 chassis
    - TestOwnerResolverOrphanComponent:某组件 ContainedIn 指向不存在的 index → 标记 Orphan=true,不 panic
  </behavior>
  <action>
先创建 internal/services/component_collector/component_set.go(类型定义,纯数据结构,无外部依赖):
  - 定义 ComponentType* / Source* 字符串常量集(对应 CONTEXT §specifics)
  - type Component struct { ComponentType string; Slot string; SerialNumber string; Model string; Source string; Raw string }
  - type ComponentSet struct { Chassis *Component; Components []Component }
  - type EntityRow struct { Index int; Class int; ContainedIn int; Name string; Serial string; Model string } (SNMP 行解析中间结构)
  - type OwnedComponent struct { Component EntityRow; ChassisIndex int; IsStackMember bool; Orphan bool }

再创建 internal/services/component_collector/owner_resolver.go:
  - type OwnerResolver struct{} 纯函数式,无状态
  - func (r *OwnerResolver) ResolveOwnership(entities []EntityRow) []OwnedComponent
    - 先扫描一遍,识别所有 Class==3(chassis) 且 ContainedIn==0 的行,排序取首为 canonical
    - 建 map[int]int(entity index → chassis index),把每个 chassis 自己映射到自己,其他组件通过 ContainedIn 链回溯(防环:遍历上限 32 次返回 Orphan=true)
    - canonical root 之外的其他 chassis 行:既出现在 entities 里(作为 component 标记 IsStackMember=true),也作为其他组件的 parent map 目标(归属到 canonical)
  - 注意:D-12 要求保留接口但本环境无 stack 设备,UAT 弱,所以测试只需覆盖 stack 数据结构正确,不要求真实设备验证

最后创建 owner_resolver_test.go(RED then GREEN — Wave 0 即 RED-first TDD 流程,不单独建 48-00-PLAN.md):3 个 TestXxx 函数覆盖上述 behavior。
  </action>
  <verify>
    <automated>cd D:/code/ClaudeCode/xingran-go-backend && go test ./internal/services/component_collector/ -run "TestOwnerResolver" -v</automated>
  </verify>
  <done>
    component_set.go 含 ComponentSet / Component / EntityRow / OwnedComponent 类型;OwnerResolver 实现 D-12 树重建 + stack canonical root;3 个 owner_resolver 测试全 PASS;go build ./... 退出码 0。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2a: SNMP ENTITY-MIB 单 GET helper + EntityCollector(temprature* 过滤 + dual-class 去重)+ fixtures_loader + 测试</name>
  <files>internal/device/snmp_entity_mib.go, internal/services/component_collector/snmp_entity_collector.go, internal/services/component_collector/snmp_entity_collector_test.go, internal/services/component_collector/fixtures_loader.go</files>
  <read_first>
    - internal/device/snmp_client.go (Get/Walk API, line 153-252)
    - templates/samples/ruijie_10_62_63_21_*.txt(锐捷 ENTITY-MIB / 真机回显格式参考)
    - templates/samples/huawei_10_62_25_253_*.txt(华为 ENTITY-MIB 真机回显)
    - .planning/phases/48-device-component-serials-planned/48-RESEARCH.md §Code Examples #1 (SNMP Get 调用模式) + §Pitfall 1/2 (temprature* / dual-class)
    - internal/services/component_collector/component_set.go + owner_resolver.go (Task 1 产出)
  </read_first>
  <behavior>
    - internal/device/snmp_entity_mib.go:导出 OID 常量 + EntityAttrs struct + 2 个 helper(WalkEntityIndex 用 class-filtered Walk 拿 index set;GetEntityAttrs 单 GET 拉 5 属性)
    - snmp_entity_collector.go:EntityCollector{client *device.SNMPClient, resolver *OwnerResolver};Collect(ctx, community string) → ComponentSet
    - 锐捷 fixture(Walk 返 627 行其中 352 个 temprature*)经 EntityCollector 后 Components 长度 <=20(精确数字由测试断言,不含 temprature* 也不含空 SN 行)
    - 华为 fixture 经 EntityCollector 后 chassis 含 SN,fan/电源 非空,module(5) SN 空行被丢弃
    - temprature* 过滤规则:`Class==8 && strings.HasPrefix(Name, "temprature")` → drop(D-11)
    - 华为 dual-class 去重:同 SN 出现在 module(5)+fan(9) 时保留 fan(9) 行(SN 非空)(Pitfall 2)
    - fixtures_loader.go:func LoadFixture(name string) string 读 templates/samples/<name>;func CountFixtures() int 用 filepath.Glob("templates/samples/*.txt") 返回实际数(本环境当前 35,但断言写 `CountFixtures() > 0` 不写死 35)
    - TestEntityCollectorRuijieFiltersTemprature:注入 synthetic fixture(精简版含 1 个 temprature* 噪声 Class=8 + 5 个真实体),断言过滤后无 "temprature" 字串前缀,真电源(power0 Class=8 但不以 temprature 开头)保留;规则可验证,不依赖精确 352 数字(本环境无 SNMP ENTITY-MIB 真机 fixture)
    - TestEntityCollectorHuaweiDualClassDedup:注入 module(5,SN 空)+fan(9,SN 实)同 entity,断言保留 fan
    - TestEntityCollectorPreservesRealPowerSupply:注入 Class==8 && Name="power0" 行(RG-PA600I 电源),断言 NOT 被过滤掉(D-11 真电源保留)
    - TestFixturesLoaderGlobCount:CountFixtures() > 0,且 LoadFixture("ruijie_10_62_63_21_show_version.txt") 返回非空字符串
  </behavior>
  <action>
A. 创建 internal/device/snmp_entity_mib.go(OID 常量 + EntityAttrs + helpers):
  - 常量 oidEntPhysicalSerialNum="1.3.6.1.2.1.47.1.1.1.1.11" 等 5 个(参见 RESEARCH Pattern 1)
  - type EntityAttrs struct { Serial, Model, Name string; Class, ContainedIn int }
  - func CountPhysicalEntitiesByClass(client *SNMPClient, classes []int) (map[int]struct{}, error) — 单次 Walk on oidEntPhysicalClass,过滤 class ∈ classes 收集 index(从 OID 后缀提取)
  - func GetEntityAttrs(client *SNMPClient, idx int) (EntityAttrs, error) — 对 5 个 OID 拼接 ".<idx>" 做 5 次 Get,转 string/int64(注意 parseSNMPValue 是包内私有,这里需在 snmp_entity_mib.go 内重做 type switch on interface{} — 避免修改 snmp_client.go)
  - 注意:SNMPClient 接口当前是 struct,Get 返回 interface{},需要 type switch string/int64;gosnmp Integer 类型实际是 int,但 Go 反射有时给 int64,需要兼容

B. 创建 internal/services/component_collector/fixtures_loader.go(放最前,被 T2b/T2c 测试共用):
  - func LoadFixture(name string) string 读 templates/samples/<name> 返回 string(os.ReadFile,参考 internal/services/portcollection 现有 fixture 加载风格)
  - func CountFixtures() int 用 filepath.Glob("templates/samples/*.txt") 返回 len(matches)
  - 注意:用相对路径 templates/samples/(go test 在包目录运行,cwd=包目录;需用 runtime.Caller 或 os.Getwd() 拼接项目根,参考 portcollection fixture_loader 实现)

C. 创建 internal/services/component_collector/snmp_entity_collector.go:
  - type EntityCollector struct { client *device.SNMPClient; resolver *OwnerResolver; community string }
  - func NewEntityCollector(client *device.SNMPClient, community string) *EntityCollector
  - func (c *EntityCollector) Collect(ctx context.Context) (*ComponentSet, error) 实现 RESEARCH Pattern 1:
    1. classes := []int{3,5,6,7,8,9} (chassis/module/port/fan/powerSupply/stack)
    2. indices := CountPhysicalEntitiesByClass(c.client, classes)
    3. 对每个 idx 调 GetEntityAttrs,组装 EntityRow
    4. 应用 temprature* 过滤(D-11):strings.HasPrefix(Name, "temprature") && Class==8 → drop
    5. 应用 dual-class 去重(华为 Pitfall 2):同 entity index 出现 module(5)+fan(9) 时保留 SN 非空者(以 idx+SN 唯一化)
    6. 调 resolver.ResolveOwnership,组装 ComponentSet(Chassis = canonical root 的 Component,Components = 其余)
    7. 跳过 SN 空行(除了 chassis 自己)

D. 创建 snmp_entity_collector_test.go(RED first — Wave 0 即 RED-first TDD 流程,不单独建 48-00-PLAN.md):4 个 TestXxx 覆盖上述 behavior。
  - 锐捷 fixture 行数据可用代码内嵌入([]string 模拟 627 行采样,或精简版 30 行含 1 个 temprature* 噪声 + 5 个真实体)
  - 华为 fixture 用代码内嵌 dual-class 数据
  - mock SNMP client 用 interface + stub struct(可在 EntityCollector 用 interface 而非 *device.SNMPClient 直接持有,便于测试)

注意(踩坑):
  - 不要调 parseSNMPValue(包私有);在 snmp_entity_mib.go 用 type switch on interface{}(string/integer/int64)
  - EntityCollector 的 SNMP client 字段建议抽 interface{ Get(oid string)(interface{},error); Walk(oid string, cb func(string, interface{}) bool) error } 便于测试 mock
  </action>
  <verify>
    <automated>cd D:/code/ClaudeCode/xingran-go-backend && go test ./internal/services/component_collector/ -v -run "TestEntityCollector|TestFixturesLoader" && go test ./internal/device/... -v -run "TestCountPhysicalEntities|TestGetEntityAttrs" 2>/dev/null; go build ./...</automated>
  </verify>
  <done>
    internal/device/snmp_entity_mib.go 含 OID 常量 + 2 helper;snmp_entity_collector.go EntityCollector 实现 D-11 temprature* 过滤 + Pitfall 2 dual-class 去重;fixtures_loader.go LoadFixture + CountFixtures(glob 动态计数);4 个新测试全 PASS;go build ./... 退出码 0。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2b: 华为 CLI 采集器(display device esn + display interface status + display interface transceiver)+ 3 个华为 TextFSM 模板 + D-10 up 接口过滤 + 测试</name>
  <files>internal/services/component_collector/cli_huawei_collector.go, internal/services/component_collector/cli_huawei_collector_test.go, templates/huawei_vrp_display_device_esn.textfsm, templates/huawei_vrp_display_interface_transceiver.textfsm, templates/huawei_vrp_display_interface_status.textfsm</files>
  <read_first>
    - templates/ruijie_os_show_version.textfsm / huawei_vrp_display_device.textfsm (现有 TextFSM 模板语法)
    - templates/huawei_vrp_display_interface.textfsm(现有 interface 模板,本任务 _status 变体可参考)
    - templates/samples/huawei_10_62_25_253_display_interface_transceiver.txt (华为样本,含 10GE5/0/4 + 10GE5/0/5 两个 SFP)
    - templates/samples/huawei_10_62_25_253_display_esn.txt (display esn 单行,真实样本可能含 "Error: Unrecognized command" — 模板需容忍)
    - templates/samples/huawei_10_62_25_253_display_interface_status.txt(**本环境未采集此真机样本**——华为 display interface status 测试用代码内嵌 minimal fixture,例如 `"10GE5/0/4  up   10G  Full  fiber  ..."` 两行(1 up + 1 down),不依赖外部文件)
    - internal/templates/textfsm.go (TextFSM parser API,ParsePattern + Parse 流程)
    - internal/services/component_collector/fixtures_loader.go (Task 2a 产出,用于加载 fixture)
    - .planning/phases/48-device-component-serials-planned/48-RESEARCH.md §Pitfall 5 (D-10 必须先 status 后 transceiver)
  </read_first>
  <behavior>
    - HuaweiCliCollector{tmplLoader} 用 internal/templates/textfsm.go 加载模板
    - ParseDisplayDeviceEsn(raw string) []Component:解析 ESN of chassis 单行 → chassis SN;Error 行返回空 slice + nil err 不 panic
    - ParseInterfaceStatus(raw string) []string:解析 display interface status,返回 up 接口名列表(如 ["10GE5/0/4", "10GE5/0/5"]);down 接口被丢弃
    - ParseInterfaceTransceiver(raw string, upInterfaces []string) []Component:**先按 upInterfaces 过滤**(若 upInterfaces=nil 则不过滤,但生产路径必须传非 nil),再解析 transceiver 块(Interface + Manu. Serial Number Filldown),丢弃 status != up 接口的收发器行 — D-10 落地
    - TestHuaweiCliParseDisplayDeviceEsn:解析 display_device_esn 样本,断言 chassis SN = "102599861597"(若样本含 Error 则断言返回空列表 + nil err,不 panic)
    - TestHuaweiCliParseInterfaceStatus:解析 display interface status 样本,断言返回的 up 接口列表(如含 10GE5/0/4 + 10GE5/0/5)
    - TestHuaweiCliParseInterfaceTransceiver:解析 display_interface_transceiver 样本,**传入 upInterfaces=["10GE5/0/4","10GE5/0/5"]**,断言返回 2 个 transceiver,SN="8000012000082" / "1000159211688"
    - TestHuaweiTransceiverFiltersDownPorts(D-10 强制):**构造 fixture 含 up 接口 10GE5/0/4(SN=A) + down 接口 10GE5/0/6(SN=B),传 upInterfaces=["10GE5/0/4"]**,断言返回的 transceiver 列表**不含** 10GE5/0/6(SN=B 被丢弃)。这是 D-10 在华为侧的硬性测试。
  </behavior>
  <action>
A. 创建 3 个华为 TextFSM 模板:
  - templates/huawei_vrp_display_device_esn.textfsm:单行匹配 `^ESN of chassis\s+(\d+)\s*:\s+(\S+)\s*$$` → Record {ChassisID, ESN};容忍 "Error: Unrecognized command" 行(Start 状态不匹配 Error 行直接 Continue)
  - templates/huawei_vrp_display_interface_transceiver.textfsm:Start 识别 `^\s*(\S+)\s+transceiver information:` → Transceiver state 设 Interface Filldown,Transceiver 内匹配 `^\s*Manu\.\s+Serial Number\s*:\s*(\S+)\s*$$` 后 Record。可参考现有 templates/huawei_vrp_display_interface.textfsm
  - templates/huawei_vrp_display_interface_status.textfsm:解析 display interface status 的表格输出,匹配 `^(\S+)\s+(up|down)\s+...` 提取 Interface + Status,Record 后由 Go 侧过滤 status=="up"

B. 创建 internal/services/component_collector/cli_huawei_collector.go:
  - type HuaweiCliCollector struct { tmplLoader textfsm.Loader } (复用现有 loader 模式)
  - func (h *HuaweiCliCollector) ParseDisplayDeviceEsn(raw string) ([]Component, error)
  - func (h *HuaweiCliCollector) ParseInterfaceStatus(raw string) ([]string, error) — 返回 status=="up" 的接口名 slice
  - func (h *HuaweiCliCollector) ParseInterfaceTransceiver(raw string, upInterfaces []string) ([]Component, error):
    1. 用 _transceiver.textfsm 解析所有 transceiver 块,得 [{Interface, Serial}, ...]
    2. **若 upInterfaces != nil: 用 map[string]bool 过滤,仅保留 Interface ∈ upInterfaces 的行(D-10)**
    3. 返回过滤后列表,组装为 Component{ComponentType:"transceiver", Slot:Interface, SerialNumber:Serial, Source:"cli-huawei"}

C. 创建 cli_huawei_collector_test.go(RED first — Wave 0 即 RED-first TDD 流程,不单独建 48-00-PLAN.md):4 个 TestXxx 覆盖上述 behavior,fixture 用 LoadFixture(name) 加载真机样本。
  - TestHuaweiTransceiverFiltersDownPorts 是 D-10 关键测试,必须显式构造 down 接口 fixture(可嵌入字符串)并断言被过滤
  </action>
  <verify>
    <automated>cd D:/code/ClaudeCode/xingran-go-backend && go test ./internal/services/component_collector/ -v -run "TestHuawei" && go build ./...</automated>
  </verify>
  <done>
    cli_huawei_collector.go 实现 ParseDisplayDeviceEsn + ParseInterfaceStatus + ParseInterfaceTransceiver(D-10 up 过滤);3 个华为 TextFSM 模板存在;4 个测试全 PASS(含 TestHuaweiTransceiverFiltersDownPorts 显式验证 D-10);go build ./... 退出码 0。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2c: 锐捷 CLI 采集器(show version modules + show interfaces status + show interface transceiver DDM)+ 3 个锐捷 TextFSM 模板 + cmd_dispatcher + D-10 up 接口过滤 + DDM 死代码消除 + 测试</name>
  <files>internal/services/component_collector/cli_ruijie_collector.go, internal/services/component_collector/cli_ruijie_collector_test.go, internal/services/component_collector/cmd_dispatcher.go, templates/ruijie_os_show_version_modules.textfsm, templates/ruijie_os_show_interfaces_status.textfsm, templates/ruijie_os_show_interfaces_transceiver_ddm.textfsm</files>
  <read_first>
    - templates/ruijie_os_show_version.textfsm (现有 chassis-only 模板,本任务新建 _modules 变体)
    - templates/ruijie_os_show_interfaces_transceiver.textfsm (现有 transceiver 模板,本任务新建 _ddm 变体加 DDM)
    - templates/samples/ruijie_10_62_63_21_show_version.txt (锐捷样本输入,含 chassis + 7 module slot)
    - templates/samples/ruijie_10_62_63_21_show_interface_transceiver.txt(若存在,锐捷 transceiver 含 G1PT549427799 等)
    - templates/samples/ruijie_10_62_63_21_show_interfaces_status.txt(若存在)
    - internal/services/portcollection/parser.go (vendor dispatcher 风格,本任务 cmd_dispatcher 镜像)
    - internal/services/component_collector/fixtures_loader.go (Task 2a 产出)
    - .planning/phases/48-device-component-serials-planned/48-RESEARCH.md §Pitfall 5 (D-10 必须先 status 后 transceiver)
  </read_first>
  <behavior>
    - RuijieCliCollector{tmplLoader} 用 internal/templates/textfsm.go 加载模板
    - ParseShowVersionModules(raw string) []Component:解析 show version Module information 段,返回 chassis + N module(M1/M2 → engine,Slot N → card);chassis SN="G1HLC0R000096",slot 1 SN="G1P7286000129"
    - ParseInterfacesStatus(raw string) []string:解析 show interfaces status,返回 up 接口名列表(D-10 前置)
    - ParseTransceiverDDM(raw string, upInterfaces []string) []Component:**先按 upInterfaces 过滤**(D-10),用 _ddm.textfsm 解析 Vendor Serial Number + DDM Bias/Tx/Rx Power(Filldown),组装为 Component{ComponentType:"transceiver", Slot:Interface, SerialNumber:VendorSN, Source:"cli-ruijie"}。该 collector 必须连接到 _ddm.textfsm 模板(WARNING 4 死代码消除)
    - TestRuijieCliParseShowVersionModules:解析 show_version 样本,断言 chassis SN="G1HLC0R000096" + 7 个 module slot(M1/M2/1-5),slot 1 SN="G1P7286000129"
    - TestRuijieCliParseInterfacesStatus:解析 show interfaces status 样本,断言返回 up 接口列表
    - TestRuijieCliParseTransceiverDDM:解析 transceiver DDM 样本(含 Vendor Serial Number),**传入 upInterfaces** 断言返回的 transceiver 含 Vendor SN(如 G1PT549427799),且 DDM Bias/Tx/Rx 字段被提取
    - TestRuijieTransceiverFiltersDownPorts(D-10 强制):**构造 fixture 含 up 接口 TenGigabitEthernet 1/47(SN=G1PT549427799,与真机 show_interfaces_transceiver.txt 一致) + down 接口 TenGigabitEthernet 1/45(SN=B),传 upInterfaces=["TenGigabitEthernet 1/47"]**,断言返回列表不含 1/45。这是 D-10 在锐捷侧的硬性测试。接口名用 TenGigabitEthernet 与真机 fixture 对齐(真机 1/47/1/48 是有 SFP 的 fiber 口)。
    - cmd_dispatcher.go:getCollectorCommands(vendor, kind) []string 返回正确命令字符串,**含两步流水线(transceiver → status + transceiver)**
  </behavior>
  <action>
A. 创建 3 个锐捷 TextFSM 模板:
  - templates/ruijie_os_show_version_modules.textfsm:State 化模板,Start 状态识别 "Module information:" → Modules 状态,Modules 状态匹配 "  Slot <SLOT> : <MODULE_TYPE>" 进入 ModuleDetail,ModuleDetail 匹配 "    Serial number       : <SN>" 后 Record,Slot Filldown。最终返回 [{Slot=M1, ModuleType=M8600E-CM, SN=G1HLC0R000096}, ...]
  - templates/ruijie_os_show_interfaces_status.textfsm:解析 show interfaces status 表格,提取 Interface + Status,Record 后由 Go 侧过滤 status=="up"
  - templates/ruijie_os_show_interfaces_transceiver_ddm.textfsm:基于现有 ruijie_os_show_interfaces_transceiver.textfsm,但增加 Vendor Serial Number 字段 + DDM 部分(Bias/Tx Power/Rx Power)为可选 Filldown。**该模板必须被 RuijieCliCollector.ParseTransceiverDDM 引用,消除死代码(WARNING 4)**

B. 创建 internal/services/component_collector/cli_ruijie_collector.go:
  - type RuijieCliCollector struct { tmplLoader textfsm.Loader }
  - func (r *RuijieCliCollector) ParseShowVersionModules(raw string) ([]Component, error):
    1. 用 _modules.textfsm 解析 Module information 段
    2. 把 System serial number(顶层)提取为 ComponentType=chassis,Slot="chassis"
    3. Slot M1/M2 提取为 ComponentType=engine;Slot N(N=数字)提取为 ComponentType=card
    4. 返回 chassis + N module
  - func (r *RuijieCliCollector) ParseInterfacesStatus(raw string) ([]string, error) — 返回 status=="up" 的接口名 slice
  - func (r *RuijieCliCollector) ParseTransceiverDDM(raw string, upInterfaces []string) ([]Component, error):
    1. 用 _ddm.textfsm 解析 Vendor Serial Number + DDM Bias/Tx/Rx Power
    2. **若 upInterfaces != nil: 用 map[string]bool 过滤(D-10)**
    3. 返回过滤后列表,组装 Component

C. 创建 internal/services/component_collector/cmd_dispatcher.go:
  - func getCollectorCommands(vendor models.DeviceVendor, kind string) []string,switch 表参考 RESEARCH Pattern 2,**含 D-10 两步流水线**:
    - huawei chassis → ["display device esn"]
    - huawei transceiver → ["display interface status", "display interface transceiver"] (两步流水线,status 先用于过滤 up)
    - ruijie module → ["show version"]
    - ruijie transceiver → ["show interfaces status", "show interface transceiver"] (两步流水线)
    - 默认 → nil

D. 创建 cli_ruijie_collector_test.go(RED first — Wave 0 即 RED-first TDD 流程,不单独建 48-00-PLAN.md):4 个 TestXxx 覆盖上述 behavior,fixture 用 LoadFixture(name) 加载真机样本。
  - TestRuijieTransceiverFiltersDownPorts 是 D-10 关键测试
  - TestRuijieCliParseTransceiverDDM 是 WARNING 4 死代码消除的关键测试
  </action>
  <verify>
    <automated>cd D:/code/ClaudeCode/xingran-go-backend && go test ./internal/services/component_collector/ -v -run "TestRuijie|TestCmdDispatcher|TestGetCollectorCommands" && go build ./...</automated>
  </verify>
  <done>
    cli_ruijie_collector.go 实现 ParseShowVersionModules + ParseInterfacesStatus + ParseTransceiverDDM(D-10 up 过滤 + DDM 字段提取);cmd_dispatcher.go 实现 vendor 分发(含 transceiver 两步流水线);3 个锐捷 TextFSM 模板存在(含 _ddm 模板被实际引用,无死代码);4 个测试全 PASS(含 TestRuijieTransceiverFiltersDownPorts + TestRuijieCliParseTransceiverDDM);go build ./... 退出码 0。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device→collector | 网络设备 SNMP/SSH 回显跨信任边界进入解析器 |
| collector→in-memory | ComponentSet 仅在内存,无 DB 写入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-04 | Tampering | CLI 输出含 regex 元字符的 SN | mitigate | TextFSM 模板用 ^...$$ 锚定,无 eval/regex 副作用;Component.SerialNumber 仅做字符串存储,后续 UPDATE 用 parameterized SQL |
| T-48-05 | DoS | 627 行 ENTITY-MIB 解析耗 CPU | accept | 单 GET 循环 bounded by index set(<150),过滤后 <=20;5 worker 池限流 |
| T-48-06 | Information Disclosure | temprature* 过滤意外丢真电源 | mitigate | 过滤条件必须 Class==8 AND HasPrefix("temprature") 同时满足,真电源(Name=power0/power1)Class=8 但不以 temprature 开头故保留;测试 TestEntityCollectorPreservesRealPowerSupply 显式断言保留 RG-PA600I;TestEntityCollectorRuijieFiltersTemprature 显式断言不含 temprature* |
| T-48-07 | Elevation | SSH 凭据泄露 | accept | 复用现有 device/connection_pool.go,不引入新凭据路径 |
| T-48-14 | Information Disclosure | D-10 up 过滤漏判导致 down 接口 transceiver 被误采 | mitigate | T2b TestHuaweiTransceiverFiltersDownPorts + T2c TestRuijieTransceiverFiltersDownPorts 两个硬性测试断言 down 接口 SN 被丢弃;cmd_dispatcher 的 transceiver 路径强制返回两步流水线(status + transceiver) |
| T-48-15 | Tampering | Ruijie _ddm.textfsm 死代码(WARNING 4) | mitigate | TestRuijieCliParseTransceiverDDM 显式断言 RuijieCliCollector.ParseTransceiverDDM 调用 _ddm.textfsm 解析 Vendor Serial Number + DDM 字段 |
| T-48-SC | Tampering | 无新 npm/pip/cargo 安装 | N/A | 本 plan 无包安装 |
</threat_model>

<verification>
- `go build ./...` 退出码 0
- `go test ./internal/services/component_collector/ -v` 全绿(覆盖 SNMP + 2 CLI + owner_resolver + fixtures_loader,4 任务测试合计 ~15 个测试)
- `go test ./internal/device/...` 全绿(snmp_entity_mib helper 测试)
- 现有 portcollection / operations 测试不回归
- templates/samples/*.txt 全部能被对应 collector/模板解析(fixtures_loader.CountFixtures() > 0 + 各 collector 测试覆盖代表性 fixture)
- D-10 在 T2b/T2c 两处都有显式测试断言(TestHuaweiTransceiverFiltersDownPorts + TestRuijieTransceiverFiltersDownPorts)
- WARNING 4 死代码消除:_ddm.textfsm 被 ParseTransceiverDDM 引用并有专项测试
- 不触碰任何 DB 写入路径(本 plan 纯解析,Wave 3 才接入)
</verification>

<success_criteria>
- ComponentSet 数据结构 + OwnerResolver 实现 D-12 stack 归属接口
- SNMP 单 GET 路径正确过滤锐捷 352 噪声 + 华为 dual-class 去重,真电源保留
- 华为 / 锐捷 CLI 收集器都实现 D-10 up 接口前置过滤(两步流水线 + 专项测试)
- 锐捷 _ddm.textfsm 被 ParseTransceiverDDM 实际使用(WARNING 4 消除)
- 6 个新 TextFSM 模板可被 textfsm.go parser 加载
- fixtures_loader 用 glob 动态计数,不写死 35
- 无运行时副作用(DB 零写入,cron 未接入)
</success_criteria>

<output>
Create `.planning/phases/48-device-component-serials-planned/48-02-SUMMARY.md` when done
</output>
