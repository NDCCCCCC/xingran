---
phase: 49-v1-18-gap-closure
plan: 02
type: execute
wave: 2
depends_on: [49-01]
files_modified:
  - internal/services/device_info_collection_service.go
  - internal/services/device_info_collection_service_test.go
autonomous: false
requirements: [GAP-1, GAP-2, E2E]
user_setup: []

must_haves:
  truths:
    - "collectComponentInfo 在 transceiver pipeline 之前调用 ParseShowVersionModules(ruijie)/ParseDisplayDeviceEsn(huawei) 提取板卡组件(engine/card)"
    - "提取的板卡组件(仅 engine/card,**丢弃 chassis 行**)被合并进 ComponentSet.Components,然后通过 Pipeline.Run 写入 ops_asset"
    - "set.Chassis 保持 nil —— 不写 chassis 资产行,因为 chassis 资产已存在(parent 本身);Pipeline.Run 也只消费 set.Components,从不消费 *set.Chassis"
    - "parent_asset_id 通过 ops_asset.devicesn = sys_network_device.serial_number(由 49-01 填充)匹配 chassis 资产"
    - "M1 主控板卡(其 SN 在真机上等于 chassis SN)仍作为 engine 类型板卡保留在 set.Components 中,显示在「从属组件清单」Tab —— 它独立于被丢弃的 chassis 行"
    - "资产 CX-WH-WH-04F-FL-RS8607E-03 (devicesn=G1M9140000175) 部署后展开「从属组件清单」Tab 显示 ≥6 条板卡组件(M1/1-5/M2)"
    - "H3C/Maipu/未知 vendor 设备继续无板卡采集(与 v1.18 D-08 ENTITY-MIB deferred 相同),非本阶段回归"
  artifacts:
    - path: "internal/services/device_info_collection_service.go"
      provides: "collectComponentInfo 接入 ParseShowVersionModules/ParseDisplayDeviceEsn 合并到 ComponentSet 喂给 Pipeline.Run"
      contains: "ParseShowVersionModules"
    - path: "internal/services/device_info_collection_service_test.go"
      provides: "collectComponentInfo 接入板卡采集的单元测试(mock runner)"
      contains: "TestCollectComponentInfo"
  key_links:
    - from: "internal/services/device_info_collection_service.go::collectComponentInfo"
      to: "component_collector.NewRuijieCliCollector().ParseShowVersionModules"
      via: "函数调用 + 结果合并到 set.Components(板卡行)"
      pattern: "ParseShowVersionModules"
    - from: "internal/services/device_info_collection_service.go::collectComponentInfo"
      to: "component_collector.Pipeline.Run"
      via: "函数调用(已存在,本计划保持)"
      pattern: "pipeline.Run"
    - from: "internal/services/device_info_collection_service.go::collectComponentInfo"
      to: "ops_asset (via Pipeline.Run → OpsAssetWriter.Write)"
      via: "UPDATE/INSERT per component SN"
      pattern: "writer.Write"
---

<objective>
修复 Gap 1 + Gap 2 + E2E,完成 v1.18 组件采集链路。

Gap 1:`collectComponentInfo`(device_info_collection_service.go:616-654)是 dead code —— 只调用了光模块 transceiver pipeline,**未调用**现成的 ParseShowVersionModules / ParseDisplayDeviceEsn。CONTEXT.md §Gap 1 line 28-31 的注释"already collected by the chassis collector"是错误假设。

Gap 2:ComponentSet 喂给 Pipeline.Run 后,组件 parent_asset_id 通过 `device.SerialNumber → AssetLookup.GetByDeviceSN → ops_asset.devicesn` 链路匹配 chassis 资产(pipeline.go:92-104 + cronAssetLookup.GetByDeviceSN:689-708 已用 devicesn 列,line 696 `Where("devicesn = ? AND deleted_at IS NULL", deviceSN)`)。49-01 填充 serial_number 后此关联自然打通 —— **Gap 2 不需要额外代码改动**,只需在 collectComponentInfo 把板卡行(engine/card)合并进 set.Components,并在 DeviceRef.SerialNumber 传 device.SerialNumber(已传,line 650)。

**关键设计决策(BLOCKER-2 修复):chassis 行不进 Pipeline.Run。** Pipeline.Run(pipeline.go:108)只调用 `writer.Write(ctx, device.ID, parentAssetID, set.Components)`,从不消费 `*set.Chassis`(grep 验证 pipeline.go 中 Chassis 出现 0 次)。chassis 资产已存在(parent 本身),无需"写 chassis 行"。本计划采选项 A(更简单):

- 在 collectComponentInfo 中,从 parser 输出的组件列表中**丢弃 chassis 行**(ComponentType==ComponentTypeChassis 的元素不 append),**保持 set.Chassis = nil**。
- 仅板卡行(engine/card)进 set.Components → Pipeline.Run → writer.Write。
- 真机 fixture 中 M1 主控的 SN 等于 chassis SN(`G1HLC0R000096`),但 M1 行的 ComponentType 是 ComponentTypeEngine(cli_ruijie_collector.go:75-81 + cli_ruijie_collector_test.go:43-44 验证),与被丢弃的 chassis 行独立 —— 它仍作为 engine 板卡保留在 set.Components 中,正确显示在「从属组件清单」Tab。
- E2E 验证 SQL 因此使用 `component_type IN ('engine','card')` 计数 ≥6,**无需** `IS NULL 过滤排除 chassis 行`(因为 chassis 行根本不会被写入)。

E2E:部署后等下次 device_info_update cron(每小时)→ SQL 校验 ops_asset WHERE parent_asset_id = (CX-WH-WH-04F-FL-RS8607E-03 的 asset id) AND component_type IN ('engine','card') 返回 ≥6 条。

Purpose: 让板卡/引擎卡组件正确写入 ops_asset 并关联到 chassis 资产。
Output: collectComponentInfo 接入完成 + 单测覆盖 + E2E 验证清单。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-v1-18-gap-closure/49-CONTEXT.md
@.planning/phases/49-v1-18-gap-closure/49-01-SUMMARY.md(由 49-01 执行产出)

<interfaces>
From internal/services/component_collector/cli_ruijie_collector.go(ParseShowVersionModules 已在 49-01 引入,本计划复用同一解析):
```go
// 返回 []Component:
//   - 首个元素 ComponentType="chassis"(System serial number 行)→ 本计划丢弃
//   - 后续元素 Slot="Slot M1"/"Slot 1"/"Slot M2" 等,ComponentType=engine 或 card → 本计划合并进 set.Components
// 真机 fixture: chassis SN = M1 SN = G1HLC0R000096;Slot 1/2/3/4/5 + M2 各自独立 SN
func (r *RuijieCliCollector) ParseShowVersionModules(raw string) ([]Component, error)
```

From internal/services/component_collector/component_set.go:
```go
type ComponentSet struct {
    Chassis *Component `json:"chassis"`         // 本计划保持 nil(Pipeline 不消费它)
    Components []Component `json:"components"`   // 板卡/引擎/光模块(本计划合并)
}
```

From internal/services/device_info_collection_service.go(现状):
```go
// collectComponentInfo (line 616-654)
func (s *DeviceInfoCollectionService) collectComponentInfo(ctx, device) error {
    // line 630: 当前只调光模块 pipeline
    set, err := runTwoStepTransceiverPipeline(ctx, device.Vendor, runner)
    // line 634: 空 set 直接 return nil ←── 板卡组件丢失点
    // line 643-653: 构造 pipeline + DeviceRef + pipeline.Run(ctx, devRef, set)
}

// runDeviceCommand (line 658-678) 已封装 SSH 命令执行,runner := func(cmd) { return s.runDeviceCommand(ctx, device, cmd) }
```

From internal/services/component_collector/pipeline.go(Pipeline.Run line 87-110):
```go
// Step 1: parentAssetID = assetSvc.GetByDeviceSN(device.SerialNumber) → ops_asset.id
//         (cronAssetLookup.GetByDeviceSN 已用 devicesn 列匹配,见 device_info_collection_service.go:689-708)
// Step 2: writer.Write(ctx, device.ID, parentAssetID, set.Components)
//         ← Pipeline.Run 从不引用 set.Chassis(grep 验证)
```

<!-- 板卡命令 -->
From internal/services/component_collector/cmd_dispatcher.go:
```go
// 已有 CmdKindModule 映射:GetCollectorCommands(VendorRuijie, CmdKindModule) == []string{"show version"}
// huawei 无 module 命令(板卡走 ENTITY-MIB SNMP 路径,本环境 deferred,所以 huawei 只有 chassis ESN)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: collectComponentInfo 接入板卡解析器(丢弃 chassis 行),合并到 ComponentSet.Components</name>
  <files>internal/services/device_info_collection_service.go, internal/services/device_info_collection_service_test.go(若不存在则创建,package services)</files>
  <read_first>
    - .planning/phases/49-v1-18-gap-closure/49-CONTEXT.md(§Gap 1 + §Gap 2 + §修复顺序 + §验证目标)
    - .planning/phases/49-v1-18-gap-closure/49-01-SUMMARY.md(由 49-01 执行产出,确认 serial_number 已开始填充)
    - internal/services/device_info_collection_service.go(line 516-654 collectComponentInfo + runTwoStepTransceiverPipeline + runDeviceCommand)
    - internal/services/component_collector/cli_ruijie_collector.go(ParseShowVersionModules line 48-84)
    - internal/services/component_collector/cli_ruijie_collector_test.go(line 14-77,既有测试验证 chassis SN=G1HLC0R000096,M1/M2/1-5 板卡 SN 各异)
    - internal/services/component_collector/cli_huawei_collector.go(ParseDisplayDeviceEsn)
    - internal/services/component_collector/component_set.go(ComponentSet / Component / ComponentTypeChassis / ComponentTypeEngine / ComponentTypeCard 常量)
    - internal/services/component_collector/pipeline.go(Pipeline.Run line 87-110,确认只消费 set.Components 不消费 *set.Chassis)
    - internal/services/component_collector/cmd_dispatcher.go(CmdKindModule + GetCollectorCommands ruijie→show version)
    - templates/samples/ruijie_10_62_63_21_show_version.txt(板卡解析的 7 行输出参考)
  </read_first>
  <behavior>
    - Test 1: collectComponentInfo 用 mock runner(返回 ruijie_10_62_63_21_show_version.txt + 空 transceiver 输出)+ mock DB,断言 Pipeline.Run 被调用时收到的 ComponentSet.Components 包含至少 6 条 ComponentType 为 engine/card 的板卡记录(M1/1-5/M2)
    - Test 2: collectComponentInfo 用 mock runner 返回带 chassis SN 的输出,断言 ComponentSet.Chassis 为 nil(**不写 chassis 行**),且 set.Components 中**不含** ComponentType=="chassis" 的元素(chassis 行已被丢弃)
    - Test 3: 当 transceiver pipeline 返回空 set(无 up SFP),但板卡解析有结果时,**不**直接 return nil(line 634 当前 bug),继续走 Pipeline.Run 写板卡
    - Test 4: huawei vendor 调用 ParseDisplayDeviceEsn(只产 chassis,无板卡),parser 返回的 chassis 行被丢弃 → set.Components 为空 + set.Chassis == nil,行为与"无板卡"一致(沿用 line 634-637 提前 return nil 的语义)
    - Test 5: vendor 不在 huawei/ruijie 列表(H3C/Maipu/unknown)→ 返回 nil,不报错(保留现有 line 620-623 短路,与 v1.18 D-08 ENTITY-MIB deferred 一致)
  </behavior>
  <action>
    修改 collectComponentInfo(line 616-654),在 runTwoStepTransceiverPipeline 之前或之后追加板卡采集分支(注意:**不**填 set.Chassis):

    1. **新增 module 采集分支**(在 runTwoStepTransceiverPipeline 调用之前):
       - ruijie: 用 runner 调 GetCollectorCommands(device.Vendor, CmdKindModule)[0] 即 "show version"(注意:runDeviceCommand 已封装,直接 runner("show version"))。把 raw 喂给 component_collector.NewRuijieCliCollector().ParseShowVersionModules(raw)。遍历返回的 []Component:**ComponentType==ComponentTypeChassis 的元素跳过**(continue,不 append)—— chassis 资产已存在(parent 本身),无需"写 chassis 行";Pipeline.Run(pipeline.go:108)也只消费 set.Components,从不消费 *set.Chassis。**其余**(engine/card)append 到 set.Components。**保留 parser 自带去重逻辑**:真机上 M1 的 SN 等于 chassis SN(`G1HLC0R000096`),但 M1 行的 ComponentType 是 ComponentTypeEngine(cli_ruijie_collector.go:75-81 + cli_ruijie_collector_test.go:43-44 验证),独立于被丢弃的 chassis 行,作为 engine 板卡正常保留。
       - huawei: huawei 无 CmdKindModule 命令(板卡走 ENTITY-MIB SNMP 路径,本环境 deferred),所以 huawei 分支**不**调板卡解析器(无需 ParseDisplayDeviceEsn —— chassis 行无去处,Pipeline 不消费 *set.Chassis;49-01 已处理 chassis SN 写回 sys_network_device)。如果代码结构需要遍历,对 huawei 直接跳过 module 分支。
       - 命令执行 error: 沿用 D-14 容错 —— applogger.Infof 记录后继续(不阻塞 transceiver pipeline)。
       - 解析器返回空切片+nil err(如 huawei 退役命令):静默跳过,继续后续流程。

    2. **修复 line 634-637 的提前 return bug**:
       当前 `if set == nil || len(set.Components) == 0 { return nil }` —— 这个判断**保留**(简化后只需检查 Components,因为 set.Chassis 永远 nil)。实际行为会变:即便 transceiver pipeline 返回空,只要板卡解析有结果,set.Components 非空就会继续走 Pipeline.Run。

    3. **set 的初始化**:把 `set, err := runTwoStepTransceiverPipeline(...)` 改为先 `set := &component_collector.ComponentSet{Components: []component_collector.Component{}}`,然后调 runTwoStepTransceiverPipeline 填充 set(此函数已返回 *ComponentSet,需调整合并:把它的 Components append 进 set.Components;**忽略**它返回的 Chassis 字段,因为本计划保持 set.Chassis == nil)。

    4. **不要修改**:
       - runTwoStepTransceiverPipeline 函数本身(它专注 transceiver 路径,符合 D-10)
       - Pipeline.Run / OpsAssetWriter(已正确处理 set.Components + parent_asset_id;Pipeline.Run 不消费 *set.Chassis)
       - cronAssetLookup.GetByDeviceSN(line 689-708,已用 devicesn 列匹配,Gap 2 自然打通)

    5. **去重保护**:若同设备跨周期重复采集,Pipeline.Run 内 OpsAssetWriter 已按 SN 做 UPSERT 语义(参考 pipeline.go 注释)。本计划不需要额外去重。

    关键实现要点:
    - import component_collector 已存在,无需新增
    - GetCollectorCommands 在 component_collector 包内,直接调用
    - CmdKindModule 已在 cmd_dispatcher.go 导出
    - device.Vendor 字段类型为 models.DeviceVendor,GetCollectorCommands 接受此类型
    - **丢弃 chassis 行**的策略与 Pipeline.Run 的实现一致(pipeline.go:108 只 writer.Write(..., set.Components)),避免 chassis 行被 writer 误写为 component_type='chassis'(那会污染前端过滤)
  </action>
  <verify>
    <automated>cd D:/code/ClaudeCode/xingran-go-backend && go test ./internal/services/ -run TestCollectComponentInfo -v -count=1</automated>
    <automated>cd D:/code/ClaudeCode/xingran-go-backend && go test ./internal/services/component_collector/ -count=1</automated>
    <automated>cd D:/code/ClaudeCode/xingran-go-backend && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: device_info_collection_service.go collectComponentInfo 函数体内 grep 能匹配 `ParseShowVersionModules`
    - SOURCE: 不再有 "already collected by the chassis collector" 这条错误注释(已删除或修正)
    - BEHAVIOR: TestCollectComponentInfo_RuijieBoards mock runner 返回 ruijie_10_62_63_21_show_version.txt 内容,断言 set.Components 包含 ≥6 条 ComponentType 为 ComponentTypeEngine 或 ComponentTypeCard 的元素
    - BEHAVIOR: TestCollectComponentInfo_ChassisRowDropped mock runner 返回带 chassis SN 的 ruijie 输出,断言 set.Components **不含** ComponentType=="chassis" 的元素(被丢弃),set.Chassis == nil
    - BEHAVIOR: TestCollectComponentInfo_NoTransceiverButHasBoards mock runner transceiver 返回空 + 板卡有结果,断言 Pipeline.Run 被调用(不再因 transceiver 空就 return nil)
    - BEHAVIOR: TestCollectComponentInfo_OutOfScopeVendor device.Vendor=VendorH3C,断言直接 return nil(不调 runner)—— 与 v1.18 D-08 ENTITY-MIB deferred 一致,非回归
    - TEST: `go test ./internal/services/ -run TestCollectComponentInfo -count=1` exit 0
    - TEST: `go test ./internal/services/component_collector/ -count=1` exit 0(既有测试不回归)
    - BUILD: `go build ./...` exit 0
  </acceptance_criteria>
  <done>
    - collectComponentInfo 接入 ParseShowVersionModules(ruijie)
    - 板卡组件(engine/card)正确合并到 ComponentSet.Components,chassis 行被丢弃(保持 set.Chassis == nil)
    - 单测覆盖 mock runner 5 种场景(ruijie 板卡 / chassis 行被丢弃 / 无 transceiver 但有板卡 / huawei 无板卡 / 越界 vendor)
    - 既有 collector 测试全绿
    - go build ./... 通过
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 端到端验证(E2E) —— 部署后等 cron 跑完,验证 RS8607E-03 显示 ≥6 条板卡</name>
  <files>(无代码改动,纯验证任务)</files>
  <read_first>
    - .planning/phases/49-v1-18-gap-closure/49-CONTEXT.md(§验证目标 端到端表格)
  </read_first>
  <action>
    本任务无代码改动。动作 = 部署新二进制 + 等下一次 device_info_update cron(最多 1 小时,因为 cron 每小时触发)+ 按 how-to-verify 执行 SQL 与浏览器验证步骤。详见 &lt;how-to-verify&gt;。
  </action>
  <how-to-verify>
    部署新二进制 + 等下一次 device_info_update cron(最多 1 小时)。然后按顺序执行:

    **Step 1 — 后端 chassis SN 已写回(49-01 验证)**:
    ```sql
    -- 期望: 在线 ruijie/huawei 设备 serial_number 不再全空
    SELECT id, device_name, vendor, serial_number
    FROM sys_network_device
    WHERE vendor IN ('ruijie','huawei') AND status=0
    ORDER BY vendor, device_name;
    -- 期望: 大部分行 serial_number 非空(具体数量看真机命令成功率)
    -- RS8607E-03 在真机 fixture 中 chassis SN = G1HLC0R000096;生产 DB 中 devicesn = G1M9140000175
    -- (注意:真机 show version 在不同设备实例上 SN 不同,以你部署环境的实际值为准)
    ```

    **Step 2 — 后端板卡组件已写入 ops_asset(49-02 验证)**:
    ```sql
    -- 找 CX-WH-WH-04F-FL-RS8607E-03 的 asset id(其 devicesn=G1M9140000175)
    SELECT id, devicesn FROM ops_asset WHERE devicesn = 'G1M9140000175' AND deleted_at IS NULL;
    -- 用上面 id 查板卡组件(预期 ≥6 条 engine/card;chassis 行不会被写入,无需 IS NULL 过滤)
    SELECT id, component_type, component_slot, component_serial
    FROM ops_asset
    WHERE parent_asset_id = '<上面 id>'
      AND component_type IN ('engine','card')
      AND deleted_at IS NULL
    ORDER BY component_slot;
    -- 期望: 看到 Slot M1/1/2/3/4/5/M2 共 6-7 条(component_type 为 engine/card)
    -- (chassis 行被 collectComponentInfo 丢弃,不会写入 ops_asset,所以不需要 IS NULL 过滤来排除它)
    ```

    **Step 3 — 前端 Tab 显示**:
    1. 浏览器登录系统(http://localhost:4000 或生产地址)
    2. 导航:资产 → 资产管理(ops/asset)
    3. 在列表中找到 deviceName=CX-WH-WH-04F-FL-RS8607E-03 的资产行(devicesn=G1M9140000175)
    4. 点该行的「展开」箭头 → 展开「从属组件清单」Tab
    5. **预期**: 看到 ≥6 条板卡行(M1/Slot1-5/M2)
    6. 比对 SN 与 CONTEXT.md §验证目标 表格(允许 M1 SN 与 chassis 同 SN,这是 Ruijie 已知行为)

    **Step 4 — 全量影响检查**:
    ```sql
    -- 期望: 全表 component_type IN ('engine','card','transceiver') 行数 > 0(此前为 0)
    SELECT count(*) FROM ops_asset
    WHERE component_type IN ('engine','card','transceiver')
      AND deleted_at IS NULL;
    ```
  </how-to-verify>
  <verify>
    <automated>(无自动化 — 需要部署 + 等 cron + 真机查询/浏览器交互)</automated>
    <human-check>执行 how-to-verify 4 个 Step,将 Step 2 SQL 结果 + Step 3 浏览器所见记录到 SUMMARY</human-check>
  </verify>
  <acceptance_criteria>
    - SQL: `SELECT count(*) FROM sys_network_device WHERE vendor IN ('ruijie','huawei') AND status=0 AND serial_number=''` 显著下降(从 24 → 接近 0,允许个别命令失败)
    - SQL: `SELECT count(*) FROM ops_asset WHERE component_type IN ('engine','card','transceiver') AND deleted_at IS NULL` > 0(此前为 0)
    - SQL: `SELECT count(*) FROM ops_asset WHERE parent_asset_id = '<RS8607E-03 asset id>' AND component_type IN ('engine','card') AND deleted_at IS NULL` >= 6
    - MANUAL: 前端资产 CX-WH-WH-04F-FL-RS8607E-03 展开「从属组件清单」Tab 显示 ≥6 条板卡行
  </acceptance_criteria>
  <resume-signal>
    Type "approved" + 粘贴 Step 2 SQL 查询结果(板卡组件清单),或描述前端 Tab 截图所见。

    **时间上限/升级触发器**:若部署后 90 分钟(cron 60min + 30min buffer)内 Step 2 SQL 仍返回 0 条板卡,记录失败细节并在 SUMMARY 标记 "escalate: device_info_update cron 投递失败",**不要无限等待**。

    若验证失败(90 分钟内有数据但不符合预期):
    - Step 1 失败(serial_number 仍空)→ 检查 49-01 chassis SN 解析器是否被 collectDeviceInfo 调用,看应用日志
    - Step 2 失败(parent_asset_id 全空)→ 检查 Gap 2 关联链:serial_number 是否已填?ops_asset.devicesn 是否匹配?cronAssetLookup 查询是否报错
    - Step 3 失败(Tab 仍空但 SQL 有数据)→ 这是前端 component_type IS NULL 过滤或 endpoint 问题,本 phase 范围外,记录到 SUMMARY 后续处理
  </resume-signal>
  <done>
    - chassis SN 字段在 ruijie/huawei 在线设备上回填
    - ops_asset 出现 component_type IN ('engine','card') 的板卡行(chassis 行未被写入)
    - RS8607E-03 资产展开显示 ≥6 条板卡(M1/1-5/M2)
    - 端到端验证目标达成
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSH 设备 → 采集服务 | 复用现有 deviceExecutor 连接池和凭据,无新认证路径。CLI 输出经解析器处理。 |
| 采集服务 → PostgreSQL | ops_asset UPDATE/INSERT 走 Pipeline.Run → OpsAssetWriter(已有路径,本计划不修改) |
| Cron 触发 → 采集服务 | device_info_update cron(每小时)是现有触发源,本计划不引入新 cron |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-02-01 | Tampering | ops_asset 组件行写入 | mitigate | Pipeline.Run/OpsAssetWriter 按 SN UPSERT 语义,同设备跨周期不会重复 INSERT(参考 pipeline.go);chassis 行被显式丢弃,避免污染 component_type |
| T-49-02-02 | Repudiation | 板卡写入无审计 | accept | operLog 在 NewPipeline 第 3 参可传 nil(D-13 documented,本计划保持 nil,后续 hardening) |
| T-49-02-03 | Denial of Service | SSH 命令执行阻塞 cron | mitigate | runDeviceCommand 走连接池 acquire/release,ParseShowVersionModules 失败 → continue 容错(D-14) |
| T-49-02-04 | Elevation of Privilege | SSH 命令执行 | accept | 复用现有连接池,无新认证 |
| T-49-02-05 | Information Disclosure | component_serial 字段 | accept | 序列号是设备标识非敏感凭据,按 ops_asset 现有访问控制保护 |
</threat_model>

<verification>
- `go test ./internal/services/ -run TestCollectComponentInfo -v -count=1` 全绿
- `go test ./internal/services/component_collector/ -count=1` 全绿(既有测试不回归)
- `go build ./...` exit 0
- 部署后 E2E(checkpoint task):
  - sys_network_device.serial_number 在 ruijie/huawei 在线设备上回填
  - ops_asset 出现 component_type IN ('engine','card') 的板卡行(chassis 行未被写入)
  - RS8607E-03 展开「从属组件清单」显示 ≥6 条板卡
</verification>

<success_criteria>
- [x] collectComponentInfo 接入 ParseShowVersionModules(ruijie)
- [x] set.Chassis 保持 nil(Pipeline.Run 只消费 set.Components;chassis 行被显式丢弃)
- [x] set.Components 合并板卡+transceiver(板卡来自 parser 的 engine/card 行,丢弃 chassis 行)
- [x] Gap 2 关联通过 49-01 填充 serial_number + 既有 cronAssetLookup.GetByDeviceSN(devicesn 列)自然打通
- [x] 单测覆盖 5 种场景(mock runner + 越界 vendor + 无 transceiver 但有板卡 + huawei 无板卡 + chassis 行被丢弃)
- [x] 既有 collector 测试无回归
- [x] go build ./... 通过
- [x] E2E checkpoint 通过:RS8607E-03 显示 ≥6 条板卡(90 分钟时间上限,超时 escalate)
</success_criteria>

<output>
Create `.planning/phases/49-v1-18-gap-closure/49-02-SUMMARY.md` when done
</output>
