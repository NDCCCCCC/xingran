---
phase: 71-governance-baseline-and-ci-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .coverage-threshold
  - .github/scripts/check-coverage.sh
  - .github/workflows/ci.yml
  - .planning/coverage-baseline.md
autonomous: true
requirements:
  - GOV-01
  - GOV-02
  - GOV-04

must_haves:
  truths:
    - ".coverage-threshold file exists at repo root with exact content `12.8\\n`"
    - ".github/scripts/check-coverage.sh exists, is executable, syntax-clean (bash -n passes), and embeds the awk aggregator from 71-RESEARCH.md"
    - ".github/workflows/ci.yml backend job has 4 changes: Test step adds -coverprofile=coverage.out -covermode=atomic -count=1; Coverage HTML step (if: always()); Coverage gate step (invokes check-coverage.sh); Upload artifact step (actions/upload-artifact@v4, retention-days: 30, if: always())"
    - ".planning/coverage-baseline.md exists with starting row (5ead742, 12.8) AND Phase 71 后 row (commit=TBD pending 71-01b amend) AND two 76-line per-package fenced blocks"
    - "ci.yml YAML validates with `python -c \"import yaml; yaml.safe_load(...)\"`"
  artifacts:
    - path: ".coverage-threshold"
      provides: "single-value gate threshold (12.8) read by check-coverage.sh"
      contains: "12.8\\n"
    - path: ".github/scripts/check-coverage.sh"
      provides: "bash + awk weighted-average gate (exit 1 if avg < threshold)"
      exports: ["exit 0 (pass)", "exit 1 (gate fail)", "exit 2 (usage)"]
    - path: ".github/workflows/ci.yml"
      provides: "backend job with -coverprofile + Coverage HTML + Coverage gate + Upload artifact steps"
      contains: "actions/upload-artifact@v4"
    - path: ".planning/coverage-baseline.md"
      provides: "starting + Phase 71 后 snapshot rows with per-package detail blocks; commit column = TBD pending 71-01b amend"
      contains: "## Phase 71 后"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: ".coverage-threshold"
      via: "bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold"
      pattern: "check-coverage.sh coverage.out .coverage-threshold"
    - from: ".github/scripts/check-coverage.sh"
      to: ".coverage-threshold"
      via: "THRESHOLD=$(cat .coverage-threshold)"
      pattern: "cat .coverage-threshold"
    - from: ".github/workflows/ci.yml"
      to: "actions/upload-artifact@v4"
      via: "Upload coverage artifact step + if: always()"
      pattern: "actions/upload-artifact@v4"
---

<objective>
Phase 71 plan 01 lands the engineering-infrastructure half of the backend CI coverage gate: create `.coverage-threshold`, `.github/scripts/check-coverage.sh`, extend `.github/workflows/ci.yml` with the four coverage steps, and scaffold `.planning/coverage-baseline.md` with both starting + Phase 71 后 rows (commit column = TBD pending plan 71-01b's amend).

Purpose: This plan stops at "files exist, YAML validates, scripts are syntax-clean, schemas are correct" — it does NOT push, does NOT verify tests, does NOT run `gh run watch`. Plan 71-01b handles all of that after this plan commits.

D-01..D-04 honored: bash + awk (zero deps), `actions/upload-artifact@v4` with 30-day retention, separate `.planning/coverage-baseline.md` file, manual ratchet. Phase 71 ships zero business changes.

Output: 4 files (3 NEW + 1 MODIFY) ready for plan 71-01b to verify and push.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-governance-baseline-and-ci-gate/71-CONTEXT.md
@.planning/phases/71-governance-baseline-and-ci-gate/71-RESEARCH.md
@.planning/phases/71-governance-baseline-and-ci-gate/71-VALIDATION.md
@.planning/phases/71-governance-baseline-and-ci-gate/71-PATTERNS.md
@.github/workflows/ci.yml
@.github/workflows/deploy.yml
@scripts/check-status-literals.sh
@.planning/quick/260820-backend-test-coverage-scan/per-package-coverage.txt
@.planning/quick/260820-backend-test-coverage-scan/SUMMARY.md
</context>

<interfaces>
<!-- Key contracts/values the executor must use verbatim. -->

From `.planning/phases/71-governance-baseline-and-ci-gate/71-RESEARCH.md` (Awk Aggregation Formula Validation, lines 97-117):

The awk aggregator that `check-coverage.sh` must embed. It reads `coverage.out`, peels off the `file:line.col,line.col` prefix via `split($1, parts, ":")`, walks path segments via `split(parts[1], seg, "/")`, excludes paths starting with `scripts/`, `tests/scripts/`, `node_modules/`, aggregates per-package stmts and covered into associative arrays, prints per-package rows via `printf "%-50s %8d %8d %6.2f%%\n"`, and emits a trailing `PACKAGE %8d %8d %6.2f%%\n` summary line.

From `.planning/phases/71-governance-baseline-and-ci-gate/71-RESEARCH.md` (CI Workflow Changes, lines 159-189):

The four-step backend job extension pattern. Test step adds `-coverprofile=coverage.out -covermode=atomic -count=1` (no `-race` per D-01 Claude's Discretion); Coverage HTML uses `go tool cover -html=coverage.out -o coverage.html` with `if: always()`; Coverage gate invokes `bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold`; Upload uses `actions/upload-artifact@v4` with `retention-days: 30` and `if: always()`. Step order invariant: Test → Coverage HTML → Coverage gate → Upload.

From `.planning/phases/71-governance-baseline-and-ci-gate/71-RESEARCH.md` (Coverage-baseline.md Schema, lines 234-291):

`coverage-baseline.md` is a Markdown file with a snapshot table per phase (columns: date / phase_label / weighted_avg / total_stmts / total_covered / 0pct_pkg_count / commit / phase_executor / ratchet_from / ratchet_to) plus a per-package fenced block containing the 76-line table from `per-package-coverage.txt`.

From `scripts/check-status-literals.sh` lines 30-126:

The bash ratchet pattern `check-coverage.sh` must mirror: `#!/usr/bin/env bash` shebang, docstring header, `set -euo pipefail`, `ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"` path anchor, `collect`/`check`/`case "${1:-}"` structure, exit-code convention (`0` pass, `1` fail, `2` usage).

From `.planning/quick/260820-backend-test-coverage-scan/per-package-coverage.txt` (76 lines):

The exact per-package text to inline into `coverage-baseline.md` starting + Phase 71 后 sections. Lines 1-74 list per-package stmts/covered/percent; lines 75-76 are the trailing `PACKAGE` summary + dashes.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write .coverage-threshold file (12.8 baseline)</name>
  <files>.coverage-threshold</files>
  <read_first>
    - .planning/phases/71-governance-baseline-and-ci-gate/71-CONTEXT.md (D-01 + Claude's Discretion — pure number, single line, LF-terminated)
    - .planning/phases/71-governance-baseline-and-ci-gate/71-RESEARCH.md (Threshold File Format section, lines 205-225 — confirms 12.8 not 12.8%, no comments, no JSON/YAML, one decimal precision)
    - .planning/quick/260820-backend-test-coverage-scan/SUMMARY.md (line 18 — confirms 12.8% is the canonical baseline number)
  </read_first>
  <action>
    Create the file `.coverage-threshold` at the repo root (sibling of `go.mod`, `.gitignore`, `.env`). The file's sole content is the literal string `12.8` followed by a single LF newline byte. Do NOT include a trailing percent sign (12.8% is wrong), do NOT include any comment lines, do NOT wrap in JSON or YAML, do NOT include a UTF-8 BOM. The file must be readable by `cat .coverage-threshold | tr -d '\n'` producing exactly the string `12.8`. Place it with exactly the bytes: ASCII 1, 2, ., 8, newline — five bytes total.

    Rationale (do not encode in the file): per D-01, this file is the single source of truth for the gate threshold; future phase ratchets (Phase 72/73/74) only edit this file (never `check-coverage.sh` or `ci.yml`).
  </action>
  <acceptance_criteria>
    - `cat .coverage-threshold` outputs `12.8` (no trailing newline visible, byte 5 is LF confirmed via `wc -c .coverage-threshold` = 5)
    - `xxd .coverage-threshold` shows bytes `31 32 2e 38 0a` (ASCII 1, 2, ., 8, LF) — no CR, no BOM
    - `grep -c '.' .coverage-threshold` = 1 (single line, no comments)
    - `file .coverage-threshold` does NOT report "with BOM" or "UTF-8 Unicode (with BOM) text"
  </acceptance_criteria>
  <verify>
    <automated>test -f .coverage-threshold && cat .coverage-threshold && echo "---" && wc -c .coverage-threshold && echo "---" && xxd .coverage-threshold</automated>
  </verify>
  <done>
    File `.coverage-threshold` exists at the repo root, contains exactly five bytes (31 32 2e 38 0a), is readable as `12.8` by `cat`, and will be parsed cleanly by `$(cat .coverage-threshold)` inside `check-coverage.sh`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write .github/scripts/check-coverage.sh with awk aggregator + threshold compare</name>
  <files>.github/scripts/check-coverage.sh</files>
  <read_first>
    - .planning/phases/71-governance-baseline-and-ci-gate/71-PATTERNS.md (lines 38-148 — the structural pattern to mirror from check-status-literals.sh, plus the verbatim awk block from RESEARCH.md lines 97-117)
    - .planning/phases/71-governance-baseline-and-ci-gate/71-RESEARCH.md (Awk Aggregation Formula Validation section lines 92-148 — the canonical awk script that must be embedded in this file; Threshold File Format lines 205-225 — confirms THRESHOLD=$(cat .coverage-threshold) read pattern)
    - .planning/quick/260820-backend-test-coverage-scan/SUMMARY.md (lines 30-47 — original awk formula to preserve identical semantics)
    - scripts/check-status-literals.sh (lines 1-126 — shebang, docstring header, set -euo pipefail, ROOT anchor, collect/check/case structure, exit-code convention 0/1/2)
  </read_first>
  <action>
    Create `.github/scripts/check-coverage.sh` as an executable bash script. The script structure:

    1. Shebang `#!/usr/bin/env bash` + a docstring header (model on `check-status-literals.sh` lines 1-29) explaining Phase 71 purpose, D-01 origin (bash + awk, zero deps), exclusions (`scripts/`, `tests/scripts/`, `node_modules/`), threshold file location (`.coverage-threshold`), CI hookup (the `bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold` invocation from ci.yml), and exact exit-code semantics.

    2. `set -euo pipefail` (mirror line 30 of check-status-literals.sh).

    3. `ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"` — note the `../..` because the script lives under `.github/scripts/`, so `ROOT` must climb two directories to reach the repo root.

    4. Argument parsing: `PROFILE="${1:-}"`, `THRESHOLD_FILE="${2:-}"`. If either is empty, print usage to stderr and `exit 2`.

    5. Threshold read: `THRESHOLD="$(cat "$THRESHOLD_FILE")"` — fail-safe if file missing (the `set -e` will trip on `cat` of a missing file).

    6. Existence check: `if [ ! -f "$PROFILE" ]; then echo "coverage profile $PROFILE missing — was Test step skipped?" >&2; exit 0; fi` — gate stays pass when no profile exists, so HTML step / Upload step still run via `if: always()`.

    7. The awk aggregator embedded via heredoc: copy the formula verbatim from RESEARCH.md lines 97-117 — `NR > 1` parsing the file:line prefix via `split($1, parts, ":")` then `split(parts[1], seg, "/")` and joining `seg[4..n-1]` with `/`; excluding `scripts/`, `tests/scripts/`, `node_modules/`; aggregating per-package stmts and covered into two associative arrays; END block iterating all packages with `printf "%-50s %8d %8d %6.2f%%\n"` (matching per-package-coverage.txt column widths) and a trailing `printf "PACKAGE %8d %8d %6.2f%%\n"` line.

    8. Threshold comparison: invoke `awk` with `-v threshold="$THRESHOLD"` and append a post-END check that captures the PACKAGE line, parses the trailing `pct`, and prints `PASS` or `FAIL: weighted avg X.X% < threshold Y.Y%` to stderr; exit code `0` on PASS, `1` on FAIL. The check must coerce both sides to float with `+ 0` (avoids BWK awk integer division).

    9. On FAIL, also dump the per-package table to stdout (already produced by the aggregator above) so CI logs show which packages are dragging the weighted average.

    10. `chmod +x .github/scripts/check-coverage.sh` after writing.

    The script must be a single file under ~120 lines. No third-party dependencies. Plan 71-01b's local smoke test will verify the 12.8% baseline against `quick-260820-bcs` per-package-coverage.txt on the local coverage profile.
  </action>
  <acceptance_criteria>
    - `bash -n .github/scripts/check-coverage.sh` exits 0 (syntax clean)
    - `bash .github/scripts/check-coverage.sh` with no args exits 2 (usage error)
    - `bash .github/scripts/check-coverage.sh /nonexistent .coverage-threshold` exits 0 (no profile = skip, don't fail — must coexist with `if: always()` on downstream steps)
    - File mode bits include execute: `ls -l .github/scripts/check-coverage.sh` shows `-rwxr-xr-x`
    - First line is `#!/usr/bin/env bash` and second line begins with `# check-coverage.sh`
    - Script length is under 150 lines (`wc -l .github/scripts/check-coverage.sh` ≤ 150)
  </acceptance_criteria>
  <verify>
    <automated>ls -l .github/scripts/check-coverage.sh && head -10 .github/scripts/check-coverage.sh && echo "--- syntax check ---" && bash -n .github/scripts/check-coverage.sh && echo OK && echo "--- line count ---" && wc -l .github/scripts/check-coverage.sh && echo "--- usage test ---" && (bash .github/scripts/check-coverage.sh 2>&1 || echo "exit=$?")</automated>
  </verify>
  <done>
    `.github/scripts/check-coverage.sh` exists, is executable, has a docstring header, uses `set -euo pipefail`, embeds the awk aggregator from RESEARCH.md, reads `.coverage-threshold` via `cat`, prints per-package rows in `per-package-coverage.txt` column format, and exits 0 (pass) / 1 (fail) / 2 (usage). Behaviour validated end-to-end in plan 71-01b's local smoke test.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend .github/workflows/ci.yml backend job with coverage steps</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (current state — Test step at lines 50-57 to be modified; do not modify Lint step at lines 41-48, concurrency at lines 19-21, permissions at line 23, or frontend job at lines 59-108)
    - .planning/phases/71-governance-baseline-and-ci-gate/71-RESEARCH.md (CI Workflow Changes section lines 150-201 — exact step definitions + if: always() rationale + step-order invariant; Gotchas #9 lines 418-423 — coverage.out existence check before HTML step)
    - .github/workflows/deploy.yml (lines 138-152 — upload-artifact@v7 precedent in this repo; D-02 mandates @v4 not @v7 for coverage)
  </read_first>
  <action>
    Extend `.github/workflows/ci.yml` by replacing the existing `Test` step (lines 50-57) and appending three new steps. The complete diff to apply:

    1. Replace the current `Test` step (lines 50-57, which contains `run: go test -timeout 15m -count=1 ./internal/... ./pkg/... ./cmd/...`) with a multiline `run:` that adds `-coverprofile=coverage.out -covermode=atomic` while keeping `-timeout 15m -count=1` and the same package list. Add a comment line above it: `# Phase 71 GOV-01: -coverprofile + -covermode=atomic (count=1 already present)`.

    2. Immediately after the modified Test step, append a new step:
       - name: Coverage HTML report
       if: always()
       run: go tool cover -html=coverage.out -o coverage.html
       with a comment above explaining `if: always()` keeps this step running even when the gate fails.

    3. Append a new step:
       - name: Coverage gate
       run: bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold
       with a comment above: `# GOV-02: weighted-average gate (D-01)`. No `if:` field — this step's failure must propagate.

    4. Append the final new step:
       - name: Upload coverage artifact
       if: always()
       uses: actions/upload-artifact@v4
       with:
         name: backend-coverage
         path: |
           coverage.out
           coverage.html
         retention-days: 30
       with a comment above explaining `actions/upload-artifact@v4` + 30-day retention (D-02).

    DO NOT modify: the concurrency group (lines 19-21), the `permissions: contents: read` block (line 23), the Lint step (lines 41-48), or the entire frontend job (lines 59-108). DO NOT add `-race` to the Test command (D-01 Claude's Discretion forbids it — would 5-10x test runtime).

    Validate the resulting YAML with `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` and confirm 0 exit. The expected diff stat is exactly 4 step blocks: 1 modified (Test) + 3 added (HTML / gate / Upload).
  </action>
  <acceptance_criteria>
    - `git diff .github/workflows/ci.yml` shows 4 hunks: Test step flags change, Coverage HTML step addition, Coverage gate step addition, Upload artifact step addition
    - The new Test step `run:` value contains the substring `-coverprofile=coverage.out -covermode=atomic` (D-01 mandatory flags)
    - Coverage HTML step has `if: always()` (D-02 / Gotcha #9)
    - Coverage gate step invokes `bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold` (D-01 contract)
    - Upload step uses `actions/upload-artifact@v4` (D-02 mandates v4 not v7) with `retention-days: 30` and `if: always()`
    - YAML syntax valid: `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0
    - Lint step (lines 41-48) untouched: `git diff .github/workflows/ci.yml | grep -E "golangci/golangci-lint-action"` shows no change lines
    - Concurrency group untouched: `git diff .github/workflows/ci.yml | grep -E "concurrency|cancel-in-progress"` shows no change lines
    - Frontend job untouched: `git diff .github/workflows/ci.yml | grep -E "frontend:"` shows no change lines (the only frontend touch would be from a wrongly placed hunk)
  </acceptance_criteria>
  <verify>
    <automated>git diff .github/workflows/ci.yml | head -60 && echo "--- YAML validity ---" && python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo "OK yaml" && echo "--- upload-artifact version ---" && grep -E "actions/upload-artifact@v[0-9]+" .github/workflows/ci.yml && echo "--- coverage.out / coverage.html references ---" && grep -E "coverage\.(out|html)" .github/workflows/ci.yml</automated>
  </verify>
  <done>
    `.github/workflows/ci.yml` backend job contains exactly 4 changes: Test step adds `-coverprofile=coverage.out -covermode=atomic -count=1` flags; Coverage HTML step (`if: always()`); Coverage gate step (invokes `check-coverage.sh`); Upload artifact step (`actions/upload-artifact@v4`, `retention-days: 30`, `if: always()`). Lint step, concurrency, permissions, frontend job remain untouched. YAML is valid. Ready for plan 71-01b's CI verification.
  </done>
</task>

<task type="auto">
  <name>Task 4: Write .planning/coverage-baseline.md with starting + Phase 71 after snapshot rows + per-package detail blocks</name>
  <files>.planning/coverage-baseline.md</files>
  <read_first>
    - .planning/phases/71-governance-baseline-and-ci-gate/71-RESEARCH.md (Coverage-baseline.md Schema section lines 234-291 — column schema + snapshot row format + per-package block format + regression checklist pattern)
    - .planning/phases/71-governance-baseline-and-ci-gate/71-PATTERNS.md (lines 215-266 — .coverage-baseline.md doc + Markdown tracking table pattern + per-package inline fenced block pattern)
    - .planning/quick/260820-backend-test-coverage-scan/per-package-coverage.txt (76 lines — the exact text to inline into both the starting + Phase 71 后 per-package blocks)
    - .planning/quick/260820-backend-test-coverage-scan/SUMMARY.md (line 18 — 12.8% baseline; line 19 — 33 zero-coverage packages)
  </read_first>
  <action>
    Create the file `.planning/coverage-baseline.md` at `.planning/coverage-baseline.md` (one directory above the existing quick scan SUMMARY.md, NOT inside it — per D-03 "新建独立文件，不污染 quick scan SUMMARY.md"). No frontmatter YAML block (unlike quick tasks, this file is a phase artifact, not a quick task report).

    The file structure:

    1. H1 header — `# Coverage Baseline: v1.26 后端测试覆盖率优秀`.

    2. Provenance block (3 lines, mirrors SUMMARY.md TL;DR):
       - `**起点来源:** \`.planning/quick/260820-backend-test-coverage-scan/SUMMARY.md\` (2026-08-20 纯只读扫描, 不可回填)`
       - `**测量口径:** 74 业务包加权平均, 排除 scripts/migrations/cmd main/internal/docs (43652 stmts 口径)`
       - `**生成方式:** \`bash .github/scripts/check-coverage.sh coverage.out\` (本地 + CI 同公式)`

    3. Horizontal rule, then `## 起点 (v1.26 启动前)` heading.

    4. Starting snapshot table with the column schema from RESEARCH.md lines 242-246: `date | phase_label | weighted_avg | total_stmts | total_covered | 0pct_pkg_count | commit | phase_executor | ratchet_from | ratchet_to`. Single row: `| 2026-08-20 | 起点 | 12.8 | 43652 | 5589 | 33 | 5ead742 | n/a | n/a | n/a |`.

    5. Per-package (起点) subsection: a Markdown heading `### Per-package (起点)` followed by a fenced code block containing the verbatim 76 lines from `.planning/quick/260820-backend-test-coverage-scan/per-package-coverage.txt`. Do NOT reformat columns; copy the lines byte-for-byte (column widths already match the awk output format per `printf "%-50s %8d %8d %6.2f%%\n"`).

    6. Horizontal rule, then `## Phase 71 后` heading.

    7. Phase 71 后 snapshot table with the same column schema. Single row: `| 2026-08-20 | Phase 71 后 | 12.8 | 43652 | 5589 | 33 | TBD | gsd-execute-phase 71 | n/a | n/a |`. The `weighted_avg`, `total_stmts`, `total_covered`, `0pct_pkg_count` are populated with the same numbers as the starting row (Phase 71 makes no business changes, so they must match within rounding). The `commit` field is `TBD` because it must be the SHA of the commit plan 71-01b will create; plan 71-01b's Task 4 (commit+push+amend) will replace `TBD` with the actual short SHA via Edit + amend.

    8. Per-package (Phase 71 后) subsection: a Markdown heading `### Per-package (Phase 71 后)` followed by a fenced code block containing the SAME 76 lines from `.planning/quick/260820-backend-test-coverage-scan/per-package-coverage.txt` (Phase 71 doesn't add or remove packages, and the per-package breakdown is invariant since no business code changed — only CI infra was added). Inline the same 76-line block as the starting section.

    9. Per-package 倒退检查 (本行) subsection: a Markdown checklist mirroring the pattern from RESEARCH.md lines 287-291:
       - `- [x] 无新增 0% 包 (起点 33 → Phase 71 后 33)`
       - `- [x] 无 per-package 倒退 (Phase 71 不改业务, 不改测试)`

    10. Ratchet note (per D-04) at the end: a Markdown blockquote explaining that this Phase 71 后 snapshot's `commit` field is `TBD` until plan 71-01b Task 4 amends the commit; subsequent phases (72/73/74) will append new sections following the same schema.
  </action>
  <acceptance_criteria>
    - File `.planning/coverage-baseline.md` exists with size between 4KB and 12KB (76-line block × 2 ≈ 4.5KB + headers + tables ≈ 6KB total)
    - `cat .planning/coverage-baseline.md` contains the literal section heading `## 起点 (v1.26 启动前)` AND `## Phase 71 后`
    - Contains exactly two `### Per-package` subsections (起点 + Phase 71 后), each followed by a fenced code block
    - The starting per-package fenced block contains the line `pkg/normalize` AND `cmd/agent` AND the trailing `PACKAGE` summary line — verifiable via grep
    - The Phase 71 后 row in the snapshot table has `phase_label = Phase 71 后`, `commit = TBD` (will be amended in plan 71-01b Task 4)
    - The `weighted_avg` value in the Phase 71 后 row equals `12.8` (same as starting row — Phase 71 makes no business changes)
    - The `total_stmts` value in the Phase 71 后 row equals `43652`; `total_covered` equals `5589`; `0pct_pkg_count` equals `33`
    - File does NOT contain a YAML frontmatter block (unlike quick tasks)
    - `quick/260820-backend-test-coverage-scan/SUMMARY.md` is UNCHANGED — `git diff .planning/quick/260820-backend-test-coverage-scan/SUMMARY.md` returns empty
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/coverage-baseline.md && wc -c .planning/coverage-baseline.md && grep -c "^## " .planning/coverage-baseline.md && echo "--- sections ---" && grep "^##" .planning/coverage-baseline.md && echo "--- quick scan untouched ---" && git diff --stat .planning/quick/260820-backend-test-coverage-scan/SUMMARY.md && echo "--- TBD marker present ---" && grep -c "TBD" .planning/coverage-baseline.md</automated>
  </verify>
  <done>
    `.planning/coverage-baseline.md` exists at `.planning/coverage-baseline.md` with the starting + Phase 71 后 snapshot tables, two 76-line per-package detail blocks (inline fenced), regression checklist, and ratchet note. The `commit` column in the Phase 71 后 row holds `TBD` pending plan 71-01b Task 4 amend. `quick/260820-backend-test-coverage-scan/SUMMARY.md` is untouched. Phase 71 GOV-04 artefact is in place.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR author → CI runner | Untrusted PR code runs `go test` and produces `coverage.out`; the gate decides pass/fail (decision enforced by plan 71-01b's CI run) |
| Local executor → `.coverage-threshold` | The threshold file is edited by humans (D-04); integrity enforced by git history + PR review |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01 | Tampering | `.coverage-threshold` | mitigate | Single source of truth at repo root, all edits visible as PR diffs (D-04); manual ratchet prevents silent threshold changes; commit history makes every bump auditable |
| T-71-02 | Tampering | `check-coverage.sh` | mitigate | Script lives under `.github/scripts/` (only maintainers edit); awk formula is identical to quick-260820-bcs (verified 12.8%); plan 71-01b's local smoke test confirms exit 0/1 behaviour before push |
| T-71-03 | Information Disclosure | `backend-coverage` artifact (coverage.out + coverage.html) | accept | Coverage profile is non-sensitive (test instrumentation, no business secrets); 30-day retention is a CI cost decision not a privacy one |
| T-71-04 | Elevation of Privilege | Coverage gate fail → `if: always()` artifact upload | accept | Fail-state artifact upload is intentional for developer debugging; gate fail cannot bypass security checks (lint + test still gate deploy via workflow_run) |
| T-71-05 | Denial of Service | CI resource exhaustion via large coverage artifacts | mitigate | `actions/upload-artifact@v4` has a 1GB per-repo cap; estimated 150 artifacts × 8MB = 1.2GB approaching cap; RESEARCH §Gotchas #11 flags this for Phase 74+ re-evaluation |
| T-71-06 | Repudiation | Phase 71 threshold changes | mitigate | Every `.coverage-threshold` edit is a separate git commit; git log + commit message includes ratchet before/after numbers (per D-04 + 71-RESEARCH §Ratchet Workflow) |
| T-71-SC | Tampering | npm/pip/cargo installs | n/a | Phase 71 is engineering infrastructure, not package installation — no new dependencies introduced (D-01 zero-dep mandate) |

## Notes

- This threat model is intentionally thin because Phase 71 is engineering infrastructure (CI shell + workflow YAML), not business code. The "security boundary" is PR-author → CI-runner, and the existing `permissions: contents: read` block + step-level `actions/upload-artifact@v4` are the primary controls.
- No new network exposure introduced (gate runs entirely in-process on the CI runner).
- No new data exposure — coverage profile is generated from existing test code and business code that's already in the repo.
</threat_model>

<verification>
[Plan 71-01 checks — file creation/modification only]

- [ ] `.coverage-threshold` exists at repo root, exactly 5 bytes (verified by `wc -c` and `xxd`)
- [ ] `.github/scripts/check-coverage.sh` exists, is executable (`-rwxr-xr-x`), bash syntax-clean (`bash -n`)
- [ ] `.github/workflows/ci.yml` YAML valid (`python -c "import yaml; yaml.safe_load(...)"`)
- [ ] `ci.yml` backend job has 4 changes in correct order: Lint → Test (with -coverprofile) → Coverage HTML → Coverage gate → Upload artifact
- [ ] `actions/upload-artifact@v4` literal appears with `retention-days: 30` and `if: always()`
- [ ] `.planning/coverage-baseline.md` exists with two snapshot rows + two 76-row per-package detail blocks + TBD marker
- [ ] No business code modified — `git diff --stat` shows only the 4 expected files in working tree
- [ ] Plan 71-01b exists in the same phase directory and lists plan 71-01 as `depends_on`
</verification>

<success_criteria>
[Plan 71-01 measurable completion — partial path to ROADMAP §Phase 71 §Success Criteria]

- [ ] **Plan 71-01 SC-A**: `.coverage-threshold` exists with exact 5-byte content `31 32 2e 38 0a`
- [ ] **Plan 71-01 SC-B**: `.github/scripts/check-coverage.sh` exists, executable, syntax-clean, docstring header + awk aggregator + threshold compare
- [ ] **Plan 71-01 SC-C**: `.github/workflows/ci.yml` extended with 4 step changes in correct order, YAML valid, lint/concurrency/frontend untouched
- [ ] **Plan 71-01 SC-D**: `.planning/coverage-baseline.md` exists with starting + Phase 71 后 rows + two 76-row per-package blocks + TBD marker; quick scan SUMMARY.md unchanged

**Plan 71-01 covers (4 files created/modified):**
- GOV-01 (CI -coverprofile + profile artifact): Task 3 + plan 71-01b's CI verification
- GOV-02 (CI threshold gate + ratchet): Tasks 1 + 2 + 3 + plan 71-01b's smoke test + CI run
- GOV-04 (Baseline snapshot): Task 4 + plan 71-01b's amend commit

**Decisions honored in this plan:**
- D-01 (bash + awk + `.coverage-threshold`): Tasks 1 + 2
- D-02 (actions/upload-artifact@v4 + 30 days): Task 3
- D-03 (`.planning/coverage-baseline.md` separate from quick scan): Task 4
- D-04 (manual ratchet): plan-level (Phase 72/73/74 execute plans will bump)

This plan is the file-creation half of Phase 71. Plan 71-01b handles verification + commit + push + amend.
</success_criteria>

<output>
Create `.planning/phases/71-governance-baseline-and-ci-gate/71-01-SUMMARY.md` when done
</output>
