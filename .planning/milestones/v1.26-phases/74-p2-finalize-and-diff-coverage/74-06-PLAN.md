---
phase: 74-p2-finalize-and-diff-coverage
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - internal/services/vdi/*_test.go
autonomous: true
requirements:
  - SCALE-01
user_setup: []

must_haves:
  truths:
    - "internal/services/vdi package coverage ≥70% (1127 stmts baseline)"
    - "VDI server + VM + base + group sub-services tested"
    - "All *_test.go files are test-only (D-12)"
    - "Documented quirks from Phase 73-02 (T-NORESP ensureVDIServer response.Error int→400) captured in SUMMARY"
  artifacts:
    - path: "internal/services/vdi/vdi_server_service_test.go"
      provides: "VDI server service tests"
      min_lines: 400
    - path: "internal/services/vdi/vm_service_test.go"
      provides: "VM service tests"
      min_lines: 300
    - path: "internal/services/vdi/base_service_test.go"
      provides: "Base/connection service tests"
      min_lines: 300
  key_links:
    - from: "internal/services/vdi/vdi_server_service_test.go"
      to: "internal/services/vdi/vdi_server_service.go"
      via: "go test invokes service methods via glebarez sqlite in-memory"
      pattern: "gorm.Open.*sqlite"
---

<objective>
Add comprehensive service tests for internal/services/vdi package (1127 stmts, currently 2.7%). Push per-package coverage to ≥70% per Phase 74 D-15 P2 floor. Addresses SCALE-01 - vdi is the 2nd-largest P2 service package.

Purpose: VDI services (Phase 22A/22B / v1.12 SHIPPED 2026-06-02) were deferred from Phase 73. Closing them lifts weighted average (estimated +755 covered stmts).

Output: ~4-5 *_service_test.go files; per-file ≥75%; D-12 honored.

NOTE: vdi_server ensureVDIServer response.Error int→400 default is a documented Phase 73-02 quirk (T-NORESP) - DO NOT fix per D-12, document in SUMMARY.
</objective>

<execution_context>
execute@$HOME/.claude/get-shit-done/workflows/execute-plan.md
execute@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
context@.planning/phases/74-p2-finalize-and-diff-coverage/74-CONTEXT.md
context@.planning/phases/73-p1-pending/73-04-SUMMARY.md (mixed DB + mock pattern)
context@.planning/phases/73-p1-pending/73-02-SUMMARY.md (VDI handler test pattern - T-NORESP quirk reference)
context@.planning/REQUIREMENTS.md (SCALE-01)
context@.planning/coverage-baseline.md (Phase 73 row - services/vdi at 2.7%)

# Source of truth
Read these files first:
- internal/services/vdi/ (list all .go files)
- internal/models/vdi/ (VDI domain models)
- .planning/phases/73-p1-pending/73-02-SUMMARY.md (VDI quirks)
- .planning/phases/73-p1-pending/73-04-SUMMARY.md (service test pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate VDI sub-services + design test strategy</name>
  <files>internal/services/vdi/*_service_test.go (4-5 new)</files>
  <read_first>internal/services/vdi/ (glob all .go files), .planning/phases/73-p1-pending/73-04-SUMMARY.md (service test pattern)</read_first>
  <action>Enumerate vdi sub-services. Expected: vdi_server, vm, base, group, desktop. Verify count. 1. For each: identify exported methods, DB models (sys_vdi_*), external deps. 2. Test strategy per Phase 73-04 mixed approach. 3. File split: 4-5 *_service_test.go files. 4. Per-file target ≥75%.</action>
  <verify>ls internal/services/vdi/ | head -20</verify>
  <done>Test strategy: 4-5 test files mapped to sub-services; mock strategy documented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement vdi_server + vm service tests (2 files)</name>
  <files>internal/services/vdi/vdi_server_service_test.go, internal/services/vdi/vm_service_test.go</files>
  <read_first>internal/services/vdi/vdi_server_service.go, vm_service.go, .planning/phases/73-p1-pending/73-04-SUMMARY.md (mixed DB+mock)</read_first>
  <action>Implement 2 largest sub-service tests. vdi_server: includes ensureVDIServer path - capture T-NORESP quirk (response.Error int→400 default) in test assertion, DO NOT fix per D-12. vm: VM lifecycle paths with mock VDI client. Each ≥300 lines, ≥10 tests, ≥75% per-file. Real glebarez sqlite + mock external. D-12 honored.</action>
  <verify>go test -cover -count=1 ./internal/services/vdi/ 2>&1 | tail -5</verify>
  <acceptance_criteria>2 test files passing, ≥75% per-file, T-NORESP quirk documented in test assertions, git diff shows only test files (D-12).</acceptance_criteria>
  <done>vdi_server + vm test files complete; per-file coverage ≥75%; T-NORESP quirk captured.</done>
</task>

<task type="auto">
  <name>Task 3: Implement base + group + desktop service tests + verify coverage</name>
  <files>internal/services/vdi/base_service_test.go, internal/services/vdi/group_service_test.go, internal/services/vdi/desktop_service_test.go (if exists)</files>
  <read_first>internal/services/vdi/base_service.go, group_service.go, desktop_service.go (verify), .planning/phases/73-p1-pending/73-04-SUMMARY.md</read_first>
  <action>Implement 3 remaining sub-service tests. base: connection/auth paths. group: VDI resource group paths. desktop: desktop session paths. Each ≥200 lines, ≥8 tests, ≥75% per-file. Per-package target ≥70%. If <70%, identify uncovered methods and add tests.</action>
  <verify>go test -cover -count=1 ./internal/services/vdi/ 2>&1 | tail -5</verify>
  <acceptance_criteria>3 test files passing, ≥75% per-file, per-package vdi ≥70% per D-15, git diff shows only test files (D-12).</acceptance_criteria>
  <done>All vdi test files complete; per-package vdi ≥70%.</done>
</task>

<task type="auto">
  <name>Task 4: Atomic commit + SUMMARY</name>
  <files>.planning/phases/74-p2-finalize-and-diff-coverage/74-06-SUMMARY.md</files>
  <read_first>.planning/phases/72-p0-core-supplement/72-13-PLAN.md (atomic commit pattern), .planning/phases/73-p1-pending/73-02-SUMMARY.md (T-NORESP quirk reference)</read_first>
  <action>1. Stage all test files. 2. Verify git diff --cached --stat shows only *_test.go. 3. Commit: git commit -m 'test(74-06): add vdi service tests (X files, 2.7%→70%, D-12 honored, T-NORESP quirk documented)'. 4. Do NOT push. 5. Capture final coverage. 6. Write 74-06-SUMMARY.md with T-NORESP documentation.</action>
  <verify>git log --oneline -2 && go test -cover -count=1 ./internal/services/vdi/ 2>&1 | tail -3</verify>
  <acceptance_criteria>Atomic commit with all test files, per-package vdi ≥70% per D-15, T-NORESP quirk documented, 74-06-SUMMARY.md complete, D-12 honored.</acceptance_criteria>
  <done>74-06 atomic commit complete; per-package vdi ≥70%; SUMMARY written; T-NORESP locked.</done>
</task>

</tasks>

<verification>go test ./internal/services/vdi/ exits 0. Per-package coverage ≥70% per D-15 P2 floor (was 2.7% in Phase 73). git diff --stat shows only *_test.go additions (D-12). Atomic commit with 4-5 test files. T-NORESP ensureVDIServer quirk documented in test assertions. 74-06-SUMMARY.md complete.</verification>

<success_criteria>internal/services/vdi per-package ≥70% (1127 stmts). Per-file coverage ≥75% (4-5 files). D-12 honored (zero business code changes). T-NORESP ensureVDIServer quirk documented (NOT fixed). Atomic commit containing all test files. 74-06-SUMMARY.md complete.</success_criteria>

<output>Create .planning/phases/74-p2-finalize-and-diff-coverage/74-06-SUMMARY.md when done.</output>