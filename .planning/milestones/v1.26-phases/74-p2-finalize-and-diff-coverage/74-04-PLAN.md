---
phase: 74-p2-finalize-and-diff-coverage
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/v1/system/*_test.go
  - internal/api/v1/agent/*_test.go
autonomous: true
requirements:
  - SCALE-01
user_setup: []

must_haves:
  truths:
    - "internal/api/v1/system package coverage ≥70% (3039 stmts baseline)"
    - "internal/api/v1/agent package coverage ≥70% (38 stmts baseline)"
    - "All *_test.go files are test-only (D-12)"
  artifacts:
    - path: "internal/api/v1/system/*_handler_test.go"
      provides: "System sub-module handler tests pushing per-package from 35.4% to ≥70%"
      min_lines: 1500
    - path: "internal/api/v1/agent/*_test.go"
      provides: "Agent endpoint tests (small package)"
      min_lines: 100
  key_links:
    - from: "internal/api/v1/system/*_handler_test.go"
      to: "internal/services/system/"
      via: "handler delegates to system service (Phase 73-01 mock pattern)"
      pattern: "system.NewXxxService"
---

<objective>
Push internal/api/v1/system from 35.4% to ≥70% per D-15 P2 floor AND cover internal/api/v1/agent (38 stmts, 0%). System is the largest api/v1 package at 3039 stmts; the lift contributes ~+1203 covered stmts (estimated +2.8 pp).

Purpose: Phase 72 deferred system 35.4% to Phase 73/74. Phase 73 only covered 8 P1 packages, not system. Phase 74 must close this gap. Per-package target ≥70% to satisfy SCALE-01 contribution.

Output: ~15-25 sub-module test files (user/role/dept/menu/dict/post/config/notice/ad_account/apikey/etc.) + agent endpoint tests; D-12 honored.

NOTE: Plan structure mirrors Phase 72 plans 72-04..12 (system sub-module split). Per-sub-module test files are committed per-file or batched - either pattern acceptable per D-12.
</objective>

<execution_context>
execute@$HOME/.claude/get-shit-done/workflows/execute-plan.md
execute@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
context@.planning/phases/74-p2-finalize-and-diff-coverage/74-CONTEXT.md
context@.planning/phases/72-p0-core-supplement/72-04..12-PLAN.md (Phase 72 system sub-module test plans)
context@.planning/phases/73-p1-pending/73-01-SUMMARY.md (mock pattern reference)
context@.planning/coverage-baseline.md (Phase 73 row - api/v1/system at 35.4%)

# Source of truth
Read these files first:
- internal/api/v1/system/ (list all .go files - many sub-handlers)
- internal/services/system/ (interface definitions per sub-module)
- .planning/phases/72-p0-core-supplement/72-04..12-PLAN.md (system sub-module test plan templates)
- CLAUDE.md (operlog convention - ad_account_handler_test.go already exempt pattern reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Identify zero-coverage sub-modules in api/v1/system + design batch plan</name>
  <files>internal/api/v1/system/*_handler_test.go (15+ new), internal/api/v1/agent/*_test.go (new)</files>
  <read_first>internal/api/v1/system/ (glob all .go files), internal/api/v1/agent/ (small package), .planning/quick/260820-backend-test-coverage-scan/per-package-coverage.txt</read_first>
  <action>1. Run per-file coverage scan on internal/api/v1/system/*.go to identify which handler files have 0% coverage (e.g. user_handler.go vs already-covered apikey_handler_test.go). 2. Prioritize 0%-coverage sub-modules first (highest leverage per stmts added). 3. Expected sub-modules: user, role, dept, menu, dict, post, config, notice, ad_account, apikey, ad_domain, ad_dept_sync, ad_group, ad_ou, etc. (15-20+). 4. Plan file split: 1 *_handler_test.go per sub-module. 5. Mock pattern: Phase 73-01 per-interface *Func fields. 6. agent sub-package: 38 stmts, single small test file. 7. Per-file target ≥75%.</action>
  <verify>go test -cover -count=1 -coverprofile=/tmp/sys.out ./internal/api/v1/system/ && go tool cover -func=/tmp/sys.out | grep -v "100.0%" | head -30</verify>
  <done>Sub-modules enumerated; 0%-coverage handlers prioritized; file plan + mock strategy documented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement batch 1 - highest-leverage 0%-coverage sub-modules (~8 files)</name>
  <files>internal/api/v1/system/{user,role,dept,menu,dict,post,config,notice}_handler_test.go</files>
  <read_first>internal/api/v1/system/user_handler.go, role_handler.go, dept_handler.go, menu_handler.go, dict_handler.go, post_handler.go, config_handler.go, notice_handler.go, .planning/phases/73-p1-pending/73-01-SUMMARY.md (mock pattern)</read_first>
  <action>Implement 8 system sub-handler test files in priority order (largest stmts first). Each ≥150 lines, ≥8 tests, ≥75% per-file. mockXxxService per Phase 73-01. D-12 honored. Run coverage per batch. Per-package target ≥50% after this batch.</action>
  <verify>go test -cover -count=1 ./internal/api/v1/system/ 2>&1 | tail -5</verify>
  <acceptance_criteria>8 test files passing, ≥75% per-file, git diff shows only test files (D-12), per-package system ≥50% after batch 1.</acceptance_criteria>
  <done>Batch 1 complete; system per-package ≥50%.</done>
</task>

<task type="auto">
  <name>Task 3: Implement batch 2 - remaining sub-modules + agent (~8 files)</name>
  <files>internal/api/v1/system/{ad_domain,ad_dept_sync,ad_group,ad_ou,logininfor,operlog,api_key,sync_task}_handler_test.go, internal/api/v1/agent/agent_handler_test.go</files>
  <read_first>internal/api/v1/system/ad_domain_handler.go, etc. (verify file names), internal/api/v1/agent/, .planning/phases/73-p1-pending/73-04-SUMMARY.md (oper_log testing pattern)</read_first>
  <action>Implement remaining 8 system sub-handler test files + 1 agent test file. ad_account_handler_test.go already exists (Phase 36 exempt per Phase 72 deviation #2). operlog handler per D-03 non-exempt (Phase 73-04 pattern: test with real operlog DB or no-op shim). Run coverage per batch. Per-package target ≥70%.</action>
  <verify>go test -cover -count=1 ./internal/api/v1/system/ ./internal/api/v1/agent/ 2>&1 | tail -5</verify>
  <acceptance_criteria>Batch 2 complete, per-package system ≥70%, per-package agent ≥70%, D-12 honored, operlog tests not exempt (D-03).</acceptance_criteria>
  <done>All system + agent test files complete; per-package system ≥70%, agent ≥70%.</done>
</task>

<task type="auto">
  <name>Task 4: Atomic commit + SUMMARY</name>
  <files>.planning/phases/74-p2-finalize-and-diff-coverage/74-04-SUMMARY.md</files>
  <read_first>.planning/phases/72-p0-core-supplement/72-13-PLAN.md (atomic commit pattern), .planning/phases/73-p1-pending/73-05-SUMMARY.md</read_first>
  <action>1. Stage all test files. 2. Verify git diff --cached --stat shows only *_test.go. 3. Commit (single or per-file): git commit -m 'test(74-04): add system + agent handler tests (X files, system 35.4%→70%, agent 0%→70%, D-12 honored)'. 4. Do NOT push. 5. Capture final coverage. 6. Write 74-04-SUMMARY.md.</action>
  <verify>git log --oneline -3 && go test -cover -count=1 ./internal/api/v1/system/ ./internal/api/v1/agent/ 2>&1 | tail -5</verify>
  <acceptance_criteria>Atomic commit with all test files, per-package system ≥70% per D-15, per-package agent ≥70%, 74-04-SUMMARY.md complete, D-12 honored.</acceptance_criteria>
  <done>74-04 atomic commit complete; per-package system ≥70%, agent ≥70%; SUMMARY written.</done>
</task>

</tasks>

<verification>go test ./internal/api/v1/system/ ./internal/api/v1/agent/ exits 0. Per-package system ≥70% per D-15 P2 floor (was 35.4% in Phase 73). Per-package agent ≥70% (was 0%). git diff --stat shows only *_test.go additions (D-12). Atomic commit with 15-25 test files. 74-04-SUMMARY.md complete.</verification>

<success_criteria>internal/api/v1/system per-package ≥70% (3039 stmts - was 35.4%). internal/api/v1/agent per-package ≥70% (38 stmts - was 0%). D-12 honored (zero business code changes). operlog handler tested per D-03 non-exempt. Atomic commit containing all test files. 74-04-SUMMARY.md complete.</success_criteria>

<output>Create .planning/phases/74-p2-finalize-and-diff-coverage/74-04-SUMMARY.md when done.</output>