---
phase: 74-p2-finalize-and-diff-coverage
plan: 10
type: execute
wave: 4
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - GOV-03
user_setup:
  - service: "PR diff coverage tool — gocover-coverage GitHub Action (or ORY/xcoverage-action) per D-14"
    why: "Phase 74 must enforce ≥80% diff coverage on every PR per GOV-03"
    dashboard_config:
      - task: "Add new `coverage-diff` job to ci.yml"
        location: ".github/workflows/ci.yml (after backend job)"
      - task: "Configure diff coverage threshold 80%"
        location: ".github/workflows/ci.yml (job step)"

must_haves:
  truths:
    - "GOV-03 PR diff coverage ≥80% gate active in CI"
    - "PR diff coverage failure blocks merge (exit non-zero)"
    - "ci.yml Coverage gate (existing weighted-avg) preserved"
    - "New diff coverage job runs in parallel with backend job OR sequentially after"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Extended CI with diff coverage job"
      contains: "coverage-diff"
    - path: ".github/workflows/diff-coverage.yml (optional — separate file)"
      provides: "Optional separate workflow for diff coverage"
      contains: "diff-coverage"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "coverage.out"
      via: "diff coverage job consumes coverage.out from backend job"
      pattern: "needs: backend"
    - from: ".github/workflows/ci.yml"
      to: "PR git diff"
      via: "action computes diff lines against base branch"
      pattern: "gocover-coverage"
---

<objective>
Land GOV-03 PR diff coverage ≥80% gate per D-14. Add a new `coverage-diff` job to ci.yml (or create a separate diff-coverage.yml workflow file) that:

1. Detects changed files in the PR (against main base branch)
2. Runs `go test -coverprofile` on the changed packages
3. Computes diff coverage (lines added/modified in PR that are covered by tests)
4. Fails if diff coverage < 80%

Per D-14: Use gocover-coverage GitHub Action (most stable, supports `min-coverage: 80` with `compare-with: diff`). Fallback: ORY/xcoverage-action. Last resort: self-implement (git diff + awk + coverage.out parsing).

Output: 1 ci.yml modification + 1 commit. Push to enable the gate.
</objective>

<execution_context>
execute@$HOME/.claude/get-shit-done/workflows/execute-plan.md
execute@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
context@.planning/phases/74-p2-finalize-and-diff-coverage/74-CONTEXT.md
context@.planning/phases/71-governance-baseline-and-ci-gate/71-RESEARCH.md (Phase 71 RESEARCH.md - go-test-coverage analysis)
context@.planning/REQUIREMENTS.md (GOV-03 section)
context@.github/workflows/ci.yml (current backend job structure)

# Source of truth
Read these files first:
- .github/workflows/ci.yml (current 4-step backend job + Upload artifact)
- .planning/phases/71-governance-baseline-and-ci-gate/71-RESEARCH.md (D-14 tool selection rationale)
- Phase 71 GitHub Action reference: golangci/golangci-lint-action@v9, actions/setup-go@v7 (use compatible v-series)
- gocover-coverage action docs (verify `diff: 80` or `min-coverage-compare-with: diff` syntax)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock D-14 diff coverage tool + verify GitHub Action syntax</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.planning/phases/71-governance-baseline-and-ci-gate/71-RESEARCH.md (Phase 71 tool analysis), .github/workflows/ci.yml (current backend job)</read_first>
  <action>1. Verify gocover-coverage action syntax via WebFetch on the action README (gocover-coverage GitHub Action). Confirm: input parameter `min-coverage: 80`, `compare-with: diff` (or similar). 2. If gocover-coverage is unavailable/unmaintained, fall back to ORY/xcoverage-action. 3. If both unsuitable, self-implement: bash + awk + git diff. 4. Lock the tool in plan SUMMARY. 5. Capture the exact YAML snippet to add.</action>
  <verify>curl -s https://raw.githubusercontent.com/marketplace/actions/gocover-coverage/README.md 2>&1 | head -50 || echo 'fallback to research docs'</verify>
  <acceptance_criteria>D-14 tool locked (gocover-coverage preferred, fallback documented). Exact YAML snippet captured.</acceptance_criteria>
  <done>D-14 tool locked; YAML snippet ready for ci.yml modification.</done>
</task>

<task type="auto">
  <name>Task 2: Add coverage-diff job to ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.github/workflows/ci.yml (current 4-step backend job), .planning/phases/74-p2-finalize-and-diff-coverage/74-10-PLAN.md Task 1 (locked YAML snippet)</read_first>
  <action>Modify .github/workflows/ci.yml to add new `coverage-diff` job:

Option A (preferred — separate job in same file):
```yaml
  coverage-diff:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: backend
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true
          cache-dependency-path: go.sum
      - uses: gocover-coverage/gocover-coverage-action@vX
        with:
          min-coverage: 80
          compare-with: diff
          path-prefix: github.com/xingran-next/xingran-go-backend/
```

Option B (fallback — separate workflow file .github/workflows/diff-coverage.yml, triggered only on pull_request).

CRITICAL: Backend job must still run (it's the source of coverage.out). Diff coverage job depends on it via `needs: backend`.

Verify syntax: yamllint or GitHub Actions linter. Run `yamllint .github/workflows/ci.yml` if available.

Preserve ALL existing ci.yml content - only ADD the new job (or new workflow file).
  </action>
  <verify>yamllint .github/workflows/ci.yml 2>&1 || echo 'yamllint unavailable, manual review'</verify>
  <acceptance_criteria>ci.yml extended with coverage-diff job (or separate workflow file added). Existing 8 backend steps preserved. YAML syntax valid. min-coverage: 80 enforced.</acceptance_criteria>
  <done>ci.yml extended with GOV-03 diff coverage gate.</done>
</task>

<task type="auto">
  <name>Task 3: Local validation + commit + SUMMARY</name>
  <files>.planning/phases/74-p2-finalize-and-diff-coverage/74-10-SUMMARY.md</files>
  <read_first>.github/workflows/ci.yml (current after Task 2 modification)</read_first>
  <action>1. Local validation: yamllint or syntax check on ci.yml. 2. If separate file: create .github/workflows/diff-coverage.yml with identical content. 3. Verify no existing job modified. 4. Commit: git add .github/workflows/ci.yml (or both files) && git commit -m 'docs(74-10): add PR diff coverage ≥80% gate (GOV-03 per D-14)'. 5. Do NOT push immediately - test on a separate PR first if possible. 6. Write 74-10-SUMMARY.md documenting: D-14 tool selection, YAML diff, expected behavior, fallback path.</action>
  <verify>git diff --stat HEAD~1 HEAD && cat .github/workflows/ci.yml | grep -A 5 "coverage-diff"</verify>
  <acceptance_criteria>Atomic commit with ci.yml modification (or new file), D-14 tool documented in SUMMARY, push deferred to user (test on PR first), 74-10-SUMMARY.md complete.</acceptance_criteria>
  <done>74-10 atomic commit complete; GOV-03 gate landed in ci.yml; SUMMARY written.</done>
</task>

</tasks>

<verification>ci.yml YAML syntax valid (yamllint or manual). New coverage-diff job present. min-coverage: 80 enforced. Existing backend Coverage gate unchanged. Atomic commit. 74-10-SUMMARY.md complete.</verification>

<success_criteria>GOV-03 met: PR diff coverage ≥80% gate active in CI per D-14. D-14 tool selection documented. ci.yml (or diff-coverage.yml) syntactically valid. Existing backend Coverage gate (weighted-avg + P1 floor) preserved (backward compatibility). 74-10-SUMMARY.md complete with D-14 tool rationale + YAML diff + expected behavior.</success_criteria>

<output>Create .planning/phases/74-p2-finalize-and-diff-coverage/74-10-SUMMARY.md when done.</output>