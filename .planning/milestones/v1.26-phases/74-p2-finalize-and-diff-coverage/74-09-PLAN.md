---
phase: 74-p2-finalize-and-diff-coverage
plan: 09
type: execute
wave: 3
depends_on: []
files_modified:
  - pkg/response/*_test.go
  - pkg/query/*_test.go
  - pkg/time/*_test.go
  - pkg/logger/*_test.go
  - pkg/captcha/*_test.go
  - pkg/gormutil/*_test.go
  - pkg/ldaputils/*_test.go
  - internal/pkg/cache/*_test.go
  - internal/pkg/system/*_test.go
  - internal/utils/*_test.go (excluding operlog - already 82.2%)
  - internal/agent/server/*_test.go
  - internal/services/addomain/*_test.go
  - internal/services/portcollection/*_test.go
autonomous: true
requirements:
  - SCALE-02
user_setup: []

must_haves:
  truths:
    - "Pure utility packages (pkg/response, pkg/query, pkg/time, pkg/logger, pkg/captcha, pkg/gormutil, pkg/ldaputils) ≥70%"
    - "internal/pkg/cache ≥70% (167 stmts, was 0%)"
    - "internal/pkg/system ≥70% (345 stmts, was 0%)"
    - "internal/utils ≥70% (531 stmts, was 4.5%)"
    - "internal/agent/server ≥70% (616 stmts, was 2.1%)"
    - "internal/services/addomain boosted to ≥50% (2415 stmts, was 15.4%)"
    - "internal/services/portcollection boosted to ≥60% (580 stmts, was 19.3%)"
    - "0% business package count reduced (target: 22→≤17 after this plan, leaving 5 for final ratchet to handle)"
    - "All *_test.go files are test-only (D-12)"
  artifacts:
    - path: "pkg/{response,query,time,logger,captcha,gormutil,ldaputils}/*_test.go"
      provides: "High-cost-effective pure utility tests"
      min_lines: 800
    - path: "internal/pkg/{cache,system}/*_test.go"
      provides: "Internal pkg tests"
      min_lines: 400
    - path: "internal/utils/*_test.go"
      provides: "Internal utility tests (excluding operlog)"
      min_lines: 300
    - path: "internal/agent/server/*_test.go"
      provides: "Agent server tests"
      min_lines: 400
    - path: "internal/services/addomain/*_test.go"
      provides: "AD domain service tests"
      min_lines: 600
    - path: "internal/services/portcollection/*_test.go"
      provides: "Port collection service tests"
      min_lines: 400
  key_links:
    - from: "pkg/response/*_test.go"
      to: "internal/api/v1/"
      via: "response.Success/Error used by all handlers"
      pattern: "response\\.Success"
    - from: "internal/agent/server/*_test.go"
      to: "internal/services/rpa/"
      via: "agent endpoints integrate with RPA services"
      pattern: "agent.*Register"
---

<objective>
Address SCALE-02 (medium coverage + high-cost-effective pure utilities) + 0% business package count reduction. Push 13 packages: 7 pure utility packages (pkg/response, pkg/query, pkg/time, pkg/logger, pkg/captcha, pkg/gormutil, pkg/ldaputils), 2 internal pkg packages (internal/pkg/cache, internal/pkg/system), internal/utils, internal/agent/server, services/addomain, services/portcollection.

Purpose: Pure utility packages have very low stmts but contribute directly to weighted average (every covered stmt counts). Combined contribution: ~+2929 covered stmts (utils +796, agent/server +419, addomain +836, portcollection +236, internal/pkg +359, others +283). Also reduces 0%-pkg count from 22 to ≤17 (leaving 5 for final ratchet).

Output: ~13-20 test files; per-file ≥75%; D-12 honored.
</objective>

<execution_context>
execute@$HOME/.claude/get-shit-done/workflows/execute-plan.md
execute@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
context@.planning/phases/74-p2-finalize-and-diff-coverage/74-CONTEXT.md
context@.planning/phases/73-p1-pending/73-03-SUMMARY.md (pure mock pattern for utilities)
context@.planning/REQUIREMENTS.md (SCALE-02)
context@.planning/coverage-baseline.md (Phase 73 row - pkg utils + internal/utils + agent/server all 0% or low)

# Source of truth
Read these files first:
- pkg/{response,query,time,logger,captcha,gormutil,ldaputils}/ (all list .go files - small packages)
- internal/pkg/{cache,system}/ (list .go files)
- internal/utils/ (list .go files, excluding operlog)
- internal/agent/server/ (list .go files)
- internal/services/addomain/ (Phase 36 reference - LDAP client + sync + account pool)
- internal/services/portcollection/ (network port collection)
- .planning/phases/73-p1-pending/73-04-SUMMARY.md (service mixed DB + mock)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate gap packages + design batch plan</name>
  <files>~13-20 test files across 13 packages</files>
  <read_first>All 13 packages (glob all .go files), .planning/quick/260820-backend-test-coverage-scan/per-package-coverage.txt</read_first>
  <action>Plan in 3 batches: A) 7 pkg/* utilities (simple table-driven tests for pure functions), B) internal/pkg/* + internal/utils + internal/agent/server (mixed patterns), C) services/addomain + services/portcollection (DB + mock). Per-file target ≥75%.</action>
  <verify>ls pkg/ | head -20 && ls internal/pkg/ 2>/dev/null | head -10</verify>
  <done>Gap packages enumerated; 3-batch plan documented.</done>
</task>

<task type="auto">
  <name>Task 2: Batch A - 7 pkg/* pure utility tests</name>
  <files>pkg/response/*_test.go, pkg/query/*_test.go, pkg/time/*_test.go, pkg/logger/*_test.go, pkg/captcha/*_test.go, pkg/gormutil/*_test.go, pkg/ldaputils/*_test.go</files>
  <read_first>pkg/response/, pkg/query/, pkg/time/, pkg/logger/, pkg/captcha/, pkg/gormutil/, pkg/ldaputils/ (all glob), .planning/phases/73-p1-pending/73-03-SUMMARY.md (table-driven pattern)</read_first>
  <action>Implement 7 small utility test files. Each ≥100 lines, ≥6 tests, ≥70% per-file (some packages may be smaller). Pure table-driven tests per Phase 73-03 service pattern. Captcha: test generation + verification paths. gormutil: test GORM helper functions. D-12 honored.</action>
  <verify>go test -cover -count=1 ./pkg/response/ ./pkg/query/ ./pkg/time/ ./pkg/logger/ ./pkg/captcha/ ./pkg/gormutil/ ./pkg/ldaputils/ 2>&1 | tail -10</verify>
  <acceptance_criteria>7 test files passing, all utilities ≥70% per-file, git diff shows only test files (D-12).</acceptance_criteria>
  <done>Batch A complete; 7 pkg utilities ≥70%.</done>
</task>

<task type="auto">
  <name>Task 3: Batch B - internal/pkg + internal/utils + internal/agent/server</name>
  <files>internal/pkg/cache/*_test.go, internal/pkg/system/*_test.go, internal/utils/*_test.go (excluding operlog), internal/agent/server/*_test.go</files>
  <read_first>internal/pkg/cache/, internal/pkg/system/, internal/utils/ (excluding operlog subdir), internal/agent/server/, .planning/phases/73-p1-pending/73-04-SUMMARY.md</read_first>
  <action>Implement 4 internal package tests. internal/pkg/cache: cache provider helpers. internal/pkg/system: system helpers. internal/utils: utility functions (NOT operlog - already 82.2%, leave alone). internal/agent/server: agent endpoints. Each ≥100 lines, ≥5 tests, ≥70% per-file. Target: internal/pkg/cache ≥70%, internal/pkg/system ≥70%, internal/utils ≥70%, internal/agent/server ≥70%.</action>
  <verify>go test -cover -count=1 ./internal/pkg/cache/ ./internal/pkg/system/ ./internal/utils/ ./internal/agent/server/ 2>&1 | tail -5</verify>
  <acceptance_criteria>4 test files passing, internal/pkg/cache ≥70%, internal/pkg/system ≥70%, internal/utils ≥70%, internal/agent/server ≥70% per D-15, git diff shows only test files (D-12), operlog NOT modified (already 82.2%).</acceptance_criteria>
  <done>Batch B complete; 4 internal packages ≥70%.</done>
</task>

<task type="auto">
  <name>Task 4: Batch C - services/addomain + services/portcollection</name>
  <files>internal/services/addomain/*_test.go (sub-services: ldap, config, group, user, ou, sync, account_pool, failover_client), internal/services/portcollection/*_test.go</files>
  <read_first>internal/services/addomain/ (list all .go files - 8 sub-services per Phase 36), internal/services/portcollection/, .planning/phases/73-p1-pending/73-04-SUMMARY.md (LDAP mock pattern from Phase 36)</read_first>
  <action>Implement 2 service test files. addomain: 8 sub-services per Phase 36 (ldap, config, group, user, ou, sync, account_pool, failover_client). Mock LDAP server (Phase 36 used real LDAP for ad_account - same pattern). Real glebarez sqlite for DB paths. portcollection: device port collection service. Each ≥300 lines, ≥10 tests, ≥70% per-file. Target: addomain ≥50%, portcollection ≥60%. If addomain targets <50%, escalate to Phase 74-11 final ratchet for completion.</action>
  <verify>go test -cover -count=1 ./internal/services/addomain/ ./internal/services/portcollection/ 2>&1 | tail -5</verify>
  <acceptance_criteria>2 test files passing, addomain ≥50%, portcollection ≥60%, git diff shows only test files (D-12).</acceptance_criteria>
  <done>Batch C complete; addomain ≥50%, portcollection ≥60%.</done>
</task>

<task type="auto">
  <name>Task 5: Atomic commit + SUMMARY</name>
  <files>.planning/phases/74-p2-finalize-and-diff-coverage/74-09-SUMMARY.md</files>
  <read_first>.planning/phases/72-p0-core-supplement/72-13-PLAN.md (atomic commit pattern)</read_first>
  <action>1. Stage all 13 packages' test files. 2. Verify git diff --cached --stat shows only *_test.go. 3. Commit: git commit -m 'test(74-09): add pkg/utils + internal/pkg + agent/server + addomain + portcollection tests (13 packages, SCALE-02 met, D-12 honored)'. 4. Do NOT push. 5. Capture final coverage. 6. Write 74-09-SUMMARY.md.</action>
  <verify>git log --oneline -2 && go test -cover -count=1 ./pkg/ ./internal/pkg/ ./internal/utils/ ./internal/agent/server/ ./internal/services/addomain/ ./internal/services/portcollection/ 2>&1 | tail -15</verify>
  <acceptance_criteria>Atomic commit with all test files, 13 packages at targets, 0%-pkg count reduced from 22 to ≤17, 74-09-SUMMARY.md complete, D-12 honored.</acceptance_criteria>
  <done>74-09 atomic commit complete; 13 packages lifted; 0%-pkg count reduced; SUMMARY written.</done>
</task>

</tasks>

<verification>go test ./pkg/ ./internal/pkg/ ./internal/utils/ ./internal/agent/server/ ./internal/services/addomain/ ./internal/services/portcollection/ exits 0. All 13 packages at targets. 0%-pkg count 22→≤17. git diff --stat shows only *_test.go (D-12). Atomic commit. 74-09-SUMMARY.md complete.</verification>

<success_criteria>SCALE-02 met: pkg/{response,query,time,logger,captcha,gormutil,ldaputils} ≥70% each. internal/pkg/cache ≥70%. internal/pkg/system ≥70%. internal/utils ≥70% (excluding operlog 82.2%). internal/agent/server ≥70% (D-15). services/addomain ≥50%. services/portcollection ≥60%. 0%-pkg count 22→≤17. D-12 honored. Atomic commit containing all test files. 74-09-SUMMARY.md complete.</success_criteria>

<output>Create .planning/phases/74-p2-finalize-and-diff-coverage/74-09-SUMMARY.md when done.</output>