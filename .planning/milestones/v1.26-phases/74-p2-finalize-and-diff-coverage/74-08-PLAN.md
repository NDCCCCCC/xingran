---
phase: 74-p2-finalize-and-diff-coverage
plan: 08
type: execute
wave: 2
depends_on: []
files_modified:
  - internal/core/*_test.go
  - internal/device/*_test.go
  - internal/services/scheduler/*_test.go
  - internal/core/db/*_test.go
  - internal/core/security/*_test.go
  - pkg/cache/*_test.go
  - pkg/crypto/*_test.go
  - pkg/middleware/*_test.go
  - internal/templates/*_test.go
autonomous: true
requirements:
  - SCALE-01
user_setup: []

must_haves:
  truths:
    - "internal/core per-package ≥70% (754 stmts, was 2.1%)"
    - "internal/device per-package ≥70% (1249 stmts, was 2.5%)"
    - "internal/services/scheduler per-package ≥70% (167 stmts, was 4.8%)"
    - "internal/core/db per-package ≥70% (643 stmts, was 37.5%)"
    - "pkg/cache per-package ≥70% (926 stmts, was 24.6%)"
    - "All *_test.go files are test-only (D-12)"
  artifacts:
    - path: "internal/core/*_test.go"
      provides: "Core package tests"
      min_lines: 400
    - path: "internal/device/*_test.go"
      provides: "Device package tests"
      min_lines: 600
    - path: "internal/services/scheduler/*_test.go"
      provides: "Scheduler service tests"
      min_lines: 200
    - path: "pkg/cache/*_test.go (additions beyond existing 15 tests)"
      provides: "Cache coverage boost"
      min_lines: 400
    - path: "pkg/crypto/*_test.go"
      provides: "Crypto (SM2/SM3/SM4) tests"
      min_lines: 300
    - path: "pkg/middleware/*_test.go"
      provides: "Middleware tests"
      min_lines: 300
  key_links:
    - from: "pkg/crypto/*_test.go"
      to: "internal/core/security/"
      via: "SM2+SM4 encryption paths"
      pattern: "sm4.*Encrypt"
    - from: "internal/device/*_test.go"
      to: "internal/services/network/"
      via: "device collection paths"
      pattern: "device.NewCollector"
---

<objective>
Lift 9 medium-to-low coverage packages that are part of SCALE-01 P2 big block / SCALE-02 medium-coverage boost: core (754), device (1249), scheduler (167), core/db (643), core/security (313), pkg/cache (926), pkg/crypto (439), pkg/middleware (609), templates (243). Combined contribution: ~+2983 covered stmts.

Purpose: These packages form the system foundation - pushing them lifts the weighted average substantially. They are NOT in the strict D-15 per-package floor list (except core, device, scheduler), but lifting core (754), device (1249), scheduler (167) IS required for D-15 P2 floor compliance.

Output: ~15-20 test files across 9 packages; per-file ≥75%; D-12 honored.
</objective>

<execution_context>
execute@$HOME/.claude/get-shit-done/workflows/execute-plan.md
execute@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
context@.planning/phases/74-p2-finalize-and-diff-coverage/74-CONTEXT.md
context@.planning/phases/73-p1-pending/73-04-SUMMARY.md (mixed DB + mock pattern)
context@.planning/REQUIREMENTS.md (SCALE-01 + SCALE-02)
context@.planning/coverage-baseline.md (Phase 73 row - core 2.1%, device 2.5%, scheduler 4.8%, etc.)

# Source of truth
Read these files first:
- internal/core/ (list all .go files - bootstrap, scheduler init, etc.)
- internal/device/ (list all .go files - Scrapli integration)
- internal/services/scheduler/ (list all .go files)
- pkg/cache/ (existing 15 tests - identify gaps)
- pkg/crypto/ (SM2/SM3/SM4 implementations)
- .planning/quick/260820-backend-test-coverage-scan/per-package-coverage.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate gap sub-packages + design batch plan</name>
  <files>~15-20 test files across 9 packages</files>
  <read_first>internal/core/, internal/device/, internal/services/scheduler/, internal/core/db/, internal/core/security/, pkg/cache/, pkg/crypto/, pkg/middleware/, internal/templates/ (all glob)</read_first>
  <action>Per-file coverage scan each of 9 packages. Identify 0%/low-coverage files. Prioritize D-15 floor packages (core, device, scheduler). Plan in 3 batches: A) D-15 floor packages, B) medium-coverage boost (db, security, cache, templates), C) crypto + middleware (D-02 mockSM4Cipher concern). Per-file target ≥75%.</action>
  <verify>go test -cover -count=1 -coverprofile=/tmp/core.out ./internal/core/ && go tool cover -func=/tmp/core.out | grep -v "100.0%" | head -20</verify>
  <done>Gap files enumerated; 3-batch plan documented.</done>
</task>

<task type="auto">
  <name>Task 2: Batch A - D-15 floor packages: core + device + scheduler</name>
  <files>internal/core/*_test.go (~4 files), internal/device/*_test.go (~3 files), internal/services/scheduler/*_test.go (~1 file)</files>
  <read_first>internal/core/core.go, internal/device/ (Scrapli TextFSM), internal/services/scheduler/, .planning/phases/73-p1-pending/73-04-SUMMARY.md</read_first>
  <action>Implement test files for D-15 P2 floor packages. core: bootstrap/init paths. device: Scrapli + TextFSM with mock scrapli client. scheduler: cron job registration/execution paths. Each ≥100 lines, ≥5 tests, ≥70% per-file. Real DB where applicable, mock external deps. D-12 honored. Target: core ≥70%, device ≥70%, scheduler ≥70%.</action>
  <verify>go test -cover -count=1 ./internal/core/ ./internal/device/ ./internal/services/scheduler/ 2>&1 | tail -5</verify>
  <acceptance_criteria>~8 test files passing, core ≥70%, device ≥70%, scheduler ≥70% per D-15, git diff shows only test files (D-12).</acceptance_criteria>
  <done>Batch A complete; 3 D-15 floor packages ≥70%.</done>
</task>

<task type="auto">
  <name>Task 3: Batch B - core/db + core/security + cache + templates</name>
  <files>internal/core/db/*_test.go (additions), internal/core/security/*_test.go, pkg/cache/*_test.go (additions beyond 5ead742 fix), internal/templates/*_test.go</files>
  <read_first>internal/core/db/, internal/core/security/, pkg/cache/ (existing tests), internal/templates/, .planning/phases/73-p1-pending/73-04-SUMMARY.md</read_first>
  <action>Implement test files for medium-coverage packages. db: migration helpers + connection pooling. security: JWT + password + SM4 helpers (per Phase 73 ad_account pattern). cache: ADD to existing 15 tests (D-09 honored - real Redis or mock). templates: template parsing paths. Each ≥150 lines, ≥6 tests, ≥70% per-file. D-12 honored. Target: db ≥70%, security ≥75%, cache ≥70%, templates ≥80%.</action>
  <verify>go test -cover -count=1 ./internal/core/db/ ./internal/core/security/ ./pkg/cache/ ./internal/templates/ 2>&1 | tail -5</verify>
  <acceptance_criteria>~6 test files passing, db ≥70%, security ≥75%, cache ≥70%, templates ≥80%, git diff shows only test files (D-12).</acceptance_criteria>
  <done>Batch B complete; 4 medium packages boosted.</done>
</task>

<task type="auto">
  <name>Task 4: Batch C - crypto + middleware + verify coverage</name>
  <files>pkg/crypto/*_test.go, pkg/middleware/*_test.go</files>
  <read_first>pkg/crypto/ (SM2/SM3/SM4 impls), pkg/middleware/ (JWT + auth + rate limit), .planning/quick/260820-backend-test-coverage-scan/per-package-coverage.txt</read_first>
  <action>Implement test files for crypto (SM2/SM3/SM4) + middleware (auth/rate-limit/permission). CRITICAL: middleware 86.2% baseline - DO NOT regress (Phase 71+ invariant). crypto: test encryption/decryption roundtrips with real SM4 (D-09). Each ≥200 lines, ≥8 tests, ≥70% per-file. Target: crypto ≥70%, middleware maintain ≥85%.</action>
  <verify>go test -cover -count=1 ./pkg/crypto/ ./pkg/middleware/ 2>&1 | tail -5</verify>
  <acceptance_criteria>~2 test files passing, crypto ≥70%, middleware maintain ≥85% (no regression), git diff shows only test files (D-12).</acceptance_criteria>
  <done>Batch C complete; crypto ≥70%, middleware ≥85% preserved.</done>
</task>

<task type="auto">
  <name>Task 5: Atomic commit + SUMMARY</name>
  <files>.planning/phases/74-p2-finalize-and-diff-coverage/74-08-SUMMARY.md</files>
  <read_first>.planning/phases/72-p0-core-supplement/72-13-PLAN.md (atomic commit pattern)</read_first>
  <action>1. Stage all 9 packages' test files. 2. Verify git diff --cached --stat shows only *_test.go. 3. Commit: git commit -m 'test(74-08): add core+device+scheduler+db+security+cache+crypto+middleware+templates tests (X files, D-15 floor compliance, D-12 honored)'. 4. Do NOT push. 5. Capture final coverage. 6. Write 74-08-SUMMARY.md.</action>
  <verify>git log --oneline -2 && go test -cover -count=1 ./internal/core/ ./internal/device/ ./internal/services/scheduler/ 2>&1 | tail -10</verify>
  <acceptance_criteria>Atomic commit with all test files, all 3 D-15 floor packages ≥70%, all medium packages boosted, middleware ≥85% preserved, 74-08-SUMMARY.md complete, D-12 honored.</acceptance_criteria>
  <done>74-08 atomic commit complete; 9 packages lifted; SUMMARY written.</done>
</task>

</tasks>

<verification>go test ./internal/core/ ./internal/device/ ./internal/services/scheduler/ ./internal/core/db/ ./internal/core/security/ ./pkg/cache/ ./pkg/crypto/ ./pkg/middleware/ ./internal/templates/ exits 0. 3 D-15 floor packages ≥70% (core, device, scheduler). 4 medium packages boosted (db ≥70%, security ≥75%, cache ≥70%, templates ≥80%). middleware ≥85% preserved (no regression). git diff --stat shows only *_test.go (D-12). Atomic commit with all test files. 74-08-SUMMARY.md complete.</verification>

<success_criteria>internal/core ≥70% (754 stmts - D-15 floor). internal/device ≥70% (1249 stmts - D-15 floor). internal/services/scheduler ≥70% (167 stmts - D-15 floor). 6 additional packages boosted. middleware ≥85% preserved (no regression per Phase 71+ invariant). D-12 honored. Atomic commit containing all test files. 74-08-SUMMARY.md complete.</success_criteria>

<output>Create .planning/phases/74-p2-finalize-and-diff-coverage/74-08-SUMMARY.md when done.</output>