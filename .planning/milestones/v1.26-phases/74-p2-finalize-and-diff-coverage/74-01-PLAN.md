---
phase: 74-p2-finalize-and-diff-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/v1/operations/*_test.go
  - internal/api/v1/operations/router_test.go
autonomous: true
requirements:
  - SCALE-01
user_setup: []

must_haves:
  truths:
    - "internal/api/v1/operations package coverage ≥70% (1285 stmts baseline)"
    - "All *_test.go files added are test-only (no business code changes per D-12)"
    - "All exported handler functions have ≥1 dedicated test case"
    - "Per-package ratchet in coverage-baseline.md shows operations ≥70%"
  artifacts:
    - path: "internal/api/v1/operations/*_handler_test.go"
      provides: "Handler tests covering building, floor, workstation, serverroom, info_point, dedicated_line, asset_location, duty_station modules"
      min_lines: 800
      contains: "TestBuilding"
    - path: "internal/api/v1/operations/router_test.go"
      provides: "Router smoke tests verifying all POST endpoints are wired correctly"
      min_lines: 100
  key_links:
    - from: "internal/api/v1/operations/building_handler_test.go"
      to: "internal/api/v1/operations/building_handler.go"
      via: "go test invokes handler methods via httptest.NewRecorder + SetupRouter"
      pattern: "httptest.NewRecorder"
    - from: "internal/api/v1/operations/*_handler.go"
      to: "internal/services/operations/"
      via: "handler delegates to service layer (mock per Phase 72 ad_account pattern)"
      pattern: "operations.NewBuildingService"
---

<objective>
Add comprehensive handler tests for internal/api/v1/operations package (1285 stmts, currently 3.0%), pushing per-package coverage to ≥70% per Phase 74 D-15 P2 package floor. This addresses SCALE-01 P2 big block - operations is the largest P2 handler package covering building/floor/workstation/serverroom/info_point/dedicated_line/asset_location/duty_station sub-modules.

Purpose: The operations package is the highest-impact business CRUD surface, and Phase 72 deferred it to Phase 74 due to its complexity (8 sub-handlers). Closing it lifts the weighted average significantly (estimated +860 covered stmts).

Output: ~8 *_handler_test.go + 1 router_test.go files; 1 atomic commit per file pattern (Phase 73-01..04 model); D-12 honored (test-only).
</objective>

<execution_context>
execute@$HOME/.claude/get-shit-done/workflows/execute-plan.md
execute@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
context@.planning/phases/74-p2-finalize-and-diff-coverage/74-CONTEXT.md
context@.planning/phases/73-p1-pending/73-CONTEXT.md
context@.planning/phases/73-p1-pending/73-01-PLAN.md (handler simple test pattern - duty+knowledge)
context@.planning/phases/73-p1-pending/73-02-PLAN.md (handler complex test pattern - rpa+vdi incl. public router)
context@.planning/REQUIREMENTS.md (SCALE-01 section)
context@.planning/coverage-baseline.md (Phase 73 row - operations at 3.0%)

# Source of truth - operations handler signatures + service interfaces
Read these files first:
- internal/api/v1/operations/ (list all .go files, identify exported handler funcs + service deps)
- internal/services/operations/ (interface definitions for mock setup per Phase 73-01 D-08)
- .planning/phases/73-p1-pending/73-01-SUMMARY.md (mock pattern: per-interface *Func fields + interface-embedded-as-nil)
- CLAUDE.md (operlog convention + response.Success pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate operations handler modules and design test strategy</name>
  <files>internal/api/v1/operations/*_handler_test.go (8+ new), internal/api/v1/operations/router_test.go</files>
  <read_first>internal/api/v1/operations/ (glob all .go files), internal/services/operations/ (interface definitions), .planning/phases/73-p1-pending/73-01-SUMMARY.md (mock service pattern)</read_first>
  <action>Enumerate all exported handler methods in internal/api/v1/operations/. Plan test strategy: 1. Identify sub-handlers: building, floor, workstation, serverroom, info_point, dedicated_line, asset_location, duty_station (8 expected). 2. For each sub-handler identify exported methods + service deps + DTO structs + router registration. 3. Mock strategy (D-02 + D-08): mockXxxService struct with *Func fields per Phase 73-01 duty_handler_test.go pattern. 4. Plan file split: 8 *_handler_test.go files + 1 router_test.go (9 total). 5. Per-file target ≥75% to ensure package ≥70%.</action>
  <verify>ls internal/api/v1/operations/ | head -30</verify>
  <done>Test strategy planned: 9 test files mapped to 8 sub-handlers + 1 router; mock pattern confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement 4 sub-handler tests (building, floor, workstation, serverroom)</name>
  <files>internal/api/v1/operations/building_handler_test.go, internal/api/v1/operations/floor_handler_test.go, internal/api/v1/operations/workstation_handler_test.go, internal/api/v1/operations/serverroom_handler_test.go</files>
  <read_first>internal/api/v1/operations/building_handler.go, floor_handler.go, workstation_handler.go, serverroom_handler.go, .planning/phases/73-p1-pending/73-01-SUMMARY.md (mock pattern)</read_first>
  <action>Implement 4 test files for largest sub-handlers. Each ≥200 lines, ≥10 test functions, ≥75% per-file coverage. mockBuildingService etc. per Phase 73-01 pattern. Use httptest.NewRecorder + SetupRouter or direct handler invocation. D-12 honored: zero business code changes. operlog.Record calls use no-op shim or real helper with mock DB per Phase 73-04 D-03.</action>
  <verify>go test -cover -count=1 ./internal/api/v1/operations/ 2>&1 | tail -5</verify>
  <acceptance_criteria>4 test files created passing, each ≥200 lines ≥10 tests ≥75% per-file, mock pattern matches Phase 73-01, git diff shows only test files (D-12), go test exits 0.</acceptance_criteria>
  <done>4 operations handler test files implemented; per-file coverage ≥75%; per-package operations ≥40% (cumulative).</done>
</task>

<task type="auto">
  <name>Task 3: Implement remaining 4 sub-handler tests + router test (5 files)</name>
  <files>internal/api/v1/operations/info_point_handler_test.go, internal/api/v1/operations/dedicated_line_handler_test.go, internal/api/v1/operations/asset_location_handler_test.go, internal/api/v1/operations/duty_station_handler_test.go, internal/api/v1/operations/router_test.go</files>
  <read_first>internal/api/v1/operations/info_point_handler.go, dedicated_line_handler.go, asset_location_handler.go, duty_station_handler.go, operations_router.go (SetupOperationsRouter), .planning/phases/73-p1-pending/73-02-SUMMARY.md (router smoke test pattern)</read_first>
  <action>Implement 4 remaining sub-handler tests + 1 router test. Same pattern as Task 2. router_test.go uses Phase 73-02 rpa_router_test.go pattern (or Phase 72 internal-JWT variant). D-12 honored. Run full coverage - target ≥70%. If <70%, identify uncovered methods and add tests.</action>
  <verify>go test -cover -count=1 ./internal/api/v1/operations/ 2>&1 | tail -3</verify>
  <acceptance_criteria>9 test files created, package ≥70%, D-12 honored, go test exits 0.</acceptance_criteria>
  <done>All 9 operations test files complete; per-package operations ≥70% per D-15 P2 floor.</done>
</task>

<task type="auto">
  <name>Task 4: Atomic commit + SUMMARY</name>
  <files>.planning/phases/74-p2-finalize-and-diff-coverage/74-01-SUMMARY.md</files>
  <read_first>.planning/phases/72-p0-core-supplement/72-13-PLAN.md (atomic ratchet structure), .planning/phases/73-p1-pending/73-05-SUMMARY.md (per-package coverage table format)</read_first>
  <action>1. Stage: git add internal/api/v1/operations/*_test.go internal/api/v1/operations/router_test.go. 2. Verify git diff --cached --stat shows only *_test.go. 3. Commit: git commit -m 'test(74-01): add operations handler tests (9 files, ≥70% per-package, D-12 honored)'. 4. Do NOT push (deferred per Phase 73-05 pattern). 5. Capture final coverage. 6. Write 74-01-SUMMARY.md with coverage delta + per-file table + commit SHA + D-12 verification + mock pattern + documented quirks per Phase 73 pattern.</action>
  <verify>git log --oneline -2 && go test -cover -count=1 ./internal/api/v1/operations/ 2>&1 | tail -3</verify>
  <acceptance_criteria>Single commit (or per-file) with all 9 test files, git diff shows zero non-test changes, 74-01-SUMMARY.md complete, per-package operations ≥70% per D-15, push deferred.</acceptance_criteria>
  <done>74-01 atomic commit complete; per-package operations ≥70%; SUMMARY written; Phase 74-01 SCALE-01 contribution met.</done>
</task>

</tasks>

<verification>go test ./internal/api/v1/operations/ exits 0. Per-package coverage ≥70% per D-15 P2 floor (SCALE-01 SC#2 met). git diff --stat shows only *_test.go additions (D-12 strict). Per-file coverage ≥75% (9 files). Atomic commit with 9 test files. 74-01-SUMMARY.md finalized.</verification>

<success_criteria>internal/api/v1/operations per-package ≥70% (1285 stmts). Per-file coverage ≥75% (9 files). D-12 honored (zero business code changes). Mock pattern matches Phase 73-01. Atomic commit containing all 9 test files. 74-01-SUMMARY.md complete with coverage delta + commit SHA.</success_criteria>

<output>Create .planning/phases/74-p2-finalize-and-diff-coverage/74-01-SUMMARY.md when done.</output>