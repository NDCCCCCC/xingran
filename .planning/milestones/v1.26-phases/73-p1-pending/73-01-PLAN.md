---
phase: 73-p1-pending
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/v1/duty/duty_handler_test.go
  - internal/api/v1/knowledge/handler_test.go
autonomous: true
requirements:
  - IMP-01
  - IMP-02
user_setup: []

must_haves:
  truths:
    - "go test -cover -count=1 ./internal/api/v1/duty/... exits 0 with coverage >= 70%"
    - "go test -cover -count=1 ./internal/api/v1/knowledge/... exits 0 with coverage >= 70%"
    - "All handler methods on DutyHandler (23 methods), ArticleHandler (10), CategoryHandler (6), TagHandler (5) have at least one happy-path + one error-path test case"
    - "DB-level assertions use glebarez/sqlite in-memory; no live Postgres / Redis dependency"
  artifacts:
    - path: "internal/api/v1/duty/duty_handler_test.go"
      provides: "Table-driven handler tests for IMP-01 (api/v1/duty, 265 stmts)"
      contains: "package duty"
    - path: "internal/api/v1/knowledge/handler_test.go"
      provides: "Table-driven handler tests for IMP-02 (api/v1/knowledge, 273 stmts)"
      contains: "package knowledge"
  key_links:
    - from: "internal/api/v1/duty/duty_handler_test.go"
      to: "internal/services/duty"
      via: "real service call through DutyCacheService interface"
      pattern: "service\\.(CreateDutyPool|GetDutyPoolList|UpdateDutyPool|DeleteDutyPool|GenerateSchedule|GetDutyScheduleList|SwapDuty|ManualDuty|CreateHoliday|UpdateHoliday|DeleteHoliday|GetDutyConfig|UpdateDutyConfig|GetMyDutyStats)"
    - from: "internal/api/v1/knowledge/handler_test.go"
      to: "internal/services/knowledge"
      via: "real service call through KnowledgeCacheService interface"
      pattern: "service\\.(GetKnowledgeArticleList|CreateKnowledgeArticle|UpdateKnowledgeArticle|DeleteKnowledgeArticle|SearchKnowledgeArticles|ConvertFromWorkOrder|GetKnowledgeCategoryList|CreateKnowledgeCategory|GetAllTags|CreateTag)"
---

<objective>
Add handler tests for two simple P1 packages: internal/api/v1/duty (265 stmts) and internal/api/v1/knowledge (273 stmts), pushing both from 0.0% to ≥70% per-package coverage.

Purpose: Phase 73 first delivery per D-01 (4 plans by complexity cross-cut). Both packages are 0% tested today with no existing test files. After this plan, IMP-01 and IMP-02 SCs are met.

Output: Two new test files (one per package) covering all handler methods with table-driven TC following the D-02 ad_account handler lightweight pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-p1-pending/73-CONTEXT.md
@.planning/phases/73-p1-pending/73-RESEARCH.md
@.planning/phases/73-p1-pending/73-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/quick/260820-backend-test-coverage-scan/per-package-coverage.txt

Read these source files first (D-02 范本 + 目标):
- internal/api/v1/system/ad_account_handler_test.go — D-02 lightweight handler pattern (READ for actual code structure)
- internal/api/v1/monitor/oper_log_handler_test.go — Phase 72 SHIPPED ad_account-pattern reference
- internal/api/v1/duty/duty_handler.go — target (23 methods)
- internal/api/v1/duty/duty_router.go — handler constructor signature
- internal/api/v1/knowledge/handler.go — target (3 handler structs: ArticleHandler 10, CategoryHandler 6, TagHandler 5)
- internal/api/v1/knowledge/router.go — constructor signatures
- internal/services/duty/duty_cache_impl.go — DutyCacheService interface methods
- internal/services/knowledge/knowledge_cache_impl.go — KnowledgeCacheService interface methods
- internal/models/operations/duty.go + internal/models/operations/knowledge.go — request/response struct definitions
- pkg/response/response.go — response.Success / response.Error return shape
- pkg/response/responseHelpers.go — HandleJSONBinding + HandleServiceError
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build duty_handler_test.go covering all 23 handler methods</name>
  <files>internal/api/v1/duty/duty_handler_test.go</files>
  <read_first>
    - internal/api/v1/duty/duty_handler.go
    - internal/api/v1/duty/duty_router.go
    - internal/services/duty/duty_cache_impl.go
    - internal/api/v1/system/ad_account_handler_test.go
    - internal/api/v1/monitor/oper_log_handler_test.go
  </read_first>
  <action>
Create internal/api/v1/duty/duty_handler_test.go (package duty). Cover all 23 handler methods on DutyHandler:

Pool methods (6): ListPools, StatisticsPools, CreatePool, GetPoolByID, UpdatePool, DeletePool
Schedule methods (8): ListSchedules, GenerateSchedule, GetTodayDuty, GetMonthlySchedule, SwapDuty, ManualDuty, DeleteSchedule, BatchDeleteSchedules
Holiday methods (6): ListHolidays, CreateHoliday, UpdateHoliday, DeleteHoliday, BatchCreateHolidays, GetHolidayYears
Config methods (2): GetConfig, UpdateConfig
Stats methods (1): GetMyStats

Test fixture (mirror Phase 72 oper_log_handler_test.go pattern):
- mockDutyService — implements dutyServices.DutyCacheService with one function-field per interface method. Embed the interface as default so unimplemented methods return zero values. Each function-field named after the interface method (e.g. GetDutyPoolListFunc).
- Compile-time assertion at top: var _ dutyServices.DutyCacheService = (*mockDutyService)(nil).
- setupTestDutyHandler(mock *mockDutyService) *DutyHandler — constructs via NewDutyHandler(mock).WithCore(&core.Core{CoreInfra: &core.CoreInfra{DB: &db.Database{}}, CoreServices: &core.CoreServices{}}).
- newTestCtxDuty(method, path string, body interface{}) (*gin.Context, *httptest.ResponseRecorder) — gin.SetMode(gin.TestMode) + httptest.NewRecorder() + gin.CreateTestContext(w) + httptest.NewRequest(method, path, body-as-bytes) + req.Header.Set("Content-Type", "application/json").

For each handler method write 2+ test cases (happy + error):
- TC1: <method>_success_returns_200 — set the relevant mock function-field to return seeded value; assert w.Code == 200.
- TC2: <method>_service_error_returns_5xx — set mock func to return errors.New(...); assert non-200.

Bind path/URL params for GetPoolByID and similar methods via c.Params.

Skip methods requiring live Redis cache; skip operlog recording (h.core nil handled gracefully).

Explicit skip enumeration (for ≥70% math): handler.go ~265-273 stmts total per package. The 21-23 handler methods each ~10 stmts avg (~210-230 stmts) are covered via mock service; remaining ~35-55 stmts are: import block, struct init, SetupXxxRouter (covered indirectly via route registration). 0 methods skipped for live Redis cache — all handler methods use mock service, no direct Redis call paths in handler layer (Redis is encapsulated in service layer, which is mockable).

Post-skip expected coverage: ~230/265 = ~87% achievable (duty); ~210/273 = ~77% achievable (knowledge). If actual falls below 70%, contingency: add glebarez sqlite-based service mock paths for any uncovered branch, or add httptest-level auth middleware integration tests using service account fixture.

Naming: TestDutyHandler_<method>_<scenario>. Aim for 46+ test functions.

Add TestDutyHandler_CompileOnly smoke test.
  </action>
  <verify>
    <automated>go test -count=1 -cover ./internal/api/v1/duty/... 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - go test -cover -count=1 ./internal/api/v1/duty/... exits 0
    - Output contains coverage: 70.X% of statements for duty package
    - 46+ test functions present (TestDutyHandler_*)
    - Compile-time interface assertion present at top of file
    - No testify/mock import
  </acceptance_criteria>
  <done>
    IMP-01 SC met: api/v1/duty >=70% coverage.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build knowledge handler_test.go covering all 3 handler structs</name>
  <files>internal/api/v1/knowledge/handler_test.go</files>
  <read_first>
    - internal/api/v1/knowledge/handler.go
    - internal/api/v1/knowledge/router.go
    - internal/services/knowledge/knowledge_cache_impl.go
    - internal/api/v1/monitor/oper_log_handler_test.go (Phase 72 SHIPPED reference)
    - internal/models/operations/knowledge.go
  </read_first>
  <action>
Create internal/api/v1/knowledge/handler_test.go (package knowledge). Cover all 3 handler structs (ArticleHandler, CategoryHandler, TagHandler) with all their methods:

ArticleHandler (10): List, Statistics, GetByID, Create, Update, Delete, ConvertFromWorkOrder, Search, Like
CategoryHandler (6): List, GetByID, Create, Update, Delete
TagHandler (5): GetAll, Create, Update, Delete

Test fixture (mirror Phase 72 oper_log_handler_test.go pattern):
- mockKnowledgeService — implements knowledgeServices.KnowledgeCacheService with one function-field per interface method. Embed the interface as default.
- Compile-time assertion at top: var _ knowledgeServices.KnowledgeCacheService = (*mockKnowledgeService)(nil).
- setupTestKnowledgeHandler(mock *mockKnowledgeService, kind string) interface{} — returns *ArticleHandler / *CategoryHandler / *TagHandler based on kind switch. Each kind calls corresponding NewXxxHandler(mock).WithCore(&core.Core{CoreInfra: &core.CoreInfra{DB: &db.Database{}}, CoreServices: &core.CoreServices{}}).
- newTestCtxKnowledge(method, path string, body interface{}) (*gin.Context, *httptest.ResponseRecorder) — same pattern as Task 1.

For each handler method write 2+ test cases (happy + error):
- TC1: <handler>_<method>_success_returns_200 — set mock function-field to return seeded value; assert w.Code == 200.
- TC2: <handler>_<method>_service_error_returns_5xx — set mock func to return errors.New(...); assert non-200.

Bind path/URL params for GetByID-style methods via c.Params.

Skip methods requiring live Redis cache; skip operlog recording (h.core nil handled gracefully).

Explicit skip enumeration (for ≥70% math): handler.go ~265-273 stmts total per package. The 21-23 handler methods each ~10 stmts avg (~210-230 stmts) are covered via mock service; remaining ~35-55 stmts are: import block, struct init, SetupXxxRouter (covered indirectly via route registration). 0 methods skipped for live Redis cache — all handler methods use mock service, no direct Redis call paths in handler layer (Redis is encapsulated in service layer, which is mockable).

Post-skip expected coverage: ~230/265 = ~87% achievable (duty); ~210/273 = ~77% achievable (knowledge). If actual falls below 70%, contingency: add glebarez sqlite-based service mock paths for any uncovered branch, or add httptest-level auth middleware integration tests using service account fixture.

Naming: TestArticleHandler_<method>_<scenario> / TestCategoryHandler_<method>_<scenario> / TestTagHandler_<method>_<scenario>. Aim for 42+ test functions total.

Add TestKnowledgeHandlers_CompileOnly smoke test that constructs all 3 handler types with empty mock.
  </action>
  <verify>
    <automated>go test -count=1 -cover ./internal/api/v1/knowledge/... 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - go test -cover -count=1 ./internal/api/v1/knowledge/... exits 0
    - Output contains coverage: 70.X% of statements for knowledge package
    - 42+ test functions present (TestArticleHandler_* / TestCategoryHandler_* / TestTagHandler_*)
    - Compile-time interface assertion present at top of file
    - No testify/mock import
  </acceptance_criteria>
  <done>
    IMP-02 SC met: api/v1/knowledge >=70% coverage.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify combined coverage + write SUMMARY + commit</name>
  <files>.planning/phases/73-p1-pending/73-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/73-p1-pending/73-CONTEXT.md (SC checklist + D-locked decisions)
    - .planning/phases/73-p1-pending/73-VALIDATION.md (Nyquist 8-dim audit table)
    - .planning/templates/phase-summary.md (SUMMARY template)
  </read_first>
  <action>
Run combined coverage and capture per-package percentages. Use commands:

- go test -coverprofile=c.out -count=1 ./internal/api/v1/duty/ ./internal/api/v1/knowledge/
- go tool cover -func=c.out | grep -E "(duty_handler|knowledge|total)"
- Per-package check: go test -cover -count=1 ./internal/api/v1/duty/... and ./internal/api/v1/knowledge/... separately

Verify each package total line shows >=70.0%.

If coverage < 70%:
- Identify uncovered stmts via go tool cover -html=c.out -o coverage.html
- Add additional test cases for uncovered branches (error paths, switch defaults, validation edges)
- Re-run until >=70% per package

If still < 70% after 3 attempts:
- Document gap in 73-01-SUMMARY.md (do NOT lower bar — escalate via Phase 73 review)

Write .planning/phases/73-p1-pending/73-01-SUMMARY.md with:
- Files modified list (2 test files)
- Per-package coverage achieved
- Test count totals
- Nyquist 8-dim audit table per VALIDATION.md (PASS/PARTIAL/N/A per dimension)
- D-01..D-13 lock verification (each marked honored)
- Commit message draft: test(73-01): IMP-01 + IMP-02 handler tests 0% to 70%+
- SC#1 + SC#2 mapping checkboxes

Clean up c.out and coverage.html (do NOT commit).

Commit the test files + SUMMARY as a single commit on local branch (per Phase 72 W2 fix pattern).
  </action>
  <verify>
    <automated>go test -cover -count=1 ./internal/api/v1/duty/... ./internal/api/v1/knowledge/... 2>&1 | grep -E "^(ok|FAIL|coverage:)"</automated>
  </verify>
  <acceptance_criteria>
    - go test -cover -count=1 ./internal/api/v1/duty/... ./internal/api/v1/knowledge/... exits 0
    - Output contains coverage: 70.X% of statements for BOTH packages
    - 73-01-SUMMARY.md exists with coverage %, file list, Nyquist 8-dim audit, commit message draft
    - 1 git commit on local branch with the 2 test files + SUMMARY
  </acceptance_criteria>
  <done>
    IMP-01 + IMP-02 SCs met; both packages >=70%; test files committed.
  </done>
</task>

</tasks>

<verification>
- go test -cover -count=1 ./internal/api/v1/duty/... exits 0 with coverage >=70%
- go test -cover -count=1 ./internal/api/v1/knowledge/... exits 0 with coverage >=70%
- Both packages total line in go tool cover -func >=70.0%
- All handler methods across both packages have >=1 happy + >=1 error test
- No business code changes (D-12)
- No new mock framework (D-02)
</verification>

<success_criteria>
- [ ] IMP-01 SC#1: internal/api/v1/duty coverage >=70% (265 stmts)
- [ ] IMP-02 SC#2: internal/api/v1/knowledge coverage >=70% (273 stmts)
- [ ] All handler methods have >=1 happy-path + >=1 error-path coverage
- [ ] No new mock framework introduced (D-02)
- [ ] Zero business code changes (D-12)
</success_criteria>

<output>
Create .planning/phases/73-p1-pending/73-01-SUMMARY.md when done.
</output>