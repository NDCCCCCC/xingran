---
phase: 73-p1-pending
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/services/monitor/cache_service_test.go
  - internal/services/monitor/login_log_service_test.go
  - internal/services/monitor/oper_log_service_test.go
  - internal/services/monitor/server_service_test.go
autonomous: true
requirements:
  - IMP-06
user_setup: []

must_haves:
  truths:
    - "go test -cover -count=1 ./internal/services/monitor/... exits 0 with coverage >= 70% (485 stmts)"
    - "oper_log_service is NOT exempted (per D-03 strict-balance principle); uses ad_account pattern (glebarez sqlite + real service)"
    - "cache_service uses portwrite pure-mock pattern (CacheProvider mocked)"
    - "login_log_service + server_service use ad_account pattern (glebarez sqlite + real service + glebarez in-memory DB)"
  artifacts:
    - path: "internal/services/monitor/cache_service_test.go"
      provides: "Pure-mock tests for monitor/cache_service.go"
      contains: "package monitor"
    - path: "internal/services/monitor/login_log_service_test.go"
      provides: "DB-backed tests for monitor/login_log_service.go"
      contains: "package monitor"
    - path: "internal/services/monitor/oper_log_service_test.go"
      provides: "DB-backed tests for monitor/oper_log_service.go (D-03 non-exempt)"
      contains: "package monitor"
    - path: "internal/services/monitor/server_service_test.go"
      provides: "DB-backed tests for monitor/server_service.go"
      contains: "package monitor"
  key_links:
    - from: "internal/services/monitor/cache_service_test.go"
      to: "internal/services/monitor/cache_service.go"
      via: "real cacheServiceImpl + mocked CacheProvider + testify/mock"
      pattern: "var _ CacheProvider = \\(\\*mockCacheProvider\\)"
    - from: "internal/services/monitor/login_log_service_test.go"
      to: "internal/services/monitor/login_log_service.go"
      via: "real loginLogService + glebarez sqlite in-memory + CREATE TABLE sys_login_log"
      pattern: "db\\.Exec\\(`CREATE TABLE sys_login_log"
    - from: "internal/services/monitor/oper_log_service_test.go"
      to: "internal/services/monitor/oper_log_service.go"
      via: "real operLogService + glebarez sqlite in-memory + CREATE TABLE sys_oper_log"
      pattern: "db\\.Exec\\(`CREATE TABLE sys_oper_log"
    - from: "internal/services/monitor/server_service_test.go"
      to: "internal/services/monitor/server_service.go"
      via: "real serverServiceImpl + glebarez sqlite in-memory + CREATE TABLE sys_server_info + sys_system_metrics"
      pattern: "db\\.Exec\\(`CREATE TABLE sys_server_info"
---

<objective>
Add service tests for the medium P1 package internal/services/monitor (485 stmts, 5 files) using a mixed strategy per D-02: cache_service.go uses portwrite pure-mock pattern; login_log_service.go + oper_log_service.go + server_service.go use ad_account pattern (glebarez sqlite + real service).

Purpose: Phase 73 fourth delivery per D-01 (services 中等). Per D-03, oper_log_service is NOT exempted (strict-balance principle); the Phase 72 api/v1/monitor/oper_log_handler 71.2% coverage does not substitute for service-level coverage. The mixed strategy reflects that cache_service depends on CacheProvider (mockable) while login/oper/server services depend on *gorm.DB directly (require real DB).

Output: 4 test files (one per non-types service file), pushing the package from 0.0% to ≥70%.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-p1-pending/73-CONTEXT.md
@.planning/phases/73-p1-pending/73-RESEARCH.md
@.planning/phases/73-p1-pending/73-VALIDATION.md
@.planning/REQUIREMENTS.md

Read these source files first (D-02 双范本 + 目标 + Phase 72 oper_log DDL):
- internal/services/portwrite/port_write_service_test.go — D-02 service complete pattern (portwrite: interface assertion + testify/mock + pure mock)
- internal/api/v1/system/ad_account_handler_test.go — D-02 handler lightweight pattern (ad_account: glebarez sqlite + real service)
- internal/api/v1/monitor/oper_log_handler_test.go — Phase 72 SHIPPED reference; READ for oper_log DDL to reuse in oper_log_service_test.go
- internal/services/monitor/cache_service.go (853 lines) — cacheServiceImpl with NewCacheService (line 163) + methods GetCacheList/OperateCache/BatchOperateCache/ClearCache/GetCacheStats/GetCacheMonitor/ExportCache/GetCacheConfigs/UpdateCacheConfig/ReloadCacheConfigs
- internal/services/monitor/login_log_service.go (176 lines) — loginLogService with NewLoginLogService (line 32) + methods List/GetByID/Delete/BatchDelete/Clean
- internal/services/monitor/oper_log_service.go (180 lines) — operLogService with NewOperLogService (line 32) + methods List/GetByID/Delete/BatchDelete/Clean
- internal/services/monitor/server_service.go (261 lines) — serverServiceImpl with NewServerService (line 77) + methods GetServerInfo/GetCurrentServerMetrics/SaveSystemMetrics/GetSystemMetricsHistory + helpers
- internal/services/monitor/types.go (9 lines) — type/param definitions (CacheListParams, CacheOperateParams, etc.)
- internal/pkg/cache/cache.go — CacheProvider interface (mock in cache_service_test.go)
- internal/models/monitor.go + internal/models/operations/server.go — model structs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build cache_service_test.go using portwrite pure-mock pattern</name>
  <files>internal/services/monitor/cache_service_test.go</files>
  <read_first>
    - internal/services/monitor/cache_service.go (cacheServiceImpl + all methods + helpers)
    - internal/services/monitor/types.go (CacheListParams, CacheOperateParams, etc.)
    - internal/services/portwrite/port_write_service_test.go (D-02 service complete pattern)
    - internal/pkg/cache/cache.go (CacheProvider interface)
  </read_first>
  <action>
Create internal/services/monitor/cache_service_test.go (package monitor). Mirror portwrite pure-mock pattern.

Test fixture:
- mockCacheProvider — embeds mock.Mock, implements CacheProvider interface (GetOrSet, Delete, DeleteByPattern, MGet, MDelete)
- Compile-time assertion: var _ CacheProvider = (*mockCacheProvider)(nil)
- setupTestCacheService(mock *mockCacheProvider) *cacheServiceImpl — constructs via NewCacheService(mockCacheProvider)

Cover all public methods (~14):
- GetCacheList, GetCacheInfo, OperateCache, BatchOperateCache, ClearCache
- GetCacheStats, GetCacheMonitor, ExportCache
- GetCacheConfigs, UpdateCacheConfig, ReloadCacheConfigs
- normalizeCacheKeyForService (helper), isSystemKeyForService (helper)
- formatCacheStatsForService (helper), convertToCacheStats (helper)

For each method ≥1 happy + ≥1 error test case (portwrite 3-case pattern).

Naming: TestCacheService_<Method>_<Scenario>. Aim for 30+ test functions.

Note: cache_service.go has helper functions like normalizeCacheKeyForService, isSystemKeyForService, formatCacheStatsForService, convertToCacheStats — these should also be tested directly via TestNormalizeCacheKeyForService / TestIsSystemKeyForService / etc.

Add TestCacheService_CompileOnly smoke test.
  </action>
  <verify>
    <automated>go test -count=1 -cover ./internal/services/monitor/... -run TestCacheService 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - go test -cover -count=1 ./internal/services/monitor/... -run TestCacheService exits 0
    - 30+ test functions present
    - Compile-time interface assertion present at top
    - testify/mock import present
  </acceptance_criteria>
  <done>
    cache_service sub-package covered via portwrite pattern.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build login_log_service_test.go using ad_account pattern (glebarez sqlite + real service)</name>
  <files>internal/services/monitor/login_log_service_test.go</files>
  <read_first>
    - internal/services/monitor/login_log_service.go (loginLogService + List/GetByID/Delete/BatchDelete/Clean)
    - internal/api/v1/monitor/login_log_handler_test.go (Phase 72 SHIPPED — READ for sys_login_log DDL to reuse)
    - internal/models/monitor.go (LoginLog struct)
    - internal/api/v1/system/ad_account_handler_test.go (D-02 ad_account pattern reference for DB setup)
  </read_first>
  <action>
Create internal/services/monitor/login_log_service_test.go (package monitor). Mirror ad_account pattern with real DB.

Test fixture:
- setupTestLoginLogDB(t *testing.T) *gorm.DB — glebarez sqlite in-memory + CREATE TABLE sys_login_log (DDL matching internal/models/monitor.go LoginLog fields: id TEXT PK, username TEXT, ip TEXT, status INTEGER, message TEXT, login_time DATETIME, created_at/updated_at/deleted_at). Reuse DDL from Phase 72 internal/api/v1/monitor/login_log_handler_test.go if available.
- newLoginLogService(db *gorm.DB) *loginLogService — constructs via NewLoginLogService(db)
- Seed helper: seedLoginLog(t, db, *models.LoginLog) — INSERT INTO sys_login_log with uuid.NewString() id

For each method write ≥2 test cases (happy + edge):
- List — TC1: empty list, TC2: list with seeded rows + filters (username/status/time range), TC3: pagination (page 1 size 10), TC4: service error
- GetByID — TC1: existing row, TC2: not found
- Delete — TC1: existing row, TC2: not found
- BatchDelete — TC1: multiple ids, TC2: empty ids, TC3: service error
- Clean — TC1: cleanup all rows

Naming: TestLoginLogService_<Method>_<Scenario>. Aim for 15+ test functions.

Add TestLoginLogService_CompileOnly smoke test.
  </action>
  <verify>
    <automated>go test -count=1 -cover ./internal/services/monitor/... -run TestLoginLogService 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - go test -cover -count=1 exits 0
    - 15+ test functions present
    - sys_login_log table DDL matches production schema (TEXT id PK, INTEGER status, DATETIME login_time, soft-delete columns)
    - Seed helper inserts rows that List/GetByID/Delete can manipulate
  </acceptance_criteria>
  <done>
    login_log_service sub-package covered via ad_account pattern.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build oper_log_service_test.go using ad_account pattern (D-03 strict-balance, NOT exempted)</name>
  <files>internal/services/monitor/oper_log_service_test.go</files>
  <read_first>
    - internal/services/monitor/oper_log_service.go (operLogService + List/GetByID/Delete/BatchDelete/Clean)
    - internal/api/v1/monitor/oper_log_handler_test.go (Phase 72 SHIPPED — READ for sys_oper_log DDL to reuse; D-03 mandates no exemption)
    - internal/models/monitor.go (OperLog struct)
    - internal/api/v1/system/ad_account_handler_test.go (D-02 ad_account pattern reference)
  </read_first>
  <action>
Create internal/services/monitor/oper_log_service_test.go (package monitor). Mirror ad_account pattern with real DB.

D-03 LOCK: oper_log_service is NOT exempted. Service-level testing is independent of handler-level (Phase 72 already at 71.2% on handler side). Per D-04 strict-balance principle, handler 71.2% does not substitute for service >=70%.

Test fixture:
- setupTestOperLogDB(t *testing.T) *gorm.DB — glebarez sqlite in-memory + CREATE TABLE sys_oper_log (DDL from internal/models/monitor.go OperLog fields: id TEXT PK, title TEXT, business_type TEXT, method TEXT, request_method TEXT, oper_name TEXT, oper_url TEXT, oper_ip TEXT, oper_param TEXT, json_result TEXT, status INTEGER, error_msg TEXT, oper_time DATETIME, cost_time BIGINT, created_at/updated_at/deleted_at). Reuse DDL from Phase 72 internal/api/v1/monitor/oper_log_handler_test.go if available.
- newOperLogService(db *gorm.DB) *operLogService — constructs via NewOperLogService(db)
- Seed helper: seedOperLog(t, db, *models.OperLog) — INSERT INTO sys_oper_log with uuid.NewString() id

For each method write ≥2 test cases (happy + edge):
- List — TC1: empty list, TC2: list with seeded rows + filters (title/business_type/oper_name/status/time range), TC3: pagination, TC4: service error
- GetByID — TC1: existing row, TC2: not found
- Delete — TC1: existing row, TC2: not found
- BatchDelete — TC1: multiple ids, TC2: empty ids
- Clean — TC1: cleanup all rows

Naming: TestOperLogService_<Method>_<Scenario>. Aim for 15+ test functions.

Add TestOperLogService_CompileOnly smoke test.

Note: D-03 emphasizes this is NOT exempted; document in test file header comment.
  </action>
  <verify>
    <automated>go test -count=1 -cover ./internal/services/monitor/... -run TestOperLogService 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - go test -cover -count=1 exits 0
    - 15+ test functions present
    - sys_oper_log table DDL matches production schema (TEXT id PK, INTEGER status, BIGINT cost_time, soft-delete columns)
    - File header comment notes D-03 (NOT exempted) for future reference
  </acceptance_criteria>
  <done>
    D-03 honored: oper_log_service covered via ad_account pattern (no exemption).
  </done>
</task>

<task type="auto">
  <name>Task 4: Build server_service_test.go using ad_account pattern (mixed real DB + MetricsProvider mock)</name>
  <files>internal/services/monitor/server_service_test.go</files>
  <read_first>
    - internal/services/monitor/server_service.go (serverServiceImpl + GetServerInfo/GetCurrentServerMetrics/SaveSystemMetrics/GetSystemMetricsHistory + helpers)
    - internal/services/monitor/types.go (MetricsHistoryParams struct)
    - internal/models/operations/server.go (ServerInfo + SystemMetrics structs)
    - internal/api/v1/monitor/server_handler_test.go (Phase 72 SHIPPED — READ for sys_server_info / sys_system_metrics DDL)
  </read_first>
  <action>
Create internal/services/monitor/server_service_test.go (package monitor). Mixed approach: glebarez sqlite for DB methods + testify/mock for MetricsProvider interface.

Test fixture:
- setupTestServerDB(t *testing.T) *gorm.DB — glebarez sqlite in-memory + CREATE TABLE sys_server_info (id TEXT PK, hostname TEXT, ip TEXT, os TEXT, cpu_usage REAL, mem_usage REAL, disk_usage REAL, status INTEGER, last_heartbeat DATETIME, created_at/updated_at/deleted_at) + CREATE TABLE sys_system_metrics (id TEXT PK, server_id TEXT, cpu_usage REAL, mem_usage REAL, disk_usage REAL, network_in BIGINT, network_out BIGINT, recorded_at DATETIME, created_at/updated_at/deleted_at). Reuse DDL from Phase 72 internal/api/v1/monitor/server_handler_test.go if available.
- mockMetricsProvider — embeds mock.Mock, implements monitorServices.MetricsProvider interface (INSPECT server_service.go for exact method signature; typically GetCurrentSystemMetrics() (*SystemMetricsData, error))
- Compile-time assertion: var _ MetricsProvider = (*mockMetricsProvider)(nil) (if applicable)
- newServerService(db *gorm.DB, provider MetricsProvider) *serverServiceImpl — constructs via NewServerService(db, provider)
- Seed helpers: seedServerInfo, seedSystemMetrics

For each method write ≥2 test cases:
- GetServerInfo — TC1: empty list, TC2: list with seeded rows + filters, TC3: pagination
- GetCurrentServerMetrics — TC1: mock provider returns seeded metrics, TC2: mock provider returns error
- SaveSystemMetrics — TC1: valid metrics row inserted, TC2: error path
- GetSystemMetricsHistory — TC1: empty, TC2: with seeded rows + time range filter
- convertToServerInfo (helper) — TestConvertToServerInfo_Valid / TestConvertToServerInfo_Empty
- getCurrentServerInfo (helper) — TestGetCurrentServerInfo_PopulatedFields
- getCurrentMetricsRealtime (helper) — TestGetCurrentMetricsRealtime_Defaults

Aim for 15+ test functions. Add TestServerService_CompileOnly smoke test.
  </action>
  <verify>
    <automated>go test -count=1 -cover ./internal/services/monitor/... -run TestServerService 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - go test -cover -count=1 exits 0
    - 15+ test functions present
    - sys_server_info + sys_system_metrics table DDL match production schema
    - MetricsProvider mock satisfies interface via compile-time assertion
    - Helper functions (convertToServerInfo, getCurrentServerInfo, getCurrentMetricsRealtime) covered
  </acceptance_criteria>
  <done>
    server_service sub-package covered via mixed ad_account + MetricsProvider mock pattern.
  </done>
</task>

<task type="auto">
  <name>Task 5: Verify combined coverage + write SUMMARY + commit</name>
  <files>.planning/phases/73-p1-pending/73-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/73-p1-pending/73-CONTEXT.md (SC checklist + D-03 lock)
    - .planning/phases/73-p1-pending/73-VALIDATION.md (Nyquist 8-dim audit)
  </read_first>
  <action>
Run combined coverage and capture per-package percentage:

Commands:
- go test -coverprofile=c.out -count=1 ./internal/services/monitor/
- go tool cover -func=c.out | grep -E "(monitor|cache_service|login_log|oper_log|server_service|total)"

Verify package total line shows >=70.0%.

If coverage < 70%:
- Identify uncovered stmts via go tool cover -html=c.out -o coverage.html
- Add additional test cases for uncovered branches (likely: edge cases in cache invalidation, helper functions, error paths)
- Re-run until >=70%

If still < 70% after 3 attempts:
- Document gap in 73-04-SUMMARY.md (do NOT lower bar)

Write .planning/phases/73-p1-pending/73-04-SUMMARY.md with:
- Files modified list (4 test files)
- Per-file coverage breakdown
- Test count totals
- Mixed strategy documentation (cache_service portwrite + login/oper/server ad_account)
- D-03 verification (oper_log NOT exempted)
- Nyquist 8-dim audit table per VALIDATION.md
- D-01..D-13 lock verification
- Commit message draft: test(73-04): IMP-06 monitor services tests 0% to 70%+
- SC#6 mapping checkboxes (services/monitor portion)

Clean up c.out and coverage.html (do NOT commit).

Commit the 4 test files + SUMMARY as a single commit on local branch (per Phase 72 W2 fix pattern).
  </action>
  <verify>
    <automated>go test -cover -count=1 ./internal/services/monitor/... 2>&1 | grep -E "^(ok|FAIL|coverage:)"</automated>
  </verify>
  <acceptance_criteria>
    - go test -cover -count=1 ./internal/services/monitor/... exits 0
    - Output contains coverage: 70.X% of statements for monitor service package
    - 73-04-SUMMARY.md exists with coverage %, file list, mixed strategy doc, D-03 verification, Nyquist audit, commit message draft
    - 1 git commit on local branch with the 4 test files + SUMMARY
  </acceptance_criteria>
  <done>
    IMP-06 (monitor portion) SC met: services/monitor >=70% coverage; D-03 honored.
  </done>
</task>

</tasks>

<verification>
- go test -cover -count=1 ./internal/services/monitor/... exits 0 with coverage >=70% (485 stmts)
- Package total line in go tool cover -func >=70.0%
- cache_service uses portwrite pure-mock (interface assertion + testify/mock)
- login_log_service + oper_log_service + server_service use ad_account pattern (glebarez sqlite + real service)
- oper_log_service NOT exempted (D-03 strict-balance honored)
- No business code changes (D-12)
- No new mock framework (D-02)
</verification>

<success_criteria>
- [ ] IMP-06 SC#6: services/monitor coverage >=70% (485 stmts, includes oper_log_service per D-03)
- [ ] Mixed strategy correctly applied (cache_service portwrite + others ad_account)
- [ ] D-03 honored: oper_log_service covered (not exempted)
- [ ] No new mock framework introduced (D-02)
- [ ] Zero business code changes (D-12)
</success_criteria>

<output>
Create .planning/phases/73-p1-pending/73-04-SUMMARY.md when done.
</output>