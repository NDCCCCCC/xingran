---
phase: 73-p1-pending
plan: 05
type: execute
wave: 5
depends_on:
  - 73-01
  - 73-02
  - 73-03
  - 73-04
files_modified:
  - .github/scripts/check-coverage.sh
  - .coverage-threshold
  - .planning/coverage-baseline.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/73-p1-pending/73-05-SUMMARY.md
autonomous: true
requirements:
  - IMP-01
  - IMP-02
  - IMP-03
  - IMP-04
  - IMP-05
  - IMP-06
user_setup: []

must_haves:
  truths:
    - "check-coverage.sh extended to validate per-package ≥70% for the 8 P1 targets (api/v1/duty + api/v1/knowledge + api/v1/rpa + api/v1/vdi + services/duty + services/knowledge + services/network + services/monitor) AND weighted total ≥ new ratchet value"
    - ".coverage-threshold updated from 21.5 to new value (estimated 26-30%, exact from actual measurement)"
    - ".planning/coverage-baseline.md has new Phase 73 后 row with commit SHA, weighted avg, total stmts, 8 P1 packages per-package %"
    - "All test files added by plans 73-01..73-04 collected in a single batch commit (Task 0) BEFORE the ratchet commit"
    - "Atomic ratchet commit (D-07 + D-11) includes: .coverage-threshold + .planning/coverage-baseline.md + .github/scripts/check-coverage.sh + .planning/STATE.md + .planning/ROADMAP.md + 73-05-SUMMARY.md"
    - "CI gate (ci.yml Coverage gate step) runs check-coverage.sh and exits 0 with new threshold"
    - "Phase 73 honors D-08 (test pattern consistency with Phase 71/72 — ad_account_handler_test.go + port_write_service_test.go范本 only, no new patterns) and D-09 (real middleware + real encryption — JWT + SM2+SM4 真实路径, no mockSM4Cipher stub)"
  artifacts:
    - path: ".github/scripts/check-coverage.sh"
      provides: "Extended coverage gate with per-package validation for 8 P1 packages"
      contains: "p1_package_check"
    - path: ".coverage-threshold"
      provides: "New threshold value ~27-30% (ratcheted from 21.5)"
      contains: "^[0-9]+\\."
    - path: ".planning/coverage-baseline.md"
      provides: "Phase 73 后 row appended with 8 P1 packages per-package % table"
      contains: "Phase 73 后"
    - path: ".planning/phases/73-p1-pending/73-05-SUMMARY.md"
      provides: "Final phase summary with coverage delta + commit SHA"
      contains: "coverage ratchet"
  key_links:
    - from: ".github/scripts/check-coverage.sh"
      to: "coverage.out"
      via: "awk parses atomic profile"
      pattern: "awk.*coverage\\.out"
    - from: ".github/workflows/ci.yml"
      to: ".github/scripts/check-coverage.sh"
      via: "Coverage gate step runs the bash script"
      pattern: "bash \\.github/scripts/check-coverage\\.sh"
---

<objective>
Collect all per-package *_test.go files added by plans 73-01..73-04 into a single batch commit (Task 0, resolves W2 commit ambiguity). Then extend the CI coverage gate to validate per-package ≥70% for the 8 P1 targets (D-04 + D-10), ratchet .coverage-threshold to new value, append .planning/coverage-baseline.md Phase 73 后 row, update STATE.md + ROADMAP.md, and commit atomically per D-07 (Tasks 3 + 4).

Purpose: Phase 73 closes the P1 loop — without this ratchet, the work has no enforcement. The gate now checks BOTH weighted average AND each P1 target sub-package, preventing silent regression.

Output: 2 commits total: (a) Task 0 batch commit of test files; (b) Tasks 3+4 atomic ratchet commit containing 6 files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-p1-pending/73-CONTEXT.md
@.planning/phases/73-p1-pending/73-RESEARCH.md
@.planning/phases/73-p1-pending/73-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/coverage-baseline.md
@.planning/quick/260820-backend-test-coverage-scan/per-package-coverage.txt

Read these source files first:
- .github/scripts/check-coverage.sh (current Phase 71 version with Phase 72 extension; further extend it)
- .github/workflows/ci.yml (CI Coverage gate step — verify step name + invocation)
- .coverage-threshold (current 21.5)
- .planning/coverage-baseline.md (Phase 72 后 row schema)
- .planning/phases/73-p1-pending/73-{01..04}-SUMMARY.md (per-plan coverage data + test files added)
- .planning/phases/72-p0-core-supplement/72-13-PLAN.md (reference ratchet plan structure)
- .planning/STATE.md (current state to update in ratchet commit)
- .planning/ROADMAP.md (Phase 73 entry to mark SHIPPED in ratchet commit)
</context>

<tasks>

<task type="auto">
  <name>Task 0: Batch commit all new P1 test files across the phase</name>
  <files>
internal/api/v1/duty/duty_handler_test.go
internal/api/v1/knowledge/handler_test.go
internal/api/v1/rpa/task_handler_test.go
internal/api/v1/rpa/worker_handler_test.go
internal/api/v1/rpa/worker_router_public_test.go
internal/api/v1/rpa/execution_handler_test.go
internal/api/v1/rpa/credential_handler_test.go
internal/api/v1/rpa/ai_handler_test.go
internal/api/v1/rpa/flow_handler_test.go
internal/api/v1/rpa/rpa_router_test.go
internal/api/v1/vdi/vdi_server_handler_test.go
internal/api/v1/vdi/vm_handler_test.go
internal/api/v1/vdi/base_handler_test.go
internal/api/v1/vdi/vdi_router_test.go
internal/services/duty/duty_cache_impl_test.go
internal/services/knowledge/knowledge_cache_impl_test.go
internal/services/network/cache_impl_test.go
internal/services/monitor/cache_service_test.go
internal/services/monitor/login_log_service_test.go
internal/services/monitor/oper_log_service_test.go
internal/services/monitor/server_service_test.go
internal/services/monitor/types_test.go
  </files>
  <read_first>
    - .planning/phases/73-p1-pending/73-{01..04}-SUMMARY.md (enumerate test files created per plan)
    - git status (verify all per-plan test files are untracked or staged)
  </read_first>
  <action>
Per W2 fix (same as Phase 72 Plan 72-13 Task 0) — collect all new *_test.go files added by plans 73-01..73-04 into a single batch commit BEFORE the ratchet commit. This resolves the "Do NOT commit" vs "commit only 4 files" ambiguity: per-module plans continue to instruct "Do NOT commit" (since Task 0 owns the batch commit), and Task 0 produces one canonical commit for all test files.

Steps:
1. Enumerate all *_test.go files added by plans 73-01..73-04 (see each plan files_modified). List must be exhaustive — read each plan SUMMARY to confirm.
2. Stage ONLY *_test.go files (no business code, per D-12). Use git add with explicit paths to avoid accidental staging of unrelated files.
3. Verify staged diff is test-only: git diff --cached --stat must show only *_test.go paths.
4. Commit with message format: test(73): add P1 test files (X new test files across Y packages) where X = total count of staged test files and Y = distinct package directories.
5. Capture the resulting commit SHA: git rev-parse HEAD — this is the TBD_SHA placeholder for Task 3 (Phase 71 D-04 amend pattern).

DO NOT include:
- Any _test.go files that existed before Phase 73 (preserve per D-12)
- Any business code (no *.go non-test changes per D-12)
- Any docs / scripts / config changes (those go in the ratchet commit per Task 3)

If git push is needed for downstream CI, push immediately after this commit so the ratchet commit can reference the upstream test files in CI.
  </action>
  <verify>
    <automated>git log --oneline -3 && git diff --stat HEAD~1 HEAD | grep -v _test.go | wc -l</automated>
  </verify>
  <done>
Batch commit containing all new *_test.go files from plans 73-01..73-04; SHA captured for Task 3 reference.
  </done>
</task>

<task type="auto">
  <name>Task 1: Capture actual coverage measurement (pre-ratchet)</name>
  <files>.planning/phases/73-p1-pending/73-05-SUMMARY.md</files>
  <read_first>
    - .planning/coverage-baseline.md (current state — Phase 72 后 row)
    - .planning/phases/73-p1-pending/73-{01..04}-SUMMARY.md (per-plan coverage numbers)
  </read_first>
  <action>
Run full backend test coverage and capture per-package %:

Commands:
- cd /path/to/repo && go test -timeout 15m -count=1 -coverprofile=coverage.out ./internal/... ./pkg/... ./cmd/... 2>&1 | tail -5
- go tool cover -func=coverage.out > cover-func.txt

Capture via concrete awk commands (per W3 — no placeholders):
- Weighted average: awk '/weighted avg/ {print $NF}' coverage.out
- Total stmts: awk '/^total:/ {print $2}' cover-func.txt
- Total covered: awk '/^total:/ {print $3}' cover-func.txt

Verify all 8 P1 targets individually pass ≥70%:
- IMP-01: internal/api/v1/duty (265 stmts)
- IMP-02: internal/api/v1/knowledge (273 stmts)
- IMP-03: internal/api/v1/rpa (612 stmts, incl. public router 3 endpoints)
- IMP-04: internal/api/v1/vdi (298 stmts)
- IMP-05: internal/services/duty + internal/services/knowledge (114 + 85 stmts)
- IMP-06: internal/services/network + internal/services/monitor (127 + 485 stmts, incl. oper_log_service)

If any P1 target < 70%:
- ESCALATE — do NOT proceed with ratchet. Identify which plan(s) underdelivered, add gap-closure tasks in 73-06-PLAN.md (created ad-hoc if needed)
- Do NOT ratchet below the actual measurement

Save raw output to a temp file for use in Task 3.
  </action>
  <verify>
    <automated>go test -cover -count=1 -timeout 15m ./internal/api/v1/duty/ ./internal/api/v1/knowledge/ ./internal/api/v1/rpa/ ./internal/api/v1/vdi/ ./internal/services/duty/ ./internal/services/knowledge/ ./internal/services/network/ ./internal/services/monitor/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 8 P1 packages exit 0 with coverage: ≥70.0%
    - Raw measurement saved to temp file (cover-func.txt)
    - Concrete values captured for: weighted_avg, total_stmts, total_covered (no placeholders)
    - Per-package % table captured for the 8 P1 targets + remaining packages
  </acceptance_criteria>
  <done>
Pre-ratchet coverage measured and verified; all 8 P1 targets ≥70%.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend check-coverage.sh for 8 P1 package per-package validation</name>
  <files>.github/scripts/check-coverage.sh</files>
  <read_first>
    - .github/scripts/check-coverage.sh (current Phase 71+72 version — extend it further for 8 P1 packages)
    - .github/workflows/ci.yml (Coverage gate step)
    - .planning/phases/73-p1-pending/73-CONTEXT.md (8 P1 targets)
  </read_first>
  <action>
Extend .github/scripts/check-coverage.sh to validate BOTH:
1. Weighted average ≥ .coverage-threshold (current behavior — preserve)
2. Per-package ≥ 70% for the 8 P1 targets (Phase 73 IMP-01..06)

Add a new section after the existing weighted-avg block. The implementation should iterate each P1 package, parse coverage.out for matching files using awk (format: pkg:file:stmt:count), sum stmts + covered across matching files, compare weighted % against 70.0% floor, and on failure print which P1 package failed and exit non-zero.

P1_PACKAGE_TARGETS (8 entries, each is a Go package path):
- internal/api/v1/duty
- internal/api/v1/knowledge
- internal/api/v1/rpa
- internal/api/v1/vdi
- internal/services/duty
- internal/services/knowledge
- internal/services/network
- internal/services/monitor

Preserve backward compatibility:
- Existing weighted-avg gate behavior unchanged
- Existing Phase 72 per-sub-package CORE validation (CORE-01..06, 28 cells) unchanged
- New per-package P1 check is additive (does not affect existing PASS/FAIL semantics)

Add a new exit code 4 for P1 package failures (distinct from exit 1 weighted avg failure and exit 3 per-sub-package failure) — optional but improves debugging.

Add tests: create .github/scripts/check-coverage-p1-test.sh with fixtures that verify the script correctly:
- Passes when all 8 P1 packages ≥70%
- Fails when one P1 package <70%
- Preserves weighted-avg + Phase 72 per-sub-package behavior
  </action>
  <verify>
    <automated>bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - check-coverage.sh exits 0 when all 8 P1 packages pass (verified with current measurement)
    - Per-package validation block added with 8 P1 packages enumerated
    - Existing weighted-avg gate still functions (backward compatibility)
    - Existing Phase 72 per-sub-package CORE validation still functions (backward compatibility)
    - Script remains bash + awk only (no new dependencies, per D-06)
  </acceptance_criteria>
  <done>
check-coverage.sh extended for 8 P1 package validation; both old and new checks coexist.
  </done>
</task>

<task type="auto">
  <name>Task 3: Atomic ratchet commit (6 files)</name>
  <files>
.coverage-threshold
.planning/coverage-baseline.md
.github/scripts/check-coverage.sh
.planning/STATE.md
.planning/ROADMAP.md
.planning/phases/73-p1-pending/73-05-SUMMARY.md
  </files>
  <read_first>
    - .coverage-threshold (current 21.5)
    - .planning/coverage-baseline.md (Phase 72 后 row schema)
    - .planning/phases/73-p1-pending/73-{01..04}-SUMMARY.md (per-plan coverage data)
    - .planning/STATE.md (current state to update)
    - .planning/ROADMAP.md (Phase 73 entry to mark SHIPPED)
  </read_first>
  <action>
Atomic ratchet per D-07 — single commit modifying 6 files. Per D-05 + D-11, STATE.md + ROADMAP.md updates go in THIS ratchet commit (not a follow-up) so all docs land together.

Concrete commands (per W3 — no placeholders):

1. .coverage-threshold: write new value from Task 1 measurement, e.g. 27.5

2. .planning/coverage-baseline.md: append Phase 73 后 section:
   - weighted_avg column: value from `awk '/weighted avg/ {print $NF}' coverage.out`
   - total_stmts column: value from `awk '/^total:/ {print $2}' cover-func.txt`
   - total_covered column: value from `awk '/^total:/ {print $3}' cover-func.txt`
   - commit column: value from `git rev-parse HEAD` (after Task 0 batch commit; same amend pattern as Phase 71 D-04)
   - 8 P1 packages per-package % table (one row per package, % from cover-func.txt)

3. .github/scripts/check-coverage.sh: extended per Task 2 (already staged for this commit)

4. .planning/STATE.md: update progress table row for Phase 73 to SHIPPED with commit SHA; update "Current Position" section

5. .planning/ROADMAP.md: update Phase 73 entry status from Pending to SHIPPED with commit SHA + date

6. .planning/phases/73-p1-pending/73-05-SUMMARY.md: write phase-end summary with:
   - Coverage delta (21.5% → NEW_VALUE%)
   - Per-IMP target achieved status (all 6)
   - 8 P1 packages per-package % table
   - Commit SHA (Task 0 batch SHA + ratchet SHA)
   - Phase executor model name
   - Test count total across all plans (73-01..73-04)
   - Files modified total across all plans (73-01..73-04)
   - Task 0 batch commit reference

After writing all 6 files:
- git add .coverage-threshold .planning/coverage-baseline.md .github/scripts/check-coverage.sh .planning/STATE.md .planning/ROADMAP.md .planning/phases/73-p1-pending/73-05-SUMMARY.md
- git commit -m "docs(73): coverage ratchet 21.5% → $(awk '/weighted avg/ {print $NF}' coverage.out)%

- IMP-01..06 all ≥70% per-package
- check-coverage.sh extended for 8 P1 package per-package validation
- coverage-baseline.md Phase 73 后 row appended
- STATE.md + ROADMAP.md marked SHIPPED per D-11
- 4 plan files (73-01..73-04) covered by Task 0 batch commit

Co-Authored-By: Claude <noreply@anthropic.com>"

If git push fails because branch is protected (Phase 71 had this issue — see cfec2c4), adapt: create a new commit instead of amend. Document in SUMMARY.

CRITICAL: All 6 file modifications MUST be in a single commit (D-07 atomic ratchet).
  </action>
  <verify>
    <automated>git log --oneline -3 && cat .coverage-threshold && grep -c "Phase 73 后" .planning/coverage-baseline.md</automated>
  </verify>
  <acceptance_criteria>
    - Single commit contains all 6 files: .coverage-threshold + .planning/coverage-baseline.md + .github/scripts/check-coverage.sh + .planning/STATE.md + .planning/ROADMAP.md + 73-05-SUMMARY.md
    - .coverage-threshold value > 21.5 (ratchet UP only)
    - .planning/coverage-baseline.md has "Phase 73 后" section with full per-package + 8 P1 packages per-package tables, populated with concrete values from Task 1
    - .planning/STATE.md and .planning/ROADMAP.md updated to mark Phase 73 SHIPPED (per D-11 — in ratchet commit)
    - Commit message starts with docs(73): coverage ratchet
    - git log --oneline -1 shows the ratchet commit on the current branch
    - Prior Task 0 batch commit contains only *_test.go files
  </acceptance_criteria>
  <done>
Phase 73 atomic ratchet committed; coverage-baseline.md updated; CI gate extended; STATE.md + ROADMAP.md marked SHIPPED.
  </done>
</task>

<task type="auto">
  <name>Task 4: CI verification + final SUMMARY closeout</name>
  <files>.planning/phases/73-p1-pending/73-05-SUMMARY.md</files>
  <read_first>
    - .github/workflows/ci.yml (Coverage gate step)
    - .planning/phases/73-p1-pending/73-05-SUMMARY.md (current draft)
  </read_first>
  <action>
Per D-11: STATE.md and ROADMAP.md updates were already shipped in the ratchet commit (Task 3) so all docs land together per D-07. This task focuses solely on CI verification + SUMMARY closeout.

Verify CI gate will pass with new ratchet value:

Commands (push + watch):
- git push origin main 2>&1 | tail -5
- gh run watch --repo OWNER/REPO --exit-status 2>&1 | tail -20

Or if local validation only:
- go test -timeout 15m -count=1 -coverprofile=c.out ./internal/... ./pkg/... ./cmd/...
- bash .github/scripts/check-coverage.sh c.out .coverage-threshold

Both should exit 0 with the new threshold. If CI fails:
- Diagnose the failing step
- Fix the underlying issue (likely: a test fails on CI runner but passed locally — investigate env difference)
- Re-push

If CI passes:
- Capture CI run URL for SUMMARY
- Finalize 73-05-SUMMARY.md with:
  - Coverage delta (21.5% → NEW_VALUE%)
  - Per-IMP target status table (IMP-01..06 ✓)
  - 8 P1 packages per-package % table
  - CI run URL
  - Final commit SHA (ratchet commit)
  - Files modified total (across all 5 plans)
  - Total test count
  - Task 0 batch commit SHA + test file count

No further commits needed — STATE.md + ROADMAP.md already SHIPPED in ratchet commit (Task 3) per D-11.
  </action>
  <verify>
    <automated>bash .github/scripts/check-coverage.sh c.out .coverage-threshold 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - bash .github/scripts/check-coverage.sh c.out .coverage-threshold exits 0
    - Output shows PASS: weighted avg X.X% >= threshold Y.Y%
    - Output shows per-package PASS lines for all 8 P1 packages
    - Output shows Phase 72 per-sub-package PASS lines for all 28 cells (14 sub-modules × 2 packages, preserved backward compatibility)
    - 73-05-SUMMARY.md finalized with all sections complete (coverage delta, per-IMP status, 8 P1 per-package, CI run URL, final SHA, files modified count, test count)
    - All commits pushed to origin/main (or documented why deferred)
  </acceptance_criteria>
  <done>
Phase 73 complete; CI gate green with new ratchet; all documentation already shipped in ratchet commit (Task 3).
  </done>
</task>

</tasks>

<verification>
- bash .github/scripts/check-coverage.sh c.out .coverage-threshold exits 0 with PASS line
- Per-package validation passes for all 8 P1 packages (new in Phase 73)
- Per-sub-package validation passes for all 28 cells (14 sub-modules × 2 packages, preserved from Phase 72)
- .coverage-threshold value > 21.5 (ratchet UP)
- .planning/coverage-baseline.md has new Phase 73 后 row
- All 6 IMP targets ≥70% per-package (D-04)
- Atomic ratchet commit with 6 files (D-07): .coverage-threshold + .planning/coverage-baseline.md + .planning/STATE.md + .planning/ROADMAP.md + .github/scripts/check-coverage.sh + 73-05-SUMMARY.md
- Prior batch commit (Task 0) contains only *_test.go files from plans 73-01..73-04
</verification>

<success_criteria>
- [ ] Phase 73 SC#1: api/v1/duty ≥70% (265 stmts)
- [ ] Phase 73 SC#2: api/v1/knowledge ≥70% (273 stmts)
- [ ] Phase 73 SC#3: api/v1/rpa ≥70% (612 stmts, incl. 3 public endpoints)
- [ ] Phase 73 SC#4: api/v1/vdi ≥70% (298 stmts)
- [ ] Phase 73 SC#5: services/duty + services/knowledge ≥70% (114 + 85 stmts)
- [ ] Phase 73 SC#6: services/network + services/monitor ≥70% (127 + 485 stmts, incl. oper_log_service)
- [ ] Phase 73 SC#7: Weighted avg ratcheted to new value (21.5% → NEW_VALUE%), CI gate updated
- [ ] Phase 73 SC#8: 0 business code changes (only test files added; per-plan documented)
- [ ] check-coverage.sh extended for 8 P1 package per-package validation
- [ ] All 6 IMP targets ≥70% per-package (D-04 strict)
- [ ] Atomic commit with 6 files (D-07) — includes STATE.md + ROADMAP.md per D-11
- [ ] Prior Task 0 batch commit collects all per-module test files (resolves W2 ambiguity)
</success_criteria>

<output>
Create .planning/phases/73-p1-pending/73-05-SUMMARY.md (final phase summary) when done.
</output>
