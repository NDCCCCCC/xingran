---
phase: 73-p1-pending
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/services/duty/duty_cache_impl_test.go
  - internal/services/knowledge/knowledge_cache_impl_test.go
  - internal/services/network/cache_impl_test.go
autonomous: true
requirements:
  - IMP-05
user_setup: []

must_haves:
  truths:
    - "go test -cover -count=1 ./internal/services/duty/... exits 0 with coverage >= 70% (114 stmts)"
    - "go test -cover -count=1 ./internal/services/knowledge/... exits 0 with coverage >= 70% (85 stmts)"
    - "go test -cover -count=1 ./internal/services/network/... exits 0 with coverage >= 70% (127 stmts)"
    - "All 3 service packages use portwrite pure-mock pattern (interface assertions + testify/mock + no real DB)"
  artifacts:
    - path: "internal/services/duty/duty_cache_impl_test.go"
      provides: "Pure-mock tests for services/duty (114 stmts)"
      contains: "package duty"
    - path: "internal/services/knowledge/knowledge_cache_impl_test.go"
      provides: "Pure-mock tests for services/knowledge (85 stmts)"
      contains: "package knowledge"
    - path: "internal/services/network/cache_impl_test.go"
      provides: "Pure-mock tests for services/network (127 stmts)"
      contains: "package network"
  key_links:
    - from: "internal/services/duty/duty_cache_impl_test.go"
      to: "internal/services/duty/duty_cache_impl.go"
      via: "real cacheServiceImpl + mocked CacheProvider"
      pattern: "var _ CacheProvider = \\(\\*mockCacheProvider\\)"
    - from: "internal/services/knowledge/knowledge_cache_impl_test.go"
      to: "internal/services/knowledge/knowledge_cache_impl.go"
      via: "real cacheServiceImpl + mocked CacheProvider"
      pattern: "var _ CacheProvider = \\(\\*mockCacheProvider\\)"
    - from: "internal/services/network/cache_impl_test.go"
      to: "internal/services/network/cache_impl.go"
      via: "real cacheServiceImpl + mocked CacheProvider"
      pattern: "var _ CacheProvider = \\(\\*mockCacheProvider\\)"
---

<objective>
Add service tests for three small P1 packages using the portwrite pure-mock pattern: internal/services/duty (114 stmts), internal/services/knowledge (85 stmts), internal/services/network (127 stmts). All three push from 0.0% to ≥70% per-package coverage.

Purpose: Phase 73 third delivery per D-01 (services 简单). Each package is a single cache_impl file with a cacheServiceImpl struct that depends on a CacheProvider interface. Per D-02, services/duty/knowledge/network all use portwrite pattern (interface assertions + testify/mock + completely mocked CacheProvider, no real DB).

Output: 3 test files (one per package) covering all service methods with table-driven TC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-p1-pending/73-CONTEXT.md
@.planning/phases/73-p1-pending/73-RESEARCH.md
@.planning/phases/73-p1-pending/73-VALIDATION.md
@.planning/REQUIREMENTS.md

Read these source files first (D-02 service 完整范本 + 目标):
- internal/services/portwrite/port_write_service_test.go — D-02 service complete pattern (READ for interface assertion + testify/mock + mockDeviceExecutor + mockCollectionSvc idioms)
- internal/services/duty/duty_cache_impl.go — dutyCacheServiceImpl with constructor NewDutyServiceWithCache (line 65) + all 23 interface methods (CreateDutyPool through UpdateDutyConfig at lines 88-291)
- internal/services/knowledge/knowledge_cache_impl.go — knowledgeCacheServiceImpl with NewKnowledgeServiceWithCache (line 55) + all interface methods (GetKnowledgeArticleList through SearchKnowledgeArticles at lines 79-290)
- internal/services/network/cache_impl.go — cacheServiceImpl with NewServiceWithCache (line 49) + all interface methods (List through InvalidateAllDeviceCache at lines 75-347)
- internal/pkg/cache/cache.go — CacheProvider interface (GetOrSet, Delete, DeleteByPattern, MGet, MDelete) - this is the dependency to mock
- internal/models/operations/duty.go + knowledge.go + network.go — model structs used in service methods
- glebarez/sqlite + gorm.io/gorm — for any necessary in-memory test setup if service calls require DB
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build duty_cache_impl_test.go covering all 23 service methods</name>
  <files>internal/services/duty/duty_cache_impl_test.go</files>
  <read_first>
    - internal/services/duty/duty_cache_impl.go (all 23 methods from CreateDutyPool through UpdateDutyConfig)
    - internal/services/duty/duty_service.go (interface contract - if exists separately)
    - internal/services/portwrite/port_write_service_test.go (D-02 service complete pattern)
    - internal/pkg/cache/cache.go (CacheProvider interface signature)
  </read_first>
  <action>
Create internal/services/duty/duty_cache_impl_test.go (package duty). Cover all 23 service methods on dutyCacheServiceImpl.

Test fixture (mirror portwrite pure-mock pattern):
- mockCacheProvider — embeds mock.Mock, implements CacheProvider interface (GetOrSet, Delete, DeleteByPattern, MGet, MDelete).
- mockDutyRepo or systemServicesCache — if duty_cache_impl.go delegates to systemServices for some operations, mock that interface too. INSPECT duty_cache_impl.go to see what dependencies it has beyond CacheProvider.
- Compile-time assertion: var _ CacheProvider = (*mockCacheProvider)(nil)
- For each method: instantiate dutyCacheServiceImpl{cache: mockCacheProvider, ...}, inject mocked deps as needed.

For each of the 23 service methods, write ≥1 happy-path + ≥1 error-path test case:
- CreateDutyPool, GetDutyPoolList, GetDutyPoolStatistics, GetDutyPoolByID, UpdateDutyPool, DeleteDutyPool
- GenerateSchedule, GetDutyScheduleList, GetTodayDuty, GetMonthlyDutySchedule, SwapDuty, ManualDuty, DeleteDutySchedule, BatchDeleteDutySchedules, GetMyDutyStats
- CreateHoliday, GetHolidayList, UpdateHoliday, DeleteHoliday, GetHolidayYears, BatchCreateHolidays
- GetDutyConfig, UpdateDutyConfig

Test pattern (portwrite style):
- TC1: <method>_success — mock cache GetOrSet to return cached value, or mock underlying repo to return seeded data; assert expected return.
- TC2: <method>_cache_miss_then_db — mock cache GetOrSet to return cache-miss + repo returns seeded data; assert method passes through correctly and calls cache.Set.
- TC3: <method>_error_propagates — mock returns error; assert method returns the error.

Naming: TestDutyService_<Method>_<Scenario>. Aim for 50+ test functions.

Special attention: duty_cache_impl delegates to systemServices for some ops (cache invalidation across modules). Mock those interfaces where used.

If duty_cache_impl requires gorm.DB for any path (e.g. transactions), use a glebarez sqlite in-memory DB with minimal DDL matching internal/models/operations/duty.go tables.
  </action>
  <verify>
    <automated>go test -count=1 -cover ./internal/services/duty/... 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - go test -cover -count=1 ./internal/services/duty/... exits 0
    - Output shows coverage: 70.X% of statements for duty service package
    - 50+ test functions present
    - Compile-time interface assertion present at top
    - testify/mock import present; no real DB connection (no glebarez/sqlite import required for purely-mocked paths)
  </acceptance_criteria>
  <done>
    IMP-05 first half: services/duty >=70% coverage.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build knowledge_cache_impl_test.go covering all service methods</name>
  <files>internal/services/knowledge/knowledge_cache_impl_test.go</files>
  <read_first>
    - internal/services/knowledge/knowledge_cache_impl.go (all interface methods)
    - internal/services/portwrite/port_write_service_test.go (D-02 service complete pattern)
    - internal/pkg/cache/cache.go (CacheProvider interface)
    - internal/models/operations/knowledge.go (KnowledgeArticle, KnowledgeCategory, KnowledgeTag structs)
  </read_first>
  <action>
Create internal/services/knowledge/knowledge_cache_impl_test.go (package knowledge). Mirror Task 1 portwrite pattern.

Test fixture:
- mockCacheProvider — embeds mock.Mock, implements CacheProvider interface
- mockKnowledgeRepo or direct gorm.DB if needed for some methods (INSPECT knowledge_cache_impl.go to see which deps it has beyond CacheProvider)
- Compile-time assertion: var _ CacheProvider = (*mockCacheProvider)(nil)

Cover all service methods (~20):
- Article: GetKnowledgeArticleList, GetArticleStatistics, GetKnowledgeArticle, CreateKnowledgeArticle, UpdateKnowledgeArticle, DeleteKnowledgeArticle
- Category: GetKnowledgeCategoryList, GetKnowledgeCategory, CreateKnowledgeCategory, UpdateKnowledgeCategory, DeleteKnowledgeCategory
- Tag: GetAllTags, GetTagByName, CreateTag, UpdateTag, DeleteTag
- Cache invalidation: InvalidateCategoryCache, InvalidateTagCache, InvalidateArticleCache, InvalidateAllArticleCache
- Counters: IncrementViewCount, IncrementLikeCount
- Search: SearchKnowledgeArticles

For each method ≥1 happy + ≥1 error test case (portwrite 3-case pattern: success, cache-miss-then-db, error).

Naming: TestKnowledgeService_<Method>_<Scenario>. Aim for 40+ test functions.

Add TestKnowledgeService_CompileOnly smoke test.
  </action>
  <verify>
    <automated>go test -count=1 -cover ./internal/services/knowledge/... 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - go test -cover -count=1 ./internal/services/knowledge/... exits 0
    - Output shows coverage: 70.X% of statements for knowledge service package
    - 40+ test functions present
    - Compile-time interface assertion present at top
    - testify/mock import present
  </acceptance_criteria>
  <done>
    IMP-05 second half: services/knowledge >=70% coverage.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build cache_impl_test.go covering all network service methods</name>
  <files>internal/services/network/cache_impl_test.go</files>
  <read_first>
    - internal/services/network/cache_impl.go (all 17 methods)
    - internal/services/portwrite/port_write_service_test.go (D-02 service complete pattern)
    - internal/pkg/cache/cache.go (CacheProvider interface)
    - internal/models/operations/network.go (NetworkDevice struct)
  </read_first>
  <action>
Create internal/services/network/cache_impl_test.go (package network). Mirror Task 1 portwrite pattern.

Test fixture:
- mockCacheProvider — embeds mock.Mock, implements CacheProvider interface
- mockNetworkRepo or direct gorm.DB if needed (INSPECT cache_impl.go for deps beyond CacheProvider)
- Compile-time assertion: var _ CacheProvider = (*mockCacheProvider)(nil)

Cover all 17 service methods:
- List, GetByID, Create, QuickCreateDevice
- Update, Delete, BatchDelete
- UpdateStatus, UpdateStatusBatch
- GetDeviceStatistics, GetDevicesByDept, GetDevicesByCredential
- InvalidateDeviceCache, InvalidateStatisticsCache, InvalidateDeptCache, InvalidateCredentialCache, InvalidateAllDeviceCache

For each method ≥1 happy + ≥1 error test case (portwrite 3-case pattern: success, cache-miss-then-db, error).

Naming: TestNetworkService_<Method>_<Scenario>. Aim for 40+ test functions.

Add TestNetworkService_CompileOnly smoke test.

Note: network package depends on models.DeviceStatus (use models.DeviceStatusNormal / models.DeviceStatusDisabled constants per Status Value Convention; do NOT hard-code 0/1).
  </action>
  <verify>
    <automated>go test -count=1 -cover ./internal/services/network/... 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - go test -cover -count=1 ./internal/services/network/... exits 0
    - Output shows coverage: 70.X% of statements for network service package
    - 40+ test functions present
    - Compile-time interface assertion present at top
    - testify/mock import present
    - Status constants reference models.* (NOT raw 0/1)
  </acceptance_criteria>
  <done>
    IMP-06 first half: services/network >=70% coverage.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify combined coverage + write SUMMARY + commit</name>
  <files>.planning/phases/73-p1-pending/73-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/73-p1-pending/73-CONTEXT.md (SC checklist + D-locked decisions)
    - .planning/phases/73-p1-pending/73-VALIDATION.md (Nyquist 8-dim audit)
  </read_first>
  <action>
Run combined coverage and capture per-package percentages:

Commands:
- go test -coverprofile=c.out -count=1 ./internal/services/duty/ ./internal/services/knowledge/ ./internal/services/network/
- go tool cover -func=c.out | grep -E "(duty|knowledge|network|total)"
- Per-package: go test -cover -count=1 ./internal/services/duty/... etc. separately

Verify each package total line shows >=70.0%.

If coverage < 70%:
- Identify uncovered stmts via go tool cover -html=c.out -o coverage.html
- Add additional test cases for uncovered branches (cache-miss paths, invalidation paths, error branches)
- Re-run until >=70% per package

If still < 70% after 3 attempts:
- Document gap in 73-03-SUMMARY.md (do NOT lower bar)

Write .planning/phases/73-p1-pending/73-03-SUMMARY.md with:
- Files modified list (3 test files)
- Per-package coverage achieved
- Test count totals
- Nyquist 8-dim audit table per VALIDATION.md (note D5=SKIP for service tests per VALIDATION.md §Plan → Dimension)
- D-02 verification (portwrite pure-mock pattern used)
- D-01..D-13 lock verification
- Commit message draft: test(73-03): IMP-05 + IMP-06 services tests 0% to 70%+
- SC#5 + SC#6 (network half) mapping checkboxes

Clean up c.out and coverage.html (do NOT commit).

Commit the 3 test files + SUMMARY as a single commit on local branch (per Phase 72 W2 fix pattern).
  </action>
  <verify>
    <automated>go test -cover -count=1 ./internal/services/duty/... ./internal/services/knowledge/... ./internal/services/network/... 2>&1 | grep -E "^(ok|FAIL|coverage:)"</automated>
  </verify>
  <acceptance_criteria>
    - go test -cover -count=1 exits 0 for all 3 packages
    - Output contains coverage: 70.X% of statements for ALL THREE packages
    - 73-03-SUMMARY.md exists with coverage %, file list, Nyquist 8-dim audit, commit message draft
    - 1 git commit on local branch with the 3 test files + SUMMARY
  </acceptance_criteria>
  <done>
    IMP-05 + IMP-06 (network half) SCs met; all 3 packages >=70%; test files committed.
  </done>
</task>

</tasks>

<verification>
- go test -cover -count=1 ./internal/services/duty/... exits 0 with coverage >=70% (114 stmts)
- go test -cover -count=1 ./internal/services/knowledge/... exits 0 with coverage >=70% (85 stmts)
- go test -cover -count=1 ./internal/services/network/... exits 0 with coverage >=70% (127 stmts)
- All 3 packages use portwrite pure-mock pattern (interface assertions + testify/mock)
- No real DB connections (where possible — minimal glebarez sqlite allowed for unavoidable gorm paths)
- No business code changes (D-12)
- No new mock framework (D-02)
</verification>

<success_criteria>
- [ ] IMP-05 SC#5: services/duty + services/knowledge coverage >=70% (114 + 85 stmts)
- [ ] IMP-06 SC#6 (network half): services/network coverage >=70% (127 stmts)
- [ ] All 3 packages use portwrite pure-mock pattern per D-02
- [ ] No new mock framework introduced (D-02)
- [ ] Zero business code changes (D-12)
</success_criteria>

<output>
Create .planning/phases/73-p1-pending/73-03-SUMMARY.md when done.
</output>