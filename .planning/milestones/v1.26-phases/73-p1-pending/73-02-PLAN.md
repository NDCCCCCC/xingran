---
phase: 73-p1-pending
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/v1/rpa/worker_handler_test.go
  - internal/api/v1/rpa/task_handler_test.go
  - internal/api/v1/rpa/execution_handler_test.go
  - internal/api/v1/rpa/credential_handler_test.go
  - internal/api/v1/rpa/ai_handler_test.go
  - internal/api/v1/rpa/flow_handler_test.go
  - internal/api/v1/rpa/router_public_test.go
  - internal/api/v1/vdi/vdi_server_handler_test.go
  - internal/api/v1/vdi/vm_handler_test.go
autonomous: true
requirements:
  - IMP-03
  - IMP-04
user_setup: []

must_haves:
  truths:
    - "go test -cover -count=1 ./internal/api/v1/rpa/... exits 0 with coverage >= 70% (612 stmts)"
    - "go test -cover -count=1 ./internal/api/v1/vdi/... exits 0 with coverage >= 70% (298 stmts)"
    - "rpa public router (SetupPublicWorkerRouter) has its 3 endpoints (Register, Heartbeat, Progress) covered without JWT auth (per D-04)"
    - "vdi base_handler.go helpers (handleJSONBinding, handleServiceError, verifyVDIServerExists, ensureVDIServer) are covered"
  artifacts:
    - path: "internal/api/v1/rpa/worker_handler_test.go"
      provides: "Tests for rpa worker_handler.go"
      contains: "package rpa"
    - path: "internal/api/v1/rpa/task_handler_test.go"
      provides: "Tests for rpa task_handler.go"
      contains: "package rpa"
    - path: "internal/api/v1/rpa/execution_handler_test.go"
      provides: "Tests for rpa execution_handler.go"
      contains: "package rpa"
    - path: "internal/api/v1/rpa/credential_handler_test.go"
      provides: "Tests for rpa credential_handler.go"
      contains: "package rpa"
    - path: "internal/api/v1/rpa/ai_handler_test.go"
      provides: "Tests for rpa ai_handler.go"
      contains: "package rpa"
    - path: "internal/api/v1/rpa/flow_handler_test.go"
      provides: "Tests for rpa flow_handler.go"
      contains: "package rpa"
    - path: "internal/api/v1/rpa/router_public_test.go"
      provides: "Tests for SetupPublicWorkerRouter public endpoints per D-04"
      contains: "SetupPublicWorkerRouter"
    - path: "internal/api/v1/vdi/vdi_server_handler_test.go"
      provides: "Tests for vdi vdi_server_handler.go"
      contains: "package vdi"
    - path: "internal/api/v1/vdi/vm_handler_test.go"
      provides: "Tests for vdi vm_handler.go"
      contains: "package vdi"
  key_links:
    - from: "internal/api/v1/rpa/*_test.go"
      to: "internal/services/rpa"
      via: "real service call through rpa.WorkerService / rpa.TaskService / rpa.ExecutionService / rpa.CredentialService / rpa.AIService"
      pattern: "service\\.(Register|Heartbeat|Progress|ScaleUp|ScaleDown|GetAutoScaleConfig|UpdateAutoScaleConfig|ListTasks|CreateTask|ExecuteTask|ListExecutions|CancelExecution|GetExecutionLogs|GetBatchReport|RequestHumanIntervention|SubmitHumanIntervention|DownloadArtifacts|ListCredentials|InvalidateSession|GenerateScript|Decide|AnalyzeFailure|RecordSelectorSuccess|GetBestSelector|EvaluateCondition|MapData|TransformValue|ExtractJSONPath|HandleError|ExecuteRetry|AggregateData)"
    - from: "internal/api/v1/vdi/*_test.go"
      to: "internal/services/vdi"
      via: "real service call through vdiServices.VDIServerService / vdiServices.VMService"
      pattern: "service\\.(Create|List|GetByID|Update|Delete|TestConnection|Operate|StartVM|StopVM|RestartVM|BindUser|UnbindUser|SyncFromVDI|ListResourceGroups|ListResources|SyncAll|ListVTPPlatforms|ListRunPositions|ListStorages|ListNetworks)"
---

<objective>
Add handler tests for two complex P1 packages: internal/api/v1/rpa (612 stmts, 7 sub-handler files) and internal/api/v1/vdi (298 stmts, 2 main handler files). Both push from 0.0% to ≥70% per-package coverage.

Purpose: Phase 73 second delivery per D-01 (handlers complex). The rpa package has 7 sub-handler structs (worker/task/execution/credential/ai/flow) plus a public router (SetupPublicWorkerRouter with 3 endpoints — D-04 mandate). vdi has 2 handler structs (VDIServerHandler 6 methods, VMHandler 19 methods) plus base_handler.go helpers.

Output: 7 test files in api/v1/rpa/ + 2 test files in api/v1/vdi/ = 9 total test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-p1-pending/73-CONTEXT.md
@.planning/phases/73-p1-pending/73-RESEARCH.md
@.planning/phases/73-p1-pending/73-VALIDATION.md
@.planning/REQUIREMENTS.md

Read these source files first:
- internal/api/v1/system/ad_account_handler_test.go — D-02 lightweight pattern
- internal/api/v1/monitor/oper_log_handler_test.go — Phase 72 SHIPPED reference (mock-with-function-fields + newTestCtxOL + setupOperLogHandler)
- internal/api/v1/rpa/rpa_router.go — has BOTH SetupPublicWorkerRouter (no auth) and SetupRPARouter (with auth); D-04 mandates public endpoints tested
- internal/api/v1/rpa/worker_handler.go — methods include Register/Heartbeat/Progress (public) + ScaleUp/ScaleDown/ScaleAll/AutoScaleConfig (auth)
- internal/api/v1/rpa/task_handler.go — 8 methods
- internal/api/v1/rpa/execution_handler.go — 10 methods + formatTime helper
- internal/api/v1/rpa/credential_handler.go — 7 methods
- internal/api/v1/rpa/ai_handler.go — 11 methods
- internal/api/v1/rpa/flow_handler.go — 7 methods + rpaError helper
- internal/api/v1/rpa/handler_helpers.go — small shared helpers
- internal/api/v1/vdi/base_handler.go — handleJSONBinding, handleServiceError, verifyVDIServerExists, ensureVDIServer helpers
- internal/api/v1/vdi/vdi_server_handler.go — 6 methods
- internal/api/v1/vdi/vm_handler.go — 19 methods
- internal/services/rpa/service_group.go — rpa.NewServiceGroup interface factory
- internal/services/vdi/ — VMService / VDIServerService interfaces
- internal/models/rpa/ — request/response struct definitions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build 6 rpa sub-handler test files (worker/task/execution/credential/ai/flow)</name>
  <files>internal/api/v1/rpa/worker_handler_test.go</files>
  <read_first>
    - internal/api/v1/rpa/rpa_router.go
    - internal/api/v1/rpa/worker_handler.go
    - internal/api/v1/rpa/task_handler.go
    - internal/api/v1/rpa/execution_handler.go
    - internal/api/v1/rpa/credential_handler.go
    - internal/api/v1/rpa/ai_handler.go
    - internal/api/v1/rpa/flow_handler.go
    - internal/api/v1/rpa/handler_helpers.go
    - internal/services/rpa/service_group.go
    - internal/api/v1/system/ad_account_handler_test.go
    - internal/api/v1/monitor/oper_log_handler_test.go
  </read_first>
  <action>
For each rpa sub-handler file, create a corresponding test file (package rpa). Mirror Phase 72 oper_log_handler_test.go pattern.

Common fixture (defined inline in each file to avoid cross-file dependencies):
- mockWorkerService / mockTaskService / mockExecutionService / mockCredentialService / mockAIService — each implements the corresponding rpa.XxxService interface with one function-field per method. Embed the interface as default.
- Compile-time assertion at top of each file: var _ rpa.WorkerService = (*mockWorkerService)(nil) (etc.).
- setupTestRpaXxxHandler(mock interface{}) *XxxHandler — constructs via NewXxxHandler(mockSvc, ...).WithCore(&core.Core{CoreInfra: &core.CoreInfra{DB: &db.Database{}}, CoreServices: &core.CoreServices{}}).
- newTestCtxRpa(method, path string, body interface{}) (*gin.Context, *httptest.ResponseRecorder) — gin.SetMode + httptest.NewRecorder + gin.CreateTestContext + httptest.NewRequest + Content-Type JSON.

For worker_handler_test.go (10+ methods):
- Register, Heartbeat, Progress, List, Statistics, ScaleUp, ScaleDown, ScaleAll, GetAutoScaleConfig, UpdateAutoScaleConfig
- 2 test cases per method (happy + error)
- Bind path/URL params via c.Params

For task_handler_test.go (8 methods):
- List, Create, GetByID, Update, Delete, Execute, UploadExcel, ExecuteWithExcel

For execution_handler_test.go (10 methods + formatTime):
- List, Statistics, GetByID, Cancel, GetLogs, GetBatchReport, RequestHumanIntervention, SubmitHumanIntervention, DownloadArtifacts
- Also test formatTime helper directly

For credential_handler_test.go (7 methods):
- List, Create, GetByID, Update, Delete, ListSessions, InvalidateSession

For ai_handler_test.go (11 methods):
- GenerateScript, OptimizeScript, Decide, AnalyzeFailure, SuggestFix, ClassifyError, RecordSelectorSuccess, RecordSelectorFailure, GetBestSelector, ScoreSelector, GetSelectorAlternatives

For flow_handler_test.go (7 methods + rpaError):
- EvaluateCondition, MapData, TransformValue, ExtractJSONPath, HandleError, ExecuteRetry, AggregateData
- Test rpaError type directly: TestRpaError_ErrorString

Naming: TestWorkerHandler_<method>_<scenario> etc. ~100+ test functions across all 6 files.
  </action>
  <verify>
    <automated>go test -count=1 ./internal/api/v1/rpa/... 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All 6 rpa test files compile and exit 0
    - Per-file coverage weighted avg ≥75% (math ensures package total ≥70% — 7 sub-handler files averaging 75% yields ≥70% package total even if rpa_router.go is lower)
    - At least 100 test functions across the 6 files
    - Compile-time interface assertions present in each file
    - No testify/mock import
  </acceptance_criteria>
  <done>
    6 rpa sub-handler test files complete with ≥75% per-file coverage (weighted package total ≥70%).
  </done>
</task>

<task type="auto">
  <name>Task 2: Build rpa_router_public_test.go for SetupPublicWorkerRouter public endpoints (D-04)</name>
  <files>internal/api/v1/rpa/router_public_test.go</files>
  <read_first>
    - internal/api/v1/rpa/rpa_router.go (SetupPublicWorkerRouter function)
    - internal/api/v1/rpa/worker_handler.go (Register/Heartbeat/Progress method bodies)
    - internal/services/rpa/service_group.go (NewServiceGroup signature)
    - internal/core/core.go (Core struct fields: DB, Config, NoticeHub, Cache)
  </read_first>
  <action>
Create internal/api/v1/rpa/router_public_test.go (package rpa). Per D-04, SetupPublicWorkerRouter must be tested without JWT auth.

The test verifies that:
- SetupPublicWorkerRouter creates 3 routes: POST /workers/register, POST /workers/:id/heartbeat, POST /workers/progress
- These routes are reachable without any auth middleware
- The handlers Register/Heartbeat/Progress can be invoked via the registered routes (using gin.Engine + httptest)

Test approach:
- Create a minimal &core.Core{CoreInfra: &core.CoreInfra{DB: &db.Database{}}, CoreServices: &core.CoreServices{Config: ...}} (nil DB is OK since public router registers routes before any DB call)
- Call SetupPublicWorkerRouter(publicGroup, &core.Core{...}) with a fresh gin.Engine
- Issue httptest.NewRequest("POST", "/workers/register", body) without any Authorization header
- Assert the handler was reached (200 or appropriate error, NOT 401)
- Repeat for /workers/test-id/heartbeat and /workers/progress

Note: This test does NOT need to verify business logic (already covered in worker_handler_test.go). It verifies the router wiring and the absence of auth middleware.

Add:
- TestSetupPublicWorkerRouter_NoAuthRequired (3 sub-tests: register, heartbeat, progress)
- TestSetupPublicWorkerRouter_RoutesRegistered (verify all 3 routes registered by route introspection or by triggering 404 on non-existent path)

Since SetupPublicWorkerRouter takes a *gin.RouterGroup (not a full Engine), wrap a test Engine and pass the group:

  engine := gin.New()
  publicGroup := engine.Group("/api/v1/rpa")
  SetupPublicWorkerRouter(publicGroup, &core.Core{...})
  // Now test via httptest.NewServer(engine) or httptest.NewRecorder with engine.ServeHTTP
  </action>
  <verify>
    <automated>go test -count=1 -v -run TestSetupPublicWorkerRouter ./internal/api/v1/rpa/... 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - TestSetupPublicWorkerRouter_NoAuthRequired passes for all 3 endpoints (register, heartbeat, progress)
    - No 401 / auth error returned (handler is reached without JWT)
    - TestSetupPublicWorkerRouter_RoutesRegistered confirms 3 routes are wired
    - File is in package rpa
  </acceptance_criteria>
  <done>
    D-04 public router endpoints covered (Register/Heartbeat/Progress reachable without auth).
  </done>
</task>

<task type="auto">
  <name>Task 3: Build 2 vdi handler test files (vdi_server_handler + vm_handler)</name>
  <files>internal/api/v1/vdi/vdi_server_handler_test.go</files>
  <read_first>
    - internal/api/v1/vdi/base_handler.go (handleJSONBinding, handleServiceError, verifyVDIServerExists, ensureVDIServer helpers)
    - internal/api/v1/vdi/vdi_server_handler.go (6 methods)
    - internal/api/v1/vdi/vm_handler.go (19 methods)
    - internal/api/v1/vdi/vdi_server_router.go (constructor signatures)
    - internal/api/v1/vdi/vm_router.go (constructor signatures)
    - internal/services/vdi/ (VDIServerService / VMService interfaces)
    - internal/models/operations/vdi.go (request/response structs)
    - internal/api/v1/monitor/oper_log_handler_test.go (Phase 72 SHIPPED reference)
  </read_first>
  <action>
Create internal/api/v1/vdi/vdi_server_handler_test.go and internal/api/v1/vdi/vm_handler_test.go (package vdi). Mirror Phase 72 oper_log_handler_test.go pattern.

Common fixture:
- mockVDIServerService — implements vdiServices.VDIServerService with function-fields
- mockVMService — implements vdiServices.VMService with function-fields
- Compile-time assertions at top of each file: var _ vdiServices.VDIServerService = (*mockVDIServerService)(nil), etc.
- setupTestVDIXxxHandler(mock interface{}) *XxxHandler
- newTestCtxVDI(method, path, body) — same gin pattern

For vdi_server_handler_test.go (6 methods):
- Create, List, GetByID, Update, Delete, TestConnection

For vm_handler_test.go (19 methods):
- Create, List, GetByID, Update, Delete, Operate, StartVM, StopVM, RestartVM, BindUser, UnbindUser, SyncFromVDI, ListResourceGroups, ListResources, SyncAll, ListVTPPlatforms, ListRunPositions, ListStorages, ListNetworks

For base_handler.go helpers (covered indirectly via handler tests above; ensure they are exercised):
- handleJSONBinding — exercised in every handler that calls c.ShouldBindJSON (Create, Update, etc.)
- handleServiceError — exercised in every handler that calls services and handles errors
- verifyVDIServerExists — exercised by vm_handler methods that take :id param and check existence
- ensureVDIServer — similar pattern

If base helpers are not reached via existing handler test flows, add 1-2 dedicated TestVerifyVDIServerExists / TestEnsureVDIServer cases that call them directly using a glebarez sqlite in-memory DB with a sys_vdi_server table (DDL from internal/models/operations/vdi.go).

Naming: TestVDIServerHandler_<method>_<scenario> / TestVMHandler_<method>_<scenario>. Aim for 50+ test functions total.

Add TestVDIHandlers_CompileOnly smoke test.
  </action>
  <verify>
    <automated>go test -count=1 -cover ./internal/api/v1/vdi/... 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - go test -cover -count=1 ./internal/api/v1/vdi/... exits 0
    - Output shows coverage: 70.X% of statements for vdi package
    - 50+ test functions across 2 files (TestVDIServerHandler_* / TestVMHandler_*)
    - base_handler.go helpers (handleJSONBinding, handleServiceError, verifyVDIServerExists, ensureVDIServer) exercised
  </acceptance_criteria>
  <done>
    IMP-04 SC met: api/v1/vdi >=70% coverage.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify combined coverage + write SUMMARY + commit</name>
  <files>.planning/phases/73-p1-pending/73-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/73-p1-pending/73-CONTEXT.md (SC checklist + D-04 lock)
    - .planning/phases/73-p1-pending/73-VALIDATION.md (Nyquist 8-dim audit)
  </read_first>
  <action>
Run combined coverage and capture per-package percentages:

Commands:
- go test -coverprofile=c.out -count=1 ./internal/api/v1/rpa/ ./internal/api/v1/vdi/
- go tool cover -func=c.out | grep -E "(rpa|vdi|total)"
- Per-package: go test -cover -count=1 ./internal/api/v1/rpa/... and ./internal/api/v1/vdi/... separately

Verify each package total line shows >=70.0%.

If coverage < 70%:
- Identify uncovered stmts via go tool cover -html=c.out -o coverage.html
- Add additional test cases for uncovered branches
- Re-run until >=70% per package

If still < 70% after 3 attempts:
- Document gap in 73-02-SUMMARY.md (do NOT lower bar)

Write .planning/phases/73-p1-pending/73-02-SUMMARY.md with:
- Files modified list (9 test files)
- Per-package coverage achieved (e.g. rpa 72.1%, vdi 75.3%)
- Per-file coverage breakdown
- Test count totals
- Nyquist 8-dim audit table per VALIDATION.md
- D-04 verification (public router endpoints tested without JWT)
- D-01..D-13 lock verification
- Commit message draft: test(73-02): IMP-03 + IMP-04 complex handler tests 0% to 70%+
- SC#3 + SC#4 mapping checkboxes

Clean up c.out and coverage.html (do NOT commit).

Commit the 9 test files + SUMMARY as a single commit on local branch (per Phase 72 W2 fix pattern).
  </action>
  <verify>
    <automated>go test -cover -count=1 ./internal/api/v1/rpa/... ./internal/api/v1/vdi/... 2>&1 | grep -E "^(ok|FAIL|coverage:)"</automated>
  </verify>
  <acceptance_criteria>
    - go test -cover -count=1 ./internal/api/v1/rpa/... ./internal/api/v1/vdi/... exits 0
    - Output contains coverage: 70.X% of statements for BOTH packages
    - 73-02-SUMMARY.md exists with coverage %, file list, Nyquist 8-dim audit, D-04 verification, commit message draft
    - 1 git commit on local branch with the 9 test files + SUMMARY
  </acceptance_criteria>
  <done>
    IMP-03 + IMP-04 SCs met; both packages >=70%; 9 test files committed.
  </done>
</task>

</tasks>

<verification>
- go test -cover -count=1 ./internal/api/v1/rpa/... exits 0 with coverage >=70% (612 stmts)
- go test -cover -count=1 ./internal/api/v1/vdi/... exits 0 with coverage >=70% (298 stmts)
- Both packages total line in go tool cover -func >=70.0%
- rpa SetupPublicWorkerRouter public endpoints (Register/Heartbeat/Progress) covered without JWT auth (D-04)
- vdi base_handler.go helpers exercised
- No business code changes (D-12)
- No new mock framework (D-02)
</verification>

<success_criteria>
- [ ] IMP-03 SC#3: internal/api/v1/rpa coverage >=70% (612 stmts)
- [ ] IMP-04 SC#4: internal/api/v1/vdi coverage >=70% (298 stmts)
- [ ] D-04 public router endpoints tested without auth
- [ ] No new mock framework introduced (D-02)
- [ ] Zero business code changes (D-12)
</success_criteria>

<output>
Create .planning/phases/73-p1-pending/73-02-SUMMARY.md when done.
</output>