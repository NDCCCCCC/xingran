---
phase: 72-p0-core-supplement
plan: 07
type: execute
wave: 2
depends_on: []
files_modified:
  - internal/api/v1/system/dashboard_handler_test.go
  - internal/services/system/dashboard_service_test.go
autonomous: true
requirements:
  - CORE-04
  - CORE-06
user_setup: []

must_haves:
  truths:
    - "Per-sub-package: dashboard_handler.go ≥70% coverage in api/v1/system"
    - "Per-sub-package: dashboard_service.go ≥70% coverage in services/system"
    - "Dashboard handler tests cover all widget data fetchers (workorder stats, device stats, user stats)"
    - "Dashboard service tests cover cross-table aggregation queries"
  artifacts:
    - path: "internal/api/v1/system/dashboard_handler_test.go"
      provides: "Tests for dashboard handler endpoints"
      contains: "package system"
    - path: "internal/services/system/dashboard_service_test.go"
      provides: "Tests for dashboard service aggregation"
      contains: "package system"
  key_links:
    - from: "internal/api/v1/system/dashboard_handler_test.go"
      to: "internal/services/system/dashboard_service.go"
      via: "real DashboardService call"
      pattern: "dashboardService\\.(GetOverview|GetWorkOrderStats|GetDeviceStats|GetUserStats|...)"
    - from: "internal/services/system/dashboard_service_test.go"
      to: "gorm.io/gorm"
      via: "glebarez sqlite in-memory for cross-table aggregation"
      pattern: "gorm\\.Open.*sqlite"
---

<objective>
Add tests for `dashboard` sub-module in both api/v1/system (CORE-04) and services/system (CORE-06) bringing per-sub-module coverage to ≥70%.

Purpose: Dashboard is the most cross-cutting sub-module (dashboard_handler.go 768 lines + dashboard_service.go 800 lines = 1568 total). It aggregates workorder, device, user, notice stats for the homepage.

Output: 1 new handler test file + 1 new service test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-p0-core-supplement/72-CONTEXT.md
@.planning/REQUIREMENTS.md

Read these source files first:
- internal/api/v1/system/ad_account_handler_test.go (D-01 lightweight pattern — READ THIS for actual mock service + gin.CreateTestContext idiom)
- internal/api/v1/system/dashboard_handler.go (768 lines; cross-cutting endpoints)
- internal/api/v1/system/dashboard_router.go (constructor signatures)
- internal/services/system/dashboard_service.go (800 lines; aggregation queries)
- internal/services/system/widget_data_fetcher.go (used by dashboard_service)
- internal/services/system/widget_data_fetcher_test.go (EXISTING — preserve)
- internal/models/dashboard.go or similar (response DTOs)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map dashboard methods + design fixtures</name>
  <files>internal/api/v1/system/dashboard_handler_test.go</files>
  <read_first>
    - internal/api/v1/system/dashboard_handler.go (full file)
    - internal/api/v1/system/dashboard_router.go (constructor signatures)
    - internal/services/system/dashboard_service.go (full file)
    - internal/services/system/widget_data_fetcher.go (widget fetchers)
  </read_first>
  <action>
Read dashboard_handler.go (768 lines) + dashboard_service.go (800 lines). Enumerate methods grouped by data source:
- Overview: GetOverview (combined stats across workorder + device + user)
- WorkOrder widgets: GetWorkOrderStats, GetWorkOrderTrend, GetWorkOrderByCategory
- Device widgets: GetDeviceStats, GetDeviceByType, GetDeviceByStatus
- User widgets: GetUserStats, GetActiveUserTrend
- Notice widgets: GetNoticeStats, GetRecentNotices
- System widgets: GetSystemHealth, GetSystemVersion

For each method, identify:
- Request struct (often empty or simple)
- Service method called
- Response shape (often custom DTO)

Design shared helpers in dashboard_handler_test.go:
- `setupDashboardTestDB(t *testing.T) *gorm.DB` — glebarez sqlite in-memory + CREATE TABLEs for ALL source tables: sys_work_order, sys_user, sys_device (or sys_network_device), sys_notice, etc. Each table with minimal columns needed for aggregation queries
- `setupDashboardHandler(t *testing.T, db *gorm.DB) *DashboardHandler` — real DashboardServiceImpl

Output: comment block with method enumeration + fixture design. Stub file compiles.

Note: dashboard_service.go uses gorm.DB heavily. Real DB tests (glebarez sqlite in-memory) are appropriate here — not pure mocks — because the value is in SQL aggregation correctness.
  </action>
  <verify>
    <automated>go build ./internal/api/v1/system/... 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Comment block enumerates ≥8 handler methods
    - setupDashboardTestDB DDL covers all source tables (sys_work_order, sys_user, sys_network_device, sys_notice at minimum)
    - Stub dashboard_handler_test.go compiles
  </acceptance_criteria>
  <done>
Method enumeration + fixture design complete; stub compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement dashboard handler tests</name>
  <files>internal/api/v1/system/dashboard_handler_test.go</files>
  <read_first>
    - internal/api/v1/system/dashboard_handler.go (full file)
    - internal/services/system/dashboard_service.go (full file)
    - pkg/response/response.go (response format)
    - internal/models/dashboard.go or similar
    - internal/api/v1/system/ad_account_handler_test.go (READ THIS — actual D-01 pattern reference for gin.CreateTestContext + httptest.NewRecorder + real-service invocation idiom)
  </read_first>
  <action>
Per D-01 lightweight pattern with REAL DashboardService + glebarez sqlite:

For each handler method, write 2 test cases:
- TC1: `<method>_empty_db_returns_zero_stats` — empty DB, assert response has zero values
- TC2: `<method>_seeded_db_returns_correct_stats` — seed rows, assert response matches expected aggregation

Seeding pattern (glebarez sqlite in-memory):
- Use `db.Exec(`INSERT INTO sys_work_order (id, status, created_at) VALUES (?, ?, ?)`, uuid.NewString(), 1, time.Now())` style inserts with positional placeholders.
- Each test seeds only the rows it needs; rely on setupDashboardTestDB to have created the schema.

Test invocation idiom (per D-01 — see ad_account_handler_test.go for actual code):
- Build `httptest.NewRecorder()` + `gin.CreateTestContext()` to obtain the context.
- Invoke the handler directly (e.g. `h.GetOverview(c)`) on the constructed context.
- Assert `w.Code == 200` and parse `w.Body` via `json.Unmarshal` to verify aggregated fields match expected counts.
- For methods requiring path parameters (e.g. `/widgets/:type`), set `c.Params = gin.Params{{Key: "type", Value: "workorder"}}` before invoking.

Use `httptest.NewRecorder()` + `gin.CreateTestContext()` + real DashboardService.

CRITICAL: Dashboard is cross-cutting — many handlers depend on multiple tables. Seed ALL required tables in setupDashboardTestDB before each test to avoid FK or column-not-found errors.

For methods that aggregate time-series data (GetWorkOrderTrend, GetActiveUserTrend):
- Seed rows with `created_at` spread across multiple days
- Assert trend bucket counts match seed distribution

For methods that aggregate by category/status (GetWorkOrderByCategory, GetDeviceByStatus):
- Seed rows with varying category/status values
- Assert group counts match
  </action>
  <verify>
    <automated>go test -count=1 -cover -run "TestDashboard" ./internal/api/v1/system/... 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - dashboard_handler_test.go compiles and passes
    - `go test -count=1 -cover -run "TestDashboard" ./internal/api/v1/system/...` exits 0
    - At least 15 test functions
    - Cross-table aggregation tested via real SQL (glebarez sqlite)
  </acceptance_criteria>
  <done>
Dashboard handler tests complete.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement dashboard service tests</name>
  <files>internal/services/system/dashboard_service_test.go</files>
  <read_first>
    - internal/services/system/dashboard_service.go (full file)
    - internal/services/system/widget_data_fetcher.go (widget helpers)
    - internal/services/portwrite/port_write_service_test.go (D-01 service pattern reference — READ for actual testify/mock + glebarez sqlite hybrid idiom)
    - internal/services/system/widget_data_fetcher_test.go (EXISTING — preserve)
  </read_first>
  <action>
Per D-01 service pattern (testify/mock + interface assertion) but with extensive glebarez sqlite for SQL testing:

Cover all exported methods on DashboardService:
- `GetOverview` — combined stats
- `GetWorkOrderStats` — count by status
- `GetWorkOrderTrend` — daily count for last N days
- `GetWorkOrderByCategory` — count grouped by category
- `GetDeviceStats` — count by device type/status
- `GetUserStats` — count by status (active/inactive)
- `GetActiveUserTrend` — login count over time
- `GetNoticeStats` — count by notice type
- `GetRecentNotices` — top N notices
- `GetSystemHealth` — uptime, DB connectivity, Redis connectivity (mock these)

For each: 2 test cases
- TC1: empty DB → zero stats
- TC2: seeded DB → correct aggregation

Use glebarez sqlite in-memory for all aggregation tests. Skip:
- Real Redis calls (use `noopCache` or `nil` cache)
- Real gopsutil calls (mock with stub returning canned values)

Do NOT modify widget_data_fetcher_test.go (preserve existing tests).

Per-method mock for cross-package dependencies:
- If DashboardService uses other services (UserService, WorkOrderService), prefer real impl via constructor injection over mocks (preserves SQL correctness)
  </action>
  <verify>
    <automated>go test -count=1 -cover -run "TestDashboard" ./internal/services/system/... 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - dashboard_service_test.go compiles and passes
    - `go test -count=1 -cover -run "TestDashboard" ./internal/services/system/...` exits 0
    - At least 15 test functions
    - widget_data_fetcher_test.go preserved (verify)
    - SQL aggregation correctness verified via real glebarez sqlite
  </acceptance_criteria>
  <done>
Dashboard service tests complete.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify per-sub-module dashboard coverage ≥70% + write SUMMARY</name>
  <files>.planning/phases/72-p0-core-supplement/72-07-SUMMARY.md</files>
  <read_first>
    - .planning/phases/72-p0-core-supplement/72-CONTEXT.md
  </read_first>
  <action>
Verify per-sub-module dashboard coverage:

Commands:
- go test -coverprofile=c.out -count=1 ./internal/api/v1/system/ ./internal/services/system/
- go tool cover -func=c.out | grep -E "(\bdashboard_handler\.go|\bdashboard_service\.go)"

Calculate weighted average across dashboard_handler.go + dashboard_service.go. Target ≥70.0%.

If < 70%:
- Identify uncovered branches via `go tool cover -html=c.out -o coverage.html`
- Add missing test cases (likely: edge cases like division by zero, empty time range, large result sets)
- Re-run

Clean up artifacts. Write `72-07-SUMMARY.md`:
- Per-file coverage breakdown
- Test count
- Commit message draft: `test(72-07): CORE-04+06 dashboard sub-module tests → ≥70%`

Do NOT commit yet — Task 0 in plan 72-13 handles the batch commit of all per-plan test files (per W2 fix).
  </action>
  <verify>
    <automated>go test -cover -count=1 ./internal/api/v1/system/ ./internal/services/system/ 2>&1 | grep -E "^(ok|FAIL|coverage:)"</automated>
  </verify>
  <acceptance_criteria>
    - Per-file dashboard coverage: weighted average ≥70% across dashboard_handler.go + dashboard_service.go
    - `72-07-SUMMARY.md` exists with breakdown
  </acceptance_criteria>
  <done>
Dashboard sub-module per-sub-package ≥70%; SUMMARY documented.
  </done>
</task>

</tasks>

<verification>
- Per-sub-module dashboard coverage: weighted average ≥70% across dashboard_handler.go + dashboard_service.go
- Real SQL aggregation tested via glebarez sqlite (not pure mocks) for SQL correctness
- widget_data_fetcher_test.go preserved (D-08)
</verification>

<success_criteria>
- [ ] CORE-04 dashboard sub-module ≥70% per-sub-module
- [ ] CORE-06 dashboard sub-module ≥70% per-sub-module
- [ ] All exported dashboard methods have ≥2 test cases
- [ ] Existing widget_data_fetcher_test.go preserved
- [ ] Real SM4 cipher used (D-03)
</success_criteria>

<output>
Create `.planning/phases/72-p0-core-supplement/72-07-SUMMARY.md` when done.
</output>