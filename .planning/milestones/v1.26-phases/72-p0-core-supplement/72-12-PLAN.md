---
phase: 72-p0-core-supplement
plan: 12
type: execute
wave: 4
depends_on: []
files_modified:
  - internal/api/v1/system/notification_config_handler_test.go
  - internal/services/system/email_config_service_test.go
  - internal/services/system/api_notification_config_service_test.go
autonomous: true
requirements:
  - CORE-04
  - CORE-06
user_setup: []

must_haves:
  truths:
    - "Per-sub-package: notification_config_handler.go ≥70% coverage in api/v1/system"
    - "Per-sub-package: email_config_service.go + api_notification_config_service.go ≥70% coverage in services/system"
    - "Notification config tests cover email/SMS/webhook channel CRUD + test send"
    - "Email config covers SMTP server settings; API notification covers webhook URL + signature"
  artifacts:
    - path: "internal/api/v1/system/notification_config_handler_test.go"
      provides: "Tests for notification config handler (email/SMS/webhook)"
      contains: "package system"
    - path: "internal/services/system/email_config_service_test.go"
      provides: "Tests for email config service"
      contains: "package system"
    - path: "internal/services/system/api_notification_config_service_test.go"
      provides: "Tests for API notification config service"
      contains: "package system"
  key_links:
    - from: "internal/api/v1/system/notification_config_handler_test.go"
      to: "internal/services/system/email_config_service.go"
      via: "real EmailConfigService + APINotificationConfigService calls"
      pattern: "(emailConfigService|apiNotificationConfigService)\\.(Create|Update|Delete|GetByID|List|TestSend)"
    - from: "internal/services/system/email_config_service_test.go"
      to: "pkg/crypto"
      via: "SM4 cipher for SMTP password encryption"
      pattern: "sm4\\.(Encrypt|Decrypt)"
---

<objective>
Add tests for `email_config` sub-module in both api/v1/system (CORE-04) and services/system (CORE-06) bringing per-sub-module coverage to ≥70%.

Purpose: Email + API notification configuration is the system integration surface for outbound alerts. notification_config_handler.go 468 lines; email_config_service.go 282 + api_notification_config_service.go 229 = 511 service lines. Handles SMTP password (SM4 encrypted) + webhook URL signing.

Output: 1 new handler test file + 2 new service test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-p0-core-supplement/72-CONTEXT.md
@.planning/REQUIREMENTS.md

Read these source files first:
- internal/api/v1/system/ad_account_handler_test.go (D-01 lightweight pattern — READ for actual mock + gin.CreateTestContext idiom)
- internal/api/v1/system/notification_config_handler.go (468 lines)
- internal/api/v1/system/notification_config_router.go (constructor signatures)
- internal/services/system/email_config_service.go (282 lines; SMTP config)
- internal/services/system/api_notification_config_service.go (229 lines; webhook config)
- internal/models/sys_email_config.go and sys_api_notification_config.go
- pkg/crypto/sm4.go (SM4 cipher for SMTP password)
- pkg/crypto/sm2.go (SM2 cipher if used)

Note (I2): The original `depends_on: [72-05..72-11]` field has been dropped for clarity. Per D-08 (0 business code changes), this plan creates its shared helpers (setupRealSM4Cipher, JWT factory, sqlite DDL for sys_email_config + sys_api_notification_config) LOCALLY in Task 1 rather than waiting on prior plans. The earlier advisory `depends_on` was informational only — actual execution does not require prior plans to complete. Shared helper definitions live in this plan's test files; no cross-plan sharing required.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map notification_config methods + design fixtures</name>
  <files>internal/api/v1/system/notification_config_handler_test.go</files>
  <read_first>
    - internal/api/v1/system/notification_config_handler.go (full file)
    - internal/api/v1/system/notification_config_router.go (constructor signatures)
    - internal/services/system/email_config_service.go (full file)
    - internal/services/system/api_notification_config_service.go (full file)
    - pkg/crypto/sm4.go (SM4 Encrypt/Decrypt signatures)
  </read_first>
  <action>
Read all source files. Enumerate methods:

notification_config_handler.go: ListEmailConfigs / CreateEmailConfig / UpdateEmailConfig / DeleteEmailConfig / TestEmailSend / ListAPIConfigs / CreateAPIConfig / UpdateAPIConfig / DeleteAPIConfig / TestAPINotify

For each method, identify request/response shape. Pay special attention to:
- SMTP password field — encrypted via SM4 before DB write; decrypted on read
- Webhook URL + signature secret — encrypted via SM4 before DB write

Design shared helpers in notification_config_handler_test.go (created LOCALLY per I2 — do not depend on prior plans):
- `setupNotifConfigTestDB(t)` — glebarez sqlite + CREATE TABLE `sys_email_config` (id TEXT PK, name TEXT, smtp_host TEXT, smtp_port INTEGER, smtp_user TEXT, smtp_password_ciphertext TEXT, from_address TEXT, ssl_enabled INTEGER, status INTEGER [0=enabled, 1=disabled], created_at DATETIME) + CREATE TABLE `sys_api_notification_config` (id TEXT PK, name TEXT, webhook_url TEXT, webhook_url_ciphertext TEXT, sign_secret_ciphertext TEXT, sign_method TEXT [HMAC-SHA256/MD5], status INTEGER, created_at DATETIME)
- `setupRealSM4Cipher(t)` — defined locally in this plan per D-03 (real SM4 cipher via pkg/crypto/sm4.go)
- `setupNotificationConfigHandler(t)` — real EmailConfigService + APINotificationConfigService

CRITICAL: Email config status uses `0=enabled, 1=disabled` per CLAUDE.md convention. Use constants.

Output: comment block with method enumeration + fixture design. Stub file compiles.
  </action>
  <verify>
    <automated>go build ./internal/api/v1/system/... ./internal/services/system/... 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Comment block enumerates ≥10 handler methods
    - setupNotifConfigTestDB DDL includes password_ciphertext + url_ciphertext + secret_ciphertext columns
    - Status uses constants per CLAUDE.md
    - Stub file compiles
  </acceptance_criteria>
  <done>
Method enumeration + fixture design complete; stub compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement notification_config handler tests</name>
  <files>internal/api/v1/system/notification_config_handler_test.go</files>
  <read_first>
    - internal/api/v1/system/notification_config_handler.go (full file)
    - internal/services/system/email_config_service.go (full file)
    - internal/services/system/api_notification_config_service.go (full file)
    - pkg/crypto/sm4.go (SM4 signatures)
    - internal/api/v1/system/ad_account_handler_test.go (D-01 pattern — READ for actual gin.CreateTestContext + httptest.NewRecorder idiom)
  </read_first>
  <action>
Per D-01 lightweight pattern with REAL services + glebarez sqlite + REAL SM4 cipher:

For each handler method, write 2-3 test cases:
- TC1: `<method>_success_returns_200`
- TC2: `<method>_validation_error_returns_400`
- TC3: `<method>_not_found_returns_404` (where applicable)

Special cases:
- `CreateEmailConfig`: assert SMTP password is stored as ciphertext (NOT plaintext), assert `smtp_password_ciphertext` column populated via real SM4.Encrypt
- `UpdateEmailConfig`: assert password re-encrypted on update
- `TestEmailSend`: seed email config, mock SMTP send (use a stub SMTP client interface), assert send attempted
- `CreateAPIConfig`: assert webhook URL + sign secret stored as ciphertext
- `TestAPINotify`: seed API config, mock HTTP POST, assert webhook called with correct HMAC signature

For SMTP client and HTTP client — if the service uses interfaces, define lightweight stub structs (no testify/mock — keep D-01 pattern). If concrete `net/smtp.SendMail` is used directly, wrap in a function field on the service struct for testability (requires constructor change — DO NOT change business code per D-08; instead, use a `smtpSendFunc` package-level variable or accept that the test will hit the real net/smtp.SendMail and use a local SMTP test server via `net` package).

All tests use `httptest.NewRecorder()` + `gin.CreateTestContext()` + real services. Real SM4 cipher per D-03.
  </action>
  <verify>
    <automated>go test -count=1 -cover -run "TestNotification" ./internal/api/v1/system/... 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - notification_config_handler_test.go compiles and passes
    - `go test -count=1 -cover -run "TestNotification" ./internal/api/v1/system/...` exits 0
    - At least 20 test functions
    - SMTP password + webhook URL encrypted before DB write
    - Test send paths covered (mocked SMTP/HTTP clients)
  </acceptance_criteria>
  <done>
Notification config handler tests complete.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement email + api notification config service tests</name>
  <files>internal/services/system/email_config_service_test.go
internal/services/system/api_notification_config_service_test.go</files>
  <read_first>
    - internal/services/system/email_config_service.go (full file)
    - internal/services/system/api_notification_config_service.go (full file)
    - internal/services/portwrite/port_write_service_test.go (D-01 service pattern reference — READ for actual mock + testify/mock idiom)
    - pkg/crypto/sm4.go (SM4 signatures)
  </read_first>
  <action>
Per D-01 service pattern with REAL services + glebarez sqlite + REAL SM4 cipher:

For email_config_service.go (282 lines):
- Cover all exported methods
- 2-3 test cases per method
- TC1: CreateConfig — assert plaintext password converted to ciphertext via SM4
- TC2: GetConfig — assert ciphertext decrypted back to plaintext when returned
- TC3: UpdateConfig — assert password re-encrypted on change; assert no re-encrypt if password empty
- TC4: TestSend — use a local SMTP server mock OR accept net/smtp.SendMail will fail (test validates path is reached)

For api_notification_config_service.go (229 lines):
- Cover all exported methods
- 2-3 test cases per method
- TC1: CreateConfig — assert URL + secret stored as ciphertext
- TC2: GetConfig — assert ciphertext decrypted on read
- TC3: TestNotify — seed config, mock HTTP POST, assert webhook called with HMAC signature
- TC4: ValidateSignature — sign a known payload with known secret, assert signature matches

Use function-field stubs for any external dependencies (SMTP client, HTTP client) — match the pattern used in port_write_service_test.go (D-01 service pattern).
  </action>
  <verify>
    <automated>go test -count=1 -cover -run "TestEmail|TestAPINotification" ./internal/services/system/... 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Both service test files compile and pass
    - `go test -count=1 -cover -run "TestEmail|TestAPINotification" ./internal/services/system/...` exits 0
    - At least 15 test functions across 2 files
    - SM4 encryption/decryption verified
    - HMAC signature validation verified
  </acceptance_criteria>
  <done>
Email + API notification config service tests complete.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify per-sub-module email_config coverage ≥70% + write SUMMARY</name>
  <files>.planning/phases/72-p0-core-supplement/72-12-SUMMARY.md</files>
  <read_first>
    - .planning/phases/72-p0-core-supplement/72-CONTEXT.md
  </read_first>
  <action>
Verify per-sub-module email_config coverage:

Commands:
- go test -coverprofile=c.out -count=1 ./internal/api/v1/system/ ./internal/services/system/
- go tool cover -func=c.out | grep -E "(\bnotification_config_handler\.go|\bemail_config_service\.go|\bapi_notification_config_service\.go)"

Calculate weighted average across the 3 email_config-related files. Target ≥70.0%.

If < 70%:
- Inspect uncovered branches via `go tool cover -html=c.out -o coverage.html`
- Add missing test cases
- Re-run

Clean up artifacts. Write `72-12-SUMMARY.md`:
- Per-file coverage breakdown
- Test count
- Commit message draft: `test(72-12): CORE-04+06 email_config sub-module tests → ≥70%`

Do NOT commit yet — Task 0 in plan 72-13 handles the batch commit of all per-plan test files (per W2 fix).
  </action>
  <verify>
    <automated>go test -cover -count=1 ./internal/api/v1/system/ ./internal/services/system/ 2>&1 | grep -E "^(ok|FAIL|coverage:)"</automated>
  </verify>
  <acceptance_criteria>
    - email_config sub-module weighted avg ≥70% across notification_config_handler.go + email_config_service.go + api_notification_config_service.go
    - `72-12-SUMMARY.md` exists with breakdown
  </acceptance_criteria>
  <done>
Email_config sub-module per-sub-package ≥70%; SUMMARY documented.
  </done>
</task>

</tasks>

<verification>
- Per-sub-module email_config coverage: weighted average ≥70% across 3 files
- SM4 encryption/decryption verified for SMTP password + webhook URL + sign secret
- HMAC signature validation verified
- Real SM4 cipher used (D-03)
</verification>

<success_criteria>
- [ ] CORE-04 email_config sub-module ≥70% per-sub-module
- [ ] CORE-06 email_config sub-module ≥70% per-sub-module
- [ ] SMTP password encrypted before DB write (verified)
- [ ] Webhook URL + sign secret encrypted before DB write (verified)
- [ ] No new mock framework (D-06)
</success_criteria>

<output>
Create `.planning/phases/72-p0-core-supplement/72-12-SUMMARY.md` when done.
</output>