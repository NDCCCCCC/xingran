---
phase: 72-p0-core-supplement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/v1/monitor/cache_handler_test.go
  - internal/api/v1/monitor/cache_enhanced_handler_test.go
  - internal/api/v1/monitor/server_handler_test.go
  - internal/api/v1/monitor/login_log_handler_test.go
  - internal/api/v1/monitor/oper_log_handler_test.go
autonomous: true
requirements:
  - CORE-02
user_setup: []

must_haves:
  truths:
    - "`go test -cover -count=1 ./internal/api/v1/monitor/...` exits 0 with coverage >= 70%"
    - "All 5 handler files (cache / cache_enhanced / server / login_log / oper_log) have tests"
    - "Cache handler tests use real `pkg/cache` interface with NoopCacheProvider for in-memory tests"
    - "Log handler tests seed login_log / oper_log rows via DDL and verify query path"
  artifacts:
    - path: "internal/api/v1/monitor/cache_handler_test.go"
      provides: "Tests for cache monitoring endpoints"
      contains: "package monitor"
    - path: "internal/api/v1/monitor/cache_enhanced_handler_test.go"
      provides: "Tests for enhanced cache stats endpoints"
      contains: "package monitor"
    - path: "internal/api/v1/monitor/server_handler_test.go"
      provides: "Tests for server monitoring endpoints"
      contains: "package monitor"
    - path: "internal/api/v1/monitor/login_log_handler_test.go"
      provides: "Tests for login log query endpoints"
      contains: "package monitor"
    - path: "internal/api/v1/monitor/oper_log_handler_test.go"
      provides: "Tests for oper log query endpoints"
      contains: "package monitor"
  key_links:
    - from: "internal/api/v1/monitor/*_test.go"
      to: "pkg/cache"
      via: "Cache interface + NoopCacheProvider for unit tests"
      pattern: "cache\\.(Get|Set|Delete|Keys|GetOrSet)"
    - from: "internal/api/v1/monitor/login_log_handler_test.go"
      to: "internal/models/sys_login_log"
      via: "glebarez sqlite in-memory table"
      pattern: "sys_login_log"
---

<objective>
Add handler tests for `internal/api/v1/monitor` (CORE-02, 518 stmts, 0.0% → ≥70%).

Purpose: Monitor package covers cache monitoring + server metrics + login/oper log query. This is critical observability surface that auditors and admins use daily.

Output: 5 `*_test.go` files in `internal/api/v1/monitor/`, one per handler file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-p0-core-supplement/72-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/quick/260820-backend-test-coverage-scan/per-package-coverage.txt

Read these source files first:
- internal/api/v1/system/ad_account_handler_test.go (D-01 lightweight handler pattern — READ THIS for actual mock service code idiom)
- internal/api/v1/monitor/cache_handler.go (cache stats endpoints)
- internal/api/v1/monitor/cache_enhanced_handler.go (enhanced cache stats)
- internal/api/v1/monitor/server_handler.go (server metrics)
- internal/api/v1/monitor/login_log_handler.go (login log queries)
- internal/api/v1/monitor/oper_log_handler.go (oper log queries)
- internal/api/v1/monitor/cache_router.go (constructor signature)
- internal/api/v1/monitor/server_router.go (constructor signature)
- pkg/cache/redis.go (Cache interface — the contract the stub must satisfy)
- pkg/cache/noop.go (verified NOT to exist as of 2026-08-21 — full stub struct required, see W4)
- internal/models/sys_login_log.go and internal/models/sys_oper_log.go (table schemas)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map 5 handler files → endpoints → test fixtures</name>
  <files>internal/api/v1/monitor/cache_handler_test.go</files>
  <read_first>
    - internal/api/v1/monitor/cache_handler.go
    - internal/api/v1/monitor/cache_enhanced_handler.go
    - internal/api/v1/monitor/server_handler.go
    - internal/api/v1/monitor/login_log_handler.go
    - internal/api/v1/monitor/oper_log_handler.go
    - internal/api/v1/monitor/cache_router.go (constructor signatures)
    - pkg/cache/redis.go (Cache interface methods — the full method set the stub must implement)
    - internal/api/v1/system/ad_account_handler_test.go (D-01 pattern reference for mock-with-function-fields)
  </read_first>
  <action>
Read each of the 5 handler files and list:
- All exported handler methods (one per endpoint)
- Each method's request struct (if any)
- Each method's service or Cache interface dependency
- Return shape (PageResponse, custom struct, or success-only)

For cache_*_handler.go — both use `cache.Cache` interface. Design a `noopCache` stub struct that implements `cache.Cache` (Get/Set/Delete/Keys/...) with in-memory map. **Per W4 fix (verified 2026-08-21):** `pkg/cache/noop.go` does NOT exist. Use minimal stub struct implementing the full `pkg/cache.Cache` interface in test code. Verify the actual Cache interface in `pkg/cache/redis.go` to ensure ALL methods are stubbed (compile-time assertion `var _ cache.Cache = (*noopCache)(nil)` at top of cache_handler_test.go enforces completeness).

For login_log_handler.go + oper_log_handler.go — both query DB tables. Use glebarez sqlite in-memory + CREATE TABLE DDL matching production schemas (lookup `internal/models/sys_login_log.go` for column types).

For server_handler.go — likely calls `runtime.ReadMemStats` or similar. Test with `httptest.NewRecorder()` + JSON unmarshal assertions.

Output: comment block in the test file header enumerating per-file test fixture design. Do NOT write executable test code yet — focus on fixture layout only. Each test file has its own setup function (e.g. `setupCacheTest(t)` returns a `*CacheHandler` + `*noopCache`).
  </action>
  <verify>
    <automated>go build ./internal/api/v1/monitor/... 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - All 5 test files have a comment block enumerating handler methods + fixtures
    - `noopCache` stub declared (full pkg/cache.Cache implementation in test code) satisfying pkg/cache.Cache interface — NO reliance on pkg/cache/noop.go since it does not exist
    - sqlite DDL matches production column types (sys_login_log, sys_oper_log)
    - `go build ./internal/api/v1/monitor/...` exits 0 (stub compiles)
  </acceptance_criteria>
  <done>
Fixture design complete; all 5 stub test files compile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement handler tests for cache + server handlers</name>
  <files>internal/api/v1/monitor/cache_handler_test.go
internal/api/v1/monitor/cache_enhanced_handler_test.go
internal/api/v1/monitor/server_handler_test.go</files>
  <read_first>
    - internal/api/v1/monitor/cache_handler.go
    - internal/api/v1/monitor/cache_enhanced_handler.go
    - internal/api/v1/monitor/server_handler.go
    - pkg/cache/redis.go (Cache interface methods to stub)
    - internal/api/v1/system/ad_account_handler_test.go (D-01 pattern — READ for actual mock service + gin.CreateTestContext idiom)
  </read_first>
  <action>
For cache_handler.go + cache_enhanced_handler.go — write 2-3 test cases per handler method:
- `TC1: <method>_empty_cache_returns_zero_stats` — noopCache with empty map, assert response fields
- `TC2: <method>_populated_cache_returns_correct_stats` — noopCache pre-seeded with 5 keys of varying TTL, assert counts
- `TC3: <method>_redis_error_returns_500` — noopCache that returns error on Get/Keys, assert error response

For server_handler.go — write 1-2 test cases per method:
- `TC1: <method>_returns_200_with_payload` — invoke handler, parse JSON body, assert fields present
- `TC2: <method>_internal_error_returns_500` — use a failing stub (e.g. `&failingServerMetrics{}` if server handler uses an injected service)

All tests use the D-01 lightweight pattern (see ad_account_handler_test.go for actual code):
- `httptest.NewRecorder()` + `gin.CreateTestContext()` for context construction
- Direct handler invocation (e.g. `h.GetStats(c)`) on the test context
- `json.Unmarshal(w.Body.Bytes(), &resp)` for response field assertions
- `noopCache` is the in-test stub struct with function fields / map backing store — NOT testify/mock (keep D-01 lightweight style).

Cover at minimum:
- Cache: `ListKeys`, `GetValue`, `DeleteKey`, `FlushPattern`, `GetStats`
- CacheEnhanced: `GetOverview`, `GetKeyDistribution`, `GetMemoryUsage`, `GetTopKeys`
- Server: `GetCPUInfo`, `GetMemoryInfo`, `GetDiskInfo`, `GetProcessList`

Skip methods that require live `os/exec` calls to gopsutil — wrap with a small `runtimeStatsFunc` field on a stub struct that returns canned data (avoid real gopsutil calls in unit tests).
  </action>
  <verify>
    <automated>go test -count=1 -cover -run "TestCache|TestServer" ./internal/api/v1/monitor/... 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `go test -count=1 -cover -run "TestCache|TestServer" ./internal/api/v1/monitor/...` exits 0
    - All cache + server handler methods have at least 1 test case
    - `noopCache` fully satisfies pkg/cache.Cache interface (compile-time assertion via `var _ cache.Cache = (*noopCache)(nil)` at top of cache_handler_test.go)
    - Test cases use `assert.Equal` / `assert.NoError` — no `assert.True(true)` placeholders
  </acceptance_criteria>
  <done>
Cache + server handler tests complete and passing.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement handler tests for login_log + oper_log</name>
  <files>internal/api/v1/monitor/login_log_handler_test.go
internal/api/v1/monitor/oper_log_handler_test.go</files>
  <read_first>
    - internal/api/v1/monitor/login_log_handler.go
    - internal/api/v1/monitor/oper_log_handler.go
    - internal/models/sys_login_log.go (table schema)
    - internal/models/sys_oper_log.go (table schema)
  </read_first>
  <action>
For login_log_handler.go + oper_log_handler.go — write 3-4 test cases per handler method:

Schema setup (glebarez sqlite in-memory):
- `setupLoginLogTestDB(t *testing.T) *gorm.DB` — CREATE TABLE `sys_login_log` (id TEXT PK, username TEXT, ip TEXT, status INTEGER, msg TEXT, created_at DATETIME)
- `setupOperLogTestDB(t *testing.T) *gorm.DB` — CREATE TABLE `sys_oper_log` (id TEXT PK, module TEXT, operator TEXT, oper_type INTEGER, request_url TEXT, request_method TEXT, status INTEGER, created_at DATETIME)
- Seed 5 rows per table per test using `uuid.NewString()` + insert via `db.Exec(...)`

Test cases (table-driven per method):
- `TC1: <method>_list_empty_returns_zero_total` — DB empty, assert total=0, list=[] (or empty slice)
- `TC2: <method>_list_seeded_returns_correct_rows` — DB has 5 rows, assert total=5, list contains 5 elements
- `TC3: <method>_list_filter_by_status` — seed mix of status=0/1, filter by status=0, assert only matching rows returned
- `TC4: <method>_list_pagination` — seed 20 rows, query page=2 size=5, assert list length=5, current=2

For methods that DELETE (e.g. `Clean`, `Delete`):
- `TC5: <method>_delete_removes_row` — seed 3 rows, call delete with id=1, assert remaining 2 rows

Use the same `httptest.NewRecorder()` + `gin.CreateTestContext()` pattern as cache tests (D-01).

Cover all methods on both handlers (grep `func (h \*\w+Handler)` to enumerate).
  </action>
  <verify>
    <automated>go test -count=1 -cover -run "TestLoginLog|TestOperLog" ./internal/api/v1/monitor/... 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `go test -count=1 -cover -run "TestLoginLog|TestOperLog" ./internal/api/v1/monitor/...` exits 0
    - All login_log + oper_log handler methods have ≥3 test cases each
    - Filter + pagination + delete paths covered
    - DDL matches production schema columns (verify by grep on `internal/models/sys_login_log.go` and `internal/models/sys_oper_log.go`)
  </acceptance_criteria>
  <done>
Login_log + oper_log handler tests complete; full monitor package coverage ≥70%.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify full monitor package coverage ≥70% + SUMMARY</name>
  <files>.planning/phases/72-p0-core-supplement/72-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/72-p0-core-supplement/72-CONTEXT.md (SC checklist)
    - .planning/templates/phase-summary.md
  </read_first>
  <action>
Run full package coverage:

Commands:
- go test -coverprofile=c.out -count=1 ./internal/api/v1/monitor/
- go tool cover -func=c.out | tail -1

If < 70%:
- Inspect uncovered lines via `go tool cover -html=c.out -o coverage.html`
- Add missing test cases (focus on branches: switch defaults, error wrapping, validation edge cases)
- Re-run

If still < 70% after exhaustive coverage:
- Document gap in `72-02-SUMMARY.md` for Phase 73 follow-up

Clean up `c.out` and `coverage.html`. Write `72-02-SUMMARY.md`:
- Files modified (5 test files)
- Per-handler coverage breakdown
- Test count and naming convention (TC1/TC2/...)
- Commit message draft: `test(72-02): CORE-02 monitor handler tests 0% → ≥70%`

Do NOT commit yet — Task 0 in plan 72-13 handles the batch commit of all per-plan test files (per W2 fix).
  </action>
  <verify>
    <automated>go test -cover -count=1 ./internal/api/v1/monitor/... 2>&1 | grep -E "^(ok|FAIL|coverage:)"</automated>
  </verify>
  <acceptance_criteria>
    - `go test -cover -count=1 ./internal/api/v1/monitor/...` exits 0 with `coverage: ≥70.0%`
    - `72-02-SUMMARY.md` exists with coverage %, file list, test count, commit message draft
    - No `c.out` or `coverage.html` artifacts left in repo
  </acceptance_criteria>
  <done>
CORE-02 target achieved (monitor package ≥70%); SUMMARY documented.
  </done>
</task>

</tasks>

<verification>
- `go test -cover -count=1 ./internal/api/v1/monitor/...` exits 0 with coverage ≥70%
- All 5 handler files have at least one test function
- No new mock framework introduced (D-06)
- Zero business code changes (D-08)
</verification>

<success_criteria>
- [ ] CORE-02 SC#2: `internal/api/v1/monitor` coverage ≥70% (518 stmts)
- [ ] All 5 monitor handler files (cache, cache_enhanced, server, login_log, oper_log) have ≥3 test cases each
- [ ] DDL for sys_login_log + sys_oper_log matches production schema
- [ ] Cache interface mocked via stub struct (no testify/mock) — full pkg/cache.Cache stub in test code (no NoopCacheProvider dependency since pkg/cache/noop.go does not exist)
</success_criteria>

<output>
Create `.planning/phases/72-p0-core-supplement/72-02-SUMMARY.md` when done.
</output>