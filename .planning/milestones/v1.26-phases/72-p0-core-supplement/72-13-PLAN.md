---
phase: 72-p0-core-supplement
plan: 13
type: execute
wave: 5
depends_on:
  - 72-01
  - 72-02
  - 72-03
  - 72-04
  - 72-05
  - 72-06
  - 72-07
  - 72-08
  - 72-09
  - 72-10
  - 72-11
  - 72-12
files_modified:
  - .github/scripts/check-coverage.sh
  - .coverage-threshold
  - .planning/coverage-baseline.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/72-p0-core-supplement/72-13-SUMMARY.md
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04
  - CORE-05
  - CORE-06
user_setup: []

must_haves:
  truths:
    - "`check-coverage.sh` extended to validate per-sub-package ≥70% (CORE-01..06 targets) AND weighted total ≥ new ratchet value"
    - "`.coverage-threshold` updated from 12.8 to new value (≥30% estimated, exact from actual measurement)"
    - "`.planning/coverage-baseline.md` has new Phase 72 后 row with commit SHA, weighted avg, total stmts, 14-sub-module per-file %"
    - "All test files added by plans 72-01..72-12 collected in a single batch commit (Task 0) BEFORE the ratchet commit"
    - "Atomic ratchet commit (D-07) includes: .coverage-threshold + .planning/coverage-baseline.md + .github/scripts/check-coverage.sh + .planning/STATE.md + .planning/ROADMAP.md + 72-13-SUMMARY.md"
    - "CI gate (`ci.yml` Coverage gate step) runs `check-coverage.sh` and exits 0 with new threshold"
  artifacts:
    - path: ".github/scripts/check-coverage.sh"
      provides: "Extended coverage gate with per-sub-package validation"
      contains: "sub_package_check"
    - path: ".coverage-threshold"
      provides: "New threshold value ≥30% (ratcheted from 12.8)"
      contains: "^[0-9]+\\."
    - path: ".planning/coverage-baseline.md"
      provides: "Phase 72 后 row appended with per-sub-module % table"
      contains: "Phase 72 后"
    - path: ".planning/phases/72-p0-core-supplement/72-13-SUMMARY.md"
      provides: "Final phase summary with coverage delta + commit SHA"
      contains: "coverage ratchet"
  key_links:
    - from: ".github/scripts/check-coverage.sh"
      to: "coverage.out"
      via: "awk parses atomic profile"
      pattern: "awk.*coverage\\.out"
    - from: ".github/workflows/ci.yml"
      to: ".github/scripts/check-coverage.sh"
      via: "Coverage gate step runs the bash script"
      pattern: "bash \\.github/scripts/check-coverage\\.sh"
---

<objective>
Collect all per-module *_test.go files added by plans 72-01..72-12 into a single batch commit (Task 0, resolves W2 commit ambiguity). Then extend the CI coverage gate to validate per-sub-package ≥70% (CORE-01..06 targets), ratchet .coverage-threshold to new value, append .planning/coverage-baseline.md Phase 72 后 row, update STATE.md + ROADMAP.md, and commit atomically per D-07 (Tasks 3 + 4).

Purpose: Phase 72 closes the loop — without this ratchet, the work has no enforcement. The gate now checks BOTH weighted average AND each CORE target sub-package, preventing silent regression.

Output: 2 commits total: (a) Task 0 batch commit of test files; (b) Tasks 3+4 atomic ratchet commit containing 6 files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-p0-core-supplement/72-CONTEXT.md
@.planning/phases/72-p0-core-supplement/72-DISCUSSION-LOG.md
@.planning/REQUIREMENTS.md
@.planning/coverage-baseline.md
@.planning/quick/260820-backend-test-coverage-scan/per-package-coverage.txt

Read these source files first:
- .github/scripts/check-coverage.sh (current Phase 71 version; extend it)
- .github/workflows/ci.yml (CI Coverage gate step — verify step name + invocation)
- .coverage-threshold (current 12.8)
- .planning/coverage-baseline.md (Phase 71 后 row schema)
- .planning/phases/72-p0-core-supplement/72-{01..12}-SUMMARY.md (per-plan coverage data + test files added)
- .planning/STATE.md (current state to update in ratchet commit)
- .planning/ROADMAP.md (Phase 72 entry to mark SHIPPED in ratchet commit)
</context>

<tasks>

<task type="auto">
  <name>Task 0: Batch commit all new test files across the phase (resolves W2)</name>
  <files>internal/api/v1/workorder/workorder_handler_test.go
internal/api/v1/monitor/cache_handler_test.go
internal/api/v1/monitor/cache_enhanced_handler_test.go
internal/api/v1/monitor/server_handler_test.go
internal/api/v1/monitor/login_log_handler_test.go
internal/api/v1/monitor/oper_log_handler_test.go
internal/api/v1/scheduler/job_handler_test.go
internal/services/workorder/service_test.go
internal/services/workorder/assignment_test.go
internal/services/workorder/comment_test.go
internal/services/workorder/rating_test.go
internal/services/workorder/periodic_test.go
internal/services/workorder/category_test.go
internal/services/workorder/reconciliation_template_test.go
internal/services/workorder/statistics_test.go
internal/services/workorder/base_test.go
internal/services/workorder/config_test.go
internal/api/v1/system/dashboard_handler_test.go
internal/services/system/dashboard_service_test.go
internal/api/v1/system/notification_config_handler_test.go
internal/services/system/email_config_service_test.go
internal/services/system/api_notification_config_service_test.go</files>
  <read_first>
    - .planning/phases/72-p0-core-supplement/72-{01..12}-SUMMARY.md (enumerate test files created per plan)
    - git status (verify all per-plan test files are untracked or staged)
  </read_first>
  <action>
Per W2 fix — collect all new *_test.go files added by plans 72-01..72-12 into a single batch commit BEFORE the ratchet commit. This resolves the "Do NOT commit" vs "commit only 4 files" ambiguity: per-module plans continue to instruct "Do NOT commit" (since Task 0 owns the batch commit), and Task 0 produces one canonical commit for all test files.

Steps:
1. Enumerate all *_test.go files added by plans 72-01..72-12 (see each plan files_modified). List must be exhaustive — read each plan SUMMARY to confirm.
2. Stage ONLY *_test.go files (no business code, per D-08). Use git add with explicit paths to avoid accidental staging of unrelated files.
3. Verify staged diff is test-only: git diff --cached --stat must show only *_test.go paths.
4. Commit with message format: test(72): add P0 test files (X new test files across Y packages) where X = total count of staged test files and Y = distinct package directories.
5. Capture the resulting commit SHA: git rev-parse HEAD — this is the TBD_SHA placeholder for Task 3 (Phase 71 D-04 amend pattern).

DO NOT include:
- Any _test.go files that existed before Phase 72 (e.g. ad_account_handler_test.go, periodic_statistics_test.go, widget_data_fetcher_test.go, apikey_service_test.go, etc. — preserve per D-08)
- Any business code (no *.go non-test changes per D-08)
- Any docs / scripts / config changes (those go in the ratchet commit per Task 3)

If git push is needed for downstream CI, push immediately after this commit so the ratchet commit can reference the upstream test files in CI.
  </action>
  <verify>
    <automated>git log --oneline -3 && git diff --stat HEAD~1 HEAD | grep -v _test.go | wc -l</automated>
  </verify>
  <done>
Batch commit containing all new *_test.go files from plans 72-01..72-12; SHA captured for Task 3 reference.
  </done>
</task>

<task type="auto">
  <name>Task 1: Capture actual coverage measurement (pre-ratchet)</name>
  <files>.planning/phases/72-p0-core-supplement/72-13-SUMMARY.md</files>
  <read_first>
    - .planning/coverage-baseline.md (current state — Phase 71 后 row)
    - .planning/phases/72-p0-core-supplement/72-{01..12}-SUMMARY.md (per-plan coverage numbers)
  </read_first>
  <action>
Run full backend test coverage and capture per-package %:

Commands:
- cd /path/to/repo && go test -timeout 15m -count=1 -coverprofile=coverage.out ./internal/... ./pkg/... ./cmd/... 2>&1 | tail -5
- go tool cover -func=coverage.out > cover-func.txt

Capture via concrete awk commands (per W3 — no placeholders):
- Weighted average: awk '/weighted avg/ {print $NF}' coverage.out
- Total stmts: awk '/^total:/ {print $2}' cover-func.txt
- Total covered: awk '/^total:/ {print $3}' cover-func.txt

Verify all 6 CORE targets individually pass ≥70%:
- CORE-01: internal/api/v1/workorder
- CORE-02: internal/api/v1/monitor
- CORE-03: internal/api/v1/scheduler
- CORE-04: weighted avg across 14 sub-modules of internal/api/v1/system
- CORE-05: internal/services/workorder
- CORE-06: weighted avg across 14 sub-modules of internal/services/system

If any CORE target < 70%:
- ESCALATE — do NOT proceed with ratchet. Identify which plan(s) underdelivered, add gap-closure tasks in 72-14-PLAN.md (created ad-hoc if needed)
- Do NOT ratchet below the actual measurement

Save raw output to a temp file for use in Task 3.
  </action>
  <verify>
    <automated>go test -cover -count=1 -timeout 15m ./internal/api/v1/workorder/ ./internal/api/v1/monitor/ ./internal/api/v1/scheduler/ ./internal/api/v1/system/ ./internal/services/workorder/ ./internal/services/system/ 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All 6 CORE packages exit 0 with coverage: ≥70.0%
    - Per-sub-module coverage for CORE-04 + CORE-06 ≥70% each (14 sub-modules × 2 packages = 28 cells)
    - Raw measurement saved to temp file
    - Concrete values captured for: weighted_avg, total_stmts, total_covered (no placeholders)
  </acceptance_criteria>
  <done>
Pre-ratchet coverage measured and verified; all 6 CORE targets ≥70%.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend check-coverage.sh for per-sub-package validation</name>
  <files>.github/scripts/check-coverage.sh</files>
  <read_first>
    - .github/scripts/check-coverage.sh (current Phase 71 version — extend it)
    - .github/workflows/ci.yml (Coverage gate step)
    - .planning/phases/72-p0-core-supplement/72-CONTEXT.md (CORE-01..06 targets)
  </read_first>
  <action>
Extend .github/scripts/check-coverage.sh to validate BOTH:
1. Weighted average ≥ .coverage-threshold (current behavior — preserve)
2. Per-sub-package ≥ 70% for CORE-01..06 targets

Add a new section after the existing weighted-avg block. The implementation should iterate each sub-module, parse coverage.out for matching files using awk (format: pkg:file:stmt:count), sum stmts + covered across matching files, compare weighted % against 70.0% floor, and on failure print which sub-module failed and exit non-zero.

CORE_TARGETS (single-package):
- internal/api/v1/workorder
- internal/api/v1/monitor
- internal/api/v1/scheduler
- internal/services/workorder

SUBMODULE_TARGETS_API_V1_SYSTEM (14 entries, pipe-separated file groups):
- user_handler.go|user_import_handler.go|user_unlock_handler.go
- role_handler.go
- department_handler.go
- menu_handler.go|fix_menu_handler.go
- dict_handler.go
- post_handler.go
- config_handler.go
- notice_handler.go|notice_user_handler.go
- settings_handler.go
- dashboard_handler.go
- apikey_handler.go
- profile_handler.go
- file_handler.go
- notification_config_handler.go

SUBMODULE_TARGETS_SERVICES_SYSTEM (14 entries):
- user_service.go|user_cache_impl.go|user_sync_service.go
- role_service.go|role_cache_impl.go
- department_service.go|department_cache_impl.go
- menu_service.go|menu_cache_impl.go
- dict_service.go|dict_cache_impl.go
- post_service.go|post_cache_impl.go
- config_service.go|config_cache_impl.go
- notice_service.go|notice_cache_impl.go
- settings_service.go|settings_cache_impl.go
- dashboard_service.go
- apikey_service.go
- profile_service.go
- file_service.go
- email_config_service.go|api_notification_config_service.go

Preserve backward compatibility:
- Existing weighted-avg gate behavior unchanged
- New per-sub-package check is additive (does not affect existing PASS/FAIL semantics for the weighted avg)

Add a new exit code 3 for per-sub-package failures (distinct from exit 1 weighted avg failure) — optional but improves debugging.

Add tests: create .github/scripts/check-coverage-subpkg-test.sh with fixtures that verify the script correctly:
- Passes when all sub-packages ≥70%
- Fails when one sub-package <70%
- Preserves weighted-avg behavior
  </action>
  <verify>
    <automated>bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - check-coverage.sh exits 0 when all CORE targets pass (verified with current measurement)
    - Per-sub-package validation block added with 14 sub-modules × 2 packages enumerated
    - Existing weighted-avg gate still functions (backward compatibility)
    - Script remains bash + awk only (no new dependencies, per D-06)
  </acceptance_criteria>
  <done>
check-coverage.sh extended for per-sub-package validation; both old and new checks coexist.
  </done>
</task>

<task type="auto">
  <name>Task 3: Atomic ratchet commit (6 files: includes STATE.md + ROADMAP.md per I3)</name>
  <files>.coverage-threshold
.planning/coverage-baseline.md
.github/scripts/check-coverage.sh
.planning/STATE.md
.planning/ROADMAP.md
.planning/phases/72-p0-core-supplement/72-13-SUMMARY.md</files>
  <read_first>
    - .coverage-threshold (current 12.8)
    - .planning/coverage-baseline.md (Phase 71 后 row schema)
    - .planning/phases/72-p0-core-supplement/72-{01..12}-SUMMARY.md (per-plan coverage data)
    - .planning/STATE.md (current state to update)
    - .planning/ROADMAP.md (Phase 72 entry to mark SHIPPED)
  </read_first>
  <action>
Atomic ratchet per D-07 + I3 — single commit modifying 6 files. Per I3, STATE.md + ROADMAP.md updates go in THIS ratchet commit (not a follow-up) so all docs land together.

Concrete commands (per W3 — no placeholders):

1. .coverage-threshold: write new value from Task 1 measurement, e.g. 30.5

2. .planning/coverage-baseline.md: append Phase 72 后 section:
   - weighted_avg column: value from `awk '/weighted avg/ {print $NF}' coverage.out`
   - total_stmts column: value from `awk '/^total:/ {print $2}' cover-func.txt`
   - total_covered column: value from `awk '/^total:/ {print $3}' cover-func.txt`
   - commit column: value from `git rev-parse HEAD` (after Task 0 batch commit; same amend pattern as Phase 71 D-04)

3. .github/scripts/check-coverage.sh: extended per Task 2 (already staged for this commit)

4. .planning/STATE.md: update progress table row for Phase 72 to SHIPPED with commit SHA

5. .planning/ROADMAP.md: update Phase 72 entry status from Pending to SHIPPED with commit SHA + date

6. .planning/phases/72-p0-core-supplement/72-13-SUMMARY.md: write phase-end summary with:
   - Coverage delta (12.8% → NEW_VALUE%)
   - Per-CORE target achieved status (all 6)
   - Commit SHA (Task 0 batch SHA + ratchet SHA)
   - Phase executor model name
   - Test count total across all plans
   - Files modified total across all plans
   - Task 0 batch commit reference

After writing all 6 files:
- git add .coverage-threshold .planning/coverage-baseline.md .github/scripts/check-coverage.sh .planning/STATE.md .planning/ROADMAP.md .planning/phases/72-p0-core-supplement/72-13-SUMMARY.md
- git commit -m "docs(72): coverage ratchet 12.8% → $(awk '/weighted avg/ {print $NF}' coverage.out)%

- CORE-01..06 all ≥70% per-sub-module
- check-coverage.sh extended for per-sub-package validation
- coverage-baseline.md Phase 72 后 row appended
- STATE.md + ROADMAP.md marked SHIPPED per I3
- 12 plan files (72-01..72-12) covered by Task 0 batch commit

Co-Authored-By: Claude <noreply@anthropic.com>"

If git push fails because branch is protected (Phase 71 had this issue — see cfec2c4), adapt: create a new commit instead of amend. Document in SUMMARY.

CRITICAL: All 6 file modifications MUST be in a single commit (D-07 atomic ratchet).
  </action>
  <verify>
    <automated>git log --oneline -3 && cat .coverage-threshold && grep -c "Phase 72 后" .planning/coverage-baseline.md</automated>
  </verify>
  <acceptance_criteria>
    - Single commit contains all 6 files: .coverage-threshold + .planning/coverage-baseline.md + .github/scripts/check-coverage.sh + .planning/STATE.md + .planning/ROADMAP.md + 72-13-SUMMARY.md
    - .coverage-threshold value > 12.8 (ratchet UP only)
    - .planning/coverage-baseline.md has "Phase 72 后" section with full per-package + per-sub-module tables, populated with concrete values from Task 1
    - .planning/STATE.md and .planning/ROADMAP.md updated to mark Phase 72 SHIPPED (per I3 — in ratchet commit)
    - Commit message starts with docs(72): coverage ratchet
    - git log --oneline -1 shows the ratchet commit on the current branch
    - Prior Task 0 batch commit contains only *_test.go files
  </acceptance_criteria>
  <done>
Phase 72 atomic ratchet committed; coverage-baseline.md updated; CI gate extended; STATE.md + ROADMAP.md marked SHIPPED.
  </done>
</task>

<task type="auto">
  <name>Task 4: CI verification + final SUMMARY closeout</name>
  <files>.planning/phases/72-p0-core-supplement/72-13-SUMMARY.md</files>
  <read_first>
    - .github/workflows/ci.yml (Coverage gate step)
    - .planning/phases/72-p0-core-supplement/72-13-SUMMARY.md (current draft)
  </read_first>
  <action>
Per I3: STATE.md and ROADMAP.md updates were already shipped in the ratchet commit (Task 3) so all docs land together per D-07. This task focuses solely on CI verification + SUMMARY closeout.

Verify CI gate will pass with new ratchet value:

Commands (push + watch):
- git push origin main 2>&1 | tail -5
- gh run watch --repo OWNER/REPO --exit-status 2>&1 | tail -20

Or if local validation only:
- go test -timeout 15m -count=1 -coverprofile=c.out ./internal/... ./pkg/... ./cmd/...
- bash .github/scripts/check-coverage.sh c.out .coverage-threshold

Both should exit 0 with the new threshold. If CI fails:
- Diagnose the failing step
- Fix the underlying issue (likely: a test fails on CI runner but passed locally — investigate env difference)
- Re-push

If CI passes:
- Capture CI run URL for SUMMARY
- Finalize 72-13-SUMMARY.md with:
  - Coverage delta (12.8% → NEW_VALUE%)
  - Per-CORE target status table (CORE-01..06 ✓)
  - CI run URL
  - Final commit SHA (ratchet commit)
  - Files modified total (across all 13 plans)
  - Total test count
  - Task 0 batch commit SHA + test file count

No further commits needed — STATE.md + ROADMAP.md already SHIPPED in ratchet commit (Task 3) per I3.
  </action>
  <verify>
    <automated>bash .github/scripts/check-coverage.sh c.out .coverage-threshold 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - bash .github/scripts/check-coverage.sh c.out .coverage-threshold exits 0
    - Output shows PASS: weighted avg X.X% >= threshold Y.Y%
    - Output shows per-sub-package PASS lines for all 28 cells (14 sub-modules × 2 packages)
    - 72-13-SUMMARY.md finalized with all sections complete (coverage delta, per-CORE status, CI run URL, final SHA, files modified count, test count)
    - All commits pushed to origin/main (or documented why deferred)
  </acceptance_criteria>
  <done>
Phase 72 complete; CI gate green with new ratchet; all documentation already shipped in ratchet commit (Task 3).
  </done>
</task>

</tasks>

<verification>
- bash .github/scripts/check-coverage.sh c.out .coverage-threshold exits 0 with PASS line
- Per-sub-package validation passes for all 28 cells (14 sub-modules × 2 packages)
- .coverage-threshold value > 12.8 (ratchet UP)
- .planning/coverage-baseline.md has new Phase 72 后 row
- All 6 CORE targets ≥70% per-sub-module
- Atomic ratchet commit with 6 files (D-07): .coverage-threshold + .planning/coverage-baseline.md + .planning/STATE.md + .planning/ROADMAP.md + .github/scripts/check-coverage.sh + 72-13-SUMMARY.md
- Prior batch commit (Task 0) contains only *_test.go files from plans 72-01..72-12
</verification>

<success_criteria>
- [ ] Phase 72 SC#7: Weighted avg ≥30% (12.8% → ≥30%), CI gate ratchet updated
- [ ] Phase 72 SC#8: 0 business code changes (only test files added; per-plan documented)
- [ ] check-coverage.sh extended for per-sub-package validation (CORE-04 + CORE-06 14 sub-modules each)
- [ ] All 6 CORE targets ≥70% per-sub-module (D-04)
- [ ] Atomic commit with 6 files (D-07) — includes STATE.md + ROADMAP.md per I3
- [ ] Prior Task 0 batch commit collects all per-module test files (resolves W2 ambiguity)
</success_criteria>

<output>
Create .planning/phases/72-p0-core-supplement/72-13-SUMMARY.md (final phase summary) when done.
</output>