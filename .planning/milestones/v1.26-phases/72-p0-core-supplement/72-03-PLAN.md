---
phase: 72-p0-core-supplement
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/v1/scheduler/job_handler_test.go
autonomous: true
requirements:
  - CORE-03
user_setup: []

must_haves:
  truths:
    - "`go test -cover -count=1 ./internal/api/v1/scheduler/...` exits 0 with coverage >= 70%"
    - "All 12+ handler methods on JobHandler have at least happy-path + error-path coverage"
    - "JobService and JobLogService interfaces mocked via lightweight stub (D-01 pattern)"
    - "JSON binding failures and service errors propagate correctly via responseHelpers"
  artifacts:
    - path: "internal/api/v1/scheduler/job_handler_test.go"
      provides: "Table-driven handler tests for CORE-03 sub-package"
      contains: "package scheduler"
  key_links:
    - from: "internal/api/v1/scheduler/job_handler_test.go"
      to: "internal/services/scheduler"
      via: "JobService + JobLogService interface mocks"
      pattern: "(jobService|jobLogService)\\.(Create|List|GetByID|Update|Delete|Start|Stop|...)"
---

<objective>
Add handler tests for `internal/api/v1/scheduler` (CORE-03, 152 stmts, 0.0% → ≥70%).

Purpose: Scheduler is the smallest of the 6 CORE targets but still a critical operation path (定时任务 management for cron jobs).

Output: One `job_handler_test.go` covering all handler methods on `*JobHandler`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-p0-core-supplement/72-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/quick/260820-backend-test-coverage-scan/per-package-coverage.txt

Read these source files first:
- internal/api/v1/system/ad_account_handler_test.go (D-01 lightweight pattern — READ THIS for actual mock service code idiom)
- internal/api/v1/scheduler/job_handler.go (already loaded; enumerates Create, List, ...)
- internal/api/v1/scheduler/job_router.go (handler constructor signature)
- internal/services/scheduler/job_service.go (JobService interface methods)
- internal/services/scheduler/job_log_service.go (JobLogService interface methods)
- internal/utils/operlog/operlog.go (Record signature)
- pkg/response/response.go + pkg/response/responseHelpers.go (response + error handling)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate JobHandler methods + design mock services</name>
  <files>internal/api/v1/scheduler/job_handler_test.go</files>
  <read_first>
    - internal/api/v1/scheduler/job_handler.go (full file; 152 stmts target)
    - internal/services/scheduler/job_service.go (JobService interface)
    - internal/services/scheduler/job_log_service.go (JobLogService interface)
    - internal/api/v1/system/ad_account_handler_test.go (D-01 lightweight pattern — READ for actual mock-with-function-fields idiom)
  </read_first>
  <action>
Read full `job_handler.go` (152 stmts). Enumerate ALL exported handler methods (grep `^func (h \*JobHandler)`). Group by category:
- CRUD: Create / List / GetByID / Update / Delete
- Lifecycle: Start / Stop / RunOnce
- Logs: GetJobLogs / ListJobLogs / GetLatestJobLog / CleanJobLogs

For each method, identify:
- Request struct (e.g. `schedulerServices.JobCreateRequest`)
- Service method called (e.g. `jobService.Create(ctx, req)`)
- Response shape

Design 2 mock stubs following D-01 lightweight pattern (mirror ad_account_handler_test.go):
- `mockJobService` — implements `schedulerServices.JobService` interface with function-field pattern (`CreateFunc func(...) (...)`)
- `mockJobLogService` — implements `schedulerServices.JobLogService` interface with same pattern

Verify both interfaces via compile-time assertion at top of test file:
- `var _ schedulerServices.JobService = (*mockJobService)(nil)`
- `var _ schedulerServices.JobLogService = (*mockJobLogService)(nil)`

Output: comment block at top of test file with method enumeration + mock design. No executable code yet.
  </action>
  <verify>
    <automated>go build ./internal/api/v1/scheduler/... 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Comment block enumerates ≥10 handler methods (152 stmts likely covers 12+ methods)
    - Mock service stubs satisfy JobService + JobLogService interfaces (compile-time assertions present)
    - `go build ./internal/api/v1/scheduler/...` exits 0
  </acceptance_criteria>
  <done>
Enumeration + mock design complete; stub file compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement table-driven handler tests for all methods</name>
  <files>internal/api/v1/scheduler/job_handler_test.go</files>
  <read_first>
    - internal/api/v1/scheduler/job_handler.go (full method bodies)
    - internal/services/scheduler/job_service.go (JobService interface contracts)
    - pkg/response/response.go (response format)
    - internal/models/scheduler/job.go (Job model fields)
    - internal/api/v1/system/ad_account_handler_test.go (READ THIS — actual D-01 pattern reference for mock service with function fields, gin.CreateTestContext + httptest.NewRecorder idiom, JSON unmarshal assertions, request body setup via httptest.NewRequest + strings.NewReader)
  </read_first>
  <action>
For each handler method identified in Task 1, write 2-3 test cases:
- `TC1: <method>_success_returns_200_with_payload` — mock service returns canned data, assert response shape via JSON unmarshal
- `TC2: <method>_service_error_returns_500` — mock service returns `errors.New("svc error")`, assert error code path
- `TC3: <method>_bad_request_returns_400` (for methods with required fields) — pass invalid JSON body, assert 400 response

Test invocation idiom (per D-01 — see ad_account_handler_test.go for actual code):
- Build a `httptest.NewRecorder()` and a gin context via `gin.CreateTestContext(w)`.
- For methods with JSON body binding: set `c.Request = httptest.NewRequest("POST", URL, strings.NewReader(body))` and set `Content-Type: application/json` header before invoking the handler.
- Invoke the handler directly (e.g. `h.Create(c)`).
- Assert `assert.Equal(t, 200, w.Code)` and parse `w.Body` via `json.Unmarshal(w.Body.Bytes(), &resp)` to verify payload fields.

Mock service setup (per D-01 function-field idiom — see ad_account_handler_test.go):
- Declare `mockJobService` struct with one function-field per interface method.
- In each test, populate only the function fields the handler under test will invoke; leave others nil (they will not be reached).
- Construct handler via `NewJobHandler(mockSvc, mockLogSvc)` with both stubs.

Coverage target: every method body line. Skip:
- `operlog.Record` calls — `h.core = nil` is safe (operlog handles nil)
- Lines that only run with real cron scheduler (use lightweight stub)
- Validation regex/parsing — covered via TC3 bad-request case

Verify total coverage ≥70% after tests written.
  </action>
  <verify>
    <automated>go test -cover -count=1 ./internal/api/v1/scheduler/... 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `go test -cover -count=1 ./internal/api/v1/scheduler/...` exits 0 with `coverage: ≥70.0%`
    - At least 20 test cases (10 methods × 2 scenarios minimum)
    - No `testify/mock` import — only `assert` + `require` from testify
    - No `assert.True(true)` placeholder assertions
  </acceptance_criteria>
  <done>
All scheduler handler methods have ≥2 test cases; coverage ≥70%.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify coverage + write SUMMARY</name>
  <files>.planning/phases/72-p0-core-supplement/72-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/72-p0-core-supplement/72-CONTEXT.md
  </read_first>
  <action>
Final coverage verification:

Commands:
- go test -coverprofile=c.out -count=1 ./internal/api/v1/scheduler/
- go tool cover -func=c.out | tail -1

If < 70%:
- Identify uncovered branches via `go tool cover -html=c.out -o coverage.html`
- Add missing test cases (likely: validation edge cases, type switch defaults)
- Re-run

Clean up `c.out` and `coverage.html`. Write `72-03-SUMMARY.md`:
- Coverage achieved (target ≥70%)
- Test count
- File modified
- Commit message draft: `test(72-03): CORE-03 scheduler handler tests 0% → ≥70%`

Do NOT commit yet — Task 0 in plan 72-13 handles the batch commit of all per-plan test files (per W2 fix).
  </action>
  <verify>
    <automated>go test -cover -count=1 ./internal/api/v1/scheduler/... 2>&1 | grep -E "^(ok|FAIL|coverage:)"</automated>
  </verify>
  <acceptance_criteria>
    - Coverage ≥70% verified
    - `72-03-SUMMARY.md` exists with all required fields
    - No leftover coverage artifacts
  </acceptance_criteria>
  <done>
CORE-03 target achieved (scheduler package ≥70%); SUMMARY documented.
  </done>
</task>

</tasks>

<verification>
- `go test -cover -count=1 ./internal/api/v1/scheduler/...` exits 0 with coverage ≥70%
- All handler methods on JobHandler have ≥2 test cases
- Zero business code changes (D-08)
</verification>

<success_criteria>
- [ ] CORE-03 SC#3: `internal/api/v1/scheduler` coverage ≥70% (152 stmts)
- [ ] All exported handler methods have at least happy-path + error-path coverage
- [ ] No new mock framework introduced (D-06)
</success_criteria>

<output>
Create `.planning/phases/72-p0-core-supplement/72-03-SUMMARY.md` when done.
</output>