---
phase: 61-resource-permission-matrix-and-rate-limit-tuning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/permission/resource_action_map.go
  - pkg/permission/resource_action_map_test.go
  - internal/middleware/apikey.go
  - internal/middleware/apikey_resource_permission_test.go
  - internal/middleware/apikey_inherit_integration_test.go
  - internal/api/router.go
autonomous: true
requirements_addressed:
  - AUTH-04

must_haves:
  truths:
    - "RequireAPIKeyResourcePermission(resource, action) 查 map 命中 → 走 RequireScope 路径;未命中 → 403 + 中止"
    - "InheritPerms=true 的 API Key 在 MultiAuth 中加载 User 权限代码并与 scopes 取并集,User 权限加载失败 → 401"
    - "InheritPerms=false 的 API Key 行为不变,只校验 API Key 自带 scopes"
    - "c.Set('username', ...) 和 c.Set('nickname', ...) 取 apiKey.User.Username/Nickname(apiKey.User 已 Preload)"
    - "static resource_action_map 覆盖 system:* 全 10 个资源 × 全部 D-04 词汇 action,新增 resource 必须显式补 map entry"
    - "MultiAuth 改造后 router.go 调用形态为 MultiAuth(apiKeyService, usageLogger, permissionService)"
    - "D-02: resource_action_map 仅覆盖 system:* 模块(user/role/menu/dept/post/workstation/dict/config/captchaBackground/notice/apikey 共 10 资源),monitor:*/network:*/tool:*/operations:* 不纳入"
    - "D-05: RequireAPIKeyResourcePermission 仍保留为公共 helper,本 phase 不挂载到 apikey_router.go(仅单元测试 + 文档化),最小爆炸半径"
  artifacts:
    - path: "pkg/permission/resource_action_map.go"
      provides: "D-01 静态 map + D-03 fail-closed lookup"
      min_lines: 80
    - path: "pkg/permission/resource_action_map_test.go"
      provides: "D-20 单元测试:命中 / 未命中 / 多种 action"
      min_lines: 60
    - path: "internal/middleware/apikey.go"
      provides: "D-06/D-07/D-08/D-09/D-10 MultiAuth+setUserContextForAPIKey+RequireAPIKeyResourcePermission"
      contains: "permission.NewService"
    - path: "internal/middleware/apikey_resource_permission_test.go"
      provides: "D-20 中间件单元测试:命中 / 未命中 403"
      min_lines: 80
    - path: "internal/middleware/apikey_inherit_integration_test.go"
      provides: "D-20/D-21 集成测试:sqlite in-memory + 真实 permission.Service"
      min_lines: 120
    - path: "internal/api/router.go"
      provides: "D-06 MultiAuth 调用形态变更"
      contains: "permission.NewService()"
  key_links:
    - from: "internal/middleware/apikey.go"
      to: "pkg/permission/resource_action_map.go"
      via: "LookupResourceAction(resource, action) 调用"
      pattern: "permission\\.LookupResourceAction"
    - from: "internal/middleware/apikey.go"
      to: "pkg/permission/service.go"
      via: "permSvc.GetUserPermissions(db, userID) InheritPerms 加载"
      pattern: "GetUserPermissions"
    - from: "internal/middleware/apikey.go"
      to: "internal/api/router.go"
      via: "MultiAuth 签名变更"
      pattern: "permission\\.NewService"
---

<objective>
让 `RequireAPIKeyResourcePermission(resource, action)` 的 `resource` 参数真实生效(接入资源→权限映射,system:* 全 10 个资源覆盖),并让 `InheritPerms=true` 实时加载 User 权限代码与 API Key scopes 取并集;同时修正 `c.Set("username"/"nickname")` 从 apiKey.User 关联读取。

Purpose: 把 Phase 60 已挂载的 `MultiAuth` 链从「认证只到 scope」推到「认证 + 资源权限细粒度 + User 继承权限」三段闭环,达成 AUTH-04 (ex-FUTURE-APIKEY-01) 的 P0 接入。

Output: 静态资源权限矩阵 + InheritPerms DB 加载合并 + username/nickname 语义修正 + 三层测试覆盖。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/61-resource-permission-matrix-and-rate-limit-tuning/61-CONTEXT.md
@.planning/phases/60-security-hardening-and-enable-decision/60-CONTEXT.md
@internal/middleware/apikey.go
@pkg/permission/config.go
@pkg/permission/service.go
@internal/services/system/apikey_service.go
@internal/api/router.go
@internal/models/api_key.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 pkg/permission/resource_action_map.go(D-01/D-02/D-03/D-04)+ 单元测试</name>
  <files>pkg/permission/resource_action_map.go, pkg/permission/resource_action_map_test.go</files>
  <read_first>
    - pkg/permission/config.go(PermissionCode 常量表,D-04 action 词汇对齐目标)
    - .planning/phases/61-resource-permission-matrix-and-rate-limit-tuning/61-CONTEXT.md(D-02 覆盖清单 10 个资源 + D-03 fail-closed 语义)
  </read_first>
  <action>
    新建文件 `pkg/permission/resource_action_map.go`,包含:

    1. **静态嵌套 map 声明**(D-01 编译期常量):
       ```go
       var resourceActionMap = map[string]map[string]PermissionCode{ ... }
       ```
       按 D-02 覆盖 system:* 10 个资源 × D-04 词汇 action:
       - `system:user` → list/view/add/edit/remove/export/import/resetPwd(7 项)
       - `system:role` → list/view/add/edit/remove/export(6 项)
       - `system:menu` → list/view/add/edit/remove(5 项)
       - `system:dept` → list/view/add/edit/remove(5 项)
       - `system:post` → list/view/add/edit/remove(5 项)
       - `system:workstation` → list/view/add/edit/remove(5 项)
       - `system:dict` → list/view/add/edit/remove(5 项)
       - `system:config` → list/view/add/edit/remove(5 项)
       - `system:captchaBackground` → list/view/add/edit/remove(5 项)
       - `system:notice` → list/view/add/edit/remove(5 项)
       - `system:apikey` → list/view/add/edit/remove(5 项)
       每个 entry 映射到 `pkg/permission/config.go` 的对应 PermissionCode 常量(如 `system:user:list` → `UserList`),不允许字符串拼接。

    2. **导出函数 `LookupResourceAction(resource, action string) (PermissionCode, bool)`**:
       - 查 map 命中 → 返回 `(code, true)`
       - 查 map 未命中 → 返回 `("", false)`,由调用方决定如何处理
       - **不**返回默认值或 panic,严格 fail-closed 由调用方判定

    3. **导出函数 `MapKeys() (resources []string, actions []string)`**(D-20 测试辅助):
       - 返回所有已注册 resource / action key 列表,供测试断言完整性

    4. **package 注释**包含:本文件由 Phase 61 AUTH-04 引入;新增 resource / action 时必须显式补 map entry(否则 D-03 拒绝);monitor:* / network:* / tool:* / operations:* 不在本 phase 范围。

    新建文件 `pkg/permission/resource_action_map_test.go`,覆盖:
    - `TestLookupResourceAction_Hit`:遍历 D-02 全部 (resource, action) 组合,断言返回 PermissionCode 非空 + bool=true + 值与 config.go 常量一致(`assert.Equal(t, UserList, code)`)。
    - `TestLookupResourceAction_UnmappedResource`:`LookupResourceAction("monitor:operlog", "list")` 返回 `("", false)`(monitor:* 不在本 phase map 中,D-03 fail-closed)。
    - `TestLookupResourceAction_UnmappedAction`:`LookupResourceAction("system:user", "flyToMoon")` 返回 `("", false)`。
    - `TestMapKeys_AllSystemModules`:断言返回的 resources 包含全部 10 个 D-02 资源名(`assert.Contains(t, resources, "system:user")` ... `system:apikey`),共 11 项(含 `system:apikey`)。
    - `TestMapKeys_ActionVocabularyAligned`:断言 actions 包含 D-04 词汇子集(`list`、`view`、`add`、`edit`、`remove`、`export`、`import`、`resetPwd`)。
  </action>
  <verify>
    <automated>cd D:\code\ClaudeCode\guoguo && go build ./pkg/permission/... && go test -v -run "TestLookupResourceAction|TestMapKeys" ./pkg/permission/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./pkg/permission/...` 退出码 0
    - `go test -v ./pkg/permission/...` 全部 PASS,新增至少 5 个测试用例
    - `grep -c "PermissionCode" pkg/permission/resource_action_map.go` >= 50(覆盖 D-02 全部 entry)
    - `grep -E "monitor:|network:|tool:|operations:" pkg/permission/resource_action_map.go` 无结果(本 phase 不纳入)
    - `grep "func LookupResourceAction" pkg/permission/resource_action_map.go` 恰好 1 个导出函数
  </acceptance_criteria>
  <done>静态 map 文件 + 单元测试就位,D-01/D-02/D-03/D-04 全覆盖,新增 resource 编译期漏报错。</done>
</task>

<task type="auto">
  <name>Task 2: 改造 internal/middleware/apikey.go MultiAuth + setUserContextForAPIKey + RequireAPIKeyResourcePermission(D-06/D-07/D-08/D-09/D-10)+ router.go 调用形态</name>
  <files>internal/middleware/apikey.go, internal/api/router.go</files>
  <read_first>
    - internal/middleware/apikey.go(原 MultiAuth 行 24-88,setUserContextForAPIKey 行 155-175,RequireAPIKeyResourcePermission 行 217-225)
    - pkg/permission/service.go(GetUserPermissions 行 272-286 真实 DB 查询)
    - internal/api/router.go(行 254-257 MultiAuth 调用形态)
    - .planning/phases/61-resource-permission-matrix-and-rate-limit-tuning/61-CONTEXT.md(D-06 scopes 并集、D-09 fail-closed、D-10 username 修正)
    - internal/services/system/apikey_service.go(行 167-170 ValidateAPIKey 已 Preload("User"))
  </read_first>
  <action>
    修改 `internal/middleware/apikey.go`,**仅限以下三个函数**,其他函数(getRequiredScope / RateLimitByScope / getScopeFromContext 等)由 Plan 02 修改:

    1. **`MultiAuth` 签名扩展**(D-06 加载 User 权限需要 permission service):
       - 原:`func MultiAuth(apiKeyService system.APIKeyService, usageLogger services.UsageLogger) gin.HandlerFunc`
       - 新:`func MultiAuth(apiKeyService system.APIKeyService, usageLogger services.UsageLogger, permSvc permission.Service) gin.HandlerFunc`
       - 调用 `setUserContextForAPIKey` 时传入新参数 `permSvc` 与 `c.Request.Context()`。

    2. **`setUserContextForAPIKey` 签名扩展 + D-06/D-07/D-09/D-10 全部落地**(行 155-175):
       - 原:`func setUserContextForAPIKey(c *gin.Context, apiKey *models.APIKey, scopes []string)`
       - 新:`func setUserContextForAPIKey(c *gin.Context, apiKey *models.APIKey, scopes []string, permSvc permission.Service)`
       - 在 `c.Set("scopes", scopes)` 之后,新增以下逻辑(D-06 InheritPerms 加载入口):
         - 若 `apiKey.InheritPerms && apiKey.User != nil && apiKey.UserID != nil`:调用 `permSvc.GetUserPermissions(c.Request.Context(), *apiKey.UserID)` 加载 User 权限代码 `[]string`
         - 加载成功:`mergedScopes` = `append(scopes, userPermissions...)` 去重;`c.Set("scopes", mergedScopes)`
         - 加载失败(DB error / userID 为空 / permSvc 返回 error):调用 `response.Error(c, response.ErrUnauthorized, "用户权限加载失败")` + `c.Abort()` + return(**不**继续 setUserContextForAPIKey 的剩余步骤)
       - D-08 行为保留:若 `apiKey.InheritPerms == false`,跳过 User 加载,`scopes` 仅含 API Key 自带。
       - D-10 username 修正:`c.Set("username", apiKey.Name)` 改为 `c.Set("username", apiKey.User.Username)`(**仅**当 `apiKey.User != nil`,否则保留 `apiKey.Name` 并用 pkg/logger.Warnf 记录缺失);同理 `c.Set("nickname", "")` 改为 `c.Set("nickname", apiKey.User.Nickname)`(同缺失兜底)。
       - D-07 不引入缓存:每次请求调用一次 DB,**不**写入 cache 或 context pre-compute。
       - 注意:`c.Set("auth_type", "api_key")` 之后执行以上 InheritPerms 分支,以保证 auth_type 总被设置;InheritPerms 分支需在 `c.Set("api_key_id", apiKey.ID)` 之前完成(因 abort 路径不应已设完所有 key)。

    3. **`RequireAPIKeyResourcePermission` 接入静态 map**(D-03 fail-closed,D-05 不挂载到 apikey_router 仅做 helper):
       - 原函数体仅 `return RequireScope(getRequiredScope(action))` — resource 参数被忽略。
       - 新增第一步:**查 map**:
         ```go
         permCode, ok := permission.LookupResourceAction(resource, action)
         if !ok {
             response.Error(c, response.ErrForbidden, "资源权限未定义: "+resource+":"+action)
             return  // 注意: 不 c.Next(), D-03 fail-closed
         }
         ```
       - 第二步:查到的 `permCode` 转换为 scope(用 `getRequiredScope(action)`,跨 Plan 02 复用既有函数,**不**重复实现 scope 映射)。
       - 第三步:**新增 `scopes` 集合检查**(利用 `c.Get("scopes")`):
         - 类型断言 `[]string`,空 / 类型错误 → `response.Error(c, ErrForbidden, "权限作用域格式错误")` + 不调 `c.Next()`
         - 遍历检查 `permCode` 是否在 `scopes` 中(支持 `admin` 通配,同 `RequireScope` 既有逻辑)
         - 不命中 → `response.Error(c, ErrForbidden, "资源权限不足: "+string(permCode))`
         - 命中 → `c.Next()`
       - **注意**:`getRequiredScope` 由 Plan 02 扩展 `list→read`,本 Plan 01 复用既有实现(view/create/edit/delete → read/write,默认 read)。

    4. **新增 import**:`"github.com/xingran-next/xingran-go-backend/pkg/permission"`(已存在的 package,无新依赖)。

    修改 `internal/api/router.go`(行 254-257,**仅修改 MultiAuth 调用行**,RateLimitByScope 行由 Plan 02 修改):
    - 原:`apikeys.Use(internalmw.MultiAuth(systemServices.NewAPIKeyService(core.GetDB()), services.NewUsageLogger(core.GetDB())))`
    - 新:在 args 后追加 `, permission.NewService()`,注意 import 路径 `"github.com/xingran-next/xingran-go-backend/pkg/permission"`。
    - 不修改 router.go 其他部分(RequirePermissions 行 243-248 由 Phase 60 锁定)。

    5. **不引入新依赖**,全部使用既有 package(permission、models、response、logger)。
  </action>
  <verify>
    <automated>cd D:\code\ClaudeCode\guoguo && go build ./... && go vet ./internal/middleware/... ./pkg/permission/... ./internal/api/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` 退出码 0
    - `go vet ./internal/middleware/... ./pkg/permission/...` 无 warning
    - `grep -n "func MultiAuth" internal/middleware/apikey.go` 签名包含 `permSvc permission.Service` 参数
    - `grep -n "func setUserContextForAPIKey" internal/middleware/apikey.go` 签名包含 `permSvc permission.Service` 参数
    - `grep -n "func RequireAPIKeyResourcePermission" internal/middleware/apikey.go` 函数体内含 `LookupResourceAction` 调用 + 未命中时 `ErrForbidden` 响应
    - `grep -n "GetUserPermissions" internal/middleware/apikey.go` 至少 1 处(在 InheritPerms 分支)
    - `grep -n "permission.NewService()" internal/api/router.go` 至少 1 处(MultiAuth 调用形态变更)
    - Phase 60 既有 `apikey_integration_test.go` 的 `TestMultiAuthIntegration` 不回归(MultiAuth 仍是 3 路径:200/403/401)
    - Phase 60 既有 `TestRateLimitHeadersInResponse` 仍编译通过(本 Plan 01 不修改 RateLimitByScope 签名,但 Plan 02 会修改 — 编译失败由 Plan 02 修复)
  </acceptance_criteria>
  <done>MultiAuth+setUserContextForAPIKey+RequireAPIKeyResourcePermission 三个函数全部改造完成,D-06/D-07/D-08/D-09/D-10 全部落地,router.go 调用形态变更,InheritPerms 加载路径真实生效。</done>
</task>

<task type="auto">
  <name>Task 3: 单元测试(apikey_resource_permission_test.go)+ 集成测试(apikey_inherit_integration_test.go)覆盖 D-20/D-21</name>
  <files>internal/middleware/apikey_resource_permission_test.go, internal/middleware/apikey_inherit_integration_test.go</files>
  <read_first>
    - internal/middleware/apikey.go(MultiAuth + setUserContextForAPIKey + RequireAPIKeyResourcePermission 改造后状态)
    - internal/middleware/apikey_integration_test.go(行 1-120 Phase 57 fake 模式 + 行 30-100 fakeAPIKeyService + fakeUsageLogger,本 Plan 01 复用 fake 模式)
    - pkg/permission/service.go(GetUserPermissions 行 272-286)
    - internal/services/system/apikey_service.go(行 167-170 ValidateAPIKey 已 Preload)
    - .planning/codebase/TESTING.md(无 gomock,真实 sqlite 模式)
    - .planning/phases/59-observability-usage-log-fix/59-CONTEXT.md(D-03 sqlite in-memory 模式)
  </read_first>
  <action>
    新建文件 `internal/middleware/apikey_resource_permission_test.go`(D-20 中间件单元测试):

    1. **`TestRequireAPIKeyResourcePermission_Hit`**:
       - 构造 gin.Engine,设置 `c.Set("scopes", []string{"admin"})`
       - 调用 `permission.RequireAPIKeyResourcePermission("system:user", "list")`
       - 路由 handler 返回 200
       - 断言响应 200(命中 → admin 含所有权限)

    2. **`TestRequireAPIKeyResourcePermission_HitReadScope`**:
       - 构造 `c.Set("scopes", []string{"read"})`
       - 调用 `RequireAPIKeyResourcePermission("system:user", "view")`
       - 断言 200(view → read 命中)

    3. **`TestRequireAPIKeyResourcePermission_MissingScope`**:
       - 构造 `c.Set("scopes", []string{"read"})`
       - 调用 `RequireAPIKeyResourcePermission("system:user", "edit")`(edit → write)
       - 断言 403 + `code != 0`(D-03 fail-closed,read 不含 write)

    4. **`TestRequireAPIKeyResourcePermission_UnmappedResource`**:
       - 构造 `c.Set("scopes", []string{"admin"})`
       - 调用 `RequireAPIKeyResourcePermission("monitor:operlog", "list")`(monitor:* 不在 map)
       - 断言 403 + 响应 message 含 "资源权限未定义"(D-03 fail-closed 未命中)

    5. **`TestRequireAPIKeyResourcePermission_UnmappedAction`**:
       - 构造 `c.Set("scopes", []string{"admin"})`
       - 调用 `RequireAPIKeyResourcePermission("system:user", "flyToMoon")`
       - 断言 403 + message 含 "资源权限未定义"

    6. **`TestRequireAPIKeyResourcePermission_MissingScopes`**:
       - 不设置 `c.Set("scopes", ...)`(模拟缺失)
       - 调用 `RequireAPIKeyResourcePermission("system:user", "list")`
       - 断言 403

    新建文件 `internal/middleware/apikey_inherit_integration_test.go`(D-20/D-21 集成测试,沿用 Phase 59 D-03 sqlite 模式):

    1. **`setupInheritPermsTestDB(t *testing.T) *gorm.DB`**:沿用 Phase 59 setupUsageLoggerTestDB 模式(os.TempDir + 唯一文件名 + busy_timeout=5000),自动迁移 `models.APIKey`、`models.User`、`models.Role`、`models.RoleMenu`、`models.Menu`、`models.UserRole` 表(必须包含本测试要 INSERT 的所有模型)。

    2. **`createTestUserWithPermissions(t, db, userID string, permCodes []string)` helper**:
       - INSERT 一个 User(`status = 0`)
       - INSERT 若干 Menu(`perms = permCodes[i]`, `status = 0`)
       - INSERT 一个 Role + RoleMenu 关联 + UserRole 关联

    3. **`createTestAPIKey(t, db, userID string, scopes []string, inheritPerms bool) (keyHash, salt, keyPrefix string, fullKey string)` helper**:
       - 沿用 Phase 60 `apikey_service.go:121-128 generateKey` + 行 137-142 `hashAPIKey` + 行 145-151 `generateSalt` 模式(直接调用,或复制 helper 函数)
       - INSERT 一个 APIKey 模型(含 `key_hash`、`salt`、`key_prefix`、`user_id`、`scopes`、`inherit_perms`、`is_active=true`)

    4. **`TestMultiAuthInheritPerms_MergeScopes`**:
       - DB 中:User(u1)关联 Role(关联 Menu `system:user:list`、`system:user:view`)
       - APIKey(`scopes=["read"]`、`inherit_perms=true`、`user_id=u1`)
       - 路由:`r.Use(MultiAuth(realAPIKeyService, fakeLogger, realPermService))` → handler 读 `c.Get("scopes")`
       - 断言:handler 读到的 scopes **同时包含** `["read", "system:user:list", "system:user:view"]`(顺序不敏感,用 `assert.ElementsMatch`)
       - D-06 验证通过

    5. **`TestMultiAuthInheritPerms_FailClosed`**:
       - DB 中:User(u1)无任何 Role 关联(用户存在但 GetUserPermissions 返回空 []string — 这不算失败,正常路径)
       - 验证另一个失败场景:**UserID 为 nil**(通过 INSERT APIKey 不带 user_id + `inherit_perms=true`)
       - 路由同上
       - 断言:响应 401(D-09 fail-closed)

    6. **`TestMultiAuthInheritPerms_DBError`**:
       - DB 中:User(u1)存在,**关闭 DB 连接**(`sqlDB.Close()`)后发请求
       - 路由同上
       - 断言:响应 401 + message 含 "用户权限加载失败"
       - 用 pkg/logger 记录失败日志(沿用 Phase 59 D-04 惯例)

    7. **`TestMultiAuthInheritPerms_UsernameFromUser`**:
       - DB 中:User(u1, `Username="zhangsan"`, `Nickname="张三"`)
       - APIKey 关联 u1,`inherit_perms=true`
       - 路由 handler 读 `c.Get("username")` + `c.Get("nickname")`
       - 断言:`username == "zhangsan"`(D-10 从 User.Username 取,不是 apiKey.Name),`nickname == "张三"`

    8. **`TestMultiAuthInheritPerms_False_NoUserLoad`**:
       - DB 中:APIKey(`scopes=["read"]`、`inherit_perms=false`)
       - 路由 handler 读 `c.Get("scopes")`
       - 断言:scopes == `["read"]`(D-08 行为不变,只含 API Key 自带 scopes)
       - 用 `assert.ElementsMatch` 比较

    注意:
    - **不**修改既有 `apikey_test.go` 与 `apikey_integration_test.go`(由 Plan 02 负责)
    - **不**引入 gomock(沿用 fake / 真实 sqlite 模式,D-21)
    - **真实** `permission.NewService()` + 真实 DB,**不**用 mock 库
  </action>
  <verify>
    <automated>cd D:\code\ClaudeCode\guoguo && go test -v -run "TestRequireAPIKeyResourcePermission|TestMultiAuthInheritPerms" ./internal/middleware/... && go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test -v -run "TestRequireAPIKeyResourcePermission" ./internal/middleware/...` 全部 PASS(5 个用例)
    - `go test -v -run "TestMultiAuthInheritPerms" ./internal/middleware/...` 全部 PASS(5 个用例)
    - `grep -c "Preload" internal/services/system/apikey_service.go` 包含 "Preload(\"User\")" 至少 1 次(D-10 验证 apikey_service 已 Preload,本 Plan 01 无需修改)
    - 全量 `go test ./...` 不引入新失败(D-21 与 Phase 57/59/60 测试基线不冲突)
    - 测试文件 `apikey_resource_permission_test.go` 与 `apikey_inherit_integration_test.go` 不修改既有 `apikey_test.go` 和 `apikey_integration_test.go`
  </acceptance_criteria>
  <done>D-20/D-21 三层测试覆盖完整:map 单测 + 中间件单测 + 集成测(sqlite 真实 DB),InheritPerms 五场景全验证。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | 客户端携带 `X-API-Key` 头进入 MultiAuth |
| middleware→DB | MultiAuth 通过 permSvc.GetUserPermissions 访问 sys_menu/sys_role_menu/sys_user_role 表 |
| middleware→pkg/permission | LookupResourceAction 静态 map 查询,无外部 IO |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Spoofing | LookupResourceAction / RequireAPIKeyResourcePermission | mitigate | D-03 fail-closed:map 未命中 → 403,新增 resource 必须显式补 entry(编译期检查);`grep` 检查 monitor:/network:/tool:/operations: 在 map 中无结果 |
| T-61-02 | Tampering | setUserContextForAPIKey InheritPerms 分支 | mitigate | D-06 加载 User 权限后用 `append + 去重` 写入 scopes,不修改 User 权限代码本身;D-07 不缓存 → 每次请求 DB 真实值,角色菜单变更即时生效 |
| T-61-03 | Repudiation | MultiAuth 使用日志 | accept | 既有 UsageLogger 已记录(Phase 59 D-02 detached context),InheritPerms 失败用 pkg/logger 记录 |
| T-61-04 | Information Disclosure | MultiAuth InheritPerms DB 查询 | accept | DB 查询仅取 `perms` 列(行 274-275 SQL 显式 SELECT DISTINCT m.perms),不暴露用户其他敏感字段;DB 查询失败仅返 "用户权限加载失败"(不区分 DB error vs 用户不存在) |
| T-61-05 | Denial of Service | MultiAuth 每次请求 DB 查询 InheritPerms | accept | D-07 决策:API Key 认证是 admin CRUD 管理面,请求频率极低;缓存层加 Redis 复杂度大,留给 FUTURE-APIKEY-04;每请求一次 DB 查询的 DoS 风险面由系统整体限流(`/system/apikeys/*` 已挂载 RateLimitByScope)兜底 |
| T-61-06 | Elevation of Privilege | RequireAPIKeyResourcePermission scope 检查 | mitigate | D-03+D-06 双重校验:① resource+action 必须命中 map(D-03 fail-closed)② merged scopes 必须含对应 PermissionCode 或 admin;D-08 InheritPerms=false 行为不变,不会绕过校验 |
| T-61-07 | Tampering | pkg/permission/resource_action_map.go 静态 map 被运行时修改 | mitigate | map 是 package-level `var`,Go runtime 不允许运行时修改 map 键的 type-safety(只能 add/delete/change value);修改源码需 PR review + 编译期 diff |

**Package Legitimacy:** 本 Plan 01 不安装任何新包,全部复用既有 import。
</threat_model>

<verification>
- [ ] Phase 60 QUAL-01 回归(限流响应头 strconv.Itoa 仍正确):`go test -v -run TestRateLimitHeaderEncoding ./internal/middleware/...` PASS
- [ ] Phase 59 OBSERV-01 回归(使用日志 StatusCode/Duration/Success 真实值):`go test -v -run TestMultiAuthUsageLogTiming ./internal/middleware/...` PASS
- [ ] Phase 57 AUTH-01 回归(MultiAuth 3 路径 200/403/401):`go test -v -run TestMultiAuthIntegration ./internal/middleware/...` PASS
- [ ] 本 Plan 01 新增:5 个 map 单测 + 5 个 ResourcePermission 中间件单测 + 5 个 InheritPerms 集成测 PASS
- [ ] 全量 `go build ./...` 退出码 0,全量 `go test ./...` 不引入新失败
- [ ] `grep -E "monitor:|network:|tool:|operations:" pkg/permission/resource_action_map.go` 无结果(D-02 范围限定)
</verification>

<success_criteria>
1. `RequireAPIKeyResourcePermission(resource, action)` 的 `resource` 参数真实生效:命中走 RequireScope 路径,未命中 → 403 + 中止(`grep` 函数体含 `LookupResourceAction` 调用 + 未命中时 ErrForbidden 响应)。
2. InheritPerms=true 加载 User 权限代码与 scopes 取并集,失败 → 401 + `response.Error(c, ErrUnauthorized, "用户权限加载失败")`(`grep` 函数体含 `GetUserPermissions` + 失败分支)。
3. 静态 map 覆盖 D-02 全 10 个资源 × D-04 词汇 action(共 56 个 entry,`grep -c PermissionCode` >= 50)。
4. `c.Set("username", apiKey.User.Username)` + `c.Set("nickname", apiKey.User.Nickname")`(`grep` 函数体含 `apiKey.User.Username` + `apiKey.User.Nickname`)。
5. router.go MultiAuth 调用形态含 `permission.NewService()` 参数(`grep -n "permission.NewService()" internal/api/router.go`)。
6. 既有 Phase 57/59/60 测试不回归(`go test ./...` 全绿,仅允许 Plan 02 修复 RateLimitByScope 签名引发的编译失败)。
</success_criteria>

<output>
Create `.planning/phases/61-resource-permission-matrix-and-rate-limit-tuning/61-01-SUMMARY.md`
</output>