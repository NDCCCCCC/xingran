---
phase: 59-observability-usage-log-fix
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - internal/middleware/apikey_integration_test.go
  - internal/services/usage_logger_test.go
  - internal/services/system/apikey_service_test.go
autonomous: true
requirements: [OBSERV-01, OBSERV-02, OBSERV-03]

must_haves:
  truths:
    - "D-03: SC#1/#2/#4 测试用 sqlite 真实文件 DB（既有 setupUsageLoggerTestDB helper，per-test 独立文件 os.TempDir+UnixNano+pid + busy_timeout=5000 + 裸 CREATE TABLE），DB 行实证而非 fake（fake 不写库无法满足 SC#1/#4）"
    - "D-03a: Phase 57 既有 fake 测试（apikey_integration_test.go 的 fakeUsageLogger/fakeAPIKeyService/setupUsageLoggerTestDB/hex64/TestMultiAuthIntegration 全部；usage_logger_test.go 既有子测试；apikey_service_test.go 既有 TestGetUsageLogSummary + 成功率计算 子测试）原样保留，新测试与之并存不回归"
    - "SC#1（2xx 请求后日志行 StatusCode∈2xx / Duration>0 / Success=true）由 TestMultiAuthUsageLogTiming 用真实 NewUsageLogger + 真实 gin.Engine + 真实 sqlite DB 行断言"
    - "SC#2（下游失败 RequireScope→403 / RateLimitByScope→429 / handler→5xx 后 Success=false / StatusCode=真实码）由 TestMultiAuthUsageLogFailure 用真实 DB 行断言；**避开 pre-auth 401**（pitfall 3：pre-auth 失败在 c.Next() 前 abort 不会记录）"
    - "SC#3（混合 success 行后 successRate ∈ (0,100) 开区间，不恒 ≈ 0%）由 TestGetUsageLogSummaryMixed 用直接 seed 真实 DB 行 + 调 GetUsageLogSummary 断言"
    - "SC#4（调用方 ctx cancel 后日志仍完整写入，P2-b 消除）由 TestLogUsageCancelledCtxStillWrites_D02 用预取消 ctx + require.Eventually 轮询 DB 行断言"
    - "异步等待用 `require.Eventually(t, predicate, 2*time.Second, 10*time.Millisecond)` 替代既有 `time.Sleep` flaky 反模式；封装 `waitForUsageLog(t, db, apiKeyID, want)` helper（middleware 包与 services 包各定义一份同名，因 Go 测试包隔离）"
    - "三个 pitfall 在测试中规避：① 不用 db.AutoMigrate（gen_random_uuid() 是 PG 专有陷阱，sqlite 报 no such function）—— 沿用既有裸 CREATE TABLE DDL；② 不用 file::memory:?cache=shared（fire-and-forget goroutine 与测试主线程争写锁撞锁）—— 沿用文件 DB；③ SC#2 失败用例用下游 RequireScope→403 不用 pre-auth 401"
  artifacts:
    - path: "internal/middleware/apikey_integration_test.go"
      provides: "SC#1（2xx 时序/字段）+ SC#2（下游失败 Success=false）真实 DB 集成测试 + waitForUsageLog helper（middleware 包）"
      contains: "TestMultiAuthUsageLogTiming"
    - path: "internal/services/usage_logger_test.go"
      provides: "SC#4（预取消 ctx 仍写库）cancel-race 单元测试 + waitForUsageLog helper（services 包）"
      contains: "TestLogUsageCancelledCtxStillWrites_D02"
    - path: "internal/services/system/apikey_service_test.go"
      provides: "SC#3（混合 success 行 → successRate ∈ (0,100)）聚合防回归测试"
      contains: "TestGetUsageLogSummaryMixed"
  key_links:
    - from: "internal/middleware/apikey_integration_test.go waitForUsageLog"
      to: "require.Eventually polling DB row count"
      via: "异步落库确定性等待（替代 time.Sleep flaky 反模式）"
      pattern: "require\\.Eventually\\("
    - from: "internal/services/usage_logger_test.go waitForUsageLog"
      to: "require.Eventually polling DB row count"
      via: "异步落库确定性等待（services 包内副本）"
      pattern: "require\\.Eventually\\("
    - from: "新增测试函数名"
      to: "59-VALIDATION.md Phase Requirements→Test Map（锁定，不得偏离）"
      via: "测试名 → VALIDATION 命令一一对应"
      pattern: "func Test(MultiAuthUsageLogTiming|MultiAuthUsageLogFailure|GetUsageLogSummaryMixed|LogUsageCancelledCtxStillWrites_D02)"
---

<objective>
为 Plan 01 修复后的源码契约提供 DB 行实证级别的回归测试覆盖。新增 4 个子测试（SC#1/#2/#3/#4 各一）+ 1 个异步等待 helper（`waitForUsageLog`），覆盖三条 requirement（OBSERV-01/02/03）的全部 success criteria。fake 测试（Phase 57）原样保留，真实 DB 测试与之职责正交并存。

Purpose: 满足 VALIDATION.md Nyquist Dimension 8 验证契约——SC#1/#4 必须 DB 行实证（非代码推断）；用 `require.Eventually` 轮询替代既有 `time.Sleep` flaky 反模式；三个 pitfall（gen_random_uuid / 撞锁 / pre-auth 401）在 acceptance 与 action 中显式规避。
Output: 3 个既有测试文件扩展，零新增依赖（仅用既有 testify require.Eventually + 既有 sqlite driver）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-observability-usage-log-fix/59-CONTEXT.md
@.planning/phases/59-observability-usage-log-fix/59-RESEARCH.md
@.planning/phases/59-observability-usage-log-fix/59-PATTERNS.md
@.planning/phases/59-observability-usage-log-fix/59-VALIDATION.md
@.planning/phases/59-observability-usage-log-fix/59-01-PLAN.md

<interfaces>
<!-- Plan 02 测试需依赖的契约（来自 Plan 01 修复后的源码 + 既有测试基建）。Executor 直接用，无需探索。 -->

From Plan 01 修复后的 internal/middleware/apikey.go（contract 不变 + 三个新字段填充）:
```go
// MultiAuth(apiKeyService system.APIKeyService, usageLogger services.UsageLogger) gin.HandlerFunc
// 修复后: setUserContextForAPIKey → start:=time.Now() → c.Next() →
//         statusCode:=c.Writer.Status() / duration:=time.Since(start).Milliseconds() →
//         usageLogger.LogUsage(c.Request.Context(), &services.LogUsageRequest{
//             APIKeyID, UserID, Method, Path, ClientIP,
//             StatusCode: statusCode,
//             Duration:   int(duration),
//             Success:    statusCode >= 200 && statusCode < 300,  // D-01
//         })
// pre-auth 失败 (apikey.go:35/42/50 c.Abort;return) 在 c.Next() 前退出，不记录 (pitfall 3)
```

From Plan 01 修复后的 internal/services/usage_logger.go（contract 不变 + detached ctx）:
```go
// LogUsage(ctx, req) error  // 签名不变; 内部 go logUsageAsync(ctx, req); return nil
// logUsageAsync 内部用 context.WithTimeout(context.Background(), 10s) 写 DB（忽略调用方 ctx cancel）
// 失败用 applogger.Errorf 记录（非 _ = err）
```

From internal/middleware/apikey_integration_test.go 既有 helpers（VERIFIED, Phase 57 留存, D-03a 保留）:
```go
// 既有 (line 33-68): fakeAPIKeyService 实现 system.APIKeyService 9 方法，ValidateAPIKey 给真逻辑
//   用法: fakeSvc := &fakeAPIKeyService{validKey: &models.APIKey{BaseModel: models.BaseModel{ID: "ak-sc1"}, Scopes: []string{"read"}, IsActive: true}}
// 既有 (line 76-100): fakeUsageLogger + newFakeUsageLogger() + waitForLog(t)  ← 本 phase 不动
// 既有 (line 111-137): setupUsageLoggerTestDB(t) *gorm.DB  ← 复用，新测试直接调
// 既有 (line 143-145): hex64() string  // 64 chars, 满足 isValidKeyFormat

// 既有 (line 147+): TestMultiAuthIntegration 三路径集成测试  ← 本 phase 不动 (D-03a)
```

From internal/services/usage_logger_test.go 既有 helper（VERIFIED, 复用）:
```go
// 既有 (line 30-57): setupUsageLoggerTestDB(t) *gorm.DB  ← 复用
//   （与 apikey_integration_test.go 版同源复制，per-test 独立文件 DB）
// 既有 (line 60+): TestNewUsageLogger + 多个 time.Sleep 反模式子测试  ← 本 phase 不动 (D-03a)
```

From internal/services/system/apikey_service_test.go 既有 helpers（VERIFIED, 复用）:
```go
// 既有 (line 37):  setupTestDB(t) *gorm.DB  // 与 setupUsageLoggerTestDB 不同，已建 sys_api_key_usage_logs 表
// 既有 (line 190): createTestUser(t, db) *models.User
// 既有 (line 210): createTestAPIKey(t, db, userID, isActive) *models.APIKey
// 既有 (line 259): cleanupTestData(t, db)
// 既有 (line 1015): TestGetUsageLogSummary + 子测试 "成功率计算" (line 1096-1133, 70% 用例)  ← 本 phase 不动 (D-03a)
```

From 59-VALIDATION.md Phase Requirements→Test Map（锁定测试名 + 命令，不得偏离）:
```
OBSERV-01/SC#1 → go test ./internal/middleware/ -run TestMultiAuthUsageLogTiming -v
OBSERV-01/SC#2 → go test ./internal/middleware/ -run TestMultiAuthUsageLogFailure -v
OBSERV-02/SC#3 → go test ./internal/services/system/ -run TestGetUsageLogSummaryMixed -v
OBSERV-03/SC#4 → go test ./internal/services/ -run TestLogUsageCancelledCtx -v   (前缀匹配 TestLogUsageCancelledCtxStillWrites_D02)
```

From 59-RESEARCH.md §异步写入可测试性机制（推荐 helper 形态）:
```go
func waitForUsageLog(t *testing.T, db *gorm.DB, apiKeyID string, want int64) {
    t.Helper()
    require.Eventually(t, func() bool {
        var count int64
        db.Model(&models.APIKeyUsageLog{}).Where("api_key_id = ?", apiKeyID).Count(&count)
        return count >= want
    }, 2*time.Second, 10*time.Millisecond,
        "usage log for key=%s not persisted within 2s", apiKeyID)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: apikey_integration_test.go — 新增 SC#1（2xx 时序/字段）+ SC#2（下游失败）真实 DB 集成测试 + waitForUsageLog helper</name>
  <files>internal/middleware/apikey_integration_test.go</files>

  <read_first>
    - `internal/middleware/apikey_integration_test.go`（全文 ~250 行，重点 line 33-68 fakeAPIKeyService / line 76-100 fakeUsageLogger（D-03a 保留不动）/ line 102-145 helpers 全部 / line 147+ TestMultiAuthIntegration（D-03a 保留不动））
    - `internal/middleware/apikey.go`（Plan 01 修复后状态——确认 c.Next() 后填字段 + 同步调 LogUsage 的契约；重点 line 23-78 MultiAuth + line 197-201 RequireScope 中断点 + line 262-267 RateLimitByScope 中断点，确认 SC#2 用 RequireScope 缺 scope→403 走下游 c.Next()）
    - `internal/services/usage_logger.go`（Plan 01 修复后——确认 LogUsage 内部异步 + logUsageAsync 用 detached ctx）
    - `internal/services/system/apikey_service.go`（APIKeyService 接口 + ValidateAPIKey 签名，用于 fakeAPIKeyService 装配）
    - `.planning/phases/59-observability-usage-log-fix/59-PATTERNS.md`（§apikey_integration_test.go 段落 + §Shared Patterns §异步写入可测试性 含 SC#1/#2 完整骨架 + waitForUsageLog helper）
    - `.planning/phases/59-observability-usage-log-fix/59-VALIDATION.md`（§Phase Requirements→Test Map 测试名锁定 + §异步写入确定性策略）
    - `.planning/phases/59-observability-usage-log-fix/59-RESEARCH.md`（§Pitfall 1/2/3 + §异步写入可测试性机制）
  </read_first>

  <action>
    在 `internal/middleware/apikey_integration_test.go` **追加**（不修改既有 fakeUsageLogger / fakeAPIKeyService / setupUsageLoggerTestDB / hex64 / TestMultiAuthIntegration）以下内容：

    **(a) 新增 helper `waitForUsageLog`**（落本文件，因 Go 测试包隔离无法跨包导入；与 services 包版本同名同形）:
    按 RESEARCH.md §异步写入可测试性机制 / PATTERNS.md §Shared Patterns 给的形态——`t.Helper()` + `require.Eventually(t, func() bool { var count int64; db.Model(&models.APIKeyUsageLog{}).Where("api_key_id = ?", apiKeyID).Count(&count); return count >= want }, 2*time.Second, 10*time.Millisecond, "usage log for key=%s not persisted within 2s", apiKeyID)`。

    **(b) 新增子测试 `TestMultiAuthUsageLogTiming`**（SC#1，2xx 时序/字段 DB 行实证）:
    - `gin.SetMode(gin.TestMode)` + `db := setupUsageLoggerTestDB(t)` + `realLogger := services.NewUsageLogger(db)`（**真实** logger，非 fake）
    - 构造 fakeAPIKeyService：`&fakeAPIKeyService{validKey: &models.APIKey{BaseModel: models.BaseModel{ID: "ak-sc1"}, Scopes: []string{"read"}, IsActive: true}}`
    - 装配 router：`router := gin.New(); router.Use(MultiAuth(fakeSvc, realLogger)); router.GET("/ok", RequireScope("read"), func(c *gin.Context) { c.JSON(200, gin.H{"ok": true}) })`
    - 请求：`req := httptest.NewRequest("GET", "/ok", nil); req.Header.Set("X-API-Key", "rec_"+hex64()); w := httptest.NewRecorder(); router.ServeHTTP(w, req)`
    - 断言：`assert.Equal(t, 200, w.Code)`
    - 等待异步落库：`waitForUsageLog(t, db, "ak-sc1", 1)`
    - 读 DB 行 + 字段断言（SC#1 DB 行实证）: `var log models.APIKeyUsageLog; require.NoError(t, db.Where("api_key_id = ?", "ak-sc1").First(&log).Error)` → `assert.Equal(t, 200, log.StatusCode)` / `assert.Greater(t, log.Duration, 0)` / `assert.True(t, log.Success)`（D-01: 2xx → Success=true）

    **(c) 新增子测试 `TestMultiAuthUsageLogFailure`**（SC#2，下游失败 Success=false；**pitfall 3 规避：用 RequireScope→403 不用 pre-auth 401**）:
    - 同 (b) setup 但 apiKey.ID 改为 `"ak-fail"`，Scopes 仍为 `["read"]`
    - 路由：`router.GET("/write-only", RequireScope("write"), func(c *gin.Context) { c.JSON(200, nil) })`——key 仅 read scope，RequireScope("write") 在 c.Next() 下游 abort 403（apikey.go RequireScope 中断）
    - 请求同 (b) 但 path=/write-only
    - 断言：`assert.Equal(t, 403, w.Code)`（确认下游 RequireScope 真实触发 403）
    - 等待异步落库：`waitForUsageLog(t, db, "ak-fail", 1)`
    - 读 DB 行 + 字段断言（SC#2 DB 行实证）: `assert.Equal(t, 403, log.StatusCode)` / `assert.False(t, log.Success)`（D-01: 4xx → Success=false）

    **pitfall 规避（务必显式落实）:**
    - **不用 `db.AutoMigrate(&models.APIKeyUsageLog{})`**——既有 setupUsageLoggerTestDB 已用裸 CREATE TABLE（绕过 gen_random_uuid PG 专有陷阱）；本任务仅复用既有 helper，不改 helper。
    - **不用 pre-auth 401**——SC#2 必须用 RequireScope→403（发生在 c.Next() 下游，MultiAuth 返回时 c.Writer.Status() 捕获 403）；若误用 pre-auth 401（invalid key / IP 拒），c.Abort 在 c.Next() 前退出，无日志行，测试会因「找不到行」误判失败。
    - **既有 fakeUsageLogger / fakeAPIKeyService / setupUsageLoggerTestDB / hex64 / TestMultiAuthIntegration 全部保留**（D-03a）——新子测试是追加，不是替换。

    import 块无需新增（testify assert/require、httptest、gorm、sqlite、services、models、gin、time 均已存在 line 3-24）。
  </action>

  <verify>
    <automated>cd D:/code/ClaudeCode/guoguo && go test ./internal/middleware/ -run "TestMultiAuthUsageLogTiming|TestMultiAuthUsageLogFailure" -v</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./internal/middleware/ -run "TestMultiAuthUsageLogTiming|TestMultiAuthUsageLogFailure" -v` 退出码 0（两个新子测试均 PASS）
    - `internal/middleware/apikey_integration_test.go` 含字面 `func TestMultiAuthUsageLogTiming(` 与 `func TestMultiAuthUsageLogFailure(`（两个新测试函数）
    - `internal/middleware/apikey_integration_test.go` 含 `require.Eventually(` 调用（waitForUsageLog helper 内）
    - `internal/middleware/apikey_integration_test.go` 含 `services.NewUsageLogger(db)`（**真实** logger 实例化，非 fake）
    - `internal/middleware/apikey_integration_test.go` 含 `assert.Equal(t, 200, log.StatusCode)` + `assert.True(t, log.Success)`（SC#1 DB 行断言）
    - `internal/middleware/apikey_integration_test.go` 含 `assert.Equal(t, 403, log.StatusCode)` + `assert.False(t, log.Success)`（SC#2 DB 行断言，下游失败码非 pre-auth 401）
    - 既有 `fakeUsageLogger` / `fakeAPIKeyService` / `setupUsageLoggerTestDB` / `hex64` / `TestMultiAuthIntegration` 全部保留（D-03a）—— grep 这些标识符命中数与 Plan 02 开始前一致
    - `internal/middleware/apikey_integration_test.go` **不**含 `db.AutoMigrate(&models.APIKeyUsageLog{})`（pitfall 1 规避）
    - `internal/middleware/apikey_integration_test.go` **不**含 `file::memory:?cache=shared`（pitfall 2 规避）
    - 既有 `go test ./internal/middleware/ -run TestMultiAuthIntegration -v`（Phase 57 fake 测试）仍 PASS（D-03a 不回归）
  </acceptance_criteria>

  <done>
    SC#1（2xx → StatusCode=200 / Duration>0 / Success=true）+ SC#2（下游 RequireScope→403 → StatusCode=403 / Success=false）通过真实 DB 行实证；waitForUsageLog helper 落地；既有 fake 测试不回归。
  </done>
</task>

<task type="auto">
  <name>Task 2: usage_logger_test.go — 新增 SC#4（cancel-race）cancel-race 单元测试 + waitForUsageLog helper</name>
  <files>internal/services/usage_logger_test.go</files>

  <read_first>
    - `internal/services/usage_logger_test.go`（全文 533 行，重点 line 18-57 setupUsageLoggerTestDB 注释化迁移决策（解释为何文件 DB 而非 in-memory）+ helper / line 60+ 既有测试含多处 time.Sleep flaky 反模式（D-03a 保留不动））
    - `internal/services/usage_logger.go`（Plan 01 修复后——确认 logUsageAsync 用 detached ctx 写 DB；调用方 ctx 不用于取消控制）
    - `.planning/phases/59-observability-usage-log-fix/59-PATTERNS.md`（§usage_logger_test.go 段落 + §Shared Patterns §异步写入可测试性 含 SC#4 完整骨架）
    - `.planning/phases/59-observability-usage-log-fix/59-VALIDATION.md`（§Phase Requirements→Test Map + §异步写入确定性策略）
    - `.planning/phases/59-observability-usage-log-fix/59-RESEARCH.md`（§异步写入可测试性机制 + §Pitfall 1/2）
  </read_first>

  <action>
    在 `internal/services/usage_logger_test.go` **追加**（不修改既有 setupUsageLoggerTestDB / TestNewUsageLogger / 其它 time.Sleep 子测试）以下内容：

    **(a) 新增 helper `waitForUsageLog`**（落本文件，因 Go 测试包隔离；与 middleware 包版本同名同形）:
    与 Task 1 (a) 完全相同的形态——`t.Helper()` + `require.Eventually(t, func() bool { var count int64; db.Model(&models.APIKeyUsageLog{}).Where("api_key_id = ?", apiKeyID).Count(&count); return count >= want }, 2*time.Second, 10*time.Millisecond, ...)`。

    **(b) 新增子测试 `TestLogUsageCancelledCtxStillWrites_D02`**（SC#4，cancel-race 防回归；测试名前缀匹配 VALIDATION 命令 `-run TestLogUsageCancelledCtx`）:
    - `db := setupUsageLoggerTestDB(t)` + `logger := NewUsageLogger(db)`
    - 预取消 ctx（模拟 P2-b 场景——请求结束 ctx.Canceled）: `ctx, cancel := context.WithCancel(context.Background()); cancel()`
    - 调用：`err := logger.LogUsage(ctx, &LogUsageRequest{APIKeyID: "cancel-race-key", UserID: "u1", Method: "GET", Path: "/test", StatusCode: 200, Success: true, Duration: 10})` + `require.NoError(t, err)`
    - 等待异步落库（SC#4 核心）: `waitForUsageLog(t, db, "cancel-race-key", 1)`
      - **修复前**（Plan 01 前）: logUsageAsync 复用 ctx，已 cancel → `s.db.WithContext(ctx).Create()` 失败 → `_ = err` 吞错 → 行永不出现 → require.Eventually 超时失败
      - **修复后**（Plan 01 后 D-02）: logUsageAsync 用 detachedCtx，忽略调用方 cancel → 行落库 → require.Eventually 成功
    - 读 DB 行 + 字段断言（SC#4 DB 行实证）: `var log models.APIKeyUsageLog; require.NoError(t, db.Where("api_key_id = ?", "cancel-race-key").First(&log).Error)` → `assert.Equal(t, 200, log.StatusCode)` / `assert.True(t, log.Success)`

    **pitfall 规避（务必显式落实）:**
    - **不用 `db.AutoMigrate(&models.APIKeyUsageLog{})`**——既有 setupUsageLoggerTestDB 已用裸 CREATE TABLE（pitfall 1 绕开 gen_random_uuid）。
    - **沿用既有文件 DB 模式**（既有 setupUsageLoggerTestDB line 30-57 已正确）——不退回 `file::memory:?cache=shared`（pitfall 2 撞锁）。
    - **既有 setupUsageLoggerTestDB / TestNewUsageLogger / 既有 TestAsyncLogging 等含 time.Sleep 的子测试全部保留**（D-03a）——本任务是追加，不重构既有 time.Sleep 反模式（保留职责正交）。

    import 块无需新增（context / time / testify assert+require / models / gorm 均已存在 line 3-16）。
  </action>

  <verify>
    <automated>cd D:/code/ClaudeCode/guoguo && go test ./internal/services/ -run TestLogUsageCancelledCtxStillWrites_D02 -v</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./internal/services/ -run TestLogUsageCancelledCtxStillWrites_D02 -v` 退出码 0（SC#4 子测试 PASS）
    - `internal/services/usage_logger_test.go` 含字面 `func TestLogUsageCancelledCtxStillWrites_D02(`
    - `internal/services/usage_logger_test.go` 含 `require.Eventually(` 调用（waitForUsageLog helper 内）
    - `internal/services/usage_logger_test.go` 含 `context.WithCancel(context.Background())` + `cancel()`（预取消 ctx 模拟 P2-b）
    - `internal/services/usage_logger_test.go` 含 `assert.Equal(t, 200, log.StatusCode)` + `assert.True(t, log.Success)`（SC#4 DB 行实证）
    - 既有 `setupUsageLoggerTestDB` / `TestNewUsageLogger` / `TestAsyncLogging` 等子测试保留（D-03a 不回归）—— 既有 `go test ./internal/services/ -run TestNewUsageLogger -v` 仍 PASS
    - `internal/services/usage_logger_test.go` **不**含 `db.AutoMigrate(&models.APIKeyUsageLog{})`（pitfall 1 规避）
    - `internal/services/usage_logger_test.go` **不**含新增 `file::memory:?cache=shared`（pitfall 2 规避——既有注释化的历史迁移决策不动）
  </acceptance_criteria>

  <done>
    SC#4（预取消 ctx 后日志仍完整落库，D-02 detached ctx 生效）通过 DB 行实证；waitForUsageLog helper 落地；既有 time.Sleep 子测试不回归。
  </done>
</task>

<task type="auto">
  <name>Task 3: apikey_service_test.go — 新增 SC#3（混合 success 行 → successRate ∈ (0,100)）聚合防回归测试</name>
  <files>internal/services/system/apikey_service_test.go</files>

  <read_first>
    - `internal/services/system/apikey_service_test.go`（重点 line 37 setupTestDB / line 190 createTestUser / line 210 createTestAPIKey / line 259 cleanupTestData / line 1015-1133 TestGetUsageLogSummary + 子测试「成功率计算」（D-03a 保留不动，本任务的克隆模板））
    - `internal/services/system/apikey_service.go:465-585`（GetUsageLogSummary 实现 + line 519 `successCount/total*100` 聚合——OBSERV-02 连锁点）
    - `.planning/phases/59-observability-usage-log-fix/59-PATTERNS.md`（§apikey_service_test.go 段落 含 SC#3 完整骨架）
    - `.planning/phases/59-observability-usage-log-fix/59-VALIDATION.md`（§Phase Requirements→Test Map）
    - `.planning/phases/59-observability-usage-log-fix/59-RESEARCH.md`（§Open Questions #1 SC#3 落点说明）
  </read_first>

  <action>
    在 `internal/services/system/apikey_service_test.go` **追加**（不修改既有 setupTestDB / createTestUser / createTestAPIKey / cleanupTestData / TestGetUsageLogSummary + 「成功率计算」子测试）一个新顶层测试函数 `TestGetUsageLogSummaryMixed`（SC#3 防回归锚）:

    按 PATTERNS.md §apikey_service_test.go 落地骨架:
    - `db := setupTestDB(t)` + `service := NewAPIKeyService(db)`（既有 setupTestDB 已建 sys_api_key_usage_logs 表，line 106-121 同裸 DDL 形态，无需另建）+ `ctx := context.Background()`
    - `user := createTestUser(t, db)` + `apiKey := createTestAPIKey(t, db, user.ID, true)`（既有 helpers）
    - **混合 seed 真实 DB 行**（不经 middleware，纯聚合逻辑防回归）:
      - 2 条 Success=true 行（StatusCode 200 / 204，Duration 50 / 30）—— 对应 OBSERV-01 修复后的「2xx → Success=true」
      - 2 条 Success=false 行（StatusCode 403 / 429，Duration 20 / 10）—— 对应 D-01「下游失败码 → Success=false」（403/429 是下游产生的失败，非 pre-auth 401）
      - 用 `db.Create(&models.APIKeyUsageLog{APIKeyID: apiKey.ID, UserID: user.ID, Method: "GET", Path: "...", StatusCode: ..., Duration: ..., Success: ...})` 直接插入
    - 调聚合：`summary, err := service.GetUsageLogSummary(ctx, apiKey.ID); require.NoError(t, err)`
    - 断言（SC#3 防回归，三道）:
      - `assert.Greater(t, summary.SuccessRate, 0.0)`（OBSERV-02 防回归：不恒 ≈ 0%——修复前 Success 永远 false → successRate 恒 0%）
      - `assert.Less(t, summary.SuccessRate, 100.0)`（不全成功）
      - `assert.InDelta(t, 50.0, summary.SuccessRate, 0.1)`（2/4 = 50%，精确锚）
    - `cleanupTestData(t, db)`

    **pitfall 规避 + 既有保留（务必显式落实）:**
    - **沿用既有 setupTestDB**（line 37-187）—— 不引入 setupUsageLoggerTestDB（该文件 setupTestDB 已正确建表，且其测试形态不同：无 fire-and-forget goroutine 与之并发，故 setupTestDB 用 `file::memory:?cache=shared&_enable_boolean=true` 是该文件既有正确选择，本任务不改）。
    - **既有 TestGetUsageLogSummary + 「成功率计算」子测试保留**（D-03a，line 1096-1133）——本任务是追加新顶层测试函数，不修改既有 t.Run 子测试。既有 70% 用例在 OBSERV-01 修复后仍应 PASS（若失败说明 Success 字段链路被破坏，是防回归锚的二道保险）。

    import 块无需新增（context / testify assert+require / models / gorm 均已存在）。
  </action>

  <verify>
    <automated>cd D:/code/ClaudeCode/guoguo && go test ./internal/services/system/ -run TestGetUsageLogSummaryMixed -v</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./internal/services/system/ -run TestGetUsageLogSummaryMixed -v` 退出码 0（SC#3 子测试 PASS）
    - `internal/services/system/apikey_service_test.go` 含字面 `func TestGetUsageLogSummaryMixed(`
    - `internal/services/system/apikey_service_test.go` 含 `assert.Greater(t, summary.SuccessRate, 0.0)` + `assert.Less(t, summary.SuccessRate, 100.0)` + `assert.InDelta(t, 50.0, summary.SuccessRate, 0.1)`（SC#3 三道防回归断言）
    - 新测试 seed 行含 Success=true 与 Success=false 各 ≥1 条（混合，验证聚合非全 0 / 非全 1）
    - 既有 `TestGetUsageLogSummary`（line 1015）+ 「成功率计算」子测试（line 1096-1133）保留（D-03a 不动）—— `go test ./internal/services/system/ -run TestGetUsageLogSummary$ -v` 仍 PASS（既有 70% 用例不回归）
    - `internal/services/system/apikey_service_test.go` **不**新增 `setupUsageLoggerTestDB`（沿用既有 setupTestDB；两个 helper 各服务其文件，不混用）
  </acceptance_criteria>

  <done>
    SC#3（混合 success 行 → successRate=50% ∈ (0,100) 开区间，OBSERV-02 不恒 ≈ 0%）通过 seed+聚合断言；既有「成功率计算」70% 子测试保留不回归。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → 测试 sqlite DB | 测试种子数据（合成 apiKeyID/UUID）写入 hermetic per-test 文件 DB，零外部状态污染 |
| Test code → 既有 fake 测试 | D-03a 防止本 plan 的新测试触碰 Phase 57 fake 测试，零回归风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-T1 | Tampering | apikey_integration_test.go / usage_logger_test.go 测试 DB DDL | mitigate | 复用既有 `setupUsageLoggerTestDB` 裸 `CREATE TABLE` DDL（id TEXT PRIMARY KEY），绕过 `gen_random_uuid()` PG 专有陷阱；任务 acceptance 显式禁止 `db.AutoMigrate(&models.APIKeyUsageLog{})` |
| T-59-T2 | Denial of Service | 测试 sqlite 文件 DB 写锁 | mitigate | 复用既有「per-test 独立文件 + busy_timeout=5000」模式；任务 acceptance 显式禁止 `file::memory:?cache=shared`（fire-and-forget goroutine 撞锁）、禁止 `t.TempDir()`（自动 cleanup 删占用文件 mark fail） |
| T-59-T3 | Repudiation | apikey_service_test.go SC#3 seed data | mitigate | seed 行 Success=true/false 混合，断言 successRate ∈ (0,100) 开区间——既验证 OBSERV-02 修复（不恒 ≈ 0%），也防止未来回归（全 0 或全 1 都会失败） |
| T-59-T4 | Information Disclosure | 测试中使用的 apiKeyID 值 | accept | 测试用合成字面量（"ak-sc1"/"ak-fail"/"cancel-race-key"）+ 既有 createTestAPIKey 生成的 UUID，非真实生产凭据；测试 DB hermetic 隔离 |
| T-59-T5 | Tampering | Phase 57 既有 fake 测试 | mitigate | D-03a：任务 acceptance 显式要求 fakeUsageLogger/fakeAPIKeyService/TestMultiAuthIntegration/既有 setupUsageLoggerTestDB 等标识符命中数与基线一致；新测试是 append-only |
| T-59-SC | Tampering | go mod 依赖（package legitimacy gate） | accept | 零新增依赖（仅用既有 testify `require.Eventually` + 既有 `gorm.io/driver/sqlite`）；gate 不适用 |

**Phase 安全态势净影响:** 中性——本 plan 仅新增测试，不动生产代码；测试 hermetic（per-test 独立文件 DB，零外部状态）；D-03a 保护既有测试不回归。
</threat_model>

<verification>
- 全 suite（必跑）: `go test ./internal/middleware/... ./internal/services/...` 退出码 0（含三个新子测试 + 既有全部测试）
- VALIDATION.md 锁定的 4 条 acceptance 命令一一通过:
  - `go test ./internal/middleware/ -run TestMultiAuthUsageLogTiming -v`（SC#1）
  - `go test ./internal/middleware/ -run TestMultiAuthUsageLogFailure -v`（SC#2）
  - `go test ./internal/services/system/ -run TestGetUsageLogSummaryMixed -v`（SC#3）
  - `go test ./internal/services/ -run TestLogUsageCancelledCtx -v`（SC#4，前缀匹配 TestLogUsageCancelledCtxStillWrites_D02）
- D-03a 防回归锚（应仍 PASS）:
  - `go test ./internal/middleware/ -run TestMultiAuthIntegration -v`（Phase 57 fake 三路径测试不回归）
  - `go test ./internal/services/ -run TestNewUsageLogger -v`（既有测试不回归）
  - `go test ./internal/services/system/ -run TestGetUsageLogSummary$ -v`（既有 70% 用例不回归）
- pitfall 规避字面检查（grep `AutoMigrate.*APIKeyUsageLog\|file::memory:?cache=shared` 在新增测试文件命中数应为 0——既有注释化的迁移决策在 usage_logger_test.go line 18-29 不算违规）
</verification>

<success_criteria>
- SC#1/#2/#3/#4 四条 Success Criteria 通过真实 DB 行实证（SC#1/#2/#4）或 seed+聚合断言（SC#3）——非代码推断
- SC#5（`go test ./internal/middleware/... ./internal/services/...` 全绿）达成
- VALIDATION.md 锁定的 4 条 Requirement→Test Map 命令一一对应、一一通过
- D-03（真实 sqlite 文件 DB）+ D-03a（既有 fake 测试不回归）字面落实
- 三个 pitfall（AutoMigrate/file::memory/pre-auth 401）在 acceptance 中显式规避
- OBSERV-01/02/03 三条 requirement 均有自动化测试覆盖（OBSERV-01=SC#1+#2；OBSERV-02=SC#3；OBSERV-03=SC#4）
</success_criteria>

<output>
Create `.planning/phases/59-observability-usage-log-fix/59-02-SUMMARY.md` when done
</output>
