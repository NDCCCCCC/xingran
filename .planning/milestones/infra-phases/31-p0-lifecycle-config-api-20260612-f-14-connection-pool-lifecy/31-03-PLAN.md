---
phase: 31
plan: 03
type: execute
wave: 3
depends_on: [31-01, 31-02]
files_modified:
  - internal/device/connection_pool_test.go (NEW)
autonomous: true
requirements:
  - F-14 (P0 connection_pool race window — 回归测试覆盖)
user_setup: []

must_haves:
  truths:
    - "新增 connection_pool_test.go 覆盖 GetConnection-Release 配对"
    - "并发测试模拟 100 goroutine 同时 GetConnection/Release,验证 refCount 正确归零"
    - "测试连接失效时 Release 不 panic"
    - "测试 cleanupIdleConnections 仅清理 refCount==0 的连接"
    - "go test ./internal/device/... 0 失败 + 新增 ≥3 个测试 case"
---

<objective>
F-14 修复第三步:补足并发回归测试,验证 31-01 + 31-02 的修复在并发场景下不再有 race window。
确保未来的重构能立刻感知到 refCount/race 退化。
</objective>

<context>
@.planning/phases/31-p0-lifecycle-config-api-20260612-f-14-connection-pool-lifecy/31-CONTEXT.md
@internal/device/connection_pool.go
@.planning/phases/31-p0-lifecycle-config-api-20260612-f-14-connection-pool-lifecy/31-01-PLAN.md
@.planning/phases/31-p0-lifecycle-config-api-20260612-f-14-connection-pool-lifecy/31-02-PLAN.md
</context>

<plan>

## 1. 新建 connection_pool_test.go

测试 case 设计:

### TestGetConnectionRefCount
```go
// 单次 GetConnection 后 refCount=1, Release 后 refCount=0
```

### TestGetConnectionReleaseConcurrent
```go
// 100 goroutine 并发 GetConnection + Release
// 最终 refCount=0,连接仍在池中(可被下次 GetConnection 复用)
```

### TestGetConnectionWhileCleanup
```go
// 启动 cleanupIdleConnections 后立刻 GetConnection
// 验证已经 +1 refCount 的连接不会被 cleanup 误关闭
```

### TestReleaseOnFailedConnection
```go
// 连接失效后 Release 不 panic (current Release 含负 refCount panic 防护)
```

由于 PooledConnection 依赖 ScrapliWrapper(真实 SSH),需要 mock 或使用 stub wrapper。
如果 mock 复杂度过高,允许仅覆盖 refCount 计数路径(不实际通信),
通过测试 helper 构造 fake PooledConnection。

## 2. 编译与执行

```bash
go test -count=1 -v -short -timeout 60s -run "TestGetConnection|TestRelease" ./internal/device/... 2>&1 | tail -30
```

## 3. -race 标志验证(可选但推荐)

```bash
go test -count=1 -race -short -timeout 120s -run "TestGetConnectionReleaseConcurrent" ./internal/device/...
```

预期 0 race detector 告警。

</plan>

<verification>
- [ ] connection_pool_test.go 包含 ≥3 个测试 case
- [ ] 单次 GetConnection/Release refCount 正确归零测试通过
- [ ] 100 goroutine 并发测试通过
- [ ] -race 标志下无 race 告警
- [ ] device 包测试 baseline failures 不变(仍是 0 个)
</verification>

<commit>
test(device): resolve F-14 part 3 — 并发回归测试覆盖 GetConnection-Release

新增 internal/device/connection_pool_test.go:
- TestGetConnectionRefCount: 单次 GetConnection/Release refCount 归零
- TestGetConnectionReleaseConcurrent: 100 goroutine 并发场景下 refCount 不漂移
- TestGetConnectionWhileCleanup: cleanup 与 GetConnection 并发安全
- TestReleaseOnFailedConnection: Release 容错验证

并发测试在 -race 模式下零告警,锁定 F-14 修复成果。

Refs: .planning/phases/31-.../31-CONTEXT.md D-01
      .planning/reviews/20260612-backend-code-review.md F-14
</commit>
