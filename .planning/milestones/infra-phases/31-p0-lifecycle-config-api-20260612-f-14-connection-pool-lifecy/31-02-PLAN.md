---
phase: 31
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - internal/device/task_scheduler.go
  - internal/services/portcollection/collection.go
  - internal/collectors/asset_collector.go
  - internal/collectors/port_collector.go
  - internal/device/executor.go (审查,可能无需修改)
autonomous: true
requirements:
  - F-14 (P0 connection_pool race window — caller 适配)
user_setup: []

must_haves:
  truths:
    - "所有 5 处 pool.GetConnection 调用方在用完后调用 conn.Release()(优先 defer)"
    - "不再调用 conn.Acquire() (因 31-01 已让 GetConnection 内部完成)"
    - "Release 在错误返回路径也正确执行(defer 模式)"
    - "go build ./... 通过"
    - "go vet ./... 通过"
    - "device + portcollection + collectors 测试 0 新增失败"
---

<objective>
F-14 修复第二步:全部 5 处 pool.GetConnection 的 caller 适配新语义。
caller 用 defer conn.Release() 替代原本可能存在的 Acquire 调用,
保证 refCount 正确递减并触发 cleanup 路径释放连接。
</objective>

<context>
@.planning/phases/31-p0-lifecycle-config-api-20260612-f-14-connection-pool-lifecy/31-CONTEXT.md
@.planning/phases/31-p0-lifecycle-config-api-20260612-f-14-connection-pool-lifecy/31-01-PLAN.md

# Caller 列表 (grep 结果)
- internal/device/task_scheduler.go:199
- internal/services/portcollection/collection.go:108-109
- internal/collectors/asset_collector.go:75-76
- internal/collectors/port_collector.go:52-53
- internal/device/executor.go:267 (经 scheduler.GetConnectionPool 间接获取,需确认是否直接 GetConnection)
</context>

<plan>

## 1. 审查每个 caller 现状

对每处 GetConnection 调用:
1. 检查是否当前有 `conn.Acquire()`,有则改为 `defer conn.Release()`
2. 检查是否当前有 `defer conn.Release()` — 若已有,保持不变(31-01 增加的 refCount 与现有 Release 仍正确配对)
3. 检查是否完全没有 Release 调用 — 若是,在 GetConnection 后立即加 `defer conn.Release()`

## 2. 修改 task_scheduler.go:199

```go
conn, err := s.connectionPool.GetConnection(ctx, task.DeviceID)
if err != nil {
    // existing handling
}
defer conn.Release()  // F-14: 必须 release 让 refCount 归零,允许 cleanup
// ... 使用 conn
```

## 3. 修改 portcollection/collection.go:108-109

```go
pool := s.executor.GetScheduler().GetConnectionPool()
conn, err := pool.GetConnection(ctx, device.ID)
if err != nil {
    // existing handling
}
defer conn.Release()
// ... 使用 conn
```

## 4. 修改 asset_collector.go:75-76 & port_collector.go:52-53

同样 pattern。

## 5. 检查 executor.go:267

经检查若仅是 `pool := e.scheduler.GetConnectionPool()` 而没有直接调用 pool.GetConnection,则不需修改。

## 6. 编译与回归

```bash
go build ./... 2>&1 | tail -5
go vet ./... 2>&1 | tail -3
go test -count=1 -short -timeout 60s ./internal/device/... ./internal/services/portcollection/... ./internal/collectors/... 2>&1 | tail -15
```

预期:
- build pass
- 测试 0 新增失败

</plan>

<verification>
- [ ] 5 处 caller 全部加 `defer conn.Release()`
- [ ] 没有 caller 在 GetConnection 后再调 `Acquire()` (会重复 +1)
- [ ] go build / vet 通过
- [ ] device + portcollection + collectors 测试无新失败
- [ ] 手动审计:错误返回路径上 Release 也被 defer 调用
</verification>

<commit>
fix(device): resolve F-14 part 2 — caller 适配新的 GetConnection refCount 语义

为所有 5 处 pool.GetConnection 调用添加 defer conn.Release(),
配对 31-01 引入的内部 refCount +1,确保 refCount 正确递减
让 cleanupIdleConnections 能在合适时机关闭连接。

caller 改动:
- internal/device/task_scheduler.go:199
- internal/services/portcollection/collection.go:108
- internal/collectors/asset_collector.go:75
- internal/collectors/port_collector.go:52
- internal/device/executor.go(仅审计)

defer 模式保证错误返回路径也会 Release,杜绝 refCount 泄漏。

Refs: .planning/phases/31-.../31-CONTEXT.md D-01
      .planning/reviews/20260612-backend-code-review.md F-14
</commit>
