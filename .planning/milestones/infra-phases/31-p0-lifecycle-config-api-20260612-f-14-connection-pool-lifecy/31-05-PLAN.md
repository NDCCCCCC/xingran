---
phase: 31
plan: 05
type: execute
wave: 5
depends_on: [31-04]
files_modified:
  - xingran-react-frontend/src/lib/configApi.ts (审查,可能无需修改)
  - xingran-react-frontend/src/pages/system/settings-page/index.tsx (审查)
  - docs/API响应规范.md 或 Swagger 自动生成 (审查)
  - .planning/phases/31-.../31-SUMMARY.md (NEW)
autonomous: true
requirements:
  - F-17 (P0 system config 键名保护 — 验证与文档同步)
user_setup: []

must_haves:
  truths:
    - "前端 TypeScript 类型如有 ConfigUpdateRequest 接口,同步添加 configKey?: string 字段"
    - "Swagger 文档重新生成,可见 ConfigKey 字段"
    - "31-SUMMARY.md 记录 phase 31 完成情况与回归数据"
    - "git stash pop 后(如有冲突)能正确合并业务开发"
---

<objective>
F-17 收尾:同步前端类型/Swagger 文档,确保新加的 ConfigKey 字段在
全栈可见;归档 phase 31 SUMMARY,验证 28 RED baseline 不变。
</objective>

<context>
@.planning/phases/31-p0-lifecycle-config-api-20260612-f-14-connection-pool-lifecy/31-CONTEXT.md
@.planning/phases/31-p0-lifecycle-config-api-20260612-f-14-connection-pool-lifecy/31-04-PLAN.md
</context>

<plan>

## 1. 前端 TypeScript 类型审查

```bash
grep -rn "ConfigUpdateRequest\|configKey" xingran-react-frontend/src/ --include="*.ts" --include="*.tsx" | head -10
```

若发现前端定义了 ConfigUpdateRequest interface,添加 `configKey?: string` 字段。
若无显式接口(直接传 object),无需修改 — 前端不传 configKey 时后端会跳过校验。

## 2. Swagger 文档更新

```bash
# 重新生成 swagger
./scripts/generate_swagger.bat  # Windows
# 或
./scripts/generate_swagger.sh   # Linux
```

确认 `docs/swagger.json` 中 `ConfigUpdateRequest` schema 包含 `configKey` 字段。

## 3. 最终全量回归

```bash
go build ./... 2>&1 | tail -3
go vet ./... 2>&1 | tail -3
go test -count=1 -short -timeout 240s \
  ./internal/services/... ./internal/api/... ./pkg/... \
  ./internal/scheduler/... ./internal/core/... ./internal/collectors/... \
  ./internal/device/... ./internal/websocket/... 2>&1 | grep -cE "^--- FAIL"
```

预期: **28** (与 baseline 完全一致)。

## 4. 归档 SUMMARY.md

写入 `.planning/phases/31-.../31-SUMMARY.md`:
- Phase 31 范围: F-14 + F-17
- 5 个 plan 完成情况
- commit 列表
- 回归数据
- 已知遗留(F-09/F-10 仍部分修复、F-16 部分修复)
- 后续建议(立项 v1.14 处理 P1 大重构 / P2 架构重构)

## 5. 准备 git stash pop

提示用户在 phase 31 全部合并后执行 `git stash pop` 恢复业务开发,
冲突预期在 addomain/、device/、operations/ 同包文件。

</plan>

<verification>
- [ ] 前端 TypeScript 类型已审查(必要时添加 configKey 字段)
- [ ] Swagger 文档重新生成
- [ ] 全量回归 28 RED 不变
- [ ] 31-SUMMARY.md 完成
- [ ] git stash 仍存在(用户决定何时 pop)
</verification>

<commit>
docs(phase-31): finalize F-17 frontend type sync + phase SUMMARY

完成 phase 31 收尾:
- 前端 TypeScript 类型同步 ConfigKey 字段(若有 explicit interface)
- 重新生成 Swagger 文档反映 API 变更
- 31-SUMMARY.md 归档 phase 进度与回归数据
- 验证 28 RED baseline 五轮 audit + 1 个 phase 累计不变

下一步建议:
- /gsd-verify-work phase 31 进行 conversational UAT
- git stash pop 恢复业务开发(addomain/device/operations 同包可能冲突)
- 立项 v1.14 处理 P1 大重构 / P2 架构重构

Refs: .planning/phases/31-.../31-CONTEXT.md D-02
      .planning/reviews/20260612-backend-code-review.md F-17
</commit>
