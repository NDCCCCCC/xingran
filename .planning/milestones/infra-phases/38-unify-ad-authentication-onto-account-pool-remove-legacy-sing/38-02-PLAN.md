---
phase: 38-unify-ad-authentication-onto-account-pool-remove-legacy-sing
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - internal/services/addomain/sync.go
  - internal/services/addomain/user.go
  - internal/services/addomain/group.go
  - internal/services/addomain/group_sync_service.go
  - internal/services/addomain/group_management_service.go
  - internal/services/addomain/dept_sync_service.go
  - internal/services/addomain/user_ad_sync_service.go
  - internal/scheduler/ad_sync_tasks.go
  - internal/scheduler/dept_sync_tasks.go
  - internal/services/addomain/config.go
autonomous: true
requirements: [D-01, D-03]
user_setup: []

must_haves:
  truths:
    - "所有生产代码 NewLDAPClient(config) 单参数调用 = 0（仅 failover_client.go 内部例外）"
    - "所有原 NewLDAPClient(config) 直连的 caller 改为 fc.ExecuteWithFailover(ctx, operation) 或 fc.PickFirstConnect(ctx) 模式"
    - "config.go TestConnection 改用 PickFirstConnect（SP-4，只建连接不操作）"
    - "SyncManagersToAD 的 updateUserAttributeFn 测试钩子保留且优先级高于 FailoverClient（SHA-5）"
    - "批量同步的 operation 闭包边界 = 整个批量任务（SP-3，非每用户一个 operation）"
    - "运行时空池错误（ErrAllAccountsUnavailable）返回引导文本引导用户到 AD 配置页服务账号池 Tab（SHA-3）"
    - "scheduler/dept_sync_tasks.go 复用 globalADSyncScheduler 的全局 pool 单例（Pitfall 4 缓存共享），禁止在 task 内临时 NewAccountPool"
    - "go build ./... 通过；TestSyncManagersToAD 7 个场景 / TestAccountPoolPasswordRoundTrip / TestFailoverClient 保持绿"
    - "D-01 Wave 1：所有生产 AD 连接统一经账号池 FailoverClient（ExecuteWithFailover / PickFirstConnect），消除单管理员单点（data 775 锁定即全断），NewLDAPClient(config) 单参数直连 = 0"
  artifacts:
    - path: "internal/services/addomain/sync.go"
      provides: "syncDataInternal 改用 ExecuteWithFailover 包裹整个同步流程"
      contains: "fc := NewFailoverClient(s.pool, config)"
    - path: "internal/services/addomain/user_ad_sync_service.go"
      provides: "SyncManagersToAD 改用 FailoverClient 但保留测试钩子分支"
      contains: "s.updateUserAttributeFn != nil"
    - path: "internal/services/addomain/config.go"
      provides: "TestConnection 改用 PickFirstConnect"
      contains: "fc.PickFirstConnect(ctx)"
    - path: "internal/scheduler/ad_sync_tasks.go"
      provides: "globalADSyncScheduler 暴露全局 pool 单例字段供 dept_sync_tasks 复用"
      contains: "pool addomain.AccountPool"
  key_links:
    - from: "internal/services/addomain/sync.go::syncDataInternal"
      to: "internal/services/addomain/failover_client.go::ExecuteWithFailover"
      via: "FailoverClient 闭包"
      pattern: "ExecuteWithFailover"
    - from: "internal/services/addomain/config.go::TestConnection"
      to: "internal/services/addomain/failover_client.go::PickFirstConnect"
      via: "PickFirstConnect"
      pattern: "PickFirstConnect"
    - from: "internal/scheduler/dept_sync_tasks.go"
      to: "internal/scheduler/ad_sync_tasks.go::globalADSyncScheduler.pool"
      via: "复用全局 pool 单例（Pitfall 4 缓存共享）"
      pattern: "globalADSyncScheduler"
---

<objective>
Wave 1 主体：将 21 处 NewLDAPClient(config) 单管理员直连改为 FailoverClient.ExecuteWithFailover / PickFirstConnect 多账号故障切换（D-01 Wave 1）。按服务文件分 3 个 Task 推进，每个 Task 独立 go build + go test，可分段提交降低风险。**本 plan 仍保留**所有 decryptPassword(config.AdminPassword) 调用（暂不删，留待 38-03 Wave 2 删除——decryptPassword 是无副作用函数，保留不影响 FailoverClient 改造的正确性，因为 FailoverClient 内部用的是 account.PasswordCiphertext 而非 config.AdminPassword）。

Purpose: 实现 D-01 Wave 1 目标——所有 AD 连接获取统一经账号池，消除单管理员单点（data 775 锁定即全断）。本 plan 是 phase 核心收益落地：单账号被锁不再阻断所有用户登录/同步。
Output: 10 个文件改造完成（含 scheduler/ad_sync_tasks.go 暴露 pool 字段供 dept_sync_tasks 复用），grep 断言 NewLDAPClient(config) 生产调用 = 0。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-CONTEXT.md
@.planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-RESEARCH.md
@.planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-PATTERNS.md
@.planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-01-SUMMARY.md

# Phase 36 设计来源
@.planning/phases/36-ad-account-pool-failover/PLAN.md

<interfaces>
<!-- FailoverClient API 实测签名（38-01 SUMMARY 应确认 s.pool 已就位） -->

From internal/services/addomain/failover_client.go:33-82:
```go
// ExecuteWithFailover 遍历账号池，对每个账号建连+执行 operation+MarkSuccess/Failure
// 关键：闭包返回后 client.Close() 立即调用，client 使用必须在闭包内（Pitfall 3）
func (f *FailoverClient) ExecuteWithFailover(
    ctx context.Context,
    operation func(client *LDAPClient) error,
) error

// 空池返回 ErrAllAccountsUnavailable（D-03 引导文本源）
var ErrAllAccountsUnavailable = errors.New("...")

// NewFailoverClient 创建 FailoverClient（pool 来自 struct 字段 s.pool）
func NewFailoverClient(pool AccountPool, config *models.ADConfig) *FailoverClient
```

From internal/services/addomain/failover_client.go:88-119 (SP-4 仅 TestConnection 用):
```go
// PickFirstConnect 只建连接+bind 成功即返回，不调 operation
// 唯一允许闭包外用 client 的场景（caller 负责 defer client.Close()）
func (f *FailoverClient) PickFirstConnect(ctx context.Context) (*LDAPClient, *models.ADServiceAccount, error)
```

From internal/core/security/ad_authenticator.go:269-278 (登录认证已就位参考样板):
```go
fc := addomain.NewFailoverClient(a.accountPool, config)
client, _, err := fc.PickFirstConnect(ctx)
if err != nil { return /* ... */ }
defer client.Close()
// ... 使用 client 做用户搜索
```

From internal/services/addomain/account_pool.go:35 (哨兵错误):
```go
var ErrAllAccountsUnavailable = errors.New("所有AD服务账号不可用（账号池为空或全部被熔断）")
```

From internal/scheduler/ad_sync_tasks.go:20-37 (W-04 改造点 — globalADSyncScheduler struct):
```go
// 改造前：
type ADSyncScheduler struct {
    cron    *cron.Cron
    started bool
    sem     *semaphore.Weighted
    db      *gorm.DB
    mu      sync.Mutex
    ctx     context.Context
    cancel  context.CancelFunc
}
// 改造后（W-04）：增加 pool 字段供 dept_sync_tasks 复用全局单例
type ADSyncScheduler struct {
    cron    *cron.Cron
    started bool
    sem     *semaphore.Weighted
    db      *gorm.DB
    pool    addomain.AccountPool   // ← 新增（W-04）：复用 line 105 创建的 pool，避免 dept_sync_tasks 临时 New
    mu      sync.Mutex
    ctx     context.Context
    cancel  context.CancelFunc
}
// globalADSyncScheduler.pool 在 StartADSyncScheduler 中赋值（与 line 105 现有 NewAccountPool 调用合并）
```

LDAPClient 关键方法（闭包内调用，executor 改造时保留方法名不变）:
- client.EnableUser(userDN string) error
- client.DisableUser(userDN string) error
- client.MoveUserToOU(userDN, ouDN string) error
- client.UpdateUserAttribute(userDN string, attrs map[string]string) error
- client.AddGroupMember(groupDN, userDN string) error
- client.RemoveGroupMember(groupDN, userDN string) error
- client.SearchOUs / SearchGroups / SearchUsers / SearchComputers (baseDN string) ([]*ldap.Entry, error)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 单次操作型服务改造 — user.go(4处) + group.go(3处) + group_management_service.go(4处)</name>
  <files>
    internal/services/addomain/user.go,
    internal/services/addomain/group.go,
    internal/services/addomain/group_management_service.go
  </files>
  <read_first>
    - internal/services/addomain/user.go（line 124 Enable / 193 Disable / 214 Move / 235 Update 四处 NewLDAPClient(config)）
    - internal/services/addomain/group.go（line 125 AddMember / 155 RemoveMember / 187 CreateGroup 三处）
    - internal/services/addomain/group_management_service.go（line 86 / 154 / 202 / 260 四处）
    - internal/services/addomain/failover_client.go:33-82（ExecuteWithFailover 完整契约）
    - internal/core/security/ad_authenticator.go:256-278（PickFirstConnect 样板 — 本任务用 ExecuteWithFailover 不是 PickFirstConnect，但闭包模式一致）
    - .planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-PATTERNS.md（SP-1 单次操作型改造前后对照样例 + Pitfall 3 闭包外访问 client 反模式）
    - .planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-01-SUMMARY.md（确认 s.pool 已就位）
  </read_first>
  <behavior>
    - 11 处 NewLDAPClient(config) + Connect + defer Close + client.XxxUser 改造为 fc.ExecuteWithFailover(ctx, func(client *LDAPClient) error { return client.XxxUser(...) })
    - DB 操作（如 Update("is_enabled", true)）保留在闭包外（DB 不依赖 LDAP 连接）
    - 所有 client.* LDAP 调用必须在闭包内（Pitfall 3）
    - 空池错误 ErrAllAccountsUnavailable 包装为引导文本（SHA-3）
    - decryptPassword(config.AdminPassword) 调用**暂保留**（留待 38-03 删除，保留不影响 FailoverClient 正确性）
    - go build ./... 通过；go test ./internal/services/addomain/ 全绿
  </behavior>
  <action>
    按 SP-1（PATTERNS.md user.go:Enable 改造前后对照）改造 11 处单次操作型 caller。统一模式：删除 `client := NewLDAPClient(config); if err := client.Connect(); err != nil { return err }; defer client.Close()`，替换为 `fc := NewFailoverClient(s.pool, config); if err := fc.ExecuteWithFailover(ctx, func(client *LDAPClient) error { return client.XxxUser(userDN) }); err != nil { return err }`。后续 DB 操作（如 user.go 的 `s.db.WithContext(ctx).Model(&models.ADUser{}).Where(...).Update(...)`）保持原位（闭包外）。错误包装：在 FailoverClient 调用返回后用 `errors.Is(err, ErrAllAccountsUnavailable)` 分支返回引导文本"AD 账号池无可用账号，请先在 AD 配置页（详情 → 服务账号池 Tab）添加服务账号"。**保留** `config.AdminPassword = decryptPassword(config.AdminPassword)` 行（38-03 统一删）。**禁止**在闭包外声明 client 变量并在闭包内赋值（Pitfall 3）。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go build ./... && go test ./internal/services/addomain/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "NewLDAPClient(config)\|NewLDAPClient(&config)" internal/services/addomain/user.go internal/services/addomain/group.go internal/services/addomain/group_management_service.go` = 0（3 个文件合计）
    - `grep -c "ExecuteWithFailover" internal/services/addomain/user.go` ≥ 4
    - `grep -c "ExecuteWithFailover" internal/services/addomain/group.go` ≥ 3
    - `grep -c "ExecuteWithFailover" internal/services/addomain/group_management_service.go` ≥ 4
    - `grep -c "ErrAllAccountsUnavailable" internal/services/addomain/user.go internal/services/addomain/group.go internal/services/addomain/group_management_service.go` 合计 ≥ 3（至少有引导文本包装，可集中在 helper 或每处都包装）
    - `go build ./...` exits 0
    - `go test ./internal/services/addomain/ -count=1` 全绿
  </acceptance_criteria>
  <done>user.go / group.go / group_management_service_service.go 共 11 处单次操作型 caller 改走 FailoverClient.ExecuteWithFailover，go build + go test 全绿。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 批量同步型服务改造 — ad_sync_tasks.go(暴露全局 pool) + sync.go + group_sync_service.go + dept_sync_service.go + user_ad_sync_service.go + scheduler/dept_sync_tasks.go(复用全局 pool)</name>
  <files>
    internal/scheduler/ad_sync_tasks.go,
    internal/services/addomain/sync.go,
    internal/services/addomain/group_sync_service.go,
    internal/services/addomain/dept_sync_service.go,
    internal/services/addomain/user_ad_sync_service.go,
    internal/scheduler/dept_sync_tasks.go
  </files>
  <read_first>
    - internal/scheduler/ad_sync_tasks.go:20-68（ADSyncScheduler struct 定义 + NewADSyncScheduler 构造函数 + globalADSyncScheduler 单例；line 105 `pool := addomain.NewAccountPool(globalADSyncScheduler.db, nil)` — W-04 改造点）
    - internal/scheduler/ad_sync_tasks.go:100-135（StartADSyncScheduler 单例初始化 + line 105 pool 创建 — 改造合并点）
    - internal/services/addomain/sync.go（line 90-98 syncDataInternal 单连接多次 Search — 整个同步流程一个 operation）
    - internal/services/addomain/group_sync_service.go（line 54-55, 92-93 两处）
    - internal/services/addomain/dept_sync_service.go（line 45-46 一处）
    - internal/services/addomain/user_ad_sync_service.go（line 59 SyncUserUpdateToAD / 204 BatchMoveUsersToNewOU / 377 一处 decryptPassword / 471-482 SyncManagersToAD 测试钩子样板 — **SHA-5 必读**）
    - internal/services/addomain/manager_sync_test.go（line 130-135 测试钩子使用方式 — 7 个 TestSyncManagersToAD_* 场景依赖）
    - internal/scheduler/dept_sync_tasks.go（line 88-89, 160-163 两处 — addomain.DecryptPassword 大写包名前缀；scheduler 外部包须用全限定 addomain.NewFailoverClient）
    - .planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-PATTERNS.md（SP-3 批量同步闭包边界 + 改造前后完整样例；Pitfall 2 测试钩子误删；Pitfall 4 缓存不共享；Pitfall 5 errgroup 超时；SHA-5 钩子优先级）
  </read_first>
  <behavior>
    - **ad_sync_tasks.go (W-04 改造):** ADSyncScheduler struct 增加 `pool addomain.AccountPool` 字段；StartADSyncScheduler 中将 line 105 的 `pool := addomain.NewAccountPool(globalADSyncScheduler.db, nil)` 改为给 globalADSyncScheduler.pool 赋值（合并创建与赋值，避免局部 pool 与字段分离）；line 263/290 NewADDomainService 调用透传 globalADSyncScheduler.pool
    - sync.go: 整个 syncDataInternal 同步流程（Search OUs/Groups/Users/Computers + DB 写入）封装进一个 operation；DB 写入不依赖 LDAP 连接的可放闭包外，但 Search 结果 []*ldap.Entry 必须 collect 后传出
    - group_sync_service.go / dept_sync_service.go: 同 sync.go 模式
    - user_ad_sync_service.go SyncManagersToAD: updateUserAttributeFn != nil 时走钩子（绕过 FailoverClient）；否则用 FailoverClient 包裹整个 errgroup 批量，g.Wait() 必须在闭包内（Pitfall 3+5）
    - user_ad_sync_service.go SyncUserUpdateToAD / BatchMoveUsersToNewOU: 改 ExecuteWithFailover
    - scheduler/dept_sync_tasks.go: 改用 addomain.NewFailoverClient(globalADSyncScheduler.pool, &adConfig)（pool 从 globalADSyncScheduler 单例读取，**禁止**在 task 内临时 addomain.NewAccountPool(db, nil)）
    - go build ./... 通过；7 个 TestSyncManagersToAD_* 全绿（钩子保留）；TestAccountPoolPasswordRoundTrip 全绿
  </behavior>
  <action>
    **W-04 第一步（ad_sync_tasks.go 改造）:** 在 ADSyncScheduler struct（line 20-28）增加 `pool addomain.AccountPool` 字段（紧跟 db 字段之后）。在 StartADSyncScheduler（约 line 120-122 的 globalADSyncSchedulerOnce.Do 闭包内或紧邻 line 105 的 pool 创建处）将现有的 `pool := addomain.NewAccountPool(globalADSyncScheduler.db, nil)` 改为 `globalADSyncScheduler.pool = addomain.NewAccountPool(globalADSyncScheduler.db, nil)`（直接赋值给字段，避免局部 pool 与字段重复或分离）。同时确认 line 263/290 的 NewADDomainService 调用透传 `globalADSyncScheduler.pool`（替换现有 `s.db` + cipher 两参数为 `s.db, globalADSyncScheduler.pool, cipher`）。

    **sync.go 按 SP-3 sync.go 改造后样例:** 将 line 90-98 的 `client := NewLDAPClient(config); client.Connect(); defer client.Close()` + 后续 client.Search* 调用整体包入 `fc := NewFailoverClient(s.pool, config); err := fc.ExecuteWithFailover(ctx, func(client *LDAPClient) error { /* 所有 client.Search* 与 result 累加在此 */ return nil }); if err != nil { s.updateSyncLog(...Failed...); return nil, fmt.Errorf("连接AD服务器失败: %w", err) }`。DB 写入（s.syncOUs / s.syncGroups 等）若依赖 Search 结果需在闭包内 collect 后传出。group_sync_service.go / dept_sync_service.go 同模式。

    **user_ad_sync_service.go SyncManagersToAD 按 SP-3 SyncManagersToAD 样例:** 保留 `if s.updateUserAttributeFn != nil { updateAttr = s.updateUserAttributeFn }` 分支（**SHA-5 不动**），else 分支改为 `fc := NewFailoverClient(s.pool, &adConfig); err := fc.ExecuteWithFailover(ctx, func(client *LDAPClient) error { updateAttr = client.UpdateUserAttribute; g, gctx := errgroup.WithContext(ctx); g.SetLimit(constants.MaxConcurrentADSync); /* 现有遍历逻辑 */ _ = g.Wait(); return nil }); if err != nil { return nil, err }`。

    **scheduler/dept_sync_tasks.go (W-04 强约束):** 改用 `fc := addomain.NewFailoverClient(globalADSyncScheduler.pool, &adConfig)` 全限定名（scheduler 是外部包），**pool 必须从 globalADSyncScheduler.pool 字段读取**（与 Pitfall 4 一致，避免多实例缓存不共享）。若 globalADSyncScheduler 为 nil（task 在调度器启动前被调用的极端情况）应作为前置 init 检查抛错而非临时 New。**绝对禁止**在 dept_sync_tasks.go 内调用 `addomain.NewAccountPool(db, nil)` 创建临时 pool（acceptance_criteria grep count=0 强约束）。decryptPassword 行**保留**（38-03 删）。**绝对禁止**删除 updateUserAttributeFn 钩子字段或其 != nil 分支（SHA-5）。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go build ./... && go test ./internal/services/addomain/ ./internal/scheduler/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pool\s*addomain.AccountPool" internal/scheduler/ad_sync_tasks.go` ≥ 1（ADSyncScheduler struct 新增 pool 字段 — W-04）
    - `grep -c "globalADSyncScheduler\.pool\s*=" internal/scheduler/ad_sync_tasks.go` ≥ 1（StartADSyncScheduler 中赋值 — W-04）
    - `grep -c "NewAccountPool" internal/scheduler/dept_sync_tasks.go` = 0（**W-04 硬约束**：禁止在 dept_sync_tasks 内临时 New，必须复用 globalADSyncScheduler.pool）
    - `grep -c "globalADSyncScheduler\.pool" internal/scheduler/dept_sync_tasks.go` ≥ 1（dept_sync_tasks 通过全局 pool 字段读取，全限定 addomain.NewFailoverClient 调用）
    - `grep -rn "NewLDAPClient(config)\|NewLDAPClient(&config)\|NewLDAPClient(&adConfig)" internal/ --include="*.go" | grep -v "_test.go" | grep -v "failover_client.go"` 输出仅剩 config.go:208 一行（Task 3 处理）
    - `grep -c "ExecuteWithFailover" internal/services/addomain/sync.go` ≥ 1
    - `grep -c "ExecuteWithFailover" internal/services/addomain/group_sync_service.go` ≥ 2
    - `grep -c "ExecuteWithFailover" internal/services/addomain/user_ad_sync_service.go` ≥ 3（SyncManagersToAD / SyncUserUpdateToAD / BatchMoveUsersToNewOU）
    - `grep -c "s.updateUserAttributeFn != nil" internal/services/addomain/user_ad_sync_service.go` ≥ 1（钩子分支保留）
    - `grep -c "updateUserAttributeFn" internal/services/addomain/user_ad_sync_service.go` 与改造前一致（字段+分支+使用点不减少）
    - `go test ./internal/services/addomain/ -run TestSyncManagersToAD -v` 全部 7 个 PASS
    - `go test ./internal/services/addomain/ -run TestAccountPoolPasswordRoundTrip -v` PASS
    - `go test ./internal/services/addomain/ -run TestFailoverClient -v` PASS
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>9 个批量同步型 caller + ad_sync_tasks.go 暴露全局 pool 字段改造完成，dept_sync_tasks 复用全局 pool（无临时 New），测试钩子保留，TestSyncManagersToAD 7 个场景全绿，仅剩 config.go:208 未改造（Task 3）。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 配置测试连接改造 — config.go TestConnection 用 PickFirstConnect（SP-4）</name>
  <files>
    internal/services/addomain/config.go
  </files>
  <read_first>
    - internal/services/addomain/config.go（line 200-217 TestConnection 函数）
    - internal/services/addomain/failover_client.go:88-119（PickFirstConnect 完整契约）
    - internal/core/security/ad_authenticator.go:269-278（PickFirstConnect 现成模板）
    - .planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-PATTERNS.md（SP-4 配置测试连接改造前后样例）
  </read_first>
  <behavior>
    - TestConnection 改用 fc.PickFirstConnect(ctx) 只建连接+bind 成功即返回
    - defer client.Close() 由 caller 负责（PickFirstConnect 唯一允许闭包外用 client 的场景）
    - 错误包装为"AD 连接测试失败（账号池无可用账号或全部 bind 失败）"
    - decryptPassword(config.AdminPassword) 行保留（38-03 删）
    - go build ./... 通过
    - 全量 grep 断言 NewLDAPClient(config) 生产调用 = 0
  </behavior>
  <action>
    按 SP-4 改造后样例修改 config.go:200-217 TestConnection：删除 `config.AdminPassword = decryptPassword(config.AdminPassword)` **暂保留**（38-03 统一删，本任务可保留——PickFirstConnect 不依赖 config.AdminPassword）；删除 `client := NewLDAPClient(config); if err := client.Connect(); err != nil { return err }; defer client.Close(); return nil`，替换为 `fc := NewFailoverClient(s.pool, config); client, _, err := fc.PickFirstConnect(ctx); if err != nil { return fmt.Errorf("AD 连接测试失败（账号池无可用账号或全部 bind 失败）: %w", err) }; defer client.Close(); applogger.Infof("[ADTest] LDAP连接测试成功"); return nil`。参考 ad_authenticator.go:269-278 模板（唯一允许闭包外用 client 的场景，因为 PickFirstConnect 不调用 operation，由 caller 控制 client 生命周期）。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go build ./... && go test ./internal/services/addomain/ -count=1 && bash -c "COUNT=$(grep -rn 'NewLDAPClient(config)\|NewLDAPClient(&config)\|NewLDAPClient(&adConfig)' internal/ --include='*.go' | grep -v '_test.go' | grep -v 'failover_client.go' | wc -l); echo \"residual NewLDAPClient(config) sites: \$COUNT\"; test \"\$COUNT\" = 0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PickFirstConnect" internal/services/addomain/config.go` ≥ 1
    - `grep -c "NewLDAPClient(config)" internal/services/addomain/config.go` = 0
    - `grep -rn "NewLDAPClient(config)\|NewLDAPClient(&config)\|NewLDAPClient(&adConfig)" internal/ --include="*.go" | grep -v "_test.go" | grep -v "failover_client.go" | wc -l` = 0（**Wave 1 完成硬指标**）
    - `go build ./...` exits 0
    - `go test ./internal/services/addomain/ -count=1` 全绿
  </acceptance_criteria>
  <done>config.go TestConnection 改走 PickFirstConnect，全量 grep 断言 NewLDAPClient(config) 生产调用 = 0，D-01 Wave 1 完成。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service→LDAP | FailoverClient 替代直连，凭据来自账号池（已 Bind 成功的 client 传入闭包） |
| 闭包生命周期 | operation 闭包内 client 有效，闭包返回后 client.Close() 已调用（Pitfall 3） |
| scheduler 进程内单例 | globalADSyncScheduler.pool 在 StartADSyncScheduler 中创建一次，所有 task 复用（Pitfall 4） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03-Closure | Tampering | 闭包外访问 client panic | mitigate | acceptance_criteria 锁闭包内 client 使用；改造前后对照 SP-1/SP-3 强制 DB 操作放闭包外、LDAP 操作放闭包内；Pitfall 3 守护 |
| T-38-04-TestHook | Tampering | updateUserAttributeFn 钩子误删 | mitigate | SHA-5 acceptance_criteria 锁 `s.updateUserAttributeFn != nil` 分支存在；7 个 TestSyncManagersToAD_* 测试守护 |
| T-38-05-EmptyPool | Denial of Service | 空池时 failover 全失败致服务不可用 | mitigate | SHA-3 引导文本包装 ErrAllAccountsUnavailable，引导用户配置；D-03 不静默 fallback |
| T-38-06-Cascade | Denial of Service | 批量同步 MarkFailure 触发多账号熔断 | accept | SP-3 operation 边界=整个批量，单次批量失败熔断多个账号是可接受语义（CONTEXT deferred 精细化）；FailoverClient maxHops=min(10, len)限制爆炸半径 |
| T-38-07-Errgroup | Denial of Service | SyncManagersToAD errgroup 闭包内阻塞超时 | mitigate | Pitfall 5：errgroup.WithContext(ctx) 继承调用方 context，超时取消整个批量；现有代码已用此模式（user_ad_sync_service.go:485） |
| T-38-08-Fallback | Tampering | 残留单管理员 fallback 路径 | mitigate | D-03 明确不保留 fallback；acceptance_criteria grep NewLDAPClient(config)=0 锁死残留路径 |
| T-38-09-PoolSingleton | Tampering | dept_sync_tasks 临时 NewAccountPool 致 Pitfall 4 缓存不共享 | mitigate | W-04 强约束：globalADSyncScheduler.pool 单例字段 + acceptance_criteria 锁 dept_sync_tasks NewAccountPool count=0，复用全局单例 |
| T-38-SC | Tampering | 无 npm/pip/cargo 安装 | N/A | 纯 Go 重构 |
</threat_model>

<verification>
- `go build ./...` exits 0
- `go test ./internal/services/addomain/ ./internal/scheduler/ -count=1` 全绿
- **D-01 Wave 1 硬指标**: `grep -rn "NewLDAPClient(config)\|NewLDAPClient(&config)\|NewLDAPClient(&adConfig)" internal/ --include="*.go" | grep -v "_test.go" | grep -v "failover_client.go" | wc -l` = 0
- **W-04 硬指标**: `grep -c "NewAccountPool" internal/scheduler/dept_sync_tasks.go` = 0（复用全局 pool 单例）
- TestSyncManagersToAD 7 个场景 + TestAccountPoolPasswordRoundTrip + TestFailoverClient 全部 PASS
</verification>

<success_criteria>
- 21 处 NewLDAPClient(config) 生产调用全部改造为 FailoverClient.ExecuteWithFailover / PickFirstConnect
- 全量 grep 断言 NewLDAPClient(config) = 0（除 failover_client.go 内部）
- SyncManagersToAD 测试钩子保留，7 个回归测试全绿
- 批量同步 operation 闭包边界 = 整个任务（非每用户）
- 空池错误返回引导文本（SHA-3）
- globalADSyncScheduler 暴露 pool 单例字段，dept_sync_tasks 复用（W-04 / Pitfall 4 缓解）
- decryptPassword(config.AdminPassword) 保留待 38-03 删除（22 处数量不变）
- go build ./... + addomain/scheduler 全量测试通过
</success_criteria>

<output>
Create `.planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-02-SUMMARY.md` when done
</output>
