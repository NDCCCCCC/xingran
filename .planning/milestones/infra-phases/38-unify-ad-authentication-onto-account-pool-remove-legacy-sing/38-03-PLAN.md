---
phase: 38-unify-ad-authentication-onto-account-pool-remove-legacy-sing
plan: 03
type: execute
wave: 3
depends_on: [38-02]
files_modified:
  - internal/services/addomain/sync.go
  - internal/services/addomain/user.go
  - internal/services/addomain/group.go
  - internal/services/addomain/group_sync_service.go
  - internal/services/addomain/group_management_service.go
  - internal/services/addomain/dept_sync_service.go
  - internal/services/addomain/user_ad_sync_service.go
  - internal/services/addomain/config.go
  - internal/core/security/ad_authenticator.go
  - internal/scheduler/dept_sync_tasks.go
  - internal/services/addomain/service.go
  - internal/api/v1/system/ad_domain_handler.go
  - xingran-react-frontend/src/lib/adDomainApi.ts
  - xingran-react-frontend/src/pages/ad-domain/configs/index.tsx
autonomous: true
requirements: [D-01, D-04]
user_setup: []

must_haves:
  truths:
    - "所有生产代码 decryptPassword(config.AdminPassword) / decryptPassword(adConfig.AdminPassword) / DecryptPassword(adConfig.AdminPassword) 调用 = 0"
    - "前端 adDomainApi.ts 中 ADConfig / ADConfigCreateRequest / ADConfigUpdateRequest 三个接口均不含 adminUsername / adminPassword 字段（零残留，不含注释）"
    - "前端 configs/index.tsx handleEdit 不再 setFieldsValue adminUsername/adminPassword: undefined，handleSubmit 不再透传 adminPassword（零残留）"
    - "后端 ADConfigCreateRequest / ADConfigUpdateRequest 不再 binding:\"required\" adminUsername/adminPassword（前后端配套同 commit）"
    - "ad_authenticator.go 的 bindAdmin @Deprecated 死代码删除；config.AdminPassword = a.decryptPassword(...) 行删除"
    - "go build ./... 通过；前端 npm run type-check + npm run build 通过"
    - "D-04：AD 配置表单移除 admin 账号输入项，账号管理统一收敛到 AccountPoolTab.tsx；前端 adminUsername/adminPassword 字段零残留（含注释），后端 binding:\"required\" 配套删除（SHA-4 同 commit）"
  artifacts:
    - path: "xingran-react-frontend/src/lib/adDomainApi.ts"
      provides: "ADConfig/Create/Update 接口移除 admin 字段"
      contains: "interface ADConfig"
    - path: "internal/core/security/ad_authenticator.go"
      provides: "bindAdmin 死代码删除"
  key_links:
    - from: "xingran-react-frontend/src/pages/ad-domain/configs/index.tsx::handleSubmit"
      to: "xingran-react-frontend/src/lib/adDomainApi.ts::updateADConfig"
      via: "字段类型一致性"
      pattern: "adminPassword"
---

<objective>
Wave 2: 删除单管理员密码的全部 Go 使用逻辑（22 处 decryptPassword 调用）+ 前端 admin 字段清理（D-01 Wave 2 + D-04）。**关键 SHA-4 约束**：前端字段删除与后端 binding:"required" 删除必须同一 commit，否则前端不传 adminUsername → 后端 400。AD 用户登录认证层（ad_authenticator.go）已有 FailoverClient 主路径，本 plan 删除其遗留 bindAdmin 死代码与 decryptPassword 调用。

Purpose: 完成 D-02 "仅删代码保留空列"——所有 Go 使用 admin 字段的代码消失，但 DB 列保留（D-02 不做 DROP COLUMN）。消除 CONTEXT debug session adpool-password-not-decrypted 揭示的双轨不对称根因。
Output: 14 个文件改造（10 Go + 4 前端 TS/TSX），grep 断言 decryptPassword(config.AdminPassword) = 0 + 前端 admin 字段 = 0。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-CONTEXT.md
@.planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-RESEARCH.md
@.planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-PATTERNS.md
@.planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-02-SUMMARY.md

<interfaces>
<!-- Wave 2 删除点清单（38-02 SUMMARY 应已确认所有 NewLDAPClient(config) = 0） -->

decryptPassword(config.AdminPassword) 调用点（22 处，需全删）:
- internal/services/addomain/sync.go:90
- internal/services/addomain/user.go:124, 193, 214, 235 (4 处)
- internal/services/addomain/group.go:123, 153, 185 (3 处)
- internal/services/addomain/group_sync_service.go:54, 92 (2 处)
- internal/services/addomain/group_management_service.go:85, 153, 201, 259 (4 处)
- internal/services/addomain/dept_sync_service.go:45
- internal/services/addomain/user_ad_sync_service.go:59, 204, 377 (3 处)
- internal/services/addomain/config.go:205 (TestConnection 保留的 decryptPassword)
- internal/core/security/ad_authenticator.go:99 (a.decryptPassword)
- internal/scheduler/dept_sync_tasks.go:88, 160 (addomain.DecryptPassword 大写包名前缀)

From internal/services/addomain/config.go:79-92 (CreateRequest/UpdateRequest binding):
```go
type ADConfigCreateRequest struct {
    // ...
    AdminUsername string `json:"adminUsername" binding:"required,max=255"`   // ← 删 required
    AdminPassword string `json:"adminPassword" binding:"required,max=500"`   // ← 删 required
    // ...
}
```

From xingran-react-frontend/src/lib/adDomainApi.ts:13-14, 161-162, 175-176:
```typescript
export interface ADConfig { adminUsername: string; adminPassword?: string; /* ... */ }
export interface ADConfigCreateRequest { adminUsername: string; adminPassword: string; /* ... */ }
export interface ADConfigUpdateRequest { adminUsername: string; adminPassword?: string; /* ... */ }
```

From internal/core/security/ad_authenticator.go:238-247 (@Deprecated bindAdmin 死代码):
```go
// bindAdmin (已 @Deprecated，被 bindAdminWithFailover 替代)
// Wave 2 后无 caller，整段删除
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Go 删除 22 处 decryptPassword(config.AdminPassword) + ad_authenticator bindAdmin 死代码</name>
  <files>
    internal/services/addomain/sync.go,
    internal/services/addomain/user.go,
    internal/services/addomain/group.go,
    internal/services/addomain/group_sync_service.go,
    internal/services/addomain/group_management_service.go,
    internal/services/addomain/dept_sync_service.go,
    internal/services/addomain/user_ad_sync_service.go,
    internal/services/addomain/config.go,
    internal/core/security/ad_authenticator.go,
    internal/scheduler/dept_sync_tasks.go
  </files>
  <read_first>
    - 上述 10 个文件的 decryptPassword 调用点（行号见 interfaces 清单）
    - internal/core/security/ad_authenticator.go:99, 238-247, 256-278（bindAdmin 死代码 + bindAdminWithFailover Phase 36 双读 fallback 分支 + 主路径）
    - internal/services/addomain/ldap_client.go:134-167（tryBindAttempts 现 c.account != nil 判断 — Wave 2 后所有 caller 走 FailoverClient c.account 必非 nil；本任务可保留防御性判断或简化，planner discretion）
    - .planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-PATTERNS.md（SP-5 Wave 2 删除模式 + SHA-3 ErrAllAccountsUnavailable 引导文本）
    - .planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-02-SUMMARY.md（确认 FailoverClient 改造已完成，decryptPassword 已是死调用）
  </read_first>
  <behavior>
    - 22 处 `config.AdminPassword = decryptPassword(config.AdminPassword)` 及变体（adConfig./a./addomain.DecryptPassword）全删
    - ad_authenticator.go:99 `config.AdminPassword = a.decryptPassword(config.AdminPassword)` 删除
    - ad_authenticator.go:238-247 bindAdmin @Deprecated 方法整段删除
    - ad_authenticator.go bindAdminWithFailover 内 Phase 36 双读 fallback 分支（line 257-267 如有 config.AdminPassword 回退）删除
    - decryptPassword / encryptPassword 函数本身**保留**（账号池路径 tryBindAttempts 内部仍用）
    - go build ./... 通过；全量 grep 断言 decryptPassword(config.AdminPassword) = 0
    - 现有测试（TestDecryptPassword_InvalidCiphertext / TestAccountPoolPasswordRoundTrip）保持绿
  </behavior>
  <action>
    对 10 个文件按 SHA-4 + SP-5 模式删除：删除每处 `config.AdminPassword = decryptPassword(config.AdminPassword)` 行（注意 scheduler 是大写包名 `addomain.DecryptPassword(adConfig.AdminPassword)`，ad_authenticator 是 `a.decryptPassword(config.AdminPassword)`）。ad_authenticator.go 额外清理：line 238-247 bindAdmin 方法整段删除（Wave 1 后无 caller），bindAdminWithFailover 内若有"config.AdminPassword 非空时回退单管理员"分支一并删除（D-03 不保留 fallback）。**保留** decryptPassword / encryptPassword 函数定义（账号池路径仍调用）。**保留** config.go:108, 169 的 encryptPassword(req.AdminPassword)（这是 Create/Update 入库时的加密，留待 Task 2 与 binding 配套删除——避免 Task 1 单独删导致 Create 入库未加密字段不一致）。若 tryBindAttempts 的 `c.account == nil` 分支变死代码可简化为防御性 error 返回（planner discretion，非强制）。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go build ./... && go test ./internal/services/addomain/ ./internal/core/security/ -count=1 && bash -c "COUNT=$(grep -rn 'decryptPassword(config\.AdminPassword)\|decryptPassword(adConfig\.AdminPassword)\|DecryptPassword(adConfig\.AdminPassword)\|a\.decryptPassword(config\.AdminPassword)' internal/ --include='*.go' | grep -v '_test.go' | wc -l); echo \"residual decryptPassword(config.AdminPassword) sites: \$COUNT\"; test \"\$COUNT\" = 0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "decryptPassword(config\.AdminPassword)\|decryptPassword(adConfig\.AdminPassword)\|DecryptPassword(adConfig\.AdminPassword)\|a\.decryptPassword(config\.AdminPassword)" internal/ --include="*.go" | grep -v "_test.go" | wc -l` = 0（**Wave 2 Go 硬指标**）
    - `grep -c "func.*bindAdmin[^W]" internal/core/security/ad_authenticator.go` = 0（bindAdmin 死代码删除；bindAdminWithFailover 保留）
    - `grep -c "func decryptPassword\|func encryptPassword\|func SetADSM4Cipher" internal/services/addomain/utils.go` ≥ 3（函数定义保留，账号池路径仍用）
    - `go test ./internal/services/addomain/ -run TestDecryptPassword_InvalidCiphertext -v` PASS
    - `go test ./internal/services/addomain/ -run TestAccountPoolPasswordRoundTrip -v` PASS
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>22 处 decryptPassword(config.AdminPassword) 调用全删，bindAdmin 死代码删除，decryptPassword 函数定义保留（账号池仍用），go build + 关键测试全绿。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 前后端配套删除 admin 字段（SHA-4 同 commit）— service.go/config.go/handler + adDomainApi.ts/configs/index.tsx</name>
  <files>
    internal/services/addomain/service.go,
    internal/services/addomain/config.go,
    internal/api/v1/system/ad_domain_handler.go,
    xingran-react-frontend/src/lib/adDomainApi.ts,
    xingran-react-frontend/src/pages/ad-domain/configs/index.tsx
  </files>
  <read_first>
    - internal/services/addomain/service.go:23-54（ADConfigCreateRequest line 29-30 / ADConfigUpdateRequest line 46-47 含 binding:"required"）
    - internal/services/addomain/config.go:100-175（Create 108 / Update 169 encryptPassword(req.AdminPassword) — Task 1 保留此处的加密逻辑待本任务配套删）
    - internal/api/v1/system/ad_domain_handler.go（CreateConfig 返回是否含 admin 字段；参考 line 100, 171 req 使用）
    - xingran-react-frontend/src/lib/adDomainApi.ts:13-14, 161-162, 175-176（三个接口 admin 字段位置）
    - xingran-react-frontend/src/pages/ad-domain/configs/index.tsx:92, 106-114, 124-145（handleEdit setFieldsValue + handleSubmit 透传）
    - .planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-PATTERNS.md（SP-6 前端字段删除样例 + SHA-4 配套约束）
    - CLAUDE.md §"Critical Development Rules" / "Frontend API Calling"
  </read_first>
  <behavior>
    - service.go: ADConfigCreateRequest / ADConfigUpdateRequest 删除 AdminUsername / AdminPassword 字段（D-04 倾向字段整删，零残留）
    - config.go: Create/Update 中 encryptPassword(req.AdminPassword) 与 req.AdminUsername 赋值给 model 的代码删除（model 字段保留作 DB 列，仅 request 不再接收）
    - ad_domain_handler.go: CreateConfig 返回清理（不再 echo admin 字段）
    - adDomainApi.ts: 三个接口删除 adminUsername / adminPassword 字段（零残留，无注释保留）
    - configs/index.tsx: handleEdit 简化（不再 setFieldsValue admin: undefined），handleSubmit 简化（不再透传 adminPassword）
    - **迁移说明集中原则（W-03）:** 所有"保留空列"的迁移说明集中在 `internal/models/ad_domain.go` 的 `@Deprecated` 注释（D-02 已说明，38-04 落地），其余文件（service.go / adDomainApi.ts / configs/index.tsx）**零残留**（含注释也不留，避免遗留误导）
    - 前后端**同一 commit** 提交（SHA-4 警告：否则前端不传 → 后端 required 400）
    - go build ./... 通过；前端 npm run type-check + npm run build 通过
  </behavior>
  <action>
    按 SHA-4 + SP-6 改造。后端：service.go 删除 ADConfigCreateRequest 内 AdminUsername/AdminPassword 两行（line 29-30）和 ADConfigUpdateRequest 内对应字段（line 46-47）；config.go Create/Update 函数内 `req.AdminUsername / req.AdminPassword` 给 model 赋值的语句删除，encryptPassword(req.AdminPassword) 调用删除（Task 1 保留的此处一并删）；ad_domain_handler.go CreateConfig handler 若返回 admin 字段则清理。前端：adDomainApi.ts 删除三个接口的 adminUsername/adminPassword 字段（连同注释一并删除，零残留）；configs/index.tsx handleEdit（约 line 92）改为 `editForm.setFieldsValue({ ...config })`（删除 adminUsername: undefined, adminPassword: undefined 及相关注释），handleSubmit（约 line 124-145）改为 `const updateData = { ...values }`（删除 adminPassword: values.adminPassword || undefined）。**W-03 零残留约束:** 迁移说明集中在 models/ad_domain.go 的 @Deprecated 注释（38-04 Task 1 已加），service.go / adDomainApi.ts / configs/index.tsx 不保留任何 admin 字段相关注释。**关键**：后端 service.go 字段删除与前端 adDomainApi.ts 字段删除**必须同一 git commit**（SHA-4），否则前端请求不含 adminUsername → 后端 binding:"required" 拒绝 → 400 错误。提交时 commit message 含 "前后端配套删除 admin 字段 (SHA-4)"。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go build ./... && cd xingran-react-frontend && npm run type-check && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "adminUsername\|adminPassword" internal/services/addomain/service.go` = 0（**W-03 精确 = 0**，含注释也不残留）
    - `grep -c "encryptPassword(req\.AdminPassword)" internal/services/addomain/config.go` = 0
    - `grep -c "adminUsername\|adminPassword" xingran-react-frontend/src/lib/adDomainApi.ts` = 0（**W-03 精确 = 0**，含注释也不残留）
    - `grep -c "adminUsername\|adminPassword" xingran-react-frontend/src/pages/ad-domain/configs/index.tsx` = 0（**W-03 精确 = 0**，含注释也不残留）
    - `go build ./...` exits 0
    - `cd xingran-react-frontend && npm run type-check` exits 0
    - `cd xingran-react-frontend && npm run build` exits 0
    - 验证 commit log：后端 service.go + 前端 adDomainApi.ts 在同一 commit（`git log --oneline -- internal/services/addomain/service.go xingran-react-frontend/src/lib/adDomainApi.ts | head -1` 显示一行）
  </acceptance_criteria>
  <done>前后端 admin 字段配套删除（同 commit），service.go / adDomainApi.ts / configs/index.tsx 零残留（含注释），go build + 前端 type-check + build 全绿，D-04 完成。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端→后端 | CreateRequest/UpdateRequest 移除 admin 字段，binding 校验同步调整（V5 Input Validation） |
| 双轨收敛 | 单管理员路径全部代码消失，仅账号池路径保留（消除不对称风险） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-09-FrontBack | Tampering | 前后端字段不一致 400 | mitigate | SHA-4 强制同 commit；acceptance_criteria 锁 git log 同一 commit；CI build 验证 |
| T-38-10-ResidualFallback | Information Disclosure | 残留单管理员 fallback 路径 | mitigate | D-03 不保留 fallback；acceptance_criteria grep decryptPassword(config.AdminPassword)=0；ad_authenticator bindAdminWithFailover 双读分支删除 |
| T-38-11-CryptoFn | Tampering | decryptPassword 函数误删 | mitigate | acceptance_criteria 锁 utils.go decryptPassword/encryptPassword/SetADSM4Cipher 三个函数定义保留；账号池 tryBindAttempts 内部仍调用 |
| T-38-12-BindRequired | Denial of Service | binding:"required" 残留致 400 | mitigate | SHA-4 配套删除；acceptance_criteria grep `adminUsername\|adminPassword` service.go = 0（精确，W-03 零残留） |
| T-38-SC | Tampering | 无 npm/pip/cargo 安装 | N/A | 纯 Go + TS 重构 |
</threat_model>

<verification>
- `go build ./...` exits 0
- `cd xingran-react-frontend && npm run type-check && npm run build` exits 0
- **Wave 2 Go 硬指标**: `grep -rn "decryptPassword(config\.AdminPassword)\|decryptPassword(adConfig\.AdminPassword)\|DecryptPassword(adConfig\.AdminPassword)\|a\.decryptPassword(config\.AdminPassword)" internal/ --include="*.go" | grep -v "_test.go" | wc -l` = 0
- **Wave 2 前端硬指标（W-03 精确 = 0）**: `grep -c "adminUsername\|adminPassword" xingran-react-frontend/src/lib/adDomainApi.ts xingran-react-frontend/src/pages/ad-domain/configs/index.tsx internal/services/addomain/service.go` 三文件均 = 0（含注释也不残留）
- TestAccountPoolPasswordRoundTrip + TestDecryptPassword_InvalidCiphertext + TestSyncManagersToAD_* 全部 PASS
</verification>

<success_criteria>
- 22 处 decryptPassword(config.AdminPassword) Go 调用全删（grep = 0）
- 前端 adminUsername/adminPassword 字段从 adDomainApi.ts 三个接口 + configs/index.tsx + service.go 零残留（含注释，grep = 0，W-03）
- 后端 binding:"required" 同步删除（前后端配套同 commit，SHA-4）
- ad_authenticator bindAdmin 死代码删除，bindAdminWithFailover 双读 fallback 分支删除
- decryptPassword / encryptPassword 函数定义保留（账号池路径仍用）
- go build ./... + 前端 type-check + build 通过
- D-04 完成：AD 配置表单移除 admin 输入项，账号管理收敛到 AccountPoolTab
</success_criteria>

<output>
Create `.planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-03-SUMMARY.md` when done
</output>
