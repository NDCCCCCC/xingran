---
phase: 38-unify-ad-authentication-onto-account-pool-remove-legacy-sing
plan: 04
type: execute
wave: 4
depends_on: [38-03]
files_modified:
  - internal/models/ad_domain.go
  - internal/core/db/migrations/migration_164_phase38_verify_admin_migrated.go
  - internal/core/db/database.go
  - internal/core/core.go
autonomous: true
requirements: [D-01, D-02, D-03]
user_setup: []

must_haves:
  truths:
    - "models.ADConfig 的 AdminUsername / AdminPassword struct tag 放宽（移除 not null 约束，标 @Deprecated 兼容读取）"
    - "DB 列 sys_ad_config.admin_username / admin_password 保留为空（D-02 不做 DROP COLUMN）"
    - "migration_164_phase38_verify 幂等保证现有 sys_ad_config 单管理员账号已入 sys_ad_service_accounts（幂等：已迁则 skip）"
    - "应用启动时空池校验仅记 WARN 日志，不阻断启动（Pitfall 6，D-03）"
    - "运行时登录/同步遇空池返回明确引导文本（D-03，38-02 已实现 ErrAllAccountsUnavailable 包装）"
    - "go build ./... + go test ./... 通过"
  artifacts:
    - path: "internal/core/db/migrations/migration_164_phase38_verify_admin_migrated.go"
      provides: "Phase 38 补迁幂等校验"
      contains: "func Migrate164Phase38VerifyAdminMigrated"
    - path: "internal/core/core.go"
      provides: "启动空池校验 WARN 日志"
      contains: "checkEmptyAccountPoolOnStartup"
  key_links:
    - from: "internal/core/db/database.go::AutoMigrate"
      to: "internal/core/db/migrations/migration_164_phase38_verify_admin_migrated.go::Migrate164Phase38VerifyAdminMigrated"
      via: "注册调用（紧跟 Migrate163ADAccountPoolMenu 之后）"
      pattern: "Migrate164"
    - from: "internal/core/core.go::initAuthFactory"
      to: "internal/core/core.go::checkEmptyAccountPoolOnStartup"
      via: "启动钩子"
      pattern: "checkEmptyAccountPoolOnStartup"
---

<objective>
Wave 3 收尾：model struct tag 清理（D-01 Wave 3）+ migration_164 补迁幂等校验（D-03）+ 启动空池 WARN 校验（D-03）+ 可选孤立文件清理（Open Question 3）。本 plan 是 phase 最后一步，完成后 D-02/D-03 全部落地，phase gate 验证全绿。

Purpose: 完成 D-02 "保留空列仅删代码" 的最后一公里——model 层 struct tag 放宽，DB 列不再被 ORM 强约束；migration_164 幂等保证生产环境现有单管理员账号已入池（避免清理后登录失败）；启动 WARN 校验提供运维可见性但不阻断新环境部署。

**演进说明（I-01）：** CONTEXT D-03 原述扩展 migration_162 校验分支；本研究/PATTERN 确认改为独立 migration_164（幂等校验），原因：(1) migration_162 已在生产执行，重写已部署 migration 会破坏版本审计链；(2) SHA-6 单一职责原则——补迁校验与首次建表迁移分离便于回滚与问题定位；(3) migration_163 已被 Phase 36 账号池菜单权限占用，下一个可用序号为 164。

Output: 4 个文件改造 + 1 个新建 migration（migration_164），go test ./... + 全量 grep 断言通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-CONTEXT.md
@.planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-RESEARCH.md
@.planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-PATTERNS.md
@.planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-03-SUMMARY.md

<interfaces>
<!-- Wave 3 改造点 + 现有 analog -->

From internal/models/ad_domain.go:29-32 (现 struct tag):
```go
type ADConfig struct {
    // ...
    AdminUsername string `json:"adminUsername" gorm:"column:admin_username;size:255;not null" ...` // ← 放宽 not null
    AdminPassword string `json:"adminPassword" gorm:"column:admin_password;size:500;not null" ...` // ← 放宽 not null
    // ...
}
```

From internal/core/db/migrations/migration_162_ad_service_accounts.go:55-68 (幂等样板):
```go
var existingCount int64
db.Table("sys_ad_service_accounts").Where("deleted_at IS NULL").Count(&existingCount)
if existingCount > 0 { log.Printf("...skip..."); return nil }
// INSERT INTO sys_ad_service_accounts ...
```

From internal/core/db/database.go:395-405 (AutoMigrate 真实注册点 — 38-04 Task 2 必须在此追加):
```go
// 第 395-397 行: Migrate162ADServiceAccounts
if err := migrations.Migrate162ADServiceAccounts(d.DB); err != nil { ... }
// 第 399-401 行: Migrate163ADAccountPoolMenu（Phase 36 账号池菜单 — 已占用 163 序号）
if err := migrations.Migrate163ADAccountPoolMenu(d.DB); err != nil {
    applogger.Errorf("AD 服务账号池菜单迁移失败: %v", err)
}
// ← Migrate164Phase38VerifyAdminMigrated 注册调用追加在此（第 401 行的 `}` 之后）
// 第 403-405 行: Migrate117CreateMacFilterRules
if err := migrations.Migrate117CreateMacFilterRules(d.DB); err != nil { ... }
```

From internal/core/core.go:566-591 (initAuthFactory — SP-8 启动校验落点):
```go
accountPool := addomain.NewAccountPool(c.GetDB(), nil)
c.AuthFactory.SetAccountPool(accountPool)
if err := accountPool.StartHotReload(context.Background()); err != nil { /* ... */ }
// ← 此处追加 c.checkEmptyAccountPoolOnStartup(accountPool)
```

From internal/services/addomain/account_pool.go:223-250 (CountByStatus API):
```go
func (p *accountPoolImpl) CountByStatus(ctx context.Context, configID string) (total, available, breaker, disabled int64, err error)
```

From CLAUDE.md memory:
- 「xingran 项目 .sql 迁移文件不会被自动加载」→ 必须用 migration_NNN_*.go 函数显式调用并加入 AutoMigrate()
- 「xingran-gorm-sql-constraint-naming-conflict」→ 手写 SQL 索引/约束命名避免与 GORM uni_*_* 冲突
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: model struct tag 放宽 + 启动空池 WARN 校验（D-02 + D-03）</name>
  <files>
    internal/models/ad_domain.go,
    internal/core/core.go
  </files>
  <read_first>
    - internal/models/ad_domain.go:1-60（ADConfig struct 全貌，AdminUsername/AdminPassword 字段 tag）
    - internal/models/ad_service_account.go:19-79（账号池 model 作为迁移目标参考）
    - internal/core/core.go:566-591（initAuthFactory — SP-8 启动校验落点）
    - internal/services/addomain/account_pool.go:223-250（CountByStatus API 签名）
    - .planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-PATTERNS.md（SP-8 启动空池校验完整样例 + Pitfall 6 不阻断启动警告）
    - .planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-03-SUMMARY.md（确认 admin 字段使用代码已全删，仅剩 model 定义）
  </read_first>
  <behavior>
    - ADConfig.AdminUsername/AdminPassword 的 gorm tag 移除 `not null` 约束（改为空 nullable），保留 column/size tag
    - 字段保留但加 @Deprecated 注释（D-02 保留空列）
    - core.go initAuthFactory 末尾追加 checkEmptyAccountPoolOnStartup(accountPool) 调用
    - checkEmptyAccountPoolOnStartup 函数仅记 applogger.Warnf，**不返回 error**（Pitfall 6 不阻断启动）
    - 校验逻辑：查 status=enabled AND sync_enabled=true 的 ADConfig，对每个调 pool.CountByStatus，total=0 或 available=0 时 WARN
    - go build ./... 通过
  </behavior>
  <action>
    model 改造（ad_domain.go:29-32）：AdminUsername/AdminPassword 字段的 gorm tag 移除 `not null`（改为 `gorm:"column:admin_username;size:255"` 形式），字段上方注释加 `// @Deprecated Phase 38: 已收敛到 sys_ad_service_accounts 账号池，此字段保留作 DB 列兼容，不再读写`。**不删除字段本身**（D-02 保留 DB 列）。启动校验（core.go:initAuthFactory 末尾，约 line 588 后）：追加 `c.checkEmptyAccountPoolOnStartup(accountPool)` 调用；新增方法 `func (c *Core) checkEmptyAccountPoolOnStartup(pool addomain.AccountPool) { ... }` 按 SP-8 analog 实现：查 `c.GetDB().Where("status = ? AND sync_enabled = ?", models.ADConfigStatusEnabled, true).Find(&configs)`，对每个 config 调 `pool.CountByStatus(ctx, cfg.ID)`，total==0||available==0 时 `applogger.Warnf("[启动校验] AD 配置 %s (ID=%s) 账号池为空（total=%d, available=%d），请在 AD 配置页详情 → 服务账号池 Tab 添加服务账号，否则登录/同步将失败", ...)`。**函数返回 void 不返回 error**（Pitfall 6 强约束）。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go build ./... && go test ./internal/core/ ./internal/services/addomain/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "AdminUsername\s+string\s+\x60[^\x60]*not null" internal/models/ad_domain.go` = 0（字段 tag 精确正则，排除注释干扰）
    - `grep -cE "AdminPassword\s+string\s+\x60[^\x60]*not null" internal/models/ad_domain.go` = 0（字段 tag 精确正则）
    - `grep -c "@Deprecated" internal/models/ad_domain.go` ≥ 2（AdminUsername + AdminPassword 两字段均加 @Deprecated Phase 38 注释）
    - `grep -c "func.*checkEmptyAccountPoolOnStartup" internal/core/core.go` ≥ 1（函数定义存在；也可放在 cmd/main.go，planner discretion）
    - `grep -c "checkEmptyAccountPoolOnStartup" internal/core/core.go` ≥ 2（定义 + 调用）
    - `grep -c "applogger.Warnf" internal/core/core.go` 启动校验函数内 ≥ 1（不阻断启动）
    - `grep -A5 "func.*checkEmptyAccountPoolOnStartup" internal/core/core.go | grep -c "error"` = 0（函数签名无 error 返回值，void）
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>model tag 放宽 + @Deprecated 注释，启动空池 WARN 校验（不阻断启动）实现，go build + addomain/core 测试通过。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: migration_164 补迁幂等校验 + 注册（D-03）</name>
  <files>
    internal/core/db/migrations/migration_164_phase38_verify_admin_migrated.go,
    internal/core/db/database.go
  </files>
  <read_first>
    - internal/core/db/migrations/migration_162_ad_service_accounts.go:1-130（完整幂等样板 — Task 必读 analog）
    - internal/core/db/database.go:390-410（AutoMigrate 真实注册点 — Migrate163ADAccountPoolMenu 在第 399-401 行；新 migration 必须在 401 行 `}` 之后、403 行 Migrate117CreateMacFilterRules 之前追加）
    - internal/core/db/migrations/migration_163_ad_account_pool_menu.go:1-20（确认 163 序号已被 Phase 36 账号池菜单占用 → 下一个必须用 164）
    - internal/models/ad_domain.go（确认 ADConfig 表名 sys_ad_config 与字段名 admin_username/admin_password）
    - internal/models/ad_service_account.go（确认 sys_ad_service_accounts 表结构）
    - .planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-PATTERNS.md（SP-7 migration 幂等改造模式 + analog 样例代码）
    - CLAUDE.md memory 「xingran 项目 .sql 迁移文件不会被自动加载」+ 「xingran-gorm-sql-constraint-naming-conflict」
  </read_first>
  <behavior>
    - 新建 migration_164_phase38_verify_admin_migrated.go，函数签名 `func Migrate164Phase38VerifyAdminMigrated(db *gorm.DB) error`
    - 幂等：对每个启用+同步开启的 sys_ad_config，若对应 config_id 在 sys_ad_service_accounts 已有账号则 skip
    - 补迁：若 admin_username/admin_password 非空且账号池为空，INSERT 一条（与 migration_162:97-116 同款 INSERT）
    - 注册到 database.go AutoMigrate 调用链（**不是 migrate.go**——该文件不存在）
    - 手写 SQL 不与 GORM uni_*_* 约束命名冲突
    - go build ./... 通过；启动时 migration 自动执行（不影响已有数据）
  </behavior>
  <action>
    按 SP-7 analog 样例（PATTERNS.md migration_164 完整模板）新建 internal/core/db/migrations/migration_164_phase38_verify_admin_migrated.go：函数 `func Migrate164Phase38VerifyAdminMigrated(db *gorm.DB) error`，逻辑：(1) 查 `SELECT id, admin_username, admin_password FROM sys_ad_config WHERE sync_enabled = true AND status = 0 AND admin_username IS NOT NULL AND admin_password IS NOT NULL`；(2) 对每个 config 查 `SELECT count(*) FROM sys_ad_service_accounts WHERE config_id = ? AND deleted_at IS NULL`；(3) 若 count > 0 skip（幂等）；(4) 若 count = 0 执行 INSERT 与 migration_162:97-116 同款（id=gen_random_uuid(), config_id=?, username=?, password_ciphertext=?, status=0, failure_count=0, remark='Phase 38 补迁', created_at/updated_at=NOW()）。

    **注册点（B-01 修复）:** AutoMigrate 真实注册点在 `internal/core/db/database.go`（不是 migrate.go，该文件不存在）。在 database.go 第 401 行（`Migrate163ADAccountPoolMenu(d.DB)` 调用块的闭合 `}` 之后）、第 403 行 `Migrate117CreateMacFilterRules` 之前，追加：
    ```go
    // Phase 38: 验证 sys_ad_config 单管理员账号已迁入账号池（幂等补迁）
    if err := migrations.Migrate164Phase38VerifyAdminMigrated(d.DB); err != nil {
        applogger.Errorf("Phase 38 账号池补迁校验失败: %v", err)
    }
    ```

    **注意** CLAUDE.md memory：手写 SQL 索引/约束命名避免 uni_*_* 前缀（GORM 自动约束命名空间），本 migration 仅 INSERT 无 DDL，无冲突风险。日志用 `log.Printf("Migration 164: ...")` 与 migration_162 风格一致。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go build ./... && go test ./internal/core/db/... -count=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test -f internal/core/db/migrations/migration_164_phase38_verify_admin_migrated.go` 文件存在
    - `grep -c "func Migrate164Phase38VerifyAdminMigrated(db \*gorm.DB) error" internal/core/db/migrations/migration_164_phase38_verify_admin_migrated.go` = 1
    - `grep -c "sys_ad_service_accounts" internal/core/db/migrations/migration_164_phase38_verify_admin_migrated.go` ≥ 2（count + insert）
    - `grep -c "deleted_at IS NULL" internal/core/db/migrations/migration_164_phase38_verify_admin_migrated.go` ≥ 1（幂等 count 条件）
    - `grep -c "skip\|Skip" internal/core/db/migrations/migration_164_phase38_verify_admin_migrated.go` ≥ 1（幂等日志）
    - `grep -c "Migrate164Phase38VerifyAdminMigrated" internal/core/db/database.go` ≥ 1（注册调用在 database.go，**不是** migrate.go）
    - `test ! -f internal/core/db/migrations/migrate.go` = 0 退出（即 migrate.go 不存在，确认注册点正确）
    - `go build ./...` exits 0
    - `go test ./internal/core/db/... -count=1` 通过（不破坏现有 migration 测试）
    - **Manual-Only（I-02）:** 实际启动后端一次，确认 migration_164 自动执行且 sys_ad_service_accounts 含迁入账号；二次启动确认幂等跳过（日志出现 "skip"）
  </acceptance_criteria>
  <done>migration_164 幂等补迁实现 + 在 database.go 第 401 行后注册，go build + db 测试通过，D-03 migration 校验落地。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 应用启动→DB | 启动 WARN 校验只读 DB，无写操作 |
| migration→DB | migration_164 INSERT 操作，幂等保证不重复 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-13-Startup | Denial of Service | 启动空池校验阻断部署 | mitigate | D-03 + Pitfall 6：函数返回 void 不返回 error，仅 applogger.Warnf；acceptance_criteria 锁 grep 函数签名无 error 返回值 |
| T-38-14-Idempotent | Tampering | migration_164 重复插入 | mitigate | SP-7 幂等模式：先 count 已有账号数 >0 则 skip；acceptance_criteria 锁 deleted_at IS NULL + skip 日志；Manual-Only 二次启动验证 |
| T-38-15-Leak | Information Disclosure | 启动 WARN 日志泄露敏感信息 | mitigate | 日志仅含 config_name/id/total/available 计数，不含密码/账号详情；与 Phase 36 WARN 风格一致 |
| T-38-16-Constraint | Tampering | 手写 SQL 约束命名冲突 | mitigate | CLAUDE.md memory 警示：migration_164 仅 INSERT 无 DDL，不创建索引/约束，无 uni_*_* 冲突风险 |
| T-38-17-DropColumn | Tampering | 误执行 DROP COLUMN | mitigate | D-02 强约束：仅删 Go 代码，DB 列保留；model tag 放宽 not null 但不删除字段；acceptance_criteria 锁字段仍存在 |
| T-38-SC | Tampering | 无 npm/pip/cargo 安装 | N/A | 纯 Go 重构 |
</threat_model>

<verification>
- `go build ./...` exits 0
- `go test ./... -count=1` 全绿
- model tag 放宽（not null 移除）+ @Deprecated 注释
- migration_164 文件存在 + 注册到 database.go AutoMigrate（第 401 行后）
- 启动空池校验函数返回 void（不阻断启动）
- **Phase gate 全量断言**（38-VALIDATION.md）:
  - `grep -rn "decryptPassword(config\.AdminPassword)" internal/` = 0
  - `grep -rn "NewLDAPClient(config)\|NewLDAPClient(&config)\|NewLDAPClient(&adConfig)" internal/ --include="*.go" | grep -v "_test.go" | grep -v "failover_client.go"` = 0
  - `grep -rn "adminUsername\|adminPassword" xingran-react-frontend/src/lib/adDomainApi.ts xingran-react-frontend/src/pages/ad-domain/configs/` = 0（无残留）
</verification>

<success_criteria>
- ADConfig model struct tag 放宽（移除 not null），字段 @Deprecated 保留（D-02 保留 DB 列）
- migration_164_phase38_verify 幂等补迁实现 + 在 database.go 注册（D-03）
- 启动空池校验 WARN 不阻断启动（D-03 + Pitfall 6）
- go build ./... + go test ./... 全绿
- Phase gate 全量 grep 断言通过（无残留单管理员路径）
- D-01/D-02/D-03/D-04 全部决策落地
</success_criteria>

<output>
Create `.planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-04-SUMMARY.md` when done
</output>
