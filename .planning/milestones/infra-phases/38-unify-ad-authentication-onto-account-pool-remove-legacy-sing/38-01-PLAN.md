---
phase: 38-unify-ad-authentication-onto-account-pool-remove-legacy-sing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/services/addomain/service.go
  - internal/services/addomain/sync.go
  - internal/services/addomain/user.go
  - internal/services/addomain/group.go
  - internal/services/addomain/group_sync_service.go
  - internal/services/addomain/group_management_service.go
  - internal/services/addomain/dept_sync_service.go
  - internal/services/addomain/user_ad_sync_service.go
  - internal/services/addomain/config.go
  - internal/api/v1/system/ad_domain_router.go
  - internal/scheduler/ad_sync_tasks.go
autonomous: true
requirements: [D-01]
user_setup: []

must_haves:
  truths:
    - "SyncService / UserService / GroupService / GroupSyncService / GroupManagementService / DeptToADSyncService / UserADSyncService / ConfigService 的 struct 均含 pool AccountPool 字段"
    - "NewSyncService / NewUserService / NewGroupService / NewGroupSyncService / NewGroupManagementService / NewDeptToADSyncService / NewUserADSyncService / NewConfigService 构造函数均接收 pool 参数"
    - "NewADDomainService 签名变更为 (db *gorm.DB, pool AccountPool, cipher ...PasswordCipher)，透传 pool 到所有 sub-service"
    - "ad_domain_router.go 与 ad_sync_tasks.go 复用同一 AccountPool 实例（Pitfall 4 缓存共享）"
    - "全项目 go build ./... 通过；现有 7 个 TestSyncManagersToAD_* + TestAccountPoolPasswordRoundTrip + TestFailoverClient 保持绿色"
    - "本 plan 暂未删除任何 decryptPassword / NewLDAPClient(config) 调用（仅注入依赖，业务逻辑保持不变）"
  artifacts:
    - path: "internal/services/addomain/service.go"
      provides: "NewADDomainService(db, pool, cipher...) 聚合根构造函数"
      contains: "func NewADDomainService(db *gorm.DB, pool AccountPool, cipher ...PasswordCipher)"
    - path: "internal/services/addomain/sync.go"
      provides: "SyncService.pool 字段"
      contains: "pool AccountPool"
  key_links:
    - from: "internal/api/v1/system/ad_domain_router.go"
      to: "internal/services/addomain/service.go::NewADDomainService"
      via: "构造函数透传 accountPool"
      pattern: "NewADDomainService\\(.*GetDB\\(\\),\\s*accountPool"
    - from: "internal/scheduler/ad_sync_tasks.go"
      to: "internal/services/addomain/service.go::NewADDomainService"
      via: "构造函数透传 pool"
      pattern: "NewADDomainService\\(.*,\\s*pool"
---

<objective>
Wave 1 前置：为 8 个同步/管理/配置服务 struct 注入 AccountPool 字段，统一改构造函数签名，使后续 Wave 1 caller 改造（38-02-PLAN）能直接通过 `s.pool` 访问账号池（SP-2 / SHA-1）。本 plan 不改动业务逻辑，仅做依赖注入脚手架，是整个 phase 的编译期前置：没有 pool 字段，后续 FailoverClient 闘包改造无法通过编译。

Purpose: 消除 38-02-PLAN 改造的最大编译阻塞——当前 NewSyncService(db) / NewUserService(db) 等签名无 pool 参数，21 处 caller 无法直接 NewFailoverClient(s.pool, config)。先做脚手架注入，后续 caller 改造可连续推进不被签名变更打断。
Output: 11 个文件的 struct/constructor/调用点同步更新；go build + 全量 addomain 测试绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-CONTEXT.md
@.planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-RESEARCH.md
@.planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-PATTERNS.md

# Phase 36 设计来源（canonical refs）
@.planning/phases/36-ad-account-pool-failover/CONTEXT.md
@.planning/phases/36-ad-account-pool-failover/PLAN.md

<interfaces>
<!-- 关键接口契约（来自现有 addomain 包，executor 直接复刻，无需 codebase 探索） -->

From internal/services/addomain/account_pool.go:121:
```go
// NewAccountPool 创建账号池（redisPubSub 可传 nil）
func NewAccountPool(db *gorm.DB, redisPubSub AccountPoolRedisPubSub) AccountPool { ... }

// AccountPool 是接口（account_pool.go 顶部定义）
type AccountPool interface {
    ListAvailable(ctx context.Context, configID string) ([]models.ADServiceAccount, error)
    Create(ctx context.Context, account *models.ADServiceAccount) error
    MarkSuccess(ctx context.Context, id string) error
    MarkFailure(ctx context.Context, id string, reason string) error
    CountByStatus(ctx context.Context, configID string) (total, available, breaker, disabled int64, err error)
    // ...
}
```

From internal/core/core.go:581 (现 AuthFactory 已注入 accountPool):
```go
accountPool := addomain.NewAccountPool(c.GetDB(), nil)
c.AuthFactory.SetAccountPool(accountPool)
```

From internal/api/v1/system/ad_domain_router.go:12, 17 (现 router 重复 NewAccountPool — Pitfall 4 风险点):
```go
service := addomainServices.NewADDomainService(core.GetDB(), core.SM4Cipher)
// ...
accountPool := addomainServices.NewAccountPool(core.GetDB(), nil)
accountHandler := NewADAccountHandler(accountPool, core)
```

From internal/scheduler/ad_sync_tasks.go:105, 263, 290 (scheduler 3 处调用):
```go
pool := addomain.NewAccountPool(globalADSyncScheduler.db, nil)
// ...
adService := addomain.NewADDomainService(s.db, getADSM4Cipher())
adService := addomain.NewADDomainService(globalADSyncScheduler.db, getADSM4Cipher())
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 为 8 个服务 struct 增加 pool AccountPool 字段并改构造函数签名</name>
  <files>
    internal/services/addomain/sync.go,
    internal/services/addomain/user.go,
    internal/services/addomain/group.go,
    internal/services/addomain/group_sync_service.go,
    internal/services/addomain/group_management_service.go,
    internal/services/addomain/dept_sync_service.go,
    internal/services/addomain/user_ad_sync_service.go,
    internal/services/addomain/config.go
  </files>
  <read_first>
    - internal/services/addomain/sync.go（现 SyncService struct 在 line 18-28，NewSyncService 在 line 30-33）
    - internal/services/addomain/user.go（NewUserService 在 line 17 附近）
    - internal/services/addomain/group.go（NewGroupService 在 line 17 附近）
    - internal/services/addomain/group_sync_service.go（line 21-25 含内部 NewSyncService(db) 调用，需透传 pool）
    - internal/services/addomain/group_management_service.go（NewGroupManagementService 在 line 37 附近）
    - internal/services/addomain/dept_sync_service.go（构造函数位置）
    - internal/services/addomain/user_ad_sync_service.go（构造函数位置；SHA-5 测试钩子 updateUserAttributeFn 字段保持不变）
    - internal/services/addomain/config.go（NewConfigService 在 line 18 附近）
    - internal/services/addomain/account_pool.go（AccountPool 接口与 NewAccountPool 签名 — SP-2 analog）
    - .planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-PATTERNS.md（SP-2 完整改造前后对照，SHA-1 复用同一实例约束）
  </read_first>
  <behavior>
    - SyncService struct 含 `pool AccountPool` 字段；NewSyncService(db, pool) 签名
    - 8 个服务 struct 均含 pool 字段；8 个构造函数均接收 pool 参数
    - group_sync_service.go 内部 `syncService: NewSyncService(db)` 改为 `NewSyncService(db, pool)` 透传
    - user_ad_sync_service.go 的 `updateUserAttributeFn` 测试钩子字段**保持不变**（SHA-5）
    - go build ./internal/services/addomain/ 通过（caller 尚未更新，全项目 build 在 Task 2 完成）
  </behavior>
  <action>
    对 8 个文件按 SP-2 模式（PATTERNS.md SP-2 改造前后样例）改造：每个 struct 增加 `pool AccountPool` 字段（紧跟 db 字段后），构造函数签名增加 `pool AccountPool` 参数并赋值到 struct 字段。注意 group_sync_service.go line 25 的内部 `syncService: NewSyncService(db)` 必须改为 `NewSyncService(db, pool)` 透传 pool 给内嵌 SyncService。user_ad_sync_service.go 改造时**绝对不动** `updateUserAttributeFn` 字段（SHA-5 保护 7 个回归测试）。本任务**不删除**任何 decryptPassword / NewLDAPClient(config) 调用（业务逻辑保持不变，留待 38-02-PLAN 改造），仅做 struct/constructor 脚手架。pool 字段在 Task 1 完成时暂时未被业务方法使用，go vet 可能报 unused 警告，Task 2 完成 caller 改造后消失。

    **W-01 强约束（原子提交纪律）:** 由于本 task 一次性触及 8 个文件，**必须**按文件原子提交——每改完一个服务（struct + 构造函数 + 该文件内部所有 sub-service caller 透传）后**立即** `go build ./...` 验证通过，再改下一个文件。8 个文件依次推进，共 ~8 次原子 commit（commit message 建议 `refactor(38-01): inject pool into {ServiceName}`），避免半改状态导致大范围编译错误难以定位。group_sync_service.go 因为内嵌 NewSyncService(db) 必须在 sync.go 改完后才能改透传，故 sync.go 必须排在 group_sync_service.go 之前。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go build ./internal/services/addomain/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pool AccountPool" internal/services/addomain/sync.go internal/services/addomain/user.go internal/services/addomain/group.go internal/services/addomain/group_sync_service.go internal/services/addomain/group_management_service.go internal/services/addomain/dept_sync_service.go internal/services/addomain/user_ad_sync_service.go internal/services/addomain/config.go` 每个文件至少 1 命中
    - `grep -n "func NewSyncService(db \*gorm.DB, pool AccountPool)" internal/services/addomain/sync.go` 命中 1 行
    - `grep -n "updateUserAttributeFn" internal/services/addomain/user_ad_sync_service.go` 命中数与改造前一致（钩子字段保留）
    - `cd internal/services/addomain && go vet ./... 2>&1 | grep -c "unused"` 可大于 0（Task 2 后清零）
    - `go build ./internal/services/addomain/` exits 0
  </acceptance_criteria>
  <done>8 个服务 struct 均含 pool 字段，构造函数均接收 pool 参数，go build ./internal/services/addomain/ 通过，user_ad_sync_service.go 测试钩子字段保留不变。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 更新 NewADDomainService 签名 + 所有 caller（router/scheduler）透传同一 AccountPool 实例</name>
  <files>
    internal/services/addomain/service.go,
    internal/api/v1/system/ad_domain_router.go,
    internal/scheduler/ad_sync_tasks.go
  </files>
  <read_first>
    - internal/services/addomain/service.go（现 NewADDomainService 在 line 117-136）
    - internal/api/v1/system/ad_domain_router.go（现 line 12 NewADDomainService(db, cipher) + line 17 独立 NewAccountPool — 需合并复用避免 Pitfall 4）
    - internal/scheduler/ad_sync_tasks.go（line 105 已有 pool，line 263/290 调 NewADDomainService 需透传）
    - internal/core/core.go:566-591（initAuthFactory 已创建 accountPool — 是否暴露 getter 由 executor 决定；选项 A：router 复用 core.GetAccountPool()，选项 B：router 处单次 New 后传给 handler 与 service 共享）
    - .planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-PATTERNS.md（SP-2 router 调用点同步改 + 反模式 Pitfall 4 缓存不共享警告）
  </read_first>
  <behavior>
    - NewADDomainService 签名变为 `func NewADDomainService(db *gorm.DB, pool AccountPool, cipher ...PasswordCipher) *ADDomainService`
    - 聚合根内 8 个 sub-service 构造调用均透传 pool（Config/Sync/User/Group/GroupSync/GroupMgmt；OU/OUGroupMapping/Log/Computer 不需要 pool 不变）
    - ad_domain_router.go 中 service 与 accountHandler 共享同一 accountPool 实例（不再重复 New）
    - ad_sync_tasks.go line 263/290 调用 NewADDomainService 透传已存在的 pool
    - go build ./... 全项目通过
    - go test ./internal/services/addomain/ 全绿（含 TestAccountPoolPasswordRoundTrip / 7 个 TestSyncManagersToAD_* / TestFailoverClient）
  </behavior>
  <action>
    按 SP-2 改造后样例（PATTERNS.md service.go analog）修改 service.go:119 的 NewADDomainService：签名加 `pool AccountPool` 参数（放在 cipher 之前），return 内 `NewConfigService(db)` 改 `NewConfigService(db, pool)`，Sync/User/Group/GroupSync/GroupMgmt 同样透传 pool；OU/OUGroupMapping/Log/Computer 保持原样（这些服务不连接 LDAP）。然后改 router：将 ad_domain_router.go:12 的 NewADDomainService(core.GetDB(), core.SM4Cipher) 改为 NewADDomainService(core.GetDB(), accountPool, core.SM4Cipher)，其中 accountPool 复用 line 17 已创建的实例（调整代码顺序：先 NewAccountPool，再 NewADDomainService，再 NewADAccountHandler）。注意 Pitfall 4：service 与 accountHandler 必须**同一** accountPool 变量（不重复 New）。然后改 ad_sync_tasks.go line 263/290：将 NewADDomainService(s.db, getADSM4Cipher()) 改为 NewADDomainService(s.db, pool, getADSM4Cipher())（pool 已在 line 105 创建，需确认作用域；若 line 105 的 pool 是局部变量需提到 syncADConfig 函数作用域或重新组织）。本任务**不删除**任何 decryptPassword / NewLDAPClient(config) 调用（业务逻辑不变）。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go build ./... && go test ./internal/services/addomain/ ./internal/core/security/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "func NewADDomainService(db \*gorm.DB, pool AccountPool, cipher" internal/services/addomain/service.go` 命中 1 行
    - `grep -c "NewSyncService(db, pool)\|NewUserService(db, pool)\|NewGroupService(db, pool)\|NewConfigService(db, pool)\|NewGroupSyncService(db, pool)\|NewGroupManagementService(db, pool)" internal/services/addomain/service.go` ≥ 6
    - `grep -n "NewADDomainService" internal/api/v1/system/ad_domain_router.go internal/scheduler/ad_sync_tasks.go` 所有命中均含 pool 参数（无 2 参数版本残留）
    - `grep -c "addomainServices.NewAccountPool(core.GetDB()" internal/api/v1/system/ad_domain_router.go` = 1（精确锚定调用语句，不重复 New，避免 Pitfall 4；grep -c 全词匹配避免注释干扰）
    - `go build ./...` exits 0
    - `go test ./internal/services/addomain/ -run TestSyncManagersToAD -v` 全部 PASS（7 个场景）
    - `go test ./internal/services/addomain/ -run TestAccountPoolPasswordRoundTrip -v` PASS
    - `grep -rn "NewLDAPClient(config)\|NewLDAPClient(&config)\|NewLDAPClient(&adConfig)" internal/ --include="*.go" | grep -v "_test.go" | grep -v "failover_client.go" | wc -l` = 21（本 plan 不改 caller，数量不变；38-02 改造后归零）
  </acceptance_criteria>
  <done>NewADDomainService 签名含 pool，router/scheduler 所有 caller 透传同一实例，go build ./... + addomain 全量测试通过；21 处 NewLDAPClient(config) 数量不变（保留待 38-02 改造）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 进程内 DI | AccountPool 实例从 core/router/scheduler 注入到 service struct，不跨网络边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01-DI | Tampering | AccountPool 实例复用 | mitigate | 强制 router 处单次 NewAccountPool 后传给 service+handler（acceptance_criteria 锁 `addomainServices.NewAccountPool(core.GetDB()` count=1），避免 Pitfall 4 缓存不共享导致熔断后账号仍被选中 |
| T-38-02-Spec | Information Disclosure | pool 字段未初始化 panic | accept | Go zero value 为 nil interface；38-02 改造的 caller 会在 FailoverClient 调用时 panic 而非静默错误，但本 plan 仅注入脚手架，caller 暂未使用 pool 字段，无运行时风险 |
| T-38-SC | Tampering | 无 npm/pip/cargo 安装 | N/A | 本 plan 纯 Go 重构，无第三方包安装；slopcheck N/A |
</threat_model>

<verification>
- `go build ./...` exits 0
- `go test ./internal/services/addomain/ ./internal/core/security/ -count=1` 全绿
- `grep -c "addomainServices.NewAccountPool(core.GetDB()" internal/api/v1/system/ad_domain_router.go` = 1（同一实例，精确锚定）
- NewADDomainService 新签名全项目 caller 已更新（router + scheduler）
</verification>

<success_criteria>
- 8 个服务 struct 均含 pool AccountPool 字段，构造函数接收 pool 参数
- NewADDomainService 签名变更为 (db, pool, cipher...)
- router/scheduler 复用同一 AccountPool 实例（Pitfall 4 缓解）
- 全项目 go build 通过；现有回归测试（TestAccountPoolPasswordRoundTrip / 7 个 TestSyncManagersToAD_* / TestFailoverClient）保持绿色
- user_ad_sync_service.go 测试钩子 updateUserAttributeFn 字段保留不变（SHA-5）
- 21 处 NewLDAPClient(config) 与 22 处 decryptPassword 调用暂未删除（留待 38-02/38-03）
</success_criteria>

<output>
Create `.planning/phases/38-unify-ad-authentication-onto-account-pool-remove-legacy-sing/38-01-SUMMARY.md` when done
</output>
