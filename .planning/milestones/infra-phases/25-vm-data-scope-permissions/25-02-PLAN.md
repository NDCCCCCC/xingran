---
phase: 25-vm-data-scope-permissions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/services/vdi/vm_data_scope_filter.go
  - internal/services/vdi/vm_service_impl.go
autonomous: true
requirements:
  - D-02
  - D-03
  - D-04
must_haves:
  truths:
    - "创建独立的数据范围过滤函数"
    - "处理 bound_user_id 字段（非 dept_id）"
    - "实现5种数据范围过滤规则"
    - "处理无绑定用户（NULL 值）的特殊情况"
  artifacts:
    - path: "internal/services/vdi/vm_data_scope_filter.go"
      provides: "VM数据范围过滤函数"
      exports: ["ApplyVMDataScopeFilter", "ApplyBoundUserFilter"]
    - path: "internal/services/vdi/vm_service_impl.go"
      provides: "Service层调用过滤函数"
      contains: "ApplyVMDataScopeFilter"
  key_links:
    - from: "vm_service_impl.go"
      to: "vm_data_scope_filter.go"
      via: "function call"
      pattern: "ApplyVMDataScopeFilter"
    - from: "vm_data_scope_filter.go"
      to: "models.DataScope"
      via: "import"
      pattern: "models.DataScope(All|Custom|Dept|DeptChild|Self)"
---

<objective>
创建虚拟机专属的数据范围过滤函数，处理基于 `bound_user_id` 字段的五层数据范围权限控制。

Purpose: 实现虚拟机数据范围权限的核心逻辑，确保用户只能访问其权限范围内的虚拟机数据。
Output: 数据范围过滤函数文件和修改后的 Service 层文件
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-vm-data-scope-permissions/25-CONTEXT.md
@.planning/phases/25-vm-data-scope-permissions/25-RESEARCH.md
@.planning/phases/25-vm-data-scope-permissions/25-PATTERNS.md
@pkg/middleware/permission.go
@internal/models/base.go
@internal/models/vdi.go
@internal/services/vdi/vm_service_impl.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VM data scope filter functions</name>
  <files>internal/services/vdi/vm_data_scope_filter.go</files>
  <read_first>
    pkg/middleware/permission.go
    internal/models/base.go
    internal/models/vdi.go
  </read_first>
  <action>
Create file `internal/services/vdi/vm_data_scope_filter.go` with the following content:

```go
package vdi

import (
	"github.com/xingran-next/xingran-go-backend/internal/core"
	"github.com/xingran-next/xingran-go-backend/internal/models"
	applogger "github.com/xingran-next/xingran-go-backend/pkg/logger"
	"gorm.io/gorm"
)

// ApplyVMDataScopeFilter 应用虚拟机数据范围过滤
// 根据 bound_user_id 字段过滤虚拟机数据，支持5种数据范围
func ApplyVMDataScopeFilter(query *gorm.DB, userID string, dataScope models.DataScope, core *core.Core) *gorm.DB {
	switch dataScope {
	case models.DataScopeAll:
		// 全部数据权限，不做过滤
		return query

	case models.DataScopeCustom:
		// 自定义数据权限：通过 sys_role_dept 表过滤
		// 查询用户可访问的部门，然后过滤 bound_user_id 属于这些部门的虚拟机
		var deptIds []string
		err := core.DB.GetDB().Raw(`
			SELECT DISTINCT rd.dept_id
			FROM sys_user_role ur
			INNER JOIN sys_role_dept rd ON ur.role_id = rd.role_id
			WHERE ur.user_id = ?
		`, userID).Scan(&deptIds).Error

		if err != nil {
			applogger.Errorf("Failed to query custom departments for data scope filtering (user_id=%s): %v", userID, err)
			return query.Where("1=0")
		}

		if len(deptIds) > 0 {
			// 过滤 bound_user_id 属于这些部门的虚拟机
			return query.Where("bound_user_id IN (SELECT id FROM sys_user WHERE dept_id IN (?))", deptIds)
		}
		return query.Where("1=0")

	case models.DataScopeDept:
		// 本部门数据权限：过滤 bound_user_id 所属部门与当前用户相同的虚拟机
		var deptId string
		err := core.DB.GetDB().Raw("SELECT dept_id FROM sys_user WHERE id = ?", userID).Scan(&deptId).Error
		if err != nil {
			if err != gorm.ErrRecordNotFound {
				applogger.Errorf("Failed to query user dept for data scope filtering (user_id=%s): %v", userID, err)
			}
			return query.Where("1=0")
		}

		if deptId != "" {
			return query.Where("bound_user_id IN (SELECT id FROM sys_user WHERE dept_id = ?)", deptId)
		}
		return query.Where("1=0")

	case models.DataScopeDeptChild:
		// 本部门及子部门数据权限：过滤 bound_user_id 属于当前部门及其子部门的虚拟机
		var deptId string
		err := core.DB.GetDB().Raw("SELECT dept_id FROM sys_user WHERE id = ?", userID).Scan(&deptId).Error
		if err != nil {
			if err != gorm.ErrRecordNotFound {
				applogger.Errorf("Failed to query user dept for data scope filtering (user_id=%s): %v", userID, err)
			}
			return query.Where("1=0")
		}

		if deptId != "" {
			// 查询本部门及所有子部门
			var childDeptIds []string
			childDeptIds = append(childDeptIds, deptId)
			getChildDepts(core, deptId, &childDeptIds)

			return query.Where("bound_user_id IN (SELECT id FROM sys_user WHERE dept_id IN (?))", childDeptIds)
		}
		return query.Where("1=0")

	case models.DataScopeSelf:
		// 仅本人数据权限：只过滤 bound_user_id 等于当前用户的虚拟机
		return query.Where("bound_user_id = ?", userID)

	default:
		return query.Where("1=0")
	}
}

// ApplyBoundUserFilter 应用无绑定用户过滤规则
// 无绑定用户的虚拟机（bound_user_id IS NULL）仅对 DataScopeAll 可见
func ApplyBoundUserFilter(query *gorm.DB, dataScope models.DataScope) *gorm.DB {
	if dataScope != models.DataScopeAll {
		// 非 DataScopeAll 的查询自动过滤掉无绑定用户的虚拟机
		return query.Where("bound_user_id IS NOT NULL")
	}
	return query
}

// getChildDepts 递归查询所有子部门（复用自 permission.go 的逻辑）
func getChildDepts(core *core.Core, parentId string, deptIds *[]string) {
	var depts []struct {
		ID string
	}

	err := core.DB.GetDB().Raw("SELECT id FROM sys_dept WHERE parent_id = ? AND status = ?", parentId, 0).Scan(&depts).Error
	if err != nil {
		return
	}

	for _, dept := range depts {
		*deptIds = append(*deptIds, dept.ID)
		getChildDepts(core, dept.ID, deptIds)
	}
}
```

**Key implementation details:**
- 函数签名：`ApplyVMDataScopeFilter(query *gorm.DB, userID string, dataScope models.DataScope, core *core.Core) *gorm.DB`
- 处理5种数据范围：DataScopeAll, DataScopeCustom, DataScopeDept, DataScopeDeptChild, DataScopeSelf
- 基于 `bound_user_id` 字段过滤（而非 `dept_id`）
- 使用子查询过滤用户所属部门
- 错误处理：记录日志并返回空结果（WHERE 1=0）
- `ApplyBoundUserFilter` 处理 NULL 值特殊情况
  </action>
  <verify>
    <automated>cat internal/services/vdi/vm_data_scope_filter.go | grep -E "(ApplyVMDataScopeFilter|ApplyBoundUserFilter|bound_user_id)" | wc -l</automated>
  </verify>
  <done>
    - 文件创建在正确路径
    - 包含 ApplyVMDataScopeFilter 函数
    - 包含 ApplyBoundUserFilter 函数
    - 包含 getChildDepts 辅助函数
    - 所有数据范围类型都有对应的 case 分支
    - 错误处理正确（记录日志 + WHERE 1=0）
    - bound_user_id 字段使用正确
    - NULL 值处理逻辑完整
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate data scope filter into VM Service</name>
  <files>internal/services/vdi/vm_service_impl.go</files>
  <read_first>
    internal/services/vdi/vm_service_impl.go
    internal/services/vdi/vm_data_scope_filter.go
  </read_first>
  <action>
Modify `internal/services/vdi/vm_service_impl.go` to integrate the data scope filter:

1. Add import statement at the top (if not already present):
```go
import (
    // ... existing imports
    "github.com/xingran-next/xingran-go-backend/internal/models"
)
```

2. In the `ListVMs` method, after building the query (around line 615, after the WHERE clauses), add the following code:

```go
// 从上下文获取用户信息
userIDVal := ctx.Value("user_id")
dataScopeVal := ctx.Value("data_scope")

// 如果有数据范围权限信息，应用过滤
if userIDVal != nil && dataScopeVal != nil {
    userID, ok1 := userIDVal.(string)
    dataScope, ok2 := dataScopeVal.(models.DataScope)
    
    if ok1 && ok2 {
        // 应用数据范围过滤
        query = vdi.ApplyVMDataScopeFilter(query, userID, dataScope, s.core)
        // 应用无绑定用户过滤规则
        query = vdi.ApplyBoundUserFilter(query, dataScope)
    }
}
```

**Implementation location:**
- File: `internal/services/vdi/vm_service_impl.go`
- Method: `ListVMs(ctx context.Context, req *ListVMRequest) (*PageResult, error)`
- Location: After line 615 (after existing WHERE clauses), before `query.Count(&total)`

**Key implementation details:**
- 从 context 获取 `user_id` 和 `data_scope`（由 DataScopePermission 中间件设置）
- 类型断言确保值类型正确
- 调用 `ApplyVMDataScopeFilter` 应用基础数据范围过滤
- 调用 `ApplyBoundUserFilter` 处理 NULL 值特殊情况
- 错误处理：如果类型断言失败，跳过过滤（向下兼容）
  </action>
  <verify>
    <automated>grep -n "ApplyVMDataScopeFilter\|ApplyBoundUserFilter" internal/services/vdi/vm_service_impl.go</automated>
  </verify>
  <done>
    - ListVMs 方法包含数据范围过滤代码
    - 从 context 正确获取 user_id 和 data_scope
    - 包含类型断言和错误处理
    - 调用 ApplyVMDataScopeFilter 函数
    - 调用 ApplyBoundUserFilter 函数
    - 代码位置正确（在 query.Count 之前）
    - 向下兼容（类型断言失败时跳过过滤）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Context → Service Layer | User ID and data scope from middleware |
| Service Layer → Database | SQL injection via bound_user_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Tampering | Data scope filter | mitigate | Use GORM parameterized queries, no SQL string concatenation |
| T-25-05 | Information Disclosure | bound_user_id filtering | accept | Low risk - filtering reduces data exposure |
| T-25-06 | Elevation of Privilege | Context value tampering | mitigate | Type assertions with validation, default to no access on error |
</threat_model>

<verification>
- [ ] vm_data_scope_filter.go created at correct path
- [ ] ApplyVMDataScopeFilter handles all 5 DataScope cases
- [ ] ApplyBoundUserFilter handles NULL value filtering
- [ ] getChildDepts recursive function implemented correctly
- [ ] vm_service_impl.go imports models package
- [ ] ListVMs method extracts context values correctly
- [ ] Type assertions include error handling
- [ ] GORM parameterized queries used (no string concatenation)
</verification>

<success_criteria>
- [ ] Data scope filter file created with all required functions
- [ ] Service layer integration complete
- [ ] Context value extraction includes type checking
- [ ] Error handling returns empty result on failure
- [ ] NULL value filtering implemented correctly
- [ ] Code compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/25-vm-data-scope-permissions/25-02-SUMMARY.md`
</output>
