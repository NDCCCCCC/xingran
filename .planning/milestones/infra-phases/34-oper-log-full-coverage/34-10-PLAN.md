---
phase: 34-oper-log-full-coverage
plan: 10
type: execute
wave: 3
depends_on: ["34-09"]
files_modified:
  - CLAUDE.md
  - docs/开发规范.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - internal/utils/operlog/regression_test.go
autonomous: true
requirements:
  - F-OPLOG-DOC

must_haves:
  truths:
    - "CLAUDE.md contains a new section explaining the operlog convention"
    - "docs/开发规范.md contains a guideline: '新增 handler 必须调用 operlog.Record()'"
    - ".planning/STATE.md records Phase 34 completion"
    - ".planning/ROADMAP.md Phase 34 row updated to x/x plans completed"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Operlog convention section for future Claude instances"
      contains: "operlog.Record"
    - path: "docs/开发规范.md"
      provides: "Coding standard: new handlers must call operlog.Record"
      contains: "operlog.Record"
    - path: "internal/utils/operlog/regression_test.go"
      provides: "Regression test that asserts the constant set is stable and Record signature is unchanged"
      contains: "TestOperTypeConstantStability"
  key_links:
    - from: "CLAUDE.md"
      to: "internal/utils/operlog/operlog.go"
      via: "Convention text references the import path"
      pattern: "internal/utils/operlog"
---

<objective>
Documentation and regression test updates to lock in the operlog convention. CLAUDE.md gets a new section that explains the import path, the constant set, and the sensitive-param filter. docs/开发规范.md gets a normative line in the handler-development section. STATE.md and ROADMAP.md record Phase 34 as complete. A regression test guards the public API of the operlog package so a future refactor cannot silently change constant values or signature without breaking the test.

Purpose: A convention that is not in the developer-facing documentation does not survive 6 months of new modules. The audit was triggered by exactly this drift. Plan 34-10 makes the convention visible in the two docs every Claude/developer reads first.

Output: 4 doc files updated, 1 regression test file added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/34-oper-log-full-coverage/34-01-PLAN.md
@.planning/phases/34-oper-log-full-coverage/34-09-PLAN.md
@.planning/notes/260615-oper-log-coverage-audit.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md + docs/开发规范.md + STATE.md + ROADMAP.md + add regression test</name>
  <files>CLAUDE.md, docs/开发规范.md, .planning/STATE.md, .planning/ROADMAP.md, internal/utils/operlog/regression_test.go</files>
  <read_first>
    - CLAUDE.md (find the existing section on "Critical Development Rules" — add new subsection just below it titled "操作日志记录约定")
    - docs/开发规范.md (find the "Handler-Service 模式" or "新增 Handler 流程" section — append the operlog line)
    - .planning/STATE.md (find the "Phase Status" table — add the Phase 34 row)
    - .planning/ROADMAP.md (find the "Phase 34" entry — update "Plans" line)
  </read_first>
  <action>
    Edit CLAUDE.md:
    Add a new section "## 操作日志记录约定 (operlog convention)" just below "Critical Development Rules". Content:
    - "所有 write 操作 handler 必须在 success path 末尾（response.Success 之前）调用 `operlog.Record(c, h.core.OperLogService, h.core.GetDB(), <module>, operlog.OperType<XXX>)`"
    - "module 是中文模块名，参考 AD 域控参考实现（用户管理 / 角色管理 / 部门管理 / 菜单管理 / 通知公告 / API密钥管理 / 楼宇管理 / 工位管理 / 网络设备 / 虚拟机管理 / 工单管理 / 值班池 / 知识库文章 / 定时任务 / Agent管理 / RPA任务 / 缓存监控 / 操作日志）"
    - "OperType 选择：Create=1 / Update=2 / Delete=3 / Grant=4 / Export=5 / Import=6 / Force=7 / GenCode=8 / Clean=9 / Status=10 / Reset=11 / Enable=12 / Disable=13 / Sync=14 / Move=15 / Batch=16 / Upload=17 / Download=18 / Login=19 / Logout=20 / Register=21 / Approve=22 / Reject=23 / Other=0"
    - "敏感字段必须通过 `operlog.WithOperParam(operlog.FilterSensitiveParams(string(body)))` 传入，关键词覆盖 password / pwd / secret / token / key / salt / privateKey / oldPassword / macKey / sm4Key / sm2Key / adminPassword / clientSecret / accessKey / secretKey / private_key / publicKey"
    - "helper 包路径：`github.com/xingran-next/xingran-go-backend/internal/utils/operlog`"
    - "参考实现：`internal/api/v1/system/ad_domain_handler.go`"

    Edit docs/开发规范.md:
    Add to the handler-development section a normative line: "## 操作日志记录 (强制)\n新增 handler 必须遵循 Handler-Service 模式，并且在 success path 末尾调用 `operlog.Record(...)` 记录操作日志。密码、密钥、token 类敏感字段必须通过 `operlog.WithOperParam(operlog.FilterSensitiveParams(string(body)))` 传入，关键词黑名单见 `internal/utils/operlog/operlog.go` 的 sensitiveKeys。"

    Edit .planning/STATE.md:
    Add a row to the Phase Status table: "| 34 | 操作日志全模块集成 | ✅ Completed | 10/10 plans |" (or update the existing row).

    Edit .planning/ROADMAP.md:
    Find "### Phase 34: 操作日志全模块集成" entry, update its status line from "📋 Planned" to "✅ Completed", and update the **Plans** line to "10 plans, all complete" with all 10 plan files listed as completed.

    Create internal/utils/operlog/regression_test.go:
    Test `TestOperTypeConstantStability`:
    - Asserts that all 24 OperType constants are pinned to their documented int values (0, 1, 2, ..., 23).
    - This catches accidental renumbering during future refactors.

    Test `TestRecordSignatureStable`:
    - Compile-time check via reflection: assert Record has 5 parameters (c, operLogSvc, db, module, operType, opts...).
    - Catches signature changes that would break all 309 call sites.

    Test `TestFilterSensitiveParamsKeywordsStable`:
    - Asserts the keyword slice contains at least the 11 mandatory keywords (password, pwd, secret, token, key, salt, privateKey, oldPassword, macKey, sm4Key, sm2Key).
    - Catches accidental removal of critical keywords.

    Test `TestOperTypeCountEquals24`:
    - Iterates the const block, counts OperType* identifiers, asserts == 24.
  </action>
  <acceptance_criteria>
    - CLAUDE.md contains "operlog.Record" AND a new section heading mentioning 操作日志
    - docs/开发规范.md contains "operlog.Record" and references FilterSensitiveParams
    - .planning/STATE.md contains "操作日志全模块集成" in the Phase Status table
    - .planning/ROADMAP.md Phase 34 status is "✅ Completed" (no longer 📋 Planned)
    - internal/utils/operlog/regression_test.go exists with TestOperTypeConstantStability, TestRecordSignatureStable, TestFilterSensitiveParamsKeywordsStable, TestOperTypeCountEquals24 (4 test functions)
    - go build ./... exits 0
    - go vet ./... exits 0
    - go test -count=1 ./internal/utils/operlog/ exits 0
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./... && go test -count=1 ./internal/utils/operlog/ -v && (grep -c "operlog.Record" CLAUDE.md | awk '{test $1 ge 1}') && (grep -c "操作日志" CLAUDE.md | awk '{test $1 ge 1}')
</automated>
  </verify>
  <done>
    CLAUDE.md and docs/开发规范.md updated with the operlog convention. STATE.md and ROADMAP.md record Phase 34 complete. Regression test guards the operlog public API. Phase 34 is fully closed and locked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future refactor -> operlog public API | constant values, Record signature, keyword list must not drift silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-DOC-01 | Repudiation (convention drift) | CLAUDE.md / 开发规范.md | mitigate | Both docs updated with explicit import path + constant list + sensitive-keyword list |
| T-34-DOC-02 | Tampering (silent API change) | operlog package | mitigate | 4 regression tests pin the public API (constant values, signature, keywords, count) |

</threat_model>

<verification>
- go build ./... exits 0
- go vet ./... exits 0
- go test -count=1 ./internal/utils/operlog/ exits 0
- grep on CLAUDE.md returns matches for operlog.Record AND 操作日志
- grep on docs/开发规范.md returns matches for operlog.Record
- .planning/STATE.md and .planning/ROADMAP.md updated

</verification>

<success_criteria>
- F-OPLOG-DOC fully resolved: convention visible in developer docs, regression test in place
- Phase 34 closed: 10/10 plans complete, 309/309 endpoints instrumented
- Single commit: docs(operlog): add convention to CLAUDE.md + 开发规范.md + regression test

</success_criteria>

<output>
Create .planning/phases/34-oper-log-full-coverage/34-10-SUMMARY.md when done
</output>
