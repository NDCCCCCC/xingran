---
phase: 34-oper-log-full-coverage
plan: 06
type: execute
wave: 5
depends_on: ["34-02"]
files_modified:
  - internal/api/v1/vdi/vdi_server_handler.go
  - internal/api/v1/vdi/vm_handler.go
  - internal/api/v1/workorder/workorder_handler.go
  - internal/api/v1/duty/duty_handler.go
  - internal/api/v1/knowledge/handler.go
  - internal/api/v1/scheduler/job_handler.go
autonomous: true
requirements:
  - F-OPLOG-W5

must_haves:
  truths:
    - "POST /vdi/vms writes 1 sys_oper_log row with title 虚拟机管理 and business_type=1"
    - "POST /workorder/orders writes 1 sys_oper_log row with title 工单管理"
    - "POST /scheduler/jobs/:id/run writes 1 sys_oper_log row with title 定时任务 and OperTypeOther"
    - "All cross-module endpoints (vdi + workorder + duty + knowledge + scheduler) trigger sys_oper_log inserts"
  artifacts:
    - path: "internal/api/v1/vdi/vm_handler.go"
      provides: "Record calls on Create/Update/Delete/Start/Stop/BindUser/Sync"
      contains: "operlog.Record"
    - path: "internal/api/v1/workorder/workorder_handler.go"
      provides: "Record calls on WorkOrderHandler (Create/Update/Delete/Assign/Approve/Reject) — single consolidated file replaces category/periodic/config splits"
      contains: "operlog.Record"
    - path: "internal/api/v1/scheduler/job_handler.go"
      provides: "Record calls on Create/Update/Delete/Run/Pause/Resume"
      contains: "operlog.Record"
    - path: "internal/api/v1/duty/duty_handler.go"
      provides: "Record calls on DutyHandler (Pool/Schedule/Holiday/Config) — single consolidated file"
      contains: "operlog.Record"
    - path: "internal/api/v1/knowledge/handler.go"
      provides: "Record calls on ArticleHandler/CategoryHandler/TagHandler (multiple receiver types in one file)"
      contains: "operlog.Record"
  key_links:
    - from: "vm_handler.go Start"
      to: "sys_oper_log"
      via: "operlog.Record(c, h.core.OperLogService, h.core.GetDB(), 虚拟机管理, operlog.OperTypeStatus)"
      pattern: "operlog\\.Record"
---

<objective>
Wave 5 of operlog full coverage: instrument the write endpoints across vdi + workorder + duty + knowledge + scheduler modules. State-change endpoints (vm Start/Stop, job Pause/Resume) use OperTypeStatus. Approval flows (workorder Approve/Reject) use OperTypeApprove/Reject.

**CRITICAL: Actual file structure (verified by `ls internal/api/v1/{workorder,duty,knowledge,vdi,scheduler}/`):**
- vdi: `vdi_server_handler.go` + `vm_handler.go` (two files, as expected)
- workorder: ONLY `workorder_handler.go` — Category/Periodic/Config methods are consolidated into the WorkOrderHandler struct (single file with many methods). **Do NOT look for `category_handler.go` or `period_template_handler.go` — they do not exist.**
- duty: ONLY `duty_handler.go` — Pool/Schedule/Holiday/Config methods are consolidated into the DutyHandler struct. **Do NOT look for `pool_handler.go`, `schedule_handler.go`, `holiday_handler.go`, or `config_handler.go`.**
- knowledge: ONLY `handler.go` — multiple receiver types (ArticleHandler, CategoryHandler, TagHandler) live in one file. **Do NOT look for `article_handler.go`, `category_handler.go`, or `tag_handler.go`.**
- scheduler: `job_handler.go` (single file, as expected)

Purpose: These 5 modules cover VM lifecycle, work order routing, duty scheduling, knowledge base, and cron job execution — all of which have compliance/audit requirements. Without operlog coverage, those questions are unanswerable.

Output: 6 handler files modified; write endpoints instrumented across all 5 modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/notes/260615-oper-log-coverage-audit.md
@.planning/phases/34-oper-log-full-coverage/34-01-PLAN.md
@.planning/phases/34-oper-log-full-coverage/34-01-SUMMARY.md
@.planning/phases/34-oper-log-full-coverage/34-02-PLAN.md
@.planning/phases/34-oper-log-full-coverage/34-02-SUMMARY.md
@internal/utils/operlog/operlog.go
@internal/api/v1/system/ad_domain_handler.go

# Source-of-truth: actual handler file inventory (SINGULAR files in workorder/duty/knowledge)
@internal/api/v1/workorder/workorder_handler.go
@internal/api/v1/duty/duty_handler.go
@internal/api/v1/knowledge/handler.go
@internal/api/v1/scheduler/job_handler.go
@internal/api/v1/vdi/vdi_server_handler.go
@internal/api/v1/vdi/vm_handler.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrument vdi + workorder + scheduler handlers</name>
  <files>internal/api/v1/vdi/vdi_server_handler.go, internal/api/v1/vdi/vm_handler.go, internal/api/v1/workorder/workorder_handler.go, internal/api/v1/scheduler/job_handler.go</files>
  <read_first>
    - internal/api/v1/system/ad_domain_handler.go (reference pattern)
    - internal/api/v1/vdi/vm_handler.go (Start/Stop/BindUser/Sync; uses Phase 22 module)
    - internal/api/v1/scheduler/job_handler.go (Run/Pause/Resume state changes)
    - internal/api/v1/workorder/workorder_handler.go (CONFIRMED: single file containing WorkOrderHandler with Category/Periodic/Config methods consolidated)
  </read_first>
  <action>
    For each handler: add core field, update constructor, add operlog import, instrument write handlers.

    Module names: VDI服务器 (vdi server), 虚拟机管理 (vdi vm), 工单管理 (workorder — single module name; the consolidated handler uses one module name for all workorder writes), 定时任务 (scheduler).

    Endpoint inventory (estimated; verify with actual `grep -c "^func.*)" <file>` counts):
    vdi_server_handler.go (~5 write methods): Create/Update/Delete/Batch/UpdateStatus
    vm_handler.go (~9 write methods): Create/Update/Delete/Batch/Start/Stop/RebindUser/Sync/ConfigIP
    workorder_handler.go (~15-20 write methods across WorkOrderHandler struct): includes CreateOrder/UpdateOrder/DeleteOrder/BatchDelete/Assign/Approve/Reject/Close/Rate/Rotate + Category CRUD (CreateCategory/UpdateCategory/DeleteCategory) + Periodic CRUD + Config CRUD. Module name stays "工单管理" for all of them.
    job_handler.go (~6 write methods): Create/Update/Delete/Run/Pause/Resume

    Special OperType mapping:
    - vm Start/Stop/Rebind -> OperTypeStatus
    - workorder Approve -> OperTypeApprove (constant 22)
    - workorder Reject -> OperTypeReject (constant 23)
    - workorder Assign -> OperTypeGrant
    - workorder Close -> OperTypeUpdate
    - workorder Rate -> OperTypeOther
    - workorder Rotate -> OperTypeOther
    - job Run -> OperTypeOther (manual trigger)
    - job Pause -> OperTypeStatus
    - job Resume -> OperTypeStatus

    **Constructor change pattern:** `WorkOrderHandler` is one struct. Add `core *core.Core` field to the WorkOrderHandler struct. The single `NewWorkOrderHandler(...)` constructor must be updated to accept core as a new parameter. Update `workorder_router.go` to pass `core` to the new constructor. Same pattern for `duty_handler.go`'s DutyHandler struct and `knowledge/handler.go`'s three receiver types (ArticleHandler/CategoryHandler/TagHandler each get their own core field via a shared base, or each gets core injected independently — match the existing constructor signatures).
  </action>
  <acceptance_criteria>
    - Each of the 4 handler files exists at the listed path (verify by ls: workorder_handler.go singular, NOT category_handler.go)
    - vdi_server_handler.go contains operlog.Record( >= 5 times
    - vm_handler.go contains operlog.Record( >= 9 times
    - workorder_handler.go contains operlog.Record( >= 15 times (Category/Periodic/Config methods included)
    - job_handler.go contains operlog.Record( >= 6 times
    - workorder_handler Approve method uses OperTypeApprove
    - workorder_handler Reject method uses OperTypeReject
    - vm_handler Start/Stop use OperTypeStatus
    - go build ./... exits 0
    - go test -count=1 ./internal/api/v1/vdi/ ./internal/api/v1/workorder/ ./internal/api/v1/scheduler/ exits 0
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./... && go test -count=1 ./internal/api/v1/vdi/ ./internal/api/v1/workorder/ ./internal/api/v1/scheduler/ && (test "$(grep -c "operlog.Record(" internal/api/v1/workorder/workorder_handler.go)" -ge 15)
</automated>
  </verify>
  <done>
    VDI + workorder + scheduler endpoints instrumented. Workorder Handler is a single struct with all category/periodic/config methods consolidated into one file; module name "工单管理" applies to all. Approve/Reject use new OperType constants. VM Start/Stop use OperTypeStatus. Build, vet, and existing tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Instrument duty + knowledge handlers + final regression</name>
  <files>internal/api/v1/duty/duty_handler.go, internal/api/v1/knowledge/handler.go</files>
  <read_first>
    - internal/api/v1/duty/duty_handler.go (CONFIRMED: single file containing DutyHandler with Pool/Schedule/Holiday/Config methods consolidated — ~22 methods)
    - internal/api/v1/knowledge/handler.go (CONFIRMED: single file with ArticleHandler, CategoryHandler, TagHandler receiver types — ~17 methods)
  </read_first>
  <action>
    For each handler: instrument per Wave 2 pattern.

    Module names:
    - 值班管理 (duty — single module name covering Pool/Schedule/Holiday/Config)
    - 知识库管理 (knowledge — single module name covering Article/Category/Tag; OR use 子 modules 知识库文章/知识库分类/知识库标签 if the existing handler distinguishes them). Choose one approach and apply consistently.

    Endpoint inventory (verify with actual `grep -c "^func.*)" <file>` counts):
    duty_handler.go (~22 methods): all CRUD + AddMember/RemoveMember/Swap/Reset — single DutyHandler struct covers pool/schedule/holiday/config
    knowledge/handler.go (~17 methods): ArticleHandler (Create/Update/Delete/Publish/Archive) + CategoryHandler (Create/Update/Delete) + TagHandler (Create/Delete)

    Special: pool.AddMember -> OperTypeGrant; pool.RemoveMember -> OperTypeStatus; schedule.Swap -> OperTypeOther; article.Publish/Archive -> OperTypeStatus.

    **Constructor change pattern:** DutyHandler is one struct with one constructor. Add core field, update constructor. knowledge/handler.go has three receiver types (ArticleHandler/CategoryHandler/TagHandler) — each must independently receive core via its constructor; update knowledge_router.go to pass core to all three constructors.

    Final regression:
    - go build ./... exits 0
    - go test -count=1 ./internal/api/v1/duty/ ./internal/api/v1/knowledge/ exits 0
    - grep -r "operlog.Record(" internal/api/v1/vdi/ internal/api/v1/workorder/ internal/api/v1/duty/ internal/api/v1/knowledge/ internal/api/v1/scheduler/ | wc -l returns the cumulative count across all 5 modules (verify against audit's section 4 numbers; vdi ~14, workorder ~25, duty ~22, knowledge ~17, scheduler ~9 — total ~87, NOT the stale 58 from the audit)
  </action>
  <acceptance_criteria>
    - duty_handler.go exists (verify by ls; NOT pool_handler.go/schedule_handler.go/etc)
    - knowledge/handler.go exists (verify by ls; NOT article_handler.go/category_handler.go/tag_handler.go)
    - duty_handler.go contains operlog.Record( >= 13 times (audit's lower bound; actual count likely ~22)
    - knowledge/handler.go contains operlog.Record( >= 10 times (audit's lower bound; actual count likely ~17)
    - pool.AddMember (or equivalent duty handler method) uses OperTypeGrant
    - grep returns cumulative count across the 5 modules (use actual file method counts as ground truth)
    - go build ./... exits 0
    - go test -count=1 ./internal/api/v1/duty/ ./internal/api/v1/knowledge/ exits 0
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./... && go test -count=1 ./internal/api/v1/duty/ ./internal/api/v1/knowledge/ && (test "$(grep -r "operlog.Record(" internal/api/v1/vdi/ internal/api/v1/workorder/ internal/api/v1/duty/ internal/api/v1/knowledge/ internal/api/v1/scheduler/ | wc -l)" -ge 60)
</automated>
  </verify>
  <done>
    Duty + knowledge handlers instrumented. DutyHandler is a single struct consolidating Pool/Schedule/Holiday/Config. Knowledge uses three receiver types (Article/Category/Tag) in one file. Build, vet, and existing tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handler -> sys_oper_log | approval/grant operations must be semantically distinguishable from create/update |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-W5-01 | Repudiation (approval denial) | workorder_handler Approve/Reject | mitigate | OperTypeApprove/Reject constants (22/23) make approval flow distinguishable in audit queries |
| T-34-W5-02 | Repudiation (manual trigger denial) | scheduler_handler Run | mitigate | Manual Run recorded as OperTypeOther with explicit module "定时任务" |
| T-34-W5-03 | Tampering (audit gap) | all consolidated handlers | mitigate | Each write endpoint instrumented across all 3 receiver types in knowledge and the consolidated structs in workorder/duty |
| T-34-W5-04 | Repudiation (operator denies pool change) | duty_handler AddMember/RemoveMember | mitigate | AddMember uses OperTypeGrant; RemoveMember uses OperTypeStatus |

</threat_model>

<verification>
- go build ./... exits 0
- go vet ./... exits 0
- go test -count=1 ./internal/api/v1/vdi/ ./internal/api/v1/workorder/ ./internal/api/v1/duty/ ./internal/api/v1/knowledge/ ./internal/api/v1/scheduler/ exits 0
- grep returns cumulative count across the 5 modules (use actual file method counts; audit's 58 is stale — actual is higher)
- All 6 handler files exist at singular consolidated paths (no category_handler.go / pool_handler.go / article_handler.go etc.)

</verification>

<success_criteria>
- F-OPLOG-W5 fully resolved: cross-module write endpoints instrumented across all 5 modules
- Endpoint counts verified against actual current handler method counts (NOT stale audit numbers)
- Build, vet, existing tests pass
- Single commit: feat(operlog): instrument cross-module endpoints (Wave 5)

</success_criteria>

<output>
Create .planning/phases/34-oper-log-full-coverage/34-06-SUMMARY.md when done
</output>
