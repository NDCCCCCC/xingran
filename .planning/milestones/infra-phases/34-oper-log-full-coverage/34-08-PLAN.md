---
phase: 34-oper-log-full-coverage
plan: 08
type: execute
wave: 7
depends_on: ["34-02"]
files_modified:
  - internal/api/v1/system/dashboard_handler.go
  - internal/api/v1/system/column_config_handler.go
  - internal/api/v1/system/notification_config_handler.go
  - internal/api/v1/system/notice_user_handler.go
  - internal/api/v1/system/ou_group_mapping_handler.go
  - internal/api/v1/system/ou_mapping_handler.go
  - internal/api/v1/system/ad_domain_user_sync_handler.go
  - internal/api/v1/system/ad_dept_sync_handler.go
autonomous: true
requirements:
  - F-OPLOG-W7

must_haves:
  truths:
    - "POST /system/dashboards writes 1 sys_oper_log row with title 仪表盘配置 and business_type=1"
    - "Dashboard handler instruments ALL write methods (Create, Update, Delete, Duplicate, SetDefault, CreateFromTemplate, CreateVersion, RestoreVersion, Export, Import, InvalidateEndpointCache, and any others — ~10+ write methods)"
    - "Notification_config handler instruments ALL write methods (CreateEmailConfig, UpdateEmailConfig, DeleteEmailConfig, TestEmailConfig, CreateAPINotificationConfig, UpdateAPINotificationConfig, DeleteAPINotificationConfig, and any others — ~7+ write methods)"
    - "POST /system/column-configs/:id/save writes 1 sys_oper_log row with title 列自定义配置"
    - "POST /system/ad-domain/user-sync writes 1 sys_oper_log row with title AD用户同步 and OperTypeSync"
    - "All ~24+ system submodule write endpoints trigger sys_oper_log inserts (NOT 8)"
  artifacts:
    - path: "internal/api/v1/system/dashboard_handler.go"
      provides: "Record calls on ALL write methods (verified ~21 methods total in file; ~10+ are writes)"
      contains: "operlog.Record"
    - path: "internal/api/v1/system/notification_config_handler.go"
      provides: "Record calls on ALL write methods (verified ~11 methods total in file; ~7+ are writes)"
      contains: "operlog.Record"
    - path: "internal/api/v1/system/ou_group_mapping_handler.go"
      provides: "Record calls on Create/Update/Delete OU-group mappings"
      contains: "operlog.Record"
  key_links:
    - from: "ad_domain_user_sync_handler.go"
      to: "sys_oper_log"
      via: "operlog.Record(c, h.core.OperLogService, h.core.GetDB(), AD用户同步, operlog.OperTypeSync)"
      pattern: "operlog\\.Record"
---

<objective>
Wave 7 of operlog full coverage: instrument ALL write endpoints (not just 1-per-handler) across the smaller system submodules. The audit's section 4 number for these handlers is undercounted — actual current file content shows:

**Verified actual method counts (via `grep -c "^func.*)" internal/api/v1/system/<file>`):**
- dashboard_handler.go: 21 methods total; ~10+ write methods (Create/Update/Delete/Duplicate/SetDefault/CreateFromTemplate/CreateVersion/RestoreVersion/Export/Import/InvalidateEndpointCache and others)
- notification_config_handler.go: 11 methods total; ~7+ write methods (CreateEmailConfig/UpdateEmailConfig/DeleteEmailConfig/TestEmailConfig/CreateAPINotificationConfig/UpdateAPINotificationConfig/DeleteAPINotificationConfig and others)
- column_config_handler.go: 3 methods (~2 writes: Save/Reset)
- notice_user_handler.go: 7 methods (~3 writes: MarkRead/MarkAllRead and others)
- ou_group_mapping_handler.go: 6 methods (~3 writes)
- ou_mapping_handler.go: 2 methods (~1 write)
- ad_domain_user_sync_handler.go: 1 write (Trigger)
- ad_dept_sync_handler.go: 3 methods (~1 write: Trigger)

**Cumulative write endpoints: ~24+ (NOT 8 as the audit estimated).**

This plan instruments ALL write methods in each file, not just one. The original "8 endpoints, one per handler" was based on stale audit numbers.

Purpose: These endpoints are the "long tail" of audit gaps identified in section 2.3 of the audit report. They handle dashboard configuration (who rearranged the executive view?), column customization (who reset a list to defaults?), notification configuration (who enabled email alerts?), and AD sync triggers (which admin triggered a manual OU sync?). The dashboard and notification_config endpoints are particularly high-volume since dashboards are frequently saved and notification configs are touched when on-call rotations change.

Output: 8 handler files modified; ~24+ write endpoints instrumented. Brings the per-module total up to date with the actual current code state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/notes/260615-oper-log-coverage-audit.md
@.planning/phases/34-oper-log-full-coverage/34-01-PLAN.md
@.planning/phases/34-oper-log-full-coverage/34-01-SUMMARY.md
@.planning/phases/34-oper-log-full-coverage/34-02-PLAN.md
@.planning/phases/34-oper-log-full-coverage/34-02-SUMMARY.md
@internal/utils/operlog/operlog.go
@internal/api/v1/system/ad_domain_handler.go

# Source-of-truth: actual handler file inventory (verified method counts)
@internal/api/v1/system/dashboard_handler.go
@internal/api/v1/system/notification_config_handler.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrument ALL write methods in 8 system submodules + final regression</name>
  <files>internal/api/v1/system/dashboard_handler.go, internal/api/v1/system/column_config_handler.go, internal/api/v1/system/notification_config_handler.go, internal/api/v1/system/notice_user_handler.go, internal/api/v1/system/ou_group_mapping_handler.go, internal/api/v1/system/ou_mapping_handler.go, internal/api/v1/system/ad_domain_user_sync_handler.go, internal/api/v1/system/ad_dept_sync_handler.go</files>
  <read_first>
    - internal/api/v1/system/ad_domain_handler.go (reference pattern)
    - internal/api/v1/system/dashboard_handler.go (21 methods; instrument all writes — Create, Update, Delete, Duplicate, SetDefault, CreateFromTemplate, CreateVersion, RestoreVersion, Export, Import, InvalidateEndpointCache)
    - internal/api/v1/system/notification_config_handler.go (11 methods; instrument all writes — CreateEmailConfig, UpdateEmailConfig, DeleteEmailConfig, TestEmailConfig, CreateAPINotificationConfig, UpdateAPINotificationConfig, DeleteAPINotificationConfig)
    - internal/api/v1/system/ad_domain_user_sync_handler.go (Phase 23 module — manual sync triggers)
    - internal/api/v1/system/ou_group_mapping_handler.go (Phase 23 module — group mapping CRUD)
  </read_first>
  <action>
    For each handler: add core field, update constructor, add operlog import, instrument ALL write handlers.

    Module names: 仪表盘配置 (dashboard), 列自定义配置 (column_config), 通知配置 (notification_config), 我的通知 (my_notices), OU组映射 (ou_group_mapping), OU部门映射 (ou_mapping), AD用户同步 (ad_domain_user_sync), AD部门同步 (ad_dept_sync).

    Endpoint inventory (use ACTUAL method counts, not the audit's stale single-endpoint-per-handler assumption):
    dashboard_handler.go (~10+ writes): Create / Update / Delete / Duplicate / SetDefault / CreateFromTemplate / CreateVersion / RestoreVersion / Export / Import / InvalidateEndpointCache -> 仪表盘配置 / Create/Update/Delete/Other/Other/Create/Other/Other/Export/Import/Other
    column_config_handler.go (~2 writes): Save -> 列自定义配置 / OperTypeUpdate; Reset -> OperTypeReset
    notification_config_handler.go (~7+ writes): CreateEmailConfig / UpdateEmailConfig / DeleteEmailConfig / TestEmailConfig / CreateAPINotificationConfig / UpdateAPINotificationConfig / DeleteAPINotificationConfig -> 通知配置 / Create/Update/Delete/Other/Create/Update/Delete
    notice_user_handler.go (~3 writes): MarkRead / MarkAllRead / (other if present) -> 我的通知 / OperTypeUpdate
    ou_group_mapping_handler.go (~3 writes): Create / Update / Delete -> OU组映射 / Create/Update/Delete
    ou_mapping_handler.go (~1 write): Create / Update / Delete (consolidated) -> OU部门映射 / Create/Update/Delete
    ad_domain_user_sync_handler.go (1 write): Trigger -> AD用户同步 / OperTypeSync
    ad_dept_sync_handler.go (~1 write): Trigger -> AD部门同步 / OperTypeSync

    Pattern for each handler: enumerate the actual write methods via `grep "^func.*)" <file>` and instrument each one. Do NOT skip methods because the audit undercounted them — the audit was wrong about these handlers.

    Final regression at the end of this task:
    - go build ./... exits 0
    - go vet ./... exits 0
    - go test -count=1 ./internal/api/v1/system/ exits 0
    - grep -r "operlog.Record(" internal/ | wc -l returns the cumulative count across all 7 waves (target: 300+ — actual depends on cumulative instrumentation)
    - All 7 wave plans (34-02 through 34-08) complete: per-wave counts use actual method counts as ground truth, not the audit's stale single-endpoint-per-handler assumption
  </action>
  <acceptance_criteria>
    - dashboard_handler.go contains operlog.Record( at least 10 times (matching its ~10+ write methods)
    - notification_config_handler.go contains operlog.Record( at least 7 times (matching its ~7+ write methods)
    - column_config_handler.go contains operlog.Record( at least 2 times (Save, Reset)
    - notice_user_handler.go contains operlog.Record( at least 3 times
    - ou_group_mapping_handler.go contains operlog.Record( at least 3 times
    - ou_mapping_handler.go contains operlog.Record( at least 1 time
    - ad_domain_user_sync_handler.Trigger uses OperTypeSync
    - ad_dept_sync_handler.Trigger uses OperTypeSync
    - column_config_handler has both Save (OperTypeUpdate) and Reset (OperTypeReset)
    - grep -r "operlog.Record(" internal/ | wc -l returns the cumulative count across all waves
    - go build ./... exits 0
    - go test -count=1 ./internal/api/v1/system/ exits 0
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./... && go test -count=1 ./internal/api/v1/system/ && (test "$(grep -c "operlog.Record(" internal/api/v1/system/dashboard_handler.go)" -ge 10) && (test "$(grep -c "operlog.Record(" internal/api/v1/system/notification_config_handler.go)" -ge 7)
</automated>
  </verify>
  <done>
    8 system submodules instrumented with ALL write methods (NOT just one-per-handler). dashboard_handler has 10+ Record calls, notification_config_handler has 7+, column_config has 2, etc. ad_domain_user_sync and ad_dept_sync use OperTypeSync. Cumulative count across all 7 waves reaches the audit's revised per-module totals.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handler -> sys_oper_log | AD sync triggers must be attributable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-W7-01 | Repudiation (manual sync denial) | ad_domain_user_sync_handler.Trigger | mitigate | OperTypeSync with explicit module name "AD用户同步" |
| T-34-W7-02 | Tampering (audit gap) | all 8 submodules | mitigate | All write methods instrumented (NOT just one-per-handler); per-module Record counts match actual method counts |
| T-34-W7-03 | Repudiation (dashboard rearrangement denial) | dashboard_handler.Update | mitigate | Each Update/Delete/Duplicate/SetDefault/CreateVersion method instrumented with module "仪表盘配置" |
| T-34-W7-04 | Repudiation (notification config change denial) | notification_config_handler | mitigate | Each Create/Update/Delete/TestConfig method instrumented with module "通知配置" |

</threat_model>

<verification>
- go build ./... exits 0
- go vet ./... exits 0
- go test -count=1 ./internal/api/v1/system/ exits 0
- grep -r "operlog.Record(" internal/ | wc -l returns cumulative count across all 7 waves (audit's 309 figure is stale; use actual cumulative)
- dashboard_handler.go: 10+ Record calls
- notification_config_handler.go: 7+ Record calls
- All 7 waves (34-02 through 34-08) reach their per-wave endpoint counts (use actual current method counts)

</verification>

<success_criteria>
- F-OPLOG-W7 fully resolved: 8 system submodules instrumented with ALL write methods
- Per-module Record counts match actual current method counts (dashboard ~10+, notification_config ~7+, etc.)
- Total endpoint coverage: cumulative count across all waves (NOT the audit's stale 309 figure)
- Build, vet, existing tests pass
- Single commit: feat(operlog): instrument system submodules (Wave 7) — all write methods

</success_criteria>

<output>
Create .planning/phases/34-oper-log-full-coverage/34-08-SUMMARY.md when done
</output>
