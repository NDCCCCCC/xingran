---
phase: 34-oper-log-full-coverage
plan: 05
type: execute
wave: 4
depends_on: ["34-02"]
files_modified:
  - internal/api/v1/network/device_handler.go
  - internal/api/v1/network/credential_handler.go
  - internal/api/v1/network/template_handler.go
  - internal/api/v1/network/command_handler.go
  - internal/api/v1/network/execution_handler.go
  - internal/api/v1/network/backup_handler.go
  - internal/api/v1/network/discovery_handler.go
  - internal/api/v1/network/topology_handler.go
  - internal/api/v1/network/mac_handler.go
  - internal/api/v1/network/port_handler.go
autonomous: true
requirements:
  - F-OPLOG-W4

must_haves:
  truths:
    - "POST /network/devices writes 1 sys_oper_log row with title 网络设备 and business_type=1"
    - "POST /network/credentials writes 1 sys_oper_log row with title 网络设备凭据 and the password is masked"
    - "POST /network/discoveries/trigger writes 1 sys_oper_log row with title 网络设备发现 and OperTypeSync"
    - "All 53 network endpoints trigger sys_oper_log inserts"
  artifacts:
    - path: "internal/api/v1/network/device_handler.go"
      provides: "Record calls on Create/Update/Delete/Batch/Backup/Collect/Sync"
      contains: "operlog.Record"
    - path: "internal/api/v1/network/credential_handler.go"
      provides: "Record calls on Create/Update/Delete with password masking"
      contains: "operlog.Record"
    - path: "internal/api/v1/network/discovery_handler.go"
      provides: "Record calls on Trigger/Cancel with OperTypeSync"
      contains: "operlog.Record"
  key_links:
    - from: "credential_handler.go Create"
      to: "sys_oper_log"
      via: "operlog.RecordWithBody (or Record with WithOperParam(FilterSensitiveParams)) to mask enable_password and password fields"
      pattern: "operlog\\.Record"
---

<objective>
Wave 4 of operlog full coverage: instrument 53 write endpoints across the network module (device / credential / template / command / execution / backup / discovery / topology / mac / port). Credential endpoints are highest-priority for sensitive-param masking (enable_password, snmp_community, ssh_key_passphrase).

Purpose: Network module is the largest, most-instrumented (53 write endpoints) and highest-risk for credential leakage. Without operlog coverage, the audit cannot answer "which admin used the root SSH credential on device X last week?" — a critical security question for any post-incident review.

Output: 10 handler files modified; 53 endpoints instrumented. Note the actual filenames use singular form (`credential_handler.go`, `template_handler.go`, `backup_handler.go`, `discovery_handler.go`, `port_handler.go`) — verified by `ls internal/api/v1/network/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/notes/260615-oper-log-coverage-audit.md
@.planning/phases/34-oper-log-full-coverage/34-01-PLAN.md
@.planning/phases/34-oper-log-full-coverage/34-01-SUMMARY.md
@.planning/phases/34-oper-log-full-coverage/34-02-PLAN.md
@.planning/phases/34-oper-log-full-coverage/34-02-SUMMARY.md
@internal/utils/operlog/operlog.go
@internal/api/v1/system/ad_domain_handler.go

# Source-of-truth: actual handler file inventory
@internal/api/v1/network/credential_handler.go
@internal/api/v1/network/template_handler.go
@internal/api/v1/network/backup_handler.go
@internal/api/v1/network/discovery_handler.go
@internal/api/v1/network/port_handler.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrument device + credential + template + command + execution handlers (28 endpoints)</name>
  <files>internal/api/v1/network/device_handler.go, internal/api/v1/network/credential_handler.go, internal/api/v1/network/template_handler.go, internal/api/v1/network/command_handler.go, internal/api/v1/network/execution_handler.go</files>
  <read_first>
    - internal/api/v1/system/ad_domain_handler.go (reference pattern)
    - internal/api/v1/network/device_handler.go (large file; Backups/Collect/Sync endpoints)
    - internal/api/v1/network/credential_handler.go (CRITICAL: enable_password, snmp_community, ssh_key fields — file is named singular `credential_handler.go`)
  </read_first>
  <action>
    For each network handler: add core field, update constructor, add operlog import, instrument write handlers.

    Module names: 网络设备 (device), 网络设备凭据 (credential), 命令模板 (template), 命令执行 (command — if separate), 执行历史 (execution).

    Endpoint inventory (28 total):
    device_handler.go (10): Create/Update/Delete/Batch/UpdateStatus/Backup/Collect/Refresh/SetCredential/Discover
    credential_handler.go (6): Create/Update/Delete/Batch/Test (verifies the credential works) — ALL use `operlog.RecordWithBody` (the new helper from Plan 34-01 that reads+restores the body and applies FilterSensitiveParams in one call) to mask password/enablePassword/snmpCommunity/sshKey
    template_handler.go (5): Create/Update/Delete/Batch/Preview
    command_handler.go (3): Create/Update/Delete (template-command)
    execution_handler.go (4): ExecuteCommand/Stop/BatchExecute/Cancel

    Special cases:
    - device_handler.Refresh/Collect -> 网络设备 / OperTypeSync
    - credential_handler.Test -> 网络设备凭据 / OperTypeOther (semantic: validate, not create)
    - execution_handler.ExecuteCommand -> 命令执行 / OperTypeOther

    Note on file naming: this plan uses singular filenames (`credential_handler.go`, `template_handler.go`, `backup_handler.go`, `discovery_handler.go`, `port_handler.go`) — these are the actual filenames in `internal/api/v1/network/`. Do NOT use plural forms (`credentials_handler.go` etc.) — they do not exist in the repo.
  </action>
  <acceptance_criteria>
    - Each of the 5 handler files exists at the singular path (verify by `ls internal/api/v1/network/credential_handler.go` and similar)
    - Each of the 5 handler files contains operlog.Record( matching the endpoint count
    - credential_handler.go uses RecordWithBody on at least 4 endpoints (Create/Update/Delete/Batch) — verified by `grep -c "operlog.RecordWithBody" internal/api/v1/network/credential_handler.go` returning >= 4
    - All Chinese module names present
    - go build ./... exits 0
    - go test -count=1 ./internal/api/v1/network/ exits 0
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./... && go test -count=1 ./internal/api/v1/network/ -run "TestDevice|TestCredential|TestTemplate"
</automated>
  </verify>
  <done>
    28 network endpoints instrumented across device/credential/template/command/execution. Credential endpoints mask enable_password/snmp_community/ssh_key via RecordWithBody (or Record+WithOperParam). Build, vet, and existing tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Instrument backup + discovery + topology + mac + port handlers (25 endpoints) + final regression</name>
  <files>internal/api/v1/network/backup_handler.go, internal/api/v1/network/discovery_handler.go, internal/api/v1/network/topology_handler.go, internal/api/v1/network/mac_handler.go, internal/api/v1/network/port_handler.go</files>
  <read_first>
    - internal/api/v1/network/discovery_handler.go (Trigger discovery — OperTypeSync; file is singular `discovery_handler.go`)
    - internal/api/v1/network/mac_handler.go (MAC collection and clear — phase 11-13 module)
    - internal/api/v1/network/topology_handler.go (topology regenerate)
  </read_first>
  <action>
    For each network handler: instrument per Wave 2 pattern.

    Module names: 设备备份 (backup), 网络发现 (discovery), 拓扑图 (topology), MAC地址采集 (mac), 端口管理 (port).

    Endpoint inventory (25 total):
    backup_handler.go (5): Create/Update/Delete/Batch/Restore
    discovery_handler.go (6): Trigger/Cancel/Update/Import/Export/PollStatus — Trigger uses OperTypeSync
    topology_handler.go (4): Regenerate/Update/SaveLayout/ExportDiagram
    mac_handler.go (5): Collect (OperTypeSync) / Clear / BatchImport / Import / Update
    port_handler.go (5): Create/Update/Delete/Batch/Refresh

    Special: mac_handler.Collect and discovery_handler.Trigger use OperTypeSync. topology_handler.Regenerate uses OperTypeSync.

    Final regression:
    - go build ./... exits 0
    - go test -count=1 ./internal/api/v1/network/ exits 0
    - grep -r "operlog.Record(" internal/api/v1/network/ | wc -l returns 53+
    - grep -r "operlog.RecordWithBody(" internal/api/v1/network/ | wc -l returns at least 4 (credential handler)
  </action>
  <acceptance_criteria>
    - Each of the 5 handler files exists at the singular path (verify by ls)
    - Each of the 5 handler files contains operlog.Record( matching the endpoint count
    - discovery_handler.Trigger uses OperTypeSync
    - mac_handler.Collect uses OperTypeSync
    - grep returns 53+ Record matches + 4+ RecordWithBody matches across all network handlers
    - go build ./... exits 0
    - go test -count=1 ./internal/api/v1/network/ exits 0
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./... && go test -count=1 ./internal/api/v1/network/ && (test "$(grep -r "operlog.Record(" internal/api/v1/network/ | wc -l)" -ge 53) && (test "$(grep -r "operlog.RecordWithBody(" internal/api/v1/network/ | wc -l)" -ge 4)
</automated>
  </verify>
  <done>
    All 53 network endpoints instrumented across 10 handler files. Credential/sensitive fields masked via RecordWithBody. Discovery/MAC/Topology sync operations use OperTypeSync. Build, vet, and existing tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handler -> sys_oper_log | enable_password, snmp_community, ssh_key must be masked |
| credential_handler -> c.Request.Body | RecordWithBody must restore body so SM2+SM4 middleware (if any) can still bind downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-W4-01 | Information Disclosure (credential leak) | credential_handler Create/Update | mitigate | RecordWithBody (or Record+WithOperParam+FilterSensitiveParams) covers password/enablePassword/snmpCommunity/sshKey |
| T-34-W4-02 | Tampering (audit gap) | all 10 network handlers | mitigate | Each write endpoint instrumented |
| T-34-W4-03 | Tampering (unattributed device change) | device_handler.Update | mitigate | OperTypeUpdate with explicit module name "网络设备" |
| T-34-W4-04 | DoS (body restore failure) | credential_handler | mitigate | RecordWithBody uses io.NopCloser internally; verified by TestRecordWithBody_RestoresBody |

</threat_model>

<verification>
- go build ./... exits 0
- go vet ./... exits 0
- go test -count=1 ./internal/api/v1/network/ exits 0
- grep -r "operlog.Record(" internal/api/v1/network/ | wc -l returns 53 or more
- grep -r "operlog.RecordWithBody(" internal/api/v1/network/ | wc -l returns 4 or more
- credential_handler.go uses RecordWithBody on at least 4 endpoints
- All 10 handler files exist at singular paths (no plural filenames like `credentials_handler.go`)

</verification>

<success_criteria>
- F-OPLOG-W4 fully resolved: 53 network endpoints now write sys_oper_log rows
- Sensitive credential fields masked via RecordWithBody (the body-aware helper from Plan 34-01)
- Build, vet, existing tests pass
- Single commit: feat(operlog): instrument 53 network endpoints (Wave 4)

</success_criteria>

<output>
Create .planning/phases/34-oper-log-full-coverage/34-05-SUMMARY.md when done
</output>
