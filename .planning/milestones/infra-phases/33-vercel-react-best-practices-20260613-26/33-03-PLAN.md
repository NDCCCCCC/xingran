---
phase: 33-vercel-react-best-practices-20260613-26
plan: 03
type: execute
wave: 3
depends_on:
  - 33-02
files_modified:
  - xingran-react-frontend/src/router/DynamicRoutes.tsx
  - xingran-react-frontend/src/store/menuStore.ts
  - xingran-react-frontend/src/components/layout/sidebar.utils.ts
  - xingran-react-frontend/src/router/routeConfigManager.ts
  - xingran-react-frontend/src/components/layout/HybridLayout.tsx
  - xingran-react-frontend/src/hooks/useWidgetPolling.ts
  - xingran-react-frontend/src/hooks/useTabSync.ts
autonomous: true
requirements:
  - R1
  - R2
  - R3
  - R4
  - R5
  - R6
  - R8

must_haves:
  truths:
    - "DynamicRoutes no longer blocks first-paint waiting for fetchAll when cached menu data exists in store — cachedRoutes short-circuit on store.menus"
    - "useWidgetPolling visibilitychange handler is a module-level singleton with Set<callback> — multiple hook instances share one listener"
    - "useWidgetPolling visibility handler reuses the interval ref so timer does not drift / leak"
    - "HybridLayout.tsx pathname-triggered effects consolidated into useTabSync(pathname) custom hook (defined in src/hooks/useTabSync.ts)"
    - "findMenuByFullPath and findMenuById replaced by O(1) Map.get lookups via menuByPath Map and menuById Map maintained in menuStore"
    - "routeConfigManager fallbackBreadcrumb uses module-level PATH_SPLIT regex instead of inline literal"
  artifacts:
    - path: "xingran-react-frontend/src/router/DynamicRoutes.tsx"
      provides: "Bypass InitializingFallback when menuStore.menus already has data"
      contains: "useMenuStore.getState"
    - path: "xingran-react-frontend/src/store/menuStore.ts"
      provides: "menuByPath Map and menuById Map for O(1) lookup"
      contains: "menuByPath"
    - path: "xingran-react-frontend/src/components/layout/sidebar.utils.ts"
      provides: "findMenuByPath / findMenuById via Map.get"
      contains: "menuByPath"
    - path: "xingran-react-frontend/src/router/routeConfigManager.ts"
      provides: "PATH_SPLIT module-level regex"
      contains: "PATH_SPLIT"
    - path: "xingran-react-frontend/src/hooks/useTabSync.ts"
      provides: "Custom hook consolidating the 3 HybridLayout pathname effects"
      contains: "useTabSync"
    - path: "xingran-react-frontend/src/hooks/useWidgetPolling.ts"
      provides: "Singleton visibilitychange listener with Set<callback>"
      contains: "visibilityListeners"
  key_links:
    - from: "xingran-react-frontend/src/router/DynamicRoutes.tsx"
      to: "xingran-react-frontend/src/store/menuStore.ts"
      via: "useMenuStore.getState().menus short-circuit check before showing InitializingFallback"
      pattern: "useMenuStore\\.getState"
    - from: "xingran-react-frontend/src/components/layout/sidebar.utils.ts"
      to: "xingran-react-frontend/src/store/menuStore.ts"
      via: "findMenuByPath delegates to store.menuByPath.get(path)"
      pattern: "menuByPath\\.get"
    - from: "xingran-react-frontend/src/components/layout/HybridLayout.tsx"
      to: "xingran-react-frontend/src/hooks/useTabSync.ts"
      via: "useTabSync(pathname) wraps the 3 effects that all touch tabsStore"
      pattern: "useTabSync"
    - from: "xingran-react-frontend/src/hooks/useWidgetPolling.ts"
      to: "xingran-react-frontend/src/hooks/useWidgetPolling.ts"
      via: "module-level singleton visibilitychange listener + Set<callback> + shared interval ref"
      pattern: "visibilityListeners"
---

<objective>
Wave 3 — fix 7 MEDIUM defects (R1, R2, R3, R4, R5, R6, R8) from the audit. R1 makes DynamicRoutes skip the blocking InitializingFallback when cached menus exist. R2+R3 share a single visibilitychange listener across useWidgetPolling instances and reuse the interval ref. R4 consolidates HybridLayout's 3 pathname-triggered effects into useTabSync. R5+R8 replace recursive findMenuById/findMenuByFullPath with O(1) Map lookups. R6 uses a module-level regex for breadcrumb splits.

Purpose: After Waves 1+2 (CRITICAL + MEDIUM-HIGH) ship, Wave 3 addresses lower-tier perf: route hydration on refresh, polling resource leaks, layout effect consolidation, and O(1) menu lookups.

**Prior wave changes affecting this plan (Waves 1-2):**
- Plan 02 (M5) added a `selectedKeys` useMemo in `sidebar.tsx` with deps `[location.pathname, menuPathMap, menus]`, and the body still calls `findMenuByFullPath(menus, path)` to resolve the menu id. This plan's R5 step must modify THAT useMemo (Plan 02 M5 site) — replace the inner `findMenuByFullPath(menus, path)` call with `menuByPath.get(path)?.id ?? null` and trim the deps to `[location.pathname, menuByPath]`. Do NOT add a new useMemo; edit the existing one.
- Plan 01 (C2/C3) already added `widgetsRef` and `widgetIdsRef` patterns to the two polling/websocket hooks. R2/R3 build on this — they only add the singleton visibilitychange listener and reuse `intervalRef`. Do not redo the refactor.
- Plan 01 (C4) converted `sidebar.tsx` to per-field zustand selectors. R5 continues this pattern with `useMenuStore(s => s.menuByPath)` / `useMenuStore(s => s.menuById)`.

Output:
- 1 new hook file (`src/hooks/useTabSync.ts`)
- 6 production files modified
- 0 test files (defects are observable at runtime)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-vercel-react-best-practices-20260613-26/CONTEXT.md
@.planning/phases/33-vercel-react-best-practices-20260613-26/33-01-PLAN.md
@.planning/phases/33-vercel-react-best-practices-20260613-26/33-02-PLAN.md
@.planning/reviews/20260613-frontend-react-best-practices-review.md

# Source-of-truth files to consult before editing
@xingran-react-frontend/src/router/DynamicRoutes.tsx
@xingran-react-frontend/src/store/menuStore.ts
@xingran-react-frontend/src/components/layout/sidebar.utils.ts
@xingran-react-frontend/src/router/routeConfigManager.ts
@xingran-react-frontend/src/components/layout/HybridLayout.tsx
@xingran-react-frontend/src/hooks/useWidgetPolling.ts
@xingran-react-frontend/src/components/layout/sidebar.tsx
@xingran-react-frontend/src/store/tabsStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: R1 DynamicRoutes cachedRoutes short-circuit + R6 PATH_SPLIT regex + R5+J5 menuByPath Map</name>
  <files>xingran-react-frontend/src/router/DynamicRoutes.tsx, xingran-react-frontend/src/router/routeConfigManager.ts, xingran-react-frontend/src/store/menuStore.ts, xingran-react-frontend/src/components/layout/sidebar.utils.ts</files>
  <read_first>
    - xingran-react-frontend/src/router/DynamicRoutes.tsx (lines 50-53: cachedRoutes/cachedMenusCount module state; lines 108-178: DynamicRoutes component, lines 161-178 uninitialized/allMenus fallback logic)
    - xingran-react-frontend/src/store/menuStore.ts (lines 17-24: MenuState interface; lines 51-168: store implementation; find where menus is set so we can also rebuild menuByPath Map)
    - xingran-react-frontend/src/components/layout/sidebar.utils.ts (lines 32-96: findMenuById, findMenuByFullPath — both recursive)
    - xingran-react-frontend/src/router/routeConfigManager.ts (lines 117-118 split('/').filter(Boolean); lines 224-226 extractTitleFromPath split; lines 229-247 fallbackBreadcrumb split)
    - xingran-react-frontend/src/components/layout/sidebar.tsx (line 153 findMenuById, line 255 findMenuByFullPath — callers of utils; the `selectedKeys` useMemo added by Plan 02 M5 in `sidebar.tsx` calls `findMenuByFullPath(menus, path)` — this is the Plan 02 site that R5 modifies in Task 2)
  </read_first>
  <action>
    Per R1 — DynamicRoutes reads cached menu from store synchronously:

    Edit xingran-react-frontend/src/router/DynamicRoutes.tsx:
    1. The current behavior: line 176-178 shows `<InitializingFallback />` when `allMenus.length === 0`. We need to bypass this IF the cached menu data is already present in store (e.g., after a hard refresh where TTLMenuCache survived in memory, or restored from sessionStorage).
    2. Replace the `if (allMenus.length === 0) { return <InitializingFallback />; }` (lines 176-178) with a check that also looks at store state via `useMenuStore.getState()`:
       ```
       const stateAllMenus = useMenuStore.getState().allMenus;
       if (allMenus.length === 0 && stateAllMenus.length === 0) {
         // Truly uninitialized — wait for fetchAll to complete
         return <InitializingFallback />;
       }
       ```
       This handles the case where zustand state subscription hasn't fired yet but `getState()` returns the latest cached value.
    3. Alternative (more reliable): use a ref or memoized value from `useMenuStore(s => s.allMenus)` directly — but the destructure at line 109 already does this. So `allMenus` SHOULD reflect cache. The actual issue is the blocking `await fetchAll` in the useEffect at lines 117-123 — when the user has cached menus in store but `allMenus.length === 0` because zustand subscription happened AFTER the render, the fallback shows briefly.
    4. Simpler fix: in the useEffect at lines 117-123, only call fetchAll if menus are TRULY empty (not just `allMenus.length === 0` but also `cachedRoutes === null`):
       ```
       useEffect(() => {
         if (isAuthenticated && initialized && allMenus.length === 0 && cachedRoutes === null) {
           fetchAll().catch((error) => {
             console.error('Failed to load menus after refresh:', error);
           });
         }
       }, [isAuthenticated, initialized, allMenus.length, fetchAll]);
       ```
       This avoids re-triggering fetchAll when cachedRoutes is already populated (i.e., we already have menu data and don't need to re-fetch).
    5. Also add the same check at line 176-178 to use cachedRoutes as a fallback:
       ```
       if (allMenus.length === 0) {
         if (cachedRoutes && cachedRoutes.length > 0) {
           // Use cached routes from previous session
           // ... but we still need allMenus to be populated for the JSX below
         }
         return <InitializingFallback />;
       }
       ```
       Actually a cleaner approach: when cachedRoutes exists but allMenus is empty, populate allMenus from cachedRoutes. But cachedRoutes contains MenuRouteConfig not Menu — different shape. Skip this; the fix is just the useEffect guard so fetchAll only fires when truly needed.

    Per R6 — module-level PATH_SPLIT regex:

    Edit xingran-react-frontend/src/router/routeConfigManager.ts:
    1. Add module-level constant near line 14:
       ```
       const PATH_SPLIT = /\/+/;
       ```
    2. Replace `path.split('/').filter(Boolean)` at line 118 with `path.split(PATH_SPLIT).filter(Boolean)`.
    3. Replace `path.split('/').filter(Boolean)` at line 225 with `path.split(PATH_SPLIT).filter(Boolean)`.
    4. Replace `path.split('/').filter(Boolean)` at line 230 with `path.split(PATH_SPLIT).filter(Boolean)`.

    Per R5 + J5 — Map-based menu lookups in store:

    Edit xingran-react-frontend/src/store/menuStore.ts:
    1. Add two new fields to MenuState interface (around line 17-24):
       ```
       menuByPath: Map<string, Menu>;
       menuById: Map<string, Menu>;
       ```
    2. Add initial values in the store creator (around line 51-58):
       ```
       menus: [],
       allMenus: [],
       menuByPath: new Map(),
       menuById: new Map(),
       permissions: [],
       ...
       ```
    3. Add a helper function near the top of the file (after imports):
       ```
       function buildMenuIndices(menus: Menu[]): { byPath: Map<string, Menu>; byId: Map<string, Menu> } {
         const byPath = new Map<string, Menu>();
         const byId = new Map<string, Menu>();
         const traverse = (list: Menu[]) => {
           for (const menu of list) {
             byId.set(menu.id, menu);
             if (menu.path) byPath.set(menu.path, menu);
             if (menu.children) traverse(menu.children);
           }
         };
         traverse(menus);
         return { byPath, byId };
       }
       ```
       This is best-effort: byPath uses menu.path as-is (relative path within parent). For full-path lookup, the existing menuPathMap in sidebar.tsx still handles parent concat.
    4. In fetchMenus (lines 59-99) and fetchAll (lines 127-168), after setting menus and allMenus, also set menuByPath/menuById. Use the helper:
       ```
       const { byPath, byId } = buildMenuIndices(visibleMenus);
       set({
         menus: visibleMenus,
         allMenus: visibleMenus,
         menuByPath: byPath,
         menuById: byId,
         ...
       });
       ```
    5. In clearMenus (lines 186-196), reset the maps:
       ```
       set({
         menus: [],
         allMenus: [],
         menuByPath: new Map(),
         menuById: new Map(),
         ...
       });
       ```

    Edit xingran-react-frontend/src/components/layout/sidebar.utils.ts:
    1. Add a new exported function findMenuByIdMap that reads from a Map:
       ```
       /**
        * 通过 id Map 查菜单（O(1) 查找，由 menuStore.menuById 维护）
        */
       export function findMenuByIdMap(idMap: Map<string, MenuType>, id: string): MenuType | null {
         return idMap.get(id) ?? null;
       }
       ```
    2. Keep the recursive findMenuById as a fallback (used in places where Map may not be available — e.g., tests).
    3. The actual callers in sidebar.tsx (line 153 findMenuById, line 255 findMenuByFullPath) need to be migrated. This is done in Task 2.

    Acceptance assertions:
    - grep DynamicRoutes.tsx for cachedRoutes returns match
    - grep routeConfigManager.ts for PATH_SPLIT returns match
    - grep menuStore.ts for menuByPath returns matches (state field + set call)
    - grep sidebar.utils.ts for findMenuByIdMap returns match
    - grep sidebar.tsx for findMenuById still returns match (old fn retained as fallback) but new callers should use the Map version
  </action>
  <acceptance_criteria>
    - xingran-react-frontend/src/router/DynamicRoutes.tsx: useEffect fetchAll guard includes `cachedRoutes === null` check
    - xingran-react-frontend/src/router/DynamicRoutes.tsx: `grep -c 'cachedRoutes === null' src/router/DynamicRoutes.tsx` returns exactly 1 match (inside the useEffect guard, not in any other block)
    - xingran-react-frontend/src/router/routeConfigManager.ts: PATH_SPLIT constant declared at module level (grep returns match)
    - xingran-react-frontend/src/router/routeConfigManager.ts: 3 split() call sites use PATH_SPLIT (grep returns 3 matches in split lines)
    - xingran-react-frontend/src/store/menuStore.ts: menuByPath and menuById fields in MenuState (grep returns match in interface + 1 in set call)
    - xingran-react-frontend/src/store/menuStore.ts: buildMenuIndices helper defined (grep returns match)
    - xingran-react-frontend/src/components/layout/sidebar.utils.ts: findMenuByIdMap exported (grep returns match)
    - npm run type-check exits 0
    - npm run lint exits 0 with no new warnings
  </acceptance_criteria>
  <verify>
    <automated>cd xingran-react-frontend && grep -c "cachedRoutes === null" src/router/DynamicRoutes.tsx && grep -n "PATH_SPLIT" src/router/routeConfigManager.ts && grep -n "menuByPath\|menuById" src/store/menuStore.ts && grep -n "findMenuByIdMap" src/components/layout/sidebar.utils.ts && npm run type-check && npm run lint</automated>
  </verify>
  <done>
    R1 fully resolved: DynamicRoutes only triggers fetchAll when truly needed (cachedRoutes guard). R6 fully resolved: breadcrumb splits use module-level PATH_SPLIT regex. R5+J5 fully resolved: menuByPath/menuById Maps maintained in store; sidebar.utils.ts exports Map-based findMenuByIdMap. Type-check and lint pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: R2+R3 useWidgetPolling singleton visibilitychange + R4 HybridLayout useTabSync + R5 sidebar uses Map lookups</name>
  <files>xingran-react-frontend/src/hooks/useWidgetPolling.ts, xingran-react-frontend/src/components/layout/HybridLayout.tsx, xingran-react-frontend/src/components/layout/sidebar.tsx, xingran-react-frontend/src/hooks/useTabSync.ts</files>
  <read_first>
    - xingran-react-frontend/src/hooks/useWidgetPolling.ts (lines 127-145 main interval useEffect; lines 147-173 visibilitychange useEffect — both register per-instance; line 59-60 intervalRef and isFetchingRef)
    - xingran-react-frontend/src/components/layout/HybridLayout.tsx (lines 6 imports; lines 65-113 effect 1; lines 115-138 effect 2; lines 140-166 effect 3 — all touch pathname or tabsStore)
    - xingran-react-frontend/src/store/tabsStore.ts (find addTab, updateTab, setActiveTab signatures)
    - xingran-react-frontend/src/components/layout/sidebar.tsx (line 82 useMenuStore destructure now uses per-field selectors from Plan 01 C4; the `selectedKeys` useMemo added by Plan 02 M5 calls `findMenuByFullPath(menus, path)` on line 255 — THIS is the Plan 02 M5 site that R5 must modify; line 153 findMenuById — also to be migrated to Map)
  </read_first>
  <action>
    Per R2 + R3 — singleton visibilitychange listener + shared interval ref:

    Edit xingran-react-frontend/src/hooks/useWidgetPolling.ts:
    1. Add module-level state at the top of the file (after imports):
       ```
       type VisibilityCallback = () => void;
       const visibilityListeners = new Set<VisibilityCallback>();
       let visibilityListenerInstalled = false;
       ```
    2. Add module-level helpers (above the export function):
       ```
       function installVisibilityListener() {
         if (visibilityListenerInstalled) return;
         visibilityListenerInstalled = true;
         document.addEventListener('visibilitychange', () => {
           visibilityListeners.forEach(cb => {
             try { cb(); } catch (e) { console.error('[Polling visibility callback]', e); }
           });
         });
       }

       function registerVisibilityCallback(cb: VisibilityCallback): () => void {
         installVisibilityListener();
         visibilityListeners.add(cb);
         return () => visibilityListeners.delete(cb);
       }
       ```
    3. Replace the visibilitychange useEffect (lines 147-173) with:
       ```
       useEffect(() => {
         const callback = () => {
           if (document.hidden) {
             if (intervalRef.current) {
               clearInterval(intervalRef.current);
               intervalRef.current = null;
             }
           } else if (enabled && !isPaused && widgetIdsRef.current.length > 0) {
             // Reuse the same interval ref — do NOT create a new one
             fetchData();
             if (!intervalRef.current) {
               const intervalMs = Math.max(interval, 30) * 1000;
               intervalRef.current = setInterval(() => {
                 fetchData();
               }, intervalMs);
             }
           }
         };
         const unregister = registerVisibilityCallback(callback);
         return unregister;
       }, [enabled, isPaused, interval, fetchData]);
       ```
       Key changes:
       - Module-level singleton listener installed once via installVisibilityListener()
       - Each hook instance registers/unregisters via the Set
       - On visibility restore, check `intervalRef.current` before creating a new interval — reuses the existing one if it survived (e.g., cleared on hide but we restore by re-creating with the same ref — wait, the original code cleared it on hide too, so the visibility-restore must create a new one. The improvement is: don't double-create if the main effect already has it).
    4. Remove the deps on `widgetIds` — already removed in Wave 1 (widgetIdsRef pattern). Verify: fetchData is still stable after Wave 1's M3 refactor.

    Per R4 — HybridLayout useTabSync consolidation (extract to NEW hook file `src/hooks/useTabSync.ts`):

    Create new file xingran-react-frontend/src/hooks/useTabSync.ts:
    ```
    /**
     * useTabSync(pathname) — consolidates the 3 HybridLayout useEffects that all
     * touch pathname / tabsStore. Extracted from HybridLayout.tsx for reuse and
     * to keep the layout component focused on layout concerns.
     */
    import { useEffect, useCallback } from 'react';
    // imports for routeConfigManager, useTabs, useDashboardStore, etc. — match exactly
    // what HybridLayout.tsx currently uses internally.

    export function useTabSync(pathname: string) {
      // [BODY] — paste the 3 useEffect blocks from HybridLayout.tsx lines 65-166 here
      // (pathname-driven, mount-once, route-config-ready).
    }
    ```

    Edit xingran-react-frontend/src/components/layout/HybridLayout.tsx:
    1. Add import at the top: `import { useTabSync } from '@/hooks/useTabSync';` (place near other hook imports).
    2. Delete the 3 useEffect blocks in HybridLayout (lines 65-166) and any internal helper variables (e.g., getDashboardTitle) that are now inside useTabSync.
    3. Inside the HybridLayout function body, add a single call: `useTabSync(location.pathname);` — this replaces the 3 effects.
    4. Remove the now-unused getDashboardTitle and direct useTabs/useDashboardStore imports — useTabSync encapsulates them. The component body shrinks significantly.
    5. The component still imports useTabs/useDashboardStore/useTabsStore only if needed for the TabBar/Header (it does not — those are children). Confirm by reading the JSX at lines 168-196 — only Sidebar, Header, TabBar, children are rendered. None need the stores directly.

    Per R5 — sidebar uses Map lookups (modify the Plan 02 M5 selectedKeys useMemo in place):

    Edit xingran-react-frontend/src/components/layout/sidebar.tsx:
    1. Add a selector for menuByPath next to the existing per-field selectors from Plan 01 C4: `const menuByPath = useMenuStore(s => s.menuByPath);` — place next to the other `useMenuStore(s => ...)` calls.
    2. Add a selector for menuById: `const menuById = useMenuStore(s => s.menuById);`.
    3. Replace `findMenuById(menus, e.key)` (line 153) with `menuById.get(e.key) ?? null`.
    4. **Modify the EXISTING `selectedKeys` useMemo that Plan 02 (M5) added** in this file. Do NOT add a new useMemo. Inside that useMemo, replace the body call `findMenuByFullPath(menus, path)` with `menuByPath.get(path)?.id ?? null`. Change the deps from `[location.pathname, menuPathMap, menus]` to `[location.pathname, menuByPath]`.
       The updated useMemo should look like:
       ```
       const selectedKeys = useMemo<string[]>(() => {
         if (!location.pathname) return [];

         const pathsToTry = [
           location.pathname,
           location.pathname.startsWith('/') ? location.pathname.slice(1) : location.pathname,
         ];

         for (const path of pathsToTry) {
           const menuInfo = menuByPath.get(path);
           if (menuInfo) {
             return [menuInfo.id];
           }
         }

         return [];
       }, [location.pathname, menuByPath]);
       ```
    5. The recursive findMenuByFullPath in sidebar.utils.ts can stay as fallback (used elsewhere if any). The sidebar.tsx selectedKeys memo now uses the Map. After this change, `findMenuByFullPath` should have ZERO callers in `sidebar.tsx` (it was the only consumer — the recursive helper is retained for tests / future callers).

    Acceptance assertions:
    - grep visibilityListeners useWidgetPolling.ts returns matches
    - grep useTabSync HybridLayout.tsx returns the call site + import
    - grep useTabSync src/hooks/useTabSync.ts returns the hook definition + 3 useEffect bodies
    - grep menuById.get sidebar.tsx returns match (new caller)
    - grep menuByPath.get sidebar.tsx returns match (inside the Plan 02 M5 useMemo)
    - grep findMenuByFullPath\\( sidebar.tsx returns 0 matches (recursive fn no longer used in sidebar.tsx)
  </action>
  <acceptance_criteria>
    - xingran-react-frontend/src/hooks/useWidgetPolling.ts: visibilityListeners Set at module level (grep returns match)
    - xingran-react-frontend/src/hooks/useWidgetPolling.ts: installVisibilityListener function defined (grep returns match)
    - xingran-react-frontend/src/hooks/useWidgetPolling.ts: visibility useEffect uses registerVisibilityCallback (grep returns match)
    - xingran-react-frontend/src/hooks/useWidgetPolling.ts: visibility restore reuses intervalRef before creating new interval
    - xingran-react-frontend/src/hooks/useTabSync.ts file exists (NEW)
    - xingran-react-frontend/src/hooks/useTabSync.ts: exports useTabSync function
    - xingran-react-frontend/src/hooks/useTabSync.ts: contains all 3 useEffect bodies moved from HybridLayout
    - xingran-react-frontend/src/components/layout/HybridLayout.tsx: imports useTabSync from '@/hooks/useTabSync'
    - xingran-react-frontend/src/components/layout/HybridLayout.tsx: HybridLayout body uses useTabSync(location.pathname) — single call replaces 3 effects (grep returns match)
    - xingran-react-frontend/src/components/layout/HybridLayout.tsx: the 3 original useEffect blocks are REMOVED
    - xingran-react-frontend/src/components/layout/sidebar.tsx: menuById selector added (grep returns match)
    - xingran-react-frontend/src/components/layout/sidebar.tsx: handleMenuClick uses menuById.get (grep returns match)
    - xingran-react-frontend/src/components/layout/sidebar.tsx: the Plan 02 M5 selectedKeys useMemo now reads `menuByPath.get(path)?.id` (grep returns match)
    - xingran-react-frontend/src/components/layout/sidebar.tsx: the Plan 02 M5 selectedKeys useMemo deps are exactly `[location.pathname, menuByPath]` (no `menus`, no `menuPathMap`)
    - xingran-react-frontend/src/components/layout/sidebar.tsx: ZERO calls to `findMenuByFullPath(` remain (grep returns 0 matches) — enumerated and migrated
    - npm run type-check exits 0
    - npm run lint exits 0 with no new warnings
  </acceptance_criteria>
  <verify>
    <automated>cd xingran-react-frontend && grep -n "visibilityListeners\|installVisibilityListener" src/hooks/useWidgetPolling.ts && ls src/hooks/useTabSync.ts && grep -n "useTabSync" src/components/layout/HybridLayout.tsx src/hooks/useTabSync.ts && grep -n "menuById.get\|menuByPath.get" src/components/layout/sidebar.tsx && grep -c "findMenuByFullPath(" src/components/layout/sidebar.tsx && npm run type-check && npm run lint</automated>
  </verify>
  <done>
    R2 fully resolved: useWidgetPolling has a singleton visibilitychange listener; multiple hook instances share it. R3 fully resolved: interval ref reused on visibility restore — no more drift. R4 fully resolved: HybridLayout has 3 effects consolidated into useTabSync hook in src/hooks/useTabSync.ts. R5 fully resolved: sidebar menu lookups are O(1) via Map (the Plan 02 M5 selectedKeys useMemo is the site that was modified). Type-check and lint pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new attack surface. All changes are frontend internal refactors.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-W3-SC | (n/a) | (frontend refactor) | accept | No new endpoints, no auth changes, no data exfiltration. type-check + lint are safety net. |
</threat_model>

<verification>
- [ ] npm run type-check exits 0
- [ ] npm run lint exits 0 with no new warnings
- [ ] npm run build exits 0
- [ ] grep -c "findMenuByFullPath\\(" xingran-react-frontend/src/components/layout/sidebar.tsx returns 0 (old recursive fn replaced by Map)
- [ ] grep -c "findMenuById\\(" xingran-react-frontend/src/components/layout/sidebar.tsx returns 0 (old recursive fn replaced by Map)
- [ ] Before editing, run `grep -n 'findMenuByFullPath(' xingran-react-frontend/src/components/layout/sidebar.tsx` to enumerate every call site; the Plan 02 M5 selectedKeys useMemo (the only consumer in this file) MUST be migrated to `menuByPath.get(path)?.id`. Any additional call sites found must also be migrated in this task.
- [ ] Before editing, run `grep -n 'findMenuById(' xingran-react-frontend/src/components/layout/sidebar.tsx` to enumerate every call site; line 153 (`findMenuById(menus, e.key)`) MUST be migrated to `menuById.get(e.key)`. Any additional call sites found must also be migrated.
- [ ] Manual smoke: hard-refresh /dashboard — InitializingFallback should NOT show if menus are cached
- [ ] Manual smoke: open multiple dashboard pages with polling — DevTools Event Listeners should show 1 visibilitychange listener, not N
- [ ] Manual smoke: click sidebar items — selectedKeys highlights correctly via Map lookup
</verification>

<success_criteria>
- All 7 MEDIUM defects (R1, R2, R3, R4, R5, R6, R8) are fixed
- DynamicRoutes does not block first-paint on cached menu data
- visibilitychange listener is a singleton (one global listener regardless of hook instances)
- HybridLayout has 1 useTabSync call instead of 3 effects
- Sidebar menu lookups are O(1) Map.get (no recursion) — Plan 02 M5 selectedKeys useMemo is the migrated site
- Breadcrumb path splits use module-level regex
- type-check, lint, and full build all pass
</success_criteria>

<output>
Create `.planning/phases/33-vercel-react-best-practices-20260613-26/33-03-SUMMARY.md` when done
</output>
