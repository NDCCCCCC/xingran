---
phase: 33-vercel-react-best-practices-20260613-26
plan: 02
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - xingran-react-frontend/src/hooks/useTableManager.ts
  - xingran-react-frontend/src/components/shared/ExcelImport.tsx
  - xingran-react-frontend/src/pages/operations/workstations/index.tsx
  - xingran-react-frontend/src/pages/operations/buildings/index.tsx
  - xingran-react-frontend/src/components/layout/sidebar.tsx
  - xingran-react-frontend/src/utils/buildSearchParams.ts
autonomous: true
requirements:
  - M1
  - M2
  - M3
  - M4
  - M5

must_haves:
  truths:
    - "useTableManager loadData callback deps no longer include current/pageSize; uses ref to read latest values"
    - "ExcelImport.handleDownloadTemplate uses api.get(url, { responseType: 'blob' }) not raw fetch — token refresh interceptor now applies"
    - "WorkstationEditModal onOk is a useCallback in the parent with stable deps so modal does not re-render on parent state changes"
    - "buildSearchParams utility extracted; called from buildings + workstations handleSearch/handleReset/handleRefresh/onChange — eliminates 3x duplication"
    - "sidebar.tsx getSelectedKeys is wrapped in useMemo([location.pathname, menuPathMap]) so it returns a stable array reference per route"
  artifacts:
    - path: "xingran-react-frontend/src/hooks/useTableManager.ts"
      provides: "loadData with ref-based current/pageSize"
      contains: "currentRef"
    - path: "xingran-react-frontend/src/components/shared/ExcelImport.tsx"
      provides: "api.get with blob responseType for template download"
      contains: "responseType: 'blob'"
    - path: "xingran-react-frontend/src/pages/operations/workstations/index.tsx"
      provides: "handleWorkstationModalOk useCallback with stable deps"
      contains: "handleWorkstationModalOk"
    - path: "xingran-react-frontend/src/utils/buildSearchParams.ts"
      provides: "buildSearchParams(searchForm, deptId, page) utility"
      contains: "export function buildSearchParams"
    - path: "xingran-react-frontend/src/components/layout/sidebar.tsx"
      provides: "useMemo wrapping getSelectedKeys"
      contains: "useMemo"
  key_links:
    - from: "xingran-react-frontend/src/hooks/useTableManager.ts"
      to: "xingran-react-frontend/src/hooks/useTableManager.ts"
      via: "currentRef/pageSizeRef pattern read inside loadData body"
      pattern: "currentRef\\.current"
    - from: "xingran-react-frontend/src/components/shared/ExcelImport.tsx"
      to: "xingran-react-frontend/src/lib/api.ts"
      via: "api.get(templateUrl, { responseType: 'blob' }) — TokenManager 401 retry now applies"
      pattern: "api\\.get"
    - from: "xingran-react-frontend/src/pages/operations/buildings/index.tsx"
      to: "xingran-react-frontend/src/utils/buildSearchParams.ts"
      via: "buildSearchParams({ searchForm, deptId, page })"
      pattern: "buildSearchParams"
    - from: "xingran-react-frontend/src/components/layout/sidebar.tsx"
      to: "xingran-react-frontend/src/components/layout/sidebar.tsx"
      via: "useMemo wrapping getSelectedKeys with [location.pathname, menuPathMap] deps"
      pattern: "selectedKeys"
---

<objective>
Wave 2 — fix 5 MEDIUM-HIGH performance defects (M1-M5) from the audit. These items do not break correctness but inflate re-render cost or break interceptor contracts. M1 stabilizes useTableManager callbacks; M2 fixes Excel download to use the axios interceptor (401 auto-retry); M3 extracts Modal onOk to useCallback; M4 deduplicates the search-form-merge pattern across buildings/workstations; M5 stabilizes sidebar selectedKeys reference.

Purpose: After Wave 1 (CRITICAL fixes) is verified, Wave 2 addresses the next-tier perf issues. M2 also restores the 401-token-refresh behavior for Excel template downloads — currently token expiry silently fails the download.

**Prior wave changes affecting this plan (Wave 1, Plan 01):**
- `pages/operations/workstations/index.tsx` (C6) — `useRef` added to React imports, `handleEditRef`/`handleDeleteRef` declared with synchronous `.current` assignment, and the `columns` line is now wrapped in `useMemo(() => getWorkstationColumns({ handleEdit: (r) => handleEditRef.current(r), handleDelete: (id) => handleDeleteRef.current(id) }), [])`. Do NOT touch this `columns` useMemo or the refs in M3 — only add the new `handleWorkstationModalOk` useCallback nearby.
- `components/layout/sidebar.tsx` (C4) — `useMenuStore` is now consumed via per-field selectors (`useMenuStore(s => s.menus)`, etc.) instead of a single destructure. The `menuPathMap` useMemo deps are now `[menus]`, not `JSON.stringify(menus)`. M5 builds on this by adding a `selectedKeys` useMemo adjacent to the existing `menuPathMap` memo.
- `hooks/useWidgetPolling.ts` (C3) — already migrated to a `widgetIdsRef` + `widgetDataCache.get(id)` pattern. M1 (useTableManager) follows the same `useRef` style so the codebase stays consistent.

Output:
- 1 new utility file (utils/buildSearchParams.ts)
- 5 production files modified
- 0 test files (defects are observable at runtime)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-vercel-react-best-practices-20260613-26/CONTEXT.md
@.planning/phases/33-vercel-react-best-practices-20260613-26/33-01-PLAN.md
@.planning/reviews/20260613-frontend-react-best-practices-review.md

# Source-of-truth files to consult before editing
@xingran-react-frontend/src/hooks/useTableManager.ts
@xingran-react-frontend/src/components/shared/ExcelImport.tsx
@xingran-react-frontend/src/lib/api.ts
@xingran-react-frontend/src/pages/operations/workstations/index.tsx
@xingran-react-frontend/src/pages/operations/workstations/modals/WorkstationEditModal.tsx
@xingran-react-frontend/src/pages/operations/buildings/index.tsx
@xingran-react-frontend/src/components/layout/sidebar.tsx
@xingran-react-frontend/src/components/layout/sidebar.utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: M1 useTableManager ref-based deps + M2 Excel api.get + M5 sidebar useMemo selectedKeys</name>
  <files>xingran-react-frontend/src/hooks/useTableManager.ts, xingran-react-frontend/src/components/shared/ExcelImport.tsx, xingran-react-frontend/src/components/layout/sidebar.tsx</files>
  <read_first>
    - xingran-react-frontend/src/hooks/useTableManager.ts (lines 73-76: current/pageSize derivation; lines 85-88: loadFunctionRef/externalPaginationRef pattern; lines 90-107: loadData with [current, pageSize] deps)
    - xingran-react-frontend/src/components/shared/ExcelImport.tsx (lines 24-25 imports; lines 66-81 handleDownloadTemplate fetch)
    - xingran-react-frontend/src/lib/api.ts (lines 467-482: get/post/put/del wrappers; line 521: api export)
    - xingran-react-frontend/src/components/layout/sidebar.tsx (lines 1 imports; lines 244-261 getSelectedKeys; line 339 Menu selectedKeys)
  </read_first>
  <action>
    Per M1 — stabilize useTableManager.loadData via refs:

    Edit xingran-react-frontend/src/hooks/useTableManager.ts:
    1. Add two refs after the existing loadFunctionRef/externalPaginationRef block (around line 87):
       ```
       const currentRef = useRef(current);
       const pageSizeRef = useRef(pageSize);
       currentRef.current = current;
       pageSizeRef.current = pageSize;
       ```
       The synchronous body-assignment ensures the ref always holds the latest values during render (effect-based assignment lags by one render).
    2. Change loadData (lines 90-107) deps from [current, pageSize] to []. Inside the body, replace `current` and `pageSize` reads (lines 94-95) with `currentRef.current` and `pageSizeRef.current`:
       ```
       const loadData = useCallback(async (params: Record<string, unknown> = {}) => {
         setLoading(true);
         try {
           const requestParams = {
             current: params.current ?? currentRef.current,
             pageSize: params.pageSize ?? pageSizeRef.current,
             ...params,
           };

           const result = await loadFunctionRef.current(requestParams);
           setData(result.list ?? []);
           const newTotal = result.total ?? 0;
           setTotal(newTotal);
           externalPaginationRef.current?.setTotal?.(newTotal);
         } finally {
           setLoading(false);
         }
       }, []);
       ```
    3. Verify: loadData is now reference-stable. handleSearch/handleReset/handleRefresh (lines 109-135) all have loadData in their deps but loadData no longer changes, so they will also become reference-stable.

    Per M2 — route Excel template download through axios:

    Edit xingran-react-frontend/src/components/shared/ExcelImport.tsx:
    1. Add import: `import { api } from '@/lib/api';` at the top with other imports (around line 14-24).
    2. Remove the `import { getAuthHeaders } from '@/utils/authHelpers';` if it is no longer used elsewhere — actually it IS still used by customRequest (line 173) so keep it.
    3. Use the FINAL handleDownloadTemplate (Step 5 below is the only version — Step 3 draft was superseded by Step 5):
       ```
       const handleDownloadTemplate = async () => {
         try {
           const response = await api.get<Blob>(templateUrl, {
             responseType: 'blob',
             timeout: 60000,
           });
           const blob = response.data;
           const filename = `${entityName}_模板.xlsx`;
           await downloadFile(new Response(blob), filename);
           message.success('模板下载成功');
         } catch {
           message.error('下载模板失败');
         }
       };
       ```
       Note: api.get returns AxiosResponse (via api instance), and response.data is Blob when responseType='blob'. The interceptor still runs (token refresh on 401).
    4. Keep downloadFile helper (lines 344-354) unchanged — it accepts a Response.

    Per M5 — memoize sidebar selectedKeys:

    Edit xingran-react-frontend/src/components/layout/sidebar.tsx:
    1. Verify useMemo is already imported (Wave 1 task 3 should have imported it). If not, add `useMemo` to the React import on line 1.
    2. After the menuPathMap useMemo (line 101) and before the buildParentKeyChain function (line 108), add:
       ```
       // 选中菜单 keys（基于当前路由 + 菜单路径映射）
       const selectedKeys = useMemo<string[]>(() => {
         if (!location.pathname) return [];

         const pathsToTry = [
           location.pathname,
           location.pathname.startsWith('/') ? location.pathname.slice(1) : location.pathname,
         ];

         for (const path of pathsToTry) {
           const menuInfo = menuPathMap.get(path);
           if (menuInfo) {
             const menuId = findMenuByFullPath(menus, path);
             if (menuId) return [menuId];
           }
         }

         return [];
       }, [location.pathname, menuPathMap, menus]);
       ```
    3. Replace `<Menu selectedKeys={getSelectedKeys()}>` (line 339) with `<Menu selectedKeys={selectedKeys}>`.
    4. Leave the getSelectedKeys function (lines 244-261) in place OR remove it — removing it is cleaner. Remove the unused function entirely.

    Acceptance assertions (verified by grep):
    - grep currentRef useTableManager.ts returns at least 3 matches (2 declarations + 1 usage in loadData)
    - grep pageSizeRef useTableManager.ts returns at least 3 matches (2 declarations + 1 usage in loadData)
    - grep responseType ExcelImport.tsx returns match
    - grep api.get ExcelImport.tsx returns match
    - grep selectedKeys sidebar.tsx returns the new useMemo variable + Menu prop usage
    - grep getSelectedKeys sidebar.tsx returns 0 matches in the function-definition sense (the function is removed)
  </action>
  <acceptance_criteria>
    - xingran-react-frontend/src/hooks/useTableManager.ts: currentRef and pageSizeRef declared with useRef
    - xingran-react-frontend/src/hooks/useTableManager.ts: loadData deps is empty []
    - xingran-react-frontend/src/hooks/useTableManager.ts: loadData body reads currentRef.current and pageSizeRef.current
    - xingran-react-frontend/src/components/shared/ExcelImport.tsx: imports api from '@/lib/api'
    - xingran-react-frontend/src/components/shared/ExcelImport.tsx: handleDownloadTemplate uses api.get(templateUrl, { responseType: 'blob' })
    - xingran-react-frontend/src/components/shared/ExcelImport.tsx: no await fetch( remains in handleDownloadTemplate
    - xingran-react-frontend/src/components/layout/sidebar.tsx: selectedKeys declared with useMemo
    - xingran-react-frontend/src/components/layout/sidebar.tsx: Menu selectedKeys uses the memoized selectedKeys variable
    - xingran-react-frontend/src/components/layout/sidebar.tsx: old getSelectedKeys function definition removed
    - npm run type-check exits 0
    - npm run lint exits 0 with no new warnings
  </acceptance_criteria>
  <verify>
    <automated>cd xingran-react-frontend && grep -n "currentRef\|pageSizeRef" src/hooks/useTableManager.ts && grep -n "api.get.*responseType.*blob" src/components/shared/ExcelImport.tsx && grep -n "selectedKeys" src/components/layout/sidebar.tsx && npm run type-check && npm run lint</automated>
  </verify>
  <done>
    M1 fully resolved: useTableManager loadData is stable across current/pageSize changes (ref-based read). M2 fully resolved: Excel template download goes through axios interceptor (401 auto-retry applies). M5 fully resolved: sidebar selectedKeys is stable per route. Type-check and lint pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: M3 extract Modal onOk useCallback + M4 buildSearchParams utility</name>
  <files>xingran-react-frontend/src/pages/operations/workstations/index.tsx, xingran-react-frontend/src/pages/operations/buildings/index.tsx, xingran-react-frontend/src/utils/buildSearchParams.ts</files>
  <read_first>
    - xingran-react-frontend/src/pages/operations/workstations/index.tsx (lines 192-201: handleSuccess and columns; lines 600-623: inline onOk on WorkstationEditModal)
    - xingran-react-frontend/src/pages/operations/workstations/modals/WorkstationEditModal.tsx (find onOk prop type — should accept (values: any) => Promise<void> | void)
    - xingran-react-frontend/src/pages/operations/buildings/index.tsx (lines 86-135: handleDeptSelect, handleSearch, handleResetWithDept, handleRefresh; lines 396-456: Table onChange and export onClick — 5+ duplicate patterns)
    - xingran-react-frontend/src/utils/ (ls to confirm no existing buildSearchParams)
    - **CROSS-WAVE NOTE (Plan 01 Wave 1, C6)**: Plan 01 already added `useRef` to the React imports, declared `handleEditRef`/`handleDeleteRef` with the `.current` sync block, and wrapped the `columns` assignment in `useMemo(() => getWorkstationColumns({...}), [])`. This task inserts `handleWorkstationModalOk` AFTER `handleSuccess` (around line 192), NOT relative to the `columns` line — the anchor is `handleSuccess`, not `columns`. Do NOT touch the `columns` useMemo or refs that Plan 01 created; only add the new useCallback next to `handleSuccess`.
  </read_first>
  <action>
    Per M3 — extract Modal onOk:

    Edit xingran-react-frontend/src/pages/operations/workstations/index.tsx:
    1. Inside WorkstationManagement function, BEFORE the JSX return statement, add a handleWorkstationModalOk useCallback. Position it right after handleSuccess (line 192-194) — NOT next to the `columns` useMemo:
       ```
       const handleWorkstationModalOk = useCallback(async (values: Record<string, unknown>) => {
         try {
           const submitValues = { ...values };
           if (Array.isArray(submitValues.floorId)) {
             submitValues.floorId = submitValues.floorId[submitValues.floorId.length - 1];
           }

           if (editingWorkstation) {
             await workstationApi.update(editingWorkstation.id, submitValues);
             showSuccessMessage('更新');
           } else {
             await workstationApi.create(submitValues);
             showSuccessMessage('创建');
           }
           closeModal(workstationForm);
           setModalVisible(false);
           handleSuccess();
         } catch (error) {
           throw error;
         }
       }, [editingWorkstation, closeModal, workstationForm, setModalVisible, handleSuccess]);
       ```
    2. In the JSX, replace the inline onOk={async (values) => { ... }} block (lines 600-623) with onOk={handleWorkstationModalOk}.
    3. NO changes needed in WorkstationEditModal.tsx — the onOk prop signature already accepts a function.
    4. Do NOT modify the `columns` useMemo, `handleEditRef`, or `handleDeleteRef` added by Plan 01 (C6) — those are unchanged.

    Per M4 — extract buildSearchParams utility:

    Create new file xingran-react-frontend/src/utils/buildSearchParams.ts with this content:
    ```
    /**
     * 合并搜索表单 + 分页 + 部门筛选的通用工具
     * 用于消除 buildings/workstations 中重复的"表单值过滤 + 部门ID注入 + 分页合并"逻辑
     */
    import type { FormInstance } from 'antd';

    export interface BuildSearchParamsOptions {
      searchForm?: FormInstance<unknown>;
      deptId?: string;
      page?: { current?: number; pageSize?: number };
    }

    /**
     * 构建搜索参数：
     * 1. 从 searchForm 提取非空字段
     * 2. 注入 deptId（如果提供且非空）
     * 3. 注入分页参数（如果提供）
     */
    export function buildSearchParams(opts: BuildSearchParamsOptions): Record<string, unknown> {
      const { searchForm, deptId, page } = opts;
      const params: Record<string, unknown> = {};

      if (searchForm) {
        const values = searchForm.getFieldsValue() as Record<string, unknown>;
        Object.keys(values).forEach(key => {
          const value = values[key];
          if (value !== undefined && value !== null && value !== '') {
            params[key] = value;
          }
        });
      }

      if (deptId) {
        params.orgId = deptId;
      }

      if (page) {
        if (page.current !== undefined) params.current = page.current;
        if (page.pageSize !== undefined) params.pageSize = page.pageSize;
      }

      return params;
    }
    ```

    Edit xingran-react-frontend/src/pages/operations/buildings/index.tsx:
    1. Add import: import { buildSearchParams } from '@/utils/buildSearchParams'; (place near other utility imports around line 18-21).
    2. Replace handleSearch (lines 99-113):
       ```
       const handleSearch = useCallback(() => {
         const searchParams = buildSearchParams({
           searchForm,
           deptId: selectedDeptId || undefined,
         });
         loadBuildings(searchParams);
         loadStatistics(searchParams);
       }, [searchForm, selectedDeptId, loadBuildings, loadStatistics]);
       ```
    3. Replace handleResetWithDept (lines 116-124):
       ```
       const handleResetWithDept = useCallback(() => {
         searchForm.resetFields();
         const searchParams = buildSearchParams({ deptId: selectedDeptId || undefined });
         loadBuildings(searchParams);
         loadStatistics(searchParams);
       }, [searchForm, selectedDeptId, loadBuildings, loadStatistics]);
       ```
    4. Replace handleRefresh (lines 127-135):
       ```
       const handleRefresh = useCallback(() => {
         const searchParams = buildSearchParams({ deptId: selectedDeptId || undefined });
         loadBuildings(searchParams);
         loadStatistics(searchParams);
       }, [selectedDeptId, loadBuildings, loadStatistics]);
       ```
    5. Replace the inline onChange handler (lines 439-456):
       ```
       onChange={(pagination) => {
         const newPage = pagination.current || 1;
         const newPageSize = pagination.pageSize || 10;
         setCurrent(newPage);
         setPageSize(newPageSize);
         const searchParams = buildSearchParams({
           searchForm,
           deptId: selectedDeptId || undefined,
           page: { current: newPage, pageSize: newPageSize },
         });
         loadBuildings(searchParams);
       }}
       ```
    6. Replace the export button onClick (lines 408-423):
       ```
       onClick={() => {
         setExportFilters(buildSearchParams({
           searchForm,
           deptId: selectedDeptId || undefined,
         }));
         setExportVisible(true);
       }}
       ```

    Edit xingran-react-frontend/src/pages/operations/workstations/index.tsx:
    1. Add import: import { buildSearchParams } from '@/utils/buildSearchParams'; (place near other utility imports).
    2. Replace the inline Table onChange pagination handler (lines 396-413):
       ```
       onChange={(pagination) => {
         const newPage = pagination.current || 1;
         const newPageSize = pagination.pageSize || 10;
         setCurrent(newPage);
         setPageSize(newPageSize);
         loadWorkstations(buildSearchParams({
           searchForm,
           page: { current: newPage, pageSize: newPageSize },
         }));
       }}
       ```
       Workstations does NOT inject deptId here — the dept filter is applied separately via selectedDeptId in the useTableManager call.
    3. Replace the export button onClick (lines 519-531):
       ```
       onClick={() => {
         setExportFilters(buildSearchParams({ searchForm }));
         setExportVisible(true);
       }}
       ```
  </action>
  <acceptance_criteria>
    - xingran-react-frontend/src/utils/buildSearchParams.ts file exists
    - xingran-react-frontend/src/utils/buildSearchParams.ts: exports buildSearchParams function
    - xingran-react-frontend/src/pages/operations/buildings/index.tsx: imports buildSearchParams
    - xingran-react-frontend/src/pages/operations/buildings/index.tsx: handleSearch/handleResetWithDept/handleRefresh all use buildSearchParams
    - xingran-react-frontend/src/pages/operations/buildings/index.tsx: Table onChange uses buildSearchParams
    - xingran-react-frontend/src/pages/operations/buildings/index.tsx: export button uses buildSearchParams
    - xingran-react-frontend/src/pages/operations/buildings/index.tsx: literal Object.keys(values).forEach pattern no longer in page file
    - xingran-react-frontend/src/pages/operations/workstations/index.tsx: imports buildSearchParams
    - xingran-react-frontend/src/pages/operations/workstations/index.tsx: Table onChange uses buildSearchParams
    - xingran-react-frontend/src/pages/operations/workstations/index.tsx: export button uses buildSearchParams
    - xingran-react-frontend/src/pages/operations/workstations/index.tsx: handleWorkstationModalOk declared as useCallback
    - xingran-react-frontend/src/pages/operations/workstations/index.tsx: WorkstationEditModal onOk prop uses handleWorkstationModalOk
    - xingran-react-frontend/src/pages/operations/workstations/index.tsx: the columns useMemo + handleEditRef/handleDeleteRef from Plan 01 (C6) remain UNCHANGED — verify by reading the file
    - npm run type-check exits 0
    - npm run lint exits 0 with no new warnings
  </acceptance_criteria>
  <verify>
    <automated>cd xingran-react-frontend && ls src/utils/buildSearchParams.ts && grep -rn "buildSearchParams" src/ | head -10 && grep -n "handleWorkstationModalOk" src/pages/operations/workstations/index.tsx && grep -n "handleEditRef\|handleDeleteRef" src/pages/operations/workstations/index.tsx && npm run type-check && npm run lint</automated>
  </verify>
  <done>
    M3 fully resolved: WorkstationEditModal onOk is a stable useCallback in the parent — modal no longer re-renders on every parent state change. The Plan 01 C6 columns useMemo + refs are preserved unchanged. M4 fully resolved: buildSearchParams utility extracted, used in 5+ call sites across buildings and workstations. Type-check and lint pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → axios interceptor | Excel template download now flows through api.get — benefits from 401 token-refresh (M2). No new entry points. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-W2-01 | Tampering | ExcelImport.tsx | mitigate | M2 routes download through api.get(templateUrl, { responseType: 'blob' }) so TokenManager 401-retry applies. No new auth surface. |
| T-33-W2-SC | (n/a) | (frontend refactor) | accept | No new dependencies. type-check + lint are safety net. |
</threat_model>

<verification>
- [ ] npm run type-check exits 0
- [ ] npm run lint exits 0 with no new warnings
- [ ] npm run build exits 0
- [ ] grep -c "Object.keys(values).forEach" xingran-react-frontend/src/pages/operations/buildings/index.tsx returns 0
- [ ] grep -c "Object.keys(formValues).forEach" xingran-react-frontend/src/pages/operations/workstations/index.tsx returns 0
- [ ] grep -c "handleEditRef" xingran-react-frontend/src/pages/operations/workstations/index.tsx returns 2 (declaration + useMemo body) — confirms Plan 01 C6 is preserved
- [ ] Manual smoke: in /operations/workstations, open Edit modal, change a field, save — modal closes, table refreshes, no console errors
- [ ] Manual smoke: in /operations/workstations, click Export — export modal opens with current filters applied
</verification>

<success_criteria>
- All 5 MEDIUM-HIGH defects (M1-M5) are fixed
- Excel template download uses axios interceptor (token refresh now applies on 401)
- useTableManager.loadData is reference-stable across current/pageSize churn
- Modal onOk is a stable useCallback (no inline closure)
- buildSearchParams utility centralizes the 5+ duplicate form-merge sites
- Sidebar selectedKeys is a stable array reference
- Plan 01 C6 columns useMemo + refs are preserved unchanged
- type-check, lint, and full build all pass
</success_criteria>

<output>
Create `.planning/phases/33-vercel-react-best-practices-20260613-26/33-02-SUMMARY.md` when done
</output>
