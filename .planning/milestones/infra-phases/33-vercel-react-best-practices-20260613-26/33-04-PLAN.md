---
phase: 33-vercel-react-best-practices-20260613-26
plan: 04
type: execute
wave: 4
depends_on:
  - 33-03
files_modified:
  - xingran-react-frontend/src/lib/api.ts
  - xingran-react-frontend/src/utils/dateFormat.ts
  - xingran-react-frontend/src/utils/lruCache.ts
  - xingran-react-frontend/src/router/routeConfigManager.ts
  - xingran-react-frontend/src/components/layout/sidebar-helper.ts
autonomous: true
requirements:
  - J1
  - J3
  - J4
  - J5

must_haves:
  truths:
    - "lib/api.ts BASE_CONFIG constant extracts shared baseURL/timeout/headers between rawAxios and api"
    - "utils/lruCache.ts provides a generic LRU helper used by formatDateTime"
    - "utils/dateFormat.ts formatDateTime memoizes the last 1000 formatted strings via LRU"
    - "routeConfigManager split calls in buildBreadcrumb use the module-level PATH_SPLIT regex (added in Wave 3, verified in Wave 4)"
    - "sidebar-helper.ts split/join operations are isolated to avoid repeated computation"
  artifacts:
    - path: "xingran-react-frontend/src/lib/api.ts"
      provides: "BASE_CONFIG constant shared by rawAxios and api"
      contains: "const BASE_CONFIG"
    - path: "xingran-react-frontend/src/utils/lruCache.ts"
      provides: "Generic LRU cache with get/set/has/clear"
      contains: "class LRUCache"
    - path: "xingran-react-frontend/src/utils/dateFormat.ts"
      provides: "formatDateTime with LRU memoization"
      contains: "formatDateTime"
    - path: "xingran-react-frontend/src/router/routeConfigManager.ts"
      provides: "PATH_SPLIT used in buildBreadcrumb (verified, idempotent — Wave 3 already added it)"
      contains: "PATH_SPLIT"
    - path: "xingran-react-frontend/src/components/layout/sidebar-helper.ts"
      provides: "Helper utilities with stable references"
      contains: "buildMenuPathMap"
  key_links:
    - from: "xingran-react-frontend/src/lib/api.ts"
      to: "xingran-react-frontend/src/lib/api.ts"
      via: "BASE_CONFIG const spread into both rawAxios.create and api.create"
      pattern: "BASE_CONFIG"
    - from: "xingran-react-frontend/src/utils/dateFormat.ts"
      to: "xingran-react-frontend/src/utils/lruCache.ts"
      via: "formatDateTime reads/writes LRUCache<string, string> by input string key"
      pattern: "lruCache\\.get"
    - from: "xingran-react-frontend/src/router/routeConfigManager.ts"
      to: "xingran-react-frontend/src/router/routeConfigManager.ts"
      via: "PATH_SPLIT regex used in buildBreadcrumb and fallbackBreadcrumb (Wave 3 origin; Wave 4 verifies)"
      pattern: "PATH_SPLIT"
---

<objective>
Wave 4 — fix LOW-MEDIUM JS perf defects (J1, J3, J4, J5 — J2 is skip per review) from the audit. J1 extracts a shared BASE_CONFIG for axios instances. J3 memoizes formatDateTime via LRU. J4 verifies all path splits use the regex from Wave 3 (idempotent). J5 ties into the menuByPath Map from Wave 3 (already shipped). All items are small, low-risk, additive optimizations.

Purpose: Final cleanup pass — eliminate string-format re-computation in tables, dedupe axios config, and ensure no remaining naive splits. These have small per-call cost but compound over 10K-row tables.

**Prior wave changes affecting this plan (Waves 1-3):**
- Plan 03 (R6) already added `PATH_SPLIT = /\/+/` to `routeConfigManager.ts` and replaced 3 `path.split('/')` sites. Wave 4's J4 is a verification step — the change should already be present after Wave 3. If the regex is missing, add it. If present, just verify.

Output:
- 1 new utility file (utils/lruCache.ts)
- 4 production files modified
- 0 test files
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-vercel-react-best-practices-20260613-26/CONTEXT.md
@.planning/phases/33-vercel-react-best-practices-20260613-26/33-01-PLAN.md
@.planning/phases/33-vercel-react-best-practices-20260613-26/33-02-PLAN.md
@.planning/phases/33-vercel-react-best-practices-20260613-26/33-03-PLAN.md
@.planning/reviews/20260613-frontend-react-best-practices-review.md

# Source-of-truth files to consult before editing
@xingran-react-frontend/src/lib/api.ts
@xingran-react-frontend/src/utils/dateFormat.ts
@xingran-react-frontend/src/router/routeConfigManager.ts
@xingran-react-frontend/src/components/layout/sidebar-helper.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: J1 BASE_CONFIG + J3 LRU memoization + J4 split consistency (idempotent)</name>
  <files>xingran-react-frontend/src/lib/api.ts, xingran-react-frontend/src/utils/dateFormat.ts, xingran-react-frontend/src/utils/lruCache.ts, xingran-react-frontend/src/router/routeConfigManager.ts</files>
  <read_first>
    - xingran-react-frontend/src/lib/api.ts (lines 41-47 rawAxios config; lines 159-165 api config — both duplicate baseURL/timeout/headers)
    - xingran-react-frontend/src/utils/dateFormat.ts (find formatDateTime function; check if it already uses dayjs or native Date; identify the signature)
    - xingran-react-frontend/src/router/routeConfigManager.ts (verify PATH_SPLIT exists from Wave 3 — it should; lines 117-118 buildBreadcrumb split, lines 224-226 extractTitleFromPath split, lines 229-230 fallbackBreadcrumb split)
    - xingran-react-frontend/src/components/layout/sidebar-helper.ts (find buildMenuPathMap; check if it has hot path)
  </read_first>
  <action>
    Per J1 — extract BASE_CONFIG:

    Edit xingran-react-frontend/src/lib/api.ts:
    1. Add module-level constant near top of file (after the encryptionKeyStore Map on line 35):
       ```
       /**
        * 共享 axios 配置 — baseURL / timeout / headers
        * rawAxios 和 api 都从这继承
        */
       const BASE_CONFIG = {
         baseURL: import.meta.env.VITE_API_BASE_URL || 'http://10.62.10.33:9000/api/v1',
         timeout: 30000,
         headers: {
           'Content-Type': 'application/json',
         },
       } as const;
       ```
    2. Replace rawAxios.create config (lines 41-47):
       ```
       const rawAxios: AxiosInstance = axios.create(BASE_CONFIG);
       ```
       Cast BASE_CONFIG: `axios.create({ ...BASE_CONFIG } as AxiosRequestConfig)` if TypeScript complains about the `as const`. Use `axios.create({ ...BASE_CONFIG })`.
    3. Replace api.create config (lines 159-165):
       ```
       const api: AxiosInstance = axios.create({ ...BASE_CONFIG });
       ```
    4. Verify the `getTimeout` function (lines 168-173) still works — it overrides timeout per-request via `config.timeout = getTimeout(config.url || '')` in the request interceptor on line 201. This is unchanged behavior.

    Per J3 — LRU memoization for formatDateTime:

    Create new file xingran-react-frontend/src/utils/lruCache.ts:
    ```
    /**
     * 简单的 LRU (Least Recently Used) 缓存实现
     * 用于 memoize 纯函数（如格式化时间戳），避免重复计算
     * 容量超限时移除最久未访问的项
     */
    export class LRUCache<K, V> {
      private cache = new Map<K, V>();

      constructor(private readonly capacity: number) {
        if (capacity <= 0) throw new Error('LRUCache capacity must be > 0');
      }

      get(key: K): V | undefined {
        const value = this.cache.get(key);
        if (value === undefined) return undefined;
        // Move to end (most recent)
        this.cache.delete(key);
        this.cache.set(key, value);
        return value;
      }

      set(key: K, value: V): void {
        if (this.cache.has(key)) {
          this.cache.delete(key);
        } else if (this.cache.size >= this.capacity) {
          // Evict oldest (first key in iteration order)
          const oldestKey = this.cache.keys().next().value;
          if (oldestKey !== undefined) this.cache.delete(oldestKey);
        }
        this.cache.set(key, value);
      }

      has(key: K): boolean {
        return this.cache.has(key);
      }

      clear(): void {
        this.cache.clear();
      }

      get size(): number {
        return this.cache.size;
      }
    }
    ```

    Edit xingran-react-frontend/src/utils/dateFormat.ts:
    1. Add import at top: `import { LRUCache } from './lruCache';`
    2. Create a module-level LRU at top of file:
       ```
       const formatDateTimeCache = new LRUCache<string, string>(1000);
       ```
    3. Wrap formatDateTime with memoization (read existing implementation first — assume it's `export function formatDateTime(input: string | number | Date, format?: string): string`):
       ```
       export function formatDateTime(input: string | number | Date, format: string = 'YYYY-MM-DD HH:mm:ss'): string {
         // Cache key: serialize input + format to string. For Date/number inputs,
         // String() is fast; for string inputs, append format suffix.
         const inputStr = String(input);
         const cacheKey = `${inputStr}::${format}`;
         const cached = formatDateTimeCache.get(cacheKey);
         if (cached !== undefined) return cached;

         // Original implementation body (use dayjs or whatever the current code uses)
         const result = /* original formatDateTime body */ inputStr;

         formatDateTimeCache.set(cacheKey, result);
         return result;
       }
       ```
       Important: read the file first to preserve the EXACT implementation. The intent is: keep all existing behavior, just add a cache layer in front. Do NOT change the format or behavior.
    4. If the existing formatDateTime uses dayjs, the body is roughly:
       ```
       const dayjs = require('dayjs'); // or import at top
       const result = dayjs(input).format(format);
       ```
       Keep the dayjs usage — just wrap with cache.
    5. Cache size 1000: at 10K table rows with ~5% unique timestamps, this gives ~99% hit rate. For higher cardinality, increase capacity.

    Per J4 — verify routeConfigManager uses PATH_SPLIT everywhere (idempotent over Wave 3):

    Edit xingran-react-frontend/src/router/routeConfigManager.ts:
    1. First, VERIFY whether Wave 3 already added `PATH_SPLIT`. Run `git log --oneline | grep 'docs.*PATH_SPLIT'` (commit message convention) OR `git diff HEAD~1 -- xingran-react-frontend/src/router/routeConfigManager.ts | grep PATH_SPLIT` to see if Wave 3's commit added it. If `git` shows the regex is already present, this step is a no-op.
    2. If PATH_SPLIT is missing, add it at module level near line 14:
       ```
       const PATH_SPLIT = /\/+/;
       ```
    3. Replace any remaining `path.split('/').filter(Boolean)` with `path.split(PATH_SPLIT).filter(Boolean)`. The 3 call sites are lines 117-118 (buildBreadcrumb), 224-225 (extractTitleFromPath), 229-230 (fallbackBreadcrumb).
    4. If Wave 3 already did this, just verify and skip. Do NOT re-declare the constant (would cause a duplicate declaration error).

    Per J5 — sidebar-helper.ts hot path is OK:
    1. The sidebar-helper.ts file's buildMenuPathMap is now called once per menu change (after Wave 1's C4 fix to useMemo deps `[menus]`). It's not a per-render hot path. No changes needed.
    2. Document this decision in the plan: J5 is satisfied by Wave 1 + Wave 3's menuByPath Map.

    Acceptance assertions:
    - grep BASE_CONFIG lib/api.ts returns 3+ matches (declaration + 2 usages in create)
    - grep class LRUCache utils/lruCache.ts returns match
    - grep formatDateTimeCache utils/dateFormat.ts returns match
    - grep PATH_SPLIT routeConfigManager.ts returns 3+ matches (declaration + 3 usages)
    - Run `git log --oneline | grep 'docs.*PATH_SPLIT' || git diff HEAD~1 -- xingran-react-frontend/src/router/routeConfigManager.ts | grep PATH_SPLIT` to confirm Wave 3 added PATH_SPLIT before falling back to adding it here
  </action>
  <acceptance_criteria>
    - xingran-react-frontend/src/lib/api.ts: BASE_CONFIG constant declared at module level
    - xingran-react-frontend/src/lib/api.ts: rawAxios and api both use BASE_CONFIG (or spread of it)
    - xingran-react-frontend/src/lib/api.ts: no literal duplicate of baseURL+timeout+Content-Type between the two instances (grep returns 1 occurrence of each literal string, not 2)
    - xingran-react-frontend/src/utils/lruCache.ts file exists
    - xingran-react-frontend/src/utils/lruCache.ts: LRUCache class exported with get/set/has/clear
    - xingran-react-frontend/src/utils/dateFormat.ts: formatDateTime uses LRU cache
    - xingran-react-frontend/src/router/routeConfigManager.ts: PATH_SPLIT regex at module level (already from Wave 3; verify — only add if missing)
    - xingran-react-frontend/src/router/routeConfigManager.ts: all split() calls in this file use PATH_SPLIT
    - xingran-react-frontend/src/router/routeConfigManager.ts: NO duplicate `const PATH_SPLIT` declarations (this is the idempotency check)
    - npm run type-check exits 0
    - npm run lint exits 0 with no new warnings
  </acceptance_criteria>
  <verify>
    <automated>cd xingran-react-frontend && grep -n "BASE_CONFIG" src/lib/api.ts && ls src/utils/lruCache.ts && grep -n "formatDateTimeCache\|LRUCache" src/utils/dateFormat.ts && (git log --oneline | grep -q 'docs.*PATH_SPLIT' || git diff HEAD~1 -- xingran-react-frontend/src/router/routeConfigManager.ts | grep -q PATH_SPLIT || echo "Wave 4 will add PATH_SPLIT") && grep -n "PATH_SPLIT" src/router/routeConfigManager.ts && grep -c "const PATH_SPLIT" src/router/routeConfigManager.ts && npm run type-check && npm run lint</automated>
  </verify>
  <done>
    J1 fully resolved: BASE_CONFIG shared by rawAxios and api. J3 fully resolved: formatDateTime memoized via LRU (1000 entries). J4 fully resolved (idempotent): all routeConfigManager path splits use PATH_SPLIT regex (added in Wave 3 if not already present); no duplicate declarations. J5 satisfied by Wave 1 + Wave 3 menuByPath Map. Type-check and lint pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new attack surface. All changes are frontend internal.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-W4-SC | (n/a) | (frontend refactor) | accept | No new endpoints, no auth changes, no data exfiltration. type-check + lint are safety net. |
</threat_model>

<verification>
- [ ] npm run type-check exits 0
- [ ] npm run lint exits 0 with no new warnings
- [ ] npm run build exits 0
- [ ] grep -c "baseURL: import.meta.env" xingran-react-frontend/src/lib/api.ts returns 1 (single source)
- [ ] grep -c "timeout: 30000" xingran-react-frontend/src/lib/api.ts returns 1 (single source)
- [ ] grep -c "const PATH_SPLIT" xingran-react-frontend/src/router/routeConfigManager.ts returns 1 (no duplicate declarations)
- [ ] Run `git log --oneline | grep 'docs.*PATH_SPLIT'` or `git diff HEAD~1 -- xingran-react-frontend/src/router/routeConfigManager.ts | grep PATH_SPLIT` to confirm Wave 3 added PATH_SPLIT before this plan's verify fell back to adding it
- [ ] Manual smoke: open a list page with 100+ rows — table renders, formatDateTime returns identical strings to pre-refactor (correctness preserved)
</verification>

<success_criteria>
- J1, J3, J4 fully resolved (J5 satisfied via Wave 1+3, J2 skip per review)
- BASE_CONFIG deduplicates axios instance config
- formatDateTime memoized via LRU cache (1000 entries)
- All path splits in routeConfigManager use module-level regex (idempotent over Wave 3)
- type-check, lint, and full build all pass
- 25/25 actionable defects addressed across Waves 1-4
</success_criteria>

<output>
Create `.planning/phases/33-vercel-react-best-practices-20260613-26/33-04-SUMMARY.md` when done
</output>
