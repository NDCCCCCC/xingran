---
phase: 33-vercel-react-best-practices-20260613-26
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - xingran-react-frontend/src/store/authStore.ts
  - xingran-react-frontend/src/pages/login/index.tsx
  - xingran-react-frontend/src/hooks/useRealtimeUpdates.ts
  - xingran-react-frontend/src/hooks/useWidgetPolling.ts
  - xingran-react-frontend/src/components/layout/sidebar.tsx
  - xingran-react-frontend/src/pages/operations/buildings/useDepartmentData.ts
  - xingran-react-frontend/src/pages/operations/workstations/columns.tsx
  - xingran-react-frontend/src/pages/operations/workstations/index.tsx
  - xingran-react-frontend/src/components/layout/header.tsx
  - xingran-react-frontend/src/pages/operations/buildings/index.tsx
autonomous: true
requirements:
  - C1
  - C2
  - C3
  - C4
  - C5
  - C6
  - C7
  - R7

must_haves:
  truths:
    - "Login flow no longer triggers duplicate menu/permission API calls (authStore.login does NOT call loadMenusAfterLogin internally; login page calls fetchMenus + fetchPermissions in single Promise.all)"
    - "useRealtimeUpdates connect effect depends only on options?.enabled; widgets array reads via widgetsRef.current so reference churn does not cause reconnection"
    - "useWidgetPolling reads cache via useDashboardStore.getState().widgetDataCache Map.get(id) instead of O(n) array.find"
    - "sidebar.tsx menuPathMap useMemo dependency is [menus] (zustand selector with stable ref), NOT JSON.stringify(menus)"
    - "useDepartmentData builds deptMap once via useMemo([departments]); getOrgName reads Map.get(id) without rebuilding"
    - "getWorkstationColumns is called inside useMemo with [] deps; handleEdit/Delete passed via useRef that updates .current each render"
    - "header.tsx inline style objects are hoisted to module-level constants (HEADER_STYLE, AVATAR_STYLE, etc.)"
    - "buildings/index.tsx card view line 355 displays '停用' instead of literal '1' when status !== 0"
  artifacts:
    - path: "xingran-react-frontend/src/store/authStore.ts"
      provides: "login() returns without auto-loading menus"
      contains: "loadMenusAfterLogin"
    - path: "xingran-react-frontend/src/pages/login/index.tsx"
      provides: "performLogin single Promise.all([fetchMenus, fetchPermissions])"
      contains: "Promise.all"
    - path: "xingran-react-frontend/src/hooks/useRealtimeUpdates.ts"
      provides: "widgetsRef.current pattern; effect deps [options?.enabled, connect, disconnect]"
      contains: "widgetsRef"
    - path: "xingran-react-frontend/src/hooks/useWidgetPolling.ts"
      provides: "widgetDataCache Map.get(id) lookup"
      contains: "widgetDataCache"
    - path: "xingran-react-frontend/src/components/layout/sidebar.tsx"
      provides: "menuPathMap useMemo with [menus] selector dependency"
      contains: "buildMenuPathMap(menus)"
    - path: "xingran-react-frontend/src/pages/operations/buildings/useDepartmentData.ts"
      provides: "deptMap useMemo + O(1) getOrgName"
      contains: "deptMap"
    - path: "xingran-react-frontend/src/pages/operations/workstations/columns.tsx"
      provides: "handleEditRef/handleDeleteRef pattern in caller"
      contains: "handleEditRef"
    - path: "xingran-react-frontend/src/components/layout/header.tsx"
      provides: "Hoisted style constants at module top"
      contains: "HEADER_STYLE"
    - path: "xingran-react-frontend/src/pages/operations/buildings/index.tsx"
      provides: "Tag displays '停用' not '1' when status !== 0"
      contains: "'停用'"
  key_links:
    - from: "xingran-react-frontend/src/pages/login/index.tsx"
      to: "xingran-react-frontend/src/store/authStore.ts"
      via: "login() returns token; caller owns menu fetch"
      pattern: "await login\\(loginData\\)"
    - from: "xingran-react-frontend/src/hooks/useRealtimeUpdates.ts"
      to: "xingran-react-frontend/src/store/dashboardStore.ts"
      via: "widgetsRef.current read inside connect; widgets array identity does not retrigger effect"
      pattern: "widgetsRef\\.current"
    - from: "xingran-react-frontend/src/pages/operations/workstations/index.tsx"
      to: "xingran-react-frontend/src/pages/operations/workstations/columns.tsx"
      via: "useRef wrappers for handleEdit/handleDelete passed into columns via useMemo([])"
      pattern: "handleEditRef"
    - from: "xingran-react-frontend/src/pages/operations/buildings/index.tsx"
      to: "xingran-react-frontend/src/pages/operations/buildings/useDepartmentData.ts"
      via: "departmentData.getOrgName(building.orgId) reads memoized deptMap"
      pattern: "departmentData\\.getOrgName"
---

<objective>
Wave 1 — fix 7 CRITICAL performance defects (C1-C7) and 1 business bug (R7) from the 2026-06-13 Vercel React Best Practices audit. These are the highest-impact items: login flow no longer double-fetches, WebSocket/Polling no longer cause reconnection storms, sidebar memo deps are stable, building tag fix replaces literal '1' with '停用'.

Purpose: Eliminate the most expensive runtime defects first (login waterfall, WebSocket disconnect loops, full-table re-renders). After this wave, dashboard widget polling is O(1), login is single-batch, sidebar doesn't re-stringify menus, and the building card view shows correct status text.

**Downstream wave dependencies (informational):**
- C6's `useRef` + `useMemo` change in `pages/operations/workstations/index.tsx` is referenced by Plan 02 (Wave 2) Task 2 (M3) — the new `handleWorkstationModalOk` useCallback is inserted AFTER `handleSuccess` (line ~192), NOT next to the `columns` useMemo. Plan 02 explicitly anchors on `handleSuccess`.
- C4's per-field zustand selectors in `sidebar.tsx` are extended by Plan 02 (Wave 2) Task 1 (M5) which adds a `selectedKeys` useMemo adjacent to `menuPathMap`. Plan 03 (Wave 3) Task 2 (R5) then modifies THAT `selectedKeys` useMemo in place to use `menuByPath.get(path)?.id`.
- C2/C3's `widgetsRef`/`widgetIdsRef` patterns in the two polling/websocket hooks are extended by Plan 03 (Wave 3) Task 2 (R2/R3) which adds a singleton visibilitychange listener.

Output:
- 8 production files modified
- 0 new test files (defects are observable at runtime; verification is type-check + lint + targeted grep)
- Bug R7 fixed (literal '1' → '停用')
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-vercel-react-best-practices-20260613-26/CONTEXT.md
@.planning/reviews/20260613-frontend-react-best-practices-review.md

# Source-of-truth files to consult before editing
@xingran-react-frontend/src/store/authStore.ts
@xingran-react-frontend/src/pages/login/index.tsx
@xingran-react-frontend/src/hooks/useRealtimeUpdates.ts
@xingran-react-frontend/src/hooks/useWidgetPolling.ts
@xingran-react-frontend/src/components/layout/sidebar.tsx
@xingran-react-frontend/src/pages/operations/buildings/useDepartmentData.ts
@xingran-react-frontend/src/pages/operations/workstations/columns.tsx
@xingran-react-frontend/src/pages/operations/workstations/index.tsx
@xingran-react-frontend/src/components/layout/header.tsx
@xingran-react-frontend/src/pages/operations/buildings/index.tsx
@xingran-react-frontend/src/store/menuStore.ts
@xingran-react-frontend/src/store/dashboardStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: C1 — remove authStore.login menu auto-load; login page owns single Promise.all</name>
  <files>xingran-react-frontend/src/store/authStore.ts, xingran-react-frontend/src/pages/login/index.tsx</files>
  <read_first>
    - xingran-react-frontend/src/store/authStore.ts (read login action lines 60-98; note loadMenusAfterLogin call on line 93 and `loadMenusAfterLogin` definition on lines 128-137)
    - xingran-react-frontend/src/pages/login/index.tsx (read performLogin lines 47-67; note `await Promise.all([fetchMenus(), fetchPermissions()])`)
    - xingran-react-frontend/src/store/menuStore.ts (read fetchMenus/fetchPermissions/fetchAll signatures — they all already use TTL cache so a second call is idempotent, but the duplication is still wasted work + triggers a second permission refresh)
  </read_first>
  <action>
    Per C1, the login flow currently triggers menu fetch twice (once inside authStore.login via loadMenusAfterLogin → fetchAll(true), once via the login page Promise.all). Make authStore.login NOT load menus internally, and have the login page own the single Promise.all with forceRefresh=true.

    Edit xingran-react-frontend/src/store/authStore.ts:
    1. Inside the `login` action (around line 60-98), REMOVE the trailing `await get().loadMenusAfterLogin();` call (currently on line 93). The login action should end after `set({...menusLoaded: false, initialized: true})` and the try/catch should still set `menusLoaded: false` on error.
    2. Keep the `loadMenusAfterLogin` action function in the store (lines 128-137) — it is still used elsewhere (e.g., 401 recovery flows). Do NOT remove it. Just stop calling it from inside `login`.
    3. Do NOT change the `login` action's return type (it returns Promise<void>). Callers already ignore the return value.
    4. Confirm `menusLoaded: false` reset on line 88 stays (it prevents stale "loaded" state when login succeeds but menus aren't yet fetched).

    Edit xingran-react-frontend/src/pages/login/index.tsx:
    1. In `performLogin` (lines 47-67), keep the existing `await Promise.all([fetchMenus(), fetchPermissions()])` call. This is the SINGLE source of menu/permission fetch after login.
    2. Force a refresh on first login by passing `(true)` to both — match the original `loadMenusAfterLogin → fetchAll(true)` semantics. Change to `await Promise.all([fetchMenus(true), fetchPermissions(true)])`. Reason: TTL cache may contain stale data from a prior session on the same browser; forceRefresh=true ensures a fresh fetch.
    3. Do NOT add a separate `loadMenusAfterLogin` call — the goal is to keep ONE menu fetch, not two.

    Acceptance assertions (verified by grep):
    - `grep -n "loadMenusAfterLogin" xingran-react-frontend/src/store/authStore.ts` shows ONLY the action definition (no call sites inside `login`)
    - `grep -n "fetchMenus(true)" xingran-react-frontend/src/pages/login/index.tsx` returns 1 line
    - `grep -n "fetchMenus()" xingran-react-frontend/src/pages/login/index.tsx` returns 0 lines (no forceRefresh-less call)
  </action>
  <acceptance_criteria>
    - xingran-react-frontend/src/store/authStore.ts: no `loadMenusAfterLogin()` call inside the `login` action (grep returns only the function definition, no invocation inside `login:`)
    - xingran-react-frontend/src/store/authStore.ts: `login` action still sets `menusLoaded: false` in the success path (line ~88) and the error path (line ~95)
    - xingran-react-frontend/src/pages/login/index.tsx: performLogin contains `Promise.all([fetchMenus(true), fetchPermissions(true)])` — verified by grep returning the exact literal string
    - xingran-react-frontend/src/pages/login/index.tsx: performLogin does NOT call `loadMenusAfterLogin` (grep returns 0 matches)
    - No other call sites of `loadMenusAfterLogin` exist that were previously relying on the auto-load behavior (grep the project to confirm only legitimate callers remain)
    - `npm run type-check` exits 0
    - `npm run lint` exits 0 with no new warnings
  </acceptance_criteria>
  <verify>
    <automated>cd xingran-react-frontend && grep -rn "loadMenusAfterLogin" src/ && grep -n "Promise.all" src/pages/login/index.tsx && npm run type-check && npm run lint</automated>
  </verify>
  <done>
    C1 fully resolved: login flow makes exactly ONE menu fetch (Promise.all) and ONE permission fetch, both with forceRefresh=true. authStore.login no longer triggers any menu/permission API calls. Type-check and lint pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: C2 — useRealtimeUpdates ref-based widgets; C3 — useWidgetPolling Map cache lookup</name>
  <files>xingran-react-frontend/src/hooks/useRealtimeUpdates.ts, xingran-react-frontend/src/hooks/useWidgetPolling.ts</files>
  <read_first>
    - xingran-react-frontend/src/hooks/useRealtimeUpdates.ts (lines 62-127 for connect useCallback, lines 161-178 for useEffect; identify widgets array reference in deps)
    - xingran-react-frontend/src/hooks/useWidgetPolling.ts (lines 66-105 for fetchData; lines 73-85 for the O(n) cache check loop; lines 127-145 for the interval useEffect)
    - xingran-react-frontend/src/store/dashboardStore.ts (find `widgetDataCache` field at line ~75 — it's `Map<string, { data: unknown; timestamp: number }>`; find any helper accessor `getCachedWidgetData`)
  </read_first>
  <action>
    Per C2, refactor useRealtimeUpdates so that `widgets` array reference changes do NOT recreate `connect` and do NOT retrigger the connection effect:

    Edit xingran-react-frontend/src/hooks/useRealtimeUpdates.ts:
    1. Add a `widgetsRef = useRef(widgets)` ref (place after `wsRef` declaration on line ~51).
    2. Immediately after the ref declarations (around line 54), add a synchronous assignment: `widgetsRef.current = widgets;` so the ref always tracks the latest prop value. This pattern is used because the ref pattern in React needs a body-assignment; effect-based assignment would lag by one render.
    3. Change `connect` useCallback (lines 63-127) deps from `[widgets, options, getWsUrl, cacheWidgetData]` to `[options, getWsUrl, cacheWidgetData]`. Inside the `connect` function body, replace `widgets.forEach(...)` with `widgetsRef.current.forEach(...)` (lines 75-86) and replace `widgets.forEach(...)` in `refreshSubscriptions` (lines 145-158).
    4. Change the main effect (lines 161-178) deps from `[widgets, options?.enabled, connect, disconnect]` to `[options?.enabled, connect, disconnect]`. Inside the effect body (line 167), replace `widgets.filter(...)` with `widgetsRef.current.filter(...)`.
    5. Do NOT change the return type or any public API of the hook.
    6. Verify: re-running the effect will not happen when `widgets` changes identity but `options?.enabled` stays the same — but `connect`/`disconnect` are stable now so the effect itself is stable.

    Per C3, refactor useWidgetPolling to use the Map.get(id) pattern instead of array.find:

    Edit xingran-react-frontend/src/hooks/useWidgetPolling.ts:
    1. In `fetchData` (lines 66-105), replace the loop on lines 73-85. Specifically, change:
       - Before: `const cached = getCachedWidgetData(id); if (!cached || ...) uncachedIds.push(id);`
       - To: read the Map directly. Get the cache Map via `const cacheMap = useDashboardStore.getState().widgetDataCache;` at the top of `fetchData`. Then `const cached = cacheMap.get(id);` inside the loop. Same expiry check `!cached || ...`.
    2. The `useDashboardStore.getState().widgetDataCache` pattern returns the live Map; if widgetDataCache is `undefined` or not a Map, fall back gracefully (the loop should still work — `cacheMap.get` will throw if cacheMap is undefined; guard with `cacheMap?.get?.(id) ?? null`).
    3. Change `fetchData` deps from `[widgetIds, cacheExpiry, cacheWidgetData, getCachedWidgetData, clearWidgetCache]` to `[cacheExpiry, cacheWidgetData, clearWidgetCache]` — note `getCachedWidgetData` is removed from deps because we no longer call it (we read via store.getState()).
    4. ALSO remove the dependency on `widgetIds` reference identity by reading it via a ref pattern. Add `const widgetIdsRef = useRef(widgetIds); widgetIdsRef.current = widgetIds;` at top of hook. Replace `widgetIds.length === 0` checks (lines 67, 128, 158) with `widgetIdsRef.current.length === 0`. Replace `for (const id of widgetIds)` (line 73) with `for (const id of widgetIdsRef.current)`.
    5. After this change, the `fetchData` callback is stable across `widgetIds` reference churn, and the parent effect (lines 127-145) will not re-trigger on widget identity changes.
    6. Do NOT remove the `widgetIds` from the destructured arguments — caller signature must stay backwards-compatible.

    Acceptance assertions (verified by grep + type-check):
    - `grep -n "widgetsRef" xingran-react-frontend/src/hooks/useRealtimeUpdates.ts` returns the ref declaration + at least 2 usages (in connect + refreshSubscriptions)
    - `grep -n "widgets" xingran-react-frontend/src/hooks/useRealtimeUpdates.ts | grep -v widgetsRef` returns only the function signature line
    - `grep -n "widgetDataCache" xingran-react-frontend/src/hooks/useWidgetPolling.ts` returns at least 2 lines (declaration read + get call)
    - `grep -n "widgetIdsRef" xingran-react-frontend/src/hooks/useWidgetPolling.ts` returns the ref + usages
  </action>
  <acceptance_criteria>
    - xingran-react-frontend/src/hooks/useRealtimeUpdates.ts: `connect` useCallback deps array does NOT contain `widgets` (grep shows `[options, getWsUrl, cacheWidgetData]` or similar without `widgets`)
    - xingran-react-frontend/src/hooks/useRealtimeUpdates.ts: connect body uses `widgetsRef.current.forEach` (grep returns at least 1 match)
    - xingran-react-frontend/src/hooks/useRealtimeUpdates.ts: main useEffect deps do NOT contain `widgets` (grep shows deps line without `widgets`)
    - xingran-react-frontend/src/hooks/useWidgetPolling.ts: `widgetDataCache` Map.get pattern replaces array.find (grep `widgetDataCache.get(` returns match)
    - xingran-react-frontend/src/hooks/useWidgetPolling.ts: `widgetIdsRef` declared (grep returns match)
    - xingran-react-frontend/src/hooks/useWidgetPolling.ts: fetchData deps no longer contain `widgetIds` or `getCachedWidgetData` (grep deps array)
    - `npm run type-check` exits 0
    - `npm run lint` exits 0 with no new warnings
  </acceptance_criteria>
  <verify>
    <automated>cd xingran-react-frontend && grep -n "widgetsRef" src/hooks/useRealtimeUpdates.ts && grep -n "widgetDataCache\|widgetIdsRef" src/hooks/useWidgetPolling.ts && npm run type-check && npm run lint</automated>
  </verify>
  <done>
    C2 fully resolved: useRealtimeUpdates connects only on `options?.enabled` change, not on `widgets` identity churn. C3 fully resolved: useWidgetPolling cache lookup is O(1) via Map.get(id), and widgetIds reference churn does not retrigger the polling effect. Type-check and lint pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: C4 sidebar JSON.stringify deps + C5 deptMap memo + C6 columns ref pattern + C7 header style hoist + R7 bug</name>
  <files>xingran-react-frontend/src/components/layout/sidebar.tsx, xingran-react-frontend/src/pages/operations/buildings/useDepartmentData.ts, xingran-react-frontend/src/pages/operations/workstations/columns.tsx, xingran-react-frontend/src/pages/operations/workstations/index.tsx, xingran-react-frontend/src/components/layout/header.tsx, xingran-react-frontend/src/pages/operations/buildings/index.tsx</files>
  <read_first>
    - xingran-react-frontend/src/components/layout/sidebar.tsx (line 82 destructures `const { menus, loading, fetchMenus } = useMenuStore();`; line 101 has `useMemo(() => buildMenuPathMap(menus), [JSON.stringify(menus)])`)
    - xingran-react-frontend/src/store/menuStore.ts (verify `menus` is a primitive-stable array — when set() is called with a new array only on actual menu fetch; identity is stable between fetches)
    - xingran-react-frontend/src/pages/operations/buildings/useDepartmentData.ts (lines 51-70: getDeptMap and getOrgName)
    - xingran-react-frontend/src/pages/operations/workstations/columns.tsx (lines 19-105: getWorkstationColumns function)
    - xingran-react-frontend/src/pages/operations/workstations/index.tsx (lines 196-201: `const columns = getWorkstationColumns(...)`)
    - xingran-react-frontend/src/components/layout/header.tsx (lines 62-100: inline style={{...}} on AntHeader and Avatar)
    - xingran-react-frontend/src/components/layout/header.constants.ts (verify constants like AVATAR_BORDER_OPACITY, HEADER_Z_INDEX exist)
    - xingran-react-frontend/src/pages/operations/buildings/index.tsx (line 355: `<Tag color={building.status === 0 ? 'success' : 'error'}>{building.status === 0 ? '正常' : '1'}</Tag>`)
  </read_first>
  <action>
    Per C4 — remove JSON.stringify deps in sidebar:

    Edit xingran-react-frontend/src/components/layout/sidebar.tsx:
    1. Line 82: split the destructure so `menus` is read via a separate selector. Replace `const { menus, loading, fetchMenus } = useMenuStore();` with two calls:
       ```
       const menus = useMenuStore(s => s.menus);
       const loading = useMenuStore(s => s.loading);
       const fetchMenus = useMenuStore(s => s.fetchMenus);
       ```
       Zustand selectors return the exact reference stored in state. The `menus` reference is only changed when the menu store calls `set({ menus: ... })`, which only happens on actual fetch. So `[menus]` is a stable dep.
    2. Line 101: change `useMemo(() => buildMenuPathMap(menus), [JSON.stringify(menus)])` to `useMemo(() => buildMenuPathMap(menus), [menus])`.

    Per C5 — memoize deptMap:

    Edit xingran-react-frontend/src/pages/operations/buildings/useDepartmentData.ts:
    1. Add `import { useMemo } from 'react';` to the React import on line 1.
    2. Replace the `getDeptMap` useCallback (lines 51-63) with a `deptMap` useMemo:
       ```
       const deptMap = useMemo(() => {
         const map = new Map<string, string>();
         const flat = (nodes: DepartmentOption[]): void => {
           for (const n of nodes) {
             map.set(n.id, n.deptName);
             if (n.children) flat(n.children);
           }
         };
         flat(departments);
         return map;
       }, [departments]);
       ```
    3. Replace `getOrgName` useCallback (lines 66-70) to read the memoized Map:
       ```
       const getOrgName = useCallback((orgId?: string): string => {
         if (!orgId) return '-';
         return deptMap.get(orgId) || '-';
       }, [deptMap]);
       ```
    4. Return type and exported interface unchanged.

    Per C6 — stabilize columns via useRef + useMemo([]):

    Edit xingran-react-frontend/src/pages/operations/workstations/columns.tsx:
    1. The `getWorkstationColumns(params)` function signature stays the same — callers continue to call it with `{ handleEdit, handleDelete }`. The optimization happens at the CALLER (index.tsx), not in columns.tsx.
    2. In columns.tsx: no changes needed. The columns generator continues to close over `handleEdit`/`handleDelete` passed in.

    Edit xingran-react-frontend/src/pages/operations/workstations/index.tsx:
    1. Add `useRef` to the React imports on line 10: `import { useState, useCallback, useEffect, useRef } from 'react';`.
    2. Inside `WorkstationManagement` function body (after the existing hooks around line 120), add refs:
       ```
       const handleEditRef = useRef<(record: WorkstationOps) => void>(() => {});
       const handleDeleteRef = useRef<(id: string) => void>(() => {});
       ```
    3. Add a sync block (after the refs and BEFORE `useTableManager`):
       ```
       handleEditRef.current = (record: WorkstationOps) => {
         handleOpenModal(record);
       };
       handleDeleteRef.current = (id: string) => handleDelete(id, handleSuccess);
       ```
       Note: handleOpenModal is already declared on line 348 with useCallback — order matters. Move this sync block AFTER handleOpenModal declaration. Easiest: place it right before the `columns` line (line 196).
    4. Replace line 196-201:
       ```
       const columns = getWorkstationColumns({
         handleEdit: (record: WorkstationOps) => {
           handleOpenModal(record);
         },
         handleDelete: (id: string) => handleDelete(id, handleSuccess),
       });
       ```
       With:
       ```
       const columns = useMemo(() => getWorkstationColumns({
         handleEdit: (r) => handleEditRef.current(r),
         handleDelete: (id) => handleDeleteRef.current(id),
       }), []);
       ```
    5. Import `useMemo` from React (line 10 imports already include useState/useCallback/useEffect; add useMemo).
    6. Order check: handleSuccess is declared on line 192 with useCallback — its deps include refreshData which includes loadWorkstations. handleOpenModal is line 348 with useCallback. Both are defined BEFORE the `columns` line 196 — but `useMemo(() => getWorkstationColumns({...handleOpenModal}), [])` only captures refs, not handleOpenModal directly, so there's no TDZ risk because refs are `const handleEditRef = useRef(...)` — but you must ensure the ref assignment runs in render body, BEFORE the columns useMemo. Place the assignment statements immediately before the useMemo, in this order: const handleEditRef/useRef, then the two `.current =` assignments, then `const columns = useMemo(...)`.

    Per C7 — hoist header inline styles:

    Edit xingran-react-frontend/src/components/layout/header.tsx:
    1. Add module-level constants near the top of the file (after the imports, before the `Header: FC = ()` declaration):
       ```
       const HEADER_STYLE = {
         position: 'relative' as const,
         zIndex: HEADER_Z_INDEX,
       };

       const AVATAR_STYLE = {
         background: 'linear-gradient(135deg, var(--theme-brand, #3b82f6) 0%, var(--theme-brand-dark, #2563eb) 100%)',
         color: 'var(--theme-text-inverse)',
         borderColor: `rgba(255, 255, 255, ${AVATAR_BORDER_OPACITY})`,
         transition: 'var(--theme-transition-base)',
       };

       const USER_NAME_STYLE = { color: 'var(--theme-text-secondary)' };
       ```
    2. Replace `style={{ position: 'relative', zIndex: HEADER_Z_INDEX }}` on the AntHeader element (line 65-68) with `style={HEADER_STYLE}`.
    3. Replace `style={{ background: 'linear-gradient(...), color: '...', borderColor: '...', transition: '...' }}` on the Avatar (lines 88-93) with `style={AVATAR_STYLE}`.
    4. Replace `style={{ color: 'var(--theme-text-secondary)' }}` on the span (line 78) with `style={USER_NAME_STYLE}`.

    Per R7 — fix building card view status display bug:

    Edit xingran-react-frontend/src/pages/operations/buildings/index.tsx:
    1. Line 355: replace `{building.status === 0 ? '正常' : '1'}` with `{building.status === 0 ? '正常' : '停用'}`.

    Acceptance assertions (verified by grep):
    - `grep -n "JSON.stringify(menus)" xingran-react-frontend/src/components/layout/sidebar.tsx` returns 0 matches
    - `grep -n "deptMap" xingran-react-frontend/src/pages/operations/buildings/useDepartmentData.ts` returns at least 4 matches (declaration, 2 usages, return)
    - `grep -n "useMemo" xingran-react-frontend/src/pages/operations/workstations/index.tsx` returns the new useMemo around columns
    - `grep -n "HEADER_STYLE\|AVATAR_STYLE" xingran-react-frontend/src/components/layout/header.tsx` returns the const declaration + at least 1 usage
    - `grep -n "'停用'" xingran-react-frontend/src/pages/operations/buildings/index.tsx` returns at least 1 match
    - `grep -n "?: '1'" xingran-react-frontend/src/pages/operations/buildings/index.tsx` returns 0 matches
  </action>
  <acceptance_criteria>
    - xingran-react-frontend/src/components/layout/sidebar.tsx: no `JSON.stringify(menus)` anywhere (grep returns 0)
    - xingran-react-frontend/src/components/layout/sidebar.tsx: menuPathMap useMemo deps is exactly `[menus]` (grep confirms)
    - xingran-react-frontend/src/components/layout/sidebar.tsx: `useMenuStore(s => s.menus)` selector pattern used (grep confirms)
    - xingran-react-frontend/src/pages/operations/buildings/useDepartmentData.ts: `useMemo` imported and `deptMap` declared as Map (grep confirms)
    - xingran-react-frontend/src/pages/operations/buildings/useDepartmentData.ts: getOrgName reads `deptMap.get(orgId)` (grep confirms)
    - xingran-react-frontend/src/pages/operations/workstations/index.tsx: imports include useRef and useMemo (grep line 10-11)
    - xingran-react-frontend/src/pages/operations/workstations/index.tsx: handleEditRef and handleDeleteRef declared (grep returns 2 matches)
    - xingran-react-frontend/src/pages/operations/workstations/index.tsx: columns wrapped in `useMemo(() => getWorkstationColumns({handleEdit: (r) => handleEditRef.current(r), handleDelete: (id) => handleDeleteRef.current(id)}), [])` (grep confirms)
    - xingran-react-frontend/src/components/layout/header.tsx: HEADER_STYLE and AVATAR_STYLE const declarations at module top (grep line < 20)
    - xingran-react-frontend/src/components/layout/header.tsx: AntHeader uses `style={HEADER_STYLE}` (grep confirms)
    - xingran-react-frontend/src/pages/operations/buildings/index.tsx: line 355 contains `'停用'` (grep confirms literal string)
    - `npm run type-check` exits 0
    - `npm run lint` exits 0 with no new warnings
  </acceptance_criteria>
  <verify>
    <automated>cd xingran-react-frontend && grep -n "JSON.stringify" src/components/layout/sidebar.tsx; grep -n "deptMap" src/pages/operations/buildings/useDepartmentData.ts; grep -n "handleEditRef\|handleDeleteRef" src/pages/operations/workstations/index.tsx; grep -n "HEADER_STYLE\|AVATAR_STYLE" src/components/layout/header.tsx; grep -n "'停用'" src/pages/operations/buildings/index.tsx; npm run type-check && npm run lint</automated>
  </verify>
  <done>
    C4 fully resolved: sidebar menuPathMap deps stable, no JSON.stringify. C5 fully resolved: deptMap built once via useMemo. C6 fully resolved: workstation columns stable via useRef + useMemo([]). C7 fully resolved: header styles hoisted to module constants. R7 fully resolved: building card view shows '停用' for status !== 0. Type-check and lint pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new attack surface introduced. All changes are performance refactors inside the frontend bundle; no new endpoints, no auth changes, no data exfiltration paths. The login flow change (C1) tightens the existing waterfall but does not change authentication semantics.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-W1-SC | (n/a) | (frontend refactor) | accept | No new attack surface. type-check + lint serve as regression safety net. |
</threat_model>

<verification>
- [ ] `npm run type-check` exits 0
- [ ] `npm run lint` exits 0 with no new warnings
- [ ] `grep -c "JSON.stringify(menus)" xingran-react-frontend/src/components/layout/sidebar.tsx` returns 0
- [ ] `grep -c "'1'" xingran-react-frontend/src/pages/operations/buildings/index.tsx` — the literal `'1'` near status === 0 must NOT exist anymore (note: there may be other `'1'` strings, so use the more specific grep above)
- [ ] `npm run build` exits 0 (full bundle still builds)
- [ ] Manual smoke: open /login, log in, confirm dashboard loads WITHOUT duplicate network calls (DevTools network tab shows 1 batch of menu+permission calls, not 2)
- [ ] Manual smoke: open /operations/buildings, switch to card view, confirm buildings with status === 1 display '停用' not '1'

> **NOTE on automated tests for R7 and C1 (per checker feedback, low-priority):**
> This plan does not add Vitest unit tests for the R7 status-tag mapping or the C1 login dedup behavior. The project pattern (and the rest of this phase) relies on manual smoke verification plus type-check + lint for these user-visible changes. If the project later introduces a Vitest suite for these layers, future phases may add targeted tests such as:
> - A unit test asserting `formatStatus(1) === '停用'` (extracted helper around the building card view).
> - A mock-store test verifying `authStore.login` does not call `loadMenusAfterLogin` and that the login page's `Promise.all([fetchMenus(true), fetchPermissions(true)])` is the only menu fetch on a fresh login.
> For this phase, manual smoke + grep assertions are the verification surface.
</verification>

<success_criteria>
- All 7 CRITICAL defects (C1-C7) and the bug R7 are fixed
- Login flow makes exactly ONE menu/permission fetch (single Promise.all)
- WebSocket/Polling effect dependency arrays no longer cause reconnection storms on `widgets` identity churn
- Sidebar memo deps are stable references (no JSON.stringify)
- Workstation columns reference is stable (useMemo([]) + ref pattern)
- Building card status display is correct ('停用' instead of '1')
- type-check, lint, and full build all pass
</success_criteria>

<output>
Create `.planning/phases/33-vercel-react-best-practices-20260613-26/33-01-SUMMARY.md` when done
</output>
