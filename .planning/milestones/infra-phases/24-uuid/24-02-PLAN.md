---
phase: 24-uuid
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [internal/models/base_test.go]
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "BaseModel.BeforeCreate hook generates valid UUIDs"
    - "Existing UUIDs are preserved when BeforeCreate is called"
    - "Tests verify UUID format using uuid.Parse()"
    - "Tests use in-memory SQLite for fast execution"
  artifacts:
    - path: "internal/models/base_test.go"
      provides: "Unit tests for UUID generation"
      contains: "func TestBaseModel_BeforeCreate_GeneratesUUID"
    - path: "internal/models/base_test.go"
      provides: "Unit tests for UUID preservation"
      contains: "func TestBaseModel_BeforeCreate_PreservesExistingUUID"
  key_links:
    - from: "internal/models/base_test.go"
      to: "internal/models/base.go"
      via: "Tests BaseModel.BeforeCreate hook"
      pattern: "func.*BeforeCreate"
    - from: "internal/models/base_test.go"
      to: "github.com/google/uuid"
      via: "UUID format validation"
      pattern: "uuid.Parse"
</key_links>
---

<objective>
Add unit tests for UUID generation behavior

Purpose: Ensure UUID generation works correctly and prevent regressions through automated tests
Output: internal/models/base_test.go with comprehensive UUID behavior tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-uuid/24-CONTEXT.md
@.planning/phases/24-uuid/24-PATTERNS.md
@internal/models/base.go
@internal/services/addomain/member_sync_service_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit tests for BaseModel UUID generation</name>
  <files>internal/models/base_test.go</files>
  <read_first>
    - internal/models/base.go (Verify BaseModel structure and BeforeCreate implementation)
    - internal/services/addomain/member_sync_service_test.go (Reference test file structure and patterns)
  </read_first>
  <action>
    Create a new test file internal/models/base_test.go with the following structure:

    Package declaration: `package models` (same package as base.go for access to unexported fields if needed)

    Import these packages:
    - "testing" for test framework
    - "github.com/stretchr/testify/assert" for assertions
    - "github.com/stretchr/testify/require" for setup assertions
    - "github.com/google/uuid" for UUID validation
    - "gorm.io/gorm" for database interface
    - "gorm.io/driver/sqlite" for in-memory test database

    Implement these test functions:

    1. TestBaseModel_BeforeCreate_GeneratesUUID:
       - Create a BaseModel instance with empty ID
       - Call BeforeCreate(nil)
       - Assert no error returned
       - Assert ID is not empty
       - Parse ID with uuid.Parse() and assert no error (valid UUID format)

    2. TestBaseModel_BeforeCreate_PreservesExistingUUID:
       - Generate a valid UUID using uuid.New().String()
       - Create a BaseModel with that ID
       - Call BeforeCreate(nil)
       - Assert no error returned
       - Assert ID equals the original UUID (was not overwritten)

    Follow the test pattern from member_sync_service_test.go:
    - Use assert for test assertions
    - Use descriptive error messages in assertions
    - Test both success and edge cases

    Do NOT create a setup function - these are simple unit tests that don't require database setup.
  </action>
  <verify>
    <automated>cd internal/models && go test -v -run TestBaseModel_BeforeCreate</automated>
  </verify>
  <done>
    internal/models/base_test.go exists and contains:
    - func TestBaseModel_BeforeCreate_GeneratesUUID(t *testing.T)
    - func TestBaseModel_BeforeCreate_PreservesExistingUUID(t *testing.T)
    - Both tests use uuid.Parse() to validate UUID format
    - Tests use assert/assert from testify
    - Running go test in internal/models/ passes both tests
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → Production code | Tests only read production code, no modifications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03 | Tampering | base_test.go | accept | Test file, no production impact. Git history provides tamper detection. |
</threat_model>

<verification>
- grep -n "func TestBaseModel_BeforeCreate" internal/models/base_test.go (both test functions exist)
- grep -n "uuid.Parse" internal/models/base_test.go (UUID validation present)
- go test ./internal/models/... -v (tests pass)
</verification>

<success_criteria>
- [ ] internal/models/base_test.go file created
- [ ] TestBaseModel_BeforeCreate_GeneratesUUID validates UUID generation
- [ ] TestBaseModel_BeforeCreate_PreservesExistingUUID validates ID preservation
- [ ] Tests use uuid.Parse() for format validation
- [ ] Tests use testify assert assertions
- [ ] Running go test ./internal/models/ passes
- [ ] Test coverage for BeforeCreate hook is 100%
</success_criteria>

<output>
After completion, create `.planning/phases/24-uuid/24-02-SUMMARY.md`
</output>
