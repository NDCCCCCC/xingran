---
phase: 24-uuid
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/开发规范.md]
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Developers can find UUID handling standards in one place"
    - "Documentation provides clear code examples for UUID usage"
    - "Anti-patterns are explicitly documented with warnings"
    - "Migration script patterns are documented with correct/incorrect examples"
  artifacts:
    - path: "docs/开发规范.md"
      provides: "UUID handling standards documentation"
      contains: "## UUID 类型处理规范"
    - path: "docs/开发规范.md"
      provides: "External system UUID field guidelines"
      contains: "### 外部系统 UUID"
    - path: "docs/开发规范.md"
      provides: "Migration script standards"
      contains: "### 迁移脚本规范"
  key_links:
    - from: "docs/开发规范.md"
      to: "internal/models/base.go"
      via: "References BaseModel.BeforeCreate hook"
      pattern: "BeforeCreate.*uuid.New\\(\\)\\.String\\(\\)"
---

<objective>
Add UUID handling standards to development documentation

Purpose: Establish clear, authoritative guidance for UUID type handling across the codebase, ensuring all future development follows consistent patterns
Output: Updated docs/开发规范.md with comprehensive UUID handling section
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-uuid/24-CONTEXT.md
@.planning/phases/24-uuid/24-PATTERNS.md
@internal/models/base.go
@internal/models/vdi.go
@internal/models/user.go
@docs/开发规范.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UUID handling standards section to development documentation</name>
  <files>docs/开发规范.md</files>
  <read_first>
    - docs/开发规范.md (Read full file to understand structure and insertion point)
    - internal/models/base.go (Verify BaseModel.BeforeCreate implementation)
  </read_first>
  <action>
    Insert a new section "## UUID 类型处理规范" after "## 2. 状态值统一规范" (after line 78, before "## 3. 数据库设计规范").

    The new section must include:

    ### 生成策略
    - 统一使用 Go 侧 BeforeCreate 钩子生成
    - 禁止在迁移脚本中使用 DEFAULT gen_random_uuid()
    - BaseModel 已实现 BeforeCreate，所有模型自动继承
    - 代码示例：
      ```go
      // BaseModel 已实现 BeforeCreate
      func (b *BaseModel) BeforeCreate(tx *gorm.DB) error {
          if b.ID == "" {
              b.ID = uuid.New().String()
          }
          return nil
      }
      ```

    ### 字段类型
    - UUID 字段使用 string 类型
    - GORM 标签：`gorm:"type:uuid"`
    - 可选 UUID 使用指针类型：*string
    - 代码示例：
      ```go
      type UserModel struct {
          BaseModel
          // UUID 字段使用 string 类型
          OrgID    string  `gorm:"type:uuid" json:"orgId"`
          ParentID *string `gorm:"type:uuid" json:"parentId,omitempty"`
      }
      ```

    ### 外部系统 UUID
    - 外部系统（如 VDI）返回的 ID 使用 varchar(100)
    - 必须添加 GORM comment 注释说明来源
    - 添加代码注释说明格式
    - 示例：
      ```go
      // VDIVirtualMachine VDI 虚拟机表
      // VMID: 深信服VDI返回的虚拟机ID，格式示例：vm-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
      type VDIVirtualMachine struct {
          BaseModel
          // 外部系统ID字段
          VMID        string `gorm:"type:varchar(100);uniqueIndex;not null;comment:VDI虚拟机ID" json:"vmId"`
          VdiServerID string `gorm:"type:varchar(100);index;not null;comment:VDI服务器ID" json:"vdiServerId"`

          // 内部关联使用 UUID 类型
          BoundUserID *string `gorm:"type:uuid" json:"boundUserId,omitempty"`
      }
      ```

    ### 迁移脚本规范
    - 新建表的主键列不使用 DEFAULT gen_random_uuid()
    - 所有 UUID 列由 Go 层 BeforeCreate 钩子生成
    - 已有表保持现状（不修改现有定义）
    - 正确示例：
      ```sql
      -- ✅ 正确：新表创建时不使用默认值
      CREATE TABLE sys_new_table (
          id UUID PRIMARY KEY,  -- 无 DEFAULT，由 Go 生成
          name VARCHAR(100) NOT NULL,
          user_id UUID NOT NULL,
          created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
      );
      ```
    - 错误示例：
      ```sql
      -- ❌ 错误：新表不要使用 DEFAULT gen_random_uuid()
      CREATE TABLE sys_new_table (
          id UUID PRIMARY KEY DEFAULT gen_random_uuid()  -- 不要这样做
      );
      ```

    ### 反模式警告
    - ❌ 不要在 GORM 标签中使用 default:gen_random_uuid()
    - ❌ 不要混用多种 UUID 生成方式
    - ❌ 不要为外部系统 ID 使用 UUID 类型（使用 varchar(100)）
    - ❌ 不要修改现有表的 UUID 列定义

    Insert this section after line 78 (end of section 2) and before line 80 (start of section 3).
    Renumber subsequent sections if needed (3→4, 4→5, etc.).
  </action>
  <verify>
    <automated>grep -n "## UUID 类型处理规范" docs/开发规范.md</automated>
  </verify>
  <done>
    docs/开发规范.md contains "## UUID 类型处理规范" section with:
    - "生成策略" subsection with BeforeCreate example
    - "字段类型" subsection with string type and *string pointer examples
    - "外部系统 UUID" subsection with VDI model example
    - "迁移脚本规范" subsection with correct/incorrect SQL examples
    - "反模式警告" subsection with 4 explicit warnings
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation editor → Codebase | Documentation changes don't affect runtime behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | docs/开发规范.md | accept | Documentation file, no code execution. Git history provides tamper detection. |
| T-24-02 | Information Disclosure | UUID generation patterns | accept | Standards documentation by design. No sensitive information exposed. |
</threat_model>

<verification>
- grep -n "## UUID 类型处理规范" docs/开发规范.md (section exists)
- grep -n "BeforeCreate" docs/开发规范.md (code example included)
- grep -n "外部系统 UUID" docs/开发规范.md (external UUID guidance included)
- grep -n "迁移脚本规范" docs/开发规范.md (migration standards included)
- grep -n "反模式警告" docs/开发规范.md (anti-pattern warnings included)
- No unintended modifications to other sections
</verification>

<success_criteria>
- [ ] UUID handling section added to docs/开发规范.md
- [ ] Section includes all 5 subsections: 生成策略, 字段类型, 外部系统 UUID, 迁移脚本规范, 反模式警告
- [ ] Code examples are accurate and match actual codebase patterns
- [ ] Anti-patterns are clearly marked with ❌ emoji
- [ ] Migration script examples show both correct and incorrect patterns
- [ ] Documentation structure is maintained (section numbering updated if needed)
</success_criteria>

<output>
After completion, create `.planning/phases/24-uuid/24-01-SUMMARY.md`
</output>
