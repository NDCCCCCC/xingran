---
phase: 37-dept-select-unify
plan: 05
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - xingran-react-frontend/src/pages/duty/pools/index.tsx
  - xingran-react-frontend/src/pages/workorder/orders/hooks/useWorkOrderData.ts
  - xingran-react-frontend/src/lib/workorderApi.ts
autonomous: true
requirements:
  - SUCC-consume-hook
  - SUCC-type-dedup
must_haves:
  truths:
    - "duty/pools 不再 useEffect+getDeptTree,改用 useDeptTree;DepartmentTreeSelect 受控模式保持,数据从 hook 喂入"
    - "workorder/useWorkOrderData 不再 fetchDepts,改用 useDeptTree"
    - "workorderApi 的 SimpleDept 改为 re-export from dutyApi(全项目唯一)"
    - "workorderApi 的 getDeptTree 副本删除(若存在)"
  artifacts:
    - path: "xingran-react-frontend/src/pages/duty/pools/index.tsx"
      provides: "值班池页消费 useDeptTree"
      contains: "useDeptTree()"
    - path: "xingran-react-frontend/src/pages/workorder/orders/hooks/useWorkOrderData.ts"
      provides: "工单数据 hook 消费 useDeptTree"
      contains: "useDeptTree()"
    - path: "xingran-react-frontend/src/lib/workorderApi.ts"
      provides: "SimpleDept re-export 而非重定义"
      contains: "export type { SimpleDept } from \"./dutyApi\""
  key_links:
    - from: "xingran-react-frontend/src/pages/workorder/orders/hooks/useWorkOrderData.ts"
      to: "xingran-react-frontend/src/hooks/useDeptTree.ts"
      via: "import { useDeptTree }"
      pattern: "useDeptTree\\(\\)"
---

<objective>
批 4 duty + workorder 模块迁移 + **workorderApi 类型去重**。

- `duty/pools/index.tsx` line 171-185 `fetchDepts` + `getDeptTree()` → `useDeptTree`(DepartmentTreeSelect 受控模式保持,数据从 hook 喂入)
- `workorder/orders/useWorkOrderData` line 149-156 `fetchDepts`(用 workorderApi 副本) → `useDeptTree`
- `lib/workorderApi.ts` line 253 `SimpleDept` 重定义 → `export type { SimpleDept } from "./dutyApi"`
- `lib/workorderApi.ts` line 575 `getDeptTree` 副本 → 删除(若存在,让消费方直接 import dutyApi 的 getDeptTree 或经 hook)

Purpose: duty/workorder 收敛 + 消除 workorderApi 重复类型/函数。
Output: 两模块消费 hook;workorderApi 改 re-export。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-dept-select-unify/37-CONTEXT.md
@.planning/phases/37-dept-select-unify/37-PATTERNS.md
@.planning/phases/37-dept-select-unify/37-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: duty/pools 迁移 — fetchDepts 改 useDeptTree,DepartmentTreeSelect 受控数据由 hook 喂入</name>
  <files>xingran-react-frontend/src/pages/duty/pools/index.tsx</files>

  <read_first>
    - xingran-react-frontend/src/pages/duty/pools/index.tsx(line 171-179 fetchDepts + `getDeptTree()`、depts useState、line 184-190 useEffect 调用 fetchDepts/fetchList/fetchStats/fetchUsers、DepartmentTreeSelect 在 JSX 中使用点 `<DepartmentTreeSelect`、传入的 `departments`/`treeData` prop)
    - xingran-react-frontend/src/hooks/useDeptTree.ts(canonical)
    - xingran-react-frontend/src/components/shared/DepartmentTreeSelect.tsx(37-01 Task 2 改造后,props departments 为 SimpleDept[],受控模式)
    - .planning/phases/37-dept-select-unify/37-PATTERNS.md `### pages/duty/pools/index.tsx` 段落 line 486-517
  </read_first>

  <action>
    1. 删除 `fetchDepts`(line 171-179)、`depts` useState。
    2. 顶部 `import { useDeptTree } from "@/hooks/useDeptTree";`,函数体内加 `const { data: depts = [] } = useDeptTree();`。
    3. useEffect 中删除 `fetchDepts()` 调用(line 185 附近)。其余 fetchList/fetchStats/fetchUsers 调用保留。
    4. JSX 中 `<DepartmentTreeSelect ... departments={depts} />`(或等价 prop 名,依据 37-01 Task 2 改造后的最终 prop 签名)从 `depts` 喂入。**保持受控模式**,不要让 DepartmentTreeSelect 内部调 hook(37-01 已禁止)。
    5. 类型:depts 现在是 `SimpleDept[]`(DeptTreeNode 别名),核对 DepartmentTreeSelect 的 departments prop 类型一致(37-01 Task 2 已统一)。
  </action>

  <verify>
    <automated>cd xingran-react-frontend && grep -n "getDeptTree\|fetchDepts" src/pages/duty/pools/index.tsx | wc -l ; grep -n "useDeptTree" src/pages/duty/pools/index.tsx | wc -l ; npm run type-check 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - Source: `grep -c "fetchDepts" xingran-react-frontend/src/pages/duty/pools/index.tsx` = 0
    - Source: `grep -cE "getDeptTree|/system/departments/tree" xingran-react-frontend/src/pages/duty/pools/index.tsx` = 0
    - Source: `grep -c "useDeptTree" xingran-react-frontend/src/pages/duty/pools/index.tsx` = 1
    - CLI: `cd xingran-react-frontend && npm run type-check` 退出码 0
    - Behavior(手动核对):值班池页 — ①新增/编辑值班池表单中"部门"下拉显示(全路径从二级开始,37-01 Task 2 行为)与迁移前一致 ②列表按部门筛选一致
  </acceptance_criteria>

  <done>duty/pools 消费 useDeptTree;fetchDepts 删除;DepartmentTreeSelect 受控数据由 hook 喂入;type-check 通过;UI 行为不变。</done>
</task>

<task type="auto">
  <name>Task 2: workorder 迁移 + workorderApi 类型去重(useWorkOrderData 改 hook + SimpleDept re-export + getDeptTree 副本删除)</name>
  <files>xingran-react-frontend/src/pages/workorder/orders/hooks/useWorkOrderData.ts, xingran-react-frontend/src/lib/workorderApi.ts</files>

  <read_first>
    - xingran-react-frontend/src/pages/workorder/orders/hooks/useWorkOrderData.ts(line 149-156 fetchDepts、depts useState、import 语句中 `getDeptTree` 来自哪个 module——预期是 `@/lib/workorderApi`)
    - xingran-react-frontend/src/lib/workorderApi.ts(line 253-258 重复 `SimpleDept` 定义、line 575 `getDeptTree` 副本实现)
    - xingran-react-frontend/src/lib/dutyApi.ts line 281-305(canonical SimpleDept + getDeptTree)
    - xingran-react-frontend/src/hooks/useDeptTree.ts(canonical)
    - .planning/phases/37-dept-select-unify/37-PATTERNS.md `### pages/workorder/orders/hooks/useWorkOrderData.ts` 段落 line 520-538 + `### lib/workorderApi.ts` 段落 line 154-173
    - .planning/phases/37-dept-select-unify/37-CONTEXT.md `<specifics>` "验收命令:SimpleDept 全项目唯一定义;workorderApi re-export 而非重定义"
  </read_first>

  <action>
    1. **useWorkOrderData.ts**:
       - 删 `fetchDepts`(line 149-156)、`depts` useState。
       - 顶部 `import { useDeptTree } from "@/hooks/useDeptTree";`,加 `const { data: depts = [] } = useDeptTree();`。
       - 删除 `import { getDeptTree } from "@/lib/workorderApi"`(若 useWorkOrderData 不再直接调 getDeptTree)。
       - 返回对象保留 `depts` 字段。
    2. **workorderApi.ts**(类型去重):
       - 删除本地 `SimpleDept` 接口定义(line 253-258)。
       - 改为 `export type { SimpleDept } from "./dutyApi";`(保持外部 import 路径不变,消费方仍可 `import { SimpleDept } from "@/lib/workorderApi"`)。
       - 删除 `getDeptTree` 副本(line 575 附近)——若有消费方仍 import 此副本,改为 import from `@/lib/dutyApi`,但本任务更推荐让消费方改用 `useDeptTree` hook(useWorkOrderData 已改)。
       - **删除前必做**:`grep -rn "from \"@/lib/workorderApi\"" xingran-react-frontend/src/ | grep -E "SimpleDept|getDeptTree"` 确认所有引用点处理完毕。
    3. **核对**:全项目 `SimpleDept` 定义只剩 `dutyApi.ts` 一份。
  </action>

  <verify>
    <automated>cd xingran-react-frontend && grep -rn "export interface SimpleDept\|interface SimpleDept" src/ ; echo "---workorderApi re-export---" ; grep -n "SimpleDept" src/lib/workorderApi.ts ; echo "---useWorkOrderData---" ; grep -n "fetchDepts\|useDeptTree" src/pages/workorder/orders/hooks/useWorkOrderData.ts ; echo "---type-check---" ; npm run type-check 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - Source: `grep -rc "export interface SimpleDept\|interface SimpleDept" xingran-react-frontend/src/` 全项目总和 = 1(只剩 dutyApi.ts 一份)
    - Source: `grep -c "export type { SimpleDept } from \"./dutyApi\"" xingran-react-frontend/src/lib/workorderApi.ts` = 1
    - Source: `grep -c "fetchDepts" xingran-react-frontend/src/pages/workorder/orders/hooks/useWorkOrderData.ts` = 0
    - Source: `grep -c "useDeptTree" xingran-react-frontend/src/pages/workorder/orders/hooks/useWorkOrderData.ts` = 1
    - Source: `grep -c "getDeptTree" xingran-react-frontend/src/lib/workorderApi.ts` = 0(副本已删)
    - CLI: `cd xingran-react-frontend && npm run type-check` 退出码 0
    - Behavior(手动核对):工单管理页 — 工单表单/筛选中部门树显示与勾选行为与迁移前一致
  </acceptance_criteria>

  <done>workorder 消费 useDeptTree;workorderApi SimpleDept 改 re-export;getDeptTree 副本删除;全项目 SimpleDept 唯一;type-check 通过;UI 行为不变。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (无新增) | 仅消费既有部门树端点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-08 | Information Disclosure | duty/pools + workorder 部门数据展示 | accept | 数据来源不变 |
| T-37-09 | Tampering | workorderApi 类型 re-export 引用断裂 | mitigate | 删除前 grep 确认所有引用点处理;re-export 保持外部 import 路径不变(向后兼容) |
| T-37-SC | (n/a) | (无 npm 安装) | accept | 0 新依赖 |
</threat_model>

<verification>
- `grep -rn "/system/departments/tree" xingran-react-frontend/src/pages/duty/ xingran-react-frontend/src/pages/workorder/` = 0
- 全项目 `SimpleDept` 定义唯一(dutyApi.ts)
- workorderApi re-export 而非重定义
- `npm run type-check` 退出码 0
- 手动:duty/pools 与 workorder/orders 部门树显示一致
</verification>

<success_criteria>
批 4 完成:duty/pools + workorder/orders 消费 useDeptTree;workorderApi 类型/函数副本消除;UI 行为不变。
</success_criteria>

<output>
Create `.planning/phases/37-dept-select-unify/37-05-SUMMARY.md` when done
</output>
