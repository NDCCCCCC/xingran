---
phase: 37-dept-select-unify
plan: 06
type: execute
wave: 3
depends_on:
  - 37-01
  - 37-02
  - 37-03
  - 37-04
  - 37-05
files_modified:
  - xingran-react-frontend/src/components/TargetSelector.tsx
autonomous: true
requirements:
  - SUCC-consume-hook
  - SUCC-final-grep-verification
must_haves:
  truths:
    - "TargetSelector 不再 get('/system/departments/tree'),改用 useDeptTree(roles/users 部分不动)"
    - "全量 grep -rn '/system/departments/tree' src/ 剩余命中只剩排除项(role tree-select / dept 管理页 / AD 模块)"
    - "npm run build 退出码 0"
    - "npm run type-check 退出码 0"
  artifacts:
    - path: "xingran-react-frontend/src/components/TargetSelector.tsx"
      provides: "目标选择器消费 useDeptTree"
      contains: "useDeptTree()"
---

<objective>
批 5 收尾:TargetSelector + 全量 grep 验证 + build/type-check。

- `components/TargetSelector.tsx` line 41-52 GET 自 fetch → `useDeptTree`(roles/users 部分不动)
- 全量 grep 验证 Success Criteria:`grep -rn '/system/departments/tree' src/` 剩余命中仅限排除项
- `npm run build` + `npm run type-check` 双通过

Purpose: 完成全阶段收敛,提交 batch 5 后整个 Phase 37 验收达标。
Output: TargetSelector 收敛;全量 grep 干净;build/type-check 通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-dept-select-unify/37-CONTEXT.md
@.planning/phases/37-dept-select-unify/37-PATTERNS.md
@.planning/phases/37-dept-select-unify/37-01-SUMMARY.md
@.planning/phases/37-dept-select-unify/37-02-SUMMARY.md
@.planning/phases/37-dept-select-unify/37-03-SUMMARY.md
@.planning/phases/37-dept-select-unify/37-04-SUMMARY.md
@.planning/phases/37-dept-select-unify/37-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TargetSelector 迁移 — GET 自 fetch 改 useDeptTree(roles/users 不动)</name>
  <files>xingran-react-frontend/src/components/TargetSelector.tsx</files>

  <read_first>
    - xingran-react-frontend/src/components/TargetSelector.tsx(line 41-52 loadDeptTree + `get<DeptTreeNode[]>("/system/departments/tree")`、deptTree useState、相关 useEffect、DeptTreeNode 本地类型、roles/users 加载逻辑)
    - xingran-react-frontend/src/hooks/useDeptTree.ts(canonical,DeptTreeNode = SimpleDept)
    - .planning/phases/37-dept-select-unify/37-PATTERNS.md `### components/TargetSelector.tsx` 段落 line 278-298
  </read_first>

  <action>
    1. 删除 `loadDeptTree`(line 41-52)、`deptTree` useState、触发 loadDeptTree 的 useEffect(若有)。
    2. 顶部 `import { useDeptTree } from "@/hooks/useDeptTree";`,函数体内加 `const { data: deptTree = [] } = useDeptTree();`。
    3. 本地 `DeptTreeNode` 类型(若定义在 TargetSelector 内)改为 `import type { DeptTreeNode } from "@/hooks/useDeptTree";` 或直接用 `SimpleDept` from `@/lib/dutyApi`(全项目唯一)。
    4. **roles/users 加载逻辑完全不动**(只迁移 dept 子任务)。如果 loadDeptTree 与 loadRoles/loadUsers 在同一个 useEffect 中,只删除 dept 相关行,保留其余。
    5. 注意 TargetSelector 可能同时渲染 dept 树 + roles + users 三类目标,dept 子树的展示形态(短名还是其他)保持与迁移前一致。
  </action>

  <verify>
    <automated>cd xingran-react-frontend && grep -n "/system/departments/tree" src/components/TargetSelector.tsx | wc -l ; grep -n "loadDeptTree" src/components/TargetSelector.tsx | wc -l ; grep -n "useDeptTree" src/components/TargetSelector.tsx | wc -l ; npm run type-check 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - Source: `grep -c "/system/departments/tree" xingran-react-frontend/src/components/TargetSelector.tsx` = 0
    - Source: `grep -c "loadDeptTree" xingran-react-frontend/src/components/TargetSelector.tsx` = 0
    - Source: `grep -c "useDeptTree" xingran-react-frontend/src/components/TargetSelector.tsx` = 1
    - CLI: `cd xingran-react-frontend && npm run type-check` 退出码 0
    - Behavior(手动核对):使用 TargetSelector 的页面(若有 notice 或其他)——部门目标树显示与勾选行为与迁移前一致;roles/users 目标不受影响
  </acceptance_criteria>

  <done>TargetSelector 消费 useDeptTree;GET 自 fetch 删除;roles/users 逻辑保留;type-check 通过;UI 行为不变。</done>
</task>

<task type="auto">
  <name>Task 2: 全量 grep 验证 + build + type-check(Phase 37 整体验收)</name>
  <files>(无文件修改,仅验证)</files>

  <read_first>
    - .planning/phases/37-dept-select-unify/37-CONTEXT.md `<specifics>` "验收命令(必须全部通过)" + `<decisions>` 排除边界
    - .planning/ROADMAP.md Phase 37 Success Criteria 段落
    - .planning/phases/37-dept-select-unify/37-01-SUMMARY.md ~ 37-05-SUMMARY.md(确认各批已完成)
  </read_first>

  <action>
    执行 Phase 37 整体验收命令并记录结果到 SUMMARY:

    1. **核心 grep — 验证部门树 fetch 全部经 useDeptTree**:
       - `grep -rn "/system/departments/tree" xingran-react-frontend/src/` 列出所有命中
       - 逐条核对每条命中是否属于**排除边界**:
         - `pages/system/role/hooks/useRoleData.ts` 中 `/system/departments/tree-select`(不同端点,不命中 `/tree` 字符串;若有命中需说明)
         - `pages/system/dept/`(部门管理页本体,管理者)
         - `pages/ad-domain/`(AD 域控,独立数据源)
         - `lib/dutyApi.ts` 中 `getDeptTree` 的 `post("/system/departments/tree")`(canonical 唯一 fetch,允许保留)
       - 非排除项的命中 = 0,否则回对应批次 plan 修正。

    2. **类型唯一性验证**:
       - `grep -rn "interface SimpleDept\|export interface Department\b" xingran-react-frontend/src/` — `SimpleDept` 只在 dutyApi.ts 定义;`Department` 接口在 DepartmentTreeSelect.tsx 不再存在。

    3. **build 验证**:
       - `cd xingran-react-frontend && npm run build` 退出码 0

    4. **type-check 验证**:
       - `cd xingran-react-frontend && npm run type-check` 退出码 0

    5. **lint 验证**(可选但推荐):
       - `cd xingran-react-frontend && npm run lint` 无新增 error

    6. **手动核对汇总**:把 37-01 ~ 37-05 各 SUMMARY 中记录的手动核对结果汇总,确认 user/notice/workstations/buildings/devices/duty/pools/workorder/TargetSelector 共 9 个迁移点的 UI 行为全部"与迁移前一致"。任一不一致则不通过。

    把以上结果写入 37-06-SUMMARY.md,作为 Phase 37 整体验收报告。
  </action>

  <verify>
    <automated>cd xingran-react-frontend && echo "=== tree grep ===" && grep -rn "/system/departments/tree" src/ ; echo "=== SimpleDept/Department def ===" && grep -rn "interface SimpleDept\|export interface Department\b" src/ ; echo "=== type-check ===" && npm run type-check 2>&1 | tail -5 ; echo "=== build ===" && npm run build 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - CLI: `grep -rn "/system/departments/tree" xingran-react-frontend/src/` 输出中,**非排除项命中 = 0**(排除项 = `lib/dutyApi.ts` 的 canonical post + 可能的 role tree-select 不同端点 + dept 管理页 + AD 模块)。在 SUMMARY 中逐条列出生效的命中文件路径并标注是否排除项。
    - CLI: `grep -rn "interface SimpleDept" xingran-react-frontend/src/` 仅命中 `lib/dutyApi.ts` 一行
    - CLI: `grep -rn "export interface Department\b" xingran-react-frontend/src/components/shared/DepartmentTreeSelect.tsx` = 0 命中
    - CLI: `cd xingran-react-frontend && npm run type-check` 退出码 0
    - CLI: `cd xingran-react-frontend && npm run build` 退出码 0
    - Behavior(汇总):9 个迁移点的 UI 行为核对结果全部记录在 37-06-SUMMARY.md 且结论"与迁移前一致"
  </acceptance_criteria>

  <done>Phase 37 全量 grep 干净(排除项外 0 命中);类型唯一;build/type-check 双通过;9 个迁移点 UI 行为汇总一致。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (无新增) | 验证型任务,不修改认证/授权/加密路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-10 | Repudiation | Phase 37 整体验收证据 | mitigate | 37-06-SUMMARY.md 记录 grep/build/type-check/手动核对完整证据链,作为审计追溯依据 |
| T-37-SC | (n/a) | (无 npm 安装) | accept | 0 新依赖 |
</threat_model>

<verification>
- 全量 grep 干净(排除项外 0 命中)
- SimpleDept 唯一、DepartmentTreeSelect.Department 已删
- build + type-check 双通过
- 9 个迁移点 UI 行为核对汇总一致
</verification>

<success_criteria>
Phase 37 整体验收达标:数据层单一数据源(useDeptTree)、类型层去重(SimpleDept 唯一)、组件层职责正交(DepartmentTreeSelect 受控 / DeptTree 消费 hook)、转换函数归一(toFullPathTree + toShortNameDataNode)、UI 形态不变。
</success_criteria>

<output>
Create `.planning/phases/37-dept-select-unify/37-06-SUMMARY.md` when done — 作为 Phase 37 整体验收报告
</output>
