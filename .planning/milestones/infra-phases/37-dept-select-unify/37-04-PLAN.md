---
phase: 37-dept-select-unify
plan: 04
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - xingran-react-frontend/src/pages/network/devices/hooks/useDeviceData.ts
autonomous: true
requirements:
  - SUCC-consume-hook
must_haves:
  truths:
    - "useDeviceData 不再 post 自 fetch,改用 useDeptTree"
    - "devices 页部门树筛选行为与迁移前一致"
  artifacts:
    - path: "xingran-react-frontend/src/pages/network/devices/hooks/useDeviceData.ts"
      provides: "设备数据 hook,消费 useDeptTree"
      contains: "useDeptTree()"
---

<objective>
批 3 network 模块迁移:devices。

- `useDeviceData.ts` line 53-61 `loadDepartments` + `post('/system/departments/tree')` → `useDeptTree`

最简单的批次之一(纯 list 数据替换,无转换函数)。

Purpose: network 模块收敛。
Output: devices 消费 hook,删自 fetch。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-dept-select-unify/37-CONTEXT.md
@.planning/phases/37-dept-select-unify/37-PATTERNS.md
@.planning/phases/37-dept-select-unify/37-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: devices 迁移 — useDeviceData 去 fetch 改 useDeptTree</name>
  <files>xingran-react-frontend/src/pages/network/devices/hooks/useDeviceData.ts</files>

  <read_first>
    - xingran-react-frontend/src/pages/network/devices/hooks/useDeviceData.ts(line 53-61 loadDepartments + `post<Department[]>("/system/departments/tree", {})`、departments useState、返回值中的 departments/loadDepartments)
    - xingran-react-frontend/src/pages/network/devices/index.tsx(解构 departments/loadDepartments 的位置、useEffect 调用 loadDepartments 的位置)
    - xingran-react-frontend/src/hooks/useDeptTree.ts(canonical)
    - .planning/phases/37-dept-select-unify/37-PATTERNS.md `### pages/network/devices/hooks/useDeviceData.ts` 段落 line 465-482
  </read_first>

  <action>
    1. **useDeviceData.ts**:删 `departments` useState、删 `loadDepartments` useCallback(line 53-61,含 `post('/system/departments/tree')` + `setDepartments` + `handleApiError` 包裹)。
    2. 顶部新增 `import { useDeptTree } from "@/hooks/useDeptTree";`,函数体内加 `const { data: departments = [] } = useDeptTree();`。
    3. 返回对象保留 `departments` 字段(消费方依赖此名),**删除**返回对象中的 `loadDepartments`。
    4. 类型:若本地 `Department` interface 仅用于 departments state,改 import `SimpleDept` from `@/lib/dutyApi`。
    5. **index.tsx**:把 `loadDepartments` 从解构中删除;删除 useEffect 中调用 `loadDepartments()` 的行。departments 仍从 hook 返回值取。
    6. 如果 devices 页还使用了 `DeptTree` 组件做左侧筛选(由 37-01 Task 3 完成后已经消费 useDeptTree),无需额外处理;若 devices 有独立的部门下拉,核对其数据源也是 hook。
  </action>

  <verify>
    <automated>cd xingran-react-frontend && grep -n "/system/departments/tree" src/pages/network/devices/hooks/useDeviceData.ts | wc -l ; grep -n "loadDepartments" src/pages/network/devices/hooks/useDeviceData.ts src/pages/network/devices/index.tsx | wc -l ; grep -n "useDeptTree" src/pages/network/devices/hooks/useDeviceData.ts | wc -l ; npm run type-check 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - Source: `grep -c "/system/departments/tree" xingran-react-frontend/src/pages/network/devices/hooks/useDeviceData.ts` = 0
    - Source: `grep -rc "loadDepartments" xingran-react-frontend/src/pages/network/devices/` 总和 = 0
    - Source: `grep -c "useDeptTree" xingran-react-frontend/src/pages/network/devices/hooks/useDeviceData.ts` = 1
    - CLI: `cd xingran-react-frontend && npm run type-check` 退出码 0
    - Behavior(手动核对):网络设备列表 — 左侧/顶部部门筛选树显示与点击筛选行为与迁移前一致
  </acceptance_criteria>

  <done>devices 消费 useDeptTree;loadDepartments 调用链清零;type-check 通过;筛选行为不变。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (无新增) | 仅消费既有部门树端点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-07 | Information Disclosure | devices 部门筛选 | accept | 数据来源不变 |
| T-37-SC | (n/a) | (无 npm 安装) | accept | 0 新依赖 |
</threat_model>

<verification>
- `grep -rn "/system/departments/tree" xingran-react-frontend/src/pages/network/devices/` = 0
- `npm run type-check` 退出码 0
- 手动:devices 筛选树显示与勾选一致
</verification>

<success_criteria>
批 3 完成:devices 消费 useDeptTree;UI 行为不变。
</success_criteria>

<output>
Create `.planning/phases/37-dept-select-unify/37-04-SUMMARY.md` when done
</output>
