---
phase: 37-dept-select-unify
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - xingran-react-frontend/src/pages/system/user/hooks/useUserData.ts
  - xingran-react-frontend/src/pages/system/user/utils.tsx
  - xingran-react-frontend/src/pages/system/user/index.tsx
  - xingran-react-frontend/src/pages/system/notice/hooks/useTargetSelector.ts
autonomous: true
requirements:
  - SUCC-consume-hook
must_haves:
  truths:
    - "user/hooks/useUserData.ts 不再 post('/system/departments/tree'),改用 useDeptTree"
    - "user/utils.tsx convertDeptTreeData 删除,调用方改用 deptUtils.toShortNameDataNode(实测短名语义)"
    - "notice/hooks/useTargetSelector.ts 不再 get('/system/departments/tree'),改用 useDeptTree"
    - "user 与 notice 页面部门树展示/筛选/选择行为与迁移前一致"
  artifacts:
    - path: "xingran-react-frontend/src/pages/system/user/hooks/useUserData.ts"
      provides: "消费 useDeptTree 的 user 数据 hook"
      contains: "useDeptTree()"
    - path: "xingran-react-frontend/src/pages/system/notice/hooks/useTargetSelector.ts"
      provides: "消费 useDeptTree 的通知目标选择 hook"
      contains: "useDeptTree()"
  key_links:
    - from: "xingran-react-frontend/src/pages/system/user/utils.tsx"
      to: "xingran-react-frontend/src/utils/deptUtils.ts"
      via: "import { toShortNameDataNode }"
      pattern: "toShortNameDataNode"
---

<objective>
批 1 system 模块迁移:user + notice。

- `useUserData` 删自 fetch → `useDeptTree`
- `user/utils.tsx` 删 `convertDeptTreeData`(实测**短名**语义,归 `toShortNameDataNode`),**保留** `renderDeptTreeOptions`(Select 专用渲染,语义独立)
- `notice/useTargetSelector` 删 GET 自 fetch + `convertTree` → `useDeptTree` + `toShortNameDataNode`

**排除边界**:不动 `system/role`(不同端点 `/system/departments/tree-select`)、不动 `system/dept`(管理者本体)、不动 AD 模块。

Purpose: system 模块收敛到单一数据源。
Output: user/notice 两处消费 hook,本地转换函数删除。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-dept-select-unify/37-CONTEXT.md
@.planning/phases/37-dept-select-unify/37-PATTERNS.md
@.planning/phases/37-dept-select-unify/37-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: user 模块迁移 — useUserData 去 fetch + utils.tsx 删 convertDeptTreeData(保留 renderDeptTreeOptions)</name>
  <files>xingran-react-frontend/src/pages/system/user/hooks/useUserData.ts, xingran-react-frontend/src/pages/system/user/utils.tsx, xingran-react-frontend/src/pages/system/user/index.tsx</files>

  <read_first>
    - xingran-react-frontend/src/pages/system/user/hooks/useUserData.ts(line 73-81 loadDepartments + `post<Department[]>("/system/departments/tree")` + `setDepartments` + 返回值中的 `loadDepartments`/`departments`)
    - xingran-react-frontend/src/pages/system/user/utils.tsx(line 14-19 TreeNode 接口、line 24-39 convertDeptTreeData **实测短名语义** `title: node.deptName`、line 44-70 `renderDeptTreeOptions` Select 专用渲染)
    - xingran-react-frontend/src/pages/system/user/index.tsx(调用 useUserData 解构 departments/loadDepartments 的位置,以及 useEffect 调用 loadDepartments 的位置)
    - xingran-react-frontend/src/hooks/useDeptTree.ts(canonical hook)
    - xingran-react-frontend/src/utils/deptUtils.ts(Task 1 from 37-01 新增的 toShortNameDataNode)
    - .planning/phases/37-dept-select-unify/37-PATTERNS.md `### pages/system/user/hooks/useUserData.ts` 段落 line 301-322 + `### pages/system/user/utils.tsx` 段落 line 326-349
  </read_first>

  <action>
    1. **useUserData.ts**:删 `departments` useState、删 `loadDepartments` useCallback、删其内部 `post('/system/departments/tree')` 调用与 `handleApiError` 包裹(若 `handleApiError` 仅此处使用,可移除该 import;若其他地方仍用,保留)。
    2. **useUserData.ts**:顶部新增 `import { useDeptTree } from "@/hooks/useDeptTree";`,函数体内加 `const { data: departments = [] } = useDeptTree();`。返回对象保留 `departments` 字段(消费方依赖此名),**删除返回对象中的 `loadDepartments`**。
    3. **index.tsx**:把 `loadDepartments` 从解构中删除;删除 useEffect 中调用 `loadDepartments()` 的行(若有 eslint-disable 注释一并清理)。departments 仍从 hook 返回值取。
    4. **utils.tsx**:删除 `convertDeptTreeData`(line 24-39)和 `TreeNode` 接口(line 14-19)。改造其调用方(在 index.tsx 或其他使用 convertDeptTreeData 渲染 TreeSelect 的位置)改为 `import { toShortNameDataNode } from "@/utils/deptUtils";`,调用 `toShortNameDataNode(departments)`。
    5. **utils.tsx**:`renderDeptTreeOptions`(line 44-70)**保留不动**(Select 专用 `<Option>` 缩进渲染,语义独立,不是 TreeSelect DataNode 转换;PATTERNS.md 明确警告"不要误收敛")。
    6. 类型清理:若 useUserData.ts 本地 `Department` interface 仅用于 departments state,改 import `SimpleDept` from `@/lib/dutyApi` 并替换引用。
  </action>

  <verify>
    <automated>cd xingran-react-frontend && grep -n "/system/departments/tree" src/pages/system/user/hooks/useUserData.ts | wc -l ; grep -n "convertDeptTreeData" src/pages/system/user/utils.tsx | wc -l ; grep -n "useDeptTree" src/pages/system/user/hooks/useUserData.ts | wc -l ; grep -n "renderDeptTreeOptions" src/pages/system/user/utils.tsx | wc -l ; npm run type-check 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - Source: `grep -c "/system/departments/tree" xingran-react-frontend/src/pages/system/user/hooks/useUserData.ts` = 0
    - Source: `grep -c "loadDepartments" xingran-react-frontend/src/pages/system/user/hooks/useUserData.ts` = 0
    - Source: `grep -c "convertDeptTreeData" xingran-react-frontend/src/pages/system/user/utils.tsx` = 0
    - Source: `grep -c "renderDeptTreeOptions" xingran-react-frontend/src/pages/system/user/utils.tsx` ≥ 1(保留 Select 渲染器)
    - Source: `grep -c "useDeptTree" xingran-react-frontend/src/pages/system/user/hooks/useUserData.ts` = 1
    - Source: `grep -c "loadDepartments" xingran-react-frontend/src/pages/system/user/index.tsx` = 0
    - CLI: `cd xingran-react-frontend && npm run type-check` 退出码 0
    - Behavior(手动核对):用户管理页 — ①新增/编辑用户表单中"所属部门"TreeSelect 下拉显示的部门名(短名,不拼路径)与迁移前一致 ②用户列表按部门筛选行为一致
  </acceptance_criteria>

  <done>user 模块消费 useDeptTree;本地 convertDeptTreeData 删除,改用 toShortNameDataNode;renderDeptTreeOptions 保留;loadDepartments 调用链清零;type-check 通过;UI 行为不变。</done>
</task>

<task type="auto">
  <name>Task 2: notice 模块迁移 — useTargetSelector 删 GET 自 fetch + convertTree,改 useDeptTree + toShortNameDataNode</name>
  <files>xingran-react-frontend/src/pages/system/notice/hooks/useTargetSelector.ts</files>

  <read_first>
    - xingran-react-frontend/src/pages/system/notice/hooks/useTargetSelector.ts(line 43-67 loadDeptTree + GET `get<DeptNode[]>("/system/departments/tree")` + `convertTree` **短名 + 透传 ...node** + `Target` 接口含 roleName/username 等额外字段)
    - xingran-react-frontend/src/hooks/useDeptTree.ts(canonical)
    - xingran-react-frontend/src/utils/deptUtils.ts(toShortNameDataNode)
    - .planning/phases/37-dept-select-unify/37-PATTERNS.md `### pages/system/notice/hooks/useTargetSelector.ts` 段落 line 353-376
  </read_first>

  <action>
    1. 删除 `loadDeptTree` useCallback、`deptTree` useState、相关 `useEffect`(触发 loadDeptTree 的)。
    2. 顶部 `import { useDeptTree } from "@/hooks/useDeptTree";` + `import { toShortNameDataNode } from "@/utils/deptUtils";`。
    3. 函数体内加 `const { data: rawDept = [], isLoading: loadingDepts } = useDeptTree();`,然后用 useMemo 派生 `const deptTree = useMemo(() => toShortNameDataNode(rawDept) as Target[], [rawDept]);`。
       - 注意:旧 `convertTree` 输出 `{ ...node, title, key, value, children }`,即**透传了 node 的所有字段**(包括 isExternalOrg 等)。`toShortNameDataNode` 在 37-01 Task 1 实现时也应透传 `...node`(参照 PATTERNS.md line 115-118 签名建议,返回 `Array<{ title; key; value?; children?; isLeaf? }>` 是最小形状,实现时建议展开 `...node` 保留额外字段)。若 toShortNameDataNode 未透传 `...node`,此处需要 cast 或在调用前 merge,确保 Target[] 类型兼容。**类型上必须 type-check 通过**。
    4. `Target` 接口保留(roles/users 子树仍需要 roleName/username 字段)。
    5. roles/users 的 fetch 逻辑**完全不动**(只迁移 dept 部分)。
  </action>

  <verify>
    <automated>cd xingran-react-frontend && grep -n "/system/departments/tree" src/pages/system/notice/hooks/useTargetSelector.ts | wc -l ; grep -n "convertTree" src/pages/system/notice/hooks/useTargetSelector.ts | wc -l ; grep -n "useDeptTree" src/pages/system/notice/hooks/useTargetSelector.ts | wc -l ; npm run type-check 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - Source: `grep -c "/system/departments/tree" xingran-react-frontend/src/pages/system/notice/hooks/useTargetSelector.ts` = 0
    - Source: `grep -c "convertTree" xingran-react-frontend/src/pages/system/notice/hooks/useTargetSelector.ts` = 0(本地转换函数已删)
    - Source: `grep -c "useDeptTree" xingran-react-frontend/src/pages/system/notice/hooks/useTargetSelector.ts` = 1
    - CLI: `cd xingran-react-frontend && npm run type-check` 退出码 0
    - Behavior(手动核对):通知公告 — 新建通知表单中"目标部门"树选择显示(短名)与勾选行为与迁移前一致;目标角色/目标用户部分不受影响
  </acceptance_criteria>

  <done>notice/useTargetSelector 消费 useDeptTree + toShortNameDataNode;本地 convertTree 删除;roles/users 逻辑保留;type-check 通过;UI 行为不变。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (无新增) | 仅消费既有 `/system/departments/tree` 端点,无新攻击面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03 | Information Disclosure | user/notice 部门树渲染 | accept | 数据仍由后端 RBAC 过滤,前端只展示 |
| T-37-04 | Denial of Service | 多消费方共享缓存触发集中 refetch | accept | useDeptTree 默认 staleTime 5min + refetchOnWindowFocus:false,触发频率低于原各自 fetch |
| T-37-SC | (n/a) | (无 npm 安装) | accept | 0 新依赖 |
</threat_model>

<verification>
- `grep -rn "/system/departments/tree" xingran-react-frontend/src/pages/system/user/ xingran-react-frontend/src/pages/system/notice/` = 0 命中
- `npm run type-check` 退出码 0
- 手动:user 表单"所属部门"下拉、notice 表单"目标部门"树展示与迁移前一致
</verification>

<success_criteria>
批 1 完成:user + notice 两处消费 useDeptTree;本地重复转换函数删除;UI 行为不变。
</success_criteria>

<output>
Create `.planning/phases/37-dept-select-unify/37-02-SUMMARY.md` when done
</output>
