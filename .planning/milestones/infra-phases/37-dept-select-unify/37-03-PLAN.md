---
phase: 37-dept-select-unify
plan: 03
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - xingran-react-frontend/src/pages/operations/workstations/hooks/useWorkstationData.ts
  - xingran-react-frontend/src/pages/operations/buildings/useDepartmentTree.tsx
  - xingran-react-frontend/src/pages/operations/buildings/useDepartmentData.ts
  - xingran-react-frontend/src/pages/operations/buildings/index.tsx
autonomous: true
requirements:
  - SUCC-consume-hook
  - SUCC-high-risk-workstations
must_haves:
  truths:
    - "workstations useWorkstationData 不再 post 自 fetch,改用 useDeptTree"
    - "workstations 的 deptTreeData 仍为全路径版本(toFullPathTree),orgTreeData 仍经 trimTitleToLastSegment 反向裁剪为短名"
    - "toFullPathTree 透传 isExternalOrg,filterExternalOrgDepts 不会把树过滤为空"
    - "buildings 的 useDepartmentTree + useDepartmentData 合并到 useDeptTree"
    - "workstations 与 buildings 页部门树/下拉显示与迁移前一致"
  artifacts:
    - path: "xingran-react-frontend/src/pages/operations/workstations/hooks/useWorkstationData.ts"
      provides: "工位数据 hook,消费 useDeptTree + 双向语义保留"
      contains: "useDeptTree()"
    - path: "xingran-react-frontend/src/pages/operations/buildings/index.tsx"
      provides: "楼宇页直接消费 useDeptTree"
      contains: "useDeptTree()"
  key_links:
    - from: "xingran-react-frontend/src/pages/operations/workstations/hooks/useWorkstationData.ts"
      to: "xingran-react-frontend/src/utils/deptUtils.ts"
      via: "toFullPathTree + trimTitleToLastSegment + filterExternalOrgDepts"
      pattern: "toFullPathTree"
---

<objective>
批 2 operations 模块迁移:**workstations(高风险)+ buildings(合并两个 hook)**。

**workstations 高风险点(D-LOCKED 关注)**:`buildTreeData` 输出全路径,页面层用 `trimTitleToLastSegment` 反向裁剪为短名(用于"所属部门"下拉)。这是**双向语义**,迁移后必须保持:
- `deptTreeData` = 全路径(toFullPathTree)
- `orgTreeData` = filterExternalOrgDepts(deptTreeData) → trimTitleToLastSegment → 短名

`toFullPathTree` **必须透传 isExternalOrg**(否则 filterExternalOrgDepts 会把整棵树过滤为空,PATTERNS.md line 125 警告)。

**buildings 合并**:CONTEXT `<decisions>` Claude's Discretion 第 4 条允许合并 useDepartmentTree + useDepartmentData 到 useDeptTree。本 plan 采用方案 A:删两个 hook 文件,buildings/index.tsx 直接消费 useDeptTree + 本地 useMemo 计算 deptMap/getOrgName。

**排除边界**:不动 floors(若 floors 有独立部门 fetch,本次不顺带处理,留批 5 grep 验证后再决定)。

Purpose: operations 模块收敛 + 消除 buildings 双 hook 重复。
Output: workstations 高风险迁移完成、buildings 合并 hook。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-dept-select-unify/37-CONTEXT.md
@.planning/phases/37-dept-select-unify/37-PATTERNS.md
@.planning/phases/37-dept-select-unify/37-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: workstations 高风险迁移 — useWorkstationData 改 useDeptTree,保留 buildTreeData+trimTitleToLastSegment 双向语义</name>
  <files>xingran-react-frontend/src/pages/operations/workstations/hooks/useWorkstationData.ts</files>

  <read_first>
    - xingran-react-frontend/src/pages/operations/workstations/hooks/useWorkstationData.ts(line 14-20 DepartmentNode 本地接口、line 33-45 DeptTreeNode 本地类型、line 75-97 buildTreeData 全路径 + isExternalOrg 透传 + line 78 注释警告、line 99-109 loadDeptOptions 调用 `post("/system/departments/tree", {})`、setDeptTreeData、orgTreeData useMemo 派生处)
    - xingran-react-frontend/src/pages/operations/workstations/index.tsx(消费 useWorkstationData 的 deptTreeData/orgTreeData 渲染"所属机构"和"所属部门"两个下拉的位置)
    - xingran-react-frontend/src/hooks/useDeptTree.ts(canonical)
    - xingran-react-frontend/src/utils/deptUtils.ts(toFullPathTree from 37-01 Task 1、filterExternalOrgDepts、trimTitleToLastSegment)
    - .planning/phases/37-dept-select-unify/37-PATTERNS.md `### pages/operations/workstations/hooks/useWorkstationData.ts` 段落 line 381-423(双向语义保留要求)
    - .planning/phases/37-dept-select-unify/37-CONTEXT.md `<specifics>` "workstations 特殊语义(高风险点)"
  </read_first>

  <action>
    1. 顶部新增 `import { useDeptTree } from "@/hooks/useDeptTree";`、`import { toFullPathTree, filterExternalOrgDepts, trimTitleToLastSegment } from "@/utils/deptUtils";`、`import type { SimpleDept } from "@/lib/dutyApi";`。
    2. 删除 `loadDeptOptions`(line 99-109 内部 `post("/system/departments/tree")` 调用),保留 `setDeptTreeData` 的 setter 若仍被其他地方用,否则一并清理。删 `buildTreeData`(line 75-97)、删 `DepartmentNode` 本地接口(line 14-20)。
    3. 函数体内加 `const { data: rawDept = [], isLoading } = useDeptTree();`。
    4. 派生 `deptTreeData`(全路径版本):
       ```
       const deptTreeData = useMemo(() => toFullPathTree(rawDept), [rawDept]);
       ```
       **关键**:确认 37-01 Task 1 实现的 `toFullPathTree` 透传了 `isExternalOrg`(若没透传,必须回 37-01 Task 1 修正,本任务不允许绕过——否则 orgTreeData 派生时 filterExternalOrgDepts 会得到空树)。
    5. 派生 `orgTreeData`(外部机构子集 → 反向裁剪为短名):保持原派生链顺序 `filterExternalOrgDepts(deptTreeData)` 再 `trimTitleToLastSegment(...)`:
       ```
       const orgTreeData = useMemo(
         () => trimTitleToLastSegment(filterExternalOrgDepts(deptTreeData)),
         [deptTreeData]
       );
       ```
       **注意**:原代码可能用不同的变量名承载这两个版本。仔细对照原 useWorkstationData 的 deptTreeData/orgTreeData 命名与用法,**输出字段名保持不变**(index.tsx 等消费方依赖这些名字)。
    6. "所属机构"下拉消费 deptTreeData(全路径);"所属部门"下拉消费 trimTitleToLastSegment 后的短名版本——**UI 行为必须与迁移前一致**。
    7. 类型:本文件局部 `DeptTreeNode`(line 33-45)若仅用于 buildTreeData,可删除;若仍被 setState 等引用,改用 SimpleDept 或保留作 toFullPathTree 输出 cast 目标。
    8. 加载态:用 `isLoading` 替代原 loading 相关 state(若有)。
  </action>

  <verify>
    <automated>cd xingran-react-frontend && grep -n "/system/departments/tree" src/pages/operations/workstations/hooks/useWorkstationData.ts | wc -l ; grep -n "buildTreeData" src/pages/operations/workstations/hooks/useWorkstationData.ts | wc -l ; grep -n "useDeptTree\|toFullPathTree\|trimTitleToLastSegment\|filterExternalOrgDepts" src/pages/operations/workstations/hooks/useWorkstationData.ts | wc -l ; npm run type-check 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - Source: `grep -c "/system/departments/tree" xingran-react-frontend/src/pages/operations/workstations/hooks/useWorkstationData.ts` = 0
    - Source: `grep -c "buildTreeData" xingran-react-frontend/src/pages/operations/workstations/hooks/useWorkstationData.ts` = 0
    - Source: `grep -cE "toFullPathTree|trimTitleToLastSegment|filterExternalOrgDepts|useDeptTree" xingran-react-frontend/src/pages/operations/workstations/hooks/useWorkstationData.ts` ≥ 4(四个标识符都要出现,双向语义链完整)
    - CLI: `cd xingran-react-frontend && npm run type-check` 退出码 0
    - Behavior(手动核对,**批 2 关键验收**):工位管理页 — ①"所属机构"下拉显示**全路径**(如"分公司本部 / 人力资源部",顶级节点直接显示其名)与迁移前一致 ②"所属部门"下拉显示**短名**(只显示末段)与迁移前一致 ③外部机构节点(isExternalOrg=1)被正确过滤进 orgTreeData,不出现"树为空"回归
  </acceptance_criteria>

  <done>workstations 消费 useDeptTree;双向语义(deptTreeData=全路径、orgTreeData=filter+trim 短名)保留;isExternalOrg 透传链完整;buildTreeData/loadDeptOptions 删除;type-check 通过;两个下拉显示与迁移前一致。</done>
</task>

<task type="auto">
  <name>Task 2: buildings 合并 useDepartmentTree + useDepartmentData 到 useDeptTree(方案 A:删两 hook,页内直接消费)</name>
  <files>xingran-react-frontend/src/pages/operations/buildings/useDepartmentTree.tsx, xingran-react-frontend/src/pages/operations/buildings/useDepartmentData.ts, xingran-react-frontend/src/pages/operations/buildings/index.tsx</files>

  <read_first>
    - xingran-react-frontend/src/pages/operations/buildings/useDepartmentTree.tsx(line 27-44 loadDepartments + post + convertToTreeData、line 47-64 deptMap/getOrgName 派生)
    - xingran-react-frontend/src/pages/operations/buildings/useDepartmentData.ts(line 26-48 与上一项几乎完全重复的实现)
    - xingran-react-frontend/src/pages/operations/buildings/index.tsx(import 两个 hook 的位置、解构 departments/deptMap/getOrgName 的位置、JSX 中使用点)
    - xingran-react-frontend/src/hooks/useDeptTree.ts(canonical)
    - .planning/phases/37-dept-select-unify/37-PATTERNS.md `### pages/operations/buildings/useDepartmentTree.tsx` + `### pages/operations/buildings/useDepartmentData.ts` 段落 line 426-461
  </read_first>

  <action>
    采用 CONTEXT Claude's Discretion 第 4 条的**方案 A**(推荐):两个 hook 文件删除,buildings/index.tsx 直接消费 useDeptTree + 本地 useMemo 计算 deptMap/getOrgName。

    1. **buildings/index.tsx**:顶部新增 `import { useDeptTree } from "@/hooks/useDeptTree";` + `import { findDeptNode } from "@/utils/deptUtils";`(getOrgName 用 findDeptNode 实现)。
    2. **buildings/index.tsx**:函数体内加 `const { data: departments = [], isLoading } = useDeptTree();`,然后用 useMemo 派生:
       - `deptMap`:从 departments 平铺为 `Record<string, SimpleDept>`(原 useDepartmentTree line 47-55 逻辑搬过来)
       - `getOrgName(id)`:用 `findDeptNode(departments, id)?.deptName ?? ''`(原 useDepartmentTree line 57-64 的等价实现)
    3. **buildings/index.tsx**:删除对 `useDepartmentTree()` 和 `useDepartmentData()` 的调用与解构。删除 import 这两个 hook 的语句。
    4. **删除文件** `useDepartmentTree.tsx` 和 `useDepartmentData.ts`(若 buildings/index.tsx 是唯一消费者,确认全项目 grep 无其他引用后删除)。
       - **删除前必做**:`grep -rn "useDepartmentTree\|useDepartmentData" xingran-react-frontend/src/` 确认只有 buildings/index.tsx 引用。若有其他文件引用,本任务改为"保留 hook 文件但内部改调 useDeptTree"(方案 B),并停止删除文件。
    5. 楼宇列表/表单中"所属机构"显示与筛选行为与迁移前一致(原本就是 post + 透传,改 hook 后数据相同)。
  </action>

  <verify>
    <automated>cd xingran-react-frontend && grep -rn "useDepartmentTree\|useDepartmentData" src/ ; grep -n "/system/departments/tree" src/pages/operations/buildings/ -r | wc -l ; grep -n "useDeptTree" src/pages/operations/buildings/index.tsx | wc -l ; npm run type-check 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - Source: `grep -rn "useDepartmentTree\|useDepartmentData" xingran-react-frontend/src/` = 0 命中(若方案 A 完成删除)或仅剩 hook 文件内部(若改方案 B)
    - Source: `grep -rc "/system/departments/tree" xingran-react-frontend/src/pages/operations/buildings/` 总和 = 0
    - Source: `grep -c "useDeptTree" xingran-react-frontend/src/pages/operations/buildings/index.tsx` ≥ 1
    - CLI: `cd xingran-react-frontend && npm run type-check` 退出码 0
    - Behavior(手动核对):楼宇管理页 — ①楼宇列表"所属机构"列显示的部门名与迁移前一致 ②新增/编辑楼宇表单"所属机构"下拉与迁移前一致 ③筛选行为一致
  </acceptance_criteria>

  <done>buildings 消费 useDeptTree;两个重复 hook 文件删除(方案 A)或内部改调 hook(方案 B);deptMap/getOrgName 派生逻辑保留;type-check 通过;UI 行为不变。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (无新增) | 仅消费既有部门树端点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05 | Tampering | workstations orgTreeData 双向语义链 | mitigate | 单独 acceptance 验证"所属机构/所属部门"下拉;toFullPathTree 必须透传 isExternalOrg(37-01 Task 1 acceptance 已锁定) |
| T-37-06 | Information Disclosure | buildings deptMap 派生 | accept | 数据来源不变,仅缓存方式优化 |
| T-37-SC | (n/a) | (无 npm 安装) | accept | 0 新依赖 |
</threat_model>

<verification>
- `grep -rn "/system/departments/tree" xingran-react-frontend/src/pages/operations/workstations/ xingran-react-frontend/src/pages/operations/buildings/` = 0
- workstations 双向语义链完整(toFullPathTree + filterExternalOrgDepts + trimTitleToLastSegment 三者都在)
- buildings 双 hook 合并完成
- `npm run type-check` 退出码 0
- 手动:工位两个下拉、楼宇列表与表单显示一致
</verification>

<success_criteria>
批 2 完成:workstations 高风险双向语义保留迁移;buildings 合并两个重复 hook;UI 行为不变。
</success_criteria>

<output>
Create `.planning/phases/37-dept-select-unify/37-03-SUMMARY.md` when done
</output>
