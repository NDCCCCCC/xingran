---
phase: 30
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - xingran-react-frontend/vite.config.ts
  - xingran-react-frontend/package.json
  - xingran-react-frontend/package-lock.json
  - xingran-react-frontend/.gitignore
  - scripts/check-bundle.sh (NEW — asserts vendor chunk layout)
  - xingran-react-frontend/src/router/componentLoader.tsx (audit, no functional change expected)
autonomous: true
requirements:
  - PERF-INFRA-01
  - PERF-INFRA-02
  - PERF-INFRA-03
  - PERF-INFRA-04
user_setup: []

must_haves:
  truths:
    - "Running `npm run build` with ANALYZE=true produces a `dist/stats.html` bundle analyzer report"
    - "Running `npm run build` outputs separate vendor chunks for react, antd, echarts, three"
    - "Each chunk gzip size is reported and visible in build output"
    - "chunkSizeWarningLimit is set to 500KB so the 500KB gzip budget is enforced by the build"
    - "All non-first-screen routes in src/pages/**/index.tsx load via createLazyComponent (verified by audit; D-08 already satisfied)"
    - "scripts/check-bundle.sh asserts the four required vendor chunks exist after build"
  artifacts:
    - path: "xingran-react-frontend/vite.config.ts"
      provides: "Vite config with visualizer + manualChunks"
      contains: "rollup-plugin-visualizer"
    - path: "xingran-react-frontend/dist/stats.html"
      provides: "Bundle composition report"
    - path: "xingran-react-frontend/.gitignore"
      provides: "Ignore generated dist/stats.html"
  key_links:
    - from: "vite.config.ts"
      to: "rollup-plugin-visualizer"
      via: "plugins array"
      pattern: "visualizer\\("
    - from: "vite.config.ts"
      to: "manualChunks"
      via: "build.rollupOptions.output"
      pattern: "manualChunks.*vendor-(react|antd|echarts|three)"
    - from: "scripts/check-bundle.sh"
      to: "dist/assets/"
      via: "asserts chunk filenames match vendor-*"
      pattern: "vendor-(react|antd|echarts|three)"
---

<objective>
Establish performance infrastructure baseline for the frontend: integrate a bundle analyzer, refine manualChunks to split react/antd/echarts/three into independent vendor chunks, tighten chunk size warning limit to the 500KB gzip budget, and capture a baseline bundle report.

Purpose: All other waves (heavy libs, React Query, render) depend on this analyzer + chunk layout to measure improvements against. Without this, subsequent changes cannot be verified quantitatively per D-04.

Output: Modified `vite.config.ts` with `rollup-plugin-visualizer`, refined `manualChunks`, 500KB warning threshold, plus an initial `dist/stats.html` snapshot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-js/30-CONTEXT.md
@.planning/STATE.md

# Source of truth files
@xingran-react-frontend/vite.config.ts
@xingran-react-frontend/package.json
@.planning/phases/30-js/30-CONTEXT.md (D-01 single-phase structure, D-02 analyzer, D-03 wave-1-of-4, D-04 lighthouse baseline, D-05 budget, D-07 chunk strategy, D-08 lazy-loading foundation)
</context>

<interfaces>
From xingran-react-frontend/vite.config.ts (current):
- `build.rollupOptions.output.manualChunks` already chunks react, antd, three, utils, ops-module, vendor.
- `optimizeDeps.include` includes `sm-crypto`, `react`, `react-dom`, `react-router-dom`.
- Plugins: `react()`, `tailwindcss()`, `removeConsole()`.
- Server proxy to `http://localhost:9000` for `/api` and `/uploads`.
- `chunkSizeWarningLimit: 1000` (current, will be tightened).

From xingran-react-frontend/package.json (relevant):
- `vite ^7.2.4`
- `react ^19.2.0`, `react-dom ^19.2.0`, `react-router-dom ^7.10.1`
- `antd ^6.1.1`
- `echarts ^6.0.0`, `echarts-for-react ^3.0.5`
- `three ^0.182.0`, `@react-three/fiber ^9.5.0`, `@react-three/drei ^10.7.7`
- `xlsx ^0.18.5`
- `@uiw/react-md-editor ^4.0.11`
- Build script: `"build": "tsc -b && vite build"`
- Lint script: `"lint": "eslint ."`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install rollup-plugin-visualizer and update package.json</name>
  <files>xingran-react-frontend/package.json, xingran-react-frontend/package-lock.json</files>
  <read_first>
    - xingran-react-frontend/package.json
    - xingran-react-frontend/vite.config.ts
  </read_first>
  <action>
Run `cd xingran-react-frontend && npm install --save-dev rollup-plugin-visualizer@^5.13.1` to add the bundle analyzer as a dev dependency. Use a minor that is compatible with Vite 7 / Rollup 4 (5.13.x or 5.14.x is appropriate).

Add a new npm script in package.json:
- `"analyze": "cross-env ANALYZE=true vite build"` (or `"build:analyze": "ANALYZE=true vite build"` on POSIX) so the visualizer output is opt-in and does not bloat the default `build` script. The default `build` script must remain unchanged so CI does not generate stats.html.

Confirm `package.json` is valid JSON after edit. Confirm `package-lock.json` is updated.
  </action>
  <acceptance_criteria>
    - `xingran-react-frontend/package.json` contains the string `"rollup-plugin-visualizer"` in `devDependencies`
    - `xingran-react-frontend/package.json` contains an `analyze` or `build:analyze` script that sets `ANALYZE=true` and runs `vite build`
    - The default `build` script is unchanged: still `"tsc -b && vite build"`
    - `cd xingran-react-frontend && npm ls rollup-plugin-visualizer --depth=0` exits 0 and prints the installed version
  </acceptance_criteria>
  <verify>
    <automated>cd xingran-react-frontend && npm ls rollup-plugin-visualizer --depth=0</automated>
  </verify>
  <done>rollup-plugin-visualizer installed and listed in devDependencies; new analyze script available; default build untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire visualizer + refine manualChunks + tighten chunkSizeWarningLimit</name>
  <files>xingran-react-frontend/vite.config.ts</files>
  <read_first>
    - xingran-react-frontend/vite.config.ts (current)
    - .planning/phases/30-js/30-CONTEXT.md (D-07 strategy)
  </read_first>
  <action>
Modify `xingran-react-frontend/vite.config.ts`:

1. Add at top of file (after existing imports, after the path import):
   `import { visualizer } from 'rollup-plugin-visualizer';`

2. Replace the `plugins` array. Add a conditional visualizer that ONLY runs when env var `ANALYZE=true` is set (e.g. via `process.env.ANALYZE` check) so the default build is unaffected. Use the gzip/brotli size options and a treemap template:
   ```
   plugins: [
     react(),
     tailwindcss(),
     removeConsole(),
     ...(process.env.ANALYZE === 'true' ? [visualizer({
       filename: 'dist/stats.html',
       gzipSize: true,
       brotliSize: true,
       template: 'treemap',
     })] : []),
   ]
   ```

3. Rewrite `build.rollupOptions.output.manualChunks` to align with D-07 chunk-naming. Replace the existing inline-if-chain with a function so we have explicit control:
   - Return `'vendor-react'` for `react-dom`, `react-router`, `scheduler`, and any path containing `/react/` (but NOT `@react-three`, `@react-spring/three`, etc.). This means: match `/node_modules/react/`, `/node_modules/react-dom/`, `/node_modules/react-router/`, `/node_modules/scheduler/`, etc.
   - Return `'vendor-three'` for paths containing `three`, `@react-three`, `@react-spring/three`, `maath`. Note: `three` substring also matches `three-vendor` content from existing rule — but since this new function runs first, OK.
   - Return `'vendor-antd'` for paths containing `antd`, `@ant-design`, or `/rc-` (rc-* utility packages AntD depends on).
   - Return `'vendor-echarts'` for paths containing `echarts` or `zrender`.
   - Return `'vendor-utils'` for paths containing `dayjs`, `axios`, or `sm-crypto` (D-06: keep sm-crypto sync).
   - Return `'vendor-commons'` for everything else in `node_modules`.
   - Return `undefined` for first-party code (so they stay as route-level chunks).

   Concretely:
   ```
   manualChunks(id) {
     if (!id.includes('node_modules')) return undefined;
     if (id.includes('three') || id.includes('@react-three') || id.includes('@react-spring/three') || id.includes('maath')) return 'vendor-three';
     if (id.includes('echarts') || id.includes('zrender')) return 'vendor-echarts';
     if (id.includes('antd') || id.includes('@ant-design') || id.includes('/rc-')) return 'vendor-antd';
     if (id.includes('/react-dom/') || id.includes('/react-router') || id.includes('/scheduler/') || (id.includes('/react/') && !id.includes('@react-'))) return 'vendor-react';
     if (id.includes('dayjs') || id.includes('axios') || id.includes('sm-crypto')) return 'vendor-utils';
     return 'vendor-commons';
   }
   ```
   Order matters: `vendor-three` and `vendor-react` checks use overlapping substrings — check three-family first so `@react-three/fiber` lands in `vendor-three`, then antd, then react core.

4. Set `chunkSizeWarningLimit: 500` (KB), per D-05 / D-07 — the 500KB gzip budget.

5. Keep `optimizeDeps.include` unchanged (sm-crypto and react must keep ESM compatibility).

6. Keep the `server.proxy` and `server.port: 4000` configuration unchanged.

After editing, the file must remain a valid ESM module exporting `defineConfig({...})`.
  </action>
  <acceptance_criteria>
    - `xingran-react-frontend/vite.config.ts` contains the import `import { visualizer } from 'rollup-plugin-visualizer'`
    - `vite.config.ts` contains the literal `chunkSizeWarningLimit: 500`
    - `vite.config.ts` contains a `manualChunks` function that returns the strings `vendor-react`, `vendor-antd`, `vendor-echarts`, `vendor-three`, `vendor-utils`, and `vendor-commons`
    - `vite.config.ts` contains a conditional `process.env.ANALYZE === 'true'` check around the visualizer
    - The file remains valid TS/ESM (no syntax errors)
  </acceptance_criteria>
  <verify>
    <automated>cd xingran-react-frontend && npx tsc -p tsconfig.node.json --noEmit</automated>
  </verify>
  <done>vite.config.ts has visualizer (opt-in via ANALYZE env), refined manualChunks, and 500KB chunk warning threshold. First-party files unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Verify build produces stats.html + capture baseline metrics</name>
  <files>xingran-react-frontend/.gitignore</files>
  <read_first>
    - xingran-react-frontend/.gitignore
    - xingran-react-frontend/vite.config.ts
    - xingran-react-frontend/package.json
  </read_first>
  <action>
1. Run the analyzer build to verify it works end-to-end and capture the baseline bundle composition:
   `cd xingran-react-frontend && npm run analyze` (this sets ANALYZE=true and runs `vite build`).

2. Verify the output:
   - `dist/stats.html` exists and is non-empty (the analyzer report).
   - `dist/` contains chunk files with names matching `vendor-react`, `vendor-antd`, `vendor-echarts`, `vendor-three`, `vendor-utils`, `vendor-commons` (the naming may include hash suffix, e.g. `vendor-react-abc123.js`).
   - Build output reports gzip size for each chunk.

3. Record the baseline numbers for comparison against Wave 2/3/4:
   - Total initial JS (sum of entry chunk + first-frame chunks) gzip
   - vendor-react gzip
   - vendor-antd gzip
   - vendor-echarts gzip
   - vendor-three gzip
   - vendor-utils gzip
   - vendor-commons gzip

   Write the numbers into `.planning/phases/30-js/baseline-bundle.md` as a markdown table with columns: chunk name | raw size | gzip size. Use the file Write tool to create this file (it is a planning artifact, not source code). The file path is `.planning/phases/30-js/baseline-bundle.md`. Format:
   ```
   # Phase 30 Wave 1 — Bundle Baseline

   Captured: {date}
   Build command: `npm run analyze`

   | chunk | raw (KB) | gzip (KB) |
   |-------|---------:|----------:|
   | vendor-react | ... | ... |
   | vendor-antd | ... | ... |
   | vendor-echarts | ... | ... |
   | vendor-three | ... | ... |
   | vendor-utils | ... | ... |
   | vendor-commons | ... | ... |
   | (entry index-*.js) | ... | ... |
   | **total initial JS** | ... | ... |
   ```

4. Add `dist/stats.html` to `.gitignore` so the analyzer report is not committed. Verify `.gitignore` ends with a newline. If `.gitignore` does not already contain a `dist/` entry, add it.

5. Run `cd xingran-react-frontend && npm run build` (without ANALYZE) to confirm the default build still succeeds without producing stats.html.
  </action>
  <acceptance_criteria>
    - `xingran-react-frontend/dist/stats.html` exists after `npm run analyze`
    - `dist/` contains chunk files matching `vendor-react*`, `vendor-antd*`, `vendor-echarts*`, `vendor-three*`
    - `.planning/phases/30-js/baseline-bundle.md` exists and contains a markdown table with the six vendor chunks and a total row
    - `.gitignore` contains `dist/` or `dist/stats.html` (so generated artifacts are excluded)
    - `npm run build` (without ANALYZE) exits 0 and does NOT create `dist/stats.html`
  </acceptance_criteria>
  <verify>
    <automated>cd xingran-react-frontend && test -f dist/stats.html && ls dist/assets/ | grep -E "vendor-(react|antd|echarts|three)" | wc -l</automated>
  </verify>
  <done>Bundle analyzer produces stats.html on demand; six named vendor chunks present in dist/; baseline numbers recorded for comparison; default build unaffected.</done>
</task>

<task type="auto">
  <name>Task 4: Audit route-level lazy loading (D-08) and add bundle-check script (D-02 sample)</name>
  <files>
    scripts/check-bundle.sh (NEW),
    xingran-react-frontend/src/router/componentLoader.tsx (read-only audit)
  </files>
  <read_first>
    - xingran-react-frontend/src/router/componentLoader.tsx (already uses import.meta.glob + lazy)
    - xingran-react-frontend/src/router/DynamicRoutes.tsx (already uses createLazyComponent)
    - xingran-react-frontend/src/router/routeGenerator.ts (generates menu routes)
    - .planning/phases/30-js/30-CONTEXT.md (D-02 sample route, D-08 route lazy)
  </read_first>
  <action>
1. **Audit D-08 (route-level lazy loading) — already implemented, just verify:**

   Run these checks to confirm the existing implementation satisfies D-08:

   ```
   # Check 1: ComponentLoader uses Vite glob with eager:false (deferred code splitting)
   grep -n "import.meta.glob" xingran-react-frontend/src/router/componentLoader.tsx
   # Expect: import.meta.glob<{ default: ComponentType }>('/src/pages/**/index.tsx', { eager: false })

   # Check 2: createLazyComponent uses React.lazy
   grep -n "return lazy" xingran-react-frontend/src/router/componentLoader.tsx
   # Expect: return lazy(moduleLoader);

   # Check 3: DynamicRoutes uses createLazyComponent (not static import for routes)
   grep -n "createLazyComponent" xingran-react-frontend/src/router/DynamicRoutes.tsx
   # Expect: import { createLazyComponent } from './componentLoader';

   # Check 4: No top-level static imports of /src/pages/* in any router file
   grep -rn "from '@/pages" xingran-react-frontend/src/router/ | grep -v "componentLoader\|routeConfig"
   # Expect: 0 matches (router files don't statically import page components)
   ```

   If all 4 checks pass, D-08 is satisfied. Document the result in the SUMMARY.

   If any check fails, add a fix:
   - Check 4 failure (router statically imports a page): refactor that import to use `createLazyComponent`
   - Check 1/2/3 failures: the lazy infrastructure is missing; this is unlikely given current state

2. **Add bundle check script (D-02 sample demonstration):**

   Create `scripts/check-bundle.sh` at the project root (not under `xingran-react-frontend/`). The script asserts that after `npm run build`, the four required vendor chunks are present:

   ```bash
   #!/usr/bin/env bash
   # scripts/check-bundle.sh
   # Asserts that the build produced the required vendor chunks.
   # Used to verify D-07 chunk strategy is working after Vite config changes.

   set -euo pipefail

   DIST="xingran-react-frontend/dist/assets"
   REQUIRED=("vendor-react" "vendor-antd" "vendor-echarts" "vendor-three")
   MISSING=()

   for chunk in "${REQUIRED[@]}"; do
     if ! ls "$DIST"/${chunk}-*.js 2>/dev/null | head -1 | grep -q .; then
       MISSING+=("$chunk")
     fi
   done

   if [ ${#MISSING[@]} -gt 0 ]; then
     echo "ERROR: Missing required vendor chunks: ${MISSING[*]}"
     echo "Expected chunks: ${REQUIRED[*]}"
     echo "Actual files in $DIST:"
     ls "$DIST" | grep vendor || echo "(no vendor-* chunks found)"
     exit 1
   fi

   echo "OK: All required vendor chunks present:"
   for chunk in "${REQUIRED[@]}"; do
     size=$(ls -la "$DIST"/${chunk}-*.js 2>/dev/null | head -1 | awk '{print $5}')
     echo "  $chunk: ${size} bytes"
   done
   ```

   Make it executable: `chmod +x scripts/check-bundle.sh` (or just commit it; on Windows Git Bash, the executable bit is preserved in git but Windows doesn't enforce it).

3. **Verify the script works:**

   ```
   cd xingran-react-frontend && npm run build
   bash scripts/check-bundle.sh
   ```

   Expect: `OK: All required vendor chunks present` with all four chunk sizes printed.

   If the script fails because chunk names differ (e.g., due to manualChunks returning different strings), update the `REQUIRED` array to match the actual chunk names produced by Wave 1's `manualChunks` function.
  </action>
  <acceptance_criteria>
    - All 4 audit checks pass (existing `createLazyComponent` infrastructure satisfies D-08; documented in SUMMARY)
    - `scripts/check-bundle.sh` exists, is executable, and asserts the four required vendor chunks (`vendor-react`, `vendor-antd`, `vendor-echarts`, `vendor-three`) exist in `xingran-react-frontend/dist/assets/`
    - Running `cd xingran-react-frontend && npm run build && bash scripts/check-bundle.sh` exits 0 and prints `OK: All required vendor chunks present`
    - The script's `MISSING` array correctly reports any chunk that is not produced (negative test)
  </acceptance_criteria>
  <verify>
    <automated>cd xingran-react-frontend && npm run build >/dev/null 2>&1 && bash scripts/check-bundle.sh 2>&1 | head -10</automated>
  </verify>
  <done>D-08 audit confirms all routes use createLazyComponent; scripts/check-bundle.sh enforces the four required vendor chunks; reproducible verification path established for Waves 2-4.</done>
</task>

</tasks>

<verification>
- `cd xingran-react-frontend && npm run analyze` exits 0
- `cd xingran-react-frontend && npm run build` exits 0 (no stats.html)
- `cd xingran-react-frontend && npm run lint` exits 0 (no rule changes in this plan)
- `bash scripts/check-bundle.sh` exits 0 (asserts four required vendor chunks)
- Manual: open `dist/stats.html` in browser — treemap shows all chunks with gzip/brotli sizes
- Manual: Lighthouse run on `http://localhost:4000/login` (desktop preset) — capture baseline LCP/FCP/TTI; save to `.planning/phases/30-js/lighthouse/wave-1-desktop.json` (gitignored)
</verification>

<success_criteria>
- visualizer is opt-in via ANALYZE env var (per D-02)
- vendor chunks renamed per D-07: vendor-react, vendor-antd, vendor-echarts, vendor-three
- chunkSizeWarningLimit = 500 (KB) per D-05
- baseline numbers captured in `.planning/phases/30-js/baseline-bundle.md`
- D-08 (route lazy loading) audit confirms existing `createLazyComponent` infrastructure covers all routes
- `scripts/check-bundle.sh` provides reproducible vendor-chunk verification
- no backend Go files modified
</success_criteria>

<output>
Create `.planning/phases/30-js/30-01-SUMMARY.md` when done
</output>
