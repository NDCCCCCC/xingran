---
phase: 77-operations-agent-server
plan: 05
type: execute
wave: 2
depends_on: ["77-04"]
files_modified:
  - internal/agent/server/config_account_77_05_test.go
  - internal/agent/server/handlers_77_05_test.go
  - internal/agent/server/config.go
  - internal/agent/server/account_manager.go
  - internal/agent/server/subprocess_stub_test.go
autonomous: true
requirements: [BLOCK-02]

must_haves:
  truths:
    - "按 D-08 切分,本 plan 交付 agent handlers(gin + Recorder,106 unc)+ config 校验/注册(~72 unc)+ account_manager(假策略上层 + re-exec 真策略体,~86 unc)+ pty_manager/middleware/logger 尾巴,并收口 BLOCK-02"
    - "Q-77-A(generateRandomSecret 确定性返回同一常量,agent JWT secret 可预测)按 D-01 发现即修:crypto/rand 选 32 字符 + 同 commit 回归测试(两次调用不同/长度 32),独立原子 commit fix(quirk-77-a);D-03 判修依据=函数名+「生成随机密钥」语义与行为不符(代码自身声明)"
    - "Q-77-B(Linux/注册表读取失败时 MachineGUID[:8] slice out of range panic)按 D-01 发现即修:长度守卫截断 + 同 commit 回归测试(MachineGUID 为空时返回含 hostname 前缀而非 panic),独立原子 commit fix(quirk-77-b);两项均按 D-02 在本 plan 内就地修,SUMMARY 记 deviation"
    - "3 个 var seam(runAccountCmd = runCommand / runAccountCmdOutput = runCommandOutput / newAccountCmd = newCommand)按 76-02 newNetworkDriver 先例落地,机械替换 account_manager.go 实测 15 处调用点(runAccountCmd ×11 于行 98/104/113/123/127/132/147/163/171/175/223 + runAccountCmdOutput ×2 于 137/179 + newAccountCmd ×2 于 188/217),生产行为 byte 不变,var 初值即原直调"
    - "seam 覆盖纪律强制:orig := runAccountCmd; t.Cleanup(恢复) 先注册后覆盖,禁 t.Parallel(),测试文件头注释警示 P-77-9(忘恢复会让后续测试真跑 powershell——Windows 危险)"
    - "BLOCK-02 收口:`go test -count=1 -cover ./internal/agent/server/` ≥70.0%,`go test ./...` 全绿,4 层 gate 不倒退"
    - "D-04 + D-05 SC#3 收口人工对比:本地 Windows `go test -cover` 与 push 后 CI backend-coverage artifact 的 per-package 数字对比,internal/agent/server 与 internal/services/operations 两包都对比,差值 <2pp 记录进 77-VERIFICATION.md(收口人工步骤,非 plan 内自动化任务)"
    - "新 quirk 一律按 D-01 发现即修 / D-02 plan 内就地修 / D-03 有据判定;按 D-09 wave 编排本 plan 位于 wave 2(依赖同包 77-04 先行,复用其假后端形态)"
  artifacts:
    - path: "internal/agent/server/config_account_77_05_test.go"
      provides: "config 校验/注册/LoadConfig + Q-77-A/B 回归 + account_manager 假策略上层/re-exec 真策略体/parse 纯函数测试(前缀 TestCfg77_ / TestAcct77_)"
      min_lines: 350
      contains: "runAccountCmd"
    - path: "internal/agent/server/handlers_77_05_test.go"
      provides: "AgentHandler 6 账号 handler + JWTAuth 端到端 + sanitizeError + pty_manager + logger 测试(前缀 TestHdl77_ / TestPty77_)"
      min_lines: 400
    - path: "internal/agent/server/account_manager.go"
      provides: "3 个包级 var seam(生产路径行为 byte 不变)"
      contains: "runAccountCmd"
    - path: "internal/agent/server/subprocess_stub_test.go"
      provides: "TestHelperProcess +4 shape(echo-args / exit-1 / passwd-style / print-users)"
      contains: "echo-args"
    - path: "internal/agent/server/config.go"
      provides: "Q-77-A crypto/rand 修复 + Q-77-B 长度守卫修复"
      contains: "crypto/rand"
  key_links:
    - from: "config_account_77_05_test.go"
      to: "account_manager.go 的 3 个 var seam"
      via: "覆盖 var → helperStubCommand(t, shape) 注入 re-exec 命令"
      pattern: "helperStubCommand\\(t, \""
    - from: "fakeStrategy 注入"
      to: "AccountManager.strategy 私有字段"
      via: "am := NewAccountManager(); am.strategy = &fakeStrategy{}(同包白盒,ldap_client_mock_test.go 范本)"
      pattern: "am\\.strategy = &fakeStrategy"
    - from: "handlers_77_05_test.go"
      to: "handlers.go NewAgentHandler(:67)"
      via: "NewAgentHandler(am, auth) + h.RegisterRoutes(gin.New()) + httptest.NewRecorder"
      pattern: "RegisterRoutes"
---

<objective>
交付 agent/server 的 handlers + config + account_manager 测试(~257 stmts + seam 后真策略体 ~50),就地修复 Q-77-A/Q-77-B 两个判修级 quirk,落地 3 个 var seam(15 处机械替换,生产行为 byte 不变),收口 BLOCK-02 与 SC#3 人工对比说明(D-04/D-05)。

Purpose: BLOCK-02 收口 plan(agent/server 21.9% → ≥70%,本 plan ~307 stmts);Q-77-A 是生产安全语义修复(agent 自动注册 JWT secret 曾可预测)。
Output: 2 个新测试文件 + config.go/account_manager.go 修复与 seam + subprocess_stub_test.go 4 个新 shape。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/77-operations-agent-server/77-CONTEXT.md
@.planning/workstreams/milestone/phases/77-operations-agent-server/77-RESEARCH.md
@.planning/workstreams/milestone/phases/77-operations-agent-server/77-PATTERNS.md
@.planning/workstreams/milestone/phases/76-test-doubles/76-PATTERNS.md

<interfaces>
<!-- 关键契约(PATTERNS 逐一复核行号;seam 替换点 15 处为 PATTERNS 实测口径,修正 RESEARCH 的 12 处)。 -->

From internal/agent/server/subprocess.go(:13-32):
```go
func runCommand(ctx context.Context, name string, args ...string) error
func runCommandOutput(ctx context.Context, name string, args ...string) ([]byte, error)
func newCommand(ctx context.Context, name string, args ...string) *exec.Cmd
```

From internal/agent/server/account_manager.go:
```go
// :12-19 platformStrategy 接口(6 方法):
//   createAccount / deleteAccount / resetPassword / enableAccount / disableAccount / listAccounts
// 新增 3 个包级 var seam(var 初值即原直调,生产路径行为 byte 不变):
var (
    runAccountCmd       = runCommand        // 11 处调用点: 98,104,113,123,127,132(powershell×6),147(useradd),163,171,175(userdel/usermod),223(chmod)
    runAccountCmdOutput = runCommandOutput  // 2 处: 137(powershell Get-LocalUser),179(getent passwd)
    newAccountCmd       = newCommand        // 2 处: 188(chpasswd),217(tee)
)
```

From internal/agent/server/handlers.go(:67):
```go
h := NewAgentHandler(am *AccountManager, auth *JWTAuthenticator)  // 持具体类型,需真构造器 + 假内芯
h.RegisterRoutes(r *gin.Engine)  // 含 SecurityHeaders/JWTAuth 挂载
```

From internal/agent/server/subprocess_stub_test.go(:25-71):
- TestHelperProcess:guard 第一行(GO_WANT_HELPER_PROCESS != "1" 即 return)+ 每分支必达 os.Exit 契约
- helperStubCommand(t, shape) — re-exec 命令构造,直接复用

config.go quirk 点: :369 fp.MachineGUID[:8](Q-77-B)/ :381-388 generateRandomSecret(Q-77-A)。
包内 init() 已设 gin.SetMode(gin.TestMode)(agent_smoke_test.go:94)——勿重复/勿改(P-77-8)。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Q-77-A + Q-77-B quirk 修复(先回归测试后修)+ config 校验/注册/LoadConfig 测试</name>
  <files>internal/agent/server/config_account_77_05_test.go, internal/agent/server/config.go</files>
  <read_first>
  - internal/agent/server/config.go — :369(MachineGUID[:8] slice panic 点)/ :381-388(generateRandomSecret 确定性输出)/ Validate/ValidateTLS/CheckCertificateFiles(GOOS 守卫)/ AutoRegisterAgent(http.Post backendURL)/ RegisterToBackend/ LoadConfig(viper 全局态)
  - .planning/workstreams/milestone/phases/77-operations-agent-server/77-RESEARCH.md — §风险与坑位 Q-77-A/Q-77-B 表 + P-77-3(viper.Reset)
  - .planning/workstreams/milestone/phases/77-operations-agent-server/77-PATTERNS.md — §77-05 quirk 修复点原文摘录
  </read_first>
  <behavior>
  - Test: generateRandomSecret() 两次调用返回不同字符串(现行为恒返回 "abcdefghijklmnopqrstuvwxyz0123..." 前缀常量 → RED)
  - Test: generateRandomSecret() 返回长度恰为 32
  - Test: RegisterToBackend 失败分支在 MachineGUID 为空(fingerprint 无 GUID)时返回 error 而非 panic(现行为 slice out of range → RED)
  - Test: 修复后 agentID 生成在 MachineGUID 非空且 ≥8 字符时行为与旧一致(截取前 8 位)
  </behavior>
  <action>
  创建 config_account_77_05_test.go(package server,前缀 TestCfg77_)。

  第一步(RED):先写上述 4 条回归测试并运行确认按现行为失败(Q-77-A 两次调用相同;Q-77-B panic)。
  第二步(GREEN,各自独立原子 commit,Phase 75 五步法,D-01/D-02):
  - fix(quirk-77-a):config.go generateRandomSecret 改用 crypto/rand 从 charset 随机选 32 字符(math/rand 禁用),同 commit 带回归测试。
  - fix(quirk-77-b):config.go:369 的 fp.MachineGUID[:8] 加长度守卫(len < 8 时全用可用前缀或空串,语义=fallback 前缀),同 commit 带回归测试;SUMMARY 记录两项 deviation(根因+证据,D-03 判修依据已列 must_haves)。
  第三步 config 其余测试:
  - TestCfg77_Validate / TestCfg77_ValidateTLS:纯 struct 分支 + t.TempDir() 造证书/密钥文件路径(os.Stat 存在性,无需真证书内容);断言 Phase 75 Q-12/Q-13 修复后的新行为(非法 level 返回 error / TLS 空参报错),不写 workaround。
  - TestCfg77_CheckCertificateFiles:两侧平台都直调(Windows 自然只覆盖 guard 行——SC#3 抵消数学的一部分,勿 Skipf);Linux 世界可读 key 分支用 os.Chmod(0o644) 造。
  - TestCfg77_AutoRegisterAgent:httptest 假后端 POST {backendURL}/api/agent/register,覆盖 200 返回 {"code":0,"data":{...}} / 非 200 / 坏 JSON / 连接失败四分支(http.Post 默认 client,backendURL 参数直达无需注入)。
  - TestCfg77_RegisterToBackend:成功分支(假后端 OK)+ 失败分支(指向已关闭端口;Q-77-B 修复后不再 panic)。
  - TestCfg77_LoadConfig:t.Setenv("BACKEND_URL"/"AGENT_ID"/"JWT_SECRET") + t.TempDir 写合法 yaml 覆盖 viper 路径 + 相对 LogPath 转 abs 分支;P-77-3:每个用例 t.Cleanup(viper.Reset) 防全局单例态跨测试污染,相关测试禁 t.Parallel。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestCfg77_" ./internal/agent/server/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - 存在断言「generateRandomSecret 两次调用返回不同值」与「长度 32」的测试且通过;`grep -c "crypto/rand" internal/agent/server/config.go` ≥ 1(Q-77-A 修复落地)
  - 存在 MachineGUID 空值不 panic 的回归测试且通过;config.go:369 一带含长度守卫代码(Q-77-B 修复落地)
  - git log 含两个独立原子 commit fix(quirk-77-a) 与 fix(quirk-77-b),各自同 commit 带回归测试
  - `grep -c "viper.Reset" internal/agent/server/config_account_77_05_test.go` ≥ 1(P-77-3)
  - `go test -count=1 -run "TestCfg77_" ./internal/agent/server/` exit 0;`go build ./...` exit 0;`go test -count=1 ./internal/agent/...` 无跨包回归
  </acceptance_criteria>
  <done>Q-77-A/Q-77-B 以 TDD 顺序(RED→GREEN)修复并各自原子 commit;config 校验/注册/LoadConfig 测试全绿;deviation 记录待 SUMMARY。</done>
</task>

<task type="auto">
  <name>Task 2: 3 个 var seam(15 处机械替换)+ TestHelperProcess +4 shape + account_manager 测试</name>
  <files>internal/agent/server/account_manager.go, internal/agent/server/subprocess_stub_test.go, internal/agent/server/config_account_77_05_test.go</files>
  <read_first>
  - internal/agent/server/account_manager.go — 全文:platformStrategy 接口(:12-19)/ windows/linux 真策略体 12 方法/ parseWindowsUsers/parseLinuxUsers/NewAccountManager;15 处 seam 替换点行号清单见 77-PATTERNS §77-05 表
  - internal/agent/server/subprocess.go — :13-32 runCommand/runCommandOutput/newCommand 签名(seam 初值来源)
  - internal/agent/server/subprocess_stub_test.go — :25-52 TestHelperProcess(guard 第一行契约)+ :65-71 helperStubCommand
  - internal/services/addomain/ldap_client_mock_test.go — :12-57 fakeStrategy 范本(preset 字段 + Fn 函数字段 + 调用计数)
  - .planning/workstreams/milestone/phases/76-test-doubles/76-PATTERNS.md — §76-02a/§76-02b(工厂 var 先例 + 覆盖纪律原文)
  </read_first>
  <action>
  生产改动(account_manager.go,行为 byte 不变,76-02 newNetworkDriver 同构先例):新增 3 个包级 var runAccountCmd = runCommand / runAccountCmdOutput = runCommandOutput / newAccountCmd = newCommand;机械替换 15 处调用点(11+2+2,行号清单见 read_first);错误文案包装留在策略体调用点不动。

  subprocess_stub_test.go 扩展:TestHelperProcess 新增 4 个 shape(echo-args:把 os.Args 打到 stdout 后 exit 0 / exit-1 / passwd-style:读一行 stdin 后 exit 0 / print-users:打印 getent 格式多行 "user:x:1000:1000:..." 后 exit 0);guard 第一行与「每分支必达 os.Exit」契约保持。

  config_account_77_05_test.go 追加(前缀 TestAcct77_,文件头注释警示 P-77-9):
  - 真策略体(经 seam 注入 re-exec):覆盖 var 前 orig := runAccountCmd; t.Cleanup(func(){ runAccountCmd = orig })(三个 seam 同款,先 Cleanup 后覆盖,禁 t.Parallel);用 helperStubCommand(t, shape) 构造 re-exec 命令注入——windows createAccount 断言 PowerShell 脚本含 ConvertTo-SecureString 与用户名;deleteAccount/resetPassword/enableAccount/disableAccount;linux createAccount 断言 useradd 参数;setLinuxPassword 经 passwd-style 断言 stdin 收到 "user:pass\n";configureSudo 断言 tee 路径 + sudoers 内容(args/stdin);listAccounts 经 print-users 喂 getent 格式数据;exit-1 shape 驱动各 "failed to ..." 错误分支 + isAdmin Add-LocalGroupMember 失败分支。
  - parse 纯函数:parseWindowsUsers/parseLinuxUsers 直调(UID>=1000 过滤 / 冒号解析 / 空行)。
  - 假策略上层:fakeStrategy 实现 platformStrategy 6 方法(ldap_client_mock_test.go 三件套风格:preset 返回值 + Fn 函数字段 + 调用计数);am := NewAccountManager(); am.strategy = &fakeStrategy{}(同包白盒)→ 覆盖 AccountManager 6 公开方法(参数透传断言 username/password/isAdmin)。

  收尾自证行为不变:`git diff` 生产改动仅 account_manager.go(机械替换)+ go build ./... + go test -count=1 ./internal/agent/... 全绿。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestAcct77_" ./internal/agent/server/ && go build ./... && go test -count=1 ./internal/agent/...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "runAccountCmd" internal/agent/server/account_manager.go` ≥ 12(var 声明 1 + 11 调用点);runAccountCmdOutput ≥ 3(var + 2 调用);newAccountCmd ≥ 3(var + 2 调用);且原直调 runCommand/runCommandOutput/newCommand 在 account_manager.go 的策略体内 0 残留调用(grep 计 runCommand\( 于该文件 = 0,声明与 seam 初值除外)
  - `grep -c "echo-args\|exit-1\|passwd-style\|print-users" internal/agent/server/subprocess_stub_test.go` ≥ 4(4 shape 落地)
  - `grep -c "t.Cleanup" internal/agent/server/config_account_77_05_test.go` ≥ 3(三个 seam 各有先恢复后覆盖;TestAcct77_ 用例禁 t.Parallel)
  - `go test -count=1 -run "TestAcct77_" ./internal/agent/server/` exit 0;`go build ./...` exit 0;`go test -count=1 ./internal/agent/...` exit 0(行为 byte 不变自证)
  - SUMMARY 记录 seam deviation 附 `git diff --stat` 仅 account_manager.go 证据
  </acceptance_criteria>
  <done>3 seam × 15 处机械替换落地且行为不变;4 shape re-exec 驱动真策略体 ~50 stmts;假策略上层 + parse 纯函数全覆盖。</done>
</task>

<task type="auto">
  <name>Task 3: handlers(gin + Recorder)+ middleware JWTAuth 端到端 + pty_manager + BLOCK-02/SC#3 收口</name>
  <files>internal/agent/server/handlers_77_05_test.go</files>
  <read_first>
  - internal/agent/server/handlers.go — 全文:NewAgentHandler(:67)/RegisterRoutes/6 账号 handler/Register(:284)/Heartbeat(:330)/HealthCheck/WebSocketTerminal/sanitizeError
  - internal/agent/server/middleware.go — JWTAuth 有效/无效 token 分支(8 unc)
  - internal/agent/server/pty_manager.go — 全文(非真 pty:Create/Close 返回 not-implemented;Write/Read/List 操作内存 map m.sessions)
  - internal/agent/server/logger.go — InitLogger 剩余/WithContext/WithRequestID/Fatal
  - internal/agent/server/agent_smoke_test.go — gin Recorder 先例(:94-156,init() 已设 TestMode)
  - internal/agent/server/jwt_conn_77_04_test.go — 77-04 假后端 helper(直接复用形态)
  </read_first>
  <action>
  创建 handlers_77_05_test.go(package server,前缀 TestHdl77_ / TestPty77_;复用 77-04 的假后端形态):
  - 装配:am := NewAccountManager(); am.strategy = &fakeStrategy{...}; auth := NewJWTAuthenticator("test-secret", srv.URL, "agent-1", "vm-1", nil); h := NewAgentHandler(am, auth); r := gin.New(); h.RegisterRoutes(r)——一次覆盖 RegisterRoutes 全部 15 stmts(含 SecurityHeaders/JWTAuth 挂载)。
  - TestHdl77_AccountHandlers:6 个账号 handler × fakeStrategy 成功/失败两态(createErr 注入 errors.New("boom") → 500)+ ShouldBindJSON 400 分支;fakeStrategy 记录 username/password/isAdmin 调用参数透传断言。httptest.NewRecorder + r.ServeHTTP(w, httptest.NewRequest("POST", "/api/v1/accounts", ...)) 形态。
  - TestHdl77_JWTAuthChain:auth.GetCurrentToken(ctx)(或 Register 后)取本地 token → 带 Authorization: Bearer 头请求受保护路由 → 200;坏 token → 401。一次测试同时覆盖 middleware 8 unc + handler 成功路径。
  - TestHdl77_RegisterHandler:body 提供/缺省 agent_id+vm_id 两分支 + RegisterToBackend 假后端成功/失败;Heartbeat(:330)同理。
  - TestHdl77_HealthCheck / WebSocketTerminal / sanitizeError:HealthCheck 200;sanitizeError 用含 "password"/"token"/"sql"/"C:\" 的错误串各一例 → 断言响应体是通用错误消息(敏感原文不出现)。
  - TestPty77_Manager:CreateSession/CloseSession 断言 "not yet implemented" 错误;同包直插 m.sessions["s1"] = &ptySession{Input: make(chan string,1), Output: make(chan string,1)} → WriteToSession("nope",...) session not found / 正常写读 / 写满 / ListSessions——零 Skipf(ROADMAP Skipf 兜底备注经 RESEARCH 实证可作废:非真 pty)。
  - logger 尾巴:InitLogger 剩余分支/WithContext/WithRequestID/Fatal 顺带覆盖。

  收口 checkpoint(BLOCK-02 + SC#3 说明):
  - `go test -count=1 -cover ./internal/agent/server/` 必须 ≥70.0%,数字落 SUMMARY(不足则从余量清单回补:NewTLSConfigFromConfig happy path / parse 函数更多形态)。
  - `go test ./...` 全绿 + check-coverage.sh 不倒退(coverage.out 口径同 CI)。
  - SUMMARY 写明 D-04/D-05 收口人工对比步骤:push 后下载 CI backend-coverage artifact,与本地 Windows 的 internal/agent/server 与 internal/services/operations per-package 数字人工对比,差值 <2pp 记录进 77-VERIFICATION.md(一次性人工验证,不写脚本不加 gate 层级);若 artifact 粒度不足,fallback 用 CI 日志 grep "ok.*coverage:" 行(A3)。
  - SUMMARY 提示 Q-77-A 运维影响:已部署 agent 的 JWT secret 曾可预测,建议收口时在 77-VERIFICATION.md 提示运维重注册 agent(RESEARCH Open Question 2,留人工裁决)。
  </action>
  <verify>
    <automated>go test -count=1 -cover ./internal/agent/server/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -cE "^func Test(Hdl77|Pty77)_" internal/agent/server/handlers_77_05_test.go` ≥ 8
  - sanitizeError 用例断言响应体不含 "password"/"token" 敏感原文(至少各一例)
  - JWTAuth 端到端用例同时存在 200(有效 token)与 401(坏 token)断言
  - `go test -count=1 -cover ./internal/agent/server/` 输出 coverage ≥70.0%(BLOCK-02 收口判据,数字落 SUMMARY)
  - `go test -count=1 ./internal/agent/server/` exit 0;`go build ./...` exit 0;pty_manager 用例无 t.Skip
  - SUMMARY 含 D-04/D-05 人工对比步骤说明与 Q-77-A 运维提示
  </acceptance_criteria>
  <done>BLOCK-02 收口:agent/server ≥70.0%;handlers/config/account_manager/pty/middleware 全覆盖;SC#3 人工对比步骤与 Q-77-A 运维提示落 SUMMARY。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 ↔ 操作系统 | seam 覆盖失败会让测试真跑 powershell/useradd/chpasswd(P-77-9,Windows 危险) |
| 测试 ↔ 网络 | 出站 HTTP 仅 httptest 本地回环(srv.URL);http.Post 的 backendURL 参数直达 |
| 生产代码 ↔ 行为 | seam 改动必须 byte 不变;Q-77-A/B 修复是有据生产行为变更(D-01 授权) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-05-01 | Spoofing | Q-77-A:agent 自动注册 JWT secret 确定性可预测 → 攻击者可伪造 agent token | mitigate | crypto/rand 修复 + 同 commit 回归测试(两次调用不同);残余:已部署 agent 的历史 secret 可预测 → 77-VERIFICATION.md 提示运维重注册(转移至运维流程) |
| T-77-05-02 | Tampering/DoS | seam var 未恢复 → 后续测试真跑 powershell/useradd | mitigate | 强制「先 t.Cleanup 后覆盖」顺序 + 测试文件头警示 + 禁 t.Parallel;验收 grep t.Cleanup ≥3 |
| T-77-05-03 | Tampering | seam 机械替换改变生产行为 | mitigate | 76-02 同构先例(var 初值即原直调)+ go build + go test ./internal/agent/... 全绿 + git diff --stat 仅 account_manager.go 审计 |
| T-77-05-04 | Elevation of Privilege | TestHelperProcess 新 shape 破坏 guard 契约被外部触发 | mitigate | guard(GO_WANT_HELPER_PROCESS 检查)必须保持第一行;每分支必达 os.Exit |
| T-77-05-05 | Spoofing | 测试误呼真实后端/真实凭据 | mitigate | 假后端仅 srv.URL;"test-secret" 字面量 |
| T-77-05-SC | Tampering | 依赖安装 | accept | 零新增依赖 |
</threat_model>

<verification>
- `go test -count=1 -cover ./internal/agent/server/` ≥70.0%(BLOCK-02 收口)
- `go test ./...` 全绿;check-coverage.sh 4 层 gate 不倒退
- `git diff --stat` 全 phase 生产 .go 改动 ≤3 文件:account_manager.go(seam)+ config.go(Q-77-A/B)+ excel_raw_rows.go(77-03 注释)
- Q-77-A/B 各自独立原子 commit 且同 commit 带回归测试
</verification>

<success_criteria>
- BLOCK-02 达成:agent/server ≥70.0% 落 SUMMARY
- Q-77-A(安全语义)与 Q-77-B(panic)TDD 修复落地,SUMMARY 有 deviation 与运维提示
- 3 seam × 15 处机械替换,行为 byte 不变自证(git diff 审计 + 全绿)
- D-04/D-05 SC#3 人工对比步骤写入 SUMMARY,供 verify-work 收口执行
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/77-operations-agent-server/77-05-SUMMARY.md` when done
</output>
