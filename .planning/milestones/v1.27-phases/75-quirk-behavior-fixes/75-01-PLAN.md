---
phase: 75-quirk-behavior-fixes
plan: 1
type: execute
wave: 0
depends_on: []
files_modified:
  - pkg/cache/memory.go
  - pkg/cache/errors.go
  - pkg/cache/cache_74_08_test.go
  - internal/core/core_74_08_test.go
autonomous: true
requirements: [QUIRK-01]
user_setup: []
must_haves:
  truths:
    - "MemoryCache.IncrementBy 对缺失 key 按 0 起算并新建缓存项:Increment 返回 1(对齐 Redis INCR 语义,RESEARCH 硬约束 1)"
    - "MemoryCache.IncrementBy 对不可解析数字串返回 ErrNotInteger error,不再静默按 0 累加"
    - "pkg/cache/cache_74_08_test.go:131,157 两处锁定断言同 commit 翻转(预种 workaround 与静默 0 断言删除)"
    - "internal/core/core_74_08_test.go:127,155,210 三处 captcha workaround(手工种 captcha 数据)删除,GenerateCaptcha 真实链路在 MemoryCache 下直测"
    - "go test -race ./pkg/cache/... ./internal/core/... 全绿(RESEARCH 硬约束 6)"
  artifacts:
    - path: "pkg/cache/memory.go"
      provides: "IncrementBy 缺 key 0 起算 + 非法串 error"
      contains: "m.items[key] = &CacheItem"
    - path: "pkg/cache/errors.go"
      provides: "ErrNotInteger 错误值(Redis not-an-integer 对齐)"
      contains: "ErrNotInteger"
    - path: "pkg/cache/cache_74_08_test.go"
      provides: "翻转后的 IncrementBy 回归断言"
      contains: "TestMemoryCache_IncrementDecrement"
    - path: "internal/core/core_74_08_test.go"
      provides: "GenerateCaptcha 真实链路直测(无手工种数据)"
      contains: "GenerateCaptcha"
  key_links:
    - from: "internal/core/captcha.go:264"
      to: "pkg/cache/memory.go IncrementBy"
      via: "s.cache.Increment(rateLimitKey) 降级路径"
      pattern: "cache\\.Increment\\(ctx, rateLimitKey\\)"
---

<objective>
修复 QUIRK-01(Q-1 + Q-2):MemoryCache.IncrementBy 的 nil-deref panic 与非法数字串静默 0 累加,对齐 Redis INCR 语义;同 commit 翻转 pkg/cache 与 internal/core 全部 5 处锁定断言,删除 core 三处 captcha workaround,解锁 GenerateCaptcha 真实链路直测。

Purpose: 本 plan 是整个 v1.27 milestone 第一个执行的 plan(ROADMAP 硬约束)。生产走 Redis(redis.go:166-181)不受影响;MemoryCache 仅 dev/测试。修复后 core captcha 98 stmts 真实链路在 Phase 78 可直测(SC-2 验收信号)。
Output: 两个原子 commit(fix(quirk-1) / fix(quirk-2)),pkg/cache + internal/core 测试全绿且 -race 通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/75-quirk-behavior-fixes/RESEARCH.md

关键坐标(已实证):
- pkg/cache/memory.go:187-220 IncrementBy — 缺 key 时 `item.Expiration`(:215)nil 解引用 panic;:201-204 string 解析失败静默 currentValue=0
- pkg/cache/cache_74_08_test.go:131-133(预种 "cnt" workaround)、:157-161(静默 0 断言)
- internal/core/core_74_08_test.go:127-132(GenerateCaptcha workaround)、:155-157(MaxAttempts workaround)、:210-214(LoginLock seedFail workaround)
- internal/core/captcha.go:264(降级 Increment 路径,MemoryCache 不实现 RateLimitCache)、:379/:439/:445(`_, _ =` 忽略错误的调用点,不炸)
- pkg/cache/errors.go:6-15 现有错误值命名惯例(ErrNotFound 等)
- Redis 参照实现:pkg/cache/redis.go:166-181(INCR 缺 key = 1)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Q-1 — IncrementBy 缺 key 按 0 起算(返回 1)+ 翻转全部锁定断言 + 删 3 处 captcha workaround</name>
  <files>pkg/cache/memory.go, pkg/cache/cache_74_08_test.go, internal/core/core_74_08_test.go</files>
  <read_first>
  - pkg/cache/memory.go:181-230
  - pkg/cache/cache_74_08_test.go:125-167
  - internal/core/core_74_08_test.go:110-232(三个 workaround 与 LoginLock 全貌)
  - internal/core/captcha.go:240-312(GenerateCaptcha 降级路径与 captcha:data/attempts 写入)
  </read_first>
  <action>
  按 RESEARCH 五步法,以下全部改动进**同一个原子 commit**(fix(quirk-1)):

  1. 生产修复 memory.go:187-220 IncrementBy:当 `!exists || item.IsExpired()` 时按缺失处理 — currentValue 从 0 起算,新建 CacheItem{Value: currentValue, Expiration: 0(永不过期,对齐 Redis INCR 新建 key 无 TTL), Created: time.Now()},绝不解引用 nil item;当 exists 且未过期时保留现值与 item.Expiration 原样传递。注意保留 default 分支返回 ErrInvalidType 的现行为(map 等不可解析类型)。
  2. 翻转 pkg/cache 断言(cache_74_08_test.go):删 :131-133 的 QUIRK 注释与预种 workaround(`m.Set(ctx, "cnt", "10", 0)`),改为直接 `m.Increment(ctx, "cnt")` 断言返回 1 — 即缺 key 0 起算;后续 IncrementBy/Decrement 链断言值相应改(11/16/15/12 那组基于预种 10 的数值全部按新起点重算:Increment=1, +5=6, Decrement=5, -3=2);int 值路径(:152-155)保留。补回归用例:全新 key `IncrementBy(ctx, "fresh", 5)` 返回 5,且 TTL 返回 -1(新建项永不过期)。
  3. 删 core 三处 workaround(core_74_08_test.go),改为直测 GenerateCaptcha 真实链路:
     - :127-132(TestCaptchaService_GenerateAndVerify_Normal):删 QUIRK 注释与手工种 `captcha:data:cap-9`/`captcha:attempts:cap-9`;改为 Enabled=Normal 后 `resp, err := svc.GenerateCaptcha(ctx, "1.2.3.4")`,require.NoError + resp 非 nil + resp.CaptchaID 非空 + CaptchaImg 含 "data:image/png;base64";用 `mem.Get(ctx, fmt.Sprintf("captcha:data:%s", resp.CaptchaID))` 取出真实 code,原验证逻辑(错误输入→"验证码错误"、正确输入→通过、二次验证→"验证码不存在或已过期")改用该真实 ID/code,删除对固定 "cap-9"/"AB12" 的引用(不存在 ID 的用例保留 "no-such-id")。
     - :155-157(TestCaptchaService_VerifyCaptcha_MaxAttempts):同样改为真实 Generate(用独立 IP 如 "9.9.9.9" 避免限流串扰),取真实 code 后保留 3 次错误→"验证码已失效"断言。
     - :210-214(TestCaptchaService_LoginLock):删 seedFail helper;直接连续调用 `svc.RecordLoginFailure(ctx, "alice")`(修复后缺 key Increment 返回 1/2/3 自然累积,与原 seedFail(n)+record 语义等价),保留原有"用户名或密码错误"/"还可尝试 1 次"/锁定与 CheckLoginLock 断言。
  4. 本包+联动包测试:`go test ./pkg/cache/... ./internal/core/...`。
  5. commit:`fix(quirk-1): MemoryCache.IncrementBy 缺 key 0 起算(对齐 Redis INCR)+ 翻转断言 + core captcha 真实链路直测`。SUMMARY 记录生产影响面(生产 Redis 路径零影响)。
  </action>
  <verify>
    <automated>go test -count=1 ./pkg/cache/... ./internal/core/... && git log -1 --oneline | grep -c "fix(quirk-1)"</automated>
  </verify>
  <done>
  缺 key Increment 返回 1 且新建缓存项;cache_74_08_test.go:131/:157 与 core_74_08_test.go:127/:155/:210 五处锁定断言全部翻转(文件内 `grep -c "QUIRK"` 在上述坐标处归零);GenerateCaptcha 在 MemoryCache 下直测无 panic;fix(quirk-1) 单原子 commit。
  </done>
</task>

<task type="auto">
  <name>Task 2: Q-2 — IncrementBy 非法数字串返回 ErrNotInteger(不再静默按 0 累加)</name>
  <files>pkg/cache/memory.go, pkg/cache/errors.go, pkg/cache/cache_74_08_test.go</files>
  <read_first>
  - pkg/cache/memory.go:196-208(string 解析分支)
  - pkg/cache/errors.go:1-31(错误值命名与谓词惯例)
  - internal/core/captcha.go:375-381, 435-447(`_, _ =` 忽略错误调用点 — 确认不 panic)
  </read_first>
  <action>
  独立原子 commit(fix(quirk-2)):

  1. errors.go 新增 `ErrNotInteger = errors.New("值不是整数")`(对齐 Redis "value is not an integer or out of range";沿用现有中文错误值惯例,不必加谓词函数 — RESEARCH Q-2 行仅要求错误值进 errors.go)。
  2. memory.go:201-204:string 分支 ParseInt 失败时返回 `0, ErrNotInteger`,不再静默落 0 累加;成功路径不变。
  3. 翻转 cache_74_08_test.go:157-161:删 QUIRK 注释,改为 `_, err = m.IncrementBy(ctx, "bad", 1)` 断言 `assert.ErrorIs(t, err, ErrNotInteger)`;补回归:错误后原值不被改写(`m.Get(ctx, "bad")` 仍返回 "not-num")。
  4. 确认调用点安全:captcha.go:379/:439/:445 为 `_, _ =` 忽略 — 改后仅"attempts 不递增",不 panic(RESEARCH 已验收该语义);captcha.go:264 路径 err 走既有 fail-close 分支,无需改动。跑 `go test ./internal/core/...` 佐证。
  5. commit:`fix(quirk-2): IncrementBy 非数字串返回 ErrNotInteger 而非静默 0 累加 + 回归测试`。
  </action>
  <verify>
    <automated>go test -count=1 ./pkg/cache/... ./internal/core/... && git log -1 --oneline | grep -c "fix(quirk-2)"</automated>
  </verify>
  <done>
  非法数字串 IncrementBy 返回 ErrNotInteger 且原值保持;errors.go 含 ErrNotInteger;fix(quirk-2) 单原子 commit;core 联动包全绿。
  </done>
</task>

<task type="auto">
  <name>Task 3: 全量验证扫描(build + 全测 + -race + 本地 CI 镜像)</name>
  <files>none</files>
  <read_first>
  - scripts/check-ci-local.sh:1-25(用法与 CI 对应关系)
  </read_first>
  <action>
  无代码改动,只做验证(v1.26 教训:本地 CI 镜像必跑)。依次:
  1. `go build ./...`
  2. `go test -count=1 ./...`
  3. `go test -race ./pkg/cache/... ./internal/core/...`(RESEARCH 硬约束 6:Q-1 修复后本地必须跑一次;Windows 支持本机 race)
  4. `bash scripts/check-ci-local.sh backend --no-npm-ci`(lint + test + coverage gate 逐条复刻 ci.yml)
  任一步失败:回到对应 task 修复后再跑,不得带病收尾。全绿后在 git log 确认恰好两个 fix(quirk-1)/fix(quirk-2) 原子 commit。
  </action>
  <verify>
    <automated>go build ./... && go test -count=1 ./... && go test -race ./pkg/cache/... ./internal/core/... && bash scripts/check-ci-local.sh backend --no-npm-ci</automated>
  </verify>
  <done>build/全测/-race/本地 CI 镜像四道全绿;无 gate 倒退。</done>
</task>

</tasks>

<verification>
- `go build ./...` 通过
- `go test -count=1 ./...` 全绿
- `go test -race ./pkg/cache/... ./internal/core/...` 全绿
- `bash scripts/check-ci-local.sh backend --no-npm-ci` PASS
- `git log --oneline` 含且仅含 fix(quirk-1) / fix(quirk-2) 两个原子 commit,每 commit 点全测绿
- `grep -rn "QUIRK" pkg/cache/cache_74_08_test.go internal/core/core_74_08_test.go` 中 Q-1/Q-2 相关注释已删除(validateFile :413 的 QUIRK 属 Q-5/75-03,不在本 plan 范围,保留)
</verification>

<success_criteria>
1. QUIRK-01 关闭:IncrementBy 缺 key 返回 1 并新建项、非法串返回 ErrNotInteger 类错误
2. 5 处锁定断言同 commit 翻转,3 处 captcha workaround 删除,GenerateCaptcha 真实链路直测(Phase 75 SC-1/SC-2 达成)
3. -race 与本地 CI 镜像全绿
</success_criteria>

<output>
执行完成后创建 `.planning/workstreams/milestone/phases/75-quirk-behavior-fixes/75-01-SUMMARY.md`(执行器写;记录:两个 commit hash、生产影响面结论、-race 实测结果、captcha 直测改写点清单)。
</output>
