---
phase: 81-closeout-ratchet-audit
plan: 02
status: pending
type: execute
wave: 2
depends_on: ["81-01"]
files_modified:
  - .github/scripts/check-coverage.sh
  - .planning/workstreams/milestone/REQUIREMENTS.md
  - .planning/workstreams/milestone/phases/81-closeout-ratchet-audit/81-02-SUMMARY.md
autonomous: true
requirements: [GATE-02, GATE-03]

must_haves:
  truths:
    - "check-coverage.sh 三条 P2_RATCHET 赋值行(39.00/38.50/19.00)与全部配套 ratchet 注释/措辞已删,P2 floor 回落 uniform 70;floor_of() 与 P2_FLOOR='70.0' 保留(D-81-02 一次删)"
    - "删除后 gate 对 81-01 绿色 profile 实跑 EXIT=0,输出尾行为 'P2 per-package floor passed (70.0% x 10 packages)',P1 8/8 + P2 10/10 PASS"
    - "push 前本地 4 层模拟全绿(check-ci-local.sh:lint + 全量 test + gate;--diff 差量层留档)"
    - "收口 commit 推送 origin/main(含此前 199 个未推送 commit),CI backend job 四层 gate 全绿 —— SC-3 口径 = backend job(A3:frontend/frontend-coverage-diff job 状态归 v1.28,不阻塞 v1.27)"
    - "REQUIREMENTS.md BLOCK-01 复选框翻转并引用 a4cdb61(operations 83.7%)—— 文档债 #5"
  artifacts:
    - path: ".github/scripts/check-coverage.sh"
      provides: "uniform 70 P2 floor(豁免只减不增闭环,GATE-02)"
      contains: "70.0% x 10 packages"
    - path: ".planning/workstreams/milestone/REQUIREMENTS.md"
      provides: "BLOCK-01 已勾选(证据 a4cdb61)"
      contains: "- [x] **BLOCK-01**"
    - path: ".planning/workstreams/milestone/phases/81-closeout-ratchet-audit/81-02-SUMMARY.md"
      provides: "CI run id + per-job 分色记录 + push/盯守证据(GATE-03)"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: ".github/scripts/check-coverage.sh"
      via: "Coverage gate 步骤调用(exit 1 即 fail workflow)—— 本 plan 零 workflow 编辑(74-11 先例)"
      pattern: "check-coverage\\.sh"
    - from: "git push origin main"
      to: "gh run view <id>"
      via: "轮询盯守(memory push-watch-ci:禁 gh run watch);backend job 四层绿 = SC-3"
      pattern: "gh run (list|view)"
    - from: "floor_of()"
      to: "P2_FLOOR=\"70.0\""
      via: "P2_RATCHET_* 全删后 floor_of 恒返回 uniform 70(回落机制本身保留)"
      pattern: "P2_FLOOR"
---

# Plan 2: P2_RATCHET 豁免行删除 + 本地 4 层验证 + push + CI 盯守 (Phase: 81-closeout-ratchet-audit)

<objective>
**Goal (ROADMAP L280):** 删除 P2_RATCHET 豁免行回落全量 70% floor,删除后 gate 本地 + CI 实跑绿,4 层 gate 在收口 commit 上 CI 全绿(UP-only 闭环:豁免只减不增)。

四步:(Task 1)删 check-coverage.sh 三条 P2_RATCHET 行(39.00/38.50/19.00)与 5 处配套注释/措辞,对 81-01 绿色 profile 秒级验证;(Task 2)本地全量 4 层模拟(lint + test + gate + --diff)作为 push 前 blocking 闸门;(Task 3)BLOCK-01 勾选翻转 commit + 单次 push main + 轮询盯守 CI 至绿;(Task 4)SUMMARY 收口落 CI run id。

Purpose: GATE-02 的「74 豁免条件已不再成立」要用真实的删行 + CI 绿来闭证;199 个未推送 commit 让 SC-2/SC-3 的 CI 半边一直空转,本 plan 一次补上。
Output: uniform 70 的 gate 脚本 + 已推送并 CI 绿的收口 commit + BLOCK-01 勾选 + 81-02-SUMMARY.md。零生产 .go 改动。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/81-closeout-ratchet-audit/81-RESEARCH.md
@.planning/workstreams/milestone/phases/81-closeout-ratchet-audit/81-01-SUMMARY.md
@.planning/workstreams/milestone/REQUIREMENTS.md
@.github/scripts/check-coverage.sh

<interfaces>
<!-- 关键契约(planner 已逐行读源码复核,2026-08-28)。executor 直接使用,无需重新探索。 -->

**删除清单(6 处;行号为 2026-08-28 现状,编辑时以内容锚点为准,勿盲信行号):**

1. L242-244 三条赋值(逐字):
```bash
P2_RATCHET_internal_core="39.00"          # CI 39.50 / local 40.2
P2_RATCHET_internal_device="38.50"        # CI 38.91 / local 39.07
P2_RATCHET_internal_agent_server="19.00"  # CI 19.48 / local 22.08 (env branches)
```
2. L39-45 头注释 "3 packages are structurally blocked in unit-test scope (SSH scrapligo driver, full Core.Init dependency graph, agent subprocess server …)" 整段
3. L218-225 section 4 注释里 "3 structurally-blocked packages carry an UP-ONLY ratcheted floor …" 段 + L230-241 方法论注释块("Ratcheted floors for structurally blocked packages (UP-only, see header)" 起,含 PR #4 round-5 三条噪声源说明)
4. L300-304 失败信息措辞:"7 packages at the 70.0% global floor + 3 structurally-blocked packages at UP-ONLY ratcheted floors (core/device/agent-server — see 74-08-SUMMARY.md). Floors may only move UP." → 改写为 uniform 措辞(如 "P2 floor is 70.0% for all 10 packages (Phase 81 closed out the Phase 74 ratchets — UP-only history in git log). Add tests for the failing package(s) to lift it back.")
5. L308 尾行:`echo "P2 per-package floor passed (70.0% x 7 + ratcheted x 3 = 10 packages)"` → `echo "P2 per-package floor passed (70.0% x 10 packages)"`
6. **保留**:`P2_FLOOR="70.0"`(L227)与 `floor_of()`(L246-254)—— 回落机制本身;三变量删后 floor_of 恒返回 70(uniform 语义)。

**保留条件先例(L225/L241 逐字):** "Removal condition unchanged: package crosses 70.0% -> delete its entry." —— 三包实测 core 82.54% / device 82.77% / agent-server 90.59%,余量 12.5-20.6pp,条件全部满足。

**三包实测数字(81-RESEARCH §4 层 gate 现状,2026-08-28):**
```
internal/core          624/756   82.54%
internal/device       1047/1265  82.77%
internal/agent/server   568/627   90.59%
```

**push/盯守契约:**
```bash
git rev-list --left-right --count origin/main...HEAD   # 现状 0 199(push 后归 0 0)
git push origin main                                    # 单次 push(D-81-06,v1.27 全程惯例)
gh run list --branch main --limit 3                     # 取 HEAD sha 对应 run id
gh run view <run-id>                                    # 轮询(60-90s 一次);禁 gh run watch(memory: 提前退出不可靠)
```

**ci.yml 事实(L25/L28-99/L122+):** `permissions: contents: read`;四个 job = backend / coverage-diff(PR-only,L93-99)/ frontend / frontend-coverage-diff;**无 race job**;push 到 main 不触发 coverage-diff。本 plan **零 workflow 编辑**(74-11 per-ratchet 零 workflow 先例)。

**check-ci-local.sh 用法(L17/L33/L60-90):** `bash scripts/check-ci-local.sh backend`(lint golangci-lint 2.12.2 pin + 全量 test + gate)/ 追加 `--diff`(模拟 PR diff gate,对 origin/main merge-base)。头注释 L1-35 有 usage。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除三条 P2_RATCHET 豁免行 + 配套措辞,本地 gate 秒级验证(D-81-02 一次删)</name>
  <files>.github/scripts/check-coverage.sh</files>
  <read_first>
  - .github/scripts/check-coverage.sh L30-50(头注释)/ L215-260(section 4 + 三行 + floor_of)/ L295-310(失败信息 + 尾行)—— 全文过一遍再动手
  - 81-01-SUMMARY.md —— 正式绿色 profile 的路径与 PACKAGE 数字
  </read_first>
  <action>
  按 <interfaces> 删除清单逐处执行(内容锚点,不用行号盲删):

  1. 删 L242-244 三条 `P2_RATCHET_*` 赋值(含行尾测量注释)。
  2. 删头注释 L39-45 "3 packages are structurally blocked…" 段;同段 "for these the floor is ratcheted…" 句一并删,保留 P1/P2 floor 的既有说明。
  3. 删 section 4 注释的 ratcheted 说明段 + L230-241 方法论注释块;保留 "P2 per-package floor (Phase 74, SCALE-01 / D-15 …)" 的段落开头与 10 包名单语义,措辞改为 "all 10 packages at the uniform 70.0% floor (Phase 81 removed the Phase 74 ratchets after core/device/agent-server crossed 70%)"。**保留** "Removal condition…" 先例句(它是对未来的规则,不是历史包袱)。
  4. 失败信息段:删除 "3 structurally-blocked packages" 措辞,改为 uniform 70 表述(见 interfaces 第 4 条示例);保留 exit 5 与 "Add tests for the failing package(s)" 指引。
  5. 尾行改为 `"70.0% x 10 packages"`。
  6. `P2_FLOOR`、`floor_of()`、`P2_PACKAGES` 十包名单、exit codes 0/1/2/4/5 语义一律不动。

  **守门检查**:删除后 `grep -n "P2_RATCHET" .github/scripts/check-coverage.sh` 必须 0 命中;`grep -n "structurally" .github/scripts/check-coverage.sh` 仅允许出现在历史引用句(如 74-08-SUMMARY 指针)或 0 命中 —— 若仍有 "3 packages / ratcheted floors" 活动措辞,继续清。
  **禁止**:动 P1 段、weighted-average 段、diff 层;动 ci.yml;顺手重构 awk(零行为变更以外的改动一律不做)。

  验证用 81-01 的 coverage.out(若已被清理,用 `go test -count=1 -coverprofile=coverage.out ./internal/core/ ./internal/device/ ./internal/agent/...` 重建三包快照后再全量 gate 也须 EXIT=0)。
  </action>
  <verify>
    <automated>grep -c "P2_RATCHET" .github/scripts/check-coverage.sh; bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold > /tmp/gate_81_02.txt 2>&1; echo GATE_EXIT=$?; grep "floor passed\|FAILED" /tmp/gate_81_02.txt; bash -n .github/scripts/check-coverage.sh && echo SYNTAX-OK</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "P2_RATCHET" .github/scripts/check-coverage.sh` = 0
  - GATE_EXIT=0 且输出含 `P2 per-package floor passed (70.0% x 10 packages)`;P1 PASS ×8 + P2 PASS ×10;输出无 "ratcheted" 活动措辞
  - `bash -n` 语法检查过(exit 0)
  - 修改只落 check-coverage.sh 一个文件(原子 commit)
  </acceptance_criteria>
  <commit>feat(81-02): 删除 core/device/agent-server 三条 P2_RATCHET 豁免行,P2 floor 回落 uniform 70</commit>
  <done>豁免只减不增闭环完成:三行删除 + 配套措辞清理,gate 对绿色 profile 实跑 EXIT=0 且尾行确认 10 包 uniform 70。</done>
</task>

<task type="auto">
  <name>Task 2: push 前本地 4 层全量模拟(check-ci-local.sh + --diff)</name>
  <files>.github/scripts/check-coverage.sh</files>
  <read_first>
  - scripts/check-ci-local.sh L1-35(usage/SCOPE/--diff 参数)+ L60-120(backend 段)
  </read_first>
  <action>
  push 是不可逆动作(公共主分支 + 199-commit 批量),本地闸门必须是 blocking:

  1. **全量模拟**:`bash scripts/check-ci-local.sh backend > /tmp/local_ci_81_02.log 2>&1; echo EXIT=$?`。覆盖三步:lint(golangci-lint 2.12.2 pin)/ 全量 test(ci.yml 同命令,~11min)/ 4 层 gate。验收 EXIT=0 且无 FAIL 行。lint 若因环境未装 golangci-lint 而 FAIL(脚本版本守卫),记录并在 SUMMARY 注明「lint 本地不可复刻,以 CI 为准」,不阻塞(199 个 commit 已在 CI 上跑过 lint 的前置不存在 —— 它们从未 push,所以这条要如实记录为已知盲区,由 Task 3 CI 结果闭合)。
  2. **差量层(D-81-06)**:`bash scripts/check-ci-local.sh backend --diff`(参数序以脚本头 usage 为准)对 origin/main merge-base 跑 PR diff coverage ≥80% 模拟。判定规则:EXIT=0 → PASS 留档;EXIT≠0 → 先分诊输出 —— 失败若集中在纯测试文件/生成文件的行,按脚本语义核对 measured-line 口径后在 SUMMARY 记录处置;失败若指向 199 批次的生产行 <80%,这是真实信号,**停下上报**(SC-3 的 diff 层证据链不能带病通过)。merge-base 差异巨大(199 commit)导致超时/噪声时,允许以「抽样最近 10 个收口相关 commit 的 diff」降采样复跑并把降采样声明写进 SUMMARY —— 禁止静默跳过。
  3. **无倒退自查(GATE-03 后半)**:`git log --oneline -30 -- .coverage-threshold .github/scripts/check-coverage.sh` 确认 threshold 链单调(12.8→21.5→25.9→55.5→77.5)且 check-coverage.sh 历史无 floor 下调 commit;结论一行写入 SUMMARY(81-03 audit 会复用)。
  4. 本 task 无文件改动(纯验证);其产出(两份 log + 无倒退结论)供 Task 3/4 引用。若 Task 1 的 gate 验证用的是三包快照 profile,本步的全量 test 会重建 coverage.out,顺带成为最终确认。
  </action>
  <verify>
    <automated>bash scripts/check-ci-local.sh backend > /tmp/local_ci_81_02.log 2>&1; echo LOCAL_CI_EXIT=$?; grep -c "^FAIL\|fail " /tmp/local_ci_81_02.log; bash scripts/check-ci-local.sh backend --diff > /tmp/local_diff_81_02.log 2>&1; echo DIFF_EXIT=$?</automated>
  </verify>
  <acceptance_criteria>
  - LOCAL_CI_EXIT=0:lint(或记录的已知盲区)+ 全量 test + gate 三段全 PASS
  - DIFF_EXIT=0,或降采样复跑 PASS 且降采样声明落 SUMMARY;真实生产行 <80% 的失败 = 停止上报
  - threshold 单调链结论写入 SUMMARY 素材(12.8→21.5→25.9→55.5→77.5,无下调)
  </acceptance_criteria>
  <commit>无独立 commit(纯验证 task;证据落 Task 4 SUMMARY)</commit>
  <done>push 前 blocking 闸门全绿:本地 4 层 + diff 层 + 无倒退自查三证据齐备,push 风险收敛到 CI 环境差异一类。</done>
</task>

<task type="auto">
  <name>Task 3: BLOCK-01 勾选翻转 + 单次 push main + CI 轮询盯守至绿(D-81-06)</name>
  <files>.planning/workstreams/milestone/REQUIREMENTS.md</files>
  <read_first>
  - .planning/workstreams/milestone/REQUIREMENTS.md L30(BLOCK-01 复选框)与 L60-77(Traceability 表)
  - memory: push-watch-ci.md(轮询 gh run view;watch 不可靠;deploy.yml gate 在 ci.yml 上)
  </read_first>
  <action>
  1. **先 commit 后 push(同一 task 内,顺序强制)**:REQUIREMENTS.md L30 `- [ ] **BLOCK-01**` → `- [x] **BLOCK-01**`,行内/行尾附证据 `(a4cdb61: operations 73.2 → 83.7,77-03 收口)`;确认 Traceability 表 L60 已有 "Complete (operations 83.7%)" 无需改。commit:`docs(81-02): BLOCK-01 复选框翻转(a4cdb61 证据)`。此 commit 即 push 载荷的最后一节。
  2. **push 前终检**:`git status --porcelain` 干净(无未预期残留);`git log --oneline origin/main..HEAD | wc -l`(预期 200 = 199 + 本 plan 的删行/翻转 commit);Task 2 两份 EXIT=0 在手。
  3. **单次 push**:`git push origin main`(D-81-06:v1.27 全程惯例,不拆批、不 PR)。push 后立即 `git rev-list --left-right --count origin/main...HEAD` 应为 `0 0`。
  4. **CI 盯守**:取 HEAD 短 sha → `gh run list --branch main --limit 5` 找到对应 run id → 循环 `gh run view <id>`(60-90s 间隔,~15-20min 预期;**禁 gh run watch**)直到 status=completed。
  5. **判定口径(A3/D-81-06,写死)**:SC-3 = **backend job 全 step 绿**(尤其 Coverage gate 步骤 PASS,P2 段输出 10 行 PASS)+ run 里 coverage-diff job 若因 PR-only 未触发属正常(push 到 main 不触发,ci.yml L93-99)。frontend / frontend-coverage-diff job 的红/绿**不影响 SC-3 判定** —— 199 批次含大量 v1.28 `test(88)` 提交,其前端债务归 v1.28;按 job 分色记录进 SUMMARY(R7)。
  6. **失败预案**:backend job 红 → `gh run view <id> --log-failed` 分诊:(a) Coverage gate exit 4/5 = 某包 CI 实测 <floor —— 按 R4 纪律**处置是补测试,禁止回滚豁免行**(违反「豁免只减不增」);(b) lint/test 环境性失败(git 网络、runner infra)→ 重跑一次 run 并注明;(c) 前端 job 红 → 记录归 v1.28,不阻塞。所有处置写入 SUMMARY。
  </action>
  <verify>
    <automated>git rev-list --left-right --count origin/main...HEAD; grep -n "BLOCK-01" .planning/workstreams/milestone/REQUIREMENTS.md | head -2; gh run list --branch main --limit 3</automated>
  </verify>
  <acceptance_criteria>
  - `git rev-list --left-right --count origin/main...HEAD` = `0 0`(push 完成)
  - REQUIREMENTS.md BLOCK-01 为 `- [x]` 且引用 a4cdb61
  - CI run completed:backend job 全 step 绿(含 Coverage gate PASS);per-job 分色结果(SUCCESS/FAILURE × 4 job)记录在案
  - 若 backend 红:已按失败预案分诊并留下处置记录(不允许无记录的重跑)
  </acceptance_criteria>
  <commit>docs(81-02): BLOCK-01 复选框翻转(a4cdb61 证据)</commit>
  <done>收口 commit 推送完成且 CI backend 四层绿 —— SC-2 的「CI 实跑绿」与 SC-3 的「收口 commit CI 全绿」同时闭合;frontend 状态分色归档。</done>
</task>

<task type="auto">
  <name>Task 4: 81-02-SUMMARY 收口 —— CI run id + 分色记录 + 无倒退证据</name>
  <files>.planning/workstreams/milestone/phases/81-closeout-ratchet-audit/81-02-SUMMARY.md</files>
  <read_first>
  - ~/.claude/get-shit-done/templates/summary.md
  - 81-01-SUMMARY.md(衔接:绿色 profile 数字)
  </read_first>
  <action>
  SUMMARY 必含五段:
  1. **删除清单执行记录**:6 处删除逐条打勾 + `grep -c P2_RATCHET` = 0 证据 + 保留项(P2_FLOOR/floor_of/exit codes)声明;
  2. **本地验证证据**:/tmp/local_ci_81_02.log EXIT、--diff EXIT(含降采样声明若有)、threshold 单调链结论(12.8→21.5→25.9→55.5→77.5);
  3. **push 记录**:push 前后 rev-list 计数(`0 200` → `0 0`)、commit 清单尾部 5 条;
  4. **CI 裁决**:run id + HEAD sha、四 job 分色表(backend SUCCESS / coverage-diff SKIPPED(PR-only)/ frontend ×2 按实情),backend Coverage gate 段输出摘录(PASS ×10 + weighted 行),SC-2/SC-3 逐条判定;
  5. **异常与处置**:Task 2 lint 盲区(若有)、diff 降采样(若有)、CI 失败分诊(若有)—— 零静默。
  收尾:STATE.md stopped_at 更新一行(81-02 完成:豁免删除 + push + CI run <id> backend 绿;余 81-03)。
  </action>
  <verify>
    <automated>grep -c "run id\|Run id\|run-id" .planning/workstreams/milestone/phases/81-closeout-ratchet-audit/81-02-SUMMARY.md; grep -c "70.0% x 10 packages" .github/scripts/check-coverage.sh; git status --porcelain | grep -v "^??" | wc -l</automated>
  </verify>
  <acceptance_criteria>
  - SUMMARY 含 CI run id(纯数字)与四 job 分色表;SC-2/SC-3 逐条判定落档
  - 删行证据 + 本地/diff/无倒退三证据全部入档,零静默条目
  - STATE.md stopped_at 已刷新
  </acceptance_criteria>
  <commit>docs(81-02): SUMMARY 收口——CI run 证据 + 分色记录 + 豁免删除闭环</commit>
  <done>GATE-02/GATE-03 全证据链入档;81-03 audit 可直接引用 run id 与三包 82.5/82.8/90.6 数字定案。</done>
</task>

</tasks>

<notes>
- 依赖:81-01 完成(threshold 77.5 已落、绿色 coverage.out 数字在案);81-03 依赖本 plan 的 CI run id 与删行终态。
- **push 不可逆**(公共主分支 + 199-commit 批量):Task 2 的本地闸门是 blocking,任何 EXIT≠0 未分诊前不得 push;push 后不拆批回滚,失败按 R4 补测试处置。
- SC-3 判定口径(A3):backend job 四层绿 = 达成;frontend job 状态归 v1.28(R7),不阻塞 v1.27 收口 —— 此口径写死,防止 milestone 被 199 批次里的前端债务错误卡住。
- 零 workflow 编辑(74-11 先例):ci.yml 不动;coverage-diff job 的 PR-only 语义在 push-to-main 下 SKIPPED 属预期,diff 层证据由本地 --diff 提供。
- 豁免删除验证只用 81-01 的绿色 profile 或重建的全量 profile;禁止用部分包 profile 冒充全量 gate 结果。
- floor_of()/P2_FLOOR 保留是**机制保留**(uniform 回落),不是豁免残留 —— SUMMARY 里要写明,防止 81-03 audit 误读。
</notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate 脚本 ↔ CI 强制层 | check-coverage.sh 的 exit code 直接决定 workflow 红/绿(exit 1/4/5 → fail) |
| 本地工作树 ↔ origin/main | push 批量 199+ commit,不可逆;含 v1.28 并发会话的历史提交(R7) |
| 本地模拟 ↔ Linux CI | agent-server env-branch 分歧史(local 22.08 vs CI 19.48)—— 本地绿 ≠ CI 绿(Pitfall 3) |
| threshold/gate 历史 ↔ audit 证据链 | 无倒退记录(GATE-03 后半)靠 git log 证据,不靠口头声明 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-02-01 | Tampering | 删豁免行被滥用为"放松 gate" | mitigate | UP-only 闭环方向相反:删除使 floor 从 39/38.5/19 升到 70(收紧);SUMMARY 记录三包 82.5/82.8/90.6 实测 |
| T-81-02-02 | Denial of Service | CI backend gate 因删行后某包 <70 而 exit 5 | mitigate | 本地余量 12.5-20.6pp;CI 盯守为验收;若发生按 R4 补测试,禁回滚豁免行 |
| T-81-02-03 | Repudiation | push 无证据/run 无留档 | mitigate | SUMMARY 强制 run id + 分色表 + rev-list 前后计数 |
| T-81-02-04 | Information Disclosure | push 泄漏未意图公开的规划内容 | accept | 199 批次均为本仓库既有提交内容(git 历史审查归属既有工作);无密钥文件入库 |
| T-81-02-05 | Elevation | gate 脚本改动引入注入面 | mitigate | 只删行与改措辞,`bash -n` + gate 实跑双验证;awk 逻辑零改动 |
| T-81-02-SC | Tampering | 依赖安装 | accept | 零新增依赖 |
</threat_model>

<verification>
- `grep -c "P2_RATCHET" .github/scripts/check-coverage.sh` = 0;`bash -n` 过;gate 对绿色 profile EXIT=0 且尾行 `70.0% x 10 packages`(Task 1)
- `bash scripts/check-ci-local.sh backend` EXIT=0;`--diff` EXIT=0 或带声明的降采样 PASS(Task 2)
- `git rev-list --left-right --count origin/main...HEAD` = `0 0`;CI run backend job 全绿(含 Coverage gate PASS ×10)(Task 3)
- REQUIREMENTS.md BLOCK-01 `- [x]` + a4cdb61 证据(Task 3)
- SUMMARY 落 run id + 四 job 分色 + threshold 单调链;每 task 一原子 commit(Task 1/3/4 各一)
</verification>

<success_criteria>
1. 三条 P2_RATCHET 行(39.00/38.50/19.00)与 5 处配套注释/措辞删除,P2 floor 回落 uniform 70,机制(P2_FLOOR/floor_of)保留 —— SC-2 达成
2. 删除后 gate 本地实跑绿(尾行 10 包 uniform)+ 本地 4 层模拟全绿 + diff 层留档
3. 收口 commit(含 199 积压)推送 origin/main,CI backend job 四层全绿,run id 入档 —— SC-2 CI 半边 + SC-3 达成;frontend 分色归档不阻塞
4. REQUIREMENTS.md BLOCK-01 翻转(a4cdb61);threshold 单调链无倒退证据入档 —— GATE-03 后半达成
5. 81-02-SUMMARY.md 五段齐全零静默;每 task 一原子 commit(共 3 个)
</success_criteria>

<decision_audit>
本 plan 涉及的 phase-81 决策(executor 必须遵守):

| ID | 决策 | 依据 / 理由 |
|----|------|------------|
| D-81-02 | 三条豁免行**一次删**(不分批):三包实测 82.54/82.77/90.59 全部 12.5pp+ 达标,分批无验证价值反多两次 CI 等待;删除条件先例("crosses 70.0% -> delete its entry")三包全满足 | research DQ2 + GATE-02 原文;planner 采纳研究建议 |
| D-81-06 | push 策略 = **单次 push main**(v1.27 全程惯例,不拆批/不 PR)+ 本地 `check-ci-local.sh --diff` 补验 diff 层(push-to-main 不触发 PR-only coverage-diff job,ci.yml L93-99);SC-3 口径 = backend job 四层(A3),frontend 归 v1.28 | research DQ6 + R3/R7 + A3;planner 采纳研究建议并写死判定口径 |
| (沿用) | 删行后某包 CI <70 → 补测试,**禁止回滚豁免行**(违反豁免只减不增) | research R4 |
| (沿用) | 盯守用轮询 `gh run view`,禁 `gh run watch` | memory push-watch-ci(watch 提前退出不可靠) |
| (沿用) | 本 plan 零 ci.yml 编辑 | 74-11 per-ratchet 零 workflow 先例 |
| (沿用) | BLOCK-01 勾选翻转以 a4cdb61 为充分证据(A4) | research Assumptions A4 + 文档债 #5 |

数字来源:81-RESEARCH.md §4 层 gate 现状 + §三条 P2_RATCHET 豁免行(2026-08-28 逐字引用;现行 floor 是 39.00/38.50/19.00,非 STATE.md:21 的 Phase 74 旧值 38.33/39.07/22.08 —— 删除时以现行值为准)。
</decision_audit>

<output>
Create `.planning/workstreams/milestone/phases/81-closeout-ratchet-audit/81-02-SUMMARY.md` when done
</output>
