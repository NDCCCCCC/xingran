---
phase: 81-closeout-ratchet-audit
plan: 01
status: pending
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/scheduler/workorder_tasks_80_02_test.go
  - .coverage-threshold
  - .planning/coverage-baseline.md
  - .planning/workstreams/milestone/STATE.md
  - .planning/workstreams/milestone/phases/81-closeout-ratchet-audit/81-01-SUMMARY.md
autonomous: true
requirements: [GATE-01]

must_haves:
  truths:
    - "全仓 `go test`(ci.yml byte-identical 口径)零失败:输出无任何 ^FAIL 行,exit 0 —— 正式 ratchet 只出自绿色跑(81-RESEARCH Pitfall 1)"
    - "internal/scheduler 满载 flake 已修:修复只落在 *_test.go(fixture 注册 / goroutine 排空),零生产 .go 改动,且非 t.Skip 掩盖(D-81-05)"
    - ".coverage-threshold 从 55.5 单向上调到 77.5(D-81-01),保持 4/5 字节无换行原格式,git diff 只见 55.5→77.5"
    - "coverage-baseline.md 落 Phase 81 后 行(weighted/stmts/covered/commit)+ Phase 75/76/77/78 缺行回填或显式不回填理由(文档债 #3)"
    - "gate 对新 threshold 复跑 exit 0:加权平均实测(~78.0x%)≥ 77.5,P1 8/8 + P2 10/10 全 PASS"
  artifacts:
    - path: ".coverage-threshold"
      provides: "v1.27 收口阈值 77.5(0.5pp CI-drift 缓冲)"
      contains: "77.5"
    - path: ".planning/coverage-baseline.md"
      provides: "Phase 81 后 行 + 75-78 回填行 + D-81-01 偏离先例理由 Notes"
      contains: "Phase 81"
    - path: ".planning/workstreams/milestone/phases/81-closeout-ratchet-audit/81-01-SUMMARY.md"
      provides: "研究跑(78.01% 带 1 FAIL)与正式绿色跑的差异记录 + flake 根因"
  key_links:
    - from: ".github/scripts/check-coverage.sh"
      to: ".coverage-threshold"
      via: "THRESHOLD=\"$(cat ...)\" 读取后 awk 比较(有无 LF 均安全)"
      pattern: "coverage-threshold"
    - from: "internal/scheduler/workorder_tasks.go:193"
      to: "internal/scheduler/*_test.go fixture"
      via: "getTodayDutyPerson 查 sys_duty_schedule —— fixture 必须建表(生产代码零改动)"
      pattern: "sys_duty_schedule"
---

# Plan 1: 全仓绿色重测 + threshold ratchet 55.5→77.5 + baseline 回填 (Phase: 81-closeout-ratchet-audit)

<objective>
**Goal (ROADMAP L280):** 全量重测把 `.coverage-threshold` 从 55.5 ratchet 到 ≥70 实测值,gate 防线全程不倒退。

交付三件事:(Task 0)修掉 `internal/scheduler` 满载 flake(`workorder_tasks.go:196` no such table `sys_duty_schedule`,单包 9.3s PASS / 满载 74.7s FAIL)—— 这是 SC「零失败全量重测」的唯一前置缺口;(Task 1)在干净工作树上按 ci.yml byte-identical 口径做一次**零失败**全仓重测 + gate 实跑;(Task 2/3)原子 ratchet commit:`.coverage-threshold` 55.5 → 77.5(D-81-01,0.5pp CI-drift 缓冲)+ coverage-baseline.md Phase 81 后行与 75-78 文档债回填。

Purpose: 81-02 的豁免行删除与 push、81-03 的 audit 全部消费本 plan 的绿色测量数字;带 FAIL 的 profile 一律不得入 threshold(研究 Pitfall 1)。
Output: 修好的测试 fixture + 绿色 coverage.out 快照 + threshold 77.5 + baseline 回填 + 81-01-SUMMARY.md。零生产 .go 改动。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/81-closeout-ratchet-audit/81-RESEARCH.md
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/STATE.md
@.planning/coverage-baseline.md

<interfaces>
<!-- 关键契约(planner 已逐一实测/读源码复核,2026-08-28)。executor 直接使用,无需重新探索。 -->

正式测量口径(ci.yml L64-66 byte-identical,勿自造参数):
```bash
go clean -testcache
go test -timeout 15m -count=1 -coverprofile=coverage.out -covermode=atomic \
  ./internal/... ./pkg/... ./cmd/...
bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold   # exit 0/1/2
```

`.coverage-threshold` 现状(xxd 实测):4 字节 `55.5`,**无结尾换行**。脚本以 `THRESHOLD="$(cat ...)"` + awk `threshold + 0` 读取,有无 LF 均安全;写新值用 `printf '77.5'` 保持无换行,git diff 最小。

check-coverage.sh 关键行(81-01 只读不动;删行归 81-02):
```bash
P2_FLOOR="70.0"                                  # L227
P2_RATCHET_internal_core="39.00"                 # L242(现行 floor,非 74 的 38.33)
P2_RATCHET_internal_device="38.50"               # L243
P2_RATCHET_internal_agent_server="19.00"         # L244
floor_of()  { ... echo "$P2_FLOOR"; }            # L246-254(回落机制,保留)
echo "P2 per-package floor passed (70.0% x 7 + ratcheted x 3 = 10 packages)"   # L308
```

scheduler 测试 fixture 版图(满载 flake 的诊断地图,planner 已核):
```go
// workorder_tasks_80_02_test.go
func newSchedDB8002(t)  // :32 只有 AutoMigrate(&models.Job{}, &models.JobLog{}) —— 无 duty 表
func newDutyDB8002(t)   // :47 手写 DDL 建 sys_duty_config/sys_user/sys_duty_pool/sys_duty_schedule/sys_work_order
func newWotFullDB8002(t)// :477 = newDutyDB8002 + AutoMigrate(WorkOrder, PeriodicWorkOrderLog)
// cron_80_01_test.go:1008 newDutyDB8001 同样手写 DDL 建 sys_duty_schedule(:1027)
// 生产面:workorder_tasks.go:185-206 getTodayDutyPerson → db.Table("sys_duty_schedule").First(...) (:193-196)
//        调用链:assignWorkOrderHandler :219 case models.PeriodicAssignTypeDutyPool
// 全局态:internal/scheduler GlobalDB/SetDB(:29/:264/:386/:423);同包已有竞态修复先例 6ef660f(ad_sync 生命周期)
```

sqlite 缺表 family pattern 修复 checklist(memory xingran-sqlite-missing-table-pattern,本 plan 为测试 fixture 形态):
1. 定位真正缺表的 fixture → 补 `models.DutySchedule{}`(及 DutyScheduleConfig/DutyPool 若被触及)AutoMigrate,或照抄 newDutyDB8002 的最小 DDL 块
2. 生产 PG 列表 / sanitize / dbprovision **一律不碰**(这是测试 fixture,不是运行期 sqlite 分支)
3. 回归证据:`-count=3` 单包压测绿 + Task 1 全量绿

既有 baseline 行格式(coverage-baseline.md L509-511,Phase 81 后行照此):
```
| date | phase_label | weighted_avg | total_stmts | total_covered | 0pct_pkg_count | commit | phase_executor | ratchet_from | ratchet_to |
| 2026-08-28 | Phase 79 后 | 70.9 | 43893 | 31119 | 7 | 3d8019e | gsd-executor 79-06 | 55.5 | 55.5 (不动,Phase 81 收口 bump) |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: 修 internal/scheduler 满载 flake —— 诊断先行 + fixture-only 修复(D-81-05)</name>
  <files>internal/scheduler/workorder_tasks_80_02_test.go</files>
  <read_first>
  - internal/scheduler/workorder_tasks.go:183-230 — getTodayDutyPerson(:185-206)+ assignWorkOrderHandler DutyPool 分支(:219 起);此文件**零改动**
  - internal/scheduler/workorder_tasks_80_02_test.go — fixture 全家族(:32/:47/:477)+ DutyPool 用例(:263/:450/:583)
  - internal/scheduler/cron_80_01_test.go:1000-1100 — newDutyDB8001 DDL 先例
  - memory: xingran-sqlite-missing-table-pattern(family 第 6 例)
  </read_first>
  <action>
  **注意:研究只记录了症状(workorder_tasks.go:196 报 no such table sys_duty_schedule,单包过/满载挂),未锁定失败用例名 —— 本 task 诊断先行,两个候选机制都要核。**

  1. **复现并锁定失败用例**(按序升级,复现即停):
     a. `go test -count=1 -timeout 5m ./internal/scheduler/`(基线,预期 PASS,~10s)
     b. `go test -count=3 -timeout 15m -v ./internal/scheduler/ 2>&1 | grep -E "^(--- FAIL|FAIL|panic)"` —— 串行重复压测
     c. 满载模拟:另一终端/后台跑 `go test ./internal/services/...`(长尾包,~9min)同时前台跑 `go test -count=2 -timeout 15m -v ./internal/scheduler/ 2>&1 | tee /tmp/sched_flake.log` —— 竞争 CPU 复现满载时序
     d. 从 log 抓 `--- FAIL: TestXxx` 用例名 + 完整错误帧(应指向 workorder_tasks.go:196)。
     若 a-c 全绿:静态审计代替复现(见第 2 步的判定表),以 Task 1 全量跑为最终复现法庭;若 Task 1 仍挂,回到本 task 按 d 定位 —— **最多一轮返工,再挂即按 D-81-05 升级上报,禁止循环改**。

  2. **按机制二选一修(只动 *_test.go)**:
     - **机制 A(fixture 缺表)**:失败用例使用的 DB fixture 没建 sys_duty_schedule(候选:某用例经 newSchedDB8002 直接触达 DutyPool 链,或跨文件 fixture 组装遗漏)。修法:给该 fixture 补 `db.AutoMigrate(&models.DutySchedule{})`;若 AutoMigrate 因模型 NOT NULL 列报错(既有 fixture 手写 DDL 的原因,见 :55 注释),改照抄 newDutyDB8002 :67-73 的最小 DDL 块(id/user_id/pool_id/schedule_date/status)。同族表(sys_duty_config/sys_duty_pool)若同报错一并补。
     - **机制 B(全局态/goroutine 泄漏)**:同包先例 6ef660f(ad_sync 全局生命周期竞态)提示 GlobalDB/SetDB 跨测试污染 —— 泄漏 goroutine 在后续用例的 DB 上触发 executePeriodicWorkOrderCreateTask,而那个 DB 没有 duty 表。修法:泄漏源测试补 goroutine 排空/停表 t.Cleanup(参照 e85152b "goroutine 排空" 手法与 6ef660f 的 Once 幂等 + safeRestore 模式);或泄漏用例自带全表 DB。
     判定依据 = 第 1 步抓到的失败用例名 → 它的 fixture 来源 + `-v` 时序。**禁止**:改 workorder_tasks.go 等任何生产 .go;用 t.Skip 掩盖(DQ5 明令);把 sys_duty_schedule "修"进生产 schema。
  </action>
  <verify>
    <automated>go test -count=3 -timeout 15m ./internal/scheduler/ && go build ./... && git diff --name-only | grep -v "_test.go$" | grep -v "^$" || echo "ONLY-TEST-FILES-CHANGED"</automated>
  </verify>
  <acceptance_criteria>
  - `go test -count=3 -timeout 15m ./internal/scheduler/` exit 0(三次全绿)
  - `git diff --name-only` 只含 internal/scheduler/*_test.go(生产 .go 零改动)
  - 失败用例名 + 根因机制(A/B)写进 commit message 与后续 SUMMARY
  - 零 t.Skip 新增(`git diff | grep -c "+.*t.Skip" ` = 0)
  </acceptance_criteria>
  <commit>test(81-01): 修 internal/scheduler 满载 flake(fixture 注册 sys_duty_schedule / goroutine 排空,零生产改动)</commit>
  <done>满载形态下 scheduler 包三次连跑全绿;根因机制记录在案;生产树零触碰。</done>
</task>

<task type="auto">
  <name>Task 1: 正式全仓零失败重测 + gate 实跑(干净树前置,R6/Pitfall 5)</name>
  <files>coverage.out</files>
  <read_first>
  - 81-RESEARCH.md §Current Measured State + §Common Pitfalls(Pitfall 1/5)+ §Risk R6
  - scripts/check-ci-local.sh L36-90(版本守卫 + backend 段口径)
  </read_first>
  <action>
  1. **干净树前置(研究 Pitfall 5/R6,必做)**:`git status --porcelain` 逐行核 —— 工作树现有 `internal/services/system/asset_columns_schema.json` 未提交改动且被 go:embed(column_config_service.go L14)引用,会让本地绿色/失败与 CI 干净 checkout 分歧。处置:先协调 v1.28 并发会话暂停;若该文件仍脏且不属本 phase,`git stash push internal/services/system/asset_columns_schema.json`(SUMMARY 记录,Task 3 后 `git stash pop` 还原)。`.planning/` 下文档改动不碍事,保留。
  2. **全量重测**(ci.yml L64-66 byte-identical,勿用研究时的 -timeout 20m 变体):
     `go clean -testcache && go test -timeout 15m -count=1 -coverprofile=coverage.out -covermode=atomic ./internal/... ./pkg/... ./cmd/...`
     预期 ~11 分钟(internal/services 单包长尾 ~9min)。验收:`echo $?` = 0 且 `go test` 输出 `grep -c "^FAIL"` = 0(70+ ok)。任何 FAIL → 按 D-81-05:该包单独复跑 1 次重验;仍挂 → 停,上报,不得用此 profile 进 Task 2。
  3. **gate 实跑**:`bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold > /tmp/gate_81_01.txt 2>&1; echo EXIT=$?`。预期 EXIT=0;从输出抓三样留档:(a) PACKAGE 行(研究参考值 `43729 34112 78.01%`,正式数以本次绿跑为准);(b) P1 PASS 行 ×8 + P2 PASS 行 ×10;(c) 全部 0% 包清单(应为 cmd/cmd/agent/internal/server/internal/docs 四个,172 stmts)。
  4. **数字校验**:若正式跑 weighted < 77.5(D-81-01 目标线)或与研究的 78.01 差 >1pp → 停下核对(分母漂移正常范围 ≤1.7%,先例 754 vs 767 stmts);超范围属异常,先查树是否干净再上报。
  5. coverage.out 保留给 81-02 Task 1 的秒级 gate 验证;**不入任何 commit**(Task 3 提交前 `rm -f coverage.out` 并确认 `git status` 无 cov 残留)。
  </action>
  <verify>
    <automated>go test -timeout 15m -count=1 -coverprofile=coverage.out -covermode=atomic ./internal/... ./pkg/... ./cmd/... > /tmp/full_81_01.log 2>&1; echo TEST_EXIT=$?; grep -c "^FAIL" /tmp/full_81_01.log; bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold > /tmp/gate_81_01.txt 2>&1; echo GATE_EXIT=$?; grep "PACKAGE\|floor passed" /tmp/gate_81_01.txt</automated>
  </verify>
  <acceptance_criteria>
  - TEST_EXIT=0 且 `grep -c "^FAIL" /tmp/full_81_01.log` = 0
  - GATE_EXIT=0;PACKAGE 行 weighted ≥ 78.0%(含 4 个 0% 结构包拖累);P1 8/8 + P2 10/10 PASS
  - 测量在干净树上完成(asset_columns_schema.json 已 stash 或已入库,SUMMARY 记录处置)
  - /tmp/gate_81_01.txt 留档供 81-02/81-03 引用
  </acceptance_criteria>
  <commit>无独立 commit(测量 task;数字落 Task 2/3 的 commit)</commit>
  <done>零失败绿色全量跑 + gate exit 0 的正式数字到手:weighted/total_stmts/total_covered/0pct 包清单,足以支撑 77.5 ratchet。</done>
</task>

<task type="auto">
  <name>Task 2: 原子 ratchet commit —— .coverage-threshold 55.5→77.5 + baseline 回填(D-81-01)</name>
  <files>.coverage-threshold, .planning/coverage-baseline.md, .planning/workstreams/milestone/STATE.md</files>
  <read_first>
  - .planning/coverage-baseline.md L505-530(Phase 79 后 行 + Notes + Ratchet note 的现行格式)
  - .planning/workstreams/milestone/phases/75-quirk-behavior-fixes/75-06-SUMMARY.md、76-test-doubles/76-05-SUMMARY.md、77-operations-agent-server/77-05-SUMMARY.md、78-block-bp-unlock-by-foundation/78-07-SUMMARY.md(75-78 回填数字的唯一合法来源)
  </read_first>
  <action>
  1. **threshold**:写入目录根执行 `printf '77.5' > .coverage-threshold`(5 字节、无结尾换行,与现文件字节格式一致)。取值依据 D-81-01:先例是 1-decimal 向下截断(21.55→21.5 / 25.97→25.9 / 55.56→55.5),本次**有意偏离**取 77.5 而非 78.0 —— 这 199 个 commit CI 从未见、首次跨 OS 大跃迁,0.01pp 刀口不值得赌;偏离理由必须写进 baseline Notes(UP-only 语义不破:55.5→77.5 是 +22pp 单向,未来 78+ 仍可再 UP)。
  2. **coverage-baseline.md 追加 `## Phase 81 后(v1.27 收口)` 段**,行格式照 Phase 79 后 先例:weighted = Task 1 实测(1-decimal)、total_stmts/total_covered = PACKAGE 行原数、0pct_pkg_count = 4、commit 列写 `TBD (81-01 ratchet)`(Task 3 回填,71-01b/79-06 two-step 先例,避免受保护分支 amend)、phase_executor = `gsd-executor 81-01`、ratchet_from = 55.5、ratchet_to = 77.5。附 Per-package fenced 块(直接贴 gate 输出的包级表,与历史行同格式 `%-50s %8d %8d %6.2f%%`)。
  3. **Notes(Phase 81 收口)** 必含:(a) D-81-01 偏离先例理由全文;(b) 实测命令两行(与 ci.yml byte-identical 声明);(c) 分母漂移口径一句(43652 v1.26 / 43893 Phase 79 后 / 本次实测 N,差异来源 = Go patch 插桩漂移 + 76/77 生产重构 stmts);(d) `> **Ratchet note (D-04):**` commit 列 TBD 说明(照 L518-521 先例)。
  4. **75-78 缺行回填(文档债 #3)**:从四个 phase 的收口 SUMMARY 提取各自当时的全仓/包级数字,各补一行(label `Phase 75 后`…`Phase 78 后`,commit 列填该 phase 收口 commit,数字 SUMMARRY 没有的格子写 `n/a` 并在该行下方注明出处边界);**若某 phase 无任何可溯源全仓数字,则不造数** —— 在 baseline Notes 写明 "Phase NN 不回填:当时未做全仓测量,包级数字见 NN-0X-SUMMARY"。禁止用研究数字冒充历史测量。
  5. **STATE.md**:stopped_at 更新为 "81-01 完成:零失败全量重测 weighted NN.N%(stmts 口径),threshold 55.5→77.5 ratchet 落地,余 81-02/81-03";last_updated 刷新。
  6. 三文件一个 commit(原子 ratchet);check-coverage.sh **本 plan 不碰**(81-02)。提交前 `rm -f coverage.out`。
  </action>
  <verify>
    <automated>cat .coverage-threshold | xxd | head -1 && bash .github/scripts/check-coverage.sh /dev/null .coverage-threshold 2>&1 | head -3; git diff HEAD~1 .coverage-threshold; grep -c "Phase 81" .planning/coverage-baseline.md</automated>
  </verify>
  <acceptance_criteria>
  - `.coverage-threshold` = `77.5` 无换行;`git diff` 仅 55.5→77.5(单向 UP,UP-only 纪律)
  - baseline 含 Phase 81 后 行 + 75-78 四行(或显式不回填理由);无 `TBD (81-01 ratchet)` 以外的 TBD
  - gate 脚本能以新 threshold 正常解析(cat 输出无尾随换行导致的解析异常)
  - 一个 commit 含且仅含这三个文件
  </acceptance_criteria>
  <commit>docs(81-01): threshold ratchet 55.5→77.5 + coverage-baseline Phase 81 后 行与 75-78 回填</commit>
  <done>v1.27 收口阈值 77.5 落地且 gate 机制完好;baseline 时间线补齐到 Phase 81;GATE-01 的 UP-only 证据链(git diff)成立。</done>
</task>

<task type="auto">
  <name>Task 3: 81-01-SUMMARY 收口 —— 两次跑差异 + SHA 回填</name>
  <files>.planning/workstreams/milestone/phases/81-closeout-ratchet-audit/81-01-SUMMARY.md, .planning/coverage-baseline.md</files>
  <read_first>
  - ~/.claude/get-shit-done/templates/summary.md
  - .planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-05-SUMMARY.md(SUMMARY 格式先例)
  </read_first>
  <action>
  1. **81-01-SUMMARY.md** 必含四段:(a) flake 根因 —— 失败用例名、机制 A/B 判定、修复 diff 摘要、`-count=3` 证据;(b) 两次跑差异(研究 Pitfall 1 指定动作)—— 研究跑(2026-08-28,78.01%,1 FAIL scheduler)vs 正式跑(本 plan Task 1,EXIT=0)对照表:stmts/covered/weighted/FAIL 数,差异归因(树干净化 + flake 修复后无数字影响声明);(c) ratchet 记录 —— 55.5→77.5、D-81-01 理由、UP-only git diff 证据;(d) 验收清单 —— SC-1 前半(weighted ≥70 + threshold 新值)逐条打勾,GATE-01 达标声明。
  2. **SHA 回填**:Task 2 的 commit 落地后取其短 SHA,把 baseline Phase 81 后 行 commit 列 `TBD (81-01 ratchet)` 替换为该 SHA(71-01b/79-06 two-step fill 先例;受保护主分支禁 amend,用后随 commit);同时删掉对应 Ratchet note 的 TBD 措辞。本 commit 与 SUMMARY 同 commit 提交(79-06 先例:closeout commit 同时携带 section 修正与 SUMMARY)。
  3. 还原 Task 1 stash 的 asset_columns_schema.json(`git stash pop`,若仍存在 stash 项);确认 `git status` 干净(除本 plan 的 planning 文档)。
  </action>
  <verify>
    <automated>grep -c "TBD (81-01 ratchet)" .planning/coverage-baseline.md; grep -c "^FAIL\|flake" .planning/workstreams/milestone/phases/81-closeout-ratchet-audit/81-01-SUMMARY.md; git status --porcelain | grep -v "^??" | head -5</automated>
  </verify>
  <acceptance_criteria>
  - baseline 中 `TBD (81-01 ratchet)` 计数 = 0(commit 列已回填短 SHA)
  - SUMMARY 含两次跑差异表 + flake 根因段
  - 工作树无未预期残留(asset_columns_schema.json 已还原或已入库)
  </acceptance_criteria>
  <commit>docs(81-01): SUMMARY 收口——两次跑差异 + flake 根因 + baseline SHA 回填</commit>
  <done>81-01 全链闭环:flake 修掉、绿色数字入档、threshold 77.5、baseline 时间线完整;81-02 可安全删豁免行。</done>
</task>

</tasks>

<notes>
- 严格串行:81-02 depends_on 本 plan(push 前必须已有 threshold ratchet 与绿色 profile);81-03 depends_on 81-02。
- **零生产 .go 改动是本 phase 硬约束**:Task 0 只许动 internal/scheduler/*_test.go;测量产物 coverage.out / cov_*.out 不入库。
- flake 政策(D-81-05):修一次 + `-count=3` 压测;全量跑若再挂,该包单独复跑 1 次;仍挂 → 停止并上报,禁止 t.Skip、禁止循环试错。
- threshold 取值(D-81-01):77.5 而非先例式 78.0 截断 —— 偏离是决策不是笔误,理由必须落 baseline Notes 与 SUMMARY。
- 测量纪律:口径与 ci.yml L64-66 byte-identical;树必须干净(go:embed 的 asset_columns_schema.json);正式数字只出自绿色跑。
- D-07 原子 ratchet 先例是六文件,本 phase 因 check-coverage.sh 归 81-02,故本 plan 原子 commit 为三文件(threshold + baseline + STATE),SUMMARY+SHA 回填随后续 commit —— 与 71-01b/79-06 two-step 先例一致。
</notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测量脚本 ↔ 仓库状态 | check-coverage.sh 只读本地 coverage.out 与 threshold 文件,零外部输入 |
| 本地工作树 ↔ CI 干净 checkout | go:embed 的 asset_columns_schema.json 脏改会让本地绿 ≠ CI 绿(Pitfall 5) |
| 测试 fixture ↔ 生产 schema 语义 | fixture 建表不得反向"修复"生产代码/迁移(约束:零生产 .go 改动) |
| threshold 文件 ↔ gate 强制力 | UP-only 是纪律不是代码:脚本无「新值 ≥ 旧值」检查,靠 git diff 证据链 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-01-01 | Tampering | 带 FAIL 的 profile 被 ratchet 入 threshold | mitigate | Task 1 验收 `grep -c "^FAIL"` = 0 才准进 Task 2;SUMMARY 强制记录两次跑差异 |
| T-81-01-02 | Tampering | threshold 向下改 / 越写越高失真 | mitigate | `git diff .coverage-threshold` 单向证据 + baseline Notes 偏离理由强制 |
| T-81-01-03 | Repudiation | flake 用 t.Skip 掩盖造成"零失败"假象 | mitigate | D-81-05 禁 skip;acceptance grep `+.*t.Skip` = 0;根因强制入档 |
| T-81-01-04 | DoS | fixture 补 AutoMigrate 引入慢表/全模型迁移拖垮全量时长 | mitigate | 只注册被触及的 duty 族表;沿用最小 DDL 先例;`-count=3` 时长对照 |
| T-81-01-SC | Tampering | 依赖安装 | accept | 零新增依赖(research Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `go test -count=3 -timeout 15m ./internal/scheduler/` exit 0;`git diff --name-only` 只含 *_test.go(Task 0)
- 全量 `go test -timeout 15m -count=1 -coverprofile=coverage.out -covermode=atomic ./internal/... ./pkg/... ./cmd/...` EXIT=0 且无 ^FAIL(Task 1)
- `bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold` EXIT=0,weighted ≥ 77.5,P1 8/8 + P2 10/10 PASS(Task 1/2)
- `.coverage-threshold` = `77.5` 无换行,git diff 单向上调(Task 2)
- baseline Phase 81 后 行 commit 列已回填短 SHA;75-78 行或显式不回填理由在案(Task 2/3)
- 生产 .go 改动 = 0 文件;coverage.out 不入库
</verification>

<success_criteria>
1. internal/scheduler 满载 flake 修复:`-count=3` 绿 + 全量绿,零生产 .go 改动、零 t.Skip、根因入 SUMMARY
2. 正式绿色全量重测成立:EXIT=0、^FAIL=0、gate EXIT=0、PACKAGE 行 weighted ≥ 78.0%(实测 ≥ 77.5 目标线)
3. `.coverage-threshold` 55.5 → 77.5(UP-only git diff 证据 + D-81-01 偏离理由落 baseline Notes)—— SC-1 的 threshold 半边达成
4. coverage-baseline.md:Phase 81 后 行(含 per-package 块与 Ratchet note)+ Phase 75/76/77/78 回填或显式不回填理由;STATE.md 进度同步
5. 81-01-SUMMARY.md 含两次跑差异表;每 task 一原子 commit(共 3 个:flake fix / ratchet / SUMMARY+SHA)
</success_criteria>

<decision_audit>
本 plan 涉及的 phase-81 决策(executor 必须遵守):

| ID | 决策 | 依据 / 理由 |
|----|------|------------|
| D-81-01 | threshold 新值 = **77.5**(非先例 1-decimal 截断 78.0):0.5pp CI-drift 缓冲;偏离理由强制落 baseline Notes("199-commit 批次 CI 未见过 + 首次跨 OS 大跃迁");UP-only 不破 | research DQ1 + R2(A1: CI 漂移 ≤1.7% 先例;0.01pp 刀口不值得赌);planner 采纳研究建议 |
| D-81-05 | flake 处置政策:修一次(fixture 注册/goroutine 排空)+ `-count=3` 压测;全量跑再挂则该包复跑 1 次;仍挂 → 停止上报。**禁止 t.Skip 掩盖**(SC 需零失败证据);AutoMigrate 缺表是确定性根因,不是时序赌运气 | research DQ5 + R1(A2);本 plan Task 0 执行 |
| (沿用) | 正式测量口径与 ci.yml L64-66 byte-identical;禁 -race(D-01);自算加权平均禁止,一律 gate 脚本 | research Don't Hand-Roll + Architectural Responsibility Map |
| (沿用) | 原子 ratchet commit 模式(本 plan 三文件变体 + two-step SHA 回填) | D-07 六文件先例(74-11)+ 71-01b/79-06 受保护分支免 amend 先例 |
| (沿用) | 干净树测量(go:embed 脏文件 stash/协调)+ v1.28 并发会话协调 | research Pitfall 5 + R6 |

数字来源:81-RESEARCH.md §Current Measured State(2026-08-28 实测 78.01% / 43729 / 34112;正式数以本 plan 绿跑为准,允许小幅修正)。
</decision_audit>

<output>
Create `.planning/workstreams/milestone/phases/81-closeout-ratchet-audit/81-01-SUMMARY.md` when done
</output>
