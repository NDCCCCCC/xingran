---
phase: 79-services-root-tail
plan: 01
status: pending
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/services/data_cache_service_79_01_test.go
  - internal/services/cache_config_service_79_01_test.go
  - internal/services/cache_infra_tail_79_01_test.go
autonomous: true
requirements: [TAIL-01]

must_haves:
  truths:
    - "按 D-79-01 裁决:SC-1 的『legacy cache services 群逐文件 ≥70%』在本 phase 重锚为 root 实际存在的 4 个 cache 文件(data_cache_service / cache_config_service / template_cache / mac_history_cache_decorator);internal/services/system 的 6 个 *_cache_impl.go 不进 Phase 79 范围(它们已有专属测试文件,归 Phase 73 收口)"
    - "data_cache_service.go(77 stmts @19.5%)≥70%:GetOrSet 命中/未命中+回写/查询失败三态、DeleteByPattern(命中/空集)、MGet/MDelete、Exists、SetTTL/GetTTL、GetStats、GetExpiration(cacheConfig nil/非nil)各有用例;P0 #9 同步写缓存语义以注释+断言锁定"
    - "cache_config_service.go(124 stmts @67.7%)≥75%:LoadConfigs/setDefaultsIfNeeded(sqlite sys_config)+ GetDuration/GetDurationWithDefault/GetRateLimit/GetAllConfigs/GetConfigInfo/getConfigName/getConfigRemark/ReloadConfig 补齐;既有 5 个 rate-limit 测试保留不重写"
    - "SC-2 具名文件 discharge:token_blacklist_service.go 73.3%→≥85%(RemoveFromBlacklist :129 从 0% 清起 + rememberNegative :115 尾支 + AddToBlacklist :61 尾支);本 plan 触及的 7 个文件无一留在 <50%"
    - "template_cache.go 0%→≥70%(Get 未命中/命中/错误 + Clear)、mac_history_cache_decorator.go 0%→100%(BuildMACQueryCacheKey 4 前缀 + 未知方法 + 序列化失败)、rate_limiter.go 84.1%→≥90%、mac_normalize.go 93.3%→100%"
    - "按 D-79-06 命名:新增文件 `<source-or-topic>_79_01_test.go` 同包共置;helper 一律带 plan 后缀 `newXxx7901`(R5 防同包重名);禁 t.Parallel"
  artifacts:
    - path: "internal/services/data_cache_service_79_01_test.go"
      provides: "DataCacheService 全方法测试(前缀 TestDcs7901_),MemoryCache + miniredis 双装配"
      min_lines: 260
      contains: "miniredis.RunT"
    - path: "internal/services/cache_config_service_79_01_test.go"
      provides: "CacheConfigService 配置路径测试(前缀 TestCcs7901_),sqlite sys_config 驱动"
      min_lines: 180
      contains: "setDefaultsIfNeeded"
    - path: "internal/services/cache_infra_tail_79_01_test.go"
      provides: "token_blacklist/template_cache/mac_history_cache_decorator/rate_limiter/mac_normalize 五小文件清欠(前缀 TestTbl7901_/TestTmc7901_/TestMcd7901_/TestRlm7901_/TestMnm7901_)"
      min_lines: 220
      contains: "BuildMACQueryCacheKey"
  key_links:
    - from: "internal/services/data_cache_service_79_01_test.go"
      to: "DataCacheService.cache 字段(cache.Cache 接口,data_cache_service.go:40)"
      via: "cache.NewMemoryCache(1000, 5*time.Minute) 与 miniredis.RunT(t)+cache.NewRedisCache 双装配注入"
      pattern: "NewMemoryCache|NewRedisCache"
    - from: "internal/services/cache_config_service_79_01_test.go"
      to: "LoadConfigs 的 sys_config 表读(cache_config_service.go:143)"
      via: "sqlite 建表 + 种子行,断言 setDefaultsIfNeeded 回填默认值"
      pattern: "LoadConfigs|sys_config"
    - from: "internal/services/cache_infra_tail_79_01_test.go"
      to: "DataCacheService.DeleteByPattern 的 cache.Keys 调用(data_cache_service.go:119)"
      via: "MemoryCache Keys 模式匹配 → MDelete,断言跨方法联动"
      pattern: "DeleteByPattern"
---

# Plan 1: root 缓存基建 + SC-2 具名文件清欠 (Phase: 79-services-root-tail)

<objective>
交付 internal/services root 的缓存基建簇测试:data_cache_service.go(62 unc)+ cache_config_service.go(40 unc)+ token_blacklist_service.go(12 unc)+ template_cache.go(18 unc)+ mac_history_cache_decorator.go(12 unc)+ rate_limiter.go(10 unc)+ mac_normalize.go(1 unc),合计 ~155 unc。

Purpose: TAIL-01 的第一段 + 全 phase 约定沉淀段。本 plan 先把 SC-2 点名的 token blacklist 清欠掉、把 D-79-01 重锚后的 4 个 root cache 文件逐个推过 70%,并落地 sqlite/MemoryCache/miniredis 三种装配 helper 供 79-02..79-06 复用(78-01 先例)。
Output: 新增 3 个测试文件(零生产代码改动,D-79 escape hatch 除外)。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/79-services-root-tail/79-RESEARCH.md

<interfaces>
<!-- 关键契约(planner 已逐一读源码复核,2026-08-27)。executor 直接使用,无需重新探索。 -->

From internal/services/data_cache_service.go:
```go
// :39-42 struct{ cache cache.Cache; cacheConfig *CacheConfigService }
func NewDataCacheService(cache cache.Cache) *DataCacheService      // :45
func (s *DataCacheService) SetCacheConfig(cacheConfig *CacheConfigService) // :50
func (s *DataCacheService) GetExpiration(configKey string, defaultExpiration time.Duration) time.Duration // :55 — cacheConfig nil → 直接返回 default
func (s *DataCacheService) Get(ctx, key, dest) error     // :63 — cache miss(data=="") → apperrors.CacheKeyNotFound();坏 JSON → Unmarshal 错误
func (s *DataCacheService) Set(ctx, key, value, expiration) error // :75 — json.Marshal 失败 → "序列化失败"
func (s *DataCacheService) GetOrSet(ctx, key, dest, expiration, query func() (interface{}, error)) error // :84
//   :85 先 Get,命中(err==nil)直接返回;:90 query() 失败 → "查询数据失败: %w"
//   :105 P0 #9 同步写缓存(context.Background()),写失败仅 Warnf 不上抛 —— 用注释+断言锁定该语义,勿改
func (s *DataCacheService) Delete(ctx, key) error        // :113 透传
func (s *DataCacheService) DeleteByPattern(ctx, pattern) error // :118 — cache.Keys → len==0 直接返回 nil(不走 MDelete)
func (s *DataCacheService) MGet(ctx, keys...) (map[string]string, error) // :130 — 缺失/空值键不进结果 map
func (s *DataCacheService) MDelete(ctx, keys...) error   // :145
func (s *DataCacheService) Exists(ctx, key) (bool, error) // :150
func (s *DataCacheService) SetTTL / GetTTL               // :155/:160 → cache.Expire / cache.TTL
func (s *DataCacheService) GetStats(ctx) (*CacheStats, error) // :165 — Keys("*") → KeyCount + Count
func NewCacheKeyBuilder(prefix string) *CacheKeyBuilder   // :197(已有测试覆盖,勿重写)
```

From internal/services/cache_config_service.go:
```go
func NewCacheConfigService(db *gorm.DB) *CacheConfigService // :127
func (s *CacheConfigService) LoadConfigs(ctx) error         // :143 — 读 sys_config 全表 → 内存 map
func (s *CacheConfigService) setDefaultsIfNeeded(_ context.Context) // :266 — 空配置回填默认值
func (s *CacheConfigService) GetDuration(configKey) time.Duration          // :379
func (s *CacheConfigService) GetDurationWithDefault(configKey, defaultDuration) time.Duration // :392
func (s *CacheConfigService) GetRateLimit(key, defaultValue int) int       // :407(既有测试覆盖)
func (s *CacheConfigService) ReloadConfig(ctx) error                       // :418
func (s *CacheConfigService) GetAllConfigs(ctx) map[string]int             // :424
func (s *CacheConfigService) GetConfigInfo() map[string]ConfigInfo         // :437
func (s *CacheConfigService) getConfigName(key) string                     // :896
func (s *CacheConfigService) getConfigRemark(key) string                   // :905
```

From internal/services/token_blacklist_service.go:
```go
func NewTokenBlacklistService(cache cache.Cache) TokenBlacklistService // :53(接口返回值)
func (s *tokenBlacklistService) AddToBlacklist(ctx, token, expiry) error // :61(80%,尾支未覆盖)
func (s *tokenBlacklistService) IsBlacklisted(ctx, token) (bool, error)  // :83(既有测试覆盖主干)
func (s *tokenBlacklistService) rememberNegative(token, now)            // :115(57.1%,过期尾支未覆盖)
func (s *tokenBlacklistService) RemoveFromBlacklist(ctx, token) error   // :129(0%,主力缺口)
func (s *tokenBlacklistService) getBlacklistKey(token) string           // :141
// 同包白盒可直接构造/注入内部 map 字段(以源码实际字段为准),走 t.Cleanup 清理
```

From internal/services/template_cache.go:
```go
func NewTemplateCache() *TemplateCache  // :16 — sync.Map 缓存
func (c *TemplateCache) Get(templatePath string) (*templates.FSM, error) // :23 — 未命中→解析模板→写缓存;二次 Get = 命中;坏路径 → error
func (c *TemplateCache) Clear()          // :51 — 清空;Clear 后再 Get = 重新解析
```

From internal/services/mac_history_cache_decorator.go(全文 42 行):
```go
// 键前缀常量(cacheKeyPrefixPortHistory/DeviceHistory/Stats/Heatmap,D-13 锁定,禁裸字符串)
func BuildMACQueryCacheKey(method string, params interface{}) (string, error) // :21
//   method ∈ {"port-history","device-history","stats","heatmap"},default → "未知方法: %s"
//   params json.Marshal 失败 → "序列化缓存参数失败: %w";成功 → "<prefix>:<sha256hex>"
```

From pkg/cache(装配契约):
```go
func NewMemoryCache(maxSize int, cleanupTime time.Duration) *MemoryCache       // pkg/cache/memory.go:25
func NewRedisCache(config *CacheConfig, keyPrefix string) (*RedisCache, error) // pkg/cache/redis.go:30
// Cache 接口(pkg/cache/cache.go:9)含 Get/Set/Delete/Exists/MGet/MSet/MDelete/Increment*/Expire/TTL/Keys
```

既有 analog(必读,直接照搬装配手法;helper 名已被占用,新 helper 必须 plan 后缀):
- internal/services/mac_history_query_service_test.go:69 `setupTestService` —— 注意它用 `file::memory:?cache=shared`(跨用例共享内存库),本 plan 新 fixture 一律改用 `t.TempDir()` 文件库(research §4 明示防串数据)。
- internal/core/captcha_78_01_test.go:37-140 `newCap78Mem`/`newCap78Redis` —— miniredis+RedisCache 装配与 t.Cleanup 纪律范本(Phase 78 已验证)。
- internal/services/cache_config_service_test.go:1-54 —— 既有 fixture 形态与 rate-limit 测试口径(只补未覆盖分支,不改既有断言)。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DataCacheService 全方法双装配(MemoryCache + miniredis)</name>
  <files>internal/services/data_cache_service_79_01_test.go</files>
  <read_first>
  - internal/services/data_cache_service.go — :39-203 全文(尤其 :84-110 GetOrSet 的 P0 #9 同步写注释)
  - internal/services/data_cache_service_test.go — 既有 TestCacheKeyBuilder_*(勿重写,只补 DataCacheService 方法)
  - pkg/cache/memory.go — NewMemoryCache 签名与 Keys/Exists/TTL/Expire/MGet/MDelete 实现语义
  - pkg/cache/redis.go — NewRedisCache(config *CacheConfig, keyPrefix string) 签名
  - internal/core/captcha_78_01_test.go — miniredis 装配与 t.Cleanup 纪律范本
  </read_first>
  <action>
  创建 internal/services/data_cache_service_79_01_test.go(package services,同包白盒)。两个文件级 helper:

  - `newDcs7901(t *testing.T) (*DataCacheService, *cache.MemoryCache)` —— `cache.NewMemoryCache(1000, 5*time.Minute)` 注入 NewDataCacheService,`t.Cleanup(func(){ _ = mc.Close() })`(只 Close 一次;QUIRK-P1 二次 Close panic 已知未修,测试内禁二次 Close,SUMMARY 记录)。
  - `newDcs7901Redis(t *testing.T) (*DataCacheService, *miniredis.Miniredis, *cache.RedisCache)` —— `miniredis.RunT(t)`(自动 cleanup)+ `cache.NewRedisCache(&cache.CacheConfig{Addr: mr.Addr()}, "")` + `t.Cleanup(func(){ _ = rc.Close() })`;CacheConfig 其余字段以 pkg/cache/redis.go 实际定义为准。

  测试函数前缀 TestDcs7901_:

  - `TestDcs7901_SetGetRoundTrip`:两装配表驱动(结构体值 → Set → Get 到同型 dest,断言字段相等)。
  - `TestDcs7901_Get_MissAndBadJSON`:未命中键 → 错误即 `apperrors.CacheKeyNotFound()` 语义(errors.Is 或文案断言,以 pkg/errors 实际形态为准);预置非 JSON 字符串值 → Unmarshal 错误分支。
  - `TestDcs7901_Set_MarshalFail`:value 传 `make(chan int)` → 错误含"序列化失败"。
  - `TestDcs7901_GetOrSet_HitMissQueryError`:命中(预置键,query 内置 fail-fast sentinel 断言未被调用)/ 未命中(query 被调,dest 被填充,第二次调用 query 不再被调)/ query 返回 error → 错误含"查询数据失败"。
  - `TestDcs7901_GetOrSet_SyncWriteLocks_P0_9`:未命中路径返回后立即 `Exists(key)==true` —— 文件头注释 + 用例内注释写明 `// P0 #9 锁定:GetOrSet 为同步写缓存(禁裸 goroutine),此断言防回退`;且断言 query 数据已落缓存值(Set 的 JSON 可被 Get 读回)。
  - `TestDcs7901_DeleteByPattern_HitAndEmpty`:预置 3 键(2 命中模式)→ 删除后仅剩 1 键;模式无匹配 → 返回 nil 且不 panic(:123 len==0 分支)。
  - `TestDcs7901_MGet_MDelete_Partial`:3 键删 1 → MGet 结果 map 只含现存 2 键(缺失键不出现,非空串占位);MDelete 后 Exists 全 false。
  - `TestDcs7901_TTL_Ops`:SetTTL 后 GetTTL>0;miniredis 变体用 `mr.FastForward(2 * time.Hour)` 推进过期后断言 miss(R-1 纪律:禁裸 time.Sleep 推 TTL)。
  - `TestDcs7901_GetStats`:预置 2 键 → `stats.KeyCount==2 && stats.Count==2`(其余字段零值,按 :165-173 实现断言)。
  - `TestDcs7901_GetExpiration_NilConfig`:不注入 cacheConfig → GetExpiration 返回 default(两装配各一遍,覆盖 :56 nil 分支;注入分支由 Task 2 的 TestCcs7901_GetExpiration_Wired 收口)。

  纪律:禁 t.Parallel();helpers 名带 7901 后缀(R5);状态/语义断言禁裸 0/1 字面量。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestDcs7901_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestDcs7901_" internal/services/data_cache_service_79_01_test.go` ≥ 10
  - `grep -c "miniredis.RunT" internal/services/data_cache_service_79_01_test.go` ≥ 1
  - 文件含字面注释 `P0 #9 锁定`
  - 测试文件无裸 `time.Sleep` 用于 TTL 推进(一律 mr.FastForward)
  - `go test -count=1 -run "TestDcs7901_" ./internal/services/` exit 0;`go build ./...` exit 0
  </acceptance_criteria>
  <commit>test(79-01): DataCacheService 全方法双装配测试(MemoryCache+miniredis)</commit>
  <done>GetOrSet 三态 + P0 #9 同步写锁定 + DeleteByPattern 两分支 + MGet/MDelete/Exists/TTL/GetStats 全覆盖;两套装配 helper 就位供后续 plan 复用。</done>
</task>

<task type="auto">
  <name>Task 2: CacheConfigService 配置路径(sqlite sys_config 驱动)</name>
  <files>internal/services/cache_config_service_79_01_test.go</files>
  <read_first>
  - internal/services/cache_config_service.go — :127-160(NewCacheConfigService/LoadConfigs)+ :266-320(setDefaultsIfNeeded 默认值清单)+ :379-460(Get*/ReloadConfig/GetAllConfigs/GetConfigInfo)+ :896-930(getConfigName/getConfigRemark)
  - internal/services/cache_config_service_test.go — 既有 fixture 与 5 个 rate-limit 测试(勿改名/勿重写,新 helper 另起 `newCcs7901`)
  - internal/core/core_74_08_test.go:31-46 — sys_config 手动 CREATE TABLE 形态(列集按 models.Config 实际字段裁剪)
  </read_first>
  <action>
  创建 internal/services/cache_config_service_79_01_test.go(package services)。helper `newCcs7901(t *testing.T, seed map[string]string) (*CacheConfigService, *gorm.DB)`:sqlite(t.TempDir 文件库)+ AutoMigrate 或手动 CREATE `sys_config`(与既有 cache_config_service_test.go 同一形态)+ 种子行写入 + `NewCacheConfigService(db)` + `LoadConfigs(ctx)`。测试函数前缀 TestCcs7901_:

  - `TestCcs7901_LoadConfigs_EmptyBackfillsDefaults`:空表 → LoadConfigs → setDefaultsIfNeeded 回填(GetAllConfigs 非空,抽 1-2 个已知默认键断言非零值,以 :266 默认清单为准)。
  - `TestCcs7901_LoadConfigs_SeedOverridesDefault`:预置某 duration 键 = "120" → GetDuration 该键返回 120*time.Second(单位换算以 :379 实现为准)。
  - `TestCcs7901_Duration_InvalidAndUnknown`:预置非数字值 → GetDuration 回退内部默认;未知键 → GetDurationWithDefault 返回显式 default(:392 分支)。
  - `TestCcs7901_GetRateLimit_ClampTail`:只补既有 TestCacheConfigService_RangeValidation 未覆盖的钳制尾支(低于下限/高于上限各一,值以 :407-417 实现为准;既有断言不改)。
  - `TestCcs7901_GetConfigInfo_NamesRemarks`:GetConfigInfo 返回 map 非空,抽 2 键断言 getConfigName/getConfigRemark 非空串;未知键 → 空串/回退分支。
  - `TestCcs7901_ReloadConfig_PicksUpDBChange`:测试中直接 UPDATE sys_config 行 → ReloadConfig → GetRateLimit 反映新值(区分 LoadConfigs 冷读与 Reload 热读两分支)。
  - `TestCcs7901_GetExpiration_Wired`:ccs 注入 DataCacheService(SetCacheConfig)→ GetExpiration(配置键, default) 返回配置值;未配置键 → default(收口 Task 1 的注入分支)。

  断言一律引用源码常量/默认值变量,禁裸数字魔法值(默认值断言引用 :266 清单里的变量)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestCcs7901_" ./internal/services/ && go test -count=1 -run "TestCacheConfigService|TestRateLimitProvider" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestCcs7901_" internal/services/cache_config_service_79_01_test.go` ≥ 7
  - 既有 5 个测试(TestCacheConfigService_*/TestRateLimitProviderInterface)仍全绿(未被改动)
  - `go test -count=1 ./internal/services/ -run "TestCcs7901_"` exit 0
  </acceptance_criteria>
  <commit>test(79-01): CacheConfigService 配置路径测试(sys_config 种子/回填/热载)</commit>
  <done>LoadConfigs/setDefaultsIfNeeded 回填与覆盖、Duration/RateLimit 尾支、GetConfigInfo、ReloadConfig 热载、GetExpiration 注入分支全覆盖。</done>
</task>

<task type="auto">
  <name>Task 3: token_blacklist 清欠(SC-2 点名)+ template_cache</name>
  <files>internal/services/cache_infra_tail_79_01_test.go</files>
  <read_first>
  - internal/services/token_blacklist_service.go — :53-150 全文(重点 :115 rememberNegative 过期尾支、:129 RemoveFromBlacklist、:61 AddToBlacklist 尾支、:141 getBlacklistKey)
  - internal/services/token_blacklist_service_test.go — 既有 4 个测试覆盖的分支清单(勿重写,只补 unc)
  - internal/services/template_cache.go — :16-58 全文 + internal/templates 包的 FSM 解析入口(确认最小合法 TextFSM 模板形态)
  </read_first>
  <action>
  创建 internal/services/cache_infra_tail_79_01_test.go(package services)。helper `newTbl7901(t *testing.T) (TokenBlacklistService, *cache.MemoryCache)`:MemoryCache 装配 + t.Cleanup 单次 Close。

  token_blacklist(TestTbl7901_ 前缀):
  - `TestTbl7901_RemoveFromBlacklist_AfterAdd`:AddToBlacklist → IsBlacklisted true → RemoveFromBlacklist → IsBlacklisted false(:129 0% 主力缺口);RemoveFromBlacklist 不存在的 token → 不报错。
  - `TestTbl7901_AddToBlacklist_TailBranch`:对照 :61-82 实现补未覆盖尾支(以 cover 数字为指向:80% 剩余分支,常见为过期时间零值/取整路径),断言写入键的 TTL 与 expiry 语义一致。
  - `TestTbl7901_RememberNegative_TailBranch`:同包白盒预置负缓存内部状态使 :115 走过期尾支(以源码字段为准;若字段为 map 则直写过期时间戳),断言过期后负缓存不再命中。
  - `TestTbl7901_GetBlacklistKey`:同包直调 getBlacklistKey,断言键前缀形态(:141)。

  template_cache(TestTmc7901_ 前缀,helper `newTmc7901(t) *TemplateCache`):
  - `TestTmc7901_Get_MissHitError`:模板文件来源二选一 —— (a) 优先 glob 仓库真实 TextFSM 模板(如 `internal/templates/**` 或 `templates/**` 下的 .textfsm/.tpl)复制一份到 t.TempDir() 作为 fixture(保证解析合法);(b) 若无可复制模板,手写最小合法 TextFSM(`Value MAC (\\S+)` + `Start` + `^${MAC} -> Record`)落 t.TempDir()。三态断言:首次 Get 未命中→返回 *templates.FSM 非 nil;同路径二次 Get 命中(可观察证据:破坏源文件后二次 Get 仍成功 = 走了 sync.Map 缓存);不存在路径 → error。Clear() 后再 Get 重新解析(先破坏源文件再 Clear+Get 应报错,证明缓存已清)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestTbl7901_|TestTmc7901_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestTbl7901_\|^func TestTmc7901_" internal/services/cache_infra_tail_79_01_test.go` ≥ 6
  - 存在断言"RemoveFromBlacklist 后 IsBlacklisted=false"的用例(SC-2 点名缺口)
  - template_cache 用例含缓存命中可观察证据(源文件破坏后命中)
  - `go test -count=1 -run "TestTbl7901_|TestTmc7901_" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-01): token_blacklist RemoveFromBlacklist 清欠 + template_cache 三态</commit>
  <done>token_blacklist ≥85%(RemoveFromBlacklist/rememberNegative 尾支/AddToBlacklist 尾支);template_cache 未命中/命中/Clear 三态覆盖。</done>
</task>

<task type="auto">
  <name>Task 4: mac_history_cache_decorator + rate_limiter 尾支 + mac_normalize 收口</name>
  <files>internal/services/cache_infra_tail_79_01_test.go</files>
  <read_first>
  - internal/services/mac_history_cache_decorator.go — :12-42 全文(4 个键前缀常量 + BuildMACQueryCacheKey 分支)
  - internal/services/rate_limiter.go — :192-260(getOrCreateWindow/cleanOlderThan/calculateRemaining/calculateReset,84.1% 剩余 ~10 stmts 的所在段)
  - internal/services/rate_limiter_test.go — 既有 10 个测试(勿重写,只补 unc 尾支)
  - internal/services/mac_normalize.go — :32-80(NormalizeMACAddress/isCanonicalMAC,93.3% 剩 1 stmt)
  - internal/services/mac_normalize_test.go — 既有断言口径
  </read_first>
  <action>
  同文件 cache_infra_tail_79_01_test.go 追加:

  - `TestMcd7901_BuildKey_AllPrefixes`:4 个合法 method 各断言返回键前缀 == 对应 `cacheKeyPrefix*` 常量 + ":" + 64 位 hex;同 params 同 method 幂等(两次调用同键)。
  - `TestMcd7901_BuildKey_UnknownMethod`:method="bogus" → 错误含"未知方法"。
  - `TestMcd7901_BuildKey_MarshalFail`:params 传 `make(chan int)` → 错误含"序列化缓存参数失败"。
  - `TestRlm7901_CalculateRemaining_Edges`:表驱动 :229 calculateRemaining 的零计数/满限/超限边界(期望值按源码公式手算写入用例注释)。
  - `TestRlm7901_CalculateReset_AndWindow`::251 calculateReset 空 times 切片分支 + :192 getOrCreateWindow 复用既有窗口分支(同 key 二次调用返回同一 *rateLimitWindow,可观察证据:往返回窗口写入时间戳后再次取窗仍可见)。
  - `TestRlm7901_CleanOlderThan_Cutoff`:构造 mixed 新旧时间戳切片,断言 cutoff 边界两侧的保留/剔除。
  - `TestMnm7901_IsCanonicalMAC_LastBranch`:对照 mac_normalize.go 现有 cover 缺口(1 stmt,通常为 isCanonicalMAC 某前缀分支)补表驱动用例;断言 NormalizeMACAddress 全链不回归(引用既有 mac_normalize_test.go 的口径,只增不改)。

  先跑一次定向 profile 找准 unc 行再写用例(命令见 verify 第二段),避免与既有测试重复。
  </action>
  <verify>
    <automated>go test -count=1 -coverprofile=/tmp/c7901_t.out -run "TestRlm7901_|TestMcd7901_|TestMnm7901_" ./internal/services/ ; go test -count=1 -run "TestRlm7901_|TestMcd7901_|TestMnm7901_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestRlm7901_\|^func TestMcd7901_\|^func TestMnm7901_" internal/services/cache_infra_tail_79_01_test.go` ≥ 6
  - BuildMACQueryCacheKey 的 4 前缀 + 未知方法 + marshal 失败 6 分支各有断言
  - `go test -count=1 -run "TestNormalizeMACAddress|TestRlm7901_|TestMcd7901_|TestMnm7901_" ./internal/services/` exit 0(既有用例不回归)
  </acceptance_criteria>
  <commit>test(79-01): mac cache decorator 100% + rate_limiter/mac_normalize 尾支收口</commit>
  <done>mac_history_cache_decorator 0%→100%;rate_limiter ≥90%;mac_normalize 100%。</done>
</task>

<task type="auto">
  <name>Task 5: plan 收口 — 全包 per-file 覆盖率实测 + -race + SUMMARY</name>
  <files>.planning/workstreams/milestone/phases/79-services-root-tail/79-01-SUMMARY.md</files>
  <read_first>
  - .planning/workstreams/milestone/phases/79-services-root-tail/79-RESEARCH.md §2 — 7 个目标文件的基线数字(对照用)
  </read_first>
  <action>
  收口三步(本 plan 交付口径):

  1. 全包实测(只跑一次,R4 防慢):
     `go test -count=1 -coverprofile=cov79_01.out ./internal/services/`
     `go tool cover -func=cov79_01.out | grep -E "data_cache_service.go|cache_config_service.go|token_blacklist_service.go|template_cache.go|mac_history_cache_decorator.go|rate_limiter.go|mac_normalize.go"`
  2. 逐文件达标判定:七个文件全部 ≥ 各自目标(data_cache ≥70% / cache_config ≥75% / token_blacklist ≥85% / template_cache ≥70% / decorator 100% / rate_limiter ≥90% / mac_normalize 100%);未达标的文件回到对应 task 补用例后重测(只补 unc 分支,不重写既有断言)。
  3. 稳定性:`go test -race -count=1 -run "TestDcs7901_|TestCcs7901_|TestTbl7901_|TestTmc7901_|TestRlm7901_|TestMcd7901_|TestMnm7901_" ./internal/services/`(本 plan 新增用例的 race 抽样,全包 -race 由 79-06 收口承担)+ `go build ./...` + `go test ./...`。

  SUMMARY 落:七个文件的实测覆盖率表(基线 → 实测)、D-79-01 重锚声明(4 个 root cache 文件为 SC-1 锚点;system 包 6 个 *_cache_impl.go 不进本 phase)、QUIRK-P1(MemoryCache.Close 二次 close panic)仍未修、留 Phase 80 的记录、沉淀的三个装配 helper 名(newDcs7901/newDcs7901Redis/newCcs7901)供 79-02..79-06 复用。
  </action>
  <verify>
    <automated>go test -count=1 -coverprofile=cov79_01.out ./internal/services/ && go tool cover -func=cov79_01.out | grep -E "data_cache_service.go|cache_config_service.go|token_blacklist_service.go|template_cache_service.go|mac_history_cache_decorator.go|rate_limiter.go|mac_normalize.go" && go test -race -count=1 -run "TestDcs7901_|TestCcs7901_|TestTbl7901_" ./internal/services/ && go build ./... && go test ./... 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
  - 七个目标文件 per-file 覆盖率全部达各自目标值(数字落 SUMMARY)
  - 本 plan 触及文件无一 <50%(SC-2 discharge)
  - `go test ./...` exit 0;`go build ./...` exit 0;-race 抽样 exit 0
  - `git status --porcelain | grep _test.go` 无未提交测试文件(每 task 一原子 commit 已收)
  </acceptance_criteria>
  <commit>docs(79-01): 收口 7 文件覆盖率实测 + D-79-01 重锚声明落 SUMMARY</commit>
  <done>SC-1(root cache 4 文件逐文件 ≥70%)+ SC-2 具名文件 discharge 完成;sqlite/MemoryCache/miniredis 装配 helper 沉淀给后续 5 个 plan。</done>
</task>

</tasks>

<notes>
- 依赖:Phase 76 的 miniredis/httpmock 已在 go.mod(research §8),本 plan 零新增依赖。
- 被依赖:79-02..79-06 复用本 plan 的 sqlite(t.TempDir 文件库)/MemoryCache/miniredis 装配纪律与 `newXxx79NN` helper 命名法。
- race 安全:MemoryCache/miniredis 都用 t.Cleanup Close 且只 Close 一次(QUIRK-P1 未修,二次 Close panic);禁 t.Parallel。
- 运行时长:root 包全量 `go test` 基线 ~377s(R4),per-task 只跑 `-run` 抽样,全包 profile 每 plan 只跑一次(收口 task)。
- 同包串行:本 plan 是 wave 1;79-02..79-06 同包必须逐 wave 串行(78-01 D-78-11 先例,整包编译互踩规避)。
- quirk 纪律:发现行为与源码注释/docs 不符 → 按 D-78-10 同构口径就地记录,零生产代码改动(D-79 escape hatch 仅限源码-vs-文档错配)。
</notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 ↔ 数据库 | 全部 glebarez sqlite(t.TempDir 文件库),不触碰生产 PG |
| 测试 ↔ Redis | 仅 miniredis.RunT 进程内实例(mr.Addr()),禁任何真实 Redis 地址字面量 |
| 测试 ↔ 文件系统 | TextFSM 模板 fixture 落 t.TempDir(),禁写仓库模板目录 |
| 测试 ↔ 生产代码 | 仅新增 *_79_01_test.go;生产 .go 改动 = 0(D-79 escape hatch 除外) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-01-01 | Tampering | 测试误连真实 Redis/PG | mitigate | 仅 mr.Addr() 与 sqlite;grep 测试文件无 redis:// / postgres DSN 字面量 |
| T-79-01-02 | DoS | MemoryCache/RedisCache 未 Close 致 goroutine 泄漏 | mitigate | t.Cleanup 单次 Close + race 抽样验收;QUIRK-P1 二次 Close panic 明确记录不触发 |
| T-79-01-03 | Tampering | miniredis TTL 语义差异致 flake | mitigate | TTL 推进一律 mr.FastForward,禁裸 time.Sleep(R-1) |
| T-79-01-SC | Tampering | 依赖安装 | accept | 零新增依赖(miniredis v2.38.0 已在 go.mod:8;sqlite/testify 已有) |
</threat_model>

<verification>
- `go test -count=1 ./internal/services/` 全绿;新增用例 `-race` 抽样全绿
- `go tool cover -func=cov79_01.out`:data_cache ≥70% / cache_config ≥75% / token_blacklist ≥85% / template_cache ≥70% / mac_history_cache_decorator =100% / rate_limiter ≥90% / mac_normalize =100%
- 本 plan 触及文件无一 <50%(SC-2)
- `go build ./...` + `go test ./...` exit 0
- 生产 .go 改动 = 0 文件
</verification>

<success_criteria>
1. 三个测试文件存在且全绿:TestDcs7901_ ≥10、TestCcs7901_ ≥7、TestTbl7901_/TestTmc7901_/TestRlm7901_/TestMcd7901_/TestMnm7901_ 合计 ≥12
2. data_cache_service.go ≥70%(基线 19.5%)、cache_config_service.go ≥75%(基线 67.7%)、token_blacklist_service.go ≥85%(基线 73.3%)
3. template_cache.go ≥70%(基线 0%)、mac_history_cache_decorator.go =100%(基线 0%)、rate_limiter.go ≥90%(基线 84.1%)、mac_normalize.go =100%(基线 93.3%)
4. D-79-01 重锚声明落 SUMMARY(SC-1 = root 4 cache 文件逐文件 ≥70%)
5. `go build ./...` + `go test ./...` exit 0;每 task 一原子 commit
</success_criteria>

<decision_audit>
本 plan 涉及的 phase-79 决策(79-RESEARCH.md DQ1-DQ6 在此定案,executor 必须遵守):

| ID | 决策 | 依据 / 理由 |
|----|------|------------|
| D-79-01 | SC-1 重锚:『legacy cache services 群(dept/role/dict/menu/user/post)逐文件 ≥70%』改判为 root 实际存在的 4 个 cache 文件(data_cache_service/cache_config_service/template_cache/mac_history_cache_decorator)逐文件 ≥70%;internal/services/system 的 6 个 *_cache_impl.go 不进 Phase 79 | 79-RESEARCH §1 实测:那 6 个文件已迁 system 包且不在 5202 stmts 口径内,ROADMAP SC-1 字面引用失实;采研究推荐 (a)+(c) 合并。ROADMAP 的 plan 标题(79-01 dept/role/dict 群 A、79-02 menu/user/post 群 B)同因失实,本 phase 计划集按 research §7 重切(D-79-07) |
| D-79-06 | 测试文件命名 `<source-or-topic>_79_01_test.go`;多源合并文件用 topic 前缀(cache_infra_tail_79_01_test.go);NN = plan 号 | DQ6 推荐 confirmed;topic 前缀是 `<source>` 在多文件合并时的自然推广,保持 phase 可追溯性 |
| D-79-07 | 本 plan 范围 = research §7 的 79-01 行(7 文件 ~155 unc);notice_query_service.go 从本 plan 移至 79-03(fixture 内聚:buildUserVisibleQuery 是 NoticeService 方法,notice 表由 79-03 建) | planner 微调,fixture 内聚优先;净效果为零遗漏(research §2 全表 45 文件在 6 plan 中各归其位,见 79-06 收口核对清单) |
| (沿用) | quirk 纪律 = Phase 77 D-01/D-02/D-03 与 78 D-78-10 同构:有据就地修+同 commit 翻转断言,无据按现行为断言+SUMMARY 记录;本 phase 默认零生产代码改动 | R7 + ROADMAP Phase 79 Notes L247(『锁定+注释,不擅自扩修』) |
| (沿用) | helper 命名 `newXxx79NN`(R5)+ 同包串行 wave(D-78-11 同构) | 78-01/77-05 已验证 |

基线数字来源:79-RESEARCH.md §2(2026-08-27 实测 `go test -count=1 -coverprofile ./internal/services/` → 5202 stmts / 589 covered / 11.3%)。
</decision_audit>

<output>
Create `.planning/workstreams/milestone/phases/79-services-root-tail/79-01-SUMMARY.md` when done
</output>
