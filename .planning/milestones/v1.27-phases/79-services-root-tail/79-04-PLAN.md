---
phase: 79-services-root-tail
plan: 04
status: pending
type: execute
wave: 4
depends_on: ["79-03"]
files_modified:
  - internal/services/knowledge_service_79_04_test.go
  - internal/services/network_device_service_79_04_test.go
  - internal/services/notification_chain_79_04_test.go
  - internal/services/auth_credential_service_79_04_test.go
autonomous: true
requirements: [TAIL-01]

must_haves:
  truths:
    - "knowledge_service.go(285 unc @1.4%)≥70%:文章 CRUD/搜索/统计/浏览点赞自增/工作单转文章 + 分类树(递归子类)/标签族(ParseTagsFromContent/AutoCreateTagsFromContent/GetOrCreateTag)全覆盖"
    - "network_device_service.go(202 unc @0%)≥70%:List(过滤/分页/关联 preload)/Create/QuickCreateDevice/GetByID/Update/Delete/BatchDelete/UpdateStatus(Batch)/GetDeviceStatistics/GetDevicesByDept/GetDevicesByCredential + getLastIPOctet 纯函数;discovery/deviceInfoCollection 依赖以 nil 注入走错误分支"
    - "notification_config_service.go(127 unc)≥70%:email/API 通知配置 CRUD + EncryptPassword/DecryptPassword(:246/:277)AES-GCM round-trip + 错 key 解密失败分支"
    - "notification_sender_service.go(120 unc)≥70%:SendNotification 派发(nil 注入 sender → 错误分支)+ buildRecipientList/getUserInfo/getUserEmails/getUserPhones/uniqueStrings/isValidPublishStatus + PublishAndSendNotice + Set/GetNotificationChannels"
    - "auth_credential_service.go(142 unc)≥70%:CRUD/GetByIDWithPassword/GetDecryptedCredential(addomain.PasswordCipher stub,Phase 73 D-02 范本)/validateCredentialConfig/GetStatistics/默认凭据唯一性"
    - "已知 quirk 锁定不修(Phase 73-03 已记录):knowledge UUID inline-Delete、knowledge GetOrCreateTag 跨 tx 连接、network GetByID 关联名丢失 —— 按现行为断言+注释,SUMMARY 复记"
  artifacts:
    - path: "internal/services/knowledge_service_79_04_test.go"
      provides: "KnowledgeService 全方法测试(前缀 TestKsv7904_),文章/分类/标签三族"
      min_lines: 520
      contains: "GetOrCreateTag"
    - path: "internal/services/network_device_service_79_04_test.go"
      provides: "NetworkDeviceService 全方法测试(前缀 TestNdv7904_),nil 依赖注入 + 关联 preload"
      min_lines: 380
      contains: "QuickCreateDevice"
    - path: "internal/services/notification_chain_79_04_test.go"
      provides: "notification_config + notification_sender 测试(前缀 TestNcf7904_/TestNsn7904_),AES round-trip + nil-sender 派发"
      min_lines: 380
      contains: "EncryptPassword"
    - path: "internal/services/auth_credential_service_79_04_test.go"
      provides: "AuthCredentialService 全方法测试(前缀 TestAcv7904_),PasswordCipher stub"
      min_lines: 300
      contains: "GetDecryptedCredential"
  key_links:
    - from: "internal/services/knowledge_service_79_04_test.go"
      to: "CreateKnowledgeArticle → GetOrCreateTag → knowledge_article_tags 关联(knowledge_service.go:220/:738)"
      via: "建文带 tag 名 → 断言标签行 + 关联行;quirk(跨 tx 连接)按现行为锁定"
      pattern: "GetOrCreateTag|ParseTagsFromContent"
    - from: "internal/services/notification_chain_79_04_test.go"
      to: "SendNotification → sendNotificationByChannels 派发(notification_sender_service.go:30/:98)"
      via: "NotificationSenderService 的 sender 依赖留 nil → 断言 email/api 通道错误分支不 panic"
      pattern: "sendEmailNotification|sendAPINotification"
    - from: "internal/services/auth_credential_service_79_04_test.go"
      to: "GetDecryptedCredential → passwordCipher.Decrypt(auth_credential_service.go:381/:20)"
      via: "addomain.PasswordCipher 接口 stub(per-interface *Func 字段,Phase 73 D-02 范本)往返断言"
      pattern: "PasswordCipher|Decrypt"
---

# Plan 4: knowledge + network + notification + auth 簇 5 文件 (Phase: 79-services-root-tail)

<objective>
交付 internal/services root 的最大 sqlite CRUD 面:knowledge_service.go(285 unc)+ network_device_service.go(202 unc)+ notification_config_service.go(127 unc)+ notification_sender_service.go(120 unc)+ auth_credential_service.go(142 unc),合计 ~876 unc。

Purpose: TAIL-01 第四段,单 plan 最大工作量。5 文件全部 (c) 类可测面 + 两个轻 test-double:AES-GCM 密码对(notification_config,纯函数 round-trip)与 addomain.PasswordCipher 接口 stub(auth_credential,Phase 73 D-02 portwrite 范本)。network_device 的 discovery/deviceInfoCollection 依赖以 nil 注入即可打开主要 CRUD 面(research §3)。
Output: 新增 4 个测试文件(零生产代码改动)。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/79-services-root-tail/79-RESEARCH.md
@.planning/workstreams/milestone/phases/79-services-root-tail/79-01-SUMMARY.md

<interfaces>
<!-- 关键契约(planner 已逐一读源码复核,2026-08-27)。executor 直接使用,无需重新探索。 -->

From internal/services/knowledge_service.go:
```go
func NewKnowledgeService(db *gorm.DB) *KnowledgeService // :23
GetArticleStatistics :40 / GetKnowledgeArticleList :148 / GetKnowledgeArticle :201 /
CreateKnowledgeArticle :220(含 tag 关联)/ UpdateKnowledgeArticle :293 / DeleteKnowledgeArticle :385 /
ConvertWorkOrderToArticle :417 / SearchKnowledgeArticles :463 / IncrementViewCount :515 /
IncrementLikeCount :525 / GetKnowledgeCategoryList :537 / loadKnowledgeChildrenCategories :569 /
GetKnowledgeCategory :592 / CreateKnowledgeCategory :609 / UpdateKnowledgeCategory :627 /
DeleteKnowledgeCategory :664 / GetAllTags :693 / GetTagByName :706 / CreateTag :722 /
GetOrCreateTag :738 / UpdateTag :757 / DeleteTag :767 / ParseTagsFromContent :784(包级可测纯函数)/
AutoCreateTagsFromContent :807
```

From internal/services/network_device_service.go:
```go
func NewNetworkDeviceService(db *gorm.DB, discoveryService *DeviceDiscoveryService, deviceInfoCollectionSvc *DeviceInfoCollectionService) *NetworkDeviceService // :21 — 两依赖可传 nil(CRUD 面不触达)
List :54 / loadAssociations :101 / Create :183 / QuickCreateDevice :239 / getLastIPOctet :357(包级纯函数)/
GetByID :368 / Update :399 / Delete :461 / BatchDelete :475 / UpdateStatus :485 / UpdateStatusBatch :494 /
GetDeviceStatistics :503 / GetDevicesByDept :576 / GetDevicesByCredential :585
// status 断言一律 models.DeviceStatus* 具名常量(Phase 69-03 判定 DeviceStatus 族)
```

From internal/services/notification_config_service.go:
```go
func NewNotificationConfigService(db *gorm.DB) *NotificationConfigService // :22
ListEmailConfigs :36 / GetEmailConfigByID :61 / GetDefaultEmailConfig :73 / CreateEmailConfig :85 /
UpdateEmailConfig :104 / DeleteEmailConfig :127 / ListAPINotificationConfigs :149 /
GetAPINotificationConfigByID :177 / CreateAPINotificationConfig :189 / UpdateAPINotificationConfig :204 /
DeleteAPINotificationConfig :229
func EncryptPassword(plainText, key string) (string, error) // :246 — AES-GCM,导出纯函数
func DecryptPassword(cipherText, key string) (string, error) // :277 — 错 key/坏密文 → error
```

From internal/services/notification_sender_service.go:
```go
func NewNotificationSenderService(db *gorm.DB) *NotificationSenderService // :21 — email/api sender 依赖为可 nil 的字段(Set* 注入,以 struct 定义为准)
SendNotification :30 / getNoticeForSending :68 / isValidPublishStatus :84(包级)/
getNotificationChannels :89 / sendNotificationByChannels :98 / sendEmailNotification :125 /
getEmailConfigID :153 / buildRecipientList :169 / sendSMSNotification :179 / sendAPINotification :201 /
sendWebNotification :222 / getUserInfo :235 / getUserEmails :262 / getUserPhones :273 /
PublishAndSendNotice :284 / SetNotificationChannels :295 / GetNotificationChannels :318 / uniqueStrings :327(包级)
```

From internal/services/auth_credential_service.go:
```go
func NewAuthCredentialService(db *gorm.DB, passwordCipher addomain.PasswordCipher) *AuthCredentialService // :20 — 接口注入,stub 之
GetStatistics :35 / List :66 / Create :118 / GetByID :178 / GetByIDWithPassword :193 /
Update :213 / Delete :280 / BatchDelete :298 / GetDefaultCredential :308 / SetDefaultCredential :322 /
GetDevicesByCredential :340 / validateCredentialConfig :350 / ValidateCredential :375 / GetDecryptedCredential :381
// addomain.PasswordCipher 为接口(位于 internal/services/addomain),stub 用 per-interface *Func 字段形态(Phase 73 D-02 portwrite 范本,无 testify/mock)
```

既有 analog(必读):
- internal/services/knowledge_statistics_test.go —— 既有知识库统计断言口径(勿重写)。
- STATE.md Phase 73-03 决策 —— 3 个已知 quirk(knowledge UUID inline-Delete / GetOrCreateTag 跨 tx 连接 / network GetByID 关联名丢失)已记录不修,本 plan 按现行为断言+注释,SUMMARY 复记。
- Phase 73-01 决策 —— mockXxx 用 per-interface *Func 字段 + interface-embedded-as-nil,无 testify/mock。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: knowledge 文章族 CRUD/搜索/统计/计数器</name>
  <files>internal/services/knowledge_service_79_04_test.go</files>
  <read_first>
  - internal/services/knowledge_service.go — :23-535(统计/List/Get/Create/Update/Delete/ConvertWorkOrder/Search/IncrementView/Like)
  - internal/models/ — KnowledgeArticle/KnowledgeCategory/KnowledgeTag/KnowledgeArticleTag model 与状态常量
  - internal/services/knowledge_statistics_test.go — 既有统计口径(勿重写)
  </read_first>
  <action>
  创建 internal/services/knowledge_service_79_04_test.go(package services)。helper `newKsv7904(t *testing.T) (*KnowledgeService, *gorm.DB)`:sqlite(t.TempDir 文件库)+ AutoMigrate(三族 model + 关联表 + ConvertWorkOrderToArticle 引用的工单 model;失败按 sqlite 缺表 family pattern 手动 CREATE TABLE)+ t.Cleanup Close。测试前缀 TestKsv7904_:

  - `TestKsv7904_CreateArticle_WithTags`:请求含 tag 名列表 → 文章行 + 标签行 + 关联行齐备;creatorID 落库。
  - `TestKsv7904_ArticleCRUD_RoundTrip`:Get/List(分页 + 状态/分类过滤)→ Update(读回)→ Delete;Get 不存在 → 错误。
  - `TestKsv7904_UpdateArticle_TagSync`:更新时替换 tag 集合 → 关联行差量正确(新增/移除)。
  - `TestKsv7904_SearchArticles`:关键字命中标题/内容两分支 + 分页;空关键字行为按实现断言。
  - `TestKsv7904_IncrementCounters`:IncrementViewCount/IncrementLikeCount 各 +1;不存在 ID 不报错(Update 语义)。
  - `TestKsv7904_ConvertWorkOrderToArticle`:预置工单行 → 转换后文章字段映射一致;工单不存在 → 错误。
  - `TestKsv7904_ArticleStatistics`:预置多状态文章 → KnowledgeArticleStatistics 各计数与手算一致(对照既有 knowledge_statistics_test.go 口径,只补 unc)。
  - `TestKsv7904_ParseTagsFromContent`:表驱动(无标记/单标记/多标记/重复/空串)→ 断言解析集(纯函数)。

  quirk 锁定:Delete 的 UUID inline 行为按现行为断言并注释(Phase 73-03 Q 记录),不修。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestKsv7904_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestKsv7904_" internal/services/knowledge_service_79_04_test.go` ≥ 8
  - 文章带 tag 创建的关联行断言存在
  - `go test -count=1 -run "TestKsv7904_" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-04): knowledge 文章族 CRUD/搜索/统计/计数器</commit>
  <done>knowledge 文章主链(9 方法)覆盖;tag 关联与计数器断言齐备。</done>
</task>

<task type="auto">
  <name>Task 2: knowledge 分类树 + 标签族(GetOrCreateTag quirk 锁定)</name>
  <files>internal/services/knowledge_service_79_04_test.go</files>
  <read_first>
  - internal/services/knowledge_service.go — :537-830(loadKnowledgeChildrenCategories 递归/分类 CRUD/标签 7 方法/AutoCreateTagsFromContent)
  - internal/services/knowledge_service_79_04_test.go — Task 1 helper(复用)
  </read_first>
  <action>
  同文件追加:

  - `TestKsv7904_CategoryTree_Recursive`:建父→子→孙三层 → GetKnowledgeCategoryList 返回树且 loadKnowledgeChildrenCategories 递归填充 Children;GetKnowledgeCategory 不存在 → 错误。
  - `TestKsv7904_CategoryCRUD`:Create/Update/Delete round-trip;删除含子类的分类行为按实现断言并注释(拒绝/级联,以源码为准)。
  - `TestKsv7904_TagFamily`:GetAllTags/GetTagByName(命中/未命中)/CreateTag(重复名分支按实现)/UpdateTag/DeleteTag 全链。
  - `TestKsv7904_GetOrCreateTag_BothPaths`:不存在 → 创建;已存在 → 返回既有行(不新建);quirk(跨 tx 连接)注释锁定不修(Phase 73-03 记录)。
  - `TestKsv7904_AutoCreateTagsFromContent`:内容含 N 个标记 → 自动建 N 标签并关联文章;重复调用幂等。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestKsv7904_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestKsv7904_" internal/services/knowledge_service_79_04_test.go` ≥ 13(Task 1+2 合计)
  - 三层分类递归断言存在;GetOrCreateTag 双路径断言存在
  - `go test -count=1 -run "TestKsv7904_" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-04): knowledge 分类树递归 + 标签族(GetOrCreateTag 双路径)</commit>
  <done>knowledge_service.go 24 个方法全覆盖,quirk 锁定注释齐备。</done>
</task>

<task type="auto">
  <name>Task 3: network_device CRUD + nil 依赖 + 统计 + getLastIPOctet</name>
  <files>internal/services/network_device_service_79_04_test.go</files>
  <read_first>
  - internal/services/network_device_service.go — :21-600 全文
  - internal/models/ — NetworkDevice model + DeviceStatus 族具名常量(Phase 69-03)
  - internal/services/discovery_statistics_test.go — 既有统计口径参考
  </read_first>
  <action>
  创建 internal/services/network_device_service_79_04_test.go(package services)。helper `newNdv7904(t) (*NetworkDeviceService, *gorm.DB)`:sqlite + AutoMigrate(NetworkDevice + loadAssociations preload 的关联 model:凭据/部门等,以 :101 实现为准)+ `NewNetworkDeviceService(db, nil, nil)`(nil 依赖注入 —— CRUD 面不触达,触达处即错误分支)。测试前缀 TestNdv7904_:

  - `TestNdv7904_CreateAndGet`:合法创建 → GetByID 读回;GetByID 关联字段按现行为断言并注释锁定(Phase 73-03 Q:关联名丢失 quirk,不修)。
  - `TestNdv7904_List_PaginationFilter`:预置多行 → 分页 + 名称/IP/状态过滤(按 req 字段)+ 排序白名单分支。
  - `TestNdv7904_Update_Delete_BatchDelete`:更新读回;单删/批删计数;不存在 ID 行为按实现。
  - `TestNdv7904_UpdateStatus_SingleAndBatch`:合法状态迁移(models.DeviceStatus* 具名常量)读回;非法值分支按实现断言。
  - `TestNdv7904_QuickCreateDevice`:最小请求 → 设备落库 + getLastIPOctet 派生逻辑断言(以 :239-356 实现为准);IP 非法分支。
  - `TestNdv7904_GetLastIPOctet`:表驱动(标准 IP/无点/空串)→ 断言返回段(包级纯函数)。
  - `TestNdv7904_GetDeviceStatistics`:预置多状态/多部门设备 → 统计 map 各键与手算一致。
  - `TestNdv7904_GetDevicesByDeptAndCredential`:按部门/凭据查询命中集;无关联 → 空集。
  - `TestNdv7904_NilDeps_ErrorBranch`:触达 discovery/deviceInfoCollection 依赖的方法(如存在)→ nil 注入下错误/panic-safe 行为按源码断言并注释(若方法内有 nil-guard 则断言 guard 生效)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestNdv7904_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestNdv7904_" internal/services/network_device_service_79_04_test.go` ≥ 9
  - status 断言引用 models.DeviceStatus*(grep 验证,禁裸 0/1)
  - getLastIPOctet 表驱动存在
  - `go test -count=1 -run "TestNdv7904_" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-04): network_device CRUD + nil 依赖注入 + 统计</commit>
  <done>network_device_service.go 全方法覆盖;nil-guard 与关联 quirk 锁定。</done>
</task>

<task type="auto">
  <name>Task 4: notification_config CRUD + EncryptPassword/DecryptPassword AES round-trip</name>
  <files>internal/services/notification_chain_79_04_test.go</files>
  <read_first>
  - internal/services/notification_config_service.go — 全文(重点 :246/:277 AES-GCM 对)
  - internal/models/ — EmailConfig/APINotificationConfig model + APIConfigType/状态常量
  </read_first>
  <action>
  创建 internal/services/notification_chain_79_04_test.go(package services)。helper `newNcf7904(t) (*NotificationConfigService, *gorm.DB)`(sqlite + AutoMigrate EmailConfig/APINotificationConfig)。测试前缀:

  - `TestNcf7904_EncryptDecrypt_RoundTrip`:EncryptPassword(plain,key) → 非 plain 且可 DecryptPassword 还原;不同明文不同密文(GCM 随机 nonce);空明文分支按实现。
  - `TestNcf7904_Decrypt_WrongKeyAndBadCipher`:错 key → error;坏密文(截断/非 base64 形态,以实现为准)→ error。
  - `TestNcf7904_EmailConfigCRUD`:Create/GetByID/List(分页+status 过滤,引用具名常量)/Update/Delete 全链;GetDefaultEmailConfig 命中默认/无默认两分支。
  - `TestNcf7904_EmailConfig_DefaultUniqueness`:第二个默认配置写入时旧默认被清或被拒(按 :85-126 实现断言,注释锁定语义)。
  - `TestNcf7904_APIConfigCRUD`:APINotificationConfig 六方法 + configType 过滤(models.APIConfigType 具名常量);Update 的加密字段(密码)更新/保留分支按实现断言。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestNcf7904_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestNcf7904_" internal/services/notification_chain_79_04_test.go` ≥ 5
  - AES round-trip + 错 key + 坏密文三断言齐备
  - 默认配置唯一性分支断言存在
  - `go test -count=1 -run "TestNcf7904_" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-04): notification_config CRUD + AES 密码对 round-trip</commit>
  <done>notification_config_service.go 12 方法 + 2 导出纯函数全覆盖。</done>
</task>

<task type="auto">
  <name>Task 5: notification_sender 派发链(nil-sender 错误分支 + 收件人解析)</name>
  <files>internal/services/notification_chain_79_04_test.go</files>
  <read_first>
  - internal/services/notification_sender_service.go — 全文(:21-340)
  - internal/services/notification_chain_79_04_test.go — Task 4 helper(可复用同一库或新 helper newNsn7904,按表需求定)
  - internal/models/ — Notice/NotificationChannel/User model + PublishStatus 常量
  </read_first>
  <action>
  同文件追加(helper `newNsn7904(t) (*NotificationSenderService, *gorm.DB)`,预置 Notice/User/EmailConfig/NotificationChannel 行)。测试前缀 TestNsn7904_:

  - `TestNsn7904_SendNotification_NoChannels`:无通道通知 → 不发送不报错(按 :89/:98 实现断言);notice 不存在/非发布态(isValidPublishStatus false)→ 错误分支。
  - `TestNsn7904_Send_EmailChannel_NilSender`:预置 email 通道但 EmailSender 依赖 nil → sendEmailNotification 错误分支被捕获、整体返回形态按实现断言(不 panic)。
  - `TestNsn7904_Send_ApiChannel_NilSender`:同上覆盖 sendAPINotification 分支。
  - `TestNsn7904_Send_SmsAndWebChannels`:sendSMSNotification(:179,按实现断言未实现/直发分支)+ sendWebNotification(:222,站内通知落库/标记分支)。
  - `TestNsn7904_BuildRecipientList`:customRecipients 优先/用户列表展开/两者合并/空入参(包级或私有直调)。
  - `TestNsn7904_GetUserInfo_Emails_Phones`:预置 3 用户(有 email 无 phone / 有 phone 无 email / 全无)→ getUserInfo map 形态 + getUserEmails/getUserPhones 过滤正确(空值剔除)。
  - `TestNsn7904_GetEmailConfigID`:通道指定 configID / 未指定走默认配置 / 默认缺失错误三分支(:153)。
  - `TestNsn7904_PublishAndSendNotice`:发布+发送复合链(发布态断言 models.PublishStatusPublished);重复调用幂等分支按实现。
  - `TestNsn7904_SetGetNotificationChannels`:Set 后 Get 读回一致(通道类型/配置 ID);空集合覆盖语义。
  - `TestNsn7904_UniqueStrings`:表驱动去重保序(包级 uniqueStrings)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestNsn7904_|TestNcf7904_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestNsn7904_" internal/services/notification_chain_79_04_test.go` ≥ 10
  - nil-sender 两个错误分支(email/api)各有用例且不 panic
  - `go test -count=1 ./internal/services/ -run "TestNsn7904_"` exit 0
  </acceptance_criteria>
  <commit>test(79-04): notification_sender 派发链 + nil-sender 分支 + 收件人解析</commit>
  <done>notification_sender_service.go 18 方法全覆盖;真发送路径留 79-05(email/api_sender 实测)。</done>
</task>

<task type="auto">
  <name>Task 6: auth_credential(PasswordCipher stub)+ plan 收口</name>
  <files>internal/services/auth_credential_service_79_04_test.go, .planning/workstreams/milestone/phases/79-services-root-tail/79-04-SUMMARY.md</files>
  <read_first>
  - internal/services/auth_credential_service.go — 全文(:20-400)
  - internal/services/addomain — PasswordCipher 接口定义(方法集以接口为准)
  - STATE.md Phase 73-01/73-03 决策 —— per-interface *Func 字段 stub 范本 + quirk 清单
  </read_first>
  <action>
  创建 internal/services/auth_credential_service_79_04_test.go(package services)。stub `stubCipher7904` 按 Phase 73 D-02 范本实现 addomain.PasswordCipher(per-interface *Func 字段 + 未注册即 panic),helper `newAcv7904(t) (*AuthCredentialService, *gorm.DB, *stubCipher7904)`。测试前缀 TestAcv7904_:

  - `TestAcv7904_CreateValidate`:Create 合法请求 → 密码字段经 cipher.Encrypt 落库(stub 断言入参);validateCredentialConfig 协议/用户名/密码校验分支表驱动(缺用户名/缺密码/新凭据强制密码等,按 :350-374 实现)。
  - `TestAcv7904_ListPagination`:预置多行 → 分页/过滤(protocol/status,具名常量)。
  - `TestAcv7904_GetByIDWithPassword`:走 cipher 解密路径(stub 返回固定明文)→ 字段一致;不存在 → 错误。
  - `TestAcv7904_GetDecryptedCredential_RoundTrip`:Create(stub 加密)→ GetDecryptedCredential(stub 解密)→ 明文一致;cipher 返回 error → 错误上抛分支。
  - `TestAcv7904_Update_Delete_BatchDelete`:更新读回;单删/批删;不存在 ID 行为按实现。
  - `TestAcv7904_DefaultCredential_Uniqueness`:SetDefaultCredential 后旧默认被清 → GetDefaultCredential 唯一;Set 不存在 ID → 错误。
  - `TestAcv7904_GetStatistics`:预置多协议/状态凭据 → CredentialStatistics 计数与手算一致。
  - `TestAcv7904_GetDevicesByCredential`:预置关联设备 → 命中集;无关联 → 空集。

  收口段(同 task):
  1. `go test -count=1 -coverprofile=cov79_04.out ./internal/services/`
     `go tool cover -func=cov79_04.out | grep -E "knowledge_service.go|network_device_service.go|notification_config_service.go|notification_sender_service.go|auth_credential_service.go"`
  2. 判定 5 文件 ≥70%,未达标回补。
  3. `go test -race -count=1 -run "TestKsv7904_|TestNdv7904_|TestNcf7904_|TestNsn7904_|TestAcv7904_" ./internal/services/` + `go build ./...` + `go test ./...`。
  SUMMARY 落:5 文件实测覆盖率表、quirk 复记(knowledge×2/network×1)、stub 范式说明、滚动增量。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestAcv7904_" ./internal/services/ && go test -count=1 -coverprofile=cov79_04.out ./internal/services/ && go tool cover -func=cov79_04.out | grep -E "knowledge_service.go|network_device_service.go|notification_|auth_credential_service.go" && go test -race -count=1 -run "TestAcv7904_|TestKsv7904_" ./internal/services/ && go build ./... && go test ./... 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestAcv7904_" internal/services/auth_credential_service_79_04_test.go` ≥ 8
  - 5 文件 per-file ≥70%(数字落 SUMMARY)
  - stub 未注册方法被调时 panic(stub 契约)+ -race 抽样 exit 0
  - `go test ./...` exit 0;`go build ./...` exit 0;生产 .go 改动 = 0
  </acceptance_criteria>
  <commit>test(79-04): auth_credential PasswordCipher stub 全链 + 簇收口 SUMMARY</commit>
  <done>最大 sqlite CRUD 面(5 文件 876 unc)收口 ≥70%,stub 范式与 quirk 复记落 SUMMARY。</done>
</task>

</tasks>

<notes>
- 依赖:79-01 装配纪律;79-05 依赖本 plan 无(独立簇),但 wave 串行使然。
- 已知 quirk(锁定不修,Phase 73-03 已记录,本 plan 复记 SUMMARY):knowledge UUID inline-Delete、knowledge GetOrCreateTag 跨 tx 连接、network GetByID 关联名丢失。断言按现行为写,勿"顺手修"。
- 真实发送路径不在本 plan:email_sender/api_sender 的 wire 级测试归 79-05(D-79-03/httpmock);本 plan 只测派发与 nil 分支,边界清晰不重叠。
- race 安全:stubCipher 的 *Func 字段每用例独立赋值,不跨用例共享;禁 t.Parallel。
- AutoMigrate 失败的 model 按 sqlite 缺表 family pattern 手动 CREATE TABLE(记忆 xingran-db-migration-and-pk-gotcha:BaseModel.ID 需显式主键)。
</notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 ↔ 数据库 | glebarez sqlite(t.TempDir 文件库),不触碰生产 PG |
| 测试 ↔ 加密 | AES-GCM 用测试内 32 字节字面量 key;PasswordCipher 为 stub,无真实 AD/LDAP 连接 |
| 测试 ↔ 生产代码 | 仅新增 *_79_04_test.go;生产 .go 改动 = 0(D-79 escape hatch 除外) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-04-01 | Tampering | 测试误连真实 PG/AD | mitigate | 仅 sqlite + 接口 stub;grep 无 ldap:// / postgres DSN 字面量 |
| T-79-04-02 | Information Disclosure | 凭据明文/密文测试值 | accept | 全部为测试字面量,无真实凭据入仓 |
| T-79-04-03 | Repudiation | 加密 key 字面量被误当生产密钥复用 | mitigate | key 用显式 32 字节测试常量并注释"仅测试" |
| T-79-04-SC | Tampering | 依赖安装 | accept | 零新增依赖 |
</threat_model>

<verification>
- `go test -count=1 ./internal/services/` 全绿;新增用例 `-race` 抽样全绿
- `go tool cover -func=cov79_04.out`:5 文件全部 ≥70%
- `go build ./...` + `go test ./...` exit 0
- 生产 .go 改动 = 0 文件;既有 knowledge_statistics_test.go 不回归
</verification>

<success_criteria>
1. 四个测试文件存在且全绿:TestKsv7904_ ≥13、TestNdv7904_ ≥9、TestNcf7904_ ≥5、TestNsn7904_ ≥10、TestAcv7904_ ≥8
2. knowledge_service.go ≥70%(基线 1.4%)、network_device_service.go ≥70%(基线 0%)
3. notification_config/notification_sender/auth_credential 三文件 ≥70%(基线 0%)
4. AES round-trip + 错 key/坏密文、nil-sender 双分支、PasswordCipher stub 解密 round-trip 齐备
5. `go build ./...` + `go test ./...` exit 0;每 task 一原子 commit
</success_criteria>

<decision_audit>
本 plan 涉及的 phase-79 决策定案(executor 必须遵守):

| ID | 决策 | 依据 / 理由 |
|----|------|------------|
| D-79-01 | 本 plan 簇 = research §7 的 79-04 行(knowledge+network+notification+auth,876 unc 最大 sqlite 面) | ROADMAP 79-04 失实标题作废,按 research 重切;profile 倒序意图在簇级保持 |
| D-79-06 | 命名 `<source-or-topic>_79_04_test.go` | DQ6 confirmed |
| (沿用) | PasswordCipher 用 Phase 73 D-02 portwrite 范本(per-interface *Func 字段),不引入 testify/mock | STATE.md 已锁范式 |
| (沿用) | 3 个已知 quirk 锁定不修(Phase 73-03 记录) | R7 + ROADMAP L247 + 记忆 local-vs-ci-test-divergence |
| (沿用) | 装配/helper/串行 wave 纪律沿用 79-01 | 已验证 |

基线数字来源:79-RESEARCH.md §2(2026-08-27 实测)。
</decision_audit>

<output>
Create `.planning/workstreams/milestone/phases/79-services-root-tail/79-04-SUMMARY.md` when done
</output>
