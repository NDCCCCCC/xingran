---
phase: 79-services-root-tail
plan: 02
status: pending
type: execute
wave: 2
depends_on: ["79-01"]
files_modified:
  - internal/services/duty_schedule_service_79_02_test.go
  - internal/services/duty_pool_service_79_02_test.go
  - internal/services/duty_family_tail_79_02_test.go
autonomous: true
requirements: [TAIL-01]

must_haves:
  truths:
    - "duty_schedule_service.go(174 stmts @0%)≥70%:GenerateSchedule 全分支(池不存在/空成员/日期非法/节假日查询失败/ClearExists/类型不匹配 continue/轮询 DailyCount/批量插入)+ getDutyType 三态(节假日 IsOffday/周末/工作日)+ isWeekend 两分支 + 6 个 CRUD 方法各有用例"
    - "duty_pool_service.go(97 stmts @4.9%)≥70%:CreateDutyPool(成员序列化+重名/校验分支)+ GetDutyPoolList/GetDutyPoolByID/UpdateDutyPool/DeleteDutyPool/GetDutyPoolStatistics"
    - "duty_stats_service.go(32)/duty_holiday_service.go(29)/duty_config_service.go(24)/duty_service.go(24)四小文件 0→≥70%:GetMyDutyStats、Holiday 六方法、DutyConfig 两方法、DutyService 门面 13 个委托方法(抽主干断言委托链通,不全量重复下层断言)"
    - "按 D-79-01 同源口径:duty 家族 6 文件全部脱离 <50% 区(本 phase 前全部为 0% 或 <5%)"
    - "按 D-79-06 命名:`<source-or-topic>_79_02_test.go`;helper 带 7902 后缀(R5);sqlite t.TempDir 文件库(79-01 沉淀纪律);禁 t.Parallel"
  artifacts:
    - path: "internal/services/duty_schedule_service_79_02_test.go"
      provides: "DutyScheduleService 全方法测试(前缀 TestDsc7902_),GenerateSchedule 分支表 + getDutyType/isWeekend 纯函数表"
      min_lines: 420
      contains: "GenerateSchedule"
    - path: "internal/services/duty_pool_service_79_02_test.go"
      provides: "DutyPoolService 全方法测试(前缀 TestDpl7902_)"
      min_lines: 220
      contains: "CreateDutyPool"
    - path: "internal/services/duty_family_tail_79_02_test.go"
      provides: "duty_stats/duty_holiday/duty_config/duty_service 四文件测试(前缀 TestDst7902_/TestDhd7902_/TestDcf7902_/TestDsv7902_)"
      min_lines: 260
      contains: "GetMyDutyStats"
  key_links:
    - from: "internal/services/duty_schedule_service_79_02_test.go"
      to: "GenerateSchedule → NewDutyPoolService(s.db).GetDutyPoolByID(duty_schedule_service.go:28-29)"
      via: "同一 sqlite 库预置 sys_duty_pool 行(含 Members),断言池缺失/空成员两错误分支"
      pattern: "GetDutyPoolByID|值班池没有成员"
    - from: "internal/services/duty_schedule_service_79_02_test.go"
      to: "getDutyType 的 holidayMap 分支(duty_schedule_service.go:123-139)"
      via: "sys_holiday 预置 IsOffday 行,断言 ScheduleModeHoliday/Weekend/Weekday 三态"
      pattern: "ScheduleModeHoliday|IsOffday"
    - from: "internal/services/duty_family_tail_79_02_test.go"
      to: "DutyService 门面委托(duty_service.go:125-199)"
      via: "门面方法断言与直调底层 service 结果一致(委托链连通)"
      pattern: "NewDutyService"
---

# Plan 2: duty 家族 6 文件 0→≥70% (Phase: 79-services-root-tail)

<objective>
交付 internal/services root 的 duty 家族测试:duty_schedule_service.go(174 unc)+ duty_pool_service.go(97 unc)+ duty_stats_service.go(32 unc)+ duty_holiday_service.go(29 unc)+ duty_config_service.go(24 unc)+ duty_service.go(24 unc),合计 ~380 unc。

Purpose: TAIL-01 第二段。duty 家族是纯 sqlite 可测面(research §3 (c) 类,零 test-double 阻塞),6 文件全部从 0%/<5% 起步,是"无单文件留在 <50%"(SC-2)的最大单簇清欠。GenerateSchedule 是本包少见的算法型函数(轮询 + 节假日映射 + 事务),必须分支表化。
Output: 新增 3 个测试文件(零生产代码改动)。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/79-services-root-tail/79-RESEARCH.md
@.planning/workstreams/milestone/phases/79-services-root-tail/79-01-SUMMARY.md

<interfaces>
<!-- 关键契约(planner 已逐一读源码复核,2026-08-27)。executor 直接使用,无需重新探索。 -->

From internal/services/duty_schedule_service.go:
```go
func NewDutyScheduleService(db *gorm.DB) *DutyScheduleService // :19
func (s *DutyScheduleService) GenerateSchedule(ctx, req *GenerateScheduleRequest, creatorID) (int, error) // :26
//   :29 池不存在 → err;:34 len(pool.Members)==0 → "值班池没有成员"
//   :39/:43 日期解析失败 → "开始/结束日期格式错误";:52 节假日查询失败 → "获取节假日失败"
//   :67 req.ClearExists → 事务内先删旧排班;:85 actualDutyType != req.DutyType → continue
//   :90 for i < pool.DailyCount 轮询 memberIndex%memberCount;:109 批量 Create;空 schedules 不插入
func (s *DutyScheduleService) getDutyType(date, holidayMap) string // :123 — IsOffday→ScheduleModeHoliday;isWeekend→ScheduleModeWeekend;否则 ScheduleModeWeekday
func isWeekend(date time.Time) bool // :142 — Sat/Sun
// :148 dutyScheduleAllowedSortFields 白名单(scheduleDate/dutyType/status → 列名)
func (s *DutyScheduleService) GetDutyScheduleList(ctx, req *DutyScheduleListRequest) ([]models.DutySchedule, int64, error) // :155 — 分页/过滤/白名单排序
func (s *DutyScheduleService) GetTodayDuty(ctx) ([]TodayDutyMember, error)       // :225
func (s *DutyScheduleService) GetMonthlyDutySchedule(ctx, year, month) (map[string][]TodayDutyMember, error) // :278
func (s *DutyScheduleService) SwapDuty(ctx, req *SwapDutyRequest, operatorID) error      // :333
func (s *DutyScheduleService) ManualDuty(ctx, req *ManualDutyRequest, creatorID) error   // :388
func (s *DutyScheduleService) DeleteDutySchedule(ctx, scheduleID) error                  // :422
func (s *DutyScheduleService) BatchDeleteDutySchedules(ctx, ids []string) error          // :430
```

From internal/services/duty_pool_service.go:
```go
func NewDutyPoolService(db *gorm.DB) *DutyPoolService  // :18
func (s *DutyPoolService) GetDutyPoolStatistics(ctx) (*DutyPoolStatistics, error) // :33
func (s *DutyPoolService) CreateDutyPool(ctx, req *DutyPoolCreateRequest, creatorID) (*models.DutyPool, error) // :59
func (s *DutyPoolService) GetDutyPoolList(ctx, req *DutyPoolListRequest) ([]models.DutyPool, int64, error) // :147
func (s *DutyPoolService) GetDutyPoolByID(ctx, poolID) (*models.DutyPool, error)  // :193
func (s *DutyPoolService) UpdateDutyPool(ctx, req *DutyPoolUpdateRequest, updaterID) error // :206
func (s *DutyPoolService) DeleteDutyPool(ctx, poolID) error // :265
```

From internal/services/duty_stats_service.go / duty_holiday_service.go / duty_config_service.go:
```go
func NewDutyStatsService(db) *DutyStatsService;  GetMyDutyStats(ctx, userID) (*MyDutyStats, error) // stats:23
func NewDutyHolidayService(db) *DutyHolidayService // holiday:19
//   CreateHoliday :24 / GetHolidayList :33 / UpdateHoliday :50 / DeleteHoliday :59 / GetHolidayYears :67 / BatchCreateHolidays :84
func NewDutyConfigService(db) *DutyConfigService // config:17 — GetDutyConfig :22(空表回默认)/ UpdateDutyConfig :41
```

From internal/services/duty_service.go(门面,内嵌两个子 service):
```go
func NewDutyService(db *gorm.DB) *DutyService // :23
// :125-199 13 个委托方法(CreateDutyPool/GetDutyPoolList/GetDutyPoolStatistics/GetDutyPoolByID/UpdateDutyPool/
//   DeleteDutyPool/GenerateSchedule/GetDutyScheduleList/GetTodayDuty/GetMonthlyDutySchedule/SwapDuty/ManualDuty/
//   DeleteDutySchedule/BatchDeleteDutySchedules)—— 全部单行委托,与直调子 service 等价
// :37-124 请求/响应 struct 定义(GenerateScheduleRequest: PoolID/StartDate/EndDate/DutyType/DailyCount/ClearExists)
```

From internal/models(断言禁裸值,一律具名常量):
```go
models.ScheduleMode{Holiday,Weekend,Weekday} / models.DutyStatusNormal / models.Holiday{HolidayDate,IsOffday}
models.DutyPool{Members(序列化字段,以 model 实际类型为准),DailyCount} / models.DutySchedule{BaseModel,PoolID,UserID,ScheduleDate,DutyType,Status,IsManual}
```

既有 analog(必读):
- internal/services/duty_pool_statistics_test.go / discovery_statistics_test.go —— 既有 duty 统计断言口径(勿重写)。
- internal/services/mac_history_query_service_test.go:69 —— sqlite fixture 形态(本 plan 用 t.TempDir 文件库版,79-01 纪律)。
- Phase 73-03 记忆(STATE.md):duty handler 测试曾踩 testify m.Called 未注册 panic + service 触发 invalidation —— 本 plan 直测 service 层(无 mock),无此风险,但 invalidation/cache 副作用分支如遇到按 quirk 纪律锁定。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GenerateSchedule 分支表 + getDutyType/isWeekend 纯函数</name>
  <files>internal/services/duty_schedule_service_79_02_test.go</files>
  <read_first>
  - internal/services/duty_schedule_service.go — :26-145(GenerateSchedule 全文 + getDutyType/isWeekend)
  - internal/services/duty_service.go — :77-101(GenerateScheduleRequest/SwapDutyRequest 等字段定义)
  - internal/models/ — DutySchedule/DutyPool/Holiday model 定义(Members 字段类型与 TableName)
  - internal/services/duty_pool_service_test.go(若存在既有 fixture,参考其建表方式;helper 另起 7902 后缀)
  </read_first>
  <action>
  创建 internal/services/duty_schedule_service_79_02_test.go(package services,同包白盒)。helper `newDsc7902(t *testing.T) (*DutyScheduleService, *gorm.DB)`:sqlite(filepath.Join(t.TempDir(),"duty79.db"))+ AutoMigrate(DutySchedule/DutyPool/Holiday 及 GenerateSchedule 链路实际引用的 model;若 AutoMigrate 对某 model 失败,按 XingRAN sqlite 缺表 family pattern 手动 CREATE TABLE,列集以 model 字段为准)+ `NewDutyScheduleService(db)` + t.Cleanup Close。再一个 `seedPool7902(t, db, members int, dailyCount int) string` 预置值班池行并返回 poolID(Members 序列化写法以 model 实际类型为准)。

  测试函数前缀 TestDsc7902_:

  - `TestDsc7902_GenerateSchedule_PoolMissing`:不存在的 PoolID → 错误(返回 0)。
  - `TestDsc7902_GenerateSchedule_EmptyMembers`:池存在但 Members 空 → 错误含"值班池没有成员"。
  - `TestDsc7902_GenerateSchedule_BadDate`:StartDate="2026/01/01" 与 EndDate 非法各一 → 错误含"日期格式错误"。
  - `TestDsc7902_GenerateSchedule_DutyTypeFilter`:预置池(3 成员,DailyCount=2)+ 请求跨 7 天(必含周末)→ DutyType=ScheduleModeWeekday → 断言返回数 == 工作日天数×2 且 DB 行 DutyType 全为该模式(周末被 :85 continue 跳过);再以 ScheduleModeWeekend 重复 → 只排周末。
  - `TestDsc7902_GenerateSchedule_RoundRobin`:跨 6 个工作日 × DailyCount=2 × 3 成员 → 断言每成员被排次数相等(轮询语义),顺序按 memberIndex 递增。
  - `TestDsc7902_GenerateSchedule_HolidayMap`:预置区间内一条 `models.Holiday{IsOffday:true}` → DutyType=weekday 请求时该日不排;DutyType=holiday 请求时只排该日(两向断言)。
  - `TestDsc7902_GenerateSchedule_ClearExists`:先跑一次生成 → 再跑 ClearExists=true → 旧记录被清、总数正确;ClearExists=false → 累计翻倍。
  - `TestDsc7902_GenerateSchedule_EmptyResult`:区间内无匹配类型 → 返回 0 且不报错(:109 len==0 分支)。
  - `TestDsc7902_GetDutyType_Table`:表驱动 getDutyType(holidayMap 含 IsOffday true / false(节假日但非休)/ 不含 × 周末/工作日)→ 三态字符串断言,引用 models.ScheduleMode* 常量。
  - `TestDsc7902_IsWeekend`:表驱动周六/周日 true,周一..周五 false(同包直调 isWeekend)。

  纪律:日期断言用 time.Date 构造(禁依赖"今天"的相对断言,防跨日 flake);状态断言引用 models.DutyStatusNormal。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestDsc7902_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestDsc7902_" internal/services/duty_schedule_service_79_02_test.go` ≥ 10
  - GenerateSchedule 的 8 个分支(池缺失/空成员/两个日期解析/节假日查询/ClearExists/类型过滤/轮询)各有独立用例或分支断言
  - 文件中不出现 `time.Now()` 参与日期期望值计算(防跨日 flake)
  - `go test -count=1 -run "TestDsc7902_" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-02): GenerateSchedule 分支表 + getDutyType/isWeekend 纯函数</commit>
  <done>算法型主函数全分支 + 轮询语义 + 节假日映射双向断言;纯函数表驱动收口。</done>
</task>

<task type="auto">
  <name>Task 2: duty_schedule CRUD 族(List/Today/Monthly/Swap/Manual/Delete)</name>
  <files>internal/services/duty_schedule_service_79_02_test.go</files>
  <read_first>
  - internal/services/duty_schedule_service.go — :147-440(GetDutyScheduleList 白名单排序 + GetTodayDuty + GetMonthlyDutySchedule + SwapDuty + ManualDuty + 两 Delete)
  - internal/services/duty_service.go — :65-101(DutyScheduleListRequest/SwapDutyRequest/ManualDutyRequest 字段)
  - internal/services/duty_schedule_service_79_02_test.go — Task 1 的 newDsc7902/seedPool7902 helper(本任务复用)
  </read_first>
  <action>
  同文件追加:

  - `TestDsc7902_GetList_PaginationAndFilter`:预置 5 行 → 分页(current/pageSize 语义以 req 字段为准)+ 按 poolID/日期范围过滤 + title 外字段过滤各一;orderByColumn="scheduleDate" 走白名单(:148),非法列名走回退分支(非白名单 OrderByColumn 的行为按源码断言并落注释,Phase 73-04 Q5 同款 quirk 纪律:只锁不修)。
  - `TestDsc7902_GetTodayDuty`:预置今天(用 time.Now 当天日期构造行,但断言只比对返回的成员/池字段,不断言日期值本身)与明天各 1 行 → 只返回今天;空库 → 空切片非 nil(以实现为准)。
  - `TestDsc7902_GetMonthlyDutySchedule`:预置同月多日 + 跨月行 → map 键为 "YYYY-MM-DD" 形态、只含当月、每日成员分组正确;无效月份(0/13)分支按实现断言。
  - `TestDsc7902_SwapDuty`:预置两行(A 日/B 日)→ 交换后双方 ScheduleDate/UserID 对调;同 ID/不存在的 scheduleID/跨池交换(若实现禁止)分支按源码断言。
  - `TestDsc7902_ManualDuty`:合法请求 → 落库行 IsManual=true(具名断言,禁裸 true/false 之外的字面量)、CreatedBy=creatorID;池不存在分支 → 错误。
  - `TestDsc7902_DeleteAndBatchDelete`:单删后行数-1;批删后只剩未列入 IDs 的行;删除不存在的 ID 不报错。

  排序/过滤 quirk 一律"锁定+注释"记 SUMMARY,不改生产代码(R7)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestDsc7902_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestDsc7902_" internal/services/duty_schedule_service_79_02_test.go` ≥ 16(Task 1+2 合计)
  - SwapDuty 的对调断言覆盖双方字段
  - `go test -count=1 ./internal/services/ -run "TestDsc7902_"` exit 0
  </acceptance_criteria>
  <commit>test(79-02): duty_schedule CRUD 族(分页/白名单排序/换班/手工排班)</commit>
  <done>duty_schedule_service.go 全部 10 个导出方法有用例,quirk 锁定记录落 SUMMARY。</done>
</task>

<task type="auto">
  <name>Task 3: duty_pool 全 CRUD + statistics</name>
  <files>internal/services/duty_pool_service_79_02_test.go</files>
  <read_first>
  - internal/services/duty_pool_service.go — :13-300 全文(CreateDutyPool 的校验/重名分支 + Members 序列化 + Statistics 聚合)
  - internal/services/duty_pool_statistics_test.go — 既有统计断言口径(勿重写)
  - internal/services/duty_service.go — :45-64(DutyPoolCreateRequest/UpdateRequest 字段)
  </read_first>
  <action>
  创建 internal/services/duty_pool_service_79_02_test.go(package services)。helper `newDpl7902(t) (*DutyPoolService, *gorm.DB)`(同 Task 1 装配纪律)。测试函数前缀 TestDpl7902_:

  - `TestDpl7902_Create_Success`:合法请求 → 返回 *models.DutyPool,Members 序列化落库(读回后成员数一致)、CreatedBy=creatorID。
  - `TestDpl7902_Create_ValidationBranches`:对照 :59-146 实现补校验分支(必填缺失/成员为空/重名等,以源码错误文案为准,逐分支一用例)。
  - `TestDpl7902_GetList_Pagination`:预置 3 行 → 分页 + 关键字过滤分支。
  - `TestDpl7902_GetByID_MissAndHit`:存在/不存在两分支。
  - `TestDpl7902_Update_FullAndMissing`:合法更新(名称+成员+DailyCount)→ 读回一致;不存在 ID → 错误;校验失败分支一例。
  - `TestDpl7902_Delete`:删除后 GetByID miss;再删 → 不报错或错误按实现断言。
  - `TestDpl7902_Statistics`:预置多池(不同成员数/状态)→ DutyPoolStatistics 各聚合字段与手算一致(对照既有 duty_pool_statistics_test.go 的口径,只补未覆盖分支)。

  Members 序列化字段读写必须 round-trip 断言(写→读→成员列表相等),防 JSON 序列化形态回归。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestDpl7902_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestDpl7902_" internal/services/duty_pool_service_79_02_test.go` ≥ 7
  - Members round-trip 断言存在
  - 既有 TestDutyPoolStatistics*(若有)不回归
  - `go test -count=1 -run "TestDpl7902_" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-02): duty_pool 全 CRUD + Members round-trip + statistics</commit>
  <done>duty_pool_service.go 6 个方法全覆盖,统计聚合与既有口径一致。</done>
</task>

<task type="auto">
  <name>Task 4: duty_stats + duty_holiday + duty_config 三小文件</name>
  <files>internal/services/duty_family_tail_79_02_test.go</files>
  <read_first>
  - internal/services/duty_stats_service.go — 全文(GetMyDutyStats 的聚合 SQL 分支)
  - internal/services/duty_holiday_service.go — 全文(6 方法 + GetHolidayYears 的 DISTINCT/去重分支)
  - internal/services/duty_config_service.go — 全文(GetDutyConfig 空表回默认分支)
  - internal/models/ — Holiday/DutyConfig/MyDutyStats 相关字段
  </read_first>
  <action>
  创建 internal/services/duty_family_tail_79_02_test.go(package services)。helper `newDtx7902(t) (*gorm.DB, *DutyStatsService, *DutyHolidayService, *DutyConfigService)`(一个库三个 service,t.Cleanup Close)。测试前缀:

  - `TestDst7902_GetMyDutyStats`:预置该用户 N 条排班(不同 DutyType/日期)→ 各统计字段与手算一致;无记录用户 → 零值结构非 nil(按实现断言)。
  - `TestDhd7902_HolidayCRUD`:CreateHoliday → GetHolidayList(按年过滤)→ UpdateHoliday → DeleteHoliday;GetHolidayYears 返回去重升序年份列表(预置跨两年数据断言);BatchCreateHolidays 空切片/含重复分支按实现断言。
  - `TestDhd7902_Holiday_DateShape`:HolidayDate 只存日期(时归零)或按实现断言,防时区/时分污染(GetHolidayList 年过滤依赖此形态,一旦漂移即 flake —— 用注释锁定)。
  - `TestDcf7902_GetConfig_EmptyBackfillsDefault`:空表 → GetDutyConfig 返回默认值结构(按 :22-40 实现断言默认字段)。
  - `TestDcf7902_UpdateConfig_RoundTrip`:Update 后 Get 读回一致;再 Update 走更新分支(非新建)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestDst7902_|TestDhd7902_|TestDcf7902_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestDst7902_\|^func TestDhd7902_\|^func TestDcf7902_" internal/services/duty_family_tail_79_02_test.go` ≥ 5
  - GetHolidayYears 去重升序断言存在
  - GetDutyConfig 空表默认分支断言存在
  - `go test -count=1 -run "TestDst7902_|TestDhd7902_|TestDcf7902_" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-02): duty_stats/holiday/config 三小文件 0→70%</commit>
  <done>三文件全方法覆盖;GetMyDutyStats 聚合、Holiday 年份去重、Config 空表回默认各有用例。</done>
</task>

<task type="auto">
  <name>Task 5: duty_service 门面委托链</name>
  <files>internal/services/duty_family_tail_79_02_test.go</files>
  <read_first>
  - internal/services/duty_service.go — :13-200(struct 定义 + NewDutyService + 13 个委托方法)
  - internal/services/duty_family_tail_79_02_test.go — Task 4 helper(复用同一库)
  </read_first>
  <action>
  同文件追加 `TestDsv7902_FacadeDelegation`:用 NewDutyService(db) 逐个调 13 个委托方法,每方法与"直调对应子 service"结果一致性断言 —— 采用轻量口径:抽 4 个代表方法做完整 round-trip(CreateDutyPool → GetDutyPoolByID → UpdateDutyPool → DeleteDutyPool)+ 3 个代表排班方法(GenerateSchedule → GetDutyScheduleList → BatchDeleteDutySchedules),其余 6 个方法以"调用不 panic + 返回形态正确(错误分支传坏参数)"覆盖(单行委托,重复全量断言无价值)。

  该任务同时兜住 duty_service.go 自身 24 stmts 中"struct 定义/构造器"的编译级覆盖。禁为门面重复下层全量分支(下层已测)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestDsv7902_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestDsv7902_" internal/services/duty_family_tail_79_02_test.go` ≥ 1
  - 13 个委托方法中 ≥10 个在本 plan 任一测试中被调用(grep 方法名计数)
  - `go test -count=1 ./internal/services/ -run "TestDsv7902_"` exit 0
  </acceptance_criteria>
  <commit>test(79-02): duty_service 门面 13 委托方法连通性</commit>
  <done>duty_service.go 门面委托链全通,round-trip 与错误分支双口径。</done>
</task>

<task type="auto">
  <name>Task 6: plan 收口 — duty 家族 per-file 覆盖率 + -race + SUMMARY</name>
  <files>.planning/workstreams/milestone/phases/79-services-root-tail/79-02-SUMMARY.md</files>
  <read_first>
  - .planning/workstreams/milestone/phases/79-services-root-tail/79-RESEARCH.md §2 — duty 家族 6 文件基线(0%/<5%)
  </read_first>
  <action>
  收口三步:

  1. `go test -count=1 -coverprofile=cov79_02.out ./internal/services/`
     `go tool cover -func=cov79_02.out | grep -E "duty_schedule_service.go|duty_pool_service.go|duty_stats_service.go|duty_holiday_service.go|duty_config_service.go|duty_service.go"`
  2. 判定:6 文件全部 ≥70%;未达标回对应 task 补 unc 分支(只增不改)。
  3. `go test -race -count=1 -run "TestDsc7902_|TestDpl7902_|TestDst7902_|TestDhd7902_|TestDcf7902_|TestDsv7902_" ./internal/services/` + `go build ./...` + `go test ./...`。

  SUMMARY 落:6 文件实测覆盖率表(基线→实测)、quirk 锁定清单(排序回退/日期形态等)、duty 家族累计贡献的 covered stmts 增量(对照 79-01 SUMMARY 的包总数,滚动累计)。
  </action>
  <verify>
    <automated>go test -count=1 -coverprofile=cov79_02.out ./internal/services/ && go tool cover -func=cov79_02.out | grep -E "duty_" && go test -race -count=1 -run "TestDsc7902_|TestDpl7902_" ./internal/services/ && go build ./... && go test ./... 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
  - duty 6 文件 per-file ≥70%(数字落 SUMMARY)
  - `go test ./...` exit 0;`go build ./...` exit 0;-race 抽样 exit 0
  - 生产 .go 改动 = 0 文件
  </acceptance_criteria>
  <commit>docs(79-02): duty 家族 6 文件覆盖率收口落 SUMMARY</commit>
  <done>duty 家族 6 文件全部 ≥70%,SC-2 的 duty 段清欠完成。</done>
</task>

</tasks>

<notes>
- 依赖:79-01 的装配纪律(sqlite t.TempDir 文件库、helper `newXxx79NN`、禁 t.Parallel、miniredis/MemoryCache 清理);本 plan 无需 miniredis(纯 sqlite 面除外无 cache 依赖)。
- GenerateSchedule 是事务函数:错误分支断言后必须验证 DB 无半写(事务回滚),可选加一条"回滚后行数不变"断言。
- race 安全:纯 sqlite 单连接,无 goroutine;`-race` 抽样在收口 task。
- flake 防线:日期期望值一律 time.Date 显式构造;GetTodayDuty 用当天日期但不断言日期值(注释说明跨日安全)。
- 同包串行:本 plan wave 2,依赖 79-01 完成后执行(整包编译互踩规避,D-78-11 同构)。
- 既有测试不回归:duty_pool_statistics_test.go 等既有文件不得改名/重写。
</notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 ↔ 数据库 | glebarez sqlite(t.TempDir 文件库),不触碰生产 PG |
| 测试 ↔ 生产代码 | 仅新增 *_79_02_test.go;生产 .go 改动 = 0(D-79 escape hatch 除外) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-02-01 | Tampering | 测试误连真实 PG | mitigate | 仅 sqlite DSN;grep 无 postgres 字面量 |
| T-79-02-02 | DoS | 大区间 GenerateSchedule 拖慢测试 | mitigate | 区间 ≤ 31 天,成员 ≤ 5,单用例毫秒级 |
| T-79-02-03 | Tampering | 日期相关断言跨日 flake | mitigate | time.Date 显式构造期望值;今天类断言只比对非日期字段 |
| T-79-02-SC | Tampering | 依赖安装 | accept | 零新增依赖 |
</threat_model>

<verification>
- `go test -count=1 ./internal/services/` 全绿;新增用例 `-race` 抽样全绿
- `go tool cover -func=cov79_02.out`:duty 6 文件全部 ≥70%
- `go build ./...` + `go test ./...` exit 0
- 生产 .go 改动 = 0 文件;既有 duty 统计测试不回归
</verification>

<success_criteria>
1. 三个测试文件存在且全绿:TestDsc7902_ ≥16、TestDpl7902_ ≥7、TestDst7902_/TestDhd7902_/TestDcf7902_/TestDsv7902_ 合计 ≥7
2. duty_schedule_service.go ≥70%(基线 0%)、duty_pool_service.go ≥70%(基线 4.9%)
3. duty_stats/duty_holiday/duty_config/duty_service 四文件 ≥70%(基线 0%)
4. GenerateSchedule 8 分支 + 轮询 + 节假日双向断言齐备
5. `go build ./...` + `go test ./...` exit 0;每 task 一原子 commit
</success_criteria>

<decision_audit>
本 plan 涉及的 phase-79 决策定案(executor 必须遵守):

| ID | 决策 | 依据 / 理由 |
|----|------|------------|
| D-79-01 | duty 家族归本 plan(research §7 的 79-02 行),不采 ROADMAP 失实标题的"menu/user/post 群"切法 | 79-RESEARCH §1/§7:ROADMAP plan 标题按已迁走的 legacy cache 群命名,失实;tech-domain 内聚切割 |
| D-79-06 | 命名 `<source-or-topic>_79_02_test.go`,NN=plan 号 | DQ6 confirmed |
| D-79-07 | 门面 duty_service.go 用"代表方法 round-trip + 其余错误分支"轻量口径,不重复下层全量断言 | 单行委托文件的下层已在本 plan 覆盖;重复断言无信息量且烧上下文 |
| (沿用) | quirk 纪律:非白名单排序回退等行为按现行为断言+注释锁定(Phase 73-04 Q5 同款),不修 | R7 + ROADMAP L247 |
| (沿用) | 装配/helper/串行 wave 纪律沿用 79-01(78-01 D-78-11 同构) | 已验证 |

基线数字来源:79-RESEARCH.md §2(2026-08-27 实测)。
</decision_audit>

<output>
Create `.planning/workstreams/milestone/phases/79-services-root-tail/79-02-SUMMARY.md` when done
</output>
