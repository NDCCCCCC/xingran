---
phase: 79-services-root-tail
plan: 03
status: pending
type: execute
wave: 3
depends_on: ["79-02"]
files_modified:
  - internal/services/notice_service_79_03_test.go
  - internal/services/template_service_79_03_test.go
  - internal/services/operlog_endpoint_tail_79_03_test.go
autonomous: true
requirements: [TAIL-01]

must_haves:
  truths:
    - "notice_service.go(116 unc @3.3%)≥70%:CreateNoticeWithTargets/GetNoticeStatusStatistics/GetNoticeList/GetNoticeByID/UpdateNotice/DeleteNotice/BatchDeleteNotices/PublishNotice/WithdrawNotice 各有用例"
    - "notice_read_service.go(70)/notice_target_service.go(71)/notice_query_service.go(10)三文件 ≥70%:MarkNoticeRead/MarkAllNoticesRead/Ignore/Unignore/GetIgnoredNotices/GetUnreadCount/GetUserNotices + GetTargetUsers/buildTargets/getChildDeptIDs/getTargetIDsByType/unique/GetNoticeStatistics + buildUserVisibleQuery(用户缺失/角色集合/E 簇 publish_status 语义)"
    - "template_service.go(166 unc @0%)≥70%:CRUD 族 + Preview/Render/ValidateVariables/GetVariables/Clone/Export/Import/GetTemplatesByVendor/GetStatistics(纯字符串替换,无 text/template 导入,research §2 确认)"
    - "oper_log_service.go(56)/api_endpoint_service.go(46)/swagger_extractor.go(18)/notice_cron_util.go(22)四文件 ≥70%:RecordOperLog/RecordAsync/RecordFromGinContext/getRequestParams/getResponseResult/FilterSensitiveParams + 端点权限判定 + swagger 路由抽取 + cron 表达式四函数"
    - "按 D-79-07:notice_query_service.go 从 79-01 移入本 plan(buildUserVisibleQuery 是 NoticeService 方法,notice 表由本 plan 建 —— fixture 内聚)"
    - "E 簇反转语义锁定:notice 的 publish_status 1=已发布(E 簇,Phase 69-04 判定)与 0=正常两套语义不可互换,断言一律引用 models.PublishStatus*/models.NoticeStatus* 具名常量"
  artifacts:
    - path: "internal/services/notice_service_79_03_test.go"
      provides: "NoticeService 全方法测试(前缀 TestNtc7903_/TestNrd7903_/TestNtg7903_/TestNqv7903_),CRUD+发布撤回+已读忽略+目标解析"
      min_lines: 500
      contains: "CreateNoticeWithTargets"
    - path: "internal/services/template_service_79_03_test.go"
      provides: "TemplateService 全方法测试(前缀 TestTsv7903_),CRUD+Preview/Render/Clone/Export/Import"
      min_lines: 380
      contains: "ValidateVariables"
    - path: "internal/services/operlog_endpoint_tail_79_03_test.go"
      provides: "oper_log/api_endpoint/swagger_extractor/notice_cron_util 测试(前缀 TestOpl7903_/TestAep7903_/TestSwe7903_/TestNcu7903_)"
      min_lines: 320
      contains: "FilterSensitiveParams"
  key_links:
    - from: "internal/services/notice_service_79_03_test.go"
      to: "CreateNoticeWithTargets → buildTargets → GetTargetUsers(notice_service.go:58 / notice_target_service.go:13/:73)"
      via: "同一 sqlite 库预置用户/部门/角色行,target_type 0/1/2/3 四类目标解析断言"
      pattern: "GetTargetUsers|buildTargets"
    - from: "internal/services/notice_service_79_03_test.go"
      to: "buildUserVisibleQuery 的权限过滤 SQL(notice_query_service.go:40-46)"
      via: "预置 ignore/dept/role 目标行,断言可见集与不可见集"
      pattern: "PublishStatusPublished|NoticeStatusNormal"
    - from: "internal/services/operlog_endpoint_tail_79_03_test.go"
      to: "RecordFromGinContext 的 gin.Context 构造(oper_log_service.go:76)"
      via: "httptest.NewRecorder + gin.CreateTestContext 注入请求/响应,断言落库字段"
      pattern: "CreateTestContext|RecordFromGinContext"
---

# Plan 3: notice/template/operlog/api-endpoint 簇 8 文件 (Phase: 79-services-root-tail)

<objective>
交付 internal/services root 的通知/模板簇测试:notice_service.go(116 unc)+ notice_read_service.go(70 unc)+ notice_target_service.go(71 unc)+ notice_query_service.go(10 unc)+ notice_cron_util.go(22 unc)+ template_service.go(166 unc)+ oper_log_service.go(56 unc)+ api_endpoint_service.go(46 unc)+ swagger_extractor.go(18 unc),合计 ~575 unc。

Purpose: TAIL-01 第三段。9 文件全部 sqlite/纯函数可测(research §3 (c) 类),是最大"文件数多但单文件浅"的簇;template_service 的 Preview/Render 是纯字符串替换(research §2 确认无 text/template),notice 链路要锁定 E 簇 publish_status 反转语义(Phase 69-04 判定)。
Output: 新增 3 个测试文件(零生产代码改动)。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/79-services-root-tail/79-RESEARCH.md
@.planning/workstreams/milestone/phases/79-services-root-tail/79-01-SUMMARY.md

<interfaces>
<!-- 关键契约(planner 已逐一读源码复核,2026-08-27)。executor 直接使用,无需重新探索。 -->

From internal/services/notice_service.go:
```go
func NewNoticeService(db *gorm.DB) *NoticeService // :19
func (s *NoticeService) CreateNoticeWithTargets(ctx, req *CreateNoticeRequest, creatorID, creatorName) (*models.Notice, error) // :58
func (s *NoticeService) GetNoticeStatusStatistics(ctx) (*NoticeStatusStatistics, error) // :116
func (s *NoticeService) GetNoticeList(ctx, page, pageSize int, title, noticeType *string, orderByColumn string, isAsc *bool) ([]models.Notice, int64, error) // :144
func (s *NoticeService) GetNoticeByID(ctx, noticeID) (*models.Notice, error) // :183
func (s *NoticeService) UpdateNotice(ctx, noticeID, req *UpdateNoticeRequest) error // :195
func (s *NoticeService) DeleteNotice(ctx, noticeID) error        // :257
func (s *NoticeService) BatchDeleteNotices(ctx, noticeIDs []string) error // :269
func (s *NoticeService) PublishNotice(ctx, noticeID) error       // :284 — 置 publish_status 为发布态(E 簇)
func (s *NoticeService) WithdrawNotice(ctx, noticeID) error      // :329 — 撤回
```

From internal/services/notice_read_service.go(同为 *NoticeService 方法):
```go
MarkNoticeRead :14 / MarkAllNoticesRead :36 / IgnoreNotice :64 / UnignoreNotice :84 /
GetIgnoredNotices :102 / GetUnreadCount :138 / GetUserNotices :173
```

From internal/services/notice_target_service.go(同为 *NoticeService 方法):
```go
GetTargetUsers(ctx, notice *models.Notice) ([]string, error) // :13 — target_type 0 全体/1 部门/2 角色/3 指定用户
buildTargets(noticeID, req) []models.NoticeTarget // :73
getChildDeptIDs(deptID) []string // :116
getTargetIDsByType(targets, targetType) []string // :131(包级)
unique(slice) []string // :142(包级)
GetNoticeStatistics(ctx, noticeID) (*models.NoticeStatistics, error) // :155
```

From internal/services/notice_query_service.go:
```go
type UserNoticeQueryContext struct { User models.User; RoleIDs []string; Query *gorm.DB } // :15
func (s *NoticeService) buildUserVisibleQuery(ctx, userID) (*UserNoticeQueryContext, error) // :23
//   :26 用户不存在 → "获取用户信息失败";:32 角色集合查 sys_user_role
//   :40-46 权限过滤:publish_status=PublishStatusPublished AND status=NoticeStatusNormal
//           AND 未被 ignore AND (target_type=0 全体 | 1 部门 | 2 角色 IN(roleIDs) | 3 指定用户)
```

From internal/services/notice_cron_util.go:
```go
func CalculateCronExpression(t time.Time) string // :12
func GenerateCronExpression(recurrenceType string, executeTime string, weekDay, monthDay *int, customExpr *string) (string, error) // :25 — 各 recurrence 分支 + 非法 executeTime
func GetNoticeJobName(noticeID string) string // :74
func GetCommonCronExpressions() []CommonCronExpression // :86
```

From internal/services/template_service.go:
```go
func NewTemplateService(db *gorm.DB) *TemplateService // :20
GetStatistics :34 / List :71 / Create :126 / GetByID :169 / GetByCode :178 / Update :200 /
Delete :242 / BatchDelete :267 / Preview :277 / Render :314 / GetVariables :351 /
ValidateVariables :360 / GetTemplatesByVendor :388 / Clone :404 / Export :440 / Import :455
// Preview/Render = 字符串替换(变量 {{xxx}} 形态以实现为准),无 text/template
```

From internal/services/oper_log_service.go:
```go
func NewOperLogService() OperLogService // :30(返回接口)
RecordOperLog(ctx, db, *models.OperLog) error // :35
RecordAsync(db, title, businessType int, method, requestMethod, operUrl string, ...) // :43
RecordFromGinContext(c *gin.Context, db, title, businessType int, method string) // :76
getRequestParams(c) *string // :149
getResponseResult(c) *string // :174
FilterSensitiveParams(params string) string // :197 — 敏感词脱敏(与 operlog 包约定一致,11 关键词)
```

From internal/services/api_endpoint_service.go / swagger_extractor.go:
```go
func NewAPIEndpointService(...) *APIEndpointService // :46(签名以源码为准,含 db/权限依赖)
GetUserAccessibleEndpoints :59 / getUserPermissions :119 / hasPermission :137 /
ValidateEndpoint :155 / InvalidateUserCache :193
func NewSwaggerExtractor(engine *gin.Engine) *SwaggerExtractor // :20
ExtractRoutes :25 / shouldExcludeRoute :45 / RouteExists :64
```

既有 analog(必读):
- internal/services/notice_status_statistics_test.go —— 既有 notice 统计断言口径(勿重写)。
- internal/services/notice_query_service.go:39-46 注释 —— publish_status(E 簇发布态)与 status(Notice 启停字段)两类语义不可互换(Phase 69-04/69-01 判定),断言一律具名常量。
- internal/utils/operlog — FilterSensitiveParams 的敏感词清单真相源(11 强制关键词,CLAUDE.md 已锁)。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: notice CRUD + 发布/撤回 + 状态统计</name>
  <files>internal/services/notice_service_79_03_test.go</files>
  <read_first>
  - internal/services/notice_service.go — :19-350 全文
  - internal/services/notice_target_service.go — :73-150(buildTargets 的 target_type 分支,Task 1 只需建行,Task 2 深测)
  - internal/models/ — Notice/NoticeTarget/NoticeIgnore model 与 NoticeStatus*/PublishStatus* 具名常量
  - internal/services/notice_status_statistics_test.go — 既有统计口径(勿重写)
  </read_first>
  <action>
  创建 internal/services/notice_service_79_03_test.go(package services)。helper `newNtc7903(t *testing.T) (*NoticeService, *gorm.DB)`:sqlite(t.TempDir 文件库)+ AutoMigrate(Notice/NoticeTarget/NoticeIgnore/User/Dept/Role/UserRole 及 CreateNoticeWithTargets 链路实际引用的 model;AutoMigrate 失败按 sqlite 缺表 family pattern 手动 CREATE TABLE)+ `NewNoticeService(db)` + t.Cleanup Close。测试前缀 TestNtc7903_:

  - `TestNtc7903_CreateWithTargets_AllTargetTypes`:target_type ∈ {0 全体,1 部门,2 角色,3 指定用户} 四种请求各建一次 → 断言 Notice 行 + sys_notice_target 行数与 target_type/target_id 形态;creatorID/creatorName 落库。
  - `TestNtc7903_GetList_PaginationFilterSort`:预置 5 行 → 分页 + title 模糊 + noticeType 精确 + orderByColumn 白名单/非法列回退(行为按源码断言并注释锁定,Phase 73-04 Q5 同款)。
  - `TestNtc7903_GetByID_Miss`:不存在 → 错误;存在 → 字段一致。
  - `TestNtc7903_UpdateNotice_FullAndMissing`:合法更新(标题/内容/类型)读回一致;不存在 ID → 错误。
  - `TestNtc7903_DeleteAndBatchDelete`:单删/批删后计数;软删语义(若 model 有 DeletedAt)断言 Unscoped 可见(glebarez sqlite 下验证软删列)。
  - `TestNtc7903_PublishAndWithdraw`:草稿 → PublishNotice → publish_status == models.PublishStatusPublished(E 簇具名常量,注释标明 1=已发布反转语义)→ WithdrawNotice → 回撤回态;重复 Publish/不存在 ID 分支按实现断言。
  - `TestNtc7903_StatusStatistics`:预置各状态行 → NoticeStatusStatistics 各计数与手算一致(对照既有 notice_status_statistics_test.go 口径,只补 unc 分支)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestNtc7903_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestNtc7903_" internal/services/notice_service_79_03_test.go` ≥ 7
  - target_type 四类建目标用例存在
  - Publish/Withdraw 断言引用 models.PublishStatus* 常量(grep 验证,禁裸 0/1)
  - `go test -count=1 -run "TestNtc7903_" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-03): notice CRUD + 发布撤回 + 状态统计(E 簇常量化断言)</commit>
  <done>notice_service.go 9 个方法全覆盖;E 簇 publish_status 反转语义以具名常量锁定。</done>
</task>

<task type="auto">
  <name>Task 2: notice_read + notice_target + notice_query(可见性链路)</name>
  <files>internal/services/notice_service_79_03_test.go</files>
  <read_first>
  - internal/services/notice_read_service.go — 全文(7 方法)
  - internal/services/notice_target_service.go — :13-190(GetTargetUsers 四类/buildTargets/getChildDeptIDs/getTargetIDsByType/unique/GetNoticeStatistics)
  - internal/services/notice_query_service.go — 全文(buildUserVisibleQuery 的 4-OR 权限过滤)
  - internal/services/notice_service_79_03_test.go — Task 1 helper newNtc7903(复用同一库)
  </read_first>
  <action>
  同文件追加:

  - `TestNrd7903_ReadChain`:MarkNoticeRead(落已读行,重复读不重复计)→ GetUnreadCount 递减 → MarkAllNoticesRead → 计数归零;ip 参数落库断言。
  - `TestNrd7903_IgnoreChain`:IgnoreNotice → GetIgnoredNotices 出现 → UnignoreNotice 后消失;重复 Ignore 幂等分支按实现断言。
  - `TestNrd7903_GetUserNotices_StatusFilter`:status 过滤参数 nil/指定值两分支;分页正确。
  - `TestNtg7903_GetTargetUsers_FourTypes`:target_type 0(全体 → 返回全部用户)/1(部门 → getChildDeptIDs 递归含子部门)/2(角色 → role 目标行解析)/3(指定用户)各断言返回集;getTargetIDsByType 过滤 + unique 去重(包级直调表驱动)。
  - `TestNtg7903_GetChildDeptIDs`:父子两层部门 → 递归含孙;无子 → 空切片。
  - `TestNtg7903_NoticeStatistics`:预置已读/忽略/目标行 → models.NoticeStatistics 各计数与手算一致。
  - `TestNqv7903_BuildVisibleQuery_UserMissing`:不存在 userID → 错误含"获取用户信息失败"。
  - `TestNqv7903_BuildVisibleQuery_FourOrMatrix`:预置 1 用户(2 角色)+ 5 条通知 —— (a) 全体型可见;(b) 部门型 target_id=用户部门可见、他部门不可见;(c) 角色型 roleIDs 命中可见、未命中不可见;(d) 指定用户型命中可见;(e) 已发布+正常之外的行(publish_status 非发布态或 status 停用)不可见;(f) 被 ignore 的行不可见。断言走 ctx.Query 实际执行 Count/Find。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestNrd7903_|TestNtg7903_|TestNqv7903_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestNrd7903_\|^func TestNtg7903_\|^func TestNqv7903_" internal/services/notice_service_79_03_test.go` ≥ 8
  - buildUserVisibleQuery 的 4-OR 可见性矩阵 + 用户缺失分支各有断言
  - getChildDeptIDs 递归(含孙级)断言存在
  - `go test -count=1 -run "TestNtc7903_|TestNrd7903_|TestNtg7903_|TestNqv7903_" ./internal/services/` exit 0(Task 1 不回归)
  </acceptance_criteria>
  <commit>test(79-03): notice 已读/忽略链 + 四类目标解析 + 可见性矩阵</commit>
  <done>notice_read/notice_target/notice_query 三文件全覆盖;4-OR 权限过滤逐支断言。</done>
</task>

<task type="auto">
  <name>Task 3: template_service CRUD + Preview/Render/ValidateVariables</name>
  <files>internal/services/template_service_79_03_test.go</files>
  <read_first>
  - internal/services/template_service.go — :20-280(GetStatistics/List/Create/GetByID/GetByCode/Update/Delete/BatchDelete)
  - internal/models/ — ConfigTemplate/TemplateVariable model 与 DeviceVendor/DeviceType 常量
  </read_first>
  <action>
  创建 internal/services/template_service_79_03_test.go(package services)。helper `newTsv7903(t) (*TemplateService, *gorm.DB)`(sqlite t.TempDir 文件库 + AutoMigrate ConfigTemplate/TemplateVariable)。测试前缀 TestTsv7903_:

  - `TestTsv7903_CreateAndGet`:合法创建(唯一 code)→ GetByID/GetByCode 双通道读回一致;重复 code → 错误分支(按实现断言)。
  - `TestTsv7903_List_PaginationFilter`:预置多行 → 分页 + 关键字/vendor/deviceType 过滤(按 req 字段)。
  - `TestTsv7903_Update_Delete_BatchDelete`:更新读回;单删/批删计数;删不存在 → 行为按实现断言。
  - `TestTsv7903_GetStatistics`:预置多状态/多 vendor 行 → TemplateStatistics 各计数与手算一致。
  - `TestTsv7903_GetVariables`:预置模板变量行 → 返回列表与序一致。
  - `TestTsv7903_ValidateVariables`:必填变量缺失 → 错误;全给 → nil;多余变量(若实现拒绝)分支按源码断言。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestTsv7903_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestTsv7903_" internal/services/template_service_79_03_test.go` ≥ 6
  - GetByCode 与 GetByID 双通道断言存在
  - `go test -count=1 -run "TestTsv7903_" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-03): template_service CRUD + 校验 + 统计</commit>
  <done>template_service.go CRUD/统计/校验段全覆盖。</done>
</task>

<task type="auto">
  <name>Task 4: template Preview/Render/Clone/Export/Import + GetTemplatesByVendor</name>
  <files>internal/services/template_service_79_03_test.go</files>
  <read_first>
  - internal/services/template_service.go — :277-490(Preview/Render/GetVariables/ValidateVariables/GetTemplatesByVendor/Clone/Export/Import)
  - internal/services/template_service_79_03_test.go — Task 3 helper(复用)
  </read_first>
  <action>
  同文件追加:

  - `TestTsv7903_Preview_Render_Substitution`:预置含变量占位的模板内容 → Preview(templateID, vars) 与 Render(templateCode, vars) 返回替换后字符串(变量形态以实现为准,注释锁定);模板不存在 → 错误。
  - `TestTsv7903_Export_Import_RoundTrip`:Export(templateID) → []byte → Import(data, createdBy) → 新模板字段一致(Clone 变体);坏 JSON/空字节 Import → 错误分支;code 冲突 Import → 错误分支(按实现)。
  - `TestTsv7903_Clone`:Clone 后 newName/newCode 落库且内容一致;源不存在 → 错误;newCode 重复 → 错误(按实现)。
  - `TestTsv7903_GetTemplatesByVendor`:预置多 vendor/deviceType 行 → 过滤正确;引用 models.DeviceVendor/models.DeviceType 具名常量(禁裸字符串/数字)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestTsv7903_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestTsv7903_" internal/services/template_service_79_03_test.go` ≥ 10(Task 3+4 合计)
  - Export→Import round-trip 断言存在
  - Preview/Render 变量替换断言存在(含变量缺失或原样保留的行为锁定注释)
  - `go test -count=1 -run "TestTsv7903_" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-03): template Preview/Render/Clone/Export-Import round-trip</commit>
  <done>template_service.go 全部 16 方法覆盖;Export-Import 与 Clone 双通道 round-trip。</done>
</task>

<task type="auto">
  <name>Task 5: oper_log + api_endpoint + swagger_extractor + notice_cron_util</name>
  <files>internal/services/operlog_endpoint_tail_79_03_test.go</files>
  <read_first>
  - internal/services/oper_log_service.go — 全文(重点 :76 RecordFromGinContext 的 gin.Context 消费与 :197 FilterSensitiveParams)
  - internal/services/api_endpoint_service.go — 全文(NewAPIEndpointService 签名/依赖注入形态)
  - internal/services/swagger_extractor.go — 全文 + internal/utils/operlog 的敏感词清单(CLAUDE.md 11 强制关键词)
  - internal/services/notice_cron_util.go — 全文
  </read_first>
  <action>
  创建 internal/services/operlog_endpoint_tail_79_03_test.go(package services)。sqlite helper `newOpl7903(t) (*gorm.DB, OperLogService, *APIEndpointService)`(AutoMigrate models.OperLog + api_endpoint 依赖 model;APIEndpointService 构造参数按 :46 签名注入,nil 依赖走错误分支)。测试前缀:

  - `TestOpl7903_RecordOperLog_Direct`:构造 models.OperLog 直调 RecordOperLog → 落库字段一致。
  - `TestOpl7903_RecordAsync`:RecordAsync(异步语义 —— 若内部起 goroutine,用轮询等待行出现,超时 2s,禁裸 sleep 长等;若为同步直写则直接断言,按实现)→ 行落库且 title/businessType 正确,引用 operlog.OperType* 具名常量语义(businessType int 值用 operlog 包常量)。
  - `TestOpl7903_RecordFromGinContext`:`gin.CreateTestContext(httptest.NewRecorder())` 构造 GET/POST 请求(client IP/UA/path/query)→ 调用后 sys_oper_log 行的 method/operUrl/requestMethod 与请求一致;getRequestParams/getResponseResult 对 nil/空 body 分支(同包直调)。
  - `TestOpl7903_FilterSensitiveParams`:表驱动 —— password/pwd/secret/token/key/salt/privateKey 等 11 强制关键词值脱敏为 ******;非敏感字段保留;坏 JSON 输入不 panic(行为按实现断言并注释锁定)。
  - `TestAep7903_GetUserAccessibleEndpoints`:预置用户/角色/端点权限行 → 返回端点集;无权限用户 → 空集;getUserPermissions/hasPermission 同包直调表驱动(命中/通配/未命中)。
  - `TestAep7903_ValidateEndpoint`:已知 route+method → EndpointDetail;未知 → 错误;InvalidateUserCache 调用后再次 GetUserAccessibleEndpoints 仍正确(缓存失效链)。
  - `TestSwe7903_ExtractRoutes`:gin.New() 注册 3 条路由(含 1 条应被排除的路径,如 /health 或 swagger 前缀,对照 shouldExcludeRoute 实现)→ ExtractRoutes 返回非排除集;RouteExists 命中/未命中;shouldExcludeRoute 表驱动。
  - `TestNcu7903_CronUtils`:CalculateCronExpression(固定 time.Date)→ 秒分时日月周六段形态;GenerateCronExpression 各 recurrence 分支(每日/每周+weekDay/每月+monthDay/自定义 customExpr/非法 executeTime → 错误);GetNoticeJobName 含 noticeID;GetCommonCronExpressions 非空且元素形态合法(段数断言)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestOpl7903_|TestAep7903_|TestSwe7903_|TestNcu7903_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestOpl7903_\|^func TestAep7903_\|^func TestSwe7903_\|^func TestNcu7903_" internal/services/operlog_endpoint_tail_79_03_test.go` ≥ 8
  - FilterSensitiveParams 覆盖 ≥6 个敏感关键词(11 关键词全清单对照 internal/utils/operlog 回归测试)
  - RecordFromGinContext 用 CreateTestContext 构造(grep 验证)
  - `go test -count=1 -run "TestOpl7903_|TestAep7903_|TestSwe7903_|TestNcu7903_" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-03): oper_log/api_endpoint/swagger/cron-util 四小文件 0→70%</commit>
  <done>四文件全方法覆盖;gin.Context 消费链与敏感词脱敏锁定。</done>
</task>

<task type="auto">
  <name>Task 6: plan 收口 — 9 文件 per-file 覆盖率 + -race + SUMMARY</name>
  <files>.planning/workstreams/milestone/phases/79-services-root-tail/79-03-SUMMARY.md</files>
  <read_first>
  - .planning/workstreams/milestone/phases/79-services-root-tail/79-RESEARCH.md §2 — 9 文件基线(0%/3.3%)
  </read_first>
  <action>
  收口三步:

  1. `go test -count=1 -coverprofile=cov79_03.out ./internal/services/`
     `go tool cover -func=cov79_03.out | grep -E "notice_service.go|notice_read_service.go|notice_target_service.go|notice_query_service.go|notice_cron_util.go|template_service.go|oper_log_service.go|api_endpoint_service.go|swagger_extractor.go"`
  2. 判定:9 文件全部 ≥70%;未达标回对应 task 补 unc 分支。
  3. `go test -race -count=1 -run "TestNtc7903_|TestTsv7903_|TestOpl7903_|TestAep7903_" ./internal/services/` + `go build ./...` + `go test ./...`。

  SUMMARY 落:9 文件实测覆盖率表(基线→实测)、D-79-07 notice_query 移入本 plan 的记录、quirk 锁定清单(排序回退/敏感词坏 JSON 行为)、滚动 covered stmts 增量(对照 79-02 SUMMARY)。
  </action>
  <verify>
    <automated>go test -count=1 -coverprofile=cov79_03.out ./internal/services/ && go tool cover -func=cov79_03.out | grep -E "notice_|template_service.go|oper_log_service.go|api_endpoint_service.go|swagger_extractor.go" && go test -race -count=1 -run "TestNtc7903_|TestTsv7903_" ./internal/services/ && go build ./... && go test ./... 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
  - 9 文件 per-file ≥70%(数字落 SUMMARY)
  - `go test ./...` exit 0;`go build ./...` exit 0;-race 抽样 exit 0
  - 生产 .go 改动 = 0 文件
  </acceptance_criteria>
  <commit>docs(79-03): notice/template/operlog 簇 9 文件覆盖率收口落 SUMMARY</commit>
  <done>通知/模板簇 9 文件全部 ≥70%,SC-2 该簇清欠完成。</done>
</task>

</tasks>

<notes>
- 依赖:79-01 装配纪律(sqlite t.TempDir 文件库/helper `newXxx79NN`/禁 t.Parallel);notice 表在本 plan 首建(79-01 只碰 cache,无 notice 表冲突 —— 同包文件级隔离)。
- E 簇语义:notice publish_status 1=已发布(Phase 69-04 判定的 E 簇反转),断言必须 models.PublishStatusPublished;status 用 models.NoticeStatusNormal。两类语义不可互换(notice_query_service.go:39 注释)。
- operlog 敏感词:11 强制关键词清单真相源在 internal/utils/operlog(回归测试锁定),FilterSensitiveParams 行为与其一致性断言即可,勿在测试内复制清单常量。
- race 安全:RecordAsync 若有异步 goroutine,等待用轮询(require.Eventually)+ 超时,禁长 sleep;其余纯 sqlite。
- 同包串行:本 plan wave 3,依赖 79-02。
</notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 ↔ 数据库 | glebarez sqlite(t.TempDir 文件库),不触碰生产 PG |
| 测试 ↔ HTTP 面 | swagger_extractor 只对进程内 gin.New() 引擎断言,不发真实网络请求 |
| 测试 ↔ 生产代码 | 仅新增 *_79_03_test.go;生产 .go 改动 = 0(D-79 escape hatch 除外) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-03-01 | Tampering | 测试误连真实 PG / 外网 | mitigate | 仅 sqlite;gin 引擎为进程内实例,无真实监听地址 |
| T-79-03-02 | Information Disclosure | operlog 测试造真实感凭据 | accept | 全部为测试字面量(password 测试值),无真实凭据入仓 |
| T-79-03-03 | DoS | RecordAsync goroutine 泄漏 | mitigate | require.Eventually 轮询 + -race 抽样验收 |
| T-79-03-SC | Tampering | 依赖安装 | accept | 零新增依赖(httptest/gin/testify 已有) |
</threat_model>

<verification>
- `go test -count=1 ./internal/services/` 全绿;新增用例 `-race` 抽样全绿
- `go tool cover -func=cov79_03.out`:9 文件全部 ≥70%
- `go build ./...` + `go test ./...` exit 0
- 生产 .go 改动 = 0 文件;既有 notice_status_statistics_test.go 不回归
</verification>

<success_criteria>
1. 三个测试文件存在且全绿:TestNtc7903_ ≥7、TestNrd7903_/TestNtg7903_/TestNqv7903_ 合计 ≥8、TestTsv7903_ ≥10、TestOpl7903_/TestAep7903_/TestSwe7903_/TestNcu7903_ 合计 ≥8
2. notice_service.go ≥70%(基线 3.3%)、template_service.go ≥70%(基线 0%)
3. notice_read/notice_target/notice_query/notice_cron_util/oper_log/api_endpoint/swagger_extractor 七文件 ≥70%(基线 0%)
4. E 簇 publish_status 反转语义以具名常量锁定;FilterSensitiveParams 敏感词清单与 operlog 包一致
5. `go build ./...` + `go test ./...` exit 0;每 task 一原子 commit
</success_criteria>

<decision_audit>
本 plan 涉及的 phase-79 决策定案(executor 必须遵守):

| ID | 决策 | 依据 / 理由 |
|----|------|------------|
| D-79-01 | 本 plan 簇 = research §7 的 79-03 行 + notice_query_service.go(自 79-01 移入) | DQ1 同源:ROADMAP 79-03 标题"token blacklist + DataCacheService/CacheConfigService 深路径"按 research §7 重切后,该内容归 79-01;本 plan 承接 notice/template/operlog/api-endpoint 簇 |
| D-79-07 | notice_query_service.go 归本 plan(fixture 内聚:buildUserVisibleQuery 是 NoticeService 方法,notice 表由本 plan 建) | planner 微调;净效果零遗漏,79-01 SUMMARY 与本 SUMMARY 双向记录 |
| D-79-06 | 命名 `<source-or-topic>_79_03_test.go` | DQ6 confirmed |
| (沿用) | E 簇反转语义/敏感词清单/排序回退 quirk 全部"锁定+注释",零生产代码改动 | Phase 69-04 + R7 + ROADMAP L247 |
| (沿用) | 装配/helper/串行 wave 纪律沿用 79-01 | 已验证 |

基线数字来源:79-RESEARCH.md §2(2026-08-27 实测)。
</decision_audit>

<output>
Create `.planning/workstreams/milestone/phases/79-services-root-tail/79-03-SUMMARY.md` when done
</output>
