---
phase: 79-services-root-tail
plan: 05
status: pending
type: execute
wave: 5
depends_on: ["79-04"]
files_modified:
  - internal/services/mac_history_query_service_79_05_test.go
  - internal/services/mac_collection_service_79_05_test.go
  - internal/services/mac_history_tail_79_05_test.go
  - internal/services/email_sender_service_79_05_test.go
  - internal/services/api_sender_ad_ldap_79_05_test.go
autonomous: true
requirements: [TAIL-01]

must_haves:
  truths:
    - "mac_history_query_service.go(358 unc @8.0%,全包 #1 缺口文件)≥70%:GetVendor/ImportOUIData/QueryPortHistory/QueryDeviceHistory/QueryHistory/ExportHistory/QueryConnectionStats 及 5 个 queryFromDB 私有实现 + getLongOccupancyThreshold + perfCacheTTL;缓存路径经 NewMACHistoryQueryServiceWithCache(MemoryCache)打开"
    - "mac_collection_service.go(208 unc @28.5%)≥70%:5 个解析纯函数(parseMACAddressTable/parseMACLine/parseRuijiePortSecurityLine/mergeMACEntries/cleanTimestampFromInterface)+ getMACCommand/getMACCommands/getMACThreshold + CRUD(GetMACAddressList/Stats/CleanOldRecords/BatchDelete)+ loadConfigFromDB/ReloadConfig;collectDeviceMAC(executor 路径)不进本 plan,归 79-06"
    - "mac_history_service.go(78)/mac_history_partition.go(95)/mac_history_heatmap_service.go(52)/mac_history_matview_service.go(30)/mac_perf_config_seed.go(11)五文件 ≥70%:PG-only DDL 一律以『字符串构造器纯断言 + sqlite 跳过分支』口径测(R6,禁真建分区/物化视图)"
    - "email_sender_service.go(189 unc @0%)按 D-79-03 达 ≥65%:plain fake SMTP(net.Listen + bufio 会话)驱动 sendPlainSMTP + Send happy/error + TLS 双路径 dial-error 分支 + 全部纯 builder(buildEmailContent/buildNoticeHTMLBody/getNoticeTypeLabel/getPriorityClass/getPriorityLabel/plainAuth.Start/Next);sendWithTLS/sendWithSTARTTLS 的 TLS 握手 happy path 因 InsecureSkipVerify:false 硬编码(email_sender_service.go:203-204/:271-272)不可达,SUMMARY 记录不覆盖"
    - "api_sender_service.go(119 unc)按 D-79-03 达 ≥75%:sendRequest 回环(fake HTTP 端点)+ buildRequestBody/buildFromTemplate/buildDefaultBody/setRequestHeaders/setAuthentication 纯函数表"
    - "ad_ldap_client.go(179 unc)按 D-79-04 达 ≥55%(SC-2 floor 为 <50%,wire ops 不进本 phase):loadADTLSSkipVerify env 矩阵/formatUsername/extractRDN/parseIntOrDefault/encodePassword/NewLDAPClient/Connect dial-error/Close/IsConnected + 全部 wire ops 的未连接前置守卫分支(表驱动逐方法断言未 Connect 即 error)"
    - "按 D-79-06 命名;helper 带 7905 后缀(R5);fake SMTP/HTTP listener 全部 127.0.0.1:0 + t.Cleanup Close;禁 t.Parallel"
  artifacts:
    - path: "internal/services/mac_history_query_service_79_05_test.go"
      provides: "MACHistoryQueryService 全链测试(前缀 TestMhq7905_),含 ExportHistory xlsx 字节断言 + 缓存装配"
      min_lines: 520
      contains: "ExportHistory"
    - path: "internal/services/mac_collection_service_79_05_test.go"
      provides: "MACCollectionService 解析纯函数表 + CRUD(前缀 TestMcl7905_)"
      min_lines: 400
      contains: "parseRuijiePortSecurityLine"
    - path: "internal/services/mac_history_tail_79_05_test.go"
      provides: "mac_history_service/partition/matview/heatmap/perf_seed 测试(前缀 TestMhs7905_/TestMhp7905_/TestMhm7905_/TestMhh7905_/TestMps7905_)"
      min_lines: 360
      contains: "isPostgres"
    - path: "internal/services/email_sender_service_79_05_test.go"
      provides: "EmailSender 测试(前缀 TestEml7905_),内嵌 plain fake SMTP server(~80 行)"
      min_lines: 420
      contains: "net.Listen"
    - path: "internal/services/api_sender_ad_ldap_79_05_test.go"
      provides: "APISender + LDAPClient Tier-1 测试(前缀 TestApd7905_/TestAdl7905_)"
      min_lines: 380
      contains: "setAuthentication"
  key_links:
    - from: "internal/services/mac_history_query_service_79_05_test.go"
      to: "NewMACHistoryQueryServiceWithCache(db, dataCache, perfConfig)(mac_history_query_service.go:150)"
      via: "79-01 的 MemoryCache/newCcs7901 装配注入 → 缓存命中路径打开"
      pattern: "NewMACHistoryQueryServiceWithCache|BuildMACQueryCacheKey"
    - from: "internal/services/email_sender_service_79_05_test.go"
      to: "sendPlainSMTP 的 smtp 会话(email_sender_service.go:314)"
      via: "net.Listen(127.0.0.1:0) + bufio 读写 220/EHLO/250/AUTH 235/MAIL/RCPT/DATA/QUIT 221 假服务"
      pattern: "sendPlainSMTP|EHLO"
    - from: "internal/services/api_sender_ad_ldap_79_05_test.go"
      to: "Connect 的 ldap.DialURL(ad_ldap_client.go:52)"
      via: "指向已关闭端口的 LDAP URL → dial-error 分支;wire ops 未连接守卫表驱动"
      pattern: "DialURL|IsConnected"
---

# Plan 5: 外呼三件套 (email/api_sender/ad_ldap) + mac 家族 (Phase: 79-services-root-tail)

<objective>
交付 internal/services root 的第二大簇:mac_history_query_service.go(358 unc,全包 #1 缺口)+ mac_collection_service.go(208 unc)+ mac_history_service.go(78)+ mac_history_partition.go(95)+ mac_history_heatmap_service.go(52)+ mac_history_matview_service.go(30)+ mac_perf_config_seed.go(11)+ email_sender_service.go(189)+ api_sender_service.go(119)+ ad_ldap_client.go(179),合计 ~1320 unc。

Purpose: TAIL-01 第五段, Novel fixture 最多的 plan —— plain fake SMTP(D-79-03)、fake HTTP 回环(httpmock/httptest)、LDAP dial-error(D-79-04)、excelize 导出字节断言、PG-only DDL 字符串化(R6)。本 plan 后仅剩 device 家族(79-06)。
Output: 新增 5 个测试文件(零生产代码改动)。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/79-services-root-tail/79-RESEARCH.md
@.planning/workstreams/milestone/phases/79-services-root-tail/79-01-SUMMARY.md

<interfaces>
<!-- 关键契约(planner 已逐一读源码复核,2026-08-27)。executor 直接使用,无需重新探索。 -->

From internal/services/mac_history_query_service.go:
```go
func NewMACHistoryQueryService(db *gorm.DB) MACHistoryQueryService // :145(cache nil)
func NewMACHistoryQueryServiceWithCache(db *gorm.DB, dataCache *DataCacheService, perfConfig *CacheConfigService) MACHistoryQueryService // :150
func (s *macHistoryQueryServiceImpl) perfCacheTTL() time.Duration // :155
func (s *macHistoryQueryServiceImpl) ImportOUIData(ctx) error // :202 — OUI 数据导入(文件/内嵌源,以实现为准)
func (s *macHistoryQueryServiceImpl) GetVendor(ctx, macAddress) (string, error) // :242(部分已测,补缓存/未命中分支)
QueryPortHistory :288 → queryPortHistoryFromDB :320
QueryDeviceHistory :413 → queryDeviceHistoryFromDB :445
QueryHistory :534(列表分页/过滤)
ExportHistory(ctx, req, w io.Writer) error // :664 — excelize 写 xlsx 到 io.Writer(bytes.Buffer 可断言 "PK" zip magic)
getLongOccupancyThreshold :796 / QueryConnectionStats :829 → queryConnectionStatsFromDB :848
// 包级纯函数 extractOUIPrefix/validateMACAddress 已有测试(mac_history_query_service_test.go),勿重写
// 既有 fixture setupTestService(:69)用 file::memory:?cache=shared 且 cache:nil —— 本 plan 新 helper 用 t.TempDir 文件库 + MemoryCache 变体
```

From internal/services/mac_collection_service.go:
```go
func NewMACCollectionService(db, executor *device.DeviceExecutor, lldpSvc, filterRuleSvc) *MACCollectionService // :39 — executor 传 nil(CRUD/解析面不触达)
CollectAllDevices :64 / CollectDevice :106 / collectDeviceMAC :116 // executor 路径 → 79-06
getMACCommand :355 / getMACCommands :366 / parseMACAddressTable :382 / parseMACLine :420 /
parseRuijiePortSecurityLine :518 / mergeMACEntries :559 / cleanTimestampFromInterface :581
GetMACAddressList :612 / GetMACAddressAddressStats → GetMACAddressStats :715 / CleanOldRecords :755 /
BatchDelete :770 / loadConfigFromDB :779 / ReloadConfig :791 / getMACThreshold :798
// 既有 mac_collection_service_test.go 与 mac_history_merge_transitions_test.go 勿重写,只补 unc
```

From internal/services/mac_history_service.go / partition / matview / heatmap / perf seed:
```go
func NewMACHistoryService(db) MACHistoryService // mac_history_service.go:83
BuildMACStateMap :100(纯)/ RecordMACChange :138 / vlanEqual :271(纯)/ MergeFlappingRecords :306 /
CleanupAllDevicesFlapping :438 / MergeByTransitions :496 / mergeTransitionsForDevice :523 / PurgeMeaninglessRecords :610
func NewPartitionService(db) PartitionService // mac_history_partition.go:44
isPostgres :51 / CreateMonthlyPartition :57(PG-only DDL :91-94)/ EnsurePartitionsExist :109 /
GetRetentionDays :149 / DropExpiredPartitions :175;sqlite 分支 :110 "跳过分区管理"(直接可测)
func NewMACHistoryMatViewService(db) // matview:47 — isPostgreSQL :51 / RefreshSingleMatView :55 / RefreshAllMaterializedViews :74(sqlite 跳过分支)
func NewMACHistoryHeatmapService(db, dataCache, perfConfig) // heatmap:57 — perfCacheTTL :62 / perfTopN :75 / QueryHeatmap :91 → queryHeatmapFromMV :131 / isPostgreSQL :185
func SeedMACPerfConfigs(db *gorm.DB) error // mac_perf_config_seed.go:25 — 幂等 upsert 3 键;db==nil → Warnf+nil
// mac_perf_config_seed.go 的 3 个配置键常量(MACPerfConfig*)为 D-06 锁定,断言引用常量禁裸字符串
```

From internal/services/email_sender_service.go:
```go
func newPlainAuth(identity, username, password, host string) smtp.Auth // :35;plainAuth.Start :23 / Next :27(包级可测)
func NewEmailSenderService(db *gorm.DB) *EmailSenderService // :45
Send(ctx, configID, msg *EmailMessage) *SendResult // :67 — config 不存在/禁用/发送失败分支
SendWithDefaultConfig(ctx, msg) *SendResult // :149
sendEmail(host, port, username, password, fromAddress, fromName, msg, useSSL, useSTARTTLS) error // :164 — 三路派发
sendWithTLS :201 / sendWithSTARTTLS :255 // ⚠ InsecureSkipVerify:false 硬编码(:203-204/:271-272)→ 只能测 dial-error
sendPlainSMTP(addr, username, password, fromAddress, toAddresses, content) error // :314 — plain fake SMTP 全链
buildEmailContent(from, msg) ([]byte, error) // :362
SendNoticeEmail(ctx, configID, notice, userAddresses) *SendResult // :408
getNoticeTypeLabel :426 / buildNoticeHTMLBody :438 / getPriorityClass :497 / getPriorityLabel :509 // 纯 builder
TestEmailConfig(ctx, configID, testTo) *SendResult // :521
```

From internal/services/api_sender_service.go:
```go
func NewAPISenderService(db *gorm.DB) *APISenderService // :32
Send :60 / SendWithDefaultConfig :110 / sendRequest :142(fake HTTP 端点驱动)/
buildRequestBody :226 / buildFromTemplate :237 / buildDefaultBody :259 / setRequestHeaders :280 /
setAuthentication :305(bearer/basic/custom-header 分支)/ SendNoticeAPI :351 / SendSMS :370 /
SendWebhook :383 / TestAPIConfig :394
```

From internal/services/ad_ldap_client.go:
```go
func loadADTLSSkipVerify() bool // :29 — env 矩阵(变量名以实现为准)
func NewLDAPClient(config *models.ADConfig) *LDAPClient // :47
func (c *LDAPClient) Connect() error // :52 — ldap.DialURL 指向已关闭端口 → dial-error
func (c *LDAPClient) formatUsername(username string) string // :101(域前缀形态)
func (c *LDAPClient) Close :119 / IsConnected :127
// wire ops(未连接前置守卫分支全部可测,逐方法断言未 Connect 即 error):
SearchOUs :134 / SearchGroups :156 / SearchUsers :181 / SearchUsersByOU :209 / SearchGroupMembers :236 /
UpdateUserAttribute :304 / UpdateUserMultipleAttributes :326 / UpdateGroupAttribute :346 / MoveUser :366 /
EnableUser :384 / DisableUser :403 / UnlockUser :422 / ResetPassword :443 / AddGroupMember :461 /
RemoveGroupMember :474 / getUserByDN :489
func (c *LDAPClient) extractRDN(dn string) string // :516(纯)
func (c *LDAPClient) parseIntOrDefault(s string, defaultValue int) int // :526(纯)
func (c *LDAPClient) encodePassword(password string) string // :537(纯,utf-16le + 引号字节,按实现断言字节形态)
```

装配契约(79-01 已沉淀):
- `cache.NewMemoryCache(1000, 5*time.Minute)` + t.Cleanup 单次 Close(QUIRK-P1 禁二次 Close)
- `cache.NewRedisCache(&cache.CacheConfig{Addr: mr.Addr()}, "")`(可选 Redis parity)
- sqlite 一律 `filepath.Join(t.TempDir(), "<topic>79.db")` 文件库

既有 analog(必读):
- internal/services/mac_history_query_service_test.go / mac_history_query_service_tz_test.go / mac_history_perf_verify_test.go —— 既有 mac query 断言口径与表结构(勿重写;`setupTestService` 名被占用)。
- internal/services/mac_collection_service_test.go / mac_history_merge_transitions_test.go / mac_history_purge_test.go —— 既有 mac 断言口径。
- internal/core/captcha_78_01_test.go —— t.Cleanup/无 t.Parallel 纪律范本。
- 78-04 经验(ROADMAP L209):Windows loopback UDP 跨 socket 会丢弃响应 —— 本 plan 不含 SNMP(归 79-06),仅备忘。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: mac_history_query 装配 + GetVendor/ImportOUIData/perfCacheTTL</name>
  <files>internal/services/mac_history_query_service_79_05_test.go</files>
  <read_first>
  - internal/services/mac_history_query_service.go — :145-300(New* 两构造/perfCacheTTL/ImportOUIData/GetVendor)
  - internal/services/mac_history_query_service_test.go — 既有 fixture/表结构/断言口径(勿重写)
  - internal/models/ — MACOUIVendor 与 MAC 历史主表 model(表名以 TableName() 为准)
  </read_first>
  <action>
  创建 internal/services/mac_history_query_service_79_05_test.go(package services)。两个 helper:

  - `newMhq7905(t) (*macHistoryQueryServiceImpl, *gorm.DB)`:sqlite(t.TempDir 文件库)+ AutoMigrate(MACOUIVendor + MAC 历史主表;失败按 sqlite 缺表 family pattern 手动 CREATE TABLE,列集以 model 为准)+ 同包构造 `&macHistoryQueryServiceImpl{db: db, cache: nil}`。
  - `newMhq7905Cached(t) (*macHistoryQueryServiceImpl, *cache.MemoryCache)`:同库 + `cache.NewMemoryCache(1000, 5*time.Minute)` 注入(cache 字段)+ perfConfig 字段用 `NewCacheConfigService(db)`(sys_config 建表可复用 79-01 的手动 CREATE 形态)+ t.Cleanup 单次 Close。

  测试前缀 TestMhq7905_:

  - `TestMhq7905_PerfCacheTTL`:perfConfig nil → 默认 TTL;sys_config 种子键(network.mac.perf.cache_ttl_seconds,引用 mac_perf_config_seed.go 的 MACPerfConfigCacheTTLSeconds 常量)= "120" → 120*time.Second;非法值 → 默认(表驱动,以 :155-168 实现为准)。
  - `TestMhq7905_GetVendor_CacheAndDB`:Cached 装配 —— 首调走 DB(预置 MACOUIVendor 行)→ 二调命中缓存(可观察证据:删行后仍返回同 vendor);未知 MAC → 空串/错误按实现;非法 MAC → validateMACAddress 错误分支。
  - `TestMhq7905_ImportOUIData`:按 :202 实现断言(内嵌数据源 → 行数增长;若读外部文件则 t.TempDir 造 CSV/源文件);幂等重跑不重复插入(以实现为准);失败源 → 错误分支。
  - `TestMhq7905_NewConstructors`:两构造器返回类型实现 MACHistoryQueryService 接口(类型断言)+ nil-db 的行为按实现断言(不 panic 或 panic-safe 注释)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestMhq7905_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestMhq7905_" internal/services/mac_history_query_service_79_05_test.go` ≥ 4
  - 缓存命中可观察证据(删行后仍命中)存在
  - MACPerfConfig* 常量被引用(grep 验证,禁裸配置键字符串)
  - `go test -count=1 -run "TestMhq7905_" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-05): mac query 装配 + GetVendor 缓存路径 + ImportOUIData</commit>
  <done>两套装配 helper 就位;GetVendor 缓存/DB 双路径 + 导入幂等覆盖。</done>
</task>

<task type="auto">
  <name>Task 2: mac_history_query 查询四链(PortHistory/DeviceHistory/History/ConnectionStats)</name>
  <files>internal/services/mac_history_query_service_79_05_test.go</files>
  <read_first>
  - internal/services/mac_history_query_service.go — :288-533(QueryPortHistory/queryPortHistoryFromDB/QueryDeviceHistory/queryDeviceHistoryFromDB/QueryHistory)+ :796-880(getLongOccupancyThreshold/QueryConnectionStats/queryConnectionStatsFromDB)
  - internal/services/mac_history_query_service_79_05_test.go — Task 1 helpers(复用)
  - internal/models/ — MAC 历史主表字段(MAC/端口/接口/VLAN/时间戳列,查询条件构造依据)
  </read_first>
  <action>
  同文件追加。先预置 MAC 历史种子数据 helper `seedMhq7905(t, db, n int)`(n 条跨时间/跨端口的记录,时间显式 time.Date 构造防跨日 flake):

  - `TestMhq7905_QueryPortHistory`:指定 device+port+时间窗 → 返回记录集与过滤一致;MAC 非法/时间窗倒挂 → 错误分支;空结果形态按实现(:288-319 校验段 + :320-412 DB 段)。
  - `TestMhq7905_QueryDeviceHistory`:指定 MAC+窗口 → 记录集;分页字段断言(:413-533)。
  - `TestMhq7905_QueryHistory_List`:列表分页 + 多过滤条件组合(MAC 前缀/设备/接口/时间范围,按 req 字段)+ 总数一致;空库 → 零值非 nil。
  - `TestMhq7905_GetLongOccupancyThreshold`:默认值分支 + sys_config 覆盖分支 + 非法值回退(引用 MACPerfConfig* 常量或 :796 实现的键)。
  - `TestMhq7905_QueryConnectionStats`:预置多设备记录 → ConnectionStatsResponse 聚合与手算一致;空库分支(:829-880)。
  - `TestMhq7905_Query_CachedParity`:Cached 装配对 QueryHistory 同参数结果与裸装配一致(缓存不改变语义;命中后二次调用结果一致)。

  时间条件一律显式 time.Date;禁 time.Now 相对断言。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestMhq7905_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestMhq7905_" internal/services/mac_history_query_service_79_05_test.go` ≥ 10(Task 1+2 合计)
  - 四查询链各有错误分支用例(非法参数/空结果)
  - `go test -count=1 -run "TestMhq7905_" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-05): mac query 四查询链 + 阈值 + 缓存语义等价</commit>
  <done>查询主干全通,#1 缺口文件的 DB 段打开。</done>
</task>

<task type="auto">
  <name>Task 3: mac_history_query ExportHistory(xlsx 字节断言)</name>
  <files>internal/services/mac_history_query_service_79_05_test.go</files>
  <read_first>
  - internal/services/mac_history_query_service.go — :664-795(ExportHistory 全文:excelize 流程/列头/过滤复用)
  - internal/services/mac_history_query_service_79_05_test.go — Task 1/2 helpers + seed(复用)
  </read_first>
  <action>
  同文件追加:

  - `TestMhq7905_ExportHistory_Happy`:seed 3 条 → `var buf bytes.Buffer` 传 io.Writer → err==nil 且 buf.Len()>0 且 bytes.HasPrefix(buf.Bytes(), []byte("PK"))(xlsx=zip magic);再经 `excelize.OpenReader(bytes.NewReader(buf.Bytes()))` 打开断言 sheet/行数与种子一致(excelize 已在 go.mod,Import 路径合规)。
  - `TestMhq7905_ExportHistory_Empty`:空数据集 → 导出成功仅表头(或按实现断言行为,注释锁定)。
  - `TestMhq7905_ExportHistory_WithFilters`:带过滤参数 → 仅导出命中行。
  - `TestMhq7905_ExportHistory_InvalidReq`:非法请求(如 MAC 非法/时间倒挂,按 :664 校验段)→ 错误分支。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestMhq7905_ExportHistory" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestMhq7905_ExportHistory" internal/services/mac_history_query_service_79_05_test.go` ≥ 4
  - "PK" zip magic + excelize.OpenReader 双重断言存在
  - `go test -count=1 -run "TestMhq7905_" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-05): mac ExportHistory xlsx 字节与内容断言</commit>
  <done>ExportHistory(#1 文件的最大单函数)四分支覆盖。</done>
</task>

<task type="auto">
  <name>Task 4: mac_collection 解析纯函数表 + CRUD/配置</name>
  <files>internal/services/mac_collection_service_79_05_test.go</files>
  <read_first>
  - internal/services/mac_collection_service.go — :355-810(getMACCommand/getMACCommands/parse 三函数/mergeMACEntries/cleanTimestampFromInterface/GetMACAddressList/Stats/CleanOldRecords/BatchDelete/loadConfigFromDB/ReloadConfig/getMACThreshold)
  - internal/services/mac_collection_service_test.go — 既有断言口径(勿重写)
  - internal/models/ — DeviceMACAddress/NetworkDevice model + DeviceVendor/DeviceStatus 常量
  </read_first>
  <action>
  创建 internal/services/mac_collection_service_79_05_test.go(package services)。helper `newMcl7905(t) (*MACCollectionService, *gorm.DB)`:`NewMACCollectionService(db, nil, nil, nil)`(executor/lldp/filterRule 全 nil —— 解析+CRUD 面不触达)+ sqlite AutoMigrate(DeviceMACAddress/NetworkDevice + loadConfigFromDB 读的配置表)。测试前缀 TestMcl7905_:

  - `TestMcl7905_ParseMACTable_ByVendor`:表驱动各 vendor(models.DeviceVendor 具名常量)的样例输出文本 → parseMACAddressTable 返回条目数/字段;不识别行跳过;空输出 → 空 slice 或错误(按实现)。
  - `TestMcl7905_ParseMACLine_Table`:合法行(多厂商格式)/缺字段行/垃圾行 → entry+ok / false 三态(以 :420-517 实现为准)。
  - `TestMcl7905_ParseRuijiePortSecurityLine`:表驱动合法/字段不足/畸形 → 两态(:518-558)。
  - `TestMcl7905_MergeMACEntries`:多 bucket 合并去重/顺序语义断言(:559-580,以实现为准注释锁定)。
  - `TestMcl7905_CleanTimestampFromInterface`:表驱动(带时间戳后缀/干净接口名/空串)→ 清洗结果(:581-611)。
  - `TestMcl7905_GetMACCommand_Vendors`:各 vendor → 命令串/命令集;未知 vendor → 默认/错误分支(:355-381)。
  - `TestMcl7905_GetMACThreshold`:配置表驱动 + 默认回退(:798)+ loadConfigFromDB/ReloadConfig 前后差异(:779-797)。
  - `TestMcl7905_MACAddressList_Pagination`:seed 设备+MAC 记录 → 分页/deviceID/deptID/macAddress/interfaceName 过滤 + orderByColumn 白名单分支(:612-714)。
  - `TestMcl7905_MACStats_Clean_BatchDelete`:Stats 聚合手算;CleanOldRecords(days) 只删老记录并返回计数;BatchDelete 返回删除数(:715-778)。
  - `TestMcl7905_Collect_NilExecutor`:CollectAllDevices/CollectDevice 在 executor nil 下的行为(空结果/错误,按 :64-115 实现断言并注释 —— executor 真路径归 79-06)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestMcl7905_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestMcl7905_" internal/services/mac_collection_service_79_05_test.go` ≥ 10
  - parse 三函数表驱动齐备(合法/畸形/垃圾行)
  - vendor/status 断言引用 models 具名常量
  - `go test -count=1 -run "TestMcl7905_" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-05): mac_collection 解析纯函数表 + CRUD/配置阈值</commit>
  <done>mac_collection_service.go 解析 ~100 stmts + CRUD 面全覆盖;executor 路径边界注释明确归 79-06。</done>
</task>

<task type="auto">
  <name>Task 5: mac_history_service + partition/matview/heatmap/perf_seed(PG-only 字符串化口径)</name>
  <files>internal/services/mac_history_tail_79_05_test.go</files>
  <read_first>
  - internal/services/mac_history_service.go — :83-640(BuildMACStateMap/vlanEqual/RecordMACChange/MergeFlappingRecords/CleanupAllDevicesFlapping/MergeByTransitions/mergeTransitionsForDevice/PurgeMeaninglessRecords)
  - internal/services/mac_history_partition.go — 全文(:49 方言守卫/:91-94 PG DDL/:110 sqlite 跳过)
  - internal/services/mac_history_matview_service.go + mac_history_heatmap_service.go + mac_perf_config_seed.go — 全文
  - internal/services/mac_history_merge_transitions_test.go / mac_history_purge_test.go — 既有口径(勿重写)
  </read_first>
  <action>
  创建 internal/services/mac_history_tail_79_05_test.go(package services)。helper `newMhs7905(t) (*gorm.DB, MACHistoryService, PartitionService, MACHistoryMatViewService, MACHistoryHeatmapService)`(同一 sqlite 库四 service;heatmap 用 79-01 MemoryCache + CacheConfigService 装配)。测试前缀:

  - `TestMhs7905_BuildMACStateMap_AndVlanEqual`:两纯函数表驱动(vlanEqual nil/相等/不等;BuildMACStateMap 键构造与覆盖语义)。
  - `TestMhs7905_RecordMACChange`:旧→新 MAC 变更 → 历史行落库;无变化 → 不落行;首见设备 → 初始化分支(对照既有 record_change 测试口径,只补 unc)。
  - `TestMhs7905_MergeFlapping`:预置抖动记录(同 MAC 短时反复)→ MergeFlappingRecords 合并数正确;无抖动 → 0;CleanupAllDevicesFlapping 汇总(既有 merge/purge 测试口径,只补 unc 分支)。
  - `TestMhs7905_MergeByTransitions`:预置转换记录 → 返回合并数;空库 → 0。
  - `TestMhs7905_PurgeMeaninglessRecords_DryRun`:dryRun=true 不删返回计数;false 实删;既有 purge 测试口径补 unc。
  - `TestMhp7905_Partition_SqliteSkip`:sqlite 下 CreateMonthlyPartition/EnsurePartitionsExist/DropExpiredPartitions 走"跳过分区管理"分支(:110 直接返回,断言 nil error + 无 DDL 副作用)。
  - `TestMhp7905_GetRetentionDays`:默认值 + 配置覆盖(:149-174)。
  - `TestMhp7905_Partition_DDL_StringShape`(R6 口径):若 :57-108 的 DDL 以可观察字符串构造(同包直调内部 builder/拼接函数,以实现为准)→ 断言含 "PARTITION OF" 与年月形态;若 DDL 直接 Exec 不可拆 → 按 D-79 quirk 纪律在 sqlite 不可达,落 SUMMARY 不覆盖(禁为覆盖率真建 PG 分区)。
  - `TestMhm7905_MatView_SqliteSkip`:RefreshSingleMatView/RefreshAllMaterializedViews 在 sqlite 分支直接跳过(断言 nil error);isPostgreSQL 同包直调 false。
  - `TestMhh7905_Heatmap`:sqlite 下 QueryHeatmap 走非 MV 回退分支(若实现有)或跳过分支(按 :91-184 与 :185 isPostgreSQL 实现断言);perfTopN 默认/配置覆盖(引用 MACPerfConfigHeatmapTopN 常量);Cached 装配命中断言。
  - `TestMps7905_SeedMACPerfConfigs`:空库 → 3 键落库(值与 macPerfConfigDefaults 一致);重复调用幂等(不覆盖已存在);db==nil → nil error(Warnf 分支)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestMhs7905_|TestMhp7905_|TestMhm7905_|TestMhh7905_|TestMps7905_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestMhs7905_\|^func TestMhp7905_\|^TestMhm7905_\|^func TestMhm7905_\|^func TestMhh7905_\|^func TestMps7905_" internal/services/mac_history_tail_79_05_test.go` ≥ 11
  - PG-only 路径零真实 DDL 执行(全部 sqlite 跳过分支或字符串断言,R6)
  - MACPerfConfig* 常量引用(grep 验证)
  - `go test -count=1 -run "TestMhs7905_|TestMhp7905_|TestMhm7905_|TestMhh7905_|TestMps7905_" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-05): mac history/partition/matview/heatmap/perf_seed(PG-only 字符串化口径)</commit>
  <done>mac 尾部五文件覆盖;PG-only 路径按 R6 口径处理并落 SUMMARY。</done>
</task>

<task type="auto">
  <name>Task 6: email_sender(plain fake SMTP + TLS dial-error + 纯 builder,D-79-03)</name>
  <files>internal/services/email_sender_service_79_05_test.go</files>
  <read_first>
  - internal/services/email_sender_service.go — 全文(重点 :164-361 三路派发与 :314-407 sendPlainSMTP 会话、:23-43 plainAuth)
  - internal/models/ — EmailConfig model(字段:host/port/username/password/fromAddress/useSSL/useSTARTTLS)+ Notice/NoticePriority 常量
  </read_first>
  <action>
  创建 internal/services/email_sender_service_79_05_test.go(package services)。两个基建:

  - `newEml7905(t) (*EmailSenderService, *gorm.DB)`:sqlite + AutoMigrate(EmailConfig)+ `NewEmailSenderService(db)`。
  - `startFakeSMTP7905(t *testing.T) (addr string, seen *smtpConversation7905)`:file-scoped fake SMTP —— `net.Listen("tcp", "127.0.0.1:0")` + goroutine 内 bufio 读写:`220 fake ESMTP\r\n` → 读命令行,EHLO/HELO 回 `250-fake\r\n250 AUTH PLAIN LOGIN\r\n`,AUTH PLAIN/LOGIN 回 `235 2.7.0 Accepted`,MAIL FROM/RCPT TO 回 `250 OK`,DATA 回 `354 End data with <CR><LF>.<CR><LF>`,读到单独 "." 行后回 `250 OK: queued`,QUIT 回 `221 bye` 并断开。记录会话(mail from/rcpt to/data 字节)到 `seen` 结构供断言。listener/goroutine 一律 `t.Cleanup` 关闭与等待退出(channel 或 WaitGroup),禁泄漏。

  测试前缀 TestEml7905_:

  - `TestEml7905_SendPlainSMTP_Happy`:fake 地址 → sendPlainSMTP 返回 nil 且 seen 记录了正确的 mail from/rcpt/data(含 Subject 头形态)。
  - `TestEml7905_SendPlainSMTP_ServerReject`:fake 服务在 MAIL FROM 回 `550 rejected` → 错误上抛;中途断开连接(先关 listener)→ IO 错误分支。
  - `TestEml7905_Send_HappyViaPlain`:EmailConfig(useSSL=false,useSTARTTLS=false,port=fake 端口)→ Send 返回 *SendResult 成功形态(SendResult 字段按实现断言)。
  - `TestEml7905_Send_ConfigMissingAndDisabled`:configID 不存在 / 配置禁用 → 失败 SendResult(错误文案断言)。
  - `TestEml7905_SendWithDefaultConfig`:无默认配置 → 失败;有默认 → 走 :149 链路(fake plain 配置)。
  - `TestEml7905_SendWithTLS_DialError`(D-79-03):useSSL=true 指向已关闭端口(127.0.0.1:1 或预关 listener)→ 错误分支;注释写明 `// InsecureSkipVerify:false 硬编码(:203-204)→ 自签 fake 不可达,TLS happy path 不覆盖(SUMMARY 记录)`。
  - `TestEml7905_SendWithSTARTTLS_DialError`:同上覆盖 :255 dial-error 分支(同注释)。
  - `TestEml7905_BuildEmailContent`:纯 builder 表驱动 —— from/subject/html+text 双体/空 msg 错误分支(:362-407)。
  - `TestEml7905_PlainAuth`:newPlainAuth → Start 返回 identity/mechanism;Next more=false nil;more=true 返回凭据串(:23-43)。
  - `TestEml7905_NoticeBuilders`:getNoticeTypeLabel/getPriorityClass/getPriorityLabel 表驱动(全部 models.NoticePriority/NoticeType 具名常量)+ buildNoticeHTMLBody 含标题/内容/优先级样式片段(:426-520)。
  - `TestEml7905_SendNoticeEmail_And_TestEmailConfig`:预置 notice + fake SMTP → SendNoticeEmail 成功;TestEmailConfig 走同一链(:408-425/:521-end)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestEml7905_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestEml7905_" internal/services/email_sender_service_79_05_test.go` ≥ 11
  - fake SMTP 会话断言(mail from/rcpt/data)存在;listener/goroutine 经 t.Cleanup 关闭
  - 两条 TLS dial-error 用例各含 InsecureSkipVerify 硬编码行号注释
  - `go test -count=1 -run "TestEml7905_" ./internal/services/` exit 0(Windows 本地亦可跑,纯 TCP loopback)
  </acceptance_criteria>
  <commit>test(79-05): email_sender plain fake SMTP + TLS dial-error + 纯 builder(D-79-03)</commit>
  <done>email_sender_service.go ≥65%;TLS happy path 不可达性已注释锁定。</done>
</task>

<task type="auto">
  <name>Task 7: api_sender(fake HTTP 回环)+ ad_ldap_client Tier-1(D-79-04)</name>
  <files>internal/services/api_sender_ad_ldap_79_05_test.go</files>
  <read_first>
  - internal/services/api_sender_service.go — 全文(:32-420)
  - internal/services/ad_ldap_client.go — 全文(重点 :29/:47/:52/:101/:119/:127 + 15 个 wire ops 的未连接守卫 + :516/:526/:537 纯函数)
  - internal/models/ — APINotificationConfig/ADConfig model
  </read_first>
  <action>
  创建 internal/services/api_sender_ad_ldap_79_05_test.go(package services)。

  api_sender(helper `newApd7905(t) (*APISenderService, *gorm.DB)`,sqlite + AutoMigrate APINotificationConfig;测试前缀 TestApd7905_):
  - fake HTTP 端点选型(确定性规则):先看 APISenderService 是否暴露可注入的 *http.Client 或全局 client 字段 —— 有则用 `httpmock.ActivateNonDefault(client)`(go.mod:17 已有);没有则用 `httptest.NewServer`(research §8 认可的 fallback)。两者择一,SUMMARY 记录选择理由。
  - `TestApd7905_SendRequest_Happy`:config(URL=fake,method/headers/auth 各形态)→ sendRequest 成功 *APISendResult;断言服务端收到的 body/header。
  - `TestApd7905_SendRequest_HTTPError`:fake 回 500 / 连接拒绝(指向已关端口)→ 失败结果与错误文案。
  - `TestApd7905_BuildBody_Trio`:buildRequestBody 模板型/默认型/错误模板(坏占位符按实现)三分支;buildFromTemplate/buildDefaultBody 表驱动(:226-279)。
  - `TestApd7905_Headers_And_Auth`:setRequestHeaders 自定义头合并/默认 UA;setAuthentication bearer/basic/custom-header/无认证四分支(:280-350,直接构造 *http.Request 断言)。
  - `TestApd7905_SendVariants`:Send/SendWithDefaultConfig/SendNoticeAPI/SendSMS/SendWebhook/TestAPIConfig 各走一次 fake 成功与 config 缺失失败(引用 models.APIConfigType 常量)。

  ad_ldap_client(helper `newAdl7905(t) *LDAPClient` = `NewLDAPClient(&models.ADConfig{...最小字段})`;测试前缀 TestAdl7905_):
  - `TestAdl7905_Connect_DialError`:config 的 host 指向 127.0.0.1 上已关闭端口 → Connect 返回 error;IsConnected()==false;Close() 未连接时调用不 panic(:119)。
  - `TestAdl7905_LoadADTLSSkipVerify_EnvMatrix`:os.Setenv 对应变量(名以 :29 实现为准)× {"true","1","false","未设"} → 期望布尔;t.Cleanup 还原 env。
  - `TestAdl7905_FormatUsername`:无域/带域/UPC 形态表驱动(:101-118,以实现注释为准)。
  - `TestAdl7905_PureHelpers`:extractRDN(标准 DN/多段/空)、parseIntOrDefault(合法/非法/空)、encodePassword(已知输入 → 期望字节/十六进制形态断言,utf-16le+引号按 :537 实现)表驱动。
  - `TestAdl7905_WireOps_NotConnected_GuardTable`:表驱动 15 个 wire ops(SearchOUs/SearchGroups/SearchUsers/SearchUsersByOU/SearchGroupMembers/UpdateUserAttribute/UpdateUserMultipleAttributes/UpdateGroupAttribute/MoveUser/EnableUser/DisableUser/UnlockUser/ResetPassword/AddGroupMember/RemoveGroupMember + getUserByDN)在未 Connect 的 client 上逐一调用 → 全部返回 error(守卫分支);断言错误非 nil 且不 panic。注释写明 `// D-79-04:wire 真路径不在本 phase(78-07 Conclusion B:BER fake 不兼容),Tier-1 只收守卫+参数段`。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestApd7905_|TestAdl7905_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestApd7905_\|^func TestAdl7905_" internal/services/api_sender_ad_ldap_79_05_test.go` ≥ 9
  - 15+1 个 wire ops 的未连接守卫全部被调用(grep 方法名计数 ≥ 15)
  - env 矩阵用例 t.Cleanup 还原环境变量
  - `go test -count=1 -run "TestApd7905_|TestAdl7905_" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-05): api_sender fake HTTP 回环 + ad_ldap Tier-1 守卫表(D-79-04)</commit>
  <done>api_sender ≥75%;ad_ldap_client ≥55%(Tier-1 上限),wire 不覆盖已注释锁定。</done>
</task>

<task type="auto">
  <name>Task 8: plan 收口 — 10 文件 per-file 覆盖率 + -race + SUMMARY</name>
  <files>.planning/workstreams/milestone/phases/79-services-root-tail/79-05-SUMMARY.md</files>
  <read_first>
  - .planning/workstreams/milestone/phases/79-services-root-tail/79-RESEARCH.md §2 — 10 文件基线
  </read_first>
  <action>
  收口三步:

  1. `go test -count=1 -coverprofile=cov79_05.out ./internal/services/`
     `go tool cover -func=cov79_05.out | grep -E "mac_history_query_service.go|mac_collection_service.go|mac_history_service.go|mac_history_partition.go|mac_history_matview_service.go|mac_history_heatmap_service.go|mac_perf_config_seed.go|email_sender_service.go|api_sender_service.go|ad_ldap_client.go"`
  2. 判定:mac 六文件 + seed ≥70%;email ≥65%;api_sender ≥75%;ad_ldap ≥55%。未达标回对应 task 补 unc(禁止为达标真建 PG 分区/物化视图或改 TLS 配置)。
  3. `go test -race -count=1 -run "TestMhq7905_|TestMcl7905_|TestEml7905_|TestApd7905_|TestAdl7905_" ./internal/services/` + `go build ./...` + `go test ./...`。

  SUMMARY 落:10 文件实测覆盖率表(基线→实测)、不可达清单(email TLS happy/email :203-204/:271-272;ad_ldap wire ops 深段;mac PG-only DDL;mac_collection collectDeviceMAC → 79-06)、fake SMTP/HTTP 选型记录、滚动增量与包级投影(为 79-06 的 D-79-05 决策提供 gap math)。
  </action>
  <verify>
    <automated>go test -count=1 -coverprofile=cov79_05.out ./internal/services/ && go tool cover -func=cov79_05.out | grep -E "mac_|email_sender_service.go|api_sender_service.go|ad_ldap_client.go" && go test -race -count=1 -run "TestEml7905_|TestApd7905_" ./internal/services/ && go build ./... && go test ./... 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
  - 10 文件 per-file 达各自目标(mac 族 ≥70% / email ≥65% / api_sender ≥75% / ad_ldap ≥55%)(数字落 SUMMARY)
  - `go test ./...` exit 0;`go build ./...` exit 0;-race 抽样 exit 0
  - 生产 .go 改动 = 0 文件;无真实网络外联(全部 127.0.0.1 loopback)
  </acceptance_criteria>
  <commit>docs(79-05): 外呼三件套 + mac 家族 10 文件覆盖率收口落 SUMMARY</commit>
  <done>~1320 unc 簇收口;79-06 的 gap math(包级投影)就绪。</done>
</task>

</tasks>

<notes>
- 依赖:79-01 的 MemoryCache/CacheConfigService 装配(newCcs7901 可复用);本 plan 是 novel fixture 最多的 plan(fake SMTP/fake HTTP/LDAP dial-error/excelize 断言)。
- D-79-03:email 只做 plain fake SMTP + dial-error + 纯 builder;禁改生产 TLS 配置(InsecureSkipVerify 硬编码是安全默认,不许为覆盖率放开)。
- D-79-04:ad_ldap 只做 Tier-1;wire fake(78-07 Conclusion B:原始 BER 与 go-ldap/v3 不兼容)明确不再尝试。
- R6:PG-only(partition/matview)一律 sqlite 跳过分支 + 字符串形态断言;禁真建分区。
- race 安全:fake SMTP/HTTP 的 listener 与 goroutine 全部 t.Cleanup + 退出等待;禁 t.Parallel(loopback 端口与共享 sqlite)。
- Windows 注意:全部走 127.0.0.1 TCP loopback(无 ICMP/UDP);78-04 的 Windows loopback UDP 坑只影响 79-06 的 SNMP 段。
- 跨 plan 归属:mac_collection 的 collectDeviceMAC 与 mac_history_query 的 executor 相关路径归 79-06;本 plan 不测。
</notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 ↔ 数据库 | glebarez sqlite(t.TempDir 文件库),不触碰生产 PG |
| 测试 ↔ 网络 | 仅 127.0.0.1 loopback(fake SMTP/HTTP/已关闭端口);禁任何外网地址字面量 |
| 测试 ↔ 凭据 | SMTP/LDAP 凭据为测试字面量;encodePassword 断言字节形态不入真实口令 |
| 测试 ↔ 生产代码 | 仅新增 *_79_05_test.go;生产 .go 改动 = 0(D-79 escape hatch 除外) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-05-01 | Tampering | fake SMTP 被误配到真实外网地址 | mitigate | listener 一律 net.Listen("tcp","127.0.0.1:0");grep 禁 smtp.qq.com 等外网字面量 |
| T-79-05-02 | DoS | fake server goroutine/listener 泄漏 | mitigate | t.Cleanup Close + WaitGroup 等待退出 + -race 抽样 |
| T-79-05-03 | Information Disclosure | 测试口令/凭据字面量 | accept | 全部形如 "testpass7905",无真实凭据入仓 |
| T-79-05-04 | Tampering | 为覆盖率放开 InsecureSkipVerify | mitigate | D-79-03 明令禁止;验收含"生产 .go 改动 = 0"检查 |
| T-79-05-SC | Tampering | 依赖安装 | accept | 零新增依赖(excelize/httpmock/testify 已在 go.mod) |
</threat_model>

<verification>
- `go test -count=1 ./internal/services/` 全绿;新增用例 `-race` 抽样全绿
- `go tool cover -func=cov79_05.out`:mac 六文件+seed ≥70% / email ≥65% / api_sender ≥75% / ad_ldap ≥55%
- `go build ./...` + `go test ./...` exit 0
- 生产 .go 改动 = 0 文件;既有 mac 系测试不回归
</verification>

<success_criteria>
1. 五个测试文件存在且全绿:TestMhq7905_ ≥14、TestMcl7905_ ≥10、mac 尾部前缀 ≥11、TestEml7905_ ≥11、TestApd7905_/TestAdl7905_ 合计 ≥9
2. mac_history_query_service.go ≥70%(基线 8.0%,#1 缺口文件)、mac_collection_service.go ≥70%(基线 28.5%)
3. email_sender_service.go ≥65%(基线 0%)、api_sender_service.go ≥75%(基线 0%)、ad_ldap_client.go ≥55%(基线 0%)
4. mac_history_service/partition/matview/heatmap/perf_seed 五文件 ≥70%(PG-only 按 R6 口径)
5. `go build ./...` + `go test ./...` exit 0;每 task 一原子 commit
</success_criteria>

<decision_audit>
本 plan 涉及的 phase-79 决策定案(executor 必须遵守):

| ID | 决策 | 依据 / 理由 |
|----|------|------------|
| D-79-03 | email 采用 (a):plain fake SMTP + TLS dial-error 分支 + 纯 builder;TLS 握手 happy path 不覆盖(InsecureSkipVerify:false 硬编码 email_sender_service.go:203-204/:271-272),SUMMARY 记录 | 79-RESEARCH DQ3 推荐 (a);(b) 的 STARTTLS 前缀 garnish 仅在 per-file <65% 时追加,默认不做 |
| D-79-04 | ad_ldap 采用 (a):param-assembly + dial-error + 纯 helper + wire ops 未连接守卫表;wire 真路径不进本 phase | 79-RESEARCH DQ4 推荐 (a) + 78-07 Conclusion B(BER fake 与 go-ldap/v3 不兼容,不再重试);SC-2 floor 是 <50%,≥55% 已 discharge |
| D-79-01 | 本 plan 簇 = research §7 的 79-05 行(外呼三件套 + mac 家族)+ mac_perf_config_seed(自 79-06 移入,mac 族内聚) | ROADMAP 79-05 失实标题作废;D-79-07 微调记录 |
| D-79-06 | 命名 `<source-or-topic>_79_05_test.go` | DQ6 confirmed |
| (沿用) | R6 PG-only 口径;R7 quirk 锁定;装配/串行 wave 纪律沿用 79-01 | 已验证 |

基线数字来源:79-RESEARCH.md §2(2026-08-27 实测)。
</decision_audit>

<output>
Create `.planning/workstreams/milestone/phases/79-services-root-tail/79-05-SUMMARY.md` when done
</output>
