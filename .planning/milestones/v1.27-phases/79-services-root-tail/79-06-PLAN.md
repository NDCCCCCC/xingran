---
phase: 79-services-root-tail
plan: 06
status: pending
type: execute
wave: 6
depends_on: ["79-05"]
files_modified:
  - internal/device/e2e_helpers.go
  - internal/services/device_credential_helper_79_06_test.go
  - internal/services/device_discovery_service_79_06_test.go
  - internal/services/config_backup_service_79_06_test.go
  - internal/services/device_info_collection_service_79_06_test.go
  - internal/services/device_monitor_service_79_06_test.go
  - internal/services/config_execution_command_dispatch_79_06_test.go
  - internal/services/snmp_fake_server_79_06_test.go
  - .planning/coverage-baseline.md
autonomous: true
requirements: [TAIL-01]

must_haves:
  truths:
    - "按 D-79-02:internal/device/e2e_helpers.go 追加 ForTesting 连接池种子 helper(沿用 54 A1 命名隔离契约 + for_testing_guard_test.go AST 守护),为唯一被豁免的生产树改动(测试基建类,零行为变更,INFRA-02 同级先例);production 代码引用即 AST guard fail"
    - "device_discovery_service.go(289 unc)≥60%:纯 IP 数学四函数(calculateIPCount/generateIPList/ipLessEqual/incrementIP)表驱动 + isAlive(net.Listen 真 TCP 对端)+ CRUD sqlite + SNMP 段按 D-79-05(fake 复制;Windows loopback 丢弃则降级 error-path,78-04 先例)"
    - "config_backup_service.go(244 unc)≥65%:纯函数(calculateHash/generateDiff/getDefaultThreshold/备份目录)表驱动 + sqlite 路径(GetBackupList/GetBackupByID/DiffBackups/RestoreBackup/GetBackupStatistics);executor 依赖按 D-79-02 种子或 nil-guard 口径"
    - "device_info_collection_service.go(275 unc)≥65%:既有 17 个 parse 测试之外补 Start/Enqueue/EnqueueAllOnlineDevices/Stop/recoverPendingTasks(t.Cleanup 停机)+ loadConfigFromDB/getCommandsByVendor sqlite + processTask/CollectDeviceInfo(经 D-79-02 种子池或 nil-guard)"
    - "device_monitor_service.go(189 unc)≥65%:Set* 四 setter + nil-guard(:74-99)+ loadConfigFromDB/ReloadConfig/Close + convertSNMPVersion 纯函数 + Collect* 委托 nil 分支"
    - "config_execution_service.go(152)/command_dispatch_service.go(112)≥60%:validate/param-assembly 段 + executor errgroup 派发(经 D-79-02 种子池可达段)+ nil/错误传播分支"
    - "device_credential_helper.go(47)≥70%:helper 函数纯/sqlite 段"
    - "SC-3 收口:`go tool cover -func` 实测 internal/services root ≥70%,数字回填 .planning/coverage-baseline.md 新增『Phase 79 后』段(行 + per-package fenced block + 倒退检查),ratchet 数据链连续;SC-4 gate 全绿(.coverage-threshold 不动,55.5 保持,Phase 81 收口)"
  artifacts:
    - path: "internal/device/e2e_helpers.go"
      provides: "SeedConnectionForTesting(及 executor 装配所需的最小 ForTesting helper),ForTesting 命名契约 + 注释隔离"
      contains: "SeedConnectionForTesting"
    - path: "internal/services/snmp_fake_server_79_06_test.go"
      provides: "进程内 SNMP UDP fake(自 78-04 fake 裁剪移植,_test.go 同包,不可被 import)"
      contains: "127.0.0.1:0"
    - path: "internal/services/device_discovery_service_79_06_test.go"
      provides: "DeviceDiscovery 测试(前缀 TestDdv7906_),IP 数学 + isAlive TCP + CRUD + SNMP"
      min_lines: 380
      contains: "generateIPList"
    - path: "internal/services/config_backup_service_79_06_test.go"
      provides: "ConfigBackup 测试(前缀 TestCbk7906_)"
      min_lines: 300
      contains: "generateDiff"
    - path: "internal/services/device_info_collection_service_79_06_test.go"
      provides: "DeviceInfoCollection 测试(前缀 TestDic7906_)"
      min_lines: 300
      contains: "recoverPendingTasks"
    - path: "internal/services/device_monitor_service_79_06_test.go"
      provides: "DeviceMonitor 测试(前缀 TestDmn7906_)"
      min_lines: 260
      contains: "convertSNMPVersion"
    - path: "internal/services/config_execution_command_dispatch_79_06_test.go"
      provides: "ConfigExecution + CommandDispatch 测试(前缀 TestCex7906_/TestCdp7906_)"
      min_lines: 300
      contains: "errgroup"
    - path: "internal/services/device_credential_helper_79_06_test.go"
      provides: "DeviceCredentialHelper 测试(前缀 TestDch7906_)"
      min_lines: 120
      contains: "Credential"
    - path: ".planning/coverage-baseline.md"
      provides: "『Phase 79 后』ratchet 行 + per-package fenced block + 倒退检查清单"
      contains: "Phase 79 后"
  key_links:
    - from: "internal/services/device_info_collection_service_79_06_test.go"
      to: "DeviceInfoCollectionService.executor 字段(device_info_collection_service.go:38)"
      via: "device.SeedConnectionForTesting 种子池 + scrapligo FileTransport 公共 API 构造 *network.Driver(78-03 手法跨包复用)"
      pattern: "SeedConnectionForTesting|FileTransport"
    - from: "internal/services/snmp_fake_server_79_06_test.go"
      to: "snmpProbe/discoverBySNMP 的 UDP 对端(device_discovery_service.go)"
      via: "自 internal/device/snmp_fake_server_78_04_test.go 裁剪移植的同包 _test.go fake"
      pattern: "snmp_fake_server_79_06"
    - from: ".planning/coverage-baseline.md"
      to: "check-coverage.sh 输出的 per-package 表(.github/scripts/check-coverage.sh)"
      via: "全仓 coverage.out → bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold → 『Phase 79 后』段回填"
      pattern: "internal/services\\s"
---

# Plan 6: device 家族 + 全包复测 + coverage-baseline 回填 (Phase: 79-services-root-tail)

<objective>
交付 internal/services root 收官段:device_credential_helper.go(47 unc)+ device_discovery_service.go(289 unc)+ config_backup_service.go(244 unc)+ device_info_collection_service.go(275 unc)+ device_monitor_service.go(189 unc)+ config_execution_service.go(152 unc)+ command_dispatch_service.go(112 unc),合计 ~1310 unc;随后做 phase 收口 —— 全包复测、`.planning/coverage-baseline.md` 回填(SC-3 ratchet 数据链)、gate 验证(SC-4)。

Purpose: TAIL-01 最后一段。device 家族是唯一含 (a) 类 `*device.DeviceExecutor` 具体依赖的簇,按 D-79-02 以 ForTesting 种子 helper 打开 executor 路径(Tier-2),Tier-1(解析/CRUD/nil-guard)先行;SNMP 段按 D-79-05 复制裁剪 78-04 fake。收口把 6 个 plan 的累计成果落成可审计的 ratchet 行。
Output: 7 个新测试文件 + internal/device/e2e_helpers.go 追加(唯一豁免)+ coverage-baseline.md 回填。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/79-services-root-tail/79-RESEARCH.md
@.planning/workstreams/milestone/phases/79-services-root-tail/79-01-SUMMARY.md
@.planning/workstreams/milestone/phases/79-services-root-tail/79-05-SUMMARY.md

<interfaces>
<!-- 关键契约(planner 已逐一读源码/复核,2026-08-27)。executor 直接使用,无需重新探索。 -->

From internal/device/e2e_helpers.go(现有契约,照抄形态):
```go
func NewPooledConnectionForTesting(d *network.Driver) *PooledConnection // :32(已存在)
// 文件头注释即三层隔离契约:ForTesting 命名后缀 = 物理隔离;无 build tag(A1 要求 e2e 全量跑);
// production 引用 *ForTesting 符号 → for_testing_guard_test.go AST guard fail(file:line 报告)
// 本 plan 追加(形态对齐):func SeedConnectionForTesting(pool *DeviceConnectionPool, deviceID string, conn *PooledConnection)
//   —— 内部 poolLock.Lock() 后写 pool.connections[deviceID]=conn(connection_pool.go:159-171 字段清单)
```

From internal/services/device_discovery_service.go:
```go
// 纯 IP 数学(~80 stmts):calculateIPCount / generateIPList / ipLessEqual / incrementIP(行号以源码为准)
// isAlive:TCP-connect(6 端口 × 1s 超时)—— net.Listen("tcp","127.0.0.1:0") 真 TCP 对端可测;禁 ICMP
// snmpProbe / discoverBySNMP:UDP SNMP → D-79-05 fake;CRUD/扫描任务为 sqlite
```

From internal/services/device_monitor_service.go:
```go
SetExecutor / SetPortCollectionService / SetMACCollectionService / SetConfigBackupService // setter(:74-99 一带 nil-guard)
loadConfigFromDB / ReloadConfig / Close
convertSNMPVersion(纯) // SNMPv1/v2c/v3 映射
CheckDeviceStatus / pingDeviceViaSNMP // 需 UDP fake(D-79-05)
Collect*(委托注入服务) // nil 分支直接可测
```

From internal/services/config_backup_service.go:
```go
// :25 持有 *device.DeviceExecutor
calculateHash / generateDiff / getDefaultThreshold / 备份目录纯函数
GetBackupList / GetBackupByID / DiffBackups / RestoreBackup / GetBackupStatistics // sqlite+文件路径(备份文件落 t.TempDir)
```

From internal/services/device_info_collection_service.go:
```go
// :38 持有 *device.DeviceExecutor;既有 device_info_collection_service_test.go 用 &DeviceInfoCollectionService{} 零值直调 parse(17 tests,勿重写)
Start / Enqueue / EnqueueAllOnlineDevices / worker / Stop // :104 Stop;goroutine 全部 t.Cleanup 停机
recoverPendingTasks / loadConfigFromDB / getCommandsByVendor // sqlite
processTask / CollectDeviceInfo // executor 路径(Tier-2)
// 既有 boards 测试文件 device_info_collection_service_boards_test.go 同样勿重写
```

From internal/services/config_execution_service.go / command_dispatch_service.go:
```go
// config_execution_service.go:19 errgroup fan-out 到 executor;validate/param-assembly 段可纯测
// command_dispatch_service.go:112 stmts,executor dispatch + parse/validate 段
```

跨包可用公共 API(78-03 已验证,internal/services 测试可直接 import):
```go
platform.NewPlatform(..., options.WithTransportType(transport.FileTransport), options.WithFileTransportFile(path)) // scrapligo 公共 API → *network.Driver
device.NewPooledConnectionForTesting(driver) // e2e_helpers.go:32
device.SeedConnectionForTesting(pool, deviceID, conn) // 本 plan Task 1 追加
// ⚠ internal/device 的 78-03/78-04 *_test.go helper(newFTWrapper78/newPool78/seedPool78/snmp fake)是 _test.go 同包文件,不可 import —— 这正是 D-79-02/D-79-05 的由来
```

既有 analog(必读):
- internal/services/device_info_collection_service_test.go:21-51(loadSampleFixture + 零值 struct parse 范本)/ device_info_collection_service_boards_test.go。
- internal/device/scrapli_wrapper_78_03_test.go:40-75 + executor_78_03_test.go:1-70 —— FileTransport fixture 手法(跨包照抄公共 API 部分)。
- internal/device/snmp_fake_server_78_04_test.go —— SNMP fake 裁剪移植源(D-79-05)。
- ROADMAP L209:Windows loopback snmp 跨 socket 丢弃响应 → 78-04 降级 error-path(D-79-05 同款处置)。
- .planning/coverage-baseline.md 表头与既有『Phase 7X 后』段格式(回填模板)。
- .github/scripts/check-coverage.sh 用法:`bash .github/scripts/check-coverage.sh <profile> [<threshold-file>]`,exit 0=gate pass / 1=weighted avg fail / 4=P1 floor / 5=P2 floor。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-79-02 ForTesting 连接池种子 helper(唯一豁免的生产树改动)</name>
  <files>internal/device/e2e_helpers.go, internal/device/e2e_helpers_79_06_test.go</files>
  <read_first>
  - internal/device/e2e_helpers.go — 全文(文件头三层隔离契约注释 + NewPooledConnectionForTesting 形态)
  - internal/device/connection_pool.go — :159-200(DeviceConnectionPool 字段清单 + NewDeviceConnectionPool 构造器签名)
  - internal/device/for_testing_guard_test.go — AST 扫描语义(production 引用 *ForTesting 符号即 fail)
  </read_first>
  <action>
  在 internal/device/e2e_helpers.go 追加(append-only,不改既有函数):

  - `func SeedConnectionForTesting(pool *DeviceConnectionPool, deviceID string, conn *PooledConnection)` —— `pool.poolLock.Lock()` 后 `pool.connections[deviceID] = conn`,Unlock;nil pool/nil conn 直接 no-op。函数注释沿用文件头契约:写明 TEST-ONLY、跳过了哪些生产 bookkeeping(建锁/容量/TOCTOU 占位)、调用方仅限测试、production 引用会被 AST guard 拦截。
  - 若 executor 装配在跨包测试中无法获得已接池的 *DeviceExecutor(以 device.NewDeviceExecutor 实际签名/字段为准,执行时探针),再追加最小 helper `NewDeviceConnectionPoolForTesting(...)` 或 `SetPoolForTesting(exec *DeviceExecutor, pool *DeviceConnectionPool)` 之一 —— **仅限 e2e_helpers.go 一个文件**,同样 ForTesting 命名 + 注释;禁改 connection_pool.go/executor.go 等任何其他生产文件。

  同目录新建 internal/device/e2e_helpers_79_06_test.go(_test.go,不触发 guard):`TestSeedConnectionForTesting7906` 断言种子后 GetConnection/Acquire 路径可见该连接(以 pool 实际查询方法为准)、二次种子覆盖、nil 参数 no-op;`go test ./internal/device/` 全绿证明 guard 不误伤。

  这是本 phase 唯一被 D-79 豁免的生产树改动(D-79-02:测试基建类,零行为变更,INFRA-02 同级先例);除此之外零生产 .go 改动的纪律不变。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestSeedConnectionForTesting7906" ./internal/device/ && go test -count=1 ./internal/device/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "ForTesting" internal/device/e2e_helpers.go` 较改动前净增 ≥1,且新增符号全部以 ForTesting 结尾
  - for_testing_guard_test.go 全绿(production 零引用新增符号)
  - e2e_helpers.go 之外的 internal/device 生产文件 diff = 0
  - `go test ./internal/device/` exit 0;`go build ./...` exit 0
  </acceptance_criteria>
  <commit>test-infra(79-06): device ForTesting 连接池种子 helper(D-79-02,AST 守护内)</commit>
  <done>跨包可种子连接池;guard 全绿;生产树仅 e2e_helpers.go 一个文件被追加。</done>
</task>

<task type="auto">
  <name>Task 2: device_credential_helper + device_discovery Tier-1(IP 数学 + isAlive TCP + CRUD)</name>
  <files>internal/services/device_credential_helper_79_06_test.go, internal/services/device_discovery_service_79_06_test.go</files>
  <read_first>
  - internal/services/device_credential_helper.go — 全文(47 stmts)
  - internal/services/device_discovery_service.go — 纯 IP 数学四函数(calculateIPCount/generateIPList/ipLessEqual/incrementIP)+ isAlive + CRUD/扫描任务段
  - internal/services/discovery_statistics_test.go — 既有统计口径(勿重写)
  </read_first>
  <action>
  创建两个文件(package services)。helper `newDdv7906(t) (*DeviceDiscoveryService, *gorm.DB)`:sqlite(t.TempDir 文件库)+ AutoMigrate(NetworkDevice/DiscoveryTask 等 discovery 链 model)。

  device_credential_helper(前缀 TestDch7906_,helper `newDch7906(t) *gorm.DB`):
  - `TestDch7906_HelperFns`:对照源码 47 stmts 逐 helper 表驱动(凭据解密回退/默认凭证选择/协议参数拼装等,以实现为准;sqlite 依赖则 AutoMigrate NetworkDevice/AuthCredential)。

  device_discovery Tier-1(前缀 TestDdv7906_):
  - `TestDdv7906_CalculateIPCount`:表驱动(/24→256、/32→1、/16→65536、非法掩码分支,按实现)。
  - `TestDdv7906_GenerateIPList`:起始+数量 → 列表顺序与进位跨段(如 192.168.0.254 ×3 → .254/.255/下一段 .0);数量 0/负数分支。
  - `TestDdv7906_IPLessEqual_Increment`:包级直调两函数表驱动(相等/小于/大于;跨段进位/末字节 255 溢出)。
  - `TestDdv7906_IsAlive_TCP`:同包/本用例内 `net.Listen("tcp","127.0.0.1:0")` 起 1-2 个真 TCP listener → isAlive 命中;全关端口 → 不存活(Windows 禁 ICMP,research §8);listener t.Cleanup Close。超时预算:用最小端口集,禁 6 端口 × 全超时的慢用例(若 isAlive 端口集固定,选第一个端口即命中的组合)。
  - `TestDdv7906_DiscoveryCRUD`:扫描任务/结果 CRUD sqlite 主链(Create/List/Get/Delete,按源码方法集);状态字段引用 models.DiscoveryStatus* 具名常量(Phase 69-05 已锁 0-4)。

  SNMP 段(snmpProbe/discoverBySNMP)不在本 task —— Task 7 处理。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestDch7906_|TestDdv7906_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestDch7906_\|^func TestDdv7906_" internal/services/device_credential_helper_79_06_test.go internal/services/device_discovery_service_79_06_test.go` 合计 ≥ 8
  - IP 数学四函数全部表驱动;isAlive 用真 TCP listener(无 ICMP)
  - DiscoveryStatus* 具名常量引用(grep 验证)
  - `go test -count=1 -run "TestDch7906_|TestDdv7906_" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-06): credential helper + discovery Tier-1(IP 数学/isAlive TCP/CRUD)</commit>
  <done>device_discovery 的纯函数与 CRUD 面打开;isAlive 真 TCP 对端可复现。</done>
</task>

<task type="auto">
  <name>Task 3: config_backup(纯函数 + sqlite/文件路径)</name>
  <files>internal/services/config_backup_service_79_06_test.go</files>
  <read_first>
  - internal/services/config_backup_service.go — 全文(:25 executor 字段 + 纯函数族 + sqlite/文件路径)
  - internal/models/ — 备份相关 model(TableName 以源码为准)
  </read_first>
  <action>
  创建 internal/services/config_backup_service_79_06_test.go(package services)。helper `newCbk7906(t) (*ConfigBackupService, *gorm.DB, string)`(sqlite + AutoMigrate 备份 model + 备份目录指 t.TempDir() —— 禁默认相对路径污染工作树,同 78-01 StoragePath 纪律)。测试前缀 TestCbk7906_:

  - `TestCbk7906_CalculateHash`:同内容同 hash/异内容异 hash/空内容(表驱动)。
  - `TestCbk7906_GenerateDiff`:两份配置文本 → diff 形态断言(增/删/无差异空输出,按实现);超长/特殊字符行不断言具体格式只断言非空与含标记。
  - `TestCbk7906_GetDefaultThreshold`:默认值 + 配置覆盖分支(引用配置键常量)。
  - `TestCbk7906_BackupListAndGet`:预置备份行 → GetBackupList 分页/过滤 + GetBackupByID 命中/未命中。
  - `TestCbk7906_DiffBackups`:预置两份落盘备份文件(t.TempDir 真文件)→ DiffBackups 返回差异;文件缺失 → 错误分支。
  - `TestCbk7906_RestoreBackup`:预置备份文件 → Restore 走 seeded executor 路径或按 nil-guard 错误分支(D-79-02 种子可用则走 happy,否则守卫断言 + 注释,以下一 task 的种子基建为准)。
  - `TestCbk7906_BackupStatistics`:预置多状态备份 → 统计计数手算一致。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestCbk7906_" ./internal/services/ && git status --porcelain | grep -v "^??" | grep -v ".planning" ; go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestCbk7906_" internal/services/config_backup_service_79_06_test.go` ≥ 7
  - 备份目录为 t.TempDir()(grep 验证),测试后仓库工作树无新备份文件残留
  - `go test -count=1 -run "TestCbk7906_" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-06): config_backup 纯函数 + sqlite/文件路径(TempDir 隔离)</commit>
  <done>config_backup_service.go 0%→≥65%;diff/hash/restore 主链覆盖。</done>
</task>

<task type="auto">
  <name>Task 4: device_info_collection(worker 生命周期 + sqlite 配置 + processTask Tier-2)</name>
  <files>internal/services/device_info_collection_service_79_06_test.go</files>
  <read_first>
  - internal/services/device_info_collection_service.go — 全文(:38 executor + :104 Stop + Start/Enqueue/worker/recoverPendingTasks/loadConfigFromDB/getCommandsByVendor/processTask/CollectDeviceInfo)
  - internal/services/device_info_collection_service_test.go / _boards_test.go — 既有 17 parse 测试与 loadSampleFixture(勿重写)
  - internal/device/scrapli_wrapper_78_03_test.go:40-75 — FileTransport fixture 手法(公共 API 跨包照抄)
  </read_first>
  <action>
  创建 internal/services/device_info_collection_service_79_06_test.go(package services)。helper `newDic7906(t) (*DeviceInfoCollectionService, *gorm.DB)`:sqlite + AutoMigrate(采集任务/命令模板相关 model)+ 构造时 executor 按可用性注入(Task 1 种子池 or nil)。测试前缀 TestDic7906_:

  - `TestDic7906_Lifecycle_StartEnqueueStop`:Start → Enqueue → 轮询任务状态(require.Eventually,超时 3s)→ Stop;再 Start/Stop 一次(幂等/重入分支按实现);`t.Cleanup(svc.Stop)` 保证 goroutine 全收(QUIRK-P2 前科:goroutine 泄漏,78 记录,本 plan 禁新增泄漏)。
  - `TestDic7906_EnqueueAllOnlineDevices`:预置在线/离线设备(models.DeviceStatus 具名常量)→ 只入队在线;空库 → 0。
  - `TestDic7906_RecoverPendingTasks`:预置 pending 状态任务行 → recoverPendingTasks 重新入队;已完成任务跳过。
  - `TestDic7906_LoadConfigFromDB_GetCommandsByVendor`:配置表/命令模板按 vendor 过滤(models.DeviceVendor 具名常量);空配置 → 默认分支。
  - `TestDic7906_ProcessTask_ExecutorPath`(Tier-2,尽力而为):用 scrapligo FileTransport 公共 API 构造 *network.Driver(fixture 回放文件落 t.TempDir())→ device.NewPooledConnectionForTesting → device.SeedConnectionForTesting 种子池 → processTask 走通一次解析入库(按 78-03 手法);若 executor 装配仍不可达(字段/构造限制),按 D-79 quirk 纪律改断言 nil-guard/错误分支并在 SUMMARY 记录 Tier-2 未达段(勿改 internal/device 其它文件)。
  - `TestDic7906_CollectDeviceInfo_NilGuard`:executor nil / 设备不存在 → 错误分支。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestDic7906_" ./internal/services/ && go test -race -count=1 -run "TestDic7906_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestDic7906_" internal/services/device_info_collection_service_79_06_test.go` ≥ 6
  - Stop 后无 goroutine 泄漏(`-race` 全绿;Cleanup 停机断言)
  - 轮询等待用 require.Eventually(禁裸长 sleep)
  - `go test -race -count=1 -run "TestDic7906_" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-06): device_info_collection 生命周期 + 配置 + processTask(Tier-2 尽力)</commit>
  <done>device_info_collection_service.go 0%→≥65%;goroutine 生命周期全收口。</done>
</task>

<task type="auto">
  <name>Task 5: device_monitor(setter/nil-guard/convertSNMPVersion/Collect 委托)</name>
  <files>internal/services/device_monitor_service_79_06_test.go</files>
  <read_first>
  - internal/services/device_monitor_service.go — 全文(:74-99 setter+nil-guard 带 / loadConfigFromDB/ReloadConfig/Close / convertSNMPVersion / CheckDeviceStatus / Collect* 委托)
  - internal/services/device_monitor_service_79_06_test.go(本 task 新建)+ Task 4 helper 形态参考
  </read_first>
  <action>
  创建 internal/services/device_monitor_service_79_06_test.go(package services)。helper `newDmn7906(t) (*DeviceMonitorService, *gorm.DB)`:sqlite + AutoMigrate(监控配置/设备 model)。测试前缀 TestDmn7906_:

  - `TestDmn7906_Setters_NilGuards`:SetExecutor/SetPortCollectionService/SetMACCollectionService/SetConfigBackupService 注入后字段生效(同包断言);对照 :74-99 逐 nil-guard 分支断言(未注入即触发的错误/降级行为)。
  - `TestDmn7906_ConvertSNMPVersion`:表驱动 v1/v2c/v3 及未知值 → 期望映射(以实现枚举为准)。
  - `TestDmn7906_LoadReloadConfig_Close`:配置表驱动加载 → Reload 后生效;Close 幂等(连调两次不 panic,对照 QUIRK-02 sync.Once 形态;若非幂等按现行为锁定注释)。
  - `TestDmn7906_Collect_Delegates_NilBranches`:Collect* 系列在注入服务为 nil 时 → 错误/跳过分支(逐方法);注入 Task 2-4 的真实 service(同包可构造)→ 委托链走通一次(可观察证据:被委托 service 的表新增/返回值)。
  - `TestDmn7906_CheckDeviceStatus_NilExecutor`:executor nil → 错误分支(SNMP 段归 Task 7)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestDmn7906_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestDmn7906_" internal/services/device_monitor_service_79_06_test.go` ≥ 5
  - 四 setter + nil-guard 逐分支断言;convertSNMPVersion 表驱动
  - `go test -count=1 -run "TestDmn7906_" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-06): device_monitor setter/nil-guard/版本映射/委托链</commit>
  <done>device_monitor_service.go 0%→≥65%(SNMP 探测段留 Task 7)。</done>
</task>

<task type="auto">
  <name>Task 6: config_execution + command_dispatch(validate 段 + executor 派发)</name>
  <files>internal/services/config_execution_command_dispatch_79_06_test.go</files>
  <read_first>
  - internal/services/config_execution_service.go — 全文(:19 errgroup fan-out)
  - internal/services/command_dispatch_service.go — 全文
  - internal/services/command_statistics_test.go — 既有 dispatch 统计口径(勿重写)
  </read_first>
  <action>
  创建 internal/services/config_execution_command_dispatch_79_06_test.go(package services)。helper `newCex7906(t) (*ConfigExecutionService, *gorm.DB, *device.DeviceConnectionPool)`(sqlite + AutoMigrate 执行记录/命令 model;pool 用 device.NewDeviceConnectionPool(db, ...实际签名)+ Task 1 种子)。测试前缀:

  - `TestCex7906_Validate_AndParamAssembly`:请求校验分支表驱动(缺设备/缺命令/非法参数,以 validate 段实现为准)+ 参数拼装纯段(变量替换/转义)断言。
  - `TestCex7906_Execute_NilExecutor`:executor nil / 连接缺失 → 错误分支与执行记录落库形态。
  - `TestCex7906_Execute_SeededPath`(Tier-2 尽力而为):FileTransport fixture 驱动一次执行(78-03 手法);errgroup fan-out 的单设备/多设备(2 设备)与一败俱败/部分失败分支按实现断言;不可达段 SUMMARY 记录(勿改 internal/device 其它文件)。
  - `TestCdp7906_Dispatch_ParseAndRoute`:命令解析/路由表(命令类型 → 处理分支,按 :源码段)表驱动;未知命令 → 错误。
  - `TestCdp7906_Dispatch_ExecutorGuards`:nil executor/设备缺失/超时配置分支;执行统计与既有 command_statistics_test.go 口径一致(只补 unc)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestCex7906_|TestCdp7906_" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestCex7906_\|^func TestCdp7906_" internal/services/config_execution_command_dispatch_79_06_test.go` ≥ 5
  - validate 段表驱动 + nil-executor 错误分支齐备
  - `go test -count=1 -run "TestCex7906_|TestCdp7906_" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-06): config_execution/command_dispatch validate 段 + executor 派发</commit>
  <done>两文件 0%→≥60%;Tier-2 可达段尽力打开并记录边界。</done>
</task>

<task type="auto">
  <name>Task 7: SNMP fake(D-79-05)+ snmpProbe/discoverBySNMP/pingDeviceViaSNMP</name>
  <files>internal/services/snmp_fake_server_79_06_test.go, internal/services/device_discovery_service_79_06_test.go, internal/services/device_monitor_service_79_06_test.go</files>
  <read_first>
  - internal/device/snmp_fake_server_78_04_test.go — 裁剪移植源(同包 _test.go,不可 import)
  - internal/services/device_discovery_service.go — snmpProbe/discoverBySNMP 段
  - internal/services/device_monitor_service.go — pingDeviceViaSNMP/CheckDeviceStatus 段
  </read_first>
  <action>
  创建 internal/services/snmp_fake_server_79_06_test.go(package services,test-only):自 internal/device/snmp_fake_server_78_04_test.go 裁剪移植 —— 只保留 snmpProbe/discoverBySNMP/pingDeviceViaSNMP 实际请求的 OID 子集;`net.ListenUDP("udp", &net.UDPAddr{IP: net.ParseIP("127.0.0.1"), Port: 0})` + 读请求按 OID 回预编码 response(78-04 的 BER 编码手法照抄);conn/goroutine 全部 t.Cleanup。文件头注释写明 `// copied/trimmed from internal/device/snmp_fake_server_78_04_test.go (in-package test-only, not importable) — D-79-05`。

  追加用例:
  - `TestDdv7906_SnmpProbe_Happy`:fake 就绪 → snmpProbe 命中 OID 断言解析值(discoverBySNMP 若依赖多 OID 组合,覆盖可达子集)。
  - `TestDdv7906_SnmpProbe_Timeout`:无 fake/黑洞端口 → 超时错误分支(Windows/CI 都稳定,超时阈值用源码默认,禁长等待)。
  - `TestDmn7906_PingDeviceViaSNMP_HappyAndTimeout`:fake 命中 / 黑洞两分支。
  - Windows loopback 处置(D-79-05,78-04 先例):若本机复现"跨 socket 丢弃响应"(happy 断言不稳定),把该 happy 用例降级为 error-path 断言并加注释 `// Windows loopback UDP cross-socket discard (78-04 precedent) — error-path locked, happy path documented in SUMMARY`;禁为稳定性改生产 SNMP 代码或加真 sleep 重试循环。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestDdv7906_Snmp|TestDmn7906_PingDeviceViaSNMP" ./internal/services/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "78_04" internal/services/snmp_fake_server_79_06_test.go` ≥ 1(溯源注释)
  - UDP conn/goroutine 经 t.Cleanup 关闭;-race 抽样全绿
  - happy/timeout 双分支存在(或按 Windows 降级口径落注释 + SUMMARY)
  - `go test -count=1 -run "TestDdv7906_Snmp|TestDmn7906_PingDeviceViaSNMP" ./internal/services/` exit 0
  </acceptance_criteria>
  <commit>test(79-06): SNMP fake 移植 + snmpProbe/pingDeviceViaSNMP(D-79-05)</commit>
  <done>device family SNMP 段收口;Windows loopback 处置按 78-04 先例落档。</done>
</task>

<task type="auto">
  <name>Task 8: phase 收口 — 全包复测 + coverage-baseline.md 回填(SC-3)+ gate 验证(SC-4)</name>
  <files>.planning/coverage-baseline.md, .planning/workstreams/milestone/phases/79-services-root-tail/79-06-SUMMARY.md</files>
  <read_first>
  - .planning/coverage-baseline.md — 表头(8 列)+ 既有『Phase 7X 后』段格式(行 + per-package fenced block + 倒退检查)
  - .github/scripts/check-coverage.sh — 用法与 exit 语义(0/1/2/4/5)
  - 79-01..79-05 各 SUMMARY — 滚动增量核对
  </read_first>
  <action>
  phase 收口四步(SC-3/SC-4):

  1. 全包复测(root 包口径):
     `go test -count=1 -coverprofile=cov79_06_root.out ./internal/services/`
     `go tool cover -func=cov79_06_root.out | tail -1`(total)
     `go tool cover -func=cov79_06_root.out | grep -E "device_|config_backup|config_execution|command_dispatch"`(device 家族 7 文件达标判定:≥60-70% 按 must_haves 口径)
     全包 `-race` 一次:`go test -race -count=1 ./internal/services/`(R4 允许,phase 收口唯一一次)。
  2. 全仓 profile + gate:
     `go test -count=1 -coverprofile=coverage.out ./internal/... ./pkg/... ./cmd/...`(73-05 收口同款)
     `bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold` → 断言 exit 0(SC-4;.coverage-threshold 保持 55.5 不动,Phase 81 才 bump)。
  3. coverage-baseline.md 回填(D-07/D-11 同款原子段):
     追加 `## Phase 79 后` 段 —— 8 列行(date/phase_label/weighted_avg/total_stmts/total_covered/0pct_pkg_count/commit/phase_executor/ratchet_from/ratchet_to 以文件实际表头为准)+ `### Per-package (Phase 79 后)` fenced block(从 check-coverage.sh 输出原样拷贝,`internal/services` 行应显示 ≥70%)+ `### Per-package 倒退检查 (Phase 79 后)` 清单(无新增 0% 包/无 P1 包回退)。
     commit 列若当时无法定 SHA 先填 TBD 并在 SUMMARY 标注(71-01b 先例允许 amend)。
  4. device 家族收尾判定与 SUMMARY:7 文件实测覆盖率表(基线→实测)、D-79-02 豁免改动清单(仅 e2e_helpers.go)、D-79-05 处置记录、Tier-2 未达段清单(如有)、phase 级 SC-1..SC-4 逐条判定表、D-79-01..07 决策执行情况汇总。

  收口后 `git status --porcelain` 应只剩已提交内容;cov*.out 临时文件不入仓(gitignore 已覆盖或删除)。
  </action>
  <verify>
    <automated>go test -count=1 -coverprofile=cov79_06_root.out ./internal/services/ && go tool cover -func=cov79_06_root.out | tail -1 && go test -race -count=1 ./internal/services/ && go build ./... && go test ./... 2>&1 | tail -5 && bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold && echo GATE-PASS</automated>
  </verify>
  <acceptance_criteria>
  - internal/services root 包 total ≥70%(SC-1 实测数字落 SUMMARY + baseline 行)
  - device 家族 7 文件全部达 must_haves 各自目标(≥60-70%)
  - `bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold` exit 0(SC-4);`.coverage-threshold` 文件 diff = 0
  - `.planning/coverage-baseline.md` 新增 Phase 79 后段,per-package block 与 gate 输出一致;`go test -race ./internal/services/` exit 0
  - `go test ./...` + `go build ./...` exit 0;仓库无未提交 *_test.go / cov*.out 残留
  </acceptance_criteria>
  <commit>docs(79-06): phase 79 收口 — device 家族达标 + coverage-baseline Phase 79 后回填 + gate 全绿</commit>
  <done>TAIL-01 收口:internal/services root ≥70% 实测落档,ratchet 数据链连续,gate 全绿,SC-1..SC-4 全判定。</done>
</task>

</tasks>

<notes>
- 依赖:79-05 SUMMARY 的包级投影是本 plan 的 gap math 输入;Task 1 的 helper 是 Task 4/6 Tier-2 段的前置(故排首)。
- D-79-02 边界:生产树只允许 internal/device/e2e_helpers.go append-only;for_testing_guard_test.go 是守护而非生产逻辑,允许随契约同步(仅当新增符号需要时)。禁改 connection_pool.go/executor.go。
- D-79-05 边界:SNMP fake 只进 _test.go;Windows loopback 丢弃响应按 78-04 先例降级 error-path,零生产改动。
- race 安全(最高风险 plan):device_info_collection worker goroutine / SNMP UDP reader / executor pool cleanup goroutine 全部 t.Cleanup 停机(QUIRK-P2 前科);等待一律 require.Eventually,禁裸长 sleep;禁 t.Parallel。
- scopping 纪律:本 plan 若 Tier-2(FileTransport 执行链)在某文件不可达,按 must_haves 的 ≥60% 下限 + SUMMARY 记录处置;包级 ≥70% 由前 5 个 plan 的余量兜底(滚动增量核对在 Task 8)。
- flake 防线:isAlive/SNMP 全部 127.0.0.1 loopback + 显式超时;日期断言同 79-02 纪律。
</notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 ↔ 数据库 | glebarez sqlite(t.TempDir 文件库),不触碰生产 PG |
| 测试 ↔ 设备连接 | FileTransport fixture 回放 + 127.0.0.1 UDP/TCP loopback;禁真实网络设备地址 |
| 测试 ↔ 生产代码 | internal/device/e2e_helpers.go append-only(D-79-02 唯一豁免,AST 守护);其余零生产改动 |
| 测试 ↔ 文件系统 | 备份/fixture 全落 t.TempDir;禁默认相对路径污染工作树 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-06-01 | Elevation of Privilege | ForTesting helper 被生产代码调用绕过池记账/凭据解密 | mitigate | ForTesting 命名契约 + for_testing_guard_test.go AST 守护(fail with file:line)+ Task 1 验收 grep |
| T-79-06-02 | DoS | worker/UDP/SNMP goroutine 泄漏拖垮 CI | mitigate | t.Cleanup 全收 + require.Eventually + 收口全包 -race(QUIRK-P2 前科防线) |
| T-79-06-03 | Tampering | 为覆盖率改 SNMP/TLS 生产逻辑 | mitigate | D-79-05 降级口径 + "生产 .go 改动 = e2e_helpers.go 之外 0 文件"验收 |
| T-79-06-04 | Information Disclosure | SNMP community/设备凭据测试值 | accept | 全部测试字面量("public"/"testpass7906"),无真实凭据入仓 |
| T-79-06-05 | Tampering | coverage-baseline 回填数字与实测不符 | mitigate | per-package block 原样拷贝 check-coverage.sh 输出;SUMMARY 附原始命令与 total 行 |
| T-79-06-SC | Tampering | 依赖安装 | accept | 零新增依赖(scrapligo/excelize/testify 已有) |
</threat_model>

<verification>
- `go test -count=1 ./internal/services/` 全绿;`go test -race -count=1 ./internal/services/` 全绿(收口一次)
- `go tool cover -func`:internal/services root total ≥70%(SC-1/SC-3);device 家族 7 文件达各自目标
- `bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold` exit 0(SC-4);`.coverage-threshold` diff = 0
- `.planning/coverage-baseline.md` 含 Phase 79 后段(行 + per-package block + 倒退检查)
- `go build ./...` + `go test ./...` exit 0;生产 .go 改动仅 internal/device/e2e_helpers.go(D-79-02)
</verification>

<success_criteria>
1. 7 个新测试文件 + e2e_helpers.go 追加存在且全绿:TestDch7906_/TestDdv7906_ ≥8、TestCbk7906_ ≥7、TestDic7906_ ≥6、TestDmn7906_ ≥5、TestCex7906_/TestCdp7906_ ≥5、SNMP 双分支
2. device_discovery ≥60%、config_backup ≥65%、device_info_collection ≥65%、device_monitor ≥65%、config_execution/command_dispatch ≥60%、device_credential_helper ≥70%
3. internal/services root 包 ≥70%(5202 stmts 基线 11.3% → 收口实测),数字落 SUMMARY + coverage-baseline.md『Phase 79 后』行
4. D-79-02 豁免仅 e2e_helpers.go 且 AST 守护全绿;D-79-05 SNMP fake 同包 test-only + Windows 处置落档
5. gate 全绿(exit 0),`.coverage-threshold` 未动;`go build ./...` + `go test ./...` exit 0;每 task 一原子 commit
</success_criteria>

<decision_audit>
本 plan 涉及的 phase-79 决策定案(executor 必须遵守):

| ID | 决策 | 依据 / 理由 |
|----|------|------------|
| D-79-02 | 批准 ForTesting 连接池种子 helper:internal/device/e2e_helpers.go append-only(`SeedConnectionForTesting` + 必要时 `NewDeviceConnectionPoolForTesting`/`SetPoolForTesting` 择一),ForTesting 命名契约 + AST 守护;Tier-1 先行,Tier-2 尽力而为且不可达段落 SUMMARY | 79-RESEARCH DQ2 推荐(approve,mirrors INFRA-02);包级 70% 数学的余量保险(R2);生产树改动被收敛到单文件 + 命名隔离 + AST 守护三层防线。executor 路径深入段(设备命令真实执行全链)若 FileTransport 不可达,按 ≥60% 下限 + SUMMARY 记录,禁改其它生产文件 |
| D-79-05 | SNMP fake 采用 (a):自 78-04 fake 裁剪移植为同包 `_test.go`(snmp_fake_server_79_06_test.go);Windows loopback 跨 socket 丢弃复现时降级 error-path(78-04 先例),零生产改动 | 79-RESEARCH DQ5 的"<69% 则 (a)"条件由 79-05 SUMMARY 投影预判满足(device 家族需要 SNMP 段);落地时仍按实际稳定性执行降级条款 |
| D-79-01 | 本 plan 簇 = research §7 的 79-06 行(device 家族)+ phase 收口;mac_perf_config_seed 已移 79-05,device_credential_helper 归入本 plan | ROADMAP 79-06 标题本身正确(research 同款),仅微调单文件归属 |
| D-79-06 | 命名 `<source-or-topic>_79_06_test.go` | DQ6 confirmed |
| (沿用) | SC-3 回填 = 73-05 收口同款(全仓 profile + gate + baseline 段);.coverage-threshold 不动(Phase 81) | ROADMAP SC-4 + 71-01b amend 先例 |
| (沿用) | QUIRK-P2(DeviceConnectionPool goroutine 泄漏)在测试侧以 t.Cleanup 全收,不修生产代码 | R7 + 78 留档 |

基线数字来源:79-RESEARCH.md §2(2026-08-27 实测)+ 79-05 SUMMARY 滚动投影。
</decision_audit>

<output>
Create `.planning/workstreams/milestone/phases/79-services-root-tail/79-06-SUMMARY.md` when done
</output>
