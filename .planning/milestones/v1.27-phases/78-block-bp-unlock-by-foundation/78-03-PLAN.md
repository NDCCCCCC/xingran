---
phase: 78-block-bp-unlock-by-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/device/scrapli_wrapper_78_03_test.go
  - internal/device/connection_pool_78_03_test.go
  - internal/device/executor_78_03_test.go
  - internal/device/testdata/huawei_vrp_ops.fixture
  - internal/device/testdata/huawei_vrp_pool.fixture
autonomous: true
requirements: [BLOCK-04]

must_haves:
  truths:
    - "本 plan 交付 device 三大件测试:scrapli_wrapper.go(基线实测多函数 0%:GetPrompt/OpenContext/Close/IsConnected/IsReady/WaitForReady/SendCommands/SendConfig/SendConfigs/GetConfig/GetResponse/isClosing)+ connection_pool.go(Release/removeConnection 0%,Execute 18.2%,GetConnection 35.6%,createConnection 6.4%)+ executor.go(ExecuteMultipleOnDevice/ExecuteCustom/executeWithRetry/GetConfig 全 0%),全部经 INFRA-02 的 `newNetworkDriver` 包级 var-seam(scrapli_wrapper.go:115)+ FileTransport fixture 回放,零真实 SSH / 零真实设备"
    - "先例照搬:`internal/device/driver_factory_76_02_test.go`:60-115 已实证 `newNetworkDriver` 覆盖 → NewScrapliWrapper → Open → SendCommand 全链 fixture 回放(fixture `testdata/huawei_vrp_open.fixture` 格式 = 命令回显行 + 输出行 + `<dummy-host>` prompt 行);`internal/services/portwrite/port_write_e2e_test.go` 是跨包先例。本 plan 只扩 fixture 与用例,不改 seam"
    - "按 D-78-05 关键路径(planner 新增决策,替代 78-RESEARCH DQ5 两选项):**不给 createConnection 加新 seam**,改用「同包直接预置 `pool.connections[deviceID]` 一条 ForTesting PooledConnection」走 GetConnection 的**复用路径**(connection_pool.go:244-262,`pc.wrapper.IsReady()` 经 FileTransport 满足)—— 一举打通 GetConnection / task_scheduler.executeTask / executor.ExecuteMultipleOnDevice / ExecuteCustom / GetConfig 全链,且生产代码零改动;createConnection 仅覆盖 DB/凭据错误早退分支(设备不存在 / 凭证不存在 / passwordCipher nil),SSH 握手段接受不覆盖并落 SUMMARY"
    - "按 D-78-09 var 注入纪律(driver_factory_76_02_test.go 文件头已立规,逐条照搬):禁 t.Parallel();覆盖 `newNetworkDriver` 前必先存 orig 并 `t.Cleanup` 恢复;覆盖之后禁用 `t.Fatal`(改 `t.Errorf` + early return),防 Cleanup 未执行导致同包后续测试拿到 FileTransport 工厂"
    - "FileTransport 消耗语义(S-2/R2 + 76-02 文件头注释):fixture 是**一次性字节流**,每个 driver 操作(Open on-open 命令 / SendCommand / GetPrompt / IsReady)都会顺序消费;fixture 耗尽后 `FileTransport.Read` 会 `select{}` 永久阻塞。因此(a) 每个用例必须为其操作序列配足 prompt/回显行,(b) 一律**不调 driver.Close()/wrapper.Close() 于 FileTransport 驱动的用例**(network-on-close 会继续读字节 → hang),wrapper.Close 的覆盖改用 `newScrapliWrapperForTesting(nil)`(driver=nil 分支)与状态机用例"
    - "按 D-78-08 命名(`<source>_78_03_test.go`);按 D-78-10 quirk 纪律;按 D-78-11 wave 编排(本 plan wave 1,与 78-01/78-05 跨包并行;同包 78-04 排 wave 2 并复用本 plan 的 fixture 与池预置 helper)"
    - "达标口径(2026-08-27 本地实测基线 internal/device 43.5%):scrapli_wrapper.go ≥80%、executor.go ≥70%、connection_pool.go ≥70%,`go build ./...` + `go test ./...` 全绿,`go test -race ./internal/device/` 全绿"
    - "本 plan 裁决细则(Decision Audit 逐条依据,executor 必须遵守):D-78-03a(FileTransport 驱动的用例一律不调 wrapper.Close()/driver.Close(),network-on-close 会在 fixture 耗尽后永久阻塞)/ D-78-03b(removeConnection 的 30s 与 Close 的 10s 超时分支接受不覆盖,SUMMARY 记剩余 stmts)/ D-78-03c(Release 负计数 panic 与 EnablePanicRecovery=false 的 panic 逃逸均按现行为 require.Panics 断言,是源码注释明示的故意不变量,不当 quirk 修)/ D-78-03d(SendConfig/SendConfigs 的 privilege fixture 推演受阻时降级为错误分支覆盖 + SUMMARY 记录)"
  artifacts:
    - path: "internal/device/scrapli_wrapper_78_03_test.go"
      provides: "ScrapliWrapper 操作全家桶 + OpenContext/状态机/Close 测试(前缀 TestSW78_)"
      min_lines: 380
      contains: "newNetworkDriver"
    - path: "internal/device/connection_pool_78_03_test.go"
      provides: "PooledConnection 引用计数/Execute + 池 GetConnection 复用/LRU/清理/Close 测试(前缀 TestCP78_)+ 池预置 helper"
      min_lines: 340
      contains: "func seedPool78"
    - path: "internal/device/executor_78_03_test.go"
      provides: "DeviceExecutor 四函数测试(前缀 TestEX78_),含 executeWithRetry 直喂 conn 与经 scheduler 的三入口"
      min_lines: 260
      contains: "NewPooledConnectionForTesting"
    - path: "internal/device/testdata/huawei_vrp_ops.fixture"
      provides: "多命令/多配置操作序列的 fixture 字节流(SendCommands/SendConfig/GetConfig/GetPrompt 用)"
      min_lines: 20
      contains: "<dummy-host>"
  key_links:
    - from: "internal/device/scrapli_wrapper_78_03_test.go"
      to: "newNetworkDriver (scrapli_wrapper.go:115)"
      via: "包级 var 覆盖为 FileTransport 工厂(orig + t.Cleanup 恢复)"
      pattern: "newNetworkDriver = func"
    - from: "internal/device/connection_pool_78_03_test.go"
      to: "GetConnection 复用路径 (connection_pool.go:244-262)"
      via: "同包直写 pool.connections[deviceID] = ForTesting PooledConnection(mu = pool.getDeviceLock(id))"
      pattern: "p\\.connections\\["
    - from: "internal/device/executor_78_03_test.go"
      to: "executeWithRetry (executor.go:201)"
      via: "NewPooledConnectionForTesting(FileTransport driver) 直喂 conn 参数,绕开 Submit"
      pattern: "executeWithRetry"
---

<objective>
交付 internal/device 的 scrapli 主链测试:利用 Phase 76 INFRA-02 已落地的 `newNetworkDriver` var-seam(scrapli_wrapper.go:115)与 FileTransport fixture 回放先例(driver_factory_76_02_test.go:60-115),把 scrapli_wrapper + connection_pool + executor 三文件共 ~510 unc stmts 解包,并用「池内预置 PooledConnection 走 GetConnection 复用路径」这一零生产改动的手法打通 executor 的三个 Submit 入口。

Purpose: BLOCK-04(internal/device ≥70%)的主力段。device 43.5% → 70% 的绝大部分缺口在这三个文件,snmp + task_scheduler 由 78-04 收口。
Output: 新增 3 个测试文件 + 2 个 fixture(零生产代码改动,除非触发 D-78-10 quirk 修复)。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/78-block-bp-unlock-by-foundation/78-RESEARCH.md

<interfaces>
<!-- 关键契约(planner 已读源码复核行号,2026-08-27)。 -->

INFRA-02 var-seam(scrapli_wrapper.go:115,**已存在,禁改**):
```go
var newNetworkDriver = func(platformName interface{}, host string, opts ...util.Option) (*network.Driver, error)
```
注入模板(driver_factory_76_02_test.go:66-90 原样照搬,含 5 个 option):
```go
orig := newNetworkDriver
t.Cleanup(func() { newNetworkDriver = orig })
newNetworkDriver = func(_ interface{}, _ string, _ ...util.Option) (*network.Driver, error) {
    p, err := platform.NewPlatform("huawei_vrp", "dummy-host",
        options.WithTransportType(transport.FileTransport),
        options.WithFileTransportFile(fixturePath),
        options.WithTransportReadSize(1),
        options.WithReadDelay(0))
    if err != nil { return nil, err }
    return p.GetNetworkDriver()
}
```
fixture 路径解析:照搬 `factoryFixturePath(t, name)`(driver_factory_76_02_test.go:47-55,`runtime.Caller(0)` + `filepath.Join` —— S-5 Windows 路径先例);**该 helper 已在同包存在,直接调用,勿重复定义**。

既有 fixture 格式(`testdata/huawei_vrp_open.fixture`,7 行):
```
<dummy-host>                 ← Open 读首个 prompt(exec pattern ^<[\w.\-@/:]{1,63}>$)
screen-length 0 temporary    ← on-open send-command 的回显
<dummy-host>                 ← prompt
display version              ← SendCommand 回显
Huawei Versatile Routing Platform Software
VRP (R) software, Version 8.180 (dummy build for fixture replay)
<dummy-host>                 ← prompt
```
**每个后续操作都要按「回显行 + 输出行(可选)+ prompt 行」追加**;GetPrompt/IsReady 各消费一个 prompt 行。

e2e_helpers.go(测试专用工厂,**已存在**):
```go
// :32 跨包用
func NewPooledConnectionForTesting(d *network.Driver) *PooledConnection   // wrapper=ForTesting, refCount=1, deviceID="e2e-dummy-device", mu=&sync.Mutex{}, pool=nil
// :50 同包私有
func newScrapliWrapperForTesting(d *network.Driver) *ScrapliWrapper       // state=StateReady(让 acquireOp 通过), device=nil!
```
⚠️ `device == nil` 陷阱:`GetConfig()`(scrapli_wrapper.go:640)读 `w.device.Vendor`、`Open()`(:304)错误分支读 `w.device.IPAddress` → **测这些函数前必须同包直写 `w.device = &models.NetworkDevice{Vendor: models.VendorHuawei, IPAddress: "dummy-host"}`**,或直接走 `NewScrapliWrapper(dev, ...)` 公开构造器(device 已装配)。

connection_pool.go 关键语义:
```go
// :82  Release()    —— atomic -1 + pc.mu.Unlock()(配对 Acquire;负计数 panic)
// :102 ReleaseRef() —— 仅 atomic -1,不动 mu(配对 GetConnection)
// :111 Execute()    —— Acquire(内含 IsConnected + WaitForReady(5s) + IsReady) → fn(wrapper) → defer Release()
// :236 GetConnection —— !IsEnabled 早退 / 复用路径(:250 pc.wrapper.IsReady() 真 → refCount+1 直接返回) /
//                       失效路径(removeConnection) / 新建路径(容量检查 + LRU 退让 + connecting 占位 + createConnection)
// :394 createConnection —— db 查 NetworkDevice → 查 AuthCredential(CredentialID 或 is_default) → passwordCipher.Decrypt → NewScrapliWrapper(真 SSH)
// :486 removeConnection —— 等 IsIdle(ticker 100ms / timeout 30s)→ wrapper.Close → delete
// :533 cleanupIdleConnections —— now-lastUsed > maxIdle && IsIdle → Close + delete
// :596 Close() —— close(done) + ticker 等 IsIdle(每连接 2s idleCheck / 整体 10s)
```
`NewDeviceConnectionPool` 构造即启 startCleanup goroutine(ticker 1min)→ **每个建池用例必须 t.Cleanup(pool.Close())**。

executor.go 关键语义:
```go
// :52  ExecuteOnDevice(65%)      Submit + ticker 轮询 taskErr/result
// :101 ExecuteMultipleOnDevice(0%) 同上,循环命令
// :161 ExecuteCustom(0%)         Submit + Callback close(done) + select
// :201 executeWithRetry(0%)      **conn 直参**:conn.wrapper.SendCommand;attempt 循环 / RetryDelay / ctx.Done / panic 恢复 / EnablePanicRecovery=false
// :267 GetConfig(0%)             pool.GetDevice(deviceID) → 按 Vendor 选命令 → Submit
```
`DeviceTask.Execute func(ctx, conn *PooledConnection) error`(task_scheduler.go:25)、`executeTask`(:189)内 `s.connectionPool.GetConnection(...)` → 这是 D-78-05 预置池手法的作用点。

sqlite 装配(createConnection 错误分支与 GetDevice 用):glebarez sqlite + 手动 CREATE TABLE `network_devices` / `auth_credentials`(列集以 models.NetworkDevice / models.AuthCredential 实际引用列为准);状态/厂商断言引用 `internal/models` 具名常量(models.VendorHuawei / models.ProtocolTypeSSH 等),禁裸字面量。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: fixture 扩容 + wrapper 操作全家桶(SendCommands/SendConfig(s)/GetConfig/GetResponse/GetPrompt)</name>
  <files>internal/device/testdata/huawei_vrp_ops.fixture, internal/device/scrapli_wrapper_78_03_test.go</files>
  <read_first>
  - internal/device/driver_factory_76_02_test.go — 全文(文件头 6 条注入纪律 + factoryFixturePath + 注入模板 + "不调 Close" 的原因)
  - internal/device/scrapli_wrapper.go — :530-694(SendCommand/SendCommands/SendConfig/SendConfigs/GetConfig/GetResponse)+ :297 GetPrompt + :266 acquireOp
  - internal/device/testdata/huawei_vrp_open.fixture — 现有 7 行格式
  - internal/services/portwrite/port_write_e2e_test.go — :42-48 / :71-90 / :106-113(FileTransport 阻塞语义与 option 组合的跨包先例)
  </read_first>
  <action>
  第一步:新建 `internal/device/testdata/huawei_vrp_ops.fixture` —— 在 huawei_vrp_open.fixture 的 3 行 Open 序列基础上,按「回显 + 输出 + prompt」追加足够的操作字节:
  - `display version` × 1(带 2 行输出)
  - `display device` / `display interface brief` 各 1(SendCommands 的两条)
  - `display current-configuration`(GetConfig 用,Vendor=huawei 时的命令,输出 2-3 行配置文本)
  - 额外 3-4 个孤立 prompt 行(供 GetPrompt / IsReady / GetResponse 消费)
  - SendConfig/SendConfigs 的 config 模式序列(scrapligo 的 `driver.SendConfig` 会先 acquire-priv 进入 system-view:必须按 huawei_vrp 平台 yaml 的 privilege 提升序列补 `system-view` 回显 + `[dummy-host]` 形态 prompt,再补 config 命令回显 + prompt)。
  ⚠️ 此处是全 plan 唯一"字节流推演"风险点:**先写最小 fixture 跑通 SendCommands,再逐条追加**,每加一段跑一次测试;若 SendConfig 的 privilege 序列反复对不上,按 D-78-10 降级 —— SendConfig/SendConfigs 改用"driver=nil 时 acquireOp 报错"的错误分支覆盖 + SUMMARY 记录 fixture 推演受阻的确切现象,不得为覆盖率删减生产逻辑。

  第二步:创建 `internal/device/scrapli_wrapper_78_03_test.go`(package device)。文件头照搬 driver_factory_76_02_test.go 的 6 条注入纪律注释。helper `newSW78(t, fixture string) *ScrapliWrapper`:注入 newNetworkDriver(orig + t.Cleanup 恢复)→ `NewScrapliWrapper(&models.NetworkDevice{DeviceName:"dummy-fixture-device", IPAddress:"dummy-host", Vendor: models.VendorHuawei}, "u", "p", models.ProtocolTypeSSH)` → `w.Open()` → 返回 w(**不注册 Close**,见 truths 的 FileTransport 消耗语义)。

  测试函数前缀 TestSW78_(**覆盖 var 之后一律 t.Errorf + return,禁 t.Fatal**):
  - `TestSW78_SendCommands`:两条命令 → 断言返回 2 个 Response、Result 非空、`ElapsedTime() >= 0`;再造一个"第二条命令因 fixture 耗尽而失败"的用例断言"返回已成功的部分 responses + error"(:563-586 的部分成功语义)。
  - `TestSW78_GetConfig_VendorSwitch`:Vendor=huawei → `display current-configuration`(fixture 命中);再用 `newScrapliWrapperForTesting(d)` + 直写 `w.device = &models.NetworkDevice{Vendor: models.VendorRuijie}` 断言选中 `show running-config` 分支(此形态下 fixture 无对应回显 → 断言 error 亦可,重点是 switch 四分支都被走到:huawei/h3c/ruijie/maipu/default,厂商常量一律引用 models.Vendor*)。
  - `TestSW78_GetResponse_And_GetPrompt`:GetPrompt 返回 `<dummy-host>` 形态;GetResponse 返回同一 prompt;acquireOp 失败时(state=StateClosed)GetResponse 返回空串(:665-694)。
  - `TestSW78_SendConfig_And_SendConfigs`:按第一步 fixture 结果实现(或按降级口径覆盖错误分支)。
  - `TestSW78_AcquireOp_ErrorBranches`:`newScrapliWrapperForTesting(nil)` → driver==nil 分支报"设备未连接";`setState(StateClosing)` → 报"连接不可用 (当前状态: ...)";并断言 releaseOp 与 acquireOp 配对不 panic(:266-295)。
  - `TestSW78_SendCommand_ConnectionErrorMarksClosed`:构造 SendCommand 返回 EOF/连接类错误的形态(fixture 耗尽或 driver=nil 前置)→ 断言 `containsEOF`/`containsConnectionError` 命中后 state 被置 StateClosed(:544-552)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestSW78_" -timeout 180s ./internal/device/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `internal/device/testdata/huawei_vrp_ops.fixture` 存在且含 ≥2 个 `<dummy-host>` prompt 行
  - `grep -c "^func TestSW78_" internal/device/scrapli_wrapper_78_03_test.go` ≥ 6
  - 文件含 `orig := newNetworkDriver` 与 `t.Cleanup(func() { newNetworkDriver = orig })`;覆盖点之后无 `t.Fatal`(grep 校验)
  - 测试文件不含 `t.Parallel()`;FileTransport 驱动的用例不调用 `w.Close()`/`d.Close()`
  - `go test -count=1 -run "TestSW78_" -timeout 180s ./internal/device/` exit 0(**无 hang**)
  </acceptance_criteria>
  <done>wrapper 操作全家桶(SendCommands/SendConfig(s)/GetConfig/GetResponse/GetPrompt/acquireOp 错误态)有测试;ops fixture 沉淀供 78-04 复用。</done>
</task>

<task type="auto">
  <name>Task 2: OpenContext / 状态机 / Close / WaitForReady / IsReady / 构造器分支</name>
  <files>internal/device/scrapli_wrapper_78_03_test.go</files>
  <read_first>
  - internal/device/scrapli_wrapper.go — :101 checkDeviceReachable / :134 NewScrapliWrapper / :187 NewScrapliWrapperWithPort / :241-262(setState/getState/isClosing)/ :304 Open / :323-426 OpenContext / :430-476 Close / :477-529(IsConnected/IsReady/WaitForReady)/ :34 String
  - internal/device/scrapli_wrapper_78_03_test.go — Task 1 的 newSW78 helper
  - .planning/research/v1.27-pitfalls.md — R2(OpenContext 30s 超时 + ticker GetPrompt 轮询;fixture 不含 prompt 会 hang 30s)
  </read_first>
  <action>
  同文件追加:

  - `TestSW78_OpenContext_Success`:注入 FileTransport(用 huawei_vrp_open.fixture,含首 prompt)→ `OpenContext(context.Background())` → 断言 nil error 且 `getState()==StateReady`、`IsConnected()==true`、`initDone` 已关闭(`WaitForReady(1s)` 立即返回 nil)。
  - `TestSW78_OpenContext_CtxCancelled`:`ctx, cancel := context.WithCancel(...)`; 先 cancel 再调 → 断言 ctx 路径返回错误且不 hang(**用例整体加 10s 硬超时守卫**,R2)。
  - `TestSW78_OpenContext_DriverOpenFail`:注入一个返回错误 driver 的工厂(或 fixture 指向不存在文件)→ 断言 state 被置 StateClosed 且错误文案含设备 IP(`w.device.IPAddress` 非 nil 前提)。
  - `TestSW78_Open_PanicRecovery`:`newScrapliWrapperForTesting(nil)` + 直写 device → `Open()` 触发 nil driver panic → 断言被 recover 且 state=StateClosed(:305-311),不逃逸。
  - `TestSW78_Close_StateMachine`:(a) `newScrapliWrapperForTesting(nil)`(driver=nil)→ Close 返回 nil、state=StateClosed、closing channel 已关;(b) 已 Closed 再 Close → 立即返回 nil(:433-436);(c) state=StateClosing → 立即返回 nil。**注意 Close 内含 `time.Sleep(100ms)` 安全网,用例接受该固定成本,不得改生产**。
  - `TestSW78_IsReady_And_WaitForReady`:IsReady 在 StateReady + GetPrompt 成功时 true(FileTransport 消费一个 prompt 行);state=StateClosed → false;WaitForReady 三分支:已 Ready 立即 nil / Closed|Closing 报"连接已关闭或正在关闭" / initDone 未关且超时 → "等待连接就绪超时"(timeout 用 50ms,禁用秒级)。
  - `TestSW78_IsClosing_And_String`:isClosing 在 close(closing) 前后 false/true;`ConnectionState.String()` 覆盖全部枚举 + 未知值(:34,当前 66.7%)。
  - `TestSW78_NewScrapliWrapperWithPort_And_CheckReachable`:`NewScrapliWrapperWithPort`(当前 16.7%)用 var 注入 + 自定义端口 → 断言构造成功;`checkDeviceReachable` 用一个本地 `net.Listen("tcp","127.0.0.1:0")` 的真实监听端口断言可达、用已关闭端口断言不可达(**仅本地回环,禁真实设备 IP**)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestSW78_" -timeout 180s ./internal/device/ && go test -count=1 -coverprofile=/tmp/d78_03a.out ./internal/device/ && go tool cover -func=/tmp/d78_03a.out | grep "scrapli_wrapper.go"</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestSW78_" internal/device/scrapli_wrapper_78_03_test.go` ≥ 14(Task 1+2 合计)
  - OpenContext 的 ctx-cancel 用例含硬超时守卫(grep `time.After` 或 `-timeout` 语义存在);全文件无用例运行超过 15s
  - `go tool cover -func=/tmp/d78_03a.out | grep "scrapli_wrapper.go"` 加权 ≥80%
  - checkDeviceReachable 用例只出现 `127.0.0.1` 字面量,无真实设备 IP
  - `go test -count=1 -run "TestSW78_" -timeout 180s ./internal/device/` exit 0
  </acceptance_criteria>
  <done>scrapli_wrapper.go ≥80%:OpenContext 三分支 + Close 三态 + IsReady/WaitForReady/isClosing/String + 两个构造器 + checkDeviceReachable 全覆盖。</done>
</task>

<task type="auto">
  <name>Task 3: PooledConnection 引用计数/Execute + 池 GetConnection 复用路径(D-78-05 预置手法)</name>
  <files>internal/device/connection_pool_78_03_test.go</files>
  <read_first>
  - internal/device/connection_pool.go — :29-156(PooledConnection 全部方法)+ :180-235(构造/SetEnabled/IsEnabled)+ :236-360 GetConnection + :364 oldestIdleConnectionLocked + :380 getDeviceLock
  - internal/device/e2e_helpers.go — 全文(两个 ForTesting 工厂的字段语义与"生产禁调"契约)
  - internal/device/scrapli_wrapper_78_03_test.go — Task 1/2 的注入 helper 与 fixture 用法
  - internal/models/ — NetworkDevice / AuthCredential 字段(CredentialID 指针、IsDefault、ProtocolType)
  </read_first>
  <action>
  创建 `internal/device/connection_pool_78_03_test.go`(package device)。两个 helper:

  - `newPool78(t) (*DeviceConnectionPool, *gorm.DB)`:sqlite :memory: + CREATE TABLE network_devices / auth_credentials → `NewDeviceConnectionPool(db, cipher, &PoolConfig{MaxIdle: 50*time.Millisecond, MaxConnections: 2})`;**t.Cleanup(func(){ _ = pool.Close() })** 必须注册(startCleanup goroutine)。cipher 用一个同包最小 fake(实现 addomain.PasswordCipher 的 Encrypt/Decrypt 直通)或 nil(测 nil 分支时)。
  - `seedPool78(t, p *DeviceConnectionPool, deviceID string, w *ScrapliWrapper) *PooledConnection`(**D-78-05 核心手法**):
    ```go
    pc := &PooledConnection{wrapper: w, refCount: 0, lastUsed: time.Now(),
        deviceID: deviceID, mu: p.getDeviceLock(deviceID), pool: p}
    p.poolLock.Lock(); p.connections[deviceID] = pc; p.poolLock.Unlock()
    return pc
    ```
    注意 `mu` 必须是 `p.getDeviceLock(deviceID)` 返回的同一把锁(否则 Acquire/Release 与 GetConnection 的 deviceLock 语义错位)。

  测试函数前缀 TestCP78_:
  - `TestCP78_AcquireReleaseRefCount`:ForTesting wrapper(FileTransport,state=Ready)→ Acquire 成功(IsConnected+WaitForReady+IsReady 三关;IsReady 消费 fixture 的一个 prompt 行,fixture 需配足)→ refCount 断言 → Release 后 refCount 归零且 mu 已解锁(可再次 Acquire 证明);`ReleaseRef` 不动 mu 的语义(GetConnection 配对)各一个用例。
  - `TestCP78_Release_NegativeCountPanics`:refCount=0 直调 Release → `require.Panics` 断言负计数 panic 文案(:88-91,这是**故意的生产不变量**,按现行为断言)。
  - `TestCP78_Execute_Success`:`pc.Execute(ctx, func(w *ScrapliWrapper) error { _, err := w.SendCommand("display version", false); return err })` → nil。
  - `TestCP78_Execute_ErrorBranches`:(a) wrapper state=Closed → Acquire 失败 → "连接不可用";(b) 已 cancel 的 ctx → 返回 ctx.Err();(c) fn 内 panic → 断言被恢复为 error 且 wrapper 被置 StateClosed(:127-136)。
  - `TestCP78_GetConnection_ReuseHit`(D-78-05 主证):seedPool78 预置 → `p.GetConnection(ctx, id)` → 断言返回同一 pc 指针、refCount 被 +1、lastUsed 被刷新;用完 `pc.ReleaseRef()`。
  - `TestCP78_GetConnection_Disabled`:`p.SetEnabled(false)` → 报"连接池未启用"。
  - `TestCP78_GetConnection_StaleThenCreateFails`:预置一个 state=Closed 的 wrapper(IsReady=false)→ 走失效路径 removeConnection → 再走新建路径 createConnection → 因设备行不存在报"创建连接失败: 查询设备失败";断言 `p.connections` 中该 key 已被清理。
  - `TestCP78_GetConnection_PoolFullAndLRU`:MaxConnections=2,预置 2 条 idle(refCount=0,lastUsed 一新一旧)→ 请求第 3 个设备 → 断言**最旧的 idle 被淘汰**(`oldestIdleConnectionLocked` 语义)且随后因 createConnection 失败返回 error;再把两条都置 refCount>0(活跃)→ 断言返回"连接池已满且无 idle 连接可退让"。
  - `TestCP78_GetDeviceLock_Reuse`:同 deviceID 两次调用返回同一指针;不同 deviceID 不同指针(补 :380 的 85.7%)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestCP78_" -timeout 180s ./internal/device/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestCP78_" internal/device/connection_pool_78_03_test.go` ≥ 9 且含 `func seedPool78`
  - `seedPool78` 中 `mu:` 赋值来自 `p.getDeviceLock(`(锁一致性)
  - 存在断言 GetConnection 复用路径返回同一指针的用例(D-78-05 主证)
  - 每个建池用例均有 `t.Cleanup` 关池;`go test -race -count=1 -run "TestCP78_" ./internal/device/` exit 0
  - `go test -count=1 -run "TestCP78_" -timeout 180s ./internal/device/` exit 0(无 hang;removeConnection 的 30s 等待路径不得被 refCount>0 的连接触发)
  </acceptance_criteria>
  <done>PooledConnection 全方法 + GetConnection 复用/失效/池满/LRU/未启用五路径覆盖;D-78-05 预置手法实证成立,供 Task 4 与 78-04 复用。</done>
</task>

<task type="auto">
  <name>Task 4: 池清理/移除/Close + createConnection 错误早退分支</name>
  <files>internal/device/connection_pool_78_03_test.go</files>
  <read_first>
  - internal/device/connection_pool.go — :394-482 createConnection(DB 查设备/凭证/解密三段)+ :486-516 removeConnection + :517-560(startCleanup/cleanupIdleConnections)+ :562-600(GetStats/GetDevice/Close)
  - internal/device/connection_pool_78_03_test.go — Task 3 的 newPool78 / seedPool78
  - internal/models/ — AuthCredential 的 Password / IsDefault / CredentialName 列
  </read_first>
  <action>
  同文件追加:

  - `TestCP78_CreateConnection_DeviceNotFound`:空表 → "查询设备失败"。
  - `TestCP78_CreateConnection_CredentialNotFound`:插入设备行(CredentialID 指向不存在 id)→ "查询凭证失败";另一个用例设备无 CredentialID 且无 is_default 凭证 → "未找到默认凭证"。
  - `TestCP78_CreateConnection_NoCipher`:设备 + 凭证齐备但 `p.passwordCipher = nil` → "密码加密器未初始化"。
  - `TestCP78_CreateConnection_EmptyPassword`:凭证 Password 为空 → 按 :475+ 的实际语义断言(读源码确认是报错还是空密码继续),按现行为断言。
    ⚠️ D-78-05:createConnection 走到 `NewScrapliWrapper` + 真 SSH 之后的段落**接受不覆盖**(SUMMARY 记录该边界与剩余 stmts 数);禁为覆盖率给 createConnection 加 transport seam(那是 Phase 79 长尾选项)。
  - `TestCP78_RemoveConnection`:预置 idle 连接(wrapper=ForTesting(nil) 避免 Close 触发 FileTransport 读)→ removeConnection 断言被删;不存在的 deviceID → 返回 nil;**refCount>0 的超时分支不测**(30s 等待,成本过高)——在注释与 SUMMARY 记录为已知不覆盖。
  - `TestCP78_CleanupIdleConnections`:MaxIdle=50ms,预置一条 idle 且 `lastUsed = time.Now().Add(-time.Second)` → 直调 `cleanupIdleConnections()` → 断言被清理;另预置一条 refCount>0 的过期连接 → 断言**不**被清理(IsIdle 守卫)。
  - `TestCP78_StartCleanup_TickerPath`:构造池后直调 `cleanupIdleConnections` 已覆盖主体;`startCleanup` 的 ticker goroutine 用"close(p.done) 后 goroutine 退出"断言(可观察口径:Close 返回 nil 且再次 Close 不 panic 或按现行为断言 —— close 已关 channel 会 panic,若如此则只调一次并把幂等缺失记 SUMMARY,D-78-10)。
  - `TestCP78_GetStats_And_GetDevice`:预置 1 idle + 1 active → 断言 total/active/idle/max/enabled 五键值;GetDevice 命中/不命中两分支。
  - `TestCP78_PoolClose_WithConnections`:预置 2 条 idle(ForTesting(nil) wrapper)→ Close 断言 10s 内返回、connections 被清空。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestCP78_" -timeout 180s ./internal/device/ && go test -count=1 -coverprofile=/tmp/d78_03b.out ./internal/device/ && go tool cover -func=/tmp/d78_03b.out | grep "connection_pool.go"</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestCP78_" internal/device/connection_pool_78_03_test.go` ≥ 17(Task 3+4 合计)
  - createConnection 四个错误早退分支各有独立用例(grep "查询设备失败|查询凭证失败|未找到默认凭证|密码加密器未初始化" ≥4 处断言)
  - `go tool cover -func=/tmp/d78_03b.out | grep "connection_pool.go"` 加权 ≥70%
  - 单个用例运行时长 <15s(无 30s removeConnection 超时路径、无 10s Close 超时路径被真触发)
  </acceptance_criteria>
  <done>connection_pool.go ≥70%:清理/移除/统计/Close + createConnection 四个错误早退全覆盖;SSH 段边界明确落 SUMMARY。</done>
</task>

<task type="auto">
  <name>Task 5: executor 四函数(executeWithRetry 直喂 conn + 三入口经预置池)+ per-file checkpoint</name>
  <files>internal/device/executor_78_03_test.go</files>
  <read_first>
  - internal/device/executor.go — 全文(:23 DefaultExecutionConfig / :52 ExecuteOnDevice / :101 ExecuteMultipleOnDevice / :161 ExecuteCustom / :201 executeWithRetry / :267 GetConfig)
  - internal/device/task_scheduler.go — :104 Submit / :156 startWorker / :189 executeTask(GetConnection → task.Execute → Callback)
  - internal/device/connection_pool_78_03_test.go — newPool78 / seedPool78(本任务复用)
  - internal/device/scrapli_wrapper_78_03_test.go — newSW78 与 fixture(本任务复用,注意 fixture 字节要够 retry 次数)
  </read_first>
  <action>
  创建 `internal/device/executor_78_03_test.go`(package device)。前缀 TestEX78_:

  **A. executeWithRetry 直喂 conn(绕开 Submit,零调度器)**:
  - `TestEX78_ExecuteWithRetry_Success`:`NewPooledConnectionForTesting(FileTransport driver)` → `e := NewDeviceExecutor(nil, &ExecutionConfig{MaxRetries:1, RetryDelay: time.Millisecond, Timeout: time.Second, EnablePanicRecovery:true})` → `e.executeWithRetry(ctx, conn, "display version", false)` 返回 Response 且 Result 含 "Huawei"。
  - `TestEX78_ExecuteWithRetry_RetryExhausted`:conn 的 wrapper state=Closed(SendCommand 必失败)+ MaxRetries=2 + RetryDelay=1ms → 断言错误含"已达最大重试次数"且**尝试次数 = MaxRetries+1**(可观察口径:自定义一个计数用的 wrapper 状态,或按错误文案断言)。
  - `TestEX78_ExecuteWithRetry_CtxCancelDuringDelay`:MaxRetries=3 + RetryDelay=200ms + 首次失败 + 50ms 后 cancel ctx → 断言返回 `context.Canceled`(:215-219 分支)。
  - `TestEX78_ExecuteWithRetry_PanicRecovered`:conn.wrapper=nil(触发 nil-deref panic)+ EnablePanicRecovery=true → 断言错误含"命令执行 panic";同形态 EnablePanicRecovery=false → 断言 **panic 逃逸**(用 `require.Panics` 断言现行为,:242-247 的 else 分支)。

  **B. 三入口经 D-78-05 预置池 + 真 scheduler**:
  helper `newExec78(t) (*DeviceExecutor, *DeviceTaskScheduler, *DeviceConnectionPool, *gorm.DB, string)`:newPool78 → `NewDeviceTaskScheduler(pool, &SchedulerConfig{TaskTimeout: 2*time.Second, QueueSize: 4})`(t.Cleanup(sched.Stop()))→ seedPool78 预置一条 FileTransport 连接 → `NewDeviceExecutor(sched, &ExecutionConfig{MaxRetries:0, RetryDelay: time.Millisecond, Timeout: time.Second, EnablePanicRecovery:true})`。
  - `TestEX78_ExecuteMultipleOnDevice`:两条命令(fixture 配足)→ 断言返回 2 个结果字符串;另一个用例:scheduler `SetEnabled(false)` → 断言 Submit 失败错误直接透传(:124-126)。
  - `TestEX78_ExecuteCustom_SuccessAndError`:executeFunc 返回 nil → ExecuteCustom 立即返回 nil(**断言耗时 < 1s**,证明走的是 `<-done` 而非 ticker 空转到 timeout+1min —— 这是 :150-158 注释记录的历史 bug,本用例即其回归锁定);executeFunc 返回 error → 断言 error 透传。
  - `TestEX78_GetConfig_VendorMatrix`:DB 插入 4 台设备(vendor = huawei / h3c / ruijie / maipu / 未知)→ 逐台调 `e.GetConfig(ctx, id)`;**断言口径**:GetDevice 成功 + 选中命令正确。命令字符串无法从返回值直接观察时,改断言"fixture 中对应命令回显被消费成功/失败的可区分结果",或对未知厂商断言默认 `show running-config` 路径;`GetDevice` 失败(设备不存在)→ 断言"获取设备信息失败"。若命令选择分支无可观察口径,按 D-78-10 以现行为断言 + SUMMARY 记录断言降级。
  - `TestEX78_ExecuteOnDevice_TimeoutPath`:Timeout=50ms + executeFunc 阻塞 → 断言"任务执行超时"(补 :52 的 35% unc);用例整体加硬超时守卫。
  - `TestEX78_DefaultExecutionConfig_And_GetScheduler`:nil config → 默认值(MaxRetries=3/RetryDelay=1s/Timeout=30s/EnablePanicRecovery=true);GetScheduler 返回注入实例。

  **收尾 checkpoint**:
  ```
  go test -count=1 -coverprofile=cov78_03.out ./internal/device/
  go tool cover -func=cov78_03.out | grep -E "scrapli_wrapper.go|connection_pool.go|executor.go|^total"
  go test -race -count=1 ./internal/device/
  ```
  三文件 per-file 数字 + 包 total 落 SUMMARY(基线包 43.5%;snmp_client 与 task_scheduler 余量由 78-04 收口到 ≥70%)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestEX78_" -timeout 180s ./internal/device/ && go test -race -count=1 ./internal/device/ && go test -count=1 -coverprofile=cov78_03.out ./internal/device/ && go tool cover -func=cov78_03.out | grep -E "scrapli_wrapper.go|connection_pool.go|executor.go" && go build ./... && go test ./... 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestEX78_" internal/device/executor_78_03_test.go` ≥ 9
  - ExecuteCustom 成功用例含"耗时 < 1s"断言(历史 30s 超时 bug 的回归锁定)
  - `go tool cover -func=cov78_03.out`:scrapli_wrapper.go ≥80%、executor.go ≥70%、connection_pool.go ≥70%
  - `go test -race -count=1 ./internal/device/` exit 0;`go test ./...` exit 0;`go build ./...` exit 0
  - `git diff --stat` 生产 .go 改动 = 0 文件(除非 D-78-10 quirk 修复 + SUMMARY deviation)
  </acceptance_criteria>
  <done>executor.go ≥70%;三文件 per-file 目标全达标数字落 SUMMARY;78-04 可在 wave 2 复用 fixture + 预置池 helper 收口 device ≥70%。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 ↔ 网络设备 | 零真实 SSH:全部经 `newNetworkDriver` var-seam + FileTransport fixture 回放;host 固定字面量 `dummy-host` |
| 测试 ↔ TCP | 仅 checkDeviceReachable 用 `127.0.0.1` 本地监听端口,禁任何真实设备 IP |
| 测试 ↔ 数据库 | glebarez sqlite :memory: + 手动 CREATE TABLE,禁生产 PG |
| 测试 ↔ 凭据 | 设备用户名/密码只用 `"u"/"p"` 字面量;passwordCipher 用同包直通 fake |
| 测试 ↔ 生产代码 | 仅新增 *_test.go 与 testdata fixture;生产改动仅 D-78-10 quirk 修复豁免 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-03-01 | DoS | FileTransport fixture 耗尽后 `Read` 永久阻塞 → 测试 hang / CI 卡死(76-02 文件头已记录) | mitigate | 每用例为其操作序列配足 prompt/回显行;FileTransport 驱动的用例一律不调 Close;`-timeout 180s` + 单例 <15s 验收 |
| T-78-03-02 | Tampering | 包级 var `newNetworkDriver` 泄漏到同包后续测试(覆盖后 t.Fatal 提前退出) | mitigate | orig + t.Cleanup 恢复;覆盖点之后禁 t.Fatal(改 t.Errorf + return);禁 t.Parallel(D-78-09) |
| T-78-03-03 | DoS | 连接池 startCleanup / scheduler worker goroutine 泄漏 | mitigate | 每个建池/建调度器用例 t.Cleanup Close/Stop;`go test -race` 验收 |
| T-78-03-04 | Spoofing | 测试误连真实网络设备(生产 IP/凭据入仓) | mitigate | 仅 `dummy-host` / `127.0.0.1`;验收 grep 测试文件与 fixture 无真实 IP/主机名/口令 |
| T-78-03-05 | Tampering | 为覆盖 createConnection 的 SSH 段擅自加生产 seam,扩大 INFRA 面 | mitigate | D-78-05 明令禁止;SSH 段接受不覆盖并在 SUMMARY 记录剩余 stmts |
| T-78-03-SC | Tampering | 依赖安装 | accept | 零新增依赖(scrapligo/FileTransport 已在 go.mod;fixture 为纯文本) |
</threat_model>

<verification>
- `go test -count=1 -timeout 180s ./internal/device/` 全绿且无 hang;`go test -race -count=1 ./internal/device/` 全绿
- `go tool cover -func=cov78_03.out`:scrapli_wrapper.go ≥80% / executor.go ≥70% / connection_pool.go ≥70%
- 测试文件与 fixture 中无真实设备 IP / 主机名 / 口令;无 `t.Parallel()`;覆盖 var 之后无 `t.Fatal`
- `go build ./...` + `go test ./...` exit 0;生产 .go 改动 = 0(或有 SUMMARY deviation)
</verification>

<success_criteria>
- 三个测试文件 + ops fixture 存在,TestSW78_ ≥14 / TestCP78_ ≥17 / TestEX78_ ≥9 全绿
- D-78-05 预置池手法实证成立:GetConnection 复用路径 + executor 三入口 + task_scheduler.executeTask 被真链路走通,生产代码零改动
- scrapli_wrapper.go ≥80%、executor.go ≥70%、connection_pool.go ≥70%(BLOCK-04 主力段)
- fixture + seedPool78 + newSW78 三件 helper 沉淀,78-04 直接复用
</success_criteria>

<decision_audit>
| ID | 决策 | 依据 / 理由 |
|----|------|------------|
| **D-78-05** | **createConnection 不加 seam;改用「同包预置 `pool.connections[deviceID]` → GetConnection 复用路径」打通 executor/task_scheduler 全链** | 78-RESEARCH DQ5 只给了 (a) 只测 Release/Execute 或 (b) 加 transport seam 两个选项,并推荐 (a)。planner 读 connection_pool.go:244-262 发现**第三条路**:复用路径的唯一门槛是 `pc.wrapper.IsReady()`,而 FileTransport wrapper 完全满足 → 既不加生产 seam(避开 (b) 的 INFRA 扩面)、也不放弃 executor 的 3 个 Submit 入口(避开 (a) 的覆盖损失)。这是对 RESEARCH Gap G2/R4"直接调 task.Execute 绕开 Submit"方案的**升级**:连 Submit→executeTask→GetConnection 一起覆盖,且 78-04 的 task_scheduler 测试可直接复用 |
| D-78-09 | device 测试禁 t.Parallel();`newNetworkDriver` 覆盖必须 orig+t.Cleanup,覆盖后禁 t.Fatal | driver_factory_76_02_test.go 文件头已立此规(Pitfall #6),本 plan 只是逐条执行 |
| D-78-03a | FileTransport 驱动的用例**一律不调 wrapper.Close()/driver.Close()** | 76-02 文件头明确:network-on-close 会在 fixture 耗尽后继续读 → `FileTransport.Read` 的 `select{}` 永久阻塞。Close 的覆盖改走 driver=nil 与状态机分支 |
| D-78-03b | `removeConnection` 的 refCount>0 超时分支(30s)与 `Close` 的 10s 超时分支**接受不覆盖** | 单测时间成本与收益不成比例(≈40s/用例),且属显式超时兜底逻辑;SUMMARY 记录剩余 stmts。与 Phase 77 "PG-only SQL 接受不覆盖"同一裁决口径 |
| D-78-03c | `Release()` 负计数 panic 与 `EnablePanicRecovery=false` 时 panic 逃逸,均按**现行为**用 `require.Panics` 断言,不当作 quirk 修 | 两者都是源码注释明示的**故意不变量**("不应该发生,说明引用计数管理有问题" / 显式 else 分支),D-78-10 的"有据判定"结论为不改 |
| D-78-03d | SendConfig/SendConfigs 的 privilege 提升 fixture 若推演受阻,降级为错误分支覆盖 + SUMMARY 记录 | 这是本 plan 唯一的字节流推演风险点(R2/S-2);预设降级路径避免 executor 为过测而改生产逻辑 |
| D-78-08 / D-78-10 / D-78-11 | 命名 / quirk 纪律 / wave 编排 | 同 78-01 |
</decision_audit>

<output>
Create `.planning/workstreams/milestone/phases/78-block-bp-unlock-by-foundation/78-03-SUMMARY.md` when done
</output>
