---
phase: 78-block-bp-unlock-by-foundation
plan: 02
type: execute
wave: 2
depends_on: [78-01]
files_modified:
  - internal/core/core_init_78_02_test.go
autonomous: true
requirements: [BLOCK-03]

must_haves:
  truths:
    - "本 plan 交付 core.go 的 Init/Close 装配链测试(302 + 60 stmts,当前逐函数实测 0.0%: 全 0),经 glebarez sqlite + MemoryCache/miniredis 两种 Cache.Type 分支装配,零真实 PG / 零真实 Redis"
    - "Task 1 是 **Init 可跑深度探针实验**(ROADMAP L211 明示的研究数据缺口):先证 Init 能在 sqlite+memory 配置下跑完且 Close 收尾不 hang,把 `实际可达的子系统清单 / 失败点 / 耗时 / goroutine 收尾行为` 写进 SUMMARY,后续 Task 才在探针结论上叠加深度断言。禁跳过探针直接写覆盖测试"
    - "按 D-78-01 缓存装配纪律:memory 分支用 `Config.Cache.Type != \"redis\"` 走纯 MemoryCache(无 L2Writer);redis 分支用 miniredis + NewMultiLevelCacheSimple(pkg/cache/redis.go:547-555,构造即不启 L2WriteWorker)作为默认形态,生产 WithWriter 形态单独一个用例并显式断言 Close 后 worker 停止(R-7)"
    - "按 D-78-03 hang 处置阶梯:探针若暴露 Close hang/goroutine 泄漏,先用 (c) 测试侧容忍(硬超时 + 显式 Close 断言,零新依赖,不引入 goleak)界定问题范围;仅在 (c) 不成立时才动 (a) 修 Close 顺序(原子 commit + SUMMARY deviation);(b) 给 scheduler 加 ctx-aware Stop 属中等重构,留 Phase 79/80"
    - "每个调 Init 的用例必须有 `t.Cleanup` 收尾 `c.Close()` + 整体硬超时守卫(禁无界等待),`go test -race ./internal/core/` 必须绿(Q-7 MetricsCacheService.Stop 幂等已由 Phase 75 修复并由 78-01 Task 5 回归锁定)"
    - "按 D-78-08 命名 `core_init_78_02_test.go`;按 D-78-10 quirk 纪律;按 D-78-11 wave 编排(本 plan wave 2,同包 78-01 已落盘,直接复用其 sqlite/miniredis 装配 helper)"
    - "达标口径:`internal/core/core.go` ≥60%(Init 302 + Close 60 的半数以上覆盖),`internal/core` 包总覆盖率 ≥70%(BLOCK-03 收口;基线 2026-08-27 实测 43.7%),`go build ./...` + `go test ./...` 全绿,check-coverage.sh 的 P2_RATCHET_internal_core=39.00 豁免行对应包实测 ≥70%(豁免行删除留 Phase 81)"
    - "本 plan 裁决细则(Decision Audit 逐条依据,executor 必须遵守):D-78-02a(sqlite 用 t.TempDir 文件库而非 `:memory:`,Init 链跨多连接会各自建库)/ D-78-02b(探针任务的通过条件是「给出结论且不 hang」而非「Init 必须返回 nil」,禁为过测改生产装配代码)/ D-78-02c(startSubprocessReaper 只覆盖当前平台实现,禁 GOOS 条件 t.Skip —— 防重造本地/CI 分歧)"
  artifacts:
    - path: "internal/core/core_init_78_02_test.go"
      provides: "Core.Init 全链 + Close 收尾 + 两条 Cache.Type 分支 + RPA/reaper/warm-up 装配测试(前缀 TestInit78_)"
      min_lines: 520
      contains: "func newInit78Config"
  key_links:
    - from: "internal/core/core_init_78_02_test.go"
      to: "core.New(cfg) → (*Core).Init() (core.go:120 / :196)"
      via: "全字段自造 *config.Config(sqlite + memory/redis + 合法 SM4Key + JWT)"
      pattern: "\\.Init\\(\\)"
    - from: "Init 用例"
      to: "(*Core).Close() (core.go:692)"
      via: "t.Cleanup 注册 + 硬超时守卫 channel"
      pattern: "t\\.Cleanup"
    - from: "redis 分支用例"
      to: "initCache 的 redis 分支 (core.go:784-862)"
      via: "miniredis.RunT + Config.Cache.Type=\"redis\" + Addr=mr.Addr()"
      pattern: "miniredis"
---

<objective>
交付 internal/core 装配层(core.go)的 Init/Close 全链测试,把 BLOCK-03 从 78-01 的"非 core.go 子集"推进到包级 ≥70%。本 plan 的第一任务是 ROADMAP 明示的 **Init 深度探针实验**——研究阶段无法回答"core.Core 完整 Init 图能否用 sqlite + miniredis 拼出"(78-RESEARCH §1.3 开放问题 Q4 / Gap G1 / R1),必须由可执行探针给出结论,再据此叠加断言。

Purpose: BLOCK-03 收口段。core.go 302 stmts 的 Init 编排 + 60 stmts 的 Close 收尾是 internal/core 40%→70% 的唯一剩余主力。
Output: 新增 internal/core/core_init_78_02_test.go(零生产代码改动,除非探针暴露 Close hang 并触发 D-78-03 阶梯 (a),或触发 D-78-10 quirk 修复)。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/78-block-bp-unlock-by-foundation/78-RESEARCH.md
@.planning/workstreams/milestone/phases/78-block-bp-unlock-by-foundation/78-01-SUMMARY.md

<interfaces>
<!-- 关键契约(planner 已读 core.go 复核行号,2026-08-27)。 -->

Init 编排链(core.go:196-249,逐步骤 → 私有方法):
```
0.  SM4Cipher != nil → addomain.SetADSM4Cipher   (:197-202)
1-4 c.initDBAndData()            (:258)  DB 连接(fail-fast) → AutoMigrate(fail-fast) → InitData(warn) → 默认角色/菜单(warn)
5-6 c.initCacheAndWarmUp()       (:338)  initCache + 缓存服务 + 异步预热 goroutine
7   c.initMetrics()              (:403)  MetricsCacheService(无 goroutine)
8-9 c.initDeviceServices()       (:416)  DeviceConnectionPool(startCleanup goroutine!)+ Executor + Discovery + InfoCollection + MAC 分区
10-12 c.initSchedulerAndTasks()  (:468)  cron 引擎(goroutine)+ 全部定时任务注册 + DeviceMonitorService   ← fail-fast(返回 error)
13-14 c.initCaptchaServices()    (:586)  CaptchaService + CaptchaBackgroundService
15-16 c.initLogsAndAuth()        (:614)  OperLogService + TokenBlacklistService + AuthFactory
17-19 c.initRPAAndAPIAndReaper() (:633)  RPA + APIEndpointService + 子进程 reaper(reaperCancel)
20  go assetSvc.StartupRefreshView(30s 超时)  (:240-248)  ← Init 返回后仍在跑的 goroutine
```

Close 收尾链(core.go:692-778,顺序与 Init 对应,历史修过 hang bug —— 见 core.go:725-732 注释"原顺序在 DeviceInfoCollectionService.Stop() 之后,前 5-15s 仍在 spawn ... WaitGroup 死等"):
```
reaperCancel → NoticeHub.Stop → Scheduler.Stop + scheduler.StopADSyncScheduler
→ DeviceInfoCollectionService.Stop → DeviceMonitorService.Close
→ MetricsCacheService.Stop(Q-7 已幂等) → RPAScalingService.Stop
→ Cache.Close(MultiLevelCache 走 L2Writer Stop 兜底) → operlog 100ms 启发式 sleep → DB.Close
```

initCache 分支(core.go:784-862):
```go
if c.Config.Cache.Type == "redis" { NewRedisCache → MultiLevelCache }   // R-7: WithWriter 形态启 L2WriteWorker goroutine
else                              { cache.NewMemoryCache(MaxSize, CleanupTime) }  // 纯内存,无 worker
```

其他必知:
```go
// core.go:120 New(cfg) —— 先 initSM4Cipher(cfg.Security.SM4Key) 后 NewJWTManager(&cfg.JWT);
//   SM4Key 为空 → New 直接 error(:167-172);为仓库默认值 dGVzdC1zZWNyZXQxNiEhIQ== → 仅 ERROR 日志放行
//   ⚠️ 测试用 SM4Key 必须是合法 base64 的 16 字节(crypto.NewSM4Cipher 能构造),但**不要**用仓库默认值以外的真实生产 key
// core.go:111 GetDB() —— DB 为 nil 时返回 nil(66.7%,补 nil 分支)
// internal/core/core_skipautomigrate_test.go —— SKIP_AUTOMIGRATE=true 在 release 模式 fail-fast 的源码断言测试(已存在);
//   本 plan 可用 t.Setenv("SKIP_AUTOMIGRATE","true") + Server.Mode="release" 真跑 initDBAndData 覆盖该 fatal 分支
// pkg/cache/redis.go:547-555 NewMultiLevelCacheSimple(l1, l2 Cache) *MultiLevelCache —— 不启 L2Writer(R-7 内建缓解)
```

复用 78-01 落地的装配手法:sqlite 用 `glebarez/sqlite` + t.TempDir 文件库(:memory: 在多连接场景下会各自建库,Init 链跨多个 GORM 会话时优先文件库);miniredis 用 `miniredis.RunT(t)`。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Init 深度探针实验(sqlite + memory cache,零 Redis 依赖,证"能跑 + 能收尾")</name>
  <files>internal/core/core_init_78_02_test.go</files>
  <read_first>
  - internal/core/core.go — :120-165 New / :196-249 Init / :258-336 initDBAndData / :692-778 Close(含 :725-732 历史 hang 注释)
  - internal/config/ — Config 全结构(Server/Database/Cache/JWT/Security/OperLog/... 必填字段清单)
  - internal/core/core_infra.go + core_services.go — CoreInfra/CoreServices 字段清单(探针要逐字段判空)
  - internal/core/core_74_08_test.go — 已有注释明确"core.New 完整 Init 链路依赖 Redis/调度器/子进程,不在单测范围" —— 本任务正是要推翻或界定这条结论
  - .planning/workstreams/milestone/phases/78-block-bp-unlock-by-foundation/78-RESEARCH.md — Gap G1 / R1 / DQ3
  </read_first>
  <action>
  创建 internal/core/core_init_78_02_test.go(package core,同包白盒)。

  第一步:helper `newInit78Config(t *testing.T, cacheType string, redisAddr string) *config.Config` —— 全字段自造配置:
  - Server.Mode = "debug"(避免 SKIP_AUTOMIGRATE 的 release fatal 守卫误触)
  - Database.Type = "sqlite" + 指向 `filepath.Join(t.TempDir(), "core_init.db")`(**禁 :memory:**:Init 链内多处取 GORM 会话,文件库语义更接近生产)
  - Cache.Type = cacheType("memory" 或 "redis")+ MaxSize/CleanupTime/Addr(redis 时 = redisAddr)
  - JWT:合法 access/refresh 密钥与过期时间(NewJWTManager 能构造)
  - Security.SM4Key:合法 base64 16 字节测试值(不用生产 key)
  - 其余按 config 结构补齐必填项(端口用 0/随机避免占用真实端口)

  第二步:**探针测试** `TestInit78_Probe_SqliteMemoryCache`:
  1. `c, err := New(cfg)` → require.NoError;断言 c.CoreInfra/c.CoreServices/c.JWTManager/c.PwdManager/c.SM4Cipher 非 nil。
  2. 在带**硬超时守卫**的 goroutine 中调 `c.Init()`:
     ```go
     initDone := make(chan error, 1)
     go func() { initDone <- c.Init() }()
     select {
     case err = <-initDone:   // 记录 err(可能非 nil —— 探针允许失败,但必须给出结论)
     case <-time.After(60 * time.Second):
         t.Fatalf("PROBE 结论: Init 在 60s 内未返回(疑似 hang),SUMMARY 必须记录该结论")
     }
     ```
  3. **子系统清单探测**:用一个 `map[string]bool` 逐字段判空(DB / Cache / Scheduler / MetricsCacheService / CaptchaService / CaptchaBackgroundService / DeviceConnectionPool / DeviceExecutor / DeviceDiscoveryService / DeviceInfoCollectionService / DeviceMonitorService / OperLogService / TokenBlacklistService / AuthFactory / RPA 相关 / APIEndpointService / NoticeHub 等,字段名以 core_infra.go / core_services.go 为准),`t.Logf` 打印完整清单 + Init 耗时。这份清单是探针的主产出,必须原样抄进 SUMMARY。
  4. `Close()` 同样带硬超时守卫(30s),超时即 `t.Fatalf` 并把 hang 结论写日志。
  5. Close 之后再调一次 `c.Close()`,断言不 panic(幂等探测;Q-7 已修 MetricsCacheService.Stop,但整链幂等未验证 —— 若二次 Close panic,按 D-78-03 (c) 先在测试侧只调一次并把 quirk 记进 SUMMARY 待裁决,不擅自改生产)。

  第三步:探针结论分流(写入 SUMMARY 的 `## Probe Findings` 小节,后续 Task 依此调整):
  - **结论 A(Init 成功返回 nil)**:Task 2-5 直接叠加深度断言。
  - **结论 B(Init 返回 error,如 AutoMigrate/InitData 在 sqlite 下失败)**:记录**确切失败点与错误文案**;Task 2 改为"逐 initXxx 私有方法直调"路径(绕过 Init 顶层编排),Init 顶层只覆盖到失败点为止 + error 形态断言。参考记忆教训:sqlite 缺表/视图依赖是本仓反复出现的 family pattern(model 存在但 AutoMigrate 未注册 / GORM ALTER 走 __temp+RENAME 被视图引用 FATA),失败点若是这类,按 D-78-10 判定是否为 quirk。
  - **结论 C(hang)**:按 D-78-03 阶梯 —— 先 (c) 在测试侧界定是哪一步 hang(用分步直调 initXxx 定位),SUMMARY 给出确切 hang 点;仅当 (c) 无法产出可跑测试时才动 (a) 修 Close 顺序(原子 commit + 同 commit 回归用例 + SUMMARY deviation)。

  纪律:本任务禁 t.Parallel();所有 goroutine 收尾必须在 t.Cleanup 中兜底;测试内不得连任何真实 PG/Redis 地址。
  </action>
  <verify>
    <automated>go test -count=1 -v -run "TestInit78_Probe_SqliteMemoryCache" -timeout 300s ./internal/core/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `internal/core/core_init_78_02_test.go` 存在且含 `func newInit78Config` 与 `func TestInit78_Probe_SqliteMemoryCache`
  - 测试对 Init 与 Close 各有一处**硬超时守卫**(grep `time.After` ≥2 处),无无界等待
  - `go test -v -run TestInit78_Probe -timeout 300s ./internal/core/` exit 0(测试本身通过 —— 允许探针断言"Init 返回 error"这一事实,但不允许 hang 或 panic 逃逸)
  - 测试输出含逐子系统非空清单(t.Logf),SUMMARY 的 `## Probe Findings` 抄录该清单 + Init/Close 耗时 + 结论分类(A/B/C)
  - `go build ./...` exit 0
  </acceptance_criteria>
  <done>Init 可跑性与 Close 收尾行为有实证结论(A/B/C 之一)并落 SUMMARY;后续任务的断言深度以此为准,研究缺口 Gap G1/DQ3 关闭。</done>
</task>

<task type="auto">
  <name>Task 2: 分阶段 initXxx 直调 + 各阶段产物断言(依探针结论选顶层/分步路径)</name>
  <files>internal/core/core_init_78_02_test.go</files>
  <read_first>
  - internal/core/core.go — :258-336 initDBAndData / :338-402 initCacheAndWarmUp / :403-415 initMetrics / :416-467 initDeviceServices / :468-585 initSchedulerAndTasks / :586-613 initCaptchaServices / :614-632 initLogsAndAuth / :633-691 initRPAAndAPIAndReaper
  - internal/core/core_init_78_02_test.go — Task 1 的 newInit78Config + 探针结论注释
  - internal/scheduler/ — StartADSyncScheduler 等注册入口(initSchedulerAndTasks 的被调方,断言"被调用"用其可观察产物)
  </read_first>
  <action>
  同文件追加 TestInit78_ 前缀用例,每个 initXxx 一个用例,**逐阶段直调 + 断言该阶段的产物字段非 nil / 该阶段依赖的上游已就绪**(分工断言被调用方):

  - `TestInit78_InitDBAndData`:直调 → 断言 c.DB 非 nil、能 `c.GetDB().Exec("SELECT 1")`、关键系统表存在(sys_user/sys_role/sys_menu/sys_config 任取 3-4 张,以 InitData 实际种子表为准)。追加 3 个分支:
    * `SKIP_AUTOMIGRATE=true` + Server.Mode="release" → 断言 return error 且文案含"半初始化"(core_skipautomigrate_test.go 只做源码断言,本用例是**真跑**该 fatal 分支)
    * `SKIP_AUTOMIGRATE=true` + Mode="debug" → 断言 warn 旁路继续(BootstrapMissingTables 走完,不 error)
    * DB 连接失败(Database.Type="sqlite" 且路径指向不可写目录)→ 断言 fail-fast error
  - `TestInit78_InitCacheAndWarmUp_Memory`:Cache.Type="memory" → 断言 c.Cache 非 nil 且为 MemoryCache 形态(类型断言);Set/Get 往返成功;预热 goroutine 不影响返回。t.Cleanup 关 Cache。
  - `TestInit78_InitMetrics`:直调 → c.MetricsCacheService 非 nil;t.Cleanup 调 Stop(幂等已由 78-01 锁定)。
  - `TestInit78_InitDeviceServices`:直调 → DeviceConnectionPool/DeviceExecutor/DeviceDiscoveryService/DeviceInfoCollectionService 逐一非 nil;**必须** t.Cleanup 依次 Stop/Close(startCleanup goroutine 泄漏防护,R-7 同类风险)。
  - `TestInit78_InitSchedulerAndTasks`:直调 → c.Scheduler 非 nil;断言注册后的任务数 > 0(以 scheduler 暴露的可观察接口为准;若无接口则断言 error==nil + Scheduler 非 nil 并 SUMMARY 记录断言降级);t.Cleanup 调 Scheduler.Stop() + scheduler.StopADSyncScheduler()。若该阶段返回 error(fail-fast 语义),按探针结论断言 error 形态。
  - `TestInit78_InitCaptchaServices`:直调 → CaptchaService/CaptchaBackgroundService 非 nil 且 SetBackgroundService 已互联(可观察证据:GenerateCaptcha 的 slider 路径能拿到自定义背景服务,或直接判字段)。
  - `TestInit78_InitLogsAndAuth`:直调 → OperLogService/TokenBlacklistService/AuthFactory 非 nil;`c.GetAuthFactory()` 返回同一实例。

  纪律:每个用例独立 newInit78Config(独立 t.TempDir 库),阶段依赖靠"前置直调上游 initXxx"表达,不共享全局 Core。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestInit78_Init" -timeout 300s ./internal/core/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestInit78_Init" internal/core/core_init_78_02_test.go` ≥ 7
  - initDBAndData 用例含 SKIP_AUTOMIGRATE release-fatal / debug-bypass / DB 失败三分支(grep `SKIP_AUTOMIGRATE` ≥2)
  - 每个启 goroutine 的阶段(initCacheAndWarmUp / initDeviceServices / initSchedulerAndTasks / initMetrics)用例内均有对应 t.Cleanup 收尾调用
  - `go test -count=1 -run "TestInit78_Init" ./internal/core/` exit 0
  </acceptance_criteria>
  <done>8 个 initXxx 私有阶段各有独立用例与产物断言;fail-fast/warn-continue 策略被真跑验证(非源码断言)。</done>
</task>

<task type="auto">
  <name>Task 3: initCache redis 分支(miniredis + MultiLevelCache 两形态)+ 缓存预热链</name>
  <files>internal/core/core_init_78_02_test.go</files>
  <read_first>
  - internal/core/core.go — :784-862 initCache(redis / memory 二分支)+ :956-1007 initSystemServicesForWarmUp + performCacheWarmUp
  - pkg/cache/redis.go — :547-555 NewMultiLevelCacheSimple(无 L2Writer)+ WithWriter 变体的 worker 启动与 Close 语义
  - .planning/research/v1.27-pitfalls.md — R-1(TTL 用 FastForward)/ R-2(不断言 INFO 统计)/ R-3(CLIENT SETINFO 已由 miniredis v2.38 no-op)/ R-7(L2Writer goroutine)
  </read_first>
  <action>
  同文件追加:

  - `TestInit78_InitCache_RedisBranch_Simple`:`mr := miniredis.RunT(t)` + Cache.Type="redis" + Addr=mr.Addr() → 直调 initCache → 断言 c.Cache 非 nil、Set/Get 往返成功、**键真实落在 miniredis**(用 `mr.Keys()` 或 `mr.Get(...)` 断言,注意生产 Redis 前缀 `xingran:` —— 断言键名时必须带前缀,见 CLAUDE.md Cache 章节)。TTL 语义用 `mr.FastForward(d)` 推进后断言键消失(R-1),禁裸 time.Sleep。t.Cleanup 关 Cache。
  - `TestInit78_InitCache_RedisBranch_WriterCloseStopsWorker`:走生产 `NewMultiLevelCacheWithWriter` 形态(若 initCache 内部固定用某一形态,则本用例直接在测试内构造该形态并断言 Close 行为)→ Close 后断言 worker 已停(可观察口径:Close 返回 nil + 后续 Set 返回错误或不再写 L2 + `go test -race` 无竞争告警)。**D-78-01 要求本用例存在**,以证明生产路径的 Close 也能收尾。
  - `TestInit78_InitCache_MemoryBranch_Fallback`:Cache.Type 为空/未知值 → 走 else 分支纯 MemoryCache(断言类型 + 无 worker:Close 立即返回)。
  - `TestInit78_InitCache_RedisUnreachable`:Cache.Type="redis" + Addr 指向已关闭端口(先 miniredis.Run 再 Close,或 127.0.0.1:1)→ 断言 initCache 的失败/降级语义**按现行为断言**(是 error 还是降级到内存,以源码为准),SUMMARY 记录该语义。
  - `TestInit78_CacheWarmUp`:直调 initSystemServicesForWarmUp + performCacheWarmUp(需先 initDBAndData + initCache)→ 断言预热后指定缓存键存在(R-2:只断言 err==nil + 键存在,禁断言 Redis 统计)。预热是异步 goroutine 时,用轮询 + 硬超时(≤5s)等待,禁裸 sleep 断言。

  R-3 已由 INFRA-01 的 miniredis v2.38 处理(CLIENT SETINFO no-op),本任务无需额外兼容代码;若仍报 CLIENT SETINFO 错误,按 D-78-10 记 SUMMARY 并升级 miniredis 小版本(原子 commit)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestInit78_(InitCache|CacheWarmUp)" -timeout 300s ./internal/core/ && go test -race -count=1 -run "TestInit78_InitCache" ./internal/core/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestInit78_InitCache" internal/core/core_init_78_02_test.go` ≥ 4 且存在 `TestInit78_CacheWarmUp`
  - 存在使用 `mr.FastForward` 的 TTL 断言;测试文件中无用于 TTL/预热等待的裸 `time.Sleep`(轮询 + 超时除外)
  - 存在断言 `xingran:` 前缀键的用例(前缀语义不被误当作业务键名)
  - `go test -race -count=1 -run "TestInit78_InitCache" ./internal/core/` exit 0(R-7 L2Writer 无泄漏/无竞争)
  </acceptance_criteria>
  <done>initCache 两条分支 + 不可达降级 + 预热链全覆盖;MultiLevelCache 的 Simple 与 WithWriter 两形态 Close 行为均被验证。</done>
</task>

<task type="auto">
  <name>Task 4: Close 收尾顺序 + 幂等 + goroutine 收敛(-race)+ reaper 平台分支</name>
  <files>internal/core/core_init_78_02_test.go</files>
  <read_first>
  - internal/core/core.go — :692-778 Close 全文(含 :725-732 历史 hang 注释)+ :633-691 initRPAAndAPIAndReaper(reaperCancel 注入点)
  - internal/core/subprocess_reaper_windows.go + subprocess_reaper_linux.go — startSubprocessReaper 两平台实现(当前 windows 分支 0.0%)
  - internal/core/core_init_78_02_test.go — Task 1 探针结论(Close 是否 hang / 二次 Close 是否 panic)
  </read_first>
  <action>
  同文件追加:

  - `TestInit78_Close_FullChain`:走完整 Init(或依探针结论 B 走"直调到最深可达阶段")→ Close 带 30s 硬超时守卫 → 断言:Close 返回 nil;Close 后 `c.GetDB()` 的连接已关(执行查询报错);Cache 已关;MetricsCacheService.Stop 已被调(可观察口径:再调 Stop 不 panic)。
  - `TestInit78_Close_Idempotent`:连调 Close() 三次 → 全部不 panic(依探针结论:若二次 Close 触发 panic/关闭已关 channel,按 D-78-03 (c) 在 SUMMARY 记为待裁决 quirk,并把断言降级为"仅首次 Close 返回 nil",禁擅自改生产 Close;若属明确 bug 且有据(函数注释声明幂等)→ D-78-10 原子修 + 同 commit 回归用例)。
  - `TestInit78_Close_PartialInit`:只跑 initDBAndData + initMetrics 就 Close(半装配)→ 断言所有 nil 字段被跳过而非 nil-deref panic(Close 的每个 `if x != nil` 守卫分支)。这是 Close 60 stmts 里最容易漏的一批分支。
  - `TestInit78_Close_NoGoroutineLeak`:Init → Close → 断言收尾。零新依赖口径(D-78-03 不引入 goleak):
    * 用 `runtime.NumGoroutine()` 在 Init 前 / Close 后各取一次,配合"轮询 ≤5s 等待回落到基线 + 容差 N"的宽松断言(容差与已知常驻 worker 清单写进注释与 SUMMARY);
    * 并以 `go test -race -count=1 ./internal/core/` 全绿作为硬门槛。
    宽松断言口径是防 flake 的必要让步(参见记忆 local-vs-ci-test-divergence),不得写成精确相等。
  - `TestInit78_ReaperAndRPA`:直调 initRPAAndAPIAndReaper → 断言 RPA 服务/APIEndpointService 非 nil、reaperCancel 非 nil;调 reaperCancel 后不 panic;补 initAPIEndpointService / initAuthFactory / initRPAServices / registerRPATasks / initRPAScalingService 的直调用例(各自的 nil 依赖早退分支 + happy path)。
  - `TestInit78_StartSubprocessReaper_Platform`:直调 startSubprocessReaper(当前 windows 实现 0.0%)→ 断言不 panic 且可被 ctx cancel 收尾。该函数按 GOOS 分文件,测试只需覆盖当前平台实现,另一平台由 CI(ubuntu)覆盖 —— **本用例必须平台无关地通过**(禁 GOOS 条件跳过),差异语义写 SUMMARY。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestInit78_(Close|Reaper|StartSubprocess)" -timeout 300s ./internal/core/ && go test -race -count=1 ./internal/core/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestInit78_Close" internal/core/core_init_78_02_test.go` ≥ 4;存在 `TestInit78_ReaperAndRPA` 与 `TestInit78_StartSubprocessReaper_Platform`
  - Close 用例全部有硬超时守卫;半装配用例覆盖 Close 的 nil-守卫分支
  - goroutine 断言为"轮询 + 容差"宽松口径(测试文件中无 `runtime.NumGoroutine()` 的精确相等断言)
  - `go test -race -count=1 ./internal/core/` exit 0
  </acceptance_criteria>
  <done>Close 60 stmts 的顺序/幂等/nil-守卫/半装配四类分支全覆盖;reaper 与 RPA 装配段有测试;goroutine 收敛有可复现口径。</done>
</task>

<task type="auto">
  <name>Task 5: 边角补齐 + BLOCK-03 收口 checkpoint(core.go ≥60% / 包 ≥70%)</name>
  <files>internal/core/core_init_78_02_test.go</files>
  <read_first>
  - internal/core/core.go — :111 GetDB / :120 New / :923 checkEmptyAccountPoolOnStartup / :951 GetAuthFactory / :1120 parseDuration / :51 loadConnectionPoolConfig
  - .github/scripts/check-coverage.sh — :242 `P2_RATCHET_internal_core="39.00"`(本 plan 后该包必须实测 ≥70%,豁免行删除留 Phase 81)
  - .planning/coverage-baseline.md — ratchet 数据链回填位置(SUMMARY 要给出可回填数字)
  </read_first>
  <action>
  同文件追加边角用例并做收口测量:

  - `TestInit78_New_ErrorPaths`:SM4Key 为空 → New 返回 error 且文案含 "SM4_KEY 未配置";SM4Key = 仓库默认值 `dGVzdC1zZWNyZXQxNiEhIQ==` → 断言**仍能构造成功**(仅 ERROR 日志,不中止 —— core.go:167-190 的明确设计);SM4Key 非法 base64 → error;JWT 配置非法 → error。
  - `TestInit78_GetDB_NilBranch`:`(&Core{CoreInfra: &CoreInfra{}, CoreServices: &CoreServices{}}).GetDB()` → nil(补 :111 的 33.3% unc)。
  - `TestInit78_Misc`:loadConnectionPoolConfig / parseDuration / checkEmptyAccountPoolOnStartup / GetAuthFactory 的剩余分支(多为已 100%,只补未覆盖形态,先跑 cover -func 确认再补,禁写无覆盖增益的重复用例)。

  收口 checkpoint(BLOCK-03):
  ```
  go test -count=1 -coverprofile=cov78_02.out ./internal/core/
  go tool cover -func=cov78_02.out | grep -E "core.go|^total"
  go test -race -count=1 ./internal/core/
  go test ./...
  ```
  把 core.go 逐函数覆盖率、包总覆盖率写入 SUMMARY,并显式对照:
  - core.go ≥60%(Init 302 + Close 60 半数以上)
  - `internal/core` 包 ≥70%(BLOCK-03 达标;基线 43.7%)
  - 与 `P2_RATCHET_internal_core="39.00"` 的对照结论(实测 ≥70% ⇒ 豁免行可在 Phase 81 删除)
  若包总覆盖率未达 70%,SUMMARY 必须给出**逐文件缺口清单 + 差多少 stmts**,并明确指出缺口是否落在"探针结论 B/C 界定的不可达装配段"(若是,属已知边界,由 Phase 79/80 长尾承接;若否,本 plan 需补测试而非降低目标)。
  </action>
  <verify>
    <automated>go test -count=1 ./internal/core/ && go test -race -count=1 ./internal/core/ && go test -count=1 -coverprofile=cov78_02.out ./internal/core/ && go tool cover -func=cov78_02.out | grep -E "core.go|^total" && go build ./... && go test ./... 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestInit78_" internal/core/core_init_78_02_test.go` ≥ 20(Task 1-5 合计)
  - `go tool cover -func=cov78_02.out | grep "core.go"` 中 Init/Close 两函数均 >0%,core.go 加权 ≥60%
  - `go tool cover -func=cov78_02.out | grep "^total"` ≥ 70.0%(BLOCK-03 收口);未达则 SUMMARY 有逐文件缺口清单 + 归因
  - `go test -race -count=1 ./internal/core/` exit 0;`go test ./...` exit 0;`go build ./...` exit 0
  - `git diff --stat` 生产 .go 改动 = 0 文件(或仅 D-78-03 (a) / D-78-10 修复 + SUMMARY deviation)
  </acceptance_criteria>
  <done>internal/core ≥70%,BLOCK-03 收口;core.go Init/Close 装配链首次有测试;P2_RATCHET_internal_core 对照结论落 SUMMARY 供 Phase 81 删豁免行。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 ↔ 数据库 | glebarez sqlite(t.TempDir 文件库),禁生产 PG DSN |
| 测试 ↔ Redis | 仅 miniredis.RunT(mr.Addr()),禁真实 Redis 地址 |
| 测试 ↔ 子进程 | reaper 相关只覆盖启动/取消,禁 spawn 真实外部命令(INFRA-04 re-exec 模式如需则用 TestHelperProcess) |
| 测试 ↔ 生产代码 | 仅新增 *_test.go;生产改动仅 D-78-03 (a) Close 顺序修复 / D-78-10 quirk 修复豁免,均需原子 commit + SUMMARY deviation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-02-01 | DoS | Init/Close hang 卡死 CI(R1,历史 core.go:725-732 死等) | mitigate | 每处 Init/Close 调用均有硬超时守卫 channel + `-timeout 300s`;探针任务专门界定 hang 点 |
| T-78-02-02 | DoS | goroutine 泄漏(scheduler cron / device pool cleanup / L2Writer / refreshView / reaper) | mitigate | 每阶段 t.Cleanup 显式 Stop/Close + `go test -race` 硬门槛 + NumGoroutine 宽松回落断言 |
| T-78-02-03 | Tampering | 测试误连生产 PG/Redis 或误起真实调度任务打外部系统 | mitigate | 全 sqlite + miniredis;Server 端口 0;测试文件 grep 无生产 DSN/域名 |
| T-78-02-04 | Information Disclosure | SM4/JWT 用真实生产密钥 | mitigate | 仅测试字面量;显式覆盖"仓库默认 SM4Key 仅告警放行"的既有设计,不引入新真实 key |
| T-78-02-05 | Tampering | 为覆盖率擅改 Close 顺序引入生产回归 | mitigate | D-78-03 阶梯:先 (c) 测试侧界定 → 仅必要时 (a) 修 + 原子 commit + 回归用例 + SUMMARY deviation |
| T-78-02-SC | Tampering | 依赖安装 | accept | 零新增依赖(miniredis 已在 go.mod;不引入 goleak) |
</threat_model>

<verification>
- `go test -count=1 -timeout 300s ./internal/core/` 全绿(无 hang / 无 panic 逃逸)
- `go test -race -count=1 ./internal/core/` 全绿
- `go tool cover -func=cov78_02.out`:core.go ≥60%,包 total ≥70%
- SUMMARY 含 `## Probe Findings`(子系统清单 + Init/Close 耗时 + 结论 A/B/C)与 P2_RATCHET_internal_core 对照结论
- `go build ./...` + `go test ./...` exit 0;生产 .go 改动 = 0(或有 SUMMARY deviation)
</verification>

<success_criteria>
- Init 深度探针实验有可执行测试与明确结论(Gap G1 / DQ3 关闭),结论抄录 SUMMARY
- 8 个 initXxx 阶段 + Close 四类分支 + initCache 两分支 + reaper/RPA 段各有测试,TestInit78_ ≥20 全绿
- core.go ≥60%;`internal/core` ≥70% → BLOCK-03 达标
- 无 goroutine 泄漏(-race 绿 + NumGoroutine 宽松回落),无 hang
</success_criteria>

<decision_audit>
| ID | 决策 | 依据 / 理由 |
|----|------|------------|
| D-78-01 | 探针与常规用例默认走 `NewMultiLevelCacheSimple`(无 L2Writer),另设**一个**用例专测生产 WithWriter 形态的 Close 收尾 | 78-RESEARCH DQ1 推荐"(a) 探针 + (b) 单独验证测试,两者都包含";Simple 变体已在 pkg/cache/redis.go:547-555 存在,R-7 内建缓解 |
| D-78-03 | Close hang / 泄漏处置阶梯 = (c) 测试侧界定 → (a) 修 Close 顺序 → (b) scheduler ctx-aware Stop 留 Phase 79/80;且 (c) **不引入 goleak**,改用 `runtime.NumGoroutine()` 轮询 + 容差 + `-race` 硬门槛 | 78-RESEARCH DQ3 推荐"先 (c) 验证问题范围";不引入 goleak 是对 v1.27 "零新依赖主推线"的坚持(REQUIREMENTS INFRA 只批了 miniredis/httpmock 两个 test-only 依赖) |
| D-78-02a | sqlite 用 **t.TempDir 文件库**而非 `:memory:` | Init 链跨多处 GORM 会话/连接;`:memory:` 在多连接下各自建库会造成"表凭空消失"的假失败(仓内 addomain 测试已有 `file:...?mode=memory&cache=shared` 变通先例) |
| D-78-02b | Init 探针的**通过条件是"给出结论且不 hang"**,而非"Init 必须返回 nil" | 探针是研究缺口的替代品(ROADMAP L211 明示);把"Init 必成功"写成硬验收会让 executor 为过测而改生产装配代码,与 D-78-10"无据不改"冲突 |
| D-78-02c | `startSubprocessReaper` 只覆盖当前平台实现,**禁用 GOOS 条件 t.Skip** | Phase 77 SC#3 已确立"Windows 本地与 ubuntu CI 覆盖率差 <2pp / env-branch divergence 消除"原则;条件跳过会重新制造本地/CI 分歧(参见记忆 local-vs-ci-test-divergence) |
| D-78-08 / D-78-10 / D-78-11 | 命名 / quirk 纪律 / wave 编排 | 同 78-01,不重复论证 |

未采纳:78-RESEARCH 提到的 goleak.ExpectedGoroutines(DQ3 选项 c 的具体实现)——改用 stdlib NumGoroutine + -race,理由见 D-78-03。
</decision_audit>

<output>
Create `.planning/workstreams/milestone/phases/78-block-bp-unlock-by-foundation/78-02-SUMMARY.md` when done
</output>
