---
phase: 78-block-bp-unlock-by-foundation
plan: 04
type: execute
wave: 2
depends_on: [78-03]
files_modified:
  - internal/device/snmp_fake_server_78_04_test.go
  - internal/device/snmp_client_78_04_test.go
  - internal/device/task_scheduler_78_04_test.go
autonomous: true
requirements: [BLOCK-04]

must_haves:
  truths:
    - "本 plan 收口 BLOCK-04:交付 snmp_client.go(基线实测 Connect/Close/Get/GetNext/Walk/GetBulk/parseSNMPValue/GetSystemInfo/ScanDevice 全 0%;extractByPattern 15.2%;extractH3C/Ruijie/Maipu/Generic 全 0%)+ task_scheduler.go(executeTask 42.9% / recordCompletion 0% / SubmitAndWait 77.8%)测试,零真实设备 / 零外网 UDP"
    - "Task 1 是 UDP 对端探针实验:先证「127.0.0.1 本地 UDP 监听 + gosnmp 自身公开编解码器(SnmpDecodePacket / SetRequestID / SnmpEncodePacket)」能完成一次 GET 往返,再据结论展开 Task 2-4。禁跳过探针直接写覆盖测试"
    - "按 D-78-06(planner 新决策,替代 78-RESEARCH Gap G3 的手写 BER 方案):不手写 BER 编码、不实现 net.PacketConn,改用 net.ListenUDP(127.0.0.1:0) 真实回环监听 + gosnmp v1.35.0 的公开 API 解请求/造响应。gosnmp 的 Connect() 只是 dial 本地 UDP,无需任何接口抽象;这把 RESEARCH 估算的 80+ 行手写 BER 降到 ~50 行且零协议风险"
    - "按 D-78-02(源自 78-RESEARCH DQ2 + R3 的风险裁决):snmp_client.go 目标 ≥50%(轻量线),优先级 Get/GetNext/Connect/Close/parseSNMPValue/型号提取长尾 > Walk/GetBulk;Walk 的多轮 GETNEXT 序列与 GetBulk 的多 varbind 响应若在探针中受阻,按 fallback 只覆盖其错误路径并落 SUMMARY,不硬上多 PDU 编码。device 包 ≥70% 的缺口由 78-03 的三大件盈余承担"
    - "task_scheduler 复用 78-03 落地的 seedPool78 预置池手法(D-78-05):executeTask 的 GetConnection 成功路径经预置 FileTransport 连接走通(recordCompletion 的唯一入口),失败路径用空池;所有建调度器用例必须 t.Cleanup(sched.Stop()) 防 worker goroutine 泄漏"
    - "按 D-78-09 device 测试纪律(禁 t.Parallel;var 注入 orig+t.Cleanup;覆盖后禁 t.Fatal);按 D-78-08 命名(`<source>_78_04_test.go`,fake server helper 也必须是 `_test.go` 而非普通 .go —— 测试专用代码不入生产二进制);按 D-78-10 quirk 纪律;按 D-78-11 wave 编排(本 plan wave 2,同包 78-03 已落盘)"
    - "达标口径(2026-08-27 本地实测基线 internal/device 43.5%):snmp_client.go ≥50%、task_scheduler.go ≥70%、internal/device 包 total ≥70%(BLOCK-04 收口);check-coverage.sh:243 的 P2_RATCHET_internal_device=38.50 豁免行对应包实测 ≥70%(豁免行删除留 Phase 81);go build ./... + go test ./... + go test -race ./internal/device/ 全绿"
    - "本 plan 裁决细则(Decision Audit 逐条依据,executor 必须遵守):D-78-04a(fake 应答器写成 `_test.go` 而非普通 .go,测试专用 UDP 服务器不入生产二进制)/ D-78-04b(SubmitAndWait 的 `Timeout+1min` 兜底与 removeConnection 30s 分支接受不覆盖,延用 78-03 的 D-78-03b 口径)/ D-78-04c(`closeLocked` 的 nil-Conn 形态是 D-78-10 判定点:有据则修 + 同 commit 翻转断言 + 回归用例,无据则 require.Panics 按现行为断言 + SUMMARY 待裁决)"
  artifacts:
    - path: "internal/device/snmp_fake_server_78_04_test.go"
      provides: "本地回环 UDP SNMP 应答器 helper(net.ListenUDP + gosnmp 公开编解码器),支持按 OID 预设响应 / 错误码 / 静默丢包"
      min_lines: 90
      contains: "net.ListenUDP"
    - path: "internal/device/snmp_client_78_04_test.go"
      provides: "SNMPClient 全方法测试(前缀 TestSN78_)+ parseSNMPValue 与型号提取长尾表驱动"
      min_lines: 320
      contains: "newFakeSNMPServer78"
    - path: "internal/device/task_scheduler_78_04_test.go"
      provides: "DeviceTaskScheduler executeTask/Submit/SubmitAndWait/Stop/worker 生命周期测试(前缀 TestTS78_)"
      min_lines: 220
      contains: "seedPool78"
  key_links:
    - from: "internal/device/snmp_fake_server_78_04_test.go"
      to: "gosnmp v1.35.0 公开 API"
      via: "SnmpDecodePacket 解请求 → SetRequestID(req.RequestID) → SnmpEncodePacket(gosnmp.GetResponse, pdus, 0, 0)"
      pattern: "SnmpEncodePacket|SnmpDecodePacket"
    - from: "internal/device/snmp_client_78_04_test.go"
      to: "NewSNMPClient (snmp_client.go:49)"
      via: "SNMPClientConfig{Target: \"127.0.0.1\", Port: fake.Port()}"
      pattern: "127\\.0\\.0\\.1"
    - from: "internal/device/task_scheduler_78_04_test.go"
      to: "executeTask (task_scheduler.go:189)"
      via: "seedPool78 预置 FileTransport 连接 → Submit → GetConnection 复用路径命中"
      pattern: "seedPool78"
---

<objective>
收口 BLOCK-04:给 snmp_client.go 建一个本地回环 UDP 应答器(用 gosnmp 自身的公开编解码器,不手写 BER),覆盖 Connect/Close/Get/GetNext(力争 Walk/GetBulk)与 parseSNMPValue/型号提取长尾;并用 78-03 落地的预置池手法把 task_scheduler 的 executeTask 剩余分支补齐,使 `internal/device` 包 total ≥70%。

Purpose: BLOCK-04 的第二段与收口段。78-03 已把 scrapli 三大件推到目标,本 plan 负责 snmp(237 unc,轻量线 D-78-02)+ task_scheduler(25 unc)并做包级达标测量与 P2_RATCHET 对照。
Output: 新增 3 个测试文件(零生产代码改动,除非触发 D-78-10 quirk 修复)。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/78-block-bp-unlock-by-foundation/78-RESEARCH.md
@.planning/workstreams/milestone/phases/78-block-bp-unlock-by-foundation/78-03-SUMMARY.md

<interfaces>
<!-- 关键契约(planner 已读源码 + go doc gosnmp 复核,2026-08-27)。 -->

snmp_client.go 结构(:18-25):
```go
type SNMPClient struct {
    client  *gosnmp.GoSNMP   // 具体类型,无接口抽象 —— 因此走真实 UDP 回环而非 stub
    mu      sync.RWMutex
    ready   bool
    readyMu sync.RWMutex
}
// :49  NewSNMPClient(config *SNMPClientConfig) —— Target/Port/Community/Version/Timeout/Retries;config==nil 走默认值分支
// :73  connectLocked  → c.client.Connect()(dial UDP,不发包)
// :82  Connect        → connectLocked + setReady(true)
// :111 closeLocked    → c.client.Conn.Close()
//        ⚠️ 只判了 c.client != nil 却直接取 c.client.Conn —— 未 Connect / 二次 Close 时 Conn 为 nil → 可能 nil-deref
// :153 Get / :188 GetNext / :224 Walk / :255 GetBulk —— 每个方法内含 recover(panic 时打印 debug.Stack)
// :290 parseSNMPValue —— gosnmp.SnmpPDU 的类型分派,纯函数零网络(单函数最大纯逻辑块)
// :340 GetSystemInfo / :778 ScanDevice —— 组合调用 Get/GetNext
// :400 ExtractModelFromSysDescr(44.4%,Q-3 已在 Phase 75 修正锚定行为 —— 断言新行为)
// :424 extractHuaweiModel(80%) / :446 extractH3CModel(0) / :466 extractRuijieModel(0)
// :486 extractMaipuModel(0) / :505 extractGenericModel(0) / :523 extractByPattern(15.2%)
// :381 DetectDeviceType(57.1%) / :685 PingCheck(83.3%) / :699 ScanIPRange(100%)
// :727 nextIP(91.7%,Q-2/Q-8 全零形态已在 Phase 75 修) / :811 ConvertPortToInt(100%)
```

gosnmp v1.35.0 可用于造应答的公开 API(planner 已 `go doc` 确认存在):
```go
func (x *GoSNMP) SnmpDecodePacket(resp []byte) (*SnmpPacket, error)
func (x *GoSNMP) SnmpEncodePacket(pdutype PDUType, pdus []SnmpPDU, nonRepeaters uint8, maxRepetitions uint32) ([]byte, error)
func (x *GoSNMP) SetRequestID(reqID uint32)
func (x *GoSNMP) SetMsgID(msgID uint32)
type SnmpPacket struct { /* RequestID / Version / Community / PDUType / Variables []SnmpPDU / Error / ErrorIndex ... */ }
type SnmpPDU struct { Name string; Type Asn1BER; Value interface{} }
```

应答器骨架(D-78-06 目标形态,~50 行):
```text
newFakeSNMPServer78(t *testing.T, table []gosnmp.SnmpPDU) *fakeSNMPServer78
  1. pc, _ := net.ListenUDP("udp", &net.UDPAddr{IP: net.IPv4(127,0,0,1), Port: 0})   // 端口 0 = 自动分配
  2. t.Cleanup(pc.Close)
  3. goroutine 读循环:n, addr, _ := pc.ReadFrom(buf)
       decoder := &gosnmp.GoSNMP{Version: gosnmp.Version2c, Community: "public"}
       req, err := decoder.SnmpDecodePacket(buf[:n])
       按 req.PDUType(GetRequest / GetNextRequest / GetBulkRequest)与 req.Variables[0].Name 在 table 中定位
       enc := &gosnmp.GoSNMP{Version: gosnmp.Version2c, Community: "public"}
       enc.SetRequestID(req.RequestID)   // 必要时 enc.SetMsgID(req.MsgID)
       out, _ := enc.SnmpEncodePacket(gosnmp.GetResponse, pdus, 0, 0)
       pc.WriteTo(out, addr)
     pc.Close() 导致 ReadFrom 返回错误时退出循环(禁 goroutine 泄漏)
  4. 方法:Port() uint16 / Addr() string / RequestCount() int(atomic,供断言重试次数)/ SetBehavior(...)
  5. 三种可注入行为:normal(正常应答)/ errorStatus(响应 Error != NoError)/ drop(静默丢包,测 Timeout+Retries)
  6. table 用有序 OID 切片实现 GETNEXT 语义:返回第一个 > 请求 OID 的条目(Walk 依赖)
```
⚠️ 若 `SnmpEncodePacket` 的内部 msgID/requestID 语义与请求不匹配导致客户端丢弃响应,探针任务必须记录确切现象并尝试 `SetMsgID`、以及"decoder 与 encoder 复用同一个 *gosnmp.GoSNMP 实例"的变体;仍不通则按 D-78-02 fallback。

task_scheduler.go 剩余缺口(78-03 已读全文,本 plan 关注):
```go
// :104 Submit(95.7%)      —— !IsEnabled / task==nil / 队列不存在则建队+startWorker / 队列满 default 分支
// :156 startWorker(94.1%) —— select 队列 / workerCtx.Done;defer 内 recover + 清理 workers/workerCancel
// :189 executeTask(42.9%) —— GetConnection 失败(recordFailure + Callback(err))/ 成功 → defer ReleaseRef
//                            → task.Execute(带 recover)→ recordFailure|recordCompletion → Callback
// :260 SubmitAndWait(77.8%) —— resultCh / ctx.Done / time.After(task.Timeout+1min) 兜底
// :355 recordCompletion(0%) —— 只在 executeTask 成功路径被调 → 依赖 seedPool78 预置池
```

复用 78-03 已落盘的同包 helper(**勿重复定义**):`newPool78` / `seedPool78` / `newSW78` / `factoryFixturePath` / `testdata/huawei_vrp_ops.fixture`。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UDP 对端探针实验(本地回环 + gosnmp 公开编解码器,证一次 GET 往返)</name>
  <files>internal/device/snmp_fake_server_78_04_test.go, internal/device/snmp_client_78_04_test.go</files>
  <read_first>
  - internal/device/snmp_client.go — :18-152(struct/config/NewSNMPClient/connectLocked/Connect/setReady/isReady/closeLocked/Close/WaitForReady)+ :153-223 Get/GetNext
  - internal/device/snmp_client_test.go — 既有 135 行(仅 WaitForReady 覆盖),避免重复
  - `go doc github.com/gosnmp/gosnmp GoSNMP` / `SnmpPacket` / `SnmpPDU` / `Asn1BER` / `PDUType` —— planner 已确认方法集,executor 需确认字段与常量名细节
  - .planning/workstreams/milestone/phases/78-block-bp-unlock-by-foundation/78-RESEARCH.md — Gap G3 / R3 / DQ2
  </read_first>
  <action>
  第一步:创建 `internal/device/snmp_fake_server_78_04_test.go`(package device,**必须 _test.go**:测试专用代码不得进生产二进制)。按 interfaces 的骨架实现 `fakeSNMPServer78` —— 字段 `pc *net.UDPConn` / `table []gosnmp.SnmpPDU` / `reqCount int64`(atomic)/ `behavior`;方法 `Port()` / `Addr()` / `RequestCount()` / `SetBehavior()` / `Close()`(t.Cleanup 自动调)。读循环按 PDUType 分派,GETNEXT 用有序 table 实现"下一个 OID"。

  第二步:创建 `internal/device/snmp_client_78_04_test.go`,写探针测试 `TestSN78_Probe_GetRoundTrip`:
  1. fake 预置一条 `1.3.6.1.2.1.1.1.0`(sysDescr)= OctetString `"Huawei VRP dummy"`。
  2. `c := NewSNMPClient(&SNMPClientConfig{Target: "127.0.0.1", Port: fake.Port(), Community: "public", Version: SNMPVersion2c, Timeout: 500*time.Millisecond, Retries: 1})`
  3. `require.NoError(t, c.Connect())`;断言 `c.isReady()` 为 true。
  4. `v, err := c.Get("1.3.6.1.2.1.1.1.0")` → **探针主断言**:err==nil 且 v 的字符串形态含 `"Huawei"`;`fake.RequestCount() == 1`。
  5. `require.NoError(t, c.Close())`;断言 isReady false。
  6. 用例整体 5s 硬超时守卫;`t.Logf` 打印往返耗时与 RequestCount。

  第三步:探针结论分流(写进 SUMMARY 的 `## Probe Findings`):
  - **结论 A(往返成功)**:Task 2/3 按计划展开,力争 Walk/GetBulk,snmp_client.go 目标 ≥50%。
  - **结论 B(响应被客户端丢弃,如 requestID/msgID 不匹配)**:记录确切现象 + 已尝试的修法(SetRequestID / SetMsgID / Version+Community 对齐 / decoder 与 encoder 复用同实例);若仍失败 → 按 D-78-02 fallback,snmp 只覆盖**不需要成功响应**的部分(NewSNMPClient 默认值分支、Connect 到已关闭端口、drop behavior 的超时+Retries 路径、parseSNMPValue 表驱动、型号提取长尾、PingCheck/ScanIPRange/nextIP 剩余、DetectVendor/DetectDeviceType),目标降为 ≥40%,并在 SUMMARY 用 78-03 实测盈余数字论证 device 包 ≥70% 仍成立。
  - **结论 C(fake goroutine 泄漏 / -race 告警)**:修 helper(非生产代码),`-race` 必须绿后才进 Task 2。
  </action>
  <verify>
    <automated>go test -count=1 -v -run "TestSN78_Probe_GetRoundTrip" -timeout 120s ./internal/device/ &amp;&amp; go test -race -count=1 -run "TestSN78_Probe" ./internal/device/ &amp;&amp; go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `internal/device/snmp_fake_server_78_04_test.go` 存在且含 `net.ListenUDP` 与 `SnmpEncodePacket`;文件名以 `_test.go` 结尾
  - `grep -c "asn1-ber" internal/device/snmp_fake_server_78_04_test.go` == 0(D-78-06:不手写 BER、不引入 asn1-ber)
  - `TestSN78_Probe_GetRoundTrip` 存在并通过(允许以 fallback 结论收尾,但测试本身必须绿且不 hang)
  - fake 监听地址只出现 `127.0.0.1`;`go test -race -run TestSN78_Probe ./internal/device/` exit 0
  - SUMMARY 的 `## Probe Findings` 记录结论 A/B/C + 往返耗时 + RequestCount
  </acceptance_criteria>
  <done>UDP 对端可行性有实证结论(Gap G3 / R3 / DQ2 关闭);fake 应答器 helper 就位,后续任务覆盖目标按结论定档。</done>
</task>

<task type="auto">
  <name>Task 2: Connect/Close/Get/GetNext 全链 + 超时重试 + 错误状态分支</name>
  <files>internal/device/snmp_client_78_04_test.go</files>
  <read_first>
  - internal/device/snmp_client.go — :49-152(构造/连接/关闭/WaitForReady)+ :153-223(Get/GetNext,含 recover 与 varbind 提取)
  - internal/device/snmp_client_78_04_test.go — Task 1 的探针与 fake helper
  </read_first>
  <action>
  同文件追加 TestSN78_ 用例(网络目标只允许 fake 应答器或本地已关闭端口):

  - `TestSN78_NewSNMPClient_Defaults`:config==nil → 断言默认 Port=161 / Community="public" / Version=v2c / Timeout=5s / Retries=3(:50-58);config 全字段 → 逐字段透传断言。
  - `TestSN78_Connect_Unreachable`:Target=127.0.0.1 + Port 指向已关闭的本地端口(先起 fake 再 Close)→ **按现行为断言**:UDP 无连接语义,Connect 很可能返回 nil;若如此则断言 nil 并在注释说明"真正的失败在后续 Get 超时暴露"(D-78-10 的"按现行为断言"典型)。
  - `TestSN78_Get_Timeout_WithRetries`:fake `SetBehavior(drop)` + Timeout=100ms + Retries=2 → 断言 Get 返回超时错误,且 `fake.RequestCount() == 3`(1 + 2 重试,证明 Retries 语义)。用例整体 3s 硬超时守卫。
  - `TestSN78_Get_ErrorStatus`:fake `SetBehavior(errorStatus)`(响应 Error != NoError)→ 按 :153-187 对 packet.Error 的实际处理断言。
  - `TestSN78_Get_EmptyVarbinds`:fake 返回 0 个 varbind → 断言 :170+ 的空结果分支(报错或 nil,按现行为)。
  - `TestSN78_GetNext`:fake table 三条有序 OID(1.3.6.1.2.1.1.1.0 / .1.2.0 / .1.3.0)→ `GetNext(".1.1.0")` 返回第二条的值;对末端 OID 调 GetNext → 断言 endOfMibView / 空 / 错误的实际终止语义。
  - `TestSN78_Close_AfterConnect_And_Idempotent`:Connect → Close → 断言 nil + isReady false;再 Close 一次 → **D-78-10 判定点**:closeLocked(:111)只判 `c.client != nil` 却直接取 `c.client.Conn.Close()`。若二次 Close(或未 Connect 即 Close)nil-deref panic,依"函数命名与注释暗示安全关闭"判为有据 quirk → 原子 commit 修(nil 守卫)+ 同 commit 翻转断言 + 回归用例 + SUMMARY deviation;若判定无据,则 `require.Panics` 按现行为断言并 SUMMARY 记待裁决。
  - `TestSN78_WaitForReady_TimeoutBranch`:未 Connect → `WaitForReady(80ms)` 断言"等待连接就绪超时"(补既有测试未覆盖分支)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestSN78_" -timeout 180s ./internal/device/ &amp;&amp; go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestSN78_" internal/device/snmp_client_78_04_test.go` ≥ 9(含 Task 1 探针)
  - 存在断言 `RequestCount() == 3` 的重试语义用例(Retries=2)
  - 所有网络目标为 `127.0.0.1`;无外网/真实设备 IP;超时参数毫秒级(单例 <5s)
  - `go test -count=1 -run "TestSN78_" -timeout 180s ./internal/device/` exit 0
  </acceptance_criteria>
  <done>Connect/Close/Get/GetNext 主链 + 超时重试 + 错误状态 + 空 varbind 分支覆盖;Close 的 nil-Conn 语义有明确裁决记录。</done>
</task>

<task type="auto">
  <name>Task 3: parseSNMPValue 表驱动 + 型号提取长尾 + Walk/GetBulk/GetSystemInfo/ScanDevice(按探针结论)</name>
  <files>internal/device/snmp_client_78_04_test.go</files>
  <read_first>
  - internal/device/snmp_client.go — :224-289(Walk/GetBulk)+ :290-339 parseSNMPValue + :340-359 GetSystemInfo + :360-399(DetectVendor/DetectDeviceType)+ :400-546(ExtractModelFromSysDescr + 5 个 extract*Model + extractByPattern)+ :685-752(PingCheck/ScanIPRange/ipToUint32/nextIP)+ :778 ScanDevice + :811 ConvertPortToInt
  - .planning/research/v1.27-pitfalls.md — Q-3(ModelExtractor 锚定已在 Phase 75 修正,有 ExtractModelFromSysDescr 回退 caller → **断言新行为**)/ Q-8(nextIP 全零形态与 ScanIPRange 循环条件同 commit 修过)
  - internal/device/snmp_client_78_04_test.go — Task 1/2 的 fake helper
  </read_first>
  <action>
  **A. 纯函数段(零网络,必做,不受探针结论影响)**:
  - `TestSN78_ParseSNMPValue_Table`:表驱动覆盖 gosnmp.SnmpPDU 全部类型分支(OctetString []byte / OctetString string / Integer / Counter32 / Counter64 / Gauge32 / TimeTicks / ObjectIdentifier / IPAddress / Null / NoSuchObject / NoSuchInstance / 未知类型 default),逐条断言返回值形态。这是 snmp_client.go 最大的纯函数块。
  - `TestSN78_ExtractModel_VendorMatrix`:每厂商 2-3 条真实形态 sysDescr → 分别直调 extractHuaweiModel / extractH3CModel / extractRuijieModel / extractMaipuModel / extractGenericModel 断言返回值;再经 `ExtractModelFromSysDescr` 顶层分派各走一遍。**Q-3 纪律**:断言 Phase 75 修正后的新锚定与回退语义;若与实现不符按 D-78-10 判定。
  - `TestSN78_ExtractByPattern_Table`:直调 extractByPattern(15.2% → ≥70%),表驱动覆盖各 pattern 命中/不命中/边界(空串、纯分隔符、模式在末尾)。
  - `TestSN78_DetectDeviceType_And_DetectVendor`:补 DetectDeviceType 的 57.1% 剩余分支(交换机/路由器/防火墙/未知);DetectVendor 补边界。
  - `TestSN78_PingCheck_And_ScanIPRange_And_NextIP`:PingCheck 用本地回环端口(可达/不可达各一);ScanIPRange 仅 `127.0.0.1-127.0.0.2` + 毫秒级 timeout;nextIP 补 91.7% 剩余(含 Q-8 的全零/进位边界)。**禁扫真实网段**。
  - `TestSN78_ConvertPortToInt`:边界(空串/非数字/超大值)。

  **B. 网络段(按 Task 1 探针结论)**:
  - 结论 A → `TestSN78_Walk_MultiOID`(fake table 3-5 条有序 OID → Walk 断言 walkFn 调用次数与顺序)、`TestSN78_GetBulk`(多 varbind 响应 → 断言 PDU 数)、`TestSN78_GetSystemInfo`、`TestSN78_ScanDevice`(组合链:断言 sysDescr/sysName/sysUpTime 等键存在)。
  - 结论 B → 上述四个用例降级为错误路径覆盖:fake `drop` behavior → Walk/GetBulk/GetSystemInfo/ScanDevice 各断言超时错误被正确包装;SUMMARY 明确记录降级与剩余 unc stmts 数。

  纪律:所有超时参数毫秒级;每用例 ≤5s;fake 应答器一律 t.Cleanup 关闭;禁 t.Parallel。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestSN78_" -timeout 180s ./internal/device/ &amp;&amp; go test -count=1 -coverprofile=/tmp/d78_04a.out ./internal/device/ &amp;&amp; go tool cover -func=/tmp/d78_04a.out | grep "snmp_client.go"</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestSN78_" internal/device/snmp_client_78_04_test.go` ≥ 16(Task 1-3 合计)
  - `go tool cover -func=/tmp/d78_04a.out`:parseSNMPValue ≥80%;extractByPattern ≥70%;extractH3CModel / extractRuijieModel / extractMaipuModel / extractGenericModel 均 >0%
  - snmp_client.go 加权 ≥50%(结论 A);结论 B 时 ≥40% 且 SUMMARY 有降级论证与包级达标论证
  - 测试文件无真实网段扫描(ScanIPRange 目标仅 127.0.0.x,范围 ≤4 个地址)
  </acceptance_criteria>
  <done>snmp_client.go 达到 D-78-02 定档目标;parseSNMPValue + 5 个型号提取器 + extractByPattern 长尾清欠;Walk/GetBulk 按探针结论覆盖或明确降级。</done>
</task>

<task type="auto">
  <name>Task 4: task_scheduler executeTask/Submit/SubmitAndWait/worker 生命周期(复用 seedPool78)+ BLOCK-04 收口 checkpoint</name>
  <files>internal/device/task_scheduler_78_04_test.go</files>
  <read_first>
  - internal/device/task_scheduler.go — 全文(:104 Submit / :156 startWorker / :189 executeTask / :260 SubmitAndWait / :289 Stop / :336-370 metrics)
  - internal/device/connection_pool_78_03_test.go — 78-03 落地的 `newPool78` / `seedPool78`(直接复用,勿重复定义)
  - internal/device/executor_78_03_test.go — 78-03 的 newExec78(调度器装配先例)
  - .github/scripts/check-coverage.sh — :243 `P2_RATCHET_internal_device="38.50"`(本 plan 后该包必须实测 ≥70%)
  - .planning/coverage-baseline.md — ratchet 数据链回填位置(SUMMARY 要给出可回填数字)
  </read_first>
  <action>
  创建 `internal/device/task_scheduler_78_04_test.go`(package device)。helper `newSched78(t) (*DeviceTaskScheduler, *DeviceConnectionPool, *gorm.DB)`:`newPool78(t)` → `NewDeviceTaskScheduler(pool, &SchedulerConfig{TaskTimeout: time.Second, QueueSize: 2})` → **t.Cleanup(func(){ sched.Stop() })**。前缀 TestTS78_:

  - `TestTS78_ExecuteTask_GetConnectionFail`:空池(不 seed)+ Submit → 断言 Callback 收到"获取连接失败"、`GetStats()` 的 TotalFailed +1、task.Execute **未**被调用(原子标记断言)。
  - `TestTS78_ExecuteTask_Success_RecordCompletion`(recordCompletion 0% 的唯一入口):`seedPool78` 预置 FileTransport 连接 → Submit 一个 Execute 内调 `conn.wrapper.SendCommand("display version", false)` 的任务 → 用 channel 等 Callback(**禁裸 sleep**)→ 断言 err==nil、TotalCompleted +1、**conn.refCount 回到预置值**(defer ReleaseRef 配对生效)。
  - `TestTS78_ExecuteTask_PanicRecovered`:Execute 内 `panic("boom")` → 断言 Callback 收到"任务执行 panic"、TotalFailed +1、worker **未死**(随后再 Submit 一个任务仍被执行 → 证明 startWorker 的 recover + 队列继续消费)。
  - `TestTS78_ExecuteTask_TaskTimeout`:task.Timeout=50ms + Execute 内 `select { case <-ctx.Done(): return ctx.Err() }` → 断言 Callback 收到 ctx 超时错误(证明 :199-205 中 task.Timeout>0 优先于 s.taskTimeout)。
  - `TestTS78_Submit_Branches`:`SetEnabled(false)` → "任务调度器未启用";`Submit(nil)` → "任务不能为空";QueueSize=1 且 worker 被一个阻塞 Execute 占住(channel 同步,禁裸 sleep)→ 第 3 个任务命中 default → "任务队列已满"。
  - `TestTS78_SubmitAndWait_Branches`:成功路径(seed 池 → 返回 nil);ctx 已 cancel → 返回 ctx.Err();Submit 失败(disabled)→ 直接透传。**`time.After(task.Timeout+time.Minute)` 兜底分支接受不覆盖**(≥60s 成本),SUMMARY 记录。
  - `TestTS78_Stop_And_WorkerCleanup`:Submit 后 Stop → 断言 `IsEnabled()==false`、后续 Submit 报未启用、workers/workerCancel map 被清空(**轮询 ≤2s + 宽松断言**,禁精确时序);再调一次 Stop → 按现行为断言(若 close 已关 channel panic,则 `require.Panics` + SUMMARY 记待裁决,D-78-10)。
  - `TestTS78_GetStats_And_GenerateTaskID`:统计四字段;generateTaskID 连调 100 次无重复。

  **收尾 checkpoint(BLOCK-04 收口)**:
  ```
  go test -count=1 -coverprofile=cov78_04.out ./internal/device/
  go tool cover -func=cov78_04.out | grep -E "snmp_client.go|task_scheduler.go|^total"
  go test -race -count=1 ./internal/device/
  go test ./...
  ```
  SUMMARY 必须给出:snmp_client.go / task_scheduler.go per-file 数字、`internal/device` 包 total、与 `P2_RATCHET_internal_device="38.50"` 的对照结论(实测 ≥70% ⇒ 豁免行可在 Phase 81 删除)。若包 total <70%,给出逐文件缺口清单 + 差多少 stmts + 归因(是否落在探针界定的不可达段:createConnection 的 SSH 段 / removeConnection 30s 超时段 / SubmitAndWait 60s 兜底段)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestTS78_" -timeout 180s ./internal/device/ &amp;&amp; go test -race -count=1 ./internal/device/ &amp;&amp; go test -count=1 -coverprofile=cov78_04.out ./internal/device/ &amp;&amp; go tool cover -func=cov78_04.out | grep -E "snmp_client.go|task_scheduler.go|^total" &amp;&amp; go build ./... &amp;&amp; go test ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestTS78_" internal/device/task_scheduler_78_04_test.go` ≥ 8
  - 存在 seedPool78 驱动的 executeTask 成功用例(recordCompletion 覆盖 >0%),且断言 refCount 配对
  - `go tool cover -func=cov78_04.out`:task_scheduler.go ≥70%;**`^total` ≥70.0%(BLOCK-04 达标)**;未达则 SUMMARY 有逐文件缺口清单 + 归因
  - 队列满/worker 清理用例用 channel 或轮询同步,测试文件无用于主断言路径的裸 `time.Sleep`
  - `go test -race -count=1 ./internal/device/` exit 0;`go test ./...` exit 0;`go build ./...` exit 0
  - `git diff --stat` 生产 .go 改动 = 0 文件(除非 D-78-10 quirk 修复 + SUMMARY deviation)
  </acceptance_criteria>
  <done>task_scheduler.go ≥70%;`internal/device` ≥70% → BLOCK-04 收口;P2_RATCHET_internal_device 对照结论落 SUMMARY 供 Phase 81 删豁免行。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 ↔ SNMP 网络 | 仅 `net.ListenUDP` 于 `127.0.0.1:0` 的进程内应答器;禁任何真实设备 IP / 外网 UDP / 真实 community |
| 测试 ↔ IP 扫描 | ScanIPRange / PingCheck 仅 127.0.0.x 且范围 ≤4 地址;禁扫真实网段(避免被当作扫描行为) |
| 测试 ↔ SSH | 复用 78-03 的 FileTransport fixture,零真实 SSH |
| 测试 ↔ 数据库 | glebarez sqlite :memory:(经 newPool78),禁生产 PG |
| 测试 ↔ 生产代码 | 仅新增 *_test.go;生产改动仅 D-78-10 quirk 修复豁免(如 closeLocked nil 守卫) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-04-01 | DoS | fake UDP 应答器 goroutine 泄漏 / ReadFrom 永久阻塞 | mitigate | pc.Close 触发 ReadFrom 错误后退出循环 + t.Cleanup 关闭 + `go test -race` 硬门槛 |
| T-78-04-02 | DoS | Get/Walk 的 Timeout×Retries 叠加拖长 CI | mitigate | 全部 timeout 毫秒级(≤500ms)、Retries ≤2、单例 ≤5s、`-timeout 180s` |
| T-78-04-03 | Spoofing | 测试误发 SNMP 报文到真实设备/网段(扫描行为) | mitigate | 目标只允许 127.0.0.1 与 fake.Port();验收 grep 无其他 IP 字面量;ScanIPRange 范围 ≤4 |
| T-78-04-04 | DoS | scheduler worker goroutine 泄漏(队列满/阻塞 Execute 用例) | mitigate | 每用例 t.Cleanup(sched.Stop());阻塞 Execute 必须由 channel 释放后退出 |
| T-78-04-05 | Tampering | 为凑覆盖率手写 BER / 引入 asn1-ber 直接依赖 | mitigate | D-78-06 明令用 gosnmp 公开编解码器;验收 grep 无 asn1-ber(go.mod 直接 require 块不得新增) |
| T-78-04-06 | Tampering | 探针失败后为达标改生产 snmp 逻辑 | mitigate | D-78-02 预设 fallback 目标档(≥40%)+ SUMMARY 论证包级达标,禁改生产 |
| T-78-04-SC | Tampering | 依赖安装 | accept | 零新增依赖(gosnmp v1.35.0 已在 go.mod:16;net/testing 为 stdlib) |
</threat_model>

<verification>
- `go test -count=1 -timeout 180s ./internal/device/` 全绿且无 hang;`go test -race -count=1 ./internal/device/` 全绿
- `go tool cover -func=cov78_04.out`:snmp_client.go ≥50%(fallback ≥40% 需论证)、task_scheduler.go ≥70%、包 total ≥70%
- SUMMARY 含 `## Probe Findings`(UDP 往返结论 A/B/C)+ P2_RATCHET_internal_device 对照结论 + 已知不覆盖清单
- 测试文件网络目标仅 127.0.0.1;go.mod 直接 require 块无新增条目
- `go build ./...` + `go test ./...` exit 0;生产 .go 改动 = 0(或有 SUMMARY deviation)
</verification>

<success_criteria>
- UDP 对端探针有实证结论并落 SUMMARY;fake 应答器为 _test.go 且零新依赖
- TestSN78_ ≥16 / TestTS78_ ≥8 全绿;recordCompletion 经 seedPool78 真链路覆盖
- snmp_client.go ≥50%(或 fallback ≥40% + 论证)、task_scheduler.go ≥70%
- **`internal/device` 包 total ≥70% → BLOCK-04 达标**,P2_RATCHET_internal_device 对照结论就绪
</success_criteria>

<decision_audit>
| ID | 决策 | 依据 / 理由 |
|----|------|------------|
| **D-78-06** | **snmp 对端用「真实 127.0.0.1 UDP 监听 + gosnmp 公开编解码器(SnmpDecodePacket / SetRequestID / SnmpEncodePacket)」,不实现 net.PacketConn、不手写 BER、不引入 asn1-ber** | 78-RESEARCH Gap G3 推荐 Path A(fakePacketConn 30 行 + 手写 BER 50 行),并把 BER 编码列为 HIGH 风险 R3。planner `go doc github.com/gosnmp/gosnmp` 确认 v1.35.0 已公开 `SnmpDecodePacket` / `SnmpEncodePacket` / `SetRequestID` / `SetMsgID` —— 用**被测库自身的编解码器**造响应,协议正确性由 gosnmp 自身保证,风险从"手写 ASN.1 BER"降为"字段对齐";且 gosnmp.Connect() 只是 dial UDP,无需替换 PacketConn(RESEARCH 假设需要接口抽象是多余的)。asn1-ber 目前只是 go.mod:42 的 indirect,直接 import 会把它提升为 direct require —— 本决策同时避免了该依赖面变动 |
| **D-78-02** | **snmp_client.go 目标定 ≥50%(轻量线),Walk/GetBulk 受阻即降级至错误路径覆盖(≥40%),包级 ≥70% 由 78-03 盈余承担** | 78-RESEARCH DQ2 把"≥70% 全覆盖"与"轻量线 + Walk/GetBulk 留 Phase 79"两个选项交给 planner 定案,R3 明确"切勿硬上 multi-PDU BER 编码"。定轻量线的理由:snmp 是 BLOCK-04 里唯一 greenfield 的对端,而 78-03 三大件(scrapli_wrapper 222 + connection_pool 144 + executor 124 = 490 stmts)按其目标达成后已为包级 ≥70% 留出足够盈余;把风险预算花在 snmp 上不划算 |
| D-78-04a | fake 应答器写成 `snmp_fake_server_78_04_test.go`(_test.go),而非 mission 描述的 `snmp_fake_test_helper.go`(普通 .go) | 普通 .go 会把测试专用 UDP 服务器编进生产二进制,与 INFRA-05(测试隔离治理)背道而驰;`_test.go` 同包可见性完全够用(e2e_helpers.go 之所以是普通 .go,是因为它需要**跨包**被 portwrite 调用,本 helper 无此需求) |
| D-78-04b | SubmitAndWait 的 `task.Timeout+1min` 兜底分支、removeConnection 的 30s 超时分支**接受不覆盖** | 与 78-03 的 D-78-03b 同一裁决口径:显式超时兜底逻辑的单测时间成本与收益不成比例;SUMMARY 记录剩余 stmts |
| D-78-04c | `closeLocked` 的 nil-Conn 形态是 D-78-10 的**判定点**:有据则修(原子 commit + 翻转断言 + 回归用例),无据则 require.Panics 按现行为断言 | snmp_client.go:111 只判了 `c.client != nil` 却直取 `c.client.Conn.Close()`;这类"守卫判错层级"在 Phase 75 QUIRK 批次里属典型 quirk 形态,但本 phase 不预判结论,交由 executor 依据源码注释/文档裁决 |
| D-78-08 / D-78-09 / D-78-10 / D-78-11 | 命名 / device 注入与并行纪律 / quirk 纪律 / wave 编排 | 同 78-01、78-03,不重复论证 |
</decision_audit>

<output>
Create `.planning/workstreams/milestone/phases/78-block-bp-unlock-by-foundation/78-04-SUMMARY.md` when done
</output>
