---
phase: 78-block-bp-unlock-by-foundation
plan: 07
type: execute
wave: 3
depends_on: [78-05, 78-06]
files_modified:
  - internal/services/addomain/ldap_client_78_07_test.go
  - internal/services/addomain/failover_client_78_07_test.go
  - internal/services/addomain/user_78_07_test.go
  - internal/services/addomain/group_78_07_test.go
  - internal/services/addomain/ldap_fake_server_78_07_test.go
autonomous: true
requirements: [BLOCK-05]

must_haves:
  truths:
    - "本 plan 收口 BLOCK-05:交付 ldap_client.go(实测 36 个函数中仅 extractRDNFromDN 有覆盖,其余全 0%)+ failover_client.go 剩余(PickFirstConnect 0% / newClient 66.7% / ExecuteWithFailover 81.5%)+ user.go(9 函数全 0%)+ group.go(7 函数全 0%)测试"
    - "**G4 已 RESOLVED(78-RESEARCH §1.1 修正)**:`FailoverClient.ExecuteWithFailover(ctx, operation func(client LDAPClientIface) error)`(failover_client.go:44)**早在 Phase 76-03 就已接口化**,且 `clientFactory func(*models.ADConfig, *models.ADServiceAccount) LDAPClientIface`(:25)注入 seam 已存在并被 `failover_client_76_03_test.go` 实证。v1.27-features.md §1.2 的「具体类型断层」结论已过期。本 plan **不做任何接口引入工作**,纯覆盖率扩张"
    - "既有 `mockLDAPClient`(ldap_client_mock_test.go,完整实现 LDAPClientIface 19 方法 + 调用计数 + searchUsersFn 函数式驱动)与 `LDAPClientIface` 编译期断言(ldap_iface.go:56 `var _ LDAPClientIface = (*LDAPClient)(nil)`)直接复用,**勿重复定义 mock**;需要新返回值字段时在既有 mock struct 上扩展"
    - "按 D-78-04(源自 78-RESEARCH DQ4 的分档裁决):**不引入 vjeantet/ldapserver 等第三方 LDAP server 依赖**。Task 1-3 先用「零 wire」手段(参数组装 / dial 错误分支 / mock 注入 / sqlite)把能拿的覆盖率拿满并**实测**;Task 4 再做**自建 in-process LDAP 应答器探针**(~150 行,基于 go-ldap 已传递依赖 `github.com/go-asn1-ber/asn1-ber`,零新模块下载)—— 若探针成功则 ldap_client.go ≥70%;若受阻则降级为 ldap_client.go ≥45% 并用 78-05/78-06 实测盈余论证包级 ≥70% 仍成立(SUMMARY 必须给出数字论证)"
    - "user.go / group.go 的 failover 包裹方法(user.go Update/Enable/Disable/Move、group.go AddMember/RemoveMember/Update)与 config.go 同构:`fc := NewFailoverClient(s.pool, config)` 局部构造、clientFactory 不可注入 → 按 D-78-07 同一裁决,**只覆盖两条失败路径**(空池 ErrAllAccountsUnavailable / 全账号 dial 失败),本地 DB 落库段(update 缓存 / 成员表 / member_count)在 Task 4 应答器成功时回补,否则文档化不覆盖"
    - "按 D-78-08 命名(`<source>_78_07_test.go`,应答器 helper 也是 `_test.go`);按 D-78-10 quirk 纪律;按 D-78-11 wave 编排(本 plan wave 3,同包 78-05/78-06 已落盘,复用其 fixture helper)"
    - "达标口径(2026-08-27 本地实测基线 internal/services/addomain 23.1%):`failover_client.go` ≥80%、`user.go`/`group.go` 各 ≥60%、`ldap_client.go` ≥70%(探针成功)或 ≥45%(fallback + 论证),**`internal/services/addomain` 包 total ≥70%(BLOCK-05 收口)**;`go build ./...` + `go test ./...` + `go test -race` 全绿"
    - "本 plan 裁决细则(Decision Audit 逐条依据,executor 必须遵守):D-78-07a(应答器写成 `_test.go`;结论 B 时保留文件 + 文件头注明未启用原因,供 Phase 79 接手,延用 78-04 的 D-78-04a 口径)/ D-78-07b(缺口扫尾优先取 service.go 22 函数全 0% 的薄委派门面,性价比最高)/ D-78-07c(`cleanDomain` 的 「@ + DomainName」 自我 TrimSuffix 是可疑语义,交由 executor 按 D-78-10 有据/无据判定并留裁决记录)/ D-78-07d(环境变量一律 t.Setenv,禁 os.Setenv 防同包污染;与 D-78-09 禁 t.Parallel 同向)"
  artifacts:
    - path: "internal/services/addomain/ldap_client_78_07_test.go"
      provides: "LDAPClient 参数组装 / dial 三分支 / bind 尝试链 / 纯函数测试(前缀 TestLC78_)"
      min_lines: 300
      contains: "dialConnection"
    - path: "internal/services/addomain/failover_client_78_07_test.go"
      provides: "ExecuteWithFailover 边界 + PickFirstConnect + newClient 生产分支测试(前缀 TestFC78_)"
      min_lines: 240
      contains: "clientFactory"
    - path: "internal/services/addomain/user_78_07_test.go"
      provides: "UserService 查询链(sqlite)+ 4 个 failover 入口失败路径测试(前缀 TestUsr78_)"
      min_lines: 280
      contains: "adUserAllowedSortFields"
    - path: "internal/services/addomain/group_78_07_test.go"
      provides: "GroupService 查询链(sqlite)+ 3 个 failover 入口失败路径测试(前缀 TestGrp78_)"
      min_lines: 220
      contains: "GetMembers"
    - path: "internal/services/addomain/ldap_fake_server_78_07_test.go"
      provides: "自建 in-process LDAP 应答器(TCP + asn1-ber,应答 BindRequest/SearchRequest/ModifyRequest)—— 探针成功时启用"
      min_lines: 150
      contains: "net.Listen"
  key_links:
    - from: "internal/services/addomain/failover_client_78_07_test.go"
      to: "FailoverClient.clientFactory(failover_client.go:25)"
      via: "同包直写 fc.clientFactory = func(...) LDAPClientIface { return &mockLDAPClient{...} }(76-03 先例)"
      pattern: "fc\\.clientFactory = "
    - from: "internal/services/addomain/user_78_07_test.go"
      to: "user.go 的 4 个 failover 入口(Update:126 / Enable:195 / Disable:216 / Move:237)"
      via: "空账号池 → ErrAllAccountsUnavailable;可用账号 + 127.0.0.1 关闭端口 → 全 bind 失败"
      pattern: "ErrAllAccountsUnavailable"
    - from: "internal/services/addomain/ldap_fake_server_78_07_test.go"
      to: "LDAPClient.Connect → dialConnection → tryBindAttempts(ldap_client.go:75/98/141)"
      via: "config.ServerAddress=127.0.0.1 + Port=应答器端口 → ldap.DialURL 成功 + Bind 返回 resultCode 0"
      pattern: "asn1-ber|ber\\."
---

<objective>
收口 BLOCK-05:补齐 addomain 最后一段 —— ldap_client.go(159 unc,参数组装与错误分支为主)、failover_client.go 剩余边界、user.go / group.go 的查询链与 failover 入口;并以一个自建 in-process LDAP 应答器探针(零新模块依赖)争取 ldap_client.go ≥70%,最终使 `internal/services/addomain` 包 total ≥70%。

Purpose: BLOCK-05 收口段。G4(FailoverClient 具体类型断层)经 78-RESEARCH 复核已 RESOLVED —— Phase 76-03 早已接口化,本 plan 是纯覆盖率扩张,无接口引入工作。
Output: 新增 5 个测试文件(零生产代码改动,除非触发 D-78-10 quirk 修复)。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/78-block-bp-unlock-by-foundation/78-RESEARCH.md
@.planning/workstreams/milestone/phases/78-block-bp-unlock-by-foundation/78-05-SUMMARY.md
@.planning/workstreams/milestone/phases/78-block-bp-unlock-by-foundation/78-06-SUMMARY.md

<interfaces>
<!-- 关键契约(planner 已读源码复核行号,2026-08-27)。 -->

ldap_client.go(36 函数,仅 extractRDNFromDN 有覆盖):
```go
// :44-50  type LDAPClient struct{ conn *ldap.Conn; config *models.ADConfig; account *models.ADServiceAccount }
// :52     NewLDAPClient(config, account ...*models.ADServiceAccount) —— **变参**:0 个 / 1 个非 nil / 1 个 nil 三形态
// :63     GetAccount / :70 Conn
// :75     Connect = dialConnection(address) → cleanDomain() → tryBindAttempts(conn, domain) → c.conn = conn
// :98     dialConnection 三分支:
//           UseSSL  → ldap.DialURL("ldaps://"+addr, DialWithTLSConfig)
//           UseTLS  → ldap.DialURL("ldap://"+addr) 后 conn.StartTLS(tlsConfig)(失败则 Close + return err)
//           default → ldap.DialURL("ldap://"+addr)
//         tlsConfig.InsecureSkipVerify 读 env `LDAP_TLS_INSECURE_SKIP_VERIFY`(默认 false),true 时调 logTLSInsecureSkipOnce(:20)
// :128    cleanDomain / :141 tryBindAttempts(UPN / NetBIOS / Direct 三次尝试;account 非 nil 时用 decryptPassword(account.PasswordCiphertext))
// :177    Close(conn nil 安全)
// :184-238 SearchOUs / SearchGroups / SearchUsers / SearchWithRequest / SearchComputers(均经 searchWithPaging)
// :240-313 searchWithPaging / searchWithPagingDepth / handleSearchError / extractPagingControl
// :314-429 UpdateUserAttribute / UpdateGroupAttribute / updateAttributes / EnableUser / DisableUser /
//          setUserAccountControl / MoveUser / AddGroupMember / RemoveGroupMember / CreateGroup / DeleteGroup /
//          AddGroupMembers / RemoveGroupMembers
// :430    extractRDNFromDN(已 100%)/ :439 extractNetBIOSName / :448 CreateOU / :473 OUExists / :509 DNExists
// ⚠️ 所有 Search*/写方法都用 c.conn —— conn 为 nil 时 `c.conn.Search(...)` 会 nil-deref panic
//    (go-ldap 的 *ldap.Conn 方法不判 receiver nil)。若断言该形态,用 require.Panics 并按 D-78-10 判定
```

failover_client.go 剩余:
```go
// :29 NewFailoverClient(pool, config)(已 100%)
// :34 newClient(66.7%)—— clientFactory != nil 走注入;nil 走 NewLDAPClient(**生产分支未覆盖**:
//      可用 fc := NewFailoverClient(pool, cfg) 后直调 fc.newClient(acct) 断言返回值类型为 *LDAPClient)
// :44 ExecuteWithFailover(81.5%)—— 补:ListAvailable 返回 error / MarkSuccess 返回 error 的 warn 分支 /
//      maxAttempts 被 DefaultMaxHops 截断(造 > DefaultMaxHops 个可用账号)/ 中途某账号 Connect 失败后下一个成功
// :99 PickFirstConnect(0%)—— ListAvailable error / 空池 ErrAllAccountsUnavailable /
//      全账号 Connect 失败(每次 MarkFailure)/ 成功路径(需 wire → Task 4)
// 既有 failover_client_76_03_test.go:29-156 已覆盖顺序遍历 + maxHops + searchUsersFn 多批次(**勿重复**)
```

user.go(9 函数全 0%)/ group.go(7 函数全 0%):
```go
// user.go :21 NewUserService(db, pool) / :26-33 adUserAllowedSortFields 白名单
// :45  GetList —— ad_config_id + deleted_at IS NULL + `username NOT LIKE '$DUPLICATE-%'` + `username NOT LIKE '%$'`
//        + OUDN 子树(`ou_dn = ? OR ou_dn LIKE '%,'+?`)+ Username LIKE + IsEnabled + Count + ApplySort
//        + OrderByColumn=="" 时 `username ASC` + Offset/Limit
// :85  GetByDN(gorm.ErrRecordNotFound → "用户不存在")/ :100 GetByID(同)
// :126 Update —— attrs 从 7 个可选指针字段组装 → fc.ExecuteWithFailover(UpdateUserAttribute)
//        → ErrAllAccountsUnavailable 分支 → 成功后本地 updateData 落库
// :195 Enable(EnableUser)/ :216 Disable(DisableUser)/ :237 Move(MoveUser)/ :258 GetUserIds
// group.go :21 NewGroupService / :26-30 adGroupAllowedSortFields / :41 GetList / :75 GetByDN / :90 GetMembers
// :125 AddMember(AddGroupMember + 本地 ADGroupMember Create + member_count+1)
// :155 RemoveMember(RemoveGroupMember + 本地删除 + member_count-1)/ :187 Update(UpdateGroupAttribute)
```

复用 78-05/78-06 已落盘同包 helper(**勿重复定义**):`setupSync78DB`(7 表)/ `entry78` / `insertConfig78` / `setupTestPool` / `insertAccount`(account_pool_test.go)/ `mockLDAPClient`(ldap_client_mock_test.go)。

asn1-ber 现状:`github.com/go-asn1-ber/asn1-ber v1.5.8-...` 在 go.mod:42 的 **indirect** 块(go-ldap 传递依赖),go.sum 已有条目 → 测试 import 它**不会下载新模块**,但 `go mod tidy` 会把它移入直接 require 块。Task 4 若启用应答器,SUMMARY 必须记录该 go.mod 变动(依赖面 +0 模块,仅 direct/indirect 归类变化)。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ldap_client 零 wire 段(变参构造 / dial 三分支错误 / bind 尝试链 / 纯函数 / Close)</name>
  <files>internal/services/addomain/ldap_client_78_07_test.go</files>
  <read_first>
  - internal/services/addomain/ldap_client.go — :1-183(struct / NewLDAPClient 变参 / GetAccount / Conn / Connect / dialConnection / cleanDomain / tryBindAttempts / Close / logTLSInsecureSkipOnce)+ :430-447(extractRDNFromDN / extractNetBIOSName)
  - internal/services/addomain/ldap_client_test.go — 既有用例(避免重复)
  - decryptPassword 定义处(grep)—— tryBindAttempts 对 account.PasswordCiphertext 的解密路径
  - internal/models/ — ADConfig 的 UseSSL / UseTLS / ServerAddress / ServerPort / DomainName / AdminUsername / AdminPassword
  </read_first>
  <action>
  创建 `internal/services/addomain/ldap_client_78_07_test.go`(package addomain)。helper `closedPort78(t) int`:`net.Listen("tcp","127.0.0.1:0")` → 取端口 → 立即 Close → 返回(**唯一允许的网络目标是 127.0.0.1**)。前缀 TestLC78_:

  - `TestLC78_NewLDAPClient_Variadic`:三形态 —— 不传 account / 传一个非 nil account / 传一个 nil account → 断言 `GetAccount()` 分别为 nil / 该实例 / nil(:52-62 的变参守卫)。
  - `TestLC78_Conn_And_Close`:未 Connect → `Conn()` 为 nil、`Close()` 不 panic(:177-182 的 nil 守卫);伪造一个已 Connect 的形态不做(需 wire)。
  - `TestLC78_CleanDomain_Table`:表驱动 —— DomainName 形如 `example.com` / 带 `@` 后缀 / 空串 → 断言 :128-139 的 TrimSuffix 与空值回退语义(**注意 :129 的 suffix 拼接看似可疑(`"@"+DomainName` 去 TrimSuffix 自身),若断言结果与函数名意图不符,按 D-78-10 判定:有据(注释/文档明示)则修 + 翻转断言 + 回归用例;无据则按现行为断言并 SUMMARY 记待裁决**)。
  - `TestLC78_ExtractNetBIOSName_Table`:`example.com` → `EXAMPLE`(或按实现)/ 单段域名 / 空串 / 多段域名。
  - `TestLC78_ExtractRDNFromDN_Edge`:补既有 100% 之外的边界(空 DN / 无逗号 / 转义逗号)——若已 100% 则跳过本条,勿写无覆盖增益用例。
  - `TestLC78_DialConnection_Default_Refused`:UseSSL=false / UseTLS=false + `127.0.0.1:<closedPort>` → 断言返回 error(TCP 拒绝),**用例 5s 硬超时守卫**。
  - `TestLC78_DialConnection_SSL_HandshakeFail`:UseSSL=true + 端口指向一个**纯 TCP echo 监听**(接受连接但不做 TLS)→ 断言 ldaps TLS 握手失败 error;并在 `t.Setenv("LDAP_TLS_INSECURE_SKIP_VERIFY","true")` 形态下再跑一次 → 断言 `logTLSInsecureSkipOnce`(:20)被走到(可观察口径:同进程内两次调用只告警一次的 sync.Once 语义无法直接观察 → 改为断言"不 panic 且仍返回握手错误",并在注释说明覆盖目的)。
  - `TestLC78_DialConnection_TLS_StartTLSFail`:UseTLS=true + 纯 TCP 监听(不响应 StartTLS 扩展)→ 断言 StartTLS 失败且**底层 conn 被 Close**(:113-117);用例 5s 硬超时守卫。
  - `TestLC78_Connect_DialFail`:Connect 在 dial 失败时直接返回 error 且 `c.conn` 仍为 nil。
  - `TestLC78_TryBindAttempts_AllFail`:用一个**接受连接但对 BindRequest 不响应/立即断开**的本地 TCP 监听 → `ldap.DialURL` 成功后 `tryBindAttempts` 三次尝试(UPN / NetBIOS / Direct)全失败 → 断言错误文案含"绑定失败"与"(尝试: UPN, NetBIOS, 直连)";再用 `account` 非 nil 形态跑一遍 → 断言走 decryptPassword 分支(可观察口径:断言仍失败但不 panic,且 username 取自 account.Username —— 若无法观察 username,注释说明覆盖目的)。**每个用例 5s 硬超时守卫**;若 go-ldap 对无响应服务端会长时间等待,改用"监听后立即 Close 连接"的 handler 使 Bind 快速返回 EOF。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestLC78_" -timeout 120s ./internal/services/addomain/ &amp;&amp; go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestLC78_" internal/services/addomain/ldap_client_78_07_test.go` ≥ 9
  - dialConnection 三分支各有独立用例(default 拒绝 / SSL 握手失败 / StartTLS 失败)
  - 全部网络目标为 `127.0.0.1`;每个网络用例有 5s 硬超时守卫;单例实测 <5s
  - `go test -count=1 -run "TestLC78_" -timeout 120s ./internal/services/addomain/` exit 0(无 hang)
  - cleanDomain 的可疑语义有明确裁决记录(修 + 翻转断言,或现行为断言 + SUMMARY 待裁决)
  </acceptance_criteria>
  <done>ldap_client 零 wire 段覆盖(变参 / dial 三分支 / bind 三尝试 / 纯函数 / Close);实测数字为 Task 4 探针的目标定档提供基线。</done>
</task>

<task type="auto">
  <name>Task 2: FailoverClient 剩余边界(ListAvailable error / maxHops 截断 / MarkSuccess warn / newClient 生产分支 / PickFirstConnect)</name>
  <files>internal/services/addomain/failover_client_78_07_test.go</files>
  <read_first>
  - internal/services/addomain/failover_client.go — 全文(:25 clientFactory / :34 newClient / :44 ExecuteWithFailover / :99 PickFirstConnect)
  - internal/services/addomain/failover_client_76_03_test.go — 全文(顺序遍历 / maxHops / searchUsersFn 多批次已覆盖 —— **勿重复**;注入手法照搬)
  - internal/services/addomain/ldap_client_mock_test.go — mockLDAPClient 全字段(需要新返回值时在此扩展)
  - internal/services/addomain/account_pool.go — AccountPool 接口(:46-106)与 DefaultMaxHops 常量定义处
  - internal/services/addomain/account_pool_test.go — setupTestPool / insertAccount(复用)
  </read_first>
  <action>
  创建 `internal/services/addomain/failover_client_78_07_test.go`(package addomain)。除了 sqlite 真 pool,再写一个**同包最小 fake pool**(实现 AccountPool 接口,可注入 ListAvailable 返回 error / MarkSuccess 返回 error / 记录 MarkFailure 调用)——命名 `fakePool78`,避免与既有 mock 命名冲突。前缀 TestFC78_:

  - `TestFC78_ExecuteWithFailover_ListAvailableError`:fakePool78 的 ListAvailable 返回 error → 断言错误文案含"查询账号池失败"(:50)。
  - `TestFC78_ExecuteWithFailover_EmptyPool`:返回空切片 → 断言 `errors.Is(err, ErrAllAccountsUnavailable)`(:53)。
  - `TestFC78_ExecuteWithFailover_MaxHopsTruncation`:造 `DefaultMaxHops + 3` 个可用账号 + clientFactory 返回 connectErr 的 mock → 断言 `mock.connectCalls == DefaultMaxHops`(:57-60 的截断语义)且错误文案含"账号池 N 个账号均失败"(N == DefaultMaxHops)。
  - `TestFC78_ExecuteWithFailover_MidLoopRecovery`:3 个账号,clientFactory 按调用序返回 [connectErr, operationErr, 成功] 三个 mock → 断言最终 nil、第 3 个 mock 的 Close 被调、pool 记录 2 次 MarkFailure(前缀 `dial:` 与 `operation:` 各一)+ 1 次 MarkSuccess(:65-96 的上报语义)。
  - `TestFC78_ExecuteWithFailover_MarkSuccessError`:fakePool78 的 MarkSuccess 返回 error → 断言 ExecuteWithFailover **仍返回 nil**(:88-91 的 warn-not-fail 语义)。
  - `TestFC78_NewClient_ProductionBranch`:`fc := NewFailoverClient(pool, cfg)`(不设 clientFactory)→ 直调 `fc.newClient(acct)` → 断言返回值可断言为 `*LDAPClient` 且其 `GetAccount()` == acct(:36-40 的生产分支,**不发起任何网络**)。
  - `TestFC78_PickFirstConnect_ListAvailableError`:fakePool78 ListAvailable error → 断言"查询账号池失败"。
  - `TestFC78_PickFirstConnect_EmptyPool`:空切片 → `errors.Is(..., ErrAllAccountsUnavailable)`。
  - `TestFC78_PickFirstConnect_AllDialFail`:sqlite pool + 2 个可用账号 + config 指向 `127.0.0.1:<closedPort>` → 断言错误文案含"账号池 2 个账号均无法连接"、两账号 failure_count 各 +1(MarkFailure 上报)。**10s 硬超时守卫**。
  - `TestFC78_PickFirstConnect_MaxHops`:造 `DefaultMaxHops + 2` 个账号 + 关闭端口 → 断言错误文案中的 N == DefaultMaxHops。

  ⚠️ PickFirstConnect 的**成功路径**(:119-126)需要真实 bind 成功 → 归 Task 4;若探针受阻则文档化不覆盖。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestFC78_" -timeout 120s ./internal/services/addomain/ &amp;&amp; go test -count=1 -coverprofile=/tmp/ad78_07a.out ./internal/services/addomain/ &amp;&amp; go tool cover -func=/tmp/ad78_07a.out | grep "failover_client.go"</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestFC78_" internal/services/addomain/failover_client_78_07_test.go` ≥ 10 且含 `fakePool78`(实现 AccountPool 接口)
  - 存在 maxHops 截断断言(`connectCalls == DefaultMaxHops`)与 MarkFailure 前缀(`dial:` / `operation:`)断言
  - `go tool cover -func=/tmp/ad78_07a.out | grep "failover_client.go"` 加权 **≥80%**
  - 测试文件不重复 failover_client_76_03_test.go 的顺序遍历/searchUsersFn 断言
  - `go test -count=1 -run "TestFC78_" ./internal/services/addomain/` exit 0
  </acceptance_criteria>
  <done>failover_client.go ≥80%:ExecuteWithFailover 五个边界 + newClient 生产分支 + PickFirstConnect 四条失败路径覆盖。</done>
</task>

<task type="auto">
  <name>Task 3: user.go / group.go 查询链(sqlite)+ 7 个 failover 入口失败路径</name>
  <files>internal/services/addomain/user_78_07_test.go, internal/services/addomain/group_78_07_test.go</files>
  <read_first>
  - internal/services/addomain/user.go — 全文 9 函数(:45 GetList 的 6 个过滤条件 / :85 GetByDN / :100 GetByID / :126 Update / :195 Enable / :216 Disable / :237 Move / :258 GetUserIds)
  - internal/services/addomain/group.go — 全文 7 函数(:41 GetList / :75 GetByDN / :90 GetMembers / :125 AddMember / :155 RemoveMember / :187 Update)
  - internal/services/base/ — BaseListRequest + ApplySort(白名单语义)
  - internal/services/addomain/sync_78_05_test.go — setupSync78DB(7 表已含 sys_ad_user / sys_ad_group / sys_ad_group_member / sys_ad_service_accounts)
  - internal/services/addomain/account_pool_test.go — insertAccount(failover 失败路径造账号用)
  </read_first>
  <action>
  **A. 创建 `user_78_07_test.go`**(package addomain),helper 复用 `setupSync78DB` + `insertConfig78`。前缀 TestUsr78_:
  - `TestUsr78_GetList_FilterMatrix`:预置 8 行 sys_ad_user —— 含 (a) 正常用户 ×3、(b) `$DUPLICATE-xxx` 前缀 1 行、(c) 以 `$` 结尾(计算机账号)1 行、(d) 软删 1 行、(e) 另一 config 1 行、(f) 子 OU 1 行 → 断言默认查询只返回 (a)+(f);OUDN 过滤命中父 OU 时包含子 OU(`ou_dn = ? OR ou_dn LIKE '%,'+?`);Username LIKE;IsEnabled true/false 各一;total 与 list 一致。
  - `TestUsr78_GetList_SortWhitelist`:白名单内 6 个字段(username/displayName/email/phone/ouDn/isEnabled)各断言顺序;白名单外字段 → 回落 `username ASC` 且不报 SQL 错误。
  - `TestUsr78_GetList_Pagination`:12 行 + pageSize=5 → 三页断言。
  - `TestUsr78_GetByDN_And_GetByID`:命中 / not-found(断言"用户不存在")/ 软删被排除 / DB 错误(DROP TABLE → "查询失败")。
  - `TestUsr78_GetUserIds`:多行 → 返回 id 集合;空 → 空切片。
  - `TestUsr78_Update_AttrsAssembly_EmptyPool`:req 只填 DisplayName + Email(其余 nil)→ **空账号池** → 断言 `errors.Is(err, ErrAllAccountsUnavailable)` 且错误文案含"AD 账号池无可用账号";并断言**本地 DB 未被更新**(失败时不落库,证明 :152-161 的早退顺序)。
  - `TestUsr78_Update_AllAccountsDialFail`:2 个可用账号 + `127.0.0.1:<closedPort>` → 断言非 ErrAllAccountsUnavailable 的 error 透传 + 本地未更新 + failure_count 递增。10s 硬超时守卫。
  - `TestUsr78_EnableDisableMove_FailurePaths`:Enable / Disable / Move 各跑"空池"与"全 dial 失败"两态(表驱动),断言错误分支一致。
  - `TestUsr78_NewUserService`:构造器。

  **B. 创建 `group_78_07_test.go`**(package addomain),同 helper。前缀 TestGrp78_:
  - `TestGrp78_GetList_FilterSortPage`:预置 6 行 sys_ad_group(含软删 / 另 config / 子 OU)→ 断言过滤(OUDN 非空且非空串才生效的守卫、GroupName LIKE)、白名单排序(groupName/memberCount/ouDn)、白名单外回落、分页 total。
  - `TestGrp78_GetByDN`:命中 / not-found / 软删排除。
  - `TestGrp78_GetMembers`:预置 sys_ad_group_member 3 行 + join sys_ad_user → 断言返回成员与 total;空组 → 空切片;分页形态。
  - `TestGrp78_AddMember_FailurePaths`:空池 → ErrAllAccountsUnavailable 文案 + **断言 sys_ad_group_member 未新增、member_count 未变**(失败早退);全 dial 失败 → error 透传 + 同样不落库。10s 硬超时守卫。
  - `TestGrp78_RemoveMember_FailurePaths`:同上两态 + 断言既有成员行未被删除、member_count 未减。
  - `TestGrp78_Update_FailurePaths`:同上两态。
  - `TestGrp78_NewGroupService`:构造器。

  ⚠️ 7 个 failover 入口的**成功路径**(AD 写成功 → 本地 DB 落库/member_count 增减)需要真实 bind 成功 → 归 Task 4;两个文件尾部各加块注释写明该边界(与 78-05 的 D-78-07 边界注释同一格式)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestUsr78_|TestGrp78_" -timeout 180s ./internal/services/addomain/ &amp;&amp; go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestUsr78_" internal/services/addomain/user_78_07_test.go` ≥ 9;`grep -c "^func TestGrp78_" internal/services/addomain/group_78_07_test.go` ≥ 7
  - GetList 用例含 `$DUPLICATE-` 与 `%$` 两个过滤断言(与 78-05 syncUsers 的过滤语义呼应)
  - 每个 failover 入口的失败用例都断言"本地 DB 未被修改"(早退顺序语义)
  - 两个文件尾部各含"成功路径归 Task 4 / 否则不覆盖"的边界块注释
  - `go test -count=1 -run "TestUsr78_|TestGrp78_" ./internal/services/addomain/` exit 0
  </acceptance_criteria>
  <done>user.go / group.go 的查询链全覆盖 + 7 个 failover 入口的两条失败路径覆盖(各文件 ≥60% 在望)。</done>
</task>

<task type="auto">
  <name>Task 4: in-process LDAP 应答器探针(asn1-ber,零新模块)→ ldap_client Search/写方法 + failover/user/group 成功路径</name>
  <files>internal/services/addomain/ldap_fake_server_78_07_test.go, internal/services/addomain/ldap_client_78_07_test.go</files>
  <read_first>
  - internal/services/addomain/ldap_client.go — :184-429(Search* / searchWithPaging / searchWithPagingDepth / handleSearchError / extractPagingControl / update* / setUserAccountControl / MoveUser / 组成员方法 / CreateGroup / DeleteGroup / CreateOU / OUExists / DNExists)
  - `go doc github.com/go-asn1-ber/asn1-ber` — Packet / Encode / NewSequence / NewInteger / NewString / ReadPacket 等编解码入口
  - github.com/go-ldap/ldap/v3 的 message.go / bind.go(理解 BindRequest/SearchRequest 的 LDAPMessage 结构:messageID + protocolOp)
  - go.mod — :42 asn1-ber 处于 indirect 块;go.sum 已有条目(import 不下载新模块)
  - .planning/workstreams/milestone/phases/78-block-bp-unlock-by-foundation/78-RESEARCH.md — DQ4 / R5 / R8
  </read_first>
  <action>
  **探针性质任务:先证可行,再扩覆盖。允许以 fallback 结论收尾,但必须给出结论。**

  第一步:创建 `internal/services/addomain/ldap_fake_server_78_07_test.go`(package addomain,**必须 _test.go**)。目标 `fakeLDAPServer78`:
  - `net.Listen("tcp", "127.0.0.1:0")` + t.Cleanup 关闭;`Port() int` / `Addr() string` / `BindCount() int`(atomic)/ `SetBindResult(code int)`。
  - accept 循环 → 每连接一个 goroutine:用 `ber.ReadPacket(conn)` 读 LDAPMessage → 取 messageID 与 protocolOp 的 application tag:
    * BindRequest(tag 0)→ 回 BindResponse(tag 1)with resultCode = SetBindResult 值(默认 0 = success)
    * SearchRequest(tag 3)→ 按预置 table 回 0..N 个 SearchResultEntry(tag 4)+ 一个 SearchResultDone(tag 5, resultCode 0);支持在 Done 中不带分页 control(简化)
    * ModifyRequest(tag 6)/ AddRequest(8)/ DelRequest(10)/ ModifyDNRequest(12)→ 回对应 Response with resultCode = 可注入值(用于覆盖写方法的成功与失败分支)
    * UnbindRequest(2)/ 未识别 tag → 关连接
  - 响应构造用 `ber.NewSequence` + `ber.NewInteger` + `ber.NewString` 组装并 `packet.Bytes()` 写回。
  - 连接关闭/读错误即退出该 goroutine(禁泄漏)。

  第二步:在 `ldap_client_78_07_test.go` 追加 **探针** `TestLC78_Probe_BindAndSearch`:
  - config: ServerAddress=`127.0.0.1`、ServerPort=应答器端口、UseSSL=false、UseTLS=false、DomainName=`example.com`、BaseDN=`DC=example,DC=com`、AdminUsername/AdminPassword=dummy。
  - `c := NewLDAPClient(cfg)`;`require.NoError(t, c.Connect())` → **探针主断言**:Connect 成功(证明 dialConnection default + tryBindAttempts 第一次 UPN 尝试成功)、`fake.BindCount() >= 1`、`c.Conn() != nil`。
  - `ous, err := c.SearchOUs(cfg.BaseDN)` → 断言 err==nil 且 len(ous) 等于预置 entry 数。
  - `c.Close()` 不 panic。整体 10s 硬超时守卫。

  第三步:结论分流(写进 SUMMARY 的 `## Probe Findings`):
  - **结论 A(bind + search 成功)**:继续补齐 ——
    * `TestLC78_Search_Family`:SearchOUs / SearchGroups / SearchUsers / SearchComputers / SearchWithRequest 各一,断言 filter 与 attributes 组装(应答器可记录收到的 SearchRequest 原始 filter 字符串供断言)。
    * `TestLC78_SearchPaging_And_Errors`:searchWithPaging 的多页(应答器分两批返回)/ searchWithPagingDepth 深度限制 / handleSearchError(应答器回非 0 resultCode)/ extractPagingControl。
    * `TestLC78_WriteMethods_Family`:UpdateUserAttribute / UpdateGroupAttribute / updateAttributes / EnableUser / DisableUser / setUserAccountControl / MoveUser / AddGroupMember / RemoveGroupMember / AddGroupMembers / RemoveGroupMembers / CreateGroup / DeleteGroup / CreateOU / OUExists / DNExists —— 每个跑"成功(resultCode 0)"与"失败(resultCode !=0)"两态(表驱动,可合并为 2-3 个测试函数)。
    * **回补跨文件成功路径**:在 `failover_client_78_07_test.go` 加 `TestFC78_PickFirstConnect_Success`(真账号 + 应答器 → 返回非 nil client 与 acct + MarkSuccess 落库);在 `user_78_07_test.go` / `group_78_07_test.go` 各加 1-2 个成功路径用例(config 指向应答器 + 可用账号 → 断言 AD 写成功后**本地 DB 落库**:user 的 display_name 更新 / group 的成员行新增 + member_count+1)。
    * 同时可选回补 78-05 的 `syncDataInternal` happy path 与 78-06 的 `TestConnection` 成功路径 —— **若时间允许**,在本 plan 内新增用例(不改 78-05/78-06 文件,新增到本 plan 的文件中,或明确记录留 Phase 79)。
  - **结论 B(BER 组装/协议对不上)**:记录**确切失败现象**(ber 解析报错 / go-ldap 客户端拒绝响应 / messageID 不匹配等)与已尝试的修法;按 D-78-04 fallback:删除或 `t.Skip` 掉应答器相关用例(**应答器文件保留并在文件头注明未启用原因**,供 Phase 79 接手),ldap_client.go 目标降为 ≥45%,并在 SUMMARY 用 78-05/78-06 实测数字论证包级 ≥70% 仍成立。
  - 若启用应答器导致 `go mod tidy` 把 asn1-ber 移入直接 require 块,SUMMARY 必须记录该 go.mod diff(依赖模块数 +0)。
  </action>
  <verify>
    <automated>go test -count=1 -v -run "TestLC78_Probe_BindAndSearch" -timeout 120s ./internal/services/addomain/ &amp;&amp; go test -race -count=1 -run "TestLC78_" ./internal/services/addomain/ &amp;&amp; go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `internal/services/addomain/ldap_fake_server_78_07_test.go` 存在且含 `net.Listen`;文件名以 `_test.go` 结尾
  - `TestLC78_Probe_BindAndSearch` 存在并通过(允许以 fallback 结论收尾:该用例可被 `t.Skip` 但必须保留并在 Skip 文案中写明结论 B 的确切原因)
  - SUMMARY 的 `## Probe Findings` 记录结论 A/B + 失败现象 + 已尝试修法 + 目标定档
  - 结论 A 时:`go tool cover -func` 中 ldap_client.go ≥70%,且 failover/user/group 各有 ≥1 个成功路径用例
  - 结论 B 时:ldap_client.go ≥45%,SUMMARY 有包级 ≥70% 的数字论证
  - `go test -race -count=1 -run "TestLC78_" ./internal/services/addomain/` exit 0;go.mod 依赖模块总数不变
  </acceptance_criteria>
  <done>in-process LDAP 应答器可行性有实证结论(DQ4 / R5 / R8 关闭);ldap_client.go 按结论定档达成;成功路径按结论回补或文档化。</done>
</task>

<task type="auto">
  <name>Task 5: BLOCK-05 收口 checkpoint(addomain 包 ≥70%)+ 缺口归因</name>
  <files>internal/services/addomain/ldap_client_78_07_test.go</files>
  <read_first>
  - .github/scripts/check-coverage.sh — :225-250(P2_RATCHET 机制;addomain 无独立豁免行,受全局 P2_FLOOR 约束)
  - .planning/coverage-baseline.md — ratchet 数据链回填位置
  - .planning/workstreams/milestone/phases/78-block-bp-unlock-by-foundation/78-05-SUMMARY.md + 78-06-SUMMARY.md — 前两段实测数字(缺口论证要引用)
  - .planning/workstreams/milestone/REQUIREMENTS.md — BLOCK-05 定义(缺 ~1165 stmts)
  </read_first>
  <action>
  收口测量与缺口归因(不写新业务断言,只补"覆盖率倒序扫尾"用例 + 出报告):

  1. 全包测量:
     ```
     go test -count=1 -coverprofile=cov78_07.out ./internal/services/addomain/
     go tool cover -func=cov78_07.out | sort -t: -k1,1 > /tmp/ad78_final.txt
     go tool cover -func=cov78_07.out | grep -E "ldap_client.go|failover_client.go|user.go|group.go|^total"
     go test -race -count=1 ./internal/services/addomain/
     go test ./...
     ```
  2. 若包 total ≥70% → 直接出 SUMMARY(见第 4 步)。
  3. 若包 total <70% → **按覆盖率倒序扫尾**:从 `/tmp/ad78_final.txt` 取覆盖率最低且 stmts 最多的文件(候选:service.go 22 函数全 0% / user_ad_sync_service.go / group_sync_service.go / dept_sync_service.go / user_ou_service.go / group_management_service.go / ou.go / dept_ou_mapper.go / utils.go 的剩余),在本 plan 新增最小成本用例补到达标为止 —— 优先级判据:(a) 纯 sqlite 或纯函数可测 > 需 mock;(b) stmts 多 > stmts 少;(c) 已有同族 helper 可复用 > 需新建 fixture。
     ⚠️ `service.go`(22 函数全 0%)是最大的隐藏候选:它是 ADDomainService 门面,方法多为**薄委派**(转调 ConfigService/UserService/GroupService/ComputerService/SyncService)→ 用 78-05/78-06 已有 fixture 构造门面并逐方法调用一次,可用极低成本吃掉大量 stmts。若需要补,新建 `service_78_07_test.go`(前缀 TestSvc78_)。
  4. SUMMARY 必须包含:
     - per-file 表:ldap_client.go / failover_client.go / user.go / group.go(+ 若补则含 service.go)覆盖率与目标对照
     - **`internal/services/addomain` 包 total** 与 BLOCK-05 达标结论
     - `## Probe Findings`(Task 4 的 LDAP 应答器结论)
     - **已知不覆盖清单**(汇总本 phase 三个 plan):syncDataInternal happy path(78-05,若未回补)/ TestConnection bind 成功(78-06,若未回补)/ StartHotReload Redis 订阅(78-06)/ ldap_client 的 PG-or-wire-only 分支(若结论 B)/ user·group failover 成功路径(若结论 B)—— 每项给出剩余 stmts 数与承接 phase(79/80)
     - `.planning/coverage-baseline.md` 可回填的三包数字(core / device / addomain 由 78-02 / 78-04 / 本 plan 分别提供)
     - Phase 81 删除 P2_RATCHET 豁免行的前置条件对照(core ≥70% / device ≥70% 已由 78-02 / 78-04 给出;addomain 无独立豁免行)
  </action>
  <verify>
    <automated>go test -count=1 -timeout 300s ./internal/services/addomain/ &amp;&amp; go test -race -count=1 ./internal/services/addomain/ &amp;&amp; go test -count=1 -coverprofile=cov78_07.out ./internal/services/addomain/ &amp;&amp; go tool cover -func=cov78_07.out | grep -E "ldap_client.go|failover_client.go|user.go|group.go|^total" &amp;&amp; go build ./... &amp;&amp; go test ./...</automated>
  </verify>
  <acceptance_criteria>
  - `go tool cover -func=cov78_07.out | grep "^total"` **≥70.0%**(BLOCK-05 达标);未达则 SUMMARY 有逐文件缺口清单 + 差多少 stmts + 明确的 Phase 79 承接项
  - failover_client.go ≥80%;user.go ≥60%;group.go ≥60%;ldap_client.go ≥70%(结论 A)或 ≥45%(结论 B + 论证)
  - SUMMARY 含 per-file 表 + Probe Findings + 已知不覆盖清单(每项带剩余 stmts 与承接 phase)+ coverage-baseline 可回填数字
  - `go test -race ./internal/services/addomain/` exit 0;`go test ./...` exit 0;`go build ./...` exit 0
  - `git diff --stat` 生产 .go 改动 = 0 文件(除非 D-78-10 quirk 修复 + SUMMARY deviation);go.mod 依赖模块总数不变
  </acceptance_criteria>
  <done>`internal/services/addomain` ≥70% → BLOCK-05 达标;Phase 78 三包(core/device/addomain)覆盖率数字齐备,Phase 81 的 GATE 收口有据可依。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 ↔ AD/LDAP | 仅进程内应答器与本地 TCP 监听,地址一律 `127.0.0.1`;禁真实域控地址/域名/管理员凭据 |
| 测试 ↔ TLS | SSL/StartTLS 分支只断言**握手失败**;`LDAP_TLS_INSECURE_SKIP_VERIFY` 只在 t.Setenv 作用域内设置(禁全局污染) |
| 测试 ↔ 数据库 | glebarez sqlite `:memory:`(复用 78-05 fixture),禁生产 PG |
| 测试 ↔ 凭据 | 账号密文/管理员口令一律 dummy 字面量;DN 用 `DC=example,DC=com` |
| 测试 ↔ 生产代码 | 仅新增 *_test.go;生产改动仅 D-78-10 quirk 修复豁免(如 cleanDomain 的可疑 TrimSuffix) |
| 测试 ↔ 依赖 | asn1-ber 已在 go.sum(indirect);import 只改 go.mod 归类,**依赖模块总数必须不变** |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-07-01 | Spoofing | 测试误连真实 AD 域控 | mitigate | 全部 `127.0.0.1` + 端口来自本地 Listen;验收 grep 无真实域名/DC IP/账号 |
| T-78-07-02 | DoS | go-ldap 对无响应服务端长时间等待 → CI hang | mitigate | 每网络用例 5-10s 硬超时守卫;无响应 handler 改为"立即 Close 连接"使 Bind 快速 EOF;`-timeout 300s` |
| T-78-07-03 | DoS | 应答器 accept/read goroutine 泄漏 | mitigate | 读错误即退出 goroutine + t.Cleanup 关 listener + `go test -race` 硬门槛 |
| T-78-07-04 | Tampering | 为达标引入 vjeantet/ldapserver 等第三方依赖 | mitigate | D-78-04 明令禁止;应答器自建且基于已有 asn1-ber;验收 go.mod 模块总数不变 |
| T-78-07-05 | Tampering | 环境变量 `LDAP_TLS_INSECURE_SKIP_VERIFY` 泄漏到同包其他测试 | mitigate | 只用 `t.Setenv`(自动恢复),禁 os.Setenv |
| T-78-07-06 | Information Disclosure | 用真实员工 DN / 邮箱 / 组名造数据 | mitigate | 全部 `example.com` 域的 dummy 值;验收 grep 无真实域后缀 |
| T-78-07-07 | Tampering | 探针失败后为达标改生产 ldap_client 逻辑 | mitigate | D-78-04 预设 fallback 档(≥45%)+ 包级数字论证;禁改生产 |
| T-78-07-SC | Tampering | 依赖安装 | accept | 零新增模块(asn1-ber 已在 go.sum 由 go-ldap 传递引入;仅 direct/indirect 归类可能变化) |
</threat_model>

<verification>
- `go test -count=1 -timeout 300s ./internal/services/addomain/` 全绿且无 hang;`go test -race -count=1 ./internal/services/addomain/` 全绿
- `go tool cover -func=cov78_07.out`:failover_client.go ≥80% / user.go ≥60% / group.go ≥60% / ldap_client.go ≥70%(或 ≥45% + 论证)/ **total ≥70%**
- SUMMARY 含 per-file 表、`## Probe Findings`、已知不覆盖清单(带剩余 stmts 与承接 phase)、coverage-baseline 可回填数字、Phase 81 P2_RATCHET 前置条件对照
- 测试文件网络目标仅 127.0.0.1;env 仅 t.Setenv;无真实域名/凭据;无 t.Parallel
- `go build ./...` + `go test ./...` exit 0;生产 .go 改动 = 0;go.mod 依赖模块总数不变
</verification>

<success_criteria>
- 5 个测试文件存在,TestLC78_ ≥10 / TestFC78_ ≥10 / TestUsr78_ ≥9 / TestGrp78_ ≥7 全绿
- in-process LDAP 应答器探针有实证结论(DQ4 关闭),零新增依赖模块
- failover_client.go ≥80%、user.go/group.go 各 ≥60%、ldap_client.go 按结论达 ≥70% 或 ≥45%+论证
- **`internal/services/addomain` ≥70% → BLOCK-05 达标**;Phase 78 三包数字齐备,已知不覆盖清单交接 Phase 79/80
</success_criteria>

<decision_audit>
| ID | 决策 | 依据 / 理由 |
|----|------|------------|
| **D-78-04** | **不引入 vjeantet/ldapserver;改为 Task 4 自建 ~150 行 in-process 应答器(基于 go.sum 已有的 asn1-ber),并预设 fallback 档(ldap_client.go ≥45% + 包级数字论证)** | 78-RESEARCH DQ4 推荐"暂不引入,若 78-07 跑完仍 <70% 再在 plan 内追加 mini-ldapserver helper(~150 行)";planner 把它**前置为 Task 4 的探针**而非事后补救,理由:ldap_client.go 的 159 unc 中约 100 stmts(Search 家族 + 写方法家族)**必须**有一个能回 resultCode 的对端才可达,零 wire 手段最多到 ~45%(Task 1 会实测确认)。同时确认 asn1-ber 在 go.sum 已有条目(go-ldap 传递依赖)→ 依赖模块数 +0,不违反 v1.27 的"零新依赖主推线"(REQUIREMENTS INFRA-03 明确"嵌入式 vjeantet/ldapserver 停更风险不担") |
| D-78-07(延用) | user.go / group.go / config.go 的 failover 入口与 sync.go 同构处理:**不加生产 seam**,只覆盖两条失败路径;成功路径依赖 Task 4 应答器,受阻则文档化 | 与 78-05 的 D-78-07 同一裁决:`fc := NewFailoverClient(s.pool, config)` 局部构造是全包一致的写法,为测试改 7 处方法签名的代价远大于收益;Phase 77 已确立"生产 .go 改动 = 0"默认纪律 |
| D-78-07a | 应答器写成 `ldap_fake_server_78_07_test.go`(_test.go),结论 B 时**保留文件 + 文件头注明未启用原因**而非删除 | 与 78-04 的 D-78-04a 同口径(测试专用代码不入生产二进制);保留失败探针的成果供 Phase 79 接手,避免重复投入 |
| D-78-07b | Task 5 的缺口扫尾优先取 `service.go`(22 函数全 0%,薄委派门面) | 实测发现的高性价比候选:门面方法多为一行委派,用已有 fixture 构造后逐方法调一次即可吃掉大量 stmts;78-RESEARCH §3 表格未列 service.go(是 planner 从实测 profile 中发现的),故记入 Decision Audit |
| D-78-07c | `cleanDomain`(ldap_client.go:128-139)的 `suffix := "@"+DomainName` 后对 DomainName 自身 TrimSuffix 是**可疑语义**,交由 executor 按 D-78-10 有据/无据判定 | 该表达式在多数输入下等价于恒等函数,疑似应为对 username 去域后缀;但无注释/文档据源 → 不预判为 bug,要求 executor 给出裁决记录(修 + 翻转断言,或现行为断言 + SUMMARY 待裁决) |
| D-78-07d | 环境变量一律 `t.Setenv` | 包级 env 泄漏会污染同包其他测试(dialConnection 的 InsecureSkipVerify 分支);t.Setenv 自动恢复且禁止与 t.Parallel 共用(与 D-78-09 同向) |
| D-78-08 / D-78-10 / D-78-11 | 命名 / quirk 纪律 / wave 编排 | 同 78-01 |

**G4 状态说明(非决策,是研究修正的执行确认)**:78-RESEARCH §1.1 已实证 `ExecuteWithFailover` 自 Phase 76-03 起接受 `LDAPClientIface`、`clientFactory` seam 已存在、`failover_client_76_03_test.go` 已在用。本 plan 据此**不含任何接口引入/签名改造任务**,v1.27-features.md §1.2 claim #2 应视为过期(建议 Phase 81 audit 时更正该研究文件)。
</decision_audit>

<output>
Create `.planning/workstreams/milestone/phases/78-block-bp-unlock-by-foundation/78-07-SUMMARY.md` when done
</output>
