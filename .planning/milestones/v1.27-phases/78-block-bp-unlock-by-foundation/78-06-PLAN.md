---
phase: 78-block-bp-unlock-by-foundation
plan: 06
type: execute
wave: 2
depends_on: [78-05]
files_modified:
  - internal/services/addomain/computer_78_06_test.go
  - internal/services/addomain/ou_group_mapping_78_06_test.go
  - internal/services/addomain/group_config_78_06_test.go
  - internal/services/addomain/config_78_06_test.go
  - internal/services/addomain/account_pool_78_06_test.go
autonomous: true
requirements: [BLOCK-05]

must_haves:
  truths:
    - "本 plan 交付 addomain 的「纯 sqlite CRUD + entry-driven」第二段:computer.go(实测 22 函数中 14 个 0%:)+ ou_group_mapping_service.go(11 函数全 0%)+ group_config_service.go(7 函数全 0%)+ config.go(7 函数全 0%)+ account_pool.go 剩余(RecoverExpiredBreakers 0% / StartHotReload 0% / PickAvailable 44.4%),零 LDAP 网络"
    - "复用 78-05 落地的同包 fixture 与 helper(**勿重复定义**):`setupSync78DB`(7 表)/ `entry78`(ldap.Entry 构造)/ `insertConfig78`;computer 相关需在其上追加 `sys_ad_computer` 表(列集以 models.ADComputer 与 syncComputers 实际写入字段为准)"
    - "syncComputers(computer.go:372)与 sync.go 的四条管道同构:entry-driven、DN 与 computer_name 双 map 去重(queryExistingComputers + queryAllComputerNames + buildComputerMaps)、processComputerEntry 分派新建/更新、batchCreate/batchUpdate 批处理 —— 逐分支造 entry 覆盖"
    - "account_pool.go 的 `RecoverExpiredBreakers`(:495)必须覆盖:预置 status=AccountStatusCircuitBroken 且 circuit_breaker_until 已过期的账号 → 断言 status 回 AccountStatusAvailable / circuit_breaker_until=NULL / failure_count=0 / 返回 RowsAffected;`StartHotReload`(:553)覆盖 `redisPubSub == nil` 的早退分支(单机部署形态,零 Redis 依赖);状态常量一律用 AccountStatus* 具名常量"
    - "config.go 的 `TestConnection`(:189)走 `FailoverClient.PickFirstConnect`:空账号池 → `errors.Is(err, ErrAllAccountsUnavailable)` 分支;有账号 + ServerAddress 指向本地已关闭端口 → 非 ErrAllAccountsUnavailable 的「全部 bind 失败」分支。**不测 bind 成功路径**(需 wire-level LDAP,归 78-07 的应答器)"
    - "按 D-78-08 命名(`<source>_78_06_test.go`);按 D-78-10 quirk 纪律;按 D-78-11 wave 编排(本 plan wave 2,同包 78-05 已落盘;同包 78-07 排 wave 3)"
    - "达标口径(2026-08-27 本地实测基线 internal/services/addomain 23.1%):computer.go / ou_group_mapping_service.go / group_config_service.go / config.go **每个单独 ≥70%**,account_pool.go ≥90%(基线已 ~85%,补两个 0% 函数),`go build ./...` + `go test ./...` + `go test -race ./internal/services/addomain/` 全绿,生产 .go 改动 = 0 文件"
    - "本 plan 裁决细则(Decision Audit 逐条依据,executor 必须遵守):D-78-06a(StartHotReload 只覆盖 redisPubSub==nil 早退,不引入 miniredis 测 Redis 订阅路径)/ D-78-06b(TestConnection 的 bind 成功路径归 78-07 的应答器,本 plan 只覆盖两条失败分支)/ D-78-06c(isUniqueConstraintError 必须用真实 sqlite driver error 触发,以暴露方言适配缺口;不命中则 SUMMARY 记待裁决)/ D-78-06d(PickAvailable 多账号用例只断言返回值 ∈ 可用集合,禁断言具体挑中项)/ D-78-06e(复用 78-05 的 setupSync78DB/entry78,禁同包重复定义)"
  artifacts:
    - path: "internal/services/addomain/computer_78_06_test.go"
      provides: "ComputerService List 链 + syncComputers entry-driven 全链测试(前缀 TestComp78_)+ sys_ad_computer 建表 helper"
      min_lines: 420
      contains: "syncComputers"
    - path: "internal/services/addomain/ou_group_mapping_78_06_test.go"
      provides: "OUGroupMappingService 11 函数 CRUD + 同步日志测试(前缀 TestOGM78_)"
      min_lines: 240
      contains: "isUniqueConstraintError"
    - path: "internal/services/addomain/group_config_78_06_test.go"
      provides: "GroupConfigService 7 函数测试(前缀 TestGC78_)"
      min_lines: 180
      contains: "GetGroupSyncConfig"
    - path: "internal/services/addomain/config_78_06_test.go"
      provides: "ConfigService CRUD + TestConnection 两条失败分支测试(前缀 TestCfg78_)"
      min_lines: 200
      contains: "TestConnection"
    - path: "internal/services/addomain/account_pool_78_06_test.go"
      provides: "RecoverExpiredBreakers / StartHotReload / PickAvailable 剩余分支测试(前缀 TestPool78_)"
      min_lines: 140
      contains: "RecoverExpiredBreakers"
  key_links:
    - from: "internal/services/addomain/computer_78_06_test.go"
      to: "syncComputers(computer.go:372,私有)"
      via: "同包直调 NewComputerService(db).syncComputers(ctx, config, entries)"
      pattern: "\\.syncComputers\\("
    - from: "internal/services/addomain/account_pool_78_06_test.go"
      to: "RecoverExpiredBreakers(account_pool.go:495)"
      via: "sqlite 预置过期熔断账号行 + NewAccountPool(db, nil)"
      pattern: "AccountStatusCircuitBroken"
    - from: "internal/services/addomain/config_78_06_test.go"
      to: "TestConnection → FailoverClient.PickFirstConnect(failover_client.go:99)"
      via: "空账号池 / 本地已关闭端口两态"
      pattern: "ErrAllAccountsUnavailable"
---

<objective>
交付 addomain 第二段:computer.go(219 unc)+ 三个纯 CRUD service(ou_group_mapping 60 / group_config 60 / config 53)+ account_pool 剩余两个 0% 函数,全部走 sqlite 与 `[]*ldap.Entry` 字面量,零 LDAP 网络,使这 4 个目标文件各自 ≥70%、account_pool ≥90%。

Purpose: BLOCK-05 的第二段。78-05 已清掉 sync.go,本 plan 清掉"纯 sqlite 可测"的剩余大头,为 78-07(Iface stub 段 + 包级收口)腾出空间。
Output: 新增 5 个测试文件(零生产代码改动,除非触发 D-78-10 quirk 修复)。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/78-block-bp-unlock-by-foundation/78-RESEARCH.md
@.planning/workstreams/milestone/phases/78-block-bp-unlock-by-foundation/78-05-SUMMARY.md

<interfaces>
<!-- 关键契约(planner 已读源码复核行号,2026-08-27)。 -->

computer.go:
```go
// :24-31  type ComputerService struct{ db *gorm.DB };NewComputerService(db) —— 唯一依赖 DB,零 LDAP
// :34-39  computerAllowedSortFields 白名单(computerName/operatingSystem/lastLogon/createdAt)
// :41-52  ComputerListRequest{ base.BaseListRequest; ConfigID; OUN(json "ouDn"); ComputerName }
// :56-77  List → normalizePagination(:78)→ buildComputerQuery(:91)→ countComputers(:108)→ fetchComputers(:117)→ convertToDetails(:135)
// :147    GetByDN
// :167-261 已 100%:parseComputerDescriptionForUser / parseComputerDescription / extractCapacityValue / parseDateTime / determineComputerStatus(**勿重复测**,见 computer_pure_test.go)
// :262    buildComputerFromEntry(entry → models.ADComputer)
// :307    updateComputerFields(existing, entry)
// :346    batchCreate / :580 batchUpdate
// :372    syncComputers(ctx, config, entries) —— 编排:extractDNs(复用 sync.go 的包级函数)→ queryExistingComputers(:478)
//         → queryAllComputerNames(:515)→ buildComputerMaps(:526)→ processComputerEntry(:547)逐条 → batchCreate + batchUpdate
```

ou_group_mapping_service.go(11 函数全 0%):
```go
// :19 NewOUGroupMappingService / :61 ListMappings / :114 CreateMapping / :154 GetMapping / :168 UpdateMapping
// :201 DeleteMapping / :215 GetMappingsByOU / :231 CreateSyncLog / :239 UpdateSyncStatus
// :252 isUniqueConstraintError / :263 containsIgnoreCase(纯函数)
// 表:sys_ad_ou_group_mapping(+ 同步日志表)—— 列名以 models 结构体 tag 为准
```

group_config_service.go(7 函数全 0%):
```go
// :18 NewGroupConfigService / :25 getConfigByKey / :35 setConfigByKey / :57 GetGroupSyncConfig
// :104 IsGroupSyncEnabled / :113 UpdateGroupSyncConfig / :148 ValidateConfig
// 依赖 sys_config(key-value)—— 表结构照 internal/core/core_74_08_test.go:37 的 sys_config 建表 SQL
```

config.go(7 函数全 0%):
```go
// :22 NewConfigService(db, pool) / :42 GetList(base.ApplySort + configAllowedSortFields + status 过滤 + 分页)
// :69 GetByID / :96 Create / :140 Update / :177 Delete
// :189 TestConnection —— fc := NewFailoverClient(s.pool, config); fc.PickFirstConnect(ctx)
//        空池 → errors.Is(err, ErrAllAccountsUnavailable) → "AD 连接测试失败：账号池无可用账号..."
//        非空但全 bind 失败 → "AD 连接测试失败（账号池无可用账号或全部 bind 失败）"
//        成功 → defer client.Close() + nil   ← 需 wire-level LDAP,归 78-07
```

account_pool.go 剩余:
```go
// :186 PickAvailable(44.4%)—— 补空池 / 单账号 / 多账号 / 错误分支
// :495 RecoverExpiredBreakers(0%)—— Pluck configIDs → 批量 Updates(status/circuit_breaker_until/failure_count)
//        → InvalidateCache(每 configID)→ redisPubSub != nil 时 Publish(nil 时跳过)→ 返回 RowsAffected
// :553 StartHotReload(0%)—— redisPubSub == nil 直接 return nil(单机形态,本 plan 只覆盖该早退)
// 状态常量:AccountStatusAvailable / AccountStatusDisabled / AccountStatusCircuitBroken(grep 定位定义处,断言禁裸 0/1/2)
```

复用 78-05 已落盘的同包 helper(**勿重复定义**):`setupSync78DB`(7 表:sys_ad_config/ou/group/user/group_member/sync_log/service_accounts)、`entry78`、`insertConfig78`、`newSyncSvc78`。
本 plan 需追加的建表:`sys_ad_computer`(id / ad_config_id / computer_dn / computer_name / dns_host_name / operating_system / operating_system_version / description / ou_dn / last_logon / when_created / enabled / status / serial_number / mac_address / ip_address / managed_by / original_description / last_sync_at / created_at / updated_at / deleted_at —— 以 models.ADComputer tag 与 syncComputers 写入字段为准)、`sys_ad_ou_group_mapping`、`sys_config`。建议在 computer_78_06_test.go 内加 `setupComp78DB(t)` = `setupSync78DB(t)` + 追加表,其余文件按需各自加最小表。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: computer.go List 查询链(normalizePagination / buildComputerQuery / count / fetch / convertToDetails / GetByDN)</name>
  <files>internal/services/addomain/computer_78_06_test.go</files>
  <read_first>
  - internal/services/addomain/computer.go — :24-166(struct / 白名单 / 请求体 / List 链 / GetByDN)
  - internal/services/addomain/computer_pure_test.go — 既有纯函数用例(parseComputerDescription 等已 100%,**勿重复**)
  - internal/services/addomain/sync_78_05_test.go — 78-05 的 setupSync78DB / insertConfig78(本任务扩展复用)
  - internal/services/base/ — BaseListRequest 与 ApplySort 语义(排序白名单外字段的处理)
  - internal/models/ — ADComputer 全字段与列名映射
  </read_first>
  <action>
  创建 `internal/services/addomain/computer_78_06_test.go`(package addomain)。helper `setupComp78DB(t) *gorm.DB` = 调 78-05 的 `setupSync78DB(t)` 后 `db.Exec` 追加 `CREATE TABLE sys_ad_computer (...)`(列集见 interfaces);helper `insertComputer78(t, db, configID, dn, name string, opts ...)` 插行。前缀 TestComp78_:

  - `TestComp78_NormalizePagination`:current/pageSize 为 0 / 负数 / 超大值 → 断言被归一到默认值(读 :78-90 确认默认值);正常值透传。
  - `TestComp78_List_FilterMatrix`:预置 5 行(不同 ou_dn / computer_name / ad_config_id)→ 逐个断言:仅 ConfigID 过滤 / OUN 子树过滤(`ou_dn = ?` OR `ou_dn LIKE '%,' + ?` 语义,以 :91-107 为准)/ ComputerName 模糊匹配 / 组合过滤;total 与 list 长度一致。
  - `TestComp78_List_SortWhitelist`:排序字段在白名单内(computerName / operatingSystem / lastLogon / createdAt)→ 断言结果顺序;白名单外字段(如 `"; DROP TABLE"`)→ 断言回落默认排序且**不报 SQL 错误**(白名单防注入语义)。
  - `TestComp78_List_Pagination`:12 行 + pageSize=5 → 断言第 1/2/3 页的条数与 total=12。
  - `TestComp78_List_ConvertToDetails`:预置一行 description 含可解析的最后登录用户(照 computer_pure_test.go 已验证的 parse 形态)→ 断言 `ComputerDetail.LastLogonUser` 被填充;description 不可解析 → 断言为空串。
  - `TestComp78_List_DBError`:DROP TABLE sys_ad_computer → 断言 count 或 fetch 的错误被包装返回(不 panic)。
  - `TestComp78_GetByDN`:命中 / 不命中(断言"不存在"类错误)/ 软删行被排除(`deleted_at IS NULL`)。
  - `TestComp78_NewComputerService`:构造器返回非 nil 且 db 透传。

  纪律:禁 t.Parallel();断言禁裸状态字面量(用 models 常量)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestComp78_" ./internal/services/addomain/ &amp;&amp; go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - 文件存在且含 `func setupComp78DB`,内部调用 78-05 的 `setupSync78DB`(不重复建 7 表)
  - `grep -c "^func TestComp78_" internal/services/addomain/computer_78_06_test.go` ≥ 8
  - 存在排序白名单外字段的用例且断言不报 SQL 错误
  - `go test -count=1 -run "TestComp78_" ./internal/services/addomain/` exit 0
  </acceptance_criteria>
  <done>computer.go List 查询链 8 个函数覆盖;sys_ad_computer fixture 就位供 Task 2 复用。</done>
</task>

<task type="auto">
  <name>Task 2: computer.go syncComputers entry-driven 全链(buildFromEntry / updateFields / 双 map 去重 / process / batch)</name>
  <files>internal/services/addomain/computer_78_06_test.go</files>
  <read_first>
  - internal/services/addomain/computer.go — :262-345(buildComputerFromEntry / updateComputerFields)+ :346-371 batchCreate + :372-477 syncComputers + :478-579(queryExistingComputers / queryAllComputerNames / buildComputerMaps / processComputerEntry)+ :580+ batchUpdate
  - internal/services/addomain/sync_78_05_test.go — `entry78` helper(本任务复用)与 78-05 的 syncUsers 用例风格(同构管道,断言手法照搬)
  - internal/services/addomain/computer_78_06_test.go — Task 1 的 setupComp78DB
  </read_first>
  <action>
  同文件追加(与 78-05 Task 3 的 syncUsers 同构):

  - `TestComp78_BuildComputerFromEntry`:完整 entry(cn / dNSHostName / operatingSystem / operatingSystemVersion / description / distinguishedName / lastLogon / whenCreated / userAccountControl / managedBy 等,属性名照 :262-306 抄)→ 断言返回 models.ADComputer 逐字段;属性缺失 → 断言零值兜底不 panic;description 含容量/序列号形态 → 断言 serial_number 等派生字段(若 build 阶段解析;否则在 processComputerEntry 用例断言)。
  - `TestComp78_UpdateComputerFields`:预置 existing + 新 entry(部分字段变更)→ 断言变更字段被更新、未变更字段保留、last_sync_at 被刷新。
  - `TestComp78_QueryExistingComputers_And_AllNames`:预置 4 行(2 行 DN 在查询列表内、1 行同 config 不同 DN、1 行不同 config)→ 断言 queryExistingComputers 只返回 DN 命中且同 config 的行;queryAllComputerNames 返回本 config 全部名称(含软删过滤断言)。
  - `TestComp78_BuildComputerMaps`:给定 existing 切片 + names 切片 → 断言两个 map 的键值(DN map / name map),重名/重 DN 的处理按现行为断言。
  - `TestComp78_ProcessComputerEntry_NewVsUpdateVsRename`:三形态 —— (a) DN 与 name 都不存在 → 归入新建;(b) DN 已存在 → 归入更新;(c) DN 不存在但 **computer_name 已存在**(改名/重建场景)→ 断言按现行为归类(更新既有行或跳过),这是双 map 去重的核心分支,注释标明依据行号。
  - `TestComp78_SyncComputers_FullChain`:空 entries → 早退 nil;5 条 entry(2 新建 / 2 更新 / 1 同名不同 DN)→ 调用后断言库中行数与逐行字段;断言不产生重复行。
  - `TestComp78_BatchCreate_And_BatchUpdate_Batching`:空切片/空 map 早退;超单批阈值条数(读 :346/:580 确认批大小)→ 断言全部落库(多批 + 末批余数)。
  - `TestComp78_SyncComputers_DBError`:DROP TABLE sys_ad_computer → 断言错误被包装返回。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestComp78_" ./internal/services/addomain/ &amp;&amp; go test -count=1 -coverprofile=/tmp/ad78_06a.out ./internal/services/addomain/ &amp;&amp; go tool cover -func=/tmp/ad78_06a.out | grep "computer.go"</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestComp78_" internal/services/addomain/computer_78_06_test.go` ≥ 16(Task 1+2 合计)
  - 存在"DN 不存在但 computer_name 已存在"的改名分支用例
  - `go tool cover -func=/tmp/ad78_06a.out | grep "computer.go"` 加权 **≥70%**
  - `go test -count=1 -run "TestComp78_" ./internal/services/addomain/` exit 0
  </acceptance_criteria>
  <done>computer.go ≥70%:syncComputers 编排 + 双 map 去重 + 批处理 + entry 映射全覆盖。</done>
</task>

<task type="auto">
  <name>Task 3: ou_group_mapping_service.go 全 11 函数(CRUD + 同步日志 + 唯一约束识别)</name>
  <files>internal/services/addomain/ou_group_mapping_78_06_test.go</files>
  <read_first>
  - internal/services/addomain/ou_group_mapping_service.go — 全文 11 函数
  - internal/models/ — OU-Group 映射与其同步日志的结构体(字段 tag → 列名)
  - internal/services/addomain/sync_78_05_test.go — setupSync78DB(复用后追加映射表)
  </read_first>
  <action>
  创建 `internal/services/addomain/ou_group_mapping_78_06_test.go`(package addomain)。helper `setupOGM78DB(t) *gorm.DB` = setupSync78DB + `CREATE TABLE sys_ad_ou_group_mapping (...)` + 映射同步日志表(表名/列名以 models 为准)。前缀 TestOGM78_:

  - `TestOGM78_ContainsIgnoreCase`:纯函数表驱动(命中/大小写混合/不命中/空串/子串在首尾)。⚠️ 注意本包内 utils.go 可能有同名函数,断言的是 ou_group_mapping_service.go:263 的这一个(同包内若重名会编译冲突 —— 若确实重名,说明存在双实现,按 D-78-10 记 SUMMARY 待裁决,不擅自合并)。
  - `TestOGM78_IsUniqueConstraintError`:构造 sqlite 唯一约束错误(先建唯一索引再插重复行,取真实 error)→ 断言 true;普通错误 / nil → false。**用真实 driver error 而非字符串字面量**,才能证明识别逻辑对 glebarez sqlite 与 PG 双方言的适配面(若只适配 PG 文案而 sqlite 不命中,按 D-78-10 记 SUMMARY:这属方言分支缺口,与记忆教训"PG 数组 ANY() 需方言分支"同族)。
  - `TestOGM78_CreateMapping_And_Duplicate`:创建成功 → 断言落库字段;重复创建 → 断言走 isUniqueConstraintError 分支返回友好错误。
  - `TestOGM78_ListMappings_FilterAndPage`:预置 6 行(不同 config / ou / group)→ 断言过滤 + 分页 + total;空结果形态。
  - `TestOGM78_GetMapping_And_GetMappingsByOU`:命中/不命中;按 OU 返回多条 + 空。
  - `TestOGM78_UpdateMapping`:字段更新 + 不存在 id 的错误分支。
  - `TestOGM78_DeleteMapping`:删除成功(断言行消失或软删标记)+ 不存在 id。
  - `TestOGM78_CreateSyncLog_And_UpdateSyncStatus`:创建日志 → 断言初始状态;UpdateSyncStatus 置成功/失败 → 断言状态与错误信息落库(状态用 models 常量);不存在 logID → 按现行为断言。
  - `TestOGM78_NewOUGroupMappingService`:构造器。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestOGM78_" ./internal/services/addomain/ &amp;&amp; go tool cover -func=/tmp/ad78_06a.out 2>/dev/null | grep "ou_group_mapping" ; go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestOGM78_" internal/services/addomain/ou_group_mapping_78_06_test.go` ≥ 9
  - isUniqueConstraintError 用例使用**真实 sqlite driver error**(测试内先建唯一索引再触发冲突),非字符串伪造
  - 重新测量后 `go tool cover -func` 中 ou_group_mapping_service.go 加权 **≥70%**
  - `go test -count=1 -run "TestOGM78_" ./internal/services/addomain/` exit 0
  </acceptance_criteria>
  <done>ou_group_mapping_service.go ≥70%:11 函数全覆盖,唯一约束方言适配面有实证记录。</done>
</task>

<task type="auto">
  <name>Task 4: group_config_service.go 7 函数 + config.go CRUD 与 TestConnection 两条失败分支</name>
  <files>internal/services/addomain/group_config_78_06_test.go, internal/services/addomain/config_78_06_test.go</files>
  <read_first>
  - internal/services/addomain/group_config_service.go — 全文 7 函数(getConfigByKey / setConfigByKey / GetGroupSyncConfig / IsGroupSyncEnabled / UpdateGroupSyncConfig / ValidateConfig)
  - internal/services/addomain/config.go — 全文 7 函数(GetList / GetByID / Create / Update / Delete / TestConnection)
  - internal/services/addomain/failover_client.go — :99-131 PickFirstConnect(空池 → ErrAllAccountsUnavailable;Connect 全失败 → "账号池 N 个账号均无法连接")
  - internal/services/addomain/account_pool_test.go — :26-49 建表 SQL + insertAccount(TestConnection 的有账号形态用)
  - internal/core/core_74_08_test.go — :37 的 sys_config 建表 SQL(group_config 依赖)
  - internal/models/ — ADConfig 全字段 + ADConfigStatus* 常量
  </read_first>
  <action>
  **A. 创建 `group_config_78_06_test.go`**(package addomain),helper 建 `sys_config` 表(照 core_74_08_test.go:37)。前缀 TestGC78_:
  - `TestGC78_GetSetConfigByKey`:setConfigByKey 新键 → 落库;已存在键 → 更新而非重复插入;getConfigByKey 命中 / 缺失(断言默认值或错误,按现行为)。
  - `TestGC78_GetGroupSyncConfig_DefaultsAndOverrides`:空 sys_config → 断言全默认值(逐字段);预置各键 → 断言被覆盖;**非法值**(如布尔位填 "abc"、数字位填负数)→ 断言回退默认值而非 panic。
  - `TestGC78_IsGroupSyncEnabled`:开/关/缺省三态。
  - `TestGC78_UpdateGroupSyncConfig_And_Validate`:合法配置 → 落库并可回读;`ValidateConfig` 逐条非法输入(阈值越界 / 必填空 / 枚举非法)→ 断言各自错误文案;合法 → nil。UpdateGroupSyncConfig 传非法配置 → 断言先校验后拒绝(不落库)。
  - `TestGC78_NewGroupConfigService`:构造器。

  **B. 创建 `config_78_06_test.go`**(package addomain),helper = setupSync78DB(已含 sys_ad_config + sys_ad_service_accounts)。前缀 TestCfg78_:
  - `TestCfg78_GetList_FilterSortPage`:预置 5 条配置(status 混合)→ 断言 status 过滤(用 models.ADConfigStatus* 常量)、白名单排序(configName/serverAddress/domainName/status/createdAt)、白名单外字段回落 `created_at DESC`、分页与 total。
  - `TestCfg78_GetByID`:命中/不命中。
  - `TestCfg78_Create`:合法 → 落库并断言字段;重复/非法(必填空)→ 按 :96-139 的校验语义断言。
  - `TestCfg78_Update`:字段更新 + 不存在 id;**若 Update 内有密码字段的特殊处理(空则保留原值),必须单独断言**(读源码确认)。
  - `TestCfg78_Delete`:删除成功(软删断言 deleted_at)+ 不存在 id。
  - `TestCfg78_TestConnection_EmptyPool`:空 sys_ad_service_accounts → 断言 `errors.Is(err, ErrAllAccountsUnavailable)` 且文案含"账号池无可用账号"。
  - `TestCfg78_TestConnection_AllBindFail`:insertAccount 造 2 个可用账号 + config.ServerAddress=`127.0.0.1` / ServerPort = 本地已关闭端口 → 断言错误文案含"全部 bind 失败"且**不**是 ErrAllAccountsUnavailable;并断言两账号 failure_count 被 MarkFailure 递增。**用例加 10s 硬超时守卫**并记录实测耗时(与 78-05 Task 5 同一纪律)。
  - `TestCfg78_NewConfigService`:构造器。
  ⚠️ **不测 bind 成功路径**(需 wire-level LDAP 应答器,归 78-07);在文件尾块注释写明该边界。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestGC78_|TestCfg78_" -timeout 120s ./internal/services/addomain/ &amp;&amp; go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestGC78_" internal/services/addomain/group_config_78_06_test.go` ≥ 5;`grep -c "^func TestCfg78_" internal/services/addomain/config_78_06_test.go` ≥ 8
  - TestConnection 两条失败分支各有独立用例,且 AllBindFail 用例断言 failure_count 递增 + 有硬超时守卫
  - config_78_06_test.go 尾部含"bind 成功路径归 78-07"的边界块注释
  - ValidateConfig 用例含 ≥3 条非法输入
  - `go test -count=1 -run "TestGC78_|TestCfg78_" ./internal/services/addomain/` exit 0
  </acceptance_criteria>
  <done>group_config_service.go 与 config.go 各 ≥70%;TestConnection 的两条失败分支覆盖,成功路径边界文档化。</done>
</task>

<task type="auto">
  <name>Task 5: account_pool RecoverExpiredBreakers / StartHotReload / PickAvailable 剩余 + per-file checkpoint</name>
  <files>internal/services/addomain/account_pool_78_06_test.go</files>
  <read_first>
  - internal/services/addomain/account_pool.go — :127-320(NewAccountPool / ListAvailable / PickAvailable / ListAll / CountByStatus / PickFirstAvailable)+ :495-552 RecoverExpiredBreakers + :542 InvalidateCache + :553+ StartHotReload
  - internal/services/addomain/account_pool_test.go + account_pool_gapfill_test.go — 既有用例清单(**勿重复**;本任务只补 0% 与 44.4% 的部分)
  - AccountStatus* 常量定义处(grep `AccountStatusCircuitBroken`)
  </read_first>
  <action>
  创建 `internal/services/addomain/account_pool_78_06_test.go`(package addomain)。复用 account_pool_test.go 的 `setupTestPool` / `insertAccount`(同包已存在,**勿重复定义**;若需要过期熔断行,新增一个 `insertBrokenAccount78(t, db, configID, username string, until time.Time) string`)。前缀 TestPool78_:

  - `TestPool78_RecoverExpiredBreakers_Basic`:预置 3 个账号 —— (a) status=AccountStatusCircuitBroken 且 circuit_breaker_until = now-1min(**过期**)、(b) 同状态但 until = now+10min(**未过期**)、(c) status=AccountStatusAvailable → 调 `RecoverExpiredBreakers(ctx)` → 断言:返回 n==1;(a) 的 status 回 AccountStatusAvailable、circuit_breaker_until 为 NULL、failure_count=0;(b)(c) 不变。
  - `TestPool78_RecoverExpiredBreakers_None`:无过期熔断行 → 返回 0 且无副作用。
  - `TestPool78_RecoverExpiredBreakers_MultiConfig`:两个 configID 各有一个过期熔断账号 → 断言两者都被恢复,且**两个 configID 的本地缓存都被 InvalidateCache**(可观察口径:先调 ListAvailable 预热缓存 → RecoverExpiredBreakers → 再调 ListAvailable 断言返回已恢复的账号;若缓存未失效则会返回旧快照,该断言即证明失效生效)。
  - `TestPool78_RecoverExpiredBreakers_DBError`:DROP TABLE sys_ad_service_accounts → 断言返回 (0, error)。
  - `TestPool78_RecoverExpiredBreakers_NoPubSub`:`NewAccountPool(db, nil)`(redisPubSub=nil)→ 断言 Publish 分支被跳过且不报错(零 Redis 依赖)。
  - `TestPool78_StartHotReload_NilPubSub`:`NewAccountPool(db, nil)` → `StartHotReload(ctx)` 断言返回 nil(单机部署早退,:555-558)。**不测 Redis 订阅路径**(需 miniredis + pubsub 语义,超出本 plan 目标且 78-04 已用尽 miniredis 预算)——在注释与 SUMMARY 记录该边界。
  - `TestPool78_PickAvailable_Branches`(基线 44.4%):空池 → 断言 ErrAllAccountsUnavailable 或对应错误;单账号 → 返回该账号;多账号 → 返回值 ∈ 集合(**禁断言具体挑中哪一个** —— 防随机性 flake);ListAvailable 出错(DROP TABLE)→ 错误透传。
  - `TestPool78_InvalidateCache`:预热 → InvalidateCache → 断言下次查询回落 DB(用"DB 侧改数据后立即可见"作可观察口径)。

  **收尾 checkpoint(本 plan 交付口径)**:
  ```
  go test -count=1 -coverprofile=cov78_06.out ./internal/services/addomain/
  go tool cover -func=cov78_06.out | grep -E "computer.go|ou_group_mapping_service.go|group_config_service.go|config.go|account_pool.go|^total"
  go test -race -count=1 ./internal/services/addomain/
  ```
  五个目标文件 per-file 数字 + 包 total 落 SUMMARY(基线包 23.1%;包级 ≥70% 由 78-07 收口)。若任一目标文件未达 ≥70%,SUMMARY 给出该文件逐函数缺口清单 + 差多少 stmts + 归因。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestPool78_" ./internal/services/addomain/ &amp;&amp; go test -race -count=1 ./internal/services/addomain/ &amp;&amp; go test -count=1 -coverprofile=cov78_06.out ./internal/services/addomain/ &amp;&amp; go tool cover -func=cov78_06.out | grep -E "computer.go|ou_group_mapping_service.go|group_config_service.go|config.go|account_pool.go" &amp;&amp; go build ./... &amp;&amp; go test ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestPool78_" internal/services/addomain/account_pool_78_06_test.go` ≥ 8
  - `go tool cover -func=cov78_06.out`:RecoverExpiredBreakers >0%(目标 ≥85%)、StartHotReload >0%、PickAvailable ≥70%、account_pool.go 加权 ≥90%
  - `go tool cover -func=cov78_06.out`:computer.go / ou_group_mapping_service.go / group_config_service.go / config.go **各 ≥70%**
  - PickAvailable 多账号用例不断言具体挑中项(防 flake)
  - `go test -race ./internal/services/addomain/` exit 0;`go test ./...` exit 0;`git diff --stat` 生产 .go 改动 = 0 文件
  </acceptance_criteria>
  <done>account_pool.go ≥90%;五个目标文件 per-file 达标数字落 SUMMARY;78-07 可在 wave 3 收口 addomain ≥70%。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 ↔ AD/LDAP | 零 LDAP 网络:computer 走 `[]*ldap.Entry` 字面量;唯一网络接触是 TestConnection 的 dial-fail 用例指向 `127.0.0.1` 已关闭端口 |
| 测试 ↔ Redis | 零 Redis:RecoverExpiredBreakers / StartHotReload 只覆盖 `redisPubSub == nil` 形态 |
| 测试 ↔ 数据库 | glebarez sqlite `:memory:` + 手动 CREATE TABLE,禁生产 PG |
| 测试 ↔ 凭据 | 账号密文只用 `"encrypted_pwd"` 字面量(account_pool_test.go 先例);AD 配置 admin_password 用 dummy |
| 测试 ↔ 生产代码 | 仅新增 *_test.go;生产改动仅 D-78-10 quirk 修复豁免 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-06-01 | Spoofing | 测试误连真实 AD 域控(TestConnection) | mitigate | server_address 仅 `127.0.0.1` + 本地 Listen 后立即 Close 取端口;验收 grep 无真实域名/DC IP |
| T-78-06-02 | DoS | LDAP dial 到关闭端口的等待时长不可控 | mitigate | 10s 硬超时守卫 + SUMMARY 记录实测耗时(与 78-05 同纪律) |
| T-78-06-03 | Tampering | sqlite 缺表/唯一约束方言差异导致改生产 SQL | mitigate | 缺表补 CREATE TABLE;isUniqueConstraintError 若不识别 sqlite 方言 → SUMMARY 记录待裁决(D-78-10 无据不改) |
| T-78-06-04 | Information Disclosure | 用真实计算机名/序列号/MAC/员工描述造 entry | mitigate | 全部 dummy(`CN=PC-TEST-01,OU=Test,DC=example,DC=com`);验收 grep 无真实资产标识 |
| T-78-06-05 | DoS | PickAvailable 随机挑选导致 flake | mitigate | 多账号用例只断言"返回值 ∈ 可用集合",禁断言具体项 |
| T-78-06-SC | Tampering | 依赖安装 | accept | 零新增依赖 |
</threat_model>

<verification>
- `go test -count=1 -timeout 180s ./internal/services/addomain/` 全绿;`go test -race -count=1 ./internal/services/addomain/` 全绿
- `go tool cover -func=cov78_06.out`:computer.go / ou_group_mapping_service.go / group_config_service.go / config.go 各 ≥70%;account_pool.go ≥90%
- SUMMARY 含 per-file 数字 + 包 total + 已知不覆盖清单(TestConnection bind 成功路径 / StartHotReload Redis 订阅路径)
- 测试文件无真实域名/资产标识/凭据;无 t.Parallel;断言无裸状态字面量
- `go build ./...` + `go test ./...` exit 0;生产 .go 改动 = 0 文件
</verification>

<success_criteria>
- 5 个测试文件存在,TestComp78_ ≥16 / TestOGM78_ ≥9 / TestGC78_ ≥5 / TestCfg78_ ≥8 / TestPool78_ ≥8 全绿
- computer.go / ou_group_mapping_service.go / group_config_service.go / config.go 各 ≥70%;account_pool.go ≥90%(RecoverExpiredBreakers 与 StartHotReload 从 0% 转正)
- 零 LDAP 网络、零 Redis 依赖、零生产改动
- 78-05 的 fixture helper 被复用而非重复实现(BLOCK-05 第二段)
</success_criteria>

<decision_audit>
| ID | 决策 | 依据 / 理由 |
|----|------|------------|
| D-78-06a | `StartHotReload` 只覆盖 `redisPubSub == nil` 早退,**不引入 miniredis 测 Redis 订阅路径** | 78-RESEARCH §3 表格未把 account_pool 的 pubsub 列入 Phase 78 范围(标 LOW/"已覆盖主体");miniredis 的使用预算已分配给 78-01(captcha 限流)与 78-02(core initCache);pubsub 订阅是跨进程语义,单测价值低。边界写进注释与 SUMMARY |
| D-78-06b | `TestConnection` 的 **bind 成功路径不在本 plan**,归 78-07 的 in-process LDAP 应答器(若落地) | PickFirstConnect 内部 `NewLDAPClient(...).Connect()` 需要真实 LDAP wire;78-RESEARCH R8 已明确"仅测 factory != nil 路径 / 不强求生产路径测试"。本 plan 覆盖两条失败分支已足以达成 config.go ≥70% |
| D-78-06c | `isUniqueConstraintError` 用**真实 sqlite driver error** 触发,而非伪造字符串 | 只有真实 error 才能暴露"仅适配 PG 文案、sqlite 不命中"这类方言缺口(与记忆教训"PG 数组 ANY() 需方言分支"同族);若确实不命中,按 D-78-10 记 SUMMARY 待裁决而非擅改 |
| D-78-06d | `PickAvailable` 多账号用例只断言"∈ 可用集合" | 该函数语义含随机/顺序挑选;断言具体项会随实现变化 flake(记忆 local-vs-ci-test-divergence) |
| D-78-06e | 复用 78-05 的 `setupSync78DB` / `entry78` 而非各文件自建 fixture | 同包 helper 重复定义会编译冲突;且 7 表建表 SQL 单一真相源便于后续维护 |
| D-78-08 / D-78-10 / D-78-11 | 命名 / quirk 纪律 / wave 编排 | 同 78-01 |
</decision_audit>

<output>
Create `.planning/workstreams/milestone/phases/78-block-bp-unlock-by-foundation/78-06-SUMMARY.md` when done
</output>
