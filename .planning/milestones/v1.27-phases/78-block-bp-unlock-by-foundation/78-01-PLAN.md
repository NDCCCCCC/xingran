---
phase: 78-block-bp-unlock-by-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/core/captcha_78_01_test.go
  - internal/core/captcha_background_78_01_test.go
  - internal/core/metrics_cache_78_01_test.go
autonomous: true
requirements: [BLOCK-03]

must_haves:
  truths:
    - "本 plan 交付 internal/core 的 captcha 三件套测试(captcha.go 15 函数 / captcha_background.go 11 函数 / metrics_cache.go 4 函数),全部经 sqlite(glebarez :memory: 或 t.TempDir 文件库)+ MemoryCache/miniredis-RedisCache 驱动,零真实 Redis / 零真实 PG"
    - "QUIRK-01(Phase 75 已修 MemoryCache.IncrementBy nil-deref)让 GenerateCaptcha 真实链路可直测:MemoryCache 走 Increment+Expire 降级分支,miniredis+RedisCache 走 cache.RateLimitCache 的 IncrementWithExpire 原子分支——两分支各自独立用例,测试注释显式标注 `// since QUIRK-01 the IncrementBy 真实链路 is reachable`,不再依赖 core_74_08_test.go 的 workaround 形态"
    - "按 D-78-01 缓存装配纪律:miniredis 用 miniredis.RunT(t)(自动 t.Cleanup),RedisCache 用完必 Close;不构造带 L2Writer 的 MultiLevelCache(R-7 goroutine 泄漏由 78-02 单独承担)"
    - "按 D-78-08 命名:测试文件 `<source>_78_01_test.go` 与源码同包共置(Phase 76/77 惯例 + 78-RESEARCH §1.3 Q1)"
    - "按 D-78-10 quirk 纪律:发现行为与据源(models 常量 / 函数注释 / docs/standards)不符 → 原子 commit 就地修 + 同 commit 翻转断言 + 回归用例 + SUMMARY deviation;无据 → 按现行为断言 + SUMMARY 记录待裁决"
    - "按 D-78-11 wave 编排:本 plan 位于 wave 1,与 78-03(device)/78-05(addomain)不同包并行;同包 78-02 排 wave 2(同包整包编译互踩规避,Phase 76/77 先例),78-02 复用本 plan 落地的 sqlite+miniredis 装配 helper"
    - "达标口径(2026-08-27 本地实测基线 internal/core 43.7%):captcha.go ≥85%(基线逐函数 GenerateCaptcha 56.5 / generateSliderWithBackground 35.4 / VerifySliderCaptcha 70.3 / LoadConfig 80.4 / abs 66.7)、captcha_background.go ≥75%(Upload 57.9 / preGenerateForConfig 14.3 / GetRandomEnabled 76.5)、metrics_cache.go = 100%,`go build ./...` + `go test ./...` 全绿"
    - "本 plan 裁决细则(Decision Audit 逐条依据,executor 必须遵守):D-78-01a(GetRandomEnabled 的 PG-only `allowed_shapes @>` / `jsonb_array_length` 分支在 sqlite 下接受不覆盖并落 SUMMARY,勿为覆盖率改 SQL)/ D-78-01b(mixed 随机分支断言口径 = N 次调用全部成功 + 至少一种形态命中,禁固定次数强断言,防 flake)"
  artifacts:
    - path: "internal/core/captcha_78_01_test.go"
      provides: "CaptchaService 全函数测试(前缀 TestCap78_),含 miniredis RateLimitCache 分支与 slider 全分支"
      min_lines: 380
      contains: "miniredis.RunT"
    - path: "internal/core/captcha_background_78_01_test.go"
      provides: "CaptchaBackgroundService 全函数测试(前缀 TestBg78_),含真 PNG 上传与预生成池"
      min_lines: 260
      contains: "StoragePath"
    - path: "internal/core/metrics_cache_78_01_test.go"
      provides: "MetricsCacheService 边缘分支测试(前缀 TestMx78_)"
      min_lines: 60
      contains: "NewMetricsCacheService"
  key_links:
    - from: "internal/core/captcha_78_01_test.go"
      to: "GenerateCaptcha 的 cache.RateLimitCache 分支(captcha.go:257)"
      via: "miniredis.RunT(t) + cache.NewRedisCache(mr.Addr()) 注入 NewCaptchaService"
      pattern: "IncrementWithExpire|NewRedisCache"
    - from: "internal/core/captcha_78_01_test.go"
      to: "generateSliderWithBackground 的自定义背景分支(captcha.go:569-613)"
      via: "SetBackgroundService(bg) + t.TempDir 真 PNG 落盘 + sys_captcha_background 行"
      pattern: "SetBackgroundService"
    - from: "internal/core/captcha_background_78_01_test.go"
      to: "Upload 的 StoragePath 落盘(captcha_background.go:80-95)"
      via: "同包私有字段 svc.config.StoragePath = t.TempDir()"
      pattern: "config\\.StoragePath"
---

<objective>
交付 internal/core 的 captcha 三件套测试:captcha.go(98 unc)+ captcha_background.go(48 unc)+ metrics_cache.go(2 unc),把"非 core.go 子集"从当前形态推到 ≥65%,并把 QUIRK-01 解锁的 GenerateCaptcha 真实链路(含 miniredis 的 RateLimitCache 原子分支)首次直测。

Purpose: BLOCK-03(internal/core ≥70%)的第一段。core.go Init 链 302 stmts 由 78-02 承担,本 plan 先把非装配层的 148 unc 清干净,并沉淀 sqlite + miniredis + MemoryCache 三种装配 helper 供 78-02 复用。
Output: 新增 3 个测试文件(零生产代码改动,除非触发 D-78-10 quirk 修复)。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/78-block-bp-unlock-by-foundation/78-RESEARCH.md

<interfaces>
<!-- 关键契约(planner 已逐一读源码复核行号,2026-08-27)。executor 直接使用,无需重新探索。 -->

From internal/core/captcha.go:
```go
// :51 构造器 —— db 是 *coredb.Database 包装(非裸 *gorm.DB),cache 是 pkg/cache.Cache
func NewCaptchaService(db *db.Database, cache cache.Cache) *CaptchaService
// :71 背景图服务注入(slider 自定义背景分支的开关)
func (s *CaptchaService) SetBackgroundService(bgService *CaptchaBackgroundService)

// :241 GenerateCaptcha 分支地图:
//   :245 config.Enabled == Disabled → 返回 (nil, nil)
//   :257 cache 断言 cache.RateLimitCache 成功 → IncrementWithExpire(仅 *RedisCache 实现,见 pkg/cache/cache.go:70)
//   :265 else → cache.Increment + count==1 时 Expire(MemoryCache 走这条 —— QUIRK-01 修复后可达)
//   :275 count > ipRateLimit → "获取验证码过于频繁"
//   :286 CaptchaTypeNormal → NewTextCaptcha(config.Type).GenerateBase64 + Set/SetInt
//   :313 CaptchaTypeSlider → generateSliderWithBackground + SetJSON
// :540 generateSliderWithBackground 分支:BackgroundMode ∈ {custom, mixed(rand 50%), auto, 未知→warn 回退}
//      × backgroundService nil/非 nil × GetFromCachePool 命中/未命中 × GetRandomEnabled 失败/nil/成功
//      × LoadBackgroundFromFile 失败/成功 × GenerateSliderWithCustomBackground 失败/成功
// :482 abs(x) —— 负数分支未覆盖(66.7%)
// :645 getIPRateLimit 读 sys_config 的 "sys.captcha.ip_rate_limit"
```

From internal/core/captcha_background.go:
```go
// :50 构造器默认 config.StoragePath = "./uploads/captcha/backgrounds"
//     ⚠️ 测试必须同包直写 svc.config.StoragePath = t.TempDir(),否则污染仓库工作树
// :74  Upload 分支:validateFile 失败 / MkdirAll 失败 / WriteFile 失败 /
//      getImageDimensions 失败(非图片字节 → 同时断言临时文件被 os.Remove)/ Create 失败 / happy path
// :142 GetRandomEnabled 分支:缓存命中(json.Unmarshal 成功且 len>0)/ 精确匹配 /
//      s.db.Type == "postgres" 的 allowed_shapes @> 分支(sqlite 不可达,Type 固定 "sqlite" 即绕过)/ 空结果 error
// :206 PreGeneratePool = 4 shape × 3 difficulty 双循环,内部错误静默吞
// :228 preGenerateForConfig(14.3%)—— 主力缺口
// :309 GetFromCachePool / :358 validateFile / :385 getImageDimensions / :401 calculateMD5
```

From internal/core/metrics_cache.go:
```go
// :19 NewMetricsCacheService(core *Core) —— 只用到 core 的少数字段,可用 &Core{CoreInfra: &CoreInfra{...}} 最小装配
// :34 GetCurrentMetrics(80%) / :57 GetServerInfo / :62 Stop(QUIRK-02 已 sync.Once 幂等)
```

既有 analog(必读,直接照搬装配手法):
- internal/core/core_74_08_test.go:31-46 `newCaptchaTestDB` —— sqlite 文件库 + 手动 CREATE TABLE sys_config + `&coredb.Database{DB: gormDB, Type: "sqlite"}` + `cache.NewMemoryCache(1000, 5*time.Minute)` + t.Cleanup Close。**本 plan 的 helper 以此为起点扩展**(加 sys_captcha_background 表 + miniredis 变体)。

miniredis 装配(INFRA-01,go.mod:8 `github.com/alicebob/miniredis/v2 v2.38.0`):
```go
mr := miniredis.RunT(t)                       // 自动 t.Cleanup 关停
rc, err := cache.NewRedisCache(&cache.RedisConfig{Addr: mr.Addr(), ...})  // 以 pkg/cache 实际签名为准,executor 读源码确认
t.Cleanup(func() { _ = rc.Close() })
// R-1: miniredis TTL 不自动流逝 —— 需要过期语义时用 mr.FastForward(d),禁裸 time.Sleep
// R-2: 不断言 Redis INFO 具体统计,只断言 err==nil + key 存在
```

真 PNG 字节(getImageDimensions/LoadBackgroundFromFile 都要求可解码):用 stdlib `image` + `image/png` 现造(`png.Encode(&buf, image.NewRGBA(image.Rect(0,0,300,150)))`),禁引入 fixture 二进制。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GenerateCaptcha 真实链路双分支(MemoryCache 降级 + miniredis RateLimitCache 原子)</name>
  <files>internal/core/captcha_78_01_test.go</files>
  <read_first>
  - internal/core/captcha.go — :241-348 GenerateCaptcha 全文 + :645 getIPRateLimit + :22-49 struct/config 定义
  - internal/core/core_74_08_test.go — :31-46 newCaptchaTestDB(装配起点)+ :110-170 既有 GenerateCaptcha 用例(避免重复,只补未覆盖分支)
  - pkg/cache/cache.go — :63-70 RateLimitCache 接口 + `var _ RateLimitCache = (*RedisCache)(nil)` 编译期断言
  - pkg/cache/redis.go — NewRedisCache 实际签名与 RedisConfig 字段(miniredis Addr 注入点)
  - pkg/cache/memory.go — QUIRK-01 修复后的 IncrementBy/Increment/Expire 语义(缺键=1 的 DB 语义)
  </read_first>
  <action>
  创建 internal/core/captcha_78_01_test.go(package core,同包白盒)。两个文件级 helper:

  - `newCap78Mem(t, configs map[string]string) (*CaptchaService, *gorm.DB, *cache.MemoryCache)` —— 照 core_74_08_test.go:31-46,但 CREATE TABLE 除 sys_config 外追加 `sys_captcha_background`(列集精简到 models.CaptchaBackground 被 GetRandomEnabled/GetFromCachePool/IncrementUseCount 实际引用的列:id/file_name/file_path/file_size/file_width/file_height/file_md5/piece_shape/difficulty_level/allowed_shapes/status/use_count/sort_order/last_used_at/created_at/updated_at/deleted_at)。
  - `newCap78Redis(t, configs map[string]string) (*CaptchaService, *gorm.DB, *miniredis.Miniredis)` —— 同一张库 + `miniredis.RunT(t)` + `cache.NewRedisCache(...)`,t.Cleanup 关 RedisCache。

  测试函数前缀 TestCap78_:

  - `TestCap78_GenerateCaptcha_MemoryCacheDegradedPath`:MemoryCache 装配 + captchaEnabled=normal。首调断言 resp 非 nil 且 CaptchaImg 前缀 `data:image/png;base64,`;文件头注释与本用例内注释写明
    `// since QUIRK-01 the IncrementBy 真实链路 is reachable —— MemoryCache 不实现 RateLimitCache,走 :265 Increment+Expire 降级分支;74-08 时代的 captcha workaround 分支已作废`
    并断言 count==1 时 Expire 被调用的可观察效果(限流键存在)。
  - `TestCap78_GenerateCaptcha_RateLimitCacheAtomicPath`:miniredis 装配 + captchaEnabled=normal → 断言走 :257 原子分支(可观察证据:限流键 `captcha:rate:<ip>` 在 miniredis 中存在且 TTL>0,用 `mr.TTL(key)` 断言;禁断言 INFO 统计,R-2)。
  - `TestCap78_GenerateCaptcha_RateLimitExceeded`:sys_config 置 `sys.captcha.ip_rate_limit=1` → 连调 2 次,第 2 次断言错误文案含"过于频繁"(:275)。MemoryCache 与 miniredis 两装配各跑一遍(表驱动)。
  - `TestCap78_GenerateCaptcha_FailClose`:限流基础设施不可用 → fail-close。miniredis 装配下 `mr.Close()` 后调用,断言错误文案含"服务繁忙"(:260 分支);注意 mr.Close 后 RunT 的 cleanup 二次 Close 必须安全(若 panic 则改用 `miniredis.Run()` + 手动 defer,SUMMARY 记录)。
  - `TestCap78_GenerateCaptcha_Disabled`:captchaEnabled 缺省(disabled)→ 断言 (nil, nil)(:245)。
  - `TestCap78_GetIPRateLimit_Edge`:sys_config 无该键 → 10;非法值(非数字)→ 10;合法值 25 → 25。

  纪律:禁 t.Parallel()(captcha 与 metrics 走包级全局/共享 sqlite 文件);miniredis 时间推进只用 mr.FastForward(R-1)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestCap78_" ./internal/core/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestCap78_" internal/core/captcha_78_01_test.go` ≥ 6
  - `grep -c "miniredis.RunT\|miniredis.Run(" internal/core/captcha_78_01_test.go` ≥ 1
  - 文件含字面注释 `since QUIRK-01 the IncrementBy 真实链路 is reachable`
  - 测试文件不含裸 `time.Sleep` 用于 TTL 推进(TTL 语义一律 mr.FastForward)
  - `go test -count=1 -run "TestCap78_" ./internal/core/` exit 0;`go build ./...` exit 0
  </acceptance_criteria>
  <done>GenerateCaptcha 的两条限流分支 + 限流超限 + fail-close + disabled 全部有独立用例;sqlite/MemoryCache/miniredis 三装配 helper 就位(78-02 复用)。</done>
</task>

<task type="auto">
  <name>Task 2: slider 全分支(自定义背景 × 缓存池 × 降级)+ VerifySliderCaptcha/abs 剩余</name>
  <files>internal/core/captcha_78_01_test.go</files>
  <read_first>
  - internal/core/captcha.go — :313-345(slider 存储链)+ :409-489 VerifySliderCaptcha + :482 abs + :540-643 generateSliderWithBackground 全文
  - internal/core/captcha_background.go — :309 GetFromCachePool / :142 GetRandomEnabled(被 slider 分支调用的两个入口)
  - pkg/captcha/ — NewSliderCaptchaWithShape / LoadBackgroundFromFile / GenerateSliderWithCustomBackground / GenerateSliderBase64 的实际签名与失败条件
  </read_first>
  <action>
  同文件追加 TestCap78_ 用例,覆盖 generateSliderWithBackground 的分支矩阵。背景图用 stdlib png 现造并落 t.TempDir,DB 行的 file_path 指向该真实文件:

  - `TestCap78_Slider_AutoFallback`:BackgroundMode="auto" → useCustom=false → 直接降级到 GenerateSliderBase64(:626-643),断言 sliderImg/pieceImg 前缀与 xPos/yPos/token 非零值。
  - `TestCap78_Slider_UnknownMode`:BackgroundMode="bogus" → default 分支(:565 warn + 回退 auto),断言仍返回有效结果(行为断言,非日志断言)。
  - `TestCap78_Slider_CustomModeNoBackgroundService`:BackgroundMode="custom" 且未 SetBackgroundService → 走 :617-624 的 nil 提示分支后降级成功。
  - `TestCap78_Slider_CustomModeCachePoolHit`:SetBackgroundService + 预先把一份合法 map 写入 GetFromCachePool 读取的缓存键 → 断言 :572-576 命中路径直接返回(可观察证据:返回 map 的 token 等于预置值)。缓存键以 captcha_background.go:309 实现为准。
  - `TestCap78_Slider_CustomModeDBHitLoadOK`:缓存池空 + sys_captcha_background 一行(status=enabled,piece_shape/difficulty 与 config 匹配,file_path=真 PNG)→ 断言返回 map 含 `backgroundId` 键(仅自定义背景路径才有,:602)且该行 use_count 被 +1(IncrementUseCount 落库断言)。
  - `TestCap78_Slider_CustomModeLoadFileFail`:DB 行 file_path 指向不存在的路径 → :591 LoadBackgroundFromFile 失败 → 降级到 auto,断言返回 map **不含** `backgroundId`。
  - `TestCap78_Slider_MixedMode`:BackgroundMode="mixed" → rand 50%。用循环调用 N=20 次并断言两种结果形态均出现过(或至少不 panic 且全部成功);随机分支不得用固定次数强断言(避免 flake),以"全部调用成功 + 至少一种形态命中"为断言口径,SUMMARY 记录该口径。
  - `TestCap78_GenerateCaptcha_SliderStorage`:config.Enabled=slider 全链 → 断言 SetJSON 写入的 SliderVerifyData 可被 VerifySliderCaptcha 用正确 xPos/token 通过、错误 xPos 拒绝、错误 token 拒绝、attempts 超限拒绝、captchaID 不存在拒绝(:409-480 剩余分支)。
  - `TestCap78_Abs`:abs(-5)==5 / abs(5)==5 / abs(0)==0(补 :482 的负数分支)。

  D-78-10:若 mixed 模式的 rand 使用方式导致测试不可稳定断言,按现行为断言 + SUMMARY 记录,禁为覆盖率改生产随机逻辑。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestCap78_" ./internal/core/ && go test -count=1 -coverprofile=/tmp/c78_01.out ./internal/core/ && go tool cover -func=/tmp/c78_01.out | grep "captcha.go"</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestCap78_" internal/core/captcha_78_01_test.go` ≥ 14(Task 1+2 合计)
  - 存在同时断言 `backgroundId` 存在(自定义路径)与不存在(降级路径)的两个用例
  - `go tool cover -func=/tmp/c78_01.out | grep "generateSliderWithBackground"` 覆盖率 ≥70%(基线 35.4%)
  - `go tool cover -func=/tmp/c78_01.out | grep "captcha.go:241.*GenerateCaptcha"` 覆盖率 ≥85%(基线 56.5%)
  - `go test -count=1 ./internal/core/` exit 0
  </acceptance_criteria>
  <done>slider 分支矩阵全覆盖(自定义/缓存池/降级三态)+ VerifySliderCaptcha 剩余分支 + abs 负数分支。</done>
</task>

<task type="auto">
  <name>Task 3: captcha_background Upload 全分支 + GetRandomEnabled(t.TempDir 落盘隔离)</name>
  <files>internal/core/captcha_background_78_01_test.go</files>
  <read_first>
  - internal/core/captcha_background.go — :50-140 Upload + UploadRequest + :142-192 GetRandomEnabled + :358-399 validateFile/getImageDimensions + :401 calculateMD5
  - internal/models/ — CaptchaBackground / PieceShape / DifficultyLevel / CaptchaBgEnabled 具名常量(断言禁裸 0/1)
  - internal/core/captcha_78_01_test.go — Task 1 落地的表结构与装配 helper(本任务复用同一张 sys_captcha_background)
  </read_first>
  <action>
  创建 internal/core/captcha_background_78_01_test.go(package core)。helper `newBg78(t) (*CaptchaBackgroundService, *gorm.DB)`:sqlite(t.TempDir 文件库)+ CREATE TABLE sys_captcha_background + `cache.NewMemoryCache` + **同包直写 `svc.config.StoragePath = t.TempDir()`**(强制,防污染仓库工作树)。测试函数前缀 TestBg78_:

  - `TestBg78_Upload_Success`:真 PNG 字节(stdlib png.Encode 300×150)→ 断言返回 bg 的 FileWidth=300/FileHeight=150、FilePath 用正斜杠(filepath.ToSlash)、FileMD5 等于 md5(data)、Status=models.CaptchaBgEnabled、落盘文件真实存在。
  - `TestBg78_Upload_ValidateFail`:非法扩展名(.exe)/ 无扩展名 / FileSize 超 MaxFileSize 三分支 → 断言错误含"文件验证失败"且 StoragePath 目录内无新文件。
  - `TestBg78_Upload_BadImageBytes`:文件名 .png 但字节是 "not-an-image" → getImageDimensions 失败分支,断言错误含"获取图片尺寸失败" **且落盘临时文件已被 os.Remove**(目录为空,:97 的清理语义)。
  - `TestBg78_Upload_DBCreateFail`:DROP TABLE sys_captcha_background 后上传真 PNG → 断言错误含"创建数据库记录失败" 且落盘文件已被 os.Remove(:124)。
  - `TestBg78_Upload_MkdirFail`:StoragePath 指向一个已存在的**普通文件**路径 → MkdirAll 失败分支;若该分支在当前平台不可复现(Windows 语义差异),按 D-78-10 降级为跳过该子用例并在 SUMMARY 记录,禁改生产代码。
  - `TestBg78_GetRandomEnabled_ExactMatch`:2 行匹配 + 1 行 shape 不匹配 → 断言返回值 ∈ 匹配集合;并断言结果被写入缓存(第二次调用命中缓存分支 :148-152,可观察证据:DROP TABLE 后第二次调用仍成功返回)。
  - `TestBg78_GetRandomEnabled_Empty`:无匹配行 → 断言错误含"没有找到可用的背景图"(sqlite 下 `s.db.Type != "postgres"` 直接跳过 PG-only 的 allowed_shapes 分支 —— 该分支为 PG 专属,sqlite 不可达,SUMMARY 记录为已知不覆盖)。
  - `TestBg78_ValidateFile_Table` / `TestBg78_CalculateMD5` / `TestBg78_GetImageDimensions`:表驱动补齐边角(含 getImageDimensions 打不开文件的错误分支)。

  状态断言一律引用 internal/models 具名常量(models.CaptchaBgEnabled / models.PieceShape*),禁裸 0/1。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestBg78_" ./internal/core/ && git status --porcelain uploads/ 2>/dev/null; go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestBg78_" internal/core/captcha_background_78_01_test.go` ≥ 8
  - `grep -c "config.StoragePath = " internal/core/captcha_background_78_01_test.go` ≥ 1 且赋值右侧为 `t.TempDir()`
  - 测试跑完 `git status --porcelain` 不出现 `uploads/` 下的新文件(落盘隔离生效)
  - `go test -count=1 -run "TestBg78_" ./internal/core/` exit 0
  </acceptance_criteria>
  <done>Upload 六分支(含两处失败清理断言)+ GetRandomEnabled 三分支 + validateFile/getImageDimensions/calculateMD5 边角全覆盖;零工作树污染。</done>
</task>

<task type="auto">
  <name>Task 4: preGenerateForConfig + PreGeneratePool + GetFromCachePool 缓存池链</name>
  <files>internal/core/captcha_background_78_01_test.go</files>
  <read_first>
  - internal/core/captcha_background.go — :206-227 PreGeneratePool + :228-308 preGenerateForConfig(14.3%,主力缺口)+ :309-357 GetFromCachePool
  - internal/core/captcha_background_78_01_test.go — Task 3 的 newBg78 helper
  </read_first>
  <action>
  同文件追加:

  - `TestBg78_PreGenerateForConfig_NoBackground`:无匹配背景行 → 早退分支(断言 err 形态与是否写缓存池,以源码实现为准)。
  - `TestBg78_PreGenerateForConfig_Success`:预置 1-2 行真 PNG 背景 → 调用后断言缓存池键被写入(用 GetFromCachePool 能取到 N 条,或直接查 cache 的键;键名以 :228-308 实现为准),并断言生成条目的 map 含 sliderImg/pieceImg/xPos/yPos/token。
  - `TestBg78_PreGenerateForConfig_BadFile`:背景行 file_path 指向不存在文件 → LoadBackgroundFromFile 失败的容错分支(断言不 panic,错误被吞或返回,按实现断言)。
  - `TestBg78_PreGeneratePool_AllShapes`:4 shape × 3 difficulty 双循环走完 → 断言不 panic 且返回 nil(内部失败静默吞的语义),并断言至少一个配置的缓存池非空(需预置对应 shape/difficulty 的背景行)。
  - `TestBg78_GetFromCachePool_MissAndHit`:空池 → 错误/空返回分支;预置池 → 命中返回并断言"取出后池内数量减少或键被消费"(以实现语义为准);缓存中存的是坏 JSON → Unmarshal 失败分支。
  - `TestBg78_IncrementUseCount`:存在行 → use_count+1 且 last_used_at 被更新;不存在 id → 不报错(GORM UpdateColumn 语义)。

  D-78-10:若 preGenerateForConfig 的缓存键或数量语义与函数注释不符,原子 commit 修 + 同 commit 翻转断言 + SUMMARY deviation。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestBg78_" ./internal/core/ && go test -count=1 -coverprofile=/tmp/c78_01b.out ./internal/core/ && go tool cover -func=/tmp/c78_01b.out | grep "captcha_background.go"</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestBg78_" internal/core/captcha_background_78_01_test.go` ≥ 14(Task 3+4 合计)
  - `go tool cover -func=/tmp/c78_01b.out | grep "preGenerateForConfig"` 覆盖率 ≥70%(基线 14.3%)
  - `go tool cover -func=/tmp/c78_01b.out | grep "captcha_background.go:74.*Upload"` 覆盖率 ≥85%(基线 57.9%)
  - `go test -count=1 ./internal/core/` exit 0
  </acceptance_criteria>
  <done>缓存池预生成链(preGenerateForConfig 主力缺口)+ GetFromCachePool 三态 + IncrementUseCount 全覆盖。</done>
</task>

<task type="auto">
  <name>Task 5: metrics_cache 边缘 + per-file coverage checkpoint(captcha ≥85% / background ≥75%)</name>
  <files>internal/core/metrics_cache_78_01_test.go</files>
  <read_first>
  - internal/core/metrics_cache.go — 全文(4 函数;NewMetricsCacheService 80% / GetCurrentMetrics 80% / GetServerInfo 100% / Stop 100%)
  - internal/core/core_74_08_test.go — 既有 MetricsCacheService 用例(避免重复,只补 unc 分支)
  - .planning/research/v1.27-pitfalls.md — M-1(差值采样零增量:只断言区间 + err==nil)/ M-2(包级全局无锁:禁 t.Parallel)
  </read_first>
  <action>
  创建 internal/core/metrics_cache_78_01_test.go(package core)。前缀 TestMx78_:

  - `TestMx78_NewMetricsCacheService_NilPaths`:最小 Core 装配(`&Core{CoreInfra: &CoreInfra{Config: cfg}, CoreServices: &CoreServices{}}`)覆盖 :19 剩余分支(Cache 为 nil / 非 nil 两形态)。
  - `TestMx78_GetCurrentMetrics_ErrorAndCacheBranch`:覆盖 :34 的 20% unc —— 缓存命中路径与查询失败路径各一;M-1 纪律:CPU/内存数值只断言 `err == nil` 与区间 [0,100],禁断言具体值。
  - `TestMx78_GetServerInfo`:断言返回 map 的必备键存在(不断言具体值)。
  - `TestMx78_Stop_Idempotent`:连调 Stop() 三次不 panic(QUIRK-02 sync.Once 幂等的回归锁定;78-02 的 Core.Close 依赖此语义)。

  收尾 checkpoint(本 plan 交付口径):
  ```
  go test -count=1 -coverprofile=cov78_01.out ./internal/core/
  go tool cover -func=cov78_01.out | grep -E "captcha.go|captcha_background.go|metrics_cache.go|^total"
  ```
  把 captcha.go / captcha_background.go / metrics_cache.go 三个文件的加权覆盖率与包总覆盖率数字落 SUMMARY(基线:包 43.7%;core.go Init 链仍 0%,包总数由 78-02 收口到 ≥70%)。另运行一次 `go test -race -count=1 ./internal/core/` 确认无数据竞争(R-7/M-2)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestMx78_" ./internal/core/ && go test -race -count=1 ./internal/core/ && go test -count=1 -coverprofile=cov78_01.out ./internal/core/ && go tool cover -func=cov78_01.out | grep -E "captcha.go|captcha_background.go|metrics_cache.go" && go test ./... 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestMx78_" internal/core/metrics_cache_78_01_test.go` ≥ 4
  - `go tool cover -func=cov78_01.out` 中 captcha.go 各函数加权 ≥85%、captcha_background.go ≥75%、metrics_cache.go 全 100%
  - "非 core.go 子集"(captcha.go + captcha_background.go + metrics_cache.go 合计)≥65%,数字落 SUMMARY
  - `go test -race -count=1 ./internal/core/` exit 0(无 goroutine 竞争)
  - `go test ./...` exit 0;`git diff --stat` 生产 .go 改动 = 0 文件(除非 D-78-10 quirk 修复 + SUMMARY deviation)
  </acceptance_criteria>
  <done>metrics_cache 100%;三文件 per-file 覆盖率达标数字落 SUMMARY;-race 全绿;78-02 可在同包 wave 2 接手 core.go Init 链。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 ↔ 数据库 | 全部 glebarez sqlite(:memory: 或 t.TempDir 文件库),不触碰生产 PG |
| 测试 ↔ Redis | 仅 miniredis.RunT 进程内实例(mr.Addr()),禁任何真实 Redis 地址字面量 |
| 测试 ↔ 文件系统 | 上传/背景图落盘一律 t.TempDir;StoragePath 默认值 `./uploads/...` 必须被覆盖 |
| 测试 ↔ 生产代码 | 仅新增 *_test.go;生产 .go 改动仅 D-78-10 quirk 修复豁免 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-01-01 | Tampering | Upload 默认 StoragePath 污染仓库工作树(`./uploads/captcha/backgrounds`) | mitigate | helper 强制 `svc.config.StoragePath = t.TempDir()`;验收含 `git status --porcelain` 无 uploads/ 新文件 |
| T-78-01-02 | Tampering | 测试误连真实 Redis/PG | mitigate | 仅 mr.Addr() 与 sqlite;grep 测试文件无 redis:// / postgres DSN 字面量 |
| T-78-01-03 | DoS | miniredis/RedisCache/MemoryCache 未 Close 导致 goroutine 泄漏 | mitigate | miniredis.RunT 自动 cleanup + RedisCache/MemoryCache t.Cleanup Close + `go test -race` 验收 |
| T-78-01-04 | Information Disclosure | 验证码 token/密钥用真实值 | accept | 全部为测试字面量与现造 PNG,无真实凭据入仓 |
| T-78-01-SC | Tampering | 依赖安装 | accept | 零新增依赖(miniredis 已在 go.mod:8 由 INFRA-01 引入;image/png/md5 为 stdlib) |
</threat_model>

<verification>
- `go test -count=1 ./internal/core/` 全绿;`go test -race -count=1 ./internal/core/` 全绿
- `go tool cover -func=cov78_01.out`:captcha.go ≥85% / captcha_background.go ≥75% / metrics_cache.go 100%
- 非 core.go 子集合计 ≥65%,数字落 SUMMARY
- `go build ./...` + `go test ./...` exit 0
- 生产 .go 改动 = 0 文件(或仅 D-78-10 quirk 修复 + SUMMARY deviation);`git status --porcelain` 无 uploads/ 残留
</verification>

<success_criteria>
- 三个测试文件存在,TestCap78_ ≥14 / TestBg78_ ≥14 / TestMx78_ ≥4 全绿
- GenerateCaptcha 的 RateLimitCache 原子分支与 MemoryCache 降级分支各有独立用例,且文件含 QUIRK-01 溯源注释
- captcha.go ≥85%、captcha_background.go ≥75%、metrics_cache.go 100%
- sqlite / MemoryCache / miniredis 三装配 helper 沉淀,78-02 直接复用(BLOCK-03 第一段)
</success_criteria>

<decision_audit>
本 plan 涉及的 phase-78 决策(78-RESEARCH.md 未逐条给出实施口径的部分,在此定案):

| ID | 决策 | 依据 / 理由 |
|----|------|------------|
| D-78-01 | captcha 的 Redis 侧只用 `miniredis.RunT + NewRedisCache`,**不**在本 plan 构造 MultiLevelCache | 源自 78-RESEARCH DQ1 推荐 (a)。captcha 只需要 `cache.RateLimitCache` 断言成立(pkg/cache/cache.go:70 只有 *RedisCache 实现),多级缓存的 R-7 L2Writer goroutine 风险与本 plan 无关,统一由 78-02 承担 |
| D-78-08 | 测试文件命名 `<source>_78_01_test.go`(如 `captcha_78_01_test.go`),而非直接叫 `captcha_test.go` | 78-RESEARCH §1.3 Q1 明确"同包 `<topic>_78_xx_test.go` 共置";Phase 76/77 已按此惯例落盘;直名 `captcha_test.go` 会与既有 core_74_08_test.go 的职责边界混淆并丢失 phase 可追溯性。文件仍与源码同包同目录,符合 Go 约定 |
| D-78-10 | quirk 纪律沿用 Phase 77 的 D-01/D-02/D-03(发现即修 / plan 内就地修 / 有据判定) | Phase 77 已验证有效;本 phase 不重新立规 |
| D-78-11 | wave 编排:同包 plan 串行隔 wave(78-01 wave1 → 78-02 wave2),跨包并行 | 沿用 Phase 77 D-09;同包并行会互踩 per-task 的整包 `go test` 编译 |
| D-78-01a | PG-only 分支(GetRandomEnabled 的 `allowed_shapes @>` / `jsonb_array_length`)在 sqlite 下**接受不覆盖**,SUMMARY 记录 | 与 Phase 77 P-77-1(DISTINCT ON/REGEXP_REPLACE 属 PG-only,勿为覆盖率改 SQL)同一裁决口径,D-78-10"无据不改" |
| D-78-01b | mixed 随机分支的断言口径 = "N 次调用全部成功 + 至少一种形态命中",不做固定次数强断言 | 防 flake(参见记忆 local-vs-ci-test-divergence:随机/异步用宽松断言);不为覆盖率改生产 rand 逻辑 |

基线数字来源:planner 于 2026-08-27 本地实测 `go test -coverprofile ./internal/core/` → 包 43.7%(ROADMAP 记录 40.2% 为更早快照),逐函数数字见各 task 的 read_first/acceptance。
</decision_audit>

<output>
Create `.planning/workstreams/milestone/phases/78-block-bp-unlock-by-foundation/78-01-SUMMARY.md` when done
</output>
