---
phase: 78-block-bp-unlock-by-foundation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/services/addomain/sync_78_05_test.go
autonomous: true
requirements: [BLOCK-05]

must_haves:
  truths:
    - "本 plan 交付 addomain `sync.go` 全链测试(基线实测 18 个函数**全部 0.0%**:NewSyncService / SyncDataByID / SyncData / syncDataInternal / syncOUs / extractDNs / safeAttr / getExistingOUs / categorizeOUs / batchCreateOUs / batchUpdateOUs / syncGroups / createGroupsInBatches / updateGroupsInBatches / syncUsers / syncGroupMembers / updateSyncLog / parseGroupTypeFromLDAP),全部以 sqlite + `[]*ldap.Entry` 字面量数据驱动,零 LDAP 网络、零 mock 框架"
    - "按 D-78-07(planner 新决策,推翻 78-RESEARCH 隐含的 seam 需求):**拒绝为 sync.go 添加生产 seam**。`syncDataInternal`(sync.go:80)内部 `fc := NewFailoverClient(s.pool, config)` 是局部构造、clientFactory 不可注入 —— 但 sync.go 的 316 stmts 里约 240 落在 **entry-driven 私有方法**(syncOUs/syncGroups/syncUsers/syncGroupMembers 及其 batch/categorize 子函数),同包测试可直调;syncDataInternal 自身的**失败路径**(账号池空 → ErrAllAccountsUnavailable / dial 全失败)也无需 seam。happy path 的 ~25 stmts 接受不覆盖(SUMMARY 记录;若 78-07 的 in-process LDAP 应答器落地,可作为可选回补)"
    - "sqlite fixture 用手动 `CREATE TABLE` 而非 AutoMigrate(仓内 addomain 既有先例:dept_ou_mapper_test.go:18 注释「直接创建表结构(避免 AutoMigrate 的外键问题)」/ group_sync_threshold_test.go:20-58 / account_pool_test.go:17-49),列集精简到被测函数实际引用列,表名用生产表名(sys_ad_ou / sys_ad_group / sys_ad_user / sys_ad_group_member / sys_ad_sync_log / sys_ad_config / sys_ad_service_accounts)"
    - "状态/枚举断言一律引用 `internal/models` 具名常量(models.ADSyncStatusRunning/Success/Failed、models.ADSyncType*、models.ADConfigStatusEnabled、组类型常量等),禁裸 0/1 字面量(status_constants_test.go AST 守护锁值)"
    - "syncUsers(sync.go:434,182 stmts 单函数最大块)必须按分支矩阵逐个造 entry:新建 / 更新 / 软删恢复 / `$DUPLICATE-` 前缀过滤 / 计算机账号(以 `$` 结尾)过滤 / manager 关联 / 时间戳属性解析失败 / OU 归属 —— 这是本 plan 的达标主力"
    - "按 D-78-08 命名(`sync_78_05_test.go`);按 D-78-10 quirk 纪律;按 D-78-11 wave 编排(本 plan wave 1,与 78-01/78-03 跨包并行;同包 78-06/78-07 依次排 wave 2/3,复用本 plan 落地的 sqlite fixture helper)"
    - "达标口径(2026-08-27 本地实测基线 internal/services/addomain 23.1%):`sync.go` ≥80%,`go build ./...` + `go test ./...` + `go test -race ./internal/services/addomain/` 全绿,生产 .go 改动 = 0 文件"
    - "本 plan 裁决细则(Decision Audit 逐条依据,executor 必须遵守):D-78-05a(fixture 用手动 CREATE TABLE 而非 AutoMigrate —— dept_ou_mapper_test.go:18 已注明理由,且 sqlite 有 __temp+RENAME 与视图依赖风险)/ D-78-05b(不重复 sync_singleflight_test.go 已覆盖的 singleflight 语义)/ D-78-05c(软删恢复、空 memberDNs、不存在 logID 等无据源语义一律按现行为断言 + 注释标注依据行号)"
  artifacts:
    - path: "internal/services/addomain/sync_78_05_test.go"
      provides: "SyncService 全链 entry-driven 测试(前缀 TestSync78_)+ 7 表 sqlite fixture helper + ldap.Entry 构造 helper"
      min_lines: 620
      contains: "func setupSync78DB"
  key_links:
    - from: "internal/services/addomain/sync_78_05_test.go"
      to: "syncOUs / syncGroups / syncUsers / syncGroupMembers(sync.go:179/313/434/616)"
      via: "同包直调私有方法,喂 []*ldap.Entry 字面量 + *models.ADConfig"
      pattern: "s\\.sync(OUs|Groups|Users|GroupMembers)\\("
    - from: "entry 构造 helper"
      to: "github.com/go-ldap/ldap/v3 Entry/EntryAttribute"
      via: "&ldap.Entry{DN: ..., Attributes: []*ldap.EntryAttribute{{Name:..., Values: []string{...}}}}"
      pattern: "ldap\\.Entry\\{"
    - from: "syncDataInternal 失败路径用例"
      to: "ErrAllAccountsUnavailable(账号池空)"
      via: "NewAccountPool(db, nil) + sys_ad_service_accounts 空表 → FailoverClient.ListAvailable 返回空"
      pattern: "ErrAllAccountsUnavailable"
---

<objective>
交付 addomain `sync.go` 的全链测试(18 个函数当前实测全 0%,316 stmts):以 sqlite + `[]*ldap.Entry` 字面量为唯一驱动面,覆盖 OU/Group/User/GroupMember 四条同步管道及其 batch/categorize 子函数、同步日志状态机、以及 syncDataInternal 的失败路径,使 `sync.go` ≥80%。

Purpose: BLOCK-05(internal/services/addomain ≥70%,缺 ~1165 stmts)的第一段与最大单文件。`[]*ldap.Entry` 数据驱动边界是 v1.27-features §1.2 seam #5,零基建依赖可先行(wave 1)。
Output: 新增 internal/services/addomain/sync_78_05_test.go(零生产代码改动 —— 见 D-78-07)。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/78-block-bp-unlock-by-foundation/78-RESEARCH.md

<interfaces>
<!-- 关键契约(planner 已读 sync.go 复核行号,2026-08-27)。 -->

```go
// sync.go:20-36
type SyncService struct {
    db        *gorm.DB
    pool      AccountPool              // Phase 38 DI;syncDataInternal 里 NewFailoverClient(s.pool, config)
    syncGroup singleflight.Group       // 同 configID+syncType 并发合并
}
func NewSyncService(db *gorm.DB, pool AccountPool) *SyncService
// 同包测试可直接 &SyncService{db: db}(sync_singleflight_test.go:34 先例)或 &SyncService{db: db, pool: pool}

// 被测私有方法签名(全部同包可直调,全部 entry-driven):
func (s *SyncService) syncOUs(ctx, config *models.ADConfig, entries []*ldap.Entry) error          // :179
func extractDNs(entries []*ldap.Entry) []string                                                   // :197
func safeAttr(...)                                                                                // :216(读源码确认签名)
func (s *SyncService) getExistingOUs(ctx, configID string, ouDNs []string) []models.ADOU           // :221
func (s *SyncService) categorizeOUs(entries, existingOUs, config, now) ([]models.ADOU, map[string]*models.ADOU) // :228
func (s *SyncService) batchCreateOUs(ctx, ous []models.ADOU) error                                 // :268
func (s *SyncService) batchUpdateOUs(ctx, ous map[string]*models.ADOU) error                        // :279
func (s *SyncService) syncGroups(ctx, config, entries) error                                       // :313
func (s *SyncService) createGroupsInBatches(ctx, groups []models.ADGroup) error                     // :383
func (s *SyncService) updateGroupsInBatches(ctx, groups map[string]*models.ADGroup) error           // :403
func (s *SyncService) syncUsers(ctx, config, entries) error                                        // :434(182 stmts,最大块)
func (s *SyncService) syncGroupMembers(ctx, config, groupDN string, memberDNs []string) error       // :616
func (s *SyncService) updateSyncLog(ctx, logID string, status models.ADSyncStatus, ou, grp, usr, comp int, errMsg string) // :652
func parseGroupTypeFromLDAP(...)                                                                   // :701
```

syncDataInternal 编排(:80-176,**不可注入 seam,见 D-78-07**):
```
1. db.Create(syncLog{SyncStatus: models.ADSyncStatusRunning})      ← 失败即 return "创建同步日志失败"
2. fc := NewFailoverClient(s.pool, config)                          ← 局部构造
   fc.ExecuteWithFailover(闭包内 SearchOUs/SearchGroups/SearchUsers/SearchComputers 收集到外层变量)
   失败 → updateSyncLog(Failed) + errors.Is(err, ErrAllAccountsUnavailable) 分支 → "AD 账号池无可用账号..." / "连接AD服务器失败"
3. syncOUs → 失败则 updateSyncLog(Failed, ouCount,0,0,0)
4. syncGroups → 失败则 updateSyncLog(Failed, ou, grp,0,0)
5. syncUsers → 失败则 updateSyncLog(Failed, ou,grp,usr,0)
6. NewComputerService(s.db).syncComputers → 失败则 updateSyncLog(Failed, 全四计数)
7. config.last_sync_at 更新(失败仅 warn)+ updateSyncLog(Success)
```
→ 可测:第 1 步失败分支、第 2 步两个失败分支(空池 / dial 全失败)。**第 3-7 步只在闭包成功后才到达 → 接受不覆盖**(私有方法已被单独直测)。

SyncData(:57)= singleflight 包裹;SyncDataByID(:48)= 先按 `id = ? AND status = ?(models.ADConfigStatusEnabled)` 查 config,查不到报"AD配置不存在或未启用"。singleflight 语义已有 `sync_singleflight_test.go` 4 个用例覆盖(**勿重复**),本 plan 只补 SyncDataByID 的两分支与 SyncData 的 error/nil 透传。

sqlite fixture 先例(照搬风格):
- `internal/services/addomain/dept_ou_mapper_test.go`:15-40(`gorm.Open(sqlite.Open(":memory:"))` + db.Exec CREATE TABLE,注释说明避开 AutoMigrate 外键问题)
- `internal/services/addomain/group_sync_threshold_test.go`:20-58(sys_ad_group / sys_ad_group_member 完整列定义,可直接复制扩展)
- `internal/services/addomain/account_pool_test.go`:17-49(sys_ad_service_accounts 完整列定义 + insertAccount helper —— 本 plan 的账号池空/有账号两态直接复用该建表 SQL)

ldap.Entry 构造(go-ldap/ldap/v3 v3.4.12):
```go
func entry78(dn string, kv map[string][]string) *ldap.Entry {
    attrs := make([]*ldap.EntryAttribute, 0, len(kv))
    for k, v := range kv { attrs = append(attrs, &ldap.EntryAttribute{Name: k, Values: v}) }
    return &ldap.Entry{DN: dn, Attributes: attrs}
}
// entry.GetAttributeValue(name) 是 go-ldap 提供的读取入口(生产代码用它),缺失属性返回 ""
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 7 表 sqlite fixture + entry helper + OU 管道全链(syncOUs/categorizeOUs/batch*/extractDNs/safeAttr)</name>
  <files>internal/services/addomain/sync_78_05_test.go</files>
  <read_first>
  - internal/services/addomain/sync.go — :179-312(syncOUs / extractDNs / safeAttr / getExistingOUs / categorizeOUs / batchCreateOUs / batchUpdateOUs)
  - internal/services/addomain/dept_ou_mapper_test.go — :15-40 手动 CREATE TABLE 风格与注释理由
  - internal/services/addomain/group_sync_threshold_test.go — :20-58 sys_ad_group / sys_ad_group_member 列定义
  - internal/services/addomain/account_pool_test.go — :17-49 sys_ad_service_accounts 列定义 + insertAccount
  - internal/models/ — ADOU / ADGroup / ADUser / ADGroupMember / ADSyncLog / ADConfig 字段与列名映射;ADSyncStatus* / ADSyncType* / ADConfigStatus* 常量
  </read_first>
  <action>
  创建 `internal/services/addomain/sync_78_05_test.go`(package addomain,同包白盒)。两个文件级 helper:

  - `setupSync78DB(t *testing.T) *gorm.DB`:`gorm.Open(sqlite.Open(":memory:"), &gorm.Config{})` + 一段 `db.Exec` 建 7 张表(表名用生产表名):
    * `sys_ad_config`(id / config_name / server_address / server_port / base_dn / domain_name / admin_username / admin_password / use_ssl / use_tls / status / last_sync_at / created_at / updated_at / deleted_at)
    * `sys_ad_ou`(id / ad_config_id / ou_dn / ou_name / parent_dn / description / last_sync_at / created_at / updated_at / deleted_at)
    * `sys_ad_group`(照 group_sync_threshold_test.go:31-46 原样)
    * `sys_ad_user`(id / ad_config_id / user_dn / username / display_name / email / phone / mobile / title / department / description / ou_dn / is_enabled / manager_dn / last_logon / when_created / last_sync_at / created_at / updated_at / deleted_at —— 列集以 syncUsers 实际写入字段为准,executor 读 :434-615 逐列确认)
    * `sys_ad_group_member`(照 group_sync_threshold_test.go:49-56)
    * `sys_ad_sync_log`(id / ad_config_id / sync_type / sync_status / ou_count / group_count / user_count / computer_count / start_time / end_time / error_message / created_at / updated_at / deleted_at)
    * `sys_ad_service_accounts`(照 account_pool_test.go:26-46 原样)
    ⚠️ **记忆教训(XingRan sqlite 缺表 family pattern)**:任何被测函数触达的表都必须建;跑测试时若出现 `no such table: X`,先补建表再继续,禁改生产 SQL。
  - `entry78(dn string, kv map[string][]string) *ldap.Entry`(见 interfaces)+ `newSyncSvc78(t) (*SyncService, *gorm.DB)` 返回 `&SyncService{db: db, pool: NewAccountPool(db, nil)}` + `insertConfig78(t, db) *models.ADConfig`(插一条 status=models.ADConfigStatusEnabled 的配置行并返回结构体)。

  测试函数前缀 TestSync78_:
  - `TestSync78_ExtractDNs_And_SafeAttr`:extractDNs 空切片 / 多条;safeAttr 的属性存在 / 缺失 / 多值(签名以源码为准)。
  - `TestSync78_CategorizeOUs_CreateVsUpdate`:预置 1 条已存在 OU(DN 匹配)+ 2 条新 entry → 断言返回的 create 切片长度 1、update map 含既有 DN;断言 create 项的 ou_name/parent_dn/description 从 entry 属性正确提取(属性缺失时的默认值也断言)。
  - `TestSync78_SyncOUs_FullChain`:空 entries → 早退 nil(:181);3 条 entry(1 条已存在)→ 调用后查库断言:新 OU 插入 2 条、既有 OU 的 ou_name/description/last_sync_at 被更新、总行数 3。
  - `TestSync78_BatchCreateOUs_Empty_And_Batching`:空切片早退;造 > 单批阈值条数(读 :268-278 确认批大小,若为 100 则造 101+)→ 断言全部落库(多批循环分支)。
  - `TestSync78_BatchUpdateOUs_Empty_And_Multi`:空 map 早退;多条更新 → 逐行断言字段变更。
  - `TestSync78_GetExistingOUs`:DN 列表命中/部分命中/空列表三形态(注意 `deleted_at IS NULL` 过滤:预置一条软删行断言被排除)。

  纪律:禁 t.Parallel()(共享 :memory: 语义与包级状态);每个用例独立 setupSync78DB。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestSync78_" ./internal/services/addomain/ &amp;&amp; go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - 文件存在且含 `func setupSync78DB` 与 `func entry78`
  - `grep -o 'CREATE TABLE' internal/services/addomain/sync_78_05_test.go | wc -l` ≥ 7
  - `grep -c "^func TestSync78_" internal/services/addomain/sync_78_05_test.go` ≥ 6
  - 断言中无裸 `SyncStatus: 0` / `status = 1` 之类字面量(状态一律 models.* 常量)
  - `go test -count=1 -run "TestSync78_" ./internal/services/addomain/` exit 0
  </acceptance_criteria>
  <done>7 表 fixture + entry helper 就位(78-06/78-07 复用);OU 管道 7 个函数全链覆盖。</done>
</task>

<task type="auto">
  <name>Task 2: Group 管道(syncGroups / createGroupsInBatches / updateGroupsInBatches / parseGroupTypeFromLDAP)</name>
  <files>internal/services/addomain/sync_78_05_test.go</files>
  <read_first>
  - internal/services/addomain/sync.go — :313-433(syncGroups / createGroupsInBatches / updateGroupsInBatches)+ :701 parseGroupTypeFromLDAP
  - internal/services/addomain/group_sync_threshold_test.go — 既有 handleDeletedGroups 相关用例(避免语义重复)
  - internal/models/ — ADGroup 的 group_scope / group_type / member_count 字段语义与常量
  </read_first>
  <action>
  同文件追加:

  - `TestSync78_ParseGroupTypeFromLDAP_Table`:表驱动覆盖 groupType 位掩码全枚举(全局/域本地/通用 × 安全组/通讯组 + 未知值 + 空值 + 非数字字符串),断言返回的 models 常量。这是纯函数,零 DB。
  - `TestSync78_SyncGroups_CreateAndUpdate`:空 entries 早退;3 条 group entry(1 条 DN 已存在于 sys_ad_group)→ 断言新建 2 条、既有 1 条被更新(group_name / description / member_count / ou_dn / group_scope / group_type / last_sync_at 逐字段);member_count 从 entry 的 member 多值属性长度推导时,造一条 member 有 3 个值的 entry 断言 count=3。
  - `TestSync78_SyncGroups_MemberSync`:group entry 带 member 属性 → 断言 syncGroupMembers 被联动(sys_ad_group_member 行落库;若 syncGroups 内不联动而是由上层调用,则本用例改为断言"syncGroups 只写 group 表",并把 member 断言留给 Task 4,按源码实际调用链为准)。
  - `TestSync78_CreateGroupsInBatches_Empty_And_Batching`:空切片早退;超单批阈值条数 → 全部落库;**冲突分支**:预置同 (ad_config_id, group_dn) 的行后再 create → 断言 `clause.OnConflict` 行为(sync.go 顶部 import 了 `gorm.io/gorm/clause`,读 :383-402 确认 upsert 语义),断言不产生重复行。
  - `TestSync78_UpdateGroupsInBatches_Empty_And_Multi`:空 map 早退;多条更新逐行断言。
  - `TestSync78_SyncGroups_DBError`:DROP TABLE sys_ad_group 后调用 → 断言错误被包装返回(而非 panic),用于覆盖各 DB 错误早退分支。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestSync78_" ./internal/services/addomain/ &amp;&amp; go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestSync78_" internal/services/addomain/sync_78_05_test.go` ≥ 12(Task 1+2 合计)
  - parseGroupTypeFromLDAP 表驱动用例含 ≥6 条数据行(全枚举 + 未知 + 空)
  - 存在超单批阈值的 batching 用例(create 与 update 各一)
  - `go test -count=1 -run "TestSync78_" ./internal/services/addomain/` exit 0
  </acceptance_criteria>
  <done>Group 管道 4 个函数 + 位掩码解析全覆盖;批处理与冲突 upsert 分支有断言。</done>
</task>

<task type="auto">
  <name>Task 3: syncUsers 分支矩阵(182 stmts,达标主力)</name>
  <files>internal/services/addomain/sync_78_05_test.go</files>
  <read_first>
  - internal/services/addomain/sync.go — :434-615 syncUsers 全文,**逐分支列清单**(新建 / 更新 / 软删恢复 / $DUPLICATE- 过滤 / `$` 结尾计算机账号过滤 / manager 关联 / 时间属性解析 / OU 归属 / 批处理 / 各 DB 错误早退)
  - internal/services/addomain/manager_sync_test.go — 既有 manager 关联相关用例(避免重复,补未覆盖部分)
  - internal/services/addomain/sync_78_05_test.go — Task 1 的 fixture 与 entry helper
  - internal/models/ — ADUser 全字段与 is_enabled 的 userAccountControl 位推导语义
  </read_first>
  <action>
  同文件追加(本任务是 sync.go ≥80% 的达标主力,用例数最多):

  - `TestSync78_SyncUsers_Empty`:空 entries 早退 nil。
  - `TestSync78_SyncUsers_CreateNew`:3 条完整 user entry(sAMAccountName / displayName / mail / telephoneNumber / mobile / title / department / description / distinguishedName / userAccountControl / manager / whenCreated / lastLogon)→ 断言 3 行落库且**逐字段**与 entry 属性对应;`is_enabled` 从 userAccountControl 位推导(启用值 512 与禁用值 514 各造一条,断言 true/false)。
  - `TestSync78_SyncUsers_UpdateExisting`:预置 1 行(旧 display_name)+ 同 DN entry(新 display_name)→ 断言被更新而非新增,行数仍 1,last_sync_at 被刷新。
  - `TestSync78_SyncUsers_RestoreSoftDeleted`:预置一行 `deleted_at` 非空 + 同 DN entry → 断言按源码语义处理(恢复 deleted_at=NULL 或新建);**按现行为断言**并在注释写明依据行号(D-78-10)。
  - `TestSync78_SyncUsers_FilterDuplicatePrefix`:entry 的 sAMAccountName 以 `$DUPLICATE-` 开头 → 断言被过滤(不落库)。
  - `TestSync78_SyncUsers_FilterComputerAccount`:sAMAccountName 以 `$` 结尾 → 断言被过滤(与 user.go GetList 的 `username NOT LIKE '%$'` 语义呼应)。
  - `TestSync78_SyncUsers_ManagerLink`:entry 带 manager DN 且该 manager 也在本批 entries 中 / 不在本批中 两形态 → 断言 manager_dn 落库与(若有)二次关联逻辑。
  - `TestSync78_SyncUsers_TimeAttrParse`:whenCreated / lastLogon 三形态 —— 合法 AD 时间戳(`20240101000000.0Z` 形态)/ Windows FILETIME 数字 / 非法字符串 → 断言解析成功值与解析失败时的零值兜底(不 panic、不整批失败)。
  - `TestSync78_SyncUsers_OUAssignment`:entry DN 位于多层 OU → 断言 ou_dn 提取正确(含无 OU 的 CN=xx,DC=... 形态)。
  - `TestSync78_SyncUsers_Batching`:造超单批阈值条数(读 :434-615 确认批大小)→ 断言全部落库(多批循环 + 末批余数分支)。
  - `TestSync78_SyncUsers_DBError`:DROP TABLE sys_ad_user 后调用 → 断言错误被包装返回。

  纪律:每条 entry 用 `entry78(dn, map[string][]string{...})` 显式构造,属性名严格照生产读取的名字(executor 从 :434-615 抄);断言禁裸 0/1。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestSync78_SyncUsers" ./internal/services/addomain/ &amp;&amp; go test -count=1 -coverprofile=/tmp/ad78_05a.out ./internal/services/addomain/ &amp;&amp; go tool cover -func=/tmp/ad78_05a.out | grep "sync.go:434"</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestSync78_SyncUsers" internal/services/addomain/sync_78_05_test.go` ≥ 11
  - `go tool cover -func=/tmp/ad78_05a.out | grep "sync.go:434"` → syncUsers ≥75%
  - 存在 userAccountControl 512/514 两形态的 is_enabled 断言;存在 `$DUPLICATE-` 与 `$` 结尾两个过滤断言
  - `go test -count=1 -run "TestSync78_SyncUsers" ./internal/services/addomain/` exit 0
  </acceptance_criteria>
  <done>syncUsers 分支矩阵(11 类)全覆盖,sync.go 最大单函数块 ≥75%。</done>
</task>

<task type="auto">
  <name>Task 4: syncGroupMembers + updateSyncLog 状态机 + SyncDataByID/SyncData 入口</name>
  <files>internal/services/addomain/sync_78_05_test.go</files>
  <read_first>
  - internal/services/addomain/sync.go — :616-651 syncGroupMembers + :652-700 updateSyncLog + :48-79(SyncDataByID / SyncData)
  - internal/services/addomain/sync_singleflight_test.go — 既有 4 个 singleflight 用例(**勿重复 singleflight 语义**)
  - internal/services/addomain/member_sync_service_test.go — 成员同步相关既有用例(避免重复)
  </read_first>
  <action>
  同文件追加:

  - `TestSync78_SyncGroupMembers_FullDiff`:预置 sys_ad_group_member 3 行(user A/B/C)+ 传入 memberDNs = [A, B, D] → 断言:C 被移除、D 被新增、A/B 保留,最终 3 行;空 memberDNs → 断言全部清空(或按源码语义早退,按现行为断言);DB 错误(DROP TABLE)→ 错误被包装。
  - `TestSync78_UpdateSyncLog_StatusMatrix`:预置一条 Running 日志 → 分别调 updateSyncLog 传 `models.ADSyncStatusSuccess`(errMsg="")与 `models.ADSyncStatusFailed`(errMsg="boom")→ 断言 sync_status / 四个 count 字段 / end_time 非零 / error_message 落库;传不存在的 logID → 断言不 panic(该函数无返回值,按"静默"语义断言库无变化)。
  - `TestSync78_SyncDataByID_ConfigNotFound`:空 sys_ad_config → 断言错误"AD配置不存在或未启用";预置一条 `status != models.ADConfigStatusEnabled` 的配置 → 同样报不存在(**status 过滤语义断言**,引用 models 常量)。
  - `TestSync78_SyncDataByID_Enabled_DelegatesToSyncData`:预置启用配置 + **空账号池** → 断言最终错误为账号池文案(证明委派到 SyncData → syncDataInternal 链路,同时覆盖 :48-56 的 happy 查询分支)。
  - `TestSync78_SyncData_ErrorAndNilPassthrough`:直调 SyncData(空账号池)→ 断言 error 透传且 result 为 nil(:70-79 的 err / v==nil 两分支)。singleflight 的合并语义**不重复测**(已有 sync_singleflight_test.go)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestSync78_" ./internal/services/addomain/ &amp;&amp; go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestSync78_" internal/services/addomain/sync_78_05_test.go` ≥ 28(Task 1-4 合计)
  - syncGroupMembers 用例含"增/删/保留"三态同批断言
  - updateSyncLog 用例同时断言 Success 与 Failed 两状态 + 四个 count 字段
  - 测试文件不含对 singleflight 合并次数的断言(不与 sync_singleflight_test.go 重复)
  - `go test -count=1 -run "TestSync78_" ./internal/services/addomain/` exit 0
  </acceptance_criteria>
  <done>成员差异同步 + 日志状态机 + 两个公开入口分支覆盖。</done>
</task>

<task type="auto">
  <name>Task 5: syncDataInternal 失败路径(空池 / dial 全失败 / 日志创建失败)+ sync.go ≥80% checkpoint</name>
  <files>internal/services/addomain/sync_78_05_test.go</files>
  <read_first>
  - internal/services/addomain/sync.go — :80-135(syncLog 创建 + FailoverClient + 两个失败分支)
  - internal/services/addomain/failover_client.go — :44-96 ExecuteWithFailover(ListAvailable 空 → ErrAllAccountsUnavailable;Connect 失败 → MarkFailure + 继续 → 最终"账号池 N 个账号均失败")
  - internal/services/addomain/account_pool_test.go — :26-49 建表 SQL + insertAccount(本任务造账号用)
  - internal/services/addomain/errors 定义处 — ErrAllAccountsUnavailable(grep 定位)
  </read_first>
  <action>
  同文件追加(**D-78-07 边界:只覆盖失败路径,不为 happy path 加生产 seam**):

  - `TestSync78_SyncDataInternal_EmptyAccountPool`:启用配置 + sys_ad_service_accounts 空 → 直调 `s.syncDataInternal(ctx, config, string(models.ADSyncTypeXxx))` → 断言:(a) 返回错误文案含"AD 账号池无可用账号"且 `errors.Is(err, ErrAllAccountsUnavailable)` 为真;(b) **sys_ad_sync_log 里那条日志已被置为 `models.ADSyncStatusFailed` 且 error_message 非空**(证明 updateSyncLog(Failed) 分支被走到)。
  - `TestSync78_SyncDataInternal_AllAccountsDialFail`:insertAccount 造 2 个 status=可用 的账号 + config.ServerAddress=`127.0.0.1` / ServerPort = 一个**本地已关闭端口**(先 net.Listen 取端口再 Close)→ 断言:(a) 错误文案含"连接AD服务器失败"(非 ErrAllAccountsUnavailable 分支);(b) 日志置 Failed;(c) 两个账号的 failure_count 均被 MarkFailure 递增(查 sys_ad_service_accounts 断言,证明 FailoverClient 的上报语义)。**必须毫秒级超时**:若 LDAP dial 到关闭端口会长时间等待,用例整体加 10s 硬超时守卫并在注释记录实测耗时。
  - `TestSync78_SyncDataInternal_SyncLogCreateFail`:DROP TABLE sys_ad_sync_log → 断言错误"创建同步日志失败"且**未**发起任何账号池查询(可观察口径:账号表无 failure_count 变化)。
  - `TestSync78_SyncDataInternal_HappyPathBoundary`(**文档化不可覆盖边界**):写一个只含注释与 `t.Skip` 之外方式的说明性用例是不允许的 —— 改为在文件尾部加一段块注释,明确写明:
    ```
    // D-78-07 覆盖边界:syncDataInternal 的 happy path(第 3-7 步:syncOUs→syncGroups→syncUsers→
    // syncComputers→last_sync_at→updateSyncLog(Success))需要 FailoverClient 的闭包成功返回,
    // 而 fc 在 :99 局部构造、clientFactory 不可注入。本 phase 决定不加生产 seam:
    // 四条管道已由 TestSync78_SyncOUs/_SyncGroups/_SyncUsers 与 78-06 的 syncComputers 单独直测,
    // 编排层剩余 ~25 stmts 接受不覆盖。若 78-07 的 in-process LDAP 应答器落地,可作为可选回补。
    ```
    并把同一结论写进 SUMMARY 的"已知不覆盖"小节 + 给出实测剩余 stmts 数。

  **收尾 checkpoint**:
  ```
  go test -count=1 -coverprofile=cov78_05.out ./internal/services/addomain/
  go tool cover -func=cov78_05.out | grep "sync.go"
  go test -race -count=1 ./internal/services/addomain/
  ```
  sync.go 逐函数数字 + 包 total 落 SUMMARY(基线包 23.1%;包级 ≥70% 由 78-06/78-07 收口)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestSync78_" -timeout 180s ./internal/services/addomain/ &amp;&amp; go test -race -count=1 ./internal/services/addomain/ &amp;&amp; go test -count=1 -coverprofile=cov78_05.out ./internal/services/addomain/ &amp;&amp; go tool cover -func=cov78_05.out | grep "sync.go" &amp;&amp; go build ./... &amp;&amp; go test ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestSync78_" internal/services/addomain/sync_78_05_test.go` ≥ 31(Task 1-5 合计)
  - 存在 `errors.Is(err, ErrAllAccountsUnavailable)` 断言;存在 failure_count 被 MarkFailure 递增的断言
  - `go tool cover -func=cov78_05.out | grep "sync.go"` 加权 **≥80%**
  - 文件尾部含 D-78-07 覆盖边界块注释;SUMMARY 有"已知不覆盖"小节 + 剩余 stmts 数
  - dial 失败用例有硬超时守卫且实测耗时 <10s;网络目标仅 127.0.0.1
  - `go test -race ./internal/services/addomain/` exit 0;`go test ./...` exit 0;`git diff --stat` 生产 .go 改动 = 0 文件
  </acceptance_criteria>
  <done>sync.go ≥80%;syncDataInternal 三条失败路径覆盖;happy path 不可覆盖边界被文档化(D-78-07),零生产改动。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 ↔ AD/LDAP | 零 LDAP 网络:全部以 `[]*ldap.Entry` 字面量驱动;唯一的网络接触是 dial-fail 用例指向 `127.0.0.1` 的已关闭端口 |
| 测试 ↔ 数据库 | glebarez sqlite `:memory:` + 手动 CREATE TABLE,禁生产 PG |
| 测试 ↔ 凭据 | 账号池 password_ciphertext 只用 `"encrypted_pwd"` 等字面量(account_pool_test.go 先例),禁真实密文/域账号 |
| 测试 ↔ 生产代码 | 仅新增 *_test.go;**D-78-07 明令不加 sync.go 生产 seam**;其他改动仅 D-78-10 quirk 修复豁免 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-05-01 | Spoofing | 测试误连真实 AD 域控(域名/IP/管理员凭据入仓) | mitigate | config 全 dummy 值;server_address 仅 `127.0.0.1`;验收 grep 无真实域名/DC IP/账号 |
| T-78-05-02 | DoS | LDAP dial 到关闭端口的等待时长不可控 → CI 拖慢 | mitigate | dial-fail 用例加 10s 硬超时守卫 + SUMMARY 记录实测耗时;端口取自本地 Listen 后立即 Close |
| T-78-05-03 | Tampering | 为覆盖 happy path 给 sync.go 加生产 seam(扩大生产改动面) | mitigate | D-78-07 明令禁止 + 覆盖边界块注释 + 验收 `git diff --stat` 生产改动 = 0 |
| T-78-05-04 | Tampering | sqlite 缺表导致改生产 SQL(仓内已重复 5 次的 family pattern) | mitigate | 缺表一律补 CREATE TABLE;禁改生产查询;记忆教训写入 Task 1 action |
| T-78-05-05 | Information Disclosure | 用真实员工 DN / 邮箱 / 手机号造 entry | mitigate | 全部 dummy(`CN=Test User1,OU=Test,DC=example,DC=com` / `u1@example.com`),验收 grep 无真实域后缀 |
| T-78-05-SC | Tampering | 依赖安装 | accept | 零新增依赖(go-ldap / glebarez sqlite / testify 均已在 go.mod) |
</threat_model>

<verification>
- `go test -count=1 -timeout 180s ./internal/services/addomain/` 全绿;`go test -race -count=1 ./internal/services/addomain/` 全绿
- `go tool cover -func=cov78_05.out | grep "sync.go"` 加权 ≥80%,逐函数数字落 SUMMARY
- SUMMARY 含"已知不覆盖"小节(syncDataInternal happy path ~25 stmts + 剩余实测数)
- 测试文件:≥7 张 CREATE TABLE、无裸状态字面量、无真实域名/IP/凭据、无 t.Parallel
- `go build ./...` + `go test ./...` exit 0;生产 .go 改动 = 0 文件
</verification>

<success_criteria>
- sync_78_05_test.go 存在,TestSync78_ ≥31 全绿
- sync.go 从 0%(18 函数全 0)推到 ≥80%,syncUsers ≥75%
- 7 表 sqlite fixture + entry78 helper 沉淀,78-06/78-07 直接复用
- 零生产代码改动(D-78-07 定案),BLOCK-05 第一段完成
</success_criteria>

<decision_audit>
| ID | 决策 | 依据 / 理由 |
|----|------|------------|
| **D-78-07** | **拒绝为 sync.go 加生产 seam(既不加包级 var 工厂,也不加 `SyncService.clientFactory` 字段);syncDataInternal 只覆盖失败路径,happy path ~25 stmts 接受不覆盖并文档化** | 78-RESEARCH §3 把 sync.go 的技法写为"sqlite + 模拟 `[]*ldap.Entry` 数据驱动 + FailoverClient.clientFactory 注入 mockLDAPClient",但 planner 读 sync.go:99 发现 `fc := NewFailoverClient(s.pool, config)` 是**局部构造**——`clientFactory` 字段虽存在(failover_client.go:25),却无法从 SyncService 外部注入。两个候选:(1) 加生产 seam;(2) 只测私有方法 + 失败路径。选 (2) 的量化理由:sync.go 316 stmts 中 ~240 落在 entry-driven 私有方法(同包可直调)、~30 落在 SyncDataByID/SyncData/updateSyncLog、~20 落在 syncDataInternal 失败路径 → 合计 ~290 ⇒ ≥80% 目标在零生产改动下可达。Phase 77 已确立"生产 .go 改动 = 0 文件"为测试 plan 的默认纪律,不值得为最后 25 stmts 破例 |
| D-78-05a | fixture 用手动 `CREATE TABLE` 而非 `AutoMigrate` | 仓内 addomain 全部既有测试的一致做法,且 dept_ou_mapper_test.go:18 明确注释理由(避免 AutoMigrate 外键问题);另参考记忆教训(sqlite view 依赖 gotcha / 缺表 family pattern),AutoMigrate 在 sqlite 下有 __temp+RENAME 与视图依赖风险 |
| D-78-05b | 不重复 `sync_singleflight_test.go` 的 singleflight 语义断言 | 该文件已有 4 个用例覆盖合并/独立 key/shared 标志/结果复用;重复断言无覆盖增益且增加维护面 |
| D-78-05c | 软删恢复、空 memberDNs、不存在 logID 等语义**按现行为断言** + 注释标注依据行号 | D-78-10 的"无据不改"分支:这些语义在源码中无注释/文档明示预期,不臆断为 bug |
| D-78-08 / D-78-10 / D-78-11 | 命名 / quirk 纪律 / wave 编排 | 同 78-01 |
</decision_audit>

<output>
Create `.planning/workstreams/milestone/phases/78-block-bp-unlock-by-foundation/78-05-SUMMARY.md` when done
</output>
