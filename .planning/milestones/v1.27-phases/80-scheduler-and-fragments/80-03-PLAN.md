---
phase: 80-scheduler-and-fragments
plan: 03
status: pending
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/v1/auth_80_03_test.go
  - internal/api/v1/captcha_80_03_test.go
  - internal/api/v1/api_v1_tail_80_03_test.go
  - internal/models/models_pure_80_03_test.go
  - internal/models/models_state_80_03_test.go
  - internal/models/models_hooks_80_03_test.go
autonomous: true
requirements: [TAIL-02]

must_haves:
  truths:
    - "internal/api/v1 包 ≥70%(578 stmts,38 covered,+367):mini-Core fixture 真装配 handler 并发请求(D-80-03)——既有 5 个测试文件的『占位 handler/形状验证』教训不得复现(SetupAuthRouter 必须真调、login 必须进 login() 体)"
    - "按 D-80-03 裁决:fixture 用真 CaptchaService(core.NewCaptchaService,具体类型无法 stub)+ 真 security.NewJWTManager + sqlite 表;否决为可测性把 CoreServices.CaptchaService 接口化(生产结构变更)"
    - "auth.go(241 stmts)≥70%:loginLocalDirect 成功/锁定/禁用/密码错 4 分支 + AuthFactory nil → fallback 分支(auth.go:157-162)+ logout/refreshToken/getAuthConfig/getPublicKey/testSM2/recordLoginLog/syncErrorReasonMessage/getNicknameOrUsername;parseUserAgent 已覆盖勿重写"
    - "internal/models 包 ≥70%(445 stmts,+311):85 个非 TableName 函数 —— Scan/Value 三态(round-trip + nil/坏输入)、vdi AES-128-GCM round-trip、状态机/DTO 纯表驱动、GORM 钩子 sqlite AutoMigrate 插入触发(open Q2:不依赖 nil-tx 假设,仅 base.go 两个已确认 nil-safe 钩子允许直调)"
    - "job_cron_util.go(67 stmts)全纯一次收满:validate/parse/describe/generate + CronExpressionBuilder 链式 + EveryXxx/Custom 常量"
    - "WS 真 httptest 握手(notice_hub_readpump_test.go:26-127 现成范式)+ CheckOrigin 分支表;零新增依赖(miniredis/sqlite/testify/gorilla 全既有)"
  artifacts:
    - path: "internal/api/v1/auth_80_03_test.go"
      provides: "mini-Core fixture newMiniCore8003 + 表 helper + auth.go handler 群测试(前缀 TestAuth8003_)"
      min_lines: 320
      contains: "newMiniCore8003"
    - path: "internal/api/v1/captcha_80_03_test.go"
      provides: "captcha_handler 4 handler + captcha_background_handler 9 handler 测试(前缀 TestCap8003_/TestCapBg8003_)"
      min_lines: 200
      contains: "sysCaptchaBackground"
    - path: "internal/api/v1/api_v1_tail_80_03_test.go"
      provides: "job_utils + ws_notice_handler 补分支(CheckOrigin 表 + 真 WS 握手)+ router 形状(前缀 TestJbu8003_/TestWs8003_)"
      min_lines: 160
      contains: "CheckOrigin"
    - path: "internal/models/models_pure_80_03_test.go"
      provides: "Scan/Value 三态 + vdi AES round-trip + 树/聚合 纯表驱动(前缀 TestMdl8003_)"
      min_lines: 200
      contains: "encryptVDIPassword"
    - path: "internal/models/models_state_80_03_test.go"
      provides: "ADServiceAccount/ADUser/ConfigBackup/CaptchaBackground 状态机与 DTO 表驱动(前缀 TestMst8003_)"
      min_lines: 180
      contains: "IsAvailable"
    - path: "internal/models/models_hooks_80_03_test.go"
      provides: "BaseModel/BaseTimeLine nil 直调 + WorkOrder 族等钩子 sqlite AutoMigrate 触发(前缀 TestMhk8003_)"
      min_lines: 160
      contains: "BeforeCreate"
  key_links:
    - from: "internal/api/v1/auth_80_03_test.go"
      to: "internal/core/core_infra.go:21 CoreInfra{Config,DB,Cache,JWTManager,PwdManager,CaptchaService}"
      via: "newMiniCore8003 组装 Core{CoreInfra,CoreServices} + coredb.Database{DB: sqlite, Type: \"sqlite\"}"
      pattern: "CoreInfra\\{|NewCaptchaService|NewJWTManager"
    - from: "internal/api/v1/captcha_80_03_test.go"
      to: "internal/core/captcha_78_01_test.go:41-60 sysCaptchaBackgroundDDL"
      via: "复制 78-01 DDL 建 captcha_background 表,9 handler 走 core.CaptchaBackgroundService.GetDB() sqlite 查询"
      pattern: "captcha_background|CREATE TABLE"
    - from: "internal/models/models_hooks_80_03_test.go"
      to: "internal/models/base.go:22 BaseModel.BeforeCreate"
      via: "nil-tx 直调(nil-safe 已确认)+ 其余钩子经 sqlite AutoMigrate + Create 触发"
      pattern: "AutoMigrate|BeforeCreate"
---

# Plan 3: 碎包 A——api/v1 handler 群 + models 全类别 (Phase: 80-scheduler-and-fragments)

<objective>
交付两个碎包:internal/api/v1(578 stmts @6.6%,gap +367)与 internal/models(445 stmts @0.2%,gap +311),合计 +678 = 全 phase 最大 slice。api/v1 走 mini-Core 真装配 handler 测试(D-80-03);models 走纯表驱动(Scan/Value/AES/状态机/DTO)+ sqlite AutoMigrate 钩子触发。两包双双 ≥70%。

Purpose: TAIL-02(SC-2)主体段。本 plan 建立的 mini-Core fixture(newMiniCore8003)是全 phase 的 keystone —— 80-04 的 SetupRouter 装配测试依赖它的同形副本;models 的"纯 struct 无逻辑可测"预设被 research §1.4 推翻(85 个非 TableName 函数,59 个 Scan/Value/钩子)。
Output: 新增 6 个测试文件(零生产代码改动,D-80 escape hatch 除外);mini-Core fixture 形状沉淀给 80-04。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-RESEARCH.md

<interfaces>
<!-- 关键契约(planner 已逐一读源码复核,2026-08-28)。executor 直接使用,无需重新探索。 -->

Fixture 原料(80-03 专属副本命名为 newMiniCore8003;80-04 另起 newMiniCore8004):
```go
// core.Core{CoreInfra, CoreServices}(core.go:104-107,字段提升保持 core.DB/core.Cache 语法)
// core.CoreInfra(core_infra.go:21-45):Config *config.Config; DB *db.Database; Cache cache.Cache;
//   JWTManager *security.JWTManager; PwdManager *security.PasswordManager; SM4Cipher;
//   Scheduler *scheduler.Scheduler; CaptchaService *CaptchaService; CaptchaBackgroundService *CaptchaBackgroundService;
//   MetricsCacheService; AuthFactory *security.AuthStrategyFactory
// coredb.Database(internal/core/db/database.go:29)导出字段:DB *gorm.DB; Type string   ← 测试手工构造合法
// core.NewCaptchaService(db *db.Database, cache cache.Cache)(internal/core/captcha.go:51,具体类型)
// security.NewJWTManager(&cfg.JWT)(SM2 keypair 由 manager 内生成);security.NewPasswordManager(nil)
// 零值 &config.Config{} → router/auth 走 RequestEncryption disabled 分支
```

From internal/api/v1(auth.go 663 行/241 stmts):
```go
parseUserAgent(ua string) — 已覆盖 82.4%,勿重写
login / logout / refreshToken / loginLocalDirect / getAuthConfig / getPublicKey / testSM2
recordLoginLog / syncErrorReasonMessage / getNicknameOrUsername
// AuthFactory nil → fallback loginLocalDirect(auth.go:157-162)
// loginLocalDirect 走 core.DB sqlite + CaptchaService 具体类型(auth.go:272-331)
```

From internal/api/v1(job_cron_util.go — 全纯):
```go
validateCronExpression(expr) bool                     // :13
calculateNextRunTime(expr) time.Time                  // :19
GetCronDescription(expr) string                       // :34
ParseCronExpression(expr) (6 字段, error)             // :58
ValidateCronExpression(expr) (valid, nextRunTime, description, err) // :87
generateCronExpression(6 字段) string                 // :97
NewCronExpressionBuilder() + SetSeconds/SetMinutes/SetHours/SetDay/SetMonth/SetDayOfWeek/Build  // :112-160
EveryMinute/EveryHour/EveryDay/EveryWeek/EveryMonth   // :165-185
Custom(seconds, minutes, hours, day, month, dayOfWeek) string // :190
```

From internal/api/v1(其余):
```go
job_utils.go(30 stmts):FormatDuration(纯)+ GetJobStatistics(sqlite sys_job/sys_job_log)
captcha_handler.go(35 stmts):getCaptcha/verifySlider/getConfig/reload → mini-Core + CaptchaService
captcha_background_handler.go(149 stmts):9 handler → core.CaptchaBackgroundService.GetDB() sqlite(:33-88 起)
ws_notice_handler.go(53 stmts):containsOrigin/newWebSocketUpgrader(已 45.3%)+ CheckOrigin 分支表(*、同源、localhost、白名单、拒绝)
monitor_router.go + router.go(3 stmts):装配形状
```

models 测试类别映射(research §1.4):
```go
// driver Valuer/Scanner:StringArray(captcha_background.go:113-155)/ DeviceIDList(config_execution.go:41-58)
//   TemplateVariable(s)(config_template.go:29-60)/ DataSourceConfig+DisplayConfig+LayoutConfig(dashboard.go:85-270)
// GORM 钩子:BaseModel.BeforeCreate(base.go:22)/ BaseTimeLine.BeforeCreate(base.go:37)—— 不触碰 tx,nil 直调安全(已确认)
//   WorkOrder 族 6 个 BeforeCreate(workorder.go)/ ADSyncLog/ConfigBackup BeforeCreate+BeforeUpdate/APIKeyUsageLog/Dashboard —— sqlite AutoMigrate 插入触发
// 状态机/断言:ADServiceAccount IsAvailable/IsCircuitBroken/IsDisabled/StatusText(ad_service_account.go:54-82)
//   ADUser Is*ByUAC 三连(ad_domain.go:250-264)/ ConfigBackup IsStoredIn*(captcha_background.go IsEnabled/GetFileURL/GetAllowedShapesList/ToDTO/ParseStringArray)
// 加密对:vdi.go encryptVDIPassword/decryptVDIPassword(:106/:130,AES-128-GCM 硬编码 key,纯 round-trip)
// 树/聚合:ADOU.Children(ad_domain.go:68)/ ADUser.GetGroupDNs(ad_domain.go:144)
```

既有 analog(必读;新 helper 一律 8003 后缀):
- internal/core/captcha_78_01_test.go:37-140 — mini-Core 装配范式 + sysCaptchaBackgroundDDL(:41-60,直接复制到 80-03 fixture)
- internal/api/v1/auth_integration_test.go:458-505 — 占位 handler 教训(新测试必须真调 SetupAuthRouter/真 handler)
- internal/websocket/notice_hub_readpump_test.go:26-127 — WS httptest harness(Upgrader + DefaultDialer.Dial)
- internal/api/v1/ws_notice_handler_test.go / auth_test.go — 既有测试(勿重写,只补未覆盖分支)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: mini-Core fixture(keystone)+ loginLocalDirect 4 分支</name>
  <files>internal/api/v1/auth_80_03_test.go</files>
  <read_first>
  - internal/api/v1/auth.go — :61-331(login 入口/AuthFactory nil fallback/loginLocalDirect 全分支)
  - internal/core/captcha_78_01_test.go:37-140 — 装配范式(复制手法,不 import)
  - internal/core/core_infra.go:21-45 / internal/core/db/database.go:29-40 — 字段名照抄
  - internal/api/v1/auth_test.go — 既有形状测试(勿重写)
  </read_first>
  <action>
  创建 internal/api/v1/auth_80_03_test.go(package v1)。fixture(80-03 与 80-04 各持一份同形副本,D-80-03;字段名以 core_infra.go/core_services.go 现场为准):

  - `newMiniCore8003(t *testing.T) (*core.Core, *gorm.DB)` — gorm.Open(glebarez sqlite, filepath.Join(t.TempDir(),"p8003.db"))+ t.Cleanup 关连接;`coredb.Database{DB: gormDB, Type: "sqlite"}`;cache.NewMemoryCache(...);&config.Config{}(零值 → 加密 disabled);security.NewJWTManager(&cfg.JWT);security.NewPasswordManager(nil);core.NewCaptchaService(coreDB, mem);组装 core.Core{CoreInfra: infra, CoreServices: &core.CoreServices{}}。
  - `seedUser8003(t, db, username, password, status)` helper — 建表(AutoMigrate models.User/models.UserRole 等按 loginLocalDirect 实际引用裁剪,78-01 精简哲学:只建 handler 引用的列)+ 种子行;密码经 security.PasswordManager 哈希(不走明文)。

  测试前缀 TestAuth8003_(gin test context / httptest.Perform 模式):
  - `TestAuth8003_LoginLocalDirect_Success`:正确凭证 → 成功返回 token 对/用户信息(断言以 :272-331 实际响应结构为准)。
  - `TestAuth8003_LoginLocalDirect_Locked`:锁定状态用户(状态值引用 models 常量)→ 锁定错误分支。
  - `TestAuth8003_LoginLocalDirect_Disabled`:禁用用户 → 禁用分支。
  - `TestAuth8003_LoginLocalDirect_WrongPassword`:错误密码 → 密码错分支 + 失败计数/记录行为(recordLoginLog 落 sys_oper_log 或 login log 表,按实现断言)。
  - `TestAuth8003_AuthFactoryNil_Fallback`:core.AuthFactory 为 nil → 走 :157-162 fallback loginLocalDirect 分支(与 Success 用例同断言面,证明 fallback 生效)。

  loginLocalDirect 若需 captcha 校验分支:用 CaptchaService 真实路径(CaptchaService 构造已在 fixture),按 :272-331 的实际前置条件决定是否需要种子 captcha 行 —— 以实现为准,不猜。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestAuth8003_" ./internal/api/v1/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestAuth8003_" internal/api/v1/auth_80_03_test.go` ≥ 5
  - `grep -c "newMiniCore8003" internal/api/v1/auth_80_03_test.go` ≥ 5
  - loginLocalDirect 4 分支(成功/锁定/禁用/密码错)+ AuthFactory nil fallback 各有用例
  - `go test -count=1 -run "TestAuth8003_" ./internal/api/v1/` exit 0
  </acceptance_criteria>
  <commit>test(80-03): mini-Core fixture + loginLocalDirect 四分支(D-80-03 真装配)</commit>
  <done>keystone fixture 落地供 80-04 同形复制;loginLocalDirect 主路径 4 分支 + fallback 全覆盖。</done>
</task>

<task type="auto">
  <name>Task 2: auth.go 其余 handler 群(logout/refreshToken/getAuthConfig/getPublicKey/testSM2 等)</name>
  <files>internal/api/v1/auth_80_03_test.go</files>
  <read_first>
  - internal/api/v1/auth.go — :331-663(logout/refreshToken/getAuthConfig/getPublicKey/testSM2/recordLoginLog/syncErrorReasonMessage/getNicknameOrUsername 逐函数体)
  </read_first>
  <action>
  同文件追加 TestAuth8003_(每 handler 至少一正一负):

  - `TestAuth8003_GetAuthConfig`:零值 Config → 加密 disabled 配置响应(断言以实现字段为准)。
  - `TestAuth8003_GetPublicKey`:JWTManager(SM2 keypair 内生成)→ 返回非空公钥 PEM/JWKS 结构(以实现为准)。
  - `TestAuth8003_TestSM2`:构造合法 SM2 载荷 → testSM2 往返;坏载荷 → 错误分支。
  - `TestAuth8003_Logout`:签发真实 token(fixture JWTManager)→ logout → 黑名单/登出行为(断言以实现为准;TokenBlacklistService 为 nil 时按 nil-safe 路径断言并在注释记录)。
  - `TestAuth8003_RefreshToken`:合法 refresh token → 新 access;非法/过期 → 错误分支。
  - `TestAuth8003_SyncErrorReasonMessage`:表驱动(命中已知错误码映射/未知码回退)。
  - `TestAuth8003_GetNicknameOrUsername`:sqlite 种子用户 → 命中昵称/回退 username/无用户三态。
  - `TestAuth8003_RecordLoginLog`:sqlite 建日志表 → 调用后行落库(状态引用 models.LoginLogStatus* 常量,禁裸 0/1)。

  纪律:请求一律经 httptest/gin test context 发出(不直调私有函数绕过绑定);handler 内引用的服务字段缺失时按实现的 nil 分支记录,不为可测性改生产结构。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestAuth8003_" ./internal/api/v1/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestAuth8003_" internal/api/v1/auth_80_03_test.go` ≥ 12
  - auth.go 每个公开 handler 函数至少被一个用例触达
  - `go test -count=1 -run "TestAuth8003_" ./internal/api/v1/` exit 0
  </acceptance_criteria>
  <commit>test(80-03): auth handler 群真装配测试(公钥/SM2/刷新/登出/日志)</commit>
  <done>auth.go 5.8% → ≥70%;占位 handler 教训纠正(全部真 handler 真请求)。</done>
</task>

<task type="auto">
  <name>Task 3: captcha 家族——captcha_handler 4 handler + captcha_background_handler 9 handler</name>
  <files>internal/api/v1/captcha_80_03_test.go</files>
  <read_first>
  - internal/api/v1/captcha_handler.go — 全文(147 行)
  - internal/api/v1/captcha_background_handler.go — 全文(337 行,9 handler,:33-88 起 sqlite 查询)
  - internal/core/captcha_78_01_test.go:41-60 — sysCaptchaBackgroundDDL(复制)
  </read_first>
  <action>
  创建 internal/api/v1/captcha_80_03_test.go。fixture 复用 auth_80_03_test.go 的 newMiniCore8003(同包测试共享文件级 helper;CoreServices.CaptchaBackgroundService 字段按 core_services.go 现场构造 —— construction 形态以实际定义为准,Open Q3)。

  TestCap8003_ 前缀(captcha_handler):
  - `TestCap8003_GetCaptcha`:生成验证码(响应结构断言)+ `TestCap8003_GetConfig`(配置响应)+ `TestCap8003_Reload`(重载不 panic)。
  - `TestCap8003_VerifySlider`:正确滑块 → 通过;错误位移 → 拒绝(以 CaptchaService 真实算法为准,经 handler 发请求)。

  TestCapBg8003_ 前缀(captcha_background_handler,sqlite 表 = 78-01 DDL 复制):
  - `TestCapBg8003_List_Paged`:种子 3 行 → list 分页(list/total 断言,分页参数约定 current/pageSize)。
  - `TestCapBg8003_Get_Create_Update_Delete`:CRUD 四请求各一正一负(不存在 ID 的 get/update/delete 负分支)。
  - `TestCapBg8003_Upload_Validate`:上传/校验 handler 按实现的参数校验分支(合法/非法文件或参数;文件类 fixture 落 t.TempDir,禁写仓库目录)。
  - `TestCapBg8003_Toggle_Status`:启停/状态类 handler(若有)→ 状态值引用 models.CaptchaBackground 常量。

  9 个 handler 以 :33-88 起的实际函数名清单为准逐一触达(允许合并用例,但每 handler 至少一条请求路径)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestCap8003_|TestCapBg8003_" ./internal/api/v1/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestCap8003_\|^func TestCapBg8003_" internal/api/v1/captcha_80_03_test.go` ≥ 8
  - captcha_background_handler.go 的 9 个 handler 全部被触达(对照源函数清单)
  - `go test -count=1 -run "TestCap8003_|TestCapBg8003_" ./internal/api/v1/` exit 0
  </acceptance_criteria>
  <commit>test(80-03): captcha 家族 handler 真装配测试(13 handler + 78-01 DDL)</commit>
  <done>captcha_handler 0% → ≥70%;captcha_background_handler 0% → ≥70%。</done>
</task>

<task type="auto">
  <name>Task 4: models 纯数据类——Scan/Value 三态 + vdi AES round-trip + 树/聚合</name>
  <files>internal/models/models_pure_80_03_test.go</files>
  <read_first>
  - internal/models/captcha_background.go:113-155(StringArray)/ config_execution.go:41-58(DeviceIDList)/ config_template.go:29-60(TemplateVariable(s))/ dashboard.go:85-270(DataSourceConfig/DisplayConfig/LayoutConfig)
  - internal/models/vdi.go:106-140(encryptVDIPassword/decryptVDIPassword)
  - internal/models/ad_domain.go:68(ADOU.Children)/ :144(ADUser.GetGroupDNs)
  </read_first>
  <action>
  创建 internal/models/models_pure_80_03_test.go(package models,同包)。前缀 TestMdl8003_:

  - `TestMdl8003_ScanValue_RoundTrip_Table`:全部 driver Valuer/Scanner 类型(StringArray/DeviceIDList/TemplateVariable/TemplateVariables/DataSourceConfig/DisplayConfig/LayoutConfig)表驱动 —— Value() 合法值 → Scan() 回读 → 断言相等;每组另测 nil 输入 / 空串 / 非法 []byte(坏 JSON/坏格式)三分支(research R4:纯直调,不走真实 PG 数组路径;driver.Value 形状按 []byte/string/nil 三态断言)。
  - `TestMdl8003_VDIPassword_AES_RoundTrip`:encryptVDIPassword → decryptVDIPassword 还原(含中文/特殊字符/空串边界);密文 ≠ 明文;对错误密文 decrypt → 错误或空值(按 :130 实现断言)。硬编码 key 是已知 quirk —— 注释锁定,不扩修。
  - `TestMdl8003_ADOU_Children`:父子/空 children/多层 三态。
  - `TestMdl8003_ADUser_GetGroupDNs`:多 DN/空/去重行为(按 :144 实现)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestMdl8003_" ./internal/models/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestMdl8003_" internal/models/models_pure_80_03_test.go` ≥ 4
  - 7 个 Valuer/Scanner 类型各有 round-trip + nil + 坏输入分支
  - `go test -count=1 -run "TestMdl8003_" ./internal/models/` exit 0
  </acceptance_criteria>
  <commit>test(80-03): models 纯数据类表驱动(Scan/Value 三态 + AES 往返 + 树聚合)</commit>
  <done>models driver Valuer/Scanner 全类别 + vdi 加密对 + ADOU/ADUser 聚合全覆盖。</done>
</task>

<task type="auto">
  <name>Task 5: models 状态机/DTO——ADServiceAccount/ADUser/ConfigBackup/CaptchaBackground</name>
  <files>internal/models/models_state_80_03_test.go</files>
  <read_first>
  - internal/models/ad_service_account.go:54-82(IsAvailable/IsCircuitBroken/IsDisabled/StatusText)
  - internal/models/ad_domain.go:250-264(ADUser Is*ByUAC 三连)
  - internal/models/config_backup.go(IsStoredIn* 等)
  - internal/models/captcha_background.go(IsEnabled/GetFileURL/GetAllowedShapesList/ToDTO/ParseStringArray)
  </read_first>
  <action>
  创建 internal/models/models_state_80_03_test.go。前缀 TestMst8003_(全部纯表驱动,状态值一律引用 models 常量,禁裸 0/1):

  - `TestMst8003_ADServiceAccount_StateMachine`:状态三态 × IsAvailable/IsCircuitBroken/IsDisabled 真值表 + StatusText 每状态映射(含未知状态回退分支)。
  - `TestMst8003_ADUser_UAC`:Is*ByUAC 三连 —— UAC 位开启/关闭/多标志组合表驱动(以 :250-264 实现的位运算为准)。
  - `TestMst8003_ConfigBackup_StoredIn`:IsStoredIn* 各存储位判断真值表 + 组合态。
  - `TestMst8003_CaptchaBackground_Methods`:IsEnabled(启用/停用常量两态)/ GetFileURL(空/非空)/ GetAllowedShapesList(列表内容断言)/ ToDTO(字段映射完整断言)/ ParseStringArray(合法/nil/坏输入)。
  - `TestMst8003_ToDTO_FieldMapping`:包内其余 ToDTO/DTO 转换函数(执行 grep "^func .*ToDTO" 盘点)逐个字段映射断言 —— 缺字段映射是常见回归点。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestMst8003_" ./internal/models/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestMst8003_" internal/models/models_state_80_03_test.go` ≥ 5
  - 状态断言零裸 0/1 字面量(grep 断言行无 `== 0`/`== 1` 硬编码 status,常量引用为主)
  - `go test -count=1 -run "TestMst8003_" ./internal/models/` exit 0
  </acceptance_criteria>
  <commit>test(80-03): models 状态机/DTO 表驱动(AD 三族 + ConfigBackup + CaptchaBackground)</commit>
  <done>models 状态机/断言/DTO 转换类别全覆盖,常量引用纪律落地。</done>
</task>

<task type="auto">
  <name>Task 6: models GORM 钩子——nil 直调(base.go)+ sqlite AutoMigrate 触发(其余)+ TableName 批量</name>
  <files>internal/models/models_hooks_80_03_test.go</files>
  <read_first>
  - internal/models/base.go:22-40(BaseModel/BaseTimeLine BeforeCreate —— 不触碰 tx,nil 直调安全已确认)
  - internal/models/workorder.go:110-365(WorkOrder 族 6 个 BeforeCreate)/ ad 域日志与 config_backup / api_key_usage_log / dashboard 的钩子体(逐个确认是否引用 tx)
  </read_first>
  <action>
  创建 internal/models/models_hooks_80_03_test.go。前缀 TestMhk8003_(open Q2 口径:不依赖未确认的 nil-tx 假设):

  - `TestMhk8003_BaseHooks_NilTxDirect`:BaseModel/BaseTimeLine 的 BeforeCreate(nil)直调 —— ID 为空 → 被填成合法 UUID;ID 已有值 → 保持不变(两态)。
  - `TestMhk8003_Hooks_ViaSqlite`:sqlite(t.TempDir)+ AutoMigrate(WorkOrder 族及含钩子的 model 全清单)→ Create 触发 BeforeCreate / Update 触发 BeforeUpdate —— 断言钩子产生的字段效果(编号/时间戳/默认值,按各钩子体实际行为);引用 tx 的钩子一律走此路径,不 nil 直调。
  - `TestMhk8003_TableNames_Batch`:51 个 TableName() 存根批量表驱动调用(反射或显式清单)断言返回表名 —— 每个存根 1 stmt,是 models 包 70% 数学的免费 stmt 基础,必须全量命中。
  - `TestMhk8003_Hook_ErrorBranch`:有错误返回分支的钩子(以钩子体实际实现为准盘点)→ 构造触发条件 → 断言错误上抛(GORM Create 返回该错误)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestMhk8003_" ./internal/models/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestMhk8003_" internal/models/models_hooks_80_03_test.go` ≥ 4
  - TableName 批量用例覆盖全部存根(对照 internal/models 的 model 清单计数)
  - 引用 tx 的钩子零 nil 直调(仅 base.go 两钩子允许)
  - `go test -count=1 -run "TestMhk8003_" ./internal/models/` exit 0
  </acceptance_criteria>
  <commit>test(80-03): models GORM 钩子测试(nil 直调 + AutoMigrate 触发 + TableName 批量)</commit>
  <done>models 钩子类别全覆盖;TableName 存根全量命中垫高包覆盖数学。</done>
</task>

<task type="auto">
  <name>Task 7: api/v1 tail——job_cron_util 纯收满 + job_utils + ws_notice_handler 补分支(真 WS 握手)</name>
  <files>internal/api/v1/job_cron_util_80_03_test.go, internal/api/v1/api_v1_tail_80_03_test.go</files>
  <read_first>
  - internal/api/v1/job_cron_util.go — 全文(:13-195)
  - internal/api/v1/job_utils.go — 全文(FormatDuration 纯 + GetJobStatistics sqlite sys_job/sys_job_log)
  - internal/api/v1/ws_notice_handler.go — 全文(CheckOrigin 分支 + Upgrader)
  - internal/websocket/notice_hub_readpump_test.go:26-127 — WS harness 范式
  </read_first>
  <action>
  创建两个文件:

  job_cron_util_80_03_test.go(前缀 TestJcu8003_,全纯 table-driven 一次收满):
  - validateCronExpression(合法 6 域/缺域/越界/垃圾)/ calculateNextRunTime(合法→未来时间/非法→零值)/ GetCronDescription(常见表达式描述断言)/ ParseCronExpression(标准表达式六字段 + 错误分支)/ ValidateCronExpression(聚合三返回值)/ generateCronExpression(六字段拼接)/ CronExpressionBuilder 链式(逐 Set 后 Build 等价表达式 + 链式返回同一实例语义)/ EveryXxx 五常量 + Custom 拼接值断言。

  api_v1_tail_80_03_test.go(前缀 TestJbu8003_ / TestWs8003_ / TestRtr8003_):
  - `TestJbu8003_FormatDuration`:表驱动(秒/分/时/组合/零值,期望值手算注释)。
  - `TestJbu8003_GetJobStatistics`:sqlite(t.TempDir)+ AutoMigrate(models.Job/models.JobLog)种子行 → 统计数断言;空表零值分支。
  - `TestWs8003_CheckOrigin_Table`:containsOrigin + newWebSocketUpgrader 的 CheckOrigin 分支表(* 通配 / 同源 / localhost / 白名单命中 / 白名单外拒绝),断言 upgrader.CheckOrigin(req) 布尔。
  - `TestWs8003_RealHandshake`:httptest.NewServer + gorilla Upgrader(ws_notice_handler 的 upgrader)→ websocket.DefaultDialer.Dial 成功握手 + 收发一帧 + 关闭(照 readpump 范式:server shutdown → conn close 顺序,R5);无 origin 头/带白名单 origin 两态。
  - `TestRtr8003_RouterShape`:monitor_router/router.go 装配函数调用一次(trivial 3 stmts)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestJcu8003_|TestJbu8003_|TestWs8003_|TestRtr8003_" ./internal/api/v1/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestJcu8003_" internal/api/v1/job_cron_util_80_03_test.go` ≥ 8
  - `grep -c "^func TestWs8003_" internal/api/v1/api_v1_tail_80_03_test.go` ≥ 2(含真握手)
  - CheckOrigin 分支表 ≥5 行(* / 同源 / localhost / 白名单 / 拒绝)
  - `go test -count=1 -run "TestJcu8003_|TestJbu8003_|TestWs8003_|TestRtr8003_" ./internal/api/v1/` exit 0
  </acceptance_criteria>
  <commit>test(80-03): job_cron_util 纯收满 + job_utils + WS 真握手与 CheckOrigin 表</commit>
  <done>job_cron_util 0% → ≥90%;job_utils 0% → ≥70%;ws_notice_handler 45.3% → ≥85%。</done>
</task>

<task type="auto">
  <name>Task 8: plan 收口 — api/v1 与 models 双包 ≥70% 实测 + race 抽样 + gate + SUMMARY</name>
  <files>.planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-03-SUMMARY.md</files>
  <read_first>
  - .planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-RESEARCH.md §1.3/§1.4 — 两包基线与逐文件表(对照)
  </read_first>
  <action>
  收口四步:

  1. 双包实测(各只跑一次):`go test -count=1 -coverprofile=cov80_03a.out ./internal/api/v1/` 与 `go test -count=1 -coverprofile=cov80_03b.out ./internal/models/` → `go tool cover -func` 逐文件;实测后删除两个 .out。
  2. 达标判定:internal/api/v1 ≥70%(≥405/578)、internal/models ≥70%(≥312/445);未达标 → `go tool cover -func` 找 0% 函数回对应 task 补(只补缺口);api/v1 的 ws/routers 小文件与 models 的 TableName 存根是数学垫脚,优先确认已命中。
  3. 稳定性:`go test -race -count=1 -run "TestAuth8003_|TestCap8003_|TestCapBg8003_|TestWs8003_" ./internal/api/v1/`(WS/httptest 是 race 敏感面)+ `go build ./...` + `go test ./...`。
  4. SUMMARY 落:两包逐文件基线 → 实测表、newMiniCore8003 fixture 形状说明(字段清单 + 建表策略,80-04 同形复制的输入)、open Q2/Q3 的现场结论(钩子 tx 依赖盘点 / CaptchaBackgroundService 构造形态)、R3 教训执行情况(表列精简)。
  </action>
  <verify>
    <automated>go test -count=1 -coverprofile=cov80_03a.out ./internal/api/v1/ && go tool cover -func=cov80_03a.out | tail -1 && go test -count=1 -coverprofile=cov80_03b.out ./internal/models/ && go tool cover -func=cov80_03b.out | tail -1 && rm -f cov80_03a.out cov80_03b.out && go build ./... && go test ./... 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
  - internal/api/v1 包 ≥70% 且 internal/models 包 ≥70%(数字落 SUMMARY)
  - api/v1 六个非 router 文件无一 <50%(auth/captcha 家族/job_cron_util/job_utils/ws_notice)
  - `go build ./...` + `go test ./...` exit 0;-race 抽样 exit 0;cov*.out 已删除
  </acceptance_criteria>
  <commit>docs(80-03): 收口 api/v1 + models 双包 ≥70% + mini-Core fixture 沉淀落 SUMMARY</commit>
  <done>碎包 A 关门:api/v1 6.6% → ≥70%、models 0.2% → ≥70%;fixture 形状交付 80-04。</done>
</task>

</tasks>

<notes>
- 依赖:无(与 80-01/80-02/80-05 零文件交集,wave 1 可并行;80-04 依赖本 plan 的 fixture 形状 → wave 2)。
- 被依赖:80-04 复制 newMiniCore8003 形状(不同包不能共享 _test.go,各持同形副本,命名 newMiniCore8004)。
- mini-Core 保真度(D-80-03):CaptchaService 是具体类型无法 stub;否决接口化改造;fixture 只建 handler 实际引用的列(78-01 精简哲学,R3)。
- 占位 handler 教训:一切断言经真 handler 真请求;禁止复现 auth_integration_test.go:458 的注册即"覆盖"假象。
- race 安全:WS 用例照 readpump 关闭顺序(server shutdown → conn close);断言不依赖时序;禁 t.Parallel(api/v1 的 gin test context 与全局 logger 面)。
- quirk 纪律:vdi AES 硬编码 key 等"行为 vs 文档"差异就地注释锁定,零生产代码改动(D-80 escape hatch 仅限源码-vs-文档错配)。
</notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 ↔ 数据库 | glebarez sqlite(t.TempDir 文件库),不触碰生产 PG |
| 测试 ↔ 网络出口 | WS 仅 httptest.NewServer 回环;SM2/JWT keypair 由 manager 进程内生成;零外呼 |
| 测试 ↔ 文件系统 | captcha 背景图等文件 fixture 落 t.TempDir,禁写仓库目录 |
| 测试 ↔ 生产代码 | 仅新增 *_80_03_test.go;生产 .go 改动 = 0(D-80 escape hatch 除外) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-03-01 | Spoofing | 测试伪造 token 绕过断言 | mitigate | token 经 fixture JWTManager 真实签发/校验,不手搓假 token 断言 |
| T-80-03-02 | DoS | WS goroutine 泄漏(race 敏感) | mitigate | readpump 关闭顺序 + -race 抽样(R5);禁裸 sleep |
| T-80-03-03 | Information Disclosure | 加密对测试输出敏感值到日志 | mitigate | 断言只比字段,不 t.Log 密文/明文对 |
| T-80-03-SC | Tampering | 依赖安装 | accept | 零新增依赖(gorilla/websocket 等全既有) |
</threat_model>

<verification>
- `go test -count=1 ./internal/api/v1/ ./internal/models/` 全绿(既有 5 个 api/v1 测试文件与 models 既有测试不回归)
- `go tool cover -func`:internal/api/v1 ≥70%、internal/models ≥70%
- `go build ./...` + `go test ./...` exit 0;-race 抽样绿
- 生产 .go 改动 = 0 文件;D-80-03 口径执行(CaptchaService 未接口化)
</verification>

<success_criteria>
1. 6 个新测试文件存在且全绿:TestAuth8003_ ≥12、TestCap8003_/TestCapBg8003_ ≥8、TestMdl8003_ ≥4、TestMst8003_ ≥5、TestMhk8003_ ≥4、TestJcu8003_ ≥8 + TestWs8003_/TestJbu8003_/TestRtr8003_
2. internal/api/v1 包 ≥70%(基线 6.6%);internal/models 包 ≥70%(基线 0.2%)
3. newMiniCore8003 fixture 落地且形状文档化(80-04 复制输入)
4. models TableName 存根与 7 个 Valuer/Scanner、59 个 Scan/Value/钩子函数全类别命中
5. `go build ./...` + `go test ./...` exit 0;每 task 一原子 commit
</success_criteria>

<decision_audit>
本 plan 涉及的 phase-80 决策(executor 必须遵守):

| ID | 决策 | 依据 / 理由 |
|----|------|------------|
| D-80-03 | api/v1 mini-Core 保真度 = research DQ3-(a):真 CaptchaService(core.NewCaptchaService,具体类型)+ 真 security.NewJWTManager + sqlite 表;80-03 与 80-04 各持一份同形副本(命名 newMiniCore8003/newMiniCore8004,跨包不能共享 _test.go);**否决 (b) 把 CoreServices.CaptchaService 改接口**(生产结构变更,违反零业务变更纪律) | research §3.1 + DQ3;CaptchaService 具体类型无法 stub,(a) 是唯一可行路径;占位 handler 教训(auth_integration_test.go:458)证明真装配是必须 |
| D-80-07(沿用) | 命名 `<source-or-topic>_80_03_test.go`(api_v1_tail 为 topic 合并件);helper 8003 后缀 | D-78-08 / D-79-06 同构 |
| (沿用) | open Q2 口径:引用 tx 的钩子一律 sqlite AutoMigrate 插入触发;nil 直调仅限已确认 nil-safe 的 base.go 两钩子 | research §11.2 推荐(不依赖未确认假设) |
| (沿用) | status/UAC 位断言引用 models.* 常量 | CLAUDE.md Status Value Convention |
| (沿用) | quirk 纪律:零生产代码改动,行为差异就地记录 | D-78-10 同构;D-80 escape hatch 仅限源码-vs-文档错配 |

基线数字来源:80-RESEARCH.md §1.3/§1.4(2026-08-28 实测;api/v1 578/38/6.6%,models 445/1/0.2%)。
</decision_audit>

<output>
Create `.planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-03-SUMMARY.md` when done
</output>
