---
phase: 80-scheduler-and-fragments
plan: 04
status: pending
type: execute
wave: 2
depends_on: ["80-03"]
files_modified:
  - internal/api/router_80_04_test.go
  - pkg/errors/errors_80_04_test.go
  - pkg/errors/codes_80_04_test.go
autonomous: true
requirements: [TAIL-02]

must_haves:
  truths:
    - "按 D-80-01 裁决:SetupRouter 采用单一大装配测试(DQ1-a)—— newMiniCore8004 + gin.New() + 全量 SetupRouter 一次覆盖 ~85-95%;probe(R1)先行,Conclusion A(全量装配通过)为默认路径,Conclusion B(某模块 panic)按模块组拆 + 只测可达组,两结论都可接受且必须落 SUMMARY"
    - "internal/api 包 ≥70%(417 stmts,0 covered,+292):router.go 单文件包,装配层无 setup 期 DB 查询/无 scheduler 注册/无 agent goroutine(grep 审计);core.Config 与 core.DB 必须非 nil(router.go:53/:619/:659 直接解引用),core.JWTManager/Cache/OperLogService 等 nil-safe 构造参数"
    - "setupNoticeHub 副作用收口(router.go:86-95):go noticeHub.Run() goroutine + scheduler.SetNoticeHub 全局改写 → t.Cleanup(hub.Stop() + SetNoticeHub 原值恢复,77-05 var-seam 纪律);禁 t.Parallel"
    - "pkg/errors 包 ≥70%(326 stmts,45 covered,+183):errors.go ~25 个构造器全 table-driven + Wrap/WrapWithHTTPStatus/WithContexts/GetCode/GetContext/IsAppError/GetAppError/GetErrorCode;codes.go DefaultHTTPStatus/DefaultMessage 全 ErrorCode 枚举表驱动一遍收满"
    - "2-3 个 spot-check 请求(如 GET /api/v1/system/auth/config)验证装配后的路由真实可走通(404/405 分支),防『装配成功但路由死』假象"
  artifacts:
    - path: "internal/api/router_80_04_test.go"
      provides: "R1 probe + SetupRouter 全量装配测试 + spot-check 请求(前缀 TestRtr8004_/TestProbe8004_)"
      min_lines: 200
      contains: "SetupRouter"
    - path: "pkg/errors/errors_80_04_test.go"
      provides: "AppError 构造器群 + Wrap/Context/Getter 全 table-driven(前缀 TestApe8004_)"
      min_lines: 240
      contains: "IsAppError"
    - path: "pkg/errors/codes_80_04_test.go"
      provides: "ErrorCode 枚举 DefaultHTTPStatus/DefaultMessage 全量表驱动(前缀 TestEdc8004_)"
      min_lines: 140
      contains: "DefaultHTTPStatus"
  key_links:
    - from: "internal/api/router_80_04_test.go"
      to: "internal/api/router.go:98 SetupRouter(r *gin.RouterGroup, core *core.Core, allowedOrigins []string)"
      via: "newMiniCore8004(80-03 同形副本)+ gin.New().Group(\"/api/v1\") → SetupRouter → spot-check 请求"
      pattern: "SetupRouter|gin\\.New"
    - from: "internal/api/router_80_04_test.go"
      to: "internal/api/router.go:86-95 setupNoticeHub"
      via: "返回的 *websocket.NoticeHub → t.Cleanup(hub.Stop);scheduler.SetNoticeHub save/restore"
      pattern: "setupNoticeHub|SetNoticeHub|hub\\.Stop"
    - from: "pkg/errors/errors_80_04_test.go"
      to: "pkg/errors/errors.go:145-265+ 构造器群"
      via: "每构造器断言 Code/HTTPStatus/message 链(引用 codes.go 常量,禁裸数字)"
      pattern: "BadRequest|RecordNotFound|Unauthorized"
---

# Plan 4: 碎包 B——internal/api SetupRouter 装配 + pkg/errors 纯函数 (Phase: 80-scheduler-and-fragments)

<objective>
交付两个碎包:internal/api(417 stmts @0.0%,gap +292,router.go 单文件装配层)与 pkg/errors(326 stmts @13.8%,gap +183,全纯函数),合计 +475 且边际工作量小。SetupRouter 走 DQ1-a 单一大装配测试(probe R1 先行);pkg/errors 一条 table-driven 大文件收满。两包双双 ≥70%。

Purpose: TAIL-02(SC-2)的装配层段。internal/api 是全仓路由装配真相源 —— 一次性覆盖它等于把全部模块 router 的装配语句纳入回归网;pkg/errors 是 P0 级低垂果实(~25 构造器 0%)。probe 结论回填 research 的 MEDIUM→HIGH 置信度缺口(R1)。
Output: 新增 3 个测试文件(零生产代码改动,D-80 escape hatch 除外);R1 probe 结论落 SUMMARY。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-RESEARCH.md
@.planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-03-SUMMARY.md

<interfaces>
<!-- 关键契约(planner 已逐一读源码复核,2026-08-28)。executor 直接使用,无需重新探索。 -->

From internal/api/router.go(417 stmts,单文件包):
```go
func setupNoticeHub(r *gin.RouterGroup, core *core.Core) *websocket.NoticeHub // :86
//   NewNoticeHub → go noticeHub.Run() → core.NoticeHub = hub → scheduler.SetNoticeHub(&noticeHubAdapter{hub})

func SetupRouter(r *gin.RouterGroup, core *core.Core, allowedOrigins []string) // :98
// setup 期依赖审计(research §1.5 逐行 grep):
//   必须非 nil:core.Config(:53 解引用;零值 Config{} 走 encryption-disabled 分支)
//              core.DB(:619/:659 core.DB.GetDB() 直接解引用 → &coredb.Database{DB: sqlite, Type: "sqlite"})
//   仅构造参数(nil-safe):core.JWTManager(:116/:335/:359…) / core.Cache / core.CacheConfigService(:265/:582/:659)
//              core.TokenBlacklistService / core.OperLogService(:118)
//   副作用:setupNoticeHub(:86-95)go Run() + SetNoticeHub 全局
//   无 setup 期 DB 查询、无 scheduler 任务注册、无 agent goroutine(grep go func|.Start(|Register*Tasks|InitDefaultRoles 在 router.go 零命中)
// 中间件链:RequestID → encryption(:53-71 disabled 分支)→ noticeHub → JWTAuthWithBlacklist → OperLogMiddleware
```

From pkg/errors/errors.go(190 stmts,32 cov):
```go
func (e *AppError) Error() string / Unwrap() error                       // :18/:26
func (e *AppError) WithContext(key string, value interface{}) *AppError  // :31
func (e *AppError) WithContexts(ctx map[string]interface{}) *AppError    // :40
func (e *AppError) GetCode() ErrorCode / GetHTTPStatus() int / GetContext() map[string]interface{} // :51/:56/:64
func New(code ErrorCode, message string) *AppError                       // :69
func NewWithHTTPStatus(code, httpStatus, message) *AppError              // :78
func Wrap(err error, code ErrorCode, message string) *AppError           // :90
func WrapWithHTTPStatus(err, code, httpStatus, message) *AppError        // :100
func IsAppError(err error) bool / GetAppError(err) *AppError / GetErrorCode(err) ErrorCode // :113/:119/:128
// 构造器群 :145-265+(~25 个,全 0%):
// ParamError/BadRequest/ParamMissing/ParamInvalid/RecordNotFound/RecordNotFoundWithMsg/RecordExists/
// DatabaseError/Unauthorized/UnauthorizedWithMsg/TokenExpired/TokenInvalid/PermissionDenied/
// PermissionDeniedWithMsg/Forbidden/ServerError/InternalServerError/InternalServerErrorWithMsg/
// NotFound/InvalidOperation/InvalidOperationWithMsg/NotImplemented/UserNotFound/UserNotFoundWithID/
// UserExists/UserExistsWithUsername/…(以 errors.go 实际清单为准逐一盘点)
```

From pkg/errors/codes.go(136 stmts,13 cov):
```go
// ErrorCode 枚举 + DefaultHTTPStatus(:195 附近,60% 已覆盖)+ DefaultMessage(:219 附近,5.6%)
// 全枚举表驱动一遍 → 两函数收满(codes.go 实际枚举清单为准)
```

Fixture(80-03 同形副本,命名 newMiniCore8004;跨包不能共享 _test.go):
```go
// 字段清单照 80-03 auth_80_03_test.go 的 newMiniCore8003(SUMMARY 有形状文档):
// core.CoreInfra{Config: &config.Config{}, DB: &coredb.Database{DB: sqlite, Type: "sqlite"},
//   Cache: cache.NewMemoryCache(...), JWTManager: security.NewJWTManager(&cfg.JWT),
//   PwdManager: security.NewPasswordManager(nil), CaptchaService: core.NewCaptchaService(coreDB, mem)}
// core.Core{CoreInfra: infra, CoreServices: &core.CoreServices{}}
// 差异点:80-04 需要更多模块表 —— AutoMigrate 按 spot-check 与装配 panic 点逐个补
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: R1 probe——SetupRouter 装配探针(15 分钟,Conclusion A/B 都可接受)</name>
  <files>internal/api/router_80_04_test.go</files>
  <read_first>
  - internal/api/router.go — :38-120(setupEncryptionMiddlewares/setupNoticeHub/SetupRouter 开头与 system 组)
  - internal/api/v1/auth_80_03_test.go — newMiniCore8003 形状(80-03 SUMMARY 为准)
  </read_first>
  <action>
  创建 internal/api/router_80_04_test.go(package api)。先落探针(一个最小可运行测试,失败即产信息):

  - `newMiniCore8004(t *testing.T) (*core.Core, *gorm.DB)` — 复制 80-03 fixture 形状(命名 8004);表先只建 system 组所需。
  - `TestProbe8004_SetupRouter_MinimalAssembly`:gin.New() → r := router.Group("/api/v1") → newMiniCore8004 → **只验证不 panic 的最小装配**:`SetupRouter(r, c, []string{"http://localhost"})` + `t.Cleanup`(hub.Stop + SetNoticeHub restore)。panic → 读栈定位模块 → 该模块所需表/服务补进 fixture 重试(15 分钟预算,二分定位)。
  - 结论记录(注释 + 后续 SUMMARY):
    - **Conclusion A**:全量 SetupRouter 一次通过 → Task 2 走单一大装配测试(DQ1-a 默认)。
    - **Conclusion B**:某模块 Setup 对 nil 服务做非常规调用 → 不改生产代码;该模块组从装配测试中剥离,Task 2 按模块组拆分只测可达组,差额由 pkg/errors(纯,余量大)吸收。

  纪律:probe 也是正式测试(保留在最终文件);t.Cleanup 收口 hub goroutine 与全局 var。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestProbe8004_" ./internal/api/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - TestProbe8004_SetupRouter_MinimalAssembly 存在且 exit 0(或 Conclusion B 时剥离声明已注释落盘)
  - `grep -c "newMiniCore8004" internal/api/router_80_04_test.go` ≥ 1
  - hub goroutine 收口:t.Cleanup 含 hub.Stop / SetNoticeHub 恢复
  - `go test -count=1 -run "TestProbe8004_" ./internal/api/` exit 0
  </acceptance_criteria>
  <commit>test(80-04): SetupRouter R1 装配探针(mini-Core 8004 + Conclusion A/B 记录)</commit>
  <done>probe 结论(A/B)落定;fixture 就位;后续任务按结论走对应路径。</done>
</task>

<task type="auto">
  <name>Task 2: SetupRouter 全量装配测试(DQ1-a)+ spot-check 请求</name>
  <files>internal/api/router_80_04_test.go</files>
  <read_first>
  - internal/api/router.go — 全文(逐模块 Setup 调用清单,重点 :619/:659 的 core.DB.GetDB() 解引用点)
  - Task 1 probe 结论(文件内注释)
  </read_first>
  <action>
  同文件追加 TestRtr8004_:

  **Conclusion A 路径(默认,单一大装配):**
  - `TestRtr8004_SetupRouter_FullAssembly`:全量 SetupRouter(r, core, allowedOrigins)一次 → 断言 r.Routes() 数量 > 某阈值(以实际路由数为准,写注释记录实测值)+ 关键路径前缀存在(/system/auth、/ops、/monitor 等按 router.go 实际模块组)。panic 时按 probe 二分法补表/服务(不加生产代码)。
  - spot-check 请求(2-3 个,证路由真实可走):
    - `GET /api/v1/system/auth/config` → 200 或业务码(经真 handler,零值 Config 走 disabled 分支);
    - 任一需认证路径无 token → 401(中间件链生效);
    - 不存在路径 → 404(路由表已挂)。
  - `TestRtr8004_NoticeHubSideEffect`:setupNoticeHub 返回 hub 非 nil;core.NoticeHub 已挂;SetNoticeHub 后 GetNoticeHub() 返回 adapter(断言全局缝被写,restore 由 t.Cleanup)。

  **Conclusion B 路径(仅当 probe 失败):**按模块组拆 2-4 个装配测试(先 system 组 → 逐组扩展可达组),每组一个 TestRtr8004_SetupRouter_<Module>;不可达组在文件头注释列名 + 原因(对应 stmt 属豁免口径,落 SUMMARY)。差额由 Task 3/4 的 pkg/errors 全量收满补齐(两包合计 gap 475,errors 上限 ~281 unc)。

  纪律:禁 t.Parallel(hub goroutine + 全局 var);t.Cleanup(hub.Stop + SetNoticeHub(save 原值));sqlite 表按 panic 点逐个 AutoMigrate,不预建全库。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestRtr8004_" ./internal/api/ && go test -count=1 -coverprofile=cov80_04r.out ./internal/api/ && go tool cover -func=cov80_04r.out | grep "router.go" && rm -f cov80_04r.out && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestRtr8004_" internal/api/router_80_04_test.go` ≥ 3
  - 存在 spot-check 请求断言(401/404/200 三类至少两类)
  - Conclusion A:router.go per-file ≥70%;Conclusion B:可达组覆盖 + 剥离清单落注释与 SUMMARY
  - `go test -count=1 -run "TestRtr8004_" ./internal/api/` exit 0
  </acceptance_criteria>
  <commit>test(80-04): SetupRouter 全量装配 + spot-check 请求(DQ1-a)</commit>
  <done>router.go 0% → ≥70%(或 Conclusion B 可达组口径);装配真相源纳入回归网。</done>
</task>

<task type="auto">
  <name>Task 3: pkg/errors errors.go——构造器群 + Wrap/Context/Getter 全 table-driven</name>
  <files>pkg/errors/errors_80_04_test.go</files>
  <read_first>
  - pkg/errors/errors.go — 全文(:18-265+,构造器清单逐一盘点)
  - pkg/errors/codes.go — ErrorCode 常量(断言引用,禁裸数字)
  </read_first>
  <action>
  创建 pkg/errors/errors_80_04_test.go(package errors)。前缀 TestApe8004_:

  - `TestApe8004_Constructors_Table`:errors.go :145-265+ 的全部构造器(以源码实际清单为准,逐个列入表驱动)—— 每行断言:返回 *AppError 非 nil、GetCode() == 对应 codes.go 常量、GetHTTPStatus() == DefaultHTTPStatus(该 code) 或构造器显式指定的 status、Error() 含预期 message 片段。
  - `TestApe8004_NewAndNewWithHTTPStatus`:New 的默认 HTTP status 与 NewWithHTTPStatus 覆盖语义。
  - `TestApe8004_Wrap_Variants`:Wrap 保留原错误(Unwrap() 链)+ 新 code/message;WrapWithHTTPStatus 覆盖 status;wrapped err 经 errors.Is/As 可还原(语义断言)。
  - `TestApe8004_WithContext`:WithContext 链式追加 → GetContext() 含 key;WithContexts 批量 → 全键在;nil map/空 map 分支。
  - `TestApe8004_TypeAssert_Helpers`:IsAppError(AppError)==true / 普通 error ==false;GetAppError 两态;GetErrorCode(AppError 取值 / 非 AppError 的回退值)。
  - `TestApe8004_ErrorAndUnwrap`:Error() 格式化(含 code/message)+ Unwrap 链。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestApe8004_" ./pkg/errors/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestApe8004_" pkg/errors/errors_80_04_test.go` ≥ 6
  - 构造器表驱动行数 ≥ errors.go 实际构造器数(逐一盘点,grep "^func " 构造器段)
  - `go test -count=1 -run "TestApe8004_" ./pkg/errors/` exit 0
  </acceptance_criteria>
  <commit>test(80-04): pkg/errors 构造器群与 Wrap/Context 全 table-driven</commit>
  <done>errors.go 16.8% → ≥85%(~25 构造器 0% 全清)。</done>
</task>

<task type="auto">
  <name>Task 4: pkg/errors codes.go——ErrorCode 枚举 DefaultHTTPStatus/DefaultMessage 全量表驱动</name>
  <files>pkg/errors/codes_80_04_test.go</files>
  <read_first>
  - pkg/errors/codes.go — 全文(枚举清单 + :195 DefaultHTTPStatus + :219 DefaultMessage)
  </read_first>
  <action>
  创建 pkg/errors/codes_80_04_test.go。前缀 TestEdc8004_:

  - `TestEdc8004_DefaultHTTPStatus_AllCodes`:遍历全部 ErrorCode 常量(grep "^.*ErrorCode = " 盘点清单,或按 codes.go 枚举块显式列表)→ DefaultHTTPStatus(code) 断言映射值(引用常量对照表,期望值手写注释)。
  - `TestEdc8004_DefaultMessage_AllCodes`:同上,DefaultMessage(code) 非空且语义对应(抽 5+ 关键码精确断言,其余非空 + 无 "<nil>"/占位符残留)。
  - `TestEdc8004_UnknownCode_Fallback`:未知 ErrorCode(越界值)→ 两函数的 fallback 分支(按 :195/:219 实现断言)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestEdc8004_" ./pkg/errors/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestEdc8004_" pkg/errors/codes_80_04_test.go` ≥ 3
  - 枚举遍历覆盖 codes.go 全部 ErrorCode 常量(数量在测试注释中记录)
  - `go test -count=1 -run "TestEdc8004_" ./pkg/errors/` exit 0
  </acceptance_criteria>
  <commit>test(80-04): pkg/errors codes 枚举全量表驱动(HTTPStatus/Message)</commit>
  <done>codes.go 9.6% → ≥90%;pkg/errors 包 ≥85%,为 internal/api 的包级 70% 提供余量。</done>
</task>

<task type="auto">
  <name>Task 5: plan 收口 — internal/api 与 pkg/errors 双包实测 + gate + R1 结论回填 SUMMARY</name>
  <files>.planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-04-SUMMARY.md</files>
  <read_first>
  - .planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-RESEARCH.md §1.5/§1.6/§5 R1 — 基线与 probe 风险条目(对照)
  </read_first>
  <action>
  收口四步:

  1. 双包实测(各只跑一次):`go test -count=1 -coverprofile=cov80_04a.out ./internal/api/` 与 `go test -count=1 -coverprofile=cov80_04b.out ./pkg/errors/` → `go tool cover -func`;实测后删除两个 .out。
  2. 达标判定:internal/api ≥70%(≥292/417,Conclusion B 时按剥离口径)、pkg/errors ≥70%(≥229/326,目标 ≥85%);两包合计 covered 增量记入 phase 总账(+475 目标,Conclusion B 时 errors 超额补差)。
  3. 稳定性:`go test -race -count=1 ./internal/api/ ./pkg/errors/`(hub goroutine 面)+ `go build ./...` + `go test ./...`。
  4. SUMMARY 落:**R1 probe 结论(A/B + panic 模块清单与修复用的表/服务补齐项)**、两包逐文件基线 → 实测表、Conclusion B 时的剥离组清单与 stmt 数、mini-Core 8004 与 8003 的差异点。
  </action>
  <verify>
    <automated>go test -count=1 -coverprofile=cov80_04a.out ./internal/api/ && go tool cover -func=cov80_04a.out | tail -1 && go test -count=1 -coverprofile=cov80_04b.out ./pkg/errors/ && go tool cover -func=cov80_04b.out | tail -1 && rm -f cov80_04a.out cov80_04b.out && go build ./... && go test ./... 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
  - internal/api 包 ≥70% 且 pkg/errors 包 ≥70%(数字落 SUMMARY)
  - R1 probe 结论明确落 SUMMARY(A 或 B + 证据)
  - `go build ./...` + `go test ./...` exit 0;-race 绿;cov*.out 已删除
  </acceptance_criteria>
  <commit>docs(80-04): 收口 internal/api + pkg/errors 双包实测 + R1 probe 结论回填</commit>
  <done>碎包 B 关门:internal/api 0% → ≥70%、pkg/errors 13.8% → ≥85%;R1 不确定性消解。</done>
</task>

</tasks>

<notes>
- 依赖:80-03 的 mini-Core 形状(newMiniCore8003 → 8004 同形副本);与 80-01/80-02/80-05 零文件交集。
- 被依赖:无(Phase 80 内)。
- D-80-01 关键:勿为此改生产代码(R1 fallback = 按模块组拆,不是加 ForTesting 钩子);probe 失败信息本身就是交付物。
- race 安全:setupNoticeHub 的 go Run() 必须配 hub.Stop();SetNoticeHub 是跨包全局(scheduler 包),save/restore 纪律同 77-05;禁 t.Parallel。
- pkg/errors 是纯函数包:一条大表驱动文件即可,断言引用 codes.go 常量(禁裸数字 status/code)。
- quirk 纪律:装配中发现的模块级隐式依赖(如某 Setup 读 nil 字段)按 quirk 记录(SUMMARY),不修生产结构。
</notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 ↔ 数据库 | glebarez sqlite(t.TempDir 文件库),不触碰生产 PG |
| 测试 ↔ 全局状态 | scheduler.SetNoticeHub 跨包全局 → save/t.Cleanup restore;hub goroutine → hub.Stop() |
| 测试 ↔ HTTP 面 | 全部经 gin test context / httptest 回环,零外呼 |
| 测试 ↔ 生产代码 | 仅新增 *_80_04_test.go;生产 .go 改动 = 0(D-80 escape hatch 除外) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-04-01 | DoS | noticeHub.Run() goroutine 泄漏 | mitigate | t.Cleanup(hub.Stop)+ -race 收口(R7);禁 t.Parallel |
| T-80-04-02 | Tampering | SetNoticeHub 全局污染跨测试/跨包 | mitigate | save → restore(77-05 纪律);本包串行 |
| T-80-04-03 | Denial of Service(装配) | 模块 Setup 隐式 panic 拖垮套件 | mitigate | probe 二分定位;Conclusion B 剥离路径预设;零生产代码改动 |
| T-80-04-SC | Tampering | 依赖安装 | accept | 零新增依赖 |
</threat_model>

<verification>
- `go test -count=1 ./internal/api/ ./pkg/errors/` 全绿
- `go tool cover -func`:internal/api ≥70%、pkg/errors ≥70%(目标 ≥85%)
- `go build ./...` + `go test ./...` exit 0;-race 绿
- 生产 .go 改动 = 0 文件;R1 结论落 SUMMARY
</verification>

<success_criteria>
1. 3 个新测试文件存在且全绿:TestProbe8004_ 1 + TestRtr8004_ ≥3、TestApe8004_ ≥6、TestEdc8004_ ≥3
2. internal/api 包 ≥70%(基线 0.0%);pkg/errors 包 ≥85%(基线 13.8%,下限 70%)
3. D-80-01 执行:单一大装配测试(DQ1-a)+ 2-3 spot-check;R1 结论(A/B)落 SUMMARY
4. setupNoticeHub 副作用全收口(hub.Stop + SetNoticeHub restore)
5. `go build ./...` + `go test ./...` exit 0;每 task 一原子 commit
</success_criteria>

<decision_audit>
本 plan 涉及的 phase-80 决策(executor 必须遵守):

| ID | 决策 | 依据 / 理由 |
|----|------|------------|
| D-80-01 | SetupRouter 测试策略 = research DQ1-(a):单一大装配测试(一个 mini-Core 8004 + gin.New() 挂全 SetupRouter,一次覆盖 ~85-95%,失败时二分定位)+ 2-3 个 spot-check 请求(认证 401/404/业务 200);**Task 1 = R1 probe 先行(15-min),Conclusion A(全量通过)/ B(按模块组拆只测可达组)都可接受,结论必须落 SUMMARY**;B 路径差额由 pkg/errors 纯函数余量吸收;**勿为此改生产代码** | research §1.5 静态审计支持(无 setup 期 DB 查询/scheduler 注册/agent goroutine),但 417 stmts 单测只有跑了才知道(§11.1);R1 是本 phase 最高严重度风险,probe-first 是唯一低成本消解路径 |
| D-80-03(沿用) | mini-Core 保真度:真 CaptchaService/JWTManager + sqlite;8004 为 8003 同形副本 | research DQ3;跨包不能共享 _test.go |
| D-80-07(沿用) | 命名 `<source>_80_04_test.go`;helper 8004 后缀 | D-78-08 / D-79-06 同构 |
| (沿用) | var-seam 纪律(SetNoticeHub 跨包全局) | 77-05 BLOCK-02 惯例 |
| (沿用) | quirk 纪律:零生产代码改动 | D-78-10 同构 |

基线数字来源:80-RESEARCH.md §1.5/§1.6(2026-08-28 实测;internal/api 417/0/0.0%,pkg/errors 326/45/13.8%)。
</decision_audit>

<output>
Create `.planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-04-SUMMARY.md` when done
</output>
