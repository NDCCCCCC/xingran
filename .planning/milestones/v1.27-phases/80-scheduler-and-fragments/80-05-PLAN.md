---
phase: 80-scheduler-and-fragments
plan: 05
status: pending
type: execute
wave: 3
depends_on: ["80-01", "80-02", "80-03", "80-04"]
files_modified:
  - pkg/cache/redis_80_05_test.go
  - pkg/permission/service_80_05_test.go
  - internal/websocket/notice_hub_broadcast_80_05_test.go
  - internal/services/base/base_80_05_test.go
  - pkg/query/pagination_80_05_test.go
  - pkg/gormutil/join_builder_80_05_test.go
  - pkg/logger/logger_hook_80_05_test.go
  - internal/services/lldp/lldp_tail_80_05_test.go
  - pkg/middleware/encryption_80_05_test.go
  - .planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-05-SUMMARY.md
autonomous: true
requirements: [TAIL-02, TAIL-03]

must_haves:
  truths:
    - "按 D-80-05 裁决:pkg/cache 缺口重锚 +161 → +49(Phase 76/78 已把包推到 64.7%)—— miniredis 驱动 redis.go typed helpers(~6-8 函数)即达标;省出的 plan 容量拨给小尾巴 sweep"
    - "pkg/cache ≥70%:redis.go 394 stmts @31.2% → ≥70%(GetJSON/SetJSON/GetInt/SetInt/GetBool/SetBool/MGetJSON/MSetJSON/MDelete/Increment·By/Decrement·By/Exists/FlushDB/getClient/getPrefix + MultiLevelCache 全方法 + 构造器);MultiLevel 构造器优先 NewMultiLevelCacheSimple(无后台 writer),起 worker 的构造器显式 Close + t.Cleanup(79-R7 教训)"
    - "按 D-80-04 裁决:SC-3 口径修正为『ROADMAP 点名的 8 包(middleware/permission/websocket/base/lldp/gormutil/query/logger)合计 ≥70%』——『<50 stmts』措辞失实(pkg/middleware 609 stmts,原意应为 <50% 覆盖的包);逐包余量:middleware +8 / gormutil +13 / permission +50 / websocket +45 / query +3 / lldp +11 / logger +1 / base +29 = +156"
    - "确不可测的按既有豁免规则逐条文档化(候选:l2_writer drainQueue 异步 goroutine、logger Fire 极端分支、middleware 加解密极端缓冲分支);豁免禁止静默"
    - "WS Broadcast 用真 WS 对(httptest + Upgrader + DefaultDialer,notice_hub_readpump_test.go:26-127 范式);断言用 Eventually 非.sleep(R5);middleware 加解密用真 RequestEncryptor(78-01 先例)+ timestamp ±window±1 表驱动,不依赖墙时钟(R6)"
    - "phase 收口(SC 验收动作):Phase 80 全部 14 个包逐包复测表(tot/cov/%/gap→实际)+ D-80-04/D-80-05/D-80-06 口径修正声明;coverage-baseline.md 全仓回填归 Phase 81(81-01),本 plan 只落 Phase 80 范围数字"
  artifacts:
    - path: "pkg/cache/redis_80_05_test.go"
      provides: "miniredis 驱动 redis.go typed helpers + MultiLevelCache 全方法(前缀 TestRds8005_/TestMlc8005_)"
      min_lines: 260
      contains: "miniredis.RunT"
    - path: "pkg/permission/service_80_05_test.go"
      provides: "permission service 81 stmts 全 0% 清欠:InitDefaultRolesAndMenus/createDefaultAdminRole/assignAllMenusToAdmin/Get·UpdateRoleMenus(前缀 TestPms8005_)"
      min_lines: 160
      contains: "InitDefaultRolesAndMenus"
    - path: "internal/websocket/notice_hub_broadcast_80_05_test.go"
      provides: "BroadcastToUsers/ToAll/RPAProgress×3/encodeMessage 真 WS 对测试(前缀 TestNhb8005_)"
      min_lines: 180
      contains: "BroadcastToUsers"
    - path: "internal/services/base/base_80_05_test.go"
      provides: "GORMRepository 泛型 CRUD/List/BatchDelete + WrapError/IsNotFound/IsDuplicate(前缀 TestBas8005_)"
      min_lines: 160
      contains: "NewGORMRepository"
    - path: "pkg/query/pagination_80_05_test.go"
      provides: "NewPaginatedResult/Normalize/GetOffset/ApplyOrder/ApplyPagination/DefaultQueryExecutor(前缀 TestPag8005_)"
      min_lines: 120
      contains: "NewPaginatedResult"
    - path: "pkg/gormutil/join_builder_80_05_test.go"
      provides: "join_builder 链式方法 sqlite 断言 SQL(前缀 TestJbt8005_)"
      min_lines: 140
      contains: "BuildJoinClause"
    - path: "pkg/logger/logger_hook_80_05_test.go"
      provides: "fileMirrorHook.Fire/Levels + fallbackToStdLogger + Fatal(前缀 TestLgr8005_)"
      min_lines: 90
      contains: "fallbackToStdLogger"
    - path: "internal/services/lldp/lldp_tail_80_05_test.go"
      provides: "ClassifyPort 纯表驱动 + lldp_service cache-hit 分支 + executor 错误分支(前缀 TestLdp8005_)"
      min_lines: 110
      contains: "ClassifyPort"
    - path: "pkg/middleware/encryption_80_05_test.go"
      provides: "RequestDecryption/ResponseEncryption/OperLogMiddleware httptest 测试(前缀 TestMdw8005_)"
      min_lines: 200
      contains: "RequestDecryption"
  key_links:
    - from: "pkg/cache/redis_80_05_test.go"
      to: "pkg/cache/redis.go:30 NewRedisCache(config *CacheConfig, keyPrefix string)"
      via: "miniredis.RunT(t) + CacheConfig{Addr: mr.Addr()} 装配;TTL 推进一律 mr.FastForward"
      pattern: "miniredis\\.RunT|FastForward"
    - from: "pkg/permission/service_80_05_test.go"
      to: "pkg/permission/service.go:30 InitDefaultRolesAndMenus(db)"
      via: "sqlite 建 sys_role/sys_menu/sys_role_menu → Init → 断言默认角色/菜单落库 → UpdateRoleMenus 往返"
      pattern: "InitDefaultRolesAndMenus|sys_role_menu"
    - from: "internal/websocket/notice_hub_broadcast_80_05_test.go"
      to: "internal/websocket/notice_hub.go:190-308 Broadcast 群"
      via: "真 WS client 连入 hub → Broadcast* → client 侧 Eventually 收到帧 → Stop() 收尾"
      pattern: "BroadcastRPAProgress|DefaultDialer"
---

# Plan 5: 碎包 C——pkg/cache 重锚(+49)+ 小尾巴 8 包 sweep + phase 逐包验收 (Phase: 80-scheduler-and-fragments)

<objective>
交付 phase 收尾段:pkg/cache(redis.go 394 stmts @31.2%,重锚后 gap +49,D-80-05)经 miniredis typed helpers 达标;小尾巴 8 包 sweep(+156,SC-3 按 D-80-04 修正口径合计 ≥70%);最后执行 phase 级逐包复测验收表(SC-1/SC-2/SC-3 全量核对)。

Purpose: TAIL-03 主体 + TAIL-02 收尾。本 plan 是 5-plan 的验收动作承载者 —— research §7 指定的"SC 逐包验收表"在此产出;pkg/cache 的 ROADMAP 数字(+161)已过时,重锚省出的 ~112 stmts 余量正是 8 小包 sweep 的容量来源。
Output: 新增 9 个测试文件 + phase 逐包验收表落 SUMMARY(零生产代码改动,D-80 escape hatch 除外)。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-RESEARCH.md
@.planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-01-SUMMARY.md
@.planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-02-SUMMARY.md
@.planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-03-SUMMARY.md
@.planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-04-SUMMARY.md

<interfaces>
<!-- 关键契约(planner 已逐一读源码复核,2026-08-28)。executor 直接使用,无需重新探索。 -->

From pkg/cache/redis.go(394 stmts,123 cov;余量集中在 :151-361 与 :595-732):
```go
func NewRedisCache(config *CacheConfig, keyPrefix string) (*RedisCache, error)   // :30(78-01 已有装配先例)
// typed helpers(全 0% 或低覆盖):GetJSON :275 / SetJSON :284 / GetInt :294 / SetInt / GetBool / SetBool
//   MGetJSON :326 / MSetJSON :360 / MDelete / Increment / IncrementBy / Decrement / DecrementBy
//   Exists / FlushDB / getClient / getPrefix(redis.go:151-361 段)
// MultiLevelCache 构造器(:519-578):NewMultiLevelCache / NewMultiLevelCacheWithWriter /
//   NewMultiLevelCacheSimple(无后台 writer,测试优先)/ NewMultiLevelCacheWithRetry / NewMultiLevelCacheWithRetryAndWriter
// MultiLevelCache 方法(:595-732 + :884-992):Get/Set/Delete/MGet/MSet/MDelete/Exists/Increment/MGetJSON/MSetJSON/SetJSON/GetJSON
// l2_writer.go drainQueue(0%,异步 goroutine)= 豁免候选(D-80-04)
```

From pkg/permission/service.go(114 stmts,30 cov;service.go 81 stmts 全 0%):
```go
func NewService() Service                                  // :25
func (s *service) InitDefaultRolesAndMenus(db *gorm.DB) error   // :30
func (s *service) createDefaultAdminRole(db) error          // :45(同包直调)
func (s *service) assignAllMenusToAdmin(db) error           // :81
func (s *service) GetRoleMenus(db, roleID) ([]string, error)      // :129
func (s *service) UpdateRoleMenus(db, roleID, menuIDs) error      // :139
func (s *service) GetRoleDepts / UpdateRoleDepts            // :162/:172
func (s *service) GetUserMenus(db, userID, includeHidden)   // :202
func (s *service) buildMenuTree(menus, parentID)            // :276(纯)
```

From internal/websocket/notice_hub.go(129 stmts,46 cov;未开在 :190-308):
```go
func NewNoticeHub() *NoticeHub                                       // :74
func (h *NoticeHub) BroadcastToUsers(userIDs []string, message NoticeMessage)   // :190
func (h *NoticeHub) BroadcastToAll(message NoticeMessage)                       // :233
func (h *NoticeHub) BroadcastRPAProgress(message RPAProgressMessage)            // :238
func (h *NoticeHub) BroadcastRPAProgressToUser(userID, message)                 // :252
func (h *NoticeHub) BroadcastRPAProgressToUsers(userIDs, message)               // :257
func (h *NoticeHub) encodeMessage(msg NoticeMessage) ([]byte, error)            // :301
```

From internal/services/base/service.go(61 stmts,14 cov):
```go
type GORMRepository[T any] struct                                  // :46
func NewGORMRepository[T any](db *gorm.DB) *GORMRepository[T]      // :51
func (r *GORMRepository[T]) Create/Update/Delete/GetByID/List/BatchDelete   // :56-159
func WrapError(err error, message string) error / IsNotFound(err) bool / IsDuplicate(err) bool // :146/:154/:159
// list_request.go ApplySort(:75)
```

From pkg/query/pagination.go(28 stmts 0%):
```go
func NewPaginatedResult(total int64, current, pageSize int, data interface{}) *PaginatedResult  // :16
func (p *PaginationRequest) Normalize() / GetOffset()              // :38/:51
func (p *ListParams) Normalize() / ApplyOrder(query) / ApplyPagination(query)  // :64/:79/:88
func NewDefaultQueryExecutor(current, pageSize int) *DefaultQueryExecutor      // :106
func (e *DefaultQueryExecutor) Execute(query *gorm.DB, dest) (*PaginatedResult, error) // :114
```

From pkg/gormutil/join_builder.go(194 stmts 包,123 cov;join_builder 链式 :89-209 未开):
```go
// 链式:Select/Where/Or/Order/Limit/Offset/Count/Scan/Find/First/Pluck/GetDB/Reset
// + BuildOnClause/BuildJoinClause/ParseSelectFields → sqlite 链式 + 断言生成 SQL
```

From pkg/logger/logger.go(79 stmts,55 cov;余 1 gap 但 hook 面 0%):
```go
func (h *fileMirrorHook) Levels() []logrus.Level    // :108
func (h *fileMirrorHook) Fire(entry *logrus.Entry) error  // :112
func fallbackToStdLogger()                          // :213
func Fatal(args ...interface{}) 系                  // :269 附近(直调会 os.Exit → 用 hook/落盘断言面替代,见 action)
```

From internal/services/lldp(96 stmts,57 cov;+11):
```go
func NewLLDPCache(ttl time.Duration) *LLDPCache      // lldp_cache.go:24(种子做 cache-hit)
func ClassifyPort(...)                                // port_classifier.go:35(纯,0%)
// lldp_service.go :31 起 cache-hit 分支;executor 错误分支
```

From pkg/middleware(609 stmts,419 cov;+8):
```go
// RequestDecryption(request_decryption.go 116 tot/47 cov,主函数 0%)
// ResponseEncryption(69 tot/12 cov,12.5%)
// OperLogMiddleware(55 tot/29 cov,0%)
// 真 RequestEncryptor:SM2 keypair 经 security.NewJWTManager(78-01 先例);timestamp 边界 ±window±1 表驱动(R6)
```

既有 analog(必读;新 helper 一律 8005 后缀):
- internal/core/captcha_78_01_test.go:37-140 — miniredis + NewRedisCache 装配先例
- internal/websocket/notice_hub_readpump_test.go:26-127 — 真 WS 对范式(server → Upgrader → DefaultDialer.Dial → defer 关闭)
- pkg/cache 既有测试(memory/retry/l2_writer 已高覆盖)勿重写,只补 redis.go/MultiLevel 缺口
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pkg/cache——miniredis 驱动 redis.go typed helpers + MultiLevelCache(D-80-05 重锚)</name>
  <files>pkg/cache/redis_80_05_test.go</files>
  <read_first>
  - pkg/cache/redis.go — :151-361(typed helpers 段)+ :519-578(构造器)+ :595-732/:884-992(MultiLevel 方法)
  - internal/core/captcha_78_01_test.go:37-140 — miniredis 装配先例
  - pkg/cache/redis.go CacheConfig 字段定义(以实际为准)
  </read_first>
  <action>
  创建 pkg/cache/redis_80_05_test.go(package cache)。helper `newRds8005(t *testing.T) (*RedisCache, *miniredis.Miniredis)`:miniredis.RunT(t)(自动 cleanup)+ NewRedisCache(&CacheConfig{Addr: mr.Addr()}, "")+ t.Cleanup(rc.Close 单次)。

  TestRds8005_ 前缀(typed helpers,每函数一正一负):
  - GetJSON/SetJSON round-trip + 坏 JSON 分支;GetInt/SetInt(含非数字值分支);GetBool/SetBool;MGetJSON/MSetJSON(部分缺失键语义);MDelete;Increment/IncrementBy(缺键=1 语义,QUIRK-01 既有修复的 DB 语义对照)/Decrement/DecrementBy;Exists(有/无);FlushDB(清后 Exists false);getClient/getPrefix 同包直调断言。
  - TTL:mr.FastForward 推进过期(R-1 纪律,禁裸 time.Sleep),断言过期后 miss。

  TestMlc8005_ 前缀(MultiLevelCache):
  - `TestMlc8005_Simple_Constructor`:NewMultiLevelCacheSimple(MemoryCache, redis)→ Get/Set/Delete/MGet/MSet/MDelete/Exists/Increment 全往返(优先此构造器,无后台 writer)。
  - `TestMlc8005_JSON_Helpers`:MGetJSON/MSetJSON/SetJSON/GetJSON 走 L1/L2 两级断言(写后直查 miniredis 原始键,证明 L2 落盘)。
  - `TestMlc8005_WithWriter_Close`:起 writer 的构造器(NewMultiLevelCacheWithWriter/WithRetryAndWriter)→ 显式 Close + t.Cleanup(79-R7 教训:worker goroutine 必须 收口)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestRds8005_|TestMlc8005_" ./pkg/cache/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestRds8005_\|^func TestMlc8005_" pkg/cache/redis_80_05_test.go` ≥ 10
  - `grep -c "miniredis.RunT" pkg/cache/redis_80_05_test.go` ≥ 1
  - TTL 推进零裸 time.Sleep(一律 mr.FastForward)
  - `go test -count=1 -run "TestRds8005_|TestMlc8005_" ./pkg/cache/` exit 0
  </acceptance_criteria>
  <commit>test(80-05): pkg/cache miniredis typed helpers + MultiLevelCache 收口(+49 重锚)</commit>
  <done>redis.go 31.2% → ≥70%;pkg/cache 包 ≥70%(D-80-05 重锚口径,基线 64.7%)。</done>
</task>

<task type="auto">
  <name>Task 2: pkg/permission——service.go 81 stmts 全 0% 清欠(8 包中最大余量 +50)</name>
  <files>pkg/permission/service_80_05_test.go</files>
  <read_first>
  - pkg/permission/service.go — :25-300 全文(Init/角色菜单/角色部门/GetUserMenus/buildMenuTree)
  - internal/models — Role/Menu 模型字段(AutoMigrate 用)
  </read_first>
  <action>
  创建 pkg/permission/service_80_05_test.go。helper `newPms8005(t *testing.T) (*service, *gorm.DB)`:sqlite(t.TempDir)+ AutoMigrate(models.Role/models.Menu 及 service 引用的关联表 sys_role_menu/sys_user_role/sys_role_dept —— 以 :30-275 实际引用为准)+ NewService()(具体类型断言 `s := NewService().(*service)` 或接口方法直调,以可行为准)。

  前缀 TestPms8005_:
  - `TestPms8005_InitDefaultRolesAndMenus`:空库 → Init → 断言默认角色与菜单落库(行数/关键角色码);幂等:二次 Init 不重复不报错。
  - `TestPms8005_CreateDefaultAdminRole`:空库直调 createDefaultAdminRole(同包)→ admin 角色行;已存在 → 跳过分支。
  - `TestPms8005_AssignAllMenusToAdmin`:种子菜单 → assignAllMenusToAdmin → sys_role_menu 关联数 == 菜单数。
  - `TestPms8005_RoleMenus_RoundTrip`:UpdateRoleMenus(3 菜单)→ GetRoleMenus 返回一致;Update 为空集 → 清空;Get 不存在角色 → 空集/错误分支。
  - `TestPms8005_RoleDepts_RoundTrip`:同上(GetRoleDepts/UpdateRoleDepts)。
  - `TestPms8005_GetUserMenus`:种子用户-角色-菜单链 → includeHidden true/false 两态(models.VisibleShow/VisibleHidden 常量断言可见性)+ buildMenuTree 树形断言(父子嵌套 + 空 parentID 根集合)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestPms8005_" ./pkg/permission/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestPms8005_" pkg/permission/service_80_05_test.go` ≥ 6
  - Init/createDefaultAdminRole/assignAllMenusToAdmin/Get·UpdateRoleMenus 全部有用例(service.go 81 stmts 主缺口)
  - `go test -count=1 -run "TestPms8005_" ./pkg/permission/` exit 0
  </acceptance_criteria>
  <commit>test(80-05): permission service 默认角色菜单/角色菜单往返测试(sqlite)</commit>
  <done>pkg/permission 26.3% → ≥70%(+50 余量清欠)。</done>
</task>

<task type="auto">
  <name>Task 3: internal/websocket——Broadcast 群真 WS 对(+45,R5 时序纪律)</name>
  <files>internal/websocket/notice_hub_broadcast_80_05_test.go</files>
  <read_first>
  - internal/websocket/notice_hub.go — :74-120(NewNoticeHub/Run)+ :190-308(Broadcast 群 + encodeMessage)
  - internal/websocket/notice_hub_readpump_test.go:26-127 — WS harness 范式(照抄关闭顺序)
  </read_first>
  <action>
  创建 internal/websocket/notice_hub_broadcast_80_05_test.go。helper `newHubWithClients8005(t *testing.T, n int) (*NoticeHub, []*websocket.Conn, func())`:httptest.NewServer + gorilla Upgrader → hub 直注 client(或经服务端 handler 升级,以 readpump 范式为准)→ n 个 DefaultDialer.Dial 客户端;返回收口函数(server shutdown → conn close → hub Stop,顺序照 readpump)。

  前缀 TestNhb8005_(断言一律 Eventually 非.sleep,R5):
  - `TestNhb8005_BroadcastToAll`:2 客户端 → BroadcastToAll(msg)→ Eventually 两客户端各收到 1 帧(encodeMessage 产物,JSON 字段断言)。
  - `TestNhb8005_BroadcastToUsers`:目标/非目标用户各一 → 只有目标收到(userIDs 匹配分支);空 userIDs → 无人收到。
  - `TestNhb8005_BroadcastRPAProgress_Trio`:BroadcastRPAProgress / ToUser / ToUsers 三方法各一用例(RPAProgressMessage 字段透传断言)。
  - `TestNhb8005_EncodeMessage`:encodeMessage 合法 msg → JSON 可解析;不可序列化 msg → 错误分支(按 :301 实现)。
  - `TestNhb8005_Broadcast_DisconnectedClient`:一个已断开的 client + 一个在线 → 广播不 panic、在线者仍收到(死连接清理分支,以实现为准)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestNhb8005_" ./internal/websocket/ && go test -race -count=1 -run "TestNhb8005_" ./internal/websocket/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestNhb8005_" internal/websocket/notice_hub_broadcast_80_05_test.go` ≥ 5
  - Broadcast 五方法 + encodeMessage 全部触达;断言用 Eventually(grep 无裸 sleep 轮询)
  - -race exit 0(R5 关键验收)
  - `go test -count=1 -run "TestNhb8005_" ./internal/websocket/` exit 0
  </acceptance_criteria>
  <commit>test(80-05): websocket Broadcast 群真 WS 对测试(Eventually + race)</commit>
  <done>internal/websocket 35.7% → ≥70%(+45);五广播方法与编码全清。</done>
</task>

<task type="auto">
  <name>Task 4: base repository + query pagination——泛型 CRUD sqlite + 分页数学</name>
  <files>internal/services/base/base_80_05_test.go, pkg/query/pagination_80_05_test.go</files>
  <read_first>
  - internal/services/base/service.go — :46-159(GORMRepository 泛型 + WrapError/IsNotFound/IsDuplicate)+ list_request.go ApplySort(:75)
  - pkg/query/pagination.go — :16-130 全文
  </read_first>
  <action>
  base_80_05_test.go(前缀 TestBas8005_;测试内嵌 model:`type basRepoRow8005 struct { models.BaseModel; Name string; Age int }`,sqlite AutoMigrate):
  - `TestBas8005_CRUD_RoundTrip`:Create → GetByID → Update → Delete 全链;GetByID 不存在 → IsNotFound 语义。
  - `TestBas8005_List_Paged`:种子 5 行 → List 分页(page/pageSize 边界 + ApplySort 升降序)。
  - `TestBas8005_BatchDelete`:3 ID 批删 → 剩余计数;空切片分支。
  - `TestBas8005_ErrorHelpers`:WrapError 包装 + IsNotFound(gorm.ErrRecordNotFound 与非 nil 别的错)/ IsDuplicate(唯一冲突 sqlite 错误映射,以 :159 实现为准)。

  pagination_80_05_test.go(前缀 TestPag8005_;全纯为主):
  - `TestPag8005_Normalize_Offset`:PaginationRequest Normalize(0/负值/超大 pageSize 默认化)+ GetOffset 数学(current=3,pageSize=10 → 20,手算注释)。
  - `TestPag8005_ListParams_Order_Pagination`:ApplyOrder(合法字段/非法字段回退)+ ApplyPagination(sqlite 断言 LIMIT/OFFSET 生效行数)。
  - `TestPag8005_NewPaginatedResult`:零 total/空 data/正常三态字段映射。
  - `TestPag8005_Executor_Execute`:NewDefaultQueryExecutor + sqlite 种子 → Execute 返回 PaginatedResult(total/list/current/pageSize 四字段,分页约定断言)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestBas8005_" ./internal/services/base/ && go test -count=1 -run "TestPag8005_" ./pkg/query/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestBas8005_" internal/services/base/base_80_05_test.go` ≥ 4;`grep -c "^func TestPag8005_" pkg/query/pagination_80_05_test.go` ≥ 4
  - internal/services/base ≥70%(基线 23.0%);pkg/query ≥70%(基线 67.6%)
  - 两个包定向测试 exit 0
  </acceptance_criteria>
  <commit>test(80-05): base 泛型 repository + query pagination 测试(sqlite + 纯数学)</commit>
  <done>internal/services/base 23.0% → ≥70%;pkg/query 67.6% → ≥70%。</done>
</task>

<task type="auto">
  <name>Task 5: gormutil join_builder + logger hook + lldp 纯函数与 cache-hit</name>
  <files>pkg/gormutil/join_builder_80_05_test.go, pkg/logger/logger_hook_80_05_test.go, internal/services/lldp/lldp_tail_80_05_test.go</files>
  <read_first>
  - pkg/gormutil/join_builder.go — :89-209(链式方法 + BuildOnClause/BuildJoinClause/ParseSelectFields)
  - pkg/logger/logger.go — :108-137(fileMirrorHook)+ :213 fallbackToStdLogger + Fatal 系(:269 附近)
  - internal/services/lldp/port_classifier.go:35(ClassifyPort)+ lldp_service.go:31 起(cache-hit)+ lldp_cache.go:24(NewLLDPCache)
  </read_first>
  <action>
  join_builder_80_05_test.go(前缀 TestJbt8005_;sqlite(t.TempDir)种子表):
  - 链式全方法表驱动:Select/Where/Or/Order/Limit/Offset/Count/Find/First/Pluck/GetDB/Reset → 每方法断言生成 SQL 片段或行为(ToSQL/DryRun 或行数断言,以实现的可观察面为准);BuildOnClause/BuildJoinClause 直接构造子句断言 SQL 文本;ParseSelectFields(合法/空/别名分支)。

  logger_hook_80_05_test.go(前缀 TestLgr8005_):
  - `TestLgr8005_Hook_Fire_Levels`:fileMirrorHook Levels() 返回集断言;Fire(entry) 落镜像 writer(构造 hook 时注入内存 writer 或临时文件 t.TempDir)→ 写一条 → 断言落盘内容;Fire 对关闭的 writer → 错误分支(按 :112 实现)。
  - `TestLgr8005_FallbackToStdLogger`:直调 fallbackToStdLogger()(:213)→ 不 panic、后续日志走 std(logger 状态断言以实现为准)。
  - Fatal 系:直接调 Fatal 会 os.Exit —— 不直调;改为断言其构建路径或以实现的内部函数为锚(若 Fatal 体不可拆,该分支按 D-80-04 豁免条目文档化,禁止为测它改生产代码)。

  lldp_tail_80_05_test.go(前缀 TestLdp8005_):
  - `TestLdp8005_ClassifyPort_Table`:纯函数表驱动(各类端口形态 → 分类值,期望值手写注释)。
  - `TestLdp8005_Service_CacheHit`:NewLLDPCache(ttl) 预种子 → lldp_service 查询走 cache-hit 分支(:31 起;命中/未命中两态,第二次调用命中)。
  - `TestLdp8005_Executor_ErrorBranch`:executor 错误分支(构造失败输入 → 错误上抛,以实现为准)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestJbt8005_" ./pkg/gormutil/ && go test -count=1 -run "TestLgr8005_" ./pkg/logger/ && go test -count=1 -run "TestLdp8005_" ./internal/services/lldp/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestJbt8005_" pkg/gormutil/join_builder_80_05_test.go` ≥ 3;`grep -c "^func TestLgr8005_" pkg/logger/logger_hook_80_05_test.go` ≥ 2;`grep -c "^func TestLdp8005_" internal/services/lldp/lldp_tail_80_05_test.go` ≥ 3
  - pkg/gormutil ≥70%(基线 63.4%);pkg/logger ≥70%(基线 69.1%);internal/services/lldp ≥70%(基线 59.4%)
  - Fatal 分支若豁免:已落注释与(后续 Task 7)SUMMARY
  - 三个包定向测试 exit 0
  </acceptance_criteria>
  <commit>test(80-05): gormutil 链式 SQL + logger hook + lldp 纯函数与 cache-hit</commit>
  <done>pkg/gormutil / pkg/logger / internal/services/lldp 三包 ≥70%。</done>
</task>

<task type="auto">
  <name>Task 6: pkg/middleware——RequestDecryption/ResponseEncryption/OperLogMiddleware(httptest + 真加密器)</name>
  <files>pkg/middleware/encryption_80_05_test.go</files>
  <read_first>
  - pkg/middleware/request_decryption.go(116 tot/47 cov,主函数 0%)/ response_encryption 同族(69/12)/ oper_log 中间件(55/29)
  - pkg/crypto/request_encryption.go — RequestEncryptor 构造与 SM2+SM4 载荷形态(pkg/crypto 自身测试的 keypair 生成手法优先参照)
  - internal/core/captcha_78_01_test.go — security.NewJWTManager 产 SM2 keypair 先例
  </read_first>
  <action>
  创建 pkg/middleware/encryption_80_05_test.go。前缀 TestMdw8005_:

  - 装配:真 RequestEncryptor(SM2 keypair 经 security.NewJWTManager 或 pkg/crypto 自测试的生成手法,78-01 先例);sqlite(t.TempDir)供 oper_log 中间件的 DB 配置分支。
  - `TestMdw8005_RequestDecryption_RoundTrip`:客户端按 SM2+SM4 混合封装请求体(正键/时间戳/nonce)→ 中间件解密 → handler 收到明文;响应回包断言(以 :1-116 实现的封装字段为准)。
  - `TestMdw8005_RequestDecryption_Replay_Table`:timestamp 边界表驱动 —— 窗口内通过 / window+1 拒绝 / window-1 通过(R6:不依赖墙时钟等待,直接构造 ±window±1 时间戳);nonce 重放 → 拒绝分支。
  - `TestMdw8005_RequestDecryption_BadPayload`:坏 SM2 密文 / 缺字段 / 非法 nonce → 各错误分支(exclude_paths 命中路径 → 直通分支)。
  - `TestMdw8005_ResponseEncryption`:enabled 时响应体为 SM4 密文(客户端可解)/ disabled 时明文直通;writeBuffer 缓冲语义以 flush 后可读为断言面(R6 边界:勿断言未 flush 的中间态)。
  - `TestMdw8005_OperLogMiddleware`:httptest 请求穿过中间件 → oper_log 落库(sqlite 行断言;敏感词脱敏若在该中间件内则断言 `******`,以实现为准)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestMdw8005_" ./pkg/middleware/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestMdw8005_" pkg/middleware/encryption_80_05_test.go` ≥ 5
  - timestamp ±window±1 表驱动存在(零墙时钟 sleep)
  - pkg/middleware ≥70%(基线 68.8%,+8 余量)
  - `go test -count=1 -run "TestMdw8005_" ./pkg/middleware/` exit 0
  </acceptance_criteria>
  <commit>test(80-05): middleware 加解密与操作日志中间件测试(真加密器 + 重放表)</commit>
  <done>RequestDecryption 主函数 0% 清起;ResponseEncryption/OperLogMiddleware 补齐;8 包 sweep 全部 ≥70%。</done>
</task>

<task type="auto">
  <name>Task 7: phase 收口 — 14 包逐包复测验收表(SC-1/SC-2/SC-3)+ 豁免汇总 + gate</name>
  <files>.planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-05-SUMMARY.md</files>
  <read_first>
  - .planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-RESEARCH.md §1.1 — 包级目标表(+2094 总账)
  - .planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-0{1,2,3,4}-SUMMARY.md — 各 plan 实测数字(逐包填表输入)
  </read_first>
  <action>
  收口四步(这是 research §7 指定的 SC 验收动作):

  1. **Phase 80 全部 14 包逐包复测**(每包只跑一次,`-count=1 -coverprofile=cov80_05_<pkg>.out` 后即删):
     internal/scheduler、internal/api/v1、internal/models、internal/api、pkg/errors、pkg/cache、pkg/middleware、pkg/gormutil、pkg/permission、internal/websocket、pkg/query、internal/services/lldp、pkg/logger、internal/services/base(scheduler 之外的包按本 plan 与前序 plan 的 SC 数字交叉核对)。
     产出表:pkg | tot | covered | % | 基线 % | gap(+NN)→ 实际增量 | 达标(Y/N)。
  2. **SC 判定**:SC-1 scheduler ≥70%;SC-2 碎包逐包 ≥70%(api/v1/models/internal/api/pkg/errors/pkg/cache);SC-3 8 包合计 ≥70%(D-80-04 修正口径);总账:phase 覆盖增量 ≥ +2094(逐包实际增量求和)。未达标包 → 回对应 plan 的 task 补(本 plan 只做验收,不做包内补缺,除非缺口在本 plan 自己的 9 包)。
  3. **豁免汇总**:D-80-06(syncADConfig wire)+ D-80-04 候选(l2_writer drainQueue/logger Fatal/middleware 极端缓冲分支)+ 执行中新发现项 —— 逐条:包/文件/stmt 数/原因/规则依据。禁止静默豁免。
  4. **gate + 文档**:`go build ./...` + `go test ./...` + 全仓 `-race` 抽样(scheduler/websocket 两包全量 -race 已在各自 plan 收口);SUMMARY 落验收表 + 口径修正声明(D-80-04/D-80-05)+ coverage-baseline.md 全仓回填归 Phase 81(81-01)的边界声明(本 plan 不改 baseline 文件)。
  </action>
  <verify>
    <automated>go test -count=1 ./pkg/cache/ ./pkg/permission/ ./internal/websocket/ ./internal/services/base/ ./pkg/query/ ./pkg/gormutil/ ./pkg/logger/ ./internal/services/lldp/ ./pkg/middleware/ && go build ./... && go test ./... 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
  - 8 个小包 + pkg/cache 逐包 ≥70%(数字落 SUMMARY 验收表);8 包合计 ≥70%(SC-3)
  - phase 覆盖增量总账 ≥ +2094(逐包求和,SUMMARY 呈现)
  - 豁免条目零静默(每条有 stmt 数与原因)
  - `go build ./...` + `go test ./...` exit 0;cov*.out 全部删除;coverage-baseline.md 未被本 plan 改动(归 Phase 81)
  </acceptance_criteria>
  <commit>docs(80-05): phase 收口——14 包逐包验收表 + 豁免汇总 + TAIL-02/03 达标声明</commit>
  <done>TAIL-02/SC-1/SC-2/SC-3 与 SC-4 gate 全程绿;phase +2094 总账关门;验收表交 Phase 81 审计。</done>
</task>

</tasks>

<notes>
- 依赖:80-01..80-04 全部完成(逐包复测需要它们的最终数字);与各 plan 零文件交集。
- 被依赖:Phase 81(81-01 全量重测 + threshold ratchet + baseline 回填)消费本 plan 的验收表;本 plan 不改 coverage-baseline.md。
- miniredis 纪律:只连 mr.Addr();TTL 一律 mr.FastForward;MultiLevel 起 worker 的构造器必须 Close + t.Cleanup(79-R7)。
- WS 纪律(R5):readpump 关闭顺序;Eventually 断言;收口 -race。
- 加解密纪律(R6):timestamp ±window±1 表驱动;writeBuffer 只断言 flush 后可读;真加密器不从环境读生产 key。
- 豁免纪律(D-80-04):逐条 stmt 数 + 原因落 SUMMARY;候选明确列出但"确不可测"才成立,能测则测。
- quirk 纪律:零生产代码改动(D-80 escape hatch 仅限源码-vs-文档错配)。
</notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 ↔ Redis | 仅 miniredis.RunT 进程内实例(mr.Addr()),禁任何真实 Redis 地址字面量 |
| 测试 ↔ 数据库 | glebarez sqlite(t.TempDir 文件库),不触碰生产 PG |
| 测试 ↔ 网络出口 | WS 仅 httptest 回环;加解密 keypair 进程内生成;零外呼 |
| 测试 ↔ 加密面 | 测试用自生成 SM2/SM4 keypair;断言不落盘明文敏感值 |
| 测试 ↔ 生产代码 | 仅新增 *_80_05_test.go;生产 .go 改动 = 0(D-80 escape hatch 除外) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-05-01 | Information Disclosure | 加解密测试泄露密钥/明文到日志 | mitigate | 断言只比字段;不 t.Log 密文/明文;keypair 进程内生成 |
| T-80-05-02 | DoS | MultiLevelCache worker / hub goroutine 泄漏 | mitigate | Close + t.Cleanup(79-R7);readpump 关闭顺序;-race 收口 |
| T-80-05-03 | Repudiation | oper_log 中间件测试绕过脱敏断言 | mitigate | 断言敏感词 `******`(若脱敏在该中间件);落库行断言 |
| T-80-05-04 | Tampering | 豁免被滥用为"跳过难测" | mitigate | D-80-04:逐条 stmt 数 + 原因 + SUMMARY 公示;能测则测 |
| T-80-05-SC | Tampering | 依赖安装 | accept | 零新增依赖(miniredis v2.38.0/sqlite/testify/gorilla 全既有) |
</threat_model>

<verification>
- `go test -count=1` 对本 plan 9 个包全绿;pkg/cache 与 websocket -race 绿
- `go tool cover -func`:pkg/cache ≥70%(D-80-05 口径);8 小包逐包 ≥70% 且合计 ≥70%(SC-3)
- phase 逐包验收表落 SUMMARY(SC-1/SC-2/SC-3 全核对,总账 ≥ +2094)
- `go build ./...` + `go test ./...` exit 0;生产 .go 改动 = 0 文件
</verification>

<success_criteria>
1. 9 个新测试文件存在且全绿:TestRds8005_/TestMlc8005_ ≥10、TestPms8005_ ≥6、TestNhb8005_ ≥5、TestBas8005_ ≥4、TestPag8005_ ≥4、TestJbt8005_ ≥3、TestLgr8005_ ≥2、TestLdp8005_ ≥3、TestMdw8005_ ≥5
2. pkg/cache ≥70%(redis.go 31.2% → ≥70%,D-80-05 重锚);8 小包逐包 ≥70%(middleware 68.8/gormutil 63.4/permission 26.3/websocket 35.7/query 67.6/lldp 59.4/logger 69.1/base 23.0 → 全 ≥70%)
3. SC-3 8 包合计 ≥70%(D-80-04 修正口径,+156 余量清欠)
4. phase 逐包验收表 + 豁免汇总 + 总账(≥ +2094)落 SUMMARY;coverage-baseline.md 归 Phase 81
5. `go build ./...` + `go test ./...` exit 0;每 task 一原子 commit
</success_criteria>

<decision_audit>
本 plan 涉及的 phase-80 决策(executor 必须遵守):

| ID | 决策 | 依据 / 理由 |
|----|------|------------|
| D-80-04 | SC-3 口径修正 = research DQ4:ROADMAP『<50 stmts 小尾巴』实为 8 个点名包(pkg/middleware 609 stmts 并非 <50 stmts;原意应为 v1.26 扫描时 <50% 覆盖的包);执行口径 = 『ROADMAP 点名的 8 包(middleware/permission/websocket/base/lldp/gormutil/query/logger)合计 ≥70%』+ 逐包豁免文档化(l2_writer drainQueue / logger Fatal / middleware 极端分支等,逐条 stmt 数 + 原因);计划阶段确认,无需用户裁决(语义唯一合理解读,research 原判) | research §1.8 + DQ4 + R8;VERIFICATION 按"8 包点名 + 修正后 gap"验收 |
| D-80-05 | pkg/cache 缺口重锚 +161 → +49:Phase 76/78 已把包推到 64.7%(924 tot/598 cov),80-05 的 cache 切片 = miniredis typed helpers;省出的 ~112 stmts 容量拨给 8 小包 sweep;SC 数字按重锚口径验收 | research §1.7 + DQ5 + §0.1 实测(ROADMAP 数字过时);R8 争议预防 |
| D-80-06(沿用) | wire 豁免口径已在 80-02 定案;本 plan 收口时汇总其条目 | 80-02 decision_audit |
| D-80-07(沿用) | 命名 `<source-or-topic>_80_05_test.go`;helper 8005 后缀 | D-78-08 / D-79-06 同构 |
| (沿用) | MultiLevel worker 构造器 Close + t.Cleanup | 79-R7 教训 |
| (沿用) | WS Eventually / TTL FastForward / ±window±1 表驱动 | local-vs-ci 记忆 + research R5/R6 |
| (沿用) | coverage-baseline.md 全仓回填归 Phase 81(81-01),本 plan 只落 phase 范围数字 | ROADMAP Phase 81 定义(避免双写冲突) |

基线数字来源:80-RESEARCH.md §1.7/§1.8(2026-08-28 实测;pkg/cache 924/598/64.7%,8 小包 1387/815/58.8% agg,+156)。
</decision_audit>

<output>
Create `.planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-05-SUMMARY.md` when done
</output>
