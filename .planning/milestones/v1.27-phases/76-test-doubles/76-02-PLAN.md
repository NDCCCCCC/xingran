---
phase: 76-test-doubles
plan: 02
type: execute
wave: 2
depends_on: ["76-01"]
files_modified:
  - internal/device/scrapli_wrapper.go
  - internal/device/driver_factory_76_02_test.go
  - internal/device/testdata/huawei_vrp_open.fixture
autonomous: true
requirements: [INFRA-02]
must_haves:
  truths:
    - "internal/device/scrapli_wrapper.go 存在包级工厂 var newNetworkDriver，NewScrapliWrapper 与 NewScrapliWrapperWithPort 都经它构造 driver，错误字符串 byte 不变"
    - "现有 internal/device 测试全绿（生产路径行为不变）"
    - "测试可临时覆盖 newNetworkDriver 注入 FileTransport，经 NewScrapliWrapper→Open→SendCommand 全链驱动 fixture 回放"
    - "覆盖必须 t.Cleanup 恢复且测试禁 t.Parallel()"
  artifacts:
    - path: "internal/device/scrapli_wrapper.go"
      provides: "newNetworkDriver 工厂 var（唯一生产改动）"
      contains: "var newNetworkDriver"
    - path: "internal/device/driver_factory_76_02_test.go"
      provides: "FileTransport 注入演示测试"
      min_lines: 50
    - path: "internal/device/testdata/huawei_vrp_open.fixture"
      provides: "Open 场景 prompt fixture（LF、dummy 值）"
  key_links:
    - from: "NewScrapliWrapper / NewScrapliWrapperWithPort"
      to: "var newNetworkDriver"
      via: "d, err := newNetworkDriver(platformName, device.IPAddress, opts...)"
      pattern: "newNetworkDriver\\(platformName"
    - from: "driver_factory_76_02_test.go"
      to: "options.WithTransportType(transport.FileTransport)"
      via: "覆盖工厂 var 后 NewScrapliWrapper 构造 → Open() 读 fixture 首个 prompt"
      pattern: "WithTransportType\\(transport\\.FileTransport\\)"
---

<objective>
INFRA-02 落地：把 scrapli_wrapper.go 两个构造器尾部完全重复的 platform→driver 构造段抽取为包级工厂 var newNetworkDriver，为 Phase 78 BLOCK-04（device ≥70% 的 FileTransport/fake SSH 注入）交付工厂入口。

Purpose: 工厂 var 方案对 connection_pool.createConnection 自动生效且零调用方改动；生产路径行为与错误字符串完全不变是硬约束（本 phase 只交付入口，fake SSH server 实现留 Phase 78）。
Output: 1 个生产文件小重构 + 1 个注入演示测试 + 1 个 testdata fixture。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/76-test-doubles/76-RESEARCH.md
@.planning/workstreams/milestone/phases/76-test-doubles/76-PATTERNS.md
@.planning/workstreams/milestone/phases/76-test-doubles/76-01-SUMMARY.md

<interfaces>
<!-- 执行者需要的既有契约（已本地复核） -->

From internal/device/scrapli_wrapper.go:
```go
func NewScrapliWrapper(device *models.NetworkDevice, username, password string, protocolType models.ProtocolType) (*ScrapliWrapper, error)          // :111，重复段 :148-162
func NewScrapliWrapperWithPort(device *models.NetworkDevice, username, password string, port int, protocolType models.ProtocolType) (*ScrapliWrapper, error)  // :174，重复段 :212-226
func (w *ScrapliWrapper) Open() error            // :301
func (w *ScrapliWrapper) OpenContext(ctx context.Context) error  // :320（GetPrompt 轮询 :382-392）
// 重复段两处错误文案："创建平台实例失败: %w" 与 "获取网络驱动失败: %w" —— 抽取后必须 byte 不变
// imports 已含 platform/options/transport/util/network（:12-17），工厂 var 零新 import
```

From internal/services/portwrite/port_write_e2e_test.go（FileTransport 已验证先例）:
```go
options.WithTransportType(transport.FileTransport)   // :71-90 选项组合
options.WithFileTransportFile(fixturePath)
options.WithTransportReadSize(1)
options.WithReadDelay(0)
// :42-48 注释原文：fixture 耗尽后 d.Close() 会 block —— 测试禁 Close
// :106-113 e2eFixturePath：runtime.Caller 定位 testdata（防 cwd 漂移）
```

From scrapligo v1.4.0（本地 module 源码已核实）:
```go
platform.NewPlatform(f interface{}, host string, opts ...util.Option) (*Platform, error)   // platform/definition.go:190
func (p *Platform) GetNetworkDriver() (*network.Driver, error)                              // platform/definition.go:296
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 抽取 newNetworkDriver 工厂 var（唯一生产改动）</name>
  <files>internal/device/scrapli_wrapper.go</files>
  <read_first>internal/device/scrapli_wrapper.go（全文，重点 :93-235 构造器与重复段）、internal/device/connection_pool.go（:393-482 createConnection 调用构造器的路径）</read_first>
  <action>
  在 scrapli_wrapper.go 增加包级工厂：`var newNetworkDriver = func(platformName, host string, opts ...util.Option) (*network.Driver, error)`，函数体内依次 platform.NewPlatform(platformName, host, opts...) 与 p.GetNetworkDriver()。错误包装的落点决策（保证错误字符串 byte 不变）：把现有两段 fmt.Errorf（"创建平台实例失败: %w" / "获取网络驱动失败: %w"）**移入工厂内部**——工厂内 NewPlatform 失败包装前者、GetNetworkDriver 失败包装后者；两个构造器调用点改为 `d, err := newNetworkDriver(platformName, device.IPAddress, opts...)`，err 非 nil 时原样 return nil, err（不再二次包装）。工厂 var 上方写明注释：生产路径行为与内联调用完全一致；测试通过临时替换该 var 注入 FileTransport/自定义 transport，替换必须 t.Cleanup 恢复且所在测试禁止 t.Parallel()。范围红线：不要顺手改 platformIdentifier（:93-98 锐捷 patched YAML 逻辑）、Telnet/SSH 分支、connection_pool.go（后者经构造器零改动自动获益）。完成后立即 `go build ./...`。
  </action>
  <verify>
    <automated>grep -n "var newNetworkDriver" internal/device/scrapli_wrapper.go && grep -c "创建平台实例失败" internal/device/scrapli_wrapper.go && go build ./... && go test -count=1 ./internal/device/</automated>
  </verify>
  <acceptance_criteria>
  - scrapli_wrapper.go 含 `var newNetworkDriver =` 定义，且 "创建平台实例失败" 与 "获取网络驱动失败" 两处错误字符串仍在（位于工厂内部），byte 不变
  - `platform.NewPlatform(` 在 scrapli_wrapper.go 中仅出现 1 次（工厂内；原先 2 次直调）
  - NewScrapliWrapper 与 NewScrapliWrapperWithPort 函数体内均含 `newNetworkDriver(platformName, device.IPAddress, opts...)` 调用
  - `go build ./...` exit 0；`go test -count=1 ./internal/device/` 现有测试全绿（生产路径不变实证）
  </acceptance_criteria>
  <done>工厂 var 落地，两个构造器经工厂构造 driver，错误字符串与现有测试零变化</done>
</task>

<task type="auto">
  <name>Task 2: FileTransport 注入演示测试 + testdata fixture</name>
  <files>internal/device/driver_factory_76_02_test.go, internal/device/testdata/huawei_vrp_open.fixture</files>
  <read_first>internal/services/portwrite/port_write_e2e_test.go（:42-48 禁 Close 注释原文 / :71-90 FileTransport 选项组合 / :106-113 fixture 定位）、internal/device/scrapli_wrapper.go（Open/SendCommand 方法签名）、internal/device/e2e_helpers.go（同包先例风格）</read_first>
  <action>
  新建 internal/device/testdata/huawei_vrp_open.fixture（LF 行尾，dummy 值）：首行为确定性 prompt `<dummy-host>`（匹配 huawei_vrp prompt 模式），随后为一段 display version 风格输出（如 "Huawei Versatile Routing Platform" 两三行）并以 `<dummy-host>` 收尾——Open 读首个 prompt 即成功，SendCommand 读到下一个 prompt 返回响应。新建 internal/device/driver_factory_76_02_test.go（package device，同包白盒）：fixture 路径用 runtime.Caller(0) 派生（照 port_write_e2e_test.go:106-113 的 e2eFixturePath 模式，指向本包 testdata）。注入纪律（顺序写法）：先 `orig := newNetworkDriver` + `t.Cleanup(func() { newNetworkDriver = orig })`，再覆盖 newNetworkDriver 为闭包——闭包内 platform.NewPlatform("huawei_vrp", "dummy-host", options.WithTransportType(transport.FileTransport), options.WithFileTransportFile(fixturePath), options.WithTransportReadSize(1), options.WithReadDelay(0)) + GetNetworkDriver()。随后走真实生产构造链：NewScrapliWrapper(&models.NetworkDevice{IPAddress: "dummy-host", ...最小必填字段}, "u", "p", models.ProtocolTypeSSH) → 断言 err==nil → w.Open() 断言 err==nil → w 的命令发送方法（以 scrapli_wrapper.go 实际签名为准，SendCommand 系）断言 err==nil 且响应文本 TrimSpace 后非空/Contains "Huawei"。纪律红线：禁 t.Parallel()；**禁 driver/wrapper Close**（fixture 耗尽后 close 路径读字节会 block，照 port_write_e2e_test.go:42-48 注释原文写明）；覆盖动作与断言之间不得用 t.Fatal（用 t.Errorf / 先构造后覆盖，防 Cleanup 前退出）。
  </action>
  <verify>
    <automated>go test -count=1 -run 'TestDriverFactory' ./internal/device/ -v && go test -count=1 ./internal/device/</automated>
  </verify>
  <acceptance_criteria>
  - driver_factory_76_02_test.go 含 t.Cleanup 恢复 newNetworkDriver 的写法，且 grep 不到 "t.Parallel()"
  - 测试经 NewScrapliWrapper 公开构造器（非 e2e_helpers 的 ForTesting 捷径）驱动 Open 与命令发送
  - internal/device/testdata/huawei_vrp_open.fixture 存在且首行为 `<dummy-host>`
  - `go test -count=1 ./internal/device/` 全绿（含注入演示与既有测试）
  </acceptance_criteria>
  <done>工厂注入能力被测试实证：FileTransport 经生产构造链驱动 Open/SendCommand，Phase 78 可直接照搬</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 生产代码 ↔ 测试注入 | 包级 var newNetworkDriver 是唯一注入点；生产默认实现必须与原内联行为一致 |
| fixture 文件 | 测试进程内读取，全 dummy 值 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-02-01 | Tampering（生产行为漂移） | scrapli_wrapper.go 重构 | mitigate | 错误字符串 byte 不变的硬约束 + 现有 device 测试全绿门 + platform.NewPlatform 直调计数验收（2→1） |
| T-76-02-02 | Elevation（注入缝被生产滥用） | newNetworkDriver var | accept | var 为小写包私有，生产代码无法跨包触达；ForTesting 类防误用由 76-05 AST 守护兜底 |
| T-76-02-03 | Information Disclosure（fixture 凭据泄漏） | testdata fixture | mitigate | 全 dummy 值（dummy-host/u/p），禁真实设备 IP/凭据；fixture 保持 LF（.gitattributes 强制） |
| T-76-02-04 | DoS（测试挂起） | FileTransport Close | mitigate | 禁 Close 纪律（portwrite 先例：fixture 耗尽后 close 读字节 block），测试注释成文 |
</threat_model>

<verification>
- `grep -n "var newNetworkDriver" internal/device/scrapli_wrapper.go` 命中
- `go build ./...` exit 0
- `go test -count=1 ./internal/device/` 全绿（现有测试 + TestDriverFactory 注入演示）
- `go test -race -count=1 ./internal/device/`（可选抽查：工厂 var 覆盖的并发安全）
</verification>

<success_criteria>
- INFRA-02 完成：工厂入口交付、生产路径零行为变化（现有测试全绿 + 错误字符串不变）、注入能力被演示测试实证
- 与 76-01 的关系：仅排序依赖（wave 2 隔离 go.mod 写入窗口），无内容依赖
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/76-test-doubles/76-02-SUMMARY.md` when done
</output>
