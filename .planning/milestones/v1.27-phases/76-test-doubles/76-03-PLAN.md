---
phase: 76-test-doubles
plan: 03
type: execute
wave: 2
depends_on: ["76-01"]
files_modified:
  - internal/services/addomain/ldap_iface.go
  - internal/services/addomain/ldap_client_mock_test.go
  - internal/services/addomain/failover_client.go
  - internal/services/addomain/group.go
  - internal/services/addomain/group_management_service.go
  - internal/services/addomain/group_sync_service.go
  - internal/services/addomain/user.go
  - internal/services/addomain/user_ad_sync_service.go
  - internal/services/addomain/dept_sync_service.go
  - internal/services/addomain/sync.go
  - internal/services/addomain/failover_client_76_03_test.go
  - internal/scheduler/dept_sync_tasks.go
autonomous: true
requirements: [INFRA-03]
must_haves:
  truths:
    - "LDAPClientIface 扩展为 19 个方法（既有 16 + UpdateGroupAttribute/CreateOU/DNExists），*LDAPClient 与 *mockLDAPClient 双侧编译期断言通过"
    - "ExecuteWithFailover 的 operation 参数类型为 LDAPClientIface，24 处类型行全部机械替换——20 处闭包 + 4 个辅助函数签名（syncDeptTree/moveUserToNewOU/syncUserAttributes/moveSingleUserToOU），仅参数类型，闭包体/函数体零改动"
    - "FailoverClient 经 clientFactory 字段注入 mock 可驱动顺序遍历与 maxHops 封顶断言，零真实网络"
    - "PickFirstConnect 签名不变（仍返回 *LDAPClient，ad_authenticator.go:215 需 .Conn()）"
    - "addomain / scheduler / core-security 三包测试全绿（零行为漂移）"
  artifacts:
    - path: "internal/services/addomain/ldap_iface.go"
      provides: "19 方法接口（16+3）"
      contains: "DNExists"
    - path: "internal/services/addomain/failover_client.go"
      provides: "clientFactory 注入字段 + newClient 辅助方法"
      contains: "clientFactory"
    - path: "internal/services/addomain/failover_client_76_03_test.go"
      provides: "顺序遍历 / maxHops 接口驱动测试"
      min_lines: 60
  key_links:
    - from: "FailoverClient.ExecuteWithFailover"
      to: "LDAPClientIface"
      via: "operation func(client LDAPClientIface) error 签名接口化"
      pattern: "operation func\\(client LDAPClientIface\\) error"
    - from: "failover_client_76_03_test.go fc.clientFactory"
      to: "mockLDAPClient"
      via: "同包白盒赋值工厂字段（账号序号 → 不同 mock）"
      pattern: "fc\\.clientFactory = "
---

<objective>
INFRA-03 落地：LDAPClientIface 扩展 3 个方法（16→19，pattern-mapper 修正口径）、FailoverClient 增 clientFactory 注入字段、ExecuteWithFailover 闭包签名接口化 + 24 处类型行机械替换（20 处闭包 + 4 处连带辅助签名），为 Phase 78 BLOCK-05（addomain ≥70%）交付接口 seam。

Purpose: 零新依赖主推线（REQUIREMENTS INFRA-03 用户锁定：嵌入式 vjeantet/ldapserver 停更风险不担）。本 plan 是 phase 内最大机械改动面（~10 文件 24 处类型行），纯类型替换、生产行为零漂移是硬约束。
Output: 接口/工厂/mock/测试 4 类改动 + 8 文件闭包替换。
边界声明（RESEARCH Open Question 2 裁决）：「walk/分页语义」取窄解读——mock 支持多批次/函数式返回驱动 service 层遍历；ldap_client.go:243-295 的具体客户端 searchWithPaging 分页降级逻辑不在本 phase 范围（需 wire 级 server，Phase 78 按缺口决定）。
文件数豁免说明（scope_sanity）：files_modified 共 12 文件、超过 10 文件警告阈值——其中 8 个为单行类型替换的机械文件，且接口签名、20 处闭包、4 处辅助签名必须同一 commit 原子完成（任一拆分点都令包不可编译），故维持单 plan、不拆分。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/76-test-doubles/76-RESEARCH.md
@.planning/workstreams/milestone/phases/76-test-doubles/76-PATTERNS.md
@.planning/workstreams/milestone/phases/76-test-doubles/76-01-SUMMARY.md

<interfaces>
<!-- 执行者需要的既有契约（已本地复核） -->

From internal/services/addomain/ldap_iface.go:
```go
type LDAPClientIface interface {  // :18-42，当前 16 方法（pattern-mapper 实证；RESEARCH 记 18 系笔误）
    // 追加 3 方法（具体类型上已存在，零新实现）：
    // UpdateGroupAttribute(groupDN string, attrs map[string]string) error   ← ldap_client.go:311
    // CreateOU(ouDN, ouName string) error                                   ← ldap_client.go:440
    // DNExists(dn string) (bool, error)                                     ← ldap_client.go:501
}
var _ LDAPClientIface = (*LDAPClient)(nil)   // :46 编译期断言（自动验证零遗漏）
```

From internal/services/addomain/failover_client.go:
```go
type FailoverClient struct {          // :20-23
    pool   AccountPool
    config *models.ADConfig
}
func (f *FailoverClient) ExecuteWithFailover(
    ctx context.Context,
    operation func(client *LDAPClient) error,   // :33-36 ← 本 plan 接口化的唯一签名
) error
// NewLDAPClient 调用点：ExecuteWithFailover :54（改走 newClient()）与 PickFirstConnect :106（保持不动）
// PickFirstConnect 返回 *LDAPClient 具体类型 —— ad_authenticator.go:215 需要 .Conn()，禁接口化
```

From internal/services/addomain/ldap_client_mock_test.go:
```go
type mockLDAPClient struct {  // :12-43 preset 字段 + 调用计数风格
    connectErr error
    searchUsersRes []*ldap.Entry
    connectCalls int
    ...
}
var _ LDAPClientIface = (*mockLDAPClient)(nil)   // :112 —— 接口 +3 后此处编译失败即守护点
```

From internal/services/addomain/account_pool_test.go（helper 直接复用，勿重写）:
```go
func setupTestPool(t *testing.T) (AccountPool, *gorm.DB, string)   // :17-49（sqlite :memory: + sys_ad_service_accounts DDL）
func insertAccount(t *testing.T, db *gorm.DB, configID, username string, status int) string  // :52-62
```

24 处类型行替换点（grep 实证行号表，PATTERNS §76-03e）= 20 处闭包 + 4 处连带辅助签名。
闭包参数名有两种（勿只 grep 单一参数名）：14 处为 `client`、6 处为 `ldapClient`——dept_sync_service.go:79、user_ad_sync_service.go:68/264/535/719、scheduler/dept_sync_tasks.go:183（后者带 addomain. 限定符）。
```
闭包 20 处（含参数名标注）：
group.go: 129, 159, 200 (client)                        | group_management_service.go: 89, 160, 219, 280 (client)
group_sync_service.go: 60, 104 (client)                 | user.go: 154, 199, 220, 241 (client)
sync.go: 105 (client)                                   | dept_sync_service.go: 79 (ldapClient)
user_ad_sync_service.go: 68, 264, 535, 719 (ldapClient) | internal/scheduler/dept_sync_tasks.go: 183 (ldapClient *addomain.LDAPClient，唯一包外点)

连带辅助签名 4 处（闭包体内以闭包参数直接调用 → 闭包参数接口化后必须同步，否则编译失败；机械性质同闭包）：
dept_sync_service.go:113      syncDeptTree(ctx context.Context, ldapClient *LDAPClient, config, dept, parentOUDN, result)
user_ad_sync_service.go:107   moveUserToNewOU(ctx context.Context, ldapClient *LDAPClient, userID, newDeptID)
user_ad_sync_service.go:142   syncUserAttributes(ctx context.Context, ldapClient *LDAPClient, user, updateReq)
user_ad_sync_service.go:305   moveSingleUserToOU(ctx context.Context, ldapClient *LDAPClient, userID, ouDN)
（调用点实证：dept_sync_service.go:82/152、user_ad_sync_service.go:77/86/275/769 均以闭包参数 ldapClient 传入）

明确不在替换范围（struct 字段注入线，非闭包链）：NewDeptToADSyncService(dept_sync_service.go:21) 与 NewUserADSyncService(user_ad_sync_service.go:33) 构造器的 ldapClient *LDAPClient 参数及对应 struct 字段 ldap *LDAPClient（:17/:24）——它们服务的具体客户端用法与 FailoverClient 闭包链无关，保持 *LDAPClient 具体类型即可满足接口。
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 接口 +3 方法与 mock 补 3 方法（编译闭环）</name>
  <files>internal/services/addomain/ldap_iface.go, internal/services/addomain/ldap_client_mock_test.go</files>
  <read_first>internal/services/addomain/ldap_iface.go（全文）、internal/services/addomain/ldap_client.go（:311 UpdateGroupAttribute / :440 CreateOU / :465 OUExists（不加）/ :501 DNExists）、internal/services/addomain/ldap_client_mock_test.go（全文）</read_first>
  <action>
  ldap_iface.go 在接口尾部追加 3 个方法（带"调用方"注释风格，照 :29/:37 惯例）：UpdateGroupAttribute(groupDN string, attrs map[string]string) error（group.go:201 闭包使用）、CreateOU(ouDN, ouName string) error（dept_sync_service.go:118 闭包使用）、DNExists(dn string) (bool, error)（user_ad_sync_service.go:187 闭包使用）。OUExists 外部零使用，按最小接口原则**不加**。三方法在 *LDAPClient 上已存在（:311/:440/:501），零新实现；ldap_iface.go:46 的 var _ LDAPClientIface = (*LDAPClient)(nil) 自动验证。同步给 mockLDAPClient 补 3 个方法（最小闭环，让 :112 的 var _ LDAPClientIface = (*mockLDAPClient)(nil) 编译通过）：新增 preset 字段 updateGroupAttrErr / createOUErr / dnExistsRes+dnExistsErr 与对应调用计数字段（照 :12-43 既有风格），方法体照 :45-48 最简风格（计数 + 返回 preset）。完成后 `go build ./...` + `go vet ./internal/services/addomain/` + 包测试绿。验收口径按 16→19 方法（pattern-mapper 修正，勿按 RESEARCH 的 18→21）。
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/services/addomain/ && go test -count=1 ./internal/services/addomain/</automated>
  </verify>
  <acceptance_criteria>
  - ldap_iface.go 接口含 UpdateGroupAttribute / CreateOU / DNExists 三个方法声明
  - ldap_client_mock_test.go 的 mockLDAPClient 实现上述 3 方法且 var _ LDAPClientIface = (*mockLDAPClient)(nil) 编译通过
  - `go build ./...` exit 0（编译期断言双侧零遗漏的实证）
  </acceptance_criteria>
  <done>接口 16→19 扩展完成，具体类型与 mock 双侧满足，编译闭环</done>
</task>

<task type="auto">
  <name>Task 2: FailoverClient clientFactory 字段 + 签名接口化 + 24 处类型行机械替换（20 闭包 + 4 辅助签名）</name>
  <files>internal/services/addomain/failover_client.go, internal/services/addomain/group.go, internal/services/addomain/group_management_service.go, internal/services/addomain/group_sync_service.go, internal/services/addomain/user.go, internal/services/addomain/user_ad_sync_service.go, internal/services/addomain/dept_sync_service.go, internal/services/addomain/sync.go, internal/scheduler/dept_sync_tasks.go</files>
  <read_first>internal/services/addomain/failover_client.go（全文）、internal/services/addomain/group.go（:128-136 代表形态）、internal/services/addomain/user_ad_sync_service.go（:68/107/142/264/305/535/719 闭包+辅助签名混布形态）、internal/scheduler/dept_sync_tasks.go（:183 唯一包外点）、internal/core/security/ad_authenticator.go（:215-238 PickFirstConnect 消费方，确认不受波及）</read_first>
  <action>
  三步走，每步后 `go build ./...` 逐文件修复（CLAUDE.md 编译验证强制）。(1) failover_client.go：struct 增第三字段 clientFactory func(*models.ADConfig, *models.ADServiceAccount) LDAPClientIface（nil 时用 NewLDAPClient 的注释写明）；新增辅助方法 newClient(acct *models.ADServiceAccount) LDAPClientIface——f.clientFactory 非 nil 走工厂，否则 NewLDAPClient(f.config, acct)；ExecuteWithFailover :54 的 NewLDAPClient 调用改走 f.newClient(acct)，**PickFirstConnect :106 的 NewLDAPClient 保持原样**（返回具体类型，ad_authenticator 需要 .Conn()）。(2) ExecuteWithFailover 的 operation 签名 func(client *LDAPClient) error → func(client LDAPClientIface) error；循环体内 client.Connect() / operation(client) / client.Close() 已全在接口方法集内，零其它改动；错误路径（MarkFailure "dial:"/"operation:" 前缀、ErrAllAccountsUnavailable、maxHops 封顶 :45-48）一律不动。(3) 24 处类型行机械替换（20 处闭包 + 4 处连带辅助签名）：仅把参数类型 *LDAPClient → LDAPClientIface（scheduler 包为 *addomain.LDAPClient → addomain.LDAPClientIface）。闭包参数名两种（client/ldapClient，清单见 context 块），执行时以 grep -rnE "func\((client|ldapClient) \*(addomain\.)?LDAPClient\)" 实测为准（勿用旧模式 func\(client \*LDAPClient\) error——会漏掉 6 处参数名为 ldapClient 的闭包），行号可能有 ±2 漂移。4 个辅助签名（dept_sync_service.go:113 syncDeptTree、user_ad_sync_service.go:107 moveUserToNewOU / :142 syncUserAttributes / :305 moveSingleUserToOU）从闭包体内以闭包参数直接调用（实证调用点 :82/:152/:77/:86/:275/:769），必须连带把签名中的 ldapClient *LDAPClient → ldapClient LDAPClientIface，否则编译失败；若某辅助函数体内调用了 19 方法接口之外的方法（编译门会拦），按同一「调用方」注释惯例把该方法补入接口（具体类型上已存在，仍是零实现机械面）。NewFailoverClient 调用、errors.Is(err, ErrAllAccountsUnavailable) 分支、闭包体内/辅助函数体内逻辑全部原样——diff 中出现类型替换以外的闭包体/函数体改动即为违规。NewDeptToADSyncService:21 / NewUserADSyncService:33 两构造器参数与 struct 字段 ldap 不在闭包链上，保持不动（见 context 块范围声明）。完成后三包回归：addomain + scheduler + core/security。
  </action>
  <verify>
    <automated>go build ./... && ! grep -rnE "func\((client|ldapClient) \*(addomain\.)?LDAPClient\)" internal/services/addomain/ internal/scheduler/ && ! grep -rnE "\) (syncDeptTree|moveUserToNewOU|syncUserAttributes|moveSingleUserToOU)\(ctx context\.Context, ldapClient \*LDAPClient" internal/services/addomain/ && go test -count=1 ./internal/services/addomain/... ./internal/scheduler/... ./internal/core/security/...</automated>
  </verify>
  <acceptance_criteria>
  - 闭包零残留门：grep -rnE "func\((client|ldapClient) \*(addomain\.)?LDAPClient\)" 在 internal/services/addomain/ 与 internal/scheduler/ 零命中（20 处闭包 + ExecuteWithFailover 签名 1 处全替换，覆盖 client/ldapClient 两种参数名）
  - 辅助签名零残留门：grep -rnE "\) (syncDeptTree|moveUserToNewOU|syncUserAttributes|moveSingleUserToOU)\(ctx context\.Context, ldapClient \*LDAPClient" 在 internal/services/addomain/ 零命中（4 处辅助签名全替换）
  - failover_client.go 含 clientFactory 字段与 newClient 辅助方法；PickFirstConnect 签名仍返回 *LDAPClient
  - `git diff` 中 9 个改动文件（8 个闭包/辅助签名文件 + failover_client.go）的类型行合计 = 闭包 20 处 + ExecuteWithFailover 签名 1 处 + 辅助签名 4 处，闭包体/函数体零改动；NewDeptToADSyncService / NewUserADSyncService 构造器与 struct 字段 ldap 保持 *LDAPClient 不动
  - `go test -count=1 ./internal/services/addomain/... ./internal/scheduler/... ./internal/core/security/...` 全绿
  </acceptance_criteria>
  <done>seam 落地：operation 已接口化，24 处类型行（20 闭包 + 4 辅助签名）机械替换零行为漂移，PickFirstConnect 不受波及</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: mock walk/分页能力 + FailoverClient 接口驱动测试</name>
  <files>internal/services/addomain/failover_client_76_03_test.go, internal/services/addomain/ldap_client_mock_test.go</files>
  <read_first>internal/services/addomain/account_pool_test.go（:17-62 setupTestPool/insertAccount 直接复用 + db.Exec 查询式断言风格）、internal/services/addomain/ldap_client_mock_test.go（Task 1 后的状态）、internal/services/addomain/failover_client.go（Task 2 后的状态）</read_first>
  <behavior>
  - 顺序遍历：池含账号0（username-0）与账号1（username-1），clientFactory 按账号返回 mock——账号0 的 mock Connect 返回 error，账号1 的 mock Connect 成功且 operation 闭包计数 → 断言 operation 执行次数 == 1（成功即止）、账号0 行 failure_count 落库 +1（查 sys_ad_service_accounts）、账号1 行走 MarkSuccess 路径
  - maxHops 封顶：池含 12 个可用账号（全部 Connect 失败 mock）→ 断言 clientFactory 被调用次数 == DefaultMaxHops（10）且 ExecuteWithFailover 返回 ErrAllAccountsUnavailable（errors.Is）
  - walk/分页：mock 增 searchUsersFn func() ([]*ldap.Entry, error) 函数字段（非 nil 时优先于 searchUsersRes，支持多批次多次调用），用例断言连续两次调用返回不同批次
  </behavior>
  <action>
  先给 mockLDAPClient 增 searchUsersFn func() ([]*ldap.Entry, error) 函数字段：SearchUsers 方法内非 nil 时优先调用并计数，否则返回既有 searchUsersRes/searchUsersErr（多批次驱动能力，窄边界见 objective）。新建 failover_client_76_03_test.go（package addomain，同包）：复用 setupTestPool/insertAccount（account_pool_test.go:17-62，勿重写 helper）；构造 `fc := NewFailoverClient(pool, cfg)` 后同包白盒赋值 fc.clientFactory = func(_ *models.ADConfig, acct *models.ADServiceAccount) LDAPClientIface，按 acct.Username 分流返回不同 mock。三个用例按 behavior 块落地（insertAccount 的 status 参数用 0=可用）。落库断言照 analog 的 db.Exec 查询式（如 SELECT failure_count FROM sys_ad_service_accounts WHERE ...）；断言风格 assert.ErrorIs / require.NoError。DefaultMaxHops 若为 min(10, len(available)) 语义，12 账号场景封顶值即 10（以 failover_client.go 实测常量为准，断言用常量名不用魔法数）。
  </action>
  <verify>
    <automated>go test -count=1 -run 'TestFailover' ./internal/services/addomain/ -v && go test -count=1 ./internal/services/addomain/</automated>
  </verify>
  <acceptance_criteria>
  - failover_client_76_03_test.go 含顺序遍历与 maxHops 两个具名测试（TestFailover 前缀），全部零真实网络（clientFactory 全 mock）
  - mockLDAPClient 含 searchUsersFn 函数字段且 SearchUsers 非 nil 优先
  - `go test -count=1 ./internal/services/addomain/` 全绿
  </acceptance_criteria>
  <done>FailoverClient 顺序遍历/maxHops 语义可经接口驱动验证，Phase 78 addomain Iface stub 段可直接复用此 mock 形态</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 生产 AD 连接 ↔ 测试注入 | clientFactory 是唯一注入缝；生产默认 NewLDAPClient 行为不变 |
| 接口面扩张 | LDAPClientIface 16→19，任何实现方（具体类型/mock）必须同步 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-03-01 | Tampering（生产行为漂移） | 24 处类型行机械替换（20 闭包 + 4 辅助签名） | mitigate | 纯类型替换纪律（diff 审查闭包体/函数体零改动）+ addomain/scheduler/core-security 三包回归门 + 双 grep 零残留验收（闭包门覆盖 client/ldapClient 两参数名 + 辅助签名门按函数名锚定） |
| T-76-03-02 | Elevation（PickFirstConnect 误接口化） | ad 认证链 | mitigate | PickFirstConnect 签名不动列为硬红线（ad_authenticator.go:215 需 .Conn() 具体方法）；Task 2 验收显式检查 |
| T-76-03-03 | Repudiation（failover 簿记绕过） | AccountPool MarkFailure/MarkSuccess | mitigate | Task 3 顺序遍历用例断言 failure_count 落库，注入缝不破坏审计簿记 |
| T-76-03-04 | Information Disclosure | mock preset 数据 | accept | 全 dummy ldap.Entry/DN 值，无真实 AD 身份信息 |
</threat_model>

<verification>
- `go build ./...` exit 0
- 闭包零残留：`grep -rnE "func\((client|ldapClient) \*(addomain\.)?LDAPClient\)" internal/services/addomain/ internal/scheduler/` 零命中
- 辅助签名零残留：`grep -rnE "\) (syncDeptTree|moveUserToNewOU|syncUserAttributes|moveSingleUserToOU)\(ctx context\.Context, ldapClient \*LDAPClient" internal/services/addomain/` 零命中
- `go test -count=1 ./internal/services/addomain/... ./internal/scheduler/... ./internal/core/security/...` 全绿
- `go vet ./internal/services/addomain/` 干净
- gate 不倒退：`bash scripts/check-ci-local.sh backend`
</verification>

<success_criteria>
- INFRA-03 完成：接口 19 方法 + 工厂字段 + 签名接口化 + 24 处类型行替换（20 闭包 + 4 辅助签名）+ mock/测试双落地，零新依赖、零生产行为漂移
- 窄边界成文：wire 级 LDAP server（vjeantet/ldapserver）不在本 phase，Phase 78 按缺口决定
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/76-test-doubles/76-03-SUMMARY.md` when done
</output>
