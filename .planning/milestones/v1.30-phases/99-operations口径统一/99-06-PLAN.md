---
phase: 99
plan_id: 99-06
title: "V130R-08/09 缺口收口（helper 收敛 + constants 合并 + 注释处置）"
type: refactor
gap_closure: true
wave: 1
depends_on: []
requirements_addressed: ["V130R-08", "V130R-09"]
files_modified:
  - "internal/services/operations/workstation_service.go"
  - "internal/services/operations/infopoint_service.go"
  - "internal/services/operations/building_service.go"
  - "internal/services/operations/asset_service.go"
  - "pkg/constants/pagination.go"
  - "pkg/constants/cache.go"
  - "pkg/constants/scheduler.go"
  - "pkg/constants/time.go"
  - "pkg/constants/uuid.go"
  - "internal/services/base/service.go"
  - ".planning/phases/99-operations口径统一/99-CONTEXT.md"
autonomous: true
---

# Plan 99-06: V130R-08/09 缺口收口（gap closure）

## Context

99-VERIFICATION.md（2026-09-07）判定 Phase 99 为 gaps_found（4/6 SC）：四项用户可见缺陷修复全部落地，失败的是三项结构性子条款。本计划一次收口：

1. **V130R-08 收敛断链**：`BuildDeptRecursiveFilter`（dept_filter.go:21）已建成 + 6 回归测试，但 8 处内联实现零迁移（ORPHANED）。四条件形式当前语义一致，但防再发散目的落空。
2. **V130R-09 双包合并**：ROADMAP SC 要求「internal/constants 与 pkg/constants 双包合并方案经 discuss 敲定落地」，99-CONTEXT 的 D-03 决策未含此项。
3. **base/service.go 过时注释**：:19-20 与 :106 仍称「三种 clamp 语义并存」，与 99-04 收敛后现实矛盾。

**裁决（D-03-10，2026-09-07 orchestrator 补充决策，经用户长程标准授权）**：**合并 internal/constants → pkg/constants**。依据：Phase 89/90 已确立 pkg/constants 为项目 leaf-const 唯一权威（`pkg/constants/pagination.go` / `timeouts.go` / `ports.go` / `protocol.go` / `concurrency.go`）；internal/constants 是该模式落地前的历史遗留（6 文件 194 行），双包并存违反单一权威。合并为有界机械重构（约 21 个生产 import 方 + 测试）。

## must_haves

**Truths（可观察行为）:**
- `BuildDeptRecursiveFilter` 有 ≥8 个生产调用方（workstation ×4 / infopoint ×1 / building ×2 / asset ×1），8 处内联四条件实现全部删除（grep 内联 SQL 形态 `ancestors LIKE` 零残留于这四文件）
- `internal/constants` 包已删除，全仓 `grep -rn "internal/constants"` 零命中（含 go.mod 无关，纯 import 路径）；其全部常量在 `pkg/constants` 可用
- `pkg/constants` 无重复常量定义（`go build ./...` 通过即证明无 redeclare）
- `base/service.go` :19-20 与 :106 注释反映单一权威现状
- `99-CONTEXT.md` 含 D-03-10 双包合并决策记录
- 回归纪律：dept_filter_test.go 既有 6 测试保持绿；受影响包 `go test` 全绿

---

## Tasks

### Task 1: 8 处内联 orgId 筛选收敛到 BuildDeptRecursiveFilter

**read_first:**
- `internal/services/operations/dept_filter.go` — helper 签名与用法（:8-21）
- `internal/services/operations/workstation_service.go` — :82/:120/:296/:481 四处
- `internal/services/operations/infopoint_service.go` — :198
- `internal/services/operations/building_service.go` — :193/:275
- `internal/services/operations/asset_service.go` — :276

**action:**
逐处将内联的「部门+全部子部门」四条件（`id = ? OR ancestors LIKE ...` 形态，已由 99-03 修正为中段漏匹配安全的四条件）替换为 `BuildDeptRecursiveFilter(deptID, "<idColumn>")` scope 调用：

1. 先核对每处的 id 列名与限定形式（如 `org_id` / `b.org_id` / `ws.dept_id`），作 idColumn 实参；deptID 实参保持各处现有的参数来源不变。
2. 替换形态：若原代码在 query 链上直接 `.Where(...)`，改为 `.Where(BuildDeptRecursiveFilter(deptID, col))`（Scope 即 `func(*gorm.DB) *gorm.DB`，可直接作 Where 实参；或按各处现有 scope 组装风格挂入 scopes 列表——以最小改动为准）。
3. 删除被替换的内联条件代码；若某处的 deptID 为空串守卫逻辑存在（如 `if orgID != ""` 包裹），守卫保留、仅替换条件构造部分。
4. 行号为 2026-09-07 verifier 实测，执行时以 grep `ancestors LIKE` 就近重定位为准，不盲信行号。

**verify:** `go build ./...` 通过；`go test ./internal/services/operations/...` 全绿；`grep -rn "ancestors LIKE" internal/services/operations/ --include="*_service.go"` 仅剩 dept_filter.go 内部的 helper 实现一处（或零处——若 helper 用别的拼写则以 grep `BuildDeptRecursiveFilter` 计数 ≥9（8 调用 + 1 定义）为准）

**done:** 8 处调用方迁移完成，helper 不再 ORPHANED

### Task 2: internal/constants 合并进 pkg/constants（D-03-10 落地）

**read_first:**
- `internal/constants/` 全部 6 文件（cache.go / doc.go / example_test.go / pagination.go / scheduler.go / time.go / uuid.go，共 194 行）
- `pkg/constants/` 既有文件清单（pagination.go / timeouts.go / ports.go / protocol.go / concurrency.go + Phase 89/90 锁值测试）

**action:**
1. **冲突清点**：逐文件比对 internal/constants 与 pkg/constants 的常量名。已知确定冲突：`DefaultCurrent` / `DefaultPageSize`（两包同值 1/10）——保留 pkg/constants 既有定义。其余（time.go vs timeouts.go、scheduler.go、cache.go、uuid.go）逐一核对，同名同值保留一处，同名异值 STOP 并报告（预期不存在）。
2. **迁移**：无冲突的常量按主题归入 pkg/constants 既有文件或新建对应主题文件（如 `cache.go` / `uuid.go` 内容少可并入相近文件；保持 pkg/constants 文件组织清晰为准）。`doc.go` 的包说明合并进 pkg/constants 既有 doc（若有）或文件头注释。`example_test.go` 相应迁移/合并。
3. **import 重写**：全仓 `grep -rln "xingran-go-backend/internal/constants"` 的所有文件（生产 + 测试，约 21+ 处）改为 `pkg/constants` 路径。若某文件同时 import 两包，去重。
4. **删除** `internal/constants/` 目录。
5. **CLAUDE.md 同步**：grep CLAUDE.md 中 `internal/constants` 引用（Pagination Constants Convention 段等）更新为 pkg/constants 口径，并补一行「internal/constants 已于 V130R-09 合并入 pkg/constants（D-03-10）」。
6. **99-CONTEXT.md 补决策**：在 V130R-09 决策块末尾追加 `**D-03-10:**（2026-09-07 orchestrator 补充裁决）internal/constants 整体合并进 pkg/constants（Phase 89/90 leaf-const 唯一权威）；DefaultCurrent/DefaultPageSize 保留 pkg 既有定义`。

**verify:** `go build ./...` 通过；`grep -rn "internal/constants" --include="*.go" internal pkg cmd` 零命中；`grep -rn "internal/constants" CLAUDE.md` 零命中（或仅存历史标注行）；`go test ./pkg/constants/... ./internal/services/operations/... ./internal/models/...` 全绿（models 侧 requests 大户在 internal/models/system/requests，也跑一下 `go test ./internal/models/...`）

**done:** internal/constants 删除，pkg/constants 单一权威，全仓 import 重写完成，CLAUDE.md 与 CONTEXT 决策同步

### Task 3: base/service.go 过时注释处置

**read_first:**
- `internal/services/base/service.go` — :19-20（PageParams doc）与 :106 附近（List doc 设计红线段）

**action:**
1. :19-20 的「项目现存三种 clamp 语义（10..100 / 10..10000 / 无 clamp）并存于各 service 层…」改为反映现状：`service 层统一经 pkg/query.NormalizePagination / NormalizePaginationWithMax 归一化后传入（V130R-09 单一权威）；repo 直信入参的设计不变`。
2. :106 附近「repo 内不 clamp 分页（PageParams 直信入参，clamp 语义归 service 层）」红线句保留（设计不变），仅确保其上下文描述与单一权威措辞一致；若「三种 clamp」措辞在别处还有残留（grep `三种 clamp` base/ 目录）一并更新。

**verify:** `go build ./internal/services/base/` 通过；`grep -n "三种 clamp" internal/services/base/service.go` 零命中

**done:** 过时注释清零，描述与 V130R-09 收敛后现实一致

---

## Verification

- `go build ./...` 通过
- `go test ./internal/services/operations/... ./pkg/constants/... ./internal/services/... ./internal/models/...` 全绿
- `grep -rn "BuildDeptRecursiveFilter" internal --include="*.go" | grep -v _test | grep -v dept_filter.go` ≥8 命中
- `grep -rn "internal/constants" --include="*.go" .` 零命中
- `grep -rn "ancestors LIKE" internal/services/operations/ --include="*.go" | grep -v dept_filter` 仅测试文件命中

## Success Criteria

1. V130R-08 收敛子条款达成：helper ≥8 生产调用方，内联实现清零（99-VERIFICATION gap 1 关闭）
2. V130R-09 双包合并子条款达成：internal/constants 删除、pkg/constants 单一权威、决策 D-03-10 落盘（gap 2 关闭）
3. base/service.go 注释与现状一致（gap 3 关闭）
4. 全部测试与构建零失败；七 gate 不倒退
