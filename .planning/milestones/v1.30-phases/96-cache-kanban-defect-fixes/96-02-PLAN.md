---
phase: "96"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - "internal/services/monitor/cache_service.go"
autonomous: true
requirements:
  - "CACHEDEF-05"
user_setup: []
gap_closure: false

must_haves:
  truths:
    - "normalizeCacheKeyForService correctly strips xingran: prefix using strings.HasPrefix + strings.TrimPrefix"
    - "Keys without xingran: prefix pass through unchanged (no false positives)"
    - "All 6 call sites of normalizeCacheKeyForService work correctly after the fix"
  artifacts:
    - path: "internal/services/monitor/cache_service.go"
      provides: "CACHEDEF-05 fixed: normalizeCacheKeyForService uses strings.HasPrefix + strings.TrimPrefix"
  key_links:
    - from: "internal/services/monitor/cache_service.go:766"
      to: "internal/services/monitor/cache_service.go:295"
      via: "normalizeCacheKeyForService call at list display"
      pattern: "normalizeCacheKeyForService"
---

<objective>
Fix CACHEDEF-05: normalizeCacheKeyForService in monitor/cache_service.go uses key[:6] which compares a 6-byte slice against the 8-byte string literal "xingran:" — always false. Replace with strings.HasPrefix + strings.TrimPrefix for correct prefix stripping.
</objective>

<context>
@.planning/phases/96-cache-kanban-defect-fixes/96-CONTEXT.md
@internal/services/monitor/cache_service.go (lines 760-800 for normalizeCacheKeyForService and isSystemKeyForService, lines 290-300 for usage context)
</context>

<interfaces>
From internal/services/monitor/cache_service.go:766-771:
```go
// Current defect: len(key) > 6 && key[:6] == "xingran:" — 6-byte slice never equals 8-byte literal
func normalizeCacheKeyForService(key string) string {
    if len(key) > 6 && key[:6] == "xingran:" {
        return key[6:]
    }
    return key
}
```

The 6 call sites confirmed by discuss research:
- :295 displayKey in cache list display
- :397 key detail view
- :470 delete operation
- :478 exists check
- :489 expire operation
- :536 batch delete
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix CACHEDEF-05 — normalizeCacheKeyForService prefix stripping</name>
  <files>internal/services/monitor/cache_service.go, internal/services/monitor/cache_service_96_05_test.go</files>
  <behavior>
    - Test: normalizeCacheKeyForService("xingran:user:1") returns "user:1"
    - Test: normalizeCacheKeyForService("xingran:cache:dept:tree:select") returns "cache:dept:tree:select"
    - Test: normalizeCacheKeyForService("user:1") returns "user:1" (no prefix — unchanged)
    - Test: normalizeCacheKeyForService("") returns "" (empty string boundary)
    - Test: normalizeCacheKeyForService("xingran") returns "xingran" (prefix without colon — unchanged)
    - Test: normalizeCacheKeyForService("xingran:") returns "" (exactly xingran: prefix — stripped)
    - Test: isSystemKeyForService("user:1") returns false (non-system key)
    - Test: isSystemKeyForService("monitor:server:1") returns true (system prefix)
  </behavior>
  <read_first>
    internal/services/monitor/cache_service.go (lines 763-800 for normalizeCacheKeyForService and isSystemKeyForService)
  </read_first>
  <action>
Fix normalizeCacheKeyForService (monitor/cache_service.go:766-771).

**Change (lines 766-771):**
Replace the entire function:

```go
func normalizeCacheKeyForService(key string) string {
    if strings.HasPrefix(key, "xingran:") {
        return strings.TrimPrefix(key, "xingran:")
    }
    return key
}
```

Add strings import to the file's import block if not already present. The strings package is already used in isSystemKeyForService (line 780: strings.HasPrefix and strings.TrimPrefix appear to be used there based on the pattern — verify and add if missing).

**Why this works:**
- strings.HasPrefix checks if the string begins with "xingran:" (correct 8-byte comparison)
- strings.TrimPrefix removes exactly one occurrence of the prefix (idempotent — no double-stripping)
- Keys without the prefix pass through unchanged

**Do NOT change:**
- isSystemKeyForService — it is working correctly with its existing implementation
- Any call sites — they all consume the return value correctly
</action>
  <acceptance_criteria>
    - <source_assertion>normalizeCacheKeyForService uses strings.HasPrefix(key, "xingran:")</source_assertion>
    - <source_assertion>normalizeCacheKeyForService uses strings.TrimPrefix(key, "xingran:")</source_assertion>
    - <source_assertion>No more key[:6] slice indexing in the function</source_assertion>
    - <test_command>go test ./internal/services/monitor/ -run TestMonitorCacheImpl96 -v</test_command>
  </acceptance_criteria>
  <done>normalizeCacheKeyForService correctly strips xingran: prefix; all 6 call sites work correctly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-----------|-------------|
| {cache monitor UI → backend} | Displayed keys cross here; prefix stripping enables real key matching |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-05 | Tampering | monitor cache key display | mitigate | CACHEDEF-05 fix uses strings.HasPrefix/TrimPrefix |
| T-96-SC | Tampering | new test file | mitigate | Regression tests are pure unit tests, no external dependencies |
</threat_model>

<verification>
1. `go build ./...` — zero errors
2. `go test ./internal/services/monitor/ -run TestMonitorCacheImpl96 -v` — all pass
3. `grep -n "key\[:6\]" internal/services/monitor/cache_service.go` — zero matches in normalizeCacheKeyForService
</verification>

<success_criteria>
- normalizeCacheKeyForService uses strings.HasPrefix + strings.TrimPrefix (no more key[:6])
- All 6 call sites in cache_service.go still compile (no signature changes)
- CACHEDEF-05 regression test passes
- `go build ./...` passes with zero errors
</success_criteria>

<output>
Create `.planning/phases/96-cache-kanban-defect-fixes/96-02-SUMMARY.md` when done
</output>
