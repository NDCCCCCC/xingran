---
phase: "100"
plan_id: "100-01"
title: "V130R-10/11 rpaApi 幽灵方法清除 + 全族裁剪至存活集 + 对账落盘 + 守卫基线"
type: "refactor"
wave: 1
depends_on: []
requirements_addressed: ["V130R-10", "V130R-11"]
files_modified:
  - "xingran-react-frontend/src/lib/rpaApi.ts"
  - "xingran-react-frontend/src/lib/rpaApi.test.ts"
  - "xingran-react-frontend/src/lib/__tests__/rpaApi.batch56.unit.test.ts"
  - "xingran-react-frontend/src/lib/download.test.ts"
  - "xingran-react-frontend/src/pages/operations/rpa/tasks/__tests__/index.test.tsx"
  - "xingran-react-frontend/src/pages/operations/rpa/workers/__tests__/index.test.tsx"
  - "xingran-react-frontend/src/lib/apiFactory.invariants.test.ts"
  - ".planning/phases/100-frontend-contract-fixes/RECONCILIATION.md"
autonomous: true
---

# Plan 100-01: rpaApi 幽灵方法清除 + 全族裁剪至存活集 + 对账落盘 + 守卫基线

## Context

V130R-10/11 主战场：`xingran-react-frontend/src/lib/rpaApi.ts`（全 683 行）经全量对账（100-RESEARCH.md §1/§2，file:line 实测）只有 **19 个方法有后端路由**，且 rpaApi.ts **零生产消费者**——RPA 三页面（tasks/executions/workers）全部内联 `post` 直调（tasks/index.tsx:18、executions/index.tsx:18、workers/index.tsx:39）。本计划把 rpaApi 裁剪至存活集端态、删除全部死方法与死测试、落盘对账台账、升级 invariants 守卫防回增。

**Decisions honored:**
- **D-100-1**: rpaApi 8 处工厂 spread 幽灵方法直接删除，不保留空壳（调用面已实测零调用方）
- **D-100-2**: invariants 守卫锁定清理后方法集（keys 基线 = 后端路由存在的实测集合）
- **D-100-3**: 全族逐一列清单三分类（alive/dead/mismatch）；rpa 族内 mismatch 为零（RESEARCH §2 结论：所有无路由方法都是 dead）
- **D-100-4**: 对账清单落盘 `RECONCILIATION.md`（六列），作为 SUMMARY 附件归档
- **D-100-5**: invariants rpaApi 档锁定清理后基线，死方法回增即红
- **D-100-9**: 端态裁至存活方法集（方案 B），**不整文件删除**；页面内联 post 既有约定债登记 RECONCILIATION.md「observed debt」段，不顺手扩 scope
- **D-100-12**: `workerApi.register`/`workerApi.heartbeat` 前端方法删除（RPA Worker 是独立进程直连后端 HTTP，不经 admin bundle）；后端公开路由（rpa_router.go:15/:18）保留不动，RECONCILIATION.md 记录依据
- 全局标准：死方法/死链一律删除，不留兼容导出（99-04 同款用户指令）

### 清理后端态（17 存活方法 = 19 alive − D-100-12 删 register/heartbeat）

后端真相源：`internal/api/v1/rpa/rpa_router.go`（挂载 router.go:953-955 公开组 + :962-968 JWT 组）。

| 对象 | 存活方法 | 来源 | 后端路由 |
|------|---------|------|---------|
| `taskApi` | list, get, create, update, delete, execute（6） | taskCrudApi 工厂 pick | rpa_router.go:48-53 ✓ |
| `workerApi` | list（工厂 pick）, statistics（手写，收窄为无参形态）（2） | 见 Task 1 | :64/:66 ✓ |
| `executionApi` | list, get, statistics（工厂 pick）, cancel, logs（手写保留）（5） | 见 Task 1 | :84-89 ✓ |
| `aiApi` | generateScript, optimizeScript, decide, analyzeFailure（4） | 手写保留 | :101-108 ✓ |
| 合计 | **17** | | |

**删除清单**（99 个方法 = RESEARCH §2.2 的 97 dead + D-100-12 的 register/heartbeat）：
- 整族删除 6 族（66 方法）：scriptApi(10)/scheduleApi(14)/variableApi(12)/templateApi(13)/notificationApi(12)/statisticsApi(5)——后端 `/rpa/{scripts,schedules,variables,templates,notifications,statistics}` 组全仓不存在（双重 grep 实证）；对应工厂常量 `scriptCrud`(:132)/`scheduleCrudApi`(:327)/`variableCrudApi`(:393)/`templateCrudApi`(:442)/`notificationCrudApi`(:545) 一并删除
- taskApi 死方法（9）：batch/statistics/searchOptions（工厂幽灵，pick 即消失）+ cancelExecution/duplicate/executions/validateScript/export/import（手写死）
- workerApi 死方法（13）：register/heartbeat（D-100-12）+ progress/getOnline/offline/restart（手写死）+ get/create/update/delete/batch/searchOptions（工厂幽灵）；statistics 的 **id 分支死**（`POST /rpa/workers/:id/statistics` 未注册，:208-231）→ 收窄为无参
- executionApi 死方法（9）：create/update/delete/batch/searchOptions（工厂幽灵）+ streamLogs/screenshots/downloadReport/retry（手写死）
- aiApi 死方法（2）：explainScript/captureState

**⚠️ 名字相近防误删（RESEARCH Pitfall 4）**：rpaApi 内 `notificationApi`/`statisticsApi` 与 KEEP 例外 `src/lib/notificationConfigApi.ts`（system 模块，工厂 KEEP 名单在册）完全无关——所有删除操作以 rpaApi.ts 文件内定位为准，diff 不得触及 notificationConfigApi.ts / noticeApi.ts。

**⚠️ `downloadFilePost` 保留（RESEARCH Pitfall 5）**：`executionApi.downloadReport` 是 rpaApi 内唯一消费者，删除 downloadReport **不删** `src/lib/download.ts` 的 `downloadFilePost` 本身（活消费者：opsApi.ts excelApi.export + asset export；且 100-03 将在其上新增 JSON 检测）。download.test.ts 只摘 executionApi 相关块。

## must_haves

**Truths（可观察行为）:**
- rpaApi.ts 导出面恰为 17 个存活方法（4 对象），每个方法的后端路由在 rpa_router.go 实际注册（D-100-9/D-100-12）；全仓 grep `scriptApi|scheduleApi|variableApi|templateApi|notificationApi|statisticsApi`（rpa 域）零命中
- rpaApi 的 8 处工厂 spread 幽灵方法（batch/statistics/searchOptions 等 54 实例）不再出现在任何导出对象上（D-100-1）
- `rpaApi.test.ts` 锁定 17 存活方法的 URL+动词接线 + 聚合对象 4 键结构；`apiFactory.invariants.test.ts` keys 基线锁定 4 对象方法集，回增/私删双向即红（D-100-2/D-100-5）
- `RECONCILIATION.md` 落盘于 phase 目录，含 116 方法六列对账表 + observed-debt 段（D-100-4/D-100-9）
- 前端三 gate 不倒退：lint 0 errors（warnings ≤ 1389）/ type-check 通过 / vitest 0 失败；45 dirs 覆盖率 ratchet 不破（重点盯 `lib 87.2` 下限）

**Artifacts:**
- `xingran-react-frontend/src/lib/rpaApi.ts`：裁剪后文件（预估 ~250 行），4 个导出对象 + rpaApi 聚合 4 键
- `xingran-react-frontend/src/lib/rpaApi.test.ts`：重写为存活契约基线（17 方法 URL 断言 + keys 结构断言）
- `xingran-react-frontend/src/lib/__tests__/rpaApi.batch56.unit.test.ts`：**删除**
- `xingran-react-frontend/src/lib/download.test.ts`：摘除 executionApi import(:54) 与 downloadReport describe(:217-250)
- `xingran-react-frontend/src/pages/operations/rpa/{tasks,workers}/__tests__/index.test.tsx`：摘除空转 `vi.mock("@/lib/rpaApi", factory)`（:16-22，mock 的方法名自始不存在）
- `xingran-react-frontend/src/lib/apiFactory.invariants.test.ts`：新增 keys 基线 describe 块（4 对象）
- `.planning/phases/100-frontend-contract-fixes/RECONCILIATION.md`：新建（六列表 + observed-debt）

**Key Links:**
- `taskApi/executionApi` 工厂方法 → `createResourceApi` pick（不再 spread）→ 后端路由 :47-58/:83-96
- `workerApi.statistics`（无参收窄版）→ `POST /rpa/workers/statistics`（:66）
- invariants keys 基线 → rpaApi 4 导出对象运行时 `Object.keys` 等值断言
- RECONCILIATION.md 台账 ↔ rpa_router.go 注册表 ↔ rpaApi.ts 端态三方一致

---

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin bundle → /api/v1/rpa/* | 删除的是前端声明面，不新增任何请求路径；register/heartbeat 摘除后 admin bundle 不再携带 Worker 节点专用端点知识 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01-1 | Information Disclosure | rpaApi.ts 幽灵方法面 | mitigate | 全量删除不保留兼容导出（D-100-1/全局标准）；keys 基线防回增 |
| T-100-01-2 | Information Disclosure | workerApi.register/heartbeat 包装器残留 admin bundle | mitigate | D-100-12 删除前端方法；后端公开路由保留（设计内公开端点，超 scope 不动） |
| T-100-01-SC | Tampering | npm/pip/cargo installs | accept | 本计划零新增依赖（RESEARCH §17：slopcheck 无对象） |
</threat_model>

---

## Tasks

### Task 1: rpaApi.ts 裁剪至 17 存活方法（pick 形态重构）

**read_first:**
- `xingran-react-frontend/src/lib/rpaApi.ts`（全 683 行）——8 个 createResourceApi 站点 :70/:132/:172/:259/:327/:393/:442/:545、workerApi 段 :175-256（register :181/heartbeat :188/progress :194/getOnline :202/statistics :208-231/offline/restart）、executionApi 段 :259-325、aiApi 段、聚合导出 :671-682、类型定义段
- `100-RESEARCH.md` §2.1（后端注册表）+ §2.2（116 方法对账总表）——存活/死逐方法判定基准
- `xingran-react-frontend/src/lib/apiFactory.ts` —— createResourceApi 8 方法签名（pick 引用形态）

**action:**
按 Context「清理后端态」表执行单文件大手术（建议整文件重写为裁剪后版本，避免 30+ 处碎片 edit）：

1. **工厂常量保留 3 个**：`taskCrudApi`(:70)、`workerCrudApi`(:172)、`executionCrudApi`(:259)；删除 5 个整族工厂常量：`scriptCrud`(:132)、`scheduleCrudApi`(:327)、`variableCrudApi`(:393)、`templateCrudApi`(:442)、`notificationCrudApi`(:545)。
2. **spread 改 pick（D-100-1：幽灵方法不得出现在导出对象上）**——三个保留工厂全部从 `...xxxCrudApi` spread 改为显式方法引用：
   - `taskApi = { list: taskCrudApi.list, get: taskCrudApi.get, create: taskCrudApi.create, update: taskCrudApi.update, delete: taskCrudApi.delete, execute: taskCrudApi.execute }`（execute 若原为手写且路由一致亦可保留手写实现，URL 必须是 `/rpa/tasks/:id/execute`）
   - `workerApi = { list: workerCrudApi.list, statistics: <收窄版手写> }`；statistics 删除 id 参数与 id 分支（`/rpa/workers/:id/statistics` 未注册），保留无参分支 `POST /rpa/workers/statistics` 及其返回类型
   - `executionApi = { list: executionCrudApi.list, get: executionCrudApi.get, statistics: executionCrudApi.statistics, cancel: <手写保留>, logs: <手写保留> }`（cancel/logs 原 URL `/rpa/executions/:id/cancel`、`/rpa/executions/:id/logs` 不动）
3. **删除手写死方法**：taskApi 的 cancelExecution/duplicate/executions/validateScript/export/import；workerApi 的 register/heartbeat（**D-100-12**）/progress/getOnline/offline/restart；executionApi 的 streamLogs/screenshots/downloadReport/retry；aiApi 的 explainScript/captureState。
4. **整族删除 6 对象**：scriptApi/scheduleApi/variableApi/templateApi/notificationApi/statisticsApi 连同其全部手写方法。
5. **类型三件套同步删**（RESEARCH Anti-Pattern「删就删导出+类型+测试」）：各死方法/死族专属类型（如 Script/Schedule/Variable/Template/Notification 相关、WorkerRegisterRequest/WorkerHeartbeatRequest/ExecutionProgress、TaskExcel 相关等）——**删除前逐一 grep**（`grep -rn "<TypeName>" xingran-react-frontend/src --include="*.ts" --include="*.tsx"`），仍被存活代码引用的类型保留并注明；`export type` 块同步收缩。
6. **聚合导出收缩（:671-682）**：`rpaApi` 从 10 键改为 4 键 `{ task, worker, execution, ai }`。
7. 文件头注释更新：注明 Phase 100 对账端态（17 方法）与 RECONCILIATION.md 指针。

**verify:**
```bash
cd xingran-react-frontend && npm run type-check
grep -c "createResourceApi" src/lib/rpaApi.ts   # 期望 3（+1 import 行不计——grep 口径自查，import 行含 createResourceApi 会有第 4 命中，允许 3-4）
grep -n "scripts\|schedules\|variables\|templates\|notifications\|/rpa/statistics" src/lib/rpaApi.ts   # 期望零命中（含类型名 Script 等已删）
npx vitest run src/lib/download.test.ts --no-typecheck 2>&1 | tail -5   # 此刻 download.test.ts 仍引用 executionApi 会红——属预期，Task 2 修复；type-check 的 TS2305 指向该文件即符合预期
```

**done:** rpaApi.ts 导出 4 对象恰 17 方法；6 死族与全部死方法/死类型零残留；type-check 唯一残留错误来自 Task 2 待改的测试文件（TS2305），无其他文件受累

---

### Task 2: 测试面同步（重写/删除/摘除）

**read_first:**
- `xingran-react-frontend/src/lib/rpaApi.test.ts`（209 行）——现锁定 dead URL（:30-188）+「10 个子 API」断言（:192-208）；mock 基建 `vi.mock("@/lib/api")` + `vi.mock("./api")` 双 mock 模式（:9-16）
- `xingran-react-frontend/src/lib/__tests__/rpaApi.batch56.unit.test.ts`（83 行）——整文件删除对象
- `xingran-react-frontend/src/lib/download.test.ts` —— :54 executionApi import、:217-250 downloadReport describe
- `xingran-react-frontend/src/pages/operations/rpa/tasks/__tests__/index.test.tsx` :16-22 与 `workers/__tests__/index.test.tsx` :16-20 —— 空转 vi.mock

**action:**
1. **重写 `rpaApi.test.ts` 为存活契约基线**（Phase 100 版）：
   - 保留既有双 `vi.mock` post 打桩模式；import 收缩为 `{ aiApi, executionApi, rpaApi, taskApi, workerApi }`
   - URL+动词断言覆盖 17 方法逐一（沿用现文件 `expect(mockPost).toHaveBeenCalledWith(url, body)` 风格）：task 6（`/rpa/tasks/list`、`/rpa/tasks`、`/rpa/tasks/:id`、`/rpa/tasks/:id/update`、`/rpa/tasks/:id/delete`、`/rpa/tasks/:id/execute`）+ worker 2（`/rpa/workers/list`、`/rpa/workers/statistics`）+ execution 5（`/rpa/executions/list`、`/rpa/executions/:id`、`/rpa/executions/statistics`、`/rpa/executions/:id/cancel`、`/rpa/executions/:id/logs`）+ ai 4（`/rpa/ai/generate`、`/rpa/ai/optimize`、`/rpa/ai/decide`、`/rpa/ai/analyze-failure`）
   - **keys 结构断言**：`Object.keys(rpaApi).sort()` = `["ai","execution","task","worker"]`；四对象 keys 各自 sort 后等于 Context 端态表方法集（此为 D-100-2 的测试侧第一层锁）
   - 文件头注释：锁定的是「后端已注册路由的实测存活集」（非前端单方面 URL 声明）
2. **整文件删除** `src/lib/__tests__/rpaApi.batch56.unit.test.ts`（其 import 的 6 族对象已不存在）。
3. **`download.test.ts` 摘除 executionApi 耦合**：删 :54 的 executionApi import 与 :217-250 的 downloadReport describe 块；文件内其余（blobAxios/downloadFile/downloadFilePost 用例）不动。
4. **摘除两处空转 vi.mock**：tasks/workers 页面测试的 `vi.mock("@/lib/rpaApi", factory)` 整块删除（页面本身内联 post 直调 @/lib/api，从不 import rpaApi——删除后用例应全绿，若出现红的说明该页面实际有 rpaApi 依赖，停下 grep 页面 import 并把该依赖显式化后重估）。

**verify:**
```bash
cd xingran-react-frontend && npm run type-check && npx vitest run src/lib/rpaApi.test.ts src/lib/download.test.ts src/pages/operations/rpa/ && npm run lint 2>&1 | tail -3
```

**done:** type-check 0 错误；rpaApi.test.ts 17 方法 URL 断言 + keys 断言全绿；batch56 文件已删；download.test.ts 无 rpaApi 残留引用；RPA 页面测试全绿；lint 0 errors

---

### Task 3: RECONCILIATION.md 落盘 + invariants keys 基线守卫（D-100-2/D-100-5）

**read_first:**
- `xingran-react-frontend/src/lib/apiFactory.invariants.test.ts` —— HARD_ALLOWED(:65-69)/EXPECTED_FILES(:72-86)/WARNING_WHITELIST(:93-107)/断言结构；本任务只在文件**末尾追加** describe 块，不改既有 tier 结构（EXPECTED_FILES 扩容属 100-03 D-100-8）
- `100-RESEARCH.md` §2.1/§2.2 —— 六列表数据源
- Task 1 产物 rpaApi.ts —— 端态对照

**action:**
1. **新建 `.planning/phases/100-frontend-contract-fixes/RECONCILIATION.md`**（D-100-4）：
   - 头部：phase、日期、裁决依据（D-100-3/D-100-9/D-100-12）、后端真相源（rpa_router.go + router.go:953-968 挂载）
   - **六列对账表（方法/动词+路径/后端路由/裁决/理由）**：rpaApi 全族 116 方法逐行（按 taskApi/scriptApi/workerApi/executionApi/scheduleApi/variableApi/templateApi/aiApi/notificationApi/statisticsApi 分族子表；数据从 RESEARCH §2.2 转录，裁决列填 alive→保留 / dead→删除 / register+heartbeat→alive-but-deleted(D-100-12)）
   - **端态段**：17 方法清单 + 文件锚点
   - **observed-debt 段（D-100-9，不顺手扩 scope）**：RPA 三页面内联 post 直调既有约定债（tasks/index.tsx:18、executions/index.tsx:18、workers/index.tsx:39——绕过 apiFactory/xxxApi 层），登记不修
   - **守卫段**：invariants keys 基线 + rpaApi.test.ts 契约测试指针
2. **`apiFactory.invariants.test.ts` 末尾追加 keys 基线 describe**（D-100-2/D-100-5，RESEARCH §5 Pattern 1 形态）：import 4 个存活对象，`POST_CLEANUP_BASELINE: Record<string, string[]>` 逐对象 `expect(Object.keys(obj).sort()).toEqual(baseline)`——taskApi 6 / workerApi 2 / executionApi 5 / aiApi 4；注释注明「D-100-9 端态 + D-100-12 register/heartbeat 已删，回增/私删双向即红；vmApi/vdiServerApi 由 100-02 追加」。rpaApi.test.ts 的 keys 断言与此处基线数值必须一致（两处注释互指）。
3. **不改** HARD_ALLOWED（rpaApi.ts 保持 0——pick 形态不产生手写 CRUD 模板命中）、不改 EXPECTED_FILES（13→15 属 100-03）。

**verify:**
```bash
cd xingran-react-frontend && npx vitest run src/lib/apiFactory.invariants.test.ts && npm run lint 2>&1 | grep -c "error"   # 期望 0
test -f ../.planning/phases/100-frontend-contract-fixes/RECONCILIATION.md && echo EXISTS
grep -c "alive\|删除" ../.planning/phases/100-frontend-contract-fixes/RECONCILIATION.md   # 六列表已填充
```

**done:** RECONCILIATION.md 六列对账表 + observed-debt 段落盘；invariants keys 基线 4 对象全绿；全文件 lint 0 errors

---

## Verification

```bash
cd xingran-react-frontend
npm run lint          # 0 errors；warnings ≤ 1389（存量只许降）
npm run type-check    # 通过
npx vitest run        # 全量 0 失败（基线 554 文件/3800 tests；本计划删 1 测试文件 → 预期 ~553 文件，0 失败为硬标准）
npm run test:coverage && bash ../.github/scripts/check-frontend-coverage.sh coverage/coverage-final.json ../.coverage-fe-floors
grep -rn "from \"@/lib/rpaApi\"\|from \"./rpaApi\"" src/ --include="*.ts" --include="*.tsx" | grep -v "rpaApi.test.ts\|apiFactory.invariants.test.ts"   # 期望零命中（零生产/测试消费者）
```

**覆盖率 ratchet 专项（RESEARCH Pitfall 3）**：删除被 rpaApi.test.ts 高覆盖的死代码会同时缩 lib 覆盖率分子/分母，方向不可预判——coverage gate 必须实跑；若 `lib` 跌破 87.2 下限，优先核查 include 口径（重写后的 rpaApi.test.ts 计入分母），必要时在 rpaApi.test.ts 补存活方法的分支用例回填，**禁止下调 floors**（floors 只升不降）。

## Success Criteria

1. rpaApi.ts 端态 = 17 存活方法（task 6/worker 2/execution 5/ai 4），8 处 spread 幽灵方法零残留（D-100-1/D-100-9）
2. register/heartbeat 前端方法已删，后端公开路由未动（D-100-12）
3. 99 个死方法 + 6 个死族 + 死类型 + 死测试全量删除，全仓零引用（D-100-3/全局标准）
4. RECONCILIATION.md 六列对账表 116 方法 + observed-debt 段落盘（D-100-4/D-100-9）
5. invariants keys 基线锁定 4 对象方法集，回增即红（D-100-2/D-100-5）
6. 前端三 gate 全绿 + 45 dirs 覆盖率 ratchet 不破
