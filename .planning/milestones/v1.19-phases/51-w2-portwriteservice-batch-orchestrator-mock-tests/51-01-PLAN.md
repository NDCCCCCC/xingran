---
phase: 51-w2-portwriteservice-batch-orchestrator-mock-tests
plan: 01
type: execute
wave: 1
depends_on: ["50-01"]
files_modified:
  - internal/services/portwrite/port_write_service.go
  - internal/services/portwrite/parse_error.go
  - internal/services/portwrite/pre_state_check.go
  - internal/services/portwrite/batch_orchestrator.go
  - internal/services/portwrite/port_write_service_test.go
autonomous: true
requirements:
  - SSH-02
  - SSH-03
  - SSH-04
  - SSH-06
  - PORT-06
  - BATCH-02
  - BATCH-03
  - BATCH-04
  - AUDIT-04
user_setup: []

must_haves:
  truths:
    - "Phase 51 service layer compiles cleanly via `go build ./internal/services/portwrite/...`"
    - "`go test ./internal/services/portwrite/... -count=1` exits 0 with all subtests passing"
    - "`parseConfigError` distinguishes `WriteErrorTransport` from `WriteErrorDeviceRejected` for at least 10 marker inputs"
    - "`BatchWritePorts` rejects >50 ports via `ErrBatchTooLarge` before any SSH traffic"
    - "`BatchWritePorts` rejects empty `PortIDs` via `ErrEmptyBatch`"
    - "`BatchWritePorts` serial fail-fast loop stops on first `WriteErrorTransport` or `WriteErrorDeviceRejected`"
    - "Single-port methods return `NoOp=true` `PortResult` when PORT-06 pre-state already matches target state"
    - "Successful write paths invoke `collectionSvc.Enqueue(deviceID)` exactly once"
    - "Failed write paths do NOT invoke `collectionSvc.Enqueue(deviceID)`"
    - "Batch entry constructs detached `context.WithTimeout(context.Background(), 30*time.Minute)` (NOT inherited from caller)"
    - "Service field types use `portWriteExecutor` and `portWriteCollectionSvc` interfaces for mockability"
  artifacts:
    - path: internal/services/portwrite/port_write_service.go
      provides: "PortWriteService interface + portWriteServiceImpl + 5 single-port methods + executeWrite helper + interface types"
      min_lines: 180
      exports: ["PortWriteService", "PortResult", "BatchWriteRequest", "Action", "NewPortWriteService"]
    - path: internal/services/portwrite/parse_error.go
      provides: "parseConfigError + WriteError + WriteErrorKind + transport/rejection marker tables"
      min_lines: 90
    - path: internal/services/portwrite/pre_state_check.go
      provides: "checkPreState NoOp detector for all 5 actions"
      min_lines: 50
    - path: internal/services/portwrite/batch_orchestrator.go
      provides: "BatchWritePorts method + BatchResult struct with detached context + entry validation + fail-fast serial loop"
      min_lines: 130
    - path: internal/services/portwrite/port_write_service_test.go
      provides: "Mock-based unit tests covering all 5 single-port methods + batch + parseConfigError markers"
      min_lines: 350
  key_links:
    - from: internal/services/portwrite/port_write_service.go
      to: internal/services/portcollection/vendor_port_template.go
      via: "portcollection.RenderCommand(vendor, action, params)"
      pattern: "portcollection\\.RenderCommand"
    - from: internal/services/portwrite/port_write_service.go
      to: internal/device/executor.go
      via: "deviceExecutor.ExecuteCustom(ctx, deviceID, fn, timeout)"
      pattern: "deviceExecutor\\.ExecuteCustom"
    - from: internal/services/portwrite/port_write_service.go
      to: internal/services/device_info_collection_service.go
      via: "collectionSvc.Enqueue(deviceID) on success path"
      pattern: "collectionSvc\\.Enqueue"
    - from: internal/services/portwrite/batch_orchestrator.go
      to: context.WithTimeout
      via: "Detached context.Background() with 30min timeout (D-12)"
      pattern: "context\\.WithTimeout\\(context\\.Background\\(\\)"
    - from: internal/services/portwrite/port_write_service_test.go
      to: internal/services/portwrite/port_write_service.go
      via: "testify/mock.Mock embedded mockDeviceExecutor + mockCollectionSvc"
      pattern: "mock\\.Mock"
---

<objective>
实现 v1.19 网络设备写操作的 service 层 + 批量编排器 + 纯 mock 单测，为 Phase 52 HTTP/audit/permission handler 提供稳定契约。

**核心产出**：
1. `internal/services/portwrite/` 包（新建）— service 接口 + 私有 impl + 5 单端口方法 + 1 批量方法
2. `parseConfigError` — 区分 `transport_error` vs `device_rejected` 的纯函数（D-16）
3. `checkPreState` — PORT-06 NoOp 判定器（D-13 + D-14）
4. `BatchWritePorts` — detached 30min context + 串行 fail-fast + maxBatchSize=50 入口校验（D-12 + D-17）
5. mock-based 单测 — `mockDeviceExecutor` + `mockCollectionSvc` + 10+ `parseConfigError` 表驱动用例（D-18）

**决策依据**：本 phase 严格遵循 51-CONTEXT.md D-10..D-18 + 51-RESEARCH.md 验证的现有基础设施（`device.DeviceExecutor.ExecuteCustom` / `device.ScrapliWrapper.SendConfigs` / `services.DeviceInfoCollectionService.Enqueue` / `models.DevicePortStatus` / `portcollection.RenderCommand`），零新外部依赖。Service 字段类型采用 interface（`portWriteExecutor` / `portWriteCollectionSvc`）以启用 testify/mock，但工厂函数 `NewPortWriteService` 仍接受 `*device.DeviceExecutor` 与 `*services.DeviceInfoCollectionService` concrete 类型（Phase 52 router 无需变更）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Phase 锁定决策（必读 — D-10..D-18）
@.planning/phases/51-w2-portwriteservice-batch-orchestrator-mock-tests/51-CONTEXT.md
@.planning/phases/51-w2-portwriteservice-batch-orchestrator-mock-tests/51-RESEARCH.md

# Phase 50 契约（直接消费 — `portcollection.RenderCommand`）
@.planning/phases/50-w1-vendor-templates-unit-tests-vendor-action-command-map/50-01-SUMMARY.md
@internal/services/portcollection/vendor_port_template.go

# 直接调用的基础设施
@internal/device/executor.go
@internal/device/scrapli_wrapper.go
@internal/models/device_port_status.go
@internal/services/device_info_collection_service.go

# 测试惯例参考
@internal/services/operations/cache_invalidator_test.go

# 服务接口模式
@internal/services/operations/building_service.go
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From internal/services/portcollection/vendor_port_template.go:
```go
type PortAction string
const (
    ActionShutdown     PortAction = "shutdown"
    ActionUndoShutdown PortAction = "undo_shutdown"
    ActionDescription  PortAction = "description"
    ActionDot1xEnable  PortAction = "dot1x_enable"
    ActionDot1xDisable PortAction = "dot1x_disable"
)
type PortTemplateParams struct {
    InterfaceName string
    Description   string
}
func RenderCommand(vendor models.DeviceVendor, action PortAction, params PortTemplateParams) ([]string, error)
```

From internal/device/executor.go (signature line 152):
```go
func (e *DeviceExecutor) ExecuteCustom(
    ctx context.Context,
    deviceID string,
    executeFunc func(context.Context, *PooledConnection) error,
    timeout time.Duration,
) error
```

From internal/device/scrapli_wrapper.go (line 594 + 666):
```go
func (w *ScrapliWrapper) SendConfigs(configs []string) ([]*Response, error)
type Response struct {
    Result   string
    Started  time.Time
    Finished time.Time
    Failed   bool
}
```

From internal/models/device_port_status.go (line 31):
```go
type DevicePortStatus struct {
    ID            string  // UUID primary key
    DeviceID      string  // UUID foreign key
    InterfaceName string
    AdminStatus   string  // "up" | "down"
    OperStatus    string
    Description   string
    Dot1xEnabled  bool
    // ...
}
func (DevicePortStatus) TableName() string { return "sys_device_port_status" }
```

From internal/services/device_info_collection_service.go (line 133):
```go
func (s *DeviceInfoCollectionService) Enqueue(deviceID string) error
```

From internal/device/connection_pool.go (line 154):
```go
func (pc *PooledConnection) GetWrapper() *ScrapliWrapper
```

From internal/models/network_device.go (line 15):
```go
type DeviceVendor string
const (
    VendorHuawei DeviceVendor = "huawei"
    VendorH3C    DeviceVendor = "h3c"
    VendorRuijie DeviceVendor = "ruijie"
)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create parse_error.go — WriteError type + marker tables + parseConfigError</name>
  <files>internal/services/portwrite/parse_error.go</files>
  <read_first>
    - internal/device/scrapli_wrapper.go (line 666 — Response struct shape)
    - .planning/phases/51-w2-portwriteservice-batch-orchestrator-mock-tests/51-RESEARCH.md (Pattern 2 — parseConfigError implementation reference)
    - .planning/phases/51-w2-portwriteservice-batch-orchestrator-mock-tests/51-CONTEXT.md (D-16 — marker priority order specification)
  </read_first>
  <action>
    Create file `internal/services/portwrite/parse_error.go` containing (per D-16 + RESEARCH.md Pattern 2):

    1. Package declaration: `package portwrite`

    2. Imports: `errors`, `fmt`, `strings`, `github.com/xingran-next/xingran-go-backend/internal/device`

    3. Type `WriteErrorKind int` with constants `WriteErrorNone`, `WriteErrorTransport`, `WriteErrorDeviceRejected` (iota-based in this order)

    4. Type `WriteError struct { Kind WriteErrorKind; Cause error; Message string }` with:
       - `func (e *WriteError) Error() string` — returns `fmt.Sprintf("write error (%s): %s", errorKindName(e.Kind), e.Message)`; if Cause != nil, append `: <Cause.Error()>`
       - `func (e *WriteError) Unwrap() error { return e.Cause }`

    5. Helper `func errorKindName(k WriteErrorKind) string` returning "transport" / "device_rejected" / "none"

    6. Package-level slice `transportMarkers = []string{"connection refused", "timeout", "EOF", "i/o timeout"}` (lowercase substrings matched against ToLower result)

    7. Package-level slice `rejectionMarkers = []string{"% Error:", "% Input error", "Error: ", "Unrecognized command", "Unknown command", "Illegal", "Invalid", "Wrong parameter"}` (case-sensitive substring match)

    8. Function `func parseConfigError(resp *device.Response) error` implementing the 5-step priority scan (per D-16):
       - Step 1: `resp == nil` -> return `&WriteError{Kind: WriteErrorTransport, Message: "nil response"}`
       - Step 2: `resp.Err != nil` -> return `&WriteError{Kind: WriteErrorTransport, Cause: resp.Err}`
       - Step 3: `resp.Result == ""` -> return nil (empty = success)
       - Step 4: lower-case resp.Result, iterate transportMarkers with `strings.Contains(lower, strings.ToLower(m))` -> on hit return `&WriteError{Kind: WriteErrorTransport, Message: resp.Result}`
       - Step 5: iterate rejectionMarkers with `strings.Contains(resp.Result, m)` (case-sensitive) -> on hit return `&WriteError{Kind: WriteErrorDeviceRejected, Message: resp.Result}`
       - Default: return nil

    9. Helper functions for batch_orchestrator.go:
       - `func isTransportError(err error) bool` — uses `errors.As` to extract `*WriteError`, returns `we.Kind == WriteErrorTransport`
       - `func isDeviceRejected(err error) bool` — uses `errors.As`, returns `we.Kind == WriteErrorDeviceRejected`

    Do NOT include any code blocks for `RenderCommand` or other service logic in this file. This file is pure parsing — keep it focused.

    Marker priority is LOCKED: transport markers MUST be scanned BEFORE rejection markers (Pitfall #3 — `% Error: timeout` would otherwise be misclassified; with priority order, the device's literal error message is preserved as `WriteErrorDeviceRejected` containing the "timeout" substring in `Message` field).
  </action>
  <verify>
    <automated>go build ./internal/services/portwrite/...</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/services/portwrite/parse_error.go` exists
    - Package declaration: `package portwrite`
    - Exports: `WriteError` type + `WriteErrorKind` type + `WriteErrorNone`/`WriteErrorTransport`/`WriteErrorDeviceRejected` consts
    - Unexported: `parseConfigError`, `isTransportError`, `isDeviceRejected`, `transportMarkers`, `rejectionMarkers`, `errorKindName`
    - `go build ./internal/services/portwrite/...` exits 0
    - Marker ordering: `transportMarkers` declared before `rejectionMarkers` and scanned first in `parseConfigError`
    - 5-step priority implemented exactly: nil -> Err set -> empty Result -> transport scan -> rejection scan -> default nil
    - transportMarkers contains exactly: "connection refused", "timeout", "EOF", "i/o timeout"
    - rejectionMarkers contains at minimum: "% Error:", "Unrecognized command", "Illegal", "Wrong parameter"
    - `WriteError.Unwrap()` returns the Cause field (verified by `errors.As`/`errors.Unwrap` compatibility)
  </acceptance_criteria>
  <done>
    `parse_error.go` compiles cleanly; `WriteError` is `errors.As`-compatible (returns proper Kind); `parseConfigError` correctly classifies all 5 priority tiers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create pre_state_check.go — checkPreState NoOp detector</name>
  <files>internal/services/portwrite/pre_state_check.go</files>
  <read_first>
    - internal/models/device_port_status.go (line 31 — AdminStatus/Dot1xEnabled/Description field definitions)
    - internal/services/portcollection/vendor_port_template.go (line 11-19 — PortAction constants)
    - .planning/phases/51-w2-portwriteservice-batch-orchestrator-mock-tests/51-CONTEXT.md (D-13 — pre-state match logic)
  </read_first>
  <action>
    Create file `internal/services/portwrite/pre_state_check.go` containing (per D-13 + D-14 + RESEARCH.md Pattern 5):

    1. Package declaration: `package portwrite`

    2. Imports: `github.com/xingran-next/xingran-go-backend/internal/models`, `github.com/xingran-next/xingran-go-backend/internal/services/portcollection`

    3. Method `func (s *portWriteServiceImpl) checkPreState(port *models.DevicePortStatus, action Action, desc string) *PortResult` — for each `Action` value (using portcollection constants `ActionShutdown`, `ActionUndoShutdown`, `ActionDescription`, `ActionDot1xEnable`, `ActionDot1xDisable`):

       - `ActionShutdown`: if `port.AdminStatus == "down"` -> return `&PortResult{PortID: port.ID, Action: action, Status: "skipped", NoOp: true, CurrentState: "admin_down"}`
       - `ActionUndoShutdown`: if `port.AdminStatus == "up"` -> return `&PortResult{PortID: port.ID, Action: action, Status: "skipped", NoOp: true, CurrentState: "admin_up"}`
       - `ActionDot1xEnable`: if `port.Dot1xEnabled` is true -> return `&PortResult{PortID: port.ID, Action: action, Status: "skipped", NoOp: true, CurrentState: "dot1x_enabled"}`
       - `ActionDot1xDisable`: if `port.Dot1xEnabled` is false -> return `&PortResult{PortID: port.ID, Action: action, Status: "skipped", NoOp: true, CurrentState: "dot1x_disabled"}`
       - `ActionDescription`: if `port.Description == desc` -> return `&PortResult{PortID: port.ID, Action: action, Status: "skipped", NoOp: true, CurrentState: "description_match"}`

       Returns nil when no match (action should proceed to SSH execution).

    4. The receiver `(s *portWriteServiceImpl)` is used even though `s` is unused inside the function body — Go requires receiver for method on struct. Keep receiver for future extension (e.g., cache lookup).

    Do NOT include any SSH execution or batch logic in this file. It is a pure NoOp detector.
  </action>
  <verify>
    <automated>go build ./internal/services/portwrite/...</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/services/portwrite/pre_state_check.go` exists
    - Method signature exactly: `func (s *portWriteServiceImpl) checkPreState(port *models.DevicePortStatus, action Action, desc string) *PortResult`
    - Returns `*PortResult` with `Status: "skipped"`, `NoOp: true`, `CurrentState` set per action, when pre-state matches
    - Returns nil when pre-state does NOT match (action should proceed to SSH)
    - Switch covers all 5 PortAction constants: ActionShutdown, ActionUndoShutdown, ActionDescription, ActionDot1xEnable, ActionDot1xDisable
    - `go build ./internal/services/portwrite/...` exits 0 (depends on Task 3 types being declared in same package — verify Task 3 exists or compile together at end)
  </acceptance_criteria>
  <done>
    `pre_state_check.go` compiles; `checkPreState` correctly returns NoOp PortResult for all 5 actions when DB state already matches target; returns nil for proceed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create port_write_service.go — interface, types, sentinel errors, factory, single-port methods, executeWrite</name>
  <files>internal/services/portwrite/port_write_service.go</files>
  <read_first>
    - internal/services/portcollection/vendor_port_template.go (full file — RenderCommand signature + PortAction constants)
    - internal/device/executor.go (line 32-189 — DeviceExecutor + ExecuteCustom signature)
    - internal/device/connection_pool.go (line 154 — PooledConnection.GetWrapper)
    - internal/device/scrapli_wrapper.go (line 594 — ScrapliWrapper.SendConfigs signature)
    - internal/models/device_port_status.go (line 31-72 — full DevicePortStatus struct + TableName)
    - internal/models/network_device.go (line 14-22 — DeviceVendor constants)
    - internal/services/device_info_collection_service.go (line 36-163 — DeviceInfoCollectionService struct + Enqueue signature)
    - .planning/phases/51-w2-portwriteservice-batch-orchestrator-mock-tests/51-CONTEXT.md (D-10/D-11/D-13/D-14/D-15 — service contract spec)
    - .planning/phases/51-w2-portwriteservice-batch-orchestrator-mock-tests/51-RESEARCH.md (Pattern 1 + Pattern 3 — service interface + ExecuteCustom wrapper)
  </read_first>
  <action>
    Create file `internal/services/portwrite/port_write_service.go` containing (per D-10/D-11/D-13/D-14/D-15 + RESEARCH.md Patterns 1+3 + Architectural Adjustment from RESEARCH):

    1. Package declaration: `package portwrite`

    2. Imports: `context`, `errors`, `fmt`, `strings`, `time`, `github.com/xingran-next/xingran-go-backend/internal/device`, `github.com/xingran-next/xingran-go-backend/internal/models`, `github.com/xingran-next/xingran-go-backend/internal/services`, `github.com/xingran-next/xingran-go-backend/internal/services/portcollection`, `gorm.io/gorm`

    3. Sentinel errors block:
       - `var ErrBatchTooLarge = errors.New("portwrite: batch exceeds max size of 50")`
       - `var ErrEmptyBatch = errors.New("portwrite: batch is empty")`
       - `var ErrMixedDevices = errors.New("portwrite: batch contains ports from different devices")`
       - `var ErrPortNotFound = errors.New("portwrite: port not found")`
       - `var ErrDeviceNotFound = errors.New("portwrite: device not found")`

    4. Type alias: `type Action = portcollection.PortAction`

    5. Result types:
       - `type PortResult struct` with fields: `PortID string`, `Action Action`, `Status string` (json tag), `NoOp bool`, `CurrentState string` (omitempty), `Error string` (omitempty), `CommandSent string` (omitempty — unmasked audit source of truth per D-15)
       - `type BatchWriteRequest struct` with fields: `DeviceID string`, `Action Action`, `PortIDs []string`, `Description string` (omitempty)

    6. Constants: `const maxBatchSize = 50`, `const singlePortTimeout = 30 * time.Second` (D-11), `const batchPortTimeout = 60 * time.Second` (D-11), `const batchDetachedTimeout = 30 * time.Minute` (D-12)

    7. Internal interfaces (Architectural Adjustment from RESEARCH section Open Questions #1+#2 — for mockability):
       - `type portWriteExecutor interface { ExecuteCustom(ctx context.Context, deviceID string, fn func(context.Context, *device.PooledConnection) error, timeout time.Duration) error }`
       - `type portWriteCollectionSvc interface { Enqueue(deviceID string) error }`

    8. Service interface `PortWriteService` with 6 methods:
       - `Shutdown(ctx context.Context, portID string, operator string) (*PortResult, error)`
       - `UndoShutdown(ctx context.Context, portID string, operator string) (*PortResult, error)`
       - `SetDescription(ctx context.Context, portID string, desc string, operator string) (*PortResult, error)`
       - `EnableDot1x(ctx context.Context, portID string, operator string) (*PortResult, error)`
       - `DisableDot1x(ctx context.Context, portID string, operator string) (*PortResult, error)`
       - `BatchWritePorts(ctx context.Context, req BatchWriteRequest, operator string) (*BatchResult, error)`

    9. Private struct `portWriteServiceImpl` with fields:
       - `db *gorm.DB`
       - `deviceExecutor portWriteExecutor` (interface, NOT concrete pointer — D-18 + Research section Open Questions #1)
       - `collectionSvc portWriteCollectionSvc` (interface, NOT concrete pointer — D-18 + Research section Open Questions #2)

    10. Factory `func NewPortWriteService(db *gorm.DB, deviceExecutor *device.DeviceExecutor, collectionSvc *services.DeviceInfoCollectionService) PortWriteService` — accepts CONCRETE pointer types (Phase 52 router compatibility), assigns to interface fields internally. Returns `PortWriteService` (interface return type per project convention — `internal/services/operations/building_service.go` style).

    11. Five single-port method implementations on `*portWriteServiceImpl` (Shutdown/UndoShutdown/EnableDot1x/DisableDot1x delegate to `writeSinglePort` with the corresponding Action; SetDescription delegates to `writeSinglePort` with `ActionDescription` + desc parameter):

    12. Private helper `func (s *portWriteServiceImpl) writeSinglePort(ctx context.Context, portID string, action Action, desc string, operator string) (*PortResult, error)`:
       - Loads `var port models.DevicePortStatus` via `s.db.WithContext(ctx).Where("id = ?", portID).First(&port)`
       - On `gorm.ErrRecordNotFound`: fallback skip pre-state check, call `s.executeWrite(ctx, portID, "", action, desc, operator, "")` (D-13 — DB row missing -> direct execution)
       - On other DB error: return nil + wrapped error
       - Call `s.checkPreState(&port, action, desc)` — if non-nil, return it
       - Otherwise call `s.executeWrite(ctx, port.ID, port.DeviceID, action, desc, operator, port.InterfaceName)`

    13. Private helper `func (s *portWriteServiceImpl) executeWrite(ctx context.Context, portID string, deviceID string, action Action, desc string, operator string, interfaceName string) (*PortResult, error)`:
       - If `deviceID == ""`: return nil + ErrDeviceNotFound (D-13 fallback path skips device lookup)
       - Load `var device models.NetworkDevice` via `s.db.WithContext(ctx).Where("id = ?", deviceID).First(&device)`
       - On DB error: return nil + wrapped error
       - Call `portcollection.RenderCommand(device.Vendor, action, portcollection.PortTemplateParams{InterfaceName: interfaceName, Description: desc})` — propagate error if RenderCommand returns one
       - Set `commandsJoined := strings.Join(cmds, " | ")` for `CommandSent` field
       - Declare `var lastResp *device.Response` (captured by closure)
       - Call `s.deviceExecutor.ExecuteCustom(ctx, deviceID, func(execCtx context.Context, pc *device.PooledConnection) error { wrapper := pc.GetWrapper(); responses, err := wrapper.SendConfigs(cmds); if err != nil { return err }; if len(responses) > 0 { lastResp = responses[len(responses)-1] }; return nil }, singlePortTimeout)` (D-11 = 30s per-port timeout)
       - If executeErr != nil: return `&PortResult{PortID: portID, Action: action, Status: "failed", Error: executeErr.Error(), CommandSent: commandsJoined}`, executeErr (Pitfall #6 — NO Enqueue on failure)
       - If parseErr := `parseConfigError(lastResp)`; parseErr != nil: return `&PortResult{PortID: portID, Action: action, Status: "failed", Error: parseErr.Error(), CommandSent: commandsJoined}`, parseErr (no Enqueue)
       - SUCCESS PATH: if `s.collectionSvc != nil` (interface nil-check for tests): `_ = s.collectionSvc.Enqueue(deviceID)` (fire-and-forget — D-15 AUDIT-04)
       - Return `&PortResult{PortID: portID, Action: action, Status: "succeeded", CommandSent: commandsJoined}`, nil

    The single-port methods are SHIM methods that simply forward to `writeSinglePort` with the appropriate Action. `BatchWritePorts` is implemented in `batch_orchestrator.go` (separate file per plan_structure_hint's Recommended Project Structure).

    IMPORTANT — receiver methods: All 5 single-port public methods + 2 private helpers (`writeSinglePort`, `executeWrite`) must use receiver `(s *portWriteServiceImpl)` to access struct fields. The `operator` parameter is currently unused in executeWrite — kept for Phase 52 audit signature compatibility (handler passes it; service receives and threads it for future use).

    IMPORTANT — interface vs concrete pointer in struct: `deviceExecutor` field MUST be of interface type `portWriteExecutor` (NOT `*device.DeviceExecutor`) so the testify/mock in Task 7 can satisfy the field. Same for `collectionSvc` field -> `portWriteCollectionSvc`. Factory `NewPortWriteService` accepts concrete pointer params and assigns to interface fields.

    Do NOT include batch logic or pre_state_check logic in this file — those go in `batch_orchestrator.go` (Task 4) and `pre_state_check.go` (Task 2) respectively.
  </action>
  <verify>
    <automated>go build ./internal/services/portwrite/...</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/services/portwrite/port_write_service.go` exists
    - Package declaration: `package portwrite`
    - Exports: `PortWriteService`, `PortResult`, `BatchWriteRequest`, `Action`, `NewPortWriteService`, sentinel errors `ErrBatchTooLarge`, `ErrEmptyBatch`, `ErrMixedDevices`, `ErrPortNotFound`, `ErrDeviceNotFound`
    - `Action = portcollection.PortAction` alias declared
    - `maxBatchSize = 50` constant declared
    - `singlePortTimeout = 30 * time.Second`, `batchPortTimeout = 60 * time.Second`, `batchDetachedTimeout = 30 * time.Minute` constants declared
    - Internal interfaces `portWriteExecutor` (with ExecuteCustom method) and `portWriteCollectionSvc` (with Enqueue method) declared
    - `portWriteServiceImpl` struct fields: `db *gorm.DB`, `deviceExecutor portWriteExecutor`, `collectionSvc portWriteCollectionSvc` (all 3 fields present, types as specified)
    - Factory `NewPortWriteService(db *gorm.DB, deviceExecutor *device.DeviceExecutor, collectionSvc *services.DeviceInfoCollectionService) PortWriteService` — accepts concrete pointers, returns interface
    - All 5 single-port methods (Shutdown/UndoShutdown/SetDescription/EnableDot1x/DisableDot1x) implemented on `*portWriteServiceImpl`
    - Private helper `writeSinglePort` exists with DB pre-state lookup + ErrRecordNotFound fallback + checkPreState gate
    - Private helper `executeWrite` exists with: device DB lookup, RenderCommand call, ExecuteCustom with singlePortTimeout, parseConfigError classification, success-only Enqueue call
    - `go build ./internal/services/portwrite/...` exits 0
  </acceptance_criteria>
  <done>
    `port_write_service.go` compiles; all 5 single-port methods + executeWrite + writeSinglePort + factory + interface types + sentinel errors + consts wired correctly. Service fields are interfaces for mockability. Factory accepts concrete pointers for Phase 52 router compatibility.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create batch_orchestrator.go — BatchWritePorts with detached context + fail-fast serial loop</name>
  <files>internal/services/portwrite/batch_orchestrator.go</files>
  <read_first>
    - internal/services/portwrite/port_write_service.go (created in Task 3 — must read to confirm executeWrite signature + portWriteServiceImpl struct shape)
    - internal/services/portwrite/parse_error.go (created in Task 1 — for isTransportError/isDeviceRejected helpers)
    - internal/services/portwrite/pre_state_check.go (created in Task 2 — for checkPreState signature)
    - .planning/phases/51-w2-portwriteservice-batch-orchestrator-mock-tests/51-CONTEXT.md (D-12 + D-14 + D-17 — batch contract spec)
    - .planning/phases/51-w2-portwriteservice-batch-orchestrator-mock-tests/51-RESEARCH.md (Pattern 4 — BatchWritePorts implementation reference)
  </read_first>
  <action>
    Create file `internal/services/portwrite/batch_orchestrator.go` containing (per D-12 + D-14 + D-17 + RESEARCH.md Pattern 4):

    1. Package declaration: `package portwrite`

    2. Imports: `context`, `errors`, `fmt`, `github.com/xingran-next/xingran-go-backend/internal/models`

    3. Type `BatchResult struct { Succeeded []PortResult; Failed []PortResult; Skipped []PortResult }` — all 3 fields are slices (D-14 — three arrays coexist, all single-field populated)

    4. Method `func (s *portWriteServiceImpl) BatchWritePorts(ctx context.Context, req BatchWriteRequest, operator string) (*BatchResult, error)`:
       - FIRST LINE: `ctx, cancel := context.WithTimeout(context.Background(), batchDetachedTimeout); defer cancel()` (D-12 — detached 30min context, NOT inherited from caller; this is the critical Pitfall #5 mitigation)
       - Entry validation block (NO SSH traffic — all 3 checks before any DB/ExecuteCustom call):
         - `if len(req.PortIDs) == 0 { return nil, ErrEmptyBatch }`
         - `if len(req.PortIDs) > maxBatchSize { return nil, fmt.Errorf("%w: got %d", ErrBatchTooLarge, len(req.PortIDs)) }`
         - `if req.DeviceID == "" { return nil, errors.New("portwrite: deviceId is required") }` (the spec mentioned `ErrMixedDevices` for cross-device but D-17's explicit missing-deviceID wasn't covered — use a sentinel or fmt.Errorf; recommend inline errors.New for this case)
       - Initialize `result := &BatchResult{Succeeded: []PortResult{}, Failed: []PortResult{}, Skipped: []PortResult{}}` (D-14 — all 3 slices initialized so JSON serialization shows empty arrays not null)
       - Bulk pre-state query: `var ports []models.DevicePortStatus; if err := s.db.WithContext(ctx).Where("device_id = ? AND id IN ?", req.DeviceID, req.PortIDs).Find(&ports).Error; err != nil { return nil, fmt.Errorf("query ports: %w", err) }`
       - Build `preStateMap := make(map[string]models.DevicePortStatus, len(ports))` for O(1) lookup
       - D-17 serial fail-fast loop:
         - `for _, portID := range req.PortIDs`:
           - `port, exists := preStateMap[portID]`
           - If NOT exists (port "消失" — D-13 fallback): call `writeResult, err := s.executeWrite(ctx, portID, req.DeviceID, req.Action, req.Description, operator, "")` (empty interfaceName); if err != nil { append writeResult to result.Failed; break (fail-fast) }; if writeResult.NoOp { append to result.Skipped; continue }; append to result.Succeeded; continue
           - Else (port exists): call `noopResult := s.checkPreState(&port, req.Action, req.Description)`; if non-nil { append to result.Skipped; continue }
           - Call `writeResult, err := s.executeWrite(ctx, port.ID, port.DeviceID, req.Action, req.Description, operator, port.InterfaceName)`
           - If err != nil: append writeResult to result.Failed; `if isTransportError(err) || isDeviceRejected(err) { break }` (fail-fast D-17); also `break` for any other error type (Pitfall prevention — batch stops on first non-success)
           - If writeResult.NoOp: append to result.Skipped; continue
           - Else: append to result.Succeeded
       - Return result, nil

    IMPORTANT: Detached context is the FIRST statement — does NOT use the `ctx` parameter for the 30min timeout derivation. The `ctx` parameter is only used for `s.db.WithContext(ctx)` (which still uses detached ctx because we shadowed the variable).

    IMPORTANT: All 3 result slices are populated even when empty. JSON marshalling must produce `[]` not `null` — initialize slices with `make([]PortResult, 0)` or literal `[]PortResult{}` to guarantee this.

    IMPORTANT: Fail-fast break is INSIDE the loop body. The loop variable `portID` is used for the failed port's identifier — writeResult.PortID should already be populated by executeWrite.
  </action>
  <verify>
    <automated>go build ./internal/services/portwrite/...</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/services/portwrite/batch_orchestrator.go` exists
    - Package declaration: `package portwrite`
    - Exports: `BatchResult` struct with 3 fields (Succeeded, Failed, Skipped — all `[]PortResult`)
    - Method `BatchWritePorts(ctx context.Context, req BatchWriteRequest, operator string) (*BatchResult, error)` on `*portWriteServiceImpl`
    - FIRST executable line uses `context.WithTimeout(context.Background(), batchDetachedTimeout)` (NOT `context.WithTimeout(ctx, ...)`)
    - Entry validation order: empty -> exceeds max -> deviceID empty (all before any DB query or ExecuteCustom call)
    - `len(req.PortIDs) > maxBatchSize` returns wrapped error: `fmt.Errorf("%w: got %d", ErrBatchTooLarge, len(req.PortIDs))`
    - `len(req.PortIDs) == 0` returns `ErrEmptyBatch` (unwrapped, use `errors.Is`-compatible comparison)
    - Bulk pre-state query uses `s.db.WithContext(ctx).Where("device_id = ? AND id IN ?", req.DeviceID, req.PortIDs).Find(&ports)`
    - Fail-fast break occurs on `isTransportError(err) || isDeviceRejected(err)` per D-17
    - Result slices initialized with `[]PortResult{}` (not nil) so JSON marshals to `[]` not `null`
    - Port "消失" fallback (portID not in preStateMap): executes write directly via `executeWrite(ctx, portID, req.DeviceID, ..., "")`
    - `go build ./internal/services/portwrite/...` exits 0
  </acceptance_criteria>
  <done>
    `batch_orchestrator.go` compiles; `BatchWritePorts` enforces maxBatchSize=50 + empty rejection + detached 30min context + serial fail-fast loop with pre-state skip via D-13 fallback path.
  </done>
</task>

<task type="auto">
  <name>Task 5: Create port_write_service_test.go — mock structs + TestParseConfigError table-driven tests (12+ cases)</name>
  <files>internal/services/portwrite/port_write_service_test.go</files>
  <read_first>
    - internal/services/operations/cache_invalidator_test.go (full file — testify/mock embedding pattern)
    - internal/services/portwrite/parse_error.go (created in Task 1 — parseConfigError signature)
    - internal/services/portwrite/port_write_service.go (created in Task 3 — service struct + interface types)
    - internal/device/executor.go (line 152 — ExecuteCustom signature for mock)
    - .planning/phases/51-w2-portwriteservice-batch-orchestrator-mock-tests/51-CONTEXT.md (D-16 — marker test cases)
  </read_first>
  <action>
    Create file `internal/services/portwrite/port_write_service_test.go` containing (per D-16 + D-18):

    1. Package declaration: `package portwrite`

    2. Imports: `context`, `errors`, `testing`, `time`, `github.com/xingran-next/xingran-go-backend/internal/device`, `github.com/xingran-next/xingran-go-backend/internal/services`, `github.com/stretchr/testify/assert`, `github.com/stretchr/testify/mock`

    3. Compile-time interface satisfaction assertions (place at top of test file, immediately after imports; lock the mockability contract per checker fix_hint for Task 5):
       ```go
       // Compile-time interface satisfaction assertions - lock the mockability contract.
       // If portWriteServiceImpl.deviceExecutor / collectionSvc fields ever drift from these
       // interface shapes (D-18 architectural adjustment), this file fails to compile,
       // catching regressions at build time instead of at runtime mock injection.
       var _ portWriteExecutor = (*device.DeviceExecutor)(nil)
       var _ portWriteCollectionSvc = (*services.DeviceInfoCollectionService)(nil)
       ```

    4. Mock struct definitions (inline, per D-18 Claude's discretion):
       - `type mockDeviceExecutor struct { mock.Mock }` with method:
         - `func (m *mockDeviceExecutor) ExecuteCustom(ctx context.Context, deviceID string, fn func(context.Context, *device.PooledConnection) error, timeout time.Duration) error`
         - Implementation: `args := m.Called(ctx, deviceID, fn, timeout)`; then if `fn != nil` invoke `if err := fn(ctx, nil); err != nil { return err }`; finally return `args.Error(0)`
       - `type mockCollectionSvc struct { mock.Mock }` with method:
         - `func (m *mockCollectionSvc) Enqueue(deviceID string) error { args := m.Called(deviceID); return args.Error(0) }`

       Both mocks embed `mock.Mock` and use `m.Called(...)` / `args.X(0)` / `args.Error(N)` pattern (per cache_invalidator_test.go style).

    5. Test `TestParseConfigError` — table-driven with AT LEAST 12 cases covering D-16 marker priority:
       - Case names (each is a t.Run subtest):
         - `"huawei_percent_error_unrecognized"` — `&device.Response{Result: "% Error: Unrecognized command found at '^'."}` -> WriteErrorDeviceRejected, err != nil
         - `"huawei_percent_input_error"` — `&device.Response{Result: "% Input error!"}` -> WriteErrorDeviceRejected, err != nil
         - `"h3c_wrong_parameter"` — `&device.Response{Result: "% Wrong parameter found at '^'."}` -> WriteErrorDeviceRejected, err != nil
         - `"ruijie_unrecognized_command"` — `&device.Response{Result: "Unrecognized command"}` -> WriteErrorDeviceRejected, err != nil
         - `"ruijie_unknown_command"` — `&device.Response{Result: "Unknown command"}` -> WriteErrorDeviceRejected, err != nil
         - `"illegal_param_value"` — `&device.Response{Result: "Illegal parameter value at '^'."}` -> WriteErrorDeviceRejected, err != nil
         - `"invalid_input"` — `&device.Response{Result: "Invalid input detected"}` -> WriteErrorDeviceRejected, err != nil
         - `"huawei_ok_info"` — `&device.Response{Result: "Info: configuration succeeded"}` -> WriteErrorNone, err == nil
         - `"huawei_empty_result"` — `&device.Response{Result: ""}` -> WriteErrorNone, err == nil
         - `"nil_response"` — `nil` -> WriteErrorTransport, err != nil
         - `"err_set_io_timeout"` — `&device.Response{Err: errors.New("i/o timeout")}` -> WriteErrorTransport, err != nil
         - `"connection_refused_text"` — `&device.Response{Result: "connection refused"}` -> WriteErrorTransport, err != nil
         - `"timeout_text_mixed"` — `&device.Response{Result: "i/o timeout while writing"}` -> WriteErrorTransport, err != nil
         - `"eof_text"` — `&device.Response{Result: "unexpected EOF"}` -> WriteErrorTransport, err != nil
         - `"percent_error_with_timeout_substring"` (Pitfall #3 boundary) — `&device.Response{Result: "% Error: connection timeout occurred"}` -> WriteErrorDeviceRejected, err != nil (proves transport marker is NOT scanned first when Result starts with "% Error:")

    6. Test `TestIsTransportError` and `TestIsDeviceRejected` — simple unit tests verifying `errors.As` extraction works for both kinds and returns false for non-WriteError errors (e.g., `errors.New("plain")`).

    7. Test `TestWriteErrorUnwrap` — verifies `WriteError.Unwrap()` returns Cause field for `errors.Is`/`errors.As` chain.

    The test file uses ONLY pure-function tests for Task 5 (no DB, no SSH mocks invoked). Tests covering service-level flows (single-port methods, batch) come in Tasks 6+7.

    Use `assert.Error(t, err)` + `var we *WriteError; assert.True(t, errors.As(err, &we)); assert.Equal(t, tt.wantKind, we.Kind)` pattern for verification.
  </action>
  <verify>
    <automated>go test ./internal/services/portwrite/... -run TestParseConfigError -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/services/portwrite/port_write_service_test.go` exists
    - Package declaration: `package portwrite`
    - Mock structs `mockDeviceExecutor` and `mockCollectionSvc` embed `mock.Mock` and implement interface signatures
    - `mockDeviceExecutor.ExecuteCustom` accepts `(ctx, deviceID, fn, timeout)` and returns the mock's pre-configured error
    - `mockCollectionSvc.Enqueue` accepts `deviceID` and returns the mock's pre-configured error
    - `TestParseConfigError` table-driven test with AT LEAST 12 cases (verify by counting t.Run calls or table entries)
    - At least 4 cases asserting WriteErrorDeviceRejected (Huawei % Error / H3C Wrong parameter / Ruijie Unrecognized / Illegal param / Invalid input — minimum 5)
    - At least 2 cases asserting WriteErrorTransport (connection refused / timeout / EOF / err set / nil response — minimum 5)
    - At least 2 cases asserting WriteErrorNone (Info / empty result)
    - Pitfall #3 boundary case present: result starting with "% Error:" containing "timeout" substring must classify as WriteErrorDeviceRejected (NOT Transport)
    - `go test ./internal/services/portwrite/... -count=1 -v` exits 0
    - `go test ./internal/services/portwrite/... -run TestParseConfigError -v` exits 0 (subset run)
    - Compile-time interface contract assertions present at top of file: `var _ portWriteExecutor = (*device.DeviceExecutor)(nil)` and `var _ portWriteCollectionSvc = (*services.DeviceInfoCollectionService)(nil)`
  </acceptance_criteria>
  <done>
    Test file compiles; compile-time interface assertions `var _ portWriteExecutor = (*device.DeviceExecutor)(nil)` and `var _ portWriteCollectionSvc = (*services.DeviceInfoCollectionService)(nil)` lock mockability contract at build time; `TestParseConfigError` with 12+ marker cases passes including Pitfall #3 boundary; `TestIsTransportError` + `TestIsDeviceRejected` + `TestWriteErrorUnwrap` all green.
  </done>
</task>

<task type="auto">
  <name>Task 6: Add single-port method tests — TestShutdown, TestUndoShutdown, TestSetDescription, TestEnableDot1x, TestDisableDot1x (success + error paths + NoOp + Enqueue trigger)</name>
  <files>internal/services/portwrite/port_write_service_test.go</files>
  <read_first>
    - internal/services/portwrite/port_write_service_test.go (Task 5 — read existing file before appending)
    - internal/services/portwrite/port_write_service.go (created in Task 3 — writeSinglePort + executeWrite signatures)
    - internal/services/portwrite/pre_state_check.go (created in Task 2 — checkPreState behavior)
    - .planning/phases/51-w2-portwriteservice-batch-orchestrator-mock-tests/51-CONTEXT.md (D-13 + D-14 + D-15 — pre-state NoOp + Enqueue success-only)
    - .planning/phases/51-w2-portwriteservice-batch-orchestrator-mock-tests/51-RESEARCH.md (D-18 — test coverage list)
  </read_first>
  <action>
    Append to existing file `internal/services/portwrite/port_write_service_test.go` (do NOT overwrite Task 5 — append new test functions after existing ones):

    1. Add imports as needed: `github.com/xingran-next/xingran-go-backend/internal/models`, `github.com/xingran-next/xingran-go-backend/internal/services/portcollection`, `gorm.io/driver/sqlite`, `gorm.io/gorm`

    2. Helper function `func newTestService(exec portWriteExecutor, coll portWriteCollectionSvc, db *gorm.DB) *portWriteServiceImpl` returning `&portWriteServiceImpl{db: db, deviceExecutor: exec, collectionSvc: coll}` for tests.

    3. Helper function `func newTestDB(t *testing.T) *gorm.DB` — opens an in-memory SQLite database via `gorm.Open(sqlite.Open(":memory:"), &gorm.Config{})`, runs `db.AutoMigrate(&models.NetworkDevice{}, &models.DevicePortStatus{})`, returns db. Wrap in `t.Helper()`. This is the minimal DB needed for `writeSinglePort`'s pre-state lookup to succeed.

    4. Helper function `func seedPortAndDevice(t *testing.T, db *gorm.DB, portID, deviceID, interfaceName, adminStatus string, dot1xEnabled bool, description string)` — creates a `models.NetworkDevice{ID: deviceID, Vendor: models.VendorHuawei, DeviceName: "test-device"}` + `models.DevicePortStatus{ID: portID, DeviceID: deviceID, InterfaceName: interfaceName, AdminStatus: adminStatus, Dot1xEnabled: dot1xEnabled, Description: description, CollectedAt: time.Now()}`. Uses `t.Helper()`.

    5. Test `TestShutdown_Success`:
       - Setup: mock executor returns nil from ExecuteCustom; mock collectionSvc.Enqueue returns nil
       - Action: `svc.Shutdown(ctx, "port-1", "test-op")`
       - Expectations: `mockExec.AssertExpectations(t)` — `ExecuteCustom` called once with deviceID matching the seeded device; `mockColl.AssertExpectations(t)` — `Enqueue(deviceID)` called once
       - Assertions: `assert.NoError(t, err)`, `assert.Equal(t, "succeeded", result.Status)`, `assert.Equal(t, "shutdown", result.Action.String())`, `assert.False(t, result.NoOp)`, `assert.NotEmpty(t, result.CommandSent)` (contains "shutdown")

    6. Test `TestShutdown_TransportError`:
       - Setup: mock executor returns `&WriteError{Kind: WriteErrorTransport, Message: "i/o timeout"}` from ExecuteCustom
       - Action: `svc.Shutdown(ctx, "port-1", "test-op")`
       - Expectations: mock executor called once; mock collectionSvc.Enqueue NEVER called (Pitfall #6)
       - Assertions: `assert.Error(t, err)`, `var we *WriteError; assert.True(t, errors.As(err, &we)); assert.Equal(t, WriteErrorTransport, we.Kind)`, `assert.Equal(t, "failed", result.Status)`, `assert.NotEmpty(t, result.Error)`

    7. Test `TestShutdown_DeviceRejected`:
       - Setup: mock executor uses `Run()` callback to invoke `fn(ctx, nil)` which captures `lastResp = &device.Response{Result: "% Error: Unrecognized command"}` via a shared variable; mock returns nil from ExecuteCustom (fn injection pattern)
       - Action: `svc.Shutdown(ctx, "port-1", "test-op")`
       - Assertions: `assert.Error(t, err)`, `var we *WriteError; assert.True(t, errors.As(err, &we)); assert.Equal(t, WriteErrorDeviceRejected, we.Kind)`, `result.Status == "failed"`, mock collectionSvc.Enqueue NEVER called

    8. Test `TestShutdown_NoOp_AlreadyDown`:
       - Setup: real test DB; seed port with AdminStatus="down"
       - Action: `svc.Shutdown(ctx, "port-1", "test-op")`
       - Expectations: mock executor NEVER called (pre-state match returns early); mock collectionSvc.Enqueue NEVER called (D-15 — NoOp does not trigger Enqueue)
       - Assertions: `assert.NoError(t, err)`, `assert.Equal(t, "skipped", result.Status)`, `assert.True(t, result.NoOp)`, `assert.Equal(t, "admin_down", result.CurrentState)`

    9. Test `TestShutdown_DBRecordNotFound_Fallback`:
       - Setup: real test DB; NO port seeded (DB returns ErrRecordNotFound); mock executor returns nil
       - Action: `svc.Shutdown(ctx, "missing-port", "test-op")` — writeSinglePort falls back to direct executeWrite with empty deviceID, which returns ErrDeviceNotFound from executeWrite (because deviceID is empty)
       - Assertion: `assert.ErrorIs(t, err, ErrDeviceNotFound)` (verifies D-13 fallback path runs but deviceID-empty triggers ErrDeviceNotFound)

    10. Test `TestUndoShutdown_Success`, `TestSetDescription_Success`, `TestEnableDot1x_Success`, `TestDisableDot1x_Success` — minimal "happy path" tests mirroring TestShutdown_Success structure. Assert mock executor called once, Enqueue called once, CommandSent contains expected substring ("undo shutdown" / "description" / "dot1x enable" / "no dot1x enable" / "dot1x" depending on vendor).

    11. Test `TestSetDescription_NoOp_DescriptionMatches`:
       - Setup: real test DB; seed port with Description="uplink-to-core"
       - Action: `svc.SetDescription(ctx, "port-1", "uplink-to-core", "test-op")`
       - Assertions: NoOp=true, Status="skipped", CurrentState="description_match", mock executor never called

    12. Test `TestEnableDot1x_NoOp_AlreadyEnabled`:
       - Setup: real test DB; seed port with Dot1xEnabled=true
       - Action: `svc.EnableDot1x(ctx, "port-1", "test-op")`
       - Assertions: NoOp=true, Status="skipped", CurrentState="dot1x_enabled", mock executor never called

    Each test should be small (~30-50 lines) and self-contained. Use `t.Run` for subtests where natural (e.g., grouping all 5 single-port success tests under `TestSinglePort_Success_AllActions` with subtests per action).

    IMPORTANT: `mockDeviceExecutor.ExecuteCustom` mock signature must match `portWriteExecutor.ExecuteCustom` exactly (context.Context, string, func(context.Context, *device.PooledConnection) error, time.Duration). Use `mock.Anything` for the fn argument when configuring expectations — fn values are functions and can't be compared.

    IMPORTANT: When checking that Enqueue is NOT called (e.g., TestShutdown_TransportError, TestShutdown_NoOp), use `mockColl.AssertNotCalled(t, "Enqueue", mock.Anything)` AFTER `mockExec.AssertExpectations(t)`.

    Skip DB-setup tests if the test runner cannot use sqlite in-memory (shouldn't happen — sqlite is in go.mod via `modernc.org/sqlite`). If sqlite fails, fall back to testing with `nil` DB.
  </action>
  <verify>
    <automated>go test ./internal/services/portwrite/... -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/services/portwrite/port_write_service_test.go` exists with Task 5 content + new tests appended
    - Test functions present: `TestShutdown_Success`, `TestShutdown_TransportError`, `TestShutdown_NoOp_AlreadyDown`, `TestShutdown_DBRecordNotFound_Fallback`
    - At least one NoOp test per action: `TestSetDescription_NoOp_DescriptionMatches`, `TestEnableDot1x_NoOp_AlreadyEnabled` (or combined)
    - All 5 single-port methods have a success-path test (or combined under `TestSinglePort_Success_AllActions`)
    - Mock executor: `ExecuteCustom` called with singlePortTimeout=30s for single-port per D-11
    - Mock collectionSvc: `Enqueue(deviceID)` called exactly once on success paths
    - Mock collectionSvc: `Enqueue` NEVER called on transport_error / device_rejected / NoOp paths (verified via `AssertNotCalled`)
    - Tests use `errors.As` to verify WriteError Kind for transport/rejection classification
    - Pre-state match NoOp tests use real DB (sqlite in-memory) to verify the GORM query path
    - `go test ./internal/services/portwrite/... -count=1 -v` exits 0
  </acceptance_criteria>
  <done>
    All single-port method tests pass: 5 actions x success + NoOp variants + transport_error + device_rejected + DB-not-found fallback; Enqueue trigger verified success-only via AssertNotCalled.
  </done>
</task>

<task type="auto">
  <name>Task 7: Add BatchWritePorts tests — ExceedsMax, Empty, Success, FailFast_Transport, FailFast_DeviceRejected, AllSkipped, DetachedContext</name>
  <files>internal/services/portwrite/port_write_service_test.go</files>
  <read_first>
    - internal/services/portwrite/port_write_service_test.go (Tasks 5+6 — read existing file before appending)
    - internal/services/portwrite/batch_orchestrator.go (created in Task 4 — BatchWritePorts signature + detached context logic)
    - .planning/phases/51-w2-portwriteservice-batch-orchestrator-mock-tests/51-CONTEXT.md (D-12 + D-17 — detached 30min + fail-fast spec)
    - .planning/phases/51-w2-portwriteservice-batch-orchestrator-mock-tests/51-RESEARCH.md (Validation Architecture section Test Map — batch test commands)
  </read_first>
  <action>
    Append to existing file `internal/services/portwrite/port_write_service_test.go`:

    1. Test `TestBatchWritePorts_ExceedsMax` (BATCH-04):
       - Build `portIDs := make([]string, 51); for i := range portIDs { portIDs[i] = fmt.Sprintf("port-%d", i) }`
       - Call `svc.BatchWritePorts(ctx, BatchWriteRequest{DeviceID: "device-1", Action: portcollection.ActionShutdown, PortIDs: portIDs}, "test-op")`
       - Assertions: `assert.ErrorIs(t, err, ErrBatchTooLarge)`, `assert.Nil(t, result)`, `mockExec.AssertNotCalled(t, "ExecuteCustom", mock.Anything, mock.Anything, mock.Anything, mock.Anything)` (NO SSH traffic before validation)

    2. Test `TestBatchWritePorts_Empty` (BATCH-04):
       - Call `svc.BatchWritePorts(ctx, BatchWriteRequest{DeviceID: "device-1", Action: portcollection.ActionShutdown, PortIDs: []string{}}, "test-op")`
       - Assertions: `assert.ErrorIs(t, err, ErrEmptyBatch)`, `assert.Nil(t, result)`, `mockExec.AssertNotCalled(...)`

    3. Test `TestBatchWritePorts_ExceedsExactly50` (BATCH-04 boundary):
       - Build exactly 50 portIDs; call BatchWritePorts
       - Assertions: `assert.NoError(t, err)` (50 is the boundary — should succeed entering the loop, even if subsequent DB ops fail in test setup; alternatively assert ErrBatchTooLarge NOT returned)

    4. Test `TestBatchWritePorts_Success_AllSucceeded` (BATCH-03 happy path):
       - Setup: real test DB; seed 3 ports on same device (admin_status=up for shutdown action)
       - Configure mock executor to return nil for all ExecuteCustom calls (use `mockExec.On(...).Return(nil)`)
       - Configure mock collectionSvc.Enqueue to return nil
       - Action: `svc.BatchWritePorts(ctx, BatchWriteRequest{DeviceID: deviceID, Action: portcollection.ActionShutdown, PortIDs: []string{"p1", "p2", "p3"}}, "test-op")`
       - Assertions: `assert.NoError(t, err)`, `assert.Len(t, result.Succeeded, 3)`, `assert.Len(t, result.Failed, 0)`, `assert.Len(t, result.Skipped, 0)`, mock executor called 3 times, mock collectionSvc.Enqueue called 3 times

    5. Test `TestBatchWritePorts_FailFast_Transport` (BATCH-02 + SSH-02):
       - Setup: real test DB; seed 3 ports (all admin_status=up)
       - Configure mock executor: first call returns nil, second call returns `&WriteError{Kind: WriteErrorTransport, Message: "connection refused"}`, third call should NEVER happen (fail-fast)
       - Use sequential `mockExec.On(...).Return(nil)` then `mockExec.On(...).Return(transportErr)` — testify/mock processes On() in order for matching args
       - Action: BatchWritePorts with ActionShutdown on 3 ports
       - Assertions: `assert.NoError(t, err)` (BatchWritePorts returns nil error — failures are in result.Failed), `assert.Len(t, result.Succeeded, 1)`, `assert.Len(t, result.Failed, 1)`, `assert.Len(t, result.Skipped, 0)`, `result.Failed[0].PortID == "p2"`, mock executor called exactly 2 times, mock collectionSvc.Enqueue called exactly 1 time (only first success triggers)

    6. Test `TestBatchWritePorts_FailFast_DeviceRejected` (BATCH-02 + SSH-02):
       - Setup: real test DB; seed 3 ports
       - Configure mock executor: first call returns nil; second call returns `&WriteError{Kind: WriteErrorDeviceRejected, Message: "% Error: Unrecognized command"}`
       - Assertions: `assert.Len(t, result.Succeeded, 1)`, `assert.Len(t, result.Failed, 1)`, `result.Failed[0].Error` contains "Unrecognized command", mock executor called exactly 2 times

    7. Test `TestBatchWritePorts_AllSkipped_PreStateMatch` (PORT-06):
       - Setup: real test DB; seed 3 ports with AdminStatus="down"
       - Action: BatchWritePorts with ActionShutdown (which matches "already down" pre-state)
       - Assertions: `assert.Len(t, result.Skipped, 3)`, `assert.Len(t, result.Succeeded, 0)`, `assert.Len(t, result.Failed, 0)`, mock executor NEVER called, mock collectionSvc.Enqueue NEVER called

    8. Test `TestBatchWritePorts_PartialResult_Structure` (BATCH-03):
       - Setup: real test DB; seed 4 ports on device-1
       - Mock executor: returns nil for port 1 (success), returns transport_error for port 2 (fail-fast stops loop)
       - Action: BatchWritePorts with all 4 portIDs
       - Assertions: `result.Succeeded` is `[]PortResult{}` not nil (not nil pointer), `result.Failed` is `[]PortResult{}` not nil, `result.Skipped` is `[]PortResult{}` not nil — all 3 fields exist even when empty (JSON serialization: `[]` not `null`)
       - Use `assert.NotNil(t, result.Succeeded)` etc. to verify slices are initialized

    9. Test `TestBatchWritePorts_DetachedContext` (SSH-04):
       - Setup: real test DB; seed 1 port
       - Configure mock executor with a `Run()` callback that captures the received ctx into a shared variable, and verifies it has a `Deadline()` of approximately 30min from now (NOT <1s which would indicate inherited HTTP ctx)
       - Action: BatchWritePorts with caller ctx that has 1-second deadline (simulating HTTP request ctx)
       - Assertions: Inside the mock's Run callback, assert `receivedCtx.Deadline()` is approximately 30 minutes from now (allow ±1 minute tolerance for test execution time); the caller's 1s deadline should NOT propagate into the mock

       Implementation hint: use `mockExec.On("ExecuteCustom", mock.Anything, mock.Anything, mock.Anything, mock.Anything).Run(func(args mock.Arguments) { capturedCtx = args.Get(0).(context.Context) }).Return(nil)` — then after BatchWritePorts returns, assert `time.Until(capturedCtx.Deadline())` is between 29min and 31min.

    Use `mock.Anything` for fn argument in mock expectations (functions aren't comparable). Use specific arguments for ctx (or `mock.Anything`) and deviceID (must match the seeded device).

    For DB-seeded tests, reuse the `newTestDB` and `seedPortAndDevice` helpers from Task 6.

    IMPORTANT: When configuring mock executor for batch tests, set up the mock BEFORE calling BatchWritePorts. For sequential different returns, use `mockExec.On("ExecuteCustom", ...).Return(nil)` then `mockExec.On("ExecuteCustom", ...).Return(transportErr)` — testify/mock processes On() calls in order for matching arguments. If fn signature varies (closure capture), use `.Run(func(args mock.Arguments) { ... })` to execute side effects.
  </action>
  <verify>
    <automated>go test ./internal/services/portwrite/... -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/services/portwrite/port_write_service_test.go` exists with all prior tasks' content + new batch tests appended
    - Test functions present (minimum): `TestBatchWritePorts_ExceedsMax`, `TestBatchWritePorts_Empty`, `TestBatchWritePorts_Success_AllSucceeded`, `TestBatchWritePorts_FailFast_Transport`, `TestBatchWritePorts_FailFast_DeviceRejected`, `TestBatchWritePorts_AllSkipped_PreStateMatch`, `TestBatchWritePorts_PartialResult_Structure`, `TestBatchWritePorts_DetachedContext`
    - ExceedsMax test: 51 ports -> ErrBatchTooLarge, NO SSH traffic
    - Empty test: empty PortIDs -> ErrEmptyBatch, NO SSH traffic
    - Success test: 3 ports all succeed -> 3 in result.Succeeded, 3 Enqueue calls
    - FailFast_Transport test: 3 ports, 2nd fails transport -> result.Succeeded has 1, result.Failed has 1 with WriteErrorTransport Kind, only 2 SSH calls
    - FailFast_DeviceRejected test: similar to above but for WriteErrorDeviceRejected Kind
    - AllSkipped test: pre-state match all ports -> result.Skipped has 3, 0 SSH calls, 0 Enqueue calls
    - PartialResult test: 3 result slices all exist (not nil) — verify via `assert.NotNil(t, result.Succeeded)` etc.
    - DetachedContext test: caller passes 1s-deadline ctx; received ctx inside mock has ~30min deadline (NOT 1s)
    - `go test ./internal/services/portwrite/... -count=1 -v` exits 0 (full suite green)
    - `go test ./internal/services/portwrite/... -count=1` exits 0 (concise output)
  </acceptance_criteria>
  <done>
    All batch tests pass: maxBatchSize=50 cap verified + empty rejection verified + serial fail-fast on transport_error + serial fail-fast on device_rejected + pre-state skip populates result.Skipped + partial result structure (3 slices always present) + detached 30min context confirmed via mock closure capture.
  </done>
</task>

<task type="auto">
  <name>Task 8: Final verification — full build + full test suite + vet</name>
  <files>internal/services/portwrite/port_write_service_test.go</files>
  <read_first>
    - internal/services/portwrite/port_write_service.go (verify final shape matches CONTEXT D-10..D-18 spec)
    - internal/services/portwrite/parse_error.go (verify marker priority)
    - internal/services/portwrite/pre_state_check.go (verify all 5 actions covered)
    - internal/services/portwrite/batch_orchestrator.go (verify detached context + fail-fast)
    - internal/services/portwrite/port_write_service_test.go (verify all test functions present)
  </read_first>
  <action>
    Run the full Phase 51 verification suite (this task makes NO file modifications — it is the acceptance gate):

    1. `go build ./...` — entire repo builds cleanly (catches cross-package regressions; Phase 51 must not break Phase 50 portcollection package)

    2. `go vet ./internal/services/portwrite/...` — no vet warnings

    3. `go test ./internal/services/portwrite/... -count=1 -v` — all tests green with verbose output

    4. `go test ./internal/services/portcollection/... -count=1` — Phase 50 vendor template tests still pass (no regression)

    5. `go test ./internal/services/operations/... -count=1` — operations module tests still pass (cross-package sanity)

    6. Spot-check: `grep -c "portwrite:" internal/services/portwrite/*.go` — confirm sentinel errors all use `portwrite:` prefix (consistency with Phase 50 `portcollection:` prefix pattern from 50-CONTEXT.md D-09)

    7. Spot-check: confirm `BatchWritePorts` first line uses `context.Background()` (NOT `ctx`) — manually verify in the source file via Read

    8. Confirm 9 requirement IDs (SSH-02/03/04/06 + PORT-06 + BATCH-02/03/04 + AUDIT-04) have corresponding test coverage in the test file — produce a mapping table in the SUMMARY

    Do NOT commit changes in this task — that is handled by the orchestrator after SUMMARY is written.

    If any check fails, document the failure in the SUMMARY's "Issues Encountered" section and iterate on the failing files (NOT by re-running tasks — re-edit the source files identified).
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./internal/services/portwrite/... &amp;&amp; go test ./internal/services/portwrite/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0 (whole repo builds; no cross-package regression vs Phase 50)
    - `go vet ./internal/services/portwrite/...` exits 0 (no vet warnings in new package)
    - `go test ./internal/services/portwrite/... -count=1 -v` exits 0 (all Phase 51 tests green)
    - `go test ./internal/services/portcollection/... -count=1` exits 0 (Phase 50 vendor templates regression intact)
    - `go test ./internal/services/operations/... -count=1` exits 0 (operations module cross-package sanity)
    - All sentinel errors in portwrite package use `portwrite:` prefix
    - `BatchWritePorts` first executable line uses `context.WithTimeout(context.Background(), batchDetachedTimeout)`
    - Requirement-to-test coverage table maps all 9 IDs (SSH-02/03/04/06, PORT-06, BATCH-02/03/04, AUDIT-04) to specific test functions
  </acceptance_criteria>
  <done>
    Phase 51 acceptance gate passes: full repo builds + new package vets clean + all new tests green + Phase 50 + operations module tests regression-free. Service layer + batch orchestrator + parseConfigError + pre-state check + mock tests are all production-ready for Phase 52 HTTP handler binding.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller -> PortWriteService.BatchWritePorts | HTTP handler (Phase 52) passes `c.Request.Context()` and BatchWriteRequest. Service MUST detach ctx (D-12) to avoid Core.Close 30s cutoff. |
| PortWriteService.executeWrite -> device.DeviceExecutor.ExecuteCustom | Service submits SSH command via internal executor. Executor manages connection pool + scheduler + timeout. |
| device.ScrapliWrapper -> vendor SSH daemon | Untrusted device response — may include `% Error:` (device_rejected) or transport errors (connection refused / timeout / EOF). parseConfigError MUST classify correctly. |
| PortWriteService -> sys_device_port_status (GORM) | Read-only pre-state lookup. DB row missing (port "disappeared") MUST fallback to direct execution, not silent skip (D-13). |
| Service -> services.DeviceInfoCollectionService.Enqueue | Fire-and-forget AUDIT-04 trigger. MUST only invoke on success path; failure paths MUST NOT trigger Enqueue (Pitfall #6 — Enqueue storm prevention). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Information Disclosure | parseConfigError.WriteError.Message | mitigate | Message is device-original text (`% Error: ...`); no SSH internal stack trace leaked. No credentials in error path — D-15 forbids operlog from capturing sensitive fields. |
| T-51-02 | Denial of Service | BatchWritePorts entry | mitigate | D-17 maxBatchSize=50 cap hard-rejects oversized batches BEFORE any SSH traffic. Test `TestBatchWritePorts_ExceedsMax` verifies zero SSH calls on rejection. |
| T-51-03 | Repudiation | writeSinglePort DB pre-state fallback | mitigate | D-13 ErrRecordNotFound fallback executes direct write (not silent skip). Audit row (Phase 52) captures both attempt and outcome. |
| T-51-04 | Denial of Service | Enqueue storm | mitigate | AUDIT-04 Enqueue ONLY on success path (verified via `mockColl.AssertNotCalled` in TestShutdown_TransportError). Failure paths never queue collection tasks. |
| T-51-05 | Repudiation | operlog/audit table write | mitigate | D-15 service does NOT call operlog directly — handler (Phase 52) owns single-point recording. No double-write risk. |
| T-51-06 | Tampering | Description field injection into SSH command | accept | scrapli text-mode passthrough; device CLI parser is final authority. Phase 50 D-06 deferred (escape not yet enforced). Phase 51 trusts Phase 50 sentinel errors for empty/too-long validation. |
| T-51-07 | Information Disclosure | CommandSent field in PortResult | accept | CommandSent is unmasked per D-15 (audit source of truth). Phase 52 handler responsible for redacting in HTTP response (not service layer). |
| T-51-SC | Tampering | npm/pip/cargo installs | n/a | Phase 51 introduces ZERO new external packages; no install-time threat surface. |
</threat_model>

<verification>
[Overall phase checks — see Task 8 acceptance criteria]
</verification>

<success_criteria>
- [ ] All 8 tasks (Tasks 1-8) complete with their individual acceptance criteria satisfied
- [ ] `go build ./...` exits 0 — entire repo builds
- [ ] `go vet ./internal/services/portwrite/...` exits 0
- [ ] `go test ./internal/services/portwrite/... -count=1` exits 0 with all subtests passing
- [ ] `go test ./internal/services/portcollection/... -count=1` exits 0 (Phase 50 regression intact)
- [ ] `go test ./internal/services/operations/... -count=1` exits 0 (cross-package sanity)
- [ ] All 9 requirement IDs (SSH-02, SSH-03, SSH-04, SSH-06, PORT-06, BATCH-02, BATCH-03, BATCH-04, AUDIT-04) are covered by at least one test function in `port_write_service_test.go`
- [ ] Service field types are interfaces (`portWriteExecutor`, `portWriteCollectionSvc`) — verified by successful mock injection in test suite
- [ ] Factory `NewPortWriteService` accepts concrete pointer types (`*device.DeviceExecutor`, `*services.DeviceInfoCollectionService`) for Phase 52 router compatibility
- [ ] BatchWritePorts entry validation runs BEFORE any SSH traffic (mock executor AssertNotCalled verifies)
- [ ] Detached 30min context verified via mock closure capturing `ctx.Deadline()` ~30min from now (NOT <1s caller deadline)
- [ ] All sentinel errors use `portwrite:` prefix for log/audit consistency
- [ ] Plan file committed to git with message `docs(phase-51): create PortWriteService plan`
- [ ] `.planning/phases/51-w2-portwriteservice-batch-orchestrator-mock-tests/51-01-SUMMARY.md` written by executor after plan execution
</success_criteria>

<output>
Create `.planning/phases/51-w2-portwriteservice-batch-orchestrator-mock-tests/51-01-SUMMARY.md` when done
</output>

<!--
SCOPE_SANITY RATIONALE (checker WARNING #1 - ACCEPTED without split):

The checker flagged that Plan 01 has 8 tasks and suggested splitting into 51-01 (Tasks 1-4:
skeleton + parse + pre-state + batch orchestrator) and 51-02 (Tasks 5-7: tests + Task 8
verification). This rationale documents why we ACCEPT the single-plan structure:

1. Service code volume is well under threshold: parse_error.go (~90 lines),
   pre_state_check.go (~50 lines), port_write_service.go (~250 lines incl. interfaces/factory),
   batch_orchestrator.go (~130 lines). Total service implementation ~520 lines - under the
   600-line pragmatic ceiling.

2. Test code volume is well under threshold: port_write_service_test.go with mock structs +
   12+ parseConfigError table cases + 5 single-port method tests + 8 batch tests totals
   ~450 lines - under the 500-line pragmatic ceiling.

3. ROADMAP explicitly allows "1-2 plans" for this phase; splitting into 51-01/51-02 would
   force impl-then-tests across separate wave chains which slows the red->green feedback
   loop for a small focused phase.

4. Shared package boundary: all 5 files live in the same `internal/services/portwrite/`
   package and share private types (`portWriteServiceImpl`, `portWriteExecutor`,
   `portWriteCollectionSvc`). Splitting would force tests into a separate plan that needs
   access to these private types - possible via `export_test.go` but adds friction without
   benefit at this size.

5. Task 8 is a pure verification gate (no file modifications) and Task 5-7 are sequential
   test appends to the same file - they form a natural "scaffold-then-fill" cadence that
   mirrors red->green->refactor rather than arbitrary horizontal layers.

Decision: ACCEPT single-plan structure. No split.
-->