---
phase: 52-w3-router-handler-operlog-permission-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/permission/config.go
  - internal/models/port_write_audit.go
  - internal/services/portcollection/cache_keys.go
  - internal/api/v1/network/port_write_handler.go
  - internal/api/v1/network/port_write_router.go
  - internal/api/v1/network/network_router.go
  - internal/api/v1/network/port_write_router_test.go
  - internal/api/v1/network/port_write_handler_test.go
autonomous: true
requirements: [PERM-01, PERM-02, INFRA-02, INFRA-03, AUDIT-01, AUDIT-02, CONV-01, CONV-02, CONV-03, CONV-04, PORT-01, PORT-02, PORT-03, PORT-04, PORT-05, BATCH-01]
requirements_addressed:
  - PERM-01: NetworkPortWrite 常量定义
  - PERM-02: 6 端点组级 RequirePermissions([network:port:write], core) 挂载
  - INFRA-02: /network/ports/write/* 路由组注册到 network_router.go
  - INFRA-03: cache_keys.go 定义 2 个常量(不写入)
  - AUDIT-01: 6 handler 在 response.Success 之前调 operlog.Record(...,"端口管理",...)
  - AUDIT-02: operlog.WithOperParam 串含 device_id/port_id/action/operator/result_status(+ audit_ids)
  - AUDIT-03(部分): handler 同步写 PortWriteAudit 行(三态 succeeded/failed/skipped)— schema 本身在 52-02 落地,Wave 1 测试用 sqlite 手动 AutoMigrate
  - CONV-01: shutdown/undo_shutdown → OperTypeStatus(=10)
  - CONV-02: description → OperTypeUpdate(=2)
  - CONV-03: dot1x enable/disable → OperTypeStatus(=10)
  - CONV-04: batch → OperTypeBatch(=16),oper_param 含 batch_size/succeeded_count/failed_count/skipped_count/action/device_id/audit_ids
  - PORT-01..05: 5 单端口 handler 调对应 PortWriteService 方法 + audit + operlog
  - BATCH-01: batch handler 调 BatchWritePorts + 写 N 条 audit + 1 条 operlog 汇总
tags: [go, gin, handler, router, operlog, permission, network, port-write]
user_setup: []

must_haves:
  truths:
    - "用户能通过 HTTP POST /network/ports/write/shutdown 关闭端口(需持 network:port:write 权限)"
    - "用户能通过 HTTP POST /network/ports/write/batch 对同设备多端口批量写"
    - "6 个写端点都在 /network/ports/write 子组下且组级挂 RequirePermissions([network:port:write], core)"
    - "每个写操作 success path 末尾、response.Success 之前同时写 1 条 operlog + 对应 audit 行(单端口=1, batch=N)"
    - "NoOp/skipped 结果也写 audit 行(status=skipped, command_sent='', device_response='无需操作')"
    - "PortResult.Status=='failed' 路径返回 200(非 4xx) + audit 行 status=failed; sentinel 错误(ErrPortNotFound 等)路径返回 4xx 且不写 audit"
  artifacts:
    - path: "pkg/permission/config.go"
      provides: "NetworkPortWrite PermissionCode 常量"
      contains: "NetworkPortWrite PermissionCode = \"network:port:write\""
    - path: "internal/models/port_write_audit.go"
      provides: "PortWriteAudit GORM 模型(Path X 提前到 Wave 1)"
      contains: "func (PortWriteAudit) TableName"
    - path: "internal/services/portcollection/cache_keys.go"
      provides: "CacheKeyPortWriteResult / CacheKeyPortWriteBatch 常量"
      contains: "CacheKeyPortWriteResult"
    - path: "internal/api/v1/network/port_write_handler.go"
      provides: "6 handler 方法 + ModulePortWrite 常量 + audit/operlog 写入"
      exports: ["NewPortWriteHandler", "Shutdown", "UndoShutdown", "SetDescription", "EnableDot1x", "DisableDot1x", "BatchWrite"]
    - path: "internal/api/v1/network/port_write_router.go"
      provides: "SetupPortWriteRouter(r, core) + /write 子组 + 组级 RequirePermissions(2-arg) + 6 kebab 端点"
      exports: ["SetupPortWriteRouter"]
    - path: "internal/api/v1/network/network_router.go"
      provides: "SetupPortWriteRouter 在 /ports 组内注册"
      contains: "SetupPortWriteRouter(ports, core)"
  key_links:
    - from: "internal/api/v1/network/network_router.go"
      to: "internal/api/v1/network/port_write_router.go::SetupPortWriteRouter"
      via: "在 SetupPortRouter 之后调用"
      pattern: "SetupPortWriteRouter\\(ports, core\\)"
    - from: "internal/api/v1/network/port_write_router.go"
      to: "pkg/middleware/permission.go::RequirePermissions"
      via: "组级 Use(2-arg)"
      pattern: "middleware\\.RequirePermissions\\(\\[\\]string\\{string\\(permission\\.NetworkPortWrite\\)\\}, core\\)"
    - from: "internal/api/v1/network/port_write_handler.go"
      to: "internal/utils/operlog/operlog.go::Record"
      via: "6 处 operlog.Record(含/不含 WithOperParam)"
      pattern: "operlog\\.Record\\(c, h\\.core\\.OperLogService, h\\.core\\.GetDB\\(\\), ModulePortWrite"
    - from: "internal/api/v1/network/port_write_handler.go"
      to: "internal/models/port_write_audit.go::PortWriteAudit"
      via: "db.Create(&PortWriteAudit{...})"
      pattern: "models\\.PortWriteAudit"
---

<objective>
Wave 1 — 把 Phase 51 已稳定的 PortWriteService(6 方法 + 28 mock 测试)暴露为 HTTP:新建 `network:port:write` 权限常量 + PortWriteAudit 模型(Path X 提前) + cache_keys 占位常量 + 6 handler 方法(单端口 5 + batch 1) + `/network/ports/write/*` 路由组 + 组级 2-arg `RequirePermissions` + 在 network_router.go 注册。本 wave 不动 Phase 51 service 任何签名(零侵入),不动 operlog 包接口(走 RESEARCH §1.1 Path C 兜底,`audit.oper_log_id` 列保留 NULL)。

Purpose: 给 Phase 53 前端 BulkWriteDrawer 提供可调用的 HTTP 契约 + 给 Phase 54 真机 UAT 提供可验证的端到端入口。
Output: 6 个文件(4 创建 + 2 修改)+ 2 测试文件(Wave 0 from VALIDATION.md)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-w3-router-handler-operlog-permission-migration/52-CONTEXT.md
@.planning/phases/52-w3-router-handler-operlog-permission-migration/52-RESEARCH.md
@.planning/phases/52-w3-router-handler-operlog-permission-migration/52-PATTERNS.md
@.planning/phases/52-w3-router-handler-operlog-permission-migration/52-VALIDATION.md
@.planning/phases/51-w2-portwriteservice-batch-orchestrator-mock-tests/51-01-SUMMARY.md
@CLAUDE.md
@internal/api/v1/network/network_router.go
@internal/api/v1/network/port_router.go
@internal/api/v1/asset/fix_suggestion_handler.go
@internal/api/v1/asset/reconciliation_handler.go
@internal/services/portwrite/port_write_service.go
@internal/services/portwrite/batch_orchestrator.go
@internal/utils/operlog/operlog.go
@internal/utils/operlog/regression_test.go
@internal/models/device_port_status.go
@internal/models/menu.go
@pkg/middleware/permission.go
@pkg/permission/config.go

<interfaces>
<!-- Phase 52 Wave 1 关键契约(全部已在源码核对)。executor 直接用,不要在代码库中"探索"。 -->

### Phase 51 PortWriteService(零侵入,Wave 1 handler 直接消费)
源:`internal/services/portwrite/port_write_service.go:75-104`
```go
type PortWriteService interface {
    Shutdown(ctx context.Context, portID string, operator string) (*PortResult, error)
    UndoShutdown(ctx context.Context, portID string, operator string) (*PortResult, error)
    SetDescription(ctx context.Context, portID string, desc string, operator string) (*PortResult, error)
    EnableDot1x(ctx context.Context, portID string, operator string) (*PortResult, error)
    DisableDot1x(ctx context.Context, portID string, operator string) (*PortResult, error)
    BatchWritePorts(ctx context.Context, req BatchWriteRequest, operator string) (*BatchResult, error)
}
func NewPortWriteService(db *gorm.DB, deviceExecutor *device.DeviceExecutor, collectionSvc *services.DeviceInfoCollectionService) PortWriteService

// PortResult.Status ∈ {"succeeded","failed","skipped"};CommandSent 不脱敏(audit 真相源)
type PortResult struct {
    PortID string; Action Action; Status string; NoOp bool; CurrentState string; Error string; CommandSent string
}
type BatchWriteRequest struct { DeviceID string; Action Action; PortIDs []string; Description string }
type BatchResult struct { Succeeded, Failed, Skipped []PortResult }  // 三个切片都初始化为 []T{} 非 nil

// Sentinel(用 errors.Is 匹配,Phase 51 用 var Err... = errors.New(...))
var (
    ErrBatchTooLarge  = errors.New("portwrite: batch exceeds max size of 50")
    ErrEmptyBatch     = errors.New("portwrite: batch is empty")
    ErrMixedDevices   = errors.New("portwrite: batch contains ports from different devices")
    ErrPortNotFound   = errors.New("portwrite: port not found")
    ErrDeviceNotFound = errors.New("portwrite: device not found")
)
```

### operlog.Record(5 fixed + variadic;3 个 RecordOption,**不存在 WithJsonResult / WithOperID**)
源:`internal/utils/operlog/operlog.go:215`(VERIFIED)
```go
func Record(c *gin.Context, operLogSvc Recorder, db *gorm.DB, module string, operType int, opts ...RecordOption)
// 仅 3 个 option:
func WithOperParam(s string) RecordOption  // ← Path C 用此把 audit_ids 嵌 operlog
func WithStatus(status int) RecordOption   // 1 = failure
func WithErrorMsg(msg string) RecordOption
// ⚠️ 禁止:不要新增 WithOperID / WithJsonResult(RESEARCH §1.1 Path C 强制)
```

### RequirePermissions(**2-arg**!CONTEXT/CLAUDE.md 漏写第二参)
源:`pkg/middleware/permission.go:200`(VERIFIED)
```go
func RequirePermissions(permissions []string, core *core.Core) gin.HandlerFunc
```

### network_router.go 现有 ports 组(Wave 1 在 SetupPortRouter 后插一行)
源:`internal/api/v1/network/network_router.go:205-214`(VERIFIED)
```go
ports := r.Group("/ports")
ports.Use(middleware.RequirePermissionsWithQuery([]string{"network:port:query"}, middleware.OpsSelectorReadPerms, core))
{
    SetupPortRouter(ports, core, exportHandler)
    // ← 在此处插入 SetupPortWriteRouter(ports, core)
}
```

### permission/config.go 插入点(VERIFIED line 186)
```go
// 端口状态查询权限
NetworkPortQuery PermissionCode = "network:port:query"
// ← 在此处插入 NetworkPortWrite PermissionCode = "network:port:write"
```

### OperType 常量(已存在,直接引用,RESEARCH §2 + CLAUDE.md operlog 表)
`operlog.OperTypeStatus`(=10) / `operlog.OperTypeUpdate`(=2) / `operlog.OperTypeBatch`(=16)

### DevicePortStatus(D-02 before_value 预 SELECT 源,handler 用 GORM First 查)
源:`internal/models/device_port_status.go:31-57`(VERIFIED)
字段:`ID / DeviceID / InterfaceName / AdminStatus / Description / Dot1xEnabled bool`

### Core DI(handler 通过 core.GetDB() + core.OperLogService + core.DeviceExecutor + core.DeviceInfoCollectionService)
源:`internal/core/core_services.go:17-29` + `core.go:276,284,434`(VERIFIED)

### 5 个 load-bearing corrections(executor 必须严格遵守)
1. **RequirePermissions 是 2-arg**`(perm []string, core *core.Core)`(非 CONTEXT/CLAUDE.md 写的 1-arg)
2. **D-13 走 Path C**:handler 先 INSERT audit 拿 audit_ids,再 `operlog.Record(..., WithOperParam(auditJSON))` 嵌 audit_ids;`audit.oper_log_id` 列**保持 NULL**;**禁止**给 operlog 包加 `WithOperID`(会破坏 regression_test.go Phase 34 锁定结构)
3. **Path X**:PortWriteAudit 模型定义放 Wave 1(本 plan Task 2),让 handler 能引用;Wave 2 只做 AutoMigrate 注册 + migration_202 + helper
4. **只有 3 个 RecordOption**:`WithOperParam` / `WithStatus` / `WithErrorMsg`;`WithJsonResult` 是 CONTEXT/CLAUDE.md 的幻觉(只有 `recordConfig.jsonResult` 字段,无导出 setter)
5. **PortResult 没有 DeviceResponse 字段**:audit.device_response 列填值策略(成功→"OK",failed→result.Error,skipped→"无需操作")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 加 NetworkPortWrite 权限常量 + PortWriteAudit 模型(Path X) + cache_keys 占位</name>
  <files>pkg/permission/config.go, internal/models/port_write_audit.go, internal/services/portcollection/cache_keys.go</files>
  <read_first>
    - `.planning/phases/52-w3-router-handler-operlog-permission-migration/52-PATTERNS.md` §1 + §2 + §3(三个 analog 的具体代码摘录)
    - `.planning/phases/52-w3-router-handler-operlog-permission-migration/52-RESEARCH.md` §3.6(PortWriteAudit GORM tag 注意事项)+ §3.7(cache_keys.go 内容)
    - `pkg/permission/config.go`(line 183-197,NetworkPortQuery 旁插入点)
    - `internal/models/device_port_status.go`(line 31-72,model shape + BeforeCreate hook analog)
    - `internal/services/portcollection/cache_keys.go`(若已存在则确认;否则参考 `internal/services/asset/cache_keys.go:25-51` 的 R1 占位注释风格)
  </read_first>
  <action>
在 `pkg/permission/config.go` 中 `NetworkPortQuery` 常量(line 186)后插入一行:`NetworkPortWrite PermissionCode = "network:port:write"`,带注释 "端口写操作权限(Phase 52: shutdown/undo_shutdown/description/dot1x_enable/dot1x_disable/batch)"。**不要**修改 `GetRoutePermissions()` 函数(network 模块路由鉴权靠 router 级 RequirePermissions,RESEARCH §3.1 VERIFIED)。

新建 `internal/models/port_write_audit.go`,定义 `PortWriteAudit` struct,12 列按 D-01 + PATTERNS.md §2 推荐 struct(VERIFIED 字段顺序与 GORM tag):ID(uuid PK)/ DeviceID(uuid, 复合索引 priority:1)/ PortID(uuid, priority:2)/ Action(varchar32,not null)/ BeforeValue(json.RawMessage, type:jsonb)/ AfterValue(json.RawMessage, type:jsonb)/ CommandSent(text)/ DeviceResponse(text)/ Status(varchar16,not null)/ FailureReason(*string, type:text, nullable)/ Operator(varchar50)/ OperLogID(*string, type:uuid, nullable, **无 index tag**)/ CreatedAt(time.Time, 复合索引 priority:3 + 单列索引 idx_port_write_audit_created)。`TableName()` 返回 `"sys_port_write_audit"`。`BeforeCreate` 钩子:`if a.ID == "" { a.ID = uuid.New().String() }`。**不要** embed `BaseTimeLine`(audit 是 append-only,无 UpdatedAt);**不要** 加 `gorm:"-:migration"` tag(走 RESEARCH §1.2 路径 A,GORM AutoMigrate 建表);**不要** 加 UpdatedAt/DeletedAt 字段。导入 `encoding/json` + `github.com/google/uuid` + `gorm.io/gorm` + `time`。

新建 `internal/services/portcollection/cache_keys.go`(package portcollection),按 PATTERNS.md §3 风格:R1 占位注释 + 2 个 const(`CacheKeyPortWriteResult = "port:write:result:%s"` / `CacheKeyPortWriteBatch = "port:write:batch:%s"`)+ 注释掉的 helper(`GetPortWriteResultKey` / `GetPortWriteBatchKey`,Phase 53+ 启用)。**不要** 在本 phase 任何位置调用 cache.Set/Get(D-10 锁定 MVP 仅定义)。
  </action>
  <verify>
    <automated>
cd "D:/code/ClaudeCode/xingran-go-backend" && \
  go build ./pkg/permission/... ./internal/models/... ./internal/services/portcollection/... && \
  grep -v '^//' pkg/permission/config.go | grep -v '^\s*//' | grep -c 'NetworkPortWrite PermissionCode = "network:port:write"' | grep -q '^1$' && \
  grep -c 'func (PortWriteAudit) TableName' internal/models/port_write_audit.go | grep -q '^1$' && \
  grep -v '^\s*//' internal/services/portcollection/cache_keys.go | grep -c 'CacheKeyPortWriteResult = "port:write:result:%s"' | grep -q '^1$' && \
  echo "TASK1 GREEN"
    </automated>
  </verify>
  <acceptance_criteria>
- `pkg/permission/config.go` 含 `NetworkPortWrite PermissionCode = "network:port:write"`(在 `NetworkPortQuery` 之后)
- `internal/models/port_write_audit.go` 含 `func (PortWriteAudit) TableName() string` + 12 个字段(类型/tag 与 PATTERNS.md §2 一致)
- `internal/models/port_write_audit.go` **不含** `gorm:"-:migration"` tag(走路径 A)
- `internal/models/port_write_audit.go` **不含** `UpdatedAt` / `DeletedAt` / `BaseTimeLine` embed
- `internal/services/portcollection/cache_keys.go` 含两个 const,值为 `"port:write:result:%s"` 和 `"port:write:batch:%s"`
- `go build ./pkg/permission/... ./internal/models/... ./internal/services/portcollection/...` exit 0
  </acceptance_criteria>
  <done>三个文件就位且全包编译通过;NetworkPortWrite 常量、PortWriteAudit.TableName 方法、两个 cache key const 都存在。下一任务可引用 permission.NetworkPortWrite 与 models.PortWriteAudit。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: port_write_handler.go(6 handler + audit + operlog + sentinel→HTTP 翻译)</name>
  <files>internal/api/v1/network/port_write_handler.go, internal/api/v1/network/port_write_handler_test.go</files>
  <behavior>
- TestPortWriteHandler_Shutdown_Success:调 Shutdown → service 返 PortResult{Status:"succeeded"} → 响应 200 + audit 行落库(status=succeeded, command_sent=result.CommandSent, device_response="OK") + operlog.Record 被调用一次(module="端口管理", OperType=OperTypeStatus)
- TestPortWriteHandler_Shutdown_NoOp_AlreadyDown:service 返 PortResult{Status:"skipped", NoOp:true} → 响应 200 + audit 行(status=skipped, command_sent="", device_response="无需操作")
- TestPortWriteHandler_Shutdown_TransportFailed:service 返 PortResult{Status:"failed", Error:"transport_error: ..."} + nil sentinel err → 响应 200(response.Success) + audit 行(status=failed, failure_reason=非空, device_response=result.Error)
- TestPortWriteHandler_Shutdown_PortNotFound:service 返 (nil, ErrPortNotFound) → 响应 404 + **不写 audit 行**(audit 表行数不变)+ **不调 operlog.Record**(sentinel 路径)
- TestPortWriteHandler_SetDescription:OperType=OperTypeUpdate(=2);audit.before_value/after_value 含 description 字段
- TestPortWriteHandler_Batch:调 BatchWritePorts → operlog.Record 调用一次(OperType=OperTypeBatch=16, WithOperParam 含 audit_ids 数组)+ audit 行数 == len(Succeeded)+len(Failed)+len(Skipped)+ 响应 200 返回 BatchResult 三切片
- TestPortWriteHandler_Batch_ExceedsMax:service 返 (nil, ErrBatchTooLarge) → 响应 400 + 不写 audit + 不调 operlog
- TestPortWriteHandler_WithOperID_NotAdded:`grep -c 'WithOperID' internal/api/v1/network/port_write_handler.go` == 0(Path C 强制)
  </behavior>
  <read_first>
    - `.planning/phases/52-w3-router-handler-operlog-permission-migration/52-PATTERNS.md` §4a-4f(handler 完整骨架 + sentinel→HTTP 翻译表 + PortResult→audit 行映射 + batch skeleton)
    - `.planning/phases/52-w3-router-handler-operlog-permission-migration/52-RESEARCH.md` §1.1(Path C 推导)+ §3.3(8 种分支 sentinel→HTTP 翻译)+ §3.4(PortResult→audit 三态映射)+ §3.5(audit.oper_log_id=NULL)
    - `internal/api/v1/asset/fix_suggestion_handler.go`(line 109-139,Accept handler operlog 实战参考)+ line 35(Module 常量定义模式)
    - `internal/services/portwrite/port_write_service.go`(line 17-104,接口 + sentinel + PortResult/BatchResult 形状)
    - `internal/utils/operlog/operlog.go`(line 215 Record 签名 + line 189 WithOperParam)
    - `internal/utils/operlog/regression_test.go`(回归锁结构 — 本任务不动 operlog 包,保 intact)
    - `internal/models/port_write_audit.go`(Task 1 产出)
    - `internal/models/device_port_status.go`(D-02 预 SELECT 源)
  </read_first>
  <action>
新建 `internal/api/v1/network/port_write_handler.go`,package network。Imports:`errors` / `net/http` / `encoding/json` / `github.com/gin-gonic/gin` / `github.com/xingran-next/xingran-go-backend/internal/core` / `internal/models` / `internal/services/portwrite` / `internal/utils` / `internal/utils/operlog` / `pkg/response` / `github.com/google/uuid`(若 batchSummary 用 uuid — 可省略)/ `gorm.io/gorm`。

定义 `const ModulePortWrite = "端口管理"`(D-07/AUDIT-01 锁定;**不**用 "端口状态")。

定义 `PortWriteHandler struct { service portwrite.PortWriteService; core *core.Core }` + `NewPortWriteHandler(svc portwrite.PortWriteService) *PortWriteHandler` + `(h *PortWriteHandler) WithCore(core *core.Core) *PortWriteHandler`(复制 fix_suggestion_handler.go:1-43 模式)。

定义单端口请求 struct:`PortWriteRequest struct { PortID string \`json:"portId"\`; Description string \`json:"description,omitempty"\`; Reason string \`json:"reason,omitempty"\` }`(Reason 不校验,UI-02 操作原因仅记录;Description 仅 description 方法用)。

实现 5 个单端口 handler 方法(Shutdown / UndoShutdown / SetDescription / EnableDot1x / DisableDot1x),每个方法的步骤严格遵循 PATTERNS.md §4c 顺序 + sentinel→HTTP 表 §4d:
1. `c.ShouldBindJSON(&req)` 失败 → response.Error(400)
2. 取 `operator := utils.GetUsername(c)`(CLAUDE.md 惯例)
3. **D-02 before_value 预 SELECT**:`h.core.GetDB().First(&port, "id = ?", req.PortID)` 取 `models.DevicePortStatus`;失败(端口行不存在)则 before_value=`{}`(json.RawMessage([]byte("{}"))),不阻塞流程
4. 拼 before_value JSON:`{"admin_status":port.AdminStatus,"dot1x_enabled":port.Dot1xEnabled,"description":port.Description}`;device_id 从 `port.DeviceID` 取
5. 调对应 service 方法(SetDescription 传 `req.Description`),用 `errors.Is` 翻译 sentinel:`ErrPortNotFound`→404 "端口不存在";`ErrDeviceNotFound`→404 "设备不存在";其他 err→500 err.Error();**任一分支 return,不写 audit,不调 operlog**
6. **关键分支**:若 `result.Status == "failed"`(PortResult 失败但 nil sentinel err)→ **不调 response.Error**;继续走 audit + operlog + 最后 response.Success(result)(200)
7. **D-04 写 audit 行**:`buildAuditRow(...)` 按 PATTERNS.md §4e 映射(after_value 按 D-03:shutdown→{"admin_status":"down"};undo→{"admin_status":"up"};dot1x_enable→{"dot1x_enabled":true};dot1x_disable→{"dot1x_enabled":false};description→{"description":req.Description};若 result.Status=="skipped" 则 after_value=before_value);device_response:成功→"OK",failed→result.Error,skipped→"无需操作";failure_reason:failed→&result.Error,其他→nil;OperLogID:**nil**(Path C 强制)。`h.core.GetDB().Create(auditRow)` 拿 auditRow.ID
8. **Path C 关键**:组装 operParam JSON 含 `{"audit_ids":[auditRow.ID], "device_id":port.DeviceID, "port_id":req.PortID, "action":<action>, "operator":operator, "result_status":result.Status}`;调 `operlog.Record(c, h.core.OperLogService, h.core.GetDB(), ModulePortWrite, <OperType>, operlog.WithOperParam(operParamJSON))`;OperType 按 D-15:shutdown/undo/dot1x_enable/dot1x_disable→`operlog.OperTypeStatus`;description→`operlog.OperTypeUpdate`
9. `response.Success(c, result)`(单端口返回 *PortResult)
10. **顺序绝对**:`audit INSERT → operlog.Record → response.Success`(CLAUDE.md operlog 强制约定 + D-04)

实现 `BatchWrite(c *gin.Context)`:绑定 `portwrite.BatchWriteRequest`(直接复用 Phase 51 struct);operator 同上;调 `service.BatchWritePorts`;sentinel 翻译:`ErrBatchTooLarge`→400 "批量端口数超过上限 50";`ErrEmptyBatch`→400 "批量端口列表为空";`ErrMixedDevices`→400 "批量端口必须属于同一设备";`ErrPortNotFound`→404;`ErrDeviceNotFound`→404;其他→500。**不写 audit,不调 operlog**(sentinel 路径)。成功路径:遍历 `append(append(result.Succeeded, result.Failed...), result.Skipped...)`,每端口 `buildAuditRow` + `db.Create`(独立 INSERT,失败 applogger.Warnf 不阻塞响应),收集 auditIDs 列表;组装 batchSummaryJSON:`{"action":req.Action, "batch_size":len(req.PortIDs), "succeeded_count":len(result.Succeeded), "failed_count":len(result.Failed), "skipped_count":len(result.Skipped), "device_id":req.DeviceID, "audit_ids":auditIDs}`;调 `operlog.Record(c, h.core.OperLogService, h.core.GetDB(), ModulePortWrite, operlog.OperTypeBatch, operlog.WithOperParam(batchSummaryJSON))`;最后 `response.Success(c, result)`。

helper `buildAuditRow(pr *portwrite.PortResult, beforeValue json.RawMessage, deviceID, operator string) *models.PortWriteAudit` 按 PATTERNS.md §4e 实现;失败路径 `pr.Error` 同时填 DeviceResponse 和 FailureReason(&pr.Error)。

`internal/utils/applogger` 用于 batch 中单条 audit 失败的告警(参考 fix_suggestion_handler.go 是否已 import applogger,如未用可用 `log.Printf` 替代 — executor 选择)。

新建 `port_write_handler_test.go`(同 package,_test.go 后缀)。用 sqlite in-memory + testify/mock:开 sqlite `gorm.Open(sqlite.Open(":memory:"))` + **手动 AutoMigrate** `&models.PortWriteAudit{}` + `&models.DevicePortStatus{}` + `&models.NetworkDevice{}`(Wave 2 才正式注册到 database.go,Wave 1 测试手动建表)。mock `portwrite.PortWriteService`(testify/mock 实现 6 方法)。mock 或 stub `core.Core` 的 `GetDB()` / `OperLogService`(OperLogService 用 nil 即可,Record 在 nil 时静默 return — 见 operlog.go:229-231);若需断言 operlog 被调用,用 testify/mock 包装 OperLogService。覆盖 <behavior> 中 8 个测试用例。

**绝对禁止**:在本任务中给 `internal/utils/operlog/operlog.go` 加任何东西(包括 WithOperID);给 audit.oper_log_id 列填值(必须 nil/NULL)。这两点 Path C 已锁定。
  </action>
  <verify>
    <automated>
cd "D:/code/ClaudeCode/xingran-go-backend" && \
  go build ./internal/api/v1/network/... && \
  go test ./internal/api/v1/network/... -run 'TestPortWriteHandler' -count=1 -v && \
  ! grep -q 'WithOperID' internal/api/v1/network/port_write_handler.go && \
  bash -c "count=$(grep -v '^\s*//' internal/api/v1/network/port_write_handler.go | grep -c 'operlog.Record') && [ \"\$count\" -ge 6 ]" && \
  echo "TASK2 GREEN"
    </automated>
  </verify>
  <acceptance_criteria>
- `internal/api/v1/network/port_write_handler.go` 含 `const ModulePortWrite = "端口管理"`
- 文件中 `operlog.Record(` 调用数 ≥ 6(5 单端口 + 1 batch)
- 文件**不**含 `WithOperID`(Path C 强制)
- 文件**不**含 `WithJsonResult`(幻觉 option,不存在)
- 文件**不**含 `RecordWithBody`(本 phase 端点非敏感端点 — 无 password/secret/key/token 字段)
- audit.oper_log_id 字段在所有 6 个 handler 写入路径都是 nil/未设置(Path C,该列 nullable)
- 5 个 sentinel 错误都用 `errors.Is` 匹配(`errors.Is(err, portwrite.ErrPortNotFound)` 等),不用字符串比较
- PortResult.Status=="failed" 路径走 `response.Success`(200)且写 audit 行(status=failed);sentinel 路径走 `response.Error`(4xx)且不写 audit 行
- batch handler 遍历 Succeeded+Failed+Skipped 三切片写 N 条 audit 行,而非只写 Succeeded
- `go test ./internal/api/v1/network/... -run TestPortWriteHandler -count=1 -v` 全 PASS(覆盖 <behavior> 8 个用例)
  </acceptance_criteria>
  <done>6 个 handler 方法全部就位,8 个 behavior 测试用例全绿;sentinel→HTTP 翻译 + audit 三态写入 + Path C(audit_ids 嵌 operlog)+ OperType 映射(D-15)全部正确。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: port_write_router.go + network_router.go 注册 + 路由测试</name>
  <files>internal/api/v1/network/port_write_router.go, internal/api/v1/network/network_router.go, internal/api/v1/network/port_write_router_test.go</files>
  <behavior>
- TestSetupPortWriteRouter_GroupPermission:跑 SetupPortWriteRouter 后断言 `/network/ports/write/shutdown` `/undo-shutdown` `/description` `/dot1x-enable` `/dot1x-disable` `/batch` 6 个 POST 端点都在 gin.Engine 路由树中
- TestSetupPortWriteRouter_UsesNetworkPortWriteConstant:断言 router 文件源码引用 `permission.NetworkPortWrite`(不硬编码字符串)— 源断言 grep
- TestSetupPortWriteRouter_RequirePermissions2Arg:断言 router 文件源码含 `middleware.RequirePermissions([]string{string(permission.NetworkPortWrite)}, core)`(2-arg,含 core 第二参)— 源断言 grep
- TestNetworkRouter_RegistersSetupPortWriteRouter:断言 network_router.go 含 `SetupPortWriteRouter(ports, core)` 调用 — 源断言 grep
  </behavior>
  <read_first>
    - `.planning/phases/52-w3-router-handler-operlog-permission-migration/52-PATTERNS.md` §5a-5c(router 完整骨架)+ §6(network_router.go 插入点)
    - `.planning/phases/52-w3-router-handler-operlog-permission-migration/52-RESEARCH.md` §2.2(network_router.go ports 组结构 VERIFIED line 206-214)+ §1.4(RequirePermissions 2-arg VERIFIED line 200)
    - `internal/api/v1/network/network_router.go`(line 205-219,ports 组 + SetupPortRouter 调用位置)
    - `internal/api/v1/network/port_router.go`(line 8-19,kebab 命名参考)
    - `pkg/middleware/permission.go`(line 200,RequirePermissions 2-arg 签名 VERIFIED)
    - `pkg/permission/config.go`(Task 1 产出的 NetworkPortWrite 常量)
  </read_first>
  <action>
新建 `internal/api/v1/network/port_write_router.go`,package network。Imports:`github.com/gin-gonic/gin` / `internal/core` / `internal/services/portwrite` / `pkg/middleware` / `pkg/permission`。

实现 `SetupPortWriteRouter(r *gin.RouterGroup, core *core.Core)`:在函数内构造 service `svc := portwrite.NewPortWriteService(core.GetDB(), core.DeviceExecutor, core.DeviceInfoCollectionService)`(Phase 51 factory 签名),`handler := NewPortWriteHandler(svc).WithCore(core)`;建子组 `write := r.Group("/write")`;**组级挂 2-arg RequirePermissions**:`write.Use(middleware.RequirePermissions([]string{string(permission.NetworkPortWrite)}, core))`(RESEARCH §1.4 VERIFIED,2-arg 不是 1-arg,第二参 core 必填);注册 6 个 kebab POST 端点:`write.POST("/shutdown", handler.Shutdown)` / `"/undo-shutdown", handler.UndoShutdown` / `"/description", handler.SetDescription` / `"/dot1x-enable", handler.EnableDot1x` / `"/dot1x-disable", handler.DisableDot1x` / `"/batch", handler.BatchWrite`。函数签名 `(r *gin.RouterGroup, core *core.Core)`(对齐 port_router.go 的 SetupPortRouter 签名风格,但去掉 exportHandler 参数 — 写路由不需要)。

修改 `internal/api/v1/network/network_router.go`:在 line 213 `SetupPortRouter(ports, core, exportHandler)` 之后(仍在 `{ ... }` 块内),插入一行 `SetupPortWriteRouter(ports, core)`。**不要**修改 ports 组本身的 `RequirePermissionsWithQuery`(那是查询路径的放宽,写路径独立鉴权)。注释标 `// Phase 52: 写操作子组 /network/ports/write/* + 组级 RequirePermissions([network:port:write])`。

新建 `port_write_router_test.go`(同 package)。注册到一个测试用 gin.Engine:`gin.New()` + `r := engine.Group("/network/ports")` + `SetupPortWriteRouter(r, &core.Core{})`(空 Core 即可,不调用 handler 体内路径,仅验证路由树)。用 `engine.Routes()` 遍历断言 6 个 method=POST path 含 `/network/ports/write/<kebab>` 全部存在。Path 用 `strings.Contains` 匹配。**注意**:Core 在 RequirePermissions 中被使用 — 测试用空 Core 会让鉴权失败但路由注册仍完成;若需绕过,可在测试中显式调用路由注册但跳过中间件执行。**替代方案**(更稳):测试用源码 grep 断言而非运行时调用,见 <behavior> 后两条。

源码 grep 断言(避免 gin 鉴权在测试中拦截):用 `grep -c` 在 router 源文件中确认 `permission.NetworkPortWrite`(非常量字符串)+ `RequirePermissions([]string{string(permission.NetworkPortWrite)}, core)`(2-arg 含 core)的存在。
  </action>
  <verify>
    <automated>
cd "D:/code/ClaudeCode/xingran-go-backend" && \
  go build ./... && \
  go test ./internal/api/v1/network/... -run 'TestSetupPortWriteRouter|TestNetworkRouter' -count=1 -v && \
  bash -c "grep -v '^\s*//' internal/api/v1/network/port_write_router.go | grep -c 'middleware.RequirePermissions(\\[\\]string{string(permission.NetworkPortWrite)}, core)' | grep -q '^1$'" && \
  grep -v '^\s*//' internal/api/v1/network/network_router.go | grep -q 'SetupPortWriteRouter(ports, core)' && \
  echo "TASK3 GREEN"
    </automated>
  </verify>
  <acceptance_criteria>
- `internal/api/v1/network/port_write_router.go` 含 `func SetupPortWriteRouter(r *gin.RouterGroup, core *core.Core)`
- 该文件含 `middleware.RequirePermissions([]string{string(permission.NetworkPortWrite)}, core)`(完整 2-arg 形式,with core)
- 该文件**不**含 `RequirePermissions([]string{string(permission.NetworkPortWrite)})`(无尾随逗号 core 的 1-arg 错误形式)
- 该文件引用 `permission.NetworkPortWrite`(非常量字符串硬编码)
- 该文件注册 6 个 kebab 路径:shutdown / undo-shutdown / description / dot1x-enable / dot1x-disable / batch
- `internal/api/v1/network/network_router.go` 含 `SetupPortWriteRouter(ports, core)` 调用(在 `SetupPortRouter(ports, core, exportHandler)` 之后)
- `go build ./...` exit 0(全包编译,无回归)
- `go test ./internal/api/v1/network/... -run 'TestSetupPortWriteRouter|TestNetworkRouter' -count=1 -v` 全 PASS
  </acceptance_criteria>
  <done>路由组 `/network/ports/write/*` 注册到 network_router.go;6 个端点全部可解析;组级 2-arg RequirePermissions 挂载;Wave 1 验证脚本全绿(见 plan 末尾 verification)。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Gin router | HTTP 请求穿越 auth 中间件(JWT)后进入 ports 组 |
| ports 组 → write 子组 | 跨越 RBAC 边界:无 network:port:write 权限用户必须被组级 RequirePermissions 拦截 |
| write 子组 → Phase 51 service | sentinel error 与 PortResult.Status 双路径分流(4xx 不写 audit / 200 写 audit) |
| handler → sys_port_write_audit 表 | audit INSERT 必须在 operlog.Record + response.Success 之前 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-01 | Elevation of Privilege | 6 个写端点 / port_write_router.go | HIGH | mitigate | 组级 `middleware.RequirePermissions([]string{string(permission.NetworkPortWrite)}, core)`(2-arg 含 core,Task 3)一处覆盖 6 端点;router 测试 grep 断言该中间件存在且引用常量非硬编码;superadmin 走 core 旁路自动放行(permission.go:209-213) |
| T-52-02 | Repudiation | port_write_handler.go audit + operlog | HIGH | mitigate | 6 handler 严格顺序 `audit INSERT → operlog.Record → response.Success`(Task 2);NoOp/skipped 路径也写 audit 行(D-04);sentinel 路径(入口校验失败,无 SSH 流量)不写 audit 是 by-design(无设备命令可追溯);handler 测试覆盖三种 PortResult.Status 都写 audit |
| T-52-03 | Tampering | batch 端点 / BatchWriteRequest | MEDIUM | mitigate | Phase 51 service 入口已锁 maxBatchSize=50 + ErrMixedDevices 拒绝跨设备(Wave 1 不重做);handler 通过 errors.Is 翻译 ErrBatchTooLarge → 400,不绕过 service 校验;oper_param.audit_ids 完整记录每端口 audit 行 |
| T-52-04 | Information Disclosure | operlog / audit.command_sent 字段 | LOW | accept | command_sent 是未脱敏真相源(D-15 / AUDIT-03 明确);本 phase 端点非敏感端点(无 password/secret/key/token 字段)→ 用 Record 不用 RecordWithBody;operlog 11 关键词自动脱敏仍生效(regression_test.go 锁定),但本端点 oper_param 不含敏感字段 |
| T-52-05 | Tampering | SQL 注入(PortWriteAudit INSERT) | LOW | mitigate | 用 GORM `db.Create(&PortWriteAudit{...})` 参数化(非字符串拼 SQL);audit.oper_log_id 列保持 NULL(Path C)避免与未写入的 operlog 行产生 FK 不一致 |
| T-52-SC | — | (无 npm/pip/cargo 包安装) | — | accept | 本 phase 仅消费 Go 标准库 + 已有依赖,无包管理器安装步骤 |

## Security Gate Decision: PASS

所有 HIGH 威胁(T-52-01 / T-52-02)在 Task 2 + Task 3 中有具体 mitigation,且通过自动化测试 + 源码 grep 断言验证。无 HIGH 威胁处于无 mitigation 状态。
</threat_model>

<verification>
Wave 1 全部任务完成后,在 plan 提交前依次跑(VALIDATION.md §4.3 Wave 1 脚本):

```bash
cd "D:/code/ClaudeCode/xingran-go-backend"

# 1. 全包编译(零回归)
go build ./...

# 2. operlog 包回归锁 intact(本 phase 不动 operlog 包,必须保持绿色)
go test ./internal/utils/operlog/... -count=1 -v

# 3. Phase 51 service 回归(零侵入验证)
go test ./internal/services/portwrite/... -count=1 -v

# 4. Wave 1 新增 handler/router 测试
go test ./internal/api/v1/network/... -count=1 -v

# 5. go vet(verify-work 前置)
go vet ./...

# 6. Path C 守卫(防止后续误改)
! grep -q 'WithOperID' internal/api/v1/network/port_write_handler.go
! grep -q 'WithJsonResult' internal/api/v1/network/port_write_handler.go
```

全部 exit 0 + 无 grep 命中 = Wave 1 GREEN,可提交。
</verification>

<success_criteria>
- ROADMAP Phase 52 Success Criteria #1, #2, #3, #4, #7 全部满足
- #1: 6 端点在 port_write_router.go 注册 + 在 network_router.go 接线(Task 3)
- #2: 组级 RequirePermissions(["network:port:write"]) 挂载(Task 3,2-arg 含 core)
- #3: 6 handler 在 response.Success 之前调 operlog.Record(...,"端口管理",...) (Task 2,OperType 按 D-15)
- #4: NetworkPortWrite 常量加入 pkg/permission/config.go(Task 1)
- #7: port_write_router.go 引用 permission.NetworkPortWrite,非硬编码字符串(Task 3 源断言)
- #8(部分):go build / go test ./internal/utils/operlog/... / go test ./internal/services/portwrite/... 全绿(Phase 34/51 回归锁 intact)
- Success Criteria #5(sys_port_write_audit 表)+ #6(菜单 seed + helper 授权)= Wave 2 范围
</success_criteria>

<output>
完成后创建 `.planning/phases/52-w3-router-handler-operlog-permission-migration/52-01-SUMMARY.md`,记录:
- 6 文件(4 创建 + 2 修改)+ 2 测试文件就位
- 8 个 handler behavior 测试 + router 测试全绿
- Path C / Path X / 2-arg RequirePermissions 三大决策落地证据(grep 输出)
- Phase 51 + Phase 34 回归锁保持绿色的命令输出
- Next: 52-02-PLAN.md(Wave 2:migration + 菜单 seed + helper)
</output>
