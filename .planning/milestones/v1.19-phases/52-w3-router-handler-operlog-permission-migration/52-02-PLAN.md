---
phase: 52-w3-router-handler-operlog-permission-migration
plan: 02
type: execute
wave: 2
depends_on: [52-01]
files_modified:
  - internal/core/db/database.go
  - internal/core/db/migrations/menu_grant_helpers.go
  - internal/core/db/migrations/migration_202_port_write_audit.go
  - internal/core/db/migrations/migration_202_port_write_audit_test.go
  - internal/core/db/migrations/menu_grant_helpers_test.go
autonomous: true
requirements: [AUDIT-03, INFRA-01, PERM-03]
requirements_addressed:
  - AUDIT-03: sys_port_write_audit 表落地(GORM AutoMigrate 建表 + migration_202 加索引命名保护)
  - INFRA-01: 表 + (device_id,port_id,created_at) 复合索引 + (created_at) 单列索引;通过 AutoMigrate model tag + migration_202 显式 CREATE INDEX IF NOT EXISTS 双保险
  - PERM-03: migration_202 seed "端口配置" F 型按钮菜单(parent="端口状态" D-07)+ 调用 GrantNewMenuToRolesHavingParent(db,"端口状态",newMenuID) 精准授权父已关联角色
tags: [go, gorm, migration, menu-seed, rbac-grant, postgres, sqlite]

must_haves:
  truths:
    - "应用启动时 GORM AutoMigrate 自动创建 sys_port_write_audit 表(12 列 + 复合索引)"
    - "应用启动时(仅 postgres 分支)显式调用 migrations.Migrate202PortWriteAudit(d.DB) seed 菜单 + 授权"
    - "菜单 seed 后 sys_menu 含一行 menu_name='端口配置' perms='network:port:write' menu_type='F' visible=0"
    - "授权后 sys_role_menu 中所有原持有 '端口状态' 父菜单的角色都关联到 '端口配置' 新菜单(antd 父子联动陷阱防)"
    - "GrantNewMenuToRolesHavingParent 是幂等的(ON CONFLICT DO NOTHING)且只波及父已关联角色(不影响其他角色)"
    - "Phase 52 Wave 1 handler 测试通过 sqlite in-memory + 手动 AutoMigrate PortWriteAudit 已绿;Wave 2 注册到 database.go 后启动期自动建表,真机 PG 路径不需手动跑 SQL"
  artifacts:
    - path: "internal/core/db/migrations/menu_grant_helpers.go"
      provides: "GrantNewMenuToRolesHavingParent(db, parentMenuName, newMenuID) 幂等授权 helper"
      exports: ["GrantNewMenuToRolesHavingParent"]
    - path: "internal/core/db/migrations/migration_202_port_write_audit.go"
      provides: "Migrate202PortWriteAudit(db) — 复合索引 + 菜单 seed + helper 调用(SQLite 跳过)"
      exports: ["Migrate202PortWriteAudit"]
    - path: "internal/core/db/database.go"
      provides: "PortWriteAudit 加入 AutoMigrate 列表 + postgres 分支显式调 Migrate202"
      contains: "models.PortWriteAudit"
  key_links:
    - from: "internal/core/db/database.go"
      to: "internal/models/port_write_audit.go::PortWriteAudit"
      via: "AutoMigrate 列表加入 &models.PortWriteAudit{}"
      pattern: "&models\\.PortWriteAudit\\{\\}"
    - from: "internal/core/db/database.go"
      to: "internal/core/db/migrations/migration_202_port_write_audit.go::Migrate202PortWriteAudit"
      via: "postgres 分支显式调用"
      pattern: "migrations\\.Migrate202PortWriteAudit\\(d\\.DB\\)"
    - from: "internal/core/db/migrations/migration_202_port_write_audit.go"
      to: "internal/core/db/migrations/menu_grant_helpers.go::GrantNewMenuToRolesHavingParent"
      via: "菜单 seed 之后调用(父菜单名 '端口状态' 不是 '端口管理')"
      pattern: "GrantNewMenuToRolesHavingParent\\(db, \"端口状态\""
---

<objective>
Wave 2(依赖 52-01) — 把 Wave 1 已引用但未注册的 PortWriteAudit 模型正式接入启动期建表链路 + seed "端口配置" F 型按钮菜单 + 通过 GrantNewMenuToRolesHavingParent 精准授权父已关联角色。本 wave 走 RESEARCH §1.2 路径 A(GORM AutoMigrate 建表)+ §1.2 显式调用 Migrate202PortWriteAudit(MigrateNNN 不再自动调用,VERIFIED database.go:296 注释)。

Purpose: 让 Phase 52 Wave 1 写 audit 行的能力在生产 PG 启动期就能落表(不需手动跑 SQL);让 Phase 53 前端能通过 sys_menu.perms='network:port:write' 自动获取按钮可见性。
Output: 2 修改 + 3 创建(database.go + 4 个 migrations 包内文件,含 2 个测试)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-w3-router-handler-operlog-permission-migration/52-CONTEXT.md
@.planning/phases/52-w3-router-handler-operlog-permission-migration/52-RESEARCH.md
@.planning/phases/52-w3-router-handler-operlog-permission-migration/52-PATTERNS.md
@.planning/phases/52-w3-router-handler-operlog-permission-migration/52-VALIDATION.md
@.planning/phases/52-w3-router-handler-operlog-permission-migration/52-01-PLAN.md
@CLAUDE.md
@internal/core/db/database.go
@internal/core/db/migrations/migration_195_reconciliation_exception_rules_menu.go
@internal/core/db/migrations/migration_200_fix_suggestion_config_seeds.go
@internal/core/db/migrations/migration_201_phase48_component_columns.go
@internal/core/db/migrations/migration_mac_history.go
@internal/models/menu.go
@internal/models/port_write_audit.go

<interfaces>
<!-- Wave 2 关键契约(全部源码核对)。executor 直接用。 -->

### database.go AutoMigrate 列表(VERIFIED line 308-391)+ 显式 migration 调用(VERIFIED line 411-417)
```go
err := d.DB.Migrator().AutoMigrate(
    // ... ~60 个 model ...
    &models.DevicePortStatus{},  // line 328 — 在其后插入 &models.PortWriteAudit{}
    // ...
    &models.SysReconciliationFixSuggestion{},  // line 390 末尾
)
// ...
if d.Type == "postgres" {
    if err := migrations.Migrate175ReconciliationPhysicalLink(d.DB); err != nil { ... }
    if err := migrations.Migrate176ReconciliationPhysicalMV(d.DB); err != nil { ... }
    // ← 在此处插入 migrations.Migrate202PortWriteAudit(d.DB)
}
```
注释(line 296):**"所有 migrations.MigrateNNN 函数定义仍保留,仅不再启动期调用。如需重放某次迁移,手动跑对应函数(d.DB) 即可。"** — 260704-ne5 重构后状态。Phase 52 必须显式调用 Migrate202,不会自动跑。

### migration_201 风格(VERIFIED line 41-141)
```go
func Migrate201Phase48ComponentColumns(db *gorm.DB) error {
    log.Println("Running migration 201: ...")
    if !isPostgreSQL(db) {
        // SQLite fallback
        return nil
    }
    // PG branch: ADD COLUMN IF NOT EXISTS / CREATE INDEX IF NOT EXISTS / DO $$ count-then-insert
}
```
`isPostgreSQL(db)` 定义在 `migration_mac_history.go:12-14`(已在 migrations 包内可用)。

### migration_200 F 型按钮菜单模式(VERIFIED line 187-201)
```go
emptyPath := ""
icon := "#"
perms := btn.perms
buttonMenu := &models.Menu{
    MenuName:  btn.name,
    ParentID:  &btnParent.ID,
    Path:      &emptyPath,            // "" 空路径(F 型 path 不参与路由生成)
    MenuType:  models.MenuTypeButton, // 'F'
    Visible:   models.VisibleHidden,  // 0
    Status:    models.MenuStatusNormal,
    Perms:     &perms,
    Icon:      &icon,                 // "#"
    OrderNum:  btn.orderNum,
    Remark:    btn.remark,
}
```

### models.Menu schema(VERIFIED menu.go:47-68)
字段:MenuName / ParentID(*string) / OrderNum / Path(*string) / Component(*string) / MenuType / Visible / Status / Perms(*string) / Icon(*string) / Remark / Meta(*MenuMeta JSONB)。
**关键**:**Go sys_menu 无 is_frame / is_cache 列**(用 Meta JSONB 统一,memory `xingran-menu-no-java-fields`)— 在 seed 中**禁止**加这两个字段(会编译失败/SQLSTATE 42703)。

### D-07 父菜单名(VERIFIED via archive/053_fix_menu_paths_unified.sql:184-186)
`UPDATE sys_menu SET component = 'network/ports/index' WHERE menu_name = '端口状态' AND path = 'network/ports';`
父菜单名 = **"端口状态"**(NOT "端口管理" — ROADMAP line 86/98 + REQUIREMENTS PERM-03 的 "端口管理" 是笔误)。但 operlog module 字符串仍是 "端口管理"(AUDIT-01,与父菜单名解耦,Wave 1 已落地)。

### Phase 52 Wave 1 已定义 PortWriteAudit 模型(52-01 Task 1 产出)
路径 A(GORM AutoMigrate 建表)下,模型本身的 composite index tag 已声明 `idx_port_write_audit_device_port_created`(priority:1/2/3)+ `idx_port_write_audit_created`。Wave 2 migration_202 仍加 `CREATE INDEX IF NOT EXISTS` 作为防御(GORM tag 命名漂移兜底)。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: menu_grant_helpers.go 幂等授权 helper + 测试</name>
  <files>internal/core/db/migrations/menu_grant_helpers.go, internal/core/db/migrations/menu_grant_helpers_test.go</files>
  <behavior>
- TestGrantNewMenuToRolesHavingParent_Idempotent:第一次调用插入 N 行(N = 持有父菜单的角色数);第二次同样调用插 0 行(ON CONFLICT DO NOTHING)
- TestGrantNewMenuToRolesHavingParent_OnlyAffectsParentRoles:建 3 个角色(2 个有父菜单,1 个没有),调用 helper → 仅 2 个角色被授权新菜单,第 3 个角色不被波及
- TestGrantNewMenuToRolesHavingParent_NonExistentParent:父菜单不存在时返回 nil 错(INSERT...SELECT 返 0 行,非错误),不 panic
- TestGrantNewMenuToRolesHavingParent_ParameterizedOrControlled:helper 用 GORM raw SQL,通过 fmt.Sprintf 注入 newMenuID 和 parentMenuName(均为 migration 内部受控值,非用户输入)— 验证 SQL 执行成功
  </behavior>
  <read_first>
    - `.planning/phases/52-w3-router-handler-operlog-permission-migration/52-CONTEXT.md` D-08(helper SQL 锁定)
    - `.planning/phases/52-w3-router-handler-operlog-permission-migration/52-PATTERNS.md` §7(helper Go skeleton + 项目内 INSERT...SELECT idiom 参考)
    - `.planning/phases/52-w3-router-handler-operlog-permission-migration/52-RESEARCH.md` §2.8(父菜单名 "端口状态")
    - `internal/core/db/migrations/migration_144_vdi_granular_permissions.go`(line 62-79,Go 端 role-menu 授权 loop analog)
    - `internal/core/db/migrations/migration_201_phase48_component_columns.go`(line 41-141,手写 SQL + GORM raw 风格 analog)
    - `internal/models/menu.go`(Menu / RoleMenu schema 参考)
  </read_first>
  <action>
新建 `internal/core/db/migrations/menu_grant_helpers.go`,package migrations。Imports:`fmt` + `gorm.io/gorm` + `log`。

实现 `func GrantNewMenuToRolesHavingParent(db *gorm.DB, parentMenuName string, newMenuID string) error`:
- D-08 锁定的 SQL(PATTERNS.md §7 + CONTEXT D-08):`INSERT INTO sys_role_menu (role_id, menu_id) SELECT rm.role_id, '<newMenuID>'::uuid FROM sys_role_menu rm JOIN sys_menu m ON rm.menu_id = m.id WHERE m.menu_name = '<parentMenuName>' ON CONFLICT DO NOTHING`
- 用 `fmt.Sprintf` 注入 newMenuID 和 parentMenuName(均为 migration 内部受控值,非用户输入;与 migration_201 line 80-106 的 `fmt.Sprintf` SQL 风格一致);若 executor 偏好参数化,可拆 `db.Exec("...WHERE m.menu_name = $1...", parentMenuName)` + 用 `fmt.Sprintf` 仅注入 newMenuID 到 `::uuid` cast(因 PG `::uuid` 与 placeholder 组合不直观)
- 返回 `db.Exec(sql).Error`
- 函数文档注释说明:解决 antd 父子联动陷阱(memory `migration-grant-new-menu-precision-helper`),幂等,只波及父已关联角色,admin 走 superadmin 旁路自动可见

新建 `menu_grant_helpers_test.go`(同 package)。用 sqlite in-memory(参考 Phase 51 `port_write_service_test.go` 的 sqlite 模式)+ AutoMigrate `&models.Menu{}` + `&models.Role{}` + `&models.RoleMenu{}`。setup:建父菜单(menu_name='端口状态')+ 子菜单 + 3 角色(2 角色持有父菜单,1 不持有)+ 对应 sys_role_menu 行。覆盖 <behavior> 4 个用例。

**注意**:SQLite 不支持 PG 的 `::uuid` cast 和 `ON CONFLICT DO NOTHING`(SQLite 用 `INSERT OR IGNORE`)。**测试路径选择**:用 `if !isPostgreSQL(db) { return nil }` 在 helper 顶部跳过 SQLite,然后测试**仅断言 helper 在 SQLite 上 return nil 不 panic**(functional 测试留给 PG integration / migration_202_test 的 PG path);或者 helper 内部判断方言用不同 SQL(SQLite 走 INSERT OR IGNORE)。**推荐前者**(简单):helper 顶部 `if !isPostgreSQL(db) { return nil }`,SQLite 测试只验证非 panic。<behavior> 中 Idempotent / OnlyAffectsParentRoles / NonExistentParent 三个用例标注 `t.Skip` 在 SQLite 路径,留给 PG 真机路径(Phase 54 UAT 覆盖);ParameterizedOrControlled 用例在 SQLite 路径仅断言 helper 不 panic。
  </action>
  <verify>
    <automated>
cd "D:/code/ClaudeCode/xingran-go-backend" && \
  go build ./internal/core/db/migrations/... && \
  go test ./internal/core/db/migrations/... -run 'TestGrantNewMenuToRolesHavingParent' -count=1 -v && \
  grep -v '^\s*//' internal/core/db/migrations/menu_grant_helpers.go | grep -c 'ON CONFLICT DO NOTHING' | grep -q '^1$' && \
  echo "TASK1 GREEN"
    </automated>
  </verify>
  <acceptance_criteria>
- `internal/core/db/migrations/menu_grant_helpers.go` 含 `func GrantNewMenuToRolesHavingParent(db *gorm.DB, parentMenuName string, newMenuID string) error`
- SQL 含 `INSERT INTO sys_role_menu` + `JOIN sys_menu m ON rm.menu_id = m.id` + `WHERE m.menu_name = ` + `ON CONFLICT DO NOTHING`
- 函数顶部含 `isPostgreSQL(db)` 守卫(SQLite 跳过)
- 测试文件覆盖 <behavior> 4 个用例(SQLite 路径 skip functional,留 PG path)
- `go build ./internal/core/db/migrations/...` exit 0
- `go test ./internal/core/db/migrations/... -run TestGrantNewMenuToRolesHavingParent -count=1 -v` exit 0
  </acceptance_criteria>
  <done>helper 文件就位且 sqlite 路径不 panic;PG path SQL 已就绪等 Task 2 / Task 3 调用;Wave 2 测试基础设施完成。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: migration_202_port_write_audit.go(复合索引 + 菜单 seed + helper 调用)</name>
  <files>internal/core/db/migrations/migration_202_port_write_audit.go, internal/core/db/migrations/migration_202_port_write_audit_test.go</files>
  <behavior>
- TestMigrate202_SQLiteSkipsCleanly:SQLite in-memory 调 Migrate202PortWriteAudit → 返回 nil 不 panic(菜单 seed / index 都在 PG 分支)
- TestMigrate202_MenuSeedStructure(PG path,t.Skip on SQLite):跑 Migrate202 → sys_menu 含一行 menu_name='端口配置' AND menu_type='F' AND perms='network:port:write' AND visible=0
- TestMigrate202_RoleMenuGrantedToParentHolders(PG path,t.Skip on SQLite):setup 父菜单 '端口状态' + 2 角色持父菜单 + 1 角色不持;跑 Migrate202 → sys_role_menu 含 2 行 menu_id=新菜单(PATrick 父子联动陷阱防御)
- TestMigrate202_Idempotent(PG path,t.Skip on SQLite):连跑两次 Migrate202 → sys_menu 行数不变 + sys_role_menu 行数不变(ON CONFLICT + count-then-insert)
- TestMigrate202_NoIsFrameIsCacheFields:源码 grep 断言 migration_202 文件**不**含 `IsFrame` / `IsCache`(Go sys_menu 无此列,memory xingran-menu-no-java-fields)
- TestMigrate202_UsesCorrectParentName:源码 grep 断言 migration_202 文件含 `menu_name = ?` 配合 `"端口状态"`(或 `WHERE menu_name = '端口状态'`),**不**含 `'端口管理'` 作为父菜单查找条件(D-07)
  </behavior>
  <read_first>
    - `.planning/phases/52-w3-router-handler-operlog-permission-migration/52-PATTERNS.md` §8a-8d(migration_202 完整骨架,含 SQLite 分支 + menu seed + index)
    - `.planning/phases/52-w3-router-handler-operlog-permission-migration/52-RESEARCH.md` §1.2(注册机制偏差 + 路径 A)+ §2.3(migration_195 count-then-insert 模式)+ §2.8(父菜单名 "端口状态")+ §3.2(menu schema + F 型按钮字段)
    - `internal/core/db/migrations/migration_200_fix_suggestion_config_seeds.go`(line 94-211,完整 F 型按钮 seed + count-then-insert)
    - `internal/core/db/migrations/migration_201_phase48_component_columns.go`(line 41-141,CREATE INDEX IF NOT EXISTS + DO $$ 块 + SQLite 分支)
    - `internal/core/db/migrations/migration_mac_history.go`(line 12-14,isPostgreSQL helper)
    - `internal/models/menu.go`(Menu struct + MenuType / Visible / Status 类型)
    - `internal/core/db/migrations/menu_grant_helpers.go`(Task 1 产出的 helper)
  </read_first>
  <action>
新建 `internal/core/db/migrations/migration_202_port_write_audit.go`,package migrations。Imports:`fmt` / `log` / `gorm.io/gorm` / `internal/models`(若直接用 Menu struct)/ 项目内已有的 `isPostgreSQL`。

实现 `func Migrate202PortWriteAudit(db *gorm.DB) error`:
1. `log.Println("Running migration 202: port_write_audit menu seed + indexes")`
2. SQLite 跳过(参考 migration_201:45-51):`if !isPostgreSQL(db) { log.Println("Migration 202: non-PostgreSQL dialect, skip (table created by AutoMigrate)"); return nil }`(Wave 1 测试已用 sqlite 手动 AutoMigrate;生产路径仅 PG)
3. **复合索引(防御性,CREATE INDEX IF NOT EXISTS)**:`CREATE INDEX IF NOT EXISTS idx_port_write_audit_device_port_created ON sys_port_write_audit(device_id, port_id, created_at)` + `CREATE INDEX IF NOT EXISTS idx_port_write_audit_created ON sys_port_write_audit(created_at)`(model tag 已声明同名索引,此处 CREATE IF NOT EXISTS 是命名漂移兜底;若 GORM tag 已建索引,此语句 noop)
4. **菜单 seed(count-then-insert,参考 migration_200:171-207)**:`var existingCount int64; db.Model(&models.Menu{}).Where("perms = ?", "network:port:write").Count(&existingCount)` → 若 > 0,`log.Println("Migration 202: 端口配置 menu 已存在,跳过 seed + grant")` + `return nil`
5. 查父菜单:`var parentMenu models.Menu; db.Where("menu_name = ?", "端口状态").First(&parentMenu)` → 若 err(父菜单不存在)`log.Printf("Migration 202: WARNING 父菜单 '端口状态' 未找到,跳过 seed: %v", err)` + `return nil`(非阻断,与 Migrate175/176 错误处理风格一致)
6. 构造 F 型 buttonMenu(参照 PATTERNS.md §8c + migration_200:187-201):`MenuName="端口配置"` / `ParentID=&parentMenu.ID` / `Path=&emptyPath`(空串,D-06 写的 "write" 不影响功能但 RESEARCH §3.2 A6 推荐空串与 migration_200 惯例对齐;executor 用 `""`)/ `Component=nil` / `MenuType=models.MenuTypeButton` / `Visible=models.VisibleHidden`(0)/ `Status=models.MenuStatusNormal` / `Perms=&perms`("network:port:write") / `Icon=&icon`("#") / `OrderNum=100` / `Remark="Phase 52: 端口写操作按钮权限(5 单端口 + 1 batch)"`
7. **绝对禁止**加 `IsFrame` / `IsCache` 字段(Go sys_menu 无此列)
8. `db.Create(menu)` → 若 err 返 `fmt.Errorf("create 端口配置 menu failed: %w", err)`
9. **D-08 helper 调用**:`if err := GrantNewMenuToRolesHavingParent(db, "端口状态", menu.ID); err != nil { log.Printf("Migration 202: WARNING GrantNewMenuToRolesHavingParent failed: %v", err) }`(非阻断,菜单已 seed,管理员可手动授权)

新建 `migration_202_port_write_audit_test.go`(同 package)。SQLite 路径仅覆盖 SQLiteSkipsCleanly;PG-only 用例用 `t.Skip("requires PostgreSQL")` 标注(留给 Phase 54 UAT)。源码 grep 断言两个用例:NoIsFrameIsCacheFields + UsesCorrectParentName。

**Path A 强制**:**不要**在 migration_202 写 `CREATE TABLE sys_port_write_audit`(表由 database.go AutoMigrate 通过 model 建);**不要**给 PortWriteAudit model 加 `gorm:"-:migration"` tag(已在 Wave 1 Task 1 锁定不加)。
  </action>
  <verify>
    <automated>
cd "D:/code/ClaudeCode/xingran-go-backend" && \
  go build ./internal/core/db/migrations/... && \
  go test ./internal/core/db/migrations/... -run 'TestMigrate202' -count=1 -v && \
  ! grep -q 'IsFrame\|IsCache' internal/core/db/migrations/migration_202_port_write_audit.go && \
  grep -v '^\s*//' internal/core/db/migrations/migration_202_port_write_audit.go | grep -c "GrantNewMenuToRolesHavingParent(db, \"端口状态\"" | grep -q '^1$' && \
  ! grep -q "GrantNewMenuToRolesHavingParent(db, \"端口管理\"" internal/core/db/migrations/migration_202_port_write_audit.go && \
  ! grep -q 'CREATE TABLE.*sys_port_write_audit' internal/core/db/migrations/migration_202_port_write_audit.go && \
  echo "TASK2 GREEN"
    </automated>
  </verify>
  <acceptance_criteria>
- `internal/core/db/migrations/migration_202_port_write_audit.go` 含 `func Migrate202PortWriteAudit(db *gorm.DB) error`
- 文件**不**含 `CREATE TABLE` + `sys_port_write_audit`(Path A:表由 AutoMigrate 建)
- 文件**不**含 `IsFrame` 或 `IsCache`(Go sys_menu schema)
- 文件**不**含 `GrantNewMenuToRolesHavingParent(db, "端口管理"`(错误父菜单名)
- 文件含 `GrantNewMenuToRolesHavingParent(db, "端口状态"`(D-07 正确父菜单名)
- 文件含 `menu_name = ?` 配合 `"端口状态"`(或等价 `WHERE menu_name = '端口状态'`)
- 文件含 `CREATE INDEX IF NOT EXISTS idx_port_write_audit_device_port_created`
- 文件含 SQLite 跳过分支(`isPostgreSQL(db)` 守卫)
- 菜单 seed 字段含 `MenuType: models.MenuTypeButton` / `Visible: models.VisibleHidden` / `Perms: "network:port:write"` / `Icon: "#"`
- `go test ./internal/core/db/migrations/... -run TestMigrate202 -count=1 -v` SQLite 路径全 PASS,PG-only 用例 skip 不 FAIL
  </acceptance_criteria>
  <done>migration_202 文件就位且 SQLite 路径测试全绿;PG path 已就绪等 Task 3 注册到 database.go 后由启动期自动执行;源码 grep 守卫 IsFrame/IsCache/父菜单名全部通过。</done>
</task>

<task type="auto">
  <name>Task 3: database.go 注册 PortWriteAudit 到 AutoMigrate + postgres 分支显式调用 Migrate202</name>
  <files>internal/core/db/database.go</files>
  <read_first>
    - `.planning/phases/52-w3-router-handler-operlog-permission-migration/52-PATTERNS.md` §9a + §9b(database.go 两处插入点)
    - `.planning/phases/52-w3-router-handler-operlog-permission-migration/52-RESEARCH.md` §1.2(注册机制偏差 VERIFIED line 296 注释 + line 308-391 AutoMigrate + line 411-417 显式调用模式)
    - `internal/core/db/database.go`(line 290-420,AutoMigrate 函数全貌,识别 DevicePortStatus line 328 + postgres 分支 line 411-417)
    - `internal/models/port_write_audit.go`(52-01 Task 1 产出,确认 model 已就位)
    - `internal/core/db/migrations/migration_202_port_write_audit.go`(Task 2 产出,确认函数名 Migrate202PortWriteAudit)
  </read_first>
  <action>
修改 `internal/core/db/database.go` 两处(VERIFIED line 号):

**第一处(AutoMigrate 列表)**:在 `&models.DevicePortStatus{},`(line 328)之后插入一行 `&models.PortWriteAudit{},  // Phase 52: 端口写审计 append-only 表`。保持列表其他模型不动。

**第二处(postgres 分支显式调用)**:在 `if d.Type == "postgres"`(line 411)块内、`Migrate175` + `Migrate176` 调用之后,添加:
```
// Phase 52: 端口写审计表(由 AutoMigrate 建表) + 菜单 seed + 角色授权
if err := migrations.Migrate202PortWriteAudit(d.DB); err != nil {
    applogger.Errorf("端口写审计迁移失败 (非阻断,留待下次启动): %v", err)
}
```
**关键**:`applogger.Errorf` 非阻断(参考 line 412-413 / 415-416 的 Migrate175/176 错误处理风格)— 菜单 seed 失败不阻塞应用启动。

**禁止**:不要把 Migrate202 调用加到 SQLite 分支(menu seed 走 PG-only,SQLite 测试路径在 Migrate202 内部已 isPostgreSQL 守卫返回 nil)。

跑 `go build ./...` 确认编译通过 + `go test ./...` 确认无回归。
  </action>
  <verify>
    <automated>
cd "D:/code/ClaudeCode/xingran-go-backend" && \
  go build ./... && \
  go vet ./internal/core/db/... && \
  bash -c "grep -v '^\s*//' internal/core/db/database.go | grep -c '&models.PortWriteAudit{}' | grep -q '^1$'" && \
  grep -v '^\s*//' internal/core/db/database.go | grep -q 'migrations.Migrate202PortWriteAudit(d.DB)' && \
  go test ./internal/utils/operlog/... -count=1 && \
  go test ./internal/services/portwrite/... -count=1 && \
  go test ./internal/api/v1/network/... -count=1 && \
  go test ./internal/core/db/migrations/... -count=1 && \
  echo "TASK3 GREEN"
    </automated>
  </verify>
  <acceptance_criteria>
- `internal/core/db/database.go` AutoMigrate 列表含 `&models.PortWriteAudit{}`(出现 1 次,不是 0 或 >1)
- `internal/core/db/database.go` postgres 分支含 `migrations.Migrate202PortWriteAudit(d.DB)` 调用(非阻断错误处理,与 Migrate175/176 同风格)
- `internal/core/db/database.go` **不**在 SQLite 分支调 Migrate202PortWriteAudit
- `go build ./...` exit 0(全包编译)
- `go vet ./internal/core/db/...` exit 0
- Phase 34 / Phase 51 / Wave 1 / Wave 2 测试全绿:`go test ./internal/utils/operlog/... ./internal/services/portwrite/... ./internal/api/v1/network/... ./internal/core/db/migrations/... -count=1` 全 exit 0
  </acceptance_criteria>
  <done>database.go 修改完成且两处插入正确;Wave 2 全部测试绿 + 全包 build/vet 绿;Phase 52 全部 19 个 requirement ID 落地完成。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 启动期 GORM AutoMigrate | model tag → DB schema(列类型 / 索引命名)的信任传递 |
| 启动期 migrations.Migrate202 | seed 数据 + 角色授权写入 sys_menu / sys_role_menu(影响 RBAC 决策) |
| sys_role_menu 表 | 角色权限直接来源 — 错误授权 → 越权;漏授权 → 合法用户失去功能 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-06 | Elevation of Privilege | GrantNewMenuToRolesHavingParent SQL | HIGH | mitigate | helper 用 `JOIN sys_menu m ON rm.menu_id = m.id WHERE m.menu_name = ?` 限定只授权父已关联角色(D-08);ON CONFLICT DO NOTHING 幂等(防重复 seed 累积);newMenuID / parentMenuName 均为 migration 内部受控值(非 HTTP 输入),无 SQL 注入面;helper 测试覆盖 OnlyAffectsParentRoles 用例(PG path) |
| T-52-07 | Tampering | migration_202 菜单 seed SQL | MEDIUM | mitigate | 用 GORM `db.Model(&models.Menu{}).Where(...).Count()` + `db.Create(&models.Menu{...})` 参数化(非字符串拼 SQL);count-then-insert 防重复 seed;父菜单 lookup 用 `db.Where("menu_name = ?", "端口状态").First(...)` 参数化 |
| T-52-08 | Tampering | sys_port_write_audit 索引命名 | LOW | mitigate | model tag 显式命名 `idx_port_write_audit_device_port_created` + `idx_port_write_audit_created`(避免 GORM 自动命名与 PG `_key` 后缀冲突,memory `xingran-gorm-sql-constraint-naming-conflict`);migration_202 加 `CREATE INDEX IF NOT EXISTS` 同名兜底;database.go:423-475 `auditConstraintNaming` 启动期审计约束命名一致性 |
| T-52-09 | Denial of Service | AutoMigrate 启动期阻塞 | LOW | accept | PortWriteAudit 是新表,无 MV 引用,无 SQLSTATE 0A000 风险(memory `gorm-automigrate-blocked-by-matview`);Migrate202 错误处理非阻断(menu seed 失败留待下次启动);启动期总开销 < 1s(12 列表 + 2 索引 + 1 菜单 + N 角色授权) |
| T-52-10 | Information Disclosure | sys_port_write_audit.command_sent 列 | LOW | accept | 这是 D-15 / AUDIT-03 锁定的未脱敏真相源(operlog 11 关键词脱敏后的补救);表本身受 RBAC 保护(Phase 53+ 详情查看 UI 会接 permission);本 phase 不暴露任何读取端点 |
| T-52-SC | — | (无 npm/pip/cargo 包安装) | — | accept | 仅消费 Go 标准库 + 已有依赖 |

## Security Gate Decision: PASS

所有 HIGH 威胁(T-52-06 helper 精准授权)在 Task 1 + Task 2 中有 mitigation(JOIN 限定 + ON CONFLICT 幂等 + 测试覆盖)。Phase 52 整体(Wave 1 + Wave 2)无 HIGH 威胁处于无 mitigation 状态。
</threat_model>

<verification>
Wave 2 全部任务完成后,在 plan 提交前依次跑(VALIDATION.md §4.3 Wave 2 脚本):

```bash
cd "D:/code/ClaudeCode/xingran-go-backend"

# 1. 全包编译(零回归)
go build ./...

# 2. go vet(verify-work 前置)
go vet ./...

# 3. Wave 2 新增 migration / helper 测试
go test ./internal/core/db/migrations/... -count=1 -v

# 4. Phase 34 / Phase 51 / Wave 1 全部回归锁保持绿色
go test ./internal/utils/operlog/... -count=1 -v
go test ./internal/services/portwrite/... -count=1 -v
go test ./internal/api/v1/network/... -count=1 -v

# 5. 全量测试(运行时间 ~15-25s)
go test ./... -count=1

# 6. database.go 注册守卫
grep -v '^\s*//' internal/core/db/database.go | grep -q '&models.PortWriteAudit{}'
grep -v '^\s*//' internal/core/db/database.go | grep -q 'migrations.Migrate202PortWriteAudit(d.DB)'

# 7. Path A 守卫(防止误加 CREATE TABLE)
! grep -q 'CREATE TABLE.*sys_port_write_audit' internal/core/db/migrations/migration_202_port_write_audit.go

# 8. D-07 父菜单名守卫(必须是 '端口状态' 不是 '端口管理')
grep -v '^\s*//' internal/core/db/migrations/migration_202_port_write_audit.go | grep -q '"端口状态"'
! grep -q 'GrantNewMenuToRolesHavingParent(db, "端口管理"' internal/core/db/migrations/migration_202_port_write_audit.go

# 9. Go sys_menu schema 守卫
! grep -q 'IsFrame\|IsCache' internal/core/db/migrations/migration_202_port_write_audit.go
```

全部 exit 0 + 无 grep 命中 = Wave 2 GREEN,Phase 52 全部 19 个 requirement ID 落地完成。
</verification>

<success_criteria>
- ROADMAP Phase 52 Success Criteria #5, #6 满足(#1-#4/#7 已在 Wave 1 满足;#8 由本 plan verification 全绿满足):
- #5: sys_port_write_audit 表由 AutoMigrate 创建 + (device_id,port_id,created_at) 复合索引 + (created_at) 单列索引(Task 3 AutoMigrate 注册 + Task 2 migration_202 CREATE INDEX IF NOT EXISTS 兜底)
- #6: sys_menu seed "端口配置" F 型按钮(parent='端口状态' D-07)+ GrantNewMenuToRolesHavingParent(db,"端口状态",newMenuID) 精准授权(Task 2)
- #8: go build / go test ./... 全绿,Phase 34 / 51 回归锁 intact
- 19 个 requirement ID 全部 traceable 到本 phase(AUDIT-03 / INFRA-01 / PERM-03 在本 plan;其余 16 在 52-01)
</success_criteria>

<output>
完成后创建 `.planning/phases/52-w3-router-handler-operlog-permission-migration/52-02-SUMMARY.md`,记录:
- 5 文件(2 创建 helper/migration + 1 修改 database.go + 2 测试)就位
- Migrate202PortWriteAudit + GrantNewMenuToRolesHavingParent 函数契约
- Path A(GORM AutoMigrate 建表)+ 路径 §1.2 显式调用(MigrateNNN 不自动跑)两大决策落地证据
- D-07 父菜单名 "端口状态" 修正(非 ROADMAP/REQUIREMENTS 写的 "端口管理")的源码证据
- Phase 52 全部 19 个 requirement ID traceability 矩阵(对应 plan + 任务)
- Next: Phase 53 — W4 前端 BulkWriteDrawer + API wrappers(HTTP 契约稳定,可基于 Wave 1 6 个端点开发)
</output>
