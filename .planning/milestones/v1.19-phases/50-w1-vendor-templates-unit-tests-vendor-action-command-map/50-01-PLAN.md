---
phase: 50-w1-vendor-templates-unit-tests-vendor-action-command-map
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/services/portcollection/vendor_port_template.go
  - internal/services/portcollection/vendor_port_template_test.go
autonomous: true
requirements:
  - SSH-05
  - SSH-01
user_setup: []

must_haves:
  truths:
    - "Caller can obtain the exact CLI command sequence for any of 15 (vendor, action) pairs via a single public function"
    - "Empty InterfaceName fails with ErrEmptyInterfaceName before any vendor lookup"
    - "Empty or >80-char Description on description action fails with ErrDescriptionEmpty / ErrDescriptionTooLong"
    - "Unsupported vendor (anything outside huawei/h3c/ruijie) fails with ErrUnsupportedVendor"
    - "Unknown action fails with ErrUnknownAction"
    - "Huawei and H3C produce identical command sequences (VRP heritage per D-08)"
    - "Ruijie dot1x enable/disable use Cisco-style two-command shape with explicit `interface ...` prefix"
    - "All 12+ table-driven tests pass and `go build ./...` exits 0"
  artifacts:
    - path: "internal/services/portcollection/vendor_port_template.go"
      provides: "Vendor->action->command map + RenderCommand public API"
      min_lines: 80
      must_contain:
        - "type PortAction string"
        - "ActionShutdown"
        - "ActionUndoShutdown"
        - "ActionDescription"
        - "ActionDot1xEnable"
        - "ActionDot1xDisable"
        - "type PortTemplateParams struct"
        - "ErrUnsupportedVendor"
        - "ErrUnknownAction"
        - "ErrEmptyInterfaceName"
        - "ErrDescriptionEmpty"
        - "ErrDescriptionTooLong"
        - "func RenderCommand("
    - path: "internal/services/portcollection/vendor_port_template_test.go"
      provides: "Table-driven coverage of 15 (vendor, action) combos + 5 negative cases"
      min_lines: 100
      must_contain:
        - "func TestRenderCommand_VendorActionMatrix"
        - "func TestRenderCommand_EmptyInterfaceName"
        - "func TestRenderCommand_UnsupportedVendor"
        - "func TestRenderCommand_UnknownAction"
        - "func TestRenderCommand_DescriptionEmpty"
        - "func TestRenderCommand_DescriptionTooLong"
        - "huawei_shutdown"
        - "h3c_shutdown"
        - "ruijie_shutdown"
        - "GigabitEthernet0/0/1"
  key_links:
    - from: "internal/services/portcollection/vendor_port_template.go"
      to: "internal/models/network_device.go"
      via: "vendor parameter typed as models.DeviceVendor using constants VendorHuawei/VendorH3C/VendorRuijie"
      pattern: "models\\.Vendor(Huawei|H3C|Ruijie)"
    - from: "internal/services/portcollection/vendor_port_template_test.go"
      to: "internal/services/portcollection/vendor_port_template.go"
      via: "same-package white-box test invoking exported RenderCommand"
      pattern: "RenderCommand\\(tt\\.vendor, tt\\.action, tt\\.params\\)"
    - from: "internal/services/portcollection/vendor_port_template.go"
      to: "operlog.OperType constants (Phase 52 consumer)"
      via: "PortAction string values carry into audit action field via PortAction.String()"
      pattern: "func \\(a PortAction\\) String\\(\\) string"

---

## Phase Goal

**As a** Phase 51 PortWriteService author, **I want to** call a single locked function that returns the exact `[]string` command sequence for any of the 15 supported `(vendor, action)` pairs, **so that** SSH write templates are vendor-syntax-correct by construction and Phase 52 handlers can wire them without re-deciding syntax.

---

<objective>
Lock the vendor to action to command contract before any HTTP/DB wiring (per D-02). Implement 3 vendors x 5 actions = 15 hardcoded templates (Huawei VRP, H3C Comware, Ruijie RGOS x shutdown / undo shutdown / description / dot1x enable / dot1x disable) in `internal/services/portcollection/vendor_port_template.go`, expose a single `RenderCommand` public function with `PortAction` / `PortTemplateParams` named types and sentinel errors, and verify with table-driven unit tests in `vendor_port_template_test.go`.

Purpose: Pure-Go map + tests (zero external dependencies). Stops vendor syntax drift early (Pitfall #3 in STATE.md) and provides the stable contract Phase 51's `PortWriteService` consumes via `SendConfig`/`SendConfigs`.
Output: One production file (about 80-120 lines) + one test file (about 100-150 lines); `go build ./...` and `go test ./internal/services/portcollection/...` both exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-w1-vendor-templates-unit-tests-vendor-action-command-map/50-CONTEXT.md

# Canonical files referenced in CONTEXT.md (executor MUST verify current state via these reads)
- internal/models/network_device.go — DeviceVendor constants (lines 14-21)
- internal/models/device_port_status.go — Description size:500, InterfaceName normalization (line 70)
- internal/device/scrapli_wrapper.go — SendConfig/SendConfigs/PlatformName/GetCommandForVendor reference signatures
- internal/services/portcollection/template_cache.go — same-package sibling (TextFSM cache) — proves coexistence pattern
- internal/services/operations/reference_resolver_test.go — table-driven test style reference
- internal/services/operations/building_service_test.go — multiple _test.go coexistence example
- .planning/codebase/TESTING.md — testify/assert conventions
- .planning/REQUIREMENTS.md — SSH-01, SSH-05 definitions
</context>

<interfaces>
<!-- Executor needs these concrete types/signatures from the codebase. -->

From internal/models/network_device.go:
```go
type DeviceVendor string

const (
    VendorHuawei DeviceVendor = "huawei"
    VendorH3C    DeviceVendor = "h3c"
    VendorRuijie DeviceVendor = "ruijie"
    VendorMaipu  DeviceVendor = "maipu"
)
```

From internal/services/portcollection/template_cache.go (proves same-package coexistence pattern):
```go
package portcollection

import "github.com/xingran-next/xingran-go-backend/internal/templates"

type TemplateCache struct { ... }
func NewTemplateCache() *TemplateCache { ... }
```

Downstream contract for Phase 51 (NOT yet implemented — Phase 50 must match these expected signatures):
```go
// Both files in package portcollection
func RenderCommand(vendor models.DeviceVendor, action PortAction, params PortTemplateParams) ([]string, error)
type PortAction string
const (
    ActionShutdown PortAction = "shutdown"
    ActionUndoShutdown PortAction = "undo_shutdown"
    ActionDescription PortAction = "description"
    ActionDot1xEnable PortAction = "dot1x_enable"
    ActionDot1xDisable PortAction = "dot1x_disable"
)
type PortTemplateParams struct {
    InterfaceName string
    Description   string
}
var (
    ErrUnsupportedVendor  error
    ErrUnknownAction      error
    ErrEmptyInterfaceName error
    ErrDescriptionEmpty   error
    ErrDescriptionTooLong error
)
func (a PortAction) String() string
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create vendor_port_template.go — types, map, RenderCommand, renderers</name>
  <files>internal/services/portcollection/vendor_port_template.go</files>
  <read_first>
    - D:\code\ClaudeCode\xingran-go-backend\internal\models\network_device.go (verify DeviceVendor + 4 vendor constants exist at lines 14-21; this file is the public-type source of truth for the `vendor` parameter)
    - D:\code\ClaudeCode\xingran-go-backend\internal\device\scrapli_wrapper.go (read lines 567, 594, 67, 682 — confirm SendConfig/SendConfigs/PlatformName/GetCommandForVendor signatures so RenderCommand's `[]string` return type aligns with SendConfigs's expected slice parameter)
    - D:\code\ClaudeCode\xingran-go-backend\internal\services\portcollection\template_cache.go (proves same-package coexistence pattern: vendor_port_template.go will sit alongside this TextFSM cache file with no symbol collisions — both files import different subpackages only)
    - D:\code\ClaudeCode\xingran-go-backend\internal\models\device_port_status.go (read line 70 area for InterfaceName normalization hook; confirms that InterfaceName arrives already-normalized via short-name form like "GE0/0/1" for Huawei/H3C and full form "GigabitEthernet0/0/1" for Ruijie per D-07)
    - D:\code\ClaudeCode\xingran-go-backend\.planning\phases\50-w1-vendor-templates-unit-tests-vendor-action-command-map\50-CONTEXT.md (re-read the specifics block for exact command strings and sentinel error messages — CONTEXT.md is the source of truth for D-01..D-09)
    - D:\code\ClaudeCode\xingran-go-backend\.planning\REQUIREMENTS.md (re-read SSH-05 definition: maintain hardcoded vendor to action to command_sequence mapping; SSH-01: SendConfig call signature contract — informs the `[]string` return design choice)
  </read_first>
  <acceptance_criteria>
    - File `internal/services/portcollection/vendor_port_template.go` exists with `package portcollection` declaration
    - `go build ./internal/services/portcollection/...` exits 0 (no compilation errors)
    - File contains the literal strings: `type PortAction string`, `ActionShutdown`, `ActionUndoShutdown`, `ActionDescription`, `ActionDot1xEnable`, `ActionDot1xDisable`, `type PortTemplateParams struct`, `ErrUnsupportedVendor`, `ErrUnknownAction`, `ErrEmptyInterfaceName`, `ErrDescriptionEmpty`, `ErrDescriptionTooLong`, `func RenderCommand(`, `func (a PortAction) String() string`
    - Source contains `models.DeviceVendor` in the `vendorPortTemplate` map type AND `models.VendorHuawei`, `models.VendorH3C`, `models.VendorRuijie` as map keys
    - Source does NOT contain `models.VendorMaipu` as a key (D-01 scope excludes Maipu)
    - Function `RenderCommand` accepts 3 parameters in order `(vendor models.DeviceVendor, action PortAction, params PortTemplateParams)` and returns `([]string, error)`
    - File contains four renderer functions: `renderH3CDescription`, `renderRuijieDescription`, `renderRuijieDot1xEnable`, `renderRuijieDot1xDisable`
    - Sentinel error messages begin with `portcollection: ` prefix per CONTEXT specifics
    - Grep on the new file for `import` shows `errors`, `fmt`, and `github.com/xingran-next/xingran-go-backend/internal/models` only — no `internal/templates`, no unused imports
  </acceptance_criteria>
  <action>
    Create the new file `internal/services/portcollection/vendor_port_template.go` with package `portcollection`. Implement per D-01..D-09:

    1) Type definitions (D-03, D-04):
       - Declare `type PortAction string` with five PortAction-typed consts: `ActionShutdown = "shutdown"`, `ActionUndoShutdown = "undo_shutdown"`, `ActionDescription = "description"`, `ActionDot1xEnable = "dot1x_enable"`, `ActionDot1xDisable = "dot1x_disable"`.
       - Implement `func (a PortAction) String() string { return string(a) }` (Claude's discretion per CONTEXT — yes, expose).
       - Declare `type PortTemplateParams struct { InterfaceName string; Description string }` with doc comments noting InterfaceName is mandatory for all actions and Description is used only by ActionDescription.

    2) Sentinel errors (D-06, D-09):
       - Single `var ( ... )` block declaring all five: `ErrUnsupportedVendor`, `ErrUnknownAction`, `ErrEmptyInterfaceName`, `ErrDescriptionEmpty`, `ErrDescriptionTooLong`. Use `errors.New(...)` with messages prefixed `portcollection: ` matching CONTEXT specifics.

    3) Internal map (D-01, D-08):
       - Declare package-level `var vendorPortTemplate = map[models.DeviceVendor]map[PortAction]func(PortTemplateParams) ([]string, error){...}`.
       - Populate 3 keys: `models.VendorHuawei`, `models.VendorH3C`, `models.VendorRuijie`. Do NOT add `models.VendorMaipu` (D-01 scope).
       - Huawei and H3C inner maps have identical content per D-08. Concrete action to command mapping:
         - ActionShutdown -> `[]string{"shutdown"}` for all 3 vendors
         - ActionUndoShutdown -> `[]string{"undo shutdown"}` for Huawei/H3C; `[]string{"no shutdown"}` for Ruijie
         - ActionDescription -> use `renderH3CDescription` for Huawei and H3C (two-command shape: `interface <name>` + `description <text>`); use `renderRuijieDescription` for Ruijie (same shape, separate function for testability)
         - ActionDot1xEnable -> `[]string{"dot1x enable"}` for Huawei/H3C; `renderRuijieDot1xEnable` for Ruijie (returns two-command Cisco-style sequence)
         - ActionDot1xDisable -> `[]string{"undo dot1x enable"}` for Huawei/H3C; `renderRuijieDot1xDisable` for Ruijie

    4) Renderers (D-06, D-07) — 4 unexported helper functions:
       - `renderH3CDescription(p PortTemplateParams) ([]string, error)`: empty Description -> return `ErrDescriptionEmpty`; `len(p.Description) > 80` -> return `fmt.Errorf("%w: %d > 80", ErrDescriptionTooLong, len(p.Description))`; else return `[]string{fmt.Sprintf("interface %s", p.InterfaceName), fmt.Sprintf("description %s", p.Description)}`.
       - `renderRuijieDescription(p PortTemplateParams) ([]string, error)`: identical body to renderH3CDescription but a distinct function (per Claude's discretion — separate functions improve debuggability).
       - `renderRuijieDot1xEnable(p)`: `[]string{fmt.Sprintf("interface %s", p.InterfaceName), "dot1x port-control auto"}`.
       - `renderRuijieDot1xDisable(p)`: `[]string{fmt.Sprintf("interface %s", p.InterfaceName), "no dot1x port-control"}`.

    5) Public RenderCommand (D-02):
       - Signature: `func RenderCommand(vendor models.DeviceVendor, action PortAction, params PortTemplateParams) ([]string, error)`.
       - Body order:
         a. If `params.InterfaceName == ""`, return `nil, ErrEmptyInterfaceName`.
         b. Lookup `vendorPortTemplate[vendor]`; missing key -> `fmt.Errorf("%w: %s", ErrUnsupportedVendor, vendor)`.
         c. Lookup `vendorMap[action]`; missing key -> `fmt.Errorf("%w: %s (vendor: %s)", ErrUnknownAction, action, vendor)`.
         d. Else invoke the renderer closure with `params` and return its `([]string, error)` result.

    6) Imports: `errors` (for errors.New), `fmt` (Sprintf + Errorf wrapping), `github.com/xingran-next/xingran-go-backend/internal/models`. Do NOT import `strings` (no transformation in Phase 50 — D-07 normalization deferred).

    AVOID hardcoded switch-on-vendor inside RenderCommand — all dispatch flows through the map. AVOID putting sentinel errors in a separate errors.go. AVOID importing internal/templates here (TextFSM is template_cache.go's concern; name collision risk in vendor_port_template.go for symbols like `TemplateCache` are eliminated by keeping this file's focus on `[]string` returns).
  </action>
  <verify>
    <automated>cd D:/code/ClaudeCode/xingran-go-backend && go build ./internal/services/portcollection/... && echo BUILD_OK</automated>
  </verify>
  <done>File vendor_port_template.go exists at internal/services/portcollection/, compiles cleanly, and exports the RenderCommand function with the locked (vendor, action, params) signature. All five PortAction constants, PortTemplateParams struct, five sentinel errors, and four renderer helpers are present and visible to the same-package test file.</done>
</task>

<task type="auto">
  <name>Task 2: Create vendor_port_template_test.go — table-driven matrix + negative cases</name>
  <files>internal/services/portcollection/vendor_port_template_test.go</files>
  <read_first>
    - D:\code\ClaudeCode\xingran-go-backend\.planning\phases\50-w1-vendor-templates-unit-tests-vendor-action-command-map\50-CONTEXT.md (re-read specifics test-skeleton block — exact expected command strings for every (vendor, action) pair and the 5 negative-case names are listed there)
    - D:\code\ClaudeCode\xingran-go-backend\internal\services\operations\reference_resolver_test.go (reference style: testify assert, table-driven t.Run naming, no testify/require unless error is unrecoverable)
    - D:\code\ClaudeCode\xingran-go-backend\.planning\codebase\TESTING.md (verify testify is approved for unit tests in this package; no DB integration tests needed)
    - D:\code\ClaudeCode\xingran-go-backend\internal\services\portcollection\vendor_port_template.go (the file under test — confirm exact sentinel error names and RenderCommand signature before writing assertions)
    - D:\code\ClaudeCode\xingran-go-backend\internal\models\network_device.go (verify vendor constant names: VendorHuawei / VendorH3C / VendorRuijie — test cases must reference these exactly, not string literals)
  </read_first>
  <acceptance_criteria>
    - File internal/services/portcollection/vendor_port_template_test.go exists with package portcollection declaration
    - File declares imports errors (for errors.Is), testing, github.com/xingran-next/xingran-go-backend/internal/models, and github.com/stretchr/testify/assert (no DB-required tags — pure unit tests)
    - Function TestRenderCommand_VendorActionMatrix exists containing 15+ subtest cases via t.Run covering every (vendor, action) pair: 5 Huawei + 5 H3C + 5 Ruijie = 15
    - Subtest name huawei_shutdown asserts result equals []string{"shutdown"} for models.VendorHuawei + ActionShutdown + PortTemplateParams{InterfaceName: "GE0/0/1"}
    - Subtest name ruijie_undo_shutdown asserts result equals []string{"no shutdown"} for models.VendorRuijie + ActionUndoShutdown
    - Subtest name ruijie_dot1x_enable asserts result equals []string{"interface GigabitEthernet0/0/1", "dot1x port-control auto"} for models.VendorRuijie + ActionDot1xEnable + PortTemplateParams{InterfaceName: "GigabitEthernet0/0/1"}
    - Subtest name huawei_description asserts result equals []string{"interface GE0/0/1", "description uplink"}
    - 5 separate negative-case test functions exist: TestRenderCommand_EmptyInterfaceName, TestRenderCommand_UnsupportedVendor, TestRenderCommand_UnknownAction, TestRenderCommand_DescriptionEmpty, TestRenderCommand_DescriptionTooLong — each uses assert.ErrorIs(t, err, <expected sentinel>)
    - TestRenderCommand_DescriptionTooLong uses strings.Repeat("x", 81) to construct a >80-char string and passes it in Description
    - TestRenderCommand_UnsupportedVendor passes models.DeviceVendor("cisco") as the vendor value (Cisco explicitly out-of-scope per REQUIREMENTS.md)
    - No DB tag (//go:build integration etc) is added — Phase 50 is pure-Go with no external dependencies
  </acceptance_criteria>
  <action>
    Create the new file internal/services/portcollection/vendor_port_template_test.go with package portcollection. Follow the test skeleton in CONTEXT.md specifics block exactly.

    1) Imports: errors, strings, testing, github.com/xingran-next/xingran-go-backend/internal/models, github.com/stretchr/testify/assert. No DB-related imports. No testify/require (use assert.NoError + assert.Equal even on error-checking subtests — keep it consistent).

    2) Test function TestRenderCommand_VendorActionMatrix(t *testing.T):
       - Define a single tests slice of struct { name string; vendor models.DeviceVendor; action PortAction; params PortTemplateParams; expected []string } containing 15 entries — 3 vendors x 5 actions. The 5 Huawei + 5 H3C cases use InterfaceName: "GE0/0/1" with Description: "uplink" for the description action. The 5 Ruijie cases use InterfaceName: "GigabitEthernet0/0/1" with Description: "uplink" for the description action. Per the CONTEXT specifics test skeleton, name each subtest with the snake_case convention: huawei_shutdown, huawei_undo_shutdown, huawei_description, huawei_dot1x_enable, huawei_dot1x_disable, same pattern for h3c_* and ruijie_*.
       - Iterate with for _, tt := range tests loop calling t.Run(tt.name, func(t *testing.T) { got, err := RenderCommand(tt.vendor, tt.action, tt.params); assert.NoError(t, err); assert.Equal(t, tt.expected, got) }).

    3) Negative-case test functions (5 standalone tests, one per sentinel path):
       - TestRenderCommand_EmptyInterfaceName: invoke RenderCommand(models.VendorHuawei, ActionShutdown, PortTemplateParams{InterfaceName: ""}) and assert.ErrorIs(t, err, ErrEmptyInterfaceName).
       - TestRenderCommand_UnsupportedVendor: invoke RenderCommand(models.DeviceVendor("cisco"), ActionShutdown, PortTemplateParams{InterfaceName: "GE0/0/1"}) and assert.ErrorIs(t, err, ErrUnsupportedVendor). Use models.DeviceVendor("cisco") cast literal to keep vendor constants unused for unsupported cases.
       - TestRenderCommand_UnknownAction: invoke RenderCommand(models.VendorHuawei, PortAction("bogus_action"), PortTemplateParams{InterfaceName: "GE0/0/1"}) and assert.ErrorIs(t, err, ErrUnknownAction).
       - TestRenderCommand_DescriptionEmpty: invoke RenderCommand(models.VendorHuawei, ActionDescription, PortTemplateParams{InterfaceName: "GE0/0/1", Description: ""}) and assert.ErrorIs(t, err, ErrDescriptionEmpty).
       - TestRenderCommand_DescriptionTooLong: define longDesc := strings.Repeat("x", 81), invoke RenderCommand(models.VendorHuawei, ActionDescription, PortTemplateParams{InterfaceName: "GE0/0/1", Description: longDesc}) and assert.ErrorIs(t, err, ErrDescriptionTooLong).

    4) Imports note: include "strings" only because TestRenderCommand_DescriptionTooLong uses strings.Repeat. All other imports should be minimal — no helper packages beyond testify/assert.

    5) AVOID duplicating test setup into a helper struct / shared fixture. Each table-driven subtest must be self-contained per CONTEXT Claude discretion (inline table preferred over testdata file).
  </action>
  <verify>
    <automated>cd D:/code/ClaudeCode/xingran-go-backend && go test ./internal/services/portcollection/... -run TestRenderCommand -v -count=1 && echo TEST_OK</automated>
  </verify>
  <done>File vendor_port_template_test.go exists, compiles, all TestRenderCommand_* functions pass with exit 0. Total 17+ test cases (15 matrix + 5 negative) collected and run. assert.NoError / assert.Equal / assert.ErrorIs used consistently; no testify/require.</done>
</task>

<task type="auto">
  <name>Task 3: Build + run all portcollection tests — Phase 50 acceptance gate</name>
  <files></files>
  <read_first>
    - D:\code\ClaudeCode\xingran-go-backend\internal\services\portcollection\vendor_port_template.go (just-created file — confirm final state before build)
    - D:\code\ClaudeCode\xingran-go-backend\internal\services\portcollection\vendor_port_template_test.go (just-created test file — confirm final state before run)
    - D:\code\ClaudeCode\xingran-go-backend\.planning\phases\50-w1-vendor-templates-unit-tests-vendor-action-command-map\50-CONTEXT.md (cross-check Phase 50 success criteria #5: go test ./internal/services/portcollection/... exits 0)
  </read_first>
  <acceptance_criteria>
    - go build ./... exits 0 with no compilation errors anywhere in the project
    - go test ./internal/services/portcollection/... -v -count=1 exits 0 and prints PASS for each TestRenderCommand_* subtest
    - go vet ./internal/services/portcollection/... exits 0
    - Test count reported by go test ./internal/services/portcollection/... shows >= 17 tests passing (15 matrix cases + 5 negative-case functions; matrix subtests counted individually)
    - No regression introduced in any other package — the existing _test.go files in internal/services/portcollection/ (collection_test.go, query_test.go, utils_test.go) still compile
  </acceptance_criteria>
  <action>
    Run the full Phase 50 acceptance gate from the project root:

    a) go build ./... — must exit 0 to confirm the new file does not break transitive compilation across the whole project.
    b) go vet ./internal/services/portcollection/... — must exit 0 to confirm no shadow variables / unused imports / suspicious constructs.
    c) go test ./internal/services/portcollection/... -v -count=1 — must exit 0 with all TestRenderCommand_* cases passing. Use -count=1 to defeat any local test-result caching. Capture the --- PASS lines for the 17+ subtests as proof.
    d) If any step fails: do NOT mark this task done. Read the failure output, identify which file/function broke, fix it, and rerun the three commands before declaring complete.
  </action>
  <verify>
    <automated>cd D:/code/ClaudeCode/xingran-go-backend && go build ./... && go vet ./internal/services/portcollection/... && go test ./internal/services/portcollection/... -count=1 -v 2>&1 | tail -30</automated>
  </verify>
  <done>go build ./... exits 0, go vet ./internal/services/portcollection/... exits 0, go test ./internal/services/portcollection/... -count=1 exits 0 with all TestRenderCommand_* cases passing. Phase 50 success criteria #1..#5 are mechanically provable from the test output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Same-package test to production code | vendor_port_template_test.go and vendor_port_template.go share package portcollection — tests invoke exported RenderCommand and read package-level sentinel errors. White-box test, fully trusted. |
| Future Phase 51 service to Phase 50 templates | internal/services/portwrite/ (Phase 51) will import internal/services/portcollection and call RenderCommand. Caller-supplied PortTemplateParams flows in; the function returns []string. Untrusted caller (a future handler) supplies InterfaceName + Description; trusted library returns escaped command strings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Tampering | Description content embedded into CLI command | accept | Phase 50 transmits Description as-is into description <text> per CONTEXT D-06. Scrapli runs in text mode against the device CLI; injection is bounded by the device own parser. Documented as deferred. Future-phase hardening (shell metachar escaping) requires explicit D-XX decision — not silently added. |
| T-50-02 | Information Disclosure | Sentinel errors expose vendor + action in Errorf wrapping | accept | ErrUnsupportedVendor: cisco and ErrUnknownAction: bogus_action (vendor: huawei) contain only the operator-supplied values; no DB rows / PII / secrets are exposed. Accept per CONTEXT D-09. |
| T-50-03 | Denial of Service | Description >80 chars consumed indefinitely | mitigate | Phase 50 returns ErrDescriptionTooLong when len(p.Description) > 80. Hard cap matches the UI constraint in device_port_status.Description size:500 (80 char UI subset). |
| T-50-04 | Elevation of Privilege | Untrusted action string reaching dispatcher | mitigate | RenderCommand looks up only against the bounded internal map; an unsupported PortAction returns ErrUnknownAction (no panic, no implicit fallback to a default vendor). |
| T-50-05 | Repudiation | Test absence hides syntax drift | mitigate | 15 matrix subtests assert exact command sequences; vendor syntax changes break at least one subtest deterministically. Per STATE.md Pitfall #3 mitigation. |
| T-50-SC | Tampering | npm/pip/cargo installs | n/a | Phase 50 adds zero new dependencies (pure stdlib + existing models + testify/assert already vendored). No package-manager activity. |

## Notes

- No SSH, no auth, no network I/O in this phase. Threat surface is limited to in-process type / value handling.
- Phase 51 (PortWriteService) and Phase 52 (handler) will inherit richer threats (credential exposure, real-device rejection); those are covered in their respective plans.
</threat_model>

<verification>
1. go build ./... exits 0 (full project compiles).
2. go vet ./internal/services/portcollection/... exits 0 (no lint regressions).
3. go test ./internal/services/portcollection/... -run TestRenderCommand -v -count=1 exits 0 with 17+ subtests passing.
4. internal/services/portcollection/vendor_port_template.go contains func RenderCommand(, type PortAction string, five Action* constants, type PortTemplateParams struct, and five Err* sentinel vars.
5. internal/services/portcollection/vendor_port_template_test.go contains the six named test functions: TestRenderCommand_VendorActionMatrix, TestRenderCommand_EmptyInterfaceName, TestRenderCommand_UnsupportedVendor, TestRenderCommand_UnknownAction, TestRenderCommand_DescriptionEmpty, TestRenderCommand_DescriptionTooLong.
6. 15 (vendor, action) matrix cases asserted by name: huawei_shutdown, huawei_undo_shutdown, huawei_description, huawei_dot1x_enable, huawei_dot1x_disable, h3c_shutdown, h3c_undo_shutdown, h3c_description, h3c_dot1x_enable, h3c_dot1x_disable, ruijie_shutdown, ruijie_undo_shutdown, ruijie_description, ruijie_dot1x_enable, ruijie_dot1x_disable.
7. Grep for models.VendorMaipu in vendor_port_template.go returns no map-key occurrences (D-01 scope).
</verification>

<success_criteria>
1. 3 vendors x 5 actions = 15 templates implemented in internal/services/portcollection/vendor_port_template.go (Huawei VRP, H3C Comware, Ruijie RGOS x shutdown / undo shutdown / description / dot1x enable / dot1x disable). (ROADMAP SC #1)
2. 12+ unit tests in vendor_port_template_test.go covering each (vendor, action) combination plus 5 negative cases (17+ total). (ROADMAP SC #2)
3. PortAction and PortTemplateParams types defined as exported named types; RenderCommand(vendor, action, params) ([]string, error) public signature locked. (ROADMAP SC #3)
4. Huawei and H3C share command shape (VRP heritage); Ruijie uses Cisco-style with dot1x port-control auto / no dot1x port-control. (ROADMAP SC #4 — asserted by ruijie_dot1x_enable and ruijie_dot1x_disable subtests)
5. go test ./internal/services/portcollection/... exits 0 with all vendor template tests passing. (ROADMAP SC #5)
</success_criteria>

<output>
Create `.planning/phases/50-w1-vendor-templates-unit-tests-vendor-action-command-map/50-01-SUMMARY.md` when done.
</output>
