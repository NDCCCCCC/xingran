---
phase: 53-w4-frontend-drawer-progress-dialog-api-wrappers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - xingran-react-frontend/src/types/network.ts
  - xingran-react-frontend/src/lib/api/networkApi.ts
  - xingran-react-frontend/src/components/network/port-write/constants.ts
autonomous: true
requirements: [UI-06, BATCH-05]
tags: [frontend, react, typescript, api-wrapper, network, port-write, foundation]

must_haves:
  truths:
    - "types/network.ts exports PortResult / BatchResult / BatchWriteRequest interfaces mirroring backend portwrite Go structs"
    - "networkApi.ts exports 6 wrapped POST functions (writeShutdown / writeUndoShutdown / writeDescription / writeDot1xEnable / writeDot1xDisable / batchWritePorts) hitting the 6 Phase 52 kebab endpoints"
    - "constants.ts exports PRESET_REASONS / ACTION_TITLE / REASON_MIN / REASON_MAX / DESCRIPTION_MAX consumed by 53-02 UI"
    - "npm run type-check exits 0"
    - "npm run build exits 0 with no vendor bundle regression > 50 kB gzip (vendor-react baseline 776 kB gzip from Phase 48)"
  artifacts:
    - path: "xingran-react-frontend/src/types/network.ts"
      provides: "PortResult + BatchResult + BatchWriteRequest TypeScript interfaces"
      contains: "export interface PortResult"
    - path: "xingran-react-frontend/src/lib/api/networkApi.ts"
      provides: "6 port-write wrapper functions"
      exports: ["writeShutdown", "writeUndoShutdown", "writeDescription", "writeDot1xEnable", "writeDot1xDisable", "batchWritePorts"]
    - path: "xingran-react-frontend/src/components/network/port-write/constants.ts"
      provides: "PRESET_REASONS / ACTION_TITLE / REASON_MIN / REASON_MAX / DESCRIPTION_MAX shared constants"
      exports: ["PRESET_REASONS", "ACTION_TITLE", "REASON_MIN", "REASON_MAX", "DESCRIPTION_MAX"]
  key_links:
    - from: "xingran-react-frontend/src/lib/api/networkApi.ts"
      to: "/network/ports/write/{shutdown,undo-shutdown,description,dot1x-enable,dot1x-disable,batch}"
      via: "post<T>(url, body) from ../api"
      pattern: "post<PortResult>\\(\"/network/ports/write/"
    - from: "xingran-react-frontend/src/lib/api/networkApi.ts"
      to: "xingran-react-frontend/src/types/network.ts"
      via: "import type { PortResult, BatchResult, BatchWriteRequest }"
      pattern: "from \"@/types/network\""
---

<objective>
为 Phase 53 W4 前端落地**类型 + API wrapper + 共享常量**地基。这是 Wave 2 UI（PortWriteModal / BulkWriteDrawer / 端口列表页改造）的依赖前置：UI 必须先有 6 个 wrapper 函数和 3 个 TypeScript 类型可消费，否则会陷入"边写 UI 边补类型"的 scavenger hunt 反模式。

Purpose: 把 Phase 52 W3 已稳定的 6 个 kebab 端点 HTTP 契约翻译成前端可调用的 wrapper；把 Phase 51 service 层的 PortResult / BatchResult Go struct 镜像成 TS 接口；把 D-02 预置原因 / D-01 action 标题 / D-03 description 上限等 UI 共享常量提前抽出，让 53-02 只聚焦组件逻辑。
Output: 3 个文件（types/network.ts 追加、networkApi.ts 追加、constants.ts 新建），完全自洽可被 53-02 直接 import。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-w4-frontend-drawer-progress-dialog-api-wrappers/53-CONTEXT.md
@.planning/phases/53-w4-frontend-drawer-progress-dialog-api-wrappers/53-PATTERNS.md
@.planning/phases/52-w3-router-handler-operlog-permission-migration/52-01-SUMMARY.md

<interfaces>
<!-- Phase 52 已落地的后端契约（本 plan wrapper 直接镜像，不需再探索后端代码） -->

后端 6 端点（internal/api/v1/network/port_write_router.go:42-47）：
- POST /network/ports/write/shutdown         body={portId,reason}            resp=PortResult
- POST /network/ports/write/undo-shutdown    body={portId,reason}            resp=PortResult
- POST /network/ports/write/description      body={portId,description,reason?} resp=PortResult
- POST /network/ports/write/dot1x-enable     body={portId,reason}            resp=PortResult
- POST /network/ports/write/dot1x-disable    body={portId,reason}            resp=PortResult
- POST /network/ports/write/batch            body={deviceId,action,portIds[],description?} resp=BatchResult

后端 Go struct 原文（internal/services/portwrite/port_write_service.go:34-42 + batch_orchestrator.go:15-19）— TS 字段名按 JSON tag 镜像：
```go
type PortResult struct {
    PortID       string `json:"portId"`
    Action       Action `json:"action"`
    Status       string `json:"status"` // "succeeded" | "failed" | "skipped"
    NoOp         bool   `json:"noOp"`
    CurrentState string `json:"currentState,omitempty"`
    Error        string `json:"error,omitempty"`
    CommandSent  string `json:"commandSent,omitempty"`
}
type BatchWriteRequest struct {
    DeviceID    string   `json:"deviceId"`
    Action      Action   `json:"action"`
    PortIDs     []string `json:"portIds"`
    Description string   `json:"description,omitempty"`
}
type BatchResult struct {
    Succeeded []PortResult `json:"succeeded"`
    Failed    []PortResult `json:"failed"`
    Skipped   []PortResult `json:"skipped"`
}
```

前端 post() 底座（xingran-react-frontend/src/lib/api.ts:468-470）：
```typescript
export function post<T = unknown>(url: string, data?: unknown): Promise<BaseResponse<T>>
```
post() 已内置 SM2+SM4 加密 + token refresh + BaseResponse envelope 解包 + 非 0 code 自动 message.error Toast + Promise.reject。wrapper 只透传 reject，不重复弹 Toast（LANDMINE #5）。

现有 networkApi.ts wrapper 模板（queryMACHistory line 85-93，新 wrapper 镜像此风格）：
```typescript
export const queryMACHistory = async (params: MACHistoryQueryParams): Promise<MACHistoryPageResult> => {
  const result = await post<MACHistoryPageResult>("/network/history/list", params);
  return result.data!;
};
```

现有 types/network.ts DevicePortStatus（line 117-137）— 新类型追加到同文件，与 DevicePortStatus 同处（LANDMINE #1：必须放 network.ts，不是 index.ts）。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: types/network.ts 追加 PortResult / BatchResult / BatchWriteRequest 三类型</name>
  <files>xingran-react-frontend/src/types/network.ts</files>
  <read_first>
    - xingran-react-frontend/src/types/network.ts (现状文件，重点 line 110-137 看 DevicePortStatus 写法风格)
    - .planning/phases/53-w4-frontend-drawer-progress-dialog-api-wrappers/53-PATTERNS.md (line 446-509：已有完整 TS 类型代码 + 后端 Go struct 原文，可直接抄)
    - internal/services/portwrite/port_write_service.go (line 34-50，确认 PortResult / BatchWriteRequest 字段)
    - internal/services/portwrite/batch_orchestrator.go (line 15-19，确认 BatchResult 三切片)
  </read_first>
  <action>
    在 types/network.ts 文件**末尾**追加 3 个 exported interface：PortResult、BatchWriteRequest、BatchResult。每个 interface 前加 JSDoc 注释标注来源（"Phase 53 端口写操作结果（镜像后端 portwrite.PortResult）/ Source: internal/services/portwrite/port_write_service.go:34-42"）。

    PortResult 字段（按后端 JSON tag 镜像，**不要**改 camelCase）：portId: string; action: string （字面量联合类型 "shutdown" | "undo_shutdown" | "description" | "dot1x_enable" | "dot1x_disable"）; status: "succeeded" | "failed" | "skipped"; noOp: boolean; currentState?: string; error?: string; commandSent?: string。

    BatchWriteRequest 字段：deviceId: string; action: PortResult["action"] （复用同一联合类型）; portIds: string[]; description?: string。

    BatchResult 字段：succeeded: PortResult[]; failed: PortResult[]; skipped: PortResult[]。

    **不**改 types/index.ts（它只是 barrel：`export * from "./network"`，新类型通过 barrel 自动导出）。**不**在 networkApi.ts inline 类型（LANDMINE #1：类型必须与 DevicePortStatus 同处，方便 53-02 UI 与现有 DevicePortStatus import 同文件）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend &amp;&amp; npx tsc --noEmit &amp;&amp; echo "TYPE_CHECK_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export interface PortResult" xingran-react-frontend/src/types/network.ts` 返回 1
    - `grep -c "export interface BatchResult" xingran-react-frontend/src/types/network.ts` 返回 1
    - `grep -c "export interface BatchWriteRequest" xingran-react-frontend/src/types/network.ts` 返回 1
    - PortResult.status 字段类型为 `"succeeded" | "failed" | "skipped"` 字面量联合（不是 string）
    - `npm run type-check` (即 npx tsc --noEmit) 退出码 0
    - types/index.ts 文件未修改（barrel 自动透传）
  </acceptance_criteria>
  <done>3 个 interface 追加到 types/network.ts 末尾，字段名严格按后端 JSON tag camelCase，status 用字面量联合类型，type-check 0 错误。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: networkApi.ts 扩展 6 个 port-write wrapper 函数</name>
  <files>xingran-react-frontend/src/lib/api/networkApi.ts</files>
  <read_first>
    - xingran-react-frontend/src/lib/api/networkApi.ts (现状文件，重点 line 1-2 import、line 80-93 queryMACHistory 模板、line 240-246 default export)
    - xingran-react-frontend/src/lib/api.ts (line 468-470 post() 底座签名 + line 360-364 错误拦截器，理解为什么 wrapper 不翻译 sentinel)
    - .planning/phases/53-w4-frontend-drawer-progress-dialog-api-wrappers/53-PATTERNS.md (line 30-117：完整 wrapper 代码 + default export 更新已给出)
    - internal/api/v1/network/port_write_router.go (确认 6 个 kebab 路径字面量，避免笔误)
  </read_first>
  <action>
    在 networkApi.ts 末尾的 default export 对象**之前**追加 6 个 async wrapper 函数，并在 default export 对象中追加 6 个引用。

    6 个函数签名（D-08 锁定，PATTERNS.md line 56-92 已给完整代码）：
    - writeShutdown(portId: string, reason: string): Promise&lt;PortResult&gt;
    - writeUndoShutdown(portId: string, reason: string): Promise&lt;PortResult&gt;
    - writeDescription(portId: string, description: string, reason?: string): Promise&lt;PortResult&gt;
    - writeDot1xEnable(portId: string, reason: string): Promise&lt;PortResult&gt;
    - writeDot1xDisable(portId: string, reason: string): Promise&lt;PortResult&gt;
    - batchWritePorts(req: BatchWriteRequest): Promise&lt;BatchResult&gt;

    URL **必须**用 kebab 命名（与后端 port_write_router.go 注册一致）："/network/ports/write/shutdown" / "/network/ports/write/undo-shutdown" / "/network/ports/write/description" / "/network/ports/write/dot1x-enable" / "/network/ports/write/dot1x-disable" / "/network/ports/write/batch"。

    每个 wrapper 函数体镜像 queryMACHistory 模板：`const result = await post&lt;T&gt;(url, body); return result.data!;`。

    文件顶部新增 import：`import type { PortResult, BatchResult, BatchWriteRequest } from "@/types/network";`（注意 type-only import，避免运行时循环依赖）。

    default export 对象追加 6 行（writeShutdown, writeUndoShutdown, writeDescription, writeDot1xEnable, writeDot1xDisable, batchWritePorts）。

    **不**在 wrapper 内 try/catch（post() 拦截器已弹 Toast，wrapper 只透传 Promise.reject — LANDMINE #5 防 double-Toast）。**不**翻译 sentinel error 为中文 message（后端 handler 已翻译，D-08）。**不**用 opsApi factory（D-08：opsApi 是 operations 模块专用）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend &amp;&amp; npx tsc --noEmit &amp;&amp; echo "TYPE_CHECK_OK"</automated>
  </verify>
  <acceptance_criteria>
    - networkApi.ts 导出 6 个函数：`grep -cE "export const (writeShutdown|writeUndoShutdown|writeDescription|writeDot1xEnable|writeDot1xDisable|batchWritePorts)" xingran-react-frontend/src/lib/api/networkApi.ts` 返回 6
    - 6 个 URL 全是 kebab：`grep -cE "/network/ports/write/(shutdown|undo-shutdown|description|dot1x-enable|dot1x-disable|batch)" xingran-react-frontend/src/lib/api/networkApi.ts` 返回 6
    - `grep -c 'from "@/types/network"' xingran-react-frontend/src/lib/api/networkApi.ts` 返回 1（type-only import）
    - wrapper 函数体内不含 try/catch / .catch（透传 reject，不拦截）
    - wrapper 函数体内不含 message.error / getAppMessage 调用（防 double-Toast）
    - default export 对象包含 6 个新引用
    - `npm run type-check` 退出码 0
  </acceptance_criteria>
  <done>6 个 wrapper 函数追加到 networkApi.ts 并加入 default export，URL 全 kebab 对齐后端，type-only import 三类型，无 try/catch 无 message.error 透传，type-check 0 错误。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 新建 port-write/constants.ts 共享常量文件</name>
  <files>xingran-react-frontend/src/components/network/port-write/constants.ts</files>
  <read_first>
    - xingran-react-frontend/src/pages/duty/management/constants.ts (line 15-20 MANUAL_REASON_OPTIONS {label,value} as const 结构 — analog)
    - .planning/phases/53-w4-frontend-drawer-progress-dialog-api-wrappers/53-PATTERNS.md (line 294-328：constants.ts 完整内容已给出)
    - .planning/phases/53-w4-frontend-drawer-progress-dialog-api-wrappers/53-CONTEXT.md (D-01/D-02/D-03 决策原文)
  </read_first>
  <action>
    新建 xingran-react-frontend/src/components/network/port-write/ 目录（若不存在），创建 constants.ts。

    导出 5 个 const（PATTERNS.md line 298-328 完整代码可直接抄）：
    - PRESET_REASONS：`as const` 数组，4 个预设项 + 1 个"其他..." sentinel。每项 `{label: string, value: string}`。预设 4 项 value 各 4 字符（"故障排查" / "安全合规" / "业务变更" / "临时测试"），满足 D-02 的 5 字符下限需保证 label 长度 ≥ 5（实际中文字符 4 个就够，但 D-02 字面要求 5-200；4 字符预设值 + 5 字符下限校验时会被卡 — **修正**：把预设项 value 改为 5+ 字符，如"故障排查处理" / "安全合规要求" / "业务变更需要" / "临时测试验证"，或保持 4 字符但 REASON_MIN 设为 4；PATTERNS.md 已写 REASON_MIN=5 与 4 字符预设矛盾，本 plan 决策：保持 REASON_MIN=5，预设 value 扩展为 5+ 字符）。"其他..." 项 value 用 sentinel "__custom__"。
    - ACTION_TITLE：Record 类型，键为 5 个 action 字面量联合，值为中文标题。shutdown→"关闭端口"、undo_shutdown→"取消关闭"、description→"修改描述"、dot1x_enable→"启用 802.1X"、dot1x_disable→"停用 802.1X"。
    - REASON_MIN=5、REASON_MAX=200（D-02 上下限）。
    - DESCRIPTION_MAX=80（D-03 description 保守上限）。

    文件顶部加 JSDoc 注释说明用途（PortWriteModal / BulkWriteDrawer 共享）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend &amp;&amp; npx tsc --noEmit &amp;&amp; echo "TYPE_CHECK_OK"</automated>
  </verify>
  <acceptance_criteria>
    - 文件存在：`test -f xingran-react-frontend/src/components/network/port-write/constants.ts &amp;&amp; echo EXISTS`
    - `grep -c "export const PRESET_REASONS" xingran-react-frontend/src/components/network/port-write/constants.ts` 返回 1
    - `grep -c "export const ACTION_TITLE" xingran-react-frontend/src/components/network/port-write/constants.ts` 返回 1
    - `grep -cE "export const (REASON_MIN|REASON_MAX|DESCRIPTION_MAX)" xingran-react-frontend/src/components/network/port-write/constants.ts` 返回 3
    - PRESET_REASONS 含 "__custom__" sentinel：`grep -c "__custom__" xingran-react-frontend/src/components/network/port-write/constants.ts` ≥ 1
    - ACTION_TITLE 覆盖 5 个 action：`grep -cE "(shutdown|undo_shutdown|description|dot1x_enable|dot1x_disable):" xingran-react-frontend/src/components/network/port-write/constants.ts` ≥ 5
    - REASON_MIN === 5 且 REASON_MAX === 200（D-02）
    - DESCRIPTION_MAX === 80（D-03）
    - PRESET_REASONS 每项 value 字符数 ≥ 5（满足 REASON_MIN 自洽，不留矛盾给 53-02 校验逻辑）
    - `npm run type-check` 退出码 0
  </acceptance_criteria>
  <done>constants.ts 新建并导出 5 个 const，PRESET_REASONS 每项 value 长度 ≥ 5 与 REASON_MIN=5 自洽，ACTION_TITLE 覆盖全 5 action，三数值常量值精确，type-check 0 错误。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend-types → backend-JSON | TypeScript 类型镜像后端 Go struct JSON tag；类型不一致会导致 wrapper 返回值字段访问 undefined |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Tampering | wrapper URL 字面量 | mitigate | 6 个 URL 严格按 Phase 52 port_write_router.go 注册的 kebab 路径，acceptance_criteria 用 grep 计数 6 验证；URL 笔误会 404 |
| T-53-02 | Information Disclosure | wrapper 函数 console.log | accept | wrapper 无 console.log（acceptance_criteria grep 验证）；PortResult.commandSent 含设备命令但仅在 UI 渲染，不入日志 |
| T-53-03 | Spoofing | wrapper 绕过 post() 拦截器 | accept | wrapper 直接调 post()，不绕过；post() 已含 token 注入 + SM2+SM4 加密 + 401 拦截 |
| T-53-04 | Repudiation | PortResult.status 字段值 | mitigate | status 用字面量联合类型 "succeeded" \| "failed" \| "skipped"，编译期锁定三态，防 typo 引入第 4 态导致 BulkWriteDrawer 结果分区漏判（LANDMINE #3） |
| T-53-SC | Tampering | npm 依赖 | accept | 本 plan 不引入新 npm 包（无 install 任务），常量/types/wrapper 均纯 TS；T-53-SC 不适用 |
</threat_model>

<verification>
本 plan 验证三件套：

1. **类型契约对齐**：types/network.ts 三 interface 字段名严格按后端 JSON tag（camelCase），status 用字面量联合防 typo。
2. **HTTP 契约对齐**：6 个 wrapper URL 全 kebab 命名对齐 port_write_router.go（grep 计数 6）。
3. **零回归**：type-check + build 双绿，build 后 vendor-react gzip 不超 Phase 48 baseline 776 kB + 50 kB。

**完整 build + bundle 验证（Wave 1 收尾）**：
```bash
cd xingran-react-frontend
npm run type-check        # 退出 0
npm run build             # 退出 0
# build 输出日志中检查 vendor-react chunk gzip ≤ 826 kB (776 + 50 容差)
```

constants.ts 三数值常量自洽性（PRESET_REASONS value 长度 ≥ REASON_MIN）由 Task 3 acceptance_criteria 守护，避免 53-02 校验逻辑踩 5 字符下限坑。
</verification>

<success_criteria>
- types/network.ts 追加 PortResult / BatchResult / BatchWriteRequest 三 interface，字段名按后端 JSON tag 镜像，status 用字面量联合类型
- networkApi.ts 追加 6 个 wrapper 函数（writeShutdown / writeUndoShutdown / writeDescription / writeDot1xEnable / writeDot1xDisable / batchWritePorts），URL 全 kebab 对齐 Phase 52 路由，wrapper 不 try/catch 不重复弹 Toast
- port-write/constants.ts 新建，PRESET_REASONS / ACTION_TITLE / REASON_MIN=5 / REASON_MAX=200 / DESCRIPTION_MAX=80 五常量齐备，预设项 value 长度与 REASON_MIN 自洽
- npm run type-check 退出 0
- npm run build 退出 0，vendor-react gzip 不超 826 kB（Phase 48 baseline 776 kB + 50 kB 容差）
- 所有改动是纯前端 TS，零新 npm 依赖
</success_criteria>

<output>
完成后创建 `.planning/phases/53-w4-frontend-drawer-progress-dialog-api-wrappers/53-01-SUMMARY.md`，记录：3 文件改动 + 6 wrapper URL + 3 类型字段映射 + 5 常量值 + type-check/build 结果 + 为 53-02 提供的 import 接口清单。
</output>
