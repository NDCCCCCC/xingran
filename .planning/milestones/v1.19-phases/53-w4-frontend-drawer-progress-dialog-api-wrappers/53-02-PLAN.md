---
phase: 53-w4-frontend-drawer-progress-dialog-api-wrappers
plan: 02
type: execute
wave: 2
depends_on: [53-01]
files_modified:
  - xingran-react-frontend/src/components/network/port-write/PortWriteModal.tsx
  - xingran-react-frontend/src/components/network/port-write/BulkWriteDrawer.tsx
  - xingran-react-frontend/src/pages/network/ports/index.tsx
  - xingran-react-frontend/src/pages/monitor/logs/index.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05]
tags: [frontend, react, antd, port-write, modal, drawer, permission-gating, monitor-logs]

must_haves:
  truths:
    - "无 network:port:write 权限的用户在端口列表页看不到操作列，也看不到批量配置按钮（前端 gating，后端 RequirePermissions 仍是真相源）"
    - "运维点击行内操作 → 弹出 PortWriteModal → 输入操作原因（5-200 字符） → 确认 → 调对应 wrapper → 成功 Toast 含'查看审计日志'链接"
    - "运维勾选多端口 → 点批量配置(N) → 弹出 BulkWriteDrawer → 选 action + reason → 提交 → indeterminate spinner → 结果面板（成功/跳过/失败三 Statistic 卡片 + 失败明细 Table）"
    - "批量进行中端口列表页'刷新'和'采集所有设备'按钮均 disabled，避免与 Enqueue 后台采集竞态"
    - "失败端口可一键重试，重试范围只含 result.failed，不含 succeeded/skipped"
    - "Toast '查看审计日志'链接 navigate('/monitor/logs?module=端口管理')，monitor/logs 页 mount 时读 URL query 预填 title 字段并触发查询"
    - "npm run type-check 退出 0；npm run build 退出 0 无 vendor 回归"
  artifacts:
    - path: "xingran-react-frontend/src/components/network/port-write/PortWriteModal.tsx"
      provides: "统一单端口操作 Modal（action prop 切 5 种操作 + reason Select+TextArea + description 特例）"
      contains: "ACTION_TITLE"
      min_lines: 120
    - path: "xingran-react-frontend/src/components/network/port-write/BulkWriteDrawer.tsx"
      provides: "批量操作 Drawer（select→executing→result 状态机 + indeterminate spinner + 三 Statistic + 失败重试）"
      contains: "batchWritePorts"
      min_lines: 180
    - path: "xingran-react-frontend/src/pages/network/ports/index.tsx"
      provides: "端口列表页改造（操作列 + 批量配置按钮 + canWrite gating + batchInProgress 禁刷新+采集）"
      contains: "network:port:write"
    - path: "xingran-react-frontend/src/pages/monitor/logs/index.tsx"
      provides: "operlog 页 URL query module 预填（mount 时读 ?module=xxx 预填 title 字段并触发查询）"
      contains: "useSearchParams"
  key_links:
    - from: "xingran-react-frontend/src/pages/network/ports/index.tsx"
      to: "xingran-react-frontend/src/components/network/port-write/PortWriteModal.tsx"
      via: "render &lt;PortWriteModal open action portRecord onClose onSuccess /&gt;"
      pattern: "&lt;PortWriteModal"
    - from: "xingran-react-frontend/src/pages/network/ports/index.tsx"
      to: "xingran-react-frontend/src/components/network/port-write/BulkWriteDrawer.tsx"
      via: "render &lt;BulkWriteDrawer open selectedPorts onClose onSuccess onExecutingChange /&gt;"
      pattern: "&lt;BulkWriteDrawer"
    - from: "xingran-react-frontend/src/components/network/port-write/PortWriteModal.tsx"
      to: "xingran-react-frontend/src/lib/api/networkApi.ts"
      via: "import { writeShutdown, writeUndoShutdown, writeDescription, writeDot1xEnable, writeDot1xDisable } from \"@/lib/api/networkApi\""
      pattern: "from \"@/lib/api/networkApi\""
    - from: "xingran-react-frontend/src/components/network/port-write/BulkWriteDrawer.tsx"
      to: "xingran-react-frontend/src/lib/api/networkApi.ts"
      via: "import { batchWritePorts } from \"@/lib/api/networkApi\""
      pattern: "batchWritePorts"
    - from: "xingran-react-frontend/src/components/network/port-write/PortWriteModal.tsx"
      to: "/monitor/logs?module=端口管理"
      via: "App.useApp message.open content 含 Link onClick navigate"
      pattern: "monitor/logs\\?module="
    - from: "xingran-react-frontend/src/pages/network/ports/index.tsx"
      to: "xingran-react-frontend/src/store/menuStore"
      via: "useMenuStore((s) =&gt; s.permissions) + hasPermission(\"network:port:write\")"
      pattern: "network:port:write"
---

<objective>
为 Phase 53 W4 前端落地**UI 消费层**：PortWriteModal（5 单端口操作统一 Modal）+ BulkWriteDrawer（批量 Drawer + indeterminate 进度 + 结果面板 + 失败重试）+ 端口列表页改造（操作列 + 批量配置按钮 + canWrite gating + batchInProgress 禁刷新+采集）+ monitor/logs URL query 预填补丁。

Purpose: 把 Phase 52 W3 的 6 端点 HTTP 契约（已被 53-01 wrapper 包装）变成运维可点击的操作入口。MVP 范围内：单端口精确操作（5 种 action + reason 必填校验）、批量同设备操作（select→executing→result 状态机 + 失败重试）、操作期间禁用刷新防 Enqueue 竞态、操作完成 Toast 反馈并跳审计日志。
Output: 2 新建组件 + 2 改造页面。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-w4-frontend-drawer-progress-dialog-api-wrappers/53-CONTEXT.md
@.planning/phases/53-w4-frontend-drawer-progress-dialog-api-wrappers/53-PATTERNS.md
@.planning/phases/53-w4-frontend-drawer-progress-dialog-api-wrappers/53-01-PLAN.md

<interfaces>
<!-- 53-01 Wave 1 提供的 import 契约（本 plan 直接消费，不需重读 networkApi.ts/types） -->

From xingran-react-frontend/src/lib/api/networkApi.ts (53-01 Task 2 提供):
```typescript
export const writeShutdown: (portId: string, reason: string) => Promise<PortResult>;
export const writeUndoShutdown: (portId: string, reason: string) => Promise<PortResult>;
export const writeDescription: (portId: string, description: string, reason?: string) => Promise<PortResult>;
export const writeDot1xEnable: (portId: string, reason: string) => Promise<PortResult>;
export const writeDot1xDisable: (portId: string, reason: string) => Promise<PortResult>;
export const batchWritePorts: (req: BatchWriteRequest) => Promise<BatchResult>;
```

From xingran-react-frontend/src/types/network.ts (53-01 Task 1 提供):
```typescript
export interface PortResult {
  portId: string;
  action: "shutdown" | "undo_shutdown" | "description" | "dot1x_enable" | "dot1x_disable";
  status: "succeeded" | "failed" | "skipped";
  noOp: boolean;
  currentState?: string;
  error?: string;
  commandSent?: string;
}
export interface BatchWriteRequest {
  deviceId: string;
  action: PortResult["action"];
  portIds: string[];
  description?: string;
}
export interface BatchResult {
  succeeded: PortResult[];
  failed: PortResult[];
  skipped: PortResult[];
}
export interface DevicePortStatus {  // 已存在 (types/network.ts:117-137)
  id: string; deviceId: string; deviceName?: string; interfaceName: string;
  adminStatus: string; operStatus: string; description?: string;
  dot1xEnabled: boolean; dot1xPortStatus: string; ...
}
```

From xingran-react-frontend/src/components/network/port-write/constants.ts (53-01 Task 3 提供):
```typescript
export const PRESET_REASONS: readonly { label: string; value: string }[];  // 末项 value === "__custom__"
export const ACTION_TITLE: Record<"shutdown" | "undo_shutdown" | "description" | "dot1x_enable" | "dot1x_disable", string>;
export const REASON_MIN: 5;
export const REASON_MAX: 200;
export const DESCRIPTION_MAX: 80;
```

<!-- post() 拦截器行为关键约束 (api.ts:360-364)：非 0 code 自动 message.error + Promise.reject。
     单端口 wrapper 调用方禁止再传 errorMessage 给 withErrorHandling（防 double-Toast，LANDMINE #5）。 -->

<!-- ports/index.tsx 现有可消费 hook 暴露 (line 81-82):
     const { data: portStatus, selectedRowKeys, ... } = useTableManager({...});
     line 44: const { message } = App.useApp();
     line 406: <Button onClick={handleRefresh}>刷新</Button>
     line 428-430: 采集所有设备 Button (loading={collecting})
     line 431-433: 批量删除(N) Button (disabled={selectedRowKeys.length === 0}) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: PortWriteModal.tsx 统一单端口操作 Modal</name>
  <files>xingran-react-frontend/src/components/network/port-write/PortWriteModal.tsx</files>
  <read_first>
    - xingran-react-frontend/src/pages/duty/management/modals/ManualScheduleModal.tsx (analog：Modal+Form+Select reason 模式，复制其 import 风格 + form.validateFields 模式)
    - xingran-react-frontend/src/components/network/port-write/constants.ts (53-01 已落地：PRESET_REASONS / ACTION_TITLE / REASON_MIN / REASON_MAX / DESCRIPTION_MAX)
    - xingran-react-frontend/src/types/network.ts (line 117-137 DevicePortStatus + 53-01 追加的 PortResult)
    - xingran-react-frontend/src/lib/api/networkApi.ts (53-01 已落地 5 个单端口 wrapper 签名)
    - xingran-react-frontend/src/utils/errorHandler.ts (withErrorHandling 签名 + onSuccess 回调模式)
    - .planning/phases/53-w4-frontend-drawer-progress-dialog-api-wrappers/53-PATTERNS.md (line 122-198：PortWriteModal 完整骨架代码已给出)
  </read_first>
  <action>
    创建 PortWriteModal.tsx，导出命名函数组件 `PortWriteModal`（非 default export，对齐项目惯例）。

    Props interface（D-01 锁定）：`{ open: boolean; action: "shutdown" | "undo_shutdown" | "description" | "dot1x_enable" | "dot1x_disable"; portRecord: DevicePortStatus | null; onClose: () =&gt; void; onSuccess: () =&gt; void; }`。

    组件内部：
    - `const [form] = Form.useForm();` + `const { message } = App.useApp();` + `const navigate = useNavigate();`（react-router-dom）
    - **action 变化时 reset 表单**：`useEffect(() =&gt; { if (open) form.resetFields(); }, [open, action, form]);`（防上次 reason/description 残留）
    - Modal 标题用 `ACTION_TITLE[action]` + portRecord.interfaceName（D-01 动态标题）
    - Modal props：`width={520}` + `destroyOnHidden` + `okText="确认执行"` + `cancelText="取消"` + `okButtonProps={{ loading: submitting }}`

    Form 内部 2 个条件字段：
    - **description action 特例字段（D-03）**：当 `action === "description"` 时渲染"新描述"`Form.Item name="description"`，rules `[{ required: true, message: "请输入新端口描述" }, { max: DESCRIPTION_MAX, message: "描述不超过 80 字符" }]`，控件 `<Input maxLength={DESCRIPTION_MAX} showCount placeholder="请输入新端口描述" />`。
    - **reason 字段（D-02）**：所有 action 都渲染，但 description action 时可空（D-03）。结构：外层 `Form.Item name="reasonSelect" label="操作原因"`（Select 预设项 PRESET_REASONS），内层用 `Form.Item shouldUpdate noStyle` 监听 reasonSelect 值，当 `reasonSelect === "__custom__"` 时展开 `Form.Item name="reasonText"` 的 `<Input.TextArea rows={2} maxLength={REASON_MAX} showCount placeholder="请输入操作原因（5-200 字符）" />`。
    - reason 字段校验逻辑（D-03 条件分支）：description action 时 rules 为 `[{ validator: validateReasonOptional }]`（可选，但若填了则校验长度 5-200）；其他 4 action 时 rules 为 `[{ required: true, validator: validateReasonRequired }]`（必填 + 长度 5-200）。validator 函数读 reasonSelect + reasonText 合并出最终 reason 值（预设项直接用 value；custom 项用 reasonText），长度校验用 REASON_MIN/REASON_MAX。

    handleOk 流程：
    1. `const values = await form.validateFields();`（失败抛 errorFields，return 不弹 Toast）
    2. 组装最终 reason：reasonSelect === "__custom__" ? values.reasonText : values.reasonSelect
    3. 按 action 分支调对应 wrapper：shutdown→writeShutdown(portRecord.id, reason)；undo_shutdown→writeUndoShutdown(...)；description→writeDescription(portRecord.id, values.description, reason)；dot1x_enable→writeDot1xEnable(...)；dot1x_disable→writeDot1xDisable(...)
    4. 成功后调 showAuditLinkToast(message, navigate)（D-10 Toast 含"查看审计日志"链接）
    5. `form.resetFields(); onSuccess(); onClose();`
    6. 异常处理：validateFields 抛的 errorFields 直接 return（antd 会标红）；wrapper reject 由 post() 拦截器已弹 Toast，**不**再传 errorMessage 给上层（LANDMINE #5 防 double-Toast）

    showAuditLinkToast helper（可放本文件顶部或 utils，建议本文件就近）：用 `message.open({ type: "success", duration: 5, content: <>&lt;span&gt;操作成功，&lt;/span&gt;&lt;Link to="/monitor/logs?module=端口管理" onClick={() =&gt; navigate("/monitor/logs?module=" + encodeURIComponent("端口管理"))}&gt;查看审计日志&lt;/Link&gt;&lt;/&gt; })`。

    **不**用 antd Modal.confirm（D-01：reason 字段 + description 输入需 Form，confirm 不够）。**不**建 5 个独立 Modal（D-01：避免 reason×5 + 确认逻辑×5 重复）。**不**在 wrapper reject 时 message.error（post() 拦截器已弹）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend &amp;&amp; npx tsc --noEmit &amp;&amp; echo "TYPE_CHECK_OK"</automated>
  </verify>
  <acceptance_criteria>
    - 文件存在：`test -f xingran-react-frontend/src/components/network/port-write/PortWriteModal.tsx`
    - `grep -c "export function PortWriteModal\|export const PortWriteModal" xingran-react-frontend/src/components/network/port-write/PortWriteModal.tsx` 返回 1
    - `grep -c "ACTION_TITLE\[action\]" xingran-react-frontend/src/components/network/port-write/PortWriteModal.tsx` ≥ 1（D-01 动态标题）
    - `grep -c 'writeShutdown\|writeUndoShutdown\|writeDescription\|writeDot1xEnable\|writeDot1xDisable' xingran-react-frontend/src/components/network/port-write/PortWriteModal.tsx` ≥ 5（5 wrapper 全调用）
    - `grep -c "REASON_MIN\|REASON_MAX\|DESCRIPTION_MAX" xingran-react-frontend/src/components/network/port-write/PortWriteModal.tsx` ≥ 2（校验用常量，不硬编码）
    - `grep -c "__custom__" xingran-react-frontend/src/components/network/port-write/PortWriteModal.tsx` ≥ 1（D-02 Select 切 TextArea sentinel）
    - `grep -c 'monitor/logs?module' xingran-react-frontend/src/components/network/port-write/PortWriteModal.tsx` ≥ 1（D-10 审计跳转）
    - `grep -c "useMenuStore\|authStore" xingran-react-frontend/src/components/network/port-write/PortWriteModal.tsx` 返回 0（PortWriteModal 入口已 gate，组件内不重复鉴权）
    - 不含 `Modal.confirm`（D-01 排除）
    - 不含 wrapper 调用上的 errorMessage option（LANDMINE #5 防 double-Toast）
    - `npm run type-check` 退出码 0
  </acceptance_criteria>
  <done>PortWriteModal.tsx 单组件覆盖 5 action，action prop 切换标题/字段/wrapper 调用，reason Select+TextArea 双层结构含 __custom__ sentinel，description action 特例字段必填+其他 action 可空，D-10 审计 Toast 链接落地，type-check 0 错误。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: BulkWriteDrawer.tsx 批量 Drawer + indeterminate spinner + 结果面板 + 失败重试</name>
  <files>xingran-react-frontend/src/components/network/port-write/BulkWriteDrawer.tsx</files>
  <read_first>
    - xingran-react-frontend/src/components/reconciliation/ReconciliationDrawer.tsx (analog：Drawer + 内部分区结构)
    - xingran-react-frontend/src/components/network/port-write/constants.ts (ACTION_TITLE 复用 + PRESET_REASONS 复用)
    - xingran-react-frontend/src/types/network.ts (53-01：BatchWriteRequest / BatchResult / PortResult / DevicePortStatus)
    - xingran-react-frontend/src/lib/api/networkApi.ts (53-01：batchWritePorts 签名)
    - xingran-react-frontend/src/pages/network/ports/index.tsx (line 322-362 Row+Col+Statistic 模式 + line 466-484 expandable Table 模式)
    - .planning/phases/53-w4-frontend-drawer-progress-dialog-api-wrappers/53-PATTERNS.md (line 202-289：BulkWriteDrawer 完整骨架代码已给出)
  </read_first>
  <action>
    创建 BulkWriteDrawer.tsx，导出命名函数组件 `BulkWriteDrawer`。

    Props interface（D-04/D-07）：`{ open: boolean; selectedPorts: DevicePortStatus[]; onClose: () =&gt; void; onSuccess: () =&gt; void; onExecutingChange?: (inProgress: boolean) =&gt; void; }`。selectedPorts 由父组件读 selectedRowKeys 过滤 portStatus 得到（D-04：Drawer 内**不**重选）。

    内部状态机 type DrawerPhase = "select" | "executing" | "result"；const [phase, setPhase] = useState&lt;DrawerPhase&gt;("select")；const [batchResult, setBatchResult] = useState&lt;BatchResult | null&gt;(null)。

    useEffect：open 变 false 时 reset phase="select" + batchResult=null（防下次打开残留）；phase 变化时 onExecutingChange?.(phase === "executing")（D-07 上抛 batchInProgress 给父组件禁刷新）。

    **select 视图**：
    - 显示 selectedPorts 只读汇总（Statistic：已选 N 端口、唯一设备数；若 &gt;1 设备数则 antd Alert 提示"批量必须同设备"并禁用提交按钮——前端预校验，后端 ErrMixedDevices 是真相源）
    - action Select（5 项 ACTION_TITLE 映射）+ 必填
    - description action 时多一个"新描述"Input（必填，maxLength=DESCRIPTION_MAX）
    - reason 字段（与 PortWriteModal 同结构 Select+TextArea）
    - 提交按钮"开始批量配置"，disabled 当 selectedPorts.length === 0 或设备数 &gt; 1 或 reason 校验未通过

    handleBatch 流程：
    1. validateFields → 组装 BatchWriteRequest：`{ deviceId: selectedPorts[0].deviceId, action, portIds: selectedPorts.map(p =&gt; p.id), description?: values.description }`
    2. setPhase("executing") + onExecutingChange?.(true)（这会触发父组件禁用刷新+采集，D-07/LANDMINE #4）
    3. `const result = await batchWritePorts(req);`（**不**用 .catch 分类，HTTP 200 + status:"failed" 是正常 resolve 路径 — LANDMINE #3）
    4. setBatchResult(result) + setPhase("result")
    5. 成功（result.failed.length === 0）后调 showAuditLinkToast（D-10）+ onSuccess()（通知父组件刷新端口列表+统计）
    6. 异常：仅 Promise reject（网络错误等）才走 catch，message.error 由 post() 拦截器已弹，catch 内只 setPhase("select") 让用户重试

    **executing 视图**（D-05 indeterminate，**不**伪造 X/Y）：`<div style={{ textAlign: "center", padding: "60px 0" }}"><Spin size="large" tip="正在批量配置...（预计 ~1s/端口）" />&lt;/div&gt;`。**禁用** Drawer 关闭（onClose 在 executing 阶段 no-op 或 Modal maskClosable=false）。

    **result 视图**：
    - 三 Statistic 卡片（Row+Col span=8）：✓ 成功 batchResult.succeeded.length（绿 #3f8600）/ ⚠ 跳过 batchResult.skipped.length（灰 #8c8c8c，Tag "无需操作"）/ ✗ 失败 batchResult.failed.length（红 #cf1322）
    - 失败明细 antd Table：dataSource=batchResult.failed，rowKey="portId"，expandable.expandedRowRender 显示 `<Typography.Text code>{port.commandSent}</Typography.Text>`，columns=[{title:"接口名", render: (_, port) =&gt; selectedPorts.find(p =&gt; p.id === port.portId)?.interfaceName ?? port.portId, width:150}, {title:"错误原因", dataIndex:"error", ellipsis:true}]。**注意**：port.commandSent / port.error 直接读 PortResult 字段，不是 .catch 拿（LANDMINE #3）
    - 跳过明细 antd Collapse（默认收起）：items=[{key:"skipped", label:`跳过明细 (${batchResult.skipped.length})`, children:&lt;ul&gt;...&lt;/ul&gt;}]
    - 重试按钮（D-06）：`<Button type="primary" disabled={batchResult.failed.length === 0} onClick={handleRetryFailed}&gt;重试失败端口 ({batchResult.failed.length})&lt;/Button&gt;`

    handleRetryFailed：收集 `failedIds = batchResult.failed.map(p =&gt; p.portId)` → 组装新 BatchWriteRequest（deviceId/action/description 沿用上次，portIds=failedIds）→ setPhase("executing") → batchWritePorts → setBatchResult(newResult) + setPhase("result")（**替换**当前结果，不累加历史，D-06）。重试结果若仍 failed，按钮继续可用。

    showAuditLinkToast helper 与 PortWriteModal 共用一份（建议从 PortWriteModal 导出 helper，或就近复制——本 plan 决策：复制，避免新建 utils 文件超出 plan 范围；两处 helper 行为一致，复制成本低于抽 utils）。

    **不**用 Progress 组件伪造 X/Y（D-05：后端 batch 同步阻塞无实时进度，伪造 X/Y 不诚实）。**不**重试 skipped（D-06：skipped = 设备已是目标态，重试无意义）。**不**禁用 Drawer 关闭的 maskClosable 在 executing 阶段必须为 false（避免用户中途关 Drawer 留下孤儿 batch 状态）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend &amp;&amp; npx tsc --noEmit &amp;&amp; echo "TYPE_CHECK_OK"</automated>
  </verify>
  <acceptance_criteria>
    - 文件存在：`test -f xingran-react-frontend/src/components/network/port-write/BulkWriteDrawer.tsx`
    - `grep -c "export function BulkWriteDrawer\|export const BulkWriteDrawer" xingran-react-frontend/src/components/network/port-write/BulkWriteDrawer.tsx` 返回 1
    - 状态机三态：`grep -cE '"select"|"executing"|"result"' xingran-react-frontend/src/components/network/port-write/BulkWriteDrawer.tsx` ≥ 3
    - D-05 indeterminate：`grep -c "Spin" xingran-react-frontend/src/components/network/port-write/BulkWriteDrawer.tsx` ≥ 1 且 `grep -c "Progress" xingran-react-frontend/src/components/network/port-write/BulkWriteDrawer.tsx` 返回 0（不用 Progress 伪造 X/Y）
    - 结果分区读 body 不读 catch：`grep -c "batchResult.failed\|result.failed" xingran-react-frontend/src/components/network/port-write/BulkWriteDrawer.tsx` ≥ 2（LANDMINE #3）
    - D-07 上抛 batchInProgress：`grep -c "onExecutingChange" xingran-react-frontend/src/components/network/port-write/BulkWriteDrawer.tsx` ≥ 2（调用点 + props 声明）
    - D-06 重试只取 failed：`grep -c "batchResult.failed.map\|result.failed.map" xingran-react-frontend/src/components/network/port-write/BulkWriteDrawer.tsx` ≥ 1
    - 三 Statistic 卡片：`grep -c "Statistic" xingran-react-frontend/src/components/network/port-write/BulkWriteDrawer.tsx` ≥ 3
    - D-10 审计跳转：`grep -c 'monitor/logs?module' xingran-react-frontend/src/components/network/port-write/BulkWriteDrawer.tsx` ≥ 1
    - 跨设备预校验：`grep -ciE "mixed|唯一设备|设备数|same device" xingran-react-frontend/src/components/network/port-write/BulkWriteDrawer.tsx` ≥ 1
    - batchWritePorts 直接调用无 .catch 分类失败：`grep -c "batchWritePorts" xingran-react-frontend/src/components/network/port-write/BulkWriteDrawer.tsx` ≥ 2（import + 调用）
    - `npm run type-check` 退出码 0
  </acceptance_criteria>
  <done>BulkWriteDrawer.tsx 单组件覆盖 select→executing→result 状态机，indeterminate Spin 不伪造 X/Y（D-05），结果面板读 result.failed/succeeded/skipped 不靠 .catch（LANDMINE #3），onExecutingChange 上抛父组件（D-07），重试只取 failed（D-06），跨设备 Alert 预校验，type-check 0 错误。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 端口列表页 + monitor/logs 页改造（操作列 + 批量配置按钮 + canWrite gating + batchInProgress 禁刷新+采集 + URL query 预填）</name>
  <files>xingran-react-frontend/src/pages/network/ports/index.tsx, xingran-react-frontend/src/pages/monitor/logs/index.tsx</files>
  <read_first>
    - xingran-react-frontend/src/pages/network/ports/index.tsx (改造目标：line 44 message、line 81-82 portStatus+selectedRowKeys、line 406 刷新按钮、line 428-430 采集所有设备、line 431-433 批量删除按钮模板)
    - xingran-react-frontend/src/pages/network/devices/index.tsx (line 192-260 操作列+ActionButtons 模式 + line 260-261 useMenuStore 权限 gating 模式)
    - xingran-react-frontend/src/pages/network/mac/history/MACHistoryPage.tsx (line 103-105 canExport 权限 gating 第二先例)
    - xingran-react-frontend/src/pages/monitor/logs/index.tsx (改造目标：line 1-60 现状 + 完整文件确认无 useSearchParams — PATTERNS.md LANDMINE #2 已验证)
    - xingran-react-frontend/src/pages/asset/reconciliation/exceptions/index.tsx (line 88 useSearchParams + line 145-172 URL→form 预填模式 analog)
    - xingran-react-frontend/src/components/shared/ActionButtons.tsx (ActionButton 类型 + actions 数组写法)
    - xingran-react-frontend/src/components/network/port-write/PortWriteModal.tsx (Task 1 已落地：props 接口)
    - xingran-react-frontend/src/components/network/port-write/BulkWriteDrawer.tsx (Task 2 已落地：props 接口)
  </read_first>
  <action>
    **3a. ports/index.tsx 改造（5 处改动）**：

    改动 1 — 权限 gating（D-09，**不**用 useAuthStore，LANDMINE #5/ROADMAP #4 笔误纠正）：
    - 文件顶部 import 加 `import { useMenuStore } from "@/store/menuStore";`
    - 组件内顶部加：`const menuPermissions = useMenuStore((s) =&gt; s.permissions);` + `const hasPermission = (perm: string) =&gt; menuPermissions.includes(perm);` + `const canWrite = hasPermission("network:port:write");`（镜像 devices/index.tsx:260-261）

    改动 2 — PortWriteModal + BulkWriteDrawer state：
    - `const [writeModalOpen, setWriteModalOpen] = useState(false);`
    - `const [writeModalAction, setWriteModalAction] = useState&lt;"shutdown"|"undo_shutdown"|"description"|"dot1x_enable"|"dot1x_disable"&gt;("shutdown");`
    - `const [writeModalRecord, setWriteModalRecord] = useState&lt;DevicePortStatus | null&gt;(null);`
    - `const [bulkWriteDrawerOpen, setBulkWriteDrawerOpen] = useState(false);`
    - `const [batchInProgress, setBatchInProgress] = useState(false);`（D-07）
    - `const navigate = useNavigate();`（D-10 跳审计，如 PortWriteModal 内部已 navigate 则端口页不必再 import — 但 onSuccess 回调可能需要，按需）
    - openWriteModal helper：`(action, record) =&gt; { setWriteModalAction(action); setWriteModalRecord(record); setWriteModalOpen(true); }`

    改动 3 — columns 数组末尾追加"操作"列（D-01 + canWrite gating，镜像 devices/index.tsx:192-249 ActionButtons 模式）：
    - 用 ActionButtons 组件（@/components/shared/ActionButtons），actions 数组 5 项：shutdown/undo_shutdown/description/dot1x_enable/dot1x_disable，每项 onClick 调 openWriteModal(action, record)
    - 整列用 `...(canWrite ? [{ title: "操作", key: "portWriteAction", fixed: "right" as const, width: 100, render: (_, record) =&gt; &lt;ActionButtons actions={actions} /&gt; }] : [])` 条件渲染（无权限整列消失，D-09）

    改动 4 — 顶部按钮区改造（D-04 + D-07/LANDMINE #4）：
    - "刷新"按钮（line 406）加 `disabled={batchInProgress}`（D-07）
    - "采集所有设备"按钮（line 428-430）加 `disabled={batchInProgress}`（LANDMINE #4：同类竞态，不只是刷新）
    - 紧挨"批量删除(N)"按钮加"批量配置(N)"按钮：`<Button icon={&lt;SettingOutlined /&gt;} onClick={() =&gt; setBulkWriteDrawerOpen(true)} disabled={selectedRowKeys.length === 0 || !canWrite} type="primary"&gt;批量配置 ({selectedRowKeys.length})&lt;/Button&gt;`（D-04：与批量删除 UX 完全一致；额外加 !canWrite 防无权限用户点击）
    - SettingOutlined import from @ant-design/icons

    改动 5 — 挂载 Modal + Drawer（在 Table 之后或 Card 内末尾）：
    ```
    <PortWriteModal open={writeModalOpen} action={writeModalAction} portRecord={writeModalRecord}
      onClose={() =&gt; setWriteModalOpen(false)}
      onSuccess={() =&gt; { loadPortStatus(); loadStatistics(); }} /&gt;  // 复用现有 load 函数名
    <BulkWriteDrawer open={bulkWriteDrawerOpen}
      selectedPorts={portStatus.filter(p =&gt; selectedRowKeys.includes(p.id))}
      onClose={() =&gt; setBulkWriteDrawerOpen(false)}
      onSuccess={() =&gt; { loadPortStatus(); loadStatistics(); }}
      onExecutingChange={setBatchInProgress} /&gt;  // D-07
    ```
    （loadPortStatus / loadStatistics 是 ports/index.tsx 现有 reload 函数；executor 须按实际函数名替换，PATTERNS.md 未给完整 reload 函数名）

    **3b. monitor/logs/index.tsx 改造（D-10 依赖，LANDMINE #2）**：

    改动 6 — URL query module 预填（参考 reconciliation/exceptions/index.tsx:88,145-172）：
    - import 加 `import { useSearchParams } from "react-router-dom";`（现有 useLocation 可保留）
    - 组件内加 `const [searchParams] = useSearchParams();`
    - 加 mount-only useEffect：读 `searchParams.get("module")`，若有值且 activeTab === "oper"（默认 tab），则 `operLogManager.searchForm.setFieldsValue({ title: moduleFromUrl });` + `operLogManager.handleSearch();`（operLogManager 是 useTableManager 返回的 controller，executor 按 monitor/logs/index.tsx 实际变量名替换）
    - **eslint-disable-next-line react-hooks/exhaustive-deps** + `[]` mount-only（参考 exceptions/index.tsx:172 注释）
    - **注意**：URL query 参数名是 `module`（前端自定义），但 form 字段名是 `title`（对应后端 sys_oper_log.title 列，PATTERNS.md line 539 已说明）。后端 /monitor/oper-logs/list 已支持 title 过滤，无需后端改动。

    **不**把 toast audit-link 逻辑放端口页（已在 PortWriteModal/BulkWriteDrawer 内）。**不**改 useAuthStore（D-09：权限源是 useMenuStore）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; echo "BUILD_OK"</automated>
  </verify>
  <acceptance_criteria>
    - ports/index.tsx 权限源正确：`grep -c "useMenuStore" xingran-react-frontend/src/pages/network/ports/index.tsx` ≥ 1 且 `grep -c "useAuthStore" xingran-react-frontend/src/pages/network/ports/index.tsx` 返回 0（D-09/ROADMAP #4 笔误纠正）
    - ports/index.tsx 含 canWrite：`grep -c 'hasPermission("network:port:write")' xingran-react-frontend/src/pages/network/ports/index.tsx` ≥ 1
    - ports/index.tsx 挂载两组件：`grep -c "<PortWriteModal" xingran-react-frontend/src/pages/network/ports/index.tsx` ≥ 1 且 `grep -c "<BulkWriteDrawer" xingran-react-frontend/src/pages/network/ports/index.tsx` ≥ 1
    - ports/index.tsx 批量配置按钮：`grep -c "批量配置" xingran-react-frontend/src/pages/network/ports/index.tsx` ≥ 1
    - D-07/LANDMINE #4 双按钮禁用：`grep -c "disabled={batchInProgress}" xingran-react-frontend/src/pages/network/ports/index.tsx` ≥ 2（刷新 + 采集所有设备）
    - ports/index.tsx onExecutingChange 接线：`grep -c "onExecutingChange={setBatchInProgress}" xingran-react-frontend/src/pages/network/ports/index.tsx` 返回 1
    - ports/index.tsx 操作列 5 wrapper 触发：`grep -cE 'openWriteModal\("(shutdown|undo_shutdown|description|dot1x_enable|dot1x_disable)"' xingran-react-frontend/src/pages/network/ports/index.tsx` ≥ 5（或等价的 actions 数组写法）
    - monitor/logs/index.tsx URL query 预填：`grep -c "useSearchParams" xingran-react-frontend/src/pages/monitor/logs/index.tsx` ≥ 1（LANDMINE #2 落地）
    - monitor/logs/index.tsx 读 module 参数：`grep -c 'searchParams.get("module")' xingran-react-frontend/src/pages/monitor/logs/index.tsx` ≥ 1
    - monitor/logs/index.tsx 预填 title 字段：`grep -cE "setFieldsValue\(\{ title:" xingran-react-frontend/src/pages/monitor/logs/index.tsx` ≥ 1
    - `npm run type-check` 退出码 0
    - `npm run build` 退出码 0，vendor-react gzip ≤ 826 kB（Phase 48 baseline 776 + 50 容差；本 plan 仅加 2 组件 + 2 页面改造，无新重型依赖，预期增量 &lt; 10 kB gzip）
  </acceptance_criteria>
  <done>ports/index.tsx 加 useMenuStore 权限源（D-09）+ canWrite 操作列（D-01）+ 批量配置按钮（D-04）+ batchInProgress 双按钮禁用（D-07/LANDMINE #4）+ Modal/Drawer 挂载；monitor/logs/index.tsx 加 useSearchParams 读 module query 预填 title 字段（D-10/LANDMINE #2）；type-check + build 双绿，vendor bundle 无回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-click → wrapper | 用户点击操作按钮触发 wrapper；前端 canWrite gating 是 UX 优化，后端 RequirePermissions 是真相源 |
| wrapper-response → UI | PortResult/BatchResult 字段值渲染到 UI；status 决定结果分区显示 |
| URL-query → form | monitor/logs 页从 location.search 读 module 参数注入 form；参数值来自其他页面的 navigate() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-05 | Elevation of Privilege | 前端 canWrite gating 绕过 | accept | 前端 hasPermission 只控制入口可见性；后端 RequirePermissions(["network:port:write"]) 是真相源（Phase 52 已落地组级中间件）。前端绕过 → 后端 403 拒绝 |
| T-53-06 | XSS | reason / description / port.commandSent / port.error 渲染 | mitigate | 全部用 antd 组件文本渲染（Form.Item/Input/Statistic/Table/Typography.Text），**禁止** dangerouslySetInnerHTML；React 默认转义文本节点；acceptance_criteria grep 验证 0 处 dangerouslySetInnerHTML |
| T-53-07 | Tampering | BulkWriteRequest 字段构造 | mitigate | wrapper 只发送白名单字段（deviceId/action/portIds/description），**禁止** spread 整个 port record；portIds 从 selectedRowKeys 派生（受控），不从 URL/外部输入 |
| T-53-08 | Information Disclosure | port.commandSent 渲染到失败明细 Table | accept | commandSent 是审计字段（运维需看到失败命令）；仅在 UI 渲染，不入 console.log；audit 表 sys_port_write_audit 是真相源（已落地 Phase 52） |
| T-53-09 | Tampering | monitor/logs URL query module 参数 | mitigate | module 参数仅用于预填 form.title 字段触发查询；不直接拼 SQL（post() 走标准 request body）；后端 title 字段是 LIKE 过滤，无注入面；空值/异常值安全降级（不预填） |
| T-53-10 | Denial of Service | BulkWriteDrawer 重试无限循环 | mitigate | 重试按钮 disabled={batchResult.failed.length === 0}；每次重试范围只含上次 failed（自然收敛，D-06）；用户主动点触发，非自动循环 |
| T-53-SC | Tampering | npm 依赖 | accept | 本 plan 不引入新 npm 包（无 install 任务）；antd/react-router-dom 均已在依赖中；T-53-SC 不适用 |
</threat_model>

<verification>
本 plan 验证四件套：

1. **UI 完整性**：单端口 5 action Modal + 批量 Drawer + 端口页操作列/批量按钮/权限 gating/batchInProgress 禁用 + monitor/logs URL 预填 — acceptance_criteria 用 grep 计数验证每个改动点。
2. **5 大 LANDMINE 全部编码**：
   - LANDMINE #1（types in network.ts）：53-01 已落地，本 plan import 验证
   - LANDMINE #2（monitor/logs URL 预填）：Task 3 改动 6 + acceptance_criteria grep useSearchParams ≥ 1
   - LANDMINE #3（HTTP 200 + status:failed 不靠 catch）：Task 2 acceptance_criteria grep batchResult.failed ≥ 2
   - LANDMINE #4（batchInProgress 双按钮禁用）：Task 3 改动 4 + acceptance_criteria grep disabled={batchInProgress} ≥ 2
   - LANDMINE #5（post 拦截器已弹 Toast 不重复）：Task 1 acceptance_criteria "不含 errorMessage option"
3. **2 ROADMAP 笔误纠正**：
   - D-05 indeterminate（非 X/Y）：Task 2 acceptance_criteria "Spin ≥ 1 且 Progress = 0"
   - D-09 useMenuStore（非 useAuthStore）：Task 3 acceptance_criteria "useMenuStore ≥ 1 且 useAuthStore = 0"
4. **零回归**：type-check + build 双绿，vendor-react gzip ≤ 826 kB。

**完整 build + bundle 验证（Wave 2 收尾）**：
```bash
cd xingran-react-frontend
npm run type-check        # 退出 0
npm run build             # 退出 0
# build 输出日志检查 vendor-react gzip
```

**手动 UAT（Phase 54 范围，本 plan 仅落地代码不执行 UAT）**：
- 无 network:port:write 权限账号登录 → 端口页无操作列无批量配置按钮
- 有权限账号 → 行内 5 操作 → Modal reason 必填（非 description action）→ 提交 → Toast 含审计链接 → 点击跳 monitor/logs?module=端口管理 且 title 字段预填
- 勾选多端口 → 批量配置(N) → Drawer → 选 action+reason → 提交 → spinner → 结果面板 → 重试失败端口 → batchInProgress 期间刷新+采集按钮 disabled
</verification>

<success_criteria>
- PortWriteModal.tsx 单组件覆盖 5 action（D-01），reason Select+TextArea（D-02），description 特例字段（D-03），审计 Toast 链接（D-10）
- BulkWriteDrawer.tsx select→executing→result 状态机，indeterminate Spin 不伪造 X/Y（D-05/ROADMAP #2 纠正），结果读 body 不读 catch（LANDMINE #3），onExecutingChange 上抛（D-07），重试只取 failed（D-06）
- ports/index.tsx 加 useMenuStore canWrite gating（D-09/ROADMAP #4 纠正），操作列（D-01），批量配置按钮（D-04），batchInProgress 禁刷新+采集（D-07/LANDMINE #4），挂载两组件
- monitor/logs/index.tsx 加 useSearchParams 读 module query 预填 title（D-10/LANDMINE #2）
- npm run type-check 退出 0
- npm run build 退出 0，vendor-react gzip ≤ 826 kB（Phase 48 baseline 776 + 50 容差）
- 5 大 LANDMINE 全部在 acceptance_criteria 编码
- 2 ROADMAP 笔误纠正全部落地（D-05 indeterminate、D-09 useMenuStore）
- UI-01/UI-02/UI-03/UI-04/UI-05 五需求覆盖
</success_criteria>

<output>
完成后创建 `.planning/phases/53-w4-frontend-drawer-progress-dialog-api-wrappers/53-02-SUMMARY.md`，记录：2 新建组件 + 2 改造页面 + 5 LANDMINE 落地点 + 2 ROADMAP 纠正 + type-check/build 结果 + 留给 Phase 54 UAT 的手动验证项清单。
</output>
