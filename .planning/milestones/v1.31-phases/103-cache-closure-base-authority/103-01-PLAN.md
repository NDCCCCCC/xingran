---
phase: "103"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "internal/services/mac_history_query_service.go"
  - "internal/services/mac_history_heatmap_service.go"
  - "internal/api/v1/network/mac_history_router.go"
  - "internal/services/mac_history_query_service_79_05_test.go"
  - "internal/services/mac_history_tail_79_05_test.go"
  - "internal/api/v1/network/setup_routers_test.go"
autonomous: true
requirements:
  - "CONV-01"
user_setup: []

must_haves:
  truths:
    - "D-103-1: mac_history_query_service struct 字段 cache base.CacheProvider + dataCache 字段删除"
    - "D-103-2: NewMACHistoryQueryServiceWithCache 入参 dataCache *DataCacheService → cache base.CacheProvider"
    - "D-103-3: 三域 struct 字段直接持有 base.CacheProvider 接口值，由 router/core 层 system.NewCacheProvider 注入"
    - "D-103-4: mac_history_query_service.go:259-281 vendor 手写 cache-aside 纳入本相，迁 base.GetOrSetJSON"
    - "D-103-5: vendor Unknown Vendor 占位行为保留，TTL 24*time.Hour 字面量不变"
    - "D-103-6: mac_history query 三处(:309/:434/:837)使用 base.GetOrSetJSON 严格语义，cache 写失败返回 error"
    - "D-103-9: mac_history_heatmap 使用 getHeatmapWithCache wrapper 保留 fallback 直查语义"
    - "D-103-10: mac_history TTL 走 s.perfCacheTTL() 不变"
    - "D-103-13: 三域 TTL 全部行为等价，无业务语义变化；常量值与原值逐处一致"
    - "D-103-14: mac_history 既有 cache.Delete 路径同 commit 改 base.Invalidate"
    - "D-103-19: mac_history query 三处 GetOrSet + vendor + heatmap fallback 回归测试锁等价语义（含可注入失败的 fake CacheProvider）"
    - "D-103-20: constructor 签名变更兼容性，mac_history_router.go:19 接线更新为 NewCacheProvider；test 文件同步适配"
    - "D-103-21: TTL 等价 + cache key 字符串等价断言"
  artifacts:
    - path: "internal/services/mac_history_query_service.go"
      provides: "mac_history_query_service struct + 4 处 GetOrSet 位点 + vendor cache-aside"
      contains: "cache base.CacheProvider"
    - path: "internal/services/mac_history_heatmap_service.go"
      provides: "mac_history_heatmap_service struct + getHeatmapWithCache wrapper"
      contains: "cache base.CacheProvider"
    - path: "internal/api/v1/network/mac_history_router.go"
      provides: "NewMACHistoryHeatmapService 接线更新"
      contains: "system.NewCacheProvider"
    - path: "internal/services/mac_history_query_service_79_05_test.go"
      provides: "fakeMACHistoryCacheProvider 实现 base.CacheProvider + 9 方法；D-103-19 错误透传测试支撑"
      contains: "fakeMACHistoryCacheProvider"
  key_links:
    - from: "internal/services/mac_history_query_service.go"
      to: "internal/services/base/cache_functions.go"
      via: "base.GetOrSetJSON[T]"
      pattern: "base\\.GetOrSetJSON"
    - from: "internal/services/mac_history_query_service.go"
      to: "internal/services/cache_config_service.go"
      via: "s.perfCacheTTL()"
      pattern: "perfCacheTTL"
    - from: "internal/api/v1/network/mac_history_router.go"
      to: "internal/services/system/adapter.go"
      via: "system.NewCacheProvider(core.DataCacheService)"
      pattern: "system\\.NewCacheProvider"
---

<objective>
mac_history 域（query_service + heatmap_service）legacy interface{} 闭包 GetOrSet 与手写 cache-aside 全部迁移 base.GetOrSetJSON[T] + base.CacheProvider。STRUCT + CONSTRUCTOR + ROUTER 接线同 commit 完成。
</objective>

<context>
@internal/services/mac_history_query_service.go
@internal/services/mac_history_heatmap_service.go
@internal/api/v1/network/mac_history_router.go
@internal/services/base/cache_functions.go
@internal/services/base/cache_provider.go
@internal/services/system/adapter.go
@internal/services/system/cache_invariants_92_test.go
@internal/services/mac_history_query_service_79_05_test.go
@internal/services/mac_history_tail_79_05_test.go
@internal/api/v1/network/setup_routers_test.go
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From internal/services/base/cache_provider.go:
```go
type CacheProvider interface {
    GetOrSet(ctx context.Context, key string, dest interface{}, expiration time.Duration, query func() (interface{}, error)) error
    Delete(ctx context.Context, key string) error
    DeleteByPattern(ctx context.Context, pattern string) error
    MGet(ctx context.Context, keys ...string) (map[string]string, error)
    MDelete(ctx context.Context, keys ...string) error
    Exists(ctx context.Context, key string) (bool, error)
    SetTTL(ctx context.Context, key string, expiration time.Duration) error
    GetTTL(ctx context.Context, key string) (time.Duration, error)
    GetStats(ctx context.Context) (*CacheStats, error)
}

type CacheStats struct {
    Hits          int64
    Misses        int64
    Count         int64
    MemorySize    int64
    HitRate       float64
    KeyCount      int
    ExtendedStats map[string]interface{}
}
```

From internal/services/data_cache_service.go:
```go
// services.CacheStats (同 base.CacheStats 结构，字段名完全一致)
// type CacheStats struct { Hits, Misses, Count, MemorySize, HitRate, KeyCount, ExtendedStats }
// dataCacheService.GetStats() returns *services.CacheStats
```

From internal/services/system/adapter.go:71-85 (GetStats adapter pattern):
```go
func (a *cacheProviderAdapter) GetStats(ctx context.Context) (*base.CacheStats, error) {
    stats, err := a.dataCache.GetStats(ctx)  // returns *services.CacheStats
    if err != nil { return nil, err }
    return &base.CacheStats{
        Hits:          stats.Hits,
        Misses:        stats.Misses,
        Count:         stats.Count,
        MemorySize:    stats.MemorySize,
        HitRate:       stats.HitRate,
        KeyCount:      stats.KeyCount,
        ExtendedStats: stats.ExtendedStats,
    }, nil
}
```

From internal/api/v1/network/mac_history_router.go:19:
```go
// current: NewMACHistoryHeatmapService(core.GetDB(), nil, nil)
// must change to: NewMACHistoryHeatmapService(core.GetDB(), system.NewCacheProvider(core.DataCacheService), core.CacheConfigService)
```

From internal/core/core.go:355-358:
```go
c.CacheConfigService = services.NewCacheConfigService(c.GetDB())
c.DataCacheService = services.NewDataCacheService(c.Cache)
```

From internal/services/mac_history_query_service_79_05_test.go:140:
```go
NewMACHistoryQueryServiceWithCache(db, NewDataCacheService(cache.NewMemoryCache(10, time.Minute)), nil)
```

From internal/services/mac_history_tail_79_05_test.go:57, :694, :717, :728:
```go
// :57  real memory cache
NewMACHistoryHeatmapService(db, NewDataCacheService(mem), NewCacheConfigService(db))
// :694, :717  fakeDB, nil, nil — no cache path
NewMACHistoryHeatmapService(fakeDB, nil, nil)
// :728  real memory cache
NewMACHistoryHeatmapService(db, NewDataCacheService(mem), NewCacheConfigService(db))
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1a: mac_history_query_service — struct 字段 + 构造函数 + vendor cache-aside</name>
  <files>internal/services/mac_history_query_service.go</files>
  <read_first>internal/services/mac_history_query_service.go (struct :138-143, constructor :150-153, vendor :259-281, perfCacheTTL :155-166)</read_first>
  <action>
    参考 internal/services/base/cache_functions.go GetOrSetJSON[T] 签名，修改 mac_history_query_service.go：

    1. **struct 字段（D-103-1/D-103-3）**：将 `macHistoryQueryServiceImpl` 的 `dataCache *DataCacheService` 字段删除；`cache cache.Cache` 字段类型改为 `cache base.CacheProvider`（vendor lookup 继续用此字段）。

    2. **构造函数（D-103-2）**：`NewMACHistoryQueryServiceWithCache(db *gorm.DB, dataCache *DataCacheService, perfConfig *CacheConfigService)` 入参 `dataCache` 改为 `cache base.CacheProvider`，构造体初始化 `s.cache = cache`。

    3. **位点A vendor cache-aside（:259-281）（D-103-4/D-103-5/D-103-6）**：将手写 cache-aside（`s.cache.Get` + `s.cache.Set` 含 `"Unknown Vendor"` 占位）替换为：
       ```go
       vendorName, err := base.GetOrSetJSON[string](ctx, s.cache, cacheKey, 24*time.Hour, func() (string, error) {
           // OUI DB 查询逻辑（errors.Is(err, gorm.ErrRecordNotFound) → "Unknown Vendor", nil）
       })
       if err != nil { return "", err }
       ```
       TTL 保持 `24*time.Hour` 字面量（D-103-5）。严格语义：cache 写失败即返回 error（D-103-6）。

    **不要改动**：vendor lookup 的 nil 检查可删除（base.GetOrSetJSON 内部 nil 防护）；`perfCacheTTL()` 函数本身不改动。
  </action>
  <verify>
    <automated>go build ./internal/services/...</automated>
  </verify>
  <done>mac_history_query_service.go struct 字段改为 base.CacheProvider；构造函数入参改为 base.CacheProvider；vendor cache-aside 收敛 base.GetOrSetJSON</done>
</task>

<task type="auto">
  <name>Task 1b: mac_history_query_service — 三处 GetOrSet 位点严格语义替换</name>
  <files>internal/services/mac_history_query_service.go</files>
  <read_first>internal/services/mac_history_query_service.go (:304-318, :429-443, :832-846)</read_first>
  <action>
    替换 query_service 三处 GetOrSet 位点为 base.GetOrSetJSON[T] 严格语义（D-103-6/D-103-10）：

    - **位点B QueryPortHistory（:304-318）**：将 `s.dataCache.GetOrSet(ctx, key, &cached, s.perfCacheTTL(), func() (interface{}, error){...})` 替换为：
      ```go
      cached, err := base.GetOrSetJSON[MACHistoryQueryResult](ctx, s.cache, cacheKey, s.perfCacheTTL(), func() (MACHistoryQueryResult, error) {
          return s.queryPortHistoryFromDB(ctx, req)
      })
      if err != nil { return nil, err }
      ```

    - **位点C QueryDeviceHistory（:429-443）**：同形态，替换 `queryDeviceHistoryFromDB`。

    - **位点D QueryConnectionStats（:832-846）**：同形态，替换 `queryConnectionStatsFromDB`。

    TTL 来源 `s.perfCacheTTL()` 不变（D-103-10）。严格语义：cache 写失败即返回 error。
  </action>
  <verify>
    <automated>go build ./internal/services/... && go test ./internal/services/ -run "MACHistory|Vendor" -count=1</automated>
  </verify>
  <done>三处GetOrSet全部改为 base.GetOrSetJSON[T] 严格语义；TTL来源 s.perfCacheTTL() 不变</done>
</task>

<task type="auto">
  <name>Task 2: mac_history_heatmap_service — struct 字段 + 构造函数 + getHeatmapWithCache wrapper</name>
  <files>internal/services/mac_history_heatmap_service.go</files>
  <read_first>internal/services/mac_history_heatmap_service.go (struct :50-54, constructor :57-59, QueryHeatmap :107-130, perfCacheTTL :62-73, queryHeatmapFromMV 函数)</read_first>
  <action>
    1. **struct 字段（D-103-1/D-103-3）**：`macHistoryHeatmapServiceImpl` 的 `dataCache *DataCacheService` 字段改为 `cache base.CacheProvider`。

    2. **构造函数（D-103-2）**：`NewMACHistoryHeatmapService(db *gorm.DB, dataCache *DataCacheService, perfConfig *CacheConfigService)` 入参 `dataCache` 改为 `cache base.CacheProvider`。

    3. **新增 getHeatmapWithCache wrapper（D-103-9 fallback 直查语义）**：
       ```go
       func (s *macHistoryHeatmapServiceImpl) getHeatmapWithCache(ctx context.Context, cacheKey string, ttl time.Duration, req *HeatmapQuery, startTime, endTime time.Time) (*HeatmapResult, error) {
           result, err := base.GetOrSetJSON[*HeatmapResult](ctx, s.cache, cacheKey, ttl, func() (*HeatmapResult, error) {
               return s.queryHeatmapFromMV(ctx, startTime, endTime, req.TopN)
           })
           if err != nil {
               applogger.Warnf("[MAC热力图] 走直查: %v", err)
               return s.queryHeatmapFromMV(ctx, startTime, endTime, req.TopN)  // fallback 直查
           }
           return result, nil
       }
       ```
       TTL 来源 `s.perfCacheTTL()` 不变。

    4. **QueryHeatmap 方法改写**：将既有的 `s.dataCache.GetOrSet` 调用替换为调用 `s.getHeatmapWithCache(ctx, cacheKey, s.perfCacheTTL(), req, startTime, endTime)`。

    **不要改动**：`queryHeatmapFromMV` 函数本身；`perfCacheTTL()` 函数本身。
  </action>
  <verify>
    <automated>go build ./internal/services/... && go test ./internal/services/ -run "Heatmap" -count=1</automated>
  </verify>
  <done>mac_history_heatmap_service.go 编译通过；dataCache字段已删除；QueryHeatmap 调用 getHeatmapWithCache wrapper；fallback 直查语义保留</done>
</task>

<task type="auto">
  <name>Task 3: mac_history_router.go — NewMACHistoryHeatmapService 接线更新 + setup_routers_test.go 字面断言更新</name>
  <files>internal/api/v1/network/mac_history_router.go, internal/api/v1/network/setup_routers_test.go</files>
  <read_first>internal/api/v1/network/mac_history_router.go (:18-21); internal/api/v1/network/setup_routers_test.go (:240-252)</read_first>
  <action>
    **mac_history_router.go:19 接线更新（D-103-20）**：

    `internal/api/v1/network` 包 import `internal/services/system` 不会成环（api 层 import services 子包是单向依赖，无环）。

    将：
    ```go
    heatmapService := mac_history_query_service.NewMACHistoryHeatmapService(core.GetDB(), nil, nil)
    ```
    改为：
    ```go
    heatmapService := mac_history_query_service.NewMACHistoryHeatmapService(
        core.GetDB(),
        system.NewCacheProvider(core.DataCacheService),
        core.CacheConfigService,
    )
    ```

    `core.DataCacheService` 由 core.go:358 初始化；`core.CacheConfigService` 由 core.go:355 初始化。

    **setup_routers_test.go:250 字面断言同步更新（B2）**：

    将：
    ```go
    assert.Contains(t, src, "NewMACHistoryHeatmapService(core.GetDB(), nil, nil)", ...)
    ```
    改为：
    ```go
    assert.Contains(t, src, "NewMACHistoryHeatmapService(core.GetDB(), system.NewCacheProvider(core.DataCacheService), core.CacheConfigService)",
        "SetupMACHistoryRouter must instantiate heatmap service with CacheProvider from system.NewCacheProvider")
    ```
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/api/v1/network/ -count=1</automated>
  </verify>
  <done>mac_history_router.go 编译通过；NewMACHistoryHeatmapService 传入 system.NewCacheProvider(core.DataCacheService)；setup_routers_test.go 字面断言已更新</done>
</task>

<task type="auto">
  <name>Task 4: mac_history 测试文件适配 — fakeMACHistoryCacheProvider + 五处调用点更新</name>
  <files>internal/services/mac_history_query_service_79_05_test.go, internal/services/mac_history_tail_79_05_test.go</files>
  <read_first>internal/services/mac_history_query_service_79_05_test.go (:1-10 imports, :135-145); internal/services/mac_history_tail_79_05_test.go (:1-15 imports, :52-65, :688-735)</read_first>
  <action>
    **背景说明（B8 import cycle 解决方案）**：
    `package services` 测试文件不能调用 `system.NewCacheProvider`（成环：services→system→services）。
    因此在 `package services` 内新建 `fakeMACHistoryCacheProvider` 测试 fixture，
    内部持有 `*services.DataCacheService` 提供真实缓存行为，自身实现 `base.CacheProvider` 接口。

    **新增 fake 类型（写到 mac_history_query_service_79_05_test.go 顶部，或新建 internal/services/mac_history_test_fake_cache.go）**：

    ```go
    // fakeMACHistoryCacheProvider 实现 base.CacheProvider，
    // 内部持有 *services.DataCacheService 提供真实缓存行为。
    // 9 方法全部转发到内部 dataCache；GetStats 做 services.CacheStats → base.CacheStats 字段搬运。
    // 支持注入 Get/Set 错误（用于 D-103-19 错误透传测试）。
    type fakeMACHistoryCacheProvider struct {
        dataCache    *services.DataCacheService
        getErr       error  // GetOrSet 注错；nil=正常
        setErr       error  // GetOrSet 内 Set 注错；nil=正常
    }

    var _ base.CacheProvider = (*fakeMACHistoryCacheProvider)(nil) // 编译期断言

    func newFakeMACHistoryCacheProvider(mem cache.Cache) *fakeMACHistoryCacheProvider {
        return &fakeMACHistoryCacheProvider{
            dataCache: services.NewDataCacheService(mem),
        }
    }

    func (f *fakeMACHistoryCacheProvider) GetOrSet(ctx context.Context, key string, dest interface{}, expiration time.Duration, query func() (interface{}, error)) error {
        if f.getErr != nil {
            return f.getErr
        }
        // 使用内嵌 DataCacheService 的 GetOrSet 走真实缓存路径
        innerErr := f.dataCache.GetOrSet(ctx, key, dest, expiration, func() (interface{}, error) {
            result, qErr := query()
            if qErr != nil {
                return nil, qErr
            }
            // 可注入 setErr
            if f.setErr != nil {
                return nil, f.setErr
            }
            // 回填已由 dataCache.GetOrSet 内部处理
            return result, nil
        })
        return innerErr
    }

    func (f *fakeMACHistoryCacheProvider) Delete(ctx context.Context, key string) error {
        return f.dataCache.Delete(ctx, key)
    }
    func (f *fakeMACHistoryCacheProvider) DeleteByPattern(ctx context.Context, pattern string) error {
        return f.dataCache.DeleteByPattern(ctx, pattern)
    }
    func (f *fakeMACHistoryCacheProvider) MGet(ctx context.Context, keys ...string) (map[string]string, error) {
        return f.dataCache.MGet(ctx, keys...)
    }
    func (f *fakeMACHistoryCacheProvider) MDelete(ctx context.Context, keys ...string) error {
        return f.dataCache.MDelete(ctx, keys...)
    }
    func (f *fakeMACHistoryCacheProvider) Exists(ctx context.Context, key string) (bool, error) {
        return f.dataCache.Exists(ctx, key)
    }
    func (f *fakeMACHistoryCacheProvider) SetTTL(ctx context.Context, key string, expiration time.Duration) error {
        return f.dataCache.SetTTL(ctx, key, expiration)
    }
    func (f *fakeMACHistoryCacheProvider) GetTTL(ctx context.Context, key string) (time.Duration, error) {
        return f.dataCache.GetTTL(ctx, key)
    }

    func (f *fakeMACHistoryCacheProvider) GetStats(ctx context.Context) (*base.CacheStats, error) {
        stats, err := f.dataCache.GetStats(ctx) // *services.CacheStats
        if err != nil {
            return nil, err
        }
        return &base.CacheStats{
            Hits:          stats.Hits,
            Misses:        stats.Misses,
            Count:         stats.Count,
            MemorySize:    stats.MemorySize,
            HitRate:       stats.HitRate,
            KeyCount:      stats.KeyCount,
            ExtendedStats: stats.ExtendedStats,
        }, nil
    }
    ```

    **五处调用点逐一修改**：

    1. `mac_history_query_service_79_05_test.go:140`（需要真实缓存行为，跑 cache hit/miss 路径）：
       ```go
       // 改前：
       NewMACHistoryQueryServiceWithCache(db, NewDataCacheService(cache.NewMemoryCache(10, time.Minute)), nil)
       // 改后：
       NewMACHistoryQueryServiceWithCache(db, newFakeMACHistoryCacheProvider(cache.NewMemoryCache(10, time.Minute)), nil)
       ```

    2. `mac_history_tail_79_05_test.go:57`（需要真实缓存行为，跑 heatmap 缓存命中路径）：
       ```go
       // 改前：
       NewMACHistoryHeatmapService(db, NewDataCacheService(mem), NewCacheConfigService(db))
       // 改后：
       NewMACHistoryHeatmapService(db, newFakeMACHistoryCacheProvider(mem), NewCacheConfigService(db))
       ```

    3. `mac_history_tail_79_05_test.go:694`（不测缓存路径，fakeDB 场景）：
       ```go
       // 改前：
       NewMACHistoryHeatmapService(fakeDB, nil, nil)
       // 改后（传 nil 会导致 base.GetOrSetJSON panic，改用 NoOp）：
       NewMACHistoryHeatmapService(fakeDB, &base.NoOpCacheProvider{}, nil)
       ```

    4. `mac_history_tail_79_05_test.go:717`（不测缓存路径，fakeDB 场景）：
       ```go
       // 改前：
       NewMACHistoryHeatmapService(fakeDB, nil, nil)
       // 改后：
       NewMACHistoryHeatmapService(fakeDB, &base.NoOpCacheProvider{}, nil)
       ```

    5. `mac_history_tail_79_05_test.go:728`（需要真实缓存行为，跑 heatmap 缓存路径）：
       ```go
       // 改前：
       NewMACHistoryHeatmapService(db, NewDataCacheService(mem), NewCacheConfigService(db))
       // 改后：
       NewMACHistoryHeatmapService(db, newFakeMACHistoryCacheProvider(mem), NewCacheConfigService(db))
       ```

    **D-103-3 兼容性说明**：上述新增是测试 fixture，不是在生产代码新建 adapter 类型；
    生产路径（mac_history_router.go）仍走 `system.NewCacheProvider(core.DataCacheService)`（Task 3 已更新），
    不违背 D-103-3。
  </action>
  <verify>
    <automated>go build ./internal/services/... && go test ./internal/services/ -run "MACHistory|Heatmap" -count=1</automated>
  </verify>
  <done>fakeMACHistoryCacheProvider 实现 base.CacheProvider 全 9 方法；五处调用点全部适配；两个测试文件均不含 `internal/services/system` import（无环）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {service → base.GetOrSetJSON} | CacheProvider is untrusted input; key/TTL from service config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01 | Tampering | mac_history_query_service.go GetOrSetJSON | mitigate | TTL from s.perfCacheTTL() config; key from BuildMACQueryCacheKey |
| T-103-02 | Tampering | vendor cache-aside "Unknown Vendor" | accept | cold path vendor lookup; no security impact |
| T-103-03 | Denial | heatmap fallback直查 | accept | err path only; fallback is DB query |
| T-103-SC | Tampering | npm/pip/cargo installs | not applicable | 本相无新外部包安装 |
</threat_model>

<verification>
go build ./... && go test ./internal/services/ -run "MACHistory|Heatmap|Vendor" -count=1 && go test ./internal/api/v1/network/ -count=1 && go test ./internal/services/system/ -run "GetOrSetResidue" -count=1 -v
</verification>

<success_criteria>
1. mac_history_query_service.go: struct 字段 `cache base.CacheProvider`，`dataCache` 字段已删除
2. mac_history_query_service.go: 构造函数入参 `cache base.CacheProvider`
3. mac_history_query_service.go: 4处GetOrSet全部改为 base.GetOrSetJSON[T]（严格语义 D-103-6）
4. mac_history_query_service.go: vendor cache-aside 收敛 base.GetOrSetJSON（Unknown Vendor占位保留 D-103-5）
5. mac_history_heatmap_service.go: struct 字段 `cache base.CacheProvider`，`dataCache` 已删除
6. mac_history_heatmap_service.go: getHeatmapWithCache wrapper 保留 fallback 直查语义（D-103-9）
7. mac_history_router.go: NewMACHistoryHeatmapService 接线传入 system.NewCacheProvider(core.DataCacheService)
8. setup_routers_test.go:250 字面断言已更新为新调用串
9. **mac_history_query_service_79_05_test.go 不含 `internal/services/system` import（无环）**
10. **mac_history_tail_79_05_test.go 不含 `internal/services/system` import（无环）**
11. fakeMACHistoryCacheProvider 编译期断言 `var _ base.CacheProvider = (*fakeMACHistoryCacheProvider)(nil)` 存在
12. go build ./... 编译通过
13. go test ./internal/services/ -run "MACHistory|Heatmap|Vendor" -count=1 通过
14. go test ./internal/api/v1/network/ -count=1 通过
</success_criteria>

<output>
Create `.planning/phases/103-cache-closure-base-authority/103-01-SUMMARY.md` when done
</output>
