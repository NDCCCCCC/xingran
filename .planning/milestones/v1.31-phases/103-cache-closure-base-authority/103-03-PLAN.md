---
phase: "103"
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - "internal/services/rpa/selector_learner.go"
  - "internal/services/rpa/ai_service.go"
  - "internal/services/rpa/service.go"
  - "internal/api/v1/rpa/rpa_router.go"
  - "internal/core/core.go"
  - "internal/services/rpa/ai_selector_excel_test.go"
autonomous: true
requirements:
  - "CONV-03"
user_setup: []

must_haves:
  truths:
    - "D-103-1: selectorLearnerImpl struct 字段 cache base.CacheProvider"
    - "D-103-2: NewSelectorLearner 入参 cache cache.Cache → base.CacheProvider"
    - "D-103-3: 三域 struct 字段直接持有 base.CacheProvider 接口值，由 router/core 层 system.NewCacheProvider 注入"
    - "D-103-8: selector_learner GetBestSelector/SaveSelector 用 getBestSelectorCached wrapper 保留 best-effort 语义（错误返回 nil,nil）"
    - "D-103-12: selector_learner TTL 保持字面量 30*time.Minute 不变"
    - "D-103-13: 三域 TTL 全部行为等价，无业务语义变化；常量值与原值逐处一致"
    - "D-103-14: selector_learner SaveSelector 失效改 base.Invalidate(ctx, l.cache, []string{l.getCacheKey(...)}, \"SelectorLearner\")"
    - "D-103-15: selector_learner 无 pattern 批量失效场景，D-103-15 仅适用于 reconciliation"
    - "D-103-17: selector_learner.go 手写 cache-aside 硬失败档，迁后 interface{} 闭包 GetOrSet 计数 == 0"
    - "D-103-19: selector_learner 回归测试锁 best-effort 静默语义 + TTL 30min 等价"
    - "D-103-20: constructor 签名变更兼容性，ai_service.go:84 + service.go:22 + rpa_router.go:11,24 + core.go:1055 接线更新；ai_selector_excel_test.go:286,328,348,536 同步适配"
    - "D-103-21: TTL 等价（30*time.Minute 字面量）+ cache key 字符串等价（getCacheKey 不变）"
  artifacts:
    - path: "internal/services/rpa/selector_learner.go"
      provides: "selectorLearnerImpl struct + getBestSelectorCached wrapper + GetBestSelector/SaveSelector 迁后"
      contains: "cache base.CacheProvider"
    - path: "internal/services/rpa/ai_service.go"
      provides: "NewSelectorLearner 接线更新，NewAIService 入参增加 cacheProvider base.CacheProvider"
      contains: "system.NewCacheProvider"
    - path: "internal/services/rpa/service.go"
      provides: "NewServiceGroup 增加 cacheProvider base.CacheProvider 入参，保留 cacheInstance 供 CredentialService/TaskService 继续使用"
      contains: "cacheProvider base.CacheProvider"
    - path: "internal/api/v1/rpa/rpa_router.go"
      provides: "NewServiceGroup 两处接线传入 system.NewCacheProvider(core.DataCacheService)"
      contains: "system.NewCacheProvider"
    - path: "internal/core/core.go"
      provides: "rpa.NewServiceGroup 调用传入 system.NewCacheProvider(c.DataCacheService)"
      contains: "system.NewCacheProvider"
    - path: "internal/services/rpa/ai_selector_excel_test.go"
      provides: "NewSelectorLearner 和 NewServiceGroup 调用适配新签名，fakeSelectorCache 改造为满足 base.CacheProvider"
      contains: "base.NoOpCacheProvider"
  key_links:
    - from: "internal/services/rpa/selector_learner.go"
      to: "internal/services/base/cache_functions.go"
      via: "base.GetOrSetJSON[T]"
      pattern: "base\\.GetOrSetJSON"
    - from: "internal/services/rpa/ai_service.go"
      to: "internal/services/system/adapter.go"
      via: "system.NewCacheProvider(cache)"
      pattern: "system\\.NewCacheProvider"
    - from: "internal/services/rpa/service.go"
      to: "internal/services/system/adapter.go"
      via: "system.NewCacheProvider(cacheInstance)"
      pattern: "system\\.NewCacheProvider"
    - from: "internal/api/v1/rpa/rpa_router.go"
      to: "internal/services/system/adapter.go"
      via: "system.NewCacheProvider(core.DataCacheService)"
      pattern: "system\\.NewCacheProvider"
---

<objective>
rpa/selector_learner 域手写 JSON cache-aside（Get + Unmarshal / Marshal + Set）迁移 base.GetOrSetJSON[T] + base.CacheProvider；getBestSelectorCached wrapper 保留 best-effort 静默语义（D-103-8）；完整接线链 service.go → ai_service.go → selector_learner.go 同步更新，rpa_router.go + core.go 同步注入。
</objective>

<context>
@internal/services/rpa/selector_learner.go
@internal/services/rpa/ai_service.go
@internal/services/rpa/service.go
@internal/api/v1/rpa/rpa_router.go
@internal/core/core.go
@internal/services/rpa/ai_selector_excel_test.go
@internal/services/base/cache_functions.go
@internal/services/base/cache_provider.go
@internal/services/system/adapter.go
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From internal/services/rpa/service.go:22:
```go
func NewServiceGroup(db *gorm.DB, cfg *config.Config, noticeHub *websocket.NoticeHub, cacheInstance cache.Cache, passwordCipher addomain.PasswordCipher) *ServiceGroup {
```

From internal/services/rpa/ai_service.go:60-85:
```go
func NewAIService(cfg *config.Config, db *gorm.DB, cache cache.Cache) AIService {
    return &aiServiceImpl{
        config:          cfg,
        selectorLearner: NewSelectorLearner(db, cache, cfg),  // :84
    }
}
```

From internal/services/rpa/selector_learner.go:26-41:
```go
type selectorLearnerImpl struct {
    db     *gorm.DB
    cache  cache.Cache
    config *config.Config
    mu     sync.RWMutex
}
func NewSelectorLearner(db *gorm.DB, cache cache.Cache, cfg *config.Config) SelectorLearner {
    return &selectorLearnerImpl{db: db, cache: cache, config: cfg}
}
```

From internal/core/core.go:1055:
```go
rpaServices := rpa.NewServiceGroup(c.GetDB(), c.Config, c.NoticeHub, c.Cache, c.SM4Cipher)
```

From internal/api/v1/rpa/rpa_router.go:11,24:
```go
services := rpa.NewServiceGroup(core.GetDB(), core.Config, core.NoticeHub, core.Cache, nil)
```

From internal/services/rpa/ai_selector_excel_test.go:348:
```go
l := NewSelectorLearner(db, &fakeSelectorCache{}, newAICfg("", "", false, false))
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: selector_learner.go — struct 字段 + 构造函数 + getBestSelectorCached wrapper + GetBestSelector + SaveSelector</name>
  <files>internal/services/rpa/selector_learner.go</files>
  <read_first>internal/services/rpa/selector_learner.go (struct :26-32, constructor :35, GetBestSelector :167-231, getCacheKey :36-44, RecordSuccess :91-128, Delete :124-126)</read_first>
  <action>
    1. **struct 字段（D-103-1/D-103-3）**：`selectorLearnerImpl` 的 `cache cache.Cache` 改为 `cache base.CacheProvider`。

    2. **构造函数（D-103-2）**：`NewSelectorLearner(db *gorm.DB, cache cache.Cache, cfg *config.Config)` 入参 `cache` 改为 `cache base.CacheProvider`。

    3. **新增 getBestSelectorCached wrapper（D-103-8 best-effort 静默语义）**：
       ```go
       func (l *selectorLearnerImpl) getBestSelectorCached(ctx context.Context, pageURL, elementID string) (*SelectorRecommendation, error) {
           cacheKey := l.getCacheKey(pageURL, elementID)
           result, err := base.GetOrSetJSON[*SelectorRecommendation](ctx, l.cache, cacheKey, 30*time.Minute, func() (*SelectorRecommendation, error) {
               // ... 原 GetBestSelector 内 DB 查询逻辑 ...
           })
           // D-103-8: best-effort 语义
           if err != nil {
               applogger.Warnf("[selector_learner] GetBestSelector cache failed: %v", err)
               return nil, nil  // 返回 nil, nil，调用方视为 cache miss
           }
           return result, nil
       }
       ```
       TTL 保持字面量 `30*time.Minute`（D-103-12）。

    4. **GetBestSelector 改写（:167-231）**：删除手写 Get + Unmarshal / Marshal + Set，替换为：
       ```go
       return l.getBestSelectorCached(ctx, pageURL, elementID)
       ```
       调用方处理 `nil, nil` 返回视为 cache miss（best-effort 语义）。

    5. **SaveSelector / RecordSuccess 失效（:124-126）（D-103-14）**：将：
       ```go
       _ = l.cache.Delete(ctx, cacheKey)
       ```
       替换为：
       ```go
       base.Invalidate(ctx, l.cache, []string{l.getCacheKey(record.PageURL, record.ElementID)}, "SelectorLearner")
       ```

    6. **TTL**：保持 `30*time.Minute` 字面量（D-103-12）。

    **不要改动**：`getCacheKey` 函数本身；`config` / `mu` 字段；其他业务逻辑。
  </action>
  <verify>
    <automated>go build ./internal/services/rpa/...</automated>
  </verify>
  <done>selector_learner.go 编译通过；cache字段改为 base.CacheProvider；手写 cache-aside 替换为 getBestSelectorCached wrapper；best-effort 语义保留；SaveSelector 失效改 base.Invalidate</done>
</task>

<task type="auto">
  <name>Task 2: ai_service.go — NewAIService 入参增加 cacheProvider + NewSelectorLearner 接线更新</name>
  <files>internal/services/rpa/ai_service.go</files>
  <read_first>internal/services/rpa/ai_service.go (:50-86)</read_first>
  <action>
    更新 ai_service.go:60-84（B4 caller audit）：

    1. **NewAIService 函数签名增加 `cacheProvider base.CacheProvider` 入参**：
       ```go
       func NewAIService(cfg *config.Config, db *gorm.DB, cacheProvider base.CacheProvider) AIService {
       ```

    2. **NewSelectorLearner 接线（:84）**：将：
       ```go
       selectorLearner: NewSelectorLearner(db, cache, cfg),
       ```
       改为：
       ```go
       selectorLearner: NewSelectorLearner(db, cacheProvider, cfg),
       ```
       注意：此处直接使用 `cacheProvider`，不需要再套 `system.NewCacheProvider`（因为入参已经是 `base.CacheProvider` 类型）。
  </action>
  <verify>
    <automated>go build ./internal/services/rpa/...</automated>
  </verify>
  <done>ai_service.go 编译通过；NewAIService 入参增加 cacheProvider base.CacheProvider；NewSelectorLearner 直接使用 cacheProvider</done>
</task>

<task type="auto">
  <name>Task 3: service.go — NewServiceGroup 增加 cacheProvider 入参 + 透传给 NewAIService</name>
  <files>internal/services/rpa/service.go</files>
  <read_first>internal/services/rpa/service.go (:11-41)</read_first>
  <action>
    更新 service.go:22（B4 caller audit）：

    1. **NewServiceGroup 签名增加 `cacheProvider base.CacheProvider` 入参，保留 `cacheInstance cache.Cache` 不动**（供 CredentialService / TaskService 继续使用）：
       ```go
       func NewServiceGroup(db *gorm.DB, cfg *config.Config, noticeHub *websocket.NoticeHub, cacheInstance cache.Cache, passwordCipher addomain.PasswordCipher, cacheProvider base.CacheProvider) *ServiceGroup {
       ```

    2. **NewAIService 调用传入 cacheProvider**：
       ```go
       AIService: NewAIService(cfg, db, cacheProvider),
       ```

    3. **CredentialService / TaskService 继续使用 cacheInstance**（不变）：
       ```go
       CredentialService: NewCredentialService(db, passwordCipher, cacheInstance),
       TaskService: NewTaskService(db, cacheInstance, credentialService),
       ```
  </action>
  <verify>
    <automated>go build ./internal/services/rpa/...</automated>
  </verify>
  <done>service.go 编译通过；NewServiceGroup 增加 cacheProvider 入参；NewAIService 传入 cacheProvider；CredentialService/TaskService 继续使用 cacheInstance</done>
</task>

<task type="auto">
  <name>Task 4: rpa_router.go + core.go — NewServiceGroup 两处接线传入 CacheProvider</name>
  <files>internal/api/v1/rpa/rpa_router.go, internal/core/core.go</files>
  <read_first>internal/api/v1/rpa/rpa_router.go (:8-28); internal/core/core.go (:1050-1060)</read_first>
  <action>
    **rpa_router.go 两处（:11, :24）接线更新（B4 caller audit）**：

    将：
    ```go
    services := rpa.NewServiceGroup(core.GetDB(), core.Config, core.NoticeHub, core.Cache, nil)
    ```
    改为：
    ```go
    services := rpa.NewServiceGroup(core.GetDB(), core.Config, core.NoticeHub, core.Cache, nil, system.NewCacheProvider(core.DataCacheService))
    ```

    **core.go:1055 接线更新**：

    将：
    ```go
    rpaServices := rpa.NewServiceGroup(c.GetDB(), c.Config, c.NoticeHub, c.Cache, c.SM4Cipher)
    ```
    改为：
    ```go
    rpaServices := rpa.NewServiceGroup(c.GetDB(), c.Config, c.NoticeHub, c.Cache, c.SM4Cipher, system.NewCacheProvider(c.DataCacheService))
    ```

    两处均使用 `system.NewCacheProvider(core.DataCacheService)` 适配器。
  </action>
  <verify>
    <automated>go build ./...</automated>
  </verify>
  <done>rpa_router.go 和 core.go 编译通过；NewServiceGroup 两处接线传入 system.NewCacheProvider(core.DataCacheService)</done>
</task>

<task type="auto">
  <name>Task 5: ai_selector_excel_test.go — fakeSelectorCache 改造 + NewSelectorLearner/NewServiceGroup 调用适配</name>
  <files>internal/services/rpa/ai_selector_excel_test.go</files>
  <read_first>internal/services/rpa/ai_selector_excel_test.go (:230-260, :280-360, :530-545)</read_first>
  <action>
    ai_selector_excel_test.go 有四处调用 NewSelectorLearner / NewServiceGroup，签名变更后编译失败（B4 caller audit）：

    **fakeSelectorCache 改造**：`&fakeSelectorCache{}` 当前满足 `cache.Cache`（7方法），改为满足 `base.CacheProvider`（9方法）。在 `fakeSelectorCache` struct 新增以下方法：
    ```go
    func (f *fakeSelectorCache) GetOrSet(ctx context.Context, key string, dest interface{}, expiration time.Duration, query func() (interface{}, error)) error { return nil }
    func (f *fakeSelectorCache) DeleteByPattern(ctx context.Context, pattern string) error { return nil }
    func (f *fakeSelectorCache) MGet(ctx context.Context, keys ...string) (map[string]string, error) { return nil, nil }
    func (f *fakeSelectorCache) MDelete(ctx context.Context, keys ...string) error { return nil }
    func (f *fakeSelectorCache) Exists(ctx context.Context, key string) (bool, error) { return false, nil }
    func (f *fakeSelectorCache) SetTTL(ctx context.Context, key string, expiration time.Duration) error { return nil }
    func (f *fakeSelectorCache) GetTTL(ctx context.Context, key string) (time.Duration, error) { return 0, nil }
    func (f *fakeSelectorCache) GetStats(ctx context.Context) (*base.CacheStats, error) { return nil, nil }
    ```

    **调用点适配**：
    - `:286` `NewSelectorLearner(db, c, ...)` → `NewSelectorLearner(db, system.NewCacheProvider(c), ...)`（c 为 `*cache.Cache`）
    - `:328` `NewSelectorLearner(db, hitCache, ...)` → `NewSelectorLearner(db, system.NewCacheProvider(hitCache), ...)`
    - `:348` `NewSelectorLearner(db, &fakeSelectorCache{}, ...)` → `NewSelectorLearner(db, &fakeSelectorCache{}, ...)`（fakeSelectorCache 已改造为满足 base.CacheProvider，无需 NewCacheProvider 包裹）
    - `:536` `NewServiceGroup(db, cfg, nil, &fakePlainCache{}, ...)` → 需增加第6参数 `system.NewCacheProvider(&fakePlainCache{})`

    需要 import `base "github.com/xingran-next/xingran-go-backend/internal/services/base"` 和 `systemServices "github.com/xingran-next/xingran-go-backend/internal/services/system"`。
  </action>
  <verify>
    <automated>go build ./internal/services/rpa/... && go test ./internal/services/rpa/ -run "Selector" -count=1</automated>
  </verify>
  <done>ai_selector_excel_test.go 编译通过；fakeSelectorCache 改造为满足 base.CacheProvider；所有 NewSelectorLearner/NewServiceGroup 调用已适配新签名</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {service → base.GetOrSetJSON} | CacheProvider untrusted; key from getCacheKey helper |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-06 | Denial | selector_learner best-effort 静默 | accept | nil,nil 返回调用方视为 miss；DB fallback |
| T-103-07 | Tampering | cache key getCacheKey | mitigate | key from static helper; no user input |
| T-103-08 | Denial | service.go 透传 cacheInstance 兼容性 | accept | cacheInstance 仍为 cache.Cache 类型，供 CredentialService/TaskService 继续使用，不在本相范围 |
| T-103-SC | Tampering | npm/pip/cargo installs | not applicable | 本相无新外部包安装 |
</threat_model>

<verification>
go build ./... && go test ./internal/services/rpa/ -run "Selector" -count=1 && go test ./internal/services/system/ -run "GetOrSetResidue" -count=1 -v
</verification>

<success_criteria>
1. selector_learner.go: struct 字段 `cache base.CacheProvider`
2. selector_learner.go: 构造函数入参 `base.CacheProvider`
3. selector_learner.go: GetBestSelector 改用 getBestSelectorCached wrapper（best-effort 语义 D-103-8）
4. selector_learner.go: SaveSelector 失效改 base.Invalidate
5. ai_service.go: NewAIService 入参增加 cacheProvider base.CacheProvider
6. ai_service.go: NewSelectorLearner 直接使用 cacheProvider
7. service.go: NewServiceGroup 增加 cacheProvider 入参，cacheInstance 保留供 CredentialService/TaskService
8. rpa_router.go: 两处 NewServiceGroup 接线传入 system.NewCacheProvider(core.DataCacheService)
9. core.go: NewServiceGroup 接线传入 system.NewCacheProvider(c.DataCacheService)
10. ai_selector_excel_test.go: fakeSelectorCache 改造为满足 base.CacheProvider
11. ai_selector_excel_test.go: 所有 NewSelectorLearner/NewServiceGroup 调用已适配新签名
12. go build ./... 编译通过
13. go test ./internal/services/rpa/ -run "Selector" -count=1 通过
</success_criteria>

<output>
Create `.planning/phases/103-cache-closure-base-authority/103-03-SUMMARY.md` when done
</output>
