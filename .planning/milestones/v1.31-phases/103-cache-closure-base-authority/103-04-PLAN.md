---
phase: "103"
plan: "04"
type: execute
wave: 2
depends_on:
  - "103-01"
  - "103-02"
  - "103-03"
files_modified:
  - "internal/services/system/cache_invariants_92_test.go"
autonomous: true
requirements:
  - "CONV-04"
user_setup: []

must_haves:
  truths:
    - "D-103-16: cache_invariants_92_test.go hardDirs 追加 servicesRoot + asset + rpa 三项"
    - "D-103-17: 本相收敛面 4 文件 interface{} 闭包 GetOrSet 硬失败档计数 == 0"
    - "D-103-18: 手写 cache-aside AST 检测（cache.Get → json.Unmarshal → cache.Set 含 json.Marshal 序列）计数 == 0"
    - "D-103-19: invariants 扩口后 mac_history/asset/rpa 三域 interface{} 闭包 GetOrSet + 手写 cache-aside 全部归零"
  artifacts:
    - path: "internal/services/system/cache_invariants_92_test.go"
      provides: "TestNoInterfaceGetOrSetResidue 扩口 + cacheAsideResidue 函数 + TestNoHandwrittenCacheAside + 4文件白名单归零"
      contains: "servicesRoot + asset + rpa in hardDirs"
  key_links:
    - from: "internal/services/system/cache_invariants_92_test.go"
      to: "internal/services/mac_history_query_service.go"
      via: "AST 扫描"
      pattern: "hardDirs"
    - from: "internal/services/system/cache_invariants_92_test.go"
      to: "internal/services/asset/reconciliation_service.go"
      via: "AST 扫描"
      pattern: "hardDirs"
    - from: "internal/services/system/cache_invariants_92_test.go"
      to: "internal/services/rpa/selector_learner.go"
      via: "AST 扫描"
      pattern: "hardDirs"
---

<objective>
cache_invariants_92_test.go 扩口——hardDirs 追加 servicesRoot + asset + rpa；新增手写 cache-aside AST 检测函数（D-103-18）；4文件 allowedResidues 归零；本相收敛面（mac_history_query_service / mac_history_heatmap / reconciliation / selector_learner）interface{} 闭包 GetOrSet 硬失败档 == 0。
</objective>

<context>
@internal/services/system/cache_invariants_92_test.go
@internal/services/mac_history_query_service.go
@internal/services/mac_history_heatmap_service.go
@internal/services/asset/reconciliation_service.go
@internal/services/rpa/selector_learner.go
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From internal/services/system/cache_invariants_92_test.go (:40-200):
```go
// hardDirs: Phase 92 既有
hardDirs := []string{
    filepath.Join(servicesRoot, "system"),
    filepath.Join(servicesRoot, "operations"),
}

// allowedResidues: 初始全空 = 期望 0
var allowedResidues = map[string]int{}

// 检测函数名：cacheImplResidue92（检测 interface{} 闭包 GetOrSet）
```

手写 cache-aside 标志性序列（Phase 103 已迁位点）：
- reconciliation_service.go: GetByWorkstation 手写 cache-aside（`cache.GetJSON` 短路 → `json.Unmarshal` → `cache.Set` + `json.Marshal`）
- selector_learner.go: GetBestSelector 手写 cache-aside（`cache.Get` → `json.Unmarshal` → `cache.Set` + `json.Marshal`）
- mac_history_query_service.go: vendor lookup（`cache.Get` → `cache.Set` 无 json.Marshal，非目标序列，但 vendor 已迁 base.GetOrSetJSON）

手写 cache-aside AST 检测目标序列（cache.Get → json.Unmarshal → cache.Set 含 json.Marshal）：
- `cache.Get(ctx, key)` 或 `s.cache.Get(ctx, key)` 调用（selector 为 Get，receiver 类型匹配 `cache.Cache` 或含 `cache` 字段的 struct）
- 检测后续 5 行内 `json.Unmarshal(bytes, &dest)` 调用
- 检测后续 10 行内 `cache.Set(ctx, key, json.Marshal(...))` 或 `s.cache.Set(ctx, key, json.Marshal(...))` 调用
- 命中即 fail（硬失败档）
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: cache_invariants_92_test.go — hardDirs 扩口 + 手写 cache-aside AST 检测函数 + 4文件白名单归零</name>
  <files>internal/services/system/cache_invariants_92_test.go</files>
  <read_first>internal/services/system/cache_invariants_92_test.go (hardDirs :139-149, cacheImplResidue92 :43-93, allowedResidues :31-33, TestNoInterfaceGetOrSetResidue :100-160)</read_first>
  <action>
    对 cache_invariants_92_test.go 进行以下改动：

    1. **hardDirs 扩口（:139-149）（D-103-16）**：在 `TestNoInterfaceGetOrSetResidue` 函数的 hardDirs slice 中追加三项：
       ```go
       hardDirs := []string{
           filepath.Join(servicesRoot, "system"),
           filepath.Join(servicesRoot, "operations"),
           // Phase 103 扩口：
           servicesRoot,                              // mac_history_query_service.go, mac_history_heatmap_service.go
           filepath.Join(servicesRoot, "asset"),       // reconciliation_service.go
           filepath.Join(servicesRoot, "rpa"),         // selector_learner.go
       }
       ```

    2. **新增手写 cache-aside AST 检测函数 `cacheAsideResidue`（D-103-18）**：

       函数签名：
       ```go
       // cacheAsideResidue 检测手写 cache-aside 模式（cache.Get → json.Unmarshal → cache.Set 含 json.Marshal）
       // 扫描目录：hardDirs 全部文件
       // 白名单：allowedResidues103（初始空 map）
       // 返回：map[filename]count（硬失败档：count > 0 → t.Error）
       func cacheAsideResidue(t *testing.T, hardDirs []string) map[string]int {
           // AST 遍历策略：
           // 1. 解析每个 .go 文件为 *ast.File
           // 2. ast.Inspect(file, func(node ast.Node) bool {
           //      if assign, ok := node.(*ast.AssignExpr); ok {
           //          // 匹配 s.cache.Get / cache.Get 调用（lhs 为 receiver + "Get" 方法名）
           //          // 记录 key 变量名（从 CallExpr.Args[1] 提取）
           //      }
           //      if call, ok := node.(*ast.CallExpr); ok {
           //          // 检查 json.Unmarshal 调用（从 bytes 反序列化）
           //      }
           //      // 在 Get 调用后 10 行内检查 cache.Set 调用含 json.Marshal
           //  })
           // 3. 白名单 allowedResidues103[key] > 0 则跳过
           // 4. 返回 count > 0 则 t.Errorf("手写 cache-aside 残留: %s (%d 处)", file, count)
       }
       ```

       具体模式匹配规则：
       - 匹配 `*ast.CallExpr`，selector 为 `cache.Get`、`s.cache.Get`、`l.cache.Get`、`c.cache.Get`（receiver 含 "cache" 关键词）
       - 提取 `CallExpr.Args[1]` 作为 key 变量引用
       - 在后续 10 行内匹配 `*ast.CallExpr`，selector 含 `json.Unmarshal`，且其第一个参数引用了上述 key 变量相关的 `[]byte` 来源
       - 继续在后续 10 行内匹配 `*ast.CallExpr`，selector 含 `cache.Set`、`s.cache.Set` 等，且参数含 `json.Marshal`

       4文件（mac_history_query_service.go / mac_history_heatmap_service.go / reconciliation_service.go / selector_learner.go）内**无**此序列，计数必须 == 0。

    3. **新增 `TestNoHandwrittenCacheAside` 测试函数（D-103-18）**：
       ```go
       func TestNoHandwrittenCacheAside(t *testing.T) {
           files := cacheAsideResidue(t, hardDirs)
           for f, count := range files {
               if count > 0 {
                   t.Errorf("手写 cache-aside 残留: %s (%d 处)", f, count)
               }
           }
       }
       ```

    4. **allowedResidues 白名单归零（D-103-17）**：本相收敛面 4 文件的 `allowedResidues` 条目保持空（初始全空 = 期望 0）：
       ```go
       // Phase 103 收敛面：4 文件 interface{} 闭包 GetOrSet 期望 0
       // 无需额外登记，allowedResidues 初始即为空 map
       ```
  </action>
  <verify>
    <automated>go test ./internal/services/system/ -run "GetOrSetResidue|HandwrittenCacheAside" -count=1 -v</automated>
  </verify>
  <done>cache_invariants_92_test.go 编译通过；hardDirs 追加 3 项；新增 cacheAsideResidue 函数和 TestNoHandwrittenCacheAside；4文件 interface{} 闭包 GetOrSet 计数 == 0；手写 cache-aside 计数 == 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {AST scan → source files} | Invariants scanner reads service files as text; scan is read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-08 | Denial | invariants 扫描误报 | mitigate | D-103-18 锁定检测模式为标志性序列，低误报风险 |
| T-103-SC | Tampering | npm/pip/cargo installs | not applicable | 本相无新外部包安装 |
</threat_model>

<verification>
go build ./... && go test ./internal/services/system/ -run "GetOrSetResidue|HandwrittenCacheAside" -count=1 -v && go test ./internal/services/ -run "MACHistory|Heatmap|Reconciliation|Selector" -count=1
</verification>

<success_criteria>
1. cache_invariants_92_test.go: hardDirs 追加 servicesRoot + asset + rpa
2. cache_invariants_92_test.go: 新增 cacheAsideResidue AST 检测函数
3. cache_invariants_92_test.go: TestNoHandwrittenCacheAside 对 4 文件执行检测，计数 == 0
4. cache_invariants_92_test.go: allowedResidues 对 4 文件保持空（interface{} 闭包 GetOrSet 期望 0）
5. go build ./... 编译通过
6. go test ./internal/services/system/ -run "GetOrSetResidue|HandwrittenCacheAside" -count=1 -v 通过
7. go test ./internal/services/ -run "MACHistory|Heatmap|Reconciliation|Selector" -count=1 通过
</success_criteria>

<output>
Create `.planning/phases/103-cache-closure-base-authority/103-04-SUMMARY.md` when done
</output>
