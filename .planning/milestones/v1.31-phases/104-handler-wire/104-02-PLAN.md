---
phase: 104-handler-wire
plan: "02"
type: execute
wave: 2
depends_on:
  - "104-01"
files_modified:
  - internal/api/v1/operations/wall_handler.go
  - internal/api/v1/operations/server_room_handler.go
  - internal/api/v1/operations/floor_handler.go
  - internal/api/v1/operations/door_handler.go
  - internal/api/v1/operations/building_handler.go
  - internal/api/v1/operations/room_device_handler.go
  - internal/api/v1/operations/floor_plan_text_handler.go
  - internal/api/v1/operations/dedicated_line_handler.go
  - internal/api/v1/operations/location_alias_handler.go
  - internal/api/v1/operations/infopoint_handler.go
  - internal/api/v1/operations/workstation_handler.go
  - internal/api/v1/operations/workstation_device_handler.go
  - internal/api/v1/operations/asset_handler.go
  - internal/api/v1/operations/asset_component_handler.go
autonomous: true
requirements:
  - HANDLER-01
user_setup: []

must_haves:
  truths:
    - "All 14 operations CRUD handlers use response.HandleJSONBinding and response.HandleServiceError from pkg"
    - "server_room List drift fixed — no direct response.Error with apperrors in service error path"
    - "No remaining calls to local handleJSONBinding or handleServiceError"
  artifacts:
    - path: internal/api/v1/operations/*_handler.go
      provides: 14 handlers switched to pkg helpers
      contains: "response.HandleJSONBinding|response.HandleServiceError"
  key_links:
    - from: operations/*_handler.go
      to: pkg/response/handler_helpers.go
      via: response.HandleJSONBinding / response.HandleServiceError
      pattern: "response\\.Handle"
---

<objective>
Switch all 14 operations CRUD handlers to use `pkg/response.HandleJSONBinding` and `pkg/response.HandleServiceError`; fix server_room List drift (line 101-103 direct response.Error call).
</objective>

<context>
@pkg/response/handler_helpers.go
@internal/api/v1/operations/building_handler.go
@internal/api/v1/operations/server_room_handler.go
@internal/api/v1/operations/wall_handler.go
@internal/api/v1/operations/floor_handler.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch first 5 operations handlers to pkg helpers (wall/floor/door/building/room_device)</name>
  <files>
    - internal/api/v1/operations/wall_handler.go
    - internal/api/v1/operations/floor_handler.go
    - internal/api/v1/operations/door_handler.go
    - internal/api/v1/operations/building_handler.go
    - internal/api/v1/operations/room_device_handler.go
  </files>
  <action>
In each handler file, replace:
1. Import: remove `"github.com/xingran-next/xingran-go-backend/internal/api/v1/operations"` if it was only used for local helpers; add/keep `response "github.com/xingran-next/xingran-go-backend/pkg/response"` if not present
2. Replace all `handleJSONBinding(c, &obj)` calls with `response.HandleJSONBinding(c, &obj)`
3. Replace all `handleServiceError(c, err, "操作名")` calls with `response.HandleServiceError(c, err, "操作名")`
4. Remove any local helper function definitions (if any remain in these files — building_handler.go uses the package-level helpers from base_handler.go which is now deleted, so all calls must switch)

For each handler, the refactoring pattern for Create/List/Update/Delete/BatchOperation methods:
- `handleJSONBinding` → `response.HandleJSONBinding`
- `handleServiceError` → `response.HandleServiceError`

**building_handler.go specific:** This file already imports both apperrors and response. Update the import to remove operations package (if it was only for the local helpers). Replace `handleJSONBinding` → `response.HandleJSONBinding` and `handleServiceError` → `response.HandleServiceError` on all call sites.
</action>
  <verify>
    <automated>go build ./internal/api/v1/operations/... 2>&1 | head -20</automated>
  </verify>
  <done>5 handlers (wall/floor/door/building/room_device) switched to response.HandleJSONBinding and response.HandleServiceError; go build passes</done>
</task>

<task type="auto">
  <name>Task 2: Switch remaining 9 operations handlers to pkg helpers</name>
  <files>
    - internal/api/v1/operations/floor_plan_text_handler.go
    - internal/api/v1/operations/dedicated_line_handler.go
    - internal/api/v1/operations/location_alias_handler.go
    - internal/api/v1/operations/infopoint_handler.go
    - internal/api/v1/operations/workstation_handler.go
    - internal/api/v1/operations/workstation_device_handler.go
    - internal/api/v1/operations/asset_handler.go
    - internal/api/v1/operations/asset_component_handler.go
    - internal/api/v1/operations/server_room_handler.go
  </files>
  <action>
Same replacement pattern as Task 1: `handleJSONBinding` → `response.HandleJSONBinding` and `handleServiceError` → `response.HandleServiceError` in all handler methods.

**server_room_handler.go additional fix (HANDLER-01 drift fix):**
- Line 101-103: `response.Error(c, apperrors.InternalServerErrorWithMsg("查询失败"))` is a drift — the service returned an error but the handler discarded it and returned a hardcoded message
- Fix: Change to `response.HandleServiceError(c, err, "查询")` which properly propagates the actual error

Also fix server_room Statistics (around line 37): `response.Error(c, http.StatusInternalServerError, err.Error())` leaks err detail. Change to `response.HandleServiceError(c, err, "统计")`.
</action>
  <verify>
    <automated>go build ./internal/api/v1/operations/... 2>&1 | head -20</automated>
  </verify>
  <done>All 14 operations handlers switched to pkg helpers; server_room drift fixed; go build passes</done>
</task>

</tasks>

<verification>
go build ./internal/api/v1/operations/...
grep -r "handleJSONBinding\|handleServiceError" --include="*.go" internal/api/v1/operations/ | grep -v "_test.go" | head -5  # should be empty
</verification>

<success_criteria>
- All 14 operations handlers use response.HandleJSONBinding and response.HandleServiceError
- server_room List drift (line 101-103) fixed — uses HandleServiceError instead of direct response.Error
- server_room Statistics drift (err.Error() leak) fixed
- No remaining references to local handleJSONBinding or handleServiceError in operations handlers
- go build ./internal/api/v1/operations/... passes
</success_criteria>

<output>
Create `.planning/phases/104-handler-wire/104-02-SUMMARY.md` when done
</output>
