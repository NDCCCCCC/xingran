---
phase: 102-mechanical-constants
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/v1/system/file_handler.go
  - internal/utils/pagination.go
  - internal/utils/utils_74_12_test.go
  - .planning/phases/102-mechanical-constants/102-PAGI-CALLER-AUDIT.md
autonomous: true
requirements: [PAGI-01]
user_setup: []

must_haves:
  truths:
    - "file_handler.go 分页归一走 query.NormalizePaginationWithMax，cap 严格 100（constants.MaxListPageSize），行为等价（既有 file_handler_test/file_service_test 全绿）"
    - "internal/utils/pagination.go 整文件删除，TestParsePaginationAndOffset 一并删除，全仓零 ParsePagination/PaginationParams/Offset/Limit/BuildPaginationResponse 引用（编译期守护）"
    - "逐调用方核对清单落盘（SC-3），cap=100 vs pkg 默认 200 历史分叉在调用处注释自证"
  artifacts:
    - path: "internal/api/v1/system/file_handler.go"
      provides: "ListFiles 分页经 NormalizePaginationWithMax(req.Page, req.PageSize, constants.MaxListPageSize) + 局部 offset 算式"
      contains: "NormalizePaginationWithMax"
    - path: ".planning/phases/102-mechanical-constants/102-PAGI-CALLER-AUDIT.md"
      provides: "逐调用方核对清单（唯一生产调用方 + 测试调用方 + 同名异型排除项，各附核对证据）"
      contains: "file_handler.go"
  key_links:
    - from: "internal/api/v1/system/file_handler.go"
      to: "pkg/query/pagination.go"
      via: "NormalizePaginationWithMax 唯一实现核心（Phase 99 D-03-2 交付）+ constants.MaxListPageSize cap=100"
      pattern: "query\\.NormalizePaginationWithMax"
---

<objective>
PAGI-01：internal/utils/pagination.go 的 ParsePagination 双口径收敛到 pkg/query.NormalizePaginationWithMax 单一口径（cap 严格 100 行为等价），唯一生产调用方迁移后按继承锁定（Phase 99 D-03-3）整文件删除 utils/pagination.go 与对应测试，不留兼容壳。

Purpose: 分页 normalize 只允许一个实现核心；utils 第二份实现是 F-09 双口径的最后一根尾巴。
Output: file_handler.go 迁移 + utils/pagination.go 删除 + 逐调用方核对清单落盘。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-mechanical-constants/102-CONTEXT.md
@.planning/phases/102-mechanical-constants/102-RESEARCH.md
@.planning/phases/102-mechanical-constants/102-PATTERNS.md

@sections: 102-RESEARCH.md §PAGI（语义等价证明 + 调用方全量核对清单）/ §Pitfall 7（TTL/configKey 顺改动禁令在本 plan 的对应物：BuildListResponse 不动）
@sections: 102-PATTERNS.md §D（分页迁移形态 BEFORE/AFTER 全摘录 + import 处置注意）
</context>

<interfaces>
<!-- 目标 API（pkg/query/pagination.go:47-62 既有，零新增 pkg 函数）：
query.NormalizePaginationWithMax(current, pageSize, maxPageSize int) (int, int)
  current <= 0 -> constants.DefaultCurrent(1)；pageSize <= 0 -> constants.DefaultPageSize(10)；pageSize > maxPageSize -> maxPageSize（钳制不重置）；1-9 原样透传（无下限放大）
cap 取 constants.MaxListPageSize = 100（pkg/constants/pagination.go），与 utils.ParsePagination 原 cap 严格一致——不用默认 cap=200（历史分叉点，D-102-9）

import 处置（PATTERNS §D 核实）：
- file_handler.go 的 internal/utils import 不能删：GetUserID/GetUsername/GetClientIP(:48/:73-74)、BuildListResponse(:173)、BuildCountResponse(:200) 仍在用
- 新增 import：pkg/query 与 pkg/constants
- utils.BuildPaginationResponse 零其他调用方（RESEARCH 全仓 grep 实证），随文件删除
- internal/api/v1/operations/requests.PaginationParams 为同名异型（operations 自有类型），不相关勿动 -->

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: file_handler.go 迁移 NormalizePaginationWithMax + 调用方核对清单落盘</name>
  <files>internal/api/v1/system/file_handler.go, .planning/phases/102-mechanical-constants/102-PAGI-CALLER-AUDIT.md</files>
  <read_first>
    internal/api/v1/system/file_handler.go :140-205（ListFiles 全貌：:160 ParsePagination、:162 Offset/Limit 消费、:173 BuildListResponse、:200 BuildCountResponse、import 块）
    pkg/query/pagination.go :40-62（NormalizePaginationWithMax 签名与语义）
    internal/utils/pagination.go（全文 49 行——被迁移语义的对照原貌）
  </read_first>
  <action>
    :160 pagination := utils.ParsePagination(req.Page, req.PageSize) 改为 current, pageSize := query.NormalizePaginationWithMax(req.Page, req.PageSize, constants.MaxListPageSize)，并在该行上方加注释自证历史分叉：本端点 cap=100（constants.MaxListPageSize），刻意不用 NormalizePagination 默认 cap=200。
    :162 的 pagination.Offset() 改局部算式 offset := (current - 1) * pageSize 并以 offset, pageSize 作实参传 ListFiles；:173 utils.BuildListResponse(result, total, pagination.Page, pagination.PageSize) 改传 current, pageSize（BuildListResponse 本身保留不动——CONTEXT 明确保留，其 page/pageSize 键名分叉是 Deferred）。
    import 块：新增 pkg/query 与 pkg/constants；internal/utils import 保留（其他 helper 仍在用）。
    新建 .planning/phases/102-mechanical-constants/102-PAGI-CALLER-AUDIT.md 落盘逐调用方核对清单（SC-3）：三行表——file_handler.go:160 唯一生产调用方（处置=迁移，附 NormalizePaginationWithMax 引用证据）；utils_74_12_test.go:203 TestParsePaginationAndOffset（处置=随 Task 2 删除）；operations/requests.PaginationParams 系列（处置=同名异型不相关，附 grep 证据）。表后附核对方命令（全仓 grep ParsePagination/ParsePaginationWithMax）与执行日期。
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/api/v1/system/ -run TestFile -count=1</automated>
  </verify>
  <acceptance_criteria>
    - file_handler.go contains query.NormalizePaginationWithMax 且 contains constants.MaxListPageSize；contains cap=100 注释
    - file_handler.go 不再引用 utils.ParsePagination；utils import 仍在（BuildListResponse 等使用证据）
    - 102-PAGI-CALLER-AUDIT.md 存在且含三行核对表与 grep 证据命令
    - go test ./internal/api/v1/system/ -run TestFile 0 失败（cap=100 行为等价的既有测试网）
  </acceptance_criteria>
  <done>唯一生产调用方迁移完成、cap=100 分叉注释自证、核对清单落盘、既有 handler 测试全绿</done>
</task>

<task type="auto">
  <name>Task 2: 删除 internal/utils/pagination.go 与对应测试（不留兼容壳）</name>
  <files>internal/utils/pagination.go, internal/utils/utils_74_12_test.go</files>
  <read_first>
    internal/utils/pagination.go（全文 49 行——确认 ParsePagination/PaginationParams/Offset/Limit/BuildPaginationResponse 五符号无其他残留消费者）
    internal/utils/utils_74_12_test.go（全文——定位 TestParsePaginationAndOffset 及同文件其他测试）
    internal/utils/response_builder.go（BuildListResponse 宿主——保留确认）
  </read_first>
  <action>
    先全仓 grep 确认 ParsePagination、PaginationParams（限 internal/utils 版）、BuildPaginationResponse 仅剩 utils 包内部与 utils_74_12_test.go 引用（file_handler 已在 Task 1 迁移；operations requests 同名异型不含）。
    删除 internal/utils/pagination.go 整文件（继承锁定 Phase 99 D-03-3：零调用方即删除，不留兼容壳）。
    从 internal/utils/utils_74_12_test.go 删除 TestParsePaginationAndOffset 函数（:203-226）；若删除后该文件无剩余测试函数则整文件删除，否则保留其余测试。
    internal/utils/response_builder.go（BuildListResponse）保留不动。
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/utils/ -count=1 && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/utils/pagination.go 文件不存在
    - 全仓 grep -rn "ParsePagination\\b" --include='*.go' 仅命中 operations requests 包自有类型与 pkg/query（零 utils 版残留）
    - grep -rn "BuildPaginationResponse" --include='*.go' 计数 0
    - utils_74_12_test.go 不再包含 TestParsePaginationAndOffset
    - go build ./... 退出码 0（编译期守护：任何残留引用立即失败）；go test ./... 0 失败
  </acceptance_criteria>
  <done>utils 分页代码全部消失（文件删除 + 测试删除），全仓编译与测试绿，PAGI-01 收口</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端分页参数 → DB 查询 | 分页归一逻辑是非法 current/pageSize 进入 DB 的唯一闸口；迁移若语义走样会产生负 offset 或超大 pageSize（Tampering 面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-12 | Tampering | 迁移引入语义差异（cap 200 替代 100 / 下限放大 / offset 算式错） | mitigate | RESEARCH §PAGI 逐行等价证明为蓝本；NormalizePaginationWithMax 显式传 MaxListPageSize；既有 file_handler/file_service 测试网锁定行为 |
| T-102-13 | DoS | pageSize 上限松绑（100 到 200）放大查询负载 | mitigate | cap 严格 100 是 D-102-9 锁定点，acceptance 断言 constants.MaxListPageSize 显式传参 |
| T-102-SC | Tampering | npm/pip/cargo 安装 | accept | 零新包安装，供应链面无变化 |
</threat_model>

<verification>
- go build ./... 退出码 0（utils 分页符号编译期归零守护）
- go test ./... 0 失败
- go test ./internal/api/v1/system/ -run TestFile -count=1 绿（cap=100 行为等价）
- internal/utils/pagination.go 不存在；全仓零 ParsePagination/BuildPaginationResponse utils 版引用
- 102-PAGI-CALLER-AUDIT.md 落盘
</verification>

<success_criteria>
PAGI-01 交付完成：分页归一唯一走 pkg/query.NormalizePaginationWithMax（cap=100 行为等价、分叉注释自证）、utils/pagination.go 整文件删除不留兼容壳、逐调用方核对清单落盘、全仓 build/test 绿。分页侧无新增守护（D-102 口径：文件删除即终结，重引入属 code review 范畴）。
</success_criteria>

<output>
Create `.planning/phases/102-mechanical-constants/102-05-SUMMARY.md` when done
</output>
