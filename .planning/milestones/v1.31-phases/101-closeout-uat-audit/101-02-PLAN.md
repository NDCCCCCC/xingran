---
phase: 101-closeout-uat-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/101-closeout-uat-audit/UAT-RUNBOOK.md
  - .planning/workstreams/milestone/phases/62-ai-internal-core-db/62-HUMAN-UAT.md
  - .planning/phases/101-closeout-uat-audit/101-02-SUMMARY.md
autonomous: true
requirements: [UAT62-01, UAT62-02, UAT62-03]
user_setup:
  - service: postgresql
    why: "UAT62-01/02 需真实 PG 环境人工执行（2026-09-07 探针：本机无 docker、无 psql、无本地 PG 服务；config.yaml PG 指向 Supabase 远端且 internal/core/db/database.go 记载 pooler 上 AutoMigrate 有卡死风险）"
    dashboard_config:
      - task: "提供一次性试验 PG（docker run postgres:16 或内网 PG 实例均可），并给出连接串（host/port/user/password/dbname）"
        location: "用户本机/内网，无需云服务"

must_haves:
  truths:
    - "UAT-RUNBOOK.md 存在且 3 场景各有可直接执行的自动化资产（SQL/启动命令/逐字 grep 断言/DB 断言）"
    - "场景 1/2 人工边界显式标注（[HUMAN EXECUTES]），executor 不谎报 passed（D-101-3）"
    - "场景 3 在 sqlite 空库口径完成全自动证据链（或如实降级 pending）"
    - "62-HUMAN-UAT.md 每个 passed 回写都附带逐字证据摘录；每个 pending 都带 runbook 引用"
  artifacts:
    - path: ".planning/phases/101-closeout-uat-audit/UAT-RUNBOOK.md"
      provides: "3 场景验证步骤清单 + 自动化前置资产（pre-R5 构造 SQL / 双实例步骤 / env 开关步骤）"
      min_lines: 80
    - path: ".planning/workstreams/milestone/phases/62-ai-internal-core-db/62-HUMAN-UAT.md"
      provides: "3 场景 result 回写（诚实口径）"
      contains: "UAT-RUNBOOK"
    - path: ".planning/phases/101-closeout-uat-audit/101-02-SUMMARY.md"
      provides: "执行记录与证据摘录索引"
  key_links:
    - from: "UAT-RUNBOOK.md 场景1 grep 断言"
      to: "internal/core/db/migrations/migration_176_reconciliation_physical_mv.go"
      via: "逐字日志串（检测到旧版本 MV schema:200 / 已重建:295 / 索引已就位:310 / 验证通过:340）"
      pattern: "检测到旧版本 MV schema"
    - from: "UAT-RUNBOOK.md 场景2 grep 断言"
      to: "internal/core/db/database.go"
      via: "逐字 WARN 串 database.go:811"
      pattern: "advisory-lock.*另一实例正在执行启动迁移"
    - from: "62-HUMAN-UAT.md result 行"
      to: "UAT-RUNBOOK.md"
      via: "pending 场景引用 runbook 章节"
      pattern: "UAT-RUNBOOK"
---

<objective>
v1.30 收口第二支：62-HUMAN-UAT 3 场景的自动化前置资产化（UAT-RUNBOOK.md）+ 场景 3 全自动执行 + 62-HUMAN-UAT.md 诚实回写。

Purpose: UAT62-01/02 需真实 PG（本机探针确认无 docker/psql/本地 PG）——按 D-101-3 保持 [pending] 并交付可执行 runbook；UAT62-03 全自动可跑，executor 直接执行并回写（D-101-2）。
Output: UAT-RUNBOOK.md + 62-HUMAN-UAT.md 回写 + 101-02-SUMMARY.md。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-closeout-uat-audit/101-CONTEXT.md
@.planning/workstreams/milestone/phases/62-ai-internal-core-db/62-HUMAN-UAT.md
@.planning/STATE.md

<interfaces>
<!-- 逐字断言锚点（executor 直接引用，勿自行改写文案） -->

migration_176_reconciliation_physical_mv.go（PG-only；sqlite 下 :81 打印「R5 MV 重写跳过(非 PostgreSQL 数据库)」直接跳过）:
- :200 Warnf `[迁移 176] 检测到旧版本 MV schema(缺 R5 标记列: %v),走 DROP+CREATE 升级路径`（缺列清单动态拼入）
- :295 Infof `[迁移] R5 reconciliation_normalized 已重建(双源 declared + 真物理链路)`
- :310 Infof `[迁移] R5 索引已就位`
- :340 Infof `[迁移] R5 reconciliation_normalized 验证通过: MV=%d 行, ...`
- :168 Infof（快路径对照）`[迁移] reconciliation_normalized 已存在且 R5 schema 完整,走 REFRESH CONCURRENTLY 快路径`
- R5 四标记列（:140-150 information_schema 检查集）：asset_username / physical_user_id / last_resolved_at / mv_refreshed_at

database.go（advisory lock）:
- :810-814 pg_try_advisory_lock(hashtext('xingran-migrations')) 单参变体；未获锁打印 :811 Warnf `[advisory-lock] 另一实例正在执行启动迁移,本实例跳过 175/176/202-205 迁移块` 后照常完成启动
- :950-960 releaseMigrationAdvisoryLock 同连接 pg_advisory_unlock(hashtext('xingran-migrations'))

init_data.go（admin 种子，双方言）:
- :253 bootstrapPassword 优先 os.Getenv("SYS_ADMIN_BOOTSTRAP_PASSWORD")，未设回退 admin123
- :288 Warnf `[安全告警] 管理员账户已使用出厂默认密码 admin123`（+ :290 提示设置 env）
- :294 Infof `默认管理员密码已从 SYS_ADMIN_BOOTSTRAP_PASSWORD 环境变量读取`
- :271 Salt 字段置空串（非 "default"；盐嵌入 $sm3$iterations$salt$hash 哈希串）

config.go:
- :340 显式环境变量绑定 {"database.dbname", "DB_NAME"} —— sqlite 模式下 DB_NAME 即库文件路径（throwaway 空库入口）
- 既有单测：internal/core/db/init_data_test.go TestCreateDefaultUser_EnvOverride / _FallbackDefault / _NoDefaultLiteralSalt / _Idempotent（salt≠default / admin123 回退 / env 覆盖三语义）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UAT-RUNBOOK.md 三场景验证手册</name>
  <files>.planning/phases/101-closeout-uat-audit/UAT-RUNBOOK.md</files>
  <action>创建 runbook，结构：§0 环境探针结论（2026-09-07：docker 不可用 / psql 缺失 / 无本地 PG / config.yaml database.type=sqlite / PG 配置指向 Supabase 远端且 database.go 记载 pooler AutoMigrate 卡死风险——结论：场景 1/2 需人工提供一次性试验 PG，场景 3 sqlite 口径全自动）；§1/§2/§3 三场景，每节含【前置条件】【自动化资产】【启动步骤】【日志 grep 断言（逐字，用上文 interfaces 锚点原文）】【DB 状态断言 SQL】。场景 1（UAT62-01）：编写完整可执行的 pre-R5 旧结构构造 SQL——步骤为 连接一次性试验库（禁用业务库）→ DROP MATERIALIZED VIEW IF EXISTS reconciliation_normalized CASCADE → CREATE MATERIALIZED VIEW reconciliation_normalized AS 从 migration_176 R5 SELECT（:237-300）剔除 4 个 R5 标记列及其专属 JOIN 列的最小子集（MV 名必须一致，这是复用资产，见 101-CONTEXT Specific Ideas）→ 附反向对照步骤（同库二次启动应走 :168 快路径无回退）。附 sqlite 跳过警示（:81）。场景 2（UAT62-02）：给两条路径——确定性变体（主推）：psql 会话 A 执行 pg_try_advisory_lock(hashtext('xingran-migrations')) 持锁 → 启动实例 B（同一 PG，端口错开：SERVER_PORT=9001 或复制 config 改 server.port，executor 执行时核对 internal/config/config.go 显式绑定表确认端口 env 键名）→ B 日志断言 :811 逐字 WARN 且启动完成 → 会话 A pg_advisory_unlock 释放；双实例变体（备选）：A/B 相隔约 5s 启动观察后启动者 WARN。残留检查 SQL：SELECT locktype, granted, pid FROM pg_locks WHERE locktype='advisory';（迁移结束后无残留行）。场景 3（UAT62-03）：记录 Task 2 已执行证据链 + 完整复跑步骤；附 PG 变体（可选）：salt 断言 SELECT username, salt FROM sys_user WHERE username='admin'; salt IS DISTINCT FROM 'default'。场景 1/2 节首放置显式 `[HUMAN EXECUTES]` 标记（提供 PG / 执行启动 / 采集日志三件事）。</action>
  <verify>
    <automated>grep -c "HUMAN EXECUTES" .planning/phases/101-closeout-uat-audit/UAT-RUNBOOK.md && grep -c "检测到旧版本 MV schema\|另一实例正在执行启动迁移\|出厂默认密码 admin123" .planning/phases/101-closeout-uat-audit/UAT-RUNBOOK.md</automated>
  </verify>
  <done>runbook 存在且 ≥80 行；HUMAN EXECUTES 标记 ≥2 处（场景 1/2）；三条关键逐字日志断言在文档中出现；含 pg_locks 残留检查与 information_schema 四列检查 SQL。</done>
</task>

<task type="auto">
  <name>Task 2: 场景 3（UAT62-03）sqlite 空库全自动执行</name>
  <files>.planning/phases/101-closeout-uat-audit/101-02-SUMMARY.md</files>
  <action>证据链三步，全部证据摘录（逐行日志）写入 101-02-SUMMARY.md。第一步 unit 级：`go test ./internal/core/db/ -run TestCreateDefaultUser -v` 4 个测试全过（env 覆盖 / admin123 回退 / salt≠"default" / 幂等）。第二步真实启动 run 1：设 `DB_NAME=<系统临时目录>/uat_empty_a.db`（sqlite 空库文件，config.go:340 绑定），不设 SYS_ADMIN_BOOTSTRAP_PASSWORD，`go run ./cmd/main.go` 启动至 HTTP listen 日志后停止——日志必须含 :288 `[安全告警] 管理员账户已使用出厂默认密码 admin123`；停止后删除临时库文件。第三步真实启动 run 2：换第二个全新空库文件 `DB_NAME=<临时目录>/uat_empty_b.db`，设随机强密码 `SYS_ADMIN_BOOTSTRAP_PASSWORD=<random>`——日志含 :294 `默认管理员密码已从 SYS_ADMIN_BOOTSTRAP_PASSWORD 环境变量读取` 且无 admin123 告警；停止后清理两个临时文件并确认 shell 环境无残留变量。注意：run 2 必须用全新空库（admin 已存在时种子幂等跳过，:294 不会出现）。若启动因 Redis 等外部依赖卡住：先排查是否为非致命降级（缓存初始化失败通常降级不阻断）；若确实无法启动，证据链降级为 unit tests only，Task 3 场景 3 保持 [pending] 并注明障碍（D-101-3 诚实纪律）。</action>
  <verify>
    <automated>go test ./internal/core/db/ -run TestCreateDefaultUser -count=1 && grep -c "安全告警\|环境变量读取" .planning/phases/101-closeout-uat-audit/101-02-SUMMARY.md</automated>
  </verify>
  <done>TestCreateDefaultUser 4 测试 exit 0；101-02-SUMMARY.md 含两次启动的日志证据摘录（默认凭据 WARN + env 覆盖 Infof），或含明确降级记录。</done>
</task>

<task type="auto">
  <name>Task 3: 62-HUMAN-UAT.md 诚实回写</name>
  <files>.planning/workstreams/milestone/phases/62-ai-internal-core-db/62-HUMAN-UAT.md</files>
  <action>按 D-101-3 回写三个场景 result 与 Summary（frontmatter status: resolved 保持不变）。场景 1/2（UAT62-01/02）：result 保持 `[pending]`，各追加一行 `自动化前置: 已就绪 → .planning/phases/101-closeout-uat-audit/UAT-RUNBOOK.md §N（2026-09-07 环境探针: 本机无 docker/psql/本地 PG，待人工提供试验 PG 执行）`——严禁标 passed。场景 3（UAT62-03）：仅当 Task 2 证据链完整时改 `result: [passed 2026-09-07]` 并引用两条日志摘录 + TestCreateDefaultUser 通过结论 + 口径注记 `（sqlite 空库口径; PG 变体步骤见 RUNBOOK §3）`；证据链降级时保持 [pending] + 同款 runbook 引用。Summary 段数字如实重算（total: 3 / passed: 实际 / pending: 3−passed），与三个 result 行严格一致。commit：subject 小写开头 `docs(101): uat62 runbook + scenario evidence writeback`，body 每行 ≤100 字符（含场景 1/2 仍 pending 的如实说明——若仍 pending 则 message 禁含「全部通过」类措辞），trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`；仅 add 本 plan files_modified 列出的 3 个文件；遇 index.lock 冲突稍候重试。</action>
  <verify>
    <automated>grep -c "result: \[pending\]" .planning/workstreams/milestone/phases/62-ai-internal-core-db/62-HUMAN-UAT.md && grep -c "UAT-RUNBOOK" .planning/workstreams/milestone/phases/62-ai-internal-core-db/62-HUMAN-UAT.md</automated>
  </verify>
  <done>每个非 pending 的 result 行都紧跟逐字证据引用；每个 pending 行都带 runbook 引用；Summary 数字与 result 行一致；commit 入库且工作树无本 plan 之外的意外暂存。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor 启动命令 → 开发机数据库 | DB_NAME 指向错误可能覆盖开发者现有 sqlite 库 |
| runbook SQL → 人工 PG 实例 | 构造/重建 MV 的 DDL 若误连业务库会破坏数据 |
| 启动日志 → planning 文档 | 日志摘录可能含连接串/凭据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-03 | Tampering | Task 2 启动步骤 | mitigate | 强制 DB_NAME 指向系统临时目录新文件（config.go:340 显式绑定），禁用开发者现有库文件；执行后删临时文件；禁 SKIP_AUTOMIGRATE 旁路 |
| T-101-04 | Tampering | runbook 场景 1/2 DDL | mitigate | runbook 节首强制「一次性试验库」红线（禁业务库/禁 Supabase 生产库），pre-R5 构造仅作用于 reconciliation_normalized 这一个 MV 对象 |
| T-101-05 | Information Disclosure | 日志证据摘录 | mitigate | 摘录入 SUMMARY/回写前人工过目，剔除 DSN/password/token 类片段；断言串本身无敏感信息 |
| T-101-SC | Tampering | 包安装 | accept | 本 plan 零包安装，slopcheck 不适用 |
</threat_model>

<verification>
- UAT-RUNBOOK.md 存在，3 场景各有可执行资产，场景 1/2 带 [HUMAN EXECUTES]
- 场景 3 证据链完整（unit 4 测试 + 两次启动日志摘录）或如实降级记录
- 62-HUMAN-UAT.md：无证据不标 passed；pending 均带 runbook 引用；Summary 数字自洽
- 临时 DB 文件与 env 变量清理完毕（`git status` 无临时文件残留）
</verification>

<success_criteria>
1. UAT62-01/02 自动化前置 100% 就绪（runbook 资产可直接执行），人工边界显式（D-101-2/D-101-3）
2. UAT62-03 回写完成：passed 带证据 + 口径注记，或 pending 带降级原因——零谎报
3. 62-HUMAN-UAT.md 成为 milestone audit 可信输入（D-101-5）
4. 场景 1 的 pre-R5 构造 SQL 可复用（后续 MV 演进 UAT 资产）
</success_criteria>

<output>
Create `.planning/phases/101-closeout-uat-audit/101-02-SUMMARY.md` when done
</output>
