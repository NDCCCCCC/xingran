---
phase: 92-p2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/services/base/cache_service_base.go
  - internal/services/base/cache_provider.go
  - internal/services/base/cache_functions.go
  - internal/services/base/cache_service_base_test.go
  - internal/services/system/cache_provider.go
  - internal/services/system/cache_utils.go
  - internal/services/cache_config_service.go
autonomous: true
requirements: [CACHE-UNIFY-01, CACHE-UNIFY-05]

must_haves:
  truths:
    - "internal/services/base 包提供缓存抽象全套：TTLResolver 接口 + CacheServiceBase 薄基类 + CacheProvider 9 方法接口 + NoOpCacheProvider + CacheStats/CacheEntry + 泛型函数族 GetOrSetJSON[T]/SetJSON[T]/Invalidate/InvalidatePattern（D-01/D-02/D-03/D-04）"
    - "base 包保持零依赖方向：只 import context/time/pkg/logger（禁止 import internal/services 与 internal/services/system，root→base 才是合法方向，root 现有 18 文件走此方向实证无环）"
    - "system/cache_provider.go 原位留 4 个 type alias（CacheProvider/NoOpCacheProvider/CacheStats/CacheEntry = base 同名类型）+ CacheServiceBase alias，20+ 外部消费者文件（api/router.go、6 处路由 NoOp fallback、duty/knowledge/network/workorder 测试的 var _ 编译期断言、core.go 装配）零改动编译通过（D-02）"
    - "CacheServiceBase.Config 字段类型从 *services.CacheConfigService 变为 TTLResolver 接口后，nil config（typed-nil 或 untyped nil）不 panic：CacheConfigService.GetDurationWithDefault 顶部有 nil-receiver 防护返回 default（Pitfall 1，~100 处 nil-config 测试构造受保护）"
    - "GetOrSetJSON 是薄包装：委托 p.GetOrSet，P0 #9 同步写语义（data_cache_service.go:99-107）与 JSON 往返语义逐字保留，禁止重写 Get/Set 逻辑（Shared Pattern 3 红线）"
    - "miniredis 双装配集成测试覆盖：命中/穿透（query 调用计数）、mr.FastForward TTL 过期、Invalidate/InvalidatePattern 失效、NoOp 透传、GetStats 读取、nil-config → default 分支（D-09 / CACHE-UNIFY-05）"
  artifacts:
    - path: internal/services/base/cache_service_base.go
      provides: "TTLResolver 接口 + CacheServiceBase{Config TTLResolver} + GetExpiration"
      contains: "type TTLResolver interface"
    - path: internal/services/base/cache_provider.go
      provides: "CacheProvider 9 方法接口 + NoOpCacheProvider + setValue + CacheStats + CacheEntry（自 system/cache_provider.go 逐字迁入）"
      contains: "func (n *NoOpCacheProvider) GetOrSet"
    - path: internal/services/base/cache_functions.go
      provides: "GetOrSetJSON[T] / SetJSON[T] / Invalidate / InvalidatePattern 泛型包级函数族（D-03/D-04）"
      contains: "func GetOrSetJSON[T any]"
    - path: internal/services/system/cache_provider.go
      provides: "5 个 type alias（含 CacheServiceBase）+ 编译期断言 var _ CacheProvider = (*NoOpCacheProvider)(nil)"
      contains: "type CacheProvider = base.CacheProvider"
    - path: internal/services/base/cache_service_base_test.go
      provides: "package base_test 双装配（MemoryCache + miniredis）测试 TestBase92_* 系列"
      contains: "TestBase92"
    - path: internal/services/cache_config_service.go
      provides: "GetDurationWithDefault nil-receiver 防护（typed-nil interface 防 panic）"
      contains: "if s == nil"
  key_links:
    - from: internal/services/system/cache_provider.go (alias 块)
      to: internal/services/base/cache_provider.go
      via: "type CacheProvider = base.CacheProvider type alias（同一类型，无适配层）"
      pattern: "type CacheProvider = base\\.CacheProvider"
    - from: internal/services/cache_config_service.go (GetDurationWithDefault)
      to: base.TTLResolver
      via: "*CacheConfigService 隐式满足 TTLResolver（方法签名逐字一致）"
      pattern: "GetDurationWithDefault\\(configKey string, defaultDuration time\\.Duration\\)"
    - from: base.GetOrSetJSON
      to: CacheProvider.GetOrSet
      via: "薄委托：dest 传 &result（泛型 T 指针），query 闭包适配 interface{} 签名"
      pattern: "p\\.GetOrSet\\(ctx, key, &result"
---

<objective>
把缓存抽象全套（TTLResolver 薄基类 + CacheProvider 全家 + 泛型包级函数族）抽取到 `internal/services/base` 单一权威位置，经 type alias 翻转实现 20+ 消费文件零改动迁移，并以 nil-receiver 防护排除 typed-nil panic 风险，miniredis 双装配测试锁定行为。

Purpose: CACHE-UNIFY-01 的达成载体（ROADMAP SC-1 实际形态 = 泛型函数族而非模板方法，D-03，Go method 不能有类型参数已编译实证）；本 plan 产出的 base 函数族是 92-02（system 29 处）与 92-03（operations + D-04 收尾）的迁移靶点。

Output: base 包 3 个新生产文件 + 1 个测试文件 + system 2 文件 alias 翻转 + cache_config_service.go 一行防护；全部既有测试 0 回归。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/92-p2/92-CONTEXT.md
@.planning/workstreams/milestone/phases/92-p2/92-PATTERNS.md
@.planning/workstreams/milestone/phases/92-p2/92-RESEARCH.md

# 迁出源（D-01/D-02 逐字搬迁对象）
@internal/services/system/cache_utils.go
@internal/services/system/cache_provider.go

# base 包现状（零依赖基线 + 契约锁测试风格参照）
@internal/services/base/service.go
@internal/services/base/service_test.go

# TTLResolver 隐式实现端（nil-receiver 防护落点 :392）
@internal/services/cache_config_service.go

# miniredis 双装配测试模板（D-09 照抄装配与纪律头部）
@internal/services/data_cache_service_79_01_test.go

<interfaces>
<!-- 迁移后 system/cache_provider.go 的 alias 形态（D-02，Go spec alias = 同一类型） -->

type CacheProvider = base.CacheProvider
type NoOpCacheProvider = base.NoOpCacheProvider
type CacheStats = base.CacheStats
type CacheEntry = base.CacheEntry
type CacheServiceBase = base.CacheServiceBase // 10 个嵌入点构造字面量 CacheServiceBase{Config: config} 零改动

// 编译期双保险
var _ CacheProvider = (*NoOpCacheProvider)(nil)

<!-- base 新增接口（D-01，consumer-defined）-->

type TTLResolver interface {
    GetDurationWithDefault(configKey string, defaultDuration time.Duration) time.Duration
}

type CacheServiceBase struct {
    Config TTLResolver // *services.CacheConfigService 隐式满足；nil 语义经 nil-receiver 防护保持
}

func (b *CacheServiceBase) GetExpiration(configKey string, defaultVal time.Duration) time.Duration

<!-- base 新增泛型函数族（D-03/D-04，签名即契约）-->

func GetOrSetJSON[T any](ctx context.Context, p CacheProvider, key string, ttl time.Duration, query func() (T, error)) (T, error)
func SetJSON[T any](ctx context.Context, p CacheProvider, key string, value T, ttl time.Duration) error
func Invalidate(ctx context.Context, p CacheProvider, keys []string, module string)          // void + warn 日志（对齐现状）
func InvalidatePattern(ctx context.Context, p CacheProvider, patterns []string, module string) // void + warn 日志
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 base 缓存抽象包三文件（D-01/D-02/D-03/D-04）</name>
  <files>internal/services/base/cache_service_base.go, internal/services/base/cache_provider.go, internal/services/base/cache_functions.go</files>
  <read_first>
    - internal/services/system/cache_utils.go（CacheServiceBase + GetExpiration 迁出源 :50-61）
    - internal/services/system/cache_provider.go（CacheProvider 9 方法 + NoOp + setValue + CacheStats/CacheEntry 逐字搬迁源，全文 129 行）
    - internal/services/base/service.go（base 包零依赖 import 基线 + "有意设计"注释风格先例 :11-21）
    - .planning/workstreams/milestone/phases/92-p2/92-PATTERNS.md（Pattern Assignments 三节给出逐字目标形态）
    - internal/services/data_cache_service.go:99-107（P0 #9 同步写语义锚点——薄包装红线）
  </read_first>
  <action>
    创建 cache_service_base.go：package base；import 仅 time。定义 TTLResolver 接口（唯一方法 GetDurationWithDefault(configKey string, defaultDuration time.Duration) time.Duration，签名与 internal/services/cache_config_service.go:392 逐字一致）；定义 CacheServiceBase struct（唯一字段 Config TTLResolver）；实现 GetExpiration 方法（b.Config != nil 时委托 b.Config.GetDurationWithDefault，否则返回 defaultVal——与 cache_utils.go:56-61 语义逐字一致）。文件头注释说明 D-01 依赖倒置决策（consumer-defined interface，base 保持零依赖，CacheConfigService 隐式满足）。

    创建 cache_provider.go：package base；import context/reflect/time。把 system/cache_provider.go 全文内容迁入——CacheProvider 接口（9 方法 GetOrSet/Delete/DeleteByPattern/MGet/MDelete/Exists/SetTTL/GetTTL/GetStats，注释逐字保留）、NoOpCacheProvider 全部 9 方法、setValue 反射函数、CacheStats、CacheEntry。**逐字搬迁，不改任何方法签名、字段名、注释语义**（D-02 锁定 9 方法enumeration，禁止顺手加 Set 方法）。setValue 的反射"AssignableTo 不成立时静默跳过"缺陷原样保留（Pitfall 5：泛型函数族从调用侧绕开它，NoOp 本体不动）。

    创建 cache_functions.go：package base；import context/time/pkg/logger。实现四个包级函数：
    (1) GetOrSetJSON[T any]——签名见 frontmatter interfaces 块；体内 var result T，err := p.GetOrSet(ctx, key, &result, ttl, func() (interface{}, error) { return query() })，返回 result, err。**薄委托红线**：不重写 Get/Set/JSON/同步写逻辑（Shared Pattern 3）。文档注释说明"命中反序列化，未命中执行 query 并同步写缓存，语义与 provider.GetOrSet 逐字一致"。
    (2) SetJSON[T any]——CacheProvider 接口无原生 Set（D-02 锁定 9 方法），实现为 Delete(ctx, key) 成功后 p.GetOrSet(ctx, key, &value-marshaled-dest, ttl, 恒返回 value 的闭包) 的组合写（先删后写=覆盖语义；中间窗口读者只会 miss 回源，无脏读）。文档注释明确说明该组合语义及"接口扩展 native Set 属 D-02 范围外"（A7：当前 32 处调用点零个 Set 使用者，本函数为 D-03 函数族完备性成员）。
    (3) Invalidate(ctx, p, keys, module)——void 返回；p == nil 时 logger.Debugf("[%s] 未配置缓存提供者，跳过缓存清理", module) 后 return；否则循环 keys 逐个 p.Delete，失败时 logger.Warnf("[%s] 清除缓存失败: key=%s, error=%v", module, key, err)（统一日志格式取信息量大的 pattern/key 字段版，对齐 operations/cache_invalidator.go:41 格式；discretion 区已定）。
    (4) InvalidatePattern(ctx, p, patterns, module)——同构，循环 p.DeleteByPattern，Warnf 带 pattern=%s 字段。

    禁止：import internal/services 或 internal/services/system（会引入反向依赖，破坏 base 零依赖基线；base 现仅依赖 gorm + pkg/errors，cache 三文件只允许 context/time/reflect/pkg/logger）。
  </action>
  <verify>
    <automated>go build ./internal/services/base/ && go vet ./internal/services/base/</automated>
  </verify>
  <acceptance_criteria>
    - 三个新文件存在且 package base
    - grep -c "type TTLResolver interface" internal/services/base/cache_service_base.go == 1
    - grep -c "func GetOrSetJSON\[T any\]" internal/services/base/cache_functions.go == 1
    - grep -c "func InvalidatePattern\|func Invalidate(" internal/services/base/cache_functions.go == 2
    - grep -rn "xingran-go-backend/internal/services\"" internal/services/base/*.go 无匹配（零反向依赖）
    - grep -c "GetOrSet(" internal/services/base/cache_provider.go >= 3（接口 + NoOp 实现）
    - go build ./... 0 错误（新文件尚未被引用，纯增量编译）
  </acceptance_criteria>
  <done>base 包三文件编译通过，含 TTLResolver/CacheServiceBase/CacheProvider 全家/泛型函数族，零反向依赖</done>
</task>

<task type="auto">
  <name>Task 2: alias 翻转 + CacheServiceBase 迁出 + nil-receiver 防护（D-02 + Pitfall 1）</name>
  <files>internal/services/system/cache_provider.go, internal/services/system/cache_utils.go, internal/services/cache_config_service.go</files>
  <read_first>
    - internal/services/system/cache_provider.go（当前全文，改造对象）
    - internal/services/system/cache_utils.go（CacheServiceBase 定义迁出对象 :50-61；InvalidateCacheByPattern/ByKey :63-79 本 plan 保留）
    - internal/services/cache_config_service.go:385-401（GetDurationWithDefault 现状，防护落点 :392-394）
    - .planning/workstreams/milestone/phases/92-p2/92-PATTERNS.md（Shared Pattern 1 nil-receiver 防护 + Pattern 2 alias 翻转 + alias 后零改动消费者实证清单）
  </read_first>
  <action>
    (1) system/cache_provider.go：删除全部类型与方法定义（CacheProvider 接口、NoOpCacheProvider 及其方法、setValue、CacheStats、CacheEntry——已迁 base），原位写入 4 个 type alias（CacheProvider/NoOpCacheProvider/CacheStats/CacheEntry = base 对应类型）+ import "github.com/xingran-next/xingran-go-backend/internal/services/base" + 编译期断言 var _ CacheProvider = (*NoOpCacheProvider)(nil)。文件头注释说明 D-02 alias 策略（alias = 同一类型，20+ 消费文件零改动；alias 后续可删）。

    (2) system/cache_utils.go：删除 CacheServiceBase struct 定义与 GetExpiration 方法（:50-61，已迁 base）。在同文件（或 cache_provider.go alias 块，二选一保持同包一致即可）写入 type CacheServiceBase = base.CacheServiceBase——10 个嵌入点（8 个 system impl + operations/floor_cache_impl）的字面量构造 CacheServiceBase{Config: config} 与 s.GetExpiration(...) 调用零改动。**InvalidateCacheByPattern / InvalidateCacheByKey（:63-79）本 plan 原样保留不删**（它们的调用面 42 处由 92-02/92-03 逐步改写，删除动作在 92-03 Task 2 与最后一个调用点改写同 commit 完成）；仅在两函数注释前追加一行"// Phase 92-03 待删除：失效底层已统一至 base.Invalidate/base.InvalidatePattern（D-04）"。filterSlice/contains/paginate 三个工具函数不动（impl 仍在用）。

    (3) internal/services/cache_config_service.go GetDurationWithDefault（:392）：函数体首行插入 nil-receiver 防护：if s == nil { return defaultDuration }（注释注明 typed-nil interface 防护，Pitfall 1）。此防护使两条路径行为与现状完全一致：(a) ~100 处测试构造传 nil config（现状 Config 为具体指针，nil 判空有效；接口化后 typed-nil 使 if b.Config != nil 失效，无防护则 s.mu.RLock() panic）；(b) 92-03 将把 DataCacheService.GetExpiration 委托到 base（s.cacheConfig 未 SetCacheConfig 时为 nil 具体指针 → 装入接口成 typed-nil）。

    (4) 全量编译验证 alias 零改动承诺：go build ./... 必须一次通过——api/router.go、6 处 NoOp fallback 路由、duty/knowledge/network/workorder 测试的 var _ systemServices.CacheProvider 断言、system.WithCacheProvider（user_sync_service.go:46）、core.go 装配等 20+ 消费文件均不应有任何改动。若出现编译错误说明 Task 1 迁移有签名漂移，回到 Task 1 修（禁止改消费者文件——那是 D-02 alias 承诺的破坏）。
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/services/system/ ./internal/services/base/</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "type CacheProvider = base.CacheProvider" internal/services/system/cache_provider.go == 1
    - grep -c "type CacheServiceBase = base.CacheServiceBase" internal/services/system/ == 1（恰一处 alias）
    - grep -c "type CacheServiceBase struct" internal/services/system/cache_utils.go == 0（定义已删）
    - grep -A2 "func (s \*CacheConfigService) GetDurationWithDefault" internal/services/cache_config_service.go 包含 "if s == nil"
    - git diff --stat 中 internal/api/ 与 internal/core/ 与 duty/knowledge/network/workorder 零改动（alias 零改动承诺）
    - go build ./... 0 错误；go test ./internal/services/system/ ./internal/services/operations/ 全绿（嵌入点 + 跨包嵌入点回归）
  </acceptance_criteria>
  <done>alias 翻转完成且全部消费者零改动编译通过；nil-receiver 防护就位；既有 system/operations 测试 0 回归</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: base/cache_service_base_test.go 双装配测试（CACHE-UNIFY-05 / D-09）</name>
  <files>internal/services/base/cache_service_base_test.go</files>
  <behavior>
    - GetOrSetJSON 命中：预置缓存键后调用，query 闭包调用计数 == 0，返回缓存值
    - GetOrSetJSON 穿透：空缓存调用，query 调用计数 == 1，返回 query 结果且值已写入缓存（第二次调用 query 计数不再增长）
    - GetOrSetJSON 错误透传：query 返回 error 时返回同 error，不写缓存
    - TTL 过期：写入后 miniredis mr.FastForward(ttl + 1s)，再调用触发穿透（query 计数 == 2）
    - Invalidate：预置 N 个键，Invalidate 后全部 miss（Exists == false）
    - InvalidatePattern：预置匹配 pattern 的键 + 不匹配键，调用后匹配键失效、不匹配键保留
    - Invalidate/InvalidatePattern nil provider：不 panic，静默返回（D-04 nil 防护）
    - NoOp 透传：NoOpCacheProvider 下 GetOrSetJSON 每次调用都执行 query 且不写缓存；且 T 为指针类型时结果正确回填（Pitfall 5：**PageResult 形态经 &result 双指针 assignable 成立，不再静默丢零值——单向改善，断言之**）
    - GetStats：经 miniredis 装配调用 p.GetStats(ctx) 返回非 nil CacheStats
    - GetExpiration nil-config 分支：CacheServiceBase{}（零值）.GetExpiration 返回 defaultVal；CacheServiceBase{Config: (*services.CacheConfigService)(nil)}（typed-nil）同样返回 defaultVal 不 panic（Pitfall 1 防护端到端验证）
  </behavior>
  <read_first>
    - internal/services/data_cache_service_79_01_test.go（miniredis 双装配模板 :58-70 + 头部纪律块 :3-24 照抄格式）
    - internal/services/base/service_test.go（契约编号 + 纪律头部的 Phase 91 风格）
    - internal/pkg/cache/ 或 pkg/cache 的 MemoryCache 构造（cache_infra_test.go 同款纯进程内装配；参照 internal/services/system/cache_infra_test.go 的 MemoryCache 分支）
    - internal/services/system/adapter.go（NewCacheProvider 装配入口——测试经它拿到 provider，同时回归 Adaptee 链）
  </read_first>
  <action>
    新建 internal/services/base/cache_service_base_test.go，**package base_test（外部测试包——测试需 import internal/services 与 internal/services/system 做装配，in-package base 会成环：system→root→base）**。

    装配 helper 两个（名字带 92 后缀防同目录重名，R5 纪律）：
    - newBase92Redis(t *testing.T) (base.CacheProvider, *miniredis.Miniredis)：miniredis.RunT → cache.NewRedisCache(&cache.CacheConfig{Host, Port}, "") → system.NewCacheProvider(services.NewDataCacheService(rc))，t.Cleanup 单次 Close（照抄 data_cache_service_79_01_test.go:58-70）。
    - newBase92Memory(t *testing.T) base.CacheProvider：pkg/cache MemoryCache → NewDataCacheService → system.NewCacheProvider。

    测试函数统一前缀 TestBase92_（与 Phase 91 TestBase91 命名系一致），覆盖 `<behavior>` 全部 10 条。纪律：文件头照抄 79_01 的纪律注释块（改注 Phase 92-01）；禁 t.Parallel()（miniredis 实例 + 后台 goroutine）；TTL 推进一律 mr.FastForward 禁裸 time.Sleep。

    NoOp 透传用例直接 base.NoOpCacheProvider{} 构造（不经 adapter）；typed-nil 用例直接构造 base.CacheServiceBase{Config: (*services.CacheConfigService)(nil)} 断言 GetExpiration 返回 default 不 panic。
  </action>
  <verify>
    <automated>go test ./internal/services/base/ -run TestBase92 -v</automated>
  </verify>
  <acceptance_criteria>
    - go test ./internal/services/base/ -run TestBase92 -v 全部 PASS（10+ 用例）
    - grep -c "TestBase92" internal/services/base/cache_service_base_test.go >= 8
    - grep -c "FastForward" internal/services/base/cache_service_base_test.go >= 1 且 grep -c "time.Sleep" == 0
    - grep -c "t.Parallel" internal/services/base/cache_service_base_test.go == 0
    - go test ./internal/services/... 全绿（含既有 79_01 双装配回归）
  </acceptance_criteria>
  <done>CACHE-UNIFY-05 的 base 侧测试面完成：泛型函数族 + 失效 + NoOp + nil-config 全部有自动化断言，进 CI gate</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 应用 → Redis | 缓存键与缓存值经 base 函数族流入 Redis（键构造本期逐字不变） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01 | Tampering | 缓存键构造（GetOrSetJSON 的 key 参数） | mitigate | 本 plan 仅建抽象不迁调用点，键构造零触碰；92-02/92-03 迁移时以 git diff 逐字校验 key 字符串零变更（Shared Pattern 2），invariants 测试（92-04）兜底 |
| T-92-02 | Tampering | 失效路径（Invalidate/InvalidatePattern） | mitigate | nil 防护 + warn 日志对齐现状语义逐字保留；92-01 本 plan 不删旧失效函数（原路径继续工作），双轨期无失效真空窗口 |
| T-92-SC | Tampering | npm/pip/cargo 安装 | accept | 本 phase 零新依赖（92-RESEARCH § Package Legitimacy Audit：无新增包），无供应链面 |
</threat_model>

<verification>
- go build ./... 0 错误（每个 task 后必跑，CLAUDE.md 强制）
- go test ./internal/services/base/ ./internal/services/system/ ./internal/services/operations/ 全绿（quick 三包，<120s）
- go test ./internal/services/... 全绿（SC-4 口径）
- alias 零改动承诺：git diff 不含 internal/api/、internal/core/、duty/knowledge/network/workorder 任何文件
</verification>

<success_criteria>
1. base 包新增 TTLResolver + CacheServiceBase + CacheProvider 全家 + 泛型函数族，零反向依赖
2. system 侧 5 个 type alias 翻转完成，20+ 消费文件零改动编译通过
3. nil-receiver 防护就位，nil-config 测试构造（~100 处）全数安全
4. TestBase92_* 系列双装配测试全绿（miniredis + MemoryCache，FastForward 纪律）
5. 既有测试 0 回归（operlog/status AST 锁值防线保持绿）
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/92-p2/92-01-SUMMARY.md` when done
</output>
