---
phase: "90"
plan: "03"
type: "execute"
wave: 3
depends_on: ["90-01"]
files_modified:
  - "internal/services/ad_ldap_client.go"
  - "internal/api/v1/ws_notice_handler.go"
  - "internal/scheduler/ad_sync_tasks.go"
  - "internal/scheduler/cron.go"
autonomous: true
requirements:
  - "TIMEOUTS-05"
  - "TIMEOUTS-06"
  - "TIMEOUTS-07"
  - "TIMEOUTS-08"

must_haves:
  truths:
    - "ad_ldap_client.go:74 replaces time.Second*30 with constants.LDAPConnTimeout"
    - "ws_notice_handler.go:47 replaces \"http://\"+host and \"https://\"+host with constants.HTTPProto+\"://\"+host and constants.HTTPSProto+\"://\"+host"
    - "ad_sync_tasks.go:42 replaces adSchedulerSyncTimeout with constants.ADSyncTimeout (renamed per D-08)"
    - "ad_sync_tasks.go:164 replaces inline 1*time.Minute with constants.ADSyncTaskTimeout"
    - "cron.go:18 replaces defaultShutdownTimeout with constants.SchedulerShutdownTimeout (renamed per D-08)"
    - "go build ./... 0 errors"
    - "go test ./... 0 failures"
  artifacts:
    - path: "internal/services/ad_ldap_client.go"
      provides: "LDAP connection timeout migrated"
      line: 74
    - path: "internal/api/v1/ws_notice_handler.go"
      provides: "WS origin protocol string literals migrated"
      line: 47
    - path: "internal/scheduler/ad_sync_tasks.go"
      provides: "AD sync timeout constants renamed + ADSyncTaskTimeout added"
      line: "42, 164"
    - path: "internal/scheduler/cron.go"
      provides: "Scheduler shutdown timeout renamed"
      line: 18
  key_links:
    - from: "ad_ldap_client.go"
      to: "pkg/constants/timeouts.go"
      via: "import + constants.LDAPConnTimeout"
      pattern: "constants\\.LDAPConnTimeout"
    - from: "ws_notice_handler.go"
      to: "pkg/constants/protocol.go"
      via: "import + constants.HTTPProto / constants.HTTPSProto"
      pattern: "constants\\.HTTPProto|constants\\.HTTPSProto"
    - from: "ad_sync_tasks.go"
      to: "pkg/constants/timeouts.go"
      via: "import + constants.ADSyncTimeout"
      pattern: "constants\\.ADSyncTimeout|constants\\.ADSyncTaskTimeout"
    - from: "cron.go"
      to: "pkg/constants/timeouts.go"
      via: "import + constants.SchedulerShutdownTimeout"
      pattern: "constants\\.SchedulerShutdownTimeout"
---

<objective>
Migrate LDAP client, WS notice handler, and scheduler cron constants to reference the new constants package. Replace 1 LDAP timeout + 1 WS protocol pair + 3 scheduler timeout constants (including 2 renames).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@internal/services/ad_ldap_client.go (lines 65-85)
@internal/api/v1/ws_notice_handler.go (lines 35-60)
@internal/scheduler/ad_sync_tasks.go (lines 35-50, 158-175)
@internal/scheduler/cron.go (lines 10-30)
@pkg/constants/timeouts.go
@pkg/constants/protocol.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ad_ldap_client.go (LDAPConnTimeout)</name>
  <files>internal/services/ad_ldap_client.go</files>
  <action>
Read internal/services/ad_ldap_client.go before editing.

**Changes needed:**

1. Add import for constants package: `"github.com/xingran-next/xingran-go-backend/pkg/constants"`

2. Line 74: Replace `time.Second * 30` with `constants.LDAPConnTimeout`.

Current: `c.conn.SetTimeout(time.Second * 30)`
After: `c.conn.SetTimeout(constants.LDAPConnTimeout)`

Note: constants.LDAPConnTimeout is already a time.Duration (30 * time.Second), so SetTimeout accepts it directly with no type conversion needed.

After the change, verify: `go build ./internal/services/ad_ldap_client.go`
  </action>
  <verify>
    <automated>go build ./internal/services/ad_ldap_client.go && echo "ad_ldap_client builds"</automated>
  </verify>
  <done>ad_ldap_client.go:74 uses constants.LDAPConnTimeout, no inline time.Second*30 literal remains</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ws_notice_handler.go (HTTPProto/HTTPSProto)</name>
  <files>internal/api/v1/ws_notice_handler.go</files>
  <action>
Read internal/api/v1/ws_notice_handler.go before editing.

**Changes needed:**

1. Add import for constants package if not already present.

2. Line 47: Replace protocol string literals with constants.

Current:
```go
if strings.HasPrefix(origin, "http://"+host) || strings.HasPrefix(origin, "https://"+host) {
```

After:
```go
if strings.HasPrefix(origin, constants.HTTPProto+"://"+host) || strings.HasPrefix(origin, constants.HTTPSProto+"://"+host) {
```

Per D-03: no helper function — bare const concatenation is used directly (consistent with Phase 89 leaf const philosophy).

After the change, verify: `go build ./internal/api/v1/ws_notice_handler.go`
  </action>
  <verify>
    <automated>go build ./internal/api/v1/ws_notice_handler.go && echo "ws_notice_handler builds"</automated>
  </verify>
  <done>ws_notice_handler.go:47 uses constants.HTTPProto and constants.HTTPSProto in bare concatenation; no inline "http://"/"https://" string literals remain in this line</done>
</task>

<task type="auto">
  <name>Task 3: Migrate scheduler/ad_sync_tasks.go (ADSyncTimeout rename + ADSyncTaskTimeout)</name>
  <files>internal/scheduler/ad_sync_tasks.go</files>
  <action>
Read internal/scheduler/ad_sync_tasks.go before editing.

**Changes needed (2 locations):**

1. Lines 41-42: Rename local const adSchedulerSyncTimeout to use constants.ADSyncTimeout.

Current:
```go
// adSchedulerSyncTimeout AD调度器同步任务超时
const adSchedulerSyncTimeout = 30 * time.Minute
```

After — remove the local const declaration and replace all uses of adSchedulerSyncTimeout in this file with constants.ADSyncTimeout:
```go
// ADSyncTimeout (imported from pkg/constants/timeouts.go)
```

Search for all uses of adSchedulerSyncTimeout in the file first, then remove the const declaration and replace each use.

2. Line 164: Replace inline `1*time.Minute` with constants.ADSyncTaskTimeout.

Current:
```go
ctx, cancel := context.WithTimeout(context.Background(), 1*time.Minute)
```

After:
```go
ctx, cancel := context.WithTimeout(context.Background(), constants.ADSyncTaskTimeout)
```

Note: ADSyncTaskTimeout is a time.Duration, so context.WithTimeout accepts it directly.

3. Add import for constants package if not already present.

After both changes, verify: `go build ./internal/scheduler/...`
  </action>
  <verify>
    <automated>go build ./internal/scheduler/... && echo "ad_sync_tasks builds"</automated>
  </verify>
  <done>ad_sync_tasks.go no longer declares local adSchedulerSyncTimeout; all references use constants.ADSyncTimeout; line 164 uses constants.ADSyncTaskTimeout instead of inline 1*time.Minute</done>
</task>

<task type="auto">
  <name>Task 4: Migrate scheduler/cron.go (SchedulerShutdownTimeout rename)</name>
  <files>internal/scheduler/cron.go</files>
  <action>
Read internal/scheduler/cron.go before editing.

**Changes needed:**

1. Add import for constants package if not already present.

2. Line 17-18: Rename local const defaultShutdownTimeout to use constants.SchedulerShutdownTimeout.

Current:
```go
// defaultShutdownTimeout 调度器关闭时等待任务完成的默认超时时间
const defaultShutdownTimeout = 5 * time.Second
```

After — remove the local const declaration and replace all uses of defaultShutdownTimeout in this file with constants.SchedulerShutdownTimeout.

Search for all uses of defaultShutdownTimeout in the file first, then remove the const declaration and replace each use.

3. After removing the local const, verify the file still references constants.SchedulerShutdownTimeout in the appropriate places (likely where defaultShutdownTimeout was used for graceful shutdown).

After the change, verify: `go build ./internal/scheduler/...`
  </action>
  <verify>
    <automated>go build ./internal/scheduler/... && echo "cron.go builds"</automated>
  </verify>
  <done>cron.go no longer declares local defaultShutdownTimeout; all references use constants.SchedulerShutdownTimeout</done>
</task>

<task type="auto">
  <name>Task 5: Full build and test regression</name>
  <files></files>
  <action>
Run full build and test suite:

```bash
go build ./...
go test ./... 2>&1 | tail -20
```

Confirm zero errors and zero failures across all packages.
  </action>
  <verify>
    <automated>go build ./... && go test ./... 2>&1 | tail -10 && echo "full regression complete"</automated>
  </verify>
  <done>go build ./... 0 errors, go test ./... 0 failures</done>
</task>

</tasks>

<verification>
go build ./...
go test ./... 2>&1 | tail -20
</verification>

<success_criteria>
1. ad_ldap_client.go:74 uses constants.LDAPConnTimeout
2. ws_notice_handler.go:47 uses constants.HTTPProto and constants.HTTPSProto
3. ad_sync_tasks.go no longer has local adSchedulerSyncTimeout; uses constants.ADSyncTimeout
4. ad_sync_tasks.go:164 uses constants.ADSyncTaskTimeout
5. cron.go no longer has local defaultShutdownTimeout; uses constants.SchedulerShutdownTimeout
6. go build ./... 0 errors
7. go test ./... 0 failures (1688+ test baseline intact)
</success_criteria>

<output>
Create .planning/milestones/v1.29-phases/90-timeouts-port-protocol-concurrency/90-03-SUMMARY.md when done
</output>
