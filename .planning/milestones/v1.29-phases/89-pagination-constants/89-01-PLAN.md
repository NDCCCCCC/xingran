---
phase: "89"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "pkg/constants/pagination.go"
  - "pkg/constants/pagination_test.go"
  - "pkg/query/pagination.go"
  - "internal/services/knowledge_service.go"
autonomous: true
requirements:
  - "PAGINATION-01"
  - "PAGINATION-08"
  - "PAGINATION-10"
  - "PAGINATION-11"

must_haves:
  truths:
    - "pkg/constants/pagination.go defines exactly 3 constants: DefaultCurrent=1, DefaultPageSize=10, MaxPageSize=200"
    - "pkg/query/pagination.go NormalizePagination(current, pageSize int) applies default and max cap"
    - "PaginationRequest.Normalize() delegates to NormalizePagination (no duplicate literal)"
    - "knowledge_service.go LineArticles path uses NormalizePagination"
    - "knowledge_service.go SearchKnowledgeArticles path uses NormalizePagination (replaces 100/500 cap)"
    - "AST lock test in pagination_test.go pins constant values"
    - "go build ./... 0 errors"
    - "go test ./pkg/query/... 0 failures"
  artifacts:
    - path: "pkg/constants/pagination.go"
      provides: "3 pagination constants (leaf const package)"
      contains: "DefaultCurrent = 1"
      contains: "DefaultPageSize = 10"
      contains: "MaxPageSize = 200"
    - path: "pkg/constants/pagination_test.go"
      provides: "AST lock test for constant values"
      exports: "TestPaginationConstantStability"
    - path: "pkg/query/pagination.go"
      provides: "NormalizePagination helper function"
      exports: "NormalizePagination"
      exports: "PaginationRequest.Normalize"
    - path: "internal/services/knowledge_service.go"
      provides: "pilot migration of 2 pagination code paths"
      line_count: "~330 lines"
  key_links:
    - from: "pkg/query/pagination.go"
      to: "pkg/constants/pagination.go"
      via: "import + NormalizePagination references constants"
      pattern: "constants\\.DefaultCurrent|constants\\.DefaultPageSize|constants\\.MaxPageSize"
    - from: "PaginationRequest.Normalize"
      to: "NormalizePagination"
      via: "method delegation"
      pattern: "NormalizePagination\\(p\\.Current, p\\.PageSize\\)"
---

<objective>
建立分页常量中央源 (pkg/constants/pagination.go 3 常量) + NormalizePagination helper + pilot 迁移 knowledge_service.go 两个代码路径。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@pkg/query/pagination.go
@internal/services/knowledge_service.go (lines 165-198, 480-505)
@internal/utils/operlog/regression_test.go (AST lock pattern)
@internal/models/status_constants_test.go (watched family scan pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pkg/constants/pagination.go (leaf const package)</name>
  <files>pkg/constants/pagination.go</files>
  <action>
Create a new leaf const package file at pkg/constants/pagination.go with exactly 3 constants:

```go
package constants

const (
    DefaultCurrent  = 1
    DefaultPageSize = 10
    MaxPageSize     = 200
)
```

No functions, no types — pure const block. This is the Single Source of Truth for all pagination defaults and caps in the project.
  </action>
  <verify>
    <automated>grep -c "^const (" pkg/constants/pagination.go && grep -c "DefaultCurrent  = 1" pkg/constants/pagination.go && grep -c "DefaultPageSize = 10" pkg/constants/pagination.go && grep -c "MaxPageSize     = 200" pkg/constants/pagination.go && echo "3 constants confirmed"</automated>
  </verify>
  <done>pkg/constants/pagination.go exists with exactly 3 constants (DefaultCurrent=1, DefaultPageSize=10, MaxPageSize=200), no extra constants</done>
</task>

<task type="auto">
  <name>Task 2: Create pkg/constants/pagination_test.go (AST lock test)</name>
  <files>pkg/constants/pagination_test.go</files>
  <action>
Create AST lock test following the operlog/regression_test.go pattern (expectedXXXValues map + TestXxxConstantStability function).

The test must:
1. Define `expectedPaginationValues = map[string]int{"DefaultCurrent": 1, "DefaultPageSize": 10, "MaxPageSize": 200}`
2. Parse pagination.go via go/ast to read actual constant values
3. Assert each constant's actual value matches expected value
4. Assert exactly 3 constants exist (count check)
5. Also assert no unexpected constants exist under this package

Reference: internal/utils/operlog/regression_test.go lines 42-105 (TestOperTypeConstantStability + readOperTypeConsts pattern).
  </action>
  <verify>
    <automated>cd pkg/constants && go test -v -run TestPaginationConstantStability && echo "AST lock test passes"</automated>
  </verify>
  <done>pagination_test.go passes with expectedPaginationValues map matching actual constants, count=3 check, bidirectional assertion</done>
</task>

<task type="auto">
  <name>Task 3: Add NormalizePagination helper + update PaginationRequest.Normalize() delegation</name>
  <files>pkg/query/pagination.go</files>
  <action>
Read pkg/query/pagination.go before editing.

Add to pkg/query/pagination.go:

1. Import path for constants package: `"github.com/xingran-next/xingran-go-backend/pkg/constants"`

2. New `NormalizePagination(current, pageSize int) (int, int)` function (pure function, value-returning):

```go
// NormalizePagination normalizes pagination parameters per industry best practice.
// Returns (normalizedCurrent, normalizedPageSize).
// Default: current=1, pageSize=10. Cap: pageSize <= MaxPageSize (200).
func NormalizePagination(current, pageSize int) (int, int) {
    if current <= 0 {
        current = constants.DefaultCurrent
    }
    if pageSize <= 0 {
        pageSize = constants.DefaultPageSize
    }
    if pageSize > constants.MaxPageSize {
        pageSize = constants.MaxPageSize
    }
    return current, pageSize
}
```

3. Update `PaginationRequest.Normalize()` method body to delegate:

```go
func (p *PaginationRequest) Normalize() {
    p.Current, p.PageSize = NormalizePagination(p.Current, p.PageSize)
}
```

The binding tag on line 33 (`binding:"min=1,max=100"`) is NOT changed here — that belongs to plan 89-02 (D-15).

IMPORTANT: Do NOT use a pointer receiver for NormalizePagination — it is a pure value function matching Go conventions (strings.TrimSpace / filepath.Clean).
  </action>
  <verify>
    <automated>cd pkg/query && go build . && go test -v -run TestPag8005 && echo "build + existing tests pass"</automated>
  </verify>
  <done>pkg/query/pagination.go exports NormalizePagination(current, pageSize int) (int, int), PaginationRequest.Normalize() delegates to it, no duplicate 1/10/100 literals in Normalize()</done>
</task>

<task type="auto">
  <name>Task 4: Pilot migration — knowledge_service.go (2 pagination code paths)</name>
  <files>internal/services/knowledge_service.go</files>
  <action>
Read internal/services/knowledge_service.go lines 165-198 and 480-505 before editing.

**Path 1 — LineArticles (lines 171-179):**
Replace the inline default logic at lines 171-178 with a call to the query package's NormalizePagination:

```go
current, pageSize := query.NormalizePagination(req.Current, req.PageSize)
offset := (current - 1) * pageSize
```

Remove the old `if current <= 0 { current = 1 }` and `if pageSize <= 0 { pageSize = 10 }` blocks.

**Path 2 — SearchKnowledgeArticles (lines 487-498):**
This path has special values (default=100, cap=500). Per D-08/D-09 (deleted), these are replaced with standard NormalizePagination (default=10, cap=200).

Replace lines 487-498 with:

```go
current, pageSize := query.NormalizePagination(req.PageNum, req.PageSize)
```

Note: req.PageNum maps to current, req.PageSize maps to pageSize. The old `if pageNum < 0 { pageNum = 0 }` guard is replaced by NormalizePagination's `<= 0 -> 1` behavior.

Remove the old `pageSize = 100` / `pageSize > 500` cap blocks entirely — NormalizePagination enforces max=200.

After both changes, verify the file still compiles: `go build ./internal/services/...`
  </action>
  <verify>
    <automated>go build ./internal/services/knowledge_service.go 2>&1 | head -5 && echo "knowledge_service.go compiles"</automated>
  </verify>
  <done>knowledge_service.go line 171-178 (LineArticles) uses NormalizePagination; line 487-498 (SearchKnowledgeArticles) uses NormalizePagination replacing 100/500 cap, no inline pageSize=10/100/500 literals remain</done>
</task>

</tasks>

<verification>
go build ./... must exit 0
go test ./pkg/constants/... -v
go test ./pkg/query/... -v
go build ./internal/services/knowledge_service.go
</verification>

<success_criteria>
1. pkg/constants/pagination.go defines 3 constants (DefaultCurrent=1, DefaultPageSize=10, MaxPageSize=200)
2. pkg/constants/pagination_test.go AST lock test passes (values pinned)
3. pkg/query/pagination.go exports NormalizePagination + PaginationRequest.Normalize() delegates to it
4. knowledge_service.go pilot migration complete (2 code paths, no inline literals)
5. go build ./... 0 errors
6. go test ./pkg/query/... 0 failures (existing Phase 80 tests still pass)
</success_criteria>

<output>
Create .planning/milestones/v1.29-phases/89-pagination-constants/89-01-SUMMARY.md when done
</output>
