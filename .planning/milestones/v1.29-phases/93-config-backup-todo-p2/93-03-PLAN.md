---
phase: 93
plan: 03
type: execute
wave: 3
depends_on: ["93-01", "93-02"]
files_modified:
  - internal/models/config_restore_task.go
  - internal/core/db/database.go
  - internal/core/db/migrations/migration_211_create_config_restore_task.go
  - internal/services/config_restore_task_service.go
autonomous: true
requirements: [BACKUP-CLOSED-03]

must_haves:
  truths:
    - "D-01: 恢复 = 把备份的设备配置下发到网络设备——StartRestore 编排调用 93-02 的 DeviceExecutor.RestoreConfig（非数据库 upsert 语汇）"
    - "D-04: service 层硬校验 backup.DeviceID == 请求 deviceID，不一致报 '备份不属于目标设备，仅限恢复到备份源设备' 并拒绝"
    - "D-08: 同设备互斥——创建任务前查询 device_id + status IN (pending,running)，命中报 '该设备存在进行中的恢复任务'（device_info_collection Enqueue 先例模式）"
    - "D-09: 恢复成功后插入版本链记录 models.ConfigBackup（BackupTypeManual + ChangeReason '恢复自版本 N' + ConfigHash=回读 hash），版本链可追溯恢复事件"
    - "D-10: runRestore 下发前先对目标设备自动创建备份（CreateBackup，ChangeReason '恢复前自动备份'），把高危操作变成可回退"
    - "D-11: 恢复后 executor.GetConfig 回读与备份 ConfigHash 比对，不一致仅 applogger.Warnf + hashMatched=false，restore 整体算成功"
    - "D-14: 恢复前备份失败即中止——任务 failed 且不触碰 RestoreConfig 下发"
    - "D-15: 独立任务表 sys_config_restore_task 存任务生命周期（status/进度/结果 JSON/错误信息），与备份版本链职责分离；Migrate211 + AutoMigrate 双线注册齐全"
    - "D-17（service 侧）: StartRestore 完成 参数校验 + 同设备/互斥校验 + 创建 pending 任务 + 返回任务记录（taskId 由 handler 93-04 返回）"
    - "D-20/D-21/D-22: 不做任务清理、不设全局并发上限（同设备互斥 + executor 调度器自然限流）、不做任务级重试（重新发起即完整流程）"
    - "D-34: 四态状态机 pending → running → success/failed，不支持取消；异步执行用 detached context（context.Background() + RestoreConfigTimeout），严禁透传 HTTP ctx"
    - "任务表设计惯例（CONTEXT 锁定）: 状态 string 常量（'pending'/'running'/'success'/'failed'）、UUID 主键 BeforeCreate hook（23502 防御）、AutoMigrate + MigrateNNN 双注册"
  artifacts:
    - path: internal/models/config_restore_task.go
      provides: "ConfigRestoreTask model 三段式（struct + BeforeCreate UUID + TableName）+ RestoreTaskStatusPending/Running/Success/Failed string 常量"
      contains: "func (ConfigRestoreTask) TableName() string"
    - path: internal/core/db/migrations/migration_211_create_config_restore_task.go
      provides: "Migrate211——PG 分支 CREATE TABLE IF NOT EXISTS sys_config_restore_task + device_id/status 索引，sqlite 分支空操作（AutoMigrate 已建）"
      contains: "sys_config_restore_task"
    - path: internal/core/db/database.go
      provides: "MigrateModelList 增 &models.ConfigRestoreTask{} + 双方言分支注册 Migrate211"
      contains: "&models.ConfigRestoreTask{}"
    - path: internal/services/config_restore_task_service.go
      provides: "ConfigRestoreTaskService：StartRestore/runRestore/GetRestoreTask/ListRestoreTasks/RecoverStaleRunningTasks"
      contains: "func (s *ConfigRestoreTaskService) StartRestore("
  key_links:
    - from: internal/services/config_restore_task_service.go runRestore
      to: internal/device/restore_config.go RestoreConfig
      via: "detached context 下调用唯一下发入口"
      pattern: "RestoreConfig\\(ctx"
    - from: internal/services/config_restore_task_service.go runRestore
      to: internal/services/config_backup_service.go CreateBackup
      via: "恢复前自动备份（D-10）"
      pattern: "恢复前自动备份"
    - from: internal/core/db/database.go
      to: internal/models/config_restore_task.go
      via: "AutoMigrate 注册（sqlite 缺表 family pattern 防线）"
      pattern: "ConfigRestoreTask\\{\\}"
---

# Plan 93-03: 恢复任务表 + ConfigRestoreTaskService 异步编排

<objective>
新增 `ConfigRestoreTask` model（string 四态状态机 + BeforeCreate UUID）+ Migrate211（双方言注册 + AutoMigrate 双线齐全）；`ConfigRestoreTaskService` 承载发起（同设备校验 + 互斥 + 建任务 + detached goroutine）与执行编排（恢复前自动备份→失败即中止→读内容→RestoreConfig 下发→回读 hash 警告不失败→版本链恢复记录→任务 success/failed + 启动时 running→failed 一次性收敛）。本 plan 只做 model + service 层；handler/路由/接线/stub 删除在 93-04，前端在 93-05，e2e 测试在 93-06——保持每 plan 单一职责且收口时 build 恒绿（RestoreBackup stub 原样保留至 93-04 同 commit 切换）。
</objective>

<decisions_covered>
D-01（恢复=设备下发，语义落地）· D-04（同设备校验）· D-08（同设备互斥，Enqueue 去重模式）· D-09（版本链恢复记录，复用 BackupTypeManual + ChangeReason="恢复自版本 N"）· D-10/D-14（恢复前自动备份，失败即中止）· D-11（hash 警告不失败）· D-15（任务表，Migrate211）· D-17（service 侧任务创建）· D-20（不做清理）· D-21（不设全局上限）· D-22（不设重试）· D-34（四态状态机无取消）· [informational] A5 discretion：启动时 running→failed 一次性收敛（状态机自洽，非清理 cron）
</decisions_covered>

<tasks>

<task id="93-03-01" type="execute">
<name>Task 1: ConfigRestoreTask model + Migrate211 双注册</name>
<files>internal/models/config_restore_task.go, internal/core/db/database.go, internal/core/db/migrations/migration_211_create_config_restore_task.go</files>
<read_first>
- internal/models/config_backup.go（三段式模板：struct + BeforeCreate UUID + TableName；string 枚举常量风格）
- internal/core/db/database.go（:511 附近 MigrateModelList 中 &models.ConfigBackup{} 行；:808-890 迁移注册双方言分支布局）
- internal/core/db/migrations/migration_210_normalize_menu_parent_id.go（MigrateNNN 骨架与 database.go 注册方式）
- .planning/workstreams/milestone/phases/93-config-backup-todo-p2/93-RESEARCH.md（migration 编号 Migrate211 占用核实 + AutoMigrate 注册点）
</read_first>
<action>
1. 新建 internal/models/config_restore_task.go：`ConfigRestoreTask` struct——ID(uuid pk)/DeviceID(uuid not null, index)/BackupID(uuid not null)/Status(string size:20 not null, index)/TotalLines(int)/SentLines(int)/FailedLine(text)/ResultJSON(text, 存 RestoreResult+hashMatched 序列化)/ErrorMessage(text)/StartedAt(*time.Time)/CompletedAt(*time.Time)/CreatedAt/UpdatedAt/CreatedBy(string size:64)；string 状态常量 `RestoreTaskStatusPending/Running/Success/Failed = "pending"/"running"/"success"/"failed"`；BeforeCreate UUID+CreatedAt hook（config_backup.go :51-64 模板，23502 防御）；`TableName() = "sys_config_restore_task"`。
2. database.go：MigrateModelList 中 `&models.ConfigBackup{}` 行后追加 `&models.ConfigRestoreTask{},`（AutoMigrate 线，sqlite/postgres 同函数生效）。
3. 新建 migration_211_create_config_restore_task.go：`func Migrate211(db *gorm.DB) error`，PG 分支 `CREATE TABLE IF NOT EXISTS sys_config_restore_task (...)`（uuid 主键 + 各列 + device_id/status 索引），sqlite 分支空操作（AutoMigrate 已建表，双方言守卫同 Migrate207/208 惯例）；在 database.go 的 PG 迁移注册序列（Migrate210 注册行后）与 sqlite 分支（:880 附近 Migrate210 行后）各追加一行非阻断调用（applogger.Errorf 风格与既有迁移一致）。
</action>
<acceptance_criteria>
- internal/models/config_restore_task.go 存在，含 4 个 RestoreTaskStatus* 常量（值为 "pending"/"running"/"success"/"failed"）、BeforeCreate hook、TableName 返回 "sys_config_restore_task"
- database.go 同时包含 `&models.ConfigRestoreTask{}` 与 Migrate211 注册（grep 双命中，PG + sqlite 双分支）
- migration_211 文件含 PG `CREATE TABLE IF NOT EXISTS sys_config_restore_task` 分支
- `go build ./...` exit 0
- `go test ./internal/core/db/ -count=1` exit 0（database_test.go 全量模型 AutoMigrate 测试捕获漏注册——必须含新 model 后仍绿）
</acceptance_criteria>
<verify>go build ./... && go test ./internal/core/db/ -count=1</verify>
<done>sys_config_restore_task 表双线上线（AutoMigrate + Migrate211），model 三段式与 string 四态常量就位</done>
</task>

<task id="93-03-02" type="tdd">
<name>Task 2: ConfigRestoreTaskService 异步编排（发起/执行/查询/收敛）</name>
<files>internal/services/config_restore_task_service.go</files>
<read_first>
- internal/services/device_info_collection_service.go（:133-163 Enqueue 同设备 pending/running IN 查询去重 = D-08 模板；worker goroutine 模式）
- internal/services/config_backup_service.go（RestoreBackup :536-546 stub 现状（本 plan 不动它，93-04 删）；CreateBackup/GetBackupByID/GetBackupContent/calculateHash 复用面）
- internal/device/restore_config.go（93-02 产物 RestoreConfig/RestoreResult）
- .planning/workstreams/milestone/phases/93-config-backup-todo-p2/93-RESEARCH.md（detached context 必须性 + 同设备互斥实现建议）
</read_first>
<action>
1. 新建 internal/services/config_restore_task_service.go：
   - `ConfigRestoreTaskService` struct{db *gorm.DB, backupSvc *ConfigBackupService, executor *device.DeviceExecutor} + `NewConfigRestoreTaskService(db, backupSvc, executor)` 构造器（Handler-Service 模式）。
   - `StartRestore(ctx, backupID, deviceID, createdBy string) (*models.ConfigRestoreTask, error)`：①GetBackupByID 查备份，校验 `backup.DeviceID == deviceID` 不一致返回 error "备份不属于目标设备，仅限恢复到备份源设备"（D-04）；②互斥查询 `device_id = ? AND status IN (pending, running)`（D-08，Enqueue:133 模板）命中返回 error "该设备存在进行中的恢复任务"；③创建任务 Status=pending/CreatedBy=createdBy → 返回；④`go s.runRestore(context.WithTimeout(context.Background(), constants.RestoreConfigTimeout), task.ID)`（D-34 detached context——严禁透传 HTTP ctx）。
   - 私有 `runRestore(ctx, taskID)`：入口 defer recover → 任务 failed（防 goroutine panic 悬挂 running）；s.executor == nil 时任务 failed "设备执行器未初始化" 并 return（handler 测试 nil-executor 环境安全）；状态→running（记 StartedAt）；⑤恢复前备份：`backupSvc.CreateBackup(ctx, &BackupRequest{DeviceID, DeviceName, BackupType: models.BackupTypeAuto, ChangeReason: "恢复前自动备份", CreatedBy: "restore", CompressLarge: true})` 失败→任务 failed（ErrorMessage）并 return（D-10/D-14，不下发）；⑥`backupSvc.GetBackupContent(ctx, backupID)` 取内容（走 93-01 解压路径）；⑦`executor.RestoreConfig(ctx, task.DeviceID, config)` 失败→任务 failed + ResultJSON 存部分 RestoreResult（D-12 留痕）；⑧回读 `executor.GetConfig(ctx, task.DeviceID)` → calculateHash 与备份 ConfigHash 比对，不一致仅 applogger.Warnf + hashMatched=false 不算失败（D-11）；⑨版本链记录直接构造 `models.ConfigBackup{DeviceID, DeviceName, BackupType: models.BackupTypeManual, StorageType: models.StorageTypeDatabase, ConfigContent: 回读配置, ConfigHash: 回读hash, BackupSize: len(回读), Version: 该设备最新版本+1, ChangeReason: fmt.Sprintf("恢复自版本 %d", backup.Version), CreatedBy: task.CreatedBy}` Create 插入（D-09，统计链路零改动）；⑩任务 success + ResultJSON（RestoreResult + hashMatched + restoredHash 序列化）+ SentLines/TotalLines 落列 + CompletedAt。
   - `GetRestoreTask(ctx, id)` / `ListRestoreTasks(ctx, current, pageSize, deviceID, orderByColumn, isAsc)`（base.ApplySort + 白名单 map `restoreTaskAllowedSortFields`：deviceId/status/createdAt——status 列在本表真实存在）。
   - `RecoverStaleRunningTasks(ctx)`：`UPDATE sys_config_restore_task SET status='failed', error_message='服务重启，任务中断' WHERE status='running'`（A5 discretion 方案①——启动收敛防崩溃残留锁死互斥，非清理 cron，不违 D-20；由 93-04 在装配点调用一次）。
2. 分页常量纪律：ListRestoreTasks 分页默认值经 `pkg/query.NormalizePagination`（CLAUDE.md Pagination Constants Convention，禁止内联字面量）。
</action>
<acceptance_criteria>
- config_restore_task_service.go 包含 `func (s *ConfigRestoreTaskService) StartRestore(`、`func (s *ConfigRestoreTaskService) runRestore(`、`RecoverStaleRunningTasks(`、`GetRestoreTask(`、`ListRestoreTasks(`
- StartRestore 含 "备份不属于目标设备" 与 "进行中的恢复任务" 两个错误分支（D-04/D-08）
- runRestore 的 goroutine 启动参数为 `context.WithTimeout(context.Background(), constants.RestoreConfigTimeout)` 且含 defer recover（detached + panic 防线）
- 恢复前备份分支含 "恢复前自动备份" ChangeReason；CreateBackup 错误 → 任务 failed → return 不触碰 RestoreConfig（D-14）
- hash 不一致分支调用 applogger.Warnf 且不返回 error（D-11）
- 含 ChangeReason "恢复自版本" 的 models.ConfigBackup 插入（D-09，BackupTypeManual）
- runRestore 含 executor nil 防护分支（任务 failed，不 panic）
- ListRestoreTasks 引用 `pkg/query.NormalizePagination`（grep 命中）
- `go build ./...` exit 0 且 `go vet ./internal/services/` exit 0
- `go test ./internal/services/ -count=1` exit 0（本 plan 不改 config_backup_service.go，存量全绿）
</acceptance_criteria>
<verify>go build ./... && go vet ./internal/services/ && go test ./internal/services/ -count=1</verify>
<done>恢复异步编排 service 层完整落地：发起校验/互斥 → detached 执行（恢复前备份→下发→回读→版本链）→ 查询/收敛方法就绪；stub 保留至 93-04 原子切换</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API 请求 ↔ 恢复任务创建 | 携带 backupId/deviceId 的恢复请求可被伪造/重放/并发 |
| 异步 goroutine ↔ 任务状态机 | 崩溃/panic 可残留 running 态锁死设备恢复入口 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01 | Tampering | StartRestore 跨设备误推（备份属 A 推给设备 B） | mitigate | D-04 service 层硬校验 backup.DeviceID == deviceID，不一致拒绝（Task 2 ①） |
| T-93-07 | Tampering | 并发恢复交叉下发（两用户同时恢复同一设备） | mitigate | D-08 任务表 pending/running IN 查询互斥（Enqueue 先例）+ RecoverStaleRunningTasks 防崩溃残留锁死 |
| T-93-08 | DoS | goroutine panic / 进程崩溃残留 running 永久锁死 | mitigate | runRestore defer recover → 任务 failed + RecoverStaleRunningTasks 启动一次性收敛 running→failed |
| T-93-09 | Information Disclosure | taskId 枚举探测他人任务 | mitigate | 任务 ID 为 UUID 不可枚举 + 查询端点同处 /backups 组权限保护（93-04 落地） |
| T-93-SC | Tampering | 依赖引入 | accept | 零新外部依赖（uuid/gorm 既有），无包合法性风险 |
</threat_model>

<verification>
1. `go build ./...` + `go vet ./internal/services/` exit 0
2. `go test ./internal/services/ -count=1` 全绿
3. `go test ./internal/core/db/ -count=1` 全绿（AutoMigrate 含新表）
4. grep：StartRestore/runRestore/RecoverStaleRunningTasks/RestoreTaskStatus* 标识符齐全
</verification>

<success_criteria>
- [ ] model + migration + AutoMigrate 双注册上线（sqlite 分支不缺表）
- [ ] 异步编排全链路代码闭环（发起/互斥/恢复前备份/下发/回读/版本链/终态）
- [ ] detached context + panic 防线 + nil executor 防护就位
</success_criteria>

<commit_plan>
1. feat(93): add ConfigRestoreTask model with Migrate211 dual registration (D-15/D-34) — model + database.go + migration_211
2. feat(93): implement ConfigRestoreTaskService async restore orchestration (D-01/04/08..11/14/17/20..22) — config_restore_task_service.go
</commit_plan>
