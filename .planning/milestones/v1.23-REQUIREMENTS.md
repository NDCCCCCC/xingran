---
last_updated: 2026-08-19
milestone: v1.23
update_trigger: v1.23 launch-block DEPLOY-01..05 all PASS, 5 commits + closeout
type: launch-block
---

# Requirements: XingRan-Next — Milestone v1.23 (Launch Block)

**Defined:** 2026-08-19
**Milestone:** v1.23 部署稳健性 & 文档一致性 (Deploy Robustness & Docs Consistency)
**Core Value:** 让 SM2 密钥配置链路在所有部署场景(document / docker-compose / sqlite / 生产线)自洽,避免再次发生"500 + 人工 ssh grep"生产事件。

> **性质:** 本启动块是**部署/文档稳健性修复**,非新功能。所有 5 项 DEPLOY 需求均来源于 `public-key-500-after-subpath-fix.md` Specialist Review §Related bugs。

## v1 Requirements

### DEPLOY — 部署稳健性 & 文档一致性

- [x] **DEPLOY-01**: `docs/deployment/{secret-management,single-machine-deployment,docker-compose}.md` + `configs/{config,config.prod,config.sqlite}.example.yaml` 共 17 处 `XINGRAN_JWT_SM2_*` 全部改为 `JWT_SM2_*`,与 `internal/config/config.go:359-360` BindEnv 一致;语义/表头/必填列保留;不动 secrets 注入值;`grep -rn "XINGRAN_JWT_SM2" docs/ configs/` 零命中
- [x] **DEPLOY-02**: `scripts/deploy/setup-server.sh` 的 `/etc/xingran/secrets.env` heredoc 在 `SM4_KEY=` 后追加 `# === SM2 密钥对（非对称,不能动态生成,否则每次重启踢全部用户） ===` 注释 + `go run scripts/crypto/gen_sm2_keys/main.go` 一次性生成命令说明 + `JWT_SM2_PRIVATE_KEY=` / `JWT_SM2_PUBLIC_KEY=` 两行占位;脚本整体可幂等重跑;**不做** `use_sm2=true` 强制校验(user-managed config 保守处理)
- [x] **DEPLOY-03**: `scripts/crypto/gen_sm2_keys/main.go` line 2 注释改为 `go run scripts/crypto/gen_sm2_keys/main.go`,与文件实际路径一致;`//go:build ignore` 标签保留;docs 中 `scripts/gen-sm2-keys*` 历史路径同步修正
- [x] **DEPLOY-04**: `internal/api/v1/auth.go` `getPublicKey` handler 在 `response.Error` 返回 500 之前打印 `applogger.Warnf("SM2 公钥不可用: useSM2=%v, sm2PublicKeyLoaded=%v, requestPath=%s, clientIP=%s", ...)`;`internal/core/security/jwt.go` 新增 `IsSM2Enabled()` 与 `HasSM2PublicKey()` 两个最小 getter;`go build ./...` + `go vet` 退出码 0;handler HTTP status / 响应文案零变化
- [x] **DEPLOY-05**: `configs/config.sqlite.example.yaml` line 58 `use_sm2: false` → `true`;line 60 env 名同步修正;文件顶部注释段新增【启用 SM2 必读】6 行迁移指引指向 `docs/deployment/secret-management.md §2.2`

## Out-of-Scope (Manual Operator Action)

`configs/config.yaml` (live, user-managed) 与 `.env` 仍含 `XINGRAN_JWT_SM2_*` 字面量 — 由运维手工迁移到 secrets.env 注入路径,**不入 phase 范围**。
