---
phase: 67-build-regression-and-visual-confirmation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/67-build-regression-and-visual-confirmation/67-01-PLAN.md
  - .planning/phases/67-build-regression-and-visual-confirmation/bundle-comparison.md
  - .planning/phases/67-build-regression-and-visual-confirmation/screenshots/
  - .planning/phases/67-build-regression-and-visual-confirmation/screen-comparison.md
  - .planning/MILESTONES.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements:
  - QA-03
  - QA-04
user_setup: []

must_haves:
  truths:
    - "QA-03: npm run build / type-check / lint / test 四门全绿,退出码 0;vendor-react gzip = 774.94 kB 或更小,无回归"
    - "QA-03: bundle 三口径对比(总 dist 字节 / vendor-react gzip / app chunk 字节)记录在 bundle-comparison.md,v1.22 期间持平或下降"
    - "QA-04: 仪表盘 / 系统用户 / 工位管理 / 监控仪表盘 / 资产对账看板 / 登录页 六屏改造后截图与 refs/ 旧截图并列对比,人工确认无布局崩坏 / 无不可读文本 / 无 indigo/slate 残留冲突色"
    - "QA-04: 每屏按 66-01-SUMMARY 速查表逐项核对侧栏底 / 表头底 / 主按钮 / Tag / 图表系列,DOM computed-style 证据存档"
    - "Milestone 归档:REQUIREMENTS.md 15 项需求勾选 / STATE.md 推进 / MILESTONES.md v1.22 条目 / 遗留项清单(Tag 2.8:1 / index.css 叠层 / PROTO-VIS Future)"
  artifacts:
    - path: ".planning/phases/67-build-regression-and-visual-confirmation/bundle-comparison.md"
      provides: "QA-03 bundle 三口径前后对比归档"
      contains: "vendor-react gzip"
    - path: ".planning/phases/67-build-regression-and-visual-confirmation/screen-comparison.md"
      provides: "QA-04 六屏前后对比表 + 预期品牌值核对结果"
      contains: "仪表盘"
    - path: ".planning/phases/67-build-regression-and-visual-confirmation/screenshots/"
      provides: "六屏改造后截图存档(或 fallback 证据文件)"
      contains: "before-after"
    - path: ".planning/MILESTONES.md"
      provides: "v1.22 milestone 条目追加 + 候选 SHIPPED 状态"
      contains: "v1.22"
    - path: ".planning/REQUIREMENTS.md"
      provides: "15 项 v1.22 需求勾选完成状态"
      contains: "Delivered"
  key_links:
    - from: ".planning/phases/67-build-regression-and-visual-confirmation/bundle-comparison.md"
      to: "xingran-react-frontend/dist/assets/vendor-react-*.js"
      via: "grep gzip size from build log"
      pattern: "vendor-react.*gzip"
    - from: ".planning/phases/67-build-regression-and-visual-confirmation/screen-comparison.md"
      to: "655aa291-9bfe-4e94-ad5d-b3c8b2d24984/refs/system-*.png"
      via: "六屏前后对比表逐项 link 到旧截图"
      pattern: "system-(dashboard|users|workstations|monitor-dashboard|assets-dashboard)"
    - from: ".planning/phases/67-build-regression-and-visual-confirmation/screen-comparison.md"
      to: ".planning/phases/66-component-styles-and-hardcoded-color-scan/66-01-SUMMARY.md"
      via: "六屏核对清单逐屏预期品牌值从 66-01-SUMMARY 速查表取用"
      pattern: "#14532D|#E9EFEB|#156031"
---

# Phase 67 Plan 01: 构建回归 + 视觉确认 (v1.22 终验)

## Status: DRAFT

**Created:** 2026-08-18
**Source:** Phase 67 ROADMAP SC#1-4 / REQUIREMENTS QA-03 + QA-04 / D-01..D-05 锁定决策
**约束 (per D-05):** 仅验证证据 + 文档 + 截图存档;不改业务代码;发现视觉小缺陷(色值级)直接修,大缺陷记录 gap 回流

## Goal

**Phase 67 Goal (ROADMAP verbatim):** 全量构建 / 类型 / 单测 / 视觉四门全绿,验证 v1.22 品牌化未引入回归 —— vendor-react 打包后体积不增(预期下降)、关键 6 屏前后截图对比无布局崩坏、登录页与后台视觉一致,里程碑可 SHIPPED + ARCHIVED。

**本 plan 收口 4 项 ROADMAP Success Criteria:**

1. `npm run build` 退出码 0,`type-check` / `lint` / `test` 全绿;vendor-react gzip ≤ 774.94 kB(Phase 64/65/66 已测基线),无回归;前后对比数值记录到 bundle-comparison.md
2. 6 屏(仪表盘 / 系统用户 / 工位管理 / 监控仪表盘 / 资产对账看板 / 登录页)改造前后截图对比 —— 无布局崩坏 / 无不可读文本 / 无 indigo/slate 残留;登录页与后台视觉一致
3. Phase 64-66 success criteria 全部复测通过(回归守护):关键变量 / AntdThemeBridge / 主题收敛 / 组件样式 / 硬编码扫描零命中
4. MILESTONES.md v1.22 条目落盘 / REQUIREMENTS.md 15 项勾选 / STATE.md 更新 / 遗留项清单

## 前后对比基线表 (来自 Phase 64/65/66 SUMMARY)

| 口径 | v1.21 baseline (Phase 64 前) | Phase 64 后 | Phase 65 后 (T1) | Phase 66 后 (T5) | Phase 67 终值 |
|------|-----------------------------|-------------|-----------------|------------------|---------------|
| vendor-react gzip | 774.96 kB | 774.94 kB | 774.94 kB | 774.94 kB | (T1 测) |
| vendor-echarts gzip | — | 374.55 kB | 374.55 kB | 374.55 kB | (T1 测) |
| vendor-three gzip | — | 242.65 kB | 242.65 kB | 242.65 kB | (T1 测) |
| vendor-xlsx gzip | — | 142.99 kB | 142.99 kB | 142.99 kB | (T1 测) |
| vendor-markdown gzip | — | 116.13 kB | 116.13 kB | 116.13 kB | (T1 测) |
| dist/assets 总量 (raw) | — | — | 7,281 kB / 134 chunks | (未单独存档) | (T1 测) |

**QA-03 SC 解读:** "不增"即满足;`vendor-react` 持平是预期(主题代码从未进入 vendor-react chunk,见 65-01 根因分析),主题代码收益在源码层(-4,357 行)与 app 路由层 chunk。

**QA-04 SC 解读:** 登录页改造前状态即品牌锚点本身(后端 :9000 当前 503,空态可接受);后台五屏"改造前"基线在 `655aa291-9bfe-4e94-ad5d-b3c8b2d24984/refs/system-*.png` 实测存档(login-live.png + system-{dashboard,users,workstations,monitor-dashboard,assets-dashboard}.png 共 6 屏旧截图齐备)。

## 六屏核对清单(从 66-01-SUMMARY §"Phase 67 QA-04 六屏核对清单" 取用)

| # | 屏幕 | 路径 | 关键品牌值预期 |
|---|------|------|---------------|
| 1 | 仪表盘 | `/dashboard` | 侧栏 `#14532D`; 顶栏白底; 图表系列 `#156031/#3B784C/#C09058/#598E5E/#C89868` |
| 2 | 系统用户 | `/system/user` | 表头 `#E9EFEB`; 分割线 `#DBD7CE`; 行 hover `#F7F5EE`; 启用状态 Tag 绿灰底 |
| 3 | 工位管理 | `/operations/workstation` | 表头 `#E9EFEB`; Input focus `rgba(21,96,49,0.15)` 2px; 白卡 + 1px 暖灰描边 |
| 4 | 监控仪表盘 | `/monitor/cache` 等 | 卡片阴影暖灰; ChartCard 系列色绿金梯度 |
| 5 | 资产对账看板 | `/asset/reconciliation/dashboard` | HealthBadge / HealthCard 背景品牌绿/铜金; ModernTag 五态 brand var |
| 6 | 登录页 | `/login` | 铜金 Login (D-04 例外保留); SM2/SM3/SM4 浅黄 `#FEF3C7` 标签 + `#B88850` 铜金字 |

## Tasks

### Task 67-T1: QA-03 四门终验 + bundle 三口径前后对比归档

**Files:** `.planning/phases/67-build-regression-and-visual-confirmation/bundle-comparison.md`, `xingran-react-frontend/dist/` (构建产物)

**Action:**

1. 在 `xingran-react-frontend/` 下依次执行四门,捕获退出码与关键输出:
   ```bash
   cd xingran-react-frontend
   npm run type-check       # tsc --noEmit
   npm run build            # tsc -b && vite build
   npm run lint             # eslint . && node scripts/check-hardcoded-colors.mjs
   npm test                 # vitest
   ```
2. 从 `npm run build` 输出解析 vendor-react / vendor-echarts / vendor-three / vendor-xlsx / vendor-markdown 的 gzip 字节,记录到 `bundle-comparison.md` 表格(沿用 66-01 表格格式)
3. `du -sh dist/` 计算 dist/assets 总量 + `ls dist/assets/*.js | wc -l` 数 chunk 数,记录到同一表格
4. 追加"对比结论"段:
   - vendor-react gzip vs Phase 66 后(774.94 kB) —— **不增**即 SC#1 通过
   - dist/assets 总量 vs Phase 65(7,281 kB / 134 chunks) —— SC#1 三口径全绿
   - 主题代码收益源头说明(65-01 已分析:不在 vendor-react,在 app chunk;raw -4,357 行)
5. 引用 66-01-SUMMARY 的"四门输出摘要"对照 —— 不一致或新 warning 即暂停、记录 gap

**Verify:**

```xml
<verify>
  <automated>cd xingran-react-frontend && npm run type-check && npm run build && npm run lint && npm test</automated>
  <manual>Compare bundle-comparison.md output against Phase 66 baseline table — all four gates exit 0; vendor-react gzip ≤ 774.94 kB; dist/assets total non-increasing</manual>
</verify>
```

**Done:**

- `bundle-comparison.md` 写入六行 gzip + 总量 + chunk 数对比表,三口径全绿(不增即过)
- 四门全部退出码 0
- 引用 66-01-SUMMARY baseline,数字一致或更小
- 无新增 lint warning(基线 1,032 持平)

---

### Task 67-T2: QA-04 六屏视觉确认 (chrome-devtools + DOM computed-style 证据)

**Files:** `.planning/phases/67-build-regression-and-visual-confirmation/screen-comparison.md`, `.planning/phases/67-build-regression-and-visual-confirmation/screenshots/*.png`

**Action:**

1. **启动 dev server** —— 后端 :9000 当前 503 可接受(空态截图记录为环境限制,不阻塞 QA-04)
   ```bash
   cd xingran-react-frontend && npm run dev   # http://localhost:4000
   ```
2. **截图存档路径** —— `screenshots/` 下创建子目录,每屏一个 PNG:
   ```
   screenshots/
   ├── 01-dashboard-after.png
   ├── 02-system-users-after.png
   ├── 03-workstations-after.png
   ├── 04-monitor-dashboard-after.png
   ├── 05-assets-dashboard-after.png
   └── 06-login-after.png
   ```
3. **chrome-devtools 自动化逐屏**(orchestrator 执行,本 plan 不直接依赖 chrome-devtools):
   - 登录 → 逐屏访问路径(见核对清单表)→ `take_screenshot` 存档
   - 同时在 DevTools `Elements` 面板提取关键 DOM 节点 computed style:
     - 侧栏节点 `background-color` (预期 `rgb(20,83,45)` = `#14532D`)
     - 表头 `background-color` (预期 `rgb(233,239,235)` = `#E9EFEB`)
     - 主按钮 `background-color` + `color` (预期 `rgb(21,96,49)` + `rgb(255,255,255)`)
     - 默认 Tag `background-color` + `color` (预期 `rgb(254,243,199)` + `rgb(184,136,80)`)
4. **Fallback 路径(若 chrome-devtools take_screenshot 再次超时)**:
   - 仅记录 DOM computed-style 证据(无须 PNG 存档)
   - 引用 66-01-SUMMARY §"T6 9 步目检清单" 中已存档的侧栏/表头/按钮/Tag/dark 模式证据(9 步已通过)
   - 引用 65-01-SUMMARY §"T9 人工视觉冒烟清单" 的 chrome-devtools DOM 证据
   - 视情况将 refs/ 旧截图与新 DOM 证据并列写入 comparison 表
5. **写入 screen-comparison.md** —— 6 行表格,每行 6 列:
   | # | 屏幕 | 路径 | 改造前(refs/) | 改造后(本 phase) | 预期品牌值核对 |
   - 预期品牌值核对列:逐屏从 66-01-SUMMARY 取值,实测值填入,差异 → "通过 / 偏差 N 处"
   - 偏差定义:布局崩坏 / 不可读文本 / 残留 indigo-slate(`#4F46E5` / `#1e293b` / `#F1F5F9`)
6. **小缺陷直接修(色值级,D-05 例外)**:
   - 若发现色值未接令牌(如某个 Tag 颜色硬编码)→ 直接修,小 commit
   - 大缺陷(布局崩塌 / 整屏错位)→ 记录 gap 回流,**不修**

**Verify:**

```xml
<verify>
  <automated>ls -la .planning/phases/67-build-regression-and-visual-confirmation/screenshots/*.png 2>/dev/null | wc -l  # 期望 6</automated>
  <automated>grep -c "rgb(20,83,45)\|rgb(233,239,235)\|rgb(21,96,49)\|rgb(255,255,255)" screen-comparison.md  # 期望 ≥ 6 (每屏至少 1 个 computed-style 证据)</automated>
  <human-check>逐屏核对:无布局崩坏 / 无不可读文本 / 无残留冲突色</human-check>
</verify>
```

**Done:**

- `screen-comparison.md` 6 屏全填(每屏含路径 / refs/ 旧截图 / 改造后截图或 DOM 证据 / 预期品牌值核对结果)
- `screenshots/` 下 6 张 PNG (或 fallback 证据文件 + DOM 引用记录)
- 视觉结论:6 屏"通过" / 大缺陷 gap 列表(若有)
- 不改业务代码(小色值修补允许,大缺陷回流)

---

### Task 67-T3: Milestone 归档准备 + 遗留项清单

**Files:** `.planning/MILESTONES.md`, `.planning/REQUIREMENTS.md`, `.planning/STATE.md`

**Action:**

1. **REQUIREMENTS.md** —— 15 项需求勾选完成状态:
   - TOKEN-01..04 → Phase 64 完成态 "Delivered" (Phase 64 完成态)
   - QA-01 → Phase 64 完成态
   - THEME-01..03 → Phase 65 完成态(已在原表中"Delivered, T9 pending";T9 已 AUTOMATED PASS,可移除 pending 备注或保留)
   - COMP-01..04 → Phase 66 完成态
   - QA-02 → Phase 66 完成态
   - **QA-03 / QA-04 → Phase 67 完成态**(本 plan 完成后置 Delivered)
2. **STATE.md** —— 追加 v1.22 进度:
   - 当前 phase: 67(最后一个)
   - 4 phases 64/65/66/67 全部 plan 数 + 总 commit 数
   - bundle 终值 + 6 屏视觉确认结论
3. **MILESTONES.md** —— 追加 v1.22 条目:
   ```
   - ✅ v1.22 前端品牌化改造 (Frontend Brand Design-System) — Phases 64-67 (shipped 2026-08-18) — 见 [milestones/v1.22-ROADMAP.md](milestones/v1.22-ROADMAP.md)
   ```
   (实际 SHIPPED 日期根据 execute 完成日填;此处先以草稿 + "候选" 标注)
4. **遗留项清单(写入 67-01-SUMMARY §Risks / Follow-ups)**:
   - **Tag 2.8:1 已知风险**(66-01 Deviation #9):默认 Tag `#B88850 on #FEF3C7` 对比度 ≈ 2.8:1,装饰性品牌锚点(非语义状态);候选收紧值 `#905D00` (brand-spec 已验证)
   - **index.css 叠层规则债**(66-01 §"叠层技术债"):4 处重复 `[data-color-mode="dark"] .ant-table-thead` + 多组 `.ant-tag` 叠层,后续 phase 合并去重
   - **PROTO-VIS Future**(REQUIREMENTS.md §Future):
     - PROTO-01..04: 路由/字段/表头/工具栏差异对齐(53 屏)
     - VIS-01..03: Three.js 3D 配色 / 过渡动效 / 打印导出样式品牌化
   - **chrome-devtools 截图管线不稳定**(65-01 / 66-01 T6 均遇超时):fallback 已就绪(DOM 证据 + refs 截图),待工具链修复
5. **不创建 v1.22-ROADMAP.md / v1.22-REQUIREMENTS.md 归档副本**(D-05 范围纪律 + 沿用 v1.21 模式:见后续 milestone archive 命令);orchestrator execute-phase 完成后若需归档,按 v1.21 模式手动触发

**Verify:**

```xml
<verify>
  <automated>grep -c "Delivered" .planning/REQUIREMENTS.md  # 期望 ≥ 15 (v1.22 全部勾选)</automated>
  <automated>grep "v1.22" .planning/MILESTONES.md  # 期望命中 v1.22 条目</automated>
  <manual>STATE.md 顶部推进段反映 67-01 完成</manual>
</verify>
```

**Done:**

- `REQUIREMENTS.md` 15 项 v1.22 需求全部勾选 / Delivered 状态
- `STATE.md` 反映 Phase 67 完成 + v1.22 milestone 候选 SHIPPED
- `MILESTONES.md` v1.22 条目追加
- 遗留项清单(4 项)写入本 SUMMARY
- 无业务代码改动

---

## Constraints

1. **D-05 范围纪律(强制):** 不改业务代码;本 plan 产出仅为验证证据 + 文档 + 截图存档
2. **小缺陷直接修(色值级):** 若 QA-04 发现某个 Tag 颜色硬编码等纯色值问题,直接修 + 小 commit;不允许回归写业务代码
3. **大缺陷记录 gap:** 布局崩塌 / 整屏错位 / 功能不可用 → 写入 67-01-SUMMARY §"Risks / Follow-ups",**不修**
4. **chrome-devtools fallback:** 若 `take_screenshot` 再次超时(65-01 / 66-01 T6 均遇),仅记录 DOM computed-style 证据,不阻塞 QA-04
5. **bundle 「预期下降」按实际结果记录:** vendor-react gzip 持平是预期(主题代码不在 vendor-react chunk,见 65-01 根因分析);dist/assets 总量对比 65-01 数据点(7,281 kB / 134 chunks)
6. **后端 :9000 503 不阻塞:** 数据屏可能空态,空态品牌化呈现即可,空态截图可接受;若后端不可用记录为环境限制,不阻塞 QA-04 SC#2
7. **不创建归档副本:** v1.22-ROADMAP.md / v1.22-REQUIREMENTS.md 由后续 milestone archive 命令生成,本 plan 不直接产出
8. **D-01 锁定决策保护:** 不重建 ThemeSwitcher / ColorSwitcher / 多主题目录;视觉确认阶段若发现布局相关问题,优先怀疑主题收敛 bug 而非回归重建
9. **截图管线不稳定:** 已记录为 known risk;本 plan 不阻塞,fallback DOM 证据等价

## Success Criteria

### SC#1 (QA-03 四门全绿 + bundle 不增) — verbatim from ROADMAP

> `npm run build` 退出码 0,`npm run type-check` / `npm run lint` / `npm run test` 全绿;`vendor-react` 打包后 gzip 体积较 v1.21 baseline(774.96 kB,见 v1.19 W4 / v1.20 close)不增(预期下降 —— 移除 6 套主题节约 ~数 kB),前后对比数值记录到 ROADMAP Progress 段;Phase 64-66 的对比度校验脚本与硬编码扫描在 CI 中运行并通过

**Phase 67 实操:** 四门退出码 0;vendor-react gzip ≤ 774.94 kB;bundle 三口径(总 dist / vendor-react gzip / app chunk)对比写入 `bundle-comparison.md`

### SC#2 (QA-04 6 屏前后视觉对比) — verbatim from ROADMAP

> 关键 6 屏(仪表盘 / 系统用户 / 工位管理 / 监控仪表盘 / 资产对账看板 / 登录页)改造前后截图对比 —— 前后两套 PNG 在同一对比画布并列展示(可视化 diff):**无布局崩坏**、**无不可读文本**、**无残留 indigo / slate 冲突色**;登录页(品牌锚点)与后台内部组件(品牌化落地)视觉一致(深绿 × 铜金 × 奶油纸感),同一品牌语汇贯穿

**Phase 67 实操:** `screen-comparison.md` 6 屏对比表 + `screenshots/` 6 张 PNG(或 fallback DOM 证据);每屏从 66-01-SUMMARY 速查表逐项核对

### SC#3 (Phase 64-66 SC 回归守护) — verbatim from ROADMAP

> Phase 64-66 的 success criteria 全部复测通过(回归守护):`--theme-primary` / `--sidebar-bg` / `--theme-neutral-100` 等关键变量值正确,AntdThemeBridge 接品牌令牌,主题切换器已移除且 light / dark + layout / density 切换可用,侧边栏 / 表格 / 按钮 / 表单 / ECharts 颜色全量品牌化,硬编码扫描零命中

**Phase 67 实操:** 已由 Phase 64/65/66 SUMMARY 完成证据存档(T1-T8 commits + colors.test.ts 40/40 + scanner 0 命中 + vendor-react 774.94 kB);本 phase 引用既有证据,不重跑(若 QA-04 发现偏差则触发局部复测)

### SC#4 (milestone 归档) — verbatim from ROADMAP

> `MILESTONES.md` v1.22 条目落盘、`REQUIREMENTS.md` 仍标记 v1.23+ 候选(PROTO 逐屏对齐 / VIS 视觉深化);里程碑 SHIPPED 报告产出,ROADMAP archived 至 `milestones/v1.22-ROADMAP.md` + `milestones/v1.22-REQUIREMENTS.md`

**Phase 67 实操:** REQUIREMENTS.md 15 项勾选 / STATE.md 推进 / MILESTONES.md v1.22 条目追加 / 遗留项清单(4 项);归档副本由后续 archive 命令生成

## Output

完成后写 `.planning/phases/67-build-regression-and-visual-confirmation/67-01-SUMMARY.md`,含:

- 任务进度表(T1/T2/T3 commits)
- QA-03 四门结果 + bundle 三口径对比
- QA-04 6 屏对比结果(6/6 通过 / 部分偏差 / gap 列表)
- REQUIREMENTS / STATE / MILESTONES 变更摘要
- Deviations / Risks / Follow-ups(含 Tag 2.8:1 / index.css 叠层 / PROTO-VIS Future / chrome-devtools 截图管线)
- Self-Check 全勾选

## Flagged Risks (前置标注)

1. **chrome-devtools 截图管线超时**(65-01 / 66-01 T6 均遇):已写 fallback(DOM computed-style 证据 + refs/ 旧截图),不阻塞 QA-04;但若执行者不读 fallback 路径,会卡死
2. **后端 :9000 503**(已知环境限制):空态截图记录为环境限制,不阻塞;但若数据屏空态本身不品牌化(空态文字 + 图标未接品牌),QA-04 会暴露空态文案设计问题 —— 66-01 已对 Table empty 处理过,antd 默认暗色文字,与画布 `#F0ECE3` 对比度需目检
3. **bundle 「预期下降」措辞修正(已在 65-01 记录):** vendor-react 持平是预期(主题代码不在 vendor-react chunk);若执行者按字面"预期下降"判定不通过,会误报失败 —— 已在 Constraints #5 / SC#1 实操段明确"持平即过"
4. **Tag 2.8:1 装饰性 vs 语义状态 Tag 混淆**(66-01 Deviation #9):QA-04 目检可能误判为"不可读文本";已确认是装饰性品牌锚点,语义状态 Tag 保留功能色;本 plan 在 SC#2 "不可读文本"判据上加注"非装饰性品牌 Tag"
5. **index.css 叠层规则胜出**(66-01 §"叠层技术债"):当前渲染均在品牌族内,但后置规则胜出意味着新增 token 时可能撞旧规则;不属 v1.22 范围,记录 v1.23+ 跟进
6. **遗留 4 项 v1.23+ 候选**:PROTO-01..04 逐屏对齐 / VIS-01..03 视觉深化 / Tag 2.8:1 收紧 / index.css 叠层去重 —— 不阻塞 SHIPPED,但需在 MILESTONES / STATE 明确标注

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>