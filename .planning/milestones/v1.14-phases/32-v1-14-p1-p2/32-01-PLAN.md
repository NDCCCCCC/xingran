---
phase: 32-v1-14-p1-p2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/crypto/request_encryption.go
  - pkg/crypto/nonce_storage.go
  - pkg/crypto/request_encryption_replay_test.go
  - pkg/crypto/nonce_storage_cleanup_test.go
  - pkg/middleware/permission.go
  - pkg/middleware/permission_inherit_test.go
  - internal/api/v1/operations/excel_handler.go
  - internal/api/v1/operations/excel_magic_bytes_test.go
  - configs/config.yaml
  - configs/config.dev.yaml
  - configs/config.prod.yaml
autonomous: true
requirements:
  - P1-S2
  - P1-S3
  - P1-S4
  - P1-S7

must_haves:
  truths:
    - "Encrypted request body with timestamp 61 seconds in the past is rejected with timestamp error"
    - "Encrypted request body with timestamp 61 seconds in the future is rejected with timestamp error"
    - "Timestamp within ±60s window (configurable via security.replay_window_sec) is accepted"
    - "Nonce cleanup goroutine is launched in NewShardedNonceStorage and runs every maxTimeDiff seconds"
    - "Child menu of type C (button-type / non-F) does NOT inherit parent menu permissions"
    - "Excel file upload without PK\x03\x04 magic bytes is rejected before parse"
    - "Excel file with valid magic bytes passes the magic verification"
  artifacts:
    - path: "pkg/crypto/request_encryption.go"
      provides: "Replay window check using security.replay_window_sec config"
      contains: "replay_window_sec"
    - path: "pkg/crypto/nonce_storage.go"
      provides: "Ticker goroutine calling cleanupExpiredNonces on interval"
      contains: "go func()"
    - path: "pkg/middleware/permission.go"
      provides: "Permission check that rejects C-type child menus inheriting parent"
      contains: "menuType"
    - path: "internal/api/v1/operations/excel_handler.go"
      provides: "verifyExcelMagicBytes helper that reads first 4 bytes and checks PK\\x03\\x04"
      contains: "0x50, 0x4B, 0x03, 0x04"
    - path: "configs/config.yaml"
      provides: "security.replay_window_sec config field with default 60"
      contains: "replay_window_sec"
  key_links:
    - from: "pkg/crypto/request_encryption.go"
      to: "configs/config.yaml"
      via: "Config field read on RequestEncryptor init"
      pattern: "ReplayWindowSec"
    - from: "pkg/crypto/nonce_storage.go"
      to: "pkg/crypto/nonce_storage.go:cleanupExpiredNonces"
      via: "ticker.C → cleanupExpiredNonces() in goroutine"
      pattern: "ticker\\.C"
    - from: "internal/api/v1/operations/excel_handler.go"
      to: "multipart.FileHeader"
      via: "verifyExcelMagicBytes called from upload handler"
      pattern: "verifyExcelMagicBytes"
---

<objective>
Wave 1 — P1 安全加固快赢 (quick wins). Four low-risk security items: tighten request-encryption replay window to ±60s with config, ensure nonce cleanup goroutine is launched, remove child-menu auto-inherit for non-button types, enforce Excel magic byte verification.

Purpose: Lock down the most exploitable P1 security holes (replay attack, privilege escalation via menu inheritance, malicious file upload) without breaking login latency or migration paths. All four items are independently deployable.

Output:
- 4 production code edits (already-fixed items get verification tests; unfixed P1-S2 gets the actual tighten)
- 4 new test files
- Config field `security.replay_window_sec` (default 60) added to config.yaml + dev + prod profiles
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-v1-14-p1-p2/CONTEXT.md
@.planning/phases/32-v1-14-p1-p2/32-RESEARCH.md
@.planning/phases/32-v1-14-p1-p2/32-VALIDATION.md

# Source-of-truth files to consult before editing
@pkg/crypto/request_encryption.go
@pkg/crypto/nonce_storage.go
@pkg/middleware/permission.go
@internal/api/v1/operations/excel_handler.go
@configs/config.yaml
@configs/config.dev.yaml
@configs/config.prod.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten replay window to ±60s (P1-S2) + add config + test</name>
  <files>pkg/crypto/request_encryption.go, configs/config.yaml, configs/config.dev.yaml, configs/config.prod.yaml, pkg/crypto/request_encryption_replay_test.go</files>
  <read_first>
    - pkg/crypto/request_encryption.go (lines 80-110, find maxTimeDiff constant and its usage in isTimestampValid / DecryptRequest)
    - configs/config.yaml (find `security:` block, currently has `request_encryption.exclude_paths`)
    - configs/config.dev.yaml (find `security:` block)
    - configs/config.prod.yaml (find `security:` block)
    - .planning/phases/32-v1-14-p1-p2/32-RESEARCH.md (Pattern section showing Config struct)
  </read_first>
  <action>
    Edit pkg/crypto/request_encryption.go:
    1. Locate the current maxTimeDiff constant (currently 120 per RESEARCH.md, set by commit af05d99). The variable name in code may be `maxTimeDiff` or `replayWindow` — match exactly.
    2. Replace literal `120` (or `300`) with a configurable value sourced from the request encryptor's config struct. Add a new field `ReplayWindowSec int` to the relevant Config struct (likely `SecurityConfig` or local package config) with default 60.
    3. The check function (likely `validateTimestamp` / `isTimestampValid` / inline in DecryptRequest) must use `time.Now().Unix() - req.Timestamp` compared against `c.ReplayWindowSec`. Both past and future directions.
    4. When the window is exceeded, return an error wrapping `apperrors.CodeParamError` with message containing "timestamp" and the actual offset seconds.

    Edit configs/config.yaml, configs/config.dev.yaml, configs/config.prod.yaml:
    Under `security:` block, add:
    ```yaml
    security:
      # ... existing fields
      replay_window_sec: 60  # ±60s tolerance for encrypted request timestamp; tighten OWASP-aligned
    ```
    For dev.yaml, allow 120 (lenient for local testing). For prod.yaml, set 60.

    Create pkg/crypto/request_encryption_replay_test.go:
    Use `github.com/stretchr/testify/assert` (already in project). Build a Config with ReplayWindowSec=60. Test cases:
    - `now`: accept (offset 0)
    - `now - 30s`: accept
    - `now - 59s`: accept (boundary)
    - `now - 61s`: reject
    - `now + 61s`: reject
    - `now + 5min`: reject

    Each case constructs a fake encrypted request payload (or calls the validator function directly with a timestamp int64) and asserts accept/reject.
  </action>
  <acceptance_criteria>
    - pkg/crypto/request_encryption.go: literal `120` or `300` no longer present in timestamp validation logic (grep `maxTimeDiff\|replayWindow` returns only the config struct field reference)
    - pkg/crypto/request_encryption.go: Config struct (or equivalent) contains `ReplayWindowSec int` field
    - configs/config.yaml contains `replay_window_sec: 60` under security section (grep `replay_window_sec` matches)
    - configs/config.dev.yaml contains `replay_window_sec:` with value 120 or 60
    - configs/config.prod.yaml contains `replay_window_sec: 60`
    - pkg/crypto/request_encryption_replay_test.go exists (test file creation; grep `go test -list ".*" ./pkg/crypto/` returns TestRequestEncryption* OR TestReplayWindow*)
    - Test cases for now-61s and now+61s both fail (assert.Error)
    - Test case for now-30s passes (assert.NoError)
    - `go build ./...` exits 0
    - `go vet ./...` exits 0 with 0 warnings
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./... && go test -count=1 -run "TestRequestEncryption\|TestReplayWindow" ./pkg/crypto/ -v</automated>
  </verify>
  <done>
    P1-S2 fully resolved: replay window is configurable at ±60s by default, test file exercises boundary at ±61s (both directions), all config profiles declare replay_window_sec, build + vet + targeted test pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify nonce cleanup goroutine + permission child-inherit + Excel magic bytes tests</name>
  <files>pkg/crypto/nonce_storage_cleanup_test.go, pkg/middleware/permission_inherit_test.go, internal/api/v1/operations/excel_magic_bytes_test.go</files>
  <read_first>
    - pkg/crypto/nonce_storage.go (find NewShardedNonceStorage, cleanupExpiredNonces; verify ticker goroutine already launched per RESEARCH.md commit 1071867)
    - pkg/middleware/permission.go (find function checking menuType; verify the auto-inherit for non-F types already removed per commit 2b55e0d)
    - internal/api/v1/operations/excel_handler.go (find verifyExcelMagicBytes; verify PK\x03\x04 check exists per commit 2c74c06)
    - .planning/phases/32-v1-14-p1-p2/32-VALIDATION.md (file list to create)
  </read_first>
  <action>
    Per RESEARCH.md, all three items are ALREADY FIXED in prior commits. The task is to add regression tests ONLY — do NOT modify production code.

    Create pkg/crypto/nonce_storage_cleanup_test.go:
    Test `TestShardedNonceStorage_CleansExpiredOnInterval`:
    - Create storage with short maxTimeDiff (e.g., 100ms) for test speed
    - Insert a nonce
    - Wait > maxTimeDiff + ticker interval
    - Assert the nonce is no longer present
    - Use sync mechanism or sleep to advance time deterministically (acceptable: time.Sleep)

    Create pkg/middleware/permission_inherit_test.go:
    Test `TestPermissionCheck_DoesNotInheritCTypeChild`:
    - Construct a menu tree: parent (type=M directory) with one child (type=C button)
    - User has permission on parent menu only
    - Assert: permission check on child returns false (no inherit)
    - Second case: parent (type=M) with one child (type=F); if F inherits per existing code, parent-only permission returns true. Otherwise false. Match existing implementation behavior — read the file first to confirm what is inherited.
    - Use table-driven test with `[]struct{ name string; childType string; expectInherit bool }`

    Create internal/api/v1/operations/excel_magic_bytes_test.go:
    Test `TestVerifyExcelMagicBytes_RejectsNonPK`:
    - Create multipart.FileHeader with content starting NOT with PK\x03\x04 (e.g., "MZ" DOS header or random bytes)
    - Call verifyExcelMagicBytes (function may be unexported; use same-package test)
    - Assert error returned
    - Second case: content starts with PK\x03\x04 — assert no error
    - Use `multipart.NewWriter` to construct a real multipart body or call with `bytes.NewReader` directly if the function accepts io.Reader
  </action>
  <acceptance_criteria>
    - pkg/crypto/nonce_storage_cleanup_test.go exists with TestShardedNonceStorage_CleansExpiredOnInterval (grep returns test function name)
    - pkg/middleware/permission_inherit_test.go exists with TestPermissionCheck_DoesNotInheritCTypeChild
    - internal/api/v1/operations/excel_magic_bytes_test.go exists with TestVerifyExcelMagicBytes_RejectsNonPK
    - All 3 tests pass: `go test -count=1 -run "TestShardedNonceStorage_CleansExpiredOnInterval" ./pkg/crypto/` exits 0
    - `go test -count=1 -run "TestPermissionCheck_DoesNotInheritCTypeChild" ./pkg/middleware/` exits 0
    - `go test -count=1 -run "TestVerifyExcelMagicBytes" ./internal/api/v1/operations/` exits 0
    - `go build ./...` exits 0 (no production code touched)
    - `go vet ./...` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./... && go test -count=1 -run "TestShardedNonceStorage_CleansExpiredOnInterval|TestPermissionCheck_DoesNotInheritCTypeChild|TestVerifyExcelMagicBytes" ./pkg/crypto/ ./pkg/middleware/ ./internal/api/v1/operations/ -v</automated>
  </verify>
  <done>
    P1-S3, P1-S4, P1-S7 regression tests added and passing. Production code untouched (per RESEARCH.md, all 3 already fixed). Wave 1 quick-wins complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server (encrypted body) | timestamp + nonce must be validated before decrypt |
| client → server (file upload) | magic bytes must match ZIP/OOXML before parse |
| role → permission check | menu permission inheritance must be explicit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering (replay) | request_encryption.go | mitigate | P1-S2: configurable ±60s window + test boundary |
| T-32-02 | DoS (memory leak) | nonce_storage.go | mitigate | P1-S3: ticker goroutine + test verifies cleanup |
| T-32-03 | EoP (privilege via inherit) | permission.go | mitigate | P1-S4: explicit per-menu permission + test asserts C-type child rejected |
| T-32-04 | Tampering (file upload) | excel_handler.go | mitigate | P1-S7: PK\x03\x04 magic check + test asserts non-PK rejected |
| T-32-SC | Tampering | (no new packages) | accept | Phase 32 uses only existing dependencies per RESEARCH.md audit |
</threat_model>

<verification>
- [ ] `go build ./...` exits 0
- [ ] `go vet ./...` exits 0
- [ ] `go test -count=1 ./pkg/crypto/ ./pkg/middleware/ ./internal/api/v1/operations/` exits 0
- [ ] All 4 new test files exist
- [ ] No production file modified for P1-S3, P1-S4, P1-S7 (only tests added)
- [ ] P1-S2 production file modified: maxTimeDiff literal replaced with config field
- [ ] All 3 config files declare `security.replay_window_sec`
</verification>

<success_criteria>
- P1-S2 fully resolved (code + config + test)
- P1-S3, P1-S4, P1-S7 regression-tested (test only)
- Build, vet, targeted tests all pass
- Wave 1 commits: `fix(security): P1-S2 tighten replay window to ±60s`, `test(security): P1-S3/S4/S7 regression coverage`
</success_criteria>

<output>
Create `.planning/phases/32-v1-14-p1-p2/32-01-SUMMARY.md` when done
</output>