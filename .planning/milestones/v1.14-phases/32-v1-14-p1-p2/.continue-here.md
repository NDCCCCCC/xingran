---
context: phase
phase: 32
phase_name: v1-14-p1-p2
task: 5 of 7
total_tasks: 7
status: in_progress
last_updated: 2026-06-13T06:30:38.758Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Token quota management — Phase 32 execution exhausted Claude API token quota mid-plan. Resume with fresh quota context or use /gsd:execute-phase with --interactive flag for lower token consumption.
- [ ] CONSTRAINT: Build failure cascade — P2-A2 cache key removal created undefined references (GetDictDataByTypeKey, GetMenuTreeKey). Fix cache_keys.go and call sites BEFORE running any build tests.

**Do not proceed until all boxes are checked.**

## Current Position

**Phase**: 32 — v1.14 P1 重构与 P2 架构优化
**Wave**: 5 of 7 (Core Refactor: A1/A2/A3/A5)
**Status**: 4/7 waves complete (Waves 1-4: all P1 items closed), Wave 5 incomplete
**Issue**: Token quota exhausted during Wave 5 executor; build failing due to incomplete P2-A2 cache key migration

## Completed Work

**Waves 1-4: All 15 P1 Security/Concurrency/Business Items Closed**

- ✅ **Wave 1 (32-01)**: P1-S2 replay window ±60s config, P1-S3/S4/S7 regression tests
  - Commits: `f9ad0dd`, `b7587ac`, `b2c070d`
  - 3 new test files, request_encryption.go tightened to configurable window

- ✅ **Wave 2 (32-02)**: P1-S5 PBKDF2 600000 iterations, P1-S1/S6 JWT/password tests
  - Commits: `f1281f3`, `ea3f7ca`, `e750408`
  - DefaultPasswordConfig.Iterations: 100000 → 600000 (OWASP)
  - 3 new test files, chi-square bias test

- ✅ **Wave 3 (32-03)**: P1-C1..C6 concurrency coverage, P1-C6 N+1 → batch IN
  - Commits: `bc067a5`, `607e991`, `7639535`
  - 6 new test files (21 tests total), validateUniqueness refactored to lazy cache

- ✅ **Wave 4 (32-04)**: P1-B1 config invalidation test, P1-B2 buildDepartmentPaths verification
  - Commits: `d4adb50`, `d3d9ad7`, `f5d9729`
  - ConfigService cache invalidation callback test (4 sub-tests)
  - Verified single call site of buildDepartmentPaths

## Remaining Work

### Wave 5 (32-05) — INCOMPLETE (blocked by build failure)

**P2-A1 ✅ DONE**: Core struct split
- Created `internal/core/core_infra.go` (infra fields: DB, Cache, JWT, etc.)
- Created `internal/core/core_services.go` (service fields: UserService, RoleService, etc.)
- Modified `internal/core/core.go` to embed both via `*CoreInfra; *CoreServices`
- Commit: `96f4762`
- **Status**: Build green for Core split itself

**P2-A2 ❌ INCOMPLETE**: Cache key deduplication
- **What was done**: `CacheKey*` constants removed from `internal/services/data_cache_service.go`
- **What's broken**: 7 `*_cache_impl.go` files in `internal/services/system/` still reference old `services.CacheKey*` pattern
- **Missing**: Helper functions `GetDictDataByTypeKey(dictType string)`, `GetMenuTreeKey(includeHidden bool)` not added to `internal/services/system/cache_keys.go`
- **Build error**: 
  ```
  internal\services\system\dict_cache_impl.go:167:14: undefined: GetDictDataByTypeKey
  internal\services\system\menu_cache_impl.go:36:14: undefined: GetMenuTreeKey
  ```
- **Files needing fix**:
  - `department_cache_impl.go` - line 78: `services.CacheKeyDeptTreeSelect` → `CacheKeyDeptTree`
  - `dict_cache_impl.go` - lines 37, 96, 167, 238: `services.CacheKeyDictType`, `services.GetDictDataByTypeKey` → direct refs
  - `menu_cache_impl.go` - lines 36, 50, 80, 108, 109: `services.CacheKeyMenu*`, `services.GetMenuTreeKey` → direct refs
  - `post_cache_impl.go`, `role_cache_impl.go`, `user_cache_impl.go` - similar pattern

**P2-A3 ✅ VERIFIED**: user_service_optimized.go removal
- Verified absent via `test -f` check
- Already cleaned up in prior commit

**P2-A5 ❌ NOT STARTED**: role_service apperrors migration
- **Target**: `internal/services/system/role_service.go`
- **Current state**: 43 `fmt.Errorf` calls need audit
- **Required changes**:
  1. Add `RoleNameExists(name string)`, `RoleIsAdmin()` helpers to `pkg/errors/errors.go`
  2. Replace `fmt.Errorf("角色名称已存在: %s", name)` → `apperrors.NewRoleNameExists(name)`
  3. Replace `fmt.Errorf("不允许操作超管角色")` → `apperrors.NewRoleIsAdmin()`
  4. Keep `fmt.Errorf` only for opaque DB error wrapping
  5. Create `role_service_apperrors_test.go` with typed error assertion tests

### Waves 6-7 — NOT STARTED

- **Wave 6 (32-06)**: P2-A4 migration conflicts, P2-A7 subprocess Setpgid + reaper, P2-A8 Excel transaction wrapper
- **Wave 7 (32-07)**: P2-A6 AD module test coverage (LDAPClientIface, mock tests)

## Decisions Made

- **Struct embedding for Core split**: Chose embedding `*CoreInfra` and `*CoreServices` in `core.Core` instead of direct field composition. **Rationale**: Preserves backward compat via Go field promotion - existing code using `core.UserService`, `core.DB`, `core.Cache` continues to compile unchanged without router updates.
- **Sequential execution**: Accepted `parallelization=false` project setting; all commits go directly to `main` branch rather than worktree isolation.

## Blockers

1. **Build failure**: Undefined references to `GetDictDataByTypeKey` and `GetMenuTreeKey` in 7 cache impl files
   - **Workaround**: Add helper functions to `cache_keys.go` and update call sites to remove `services.` prefix
   - **Blocking**: Yes - cannot run tests or proceed to Wave 6 until build passes

2. **Token quota exhaustion**: Wave 5 executor hit 429 rate limit mid-task
   - **Workaround**: Resume with fresh token context or use `--interactive` mode for lower consumption
   - **Blocking**: No - handoff created for fresh session

## Required Reading (in order)

1. `.planning/phases/32-v1-14-p1-p2/CONTEXT.md` — Phase scope and P1/P2 item list
2. `.planning/phases/32-v1-14-p1-p2/32-RESEARCH.md` — Patterns and pitfalls (11/15 P1 already fixed)
3. `.planning/phases/32-v1-14-p1-p2/32-05-PLAN.md` — Wave 5 detailed plan
4. `.planning/phases/32-v1-14-p1-p2/32-01-SUMMARY.md` through `32-04-SUMMARY.md` — What was already completed
5. `internal/core/core_infra.go`, `core_services.go` — New Core split structure (reference for embedding pattern)

## Critical Anti-Patterns (do NOT repeat these)

- **Premature constant removal without call site updates**: P2-A2 removed `CacheKey*` constants from `data_cache_service.go` but failed to update 7 `*_cache_impl.go` files that still referenced them. This caused build cascade. **Mitigation**: Always grep for all usages of a constant before removing it; update all call sites atomically in same commit.

## Infrastructure State

- **Git branch**: `main` (all commits pushed to main, no feature branch)
- **Build status**: ❌ FAILING (undefined GetDictDataByTypeKey, GetMenuTreeKey)
- **Test status**: Unknown (cannot run tests while build fails)
- **Modified files**: 14 uncommitted files (cache impl files, Core split files, plus 1 frontend file)
- **Running services**: None (all background agents completed)

## Pre-Execution Critique Required

_N/A - pausing mid-execution, not between design and execution._

## Context Notes

Wave 5 executor (`a033e3ea09be9d0ed`) successfully completed P2-A1 Core split (commit `96f4762`) but hit token quota before starting Task 2 (P2-A2/A3/A5). The executor left `data_cache_service.go` with `CacheKey*` constants removed but call sites in `internal/services/system/` still referencing old `services.*` pattern.

Attempted inline fix to restore build state:
1. Restored `cache_keys.go` from git (removes broken helper functions)
2. Updated call sites to remove `services.` prefix
3. Added `GetMenuTreeKey(includeHidden bool)` helper
4. Hit sed syntax error and reverted `cache_keys.go` again

**Current state**: `cache_keys.go` restored to git HEAD (no helpers added), call sites partially updated (still broken references). Need clean slate approach:
- Add both helper functions cleanly to `cache_keys.go`
- Verify all 7 cache impl files reference constants correctly
- Complete P2-A5 apperrors migration
- Run `go build ./...` to verify
- Create `32-05-SUMMARY.md` and commit

**Key insight**: Wave 5 is the highest-risk wave (major refactor + apperrors migration). Should consider `--interactive` mode for remaining waves to allow checkpoint review between tasks.

## Next Action

**Start with: Fix P2-A2 build failure**

1. Add helper functions to `internal/services/system/cache_keys.go`:
   ```go
   func GetDictDataByTypeKey(dictType string) string {
       return CacheKeyDictData + ":" + dictType
   }
   func GetMenuTreeKey(includeHidden bool) string {
       if includeHidden {
           return CacheKeyMenuTree + ":all"
       }
       return CacheKeyMenuTree
   }
   ```

2. Update all 7 cache impl files to reference constants directly (no `services.` prefix, no `system.` prefix needed - they're in same `system` package)

3. Run `go build ./...` to verify green

4. Then proceed to P2-A5: add apperrors helpers, migrate `role_service.go`, create test

5. Create `32-05-SUMMARY.md` and commit

**Resume command**: `/gsd:resume-work` (reads `.planning/HANDOFF.json` and this `.continue-here.md`)

**Alternative if quota exhausted**: Use `/gsd:execute-phase 32 --wave 5 --interactive` to complete Wave 5 with checkpoints between tasks (lower token consumption, pair-programming style).
