---
phase: 32-v1-14-p1-p2
plan: 03
type: execute
wave: 3
depends_on:
  - 32-01
files_modified:
  - internal/services/addomain/sync_singleflight_test.go
  - internal/services/addomain/group_sync_threshold_test.go
  - internal/services/addomain/dept_ou_mapper_atomic_test.go
  - internal/collectors/port_collector_batch_test.go
  - internal/websocket/notice_hub_readpump_test.go
  - internal/services/operations/excel_service.go
  - internal/services/operations/excel_uniqueness_batch_test.go
autonomous: true
requirements:
  - P1-C1
  - P1-C2
  - P1-C3
  - P1-C4
  - P1-C5
  - P1-C6

must_haves:
  truths:
    - "SyncData called 10 times concurrently with same configID results in only 1 actual sync"
    - "handleDeletedGroups rejects when LDAP returns 0 entries"
    - "handleDeletedGroups rejects when delete ratio exceeds 50% threshold"
    - "UpsertMapping wraps delete+insert in single transaction; rollback on mid-tx failure"
    - "Port collector batches 100 inserts per call (single statement, not 100 separate)"
    - "WebSocket readPump closes stale connection after ping timeout"
    - "validateUniqueness issues a single SQL query with IN clause, not N queries"
  artifacts:
    - path: "internal/services/addomain/sync.go"
      provides: "singleflight.Group deduplication (already fixed per commit 5c573c5)"
      contains: "syncGroup singleflight.Group"
    - path: "internal/services/addomain/group_sync_service.go"
      provides: "Threshold protection (already fixed per commit 887a438)"
      contains: "delete ratio"
    - path: "internal/services/addomain/dept_ou_mapper.go"
      provides: "Single transaction wrapper (already fixed per commit ffaecae)"
      contains: "Transaction(func(tx"
    - path: "internal/collectors/port_collector.go"
      provides: "Batch insert with CreateInBatches (already fixed per commit 12d3139)"
      contains: "CreateInBatches"
    - path: "internal/websocket/notice_hub.go"
      provides: "readPump goroutine (already fixed per commit a1e7324)"
      contains: "go client.readPump()"
    - path: "internal/services/operations/excel_service.go"
      provides: "validateUniqueness using single IN query (NOT YET FIXED — needs refactor)"
      contains: "WHERE.*IN"
    - path: "internal/services/operations/excel_uniqueness_batch_test.go"
      provides: "Test asserting single IN query, not N queries"
      contains: "TestValidateUniqueness_SingleBatchQuery"
  key_links:
    - from: "internal/services/addomain/sync.go"
      to: "singleflight.Group"
      via: "s.syncGroup.Do(key, func() ...)"
      pattern: "syncGroup\\.Do"
    - from: "internal/services/addomain/group_sync_service.go"
      to: "models.SysDeptGroup"
      via: "Deletion ratio check before bulk delete"
      pattern: "len\\(deleted\\).*total"
    - from: "internal/services/operations/excel_service.go"
      to: "gorm.DB"
      via: "Where(col).In(values) instead of per-row Count"
      pattern: "Where\\(.*\\)\\.In"
---

<objective>
Wave 3 — P1 并发与一致性. Six P1 items covering concurrency hardening across AD sync, port collector, WebSocket, and Excel import. FIVE items already fixed in prior commits (P1-C1 through P1-C5) — add regression tests only. ONE item (P1-C6 validateUniqueness N+1) still unfixed — refactor + add test.

Purpose: Lock down the concurrency boundary: prevent double AD sync (singleflight), mass-delete on LDAP outage (threshold), partial-mapping state on crash (transaction), connection-pool exhaustion (batched insert), zombie connections (readPump), and N+1 queries (batch IN).

Output:
- 1 production code edit (P1-C6 refactor)
- 6 new test files
- ~8 test functions
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-v1-14-p1-p2/CONTEXT.md
@.planning/phases/32-v1-14-p1-p2/32-RESEARCH.md
@.planning/phases/32-v1-14-p1-p2/32-VALIDATION.md

# Source-of-truth files to consult before editing
@internal/services/addomain/sync.go
@internal/services/addomain/group_sync_service.go
@internal/services/addomain/dept_ou_mapper.go
@internal/collectors/port_collector.go
@internal/websocket/notice_hub.go
@internal/services/operations/excel_service.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor validateUniqueness N+1 to batch IN query (P1-C6)</name>
  <files>internal/services/operations/excel_service.go</files>
  <read_first>
    - internal/services/operations/excel_service.go (lines 900-1000 — find validateUniqueness function and any helper functions it calls)
    - .planning/phases/32-v1-14-p1-p2/32-RESEARCH.md (P1-C6 section — current N+1 pattern at line 905-906)
    - .planning/codebase/CONVENTIONS.md (GORM usage patterns)
  </read_first>
  <action>
    Per RESEARCH.md, P1-C6 is the ONLY unfixed item in this wave. Refactor required.

    Refactor internal/services/operations/excel_service.go validateUniqueness (and any helper it delegates to):
    1. Identify the current per-row pattern: for each row, for each unique column, execute `db.Model(&Entity{}).Where("col = ?", val).Count(&count)`.
    2. Restructure to: (a) collect ALL unique values per column across all rows into slices; (b) for each column, execute ONE query `db.Model(&Entity{}).Where("col IN ?", values).Find(&existing)` to fetch existing rows; (c) build a set of "values that already exist" from the result; (d) check each row against the set.
    3. The number of DB round-trips for N rows × M unique columns should drop from N×M to M (one per column).
    4. Use GORM's `Where("col IN ?", values)` with `?` for slice binding. Verify the column names map to actual GORM field names (may need column name → struct field mapping, or use `.Where("col IN ?", values)` with explicit column name string).
    5. Maintain backward compatibility: signature of validateUniqueness must NOT change (callers depend on it).
    6. Keep error handling: if the batch query fails, return wrapped error.

    DO NOT touch other parts of ImportData or any unrelated logic in this file.
  </action>
  <acceptance_criteria>
    - internal/services/operations/excel_service.go: validateUniqueness function body no longer contains a per-row `Count` call in a loop (grep `Count\\(&` inside the function returns at most 0 or only the batch one)
    - internal/services/operations/excel_service.go: contains at least one `Where(.*).In\(` call (grep `Where\(.*\)\.In` matches)
    - `go build ./...` exits 0
    - `go vet ./...` exits 0
    - validateUniqueness function signature unchanged (callers in same file still compile)
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./... && grep -E "Where\(.*\)\.In" internal/services/operations/excel_service.go | head -3</automated>
  </verify>
  <done>
    P1-C6 refactored: N+1 → single IN query per unique column. Build passes. Code is ready for the test added in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add regression tests for P1-C1..C5 + P1-C6 batch query test</name>
  <files>internal/services/addomain/sync_singleflight_test.go, internal/services/addomain/group_sync_threshold_test.go, internal/services/addomain/dept_ou_mapper_atomic_test.go, internal/collectors/port_collector_batch_test.go, internal/websocket/notice_hub_readpump_test.go, internal/services/operations/excel_uniqueness_batch_test.go</files>
  <read_first>
    - internal/services/addomain/sync.go (find SyncData method, verify singleflight usage)
    - internal/services/addomain/group_sync_service.go (find handleDeletedGroups signature, find threshold constant)
    - internal/services/addomain/dept_ou_mapper.go (find UpsertMapping, verify transaction wrapper)
    - internal/collectors/port_collector.go (find SaveToDatabase, find batch size constant)
    - internal/websocket/notice_hub.go (find readPump method, find ping interval / pong wait)
    - internal/services/operations/excel_service.go (verify Task 1's refactor completed)
    - .planning/phases/32-v1-14-p1-p2/32-VALIDATION.md (32-03-01 through 32-03-06 rows for test names)
  </read_first>
  <action>
    Per RESEARCH.md, P1-C1..C5 already fixed. P1-C6 was just refactored in Task 1. All 6 tests go here.

    Create internal/services/addomain/sync_singleflight_test.go:
    Test `TestSyncData_ConcurrentCallsDeduplicated`:
    - Construct SyncService with a real (or mock) DB and a mock doSync function that increments an atomic counter and sleeps 100ms.
    - Fire 10 goroutines simultaneously, each calling `svc.SyncData(ctx, config, "type")` with the same config.ID.
    - Wait for all 10 to return.
    - Assert: atomic counter is exactly 1, not 10.
    - Use `sync.WaitGroup` + `time.Sleep(small)` or a barrier channel to align goroutine starts.

    Create internal/services/addomain/group_sync_threshold_test.go:
    Test `TestHandleDeletedGroups_RejectsEmptyLDAP`:
    - Call handleDeletedGroups with an empty LDAP entry slice (len=0).
    - Assert: error returned with message indicating "LDAP" or "empty".

    Test `TestHandleDeletedGroups_RejectsOverThreshold`:
    - Setup: 20 existing DB rows; pass LDAP entries that would delete 15 (75%, over threshold).
    - Assert: error returned indicating ratio exceeded.
    - Second case: delete 5 of 20 (25%, under threshold) — assert no error and 5 rows deleted.

    Create internal/services/addomain/dept_ou_mapper_atomic_test.go:
    Test `TestUpsertMapping_AtomicDeleteInsert`:
    - Use SQLite in-memory DB (`gorm.io/driver/sqlite`).
    - Pre-populate: 2 existing mapping rows.
    - Setup: a DB hook or mock that causes the INSERT to fail mid-transaction (e.g., add a unique constraint violation on the second insert).
    - Call UpsertMapping with new mappings.
    - Assert: returns error.
    - Assert: DB state shows the original 2 mappings STILL EXIST (delete was rolled back atomically with the failed insert).

    Create internal/collectors/port_collector_batch_test.go:
    Test `TestPortCollector_SaveToDatabase_Batches100PerCall`:
    - Construct 250 port status records.
    - Use SQL mock or query counter (gorm.io/driver/sqlite with a counting hook).
    - Call SaveToDatabase.
    - Assert: number of INSERT statements is ceil(250/100) = 3 (or batch_size = 100), not 250.

    Create internal/websocket/notice_hub_readpump_test.go:
    Test `TestClient_ReadPump_ClosesStaleConnection`:
    - Construct a Hub + Client with a mock connection (use `net.Pipe()` or `httptest.NewServer` with WS).
    - Start readPump goroutine.
    - Do NOT write any ping/pong from client side; advance time past ping interval.
    - Assert: client is unregistered from hub within timeout.
    - Use `time.Sleep` or a controllable time source.

    Create internal/services/operations/excel_uniqueness_batch_test.go:
    Test `TestValidateUniqueness_SingleBatchQuery`:
    - Setup mock DB that counts SELECT queries.
    - Call validateUniqueness with 100 rows and 2 unique columns.
    - Assert: query count for the unique-column check is 2 (one IN query per column), not 200.
    - Positive case: existing values correctly identified as duplicates.
    - Negative case: non-existing values not flagged.

    Use `package <pkgname>` (same package, not _test) where needed to access unexported helpers like `handleDeletedGroups`, `UpsertMapping`, `validateUniqueness`.
  </action>
  <acceptance_criteria>
    - All 6 test files exist (grep `<test_func_name>` returns matches in expected files)
    - `go test -count=1 -run "TestSyncData_ConcurrentCallsDeduplicated" ./internal/services/addomain/` exits 0
    - `go test -count=1 -run "TestHandleDeletedGroups" ./internal/services/addomain/` exits 0
    - `go test -count=1 -run "TestUpsertMapping_AtomicDeleteInsert" ./internal/services/addomain/` exits 0
    - `go test -count=1 -run "TestPortCollector_SaveToDatabase_Batches100PerCall" ./internal/collectors/` exits 0
    - `go test -count=1 -run "TestClient_ReadPump_ClosesStaleConnection" ./internal/websocket/` exits 0
    - `go test -count=1 -run "TestValidateUniqueness_SingleBatchQuery" ./internal/services/operations/` exits 0
    - `go build ./...` exits 0
    - `go vet ./...` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./... && go test -count=1 -run "TestSyncData_ConcurrentCallsDeduplicated|TestHandleDeletedGroups|TestUpsertMapping_AtomicDeleteInsert|TestPortCollector_SaveToDatabase_Batches100PerCall|TestClient_ReadPump_ClosesStaleConnection|TestValidateUniqueness_SingleBatchQuery" ./internal/services/addomain/ ./internal/collectors/ ./internal/websocket/ ./internal/services/operations/ -v</automated>
  </verify>
  <done>
    P1-C1..C6 fully resolved: C6 refactored + tested; C1-C5 regression-tested. Wave 3 complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduler → AD sync | concurrent triggers must not double-execute |
| LDAP outage → mass delete | threshold must protect DB |
| delete+insert → mapping state | must be atomic in single transaction |
| port collector → DB pool | batched inserts protect pool exhaustion |
| WS client → server | zombie connections must be reaped |
| Excel import → uniqueness | N+1 queries must be batched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-08 | Tampering (race) | sync.go | mitigate | P1-C1: singleflight + concurrent test |
| T-32-09 | Tampering (mass delete) | group_sync_service.go | mitigate | P1-C2: 50% threshold + 2 tests |
| T-32-10 | Tampering (partial state) | dept_ou_mapper.go | mitigate | P1-C3: single transaction + atomic test |
| T-32-11 | DoS (pool exhaustion) | port_collector.go | mitigate | P1-C4: CreateInBatches + SQL counter test |
| T-32-12 | DoS (FD leak) | notice_hub.go | mitigate | P1-C5: readPump + ping/pong + test |
| T-32-13 | DoS (N+1 latency) | excel_service.go | mitigate | P1-C6: batch IN + query counter test |
</threat_model>

<verification>
- [ ] `go build ./...` exits 0
- [ ] `go vet ./...` exits 0
- [ ] All 6 tests pass in targeted mode
- [ ] validateUniqueness refactored (single IN pattern present)
- [ ] All 5 already-fixed items have regression tests
</verification>

<success_criteria>
- P1-C6 refactored + tested
- P1-C1..C5 regression-tested
- Wave 3 commits: `fix(perf): P1-C6 batch uniqueness validation`, `test(concurrency): P1-C1..C5 regression coverage`
</success_criteria>

<output>
Create `.planning/phases/32-v1-14-p1-p2/32-03-SUMMARY.md` when done
</output>