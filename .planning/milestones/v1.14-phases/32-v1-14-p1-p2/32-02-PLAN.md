---
phase: 32-v1-14-p1-p2
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - internal/core/security/password.go
  - internal/core/security/password_owasp_test.go
  - internal/core/security/random_password_bias_test.go
  - pkg/crypto/sm2_jwt.go
  - pkg/crypto/sm2_jwt_alg_test.go
autonomous: true
requirements:
  - P1-S1
  - P1-S5
  - P1-S6

must_haves:
  truths:
    - "DefaultPasswordConfig.Iterations is 600000 (OWASP 2023 baseline)"
    - "Password hashes created with old 100000 iterations still verify correctly"
    - "Newly created password hashes use 600000 iterations"
    - "Random password generation shows uniform distribution across charset (chi-square or KS test passes)"
    - "JWT with alg=none is rejected during parse"
    - "JWT with alg=HS256 confusion is rejected during parse"
    - "JWT with correct alg=SM2 still verifies successfully"
  artifacts:
    - path: "internal/core/security/password.go"
      provides: "DefaultPasswordConfig.Iterations = 600000 + comment explaining OWASP"
      contains: "600000"
    - path: "internal/core/security/password_owasp_test.go"
      provides: "Tests for new 600k default + backward compat with 100k hashes"
      contains: "TestPasswordManager_DefaultIterationsAre600k"
    - path: "internal/core/security/random_password_bias_test.go"
      provides: "Statistical test for no modulo bias in GenerateRandomPassword"
      contains: "TestGenerateRandomPassword_NoBias"
    - path: "pkg/crypto/sm2_jwt.go"
      provides: "alg header whitelist check (already fixed per commit 64b1b40)"
      contains: "unexpected JWT signing algorithm"
    - path: "pkg/crypto/sm2_jwt_alg_test.go"
      provides: "Tests for alg=none and alg=HS256 confusion rejection"
      contains: "TestSM2JWT_RejectsAlgNone"
  key_links:
    - from: "internal/core/security/password.go"
      to: "internal/core/security/password.go:VerifyPassword"
      via: "Hash format embeds iterations; VerifyPassword reads embedded value"
      pattern: "iterations.*extract"
    - from: "pkg/crypto/sm2_jwt.go"
      to: "sm2Method.Alg()"
      via: "header.Alg compared against expected alg in parse function"
      pattern: "header\\.Alg"
---

<objective>
Wave 2 — P1 密码强度迁移 + JWT 算法校验. Three P1 items centered on authentication: (a) P1-S1 already-fixed JWT alg validation — add 2 regression tests; (b) P1-S5 partial — bump DefaultPasswordConfig.Iterations from 100000 to 600000 with backward compat; (c) P1-S6 already-fixed random password no-bias — add chi-square/KS distribution test.

Purpose: Strengthen the authentication boundary to OWASP 2023 baseline. Critical: backward compatibility for existing 100k-hashed users must be preserved (VerifyPassword reads iterations from the stored hash format).

Output:
- 2 production code edits (P1-S5 iterations bump; P1-S1 stays untouched — already fixed)
- 3 new test files
- 5 new test functions total
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-v1-14-p1-p2/CONTEXT.md
@.planning/phases/32-v1-14-p1-p2/32-RESEARCH.md
@.planning/phases/32-v1-14-p1-p2/32-VALIDATION.md

# Source-of-truth files to consult before editing
@internal/core/security/password.go
@pkg/crypto/sm2_jwt.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump PBKDF2 iterations to 600000 (P1-S5) + add OWASP + backward-compat tests</name>
  <files>internal/core/security/password.go, internal/core/security/password_owasp_test.go</files>
  <read_first>
    - internal/core/security/password.go (lines 20-50 for DefaultPasswordConfig; lines 100-170 for HashPassword + VerifyPassword; verify hash format embeds iteration count)
    - .planning/phases/32-v1-14-p1-p2/32-RESEARCH.md (Pitfall 1 — login latency budget)
  </read_first>
  <action>
    Edit internal/core/security/password.go:
    1. Locate the DefaultPasswordConfig struct definition (line ~20-35 per RESEARCH.md). Current Iterations = 100000.
    2. Change `Iterations: 100000` to `Iterations: 600000`.
    3. Add a comment line above the field: `// OWASP 2023 baseline for PBKDF2-SM3; backward-compat with 100k hashes via embedded iteration count`
    4. Verify HashPassword uses `pm.config.Iterations` (the runtime field, not the literal). If any place hardcodes 100000, replace with `pm.config.Iterations`.
    5. Verify VerifyPassword extracts iterations from the stored hash format (e.g., from the prefix `$pbkdf2-sm3$ITER$SALT$HASH`) and uses that value — DO NOT change VerifyPassword to always use 600000.

    Create internal/core/security/password_owasp_test.go with 2 tests:

    Test 1: `TestPasswordManager_DefaultIterationsAre600k`
    - Create a PasswordManager with default config
    - Hash a password
    - Parse the resulting hash string, extract the iteration count (use the same parsing logic VerifyPassword uses — make it accessible via package function or test in same package)
    - Assert iteration count == 600000
    - Second assertion: assert `DefaultPasswordConfig.Iterations == 600000`

    Test 2: `TestPasswordManager_VerifyBackwardCompat_100k`
    - Construct a hash using iterations=100000 (call pbkdf2SM3 directly with 100000 OR use a fixed test fixture hash string)
    - Verify that VerifyPassword succeeds against this hash when given the correct plaintext
    - Assert: VerifyPassword is invoked; result is nil error (password matches)
    - This proves backward compat — existing users with old hashes can still log in.
    - For test speed: use a short password (e.g., "test123") and consider lowering iterations only in a fixture (don't change production code's verify behavior).

    Note: tests in the same package (not `_test` package) so they can access unexported `pbkdf2SM3`. Use `package security` not `package security_test`.
  </action>
  <acceptance_criteria>
    - internal/core/security/password.go: literal `Iterations: 100000` replaced with `Iterations: 600000` (grep `Iterations:\s*100000` returns 0 matches in password.go; `Iterations:\s*600000` returns 1 match)
    - internal/core/security/password.go: contains comment with "OWASP" near DefaultPasswordConfig definition
    - internal/core/security/password_owasp_test.go exists with TestPasswordManager_DefaultIterationsAre600k AND TestPasswordManager_VerifyBackwardCompat_100k (grep returns both function names)
    - `go test -count=1 -run "TestPasswordManager_DefaultIterationsAre600k|TestPasswordManager_VerifyBackwardCompat_100k" ./internal/core/security/ -v` exits 0 with both tests passing
    - `go build ./...` exits 0
    - `go vet ./...` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./... && go test -count=1 -run "TestPasswordManager_DefaultIterationsAre600k|TestPasswordManager_VerifyBackwardCompat_100k" ./internal/core/security/ -v</automated>
  </verify>
  <done>
    P1-S5 fully resolved: DefaultPasswordConfig.Iterations = 600000, OWASP comment present, hash format preserves backward compat (existing 100k hashes still verify), 2 tests prove both behaviors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add JWT alg confusion regression tests + random password bias test (P1-S1 + P1-S6)</name>
  <files>pkg/crypto/sm2_jwt_alg_test.go, internal/core/security/random_password_bias_test.go</files>
  <read_first>
    - pkg/crypto/sm2_jwt.go (find ValidateTokenWithSM2 / parse function, expected alg constant, signature verification — lines 220-280 per RESEARCH.md)
    - internal/core/security/password.go (find GenerateRandomPassword function — lines 140-170 per RESEARCH.md; verify it uses crypto/rand.Int rejection sampling per commit 07f210c)
    - .planning/phases/32-v1-14-p1-p2/32-RESEARCH.md (Pattern 1 — alg=none test example)
  </read_first>
  <action>
    Per RESEARCH.md, P1-S1 and P1-S6 are ALREADY FIXED. The task is to add regression tests ONLY.

    Create pkg/crypto/sm2_jwt_alg_test.go with 3 tests:

    Test 1: `TestSM2JWT_RejectsAlgNone`
    - Build a JWT manually: header `{"alg":"none","typ":"JWT"}` base64url-encoded, payload `{"sub":"admin","exp":99999999999}` base64url-encoded, signature empty string.
    - Call `ValidateTokenWithSM2(token, publicKey)` (or whatever the actual function name is — read pkg/crypto/sm2_jwt.go first).
    - Assert: returned error is non-nil and contains "algorithm" or "alg".

    Test 2: `TestSM2JWT_RejectsAlgHS256Confusion`
    - Build a JWT with header `{"alg":"HS256","typ":"JWT"}`, payload as above, signature = HMAC-SHA256(payload, publicKey_as_bytes).
    - This is the classic confusion attack: client sends HS256 token using public key as HMAC secret; server must reject because expected alg is SM2.
    - Call ValidateTokenWithSM2.
    - Assert: error returned (specifically "unexpected JWT signing algorithm").

    Test 3: `TestSM2JWT_AcceptsCorrectSM2Alg` (positive test)
    - Build a valid SM2-signed JWT (use existing helpers in package to mint a token).
    - Call ValidateTokenWithSM2.
    - Assert: no error; claims returned.

    Use a test helper to load a fixed test public key (PEM-encoded SM2 key). Can use `t.TempDir()` to write a temp PEM file or use a fixed test key string.

    Create internal/core/security/random_password_bias_test.go with 1 statistical test:

    Test: `TestGenerateRandomPassword_NoBiasDistribution`
    - Call GenerateRandomPassword(N) for N=10000 with length 16
    - Count character frequency by category (uppercase, lowercase, digit, symbol)
    - Each password should have approximately equal count of each category (~4 of each in a length-16 password with 4 categories)
    - Use chi-square test: compute chi-square statistic, assert it is below critical value at alpha=0.01
    - OR simpler: assert each category count is within [expected-3*sqrt(expected), expected+3*sqrt(expected)] (3-sigma rule) across 10000 passwords
    - Use `math.Abs` and a `for _, p := range passwords` loop to count.
    - Test must complete in < 5 seconds (avoid overly large N).
  </action>
  <acceptance_criteria>
    - pkg/crypto/sm2_jwt_alg_test.go exists with TestSM2JWT_RejectsAlgNone, TestSM2JWT_RejectsAlgHS256Confusion, TestSM2JWT_AcceptsCorrectSM2Alg (grep returns 3 function names)
    - internal/core/security/random_password_bias_test.go exists with TestGenerateRandomPassword_NoBiasDistribution
    - `go test -count=1 -run "TestSM2JWT_RejectsAlgNone|TestSM2JWT_RejectsAlgHS256Confusion" ./pkg/crypto/ -v` exits 0 — both alg-rejection tests pass
    - `go test -count=1 -run "TestSM2JWT_AcceptsCorrectSM2Alg" ./pkg/crypto/ -v` exits 0 — positive test passes
    - `go test -count=1 -run "TestGenerateRandomPassword_NoBiasDistribution" ./internal/core/security/ -v` exits 0 — bias test passes
    - `go build ./...` exits 0 (no production code touched)
    - `go vet ./...` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./... && go test -count=1 -run "TestSM2JWT_RejectsAlgNone|TestSM2JWT_RejectsAlgHS256Confusion|TestSM2JWT_AcceptsCorrectSM2Alg|TestGenerateRandomPassword_NoBiasDistribution" ./pkg/crypto/ ./internal/core/security/ -v</automated>
  </verify>
  <done>
    P1-S1 and P1-S6 fully regression-tested. Production code untouched. Wave 2 complete (combined with Plan 01).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server (JWT token) | alg header must be validated before signature verification |
| user → password storage | PBKDF2 iterations must be sufficient to deter offline brute-force |
| user → generated password | charset sampling must be unbiased (no modulo skew) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05 | Spoofing (alg confusion) | sm2_jwt.go | mitigate | P1-S1: alg whitelist + 2 regression tests |
| T-32-06 | Info Disclosure (weak hash) | password.go | mitigate | P1-S5: PBKDF2 600k + backward-compat test |
| T-32-07 | Info Disclosure (modulo bias) | password.go | mitigate | P1-S6: rejection sampling + chi-square test |
</threat_model>

<verification>
- [ ] `go build ./...` exits 0
- [ ] `go vet ./...` exits 0
- [ ] Default iterations == 600000 (grep assertion)
- [ ] Old 100k hash verifies correctly (test passes)
- [ ] alg=none rejected (test passes)
- [ ] alg=HS256 rejected (test passes)
- [ ] Correct alg accepted (test passes)
- [ ] Random password distribution uniform (test passes)
</verification>

<success_criteria>
- P1-S5 fully resolved (bump + tests)
- P1-S1, P1-S6 regression-tested
- All Wave 2 commits pass build + vet + targeted tests
- Wave 2 commits: `fix(security): P1-S5 bump PBKDF2 iterations to 600000 (OWASP)`, `test(security): P1-S1/S6 regression coverage`
</success_criteria>

<output>
Create `.planning/phases/32-v1-14-p1-p2/32-02-SUMMARY.md` when done
</output>