---
phase: 32-v1-14-p1-p2
plan: 05
type: execute
wave: 5
depends_on:
  - 32-04
files_modified:
  - internal/core/core.go
  - internal/core/core_infra.go
  - internal/core/core_services.go
  - internal/services/cache_keys.go
  - internal/services/data_cache_service.go
  - pkg/errors/errors.go
  - internal/services/system/role_service.go
  - internal/services/system/role_service_apperrors_test.go
  - internal/core/core_split_compat_test.go
autonomous: true
requirements:
  - P2-A1
  - P2-A2
  - P2-A3
  - P2-A5

must_haves:
  truths:
    - "core.Core embeds CoreInfra and CoreServices; existing field access syntax (core.UserService) still compiles"
    - "CacheKey* constants in data_cache_service.go are removed; all call sites use the single cache_keys.go in services/system/"
    - "user_service_optimized.go does not exist"
    - "role_service.go uses apperrors.New* helpers instead of fmt.Errorf for business errors"
    - "go build ./...` succeeds after Core split (no broken router setup)"
  artifacts:
    - path: "internal/core/core.go"
      provides: "Core struct embedding CoreInfra + CoreServices (preserves field access)"
      contains: "type Core struct"
    - path: "internal/core/core_infra.go"
      provides: "CoreInfra struct with infra fields (DB, Cache, JWT, Pwd, Config, SM4, Scheduler, RPA)"
      contains: "type CoreInfra struct"
    - path: "internal/core/core_services.go"
      provides: "CoreServices struct with service fields (UserService, RoleService, MenuService, etc.)"
      contains: "type CoreServices struct"
    - path: "internal/services/system/cache_keys.go"
      provides: "Single source of truth for cache keys"
      contains: "BuildDictDataCacheKey"
    - path: "internal/services/data_cache_service.go"
      provides: "DataCacheService struct WITHOUT CacheKey* constants (moved/removed)"
      contains: "DataCacheService"
    - path: "internal/services/system/role_service.go"
      provides: "Uses apperrors.New* helpers (no fmt.Errorf for business errors)"
      contains: "apperrors\\."
    - path: "internal/services/system/role_service_apperrors_test.go"
      provides: "Test asserting role_service uses apperrors, not fmt.Errorf"
      contains: "TestRoleService_UsesAppErrors"
    - path: "internal/core/core_split_compat_test.go"
      provides: "Compile-level backward compat test"
      contains: "TestCoreSplit_BackwardCompat"
  key_links:
    - from: "internal/core/core.go"
      to: "internal/core/core_infra.go"
      via: "struct embedding *CoreInfra"
      pattern: "\\*CoreInfra"
    - from: "internal/core/core.go"
      to: "internal/core/core_services.go"
      via: "struct embedding *CoreServices"
      pattern: "\\*CoreServices"
    - from: "internal/services/data_cache_service.go"
      to: "internal/services/system/cache_keys.go"
      via: "calls BuildDictDataCacheKey instead of inline concatenation"
      pattern: "BuildDictDataCacheKey"
    - from: "internal/services/system/role_service.go"
      to: "pkg/errors/errors.go"
      via: "apperrors.New* / Wrap calls"
      pattern: "apperrors\\.New"
---

<objective>
Wave 5 — P2 架构债（重构）: Four architectural-debt items in dependency order. (a) P2-A1: split god struct `core.Core` (18+ fields) into `CoreInfra` + `CoreServices` via embedding (preserves backward compat). (b) P2-A2: dedupe cache keys — single source of truth in `services/system/cache_keys.go`. (c) P2-A3: verify `user_service_optimized.go` already removed. (d) P2-A5: migrate `role_service.go` from `fmt.Errorf` to `apperrors.New*` helpers.

Purpose: Reduce structural complexity, eliminate parallel cache key systems, unify error handling for HTTP mapping. The dependency order A1 → A2 → A5 must hold (A5 uses Core services; A2 dedup must happen before A5 touches cache paths).

Output:
- 3 new core files (core_infra.go, core_services.go, core_split_compat_test.go)
- 1 modified core.go (embed fields)
- 2 modified cache files (cache_keys.go + data_cache_service.go)
- 2 new role_service helpers in pkg/errors/errors.go (RoleNameExists, RoleIsAdmin)
- 1 modified role_service.go (replace fmt.Errorf with apperrors)
- 1 new test file
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-v1-14-p1-p2/CONTEXT.md
@.planning/phases/32-v1-14-p1-p2/32-RESEARCH.md
@.planning/phases/32-v1-14-p1-p2/32-VALIDATION.md

# Source-of-truth files
@internal/core/core.go
@internal/services/cache_keys.go
@internal/services/data_cache_service.go
@internal/services/system/role_service.go
@pkg/errors/errors.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split core.Core into CoreInfra + CoreServices (P2-A1)</name>
  <files>internal/core/core.go, internal/core/core_infra.go, internal/core/core_services.go, internal/core/core_split_compat_test.go</files>
  <read_first>
    - internal/core/core.go (full file; identify all 18+ fields and categorize: infra vs services)
    - .planning/phases/32-v1-14-p1-p2/32-RESEARCH.md (P2-A1 section + Pitfall 2 — embedding pattern)
    - .planning/codebase/ARCHITECTURE.md (Core DI pattern)
  </read_first>
  <action>
    CRITICAL: use struct embedding to preserve backward compat. NO router setup changes needed.

    Step 1 — Categorize fields in core.go:
    - INFRA fields (DB, Cache, JWTManager, PwdManager, Config, SM4Cipher, Scheduler, RPAScalingService, any infrastructure): belong in CoreInfra
    - SERVICE fields (UserService, RoleService, MenuService, DeptService, PostService, DictService, ConfigService, all device/scheduler services, all AD services): belong in CoreServices
    - Read core.go line-by-line; make a list.

    Step 2 — Create internal/core/core_infra.go:
    ```go
    package core

    type CoreInfra struct {
        Config           *config.Config
        DB               *db.Database
        Cache            cache.Cache
        JWTManager       *security.JWTManager
        PwdManager       *security.PasswordManager
        SM4Cipher        crypto.SM4Cipher
        Scheduler        *scheduler.Engine
        // ... other infra fields
    }
    ```
    Plus a constructor `NewCoreInfra(...) *CoreInfra` if needed (optional — fields can be set inline).

    Step 3 — Create internal/core/core_services.go:
    ```go
    package core

    type CoreServices struct {
        UserService   system.UserService
        RoleService   system.RoleService
        MenuService   system.MenuService
        // ... other services
    }
    ```
    Plus a constructor `NewCoreServices(...) *CoreServices` (optional).

    Step 4 — Edit internal/core/core.go:
    - Remove all categorized fields from the Core struct.
    - Add embedded fields: `*CoreInfra; *CoreServices`.
    - Keep the existing constructor `NewCore(...)` working: it must populate both `core.CoreInfra` and `core.CoreServices` before returning.
    - All callers (routers) that use `core.UserService` will compile UNCHANGED because of embedding (field promotion).

    Step 5 — Create internal/core/core_split_compat_test.go:
    Test `TestCoreSplit_BackwardCompat`:
    - Construct a Core with all infra + services wired.
    - Access `core.UserService`, `core.RoleService`, `core.DB`, `core.Cache` etc. via field syntax.
    - Assert: all accessors return non-nil.
    - This is a COMPILE-TIME test primarily; runtime assertions are secondary.
    - Compile success is the main proof.

    Step 6 — Run `go build ./...`. Fix any compilation errors that arise from miscategorization (a field that should be infra was put in services, or vice versa).
  </action>
  <acceptance_criteria>
    - internal/core/core_infra.go exists with `type CoreInfra struct` (grep matches)
    - internal/core/core_services.go exists with `type CoreServices struct` (grep matches)
    - internal/core/core.go: contains both `*CoreInfra` and `*CoreServices` embedded (grep `Core struct` then check embedded fields)
    - internal/core/core.go: 18+ categorized fields removed from Core struct (grep shows field count reduced)
    - internal/core/core_split_compat_test.go exists with TestCoreSplit_BackwardCompat
    - `go build ./...` exits 0 (proves backward compat preserved — no router changes needed)
    - `go vet ./...` exits 0
    - `go test -count=1 -run "TestCoreSplit_BackwardCompat" ./internal/core/ -v` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./... && go test -count=1 -run "TestCoreSplit_BackwardCompat" ./internal/core/ -v</automated>
  </verify>
  <done>
    P2-A1 fully resolved: Core split into CoreInfra + CoreServices via embedding; build green; backward compat preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dedupe cache keys + verify P2-A3 + migrate role_service to apperrors (P2-A2 + P2-A3 + P2-A5)</name>
  <files>internal/services/cache_keys.go, internal/services/data_cache_service.go, pkg/errors/errors.go, internal/services/system/role_service.go, internal/services/system/role_service_apperrors_test.go</files>
  <read_first>
    - internal/services/cache_keys.go (find all CacheKey* constants and helper functions)
    - internal/services/data_cache_service.go (find duplicate CacheKey* constants — should be removed)
    - internal/services/system/role_service.go (find all fmt.Errorf calls — 43 per RESEARCH.md)
    - pkg/errors/errors.go (find existing helpers like RoleNotFound, RoleHasUsers to understand naming convention)
    - .planning/phases/32-v1-14-p1-p2/32-RESEARCH.md (P2-A2 + P2-A3 + P2-A5 sections + Pattern 3 apperrors usage example)
  </read_first>
  <action>
    Sub-task A — P2-A3 (already done per commit 3bdd3fc):
    - Run `test -f internal/services/system/user_service_optimized.go`
    - Assert: returns non-zero (file does NOT exist).
    - No action needed beyond verification.

    Sub-task B — P2-A2 cache key dedup:
    - Identify which file is the SINGLE source of truth. Per RESEARCH.md, `internal/services/system/cache_keys.go` is newer and uses `BuildDictDataCacheKey` style. The `internal/services/data_cache_service.go` file is LEGACY with 21 `CacheKey*` constants.
    - Edit `internal/services/data_cache_service.go`: REMOVE all 21 `CacheKey*` constants that duplicate ones in `system/cache_keys.go`.
    - Replace internal usages of removed constants with calls to `system.BuildDictDataCacheKey(...)` or equivalent helpers from `system/cache_keys.go`.
    - Add import alias if needed to avoid cycle.
    - Verify by `grep -rn "CacheKey" internal/services/` returns references only in `system/cache_keys.go` (definitions) and in non-legacy call sites.

    Sub-task C — P2-A5 apperrors migration for role_service:
    - Edit pkg/errors/errors.go: add 2 new helpers if not present:
      - `RoleNameExists(name string) *AppError` returning `CodeRecordExists` with message "角色名称已存在: " + name
      - `RoleIsAdmin() *AppError` returning `CodeForbidden` with message "不允许操作超管角色"
    - Existing helpers to reuse: `RoleNotFound()`, `RoleHasUsers()`, `RoleHasMenus()`, `RoleHasDepts()`, `RoleIsSuper()` — verify these exist before adding new ones.
    - Edit internal/services/system/role_service.go: replace `fmt.Errorf("...: %w", err)` calls that represent KNOWN business errors (duplicate name/key, role-not-found, has-users, has-menus, etc.) with `apperrors.New*` or `apperrors.Wrap(..., code, msg)`.
    - Keep `fmt.Errorf` only for:
      - Wrapping DB errors where the underlying cause is opaque (`fmt.Errorf("查询角色失败: %w", err)`)
      - Wrapping unexpected internal errors
    - DO NOT change function signatures.
    - DO NOT change role_handler.go (the existing `response.Error(c, err)` already handles AppError).

    Sub-task D — Create internal/services/system/role_service_apperrors_test.go:
    Test `TestRoleService_UsesAppErrors`:
    - Setup: role_service with mock DB that returns "duplicate key" error for name insert.
    - Call CreateRole with duplicate name.
    - Assert: returned error is `*apperrors.AppError` (use type assertion), and error.Code == `apperrors.CodeRecordExists`.
    - Second case: CreateRole with non-existent role ID → error is `*apperrors.AppError` with `CodeRoleNotFound`.
    - Third case: an internal DB error → returned error wraps DB error via fmt.Errorf (still uses %w format), but the test verifies it is NOT an AppError.

    Use `package system` (same package) to access unexported helpers.
  </action>
  <acceptance_criteria>
    - `test -f internal/services/system/user_service_optimized.go` returns non-zero (file absent) — P2-A3 verified
    - internal/services/data_cache_service.go: 0 `CacheKey*` constants remain (grep `const CacheKey` returns 0)
    - internal/services/system/cache_keys.go: contains `BuildDictDataCacheKey` function
    - pkg/errors/errors.go: contains `RoleNameExists` and `RoleIsAdmin` helpers (or equivalent)
    - internal/services/system/role_service.go: contains apperrors.New / apperrors.Wrap calls (grep `apperrors\\.` returns matches)
    - internal/services/system/role_service_apperrors_test.go exists with TestRoleService_UsesAppErrors
    - `go test -count=1 -run "TestRoleService_UsesAppErrors" ./internal/services/system/ -v` exits 0
    - `go build ./...` exits 0
    - `go vet ./...` exits 0
  </acceptance_criteria>
  <verify>
    <automated>test -f internal/services/system/user_service_optimized.go || echo "P2-A3 OK: optimized removed"; grep -c "^const CacheKey" internal/services/data_cache_service.go; go build ./... && go vet ./... && go test -count=1 -run "TestRoleService_UsesAppErrors" ./internal/services/system/ -v</automated>
  </verify>
  <done>
    P2-A1, A2, A3, A5 all resolved. Core split, cache keys deduped, apperrors unified, user_service_optimized verified absent. Wave 5 complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| startup → Core init | god struct hidden coupling risks untracked dependencies |
| service → cache | parallel key systems risk collision bugs |
| service → handler | business errors must map to HTTP codes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-16 | DoS (coupling) | core.go | mitigate | P2-A1: struct embedding split preserves compat |
| T-32-17 | Tampering (key collision) | data_cache_service.go | mitigate | P2-A2: dedupe to single source |
| T-32-18 | Repudiation (silent error) | role_service.go | mitigate | P2-A5: typed AppError enables HTTP code mapping |
</threat_model>

<verification>
- [ ] `go build ./...` exits 0 after Core split (router compat preserved)
- [ ] `go vet ./...` exits 0
- [ ] user_service_optimized.go absent
- [ ] Only one cache_keys.go source of truth
- [ ] role_service uses apperrors.New for known business errors
- [ ] Core split backward compat test passes
- [ ] Role apperrors test passes
</verification>

<success_criteria>
- P2-A1, A2, A3, A5 all resolved
- Build + vet pass after major refactor
- Wave 5 commits: `refactor(core): P2-A1 split god struct via embedding`, `refactor(cache): P2-A2 dedupe cache keys`, `refactor(errors): P2-A5 migrate role_service to apperrors`
</success_criteria>

<output>
Create `.planning/phases/32-v1-14-p1-p2/32-05-SUMMARY.md` when done
</output>