---
phase: 32-v1-14-p1-p2
plan: 06
type: execute
wave: 6
depends_on:
  - 32-05
files_modified:
  - internal/core/db/migrations/027_cleanup_duplicate_indexes.sql
  - internal/core/db/migrations/027_create_user_column_config.sql
  - internal/core/db/migrations/028_merge_duty_menus.sql
  - internal/core/db/migrations/029_add_building_coordinates.sql
  - internal/core/db/migrations/029_add_building_spaces_menu.sql
  - internal/core/db/migrations/030_add_building_spaces_3d_menu.sql
  - internal/core/db/migrations/030_create_workstation_device.sql
  - internal/core/db/migrations/030_enhance_workstation_table.sql
  - internal/core/db/migrations/031_enhance_server_room_table.sql
  - internal/core/db/migrations/031_update_building_coordinates.sql
  - internal/core/db/migrations/036_update_workstation_status_values.sql
  - internal/agent/server/account_manager.go
  - internal/agent/server/subprocess_pgroup_test.go
  - internal/services/operations/excel_service.go
  - internal/services/operations/excel_transaction_test.go
  - internal/core/core.go
autonomous: true
requirements:
  - P2-A4
  - P2-A7
  - P2-A8

must_haves:
  truths:
    - "Migration files have no conflicting numeric prefixes; each NNN used at most once OR files are documented with source comments"
    - "All exec.Command calls in account_manager.go set Setpgid: true via SysProcAttr"
    - "Subprocess reaper goroutine runs in core.Init() and stops on core.Close()"
    - "Excel ImportData wraps entire flow in single GORM Transaction"
    - "processThreeLevelDepartments runs inside the same transaction as Upsert"
    - "Excel import rolls back all changes if any sub-process fails mid-import"
  artifacts:
    - path: "internal/core/db/migrations/"
      provides: "Renamed migration files OR source comments documenting original commits"
      contains: "P2-A4"
    - path: "internal/agent/server/account_manager.go"
      provides: "SysProcAttr.Setpgid: true on every exec.Command"
      contains: "Setpgid: true"
    - path: "internal/core/core.go"
      provides: "Subprocess reaper started in Init, stopped in Close"
      contains: "subprocessReaper"
    - path: "internal/services/operations/excel_service.go"
      provides: "ImportData wrapped in Transaction"
      contains: "WithContext.*Transaction"
    - path: "internal/agent/server/subprocess_pgroup_test.go"
      provides: "Test verifying Setpgid on exec.Command"
      contains: "TestSubprocess_SetProcessGroup"
    - path: "internal/services/operations/excel_transaction_test.go"
      provides: "Test asserting rollback on sub-process failure"
      contains: "TestImportData_TransactionalRollback"
  key_links:
    - from: "internal/agent/server/account_manager.go"
      to: "syscall.SysProcAttr"
      via: "cmd.SysProcAttr = &syscall.SysProcAttr{Setpgid: true}"
      pattern: "Setpgid"
    - from: "internal/services/operations/excel_service.go"
      to: "gorm.DB.Transaction"
      via: "s.db.WithContext(ctx).Transaction(func(tx *gorm.DB) error)"
      pattern: "Transaction\\(func\\(tx"
---

<objective>
Wave 6 — P2 迁移与子进程. Three items with HIGH refactor risk: (a) P2-A4 migration file renumbering (10 conflicting files); (b) P2-A7 subprocess management (process group + reaper for account_manager.go); (c) P2-A8 Excel import transaction wrapper.

Purpose: Eliminate migration file ordering ambiguity, prevent zombie process FD leak from VM agent account manager, and make Excel import atomic (rollback on partial failure). Critical correctness items for production stability.

Output:
- 10 migration files renamed OR documented with source comments (safer path: source comments per RESEARCH.md Pitfall 3)
- 1 modified account_manager.go (15+ exec.Command calls get Setpgid)
- 1 modified core.go (reaper lifecycle)
- 1 modified excel_service.go (Transaction wrapper)
- 2 new test files
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-v1-14-p1-p2/CONTEXT.md
@.planning/phases/32-v1-14-p1-p2/32-RESEARCH.md
@.planning/phases/32-v1-14-p1-p2/32-VALIDATION.md

# Source-of-truth files
@internal/core/db/migrations/ (10 conflicting files; use Glob to enumerate)
@internal/agent/server/account_manager.go
@internal/services/operations/excel_service.go
@internal/core/core.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document migration file conflicts via source comments (P2-A4 safer path)</name>
  <files>internal/core/db/migrations/027_cleanup_duplicate_indexes.sql, internal/core/db/migrations/027_create_user_column_config.sql, internal/core/db/migrations/028_merge_duty_menus.sql, internal/core/db/migrations/029_add_building_coordinates.sql, internal/core/db/migrations/029_add_building_spaces_menu.sql, internal/core/db/migrations/030_add_building_spaces_3d_menu.sql, internal/core/db/migrations/030_create_workstation_device.sql, internal/core/db/migrations/030_enhance_workstation_table.sql, internal/core/db/migrations/031_enhance_server_room_table.sql, internal/core/db/migrations/031_update_building_coordinates.sql, internal/core/db/migrations/036_update_workstation_status_values.sql</files>
  <read_first>
    - internal/core/db/migrations.go (or similar runner file) — find how migrations are loaded (filename sort vs DB record)
    - .planning/phases/32-v1-14-p1-p2/32-RESEARCH.md (P2-A4 + Pitfall 3 — runner ordering check)
    - The 10 conflicting migration files themselves — open them and note first 10 lines of each
  </read_first>
  <action>
    Per RESEARCH.md Open Question #1, the migration runner's ordering mechanism is unclear. Per Pitfall 3, renumbering is risky if any environment has applied the old filenames.

    SAFER PATH (default): Add a source-tracking comment to each conflicting file, leave filenames unchanged. This documents the conflict without risking production breakage.

    Steps:
    1. Determine runner ordering mechanism: read the migration runner code (likely `internal/core/db/migrate.go` or similar). Look for sort by filename (string sort) vs sort by DB record (created_at from schema_migrations table).
    2. If runner uses DB record ordering: the conflict is harmless; just add source comments documenting original commit. STOP here for this task.
    3. If runner uses filename sort: the conflict IS a real problem; renumber. Use `git mv` to preserve history. For each conflicting file, find the next unused NNN in the migration directory and rename. Add a comment at the top of each renumbered file: `-- Renamed from NNN_<old>.sql on 2026-06-XX during Phase 32 (P2-A4)`.

    For BOTH paths, every conflicting file MUST end up with:
    - Either a renamed filename + top-of-file source comment
    - Or an unchanged filename + top-of-file source comment documenting original commit hash + creation date

    Source comment format:
    ```sql
    -- Phase 32 P2-A4 source-tracking:
    --   Original commit: <hash> (find via `git log --follow --oneline -- <filename>`)
    --   Created: <date from git log>
    --   Note: <why this filename conflicts with another file; what renumber was applied or why none was applied>
    ```

    DO NOT modify any SQL DDL or content. Source comments are header-only.

    After edits, run `go build ./...` to confirm nothing broken (SQL files are not compiled, but the test file at .planning/phases/32-v1-14-p1-p2/32-VALIDATION.md requires verification).
  </action>
  <acceptance_criteria>
    - Each of the 10 conflicting migration files contains a header comment referencing P2-A4 (grep for "P2-A4" in the migrations directory returns 10 matches OR the runner mechanism is documented in a separate note)
    - If filename-sort runner: filenames no longer conflict (ls migrations shows each NNN used at most once for the conflicting range)
    - If DB-record runner: filenames unchanged but every conflicting file has source-tracking header
    - `git log --follow --oneline -- <each_file>` still works (history preserved)
    - No SQL DDL changed (file content except the header is byte-identical to original)
    - `go build ./...` exits 0 (SQL files not compiled, but runner might import them)
  </acceptance_criteria>
  <verify>
    <automated>grep -l "P2-A4" internal/core/db/migrations/*.sql | wc -l && go build ./... && go vet ./...</automated>
  </verify>
  <done>
    P2-A4 resolved: every conflicting migration file has source-tracking header documenting original commit; filenames either renamed or left as-is based on runner mechanism. No SQL DDL changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Setpgid to all exec.Command + start subprocess reaper (P2-A7)</name>
  <files>internal/agent/server/account_manager.go, internal/core/core.go, internal/agent/server/subprocess_pgroup_test.go</files>
  <read_first>
    - internal/agent/server/account_manager.go (find ALL exec.Command call sites — RESEARCH.md says 15+; identify each by searching `exec.Command(`)
    - internal/core/core.go (find Init and Close methods; identify where to hook reaper lifecycle)
    - .planning/phases/32-v1-14-p1-p2/32-RESEARCH.md (P2-A7 + Pitfall 7 — process group isolation)
  </read_first>
  <action>
    Edit internal/agent/server/account_manager.go:
    1. For every `exec.Command(...)` call (find with `grep -n "exec.Command" internal/agent/server/account_manager.go`):
       - Add `cmd.SysProcAttr = &syscall.SysProcAttr{Setpgid: true}` immediately after the exec.Command line.
       - Add `import "syscall"` if not already imported.
    2. Wrap each command execution in a function helper that:
       - Sets Setpgid
       - Calls cmd.Start()
       - On context cancellation: calls `syscall.Kill(-cmd.Process.Pid, syscall.SIGKILL)` to kill the entire process group
       - Calls cmd.Wait() to reap the process
       - Returns exit error if non-zero
    3. For platform compatibility: use build tags `//go:build !windows` and `//go:build windows` if Setpgid is not supported on Windows; for Windows use `CreationFlags: syscall.CREATE_NEW_PROCESS_GROUP`. Since this project builds for both Windows and Linux (per CLAUDE.md), use the appropriate build tag for each.

    Edit internal/core/core.go:
    1. Add a new method `startSubprocessReaper(ctx context.Context)`:
       - Spawns a goroutine that calls `syscall.Wait4(-1, &status, syscall.WNOHANG, nil)` in a loop with sleep interval (e.g., 30s).
       - Loop exits when ctx is cancelled.
    2. Call `core.startSubprocessReaper(ctx)` in `core.Init()`.
    3. Call `core.cancelSubprocessReaper()` (or use ctx cancellation) in `core.Close()`.

    Create internal/agent/server/subprocess_pgroup_test.go:
    Test `TestSubprocess_SetProcessGroup`:
    - Construct an exec.Cmd that runs `/bin/sh -c "echo $$"` (Linux) or `cmd /c echo` (Windows).
    - Call the helper function (e.g., `runSubprocess(ctx, cmd)`) that sets Setpgid.
    - Assert: cmd.Process.Pid is non-zero.
    - Assert: cmd.SysProcAttr.Setpgid == true (Linux) or cmd.SysProcAttr.CreationFlags has CREATE_NEW_PROCESS_GROUP bit (Windows).
    - Assert: cmd.Wait() returns nil and stdout contains expected output.
    - Use `//go:build !windows` and `//go:build windows` for cross-platform.

    For Windows-specific test: use `cmd /c "echo %RANDOM%"` and assert it returns 0.
  </action>
  <acceptance_criteria>
    - internal/agent/server/account_manager.go: every `exec.Command(` call is followed by `cmd.SysProcAttr = &syscall.SysProcAttr{Setpgid: true}` (grep `exec.Command` and `Setpgid` counts match)
    - internal/agent/server/account_manager.go: imports `syscall`
    - internal/core/core.go: contains `subprocessReaper` method (grep matches)
    - internal/core/core.go: reaper is started in Init() and stopped in Close()
    - internal/agent/server/subprocess_pgroup_test.go exists with TestSubprocess_SetProcessGroup
    - `go test -count=1 -run "TestSubprocess_SetProcessGroup" ./internal/agent/server/ -v` exits 0
    - `go build ./...` exits 0
    - `go vet ./...` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -c "exec.Command" internal/agent/server/account_manager.go && grep -c "Setpgid" internal/agent/server/account_manager.go && go build ./... && go vet ./... && go test -count=1 -run "TestSubprocess_SetProcessGroup" ./internal/agent/server/ -v</automated>
  </verify>
  <done>
    P2-A7 resolved: every subprocess gets its own process group; reaper goroutine prevents zombie FD accumulation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wrap Excel ImportData in single Transaction (P2-A8)</name>
  <files>internal/services/operations/excel_service.go, internal/services/operations/excel_transaction_test.go</files>
  <read_first>
    - internal/services/operations/excel_service.go (find ImportData method around line 224-404; find processThreeLevelDepartments around line 1270)
    - .planning/codebase/CONVENTIONS.md (GORM Transaction pattern)
    - .planning/phases/32-v1-14-p1-p2/32-RESEARCH.md (P2-A8 + Pitfall 4 — deadlock risk)
    - internal/services/operations/room_photo_service.go:129 (existing transaction pattern to mirror)
  </read_first>
  <action>
    Refactor internal/services/operations/excel_service.go ImportData:

    1. Wrap the entire ImportData body in a single GORM Transaction:
       ```go
       return s.db.WithContext(ctx).Transaction(func(tx *gorm.DB) error {
           // existing body, but using tx instead of s.db
           // ... all sub-services called with tx instead of s.db
           return nil
       })
       ```

    2. Audit every nested call inside ImportData and pass `tx` instead of `s.db`:
       - referenceResolver.ResolveBatch(tx, ...) (or whatever the resolver signature)
       - upserter.Upsert(tx, ...)
       - cacheInvalidator.InvalidateByEntityType(tx, ...) — note: cache invalidation is generally NOT transactional (cache writes are not in DB transaction). Use post-commit hook OR call after Transaction returns.
       - s.processThreeLevelDepartments(tx, ...) (this is the line 1270 function)

    3. For cache invalidation: move OUTSIDE the Transaction function (run after Transaction returns nil). Cache writes are not part of the DB transaction anyway; calling them inside can cause stale cache to be served if rollback happens. Pattern:
       ```go
       if err := s.db.WithContext(ctx).Transaction(func(tx *gorm.DB) error { ... }); err != nil {
           return err
       }
       // post-commit: invalidate cache
       s.cacheInvalidator.InvalidateByEntityType(ctx, entityType)
       ```

    4. DO NOT change ImportData signature.

    5. Audit carefully for any `s.db.WithContext(ctx)` calls inside ImportData that should now be `tx.WithContext(ctx)`. The check: if the DB operation should be rolled back on outer failure, it MUST use tx.

    6. Reference the existing pattern in `room_photo_service.go:129` if available.

    Create internal/services/operations/excel_transaction_test.go:
    Test `TestImportData_TransactionalRollback`:
    - Use SQLite in-memory DB.
    - Setup: pre-populate 1 valid building row + 1 invalid row that will cause validation failure.
    - Mock or arrange for processThreeLevelDepartments to fail at row 2.
    - Call ImportData.
    - Assert: returns error.
    - Assert: DB state shows the row 1 building was NOT inserted (rolled back). Count == 0 for new buildings.
    - Second case: all rows valid → ImportData returns nil and DB shows all buildings inserted.
  </action>
  <acceptance_criteria>
    - internal/services/operations/excel_service.go: ImportData body wrapped in `s.db.WithContext(ctx).Transaction(func(tx *gorm.DB) error {` (grep matches)
    - internal/services/operations/excel_service.go: processThreeLevelDepartments called with `tx` not `s.db` (grep inside ImportData)
    - internal/services/operations/excel_service.go: cacheInvalidator called AFTER Transaction returns nil (grep pattern shows invalidation outside Transaction closure)
    - internal/services/operations/excel_transaction_test.go exists with TestImportData_TransactionalRollback
    - `go test -count=1 -run "TestImportData_TransactionalRollback" ./internal/services/operations/ -v` exits 0
    - `go build ./...` exits 0
    - `go vet ./...` exits 0
    - ImportData signature unchanged
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./... && go test -count=1 -run "TestImportData_TransactionalRollback" ./internal/services/operations/ -v</automated>
  </verify>
  <done>
    P2-A8 resolved: ImportData wrapped in Transaction; processThreeLevelDepartments and Upsert share the same tx; cache invalidation runs post-commit; rollback test passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → file order | filename conflict causes ambiguous ordering |
| agent → subprocess (account manager) | Setpgid isolates process groups for clean kill |
| Excel import → DB atomicity | multi-step insert must rollback on partial failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-19 | Tampering (order ambiguity) | migrations/*.sql | mitigate | P2-A4: source comments document original commits |
| T-32-20 | DoS (zombie FD) | account_manager.go | mitigate | P2-A7: Setpgid + reaper goroutine |
| T-32-21 | Tampering (partial state) | excel_service.go | mitigate | P2-A8: Transaction wrapper + rollback test |
</threat_model>

<verification>
- [ ] All 10 conflicting migrations have P2-A4 source comments
- [ ] Every exec.Command in account_manager.go has Setpgid
- [ ] subprocessReaper exists in core.go
- [ ] ImportData wrapped in Transaction; cache invalidation post-commit
- [ ] Subprocess + Excel transaction tests pass
- [ ] `go build ./...` exits 0
- [ ] `go vet ./...` exits 0
</verification>

<success_criteria>
- P2-A4, P2-A7, P2-A8 all resolved
- Wave 6 commits: `docs(migrations): P2-A4 source-track conflicting files`, `fix(agent): P2-A7 Setpgid + reaper`, `fix(excel): P2-A8 transactional import`
</success_criteria>

<output>
Create `.planning/phases/32-v1-14-p1-p2/32-06-SUMMARY.md` when done
</output>