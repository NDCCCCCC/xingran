---
phase: 32-v1-14-p1-p2
plan: 04
type: execute
wave: 4
depends_on:
  - 32-03
files_modified:
  - internal/services/system/config_service.go
  - internal/services/system/config_invalidation_test.go
  - internal/services/system/user_service.go
autonomous: true
requirements:
  - P1-B1
  - P1-B2

must_haves:
  truths:
    - "Updating sys.request.encryption.enabled via ConfigService.Update invalidates the middleware encryption cache immediately"
    - "Subsequent encrypted requests use the new encryption-enabled flag without server restart"
    - "buildDepartmentPaths is called exactly once in userService.List (no duplicate call)"
  artifacts:
    - path: "internal/services/system/config_service.go"
      provides: "OnEncryptionConfigChanged callback invoked on Update (already fixed per commit 0bcac33)"
      contains: "OnEncryptionConfigChanged"
    - path: "internal/services/system/config_invalidation_test.go"
      provides: "Test asserting Update invalidates middleware cache"
      contains: "TestConfigService_UpdateEncryptionFlag"
    - path: "internal/services/system/user_service.go"
      provides: "buildDepartmentPaths called once in List (already fixed per commit ab0a279)"
      contains: "buildDepartmentPaths"
  key_links:
    - from: "internal/services/system/config_service.go"
      to: "middleware encryption cache"
      via: "OnEncryptionConfigChanged callback registered at startup"
      pattern: "onEncryptionConfigChanged"
    - from: "internal/services/system/user_service.go"
      to: "buildDepartmentPaths"
      via: "single call in List method, before serializing response"
      pattern: "buildDepartmentPaths"
---

<objective>
Wave 4 — P1 业务逻辑收尾 (business logic closeout). Two small P1 items: (a) P1-B1 config cache invalidation already wired — add regression test; (b) P1-B2 duplicate buildDepartmentPaths call already removed — verify only one call site remains.

Purpose: Close the two remaining P1 business logic items and confirm no regression in user listing hot path.

Output:
- 0 production code edits (both already fixed)
- 1 new test file
- 1 grep-based verification assertion
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-v1-14-p1-p2/CONTEXT.md
@.planning/phases/32-v1-14-p1-p2/32-RESEARCH.md
@.planning/phases/32-v1-14-p1-p2/32-VALIDATION.md

# Source-of-truth files
@internal/services/system/config_service.go
@internal/services/system/user_service.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ConfigService invalidation test + verify buildDepartmentPaths single call (P1-B1 + P1-B2)</name>
  <files>internal/services/system/config_invalidation_test.go</files>
  <read_first>
    - internal/services/system/config_service.go (find Update method, find OnEncryptionConfigChanged callback registration; verify it exists per commit 0bcac33)
    - internal/services/system/user_service.go (find List method around line 398-410; verify buildDepartmentPaths appears exactly once per commit ab0a279)
    - .planning/phases/32-v1-14-p1-p2/32-VALIDATION.md (32-04-01 and 32-04-02 rows)
  </read_first>
  <action>
    Both P1-B1 and P1-B2 are ALREADY FIXED per RESEARCH.md. No production code changes.

    Part 1 — Verify P1-B2 with grep (no test file needed; manual verification is in VALIDATION.md):
    - Run `grep -n "buildDepartmentPaths" internal/services/system/user_service.go`
    - Assert: result shows exactly 1 call site (excluding the function definition itself).
    - If multiple call sites are found, STOP and report to user — the "fix" claim was wrong; do not silently fix.

    Part 2 — Create internal/services/system/config_invalidation_test.go:

    Test `TestConfigService_UpdateEncryptionFlag_InvalidatesMiddlewareCache`:
    - Construct ConfigService with a mock or real DB (use SQLite in-memory via gorm.io/driver/sqlite).
    - Construct a counter/callback registry that increments on each OnEncryptionConfigChanged invocation.
    - Pre-populate the config table with `sys.request.encryption.enabled = false`.
    - Call ConfigService.Update(ctx, configID, newValue=true) (or the actual signature — read first).
    - Assert: counter is exactly 1 after Update returns.
    - Assert: the DB now stores the new value.
    - Second sub-test: Update a NON-encryption config flag (e.g., sys.user.init.password).
    - Assert: counter is 0 (encryption callback only fires for encryption flag changes).

    Use `package system` (same package) to access unexported fields like the callback map.
  </action>
  <acceptance_criteria>
    - `grep -c "buildDepartmentPaths" internal/services/system/user_service.go` returns 2 (one definition + one call) OR 1 if it's a method (not a function)
    - If method call: `grep -n "buildDepartmentPaths" internal/services/system/user_service.go` returns exactly 1 line referencing the call (not the function definition)
    - internal/services/system/config_invalidation_test.go exists with TestConfigService_UpdateEncryptionFlag_InvalidatesMiddlewareCache
    - `go test -count=1 -run "TestConfigService_UpdateEncryptionFlag_InvalidatesMiddlewareCache" ./internal/services/system/ -v` exits 0
    - `go build ./...` exits 0
    - `go vet ./...` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./... && grep -c "buildDepartmentPaths" internal/services/system/user_service.go && go test -count=1 -run "TestConfigService_UpdateEncryptionFlag_InvalidatesMiddlewareCache" ./internal/services/system/ -v</automated>
  </verify>
  <done>
    P1-B1 regression-tested; P1-B2 verified (only 1 call site). Wave 4 complete. All 15 P1 items closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → config update | encryption flag toggle must immediately affect runtime behavior |
| user list → dept path | duplicate DB calls waste pool capacity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-14 | Tampering (stale config) | config_service.go | mitigate | P1-B1: cache invalidation callback + test |
| T-32-15 | DoS (wasted query) | user_service.go | accept | P1-B2: already fixed; verified by grep + build |
</threat_model>

<verification>
- [ ] buildDepartmentPaths called once in user_service.go
- [ ] ConfigService invalidation test passes
- [ ] `go build ./...` exits 0
- [ ] `go vet ./...` exits 0
</verification>

<success_criteria>
- P1-B1, P1-B2 fully closed
- All 15 P1 items resolved (regression-tested or implemented)
- Wave 4 commit: `test(business): P1-B1/B2 regression coverage + verification`
</success_criteria>

<output>
Create `.planning/phases/32-v1-14-p1-p2/32-04-SUMMARY.md` when done
</output>