---
phase: 32-v1-14-p1-p2
plan: 07
type: execute
wave: 7
depends_on:
  - 32-06
files_modified:
  - internal/services/addomain/ldap_client.go
  - internal/services/addomain/ldap_iface.go
  - internal/services/addomain/ldap_client_mock_test.go
  - internal/services/addomain/group_sync_service.go
  - internal/services/addomain/group_sync_service_test.go
  - internal/services/addomain/user_ou_service_test.go
  - internal/services/addomain/stripBaseDN_test.go
  - internal/services/addomain/dept_ou_mapper_test.go
autonomous: true
requirements:
  - P2-A6

must_haves:
  truths:
    - "ldap_client.go exposes an interface (LDAPClientIface or similar) so it can be mocked"
    - "Connect() success and failure paths have mock-based unit tests"
    - "Bind() success and failure paths have mock-based unit tests"
    - "Search() success and empty-result paths have mock-based unit tests"
    - "group_sync_service has mock-based tests for happy path and threshold rejection"
    - "user_ou_service_test.go has expanded coverage (basic tests already exist per RESEARCH.md)"
    - "stripBaseDN_test.go and dept_ou_mapper_test.go are verified to have real test content (NOT empty stubs)"
  artifacts:
    - path: "internal/services/addomain/ldap_iface.go"
      provides: "LDAPClientIface interface (Connect/Bind/Search/Close)"
      contains: "type LDAPClientIface interface"
    - path: "internal/services/addomain/ldap_client.go"
      provides: "LDAPClient implementation that satisfies LDAPClientIface"
      contains: "func (c \\*LDAPClient) Connect"
    - path: "internal/services/addomain/ldap_client_mock_test.go"
      provides: "Mock-based tests for Connect/Bind/Search"
      contains: "TestLDAPClient_Connect_Success"
    - path: "internal/services/addomain/group_sync_service_test.go"
      provides: "Mock-based tests for group sync happy path + threshold rejection"
      contains: "TestGroupSync_HappyPath"
    - path: "internal/services/addomain/user_ou_service_test.go"
      provides: "Expanded coverage for user OU operations"
      contains: "TestUserOUService"
    - path: "internal/services/addomain/stripBaseDN_test.go"
      provides: "Existing tests preserved; additional cases if thin"
      contains: "TestStripBaseDN"
    - path: "internal/services/addomain/dept_ou_mapper_test.go"
      provides: "Existing tests preserved; additional atomic-case coverage"
      contains: "TestUpsertMapping_Atomic"
  key_links:
    - from: "internal/services/addomain/ldap_iface.go"
      to: "internal/services/addomain/ldap_client.go"
      via: "LDAPClient struct implements all interface methods"
      pattern: "func \\(c \\*LDAPClient\\) (Connect|Bind|Search|Close)"
    - from: "internal/services/addomain/group_sync_service_test.go"
      to: "internal/services/addomain/ldap_iface.go"
      via: "mock LDAPClientIface injected for test isolation"
      pattern: "mockLDAPClient"
    - from: "internal/services/addomain/user_ou_service_test.go"
      to: "internal/services/addomain/user_ou_service.go"
      via: "test exercises public methods on user_ou_service"
      pattern: "userOUService\\."
---

<objective>
Wave 7 — P2 测试覆盖 (AD module test coverage). Single P2-A6 item covering AD module unit tests with proper mocking.

Purpose: Per RESEARCH.md (correction to CONTEXT.md): stripBaseDN_test.go and dept_ou_mapper_test.go already have real tests (NOT empty stubs). The actual gaps are: (a) ldap_client.go has no Connect/Bind/Search mock tests; (b) group_sync_service has no test file; (c) user_ou_service_test.go has only basic tests.

Strategy: Extract LDAP operations into an interface (LDAPClientIface) so the actual go-ldap library binding is mockable. Create hand-rolled mock struct (no gomock — project doesn't use it).

Output:
- 1 new interface file (ldap_iface.go)
- 1 modified ldap_client.go (satisfy interface)
- 3 new test files (ldap_client_mock_test.go, group_sync_service_test.go, expanded user_ou_service_test.go)
- 2 expanded existing test files (stripBaseDN_test.go, dept_ou_mapper_test.go add atomic case)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-v1-14-p1-p2/CONTEXT.md
@.planning/phases/32-v1-14-p1-p2/32-RESEARCH.md
@.planning/phases/32-v1-14-p1-p2/32-VALIDATION.md

# Source-of-truth files
@internal/services/addomain/ldap_client.go
@internal/services/addomain/group_sync_service.go
@internal/services/addomain/user_ou_service.go
@internal/services/addomain/stripBaseDN_test.go
@internal/services/addomain/dept_ou_mapper_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract LDAPClientIface + add Connect/Bind/Search mock tests</name>
  <files>internal/services/addomain/ldap_iface.go, internal/services/addomain/ldap_client.go, internal/services/addomain/ldap_client_mock_test.go</files>
  <read_first>
    - internal/services/addomain/ldap_client.go (find Connect, Bind, Search, Close method signatures)
    - .planning/phases/32-v1-14-p1-p2/32-RESEARCH.md (Pattern 2 — LDAP interface extraction example)
  </read_first>
  <action>
    Create internal/services/addomain/ldap_iface.go:
    ```go
    package addomain

    import "github.com/go-ldap/ldap/v3"

    // LDAPClientIface abstracts the LDAP operations used by AD sync services.
    // Implementations: real LDAPClient (calls go-ldap), mockLDAPClient (tests).
    type LDAPClientIface interface {
        Connect() error
        Bind(username, password string) error
        Search(baseDN, filter string, attrs []string) (*ldap.SearchResult, error)
        Close() error
    }
    ```
    Plus a mock implementation in the same file (or in a separate test helper file):
    ```go
    type mockLDAPClient struct {
        connectErr  error
        bindErr     error
        searchRes   *ldap.SearchResult
        searchErr   error
        closeErr    error
        connectCalls int
        bindCalls    int
        searchCalls  int
    }
    func (m *mockLDAPClient) Connect() error { m.connectCalls++; return m.connectErr }
    func (m *mockLDAPClient) Bind(u, p string) error { m.bindCalls++; return m.bindErr }
    // ... etc
    ```

    Edit internal/services/addomain/ldap_client.go:
    - Add `var _ LDAPClientIface = (*LDAPClient)(nil)` declaration to verify at compile time that LDAPClient satisfies the interface.
    - If LDAPClient methods don't already match the interface signatures, adjust them (no behavior change).

    Create internal/services/addomain/ldap_client_mock_test.go:
    Test `TestLDAPClient_Connect_Success`:
    - Use mockLDAPClient with connectErr=nil.
    - Call Connect.
    - Assert: returns nil error; connectCalls == 1.

    Test `TestLDAPClient_Connect_Failure`:
    - Use mockLDAPClient with connectErr=someError.
    - Call Connect.
    - Assert: returns the error; connectCalls == 1.

    Test `TestLDAPClient_Bind_Success`:
    - mockLDAPClient with bindErr=nil.
    - Call Bind("user", "pass").
    - Assert: nil; bindCalls == 1.

    Test `TestLDAPClient_Bind_InvalidCredentials`:
    - mockLDAPClient with bindErr=ldap.NewError(49, ...).
    - Call Bind.
    - Assert: error returned; bindCalls == 1.

    Test `TestLDAPClient_Search_ReturnsEntries`:
    - mockLDAPClient with searchRes pre-populated with 2 entries.
    - Call Search.
    - Assert: returns 2 entries; searchCalls == 1.

    Test `TestLDAPClient_Search_Empty`:
    - mockLDAPClient with searchRes.Entries = nil.
    - Call Search.
    - Assert: returns 0 entries; no error.

    Use `package addomain` (same package, not _test) so the mock struct is accessible. Place mock in `ldap_iface_test.go` (test-only file with `_test.go` suffix) to avoid leaking into production.
  </action>
  <acceptance_criteria>
    - internal/services/addomain/ldap_iface.go exists with `type LDAPClientIface interface` (grep matches)
    - internal/services/addomain/ldap_client.go contains `var _ LDAPClientIface = ` (compile-time interface satisfaction check)
    - internal/services/addomain/ldap_client_mock_test.go exists with at least 4 test functions (TestLDAPClient_Connect_Success, TestLDAPClient_Connect_Failure, TestLDAPClient_Bind_Success, TestLDAPClient_Search_ReturnsEntries)
    - `go test -count=1 -run "TestLDAPClient" ./internal/services/addomain/ -v` exits 0 with all tests passing
    - `go build ./...` exits 0
    - `go vet ./...` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./... && go test -count=1 -run "TestLDAPClient" ./internal/services/addomain/ -v</automated>
  </verify>
  <done>
    LDAP interface extracted; 6 mock-based tests cover Connect/Bind/Search success+failure paths. P2-A6 partial progress.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add group_sync_service tests + expand user_ou_service + verify existing test files</name>
  <files>internal/services/addomain/group_sync_service_test.go, internal/services/addomain/user_ou_service_test.go, internal/services/addomain/stripBaseDN_test.go, internal/services/addomain/dept_ou_mapper_test.go</files>
  <read_first>
    - internal/services/addomain/group_sync_service.go (find SyncGroups, handleDeletedGroups signatures; verify threshold logic per P1-C2 fix)
    - internal/services/addomain/user_ou_service.go (find public methods: SyncUserOU, etc.)
    - internal/services/addomain/stripBaseDN_test.go (read existing tests — RESEARCH.md says 2 real tests, NOT empty)
    - internal/services/addomain/dept_ou_mapper_test.go (read existing tests — RESEARCH.md says 4 real tests, NOT empty)
    - .planning/phases/32-v1-14-p1-p2/32-RESEARCH.md (P2-A6 section + CONTEXT.md correction)
  </read_first>
  <action>
    CONTEXT.md's claim that stripBaseDN_test.go and dept_ou_mapper_test.go are "empty" is INCORRECT per RESEARCH.md. Real tests exist. Do NOT delete them. Expand them only if thin.

    Create internal/services/addomain/group_sync_service_test.go:
    Test `TestGroupSync_HappyPath`:
    - Construct GroupSyncService with mockLDAPClient returning 5 entries.
    - Pre-populate DB with 5 corresponding groups.
    - Call SyncGroups.
    - Assert: no error; all 5 groups marked synced.

    Test `TestGroupSync_RejectsEmptyLDAP` (regression for P1-C2):
    - mockLDAPClient returns 0 entries.
    - Call SyncGroups (or handleDeletedGroups).
    - Assert: error returned with "LDAP" in message.

    Test `TestGroupSync_RejectsOverThreshold`:
    - mockLDAPClient returns 2 entries.
    - DB has 20 groups; delete ratio would be 18/20 = 90%.
    - Call handleDeletedGroups.
    - Assert: error returned; no rows deleted from DB.

    Test `TestGroupSync_UnderThreshold`:
    - mockLDAPClient returns 18 entries.
    - DB has 20 groups; delete ratio 2/20 = 10% (under 50%).
    - Call handleDeletedGroups.
    - Assert: no error; 2 rows deleted.

    Expand internal/services/addomain/user_ou_service_test.go (already has basic tests per RESEARCH.md):
    - Read existing tests first; do NOT duplicate.
    - Add: Test `TestUserOUService_SyncFromAD_NoChange` (DB already in sync, no rows changed).
    - Add: Test `TestUserOUService_GetByUsername_NotFound` (returns ErrNotFound).
    - Add: Test `TestUserOUService_SyncFromAD_InsertsNewUser` (1 new user in LDAP, none in DB → 1 insert).
    - Use SQLite in-memory DB.

    Verify existing stripBaseDN_test.go and dept_ou_mapper_test.go:
    - `grep -c "^func Test" internal/services/addomain/stripBaseDN_test.go` — assert ≥ 1 (real tests present).
    - `grep -c "^func Test" internal/services/addomain/dept_ou_mapper_test.go` — assert ≥ 1 (real tests present).
    - If both ≥ 1: NO ACTION on these files (do not delete or modify).
    - If either is empty/has only stub comments: report to user via stderr; do not auto-fix.
    - Optional: add `TestUpsertMapping_Atomic` to dept_ou_mapper_test.go if not already present (P1-C3 regression test). Read the file first.
  </action>
  <acceptance_criteria>
    - internal/services/addomain/group_sync_service_test.go exists with TestGroupSync_HappyPath + 3 other tests (TestGroupSync_RejectsEmptyLDAP, TestGroupSync_RejectsOverThreshold, TestGroupSync_UnderThreshold) — grep returns 4 function names
    - internal/services/addomain/user_ou_service_test.go: contains at least 3 new test functions
    - internal/services/addomain/stripBaseDN_test.go: untouched (file content unchanged from git HEAD)
    - internal/services/addomain/dept_ou_mapper_test.go: untouched OR has TestUpsertMapping_Atomic added
    - `go test -count=1 -run "TestGroupSync|TestUserOUService|TestStripBaseDN|TestUpsertMapping" ./internal/services/addomain/ -v` exits 0
    - `go build ./...` exits 0
    - `go vet ./...` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^func Test" internal/services/addomain/stripBaseDN_test.go && grep -c "^func Test" internal/services/addomain/dept_ou_mapper_test.go && go build ./... && go vet ./... && go test -count=1 -run "TestGroupSync|TestUserOUService|TestStripBaseDN|TestUpsertMapping" ./internal/services/addomain/ -v</automated>
  </verify>
  <done>
    P2-A6 fully resolved: LDAP interface + 6 mock tests + 4 group_sync tests + 3 user_ou tests + existing tests preserved. AD module coverage meets ≥70% threshold for critical paths.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → LDAP server | mocked in tests, real in production |
| tests → AD sync orchestration | must cover happy + failure + edge cases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-22 | Info Disclosure (test gap) | ldap_client.go | mitigate | P2-A6: LDAPClientIface + mock tests for Connect/Bind/Search |
| T-32-23 | Tampering (untested code path) | group_sync_service.go | mitigate | P2-A6: 4 group_sync tests incl. threshold regression |
| T-32-24 | Repudiation (untested) | user_ou_service.go | mitigate | P2-A6: 3 expanded user_ou tests |
</threat_model>

<verification>
- [ ] LDAPClientIface exists; LDAPClient satisfies it (compile assertion present)
- [ ] 6 LDAP mock tests pass
- [ ] 4 group_sync tests pass
- [ ] 3+ user_ou tests pass
- [ ] Existing stripBaseDN/dept_ou_mapper tests preserved
- [ ] `go build ./...` exits 0
- [ ] `go vet ./...` exits 0
- [ ] Test coverage for AD module ≥ 70% (`go test -cover ./internal/services/addomain/`)
</verification>

<success_criteria>
- P2-A6 fully resolved (all 8 P2 categories closed)
- AD module coverage ≥70%
- Build + vet + targeted tests all pass
- Wave 7 commit: `test(addomain): P2-A6 LDAP mock + group_sync + user_ou coverage`
</success_criteria>

<output>
Create `.planning/phases/32-v1-14-p1-p2/32-07-SUMMARY.md` when done
</output>