---
phase: 115-selector-completion-zustand-selector
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - xingran-react-frontend/src/store/tabsStore.ts
  - xingran-react-frontend/src/store/layoutStore.ts
autonomous: true
requirements:
  - SELECTOR-01
  - SELECTOR-02
must_haves:
  truths:
    - "useTabs 内部为 14 个逐字段 useTabsStore((s) => s.xxx) 订阅，返回对象形态一字不改（TabBar.tsx:55 等消费组件零改动）——SELECTOR-01"
    - "useLayout 内部为 10 个逐字段 useLayoutStore((s) => s.xxx) 订阅，layoutConfig + 6 个派生布尔 + 2 个 effect（含依赖数组）原样保留——SELECTOR-02"
    - "tabsStore/layoutStore 任意未被订阅字段的 set() 不再触发 useTabs/useLayout 消费组件重渲（订阅粒度机械保证，D-04 零行为变更）"
    - "两个 store 文件内 useTabsStore() / useLayoutStore() 无参调用归零（grep 断言）"
  artifacts:
    - path: "xingran-react-frontend/src/store/tabsStore.ts"
      provides: "useTabs hook 逐字段 selector 化（310-345 行区域）"
      contains: "useTabsStore((s) => s.tabs)"
    - path: "xingran-react-frontend/src/store/layoutStore.ts"
      provides: "useLayout hook 逐字段 selector 化（287-346 行区域）"
      contains: "useLayoutStore((s) => s.currentLayout)"
  key_links:
    - from: "xingran-react-frontend/src/store/tabsStore.ts useTabs"
      to: "useTabsStore 逐字段 selector（14 个）"
      via: "每字段一次 useTabsStore((s) => s.field)"
      pattern: "useTabsStore\\(\\(s\\) =>"
    - from: "xingran-react-frontend/src/store/layoutStore.ts useLayout"
      to: "useLayoutStore 逐字段 selector（10 个）"
      via: "每字段一次 useLayoutStore((s) => s.field)"
      pattern: "useLayoutStore\\(\\(s\\) =>"
---

<objective>
SELECTOR-01/02：把 tabsStore 的 useTabs hook（310-345 行，14 字段整店解构）与 layoutStore 的 useLayout hook（287-346 行，10 字段整店解构）内部改为逐字段 selector 订阅（D-05 唯一范本 useRouteTabs.ts:45-48 风格），hook 返回形态零改动，消费组件零改动。

Purpose: tabsStore/layoutStore 任意 set()（含 persist rehydration、未来新增字段写入）不再牵连 TabBar / LayoutSwitcher / DensitySwitcher 等常驻组件重渲；这是全库 selector 收尾的第一波，确立本 phase 统一改造范式。
Output: 2 个 store 文件内 hook 订阅形态改造，既有测试/lint/type-check 零回归。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/115-selector-completion-zustand-selector/115-CONTEXT.md
@.planning/phases/115-selector-completion-zustand-selector/115-RESEARCH.md
@.planning/phases/115-selector-completion-zustand-selector/115-PATTERNS.md

<interfaces>
<!-- planner 已实读核验（2026-09-14）：两段 hook 的现状与返回形态。改造只动订阅来源，返回对象一字不改。 -->

tabsStore.ts useTabs（310-345 行）现状解构 14 字段：tabs / activeTab / history / addTab / removeTab / setActiveTab / closeOtherTabs / closeAllTabs / closeLeftTabs / closeRightTabs / updateTab / pinTab / unpinTab / reset；返回同一组字段 + hasTabs: tabs.length > 0（345 行收口）。

layoutStore.ts useLayout（287-346 行）现状解构 10 字段：currentLayout / configuration / sidebarCollapsed / density / syncFromSettings / toggleSidebar / setSidebarCollapsed / setDensity / setLayout / saveState；effect 1（302-306）data-layout/data-density 属性同步，依赖 [currentLayout, density]，体内用 useLayoutStore.getState()（静态方法，保留）；effect 2（311-323）settings-changed 监听 + 卸载 removeEventListener 清理，依赖 [syncFromSettings]；layoutConfig = layoutConfigs[currentLayout]（325 行）；返回 layout(=currentLayout 别名) / layoutConfig / 上述 10 字段 / 6 个派生布尔 isClassic / isHybrid / isInnovative / isCompact / isComfortable / isSpacious（RESEARCH 写"7 个"系笔误，实读为 6 个，以本清单为准）。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useTabs hook 14 字段逐字段 selector 化（SELECTOR-01）</name>
  <files>xingran-react-frontend/src/store/tabsStore.ts</files>
  <read_first>
    - xingran-react-frontend/src/store/tabsStore.ts（useTabs hook 310-345 行 + store 本体定义，确认 14 字段名）
    - xingran-react-frontend/src/components/layout/shared/useRouteTabs.ts（D-05 唯一范本，44-48 行 selector 风格）
    - .planning/phases/115-selector-completion-zustand-selector/115-PATTERNS.md（Pattern Assignments → tabsStore 段：现状/目标形态/4 条改造规则）
    - xingran-react-frontend/src/components/layout/shared/TabBar.tsx（消费方 :55 解构 10 字段——只读确认，零改动）
  </read_first>
  <action>
将 useTabs 体内 310-326 行的 `useTabsStore()` 无参整店解构替换为 14 个独立 const，每个字段一次 `useTabsStore((s) => s.字段名)`：tabs、activeTab、history、addTab、removeTab、setActiveTab、closeOtherTabs、closeAllTabs、closeLeftTabs、closeRightTabs、updateTab、pinTab、unpinTab、reset。形态严格照抄 D-05 范本 useRouteTabs.ts:45-48（action 与 state 字段写法相同）。

约束：
1. 返回对象（328-344 行）一字不改：字段名、顺序、`hasTabs: tabs.length > 0` 派生（由局部 tabs 计算）全部原样——消费组件 TabBar.tsx:55 零改动。
2. history 字段按 OQ-2 定案保留在返回对象中（虽然消费方 0 人解构，移除属超范围行为变更，D-04 零回归优先）。
3. 禁止对象 selector（`useTabsStore(s => ({...}))`）与 useShallow（项目 0 使用，RESEARCH Anti-Patterns；多字段一律拆单值 selector）。
4. hooks 数量从 1 → 14 固定不变，React 19 rules-of-hooks 合法。
5. 只动 useTabs 函数体；store 本体（create 定义、persist partialize）、文件内其他 hook 一行不动。
  </action>
  <verify>
    <automated>! grep -qE "useTabsStore\(\)" xingran-react-frontend/src/store/tabsStore.ts && test "$(grep -cE 'useTabsStore\(\(s\) =>' xingran-react-frontend/src/store/tabsStore.ts)" -ge 14 && cd xingran-react-frontend && npx vitest run src/store/tabsStore.test.ts src/components/layout/shared/__tests__/TabBar.render.test.tsx src/components/layout/shared/__tests__/useRouteTabs.test.tsx src/hooks/useUtilityHooks.test.tsx && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - behavior: useTabs 返回对象与改造前逐字段相同（含 hasTabs 派生）；tabsStore 任意字段 set 时仅订阅了该字段身份的 selector 触发更新
    - source-assertion: `grep -cE "useTabsStore\(\)" xingran-react-frontend/src/store/tabsStore.ts` == 0
    - source-assertion: `grep -cE "useTabsStore\(\(s\) =>" xingran-react-frontend/src/store/tabsStore.ts` >= 14
    - test-command: `cd xingran-react-frontend && npx vitest run src/store/tabsStore.test.ts src/components/layout/shared/__tests__/TabBar.render.test.tsx src/components/layout/shared/__tests__/useRouteTabs.test.tsx src/hooks/useUtilityHooks.test.tsx` 全绿
    - cli: `cd xingran-react-frontend && npm run type-check` exit 0
  </acceptance_criteria>
  <done>
useTabs 内部为 14 个逐字段 selector；`useTabsStore()` 无参调用在 tabsStore.ts 归零；返回对象一字不改；4 个受影响测试文件全绿；type-check 通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: useLayout hook 10 字段逐字段 selector 化 + 派生/effect 原样保留（SELECTOR-02）</name>
  <files>xingran-react-frontend/src/store/layoutStore.ts</files>
  <read_first>
    - xingran-react-frontend/src/store/layoutStore.ts（useLayout hook 287-346 行全文：10 字段解构、2 个 effect、layoutConfig、6 个派生布尔）
    - xingran-react-frontend/src/store/layoutStore.test.ts（:104-149 renderHook 用真实 store 断言 isClassic/isCompact/layoutConfig/isHybrid 与 settings-changed 监听卸载——改造后必须仍绿）
    - xingran-react-frontend/src/components/layout/shared/useRouteTabs.ts（D-05 范本 44-48 行）
    - .planning/phases/115-selector-completion-zustand-selector/115-PATTERNS.md（Pattern Assignments → layoutStore 段：4 条改造规则）
  </read_first>
  <action>
将 useLayout 体内 288-299 行的 `useLayoutStore()` 无参整店解构替换为 10 个独立 const，每个字段一次 `useLayoutStore((s) => s.字段名)`：currentLayout、configuration、sidebarCollapsed、density、syncFromSettings、toggleSidebar、setSidebarCollapsed、setDensity、setLayout、saveState。

约束：
1. 两个 effect 原样保留——effect 1（302-306，data-layout/data-density 属性同步，依赖 [currentLayout, density]，体内 useLayoutStore.getState() 静态调用不动）与 effect 2（311-323，settings-changed 监听 + 卸载清理，依赖 [syncFromSettings]）。effect 依赖数组中的标识符名不变，只是来源从解构变为独立 const。
2. `layoutConfig = layoutConfigs[currentLayout]`（325 行）与返回对象（327-345 行）一字不改：layout 别名、10 字段、6 个派生布尔 isClassic/isHybrid/isInnovative/isCompact/isComfortable/isSpacious 全部原样——layoutStore.test.ts:104-149 直接断言它们。
3. 禁止对象 selector 与 useShallow（同 Task 1 约束 3）。
4. 只动 useLayout 函数体的订阅来源；store 本体与文件内其他代码一行不动。
  </action>
  <verify>
    <automated>! grep -qE "useLayoutStore\(\)" xingran-react-frontend/src/store/layoutStore.ts && test "$(grep -cE 'useLayoutStore\(\(s\) =>' xingran-react-frontend/src/store/layoutStore.ts)" -ge 10 && cd xingran-react-frontend && npx vitest run src/store/layoutStore.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - behavior: useLayout 返回对象（layoutConfig + 6 派生布尔 + 10 字段）与改造前逐字段相同；settings-changed 事件监听注册/卸载行为不变
    - source-assertion: `grep -cE "useLayoutStore\(\)" xingran-react-frontend/src/store/layoutStore.ts` == 0
    - source-assertion: `grep -cE "useLayoutStore\(\(s\) =>" xingran-react-frontend/src/store/layoutStore.ts` >= 10
    - test-command: `cd xingran-react-frontend && npx vitest run src/store/layoutStore.test.ts` 全绿（:104-149 派生值断言通过）
    - cli: `cd xingran-react-frontend && npm run type-check` exit 0
  </acceptance_criteria>
  <done>
useLayout 内部为 10 个逐字段 selector；`useLayoutStore()` 无参调用在 layoutStore.ts 归零；2 个 effect、layoutConfig、6 个派生布尔、返回对象一字不改；layoutStore.test.ts 全绿；type-check 通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新增 | 本 plan 仅改客户端 store 订阅形态（React 渲染层），不触任何输入处理、认证、权限或网络面（RESEARCH Security Domain：Applicable ASVS Categories 全部 no） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01-A | Tampering（行为漂移） | useTabs/useLayout 返回形态 | mitigate | 返回对象一字不改 + tabsStore.test.ts / layoutStore.test.ts:104-149 派生值断言守护；grep 断言锁定无参调用归零 |
| T-115-01-B | DoS（无限重渲） | 新增 selector | mitigate | 禁止对象 selector/无 useShallow 的字面量返回（v5 Object.is 比较会死循环）；一律逐字段单值 selector，回归由全量测试兜底 |
| T-115-SC | Tampering | 依赖安装 | mitigate（不适用） | 本 plan 零新增依赖（RESEARCH Package Legitimacy Audit：本 phase 不安装任何包），无 supply-chain 暴露，无 [ASSUMED]/[SUS] 包 |
</threat_model>

<verification>
- `grep -rnE "use(Tabs|Layout)Store\(\)" xingran-react-frontend/src/store/` 零命中
- `cd xingran-react-frontend && npx vitest run src/store/tabsStore.test.ts src/store/layoutStore.test.ts src/components/layout/shared/__tests__/TabBar.render.test.tsx src/components/layout/shared/__tests__/useRouteTabs.test.tsx src/hooks/useUtilityHooks.test.tsx` 全绿
- `cd xingran-react-frontend && npm run lint && npm run type-check` 通过（D-02 七 gate 相关项）
- 判红前注意 RESEARCH Pitfall 3：冷缓存批量首跑可能出现 1 例超时假失败，隔离重跑绿即非回归
</verification>

<success_criteria>
1. useTabs：14 个逐字段 selector，返回对象（含 hasTabs）一字不改，消费组件零改动（SELECTOR-01）
2. useLayout：10 个逐字段 selector，2 个 effect + layoutConfig + 6 个派生布尔原样（SELECTOR-02）
3. 两个 store 文件无参 `useXxxStore()` grep 归零
4. 既有测试全绿 + lint/type-check 零回归（D-04 口径）；无 Profiler 渲染计数断言（RESEARCH Pitfall 4）
</success_criteria>

<output>
Create `.planning/phases/115-selector-completion-zustand-selector/115-01-SUMMARY.md` when done
</output>
