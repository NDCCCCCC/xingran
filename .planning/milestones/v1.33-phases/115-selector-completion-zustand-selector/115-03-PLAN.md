---
phase: 115-selector-completion-zustand-selector
plan: 03
type: execute
wave: 2
depends_on:
  - 115-01
  - 115-02
files_modified:
  - xingran-react-frontend/src/pages/login/index.tsx
  - xingran-react-frontend/src/pages/login/index.test.tsx
  - xingran-react-frontend/src/pages/login/__tests__/index.test.tsx
  - xingran-react-frontend/src/pages/profile/index.tsx
  - xingran-react-frontend/src/pages/my-notices/detail.tsx
  - xingran-react-frontend/src/pages/my-notices/__tests__/detail.test.tsx
  - xingran-react-frontend/src/pages/duty/my-duty/index.tsx
  - xingran-react-frontend/src/components/dashboard/settings/DashboardScopeSelector.tsx
  - xingran-react-frontend/src/components/dashboard/settings/__tests__/DashboardScopeSelector.test.tsx
  - xingran-react-frontend/src/components/NotificationBell.tsx
autonomous: true
requirements:
  - SELECTOR-05
user_setup: []
must_haves:
  truths:
    - "SELECTOR-05 的 6 处页面级整店订阅全部单字段 selector 化：login/index.tsx:47-48（3 个 action）、profile:47（updateUser）、my-notices/detail:19（markAsRead）、my-duty:64（user）、DashboardScopeSelector:25（user）"
    - "NotificationBell.tsx:67-75 纳入本 phase（OQ-1 定案采纳 RESEARCH 推荐 a）：7 个 selector（unreadCount/notifications/loading + 4 action），使 criterion 5 全库 grep 口径在本 phase 可验"
    - "4 个测试 mock 更新为 dual-form（selector ? selector(state) : state），let 变量保持调用时求值不快照；DashboardScopeSelector.test.tsx 的 isAdmin/dataScope 断言语义（V4 守护项）原样"
    - "全库无参 useXxxStore() 调用 grep == 12 处且全部位于 Phase 116/120 清单文件（dashboardStore 家族 11 处 + useTabSync 1 处）；全量测试 + lint + type-check 零回归"
  artifacts:
    - path: "xingran-react-frontend/src/pages/login/index.tsx"
      provides: "login/fetchMenus/fetchPermissions 3 个单字段 selector"
      contains: "useAuthStore((s) => s.login)"
    - path: "xingran-react-frontend/src/components/dashboard/settings/DashboardScopeSelector.tsx"
      provides: "user 单字段 selector（isAdmin/dataScope 派生保留）"
      contains: "useAuthStore((s) => s.user)"
    - path: "xingran-react-frontend/src/components/NotificationBell.tsx"
      provides: "noticeStore 7 个单字段 selector"
      contains: "useNoticeStore((s) => s.unreadCount)"
    - path: "xingran-react-frontend/src/components/dashboard/settings/__tests__/DashboardScopeSelector.test.tsx"
      provides: "dual-form authStore mock（mockUser 调用时求值）"
      contains: "selector ? selector"
  key_links:
    - from: "4 个测试 mock 文件"
      to: "dual-form mock 范本 useRouteTabs.test.tsx:24-36"
      via: "useXxxStoreFn 同时支持 selector 调用与无参调用"
      pattern: "selector \\? selector\\("
    - from: "6 处页面级组件 + NotificationBell"
      to: "对应 store 单字段订阅"
      via: "每字段一次 useXxxStore((s) => s.field)"
      pattern: "Store\\(\\(s\\) =>"
---

<objective>
SELECTOR-05：页面级 6 处 action-only / state 单字段整店订阅清理 + NotificationBell 1 处（OQ-1 纳入），并把 4 个非 selector 兼容的测试 mock 更新为 dual-form（组件 selector 化后这些 mock 必然红，与组件同任务落位）；最后执行 phase gate（全库 grep 断言 + 全量测试 + lint/type-check）。

Purpose: 完成本 phase 最后一批改造点，使全库剩余无参 `useXxxStore()` 调用仅剩 Phase 116（dashboard 11 处）与 Phase 120（useTabSync 死代码 1 处）有明确归属的 12 处——criterion 5 在本 phase 结束时口径闭合。
Output: 6 个源文件 + 4 个测试文件订阅/mock 形态改造 + phase gate 全绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/115-selector-completion-zustand-selector/115-CONTEXT.md
@.planning/phases/115-selector-completion-zustand-selector/115-RESEARCH.md
@.planning/phases/115-selector-completion-zustand-selector/115-PATTERNS.md

<interfaces>
<!-- planner 实读核验（2026-09-14）。SELECTOR-05 六处中 4 处为 action-only、2 处实为 state 字段 user（RESEARCH 已纠正 REQUIREMENTS 的"action-only"表述）： -->

action-only 4 处：login/index.tsx:47 `const { login } = useAuthStore();` + :48 `const { fetchMenus, fetchPermissions } = useMenuStore();`；profile/index.tsx:47 `const { updateUser } = useAuthStore();`；my-notices/detail.tsx:19 `const { markAsRead } = useNoticeStore();`（:49 useCallback 依赖 [id, navigate, markAsRead] 不变）。

state 字段 2 处：my-duty/index.tsx:64 `const { user } = useAuthStore();`；DashboardScopeSelector.tsx:25 `const { user } = useAuthStore();`（:26 isAdmin 派生、:31 dataScope 派生保留）。

NotificationBell.tsx:67-75（OQ-1 纳入）解构 7 项：unreadCount / notifications / loading / markAsRead / markAllAsRead / removeNotification / setNotifications。

4 个必改 mock（PATTERNS 逐文件对照）：login/index.test.tsx:21-30（authStore + menuStore 两 mock）；login/__tests__/index.test.tsx:20-24（authStore）；my-notices/__tests__/detail.test.tsx:15-18（noticeStore，let noticeStoreState 须调用时求值）；DashboardScopeSelector.test.tsx:15-17（authStore，let mockUser 须调用时求值，:47/:56/:76 改 mockUser 后 admin 分支断言 :46-53 必须仍通过）。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: login/profile/my-notices detail 单字段 selector 化 + 3 个 mock dual-form 化（SELECTOR-05 前簇）</name>
  <files>xingran-react-frontend/src/pages/login/index.tsx, xingran-react-frontend/src/pages/login/index.test.tsx, xingran-react-frontend/src/pages/login/__tests__/index.test.tsx, xingran-react-frontend/src/pages/profile/index.tsx, xingran-react-frontend/src/pages/my-notices/detail.tsx, xingran-react-frontend/src/pages/my-notices/__tests__/detail.test.tsx</files>
  <read_first>
    - xingran-react-frontend/src/pages/login/index.tsx（:47-48 订阅行及消费上下文）
    - xingran-react-frontend/src/pages/profile/index.tsx（:47）与 xingran-react-frontend/src/pages/my-notices/detail.tsx（:19 + :49 useCallback 依赖）
    - xingran-react-frontend/src/pages/login/index.test.tsx:21-30、src/pages/login/__tests__/index.test.tsx:20-24、src/pages/my-notices/__tests__/detail.test.tsx:15-18（三个待改 mock 现状）
    - xingran-react-frontend/src/components/layout/shared/__tests__/useRouteTabs.test.tsx（:24-36 dual-form mock 既有范本——照抄结构）
    - xingran-react-frontend/src/components/layout/shared/useRouteTabs.ts（D-05 范本 :45-46 action selector 行）
    - .planning/phases/115-selector-completion-zustand-selector/115-PATTERNS.md（Pattern Assignments → SELECTOR-05 段 + 4 mock 文件逐文件改造对照）
  </read_first>
  <action>
源文件（3 个 selector + 2 个单字段）：
1. login/index.tsx:47-48 → `const login = useAuthStore((s) => s.login);` + `const fetchMenus = useMenuStore((s) => s.fetchMenus);` + `const fetchPermissions = useMenuStore((s) => s.fetchPermissions);`（跨两 store 共 3 个 action selector）。
2. profile/index.tsx:47 → `const updateUser = useAuthStore((s) => s.updateUser);`。
3. my-notices/detail.tsx:19 → `const markAsRead = useNoticeStore((s) => s.markAsRead);`（:49 useCallback 依赖 [id, navigate, markAsRead] 不变）。

mock 文件（dual-form，逐文件照 PATTERNS 对照）：
4. login/index.test.tsx:21-30：authStore 与 menuStore 两个 vi.mock 工厂都改为 dual-form——工厂内定义 `useXxxStoreFn`，函数体 `selector ? selector(state) : state`，state 含对应 mock action（mockLogin / mockFetchMenus / mockFetchPermissions 来自 vi.hoisted，工厂内可直引）。
5. login/__tests__/index.test.tsx:20-24：authStore 改 dual-form，state 提到工厂作用域，login 的 vi.fn() 身份保持（供断言）。
6. my-notices/__tests__/detail.test.tsx:15-18：noticeStore 改 dual-form——**必须保持对 let 变量 noticeStoreState 的调用时求值**（函数体内读 noticeStoreState，不得在工厂执行时快照；beforeEach 重赋值必须继续生效）。

约束：action 引用直接 `s => s.action`，禁止 useMemo/useCallback 包裹（zustand 5.0.15 action 身份稳定）；禁止对象 selector 与 useShallow；只动订阅行与 mock 工厂，登录流程/SM2 加密链路/其余测试逻辑一行不动（RESEARCH Security Domain V2/V3 不适用）。
  </action>
  <verify>
    <automated>! grep -qE "use(Auth|Menu|Notice)Store\(\)" xingran-react-frontend/src/pages/login/index.tsx xingran-react-frontend/src/pages/profile/index.tsx xingran-react-frontend/src/pages/my-notices/detail.tsx && grep -q "selector ? selector" xingran-react-frontend/src/pages/login/index.test.tsx xingran-react-frontend/src/pages/login/__tests__/index.test.tsx xingran-react-frontend/src/pages/my-notices/__tests__/detail.test.tsx && cd xingran-react-frontend && npx vitest run src/pages/login/ src/pages/my-notices/__tests__/detail.test.tsx src/pages/profile/ && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - behavior: 登录提交调 login→fetchMenus→fetchPermissions 流程行为不变；profile 更新与通知已读标记行为不变；4 个受影响测试文件全绿（含 login"导出为函数组件"纯 import 断言）
    - source-assertion: 3 个源文件内 `use(Auth|Menu|Notice)Store()` 无参调用 == 0
    - source-assertion: 3 个 mock 文件均含 dual-form 结构（`selector ? selector` 命中）
    - test-command: `cd xingran-react-frontend && npx vitest run src/pages/login/ src/pages/my-notices/__tests__/detail.test.tsx src/pages/profile/` 全绿（判红前先隔离重跑——Pitfall 3 冷缓存假失败）
    - cli: `cd xingran-react-frontend && npm run type-check` exit 0
  </acceptance_criteria>
  <done>
login/profile/my-notices detail 全部单字段 selector；3 个 mock dual-form 化且 let 变量惰性求值保持；login 簇 + profile + detail 测试全绿；type-check 通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: my-duty/DashboardScopeSelector/NotificationBell selector 化 + DashboardScopeSelector mock dual-form 化（SELECTOR-05 后簇 + OQ-1 纳入）</name>
  <files>xingran-react-frontend/src/pages/duty/my-duty/index.tsx, xingran-react-frontend/src/components/dashboard/settings/DashboardScopeSelector.tsx, xingran-react-frontend/src/components/dashboard/settings/__tests__/DashboardScopeSelector.test.tsx, xingran-react-frontend/src/components/NotificationBell.tsx</files>
  <read_first>
    - xingran-react-frontend/src/pages/duty/my-duty/index.tsx（:64 订阅行）
    - xingran-react-frontend/src/components/dashboard/settings/DashboardScopeSelector.tsx（:25 订阅 + :26 isAdmin 派生 + :31 dataScope 派生）
    - xingran-react-frontend/src/components/NotificationBell.tsx（:67-75 解构块 + 全部消费点）
    - xingran-react-frontend/src/components/dashboard/settings/__tests__/DashboardScopeSelector.test.tsx（:15-17 mock + :46-53 admin 分支断言——改造后必须仍绿）
    - xingran-react-frontend/src/components/layout/shared/__tests__/useRouteTabs.test.tsx（:24-36 dual-form 范本）
    - .planning/phases/115-selector-completion-zustand-selector/115-PATTERNS.md（SELECTOR-05 段 state 字段 2 处对照 + OQ-1 挂账点说明 + mock 文件 4 改造对照）
  </read_first>
  <action>
1. my-duty/index.tsx:64 → `const user = useAuthStore((s) => s.user);`（state 字段，非 action——RESEARCH 已纠正）。
2. DashboardScopeSelector.tsx:25 → `const user = useAuthStore((s) => s.user);`；:26 行 isAdmin 派生与 :31 行 dataScope 派生计算原样保留。
3. NotificationBell.tsx:67-75 → 拆 7 个独立 selector：unreadCount / notifications / loading（3 state）+ markAsRead / markAllAsRead / removeNotification / setNotifications（4 action）。（OQ-1 定案：纳入本 phase——RESEARCH 推荐 a，1 行级改动让 criterion 5 全库口径可验；其两个测试 notificationBell.interact.test.tsx 与 components.render.test.tsx 均用真实 store，零 mock 风险。）
4. DashboardScopeSelector.test.tsx:15-17 → authStore mock 改 dual-form：工厂内 `useAuthStoreFn` 函数体 `selector ? selector({ user: mockUser }) : { user: mockUser }`——**保持对 let mockUser 的调用时求值**（beforeEach 重赋值继续生效）。

约束（V4 守护项）：此处用 dual-form 而非简化返回——:46-53"管理员 → 显示系统仪表盘 Switch"等 isAdmin/dataScope 断言语义必须原样保住，不得为让测试变绿而削弱 mock 返回结构；禁止对象 selector 与 useShallow；只动订阅行与 mock 工厂。
  </action>
  <verify>
    <automated>! grep -qE "use(Auth|Notice)Store\(\)" xingran-react-frontend/src/pages/duty/my-duty/index.tsx xingran-react-frontend/src/components/dashboard/settings/DashboardScopeSelector.tsx xingran-react-frontend/src/components/NotificationBell.tsx && test "$(grep -cE 'useNoticeStore\(\(s\) =>' xingran-react-frontend/src/components/NotificationBell.tsx)" -eq 7 && grep -q "selector ? selector" xingran-react-frontend/src/components/dashboard/settings/__tests__/DashboardScopeSelector.test.tsx && cd xingran-react-frontend && npx vitest run src/pages/duty/my-duty/ src/components/dashboard/settings/__tests__/DashboardScopeSelector.test.tsx src/components/__tests__/notificationBell.interact.test.tsx src/components/__tests__/components.render.test.tsx && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - behavior: DashboardScopeSelector 管理员/非管理员分支渲染行为不变（:46-53 断言通过）；my-duty 用户信息展示不变；NotificationBell 未读计数/通知列表/已读与批量已读行为不变
    - source-assertion: 3 个源文件内 `use(Auth|Notice)Store()` 无参调用 == 0
    - source-assertion: NotificationBell.tsx 内 `useNoticeStore((s) =>` 计数 == 7
    - source-assertion: DashboardScopeSelector.test.tsx 含 dual-form mock 结构
    - test-command: `cd xingran-react-frontend && npx vitest run src/pages/duty/my-duty/ src/components/dashboard/settings/__tests__/DashboardScopeSelector.test.tsx src/components/__tests__/notificationBell.interact.test.tsx src/components/__tests__/components.render.test.tsx` 全绿
    - cli: `cd xingran-react-frontend && npm run type-check` exit 0
  </acceptance_criteria>
  <done>
my-duty/DashboardScopeSelector/NotificationBell 全部单字段 selector（7+1+1）；DashboardScopeSelector mock dual-form 化且 isAdmin/dataScope 断言语义原样；后簇测试全绿；type-check 通过。
  </done>
</task>

<task type="auto">
  <name>Task 3: Phase gate——全库 grep 断言 + 全量测试 + lint/type-check（criterion 5 收口）</name>
  <files></files>
  <read_first>
    - .planning/phases/115-selector-completion-zustand-selector/115-VALIDATION.md（Per-Task Verification Map 的 Criterion 5 行 + 七 gate 行）
    - .planning/phases/115-selector-completion-zustand-selector/115-RESEARCH.md（OQ-1 口径定案说明 + Pitfall 3 假失败警告）
    - 本 phase 前两个 plan 的 SUMMARY（确认 SELECTOR-01~04 已落位，grep 断言口径包含其文件）
  </read_first>
  <action>
不修改任何源文件，仅执行 phase 级验收：
1. 全库 grep 断言（criterion 5，OQ-1 定案全库口径）：统计 src 下非测试文件的无参 `use[A-Z][A-Za-z0-9]*Store()` 调用，期望恰好 12 处，且全部位于 Phase 116/120 清单文件——`hooks/useTabSync.ts`（Phase 120 DEAD-01 删）、`hooks/useWidgetData.ts`（Phase 116 DASH-01，2 处）、`pages/dashboard-system/` 下 7 文件 + `components/dashboard/settings/DashboardSettings.tsx` + `components/dashboard/widgets/WidgetEditor.tsx`（Phase 116 DASH-03，9 处）。出现清单外命中 = 本 phase 有遗漏，回到对应簇修复。
2. 全量回归：`npx vitest run` 全绿（判红前必须重跑一次——RESEARCH Pitfall 3：冷缓存批量首跑 login"导出为函数组件"可能出现 1 例超时假失败，隔离跑/热缓存重跑绿即非回归）。
3. 七 gate 相关项：`npm run lint` + `npm run type-check` 通过（D-02）；`npm run test:coverage` / `npm run size` 留给 /gsd:verify-work 的 phase 级验证，不在本 task 重复。
4. 若断言失败，修复后重跑本 task 全部命令；不修改断言口径来让门通过。
  </action>
  <verify>
    <automated>HITS=$(grep -rnE "use[A-Z][A-Za-z0-9]*Store\(\)" xingran-react-frontend/src --include="*.ts" --include="*.tsx" | grep -vE "__tests__|\.test\.") && echo "$HITS" && test "$(echo "$HITS" | grep -c .)" -eq 12 && test -z "$(echo "$HITS" | grep -vE "hooks/(useTabSync|useWidgetData)\.ts|pages/dashboard-system/|components/dashboard/")" && cd xingran-react-frontend && npx vitest run && npm run lint && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: 全库无参调用恰好 12 处，且路径全部匹配 `hooks/(useTabSync|useWidgetData).ts` / `pages/dashboard-system/` / `components/dashboard/` 三组白名单前缀
    - test-command: `cd xingran-react-frontend && npx vitest run` 全绿（首跑单例超时红须重跑复核后再定性）
    - cli: `cd xingran-react-frontend && npm run lint` exit 0 且 `npm run type-check` exit 0
  </acceptance_criteria>
  <done>
criterion 5 口径闭合：本 phase 16+1 处归零，剩余 12 处全部归属 Phase 116/120；全量测试 + lint + type-check 零回归（D-04/D-02）。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 mock ↔ 权限相关组件行为 | DashboardScopeSelector 消费 authStore.user（isAdmin/dataScope）——mock 改造不得弱化权限分支断言（RESEARCH Security Domain 明示守护项） |
| 无新增运行时攻击面 | 本 plan 仅改订阅形态与测试 mock，不触认证（V2）/会话（V3）/输入校验（V5）/密码学（V6） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-03-A | Elevation of Privilege（测试弱化） | DashboardScopeSelector.test.tsx mock | mitigate | 用 dual-form mock（selector ? selector({user: mockUser}) : {user: mockUser}）而非简化返回；:46-53 isAdmin/dataScope 断言原样保留并作为 verify 命令的一部分跑绿 |
| T-115-03-B | Tampering（行为漂移） | my-notices/detail.tsx:49 useCallback 依赖 | mitigate | markAsRead 引用语义不变（zustand action 身份稳定），依赖数组不动；detail.test.tsx 守护 |
| T-115-03-C | Repudiation（验收口径漂移） | Task 3 grep 断言 | mitigate | 断言口径写死（12 处 + 三组白名单前缀），禁止修改断言来让门通过；清单外命中必须回修 |
| T-115-SC | Tampering | 依赖安装 | mitigate（不适用） | 本 plan 零新增依赖（RESEARCH Package Legitimacy Audit：本 phase 不安装任何包），无 supply-chain 暴露，无 [ASSUMED]/[SUS] 包 |
</threat_model>

<verification>
- `grep -rnE "use[A-Z][A-Za-z0-9]*Store\(\)" xingran-react-frontend/src --include="*.ts" --include="*.tsx" | grep -vE "__tests__|\.test\."` 恰好 12 处，全部位于 Phase 116/120 清单文件
- `cd xingran-react-frontend && npx vitest run` 全绿（Pitfall 3：判红前重跑）
- `cd xingran-react-frontend && npm run lint && npm run type-check` 通过（D-02 七 gate 相关项）
- DashboardScopeSelector.test.tsx 的 admin 分支断言（isAdmin/dataScope）原样通过
</verification>

<success_criteria>
1. SELECTOR-05 六处全部单字段 selector 化（4 action-only + 2 state 字段 user）
2. NotificationBell.tsx 7 个 selector（OQ-1 纳入，RESEARCH 推荐 a）
3. 4 个 mock dual-form 化，let 变量调用时求值保持，权限断言语义零弱化
4. criterion 5 收口：全库剩余 12 处全部归属 Phase 116/120；全量测试 + lint/type-check 零回归（D-04/D-02）
</success_criteria>

<output>
Create `.planning/phases/115-selector-completion-zustand-selector/115-03-SUMMARY.md` when done
</output>
