---
phase: "116-dashboard-cascade-dashboard"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - xingran-react-frontend/src/hooks/useWidgetData.ts
  - xingran-react-frontend/src/pages/dashboard-system/index.tsx
  - xingran-react-frontend/src/pages/dashboard-system/components/DashboardHome.tsx
  - xingran-react-frontend/src/pages/dashboard-system/components/DashboardList.tsx
  - xingran-react-frontend/src/pages/dashboard-system/components/DashboardView.tsx
  - xingran-react-frontend/src/pages/dashboard-system/components/DashboardEdit.tsx
  - xingran-react-frontend/src/pages/dashboard-system/edit.tsx
  - xingran-react-frontend/src/pages/dashboard-system/view.tsx
  - xingran-react-frontend/src/components/dashboard/widgets/WidgetEditor.tsx
  - xingran-react-frontend/src/components/dashboard/settings/DashboardSettings.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-03
must_haves:
  truths:
    - "useWidgetData（:70,115）getCachedWidgetData 改为 getState().getCachedWidgetData 调用，cacheWidgetData 改为 selector 获取，action 引用稳定——widget memo 恢复有效，单 widget 数据更新只重渲该 widget"
    - "dashboard 模块 9 处整店订阅全部改为逐字段 selector，任意 dashboardStore 字段变化不再牵连未订阅该字段的组件重渲"
    - "useWidgetData queryFn deps 从 5 项缩减为 4 项（移除 getCachedWidgetData），queryKey 仍含 widget.id 保持去重语义"
    - "全库 dashboard-system 目录与 useWidgetData grep 无参 useDashboardStore() 归零"
  artifacts:
    - path: "xingran-react-frontend/src/hooks/useWidgetData.ts"
      provides: "useWidgetData selector 化：DASH-01"
      contains: "useDashboardStore.getState().getCachedWidgetData"
    - path: "xingran-react-frontend/src/pages/dashboard-system/index.tsx"
      provides: "逐字段 selector：setPageMode"
      contains: "useDashboardStore((s) => s.setPageMode)"
    - path: "xingran-react-frontend/src/pages/dashboard-system/components/DashboardHome.tsx"
      provides: "逐字段 selector：defaultDashboard / defaultDashboardLoading / fetchDefaultDashboard"
      contains: "useDashboardStore((s) => s.defaultDashboard)"
    - path: "xingran-react-frontend/src/pages/dashboard-system/components/DashboardList.tsx"
      provides: "逐字段 selector：dashboards / listLoading / listPagination / fetchDashboards / createDashboard / deleteDashboard / duplicateDashboard / setDefaultDashboard"
      contains: "useDashboardStore((s) => s.dashboards)"
    - path: "xingran-react-frontend/src/pages/dashboard-system/components/DashboardView.tsx"
      provides: "逐字段 selector：currentDashboard / currentLoading / fetchDashboard / setViewMode / updateWidgetLayouts / clearCurrentDashboard"
      contains: "useDashboardStore((s) => s.currentDashboard)"
    - path: "xingran-react-frontend/src/pages/dashboard-system/components/DashboardEdit.tsx"
      provides: "逐字段 selector：currentDashboard / currentLoading / fetchDashboard / setViewMode / updateWidgetLayouts / addWidget / selectWidget / selectedWidgetId / clearCurrentDashboard"
      contains: "useDashboardStore((s) => s.currentDashboard)"
    - path: "xingran-react-frontend/src/pages/dashboard-system/edit.tsx"
      provides: "逐字段 selector：currentDashboard / currentLoading / fetchDashboard / setViewMode / updateWidgetLayouts / addWidget / selectWidget / selectedWidgetId / clearCurrentDashboard"
      contains: "useDashboardStore((s) => s.currentDashboard)"
    - path: "xingran-react-frontend/src/pages/dashboard-system/view.tsx"
      provides: "逐字段 selector：currentDashboard / currentLoading / fetchDashboard / setViewMode / updateWidgetLayouts / clearCurrentDashboard"
      contains: "useDashboardStore((s) => s.currentDashboard)"
    - path: "xingran-react-frontend/src/components/dashboard/widgets/WidgetEditor.tsx"
      provides: "逐字段 selector：updateWidget / removeWidget"
      contains: "useDashboardStore((s) => s.updateWidget)"
    - path: "xingran-react-frontend/src/components/dashboard/settings/DashboardSettings.tsx"
      provides: "逐字段 selector：currentDashboard / updateDashboard"
      contains: "useDashboardStore((s) => s.currentDashboard)"
  key_links:
    - from: "useWidgetData.ts fetchWidgetData"
      to: "useDashboardStore.getState()"
      via: "getState().getCachedWidgetData() 内部调用，getCachedWidgetData 移出闭包"
      pattern: "useDashboardStore\\.getState\\(\\)"
    - from: "9 处 dashboard-system 组件"
      to: "dashboardStore 字段 selector"
      via: "逐字段 useDashboardStore((s) => s.field)"
      pattern: "useDashboardStore\\(\\(s\\) => s\\."
---

<objective>
DASH-01 + DASH-03：useWidgetData selector 化（action 引用稳定）+ dashboard 模块 9 处整店订阅收敛为逐字段 selector。

Purpose: useWidgetData 的 getCachedWidgetData 改为 getState().x 内部调用后，queryFn deps 从 5 项缩减为 4 项，action 引用稳定 widget memo 恢复有效；9 处整店订阅改为逐字段 selector 后，dashboardStore 任意字段变化不再牵连未订阅该字段的组件重渲。
Output: 10 个文件改动，既有测试/lint/type-check 零回归。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-dashboard-cascade-dashboard/116-CONTEXT.md
@.planning/ROADMAP.md Phase 116 段
@.planning/REQUIREMENTS.md DASH-01 / DASH-03
@.planning/phases/115-selector-completion-zustand-selector/115-01-PLAN.md（格式范本）
@.planning/phases/115-selector-completion-zustand-selector/115-SUMMARY.md（Phase 115 selector 风格定案）

<interfaces>
<!-- 核验完毕（2026-09-14）：useWidgetData 现状与改造后对比 -->

useWidgetData.ts:70 现状：
const { getCachedWidgetData, cacheWidgetData } = useDashboardStore();

useWidgetData.ts:115（useBatchWidgetData）现状：
const { cacheWidgetData } = useDashboardStore();

改造后（getCachedWidgetData 移出闭包，用 getState().x 内部调用）：
const cacheWidgetData = useDashboardStore((s) => s.cacheWidgetData);
// getCachedWidgetData 不再是 hook 调用，fetchWidgetData 内部通过 useDashboardStore.getState().getCachedWidgetData(widget.id) 读取

queryFn deps 改造后：[widget.id, widget.enabled, widget.dataSource, cacheWidgetData]
（移除 getCachedWidgetData，因为读取路径改为 getState() 内部调用）

---

9 处整店订阅改造范式（复用 useRouteTabs.ts:45-48 风格，逐字段 useDashboardStore((s) => s.field)）：

index.tsx:30 现状：
const { setPageMode } = useDashboardStore();

改造后：
const setPageMode = useDashboardStore((s) => s.setPageMode);

DashboardHome.tsx:19 现状：
const { defaultDashboard, defaultDashboardLoading, fetchDefaultDashboard } = useDashboardStore();

改造后（3 个独立 const）：
const defaultDashboard = useDashboardStore((s) => s.defaultDashboard);
const defaultDashboardLoading = useDashboardStore((s) => s.defaultDashboardLoading);
const fetchDefaultDashboard = useDashboardStore((s) => s.fetchDefaultDashboard);

DashboardList.tsx:47 现状（解构 8 个）：
const { dashboards, listLoading, listPagination, fetchDashboards, createDashboard, deleteDashboard, duplicateDashboard, setDefaultDashboard } = useDashboardStore();

改造后（8 个独立 const）。

DashboardView.tsx:36 现状（解构 6 个）：
const { currentDashboard, currentLoading, fetchDashboard, setViewMode, updateWidgetLayouts, clearCurrentDashboard } = useDashboardStore();

改造后（6 个独立 const）。

DashboardEdit.tsx:43 现状（解构 9 个）：
const { currentDashboard, currentLoading, fetchDashboard, setViewMode, updateWidgetLayouts, addWidget, selectWidget, selectedWidgetId, clearCurrentDashboard } = useDashboardStore();

改造后（9 个独立 const）。

edit.tsx:41 现状（解构 9 个，与 DashboardEdit.tsx:43 相同字段）：
改造后（9 个独立 const）。

view.tsx:31 现状（解构 6 个，与 DashboardView.tsx:36 相同字段）：
改造后（6 个独立 const）。

WidgetEditor.tsx:33 现状：
const { updateWidget, removeWidget } = useDashboardStore();

改造后（2 个独立 const）。

DashboardSettings.tsx:30 现状：
const { currentDashboard, updateDashboard } = useDashboardStore();

改造后（2 个独立 const）。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useWidgetData getCachedWidgetData 移出闭包，cacheWidgetData 改 selector（DASH-01）</name>
  <files>xingran-react-frontend/src/hooks/useWidgetData.ts</files>
  <read_first>
    - xingran-react-frontend/src/hooks/useWidgetData.ts（:70 useDashboardStore() 解构、:78-84 queryFn useCallback、:115 useBatchWidgetData useDashboardStore() 解构）
    - xingran-react-frontend/src/store/dashboardStore.ts（getCachedWidgetData :416-427 读取 widgetDataCache Map、cacheWidgetData :405-413 写入 widgetDataCache Map）
    - xingran-react-frontend/src/store/noticeStore.ts（:8-11 P1-M4 模块级 Set 模式参考）
  </read_first>
  <action>
改造 useWidgetData.ts 两处整店订阅为 selector + getState 内部调用：

1. useWidgetData（:70）：
   - 删除 `const { getCachedWidgetData, cacheWidgetData } = useDashboardStore();`
   - 改为 `const cacheWidgetData = useDashboardStore((s) => s.cacheWidgetData);`（逐字段 selector）
   - getCachedWidgetData 读取路径改为 fetchWidgetData 内部调用 `useDashboardStore.getState().getCachedWidgetData(widget.id)`

2. fetchWidgetData 函数签名改造：
   - 移除 _getCachedWidgetData 参数（:50），fetchWidgetData 不再接收此函数
   - 内部读取改为 `useDashboardStore.getState().getCachedWidgetData(widget.id)`

3. useCallback queryFn（:78-84）deps 改造：
   - 删除 getCachedWidgetData 从 deps 数组
   - 保留 [widget.id, widget.enabled, widget.dataSource, cacheWidgetData]
   - cacheWidgetData action 引用稳定（selector 返回的函数引用不变）

4. useBatchWidgetData（:115）：
   - 删除 `const { cacheWidgetData } = useDashboardStore();`
   - 改为 `const cacheWidgetData = useDashboardStore((s) => s.cacheWidgetData);`

约束：
1. queryFn 内部通过 getState().x 调用 getCachedWidgetData 时序语义不变（每次 fetch 时读当前缓存）
2. cacheWidgetData action 引用稳定不引入 useMemo 包装（action 本身在 store 生命周期内引用不变）
3. 禁止引入 useShallow（全库 0 使用）
4. fetchWidgetData 移除参数后内部调用点：result = await dataFetcher.fetch 后直接调用 cacheWidgetData 写入，getCachedWidgetData 仅在 fetchWidgetData 内部使用一次
5. useBatchWidgetData batch queryFn 内部 cacheWidgetData 调用路径不变（仍为闭包捕获），仅订阅形态改 selector
  </action>
  <verify>
    <automated>! grep -qE "useDashboardStore\(\)" xingran-react-frontend/src/hooks/useWidgetData.ts && grep -cE "useDashboardStore\(\(s\) => s\.(getCachedWidgetData|cacheWidgetData)" xingran-react-frontend/src/hooks/useWidgetData.ts</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -cE "useDashboardStore\(\)" xingran-react-frontend/src/hooks/useWidgetData.ts` == 0
    - source-assertion: `grep -cE "useDashboardStore\(\(s\) => s\.cacheWidgetData" xingran-react-frontend/src/hooks/useWidgetData.ts` >= 1
    - source-assertion: `grep -cE "useDashboardStore\.getState\(\)\.getCachedWidgetData" xingran-react-frontend/src/hooks/useWidgetData.ts` >= 1
    - source-assertion: `grep -cE "getCachedWidgetData.*useCallback" xingran-react-frontend/src/hooks/useWidgetData.ts` == 0（deps 不含 getCachedWidgetData）
    - cli: cd xingran-react-frontend && npm run type-check exit 0
  </acceptance_criteria>
  <done>
useWidgetData 两处整店订阅改为 selector（cacheWidgetData）和 getState 内部调用（getCachedWidgetData）；queryFn deps 缩减为 4 项；type-check 通过。
</done>
</task>

<task type="auto">
  <name>Task 2: dashboard-system 9 处整店订阅改为逐字段 selector（DASH-03）</name>
  <files>
    - xingran-react-frontend/src/pages/dashboard-system/index.tsx
    - xingran-react-frontend/src/pages/dashboard-system/components/DashboardHome.tsx
    - xingran-react-frontend/src/pages/dashboard-system/components/DashboardList.tsx
    - xingran-react-frontend/src/pages/dashboard-system/components/DashboardView.tsx
    - xingran-react-frontend/src/pages/dashboard-system/components/DashboardEdit.tsx
    - xingran-react-frontend/src/pages/dashboard-system/edit.tsx
    - xingran-react-frontend/src/pages/dashboard-system/view.tsx
    - xingran-react-frontend/src/components/dashboard/widgets/WidgetEditor.tsx
    - xingran-react-frontend/src/components/dashboard/settings/DashboardSettings.tsx
  </files>
  <read_first>
    - xingran-react-frontend/src/pages/dashboard-system/index.tsx（:30 setPageMode 解构）
    - xingran-react-frontend/src/pages/dashboard-system/components/DashboardHome.tsx（:19 defaultDashboard/defaultDashboardLoading/fetchDefaultDashboard 解构）
    - xingran-react-frontend/src/pages/dashboard-system/components/DashboardList.tsx（:47 8 字段解构）
    - xingran-react-frontend/src/pages/dashboard-system/components/DashboardView.tsx（:36 6 字段解构）
    - xingran-react-frontend/src/pages/dashboard-system/components/DashboardEdit.tsx（:43 9 字段解构）
    - xingran-react-frontend/src/pages/dashboard-system/edit.tsx（:41 9 字段解构）
    - xingran-react-frontend/src/pages/dashboard-system/view.tsx（:31 6 字段解构）
    - xingran-react-frontend/src/components/dashboard/widgets/WidgetEditor.tsx（:33 updateWidget/removeWidget 解构）
    - xingran-react-frontend/src/components/dashboard/settings/DashboardSettings.tsx（:30 currentDashboard/updateDashboard 解构）
    - xingran-react-frontend/src/store/dashboardStore.ts（确认各字段名与类型）
  </read_first>
  <action>
逐文件改造，每处整店解构改为多个独立 const useDashboardStore((s) => s.field) 调用。改造文件与字段对应关系：

index.tsx（1 个）：setPageMode

DashboardHome.tsx（3 个）：defaultDashboard、defaultDashboardLoading、fetchDefaultDashboard

DashboardList.tsx（8 个）：dashboards、listLoading、listPagination、fetchDashboards、createDashboard、deleteDashboard、duplicateDashboard、setDefaultDashboard

DashboardView.tsx（6 个）：currentDashboard、currentLoading、fetchDashboard、setViewMode、updateWidgetLayouts、clearCurrentDashboard

DashboardEdit.tsx（9 个）：currentDashboard、currentLoading、fetchDashboard、setViewMode、updateWidgetLayouts、addWidget、selectWidget、selectedWidgetId、clearCurrentDashboard

edit.tsx（9 个）：currentDashboard、currentLoading、fetchDashboard、setViewMode、updateWidgetLayouts、addWidget、selectWidget、selectedWidgetId、clearCurrentDashboard

view.tsx（6 个）：currentDashboard、currentLoading、fetchDashboard、setViewMode、updateWidgetLayouts、clearCurrentDashboard

WidgetEditor.tsx（2 个）：updateWidget、removeWidget

DashboardSettings.tsx（2 个）：currentDashboard、updateDashboard

约束：
1. 每字段一个独立 const，形态严格照抄 useRouteTabs.ts:45-48 范本：`const fieldName = useDashboardStore((s) => s.fieldName);`
2. action 字段（函数）直接返回函数引用，调用方不变（闭包捕获的函数引用稳定）
3. 禁止对象 selector（`useDashboardStore(s => ({...}))`）与 useShallow（全库 0 使用）
4. effect 依赖数组中的字段标识符名不变，仅来源从解构变量变为 useDashboardStore((s) => s.字段名)
5. 各文件的 useEffect 依赖数组一字不改（仅内部变量名映射改变，语义不变）
6. 业务逻辑、数据流、JSX 渲染逻辑一行不动
  </action>
  <verify>
    <automated>! grep -qE "useDashboardStore\(\)" xingran-react-frontend/src/pages/dashboard-system/index.tsx && ! grep -qE "useDashboardStore\(\)" xingran-react-frontend/src/pages/dashboard-system/components/DashboardHome.tsx && ! grep -qE "useDashboardStore\(\)" xingran-react-frontend/src/pages/dashboard-system/components/DashboardList.tsx && ! grep -qE "useDashboardStore\(\)" xingran-react-frontend/src/pages/dashboard-system/components/DashboardView.tsx && ! grep -qE "useDashboardStore\(\)" xingran-react-frontend/src/pages/dashboard-system/components/DashboardEdit.tsx && ! grep -qE "useDashboardStore\(\)" xingran-react-frontend/src/pages/dashboard-system/edit.tsx && ! grep -qE "useDashboardStore\(\)" xingran-react-frontend/src/pages/dashboard-system/view.tsx && ! grep -qE "useDashboardStore\(\)" xingran-react-frontend/src/components/dashboard/widgets/WidgetEditor.tsx && ! grep -qE "useDashboardStore\(\)" xingran-react-frontend/src/components/dashboard/settings/DashboardSettings.tsx</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: 上述 9 个文件 grep "useDashboardStore\(\)" 全部 0 命中
    - source-assertion: 各文件 grep "useDashboardStore\(\(s\) => s\." 数量 >= 原解构字段数
    - cli: cd xingran-react-frontend && npm run type-check exit 0
    - cli: cd xingran-react-frontend && npm run lint -- --max-warnings 0 xingran-react-frontend/src/pages/dashboard-system xingran-react-frontend/src/components/dashboard exit 0
  </acceptance_criteria>
  <done>
9 处整店订阅全部改为逐字段 selector；9 个文件 grep 无参 useDashboardStore() 归零；type-check + lint 通过。
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Zustand store | 纯前端订阅形态重构，无网络/认证面，无输入处理变更 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-01-A | Tampering（行为漂移） | useWidgetData queryFn deps | mitigate | getCachedWidgetData 移出 deps 后 queryKey 仍含 widget.id 保持去重；getState() 内部调用时机为每次 fetch 时读取，等同原实现 |
| T-116-01-B | DoS（无限重渲） | dashboard-system 9 处 selector | mitigate | 逐字段 selector 机械保证：未订阅字段变化不触发重渲；禁止对象 selector/useShallow |
| T-116-01-C | Tampering（引用断裂） | action 字段 selector | mitigate | action 字段（函数）selector 返回稳定函数引用；effect dep 数组仅变量名映射改变，语义不变 |
| T-116-01-SC | Supply Chain | 依赖安装 | mitigate（不适用） | 本 plan 零新增依赖，纯重构无 supply-chain 暴露 |
</threat_model>

<verification>
- `grep -rnE "useDashboardStore\(\)" xingran-react-frontend/src/pages/dashboard-system/ xingran-react-frontend/src/components/dashboard/ xingran-react-frontend/src/hooks/useWidgetData.ts` 零命中
- `grep -cE "useDashboardStore\(\(s\) => s\." xingran-react-frontend/src/pages/dashboard-system/index.tsx` >= 1（每文件实际数量见 acceptance_criteria）
- `cd xingran-react-frontend && npm run type-check` exit 0
- `cd xingran-react-frontend && npm run lint -- --max-warnings 0` exit 0
</verification>

<success_criteria>
1. useWidgetData getCachedWidgetData 移出 queryFn deps，改为 getState().getState().getCachedWidgetData 内部调用，cacheWidgetData 改 selector（DASH-01）
2. dashboard 模块 9 处整店订阅全部改为逐字段 selector，useDashboardStore() 无参调用 9 文件归零（DASH-03）
3. 既有 effect 依赖数组、业务逻辑、JSX 渲染逻辑一字不改
4. type-check + lint 零回归（D-04 口径）
</success_criteria>

<output>
Create `.planning/phases/116-dashboard-cascade-dashboard/116-01-SUMMARY.md` when done
</output>
