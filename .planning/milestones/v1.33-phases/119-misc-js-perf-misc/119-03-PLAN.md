---
phase: "119-misc-js-perf-misc"
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - xingran-react-frontend/src/pages/operations/workstations/index.tsx
autonomous: true
requirements:
  - MISC-05
must_haves:
  truths:
    - "workstations/index.tsx expandedRowRender 内联回调 useCallback 化，HealthCard / WorkstationDeviceTable memo 生效，展开行操作不触发已渲染行重渲"
  artifacts:
    - path: "xingran-react-frontend/src/pages/operations/workstations/index.tsx"
      provides: "expandedRowRender useCallback 化"
      contains: "useCallback"
---

<objective>
MISC-05：workstations/index.tsx expandedRowRender 内联回调 useCallback 化。
Purpose: HealthCard 和 WorkstationDeviceTable 已用 React.memo 包裹，expandedRowRender 内联回调每次创建新函数引用导致 memo 失效；useCallback 化后 memo 保护生效，展开行操作不触发已渲染行重渲。
Output: workstations/index.tsx expandedRowRender 区域改造。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md（MISC-05）

<interfaces>
<!-- 已实读核验（2026-09-14）-->

workstations/index.tsx :652-687 expandedRowRender 区域现状（行号精确）：
- expandable={{ expandedRowRender: (record: WorkstationOps) => (...) , ... }}
- expandedRowRender 内有两个内联回调：
  1. HealthCard onApplyException（:657-665）：window.open 跳转
  2. WorkstationDeviceTable onDeviceChange（:670）：refreshData 引用
  3. WorkstationDeviceTable onBadgeClick（:671-677）：setDrawerState 闭包捕获

HealthCard 和 WorkstationDeviceTable 组件已用 React.memo 包裹（组件定义处），内联回调导致每次父渲染新函数引用，memo 保护失效。

useCallback 签名参考（取自现有 hooks）：
- 无参数的 () => void：用 useCallback(() => {...}, [])
- 带参数的 (arg: T) => void：用 useCallback((arg) => {...}, [dep])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: workstations expandedRowRender 内联回调 useCallback 化（MISC-05）</name>
  <files>xingran-react-frontend/src/pages/operations/workstations/index.tsx</files>
  <read_first>
    - xingran-react-frontend/src/pages/operations/workstations/index.tsx:640-690（expandable expandedRowRender 区域）
    - xingran-react-frontend/src/pages/operations/workstations/index.tsx:1-50（import 区域，确认 useCallback 可用）
    - xingran-react-frontend/src/pages/operations/workstations/index.tsx:200-240（组件定义处，确认是 React.FC 或 function 组件）
  </read_first>
  <action>
将 workstations/index.tsx expandedRowRender 内 3 个内联回调 useCallback 化：

1. 在组件内（expandedRowRender 定义前）提取并 useCallback 化：
```typescript
// MISC-05: useCallback 化使 memo 组件避免不必要重渲
const handleApplyException = useCallback(
  (workstationId: string) => {
    window.open(`/asset/reconciliation/exception-rules/new?workstationId=${workstationId}`, "_blank");
  },
  []
);

const handleDeviceChange = useCallback(() => {
  refreshData();
}, [refreshData]);

const handleBadgeClick = useCallback(
  (assetId: string, workstationId: string) => {
    setDrawerState({ open: true, assetId, workstationId, activeTab: "summary" });
  },
  []
);
```

2. expandedRowRender 内联回调替换为 useCallback 引用：
```typescript
expandedRowRender={(record: WorkstationOps) => (
  <div>
    {reconciliationVisible && (
      <HealthCard
        workstationId={record.id}
        onApplyException={() => handleApplyException(record.id)}
      />
    )}
    <WorkstationDeviceTable
      workstationId={record.id}
      conflictTypeMap={assetConflictMap}
      onDeviceChange={handleDeviceChange}
      onBadgeClick={(assetId, conflictType) => handleBadgeClick(assetId, record.id)}
    />
  </div>
)}
```

约束：
- handleDeviceChange deps 依赖 refreshData（已有 useCallback 包装），handleBadgeClick deps 依赖 setDrawerState（来自 useState），handleApplyException 无依赖。
- expandedRowRender 从内联箭头函数改为 () => JSX.Element，不额外包 useMemo。
- 不改 HealthCard / WorkstationDeviceTable 组件本身。
- onApplyException/onDeviceChange/onBadgeClick 签名不变。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npx vitest run src/pages/operations/workstations/__tests__/index.test.tsx 2>/dev/null || echo "NO_TEST_FILE" && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - behavior: expandedRowRender 内联回调提取为 useCallback，HealthCard / WorkstationDeviceTable memo 保护生效
    - source-assertion: `grep -c "useCallback" xingran-react-frontend/src/pages/operations/workstations/index.tsx` >= 3
    - source-assertion: `grep "expandedRowRender" xingran-react-frontend/src/pages/operations/workstations/index.tsx | grep -c "useCallback"` == 0
    - cli: `cd xingran-react-frontend && npm run type-check` exit 0
  </acceptance_criteria>
  <done>
expandedRowRender 3 个内联回调 useCallback 化；memo 组件保护生效；type-check 通过。
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-side only | 纯前端性能重构，无网络/认证/权限面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-03-A | Performance | expandedRowRender callbacks | mitigate | useCallback 化使 memo 组件避免不必要重渲，展开行操作不触发已渲染行 |
| T-119-SC | Tampering | npm/pip/cargo installs | mitigate（不适用） | 本 plan 零新增依赖，纯性能重构 |
</threat_model>

<verification>
- `cd xingran-react-frontend && npm run type-check` exit 0
- `cd xingran-react-frontend && npm run lint` exit 0
- `grep "useCallback" xingran-react-frontend/src/pages/operations/workstations/index.tsx` 命中 >= 3
</verification>

<success_criteria>
1. expandedRowRender 3 个内联回调提取为 useCallback（handleApplyException / handleDeviceChange / handleBadgeClick）
2. HealthCard / WorkstationDeviceTable memo 保护生效（闭包引用稳定）
3. type-check + lint 全绿
</success_criteria>

<output>
Create `.planning/phases/119-misc-js-perf-misc/119-03-SUMMARY.md` when done
</output>
