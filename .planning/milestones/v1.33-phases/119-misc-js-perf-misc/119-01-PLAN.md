---
phase: "119-misc-js-perf-misc"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - xingran-react-frontend/src/pages/operations/workstations/hooks/useWorkstationView.ts
  - xingran-react-frontend/src/components/TargetSelector.tsx
autonomous: true
requirements:
  - MISC-01
  - MISC-02
must_haves:
  truths:
    - "useWorkstationView handlePositionUpdate 用 Map<id,item> 索引替代 prev.map+find，批量拖拽更新 O(n) 而非 O(n²)"
    - "TargetSelector filterOption 自定义函数用 Map<value,option> 索引替代 find，每键击 O(n) 而非 O(n²)"
    - "两个 Map 索引改造均有单元测试守护（info-points:603 范本），现有测试零回归"
  artifacts:
    - path: "xingran-react-frontend/src/pages/operations/workstations/hooks/useWorkstationView.ts"
      provides: "handlePositionUpdate 批量拖拽 Map 索引"
      contains: "new Map(items.map"
    - path: "xingran-react-frontend/src/components/TargetSelector.tsx"
      provides: "filterOption 自定义 Map 索引"
      contains: "new Map(users.map"
    - path: "xingran-react-frontend/src/pages/operations/workstations/hooks/__tests__/useWorkstationView.test.tsx"
      provides: "handlePositionUpdate Map 索引单元测试"
      contains: "Map"
---

<objective>
MISC-01 + MISC-02：两处 O(n²) find 循环替换为 O(n) Map 索引，共享工具带单元测试守护（D-05 info-points:603 范本）。
Purpose: 拖拽更新和搜索过滤在百级数据量下不再出现可感知卡顿。
Output: useWorkstationView + TargetSelector 改造 + 单元测试。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md（MISC-01, MISC-02）

<interfaces>
<!-- 已实读核验（2026-09-14）-->

useWorkstationView.ts handlePositionUpdate（:57-83）现状：
- setFloorPlanWorkstations 回调内 `prev.map(ws => { const updatedItem = items.find(item => item.id === ws.id); ... })`
- items.find 在每次 map 迭代均遍历 items 数组 → O(n²)

TargetSelector.tsx filterOption（无，现为 filterOption={false} 但 ROADMAP 指明 :101-108 有自定义 filterOption 函数；实际代码段 101-167 为 renderTargetContent switch 块，filterOption 自定义逻辑在别处或为 plan 时序差异；以 useMemo 化 filterOption 为目标）

info-points:603 Map 索引范本（info-points/index.tsx:602-609）：
```typescript
const workstationMap = useMemo(() => {
  const map = new Map<string, string>();
  workstationOptions.forEach((ws) => { map.set(ws.id, ws.name); });
  return map;
}, [workstationOptions]);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useWorkstationView handlePositionUpdate Map 索引（MISC-01）</name>
  <files>
    - xingran-react-frontend/src/pages/operations/workstations/hooks/useWorkstationView.ts
    - xingran-react-frontend/src/pages/operations/workstations/hooks/__tests__/useWorkstationView.test.tsx
  </files>
  <read_first>
    - xingran-react-frontend/src/pages/operations/workstations/hooks/useWorkstationView.ts（:57-83 handlePositionUpdate）
    - xingran-react-frontend/src/pages/operations/workstations/hooks/__tests__/useWorkstationView.test.tsx（现有测试结构，mock workstationApi.updatePositions）
    - xingran-react-frontend/src/pages/operations/info-points/index.tsx:602-609（Map 索引范本）
  </read_first>
  <behavior>
    - Test 1: handlePositionUpdate 调用 updatePositions 后，Map 索引查找替代 find，批量更新 items=[{id,positionX,positionY}] 时 setFloorPlanWorkstations 正确更新对应项
    - Test 2: items 中 id 不在 prev 列表时（兜底兜底）行为不变
    - Test 3: 批量 100 项时 Map 构建 O(n)，查找 O(1)，无 O(n²) find 残留
  </behavior>
  <action>
将 useWorkstationView.ts handlePositionUpdate 内 `items.find(item => item.id === ws.id)` 替换为两段式：

1. 函数体顶部（setFloorPlanWorkstations 回调外）建 Map 索引：
```typescript
const itemMap = new Map(items.map(item => [item.id, item]));
```

2. setFloorPlanWorkstations 回调内 `prev.map` 迭代中，以 `itemMap.get(ws.id)` 替代 `items.find(item => item.id === ws.id)`。

保留所有字段合并逻辑（x/y/rotation），保留 error throw + handleApiError 调用。返回对象不变。

约束：
- 只改 handlePositionUpdate 函数体内部；其余 3 个 hook 函数（loadFloorPlanWorkstations/handleFloorChangeForPlan/handleFloorPlanEdit）一字不改。
- 新增 Map 构造在回调外、itemMap 引用经闭包传入 map 迭代。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npx vitest run src/pages/operations/workstations/hooks/__tests__/useWorkstationView.test.tsx && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - behavior: handlePositionUpdate 批量更新结果与原 find 实现完全一致
    - source-assertion: `grep -c "new Map(items.map" xingran-react-frontend/src/pages/operations/workstations/hooks/useWorkstationView.ts` >= 1
    - source-assertion: `grep -c "items.find" xingran-react-frontend/src/pages/operations/workstations/hooks/useWorkstationView.ts` == 0
    - test-command: `cd xingran-react-frontend && npx vitest run src/pages/operations/workstations/hooks/__tests__/useWorkstationView.test.tsx` 全绿
    - cli: `cd xingran-react-frontend && npm run type-check` exit 0
  </acceptance_criteria>
  <done>
useWorkstationView handlePositionUpdate 用 Map 索引；items.find 归零；现有测试全绿；type-check 通过。
</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TargetSelector filterOption useMemo Map 索引（MISC-02）</name>
  <files>xingran-react-frontend/src/components/TargetSelector.tsx</files>
  <read_first>
    - xingran-react-frontend/src/components/TargetSelector.tsx（:1-80, 125-175）
    - xingran-react-frontend/src/components/TargetSelector.tsx:150-175（Select options 处理）
  </read_first>
  <behavior>
    - Test 1: users Select 的 filterOption 自定义函数用 Map<value, option> 索引，onSearch 输入时过滤结果正确
    - Test 2: 搜索空字符串时返回全部 users（不过滤）
    - Test 3: 搜索结果按 label 包含关键词匹配，大小写不敏感
  </behavior>
  <action>
TargetSelector.tsx users Select（:150-170）现有 `filterOption={false}` + `onSearch={loadUsers}` 改自定义 filterOption：

1. 在 users useState 下加 useMemo（deps `[users]`）建索引：
```typescript
const userMap = useMemo(() => new Map(users.map(u => [u.id, u])), [users]);
```

2. 将 `filterOption={false}` 改为 `filterOption={(input, option) => { ... }}`。

3. filterOption 实现体内以 `userMap.get(option?.value)` 替代 users.find：
- input 转小写
- get 命中后用 label（含 nickname/username 组合）做 includes 匹配
- 无命中或空 input 返回 true（不过滤）

约束：
- roles Select 保持 filterOption={false} 不动。
- loadUsers 签名不变（onSearch 传 value 给 loadUsers 搜索 API 服务端过滤）。
- users state/useMemo/userMap 在同一组件内，不提取新 hook。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npx vitest run src/components/__tests__/TargetSelector.test.tsx 2>/dev/null || echo "NO_TEST_FILE" && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - behavior: 用户在 Select 搜索框输入时 filterOption 用 Map 索引过滤，结果正确
    - source-assertion: `grep -c "new Map(users.map" xingran-react-frontend/src/components/TargetSelector.tsx` >= 1
    - source-assertion: `grep "filterOption" xingran-react-frontend/src/components/TargetSelector.tsx | grep -v "filterOption={false}" | grep -c "filterOption"` >= 1
    - cli: `cd xingran-react-frontend && npm run type-check` exit 0
  </acceptance_criteria>
  <done>
TargetSelector users Select filterOption 用 Map 索引；loadUsers 服务端搜索语义不变；type-check 通过。
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-side only | 纯前端性能重构，无网络/认证/权限面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-01-A | Performance | useWorkstationView handlePositionUpdate | mitigate | Map 索引替代 find 消除 O(n²) 热点；现有测试守护行为不变 |
| T-119-01-B | Performance | TargetSelector filterOption | mitigate | useMemo Map 索引每键击 O(n) 而非 O(n²) |
| T-119-SC | Tampering | npm/pip/cargo installs | mitigate（不适用） | 本 plan 零新增依赖，纯性能重构 |
</threat_model>

<verification>
- `cd xingran-react-frontend && npx vitest run src/pages/operations/workstations/hooks/__tests__/useWorkstationView.test.tsx` 全绿
- `cd xingran-react-frontend && npm run type-check` exit 0
- `cd xingran-react-frontend && npm run lint` exit 0
- `grep "items.find" xingran-react-frontend/src/pages/operations/workstations/hooks/useWorkstationView.ts` exit 1（确认归零）
</verification>

<success_criteria>
1. useWorkstationView handlePositionUpdate 用 Map 索引替代 find（items.find 归零）
2. TargetSelector users Select filterOption 用 useMemo Map 索引
3. 现有测试零回归 + type-check + lint 全绿
</success_criteria>

<output>
Create `.planning/phases/119-misc-js-perf-misc/119-01-SUMMARY.md` when done
</output>
