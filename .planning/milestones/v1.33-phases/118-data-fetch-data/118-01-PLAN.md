---
phase: "118-data-fetch-data"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx
  - xingran-react-frontend/src/lib/queryKeys.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-04
must_haves:
  truths:
    - "DATA-01: VirtualMachineList 4 处 vdiServerApi.list 裸调用（:136/:468/:484/:653）共享 react-query queryKey ['vdi','servers']，同页多组件只发 1 次网络请求"
    - "DATA-04: useHolidayData（holidays/management 两份）fetchAvailableYears 内部 fetchList 调用改为 Promise.all 并行，请求数 2→1"
  artifacts:
    - path: "xingran-react-frontend/src/lib/queryKeys.ts"
      provides: "vdi.servers queryKey 添加（DATA-01）"
      contains: "vdi:"
    - path: "xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx"
      provides: "vdiServerApi.list → useQuery(['vdi','servers']) 去重，4 处调用归 1"
      contains: "useQuery"
    - path: "xingran-react-frontend/src/pages/duty/holidays/hooks/useHolidayData.ts"
      provides: "fetchAvailableYears 内 fetchList 改为 Promise.all 并行"
      contains: "Promise.all"
    - path: "xingran-react-frontend/src/pages/duty/management/hooks/useHolidayData.ts"
      provides: "同上"
      contains: "Promise.all"
  key_links:
    - from: "VirtualMachineList/index.tsx"
      to: "useQuery(['vdi','servers'])"
      via: "4 处调用统一使用 queryClient.fetchQuery"
      pattern: "vdiServerApi\\.list"
    - from: "useHolidayData.ts (holidays/management)"
      to: "Promise.all([getHolidayYears(), getHolidayList()])"
      via: "fetchAvailableYears 内联合并"
      pattern: "Promise\\.all"
---

<objective>
DATA-01 + DATA-04：两个纯性能重构，零行为变更，零回归测试新增。

DATA-01: VirtualMachineList 4 处 vdiServerApi.list 裸调用（lines 136/468/484/653）归一为 react-query useQuery + shared queryKey ['vdi','servers']，消除同页重复请求。

DATA-04: useHolidayData（holidays/management 两份副本）fetchAvailableYears 内 sequential fetchList 调用改为 Promise.all 并行，请求数 2→1。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md (DATA-01, DATA-04)
@.planning/STATE.md

<interfaces>
<!-- queryKeys.ts vdi 段添加（DATA-01） -->
<!-- 现有 queryKeys.ts 只有 dict/role/dept/duty 等，本 plan 添加 vdi 命名空间 -->
export const queryKeys = {
  // ... existing ...
  vdi: {
    all: ["vdi"] as const,
    servers: () => ["vdi", "servers"] as const,
  },
}

<!-- VirtualMachineList/index.tsx 4 处 vdiServerApi.list 调用 -->
<!-- Line 136: preloadVDIData 内 -->
<!-- Line 468: openCreateModal 内 -->
<!-- Line 484: openCreateModal fallback 内 -->
<!-- Line 653: loadQuickCreateDefaults 内 -->
<!-- 所有调用完全相同: vdiServerApi.list({ current: 1, pageSize: 100 }) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add vdi.servers queryKey to queryKeys.ts (DATA-01 foundation)</name>
  <files>xingran-react-frontend/src/lib/queryKeys.ts</files>
  <read_first>
    - xingran-react-frontend/src/lib/queryKeys.ts（全文，确认现有键结构）
    - xingran-react-frontend/src/App.tsx（QueryClientProvider 已挂载，确认 staleTime 5min 默认）
  </read_first>
  <action>
在 queryKeys.ts 的 queryKeys 对象末尾添加 vdi 命名空间：
```typescript
vdi: {
  all: ["vdi"] as const,
  servers: () => ["vdi", "servers"] as const,
},
```
遵循既有 as const 元组风格，与 duty/role 等命名空间完全对齐。
  </action>
  <verify>
    <automated>grep -q "vdi:" xingran-react-frontend/src/lib/queryKeys.ts && grep -q "servers:" xingran-react-frontend/src/lib/queryKeys.ts</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: queryKeys.vdi.servers() === ["vdi","servers"] as const
    - cli: cd xingran-react-frontend && npm run type-check exit 0
  </acceptance_criteria>
  <done>
queryKeys.vdi.servers() 添加，type-check 通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: VirtualMachineList 4处 vdiServerApi.list 替换为 useQuery (DATA-01)</name>
  <files>xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx</files>
  <read_first>
    - xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx（:1-30 imports, :93 vdiServers state, :119-179 preloadVDIData, :455-495 openCreateModal, :651-751 loadQuickCreateDefaults）
    - xingran-react-frontend/src/App.tsx（QueryClientProvider 已挂载，staleTime 5min）
    - xingran-react-frontend/src/pages/vdi/VirtualMachineList/__tests__/index.test.tsx（确认现有测试仍有效）
  </read_first>
  <action>
在 VirtualMachineList/index.tsx 中：

1. 添加 react-query import: `import { useQuery } from "@tanstack/react-query";`
2. 在组件内（vdiServers state 定义处附近）添加 useQuery hook:
```typescript
const { data: serverData } = useQuery({
  queryKey: ["vdi", "servers"],
  queryFn: () => vdiServerApi.list({ current: 1, pageSize: 100 }),
  staleTime: 5 * 60 * 1000, // 5 min, matches App default
});
```
3. 替换 4 处 vdiServerApi.list 调用：
   - :136 preloadVDIData 内: `const serverResult = await vdiServerApi.list(...); const servers = serverResult.data?.list || [];` → 删除整行，改为 `const servers = serverData || [];`（删除整个 if !availableServer 块外的 serverResult 获取）
   - :468 openCreateModal 内: 同上
   - :484 openCreateModal fallback 内: 同上
   - :653 loadQuickCreateDefaults 内: 同上

关键约束：
1. 所有 4 处共享同一个 `const servers = serverData || []`（无需重复 await）
2. 删除对应的 `await vdiServerApi.list(...)` 行，不再有独立网络请求
3. availableServer 查找改为: `const availableServer = servers.find((s) => s.status === 0);`（servers 已是 VDIServer[]）
4. vdiServers state（:93 `useState<VDIServer[]>([])`）仍然保留给下拉框渲染使用，赋值改为: `setVdiServers(serverData || [])`
5. 不要修改 vmApi.listVTPPlatforms / listRunPositions / listStorages / listNetworks 等其他 API 调用
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npx vitest run src/pages/vdi/VirtualMachineList/__tests__/index.test.tsx src/pages/vdi/VirtualMachineList/__tests__/index.render.test.tsx && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - behavior: 页面加载后只发出 1 次 VDI 服务器列表网络请求（而不是 4 次），同页打开创建弹窗不产生额外请求
    - source-assertion: `grep -c "vdiServerApi\.list" xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx` == 0（已迁移到 useQuery）
    - source-assertion: `grep -c "useQuery.*vdi.*servers" xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx` >= 1
    - test-command: npx vitest run src/pages/vdi/VirtualMachineList/__tests__/index.test.tsx 全绿
    - cli: npm run type-check exit 0
  </acceptance_criteria>
  <done>
4 处 vdiServerApi.list 裸调用归并为 1 个 useQuery，VDI 服务器下拉框功能不变，现有测试全绿。
  </done>
</task>

<task type="auto">
  <name>Task 3: useHolidayData fetchAvailableYears Promise.all 合并（DATA-04）</name>
  <files>
    - xingran-react-frontend/src/pages/duty/holidays/hooks/useHolidayData.ts
    - xingran-react-frontend/src/pages/duty/management/hooks/useHolidayData.ts
  </files>
  <read_first>
    - xingran-react-frontend/src/pages/duty/holidays/hooks/useHolidayData.ts（:56-72 fetchAvailableYears，sequential fetchList call）
    - xingran-react-frontend/src/pages/duty/management/hooks/useHolidayData.ts（:44-58 fetchYears，sequential fetchList call）
    - xingran-react-frontend/src/pages/duty/holidays/hooks/useHolidayData.test.tsx（现有测试，确认行为不变）
    - xingran-react-frontend/src/pages/duty/management/hooks/__tests__/useHolidayData.test.tsx（现有测试）
  </read_first>
  <action>
在 holidays/hooks/useHolidayData.ts fetchAvailableYears 中：
将 sequential 调用:
```
const result = await getHolidayYears();
setAvailableYears(years);
if (years.length > 0 && year === undefined) {
  const latestYear = years[0];
  setYear(latestYear);
  fetchList(latestYear);   // <-- 串行 second request
}
```
替换为 Promise.all 并行:
```
const [yearsResult, listResult] = await Promise.all([
  getHolidayYears(),
  getHolidayList(year ?? new Date().getFullYear()),
]);
const years = yearsResult.data || [];
setAvailableYears(years);
if (years.length > 0) {
  const latestYear = years[0];
  setYear(latestYear);
  setDataSource(listResult.data || []);
} else {
  setDataSource([]);
}
```

在 management/hooks/useHolidayData.ts fetchYears 中做完全相同改造（parallel structure）。

关键约束：
1. getHolidayList 调用参数用 year（当前选中，如果 undefined 则 fallback 到今年）
2. 不再调用 fetchList（避免双重 setState），直接 setDataSource(listResult.data)
3. 两个文件结构相同，改造方式一致
4. 不改变返回的 loading 状态语义（fetchYears/fetchAvailableYears 本身是 async 函数，调用方用 await）
5. 如果 getHolidayYears 返回空数组，setDataSource([]) 也要调用（保持原有行为）
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npx vitest run src/pages/duty/holidays/hooks/useHolidayData.test.tsx src/pages/duty/management/hooks/__tests__/useHolidayData.test.tsx && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - behavior: fetchAvailableYears 只产生 1 次网络 round-trip（而不是 2 次 sequential）
    - source-assertion: holidays hooks grep "Promise.all" >= 1 && management hooks grep "Promise.all" >= 1
    - source-assertion: holidays hooks grep -v "fetchList" useHolidayData.ts | grep "await getHolidayList" == 0（不再串行调用 fetchList）
    - test-command: npx vitest run src/pages/duty/holidays/hooks/useHolidayData.test.tsx 全绿
    - test-command: npx vitest run src/pages/duty/management/hooks/__tests__/useHolidayData.test.tsx 全绿
    - cli: npm run type-check exit 0
  </acceptance_criteria>
  <done>
两个 useHolidayData 的 fetchAvailableYears/fetchYears 内 Promise.all 并行，请求数 2→1，现有测试全绿。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | 同页多组件共享 react-query 缓存命中时减少 RTT，无安全影响 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-01-A | Denial of Service | useQuery staleTime | mitigate | 5 min staleTime 与 App.tsx defaultOptions 一致；硬刷新场景 DATA-02 处理 |
| T-118-01-B | Information Disclosure | VDI server list cache | accept | 数据为业务非敏感信息（服务器名称列表），缓存 5min 符合既有实践 |
| T-118-01-SC | Supply Chain | react-query useQuery | mitigate | 零新增依赖（react-query 已在 App.tsx 使用，queryKeys 纯数据添加） |
| T-118-04-SC | Supply Chain | Promise.all | accept | 标准 JS API，无依赖引入 |
</threat_model>

<verification>
- DATA-01: `grep -c "vdiServerApi\.list" xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx` == 0
- DATA-01: `grep -c "useQuery.*vdi.*servers" xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx` >= 1
- DATA-04 holidays: `grep "Promise.all" xingran-react-frontend/src/pages/duty/holidays/hooks/useHolidayData.ts`
- DATA-04 management: `grep "Promise.all" xingran-react-frontend/src/pages/duty/management/hooks/useHolidayData.ts`
- `cd xingran-react-frontend && npx vitest run src/pages/vdi/VirtualMachineList/__tests__/index.test.tsx src/pages/duty/holidays/hooks/useHolidayData.test.tsx src/pages/duty/management/hooks/__tests__/useHolidayData.test.tsx && npm run lint && npm run type-check`
</verification>

<success_criteria>
1. DATA-01: 4 处 vdiServerApi.list 裸调用归 1 个 useQuery，VirtualMachineList 功能不变，现有测试全绿
2. DATA-04: fetchAvailableYears Promise.all 并行，holidays + management 两处，holidays/management 测试全绿
3. lint/type-check 零回归
</success_criteria>

<output>
Create `.planning/phases/118-data-fetch-data/118-01-SUMMARY.md` when done
</output>
