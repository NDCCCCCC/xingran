---
phase: "120"
plan: "04"
type: execute
wave: 4
depends_on: []
files_modified:
  - "xingran-react-frontend/package.json"
autonomous: true
requirements:
  - DEAD-02
must_haves:
  truths:
    - "僵尸依赖移除后 npm install + build + type-check 全绿"
    - "全库无残留 import"
  artifacts:
    - path: "xingran-react-frontend/package.json"
      provides: "4 个僵尸依赖已移除"
---

<objective>
DEAD-02: 移除 4 个僵尸依赖（cron-parser / @react-spring/three / maath / @uiw/react-baidu-map），npm install + build + type-check 全绿。前置：Phase 114 MAP3D-06 已删除 BuildingMarkers/CityMarkers（@uiw/react-baidu-map 主要消费方已清）。
</objective>

<context>
**package.json 当前依赖（需移除的 4 项）：**
```json
"cron-parser": "^5.10.0",           // line 43 — zombie (调度用 @breejs/later 替代)
"@react-spring/three": "^10.1.2",    // line 31 — zombie (已迁移到 @react-three/drei)
"maath": "^0.10.8",                  // line 48 — zombie (three 扩展，已迁移)
"@uiw/react-baidu-map": "^2.7.5",   // line 38 — zombie (Phase 114 删除了 BuildingMarkers/CityMarkers)
```

**验证无残留 import:**
- `@uiw/react-baidu-map`: Phase 114 MAP3D-06 确认 BuildingMarkers.tsx / CityMarkers.tsx 已删除，全库无 @uiw/react-baidu-map import
- `@react-spring/three`: 全库 grep 验证无 import
- `maath`: 全库 grep 验证无 import
- `cron-parser`: 全库 grep 验证无 import（调度用 @breejs/later）

**重要：DEAD-02 必须在 Phase 120 其他 plan 全执行完后执行**，避免 npm install 中途被其他 plan 的文件修改打断。
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight check — verify no residual imports</name>
  <files>
    xingran-react-frontend/package.json
  </files>
  <read_first>
    xingran-react-frontend/package.json
  </read_first>
  <acceptance_criteria>
    - grep -r "@uiw/react-baidu-map\|@react-spring/three\|maath\|cron-parser" src/ returns 0
    - package.json contains all 4 packages currently
  </acceptance_criteria>
  <action>
Before modifying package.json, verify no residual imports exist in src/:
```bash
grep -r "@uiw/react-baidu-map" src/ --include="*.ts" --include="*.tsx" | head -5
grep -r "@react-spring/three" src/ --include="*.ts" --include="*.tsx" | head -5
grep -r "maath" src/ --include="*.ts" --include="*.tsx" | head -5
grep -r "cron-parser" src/ --include="*.ts" --include="*.tsx" | head -5
```
All should return empty (0 results).
</action>
</task>

<task type="auto">
  <name>Task 2: Remove 4 zombie packages from package.json (DEAD-02)</name>
  <files>
    xingran-react-frontend/package.json
  </files>
  <read_first>
    xingran-react-frontend/package.json
  </read_first>
  <acceptance_criteria>
    - package.json no longer contains: cron-parser, @react-spring/three, maath, @uiw/react-baidu-map
    - Other dependencies intact
  </acceptance_criteria>
  <action>
From package.json, remove these 4 lines from the dependencies section:
- `"cron-parser": "^5.10.0",`
- `"@react-spring/three": "^10.1.2",`
- `"maath": "^0.10.8",`
- `"@uiw/react-baidu-map": "^2.7.5",`

Do NOT modify any other dependencies.
</action>
</task>

<task type="auto">
  <name>Task 3: npm install + build + type-check verification (DEAD-02)</name>
  <files>
    xingran-react-frontend/package.json
    xingran-react-frontend/package-lock.json
  </files>
  <acceptance_criteria>
    - npm install exits 0 (no missing peer dep errors)
    - npm run build exits 0
    - npm run type-check exits 0
    - npm run lint --silent exits 0
  </acceptance_criteria>
  <action>
Run in sequence:
```bash
cd xingran-react-frontend
npm install
npm run build
npm run type-check
npm run lint --silent
```
If any step fails, report the specific error and do not proceed further.
</action>
</task>

</tasks>

<verification>
npm run build 2>&1 | tail -10
npm run type-check 2>&1 | tail -5
npm run lint --silent 2>&1 | tail -5
</verification>

<success_criteria>
- DEAD-02: 4 zombie packages removed from package.json
- npm install + build + type-check + lint all exit 0
- size-limit gate maintained (entry gzip ≤ 1MB)
</success_criteria>

<output>
Create `.planning/phases/120-bundle-dead-cleanup-bundle/120-04-SUMMARY.md` when done
</output>
