---
phase: "120"
plan: "03"
type: execute
wave: 3
depends_on: []
files_modified:
  - "xingran-react-frontend/src/hooks/useTabSync.ts"
  - "xingran-react-frontend/src/hooks/__tests__/useTabSync.test.tsx"
  - "xingran-react-frontend/src/hooks/useUtilityHooks.test.tsx"
  - "xingran-react-frontend/src/components/dashboard/DashboardView.tsx"
  - "xingran-react-frontend/src/pages/operations/building-spaces-3d/utils.ts"
  - "xingran-react-frontend/src/pages/network/executions/index.tsx"
  - "xingran-react-frontend/src/components/operations/index.ts"
  - "xingran-react-frontend/vite.config.ts"
autonomous: true
requirements:
  - DEAD-01
  - DEAD-03
must_haves:
  truths:
    - "死代码删除完成，全库无引用"
    - "vite.config 注释修正"
  artifacts:
    - path: "xingran-react-frontend/src/hooks/useTabSync.ts"
      provides: "删除（无消费方）"
    - path: "xingran-react-frontend/src/components/dashboard/DashboardView.tsx"
      provides: "删除（DashboardSelector 重构后无消费方）"
    - path: "xingran-react-frontend/src/pages/operations/building-spaces-3d/utils.ts"
      provides: "getWorkstationStats 函数删除（无调用）"
    - path: "xingran-react-frontend/src/pages/network/executions/index.tsx"
      provides: "_detailColumns 变量删除"
    - path: "xingran-react-frontend/src/components/operations/index.ts"
      provides: "barrel 删除（全库无引用）"
---

<objective>
DEAD-01: 5 处死代码删除（全库 grep 验证无引用）+ DEAD-03: vite.config.ts 注释修正。
前置条件已满足：Phase 116（DashboardSelector）改造完成 DashboardView 无消费方；Phase 117（executions columns）改造完成 _detailColumns 无消费方。
</objective>

<context>
**DEAD-01 清理项：**

1. `hooks/useTabSync.ts` — 全库仅测试文件调用（useTabSync.test.tsx + useUtilityHooks.test.tsx），非测试消费零处
2. `components/dashboard/DashboardView.tsx` — Phase 116 已重构 dashboard selector，DashboardView 无消费方（components/dashboard/__tests__/DashboardView.render.test.tsx 仅有 mock vi.mock，不算真实消费）
3. `pages/operations/building-spaces-3d/utils.ts` — `getWorkstationStats` 函数（lines 350-369）全库无调用，grep 确认
4. `pages/network/executions/index.tsx` — `_detailColumns` 变量（line 122）已无消费方，Phase 117 columns 改造后孤立
5. `components/operations/index.ts` — barrel 文件，全库无 `from "@/components/operations"` 引用（grep 确认），但 DeptSidebar/StatisticsCards 仍有直接路径导入

**DEAD-03:**
- vite.config.ts:23（react-markdown 注释行号）— 注释过时需核查实际代码
- vite.config.ts:169-171 — 注释与 vendor-md-editor 实际行为不符

**前置条件验证（来自 Phase 116/117 SUMMARYs）：**
- Phase 116-01 DashboardView selector 化改造完成
- Phase 117 executions columns 改造完成（_detailColumns 孤立）
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete useTabSync.ts + its test files (DEAD-01)</name>
  <files>
    xingran-react-frontend/src/hooks/useTabSync.ts
    xingran-react-frontend/src/hooks/__tests__/useTabSync.test.tsx
    xingran-react-frontend/src/hooks/useUtilityHooks.test.tsx
  </files>
  <read_first>
    xingran-react-frontend/src/hooks/useTabSync.ts
  </read_first>
  <acceptance_criteria>
    - `useTabSync.ts` deleted
    - `useTabSync.test.tsx` deleted
    - useUtilityHooks.test.tsx no longer imports or tests useTabSync (grep -c "useTabSync" useUtilityHooks.test.tsx = 0)
    - `grep -r "useTabSync" src/` returns only DynamicRoutes.tsx:232 comment reference (not a code import)
    - All tests still pass: npm test -- --run src/hooks/ 2>&1 | tail -10
  </acceptance_criteria>
  <action>
1. Delete `src/hooks/useTabSync.ts`
2. Delete `src/hooks/__tests__/useTabSync.test.tsx`
3. In `useUtilityHooks.test.tsx`, remove the `useTabSync` test section (lines 423-505) and the import line for useTabSync

DynamicRoutes.tsx:232 has a comment reference (not code): `// 同步初始化 routeConfigManager (为 useTabSync 等其他消费者提供 routeTitle/breadcrumb)` — this comment is fine, not a functional import.
  </action>
</task>

<task type="auto">
  <name>Task 2: Delete DashboardView.tsx (DEAD-01)</name>
  <files>
    xingran-react-frontend/src/components/dashboard/DashboardView.tsx
    xingran-react-frontend/src/components/dashboard/__tests__/DashboardView.render.test.tsx
    xingran-react-frontend/src/components/dashboard/DashboardView.css
  </files>
  <read_first>
    xingran-react-frontend/src/components/dashboard/DashboardView.tsx
  </read_first>
  <acceptance_criteria>
    - DashboardView.tsx deleted
    - DashboardView.css deleted
    - DashboardView.render.test.tsx deleted
    - `grep -r "DashboardView" src/components/` returns 0 (excluding tests already deleted)
    - npm run type-check passes
  </acceptance_criteria>
  <action>
1. Delete `src/components/dashboard/DashboardView.tsx`
2. Delete `src/components/dashboard/DashboardView.css` (there are two identical imports on lines 27-28 of DashboardView.tsx)
3. Delete `src/components/dashboard/__tests__/DashboardView.render.test.tsx`

The vi.mock in the test file mocks useWidgetPolling — since the component is deleted, the test is no longer needed.
  </action>
</task>

<task type="auto">
  <name>Task 3: Remove getWorkstationStats from building-spaces-3d utils.ts (DEAD-01)</name>
  <files>
    xingran-react-frontend/src/pages/operations/building-spaces-3d/utils.ts
  </files>
  <read_first>
    xingran-react-frontend/src/pages/operations/building-spaces-3d/utils.ts
  </read_first>
  <acceptance_criteria>
    - `grep "getWorkstationStats" utils.ts` returns nothing
    - `grep -r "getWorkstationStats" src/pages/operations/building-spaces-3d/` returns 0
    - calculateWorkstationStats function (lines 350-369) removed
    - Other functions in utils.ts remain intact
    - npm run type-check passes
  </acceptance_criteria>
  <action>
Remove the `calculateWorkstationStats` function from `utils.ts` (lines 350-369). Keep all other functions (calculateWorkstationPositions, convertApiWorkstation, getWorkstationColor, etc.).
  </action>
</task>

<task type="auto">
  <name>Task 4: Remove _detailColumns from executions index.tsx (DEAD-01)</name>
  <files>
    xingran-react-frontend/src/pages/network/executions/index.tsx
  </files>
  <read_first>
    xingran-react-frontend/src/pages/network/executions/index.tsx
  </read_first>
  <acceptance_criteria>
    - grep "_detailColumns" executions/index.tsx returns 0
    - `grep -r "_detailColumns" src/` returns 0
    - npm run type-check passes
  </acceptance_criteria>
  <action>
In `src/pages/network/executions/index.tsx`, remove the line (around 122):
  const _detailColumns = getDetailColumns({...
The `getDetailColumns` call and its result variable `_detailColumns` are unused after Phase 117 columns改造.
  </action>
</task>

<task type="auto">
  <name>Task 5: Delete operations barrel + fix vite.config comments (DEAD-01 + DEAD-03)</name>
  <files>
    xingran-react-frontend/src/components/operations/index.ts
    xingran-react-frontend/vite.config.ts
  </files>
  <read_first>
    xingran-react-frontend/src/components/operations/index.ts
    xingran-react-frontend/vite.config.ts
  </read_first>
  <acceptance_criteria>
    - operations/index.ts deleted
    - `grep -r "from \"@/components/operations\"" src/` returns 0 (all direct imports from subpaths are fine)
    - vite.config.ts:23 comment accurate (react-markdown behavior)
    - vite.config.ts:169-171 comment accurate (vendor-md-editor chunk naming)
    - npm run type-check passes
  </acceptance_criteria>
  <action>
1. Delete `src/components/operations/index.ts` (barrel file with zero consumers via the barrel path)

2. In `vite.config.ts`, fix the comment at line 23 (react-markdown 过时注释) and lines 169-171:
   - Line 23: The comment references react-markdown behavior. Verify the actual comment and correct if it describes the current behavior incorrectly.
   - Lines 169-171: The comment says `vendor-md-editor` but the actual chunk name is `vendor-md-editor`. Verify and correct comment to match actual behavior.
</action>
</task>

</tasks>

<verification>
# Dead code verification
grep -r "useTabSync\|DashboardView\|getWorkstationStats\|_detailColumns" \
  --include="*.ts" --include="*.tsx" \
  "D:/CODE/ClaudeCode/guoguo/xingran-react-frontend/src/" | \
  grep -v "node_modules\|__tests__\|.test.\|test.\|render." | head -20

npm run type-check 2>&1 | tail -5
npm test -- --run src/hooks/ 2>&1 | tail -5
</verification>

<success_criteria>
- DEAD-01: 5 dead code items deleted; full-grep confirms zero references
- DEAD-03: vite.config.ts comments corrected
- type-check clean; relevant tests pass
</success_criteria>

<output>
Create `.planning/phases/120-bundle-dead-cleanup-bundle/120-03-SUMMARY.md` when done
</output>
