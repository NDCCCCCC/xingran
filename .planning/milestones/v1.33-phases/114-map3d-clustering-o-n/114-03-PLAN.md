---
phase: 114-map3d-clustering-o-n
plan: 03
type: execute
wave: 3
depends_on:
  - 114-02
files_modified:
  - xingran-react-frontend/src/pages/operations/building-spaces-3d/__tests__/hubei-map-cleanup.test.tsx
autonomous: false
requirements:
  - MAP3D-02
  - MAP3D-05
  - MAP3D-06

must_haves:
  truths:
    - "BuildingMarkers.tsx / CityMarkers.tsx 已删除，全库 grep 零引用（MAP3D-06）"
    - "@uiw/react-baidu-map 在 src/ 内零 import——Phase 120 DEAD-02 僵尸依赖移除解锁"
    - "组件卸载后 zoomend/tiltend 监听不再触发——cleanup spy 测试锁定 removeEventListener 以同一 handler 引用被调用（MAP3D-05 行为锁定）"
    - "n=1000 楼宇缩放/倾斜切换无秒级主线程长任务、聚类视觉与改造前一致（MAP3D-02 人工验证收口）"
  artifacts:
    - path: "xingran-react-frontend/src/pages/operations/building-spaces-3d/__tests__/hubei-map-cleanup.test.tsx"
      provides: "两组件事件 cleanup spy 测试（removeEventListener 同引用断言）"
      contains: "removeEventListener"
      min_lines: 80
    - path: "xingran-react-frontend/src/pages/operations/building-spaces-3d/components/BuildingMarkers.tsx"
      provides: "已删除（不存在）"
    - path: "xingran-react-frontend/src/pages/operations/building-spaces-3d/components/CityMarkers.tsx"
      provides: "已删除（不存在）"
  key_links:
    - from: "cleanup spy 测试"
      to: "HubeiMap.tsx / HubeiMapGL.tsx init effect cleanup"
      via: "unmount 后断言 removeEventListener(event, 同一 handler 引用)"
      pattern: "toHaveBeenCalledWith|mock\\.calls"
---

<objective>
Phase 114 收尾：删除死组件 BuildingMarkers.tsx / CityMarkers.tsx（MAP3D-06，解锁 Phase 120 DEAD-02 的 @uiw/react-baidu-map 移除），新增事件 cleanup spy 测试锁定 MAP3D-05 行为，最后以人工浏览器验证收口 MAP3D-02（n=1000 无秒级长任务 + 聚类视觉一致）。

Purpose: 死组件是 @uiw/react-baidu-map 仅有的两处 import；cleanup 行为需要测试守护防回归；MAP3D-02 的正式验收是人工验证（jsdom 无法测真实 WebGL 地图长任务，VALIDATION.md Manual-Only 表）。
Output: 两死组件删除、cleanup 测试绿、全量前端 gate 绿、MAP3D-02 人工验证通过（或按 A4 降级路径记录）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-map3d-clustering-o-n/114-RESEARCH.md
@.planning/phases/114-map3d-clustering-o-n/114-PATTERNS.md
@.planning/phases/114-map3d-clustering-o-n/114-VALIDATION.md

# 待删除的死组件
@xingran-react-frontend/src/pages/operations/building-spaces-3d/components/BuildingMarkers.tsx
@xingran-react-frontend/src/pages/operations/building-spaces-3d/components/CityMarkers.tsx

# cleanup 测试对象（Plan 02 改造后形态）
@xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMap.tsx
@xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMapGL.tsx

# mock 风格范本与测试基建
@xingran-react-frontend/src/pages/operations/building-spaces-3d/components/__tests__/BaiduMapScript.test.ts
@xingran-react-frontend/src/pages/operations/building-spaces-3d/__tests__/index.render.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除死组件 BuildingMarkers.tsx / CityMarkers.tsx（MAP3D-06）</name>
  <files>（删除）xingran-react-frontend/src/pages/operations/building-spaces-3d/components/BuildingMarkers.tsx、xingran-react-frontend/src/pages/operations/building-spaces-3d/components/CityMarkers.tsx</files>
  <read_first>
    - xingran-react-frontend/src/pages/operations/building-spaces-3d/components/BuildingMarkers.tsx（确认待删——:6 import @uiw/react-baidu-map）
    - xingran-react-frontend/src/pages/operations/building-spaces-3d/components/CityMarkers.tsx（同上）
    - .planning/phases/114-map3d-clustering-o-n/114-PATTERNS.md（死组件删除段——删除雷区表格最高优先级警告）
    - .planning/phases/114-map3d-clustering-o-n/114-RESEARCH.md（Pitfall 7：连带边界）
  </read_first>
  <action>
    仅删除两个文件：components/BuildingMarkers.tsx 与 components/CityMarkers.tsx（全库 grep 已确认零外部消费方，二者是 @uiw/react-baidu-map 仅有的两处 import）。

    删除雷区（PATTERNS 已警告为最高优先级）——components/ 下存在平行的第二套同名基础设施，以下文件一个都不许删、不许改：components/utils.ts（存活消费方 BuildingModel3D/AddressInput）、components/constants.ts（存活消费方 BuildingModel3D）、components/types.ts（CityGroup 另一定义）、components/__tests__/utils.test.ts（测的是 components/utils.ts，照常跑）、src/types/global.d.ts（:57 有 import type {} from HubeiMapGL，不得误动）。components/utils.ts 里 getBuildingMarkerColors / generateClusterIconSVG 等导出删除死组件后沦为"仅测试引用"（knip 可见但 knip 非七 gate）——不顺带清理（属 Phase 120 死代码批次）。父级 utils.ts / constants.ts / types.ts / cluster.ts 全部不动。

    删除后验证两条 grep 断言 + 目录测试（见 verify）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && test $(grep -r "BuildingMarkers\|CityMarkers" src/ --include="*.ts" --include="*.tsx" -l | wc -l) -eq 0 && test $(grep -rl "@uiw/react-baidu-map" src/ | wc -l) -eq 0 && test ! -f src/pages/operations/building-spaces-3d/components/BuildingMarkers.tsx && test ! -f src/pages/operations/building-spaces-3d/components/CityMarkers.tsx && test -f src/pages/operations/building-spaces-3d/components/utils.ts && test -f src/pages/operations/building-spaces-3d/components/constants.ts && npx vitest run src/pages/operations/building-spaces-3d && npm run type-check && npm run lint</automated>
  </verify>
  <done>两死组件文件不存在；grep "BuildingMarkers|CityMarkers" 在 src/ 全库零命中；grep "@uiw/react-baidu-map" 在 src/ 零命中（Phase 120 DEAD-02 解锁）；components/utils.ts、components/constants.ts、components/types.ts、components/__tests__/utils.test.ts、src/types/global.d.ts 全部原样存在；目录测试 + type-check + lint 全绿</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cleanup spy 测试——锁定 MAP3D-05 监听器生命周期</name>
  <files>xingran-react-frontend/src/pages/operations/building-spaces-3d/__tests__/hubei-map-cleanup.test.tsx</files>
  <read_first>
    - xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMap.tsx（Plan 02 改造后——init effect 的 handleZoomEnd 提升形态与 cleanup、31 行本地 BAIDU_MAP_AK 常量、App.useApp 依赖）
    - xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMapGL.tsx（同上 + handleTiltEnd + ../constants 的 BAIDU_MAP_AK 导入来源差异）
    - xingran-react-frontend/src/pages/operations/building-spaces-3d/components/__tests__/BaiduMapScript.test.ts（mock 风格范本：操纵 window.BMap/BMapGL 全局 + vi.fn）
    - xingran-react-frontend/src/pages/operations/building-spaces-3d/__tests__/index.render.test.tsx（vi.mock 模块风格 + renderWithProviders 用法 + 动态 import 模式）
    - xingran-react-frontend/src/types/baidu-map.ts（getBMap/getBMapGL 导出与 BMapNamespace/BMapGLNamespace 形状——fake 构造器需覆盖的成员）
  </read_first>
  <behavior>
    - Test: HubeiMapGL 渲染 → fake map 的 addEventListener 以 "zoomend" 与 "tiltend" 各被调用一次 → unmount → removeEventListener 以 "zoomend" 与 "tiltend" 被调用，且第二参数与 addEventListener 时捕获的 handler 引用全等（同一函数引用）
    - Test: HubeiMap 渲染 → addEventListener("zoomend", ...) 一次 → unmount → removeEventListener("zoomend", 同引用) 被调用
    - Test: 未挂载完成的组件（AK 为空字符串分支）不挂任何监听、unmount 不抛错（守卫路径冒烟）
  </behavior>
  <action>
    新建 __tests__/hubei-map-cleanup.test.tsx。关键技术点（缺一测试必炸）：

    (1) AK 常量在模块加载时求值——HubeiMap.tsx:31 本地 `const BAIDU_MAP_AK = import.meta.env.VITE_BAIDU_MAP_AK || ""`，HubeiMapGL 经 ../constants 导出读取。必须用 vi.stubEnv("VITE_BAIDU_MAP_AK", "test-ak") 在动态 import 两组件之前设置，并用 `beforeAll(async () => { 组件 = (await import("../components/HubeiMapGL")).default })` 动态导入模式（参照 index.render.test.tsx:42-45 的既有写法）；测守卫路径的用例反过来用 vi.stubEnv("VITE_BAIDU_MAP_AK", "") 后 vi.resetModules() 再动态导入。测试结束 vi.unstubAllEnvs()。

    (2) mock "../components/BaiduMapScript"：loadBaiduMapScript 与 loadBaiduMapGLScript 均为 vi.fn(() => Promise.resolve())。

    (3) mock "@/types/baidu-map"：getBMapGL 返回 fake namespace（Map: vi.fn(() => fakeMap)、Point: vi.fn((lng, lat) => ({ lng, lat }))、Pixel: vi.fn((x, y) => ({ x, y }))、InfoWindow: vi.fn(() => ({}))、ZoomControl/ScaleControl/NavigationControl: vi.fn()、Boundary: vi.fn(() => ({ get: (_q, cb) => cb({ boundaries: [] }) }))、Polygon: vi.fn(() => ({}))、Size: vi.fn((w, h) => ({ width: w, height: h }))）；getBMap 同构（NavigationControl/ScaleControl/Icon/Marker/InfoWindow/Polygon/Point/Pixel/Size，无 ZoomControl）。fakeMap 是一个手写对象：addEventListener: vi.fn()、removeEventListener: vi.fn()、getZoom: vi.fn(() => 10)、getTilt: vi.fn(() => 0)、centerAndZoom/setMinZoom/setMaxZoom/enableScrollWheelZoom/enableDoubleClickZoom/addControl/setMapStyleV2/addOverlay/removeOverlay: vi.fn()。注意 addHubeiMask 是 async fire-and-forget，boundary 空结果会走 addFallbackMask 分支继续调 Point/Polygon/addOverlay——fake 全部 vi.fn() 兜住即可。

    (4) 渲染：renderWithProviders 包 antd App（组件用 App.useApp()——若 renderWithProviders 未内置 App context，在测试里显式包 <App>）；buildings 传 []。用 @testing-library/react 的 await waitFor(() => expect(fakeMap.addEventListener).toHaveBeenCalled()) 等异步 init 完成。

    (5) 引用一致性断言核心：unmount 前从 addEventListener.mock.calls 捕获 handler（const [zoomEvt, zoomHandler] = fakeMap.addEventListener.mock.calls.find(([e]) => e === "zoomend")）；unmount 后断言 fakeMap.removeEventListener.mock.calls 中存在 [zoomEvt, zoomHandler] 同引用条目（直接 find + toBe(strict equal by reference) 或 expect(calls.some(([e, h]) => e === zoomEvt && h === zoomHandler)).toBe(true)）。该断言在 Plan 02 改造前的内联箭头形态下必红、改造后必绿——这就是 MAP3D-05 的回归守护。

    (6) 组件级 mock 边界：不要 mock 整个 HubeiMap/HubeiMapGL（本任务测的就是它们本体）；聚类 effect 因 mapLoaded 后 buildings 为空数组自然走零聚类路径，无需额外处理。渲染 effect 里 getBMap/getBMapGL 返回 fake namespace 已兜住所有构造器调用。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npx vitest run src/pages/operations/building-spaces-3d/__tests__/hubei-map-cleanup.test.tsx && npx vitest run src/pages/operations/building-spaces-3d</automated>
  </verify>
  <done>cleanup spy 测试 3 用例全绿：GL 版 zoomend+tiltend 同引用移除、BMap 版 zoomend 同引用移除、AK 缺失守卫路径不挂监听不抛错；目录级回归全绿</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: MAP3D-02 人工性能验证——n=1000 缩放/倾斜无秒级卡死 + 聚类视觉一致</name>
  <files>（无文件改动——验证任务）</files>
  <read_first>
    - .planning/phases/114-map3d-clustering-o-n/114-VALIDATION.md（Manual-Only Verifications 表——本任务的操作指引来源）
    - .planning/phases/114-map3d-clustering-o-n/114-02-SUMMARY.md（Plan 02 改造内容回顾）
  </read_first>
  <action>检查点任务：无文件改动。Plan 01/02/03 Task 1-2 的全部自动化改动完成后，向用户呈报 what-built 与 how-to-verify 步骤（浏览器人工性能验证，详见 VALIDATION.md Manual-Only 表），暂停等待用户回复 resume-signal；用户回复 approved 即通过，否则按反馈返工。AK 缺失时按 RESEARCH A4 降级：以 Plan 01 性能冒烟单测 + code review 替代人工验证并在 SUMMARY 记录降级。</action>
  <what-built>
    Phase 114 全部代码改动已完成：聚类从 O(n²) 双循环（每次 zoomend 最多 ~200 万次地图 API 调用）改为单遍 pointToOverlayPixel 预计算 + 40px 网格分桶纯函数（近 O(n)），输出与旧实现逐位一致（一致性单测已锁定）；两组件渲染体记忆化 + 事件监听 cleanup；死组件已删除。
  </what-built>
  <how-to-verify>
    1. 启动前端开发服务器：cd xingran-react-frontend && npm run dev（需要 .env.development 中 VITE_BAIDU_MAP_AK 已配置——项目日常配置，通常已就绪）
    2. 登录系统，导航到楼宇空间 3D 页面（operations/building-spaces-3d）
    3. 确保楼宇数据达千级（若生产数据不足，可在楼宇管理中批量导入测试楼宇，或接受现有数据量验证）
    4. 打开 DevTools Performance 面板录制，连续执行：滚轮缩放（zoom 8→10→8 往返数次）→ WebGL 版切换 3D 倾斜（tilt 拖拽）→ 普通版/WebGL 版 Switch 来回切换
    5. 检查录制结果：主线程无 >1s 长任务（改造前每次 zoomend 有秒级卡死）；交互跟手无明显掉帧段
    6. 聚类视觉一致性：缩放到楼宇重叠区域，确认聚类圆圈数量/位置/数字与改造前直觉一致（成员顺序、tooltip 前排楼宇、点击侧栏列表顺序无肉眼可见漂移）
    7. 控制台无新增报错/警告（特别留意 unmount 相关——快速切换地图版本开关时）
  </how-to-verify>
  <resume-signal>Type "approved" 或描述发现的问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| （无新增） | 删除死代码与新增测试不触及任何网络/输入/认证边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-01 | Tampering（既有低危） | SVG data-URI 模板内插楼宇名（HubeiMap.tsx:401 等） | accept | 33 项 findings 之外的既有行为，原样保留不处理（Scope Constrainment），归后续 quick task |
| T-114-SC | Tampering | npm 包安装 | mitigate | 本 plan 零新增依赖（不执行 npm install/uninstall——@uiw/react-baidu-map 的 package.json 卸载属 Phase 120 DEAD-02），无供应链面 |

本 phase 纯前端性能重构，无新增攻击面（RESEARCH.md Security Domain：无认证/会话/访问控制/输入验证/密码学改动）。
</threat_model>

<verification>
- grep -r "BuildingMarkers\|CityMarkers" src/ 零命中；grep -r "@uiw/react-baidu-map" src/ 零命中（MAP3D-06）
- npx vitest run 全量绿（含新增 cleanup 测试；D-04 零回归口径）
- npm run lint && npm run type-check && npm run build 全绿（D-02 七 gate 前端项不倒退；D-03 entry gzip 不推高）
- MAP3D-02 人工验证完成（checkpoint）；AK 缺失时按 RESEARCH A4 降级：以 Plan 01 的 500ms 性能冒烟单测 + code review 记录替代，不阻塞代码交付
</verification>

<success_criteria>
- MAP3D-06：死组件删除、全库零引用、@uiw/react-baidu-map 零 import（Phase 120 DEAD-02 解锁）
- MAP3D-05：cleanup 行为被自动化测试锁定（同引用 removeEventListener）
- MAP3D-02：人工验证通过——n=1000 缩放/倾斜切换无秒级长任务、聚类视觉与改造前一致
- 全部前端 gate 绿，phase 可进入 /gsd:verify-work
</success_criteria>

<output>
Create `.planning/phases/114-map3d-clustering-o-n/114-03-SUMMARY.md` when done
</output>
