---
phase: 114-map3d-clustering-o-n
plan: 02
type: execute
wave: 2
depends_on:
  - 114-01
files_modified:
  - xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMapGL.tsx
  - xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMap.tsx
autonomous: true
requirements:
  - MAP3D-01
  - MAP3D-02
  - MAP3D-04
  - MAP3D-05

must_haves:
  truths:
    - "两组件聚类 effect 内层循环无地图 API 调用——pointToOverlayPixel 每组件恰 1 处（单遍预计算循环内），n=1000 时地图 API 调用从 ~200 万次降到 n 次（MAP3D-01/02）"
    - "HubeiMap 与 HubeiMapGL 消费同一 clusterBuildings 共享函数，两组件本地 ClusterGroup interface 副本已删除（MAP3D-03 落地）"
    - "HubeiMap 渲染体 5 道全量 filter（664/667/672/676/705）收敛为单个 useMemo（deps 严格 [buildings]），buildings 未变时 hover 不再重复全量过滤（MAP3D-04，D-05 info-points:603 范本风格）"
    - "zoomend/tiltend handler 提升为 effect 作用域具名函数，cleanup 以同一函数引用 removeEventListener（MAP3D-05）"
    - "聚类视觉行为逐位不变——坐标系统混用、zoom === 10 语义、level 全 2 映射等既有行为原样保留（纯重构红线）"
  artifacts:
    - path: "xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMapGL.tsx"
      provides: "单遍预计算 + clusterBuildings 消费 + 具名 handler cleanup"
      contains: "clusterBuildings"
    - path: "xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMap.tsx"
      provides: "同上 + useMemo { level1, level2, withCoords } + 常量/过滤单点消费"
      contains: "useMemo"
  key_links:
    - from: "HubeiMapGL.tsx / HubeiMap.tsx"
      to: "../cluster"
      via: "import { clusterBuildings, type ClusterGroup } 消费共享纯函数"
      pattern: "from \"../cluster\""
    - from: "HubeiMap.tsx"
      to: "../constants"
      via: "import CLUSTER_PIXEL_THRESHOLD（删除内联 40 副本）"
      pattern: "CLUSTER_PIXEL_THRESHOLD.*from \"../constants\""
    - from: "两组件 init effect cleanup"
      to: "map.removeEventListener"
      via: "同一 handler 引用移除，if (map) 判空"
      pattern: "removeEventListener"
---

<objective>
改造 HubeiMapGL 与 HubeiMap 两组件：聚类双循环替换为"单遍 pointToOverlayPixel 预计算 Map + clusterBuildings 纯函数调用"，事件监听补 effect cleanup，HubeiMap 渲染体 5 道 filter 收敛 useMemo。消除 O(n²) 地图 API 调用（MAP3D-01/02），落地共享函数消费（MAP3D-03 完成态），渲染体记忆化（MAP3D-04），监听器生命周期修复（MAP3D-05）。

Purpose: H-2 热点落地——n=1000 楼宇缩放/倾斜切换不再秒级卡死；同时以纯重构口径保证聚类视觉结果与改造前逐位一致（D-04 零回归）。
Output: 两组件改造完成，目录级测试全绿 + type-check + lint 通过，grep 断言（pointToOverlayPixel ≤1/组件、processedBuildings 归零）成立。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-map3d-clustering-o-n/114-RESEARCH.md
@.planning/phases/114-map3d-clustering-o-n/114-PATTERNS.md

# 改造对象（先读现状再动手）
@xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMapGL.tsx
@xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMap.tsx

# Plan 01 产物（消费契约）
@xingran-react-frontend/src/pages/operations/building-spaces-3d/cluster.ts

# 复用资产
@xingran-react-frontend/src/pages/operations/building-spaces-3d/utils.ts
@xingran-react-frontend/src/pages/operations/building-spaces-3d/constants.ts
@xingran-react-frontend/src/types/baidu-map.ts

<interfaces>
<!-- Plan 01 产物：cluster.ts 已导出（executor 直接 import，勿改签名） -->

```typescript
// building-spaces-3d/cluster.ts
export interface ClusterGroup {
  buildings: BuildingItem[];
  centerPixel: { x: number; y: number };
  clusterLng: number;
  clusterLat: number;
}
export type ClusterCenterProjector = (
  pixel: { x: number; y: number }
) => { lng: number; lat: number } | undefined;
export function clusterBuildings(
  buildings: BuildingItem[],
  pixels: ReadonlyMap<string, { x: number; y: number }>,
  threshold: number,
  toCenterLngLat?: ClusterCenterProjector
): ClusterGroup[];
```

<!-- 既有 API：事件监听类型（baidu-map.ts:58-62） -->
```typescript
export interface BMapEventEmitter {
  addEventListener(event: string, handler: (e: BMapEvent) => void): BMapEventListener;
  removeEventListener(event: string, handler: (e: BMapEvent) => void): void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HubeiMapGL.tsx——预计算替换双循环 + 监听器提升具名 + cleanup</name>
  <files>xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMapGL.tsx</files>
  <read_first>
    - xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMapGL.tsx（全文——1-59 imports 与本地 ClusterGroup:54-59；90-164 init effect 与内联监听:150-151；263-330 渲染 effect 与聚类双循环:275-323；332-415 renderClusterMarker 不动区）
    - xingran-react-frontend/src/pages/operations/building-spaces-3d/cluster.ts（Plan 01 产物——消费契约）
    - xingran-react-frontend/src/types/baidu-map.ts（BMapEventEmitter:58-62 removeEventListener 签名）
    - .planning/phases/114-map3d-clustering-o-n/114-PATTERNS.md（HubeiMapGL 段 + Shared Patterns 4/5）
  </read_first>
  <action>
    三处改动，其余一概不动：

    (1) 类型收敛（MAP3D-03 完成态）：删除本地 interface ClusterGroup（54-59 行）→ 顶部新增 `import { clusterBuildings, type ClusterGroup } from "../cluster";`（verbatimModuleSyntax 下类型部分必须 type 修饰）。组件内所有 ClusterGroup 引用（useState selectedCluster、renderClusterMarker 签名等）自动切换到导入类型，不需要其他改动。

    (2) init effect（90-164）：把 150-151 的两条内联箭头监听替换为 effect 作用域具名函数 + cleanup。做法：effect 体内、initMap 定义之前声明 `let map: BMapMapGL | null = null;`（initMap 内 `const map = new BMapGL.Map(...)` 改为对 effect 作用域变量赋值，mapInstanceRef.current 赋值保留）；声明 `const handleZoomEnd = () => { if (map) setCurrentZoom(map.getZoom()); };` 与 `const handleTiltEnd = () => { if (map) setCurrentTilt(map.getTilt()); };`；initMap 内改为 `map.addEventListener("zoomend", handleZoomEnd); map.addEventListener("tiltend", handleTiltEnd);`；effect 返回 cleanup `() => { if (map) { map.removeEventListener("zoomend", handleZoomEnd); map.removeEventListener("tiltend", handleTiltEnd); } }`（内联箭头永远无法被 removeEventListener 移除——引用不等，这就是本改动的原因）。保留 103 行 `if (!mapRef.current) return;` 竞态守卫（unmount 时 React 已将 ref 置 null，防"卸载后才建图挂监听"，勿删）。tiltend 是 GL 独有事件（2D 无 tilt），两条监听都要 cleanup。

    (3) 渲染 effect（263-330）：275 行 filterBuildingsByZoom 调用与 276 行 buildingsWithCoords 过滤保持不变；278-323 的 processedBuildings Set + forEach 双循环整段删除，替换为：单遍预计算循环——`const pixels = new Map<string, { x: number; y: number }>();` 对 buildingsWithCoords 逐个 `pixels.set(b.id, map.pointToOverlayPixel(new BMapGL.Point(b.longitude!, b.latitude!)));`（每组件唯一 pointToOverlayPixel 调用点，n 次 API 调用替代 n² 次）；随后 `const clusterGroups = clusterBuildings(buildingsWithCoords, pixels, CLUSTER_PIXEL_THRESHOLD, (p) => map.pixelToPoint?.(new BMapGL.Pixel(p.x, p.y)));`（保留 ?. 防御与回退链——回调返回 undefined 时 cluster.ts 内部回退锚点经纬度，与旧代码 `|| { lng, lat }` 逐位等价）。后续 clusterGroups.forEach(renderClusterMarker) 不变。

    连带 import 清理：pixelDistance 与 averagePixelPosition 若在组件内再无其他调用点（改造后应无），从 "../utils" 的具名导入列表中移除这两个符号；filterBuildingsByZoom、toBase64 等其余导入保留。effect deps `[mapLoaded, buildings, currentZoom]` 原样（330 行附近），不得增删。

    纯重构红线（违反即返工）：不改 pixelToPoint 为 overlayPixelToPoint；不改 zoom 过滤语义；不动 renderClusterMarker（332-415）；不动 index.tsx 的 level 映射；不新增 exhaustive-deps disable 注释。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && test $(grep -c "pointToOverlayPixel" src/pages/operations/building-spaces-3d/components/HubeiMapGL.tsx) -le 1 && test $(grep -c "processedBuildings" src/pages/operations/building-spaces-3d/components/HubeiMapGL.tsx) -eq 0 && grep -q 'clusterBuildings' src/pages/operations/building-spaces-3d/components/HubeiMapGL.tsx && grep -q 'removeEventListener("tiltend", handleTiltEnd)' src/pages/operations/building-spaces-3d/components/HubeiMapGL.tsx && npx vitest run src/pages/operations/building-spaces-3d && npm run type-check</automated>
  </verify>
  <done>pointToOverlayPixel 在该文件恰 1 处（预计算循环内）；processedBuildings 零命中（双循环已删）；clusterBuildings 已消费且本地 ClusterGroup 定义删除；handleZoomEnd/handleTiltEnd 以同一引用 removeEventListener（zoomend+tiltend 两条）；`if (!mapRef.current) return` 守卫保留；目录测试全绿 + type-check 通过</done>
</task>

<task type="auto">
  <name>Task 2: HubeiMap.tsx——useMemo 收敛 5 filter + 预计算替换双循环 + cleanup + 单点收敛</name>
  <files>xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMap.tsx</files>
  <read_first>
    - xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMap.tsx（全文——23-28 本地 ClusterGroup；50-56 toBase64 本地副本（不动）；162-238 init effect 与 handleZoomEnd:219-223；241-321 渲染 effect：254-257 内联三目、260 内联阈值 40、267-314 双循环、284-286 内联 sqrt、296-297 内联 reduce；645-720 统计面板 5 道 filter:664/667/672/676/705）
    - xingran-react-frontend/src/pages/operations/building-spaces-3d/cluster.ts（消费契约）
    - xingran-react-frontend/src/pages/operations/building-spaces-3d/constants.ts:53（CLUSTER_PIXEL_THRESHOLD——收敛目标）
    - xingran-react-frontend/src/pages/operations/info-points/index.tsx 600-612 行（D-05 复用范本：useMemo + Map 构建 + deps 数组风格）
    - .planning/phases/114-map3d-clustering-o-n/114-PATTERNS.md（HubeiMap 段 + Shared Patterns 2/3/5）
  </read_first>
  <action>
    五处改动，其余一概不动：

    (1) 类型收敛：删除本地 interface ClusterGroup（23-28 行）→ `import { clusterBuildings, type ClusterGroup } from "../cluster";`。

    (2) 渲染体 useMemo（MAP3D-04，D-05 范本风格）：react 导入补 useMemo（当前仅 useRef/useState/useEffect）；组件体内（currentZoom state 之后）新增 `const { level1, level2, withCoords } = useMemo(() => ({ level1: buildings.filter((b) => b.level === 1), level2: buildings.filter((b) => b.level === 2), withCoords: buildings.filter((b) => b.longitude && b.latitude) }), [buildings]);`——deps 严格 [buildings]（index.tsx 仅 mount 时 setBuildings 一次，引用稳定）。统计面板 5 处改读记忆化数组：664 行 Badge count 改 level1.length；667 行改 level2.length；672 行改 withCoords.length；676 行 backgroundColor 三目改 withCoords.length > 0；705 行条件改 withCoords.length === 0。渲染体不再出现任何 buildings.filter(...)。

    (3) 聚类 effect 收敛（MAP3D-01 BMap 半 + 单点消费）：254-257 的内联三目改 `const filteredBuildings = currentZoom === 10 ? buildings : level1;`——只复用层级过滤，zoom === 10 语义原样保留（禁改 >= 10，Pitfall 6）。有坐标过滤必须保留在 effect 内：`const buildingsWithCoords = filteredBuildings.filter((b) => b.longitude && b.latitude);`——不得改用 useMemo 的 withCoords 数组替代（旧代码过滤顺序是"先层级过滤再有坐标过滤"，useMemo 的 withCoords 未做层级过滤，换用会改变聚类输入集合，行为变更）。删除 260 行内联 `const CLUSTER_PIXEL_THRESHOLD = 40;` → 顶部 `import { CLUSTER_PIXEL_THRESHOLD } from "../constants";`。263-314 的双循环整段删除，替换为与 Task 1 同型的单遍预计算 + clusterBuildings 调用，namespace 用 BMap：`pixels.set(b.id, map.pointToOverlayPixel(new BMap.Point(b.longitude!, b.latitude!)));` 与 `(p) => map.pixelToPoint?.(new BMap.Pixel(p.x, p.y))`（保留 ?. 防御）。这同时消掉 284-286 内联 sqrt 与 296-297 内联 reduce（聚类内部已统一走 utils 的 pixelDistance/averagePixelPosition）。effect deps `[mapLoaded, buildings, currentZoom]` 原样。

    (4) init effect cleanup（MAP3D-05 BMap 半）：把 219-223 定义在 async initMap 内部的 handleZoomEnd 提升到 effect 作用域——effect 体内 initMap 之前声明 `let map: BMapMap | null = null;`（initMap 内改为对该变量赋值，mapInstanceRefLocal.current 赋值保留），声明 `const handleZoomEnd = () => { if (map) setCurrentZoom(map.getZoom()); };`，initMap 内 `map.addEventListener("zoomend", handleZoomEnd);`，effect 返回 cleanup `() => { if (map) map.removeEventListener("zoomend", handleZoomEnd); }`（2D 无 tilt，仅 zoomend 一条）。保留 175 行 `if (!mapRef.current) return;` 竞态守卫。

    (5) 纯重构红线（违反即返工）：不动 50-56 的 toBase64 本地副本（不在 MAP3D-01~06 范围，PATTERNS 标注为可选收敛项、本 phase 明确不做）；不动 MAP_CONFIG/HUBEI_BOUNDARY/getOfficialHubeiBoundary/addFallbackMask/renderClusterMarker；不改 index.tsx 的 level 全 2 映射；距离/均值/阈值不再出现任何内联表达式。

    完成后构建校验以 type-check + lint + vitest 为准（本 plan 纯前端，不涉 go build）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && test $(grep -c "pointToOverlayPixel" src/pages/operations/building-spaces-3d/components/HubeiMap.tsx) -le 1 && test $(grep -c "processedBuildings" src/pages/operations/building-spaces-3d/components/HubeiMap.tsx) -eq 0 && test $(grep -c "buildings.filter" src/pages/operations/building-spaces-3d/components/HubeiMap.tsx) -eq 3 && ! grep -q "const CLUSTER_PIXEL_THRESHOLD = 40" src/pages/operations/building-spaces-3d/components/HubeiMap.tsx && grep -q 'clusterBuildings' src/pages/operations/building-spaces-3d/components/HubeiMap.tsx && grep -q 'removeEventListener("zoomend", handleZoomEnd)' src/pages/operations/building-spaces-3d/components/HubeiMap.tsx && npx vitest run src/pages/operations/building-spaces-3d && npm run type-check && npm run lint</automated>
  </verify>
  <done>pointToOverlayPixel 恰 1 处；processedBuildings 零命中；渲染体 buildings.filter 恰 3 处（全部在 useMemo 内）且统计面板 5 处读 .length；内联阈值 40 副本删除、改消费 ../constants；handleZoomEnd 提升 effect 作用域并以同引用 removeEventListener；175 守卫保留；目录测试全绿 + type-check + lint 通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| （无新增） | 组件改造不触及任何网络/输入/认证边界；数据仍来自既有已鉴权 buildingApi.list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-01 | Tampering（既有低危） | SVG data-URI 模板内插楼宇名（HubeiMap.tsx:401 等） | accept | 33 项 findings 之外的既有行为，重构时原样保留不处理（Scope Constrainment），归后续 quick task |
| T-114-SC | Tampering | npm 包安装 | mitigate | 本 plan 零新增依赖，无供应链面 |

本 phase 纯前端性能重构，无新增攻击面（RESEARCH.md Security Domain：无认证/会话/访问控制/输入验证/密码学改动）。
</threat_model>

<verification>
- grep pointToOverlayPixel 在两组件各 ≤1 处（每组件唯一预计算调用点，MAP3D-01 验收口径）
- grep processedBuildings 在两组件零命中（O(n²) 双循环已删）
- npx vitest run src/pages/operations/building-spaces-3d 全绿（含 Plan 01 一致性测试——聚类输出逐位一致，D-04）
- npm run type-check && npm run lint 通过（D-02 七 gate 前端项不倒退）
- HubeiMap 渲染体 useMemo deps [buildings]，无新增 exhaustive-deps disable 注释（Pitfall 8）
</verification>

<success_criteria>
- MAP3D-01：内层循环无 new BMap(Point)/pointToOverlayPixel——每组件地图投影 API 恰 1 处
- MAP3D-02：聚类走 40px 网格分桶共享函数（算法正确性由 Plan 01 测试守护；n=1000 长任务清零由 Plan 03 人工验证收口）
- MAP3D-03：两组件均消费 cluster.ts，本地 ClusterGroup 副本删除
- MAP3D-04：5 道 filter 收敛为 useMemo 一次计算，统计面板读 .length
- MAP3D-05：两组件 zoomend/tiltend cleanup 生效（同引用移除）
- 聚类视觉结果与改造前一致（一致性测试 + 既有渲染层不动双重保障）
</success_criteria>

<output>
Create `.planning/phases/114-map3d-clustering-o-n/114-02-SUMMARY.md` when done
</output>
