---
phase: 114-map3d-clustering-o-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - xingran-react-frontend/src/pages/operations/building-spaces-3d/cluster.ts
  - xingran-react-frontend/src/pages/operations/building-spaces-3d/__tests__/cluster.test.ts
autonomous: true
requirements:
  - MAP3D-02
  - MAP3D-03

must_haves:
  truths:
    - "clusterBuildings 是 HubeiMap/HubeiMapGL 未来共用的唯一聚类实现——纯数学无 BMap/BMapGL 依赖，Vitest 可直测（MAP3D-03）"
    - "新 40px 网格分桶实现与旧 O(n²) 锚点贪心实现输出逐位一致（簇分组 + 簇内成员顺序 + 中心坐标），由内嵌参考实现对照测试锁定（MAP3D-02 / D-04 聚类一致性口径）"
    - "桶大小=阈值=40、3×3 邻域完备、桶值存原始下标升序扫描、保留 sqrt 距离表达式——四要素全部有测试覆盖"
  artifacts:
    - path: "xingran-react-frontend/src/pages/operations/building-spaces-3d/cluster.ts"
      provides: "clusterBuildings 纯函数 + ClusterGroup/ClusterCenterProjector 类型导出"
      exports: ["clusterBuildings", "ClusterGroup", "ClusterCenterProjector"]
      min_lines: 40
      contains: "ReadonlyMap"
    - path: "xingran-react-frontend/src/pages/operations/building-spaces-3d/__tests__/cluster.test.ts"
      provides: "参考实现对照 + 种子随机 + 边界 + 中心回退 + 性能冒烟测试"
      contains: "legacyCluster"
      min_lines: 100
  key_links:
    - from: "cluster.ts"
      to: "./utils"
      via: "复用既有 pixelDistance / averagePixelPosition，不重写"
      pattern: "import \\{ pixelDistance, averagePixelPosition \\} from \"./utils\""
    - from: "cluster.ts"
      to: "./types"
      via: "type-only import BuildingItem（verbatimModuleSyntax）"
      pattern: "import type \\{ BuildingItem \\} from \"./types\""
---

<objective>
新建共享聚类纯函数 cluster.ts（40px 像素网格分桶 + 锚点贪心，输出与旧 O(n²) 实现逐位一致）及其一致性单元测试。本 plan 是 Phase 114 的 Wave 0 基础：先写测试（RED）、再实现（GREEN），为 Plan 02 两组件改造提供唯一可测的聚类实现。

Purpose: H-2 全库最重 JS 热点的算法核心——把"像素投影"从聚类中剥离后，聚类本体是纯数学，天然可单测，且同时服务 BMap/BMapGL 两套 API（MAP3D-02/03；聚类结果一致性满足 D-04 口径）。
Output: `building-spaces-3d/cluster.ts`（约 50-60 行）+ `__tests__/cluster.test.ts`（参考实现对照 + 边界 + 性能冒烟），全部测试绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-map3d-clustering-o-n/114-RESEARCH.md
@.planning/phases/114-map3d-clustering-o-n/114-PATTERNS.md

# 行为契约来源——旧 O(n²) 算法（GL 版是两份副本中更干净的一份，逐行复刻基准）
@xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMapGL.tsx

# 复用资产与类型契约
@xingran-react-frontend/src/pages/operations/building-spaces-3d/utils.ts
@xingran-react-frontend/src/pages/operations/building-spaces-3d/types.ts
@xingran-react-frontend/src/pages/operations/building-spaces-3d/constants.ts

# 同目录测试风格范本
@xingran-react-frontend/src/pages/operations/building-spaces-3d/__tests__/utils.test.ts

<interfaces>
<!-- 本 plan 创建的契约（Plan 02 两组件将消费）——签名必须与下述完全一致 -->

新建 cluster.ts 导出（类型定义参考 utils.ts 风格，中文 JSDoc）：
```typescript
export interface ClusterGroup {
  buildings: BuildingItem[];
  centerPixel: { x: number; y: number };
  clusterLng: number;
  clusterLat: number;
}

export type ClusterCenterProjector = (
  pixel: { x: number; y: number }
) => { lng: number; lat: number } | undefined;

export function clusterBuildings(
  buildings: BuildingItem[],
  pixels: ReadonlyMap<string, { x: number; y: number }>,
  threshold: number,
  toCenterLngLat?: ClusterCenterProjector
): ClusterGroup[];
```

行为契约（与旧 O(n²) 实现逐位一致，缺一不可）：
- 锚点贪心：按数组顺序取首个未处理楼宇为锚，吸收全部距锚 < threshold（严格小于）的未处理楼宇
- 簇内成员顺序 = 原数组顺序；簇的输出顺序 = 锚点出现顺序
- 距离比较保留 sqrt 表达式 pixelDistance(a, b) < threshold（禁平方优化）
- 中心 = 成员像素算术平均（averagePixelPosition）→ toCenterLngLat 回调；回调缺失/返回 undefined/返回值 lng 为 falsy 时回退锚点（cluster.buildings[0]）的 longitude/latitude（对齐旧代码 `centerPoint.lng || building.longitude!` 两级回退链）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建聚类一致性测试 __tests__/cluster.test.ts（RED）</name>
  <files>xingran-react-frontend/src/pages/operations/building-spaces-3d/__tests__/cluster.test.ts</files>
  <read_first>
    - xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMapGL.tsx（275-323 行——旧 O(n²) 算法逐行复刻基准：forEach 双循环、pixelDistance 严格小于、averagePixelPosition 均值、pixelToPoint?.() || 锚点回退、centerPoint.lng || falsy 兜底）
    - xingran-react-frontend/src/pages/operations/building-spaces-3d/__tests__/utils.test.ts（同目录测试组织：vitest 具名导入、中文 describe 嵌套、`as BuildingItem` 局部断言构造 fixture）
    - xingran-react-frontend/src/pages/operations/building-spaces-3d/types.ts（BuildingItem 必填字段：id/name/code/cityCode/cityName/address/level/status）
    - xingran-react-frontend/src/pages/operations/building-spaces-3d/utils.ts（pixelDistance:70 / averagePixelPosition:82——参考实现必须复用同一语义）
  </read_first>
  <behavior>
    测试文件 import { clusterBuildings, type ClusterGroup } from "../cluster"（模块尚不存在——本任务预期 RED）。
    文件内嵌参考实现 legacyCluster(buildings, pixels, threshold, toCenterLngLat?)：逐行复刻 HubeiMapGL.tsx:282-323 的算法，唯一差异是像素来源从 map.pointToOverlayPixel(new Point(lng, lat)) 换成 pixels.get(building.id)（组件预计算循环对同一楼宇用同一 API 同一参数，结果逐位相同）。复用 import { pixelDistance, averagePixelPosition } from "../utils"（对齐旧 GL 代码的消费方式）。

    测试用例（vitest，globals 可用但用具名导入保持风格一致）：
    - Test: 种子随机一致性——实现 mulberry32 种子 PRNG（标准 32 位版本，约 8 行），mkBuildings(n, rng) 生成 BuildingItem[]（id 为 "b${i}"，longitude=100+i、latitude=30+i*0.1 等各点互异非零值，供回退断言分辨），mkRandomPixels(buildings, rng) 生成 Map<string,{x,y}>（稀疏团簇：取 5-8 个随机簇心、成员在簇心 ±35px 内扰动；另混 30% 全域随机散点——制造跨桶边界与重叠混合）。种子 (buildings 500 点, rng=42) / (pixels, rng=7) 以及至少另两组种子（如 2024/99、314/27），逐组 expect(clusterBuildings(buildings, pixels, 40, undefined)).toEqual(legacyCluster(buildings, pixels, 40))
    - Test: 距离恰为 40 不聚——两点像素 (0,0) 与 (40,0)（distance=40，严格小于不成立）→ 2 簇各 1 成员
    - Test: 距离 39 聚——(0,0) 与 (39,0) → 1 簇 2 成员
    - Test: 斜向边界——(0,0) 与 (28,28)（sqrt(1568)≈39.60）同簇；(0,0) 与 (29,29)（sqrt(1682)≈41.01）异簇
    - Test: 负像素坐标——两点 (-45,-10) 与 (-25,-5)（相距约 20.6）→ 聚为一簇；且整体输出与 legacyCluster 一致（验证 Math.floor 对负数分桶正确）
    - Test: 空数组 → []；单点 → 1 簇 1 成员、centerPixel 即该点像素
    - Test: 1000 点全部落在同一 40px 桶（像素全在 (0..39, 0..39)）→ 1 簇 1000 成员，无栈溢出，与 legacyCluster toEqual 一致
    - Test: 中心回退——不传 toCenterLngLat → clusterLng/clusterLat 等于锚点（成员经纬度值可分辨）；传返回 undefined 的回调 → 同样回退锚点
    - Test: falsy 半回退——回调返回 { lng: 0, lat: 5 } → clusterLng 回退锚点经度（0 为 falsy 触发兜底）但 clusterLat === 5（非零 truthy 保留）——锁定旧代码 `centerPoint.lng ||` / `centerPoint.lat ||` 的字段级兜底语义
    - Test: 性能冒烟（宽松防 flake）——1000 点合成夹具（上述团簇+散点混合），performance.now() 计时 clusterBuildings 单次调用 < 500ms
  </behavior>
  <action>
    新建 xingran-react-frontend/src/pages/operations/building-spaces-3d/__tests__/cluster.test.ts。文件头中文 JSDoc（注明 Phase 114 MAP3D-03——聚类一致性守护 + 旧 O(n²) 参考实现只活在测试里）。fixture 构造参照 utils.test.ts 的 `as BuildingItem` 局部断言风格。参考实现 legacyCluster 不做任何"改进"——包括保留锚点像素取 pixels.get(building.id)!、保留严格小于、保留回退链两级语义。所有随机用例必须种子固定（mulberry32），禁止 Math.random（CI 不可复现）。deep-equal 断言一律用 toEqual（簇分组 + 成员顺序 + 中心坐标全结构相等，顺序漂移只有 toEqual 能抓住）。不要创建 cluster.ts（那是 Task 2）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npx vitest run src/pages/operations/building-spaces-3d/__tests__/cluster.test.ts 2>&1; echo "exit=$?"</automated>
  </verify>
  <done>测试文件就位且以非零退出码失败（RED 预期——因 ../cluster 模块尚不存在，vitest 报 Failed to resolve import 或同级别失败）；测试结构覆盖 behavior 列出的全部用例；无 Math.random 等非种子随机源</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 cluster.ts 共享聚类纯函数（GREEN）</name>
  <files>xingran-react-frontend/src/pages/operations/building-spaces-3d/cluster.ts</files>
  <read_first>
    - xingran-react-frontend/src/pages/operations/building-spaces-3d/__tests__/cluster.test.ts（Task 1 产物——测试即规格，实现以让其全绿为准）
    - xingran-react-frontend/src/pages/operations/building-spaces-3d/utils.ts（复用 pixelDistance:70 与 averagePixelPosition:82，禁止重写）
    - xingran-react-frontend/src/pages/operations/building-spaces-3d/types.ts（BuildingItem）
    - .planning/phases/114-map3d-clustering-o-n/114-PATTERNS.md（cluster.ts 段——纯函数风格 + import type 纪律）
  </read_first>
  <behavior>
    Task 1 的全部测试由 RED 转绿：`npx vitest run src/pages/operations/building-spaces-3d/__tests__/cluster.test.ts` 退出码 0。特别地：随机种子对照用例证明与旧 O(n²) 实现逐位一致；1000 点同桶用例证明分桶实现无栈溢出且顺序保持；falsy 半回退用例证明回退链两级语义。
  </behavior>
  <action>
    新建 xingran-react-frontend/src/pages/operations/building-spaces-3d/cluster.ts，导出 ClusterGroup / ClusterCenterProjector / clusterBuildings（签名见 context interfaces block，不得偏差）。imports 仅两处：`import type { BuildingItem } from "./types";` 与 `import { pixelDistance, averagePixelPosition } from "./utils";`——禁止 import @/types/baidu-map 或任何 SDK/DOM 依赖（可测性红线；经 utils 的传递引用不算违规）。

    实现三步（约 50 行）：
    ① 单遍分桶：bucketSize = threshold（铁律 bucketSize ≥ threshold，取等号即 40px 桶——此时 |Δx|<40 且 |Δy|<40 蕴含桶索引差 ≤1，3×3 邻域完备）。遍历 buildings 按下标 i：p = pixels.get(buildings[i].id)，p 为 undefined 则跳过（防御路径，JSDoc 声明前置条件为 pixels 覆盖待聚类楼宇）；key = Math.floor(p.x / threshold) + "," + Math.floor(p.y / threshold)（Math.floor 对负坐标同样正确）；Map<string, number[]> 桶值 push 原始下标 i（天然按插入序 = 原数组序）。
    ② 主扫描：processed 用 Set<string>（存楼宇 id）。for i 从 0 到 n-1 顺序扫描：processed.has(当前 id) 则 continue；anchorPixel = pixels.get(id)。收集 3×3 邻域候选：桶索引 bx = Math.floor(anchorPixel.x / threshold)、by = Math.floor(anchorPixel.y / threshold)，双层循环 dx、dy 各取 -1/0/1，把命中桶的下标合并进 neighbors 数组；neighbors.sort((a, b) => a - b) 升序（复刻旧实现成员顺序的关键）；逐个 j 扫描：j 对应 id 已 processed 或 j === i 则跳过；pixelDistance(anchorPixel, pixels.get(id_j)) < threshold（严格小于、保留 utils 的 sqrt 实现，禁平方优化）→ overlappedBuildings.push(buildings[j])、clusterPixels.push(该点像素)、processed.add(id_j)。
    ③ 中心与回退：avgPixel = averagePixelPosition(clusterPixels)；centerPoint = toCenterLngLat?.(avgPixel)；clusterLng = centerPoint?.lng || anchor.longitude!；clusterLat = centerPoint?.lat || anchor.latitude!（两级回退对齐旧代码：回调缺失/返回 undefined → 整体回退锚点；返回值字段为 falsy（如 lng=0）→ 字段级回退）。clusterGroups.push({ buildings: overlappedBuildings, centerPixel: avgPixel, clusterLng, clusterLat })；最后 processed.add(锚点 id)。

    clusterBuildings 的 JSDoc 必须写明：故意保留"pointToOverlayPixel 求均值像素 + pixelToPoint 逆变换转回经纬度"的既有坐标系统混用（调用方语义，RESEARCH Pitfall 1），勿改 overlayPixelToPoint；输出与旧 O(n²) 实现逐位一致的三要素（桶=阈值、3×3 邻域、下标升序）。

    禁止改动 utils.ts / constants.ts / types.ts / 两组件（那是 Plan 02 的事）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npx vitest run src/pages/operations/building-spaces-3d/__tests__/cluster.test.ts && test $(grep -c "baidu-map" src/pages/operations/building-spaces-3d/cluster.ts) -eq 0 && echo "NO_SDK_IMPORT_OK"</automated>
  </verify>
  <done>cluster.test.ts 全部用例绿（一致性 + 边界 + 回退 + 性能冒烟）；cluster.ts 导出 clusterBuildings/ClusterGroup/ClusterCenterProjector 三个符号；grep baidu-map 零命中（纯数学无 SDK 依赖）；utils.ts 等既有文件零改动</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| （无新增） | 本 plan 新增纯数学函数与测试，不触及任何网络/输入/认证边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-01 | Tampering（既有低危） | SVG data-URI 模板内插楼宇名（HubeiMap.tsx:401 等，本 plan 不触及） | accept | 33 项 findings 之外的既有行为，按 Scope Constrainment 原样保留，归后续 quick task |
| T-114-SC | Tampering | npm 包安装 | mitigate | 本 plan 零新增依赖（RESEARCH Package Legitimacy Audit：不适用），无供应链面 |

本 phase 纯前端性能重构，无新增攻击面（RESEARCH.md Security Domain：无认证/会话/访问控制/输入验证/密码学改动）。
</threat_model>

<verification>
- npx vitest run src/pages/operations/building-spaces-3d/__tests__/cluster.test.ts 全绿（一致性 + 边界 + 回退 + 性能冒烟）
- npx vitest run src/pages/operations/building-spaces-3d 目录级回归全绿（既有 9 文件 64 用例 + 新增 cluster.test.ts，D-04 零回归基线）
- grep -c "baidu-map" cluster.ts 为 0
</verification>

<success_criteria>
- clusterBuildings 为唯一聚类实现，签名与 context interfaces block 完全一致，Plan 02 可直接消费
- 参考实现对照测试锁定逐位一致行为（簇分组 / 成员顺序 / 中心坐标）
- 桶=阈值、3×3 邻域、下标升序、sqrt 保留四要素有测试覆盖
- 既有目录测试零回归
</success_criteria>

<output>
Create `.planning/phases/114-map3d-clustering-o-n/114-01-SUMMARY.md` when done
</output>
