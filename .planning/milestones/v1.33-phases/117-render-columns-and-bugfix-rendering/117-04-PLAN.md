---
phase: "117-render-columns-and-bugfix"
plan: "04"
type: tdd
wave: 3
depends_on:
  - "117-01"
files_modified:
  - xingran-react-frontend/src/pages/operations/dedicated-lines/index.tsx
  - xingran-react-frontend/src/pages/operations/floors/components/FloorCardView.tsx
  - xingran-react-frontend/src/pages/network/templates/modals/VariablesModal.tsx
  - xingran-react-frontend/src/components/cad-editor/CADFloorPlanEditor.tsx
autonomous: true
requirements:
  - BUGFIX-01
  - BUGFIX-02
  - BUGFIX-03
must_haves:
  truths:
    - "BUGFIX-01: dedicated-lines:494 monthlyFee=0 渲染 '0' 而非空白；FloorCardView:82 area=0 渲染 '0' 而非空白（改 != null 判断）"
    - "BUGFIX-02: VariablesModal.tsx:31-53 columns 定义去重后恰好渲染 3 列（key/description/defaultValue），非 6 列"
    - "BUGFIX-03: CADFloorPlanEditor.tsx:870-1028 updater stale closure 修复，snap 操作使用最新 prev.snapToGrid/gridSize，从 deps 删除 floorPlanData"
  artifacts:
    - path: "xingran-react-frontend/src/pages/operations/dedicated-lines/index.tsx"
      provides: "BUGFIX-01: monthlyFee != null 判断"
      contains: "monthlyFee != null"
    - path: "xingran-react-frontend/src/pages/operations/floors/components/FloorCardView.tsx"
      provides: "BUGFIX-01: area != null 判断"
      contains: "area != null"
    - path: "xingran-react-frontend/src/pages/network/templates/modals/VariablesModal.tsx"
      provides: "BUGFIX-02: columns 去重为 3 列"
      contains: "useMemo"
    - path: "xingran-react-frontend/src/components/cad-editor/CADFloorPlanEditor.tsx"
      provides: "BUGFIX-03: updater 读 prev.snapToGrid/gridSize，deps 删除 floorPlanData"
      contains: "prev.snapToGrid"
  key_links:
    - from: "dedicated-lines/index.tsx monthlyFee 渲染"
      to: "!= null 判断"
      via: "{line.monthlyFee != null && ...}"
      pattern: "monthlyFee != null"
    - from: "FloorCardView.tsx area 渲染"
      to: "!= null 判断"
      via: "{floor.area != null && ...}"
      pattern: "area != null"
    - from: "VariablesModal.tsx columns"
      to: "去重 3 列 useMemo"
      via: "useMemo + map sorter"
      pattern: "useMemo.*baseColumns"
    - from: "CADFloorPlanEditor.tsx updater"
      to: "prev.snapToGrid / gridSize"
      via: "setFloorPlanData((prev) => { ... snapToGrid: prev.snapToGrid ... })"
      pattern: "prev\\.snapToGrid"
---

<objective>
BUGFIX-01/02/03：3 个正确性修复，全部采用 TDD 红→绿方式——先写失败测试，再改代码使变绿（D-04 锁定）。

BUGFIX-01: dedicated-lines:494 monthlyFee=0 与 FloorCardView:82 area=0 当前值 0 被 `if (value)` 判断为 falsy 导致渲染空白，修复为 `!= null` 判断。
BUGFIX-02: VariablesModal.tsx:31-53 同一 3 列定义重复出现 2 次（共 6 列），去重后恰好 3 列。
BUGFIX-03: CADFloorPlanEditor.tsx:870-1028 updater 内读 floorPlanData.snapToGrid/gridSize（闭包内旧值），应读 prev.snapToGrid/gridSize，并从 deps 删除 floorPlanData。

Purpose: 行为变更修复必须附回归测试，锁定正确行为防止未来退化。
Output: 3 个 bug 修复 + 对应回归测试，既有测试零回归。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/ROADMAP.md Phase 117 段
@.planning/REQUIREMENTS.md BUGFIX-01 / BUGFIX-02 / BUGFIX-03
@xingran-react-frontend/src/pages/operations/dedicated-lines/index.tsx（:494 monthlyFee 渲染）
@xingran-react-frontend/src/pages/operations/floors/components/FloorCardView.tsx（:82 area 渲染）
@xingran-react-frontend/src/pages/network/templates/modals/VariablesModal.tsx（:31-53 columns 重复定义）
@xingran-react-frontend/src/components/cad-editor/CADFloorPlanEditor.tsx（:870-1028 updater stale closure）

<interfaces>
<!-- 核验完毕（2026-09-14）：3 个 bug 的现状 -->

BUGFIX-01 — dedicated-lines/index.tsx :494 现状：
  {line.monthlyFee && (          ← monthlyFee=0 时 falsy，不渲染
    <div><strong>月费：</strong>¥{line.monthlyFee}</div>
  )}
  修复后：
  {line.monthlyFee != null && (
    <div><strong>月费：</strong>¥{line.monthlyFee}</div>
  )}

BUGFIX-01 — FloorCardView.tsx :82 现状：
  {floor.area && (              ← area=0 时 falsy，不渲染
    <div><strong>面积：</strong>{floor.area}m²</div>
  )}
  修复后：
  {floor.area != null && (
    <div><strong>面积：</strong>{floor.area}m²</div>
  )}

BUGFIX-02 — VariablesModal.tsx :31-53 现状（6 列重复）：
  const columns = [
    { title: "变量名", dataIndex: "key", key: "key" },          ← 第 1 次
    { title: "描述", dataIndex: "description", key: "description" },
    { title: "默认值", dataIndex: "defaultValue", key: "defaultValue" },
    { title: "变量名", dataIndex: "key", key: "key", sorter },  ← 第 2 次（重复）
    { title: "描述", dataIndex: "description", key: "description", sorter },
    { title: "默认值", dataIndex: "defaultValue", key: "defaultValue", sorter },
  ];
  修复后（3 列 + useMemo）：
  const columns = useMemo(() => {
    const base = [
      { title: "变量名", dataIndex: "key", key: "key" },
      { title: "描述", dataIndex: "description", key: "description" },
      { title: "默认值", dataIndex: "defaultValue", key: "defaultValue" },
    ];
    return base.map((col) => ({
      ...col,
      sorter: createSorter<VariableRow>(col.dataIndex as keyof VariableRow, "string"),
    }));
  }, []);

BUGFIX-03 — CADFloorPlanEditor.tsx :870-1028 updater stale closure：
  setFloorPlanData((prev) => {
    const snappedX = floorPlanData.snapToGrid   ← 读的是闭包创建时的旧值
      ? snapToGrid(point.x, floorPlanData.gridSize ?? DEFAULT_GRID_SIZE)
      : point.x;
  });
  修复后：
  setFloorPlanData((prev) => {
    const snappedX = prev.snapToGrid            ← 读的是最新 state
      ? snapToGrid(point.x, prev.gridSize ?? DEFAULT_GRID_SIZE)
      : point.x;
  });
  并从 useCallback deps 删除 floorPlanData（:856-867 deps 数组）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BUGFIX-01 dedicated-lines monthlyFee != null（TDD RED）</name>
  <files>
    - xingran-react-frontend/src/pages/operations/dedicated-lines/__tests__/render.test.tsx
    - xingran-react-frontend/src/pages/operations/dedicated-lines/index.tsx
  </files>
  <read_first>
    - xingran-react-frontend/src/pages/operations/dedicated-lines/index.tsx（:490-498 monthlyFee 渲染上下文）
    - xingran-react-frontend/src/pages/operations/dedicated-lines/__tests__/render.test.tsx（如存在）
  </read_first>
  <action>
TDD RED 阶段：为 dedicated-lines index.tsx 的 LineCard 组件（:480-506）写渲染测试，验证 monthlyFee=0 时渲染 "0" 而非空白。

**RED**: 先在 dedicated-lines/__tests__/render.test.tsx 添加测试：
```typescript
it("monthlyFee=0 时应渲染 '0' 而非空白", () => {
  const lineWithZeroFee = {
    ...mockLineData,
    monthlyFee: 0,
  };
  // render with lineWithZeroFee
  // expect(screen.getByText(/月费.*¥0/)).toBeInTheDocument();
});
```

运行测试确认 RED（测试失败，因为当前代码用 `&&` 判断 falsy）。

如无现有 render.test.tsx，创建新测试文件。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npx vitest run src/pages/operations/dedicated-lines/__tests__/render.test.tsx --reporter=verbose 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>
    - test-command: 测试运行且因 monthlyFee=0 不渲染而 FAIL（RED 确认）
    - cli: `cd xingran-react-frontend && npm run type-check` exit 0
  </acceptance_criteria>
  <done>
RED 测试写完并确认失败；type-check 通过。
</done>
</task>

<task type="auto">
  <name>Task 2: BUGFIX-01 dedicated-lines monthlyFee != null（TDD GREEN）</name>
  <files>xingran-react-frontend/src/pages/operations/dedicated-lines/index.tsx</files>
  <read_first>
    - xingran-react-frontend/src/pages/operations/dedicated-lines/index.tsx（:494 monthlyFee 渲染行）
  </read_first>
  <action>
TDD GREEN 阶段：修复 dedicated-lines/index.tsx :494 的 monthlyFee 判断。

将：
```tsx
{line.monthlyFee && (
  <div><strong>月费：</strong>¥{line.monthlyFee}</div>
)}
```

改为：
```tsx
{line.monthlyFee != null && (
  <div><strong>月费：</strong>¥{line.monthlyFee}</div>
)}
```

约束：
1. 仅改判断条件，不改 JSX 结构
2. `!= null` 覆盖 0 值（numeric 0 不是 null，但 `!=` 运算符会认为 0 != null 为 true）
  </action>
  <verify>
    <automated>grep -cE "monthlyFee != null" xingran-react-frontend/src/pages/operations/dedicated-lines/index.tsx && cd xingran-react-frontend && npx vitest run src/pages/operations/dedicated-lines/__tests__/render.test.tsx && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -cE "monthlyFee != null" xingran-react-frontend/src/pages/operations/dedicated-lines/index.tsx` >= 1
    - test-command: `cd xingran-react-frontend && npx vitest run src/pages/operations/dedicated-lines/__tests__/render.test.tsx` 全绿
    - cli: `cd xingran-react-frontend && npm run type-check` exit 0
  </acceptance_criteria>
  <done>
dedicated-lines monthlyFee != null 修复完成；测试从 RED 变 GREEN；type-check 通过。
</done>
</task>

<task type="auto">
  <name>Task 3: BUGFIX-01 FloorCardView area != null（TDD RED→GREEN）</name>
  <files>
    - xingran-react-frontend/src/pages/operations/floors/components/FloorCardView.tsx
    - xingran-react-frontend/src/pages/operations/floors/__tests__/FloorCardView.render.test.tsx
  </files>
  <read_first>
    - xingran-react-frontend/src/pages/operations/floors/components/FloorCardView.tsx（:82 area 渲染行）
    - xingran-react-frontend/src/pages/operations/floors/__tests__/FloorCardView.render.test.tsx（如存在）
  </read_first>
  <action>
TDD RED→GREEN 合并执行：

RED：写测试验证 area=0 渲染 "0" 而非空白（如测试文件不存在则创建）。

GREEN：修复 FloorCardView.tsx :82 的 area 判断。

将：
```tsx
{floor.area && (
  <div><strong>面积：</strong>{floor.area}m²</div>
)}
```

改为：
```tsx
{floor.area != null && (
  <div><strong>面积：</strong>{floor.area}m²</div>
)}
```

运行测试确认 GREEN。
  </action>
  <verify>
    <automated>grep -cE "area != null" xingran-react-frontend/src/pages/operations/floors/components/FloorCardView.tsx && cd xingran-react-frontend && npx vitest run src/pages/operations/floors/__tests__/FloorCardView.render.test.tsx 2>/dev/null && npm run type-check --prefix xingran-react-frontend</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -cE "area != null" xingran-react-frontend/src/pages/operations/floors/components/FloorCardView.tsx` >= 1
    - test-command: `cd xingran-react-frontend && npx vitest run src/pages/operations/floors/__tests__/FloorCardView.render.test.tsx` 全绿
    - cli: `cd xingran-react-frontend && npm run type-check` exit 0
  </acceptance_criteria>
  <done>
FloorCardView area != null 修复完成；测试全绿；type-check 通过。
</done>
</task>

<task type="auto">
  <name>Task 4: BUGFIX-02 VariablesModal columns 去重（TDD RED→GREEN）</name>
  <files>xingran-react-frontend/src/pages/network/templates/modals/VariablesModal.tsx</files>
  <read_first>
    - xingran-react-frontend/src/pages/network/templates/modals/VariablesModal.tsx（:31-53 columns 定义全文）
    - xingran-react-frontend/src/pages/network/templates/__tests__/VariablesModal.test.tsx（如存在）
  </read_first>
  <action>
TDD RED→GREEN 合并执行：

RED：写测试验证 columns 数组长度为 3（当前 6 列重复 bug）。

GREEN：修复 VariablesModal.tsx columns 定义。

当前 bug（:31-53）：
```typescript
const columns = [
  { title: "变量名", dataIndex: "key", key: "key" },           // 第 1 次
  { title: "描述", dataIndex: "description", key: "description" },
  { title: "默认值", dataIndex: "defaultValue", key: "defaultValue" },
  { title: "变量名", dataIndex: "key", key: "key", sorter },   // 第 2 次（重复）
  { title: "描述", dataIndex: "description", key: "description", sorter },
  { title: "默认值", dataIndex: "defaultValue", key: "defaultValue", sorter },
];
```

修复后（useMemo + 去重）：
```typescript
const columns = useMemo(() => {
  const baseColumns: ColumnsType<VariableRow> = [
    { title: "变量名", dataIndex: "key", key: "key" },
    { title: "描述", dataIndex: "description", key: "description" },
    { title: "默认值", dataIndex: "defaultValue", key: "defaultValue" },
  ];
  return baseColumns.map((col) => ({
    ...col,
    sorter: createSorter<VariableRow>(col.dataIndex as keyof VariableRow, "string"),
  }));
}, []);
```

验证 columns 数组长度 === 3。
  </action>
  <verify>
    <automated>grep -cE "useMemo" xingran-react-frontend/src/pages/network/templates/modals/VariablesModal.tsx && cd xingran-react-frontend && npx vitest run src/pages/network/templates/__tests__/VariablesModal.test.tsx 2>/dev/null && npm run type-check --prefix xingran-react-frontend</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -cE "useMemo" xingran-react-frontend/src/pages/network/templates/modals/VariablesModal.tsx` >= 1
    - source-assertion: `grep -cE "title.*变量名.*key.*key" xingran-react-frontend/src/pages/network/templates/modals/VariablesModal.tsx | wc -l` == 1（仅 1 次）
    - test-command: `cd xingran-react-frontend && npx vitest run src/pages/network/templates/__tests__/VariablesModal.test.tsx` 全绿
    - cli: `cd xingran-react-frontend && npm run type-check` exit 0
  </acceptance_criteria>
  <done>
VariablesModal columns 去重为 3 列 + useMemo 化；测试全绿；type-check 通过。
</done>
</task>

<task type="auto">
  <name>Task 5: BUGFIX-03 CADFloorPlanEditor stale closure 修复（TDD RED→GREEN）</name>
  <files>xingran-react-frontend/src/components/cad-editor/CADFloorPlanEditor.tsx</files>
  <read_first>
    - xingran-react-frontend/src/components/cad-editor/CADFloorPlanEditor.tsx（:870-1028 updater 全文 + :856-867 deps 数组）
    - xingran-react-frontend/src/components/cad-editor/__tests__/CADFloorPlanEditor.snap.test.tsx（如存在）
  </read_first>
  <action>
TDD RED→GREEN 合并执行：

RED：写测试验证 snapToGrid/gridSize 变更后拖动能正确吸附（stale closure 测试）。

GREEN：修复 CADFloorPlanEditor.tsx :870-1028 updater 的 stale closure。

在 setFloorPlanData updater 内部：
1. 将 `floorPlanData.snapToGrid` 改为 `prev.snapToGrid`
2. 将 `floorPlanData.gridSize` 改为 `prev.gridSize`

示例（:720-725 既有正确模式）：
```typescript
// 修复前（stale closure）：
const snappedX = floorPlanData.snapToGrid
  ? snapToGrid(point.x, floorPlanData.gridSize ?? DEFAULT_GRID_SIZE)
  : point.x;

// 修复后：
const snappedX = prev.snapToGrid
  ? snapToGrid(point.x, prev.gridSize ?? DEFAULT_GRID_SIZE)
  : point.x;
```

同时从 useCallback deps（:856-867）删除 floorPlanData（保留其他依赖）。

约束：
1. updater 内有 3 处需修复：新建工位（:720-725）、显示文本输入（:765-770）、批量拖动（:931-936）
2. 每处均改 floorPlanData.snapToGrid → prev.snapToGrid，floorPlanData.gridSize → prev.gridSize
3. 仅改读取来源，不改 snapToGrid/snapCoord 函数本身
  </action>
  <verify>
    <automated>grep -cE "prev\.snapToGrid" xingran-react-frontend/src/components/cad-editor/CADFloorPlanEditor.tsx && grep -cE "floorPlanData.*snapToGrid" xingran-react-frontend/src/components/cad-editor/CADFloorPlanEditor.tsx | wc -l | xargs test "\$1" -eq 0 && cd xingran-react-frontend && npx vitest run src/components/cad-editor/__tests__/CADFloorPlanEditor.snap.test.tsx 2>/dev/null && npm run type-check --prefix xingran-react-frontend</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -cE "prev\.snapToGrid" xingran-react-frontend/src/components/cad-editor/CADFloorPlanEditor.tsx` >= 3（3 处修复点）
    - source-assertion: `grep -cE "floorPlanData\.snapToGrid" xingran-react-frontend/src/components/cad-editor/CADFloorPlanEditor.tsx` == 0（在 updater 内已消除）
    - source-assertion: deps 数组中 floorPlanData 已删除
    - test-command: `cd xingran-react-frontend && npx vitest run src/components/cad-editor/__tests__/CADFloorPlanEditor.snap.test.tsx` 全绿
    - cli: `cd xingran-react-frontend && npm run type-check` exit 0
  </acceptance_criteria>
  <done>
CADFloorPlanEditor 3 处 stale closure 修复完成；prev.snapToGrid/gridSize 替代 floorPlanData.snapToGrid/gridSize；deps 删除 floorPlanData；测试全绿；type-check 通过。
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→React rendering | 纯前端正确性修复，无网络/认证面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-04-A | Tampering（行为变更） | BUGFIX-01 monthlyFee/area | mitigate | `!= null` 判断是语义修正，0 是合法业务值；回归测试锁定正确行为 |
| T-117-04-B | Tampering（行为变更） | BUGFIX-02 VariablesModal 列去重 | mitigate | 6 列→3 列减少冗余显示；回归测试锁定 3 列长度 |
| T-117-04-C | Tampering（功能破坏） | BUGFIX-03 snapToGrid stale closure | mitigate | 修复后 snap 操作使用最新 state；回归测试锁定吸附行为正确性 |
| T-117-04-SC | Supply Chain | 依赖安装 | mitigate（不适用） | 本 plan 零新增依赖 |
</threat_model>

<verification>
- `cd xingran-react-frontend && npx vitest run src/pages/operations/dedicated-lines/__tests__/render.test.tsx src/pages/operations/floors/__tests__/FloorCardView.render.test.tsx src/pages/network/templates/__tests__/VariablesModal.test.tsx src/components/cad-editor/__tests__/CADFloorPlanEditor.snap.test.tsx` 全绿
- `cd xingran-react-frontend && npm run type-check` exit 0
- `cd xingran-react-frontend && npm run lint -- --max-warnings 0` exit 0
</verification>

<success_criteria>
1. BUGFIX-01: monthlyFee != null + area != null，0 值正确渲染 "0"
2. BUGFIX-02: columns 数组长度 === 3（非 6）
3. BUGFIX-03: prev.snapToGrid/gridSize 替代 floorPlanData.snapToGrid/gridSize，deps 删除 floorPlanData
4. 所有回归测试 GREEN（D-04 红→绿口径）
</success_criteria>

<output>
Create `.planning/phases/117-render-columns-and-bugfix-rendering/117-04-SUMMARY.md` when done
</output>
