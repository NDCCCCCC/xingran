---
phase: "117-render-columns-and-bugfix"
plan: "03"
type: execute
wave: 2
depends_on: []
files_modified:
  - xingran-react-frontend/src/components/network/MACHeatmapChart.tsx
  - xingran-react-frontend/src/components/cad-elements/DoorElement.tsx
autonomous: true
requirements:
  - RENDER-03
  - RENDER-04
must_haves:
  truths:
    - "MACHeatmapChart.tsx:118 移动端分支 topPorts sort+spread 提 useMemo，desktop 分支不受影响"
    - "DoorElement.tsx:162,170,185,188 hingePoint/openEndPoint 坐标补 snapCoord 精度处理（对齐 DoorElement.tsx:94-95 既有 snapCoord 机制）"
  artifacts:
    - path: "xingran-react-frontend/src/components/network/MACHeatmapChart.tsx"
      provides: "移动端 topPorts sort+spread useMemo 化（RENDER-03）"
      contains: "useMemo"
    - path: "xingran-react-frontend/src/components/cad-elements/DoorElement.tsx"
      provides: "hingePoint/openEndPoint snapCoord 精度处理（RENDER-04）"
      contains: "snapCoord"
  key_links:
    - from: "MACHeatmapChart.tsx 移动端分支"
      to: "topPorts sort+spread useMemo"
      via: "useMemo([data.cells])"
      pattern: "useMemo.*topPorts"
    - from: "DoorElement.tsx hingePoint/openEndPoint"
      to: "snapCoord 精度处理"
      via: "snapCoord(geometry.hingePoint.x / y)"
      pattern: "snapCoord.*geometry\\.(hingePoint|openEndPoint)"
---

<objective>
RENDER-03 + RENDER-04：两个独立组件的渲染优化。

RENDER-03: MACHeatmapChart.tsx 移动端分支的 topPorts sort+spread 提 useMemo（desktop 分支已正确）。
RENDER-04: DoorElement.tsx hingePoint/openEndPoint 坐标补 snapCoord 精度处理（对齐 DoorElement.tsx:94-95 既有机制）。

Purpose: 两个独立组件的局部渲染优化，互不干扰，独立 wave 可并行。
Output: 2 个组件优化，既有测试零回归。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md Phase 117 段
@.planning/REQUIREMENTS.md RENDER-03 / RENDER-04
@xingran-react-frontend/src/components/network/MACHeatmapChart.tsx（:116-118 移动端分支，sort+spread 无 useMemo）
@xingran-react-frontend/src/components/cad-elements/DoorElement.tsx（:162,170,185,188 hingePoint/openEndPoint 坐标 + :94-95 snapCoord 机制参考）

<interfaces>
<!-- 核验完毕（2026-09-14）：两组件现状 -->

MACHeatmapChart.tsx :116-118 移动端分支现状：
  const topPorts = [...data.cells].sort((a, b) => b.changeCount - a.changeCount).slice(0, 20);
  const maxCount = Math.max(...topPorts.map((c) => c.changeCount), 1);
  // 每次 render 重新 sort + spread，useMemo 化后可避免

DoorElement.tsx :94-95 既有 snapCoord 机制（参考）：
  const snapCoord = (coord: number) => Math.round(coord / GRID_SIZE) * GRID_SIZE;
  // 用于单开门/双开门的门轴和开门终点坐标精度对齐

DoorElement.tsx :162,170,185,188 hingePoint/openEndPoint 现状（缺 snapCoord）：
  <path d={`M ${geometry.hingePoint.x} ${geometry.hingePoint.y} ...`}
  <path d={`M ${geometry.openEndPoint.x} ${geometry.openEndPoint.y} ...`}
  // 坐标值未经 snapCoord 处理，与 :94-95 机制不一致
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MACHeatmapChart.tsx 移动端 topPorts sort+spread useMemo 化（RENDER-03）</name>
  <files>xingran-react-frontend/src/components/network/MACHeatmapChart.tsx</files>
  <read_first>
    - xingran-react-frontend/src/components/network/MACHeatmapChart.tsx（:116-140 移动端分支全文 + desktop 分支对比）
  </read_first>
  <action>
将 MACHeatmapChart.tsx :118-119 移动端分支的 topPorts sort+spread 提 useMemo：

```typescript
const topPorts = useMemo(
  () => [...data.cells].sort((a, b) => b.changeCount - a.changeCount).slice(0, 20),
  [data.cells]
);

const maxCount = useMemo(
  () => Math.max(...topPorts.map((c) => c.changeCount), 1),
  [topPorts]
);
```

约束：
1. useMemo deps 为 [data.cells]（数据源变化才重新计算）
2. maxCount 依赖 topPorts 变化，形成 useMemo chain
3. desktop 分支（:112 之前）不受影响
4. isMobile 判断逻辑不变
  </action>
  <verify>
    <automated>grep -cE "useMemo" xingran-react-frontend/src/components/network/MACHeatmapChart.tsx && cd xingran-react-frontend && npx vitest run src/components/network/__tests__/MACHeatmapChart.test.tsx 2>/dev/null || echo "no test" && npm run type-check --prefix xingran-react-frontend</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -cE "useMemo" xingran-react-frontend/src/components/network/MACHeatmapChart.tsx` >= 2（topPorts + maxCount）
    - source-assertion: 移动端分支 topPorts sort 在 useMemo 内，非 inline
    - cli: `cd xingran-react-frontend && npm run type-check` exit 0
  </acceptance_criteria>
  <done>
MACHeatmapChart.tsx 移动端 topPorts + maxCount 改为 useMemo；type-check 通过。
</done>
</task>

<task type="auto">
  <name>Task 2: DoorElement.tsx hingePoint/openEndPoint 补 snapCoord 精度处理（RENDER-04）</name>
  <files>xingran-react-frontend/src/components/cad-elements/DoorElement.tsx</files>
  <read_first>
    - xingran-react-frontend/src/components/cad-elements/DoorElement.tsx（:90-100 snapCoord 定义 + :162,170,185,188 坐标引用全文）
    - 确认 GRID_SIZE 常量来源（:90 附近定义）
  </read_first>
  <action>
将 DoorElement.tsx :162,170,185,188 的 hingePoint/openEndPoint 坐标用 snapCoord 包装：

1. 确认 :90 附近已定义 snapCoord 函数（如未定义则补充）：
   ```typescript
   const GRID_SIZE = 10; // 或从 props/常量导入
   const snapCoord = (coord: number) => Math.round(coord / GRID_SIZE) * GRID_SIZE;
   ```

2. 改造 4 处坐标引用：
   - :162 `geometry.hingePoint.x / y` → `snapCoord(geometry.hingePoint.x) / snapCoord(geometry.hingePoint.y)`
   - :170 `geometry.openEndPoint.x / y` → `snapCoord(geometry.openEndPoint.x) / snapCoord(geometry.openEndPoint.y)`
   - :185 同 hingePoint
   - :188 `geometry.leafEndX / Y` → `snapCoord(geometry.leafEndX) / snapCoord(geometry.leafEndY)`

3. 确认 :94-95 单开门分支（:183-192）的 hingePoint + leafEnd 也已处理（:185,188）

约束：
1. snapCoord 已在 :94-95 存在则复用，不重复定义
2. 仅改坐标值处理，不改 SVG path 字符串模板结构
3. 双开门分支（:157-179）的 hingePoint + leafEnd + leafEnd2 + openEndPoint 全部处理
  </action>
  <verify>
    <automated>grep -cE "snapCoord.*hingePoint" xingran-react-frontend/src/components/cad-elements/DoorElement.tsx && grep -cE "snapCoord.*openEndPoint" xingran-react-frontend/src/components/cad-elements/DoorElement.tsx && npm run type-check --prefix xingran-react-frontend</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `grep -cE "snapCoord.*hingePoint" xingran-react-frontend/src/components/cad-elements/DoorElement.tsx` >= 2
    - source-assertion: `grep -cE "snapCoord.*openEndPoint" xingran-react-frontend/src/components/cad-elements/DoorElement.tsx` >= 1
    - source-assertion: `grep -cE "snapCoord.*leafEnd" xingran-react-frontend/src/components/cad-elements/DoorElement.tsx` >= 2
    - cli: `cd xingran-react-frontend && npm run type-check` exit 0
  </acceptance_criteria>
  <done>
DoorElement.tsx 4 处 hingePoint/openEndPoint/leafEnd 坐标补 snapCoord 处理；type-check 通过。
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→React rendering | 纯前端渲染优化，无网络/认证面，无输入处理变更 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-03-A | Tampering（精度漂移） | snapCoord 改动 | mitigate | snapCoord 是纯数学取整，不改变几何语义；现有测试（如有）零回归守护 |
| T-117-03-B | Tampering（性能劣化） | useMemo chain | mitigate | maxCount 依赖 topPorts 形成 chain，首次渲染多一次计算但避免重复 sort |
| T-117-03-SC | Supply Chain | 依赖安装 | mitigate（不适用） | 本 plan 零新增依赖 |
</threat_model>

<verification>
- `cd xingran-react-frontend && npm run type-check` exit 0
- `cd xingran-react-frontend && npx vitest run src/components/network/__tests__/MACHeatmapChart.test.tsx 2>/dev/null || echo "no test"` 全绿
</verification>

<success_criteria>
1. MACHeatmapChart.tsx 移动端 topPorts + maxCount 改为 useMemo chain
2. DoorElement.tsx 4 处坐标引用全部经 snapCoord 处理
3. 既有测试零回归（D-04 口径）
</success_criteria>

<output>
Create `.planning/phases/117-render-columns-and-bugfix-rendering/117-03-SUMMARY.md` when done
</output>
