---
phase: 44-ip-r3
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - internal/scheduler/reconciliation_tasks.go
  - internal/scheduler/reconciliation_tasks_test.go
  - internal/services/asset/reconciliation_baseline.go
  - internal/services/asset/reconciliation_baseline_test.go
  - internal/services/asset/reconciliation_exception.go
  - internal/services/asset/reconciliation_exception_handler.go
  - internal/services/asset/reconciliation_exception_handler_test.go
  - internal/api/v1/asset/reconciliation_exception_handler.go
  - internal/api/v1/asset/reconciliation_exception_router.go
  - internal/services/operations/excel_config.go
  - internal/services/operations/excel_service.go
  - internal/services/operations/excel_service_test.go
  - xingran-react-frontend/src/pages/asset/reconciliation/exception-rules/index.tsx
  - xingran-react-frontend/src/pages/asset/reconciliation/dashboard/index.tsx
  - xingran-react-frontend/src/lib/assetApi.ts
autonomous: true
requirements:
  - EXCEPTION-03
  - EXCEPTION-02
  - AUDIT-01

must_haves:
  truths:
    - "SC 5: cleanupExpiredExceptions cron 每日 03:00 软停用过期例外规则（UPDATE is_active=1，不删记录），二次调用 rowsAffected=0（幂等）"
    - "SC 8（可观察条件）: 给定运维已调用 Snapshot 记录的 baseline 存在时，dashboard 降噪效果卡片显示 exceptions/workorders/critical 三项下降百分比，目标 ≥60%；无 baseline 时卡片显示引导提示 \"请先到例外规则管理页记录基线\"。SC 8 ≥60% 降噪成立的前置：运维必须在 R3 部署前 + R2 数据保留期内调用 Snapshot 记录 R2 末期基线（manual-only，无 baseline 则 SC 8 不可量化验证）"
    - "SC 9: 运维可用 Excel 导入/导出例外规则（name/ip_range/conflict_types/exception_actions/severity_override/scope_type/scope_name/expires_at/reason 9 列）"
    - "EXCEPTION-03: 过期软停用后历史 sys_data_reconciliation.exception_rule_id 仍指向有效（虽停用）记录，审计链不断"
    - "EXCEPTION-02 转单通路: createWorkorderCritical/High SQL 加 (applied_actions IS NULL OR 'no_workorder' != ANY(applied_actions))，命中 no_workorder 的异常不转单；applied_actions=NULL（model 无 default tag 的 INSERT 默认值）的 critical/high 异常必须被转单（BLOCKER-4）"
    - "EXCEPTION-02 Excel 通路: 导入的 scope_name 按 scope_type 解析为 scope_id（dept→sys_dept.dept_name / user→sys_user.username / global→留空）"
    - "降噪基线 snapshot 存 sys_config JSON（config_key=asset.reconciliation.baseline），Snapshot COUNT 包含 silence 记录（与 ListExceptions UI 默认隐藏 silence 的语义解耦）；Compare 端点返回下降百分比"
  artifacts:
    - path: "internal/services/asset/reconciliation_baseline.go"
      provides: "BaselineSnapshot struct + Snapshot/Compare service（独立 COUNT + sys_config JSON 读写）"
      exports: ["Snapshot", "Compare"]
    - path: "internal/scheduler/reconciliation_tasks.go"
      provides: "cleanupExpiredExceptions case 真实实现（软停用 is_active=1）+ createWorkorderCritical/High SQL 加 no_workorder 过滤（IS NULL 兜底）"
      contains: "UPDATE.*is_active.*1.*expires_at < NOW()"
    - path: "internal/api/v1/asset/reconciliation_exception_handler.go"
      provides: "SnapshotBaseline + CompareBaseline handler（44-01 仅有 Create/Update/Delete/TestRule，本 plan 新增 baseline handler）"
      contains: "func (h *ReconciliationExceptionHandler) SnapshotBaseline"
    - path: "internal/services/operations/excel_config.go"
      provides: "reconciliationExceptionRule ExcelConfig 条目（9 列 + UpsertKey name + DBField 配齐）"
      contains: "reconciliationExceptionRule"
  key_links:
    - from: "internal/scheduler/reconciliation_tasks.go"
      to: "sys_reconciliation_exception (is_active 字段)"
      via: "cleanupExpiredExceptions UPDATE is_active=1 WHERE expires_at < NOW()"
      pattern: "expires_at < NOW\\(\\).*is_active"
    - from: "internal/scheduler/reconciliation_tasks.go"
      to: "sys_data_reconciliation (applied_actions 字段)"
      via: "createWorkorderBySeverity SQL 加 (applied_actions IS NULL OR 'no_workorder' != ANY(applied_actions))"
      pattern: "applied_actions IS NULL OR 'no_workorder' != ANY\\(applied_actions\\)"
    - from: "internal/services/operations/excel_service.go"
      to: "internal/services/asset/reconciliation_exception.go"
      via: "ImportRules handler 调 ImportData 后处理 scope_name → scope_id + TEXT[] 转换"
      pattern: "scope_id"
---

<objective>
落地 R3 收尾：cleanupExpiredExceptions cron 真实实现（软停用 + 幂等）+ 转单 cron SQL 加 no_workorder 过滤（含 applied_actions IS NULL 兜底）+ 降噪基线快照/对比端点（独立 COUNT，规避 MaxPageSize=100 钳制）+ SnapshotBaseline/CompareBaseline handler（44-01 未实现后端 baseline handler，本 plan 新增）+ dashboard 降噪效果卡片 + Excel 导入导出例外规则（专用 ImportRules handler + scope_name 后处理 UPDATE scope_id + TEXT[] 转换）。

Purpose: 让 R3 例外规则引擎形成闭环——规则能自动过期、转单通路尊重 no_workorder（且不漏转 applied_actions=NULL 的 critical 异常）、运维能量化验证 ≥60% 降噪效果（SC 8，前置：R3 部署前记录 R2 末期基线）、规则能批量 Excel 管理。覆盖 SC 5/8/9 + EXCEPTION-03 + EXCEPTION-02 转单/Excel 通路。
Output: 1 个新 baseline service + 1 个 ExcelConfig 条目 + 2 个修改的 scheduler/cron 文件 + SnapshotBaseline/CompareBaseline handler + 3 个修改的前端文件 + 6 个测试桩。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-ip-r3/44-CONTEXT.md
@.planning/phases/44-ip-r3/44-RESEARCH.md
@.planning/phases/44-ip-r3/44-PATTERNS.md
@.planning/phases/44-ip-r3/44-VALIDATION.md
@.planning/phases/44-ip-r3/44-01-PLAN.md

<interfaces>
<!-- 44-01 产出的接口（44-02 直接消费）。Executor 直接用，无需 codebase 探索。 -->

From 44-01 Task 3（CRUD service 接口，44-02 baseline 复用 db + COUNT 模式）:
```go
type reconciliationExceptionServiceImpl struct { db *gorm.DB }
// NewReconciliationExceptionService(db) 已存在
```

From 44-01 Task 5（module 常量，44-02 baseline handler 复用）:
```go
ModuleReconciliationExceptionRule = "资产对账-例外规则"  // 已在 reconciliation_handler.go
```

⚠ **From 44-01 Task 5 实测（BLOCKER-2 修正）**：44-01 Task 5 **仅**产出 `CreateRule` / `UpdateRule` / `DeleteRule` / `TestRule` 4 个 handler。**44-01 未实现后端 baseline handler（无 SnapshotBaseline / CompareBaseline）**。本 plan Task 3 在 `reconciliation_exception_handler.go` 新增 SnapshotBaseline + CompareBaseline handler（struct 加 baselineSvc 字段）+ 路由注册。

From 44-01 Task 6a（assetApi baseline 端点 wire + queryKeys baselineCompare 已注册）:
```ts
baseline: {
  snapshot: () => post('/asset/reconciliation/baseline/snapshot'),
  compare: () => post('/asset/reconciliation/baseline/compare'),
}
// queryKeys.reconciliation.baselineCompare() 已注册（44-01 Task 6a）
```

From internal/scheduler/reconciliation_tasks.go:42-102（单 taskType "reconciliation" + params["param"] 分发模式）:
```go
case "cleanupExpiredExceptions":  // :78 placeholder，R3 真实实现
    applogger.Infof("[reconciliation:cleanupExpiredExceptions] R1 placeholder, R3 真实实现")
```

From internal/scheduler/reconciliation_tasks.go:188-194（转单 SQL 现有 WHERE）:
```go
db.WithContext(ctx).
    Where("severity = ? AND deleted_at IS NULL AND resolved_at IS NULL AND workorder_id IS NULL", severity).
    Order("detected_at ASC").Limit(limit).Find(&exceptions)
```

⚠ **From internal/models/reconciliation.go:50（BLOCKER-4 实测）**:
```go
AppliedActions pq.StringArray `gorm:"type:text[];column:applied_actions"`  // 无 default tag
```
PG INSERT 未指定 applied_actions 值时**默认 NULL**（非空数组）。`'no_workorder' != ANY(NULL)` 返回 NULL → 整个 WHERE 子句该行被过滤（PG 三值逻辑）。**必须**用 `AND (applied_actions IS NULL OR 'no_workorder' != ANY(applied_actions))` 兜底，否则所有"无例外命中"的 critical/high 异常被转单 cron 漏转，反向破坏 R2 WORKORDER-01/02（已 shipped）。PG ANY(NULL) 语义参考：https://www.postgresql.org/docs/current/functions-comparisons.html#id-1.5.8.30.16（"NOT IN 与 ANY 对 NULL 数组返回 NULL"）。

From internal/services/system/config_service.go:201-211（sys_config 读写模式）:
```go
func (s *configService) GetByKey(ctx context.Context, configKey string) (*models.Config, error)
```

From internal/services/operations/excel_config.go:101-152（building ExcelConfig，Columns + Reference + UpsertKey 模式）:
```go
"building": {
    Columns: []ExcelColumn{
        {Field: "name", Header: "楼宇名称", Required: true, MaxLength: 100, UpsertKey: true},
        {Field: "orgName", Header: "所属机构名称/编码", Required: true,
         Reference: "sys_dept.dept_code", DBField: "org_id"},
    },
}
```

From internal/services/operations/excel_service.go（ImportData + GenerateTemplate + AffectedKeys 返回模式 + validateAndParseRow + 后处理钩子位置 — Task 4 read_first 完整读取确认 hook 是否存在）:
```go
// ImportData 返回受影响的记录键列表，可用于后处理
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cleanupExpiredExceptions cron 软停用 + 转单 SQL 加 no_workorder 过滤（IS NULL 兜底强制）</name>
  <files>
    internal/scheduler/reconciliation_tasks.go,
    internal/scheduler/reconciliation_tasks_test.go
  </files>
  <read_first>
    - internal/scheduler/reconciliation_tasks.go:42-102（单 taskType "reconciliation" 分发 + cleanupExpiredExceptions placeholder :78-79）
    - internal/scheduler/reconciliation_tasks.go:127-131（sys_job 已 seed "对账-例外规则清理" cron 0 0 3 * * *，InvokeTarget=reconciliation:cleanupExpiredExceptions）
    - internal/scheduler/reconciliation_tasks.go:188-194（createWorkorderBySeverity 现有 SQL）
    - internal/scheduler/reconciliation_tasks.go:232-233（InvokeTarget 映射）
    - internal/models/reconciliation.go:44-50（⚠ AppliedActions pq.StringArray **无 default tag** → INSERT 默认 NULL，BLOCKER-4 实测证据）
    - 44-RESEARCH.md §Pattern 5 :407-423 + §Pattern 6 :425-443 + §Code Examples :789-817（verbatim 实现代码）
    - 44-PATTERNS.md §7（cleanupExpiredExceptions + 转单 SQL 双 anchor）
    - 44-CONTEXT.md D-R3-A1-02（转单 SQL 加 no_workorder）/ D-R3-A4-03（软停用 + 审计链）
    - 44-RESEARCH.md §Common Pitfalls Pitfall 4（硬删除致外键断链）
    - CLAUDE.md "Status Value Convention"（0=启用 1=停用）
    - PG ANY(NULL) 语义文档：https://www.postgresql.org/docs/current/functions-comparisons.html#id-1.5.8.30.16（INFO-13：ANY 对 NULL 数组返回 NULL，三值逻辑导致 WHERE 过滤）
  </read_first>
  <behavior>
    - TestCleanupExpiredExceptions：DB 含 2 条规则，1 条 expires_at=过去 + 1 条 expires_at=未来，调用 cleanupExpiredExceptions，断言过去那条 is_active=1（停用）+ 未来那条 is_active=0（保持启用）+ 过去那条 deleted_at 仍为 NULL（软停用不软删除）
    - TestCleanupExpiredExceptionsIdempotent：二次调用 cleanupExpiredExceptions，rowsAffected=0（WHERE is_active=0 已不匹配已停用的）
    - TestCleanupExpiredExceptionsNoExpiresAt：expires_at=nil 的规则不被停用（WHERE expires_at IS NOT NULL）
    - TestCleanupExpiredExceptionsAlreadyDeleted：deleted_at 非 NULL 的规则不被处理（WHERE deleted_at IS NULL）
    - TestCreateWorkorderNoWorkorderFilter：DB 含 2 条 critical 异常，1 条 applied_actions=['no_workorder'] + 1 条 applied_actions=nil，调用 createWorkorderCritical，断言仅 nil 那条被选中转单
    - TestCreateWorkorderNoWorkorderFilterEmptyActions：applied_actions 为空数组的异常仍转单（不是 no_workorder）
    - TestCreateWorkorderNoWorkorderFilterNullActions（BLOCKER-4 强制）：DB 含 1 条 critical 异常 applied_actions=NULL（模拟 model 无 default tag 的 INSERT 默认值），调用 createWorkorderCritical，断言该条**必须被选中转单**（验证 SQL 用 `applied_actions IS NULL OR ...` 兜底，而非裸 `'no_workorder' != ANY(applied_actions)`——后者对 NULL 返回 NULL 导致漏转）
  </behavior>
  <action>
    先建/扩展 `reconciliation_tasks_test.go`（RED，sqlite 测试 DB 或 sqlmock 验证 SQL，参照项目现有 scheduler 测试模式；若不存在则新建）。

    **修改 `reconciliation_tasks.go`（GREEN）**：

    (a) **cleanupExpiredExceptions case 真实实现**（替换 :78-79 placeholder，44-RESEARCH.md:793-801 verbatim）：
    ```
    case "cleanupExpiredExceptions":
        result := db.WithContext(ctx).
            Model(&models.SysReconciliationException{}).
            Where("expires_at IS NOT NULL AND expires_at < NOW() AND is_active = ? AND deleted_at IS NULL", 0).
            Update("is_active", 1)  // Status Convention: 0=启用 → 1=停用
        applogger.Infof("[reconciliation:cleanupExpiredExceptions] 软停用 %d 条过期例外规则", result.RowsAffected)
    ```
    **幂等性**：WHERE 含 `is_active=0`（仅启用规则），二次 cron 调用 rowsAffected=0（Pitfall 4 防外键断链 + 审计链保留）。

    (b) **转单 SQL 加 no_workorder 过滤（BLOCKER-4 强制 IS NULL 兜底）**（D-R3-A1-02）：找到 `createWorkorderBySeverity` 函数（约 :188-194，被 createWorkorderCritical/createWorkorderHigh 调用），现有 WHERE 末尾**强制**追加（44-RESEARCH.md:807-817 + BLOCKER-4 修正）：
    ```
    db.WithContext(ctx).
        Where("severity = ? AND deleted_at IS NULL AND resolved_at IS NULL AND workorder_id IS NULL "+
              "AND (applied_actions IS NULL OR 'no_workorder' != ANY(applied_actions))", severity).
        Order("detected_at ASC").Limit(limit).Find(&exceptions)
    ```
    **关键（BLOCKER-4，非 executor discretion）**：**必须**用 `AND (applied_actions IS NULL OR 'no_workorder' != ANY(applied_actions))`，**禁止**用裸 `AND 'no_workorder' != ANY(applied_actions)`。理由：`internal/models/reconciliation.go:50` 的 `AppliedActions pq.StringArray` **无** `default:` tag，PG INSERT 未指定值时默认 NULL（非空数组）。PG 三值逻辑下 `'no_workorder' != ANY(NULL)` 返回 NULL，整个 WHERE 子句该行被过滤——会导致所有"无例外命中"（applied_actions=NULL）的 critical/high 异常被转单 cron **漏转**，反向破坏 R2 WORKORDER-01/02（已 shipped 的转单通路）。TestCreateWorkorderNoWorkorderFilterNullActions 锁定此行为。

    **sys_job 已 seed**（无需新建 job 记录）：`对账-例外规则清理` cron `0 0 3 * * *` 已在 :127-131 注册，InvokeTarget 已映射 :232-233，R3 仅填 case 实现。

    **不要**：改 sys_job seed；改其他 cron case；改前端（Task 3）；改 Excel（Task 4）。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go test ./internal/scheduler/ -run 'TestCleanupExpiredExceptions|TestCreateWorkorderNoWorkorder' -count=1 -v</automated>
  </verify>
  <done>
    - reconciliation_tasks.go 的 cleanupExpiredExceptions case 含 `Update("is_active", 1)` + `Where("expires_at IS NOT NULL AND expires_at < NOW() AND is_active = ? AND deleted_at IS NULL", 0)`
    - reconciliation_tasks.go 不再含 placeholder 字符串 `R1 placeholder, R3 真实实现`
    - createWorkorderBySeverity 的 SQL 含 `(applied_actions IS NULL OR 'no_workorder' != ANY(applied_actions))`（grep 验证 IS NULL 兜底存在，BLOCKER-4）
    - 7 个 test case 全部通过（含 TestCreateWorkorderNoWorkorderFilterNullActions 锁定 NULL 不漏转）
    - 软停用的规则 deleted_at 仍为 NULL（验证非软删除）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 降噪基线 service（Snapshot + Compare，独立 COUNT，COUNT 含 silence 记录）</name>
  <files>
    internal/services/asset/reconciliation_baseline.go,
    internal/services/asset/reconciliation_baseline_test.go
  </files>
  <read_first>
    - internal/services/system/config_service.go:82-211（config CRUD + GetByKey，sys_config 读写 source-of-truth）
    - internal/services/asset/reconciliation_statistics.go:443-478（ExceptionRuleStats 端点已实现，COUNT 模式参考）
    - internal/services/asset/reconciliation_exception.go（44-01 Task 3 产出的 reconciliationExceptionServiceImpl struct + db 字段）
    - internal/models/reconciliation.go（SysDataReconciliation + WorkorderID + Severity + AppliedActions 字段）
    - 44-RESEARCH.md §Pattern 7 :445-480 + §Code Examples（verbatim）+ §Common Pitfalls Pitfall 5（list.length 钳制）
    - 44-PATTERNS.md §6（baseline service 模式）
    - 44-CONTEXT.md D-R3-A1-01（silence 仍写表，列表隐藏但 COUNT 应含）/ D-R3-A4-01（基线快照 sys_config JSON）
    - 44-RESEARCH.md §Open Questions（sys_config JSON vs 独立表，Claude's Discretion 已选 sys_config）
    - 项目记忆 MEMORY.md `stat-cards-from-list-length-capped-at-100`（必须独立 COUNT，禁 list.length）
  </read_first>
  <behavior>
    - TestSnapshotCreatesConfig：调 Snapshot(ctx)，sys_config 表新增/更新 config_key='asset.reconciliation.baseline'，config_value JSON 含 snapshot_at + total_exceptions + total_workorders + critical_exceptions 4 个字段
    - TestSnapshotReflectsCurrentCounts：DB 含 500 异常 + 120 工单（WorkorderID 非 NULL）+ 30 critical，Snapshot 后 config_value 的对应字段值 = 500/120/30
    - TestSnapshotIdempotentOverwrite：二次 Snapshot 覆盖原有 baseline（key 存在则 Update，而非 Create 重复）
    - TestSnapshotCountIncludesSilenceRecords（WARN-8 锁定）：DB 含 480 普通异常 + 20 applied_actions=['silence'] 异常，Snapshot 后 total_exceptions=500（**含 silence**，与 ListExceptions 默认隐藏 silence 的 UI 语义解耦；COUNT WHERE 仅 `deleted_at IS NULL`，不加 silence 过滤）
    - TestCompareReturnsReductionPct：baseline JSON 含 {total_exceptions:500,total_workorders:120,critical_exceptions:30}，当前 DB 含 200/50/10，Compare 返回 exceptions_reduction_pct=60.0 + workorders_reduction_pct≈58.3 + critical_reduction_pct≈66.7
    - TestCompareNoBaselineReturnsError：sys_config 无 baseline key 时 Compare 返回 error 提示"先记录基线"
    - TestCompareCountsUseIndependentCountNotListLength：静态检查 Compare 实现不含 ListExceptions 调用（grep 验证 service 文件不含 `ListExceptions`，仅含 `.Count(`）
  </behavior>
  <action>
    先建 `reconciliation_baseline_test.go`（RED，sqlite/sqlmock 验证 sys_config 读写 + COUNT 逻辑）。

    建 `reconciliation_baseline.go`（GREEN）：

    **struct 定义**（44-RESEARCH.md:465-471 verbatim）：
    ```
    type BaselineSnapshot struct {
        SnapshotAt         time.Time `json:"snapshot_at"`
        TotalExceptions    int64     `json:"total_exceptions"`
        TotalWorkorders    int64     `json:"total_workorders"`
        CriticalExceptions int64     `json:"critical_exceptions"`
    }
    type BaselineCompareResult struct {
        Baseline                BaselineSnapshot `json:"baseline"`
        Current                 BaselineSnapshot `json:"current"`
        ExceptionsReductionPct  float64          `json:"exceptions_reduction_pct"`
        WorkordersReductionPct  float64          `json:"workorders_reduction_pct"`
        CriticalReductionPct    float64          `json:"critical_reduction_pct"`
    }
    ```

    **service**：
    ```
    type ReconciliationBaselineService interface {
        Snapshot(ctx context.Context) (*BaselineSnapshot, error)
        Compare(ctx context.Context) (*BaselineCompareResult, error)
    }
    type reconciliationBaselineServiceImpl struct {
        db          *gorm.DB
        configSvc   system.ConfigService  // 注入，复用 GetByKey/Create/Update
    }
    func NewReconciliationBaselineService(db *gorm.DB, configSvc system.ConfigService) ReconciliationBaselineService
    ```

    **Snapshot 实现**（D-R3-A4-01 + WARN-8 silence 语义锁定）：
    - 独立 COUNT 查询当前快照（**禁止** ListExceptions，Pitfall 5）。**COUNT 包含 silence 记录**（WARN-8：baseline 是降噪效果的全量基准，silence 是降噪手段之一，应计入"当前告警量"；与 ListExceptions UI 默认隐藏 silence 的语义解耦——UI 隐藏是运维视图偏好，baseline COUNT 是量化指标）：
      ```
      var totalExceptions int64
      s.db.WithContext(ctx).Model(&models.SysDataReconciliation{}).
          Where("deleted_at IS NULL").Count(&totalExceptions)  // 不加 silence 过滤
      var totalWorkorders int64
      s.db.WithContext(ctx).Model(&models.SysDataReconciliation{}).
          Where("deleted_at IS NULL AND workorder_id IS NOT NULL").Count(&totalWorkorders)
      var criticalExceptions int64
      s.db.WithContext(ctx).Model(&models.SysDataReconciliation{}).
          Where("deleted_at IS NULL AND severity = ?", "critical").Count(&criticalExceptions)
      ```
    - 构造 BaselineSnapshot{SnapshotAt: time.Now(), TotalExceptions:..., ...}
    - 序列化 JSON → 写 sys_config：先 GetByKey(ctx, "asset.reconciliation.baseline")，存在则 Update config_value，不存在则 Create（config_key + config_value + config_type="JSON"）。可用 config_service 现有方法（参照 44-RESEARCH.md §config_service.go:201-211）。

    **Compare 实现**（D-R3-A4-01）：
    - 读 baseline：GetByKey(ctx, "asset.reconciliation.baseline")，不存在返回 `fmt.Errorf("未找到基线快照，请先调用 Snapshot 记录基线")`
    - 反序列化 JSON → BaselineSnapshot
    - 独立 COUNT 当前（同 Snapshot 的 3 个 COUNT，含 silence）
    - 算下降 %：`pct := func(b, c int64) float64 { if b == 0 { return 0 }; return float64(b-c) / float64(b) * 100 }`
    - 构造 BaselineCompareResult 返回

    **import**：`time` / `encoding/json` / `gorm.io/gorm` / `github.com/xingran-next/xingran-go-backend/internal/models` / `github.com/xingran-next/xingran-go-backend/internal/services/system`（ConfigService）

    **不要**：改前端（Task 3）；改 handler/router（本 task 加 service，**handler 在 Task 3 新增**——44-01 未实现后端 baseline handler，本 plan Task 3 在 reconciliation_exception_handler.go 新增 SnapshotBaseline + CompareBaseline）；用 ListExceptions/pageSize=99999；在 COUNT 加 silence 过滤（WARN-8）。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go test ./internal/services/asset/ -run 'TestSnapshot|TestCompare' -count=1 -v</automated>
  </verify>
  <done>
    - 文件含 `type BaselineSnapshot struct` / `type BaselineCompareResult struct` / `func (s *reconciliationBaselineServiceImpl) Snapshot(` / `Compare(`
    - service 用 `.Count(&...).Error` 独立 COUNT（3 个：total/totalWorkorders/critical），不含 ListExceptions 调用
    - Snapshot 的 totalExceptions COUNT 仅 `Where("deleted_at IS NULL")`，**不含** silence 过滤（grep 验证 Snapshot 函数体内无 `'silence' = ANY`，WARN-8）
    - config_key = `asset.reconciliation.baseline`（grep 验证）
    - Snapshot 幂等覆盖（key 存在则 Update）
    - Compare 无 baseline 时返回 error
    - 7 个 test case 全通过（含 TestSnapshotCountIncludesSilenceRecords）
    - **运维文档化要求**：在 reconciliation_baseline.go 文件头注释或 Snapshot 函数 doc comment 明确写 "运维必须在 R3 部署前 + R2 数据保留期内调用 Snapshot 记录 R2 末期基线，否则 SC 8 ≥60% 降噪不可量化验证"（BLOCKER-3）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 新增 SnapshotBaseline + CompareBaseline handler + 路由注册 + dashboard 降噪卡片</name>
  <files>
    internal/api/v1/asset/reconciliation_exception_handler.go,
    internal/api/v1/asset/reconciliation_exception_handler_test.go,
    internal/api/v1/asset/reconciliation_exception_router.go,
    xingran-react-frontend/src/pages/asset/reconciliation/dashboard/index.tsx,
    xingran-react-frontend/src/pages/asset/reconciliation/exception-rules/index.tsx
  </files>
  <read_first>
    - internal/api/v1/asset/reconciliation_exception_handler.go（44-01 Task 5 产出的 CreateRule/UpdateRule/DeleteRule/TestRule handler + operlog 接入模式 + ReconciliationExceptionHandler struct 现有字段 service/core）
    - internal/api/v1/asset/reconciliation_exception_router.go（44-01 Task 5 产出的路由注册 + RequirePermissions 模式）
    - internal/services/asset/reconciliation_baseline.go（Task 2 产出的 ReconciliationBaselineService 接口 + Snapshot/Compare 方法签名）
    - xingran-react-frontend/src/pages/asset/reconciliation/dashboard/index.tsx（现有 dashboard，参照统计卡片结构）
    - xingran-react-frontend/src/pages/asset/reconciliation/exception-rules/index.tsx（44-01 Task 6b 产出的 admin 页，确认"记录基线"按钮已 wire 到 reconciliationApi.baseline.snapshot）
    - xingran-react-frontend/src/lib/assetApi.ts（44-01 Task 6a 产出的 baseline.{snapshot,compare} 端点 wire）
    - xingran-react-frontend/src/lib/queryKeys.ts（44-01 Task 6a 产出的 queryKeys.reconciliation.baselineCompare()）
    - 44-CONTEXT.md D-R3-A4-01（dashboard 降噪卡片 + 对比端点返回下降%）
    - CLAUDE.md "操作日志记录约定"（SnapshotBaseline 是写操作，调 operlog.Record OperTypeUpdate）
    - 项目记忆 MEMORY.md `stat-cards-from-list-length-capped-at-100`（前端用 compare 端点，不读 list.length）
  </read_first>
  <behavior>
    - TestSnapshotBaselineHandler：POST /baseline/snapshot，handler 调 ReconciliationBaselineService.Snapshot + success path 调 operlog.Record(OperTypeUpdate)，返回 BaselineSnapshot JSON
    - TestCompareBaselineHandler：POST /baseline/compare，handler 调 ReconciliationBaselineService.Compare，返回 BaselineCompareResult JSON；无 baseline 时返回 400 + 错误信息
    - TestCompareBaselineHandlerNoOperlog：Compare 是读操作，不调 operlog.Record（grep 验证 CompareBaseline 函数体不含 operlog.Record）
    - 前端 `npm run type-check` + `npm run build` 退出 0
    - dashboard 含"降噪效果"卡片，显示 3 个下降% 指标（exceptions/workorders/critical reduction pct）；无 baseline 时（compare 返回 400）显示 Alert "请先到例外规则管理页记录基线"
    - exception-rules admin 页"记录基线"按钮 onClick 调 reconciliationApi.baseline.snapshot + 成功 message
  </behavior>
  <action>
    先扩展 `reconciliation_exception_handler_test.go`（RED）。

    **修改 `reconciliation_exception_handler.go`（BLOCKER-2：44-01 未实现 baseline handler，本 task 新增）**：扩展 ReconciliationExceptionHandler 加 baselineSvc 字段 + 2 个新 handler（参照 44-01 Task 5 CreateRule/UpdateRule/DeleteRule 模式）：
    - struct 加 `baselineSvc asset.ReconciliationBaselineService`（在现有 `service` + `core` 字段之后）
    - struct 构造：现有 handler 用 `WithCore(core *core.Core)` 链式注入。本 task 在 `SetupReconciliationExceptionRouter` 中（router 文件）构造 baseline service 并通过新方法注入，例如加 `func (h *ReconciliationExceptionHandler) WithBaselineService(svc asset.ReconciliationBaselineService) *ReconciliationExceptionHandler`，或在 router 内直接 `handler.baselineSvc = NewReconciliationBaselineService(...)`（若字段包级可见）。executor 选其一与现有 WithCore 模式一致的方式。
    - `SnapshotBaseline(c *gin.Context)`：调 `h.baselineSvc.Snapshot(c.Request.Context())` 失败 → `response.Error(c, http.StatusInternalServerError, err.Error())`；success path 末尾 `operlog.Record(c, h.core.OperLogService, h.core.GetDB(), ModuleReconciliationExceptionRule, operlog.OperTypeUpdate)` 然后 `response.Success(c, snapshot)`
    - `CompareBaseline(c *gin.Context)`：调 `h.baselineSvc.Compare(...)` 失败（含"未找到基线"）→ `response.Error(c, http.StatusBadRequest, err.Error())`；success → `response.Success(c, result)`。**读操作不调 operlog**（compare 是读 sys_config + COUNT，不改数据）

    **修改 `reconciliation_exception_router.go`**：SetupReconciliationExceptionRouter 内构造 baseline service + 注入 handler + 加 2 路由：
    ```
    baselineSvc := asset.NewReconciliationBaselineService(core.GetDB(), core.ConfigService)  // 若 core 无 ConfigService 字段，参照 Task 2 的 fallback：直接传 db，baseline service 内部 SQL sys_config
    handler.baselineSvc = baselineSvc  // 或 handler.WithBaselineService(baselineSvc)
    r.POST("/baseline/snapshot", handler.SnapshotBaseline)
    r.POST("/baseline/compare", handler.CompareBaseline)
    ```
    baseline 端点权限：复用 admin 页访问权限，可不加 RequirePermissions（参照 44-01 list/:id 现有放宽模式，audit-only 场景）或加 `asset:reconciliation:exception:list`（读权限，admin 默认持）。executor 视项目 RBAC 惯例决定，与 44-01 现有路由风格保持一致。

    **修改前端 `dashboard/index.tsx`**：在现有统计卡片区域加"降噪效果"卡片：
    - useQuery 调 `reconciliationApi.baseline.compare()`（queryKey 用 `queryKeys.reconciliation.baselineCompare()`）
    - 渲染 3 个 Statistic：异常下降% / 工单下降% / Critical 下降%，colored（>=60% 绿色达标 + Tag"达标"，<60% 橙色）
    - 加 fallback：compare 返回 400（无基线）时显示 Alert "请先到例外规则管理页记录基线"（BLOCKER-3 可观察条件：无 baseline 显示引导提示）
    - **useEffect 稳定性**：useQuery enabled=true 直接拉，依赖用 queryKey 不用内联对象

    **确认 `exception-rules/index.tsx`**（44-01 Task 6b 已 wire）：admin 页顶部"记录当前为基线"按钮 onClick 调 `reconciliationApi.baseline.snapshot()` + useMutation invalidate `queryKeys.reconciliation.baselineCompare()` + 成功 message "基线已记录"（44-01 Task 6b 已实现 mutation 接入，本 task 仅 grep 验证存在，不重构）。

    **不要**：改 cron（Task 1）；改 Excel（Task 4）；改 44-01 已完成的 CRUD handler；删除 44-01 任何 handler。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go test ./internal/api/v1/asset/ -run 'TestSnapshotBaselineHandler|TestCompareBaselineHandler' -count=1 -v && cd xingran-react-frontend && npm run type-check && npm run build</automated>
  </verify>
  <done>
    - handler 含 `func (h *ReconciliationExceptionHandler) SnapshotBaseline(` + `CompareBaseline(`（BLOCKER-2：新增，非复用占位）
    - ReconciliationExceptionHandler struct 含 `baselineSvc asset.ReconciliationBaselineService` 字段
    - SnapshotBaseline 含 `operlog.Record(..., operlog.OperTypeUpdate)`；CompareBaseline 不含 operlog（grep 验证 CompareBaseline 函数体无 operlog.Record）
    - router 含 `/baseline/snapshot` + `/baseline/compare` 路由 + baselineSvc 构造注入
    - dashboard 含"降噪效果"卡片 + 3 个 Statistic（异常/工单/Critical 下降%）+ 无 baseline 时 Alert 引导提示
    - exception-rules admin 页"记录基线"按钮调 baseline.snapshot + invalidate baselineCompare + message（grep 验证 44-01 Task 6b 已实现，本 task 不重构）
    - 前端 type-check + build 退出 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Excel 导入导出例外规则（专用 ImportRules handler + 后处理 UPDATE scope_id + TEXT[] 转换）</name>
  <files>
    internal/services/operations/excel_config.go,
    internal/services/operations/excel_service.go,
    internal/services/operations/excel_service_test.go,
    internal/services/asset/reconciliation_exception.go,
    internal/api/v1/asset/reconciliation_exception_handler.go,
    internal/api/v1/asset/reconciliation_exception_router.go
  </files>
  <read_first>
    - internal/services/operations/excel_config.go:50-297（ExcelConfigs map + building/floor/workstation 条目，Columns/Reference/UpsertKey/DBField 模式）
    - internal/services/operations/excel_service.go **完整文件**（WARN-7：ImportData + GenerateTemplate + AffectedKeys 返回模式 + validateAndParseRow + 后处理钩子位置——必须完整读取确认现有架构是否支持 post-process hook，以决定方案 B 的接入点）
    - internal/services/operations/reference_resolver.go:29-100,298-322（ResolveSingle/Resolve 名称→UUID 解析）
    - internal/services/asset/reconciliation_exception.go（44-01 Task 3 产出的 model 字段 + Create service）
    - internal/models/reconciliation.go:76-108（SysReconciliationException model，确认 conflict_types/exception_actions 是 TEXT[] + scope_type/scope_id/expires_at 字段）
    - internal/api/v1/asset/reconciliation_handler.go 或 router.go（确认 Excel import/export/template 路由是否已注册——参照项目记忆 `xingran-excel-import-route-conflict`：不要在 router.go 预注册 /asset/reconciliation/import，由 SetupReconciliationExceptionRouter 自管或复用全局 ExcelRouter）
    - 44-RESEARCH.md §Code Examples :820-852（ExcelConfig verbatim）+ §Open Questions A3/A4（TEXT[] + 条件 Reference 两个 OPEN）+ §Common Pitfalls Pitfall 2/3（列错位 + UpsertKey 漏 DBField）
    - 44-PATTERNS.md §8（Excel 配置 + 3 项目记忆强约束）
    - 44-CONTEXT.md D-R3-A4-02（Excel 字段映射）
    - 项目记忆 MEMORY.md `xingran-excel-import-column-position-matching` / `xingran-excel-import-upsertkey-needs-dbfield` / `xingran-excel-import-route-conflict`
  </read_first>
  <behavior>
    - TestReconciliationExceptionExcelConfigExists：excel_config.go ExcelConfigs map 含 key "reconciliationExceptionRule"，Columns 长度=9，含 SheetName "对账例外规则" + TableName "sys_reconciliation_exception"
    - TestReconciliationExceptionExcelColumnOrder：Columns 顺序严格 = name/ip_range/conflict_types/exception_actions/severity_override/scope_type/scope_name/expires_at/reason（项目记忆 xingran-excel-import-column-position-matching）
    - TestReconciliationExceptionExcelUpsertKeyHasDBField：name 列 UpsertKey=true 且 DBField="name"（项目记忆 xingran-excel-import-upsertkey-needs-dbfield）
    - TestImportReconciliationExceptionBasicFields：导入 Excel 含一行合法数据（name/ip_range/severity_override/scope_type/expires_at/reason），调用 ImportRules 后 sys_reconciliation_exception 新增该行 + 字段值正确
    - TestImportReconciliationExceptionScopeNameResolution：scope_type=dept + scope_name="研发部" → post-process 后 scope_id = sys_dept 中 dept_name="研发部" 的 id；scope_type=user + scope_name="admin" → scope_id = sys_user.username="admin" 的 id；scope_type=global + scope_name="" → scope_id 保持 NULL
    - TestImportReconciliationExceptionTextArrayConversion：conflict_types="B,C,D" + exception_actions="no_alert,no_notice" → DB conflict_types=['B','C','D'] + exception_actions=['no_alert','no_notice']（验证 TEXT[] 转换，A3 OPEN QUESTION 解决方案）
    - TestExportReconciliationException：导出生成的 Excel 含 header 行 + 数据行，字段映射与导入一致
    - TestImportReconciliationExceptionInvalidCIDRRejected：ip_range="999.999.0.0/16" → 导入该行失败（service 层 ValidateCIDR 或 DB cidr 列兜底）
  </behavior>
  <action>
    先扩展 `excel_service_test.go`（RED）。

    **修改 `excel_config.go`**：加 `reconciliationExceptionRule` 条目（44-RESEARCH.md:823-852 + 44-PATTERNS.md §8 verbatim，**列顺序严格**）：
    ```
    "reconciliationExceptionRule": {
        SheetName:     "对账例外规则",
        TableName:     "sys_reconciliation_exception",
        EntityName:    "例外规则",
        HasHeader:     true,
        StartRow:      2,
        CachePatterns: []string{"reconciliation:*"},
        UniqueKeys:    []string{"name"},
        PartialUpdate: true,
        Columns: []ExcelColumn{
            {Field: "name", Header: "规则名称", Required: true, MaxLength: 128, UpsertKey: true, DBField: "name"},
            {Field: "ipRange", Header: "IP段(CIDR)", Required: true, DBField: "ip_range"},
            {Field: "conflictTypes", Header: "冲突类型(逗号分隔B/C/D/E/F,空=全部)", DBField: "conflict_types"},
            {Field: "exceptionActions", Header: "动作(逗号分隔no_alert/no_notice/no_workorder/skip_severity/silence)", Required: true, DBField: "exception_actions"},
            {Field: "severityOverride", Header: "严重度覆盖(low/medium/high,可空)", DBField: "severity_override"},
            {Field: "scopeType", Header: "范围(global/dept/user)", DBField: "scope_type"},
            {Field: "scopeName", Header: "范围名称(部门名/用户名,global留空)"},  // 无 DBField，临时字段
            {Field: "expiresAt", Header: "过期时间(可空,YYYY-MM-DD)", DBField: "expires_at"},
            {Field: "reason", Header: "原因(≥10字符)", Required: true, DBField: "reason"},
        },
    }
    ```
    **3 项目记忆强约束**：
    1. xingran-excel-import-column-position-matching：Columns 顺序严格 = Excel 模板列序（GenerateTemplate 按此顺序生成模板，运维按此填）
    2. xingran-excel-import-upsertkey-needs-dbfield：name 列 UpsertKey=true **必须**配 DBField="name"
    3. xingran-excel-import-route-conflict：不在 router.go 预注册 /asset/reconciliation/* import/export/template，由 SetupReconciliationExceptionRouter 自管（专用 ImportRules handler）

    **解决 OPEN QUESTION A3/A4（WARN-7：锁定方案 B 专用 ImportRules handler + 后处理 UPDATE scope_id）**：

    **方案 B（锁定，非 executor discretion）**：在 `reconciliation_exception.go` service 加专用导入方法 + 在 handler 加专用 ImportRules handler，调完标准 ImportData 后逐条 SELECT name + scope_name + scope_type，按 scope_type 解析 scope_id + UPDATE scope_id + TEXT[] 转换。

    A4 scope_name 条件 Reference（HIGH risk，方案 B 后处理）：
    - 在 `reconciliation_exception.go` service 加方法：
      ```go
      // ImportFromExcel 调 excel_service.ImportData 拿 AffectedKeys → 逐条 SELECT + ResolveScopeID + UPDATE scope_id
      func (s *reconciliationExceptionServiceImpl) ImportFromExcel(ctx context.Context, file *multipart.FileHeader) (*ImportResult, error)
      ```
    - 实现：(1) 调 `excel_service.ImportData(file, "reconciliationExceptionRule")` 拿 AffectedKeys（导入的 name 列表）；(2) 逐条 `SELECT id, scope_type, scope_name FROM sys_reconciliation_exception WHERE name = ? AND deleted_at IS NULL`（scope_name 是临时字段无 DBField，**必须**在 ImportData 后立即读 raw Excel 行或在 model 加 `ScopeName string `gorm:"-"`` 临时字段读出——executor 在 read_first 完整读 excel_service.go 后确认 AffectedKeys 是否携带 raw row，若不携带则在 model 加 gorm:"-" 临时字段 + ImportData 后 SELECT raw Excel 解析）；(3) 按 scope_type 解析：dept→`SELECT id FROM sys_dept WHERE dept_name = ? AND deleted_at IS NULL` / user→`SELECT id FROM sys_user WHERE username = ? AND deleted_at IS NULL` / global→NULL；(4) `UPDATE sys_reconciliation_exception SET scope_id = ? WHERE id = ?`
    - 用 GORM 占位符 `Where("dept_name = ?", scopeName)`，禁止字符串拼接（T-44-10）

    A3 TEXT[] 转换（MEDIUM risk）：
    - 在方案 B 的 ImportFromExcel 后处理中，对 conflict_types/exception_actions 列做 TEXT[] 转换：从 raw Excel 字符串 "B,C,D" split 逗号 → pq.StringArray{"B","C","D"} → `UPDATE sys_reconciliation_exception SET conflict_types = ?, exception_actions = ? WHERE id = ?`
    - 若 excel_service 原生支持 TEXT[] 列（DBField 指向 text[] 列时自动 split），则无需后处理，executor 在 read_first 完整读 excel_service.go 的 validateAndParseRow 后确认；若不支持则用上述后处理 UPDATE

    **专用 ImportRules handler**（在 reconciliation_exception_handler.go 加）：
    ```go
    func (h *ReconciliationExceptionHandler) ImportRules(c *gin.Context) {
        file, err := c.FormFile("file")
        // ... 错误处理
        result, err := h.service.ImportFromExcel(c.Request.Context(), file)
        // ... 错误处理
        operlog.Record(c, h.core.OperLogService, h.core.GetDB(), ModuleReconciliationExceptionRule, operlog.OperTypeImport)
        response.Success(c, result)
    }
    // ExportRules / DownloadTemplate 类似，调 excel_service.GenerateTemplate + Export
    ```

    **导出**：复用 excel_service.GenerateTemplate + Export（参照 building/workstation 模式），按同字段映射导出。

    **路由注册**（方案 B 专路由，避免项目记忆 xingran-excel-import-route-conflict）：在 SetupReconciliationExceptionRouter 加：
    ```
    r.POST("/exception-rule/import", middleware.RequirePermissions([]string{"asset:reconciliation:exception:create"}, core), handler.ImportRules)
    r.POST("/exception-rule/export", handler.ExportRules)
    r.POST("/exception-rule/template", handler.DownloadTemplate)
    ```
    ImportRules handler 调 service.ImportFromExcel + operlog.Record(OperTypeImport)。

    **不要**：改 cron（Task 1）；改 baseline（Task 2/3）；在 router.go 预注册全局 /asset/reconciliation/import（项目记忆强约束）；把方案选择权留给 executor（WARN-7 已锁定方案 B）。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go test ./internal/services/operations/ ./internal/services/asset/ -run 'TestReconciliationException' -count=1 -v && go test ./internal/api/v1/asset/ -run 'TestImportRules' -count=1 -v && go build ./...</automated>
  </verify>
  <done>
    - excel_config.go 含 key "reconciliationExceptionRule"（grep 验证）
    - Columns 顺序严格 = name/ip_range/conflict_types/exception_actions/severity_override/scope_type/scope_name/expires_at/reason
    - name 列 UpsertKey=true + DBField="name"
    - reconciliation_exception.go service 含 `func (s *reconciliationExceptionServiceImpl) ImportFromExcel(`（方案 B 锁定，WARN-7）
    - ImportFromExcel 实现含 scope_name → scope_id 按 scope_type 条件解析的 post-process 代码（grep 验证 scope_id 解析逻辑存在：`ResolveScopeID` 或 `UPDATE sys_reconciliation_exception SET scope_id`）
    - handler 含 `func (h *ReconciliationExceptionHandler) ImportRules(` + ExportRules + DownloadTemplate
    - ImportRules 含 `operlog.Record(..., operlog.OperTypeImport)`
    - router 含 `/exception-rule/import` + `/export` + `/template` 路由
    - A3 TEXT[] 转换：conflict_types/exception_actions TEXT[] 转换在导入后正确写入 DB（测试 TestImportReconciliationExceptionTextArrayConversion 通过）
    - 路由不冲突（启动 panic "handlers already registered" 不出现）
    - 8 个 test case 全通过 + go build ./... 退出 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron scheduler → sys_reconciliation_exception (is_active 字段) | 过期清理 cron 写路径，误硬删除会断外键链 |
| cron scheduler → sys_data_reconciliation (转单 SQL 读 applied_actions) | 转单过滤依赖 Layer 3.5 写入的 applied_actions 数据正确性；applied_actions=NULL 漏转风险（BLOCKER-4） |
| admin 用户 → `/asset/reconciliation/baseline/snapshot` | 基线快照写入 sys_config，覆盖现有基线（写操作） |
| admin 用户 → `/asset/reconciliation/exception-rule/import` | Excel 上传是文件输入，scope_name 解析走 SQL 查询，注入风险点 |
| 运维 Excel 文件 → sys_reconciliation_exception (upsert) | Excel 内容是外部输入，CIDR/actions 校验必须在 service 层 enforce |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-07 | Repudiation (审计链断裂) | cleanupExpiredExceptions 硬删除风险 | mitigate | 软停用 `UPDATE is_active=1` 不删记录（Task 1 TestCleanupExpiredExceptions 验证 deleted_at 仍 NULL）；D-R3-A4-03 锁定；Pitfall 4 |
| T-44-08 | Tampering (转单过滤数据依赖 + NULL 漏转) | createWorkorderCritical/High SQL 读 applied_actions | mitigate | Layer 3.5（44-01 Task 4）单一真相源写入 applied_actions；转单 SQL 仅读不写；**强制 IS NULL 兜底**（BLOCKER-4）`AND (applied_actions IS NULL OR 'no_workorder' != ANY(applied_actions))` 防 NULL 漏转；D-R3-A1-02 单一真相源原则；TestCreateWorkorderNoWorkorderFilterNullActions 锁定 |
| T-44-09 | Tampering (Excel CIDR 注入) | excel import → service 层 ValidateCIDR | mitigate | Excel 导入复用 44-01 Task 3 的 service 层 Create（含 ValidateCIDR），或 DB cidr 列兜底 SQLSTATE 22P02；非法 CIDR 行级拒绝（TestImportReconciliationExceptionInvalidCIDRRejected） |
| T-44-10 | Tampering (Excel SQL 注入) | scope_name 解析的 SQL 查询 | mitigate | 用 GORM 占位符 `Where("dept_name = ?", scopeName)`，禁止字符串拼接；参照 building 导入 ReferenceResolver 模式 |
| T-44-11 | Spoofing (基线覆盖) | SnapshotBaseline 覆盖现有 baseline | accept | 基线是运维主动触发的快照点，覆盖是有意行为；operlog.Record 留痕（Task 3 SnapshotBaselineHandler 调 OperTypeUpdate）；admin 权限限定 |
| T-44-12 | Information Disclosure (Compare 无 baseline) | CompareBaseline 返回 400 | accept | 错误信息"未找到基线"不泄露敏感数据；前端 fallback 显示引导提示（BLOCKER-3 可观察条件） |
| T-44-SC | Tampering (依赖) | npm/pip/cargo install | mitigate | R3 零新增依赖（44-RESEARCH.md §Package Legitimacy Audit 确认） |
</threat_model>

<verification>
## Plan-Level Checks

- [ ] `go build ./...` 退出 0（CLAUDE.md 强制）
- [ ] `go test ./internal/services/asset/... ./internal/api/v1/asset/... ./internal/scheduler/... ./internal/services/operations/... -count=1 -short` 全通过
- [ ] `cd xingran-react-frontend && npm run type-check && npm run build` 退出 0
- [ ] reconciliation_tasks.go cleanupExpiredExceptions case 含 `Update("is_active", 1)` + 不含 placeholder 字符串
- [ ] reconciliation_tasks.go createWorkorderBySeverity SQL 含 `(applied_actions IS NULL OR 'no_workorder' != ANY(applied_actions))`（BLOCKER-4 IS NULL 兜底）
- [ ] reconciliation_baseline.go 含 `Snapshot(` / `Compare(` + 用 `.Count(&` 独立 COUNT，不含 ListExceptions；COUNT 不加 silence 过滤（WARN-8）
- [ ] reconciliation_baseline.go 文件头/Snapshot doc 含运维文档化注释（R3 部署前记录 R2 基线，BLOCKER-3）
- [ ] handler 含 `SnapshotBaseline(` / `CompareBaseline(`（新增，非复用占位，BLOCKER-2），前者调 operlog.Record 后者不调
- [ ] ReconciliationExceptionHandler struct 含 `baselineSvc` 字段
- [ ] router 含 `/baseline/snapshot` + `/baseline/compare` 路由 + baselineSvc 构造注入
- [ ] excel_config.go 含 "reconciliationExceptionRule" 条目 + Columns 顺序严格 + name 列 UpsertKey=true+DBField="name"
- [ ] reconciliation_exception.go service 含 `ImportFromExcel(`（方案 B，WARN-7）+ scope_name→scope_id 解析逻辑
- [ ] handler 含 `ImportRules(` + ExportRules + DownloadTemplate
- [ ] dashboard 含"降噪效果"卡片 + 3 个下降% Statistic + 无 baseline Alert 引导提示
- [ ] A3/A4 OPEN QUESTION 解决方案在测试中验证（TEXT[] + scope_name 解析）

## Manual-Only

- 降噪效果卡片渲染（需真实告警数据对比）
- Excel 导入交互（下载模板 → 填数据 → 上传 → 校验列表刷新）
- 跨夜 cron 自动停用过期规则（需等 03:00 或手动触发 CleanupNow）

## Phase-Gate（44-01 + 44-02 合并验证）

- [ ] `go test ./...` 全套通过
- [ ] `cd xingran-react-frontend && npm run test && npm run build` 全套通过
- [ ] `go build ./...` 退出 0
- [ ] SC 1-10 全部 10 条 success criteria 在 UAT 中走通
- [ ] **SC 8 ≥60% 降噪量化验证 — 前置条件（BLOCKER-3）**：
  - (i) 运维必须在 R3 部署前 + R2 数据保留期内调用 Snapshot 记录 R2 末期基线（manual-only，无 baseline 则 SC 8 不可量化）
  - (ii) R3 例外规则生效后，运维在 dashboard 触发 compare，校验下降% ≥60%
  - (iii) 无 baseline 时 dashboard 显示引导提示（不报错）
- [ ] `go test ./internal/utils/operlog/ -count=1` 通过（25 OperType + **18** mandatorySensitiveKeywords 守护，WARN-11）
</verification>

<success_criteria>
本 plan 覆盖 ROADMAP Phase 44 success criteria：
- SC 5（有效期自动停用 cron 每日清理）→ Task 1 cleanupExpiredExceptions 软停用 + sys_job cron 0 0 3 * * *
- SC 8（告警量下降 ≥60%）→ Task 2 baseline service + Task 3 dashboard 降噪卡片（独立 COUNT + 基线对比；前置：运维记录 R2 末期基线，BLOCKER-3）
- SC 9（Excel 导入导出）→ Task 4 reconciliationExceptionRule ExcelConfig + 专用 ImportRules handler + scope_name 后处理 UPDATE scope_id + TEXT[] 转换（方案 B，WARN-7）

EXCEPTION-03（有效期自动停用）→ Task 1
EXCEPTION-02 转单通路（no_workorder 过滤 + IS NULL 兜底）→ Task 1（BLOCKER-4）
EXCEPTION-02 Excel 通路（5 actions + scope 双条件）→ Task 4
AUDIT-01（Excel import / baseline snapshot operlog）→ Task 3/4

本 plan 依赖 44-01 产出：CRUD service / module 常量 / assetApi baseline wire / queryKeys baselineCompare key / silence 过滤。
</success_criteria>

<output>
Create `.planning/phases/44-ip-r3/44-02-SUMMARY.md` when done.
</output>
