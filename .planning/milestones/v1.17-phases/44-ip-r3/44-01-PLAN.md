---
phase: 44-ip-r3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/core/db/migrations/migration_174_reconciliation_exception_gist.go
  - internal/core/db/database.go
  - internal/services/asset/reconciliation_exception_matcher.go
  - internal/services/asset/reconciliation_exception_matcher_test.go
  - internal/services/asset/reconciliation_exception.go
  - internal/services/asset/reconciliation_exception_test.go
  - internal/services/asset/reconciliation_detection.go
  - internal/services/asset/reconciliation_detection_test.go
  - internal/services/asset/reconciliation_service.go
  - internal/services/asset/reconciliation_service_test.go
  - internal/api/v1/asset/reconciliation_exception_handler.go
  - internal/api/v1/asset/reconciliation_exception_handler_test.go
  - internal/api/v1/asset/reconciliation_exception_router.go
  - internal/api/v1/asset/reconciliation_handler.go
  - xingran-react-frontend/src/lib/assetApi.ts
  - xingran-react-frontend/src/lib/queryKeys.ts
  - xingran-react-frontend/src/pages/asset/reconciliation/exception-rules/index.tsx
  - xingran-react-frontend/src/pages/asset/reconciliation/exceptions/index.tsx
  - xingran-react-frontend/src/components/asset/reconciliation/ExceptionRuleForm.tsx
  - xingran-react-frontend/src/components/asset/reconciliation/MatchTestPanel.tsx
autonomous: true
requirements:
  - EXCEPTION-01
  - EXCEPTION-02
  - EXCEPTION-04
  - AUDIT-01

must_haves:
  truths:
    - "SC 1: 运维可输入 IPv4/IPv6 CIDR（如 192.168.0.0/16）创建例外规则，非法 CIDR 被 service 层 ValidateCIDR 拒绝"
    - "SC 1: migration_174 在 PG 上创建 GiST inet_ops 索引 idx_recon_exc_active_range"
    - "SC 2: 运维可在 admin 页对例外规则做 创建/更新/启用/停用/删除，每个写操作在 response.Success 前调 operlog.Record"
    - "SC 3: 创建规则时 exception_actions 仅接受 5 个白名单值（no_alert/no_notice/no_workorder/skip_severity/silence），DB CHECK chk_recon_exc_actions 兜底"
    - "SC 4: 命中测试工具输入 IP 后返回顶部合并卡片（actions 并集 + finalSeverity + isSilence）+ 命中规则列表"
    - "SC 6: 命中测试端点 POST /asset/reconciliation/exception-rule/test 返回 MatchTestResult"
    - "SC 7: 异常列表默认隐藏 silence 记录（SQL WHERE NOT ('silence' = ANY(applied_actions))），ShowSilenced=true 可见"
    - "SC 10: DetectLayer3 循环命中例外时仍 INSERT sys_data_reconciliation，且 exception_rule_id + applied_actions 非空"
    - "EXCEPTION-02: 多规则命中同一 (asset, conflict_type) 时 mergeActions 取并集；skip_severity 降一级；severity_override 取最低（最宽松）"
  artifacts:
    - path: "internal/core/db/migrations/migration_174_reconciliation_exception_gist.go"
      provides: "GiST inet_ops 索引 + 2 CHECK 约束（纯 SQL DO$$ 幂等）"
      contains: "idx_recon_exc_active_range"
    - path: "internal/services/asset/reconciliation_exception_matcher.go"
      provides: "compiledRule struct + preloadActiveRules + matchException + mergeActions + applySkipSeverity 纯函数"
      exports: ["matchException", "mergeActions", "applySkipSeverity", "preloadActiveRules"]
    - path: "internal/services/asset/reconciliation_exception.go"
      provides: "ReconciliationExceptionService 扩展 Create/Update/Delete/MatchTest + ValidateCIDR/ValidateActions/ValidateSeverityOverride/ValidateReason"
      contains: "func (s *reconciliationExceptionServiceImpl) MatchTest"
    - path: "internal/api/v1/asset/reconciliation_exception_handler.go"
      provides: "CreateRule/UpdateRule/DeleteRule/TestRule handler，全部写操作调 operlog.Record"
      contains: "ModuleReconciliationExceptionRule"
    - path: "xingran-react-frontend/src/lib/queryKeys.ts"
      provides: "reconciliation 命名空间补 ruleList/ruleDetail/matchTest/baselineCompare 4 key（当前仅含 summary/byConflictType/bySeverity/healthTrend/topUnresolved/exceptionList/exceptionDetail/ruleStats）"
      contains: "matchTest"
    - path: "xingran-react-frontend/src/pages/asset/reconciliation/exception-rules/index.tsx"
      provides: "例外规则 admin 页（统计卡片 + 列表 + Modal CRUD + 命中测试入口）"
  key_links:
    - from: "internal/services/asset/reconciliation_detection.go"
      to: "internal/services/asset/reconciliation_exception_matcher.go"
      via: "DetectLayer3 循环内调用 matchException"
      pattern: "matchException\\(activeRules"
    - from: "internal/api/v1/asset/reconciliation_exception_handler.go"
      to: "internal/utils/operlog"
      via: "success path 末尾 operlog.Record"
      pattern: "operlog\\.Record\\(.*ModuleReconciliationExceptionRule"
    - from: "internal/services/asset/reconciliation_exception.go"
      to: "sys_reconciliation_exception (GiST inet_ops)"
      via: "MatchTest 用 GORM 占位符 Where(\"ip_range >> ?::inet\", ip)"
      pattern: "ip_range >> \\?::inet"
---

> **PLAN NOTE (WARN-9 决策 — 不拆 a/b)：** 本 plan 含 6 个 task / 19 文件，触发 scope_sanity 阈值。决策**不拆**为 44-01a/44-01b，理由：(1) 6 task 形成清晰垂直切片链（migration → matcher → service → detection → handler → 前端），任一中间拆点都会切断同一 feature 的层叠依赖，强行拆 a/b 后 b 几乎所有 task 依赖 a 全部产出，并行收益为零；(2) BLOCKER-1 已要求把 Task 6 拆为 6a/6b/6c 三个子任务（基建 / admin 页 / MatchTestPanel+silence 开关），把前端复杂度内化到单 task 内分解，已是最细粒度；(3) execute-phase 可承载 6 task 单 plan，每个 task 严格 tdd + 独立 verify，context 预算 ≈ 45-50%（前端 3 子任务共消耗约 25%，后端 3 task 约 20-25%），未超 50%。保留单 plan 不改 ROADMAP Phase 44 plan 数（仍为 2），不触发 REQUIREMENTS Traceability / 44-VALIDATION.md Plan 列同步。

<objective>
落地 R3 核心：CIDR 例外规则引擎（migration_174 GiST 索引 + 纯函数匹配器）+ Layer 3.5 拦截写入 exception_rule_id/applied_actions + 例外规则 CRUD API（含 operlog 强制约定）+ admin 管理页 + 命中测试工具 + 异常列表 silence 默认过滤。

Purpose: 把 v1.17 资产对账的告警通路接入 IP 段例外规则引擎，让运维用 CIDR 批量豁免低风险网段，为 44-02 的 ≥60% 降噪验证提供匹配基建（覆盖 SC 1/2/3/4/6/7/10 + EXCEPTION-01/02/04 + AUDIT-01）。
Output: 1 个 migration + 1 个新 matcher service + 4 个修改的 service/scheduler/handler/router 文件 + 1 个新 module 常量 + 6 个前端文件（含 queryKeys.ts 4 key 扩展）+ 8 个测试桩。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-ip-r3/44-CONTEXT.md
@.planning/phases/44-ip-r3/44-RESEARCH.md
@.planning/phases/44-ip-r3/44-PATTERNS.md
@.planning/phases/44-ip-r3/44-VALIDATION.md
@.planning/phases/42-r1/42-CONTEXT.md
@.planning/phases/43-r2/43-CONTEXT.md

<interfaces>
<!-- 关键 model + 现有 skeleton 接口。Executor 直接用，无需 codebase 探索。 -->

From internal/models/reconciliation.go:44-50（R3 写入点字段已存在）:
```go
AssetIP          *string        `gorm:"type:inet;column:asset_ip"`
ExceptionRuleID  *string        `gorm:"type:uuid;column:exception_rule_id"`
AppliedActions   pq.StringArray `gorm:"type:text[];column:applied_actions"`  // ⚠ 无 default tag → INSERT 默认 NULL（44-02 BLOCKER-4 关联）
```

From internal/models/reconciliation.go:76-108（例外规则 model，字段全在，缺 GiST+CHECK）:
```go
type SysReconciliationException struct {
    BaseModel
    Name             string        `gorm:"size:128;not null;column:name"`
    IPRange          string        `gorm:"type:cidr;not null;column:ip_range"`           // 不加 gorm:"index"
    ConflictTypes    pq.StringArray `gorm:"type:text[];column:conflict_types"`
    ExceptionActions pq.StringArray `gorm:"type:text[];not null;column:exception_actions"`
    SeverityOverride *string       `gorm:"size:16;column:severity_override"`            // 白名单 low/medium/high（不含 critical）
    ScopeType        string        `gorm:"size:16;not null;default:'global';column:scope_type"`
    ScopeID          *string       `gorm:"type:uuid;column:scope_id"`
    Reason           string        `gorm:"type:text;not null;column:reason"`            // service 层 enforce ≥10 字符
    IsActive         int           `gorm:"default:0;column:is_active"`                  // Status Convention: 0=启用,1=停用
    ExpiresAt        *time.Time    `gorm:"column:expires_at"`
    CreatedBy        int64         `gorm:"not null;column:created_by"`
}
```

From internal/services/asset/reconciliation_exception.go:48-68（现有 R1 skeleton，R3 扩展）:
```go
type ReconciliationExceptionService interface {
    List(ctx context.Context, params *ExceptionRuleListParams) (*base.PageResult, error)
    GetByID(ctx context.Context, id string) (*models.SysReconciliationException, error)
}
type reconciliationExceptionServiceImpl struct { db *gorm.DB }
func NewReconciliationExceptionService(db *gorm.DB) ReconciliationExceptionService
```

From internal/api/v1/asset/reconciliation_exception_handler.go:17-31（现有 WithCore 模式）:
```go
type ReconciliationExceptionHandler struct {
    service asset.ReconciliationExceptionService
    core    *core.Core
}
func (h *ReconciliationExceptionHandler) WithCore(core *core.Core) *ReconciliationExceptionHandler
```

From internal/middleware/apikey.go:110-141（CIDR 匹配现成模式，复用）:
```go
_, ipNet, err := net.ParseCIDR(allowed)
if ipNet.Contains(ip) { return true }
```

From internal/services/asset/cache_keys.go:33-37（INFRA-04 已就位）:
```go
const CacheKeyReconciliationExceptionRuleList = "reconciliation:exceptionRule:list"
func GetReconciliationExceptionRuleByIDKey(id string) string
```

From xingran-react-frontend/src/lib/queryKeys.ts:44-69（INFRA-05 reconciliation 命名空间现状 — Task 6a 扩展前）:
```ts
reconciliation: {
    all: ["reconciliation"] as const,
    summary: (windowDays: number) => ["reconciliation", "summary", windowDays] as const,
    byConflictType: (windowDays: number) => ["reconciliation", "by-conflict-type", windowDays] as const,
    bySeverity: (windowDays: number) => ["reconciliation", "by-severity", windowDays] as const,
    healthTrend: (windowDays: number) => ["reconciliation", "health-trend", windowDays] as const,
    topUnresolved: (limit: number) => ["reconciliation", "top-unresolved", limit] as const,
    exceptionList: (params: ExceptionListParams) => ["reconciliation", "exception-list", params] as const,
    exceptionDetail: (id: string) => ["reconciliation", "exception-detail", id] as const,
    ruleStats: () => ["reconciliation", "rule-stats"] as const,
    // ⚠ 缺 ruleList / ruleDetail / matchTest / baselineCompare — Task 6a 补
}
```

operlog 强约束（CLAUDE.md + regression_test.go:188-207 实测）:
```go
operlog.Record(c, h.core.OperLogService, h.core.GetDB(), ModuleReconciliationExceptionRule, operlog.OperTypeCreate)
// OperType 常量集 25 个（值固定），Create=1/Update=2/Delete=3/Enable=12/Disable=13
// mandatorySensitiveKeywords 实测 18 个（regression_test.go:188-207），CLAUDE.md "11 关键词" 已过时，新 module 常量不破坏（用 >= 断言）
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: migration_174 GiST 索引 + CHECK 约束 + 注册 AutoMigrate</name>
  <files>
    internal/core/db/migrations/migration_174_reconciliation_exception_gist.go,
    internal/core/db/database.go
  </files>
  <read_first>
    - internal/core/db/migrations/migration_168_reconciliation_tables.go:139-156（DO$$ + pg_indexes IF NOT EXISTS 模式，是 44-PATTERNS.md §1 analog 的 source-of-truth）
    - internal/core/db/database.go:455-483（migration 168-173 链式注册，参照追加位置）
    - internal/models/reconciliation.go:76-108（被索引表 model，确认列名 ip_range/is_active 与 Status Convention）
    - 44-RESEARCH.md §Code Examples :644-734（migration_174 完整 verbatim SQL）
    - 44-RESEARCH.md §Common Pitfalls Pitfall 1/6（GORM tag 禁用 + model 不加 gorm:"index"）
    - 项目记忆 MEMORY.md `xingran-gorm-sql-constraint-naming-conflict`、`xingran-migrations-no-sql-autoloader`
  </read_first>
  <behavior>
    - 在 PG 上执行 migration 后，`SELECT indexname FROM pg_indexes WHERE tablename='sys_reconciliation_exception'` 含 `idx_recon_exc_active_range`
    - `SELECT conname FROM pg_constraint WHERE conname LIKE 'chk_recon_exc_%'` 返回 `chk_recon_exc_actions` + `chk_recon_exc_severity_override` 两行
    - 重复执行 migration（应用二次启动）不报 SQLSTATE 42710（DO$$ IF NOT EXISTS 幂等）
    - 在 SQLite 上 migration 跳过（isPostgreSQL 返回 false → return nil）
    - INSERT exception_actions 含非法值（如 `['evil']`）报 SQLSTATE 23514
    - INSERT severity_override='critical' 报 SQLSTATE 23514（白名单仅 low/medium/high）
  </behavior>
  <action>
    创建 `migration_174_reconciliation_exception_gist.go`，函数名 `Migrate174ReconciliationExceptionGist(db *gorm.DB) error`。

    文件头先 `isPostgreSQL(db)` 判断（参照 migration_168 现有 isPostgreSQL helper），非 PG 直接 return nil。

    三段纯 SQL `DO $$ ... END$$`（参照 migration_168:139-156 模式 + 44-RESEARCH.md:644-725 verbatim，**不放完整代码块进 action，executor 从 44-RESEARCH.md:644-725 复制**）：

    1. **GiST inet_ops 索引**：查 `pg_indexes WHERE indexname='idx_recon_exc_active_range' AND schemaname='public'`，不存在则 `EXECUTE 'CREATE INDEX idx_recon_exc_active_range ON sys_reconciliation_exception USING gist (ip_range inet_ops) WHERE is_active = 0 AND deleted_at IS NULL'`。**注意 partial index WHERE 子句用 is_active=0（Status Convention: 0=启用，仅索引启用规则）**。

    2. **CHECK chk_recon_exc_actions**：查 `pg_constraint WHERE conname='chk_recon_exc_actions'`，不存在则 `EXECUTE 'ALTER TABLE sys_reconciliation_exception ADD CONSTRAINT chk_recon_exc_actions CHECK (exception_actions <@ ARRAY[''no_alert'',''no_notice'',''no_workorder'',''skip_severity'',''silence''])'`。PG `<@` = "是...的子集"。

    3. **CHECK chk_recon_exc_severity_override**：查 `pg_constraint WHERE conname='chk_recon_exc_severity_override'`，不存在则 `EXECUTE 'ALTER TABLE sys_reconciliation_exception ADD CONSTRAINT chk_recon_exc_severity_override CHECK (severity_override IS NULL OR severity_override IN (''low'',''medium'',''high''))'`。**不含 critical**（Pitfall 8）。

    每段失败用 `fmt.Errorf("创建 X 失败: %w", err)` 包装返回。

    在 `internal/core/db/database.go` 现有 `Migrate173` 调用之后（约 :483）追加：
    ```
    // Phase 44 R3: sys_reconciliation_exception GiST inet_ops 索引 + CHECK 约束
    if err := migrations.Migrate174ReconciliationExceptionGist(d.DB); err != nil {
        applogger.Errorf("Phase 44 R3 reconciliation GiST/CHECK 失败: %v", err)
    }
    ```
    失败用 `applogger.Errorf` 不 panic（参照 168-173 链式模式）。

    **强制约束（Pitfall 1/6 + A7）**：
    - 纯 SQL `DO$$`，**禁止** GORM `check:` tag
    - `CREATE INDEX` 用 `IF NOT EXISTS`（通过 DO$$ 包装）
    - model `IPRange` 字段保持 `gorm:"type:cidr;not null;column:ip_range"`，**不加** `gorm:"index"`（防 AutoMigrate 误建 btree 与 GiST 冲突）
    - migration 前若担心 Phase 42 R1 seed 数据含非法 actions（A7 假设），可在 migration_174 函数开头先跑一次 sanity SELECT，发现非法值则 logrus.Warnf 提示但不阻塞（CHECK 添加时 PG 自动校验，非法数据会 FATA 暴露问题）
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go build ./internal/core/db/... && go vet ./internal/core/db/migrations/migration_174_reconciliation_exception_gist.go</automated>
  </verify>
  <done>
    - 文件 `internal/core/db/migrations/migration_174_reconciliation_exception_gist.go` 存在，含 `func Migrate174ReconciliationExceptionGist(db *gorm.DB) error`
    - 文件含字符串 `idx_recon_exc_active_range` / `chk_recon_exc_actions` / `chk_recon_exc_severity_override` / `USING gist (ip_range inet_ops)` / `isPostgreSQL(db)`
    - `database.go` 含 `Migrate174ReconciliationExceptionGist(d.DB)` 调用，位于 `Migrate173` 之后
    - `go build ./internal/core/db/...` 退出 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 例外匹配纯函数（matchException/mergeActions/applySkipSeverity）+ 单测</name>
  <files>
    internal/services/asset/reconciliation_exception_matcher.go,
    internal/services/asset/reconciliation_exception_matcher_test.go
  </files>
  <read_first>
    - internal/middleware/apikey.go:110-141（`net.ParseCIDR` + `ipNet.Contains` 现成模式，是 44-PATTERNS.md §3 analog）
    - internal/models/reconciliation.go:76-108（SysReconciliationException 字段，确认 IPRange/ExceptionActions/SeverityOverride/ScopeType/ScopeID/ConflictTypes）
    - 44-RESEARCH.md §Pattern 1 :259-288 + §Pattern 2 :299-354（matchException + mergeActions + applySkipSeverity verbatim 代码）
    - 44-CONTEXT.md D-R3-A1-03 / A2-01 / A2-02 / A3-01 / A3-02（性能架构 / severity 取最低 / skip_severity 降级链 / dept-user 双条件 / 空 conflict_types 匹配全部）
  </read_first>
  <behavior>
    - TestApplySkipSeverity：critical→high / high→medium / medium→low / low→low（不再降）
    - TestMergeActions：2 规则各带 actions ['no_alert'] 与 ['no_notice','skip_severity']，原始 severity=critical → 合并后 actions 含 no_alert+no_notice+skip_severity（并集去重），finalSeverity=high（critical 降一级），isSilence=false
    - TestMergeActionsSeverityOverride：2 规则 override 分别 low/medium，原始 critical → finalSeverity=low（取最低，与 skip_severity 后再覆盖取更宽）
    - TestMergeActionsSilenceUnion：1 规则带 silence，isSilence=true
    - TestMatchExceptionSingleGlobalRule：1 条 global scope 规则 IP 192.168.1.0/24，资产 IP 192.168.1.10 命中，返回 ruleID 非空 + 该规则 actions
    - TestMatchExceptionCIDRBoundary：资产 IP 192.168.1.0（网络地址）和 192.168.1.255（广播地址）都命中 192.168.1.0/24
    - TestMatchExceptionNoMatch：资产 IP 10.0.0.1 不命中 192.168.1.0/24，返回空 ruleID + nil actions
    - TestMatchExceptionDeptScopeDoubleCondition：dept scope 规则 scope_id=deptA，资产 user_id 属 deptA 命中；资产 user_id 属 deptB 不命中
    - TestMatchExceptionEmptyConflictTypesMatchesAll：规则 ConflictTypes=nil，conflictType='B'/'C'/'D'/'E'/'F' 都命中；conflictType='A' 不命中（A 不入主表）
    - TestPreloadActiveRulesSkipsInvalidCIDR：DB 含一条非法 CIDR 规则（如 '999.999.999.999/32'），preloadActiveRules 跳过该规则不 panic，返回的切片不含它
    - TestMatchExceptionIPv6：规则 2001:db8::/32，资产 2001:db8::1 命中
  </behavior>
  <action>
    先建测试文件 `reconciliation_exception_matcher_test.go`（纯函数 unit 测试，无 DB），写上述 11 个 test case 失败（RED）。

    再建 `reconciliation_exception_matcher.go` 实现（GREEN），关键标识符：

    - `type compiledRule struct { rule models.SysReconciliationException; ipNet *net.IPNet }`
    - `var severityOrder = map[string]int{"low":0, "medium":1, "high":2, "critical":3}`（D-R3-A2-02）
    - `func applySkipSeverity(s string) string`：取 severityOrder[s]，<=0 返回 "low"，否则找 v==level-1 的 key 返回
    - `func preloadActiveRules(db *gorm.DB) []compiledRule`：`db.Where("is_active = ? AND deleted_at IS NULL", 0).Find(&rules)`，逐条 `net.ParseCIDR(r.IPRange)`，失败 `logrus.Warnf("[reconciliation] 例外规则 %s CIDR 解析失败(%s): %v", r.ID, r.IPRange, err)` continue，成功 append（D-R3-A1-03）
    - `func matchException(rules []compiledRule, assetIP string, assetUserID string, conflictType string) (matchedRuleID string, appliedActions pq.StringArray, finalSeverity string, isSilence bool)`：
      * step 1: `net.ParseIP(assetIP)`，nil 直接返回空
      * step 2: 遍历 rules，对每条：`r.ipNet.Contains(parsedIP)` 不匹配跳过；ConflictTypes 非空且不含 conflictType 跳过（A3-02 空数组匹配全部 B-F，但调用方传入的 conflictType 不会是 'A' 因为 A 不入主表）；ScopeType='dept'/'user' 时校验 assetUserID 与 r.rule.ScopeID 匹配（A3-01 双条件），global 直接放行
      * step 3: 收集所有命中规则到 matched 切片
      * step 4: 若 matched 为空返回空；否则记录 matchedRuleID = matched[0].rule.ID（首条命中），调用 mergeActions(originalSeverity, matched, conflictType) 算合并结果
    - `func mergeActions(originalSeverity string, matched []compiledRule, conflictType string) (pq.StringArray, string, bool)`：D-R3-A2-01/02 verbatim
      * step1 skip：遍历 matched 的 ExceptionActions，任一 == "skip_severity" 触发，触发后 `sev = applySkipSeverity(originalSeverity)`，否则 sev = originalSeverity
      * step2 override：遍历 matched 的 SeverityOverride，非 nil 时 `if severityOrder[ov] < severityOrder[sev] { sev = ov }`（取最低/最宽松）
      * step3 actions UNION：seen map 去重，遍历 matched ExceptionActions append
      * step4 isSilence：actions 中含 "silence" 则 true

    **导入**：`net` / `github.com/lib/pq` / `github.com/sirupsen/logrus` / `gorm.io/gorm` / `github.com/xingran-next/xingran-go-backend/internal/models`

    **注意**：matchedRuleID 取首条命中（审计指向触发规则），但 actions/severity 取所有命中的并集（D-R3-A2-01）。

    **不要**：在本 task 改 DetectLayer3 循环（Task 4 做）；不要实现 MatchTest（Task 3 做，MatchTest 走 GiST SQL 不是纯函数）。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go test ./internal/services/asset/ -run 'TestApplySkipSeverity|TestMergeActions|TestMatchException|TestPreloadActiveRules' -count=1 -v</automated>
  </verify>
  <done>
    - 文件含 `func applySkipSeverity(` / `func mergeActions(` / `func matchException(` / `func preloadActiveRules(` / `type compiledRule struct`
    - 11 个 test case 全部通过（go test 退出 0）
    - 测试覆盖 IPv4/IPv6/边界/dept-user 双条件/空 conflict_types/非法 CIDR 跳过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ReconciliationExceptionService 扩展 CRUD + MatchTest + 校验函数</name>
  <files>
    internal/services/asset/reconciliation_exception.go,
    internal/services/asset/reconciliation_exception_test.go
  </files>
  <read_first>
    - internal/services/asset/reconciliation_exception.go:1-146（现有 R1 skeleton，List/GetByID + ExceptionRuleListParams + impl struct + List 查询模式）
    - internal/services/asset/cache_keys.go:33-37（CacheKeyReconciliationExceptionRuleList + GetReconciliationExceptionRuleByIDKey，CRUD 写操作后失效缓存）
    - internal/services/asset/reconciliation_exception_matcher.go（Task 2 产出的 mergeActions 等纯函数，MatchTest 合并调用）
    - 44-RESEARCH.md §Pattern 3 :357-389 + §Code Examples :736-770（MatchTest GiST SQL verbatim）
    - 44-PATTERNS.md §4（service CRUD + 校验 + GiST SQL 模式）
    - 44-CONTEXT.md D-R3-A3-01/02/03（scope 双条件 / 空 conflict_types / 命中测试输入）
    - 44-RESEARCH.md §Common Pitfalls Pitfall 8（severity_override 白名单不含 critical）
    - 44-RESEARCH.md §Security Domain T-CIDR-Inject / T-SQLInject（CIDR 校验 + GORM 占位符）
  </read_first>
  <behavior>
    - TestValidateCIDR："192.168.0.0/16" / "2001:db8::/32" 通过；"999.999.0.0/16" / "192.168.1.10"（无掩码）/ "not-a-cidr" 返回 error
    - TestValidateActions：["no_alert","silence"] 通过；["evil"] 返回 error；[] 返回 error（必填）；nil 返回 error
    - TestValidateSeverityOverride：nil 通过；"low"/"medium"/"high" 通过；"critical" 返回 error；"evil" 返回 error
    - TestValidateReason：len>=10 通过；len<10 返回 error
    - TestCreateExceptionRule：mock DB 接收合法 req，返回 *SysDataReconciliation 含 IsActive=0（默认启用）+ CreatedBy 来自 ctx
    - TestUpdateExceptionRule：存在 id 更新成功；不存在返回 error
    - TestDeleteExceptionRule：软删除（deleted_at 被填），不硬删
    - TestMatchTestGlobalScope：DB 含 1 条 global 规则 IP 192.168.0.0/16 + actions ['no_alert']，调 MatchTest("192.168.0.10","","")，返回 matchedRules 含该规则 + needsUserDept=true + mergedActions=['no_alert']
    - TestMatchTestWithUserID：DB 含 user scope 规则 scope_id=userA，调 MatchTest(ip,"userA","")，该规则参与合并
    - TestMatchTestNeedsUserDept：未传 userID/deptID 时，user/dept scope 规则不参与，仅 global 命中
    - TestMatchTestExcludesInactiveRules：DB 含 1 条 is_active=1（停用）的规则 IP 命中测试 IP，MatchTest 不返回该规则（partial index WHERE is_active=0 + SQL WHERE is_active=? 双重保险，WARN-6）
  </behavior>
  <action>
    先扩展 `reconciliation_exception_test.go`（若不存在则参照 Phase 42 现有 reconciliation_*_test.go 模式新建），写上述测试失败（RED，CRUD 部分可用 sqlmock 或 sqlite 测试 DB，参照项目现有测试）。

    再修改 `reconciliation_exception.go`（GREEN）：

    **interface 扩展（参照 44-PATTERNS.md §4）**：
    - `Create(ctx context.Context, req *CreateExceptionRuleRequest) (*models.SysReconciliationException, error)`
    - `Update(ctx context.Context, id string, req *UpdateExceptionRuleRequest) error`
    - `Delete(ctx context.Context, id string) error`（软删除 `db.Delete`，填 deleted_at；与 cron 软停用 is_active=1 区分）
    - `MatchTest(ctx context.Context, ip string, userID string, deptID string) (*MatchTestResult, error)`
    - 可选 `CleanupNow(ctx context.Context) (int64, error)`（运维手动触发，44-02 cron 也复用）

    **Request struct**：`CreateExceptionRuleRequest` / `UpdateExceptionRuleRequest` 含字段 Name/IPRange/ConflictTypes/ExceptionActions/SeverityOverride/ScopeType/ScopeID/ExpiresAt/Reason（参照 model 字段，用 pq.StringArray 接 actions/conflict_types）。

    **校验函数（service 层 enforce，V5 Input Validation）**：
    - `ValidateCIDR(ipRange string) error`：`_, _, err := net.ParseCIDR(ipRange); if err != nil { return fmt.Errorf("CIDR 格式非法: %w", err) }`（T-CIDR-Inject 缓解）
    - `ValidateActions(actions pq.StringArray) error`：len==0 报错（必填）；逐个对照白名单 `["no_alert","no_notice","no_workorder","skip_severity","silence"]`，非法值报错（与 chk_recon_exc_actions 同步）
    - `ValidateSeverityOverride(sev *string) error`：nil 通过；非 nil 仅允许 `["low","medium","high"]`（**不含 critical**，Pitfall 8）
    - `ValidateReason(reason string) error`：utf8.RuneCountInString(reason) < 10 报错（告警风暴缓解）

    **Create 实现**：依次调 4 个 Validate；构造 model（IsActive=0 默认启用，CreatedBy 从 ctx 取或 0）；`db.Create`；写后调 `s.invalidateCache()`（用 cache_keys.go 的 CacheKeyReconciliationExceptionRuleList）。失败用 `fmt.Errorf("创建例外规则失败: %w", err)`。

    **Update 实现**：Validate（同上）；`db.Updates`；失效缓存。

    **Delete 实现**：`db.Where("id = ?", id).Delete(&models.SysReconciliationException{})`（GORM 软删除）；失效缓存。

    **MatchTest 实现（GiST SQL 单点查询，D-R3-A3-03 + A1-03）**：
    - 构造 `MatchTestResult struct { MatchedRules []models.SysReconciliationException; MergedActions pq.StringArray; FinalSeverity string; IsSilence bool; NeedsUserDept bool }`
    - global scope：`s.db.Where("ip_range >> ?::inet AND is_active = ? AND deleted_at IS NULL AND scope_type = ?", ip, 0, "global").Find(&globalMatched)`（**GORM 占位符 ?::inet 防 SQL 注入**，T-SQLInject；`is_active = 0` 双重保险过滤停用规则，与 partial index 一致 — WARN-6）
    - user scope：仅当 userID != "" 时 `Where("... AND scope_type = ? AND scope_id = ?", "user", userID).Find(&userMatched)` 并 append
    - dept scope：**R3 dept scope 不递归子部门，仅匹配 scope_id == deptID 直接部门**（WARN-10 锁定，D-R3-A3-01 planner discretion 决议：递归需 JOIN sys_dept.ancestors 增加复杂度且 R3 admin 页 dept Select 通常选具体部门，递归收益低于成本）。仅当 deptID != "" 时 `Where("... AND scope_type = ? AND scope_id = ?", "dept", deptID).Find(&deptMatched)` 并 append
    - needsUserDept = (userID == "" && deptID == "")
    - 调用 `mergeActions` 纯函数（Task 2）对 matched 切片合并，传入 originalSeverity（命中测试默认 "medium" 或由调用方指定，简化版固定 medium）
    - 返回 MatchTestResult

    **import 补**：`net` / `unicode/utf8` / `github.com/lib/pq` / cache 相关。

    **不要**：在本 task 改 handler/router（Task 5）；不改 DetectLayer3（Task 4）；不递归子部门（WARN-10）。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go test ./internal/services/asset/ -run 'TestValidateCIDR|TestValidateActions|TestValidateSeverityOverride|TestValidateReason|TestCreateExceptionRule|TestUpdateExceptionRule|TestDeleteExceptionRule|TestMatchTest' -count=1 -v</automated>
  </verify>
  <done>
    - interface 含 `Create(` / `Update(` / `Delete(` / `MatchTest(` 4 个新方法签名
    - service 含 `ValidateCIDR` / `ValidateActions` / `ValidateSeverityOverride` / `ValidateReason` / `invalidateCache` 函数
    - MatchTest 用 GORM 占位符 `Where("ip_range >> ?::inet ...` 不拼接字符串
    - MatchTest SQL 含 `is_active = ?` (=0) 过滤停用规则（grep 验证 `is_active = ?` 在 MatchTest 函数体内）
    - dept scope 分支仅匹配 `scope_id = ?` 直接部门，不含 ancestors 递归（grep 验证 MatchTest 函数体内不含 `ancestors`）
    - 所有 11 个 test case 通过（含 TestMatchTestExcludesInactiveRules）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: DetectLayer3 插入 Layer 3.5 例外匹配 + 异常列表 silence 默认过滤</name>
  <files>
    internal/services/asset/reconciliation_detection.go,
    internal/services/asset/reconciliation_detection_test.go,
    internal/services/asset/reconciliation_service.go,
    internal/services/asset/reconciliation_service_test.go
  </files>
  <read_first>
    - internal/services/asset/reconciliation_detection.go:191-332（DetectLayer3 完整循环，确认插入点 :262 前，guard 2 在 :258-261，INSERT 在 :305-316；row.AssetUserID 字段存在用于 matchException 入参）
    - internal/services/asset/reconciliation_exception_matcher.go（Task 2 产出的 preloadActiveRules + matchException）
    - internal/services/asset/reconciliation_service.go:296-301（ListExceptions 基础 query + ExceptionListParams struct）
    - 44-RESEARCH.md §Code Examples :607-641（Layer 3.5 插入 verbatim）+ :774-787（silence 过滤 verbatim）
    - 44-PATTERNS.md §5 + §13（DetectLayer3 改造 + exceptions/index.tsx silence 过滤）
    - 44-CONTEXT.md D-R3-A1-01（silence 仍写表但列表隐藏）/ D-R3-A1-02（Layer 3.5 单一真相源）
  </read_first>
  <behavior>
    - TestDetectLayer3ExceptionHit：mock 含 1 条 active global 规则 IP 192.168.0.0/16 actions=['no_alert']，资产 IP 192.168.0.10 触发检测，断言 INSERT 的 SysDataReconciliation.ExceptionRuleID 非空 + AppliedActions 含 'no_alert' + Severity 可能因 skip/override 降级
    - TestDetectLayer3NoExceptionNoChange：资产 IP 10.0.0.1 不命中任何规则，ExceptionRuleID=nil + AppliedActions=nil + Severity 保持原值
    - TestDetectLayer3SilenceStillWrites：规则 actions=['silence']，资产命中，断言仍 INSERT（D-R3-A1-01 silence 仍写表）
    - TestDetectLayer3SkipSeverityDegrades：规则 actions=['skip_severity']，原始 severity=critical，断言 INSERT 的 Severity=high（降一级）
    - TestDetectLayer3DeptScopeMatch：dept scope 规则 scope_id=deptA，资产 row.AssetUserID 指向 deptA 关联用户，IP 命中，断言 INSERT 的 ExceptionRuleID 非空 + AppliedActions 含该规则 actions（WARN-5：dept scope 端到端覆盖）
    - TestDetectLayer3DeptScopeNoMatch：dept scope 规则 scope_id=deptA，资产 row.AssetUserID 指向 deptB 关联用户（或 nil），IP 命中但 user 不匹配，断言 ExceptionRuleID=nil（WARN-5：dept scope 端到端反例覆盖）
    - TestListExceptionsSilenceFilter：DB 含 2 条异常 1 条 applied_actions=['silence'] + 1 条 nil，默认调用返回 1 条（silence 被过滤）
    - TestListExceptionsShowSilenced：同样数据 + ShowSilenced=true，返回 2 条
  </behavior>
  <action>
    先扩展测试文件（RED）。

    **修改 `reconciliation_detection.go`（GREEN，Layer 3.5 插入）**：

    - DetectLayer3 函数开头（rows 加载后约 :213 附近、循环开始前）调用一次：`activeRules := preloadActiveRules(s.db)`（preloadActiveRules 是 Task 2 的包级函数，直接调用；若 matcher 在同 package asset 则无需额外 import）
    - 循环内 guard 2 (24h 节流 :258-261) 之后、脏数据防御 (:263-271) 之前插入（44-RESEARCH.md:607-641 verbatim）：
      ```
      // === R3 / D-R3-A1-02 Layer 3.5: 例外规则匹配 ===
      var exceptionRuleID *string
      var appliedActions pq.StringArray
      matchedSeverity := severity
      if row.AssetIP != nil && *row.AssetIP != "" {
          assetUserID := ""
          if row.AssetUserID != nil { assetUserID = *row.AssetUserID }
          ruleID, actions, finalSev, _ := matchException(activeRules, *row.AssetIP, assetUserID, conflictType)
          if ruleID != "" {
              exceptionRuleID = &ruleID
              appliedActions = actions
              matchedSeverity = finalSev
          }
      }
      ```
    - 现有 rec 构造点（:305-316）填入新字段：`Severity: matchedSeverity`（替换原 `severity`）+ `ExceptionRuleID: exceptionRuleID` + `AppliedActions: appliedActions`
    - import 补 `"github.com/lib/pq"`（若 reconciliation_exception_matcher.go 在同 package，preloadActiveRules/matchException 无需 import）

    **返回值**：现有 `(int, int, int, int, error)` = (inserted, skipped, skippedSilence, skippedThrottle)。R3 命中例外的记录仍计入 inserted（D-R3-A1-01 silence 仍写表），**不改签名**。可选在 logrus.Infof 加 `exceptionHit` 计数（"Layer 3.5 命中例外 X 条"），不影响返回。

    **修改 `reconciliation_service.go`（GREEN，silence 默认过滤）**：

    - `ExceptionListParams` 加字段 `ShowSilenced bool `json:"showSilenced"``（默认 false）
    - `ListExceptions` 基础 query 追加（44-RESEARCH.md:774-787 verbatim）：
      ```
      if !params.ShowSilenced {
          query = query.Where("NOT ('silence' = ANY(sys_data_reconciliation.applied_actions))")
      }
      ```
      位置：现有基础 query 构建段，在 Count/Find 之前。**必须用全限定列名 `sys_data_reconciliation.applied_actions`**（避免 JOIN 时歧义）。

    **不要**：改 handler/router（Task 5）；改前端（Task 6）；改转单 cron SQL（44-02 Task）。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go test ./internal/services/asset/ -run 'TestDetectLayer3|TestListExceptions' -count=1 -v</automated>
  </verify>
  <done>
    - reconciliation_detection.go 含字符串 `Layer 3.5` 注释 + `matchException(activeRules` 调用 + `ExceptionRuleID: exceptionRuleID` + `AppliedActions: appliedActions` + `matchedSeverity`
    - reconciliation_service.go 的 `ExceptionListParams` 含 `ShowSilenced bool` 字段
    - ListExceptions 含 `WHERE NOT ('silence' = ANY(sys_data_reconciliation.applied_actions))`（默认分支）
    - 8 个 test case 全部通过（含 TestDetectLayer3DeptScopeMatch / NoMatch，WARN-5 端到端覆盖）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: 例外规则 CRUD/测试 handler + router + operlog 接入 + module 常量</name>
  <files>
    internal/api/v1/asset/reconciliation_exception_handler.go,
    internal/api/v1/asset/reconciliation_exception_handler_test.go,
    internal/api/v1/asset/reconciliation_exception_router.go,
    internal/api/v1/asset/reconciliation_handler.go
  </files>
  <read_first>
    - internal/api/v1/asset/reconciliation_exception_handler.go:1-67（现有 ListRules/GetRuleByID + WithCore 模式）
    - internal/api/v1/asset/reconciliation_exception_router.go:1-23（现有 list/:id 路由）
    - internal/api/v1/asset/reconciliation_handler.go:14-19（ModuleReconciliation 常量定义位置）
    - internal/api/v1/system/config_handler.go:101-121,322-352（operlog.Record 接入 Create/Update/Delete 的 source-of-truth）
    - internal/services/asset/reconciliation_exception.go（Task 3 产出的 Create/Update/Delete/MatchTest service 方法签名）
    - internal/utils/operlog/regression_test.go:45-207（25 OperType + **18** mandatorySensitiveKeywords 回归守护，确认新增 module 常量不破坏 — WARN-11）
    - pkg/middleware/permission.go:200（RequirePermissions 签名）
    - 44-PATTERNS.md §9 + §10（handler/router 扩展 + operlog 接入）
    - 44-CONTEXT.md（权限命名 `asset:reconciliation:exception:{list,create,update,delete,test}`）
    - CLAUDE.md "操作日志记录约定 — 强制"（25 OperType 常量 + module 中文名规范 "资产对账-例外规则"；⚠ CLAUDE.md "11 关键词" 已过时，regression_test.go:188-207 实测 18 个 — WARN-11）
  </read_first>
  <behavior>
    - TestCreateRuleHandlerOperlog：POST /exception-rule/create 合法 body，handler 在 response.Success 前调 operlog.Record，参数含 ModuleReconciliationExceptionRule + OperTypeCreate
    - TestUpdateRuleHandlerOperlog：Update 调 OperTypeUpdate
    - TestDeleteRuleHandlerOperlog：Delete 调 OperTypeDelete
    - TestTestRuleHandlerNoOperlog：命中测试是读操作，不调 operlog.Record
    - TestCreateRuleValidationFail：非法 CIDR / actions / reason <10 字符，handler 返回 400 + 不调 operlog（service 层 err 短路）
    - TestOperlogRegressionStillPasses：现有 `go test ./internal/utils/operlog/` 仍通过（25 OperType + **18** mandatorySensitiveKeywords 锁定不被新 module 常量破坏，WARN-11）
  </behavior>
  <action>
    先扩展 `reconciliation_exception_handler_test.go`（RED，可用 httptest + mocked service + mocked OperLogService 验证 operlog.Record 调用）。

    **修改 `reconciliation_handler.go`**：在 `ModuleReconciliation = "资产对账"` 旁加：
    ```
    ModuleReconciliationExceptionRule = "资产对账-例外规则"  // Phase 44 R3 / Phase 42 D-16 锁定
    ```

    **修改 `reconciliation_exception_handler.go`**：扩展 handler 方法（参照 44-PATTERNS.md §9 + config_handler.go:101-121 模式）：

    - `CreateRule(c *gin.Context)`：`c.ShouldBindJSON(&req)` 失败 → `response.Error(c, http.StatusBadRequest, "请求参数错误")`；调 `h.service.Create(c.Request.Context(), &req)` 失败 → `response.Error(c, http.StatusInternalServerError, err.Error())`；success path 末尾 `operlog.Record(c, h.core.OperLogService, h.core.GetDB(), ModuleReconciliationExceptionRule, operlog.OperTypeCreate)` 然后 `response.Success(c, rule)`。
    - `UpdateRule(c *gin.Context)`：同模式，operlog.OperTypeUpdate。
    - `DeleteRule(c *gin.Context)`：同模式，operlog.OperTypeDelete。
    - `TestRule(c *gin.Context)`：bind `{IP, UserID, DeptID}` → 调 `h.service.MatchTest(...)` → `response.Success(c, result)`。**读操作不调 operlog**（参照现有 ListRules/GetRuleByID 无 operlog）。

    **import 补**：`net/http` / `github.com/xingran-next/xingran-go-backend/internal/utils/operlog` / `github.com/xingran-next/xingran-go-backend/pkg/response`。

    **修改 `reconciliation_exception_router.go`**：扩展 SetupReconciliationExceptionRouter 加 4 路由 + 权限中间件（44-PATTERNS.md §10 verbatim）：
    ```
    r.POST("/exception-rule/create", middleware.RequirePermissions([]string{"asset:reconciliation:exception:create"}, core), handler.CreateRule)
    r.POST("/exception-rule/:id/update", middleware.RequirePermissions([]string{"asset:reconciliation:exception:update"}, core), handler.UpdateRule)
    r.POST("/exception-rule/:id/delete", middleware.RequirePermissions([]string{"asset:reconciliation:exception:delete"}, core), handler.DeleteRule)
    r.POST("/exception-rule/test", middleware.RequirePermissions([]string{"asset:reconciliation:exception:test"}, core), handler.TestRule)
    ```
    **import 补**：`github.com/xingran-next/xingran-go-backend/pkg/middleware`。

    list/:id 路由现有无权限中间件（R1 skeleton），R3 视情况补 `:list` 读权限（参照项目记忆 `xingran-perm-namespace-split-readonly-page` 教训：读路径可放宽，避免误锁只读场景），保持现状不强制补。

    **operlog 注意（CLAUDE.md 强制约定）**：例外规则 CRUD 无敏感字段（reason 是普通文本），用 `operlog.Record` 非 `RecordWithBody`。回归守护 `internal/utils/operlog/regression_test.go` 自动覆盖 module 常量不破坏 25 OperType + 18 mandatorySensitiveKeywords（module 是自由字符串， WARN-11：CLAUDE.md 写 "11 关键词" 已过时，实测 18 个，断言用 `>=`）。

    **不要**：改前端（Task 6）；改降噪基线（44-02）；改转单 SQL（44-02）。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go test ./internal/api/v1/asset/ -run 'TestCreateRuleHandler|TestUpdateRuleHandler|TestDeleteRuleHandler|TestTestRuleHandler' -count=1 -v && go test ./internal/utils/operlog/ -count=1</automated>
  </verify>
  <done>
    - reconciliation_handler.go 含常量 `ModuleReconciliationExceptionRule = "资产对账-例外规则"`
    - reconciliation_exception_handler.go 含 `func (h *ReconciliationExceptionHandler) CreateRule(` / `UpdateRule(` / `DeleteRule(` / `TestRule(`
    - CreateRule/UpdateRule/DeleteRule 各含 `operlog.Record(c, h.core.OperLogService, h.core.GetDB(), ModuleReconciliationExceptionRule, operlog.OperType...)`
    - TestRule 不含 operlog.Record 调用
    - reconciliation_exception_router.go 含 4 个新路由 + RequirePermissions 中间件
    - 6 个 test case 全通过 + operlog 回归测试通过（18 mandatorySensitiveKeywords 守护不破坏）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6a: queryKeys.ts reconciliation 命名空间补 4 key + assetApi.ts 扩展（前端基建）</name>
  <files>
    xingran-react-frontend/src/lib/queryKeys.ts,
    xingran-react-frontend/src/lib/assetApi.ts
  </files>
  <read_first>
    - xingran-react-frontend/src/lib/queryKeys.ts:44-69（reconciliation 命名空间现状：仅含 summary/byConflictType/bySeverity/healthTrend/topUnresolved/exceptionList/exceptionDetail/ruleStats — BLOCKER-1 实测证据）
    - xingran-react-frontend/src/lib/assetApi.ts（现有 reconciliationApi 定义，参照扩展 exceptionRule + baseline 命名空间，44-PATTERNS.md §16 verbatim）
    - 44-PATTERNS.md §16（assetApi 扩展 verbatim）
    - CLAUDE.md "Frontend API Calling"（用 wrapped `post` from `@/lib/api`，不用 raw axios）
  </read_first>
  <behavior>
    - 前端 `npm run type-check` 退出 0
    - queryKeys.ts reconciliation 命名空间含 ruleList / ruleDetail / matchTest / baselineCompare 4 个新 key（grep 验证）
    - assetApi.ts 含 exceptionRule.{list,getById,create,update,delete,test} + baseline.{snapshot,compare}
    - ExceptionListParams（assetApi.ts）含 `showSilenced?: boolean` 字段
  </behavior>
  <action>
    **修改 `queryKeys.ts`**：在现有 reconciliation 命名空间（:44-69）的 `ruleStats` 之后追加 4 个新 key（BLOCKER-1 强制）：
    ```ts
    /** 例外规则列表（admin 页，分页 + 筛选） */
    ruleList: (params?: Record<string, unknown>) =>
      ["reconciliation", "rule-list", params ?? {}] as const,
    /** 例外规则详情（单条，编辑回填） */
    ruleDetail: (id: string) =>
      ["reconciliation", "rule-detail", id] as const,
    /** 命中测试结果（按 IP+userID+deptID 入参缓存） */
    matchTest: (params: { ip: string; userId?: string; deptId?: string }) =>
      ["reconciliation", "match-test", params] as const,
    /** 降噪基线对比结果（dashboard 降噪卡片） */
    baselineCompare: () =>
      ["reconciliation", "baseline-compare"] as const,
    ```
    注意保持 `as const` 元组语义与现有 key 风格一致。

    **修改 `assetApi.ts`**：在现有 reconciliationApi 命名空间内加 exceptionRule + baseline（44-PATTERNS.md §16 verbatim，用 `post` from `@/lib/api`，**不用** raw axios）：
    ```
    exceptionRule: {
      list: (params) => post('/asset/reconciliation/exception-rule/list', params),
      getById: (id) => post(`/asset/reconciliation/exception-rule/${id}`),
      create: (data) => post('/asset/reconciliation/exception-rule/create', data),
      update: (id, data) => post(`/asset/reconciliation/exception-rule/${id}/update`, data),
      delete: (id) => post(`/asset/reconciliation/exception-rule/${id}/delete`),
      test: (data) => post('/asset/reconciliation/exception-rule/test', data),
    },
    baseline: {
      snapshot: () => post('/asset/reconciliation/baseline/snapshot'),
      compare: () => post('/asset/reconciliation/baseline/compare'),
    }
    ```
    ExceptionListParams 加 `showSilenced?: boolean`。

    **不要**：在本子任务创建页面组件（Task 6b/6c 做）；不改 exceptions/index.tsx 的 Switch（Task 6c 做）。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend/xingran-react-frontend && npm run type-check</automated>
  </verify>
  <done>
    - queryKeys.ts 的 reconciliation 命名空间含 `ruleList` / `ruleDetail` / `matchTest` / `baselineCompare`（`grep -E "ruleList|ruleDetail|matchTest|baselineCompare" xingran-react-frontend/src/lib/queryKeys.ts` 返回 4 行匹配）
    - assetApi.ts 含 `exceptionRule:` 命名空间（6 方法）+ `baseline:` 命名空间（2 方法）
    - ExceptionListParams 含 `showSilenced?: boolean`
    - `npm run type-check` 退出 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6b: 例外规则 admin 页 + ExceptionRuleForm（统计卡片 + 列表 + Modal CRUD + 基线按钮）</name>
  <files>
    xingran-react-frontend/src/pages/asset/reconciliation/exception-rules/index.tsx,
    xingran-react-frontend/src/components/asset/reconciliation/ExceptionRuleForm.tsx
  </files>
  <read_first>
    - xingran-react-frontend/src/lib/queryKeys.ts（Task 6a 产出的 ruleList/ruleDetail/baselineCompare key）
    - xingran-react-frontend/src/lib/assetApi.ts（Task 6a 产出的 exceptionRule.{list,getById,create,update,delete} + baseline.snapshot）
    - xingran-react-frontend/src/pages/asset/reconciliation/exceptions/index.tsx:1-200（同模块列表页结构：Form 筛选 + Table + 分页 + useDict + useServerSort + useEffect 稳定性注释）
    - xingran-react-frontend/src/pages/ad-domain/accounts/index.tsx（Phase 36 admin 页：统计卡片 + 列表 + Modal CRUD 模式，44-PATTERNS.md §12 第二个 analog）
    - 44-PATTERNS.md §12（admin 页模式）
    - 44-CONTEXT.md D-R3-A4-01（admin 页"记录基线"按钮，按钮 UI 本 task 加，逻辑调 baseline.snapshot；44-02 后端实现，本 task 先 wire UI）
    - CLAUDE.md "Frontend/React Best Practices / useEffect Dependencies"（params 用 useMemo + JSON.stringify 稳定，避免无限请求循环）
    - CLAUDE.md "Frontend API Calling"（用 wrapped `post` from `@/lib/api`，不用 raw axios）
  </read_first>
  <behavior>
    - 前端 `npm run type-check` 退出 0
    - 前端 `npm run build` 退出 0
    - admin 页 `/asset/reconciliation/exception-rules` 渲染：统计卡片（总规则/启用/停用）+ 筛选表单 + Table + Modal 表单 + 命中测试入口（按钮 onClick 仅打开 Modal/Drawer，面板内容 Task 6c）
    - ExceptionRuleForm 含字段：Name(Input) / IPRange(Input, CIDR) / ConflictTypes(多选 B/C/D/E/F 或留空=全部) / ExceptionActions(多选 5 actions) / SeverityOverride(Select low/medium/high/null) / ScopeType(Radio global/dept/user) / ScopeID(条件 Select dept 或 user) / ExpiresAt(DatePicker) / Reason(TextArea, ≥10 字符提示)
    - admin 页顶部"记录当前为基线"按钮 onClick 调 reconciliationApi.baseline.snapshot() + useMutation invalidate queryKeys.reconciliation.baselineCompare + 成功 message
    - useEffect 依赖稳定（params 用 useMemo，无内联对象数组依赖）
  </behavior>
  <action>
    **新建 `exception-rules/index.tsx`**：参照 exceptions/index.tsx + ad-domain/accounts/index.tsx 模式：
    - imports：React hooks + antd（Table/Form/Input/Select/DatePicker/Button/Space/Tag/Card/Modal/Switch/Badge）+ `useQuery`/`useMutation`/`useQueryClient` from `@tanstack/react-query` + `reconciliationApi` from `@/lib/assetApi` + `queryKeys` from `@/lib/queryKeys`
    - 顶部统计卡片（useQuery `queryKeys.reconciliation.ruleStats()` 或 reconciliationApi.exception.stats，复用 Phase 42 已实现的 ExceptionRuleStats 端点 reconciliation_statistics.go:77）
    - 筛选表单：name 模糊 / is_active Select / scope_type Select
    - Table 列：name / ip_range / exception_actions(Tag 多色) / severity_override / scope(scope_type + scope_name) / expires_at / is_active(Switch 或 Tag) / 操作(编辑/删除/命中测试)
    - Modal 嵌 ExceptionRuleForm 组件（Task 6b 本身创建）
    - 命中测试按钮打开 Modal/Drawer，**Drawer 内容渲染留空 placeholder + 注释 "Task 6c 接入 MatchTestPanel"**（不在本 task 实现 MatchTestPanel）
    - 顶部加"记录当前为基线"按钮（D-R3-A4-01），onClick 调 `reconciliationApi.baseline.snapshot()`，useMutation 成功后 `qc.invalidateQueries({ queryKey: queryKeys.reconciliation.baselineCompare() })` + message.success("基线已记录")
    - **useEffect 稳定性**：listParams 用 `useMemo(() => ({...filters, current, pageSize, orderByColumn, isAsc}), [JSON.stringify(filters), current, pageSize, orderByColumn, isAsc])`（参照 exceptions/index.tsx 现有模式 + CLAUDE.md 强约束）

    **新建 `ExceptionRuleForm.tsx`**：antd Form + Modal 模式（参照 exceptions/index.tsx Form 字段布局）：
    - props: `{ open, initialValues, onSubmit, onCancel }`
    - 字段：Name(Input required) / IPRange(Input required, placeholder "192.168.0.0/16 或 2001:db8::/32") / ConflictTypes(Select mode=multiple，options B/C/D/E/F，留空提示"匹配全部 B-F") / ExceptionActions(Select mode=multiple required，options no_alert/no_notice/no_workorder/skip_severity/silence) / SeverityOverride(Select allowClear，options low/medium/high) / ScopeType(Radio.Group global/dept/user) / ScopeID(条件渲染：dept→TreeSelect 部门 / user→Select 用户，参照 useDeptTree/useUserList hooks) / ExpiresAt(DatePicker showTime) / Reason(TextArea required, minLength 10 提示)
    - 校验：Reason 用 `min: 10` message "原因至少 10 字符"；IPRange 用 pattern required
    - onSubmit 调 props.onSubmit(values)

    **CLAUDE.md 强约束**：
    - 所有 API 调用用 `reconciliationApi.exceptionRule.*`，**不用** raw axios / `api.post`
    - useEffect 依赖必须稳定（无内联对象/数组依赖，参照项目记忆 `rc-tree-virtual-motion-infinite-loop` 教训）

    **不要**：在本子任务实现 MatchTestPanel（Task 6c）；不改 exceptions/index.tsx 的 silence Switch（Task 6c）。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend/xingran-react-frontend && npm run type-check && npm run build</automated>
  </verify>
  <done>
    - exception-rules/index.tsx 渲染统计卡片 + 筛选 + Table + Modal 表单 + 命中测试入口（Drawer placeholder）+ 基线按钮（调 baseline.snapshot + invalidate baselineCompare + message）
    - ExceptionRuleForm.tsx 含 9 个表单字段（Name/IPRange/ConflictTypes/ExceptionActions/SeverityOverride/ScopeType/ScopeID/ExpiresAt/Reason）
    - useMutation 接入 baseline.snapshot，invalidate 用 queryKeys.reconciliation.baselineCompare()
    - `npm run type-check` + `npm run build` 退出 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6c: MatchTestPanel 命中测试面板 + exceptions/index.tsx silence 开关</name>
  <files>
    xingran-react-frontend/src/components/asset/reconciliation/MatchTestPanel.tsx,
    xingran-react-frontend/src/pages/asset/reconciliation/exceptions/index.tsx
  </files>
  <read_first>
    - xingran-react-frontend/src/lib/queryKeys.ts（Task 6a 产出的 matchTest key）
    - xingran-react-frontend/src/lib/assetApi.ts（Task 6a 产出的 exceptionRule.test 方法）
    - xingran-react-frontend/src/pages/asset/reconciliation/exception-rules/index.tsx（Task 6b 产出的 admin 页，本 task 把命中测试 Drawer 的 placeholder 替换为 MatchTestPanel 渲染——本子任务文件列表不含 admin 页，由 executor 在 6b 已留 Drawer 入口的前提下，新建 MatchTestPanel 并 export 给 admin 页 import；若 6b Drawer 直接 import 失败，executor 在 6c 同步更新 admin 页 import 语句，但**不重构** 6b 已完成的列表/表单）
    - xingran-react-frontend/src/hooks/useExceptionList.ts（useQuery 模式，R3 仿写）
    - xingran-react-frontend/src/pages/asset/reconciliation/exceptions/index.tsx:1-200（同模块列表页，本 task 加 Switch）
    - 44-PATTERNS.md §13/§15（silence 过滤 + MatchTestPanel 模式）
    - 44-CONTEXT.md D-R3-A2-03（命中测试合并卡片形态）/ D-R3-A3-03（命中测试输入 + needsUserDept 徽标）
    - CLAUDE.md "Frontend/React Best Practices / useEffect Dependencies"
  </read_first>
  <behavior>
    - 前端 `npm run type-check` + `npm run build` 退出 0
    - MatchTestPanel：输入 IP(Input, 必填) + UserID(Select, 可选) + DeptID(Select, 可选) → 调 reconciliationApi.exceptionRule.test → 渲染顶部合并卡片（mergedActions Tag union + finalSeverity + isSilence Badge）+ 下方命中规则 Table + needsUserDept 时显示"需指定 user/dept 才能评估 dept/user scope 规则"提示
    - exceptions/index.tsx 筛选表单加 Switch "显示已静默"，透传 showSilenced 到后端
    - admin 页命中测试 Drawer 内容渲染 MatchTestPanel（替换 6b placeholder）
  </behavior>
  <action>
    **新建 `MatchTestPanel.tsx`**：合并卡片 + 命中规则列表（greenfield，参考 hooks/useExceptionList.ts useQuery 模式）：
    - props: `{ open, onClose }` 或嵌入父组件（默认导出可独立使用的 Panel）
    - 输入区：IP(Input required) + UserID(Select 可选) + DeptID(Select 可选) + "测试"按钮
    - useQuery `queryKeys.reconciliation.matchTest({ ip, userId, deptId })` with options `enabled=!!testInput.ip`，调 `reconciliationApi.exceptionRule.test({ip, userId, deptId})`
    - 结果区顶部 Card：mergedActions(Tag union 多色) + finalSeverity(Tag colored) + isSilence(Badge "已静默" 红/灰) + needsUserDept(true 时 Alert "需指定 user/dept 才能评估 dept/user scope 规则")
    - 结果区下方 Table：命中规则列 name / ip_range / exception_actions / severity_override / scope / expires_at / reason
    - **useQuery 依赖稳定**：testInput 用 useState 持有原始字符串，queryKey 入参对象 useMemo 稳定（参照 CLAUDE.md useEffect 强约束）

    **修改 `exceptions/index.tsx`**：
    - 筛选表单加 `<Form.Item name="showSilenced" label="显示已静默"><Switch /></Form.Item>`
    - listParams 透传 showSilenced（默认 false / undefined）
    - useEffect 依赖按现有稳定模式扩展（listParams useMemo 的 deps 数组加 showSilenced 原始值）

    **修改 `exception-rules/index.tsx`**（仅在 6b 留下 Drawer placeholder 的前提下）：把 Drawer body 的 placeholder 注释替换为 `<MatchTestPanel open={...} onClose={...} />`，补 import。**不重构** 6b 已完成的列表/表单/基线按钮逻辑。

    **CLAUDE.md 强约束**：所有 API 调用用 `reconciliationApi.exceptionRule.*`；useEffect 依赖必须稳定。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend/xingran-react-frontend && npm run type-check && npm run build</automated>
  </verify>
  <done>
    - MatchTestPanel.tsx 含 IP/UserID/DeptID 输入 + 合并卡片（mergedActions/finalSeverity/isSilence/needsUserDept）+ 命中规则 Table
    - useQuery 用 `queryKeys.reconciliation.matchTest(` 入参（grep 验证）
    - exceptions/index.tsx 含 showSilenced Switch + 透传逻辑（listParams 含 showSilenced）
    - admin 页 Drawer body 渲染 MatchTestPanel（grep 验证 exception-rules/index.tsx 含 `<MatchTestPanel`）
    - `npm run type-check` + `npm run build` 退出 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin 用户 → `/asset/reconciliation/exception-rule/{create,update,delete}` | 例外规则写操作入口，未授权用户不应能创建 silence 规则掩盖告警 |
| admin 用户 → `/asset/reconciliation/exception-rule/test` | 命中测试 IP 参数进入 SQL `ip_range >> ?::inet`，注入风险点 |
| 检测 cron → `sys_data_reconciliation` (写 exception_rule_id/applied_actions) | Layer 3.5 写入路径，规则数据来自 sys_reconciliation_exception |
| 例外规则 CRUD → operlog sys_oper_log | 写操作必须留痕，否则审计链断（AUDIT-01） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering (CIDR 注入) | `reconciliation_exception.go:ValidateCIDR` + `ip_range` 列 | mitigate | service 层 `net.ParseCIDR` 严格校验（Task 3 TestValidateCIDR 锁定），解析失败拒绝创建；DB `cidr` 列类型兜底 INSERT 非法值 SQLSTATE 22P02 |
| T-44-02 | Elevation (越权创建例外规则) | router `/exception-rule/create` + RequirePermissions 中间件 | mitigate | 路由层 `middleware.RequirePermissions([]string{"asset:reconciliation:exception:create"}, core)`（Task 5），仅持权限角色可调；权限命名空间 44-CONTEXT.md 锁定 |
| T-44-03 | Tampering (SQL 注入) | `reconciliation_exception.go:MatchTest` GiST 查询 | mitigate | GORM 占位符 `Where("ip_range >> ?::inet", ip)`，禁止字符串拼接（Task 3 TestMatchTest 验证）；44-RESEARCH.md §Pattern 3 + §Security Domain T-SQLInject |
| T-44-04 | Denial of Service (告警风暴误配) | `0.0.0.0/0 silence` 全静默 | mitigate | service 层 `ValidateReason` 强制 reason ≥10 字符（Task 3 TestValidateReason）；`expires_at` 默认 30 天（INFRA-02 default_expiry_days seed）；admin 列表高亮覆盖范围过广规则（可选，44-CONTEXT.md Claude's Discretion） |
| T-44-05 | Repudiation (审计链断裂) | 例外规则 CRUD + Layer 3.5 写入 + 硬删除风险 | mitigate | (a) 所有 CRUD handler success path 调 `operlog.Record(..., ModuleReconciliationExceptionRule, ...)`（Task 5）；(b) Layer 3.5 命中例外仍 INSERT sys_data_reconciliation 含 exception_rule_id + applied_actions（D-R3-A1-02 单一真相源 + Task 4）；(c) Delete 是软删除（db.Delete 填 deleted_at），cron 软停用只改 is_active（44-02 Task 1）；历史外键链不断 |
| T-44-06 | Information Disclosure (越权读例外规则) | `/exception-rule/list` + `/exception-rule/:id` | accept | R1 skeleton 现有无权限中间件；参照项目记忆 `xingran-perm-namespace-split-readonly-page` 教训，读路径可放宽避免误锁只读场景（44-PATTERNS.md §10 已说明）。admin 角色默认持 list 权限，普通用户不进 admin 页 |
| T-44-SC | Tampering (依赖) | npm/pip/cargo install | mitigate | R3 零新增依赖（44-RESEARCH.md §Package Legitimacy Audit 确认），无 slopcheck 风险 |
</threat_model>

<verification>
## Plan-Level Checks

- [ ] `go build ./...` 退出 0（CLAUDE.md 强制）
- [ ] `go test ./internal/services/asset/... ./internal/api/v1/asset/... ./internal/scheduler/... -count=1 -short` 全通过
- [ ] `go test ./internal/utils/operlog/ -count=1` 通过（25 OperType + 18 mandatorySensitiveKeywords 回归守护不被新 module 常量破坏，WARN-11）
- [ ] `cd xingran-react-frontend && npm run type-check && npm run build` 退出 0
- [ ] migration_174 文件含 `idx_recon_exc_active_range` / `chk_recon_exc_actions` / `chk_recon_exc_severity_override`
- [ ] reconciliation_exception_matcher.go 含 `matchException` / `mergeActions` / `applySkipSeverity` / `preloadActiveRules` / `compiledRule`
- [ ] reconciliation_exception.go interface 含 `Create` / `Update` / `Delete` / `MatchTest` + 4 个 Validate 函数
- [ ] reconciliation_detection.go 含 `Layer 3.5` 注释 + `matchException(activeRules` + `ExceptionRuleID` + `AppliedActions`
- [ ] reconciliation_service.go ListExceptions 含 `WHERE NOT ('silence' = ANY(sys_data_reconciliation.applied_actions))`
- [ ] handler 含 4 个 operlog.Record 调用（Create/Update/Delete）+ TestRule 不调 operlog
- [ ] router 含 4 个新路由 + RequirePermissions
- [ ] queryKeys.ts reconciliation 命名空间含 `ruleList` / `ruleDetail` / `matchTest` / `baselineCompare`（Task 6a）
- [ ] 前端 admin 页 + ExceptionRuleForm + MatchTestPanel + silence Switch 全部存在（Task 6b/6c）

## Manual-Only（execute-phase human verify）

- admin 页表单交互（CIDR 输入 + 多选 + DatePicker）渲染正确
- 命中测试 UI 合并卡片可视化（需后端运行 + 规则数据）
- 跨模块权限边界（无权限账号访问 admin 页 403）
- GiST 索引在真实 PG 创建成功（需 dev DB：`SELECT indexname FROM pg_indexes WHERE tablename='sys_reconciliation_exception'`）
</verification>

<success_criteria>
本 plan 覆盖 ROADMAP Phase 44 success criteria：
- SC 1（CIDR 校验 + GiST 索引）→ Task 1 migration_174 + Task 3 ValidateCIDR
- SC 2（CRUD + operlog）→ Task 5 handler/router + module 常量
- SC 3（5 actions 多选）→ Task 3 ValidateActions + DB CHECK chk_recon_exc_actions
- SC 4（多规则并集可视化）→ Task 2 mergeActions + Task 6c MatchTestPanel 合并卡片
- SC 6（命中测试工具）→ Task 3 MatchTest + Task 5 TestRule handler + Task 6c MatchTestPanel
- SC 7（生效统计）→ Task 4 silence 过滤 + Phase 42 已实现 ExceptionRuleStats 端点自动接入数据
- SC 10（命中例外仍记录 + exception_rule_id + applied_actions）→ Task 4 Layer 3.5 插入

EXCEPTION-01（CIDR 配置）→ Task 1/2/3
EXCEPTION-02（5 actions 并集）→ Task 2/3
EXCEPTION-04（命中测试工具）→ Task 3/5/6a/6b/6c
AUDIT-01（CRUD operlog）→ Task 5

下游 44-02 依赖：本 plan 的 CRUD service / matchException 纯函数 / module 常量 / assetApi baseline 端点 wire / queryKeys baselineCompare key。
</success_criteria>

<output>
Create `.planning/phases/44-ip-r3/44-01-SUMMARY.md` when done.
</output>
