---
phase: 45-r4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/services/asset/reconciliation_service.go
  - internal/services/asset/cache_keys.go
  - internal/api/v1/asset/reconciliation_router.go
  - internal/api/v1/asset/reconciliation_handler.go
  - internal/api/v1/operations/workstation_handler.go
  - internal/api/router.go
  - internal/models/workstation.go
  - pkg/middleware/permission_query_helper.go
  - xingran-react-frontend/src/lib/queryKeys.ts
  - xingran-react-frontend/src/lib/assetApi.ts
  - xingran-react-frontend/src/components/reconciliation/HealthCard.tsx
  - xingran-react-frontend/src/components/reconciliation/HealthBadge.tsx
  - xingran-react-frontend/src/components/reconciliation/ReconciliationDrawer.tsx
  - xingran-react-frontend/src/components/reconciliation/ReconciliationTimeline.tsx
  - xingran-react-frontend/src/components/reconciliation/ExceptionMatchList.tsx
  - xingran-react-frontend/src/components/reconciliation/index.ts
  - xingran-react-frontend/src/components/reconciliation/hooks/useWorkstationHealth.ts
  - xingran-react-frontend/src/components/reconciliation/hooks/useAssetHealth.ts
  - xingran-react-frontend/src/components/reconciliation/hooks/useExceptionMatch.ts
  - xingran-react-frontend/src/components/reconciliation/hooks/useReconciliationVisibility.ts
  - xingran-react-frontend/src/pages/operations/workstations/index.tsx
  - xingran-react-frontend/src/pages/operations/workstations/views/CardView.tsx
  - xingran-react-frontend/src/components/operations/WorkstationDeviceTable/index.tsx
  - xingran-react-frontend/src/pages/operations/assets/index.tsx
  - internal/api/v1/operations/workstation_handler_test.go
autonomous: true
requirements:
  - INTEGRATE-01
  - INTEGRATE-02
  - INTEGRATE-03
must_haves:
  truths:
  - "WorkstationHandler.GetByID success path injects reconciliationVisible flag based on hasReconciliationPerm helper (NOT PermissionService.UserHasPerm which does NOT exist on core.Core)"
  - "POST /asset/reconciliation/by-workstation returns ByWorkstationResponse with visible=false when user lacks perm (silent hide, NOT 403)"
  - "HealthCard renders in workstation expand panel above WorkstationDeviceTable when useReconciliationVisibility returns true"
  - "HealthBadge renders as 8px dot in WorkstationDeviceTable asset/AD sub-tables + assets page list"
  - "ReconciliationDrawer opens at 780px width with 3 Tabs (冲突摘要 / 历史变更 / 例外规则)"
  - "useReconciliationVisibility reads from useMenuStore.permissions (NOT authStore.perms which does NOT exist)"
  - "Cache invalidation: ResolveException success path triggers InvalidateWorkstationHealth BEFORE operlog.Record"
  - "WorkstationDeviceTable receives conflictType Map<assetId, string> from lifted parent useWorkstationHealth call (NOT per-row N+1)"
  - "useWorkstationHealth has enabled: visible && Boolean(workstationId) && data?.visible !== false (defense-in-depth per UI-SPEC)"
artifacts:
  - path: internal/services/asset/reconciliation_service.go
    provides: "ReconciliationService.GetByWorkstation(wsID, window) + cache injection"
    exports: ["GetByWorkstation"]
  - path: internal/services/asset/cache_keys.go
    provides: "InvalidateWorkstationHealth cache invalidation helper"
    exports: ["InvalidateWorkstationHealth"]
  - path: pkg/middleware/permission_query_helper.go
    provides: "HasUserPermission(c, perm) helper reading perm from JWT claims after RequirePermissions middleware"
    exports: ["HasUserPermission"]
  - path: internal/api/v1/asset/reconciliation_router.go
    provides: "POST /asset/reconciliation/by-workstation route"
  - path: internal/api/v1/asset/reconciliation_handler.go
    provides: "GetByWorkstation handler + hasReconciliationPerm using HasUserPermission helper"
  - path: internal/api/v1/operations/workstation_handler.go
    provides: "ReconciliationService injection + hasReconciliationPerm using HasUserPermission helper"
  - path: internal/models/workstation.go
    provides: "Workstation fields: Reconciliation/ReconciliationVisible/ReconciliationHiddenReason"
  - path: xingran-react-frontend/src/components/reconciliation/HealthCard.tsx
    provides: "Workstation health card (5 KPI + trend mini chart + score)"
  - path: xingran-react-frontend/src/components/reconciliation/HealthBadge.tsx
    provides: "Inline reconciliation health badge (6 color dots A-F)"
  - path: xingran-react-frontend/src/components/reconciliation/ReconciliationDrawer.tsx
    provides: "Conflict detail drawer (3 Tabs)"
  - path: xingran-react-frontend/src/components/reconciliation/hooks/useWorkstationHealth.ts
    provides: "useQuery hitting by-workstation, 5min staleTime, defense-in-depth enabled gate"
  - path: xingran-react-frontend/src/components/reconciliation/hooks/useReconciliationVisibility.ts
    provides: "Cross-module permission gate reading useMenuStore.permissions (NOT authStore.perms)"
key_links:
  - from: internal/api/v1/asset/reconciliation_handler.go
    to: internal/services/asset/reconciliation_service.go
    via: "h.service.GetByWorkstation(ctx, workstationId, window)"
    pattern: "h\\.service\\.GetByWorkstation"
  - from: internal/services/asset/reconciliation_service.go
    to: reconciliation_normalized MV
    via: "LEFT JOIN reconciliation_normalized ON workstation_id"
    pattern: "reconciliation_normalized"
  - from: internal/services/asset/reconciliation_service.go
    to: pkg/cache.Cache
    via: "CacheProvider.GetOrSet(key, TTL=5min)"
    pattern: "cache\\.GetOrSet|GetReconciliationHealthByWorkstationKey"
  - from: internal/api/v1/asset/reconciliation_handler.go
    to: pkg/middleware/permission_query_helper.go
    via: "middleware.HasUserPermission(c, 'asset:reconciliation:list')"
    pattern: "middleware\\.HasUserPermission"
  - from: internal/api/v1/operations/workstation_handler.go
    to: pkg/middleware/permission_query_helper.go
    via: "middleware.HasUserPermission(c, 'asset:reconciliation:list')"
    pattern: "middleware\\.HasUserPermission"
  - from: internal/api/v1/operations/workstation_handler.go
    to: internal/services/asset/reconciliation_service.go
    via: "Cross-module injection reconciliationSvc.GetByWorkstation"
    pattern: "reconciliationSvc\\.GetByWorkstation"
  - from: xingran-react-frontend/src/store/menuStore.ts
    to: xingran-react-frontend/src/components/reconciliation/hooks/useReconciliationVisibility.ts
    via: "useMenuStore((s) => s.permissions).includes(REQUIRED_PERM)"
    pattern: "useMenuStore.*permissions.*asset:reconciliation:list"
  - from: xingran-react-frontend/src/pages/operations/workstations/index.tsx
    to: xingran-react-frontend/src/components/reconciliation/HealthCard.tsx
    via: "expandable.expandedRowRender embeds HealthCard"
    pattern: "<HealthCard"
  - from: xingran-react-frontend/src/pages/operations/workstations/index.tsx
    to: xingran-react-frontend/src/components/operations/WorkstationDeviceTable/index.tsx
    via: "Lifts useWorkstationHealth result + passes Map<assetId,conflictType> down to WorkstationDeviceTable"
    pattern: "assetConflictMap|conflictTypeMap"
  - from: xingran-react-frontend/src/components/operations/WorkstationDeviceTable/index.tsx
    to: xingran-react-frontend/src/components/reconciliation/HealthBadge.tsx
    via: "createColumns renders HealthBadge with prop conflictType from lifted map"
    pattern: "<HealthBadge"
---

<objective>
Phase 45 R4 把对账健康度嵌入工位详情页与资产列表页，提供冲突详情抽屉，并实现跨模块调用、权限降级、N+1 优化。本 plan（45-01）交付前端整合的所有可视层与组件 + 后端聚合 API + 跨模块注入，覆盖 INTEGRATE-01/02/03 全集。

Purpose: 一次性把"读路径 + UI 集成"做完整，45-02 单独处理缓存主动失效闭环。

Output: 12 个新前端文件（4 组件 + 4 hooks + 1 barrel + 3 css 模块）+ 8 个后端文件修改 + 1 个 helper 文件 + 1 个测试文件。

**Scope correction from checker revision:**
- Plan 01 does NOT modify `internal/api/v1/asset/reconciliation_exception_handler.go` (that file has no ResolveException — ResolveException lives in `reconciliation_handler.go:110`).
- Plan 01 does NOT introduce a new file for IP resolution — IP resolution chain is part of GetByWorkstation, stays inline in `reconciliation_service.go`. Plan 02 may extract `resolveAssetIP` into its own file only if the file gets too long.
- Plan 01 does NOT call InvalidateWorkstationHealth anywhere — cache invalidation is purely Plan 02 work. Plan 01 only establishes the InvalidateWorkstationHealth helper in `cache_keys.go` for Plan 02 to use.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/45-r4/45-CONTEXT.md
@.planning/phases/45-r4/45-UI-SPEC.md
@.planning/phases/45-r4/45-PATTERNS.md
@.planning/notes/260627-cross-module-permission.md
@.planning/notes/260627-reconciliation-reuse-audit.md
@.planning/notes/asset-reconciliation-strategy.md

**Mandatory read_first list before implementation:**
- `internal/services/asset/reconciliation_service.go:128-200` — interface + constructor + impl struct
- `internal/services/asset/cache_keys.go:40-90` — cache key helpers + constants
- `internal/services/asset/reconciliation_statistics.go` — Summary / HealthTrend for COUNT pattern
- `internal/api/v1/asset/reconciliation_router.go` — SetupReconciliationRouter wiring
- `internal/api/v1/asset/reconciliation_handler.go:50-110` — ListExceptions / GetExceptionByID for handler pattern
- `internal/api/v1/asset/reconciliation_handler.go:110-166` — ResolveException (current shape: no InvalidateWorkstationHealth, no core field initialized in constructor)
- `internal/api/v1/operations/workstation_handler.go:15-30` — constructor + WithCore; lines 131-145 — GetByID
- `internal/api/router.go:591-602` — WorkstationHandler wiring; `internal/api/router.go:25` — `svc := asset.NewReconciliationService(core.DB.GetDB())` call site
- `internal/models/workstation.go` — Workstation struct definition
- `internal/services/asset/reconciliation_exception_matcher.go` — MatchException for IP CIDR matching
- `pkg/middleware/permission.go:1-100` — to identify existing `getUserIDAsString` + `checkUserPermission` helpers to reuse in the new permission_query_helper.go
- `pkg/middleware/permission_query_test.go` — test pattern for the new helper
- `.planning/notes/260627-cross-module-permission.md` — cross-module permission §2.3
- `xingran-react-frontend/src/store/menuStore.ts:18-50` — MenuState has `permissions: string[]` (NOT `authStore.perms`)
- `xingran-react-frontend/src/types/system.ts:12-34` — User interface has `permissions: string[]` (also not in store currently, but type-defined)
- `xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx:38` — `hasPermission(permissions, perm)` reference pattern
- `xingran-react-frontend/src/lib/queryKeys.ts:45-82` — reconciliation.* factories
- `xingran-react-frontend/src/lib/assetApi.ts:115-330` — reconciliationApi factory + ByWorkstationResponse type
- `xingran-react-frontend/src/pages/operations/workstations/index.tsx:539-551` — expandable pattern
- `xingran-react-frontend/src/components/operations/WorkstationDeviceTable/index.tsx:271-390` — createColumns factory
- `xingran-react-frontend/src/pages/asset/reconciliation/exception-rules/index.tsx:478-493` — actionTagColor helper
- `xingran-react-frontend/src/hooks/useDict.ts` — useDict for color list
- `xingran-react-frontend/src/hooks/useDashboard.ts:49-113` — useQuery parallel pattern
- `xingran-react-frontend/src/components/asset/reconciliation/MatchTestPanel.tsx:71-87` — useQuery matchTest pattern
</context>

<interfaces>
**From existing code (must NOT change):**

- `pkg/middleware/permission.go:checkUserPermission(core, userID, permission) bool` — internal helper, can be invoked from sibling file in same package
- `pkg/middleware/permission.go:getUserIDAsString(c) (string, bool)` — internal helper for userID extraction
- `pkg/middleware/permission.go:isSuperAdmin(core, userID) bool` — internal helper

**From Plan 01 outputs (must exist for Plan 02):**

- `pkg/middleware/permission_query_helper.go:HasUserPermission(c *gin.Context, perm string) bool`
  - Reuses `getUserIDAsString` + `isSuperAdmin` + `checkUserPermission` from sibling permission.go
  - Returns `true` when user has the permission or is super admin
  - Returns `false` when user not authenticated OR lacks permission
  - **Does NOT abort** — caller decides whether to 403, silently hide, or log

- `internal/services/asset/cache_keys.go:InvalidateWorkstationHealth(ctx context.Context, c cache.Cache, workstationID string) error`
  - Body: `return c.Delete(ctx, GetReconciliationHealthByWorkstationKey(workstationID))`
  - nil-check on both c and workstationID

**From internal/services/asset/reconciliation_service.go (existing):**

- `type ReconciliationService interface` at line 128 (ListExceptions / GetByID / ResolveException + others)
- `func NewReconciliationService(db *gorm.DB) ReconciliationService` at line 169 — signature change in Plan 01

**From internal/api/v1/asset/reconciliation_handler.go (existing):**

- `type ReconciliationHandler struct { service asset.ReconciliationService; core *core.Core }` at line 35-38
- `func NewReconciliationHandler(svc asset.ReconciliationService) *ReconciliationHandler` at line 40 — NOTE: core is set via `WithCore(core)` setter, NOT in constructor

**From internal/api/v1/operations/workstation_handler.go (existing):**

- `type WorkstationHandler struct { service opsServices.WorkstationService; core *core.Core }` at line 15
- `func NewWorkstationHandler(service opsServices.WorkstationService) *WorkstationHandler` at line 20
- `func (h *WorkstationHandler) WithCore(core *core.Core) *WorkstationHandler` at line 25

**From internal/api/router.go (existing):**

- Line 591: `workstationHandler := operations.NewWorkstationHandler(workstationService).WithCore(core)` (wiring point)
- Line 25: `svc := asset.NewReconciliationService(core.DB.GetDB())` (call site for constructor change)

**From xingran-react-frontend/src/store/menuStore.ts (CONFIRMED via grep):**

- `MenuState.permissions: string[]` at line 21
- `useMenuStore` selector: `useMenuStore((s) => s.permissions)`

**From CLAUDE.md (强约束):**

- operlog.Record(Module, OperType) — 11 关键词 + 25 OperType 常量
- Cache Key Prefix: `cache.Set` 自动加 `xingran:` 前缀；`cache.Delete` 自动剥离
- Status Convention: 0=启用 1=停用
- useEffect Dependencies: 严禁 inline-object deps；stable primitive deps 即可

**From project memory (MEMORY.md):**

- `stat-cards-from-list-length-capped-at-100` — KPI 数字必须用 COUNT 端点，不用 list.length
- `xingran-perm-namespace-split-readonly-page` — 静默降级而非 403
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 后端 — Permission helper + ReconciliationService.GetByWorkstation + 路由 + Handler + 跨模块注入</name>
  <files>
    - pkg/middleware/permission_query_helper.go
    - internal/services/asset/reconciliation_service.go
    - internal/services/asset/cache_keys.go
    - internal/api/v1/asset/reconciliation_router.go
    - internal/api/v1/asset/reconciliation_handler.go
    - internal/api/v1/operations/workstation_handler.go
    - internal/api/router.go
    - internal/models/workstation.go
    - internal/api/v1/operations/workstation_handler_test.go
  </files>
  <read_first>
    - pkg/middleware/permission.go (full file — to identify exported-vs-internal helpers)
    - pkg/middleware/permission_query_test.go (test pattern for new helper)
    - internal/services/asset/reconciliation_service.go:128-200 — interface + constructor + impl struct
    - internal/services/asset/cache_keys.go:40-90 — cache key helpers + constants
    - internal/services/asset/reconciliation_statistics.go — Summary / HealthTrend for COUNT pattern
    - internal/api/v1/asset/reconciliation_router.go — SetupReconciliationRouter wiring
    - internal/api/v1/asset/reconciliation_handler.go:35-50, 110-166 — handler pattern + existing ResolveException
    - internal/api/v1/operations/workstation_handler.go:15-30, 131-145 — constructor + GetByID
    - internal/api/router.go:591-602 — WorkstationHandler wiring; line 25 — ReconciliationService call site
    - internal/models/workstation.go — Workstation struct
    - internal/services/asset/reconciliation_exception_matcher.go — MatchException for IP CIDR matching
    - .planning/notes/260627-cross-module-permission.md — cross-module permission §2.3
  </read_first>
  <behavior>
    - pkg/middleware/permission_query_helper.go exports HasUserPermission(c, perm) bool — reusable, no scope creep
    - HasUserPermission reuses getUserIDAsString + isSuperAdmin + checkUserPermission from sibling permission.go
    - HasUserPermission does NOT abort the request — caller chooses how to handle false (silent hide, 403, log)
    - ReconciliationService interface 新增 GetByWorkstation(ctx, wsID, window) (*ByWorkstationResponse, error)
    - 构造函数 NewReconciliationService(db, cache) — cache 参数从 1 个增加到 2 个参数
    - ByWorkstationResponse 含 workstation / healthScore / assets / visible 字段（参 D-A4-02 锁定）
    - HealthScore 含 total/normal/drift/conflict/noData/exceptionHit/score/trend（参 D-A4-02）
    - score 公式：clamp(round((1 - 异常资产数/总资产数) × 100), 0, 100)，用 COUNT FILTER 不允许 list.length
    - assets[] 用 LEFT JOIN reconciliation_normalized ON workstation_id 一次拉取
    - CacheProvider.GetOrSet(key, TTL=5*time.Minute)（参 D-A4-03 与 R1 MV 刷新对齐）
    - cache_keys.go 新增 InvalidateWorkstationHealth helper（仅声明，调用属 Plan 02）
    - reconciliation_router.go 新增 POST /by-workstation 路由
    - reconciliation_handler.go 新增 GetByWorkstation handler — read 路径，无 operlog.Record；hasReconciliationPerm 用 HasUserPermission 而非 PermissionService.UserHasPerm（不存在）
    - workstation_handler.go 注入 ReconciliationService 字段 + WithReconciliationService 链式 setter
    - workstation_handler.go GetByID 调 hasReconciliationPerm 门控，无权限时 ReconciliationVisible=false（参 D-A1-03 静默降级）
    - router.go WorkstationHandler 链式注入 reconciliationSvc
    - models/workstation.go 新增 Reconciliation *asset.ByWorkstationResponse / ReconciliationVisible bool / ReconciliationHiddenReason string 字段
    - internal/api/v1/operations/workstation_handler_test.go 新增 TestWorkstationHandler_GetByID_WithReconciliation 测试 stub（参 W7）
  </behavior>
  <action>
    按以下顺序修改后端 9 个文件：

    1. **pkg/middleware/permission_query_helper.go**（新建）：
       - package middleware
       - import "github.com/gin-gonic/gin" + 现有 import（如 core）
       - 导出 `func HasUserPermission(c *gin.Context, perm string) bool`：
         * 调 getUserIDAsString(c)；取不到 → return false
         * 调 isSuperAdmin(core, userID) → return true
         * 调 checkUserPermission(core, userID, perm) → return result
       - **核心约定：不调 c.Abort()，不返回错误，不写日志** — 这是个"查询式"辅助函数，让 handler 自由决定 403 还是静默 hide
       - **重要**：因 permission.go 中 getUserIDAsString/checkUserPermission/isSuperAdmin 同包可见但非导出，HasUserPermission 也放在同一包内（同 package middleware）。**不要**把这些 helper 改成导出大写——会扩大 API surface，与项目"内部 helper 不导出"约定不符
       - JSDoc 风格注释引用 cross-module-permission.md §2.3 + D-A1-03

    2. **internal/services/asset/reconciliation_service.go**：
       - 在 interface 第 128 行附近新增方法签名：
         ```
         GetByWorkstation(ctx context.Context, wsID string, window string) (*ByWorkstationResponse, error)
         ```
       - 修改构造函数签名：`func NewReconciliationService(db *gorm.DB, c cache.Cache) ReconciliationService`，impl struct 增加 `cache cache.Cache` 字段
       - **不要修改** ResolveException 签名（参 B2 — 它已被 R2 cron 调用）
       - 在文件底部新增类型定义：
         ```
         type ByWorkstationResponse struct {
             Workstation  WorkstationBrief  `json:"workstation"`
             HealthScore  HealthScore       `json:"healthScore"`
             Assets       []AssetHealthItem `json:"assets"`
             Visible      bool              `json:"visible"`     // 由 handler 注入
         }
         type WorkstationBrief struct {
             ID   string `json:"id"`
             Name string `json:"name"`
             Code string `json:"code"`
         }
         type HealthScore struct {
             Total        int          `json:"total"`
             Normal       int          `json:"normal"`
             Drift        int          `json:"drift"`
             Conflict     int          `json:"conflict"`
             NoData       int          `json:"noData"`
             ExceptionHit int          `json:"exceptionHit"`
             Score        int          `json:"score"`
             Trend        []TrendPoint `json:"trend"`
         }
         // TrendPoint 复用 reconciliation_statistics.go 既有定义
         type AssetHealthItem struct {
             AssetID         string   `json:"assetId"`
             AssetCode       string   `json:"assetCode"`
             ConflictType    string   `json:"conflictType"`
             Severity        string   `json:"severity"`
             IP              string   `json:"ip,omitempty"`
             ExceptionRuleID *string  `json:"exceptionRuleId,omitempty"`
             AppliedActions  []string `json:"appliedActions,omitempty"`
             ConfidenceScore float64  `json:"confidenceScore"`
         }
         ```
       - 实现 `reconciliationServiceImpl.GetByWorkstation`：
         * 缓存 key 用 `GetReconciliationHealthByWorkstationKey(wsID)`
         * 用 `cache.GetOrSet(ctx, key, &dest, 5*time.Minute, queryFn)` 模式（参考 pkg/cache/redis.go 接口）
         * queryFn 内：
           - 用 GORM LEFT JOIN reconciliation_normalized (workstation_id = ?) 拉资产列表 + COUNT FILTER 按 conflict_type 分桶（normal/drift/conflict/nodata）— 严格使用 SELECT COUNT(*) FILTER (WHERE ...) 不允许 list.length
           - exceptionHit 字段通过调用 asset.ReconciliationExceptionMatcher.MatchException(assetIP, userID, conflictType) 计数
           - trend 数组复用 reconciliation_statistics.go:HealthTrend() 实现，按 date_trunc("day", detected_at) 聚合最近 7 天
           - **IP 解析链 inline 实现**（不要拆新文件，B5 修复）：按 strategy §4.4 顺序：asset.ip → workstation.ip → network_device.ip via port → "unknown"
           - **不实现 Visible 字段的赋值**：service 只返数据，handler 注入 visible — 保持 service 单一职责
       - 单元测试覆盖率见 Task 1 测试 stub（mock cache，验证 TTL=5min、cache hit、异常归零 score=100、score 公式）

    3. **internal/services/asset/cache_keys.go**：
       - 在文件 import 块新增 `"github.com/xingran-next/xingran-go-backend/pkg/cache"` 与 `"context"`
       - 在文件底部新增：
         ```
         func InvalidateWorkstationHealth(ctx context.Context, c cache.Cache, workstationID string) error {
             if c == nil || workstationID == "" {
                 return nil
             }
             return c.Delete(ctx, GetReconciliationHealthByWorkstationKey(workstationID))
         }
         ```
       - 注：此函数本 plan 仅定义，**实际调用在 Plan 02**（ResolveException + R2 scheduler）。grep 验证可放 Plan 02 验收

    4. **internal/api/v1/asset/reconciliation_router.go**：
       - 修改 ReconciliationService 构造函数调用为 `asset.NewReconciliationService(core.DB.GetDB(), core.Cache)`
       - 在 `/exception/:id/resolve` 注册行之后新增 `r.POST("/by-workstation", handler.GetByWorkstation)`

    5. **internal/api/v1/asset/reconciliation_handler.go**：
       - 新增 import: `"github.com/xingran-next/xingran-go-backend/pkg/middleware"`
       - 新增 handler 方法 `func (h *ReconciliationHandler) GetByWorkstation(c *gin.Context)`：
         * 定义本地 struct `{ WorkstationID string \`json:"workstationId" binding:"required"\`; Window string \`json:"window"\` }`
         * `c.ShouldBindJSON(&req)` 校验
         * window 为空时设默认 "7d"
         * 调 `h.service.GetByWorkstation(ctx, req.WorkstationID, req.Window)` 拿数据
         * 注入 `result.Visible = middleware.HasUserPermission(c, "asset:reconciliation:list")` — 用新 helper 而非不存在的 PermissionService
         * 若 `!result.Visible`，把 `result.Assets` 设为空切片（避免返回数据但前端 hide — B3 安全 invariant）
         * `response.Success(c, result)` — 不调 operlog.Record（读路径）
       - 新增私有方法 `func (h *ReconciliationHandler) hasReconciliationPerm(c *gin.Context) bool { return middleware.HasUserPermission(c, "asset:reconciliation:list") }`（便于同 handler 内其他读端点复用）
       - **不要**在 Plan 01 修改 ResolveException — 缓存失效归 Plan 02（B1/B2 锁定）

    6. **internal/api/v1/operations/workstation_handler.go**：
       - 在 struct 第 15 行附近新增 `reconciliationSvc asset.ReconciliationService` 字段
       - 新增链式 setter `func (h *WorkstationHandler) WithReconciliationService(svc asset.ReconciliationService) *WorkstationHandler`（与 WithCore 同模式）
       - 修改 `GetByID`（第 131-140 行）：
         * 现有 `workstation, err := h.service.GetByID(ctx, id)` 之后
         * 加：
           ```
           visible := h.hasReconciliationPerm(c)
           workstation.ReconciliationVisible = visible
           if visible {
               if recon, err := h.reconciliationSvc.GetByWorkstation(ctx, id, "7d"); err == nil {
                   workstation.Reconciliation = recon
               } else {
                   // service 失败但有权限 — ReconciliationVisible=true 但 Reconciliation=nil（前端会渲染错误态）
                   applogger.Warnf("[workstation:reconciliation] GetByWorkstation 失败 workstationID=%s: %v", id, err)
               }
           } else {
               workstation.ReconciliationHiddenReason = "无资产对账查看权限"
           }
           ```
         * `response.Success(c, workstation)`
       - 新增私有方法：
         ```
         func (h *WorkstationHandler) hasReconciliationPerm(c *gin.Context) bool {
             return middleware.HasUserPermission(c, "asset:reconciliation:list")
         }
         ```
       - import 块新增 middleware 包

    7. **internal/api/router.go**：
       - 第 591 行附近修改为：
         ```
         reconciliationSvc := asset.NewReconciliationService(core.DB.GetDB(), core.Cache)
         workstationHandler := operations.NewWorkstationHandler(workstationService).
             WithCore(core).
             WithReconciliationService(reconciliationSvc)
         ```
       - 注意：`asset` 包已被现有代码 import，无新增 import

    8. **internal/models/workstation.go**：
       - 在 Workstation struct 添加字段：
         ```
         Reconciliation          *asset.ByWorkstationResponse `json:"reconciliation,omitempty"`
         ReconciliationVisible   bool                        `json:"reconciliationVisible"`
         ReconciliationHiddenReason string                    `json:"reconciliationHiddenReason,omitempty"`
         ```
       - 若 import 循环冲突（models → asset services），改用 `Reconciliation map[string]interface{}` 弱类型 + handler 层做类型转换；首选前者（asset 包内 ByWorkstationResponse 是纯类型，不带 db 依赖，理论上不致循环）
       - 若确认循环，备选：在 `internal/api/v1/asset/reconciliation_types.go` 定义 `ByWorkstationResponse`，Workstation 引用该路径而非 asset

    9. **internal/api/v1/operations/workstation_handler_test.go**（新建 — 参 W7）：
       - stub 测试覆盖 GetByID with reconciliation：
         ```
         func TestWorkstationHandler_GetByID_WithReconciliation(t *testing.T) {
             // mock workstationService + reconciliationService
             // case 1: user has perm → reconciliationVisible=true, reconciliation populated
             // case 2: user lacks perm → reconciliationVisible=false, reconciliation=nil, hiddenReason="无资产对账查看权限"
             // case 3: user has perm but service fails → reconciliationVisible=true, reconciliation=nil (degrade gracefully)
         }
         ```
       - 简化 mock：用 sqlite + gorm 内存 DB 初始化 workstation 表，注入假 service 实现
  </action>
  <verify>
    <automated>go build ./...</automated>
    <automated>go test ./internal/services/asset/... -count=1 -timeout=60s</automated>
    <automated>go test ./internal/api/v1/asset/... -count=1 -timeout=60s</automated>
    <automated>go test ./internal/api/v1/operations/... -count=1 -timeout=60s -run Workstation</automated>
    <automated>grep -rn "PermissionService.UserHasPerm" internal/ | grep -v "_test.go" | wc -l</automated>
    <automated>grep -rn "HasUserPermission" internal/ pkg/ | wc -l</automated>
    <automated>grep -n "by-workstation" internal/api/v1/asset/reconciliation_router.go</automated>
  </verify>
  <done>
    - go build ./... exit 0
    - 服务既有测试不回归（PASS）
    - Plan 01 不引入对不存在的 PermissionService.UserHasPerm 的引用（grep count = 0）
    - HasUserPermission 在 pkg/middleware/permission_query_helper.go 中导出
    - POST /asset/reconciliation/by-workstation 路由就位（router.go 含 "by-workstation" 字串）
    - WorkstationHandler.GetByID 调 hasReconciliationPerm 门控（workstation_handler.go 含 "hasReconciliationPerm" + "ReconciliationVisible"）
    - reconciliation_service.go 含 GetByWorkstation 方法 + ByWorkstationResponse/HealthScore/AssetHealthItem 类型
    - cache_keys.go 含 InvalidateWorkstationHealth 函数（虽然本 plan 不调用，定义先行）
    - TestWorkstationHandler_GetByID_WithReconciliation 通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 前端 — queryKeys + assetApi + 4 组件 + 4 hooks + barrel + 整合点（工位 expand / WorkstationDeviceTable / 资产列表）</name>
  <files>
    - xingran-react-frontend/src/lib/queryKeys.ts
    - xingran-react-frontend/src/lib/assetApi.ts
    - xingran-react-frontend/src/components/reconciliation/HealthCard.tsx
    - xingran-react-frontend/src/components/reconciliation/HealthBadge.tsx
    - xingran-react-frontend/src/components/reconciliation/ReconciliationDrawer.tsx
    - xingran-react-frontend/src/components/reconciliation/ReconciliationTimeline.tsx
    - xingran-react-frontend/src/components/reconciliation/ExceptionMatchList.tsx
    - xingran-react-frontend/src/components/reconciliation/index.ts
    - xingran-react-frontend/src/components/reconciliation/hooks/useWorkstationHealth.ts
    - xingran-react-frontend/src/components/reconciliation/hooks/useAssetHealth.ts
    - xingran-react-frontend/src/components/reconciliation/hooks/useExceptionMatch.ts
    - xingran-react-frontend/src/components/reconciliation/hooks/useReconciliationVisibility.ts
    - xingran-react-frontend/src/pages/operations/workstations/index.tsx
    - xingran-react-frontend/src/pages/operations/workstations/views/CardView.tsx
    - xingran-react-frontend/src/components/operations/WorkstationDeviceTable/index.tsx
    - xingran-react-frontend/src/pages/operations/assets/index.tsx
  </files>
  <read_first>
    - xingran-react-frontend/src/lib/queryKeys.ts (lines 45-82 reconciliation.* factories)
    - xingran-react-frontend/src/lib/assetApi.ts (lines 115-330 reconciliationApi factory + ByWorkstationResponse/HealthScore/AssetHealthItem/TrendPoint 类型定义)
    - xingran-react-frontend/src/store/menuStore.ts (lines 18-50 — MenuState with `permissions: string[]`)
    - xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx:38 — `hasPermission(permissions, perm)` pattern
    - xingran-react-frontend/src/pages/operations/workstations/index.tsx (lines 539-551 expandable pattern)
    - xingran-react-frontend/src/pages/operations/workstations/views/CardView.tsx (CardView expand pattern)
    - xingran-react-frontend/src/components/operations/WorkstationDeviceTable/index.tsx (createColumns factory line 271-390)
    - xingran-react-frontend/src/pages/operations/assets/index.tsx (assets list columns)
    - xingran-react-frontend/src/pages/asset/reconciliation/exception-rules/index.tsx (lines 248-258 Tag color listClass map; lines 478-493 actionTagColor helper)
    - xingran-react-frontend/src/hooks/useDict.ts (useDict for "asset_reconciliation_conflict_type" / "asset_reconciliation_severity")
    - xingran-react-frontend/src/hooks/useDashboard.ts (lines 49-113 useQuery parallel pattern)
    - xingran-react-frontend/src/components/asset/reconciliation/MatchTestPanel.tsx (lines 71-87 useQuery matchTest pattern + line 80 Skeleton active)
    - .planning/phases/45-r4/45-UI-SPEC.md (Component Inventory + Copywriting Contract + Color mapping)
    - .planning/phases/45-r4/45-PATTERNS.md (lines 340-980 component pattern details)
  </read_first>
  <behavior>
    - queryKeys.ts reconciliation.* 新增 workstationHealth(workstationId) + assetHealth(assetId) factory
    - assetApi.ts 新增 ByWorkstationResponse / HealthScore / AssetHealthItem 类型 + reconciliationApi.byWorkstation({workstationId, window}) 方法
    - HealthCard.tsx: antd Card 包含 5 KPI + 趋势 mini chart + 得分；空态/加载/错误态如 UI-SPEC；useReconciliationVisibility 返回 false 时 render null；React.memo 包装；useEffect deps 仅 workstationId 字符串
    - HealthBadge.tsx: 8px 圆点 + useDict("asset_reconciliation_conflict_type") listClass 颜色映射；6 种类型 A-F 不同颜色；Tooltip 鼠标悬停 1s 显示；点击 → openDrawer；useReconciliationVisibility 返回 false 时 render "-" 占位；React.memo 包装
    - ReconciliationDrawer.tsx: 780px 宽 + extra 区域含"申请例外"按钮 + 三 Tab；申请例外按钮 navigate 时 query string 传 workstationId + assetIp + conflictType（参 B + D-A1-03 + UI-SPEC）
    - ReconciliationTimeline.tsx: antd Timeline mode="left"；纯文本展示；resolved_by 用户名 JOIN sys_user 后展示；dayjs 格式化时间
    - ExceptionMatchList.tsx: antd List 渲染命中的例外规则；actionTagColor helper 从 exception-rules/index.tsx:478 复制
    - useWorkstationHealth.ts: useQuery + reconciliationApi.byWorkstation + staleTime 5min + gcTime 10min + refetchOnWindowFocus:false + enabled: visible && Boolean(workstationId) && data?.visible !== false（参 W3 defense-in-depth）
    - useAssetHealth.ts: 从 useWorkstationHealth 缓存取单资产切片（避免 N+1）
    - useExceptionMatch.ts: useQuery + reconciliationApi.exceptionRule.test + staleTime 5min
    - **useReconciliationVisibility.ts: 从 useMenuStore.permissions 读权限**（不是 authStore.perms，参 B4）
    - **workstations/index.tsx expand 顶层嵌入 HealthCard；在 page level 用 useWorkstationHealth(workstationId) 一次，把 data?.assets 提取为 Map<assetId, conflictType> 通过 prop 传给 WorkstationDeviceTable**（参 B6 修正 N+1 + lift state）
    - WorkstationDeviceTable/index.tsx createColumns 内对账健康列 render HealthBadge，conflictType 从 props.conflictTypeMap 取
    - assets/index.tsx columns 内对账健康列 render HealthBadge（每行 useAssetHealth 调用即可，单资产查询低开销）
    - index.ts barrel 导出全部组件 + hooks
  </behavior>
  <action>
    按以下顺序实现前端 16 个文件，每个文件具体动作为：

    1. **src/lib/queryKeys.ts**：
       - 在 reconciliation 对象（约 76 行 matchTest 之后）新增两个 factory：
         ```
         workstationHealth: (workstationId: string) => ["reconciliation", "workstation-health", workstationId] as const,
         assetHealth: (assetId: string) => ["reconciliation", "asset-health", assetId] as const,
         ```

    2. **src/lib/assetApi.ts**：
       - 在文件顶部（reconciliationApi 工厂之前）新增三个 interface：`ByWorkstationResponse`、`HealthScore`、`AssetHealthItem`（字段严格按 UI-SPEC "API Contract" 锁定）
       - 在 reconciliationApi 对象（约 line 290 之后）新增方法 `byWorkstation`：
         ```
         byWorkstation: async (data: { workstationId: string; window?: string }): Promise<ByWorkstationResponse> => {
           const res = await post<ByWorkstationResponse>("/asset/reconciliation/by-workstation", {
             workstationId: data.workstationId,
             window: data.window ?? "7d",
           });
           return res.data as ByWorkstationResponse;
         },
         ```

    3. **src/components/reconciliation/hooks/useReconciliationVisibility.ts**（参 B4 修复）：
       - 从 `@/store/menuStore` import `useMenuStore`（不是 authStore — authStore 没有 perms 字段）
       - 常量 `REQUIRED_PERM = "asset:reconciliation:list"`
       - 返回 `useMenuStore((s) => s.permissions)?.includes(REQUIRED_PERM) ?? false`
       - JSDoc 说明"D-A1-03 静默降级"语义
       - 理由：menuStore.permissions 在 login 时通过 `getUserPermissions()` API 加载并缓存（menuStore.ts:114-121），是当前唯一可用的前端 perm 来源

    4. **src/components/reconciliation/hooks/useWorkstationHealth.ts**（参 W3 修复）：
       - 5 个 import: useQuery, reconciliationApi, ByWorkstationResponse (来自 assetApi), queryKeys, useReconciliationVisibility
       - STALE_TIME_MS = 5*60*1000; GC_TIME_MS = 10*60*1000
       - 三段门控（defense-in-depth per UI-SPEC §"Cross-Module Permission Degradation"）：
         ```
         return useQuery({
           queryKey: queryKeys.reconciliation.workstationHealth(workstationId),
           queryFn: () => reconciliationApi.byWorkstation({ workstationId, window: "7d" }),
           enabled: visible === true && Boolean(workstationId) && data?.visible !== false,
           staleTime: STALE_TIME_MS,
           gcTime: GC_TIME_MS,
           refetchOnWindowFocus: false,
         })
         ```
       - 注：`data?.visible !== false` 引用 useQuery 返回的 cache data，对首次未拉到数据时（undefined）放行；后续若 backend 报告 visible=false 立即停 refetch

    5. **src/components/reconciliation/hooks/useAssetHealth.ts**：
       - 内部直接调用 useWorkstationHealth(workstationId) 拿 data
       - 在 data?.assets 中 find(assetId)，返回 AssetHealthItem | undefined
       - 单资产切片无新 API 调用（避免 N+1 参 SC7）
       - 用于 ReconciliationDrawer Tab 1，从父级 lift-up 的 cache 取数

    6. **src/components/reconciliation/hooks/useExceptionMatch.ts**：
       - 复用 MatchTestPanel.tsx 模式：useQuery + reconciliationApi.exceptionRule.test + queryKey 用 useMemo 稳定
       - enabled: Boolean(params.ip)；staleTime 5min；gcTime 10min

    7. **src/components/reconciliation/HealthBadge.tsx**：
       - React.memo(props: { assetId: string; conflictType: string | null; onClick: (assetId, conflictType) => void })
       - 内部 useDict("asset_reconciliation_conflict_type") + useMemo 颜色映射
       - colorForConflict：A 或 null → 绿（健康）；其他查 dict listClass
       - 渲染：8px 圆点 + Tooltip(mouseEnterDelay=1) 包裹；B-F 时 cursor pointer + onClick；A 或 null 时 cursor default
       - useReconciliationVisibility() 返回 false 时 render <>{"-"}</>（保持列宽一致，参 D-A1-03 + UI-SPEC）
       - a11y：冲突类型存在时 role="button" tabIndex=0 + Enter 键激活；健康时 role="img" aria-label="健康"
       - JSDoc 引用 D-A2-04 + D-A1-03

    8. **src/components/reconciliation/HealthCard.tsx**：
       - React.memo(props: { workstationId: string; onApplyException: () => void })
       - 内部 useWorkstationHealth(workstationId)
       - useReconciliationVisibility() 返回 false 时 render null
       - 加载态：Skeleton active paragraph={{rows:2}}
       - 空态（data.healthScore.total === 0）："该工位暂无关联资产。" 居中文本
       - 错误态：Result status="error" + 重试按钮
       - 主布局：antd Card title="对账健康度" + Tag "本周" + score Statistic（30px bold + 颜色按 score band：≥80 绿/60-79 黄/<60 红）+ 两个链接
       - 5 KPI grid：Row gutter={16} + 5 Col span={4}，每格 antd Statistic（title=label, valueStyle={fontSize:24,fontWeight:600}），5 个标签依次为：正常/漂移/冲突/无数据/例外命中
       - 趋势 mini chart：ReactECharts style={{height:56, width:"100%"}} opts={{renderer:"svg"}}；单 line series + 7 TrendPoint，无坐标轴无图例无 tooltip
       - 申请例外按钮 onClick 调 props.onApplyException 跳转路由
       - useEffect deps：仅 workstationId 字符串

    9. **src/components/reconciliation/ReconciliationTimeline.tsx**：
       - props: { records: Array<{id, conflictType, detectedAt, resolvedAt, resolvedByUsername, resolutionNote}>, loading: boolean }
       - 加载态：Skeleton active paragraph={{rows:3}}
       - 空态：Empty description="该资产暂无已解决的冲突记录。"
       - 主布局：antd Timeline mode="left"
       - 每项 Timeline.Item color={CONFLICT_TYPE_HEX[conflictType]} dot={<HealthBadge conflictType={type} interactive={false} size={12} />}>
         * 行 1：`{CONFLICT_TYPE_LABEL[type]} · {dayjs(detectedAt).format("YYYY-MM-DD HH:mm")}`
         * 行 2：`由 {resolvedByUsername} 于 {dayjs(resolvedAt).format("YYYY-MM-DD HH:mm")} 解决`
         * 行 3：fontSize 12, color "#6b7280"，"说明: {resolutionNote || "(无)"}"
       - D-A3-02 锁定：raw_snapshot 不展开

    10. **src/components/reconciliation/ExceptionMatchList.tsx**：
        - props: { rules: Array<ExceptionRule>, loading: boolean, onCreateRule: () => void }
        - 复制 actionTagColor helper（从 exception-rules/index.tsx:478，完整 switch case 不修改）
        - 加载态：Spin
        - 空态：Empty description="当前没有例外规则覆盖该资产所在 IP 段。" + Button onClick={onCreateRule}>去创建例外规则</Button>
        - 主布局：antd List size="small"
        - 每项 List.Item：3 行 — (1) rule.name + scope Tag；(2) <code>{rule.ipRange}</code>；(3) Space 包 actions Tag + reason + 有效期

    11. **src/components/reconciliation/ReconciliationDrawer.tsx**：
        - props: { open: boolean; onClose: () => void; selectedAssetId: string | null; workstationId: string | null; activeTab: "summary" | "timeline" | "exception"; onTabChange: (key) => void; onApplyException: () => void }
        - width=780
        - title：动态 — 资产行 → "资产对账详情 — {assetCode}"；工位 expand → "工位对账详情 — {workstationName}"
        - extra={<Button type="primary" onClick={onApplyException}>申请例外</Button>}
        - Tabs activeKey={activeTab} onChange={onTabChange} tabBarGutter={24}
        - Tab 1（冲突摘要）：从 useAssetHealth(selectedAssetId, workstationId) 拿数据，渲染 severity Tag + conflict type Tag + IP + responsibleUserId + confidenceScore + appliedActions
        - Tab 2（历史变更）：渲染 ReconciliationTimeline
        - Tab 3（例外规则）：渲染 ExceptionMatchList
        - useReconciliationVisibility() 返回 false 时 render null
        - 关闭时 onClose + 重置 activeTab

    12. **src/components/reconciliation/index.ts**：
        - barrel 导出：HealthCard / HealthBadge / ReconciliationDrawer / ReconciliationTimeline / ExceptionMatchList + 4 hooks

    13. **src/pages/operations/workstations/index.tsx**（参 B6 修复 — lift state）：
        - import HealthCard + ReconciliationDrawer + useReconciliationVisibility + useWorkstationHealth from "@/components/reconciliation"
        - **关键变更**：在 page level 加 lift state（不是子组件内）：
          ```
          const expandedWorkstationId: string | null = <从 Table expandable.expandedRowKeys 读>;
          const workstationHealthQuery = useWorkstationHealth(expandedWorkstationId ?? "");
          const assetConflictMap = useMemo(() => {
            const m = new Map<string, string>();
            (workstationHealthQuery.data?.assets ?? []).forEach((a) => m.set(a.assetId, a.conflictType));
            return m;
          }, [workstationHealthQuery.data?.assets]);
          ```
        - 增加 page-level drawer state：`const [drawerState, setDrawerState] = useState<{open: boolean; assetId: string | null; workstationId: string | null}>(...)`
        - expandable.exandedRowRender 改为：
          ```
          expandedRowRender: (record: WorkstationOps) => (
            <div>
              <HealthCard workstationId={record.id} onApplyException={() => navigate(`/asset/reconciliation/exception-rules/new?workstationId=${record.id}`)} />
              <WorkstationDeviceTable workstationId={record.id} conflictTypeMap={assetConflictMap} onDeviceChange={refreshData} onBadgeClick={(assetId, conflictType) => setDrawerState({open: true, assetId, workstationId: record.id})} />
            </div>
          ),
          ```
        - 在 page 末尾 render ReconciliationDrawer 一次：`<ReconciliationDrawer open={drawerState.open} onClose={...} selectedAssetId={drawerState.assetId} workstationId={drawerState.workstationId} ... />`

    14. **src/pages/operations/workstations/views/CardView.tsx**：
        - 同 workstations/index.tsx 模式（若 CardView 有自己的 expand 区域，参 UI-SPEC "CardView/FloorPlanView parity"）

    15. **src/components/operations/WorkstationDeviceTable/index.tsx**（参 B6 修复）：
        - import HealthBadge from "@/components/reconciliation"
        - **修改 props**：WorkstationDeviceTable 接收 `conflictTypeMap: Map<string, string>` + `onBadgeClick: (assetId, conflictType) => void`（由父级 workstations/index.tsx lift 传入）
        - **不要**在组件内 useWorkstationHealth（已在父级 lift）
        - 在 createColumns(canEdit) 内、状态列与主设备列之间新增列：
          ```
          { title: "对账健康", key: "reconciliation", width: 96, render: (_, record) => (
              <HealthBadge
                assetId={record.assetId}
                conflictType={conflictTypeMap.get(record.assetId) ?? null}
                onClick={(id, t) => onBadgeClick(id, t)}
              />
          ) }
          ```

    16. **src/pages/operations/assets/index.tsx**：
        - import HealthBadge + useReconciliationVisibility + useWorkstationHealth
        - 资产列表 columns 数组内、资产编号列与资产类型列之间新增列：
          ```
          { title: "对账健康", key: "reconciliation", width: 96, render: (_, record) => {
              // 资产行点击：在 open drawer 时再触发 byWorkstation（用 record.workstationId）
              // 列表本身只展示占位 badge，避免 N+1
              // 单资产查询在 drawer 打开后由 useAssetHealth(workstationId) 拿
              return <HealthBadge assetId={record.id} conflictType={null} onClick={(id) => setDrawerState({open: true, assetId: id, workstationId: record.workstationId})} />;
          } }
          ```
        - page level render ReconciliationDrawer（同 workstations 模式）
        - 抽屉打开时 useAssetHealth(workstationId) 拉数据再回填 conflictType

    **关键约定**（参 CLAUDE.md 强约束）：
    - useEffect deps 必须稳定；所有 useQuery 必须在 workstationId/assetId 是 primitive 时使用
    - 任何对象/数组 deps 必须 useMemo 包裹
    - HealthBadge / HealthCard / Drawer 必须 React.memo 包裹
    - useReconciliationVisibility 必须使用 useMenuStore.permissions（不是 authStore.perms，B4 修复）
    - useWorkstationHealth 必须带三段门控（visible + workstationId + data?.visible !== false）
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npm run lint</automated>
    <automated>cd xingran-react-frontend && npm run type-check</automated>
    <automated>cd xingran-react-frontend && npm run build</automated>
    <automated>grep -rn "authStore.*perms\|state.perms" xingran-react-frontend/src/components/reconciliation/ 2>&1 | wc -l</automated>
    <automated>grep -rn "useMenuStore.*permissions" xingran-react-frontend/src/components/reconciliation/hooks/useReconciliationVisibility.ts</automated>
    <automated>grep -n "<HealthCard" xingran-react-frontend/src/pages/operations/workstations/index.tsx</automated>
    <automated>grep -n "<HealthBadge" xingran-react-frontend/src/components/operations/WorkstationDeviceTable/index.tsx</automated>
    <automated>grep -n "<HealthBadge" xingran-react-frontend/src/pages/operations/assets/index.tsx</automated>
    <automated>grep -n "conflictTypeMap" xingran-react-frontend/src/pages/operations/workstations/index.tsx xingran-react-frontend/src/components/operations/WorkstationDeviceTable/index.tsx</automated>
  </verify>
  <done>
    - npm run build exit 0（无 type 错误，无 lint 错误）
    - queryKeys.ts 含 workstationHealth + assetHealth factory
    - assetApi.ts 含 ByWorkstationResponse/HealthScore/AssetHealthItem 类型 + byWorkstation 方法
    - workstations/index.tsx 含 `<HealthCard` 字串 + conflictTypeMap lift
    - WorkstationDeviceTable/index.tsx 含 `<HealthBadge` 字串 + conflictTypeMap prop
    - assets/index.tsx 含 `<HealthBadge` 字串
    - 所有新组件调用 useReconciliationVisibility 门控（D-A1-03 静默降级）
    - useReconciliationVisibility 引用 useMenuStore.permissions（grep count = 1 in that file；其他 reconciliation 目录不应有 `state.perms` 或 `authStore.perms`）
    - useEffect/useQuery deps 仅 primitive 或 useMemo 包裹的稳定引用
    - useWorkstationHealth 含三段 enabled gate
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Gin handler | Untrusted JSON body {workstationId, window} crosses here |
| ops WorkstationHandler → asset ReconciliationService | Cross-module service injection crosses here |
| Backend → Frontend cache | ByWorkstationResponse cached server-side; visible flag must mirror handler perm check |
| Frontend menuStore.permissions → useReconciliationVisibility hook | Permission gating crosses here (NOT authStore which lacks `perms`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | POST /asset/reconciliation/by-workstation body | mitigate | binding:"required" on workstationId; UUID format validated via existing workstation_service.GetByID path (returns 404 for non-UUID); window enum default to "7d" |
| T-45-02 | Information Disclosure | ReconciliationVisible flag in Workstation response | mitigate | Handler-side hasReconciliationPerm gates via pkg/middleware.HasUserPermission; if no perm, ReconciliationVisible=false and Reconciliation field omitted via omitempty; assets array set to empty (not returned); no 403 returned (silent hide per D-A1-03) |
| T-45-03 | Elevation of Privilege | Cross-module ReconciliationService injection in WorkstationHandler | mitigate | Service is read-only (GetByWorkstation); no write methods exposed; hasReconciliationPerm enforces RBAC at handler boundary |
| T-45-04 | Denial of Service | Cache key collision via crafted workstationId | mitigate | workstationId validated as UUID by upstream GetByID; cache key uses helper that requires non-empty string; no user-controlled segments |
| T-45-05 | Repudiation | ResolveException success path | mitigate | **Plan 02 scope** — operlog.Record + InvalidateWorkstationHealth sequence enforced there |
| T-45-06 | Side-channel | HealthBadge render reveals perm state to user with no perm | mitigate | HealthBadge returns "-" placeholder when useReconciliationVisibility===false; backend visible flag mirrors; no log noise, no 403 |
| T-45-09 | Spoofing | menuStore.permissions cache vs backend visible flag mismatch | mitigate | Frontend defense-in-depth: enabled gate has `data?.visible !== false` clause — when backend visible===false, query auto-disables regardless of menuStore.permissions; matches UI-SPEC §"Defense in depth" |
| T-45-10 | Tampering | Cache TTL too long vs workorder/resolve update | mitigate | **Plan 02 scope** — InvalidateWorkstationHealth called from ResolveException + R2 scheduler |
| T-45-SC | Tampering | npm install / package additions | mitigate | R4 adds zero new npm packages; no install gate needed |
</threat_model>

<verification>
[Overall phase checks]
- `go build ./...` exits 0
- `cd xingran-react-frontend && npm run build` exits 0
- `cd xingran-react-frontend && npm run lint` exits 0
- `cd xingran-react-frontend && npm run type-check` exits 0
- `go test ./internal/services/asset/... -count=1` passes
- `go test ./internal/api/v1/asset/... -count=1` passes
- `go test ./internal/api/v1/operations/... -count=1 -run Workstation` passes
- `go test ./pkg/middleware/... -count=1` passes (new HasUserPermission helper tested via existing pattern)
- POST /asset/reconciliation/by-workstation returns ByWorkstationResponse{workstation,healthScore,assets,visible} with visible=true for perm-holding user, visible=false (and Assets=[]) for no-perm user
- WorkstationHandler.GetByID response includes reconciliationVisible field reflecting HasUserPermission
- HealthCard renders in workstation expand panel above WorkstationDeviceTable
- HealthBadge renders as 8px dot in WorkstationDeviceTable asset/AD sub-tables + assets page list
- ReconciliationDrawer opens at 780px width with 3 Tabs
- grep confirms: zero references to `authStore.perms` or `state.perms` in src/components/reconciliation/
- grep confirms: useMenuStore.permissions is the source for useReconciliationVisibility
- grep confirms: useWorkstationHealth has triple-enabled gate (visible + workstationId + data?.visible !== false)
- grep confirms: workstations/index.tsx contains `<HealthCard` and `conflictTypeMap` lift wiring
</verification>

<success_criteria>
[Measurable completion — Plan 01]
- INTEGRATE-01: 工位详情页顶部 HealthCard 显示（5 KPI + 趋势 mini chart）
- INTEGRATE-02: 资产设备子表 + 域控设备子表"对账健康"列行内徽标显示
- INTEGRATE-03: 点击徽标打开 ReconciliationDrawer（冲突摘要 / 历史变更 / 例外规则三 Tab）
- ROADMAP SC1: HealthCard renders null when useReconciliationVisibility === false (silent hide)
- ROADMAP SC4: 资产详情页 /asset/card/:id 顶部摘要块（由 drawer 顶部承担，参 W6）
- ROADMAP SC6: 无 asset:reconciliation:list 权限时静默隐藏（非 403）— 由 menuStore.permissions 门控 + HasUserPermission 后端门控双重确认
- ROADMAP SC7: 工位 N+1 查询优化（后端聚合 API 一次拿完 healthScore + assets；前端通过 lift useWorkstationHealth + conflictTypeMap 避免列内 N+1）
- ROADMAP SC10: npm run build + go build ./... 全通过

**Out-of-scope for Plan 01 (moved to Plan 02):**
- Cache invalidation on ResolveException (T-45-05, T-45-10)
- R2 scheduler cache invalidation on createWorkorder (T-45-05)
- IP resolution chain in GetByWorkstation queryFn (Plan 02 Task 1 may extract or inline)
- VERIFICATION.md (Plan 02 Task 2)
- operlog full coverage audit (Plan 02 Task 1)
</success_criteria>

<output>
Create `.planning/phases/45-r4/45-01-SUMMARY.md` when done
</output>