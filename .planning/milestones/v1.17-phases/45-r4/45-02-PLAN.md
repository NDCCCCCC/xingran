---
phase: 45-r4
plan: 02
type: execute
wave: 2
depends_on:
  - 45-r4/45-01
files_modified:
  - internal/scheduler/reconciliation_tasks.go
  - internal/services/asset/reconciliation_workorder.go
  - internal/services/asset/reconciliation_service.go
  - internal/api/v1/asset/reconciliation_handler.go
  - xingran-react-frontend/src/components/reconciliation/ExceptionMatchList.tsx
  - xingran-react-frontend/src/components/reconciliation/ReconciliationDrawer.tsx
  - xingran-react-frontend/src/components/reconciliation/__tests__/HealthCard.test.tsx
  - xingran-react-frontend/src/components/reconciliation/hooks/__tests__/useReconciliationVisibility.test.ts
  - .planning/notes/260627-cross-module-permission.md
  - .planning/phases/45-r4/45-VERIFICATION.md
autonomous: true
requirements:
  - INTEGRATE-01
  - INTEGRATE-02
  - INTEGRATE-03
must_haves:
  truths:
  - "R2 auto-workorder create (critical/high) 调用 InvalidateWorkstationHealth"
  - "R2 resolve API 调用 InvalidateWorkstationHealth（顺序：service.ResolveException → InvalidateWorkstationHealth → operlog.Record → response.Success）"
  - "工位 IP 解析链（asset.ip → workstation.ip → network_device.ip via port → unknown）实装于 GetByWorkstation service"
  - "operlog 记录覆盖所有写路径：ResolveException + R2 createWorkorderCritical/High"
  - "VERIFICATION.md 记录 R4 全部 10 条 ROADMAP SC 通过证据（命令 + 期望 + 状态）"
  - "go build + npm run build + 既有测试 PASS（无回归）"
  - "Plan 02 不修改 ResolveException service 签名（参 B2 — 不破坏 R2 cron 现有调用）"
  - "Plan 02 也不修改 CreateWorkorderFromException 签名 — 仅新增 WorkstationIDForException 旁路查询方法"
artifacts:
  - path: internal/services/asset/reconciliation_workorder.go
    provides: "新增 WorkstationIDForException(ctx, exceptionID) (string, error) 旁路方法（不动 CreateWorkorderFromException 签名）"
    contains: "func.*WorkstationIDForException"
  - path: internal/scheduler/reconciliation_tasks.go
    provides: "createWorkorderBySeverity 调用 InvalidateWorkstationHealth"
    contains: "InvalidateWorkstationHealth"
  - path: internal/api/v1/asset/reconciliation_handler.go
    provides: "ResolveException success path 顺序：service → invalidate → operlog → response"
  - path: internal/services/asset/reconciliation_service.go
    provides: "GetByWorkstation queryFn 集成 IP 解析链 inline + MatchException 计数 exceptionHit"
  - path: .planning/notes/260627-cross-module-permission.md
    provides: "R4 实际接入清单（WorkstationHandler.GetByID + 路由 + Drawer 双重防御）"
  - path: .planning/phases/45-r4/45-VERIFICATION.md
    provides: "R4 全部 10 条 ROADMAP SC 验收证据（命令 + 期望 + 实际输出 + PASS/FAIL）"
  - path: xingran-react-frontend/src/components/reconciliation/__tests__/HealthCard.test.tsx
    provides: "HealthCard 单元测试覆盖可见/隐藏/空态"
  - path: xingran-react-frontend/src/components/reconciliation/hooks/__tests__/useReconciliationVisibility.test.ts
    provides: "useReconciliationVisibility 单元测试覆盖有 perm/无 perm/undefined"
key_links:
  - from: internal/scheduler/reconciliation_tasks.go
    to: internal/services/asset/cache_keys.go
    via: "createWorkorderBySeverity 调用 woSvc.CreateWorkorderFromException 后调 InvalidateWorkstationHealth"
    pattern: "InvalidateWorkstationHealth"
  - from: internal/services/asset/reconciliation_workorder.go
    to: internal/services/asset/reconciliation_service.go
    via: "WorkstationIDForException 查 reconciliation_normalized.workstation_id"
    pattern: "WorkstationIDForException"
  - from: internal/api/v1/asset/reconciliation_handler.go
    to: internal/services/asset/cache_keys.go
    via: "ResolveException success path 调 InvalidateWorkstationHealth"
    pattern: "InvalidateWorkstationHealth"
  - from: internal/services/asset/reconciliation_service.go
    to: internal/services/asset/reconciliation_exception_matcher.go
    via: "GetByWorkstation 内调 MatchException 计数 exceptionHit"
    pattern: "MatchException"
  - from: .planning/phases/45-r4/45-VERIFICATION.md
    to: ROADMAP SC1-SC10
    via: "每条 SC 含 (a) 命令字符串、(b) 期望输出、(c) 实际状态"
    pattern: "SC[0-9]+:"
---

<objective>
Phase 45 R4 的本 plan（45-02）负责**闭环补全**：R2 auto-workorder 触发的缓存失效补齐、operlog 完整接入、IP 解析链落地、跨模块权限文档更新、VERIFICATION.md 验收记录、回归守护测试。本 plan 建立在 45-01 已完成"读路径 + UI 整合 + 跨模块注入"基础之上，专做收尾验证 + 回归守护。

Purpose: 把 45-01 留作的隐含契约（R2 转单也失效缓存 / 工位 IP 解析 / operlog 全覆盖 / VERIFICATION 记录）全部补齐，避免 Phase 46 启动时才发现闭环缺口。

Output: 4 个后端文件修改（不动签名）+ 2 个前端文件微调 + 2 个回归测试文件 + 2 个文档文件。

**Scope correction from checker revision:**
- Plan 02 不修改 ResolveException service 签名（B2 锁定）— R2 cron 期望 `*models.WorkOrder` 不被破坏
- Plan 02 不修改 CreateWorkorderFromException 签名（B2 锁定）— 走"新增 WorkstationIDForException 旁路方法"路径
- Plan 02 操作的 scheduler 文件是 `internal/scheduler/reconciliation_tasks.go`（不是不存在的 `reconciliation_workorder.go`）
- Plan 02 操作的 service 文件是 `internal/services/asset/reconciliation_workorder.go:123`（CreateWorkorderFromException 在此）
- Plan 02 操作的 handler 文件是 `internal/api/v1/asset/reconciliation_handler.go:110`（ResolveException 在此）
- IP 解析链 inline 集成到 GetByWorkstation queryFn（不抽新文件，B5 修复 — 避免文件清单膨胀）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/45-r4/45-CONTEXT.md
@.planning/phases/45-r4/45-PATTERNS.md
@.planning/phases/45-r4/45-01-PLAN.md
@.planning/phases/45-r4/45-01-SUMMARY.md
@.planning/notes/260627-cross-module-permission.md
@.planning/notes/asset-reconciliation-strategy.md

**Mandatory read_first list before implementation:**
- `internal/scheduler/reconciliation_tasks.go:200-240` — createWorkorderBySeverity function with woSvc.CreateWorkorderFromException call
- `internal/services/asset/reconciliation_workorder.go:100-200` — CreateWorkorderFromException 12-step flow
- `internal/services/asset/reconciliation_workorder.go` — file location for new WorkstationIDForException
- `internal/api/v1/asset/reconciliation_handler.go:110-166` — ResolveException current shape
- `internal/services/asset/reconciliation_service.go:GetByWorkstation` (45-01 created) — IP 解析链需集成此处
- `internal/services/asset/reconciliation_exception_matcher.go` — MatchException function for exceptionHit 计数
- `internal/services/asset/cache_keys.go:InvalidateWorkstationHealth` (45-01 created)
- `.planning/notes/260627-cross-module-permission.md` (R4 实际接入清单更新)
- `xingran-react-frontend/src/components/reconciliation/ExceptionMatchList.tsx` (45-01 created)
- `xingran-react-frontend/src/components/reconciliation/ReconciliationDrawer.tsx` (45-01 created)
- `.planning/ROADMAP.md` (Phase 45 SC1-SC10 list)
- `.planning/phases/42-r1/42-VERIFICATION.md` (作为 VERIFICATION.md 模板)
</context>

<interfaces>
From 45-01 (must exist post-execution):
- `internal/services/asset/cache_keys.go:InvalidateWorkstationHealth(ctx, c, wsID) error` function (45-01 declared)
- `internal/api/v1/operations/workstation_handler.go:GetByID` handler with `hasReconciliationPerm` gate + ReconciliationVisible flag (45-01 wired)
- `internal/services/asset/reconciliation_service.go:GetByWorkstation(wsID, window)` (45-01 implemented)
- `pkg/middleware/permission_query_helper.go:HasUserPermission(c, perm)` (45-01 exported)

From internal/services/asset/reconciliation_workorder.go (existing — created in R2):
- `func (s *ReconciliationWorkorderService) CreateWorkorderFromException(ctx context.Context, exceptionID string) (*models.WorkOrder, error)` at line 123 (12-step flow returning *models.WorkOrder)

From internal/scheduler/reconciliation_tasks.go (existing — created in R2):
- `createWorkorderBySeverity(ctx, db, woSvc, severity, limit)` at line 209 (calls woSvc.CreateWorkorderFromException at line 228)

From internal/api/v1/asset/reconciliation_handler.go (45-01 leaves this intact):
- `func (h *ReconciliationHandler) ResolveException(c *gin.Context)` at line 110 (current shape: no InvalidateWorkstationHealth call)

From CLAUDE.md (强约束):
- operlog.Record(Module, OperType) — 11 关键词 + 25 OperType 常量
- OperTypeCreate=1 / OperTypeUpdate=2 / OperTypeDelete=3 / OperTypeApprove=22 / OperTypeReject=23
- ModuleReconciliation = "资产对账" + ModuleReconciliationExceptionRule = "例外规则管理" (constants in reconciliation_handler.go:14-19)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 后端闭环 — R2 scheduler 失效缓存 + IP 解析链 + operlog 接入 + ResolveException 顺序</name>
  <files>
    - internal/scheduler/reconciliation_tasks.go
    - internal/services/asset/reconciliation_workorder.go
    - internal/services/asset/reconciliation_service.go
    - internal/api/v1/asset/reconciliation_handler.go
    - .planning/notes/260627-cross-module-permission.md
  </files>
  <read_first>
    - internal/scheduler/reconciliation_tasks.go:200-240 (existing createWorkorderBySeverity function)
    - internal/services/asset/reconciliation_workorder.go (existing CreateWorkorderFromException at line 123)
    - internal/services/asset/reconciliation_service.go:GetByWorkstation (45-01 implemented)
    - internal/services/asset/reconciliation_exception_matcher.go (MatchException function)
    - internal/services/asset/cache_keys.go (InvalidateWorkstationHealth from 45-01)
    - internal/api/v1/asset/reconciliation_handler.go:110-166 (ResolveException current shape)
    - .planning/notes/260627-cross-module-permission.md (R4 实际接入清单更新)
  </read_first>
  <behavior>
    - R2 scheduler createWorkorderBySeverity 在 woSvc.CreateWorkorderFromException 成功后调 InvalidateWorkstationHealth（避免用户重看页面仍命中旧缓存）
    - 新增 WorkstationIDForException(ctx, exceptionID) (string, error) 旁路方法（不动 CreateWorkorderFromException 签名）
    - WorkstationIDForException 通过 reconciliation_normalized.workstation_id 查找；若查不到返回 ""（孤立资产）
    - IP 解析链 inline 集成到 GetByWorkstation queryFn：asset.ip → workstation.ip → network_device.ip via port → "unknown"
    - exceptionHit 计数基于 IP 解析结果 + MatchException 命中；不允许 list.length
    - ResolveException handler success path 完整调用链：service.ResolveException → InvalidateWorkstationHealth → operlog.Record → response.Success
    - operlog 模块常量：ResolveException 用 ModuleReconciliation（"资产对账"），非 ModuleReconciliationExceptionRule（45-01 已设）
    - 跨模块权限文档补充 R4 实际接入清单
  </behavior>
  <action>
    按以下顺序修改 5 个文件：

    1. **internal/services/asset/reconciliation_workorder.go**（旁路方法 — 不动现有签名）：
       - 在 `ReconciliationWorkorderService` 上新增方法：
         ```
         // WorkstationIDForException 通过 exceptionID 反查工位ID（R4 / D-A4-04 缓存失效用）
         //
         // 设计:不修改 CreateWorkorderFromException 签名（保持 R2 cron 既有 12 步流程不变），
         //        单独提供轻量级反查方法,scheduler 在 CreateWorkorderFromException 成功后
         //        调此方法拿 wsID,再触发 InvalidateWorkstationHealth。
         //
         // 查询:sys_data_reconciliation JOIN reconciliation_normalized.workstation_id
         //      通过 asset_id 间接反查(若 asset 已无 reconciliation_normalized 记录,
         //      返回 "" + nil error,caller 跳过 invalidate)。
         //
         // 返回:(workstationID, error)
         //   - workstationID == "" 表示该 exception 孤立无工位归属,正常情况
         //   - error != nil 表示 DB 故障,scheduler 应 logger.Warnf 但不阻断 cron 流程
         func (s *ReconciliationWorkorderService) WorkstationIDForException(ctx context.Context, exceptionID string) (string, error) {
             var wsID string
             err := s.db.WithContext(ctx).
                 Table("reconciliation_normalized").
                 Select("reconciliation_normalized.workstation_id").
                 Joins("JOIN sys_data_reconciliation ON sys_data_reconciliation.asset_id = reconciliation_normalized.asset_id").
                 Where("sys_data_reconciliation.id = ? AND sys_data_reconciliation.deleted_at IS NULL AND reconciliation_normalized.workstation_id IS NOT NULL", exceptionID).
                 Limit(1).
                 Row().
                 Scan(&wsID)
             if err == gorm.ErrRecordNotFound {
                 return "", nil
             }
             if err != nil {
                 return "", err
             }
             return wsID, nil
         }
         ```
       - **不修改** CreateWorkorderFromException 任何一行代码（保持 R2 cron 行为不变）
       - 单元测试可加：`TestWorkstationIDForException_*`（mock db / 空 case）

    2. **internal/scheduler/reconciliation_tasks.go**：
       - 在 `createWorkorderBySeverity` 函数（约 line 209）的 for 循环内（约 line 228 `woSvc.CreateWorkorderFromException` 调用之后）插入：
         ```
         successCount++
         // 🆕 R4 / D-A4-04: 缓存主动失效(避免用户重看页面仍命中旧缓存)
         wsID, wsErr := woSvc.WorkstationIDForException(ctx, rec.ID)
         if wsErr != nil {
             logrus.Warnf("[reconciliation:createWorkorder%s] WorkstationIDForException 失败 exceptionID=%s: %v", severity, rec.ID, wsErr)
         } else if wsID != "" {
             if err := asset.InvalidateWorkstationHealth(ctx, db, wsID); err != nil {
                 logrus.Warnf("[reconciliation:createWorkorder%s] R4 invalidate cache failed exceptionID=%s wsID=%s: %v", severity, rec.ID, wsID, err)
             }
         }
         ```
       - 注：scheduler 文件可能没有 cache.Cache 句柄 — 但 `asset.InvalidateWorkstationHealth(ctx, db, wsID)` 中 db 参数实际是 `*gorm.DB` 而非 cache.Cache。该函数签名从 cache_keys.go 接收 cache.Cache，scheduler 可改为调 `cache.NewRedisCache(...).Delete(ctx, key)` 或更简单的方式：scheduler 通过注入的 `woSvc` 间接持有 cache（Plan 02 不扩展 scheduler 依赖，通过 woSvc 实例方法暴露新方法）。
       - **更简洁的方案**：把 InvalidateWorkstationHealth 调用改为 `woSvc.InvalidateWorkstationHealth(ctx, rec.ID)`（在 ReconciliationWorkorderService 上加一个同名方法，内部调用 cache_keys.InvalidateWorkstationHealth）。这样 scheduler 不需要新增 cache 依赖：
         ```
         // 步骤 a:在 internal/services/asset/reconciliation_workorder.go 上加方法
         func (s *ReconciliationWorkorderService) InvalidateWorkstationHealth(ctx context.Context, workstationID string) error {
             if s.cache == nil || workstationID == "" {
                 return nil
             }
             return asset.InvalidateWorkstationHealth(ctx, s.cache, workstationID)
         }
         ```
         （注意：ReconciliationWorkorderService 当前可能没有 cache 字段，需在构造函数加；如果构造函数改动会破坏 R2 cron 其他调用方 — 用一个 NEW 函数 `NewReconciliationWorkorderServiceWithCache(db, cache)` 替代，保留旧构造函数为兼容壳）
       - 实际推荐：scheduler 文件顶部 import `pkg/cache` + 通过 `core` 全局拿 cache。最简方案是把 scheduler 文件改为接收 `cache.Cache` 参数：`createWorkorderBySeverity(ctx, db, cache, woSvc, severity, limit)`，调用方 `StartReconciliationScheduler` 同步改传 cache。
       - **最终选择**（避免侵入 R2 既有代码）：在 scheduler 文件的 cron 入口函数 `StartReconciliationScheduler` 或调用 `createWorkorderBySeverity` 的入口，从 `core.Cache` 注入 cache，然后向下传。
       - operlog.Record 调用（如尚未添加）：createWorkorder → OperTypeCreate；workorder 状态变更 → OperTypeUpdate/Approve；按 CLAUDE.md operlog 强约束补齐。注：scheduler 的 createWorkorderBySeverity 是 cron 上下文（无 c *gin.Context），operlog.Record 需调用无 c 的版本：`operlog.RecordContext(ctx, ...)` 或类似 — 查 operlog.Record 是否有 ctx-only 入口。如果没有，operlog 记录改在工单后续 lifecycle handler 中补（不在 cron 入口）。
         简化路径：R2 auto-workorder 创建本身是 cron 自动化流程，不强求 operlog；按 CLAUDE.md "operlog 记录约定 — 强制" 主要针对 handler endpoint，cron 内部流程可豁免（豁免理由写到注释）。只需保证**用户主动操作**（如 R2 resolve API）的 operlog 完整即可。
       - **最终决定**：本 Task 只补 scheduler 缓存失效；operlog 在 cron 内的覆盖范围 audit 写到 VERIFICATION.md 备注

    3. **internal/services/asset/reconciliation_service.go**：
       - 在 Plan 01 GetByWorkstation 的 queryFn 内、assets 列表拉取后（约 Plan 01 实现后），inline 集成 IP 解析 + exceptionHit 计数：
         ```
         // IP 解析链(参 strategy §4.4)
         for i := range assets {
             ip := resolveAssetIP(ctx, r.db, assets[i].AssetID, wsID)
             assets[i].IP = ip
             if ip != "" {
                 match, _ := r.matcher.MatchException(ctx, ip, assets[i].ResponsibleUserID, assets[i].ConflictType)
                 if match != nil {
                     assets[i].ExceptionRuleID = &match.RuleID
                     assets[i].AppliedActions = match.AppliedActions
                     healthScore.ExceptionHit++
                 }
             }
         }
         ```
       - 实现 `resolveAssetIP(ctx, db, assetID, wsID) string` 私有函数，**inline** 在 reconciliation_service.go 底部（不抽新文件，B5 修复）：
         ```
         func resolveAssetIP(ctx context.Context, db *gorm.DB, assetID, workstationID string) string {
             // 第一优先:ops_asset.ip
             var ip string
             if err := db.WithContext(ctx).
                 Table("ops_asset").
                 Select("ip").
                 Where("id = ? AND deleted_at IS NULL AND ip IS NOT NULL AND ip != ''", assetID).
                 Row().
                 Scan(&ip); err == nil && ip != "" {
                 return ip
             }
             // 第二优先:sys_workstation.ip
             if workstationID != "" {
                 if err := db.WithContext(ctx).
                     Table("sys_workstation").
                     Select("ip").
                     Where("id = ? AND deleted_at IS NULL AND ip IS NOT NULL AND ip != ''", workstationID).
                     Row().
                     Scan(&ip); err == nil && ip != "" {
                     return ip
                 }
             }
             // 第三优先:通过 sys_port JOIN sys_network_device
             if err := db.WithContext(ctx).
                 Table("sys_port").
                 Select("sys_network_device.ip").
                 Joins("JOIN sys_network_device ON sys_network_device.id = sys_port.device_id").
                 Joins("JOIN sys_workstation_info_point ON sys_workstation_info_point.port_id = sys_port.id").
                 Where("sys_workstation_info_point.workstation_id = ? AND sys_network_device.ip IS NOT NULL AND sys_network_device.ip != ''", workstationID).
                 Limit(1).
                 Row().
                 Scan(&ip); err == nil && ip != "" {
                 return ip
             }
             return "unknown"
         }
         ```
       - 注：真实表名以 codebase 为准（grep `CREATE TABLE` migrations 验证 `sys_port` / `sys_network_device` / `sys_workstation_info_point` 是否存在）。若表名/列名不符，按 migration 实际 schema 调整

    4. **internal/api/v1/asset/reconciliation_handler.go**（ResolveException — 在 110-166 行修改）：
       - 修改 ResolveException success path（约 142 行 service 调用之后）：
         * **不修改** service.ResolveException 签名（保持 4 参返回 error）
         * 通过 service.GetByID 拿 exception 详情（已有方法）+ 通过 reconciliation_normalized 反查 workstation_id
         * 推荐更简洁的方式：在 handler 内直接调 reconciliation_normalized（handler 可访问 db via h.core.GetDB()）：
           ```
           // 现有: service.ResolveException(...) → err != nil → return
           // 🆕 R4 / D-A4-04: 缓存主动失效
           var wsID string
           if err := h.core.GetDB().WithContext(c.Request.Context()).
               Table("reconciliation_normalized").
               Select("workstation_id").
               Where("asset_id = (SELECT asset_id FROM sys_data_reconciliation WHERE id = ? AND deleted_at IS NULL)", id).
               Limit(1).
               Row().
               Scan(&wsID); err == nil && wsID != "" {
               asset.InvalidateWorkstationHealth(c.Request.Context(), h.core.Cache, wsID)
           }
           ```
         * 然后保持现有 `operlog.Record(c, h.core.OperLogService, h.core.GetDB(), ModuleReconciliation, operlog.OperTypeUpdate)`
         * 然后保持现有 `response.Success(...)`
         * **严格顺序**：service.ResolveException → InvalidateWorkstationHealth → operlog.Record → response.Success
       - 检查 operlog.Record 调用的 Module 常量：必须是 `ModuleReconciliation`（"资产对账"）而非 `ModuleReconciliationExceptionRule`（"例外规则管理"）— 45-01 已确认（line 158 是 ModuleReconciliation）
       - 检查 operlog.Record 的 OperType：resolve 是 OperTypeUpdate（=2）— 45-01 已确认
       - 若该 handler 处理任何带 password/key/token 等敏感字段的请求体，改为 operlog.RecordWithBody 自动脱敏。ResolutionNote 是普通文本，不需要 RecordWithBody

    5. **.planning/notes/260627-cross-module-permission.md**：
       - 在文档末尾追加 "R4 实际接入清单 (2026-06-28)" 章节：
         ```
         ## R4 实际接入清单

         ### 后端
         - `pkg/middleware/permission_query_helper.go:HasUserPermission` — 跨模块 perm 检查 helper（Plan 01 新增）
         - `internal/api/v1/operations/workstation_handler.go` — WorkstationHandler.GetByID 调用 hasReconciliationPerm → ReconciliationVisible
         - `internal/api/v1/asset/reconciliation_handler.go:GetByWorkstation` — handler 内 middleware.HasUserPermission 设置 visible 字段
         - `internal/services/asset/cache_keys.go:InvalidateWorkstationHealth` — 缓存失效 helper
         - `internal/services/asset/reconciliation_service.go:GetByWorkstation` — service 层 COUNT 端点（不允许 list.length）
         - `internal/scheduler/reconciliation_tasks.go:createWorkorderBySeverity` — R2 转单完成后通过 woSvc.WorkstationIDForException + woSvc.InvalidateWorkstationHealth 失效缓存（Plan 02）
         - `internal/services/asset/reconciliation_workorder.go:WorkstationIDForException` — 旁路方法不动现有签名（Plan 02）
         - `internal/api/v1/asset/reconciliation_handler.go:ResolveException` — success path 顺序：service → invalidate → operlog → response（Plan 02）

         ### 前端
         - `src/components/reconciliation/hooks/useReconciliationVisibility.ts` — useMenuStore.permissions 检查（不是 authStore.perms）
         - `src/components/reconciliation/HealthCard.tsx` — useReconciliationVisibility() === false 时 render null
         - `src/components/reconciliation/HealthBadge.tsx` — useReconciliationVisibility() === false 时 render "-"
         - `src/components/reconciliation/ReconciliationDrawer.tsx` — useReconciliationVisibility() === false 时 render null
         - `src/components/reconciliation/hooks/useWorkstationHealth.ts` — enabled: visible && Boolean(workstationId) && data?.visible !== false 三重门控
         - `src/pages/operations/workstations/index.tsx` — page level lift useWorkstationHealth + conflictTypeMap 传给 WorkstationDeviceTable
         ```

       - 该章节为 Phase 46 + 未来审计提供快速检索清单
  </action>
  <verify>
    <automated>go build ./...</automated>
    <automated>go test ./internal/services/asset/... -count=1 -timeout=120s</automated>
    <automated>go test ./internal/api/v1/asset/... -count=1 -timeout=120s</automated>
    <automated>go test ./internal/scheduler/... -count=1 -timeout=120s</automated>
    <automated>grep -rn "InvalidateWorkstationHealth" internal/ | grep -v _test.go | wc -l</automated>
    <automated>grep -rn "operlog.Record" internal/api/v1/asset/ | wc -l</automated>
    <automated>grep -rn "WorkstationIDForException" internal/ | wc -l</automated>
  </verify>
  <done>
    - go build ./... exit 0
    - 既有服务测试 PASS（无回归）
    - internal/scheduler/reconciliation_tasks.go 含 InvalidateWorkstationHealth 调用字串
    - internal/services/asset/reconciliation_workorder.go 含 WorkstationIDForException 旁路方法
    - **CreateWorkorderFromException 签名未被修改**（grep 验证：仍然返回 *models.WorkOrder）
    - internal/services/asset/reconciliation_service.go 含 resolveAssetIP 私有函数 + MatchException 调用
    - internal/api/v1/asset/reconciliation_handler.go ResolveException 调用链完整（service → invalidate → operlog → response）
    - operlog.Record 调用覆盖所有用户主动操作（ResolveException）
    - .planning/notes/260627-cross-module-permission.md 含 "R4 实际接入清单" 章节
  </done>
</task>

<task type="auto">
  <name>Task 2: 前端微调（申请例外预填 IP）+ VERIFICATION.md + 回归守护测试</name>
  <files>
    - xingran-react-frontend/src/components/reconciliation/ExceptionMatchList.tsx
    - xingran-react-frontend/src/components/reconciliation/ReconciliationDrawer.tsx
    - xingran-react-frontend/src/components/reconciliation/__tests__/HealthCard.test.tsx
    - xingran-react-frontend/src/components/reconciliation/hooks/__tests__/useReconciliationVisibility.test.ts
    - .planning/phases/45-r4/45-VERIFICATION.md
  </files>
  <read_first>
    - xingran-react-frontend/src/components/reconciliation/ExceptionMatchList.tsx (45-01 created)
    - xingran-react-frontend/src/components/reconciliation/ReconciliationDrawer.tsx (45-01 created)
    - .planning/ROADMAP.md (Phase 45 SC1-SC10 list)
    - .planning/phases/42-r1/42-VERIFICATION.md (作为 VERIFICATION.md 模板)
    - xingran-react-frontend/src/components/reconciliation/hooks/useWorkstationHealth.ts (45-01 created)
    - xingran-react-frontend/src/components/reconciliation/hooks/useReconciliationVisibility.ts (45-01 created)
    - .planning/phases/45-r4/45-CONTEXT.md (SC9 "抽屉申请例外按钮预填 IP/类型正确" 锁定)
  </read_first>
  <behavior>
    - ExceptionMatchList "去创建例外规则" 按钮 onClick 携带 assetIp + conflictType 参数
    - ReconciliationDrawer "申请例外" header 按钮 navigate 时 query string 含 assetIp + conflictType + workstationId
    - VERIFICATION.md 列 R4 全部 10 条 ROADMAP SC 通过证据：每条 SC 含 (a) 命令字符串、(b) 期望输出、(c) 实际状态（PASS/FAIL）
    - useReconciliationVisibility 单元测试覆盖：有 perm → true；无 perm → false；store 为空 → false
    - HealthCard 单元测试覆盖：可见 → 渲染 5 KPI + score；不可见 → render null；空态 total=0 → "该工位暂无关联资产。"
  </behavior>
  <action>
    按以下顺序实现 5 个文件：

    1. **xingran-react-frontend/src/components/reconciliation/ExceptionMatchList.tsx**：
       - 修改"去创建例外规则"按钮 onClick：
         ```
         const url = `/asset/reconciliation/exception-rules/new?assetIp=${encodeURIComponent(assetIp ?? '')}&conflictType=${encodeURIComponent(conflictType ?? '')}`;
         navigate(url);
         ```
       - props: { rules, loading, assetIp, conflictType, onCreateRule?: () => void } — assetIp/conflictType 必填
       - 若保留 onCreateRule，由 ReconciliationDrawer 统一管理 navigate 优先级更高

    2. **xingran-react-frontend/src/components/reconciliation/ReconciliationDrawer.tsx**：
       - 修改 header "申请例外" 按钮 onClick：
         ```
         const handleApplyException = () => {
             const url = `/asset/reconciliation/exception-rules/new?workstationId=${encodeURIComponent(workstationId ?? '')}&assetIp=${encodeURIComponent(selectedAssetIp ?? '')}&conflictType=${encodeURIComponent(selectedAssetConflictType ?? '')}`;
             window.open(url, '_blank');
             message.success('已跳转到例外规则新建页，IP 与冲突类型已预填。');
         };
         ```
       - props 新增 workstationId / selectedAssetIp / selectedAssetConflictType
       - Tab 1 渲染时把 selectedAssetIp + selectedAssetConflictType 传给 ExceptionMatchList

    3. **.planning/phases/45-r4/45-VERIFICATION.md**（新建 — 参 W2）：
       - 模板参考 `.planning/phases/42-r1/42-VERIFICATION.md`（按该文件结构）
       - 列 R4 全部 10 条 ROADMAP SC，**每条 SC 含 (a) 命令字符串、(b) 期望输出、(c) 实际状态**。**必须 capture 命令实际输出后填"Status: PASS/FAIL"**，不允许预填 PASS：
         ```
         # Phase 45 (R4) — Verification Report

         **Generated:** 2026-06-28
         **Phase Goal:** 把对账健康度嵌入现有 ops/workstation 与 asset/card 页面，提供冲突详情抽屉。

         ## Success Criteria

         ### SC1 — 工位详情页顶部 HealthCard
         - **Command:** `curl -X POST http://localhost:9000/api/ops/workstation/{id} -H "Authorization: Bearer $TOKEN"`
         - **Expected:** 响应包含 reconciliationVisible=true + reconciliation.healthScore
         - **Actual:** <paste 实际 curl 输出>
         - **Status:** PASS / FAIL

         ### SC2 — 资产设备子表 + 域控设备子表对账健康列
         - **Command:** 浏览器手动访问 /ops/workstation/{id} 展开区 + /asset/card 列表
         - **Expected:** WorkstationDeviceTable 资产/AD 子表 + 资产列表显示 8px 圆点
         - **Actual:** <paste 浏览器截图链接或 describe 实际效果>
         - **Status:** PASS / FAIL

         ### SC3 — 点击徽标打开 ReconciliationDrawer 三 Tab
         - **Command:** 点击任意冲突徽标
         - **Expected:** 780px Drawer 打开 + 冲突摘要/历史变更/例外规则 三 Tab 可切换
         - **Actual:** <describe>
         - **Status:** PASS / FAIL

         ### SC4 — 资产详情页 /asset/card/:id 顶部摘要块
         - **Command:** 资产列表行点击 → Drawer 顶部
         - **Expected:** Drawer 顶部显示 severity Tag + conflict type Tag + IP
         - **Note:** ROADMAP SC4 文字 "顶部摘要块" 在 R4 由 drawer 顶部 title + summary Tab 顶部 region 共同承担（D-A1-02 锁定 + CONTEXT 备注）。本页不新建独立 detail page 路由。
         - **Actual:** <describe>
         - **Status:** PASS / FAIL

         ### SC5 — 跨模块调用性能 ≤ 200ms
         - **Command:** `time curl -X POST http://localhost:9000/api/asset/reconciliation/by-workstation -d '{"workstationId":"...","window":"7d"}'`
         - **Expected:** 总耗时 ≤ 200ms (含 Redis L2 命中)
         - **Actual:** <paste time 输出>
         - **Status:** PASS / FAIL

         ### SC6 — 无 asset:reconciliation:list 权限时静默隐藏
         - **Command:** 用无 perm 用户 token 调 WorkstationHandler.GetByID
         - **Expected:** 响应 reconciliationVisible=false，无 403
         - **Actual:** <paste 实际响应>
         - **Status:** PASS / FAIL

         ### SC7 — 工位 N+1 查询优化
         - **Command:** `grep -rn "left.*join\|reconciliation_normalized" internal/services/asset/reconciliation_service.go`
         - **Expected:** service.GetByWorkstation 单次 LEFT JOIN reconciliation_normalized 拉完所有资产
         - **Actual:** <paste grep 输出>
         - **Status:** PASS / FAIL

         ### SC8 — 修复闭环解耦
         - **Command:** `grep -rn "InvalidateWorkstationHealth" internal/`
         - **Expected:** 至少 3 处调用（ResolveException + scheduler createWorkorder + service.GetByWorkstation 入口旁路）
         - **Actual:** <paste grep 输出 + 行号>
         - **Status:** PASS / FAIL

         ### SC9 — 抽屉申请例外按钮预填 IP/类型正确
         - **Command:** 浏览器开发者工具检查 Drawer 申请例外按钮 click 后 URL
         - **Expected:** URL 含 assetIp + conflictType + workstationId 三个 query 参数
         - **Actual:** <paste URL 或 describe>
         - **Status:** PASS / FAIL

         ### SC10 — npm run build + go build ./... 全通过
         - **Command:** `cd xingran-react-frontend && npm run build && go build ./...`
         - **Expected:** exit 0
         - **Actual:** <paste 实际 exit code + 关键输出>
         - **Status:** PASS / FAIL

         ## Build Verification

         ```
         $ cd xingran-react-frontend && npm run build
         <paste 实际 BUILD OUTPUT>

         $ go build ./...
         <paste 实际 BUILD OUTPUT>
         ```

         ## Notes

         - ROADMAP SC5 性能指标通过 CacheProvider.GetOrSet 实现（TTL 5min，与 R1 MV 刷新一致）
         - ROADMAP SC7 N+1 优化通过单次 LEFT JOIN reconciliation_normalized
         - ROADMAP SC8 修复闭环通过 InvalidateWorkstationHealth 主动失效而非触发引擎
         - SC4 实现备注：drawer 顶部 title 区域作为摘要块（D-A1-02）
         - operlog 覆盖范围：用户主动操作（ResolveException + exception-rule CRUD）全量；R2 auto-workorder cron 因是系统行为豁免 operlog（在注释中说明）
         ```

    4. **xingran-react-frontend/src/components/reconciliation/hooks/__tests__/useReconciliationVisibility.test.ts**（新建）：
       - 覆盖三种状态：
         ```
         describe("useReconciliationVisibility", () => {
           it("returns true when useMenuStore.permissions includes 'asset:reconciliation:list'", () => {
             // mock useMenuStore to return { permissions: ["asset:reconciliation:list", ...] }
             // expect result.current === true
           })
           it("returns false when useMenuStore.permissions is empty array", () => {
             // mock useMenuStore to return { permissions: [] }
             // expect result.current === false
           })
           it("returns false when useMenuStore.permissions is undefined", () => {
             // mock useMenuStore to return { permissions: undefined }
             // expect result.current === false
           })
           it("returns false when useMenuStore.permissions lacks 'asset:reconciliation:list'", () => {
             // mock useMenuStore to return { permissions: ["other:perm"] }
             // expect result.current === false
           })
         })
         ```
       - 用 vitest + @testing-library/react-hooks（或新版 `@testing-library/react` 的 `renderHook`）

    5. **xingran-react-frontend/src/components/reconciliation/__tests__/HealthCard.test.tsx**（新建 — 可选但推荐）：
       - 覆盖五种状态：
         ```
         describe("HealthCard", () => {
           it("renders null when useReconciliationVisibility returns false", () => {
             // mock useReconciliationVisibility → false
             // expect container.firstChild === null
           })
           it("renders 5 KPIs + score when data loaded", () => {
             // mock useWorkstationHealth → { data: { healthScore: {...}, assets: [...] }, isLoading: false }
             // expect 5 KPI cells rendered + score number visible
           })
           it("renders empty state when total=0", () => {
             // mock data.healthScore.total === 0
             // expect "该工位暂无关联资产。" visible
           })
           it("renders skeleton when isLoading=true", () => {
             // mock isLoading: true
             // expect Skeleton component visible
           })
           it("calls onApplyException when 申请例外 button clicked", () => {
             // mock props.onApplyException
             // click button → expect mock called once
           })
         })
         ```
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npm run lint</automated>
    <automated>cd xingran-react-frontend && npm run type-check</automated>
    <automated>cd xingran-react-frontend && npm run build</automated>
    <automated>cd xingran-react-frontend && npx vitest run src/components/reconciliation 2>&1 | tail -30</automated>
    <automated>go build ./...</automated>
    <automated>cat .planning/phases/45-r4/45-VERIFICATION.md | head -50</automated>
    <automated>grep -c "^### SC" .planning/phases/45-r4/45-VERIFICATION.md</automated>
  </verify>
  <done>
    - go build ./... exit 0
    - npm run build exit 0（无 type 错误、无 lint 错误）
    - 既有 vitest 测试 PASS + 新增 HealthCard/useReconciliationVisibility 单元测试 PASS
    - 申请例外按钮 navigate URL 含 assetIp + conflictType + workstationId query 参数（vitest 断言 or 手动验证）
    - VERIFICATION.md 包含 R4 全部 10 条 ROADMAP SC 验收证据（grep -c "^### SC" = 10）
    - VERIFICATION.md 每条 SC 含 Actual + Status 字段（不是预填 PASS）
    - 跨模块权限文档含 R4 实际接入清单
    - .planning/phases/45-r4/45-02-SUMMARY.md 已创建
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R2 scheduler cron → asset service | Internal scheduled job creating workorders crosses here |
| asset IP resolver → ops_asset / sys_workstation / sys_network_device | Cross-table lookup crosses here |
| Browser → exception-rules/new page | Query string params assetIp/conflictType/workstationId cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-11 | Tampering | R2 createWorkorder Critical/High cache invalidation | mitigate | InvalidateWorkstationHealth called AFTER service.CreateWorkorderFromException returns nil; err logged via logger.Warn but does not abort cron |
| T-45-12 | Information Disclosure | resolveAssetIP reveals internal IPs to frontend | accept | IPs already exposed via existing reconciliation_normalized MV and existing list endpoints; new IP resolution chain does not add attack surface |
| T-45-13 | Spoofing | navigate URL query string injection (assetIp/conflictType) | mitigate | URL-encode params via encodeURIComponent; downstream page (exception-rules/new) uses React Router useSearchParams which parses safely |
| T-45-14 | Tampering | operlog RecordWithBody missing for sensitive fields | mitigate | Audit resolve/create workorder payloads: any field matching password/key/token keywords (11 listed in operlog.RecordWithBody doc) → use RecordWithBody; default to Record for non-sensitive ops |
| T-45-15 | Repudiation | VERIFICATION.md missing evidence for some SC | mitigate | Each of 10 ROADMAP SC must have explicit pass command + expected + actual + status; orchestrator review catches missing entries; verifier captures command output before writing Status |
| T-45-SC | Tampering | npm install / package additions | mitigate | 45-02 adds zero new packages; only message API from antd (already in deps) |

## Compliance Checks

- operlog.RecordWithBody keyword coverage: 11 sensitive keywords enforced
- OperType values locked: OperTypeCreate=1, OperTypeUpdate=2, OperTypeApprove=22
- Module constants locked: ModuleReconciliation="资产对账", ModuleReconciliationExceptionRule="例外规则管理"
- Plan 02 does NOT modify ResolveException service signature (B2 — R2 cron stability)
- Plan 02 does NOT modify CreateWorkorderFromException signature (B2 — R2 cron stability)
- IP resolution chain inline in reconciliation_service.go (no new file; B5 fix)
</threat_model>

<verification>
[Overall phase checks — final pass]
- `go build ./...` exits 0
- `cd xingran-react-frontend && npm run build` exits 0
- `cd xingran-react-frontend && npm run lint` exits 0
- `cd xingran-react-frontend && npm run type-check` exits 0
- `go test ./internal/services/asset/... -count=1 -timeout=120s` passes
- `go test ./internal/api/v1/asset/... -count=1 -timeout=120s` passes
- `go test ./internal/scheduler/... -count=1 -timeout=120s` passes
- `cd xingran-react-frontend && npx vitest run src/components/reconciliation` passes
- R2 createWorkorder triggers InvalidateWorkstationHealth（scheduler grep 验证）
- resolveAssetIP returns correct IP from asset → workstation → network_device chain
- VERIFICATION.md exists with all 10 SC entries (each containing actual command output + status)
- cross-module-permission.md contains "R4 实际接入清单" section
- operlog.Record called for all user-initiated R4 write paths (ResolveException)
- 申请例外按钮 navigate URL contains assetIp + conflictType + workstationId params
- CreateWorkorderFromException signature UNCHANGED (grep verification)
- ResolveException service signature UNCHANGED (4-arg, returns error only)
</verification>

<success_criteria>
[Measurable completion — final R4 closure]
- All 10 ROADMAP SC PASS with documented evidence in VERIFICATION.md
- R2 auto-workorder create + R2 resolve both invalidate cache (SC8 闭环)
- 工位 IP 解析链实装（asset → workstation → network_device via port → unknown）
- operlog.Record/RecordWithBody covers all user-initiated write paths per CLAUDE.md mandate
- 跨模块权限文档更新含 R4 实际接入清单
- All new unit tests pass (HealthCard + useReconciliationVisibility)
- CreateWorkorderFromException / ResolveException signatures UNCHANGED (R2 cron stability)
- Phase 45 ready for handoff to Phase 46 (R5 半自动修复)
</success_criteria>

<output>
Create `.planning/phases/45-r4/45-02-SUMMARY.md` when done
</output>