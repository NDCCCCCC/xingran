---
phase: 43-r2
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified: [internal/core/db/migrations/migration_173_reconciliation_silence_mv.go, internal/services/asset/reconciliation_detection.go, internal/services/asset/reconciliation_service.go, internal/api/v1/asset/reconciliation_handler.go, internal/api/v1/asset/reconciliation_router.go, internal/core/db/database.go]
autonomous: true
requirements: [WORKORDER-02, MONITOR-02]
user_setup: []
must_haves:
  truths:
    - "7d 静默期生效:同一 asset+type 静默期 last_resolved_at 7d 内不重复触发"
    - "24h 节流生效:同一 asset+type 24h 内只 1 条新异常"
    - "DetectLayer3 计数 skipped_silence / skipped_throttle 区分"
    - "MV 扩展 last_resolved_at / last_resolved_by / last_conflict_type 3 字段"
    - "POST /asset/reconciliation/exception/{id}/resolve 后端接口就位"
    - "resolve 操作写 operlog.Record(OperTypeUpdate)"
    - "权限粒度 asset:reconciliation:resolve(只有该权限用户看得到按钮)"
    - "resolved_at / resolved_by / resolution_note 三字段同时 SET"
  artifacts: [{path: migration_173_reconciliation_silence_mv.go, min_lines: 100}, {path: internal/services/asset/reconciliation_detection.go, min_lines: 320}, {path: internal/services/asset/reconciliation_service.go, min_lines: 420}, {path: reconciliation_handler.go, min_lines: 110}, {path: reconciliation_router.go, min_lines: 30}]
  key_links: [{from: reconciliation_detection, to: MV, via: LEFT JOIN LATERAL, pattern: last_resolved_at.*NOW.*INTERVAL.*7.day}, {from: reconciliation_service, to: reconciliation_handler, via: ResolveException, pattern: ResolveException}, {from: reconciliation_router, to: handler, via: POST /:id/resolve, pattern: /exception/:id/resolve}, {from: database.go, to: Migrate173, via: explicit call, pattern: Migrate173}]
---

<objective>
实现 7d 静默期(修复回写后 7 天不重复触发)+ 24h 节流(同 asset+type 24h 内只 1 条)+ ResolveException 后端 API(对应前端 43-03 的"标记已解决"按钮 + operlog 写入)。扩展 reconciliation_normalized MV 加 last_resolved_at / last_resolved_by / last_conflict_type 3 字段供静默期查询。符合 WORKORDER-02(operlog 状态变更)+ 部分 MONITOR-02(Resolve 端点供前端 WebSocket 同步用)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md @.planning/STATE.md @.planning/phases/43-r2/43-CONTEXT.md @.planning/phases/42-r1/42-PATTERNS.md @.planning/notes/260627-reconciliation-reuse-audit.md
@internal/services/asset/reconciliation_detection.go (DetectLayer3 line 181-269 循环结构)
@internal/services/asset/reconciliation_service.go (ReconciliationService 接口 + ListExceptions)
@internal/services/asset/reconciliation_statistics.go (mvAvailable 模式)
@internal/api/v1/asset/reconciliation_handler.go (现有 handler 风格)
@internal/api/v1/asset/reconciliation_router.go (现有 router)
@internal/core/db/migrations/migration_168_reconciliation_tables.go (MV 创建 SQL 参考)
@internal/core/db/database.go:216-245 (dropDependentMaterializedViews 列表,本 plan 加 reconciliation_normalized 已在)
@internal/models/reconciliation.go (SysDataReconciliation 字段)
@internal/utils/operlog/operlog.go:Record 5 参签名
@CLAUDE.md operlog 约定
</context>

<interfaces>
DetectLayer3 当前返回 (inserted, skipped int, err error);R2 扩展为 (inserted int, skipped int, skippedSilence int, skippedThrottle int, err error)。
reconciliation_normalized MV 当前 schema:R1 已含 asset_id/asset_code/asset_ip/physical_user_id/physical_username/asset_user_id/asset_username/ad_id/ad_username/ad_is_enabled/mv_refreshed_at/mac1/mac2/mac_join。R2 加 last_resolved_at timestamptz / last_resolved_by uuid / last_conflict_type varchar(2) 三列,通过 LEFT JOIN LATERAL 取最近一条已解决记录。
sys_data_reconciliation 现有 ResolvedAt/ResolvedBy/ResolutionNote 三个 *string 字段(Phase 42 R1 已建,model 列名 resolved_at/resolved_by/resolution_note);ResolveException SET 三字段同时。
operlog.Record(c, core.OperLogService, core.GetDB(), "资产对账", operlog.OperTypeUpdate):调用模式见 CLAUDE.md。
GET 路由风格:existing router 用 POST /exception/list + POST /exception/:id;R2 加 POST /exception/:id/resolve(per D-A4-04 决策)。
</interfaces>

<tasks>
<task type="auto" tdd="false">
  <name>Task 1: 扩展 MV + 7d 静默期 + 24h 节流</name>
  <files>internal/core/db/migrations/migration_173_reconciliation_silence_mv.go, internal/services/asset/reconciliation_detection.go</files>
  <read_first>- internal/core/db/migrations/migration_168_reconciliation_tables.go:60-130 (MV SQL 模板)- internal/services/asset/reconciliation_detection.go:181-269 (DetectLayer3 循环 + isReconciliationDuplicate)- 43-CONTEXT.md D-A3-01~D-A3-03 静默期与节流 3 决策</read_first>
  <behavior>MV 扩展 3 字段(LEFT JOIN LATERAL);DetectLayer3 在 ClassifyType 后 INSERT 前加 2 guard;返回值扩展 (inserted, skipped, skippedSilence, skippedThrottle, err)</behavior>
  <action>(1) 创建 migration_173_reconciliation_silence_mv.go:Migrate173ReconciliationSilenceMV(db) 函数,先 DROP MATERIALIZED VIEW IF EXISTS reconciliation_normalized CASCADE(因 dependency,需先 drop)— 实际上 dropDependentMaterializedViews() 已在 AutoMigrate 前 drop,本 migration 在 AutoMigrate 后跑所以可以直接 CREATE;然后用 db.Exec CREATE MATERIALIZED VIEW reconciliation_normalized AS SELECT a.id AS asset_id, ..., last_resolved.resolved_at AS last_resolved_at, last_resolved.resolved_by AS last_resolved_by, last_resolved.conflict_type AS last_conflict_type, ...(其余 R1 字段保持)FROM ops_asset a LEFT JOIN sys_user u ... LEFT JOIN LATERAL ( SELECT resolved_at, resolved_by, conflict_type FROM sys_data_reconciliation r WHERE r.asset_id = a.id AND r.resolved_at IS NOT NULL AND r.deleted_at IS NULL ORDER BY r.resolved_at DESC LIMIT 1 ) last_resolved ON true WHERE a.deleted_at IS NULL;然后 CREATE UNIQUE INDEX idx_recon_norm_asset ON reconciliation_normalized(asset_id);CREATE INDEX idx_recon_norm_last_resolved ON reconciliation_normalized(last_resolved_at) WHERE last_resolved_at IS NOT NULL(加速静默期查询)。SQL 严格遵循 43-CONTEXT.md D-A3-01 specifics 锁定的 LEFT JOIN LATERAL 语法。(2) 改 reconciliation_detection.go:DetectLayer3 函数签名改为 (ctx context.Context) (inserted int, skipped int, skippedSilence int, skippedThrottle int, err error),在 for 循环 ClassifyType 后 INSERT 前加 2 个 guard:guard 1(7d 静默期,line ~196)— 如果 row.LastResolvedAt != nil && row.LastConflictType != nil && *row.LastConflictType == conflictType && time.Since(*row.LastResolvedAt) < 7*24*time.Hour { skippedSilence++; continue };guard 2(24h 节流,line ~200)— `var recentCount int64; s.db.WithContext(ctx).Model(&models.SysDataReconciliation{}).Where("asset_id = ? AND conflict_type = ? AND detected_at > ? AND deleted_at IS NULL", row.AssetID, conflictType, time.Now().Add(-24*time.Hour)).Count(&recentCount); if recentCount > 0 { skippedThrottle++; continue }`。同时给 NormalizedRow struct 加 LastResolvedAt / LastResolvedBy / LastConflictType 三个 *string/*time.Time 字段(line ~25)。最后返回时:`return inserted, skipped, skippedSilence, skippedThrottle, nil`(注意:skipped 变量仍保留 D-A 健康 A 类型跳过计数)。</action>
  <verify><automated>go build ./... 2>&1 | head -20 && grep -n "Migrate173\|skippedSilence\|skippedThrottle" internal/core/db/migrations/migration_173_reconciliation_silence_mv.go internal/services/asset/reconciliation_detection.go</automated></verify>
  <acceptance_criteria>go build 退出码 0;grep 命中 Migrate173;grep 命中 skippedSilence 与 skippedThrottle;NormalizedRow 增 3 字段;DetectLayer3 5 返回值;迁移函数 idempotent(DROP IF EXISTS 在前)</acceptance_criteria>
  <done>MV 扩展 3 字段 + 索引;2 guard 落位;DetectLayer3 返回 4 计数</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ResolveException service + handler + router + database.go 注册</name>
  <files>internal/services/asset/reconciliation_service.go, internal/api/v1/asset/reconciliation_handler.go, internal/api/v1/asset/reconciliation_router.go, internal/core/db/database.go</files>
  <read_first>- internal/services/asset/reconciliation_service.go:117-123 (interface 定义位置 + ListExceptions/GetByID 风格)- internal/api/v1/asset/reconciliation_handler.go (现有 handler 风格,WithCore pattern)- internal/api/v1/asset/reconciliation_router.go:17-23 (现有路由注册风格)- internal/core/db/database.go:455-468 (43-01 注册块位置,本 plan 在其后追加 1 行)</read_first>
  <behavior>ResolveException(ctx, id, userID, note) 写 resolved_at/resolved_by/resolution_note;handler 解析当前 user_id 从 c.Get("userID");router 加 POST /exception/:id/resolve</behavior>
  <action>(1) 改 reconciliation_service.go:在 ReconciliationService interface(line 117)加 `ResolveException(ctx context.Context, id string, userID string, note *string) error`;实现方法(line ~393 后追加):func (s *reconciliationServiceImpl) ResolveException:step a `var rec models.SysDataReconciliation; if err := s.db.WithContext(ctx).Where("id = ? AND deleted_at IS NULL", id).First(&rec).Error; err != nil { return fmt.Errorf("查询异常失败: %w", err) }`;step b 防御:已 resolved 的不让重复 resolve,`if rec.ResolvedAt != nil { return errors.New("该异常已标记为已解决") }`;step c `updates := map[string]interface{}{"resolved_at": time.Now(), "resolved_by": userID}; if note != nil && *note != "" { updates["resolution_note"] = *note }; s.db.WithContext(ctx).Model(&rec).Updates(updates)`。(2) 改 reconciliation_handler.go:加 const ModuleReconciliationResolve = "资产对账"(D-16 R2 增加 module const);加 `ResolveException(c *gin.Context)`:line 1 `id := c.Param("id")`;line 2 `var req struct{ ResolutionNote *string \`json:"resolutionNote"\` }; c.ShouldBindJSON(&req)`(resolution_note 可选);line 3 `userID, _ := c.Get("userID"); userIDStr, _ := userID.(string)`(从 gin context 拿当前用户);line 4 `if err := h.service.ResolveException(c.Request.Context(), id, userIDStr, req.ResolutionNote); err != nil { response.Error(c, http.StatusInternalServerError, err.Error()); return }`;line 5 `operlog.Record(c, h.core.OperLogService, h.core.GetDB(), "资产对账", operlog.OperTypeUpdate)`;line 6 `response.Success(c, gin.H{"id": id, "resolvedAt": time.Now()})`。(3) 改 reconciliation_router.go:在 line 22 后追加 `r.POST("/exception/:id/resolve", handler.ResolveException)`(per D-A4-04 路径)。注意:router 注册的 handler 共享 WithCore 注入的 core 字段(已在 42-02 创建时注入);本 plan 不需修改 SetupReconciliationRouter 签名。(4) 改 database.go:在 Migrate172(43-01 注册)之后追加 1 行 `if err := migrations.Migrate173ReconciliationSilenceMV(d.DB); err != nil { applogger.Errorf("Phase 43 R2 reconciliation silence MV 失败: %v", err) }`。</action>
  <verify><automated>go build ./... 2>&1 | head -20 && grep -n "ResolveException\|/exception/:id/resolve\|Migrate173" internal/services/asset/reconciliation_service.go internal/api/v1/asset/reconciliation_handler.go internal/api/v1/asset/reconciliation_router.go internal/core/db/database.go</automated></verify>
  <acceptance_criteria>go build 退出码 0;grep 命中 ResolveException 4 处(interface + impl + handler + 1 调用);grep 命中 /exception/:id/resolve;grep 命中 Migrate173;operlog.Record 调 OperTypeUpdate</acceptance_criteria>
  <done>ResolveException service + handler + router 就位;database.go 注册;前端 43-03 可调此端点</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Cron → MV → Service | DetectLayer3 读 MV,MV 物化视图无权限隔离 |
| Admin user → Resolve API | /asset/reconciliation/exception/:id/resolve 需 asset:reconciliation:resolve 权限(43-03 前端按钮控制) |
## STRIDE Threat Register
| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-43-05 | EoP | 未授权 resolve | mitigate | 权限 asset:reconciliation:resolve 在 R3 路由 RequirePermissions 加(本 plan 不动 router.go middleware);前端按钮 disabled when no perm |
| T-43-06 | T | MV 子查询注入 | mitigate | 子查询值硬编码,无用户输入 |
| T-43-07 | DoS | resolve 并发 | mitigate | Service 层 WHERE resolved_at IS NULL 防并发;先 First 验证状态 |
| T-43-08 | Repudiation | resolve 无审计 | mitigate | operlog.Record(OperTypeUpdate) 强制写 sys_oper_log |
| T-43-09 | Infodisclosure | resolution_note | mitigate | resolution_note 走 operlog 自动记录(非敏感字段,不入 sensitive keywords) |
| T-43-SC | T | deps | mitigate | 无新依赖 |
</threat_model>

<verification>
1. go build ./... 退出码 0
2. DetectLayer3 返回 4 计数(inserted, skipped, skippedSilence, skippedThrottle)
3. reconciliation_normalized MV 含 last_resolved_at / last_resolved_by / last_conflict_type
4. ResolveException service + handler + router 就位
5. POST /exception/:id/resolve 路由注册
6. operlog.Record 调 OperTypeUpdate
7. database.go 注册 Migrate173
</verification>

<success_criteria>
- 7d 静默期生效(SC 7)
- 24h 节流生效(SC 8)
- Resolve API 就位(前端 43-03 用)
- operlog 完整接入(SC 6 部分:状态变更)
- go build 通过
</success_criteria>

<output>
Create .planning/phases/43-r2/43-02-SUMMARY.md when done
</output>
