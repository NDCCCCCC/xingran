---
phase: 43-r2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [internal/services/workorder/reconciliation_template.go, internal/services/asset/reconciliation_workorder.go, internal/scheduler/reconciliation_tasks.go, internal/core/db/migrations/migration_171_reconciliation_workorder_assignee_role.go, internal/core/db/migrations/migration_172_reconciliation_workorder_templates_seed.go, internal/core/db/database.go]
autonomous: true
requirements: [WORKORDER-01]
user_setup: []
must_haves:
  truths: [critical 异常 ≤2min 自动转工单, high 异常 ≤5min 自动转工单, 标题按 D-A2-01 模板, 6 类工单(B-F)模板差异化, type→role 从 sys_config 读, SLA critical=30m/high=4h/medium=24h/low=7d, 转单成功 UPDATE workorder_id, 失败 logrus.Errorf 不写 SysNotice]
  artifacts: [{path: internal/services/workorder/reconciliation_template.go, min_lines: 80}, {path: internal/services/asset/reconciliation_workorder.go, min_lines: 100}, {path: internal/scheduler/reconciliation_tasks.go, min_lines: 170}, {path: migration_171, min_lines: 80}, {path: migration_172, min_lines: 80}]
  key_links: [{from: scheduler, to: asset.reconciliation_workorder, pattern: CreateWorkorderFromException}, {from: asset.reconciliation_workorder, to: workorder.base, pattern: NewBaseService.Create}, {from: database.go, to: Migrate171+172, pattern: Migrate171|Migrate172}]
---

<objective>
实现 critical/high 异常的自动转工单(2 个新 cron @every 2m/5m)+ 6 类工单模板差异化(B-F)+ type→role 映射从 sys_config 读取,为 Phase 43 R2 闭环的核心第一步。后续 43-02/43-03 在此基础上叠加修复回写与实时推送。符合 WORKORDER-01 ROADMAP success criteria 1+2+3。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md @.planning/STATE.md @.planning/phases/43-r2/43-CONTEXT.md @.planning/phases/42-r1/42-PATTERNS.md
@internal/scheduler/reconciliation_tasks.go @internal/services/workorder/base.go:237-289 @internal/models/workorder.go @internal/models/reconciliation.go @internal/services/system/config_service.go:201 @internal/core/db/migrations/migration_169_reconciliation_dicts_configs.go @internal/core/db/database.go:455-468 @internal/utils/operlog/operlog.go @CLAUDE.md
</context>

<interfaces>
BaseService.Create(ctx,req,submitterID): req=Title/CategoryID/Type/Priority/Description/AssigneeID。WorkOrderTypeIncident=incident。WorkOrderPriority Low(0)/Medium(1)/High(2)/Urgent(3)。R1 dispatch taskType="reconciliation"+params["param"]。GetByKey 返 *models.Config(ConfigValue 存 JSONB)。SysDataReconciliation.WorkorderID *string uuid。
</interfaces>

<tasks>
<task type="auto" tdd="false">
  <name>Task 1: 模板 + 转单 service</name>
  <files>internal/services/workorder/reconciliation_template.go, internal/services/asset/reconciliation_workorder.go</files>
  <read_first>- internal/services/workorder/base.go:237-289 - internal/models/workorder.go:23-66 - internal/models/reconciliation.go - internal/services/system/config_service.go:201 - 43-CONTEXT.md D-A1~D-A2</read_first>
  <behavior>6 个 B-F 模板(5 句中文/DefaultPriority/AssigneeRoleKey);GetTemplate 返回模板;CreateWorkorderFromException 12 步;SLA 写入 description;失败 logrus.Errorf 不写 SysNotice</behavior>
  <action>reconciliation_template.go(同 workorder 包):ReconciliationWorkorderTemplate struct{ConflictType,TypePrefix,AssigneeRoleKey string;DescriptionLines []string;DefaultPriority models.WorkOrderPriority};5 个 B-F 模板(B="补责任人+核查转岗",C="核对物理使用人+确认 ops_asset.user_id 过期",D="确认是否在用+标记废弃",E="反查 sys_workstation+补充 ops_asset 或标记下线",F="检查 AD is_enabled+同步 sys_user.status"),每条 TypePrefix="对账-X类"+5 句中文;DefaultPriority B/C=High(2),D/F=Medium(1),E=Low(0);AssigneeRoleKey B/C/D="asset_owner",E="ops_owner",F="responsible_owner";func GetTemplate(conflictType)*ReconciliationWorkorderTemplate(空/A/未知返回 nil)。reconciliation_workorder.go(asset 包):ReconciliationWorkorderService+构造器;type AssigneeRoleMap map[string]int64;func CreateWorkorderFromException 12 步:(a) SELECT SysDataReconciliation WHERE id=? AND workorder_id IS NULL AND deleted_at IS NULL;(b) SELECT WorkOrderCategory WHERE category_name="对账-{Type}类";(c) tpl=workorder.GetTemplate(rec.ConflictType),nil→err;(d) configSvc.GetByKey("asset.reconciliation.workorder.assignee_role_map") 软失败;(e) JSONB 反查 role_id→SELECT sys_user WHERE role_id=? AND status=0 LIMIT 1;(f) json.Unmarshal(rec.RawSnapshot) 取 asset_code;(g) title=fmt.Sprintf("[资产对账·%s类] 资产 %s (%s) %s",conflictType,asset_code,severity,detectedAt.Format("2006-01-02 15:04"));(h) description=descLines+"\n\n【原始数据】\n"+raw_snapshot+"\n\n## SLA: "+slaMin+"m";(i) workorder.NewBaseService(s.db).Create(ctx,&workorder.CreateRequest{...},"SYSTEM");(j) 失败 logrus.Errorf,return nil,err;(k) 成功 s.db.Model(&rec).Update("workorder_id",wo.ID);(l) return wo。helper severityToSLAMinutes(severity) int:critical=30,high=240,medium=1440,low=10080。</action>
  <verify><automated>go build ./internal/services/workorder/... ./internal/services/asset/... 2>&1 | head -20</automated></verify>
  <acceptance_criteria>go build 退出码 0;reconciliation_template.go 含 5 个 B-F var+GetTemplate;reconciliation_workorder.go 含 CreateWorkorderFromException;imports 含 encoding/json,strings,logrus,workorder,system</acceptance_criteria>
  <done>模板常量就位;CreateWorkorderFromException 12 步完整;失败仅 logrus</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: cron 续写 + 2 migration + database.go 注册</name>
  <files>internal/scheduler/reconciliation_tasks.go, internal/core/db/migrations/migration_171_reconciliation_workorder_assignee_role.go, internal/core/db/migrations/migration_172_reconciliation_workorder_templates_seed.go, internal/core/db/database.go</files>
  <read_first>- internal/scheduler/reconciliation_tasks.go (R1 4 case + 2 jobNameToInvokeTarget mapping)- internal/core/db/migrations/migration_169_reconciliation_dicts_configs.go (seed count-then-insert 模式)- internal/core/db/database.go:455-468 (R1 注册块位置)</read_first>
  <behavior>scheduler switch 新增 2 case(createWorkorderCritical/createWorkorderHigh);实例化 ReconciliationWorkorderService;查未转单异常+CreateWorkorderFromException;migration_171 seed sys_config 2 个 key;database.go 在 Migrate170 后追加 2 行注册</behavior>
  <action>(1) 续写 internal/scheduler/reconciliation_tasks.go:在 NewReconciliationWorkorderService(db) 后,RegisterTask switch 加 2 case("createWorkorderCritical":SELECT * FROM sys_data_reconciliation WHERE severity='critical' AND deleted_at IS NULL AND resolved_at IS NULL AND workorder_id IS NULL ORDER BY detected_at ASC LIMIT 50 → for each { woSvc.CreateWorkorderFromException(ctx, rec.ID); if err != nil { logrus.Errorf(...); continue } } 同 case "createWorkorderHigh" 但 severity='high' LIMIT 30);在 reconJobs slice 加 2 条 record(jobName="对账-自动转工单critical"/"high",cronExpression="@every 2m"/"@every 5m",remark="Phase 43 R2: critical/high 异常自动转工单");jobNameToInvokeTarget 加 2 个 mapping("对账-自动转工单critical"→"reconciliation:createWorkorderCritical", "对账-自动转工单high"→"reconciliation:createWorkorderHigh")。(2) 创建 migration_171_reconciliation_workorder_assignee_role.go:Migrate171ReconciliationWorkorderAssigneeRole(db) 函数,seed sys_config:key="asset.reconciliation.workorder.assignee_role_map",value=`{"asset_owner":1,"ops_owner":2,"responsible_owner":3}`(JSONB 字符串,role_id 1/2/3 是占位 — 运维按实际 role id 改),config_type='Y',is_system=1;外加 key="asset.reconciliation.workorder.sla_minutes_by_severity",value=`{"critical":30,"high":240,"medium":1440,"low":10080}`,config_type='Y',is_system=1;幂等:count-then-insert 模式(migration_165 风格)。(3) 创建 migration_172_reconciliation_workorder_templates_seed.go:Migrate172ReconciliationWorkorderTemplatesSeed(db),补种 6 个 category 的 description:SELECT id, category_name FROM sys_workorder_category WHERE category_name LIKE '对账-%类' → 对 B-F 5 个 category,UPDATE description = 5 句中文(同 reconciliation_template.go 的 DescriptionLines 内容);A 类 description = "物理+责任人有且一致 健康无需动作";幂等:UPDATE 仅在原 description 为空或 R1 默认值时执行,WHERE 条件加 description IS NULL OR description=''。(4) 修改 internal/core/db/database.go:在 Migrate170 之后(line 469 之后)追加 2 行注册:`if err := migrations.Migrate171ReconciliationWorkorderAssigneeRole(d.DB); err != nil { applogger.Errorf("Phase 43 R2 资产对账 workorder assignee_role 失败: %v", err) }` + `Migrate172ReconciliationWorkorderTemplatesSeed(d.DB)`。</action>
  <verify><automated>go build ./... 2>&1 | head -20 && grep -n "createWorkorderCritical\|createWorkorderHigh" internal/scheduler/reconciliation_tasks.go && grep -n "Migrate171\|Migrate172" internal/core/db/database.go && grep -n "asset.reconciliation.workorder.assignee_role_map\|asset.reconciliation.workorder.sla_minutes_by_severity" internal/core/db/migrations/migration_171_reconciliation_workorder_assignee_role.go</automated></verify>
  <acceptance_criteria>go build ./... 退出码 0;grep 命中 createWorkorderCritical/createWorkorderHigh 2 case;grep 命中 Migrate171/Migrate172 2 行注册;grep 命中 sys_config 2 个 key 字符串(asset.reconciliation.workorder.assignee_role_map + .sla_minutes_by_severity);sys_job 表存在 2 条 R2 cron record(execute-phase 启动后,SQL: SELECT count(*) FROM sys_job WHERE job_name LIKE '对账-自动转工单%' 应 = 2;若 DB 未启动则记录于 SUMMARY.md 由用户 UAT 验证)</acceptance_criteria>
  <done>cron handler 就位;sys_config role_map + sla_minutes 2 条 seed;6 个 category description 补种;database.go 注册完成</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Cron → DB | Scheduler 触发转单后读写 sys_data_reconciliation + sys_workorder |
| Sys_config → Service | GetByKey 读 assignee_role_map JSONB,运维预配置 |
## STRIDE Threat Register
| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-43-01 | EoP | 转单越权 | mitigate | submitterID="SYSTEM"+workorder status=Pending |
| T-43-02 | Repudiation | 转单无审计 | mitigate | BaseService.Create 内部写 sys_oper_log |
| T-43-03 | DoS | 大量异常 | mitigate | uniq_recon_asset_type_open 唯一索引 |
| T-43-04 | T | JSONB 解析 | mitigate | json.Unmarshal 失败回退 nil |
| T-43-SC | T | deps | mitigate | 无新依赖 |
</threat_model>

<verification>
1. go build ./... 退出码 0
2. reconciliation_template.go 含 5 个 B-F var+GetTemplate
3. reconciliation_workorder.go 含 CreateWorkorderFromException
4. scheduler/reconciliation_tasks.go 续写 2 case+2 sys_job
5. database.go 注册 Migrate171+172
6. migration_171 seed 2 个 sys_config
7. migration_172 补种 6 个 category description
</verification>

<success_criteria>
- critical ≤2min/high ≤5min 自动转工单(SC 1+2)
- 工单标题 D-A2-01 模板(SC 3)
- 6 类工单模板差异化(SC 3)
- 失败仅 logrus(D-A1-03)
- 转单成功 UPDATE workorder_id(D-A1-04)
- go build 通过
</success_criteria>

<output>
Create .planning/phases/43-r2/43-01-SUMMARY.md when done
</output>
