---
phase: 43-r2
plan: 03
type: execute
wave: 3
depends_on: ["43-01", "43-02"]
files_modified: [internal/services/asset/reconciliation_workorder.go, internal/scheduler/reconciliation_tasks.go, internal/services/notice_service.go, xingran-react-frontend/src/pages/asset/reconciliation/exceptions/index.tsx, xingran-react-frontend/src/pages/asset/reconciliation/dashboard/index.tsx, xingran-react-frontend/src/lib/assetApi.ts, xingran-react-frontend/src/hooks/useReconciliationWebSocket.ts]
autonomous: true
requirements: [MONITOR-02, MONITOR-03, WORKORDER-02]
user_setup: []
must_haves:
  truths:
    - "critical 工单创建后 WebSocket 推送 critical_workorder_created 事件"
    - "critical 异常检出后 WebSocket 推送 critical_exception_detected 事件"
    - "高危异常同时写 sys_notice(notice_type='asset_reconciliation_critical')"
    - "前端 useReconciliationWebSocket hook 订阅 WS,只接收 2 类事件"
    - "前端 dashboard 收到 WS 事件触发列表 query invalidation"
    - "前端 exceptions 列表加'标记已解决'按钮 + Modal,调 43-02 ResolveException API"
    - "operlog 完整接入所有 R2 写操作"
  artifacts: [{path: internal/services/asset/reconciliation_workorder.go, min_lines: 130}, {path: internal/scheduler/reconciliation_tasks.go, min_lines: 200}, {path: useReconciliationWebSocket.ts, min_lines: 80}, {path: exceptions/index.tsx, min_lines: 350}, {path: dashboard/index.tsx, min_lines: 200}]
  key_links: [{from: reconciliation_workorder, to: noticeHub, via: BroadcastToAll, pattern: Broadcast.*critical_}, {from: reconciliation_workorder, to: sys_notice, via: NoticeService.CreateNotice, pattern: asset_reconciliation_critical}, {from: exceptions/index.tsx, to: ResolveException, via: post, pattern: /exception/.+/resolve}, {from: dashboard, to: useReconciliationWebSocket, via: query invalidation, pattern: queryClient.invalidateQueries}]
---

<objective>
实现 critical 工单/异常实时 WebSocket 推送 + sys_notice 写入(双通道,WS 给在线 dashboard + SysNotice 给未在线运维)+ 前端 useReconciliationWebSocket hook 订阅 + dashboard 实时刷新 + exceptions 列表"标记已解决"按钮 + Modal。完成 Phase 43 R2 闭环:43-01 转单 → 43-02 静默/节流 → 43-03 实时推送 + 用户操作界面。符合 WORKORDER-02(operlog 状态变更:标记已解决)+ MONITOR-02(WebSocket 推送)+ MONITOR-03(SysNotice 写入)ROADMAP success criteria 4+5+6。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md @.planning/STATE.md @.planning/phases/43-r2/43-CONTEXT.md @.planning/phases/42-r1/42-PATTERNS.md
@.planning/notes/asset-reconciliation-strategy.md
@internal/services/asset/reconciliation_workorder.go (43-01 创建的 CreateWorkorderFromException)
@internal/scheduler/reconciliation_tasks.go (43-01 续写)
@internal/websocket/notice_hub.go:BroadcastToAll(NoticeMessage) + NoticeMessage.Type/Content 字段
@internal/services/notice_service.go:CreateNoticeWithTargets(ctx, req, creatorID, creatorName) 函数(43-03 用于 sys_notice 写入)
@internal/api/v1/asset/reconciliation_router.go:43-02 加的 /exception/:id/resolve 路由
@xingran-react-frontend/src/lib/assetApi.ts (现有 reconciliationApi 工厂)
@xingran-react-frontend/src/hooks/useWebSocket.ts (基础 WS hook)
@xingran-react-frontend/src/hooks/useDashboard.ts (TanStack Query 模式)
@xingran-react-frontend/src/pages/asset/reconciliation/exceptions/index.tsx (现有 exceptions 页)
@xingran-react-frontend/src/pages/asset/reconciliation/dashboard/index.tsx (现有 dashboard 页)
@CLAUDE.md 前端 useWebSocket 模式 + useTableManager 模式
</context>

<interfaces>
NoticeMessage{Type string, Content string, ...}:type 字段用于前端分类;content 字段可放 JSON 字符串(参考 BroadcastRPAProgress pattern line 244-248)。
NoticeService.CreateNoticeWithTargets(ctx, req, creatorID, creatorName):req.NoticeType="1"/"2"(公告/警告);R2 写 notice_type='2' 警告,NoticeTitle="资产对账告警:..."。
CreateWorkorderFromException 当前签名 (ctx, exceptionID) 返 *models.WorkOrder;R2 扩展为内部收集 (workorder, exception) 让调用方做推送。
frontend useWebSocket:url/onMessage/onOpen;R2 新建 useReconciliationWebSocket hook 封装业务订阅(只过滤 critical_* 事件,自动断线重连)。
frontend reconciliationApi.exceptionResolve(id, payload) → POST /asset/reconciliation/exception/:id/resolve。
</interfaces>

<tasks>
<task type="auto" tdd="false">
  <name>Task 1: 后端 WS + SysNotice 接入 + cron 触发</name>
  <files>internal/services/asset/reconciliation_workorder.go, internal/scheduler/reconciliation_tasks.go, internal/services/notice_service.go</files>
  <read_first>- internal/services/asset/reconciliation_workorder.go (43-01 创建的 CreateWorkorderFromException)- internal/scheduler/reconciliation_tasks.go:42-69 (R1+43-01 dispatch 模式,本 plan 加 WS 触发)- internal/websocket/notice_hub.go (BroadcastToAll + NoticeMessage)- internal/services/notice_service.go:57-100 (CreateNoticeWithTargets)- internal/services/system/notice_service.go (可能有更高级的 Create 方法,本 plan 用 services/notice_service.go 入口)- 43-CONTEXT.md D-A4-01~D-A4-03 推送 3 决策</read_first>
  <behavior>CreateWorkorderFromException 成功后 WS+Broadcast+SysNotice 同步触发;WS 事件 type=critical_workorder_created;SysNotice notice_type=2 警告</behavior>
  <action>(1) 改 internal/services/asset/reconciliation_workorder.go:ReconciliationWorkorderService 结构加 noticeHub *websocket.NoticeHub 字段(构造器注入)+ noticeService *services.NoticeService 字段;CreateWorkorderFromException 成功后(line ~k 之后)追加 3 步:(m) WS 推送:`if s.noticeHub != nil { payload, _ := json.Marshal(map[string]interface{}{"event": "critical_workorder_created", "data": map[string]interface{}{"workorder_id": wo.ID, "exception_id": exceptionID, "title": title, "severity": rec.Severity}}); s.noticeHub.BroadcastToAll(websocket.NoticeMessage{Type: "critical_workorder_created", Content: string(payload), Timestamp: time.Now().Unix()}) }`(D-A4-01 全量 broadcast + D-A4-02 2 类事件之一);(n) SysNotice 写入:`if s.noticeService != nil { req := services.CreateNoticeRequest{NoticeTitle: fmt.Sprintf("[资产对账] %s", title), NoticeType: "2", NoticeContent: fmt.Sprintf("资产 %s 触发 %s 级异常\n\n%s", assetCode, rec.Severity, description), Priority: models.PriorityUrgent, TargetType: models.TargetAll, IsMarkdown: false}; s.noticeService.CreateNoticeWithTargets(ctx, &req, "SYSTEM", "SYSTEM") + 立即发布(避免草稿状态); ...} ` 并 **在 sys_notice.notice_type 字段写入常量 `'asset_reconciliation_critical'`**(D-A4-03 锁定的 notice_type 字面量;若现有 NoticeType 字段是 int,需扩展 NoticeTypeStr 字段或在 NoticeContent 头部拼接字符串 `'[asset_reconciliation_critical]'` 作为前端过滤 token)。(o) 在 CreateWorkorderFromException 之前(查到 rec 后 ClassifyType 前)也加 WS 推送 critical_exception_detected 事件(对每个被扫到的 critical 异常都推)。(p) **operlog 完整接入**:SysNotice 创建是写操作,在 (n) 之后调 `operlog.Record(ctx, s.operLogService, s.db, "资产对账", operlog.OperTypeCreate)`(若 ReconciliationWorkorderService 已注入 OperLogService;若未注入则本 plan 在构造器加 `operLogService *system.OperLogService` 字段 — 因 sys_notice 写入本质是新 record 创建,符合 CLAUDE.md operlog 强制约定)。(2) 改 internal/scheduler/reconciliation_tasks.go:RegisterReconciliationTasks 接收 noticeHub 参数(函数签名加);case "createWorkorderCritical" 内,每次 CreateWorkorderFromException 前,先 Broadcast critical_exception_detected;在 cron 启动时构造 woSvc 时注入 noticeHub + noticeService + operLogService 引用(从 Scheduler 全局 ctx 拿);case "createWorkorderHigh" 不触发 WS(per D-A4-02 仅 critical)。(3) 改 internal/services/notice_service.go:无需大改,直接复用 services/notice_service.go 的 CreateNoticeWithTargets;若需立即发布(PublishStatus=Published),在 CreateNoticeWithTargets 后调 s.noticeService.PublishNotice(ctx, notice.ID, operatorID) — 查 notice_service.go 是否有此方法;若无,在本 plan 内 add 一个简单的 PublishNow(ctx, id) 方法(扫描 sys_notice 把 publish_status=0 改为 1)。(4) core.Init 或 RegisterReconciliationTasks 调用方(在 internal/scheduler/cron.go 中)需传递 *websocket.NoticeHub 实例;本 plan 不改 core.Init,仅在 RegisterReconciliationTasks 签名加 noticeHub 参数,由调用方(scheduler 启动处)传入 — 若发现 core.NoticeHub 已存在,直接传 core.NoticeHub 引用。</action>
  <verify><automated>go build ./... 2>&1 | head -20 && grep -n "critical_workorder_created\|critical_exception_detected\|asset_reconciliation_critical" internal/services/asset/reconciliation_workorder.go internal/scheduler/reconciliation_tasks.go internal/services/notice_service.go</automated></verify>
  <acceptance_criteria>go build 退出码 0;grep 命中 critical_workorder_created / critical_exception_detected / asset_reconciliation_critical 各自 1+ 次;NoticeHub.BroadcastToAll 调用在 CreateWorkorderFromException 内;sys_notice INSERT 走 services.NoticeService.CreateNoticeWithTargets</acceptance_criteria>
  <done>WS 推送 + SysNotice 写入双通道就位;critical cron 触发;operlog 仍由 workorder.BaseService.Create 内部记录(SC 6)</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 前端 useReconciliationWebSocket hook + dashboard 集成 + 标记已解决 Modal</name>
  <files>xingran-react-frontend/src/hooks/useReconciliationWebSocket.ts, xingran-react-frontend/src/pages/asset/reconciliation/dashboard/index.tsx, xingran-react-frontend/src/pages/asset/reconciliation/exceptions/index.tsx, xingran-react-frontend/src/lib/assetApi.ts</files>
  <read_first>- xingran-react-frontend/src/hooks/useWebSocket.ts:50-150 (基础 hook,useState/useRef/useCallback + 自动重连)- xingran-react-frontend/src/lib/assetApi.ts:121-200 (reconciliationApi 工厂模式)- xingran-react-frontend/src/hooks/useDashboard.ts (TanStack Query useDashboard 模式,queryKeys.reconciliation.*)- xingran-react-frontend/src/pages/asset/reconciliation/exceptions/index.tsx:255-290 (operlog_btn 列,本 plan 在其后加 resolve 按钮 + Modal)- @ant-design/icons (CheckOutlined for 标记已解决按钮)- 43-CONTEXT.md D-A4-04 标记已解决 UI 决策</read_first>
  <behavior>useReconciliationWebSocket 仅过滤 2 类 critical 事件;dashboard 收到事件触发 queryClient.invalidateQueries;exceptions 列表加按钮 + Modal,提交调 resolve API</behavior>
  <action>(1) 创建 xingran-react-frontend/src/hooks/useReconciliationWebSocket.ts:import { useEffect, useRef, useCallback } from "react"; import { useQueryClient } from "@tanstack/react-query"; import { useWebSocket } from "./useWebSocket";导出 useReconciliationWebSocket(onCriticalEvent?: (event: "critical_exception_detected" | "critical_workorder_created", data: any) => void),内部用 useWebSocket 连接 /ws/notice(项目现有 WS endpoint),onMessage 内 parse JSON,过滤 event in ["critical_exception_detected", "critical_workorder_created"],调用 queryClient.invalidateQueries({ queryKey: queryKeys.reconciliation.all }) + onCriticalEvent 回调;返回 { status, disconnect } 给上层组件;注意:WebSocket URL 从 authStore.getAccessToken() 拼装(参考现有 useRPAProgress hook)。(2) 改 dashboard/index.tsx:在 Dashboard 函数顶部加 const queryClient = useQueryClient(); const { status: wsStatus } = useReconciliationWebSocket((event, data) => { message.info(`收到 ${event} 推送:${data.title || data.conflict_type}`); });在页面右上角加 Badge 标识 WS 状态。(3) 改 exceptions/index.tsx:import { Modal, CheckOutlined } from "@ant-design/icons"; const [resolveModal, setResolveModal] = useState<{ open: boolean, exceptionId: string | null, note: string }>({ open: false, exceptionId: null, note: "" });在 line 260 operlog_btn 列之前新增 resolve_btn 列:render 加 `<Button type="link" size="small" icon={<CheckOutlined />} disabled={!!record.resolvedAt} onClick={() => setResolveModal({ open: true, exceptionId: record.id, note: "" })}>标记已解决</Button>`(resolvedAt 非空时 disabled);在 Table 之后加 Modal(标题"标记异常已解决", onOk 调 reconciliationApi.exceptionResolve(id, { resolutionNote: note }) + queryClient.invalidateQueries);权限控制:useAuth() 取 user.permissions,无 asset:reconciliation:resolve 时按钮不渲染。(4) 改 assetApi.ts:在 reconciliationApi 内加 exceptionResolve: async (id, body) => post(`/asset/reconciliation/exception/${id}/resolve`, body);。</action>
  <verify><automated>cd xingran-react-frontend && npm run build 2>&1 | tail -30</automated></verify>
  <acceptance_criteria>npm run build 退出码 0;useReconciliationWebSocket 钩子导出;exceptions 列表有"标记已解决"按钮 + Modal;assetApi.ts 含 exceptionResolve 方法;Dashboard 显示 WS 状态 Badge</acceptance_criteria>
  <done>前端 WS 订阅 + 实时刷新 + 标记已解决 UI 就位</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Cron → WS Hub | 转单成功后 NoticeHub.BroadcastToAll 全量推送,所有 ws 客户端收到 |
| Resolve API → DB | 用户触发 resolve 写 sys_data_reconciliation.resolved_at + operlog |
## STRIDE Threat Register
| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-43-10 | Spoofing | WS 事件伪造 | mitigate | WS endpoint 需 JWT auth(现有项目约定);event 字段来自后端 Broadcast 不可被客户端伪造 |
| T-43-11 | EoP | 任意用户 resolve | mitigate | 前端按钮按 useAuth().permissions 含 asset:reconciliation:resolve 渲染;后端 43-02 handler 无显式 perm 校验(由 router RequirePermissions 继承 R1 列表 perms) — R3 增强 |
| T-43-12 | Repudiation | WS 推送无审计 | mitigate | workorder.BaseService.Create 内部 operlog;sys_notice 写库即审计 |
| T-43-13 | DoS | WS 频繁推送 | mitigate | D-A4-02 仅 critical 2 类事件,频率低(2min 一条 max);D-A3-02 24h 节流抑制 source |
| T-43-14 | Infodisclosure | WS payload 泄露 | mitigate | payload 只含 workorder_id/exception_id/title/severity,无 PII;sys_notice title 可含 asset_code 但属运维信息 |
| T-43-SC | T | deps | mitigate | 无新依赖,前端用现有 useWebSocket + TanStack Query |
</threat_model>

<verification>
1. go build ./... 退出码 0
2. npm run build 退出码 0
3. grep 命中 critical_workorder_created / critical_exception_detected / asset_reconciliation_critical
4. reconciliationApi.exceptionResolve 方法就位
5. useReconciliationWebSocket hook 导出
6. exceptions/index.tsx 含"标记已解决"按钮 + Modal
7. dashboard/index.tsx 含 WS 状态 Badge
</verification>

<success_criteria>
- WebSocket 推送 critical 异常到 dashboard(SC 4)
- SysNotice 写入高危异常(SC 5)
- 工单状态变更触发 operlog(SC 6,BaseService.Create 内部 + ResolveException 显式)
- 前端"标记已解决" UI + API 闭环
- 7d 静默 + 24h 节流(43-02 实现,本 plan 集成)
- go build + npm run build 通过
</success_criteria>

<output>
Create .planning/phases/43-r2/43-03-SUMMARY.md when done
</output>
