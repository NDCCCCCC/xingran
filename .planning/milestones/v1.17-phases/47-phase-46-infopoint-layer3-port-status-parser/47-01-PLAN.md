---
phase: 47-phase-46-infopoint-layer3-port-status-parser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/services/asset/reconciliation_detection.go
  - internal/services/asset/reconciliation_detection_test.go
autonomous: true
requirements_addressed:
  - R2
  - R3
estimated_duration: 1 上下文窗口(~45 分钟)
must_haves:
  truths:
    - "Layer 3 cron 5 分钟内能将 sys_data_reconciliation 卡住的 open 异常重新分类(原 D 异常可在 UPSERT 后变 C)"
    - "Layer 3 不再因 SQLSTATE 23505 吞错 (D-01) — UPSERT 命中 UPDATE 路径不再抛错"
    - "DetectLayer3 返回签名 (inserted, skipped, skippedSilence, skippedThrottle, error) 不变;UPSERT 命中计入 inserted"
    - "对账看板 HealthScore 不再因 open conflict 累积红点误报"
    - "go build ./... 退出码 0;go test -run TestDetectLayer3 全部用例通过"
  artifacts:
    - path: "internal/services/asset/reconciliation_detection.go"
      provides: "DetectLayer3 INSERT→UPSERT 改造"
      contains: "clause.OnConflict"
    - path: "internal/services/asset/reconciliation_detection_test.go"
      provides: "UPSERT 行为 3 个新子测试"
      contains: "TestDetectLayer3Upsert"
  key_links:
    - from: "internal/services/asset/reconciliation_detection.go"
      to: "uniq_recon_asset_type_open"
      via: "GORM clause.OnConflict.Target"
      pattern: "OnConflict.*uniq_recon_asset_type_open"
    - from: "internal/services/asset/reconciliation_detection.go"
      to: "isReconciliationDuplicate"
      via: "死代码删除(SQLSTATE 23505 catch 已不需要,但函数定义保留)"
      pattern: "if isReconciliationDuplicate"
---

<objective>
把 `internal/services/asset/reconciliation_detection.go` 的 `DetectLayer3` 由 INSERT-only 改造为 GORM UPSERT(ON CONFLICT 复用 partial unique index `uniq_recon_asset_type_open`),删除死代码 `isReconciliationDuplicate(err)` catch 路径,新增 3 个子测试覆盖 UPSERT 三种命中场景(无 open → INSERT;有 open → UPDATE;resolved_at NOT NULL → INSERT)。

按 CONTEXT.md 锁定:
- D-01: UPSERT 命中 partial unique index `uniq_recon_asset_type_open` → 9 字段全 SET `EXCLUDED.*` + `detected_at = NOW()`
- D-02: 9 字段为 severity / raw_snapshot / physical_value / declared_value / ad_value / asset_ip / exception_rule_id / applied_actions / confidence_score + detected_at
- D-03: 返回签名 `(inserted, skipped, skippedSilence, skippedThrottle int, error)` 不变;UPSERT 命中计入 `inserted++`(不论是 INSERT 还是 UPDATE 路径)

D-04/D-05(R5)由 47-02 处理,本计划不涉及。
R1/R4 已由 migration_188 / migration_183 闭合,本计划不复述。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-phase-46-infopoint-layer3-port-status-parser/47-CONTEXT.md
@internal/services/asset/reconciliation_detection.go
@internal/services/asset/reconciliation_detection_test.go
@internal/core/db/migrations/migration_168_reconciliation_tables.go
@internal/models/reconciliation.go
@CLAUDE.md (operlog 跳过 cron,Status 0=正常)
</context>

<interfaces>
From internal/services/asset/reconciliation_detection.go:209-404:
```go
func (s *reconciliationDetectionImpl) DetectLayer3(ctx context.Context) (int, int, int, int, error)
// 返回: inserted, skipped, skippedSilence, skippedThrottle, error
// 当前实现: 行 385-394 = INSERT-only 路径,行 387 isReconciliationDuplicate(err) catch
```

From internal/core/db/migrations/migration_168_reconciliation_tables.go:144-157:
```sql
CREATE UNIQUE INDEX uniq_recon_asset_type_open
    ON sys_data_reconciliation (asset_id, conflict_type)
    WHERE resolved_at IS NULL AND deleted_at IS NULL;
-- partial unique index 名称: uniq_recon_asset_type_open
```

From internal/models/reconciliation.go (SysDataReconciliation 字段):
```go
type SysDataReconciliation struct {
    ID               string          `gorm:"primaryKey;type:varchar(64)"`
    AssetID          string          `gorm:"type:varchar(64);not null;index"`
    ConflictType     string          `gorm:"type:varchar(8);not null"`
    Severity         string          `gorm:"type:varchar(16)"`
    ConfidenceScore  float64         `gorm:"type:decimal(5,2)"`
    RawSnapshot      datatypes.JSON  `gorm:"type:jsonb"`
    PhysicalValue    datatypes.JSON  `gorm:"type:jsonb"`
    DeclaredValue    datatypes.JSON  `gorm:"type:jsonb"`
    ADValue          datatypes.JSON  `gorm:"type:jsonb"`
    AssetIP          *string         `gorm:"type:inet"`
    ExceptionRuleID  *string         `gorm:"type:varchar(64)"`
    AppliedActions   pq.StringArray  `gorm:"type:varchar(64)[]"`
    DetectedAt       time.Time       `gorm:"not null;default:NOW()"`
    CreatedAt        time.Time       `gorm:"autoCreateTime"`
    UpdatedAt        time.Time       `gorm:"autoUpdateTime"`
    DeletedAt        gorm.DeletedAt  `gorm:"index"`
    ResolvedAt       *time.Time
    ResolvedBy       *string
}
```

From internal/services/asset/reconciliation_detection_test.go (test 模式参考):
```go
func setupTestDB(t *testing.T, name string) *gorm.DB
// 使用 sqlite in-memory + raw SQL seed,然后 svc := &reconciliationDetectionImpl{db: db}
// svc.DetectLayer3(context.Background()) 验证 inserted 数 + db.Raw(...) 验证字段
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.1: 改造 DetectLayer3 INSERT 为 GORM UPSERT</name>
  <read_first>
    - D:\CODE\ClaudeCode\xingran-go-backend\internal\services\asset\reconciliation_detection.go (lines 380-404)
    - D:\CODE\ClaudeCode\xingran-go-backend\internal\models\reconciliation.go (SysDataReconciliation 字段定义)
    - D:\CODE\ClaudeCode\xingran-go-backend\internal\core\db\migrations\migration_168_reconciliation_tables.go (lines 144-157,确认 uniq_recon_asset_type_open 名称)
    - D:\CODE\ClaudeCode\xingran-go-backend\internal\services\asset\reconciliation_detection_test.go (现有 TestDetectLayer3* 模式参考)
  </read_first>
  <action>
修改 `internal/services/asset/reconciliation_detection.go` 的 `DetectLayer3` 函数:

**1.1.1** 在文件顶部 import 块加 `"gorm.io/gorm/clause"`(若尚未引入;先 `grep -n "gorm.io/gorm/clause" internal/services/asset/reconciliation_detection.go`,若 0 命中则追加)。

**1.1.2** 替换 line 385-394 的 INSERT+catch 路径为 UPSERT。

原代码块:
```go
err := s.db.WithContext(ctx).Create(rec).Error
if err != nil {
    if isReconciliationDuplicate(err) {
        logrus.WithError(err).WithField("asset_id", row.AssetID).
            Debug("Layer 3 detect: duplicate key skipped (D-11)")
        skipped++
        continue
    }
    return inserted, skipped, skippedSilence, skippedThrottle, err
}
inserted++
```

替换为(在 line 370 `rec := &models.SysDataReconciliation{...}` 之后):
```go
// R3 / D-01/D-02/D-03: UPSERT 语义(2026-07-03 Phase 47 改造)
// 原实现 INSERT-only, 24h 内同 (asset_id, conflict_type) 已存在 open 行 → 抛 SQLSTATE 23505 →
//   isReconciliationDuplicate catch → skipped++. 现改为 ON CONFLICT 复用 partial unique index
//   uniq_recon_asset_type_open (resolved_at IS NULL AND deleted_at IS NULL),命中即更新 9 字段
//   severity/raw_snapshot/physical_value/declared_value/ad_value/asset_ip/exception_rule_id/
//   applied_actions/confidence_score + detected_at=NOW()。
// 返回 inserted 累计 INSERT 或 UPDATE 命中数(语义统一 D-03)。
rec.ID = ""                  // 显式重置,确保 GORM 走 INSERT-or-UPDATE 而非 full UPDATE
rec.CreatedAt = time.Time{}  // 同上
err := s.db.WithContext(ctx).Clauses(clause.OnConflict{
    Columns: []clause.Column{
        {Name: "asset_id"},
        {Name: "conflict_type"},
    },
    Target: "uniq_recon_asset_type_open", // partial unique index 名
    DoUpdates: clause.Assignments(map[string]interface{}{
        "severity":          gorm.Expr("EXCLUDED.severity"),
        "raw_snapshot":      gorm.Expr("EXCLUDED.raw_snapshot"),
        "physical_value":    gorm.Expr("EXCLUDED.physical_value"),
        "declared_value":    gorm.Expr("EXCLUDED.declared_value"),
        "ad_value":          gorm.Expr("EXCLUDED.ad_value"),
        "asset_ip":          gorm.Expr("EXCLUDED.asset_ip"),
        "exception_rule_id": gorm.Expr("EXCLUDED.exception_rule_id"),
        "applied_actions":   gorm.Expr("EXCLUDED.applied_actions"),
        "confidence_score":  gorm.Expr("EXCLUDED.confidence_score"),
        "detected_at":       gorm.Expr("NOW()"),
    }),
}).Create(rec).Error
if err != nil {
    return inserted, skipped, skippedSilence, skippedThrottle, err
}
inserted++  // D-03: UPSERT 命中(无论 INSERT 或 UPDATE)都计入 inserted
```

**1.1.3** 删除原 SQLSTATE 23505 catch 块(在替换中已删)。保留 `isReconciliationDuplicate(err) bool` 函数定义(line 407-420)— 它仍可能在 unit test / 外部兼容路径中被引用。先 `grep -rn "isReconciliationDuplicate" internal/` 确认无外部调用,**只删 call site,不删函数定义**(CONTEXT canonical_refs 第 134 行明确"保留不删 — 防回归 / 单元测试可复用")。

**1.1.4** 在 `DetectLayer3` 函数 doc comment 区域追加变更日志注释:
```go
// 2026-07-03 Phase 47 R3 改造:
//   - D-01: INSERT → UPSERT (ON CONFLICT uniq_recon_asset_type_open)
//   - D-02: 9 字段 EXCLUDED.* + detected_at=NOW() (resolved_at/resolved_by 不更新)
//   - D-03: 返回签名保留,UPSERT 命中计入 inserted 而非 skipped
//   - 移除 isReconciliationDuplicate(err) catch 路径,函数定义保留
```

**注意事项**:
- 不要修改 `rec.DetectedAt = time.Now()` (line 382) 的赋值 — UPSERT 路径 DoUpdates 已显式 `EXCLUDED.detected_at` + `NOW()`,但保留 `time.Now()` 保证 INSERT 路径默认有值。
- `clause.OnConflict{Columns:...}` 的两列 `asset_id` + `conflict_type` 必须与 partial unique index `uniq_recon_asset_type_open` 列序一致,否则 PG 报 "there is no unique or exclusion constraint matching the ON CONFLICT specification"。
- `EXCLUDED.` 是 PG INSERT ON CONFLICT 标准语法(GORM 会翻译成 SQLite 的 ON CONFLICT DO UPDATE),sqlite test 兼容。
- 不要把 `gorm.Expr("NOW()")` 改为 `gorm.Expr("EXCLUDED.detected_at")` — detected_at 不在 partial index 包含字段中,但 `NOW()` 让 UPDATE 路径也刷新时间戳,符合"重新分类"的语义。
- 不要加 operlog(cron 调用路径,operlog helper 注释明确跳过)。
- 不要把 `skipped++` 移到 UPSERT 失败分支 — UPSERT 失败应直接 return err(原路径也是)。
  </action>
  <acceptance_criteria>
    - `grep -n "clause.OnConflict" internal/services/asset/reconciliation_detection.go` 返回 ≥1 行
    - `grep -n "uniq_recon_asset_type_open" internal/services/asset/reconciliation_detection.go` 返回 ≥1 行
    - `grep -n "isReconciliationDuplicate(err)" internal/services/asset/reconciliation_detection.go` 返回 0 行(call site 已删)
    - `grep -n "func isReconciliationDuplicate" internal/services/asset/reconciliation_detection.go` 返回 1 行(函数定义保留)
    - `grep -c "EXCLUDED\." internal/services/asset/reconciliation_detection.go` 返回 ≥9 行(9 字段全覆盖,精确:severity / raw_snapshot / physical_value / declared_value / ad_value / asset_ip / exception_rule_id / applied_actions / confidence_score 共 9 个 EXCLUDED 引用,且最后 `detected_at` 使用 `NOW()` 而非 `EXCLUDED.detected_at`)
    - `go build ./...` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go test -run "TestDetectLayer3" -v ./internal/services/asset/...</automated>
  </verify>
  <done>
    3 个新 UPSERT 测试全绿:TestDetectLayer3UpsertInsertNoExisting / TestDetectLayer3UpsertUpdateExisting / TestDetectLayer3UpsertInsertAfterResolved;5 个原 TestDetectLayer3* 测试仍然 PASS;`go test ./internal/services/asset/...` 退出码 0。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.2: 新增 3 个 UPSERT 子测试覆盖三层命中场景</name>
  <read_first>
    - D:\CODE\ClaudeCode\xingran-go-backend\internal\services\asset\reconciliation_detection_test.go (TestDetectLayer3ExceptionHit 行 26-54 模式参考)
    - D:\CODE\ClaudeCode\xingran-go-backend\internal\services\asset\reconciliation_detection.go (新 UPSERT 段)
  </read_first>
  <action>
在 `internal/services/asset/reconciliation_detection_test.go` 末尾新增 3 个 UPSERT 行为子测试。

**测试模式参考** (TestDetectLayer3ExceptionHit 行 26-54):
```go
db := setupTestDB(t, "test_layer35_hit")
require.NoError(t, db.Exec(`INSERT INTO sys_reconciliation_exception ...`).Error)
uid := "00000000-0000-0000-0000-000000000010"
require.NoError(t, db.Exec(`INSERT INTO ops_asset (...) VALUES (...)`, uid, ...).Error)
require.NoError(t, db.Exec(`INSERT INTO ops_asset_physical (...) VALUES (...)`, uid, ...).Error)

svc := &reconciliationDetectionImpl{db: db}
inserted, _, _, _, err := svc.DetectLayer3(context.Background())
require.NoError(t, err)
assert.Equal(t, 1, inserted)

var rec struct { ... }
require.NoError(t, db.Raw(`SELECT ... FROM sys_data_reconciliation WHERE asset_id = ?`, uid).Scan(&rec).Error)
```

**1.2.1** TestDetectLayer3UpsertInsertNoExisting — 无 open → INSERT 路径:
```go
func TestDetectLayer3UpsertInsertNoExisting(t *testing.T) {
    db := setupTestDB(t, "test_upsert_insert")
    uid := "00000000-0000-0000-0000-000000000100"
    require.NoError(t, db.Exec(`INSERT INTO ops_asset (id, devicesn, machine_ip, user_id, status, deleted_at)
        VALUES (?, 'IP-100', '10.0.0.100', NULL, 0, NULL)`, uid).Error)
    require.NoError(t, db.Exec(`INSERT INTO ops_asset_physical (asset_id, physical_user_id, physical_username)
        VALUES (?, ?, 'alice')`, uid, uid).Error)

    svc := &reconciliationDetectionImpl{db: db}
    inserted, _, _, _, err := svc.DetectLayer3(context.Background())
    require.NoError(t, err)
    assert.Equal(t, 1, inserted)

    var rowCount int64
    require.NoError(t, db.Model(&models.SysDataReconciliation{}).
        Where("asset_id = ? AND deleted_at IS NULL", uid).Count(&rowCount).Error)
    assert.Equal(t, int64(1), rowCount, "无 open 时 UPSERT INSERT 应产生 1 行")

    var severity string
    require.NoError(t, db.Raw(`SELECT severity FROM sys_data_reconciliation WHERE asset_id = ?`, uid).
        Scan(&severity).Error)
    assert.NotEmpty(t, severity, "severity 应非空")
}
```

**1.2.2** TestDetectLayer3UpsertUpdateExisting — 有 open → UPDATE 路径:
```go
func TestDetectLayer3UpsertUpdateExisting(t *testing.T) {
    db := setupTestDB(t, "test_upsert_update")
    uid := "00000000-0000-0000-0000-000000000200"
    require.NoError(t, db.Exec(`INSERT INTO ops_asset (id, devicesn, machine_ip, user_id, status, deleted_at)
        VALUES (?, 'IP-200', '10.0.0.200', NULL, 0, NULL)`, uid).Error)
    require.NoError(t, db.Exec(`INSERT INTO ops_asset_physical (asset_id, physical_user_id, physical_username)
        VALUES (?, ?, 'bob')`, uid, uid).Error)

    // 预存一条 open 冲突行(模拟卡住的 D 异常)
    require.NoError(t, db.Exec(`INSERT INTO sys_data_reconciliation
        (id, asset_id, conflict_type, severity, raw_snapshot, physical_value, declared_value, ad_value,
         confidence_score, detected_at, resolved_at, deleted_at)
        VALUES (?, ?, 'D', 'medium', '{}', '{}', '{}', '{}', 0.5, ?, NULL, NULL)`,
        "rec-pre-existing", uid, time.Now().Add(-48*time.Hour)).Error)

    svc := &reconciliationDetectionImpl{db: db}
    inserted, _, _, _, err := svc.DetectLayer3(context.Background())
    require.NoError(t, err)
    assert.Equal(t, 1, inserted, "UPSERT UPDATE 命中应计入 inserted(D-03)")

    // 验证: 仍只有 1 行 (UPDATE 不增行), 且 severity 已覆盖
    var rowCount int64
    require.NoError(t, db.Model(&models.SysDataReconciliation{}).
        Where("asset_id = ? AND deleted_at IS NULL", uid).Count(&rowCount).Error)
    assert.Equal(t, int64(1), rowCount, "UPSERT UPDATE 不应新增行")

    var severity string
    require.NoError(t, db.Raw(`SELECT severity FROM sys_data_reconciliation WHERE asset_id = ?`, uid).
        Scan(&severity).Error)
    assert.NotEqual(t, "medium", severity, "severity 应已被 UPSERT 覆盖")
}
```

**1.2.3** TestDetectLayer3UpsertInsertAfterResolved — resolved 后 INSERT 新行:
```go
func TestDetectLayer3UpsertInsertAfterResolved(t *testing.T) {
    db := setupTestDB(t, "test_upsert_resolved")
    uid := "00000000-0000-0000-0000-000000000300"
    require.NoError(t, db.Exec(`INSERT INTO ops_asset (id, devicesn, machine_ip, user_id, status, deleted_at)
        VALUES (?, 'IP-300', '10.0.0.300', NULL, 0, NULL)`, uid).Error)
    require.NoError(t, db.Exec(`INSERT INTO ops_asset_physical (asset_id, physical_user_id, physical_username)
        VALUES (?, ?, 'charlie')`, uid, uid).Error)

    // 预存一条 resolved 历史行(不参与 partial unique index 冲突)
    resolvedAt := time.Now().Add(-72 * time.Hour)
    require.NoError(t, db.Exec(`INSERT INTO sys_data_reconciliation
        (id, asset_id, conflict_type, severity, raw_snapshot, physical_value, declared_value, ad_value,
         confidence_score, detected_at, resolved_at, resolved_by, deleted_at)
        VALUES (?, ?, 'D', 'medium', '{}', '{}', '{}', '{}', 0.5, ?, ?, 'manual', NULL)`,
        "rec-resolved", uid, resolvedAt, resolvedAt).Error)

    svc := &reconciliationDetectionImpl{db: db}
    inserted, _, _, _, err := svc.DetectLayer3(context.Background())
    require.NoError(t, err)
    assert.Equal(t, 1, inserted)

    // 验证: 历史 resolved 行保留 + 新 INSERT 1 行 = 2 行(partial index 不约束 resolved)
    var rowCount int64
    require.NoError(t, db.Model(&models.SysDataReconciliation{}).
        Where("asset_id = ? AND deleted_at IS NULL", uid).Count(&rowCount).Error)
    assert.Equal(t, int64(2), rowCount, "resolved 历史 + 新 open 行 = 2 行")
}
```

**注意事项**:
- 测试需要的 `models.SysDataReconciliation` import — 检查现有 test 文件顶部 import 块是否已包含(grep `models.SysDataReconciliation` 若 0 命中则加 `"github.com/xingran-next/xingran-go-backend/internal/models"`)。
- 若 sqlite in-memory 不支持 partial unique index WHERE 条件(GORM AutoMigrate 不一定建),则 UPDATE 测试用例可能 INSERT 重复而非 UPDATE — 此时改用手动 `CREATE UNIQUE INDEX uniq_recon_asset_type_open ON sys_data_reconciliation(asset_id, conflict_type) WHERE resolved_at IS NULL AND deleted_at IS NULL` 在 setupTestDB 后立即建。
- 检测逻辑依赖 `reconciliation_normalized` MV(读取资产+物理+AD 状态),setupTestDB 必须 seed 完整 ops_asset + ops_asset_physical + ops_asset_ad(ad_dn/dept_path 可选),否则 inserted=0。
- 测试不能用 `INSERT INTO sys_data_reconciliation ... resolved_at=NULL` 复现 UPDATE 场景(那样会被 partial index 当作新 open 行) — 必须 seed 已存在的 open 行(预存 unresolved)。
- 不要修改现有 TestDetectLayer3ExceptionHit / NoExceptionNoChange / SilenceStillWrites / SkipSeverityDegrades / DeptScopeMatch — 它们是 D-03 返回签名兼容性的回归守护,Task 1.1 改造后必须仍然 PASS。
  </action>
  <acceptance_criteria>
    - `grep -n "TestDetectLayer3Upsert" internal/services/asset/reconciliation_detection_test.go` 返回 ≥3 行(3 个新测试函数名)
    - `go test -run "TestDetectLayer3Upsert" -v ./internal/services/asset/...` 退出码 0
    - 3 个新测试输出 `--- PASS:` 行(每个 PASS 一行)
    - 5 个原 TestDetectLayer3* 测试仍然 PASS(输出含 `--- PASS: TestDetectLayer3ExceptionHit` 等)
    - `go test ./internal/services/asset/...` 全包退出码 0
  </acceptance_criteria>
  <verify>
    <automated>go test -run "TestDetectLayer3" -v ./internal/services/asset/... 2>&1 | tail -50</automated>
  </verify>
  <done>
    3 个新 UPSERT 测试全绿:TestDetectLayer3UpsertInsertNoExisting / TestDetectLayer3UpsertUpdateExisting / TestDetectLayer3UpsertInsertAfterResolved;5 个原 TestDetectLayer3* 测试仍然 PASS;go test ./internal/services/asset/... 退出码 0。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron -> DetectLayer3 | internal 边界,本计划内调用,operlog 跳过(per helper 注释) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-47-01 | Tampering | DetectLayer3 rec 字段未重置 ID/CreatedAt 致 GORM 走 full UPDATE 而非 INSERT-or-UPDATE | mitigate | 显式 rec.ID = "" + rec.CreatedAt = time.Time{} 强制 GORM INSERT-or-UPDATE 路径 |
| T-47-02 | Information Disclosure | EXCLUDED.* 引用 INET 列含 IP | accept | 仅 cron 调用,无外部用户直接触发;PII 不外泄 |
| T-47-03 | Denial of Service | UPSERT 部分索引不命中导致全表 UPSERT | accept | partial unique index WHERE resolved_at IS NULL 限定路径,open 行数 << 总资产 |
| T-47-SC | Tampering | npm/pip/cargo installs | mitigate | 本计划 0 新外部依赖;grep imports 仅引用已有 gorm.io/gorm/clause(GORM 子包,已在 go.mod) |
</threat_model>

<verification>
1. go build ./... 退出码 0 - 检查所有 package 编译通过
2. grep -c "clause.OnConflict" internal/services/asset/reconciliation_detection.go >= 1 - 确认 UPSERT 改造落地
3. grep -c "isReconciliationDuplicate(err)" internal/services/asset/reconciliation_detection.go == 0 - 确认 catch 路径已删(call site)
4. grep -c "func isReconciliationDuplicate" internal/services/asset/reconciliation_detection.go == 1 - 函数定义保留
5. grep -c "EXCLUDED\." internal/services/asset/reconciliation_detection.go >= 9 - 9 字段全覆盖
6. go test -run "TestDetectLayer3" -v ./internal/services/asset/... 退出码 0,含 3 个新 TestDetectLayer3Upsert* + 5 个原 TestDetectLayer3*
7. (可选 dev 验证) 启动后端跑一次 Layer 3 cron,SELECT asset_id, conflict_type, severity, detected_at FROM sys_data_reconciliation ORDER BY detected_at DESC LIMIT 5; 验证 UPSERT 实际更新 detected_at 时间戳
</verification>

<success_criteria>
1. R3(Phase 47 ROADMAP 锁定):Layer 3 检测改为 UPSERT 语义,不再吃 SQLSTATE 23505 ✓
2. R2(Phase 47 ROADMAP 锁定):sys_data_reconciliation 卡住的 open 异常 5 分钟内可被 Layer 3 cron 重新分类 ✓
3. DetectLayer3 返回签名 (int, int, int, int, error) 完全不变 ✓
4. 9 字段 + detected_at UPSERT 命中后,新 severity/raw_snapshot/confidence_score 立即反映到 DB ✓
5. go build ./... exit 0 + go test -run TestDetectLayer3 ./internal/services/asset/... exit 0
6. 没有任何"v1 简化版 / 占位 / 后续阶段再实现"的语言偷工减料
</success_criteria>

<output>
创建 .planning/phases/47-phase-46-infopoint-layer3-port-status-parser/47-01-SUMMARY.md 当执行完成时
</output>
