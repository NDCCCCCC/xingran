---
phase: 47-phase-46-infopoint-layer3-port-status-parser
plan: 02
type: execute
wave: 2
depends_on:
  - 47-01
files_modified:
  - internal/services/mac_collection_service.go
  - internal/services/mac_collection_service_test.go
  - internal/services/mac_normalize.go (新建)
  - internal/core/db/migrations/migration_198_cleanup_dirty_mac_rows.go (新建)
  - internal/core/db/database.go
autonomous: true
requirements_addressed:
  - R5
estimated_duration: 1 上下文窗口(~50 分钟)
must_haves:
  truths:
    - "parseRuijiePortSecurityLine 拒绝 'Flags:' / 'Total' / '#' / 空字段等垃圾行,不入库不入 mac_history"
    - "TestParseRuijiePortSecurityLine 新增 4 个负向 sub-test 全部 PASS (Header / Summary / Comment / EmptyField)"
    - "migration_198 软删除 sys_mac_address 中所有非 canonical MAC 行,deleted_at = NOW()"
    - "sys_dirty_mac_rows_backup 表保存脏行 id/device_id/mac_address/interface_name/collected_at/created_at/deleted_at 审计链"
    - "mac_history 表完全不动(脏数据历史链保留,符合 AUDIT-02)"
    - "migration_198 幂等可重复执行(再次执行 affected=0,不重建 backup 表)"
    - "go build ./... exit 0 + go test -run TestParseRuijiePortSecurityLine exit 0 + 启动后端日志含 'migration_198 cleanup: soft-deleted N dirty MAC rows'"
  artifacts:
    - path: "internal/services/mac_collection_service.go"
      provides: "parseRuijiePortSecurityLine isCanonicalMAC 校验 + 调用 NormalizeMACAddress"
      contains: "isCanonicalMAC(entry.MACAddress)"
    - path: "internal/services/mac_normalize.go"
      provides: "NormalizeMACAddress + isCanonicalMAC 工具函数"
      contains: "func isCanonicalMAC"
    - path: "internal/services/mac_collection_service_test.go"
      provides: "TestParseRuijiePortSecurityLine 4 个负向 sub-test"
      contains: "TestParseRuijiePortSecurityLine"
    - path: "internal/core/db/migrations/migration_198_cleanup_dirty_mac_rows.go"
      provides: "脏 MAC 行软删除 + 备份表 migration"
      contains: "Migrate198CleanupDirtyMACRows"
    - path: "internal/core/db/database.go"
      provides: "Migrate198 注册入口"
      contains: "migrations.Migrate198CleanupDirtyMACRows"
  key_links:
    - from: "internal/services/mac_collection_service.go"
      to: "internal/services/mac_normalize.go:isCanonicalMAC"
      via: "新文件 + parseRuijiePortSecurityLine 末尾守卫"
      pattern: "isCanonicalMAC"
    - from: "internal/core/db/migrations/migration_198_cleanup_dirty_mac_rows.go"
      to: "sys_dirty_mac_rows_backup"
      via: "CREATE TABLE AS SELECT 备份脏行"
      pattern: "CREATE TABLE sys_dirty_mac_rows_backup"
    - from: "internal/core/db/migrations/migration_198_cleanup_dirty_mac_rows.go"
      to: "sys_mac_address"
      via: "UPDATE deleted_at = NOW() 软删除"
      pattern: "UPDATE sys_mac_address SET deleted_at"
---

<objective>
完成 Phase 47 R5:在 `parseRuijiePortSecurityLine` 末尾加 `isCanonicalMAC` 校验(D-04)防止 'Flags:'/'Total'/'#'/注释行入库;新建 `internal/services/mac_normalize.go`(worktree 缺这两个 helper,与 D-04 一并补充);新建 `migration_198_cleanup_dirty_mac_rows.go`(D-05)用备份表 + 软删除清理历史脏 MAC 行;mac_history 表不动保留审计链。

按 CONTEXT.md 锁定:
- D-04: parseRuijiePortSecurityLine 末加 `if entry.MACAddress == "" || !isCanonicalMAC(entry.MACAddress) { return MACAddressEntry{}, false }`
- D-05: 备份表 `sys_dirty_mac_rows_backup` (id, device_id, mac_address, interface_name, collected_at, created_at, deleted_at) + `UPDATE sys_mac_address SET deleted_at = NOW()` + mac_history 不动 + 幂等

Claude's Discretion (按推荐执行):
- Migration 编号 `migration_198`(R2/R3 不需要新 migration,只有 R5 清理这一个;选 198 而非 194 原因:worktree 与 main 已合并,但出于保守执行链考虑用最大预留号避免与尚未同步的 migration 184-197 撞名;executor 实施前应先 `ls internal/core/db/migrations/migration_1*.go | tail -1` 确认当前最大号,**比最大号 +1** 选用;若 executor 发现 198 已占用则按 199, 200 顺延)
- 备用方案:executor 先执行 `git fetch origin main && git log origin/main --oneline -- internal/core/db/migrations/ | head -5` 探明 main 最新 migration 号
- TestParseRuijiePortSecurityLine 新增 4 个负向 sub-test: Header / Summary / Comment / EmptyField
- migration 输出 `applogger.Infof("migration_198 cleanup: soft-deleted %d dirty MAC rows", affected)` 统计
- 新建 `internal/services/mac_normalize.go`(因 worktree 当前不包含该文件;虽然 CONTEXT.md 假定文件已存在 — 实测 worktree 缺失)

依赖 47-01:本计划在 47-01 完成 R2/R3 UPSERT 改造后启动(避免同时改 reconciliation_detection.go + mac_collection_service.go 等系列文件导致大范围冲突)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-phase-46-infopoint-layer3-port-status-parser/47-CONTEXT.md
@internal/services/mac_collection_service.go
@internal/services/mac_collection_service_test.go
@internal/core/db/migrations/migration_188_fix_info_points_port_id_drift.go (备份表模式参考)
@internal/core/db/migrations/ (最新一个 migration_*.go 文件作为编号 + 注册模式参考)
@internal/core/db/database.go (migration 注册机制)
@internal/models/mac_address.go 或类似文件 (SysDeviceMACAddress 模型位置)
@CLAUDE.md
</context>

<interfaces>
工作树实测状态 (executor 必须以 `ls`/`grep` 验证以下接口,不要假定):

**A. `parseRuijiePortSecurityLine` (worktree 已存在的函数)**:
位置: `internal/services/mac_collection_service.go` 内 — 通过 `grep -n "func (s \*MACCollectionService) parseRuijiePortSecurityLine" internal/services/mac_collection_service.go` 定位锚点。
当前实现特征(以 worktree 实测为准):
- 行首 `if len(fields) < 7 { return ..., false }`
- 中部 `entry.MACAddress = fields[2]` (直接取原值,**未归一化**)
- 行尾 `entry.MACType = fields[typeIdx]` 后 `return entry, true`

**B. `isCanonicalMAC` 与 `NormalizeMACAddress` (worktree 缺失,任务 2.1.1 必须新建)**:
- 文件路径: `internal/services/mac_normalize.go` (新文件)
- 正则源: CONTEXT D-04 锁定的 `^[0-9A-F]{2}(:[0-9A-F]{2}){5}$`
- 入参形式: `NormalizeMACAddress(string) string`、`isCanonicalMAC(string) bool`
- 设计: NormalizeMACAddress 复用 `mac-address-normalize-returns-colon-format` 项目记忆 — 接收任意分隔符(`.`/`-`/`:`/无分隔符)→ 输出大写冒号格式;非 MAC 字符串返回 ""
- 内部分解:NormalizeMACAddress → strip 所有分隔符 → 循环 i+=2 重新插入冒号;若长度非 12 hex 字符则返回 ""

**C. `migration_198_cleanup_dirty_mac_rows.go` (新建迁移)**:
- 包名: `migrations`
- 函数签名: `func Migrate198CleanupDirtyMACRows(db *gorm.DB) error`
- 使用别名: `applogger "github.com/xingran-next/xingran-go-backend/pkg/logger"`
- 备份表 DDL: `CREATE TABLE sys_dirty_mac_rows_backup (...)`

**D. `database.go` 注册模式 (worktree 需探索)**:
通过以下命令定位锚点:
```bash
grep -n "if err := migrations.Migrate" internal/core/db/database.go | tail -5
grep -n "// migration" internal/core/db/database.go | tail -3
```
在**最高 Migrate1NN** 行后插入 Migrate198 调用块(用 content anchor,不要用 line number)。

**E. `models.SysDeviceMACAddress`**:
对应模型名为 `SysDeviceMACAddress`,主键 `id` `gorm:"primaryKey;type:varchar(64)"`,通过 `grep -rn "type SysDeviceMACAddress struct" internal/models/` 定位锚点。
</interfaces>

<tasks>

<task type="auto">
  <name>Task 2.1: 新建 mac_normalize.go + 改造 parseRuijiePortSecurityLine (D-04)</name>
  <read_first>
    - D:\CODE\ClaudeCode\xingran-go-backend\.claude\worktrees\phase47-discuss\internal\services\mac_collection_service.go (用 grep 锚点定位 parseRuijiePortSecurityLine 而非依赖行号)
    - D:\CODE\ClaudeCode\xingran-go-backend\.claude\worktrees\phase47-discuss\.planning\phases\47-phase-46-infopoint-layer3-port-status-parser\47-CONTEXT.md (D-04 锁定决策)
  </read_first>
  <action>

**2.1.1** 新建 `internal/services/mac_normalize.go`:

```go
// Package services - mac_normalize.go
// 2026-07-03 Phase 47 R5 (D-04): MAC 格式归一化与 canonical 校验工具
// 在 parseRuijiePortSecurityLine 与 (未来) parseMACLine 调用。
// 复用项目记忆 `mac-address-normalize-returns-colon-format` 的归一规则。
package services

import (
    "regexp"
    "strings"
)

// canonicalMACPattern 匹配标准大写冒号格式 MAC 地址: AA:BB:CC:DD:EE:FF
var canonicalMACPattern = regexp.MustCompile(`^[0-9A-F]{2}(:[0-9A-F]{2}){5}$`)

// isHexOnlyMACPattern 用于 NormalizeMACAddress 中间态校验: 12 位 hex 字符
var isHexOnlyMACPattern = regexp.MustCompile(`^[0-9A-F]{12}$`)

// NormalizeMACAddress 将任意 MAC 格式归一为大写冒号格式 `AA:BB:CC:DD:EE:FF`。
//
// 支持的输入格式:
//   - `aa:bb:cc:dd:ee:ff` (冒号)
//   - `aa-bb-cc-dd-ee-ff` (连字符)
//   - `aabb.ccdd.eeff`    (点分 cisco 风格)
//   - `AABBCCDDEEFF`      (无分隔符)
//
// 不符合 12 hex 字符的输入返回 "" (丢弃语义与 parseRuijiePortSecurityLine 失败路径对齐)。
//
// 调用方应配合 isCanonicalMAC 一起使用:
//   mac := NormalizeMACAddress(raw)
//   if mac == "" || !isCanonicalMAC(mac) { return /* skip */ }
func NormalizeMACAddress(input string) string {
    if input == "" {
        return ""
    }
    // 去除常见分隔符
    stripped := strings.NewReplacer(
        ":", "",
        "-", "",
        ".", "",
        " ", "",
    ).Replace(input)
    stripped = strings.ToUpper(stripped)
    if !isHexOnlyMACPattern.MatchString(stripped) {
        return ""
    }
    // 重新插入冒号
    var b strings.Builder
    for i := 0; i < 12; i += 2 {
        if i > 0 {
            b.WriteByte(':')
        }
        b.WriteString(stripped[i : i+2])
    }
    return b.String()
}

// isCanonicalMAC 判定输入是否符合 `^[0-9A-F]{2}(:[0-9A-F]{2}){5}$` canonical 格式。
// 用于 parseRuijiePortSecurityLine 末尾守卫 + 数据清理 migration 脏行判定。
func isCanonicalMAC(mac string) bool {
    if mac == "" {
        return false
    }
    return canonicalMACPattern.MatchString(mac)
}
```

**2.1.2** 修改 `parseRuijiePortSecurityLine` 函数:

- 用 `grep -n "entry.MACAddress = fields\[2\]" internal/services/mac_collection_service.go` 定位 `entry.MACAddress = fields[2]` 行(注:worktree 实测为 `entry.MACAddress = fields[2]`,与 main 分支的 Normalize 化版本不同)
- 替换该行为: `entry.MACAddress = NormalizeMACAddress(fields[2])`
- 在函数尾部(用 `grep -n "return entry, true" internal/services/mac_collection_service.go` 锚点定位 `return entry, true` 行)前**插入 D-04 守卫**:
```go
// 2026-07-03 Phase 47 R5 (D-04): canonical MAC 校验, 拦截解析层垃圾行
// 背景: parseRuijiePortSecurityLine (锐捷 show port-security address 输出)
//   混有 'Flags:'/'Total'/'#'/注释行/空字段,此前未校验,垃圾以原样入库。
//   与 parseMACLine 的丢弃语义对齐: 空 / 非 canonical → 返回 (_, false)。
if entry.MACAddress == "" || !isCanonicalMAC(entry.MACAddress) {
    return MACAddressEntry{}, false
}
```

**2.1.3** 在 `parseRuijiePortSecurityLine` 函数 doc comment 区域顶部追加变更日志注释:
```
// 2026-07-03 Phase 47 R5 (D-04): 加 NormalizeMACAddress 归一 + isCanonicalMAC 守卫,
//   过滤解析层垃圾行;脏历史由 migration_198 软删除清理(D-05)。
```

**注意事项**:
- worktree 的 parseRuijiePortSecurityLine 起始位置可能略有漂移,**必须用 grep 锚点而非 line number**。
- mac_normalize.go 的 package = `services` (mac_collection_service.go 同包),不需要 import。
- 不要修改 `if len(fields) < 7`、`strconv.Atoi(fields[1])` 防御逻辑 — VLAN 解析保留。
- 不要改函数签名 `(fields []string) (MACAddressEntry, bool)`。
- 不要把守卫挪到 InterfaceName 解析前 — 与 parseMACLine 一致在末尾(便于未来调整守卫内顺序不影响 Interface 信息采集)。
- **worktree 内 mac_normalize.go 不存在**,这是任务必做项,不要跳过 — 写代码前先 `ls internal/services/mac_normalize.go` 二次确认;若已有(别的工作流已建),跳到 2.1.2。

  </action>
  <acceptance_criteria>
    - `ls internal/services/mac_normalize.go` 返回文件存在
    - `grep -n "func NormalizeMACAddress" internal/services/mac_normalize.go` 返回 ≥1 行
    - `grep -n "func isCanonicalMAC" internal/services/mac_normalize.go` 返回 ≥1 行
    - `grep -n "isCanonicalMAC(entry.MACAddress)" internal/services/mac_collection_service.go` 返回 ≥1 行(parseRuijiePortSecurityLine 内守卫)
    - `grep -n "NormalizeMACAddress(fields\[2\])" internal/services/mac_collection_service.go` 返回 ≥1 行(替换 fields[2])
    - `grep -n "phase 47 R5 (D-04)" internal/services/mac_collection_service.go` 返回 ≥1 行(变更日志注释)
    - `go build ./...` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>go build ./... 2>&1 | tail -10</automated>
  </verify>
  <done>
    mac_normalize.go 就位;parseRuijiePortSecurityLine 已 Normalize + canonical 守卫;go build 0。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.2: 新增 4 个负向 sub-test 到 TestParseRuijiePortSecurityLine</name>
  <read_first>
    - D:\CODE\ClaudeCode\xingran-go-backend\.claude\worktrees\phase47-discuss\internal\services\mac_collection_service_test.go (用 grep "TestParseRuijiePortSecurityLine" 锚点定位)
    - D:\CODE\ClaudeCode\xingran-go-backend\.claude\worktrees\phase47-discuss\internal\services\mac_normalize.go (新 NormalizeMACAddress 行为)
  </read_first>
  <action>

在 `internal/services/mac_collection_service_test.go` 的 `TestParseRuijiePortSecurityLine` 内 `tests := []struct{...}{...}` 切片末尾追加 4 个负向 sub-test。

**结构参考**:定位 `tests := []struct {` 行后,在切片末尾 `}` 前插入,不要在切片外另起新切片。

**2.2.1** 切片末尾追加 4 个负向 sub-test:

```go
// === Phase 47 R5 (D-04) 负向用例: isCanonicalMAC 拦截解析层垃圾行 ===
{
    // fields[2] = "Flags:" → NormalizeMACAddress 剥离分隔符后是 "FLAGS" → 非 12 hex → 返回 ""
    // entry.MACAddress == "" 短路,守卫返回 (_, false)
    name:      "R5-D04 Header row 'Flags:' rejected",
    line:      "1   100   Flags:    GigabitEthernet 0/1      Dynamic    --    active",
    wantEntry: false,
},
{
    // fields[2] = "Total"(strings.Fields 按空格 split,误认"Total entries: 42" 中 9 个 token)
    //   → NormalizeMACAddress 剥离分隔符后是 "TOTAL" → 非 12 hex → ""
    // entry.MACAddress == "" 守卫短路
    name:      "R5-D04 Summary row 'Total' rejected",
    line:      "1   100   Total entries: 42    --    --    --    active",
    wantEntry: false,
},
{
    // fields[2] = "#" → NormalizeMACAddress 剥离后是 "#" → 非 hex → ""
    name:      "R5-D04 Comment line '#' rejected",
    line:      "1   100   #    GigabitEthernet 0/1    Dynamic    --    active",
    wantEntry: false,
},
{
    // fields[2] = "GigabitEthernet"(因 MAC 槽被接口名占据 — 真实锐捷 show port-security 在某些版本会这样)
    //   → NormalizeMACAddress 剥离后是 "GIGABITETHERNET" → 非 12 hex → ""
    // entry.MACAddress == "" 守卫短路
    name:      "R5-D04 MAC slot occupied by interface name rejected",
    line:      "1   100   GigabitEthernet 0/1    Dynamic    --    active",
    wantEntry: false,
},
```

**2.2.2** 验证测试循环 (现有断言逻辑 `if tt.wantEntry { ... } else { assert.False }`) 已能处理负向用例,无需修改。

**注意事项**:
- 4 个 sub-test 全部期望 `wantEntry: false`,必须确认现有断言路径在 `wantEntry=false` 时只调用 `assert.False(ok)`,不要断言 entry 字段(否则空 struct 的 MACAddress/VLANID 默认为零值会撞上 has-not-been-set 检查)。
- 用空格字段分隔(与现有测试一致),连续空格数量不影响 strings.Fields 切分。
- Comment line 的行至少 7 个 token(让 `len(fields) < 7` 防御不短路),executor 实施时应预先 `echo` 出来数一数字段数。
- 测试入口 `service.parseMACLine(tt.line, models.VendorRuijie, MACCommandTypePortSecurity)` 保持不变 — 会 dispatch 到 parseRuijiePortSecurityLine。
- **不要删除**已有的 "Too few fields for port-security format" 负向用例 — 它测试 `len(fields) < 7` 短路,与本任务的 canonical 守卫是两道独立防线。

  </action>
  <acceptance_criteria>
    - `grep -n "R5-D04 Header row" internal/services/mac_collection_service_test.go` 返回 1 行
    - `grep -n "R5-D04 Summary row" internal/services/mac_collection_service_test.go` 返回 1 行
    - `grep -n "R5-D04 Comment line" internal/services/mac_collection_service_test.go` 返回 1 行
    - `grep -n "R5-D04 MAC slot occupied" internal/services/mac_collection_service_test.go` 返回 1 行
    - `go test -run "TestParseRuijiePortSecurityLine" -v ./internal/services/...` 退出码 0
    - 输出含 `--- PASS: TestParseRuijiePortSecurityLine/` 至少 8 个 PASS 子测试(原 4 个 + 新 4 个)
  </acceptance_criteria>
  <verify>
    <automated>go test -run "TestParseRuijiePortSecurityLine" -v ./internal/services/... 2>&1 | tail -30</automated>
  </verify>
  <done>
    4 个新负向 sub-test 全 PASS,原有用例不回归,TestParseRuijiePortSecurityLine exit 0。
  </done>
</task>

<task type="auto">
  <name>Task 2.3: 创建 migration_198_cleanup_dirty_mac_rows.go (D-05)</name>
  <read_first>
    - D:\CODE\ClaudeCode\xingran-go-backend\.claude\worktrees\phase47-discuss\internal\core\db\migrations\migration_188_fix_info_points_port_id_drift.go (事务 + 备份表模式)
    - D:\CODE\ClaudeCode\xingran-go-backend\.claude\worktrees\phase47-discuss\internal\core\db\migrations\ (ls 找到当前最新 migration_NNN_*.go 文件作为编号 + 注册模式参考 — 工作树实测最高为 migration_180_reconciliation_physical_chain_perf.go,但 main 分支可能 184-197 已存在;executor 实施前用 `git log origin/main --oneline -- internal/core/db/migrations/ | head -3` 确认 main 是否已合并更高编号)
    - D:\CODE\ClaudeCode\xingran-go-backend\.claude\worktrees\phase47-discuss\internal\core\db\database.go (grep "if err := migrations.Migrate" 找到当前最大 Migrate1NN 锚点)
    - D:\CODE\ClaudeCode\xingran-go-backend\.claude\worktrees\phase47-discuss\internal\models\ (grep "type SysDeviceMACAddress struct" 定位模型)
  </read_first>
  <action>

**2.3.0 编号选择策略**(关键):
1. 在 worktree 实施前必须先 `git fetch origin main && git log origin/main --oneline --diff-filter=A --name-only -- internal/core/db/migrations/migration_1*.go | head -20` 与 `git ls-tree -r origin/main -- internal/core/db/migrations/migration_1*.go | awk -F'migration_' '{print $2}' | cut -d'_' -f1 | sort -u | tail -5`
2. 取 main 分支 + worktree 实测两者的最高 migration 编号,再加 1
3. 默认使用 `migration_198`(假设 main 最高 migration_197,198 空) — 但若 executor 探测到 main 已经有 migration_198+ 占用,顺延至 199 / 200
4. 不要假设 migration_194 — CONTEXT 推荐的 194 在 main 上已被 `migration_194_clean_iface_security_suffix.go` 占用
5. 函数命名同步: `Migrate198CleanupDirtyMACRows`(若改用 199/200 则 `Migrate199...`/`Migrate200...`)

**2.3.1** 新建 `internal/core/db/migrations/migration_NNN_cleanup_dirty_mac_rows.go`(NNN = 198 或经探测后确定的编号):

```go
// Package migrations - migration_NNN_cleanup_dirty_mac_rows.go
// 2026-07-03 Phase 47 R5 (D-05): 软删除 sys_mac_address 中非 canonical MAC 格式的行
// 配合 parseRuijiePortSecurityLine isCanonicalMAC 守卫(D-04)+ mac_normalize.go 工具函数。
//
// 根因:
//   - 解析层此前未校验 MAC canonical 格式 (锐捷 'show port-security address'
//     输出混有 'Flags:'/'Total'/'#'/注释行/空字段, 这些垃圾以原样入库)。
//   - 连锁污染 sys_device_mac_history 轨迹表 (脏数据的轨迹本身保留为审计价值,
//     本 migration 不动 mac_history; 符合 AUDIT-02)。
//
// 策略:
//   1. 备份脏行到 sys_dirty_mac_rows_backup (审计链 30 天后续手动 DROP, 不写 cron)
//   2. 软删除 sys_mac_address 脏行 (deleted_at = NOW(), 不物理删除)
//   3. 幂等: 重新执行时 SELECT 过滤 deleted_at IS NOT NULL, affected = 0, 不重建 backup
//
// 不动:
//   - sys_device_mac_history 表 (AUDIT-02 审计链保留)
//   - sys_device_arp_entries 表 (ARP 路径另议, 不在 R5 scope)
//   - 物理链路 / 信息点 / 工位模块
package migrations

import (
    "fmt"

    applogger "github.com/xingran-next/xingran-go-backend/pkg/logger"
    "gorm.io/gorm"

    "github.com/xingran-next/xingran-go-backend/internal/models"
)

// MigrateNNNCleanupDirtyMACRows 软删除 sys_mac_address 中非 canonical MAC 行
func MigrateNNNCleanupDirtyMACRows(db *gorm.DB) error {
    applogger.Info("Running migration NNN: 软删除 sys_mac_address 非 canonical MAC 行 + 备份到 sys_dirty_mac_rows_backup")

    // === 1. 备份脏行 ===
    if err := db.Exec("DROP TABLE IF EXISTS sys_dirty_mac_rows_backup").Error; err != nil {
        return fmt.Errorf("清理旧备份表失败: %w", err)
    }

    var dirtyCount int64
    if err := db.Model(&models.SysDeviceMACAddress{}).
        Where("mac_address IS NOT NULL AND mac_address !~ ? AND deleted_at IS NULL",
            `^[0-9A-F]{2}(:[0-9A-F]{2}){5}$`).
        Count(&dirtyCount).Error; err != nil {
        return fmt.Errorf("统计脏行失败: %w", err)
    }
    if dirtyCount == 0 {
        applogger.Info("[迁移] NNN 无脏行, 跳过 (幂等)")
        return nil
    }

    if err := db.Exec(`
CREATE TABLE sys_dirty_mac_rows_backup (
    id              varchar(64)  PRIMARY KEY,
    device_id       varchar(64),
    mac_address     varchar(64),
    interface_name  varchar(100),
    collected_at    timestamp,
    created_at      timestamp,
    deleted_at      timestamp    NOT NULL DEFAULT NOW()
)`).Error; err != nil {
        return fmt.Errorf("创建备份表失败: %w", err)
    }
    if err := db.Exec(`
INSERT INTO sys_dirty_mac_rows_backup (id, device_id, mac_address, interface_name, collected_at, created_at)
SELECT id, device_id, mac_address, interface_name, collected_at, created_at
  FROM sys_mac_address
 WHERE mac_address IS NOT NULL
   AND mac_address !~ ?
   AND deleted_at IS NULL`,
        `^[0-9A-F]{2}(:[0-9A-F]{2}){5}$`).Error; err != nil {
        return fmt.Errorf("备份脏行失败: %w", err)
    }
    applogger.Infof("[迁移] NNN 备份完成 %d 行到 sys_dirty_mac_rows_backup", dirtyCount)

    // === 2. 软删除 ===
    result := db.Model(&models.SysDeviceMACAddress{}).
        Where("mac_address IS NOT NULL AND mac_address !~ ? AND deleted_at IS NULL",
            `^[0-9A-F]{2}(:[0-9A-F]{2}){5}$`).
        Update("deleted_at", gorm.Expr("NOW()"))
    if result.Error != nil {
        return fmt.Errorf("软删除脏行失败: %w", result.Error)
    }

    applogger.Infof("[迁移] NNN cleanup: soft-deleted %d dirty MAC rows (备份在 sys_dirty_mac_rows_backup)", result.RowsAffected)
    return nil
}
```

**2.3.2** 在 `internal/core/db/database.go` 注册 MigrateNNN:
- 用 `grep -n "if err := migrations.Migrate" internal/core/db/database.go | tail -3` 找到当前最大 Migrate1NN 调用块
- **content anchor**:在该 Migrate1NN 块的正下方插入新块:
```go
// 2026-07-03 mac-r5-cleanup: 软删除 sys_mac_address 非 canonical MAC 行 (D-05)
//   备份表 sys_dirty_mac_rows_backup 保留 30 天审计链 (手动 DROP)。
//   mac_history 表不动 (AUDIT-02)。
//   幂等可重复执行。
if err := migrations.MigrateNNNCleanupDirtyMACRows(d.DB); err != nil {
    applogger.Errorf("软删除脏 MAC 行失败: %v", err)
}
```
(若使用 `migration_198` 则所有 `MigrateNNN` 替换为 `Migrate198`,所有 `migration NNN` 替换为 `migration 198`)

**注意事项**:
- 必须 import `fmt` (用于 fmt.Errorf) 和 models 包(用于 `models.SysDeviceMACAddress`)
- 备份表名 `sys_dirty_mac_rows_backup` 与 `migration_188` 的 `sys_info_points_port_id_drift_backup` 风格一致
- 备份表**不**对 mac_history 操作(grep 检查 `FROM|UPDATE|DELETE FROM sys_device_mac_history` 应 = 0)
- 不要建 backup 表的额外索引(已 PRIMARY KEY on `id` 足够)
- 不要在 migration 内调用 `tx.Transaction` 包裹(单条 UPDATE 在生产规模 < 5000 行 < 1s,事务复杂度得不偿失)
- 不要在 migration 里调用 operlog(batch 数据迁移,operlog 跳过)
- 不要用 `time.Now()` 替换 `gorm.Expr("NOW()")`(PG 端时间戳统一)
- 不要备份 `deleted_at IS NOT NULL` 的脏行(二次执行不应再覆盖,保护幂等)

  </action>
  <acceptance_criteria>
    - `ls internal/core/db/migrations/migration_*cleanup_dirty_mac_rows.go` 返回文件存在
    - `grep -n "MigrateNNNCleanupDirtyMACRows" internal/core/db/migrations/migration_*cleanup_dirty_mac_rows.go` 返回 ≥2 行(定义 + 自身 doc comment 引用)
    - `grep -n "sys_dirty_mac_rows_backup" internal/core/db/migrations/migration_*cleanup_dirty_mac_rows.go` 返回 ≥3 行(CREATE + INSERT + applogger log)
    - `grep -n "MigrateNNNCleanupDirtyMACRows" internal/core/db/database.go` 返回 ≥1 行(注册入口)
    - `grep -E "FROM|UPDATE|DELETE FROM[[:space:]]+sys_device_mac_history" internal/core/db/migrations/migration_*cleanup_dirty_mac_rows.go` 返回 0 行(mac_history 未被引用)
    - `grep -E "(FROM|UPDATE|DELETE FROM)[[:space:]]+sys_(mac_address|dirty_mac_rows_backup)" internal/core/db/migrations/migration_*cleanup_dirty_mac_rows.go` 返回 ≥2 行
    - `go build ./...` 退出码 0
    - dev 环境启动后端日志含 `[迁移] NNN cleanup: soft-deleted N dirty MAC rows` 行(N > 0 首次,N = 0 二次执行)
  </acceptance_criteria>
  <verify>
    <automated>go build ./... 2>&1 | tail -10 && echo "---" && ls -la internal/core/db/migrations/migration_*cleanup_dirty_mac_rows.go 2>&1 && echo "---" && grep -n "Migrate198\|Migrate199\|Migrate200\|MigrateNNN" internal/core/db/database.go</automated>
  </verify>
  <done>
    migration 文件就位,database.go 注册完成,go build 通过,首次执行 N dirty rows 软删 + 备份,二次执行幂等 (N = 0, 不重建 backup 表)。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron/collector -> parseRuijiePortSecurityLine | 解析层入口,所有锐捷 'show port-security address' 输出从此过 |
| AutoMigrate -> migration_198 | 启动时一次性执行,operlog 跳过(batch 数据迁移) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-47-10 | Tampering | parseRuijiePortSecurityLine 缺 isCanonicalMAC 校验 | mitigate | Task 2.1 显式加 entry.MACAddress == "" || !isCanonicalMAC 短路 |
| T-47-11 | Tampering | 历史脏 MAC 行未清理致下游过滤逻辑误判 | mitigate | Task 2.3 migration 软删除 + 备份 |
| T-47-12 | Repudiation | 软删除不带审计链 | mitigate | sys_dirty_mac_rows_backup 保存 (id, device_id, mac_address, interface_name, collected_at, created_at, deleted_at=NOW()) 30 天审计链 |
| T-47-13 | Information Disclosure | mac_history 含 IP/MAC PII | accept | mac_history 不动,符合 AUDIT-02;访问受现有 RBAC 控制 |
| T-47-14 | Denial of Service | migration 全表 UPDATE 锁 sys_mac_address | accept | 软删除仅 UPDATE deleted_at,行级锁;UPDATE WHERE mac_address !~ ... 命中脏行,生产预计 < 5000 行, < 1s |
| T-47-15 | Tampering | migration 误删合法行 (canonical 误判) | mitigate | 正则与 isCanonicalMAC 同源: ^[0-9A-F]{2}(:[0-9A-F]{2}){5}$;备份表可恢复 |
| T-47-16 | Tampering | 解析层补丁绕过(调用方传非 raw 字段) | mitigate | isCanonicalMAC 守卫在 Normalize 之后;Normalize 失败 → 守卫短路 → 不入库 |
| T-47-17 | Spoofing | 备份表 DROP+RECREATE 期间无锁保护导致 insert race | accept | 备份表是 audit 链 refresh,不是 hot path;生产 cron 启动期无人操作 |
| T-47-SC | Tampering | npm/pip/cargo installs | mitigate | 本计划 0 新外部依赖,仅使用已有 gorm + applogger + models + fmt |
</threat_model>

<verification>
1. go build ./... 退出码 0 - 全部编译通过
2. ls -la internal/core/db/migrations/migration_*cleanup_dirty_mac_rows.go 存在
3. grep "Migrate198\|MigrateNNN" internal/core/db/database.go >= 1 - 注册完成
4. grep "isCanonicalMAC" internal/services/mac_collection_service.go >= 1 (parseRuijiePortSecurityLine 守卫)
5. grep "NormalizeMACAddress" internal/services/mac_normalize.go >= 1 (新工具文件)
6. grep "TestParseRuijiePortSecurityLine" internal/services/mac_collection_service_test.go >= 1
7. go test -run "TestParseRuijiePortSecurityLine" -v ./internal/services/... 退出码 0, 4 新 sub-test PASS
8. dev 环境: 启动后端 → 日志含 "Running migration NNN: 软删除 sys_mac_address 非 canonical MAC 行 + 备份到 sys_dirty_mac_rows_backup"
9. dev 环境: 日志含 "[迁移] NNN cleanup: soft-deleted N dirty MAC rows" (N > 0 首次)
10. dev 环境: SELECT count(*) FROM sys_dirty_mac_rows_backup; 返回 N (等于 cleanup log 的 N)
11. dev 环境: SELECT count(*) FROM sys_mac_address WHERE deleted_at IS NOT NULL AND mac_address !~ '^[0-9A-F]{2}(:[0-9A-F]{2}){5}$'; 返回 0 (脏行已全部软删)
12. dev 环境: SELECT count(*) FROM sys_device_mac_history; 与 migration 执行前一致 (mac_history 未动)
</verification>

<success_criteria>
1. R5(Phase 47 ROADMAP 锁定):parseRuijiePortSecurityLine 加 MAC 格式校验(12位 hex),过滤 #/flags/total 等噪声 ✓
2. 解析层 + 数据层双层防线: parseRuijiePortSecurityLine 拒收 + migration 软删除清理历史 ✓
3. mac_history 审计链完整保留 (AUDIT-02) ✓
4. 4 个负向 sub-test 全 PASS, 4 个原 sub-test 不回归 ✓
5. migration 幂等可重复执行 ✓
6. mac_normalize.go 新建工具函数(因 worktree 缺失,作为本 phase 必要前置)
7. go build ./... exit 0 + go test -run TestParseRuijiePortSecurityLine exit 0 + 启动后端日志含 "soft-deleted N dirty MAC rows"
8. 没有任何"v1 简化版 / 占位 / 后续阶段再实现"的语言偷工减料
</success_criteria>

<output>
创建 .planning/phases/47-phase-46-infopoint-layer3-port-status-parser/47-02-SUMMARY.md 当执行完成时
</output>
