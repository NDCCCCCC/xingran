---
phase: 42-r1
plan: 02
type: execute
wave: 2
depends_on:
  - 42-01
files_modified:
  - internal/services/asset/reconciliation_service.go
  - internal/services/asset/reconciliation_exception.go
  - internal/services/asset/reconciliation_snapshot.go
  - internal/services/asset/reconciliation_detection.go
  - internal/services/asset/reconciliation_test.go
  - internal/api/v1/asset/reconciliation_handler.go
  - internal/api/v1/asset/reconciliation_router.go
  - internal/api/v1/asset/reconciliation_exception_handler.go
  - internal/api/v1/asset/reconciliation_exception_router.go
autonomous: true
requirements:
  - RECON-01
  - RECON-02
  - RECON-05
  - RECON-06
  - RECON-07
  - INFRA-04
user_setup: []

must_haves:
  truths:
    - "ReconciliationService 接口就位,含 ListExceptions / GetByID 读取方法"
    - "ReconciliationExceptionService 接口就位,含 List / GetByID 占位 + R3 文档"
    - "Layer 3 检测引擎可对每行 reconciliation_normalized 输出 Type A-F + severity + confidence"
    - "catch unique violation on uniq_recon_asset_type_open 静默跳过(D-11)"
    - "RefreshView 函数可执行 REFRESH MATERIALIZED VIEW CONCURRENTLY reconciliation_normalized"
    - "RECON-01/02/05/07 全部由本 plan 服务层实现"
    - "D-01: 物化视图 5min 定时 CONCURRENTLY 刷新(sys_job @every 5m 触发 RefreshView)"
    - "D-07: R1 同步做完整 Layer 3 引擎(A-F 分类 SQL + cron 写 sys_data_reconciliation)"
    - "D-08: 物化视图 mac1 优先(COALESCE 保证);Layer 3 按物理链路输出"
    - "D-09: Type A 仅 dashboard 统计,不入 sys_data_reconciliation 主表"
    - "D-13: sqlmock 单元测试 + 集成测试 PG dev DB ETL(不引入 testcontainers-go)"
  artifacts:
    - path: "internal/services/asset/reconciliation_service.go"
      provides: "ReconciliationService interface + impl with ListExceptions/GetByID (含 workstation_info_point JOIN 链路)"
      min_lines: 150
    - path: "internal/services/asset/reconciliation_detection.go"
      provides: "Layer 3 detect logic: Type A-F classification + confidence scoring + unique violation catch"
      min_lines: 200
    - path: "internal/services/asset/reconciliation_snapshot.go"
      provides: "RefreshView() executes REFRESH MATERIALIZED VIEW CONCURRENTLY + error log only"
      min_lines: 60
    - path: "internal/services/asset/reconciliation_exception.go"
      provides: "ReconciliationExceptionService R1 skeleton (List/GetByID stub + R3 doc)"
      min_lines: 80
    - path: "internal/services/asset/reconciliation_test.go"
      provides: "sqlite in-memory tests for Layer 3 classification + unique violation skip"
      min_lines: 200
  key_links:
    - from: "internal/services/asset/reconciliation_detection.go"
      to: "reconciliation_normalized materialized view"
      via: "SELECT * FROM reconciliation_normalized"
      pattern: "reconciliation_normalized"
    - from: "internal/services/asset/reconciliation_detection.go"
      to: "isDuplicateKeyError"
      via: "catch unique violation"
      pattern: "isDuplicateKeyError"
    - from: "internal/services/asset/reconciliation_snapshot.go"
      to: "DB.Exec"
      via: "REFRESH MATERIALIZED VIEW CONCURRENTLY"
      pattern: "REFRESH MATERIALIZED VIEW CONCURRENTLY"
    - from: "internal/services/asset/reconciliation_service.go"
      to: "sys_workstation_info_point wip"
      via: "LEFT JOIN for physical_username lookup"
      pattern: "sys_workstation_info_point wip"
---

<objective>
实现 Phase 42 R1 的核心服务层:ReconciliationService(读异常列表/详情)、ReconciliationExceptionService(R1 占位 + R3 文档)、ReconciliationDetection(Layer 3 Type A-F 分类 + 置信度评分 + unique violation catch)、ReconciliationSnapshot(物化视图 CONCURRENTLY 刷新)、对应的 handler + router(R1 注册读端点)。Task 2 已拆分为 Task 2(snapshot)+ Task 3(detection + test)以避免单 task 范围过大。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-r1/42-CONTEXT.md
@.planning/phases/42-r1/42-PATTERNS.md
@.planning/notes/asset-reconciliation-strategy.md
@.planning/notes/260627-reconciliation-reuse-audit.md

# Reference implementations
@internal/services/operations/location_alias_service.go (interface + impl + constructor pattern)
@internal/services/operations/asset_service.go (Statistics + ApplySort pattern)
@internal/services/operations/asset_statistics_test.go (sqlite in-memory test pattern)
@internal/services/operations/batch_upserter.go (isDuplicateKeyError pattern)
@internal/models/asset.go (v0.5 field names: mac1, mac2, machine_ip, user_id)
@internal/models/reconciliation.go (created in 42-01, schema reference)
@internal/utils/operlog/operlog.go (Record helper for write operations, R2 onwards)
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From `internal/models/reconciliation.go` (created in 42-01):
```go
type SysDataReconciliation struct {
    BaseModel
    AssetID          string
    ConflictType     string  // A/B/C/D/E/F
    Severity         string  // low/medium/high/critical
    PhysicalValue    datatypes.JSON
    DeclaredValue    datatypes.JSON
    ADValue          datatypes.JSON
    ConfidenceScore  float64
    RawSnapshot      datatypes.JSON
    AssetIP          *string
    ExceptionRuleID  *string
    AppliedActions   pq.StringArray
    DetectedAt       time.Time
    ResolvedAt       *time.Time
    ResolvedBy       *string
    ResolutionNote   *string
    WorkorderID      *string
}
```

From `internal/services/operations/asset_service.go` (Statistics pattern):
```go
type AssetStatisticsResult struct {
    Total  int64 `json:"total"`
    Normal int64 `json:"normal"`
    // ...
}

func (s *assetService) Statistics(ctx context.Context) (*AssetStatisticsResult, error) {
    var result AssetStatisticsResult
    err := s.db.WithContext(ctx).
        Model(&models.Asset{}).
        Select("COUNT(*) AS total, COALESCE(SUM(CASE WHEN status = 0 THEN 1 ELSE 0 END), 0) AS normal, ...").
        Scan(&result).Error
    // ...
}
```

From `internal/services/operations/location_alias_service.go:42-89`:
```go
type LocationAliasService interface {
    List(ctx context.Context, pageNum, pageSize int) (*PageResult, error)
    GetByID(ctx context.Context, id string) (*models.SysDeptLocationAlias, error)
    // ...
}

type locationAliasServiceImpl struct {
    db *gorm.DB
}

func NewLocationAliasService(db *gorm.DB) LocationAliasService {
    return &locationAliasServiceImpl{db: db}
}
```

From `internal/services/operations/batch_upserter.go`: `isDuplicateKeyError(err) bool` detects `pgconn 23505` + string fallback.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ReconciliationService + ReconciliationExceptionService interfaces (read paths only for R1)</name>
  <files>internal/services/asset/reconciliation_service.go, internal/services/asset/reconciliation_exception.go</files>
  <read_first>
    - internal/services/operations/location_alias_service.go (full pattern)
    - internal/services/operations/asset_service.go:60-75 (Statistics + ApplySort whitelist)
    - internal/models/reconciliation.go (created in 42-01)
    - internal/models/asset.go (v0.5 field names)
    - .planning/phases/42-r1/42-PATTERNS.md (cache_keys, allowed sort fields)
  </read_first>
  <behavior>
    - ListExceptions(params) returns page result with JOIN ops_asset (asset_code, asset_ip) + JOIN sys_user (physical_username, responsible_username) + filter by conflictType/severity/assetCode/detectedAt range
    - GetByID(id) returns full sys_data_reconciliation row + asset_code/asset_ip JOIN
    - Default sort: detected_at DESC
    - Server-side sort whitelist: detectedAt, conflictType, severity, confidenceScore
    - ReconciliationExceptionService R1 has List (no UI, returns empty) + GetByID (returns 404) + R3 doc comment for CRUD
  </behavior>
  <action>
    1. 创建 `internal/services/asset/reconciliation_service.go`,完全模仿 `internal/services/operations/location_alias_service.go` 模式:
       - `package asset`
       - import: context, errors, gorm.io/gorm, github.com/xingran-next/xingran-go-backend/internal/models, pkg/common(若有)
       - 定义 `type ExceptionListParams struct`:`ConflictType string` / `Severity string` / `AssetCode string` / `DetectedFrom *time.Time` / `DetectedTo *time.Time` / `common.ListParams`(继承 BaseListRequest,见 `internal/services/operations/asset_service.go:53-60` 嵌入方式)
       - 定义 `type ExceptionListItem struct` 嵌入 `models.SysDataReconciliation` + `AssetCode string gorm:"column:asset_code"` + `AssetIPDisplay string gorm:"column:asset_ip_display"` + `PhysicalUsername *string gorm:"column:physical_username"` + `ResponsibleUsername *string gorm:"column:responsible_username"`
       - 定义 `reconAllowedSortFields = map[string]string{"detectedAt": "detected_at", "conflictType": "conflict_type", "severity": "severity", "confidenceScore": "confidence_score"}`
       - 定义 `type ReconciliationService interface { ListExceptions(ctx, params) (*PageResult, error); GetByID(ctx, id) (*models.SysDataReconciliation, error) }`
       - 定义 `reconciliationServiceImpl struct { db *gorm.DB }` + `NewReconciliationService(db *gorm.DB) ReconciliationService` 构造函数
       - 实现 `ListExceptions`:**完整 LEFT JOIN 链路**(reference 实现,production 用 GORM 链式 API):
         ```
         SELECT
             sys_data_reconciliation.*,
             a.devicesn AS asset_code,
             a.machine_ip AS asset_ip_display,
             wu.username AS physical_username,    -- 来自工位链路
             ru.username AS responsible_username  -- 来自 ops_asset.user_id
         FROM sys_data_reconciliation r
         LEFT JOIN ops_asset a ON a.id = r.asset_id AND a.deleted_at IS NULL
         LEFT JOIN sys_workstation_info_point wip ON wip.info_point_id IN (
             SELECT ip.id FROM sys_info_point ip
             LEFT JOIN sys_port_mac pm ON pm.id = ip.port_id
             WHERE pm.mac = COALESCE(NULLIF(a.mac1, ''), NULLIF(a.mac2, ''))
         ) AND wip.deleted_at IS NULL
         LEFT JOIN sys_workstation w ON w.id = wip.workstation_id AND w.deleted_at IS NULL
         LEFT JOIN sys_user wu ON wu.id = w.user_id AND wu.deleted_at IS NULL
         LEFT JOIN sys_user ru ON ru.id = a.user_id AND ru.deleted_at IS NULL
         WHERE r.deleted_at IS NULL
           AND <filters: conflictType / severity / assetCode / detectedFrom / detectedTo>
         ORDER BY r.detected_at DESC
         OFFSET ? LIMIT ?
         ```
         实际 SQL 用 GORM 链式 API,JOIN 通过 `.Joins()` 实现;filter by params;ApplySort;OFFSET/LIMIT
       - 实现 `GetByID`:简单 First where id=? AND deleted_at IS NULL
       - 顶部注释说明:`R1: 观测底座读端点(只读)。MarkResolved / Create / Update / Delete 在 R2 才实现,本接口不暴露。`

    2. 创建 `internal/services/asset/reconciliation_exception.go`:
       - `package asset`
       - 定义 `type ReconciliationExceptionService interface { List(ctx, pageNum, pageSize) (*PageResult, error); GetByID(ctx, id) (*models.SysReconciliationException, error) }`
       - 定义 `reconciliationExceptionServiceImpl struct { db *gorm.DB }` + `NewReconciliationExceptionService(db *gorm.DB) ReconciliationExceptionService`
       - 实现 `List`:查 sys_reconciliation_exception 全部(分页)— R1 无 UI 入口,仅 API 占位;返回当前 seeds
       - 实现 `GetByID`:First where id=? AND deleted_at IS NULL
       - 顶部注释说明:`R1: 例外规则接口骨架。Create/Update/Delete 在 R3 实现。R1 走 seed 数据。`
       - 注释参考 `.planning/notes/260627-reconciliation-reuse-audit.md` 4.6 (F2 路由注册) 与 asset-reconciliation-strategy.md §4
  </action>
  <verify>
    <automated>go build ./internal/services/asset/... 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/services/asset/...` 退出码 0
    - `ReconciliationService` 接口包含 `ListExceptions` 与 `GetByID` 两个方法
    - `ReconciliationExceptionService` 接口包含 `List` 与 `GetByID` 两个方法
    - `reconAllowedSortFields` 包含 detectedAt/conflictType/severity/confidenceScore 四个 key
    - 构造函数 `NewReconciliationService(db)` 与 `NewReconciliationExceptionService(db)` 返回接口
    - 代码无 `MarkResolved` / `Create` / `Update` / `Delete` 字样(R1 边界)
    - ListExceptions SQL 用 LEFT JOIN 拉 asset_code / physical_username / responsible_username
    - **`grep -q "sys_workstation_info_point wip" internal/services/asset/reconciliation_service.go` 返回 match**(W-4 显式断言,JOIN 链路就位)
    - **`grep -q "sys_user wu" internal/services/asset/reconciliation_service.go` 返回 match**(physical_username JOIN)
    - **`grep -q "sys_user ru" internal/services/asset/reconciliation_service.go` 返回 match**(responsible_username JOIN)
  </acceptance_criteria>
  <done>
    - reconciliation_service.go + reconciliation_exception.go 就位
    - R1 读端点接口稳定,R2 写端点不会破坏 R1 调用方
    - ListExceptions JOIN 链路显式(wip → w → wu / a → ru)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReconciliationSnapshot (MV refresh) + 4 cron global functions</name>
  <files>internal/services/asset/reconciliation_snapshot.go</files>
  <read_first>
    - .planning/phases/42-r1/42-CONTEXT.md D-02 (失败仅 logrus 日志) + D-10 (cron 走 sys_job 表,无新 cron 文件)
    - internal/core/db/migrations/migration_152_mac_matview.go (existing MV analog,看 REFRESH MATERIALIZED VIEW 模式)
    - internal/api/v1/scheduler/job_cron_util.go (sys_job InvokeTarget 字符串调用模式)
  </read_first>
  <behavior>
    - ReconciliationSnapshot interface: RefreshView + LastRefreshAt
    - RefreshView() executes `REFRESH MATERIALIZED VIEW CONCURRENTLY reconciliation_normalized`;失败 log.WithError+ log.Warn,handler 返回 500 + cron 写 job_log
    - LastRefreshAt 从 sys_config.config_key='asset.reconciliation.view.last_refresh_at' 读 ISO8601 — 若无则返回 nil
    - 4 个全局函数可被 sys_job InvokeTarget 字符串调用: reconciliation:refreshView / reconciliation:detectLayer3 / reconciliation:detectExpiredSilence / reconciliation:cleanupExpiredExceptions
  </behavior>
  <action>
    1. 创建 `internal/services/asset/reconciliation_snapshot.go`:
       - `package asset`
       - import: context, log "github.com/sirupsen/logrus", gorm.io/gorm
       - 定义 `type ReconciliationSnapshot interface { RefreshView(ctx) error; LastRefreshAt(ctx) (*time.Time, error) }`
       - 定义 `reconciliationSnapshotServiceImpl struct { db *gorm.DB }` + `NewReconciliationSnapshotService(db *gorm.DB) ReconciliationSnapshot`
       - 实现 `RefreshView`:`return s.db.WithContext(ctx).Exec("REFRESH MATERIALIZED VIEW CONCURRENTLY reconciliation_normalized").Error;失败 log.WithError+ log.Warn("snapshot refresh failed, retry next cycle"),不返回 error 给调用方(本函数返回 error 给 handler/cron,handler 返回 500 + cron 写 job_log)`
       - 实现 `LastRefreshAt`:从 `sys_config.config_key='asset.reconciliation.view.last_refresh_at'` 读 ISO8601 时间戳 — 若无则返回 nil(此 config 在 RefreshView 成功后由调用方写入;R1 不强求,占位接口)
       - 暴露全局函数 `func ExecuteRefreshViewTask(ctx context.Context) error`(供 sys_job InvokeTarget=`reconciliation:refreshView` 调用):内部 NewReconciliationSnapshotService + RefreshView
       - 暴露全局函数 `func ExecuteDetectLayer3Task(ctx context.Context) error`(供 sys_job InvokeTarget=`reconciliation:detectLayer3` 调用):内部 RefreshView(确保 MV 是最新的)+ NewReconciliationDetection + DetectLayer3(R1 真实执行,见 Task 3 detection file)
       - 暴露 `func ExecuteDetectExpiredSilenceTask(ctx context.Context) error` 占位:log.Info("R1 placeholder, R2 implements") + return nil
       - 暴露 `func ExecuteCleanupExpiredExceptionsTask(ctx context.Context) error` 占位:同上
       - 注释:D-02 失败仅 log,无 SysNotice/无告警位
  </action>
  <verify>
    <automated>go build ./internal/services/asset/... 2>&1 | head -20 && grep -n "ExecuteRefreshViewTask\|ExecuteDetectLayer3Task\|ExecuteDetectExpiredSilenceTask\|ExecuteCleanupExpiredExceptionsTask" internal/services/asset/reconciliation_snapshot.go
</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/services/asset/...` 退出码 0
    - 4 个全局函数全部存在:ExecuteRefreshViewTask / ExecuteDetectLayer3Task / ExecuteDetectExpiredSilenceTask / ExecuteCleanupExpiredExceptionsTask
    - `RefreshView` 调用 `REFRESH MATERIALIZED VIEW CONCURRENTLY reconciliation_normalized`
    - `LastRefreshAt` 从 `sys_config.config_key='asset.reconciliation.view.last_refresh_at'` 读 ISO8601,无则返回 nil
    - 失败 log 路径:log.WithError + log.Warn,不返回 error 给 cron
  </acceptance_criteria>
  <done>
    - reconciliation_snapshot.go 就位
    - 4 个 cron InvokeTarget 全局函数就位
    - RefreshView 失败仅 log(D-02)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ReconciliationDetection (Layer 3 Type A-F + confidence + unique violation catch) + tests</name>
  <files>internal/services/asset/reconciliation_detection.go, internal/services/asset/reconciliation_test.go</files>
  <read_first>
    - .planning/notes/asset-reconciliation-strategy.md §3.1 (Type A-F 分类逻辑)
    - .planning/phases/42-r1/42-CONTEXT.md D-07/D-08/D-09/D-11 (Layer 3 边界、mac1 优先、Type A 跳过、unique violation catch)
    - internal/services/operations/batch_upserter.go (isDuplicateKeyError function)
    - internal/services/operations/asset_statistics_test.go (sqlite in-memory test pattern)
    - .planning/notes/260627-reconciliation-reuse-audit.md F7 (cron 走 sys_job 表,无新 cron 文件)
  </read_first>
  <behavior>
    - DetectLayer3() iterates reconciliation_normalized, classifies each row into Type A/B/C/D/E/F based on physical/declared/ad presence + match
    - Type A (physical + declared match): NO insert to sys_data_reconciliation (D-09)
    - Type B/C/D/E/F: insert with confidence = physical*0.5 + declared*0.3 + ad*0.2, severity=low/medium/high/critical mapping (B/C=high, D=medium, E=low, F=medium)
    - On insert: catch pgconn 23505 via isDuplicateKeyError + log + continue (D-11)
    - 5 unit tests + 1 sqlite in-memory integration test,total 6 个测试
  </behavior>
  <action>
    1. 创建 `internal/services/asset/reconciliation_detection.go`:
       - `package asset`
       - import: context, encoding/json, time, log, gorm.io/gorm, gorm.io/datatypes, lib/pq, models
       - 定义 `type ConflictSignals struct { HasPhysical bool; HasDeclared bool; HasAD bool; PhysicalMatchDeclared bool; PhysicalMatchAD bool }`
       - 定义 `type ClassifiedException struct { AssetID string; ConflictType string; Severity string; Confidence float64; RawSnapshot map[string]interface{} }`
       - 定义 `type ReconciliationDetection interface { DetectLayer3(ctx) (inserted int, skipped int, err error); classifySignals(row interface{}) ConflictSignals; classifyType(s ConflictSignals) string; computeConfidence(s ConflictSignals) float64; computeSeverity(t string) string }`
       - 定义 `reconciliationDetectionImpl struct { db *gorm.DB }` + `NewReconciliationDetection(db *gorm.DB) ReconciliationDetection`
       - 实现 `classifySignals`:从 row 提取 user_id / workstation_id / ad_user_id 的存在与匹配
       - 实现 `classifyType`:
         - physical=true, declared=true 且匹配 → A
         - physical=true, declared=false → B
         - physical=true, declared=true 但不匹配 → C
         - physical=false, declared=true → D
         - 都没有 → E
         - AD 单独不一致(physical/declared 一致但 ad 不一致或 ad 存在但 physical/declared 没有) → F
       - 实现 `computeConfidence`:physical_match*0.5 + declared_match*0.3 + ad_match*0.2
       - 实现 `computeSeverity`:B/C=high, D/F=medium, E=low
       - 实现 `DetectLayer3`:
         a. 查 `reconciliation_normalized` 全部行(SELECT asset_id, asset_code, asset_ip, physical_user_id, physical_username, responsible_user_id, responsible_username, ad_user_id, observed_at)
         b. 遍历每行,调 `classifySignals(row)` → ConflictSignals
         c. 调 `classifyType(signals)` → string (A-F)
         d. 若 type==A:continue(D-09)
         e. 调 `computeConfidence(signals)` → float64
         f. 调 `computeSeverity(type)` → string
         g. 构造 RawSnapshot(包含所有原始字段,序列化 JSON)
         h. INSERT INTO sys_data_reconciliation(...)
         i. 若 err 包含 unique violation on uniq_recon_asset_type_open:log.Debug + continue(D-11 静默)
         j. 计数 inserted/skipped
       - 顶部注释:D-07 完整 Layer 3、D-08 mac1 优先(由 MV SQL `COALESCE(NULLIF(a.mac1,''), NULLIF(a.mac2,''))` 保证)、D-09 Type A 不入主表、D-11 catch unique violation

    2. 创建 `internal/services/asset/reconciliation_test.go`:
       - 模仿 `internal/services/operations/asset_statistics_test.go` 的 sqlite in-memory 模式
       - Test 1: `TestClassifyType` — 表驱动测试,输入 ConflictSignals,期望 A-F 字符串
       - Test 2: `TestComputeConfidence` — 给定 signals 期望 confidence 数值
       - Test 3: `TestComputeSeverity` — 给定 type 期望 severity
       - Test 4: `TestDetectLayer3_TypeA_NotInserted` — 构造 reconciliation_normalized 视图(用真 SQLite 模拟),运行 DetectLayer3,断言 sys_data_reconciliation 表中无 A 类记录(D-09)
       - Test 5: `TestDetectLayer3_DuplicateViolation_Skipped` — 预插入 Type C 异常,再运行 DetectLayer3,断言不重复插入(D-11)
       - 用 `gorm.io/driver/sqlite` 内存模式 + `CREATE TABLE sys_data_reconciliation` + `CREATE TABLE ops_asset` + `CREATE VIEW reconciliation_normalized AS ...` (SQLite 不支持 MV,用 VIEW 模拟)
  </action>
  <verify>
    <automated>go test ./internal/services/asset/... -v -run TestClassifyType\|TestComputeConfidence\|TestComputeSeverity\|TestDetectLayer3 2>&1 | tail -50
</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/services/asset/...` 全部 PASS(累计 5 个 Layer 3 测试)
    - Layer 3 分类覆盖 A/B/C/D/E/F 六种
    - Test 4 验证 Type A 不入 sys_data_reconciliation(0 行)
    - Test 5 验证重复异常不抛错(inserted=0 skipped>=1)
    - `DetectLayer3` 返回值 (inserted, skipped int, err error)
    - unique violation 错误识别依赖 isDuplicateKeyError 或本地等价的 isReconciliationDuplicate 函数
    - 顶部注释明确 D-07 / D-08 / D-09 / D-11
  </acceptance_criteria>
  <done>
    - reconciliation_detection.go 就位
    - reconciliation_test.go 5 个测试全部通过
    - Layer 3 分类函数可独立测试(classifySignals/classifyType/computeConfidence/computeSeverity 纯函数)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Handler + Router (R1 read endpoints + exception skeleton)</name>
  <files>internal/api/v1/asset/reconciliation_handler.go, internal/api/v1/asset/reconciliation_router.go, internal/api/v1/asset/reconciliation_exception_handler.go, internal/api/v1/asset/reconciliation_exception_router.go</files>
  <read_first>
    - internal/api/v1/operations/asset_handler.go (struct + WithCore + CRUD + operlog pattern)
    - internal/api/v1/scheduler/job_router.go (Setup*Router pattern)
    - internal/api/v1/system/ad_account_handler.go (8-endpoint batch handler pattern)
    - .planning/phases/42-r1/42-PATTERNS.md (ModuleReconciliation constant + D-16 + D-18 no mark-resolved)
  </read_first>
  <behavior>
    - POST /asset/reconciliation/exception/list → handler.ListExceptions
    - POST /asset/reconciliation/exception/:id → handler.GetExceptionByID
    - POST /asset/reconciliation/exception-rule/list → handler.ListRules (R1 skeleton)
    - POST /asset/reconciliation/exception-rule/:id → handler.GetRuleByID (R1 skeleton)
    - All read endpoints: no operlog.Record (read operations, CLAUDE.md "Statistics 不记 operlog")
    - R1 has NO MarkResolved / Create / Update / Delete handlers (D-18)
    - ModuleReconciliation = "资产对账" constant defined
  </behavior>
  <action>
    1. 创建 `internal/api/v1/asset/reconciliation_handler.go`:
       - `package asset`
       - import: net/http, gin, internal/core, internal/models, internal/services/asset, internal/utils/operlog, pkg/response
       - 顶部 const: `const ModuleReconciliation = "资产对账"`(D-16 仅 1 个 module 常量;R2 才会加 ModuleReconciliationExceptionRule 等)
       - 定义 `type ReconciliationHandler struct { service asset.ReconciliationService; core *core.Core }`
       - `func NewReconciliationHandler(svc asset.ReconciliationService) *ReconciliationHandler`
       - `func (h *ReconciliationHandler) WithCore(core *core.Core) *ReconciliationHandler`(模仿 asset_handler.go:31-37 模式)
       - `func (h *ReconciliationHandler) ListExceptions(c *gin.Context)`:
         - 解析 body 为 `asset.ExceptionListParams`(用 ShouldBindJSON)
         - 调 `h.service.ListExceptions(ctx, &params)`
         - 错误 → response.Error(c, http.StatusInternalServerError, err.Error())
         - 成功 → response.Success(c, pageResult)
         - **不**调 operlog.Record(读操作,参考 operations/asset_handler.go:198-207)
       - `func (h *ReconciliationHandler) GetExceptionByID(c *gin.Context)`:
         - `c.Param("id")` → 调 service.GetByID
         - nil → response.Error(c, http.StatusNotFound, "异常不存在")
         - 成功 → response.Success
       - 顶部注释说明:D-18 R1 无 mark-resolved handler

    2. 创建 `internal/api/v1/asset/reconciliation_router.go`:
       - `package asset`
       - `func SetupReconciliationRouter(r *gin.RouterGroup, core *core.Core)`:
         - `service := assetSvc.NewReconciliationService(core.DB.GetDB())`
         - `handler := asset.NewReconciliationHandler(service).WithCore(core)`
         - 注册:`r.POST("/exception/list", handler.ListExceptions)` + `r.POST("/exception/:id", handler.GetExceptionByID)`
         - 注释:本函数是 reconciliation 读端点入口,42-04 会再 SetupReconciliationStatisticsRouter;42-06 整合到 router.go

    3. 创建 `internal/api/v1/asset/reconciliation_exception_handler.go`:
       - `package asset`
       - 与 reconciliation_handler.go 同结构,但 service 类型为 `asset.ReconciliationExceptionService`
       - `func (h *ExceptionHandler) ListRules(c *gin.Context)`:解析分页 → 调 List → response
       - `func (h *ExceptionHandler) GetRuleByID(c *gin.Context)`:调 GetByID
       - 注释:R1 skeleton,R3 实现 Create/Update/Delete/enable/disable

    4. 创建 `internal/api/v1/asset/reconciliation_exception_router.go`:
       - `package asset`
       - `func SetupReconciliationExceptionRouter(r *gin.RouterGroup, core *core.Core)`:
         - `service := assetSvc.NewReconciliationExceptionService(core.DB.GetDB())`
         - `handler := asset.NewExceptionHandler(service).WithCore(core)`
         - 注册:`r.POST("/exception-rule/list", handler.ListRules)` + `r.POST("/exception-rule/:id", handler.GetRuleByID)`
         - 注释:本函数为 R3 路由预留,R1 仅 skeleton;42-06 整合到 router.go
  </action>
  <verify>
    <automated>go build ./... 2>&1 | head -20
</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` 退出码 0
    - 4 个新文件就位
    - `ModuleReconciliation = "资产对账"` const 在 reconciliation_handler.go 顶部(D-16)
    - 2 个 Setup*Router 函数存在
    - 共 4 个 POST 端点(2 reconciliation + 2 reconciliation exception)
    - 无 mark-resolved / Create / Update / Delete handler(D-18)
    - 读端点 handler 不调用 operlog.Record
    - Setup*Router 自管路由,不预注册到 router.go(避免 Excel 导入路由冲突陷阱)
  </acceptance_criteria>
  <done>
    - Handler + Router 4 个文件就位
    - 4 个 POST 端点就位
    - 42-06 任务会在 internal/api/router.go 调用这两个 Setup*Router
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Client requests include query params (pageNum, pageSize, filters) |
| Service → DB | Layer 3 detection reads reconciliation_normalized (no PII surface) |
| Cron → Service | sys_job cron calls ExecuteXxxTask globally; no user auth in cron context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05 | Repudiation | Layer 3 detection cron writes | accept | R1 writes are internal (cron-triggered, not user-driven); R2 onwards user actions will be operlog'd |
| T-42-06 | Denial of Service | DetectLayer3 long iteration | mitigate | Single cron trigger per 6min, MV limits row count; if >10K assets add LIMIT + pagination in R2 |
| T-42-07 | Information Disclosure | RawSnapshot JSONB | accept | Contains asset_id, MAC, user_id — already in `ops_asset` accessible to authenticated users; no new exposure |
| T-42-08 | Tampering | confidence score computation | mitigate | Score uses fixed formula (physical*0.5 + declared*0.3 + ad*0.2); coefficients read from sys_config at runtime but seed-locked in R1 |
| T-42-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in this plan |
</threat_model>

<verification>
1. `go build ./...` 退出码 0
2. `go test ./internal/services/asset/... -v` 全部 PASS(5 个 Layer 3 测试)
3. ReconciliationService + ReconciliationExceptionService 接口可被编译
4. Layer 3 分类函数可对 6 种 signals 正确返回 Type A-F
5. RefreshView 调用正确 SQL
6. 4 个 POST 端点注册
7. 4 个 cron InvokeTarget 全局函数就位
</verification>

<success_criteria>
- ReconciliationService (ListExceptions + GetByID) 就位 + JOIN 链路显式
- ReconciliationExceptionService R1 skeleton (List + GetByID) 就位
- Layer 3 检测覆盖 Type A-F + 置信度评分 + Type A skip + unique violation catch
- RefreshView CONCURRENTLY 刷新 + 失败 log
- 4 个 sys_job InvokeTarget 全局函数就位(2 真实 + 2 占位)
- Handler + Router 4 个文件 + 4 个端点
- operlog module 常量 ModuleReconciliation 定义
- 单元测试 5 个全部通过
</success_criteria>

<output>
Create `.planning/phases/42-r1/42-02-SUMMARY.md` when done
</output>
