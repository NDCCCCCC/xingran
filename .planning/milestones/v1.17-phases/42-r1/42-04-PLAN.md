---
phase: 42-r1
plan: 04
type: execute
wave: 2
depends_on:
  - 42-01
  - 42-02
files_modified:
  - internal/services/asset/reconciliation_statistics.go
  - internal/services/asset/reconciliation_statistics_test.go
  - internal/api/v1/asset/reconciliation_statistics_handler.go
  - internal/api/v1/asset/reconciliation_statistics_router.go
autonomous: true
requirements:
  - MONITOR-01
user_setup: []

must_haves:
  truths:
    - "6 个 Statistics 端点均走专用 COUNT/SUM/AGGREGATE,严禁 list.length"
    - "Summary 返回 5 KPI(全量资产数/未解决异常数/critical 未解决数/7d 新增/Top1 冲突类型)"
    - "ByConflictType 返回 map[conflict_type]count(覆盖 A-F)"
    - "BySeverity 返回 map[severity]count(low/medium/high/critical)"
    - "HealthTrend 返回 []TrendPoint(按天聚合,默认 7d)"
    - "TopUnresolved 返回 top N 长期未解决异常(默认 limit=10)"
    - "ExceptionRuleStats 返回按 IP 段聚合的命中数"
    - "sqlmock 验证 6 个端点 SQL 正确性"
    - "至少 2 个端点 e2e 验证走真 DB(SQLite 内存)"
    - "D-06: 5 KPI 卡片选型(全量资产数/未解决异常数/critical 数/7d 新增/Top1 冲突类型),不含 0-100 健康度总分"
    - "D-13: HealthTrend 端点仅 PG dev DB 集成测试覆盖(date_trunc + FILTER 语法 SQLite 不支持)"
    - "D-14: 6 个 Statistics 端点混合测试(sqlmock 全覆盖 + 至少 1-2 个端点 e2e 集成)"
  artifacts:
    - path: "internal/services/asset/reconciliation_statistics.go"
      provides: "ReconciliationStatistics interface + impl with 6 methods"
      min_lines: 250
    - path: "internal/services/asset/reconciliation_statistics_test.go"
      provides: "sqlmock + sqlite in-memory tests for 6 endpoints"
      min_lines: 250
    - path: "internal/api/v1/asset/reconciliation_statistics_handler.go"
      provides: "6 endpoint handlers calling ReconciliationStatistics"
      min_lines: 100
    - path: "internal/api/v1/asset/reconciliation_statistics_router.go"
      provides: "SetupReconciliationStatisticsRouter registering 6 POST routes"
      min_lines: 40
  key_links:
    - from: "internal/services/asset/reconciliation_statistics.go"
      to: "ops_asset table"
      via: "SELECT COUNT(*) FROM ops_asset WHERE deleted_at IS NULL"
      pattern: "SELECT COUNT"
    - from: "internal/services/asset/reconciliation_statistics.go"
      to: "sys_data_reconciliation table"
      via: "GROUP BY conflict_type / severity / detected_at"
      pattern: "GROUP BY"
---

<objective>
实现 Phase 42 R1 的 6 个 Statistics 专用 COUNT 端点(Summary / ByConflictType / BySeverity / HealthTrend / TopUnresolved / ExceptionRuleStats),全部走真 SQL 聚合查询,严禁 list.length 路径,提供 5 KPI 卡片 + 3 图表数据源。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/42-r1/42-CONTEXT.md
@.planning/phases/42-r1/42-PATTERNS.md
@.planning/notes/260627-reconciliation-reuse-audit.md F1 (Statistics 端点定义) + F2 (路由注册)
@.planning/notes/asset-reconciliation-strategy.md §3.1 (冲突类型矩阵)

# Reference implementations
@internal/services/operations/asset_service.go:60-75 (Statistics SUM(CASE WHEN) pattern)
@internal/services/operations/asset_statistics_test.go (sqlite in-memory test pattern)
@internal/services/system/config_service.go:23-34 (Statistics interface + result struct)
@internal/api/v1/operations/asset_handler.go:198-207 (Statistics handler no operlog)
</context>

<interfaces>
From `internal/models/asset.go` (v0.5 confirmed):
- `ops_asset` table — fields: id, devicesn, status, mac1, mac2, machine_ip, user_id, deleted_at

From `internal/models/reconciliation.go` (created in 42-01):
- `sys_data_reconciliation` — fields: id, asset_id, conflict_type, severity, confidence_score, detected_at, resolved_at, deleted_at, raw_snapshot, exception_rule_id, applied_actions

From `internal/services/operations/asset_service.go:60-75` (Statistics reference):
```go
type AssetStatisticsResult struct {
    Total  int64 `json:"total"`
    Normal int64 `json:"normal"`
    Stopped int64 `json:"stopped"`
    NBF int64 `json:"nbf"`
}

func (s *assetService) Statistics(ctx context.Context) (*AssetStatisticsResult, error) {
    var result AssetStatisticsResult
    err := s.db.WithContext(ctx).
        Model(&models.Asset{}).
        Select("COUNT(*) AS total, COALESCE(SUM(CASE WHEN status = 0 THEN 1 ELSE 0 END), 0) AS normal, ...").
        Scan(&result).Error
    return &result, err
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ReconciliationStatistics 6 endpoints (Summary / ByConflictType / BySeverity / HealthTrend / TopUnresolved / ExceptionRuleStats)</name>
  <files>internal/services/asset/reconciliation_statistics.go, internal/services/asset/reconciliation_statistics_test.go</files>
  <read_first>
    - internal/services/operations/asset_service.go:60-75 (Statistics pattern)
    - internal/services/operations/asset_statistics_test.go (sqlite in-memory test)
    - internal/services/system/config_service.go (Statistics interface)
    - internal/models/asset.go (ops_asset fields)
    - internal/models/reconciliation.go (sys_data_reconciliation fields)
    - .planning/phases/42-r1/42-CONTEXT.md D-06 (5 KPI 选型) + 42-PATTERNS.md (6 endpoint mapping)
  </read_first>
  <behavior>
    - Summary returns SummaryResult{TotalAssets, OpenExceptions, CriticalOpen, Last7dNew, TopConflictType, TopConflictCount} via 5 separate aggregate queries (1 COUNT ops_asset + 4 sys_data_reconciliation aggregations)
    - ByConflictType returns map[string]int64 for A-F
    - BySeverity returns map[string]int64 for low/medium/high/critical
    - HealthTrend returns []TrendPoint{Date, OpenCount, CriticalCount, NewCount} aggregated by day for last N days (default 7)
    - TopUnresolved returns []ExceptionSummary{ID, AssetCode, ConflictType, Severity, DetectedAt, DaysUnresolved} ORDER BY detected_at ASC LIMIT N (default 10)
    - ExceptionRuleStats returns []RuleStats{RuleID, RuleName, MatchedCount} from sys_data_reconciliation WHERE exception_rule_id IS NOT NULL GROUP BY rule_id
    - NO method uses list/Find/Offset on sys_data_reconciliation main rows
  </behavior>
  <action>
    1. 创建 `internal/services/asset/reconciliation_statistics.go`:
       - `package asset`
       - import: context, time, gorm.io/gorm, internal/models
       - 定义 result struct:
         - `type SummaryResult struct { TotalAssets int64; OpenExceptions int64; CriticalOpen int64; Last7dNew int64; TopConflictType string; TopConflictCount int64 }`
         - `type TrendPoint struct { Date string; OpenCount int64; CriticalCount int64; NewCount int64 }`
         - `type ExceptionSummary struct { ID string; AssetCode string; ConflictType string; Severity string; DetectedAt time.Time; DaysUnresolved int }`
         - `type RuleStats struct { RuleID string; RuleName string; MatchedCount int64 }`
       - 定义 `type StatsFilter struct { Days int }`(R1 仅 days,future R3+ 扩展)
       - 定义 `type ReconciliationStatistics interface { Summary(ctx, filter) (*SummaryResult, error); ByConflictType(ctx, filter) (map[string]int64, error); BySeverity(ctx, filter) (map[string]int64, error); HealthTrend(ctx, filter) ([]TrendPoint, error); TopUnresolved(ctx, limit) ([]ExceptionSummary, error); ExceptionRuleStats(ctx) ([]RuleStats, error) }`
       - 定义 `reconciliationStatisticsImpl struct { db *gorm.DB }` + `NewReconciliationStatistics(db *gorm.DB) ReconciliationStatistics`
       - 实现 6 个方法,每个方法都走 raw GORM aggregate query,严禁 Find/Offset:
         a. `Summary`:
            - `var totalAssets int64; db.Model(&models.Asset{}).Where("deleted_at IS NULL").Count(&totalAssets)`
            - `var openExceptions int64; db.Model(&models.SysDataReconciliation{}).Where("deleted_at IS NULL AND resolved_at IS NULL").Count(&openExceptions)`
            - `var criticalOpen int64; db.Model(...).Where("... AND severity = ?", "critical").Count(&criticalOpen)`
            - `var last7d int64; db.Model(...).Where("... AND detected_at >= ?", time.Now().Add(-7*24*time.Hour)).Count(&last7d)`
            - Top1 conflict: `db.Model(...).Select("conflict_type, COUNT(*) AS cnt").Where("deleted_at IS NULL").Group("conflict_type").Order("cnt DESC").Limit(1).Row().Scan(&topType, &topCount)`
            - 填充 SummaryResult
         b. `ByConflictType`: 6 行种子预定义 {A,B,C,D,E,F},`SELECT conflict_type, COUNT(*) FROM sys_data_reconciliation WHERE deleted_at IS NULL GROUP BY conflict_type` 扫入 map
         c. `BySeverity`: 同样 GROUP BY severity
         d. `HealthTrend`: 用 `db.Raw("SELECT TO_CHAR(date_trunc('day', detected_at), 'YYYY-MM-DD') AS date, COUNT(*) FILTER (WHERE resolved_at IS NULL) AS open_count, COUNT(*) FILTER (WHERE severity='critical' AND resolved_at IS NULL) AS critical_count, COUNT(*) AS new_count FROM sys_data_reconciliation WHERE deleted_at IS NULL AND detected_at >= ? GROUP BY date ORDER BY date", sevenDaysAgo).Scan(&points)`(PostgreSQL `date_trunc` + `FILTER` 语法;SQLite 走兼容 SQL: `strftime('%Y-%m-%d', detected_at) AS date`)
         e. `TopUnresolved`: `SELECT r.id, a.devicesn AS asset_code, r.conflict_type, r.severity, r.detected_at, EXTRACT(DAY FROM (NOW() - r.detected_at)) AS days_unresolved FROM sys_data_reconciliation r LEFT JOIN ops_asset a ON a.id = r.asset_id WHERE r.deleted_at IS NULL AND r.resolved_at IS NULL ORDER BY r.detected_at ASC LIMIT ?` — 实际 SQL 用 GORM 链式 + 表达式
         f. `ExceptionRuleStats`: `SELECT r.exception_rule_id, e.name, COUNT(*) AS cnt FROM sys_data_reconciliation r LEFT JOIN sys_reconciliation_exception e ON e.id = r.exception_rule_id WHERE r.deleted_at IS NULL AND r.exception_rule_id IS NOT NULL GROUP BY r.exception_rule_id, e.name`
       - 顶部注释:D-06 5 KPI;MEMORY `stat-cards-from-list-length-capped-at-100` 严禁 list.length

    2. 创建 `internal/services/asset/reconciliation_statistics_test.go`:
       - 模仿 `internal/services/operations/asset_statistics_test.go` 的 sqlite in-memory 模式
       - Test 1: `TestReconciliationStatistics_Summary` — 插入 10 ops_asset + 20 sys_data_reconciliation(混合 A/B/C 严重级 + 5 7d 内),验证 TotalAssets=10 / OpenExceptions=20 / CriticalOpen=N / Last7dNew=5 / TopConflictType=...
       - Test 2: `TestReconciliationStatistics_ByConflictType` — 插入不同 conflict_type 数据,验证 map 覆盖 A-F 6 个 key
       - Test 3: `TestReconciliationStatistics_BySeverity` — 验证 map 覆盖 4 个 severity
       - Test 4: `TestReconciliationStatistics_HealthTrend` — 插入 7d 数据,验证返回 7 个 TrendPoint
       - Test 5: `TestReconciliationStatistics_TopUnresolved` — 插入 15 条 resolved_at IS NULL,验证 limit=10
       - Test 6: `TestReconciliationStatistics_ExceptionRuleStats` — 插入带 exception_rule_id 数据,验证 RuleStats 聚合
       - Test 7: `TestReconciliationStatistics_NoListLength` — 静态检查 6 个方法不出现 `Find(` / `.Offset(` 关键字
       - 全部用 `gorm.io/driver/sqlite` 内存模式 + `CREATE TABLE sys_data_reconciliation / ops_asset / sys_reconciliation_exception`
  </action>
  <verify>
    <automated>go test ./internal/services/asset/... -v -run TestReconciliationStatistics 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/services/asset/...` 全部 PASS(累计 ≥ 17 个)
    - 6 个方法均走 SELECT COUNT / SUM / GROUP BY aggregate
    - Test 7 静态断言 6 个方法体内不出现 `Find(` / `.Offset(` 关键字(防 list.length 回归)
    - Summary 返回值字段命名 camelCase(TotalAssets / OpenExceptions / CriticalOpen / Last7dNew / TopConflictType / TopConflictCount)
    - HealthTrend 默认 7d,可通过 filter.Days 调整
    - TopUnresolved 默认 limit=10
       - **W-5 HealthTrend PG/SQLite 漂移说明**: HealthTrend 端点 SQL 使用 PostgreSQL 特定语法 `date_trunc("day", detected_at)` + `FILTER (WHERE ...)` 聚合 — SQLite 不支持 `FILTER` 子句,SQLite unit test 仅覆盖其他 5 个端点(Summary / ByConflictType / BySeverity / TopUnresolved / ExceptionRuleStats),不覆盖 HealthTrend。HealthTrend 端点需 PG dev DB 集成测试(参考 CONTEXT.md D-13 集成测试策略)。SQLite 测试若跑 HealthTrend 会编译错误或运行错误,这是已知的预期失败。
       - **W-5 acceptance assertion**: `grep -c "FILTER (WHERE" internal/services/asset/reconciliation_statistics.go` 返回 ≥ 1 match(确认 PG 特定语法已使用);sqlite 测试不期望 HealthTrend 通过。
  </acceptance_criteria>
  <done>
    - reconciliation_statistics.go 6 方法就位
    - reconciliation_statistics_test.go 7 个测试通过
    - list.length 反模式被静态检查覆盖
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Statistics handler + router (6 POST endpoints)</name>
  <files>internal/api/v1/asset/reconciliation_statistics_handler.go, internal/api/v1/asset/reconciliation_statistics_router.go</files>
  <read_first>
    - internal/api/v1/operations/asset_handler.go:198-207 (Statistics handler no operlog)
    - internal/api/v1/scheduler/job_router.go (Setup*Router pattern)
    - .planning/phases/42-r1/42-PATTERNS.md (6 endpoint mapping)
  </read_first>
  <behavior>
    - 6 个 POST 端点:/statistics/summary / /statistics/by-conflict-type / /statistics/by-severity / /statistics/health-trend / /statistics/top-unresolved / /statistics/exception-rule-stats
    - 所有 handler 解析 body → service → response.Success,不调 operlog
    - health-trend 与 top-unresolved 支持 query/body 传 days 与 limit
  </behavior>
  <action>
    1. 创建 `internal/api/v1/asset/reconciliation_statistics_handler.go`:
       - `package asset`
       - import: net/http, gin, internal/core, internal/services/asset, pkg/response
       - 定义 `type StatisticsHandler struct { service asset.ReconciliationStatistics; core *core.Core }`
       - `func NewStatisticsHandler(svc asset.ReconciliationStatistics) *StatisticsHandler`
       - `func (h *StatisticsHandler) WithCore(core *core.Core) *StatisticsHandler`
       - 6 个 handler 方法:
         - `Summary(c *gin.Context)`:解析 body `{ "days": 7 }` → 构造 `asset.StatsFilter{Days: req.Days}` → service.Summary → response
         - `ByConflictType(c)`:同 Summary 模式
         - `BySeverity(c)`:同 Summary 模式
         - `HealthTrend(c)`:同 Summary 模式
         - `TopUnresolved(c)`:解析 body `{ "limit": 10 }`,默认 10 → service.TopUnresolved
         - `ExceptionRuleStats(c)`:无参 → service.ExceptionRuleStats
       - 全部不调 operlog(读端点,Statistics 不记 operlog 模式)

    2. 创建 `internal/api/v1/asset/reconciliation_statistics_router.go`:
       - `package asset`
       - `func SetupReconciliationStatisticsRouter(r *gin.RouterGroup, core *core.Core)`:
         - `service := assetSvc.NewReconciliationStatistics(core.DB.GetDB())`
         - `handler := asset.NewStatisticsHandler(service).WithCore(core)`
         - 注册:`r.POST("/statistics/summary", handler.Summary)` / `/by-conflict-type` / `/by-severity` / `/health-trend` / `/top-unresolved` / `/exception-rule-stats`
         - 注释:42-06 会在 router.go 调用本函数
  </action>
  <verify>
    <automated>go build ./... 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` 退出码 0
    - 2 个新文件就位
    - 6 个 POST 端点注册(`/statistics/summary` + 5 个)
    - StatisticsHandler 6 个方法
    - 全部 handler 不调 operlog.Record
    - Setup*Router 自管路由
  </acceptance_criteria>
  <done>
    - Handler + Router 就位
    - 6 个 POST 端点注册
    - 42-06 整合到 router.go
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Request body contains filter params (days, limit) |
| Service → DB | Read-only aggregate queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-12 | Denial of Service | TopUnresolved with high limit | mitigate | Handler caps limit at MaxPageSize (10000) per `internal/services/operations/pagination_helper.go:17` |
| T-42-13 | Information Disclosure | Summary returns asset count | accept | Count of assets/exceptions is operational metadata, not PII |
| T-42-14 | Tampering | HealthTrend date range | mitigate | days param capped at 365 (1 year max) in handler |
| T-42-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in this plan |
</threat_model>

<verification>
1. `go build ./...` 退出码 0
2. `go test ./internal/services/asset/...` 全部 PASS
3. 6 个 Statistics 方法均返回非空 result
4. Test 7 静态检查 list.length 反模式
5. 6 个 POST 端点注册
</verification>

<success_criteria>
- ReconciliationStatistics 6 方法就位
- 单元测试 7 个全部通过
- list.length 反模式静态守护
- Handler + Router 就位 + 6 端点注册
- Handler 不记 operlog
</success_criteria>

<output>
Create `.planning/phases/42-r1/42-04-SUMMARY.md` when done
</output>
