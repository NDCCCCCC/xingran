---
phase: 42-r1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/models/reconciliation.go
  - internal/core/db/migrations/migration_168_reconciliation_tables.go
  - internal/core/db/migrations/migration_169_reconciliation_dicts_configs.go
  - internal/services/asset/cache_keys.go
  - internal/core/db/database.go
autonomous: true
requirements:
  - RECON-03
  - RECON-04
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05
user_setup: []

must_haves:
  truths:
    - "sys_data_reconciliation + sys_reconciliation_exception 表创建成功(UUID + 软删除 + BaseModel)"
    - "reconciliation_normalized 物化视图可执行 REFRESH MATERIALIZED VIEW CONCURRENTLY"
    - "4 个字典 seed 完整(asset_reconciliation_conflict_type / severity / exception_action / status)"
    - "8 个 config seed 完整(asset.reconciliation.*)"
    - "6 个 workorder 分类 seed 完整(对账-A/B/C/D/E/F 类)"
    - "4 个 sys_job records 已 seed(对账-物化视图刷新 / Layer3 检测 / 静默期重检测 / 例外规则清理)"
    - "internal/services/asset/cache_keys.go 定义 8 个 cache key 常量 + 8 个 helper 函数"
    - "uniq_recon_asset_type_open 部分唯一索引就位(防 R1 告警风暴)"
    - "D-03: 物化视图 asset_ip = ops_asset.machine_ip 单值直取(无多 IP 解析链)"
    - "D-08: 物化视图 SQL 用 COALESCE(NULLIF(a.mac1,''), NULLIF(a.mac2,'')) 保证 mac1 优先"
    - "D-10: cron 走 sys_job 表(4 个 records),不引入 internal/scheduler/reconciliation_tasks.go"
  artifacts:
    - path: "internal/models/reconciliation.go"
      provides: "SysDataReconciliation / SysReconciliationException GORM models"
      min_lines: 100
    - path: "internal/core/db/migrations/migration_168_reconciliation_tables.go"
      provides: "DDL for 2 tables + materialized view + unique index"
      min_lines: 200
    - path: "internal/core/db/migrations/migration_169_reconciliation_dicts_configs.go"
      provides: "4 dict types + 8 configs + 6 workorder categories + 4 sys_job + 6 sys_menu"
      min_lines: 300
    - path: "internal/services/asset/cache_keys.go"
      provides: "8 cache key constants + 8 helper functions"
      min_lines: 50
  key_links:
    - from: "internal/core/db/database.go"
      to: "migrations.Migrate168ReconciliationTables"
      via: "explicit call"
      pattern: "Migrate168ReconciliationTables"
    - from: "internal/core/db/database.go"
      to: "migrations.Migrate169ReconciliationDictsConfigs"
      via: "explicit call"
      pattern: "Migrate169ReconciliationDictsConfigs"
    - from: "internal/models/reconciliation.go"
      to: "internal/core/db/migrations/migration_168_reconciliation_tables.go"
      via: "GORM AutoMigrate"
      pattern: "db\\.AutoMigrate\\(&models\\.SysDataReconciliation"
---

<objective>
建立 Phase 42 R1 的数据库底座:GORM 模型 + DDL migration + 物化视图 + 唯一索引 + 字典/config/workorder 分类/sys_job/菜单 seed + 缓存 key helper。后续 5 个 plan(42-02 ~ 42-06)的所有读写都依赖本 plan 的 schema、字典枚举与 seed 记录。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-r1/42-CONTEXT.md
@.planning/phases/42-r1/42-PATTERNS.md
@.planning/notes/asset-reconciliation-strategy.md
@.planning/notes/260627-reconciliation-reuse-audit.md

# Reference implementations
@internal/models/asset.go (v0.5 confirmed field names: mac1 / mac2 / machine_ip / user_id)
@internal/models/workorder.go (WorkOrderCategory pattern for 6 categories)
@internal/models/log.go (Job model for sys_job seed)
@internal/core/db/migrations/migration_165_sys_dept_location_alias.go (DDL + button permission seed pattern)
@internal/core/db/migrations/migration_148_create_ops_asset_table.go (DO $$ block for explicit unique constraint naming)
@internal/core/db/database.go (lines 393-417 - migration registration pattern)
@internal/services/system/cache_keys.go (cache key helper pattern)
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From `internal/models/log.go:60-72`:
```go
type Job struct {
    BaseModel
    JobName        string        `gorm:"size:64;not null" json:"jobName"`
    JobGroup       string        `gorm:"size:64;not null" json:"jobGroup"`
    InvokeTarget   string        `gorm:"size:500;not null" json:"invokeTarget"`
    CronExpression string        `gorm:"size:255" json:"cronExpression"`
    MisfirePolicy  MisfirePolicy `gorm:"default:0" json:"misfirePolicy"`
    Concurrent     bool          `gorm:"default:false" json:"concurrent"`
    Status         JobStatus     `gorm:"default:0" json:"status"`
    // ...
}
```

From `internal/models/base.go:11-19`: BaseModel provides ID (UUID), CreatedAt, UpdatedAt, DeletedAt (soft delete).

From `internal/core/db/migrations/migration_148_create_ops_asset_table.go:121-130`: DO $$ block pattern for explicit unique constraint naming (`uni_ops_asset_devicesn`).

From `internal/core/db/database.go:393-417`: Migration registration block (sequential calls wrapped in `if err := ...` with `applogger.Errorf`).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: GORM models + DDL migration 168 (tables + MV + unique index)</name>
  <files>internal/models/reconciliation.go, internal/core/db/migrations/migration_168_reconciliation_tables.go</files>
  <read_first>
    - internal/models/asset.go (v0.5 field names)
    - internal/models/workorder.go (BaseModel + enum patterns)
    - internal/core/db/migrations/migration_148_create_ops_asset_table.go (DO $$ block pattern)
    - internal/core/db/migrations/migration_152_mac_matview.go (existing materialized view analog)
    - internal/models/base.go (BaseModel structure)
  </read_first>
  <behavior>
    - sys_data_reconciliation table: 8 business fields + BaseModel + INET asset_ip + JSONB raw_snapshot + JSONB physical_value/declared_value/ad_value + DECIMAL confidence_score
    - sys_reconciliation_exception table: CIDR ip_range + TEXT[] conflict_types + TEXT[] exception_actions + reason ≥ 10 chars
    - reconciliation_normalized MV: full chain asset → port_mac → info_point → workstation → user_id, COALESCE(mac1, mac2) join key
    - unique index uniq_recon_asset_type_open on (asset_id, conflict_type) WHERE resolved_at IS NULL AND deleted_at IS NULL
    - idx_recon_norm_asset unique on reconciliation_normalized(asset_id) for CONCURRENTLY refresh
    - idx_recon_norm_asset_code on reconciliation_normalized(asset_code) for JOIN lookups
  </behavior>
  <action>
    创建 `internal/models/reconciliation.go`,定义两个 GORM model:

    1. `SysDataReconciliation` (table `sys_data_reconciliation`),字段:
       - `BaseModel` 嵌入(UUID + soft delete + timestamps)
       - `AssetID string` gorm:"type:uuid;not null;column:asset_id;index"
       - `ConflictType string` gorm:"size:2;not null;column:conflict_type" (A/B/C/D/E/F)
       - `Severity string` gorm:"size:16;not null;column:severity" (low/medium/high/critical)
       - `PhysicalValue datatypes.JSON` gorm:"type:jsonb;column:physical_value" (optional, use sql.Null or *string for nullable)
       - `DeclaredValue datatypes.JSON` gorm:"type:jsonb;column:declared_value"
       - `ADValue datatypes.JSON` gorm:"type:jsonb;column:ad_value"
       - `ConfidenceScore float64` gorm:"type:decimal(3,2);column:confidence_score"
       - `RawSnapshot datatypes.JSON` gorm:"type:jsonb;not null;column:raw_snapshot"
       - `AssetIP *string` gorm:"type:inet;column:asset_ip"
       - `ExceptionRuleID *string` gorm:"type:uuid;column:exception_rule_id"
       - `AppliedActions pq.StringArray` gorm:"type:text[];column:applied_actions"
       - `DetectedAt time.Time` gorm:"not null;default:now();column:detected_at;index"
       - `ResolvedAt *time.Time` gorm:"column:resolved_at"
       - `ResolvedBy *string` gorm:"column:resolved_by"
       - `ResolutionNote *string` gorm:"type:text;column:resolution_note"
       - `WorkorderID *string` gorm:"type:uuid;column:workorder_id"
       - 实现 `TableName()` 返回 `"sys_data_reconciliation"`

    2. `SysReconciliationException` (table `sys_reconciliation_exception`),字段:
       - `BaseModel` 嵌入
       - `Name string` gorm:"size:128;not null;column:name"
       - `IPRange string` gorm:"type:cidr;not null;column:ip_range"
       - `ConflictTypes pq.StringArray` gorm:"type:text[];column:conflict_types"
       - `ExceptionActions pq.StringArray` gorm:"type:text[];not null;column:exception_actions"
       - `SeverityOverride *string` gorm:"size:16;column:severity_override"
       - `ScopeType string` gorm:"size:16;not null;default:'global';column:scope_type"
       - `ScopeID *string` gorm:"type:uuid;column:scope_id"
       - `Reason string` gorm:"type:text;not null;column:reason" (应用层校验 ≥10 字符)
       - `IsActive int` gorm:"default:0;column:is_active" (0=启用 1=停用,遵循 Status Convention)
       - `ExpiresAt *time.Time` gorm:"column:expires_at"
       - `CreatedBy int64` gorm:"not null;column:created_by"
       - 实现 `TableName()` 返回 `"sys_reconciliation_exception"`

    然后创建 `internal/core/db/migrations/migration_168_reconciliation_tables.go`:
    - 函数签名 `Migrate168ReconciliationTables(db *gorm.DB) error`
    - 步骤 1: `db.AutoMigrate(&models.SysDataReconciliation{}, &models.SysReconciliationException{})` — GORM 会按 tag 创建列
    - 步骤 2: 用 `db.Exec` 创建物化视图 `reconciliation_normalized`(参考 migration_152_mac_matview.go 的 CREATE MATERIALIZED VIEW 模式),完整 LEFT JOIN 链路:
      ```
      FROM ops_asset a
      LEFT JOIN sys_user u ON u.id = a.user_id AND u.deleted_at IS NULL
      LEFT JOIN sys_port_mac pm ON pm.mac = COALESCE(NULLIF(a.mac1, ''), NULLIF(a.mac2, '')) AND pm.deleted_at IS NULL
      LEFT JOIN sys_info_point ip ON ip.port_id = pm.port_id AND ip.deleted_at IS NULL
      LEFT JOIN sys_workstation_info_point wip ON wip.info_point_id = ip.id
      LEFT JOIN sys_workstation w ON w.id = wip.workstation_id AND w.deleted_at IS NULL
      LEFT JOIN sys_user wu ON wu.id = w.user_id AND wu.deleted_at IS NULL
      LEFT JOIN sys_user_ad_attrs ad ON ad.user_id = COALESCE(w.user_id, a.user_id)
      WHERE a.deleted_at IS NULL
      ```
    - 步骤 3: 创建 unique index `idx_recon_norm_asset` on `reconciliation_normalized(asset_id)`(CONCURRENTLY 必需)
    - 步骤 4: 创建 unique index `uniq_recon_asset_type_open` on `sys_data_reconciliation(asset_id, conflict_type) WHERE resolved_at IS NULL AND deleted_at IS NULL`(用 DO $$ 块,显式命名,参考 migration_148 模式)
    - 步骤 5: 验证:`SELECT COUNT(*) FROM reconciliation_normalized` 不报错(物化视图语法正确)
    - 错误处理遵循 migration_165 风格:DDL 错误 log + return,AutoMigrate 失败 log + return
    - 添加 `// migration_152_mac_matview.go` 与 `// migration_148_create_ops_asset_table.go` 的引用注释
  </action>
  <verify>
    <automated>go build ./internal/models/... ./internal/core/db/migrations/... 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/models/...` 退出码 0
    - migration_168 文件存在,`Migrate168ReconciliationTables` 函数可被 database.go 调用
    - 模型字段 column tag 与 ops_asset v0.5 实际列名匹配(MAC1 → mac1, MachineIP → machine_ip, UserID → user_id)
    - 物化视图 SQL 引用 `ops_asset` 实际存在的列名(无 IP 列,只用 MachineIP 或 machine_ip,本 plan 使用 machine_ip 列名)
    - uniq_recon_asset_type_open 部分唯一索引使用 DO $$ 块命名,符合 `xingran-gorm-sql-constraint-naming-conflict` 教训
    - `pq.StringArray` 导入路径 `github.com/lib/pq`,`datatypes.JSON` 导入路径 `gorm.io/datatypes`(若项目已用)或回退到 `json.RawMessage`
  </acceptance_criteria>
  <done>
    - `internal/models/reconciliation.go` 创建,SysDataReconciliation + SysReconciliationException 表名正确
    - `internal/core/db/migrations/migration_168_reconciliation_tables.go` 创建,2 个 AutoMigrate + 物化视图 + 2 个 unique index(普通 + partial)
    - `go build ./...` 退出码 0
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Cache key helper + migration 169 (dict + config + workorder category + sys_job + menu seeds)</name>
  <files>internal/services/asset/cache_keys.go, internal/core/db/migrations/migration_169_reconciliation_dicts_configs.go, internal/core/db/database.go</files>
  <read_first>
    - internal/services/system/cache_keys.go (cache key helper pattern)
    - internal/core/db/migrations/migration_165_sys_dept_location_alias.go (4 button permission seed pattern)
    - internal/core/db/migrations/migration_163_ad_account_pool_menu.go (menu seed pattern with parent lookup)
    - internal/core/db/database.go:393-417 (migration registration block)
    - .planning/notes/260627-reconciliation-reuse-audit.md (seed data values)
  </read_first>
  <behavior>
    - 4 dict types seed with their data:
      - asset_reconciliation_conflict_type: A/B/C/D/E/F (6 values)
      - asset_reconciliation_severity: low/medium/high/critical (4 values)
      - asset_reconciliation_exception_action: no_alert/no_notice/no_workorder/skip_severity/silence (5 values)
      - asset_reconciliation_status: open/resolved (2 values)
    - 8 sys_config seeds with key prefix asset.reconciliation.*
    - 6 sys_workorder_category records: 对账-A/B/C/D/E/F 类
    - 4 sys_job records: 物化视图刷新 / Layer3 检测 / 静默期重检测 / 例外规则清理
    - 6 sys_menu records: 资产对账(目录) + 对账看板 / 异常列表 / 例外规则 + 6 个 button permissions
  </behavior>
  <action>
    1. 创建 `internal/services/asset/cache_keys.go`,定义 8 个 const 模板字符串 + 8 个 helper 函数,完全模仿 `internal/services/system/cache_keys.go:290-296` 的模式:
       - `CacheKeyReconciliationDashboard = "reconciliation:dashboard:%s"` + `GetReconciliationDashboardKey(scope string)`
       - `CacheKeyReconciliationExceptionList = "reconciliation:exception:list:%s"` + `GetReconciliationExceptionListKey(scope string)`
       - `CacheKeyReconciliationExceptionByID = "reconciliation:exception:byID:%s"` + `GetReconciliationExceptionByIDKey(id string)`
       - `CacheKeyReconciliationExceptionRuleList = "reconciliation:exceptionRule:list"` + `GetReconciliationExceptionRuleListKey()`
       - `CacheKeyReconciliationExceptionRuleByID = "reconciliation:exceptionRule:byID:%s"` + `GetReconciliationExceptionRuleByIDKey(id string)`
       - `CacheKeyReconciliationViewLastRefresh = "reconciliation:view:lastRefresh"` + `GetReconciliationViewLastRefreshKey()`
       - `CacheKeyReconciliationHealthByWorkstation = "reconciliation:health:workstation:%s"` + `GetReconciliationHealthByWorkstationKey(wsID string)`
       - `CacheKeyReconciliationHealthByAsset = "reconciliation:health:asset:%s"` + `GetReconciliationHealthByAssetKey(assetID string)`
       - import `"fmt"` 和 `"strings"`(为 R2 留 prefix 剥离函数)
       - 加 R2/R3 注释说明:`F1/R3` 部分 key 在 R2/R3 才被使用,R1 仅 cache_keys 文件就位,无运行时调用

    2. 创建 `internal/core/db/migrations/migration_169_reconciliation_dicts_configs.go`,函数签名 `Migrate169ReconciliationDictsConfigs(db *gorm.DB) error`,包含以下幂等 seed 块(全部按 migration_165 的 count-then-insert 模式,跳过已存在):

       A. 4 dict types + data:
          - dict_type: `asset_reconciliation_conflict_type`,data: 6 条(A=success, B=warning, C=error, D=warning, E=default, F=processing)
          - dict_type: `asset_reconciliation_severity`,data: 4 条(low=default, medium=warning, high=error, critical=error)
          - dict_type: `asset_reconciliation_exception_action`,data: 5 条(no_alert/no_notice/no_workorder=default, skip_severity=warning, silence=default)
          - dict_type: `asset_reconciliation_status`,data: 2 条(open=warning, resolved=success)
          - 使用 `models.DictType` + `models.DictData`(在 `internal/models/dict.go`)

       B. 8 sys_config seeds:
          - `asset.reconciliation.view.refresh_interval` = "5m" (config_type='Y', is_system=1)
          - `asset.reconciliation.score.physical` = "0.5" (Y, system=1)
          - `asset.reconciliation.score.declared` = "0.3" (Y, system=1)
          - `asset.reconciliation.score.ad` = "0.2" (Y, system=1)
          - `asset.reconciliation.exception.default_expiry_days` = "30" (Y, system=1)
          - `asset.reconciliation.alert.critical_threshold` = "5" (Y, system=1)
          - `asset.reconciliation.alert.silence_after_resolved_hours` = "168" (Y, system=1)
          - `asset.reconciliation.health.score_weights` = `{"normal":1.0,"drift":0.5,"conflict":0.0,"nodata":0.7}` (Y, system=1)
          - 使用 `models.Config` + configKey/configValue/configType/isSystem 字段

       C. 6 sys_workorder_category records(对账-A/B/C/D/E/F 类,status=0 enabled,sort_order 100-105):
          - "对账-A类"(description="物理有/责任人有且一致 健康无需动作")
          - "对账-B类" / "对账-C类" / "对账-D类" / "对账-E类" / "对账-F类"
          - 描述参考 asset-reconciliation-strategy.md §3.1
          - 使用 `models.WorkOrderCategory`

       D. 4 sys_job records(JobGroup="reconciliation", InvokeTarget 字符串对应 4 个 task function name):
          - "对账-物化视图刷新" / cron=`@every 5m` / InvokeTarget=`reconciliation:refreshView` / Status=0
          - "对账-Layer3检测" / cron=`@every 6m` / InvokeTarget=`reconciliation:detectLayer3` / Status=0
          - "对账-静默期重检测" / cron=`0 2 * * *` / InvokeTarget=`reconciliation:detectExpiredSilence` / Status=0(R2 才实际执行,R1 seed 占位)
          - "对账-例外规则清理" / cron=`0 3 * * *` / InvokeTarget=`reconciliation:cleanupExpiredExceptions` / Status=0(R3 才实际执行,R1 seed 占位)
          - 使用 `models.Job`

       E. 6 sys_menu records + button permissions:
          - 父菜单 "资产管理" 若已存在则挂载;否则 log 警告 + 跳过挂载(参考 migration_165 缺失父菜单容错)
          - "资产对账"(menu_type='M', parent=资产管理, perms=`asset:reconciliation:list`)
          - "对账看板"(menu_type='C', parent=资产对账, perms=`asset:reconciliation:dashboard`)
          - "异常列表"(menu_type='C', parent=资产对账, perms=`asset:reconciliation:list`)
          - "例外规则"(menu_type='C', parent=资产对账, perms=`asset:reconciliation:exception:list`)— R3 skeleton,R1 仅 seed
          - 6 个 button permissions(visible=0 hidden): 对账-导出 / 对账-例外创建 / 对账-例外更新 / 对账-例外删除 / 对账-例外测试 / 对账-标记已解决
          - 对应 perms: `asset:reconciliation:export` / `:exception:create` / `:exception:update` / `:exception:delete` / `:exception:test` / `:markResolved`
          - 注意:菜单 perms 单数连字符,遵循 `ops 菜单 seed perms 与路由命名不一致` 教训

    3. 修改 `internal/core/db/database.go`:
       - 在 migration_167 之后(migration_167_workstation_backfill_status.go 之后)插入:
         ```
         // Phase 42 R1: 资产对账观测底座 — 表 + 物化视图
         if err := migrations.Migrate168ReconciliationTables(d.DB); err != nil {
             applogger.Errorf("Phase 42 R1 reconciliation tables 迁移失败: %v", err)
         }
         // Phase 42 R1: 字典 + config + workorder category + sys_job + 菜单 seed
         if err := migrations.Migrate169ReconciliationDictsConfigs(d.DB); err != nil {
             applogger.Errorf("Phase 42 R1 reconciliation seed 迁移失败: %v", err)
         }
         ```
       - 保持现有 `applogger.Errorf` + 不中断风格
  </action>
  <verify>
    <automated>go build ./... 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `internal/services/asset/cache_keys.go` 文件创建,8 个 const + 8 个 helper 函数齐全,`go build ./internal/services/asset/...` 退出码 0
    - `internal/core/db/migrations/migration_169_reconciliation_dicts_configs.go` 文件创建
    - `go build ./...` 整体退出码 0
    - 4 个 dict_type 名称严格匹配 `asset_reconciliation_conflict_type` / `asset_reconciliation_severity` / `asset_reconciliation_exception_action` / `asset_reconciliation_status`
    - 8 个 config_key 全部以 `asset.reconciliation.` 开头
    - 6 个 workorder category 名称严格为 "对账-A类" ~ "对账-F类",status=0
    - 4 个 sys_job InvokeTarget 严格匹配 `reconciliation:refreshView` / `reconciliation:detectLayer3` / `reconciliation:detectExpiredSilence` / `reconciliation:cleanupExpiredExceptions`
    - 6 个 menu perms 严格匹配 `asset:reconciliation:list|dashboard|export|exception:list|exception:create|exception:update|exception:delete|exception:test|markResolved`
    - database.go 在 migration_167 之后插入两条新调用
    - 所有 seed 块都使用 count-then-insert 幂等模式(参考 migration_165 风格)
  </acceptance_criteria>
  <done>
    - 8 个 cache key 常量 + helper 函数就位
    - 4 dict + 8 config + 6 workorder category + 4 sys_job + 6 menu seed 全部写入 migration_169
    - database.go 注册 migration_168 + migration_169
    - `go build ./...` 退出码 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → DB | Migration functions run at app startup with elevated DB privileges |
| DB → Service | GORM models expose DB rows to service layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering | migration_169 seed values | mitigate | DDL/seed run only at app startup, source-controlled; no user input |
| T-42-02 | Information Disclosure | JSONB raw_snapshot | mitigate | Stores only internal asset data, no PII outside existing fields; in v1.17 scope |
| T-42-03 | Denial of Service | Unique partial index on reconciliation_normalized | mitigate | asset_id already has 1:1 mapping to ops_asset, CONCURRENTLY refresh requires unique index; documented as D-01 design choice |
| T-42-04 | Elevation of Privilege | Menu seed perms | mitigate | All menu perms use `asset:reconciliation:*` namespace, RequirePermissions enforced by router registration in 42-06 |
| T-42-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan — only existing dependencies (`github.com/lib/pq`, `gorm.io/datatypes`) |
</threat_model>

<verification>
1. `go build ./...` 退出码 0
2. `internal/models/reconciliation.go` 包含 SysDataReconciliation + SysReconciliationException 两个 model
3. migration_168 函数名 + 物化视图 DDL 存在
4. migration_169 函数名 + 4 dict + 8 config + 6 workorder category + 4 sys_job + 6 menu seed 存在
5. database.go 在 migration_167 后有 2 个新调用
6. cache_keys.go 8 const + 8 helper 函数齐全
</verification>

<success_criteria>
- sys_data_reconciliation + sys_reconciliation_exception 表创建成功,UUID + 软删除 + BaseModel
- reconciliation_normalized 物化视图 DDL 正确,unique index on asset_id 就位(CONCURRENTLY 必需)
- 4 dict + 8 config + 6 workorder category + 4 sys_job + 6 menu seed 全部 seed
- uniq_recon_asset_type_open 部分唯一索引就位(D-11 防告警风暴)
- 8 cache key helper 函数就位(INFRA-03)
- operlog module 常量定义在 migration 后续 plan(42-06 统一做)
- go build ./... 通过
</success_criteria>

<output>
Create `.planning/phases/42-r1/42-01-SUMMARY.md` when done
</output>
