---
phase: 42-r1
plan: 05
type: execute
wave: 3
depends_on:
  - 42-02
  - 42-04
files_modified:
  - xingran-react-frontend/src/lib/assetApi.ts
  - xingran-react-frontend/src/lib/queryKeys.ts
  - xingran-react-frontend/src/pages/asset/reconciliation/index.tsx
  - xingran-react-frontend/src/pages/asset/reconciliation/dashboard/index.tsx
  - xingran-react-frontend/src/pages/asset/reconciliation/exceptions/index.tsx
  - xingran-react-frontend/src/hooks/useDashboard.ts
  - xingran-react-frontend/src/hooks/useExceptionList.ts
  - xingran-react-frontend/src/router/routes.tsx
autonomous: false
requirements:
  - RECON-04
  - INFRA-05
  - MONITOR-01
user_setup: []

must_haves:
  truths:
    - "父路由 /asset/reconciliation 302 → /asset/reconciliation/dashboard"
    - "Dashboard 顶部 5 KPI 卡片正确渲染(全量资产数 / 未解决异常数 / critical 数 / 7d 新增 / Top1 冲突类型)"
    - "Dashboard 3 图表(饼图按 Type A-F / 柱状图按 severity / 趋势图 7d 默认)正确渲染"
    - "异常列表 admin 页面正确展示 + 9 列(默认 detected_at DESC)+ 分页 + 筛选"
    - "异常列表读 URL query string 作为初始筛选(type / severity / from / to)"
    - "src/lib/queryKeys.ts 注册 9 个 reconciliation queryKey"
    - "src/lib/assetApi.ts 提供 reconciliationApi 工厂(6 statistics + 2 exception)"
    - "npm run build 通过"
    - "npm run type-check 0 errors"
    - "D-05: Dashboard 与异常列表双向打通(点击图表扇区/柱条 → 跳异常列表 + URL 预填筛选)"
    - "D-15: 异常列表 admin 页依赖 dev DB seed + 手工 UAT(不引入 Vitest,Task 3 checkpoint:human-verify)"
  artifacts:
    - path: "xingran-react-frontend/src/lib/assetApi.ts"
      provides: "reconciliationApi factory with 6 statistics + 2 exception methods"
      min_lines: 80
    - path: "xingran-react-frontend/src/lib/queryKeys.ts"
      provides: "reconciliation namespace with 9 queryKey factories"
      min_lines: 50
    - path: "xingran-react-frontend/src/pages/asset/reconciliation/dashboard/index.tsx"
      provides: "Dashboard with 5 KPI + 3 charts (echarts-for-react)"
      min_lines: 200
    - path: "xingran-react-frontend/src/pages/asset/reconciliation/exceptions/index.tsx"
      provides: "Admin exception list with filter from URL + 9 columns + pagination"
      min_lines: 250
    - path: "xingran-react-frontend/src/pages/asset/reconciliation/index.tsx"
      provides: "Parent route 302 redirect to dashboard"
      min_lines: 30
  key_links:
    - from: "xingran-react-frontend/src/pages/asset/reconciliation/dashboard/index.tsx"
      to: "/asset/reconciliation/statistics/summary"
      via: "post() from lib/api + reconciliationApi.summary()"
      pattern: "reconciliationApi\\.summary"
    - from: "xingran-react-frontend/src/pages/asset/reconciliation/dashboard/index.tsx"
      to: "echarts-for-react"
      via: "ReactECharts component with option"
      pattern: "ReactECharts"
    - from: "xingran-react-frontend/src/pages/asset/reconciliation/exceptions/index.tsx"
      to: "URL query string"
      via: "useSearchParams from react-router-dom"
      pattern: "useSearchParams"
    - from: "xingran-react-frontend/src/lib/queryKeys.ts"
      to: "TanStack Query"
      via: "queryKeys.reconciliation.* factory"
      pattern: "queryKeys\\.reconciliation"
---

<objective>
实现 Phase 42 R1 的前端页面:父路由 302、Dashboard(5 KPI + 3 图表)、异常列表 admin 页(URL query 同步筛选)、useDashboard / useExceptionList hooks、assetApi 工厂、queryKeys 注册。Dashboard 与异常列表双向打通(D-05 决策)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/42-r1/42-CONTEXT.md
@.planning/phases/42-r1/42-PATTERNS.md
@.planning/notes/asset-reconciliation-strategy.md §6 (前端整合点)
@.planning/notes/260627-reconciliation-reuse-audit.md F6 (queryKeys 注册)

# Reference implementations
@xingran-react-frontend/src/lib/opsApi.ts (assetApi factory pattern)
@xingran-react-frontend/src/lib/queryKeys.ts (current structure with `as const`)
@xingran-react-frontend/src/lib/api.ts (post wrapper from CLAUDE.md)
@xingran-react-frontend/src/pages/operations/assets/index.tsx (statistics cards + table pattern)
@xingran-react-frontend/src/hooks/useDict.ts (useDict hook)
@xingran-react-frontend/src/store/ (authStore for token access)
@xingran-react-frontend/src/router/routes.tsx (route registration pattern)
@xingran-react-frontend/src/hooks/useDeptTree.ts (TanStack Query useQuery pattern)

# Backend API contracts (from 42-02 + 42-04)
@internal/api/v1/asset/reconciliation_handler.go (exception/list + exception/:id)
@internal/api/v1/asset/reconciliation_statistics_handler.go (6 statistics endpoints)
@internal/services/asset/reconciliation_statistics.go (SummaryResult / TrendPoint / ExceptionSummary struct)
</context>

<interfaces>
Backend API contracts (executor must use these exact paths):

| Endpoint | Method | Request | Response |
|----------|--------|---------|----------|
| `/asset/reconciliation/exception/list` | POST | `{ conflictType?, severity?, assetCode?, detectedFrom?, detectedTo?, current, pageSize, orderByColumn?, isAsc? }` | `{ list, total, current, pageSize }` |
| `/asset/reconciliation/exception/:id` | POST | — | `SysDataReconciliation row` |
| `/asset/reconciliation/statistics/summary` | POST | `{ days?: 7 }` | `SummaryResult` |
| `/asset/reconciliation/statistics/by-conflict-type` | POST | `{ days?: 7 }` | `map<string, int64>` |
| `/asset/reconciliation/statistics/by-severity` | POST | `{ days?: 7 }` | `map<string, int64>` |
| `/asset/reconciliation/statistics/health-trend` | POST | `{ days?: 7 }` | `TrendPoint[]` |
| `/asset/reconciliation/statistics/top-unresolved` | POST | `{ limit?: 10 }` | `ExceptionSummary[]` |
| `/asset/reconciliation/statistics/exception-rule-stats` | POST | — | `RuleStats[]` |

TypeScript types (from backend):
```typescript
interface SummaryResult {
  totalAssets: number
  openExceptions: number
  criticalOpen: number
  last7dNew: number
  topConflictType: string
  topConflictCount: number
}
interface TrendPoint {
  date: string
  openCount: number
  criticalCount: number
  newCount: number
}
interface ExceptionSummary {
  id: string
  assetCode: string
  conflictType: string
  severity: string
  detectedAt: string
  daysUnresolved: number
}
```

From `src/lib/opsApi.ts:586-651` (assetApi factory):
```typescript
const assetCrudApi = createCrudApi<Asset>({ basePath: "/ops/asset" });
export const assetApi = { ...assetCrudApi, searchBySerial, getDeviceTypes, statistics, ... };
```

From `src/lib/queryKeys.ts:13-42` (current structure):
```typescript
export const queryKeys = {
  dict: { all, list },
  list: { all, page },
  dept: { all, tree },
  duty: { all, poolMembers },
} as const;
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: API factory + queryKeys + hooks (frontend infrastructure)</name>
  <files>xingran-react-frontend/src/lib/assetApi.ts, xingran-react-frontend/src/lib/queryKeys.ts, xingran-react-frontend/src/hooks/useDashboard.ts, xingran-react-frontend/src/hooks/useExceptionList.ts</files>
  <read_first>
    - xingran-react-frontend/src/lib/opsApi.ts (assetApi factory pattern, lines 586-651)
    - xingran-react-frontend/src/lib/api.ts (post wrapper)
    - xingran-react-frontend/src/lib/queryKeys.ts (current structure)
    - xingran-react-frontend/src/hooks/useDict.ts (useQuery pattern with stable deps)
    - xingran-react-frontend/src/pages/operations/assets/index.tsx (useMemo params pattern for useEffect deps)
    - .planning/notes/260627-reconciliation-reuse-audit.md F6 (9 queryKey 命名)
  </read_first>
  <behavior>
    - assetApi.ts exports reconciliationApi with 6 statistics + exceptionList + exceptionGetByID
    - queryKeys.reconciliation namespace with 9 keys
    - useDashboard hook returns 5 useQuery results
    - useExceptionList hook returns useQuery with stable params via useMemo
  </behavior>
  <action>
    1. Create xingran-react-frontend/src/lib/assetApi.ts. Import post from @/lib/api. Define TypeScript interfaces ExceptionListParams, ExceptionListItem, SummaryResult, TrendPoint, ExceptionSummary, RuleStats, PageResult with field names matching backend JSON. Export reconciliationApi object with 8 methods: summary(days=7) → post /asset/reconciliation/statistics/summary; byConflictType(days=7); bySeverity(days=7); healthTrend(days=7); topUnresolved(limit=10); exceptionRuleStats(); exceptionList(params); exceptionGetByID(id). Each method calls post() and returns res.data.

    2. Modify xingran-react-frontend/src/lib/queryKeys.ts. Add reconciliation namespace under existing queryKeys object. Define 9 keys with as const: all, summary, byConflictType (days), bySeverity (days), healthTrend (days), topUnresolved (limit), exceptionList (params), exceptionDetail (id), ruleList. Add type-only import for ExceptionListParams from assetApi at top.

    3. Create xingran-react-frontend/src/hooks/useDashboard.ts. Import useQuery from @tanstack/react-query, reconciliationApi from assetApi, queryKeys from queryKeys. Export useDashboard(windowDays=7) returning object with summary, byConflictType, bySeverity, healthTrend, topUnresolved (each a useQuery result) plus isLoading/isError booleans. Set staleTime: 30_000. Add docstring noting R4 will add useWorkstationHealth.

    4. Create xingran-react-frontend/src/hooks/useExceptionList.ts. Import useQuery and keepPreviousData from @tanstack/react-query, reconciliationApi, queryKeys, ExceptionListParams type. Export useExceptionList(params). Use useMemo(() => params, [JSON.stringify(params)]) for stable reference (per CLAUDE.md useEffect dependencies rule). useQuery with queryKey=queryKeys.reconciliation.exceptionList(stableParams), queryFn returns reconciliationApi.exceptionList, placeholderData: keepPreviousData. Return { data, isLoading, isError }.
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit 退出码 0
    - assetApi.ts 含 8 个 method 导出
    - queryKeys.reconciliation 含 9 个 key
    - useDashboard 返回 5 个 useQuery 结果
    - useExceptionList 用 useMemo + JSON.stringify 稳定 params
  </acceptance_criteria>
  <done>
    - Frontend infrastructure (API + keys + hooks) 就位
    - 2 个 hook 与 2 个 lib 文件就位
    - type-check 通过
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Dashboard + Exceptions pages + parent route 302 + router registration</name>
  <files>xingran-react-frontend/src/pages/asset/reconciliation/index.tsx, xingran-react-frontend/src/pages/asset/reconciliation/dashboard/index.tsx, xingran-react-frontend/src/pages/asset/reconciliation/exceptions/index.tsx, xingran-react-frontend/src/router/routes.tsx</files>
  <read_first>
    - xingran-react-frontend/src/pages/operations/assets/index.tsx (statistics cards pattern, lines 446-466)
    - xingran-react-frontend/src/pages/operations/assets/index.tsx (table + pagination pattern, lines 559-581)
    - xingran-react-frontend/src/hooks/useDict.ts (useDict hook for conflict_type/severity dict)
    - xingran-react-frontend/src/router/routes.tsx (route registration pattern)
    - xingran-react-frontend/src/lib/assetApi.ts (from Task 1)
    - .planning/phases/42-r1/42-CONTEXT.md D-04 (父路由 302) + D-05 (Dashboard 与异常列表双向)
  </read_first>
  <behavior>
    - Parent route renders Navigate to /dashboard
    - Dashboard renders 5 KPI cards in Row/Col grid + 3 ECharts (pie/bar/line)
    - Pie slice click navigates to /asset/reconciliation/exceptions?type=X
    - Bar segment click navigates to /asset/reconciliation/exceptions?severity=Y
    - Exceptions page reads URL query params (type/severity/from/to) as initial filter values
    - Exceptions page has 9 columns: detected_at / conflict_type / severity / asset_code / asset_ip / physical_username / responsible_username / exception_rule_id / operlog_btn
    - operlog_btn shows 查看日志 link to /monitor/operlog?bizId={id} (R1 read-only)
    - NO 标记已解决 button (D-18)
  </behavior>
  <action>
    1. Create xingran-react-frontend/src/pages/asset/reconciliation/index.tsx. Import Navigate from react-router-dom. Return Navigate to /asset/reconciliation/dashboard with replace prop. No content rendering.

    2. Create xingran-react-frontend/src/pages/asset/reconciliation/dashboard/index.tsx. Import ReactECharts from echarts-for-react, Row/Col/Card from antd, useDashboard from @/hooks/useDashboard, useNavigate from react-router-dom. Use useDashboard(7) for default 7d window. Render 5 KPI cards in Row/Col grid. Card 1: TotalAssets with 全量资产数 label. Card 2: OpenExceptions warning style, 未解决异常数 label. Card 3: CriticalOpen red text, critical 级未解决 label. Card 4: Last7dNew, 7d 新增 label. Card 5: TopConflictType + TopConflictCount, Top1 冲突类型 label. Below: 3 ECharts in Row: Pie (ByConflictType map, click event navigates to /asset/reconciliation/exceptions?type=X), Bar (BySeverity map, click navigates to /asset/reconciliation/exceptions?severity=Y), Line (HealthTrend array with 3 lines: openCount, criticalCount, newCount). Use Card title for each chart. Use h2 page title 对账看板.

    3. Create xingran-react-frontend/src/pages/asset/reconciliation/exceptions/index.tsx. Import Table/Form/Input/Select/DatePicker/Button from antd, useSearchParams/useNavigate from react-router-dom, useExceptionList from @/hooks/useExceptionList, useDict from @/hooks/useDict. Use useSearchParams to read initial filter (type/severity/from/to). Build form initial values from URL query. Use useExceptionList(params) for data. Render Table with 9 columns: 1) detected_at (default DESC, sortable via useServerSort), 2) conflict_type (useDict label, color by list_class), 3) severity (useDict label, red for critical), 4) asset_code (from JOIN), 5) asset_ip (INET display), 6) physical_username, 7) responsible_username, 8) exception_rule_id (hidden in R1 per D-18), 9) operlog_btn (Link to /monitor/operlog?bizId={id}). Pagination with showSizeChanger + showTotal. Filter form on top: conflict_type Select, severity Select, asset_code Input, detected_at RangePicker. Submit button updates URL query via setSearchParams. NO 标记已解决 button per D-18.

    4. Modify xingran-react-frontend/src/router/routes.tsx. Import lazy from react. Add lazy import for ReconciliationIndex, ReconciliationDashboard, ReconciliationExceptions. Add 3 route entries under parent path /asset/reconciliation: index (ReconciliationIndex), /dashboard (ReconciliationDashboard), /exceptions (ReconciliationExceptions). Set meta.title to 资产对账 / 对账看板 / 异常列表. Set meta.perms to asset:reconciliation:list / asset:reconciliation:dashboard / asset:reconciliation:list.
  </action>  <verify>
    <automated>cd xingran-react-frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - npm run build 退出码 0
    - 父路由 /asset/reconciliation 渲染 Navigate to /dashboard
    - Dashboard 5 KPI 卡片 + 3 ECharts 渲染
    - Dashboard 饼图 click 事件 navigate 到 /exceptions?type=X
    - Dashboard 柱状图 click 事件 navigate 到 /exceptions?severity=Y
    - Exceptions 9 列渲染(operlog_btn 显示 查看日志 链接,无 标记已解决 按钮)
    - Exceptions 用 useSearchParams 初始化 filter
    - routes.tsx 注册 3 个 lazy route
  </acceptance_criteria>
  <done>
    - Frontend 3 个页面就位(parent 302 + dashboard + exceptions)
    - 路由注册到 routes.tsx
    - npm run build 通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | Client request carries filters + token (auto-attached by authStore interceptor) |
| URL → State | URL query params control initial filter state (D-05 bidirectional) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-15 | Tampering | URL query string injects filter | mitigate | React Router v7 useSearchParams returns typed values; handler decodes string array safely |
| T-42-16 | Repudiation | Dashboard KPI navigates to exception list | accept | Read-only navigation, no destructive operation |
| T-42-17 | Information Disclosure | Dashboard exposes asset counts | accept | KPI values are operational metadata, not PII |
| T-42-18 | Denial of Service | ECharts rendering large trend data | mitigate | healthTrend default 7d, max 365d via filter cap; ECharts lazy renders via onEvents |
| T-42-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (uses existing echarts-for-react, antd, react-router-dom, @tanstack/react-query) |
</threat_model>

<verification>
1. cd xingran-react-frontend && npm run build 退出码 0
2. cd xingran-react-frontend && npx tsc --noEmit 退出码 0
3. 父路由 /asset/reconciliation 渲染 Navigate to /dashboard
4. Dashboard 5 KPI 卡片 + 3 ECharts 渲染
5. Dashboard 饼图 click 事件 navigate 到 /exceptions?type=X
6. Dashboard 柱状图 click 事件 navigate 到 /exceptions?severity=Y
7. Exceptions 9 列渲染(operlog_btn 显示 查看日志 链接,无 标记已解决 按钮)
8. Exceptions 用 useSearchParams 初始化 filter
9. routes.tsx 注册 3 个 lazy route
</verification>

<success_criteria>
- 父路由 /asset/reconciliation 302 到 /dashboard
- Dashboard 5 KPI + 3 图表正确渲染
- 异常列表 admin 页 9 列 + 筛选 + 分页
- URL query 同步筛选(D-05 双向打通)
- queryKeys 9 个 key 注册
- assetApi 8 个 method 导出
- useDashboard + useExceptionList hooks 就位
- npm run build + npx tsc --noEmit 通过
</success_criteria>

<output>
Create `.planning/phases/42-r1/42-05-SUMMARY.md` when done
</output>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 手工 UAT 验证 Dashboard + 异常列表 admin 页(W-1/W-2 守卫)</name>
  <what-built>
    前端 3 个页面:父路由 302(/asset/reconciliation → /dashboard)、Dashboard(5 KPI + 3 图表)、异常列表 admin 页(URL query 同步筛选)。由 Task 1 (lib/hooks) + Task 2 (pages) 共同产出。
  </what-built>
  <how-to-verify>
    依据 .planning/phases/42-r1/42-CONTEXT.md D-15 (依赖 dev DB seed + 手工 UAT) + ROADMAP success criteria 7 (异常列表 admin 页面可正确展示 Type A-F 分布与 Top 10 长期未解决),执行以下步骤:

    1. **启动 dev server**:
       - 后端:`go run ./cmd/main.go`(或 `.\xingran-backend.exe`)
       - 前端:`cd xingran-react-frontend && npm run dev`
       - 确认两端启动无错误日志

    2. **seed dev DB**(如果 42-01 migration 已跑过,seed 数据应已就位):
       - 验证 4 个 dict_type 已 seed(查 SELECT * FROM sys_dict_type WHERE dict_type LIKE 'asset_reconciliation%')
       - 验证 6 个 workorder category 已 seed(查 SELECT * FROM sys_workorder_category WHERE name LIKE '对账-%')
       - 验证 4 个 sys_job records 已 seed(查 SELECT * FROM sys_job WHERE job_group='reconciliation')
       - 触发 cron 或手动调 `POST /system/job/run` 跑 `reconciliation:refreshView` + `reconciliation:detectLayer3`,让 `sys_data_reconciliation` 表有真实数据

    3. **登录 + 打开 Dashboard**:
       - 浏览器打开 `http://localhost:4000`(前端 dev server)
       - 用 admin 账号登录
       - 导航到 `/asset/reconciliation`,应自动 302 到 `/asset/reconciliation/dashboard`

    4. **验证 5 KPI 卡片** (D-06 选型):
       - **Card 1**:全量资产数 — 显示 `summary.totalAssets` 数值(查 `SELECT COUNT(*) FROM ops_asset WHERE deleted_at IS NULL` 应一致)
       - **Card 2**:未解决异常数 — 显示 `summary.openExceptions`(查 `sys_data_reconciliation WHERE deleted_at IS NULL AND resolved_at IS NULL`)
       - **Card 3**:critical 级未解决数 — 显示 `summary.criticalOpen` 红色字体
       - **Card 4**:7d 新增异常数 — 显示 `summary.last7dNew`
       - **Card 5**:Top1 冲突类型 + Top1 计数 — 显示 `summary.topConflictType` + `summary.topConflictCount`
       - **截图验证**:5 个 Card 都在 Row/Col 网格内正确渲染,无 loading 态卡死

    5. **验证 3 个图表** (D-05 双向打通):
       - **饼图 (ByConflictType)**:6 个扇区 A/B/C/D/E/F,点击任一扇区应 navigate 到 `/asset/reconciliation/exceptions?type=X`
       - **柱状图 (BySeverity)**:4 个柱 low/medium/high/critical,点击任一柱条应 navigate 到 `/asset/reconciliation/exceptions?severity=Y`
       - **趋势图 (HealthTrend)**:3 条线 openCount / criticalCount / newCount,默认 7d,横轴日期,纵轴计数
       - **截图验证**:3 个 ECharts 都正常 render,无空白或 undefined 错误

    6. **验证异常列表 admin 页** (D-18 只读 + ROADMAP success criteria 7):
       - 导航到 `/asset/reconciliation/exceptions` 或从图表点击跳转
       - **URL query 同步筛选**:从图表带 `?type=C` 跳过来,页面 filter 框应预填 C,查询结果只显示 Type C
       - **9 列渲染**:detected_at / conflict_type / severity / asset_code / asset_ip / physical_username / responsible_username / exception_rule_id / operlog_btn
       - **operlog_btn**:点击 "查看日志" 应打开 `/monitor/operlog?bizId={id}`(只读链接,无 标记已解决 按钮)
       - **默认排序**:detected_at DESC
       - **分页**:showSizeChanger + showTotal 正常,翻页加载不卡死
       - **筛选表单**:conflict_type Select / severity Select / asset_code Input / detected_at RangePicker,提交后 URL query 同步更新

    7. **跨模块权限边界** (W-1 后置验证):
       - 用只含 `ops:workstation:list` 权限(无 `asset:reconciliation:list`)的账号登录
       - 直接访问 `/asset/reconciliation/dashboard` → 应被路由守卫拦截(无权限或 403)
       - 切换 admin 账号后正常
  </how-to-verify>
  <resume-signal>
    Type "approved" 如果所有验证项通过(包括 5 KPI + 3 图表 + 9 列 + URL query + 跨模块权限)。

    如果有问题,描述:
    - 哪个 KPI 数字与 DB count 不符
    - 哪个图表未渲染或 click 事件无响应
    - 哪个列缺失或样式错乱
    - URL query 是否正确预填
    - 跨模块权限是否生效

    失败回滚:报告 dashboard 字段渲染异常,Task 1/2 修复后重跑 UAT。
  </resume-signal>
</task>
