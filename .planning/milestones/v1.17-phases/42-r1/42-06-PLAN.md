---
phase: 42-r1
plan: 06
type: execute
wave: 3
depends_on:
  - 42-01
  - 42-02
  - 42-04
  - 42-05
files_modified:
  - internal/api/router.go
  - internal/api/v1/asset/reconciliation_handler.go
  - internal/core/core.go
  - internal/services/asset/reconciliation_permission_test.go
  - pkg/middleware/oper_log.go
autonomous: true
requirements:
  - AUDIT-01
  - RECON-07
  - INFRA-04
user_setup: []

must_haves:
  truths:
    - "internal/api/router.go 在 /asset/reconciliation 路径下注册 3 个 Setup*Router 调用(reconciliation + exception + statistics)"
    - "assetReconciliation group 显式挂载 OperLogMiddleware(core.OperLogService, core) 在 RequirePermissions 之前(D-17 全部写操作都走 operlog)"
    - "pkg/middleware/oper_log.go 的 DefaultOperLogConfig().LogPaths 追加 /asset/reconciliation,否则 middleware 不会触发"
    - "RequirePermissions middleware 限定 asset:reconciliation:list / dashboard / export"
    - "operlog module constant ModuleReconciliation 已就位(R1 仅 1 个,D-16) — 通过 grep 静态断言验证"
    - "sys_job 表已有 4 条 R1 cron 记录(由 42-01 seed)"
    - "4 个 ExecuteXxxTask 全局函数可被 InvokeTarget 字符串调用"
    - "应用启动时 core.Init 调用 RefreshView 一次(D-02 避免冷启 0-5min 数据为空)"
    - "跨模块权限边界测试通过(Option A 完整 router spin-up): 无 token → 401;无 asset:reconciliation:list token → 403;有权限 token → 200"
    - "D-18: R1 异常列表不上'标记已解决'按钮(只读);router 不注册 PATCH/POST resolve 路径(只有 LIST/GET 查询路径)"
  artifacts:
    - path: "internal/api/router.go"
      provides: "assetReconciliation group with 3 Setup*Router calls + JWTAuth + OperLogMiddleware + RequirePermissions"
      min_lines: 30
    - path: "internal/api/v1/asset/reconciliation_handler.go"
      provides: "ModuleReconciliation const + WithCore pattern (already in 42-02, verified by static grep)"
      min_lines: 10
    - path: "internal/services/asset/reconciliation_permission_test.go"
      provides: "Test 1: list.length static check; Test 2: 完整 router spin-up 验证 401/403/200 三态"
      min_lines: 150
    - path: "pkg/middleware/oper_log.go"
      provides: "DefaultOperLogConfig.LogPaths 追加 /asset/reconciliation"
      min_lines: 5
  key_links:
    - from: "internal/api/router.go"
      to: "internal/api/v1/asset/reconciliation_router.go"
      via: "asset.SetupReconciliationRouter"
      pattern: "SetupReconciliationRouter"
    - from: "internal/api/router.go"
      to: "internal/api/v1/asset/reconciliation_exception_router.go"
      via: "asset.SetupReconciliationExceptionRouter"
      pattern: "SetupReconciliationExceptionRouter"
    - from: "internal/api/router.go"
      to: "internal/api/v1/asset/reconciliation_statistics_router.go"
      via: "asset.SetupReconciliationStatisticsRouter"
      pattern: "SetupReconciliationStatisticsRouter"
    - from: "internal/api/router.go"
      to: "pkg/middleware/oper_log.go"
      via: "OperLogMiddleware(core.OperLogService, core)"
      pattern: "OperLogMiddleware"
    - from: "internal/core/core.go"
      to: "internal/services/asset/reconciliation_snapshot.go"
      via: "core.Init() calls ExecuteRefreshViewTask once"
      pattern: "ExecuteRefreshViewTask"
---

<objective>
将 42-02 / 42-04 创建的 3 个 Setup*Router 整合到 internal/api/router.go,补齐 JWTAuth + OperLogMiddleware(D-17 全部写操作都走 operlog)+ RequirePermissions 中间件,验证 operlog module 常量就位(D-16),在应用启动时执行一次 RefreshView(D-02 冷启兜底),并补齐跨模块权限边界测试(Option A 完整 router spin-up)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-r1/42-CONTEXT.md
@.planning/phases/42-r1/42-PATTERNS.md
@.planning/notes/260627-reconciliation-reuse-audit.md F2 (路由注册) + F7 (operlog module)
@.planning/notes/260627-cross-module-permission.md (跨模块权限边界)

# Reference implementations (READ FIRST)
@pkg/middleware/oper_log.go (OperLogMiddleware signature + DefaultOperLogConfig.LogPaths)
@internal/api/router.go:664-693 (existing asset registration)
@internal/api/router.go (full file: 查看现有 module 是如何挂中间件的; 找 operations/network/scheduler 中是否有 OperLogMiddleware Use 调用)
@internal/api/v1/scheduler/job_router.go (Setup*Router pattern)
@internal/api/v1/system/ad_account_handler.go (Module constants pattern)
@internal/api/v1/system/ad_account_handler_test.go (参考测试模式:sqlite 内存 DB + mock core 字段)
@internal/core/core.go (init lifecycle, location of RefreshView call)
@internal/utils/operlog/operlog.go (Record function signature)
</context>

<interfaces>
From `internal/api/router.go:664-693` (asset block reference):
```go
assets := ops.Group("/asset")
assets.Use(middleware.RequirePermissions([]string{
    "ops:asset:list",
    "ops:asset:add",
    "ops:asset:edit",
    "ops:asset:delete",
}, core))
{
    assetService := opsServices.NewAssetService(core.DB.GetDB())
    assetHandler := operations.NewAssetHandler(assetService).WithCore(core)
    // ...
}
```

From `pkg/middleware/oper_log.go:64-65` (OperLogMiddleware signature):
```go
func OperLogMiddleware(operLogSvc services.OperLogService, core *core.Core) gin.HandlerFunc {
    config := DefaultOperLogConfig()
    // ...
}
```

**CRITICAL — DefaultOperLogConfig.LogPaths 当前不包含 `/asset/reconciliation`**: Task 1 必须扩展 `DefaultOperLogConfig().LogPaths` 追加 `"/asset/reconciliation"`,否则 OperLogMiddleware 不会记录 R1 写操作。

From `internal/api/v1/asset/reconciliation_handler.go` (created in 42-02):
- Contains `const ModuleReconciliation = "资产对账"`(R1 唯一 module 常量,D-16)

From `internal/services/asset/reconciliation_snapshot.go` (created in 42-02):
- Contains `func ExecuteRefreshViewTask(ctx context.Context) error`

From `internal/utils/operlog/operlog.go`:
- `func Record(c *gin.Context, service OperLogServiceInterface, db *gorm.DB, module string, operType int)` — for R1 cron writes (in 42-02 detection code)
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Register /asset/reconciliation routes in router.go with JWTAuth + OperLogMiddleware + RequirePermissions</name>
  <files>internal/api/router.go, pkg/middleware/oper_log.go</files>
  <read_first>
    - pkg/middleware/oper_log.go (OperLogMiddleware 签名 + DefaultOperLogConfig.LogPaths 当前 17 个 path 列表)
    - internal/api/router.go:660-700 (现有 asset block 完整内容,确认 imports 列表)
    - internal/api/router.go (全文 grep `OperLogMiddleware` 找现有 module 是如何挂的,作为本次挂载参考)
    - internal/api/v1/asset/reconciliation_router.go (created in 42-02)
    - internal/api/v1/asset/reconciliation_exception_router.go (created in 42-02)
    - internal/api/v1/asset/reconciliation_statistics_router.go (created in 42-04)
    - .planning/notes/260627-reconciliation-reuse-audit.md F2 (路由注册 + 冲突规避)
    - .planning/notes/260627-cross-module-permission.md (跨模块权限边界)
  </read_first>
  <behavior>
    - /asset/reconciliation/* routes registered under new top-level assetReconciliation group (NOT inside /ops)
    - JWTAuth middleware (D-17 全部写操作都走 operlog 需先 auth)
    - OperLogMiddleware(core.OperLogService, core) 在 RequirePermissions 之前(按 D-17 R1 全部写操作都走 operlog)
    - DefaultOperLogConfig.LogPaths 追加 /asset/reconciliation(middleware 触发条件)
    - RequirePermissions middleware accepts asset:reconciliation:list / asset:reconciliation:dashboard / asset:reconciliation:export
    - 3 Setup*Router functions called: SetupReconciliationRouter / SetupReconciliationExceptionRouter / SetupReconciliationStatisticsRouter
    - No route conflicts with /ops/asset/* (existing asset block unchanged)
    - No pre-registration of generic /asset/reconciliation/* paths in router.go (memory `Excel 导入路由冲突陷阱`)
  </behavior>
  <action>
    1. Modify internal/api/router.go. After the existing asset block (line 693, end of `assets := ops.Group("/asset")` block), add a NEW top-level group called assetReconciliation. Import path: `import assetHandler "github.com/xingran-next/xingran-go-backend/internal/api/v1/asset"` and `import assetSvc "github.com/xingran-next/xingran-go-backend/internal/services/asset"`. Add code:
       - `assetReconciliation := r.Group("/asset/reconciliation")`
       - `assetReconciliation.Use(middleware.JWTAuth(core.JWTManager))` — 鉴权
       - `assetReconciliation.Use(middleware.OperLogMiddleware(core.OperLogService, core))` — **D-17 全部写操作都走 operlog**,放在 RequirePermissions 之前
       - `assetReconciliation.Use(middleware.RequirePermissions([]string{"asset:reconciliation:list", "asset:reconciliation:dashboard", "asset:reconciliation:export"}, core))` — 权限门
       - Inside `{ }`: call `assetHandler.SetupReconciliationRouter(assetReconciliation, core)` then `assetHandler.SetupReconciliationExceptionRouter(assetReconciliation, core)` then `assetHandler.SetupReconciliationStatisticsRouter(assetReconciliation, core)`. Do NOT pre-register any /asset/reconciliation/* generic routes in router.go itself (per memory `Excel 导入路由冲突陷阱`).
    2. Modify pkg/middleware/oper_log.go. In `DefaultOperLogConfig().LogPaths`, **append** `"/asset/reconciliation"` as the last entry (or alongside other /ops/* paths). Without this, `shouldLogOperation` returns false for /asset/reconciliation/* and middleware becomes no-op. (R1 全部写操作包括 Layer 3 cron 触发 sys_data_reconciliation 写入,但 cron 路径走 `operlog.Record` 直调不依赖 middleware;admin page 暂无写操作,middleware 主要是为 R2 admin CRUD 操作预留 — 即使 R1 admin 写操作暂无,middleware 挂载对 R2/R3 一致性是必须的。)
    3. Verify imports already present in router.go. If not, add: `"github.com/xingran-next/xingran-go-backend/internal/api/v1/asset"` and `"github.com/xingran-next/xingran-go-backend/internal/services/asset"`. Also confirm `middleware` is already imported (it should be, since other groups use it).
    4. Save the file.
  </action>
  <verify>
    <automated>go build ./... 2>&1 | head -20 && grep -n "OperLogMiddleware" internal/api/router.go && grep -n '"/asset/reconciliation"' pkg/middleware/oper_log.go</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... 退出码 0
    - grep -q "OperLogMiddleware" internal/api/router.go 返回 match
    - grep -q '"/asset/reconciliation"' pkg/middleware/oper_log.go 返回 match
    - /asset/reconciliation/* 路径下注册 3 个 Setup*Router 调用
    - RequirePermissions 接受 asset:reconciliation:list / dashboard / export
    - 无 /ops/asset/* 路径冲突
    - 无 router.go 内预注册 /asset/reconciliation/* 通用路径
  </acceptance_criteria>
  <done>
    - 3 个 Setup*Router 整合到 router.go + JWTAuth + OperLogMiddleware + RequirePermissions
    - 权限中间件限定
    - operlog middleware 挂载就位(LogPaths 追加 /asset/reconciliation)
    - go build 通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify operlog module constant + startup RefreshView + permission boundary test (Option A 完整 router spin-up)</name>
  <files>internal/api/v1/asset/reconciliation_handler.go, internal/core/core.go, internal/services/asset/reconciliation_permission_test.go</files>
  <read_first>
    - internal/api/v1/asset/reconciliation_handler.go (created in 42-02, 顶部应已含 ModuleReconciliation const)
    - internal/api/v1/system/ad_account_handler_test.go (sqlite 内存 DB + mock core 字段测试模式)
    - internal/api/v1/system/user_handler_test.go (如存在,看完整 handler 集成测试模式)
    - internal/core/core.go (init lifecycle, location of RefreshView call)
    - internal/services/scheduler/job_service.go:138-148 (sys_job cron registration, no Go code required per D-10)
    - pkg/middleware/permission.go (RequirePermissions 签名,看如何 mock 无权限场景)
    - pkg/middleware/jwt.go (JWTAuth 签名,看如何 mock 无 token 401 场景)
    - .planning/notes/260627-cross-module-permission.md (跨模块权限边界测试)
    - .planning/phases/42-r1/42-CONTEXT.md D-02 (启动 refresh) + D-16 (1 module const)
  </read_first>
  <behavior>
    - **Test 1 (static assertion)**: `grep -n "ModuleReconciliation" internal/api/v1/asset/reconciliation_handler.go` 返回 ≥ 1 match — ModuleReconciliation const 就位(D-16)
    - **Test 2a (no token → 401)**: 完整 router spin-up(`gin.New()` + `SetupReconciliationRouter` + `SetupReconciliationStatisticsRouter` + JWTAuth),无 Authorization header → POST /asset/reconciliation/exception/list 返回 HTTP 401
    - **Test 2b (无 asset:reconciliation:list → 403)**: 注入仅含 `ops:workstation:list` 的 mock user token → POST /asset/reconciliation/statistics/summary 返回 HTTP 403
    - **Test 2c (有 asset:reconciliation:list → 200)**: 注入含 `asset:reconciliation:list` 的 mock user token → POST /asset/reconciliation/statistics/summary 返回 HTTP 200(可空 body)
    - **Test 2d (异常列表)**: 同 Test 2c pattern,POST /asset/reconciliation/exception/list → 200
    - core.Init() (or equivalent lifecycle hook) calls ExecuteRefreshViewTask once on startup (D-02)
    - 4 sys_job records exist in DB (seeded by 42-01 migration_169) but no new Go cron file (D-10)
  </behavior>
  <action>
    1. **Verify ModuleReconciliation const** in `internal/api/v1/asset/reconciliation_handler.go`. The file was created in 42-02 with `const ModuleReconciliation = "资产对账"`. **Static assertion via grep**: the implementation MUST verify with `grep -n "ModuleReconciliation" internal/api/v1/asset/reconciliation_handler.go` returning at least 1 match. If missing, add the const at file top (defensive path expected to be dead code, since 42-02 already created it). Add docstring: D-16 R1 only 1 const; R2 adds ModuleReconciliationExceptionRule / AutoWorkorder / ReportExport.

    2. **Modify internal/core/core.go**. In the `Init()` function (or equivalent startup hook), after migrations run, add a call:
       - `go func() { ctx, cancel := context.WithTimeout(context.Background(), 30*time.Second); defer cancel(); if err := assetSvc.ExecuteRefreshViewTask(ctx); err != nil { applogger.Errorf("Phase 42 R1 startup RefreshView failed: %v", err) } }()`
       - This executes the MV refresh once at startup to avoid 0-5min cold start data gap (D-02).
       - Import `github.com/xingran-next/xingran-go-backend/internal/services/asset` as `assetSvc`.
       - Use `applogger` (existing import) for failure log. No SysNotice per D-02.

    3. **Create `internal/services/asset/reconciliation_permission_test.go`** (Option A 完整 router spin-up). This file contains 2 tests:
       - **Test 1 `TestReconciliationModuleConstExists`**: 静态 grep 断言。`out, _ := exec.Command("grep", "-n", "ModuleReconciliation", "../api/v1/asset/reconciliation_handler.go").Output(); assert.Contains(t, string(out), "ModuleReconciliation")` — 防 D-16 回归。
       - **Test 2 `TestReconciliationEndpoints_PermissionBoundary`**: 完整 router spin-up。步骤:
         a. `gin.SetMode(gin.TestMode); router := gin.New()`
         b. 构造 minimal core mock(只填 DB + JWTManager + OperLogService + PwdManager stub 字段,参考 `internal/api/v1/system/ad_account_handler_test.go` 的 sqlite 内存 DB 模式)
         c. `g := router.Group("/asset/reconciliation"); g.Use(middleware.JWTAuth(core.JWTManager)); g.Use(middleware.RequirePermissions([]string{"asset:reconciliation:list", "asset:reconciliation:dashboard", "asset:reconciliation:export"}, core))`
         d. 调 `asset.SetupReconciliationRouter(g, core)` + `asset.SetupReconciliationStatisticsRouter(g, core)`(注入 minimal core 字段)
         e. **Test 2a**: 无 token,`httptest.NewRecorder() + router.ServeHTTP(rec, req)` 调 POST /asset/reconciliation/exception/list → assert.Equal(t, 401, rec.Code)
         f. **Test 2b**: 构造 mock user 仅含 `ops:workstation:list` 权限(无 `asset:reconciliation:list`),发 POST /asset/reconciliation/statistics/summary → assert.Equal(t, 403, rec.Code)
         g. **Test 2c**: 构造 mock user 含 `asset:reconciliation:list` 权限,发 POST /asset/reconciliation/statistics/summary → assert.Equal(t, 200, rec.Code)
         h. **Test 2d**: 同 2c,发 POST /asset/reconciliation/exception/list → assert.Equal(t, 200, rec.Code)
         i. 用 `httptest.NewRequest("POST", path, body)` + 注入 Authorization header(Bearer token stub)+ ctx.Set("user", mockUser) 模拟已 auth 状态
         j. 若 RequirePermissions 实现需要查 DB 校验权限,需 seed `sys_user` + `sys_role` + `sys_role_menu` 关联(参考 migration_165 button permission seed 模式)
       - **Test 3 `TestReconciliationStatistics_NoListLength`** (从 Task 2 移到此处,作为防 stat-cards-from-list-length-capped-at-100 回归): 静态检查 6 Statistics 方法不出现 `Find(` / `.Offset(` 关键字,使用 `parser.ParseFile` 读取 `reconciliation_statistics.go` AST

    4. **Run go test ./...** to verify all tests pass (including 42-02 / 42-03 / 42-04 unit tests + 42-06 permission tests).
  </action>
  <verify>
    <automated>go test ./... 2>&1 | tail -30 && echo "---" && grep -n "ModuleReconciliation" internal/api/v1/asset/reconciliation_handler.go
</automated>
  </verify>
  <acceptance_criteria>
    - go test ./... 全部 PASS(累计 42-02 5 + 42-03 0 + 42-04 7 + 42-06 2 = 14 个测试;42-03 改为 skeleton 0 测试)
    - `grep -n "ModuleReconciliation" internal/api/v1/asset/reconciliation_handler.go` 返回 ≥ 1 match
    - ModuleReconciliation = "资产对账" const 存在(42-02 已创建,42-06 静态断言防回归)
    - core.Init 调用 ExecuteRefreshViewTask
    - 4 个 sys_job records 存在(由 42-01 seed)
    - Test 2a: 无 token → HTTP 401
    - Test 2b: 仅 ops:workstation:list → HTTP 403
    - Test 2c: 含 asset:reconciliation:list → HTTP 200
    - Test 2d: 异常列表 + 有权限 → HTTP 200
    - Test 3: 6 个 Statistics 方法体不出现 `Find(` / `.Offset(`
  </acceptance_criteria>
  <done>
    - operlog module constant 就位(静态断言防回归)
    - 启动时 RefreshView 一次
    - 跨模块权限测试通过(Option A 完整 router spin-up,401/403/200 三态)
    - 全套测试通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /asset/reconciliation/* | JWTAuth + OperLogMiddleware + RequirePermissions 三层中间件 |
| core.Init() → DB | Startup hook calls ExecuteRefreshViewTask with 30s timeout |
| Test → Permission middleware | Boundary test verifies 401 (no token) / 403 (no perm) / 200 (with perm) path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-19 | Elevation of Privilege | Missing RequirePermissions on /asset/reconciliation/* | mitigate | Task 1 explicitly adds middleware.RequirePermissions with R1 perms; boundary test verifies 403 |
| T-42-20 | Denial of Service | Startup RefreshView blocks boot | mitigate | Wrapped in goroutine with 30s context timeout; failure logs only (no panic) |
| T-42-21 | Information Disclosure | Stats endpoints expose counts to non-privileged | mitigate | Same RequirePermissions gate as exception list; 403 if no asset:reconciliation:list |
| T-42-22 | Tampering | sys_job InvokeTarget strings | mitigate | D-10 strategy: cron in sys_job table; UI operators can modify cron without code changes; InvokeTarget is constant string |
| T-42-23 | Repudiation | Missing OperLogMiddleware on write endpoints | mitigate | Task 1 explicitly adds OperLogMiddleware BEFORE RequirePermissions (D-17); DefaultOperLogConfig.LogPaths 追加 /asset/reconciliation |
| T-42-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in this plan |
</threat_model>

<verification>
1. go build ./... 退出码 0
2. go test ./... 全部 PASS(14 个测试)
3. /asset/reconciliation/* 路径下注册 3 个 Setup*Router 调用
4. `grep -n "OperLogMiddleware" internal/api/router.go` 返回 match
5. `grep -n '"/asset/reconciliation"' pkg/middleware/oper_log.go` 返回 match
6. RequirePermissions 接受 asset:reconciliation:list / dashboard / export
7. 无 /ops/asset/* 路径冲突
8. 无 router.go 内预注册 /asset/reconciliation/* 通用路径
9. `grep -n "ModuleReconciliation" internal/api/v1/asset/reconciliation_handler.go` 返回 ≥ 1 match
10. core.Init 调用 ExecuteRefreshViewTask
11. 4 个 sys_job records 存在(由 42-01 seed)
12. permission boundary test 验证 401 / 403 / 200 三态
</verification>

<success_criteria>
- /asset/reconciliation/* 路由全部就位(3 个 Setup*Router)
- JWTAuth + OperLogMiddleware + RequirePermissions 三层中间件挂载
- DefaultOperLogConfig.LogPaths 追加 /asset/reconciliation
- operlog module constant 就位(D-16,静态断言防回归)
- 应用启动时 RefreshView 一次(D-02)
- sys_job 表已有 4 条 R1 cron 记录
- 跨模块权限边界测试通过(Option A:401/403/200 三态)
- Test 3 list.length 反模式静态守护
- go build + go test ./... 全部通过
</success_criteria>

<output>
Create `.planning/phases/42-r1/42-06-SUMMARY.md` when done
</output>
