---
phase: 42-r1
plan: 03
type: execute
wave: 2
depends_on:
  - 42-01
  - 42-02
files_modified:
  - internal/services/asset/reconciliation_exception.go
autonomous: true
requirements:
  - RECON-03
user_setup: []

must_haves:
  truths:
    - "ReconciliationExceptionService interface 就位,含 List 与 GetByID 方法(skeleton,R1 不实现真实逻辑)"
    - "List 与 GetByID 返回 NotImplemented error 或 empty slice(不查 DB,避免 42-02 已有 simple List 重复)"
    - "sys_reconciliation_exception 表已在 42-01 创建(R1 success criteria 1 基础)"
    - "CacheKeyReconciliationExceptionRuleList/ByID 常量已在 42-01 定义"
    - "R3 实现 IP/CIDR 匹配引擎 + CRUD + UI 时,本 skeleton 即可被替换"
    - "D-12: R1 不做 HealthScore 函数(0-100 健康度总分),仅保留 skeleton exception 接口为 R3 准备"
  artifacts:
    - path: "internal/services/asset/reconciliation_exception.go"
      provides: "ReconciliationExceptionService skeleton interface + stub impl (R1 placeholder, R3 implements)"
      min_lines: 60
  key_links:
    - from: "internal/services/asset/reconciliation_exception.go"
      to: "internal/models/reconciliation.go (SysReconciliationException)"
      via: "interface reference only, no runtime query in R1"
      pattern: "ReconciliationExceptionService"
---

<objective>
为 R3 (Phase 44) IP/CIDR 例外规则引擎预留 skeleton interface + skeleton service 实现(空方法):R1 不实现 match/CRUD/UI。sys_reconciliation_exception 表与 cache key 常量由 42-01 提供,本 plan 仅就位 service interface 骨架供 42-02 路由调用。

依据 CONTEXT.md 显式边界:
- R1 写操作仅限 migration 初始化 + Cron 触发的 Layer 3 检测写入 sys_data_reconciliation,不上"标记已解决" UI
- R3 才实现 IP/CIDR 例外规则引擎(GiST 索引 + CRUD admin 页 + 命中测试工具)
- R1 必须确保例外规则 service 接口位置已就位,避免 42-02 路由 import 失败,但**实现空方法**(R3 再补)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/42-r1/42-CONTEXT.md
@.planning/phases/42-r1/42-PATTERNS.md
@.planning/notes/asset-reconciliation-strategy.md §4 (R3 例外规则体系 deferred)
@.planning/notes/260627-reconciliation-reuse-audit.md F1 (cache_keys) + 4.5 (HealthScore)

# Reference implementations
@internal/services/operations/location_alias_service.go (service pattern)
@internal/models/reconciliation.go (created in 42-01, SysReconciliationException model)
@.planning/notes/260627-reconciliation-reuse-audit.md §3.4 (P4 operlog module constant)
</context>

<interfaces>
From `internal/models/reconciliation.go` (created in 42-01):
```go
type SysReconciliationException struct {
    BaseModel
    Name             string
    IPRange          string  // type:cidr
    ConflictTypes    pq.StringArray
    ExceptionActions pq.StringArray
    SeverityOverride *string
    ScopeType        string  // 'global' / 'building' / 'floor'
    ScopeID          *string
    Reason           string
    IsActive         int     // 0=启用 1=停用
    ExpiresAt        *time.Time
    CreatedBy        int64
}
```

From `internal/services/asset/cache_keys.go` (created in 42-01):
- `CacheKeyReconciliationExceptionRuleList = "reconciliation:exceptionRule:list"`
- `GetReconciliationExceptionRuleListKey()`
- `CacheKeyReconciliationExceptionRuleByID = "reconciliation:exceptionRule:byID:%s"`
- `GetReconciliationExceptionRuleByIDKey(id string)`
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ReconciliationExceptionService skeleton (R3 foundation only)</name>
  <files>internal/services/asset/reconciliation_exception.go</files>
  <read_first>
    - internal/services/operations/location_alias_service.go (interface + impl + constructor pattern)
    - internal/models/reconciliation.go (SysReconciliationException fields from 42-01)
    - .planning/phases/42-r1/42-CONTEXT.md domain 段(R1 不做 CIDR 例外匹配,推到 R3)
    - .planning/phases/42-r1/42-CONTEXT.md deferred 段(R3 = Phase 44 才实现 IP/CIDR + CRUD admin)
  </read_first>
  <behavior>
    - ReconciliationExceptionService interface declares List / GetByID methods (signatures stable for R3 to fill)
    - reconciliationExceptionServiceImpl.List() returns empty PageResult + nil error (skeleton, not NotImplemented — silent no-op for R1 admin UI)
    - reconciliationExceptionServiceImpl.GetByID() returns nil + ErrNotImplemented (admin page will hide access to R1; R3 will replace impl)
    - No DB query in R1 (R3 will add CIDR matching + scope filters)
  </behavior>
  <action>
    1. 修改 `internal/services/asset/reconciliation_exception.go`(本 plan 之前已由 42-02 Task 1 创建 `List/GetByID` 的 simple impl,本 plan 改为 skeleton 形式):
       - `package asset`
       - import: context, errors, gorm.io/gorm, internal/models
       - 顶部常量: `var ErrReconciliationExceptionNotImplemented = errors.New("reconciliation exception service: R1 skeleton, R3 implements")`
       - 定义 `type ReconciliationExceptionService interface { List(ctx context.Context, params ExceptionListParams) (*PageResult, error); GetByID(ctx context.Context, id string) (*models.SysReconciliationException, error) }`
       - 定义 `reconciliationExceptionServiceImpl struct { db *gorm.DB }` + `NewReconciliationExceptionService(db *gorm.DB) ReconciliationExceptionService`
       - 实现 `List`:
         - **skeleton 行为**: 直接返回 `&PageResult{List: []models.SysReconciliationException{}, Total: 0, Current: 1, PageSize: 0}, nil` — 不查 DB,不让 R1 admin UI 显示 seed 数据(避免与 R3 真实数据冲突)
         - 顶部注释 `// R1 skeleton: 不查询 DB,返回空集。R3 (Phase 44) 替换为真实 CIDR 匹配查询。`
       - 实现 `GetByID`:
         - **skeleton 行为**: 返回 `nil, ErrReconciliationExceptionNotImplemented` — admin page 调用会显示 404/未实现
         - 顶部注释 `// R1 skeleton: 始终返回 NotImplemented。R3 替换为 First where id=? AND deleted_at IS NULL。`
       - 顶部文件注释 (大段,作为 R1/R3 边界契约):
         ```
         // ReconciliationExceptionService — R1 SKELETON (R3 implementation deferral)
         //
         // R1 (本 phase) 不实现 IP/CIDR 例外规则匹配引擎。依据 42-CONTEXT.md:
         //   - domain 段: "R1 不做 CIDR 例外匹配(IP 段规则是 R3 范围)"
         //   - deferred 段: "R3(Phase 44) — IP 段例外规则引擎(CIDR GiST 索引)
         //                   + 例外规则 CRUD admin 页 + 命中测试工具 + Excel 导入导出"
         //
         // sys_reconciliation_exception 表与 cache key 常量由 42-01 提供,
         // 本文件仅就位 service interface 骨架,所有方法返回空集/NotImplemented。
         // R3 实现时:
         //   1) List: SELECT ... WHERE deleted_at IS NULL AND is_active = 0
         //            AND (expires_at IS NULL OR expires_at > now())
         //            AND conflict_types @> ARRAY['all'] OR ? = ANY(conflict_types)
         //   2) GetByID: First where id=? AND deleted_at IS NULL
         //   3) 新增 Create / Update / Delete / Enable / Disable / TestCIDRMatch 方法
         ```
  </action>
  <verify>
    <automated>go build ./internal/services/asset/... 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/services/asset/...` 退出码 0
    - `ReconciliationExceptionService` 接口包含 `List` 与 `GetByID` 两个方法
    - `List` 方法体不包含 `db.` 或 `s.db.` 关键字(纯 skeleton,无 DB 查询)
    - `GetByID` 方法体返回 `nil, ErrReconciliationExceptionNotImplemented`
    - `ErrReconciliationExceptionNotImplemented` 常量存在
    - 构造函数 `NewReconciliationExceptionService(db)` 返回接口
    - 代码无 `MatchExceptionRules` / `<<=` / `cidr` / `MergeActions` 字样(R1 边界)
    - 顶部文件注释明确声明 R1 skeleton + R3 实现范围
  </acceptance_criteria>
  <done>
    - ReconciliationExceptionService skeleton interface 就位
    - List 返回空集 + GetByID 返回 NotImplemented
    - 42-02 路由可正常 import 并调用
    - R3 实现替换时不需要改 interface 签名(只换 impl)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → DB | R1 skeleton does NOT query DB; R3 will introduce read-only query path |
| Caller → Service | Caller is admin UI (R3 will gate; R1 has no UI consumption) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-09 | Denial of Service | Match query with many rules | accept | R1 skeleton has no query; R3 admin CRUD may add more but index on is_active+expires_at mitigates; not a P0 threat for R1 |
| T-42-10 | Information Disclosure | R1 skeleton returns empty/NotImplemented | accept | R1 admin UI not built (no exposure); R3 CRUD will use operlog + RequirePermissions |
| T-42-11 | Repudiation | R1 skeleton has no write operations | accept | R3 admin CRUD will use operlog.Record; R1 skeleton is read-only stub |
| T-42-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in this plan |
</threat_model>

<verification>
1. `go build ./internal/services/asset/...` 退出码 0
2. `ReconciliationExceptionService` 接口可被编译
3. List 返回空 PageResult,GetByID 返回 ErrReconciliationExceptionNotImplemented
4. 代码无 CIDR / Match / Merge 字样(R1 边界)
5. 顶部文件注释明确 R1/R3 范围
</verification>

<success_criteria>
- ReconciliationExceptionService skeleton interface 就位
- R1 边界明确:无 CIDR 匹配、无 CRUD、无 admin UI
- 42-02 路由调用可正常 import
- R3 实现接口契约稳定(无需改 interface 签名)
</success_criteria>

<output>
Create `.planning/phases/42-r1/42-03-SUMMARY.md` when done
</output>
