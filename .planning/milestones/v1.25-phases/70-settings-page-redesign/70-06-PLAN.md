---
phase: 70-settings-page-redesign
plan: 06
type: execute
wave: 3
depends_on: ["70-02"]
files_modified:
  - xingran-react-frontend/src/pages/system/settings/index.tsx
  - xingran-react-frontend/src/pages/system/settings-page/index.tsx   # 删除
  - internal/core/db/migrations/migration_208_update_settings_menu_component.go      # 新建（编号以执行时实际 max+1 为准）
  - internal/core/db/migrations/migration_208_update_settings_menu_component_test.go # 新建
  - internal/core/db/database.go
  - internal/core/core.go
autonomous: true
requirements: [D-11, D-01]
must_haves:
  truths:
    - "D-11 落地：三个 settings 目录合并为单一 xingran-react-frontend/src/pages/system/settings/（入口 index.tsx 由 barrel 改写为 SettingsShell 系统设置实例），src/pages/system/settings-page/ 目录删除，sys_menu 系统设置记录（id 308d89be-e516-4556-b949-bc22bf6ab759）component 字段经数据迁移从 system/settings-page/index 更新为 system/settings/index（path 字段不动，路由 URL 保持 /system/settings-page）"
    - "菜单缓存失效闭环（Pitfall 2）：迁移只改 DB 不失效缓存会导致 Redis 30 分钟旧 component 路径白屏——本 plan 按 PATTERNS 事实 1 的时序约束实现（迁移函数在 cache 创建前执行拿不到 cache 实例），采用 changed 标志方案：Migrate208 返回 changed，core.go 在 c.DataCacheService 就绪后按标志失效 6 个 menu: key 前缀"
    - "D-01 落地（系统设置实例）：系统设置页入口为 SettingsShell 实例——email(默认)/api/captcha 三分类（cat 值从 captcha-background 收敛为 captcha），URL 形如 /system/settings-page?cat=email"
  artifacts:
    - path: xingran-react-frontend/src/pages/system/settings/index.tsx
      provides: "系统设置页入口（SettingsShell 系统实例）"
      exports: ["default（页面组件）", "systemSettingsCategories"]
      min_lines: 40
    - path: internal/core/db/migrations/migration_208_update_settings_menu_component.go
      provides: "sys_menu component 数据修正迁移（幂等 UPDATE + changed 返回）"
      exports: ["Migrate208UpdateSettingsMenuComponent"]
      min_lines: 25
    - path: internal/core/db/migrations/migration_208_update_settings_menu_component_test.go
      provides: "迁移幂等 + 双条件守护 Go 单测"
      min_lines: 40
  key_links:
    - from: internal/core/db/database.go
      to: internal/core/db/migrations/migration_208_update_settings_menu_component.go
      via: "PG advisory-lock 分支 + sqlite else 分支双调用（207 挂法 :837-847 同款），changed 写入 Database 结构体字段"
      pattern: "Migrate208|SettingsMenuComponentChanged"
    - from: internal/core/core.go
      to: internal/services/system InvalidateCacheByPattern
      via: "initCacheAndWarmUp 内 DataCacheService 就绪后（:357 之后），按 SettingsMenuComponentChanged 标志失效 menu keys（system.NewCacheProvider(c.DataCacheService) 桥接）"
      pattern: "InvalidateCacheByPattern|CacheKeyMenu"
    - from: xingran-react-frontend/src/pages/system/settings/index.tsx
      to: xingran-react-frontend/src/router/componentLoader.tsx glob
      via: "入口必须命名 index.tsx（glob 模式 /src/pages/**/{index,detail}.tsx）；DB component 值不带 pages/ 前缀不带 .tsx"
      pattern: "SettingsShell|systemSettingsCategories"
---

<objective>
完成 D-11 端到端闭环：前端三目录合并为 system/settings/（新入口 + 删除 settings-page/），后端 Migrate208 数据迁移更新 sys_menu component 路径，并按正确时序失效 Redis 菜单缓存（防 30 分钟旧路径白屏）。

Purpose: 目录合并是纯前端边界内最后一步结构变化；没有 sys_menu 数据迁移则合并即白屏，没有缓存失效则白屏持续 30 分钟。
Output: 新入口、迁移文件 + 测试、database.go/core.go 注册与失效逻辑。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-settings-page-redesign/70-PATTERNS.md
@.planning/phases/70-settings-page-redesign/70-RESEARCH.md

<interfaces>
<!-- 执行者需要的既有契约（PATTERNS/实测） -->

sys_menu 权威记录（menu_catalog_seed.sql）：
- 系统设置：id = 308d89be-e516-4556-b949-bc22bf6ab759，component = "system/settings-page/index"（迁移目标 "system/settings/index"），path = "settings-page" 不动
- 用户设置：id = c480cc01-...，component = "settings/index" 不变（目录名不变，无迁移）

缓存失效（已实测核对本 planner session）：
- internal/services/system/cache_utils.go:64 — func InvalidateCacheByPattern(ctx context.Context, cache CacheProvider, patterns []string, module string)
- internal/services/system/cache_keys.go:99-103 — CacheKeyMenuTree="menu:tree" / CacheKeyMenuRouter="menu:router" / CacheKeyMenuAll="menu:all" / CacheKeyMenuUserMenus="menu:user:menus" / CacheKeyMenuUserAllMenus="menu:user:all" / CacheKeyMenuUserPermissions="menu:user:perms"
- 6 个 pattern 形态（menu_cache_impl.go:161-171 InvalidateMenuCache 同款）：前三个 + "*"，三个 user-scoped + ":*"
- core.go：迁移经 db.NewDatabase（initDBAndData，:206/:258）执行，c.Cache :343、c.DataCacheService :356 才创建——迁移函数内拿不到 cache
- 桥接：system.NewCacheProvider(c.DataCacheService)（core.go :882/:940 既有用法）

SettingsShell 契约（70-02 交付）：SettingsCategory { key, label, icon, content, maxWidth? }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 系统设置入口改写与 settings-page 目录删除</name>
  <files>xingran-react-frontend/src/pages/system/settings/index.tsx, xingran-react-frontend/src/pages/system/settings-page/index.tsx（删除）</files>
  <read_first>
  - xingran-react-frontend/src/pages/system/settings-page/index.tsx（现壳全文——分类注册表迁移源 :23-54 + usePersistedStateController 段 :16-21，两段均被替换/删除）
  - xingran-react-frontend/src/pages/system/settings/index.tsx（现 barrel 3 行——改写对象）
  - xingran-react-frontend/src/pages/settings/index.tsx（70-05 产物——同构对照：userSettingsCategories 组织方式）
  - xingran-react-frontend/src/router/componentLoader.tsx:34-42（glob 模式硬约束）
  - .planning/phases/70-settings-page-redesign/70-UI-SPEC.md L-1 分类注册表
  </read_first>
  <action>
  1. 把 `src/pages/system/settings/index.tsx` 从 barrel 改写为页面入口：模块级 `export const systemSettingsCategories: SettingsCategory[]`——email（MailOutlined「邮箱配置」，默认，无 maxWidth 表格撑满）/ api（ApiOutlined「API配置」，无 maxWidth）/ captcha（PictureOutlined「验证码背景图」，无 maxWidth 网格墙撑满；**cat 值收敛为 "captcha"**，不再是 "captcha-background"）。content 分别渲染 `<EmailConfigPage />` / `<APIConfigPage />` / `<CaptchaBackgroundSettingsPage />`（从 ./email-config、./api-config、./captcha-background 默认导入）。default export 渲染 `<SettingsShell categories={systemSettingsCategories} defaultCat="email" />`。入口文件必须保持命名 index.tsx（componentLoader glob 硬约束，Pitfall 4）。
  2. 删除 `src/pages/system/settings-page/` 整个目录（Tabs 壳 + usePersistedStateController activeTab 持久化随目录消失，D-12 第 1 项的调用点清理在此完成；hook 本体保留——user 页 selectedDeptId 等仍在用）。
  3. grep 全 src 确认 `"system/settings-page"` 与 `settings-page/index` 的 import 零引用残留。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npm run type-check && test ! -d src/pages/system/settings-page && grep -rn "system/settings-page" src/ --include="*.tsx" --include="*.ts" | grep -v "__tests__" | wc -l == 0 且 grep -c "systemSettingsCategories\|SettingsShell" src/pages/system/settings/index.tsx >= 2</automated>
  </verify>
  <acceptance_criteria>
  - `src/pages/system/settings-page/` 目录不存在；`src/pages/system/settings/index.tsx` 含 default export 与 systemSettingsCategories 导出
  - src 内对 system/settings-page 的 import 引用为零（测试文件对旧路径的字符串断言除外，如无可为 0）
  - 分类 cat 值为 email/api/captcha（无 captcha-background）
  - type-check 通过（barrel 旧消费方已随 settings-page 删除而消失）
  </acceptance_criteria>
  <done>目录合并前端侧完成，旧 Tabs 壳与 persisted activeTab 调用点清零</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate208 数据迁移 + 双分支注册 + 菜单缓存失效</name>
  <files>internal/core/db/migrations/migration_208_update_settings_menu_component.go, internal/core/db/migrations/migration_208_update_settings_menu_component_test.go, internal/core/db/database.go, internal/core/core.go</files>
  <read_first>
  - internal/core/db/migrations/migration_207_menu_catalog_seed.go + migration_207_menu_catalog_seed_test.go（幂等/断言/双方言/内存 sqlite 夹具范本）
  - internal/core/db/migrations/migration_205_rpa_worker_id_default.go:31-51（log + 错误包装 + applogger 风格）
  - internal/core/db/database.go:803-855（迁移注册区 + 207 双分支挂法 :837-847）与 Database 结构体定义
  - internal/core/core.go:338-360（initCacheAndWarmUp：c.Cache :343 / DataCacheService :356 / SetCacheConfig :357）
  - internal/services/system/cache_utils.go:64 与 cache_keys.go:95-110 与 menu_cache_impl.go:158-175（失效工具与 6 pattern）
  - .planning/phases/70-settings-page-redesign/70-PATTERNS.md「migration_208」节（缓存失效时序事实与方案 A/B）
  </read_first>
  <behavior>
  - Go 用例 1（幂等）：预插 Menu{ID: "308d89be-e516-4556-b949-bc22bf6ab759", Component: "system/settings-page/index"} → 跑迁移 → component == "system/settings/index" 且 changed == true → 再跑一遍 → 值不变且 changed == false
  - Go 用例 2（双条件守护）：component 为其他值/无该 id 的库 → 跑迁移 → 值未被误改、changed == false
  </behavior>
  <action>
  1. **编号裁定**：`ls internal/core/db/migrations/` 取实际最大编号 +1（本 plan 撰写时实测 max=207 → 预期 208；**若 Phase 69 先落地占用了 208，顺延 209 并同步改文件名/函数名/测试名/注册点**——Phase 69 的 PATTERNS 同样规划了 208 编号，以执行时目录状态为准）。
  2. **迁移文件** `migration_208_update_settings_menu_component.go`：`func Migrate208UpdateSettingsMenuComponent(db *gorm.DB) (bool, error)`——`db.Model(&models.Menu{}).Where("id = ?", "308d89be-e516-4556-b949-bc22bf6ab759").Where("component = ?", "system/settings-page/index").Update("component", "system/settings/index")`；err 包装 `fmt.Errorf("migration 208: %w", err)`；`changed := res.RowsAffected > 0`；changed 时 `applogger.Infof("[迁移] 208 系统设置菜单组件路径已更新 (rows=%d)", res.RowsAffected)`。开头 log.Println 与 205 风格一致。**UPDATE 双 id+旧值双条件 = 天然幂等，无 PG 专有语法，不写方言分支**。
  3. **注册** database.go：Database 结构体加导出字段 `SettingsMenuComponentChanged bool`；PG advisory-lock 分支（207 调用之后）与 sqlite else 分支（207 之后）各调用一次：`changed, err := migrations.Migrate208UpdateSettingsMenuComponent(d.DB)`，err → `applogger.Errorf("... (非阻断,留待下次启动): %v", err)`（207 同款），changed → `d.SettingsMenuComponentChanged = true`。
  4. **缓存失效（时序正确版，PATTERNS 事实 1）**：core.go `initCacheAndWarmUp` 内，`c.DataCacheService.SetCacheConfig(...)`（:357）之后、缓存预热之前：`if c.DB.SettingsMenuComponentChanged { system.InvalidateCacheByPattern(context.Background(), system.NewCacheProvider(c.DataCacheService), []string{ CacheKeyMenuTree + "*", CacheKeyMenuRouter + "*", CacheKeyMenuAll + "*", CacheKeyMenuUserMenus + ":*", CacheKeyMenuUserAllMenus + ":*", CacheKeyMenuUserPermissions + ":*" }, "MENU"); applogger.Infof("[迁移] 208 菜单缓存已失效 (component 路径变更)") }`。常量从 internal/services/system 导入（core.go 已 import 该包，:882/:940 既有用法）。**禁止**在迁移函数内做任何 cache 操作（cache 在迁移执行时尚未创建——RESEARCH A4 的「迁移函数内 DeleteByPattern」不可行，已被 PATTERNS 推翻）。
  5. **Go 测试** `migration_208_update_settings_menu_component_test.go`：对照 207 test 的内存 sqlite 夹具（`file:` + `mode=memory&cache=shared` + glebarez/sqlite），AutoMigrate 最小表集只留 `&models.Menu{}`；两个用例按 behavior 块编写。
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/core/db/migrations/ -run TestMigrate208 -v && grep -c "SettingsMenuComponentChanged" internal/core/db/database.go internal/core/core.go 且 grep -c "InvalidateCacheByPattern" internal/core/core.go == 1</automated>
  </verify>
  <acceptance_criteria>
  - `go test ./internal/core/db/migrations/ -run TestMigrate208` 两个用例全绿（幂等 + 双条件守护）
  - database.go PG 与 sqlite 两个分支均注册 Migrate208，错误处理为非阻断 applogger.Errorf（207 同款）
  - core.go 的失效调用位于 DataCacheService 就绪之后、且受 SettingsMenuComponentChanged 标志守卫（grep 双文件各命中字段）
  - 迁移只 UPDATE component 字段，不触碰 perms/visible/path（安全边界 T-70-01）
  - `go build ./...` 通过
  </acceptance_criteria>
  <done>sys_menu component 迁移 + 双分支注册 + 缓存失效时序闭环（DB 改 → cache 就绪 → 按标志失效）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sys_menu.component → componentLoader glob | DB 字段值决定前端懒加载组件路径（既有 componentLoader 白名单校验，componentLoader.tsx:18-27） |
| 迁移 → Redis 菜单缓存 | DB 与缓存的一致性窗口（失效前缓存返回旧值） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-70-01 | Tampering | sys_menu.component 路径注入 | mitigate | 迁移只写固定常量值（id + 旧值双条件 WHERE，不接受任何用户输入）；componentLoader 白名单与危险模式校验既有，不改 |
| T-70-0601 | Tampering | 迁移误改权限语义 | mitigate | UPDATE 仅 component 字段，perms/visible/path 零触碰（Go 用例 2 锁定误改守护）；错误非阻断不中断启动 |
| T-70-0602 | DoS | 缓存失效缺失 → 白屏 30min | mitigate | changed 标志 + core.go 就绪后失效 6 个 menu key 前缀（Pitfall 2 闭环；accept：失效窗口内首次请求触发懒重建，成本可忽略） |
| T-70-SC | Tampering | 包安装 | accept | 零新增依赖，legitimacy gate N/A |
</threat_model>

<verification>
- `go build ./...` + `go test ./internal/core/db/migrations/ -run TestMigrate208 -v`
- `npm run type-check`（Task 1 后）
- 运行时验证（开发库 sqlite）：启动后端确认日志出现 Migrate208 执行/菜单缓存失效行 → 前端登录后侧边栏「系统设置」点击不白屏、URL 为 /system/settings-page 且渲染 SettingsShell（归档到 70-07 截图 checkpoint 一并确认）
</verification>

<success_criteria>
- 前端：settings-page 目录消失，system/settings/index.tsx 为 SettingsShell 系统实例
- 后端：迁移幂等可重跑、双方言注册、changed 标志驱动缓存失效
- 路由 URL /system/settings-page 不变（path 字段未动）
</success_criteria>

<output>
Create `.planning/phases/70-settings-page-redesign/70-06-SUMMARY.md` when done
</output>
