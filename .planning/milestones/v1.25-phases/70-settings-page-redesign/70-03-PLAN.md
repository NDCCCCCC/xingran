---
phase: 70-settings-page-redesign
plan: 03
type: execute
wave: 2
depends_on: ["70-01"]
files_modified:
  - xingran-react-frontend/src/pages/system/settings/email-config.tsx
  - xingran-react-frontend/src/pages/system/settings/api-config.tsx
autonomous: true
requirements: [D-07, D-09, D-12]
must_haves:
  truths:
    - "D-07 落地：邮箱配置与 API 配置两个分类页完整对齐 v16 用户管理范式——顶部统计卡行（总配置数/启用/停用 3 卡）+ 品牌工具栏（搜索/状态筛选/深绿主按钮「新增配置」）+ 双层纸感表格（.xr-table-zebra 绿灰表头、斑马纹），统计计数经 list 的 status 筛选轻请求获得（零后端改动）"
    - "D-09 落地：全部配置编辑/测试 Modal 容器结构与宽度不变（email 700 / api 800），api Modal 内 headers/template/auth 三段 Tabs 原样保留（仅页面级分类 Tabs 被 SettingsShell 替代），品牌化仅靠既有 AntdThemeBridge 全局令牌"
    - "D-12 落地（本 plan 分量）：email/api 页内 Antd preset Tag（blue/green/orange/cyan/purple）全部迁移为 xr-tag / xr-tag-green / xr-tag-gold 品牌圆点标签"
  artifacts:
    - path: xingran-react-frontend/src/pages/system/settings/email-config.tsx
      provides: "邮箱配置分类页（v16 三段式）"
      min_lines: 380
    - path: xingran-react-frontend/src/pages/system/settings/api-config.tsx
      provides: "API 通知配置分类页（v16 三段式 + configType 筛选）"
      min_lines: 420
  key_links:
    - from: xingran-react-frontend/src/pages/system/settings/email-config.tsx
      to: "@/lib/notificationConfigApi getEmailConfigList({status})"
      via: "统计卡轻请求：pageSize=1 + status=0/1 各一次取 total（EmailConfigListParams.status 已存在）"
      pattern: "status: 0|status: 1|pageSize: 1"
    - from: xingran-react-frontend/src/pages/system/settings/email-config.tsx
      to: src/index.css v16 类
      via: "className 消费 .stat-cards/.stat-card/.sc-green/.sc-gray + .xr-table-zebra + .xr-row-ops"
      pattern: "stat-cards|xr-table-zebra|xr-row-ops"
    - from: xingran-react-frontend/src/pages/system/settings/api-config.tsx
      to: api Modal 内 Tabs（headers/template/auth）
      via: "保留段——diff 中不得出现 Modal 内 Tabs 删除"
      pattern: "TabPane|items=\\{\\["
---

<objective>
把邮箱配置与 API 通知配置两个分类子页内部改造为 v16 用户管理三段式（统计卡行 + 工具栏卡 + 双层纸感表格），完成 Tag 品牌迁移、删除确认升级与 email 双请求收敛；Modal 容器保持不动（D-09）。

Purpose: D-07 的主体实现。两页是 SettingsShell 右侧内容区中仅有的表格语汇分类，改造后与 v16 用户管理页视觉一致。
Output: email-config.tsx 与 api-config.tsx 的 v16 化重写。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-settings-page-redesign/70-PATTERNS.md
@.planning/phases/70-settings-page-redesign/70-UI-SPEC.md

<interfaces>
<!-- 执行者需要的既有契约（来自 PATTERNS.md 实测） -->

src/lib/notificationConfigApi.ts:
```typescript
// EmailConfigListParams 已含 status?: number（:21-26）
// APINotificationConfigListParams 已含 status?: number（:160 附近）
 getEmailConfigList / createEmailConfig / updateEmailConfig / deleteEmailConfig / testEmailConfig
 getAPINotificationConfigList / createAPINotificationConfig / updateAPINotificationConfig / deleteAPINotificationConfig
```

src/index.css v16 类（消费不修改）:
.stat-cards / .stat-card / .sc-green / .sc-gray / .xr-table-zebra /
.xr-cell-id / .xr-cell-time / .xr-row-ops / .xr-op-danger /
.xr-tag / .xr-tag-green / .xr-tag-gold

src/pages/system/user/index.tsx:161-167（xr-tag 消费先例）、:531-552（统计卡）、:568-646（工具栏）、:649-660（表格卡）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: email-config.tsx v16 三段式改造</name>
  <files>xingran-react-frontend/src/pages/system/settings/email-config.tsx</files>
  <read_first>
  - xingran-react-frontend/src/pages/system/settings/email-config.tsx（全文——改造对象；重点保留段 :107-113 密码脱敏、:278-309 双请求收敛点、:314-421 双 Modal、:125-129 错误处理惯例）
  - xingran-react-frontend/src/pages/system/user/index.tsx:531-660（v16 三段式范本，直接对照改写）+ :161-167（xr-tag 先例）+ :198/:79/:208-231（.xr-cell-time/.xr-cell-id/.xr-row-ops 先例）
  - xingran-react-frontend/src/lib/notificationConfigApi.ts:21-26（EmailConfigListParams.status 字段确认）
  - .planning/phases/70-settings-page-redesign/70-UI-SPEC.md（L-2 + Copywriting 表 + Destructive confirmations 表）
  </read_first>
  <action>
  保持组件默认导出与文件名不变（SettingsShell 实例化在 70-06），把页面主体从 `Card(title)+Table` 改为三段式：
  1. **统计卡行**：`.stat-cards` 容器 + 3 张 `.stat-card`——「总配置数」（默认绿条，stat-trend「全部通知渠道」）/「启用」（`.sc-green`，trend「占比 N%」）/「停用」（`.sc-gray`，trend「可随时恢复」）。数据口径：total 取主列表 loadConfigs 的 response total；enabled/disabled 各一次轻请求 `getEmailConfigList({ page: 1, pageSize: 1, status: 0 })` / `status: 1` 只取 total（email 遵循全局惯例 0=启用/1=停用，IC-4）。占比 = total>0 ? Math.round(enabled/total*100) : 0。
  2. **工具栏卡**：`Card style={{ marginBottom: 14 }}`，内部 flex space-between + gap 16px：左侧 inline Form（配置名称搜索 Input + 状态 Select allowClear，options：启用=0/停用=1 + 重置(default)/搜索(primary) 按钮，次序与 user 页一致）；右侧 `<Button type="primary" icon={<PlusOutlined />}>新增配置</Button>`（深绿主按钮，v1.22 按钮纪律）。
  3. **表格卡**：`Card > Table className="xr-table-zebra" size="middle"`；列语汇迁移——configName 列加 `.xr-cell-id`、createdAt 列 `.xr-cell-time`（mono）、操作列 `.xr-row-ops` 文字链接（测试/编辑/删除，删除链接 `.xr-op-danger`）。空状态用 Table `locale={{ emptyText }}` 渲染 Antd Empty：标题「暂无邮箱配置」+ 描述「新增第一个邮箱服务器配置，用于发送通知邮件」。
  4. **Tag 迁移（D-12 第 6 项）**：默认标记 `Tag color="blue"`（:185）→ `xr-tag-gold`；SSL Tag（:197）→ `xr-tag`；状态 Tag（:223）→ `status === 0 ? "xr-tag-green"（正常）: "xr-tag"（停用）`。
  5. **删除确认升级**：删除操作改 `Modal.confirm({ title: "删除邮箱配置？", content: "删除后不可恢复，使用该配置的通知邮件将发送失败。", okText: "删除", okButtonProps: { danger: true }, cancelText: "取消", onOk: ... })`。
  6. **业务保留段逐字保留**：密码脱敏（`if (!values.password || values.password === "******") { delete updateData.password; }`）；编辑/测试双 Modal 结构与宽度 700 不变；`isFormValidationError(error)` 早退惯例；加载函数 setLoading/try/finally 结构。
  7. **双请求收敛**：删除 Table `onChange` 内手动 `loadConfigs()` 调用（:305-309），仅保留 setCurrent/setPageSize，由既有 `useEffect([paginationProps.current, pageSize])` 单一路径触发加载。
  8. 错误文案按 UI-SPEC：「加载邮箱配置失败，请刷新重试」。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npm run type-check && npx vitest run src/pages/system/settings/__tests__/ --passWithNoTests && grep -c "stat-cards\|xr-table-zebra\|xr-tag-gold" src/pages/system/settings/email-config.tsx >= 3 且 grep -c "Tag color=\"blue\"\|Tag color=\"green\"" src/pages/system/settings/email-config.tsx == 0 且 grep -c "values.password === \"\\*\\*\\*\\*\\*\\*\"" src/pages/system/settings/email-config.tsx == 1</automated>
  </verify>
  <acceptance_criteria>
  - email-config.tsx 含 `.stat-cards`/`.stat-card sc-green`/`.sc-gray` 三卡、工具栏「新增配置」primary 按钮、`.xr-table-zebra` 表格
  - preset color Tag（blue/green/orange/cyan/purple）在本文件零命中
  - 密码 `******` 脱敏分支原样存在；Modal 宽度 700 与双 Modal 结构未变
  - Table onChange 内无 loadConfigs() 手动调用（双请求收敛完成）
  - 统计轻请求使用 status: 0 / status: 1 且 pageSize: 1
  - type-check 通过
  </acceptance_criteria>
  <done>邮箱配置页 v16 三段式落位，业务保留段完好</done>
</task>

<task type="auto">
  <name>Task 2: api-config.tsx v16 三段式改造</name>
  <files>xingran-react-frontend/src/pages/system/settings/api-config.tsx</files>
  <read_first>
  - xingran-react-frontend/src/pages/system/settings/api-config.tsx（全文；重点保留段 :351-436 Modal 内 Tabs、:392-431 authType 条件表单、:52 configTypeFilter、:154/:163-168/:204 Tag 位点）
  - xingran-react-frontend/src/pages/system/settings/email-config.tsx（Task 1 产物——同构对照）
  - xingran-react-frontend/src/lib/notificationConfigApi.ts（APINotificationConfigListParams.status 字段确认）
  - .planning/phases/70-settings-page-redesign/70-UI-SPEC.md（L-2 API 差异点 + Copywriting 表）
  </read_first>
  <action>
  复用 Task 1 的三段式结构，按 API 页差异点落地：
  1. **统计卡 3 卡**（不加第 4 卡，UI-SPEC L-2 克制裁定）：总配置数（trend「全部推送渠道」）/ 启用 sc-green（占比 N%）/ 停用 sc-gray（可随时恢复）；轻请求 `getAPINotificationConfigList({ page: 1, pageSize: 1, status: 0/1 })`（API 配置同样 0=启用/1=停用，IC-4）。
  2. **工具栏筛选多一档**：把现有 `configTypeFilter`（:52/:67-277）并入 inline Form——配置名称搜索 + configType Select（SMS/WEBHOOK/PUSH）+ 状态 Select allowClear；右侧「新增配置」primary 按钮。
  3. **表格列语汇迁移**：同 email（.xr-cell-id/.xr-cell-time/.xr-row-ops + 删除 .xr-op-danger）；空状态 Empty「暂无 API 通知配置」/「新增 Webhook / 短信 / 推送渠道配置，用于系统通知推送」。
  4. **Tag 迁移**：类型 Tag（:163-168，SMS=cyan/WEBHOOK=purple/PUSH=orange）→ `xr-tag` 中性；「默认」（:154）→ `xr-tag-gold`；状态（:204）→ `status === 0 ? xr-tag-green : xr-tag`。
  5. **Modal 完全不动（D-09）**：Modal 宽度 800、内部 headers/template/auth Tabs（:351-436）与 authType 条件表单（:392-431 `Form.Item noStyle shouldUpdate` + getFieldValue 分支）逐字保留——diff 里不得出现 Modal 内 Tabs 删除（Pitfall 6）。
  6. **删除确认升级**：`Modal.confirm({ title: "删除 API 配置？", content: "删除后不可恢复，使用该配置的通知推送将失败。", okText: "删除", okButtonProps: { danger: true }, cancelText: "取消" })`。
  7. 错误文案「加载 API 配置失败，请刷新重试」。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npm run type-check && grep -c "stat-cards\|xr-table-zebra" src/pages/system/settings/api-config.tsx >= 2 且 grep -c "Tag color=\"cyan\"\|Tag color=\"purple\"\|Tag color=\"orange\"" src/pages/system/settings/api-config.tsx == 0 且 grep -c "headers\|template\|auth" src/pages/system/settings/api-config.tsx >= 3 且 grep -c "Tabs" src/pages/system/settings/api-config.tsx >= 1</automated>
  </verify>
  <acceptance_criteria>
  - api-config.tsx 三段式落位，统计卡仅 3 张（无第 4 卡）
  - preset color Tag 零命中；configType 筛选在工具栏 inline Form 内
  - Modal 内 Tabs（headers/template/auth 三段）与 authType 条件表单原样存在（grep 三关键词 + 「Tabs」计数 ≥ 1——api 页唯一合法 Tabs 即 Modal 内，Pitfall 6 守护）
  - type-check 通过
  </acceptance_criteria>
  <done>API 配置页 v16 三段式落位，Modal 契约（D-09）完好</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新增边界 | 纯 UI 重排，API 调用走既有 @/lib/notificationConfigApi 封装（SM2+SM4 传输与鉴权不变） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-0301 | Repudiation | 删除操作 | mitigate | 删除确认弹窗升级为具名确认（标题+后果正文+danger 按钮），操作留痕走后端既有 operlog，不因重构丢失 confirm 环节 |
| T-70-0302 | Information Disclosure | 密码字段 | mitigate | 「password === ****** 则不发送」脱敏惯例逐字保留（acceptance 锁定 grep 命中 = 1） |
| T-70-SC | Tampering | npm installs | accept | 零新增依赖，legitimacy gate N/A |
</threat_model>

<verification>
- `npm run type-check` 两任务后各一次
- `npm run lint`（含硬编码色扫描）
- preset Tag / 手动 loadConfigs 收敛点 grep 门（见各 task acceptance）
- Wave 2 汇合门（与 70-02 后完成者执行）：`npm run build && npm run lint && npm run test -- run`
</verification>

<success_criteria>
- 两页均为 stat-cards + 工具栏卡 + xr-table-zebra 表格卡三段式
- 统计计数零后端改动（status 轻请求）
- Modal 容器与内部结构零变化（D-09）
- 密码脱敏、错误处理、Modal 交互全部保留
</success_criteria>

<output>
Create `.planning/phases/70-settings-page-redesign/70-03-SUMMARY.md` when done
</output>
