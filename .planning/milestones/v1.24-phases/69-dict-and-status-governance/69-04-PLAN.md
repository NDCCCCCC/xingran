---
phase: 69-dict-and-status-governance
plan: 04
type: execute
wave: 3
depends_on: ["69-03"]
files_modified:
  - internal/services/vdi/vm_service_impl.go
  - internal/services/addomain/account_pool.go
  - internal/services/notice_service.go
  - internal/services/knowledge_service.go
  - internal/services/rpa/credential_service.go
  - internal/services/scheduler/job_service.go
  - internal/services/scheduler/job_log_service.go
  - internal/models/ad_service_account.go
  - internal/models/rpa.go
  - internal/models/status_constants_test.go
  - scripts/check-status-literals.sh
autonomous: true
requirements: [DICT-01]

must_haves:
  truths:
    - "DICT-01: 批 3 完成——internal/services/{vdi,addomain,notice,knowledge,rpa,scheduler} 全部命中文件裸 status 字面量清零，守护白名单移除对应条目，`go build ./...` + 受影响包 go test 全绿"
    - "DICT-01: E 簇反转零误替——knowledge/notice 的 status=1 替换为 KnowledgeArticleStatusPublished / PublishStatusPublished 而非任何 Disabled/Stopped 常量，diff 中 knowledge/notice 不出现 Enabled/Stop 字样套用"
    - "DICT-01: 批内补缺常量同步锁值——ADAccountStatus*（internal/models/ad_service_account.go，0=可用/1=停用/2=熔断）与 rpa 凭据启停等新增常量家族一律落位 status_constants_test.go 扫描路径内的 model 文件，并同步登记进 status_constants_test.go 的 watched 前缀集合与 expectedStatusValues——锁值覆盖无静默缺口"
    - "DICT-01: 行为零变化——纯常量替换，无逻辑分支/SQL 结构改动；拿不准的字段宁保持字面量并在 SUMMARY 标记待人工复核（宁缺勿错）"
  artifacts:
    - path: "internal/services/knowledge_service.go"
      provides: "E 簇反转替换（status=1 已发布）——本批最高风险语义点"
      contains: "KnowledgeArticleStatus"
    - path: "internal/models/ad_service_account.go"
      provides: "AD 账号池三态常量补缺落点（Status 字段所在 model 文件，AST 测试扫描路径内）"
      contains: "Status"
    - path: "internal/models/status_constants_test.go"
      provides: "批内新增家族的 watched 集合与 expectedStatusValues 登记"
      contains: "TestStatusConstants"
  key_links:
    - from: "internal/services/{knowledge_service,notice_service}.go"
      to: "internal/models/{knowledge,notice_enhanced}.go"
      via: "E 簇反转常量替换 status = 1 字面量"
      pattern: "KnowledgeArticleStatusPublished|PublishStatusPublished"
    - from: "internal/models/status_constants_test.go (watched 集合 + expectedStatusValues)"
      to: "internal/models/ad_service_account.go / internal/models/rpa.go 新增家族"
      via: "批内补缺常量的前缀登记与期望值登记（锁值闭环）"
      pattern: "ADAccountStatus|RPACredentialStatus"
    - from: "scripts/check-status-literals.sh 白名单"
      to: "批 3 全部替换文件"
      via: "批完成即删除对应条目（与批 2/批 4 串行共享此白名单文件）"
      pattern: "ALLOWED"
---

# Phase 69 Plan 04: DICT-01 批 3 — vdi/addomain/notice/knowledge/rpa/scheduler 替换（簇 A + D 账号池 + E 反转）

**Status:** READY FOR EXECUTION
**Created:** 2026-08-19（2026-08-19 revision：由原 69-03（批 2-4 合体）拆出批 3 为独立 plan；补 checker 要求的「批内补缺常量同步登记锁值测试 watched 集合」约束）
**Source:** ROADMAP Phase 69（DICT-01）+ 69-RESEARCH.md A1/A2 批次表 + 69-PATTERNS.md
**决策依据:** orchestrator 规划要点 1 —— E 簇反转与 D 簇三态是全 phase 最高风险语义判断，独立成批单 commit 控制爆炸半径；批 3 与批 2/批 4 串行（共享守护白名单文件）

<objective>

完成 DICT-01 批 3：internal/services/ 下 vdi/addomain/notice/knowledge/rpa/scheduler 六目录全部命中文件的裸 status 字面量替换。E 簇反转（knowledge/notice 已发布=1）与 D 簇三态（AD 账号池 0=可用/1=停用/2=熔断）是本批两个高风险语义点；批内允许的常量补缺（ADAccountStatus* / rpa 凭据启停）必须落位 AST 锁值测试扫描路径内并同步登记 watched 集合与 expectedStatusValues。

Purpose: 69-03（批 2）已收缩守护白名单；本 plan 继续清偿六目录欠账。锁值登记约束确保批内补缺的新常量家族不被锁值测试静默漏看（否则改值无测试兜底，DICT-01 的锁值闭环出现缺口）。
Output: 约 7 个 service 文件常量化 + 条件性 2 个 model 文件补常量 + status_constants_test.go 登记 + 守护白名单收缩；1 个批次 commit；受影响包测试全绿。

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/69-dict-and-status-governance/69-RESEARCH.md
@.planning/phases/69-dict-and-status-governance/69-PATTERNS.md
@.planning/phases/69-dict-and-status-governance/69-01-SUMMARY.md
@.planning/phases/69-dict-and-status-governance/69-03-SUMMARY.md

<interfaces>
<!-- 关键现状契约（已从代码库提取，executor 无需再探索） -->

语义簇替换对照表（RESEARCH A2，批 3 涉及簇 A + D + E）:

| 簇 | 语义 | 涉及文件（批 3） | 目标常量 | 严禁 |
|----|------|---------------|---------|------|
| A 通用启停 | 0=正常/启用, 1=停用 | vm_service_impl（实体启停部分）、notice 启停、job_service、rpa credential | VDIServer 常量（69-01 补）、NoticeStatus（69-01 补）、models.JobStatusNormal/JobStatusPause（注意注释语义是 正常/暂停）、rpa 凭据常量（本批核对/补） | — |
| D 多态(3+值) | 0=可用, 1=停用, 2=熔断 | addomain/account_pool.go（3 处） | ADAccountStatus* 三常量（补缺落点 internal/models/ad_service_account.go——Status 字段所在 model 文件 :24，值语义见 :46-49 与 CLAUDE.md Phase 36 状态机记载） | 一刀切 |
| E 语义反转 | **1=正向（已发布）** | knowledge_service.go（4 处 status=1）、notice 发布态 | models.KnowledgeArticleStatusDraft/Published、models.PublishStatus* | **套 1=停用** |

替换形态双模板:
- Where 参数化（范本 department_cache_impl.go:65）: Where("status = ?", models.DeptStatusNormal)
- CASE WHEN 聚合 Sprintf: fmt.Sprintf("SUM(CASE WHEN status = %d THEN 1 ELSE 0 END) AS enabled", int(models.Xxx))

关键 model 现状（已核实，executor 直接用）:
- internal/models/ad_service_account.go:24 `Status int`（not null;default:0）——无常量定义；:46 `if a.Status == 1 { // 停用`、:49 `if a.Status == 2 { // 熔断中且未到期`、:59/:64 同族比较。**ADAccountStatus* 补缺落点即此文件**（package models，在 status_constants_test.go 的 Glob 扫描路径 internal/models/*.go 内）。
- internal/models/rpa.go 已有 TaskStatus/RPAExecutionStatus/WorkerStatus/ScheduleStatus 家族（:12-62）；RPA 凭据实体在 internal/models/rpa/credentials.go:34 `Status int`（check:status IN (0,1)）且该子目录**不在** status_constants_test.go 的 Glob 范围——rpa 凭据启停若需补常量，一律放 internal/models/rpa.go（不放 rpa/ 子目录），否则锁值测试扫不到。
- internal/models/knowledge.go:16-17 KnowledgeArticleStatusDraft=0/Published=1；notice_enhanced.go:23-26 PublishStatus 四值（已存在勿动）；notice_enhanced.go 的 Notice 实体自身启停 NoticeStatus 由 69-01 T1 补齐。
- internal/models/log.go:51-56 JobStatusNormal=0 正常/JobStatusPause=1 暂停；job_log 成败常量由 69-01 T1 在 log.go 核对/补齐。

批次文件清单权威来源: `bash scripts/check-status-literals.sh --baseline`（批 2 后剩余条目中取 vdi/addomain/notice/knowledge/rpa/scheduler 六目录全部条目，以脚本实际输出为准，多不漏）。

锁值登记机制（69-01 T1 预留，本批执行）: status_constants_test.go 的 readStatusConsts 只扫 internal/models/*.go + internal/models/operations/*.go 且只收集 watched 前缀集合内的家族——本批每新增一个常量家族必须同步做两件事：(a) 家族前缀加入 watched 集合；(b) 每个常量登记进 expectedStatusValues（值以补缺时的定义为准冻结）。漏 (a) 则 AST 扫不到（静默缺口），漏 (b) 则双向断言报 "add it to expectedStatusValues" 直接 fail——后者有测试兜底，前者靠本 plan action 约束。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>T1: 批 3 — vdi/addomain/notice/knowledge/rpa/scheduler（簇 A + D 账号池 + E 反转 + 补缺常量锁值登记）</name>
  <files>internal/services/vdi/vm_service_impl.go, internal/services/addomain/account_pool.go, internal/services/notice_service.go, internal/services/knowledge_service.go, internal/services/rpa/credential_service.go, internal/services/scheduler/job_service.go, internal/services/scheduler/job_log_service.go, internal/models/ad_service_account.go, internal/models/rpa.go, internal/models/status_constants_test.go, scripts/check-status-literals.sh</files>
  <read_first>
  - scripts/check-status-literals.sh --baseline（vdi/addomain/notice/knowledge/rpa/scheduler 目录精确条目）
  - internal/models/knowledge.go:16-17 与 internal/models/notice_enhanced.go:23-26（E 簇反转常量——status=1 是「已发布」，绝不能套停用常量）
  - internal/models/log.go JobStatus/JobLog 常量（job_service/job_log_service 替换依据；job_log 成败语义若 69-01 补了常量则用之）
  - internal/models/ad_service_account.go（:24 Status 字段与 :46-64 裸比较现状——ADAccountStatus* 补缺落点与值语义依据）
  - internal/models/rpa.go（既有四个 Status 家族——rpa 凭据常量若补，加在此文件）
  - internal/models/vdi.go（VDIServer 常量，69-01 补）、internal/models/notice_enhanced.go Notice Status（NoticeStatus，69-01 补）
  - internal/models/status_constants_test.go（watched 前缀集合与 expectedStatusValues 现状——登记落点）
  - 69-01-SUMMARY.md 新增常量清单（引用来源）
  </read_first>
  <action>
  1. --baseline 取批 3 全部条目（含 addomain 目录下 account_pool 之外的命中文件）。
  2. vm_service_impl.go 7 处：按上下文区分——VM 实体启停（簇 A，VDIServer/VM 常量）与任务状态（簇 B）分别映射，逐处读 model 注释。
  3. account_pool.go 3 处：AD 账号池三态（0=可用/1=停用/2=熔断）当前无常量——在 internal/models/ad_service_account.go 按 base.go 风格补 ADAccountStatus* 三常量（ADAccountStatusAvailable = 0 // 可用 / ADAccountStatusDisabled = 1 // 停用 / ADAccountStatusBreaker = 2 // 熔断，命名以 Phase 36 状态机语义为准），account_pool.go 与 ad_service_account.go 内既有裸比较（:46/:49/:59/:64 等同族）一并替换引用；**同步把 ADAccountStatus 前缀登记进 status_constants_test.go 的 watched 集合与 expectedStatusValues**（这是唯一允许的批内补缺之一）。
  4. knowledge_service.go 4 处：E 簇——status = 0 是草稿、status = 1 是已发布，替换为 models.KnowledgeArticleStatusDraft / models.KnowledgeArticleStatusPublished；notice_service.go 同理区分 Notice 启停（NoticeStatus*）与发布态（PublishStatus*）两类字段，按 SQL 上下文的字段名（status vs publish_status）判定，拿不准的字段保持字面量并在 SUMMARY 标记待人工复核（宁缺勿错）。
  5. scheduler 两文件：job_service 启停用 models.JobStatusNormal/JobStatusPause（注意注释语义是 正常/暂停，非启用/停用）；job_log_service 按成败语义用 69-01 补的常量。
  6. rpa/credential_service.go：凭据启停（簇 A）——核对 internal/models/rpa.go 是否已有凭据启停常量；若确无，在 rpa.go 补 RPACredentialStatus* 家族（值 0/1 对齐 credentials.go:34 的 check 约束），credential_service.go 替换引用；**同样同步登记 watched 集合与 expectedStatusValues**。禁止把常量放进 internal/models/rpa/ 子目录（不在 AST 测试 Glob 范围）。
  7. 登记完成后跑 `go test ./internal/models/ -run TestStatusConstants` 确认新家族被锁值覆盖（双向断言通过 = watched + expected 登记齐全）。
  8. 白名单删除本批条目；脚本退出码 0。
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/models/ -run TestStatusConstants && go test ./internal/services/... && bash scripts/check-status-literals.sh</automated>
  </verify>
  <acceptance_criteria>
  - `go build ./...` + 受影响包 go test 退出码 0
  - knowledge_service.go 的 diff 只出现 KnowledgeArticleStatusDraft/Published，不出现 Disabled/Stopped 套用（E 簇零误替的源码断言）
  - notice_service.go 每处替换在 SUMMARY 有字段语义判定记录（status=启停 vs publish_status=发布态）
  - 守护脚本退出码 0，白名单无 vdi/addomain/notice/knowledge/rpa/scheduler 条目
  - 若补了 ADAccountStatus*/RPACredentialStatus* 常量：值与 CLAUDE.md Phase 36 状态机记载一致（0=可用/1=停用/2=熔断）；`go test ./internal/models/ -run TestStatusConstants` 全绿证明两个新家族均已登记进 watched 集合与 expectedStatusValues（锁值覆盖无静默缺口）
  - 新常量全部位于 internal/models/ad_service_account.go 与 internal/models/rpa.go（不在 models/rpa/ 子目录、不在 services 包内）
  </acceptance_criteria>
  <done>批 3 清零：E 簇反转与 D 簇三态全部正确映射，批内补缺常量完成锁值登记闭环，白名单收缩，构建与包测试全绿。Commit: refactor(services): replace raw status literals with models constants (batch 3) per DICT-01</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| models 常量 → raw SQL 字符串（CASE WHEN Sprintf） | 簇映射错误即统计口径损坏；E 簇反转错替即已发布内容不可见 |
| 锁值测试 watched 集合 → 批内补缺常量 | 不在扫描路径/未登记前缀的家族 = 锁值盲区（改值无测试兜底） |
| 守护白名单 → 替换进度 | 白名单是唯一进度账本；漏删条目即掩盖欠账 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-09 | Tampering | E 簇反转误替（knowledge/notice status=1 已发布被套停用常量）→ 已发布内容不可见 | mitigate | T1: 替换前读 model 常量注释；acceptance 源码断言 diff 只出现 KnowledgeArticleStatusDraft/Published；拿不准的字段宁保持字面量并标记待复核 |
| T-69-18 | Tampering | 批内补缺常量未登记锁值测试（watched 集合漏加前缀）→ 新家族改值无测试兜底 | mitigate | T1 action 3/6/7：补缺落点限定 AST 扫描路径内 model 文件（ad_service_account.go / rpa.go，禁止 models/rpa/ 子目录与 services 包）；watched 集合 + expectedStatusValues 双登记；acceptance 以 TestStatusConstants 全绿作登记闭环证据 |
| T-69-19 | Tampering | ADAccountStatus 三态值与 Phase 36 状态机记载漂移（账号池熔断逻辑依赖 0/1/2 精确语义） | mitigate | 补缺时以 ad_service_account.go 既有裸比较注释与 CLAUDE.md Phase 36 记载为准锁值，登记 expectedStatusValues 后由锁值测试长期兜底 |
| T-69-12 | Repudiation | 替换引入行为变化但无测试覆盖 | accept(受控) | 纯常量等值替换 + 包级 go test + `go test ./...` 终态门（69-05） |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | 零依赖安装，Package Legitimacy Gate 不适用 |
</threat_model>

<verification>

**每任务边界：** `go build ./...` + `go test ./internal/models/ -run TestStatusConstants`（锁值登记闭环）+ 受影响包 go test + 守护脚本退出码 0 + 白名单条目删除。

**Plan 终态（批 3 完成）：**
1. `bash scripts/check-status-literals.sh` 退出码 0，白名单不含六目录条目
2. `go build ./...` + 受影响包 go test 全绿
3. `go test ./internal/models/ -run TestStatusConstants` 全绿且输出含批 3 新增家族（若有补缺）

</verification>

<success_criteria>

（ROADMAP Phase 69 SC#1 的 DICT-01 替换部分——批 3）

1. vdi/addomain/notice/knowledge/rpa/scheduler 清零（E 簇反转 + 账号池三态正确映射）—— 由 T1 交付
2. 批内补缺常量（ADAccountStatus* / RPACredentialStatus*）落位扫描路径内并完成 watched 集合 + expectedStatusValues 双登记，锁值闭环无静默缺口 —— 由 T1 交付
3. 守护白名单对应条目收缩，行为零变化 —— 由 T1 交付

</success_criteria>

<output>
Create `.planning/phases/69-dict-and-status-governance/69-04-SUMMARY.md` when done，必须包含：
- 1 批 × commit hash 对照表与文件数/替换处数（守护脚本基线前后快照佐证）
- 语义簇映射台账：每文件 → 簇 → 所用常量（尤其 E 簇 knowledge/notice 判定/D 簇账号池三态）
- 批内补缺常量清单（家族名/常量名/值/落点文件/watched 与 expectedStatusValues 登记证据）
- notice_service.go 每处替换的字段语义判定记录（status=启停 vs publish_status=发布态）
- 「保持字面量并标记待人工复核」的条目清单（若有保守跳过项，逐条列出字段与原因）
- 白名单快照（批 3 条目删除后剩余条目——批 4 工作面）
</output>
