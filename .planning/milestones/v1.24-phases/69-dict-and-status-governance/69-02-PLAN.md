---
phase: 69-dict-and-status-governance
plan: 02
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - internal/core/db/migrations/migration_208_dict_seed.go
  - internal/core/db/migrations/migration_208_dict_seed_test.go
  - internal/core/db/database.go
autonomous: true
requirements: [DICT-02]

must_haves:
  truths:
    - "DICT-02: migration_208 重建 11 组字典 seed（8 组 archive 存量 + 新增 ops_workstation_type / sys_user_sex / duty_holiday_type），组级 count 查重 + 单事务 + 失败 WARN 不阻断启动"
    - "DICT-02: migration_208 同时挂载 database.go 的 PG advisory-lock 分支与 SQLite else 分支（照抄 207 双分支挂法）——dev sqlite 启动后 `SELECT COUNT(*) FROM sys_dict_type` ≥ 11 且 sys_dict_data > 0"
    - "DICT-02: 幂等双保险——sqlite 内存库测试跑两遍断言行数不变；预插 dictType 的组 seed 整组跳过不复活（尊重管理员删除意图）"
    - "DICT-02: DictValue 一律写字符串（int 常量 seed 时写 \"0\"/\"1\"/\"2\"），DictType/DictData 的 Status 一律用 models 常量（models.DictStatusNormal，69-01 已交付——本 plan 硬依赖 69-01，不存在裸 0 回退路径）"
  artifacts:
    - path: "internal/core/db/migrations/migration_208_dict_seed.go"
      provides: "11 组 sys_dict_type/sys_dict_data 幂等 seed"
      contains: "Migrate208"
    - path: "internal/core/db/migrations/migration_208_dict_seed_test.go"
      provides: "幂等 + 不复活 + isDefault 语义三用例（sqlite 内存库）"
      contains: "TestMigrate208"
    - path: "internal/core/db/database.go"
      provides: "PG advisory-lock 块 + SQLite else 块双分支注册"
      contains: "Migrate208"
  key_links:
    - from: "internal/core/db/database.go (PG advisory-lock 块 + sqlite else 块)"
      to: "internal/core/db/migrations/migration_208_dict_seed.go"
      via: "两分支各一次 Migrate208 调用 + applogger.Errorf 非阻断错误处理"
      pattern: "Migrate208.*dict|字典 seed 失败 \\(非阻断"
    - from: "internal/core/db/migrations/migration_208_dict_seed.go"
      to: "internal/models/dict.go (DictType/DictData) + internal/models/dict.go (DictStatus 常量)"
      via: "GORM 结构体 Create（count 查重走 dict_type uniqueIndex；Status 字段引用 models.DictStatusNormal）"
      pattern: "models\\.DictType|models\\.DictData|models\\.DictStatus"
---

# Phase 69 Plan 02: DICT-02 — sys_dict seed 链路重建（migration_208 双分支挂载）

**Status:** READY FOR EXECUTION
**Created:** 2026-08-19（2026-08-19 revision：depends_on 改为 ["69-01"] 消除 DictStatus 常量竞态——原「临时用 0 并标注待收口」回退条款删除，DictStatus 家族由 69-01 T1 先行交付，本 plan 直接引用）
**Source:** ROADMAP Phase 69（DICT-02）+ 69-RESEARCH.md A4/A6 + 69-PATTERNS.md
**决策依据:** orchestrator 规划要点 2 —— dev 库 sys_dict_type/sys_dict_data 实测 0 行（archive 迁移不执行、init_data.go 不 seed 字典），DICT-02 是「重建 seed 链路」而非「补新枚举」；Q4 候选按「前端有对应下拉的优先」圈定

<objective>

新建 migration_208 重建 11 组字典 seed 并挂载到 database.go 的 PG 与 SQLite 双分支——修复 dev 库字典表为空导致 4 个既有 useDict 页下拉空白的问题，同时为 69-07 的前端迁移（ops_workstation_type / sys_user_sex / duty_holiday_type）提供数据。dashboard_* 三组不重建（前端零消费，见 Source Audit）。

Purpose: dev 库 2026-08-17 从 Supabase PG 切到本地 SQLite 后，全部历史字典 seed（archive/ 不执行）失效，字典基础设施（API/管理页/缓存/useDict）完整但无数据——本 plan 是整条字典链路的数据恢复点。**依赖关系：** seed 的 DictType/DictData Status 字段引用 models.DictStatus 常量，该常量由 69-01 T1 交付——本 plan 硬依赖 69-01（Wave 2 执行），不存在「常量未就绪先用裸 0」的回退路径（truth #4 由依赖顺序保证）。
Output: 2 个新文件（migration + test）+ database.go 双分支各加 3 行调用；dev 库启动后 sys_dict_type ≥ 11 行；2-3 个独立 commit。

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/69-dict-and-status-governance/69-RESEARCH.md
@.planning/phases/69-dict-and-status-governance/69-PATTERNS.md
@.planning/phases/69-dict-and-status-governance/69-01-SUMMARY.md

<interfaces>
<!-- 关键现状契约（已从代码库提取，executor 无需再探索） -->

`internal/models/dict.go:3-24` 被 seed 的模型（字段名直接决定 seed 结构）:

```go
type DictType struct {
    BaseModel
    DictName string `gorm:"size:100;not null" json:"dictName"`
    DictType string `gorm:"uniqueIndex;size:100;not null" json:"dictType"`
    Status   int    `gorm:"default:0" json:"status"`
    Remark   string `gorm:"size:500" json:"remark,omitempty"`
}

type DictData struct {
    BaseModel
    DictSort  int     `gorm:"default:0" json:"dictSort"`
    DictLabel string  `gorm:"size:100;not null" json:"dictLabel"`
    DictValue string  `gorm:"size:100;not null" json:"dictValue"`   // 注意: string
    DictType  string  `gorm:"size:100;index" json:"dictType"`
    ListClass *string `gorm:"size:100" json:"listClass,omitempty"`   // tag 颜色
    IsDefault bool    `gorm:"default:false" json:"isDefault"`
    Status    int     `gorm:"default:0" json:"status"`
}
```

依赖契约（69-01 T1 交付，69-01-SUMMARY 有完整清单）: `models.DictStatusNormal = 0 // 正常`、`models.DictStatusDisabled = 1 // 停用` 已存在于 internal/models/dict.go——本 plan seed 的 Status 字段直接引用，不写裸 0。

seed 幂等模式主范本 `migration_203_connection_pool_sysconfig.go:45-66`（count 查重 → 跳过 → 结构体 Create）+ 辅范本 `migration_207_menu_catalog_seed.go:41-53,70-98`（幂等语义四条注释 + db.Transaction 包裹）。改造点（PATTERNS 明确）：**不要照抄 203 的失败 continue——字典组失败应事务回滚整组并由调用方 WARN**。

`internal/core/db/database.go:837-847` 双分支挂法（唯一必须逐字照抄的位置模式，Pitfall 1 标准答案）:

```go
// PG 分支 (advisory-lock 块内, 207 调用之后):
if err := migrations.Migrate207SeedCanonicalMenuCatalog(d.DB); err != nil {
    applogger.Errorf("规范菜单目录种子失败 (非阻断,留待下次启动): %v", err)
}

} else {
    // sqlite 分支: 规范菜单目录种子 (双方言迁移; PG 分支在上方 advisory-lock 块内执行)
    if err := migrations.Migrate207SeedCanonicalMenuCatalog(d.DB); err != nil {
        applogger.Errorf("规范菜单目录种子失败 (非阻断,留待下次启动): %v", err)
    }
```

migration_208：两个分支都在各自 207 调用之后追加同构调用，错误文案用「字典 seed 失败 (非阻断,留待下次启动)」。反例（勿学）：migration 202-206 只挂 PG 分支，configs/config.yaml:12-16 注释已文档化该缺口。

测试夹具范本 `migration_207_menu_catalog_seed_test.go:12-28`:
- import "github.com/glebarez/sqlite"（纯 Go 驱动，非 mattn/go-sqlite3）
- DSN: "file:" + t.Name() + "?mode=memory&cache=shared&_busy_timeout=5000"
- 208 版 AutoMigrate 最小表集: &models.DictType{}, &models.DictData{}

seed 值来源（抄值不改链路，RESEARCH A6/Q3）:

| dictType | 组 | 值来源文件 |
|----------|----|-----------|
| network_device_type | 重建 | archive/legacy-2026-06-15/002_init_network_device_type_dict.sql + models/network_device.go:7-11（router/switch/firewall/ap/loadbalancer，label 见行尾中文注释；ListClass 照 archive SQL） |
| ops_dedicated_line_type | 重建(6值) | archive/legacy-2026-06-15/047_add_dedicated_line_type_dict.sql + internal/services/operations/excel_config.go:265（internet=互联网专线/intranet=内网专线/cloud_desktop=云桌面专线/mpls=MPLS VPN/fiber=光纤专线/leased_line=租用专线） |
| ops_isp | 重建 | archive/legacy-2026-06-15/048_add_isp_dict.sql |
| ops_info_point_type | 重建 | archive/legacy-2026-06-15/033_add_info_point_type_dict.sql |
| asset_reconciliation_conflict_type | 重建 | archive/applied/migration_169_reconciliation_dicts_configs.go + migration_196_reconciliation_dict_labels_align.go（label 以 196 对齐后为准） |
| asset_reconciliation_exception_action | 重建 | 同上 |
| asset_reconciliation_severity | 重建 | 同上 |
| asset_reconciliation_status | 重建 | 同上 |
| ops_workstation_type | 新增(3值) | internal/models/workstation.go:4-18 + excel_config.go:146（固定/灵活/管理工位；dictValue 以 WorkstationType 常量实际值为准——先读文件确认 int 还是 string，int 则写 "0"/"1"/"2"） |
| sys_user_sex | 新增(3值) | internal/models/base.go Gender（"0"=男/"1"=女/"2"=保密；useDict.ts:4 docstring 本以它举例） |
| duty_holiday_type | 新增 | internal/models/duty.go HolidayType（值与 label 以文件内常量注释为准，RESEARCH 记 legal/workday/custom） |

不重建：dashboard_widget_type / dashboard_template_scope / dashboard_scope（前端零 useDict 消费，planner 按 Q4 圈定剔除，见本 plan Source Audit）。workorder_type / workorder_priority 剔除（workorder/orders/constants.ts 实测无 OPTIONS 下拉，无消费方）。

既有前端消费方（seed 落地后自动恢复，无需改代码）: dedicated-lines/index.tsx:92-93（ops_dedicated_line_type + ops_isp）、info-points/index.tsx:125（ops_info_point_type）、asset/reconciliation/exceptions/index.tsx:124-125 + components/reconciliation/HealthBadge.tsx:44（asset_reconciliation_conflict_type/severity）。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>T1: migration_208_dict_seed.go + migration_208_dict_seed_test.go（11 组 seed + 三用例）</name>
  <files>internal/core/db/migrations/migration_208_dict_seed.go, internal/core/db/migrations/migration_208_dict_seed_test.go</files>
  <read_first>
  - internal/core/db/migrations/migration_203_connection_pool_sysconfig.go（GORM count 查重主范本）
  - internal/core/db/migrations/migration_207_menu_catalog_seed.go（幂等语义注释 + db.Transaction 范本）与 migration_207_menu_catalog_seed_test.go（sqlite 内存库夹具 + 幂等 + 不复活用例母版）
  - internal/models/dict.go（DictType/DictData 字段 + 69-01 新增的 DictStatus 常量——Status 引用来源）
  - internal/core/db/migrations/archive/legacy-2026-06-15/{002_init_network_device_type_dict,033_add_info_point_type_dict,047_add_dedicated_line_type_dict,048_add_isp_dict}.sql（重建组值/label/ListClass/isDefault 抄值来源）
  - internal/core/db/migrations/archive/applied/migration_169_reconciliation_dicts_configs.go 与 migration_196_reconciliation_dict_labels_align.go（asset_reconciliation 4 组值）
  - internal/models/workstation.go:4-18、internal/models/base.go Gender、internal/models/duty.go HolidayType（新增组值来源；确认常量实际值类型）
  - internal/services/operations/excel_config.go:146,265（ops_workstation_type / ops_dedicated_line_type 与导入链路的一致性基准——seed 值必须与 excel_config 完全一致，Pitfall 6）
  </read_first>
  <action>
  1. 写 internal/core/db/migrations/migration_208_dict_seed.go：
     - 导出函数 Migrate208DictSeed(db *gorm.DB) error。
     - 文件头注释照 207:41-53 风格写四条幂等语义：组级快速路径跳过（dictType 存在整组不写，尊重管理员增删/删除意图）、组内不重复查重（组级跳过已保证）、单事务包裹（组失败整组回滚并返回 error，由调用方 WARN 不阻断启动）、双方言（无 PG 专有语法，纯 GORM 调用）。
     - 数据结构：包级 dictSeedGroups 变量，元素为匿名struct{ Type models.DictType; Data []models.DictData } 的 11 组（清单与值来源见 interfaces 表）。DictValue 一律 string（int 常量写 "0"/"1"/"2"）；每组 DictData 的 DictType 字段回填组 key；DictSort 按组内顺序 1 起递增；Status 一律用 models.DictStatusNormal（69-01 已交付，本 plan 硬依赖 69-01——严禁裸 0 回退，若该常量缺失视为依赖未就绪，停下核查而不是绕过）；ListClass/isDefault 照 archive SQL / 模型注释抄值，无来源的组 ListClass 留 nil。
     - 逻辑：遍历 dictSeedGroups——db.Model(&models.DictType{}).Where("dict_type = ?", key).Count 查重，n > 0 则 continue；否则 db.Transaction 内先 Create(&group.Type) 再 Create(&group.Data)（批量 Create 一次调用），任何错误 return err（事务自动回滚）。
     - 值一致性硬约束：ops_dedicated_line_type 六值与 excel_config.go:265 map 的 key 完全一致；ops_workstation_type 与 excel_config.go:146 + workstation.go 常量一致——不一致即视为抄错。
  2. 写 internal/core/db/migrations/migration_208_dict_seed_test.go（207 test 模式，三用例，函数名以 TestMigrate208 开头保证 -run TestMigrate208 命中）：
     - 夹具 freshSQLiteDBForMigrate208(t)：glebarez/sqlite 内存库 + AutoMigrate(&models.DictType{}, &models.DictData{})。
     - 用例 1 TestMigrate208SeedsAndIdempotent：空库跑 Migrate208DictSeed → 断言 sys_dict_type 行数 = 11、sys_dict_data 总行数 = 11 组值数之和、每组按 dict_type 查 DictData 行数等于预期值数 → 再跑第二遍 → 两个行数断言完全不变（幂等）。
     - 用例 2 TestMigrate208RespectsExistingGroups：预插一组 dict_type 已存在的 DictType（如 ops_isp）+ 一条自定义 DictData → 跑 Migrate208 → 该组 DictData 行数仍为 1（seed 未写入、未复活）；其余 10 组正常 seed。
     - 用例 3 TestMigrate208IsDefaultSemantics：每组恰好存在一条 IsDefault=true 的 DictData（消费端 isDefault 默认值逻辑的数据前提，dedicated-lines 三件套依赖）。
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/core/db/migrations/ -run TestMigrate208 -v</automated>
  </verify>
  <acceptance_criteria>
  - `go test ./internal/core/db/migrations/ -run TestMigrate208 -v` 三用例全部 PASS
  - 幂等用例第二遍运行后行数断言不变（测试内显式断言，非人工观察）
  - migration_208_dict_seed.go 内无 "INSERT INTO" 裸 SQL（纯 GORM 结构体 Create）且组级跳过用 Count 实现
  - dictSeedGroups 恰好 11 组；grep ops_dedicated_line_type 的六值与 excel_config.go:265 完全一致；grep dashboard_ 在本文件零命中
  - DictValue 字段全部为字符串字面量（无 int 直传）；DictType/DictData 的 Status 引用 models.DictStatusNormal（grep 无裸 `Status: 0` 直赋）
  - `go build ./...` 退出码 0
  </acceptance_criteria>
  <done>11 组 seed 迁移与三用例测试落地，幂等/不复活/isDefault 语义全部被测试锁定，Status 常量引用（依赖 69-01）到位，构建全绿。Commit: feat(migrations): add migration_208 dict seed for 11 dict groups per DICT-02</done>
</task>

<task type="auto">
  <name>T2: database.go 双分支注册 + dev 库 smoke 验证</name>
  <files>internal/core/db/database.go</files>
  <read_first>
  - internal/core/db/database.go:806-860（PG advisory-lock if 块与 SQLite else 块的完整现状，207 挂点上下文）
  - configs/config.yaml:12-16（sqlite 迁移缺口的历史注释——理解为什么必须双分支）
  </read_first>
  <action>
  1. 在 database.go 的 PG 分支 advisory-lock 块内、Migrate207SeedCanonicalMenuCatalog 调用之后追加 Migrate208DictSeed 调用，错误处理 applogger.Errorf 文案含「字典 seed 失败 (非阻断,留待下次启动)」。
  2. 在 SQLite else 分支的 207 调用之后追加同构调用（注释说明 PG 分支在上方 advisory-lock 块内执行，照 207 的注释风格）。
  3. 不改动 202-206 及其他任何迁移挂载；不动 if/else 结构本身。
  4. dev 库 smoke（自动化优先）：先 sqlite3 记录基线（sqlite3 data/xingran.db "SELECT COUNT(*) FROM sys_dict_type;"，预期 0）；后台启动 go run ./cmd/main.go（或 go build 后运行二进制），等待启动日志出现迁移/seed 完成迹象（轮询日志或固定 sleep ≤ 30s），随后再次查询 sys_dict_type ≥ 11、sys_dict_data > 0，然后终止进程。sqlite3 可执行文件在 PATH 的 sqlite3 或 /d/Program Files/Sqlite3/sqlite3。
  5. 若后端因环境原因（Redis/端口占用等）无法本地启动：改用只读验证——写一个临时 go test 或 main 调用 Migrate208DictSeed 指向 data/xingran.db 文件库跑一遍后查询行数（运行后删除临时文件），并在 SUMMARY 记录偏离；字典管理页目检归入 69-08 的 checkpoint。
  </action>
  <verify>
    <automated>go build ./... && grep -c "Migrate208DictSeed" internal/core/db/database.go</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "Migrate208DictSeed" internal/core/db/database.go` 输出 2（PG 块 + SQLite else 块各一）
  - 两处调用的错误分支均为 applogger.Errorf 且文案含「非阻断」
  - dev 库验证：sqlite3 data/xingran.db "SELECT COUNT(*) FROM sys_dict_type;" 返回 ≥ 11；"SELECT COUNT(*) FROM sys_dict_data;" 返回 > 0（具体数值记录进 SUMMARY）
  - 二次启动（或二跑 seed）后行数不变（幂等在真实 dev 库成立）
  - `go build ./...` 与 `go test ./internal/core/db/...` 退出码 0
  </acceptance_criteria>
  <done>migration_208 双分支挂载完成，dev sqlite 库字典表从 0 行恢复到 ≥ 11 类型，幂等在真实库验证通过，4 个既有 useDict 页的数据前提就绪。Commit: feat(db): register migration_208 dict seed in both PG and sqlite branches per DICT-02</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed 迁移 → 生产 PG 库既有字典数据 | 生产库 sys_dict 状态未知（RESEARCH 假设 A3）；seed 不得覆盖/复活管理员维护的数据 |
| 字典 dictValue → 前端下拉/UI 展示 | dictValue 仅供 label 展示与筛选传参；status 类语义不进字典（代码分支不受字典可配值影响） |
| 迁移执行 → 启动流程 | seed 失败不得阻断服务启动（非阻断 WARN 语义） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-05 | Tampering | seed 覆盖生产字典数据 / 复活管理员删除项 | mitigate | 组级 count 查重跳过（dictType 存在整组不写）+ 单事务回滚 + 失败 WARN 不阻断（migration_207 已验证语义） |
| T-69-06 | Tampering | 字典值注入业务逻辑（若 status 进字典，管理员可配出代码未识别值） | mitigate | 本 plan 只 seed type/性别/假日类枚举；status 0/1 不进字典（69-06 走共享常量，orchestrator Q2 决策）；dictValue 仅作 UI 展示 |
| T-69-07 | Denial of Service | seed 只挂 PG 分支 → dev sqlite 不可见（202-206 前车之鉴） | mitigate | T2 双分支挂载 + dev 库行数 smoke 断言；grep 计数 = 2 作 automated gate |
| T-69-08 | Tampering | seed 值与 excel 导入链路漂移（excel_config.go 第三拷贝） | mitigate | T1 硬约束：ops_dedicated_line_type/ops_workstation_type 值必须与 excel_config.go:146,265 逐字一致；本 phase 不改 excel 链路（Q3 决策） |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | 零依赖安装（GORM/glebarez sqlite 均仓内既有），Package Legitimacy Gate 不适用 |
</threat_model>

<verification>

**每任务边界：** T1 `go build ./...` + `go test ./internal/core/db/migrations/ -run TestMigrate208 -v`；T2 `go build ./...` + grep 双分支计数 + dev 库 sqlite3 行数断言。

**Plan 终态：**
1. `go test ./internal/core/db/migrations/ -run TestMigrate208 -v` 三用例全绿
2. `grep -c "Migrate208DictSeed" internal/core/db/database.go` = 2
3. dev 库 sys_dict_type ≥ 11 / sys_dict_data > 0，且二次执行行数不变
4. `go test ./...` 全绿（全仓回归）

</verification>

<success_criteria>

（ROADMAP Phase 69 SC#2 的 DICT-02 全部）

1. type/category 真枚举 seed 进 sys_dict_type/sys_dict_data——8 组 archive 存量重建 + 3 组新增（ops_workstation_type / sys_user_sex / duty_holiday_type，按 Q4「前端有对应下拉」圈定）——由 T1 交付
2. 迁移随启动自动执行（database.go PG + SQLite 双分支，dev sqlite 可见）——由 T2 交付
3. 幂等：sqlite 内存库两遍行数不变 + dictType 存在组不复活——由 T1 测试锁定
4. 字典管理页可见可维护（页面目检归 69-08 checkpoint，数据前提由本 plan 交付）

</success_criteria>

<output>
Create `.planning/phases/69-dict-and-status-governance/69-02-SUMMARY.md` when done，必须包含：
- 2 个 task × commit hash 对照表
- 11 组 dictType 的完整清单（组名 / 值数 / 值来源文件 / isDefault 项）——69-07 前端迁移与 69-08 DICT-04 文档直接引用
- dev 库前后行数快照（sys_dict_type / sys_dict_data）与二次启动幂等证据
- dashboard_* 与 workorder_* 剔除决策记录（planner 圈定依据）
- 若走 T2 备选验证路径（临时文件库），记录偏离原因与字典管理页目检移交 69-08 checkpoint 的说明
</output>
