---
phase: 69-dict-and-status-governance
plan: 08
type: execute
wave: 5
depends_on: ["69-05", "69-07"]
files_modified:
  - CLAUDE.md
autonomous: false
requirements: [DICT-04]

must_haves:
  truths:
    - "DICT-04: CLAUDE.md Status Value Convention 段删除 6 行模块值表格，改写为指向 internal/models 常量（base.go + status_constants_test.go 锁值）与 sys_dict 真相源的引用 + Menu visible 例外（models.VisibleShow/VisibleHidden）"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Status Value Convention 指针化改写（不再承载值语义本体）"
      contains: "internal/models"
  key_links:
    - from: "CLAUDE.md (Status Value Convention)"
      to: "internal/models/base.go + internal/models/status_constants_test.go + sys_dict"
      via: "文档指针（删除独立值表格）"
      pattern: "status_constants_test"
---

# Phase 69 Plan 08: DICT-04 — CLAUDE.md Status Value Convention 指针化 + 字典链路端到端 checkpoint

**Status:** READY FOR EXECUTION
**Created:** 2026-08-19（2026-08-19 revision：由原 69-04 拆出 DICT-04 文档线 + 端到端 checkpoint 为独立收尾 plan，依赖后端终态（69-05）与前端迁移完成（69-07））
**Source:** ROADMAP Phase 69（DICT-04）+ 69-RESEARCH.md A3/A5 + 69-PATTERNS.md
**决策依据:** orchestrator 规划要点 4 —— DICT-04 删除 CLAUDE.md 独立值表格（漂移源头）；checkpoint 需要 DICT-02 seed + DICT-03 两线全部就位才有验证意义

<objective>

(1) CLAUDE.md `### Status Value Convention (IMPORTANT)` 段改写为真相源指针（models 常量 + status_constants_test.go 锁值 + sys_dict + 前端共享模块），删除 6 行手工值表格；(2) 字典链路端到端人工 checkpoint——验证「字典管理页改 label → 消费页下拉变化」（DICT-03 SC#3 核心验收）、4 个既有 useDict 页恢复、fallback 生效、status 零 UX 变化。

Purpose: CLAUDE.md 的独立值表是状态语义的第三份手工拷贝（已与前端 label 漂移），是指针化消除的最后一个漂移源头；checkpoint 是 phase 级端到端验收（覆盖 DICT-02 目检 + DICT-03 SC#3）。
Output: CLAUDE.md 一段改写 + checkpoint 五步验证记录进 SUMMARY；1 个 commit。

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/69-dict-and-status-governance/69-RESEARCH.md
@.planning/phases/69-dict-and-status-governance/69-01-SUMMARY.md
@.planning/phases/69-dict-and-status-governance/69-02-SUMMARY.md

<interfaces>
<!-- 关键现状契约（已从代码库提取，executor 无需再探索） -->

CLAUDE.md `### Status Value Convention (IMPORTANT)` 段现状结构（改写对象）：通用规则一句（0 = enabled/normal/visible, 1 = disabled/stopped/hidden）+ Menu Visibility 例外句（1 = visible, 0 = hidden）+ 6 行模块值表格（| Module | Field | 0 Value | 1 Value | Default |，覆盖 User/Role/Menu×2/Dept/Post）。

改写后的指针段要点（四个指针 + 一个例外 + 一个安全决策）：
- 状态语义唯一真相源是 internal/models 具名常量（internal/models/base.go 及各 model 文件，例 models.UserStatusEnabled = 0、models.VisibleShow = 1）
- 常量值由 internal/models/status_constants_test.go 回归测试锁定，改值即测试失败
- type/category 类运营可维护枚举的真相源是 sys_dict_type / sys_dict_data（字典管理页维护，seed 见 internal/core/db/migrations/migration_208_dict_seed.go）
- 前端通用启停选项共享 xingran-react-frontend/src/constants/status.ts
- Menu visible 例外句保留（1 = visible, 0 = hidden，引用 models.VisibleShow/VisibleHidden）
- status 0/1 不入字典（代码分支语义，管理员可配值会破坏逻辑）

`## Common Gotchas` 段落引用的是通用规则而非表格，不动。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>T1: DICT-04 — CLAUDE.md Status Value Convention 指针化改写</name>
  <files>CLAUDE.md</files>
  <read_first>
  - CLAUDE.md 的 `### Status Value Convention (IMPORTANT)` 段全文（改写对象——通用规则 + Menu visible 例外 + 6 行模块值表格）
  - 69-01-SUMMARY.md 新增常量清单与 status_constants_test.go 说明（指针指向的真实目标）
  - 69-02-SUMMARY.md 的 11 组 dictType 清单（sys_dict 真相源描述素材）
  </read_first>
  <action>
  1. 改写 `### Status Value Convention (IMPORTANT)` 段：保留通用规则一句（0 = enabled/normal/visible, 1 = disabled/stopped/hidden）与 Menu Visibility 例外句（1 = visible, 0 = hidden），随后替换 6 行模块值表格为 interfaces 列出的指针段（models 常量 / status_constants_test.go 锁值 / sys_dict + migration_208 / 前端 constants/status.ts / status 不入字典安全决策，五要点齐备）。
  2. 删除原 6 行表格（| Module | Field | ... 全部行）。
  3. `## Common Gotchas` 段落引用的是通用规则而非表格，不动；CLAUDE.md 其余段落零改动。
  4. 门禁：grep 验证表格残留清零 + 指针关键词存在；go build / 前端门禁不受文档影响（无需重跑，本任务只动 CLAUDE.md）。
  </action>
  <verify>
    <automated>grep -A 20 "Status Value Convention" CLAUDE.md | grep -c "| User | status |"; grep -A 20 "Status Value Convention" CLAUDE.md | grep -c "internal/models"</automated>
  </verify>
  <acceptance_criteria>
  - `grep -A 20 "Status Value Convention" CLAUDE.md | grep -c "| User | status |"` 输出 0（值表格删除）
  - 同段 grep internal/models 命中 ≥ 1、grep status_constants_test 命中 ≥ 1、grep sys_dict 命中 ≥ 1、grep constants/status.ts 命中 ≥ 1（四类指针齐全）
  - Menu visible 例外句保留（grep -i "visible" 命中且语义为 1 = visible）
  - CLAUDE.md 其余段落 diff 为零（git diff --stat CLAUDE.md 只含该段改动）
  </acceptance_criteria>
  <done>CLAUDE.md 不再承载值语义本体，成为指向 models 常量 + sys_dict + 前端共享模块的指针；漂移源头（第三份手工拷贝）消除。Commit: docs(claude): rewrite Status Value Convention as source-of-truth pointers per DICT-04</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>T2: Checkpoint — 字典链路端到端人工验证（DICT-02 目检 + DICT-03 SC#3 验收）</name>
  <files>无文件改动——纯人工验证 checkpoint，验证结果记录进 SUMMARY</files>
  <action>启动后端（sqlite dev 库）与前端 dev server，按 how-to-verify 五步逐项人工验证字典链路端到端行为；全部通过回复 approved，任一步失败则描述问题交回 executor 修复后复验。</action>
  <what-built>DICT-02 字典 seed（69-02）+ 前端 status 共享常量（69-06）+ 四页 useDict 迁移（69-07）+ 后端常量替换终态（69-03/69-04/69-05）+ CLAUDE.md 指针化（本 plan T1）——字典链路端到端首次打通：字典管理页的修改能实时反映到消费页下拉。</what-built>
  <how-to-verify>
  前置：后端已启动（sqlite dev 库，确认启动日志含 migration_208/字典 seed 成功）+ 前端 npm run dev（http://localhost:4000）+ 用 admin 账号登录。
  1. 字典管理页可见性（DICT-02 目检）：系统管理 → 字典管理，确认出现 69-02 的 11 组字典类型（network_device_type / ops_dedicated_line_type / ops_isp / ops_info_point_type / asset_reconciliation_* 4 组 / ops_workstation_type / sys_user_sex / duty_holiday_type），每组 data 行数与 69-02-SUMMARY 记录一致。
  2. 既有 useDict 页恢复（dev 库字典曾为空）：打开 专线管理（dedicated-lines）→ 线路类型/ISP 下拉有选项（迁移前为空）；信息点管理 → 类型下拉有值；资产对账 exceptions 页 → 冲突类型下拉有值。
  3. 字典改值 → 下拉变化（SC#3 核心验收）：字典管理 → ops_workstation_type → 把「固定工位」label 改为「固定工位(测试)」保存 → 打开 工位管理 页刷新 → 类型下拉显示新 label。
  4. 四页迁移验证：系统用户（性别下拉 3 项）、工位管理（类型下拉）、假日管理（假日类型下拉）、网络设备（设备类型下拉 5 项）均正常出选项；任选一页 DevTools Network 面板把 /system/dicts/data/list 请求断网/改失败后刷新，下拉仍显示静态兜底选项（fallback 生效）。
  5. status 共享常量回归：系统用户列表状态列仍显示 启用/禁用、角色管理仍显示 正常/停用（零 UX 变化）；菜单管理显示/隐藏筛选行为不变（VISIBLE 反转未破坏）。
  </how-to-verify>
  <verify>
    <human-check>how-to-verify 五步全部通过：11 组字典可见 / 既有 useDict 页恢复 / ops_workstation_type 改 label 后工位页下拉随之变化 / 断网 fallback 生效 / status 列零 UX 变化</human-check>
  </verify>
  <resume-signal>回复 "approved" 或逐条描述问题（如某页下拉空白/label 未变化）</resume-signal>
  <done>checkpoint 五步人工验证全部通过（用户回复 approved）且结论逐条记录进 SUMMARY；或问题修复后复验通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE.md 文档 → 开发者/AI 行为 | 文档不再是值真相源，错误表格即误导源头（本 plan 消除） |
| 字典管理页（管理员输入）→ 消费页渲染 | checkpoint 验证该链路的正确性与 fallback 边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-22 | Tampering | 指针化后文档指向失效目标（路径/文件名写错）→ 开发者被误导 | mitigate | T1 acceptance 四类指针 grep 断言齐全；指针目标均为本 phase 实际交付文件（69-01/69-02/69-06 SUMMARY 佐证） |
| T-69-23 | Denial of Service | 字典链路端到端断裂未被自动化捕获（跨页联动只能人工验证） | mitigate | T2 checkpoint 五步覆盖：11 组可见 / 既有页恢复 / 改值联动 / fallback 断网 / status 零 UX——blocking 门不通过不收尾 |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | 零依赖安装，Package Legitimacy Gate 不适用 |
</threat_model>

<verification>

**每任务边界：** T1 grep 断言；T2 checkpoint 人工五步。

**Plan 终态：**
1. grep: CLAUDE.md 值表格 0 残留 + 四类指针齐全
2. checkpoint 五步人工验证通过（字典改值 → 下拉变化 / 既有 useDict 页恢复 / fallback 生效 / status 零 UX 变化）

</verification>

<success_criteria>

（ROADMAP Phase 69 SC#4 全部 + SC#2/SC#3 的验收步）

1. CLAUDE.md Status Value Convention 改为指向 models 常量 + sys_dict 真相源的引用，不再作为第三份手工拷贝 —— 由 T1 交付
2. 字典链路端到端人工验收通过（DICT-02 目检 + DICT-03 SC#3 改值联动 + fallback）—— 由 T2 checkpoint 交付

</success_criteria>

<output>
Create `.planning/phases/69-dict-and-status-governance/69-08-SUMMARY.md` when done，必须包含：
- 2 个 task × commit hash 对照表 + checkpoint 验收结论（五步逐条记录）
- CLAUDE.md 改写前后对照段
</output>
