---
phase: 69-dict-and-status-governance
plan: 05
type: execute
wave: 4
depends_on: ["69-04"]
files_modified:
  - internal/services/workorder/base.go
  - internal/services/workorder/reconciliation_template.go
  - internal/services/duty_pool_service.go
  - internal/services/device_discovery_service.go
  - internal/services/config_execution_service.go
  - internal/services/command_dispatch_service.go
  - internal/services/monitor/server_service.go
  - internal/services/asset/fix_suggestion_monitor.go
  - internal/api/v1/system/notice_handler.go
  - internal/api/v1/monitor/oper_log_handler.go
  - internal/services/operations/geocoding_service.go
  - scripts/check-status-literals.sh
autonomous: true
requirements: [DICT-01]

must_haves:
  truths:
    - "DICT-01: 批 4 完成——internal/services/{workorder,duty_pool,device_discovery,config_execution,command_dispatch,monitor,asset} + api/v1/{system/notice,monitor/oper_log} 两 handler 的裸 status 字面量清零，守护脚本白名单终态只剩 F 簇 geocoding_service.go 一条，`--baseline` 输出恰 1 行"
    - "DICT-01: B 簇状态机零误替——config_execution 的 CASE WHEN 按 struct 已用同族常量映射（ExecutionStatusPending/Running/... 0-4，AS 别名不变），diff 中 config_execution/device_discovery/command_dispatch 不出现 Enabled/Stop 字样套用"
    - "DICT-01: F 簇收口——geocoding_service.go:332 百度 API 返回码保持字面量并加 F 簇豁免注释（不替换），`go test ./...` 全绿（DICT-01 替换面全部完成的终态门）"
  artifacts:
    - path: "internal/services/config_execution_service.go"
      provides: "半迁移收口——raw SQL CASE WHEN 补齐到与 struct 赋值同等常量化（B 簇状态机）"
      contains: "ExecutionStatus"
    - path: "internal/services/operations/geocoding_service.go"
      provides: "F 簇永久豁免注释落点（外部契约，不迁移）"
      contains: "F 簇"
    - path: "scripts/check-status-literals.sh"
      provides: "白名单 ratchet 终态：只剩 geocoding_service.go 一条"
      contains: "geocoding_service.go"
  key_links:
    - from: "internal/services/config_execution_service.go (raw SQL CASE WHEN)"
      to: "internal/models/config_execution.go ExecutionStatus 家族"
      via: "Sprintf + int(models.ExecutionStatus*)，与同文件 struct 赋值（:136 已用常量）自洽"
      pattern: "ExecutionStatus(Pending|Running|Success|Failed|Cancelled)"
    - from: "internal/services/duty_pool_service.go:37-41 (CASE WHEN)"
      to: "internal/models/duty.go DutyPoolStatus 常量"
      via: "Sprintf + int(models.DutyPoolStatus*)"
      pattern: "DutyPoolStatus"
    - from: "scripts/check-status-literals.sh 白名单"
      to: "批 4 全部替换文件"
      via: "批完成即删除对应条目——终态只剩 F 簇一条"
      pattern: "ALLOWED"
---

# Phase 69 Plan 05: DICT-01 批 4 — workorder/duty/discovery/execution/dispatch/monitor + 三 handler + F 簇收口（簇 B/C CASE WHEN）

**Status:** READY FOR EXECUTION
**Created:** 2026-08-19（2026-08-19 revision：由原 69-03（批 2-4 合体）拆出批 4 为独立 plan，承担 DICT-01 替换面终态门）
**Source:** ROADMAP Phase 69（DICT-01）+ 69-RESEARCH.md A1/A2 批次表 + 69-PATTERNS.md
**决策依据:** orchestrator 规划要点 1 —— B 簇状态机 CASE WHEN 是本批主风险（半迁移自洽化）；批 4 与批 2/批 3 串行（共享守护白名单文件），本批收口后白名单只剩 F 簇

<objective>

完成 DICT-01 批 4（最后一批替换）：workorder/duty_pool/device_discovery/config_execution/command_dispatch/monitor/asset fix_suggestion + api/v1 两 handler + 散点文件清零，geocoding F 簇加豁免注释，守护白名单 ratchet 至终态（只剩 geocoding_service.go 一条），全仓 `go test ./...` 作为 DICT-01 替换面的终态门。

Purpose: 批 2（69-03）/批 3（69-04）已清偿大部分欠账；本批处理 B 簇状态机 CASE WHEN（config_execution 半迁移自洽化）与 C 簇成败语义，并以全仓测试 + 白名单终态双证据关闭 DICT-01 替换面。
Output: 约 11 个 service/handler 文件常量化 + geocoding 豁免注释 + 守护白名单终态 1 条；1 个批次 commit；`go build ./...` + `go test ./...` 全绿。

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/69-dict-and-status-governance/69-RESEARCH.md
@.planning/phases/69-dict-and-status-governance/69-PATTERNS.md
@.planning/phases/69-dict-and-status-governance/69-01-SUMMARY.md
@.planning/phases/69-dict-and-status-governance/69-04-SUMMARY.md

<interfaces>
<!-- 关键现状契约（已从代码库提取，executor 无需再探索） -->

语义簇替换对照表（RESEARCH A2，批 4 涉及簇 A + B + C + E 残留）:

| 簇 | 语义 | 涉及文件（批 4） | 目标常量 | 严禁 |
|----|------|---------------|---------|------|
| B 任务状态机 | 0=待执行,1=执行中,2=成功,3=失败,4=取消 | config_execution/device_discovery/command_dispatch | models.ExecutionStatus*（config_execution.go:19-27）；discovery/dispatch 若用独立 Status 家族则读各自 model 映射 | 套 Enabled/Stop、跨实体套用 ExecutionStatus |
| C 成功/失败 | 0=成功, 1=失败 | oper_log_handler.go:190、job_log_service 残余（批 3 已清主体）、fix_suggestion_monitor.go:181（Status: 0 // 0=成功） | 69-01 在 log.go 补的成败常量 | — |
| A 通用启停 | 0=正常, 1=停用 | duty_pool、monitor/server_service、workorder 状态之外的启停字段 | DutyPoolStatus/各实体常量 | — |
| E 语义反转（残留） | 1=正向 | api/v1/system/notice_handler.go（notice 启停 vs 发布态两类字段，批 3 同法判定） | NoticeStatus* / PublishStatus* | 套 1=停用 |
| F 外部契约 | 百度 API 返回码 | geocoding_service.go:332 | 不替换，加注释「F 簇:百度 API 返回码契约,不迁移到 models 常量(见 scripts/check-status-literals.sh 白名单)」 | 替换 |

替换形态双模板:
- Where 参数化: Where("status = ?", models.DeptStatusNormal)
- CASE WHEN 聚合 Sprintf（范本 duty_pool_service.go:37-41 before → after）: fmt.Sprintf("SUM(CASE WHEN status = %d THEN 1 ELSE 0 END) AS enabled", int(models.DutyPoolStatusEnabled))

半迁移先例（批 4 安全参照）: config_execution_service.go——struct 赋值已用常量（:136 Status: models.ExecutionStatusPending），同文件 raw SQL 仍字面量（:46-49 "SUM(CASE WHEN status = 0 THEN 1 ELSE 0 END) AS pending"）。批 4 把这类文件补齐到自洽。

批次文件清单权威来源: `bash scripts/check-status-literals.sh --baseline`（批 3 后全部剩余条目即本 plan 工作面，以脚本实际输出为准，多不漏——interfaces 预估之外的散点文件一并纳入）:
- workorder/{base,reconciliation_template}.go（4+4 处）、duty_pool_service.go（4 处 CASE WHEN）、device_discovery_service.go（4 处）、config_execution_service.go、command_dispatch_service.go（2 处）、monitor/server_service.go、asset/fix_suggestion_monitor.go、api/v1/system/notice_handler.go、api/v1/monitor/oper_log_handler.go
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>T1: 批 4 — workorder/duty/discovery/execution/dispatch/monitor + 两 handler + F 簇收口（簇 B/C CASE WHEN）</name>
  <files>internal/services/workorder/base.go, internal/services/workorder/reconciliation_template.go, internal/services/duty_pool_service.go, internal/services/device_discovery_service.go, internal/services/config_execution_service.go, internal/services/command_dispatch_service.go, internal/services/monitor/server_service.go, internal/services/asset/fix_suggestion_monitor.go, internal/api/v1/system/notice_handler.go, internal/api/v1/monitor/oper_log_handler.go, internal/services/operations/geocoding_service.go, scripts/check-status-literals.sh</files>
  <read_first>
  - scripts/check-status-literals.sh --baseline（批 4 全部剩余条目——此批后白名单应只剩 geocoding）
  - internal/models/config_execution.go:19-27（ExecutionStatus 0-4 全家族——B 簇状态机唯一合法映射）
  - internal/services/config_execution_service.go:46-49,136,151-159（半迁移现状——struct 已用常量、SQL 未替换）
  - internal/models/duty.go（DutyPoolStatus 常量名与值）
  - internal/models/workorder.go（WorkOrderStatus 0-4 及类型常量）
  - internal/models/log.go（OperLog/Logininfor 成败常量，69-01 补）与 oper_log_handler.go:190 上下文
  - internal/models/notice_enhanced.go（NoticeStatus/PublishStatus——notice_handler 字段判定依据，批 3 同法）
  - internal/services/operations/geocoding_service.go:332（F 簇豁免注释落点）
  </read_first>
  <action>
  1. --baseline 取全部剩余条目（workorder/duty_pool/device_discovery/config_execution/command_dispatch/monitor/asset fix_suggestion + api/v1 两 handler + 散点文件，interfaces 预估之外的全部纳入）。
  2. config_execution_service.go：raw SQL CASE WHEN（:46-49 等）按 struct 已用常量的同族映射补齐——status = 0/1/2/3/4 分别 Sprintf models.ExecutionStatusPending/Running/Success/Failed/Cancelled（以 config_execution.go 常量实际命名为准），AS 别名保持不变。
  3. device_discovery_service.go / command_dispatch_service.go：同属 B 簇状态机（若其模型用独立 Status 家族则读各自 model 文件映射，禁止跨实体套用 ExecutionStatus）。
  4. duty_pool_service.go:37-41 CASE WHEN：SUM(CASE WHEN status = 0 → models.DutyPoolStatus 对应常量（读 duty.go 确认命名，如 DutyPoolStatusEnabled/Disabled），Sprintf+int() 形态。
  5. workorder/base.go 4 处 + reconciliation_template.go 4 处：WorkOrderStatus 0-4 状态机映射（models/workorder.go 常量）。
  6. monitor/server_service.go（服务器监控状态语义按其 model 注释）、asset/fix_suggestion_monitor.go:181 的 Status: 0 注释 0=成功（簇 C——用成败常量替换并保留语义注释改写为常量引用）。
  7. oper_log_handler.go:190 与 notice_handler.go：分别按成败（簇 C）与 notice 启停/发布态（批 3 同法判定）替换。
  8. F 簇收口：geocoding_service.go:332 的 Status == 0（百度 API 返回码）不替换，在该行上方加单行注释「// F 簇:百度地图 API 返回码契约,不迁移到 models 常量(见 scripts/check-status-literals.sh 白名单)」。
  9. 白名单删除本批全部条目——终态白名单只剩 geocoding_service.go=F 簇一条；脚本退出码 0。运行全量 `go test ./...`。
  </action>
  <verify>
    <automated>go build ./... && go test ./... && bash scripts/check-status-literals.sh && bash scripts/check-status-literals.sh --baseline | wc -l</automated>
  </verify>
  <acceptance_criteria>
  - `go build ./...` 与 `go test ./...` 全绿
  - `bash scripts/check-status-literals.sh` 退出码 0；`--baseline` 输出行数 = 1（仅 geocoding_service.go）
  - config_execution_service.go 的 CASE WHEN 替换后 AS pending/running 等别名与替换前一致（grep 断言别名保留）
  - geocoding_service.go:332 保持字面量且上方有 F 簇注释（grep 'F 簇' 命中 1 行）
  - 全部 diff 无逻辑/SQL 结构改动（仅常量替换 + import + Sprintf 改造）
  </acceptance_criteria>
  <done>批 4 清零：DICT-01 替换面全部完成，守护白名单终态只剩 F 簇永久豁免，全仓构建与测试全绿。Commit: refactor(services): replace remaining status literals with constants and finalize guard allowlist (batch 4) per DICT-01</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| models 常量 → raw SQL 字符串（CASE WHEN Sprintf） | 状态机值写入聚合 SQL；B 簇错替即统计口径损坏 |
| 守护白名单 → 替换进度终态 | 白名单是唯一进度账本；终态必须只剩 F 簇一条，多一条即欠账未清 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-10 | Tampering | B 簇状态机误替（CASE WHEN status=1 执行中被套 Stop）→ 统计口径损坏 | mitigate | T1: config_execution 以同文件 struct 已用常量为映射基准（半迁移自洽化），AS 别名 grep 断言不变；discovery/dispatch 禁止跨实体套用 |
| T-69-20 | Tampering | F 簇被误替换（百度返回码换成常量）→ 外部契约比较语义损坏 | mitigate | T1 action 8 明确不替换 + 豁免注释；acceptance grep 'F 簇' 命中 1 行 + 白名单终态恰 1 条双断言 |
| T-69-12 | Repudiation | 替换引入行为变化但无测试覆盖 | accept(受控) | 纯常量等值替换 + 全仓 `go test ./...` 终态门；替换面为查询条件/聚合，行为等值由常量值锁值测试背书 |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | 零依赖安装，Package Legitimacy Gate 不适用 |
</threat_model>

<verification>

**每任务边界：** 每批 `go build ./...` + 受影响包 go test + 守护脚本退出码 0 + 白名单条目删除。

**Plan 终态（DICT-01 替换面全部完成）：**
1. `bash scripts/check-status-literals.sh` 退出码 0，`--baseline` 输出仅 1 行（geocoding_service.go）
2. `go build ./...` + `go test ./...` 全绿
3. `go test ./internal/models/ -run TestStatusConstants` 仍绿（锁值未破坏）
4. 抽样 grep：internal/services/ + internal/api/v1/ 非注释 `status = [01]` 命中数 = 1（仅 geocoding F 簇）

</verification>

<success_criteria>

（ROADMAP Phase 69 SC#1 的 DICT-01 替换部分——批 4 终态）

1. workorder/duty/discovery/execution/dispatch/monitor + 两 handler 清零，F 簇注释豁免，守护白名单终态 1 条 —— 由 T1 交付
2. B 簇状态机 CASE WHEN 半迁移自洽化（AS 别名不变）—— 由 T1 交付
3. 全仓 `go build ./...` / `go test ./...` 全绿，行为零变化 —— 由 T1 交付

</success_criteria>

<output>
Create `.planning/phases/69-dict-and-status-governance/69-05-SUMMARY.md` when done，必须包含：
- 1 批 × commit hash 对照表与文件数/替换处数（守护脚本基线前后快照佐证）
- 语义簇映射台账：每文件 → 簇 → 所用常量（尤其 B 簇状态机 CASE WHEN 自洽化与 C 簇成败）
- 白名单终态快照（只剩 geocoding 条目）
- go test ./... 全绿证据
- F 簇豁免注释落点记录
</output>
