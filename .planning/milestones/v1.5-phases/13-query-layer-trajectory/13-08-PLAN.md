---
phase: 13-query-layer-trajectory
plan: 08
type: execute
wave: 2
depends_on:
  - 13-07
gap_closure: true
gap_refs:
  - W1-CR-01
  - W1-CR-02
  - W1-CR-03
  - W4-vendor
  - W5-echarts
files_modified:
  - xingran-react-frontend/src/lib/api/networkApi.ts
  - xingran-react-frontend/src/components/network/MACTrajectoryChart.tsx
  - xingran-react-frontend/src/pages/network/mac/trajectory/TrajectoryPage.tsx
autonomous: true
requirements:
  - QUERY-02
  - QUERY-04
  - UI-03

must_haves:
  truths:
    - "queryMACTrajectory 返回 TrajectoryNode[] 而非 undefined(result.data!.trajectory 不存在 → 改用 result.data!.nodes)"
    - "MACTrajectoryChart 接收的每个 node 都能从 .mac 字段读到 MAC 地址(由 13-07 后端填充)"
    - "MACTrajectoryChart 接收的每个 node 都从 .deviceId/.deviceName/.interface/.vlanId/.startTime/.endTime/.duration/.eventType 正确读取(原 contract mismatch 修复)"
    - "queryMACTrajectory 返回后可调用 queryMACVendor(mac) 获取厂商名,合并到 TrajectoryNode.vendor 字段供 tooltip 展示"
    - "queryMACVendor 函数 POST /network/history/vendor 并解包 result.data.vendorName(由 13-07 字段名变更支撑)"
    - "MACTrajectoryChart 标题不再使用 data[0]?.mac(因节点内 .mac 字段存在),统一从 prop 传入或 props.data[0]?.mac"
    - "MACTrajectoryChart tooltip formatter 通过 nodes[params.dataIndex] 节点对象访问字段,不再依赖易错的 value 数组下标(规避 13-04 shipped index bug:data[4]/data[5]/data[6] 错位)"
  artifacts:
    - path: "xingran-react-frontend/src/lib/api/networkApi.ts"
      provides: "queryMACTrajectory 解包逻辑 + 新增 queryMACVendor 工具"
      contains: "queryMACVendor"
    - path: "xingran-react-frontend/src/components/network/MACTrajectoryChart.tsx"
      provides: "vendor 字段在 tooltip 中显示 + data 字段名对齐 camelCase + tooltip formatter 改用节点对象访问"
      contains: "node.vendor"
    - path: "xingran-react-frontend/src/pages/network/mac/trajectory/TrajectoryPage.tsx"
      provides: "trajectory 拉取后并行查询 vendor 并合并到节点"
      contains: "queryMACVendor"
  key_links:
    - from: "xingran-react-frontend/src/lib/api/networkApi.ts"
      to: "internal/services/mac_history_query_service.go"
      via: "POST /network/history/trajectory → result.data.nodes 解包"
      pattern: "result\\.data\\.nodes|return result\\.data!"
    - from: "xingran-react-frontend/src/lib/api/networkApi.ts"
      to: "internal/api/v1/network/mac_history_handler.go"
      via: "POST /network/history/vendor → result.data.vendorName 解包"
      pattern: "result\\.data\\.vendorName"
    - from: "xingran-react-frontend/src/components/network/MACTrajectoryChart.tsx"
      to: "xingran-react-frontend/src/lib/api/networkApi.ts"
      via: "TrajectoryNode.mac/.deviceId/.interface 字段直接读入 ECharts seriesData + tooltip formatter 通过 nodes[params.dataIndex] 访问"
      pattern: "node\\.mac|node\\.deviceId|node\\.interface|nodes\\[params\\.dataIndex\\]"
---

<objective>
修复 Phase 13 前端 3 个 contract 缺陷:
1. `queryMACTrajectory` 函数解包 `result.data!.trajectory`(后端无此字段,返回 undefined,VERIFICATION CR-01)→ 改为 `result.data!.nodes`
2. MACTrajectoryChart 假设节点含 `mac` 字段(VERIFICATION W5),由 13-07 后端补齐,前端读取对齐
3. 缺失 vendor 集成(VERIFICATION W4):新增 `queryMACVendor(mac)` API 客户端,TrajectoryPage 在拿到轨迹后并行调用并把厂商名合并到每个节点的 `vendor` 字段,Tooltip 显示
4. **修复 shipped tooltip index bug(13-04 遗留 CR)**:MACTrajectoryChart 旧代码 tooltip formatter 行 97-101 通过 `data[4]/data[5]/data[6]` 读取 MAC/设备/事件,但 seriesData value 数组(行 69-77)实际索引是 `[deviceIdx, startTime, endTime, duration, eventType, mac, deviceName]` — data[4]=eventType、data[5]=mac、data[6]=deviceName,导致 tooltip 三行错位。本次重写 formatter 通过闭包内 `nodes[params.dataIndex]` 节点对象访问字段,既修正 bug 又新增 vendor 行。

Purpose: 让 ECharts Gantt tooltip 能展示完整信息(MAC / 厂商 / 设备 / 端口 / VLAN / 停留时长 / 事件类型),从 5 项扩展到 7 项;修复 trajectory 永远为空数组的运行时 bug;修复 shipped tooltip index 错位 bug。
Output: 前端 3 个文件修改 + `npm run type-check` 退出码 0。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-query-layer-trajectory/13-VERIFICATION.md (CR-01 + CR-03 + W4 + W5)
@.planning/phases/13-query-layer-trajectory/13-CONTEXT.md (D-13-4.2 tooltip 字段)
@.planning/phases/13-query-layer-trajectory/13-07-PLAN.md (后端 TrajectoryNode.MACAddress 字段)
@.planning/CLAUDE.md § Frontend Conventions (API 调用 + useEffect 依赖)

# 当前实现状态(无需重新读取)
- xingran-react-frontend/src/lib/api/networkApi.ts:
  - 行 34-69: TrajectoryQueryParams + TrajectoryNode + TrajectoryResponse + queryMACTrajectory
  - 当前 bug:TrajectoryResponse 期望 `{ trajectory: [] }`,后端返回 `{ macAddress, nodes: [] }`
  - 当前 bug:TrajectoryNode 字段名是 camelCase(已正确),但缺 vendor 字段
- xingran-react-frontend/src/components/network/MACTrajectoryChart.tsx:
  - 行 8-18: 本地 TrajectoryNode 接口(无 vendor 字段)
  - 行 67-77: seriesData 用 node.interface/.vlanId/.startTime/.endTime/.duration/.eventType/.mac/.deviceName
  - 行 69-77 value 数组索引映射:[0]=deviceIdx, [1]=startTime, [2]=endTime, [3]=duration, [4]=eventType, [5]=mac, [6]=deviceName
  - 行 88-103: tooltip formatter 现有实现读取 data[4]/data[5]/data[6] — 🛑 SHIPPED BUG:index 错位(data[4] 实为 eventType 被当作 MAC;data[5] 实为 mac 被当作设备;data[6] 实为 deviceName 被当作事件)
- xingran-react-frontend/src/pages/network/mac/trajectory/TrajectoryPage.tsx:
  - 行 56-66: useQuery 调用 queryMACTrajectory → trajectoryData
  - 行 274-282: 渲染 MACTrajectoryChart
  - 缺:vendor 查询 + 节点合并逻辑
</context>

<tasks>

<task type="auto">
  <name>Task 1: queryMACTrajectory 解包修复 + 新增 queryMACVendor 工具函数 + TrajectoryQueryParams 字段名 camelCase 化</name>
<files>xingran-react-frontend/src/lib/api/networkApi.ts</files>
<read_first>
  - xingran-react-frontend/src/lib/api/networkApi.ts 行 34-69(当前 TrajectoryQueryParams/TrajectoryNode/TrajectoryResponse/queryMACTrajectory)
  - xingran-react-frontend/src/lib/api/networkApi.ts 行 169-202(exportMACHistory 私有 blobAxios 实例参考)
  - .planning/phases/14-frontend-ux/14-fix-04-PLAN.md (networkApi.ts 修改模式参考)
  - internal/services/mac_history_query_service.go 行 80(MACTrajectoryQuery 结构体,确认 JSON tag 是 camelCase:macAddress/StartTime/EndTime)
</read_first>
<action>
  1. **修复 TrajectoryResponse 解包**(行 52-69 区域):
     - 当前:`TrajectoryResponse { trajectory: TrajectoryNode[] }` + `return result.data!.trajectory;`
     - 改为:`TrajectoryResponse { macAddress: string; nodes: TrajectoryNode[] }` + `return result.data!.nodes;`
     - 同步调整 `TrajectoryNode` 接口(行 40-50):增加 `vendor?: string` 可选字段(允许后端不返回时省略,前端在合并时补)。
  2. **新增 queryMACVendor 工具函数**(行 69 之后或行 202 之后):
     - 签名:`export const queryMACVendor = async (mac: string): Promise<string> => { ... }`
     - 实现:`const result = await post<{ vendorName: string }>('/network/history/vendor', { mac }); return result.data?.vendorName ?? 'Unknown Vendor';`
     - 依赖:13-07 的 `GetVendorResponse` 字段名 `vendorName` 已变更,本函数按 camelCase 解包。
     - 错误处理:若后端返回 4xx/5xx,`post` 包装层会抛错,本函数直接向上抛(由调用方 useQuery 的 onError 处理)。
  3. **更新默认导出**(行 266-272):
     - 在 `export default { ... }` 中添加 `queryMACVendor`。
  4. **TrajectoryQueryParams 字段名修正**(行 34-38):
     - 当前:`mac / start_time / end_time`(snake_case)
     - 改为:`macAddress / startTime / endTime`(camelCase,与后端 `MACTrajectoryQuery` 的 JSON tag 对齐 — 后端 `internal/services/mac_history_query_service.go:80` 使用 `MACAddress string \`json:"macAddress"\``,Go 后端字段命名约定为 camelCase JSON,前端 queryParams 必须一致才能正确绑定请求体字段)。
     - 注意:TrajectoryPage.tsx 调用时使用 `queryParams.mac` / `queryParams.start_time` / `queryParams.end_time`,需同步调整(在 Task 3 中处理)。
  5. **验证**:`cd xingran-react-frontend && npx tsc --noEmit -p .` 退出码 0。
</action>
<verify>
  <automated>cd xingran-react-frontend && npx tsc --noEmit -p .  # 退出码 0</automated>
  <automated>grep -n "return result.data!.nodes" xingran-react-frontend/src/lib/api/networkApi.ts  # 至少 1 处命中</automated>
  <automated>grep -n "export const queryMACVendor" xingran-react-frontend/src/lib/api/networkApi.ts  # 至少 1 处命中</automated>
  <automated>grep -n "vendorName" xingran-react-frontend/src/lib/api/networkApi.ts  # 至少 1 处命中(查询函数解包)</automated>
  <automated>grep -n "return result.data!.trajectory" xingran-react-frontend/src/lib/api/networkApi.ts  # 必须为 0(已修复)</automated>
</verify>
<acceptance_criteria>
  - TrajectoryResponse 字段从 `trajectory` 改为 `macAddress` + `nodes`
  - queryMACTrajectory 函数返回 `result.data!.nodes`
  - TrajectoryNode 接口增加 `vendor?: string` 可选字段
  - 新增 `queryMACVendor(mac: string): Promise<string>` 导出函数
  - `result.data!.trajectory` 残留为 0
  - TrajectoryQueryParams 字段名 camelCase(macAddress/startTime/endTime)
  - TypeScript 编译 0 退出码
</acceptance_criteria>
<done>queryMACTrajectory 不再返回 undefined;queryMACVendor 就绪;字段名 camelCase 与后端一致;TypeScript 0 错误。</done>
</task>

<task type="auto">
  <name>Task 2: MACTrajectoryChart 修复 camelCase 字段读取 + tooltip formatter 改用节点对象访问(修复 shipped index bug + 新增 vendor 行)</name>
<files>xingran-react-frontend/src/components/network/MACTrajectoryChart.tsx</files>
<read_first>
  - xingran-react-frontend/src/components/network/MACTrajectoryChart.tsx 行 8-18(本地 TrajectoryNode 接口定义)
  - xingran-react-frontend/src/components/network/MACTrajectoryChart.tsx 行 67-77(seriesData value 数组构造 — 索引映射 [0]=deviceIdx, [1]=startTime, [2]=endTime, [3]=duration, [4]=eventType, [5]=mac, [6]=deviceName)
  - xingran-react-frontend/src/components/network/MACTrajectoryChart.tsx 行 88-103(tooltip formatter — shipped bug 区域,data[4]/data[5]/data[6] 错位)
  - xingran-react-frontend/src/lib/api/networkApi.ts 行 40-55(更新后的 TrajectoryNode 接口)
</read_first>
<action>
  1. **本地 TrajectoryNode 接口更新**(行 8-18):
     - 当前:已有 `mac / deviceId / deviceName / interface / vlanId / startTime / endTime / duration / eventType` 9 个字段(已 camelCase,无需改名)。
     - 增加:`vendor?: string` 可选字段(由 TrajectoryPage 在拿到轨迹后调用 queryMACVendor 合并)。
     - 备注:本组件是 `export interface TrajectoryNode`(行 8),与 networkApi.ts 的内部 TrajectoryNode 类型各自独立。修复方案:仅在本组件顶部新增 `vendor?: string` 字段,保持两处类型对齐(后续如需严格统一可单开重构任务,不在本 gap closure scope — 13-04 已合并)。
  2. **seriesData 字段读取确认**(行 67-77):
     - 当前读取:`node.interface / node.vlanId / node.startTime / node.endTime / node.duration / node.eventType / node.mac / node.deviceName`
     - 这些字段名在 13-07 后端 TrajectoryNode 输出后已全部对齐(`interface` 字段 Go JSON tag 是 `interface`,前端变量名也是 `interface`,无变更)。
     - **value 数组索引映射(关键不变量,Tooltip formatter 改造参考)**:`[0]=devices.indexOf(node.deviceName)`, `[1]=new Date(node.startTime).getTime()`, `[2]=new Date(node.endTime).getTime()`, `[3]=node.duration`, `[4]=node.eventType`, `[5]=node.mac`, `[6]=node.deviceName`。
     - 无需修改 value 数组本身(它服务于 ECharts custom series 的 renderItem,需要 position 索引);只需在 tooltip formatter 内通过节点对象访问,避免误读 value 数组下标。
  3. **Tooltip formatter 重写(REQUIRED,非 optional,修复 shipped index bug)**(行 88-105):
     - 🛑 **SHIPPED BUG 根因**:旧代码读取 `data[4]/data[5]/data[6]` 但这三个下标在 value 数组中实际是 `eventType/mac/deviceName`,导致 MAC 标签行实际显示 eventType、设备标签行实际显示 mac、事件标签行实际显示 deviceName,信息错位。
     - **REQUIRED 修复**:重写 formatter 闭包,在 useMemo 内捕获 `data` 引用,通过 `data[params.dataIndex]` 拿到节点对象,直接读字段:
       ```ts
       formatter: (params: any) => {
         const node = data[params.dataIndex];
         const durationHours = (node.duration / 3600).toFixed(1);
         const dwell = getDwellHeatmap(node.duration);
         return `
           <div>
             <span style="display:inline-block;padding:0 6px;background:${dwell.color};color:#fff;border-radius:2px;font-size:12px;">停留${dwell.label}</span><br/>
             <strong>MAC: ${node.mac}</strong><br/>
             厂商: ${node.vendor ?? 'Unknown'}<br/>
             设备: ${node.deviceName}<br/>
             端口: ${params.name}<br/>
             停留: ${durationHours}小时<br/>
             事件: ${node.eventType}<br/>
             VLAN: ${node.vlanId ?? '-'}
           </div>
         `;
       },
       ```
     - **关键依赖**:useMemo 内 `data` 数组闭包(ECharts tooltip formatter 在 useMemo return 的 option 内访问 `data`,可正确捕获当前 data 数组)。ECharts custom series 的 `params.dataIndex` 即 seriesData 数组下标,与 data 数组下标一一对应(seriesData 是 data.map 产出,顺序一致)。
     - **优势**:① 修复 shipped index bug;② 一次性新增 vendor + VLAN 行(7 项 tooltip 信息);③ 不依赖脆弱的下标约定,后续字段增减无需同步修改 formatter。
  4. **title 字段来源确认**(行 85):
     - 当前:`text: \`MAC轨迹: ${data[0]?.mac || "N/A"}\``
     - 由于 13-07 已保证每个 node 含 `mac` 字段(同顶层 macAddress),此处保留即可(已正确)。
  5. **验证**:`cd xingran-react-frontend && npx tsc --noEmit -p .` 退出码 0。
</action>
<verify>
  <automated>cd xingran-react-frontend && npx tsc --noEmit -p .  # 退出码 0</automated>
  <automated>grep -n "node.vendor\|\\.vendor" xingran-react-frontend/src/components/network/MACTrajectoryChart.tsx  # 至少 1 处命中</automated>
  <automated>grep -n "厂商: " xingran-react-frontend/src/components/network/MACTrajectoryChart.tsx  # 至少 1 处命中</automated>
  <automated>grep -n "nodes\[params.dataIndex\]\|data\[params.dataIndex\]" xingran-react-frontend/src/components/network/MACTrajectoryChart.tsx  # 至少 1 处命中(tooltip 改用节点对象访问)</automated>
  <automated>grep -nE "device_name|port_name|vlan_id" xingran-react-frontend/src/components/network/MACTrajectoryChart.tsx  # 必须为 0(无 snake_case 残留)</automated>
  <automated>grep -nE "data\[4\]|data\[5\]|data\[6\]" xingran-react-frontend/src/components/network/MACTrajectoryChart.tsx  # 必须为 0(tooltip 不再依赖 value 数组下标)</automated>
</verify>
<acceptance_criteria>
  - 本地 TrajectoryNode 接口新增 `vendor?: string` 字段
  - tooltip formatter 改用节点对象访问(`data[params.dataIndex]`),不再读 `data[4]/data[5]/data[6]`
  - tooltip 输出含 7 行:MAC / 厂商 / 设备 / 端口 / 停留 / 事件 / VLAN
  - 无 snake_case 字段名残留(device_name/port_name/vlan_id 全部清除)
  - 字段读取 node.interface/.vlanId/.startTime/.endTime/.duration/.eventType/.mac/.deviceName 全部保留
  - shipped index bug 修复(value 数组索引错位不再影响 tooltip)
  - TypeScript 编译 0 退出码
</acceptance_criteria>
<done>MACTrajectoryChart 字段读取对齐 13-07 后端响应;tooltip formatter 改用节点对象访问,修复 shipped index bug + 新增厂商/VLAN 2 行 tooltip 信息;无 snake_case 残留;无 value 数组下标误用残留。</done>
</task>

<task type="auto">
  <name>Task 3: TrajectoryPage 集成 vendor 查询 + 字段名 camelCase 同步 + vendor 合并到节点</name>
<files>xingran-react-frontend/src/pages/network/mac/trajectory/TrajectoryPage.tsx</files>
<read_first>
  - xingran-react-frontend/src/pages/network/mac/trajectory/TrajectoryPage.tsx (整个文件,~325 行)
  - xingran-react-frontend/src/lib/api/networkApi.ts (更新后的 queryMACTrajectory + 新增 queryMACVendor + TrajectoryQueryParams 字段名)
</read_first>
<action>
  1. **TrajectoryQueryParams 字段名同步**(行 36-40):
     - 当前:`{ mac: string; start_time: string; end_time: string }`
     - 改为:`{ macAddress: string; startTime: string; endTime: string }`(与 13-08 Task 1 + 后端 MACTrajectoryQuery 对齐)。
  2. **导入 queryMACVendor**(行 14 之后):
     - 当前:`import { queryMACTrajectory } from "@/lib/api/networkApi";`
     - 改为:`import { queryMACTrajectory, queryMACVendor } from "@/lib/api/networkApi";`
  3. **新增 vendor query**(行 56-66 之后):
     - 新增 useQuery:
       ```tsx
       const {
         data: vendorName,
         isLoading: vendorLoading,
       } = useQuery({
         queryKey: ["macVendor", queryParams?.macAddress],
         queryFn: () => queryMACVendor(queryParams!.macAddress),
         enabled: !!queryParams,
         staleTime: 24 * 60 * 60 * 1000, // 24h 缓存(与后端 OUI 缓存一致)
       });
       ```
  4. **数据合并 + 渲染节点 vendor 字段**(行 274-282):
     - 用 useMemo 把 trajectoryData 与 vendorName 合并为 chartData:
       ```tsx
       const chartData = useMemo(() => {
         if (!trajectoryData) return [];
         return trajectoryData.map((node) => ({ ...node, vendor: vendorName }));
       }, [trajectoryData, vendorName]);
       ```
     - 把 `<MACTrajectoryChart data={trajectoryData || []} ...>` 改为 `<MACTrajectoryChart data={chartData} ...>`。
  5. **queryParams 字段引用同步**(全文):
     - 行 128-132:`setQueryParams({ mac: normalizedMAC, start_time: ..., end_time: ... })` → 改为 `macAddress / startTime / endTime`
     - 行 150-154:同上
     - 行 296-298:`<MACEventsTimeline mac={queryParams.mac} startTime={queryParams.start_time} endTime={queryParams.end_time} />` → 改为对应 camelCase 字段(注意 MACEventsTimeline props 签名需先确认,本任务不修改该组件)。
  6. **MACEventsTimeline props 适配**(可选):
     - 检查 `MACEventsTimeline` 的 props 签名:若仍是 `mac / startTime / endTime`,行 296-298 改 `mac={queryParams.macAddress} startTime={queryParams.startTime} endTime={queryParams.endTime}` 即可(若 props 期望 snake_case,需在 MACEventsTimeline 内同步调整)。
  7. **URL 参数预填同步**(行 105-136):
     - 当前 `useEffect` 读 `sp.get("mac")` / `sp.get("startTime")` / `sp.get("endTime")`,这些 URL 参数名保持不变(D-17 锁定,URL 端 snake_case 命名保留),但 setQueryParams 内部字段改 camelCase。
     - 行 128-132 setQueryParams 同步变更。
  8. **验证**:`cd xingran-react-frontend && npx tsc --noEmit -p .` 退出码 0。
</action>
<verify>
  <automated>cd xingran-react-frontend && npx tsc --noEmit -p .  # 退出码 0</automated>
  <automated>grep -n "queryMACVendor" xingran-react-frontend/src/pages/network/mac/trajectory/TrajectoryPage.tsx  # 至少 1 处命中</automated>
  <automated>grep -nE "mac: normalizedMAC|start_time:|end_time:" xingran-react-frontend/src/pages/network/mac/trajectory/TrajectoryPage.tsx  # 必须为 0(已改 camelCase)</automated>
  <automated>grep -n "macAddress: normalizedMAC\|startTime:.*toISOString\|endTime:.*toISOString" xingran-react-frontend/src/pages/network/mac/trajectory/TrajectoryPage.tsx  # 至少 3 处命中</automated>
  <automated>grep -n "data={chartData}" xingran-react-frontend/src/pages/network/mac/trajectory/TrajectoryPage.tsx  # 至少 1 处命中(vendor 合并后传入 chart)</automated>
</verify>
<acceptance_criteria>
  - TrajectoryPage 导入 queryMACVendor
  - 新增 useQuery 查询 vendor(以 macAddress 为 queryKey)
  - useMemo 合并 trajectoryData + vendorName 为 chartData(每个 node 含 vendor 字段)
  - MACTrajectoryChart 接收 chartData(而非原 trajectoryData)
  - queryParams 字段全部 camelCase(macAddress/startTime/endTime)
  - TypeScript 编译 0 退出码
  - URL 参数读取(URL snake_case)与 setQueryParams 内部 camelCase 字段桥接正确
</acceptance_criteria>
<done>TrajectoryPage 拉取轨迹后并行查 vendor 并合并到节点;queryParams 字段 camelCase 与 13-08 Task 1 + 后端对齐;TypeScript 0 错误。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → Backend | queryMACTrajectory 解包由 `trajectory` 改为 `nodes`;queryMACVendor 新增;字段名 camelCase 一致 |
| Component → Page | TrajectoryNode.vendor 字段从 page 通过 useMemo 合并后传入 chart |
| URL → Page state | URL snake_case 参数(D-17 锁定) → 内部 state camelCase 桥接 |
| ECharts tooltip formatter → node data | tooltip formatter 通过 `data[params.dataIndex]` 节点对象访问字段,不再依赖脆弱的 value 数组下标 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13W8-01 | Tampering | queryMACTrajectory 解包错误 | mitigate | 单元类型检查 + grep 验证 result.data!.trajectory 完全清除 |
| T-13W8-02 | Repudiation | vendor 合并时机 | mitigate | useQuery enabled: !!queryParams 避免空查询;useMemo deps 正确 |
| T-13W8-03 | Information Disclosure | vendor 缓存 24h | accept | 与后端 OUI Redis 缓存 TTL 一致,无敏感数据 |
| T-13W8-04 | Tampering | shipped tooltip index bug (data[4]/data[5]/data[6]) | mitigate | tooltip formatter 改用 `data[params.dataIndex]` 节点对象访问,grep 验证 data[4-6] tooltip 残留为 0 |
| T-13W8-SC | Tampering | npm/pip/cargo installs | mitigate | 无新依赖,仅 TS interface + 函数体变更 |
</threat_model>

<verification>
- `cd D:/code/ClaudeCode/xingran-go-backend/xingran-react-frontend && npx tsc --noEmit -p .` 退出码 0
- `grep "return result.data!.nodes" xingran-react-frontend/src/lib/api/networkApi.ts` 命中
- `grep "export const queryMACVendor" xingran-react-frontend/src/lib/api/networkApi.ts` 命中
- `grep "node.vendor\|厂商:" xingran-react-frontend/src/components/network/MACTrajectoryChart.tsx` 命中
- `grep "data\[params.dataIndex\]" xingran-react-frontend/src/components/network/MACTrajectoryChart.tsx` 命中(节点对象访问)
- `grep -E "data\[4\]|data\[5\]|data\[6\]" xingran-react-frontend/src/components/network/MACTrajectoryChart.tsx` 0 命中(无脆弱下标访问)
- `grep "data={chartData}" xingran-react-frontend/src/pages/network/mac/trajectory/TrajectoryPage.tsx` 命中
- `grep "result.data!.trajectory" xingran-react-frontend/src/lib/api/networkApi.ts` 0 命中
- `grep -E "start_time:|end_time:" xingran-react-frontend/src/pages/network/mac/trajectory/TrajectoryPage.tsx` 0 命中
</verification>

<success_criteria>
- queryMACTrajectory 不再返回 undefined,正确返回 TrajectoryNode[]
- queryMACVendor 工具就绪,TrajectoryPage 调用并合并 vendor 字段
- MACTrajectoryChart tooltip 改用节点对象访问,修复 shipped index bug + 显示 7 项完整信息(MAC / 厂商 / 设备 / 端口 / VLAN / 停留 / 事件)
- queryParams / TrajectoryQueryParams 字段全部 camelCase
- TypeScript 编译 0 退出码
- 无 snake_case 字段名残留
- 无 value 数组下标误用残留(data[4-6] tooltip 路径全部清除)
</success_criteria>

<output>
Create `.planning/phases/13-query-layer-trajectory/13-08-SUMMARY.md` when done
</output>
