---
phase: 13-query-layer-trajectory
plan: 07
type: execute
wave: 1
depends_on: []
gap_closure: true
gap_refs:
  - W1-CR-01
  - W1-CR-03
  - W4-vendor
files_modified:
  - internal/services/mac_history_query_service.go
  - internal/api/v1/network/mac_history_handler.go
  - internal/services/mac_history_query_service_test.go
autonomous: true
requirements:
  - QUERY-02
  - QUERY-04

must_haves:
  truths:
    - "TrajectoryNode JSON 响应包含 mac 字段(每节点均带 MAC 地址),用于前端 tooltip 展示"
    - "GetVendorResponse JSON 字段名统一为 camelCase(vendorName),与项目 Go 命名约定一致"
    - "MACTrajectoryResult 响应结构 macAddress 顶层字段保持不变(向后兼容)"
    - "单元测试覆盖 MACAddress 字段在 TrajectoryNode 上的填充行为"
  artifacts:
    - path: "internal/services/mac_history_query_service.go"
      provides: "TrajectoryNode.MACAddress 字段 + aggregateTrajectory 填充"
      contains: "MACAddress.*json:\"mac\""
    - path: "internal/api/v1/network/mac_history_handler.go"
      provides: "GetVendorResponse 字段名 camelCase 规范化"
      contains: "VendorName.*json:\"vendorName\""
    - path: "internal/services/mac_history_query_service_test.go"
      provides: "TrajectoryNode MACAddress 字段的单元测试"
      contains: "TestAggregateTrajectory.*MACAddress"
  key_links:
    - from: "internal/services/mac_history_query_service.go"
      to: "sys_device_mac_history"
      via: "rawEvent.MACAddress 透传到 TrajectoryNode.MACAddress"
      pattern: "MACAddress:\\s*evt\\.MACAddress"
    - from: "internal/api/v1/network/mac_history_handler.go"
      to: "GetVendorResponse"
      via: "响应 JSON 字段名变更"
      pattern: "json:\\\"vendorName\\\""
---

<objective>
补齐 Phase 13 后端响应结构的两个 contract 缺陷:
1. `TrajectoryNode` 结构体缺少 `MACAddress` 字段(VERIFICATION CR-03):每个轨迹节点无法在 tooltip 中独立展示 MAC 地址,只能依赖顶层 `MACTrajectoryResult.MACAddress` 间接携带。
2. `GetVendorResponse` 字段名是 `vendor_name` (snake_case,VERIFICATION W4),与项目其他响应字段约定(camelCase,如 `macAddress`)不一致,前端需为单字段做特殊转换。

Purpose: 修复 CR-03 让前端 tooltip 能直接读 `node.mac` 展示每节点 MAC;修复 W4 让 vendor 响应字段名与全项目对齐,前端 `result.data.vendorName` 通用解包能直接命中。
Output: 后端 3 个文件修改 + 1 个测试覆盖 + `go build ./...` 退出码 0。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-query-layer-trajectory/13-VERIFICATION.md (CR-03 + W4)
@.planning/phases/13-query-layer-trajectory/13-CONTEXT.md (D-13-1.2 区间聚合)
@.planning/phases/13-query-layer-trajectory/13-RESEARCH.md (Pattern 1 TrajectoryNode 定义参考)
@.planning/phases/13-query-layer-trajectory/13-01-SUMMARY.md (TrajectoryNode 现状)
@.planning/phases/13-query-layer-trajectory/13-03-SUMMARY.md (GetVendor 现状)
@.planning/CLAUDE.md (Go 命名约定 + JSON 标签)

# 当前实现状态(无需重新读取)
- internal/services/mac_history_query_service.go:
  - 行 86-95: TrajectoryNode 结构体,缺 MACAddress 字段
  - 行 98-101: MACTrajectoryResult 含 macAddress 顶层字段
  - 行 104-118: rawEvent 已有 MACAddress 字段
  - 行 918-969: aggregateTrajectory 聚合逻辑
- internal/api/v1/network/mac_history_handler.go:
  - 行 129-136: GetVendorRequest + GetVendorResponse,响应字段名 vendor_name (snake_case)
  - 行 149-163: GetVendor handler
- internal/services/mac_history_query_service_test.go:
  - 行 70-150: TestAggregateTrajectory 已存在
</context>

<tasks>

<task type="auto">
  <name>Task 1: TrajectoryNode.MACAddress 字段补齐 + aggregateTrajectory 填充 + 测试更新</name>
  <files>internal/services/mac_history_query_service.go, internal/services/mac_history_query_service_test.go</files>
  <read_first>
    - internal/services/mac_history_query_service.go 行 86-95(TrajectoryNode 当前定义)
    - internal/services/mac_history_query_service.go 行 918-969(aggregateTrajectory 实现)
    - internal/services/mac_history_query_service_test.go 行 69-200(TestAggregateTrajectory 测试用例)
  </read_first>
  <action>
    1. **TrajectoryNode 结构体扩展**(行 86-95 区域):
       - 在 `TrajectoryNode` struct 中新增 `MACAddress string \`json:\"mac\"\`` 字段。
       - 字段位置:放在 `DeviceID` 之后(与现有字段顺序:DeviceID, DeviceName, Interface, VLANID, EventType 保持视觉对齐)。
       - JSON tag 用 `mac`(单字段最短,非全名),与 MACTrajectoryResult 顶层 `macAddress` 区分(顶层是查询输入,节点是单条轨迹内嵌)。
    2. **aggregateTrajectory 填充逻辑更新**(行 918-969 区域):
       - 第一个节点初始化块(`current = &TrajectoryNode{...}` 共 2 处,行 929-937 与行 949-957):为 `current.MACAddress = evt.MACAddress`。
       - 区间合并分支(同一位置,行 941-943)无需设置 MACAddress,因为复合键比较已保证 mac 相同。
       - **关键不变量**:每个 TrajectoryNode 的 MACAddress 必须与 MACTrajectoryResult.MACAddress 顶层值一致(因为 WHERE mac_address = ? 已过滤单 MAC)。
    3. **单元测试更新**(TestAggregateTrajectory 函数):
       - 读取行 70-200 的现有测试用例结构(同一位置/不同位置/合并边界)。
       - 在每个 `assert` 块追加 `assert.Equal(t, \"AABBCCDDEEFF\", node.MACAddress)`(或对应测试 MAC 值)。
       - 增加 1 个子用例 `TestAggregateTrajectory_MACAddressPropagation`:传入 2 个 rawEvent(不同位置但同 MAC),验证两个 TrajectoryNode 的 MACAddress 字段都被正确填充。
    4. **验证**:`go build ./...` 退出码 0;`go test -v -run TestAggregateTrajectory ./internal/services/` 全部 PASS。
  </action>
  <verify>
    <automated>go build ./...  # 退出码 0</automated>
    <automated>go test -v -run TestAggregateTrajectory ./internal/services/  # 全部 PASS</automated>
    <automated>grep -n "MACAddress.*json:\"mac\"" internal/services/mac_history_query_service.go  # 至少 1 处命中</automated>
    <automated>grep -n "current.MACAddress = evt.MACAddress\|MACAddress:\s*evt.MACAddress" internal/services/mac_history_query_service.go  # 至少 2 处命中(两个初始化点)</automated>
    <automated>grep -c "assert.Equal.*MACAddress" internal/services/mac_history_query_service_test.go  # 大于等于 1(测试断言)</automated>
  </verify>
  <acceptance_criteria>
    - TrajectoryNode 结构体新增 `MACAddress string \`json:\"mac\"\`` 字段
    - aggregateTrajectory 在两处初始化 current 的地方都设置 `current.MACAddress = evt.MACAddress`
    - TestAggregateTrajectory 至少 1 处断言 `assert.Equal(..., node.MACAddress, ...)`
    - `go build ./...` 退出码 0
    - `go test -v -run TestAggregateTrajectory ./internal/services/` 全部通过
    - 无回归:TrajectoryNode 已有 7 个 JSON 字段全部保留(DeviceID/DeviceName/Interface/VLANID/EventType/StartTime/EndTime/Duration)
  </acceptance_criteria>
  <done>TrajectoryNode JSON 序列化输出含 `mac` 字段;单元测试覆盖 MACAddress 字段填充;go build + go test 全绿;无其他字段回归。</done>
</task>

<task type="auto">
  <name>Task 2: GetVendorResponse 字段名 camelCase 规范化(vendor_name → vendorName)</name>
  <files>internal/api/v1/network/mac_history_handler.go</files>
  <read_first>
    - internal/api/v1/network/mac_history_handler.go 行 128-163(GetVendorRequest + GetVendorResponse + GetVendor handler)
    - .planning/CLAUDE.md § Go Code Style(JSON 标签约定)
  </read_first>
  <action>
    1. **修改 GetVendorResponse 字段 JSON tag**(行 134-136 区域):
       - 当前:`VendorName string \`json:\"vendor_name\"\``
       - 改为:`VendorName string \`json:\"vendorName\"\``
       - 依据:与项目其他响应字段一致(如 `macAddress` / `deviceName` / `vlanId`),便于前端 `result.data.vendorName` 通用解包,无需特殊映射。
    2. **Swagger 注释保持不变**:handler 上的 `@Success {object} response.Response{data=GetVendorResponse}` 自动反映结构体变更,无需手改。
    3. **方法实现不变**:handler 内部 `response.Success(c, GetVendorResponse{VendorName: vendorName})` 不变,只是序列化输出字段名变更。
    4. **路由不变**:`POST /network/history/vendor` 保持(VERIFICATION.md 说 GET 是错误的,实际代码是 POST + ShouldBindJSON,语义正确)。
    5. **operlog 检查**:GetVendor 是读操作,不写 sys_oper_log,无需 `operlog.Record` 调用(参考 CLAUDE.md 强制约束:所有业务**写**操作 handler 必须在 success path 末尾、response.Success(...) 之前调用 operlog.Record(...))。
    6. **验证**:`go build ./...` 退出码 0;无新增测试(本任务只改 JSON 标签,无业务逻辑变更)。
  </action>
  <verify>
    <automated>go build ./...  # 退出码 0</automated>
    <automated>grep -n "json:\"vendorName\"" internal/api/v1/network/mac_history_handler.go  # 至少 1 处命中</automated>
    <automated>grep -n "json:\"vendor_name\"" internal/api/v1/network/mac_history_handler.go  # 必须为 0(已删除)</automated>
  </verify>
  <acceptance_criteria>
    - GetVendorResponse.VendorName JSON tag 改为 `vendorName`
    - Go 编译退出码 0
    - 无 snake_case 字段名残留
  </acceptance_criteria>
  <done>Vendor 查询响应 JSON 字段与全项目 camelCase 约定一致;无 snake_case 残留;编译通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API consumer → backend response | TrajectoryNode 新增 mac 字段;GetVendorResponse 字段名变更为 vendorName — 前端需同步消费(13-08 处理) |
| Backend Go type → JSON wire format | JSON tag 变更直接影响 wire format,需同步前端 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13W7-01 | Tampering | TrajectoryNode.MACAddress 字段 | mitigate | 单元测试断言 MACAddress 与 rawEvent.MACAddress 一致,防漂移 |
| T-13W7-02 | Repudiation | GetVendorResponse 字段名变更 | mitigate | Grep 验证 `vendor_name` 完全清除;前端 13-08 同步消费 vendorName |
| T-13W7-SC | Tampering | npm/pip/cargo installs | mitigate | 无新依赖,仅 struct field 增删 + json tag 变更 |
</threat_model>

<verification>
- `go build ./...` 退出码 0
- `go test -v -run TestAggregateTrajectory ./internal/services/` 全部 PASS
- `grep -n 'json:"mac"' internal/services/mac_history_query_service.go` 命中
- `grep -n 'json:"vendorName"' internal/api/v1/network/mac_history_handler.go` 命中
- `grep -n 'json:"vendor_name"' internal/api/v1/network/mac_history_handler.go` 0 命中
</verification>

<success_criteria>
- TrajectoryNode JSON 响应包含 `mac` 字段,前端可读 `node.mac`
- GetVendorResponse 字段名为 `vendorName`(camelCase)
- Go 编译 0 退出码
- 单元测试覆盖 MACAddress 字段填充
- 无 snake_case 字段名残留
</success_criteria>

<output>
Create `.planning/phases/13-query-layer-trajectory/13-07-SUMMARY.md` when done
</output>
