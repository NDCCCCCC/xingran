---
phase: 13-query-layer-trajectory
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/services/mac_history_query_service.go
  - internal/services/mac_history_query_service_test.go
  - internal/api/v1/network/mac_history_handler.go
autonomous: true
requirements:
  - QUERY-03

must_haves:
  truths:
    - "用户可查询指定时间范围内每条 MAC 连接的停留时长（明细）"
    - "返回长期占用 Top-N（按 MAC 聚合）和端口热点 Top-N（按端口聚合）"
    - "停留时长 = last_seen - first_seen 秒（固化区间）"
    - "长期占用阈值通过 sys_config.network.mac.history.long_occupancy_threshold_days 可配置（默认 30 天）"
  artifacts:
    - path: "internal/services/mac_history_query_service.go"
      provides: "QueryConnectionStats 接口方法 + 实现"
      contains: "func (s \\*macHistoryQueryServiceImpl) QueryConnectionStats"
    - path: "internal/services/mac_history_query_service.go"
      provides: "统计请求/响应类型"
      contains: "ConnectionStatsResponse"
    - path: "internal/services/mac_history_query_service_test.go"
      provides: "连接时长统计单元测试"
      contains: "func TestQueryConnectionStats"
    - path: "internal/api/v1/network/mac_history_handler.go"
      provides: "替换 GetStats TODO 为 QueryConnectionStats"
      contains: "func \\(h \\*MACHistoryHandler\\) QueryConnectionStats"
  key_links:
    - from: "internal/api/v1/network/mac_history_handler.go"
      to: "internal/services/mac_history_query_service.go"
      via: "QueryConnectionStats 调用"
      pattern: "historyQueryService.QueryConnectionStats"
    - from: "internal/services/mac_history_query_service.go"
      to: "sys_config"
      via: "GetConfigByKey 查询长期占用阈值"
      pattern: "network.mac.history.long_occupancy_threshold_days"
---

<objective>
实现连接时长统计 API（QUERY-03），输出明细（每个 MAC × 端口的停留时长 + flapping 计数）+ Top-N（按 MAC 长期占用 Top + 按端口热门连接 Top）三段式结构。替换原 `GetStats` TODO stub。

Purpose: 替换 mac_history_handler.go:83 的 TODO stub；提供运维"看见设备长期占用"的能力。
Output: service.go + handler.go 修改 + test.go 新增。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-query-layer-trajectory/13-CONTEXT.md
@.planning/phases/13-query-layer-trajectory/13-RESEARCH.md
@.planning/phases/13-query-layer-trajectory/13-PATTERNS.md
@.planning/phases/13-query-layer-trajectory/13-VALIDATION.md
@.planning/phases/12-data-model-integration/12-CONTEXT.md
@internal/services/mac_history_query_service.go
@internal/api/v1/network/mac_history_handler.go
@internal/services/mac_history_service.go
@internal/models/device_mac_history.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 service 层实现 QueryConnectionStats（含明细 + Top-N + 阈值配置）</name>
  <files>
    internal/services/mac_history_query_service.go
    internal/services/mac_history_query_service_test.go
  </files>
  <read_first>
    - internal/services/mac_history_query_service.go (lines 99-117 maxQueryRange, lines 132-149 Count + paginate 模式)
    - internal/api/v1/network/mac_history_handler.go (line 83 GetStats TODO stub)
    - .planning/phases/13-query-layer-trajectory/13-CONTEXT.md § 2 (D-13-2.1 ~ 2.4)
    - .planning/phases/13-query-layer-trajectory/13-RESEARCH.md § Pattern 2 (明细 + Top-N 两段式 SQL)
    - internal/models/config.go (sys_config 表模型，ConfigKey / ConfigValue 字段)
    - internal/services/system/config_service.go (ConfigService.GetConfigByKey 使用范例)
  </read_first>
  <action>
    1. 新增请求/响应类型（同 `mac_history_query_service.go`）：
       ```go
       type ConnectionStatsQuery struct {
           MACAddress string  // 可选，留空统计所有 MAC
           StartTime  string  // RFC3339, 必填
           EndTime    string  // RFC3339, 必填
           TopN       int     // 默认 10
       }
       type ConnectionStatsDetail struct {
           MACAddress      string    `json:"macAddress"`
           DeviceID        string    `json:"deviceId"`
           DeviceName      string    `json:"deviceName"`
           Interface       string    `json:"interface"`
           FirstSeen       time.Time `json:"firstSeen"`
           LastSeen        time.Time `json:"lastSeen"`
           Duration        int64     `json:"duration"`         // 秒
           EventCount      int       `json:"eventCount"`
           FlappingCount   int       `json:"flappingCount"`    // event_type='moved' 计数
           IsLongOccupancy bool      `json:"isLongOccupancy"`  // duration > threshold
       }
       type LongOccupancyByMAC struct {
           MACAddress    string `json:"macAddress"`
           Vendor        string `json:"vendor"`         // Phase 13 留空字符串，由前端 OUI 调用补齐
           TotalDuration int64  `json:"totalDuration"`  // 秒
           PortCount     int    `json:"portCount"`
       }
       type HotspotByPort struct {
           DeviceID       string `json:"deviceId"`
           DeviceName     string `json:"deviceName"`
           Interface      string `json:"interface"`
           UniqueMACCount int    `json:"uniqueMacCount"`
           TotalDuration  int64  `json:"totalDuration"`  // 秒
       }
       type ConnectionStatsResponse struct {
           Details             []ConnectionStatsDetail `json:"details"`
           TopByMAC            []LongOccupancyByMAC    `json:"topByMac"`
           TopByPort           []HotspotByPort         `json:"topByPort"`
           LongOccupancyDays   int                     `json:"longOccupancyDays"`  // 当前阈值
       }
       ```

    2. 接口扩展：`MACHistoryQueryService` 增加 `QueryConnectionStats(ctx context.Context, req *ConnectionStatsQuery) (*ConnectionStatsResponse, error)`。

    3. 新增 helper `getLongOccupancyThreshold(ctx context.Context) (int, error)`：
       - 查 `sys_config` 表 `config_key = 'network.mac.history.long_occupancy_threshold_days'`
       - 存在且为整数 → 返回天数；不存在或解析失败 → 返回默认值 30
       - 用 `db.Table("sys_config").Where("config_key = ?", key).Select("config_value").Row().Scan(&value)` 单字段查询

    4. 实现 `(*macHistoryQueryServiceImpl).QueryConnectionStats`：
       - 校验 StartTime/EndTime 必填且跨度 ≤ 365 天（**复用 maxQueryRange**）
       - 调 `getLongOccupancyThreshold(ctx)` 拿到阈值天 → 转秒 `thresholdSec := days * 86400`
       - 设置 TopN 默认 10
       - **明细 SQL**（一条 Raw）：
         ```sql
         SELECT mac_address, device_id, device_name_snapshot, interface_name,
                MIN(first_seen) AS first_seen, MAX(last_seen) AS last_seen,
                EXTRACT(EPOCH FROM (MAX(last_seen) - MIN(first_seen)))::bigint AS duration,
                COUNT(*) AS event_count,
                COUNT(*) FILTER (WHERE event_type = 'moved') AS flapping_count
         FROM sys_device_mac_history
         WHERE first_seen >= ? AND first_seen <= ?
         [AND mac_address = ?]   -- 若 req.MACAddress 非空
         GROUP BY mac_address, device_id, device_name_snapshot, interface_name
         ORDER BY duration DESC
         LIMIT ? OFFSET ?
         ```
         `IsLongOccupancy = duration > thresholdSec` 在 Go 端标记。
       - **TopByMAC SQL**（一条 Raw）：
         ```sql
         SELECT mac_address,
                SUM(EXTRACT(EPOCH FROM (MAX(last_seen) - MIN(first_seen))))::bigint AS total_duration,
                COUNT(DISTINCT device_id || ':' || interface_name) AS port_count
         FROM sys_device_mac_history
         WHERE first_seen >= ? AND first_seen <= ?
         GROUP BY mac_address
         HAVING SUM(EXTRACT(EPOCH FROM (MAX(last_seen) - MIN(first_seen)))) > ?
         ORDER BY total_duration DESC
         LIMIT ?
         ```
       - **TopByPort SQL**（一条 Raw）：
         ```sql
         SELECT device_id, device_name_snapshot, interface_name,
                COUNT(DISTINCT mac_address) AS unique_mac_count,
                SUM(EXTRACT(EPOCH FROM (MAX(last_seen) - MIN(first_seen))))::bigint AS total_duration
         FROM sys_device_mac_history
         WHERE first_seen >= ? AND first_seen <= ?
         GROUP BY device_id, device_name_snapshot, interface_name
         ORDER BY total_duration DESC
         LIMIT ?
         ```
       - 三个 SQL 都用 `s.db.WithContext(ctx).Raw().Scan(&dest)` 模式
       - 返回 `&ConnectionStatsResponse{Details, TopByMAC, TopByPort, LongOccupancyDays: days}`

    5. 单元测试 `mac_history_query_service_test.go` 新增：
       - `TestQueryConnectionStats` — 插入 5 条测试数据（不同 MAC/接口），验证 Details 数 = 3 组（按 MAC×Device×Interface 聚合）、TopByMAC 按总时长排序、TopByPort 按 port 时长排序
       - `TestLongOccupancyFilter` — 验证 IsLongOccupancy 标记逻辑（threshold = 30 days → duration=29d 标记 false, 31d 标记 true），不依赖 sys_config 表存在性（直接 mock getLongOccupancyThreshold 或用临时 config 记录）
       - `TestGetLongOccupancyThreshold` — sys_config 不存在记录 → 返回 30 默认值；存在 config_key 但 config_value 非数字 → 返回 30
  </action>
  <acceptance_criteria>
    - `go build ./...` 0 errors
    - `go test -v -run "TestQueryConnectionStats|TestLongOccupancyFilter|TestGetLongOccupancyThreshold" ./internal/services/...` exit 0
    - 文件 `internal/services/mac_history_query_service.go` 包含字符串 `func (s *macHistoryQueryServiceImpl) QueryConnectionStats`
    - 文件 `internal/services/mac_history_query_service.go` 包含字符串 `network.mac.history.long_occupancy_threshold_days`
    - 文件 `internal/services/mac_history_query_service.go` 包含字符串 `LongOccupancyByMAC` 和 `HotspotByPort`
    - 文件 `internal/services/mac_history_query_service.go` 的 `MACHistoryQueryService` 接口包含 `QueryConnectionStats`
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go test -v -run "TestQueryConnectionStats|TestLongOccupancyFilter|TestGetLongOccupancyThreshold" ./internal/services/...</automated>
  </verify>
  <done>
    - 明细 (按 MAC×Device×Interface 聚合) + TopByMAC + TopByPort 三段式输出
    - 长期占用阈值从 sys_config 读取，默认 30 天
    - Flapping 计数 = event_type='moved' 计数
    - 时间跨度 DoS 保护复用 maxQueryRange
  </done>
</task>

<task type="auto">
  <name>Task 2: 替换 mac_history_handler.go 的 GetStats TODO stub</name>
  <files>
    internal/api/v1/network/mac_history_handler.go
  </files>
  <read_first>
    - internal/api/v1/network/mac_history_handler.go (lines 74-86 GetStats 现有 TODO)
    - .planning/phases/13-query-layer-trajectory/13-CONTEXT.md § Claude's Discretion (统计 API 返回 JSON 结构由 Claude 决定)
    - pkg/response/handler_helpers.go
  </read_first>
  <action>
    1. 替换 mac_history_handler.go lines 74-86 GetStats：
       - 删除旧 `GetStats` 方法（line 83-86）和 TODO
       - 新增：
         ```go
         // QueryConnectionStats 查询连接时长统计
         // @Summary 查询MAC连接时长统计
         // @Description 输出明细(MAC×端口停留时长)+ Top-N(按MAC长期占用、端口热门连接)
         // @Tags 网络设备管理
         // @Accept json
         // @Produce json
         // @Param request body mac_history_query_service.ConnectionStatsQuery true "统计条件"
         // @Success 200 {object} response.Response{data=mac_history_query_service.ConnectionStatsResponse}
         // @Failure 400 {object} response.Response
         // @Failure 500 {object} response.Response
         // @Router /network/history/stats [post]
         func (h *MACHistoryHandler) QueryConnectionStats(c *gin.Context) {
             var req mac_history_query_service.ConnectionStatsQuery
             if !responseHelpers.HandleJSONBinding(c, &req) {
                 return
             }
             result, err := h.historyQueryService.QueryConnectionStats(c.Request.Context(), &req)
             if !responseHelpers.HandleServiceError(c, err, "查询连接时长统计") {
                 return
             }
             response.Success(c, result)
         }
         ```

    2. 路由文件 `mac_history_router.go` 同步：把 `r.POST("/history/stats", historyHandler.GetStats)` 改为 `r.POST("/history/stats", historyHandler.QueryConnectionStats)`；`applogger.Infof` 消息不变。
  </action>
  <acceptance_criteria>
    - `go build ./...` 0 errors
    - 文件 `internal/api/v1/network/mac_history_handler.go` 包含 `func (h *MACHistoryHandler) QueryConnectionStats`
    - 文件 `internal/api/v1/network/mac_history_handler.go` 不再包含 `TODO: 实现统计功能`
    - 文件 `internal/api/v1/network/mac_history_handler.go` 不再包含 `func (h *MACHistoryHandler) GetStats`
    - 文件 `internal/api/v1/network/mac_history_router.go` 包含 `historyHandler.QueryConnectionStats`
    - Swagger 注释完整 5 项必备 tag
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && grep -E 'QueryConnectionStats|GetStats|TODO' internal/api/v1/network/mac_history_handler.go | grep -v '^//' | head -20</automated>
  </verify>
  <done>
    - GetStats TODO stub 完全替换为 QueryConnectionStats 实现
    - 端点路径保持 /history/stats 不变（向后兼容）
    - Swagger 注释完整
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Client 时间范围 + MAC 地址穿越此处 |
| Service → DB | GORM Raw SQL 参数化 + sys_config 读 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Tampering | QueryConnectionStats SQL | mitigate | GORM `?` 参数化；StartTime/EndTime 必填校验；MAC 格式同 Plan 01 校验 |
| T-13-02-DOS | DoS | QueryConnectionStats | mitigate | 复用 `maxQueryRange = 365 * 24 * time.Hour` 限制跨度 |
| T-13-02-CFG | Info Disclosure | sys_config 读 | accept | config_value 仅阈值数字，无敏感信息 |
</threat_model>

<verification>
- `go build ./...` 0 errors
- `go test -v -run "TestQueryConnectionStats|TestLongOccupancyFilter|TestGetLongOccupancyThreshold" ./internal/services/...` 全部通过
- `grep -c "QueryConnectionStats" internal/services/mac_history_query_service.go internal/api/v1/network/mac_history_handler.go` ≥ 4
- `grep -c "TODO: 实现统计功能" internal/api/v1/network/mac_history_handler.go` = 0
</verification>

<success_criteria>
- 调用 /history/stats 返回 ConnectionStatsResponse（含 details/topByMac/topByPort/longOccupancyDays）
- 长期占用 Top-N 仅包含 duration > 阈值秒的 MAC
- 端口热点 Top-N 按端口总停留时长降序
- 默认阈值 = 30 天，可被 sys_config 记录覆盖
</success_criteria>

<output>
Create `.planning/phases/13-query-layer-trajectory/13-02-SUMMARY.md` when done
</output>