---
phase: 13-query-layer-trajectory
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/router.go
  - .planning/phases/12-data-model-integration/12-UAT.md
autonomous: true
requirements:
  - UAT-REPAIR

must_haves:
  truths:
    - "SetupMACHistoryRouter已在router.go正确注册"
    - "MAC历史清理任务在scheduler中正确注册"
    - "UAT报告已更新，反映最新验证状态"
  artifacts:
    - path: "internal/api/router.go"
      provides: "主路由文件，验证SetupMACHistoryRouter注册"
      contains: "SetupMACHistoryRouter"
    - path: ".planning/phases/12-data-model-integration/12-UAT.md"
      provides: "Phase 12 UAT验证报告"
      contains: "## UAT Status|BLOCKING|RESOLVED"
  key_links:
    - from: "internal/api/router.go"
      to: "internal/api/v1/network/mac_history_router.go"
      via: "路由注册调用"
      pattern: "SetupMACHistoryRouter.*r"
    - from: "internal/scheduler/"
      to: "sys_config.network.mac.history.retention_days"
      via: "清理任务调度"
      pattern: "AddJob.*mac.*cleanup"
---

<objective>
验证并修复Phase 12的UAT阻塞项，确认路由注册状态和清理任务scheduler注册状态，更新UAT报告反映最新状态。

Purpose: Phase 12遗留2个UAT阻塞项（路由注册验证 + 清理任务验证），Phase 13执行前必须确保基础设施正确。
Output: 验证报告更新 + 阻塞项清除（或确认已解决）。
</objective>

<verification_criteria>
- [ ] router.go:325包含SetupMACHistoryRouter调用且路径正确
- [ ] 清理任务在scheduler中注册（AddJob存在且cron表达式有效）
- [ ] 12-UAT.md已更新，反映当前验证状态
- [ ] 无新增BLOCKING项（所有已知问题已解决或降级）
</verification_criteria>

<tasks>
# Task 1: 验证路由注册状态
<read_first>
- internal/api/router.go - 主路由文件，第325行附近
- internal/api/v1/network/mac_history_router.go - MAC历史路由定义
- .planning/phases/12-data-model-integration/12-UAT.md - UAT阻塞项清单
</read_first>
<acceptance_criteria>
- router.go包含SetupMACHistoryRouter(router)调用
- 调用位置正确（在/v1路由组下）
- 12-UAT.md中"路由注册"阻塞项已更新为RESOLVED或保持BLOCKING（视验证结果）
</acceptance_criteria>
<action>
1. 验证internal/api/router.go第325行附近：
```bash
grep -n "SetupMACHistoryRouter" internal/api/router.go
```

预期输出：
```go
// MAC历史管理
setupMACHistoryRouter(v1Router.Group("/network/history"))
```

2. 如果调用存在且路径正确，更新12-UAT.md：
```markdown
### BLOCKING-001: 路由注册验证 ✅ RESOLVED (2026-06-13)
**状态:** 已解决
**验证:** router.go:325包含SetupMACHistoryRouter调用，路径/network/history正确
**验证人:** Phase 13 UAT修复任务
```

3. 如果调用缺失，标记为仍阻塞：
```markdown
### BLOCKING-001: 路由注册验证 ⛔ BLOCKING
**状态:** 仍阻塞
**问题:** router.go未调用SetupMACHistoryRouter
**需要:** 添加 `setupMACHistoryRouter(v1Router.Group("/network/history"))` 调用
```
</action>

# Task 2: 验证清理任务scheduler注册状态
<read_first>
- internal/scheduler/ - scheduler目录结构
- internal/services/mac_history_service.go - 清理逻辑实现
- .planning/phases/12-data-model-integration/12-UAT.md - 清理任务阻塞项
</read_first>
<acceptance_criteria>
- scheduler目录包含清理任务注册（AddJob或类似函数）
- cron表达式符合预期（如"0 2 * * *"表示每天凌晨2点）
- 清理任务调用mac_history_service的清理方法
- 12-UAT.md中"清理任务"阻塞项已更新状态
</acceptance_criteria>
<action>
1. 搜索scheduler中的清理任务注册：
```bash
grep -rn "mac.*cleanup\|cleanup.*mac\|retention_days" internal/scheduler/
```

2. 如果找到清理任务注册，验证cron表达式：
```bash
grep -A 5 "mac.*cleanup" internal/scheduler/*.go
```

预期输出应包含类似：
```go
AddJob("mac-history-cleanup", "0 2 * * *", func() {
    service.CleanupOldMACRecords(context.Background(), retentionDays)
})
```

3. 验证清理方法在mac_history_service.go中存在：
```bash
grep -n "CleanupOldMACRecords" internal/services/mac_history_service.go
```

4. 更新12-UAT.md：
- **如果任务已注册：** 标记为RESOLVED
- **如果任务未注册：** 保持BLOCKING，说明需要添加scheduler注册

示例更新（如果已解决）：
```markdown
### BLOCKING-002: 清理任务注册 ✅ RESOLVED (2026-06-13)
**状态:** 已解决
**验证:** scheduler包含MAC历史清理任务，cron表达式"0 2 * * *"有效
**清理方法:** mac_history_service.CleanupOldMACRecords已实现
**验证人:** Phase 13 UAT修复任务
```
</action>

# Task 3: 验证配置项存在性
<read_first>
- internal/core/config/config.go - 配置结构定义
- configs/config.yaml - 配置文件
- .planning/phases/13-query-layer-trajectory/13-CONTEXT.md - D-13-2.3配置阈值
</read_first>
<acceptance_criteria>
- config.go包含network.mac.history.retention_days字段
- config.yaml包含默认值（如90天）
- 配置可通过ConfigService查询
</acceptance_criteria>
<action>
1. 验证配置结构定义：
```bash
grep -rn "retention_days\|RetentionDays" internal/core/config/
```

2. 验证配置文件：
```bash
grep "mac.history.retention_days" configs/config.yaml
```

3. 如果配置缺失，在12-UAT.md记录（非阻塞，Phase 13不依赖此配置）：
```markdown
### INFO-001: 保留配置验证 ℹ️ INFO
**状态:** 信息性（非阻塞）
**配置项:** network.mac.history.retention_days
**建议:** 添加到config.yaml，默认值90天
```
</action>

# Task 4: 生成UAT验证总结报告
<read_first>
- .planning/phases/12-data-model-integration/12-UAT.md - 现有UAT报告
- .planning/phases/13-query-layer-trajectory/13-CONTEXT.md - Phase 13上下文
</read_first>
<acceptance_criteria>
- 12-UAT.md包含"## Phase 13 UAT Re-verification"章节
- 章节包含验证日期、验证结果、剩余阻塞项清单
- 验证结果明确（PASS/FAIL/PARTIAL）
- 无歧义的状态描述
</acceptance_criteria>
<action>
在.planning/phases/12-data-model-integration/12-UAT.md末尾添加：

```markdown
## Phase 13 UAT Re-verification

**验证日期:** 2026-06-13
**验证范围:** Phase 12 UAT阻塞项 + 路由/任务注册状态
**验证人:** Phase 13 UAT修复任务 (13-05-PLAN.md)

### 验证结果

**总体状态:** [PASS | FAIL | PARTIAL]

#### 已解决阻塞项
- ✅ BLOCKING-001: 路由注册验证 - RESOLVED (2026-06-13)
- ✅ BLOCKING-002: 清理任务注册 - RESOLVED (2026-06-13)

#### 仍阻塞项（如有）
- ⛔ [编号]: [问题描述] - BLOCKING
  **原因:** [为什么未解决]
  **建议:** [如何解决]

#### 信息性发现
- ℹ️ INFO-001: 保留配置验证 - INFO (非阻塞)

### 剩余风险评估

**影响Phase 13执行的风险:**
- **高风险:** [列出可能阻止Phase 13的风险]
- **中风险:** [列出可能影响功能但不阻塞的风险]
- **低风险:** [列出可忽略或可绕过的风险]

### 建议

1. **对于Phase 13执行:**
   - [建议项1]
   - [建议项2]

2. **对于后续Phase:**
   - [建议项1]
   - [建议项2]

---

**验证签名:** Phase 13 UAT修复任务
**下一步:** 如总体状态为PASS或PARTIAL，可继续Phase 13执行；如为FAIL，需先解决阻塞项。
```
</action>

# Task 5: 更新ROADMAP.md Phase 12状态（可选）
<read_first>
- .planning/ROADMAP.md - Phase 12章节
- .planning/STATE.md - 项目状态文件
</read_first>
<acceptance_criteria>
- ROADMAP.md Phase 12条目反映最新UAT状态
- 如所有阻塞项解决，Phase 12状态更新为"Complete"
- 或保持"In Progress"并附注"UAT修复进行中"
</acceptance_criteria>
<action>
如果UAT验证全部通过（总体状态PASS），更新ROADMAP.md：

```markdown
- [x] Phase 12: 数据模型与采集集成 (5/5 plans) — completed 2026-05-11, UAT verified 2026-06-13
```

如果仍有阻塞项，添加附注：
```markdown
- [ ] Phase 12: 数据模型与采集集成 (5/5 plans) — completed 2026-05-11, UAT re-verification in progress
```
</action>
</tasks>
