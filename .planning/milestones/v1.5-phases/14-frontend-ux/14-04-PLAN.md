<!-- Revised 2026-06-14 — plan-checker iteration 1: 修复 BLOCKER 1+2 + WARNING 1-4 -->
---
phase: 14-frontend-ux
plan: 04
type: execute
wave: 2
depends_on:
  - 14-03
  - 14-01
  - 14-05a
files_modified:
  - xingran-react-frontend/src/lib/api/networkApi.ts
  - xingran-react-frontend/src/pages/network/mac/history.tsx
  - xingran-react-frontend/src/pages/network/mac/history/index.tsx
autonomous: true
requirements:
  - UI-02

must_haves:
  truths:
    - "工具栏出现两个互斥按钮 '导出当前查询' / '导出全量',点击后浏览器下载 .xlsx 文件"
    - "导出当前查询:传入当前过滤条件(时间/MAC/设备/端口/VLAN/事件类型/状态),文件名含时间范围"
    - "导出全量:无过滤条件,文件名 mac_history_all_YYYYMMDD_HHmmss.xlsx"
    - "下载走 api.get(url, { params, responseType: 'blob' }) + URL.createObjectURL + a.download 模式(Phase 33 M2 锁定)"
    - "后端响应 Content-Type 异常(< 1KB blob 被识别为 JSON 错误)时,解析 JSON 抛业务错误"
    - "导出按钮的可见性由 `network:mac:export` 权限点严格控制(14-03 已注册),权限缺失时按钮不渲染"
  artifacts:
    - path: "xingran-react-frontend/src/lib/api/networkApi.ts"
      provides: "exportMACHistory 函数,返回 Blob"
      contains: "exportMACHistory"
    - path: "xingran-react-frontend/src/pages/network/mac/history.tsx"
      provides: "工具栏两个导出按钮(互斥),通过 props 接收 exportScope='current'|'all'"
      contains: "导出当前查询|导出全量"
  key_links:
    - from: "xingran-react-frontend/src/pages/network/mac/history.tsx"
      to: "xingran-react-frontend/src/lib/api/networkApi.ts"
      via: "exportMACHistory 调用"
      pattern: "exportMACHistory"
    - from: "xingran-react-frontend/src/lib/api/networkApi.ts"
      to: "POST /network/history/list?format=xlsx&exportScope=all"
      via: "api.get (axios 拦截器 + blob responseType)"
      pattern: "format=xlsx"
    - from: "xingran-react-frontend/src/pages/network/mac/history.tsx"
      to: "URL.createObjectURL + a.download"
      via: "标准浏览器下载触发"
      pattern: "createObjectURL"
    - from: "xingran-react-frontend/src/pages/network/mac/history.tsx"
      to: "authStore / useAuth hasPermission('network:mac:export')"
      via: "按钮可见性条件渲染"
      pattern: "hasPermission.*network:mac:export"
---

<objective>
在 14-01 列表页(`/network/mac/history`)工具栏新增两个互斥导出按钮,复用 Phase 13 `/network/history/list` 端点,通过 `format=xlsx` query 参数走 blob 下载,不新增后端 API 端点(符合 D-01 锁定的"现有 endpoint 增强")。Excel 格式与项目其他模块(资产/工位)保持一致(.xlsx + 文件名含时间戳)。按钮可见性由 14-03 注册的 `network:mac:export` 权限点控制。

**关于 depends_on 新增 14-05a 的说明:** 14-05a 负责创建 `EmptyStateWithAction` / `ErrorAlertWithRetry` 共享组件与 shared/index.ts barrel,本 plan 14-04 在 history.tsx 工具栏注入导出按钮时,需要 14-05a 提供的 shared barrel 已被建立(避免 14-04 与 14-05a 同时修改 components/shared/index.ts 造成冲突);14-05b 进一步对 history.tsx 整体改造时,**必须保留** 14-04 注入的"导出当前查询"/"导出全量"两个按钮 prop 名称与 React 节点位置不变。

Purpose: 满足 REQUIREMENTS UI-02 "导出当前查询"与"导出全量"两类操作,提供运维离线分析能力。

Output: 1 个 API 函数 + 14-01 history 列表页的导出按钮组(改 14-01 文件而非新建)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-frontend-ux/14-CONTEXT.md (D-12 格式 / D-13 范围 / D-14 端点 / D-15 下载模式)
@.planning/REQUIREMENTS.md (UI-02 验收条款)
@.planning/phases/14-frontend-ux/14-01-PLAN.md (history 列表页结构,本 plan 增强其工具栏)
@.planning/phases/14-frontend-ux/14-05a-PLAN.md (提供 shared barrel 出口,本 plan 避免冲突)
@.planning/phases/14-frontend-ux/14-05b-PLAN.md (改造 history.tsx 整体,本 plan 按钮需保留)

# 参考实现
@xingran-react-frontend/src/components/shared/ExcelImport.tsx (responseType: 'blob' 模式)
@xingran-react-frontend/src/components/shared/ExcelExport.tsx (downloadFile 实现 lines 186-196)
@xingran-react-frontend/src/lib/opsApi.ts (URL.createObjectURL + a.download 模式 lines 216-235)
@xingran-react-frontend/src/lib/api.ts (api 导出 + 拦截器 lines 526-528)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 networkApi 与在 history 列表页加导出按钮组</name>
  <files>xingran-react-frontend/src/lib/api/networkApi.ts, xingran-react-frontend/src/pages/network/mac/history.tsx, xingran-react-frontend/src/pages/network/mac/history/index.tsx</files>
  <read_first>
    - D:\CODE\ClaudeCode\xingran-go-backend\xingran-react-frontend\src\lib\api\networkApi.ts (14-01 改动后,本 plan 追加)
    - D:\CODE\ClaudeCode\xingran-go-backend\xingran-react-frontend\src\lib\api.ts (api 导出 lines 526-528,确认 responseType 透传)
    - D:\CODE\ClaudeCode\xingran-go-backend\xingran-react-frontend\src\components\shared\ExcelExport.tsx (lines 175-196 Content-Disposition + downloadFile)
    - D:\CODE\ClaudeCode\xingran-go-backend\xingran-react-frontend\src\pages\network\mac\history.tsx (14-01 创建,本 plan 修改其工具栏)
    - D:\CODE\ClaudeCode\xingran-go-backend\xingran-react-frontend\src\store\authStore.ts (确认 hasPermission 工具方法)
  </read_first>
  <action>
    1. 在 `lib/api/networkApi.ts` 中追加 import `import { api } from '../api';` 与新函数:
       - 函数签名 `exportMACHistory(params: MACHistoryQueryParams, exportScope: 'current' | 'all'): Promise<Blob>`
       - 内部:`api.get('/network/history/list', { params: { ...params, format: 'xlsx', exportScope }, responseType: 'blob', timeout: 120000 })`
       - 错误处理:当 blob.size < 1024 时,`FileReader.readAsText(blob, 'utf-8')` 解析 JSON {code, message},message 用 AntD message.error 展示并 throw new Error(message)
       - 返回正常 blob (size >= 1024)
       并 export 该函数;同步在文件底部 default export 加上 `exportMACHistory`。
    2. 在 `pages/network/mac/history.tsx` 工具栏(查询/重置按钮之后)新增两个 `<Button>`:
       - 按钮 1: `icon={<DownloadOutlined />}`, `onClick={() => handleExport('current')}`, 文字 "导出当前查询"
       - 按钮 2: `icon={<DownloadOutlined />}`, `onClick={() => handleExport('all')}`, 文字 "导出全量", `type="default"` (与按钮 1 区分,1 用 `type="primary"` 蓝色)
       - `handleExport(exportScope)` 函数:
         * `exportScope === 'current'` 时,传当前 `searchParams` (含 mac/deviceId/interfaceName/eventType/status/startTime/endTime 等)
         * `exportScope === 'all'` 时,只传 `format=xlsx&exportScope=all`,其余过滤项清空
         * 调用 `exportMACHistory(params, exportScope)` 拿到 blob
         * 从 `blob` 推断文件名(优先读 `Content-Disposition` header;若没有则用 `mac_history_${scope}_${dayjs().format('YYYYMMDD_HHmmss')}.xlsx`)
         * 触发下载:`URL.createObjectURL(blob)` + `a.download` + `a.click()` + `URL.revokeObjectURL` (复用 ExcelExport.tsx:186-196 模式)
         * 异常用 AntD `message.error` 展示
       - **权限控制(强制)**:两个按钮必须包 `<AuthWrapper permission="network:mac:export">`(若项目无 AuthWrapper 组件,改用 `useAuth().hasPermission('network:mac:export')` 或 `authStore.getState().hasPermission('network:mac:export')` 或简单 `if (!hasPermission('network:mac:export')) return null;` 由 executor 根据仓库实际权限 hook 决定)。**按钮必须以条件渲染方式呈现,无条件 `return null` 或始终不渲染视为违规**。
       - **保留契约**:executor 须保持按钮 prop 名称(`exportScope='current'|'all'`)与 React 节点位置(工具栏查询/重置按钮之后)不变,以便 14-05b 改造时复用。
    3. `pages/network/mac/history/index.tsx` 是 re-export,无需修改。
  </action>
  <verify>
    <automated>cd "D:/CODE/ClaudeCode/xingran-go-backend/xingran-react-frontend" && npx tsc --noEmit -p . 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `networkApi.ts` 含 `exportMACHistory` 函数 export(grep `export (const|function) exportMACHistory` 命中 ≥ 1 行)
    - `exportMACHistory` 内部使用 `api.get` + `responseType: 'blob'`(grep `responseType: 'blob'` 在该函数体内命中 ≥ 1 行)
    - `exportMACHistory` 含 `format: 'xlsx'` 参数(grep `format: 'xlsx'` 命中 ≥ 1 行)
    - `history.tsx` 含 `导出当前查询` 与 `导出全量` 两个按钮文字(grep 命中 2 行)
    - `history.tsx` 含 `createObjectURL` + `a.click()`(grep `createObjectURL` 命中 ≥ 1 行)
    - `history.tsx` 含异常处理分支 `blob.size < 1024` 或 `FileReader`(grep 命中至少其中之一)
    - **`history.tsx` 含 `network:mac:export` 权限可见性判断**(git grep -E "hasPermission|useAuth" history.tsx | grep -c "network:mac:export" 应 ≥ 1 行)
    - `npm run type-check` (或 `npx tsc --noEmit`) 退出码 0
  </acceptance_criteria>
  <done>列表页工具栏拥有 2 个互斥导出按钮,点击后浏览器下载 .xlsx,文件名带时间戳;按钮可见性由 `network:mac:export` 权限点严格控制;错误信息友好展示。按钮 prop 名称与位置在 14-05b 改造时保留。</done>
</task>

</tasks>

<verification>
1. 用户访问 /network/mac/history,工具栏右侧出现"导出当前查询" / "导出全量" 两个按钮
2. 持有 `network:mac:export` 权限,点击"导出当前查询"浏览器下载 mac_history_current_YYYYMMDD_HHmmss.xlsx(后端真实接口需返回 `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`,xlsx 大小 ≥ 1KB)
3. 点击"导出全量"浏览器下载 mac_history_all_YYYYMMDD_HHmmss.xlsx
4. 模拟后端 500 错误(测试环境下由开发同学配合),blob < 1KB 触发 JSON 解析,AntD message.error 展示后端 message 字段
5. 切换时间预设后再点"导出当前查询",请求参数含新时间范围
6. 用户缺失 `network:mac:export` 权限时,两个导出按钮均不渲染(由 authStore.hasPermission 控制)
7. `npm run type-check` 0 错误
</verification>

<success_criteria>
- [ ] UI-02 完整满足(Excel 格式 + 当前查询范围 + 全量范围)
- [ ] 不新增后端 API 端点(D-01 锁定)
- [ ] 走 `api.get(url, { params, responseType: 'blob' })` 拦截器(Phase 33 M2 锁定)
- [ ] blob 异常检测 < 1KB(D-15 锁定)
- [ ] 按钮权限 `network:mac:export` 严格隔离(grep 验证 ≥ 1 行)
- [ ] 文件名含时间戳
- [ ] depends_on 包含 14-05a,避免与 14-05a 改造冲突
</success_criteria>

<output>
Create `.planning/phases/14-frontend-ux/14-04-SUMMARY.md` when done
</output>
