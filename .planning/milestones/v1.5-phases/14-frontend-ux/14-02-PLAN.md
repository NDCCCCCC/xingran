<!-- Revised 2026-06-14 — plan-checker iteration 1: 修复 BLOCKER 1+2 + WARNING 1-4 -->
---
phase: 14-frontend-ux
plan: 02
type: execute
wave: 2
depends_on:
  - 14-03
  - 14-01
files_modified:
  - xingran-react-frontend/src/pages/network/mac/trajectory.tsx
  - xingran-react-frontend/src/components/network/MACTrajectoryChart.tsx
autonomous: true
requirements:
  - UI-01
  - UI-04

must_haves:
  truths:
    - "用户在 /network/mac/trajectory 看到带快捷预设 + RangePicker 的时间范围,默认近 7d"
    - "URL 含 ?mac=...&startTime=...&endTime=...&deviceId=... 参数时,进入页面表单/RangePicker 自动填入;MAC 输入失焦自动规范化"
    - "MACTrajectoryChart 渲染时,dataZoom 滑块默认聚焦到最近 1/3 时间区间(若数据跨度允许)"
    - "MACTrajectoryChart 节点 tooltip 显示停留时长热力颜色:停留 < 1h 显示灰色,1h-24h 显示蓝色,#1890ff;24h-7d 显示橙色 #faad14;> 7d 显示红色 #ff4d4f"
    - "页面右侧新增 '事件时间线' 侧边栏,使用 14-01 创建的 MACEventsTimeline 组件(传入当前 mac + 时间范围)"
  artifacts:
    - path: "xingran-react-frontend/src/pages/network/mac/trajectory.tsx"
      provides: "轨迹页 UX 增强:时间预设 / URL 注入 / dataZoom 默认范围 / 停留时长热力 / 事件侧边栏"
      exports: ["default"]
    - path: "xingran-react-frontend/src/components/network/MACTrajectoryChart.tsx"
      provides: "dataZoom 默认 start/end props 注入 + tooltip formatter 停留时长热力(追加 props 与 merge 逻辑,不动核心 Gantt)"
      contains: "dataZoom"
  key_links:
    - from: "xingran-react-frontend/src/pages/network/mac/trajectory.tsx"
      to: "xingran-react-frontend/src/components/network/MACTrajectoryChart.tsx"
      via: "dataZoomStart / dataZoomEnd props 注入默认范围"
      pattern: "dataZoom"
    - from: "xingran-react-frontend/src/pages/network/mac/trajectory.tsx"
      to: "xingran-react-frontend/src/components/network/MACEventsTimeline.tsx"
      via: "右侧 Drawer/Sider 渲染"
      pattern: "MACEventsTimeline"
    - from: "xingran-react-frontend/src/pages/network/mac/trajectory.tsx"
      to: "URL query (?mac, ?startTime, ?endTime, ?deviceId)"
      via: "useEffect 读取 setFieldsValue / setQueryParams"
      pattern: "URLSearchParams"
---

<objective>
对 Phase 13-04 已交付的 `/network/mac/trajectory` 页面进行 UX 增强(D-11 + D-17 + Claude's Discretion),不重写 MACTrajectoryChart 核心可视化。新增 4 项能力:
1. **时间范围快捷预设** (近 1h/24h/7d/30d/90d/自定义),与 14-01 列表页预设一致
2. **URL 参数预填** (?mac=&startTime=&endTime=&deviceId=),支持从 14-01 时间线跳转而来
3. **ECharts Gantt dataZoom 默认聚焦** 最近 1/3 时间区间
4. **停留时长热力** (tooltip 颜色按 < 1h 灰 / 1h-24h 蓝 / 24h-7d 橙 / > 7d 红 分级)
5. **右侧"事件时间线"侧边栏** 复用 14-01 的 MACEventsTimeline 组件

Purpose: 闭环"查询页 → 轨迹页 → 事件细节"的导航流;数据已全在 Phase 13,Phase 14 仅做交互层增强。

Output: 1 个增强版 trajectory 页面(原地修改 trajectory.tsx)+ MACTrajectoryChart 组件追加 2 个可选 props + tooltip formatter 增强(仅追加,不重写)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-frontend-ux/14-CONTEXT.md (D-11 URL 跳轨迹页 + D-17 query 串模式)
@.planning/phases/13-query-layer-trajectory/13-04-SUMMARY.md (MACTrajectoryChart 颜色 + tooltip 内容)
@.planning/REQUIREMENTS.md (UI-03 已在 Phase 13 交付,UI-04 复用 timeline)

# Phase 13 既有资产(增强而非重写)
@xingran-react-frontend/src/pages/network/mac/trajectory.tsx
@xingran-react-frontend/src/components/network/MACTrajectoryChart.tsx
@xingran-react-frontend/src/components/network/MACEventsTimeline.tsx (14-01 创建)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 增强 trajectory 页 — 时间预设 + URL 注入 + dataZoom + 停留时长热力 + 时间线侧栏</name>
  <files>xingran-react-frontend/src/pages/network/mac/trajectory.tsx, xingran-react-frontend/src/components/network/MACTrajectoryChart.tsx</files>
  <read_first>
    - D:\CODE\ClaudeCode\xingran-go-backend\xingran-react-frontend\src\pages\network\mac\trajectory.tsx
    - D:\CODE\ClaudeCode\xingran-go-backend\xingran-react-frontend\src\components\network\MACTrajectoryChart.tsx (dataZoom 配置 + tooltip formatter 在 lines 78-93,146-152)
    - D:\CODE\ClaudeCode\xingran-go-backend\xingran-react-frontend\src\components\network\MACEventsTimeline.tsx (14-01 创建,本次复用)
  </read_first>
  <action>
    修改 `pages/network/mac/trajectory.tsx`,沿用现有 `queryMACTrajectory` + `useQuery` 调用,在 Card + Form 之外新增 4 处增强:
    1. **时间预设按钮组**:在 `Form` 上方加 `Space`,6 个 `<Button>` 同 14-01(近 1h/24h/7d/30d/90d/自定义),`size="small"`,互斥逻辑与 14-01 一致,默认 `近 7d`。
    2. **URL 参数预填**:`useEffect(() => { const sp = new URLSearchParams(location.search); if (sp.get('mac')) form.setFieldsValue({ mac: normalizeMACAddress(sp.get('mac')!)}); if (sp.get('startTime') && sp.get('endTime')) { form.setFieldsValue({ dateRange: [dayjs(sp.get('startTime')!), dayjs(sp.get('endTime')!)]}); setTimePreset('自定义'); } else if (sp.get('startTime')) { form.setFieldsValue({ dateRange: [dayjs(sp.get('startTime')!), dayjs()]}); setTimePreset('近 24h'); } ... }, [location.search])` 自动触发查询(在 URL 完整时直接 setQueryParams)。
    3. **MACTrajectoryChart dataZoom 默认范围 — 方案 (a) 固化**:在 MACTrajectoryChart.tsx 中追加 2 个可选 props:`dataZoomStart?: number` (默认 66) 与 `dataZoomEnd?: number` (默认 100),并在 `option.dataZoom` 数组中以 `[merged, ...]` 形式合并(不动现有 `type: 'inside'` 配置,只在末尾追加一个 `type: 'slider'` 项且 `start`/`end` 来自 props 或默认值)。trajectory.tsx 调用处无需传 prop,自动走默认 66/100。**不重写 MACTrajectoryChart**,仅追加 2 个可选 props + 1 行 merge 逻辑。
    4. **停留时长热力 tooltip**:MACTrajectoryChart 当前 tooltip 显示的是固定设备/端口/停留/事件。增强方式:在 tooltip formatter 内,根据 `data[3]` (duration 秒) 判定颜色: `duration < 3600 → '#bfbfbf'` (灰), `< 86400 → '#1890ff'` (蓝), `< 604800 → '#faad14'` (橙),`>= 604800 → '#ff4d4f'` (红);在文本前加 `<Tag color="...">短/中/长/超长</Tag>`。**不破坏现有 tooltip 字段**,仅在 formatter 字符串前加一行。
    5. **右侧事件时间线侧边栏**:使用 AntD `Drawer` (或 `Layout.Sider`,Drawer 更轻量),`title="事件时间线"`, `width={420}`, 默认 `open={true}`,可在右上角关闭。Drawer body 内: `<MACEventsTimeline mac={normalizedMAC} startTime={queryParams.start_time} endTime={queryParams.end_time} />` (mac 与时间范围与当前查询一致)。Drawer 关闭按钮触发 `setDrawerOpen(false)`,提供一个 `Button` "显示时间线" 重新打开。
    6. **MACEventsTimeline 兼容性确认**:14-01 组件已支持 `mac / startTime / endTime / deviceId?` props,本 plan 不需要修改组件(若 14-01 executor 漏掉了 `deviceId` prop,本 plan 在 trajectory 侧不传 deviceId,保持 D-09 设计的最小 API)。**Gantt 节点聚焦行为(Gantt 高亮)属 deferred 增强,本 plan 不实施**。

    整个文件改动 < 80 行,主要为追加 `useEffect / state / Drawer / dataZoom props`,不删除既有 `useQuery / queryMACTrajectory / normalizeMACAddress / Form / Alert` 任何逻辑。
  </action>
  <verify>
    <automated>cd "D:/CODE/ClaudeCode/xingran-go-backend/xingran-react-frontend" && npx tsc --noEmit -p . 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `trajectory.tsx` 含 5 个时间预设按钮(grep `近 1h` `近 24h` `近 7d` `近 30d` `近 90d` 全部命中)
    - `trajectory.tsx` 含 `URLSearchParams` 读取逻辑(grep 命中 ≥ 1 行)
    - `trajectory.tsx` 渲染 `<MACEventsTimeline` 组件(grep 命中 ≥ 1 行)
    - `trajectory.tsx` 渲染 AntD `Drawer` (grep `Drawer` 命中 ≥ 1 行)
    - MACTrajectoryChart.tsx `option.dataZoom` 块含 `start: 66, end: 100` 默认值(grep `start: 66` 命中 ≥ 1 行)
    - MACTrajectoryChart.tsx 含 `dataZoomStart` 与 `dataZoomEnd` 2 个新 prop 定义(grep 命中 2 行)
    - MACTrajectoryChart tooltip formatter 含停留时长颜色 hex(`#bfbfbf` 与 `#1890ff` 与 `#faad14` 与 `#ff4d4f` 全部出现在 tooltip 字符串,grep 命中 4 个)
    - `npm run type-check` (或 `npx tsc --noEmit`) 退出码 0
  </acceptance_criteria>
  <done>trajectory 页面 4 项 UX 增强全部生效,既不破坏 Phase 13 已交付的 Gantt 可视化,又为 14-01 时间线跳转提供落脚点。Gantt 节点聚焦属 deferred 项,不在本 plan 范围。</done>
</task>

</tasks>

<verification>
1. 用户访问 `/network/mac/trajectory` 看到时间预设按钮 + 7d 默认值
2. 从 14-01 列表页点击"查看轨迹"按钮,URL 携带 mac/startTime/endTime/deviceId,trajectory 页自动填表并触发查询
3. MACTrajectoryChart 加载后 dataZoom 滑块自动聚焦到最近 1/3
4. 鼠标 hover 节点,tooltip 出现停留时长颜色(短/中/长/超长 4 档)
5. 页面右侧 Drawer 显示事件时间线
6. `npm run type-check` 0 错误
</verification>

<success_criteria>
- [ ] 时间预设 5 个 + 自定义 1 个按钮组与 14-01 一致(D-07 锁定)
- [ ] URL 参数预填生效(D-17 锁定)
- [ ] dataZoom 默认 66/100 范围,通过追加 props 注入(不动 MACTrajectoryChart 核心)
- [ ] 停留时长热力 4 档 tooltip(MACTrajectoryChart 增强)
- [ ] Drawer 复用 MACEventsTimeline 组件(14-01 资产复用)
- [ ] Phase 13 既有 Gantt 行为不被破坏
- [ ] Gantt 节点聚焦属 deferred,本 plan 不实施
</success_criteria>

<output>
Create `.planning/phases/14-frontend-ux/14-02-SUMMARY.md` when done
</output>
