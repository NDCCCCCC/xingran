<!-- Revised 2026-06-14 — plan-checker iteration 1: 修复 BLOCKER 1+2 + WARNING 1-4 -->
---
phase: 14-frontend-ux
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/14-frontend-ux/14-menu-registration.sql
  - .planning/phases/14-frontend-ux/14-03-ROUTE-SETUP.md
autonomous: true
requirements:
  - UI-01
  - UI-04

must_haves:
  truths:
    - "MAC 历史查询菜单条目在 sys_menu 表中存在(parent_id = 0013f129-3ec0-4e55-8ffc-25d97b20c37b, path = 'mac/history', component = 'pages/network/mac/history')"
    - "三个权限点 network:mac:list / network:mac:query / network:mac:export 已注册到 sys_menu.perms 字段并与角色绑定(注意:本 plan 仅注册 export 权限点,实际按钮渲染与拦截由 14-04 实施)"
    - "前端路由通过 sys_menu 动态注册后,登录用户可在侧边栏看到 'MAC 历史查询' 菜单项并跳转到 /network/mac/history"
  artifacts:
    - path: ".planning/phases/14-frontend-ux/14-menu-registration.sql"
      provides: "可重复执行的 sys_menu INSERT 脚本,带幂等检查"
      contains: "INSERT INTO sys_menu"
    - path: ".planning/phases/14-frontend-ux/14-03-ROUTE-SETUP.md"
      provides: "路由与权限注册说明,引用 CONTEXT.md D-04 与 13-06 v4 SQL 模板"
      contains: "mac-history"
  key_links:
    - from: ".planning/phases/14-frontend-ux/14-menu-registration.sql"
      to: "sys_menu (id = 0013f129-3ec0-4e55-8ffc-25d97b20c37b)"
      via: "INSERT parent_id 引用"
      pattern: "parent_id.*0013f129-3ec0-4e55-8ffc-25d97b20c37b"
    - from: "前端 DynamicRoutes (RouteGenerator)"
      to: "sys_menu.path = 'mac/history'"
      via: "菜单驱动动态路由"
      pattern: "path.*mac/history"
---

<objective>
在 sys_menu 表中注册 `MAC 历史查询` 子菜单及三个权限点,沿用 Phase 13-06 v4 SQL 模板的结构与命名规范。注册完成后,前端 DynamicRoutes 会通过菜单驱动自动添加路由,运维角色即可在侧边栏看到并访问 `/network/mac/history`(本 Phase 14-01/14-02/14-04/14-05 创建)。

**注意 UI-02 归属边界:** 本 plan 仅在 sys_menu 中注册 `network:mac:export` 权限点(供按钮可见性判断),实际 Excel 导出按钮的渲染、下载、错误处理由 14-04 实施。`requirements` 字段不再列出 UI-02,以保持 14-03 与 14-04 职责清晰。

Purpose: 14-01/14-02/14-04/14-05 四个 Wave 2 plan 的页面都依赖该菜单条目可见;无菜单则路由不可达,UAT 无法进行。Wave 1 隔离避免 4 个并行 plan 同时操作 sys_menu 导致主键冲突。

Output: 一份幂等 SQL 脚本 + 一份路由注册说明文档。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-frontend-ux/14-CONTEXT.md (D-04 锁定菜单与权限规范;canonical_refs 引用 13-06 v4 SQL)
@.planning/phases/13-query-layer-trajectory/13-06-menu-registration-v4.sql (Phase 13 已交付的菜单注册模板 — 完全沿用结构)
@.planning/phases/13-query-layer-trajectory/13-04-ROUTE-SETUP.md (Phase 13 路由注册说明的格式参考)
@.planning/REQUIREMENTS.md (UI-01/UI-04;UI-02 归属 14-04 实施)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 编写 sys_menu 注册 SQL 脚本</name>
  <files>.planning/phases/14-frontend-ux/14-menu-registration.sql</files>
  <read_first>
    - D:\CODE\ClaudeCode\xingran-go-backend\.planning\phases\13-query-layer-trajectory\13-06-menu-registration-v4.sql
    - D:\CODE\ClaudeCode\xingran-go-backend\.planning\phases\14-frontend-ux\14-CONTEXT.md (D-04 段)
  </read_first>
  <action>
    创建 `.planning/phases/14-frontend-ux/14-menu-registration.sql`,内容包含 3 个 SQL 段:
    1. 验证父菜单存在的 SELECT 语句(父菜单 id = 0013f129-3ec0-4e55-8ffc-25d97b20c37b,path = 'mac',与 13-06 v4 SQL 一致)。
    2. 幂等 INSERT:在 sys_menu 中插入 `MAC 历史查询` 子菜单,字段完全沿用 13-06 v4 SQL 的列序与值结构:
       - `menu_name = 'MAC 历史查询'`
       - `parent_id = '0013f129-3ec0-4e55-8ffc-25d97b20c37b'`
       - `order_num = 11`(排在 MAC轨迹查询 = 10 之后)
       - `path = 'mac/history'`(相对父菜单)
       - `component = 'pages/network/mac/history'`(14-01 计划创建的页面路径)
       - `menu_type = 'C'`,`visible = 1`,`status = 0`
       - `perms = 'network:mac:list'`(主权限点,作为该菜单项可见性判断)
       - `icon = 'history'`,`remark = 'MAC地址历史数据查询、导出页面'`
       - `meta = '{"icon": "history", "affix": false, "title": "MAC 历史查询", "hidden": false, "keepAlive": false}'::jsonb`
       - 幂等保护:使用 `INSERT ... WHERE NOT EXISTS (SELECT 1 FROM sys_menu WHERE path = 'mac/history' AND parent_id = '0013f129-3ec0-4e55-8ffc-25d97b20c37b')` 形式,或前置 SELECT 检查。
    3. 注册两个细粒度操作权限点(sys_menu.type = 'F' = 按钮权限),不显示在菜单,只用于按钮控制:
       - `menu_name = 'MAC历史查询-查询'`,`perms = 'network:mac:query'`,`menu_type = 'F'`,`visible = 0`,`status = 0`
       - `menu_name = 'MAC历史查询-导出'`,`perms = 'network:mac:export'`,`menu_type = 'F'`,`visible = 0`,`status = 0`(此权限点注册后由 14-04 在 history.tsx 工具栏按钮上做可见性判断)
    4. 验证注册结果的 SELECT(按 path = 'mac/history' 过滤),预期返回 1 条主菜单 + 2 条按钮权限,共 3 行。
    SQL 顶部写明执行人/执行时间注释、Phase 标识。
  </action>
  <verify>
    <automated>grep -c "INSERT INTO sys_menu" ".planning/phases/14-frontend-ux/14-menu-registration.sql" | grep -v '^#' | awk -F: '$2>=3{exit 0}{exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/14-frontend-ux/14-menu-registration.sql 文件存在且包含至少 3 个 INSERT INTO sys_menu 语句块(1 个菜单 + 2 个按钮权限)
    - 菜单 INSERT 的 parent_id 字面量 = 0013f129-3ec0-4e55-8ffc-25d97b20c37b,path = 'mac/history',component = 'pages/network/mac/history',perms = 'network:mac:list',order_num = 11
    - 按钮权限 INSERT 的 menu_type = 'F',perms 分别为 'network:mac:query' 和 'network:mac:export',visible = 0
    - 包含 WHERE NOT EXISTS 形式的幂等保护
    - 包含验证 INSERT 结果的 SELECT 语句
  </acceptance_criteria>
  <done>SQL 脚本就位,可在 PostgreSQL 重复执行且不产生重复菜单条目;菜单名/路径/权限点命名与 CONTEXT.md D-04 完全一致。`network:mac:export` 权限点已注册,等待 14-04 实施按钮渲染。</done>
</task>

<task type="auto">
  <name>Task 2: 编写路由注册说明文档</name>
  <files>.planning/phases/14-frontend-ux/14-03-ROUTE-SETUP.md</files>
  <read_first>
    - D:\CODE\ClaudeCode\xingran-go-backend\.planning\phases\13-query-layer-trajectory\13-04-ROUTE-SETUP.md
    - D:\CODE\ClaudeCode\xingran-go-backend\.planning\phases\14-frontend-ux\14-CONTEXT.md (canonical_refs / Integration Points 段)
  </read_first>
  <action>
    创建 `.planning/phases/14-frontend-ux/14-03-ROUTE-SETUP.md`,内容按 13-04-ROUTE-SETUP.md 的同构格式组织:
    1. **路由配置**: 列出 sys_menu 必填字段(name / path / component / parent_id / perms)与对应值,说明前端 DynamicRoutes 读取 sys_menu 自动生成路由(不需手动改 React Router)。
    2. **SQL 执行步骤**: 引用同目录的 `14-menu-registration.sql`,要求执行人在 PostgreSQL 运行,并截图或粘贴执行结果。
    3. **权限分配**: 说明三个权限点 `network:mac:list` / `network:mac:query` / `network:mac:export` 应绑定到 `sys_role_menu` 表中相关角色(参考 13-04 文档同节,占位 `INSERT INTO sys_role_menu (role_id, menu_id) SELECT role_id, <新菜单id> FROM sys_role WHERE role_key IN ('admin', 'ops')`)。明确标注: `network:mac:export` 由 14-04 实施时调用,本 plan 仅注册。
    4. **验收清单 (Verification Checklist)**: 6-8 个 checkbox,涵盖 (a) SQL 成功执行 (b) sys_menu 有 3 行新记录 (c) 角色绑定 (d) 登录后侧边栏可见 (e) 访问 /network/mac/history 返回 200 (f) 点击子菜单路由跳转正常 (g) network:mac:query 权限控制查询按钮可见 (h) network:mac:export 权限控制导出按钮可见(由 14-04 验证)。
    5. **回滚说明**: 提供 `DELETE FROM sys_menu WHERE perms IN ('network:mac:list', 'network:mac:query', 'network:mac:export')` 作为回滚 SQL。
    文档使用中文,引用 14-01/14-04 计划创建的实际文件路径(`/network/mac/history`)。
  </action>
  <verify>
    <automated>grep -c "Verification Checklist\|sys_menu\|network:mac" ".planning/phases/14-frontend-ux/14-03-ROUTE-SETUP.md" | awk '$1>=3{print "PASS"; exit 0}{print "FAIL"; exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/14-frontend-ux/14-03-ROUTE-SETUP.md 文件存在
    - 文档包含至少 6 个 Verification Checklist 项
    - 文档引用 14-menu-registration.sql 作为执行入口
    - 文档说明三个权限点 network:mac:list / network:mac:query / network:mac:export 的用途
    - 文档包含回滚 SQL (DELETE FROM sys_menu WHERE perms ...)
  </acceptance_criteria>
  <done>运维/开发人员根据该文档可独立完成菜单注册、权限绑定、UAT 验收、回滚四步操作,无需查阅 CONTEXT.md。</done>
</task>

</tasks>

<verification>
1. .planning/phases/14-frontend-ux/14-menu-registration.sql 可在 PostgreSQL 重复执行(幂等)
2. .planning/phases/14-frontend-ux/14-03-ROUTE-SETUP.md 引用同目录 SQL 脚本并提供完整验收清单
3. SQL 命名与 13-06 v4 完全一致(同表同列序同风格)
4. Phase 14-01/14-02/14-04/14-05 创建的页面在 sys_menu 注册后可通过 DynamicRoutes 路由到
5. 14-04 executor 在 history.tsx 工具栏可读取 `network:mac:export` 权限点判断按钮可见性
</verification>

<success_criteria>
- [ ] 14-menu-registration.sql 包含 1 个主菜单 + 2 个按钮权限 INSERT
- [ ] 主菜单 parent_id/path/component/perms 字段值与 14-CONTEXT.md D-04 完全一致
- [ ] 14-03-ROUTE-SETUP.md 提供 6+ 项验收 checklist 与回滚 SQL
- [ ] SQL 与文档都在 .planning/phases/14-frontend-ux/ 目录下,文件名与命名规范一致
- [ ] `network:mac:export` 权限点已注册但归属 14-04 实施
- [ ] UI-02 不在 14-03 的 requirements 字段中(归属 14-04)
</success_criteria>

<output>
Create `.planning/phases/14-frontend-ux/14-03-SUMMARY.md` when done
</output>
