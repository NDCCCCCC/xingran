<!-- Revised 2026-06-14 — plan-checker iteration 1: 修复 BLOCKER 1+2 + WARNING 1-4 -->
---
phase: 14-frontend-ux
plan: 01
type: execute
wave: 2
depends_on:
  - 14-03
files_modified:
  - xingran-react-frontend/src/lib/api/networkApi.ts
  - xingran-react-frontend/src/components/network/MACEventsTimeline.tsx
  - xingran-react-frontend/src/pages/network/mac/history.tsx
  - xingran-react-frontend/src/pages/network/mac/history/index.tsx
  - xingran-react-frontend/src/components/network/index.ts
autonomous: true
requirements:
  - UI-01
  - UI-04

must_haves:
  truths:
    - "用户访问 /network/mac/history 看到带时间筛选 + 8 列(MAC/设备/端口/事件类型/VLAN/状态/操作)的列表,默认近 7d"
    - "快捷预设按钮 近 1h/24h/7d/30d/90d/自定义 单击切换时间范围,与 RangePicker 互斥同步"
    - "点击列表行内 '查看事件' 操作,展开/侧边栏显示 MACEventsTimeline 垂直时间线(同色系图标),点击事件项可跳 /network/mac/trajectory?mac=...&startTime=...&endTime=...&deviceId=..."
    - "列表行支持虚拟滚动(AntD Table virtual),placeholderData: keepPreviousData 保持滚动位置"
    - "URL 含 ?deviceId=xxx&portName=yyy 参数时,进入页面自动填入设备/端口过滤项"
  artifacts:
    - path: "xingran-react-frontend/src/lib/api/networkApi.ts"
      provides: "新增 queryMACHistory / getMACEvents API 函数"
      contains: "queryMACHistory"
    - path: "xingran-react-frontend/src/components/network/MACEventsTimeline.tsx"
      provides: "跨页复用垂直时间线组件,接收 mac + 时间范围 props"
      exports: ["default"]
    - path: "xingran-react-frontend/src/pages/network/mac/history.tsx"
      provides: "MAC 历史查询主列表页(桌面 + 移动卡片)"
      exports: ["default"]
    - path: "xingran-react-frontend/src/pages/network/mac/history/index.tsx"
      provides: "路由兼容 re-export"
      exports: ["default"]
    - path: "xingran-react-frontend/src/components/network/index.ts"
      provides: "MACEventsTimeline 组件 barrel export"
      contains: "MACEventsTimeline"
  key_links:
    - from: "xingran-react-frontend/src/pages/network/mac/history.tsx"
      to: "xingran-react-frontend/src/lib/api/networkApi.ts"
      via: "queryMACHistory 拉列表"
      pattern: "queryMACHistory"
    - from: "xingran-react-frontend/src/components/network/MACEventsTimeline.tsx"
      to: "xingran-react-frontend/src/lib/api/networkApi.ts"
      via: "getMACEvents 拉事件序列"
      pattern: "getMACEvents"
    - from: "xingran-react-frontend/src/pages/network/mac/history.tsx"
      to: "xingran-react-frontend/src/components/network/MACEventsTimeline.tsx"
      via: "操作列点击展开行渲染"
      pattern: "MACEventsTimeline"
    - from: "xingran-react-frontend/src/components/network/MACEventsTimeline.tsx"
      to: "/network/mac/trajectory"
      via: "useNavigate 跳路由带 query 参数"
      pattern: "navigate.*mac/trajectory"
---

<objective>
创建 MAC 历史查询主列表页(`/network/mac/history`),实现 UI-01 与 UI-04 的核心交互:时间筛选(快捷预设 + RangePicker)、MAC/设备/端口过滤、8 列全列展示(走 useColumnConfig)、操作列展开事件时间线、时间线点击跳轨迹页。列表分页使用虚拟滚动(`useTableQuery` + AntD Table `virtual` + `placeholderData: keepPreviousData`)。同时抽离独立可复用组件 `MACEventsTimeline` 为后续 14-02 轨迹页右侧栏铺垫(沿用 Phase 13 MACTrajectoryChart 颜色体系)。

Purpose: 这是 v1.5 MAC 历史数据管理的前端入口页;Wave 2 其余 3 个 plan 都依赖该页确立的列表范式与时间线组件(14-02 复用 timeline 组件展示单 MAC 事件序列;14-04 在工具栏新增导出按钮;14-05 移动端卡片视图)。

Output: 1 个查询页 + 1 个时间线组件 + 2 个 API 函数 + 1 个 re-export + 1 个 barrel export。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-frontend-ux/14-CONTEXT.md (D-01 锁定端点 /network/history/list,D-05..D-11,D-17,D-19 全部锁定,color 体系 13-04 已锁)
@.planning/phases/13-query-layer-trajectory/13-04-SUMMARY.md (MACTrajectoryChart 颜色 + 字段 + URL 参数注入)
@.planning/REQUIREMENTS.md (UI-01/UI-04 验收条款)
@.planning/ROADMAP.md (14-01 标题与 5 列全列展示约束)

# Phase 13 既有资产(必须沿用)
@xingran-react-frontend/src/components/network/MACTrajectoryChart.tsx
@internal/api/v1/network/mac_history_handler.go (Phase 13 已交付,锁定端点 /network/history/list)
@xingran-react-frontend/src/lib/api/networkApi.ts
@xingran-react-frontend/src/pages/network/mac/trajectory.tsx
@xingran-react-frontend/src/hooks/useTableManager.ts
@xingran-react-frontend/src/hooks/useTableQuery.ts
@xingran-react-frontend/src/hooks/useColumnConfig.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 networkApi 与新增 MACEventsTimeline 组件</name>
  <files>xingran-react-frontend/src/lib/api/networkApi.ts, xingran-react-frontend/src/components/network/MACEventsTimeline.tsx, xingran-react-frontend/src/components/network/index.ts</files>
  <read_first>
    - D:\CODE\ClaudeCode\xingran-go-backend\xingran-react-frontend\src\lib\api\networkApi.ts
    - D:\CODE\ClaudeCode\xingran-go-backend\xingran-react-frontend\src\components\network\MACTrajectoryChart.tsx
    - D:\CODE\ClaudeCode\xingran-go-backend\xingran-react-frontend\src\components\network\MACEventsTimeline.tsx (不存在,需新建)
    - D:\CODE\ClaudeCode\xingran-go-backend\internal\api\v1\network\mac_history_handler.go (锁定后端端点 /network/history/list,确认 POST 接收 current/pageSize/mac/deviceId/interfaceName/vlanId/eventType/status/startTime/endTime 字段)
  </read_first>
  <action>
    1. 在 `networkApi.ts` 中新增两个 TypeScript 接口与两个 API 函数(沿用现有 `import { post } from '../api'`):
       - `interface MACHistoryQueryParams { current: number; pageSize: number; mac?: string; deviceId?: string; interfaceName?: string; vlanId?: number; eventType?: string; status?: number; startTime?: string; endTime?: string; }`
       - `interface MACHistoryRecord { id: string; macAddress: string; deviceId: string; deviceNameSnapshot: string; interfaceName: string; vlanId?: number; eventType: 'appeared' | 'disappeared' | 'moved' | 'vlan_changed'; firstSeen: string; lastSeen: string; collectedAt: string; status: number; }` (字段对应后端 MACHistoryRecord,见 mac_history_query_service.go:41-52)
       - `interface MACHistoryPageResult { list: MACHistoryRecord[]; total: number; current: number; pageSize: number; }`
       - `queryMACHistory(params: MACHistoryQueryParams): Promise<MACHistoryPageResult>` 调用 `post<MACHistoryPageResult>('/network/history/list', params)`,返回 `result.data!`(**严格按 D-01 锁定端点 `/network/history/list`** — Phase 13-01 SUMMARY 已交付该后端 handler,executor 不得假设其他端点)
       - `getMACEvents(mac: string, startTime: string, endTime: string): Promise<MACHistoryRecord[]>` 调用 `post('/network/history/list', { mac, startTime, endTime, current: 1, pageSize: 100 })`,过滤后返回 `list`。
    2. 在 `components/network/MACEventsTimeline.tsx` 中创建时间线组件:
       - 导入 `Timeline, Empty, Spin, Skeleton, Tag, Card` from 'antd';`@ant-design/icons` 的 `PlusCircleOutlined, SwapOutlined, MinusCircleOutlined, TagOutlined`;`useQuery` from `@tanstack/react-query`;`getMACEvents` from `@/lib/api/networkApi`;`useNavigate` from `react-router-dom`。
       - 颜色与图标常量(与 MACTrajectoryChart.tsx:24-29 完全一致): `appeared = { color: '#52c41a', icon: PlusCircleOutlined, label: '出现' };disappeared = { color: '#ff4d4f', icon: MinusCircleOutlined, label: '消失' };moved = { color: '#faad14', icon: SwapOutlined, label: '迁移' };vlan_changed = { color: '#1890ff', icon: TagOutlined, label: 'VLAN 变更' }`。
       - 组件 props: `interface MACEventsTimelineProps { mac: string; startTime: string; endTime: string; deviceId?: string; }`。
       - 内部用 `useQuery({ queryKey: ['macEvents', mac, startTime, endTime], queryFn: () => getMACEvents(mac, startTime, endTime), enabled: !!mac, staleTime: 60 * 1000 })`。
       - 渲染 AntD `Timeline` (mode = 'left'),每条 item 含: 圆点 `dot={React.createElement(meta.icon, { style: { color: meta.color, fontSize: 16 } })}`、颜色 `color={meta.color}`;children 区域显示 `firstSeen` 时间 (dayjs 格式化 `YYYY-MM-DD HH:mm`) + `meta.label` Tag + 设备 + 端口 + VLAN。
       - 事件项可点击 (整个 children 加 onClick),调用 `navigate(\`/network/mac/trajectory?mac=${encodeURIComponent(mac)}&startTime=${encodeURIComponent(startTime)}&endTime=${encodeURIComponent(endTime)}${deviceId ? `&deviceId=${encodeURIComponent(deviceId)}` : ''}\`)`。
       - 空数据: `<Empty description="该 MAC 在此时间范围内无事件" />`;加载: `<Skeleton active />`;错误: `<Empty description="加载失败" />`。
       - 卡片包裹,标题: `MAC 事件时间线 — {mac}`。
    3. 在 `components/network/index.ts` 中 export `MACEventsTimeline`。
  </action>
  <verify>
    <automated>cd "D:/CODE/ClaudeCode/xingran-go-backend/xingran-react-frontend" && npx tsc --noEmit -p . 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `networkApi.ts` 包含 `queryMACHistory` 与 `getMACEvents` 两个 export 函数(grep -E "export (const|function) (queryMACHistory|getMACEvents)" src/lib/api/networkApi.ts 命中 2 行)
    - `queryMACHistory` 内部调用 `post('/network/history/list', params)`(grep -E "/network/history/list" src/lib/api/networkApi.ts 命中 ≥ 1 行 — D-01 锁定端点)
    - `MACEventsTimeline.tsx` 存在,export default 一个函数组件,接收 `mac / startTime / endTime / deviceId?` props
    - 颜色常量 `appeared: '#52c41a'`, `moved: '#faad14'`, `disappeared: '#ff4d4f'`, `vlan_changed: '#1890ff'` 出现在 MACEventsTimeline.tsx 中(grep 命中全部 4 个 hex 字符串)
    - 图标常量包含 `PlusCircleOutlined / SwapOutlined / MinusCircleOutlined / TagOutlined` 全部 4 个
    - 事件 onClick 调用 `navigate('/network/mac/trajectory?mac=...')`(grep `navigate.*mac/trajectory` 命中至少 1 行)
    - `components/network/index.ts` 含 `MACEventsTimeline` export
    - `npm run type-check` (或 `npx tsc --noEmit`) 退出码 0
  </acceptance_criteria>
  <done>API 函数与时间线组件就位,颜色体系与 Phase 13-04 一致,可被 history.tsx 列表页与(后续)trajectory.tsx 增强页直接 import 使用。</done>
</task>

<task type="auto">
  <name>Task 2: 创建 MAC 历史查询主列表页(桌面 + 移动卡片 + URL 参数注入)</name>
  <files>xingran-react-frontend/src/pages/network/mac/history.tsx, xingran-react-frontend/src/pages/network/mac/history/index.tsx</files>
  <read_first>
    - D:\CODE\ClaudeCode\xingran-go-backend\xingran-react-frontend\src\pages\network\mac\trajectory.tsx (URL query 注入参考)
    - D:\CODE\ClaudeCode\xingran-go-backend\xingran-react-frontend\src\hooks\useTableQuery.ts (React Query 5 + placeholderData 模式)
    - D:\CODE\ClaudeCode\xingran-go-backend\xingran-react-frontend\src\pages\operations\buildings\index.tsx (桌面/移动表格↔卡片切换参考)
    - D:\CODE\ClaudeCode\xingran-go-backend\xingran-react-frontend\src\hooks\useColumnConfig.ts
  </read_first>
  <action>
    1. 创建 `pages/network/mac/history.tsx`:
       - 顶部筛选 Card:AntD `Form layout="inline"` 含 5 个字段 — `MAC 地址` (Input,12 字符自动格式化,`AA:BB:CC:DD:EE:FF` 大写,复用 trajectory.tsx:63-70 `normalizeMACAddress`) / `设备` (Input,受控) / `端口` (Input) / `事件类型` (Select,options = 全部/出现/迁移/消失/VLAN 变更,值 = all/appeared/moved/disappeared/vlan_changed) / `VLAN` (InputNumber) / `状态` (Select,0=正常 1=停用,值 = all/0/1)。
       - 时间范围:6 个 `<Button>` 预设(`近 1h` / `近 24h` / `近 7d` / `近 30d` / `近 90d` / `自定义`)+ 自定义模式下显示 AntD `RangePicker showTime format="YYYY-MM-DD HH:mm"`。预设按钮互斥:单击切换时调用 `dayjs().subtract(N, 'hour'/'day').toISOString()` 写入 `timeRange` state 并清空 RangePicker 的 form 字段;自定义按钮显示 RangePicker 并清空预设。默认值: `近 7d`。
       - 工具栏右侧: `查询` / `重置` / 移动端切换按钮 (`AppstoreOutlined` / `TableOutlined`,用 `Grid.useBreakpoint()` 判定 `xs`) 隐藏后展示。
       - 列表:AntD `Table` 启用 `virtual` 属性 + `scroll={{ x: 1200, y: 600 }}`;列定义通过 `useColumnConfig({ pageKey: 'mac.history.list', defaultColumns: [...] })` 渲染。8 列全列默认配置: 时间 (`firstSeen` dayjs 格式) / MAC 地址 (复制按钮) / 设备 (`deviceNameSnapshot`) / 端口 (`interfaceName`) / 事件类型 (Tag 颜色按 D-10 hex) / VLAN / 状态 (Tag 0=绿色 1=红色) / 操作 (`查看轨迹` / `查看事件` 两个 LinkButton,事件点击切换 `expandedRowKeys`)。
       - 展开行:`expandedRowRender` 返回 `<MACEventsTimeline mac={record.macAddress} startTime={searchParams.startTime} endTime={searchParams.endTime} />` (使用当前页面的查询时间范围)。
       - 数据源: `useTableQuery<MACHistoryRecord>({ resource: 'mac.history.list', current, pageSize, filters: searchParams, queryFn: queryMACHistory })`。
       - 移动端卡片: 当 `breakpoint.xs === true` 时,渲染 `List` (AntD) 而非 `Table`,每条卡片显示 MAC / 设备 / 端口 / 事件类型 Tag / 时间,点击展开 `<MACEventsTimeline>`。
       - URL 参数注入: `useEffect` 在 mount 时读 `URLSearchParams(location.search)`,提取 `deviceId/portName/startTime/endTime/mac`,调用 `searchForm.setFieldsValue()` 并将 `timeRange` state 设为对应值(若 startTime/endTime 存在则视作"自定义"模式)。无参数时使用默认 `近 7d`。
       - 状态兜底:数据为 0 时显示 `<Empty description="该范围内未采集到 MAC 记录,请检查设备是否启用了 MAC 采集/端口采集周期" />` + 链接 `前往设备管理 → /network/devices`(D-18);错误显示 `<ErrorAlertWithRetry>` (由 14-05 提供;若 14-05 还未完成,先用内联 AntD `<Alert type="error" message={error.message} />` 兜底,后续 14-05 替换)。
    2. 创建 `pages/network/mac/history/index.tsx`: `export { default } from '../history';`(与 trajectory 一致)。
    3. 该页面是 **Wave 2 的核心**,14-02/14-04/14-05 都依赖其结构稳定;**不在此任务中实现导出按钮**(14-04)、**不在此任务中实现完整 ErrorAlertWithRetry**(14-05)、**不在此任务中实现最终卡片样式**(14-05 统一收口)。
  </action>
  <verify>
    <automated>cd "D:/CODE/ClaudeCode/xingran-go-backend/xingran-react-frontend" && npx tsc --noEmit -p . 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `pages/network/mac/history.tsx` 与 `pages/network/mac/history/index.tsx` 文件存在
    - `history.tsx` 中 `useColumnConfig` 的 `pageKey` = `'mac.history.list'`,`defaultColumns` 含 8 个 ColumnConfig (key: time/mac/device/port/eventType/vlan/status/action)(grep `key: 'time'` ... `key: 'action'` 全部命中)
    - `history.tsx` 含 5 个预设按钮(grep `近 1h` `近 24h` `近 7d` `近 30d` `近 90d` 全部命中)+ 1 个 `自定义` 按钮
    - `Table` 标签含 `virtual` 属性(grep `virtual` 命中至少 1 行)
    - `useTableQuery` 被调用,`queryKey` 资源名 `'mac.history.list'`,`placeholderData: keepPreviousData` 经由 hook 内部自动注入
    - `expandedRowRender` 渲染 `MACEventsTimeline`(grep `MACEventsTimeline` 命中 ≥ 1 行)
    - `useEffect` + `URLSearchParams` 读 `deviceId/portName`(grep `URLSearchParams` 命中 ≥ 1 行)
    - `npm run type-check` (或 `npx tsc --noEmit`) 退出码 0
  </acceptance_criteria>
  <done>列表页可访问,默认近 7d,URL 参数可预填,操作列点击展开时间线,事件点击跳轨迹页。颜色/图标与 Phase 13 体系一致。</done>
</task>

</tasks>

<verification>
1. 用户登录后(拥有 `network:mac:list` 权限)访问 `/network/mac/history`,页面无 404,默认 7d 数据加载
2. 切换预设按钮 / 自定义 RangePicker,查询参数更新,表格 useTableQuery refetch
3. 列配置抽屉(`useColumnConfig` 提供)可隐藏/重排 8 列
4. 桌面端表格开启虚拟滚动,行数 ≥ 100 时滚动流畅不闪烁(由 `placeholderData: keepPreviousData` 支撑)
5. 移动端(< 576px)自动切换 List 卡片视图
6. 携带 `?deviceId=xxx` 进入,搜索框自动填入设备 ID
7. 操作列"查看事件"展开,出现垂直时间线;单击事件项跳 `/network/mac/trajectory?mac=...&startTime=...&endTime=...`
8. `npm run type-check` 0 错误
</verification>

<success_criteria>
- [ ] 列表页与时间线组件就位,UI-01 全部满足(8 列 + 筛选 + 分页 + 操作)
- [ ] UI-04 满足(垂直时间线,4 事件类型颜色与图标与 Phase 13 一致)
- [ ] URL 参数注入生效(D-17 锁定)
- [ ] 虚拟滚动 + placeholderData 生效(D-06 锁定)
- [ ] 时间筛选预设 + RangePicker 互斥同步(D-07 锁定)
- [ ] useColumnConfig 8 列配置可隐藏/重排(D-08 锁定)
- [ ] D-01 锁定端点 `/network/history/list` 被严格调用,无备选端点假设
</success_criteria>

<output>
Create `.planning/phases/14-frontend-ux/14-01-SUMMARY.md` when done
</output>
