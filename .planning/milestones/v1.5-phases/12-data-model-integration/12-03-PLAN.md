---
phase: 12-data-model-integration
plan: 03
type: execute
wave: 3
depends_on: [12-01]
files_modified:
  - internal/services/mac_history_query_service.go
  - internal/api/v1/network/mac_history_handler.go
  - internal/api/v1/network/mac_history_router.go
  - internal/api/router.go
autonomous: false
requirements:
  - QUERY-01
user_setup: []
must_haves:
  truths:
    - "用户可按设备ID和接口名查询MAC地址变化历史"
    - "查询支持时间范围过滤（开始时间、结束时间）"
    - "查询结果分页显示（默认每页20条）"
    - "返回设备名称快照，设备删除后历史数据仍然可读"
    - "显示变更类型标签（appeared/disappeared/moved/vlan_changed）"
  artifacts:
    - path: "internal/services/mac_history_query_service.go"
      provides: "历史查询服务（按设备、接口、时间范围查询）"
      min_lines: 100
      contains:
        - "type MACHistoryQueryService interface"
        - "QueryPortHistory"
        - "QueryDeviceHistory"
    - path: "internal/api/v1/network/mac_history_handler.go"
      provides: "历史查询 HTTP 处理器"
      min_lines: 80
      contains:
        - "type MACHistoryHandler struct"
        - "QueryPortHistory"
        - "QueryDeviceHistory"
    - path: "internal/api/v1/network/mac_history_router.go"
      provides: "历史查询路由注册"
      min_lines: 30
      contains:
        - "SetupMACHistoryRouter"
        - "r.POST(\"/history/port\", handler.QueryPortHistory)"
  key_links:
    - from: "internal/api/router.go"
      to: "internal/api/v1/network/mac_history_router.go"
      via: "SetupMACHistoryRouter 调用"
      pattern: "network.SetupMACHistoryRouter\\(networkGroup, core\\)"
    - from: "mac_history_handler.go"
      to: "mac_history_query_service.go"
      via: "服务接口调用"
      pattern: "historyQueryService.QueryPortHistory\\(ctx, req\\)"
    - from: "mac_history_query_service.go"
      to: "sys_device_mac_history (database)"
      via: "GORM 查询"
      pattern: "db.Where\\(\"device_id = \\?\", deviceID\\)"

---

<objective>
提供MAC地址历史数据查询API，支持按设备、接口和时间范围过滤变更事件。

**Purpose:** 让用户能够查询历史MAC地址变化记录，了解设备端口的MAC迁移情况，为网络故障排查和安全审计提供数据支持。

**Output:**
- 历史查询服务（按设备、接口、时间范围查询）
- HTTP 处理器和路由（分页、时间过滤）
- 返回设备名称快照（设备删除后历史仍然可读）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-data-model-integration/12-CONTEXT.md
@.planning/phases/12-data-model-integration/12-RESEARCH.md
@.planning/phases/12-data-model-integration/12-PATTERNS.md
@.planning/ROADMAP.md (Phase 12 requirements QUERY-01)
@.planning/codebase/CONVENTIONS.md (Handler-Service 模式)
@internal/api/v1/network/mac_handler.go (参考现有 MAC 处理器)
@internal/services/mac_collection_service.go (参考查询模式)

## Locked Decisions from CONTEXT.md

**D-04**: 采用混合模式设计历史表 `sys_device_mac_history`
- 存储设备名称快照（`device_name_snapshot` 字段），避免外键关联
- 设备删除后历史数据仍然可读
- 包含 `event_type` 字段（appeared/disappeared/moved/vlan_changed）

**QUERY-01 需求** (来自 REQUIREMENTS.md):
- 按设备ID和接口名查询MAC地址变化历史
- 支持时间范围过滤（开始时间、结束时间）
- 支持分页显示（默认每页20条）
- 显示变更类型（appeared/disappeared/moved/vlan_changed）
- 返回设备名称快照（避免设备删除后无法识别）

## Interfaces from Existing Code

From `internal/api/v1/network/mac_handler.go` (existing MAC handler pattern):
```go
type MACHandler struct {
    macCollectionService *mac_collection_service.MACCollectionService
}

func (h *MACHandler) Query(c *gin.Context) {
    var req QueryRequest
    if err := c.ShouldBindJSON(&req); err != nil {
        response.Error(c, http.StatusBadRequest, "请求参数错误")
        return
    }

    result, err := h.macCollectionService.Query(c.Request.Context(), &req)
    if err != nil {
        response.Error(c, http.StatusInternalServerError, err.Error())
        return
    }

    response.Success(c, result)
}
```

From `.planning/codebase/CONVENTIONS.md` (Handler-Service pattern):
```go
// Service interface
type XxxService interface {
    QueryXxx(ctx context.Context, req *QueryRequest) (*QueryResult, error)
}

// Handler struct
type XxxHandler struct {
    xxxService XxxService
}

// Router setup
func SetupXxxRouter(r *gin.RouterGroup, core *core.Core) {
    svc := module.NewXxxService(core.GetDB())
    handler := module.NewXxxHandler(svc)
    r.POST("/list", handler.List)
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MAC history query service</name>
  <files>internal/services/mac_history_query_service.go</files>
  <read_first>
    internal/services/mac_collection_service.go (参考查询模式)
    internal/models/device_mac_history.go (Plan 01 创建的模型)
    .planning/codebase/CONVENTIONS.md (Service 接口模式)
  </read_first>
  <action>
创建 `internal/services/mac_history_query_service.go`，实现历史查询服务：

1. **定义查询请求结构**:
   ```go
   type PortHistoryQuery struct {
       DeviceID      string `json:"deviceId"`
       InterfaceName string `json:"interfaceName,omitempty"`
       StartTime     string `json:"startTime,omitempty"`     // RFC3339 格式
       EndTime       string `json:"endTime,omitempty"`       // RFC3339 格式
       Current       int    `json:"current"`
       PageSize      int    `json:"pageSize"`
   }

   type DeviceHistoryQuery struct {
       DeviceID  string `json:"deviceId"`
       StartTime string `json:"startTime,omitempty"`
       EndTime   string `json:"endTime,omitempty"`
       Current   int    `json:"current"`
       PageSize  int    `json:"pageSize"`
   }
   ```

2. **定义查询响应结构**:
   ```go
   type MACHistoryRecord struct {
       ID                 string      `json:"id"`
       DeviceID           string      `json:"deviceId"`
       DeviceNameSnapshot string      `json:"deviceNameSnapshot"`
       MACAddress         string      `json:"macAddress"`
       InterfaceName      string      `json:"interfaceName"`
       VLANID             *int        `json:"vlanId,omitempty"`
       EventType          string      `json:"eventType"`
       FirstSeen          time.Time   `json:"firstSeen"`
       LastSeen           time.Time   `json:"lastSeen"`
       CollectedAt        time.Time   `json:"collectedAt"`
   }

   type MACHistoryQueryResult struct {
       List     []MACHistoryRecord `json:"list"`
       Total    int64              `json:"total"`
       Current  int                `json:"current"`
       PageSize int                `json:"pageSize"`
   }
   ```

3. **定义服务接口**:
   ```go
   type MACHistoryQueryService interface {
       QueryPortHistory(ctx context.Context, req *PortHistoryQuery) (*MACHistoryQueryResult, error)
       QueryDeviceHistory(ctx context.Context, req *DeviceHistoryQuery) (*MACHistoryQueryResult, error)
   }
   ```

4. **实现私有结构体** (`macHistoryQueryServiceImpl`):
   - 字段: `db *gorm.DB`
   - 构造函数: `NewMACHistoryQueryService(db *gorm.DB) MACHistoryQueryService`

5. **实现 QueryPortHistory 方法** (按 QUERY-01 需求):
   - 验证请求参数（deviceID 必填，current/pageSize 默认值）
   - 构建查询: `db.WithContext(ctx).Table("sys_device_mac_history")`
   - 添加过滤条件:
     - `device_id = ?`
     - `interface_name = ?` (可选)
     - `first_seen >= ?` (如果提供 startTime)
     - `first_seen <= ?` (如果提供 endTime)
   - 排序: `ORDER BY first_seen DESC`
   - 分页: `.Limit(req.PageSize).Offset((req.Current - 1) * req.PageSize)`
   - 查询总数: `db.Model(&models.DeviceMACHistory{}).Count(&total)`
   - 映射结果到 `MACHistoryRecord` 结构体
   - 返回 `MACHistoryQueryResult`

6. **实现 QueryDeviceHistory 方法**:
   - 类似 `QueryPortHistory`，但不过滤 `interface_name`
   - 支持时间范围过滤
   - 支持分页
   - 返回该设备的所有历史记录

7. **时间解析**:
   - 解析 RFC3339 格式时间字符串（使用 `time.Parse(time.RFC3339, req.StartTime)`）
   - 解析失败时返回 `fmt.Errorf("无效的时间格式")`

8. **分页参数验证**:
   - `current` 默认值为 1
   - `pageSize` 默认值为 20
   - `pageSize` 最大值为 100（防止查询过多数据）

9. **错误处理**:
   - 参数验证失败时返回 error
   - 数据库查询失败时返回 error
   - 使用 `applogger.Errorf` 记录错误
  </action>
  <verify>
    <automated>go build ./internal/services/mac_history_query_service.go</automated>
  </verify>
  <done>
    文件 `internal/services/mac_history_query_service.go` 存在
    包含 `MACHistoryQueryService` 接口定义
    包含 `QueryPortHistory` 和 `QueryDeviceHistory` 方法
    支持时间范围过滤和分页
    `go build ./internal/services/mac_history_query_service.go` 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MAC history query HTTP handlers</name>
  <files>internal/api/v1/network/mac_history_handler.go</files>
  <read_first>
    internal/api/v1/network/mac_handler.go (参考现有处理器模式)
    internal/services/mac_history_query_service.go (Task 1 创建的服务)
    .planning/codebase/CONVENTIONS.md (Handler 模式)
  </read_first>
  <action>
创建 `internal/api/v1/network/mac_history_handler.go`，实现 HTTP 处理器：

1. **定义 Handler 结构体**:
   ```go
   type MACHistoryHandler struct {
       historyQueryService mac_history_query_service.MACHistoryQueryService
   }
   ```

2. **定义构造函数**:
   ```go
   func NewMACHistoryHandler(historySvc mac_history_query_service.MACHistoryQueryService) *MACHistoryHandler {
       return &MACHistoryHandler{historyQueryService: historySvc}
   }
   ```

3. **实现 QueryPortHistory 方法**:
   - 输入: `c *gin.Context`
   - 绑定请求参数:
     ```go
     var req mac_history_query_service.PortHistoryQuery
     if err := c.ShouldBindJSON(&req); err != nil {
         response.Error(c, http.StatusBadRequest, "请求参数错误")
         return
     }
     ```
   - 调用服务: `result, err := h.historyQueryService.QueryPortHistory(c.Request.Context(), &req)`
   - 错误处理:
     ```go
     if err != nil {
         applogger.Errorf("[MAC历史查询] 查询失败: %v", err)
         response.Error(c, http.StatusInternalServerError, err.Error())
         return
     }
     ```
   - 返回响应: `response.PageSuccess(c, result.List, result.Total, result.Current, result.PageSize)`

4. **实现 QueryDeviceHistory 方法**:
   - 类似 `QueryPortHistory`，但使用 `DeviceHistoryQuery` 请求
   - 调用 `QueryDeviceHistory` 服务方法
   - 返回分页结果

5. **错误处理**:
   - 参数错误: 400 Bad Request
   - 服务错误: 500 Internal Server Error
   - 使用 `applogger.Errorf` 记录错误日志

6. **上下文传播**:
   - 使用 `c.Request.Context()` 传递给服务层
   - 确保请求超时和取消信号正确传递

7. **响应格式** (按 CONVENTIONS.md):
   - 使用 `response.PageSuccess` 返回分页数据
   - 格式: `{code: 0, message: "success", data: {list, total, current, pageSize}}`
  </action>
  <verify>
    <automated>go build ./internal/api/v1/network/mac_history_handler.go</automated>
  </verify>
  <done>
    文件 `internal/api/v1/network/mac_history_handler.go` 存在
    包含 `MACHistoryHandler` 结构体
    包含 `QueryPortHistory` 和 `QueryDeviceHistory` 方法
    使用 `response.PageSuccess` 返回分页数据
    `go build ./internal/api/v1/network/mac_history_handler.go` 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 3: Register MAC history query routes</name>
  <files>internal/api/v1/network/mac_history_router.go, internal/api/router.go</files>
  <read_first>
    internal/api/v1/network/mac_router.go (参考现有路由注册)
    internal/api/router.go (主路由文件)
    internal/services/mac_history_query_service.go (Task 1 创建的服务)
    internal/api/v1/network/mac_history_handler.go (Task 2 创建的处理器)
  </read_first>
  <action>
创建 `internal/api/v1/network/mac_history_router.go` 并注册到主路由：

**Part 1: 创建 `internal/api/v1/network/mac_history_router.go`**

1. **定义路由注册函数**:
   ```go
   func SetupMACHistoryRouter(r *gin.RouterGroup, core *core.Core) {
       // 创建查询服务
       historyQueryService := mac_history_query_service.NewMACHistoryQueryService(core.GetDB())

       // 创建处理器
       historyHandler := mac_history_handler.NewMACHistoryHandler(historyQueryService)

       // 注册路由
       r.POST("/history/port", historyHandler.QueryPortHistory)
       r.POST("/history/device", historyHandler.QueryDeviceHistory)
   }
   ```

2. **路由规范** (按 CONVENTIONS.md):
   - `POST /history/port` — 查询指定端口的历史记录
   - `POST /history/device` — 查询指定设备的历史记录
   - 使用 POST 方法支持复杂查询参数（JSON body）

**Part 2: 修改 `internal/api/router.go`**

1. **导入路由包**:
   ```go
   "github.com/xingran-next/xingran-go-backend/internal/api/v1/network"
   ```

2. **在 network 路由组中注册**:
   - 找到 `networkGroup := v1.Group("/network")` 部分
   - 添加: `network.SetupMACHistoryRouter(networkGroup, core)`

3. **路由顺序**:
   - 建议放在现有 MAC 路由之后:
     ```go
     network.SetupMACRouter(networkGroup, core)
     network.SetupMACHistoryRouter(networkGroup, core)  // 新增
     ```

4. **认证中间件**:
   - 确保路由受到认证保护（继承 v1 路由组的认证中间件）
   - 历史查询需要登录用户权限

5. **API 路径**:
   - `/api/v1/network/history/port` — 端口历史查询
   - `/api/v1/network/history/device` — 设备历史查询

6. **错误处理**:
   - 路由注册失败不应阻断应用启动
   - 记录注册日志: `applogger.Infof("MAC历史查询路由已注册")`
  </action>
  <verify>
    <automated>go build ./internal/api/router.go</automated>
  </verify>
  <done>
    文件 `internal/api/v1/network/mac_history_router.go` 存在
    包含 `SetupMACHistoryRouter` 函数
    `internal/api/router.go` 调用 `SetupMACHistoryRouter`
    路由路径为 `/api/v1/network/history/port` 和 `/api/v1/network/history/device`
    `go build ./internal/api/router.go` 编译通过
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 4: Test MAC history query API with real data</name>
  <what-built>
    MAC 历史查询 API 已完成，包括：
    - 端口历史查询: POST /api/v1/network/history/port
    - 设备历史查询: POST /api/v1/network/history/device
    - 支持时间范围过滤和分页
  </what-built>
  <how-to-verify>
    **验证步骤**:

    1. **启动应用**:
       ```bash
       go run cmd/main.go
       ```

    2. **准备测试数据**:
       - 确保历史表中有数据（运行一次 MAC 采集）
       - 或手动插入测试数据到 `sys_device_mac_history` 表

    3. **调用端口历史查询 API**:
       ```bash
       curl -X POST http://localhost:9000/api/v1/network/history/port \
         -H "Content-Type: application/json" \
         -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
         -d '{
           "deviceId": "YOUR_DEVICE_ID",
           "interfaceName": "GigabitEthernet0/1",
           "startTime": "2025-01-01T00:00:00Z",
           "endTime": "2025-12-31T23:59:59Z",
           "current": 1,
           "pageSize": 20
         }'
       ```

    4. **验证响应格式**:
       - 检查 `code` 是否为 `0`（成功）
       - 检查 `data.list` 是否包含历史记录
       - 检查 `data.total` 是否正确
       - 检查每条记录是否包含: `id`, `deviceId`, `deviceNameSnapshot`, `macAddress`, `interfaceName`, `eventType`, `firstSeen`, `lastSeen`

    5. **调用设备历史查询 API**:
       ```bash
       curl -X POST http://localhost:9000/api/v1/network/history/device \
         -H "Content-Type: application/json" \
         -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
         -d '{
           "deviceId": "YOUR_DEVICE_ID",
           "current": 1,
           "pageSize": 20
         }'
       ```

    6. **验证设备删除后历史可读**:
       - 删除一个设备（从 `sys_network_device` 表）
       - 调用设备历史查询 API
       - 检查返回的 `deviceNameSnapshot` 是否仍然显示设备名称

    7. **验证时间范围过滤**:
       - 查询最近 7 天的数据
       - 检查返回的记录是否都在时间范围内
       - 验证 `firstSeen` 字段正确排序（DESC）

    8. **验证分页功能**:
       - 查询第 1 页（current: 1, pageSize: 10）
       - 查询第 2 页（current: 2, pageSize: 10）
       - 检查返回的记录是否正确分页
  </how-to-verify>
  <resume-signal>
    Type "approved" if API works correctly, or describe issues found
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → handler | 不可信的查询参数（deviceID, 时间范围） |
| handler → service | 参数化查询风险 |
| service → database | SQL 注入风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Spoofing | deviceID 参数伪造 | accept | 查询需要认证，用户只能查询有权限的设备（Phase 14 实现） |
| T-12-08 | Injection | 时间范围参数注入 | mitigate | 使用 GORM 参数化查询，时间字符串严格解析（RFC3339） |
| T-12-09 | Information Disclosure | 历史数据暴露网络拓扑 | accept | 本阶段不实施访问控制，Phase 13-14 实现权限管理 |

**Disposition Rationale:**
- T-12-07: 认证由中间件处理，用户只能看到有权限的设备（后续阶段实现）
- T-12-08: 使用 GORM 参数化查询，时间字符串使用 `time.Parse` 严格解析
- T-12-09: 本阶段仅实现查询功能，访问控制在后续阶段完善
</threat_model>

<verification>
## Phase-Level Verification

1. **编译检查**: `go build ./...`
2. **单元测试**: `go test -v ./internal/services/mac_history_query_service.go`
3. **集成测试**: 使用真实数据测试查询 API
4. **手动验证**:
   - 调用 `/api/v1/network/history/port` 验证端口历史查询
   - 调用 `/api/v1/network/history/device` 验证设备历史查询
   - 测试时间范围过滤功能
   - 测试分页功能
   - 验证设备删除后历史数据仍然可读
</verification>

<success_criteria>
1. 端口历史查询 API 正常工作，返回正确的分页数据
2. 设备历史查询 API 正常工作，返回该设备的所有历史记录
3. 时间范围过滤正确，返回指定时间范围内的数据
4. 分页功能正常，支持自定义 current 和 pageSize
5. 设备删除后，历史记录中的 `deviceNameSnapshot` 仍然可读
</success_criteria>

<output>
After completion, create `.planning/phases/12-data-model-integration/12-03-SUMMARY.md`
</output>
