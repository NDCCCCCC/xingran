---
phase: 12-data-model-integration
plan: 04
type: execute
wave: 4
depends_on: [12-01, 12-02, 12-03]
files_modified: [internal/api/router.go, internal/api/v1/network/network_router.go]
autonomous: true
requirements: [QUERY-01]
gap_closure: true
uat_test: 5
uat_gap: 2
root_cause: "路由注册缺失。mac_history_router.go:11-24定义了SetupMACHistoryRouter()函数，但该路由在network_router.go:159中被注册到/mac子路由组下，导致实际路由为/api/v1/network/mac/history/*，而非预期的/api/v1/network/history/*。"
severity: blocker

must_haves:
  truths:
    - "MAC历史查询API路由在主路由中正确注册"
    - "POST /api/v1/network/history/device 返回指定设备的MAC历史记录"
    - "POST /api/v1/network/history/port 返回指定接口的MAC历史记录"
    - "路由路径符合API设计规范（/network/history/* 而非 /network/mac/history/*）"
  artifacts:
    - path: "internal/api/router.go"
      provides: "主路由文件，注册所有模块路由"
      min_lines: 350
    - path: "internal/api/v1/network/network_router.go"
      provides: "网络设备模块路由，包含MAC历史查询路由"
      contains: "SetupMACHistoryRouter"
    - path: "internal/api/v1/network/mac_history_router.go"
      provides: "MAC历史查询路由定义（已存在，无需修改）"
      contains: "SetupMACHistoryRouter"
  key_links:
    - from: "internal/api/router.go"
      to: "internal/api/v1/network/network_router.go"
      via: "networkV1.SetupNetworkRouter(network, core)"
      pattern: "SetupNetworkRouter"
    - from: "internal/api/v1/network/network_router.go"
      to: "internal/api/v1/network/mac_history_router.go"
      via: "SetupMACHistoryRouter(network, core)"
      pattern: "SetupMACHistoryRouter"

---

<objective>
修复MAC历史查询API路由注册位置，使路由路径符合API设计规范。

Purpose: 当前MAC历史查询路由被注册到 /network/mac/history/* 子路径下（network_router.go:159），导致实际访问路径为 /api/v1/network/mac/history/device 和 /api/v1/network/mac/history/port，与UAT测试预期的 /api/v1/network/history/device 和 /api/v1/network/history/port 不一致。需要将SetupMACHistoryRouter调用移至正确的路由组下。

Output: 修复后的路由注册，MAC历史查询API可通过 /api/v1/network/history/* 路径访问
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-data-model-integration/12-UAT.md
@.planning/phases/12-data-model-integration/12-03-SUMMARY.md
@internal/api/router.go
@internal/api/v1/network/network_router.go
@internal/api/v1/network/mac_history_router.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 移除MAC历史路由错误注册</name>
  <files>internal/api/v1/network/network_router.go</files>
  <action>
    在 network_router.go:159 行删除 SetupMACHistoryRouter(mac, core) 调用。
    
    具体步骤：
    1. 定位到第152-160行的MAC地址管理路由组：
       ```go
       // ==================== MAC地址管理路由（独立权限） ====================
       mac := r.Group("/mac")
       mac.Use(middleware.RequirePermissions([]string{
           "network:mac:query",
       }, core))
       {
           SetupMACRouter(mac, core, exportHandler)
           SetupMACHistoryRouter(mac, core)  // 删除这一行
       }
       ```
    2. 删除第159行的 SetupMACHistoryRouter(mac, core)
    3. 保持 mac 路由组只包含 SetupMACRouter 调用
  </action>
  <verify>
    <automated>grep -n "SetupMACHistoryRouter" internal/api/v1/network/network_router.go</automated>
  </verify>
  <done>network_router.go 中不再包含对 SetupMACHistoryRouter 的调用</done>
</task>

<task type="auto">
  <name>Task 2: 在主路由中正确注册MAC历史路由</name>
  <files>internal/api/router.go</files>
  <action>
    在 router.go 的网络设备管理路由组中添加对 SetupMACHistoryRouter 的调用。
    
    具体步骤：
    1. 定位到第295-326行的网络设备管理模块路由组：
       ```go
       // 网络设备管理模块（统一管理）
       network := r.Group("/network")
       network.Use(middleware.JWTAuth(core.JWTManager))
       network.Use(middleware.OperLogMiddleware(core.OperLogService, core))
       network.Use(middleware.RequirePermissions([...], core))
       {
           networkV1.SetupNetworkRouter(network, core)
           // 拓扑管理（MAC过滤规则）
           networkV1.SetupTopologyRouter(network, core.GetDB())
       }
       ```
    2. 在第326行的右大括号前添加一行：
       ```go
       networkV1.SetupMACHistoryRouter(network, core)
       ```
    3. 确保调用位置在 SetupNetworkRouter 之后，但在 network 路由组的闭合大括号之前
  </action>
  <verify>
    <automated>grep -A 2 "SetupTopologyRouter" internal/api/router.go | grep "SetupMACHistoryRouter"</automated>
  </verify>
  <done>router.go 中包含对 SetupMACHistoryRouter 的调用，位于 network 路由组内</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>路由注册修复完成，MAC历史查询API现在注册在 /api/v1/network/history/* 路径下</what-built>
  <how-to-verify>
    1. 启动应用程序：go run cmd/main.go
    2. 检查启动日志，应看到："[路由注册] MAC历史查询路由已注册: /history/port, /history/device, /history/stats"
    3. 使用 curl 或 Postman 测试路由：
       curl -X POST http://localhost:9000/api/v1/network/history/device \
         -H "Content-Type: application/json" \
         -H "Authorization: Bearer <valid-token>" \
         -d '{"deviceId": "uuid-here", "current": 1, "pageSize": 20}'
    4. 预期结果：返回200响应（即使查询为空，也应返回标准响应格式），而非404路由未找到
    5. 确认实际路由路径为 /api/v1/network/history/device（而非 /api/v1/network/mac/history/device）
  </how-to-verify>
  <resume-signal>路由测试通过，输入 "approved" 继续或描述遇到的问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | 未信任的用户请求 crosses here |
| API→Service | 请求参数从HTTP层传递到服务层 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Injection | router.go (SetupMACHistoryRouter call) | mitigate | 路由注册使用框架提供的方法，不涉及用户输入。路由路径由框架固定，无注入风险。 |
| T-12-09 | Tampering | history query endpoints | mitigate | 路由已受 JWTAuth + RequirePermissions 中间件保护，确保只有授权用户可访问。 |

</threat_model>

<verification>
1. 检查 router.go 中包含 SetupMACHistoryRouter 调用
2. 检查 network_router.go 中不再包含 SetupMACHistoryRouter 调用
3. 启动应用，确认日志显示MAC历史查询路由已注册
4. 测试 /api/v1/network/history/device 端点返回200（非404）
5. 测试 /api/v1/network/history/port 端点返回200（非404）
6. 确认路由路径不再包含 /mac/ 子路径
</verification>

<success_criteria>
1. MAC历史查询API可通过 /api/v1/network/history/device 访问（非 /api/v1/network/mac/history/device）
2. MAC历史查询API可通过 /api/v1/network/history/port 访问（非 /api/v1/network/mac/history/port）
3. 应用启动日志显示路由已注册到正确路径
4. UAT测试5（Device History Query API）和测试6（Port History Query API）可通过路由验证
</success_criteria>

<output>
After completion, create `.planning/phases/12-data-model-integration/12-04-SUMMARY.md`
</output>
