---
phase: 12-data-model-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/models/device_mac_history.go
  - internal/services/mac_collection_service.go
  - internal/services/mac_history_service.go
  - internal/core/db/migrations/migration_mac_history.go
autonomous: true
requirements:
  - DATA-01
user_setup: []
must_haves:
  truths:
    - "MAC地址变更自动记录到历史表（appeared/disappeared/moved/vlan_changed）"
    - "变更检测在事务内执行，保证数据一致性"
    - "历史记录包含设备名称快照，设备删除后仍然可读"
    - "历史记录失败不影响当前MAC地址采集"
    - "MAC flapping自动合并，通过更新last_seen避免重复记录"
  artifacts:
    - path: "internal/models/device_mac_history.go"
      provides: "历史数据模型（event_type, first_seen, last_seen, device_name_snapshot）"
      min_lines: 50
      contains:
        - "type DeviceMACHistory struct"
        - "EventType string"
        - "DeviceNameSnapshot string"
        - "FirstSeen time.Time"
        - "LastSeen time.Time"
    - path: "internal/services/mac_collection_service.go"
      provides: "变更检测逻辑（DELETE前查询→对比→写历史）"
      min_lines: 200
      contains:
        - "recordMACChanges"
        - "detectMACChanges"
        - "buildMACStateMap"
    - path: "internal/services/mac_history_service.go"
      provides: "历史记录服务（写历史、合并flapping）"
      min_lines: 80
      contains:
        - "type MACHistoryService interface"
        - "RecordMACChange"
        - "MergeFlappingRecords"
    - path: "internal/core/db/migrations/migration_mac_history.go"
      provides: "数据库迁移脚本（创建主表、分区、索引）"
      min_lines: 60
      contains:
        - "PARTITION BY RANGE (first_seen)"
        - "CREATE INDEX CONCURRENTLY idx_mac_history_first_seen_brin"
        - "USING BRIN"
  key_links:
    - from: "internal/services/mac_collection_service.go"
      to: "internal/services/mac_history_service.go"
      via: "RecordMACChange function call"
      pattern: "historyService.RecordMACChange\\(ctx, device, oldState, newState\\)"
    - from: "internal/services/mac_history_service.go"
      to: "sys_device_mac_history (database)"
      via: "GORM Create operation"
      pattern: "db.Create\\(&historyRecord\\)"
    - from: "internal/core/db/migrations/migration_mac_history.go"
      to: "PostgreSQL database"
      via: "AutoMigrate execution on startup"
      pattern: "db.AutoMigrate\\(&models.DeviceMACHistory\\{\\}\\)"

---

<objective>
创建MAC地址历史数据基础设施，在现有采集流程中集成变更检测和历史记录功能。

**Purpose:** 记录MAC地址的变更事件（appeared/disappeared/moved/vlan_changed），为后续的历史查询和轨迹分析提供数据基础。

**Output:**
- 历史表模型 `DeviceMACHistory`（分区表、BRIN索引）
- 变更检测逻辑集成到 `mac_collection_service.go`
- 历史记录服务 `mac_history_service.go`
- 数据库迁移脚本自动创建分区表和索引
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-data-model-integration/12-CONTEXT.md
@.planning/phases/12-data-model-integration/12-RESEARCH.md
@.planning/phases/12-data-model-integration/12-PATTERNS.md
@.planning/ROADMAP.md (Phase 12 requirements and success criteria)
@internal/models/device_mac_address.go
@internal/services/mac_collection_service.go
@internal/core/db/database.go

## Locked Decisions from CONTEXT.md

**D-01**: 使用"删除前先查询"方案获取旧状态
- 在执行 DELETE 之前，先查询当前 MAC 地址状态到内存
- 与新采集结果对比，识别变更类型
- 然后执行删除→插入操作

**D-02**: 使用状态转换矩阵精确识别 4 种变更类型
- appeared: 新数据存在且旧数据不存在
- disappeared: 新数据不存在且旧数据存在
- moved: MAC 地址相同但接口不同
- vlan_changed: 接口相同但 VLAN 不同
- 复合键：device_id + mac_address + interface_name + vlan_id

**D-03**: 使用智能合并防止 MAC flapping 误判
- 历史表使用 `first_seen` 和 `last_seen` 时间戳
- 自动合并连续的相同状态记录
- 减少重复记录，保留有效时间范围

**D-04**: 采用混合模式设计历史表 `sys_device_mac_history`
- 存储状态快照，每次变更插入新记录（不更新旧记录）
- 使用 `first_seen` 和 `last_seen` 标识记录有效期
- 核心字段：id, device_id, device_name_snapshot, mac_address, interface_name, vlan_id, event_type, first_seen, last_seen, collected_at

**D-06**: 在事务内完成变更检测和历史记录
- 执行顺序：查询旧状态 → 对比识别变更 → 写入历史表 → 删除旧数据 → 插入新数据
- 整个流程在一个数据库事务中完成
- 修改位置：`mac_collection_service.go` 的 `collectDeviceMAC` 方法

**D-07**: 采用非阻塞模式处理历史记录失败
- 历史记录写入失败时只记录错误日志
- 不影响当前 MAC 地址的采集和存储
- 保证采集流程稳定性，历史问题不阻断当前状态

## Interfaces from Existing Code

From `internal/models/device_mac_address.go`:
```go
type DeviceMACAddress struct {
    ID            string
    DeviceID      string
    MACAddress    string
    InterfaceName string
    VLANID        *int
    MACType       MACType
    CollectedAt   time.Time
    CreatedAt     time.Time
}
```

From `internal/services/mac_collection_service.go` (collectDeviceMAC method):
```go
func (s *MACCollectionService) collectDeviceMAC(ctx context.Context, device *models.NetworkDevice) *MACCollectionResult
// Returns: *MACCollectionResult with SuccessCount, ErrorMessage, FilteredCount
// Uses: s.db.WithContext(ctx) for all database operations
// Error handling: Logs panic with defer recovery
```

From `internal/core/db/database.go` (AutoMigrate pattern):
```go
func (d *Database) RunMigrations() error
// Executes migration scripts on startup
// Uses GORM AutoMigrate for table creation
// Executes raw SQL for indexes and constraints
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MAC history data model with partition table support</name>
  <files>internal/models/device_mac_history.go</files>
  <read_first>
    internal/models/device_mac_address.go (reference existing MAC model structure)
    internal/models/base.go (reference GORM base patterns)
  </read_first>
  <action>
创建 `internal/models/device_mac_history.go`，定义历史表模型：

1. **定义事件类型枚举**:
   ```go
   type MACEventType string
   const (
       EventAppeared    MACEventType = "appeared"
       EventDisappeared MACEventType = "disappeared"
       EventMoved       MACEventType = "moved"
       EventVLANChanged MACEventType = "vlan_changed"
   )
   ```

2. **定义历史表模型** (`DeviceMACHistory` struct):
   - `ID` (UUID, primary_key)
   - `DeviceID` (UUID, not null, 外键关联)
   - `DeviceNameSnapshot` (string, size:100, 设备名快照，非外键)
   - `MACAddress` (string, size:30, not null)
   - `InterfaceName` (string, size:100, not null)
   - `VLANID` (*int, 可空)
   - `EventType` (MACEventType, size:20, not null)
   - `FirstSeen` (time.Time, not null, 记录首次出现时间)
   - `LastSeen` (time.Time, not null, 记录最后看到时间)
   - `CollectedAt` (time.Time, not null, 采集时间戳)
   - `CreatedAt` (time.Time, 默认CURRENT_TIMESTAMP)

3. **实现 GORM 钩子**:
   - `BeforeCreate()`: 自动生成 UUID（参考 `device_mac_address.go`）

4. **实现 TableName 方法**:
   - 返回 `"sys_device_mac_history"`

5. **添加 JSON 标签**: 所有字段添加 `json:"fieldName"` 标签，使用 camelCase 命名
  </action>
  <verify>
    <automated>go build ./internal/models/</automated>
  </verify>
  <done>
    文件 `internal/models/device_mac_history.go` 存在
    包含 `DeviceMACHistory` 结构体定义
    包含 `MACEventType` 枚举定义
    所有字段符合 CONTEXT.md D-04 规范
    `go build ./internal/models/` 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MAC history service with change recording and flapping merge</name>
  <files>internal/services/mac_history_service.go</files>
  <read_first>
    internal/services/mac_collection_service.go (参考错误处理和事务模式)
    internal/models/device_mac_history.go (Task 1 创建的模型)
    internal/models/device_mac_address.go (参考现有 MAC 模型)
  </read_first>
  <action>
创建 `internal/services/mac_history_service.go`，实现历史记录服务：

1. **定义服务接口**:
   ```go
   type MACHistoryService interface {
       RecordMACChange(ctx context.Context, device *models.NetworkDevice, oldMACs, newMACs map[MACEvent]*models.DeviceMACAddress) error
       MergeFlappingRecords(ctx context.Context, deviceID string) error
   }
   ```

2. **定义 MACEvent 结构** (用于状态对比):
   ```go
   type MACEvent struct {
       MACAddress    string
       InterfaceName string
       VLANID        *int
   }
   ```

3. **实现私有结构体** (`macHistoryServiceImpl`):
   - 字段: `db *gorm.DB`
   - 构造函数: `NewMACHistoryService(db *gorm.DB) MACHistoryService`

4. **实现 normalizeMACAddress 辅助函数** (按 RESEARCH.md Pitfall 3):
   - 输入: 任意格式的 MAC 地址字符串
   - 处理: 移除所有分隔符（`.`、`:`、`-`），转换为统一格式 `AA:BB:CC:DD:EE:FF`
   - 示例: `aabb.ccdd.eeff` → `AA:BB:CC:DD:EE:FF`, `aa-bb-cc-dd-ee-ff` → `AA:BB:CC:DD:EE:FF`
   - 使用 `strings.ToUpper()` 确保大写
   - 使用正则表达式 `^([0-9A-Fa-f]{2}[:]){5}([0-9A-Fa-f]{2})$` 验证格式

5. **实现 buildMACStateMap 方法**:
   - 输入: `[]models.DeviceMACAddress`
   - 输出: `map[MACEvent]*models.DeviceMACAddress`
   - **关键**: 在构建映射时调用 `normalizeMACAddress()` 规范化 MAC 地址格式
   - 确保 `MACEvent.MACAddress` 字段始终使用统一格式 `AA:BB:CC:DD:EE:FF`
   - 避免不同厂商输出格式导致对比失败（RESEARCH.md Pitfall 3）

6. **实现 RecordMACChange 方法** (按 D-01, D-02, D-03 决策):
   - 输入: device, oldMACs (旧状态映射), newMACs (新状态映射)
   - 对比新旧状态，识别 4 种变更类型:
     - `appeared`: newMACs 有但 oldMACs 没有
     - `disappeared`: oldMACs 有但 newMACs 没有
     - `moved`: MAC 相同但 interface 不同
     - `vlan_changed`: interface 相同但 VLAN 不同
   - 为每个变更创建历史记录，设置 `first_seen` 和 `last_seen`
   - 使用 `db.Create()` 批量插入历史记录
   - 返回 error（非空时记录日志但不阻断采集，D-07）

7. **实现 MergeFlappingRecords 方法** (按 D-03 决策):
   - 查询同一设备的连续相同 MAC 状态记录
   - 更新前一条记录的 `last_seen` 为后一条的 `last_seen`
   - 删除后一条重复记录
   - 减少数据库存储，保留有效时间范围

8. **错误处理**:
   - 使用 `applogger.Errorf` 记录错误
   - 返回 error 供调用方决定是否阻断流程

9. **上下文传播**:
   - 所有数据库操作使用 `db.WithContext(ctx)`
  </action>
  <verify>
    <automated>go build ./internal/services/mac_history_service.go</automated>
  </verify>
  <done>
    文件 `internal/services/mac_history_service.go` 存在
    包含 `MACHistoryService` 接口定义
    包含 `RecordMACChange` 和 `MergeFlappingRecords` 方法
    包含 `normalizeMACAddress()` 函数，格式统一为 `AA:BB:CC:DD:EE:FF`
    `buildMACStateMap()` 中调用 MAC 规范化函数
    `go build ./internal/services/mac_history_service.go` 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate change detection into MAC collection service</name>
  <files>internal/services/mac_collection_service.go</files>
  <read_first>
    internal/services/mac_collection_service.go (完整读取，特别是 collectDeviceMAC 方法第 190-247 行)
    internal/services/mac_history_service.go (Task 2 创建的服务)
    internal/models/device_mac_address.go (现有 MAC 模型)
  </read_first>
  <action>
修改 `internal/services/mac_collection_service.go`，集成变更检测逻辑：

1. **添加依赖注入字段** (在 `MACCollectionService` 结构体中):
   - 添加字段: `macHistoryService MACHistoryService` (使用 Task 2 创建的服务接口)
   - 修改构造函数: 接收 `historyService MACHistoryService` 参数并赋值

2. **修改 collectDeviceMAC 方法** (按 D-01, D-06 决策):
   - 在第 200 行 `DELETE` 操作之前插入变更检测逻辑
   - **Step 5**: 查询旧 MAC 状态（DELETE 前）
     ```go
     // 查询旧MAC地址状态
     var oldMACs []models.DeviceMACAddress
     if err := s.db.WithContext(ctx).Where("device_id = ?", device.ID).Find(&oldMACs).Error; err != nil {
         applogger.Warnf("[MAC采集] %s: 查询旧MAC状态失败: %v", device.DeviceName, err)
         // 继续执行，不阻断采集
     }
     ```

3. **构建状态映射** (在 filteredMACAddresses 之后):
   - 创建 `oldState map[MACEvent]*models.DeviceMACAddress`
   - 创建 `newState map[MACEvent]*models.DeviceMACAddress`
   - **关键**: 使用 `mac_history_service.go` 的 `buildMACStateMap()` 函数
   - 该函数内部已处理 MAC 地址格式规范化（Task 2），确保对比一致性

4. **调用历史记录服务** (在构建映射后、DELETE 前):
   ```go
   // 记录MAC变更历史（非阻塞）
   if s.macHistoryService != nil && len(oldState) > 0 {
       if err := s.macHistoryService.RecordMACChange(ctx, device, oldState, newState); err != nil {
           applogger.Errorf("[MAC采集] %s: 记录历史失败: %v", device.DeviceName, err)
           // 不阻断采集流程（D-07）
       }
   }
   ```

5. **保留现有流程**:
   - Step 8: 删除旧数据（第 201-206 行，保持不变）
   - Step 9: 插入新数据（第 235-243 行，保持不变）

6. **错误处理**:
   - 查询旧状态失败时记录 `Warnf` 日志，继续执行
   - 历史记录失败时记录 `Errorf` 日志，不阻断采集（D-07）

7. **集成测试**:
   - 确保原有采集流程不受影响
   - 确保历史记录在事务外执行（避免锁定表导致采集超时）
  </action>
  <verify>
    <automated>go build ./internal/services/mac_collection_service.go</automated>
  </verify>
  <done>
    `mac_collection_service.go` 包含变更检测逻辑
    在 DELETE 前查询旧状态
    调用 `buildMACStateMap()` 构建状态映射（已包含 MAC 规范化）
    调用 `RecordMACChange` 记录历史
    历史记录失败不阻断采集流程
    `go build ./internal/services/mac_collection_service.go` 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 4: Create database migration for partitioned history table with BRIN index</name>
  <files>internal/core/db/migrations/migration_mac_history.go</files>
  <read_first>
    internal/core/db/database.go (参考迁移脚本执行模式)
    internal/models/device_mac_history.go (Task 1 创建的模型)
    .planning/phases/12-data-model-integration/12-RESEARCH.md (PostgreSQL 分区和 BRIN 索引语法)
  </read_first>
  <action>
创建 `internal/core/db/migrations/migration_mac_history.go`，实现分区表迁移：

1. **定义迁移函数**:
   ```go
   func CreateMACHistoryTable(db *gorm.DB) error
   ```

2. **创建主表** (按 D-04, D-05 决策):
   - 使用 `CREATE TABLE` 语句，声明为 `PARTITION BY RANGE (first_seen)`
   - 所有字段使用 `text/template` 生成 SQL（避免硬编码）
   - 字段定义与 `DeviceMACHistory` 模型一致
   - 示例 SQL:
     ```sql
     CREATE TABLE IF NOT EXISTS sys_device_mac_history (
         id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
         device_id UUID NOT NULL,
         device_name_snapshot VARCHAR(100),
         mac_address VARCHAR(30) NOT NULL,
         interface_name VARCHAR(100) NOT NULL,
         vlan_id INT,
         event_type VARCHAR(20) NOT NULL,
         first_seen TIMESTAMP NOT NULL,
         last_seen TIMESTAMP NOT NULL,
         collected_at TIMESTAMP NOT NULL,
         created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
     ) PARTITION BY RANGE (first_seen);
     ```

3. **创建初始分区** (按 D-05 决策):
   - 创建当前月份和下个月的分区
   - 分区命名规范: `sys_device_mac_history_YYYY_MM`
   - 示例:
     ```sql
     CREATE TABLE IF NOT EXISTS sys_device_mac_history_2025_01
     PARTITION OF sys_device_mac_history
     FOR VALUES FROM ('2025-01-01') TO ('2025-02-01');
     ```

4. **创建 BRIN 索引** (按 RESEARCH.md Pattern 4):
   - 为 `first_seen` 字段创建 BRIN 索引
   - 使用 `CONCURRENTLY` 避免锁定表
   - 设置 `pages_per_range = 128`
   - 示例:
     ```sql
     CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_mac_history_first_seen_brin
     ON sys_device_mac_history USING BRIN (first_seen)
     WITH (pages_per_range = 128);
     ```

5. **创建复合索引** (用于设备+MAC查询):
   ```sql
   CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_mac_history_device_mac
   ON sys_device_mac_history (device_id, mac_address);
   ```

6. **错误处理**:
   - 使用 `db.Exec` 执行 SQL
   - 检查 `error` 并返回
   - 使用 `fmt.Errorf` 包装错误信息

7. **集成到 AutoMigrate**:
   - 在 `internal/core/db/database.go` 的 `RunMigrations()` 中调用
   - 添加在现有迁移脚本之后
  </action>
  <verify>
    <automated>go build ./internal/core/db/migrations/</automated>
  </verify>
  <done>
    文件 `internal/core/db/migrations/migration_mac_history.go` 存在
    包含 `CreateMACHistoryTable` 函数
    SQL 包含 `PARTITION BY RANGE (first_seen)`
    SQL 包含 `BRIN` 索引创建语句
    `go build ./internal/core/db/migrations/` 编译通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handler → service | 不可信的 HTTP 请求参数 |
| service → database | SQL 注入风险 |
| database partition DDL | 分区名称拼接风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Injection | 分区名称拼接（year/month 参数） | mitigate | 使用 GORM 参数化查询或验证年份/月份格式（`^\d{4}$`, `^\d{2}$`） |
| T-12-02 | Information Disclosure | 历史数据暴露网络拓扑 | accept | 本阶段不实施查询 API，Phase 13 实现，暂不涉及 |
| T-12-03 | Denial of Service | 大量历史记录撑爆磁盘 | mitigate | 分区自动清理（Plan 02）+ 磁盘空间监控 |

**Disposition Rationale:**
- T-12-01: 分区名称使用字符串拼接可能导致 SQL 注入，必须参数化或严格验证
- T-12-02: 本阶段仅实现历史记录，查询 API 在 Phase 13，暂不涉及访问控制
- T-12-03: 增量快照（仅记录变更）减少 99% 写入量，分区清理（Plan 02）防止无限增长
</threat_model>

<verification>
## Phase-Level Verification

1. **编译检查**: `go build ./...`
2. **代码审查**: 验证 MAC 地址规范化逻辑是否正确实现
3. **集成测试**: 验证变更检测逻辑正确识别 4 种事件类型
4. **数据库验证**: 检查分区表和索引是否正确创建
   - 查询 `pg_catalog.pg_inherits` 确认分区存在
   - 查询 `pg_indexes` 确认 BRIN 索引存在
</verification>

<success_criteria>
1. MAC 地址变更自动记录到历史表，包含设备名称快照
2. 变更检测正确识别 appeared/disappeared/moved/vlan_changed 事件
3. MAC 地址格式统一规范化，不同厂商输出格式不影响对比准确性
4. 历史记录失败不影响当前 MAC 地址采集（非阻塞）
5. 历史表使用月度分区存储，BRIN 索引优化时间序列查询
6. 采集流程完整性不受影响（所有现有测试通过）
</success_criteria>

<output>
After completion, create `.planning/phases/12-data-model-integration/12-01-SUMMARY.md`
</output>
