---
phase: 12-data-model-integration
plan: 05
type: execute
wave: 4
depends_on: [12-02]
files_modified: [internal/core/core.go, cmd/main.go]
autonomous: true
requirements: [DATA-02]
gap_closure: true
uat_test: 1
uat_gap: 0
root_cause: "任务注册时机错误。cmd/main.go:115-120的任务注册发生在core.Init()之后，调度器已启动并从数据库加载任务。此时注册的任务不会出现在当前运行周期的任务列表中，直到应用重启。需要将任务注册移至core.go:236-273的调度器初始化代码中，在调度器Start()之后、从数据库加载任务之前注册。"
severity: major

must_haves:
  truths:
    - "MAC历史清理任务在调度器任务列表中可见"
    - "应用启动后清理任务自动注册，无需等待重启"
    - "network.mac.history.retention_days配置项存在于sys_config表中"
    - "清理任务按预期时间执行（每天凌晨2点）"
  artifacts:
    - path: "internal/core/core.go"
      provides: "核心模块初始化，包含调度器初始化逻辑"
      contains: "Scheduler.*Start"
    - path: "cmd/main.go"
      provides: "应用入口，移除错误位置的任务注册"
      min_lines: 130
    - path: "internal/scheduler/mac_history_tasks.go"
      provides: "MAC历史清理任务定义（已存在，无需修改）"
      contains: "RegisterMACHistoryTasks"
  key_links:
    - from: "cmd/main.go"
      to: "internal/core/core.go"
      via: "core.Init() → scheduler initialization"
      pattern: "coreModule.Init()"
    - from: "internal/core/core.go"
      to: "internal/scheduler/mac_history_tasks.go"
      via: "RegisterMACHistoryTasks(scheduler)"
      pattern: "RegisterMACHistoryTasks"

---

<objective>
修复MAC历史清理任务注册时机，确保任务在应用启动时自动注册到调度器。

Purpose: 当前MAC历史清理任务注册在cmd/main.go的core.Init()之后（line 115-120），此时调度器已经启动并从数据库加载了任务列表，导致新注册的任务不会出现在当前运行周期的任务列表中。需要将任务注册移至core.go的调度器初始化代码中（line 236-273区域），在调度器Start()调用之后、从数据库加载任务之前完成注册。

Output: MAC历史清理任务在应用启动时正确注册，立即可在调度器任务列表中看到
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-data-model-integration/12-UAT.md
@.planning/phases/12-data-model-integration/12-02-SUMMARY.md
@internal/core/core.go
@cmd/main.go
@internal/scheduler/mac_history_tasks.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在core.go中注册MAC历史清理任务</name>
  <files>internal/core/core.go</files>
  <action>
    在 core.go 的调度器初始化代码中添加MAC历史清理任务注册。
    
    具体步骤：
    1. 定位到第235-256行的调度器初始化代码：
       ```go
       // 10. 初始化定时任务调度器
       c.Scheduler = scheduler.NewScheduler(c.GetDB())
       if err := c.Scheduler.Start(context.Background()); err != nil {
           applogger.Warnf("启动调度器失败: %v", err)
       } else {
           applogger.Infof("定时任务调度器启动成功")
           // 设置全局调度器引用
           scheduler.SetGlobalScheduler(c.Scheduler)
           // 注册网络设备相关定时任务
           scheduler.RegisterNetworkDeviceTasks(c.Scheduler, c.GetDB())
           // 注册通知相关定时任务
           scheduler.RegisterNoticeTasks(c.Scheduler)
           // 注册运维工单相关定时任务
           scheduler.RegisterWorkOrderTasks(c.Scheduler)
       }
       ```
    2. 在第255行（RegisterWorkOrderTasks之后）添加MAC历史清理任务注册：
       ```go
       // 注册MAC历史清理任务
       if c.PartitionService != nil {
           mac_history_tasks.SetPartitionService(c.PartitionService)
           mac_history_tasks.RegisterMACHistoryTasks(c.Scheduler)
           applogger.Infof("MAC历史清理任务注册完成")
       }
       ```
    3. 在文件顶部import区域添加：
       ```go
       mac_history_tasks "github.com/xingran-next/xingran-go-backend/internal/scheduler"
       ```
  </action>
  <verify>
    <automated>grep -n "RegisterMACHistoryTasks" internal/core/core.go</automated>
  </verify>
  <done>core.go 中包含MAC历史清理任务注册代码，位于调度器初始化代码块内</done>
</task>

<task type="auto">
  <name>Task 2: 移除main.go中的错误任务注册</name>
  <files>cmd/main.go</files>
  <action>
    从 main.go 中移除错误的MAC历史清理任务注册代码。
    
    具体步骤：
    1. 定位到第114-120行：
       ```go
       // 注册MAC历史清理任务
       if coreModule.Scheduler != nil && coreModule.PartitionService != nil {
           mac_history_tasks.SetPartitionService(coreModule.PartitionService)
           mac_history_tasks.RegisterMACHistoryTasks(coreModule.Scheduler)
           applogger.Infof("MAC历史清理任务已注册")
       }
       ```
    2. 删除整个if语句块（第114-120行）
    3. 同时删除第16行的import（如果不再使用）：
       ```go
       mac_history_tasks "github.com/xingran-next/xingran-go-backend/internal/scheduler"
       ```
  </action>
  <verify>
    <automated>grep -n "RegisterMACHistoryTasks\|mac_history_tasks" cmd/main.go</automated>
  </verify>
  <done>main.go 中不再包含MAC历史清理任务注册代码</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>任务注册时机修复完成，MAC历史清理任务在应用启动时自动注册</what-built>
  <how-to-verify>
    1. 启动应用程序：go run cmd/main.go
    2. 检查启动日志，应按顺序看到：
       - "定时任务调度器启动成功"
       - "网络设备定时任务注册完成"
       - "通知定时任务注册完成"
       - "运维工单定时任务注册完成"
       - "MAC历史清理任务注册完成"（新增）
    3. 访问调度器任务列表API（如果存在）或查询数据库：
       SELECT * FROM sys_job WHERE job_type = 'mac_history_cleanup';
    4. 预期结果：能查询到任务条目，invoke_interval字段为 "0 0 2 * * ?"（每天凌晨2点）
    5. 验证配置项存在：
       SELECT * FROM sys_config WHERE config_key = 'network.mac.history.retention_days';
    6. 预期结果：配置项存在，config_value为 "120"
  </how-to-verify>
  <resume-signal>任务注册验证通过，输入 "approved" 继续或描述遇到的问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application→scheduler | 应用启动时注册定时任务 |
| scheduler→database | 调度器从数据库加载任务列表 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Tampering | mac_history cleanup task registration | mitigate | 任务注册在应用启动时执行，非用户可控。PartitionService在注册前已初始化，使用nil检查防止空指针。 |
| T-12-11 | Denial of Service | cleanup task execution frequency | mitigate | 清理任务配置为每天执行一次（凌晨2点），通过Cron表达式控制，不会导致资源耗尽。 |

</threat_model>

<verification>
1. 检查 core.go 中包含MAC历史清理任务注册代码
2. 检查 main.go 中不再包含MAC历史清理任务注册代码
3. 启动应用，确认日志显示"MAC历史清理任务注册完成"
4. 查询 sys_job 表，确认 mac_history_cleanup 任务存在
5. 查询 sys_config 表，确认 network.mac.history.retention_days 配置存在
6. 确认任务注册发生在调度器初始化代码块内（而非main.go）
</verification>

<success_criteria>
1. 应用启动日志显示"MAC历史清理任务注册完成"
2. sys_job表中存在job_type='mac_history_cleanup'的任务记录
3. sys_config表中存在config_key='network.mac.history.retention_days'的配置记录
4. UAT测试1（Cold Start Smoke Test）中清理任务在任务列表中可见
5. UAT测试8（Scheduled Cleanup Task）可通过任务列表验证
</success_criteria>

<output>
After completion, create `.planning/phases/12-data-model-integration/12-05-SUMMARY.md`
</output>
