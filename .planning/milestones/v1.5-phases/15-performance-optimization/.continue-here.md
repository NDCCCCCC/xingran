---
context: phase
phase: 15-performance-optimization
task: 04b
total_tasks: 5
status: paused
last_updated: 2026-06-15T04:06:05.855Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: `internal/services/system/` 是 `system` 包不是 `services` 包 — 若新文件放这里必须用 `package system` (我曾误放 `sys_config_seed.go` 到此目录,因 package 不匹配导致 LSP 报 17 个文件级 BrokenImport)
- [ ] CONSTRAINT: `Config` 模型无 `IsBuiltin` 字段,只有 `ConfigName` (not null) / `ConfigKey` (unique) / `ConfigValue` / `ConfigType` / `IsSystem` / `Remark`
- [ ] CONSTRAINT: `CacheConfigService` 没有 `GetConfigValueByKey` 方法,只缓存 Duration 类型;int/string 配置必须走 DB 读 sys_config

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Wrong package in subdir | 把 `package services` 的新文件放进 `internal/services/system/` 子目录 → LSP BrokenImport | blocking | 创建新文件前先 `head -3 <邻居文件>` 确认包名 |
| Assume Config model field | 用 `IsBuiltin` 等不存在的字段 → 编译错 | advisory | 用前 `grep "type Config struct"` 看实际字段 |
| Assume CacheConfigService API | 用 `GetConfigValueByKey` 等不存在的 API | advisory | 用前 `grep "func.*CacheConfigService.*) Get"` 看实际方法 |

<current_state>
Phase 15 性能优化 — 4/5 计划已完成 commit,15-04b (前端 ECharts 热力图页) + 15-05 (E2E 验证) 因 context 紧张未启动。后端所有能力已就绪 (DB 索引 + 4 MV + 5分钟 cron + 3 查询缓存装饰 + heatmap 后端 API)。
</current_state>

<completed_work>

- ✅ 15-01: 复合 B-tree 索引 `(device_id, mac_address, first_seen)` + 4 个物化视图 (mv_mac_port_latest / mv_mac_device_summary / mv_mac_long_occupancy_top / mv_mac_port_daily_count) + 各自 UNIQUE 索引
- ✅ 15-02: `mac_history_matview_refresh` cron 任务 (0 */5 * * * *) + 部分失败容错 service + 3 个 sys_config 键 seed + Core 注册
- ✅ 15-03: 缓存键构造函数 (SHA-256 + JSON params) + cache-aside 装饰 3 个查询方法 (port-history/device-history/stats),trajectory 不缓存
- ✅ 15-04a: HeatmapService + HeatmapHandler + 路由 `POST /network/history/heatmap`,数据源 MV-04,默认 7 天/TopN 100

Commits:
- 5e54f32 feat(15-01)
- 3805848 feat(15-02)
- bdf9380 feat(15-03)
- 9614cda feat(15-04a)
</completed_work>

<remaining_work>

- ❌ 15-04b: 前端 5 个文件 (ECharts heatmap 组件 + 页面 + API 包装 + 路由 + 菜单 SQL migration)
  - `xingran-react-frontend/src/components/network/MACHeatmapChart.tsx`
  - `xingran-react-frontend/src/pages/network/mac/heatmap.tsx`
  - `xingran-react-frontend/src/lib/api/macHeatmapApi.ts`
  - `xingran-react-frontend/src/router/routeConfigManager.ts` (新增 path)
  - `internal/core/db/migrations/15X_mac_heatmap_menu.sql` + `migration_NNN_mac_heatmap_menu.go`
- ❌ 15-05: `internal/services/mac_history_perf_verify_test.go` (EXPLAIN ANALYZE 3-5 查询断言) + `.planning/phases/15-performance-optimization/15-VERIFICATION.md`
- ❌ code-review gate
- ❌ 阶段验证 + ROADMAP 更新
</remaining_work>

<decisions_made>

- 缓存键: `<prefix>:<sha256(json(params))>`,不带 `xingran:` 前缀 (Core 自动加)
- CacheConfigService.GetDuration 兜底 5 分钟 (300 秒)
- perfTopN 不走 CacheConfigService (它不存 int),改走 DB 读 sys_config `network.mac.perf.heatmap_top_n`,失败 fallback 100
- QueryMACTrajectory 不缓存 (D-12 锁定)
- 每个方法缓存命中失败 → `applogger.Warnf` + 走 DB 直查 (D-15 降级)
- MV-03 (长期占用 Top-50) 用 GROUP BY + ORDER BY total_duration DESC LIMIT 50,UNIQUE 索引 (mac_address, last_port)
- 4 个 MV 全部建在分区表 sys_device_mac_history 上享受分区裁剪
</decisions_made>

<blockers>
- Context 78% used (剩余 22%) — 无法继续前端实施
</blockers>

## Required Reading (in order)

1. `.planning/phases/15-performance-optimization/15-CONTEXT.md` — D-01..D-18 全部锁定决策
2. `.planning/phases/15-performance-optimization/15-04b-PLAN.md` — 前端实施任务清单
3. `internal/services/mac_history_heatmap_service.go` — 后端 API 响应结构 (HeatmapCell / HeatmapResult)
4. `internal/api/v1/network/mac_history_heatmap_handler.go` — POST /network/history/heatmap
5. `xingran-react-frontend/src/pages/network/mac/trajectory.tsx` — 参考 UI 模式 (ECharts + 时间预设)
6. `xingran-react-frontend/src/components/network/MACTrajectoryChart.tsx` — 参考 ECharts 组件模式

## Critical Anti-Patterns (do NOT repeat these)

- **包名错位**: 写 `internal/services/system/<新文件>.go` 时必须用 `package system` (我曾误用 `package services` 触发 17 个文件级 BrokenImport)
- **不存在的字段**: 不要用 `IsBuiltin` 等虚构的 Config 字段,只用具名实际字段
- **不存在的 API**: 不要用 `GetConfigValueByKey`,CacheConfigService 只缓存 Duration

## Infrastructure State

- PostgreSQL: 数据库驱动 `gorm.io/driver/postgres` 已在 `cmd/main.go` 配置
- Redis: `core.Cache` 已就位,DataCacheService.GetOrSet 已实现
- Cron: `internal/scheduler/` 框架已就位,RegisterTask + AddJob 双注册模式
- 路由: `internal/api/v1/network/mac_history_router.go` 已包含 /history/heatmap

<context>
所有 Phase 15 后端架构 (DB 索引 + 4 MV + 5 分钟 cron + cache-aside + heatmap API) 已完成 commit。下一步只需 5 个前端文件把后端 API 暴露给用户,以及一个验证测试 + VERIFICATION.md。Context 紧张时主动停止优于继续但质量下降。
</context>

<next_action>
执行 `/gsd:resume-work` 续接 15-04b。第一个动作:Read `xingran-react-frontend/src/pages/network/mac/trajectory.tsx` 确认 UI 模式,然后并行创建 5 个前端文件 (Chart + Page + API + Router + SQL migration)。
</next_action>
