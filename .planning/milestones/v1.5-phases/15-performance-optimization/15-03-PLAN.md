---
phase: 15-performance-optimization
plan: 03
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - internal/services/mac_history_query_service.go
  - internal/services/mac_history_cache_decorator.go
  - internal/services/mac_history_query_service_test.go
autonomous: true
requirements:
  - PERF-03
must_haves:
  truths:
    - `QueryPortHistory` / `QueryDeviceHistory` / `QueryConnectionStats` wrap SQL with cache-aside
    - `QueryMACTrajectory` is NOT cached
    - Cache key format `mac:query:<method>:<sha256(normalized-params)>`, TTL = 5 minutes
    - Redis unavailable → SQL direct query, no error returned to caller
    - Reuses existing `DataCacheService` and `CacheConfigService`, no new cache layer
    - `go build ./...` exits 0
  artifacts:
    - path: "internal/services/mac_history_cache_decorator.go"
      provides: "Cache-aside wrapper logic + SHA-256 key builder"
      contains: "buildCacheKey"
    - path: "internal/services/mac_history_query_service.go"
      provides: "Service impl with cache integration in 3 methods"
      contains: "macHistoryQueryServiceImpl"
  key_links:
    - from: "internal/services/mac_history_cache_decorator.go"
      to: "DataCacheService"
      via: "GetOrSet pattern"
      pattern: "dataCacheService\\.GetOrSet"
    - from: "internal/services/mac_history_cache_decorator.go"
      to: "CacheConfigService"
      via: "TTL lookup"
      pattern: "cacheConfigService\\.GetTTL"
---

<objective>
在 `macHistoryQueryService` 实现层注入 cache-aside 装饰:为 `QueryPortHistory` / `QueryDeviceHistory` / `QueryConnectionStats` 三个高频点查方法加 5 分钟 Redis 缓存,`QueryMACTrajectory` 不缓存。Redis 不可用降级 SQL 直查,符合 D-12/D-15 锁定。

Purpose: 落地 PERF-03 缓存层;复用 `DataCacheService` + `CacheConfigService` + `pkg/cache` 已就位能力,不引入新缓存层。

Output: 1 个新装饰器文件 + 扩展 query service 实现 + 扩展单元测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-performance-optimization/15-CONTEXT.md
@.planning/phases/13-query-layer-trajectory/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 cache 装饰器 (key + decorator + degrade)</name>
  <files>internal/services/mac_history_cache_decorator.go</files>
  <read_first>
    - internal/services/mac_history_query_service.go (PortHistoryQuery / DeviceHistoryQuery / ConnectionStatsQuery struct 定义)
    - internal/services/data_cache_service.go (DataCacheService.GetOrSet 签名)
    - internal/services/cache_config_service.go (CacheConfigService.GetTTL 签名)
    - pkg/cache/redis.go (cache.Cache 接口)
  </read_first>
  <action>
    新建 `internal/services/mac_history_cache_decorator.go`:

    1. 包名 `services`。
    2. import 含 `crypto/sha256` + `encoding/hex` + `encoding/json` + `time`。
    3. 常量:`cacheKeyPrefixPortHistory = "mac:query:port-history"`、`cacheKeyPrefixDeviceHistory = "mac:query:device-history"`、`cacheKeyPrefixStats = "mac:query:stats"`、`cacheKeyPrefixHeatmap = "mac:query:heatmap"`、`defaultCacheTTLSeconds = 300`。
    4. 导出 `BuildMACQueryCacheKey(method string, params interface{}) (string, error)`:
       - `json.Marshal` 序列化 params(struct 字段顺序稳定)。
       - `sha256.Sum256` → hex 字符串(64 字符)。
       - 返回 `<prefix>:<sha256>`,**不带** `xingran:` 前缀(Core 缓存层自动加,沿用 D-13-3.2 风格)。
       - 序列化失败返回 error。
    5. 导出 `GetCacheTTL(cacheConfig CacheConfigService) time.Duration`:
       - 尝试 `cacheConfig.GetTTL(ctx, "network.mac.perf.cache_ttl_seconds")`,失败 fallback 5 分钟。
    6. 导出 `WithCacheAside(ctx, cache DataCacheService, key, ttl, queryFn) (interface{}, error)`:
       - 先 `cache.Get(ctx, key, &result)`,命中且无错直接返回。
       - 未命中或 Get 错时,`applogger.Warnf("perf_cache_unavailable: %v", err)` 继续走 queryFn。
       - 调 `queryFn()` 拿原始结果,`data, _ := json.Marshal(result); cache.Set(ctx, key, data, ttl)`,Set 失败只 warn 不 return。
       - 返回原始 result。
       - 严格遵循 D-15 锁定:Redis 错误不阻断主流程。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go build ./internal/services/... 2>&1 | head -20</automated>
  </verify>
  <done>
    - mac_history_cache_decorator.go 存在,导出 `BuildMACQueryCacheKey` + `WithCacheAside`
    - 不带 `xingran:` 前缀(Core 自动加)
    - `go build ./internal/services/...` 退出码 0
  </done>
</task>

<task type="auto">
  <name>Task 2: 在 query service impl 注入装饰器</name>
  <files>internal/services/mac_history_query_service.go</files>
  <read_first>
    - internal/services/mac_history_query_service.go (现有 `macHistoryQueryServiceImpl` 结构与 4 个方法)
  </read_first>
  <action>
    修改 `internal/services/mac_history_query_service.go`:

    1. 在 `macHistoryQueryServiceImpl` struct 上追加 2 个字段:`dataCache DataCacheService` + `cacheConfig CacheConfigService`。
    2. 修改构造函数 `NewMACHistoryQueryService(db *gorm.DB)` 为接收 `DataCacheService` + `CacheConfigService` 参数(或保持 1 个参数但内部从 `core.GetCache()` 之类获取 — 参照 DataCacheService 在 Core 中的获取方式)。如果其他调用方已存在,需要同步更新 `internal/api/v1/network/mac_history_router.go:13` 的 `NewMACHistoryQueryService(core.GetDB())` 调用。
    3. **不动接口签名** — `MACHistoryQueryService` 接口保持 4 个方法。
    4. 改造 3 个方法:
       - `QueryPortHistory`:先尝试 `WithCacheAside(ctx, dataCache, BuildMACQueryCacheKey("port-history", req), GetCacheTTL(cacheConfig), func()...)`,queryFn 内执行原 SQL。返回值赋给 `result`。
       - `QueryDeviceHistory`:同上,键前缀 `device-history`。
       - `QueryConnectionStats`:同上,键前缀 `stats`。
       - `QueryMACTrajectory`:**不加装饰**,保持原样(参数组合爆炸)。
    5. queryFn 内部:沿用方法体原 SQL,GORM `.WithContext(ctx).Where(...).Find(&list).Count(&total)`,不要修改查询逻辑,只套装饰。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go build ./internal/services/... 2>&1 | head -20
  </verify>
  <done>
    - mac_history_query_service.go 中 3 个方法体内包含 `WithCacheAside` 或 `dataCache.Get` 调用
    - `QueryMACTrajectory` 方法体内不含 `WithCacheAside` 或 `dataCache.Get`
    - `go build ./internal/services/...` 退出码 0
  </done>
</task>

<task type="auto">
  <name>Task 3: 单元测试覆盖 (cache hit / miss / degrade)</name>
  <files>internal/services/mac_history_query_service_test.go</files>
  <read_first>
    - internal/services/mac_history_query_service_test.go (现有测试结构,扩展新场景)
    - internal/services/data_cache_service.go (DataCacheService.Get 错误返回测试模式)
  </read_first>
  <action>
    修改 `internal/services/mac_history_query_service_test.go`,追加 3 个测试函数:

    1. `TestQueryPortHistory_CacheHit`:mock DataCacheService.Get 返回缓存值,断言 SQL 未执行(用 sqlmock 或 mock counter),响应等于缓存值。
    2. `TestQueryPortHistory_CacheMiss`:mock Get 返回 miss,Set 调用 1 次,SQL 执行 1 次,响应正确。
    3. `TestQueryPortHistory_CacheDegrade`:mock Get 返回 error,断言 SQL 仍执行,响应正确,无 error 返回(降级语义)。
    4. 同样 3 个模式可对 QueryDeviceHistory 重复(共 6 测试函数),或简化为只测 1 个 + `TestQueryMACTrajectory_NoCache` 验证 trajectory 不走 cache(第 7 个测试)。

    风格沿用现有测试:用 `*testing.T` + `gomock` 或直接 `sqlmock`,不要引入新依赖。Go 1.24 已内置 `testing.T.Setenv` 可用。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go test ./internal/services/ -run TestQueryPortHistory -v 2>&1 | head -30
  </verify>
  <done>
    - 测试文件含 4-7 个新测试函数(hit / miss / degrade / trajectory-no-cache)
    - `go test ./internal/services/ -run TestQueryPortHistory` 退出码 0
  </done>
</task>

</tasks>

<verification>
- `go build ./...` 退出码 0
- 3 个查询方法走 cache-aside,1 个不缓存
- 单元测试覆盖 hit / miss / degrade
</verification>

<success_criteria>
- Redis 缓存 5 分钟 TTL,与物化视图刷新周期对齐
- 缓存键 `mac:query:<method>:<sha256>`
- Redis 不可用时 SQL 直查降级
- `go build ./...` 退出码 0
</success_criteria>

<output>
Create `.planning/phases/15-performance-optimization/15-03-SUMMARY.md` when done
</output>
