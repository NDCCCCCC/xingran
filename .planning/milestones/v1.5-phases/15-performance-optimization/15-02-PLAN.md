---
phase: 15-performance-optimization
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - internal/services/mac_history_matview_service.go
  - internal/scheduler/mac_history_matview_tasks.go
  - internal/scheduler/mac_history_tasks.go
  - internal/services/system/sys_config_seed.go
  - internal/core/core.go
autonomous: true
requirements:
  - PERF-02
must_haves:
  truths:
    - Cron job `mac_history_matview_refresh` registered in `internal/scheduler` with expression `0 */5 * * * *`
    - Task refreshes 4 MVs in order MV-01 → MV-02 → MV-03 → MV-04 using REFRESH MATERIALIZED VIEW CONCURRENTLY
    - Partial-failure tolerance: one MV failure does not abort the rest
    - `sys_config` has 3 keys: `network.mac.perf.mat_view_refresh_cron` (default `0 */5 * * * *`), `network.mac.perf.cache_ttl_seconds` (default 300), `network.mac.perf.heatmap_top_n` (default 100)
    - `go build ./...` exits 0
  artifacts:
    - path: "internal/scheduler/mac_history_matview_tasks.go"
      provides: "Matview refresh task registration + handler"
      contains: "mac_history_matview_refresh"
    - path: "internal/services/mac_history_matview_service.go"
      provides: "MV refresh service with partial-failure handling"
      contains: "RefreshAllMaterializedViews"
    - path: "internal/core/core.go"
      provides: "Core init calls matview task registration"
      contains: "RegisterMACHistoryMatViewTasks"
  key_links:
    - from: "internal/scheduler/mac_history_matview_tasks.go"
      to: "internal/services/mac_history_matview_service.go"
      via: "service method call"
      pattern: "mac_history_matview_service\\.RefreshAllMaterializedViews"
    - from: "internal/core/core.go"
      to: "internal/scheduler/mac_history_matview_tasks.go"
      via: "init hook"
      pattern: "scheduler\\.RegisterMACHistoryMatViewTasks"
---

<objective>
注册统一的 5 分钟定时刷新任务,使用 REFRESH MATERIALIZED VIEW CONCURRENTLY 避免锁表,实现"单视图失败不阻断后续"的容错语义;3 个配置项 (cron / ttl / topN) 落到 sys_config 表。

Purpose: 落地 PERF-02 刷新语义;配置外部化让运维可运行时调整;复用 Phase 12 `RegisterMACHistoryTasks` 模式(任务处理器 + 数据库 Job 记录双注册)。

Output: 5 个文件(1 service + 1 scheduler task + 2 init 调用 + 1 sys_config seed)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-performance-optimization/15-CONTEXT.md
@.planning/phases/12-data-model-integration/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现物化视图刷新服务</name>
  <files>internal/services/mac_history_matview_service.go</files>
  <read_first>
    - internal/services/mac_history_partition.go (service 层文件结构与 package 风格)
    - internal/core/db/database.go (isPostgreSQL 工具函数)
    - .planning/phases/15-performance-optimization/15-CONTEXT.md (D-10/D-11 锁定)
  </read_first>
  <action>
    新建 `internal/services/mac_history_matview_service.go`:

    1. 包名 `services`,import `applogger` + `gorm.io/gorm`。
    2. 接口 `MACHistoryMatViewService` 含 `RefreshAllMaterializedViews(ctx) error` 与 `RefreshSingleMatView(ctx, name string) error`。
    3. 私有实现 `macHistoryMatViewServiceImpl{ db *gorm.DB }`。
    4. 构造函数 `NewMACHistoryMatViewService(db *gorm.DB) MACHistoryMatViewService`。
    5. `RefreshSingleMatView`:检查 PG → 校验 name 在白名单(mv_mac_port_latest / mv_mac_device_summary / mv_mac_long_occupancy_top / mv_mac_port_daily_count)→ 执行 `REFRESH MATERIALIZED VIEW CONCURRENTLY ` + name,失败 `applogger.Errorf` + wrapped error。
    6. `RefreshAllMaterializedViews`:视图顺序硬编码 `[]string{"mv_mac_port_latest", "mv_mac_device_summary", "mv_mac_long_occupancy_top", "mv_mac_port_daily_count"}`;循环中 `var firstErr error` 捕获第一个失败但**不 return**;循环结束 `applogger.Infof` 报告成功 N 失败 M;`firstErr != nil` 返回 `fmt.Errorf("物化视图刷新部分失败: %w", firstErr)`,否则 nil。严禁循环遇错 break(D-11 部分失败容错锁定)。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go build ./internal/services/... 2>&1 | head -20</automated>
  </verify>
  <done>
    - mac_history_matview_service.go 存在,导出 `MACHistoryMatViewService` 接口
    - `RefreshAllMaterializedViews` 实现部分失败容错
    - `go build ./internal/services/...` 退出码 0
  </done>
</task>

<task type="auto">
  <name>Task 2: 注册调度任务</name>
  <files>internal/scheduler/mac_history_matview_tasks.go, internal/scheduler/mac_history_tasks.go</files>
  <read_first>
    - internal/scheduler/mac_history_tasks.go (Phase 12 已交付的清理任务注册模式)
    - internal/scheduler/cron.go (Scheduler.RegisterTask 签名 + Job model 字段)
    - internal/scheduler/ad_sync_tasks.go (参考另一个 cron 任务注册风格)
  </read_first>
  <action>
    新建 `internal/scheduler/mac_history_matview_tasks.go`:

    1. 包名 `scheduler`。
    2. 导出 `RegisterMACHistoryMatViewTasks(scheduler *Scheduler, db *gorm.DB, matViewSvc services.MACHistoryMatViewService)`。
    3. 函数体:
       - `scheduler.RegisterTask("mac_history_matview_refresh", func(ctx context.Context, params map[string]interface{}) error { return matViewSvc.RefreshAllMaterializedViews(ctx) })`
       - 仿照 `RegisterMACHistoryTasks` 模式,检查 `Job` 表是否已存在 `MAC历史物化视图刷新` 任务,不存在则创建,`CronExpression = "0 */5 * * * *"`(6 字段,每 5 分钟),`InvokeTarget = "mac_history_matview_refresh"`,`JobGroup = "SYSTEM"`,`MisfirePolicy = models.MisfirePolicyExecuteOnce`,`Status = 0`。
       - 计算 `NextRunTime = 下一个 5 分钟整点`。
       - 末尾 `applogger.Infof("MAC历史物化视图刷新任务已注册")`。

    修改 `internal/scheduler/mac_history_tasks.go`:

    1. 在 `RegisterMACHistoryTasks` 函数尾部追加 1 行:`scheduler.RegisterMACHistoryMatViewTasks(scheduler, db, matViewService)`(传入 matViewService 由 Core 注入)。

    不需要新加 init 调用 — 走 Phase 12 已有的 `RegisterMACHistoryTasks` 入口。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go build ./internal/scheduler/... 2>&1 | head -20
</automated>
  </verify>
  <done>
    - mac_history_matview_tasks.go 存在,导出 `RegisterMACHistoryMatViewTasks`
    - mac_history_tasks.go 包含 1 行 `RegisterMACHistoryMatViewTasks` 调用
    - `go build ./internal/scheduler/...` 退出码 0
  </done>
</task>

<task type="auto">
  <name>Task 3: sys_config seed + Core 注册</name>
  <files>internal/services/system/sys_config_seed.go, internal/core/core.go</files>
  <read_first>
    - internal/services/cache_config_service.go (sys_config seed 模式参考)
    - internal/core/core.go (查找 scheduler.RegisterMACHistoryTasks 调用位置)
  </read_first>
  <action>
    **新建** `internal/services/system/sys_config_seed.go`:

    1. 函数 `SeedMACPerfConfigs(db *gorm.DB) error`,幂等 upsert 3 个 sys_config 键:
       - `network.mac.perf.mat_view_refresh_cron` = `0 */5 * * * *` (String 类型,ConfigValue)
       - `network.mac.perf.cache_ttl_seconds` = `300` (String 类型)
       - `network.mac.perf.heatmap_top_n` = `100` (String 类型)
    2. 用 GORM 链式 API 与 `models.Config{}` 结构体做 upsert, 模式:
       - `db.Where(&models.Config{ConfigKey: key}).Attrs(&models.Config{ConfigKey: key, ConfigValue: value, ConfigType: "yaml"}).FirstOrCreate(&models.Config{})`
       - 通过 `ConfigKey` 字段 (GORM 映射到 `config_key` 列) 查询, 已存在则跳过, 不存在则插入。
       - 不要用裸 SQL 列名字符串。

    修改 `internal/core/core.go`:

    1. 在 `scheduler.RegisterMACHistoryTasks(...)` 调用位置(查找现有 mac_history_tasks 调用)之后,追加:
       - `services.SeedMACPerfConfigs(core.GetDB())`(启动时确保 3 个配置存在)
       - `matViewSvc := services.NewMACHistoryMatViewService(core.GetDB())`
       - `scheduler.RegisterMACHistoryMatViewTasks(scheduler, core.GetDB(), matViewSvc)`
    2. 注意:Core 启动时 mat_view 可能尚未建(在 15-01 migration 中建),但 cron 任务首次执行在启动 5 分钟后,届时 MV 已存在。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go build ./... 2>&1 | head -20
  </verify>
  <done>
    - sys_config_seed.go 存在,导出 `SeedMACPerfConfigs`
    - core.go 包含 3 行新调用
    - `go build ./...` 退出码 0
  </done>
</task>

</tasks>

<verification>
- `go build ./...` 退出码 0
- mac_history_matview_service.go + mac_history_matview_tasks.go 存在
- sys_config 3 个键存在(mat_view_refresh_cron / cache_ttl_seconds / heatmap_top_n)
- 任务 `mac_history_matview_refresh` 注册,Cron `0 */5 * * * *`
</verification>

<success_criteria>
- 5 分钟 cron 任务注册成功
- 4 个物化视图 CONCURRENTLY 刷新,部分失败容错
- `go build ./...` 退出码 0
</success_criteria>

<output>
Create `.planning/phases/15-performance-optimization/15-02-SUMMARY.md` when done
</output>
