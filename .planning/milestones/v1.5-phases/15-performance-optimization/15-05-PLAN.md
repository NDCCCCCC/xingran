---
phase: 15-performance-optimization
plan: 05
type: execute
wave: 4
depends_on:
  - 15-01
  - 15-02
  - 15-03
  - 15-04a
  - 15-04b
files_modified:
  - internal/services/mac_history_perf_verify_test.go
  - .planning/phases/15-performance-optimization/15-VERIFICATION.md
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04
must_haves:
  truths:
    - All 4 materialized views can be refreshed manually (REFRESH MATERIALIZED VIEW CONCURRENTLY returns success)
    - EXPLAIN ANALYZE for 3-5 typical queries shows index / MV usage (Index Scan, Bitmap Index Scan on mv_mac_*)
    - Frontend heatmap page returns 200 with non-empty data when run end-to-end
    - `go build ./...` exits 0
    - Targeted unit tests for cache decorator and matview refresh all pass
    - NO independent perf benchmark script (per D-15 deferred)
  artifacts:
    - path: "internal/services/mac_history_perf_verify_test.go"
      provides: "EXPLAIN ANALYZE assertions for 3-5 typical queries"
      contains: "EXPLAIN ANALYZE"
    - path: ".planning/phases/15-performance-optimization/15-VERIFICATION.md"
      provides: "Phase verification report"
      contains: "## 验证结果"
  key_links:
    - from: "internal/services/mac_history_perf_verify_test.go"
      to: "sys_device_mac_history / mv_mac_*"
      via: "EXPLAIN ANALYZE GORM raw query"
      pattern: "EXPLAIN\\s+ANALYZE"
    - from: ".planning/phases/15-performance-optimization/15-VERIFICATION.md"
      to: "phase plans 15-01..15-04"
      via: "Verification checklist"
      pattern: "15-0[1-4]"
---

<objective>
端到端联调与抽样验证:手动刷新 4 个物化视图无报错;EXPLAIN ANALYZE 抽样 3-5 个典型查询断言索引/MV 命中;跑通前端 heatmap 页确保 200 + 非空数据;目标单元测试全通过;汇编 phase 验证报告。

Purpose: 把"计划承诺的优化效果"用 EXPLAIN ANALYZE 抽样证据化(D-15 锁定非 benchmark);暴露潜在索引失效 / MV 未命中问题;生成 phase 验证报告。

Output: 1 个测试文件 + 1 个验证报告。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-performance-optimization/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 编写 EXPLAIN ANALYZE 抽样测试</name>
  <files>internal/services/mac_history_perf_verify_test.go</files>
  <read_first>
    - internal/services/mac_history_query_service.go (4 个查询 SQL 真实位置)
    - internal/core/db/database.go (isPostgreSQL 函数)
  </read_first>
  <action>
    新建 `internal/services/mac_history_perf_verify_test.go`:

    1. 包名 `services`。
    2. 测试入口 `TestMACPerfVerify(t *testing.T)`:
       - 顶部 `t.Skip("Phase 15 抽样验证,需在 IMPLEMENT 阶段手动跑 -perf_verify")`(默认跳过)。
    3. 5 个子测试(`t.Run`):
       - `TestPortHistory_90d_IndexScan`:EXPLAIN ANALYZE 对应 QueryPortHistory,断言含 `idx_mac_history_device_mac_first_seen`。
       - `TestDeviceHistory_90d_IndexScan`:EXPLAIN ANALYZE 对应 QueryDeviceHistory,断言同上。
       - `TestConnectionStats_7d_IndexScan`:EXPLAIN ANALYZE 对应 QueryConnectionStats,7 天范围,断言含 `idx_mac_history_device_mac_first_seen` 或 `mv_mac_device_summary`。
       - `TestHeatmap_MV04_BitmapScan`:EXPLAIN ANALYZE heatmap SQL,断言含 `mv_mac_port_daily_count` + `Bitmap Index Scan` / `Index Scan`。
       - `TestMatViewRefresh_All4_Success`:依次 4 个 REFRESH CONCURRENTLY,断言 4 个返回 nil。
    4. 工具 `assertPlanContains(t, plan, substrings []string)`,任一 substring 不命中 `t.Errorf`。
    5. `db.Raw("EXPLAIN ANALYZE ...").Scan(&planString).Error` 拿执行计划。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go build ./internal/services/... 2>&1 | head -20</automated>
  </verify>
  <done>
    - mac_history_perf_verify_test.go 存在,含 5 个子测试
    - 5 个子测试函数名:`TestPortHistory_90d_IndexScan` / `TestDeviceHistory_90d_IndexScan` / `TestConnectionStats_7d_IndexScan` / `TestHeatmap_MV04_BitmapScan` / `TestMatViewRefresh_All4_Success`
    - `go build ./internal/services/...` 退出码 0
  </done>
</task>

<task type="auto">
  <name>Task 2: 汇编 phase 验证报告</name>
  <files>.planning/phases/15-performance-optimization/15-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/15-performance-optimization/15-CONTEXT.md (Phase 边界)
    - .planning/REQUIREMENTS.md (PERF-01..04 需求与目标)
  </read_first>
  <action>
    新建 `.planning/phases/15-performance-optimization/15-VERIFICATION.md`,模板:

    ```markdown
    # Phase 15: 性能优化 - 验证报告

    **Gathered:** 2026-06-15
    **Status:** Ready for review

    ## 验证结果

    ### 计划执行清单
    - [ ] 15-01: 复合索引 + 4 个物化视图迁移 — `migrations/151` + `migrations/152` + database.go 注册
    - [ ] 15-02: 5 分钟 cron 任务注册 — `mac_history_matview_refresh`
    - [ ] 15-03: 3 个查询方法 cache-aside 装饰
    - [ ] 15-04: heatmap 后端 API + 前端独立页
    - [ ] 15-05: EXPLAIN ANALYZE 抽样验证

    ### EXPLAIN ANALYZE 抽样
    (在 IMPLEMENT 阶段填入实际 EXPLAIN ANALYZE 输出,断言索引/MV 命中)

    ### 端到端测试
    - [ ] 后端 `POST /network/history/heatmap` 返回 200 + 非空数据
    - [ ] 前端 `/network/mac/heatmap` 渲染 ECharts heatmap
    - [ ] 移动端降级为 Top-20 端口列表

    ### 单元测试
    - [ ] cache decorator hit / miss / degrade 测试通过
    - [ ] matview refresh 部分失败容错测试通过

    ### 编译验证
    - [ ] `go build ./...` 退出码 0

    ## 已知问题
    (若有)

    ## 结论
    (Phase 15 是否达到目标)
    ```

    实际验证由 IMPLEMENT 阶段在执行时填入具体 EXPLAIN ANALYZE 输出与测试结果。
  </action>
  <verify>
    <automated>test -f "D:/CODE/ClaudeCode/xingran-go-backend/.planning/phases/15-performance-optimization/15-VERIFICATION.md" && echo "VERIFICATION.md exists" && go build ./... 2>&1 | head -10
  </verify>
  <done>
    - 15-VERIFICATION.md 文件存在
    - 文件包含 `## 验证结果` 标题
    - 文件包含 5 个子计划执行清单
    - `go build ./...` 退出码 0
  </done>
</task>

</tasks>

<verification>
- `go build ./...` 退出码 0
- 5 个子测试函数存在 (EXPLAIN ANALYZE + 刷新)
- 15-VERIFICATION.md 存在并包含验证清单
- 不存在独立 perf benchmark 脚本(符合 D-15 deferred)
</verification>

<success_criteria>
- 物化视图手动刷新 4 个全部成功
- EXPLAIN ANALYZE 抽样断言索引/MV 命中
- 前端 heatmap 端到端 200 + 非空数据
- 目标单元测试全通过
- `go build ./...` 退出码 0
- 无独立 perf benchmark 脚本
</success_criteria>

<output>
Create `.planning/phases/15-performance-optimization/15-05-SUMMARY.md` when done
</output>
