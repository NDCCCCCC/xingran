---
phase: 15-performance-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/core/db/migrations/migration_151_mac_perf_indexes.go
  - internal/core/db/migrations/migration_152_mac_matview.go
  - internal/core/db/database.go
autonomous: true
requirements:
  - PERF-01
  - PERF-02
must_haves:
  truths:
    - Composite B-tree index `(device_id, mac_address, first_seen)` exists on `sys_device_mac_history`
    - Four materialized views exist with UNIQUE indexes (CONCURRENTLY-refreshable)
    - All 4 MVs registered in `mv_mac_port_latest` / `mv_mac_device_summary` / `mv_mac_long_occupancy_top` / `mv_mac_port_daily_count`
    - Both migrations registered in `database.go` AutoMigrate flow
    - `go build ./...` exits 0
  artifacts:
    - path: "internal/core/db/migrations/migration_151_mac_perf_indexes.go"
      provides: "Composite B-tree index creation"
      contains: "idx_mac_history_device_mac_first_seen"
    - path: "internal/core/db/migrations/migration_152_mac_matview.go"
      provides: "Four materialized views with UNIQUE indexes"
      contains: "CREATE MATERIALIZED VIEW mv_mac_port_latest"
    - path: "internal/core/db/database.go"
      provides: "Migration registration"
      contains: "Migrate151MACPerfIndexes"
  key_links:
    - from: "internal/core/db/migrations/migration_151_mac_perf_indexes.go"
      to: "sys_device_mac_history"
      via: "CREATE INDEX on partitioned table"
      pattern: "CREATE INDEX.*sys_device_mac_history"
    - from: "internal/core/db/migrations/migration_152_mac_matview.go"
      to: "sys_device_mac_history"
      via: "CREATE MATERIALIZED VIEW AS SELECT"
      pattern: "CREATE MATERIALIZED VIEW.*AS SELECT"
---

<objective>
建立 MAC 历史查询性能基础:复合 B-tree 索引(对齐 Phase 13 主查询路径)+ 4 个物化视图(端口最新 MAC / 设备 MAC 汇总 / 长期占用 Top-50 / 每日端口使用计次),每个物化视图建 UNIQUE 索引以支持后续 CONCURRENTLY 刷新。

Purpose: 落地 PERF-01 (复合索引) + PERF-02 DDL (物化视图) — 索引优化 4 个查询 API 中点查场景;物化视图为 15-02 刷新任务、15-04 热力图提供预聚合数据源,跨月查询走分区裁剪。

Output: 2 个新 migration 文件 + database.go 追加 2 行调用。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-performance-optimization/15-CONTEXT.md
@.planning/phases/12-data-model-integration/12-CONTEXT.md
@.planning/phases/13-query-layer-trajectory/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建复合 B-tree 索引迁移</name>
  <files>internal/core/db/migrations/migration_151_mac_perf_indexes.go</files>
  <read_first>
    - internal/core/db/migrations/migration_mac_history.go (现有 BRIN 索引命名与表结构)
    - internal/core/db/migrations/migration_148_create_ops_asset_table.go (migration Go 模板,函数名 `Migrate151MACPerfIndexes`)
    - internal/core/db/database.go (迁移调用位置在 line 349 之后)
  </read_first>
  <action>
    新建 `internal/core/db/migrations/migration_151_mac_perf_indexes.go`:

    1. 包名 `migrations`,与其他 migration 一致。
    2. 导出 `Migrate151MACPerfIndexes(db *gorm.DB) error`,签名/返回错误风格对齐 `Migrate148CreateOpsAssetTable`。
    3. 函数体:
       - 先 `isPostgreSQL(db)`,非 PG 走 `applogger.Infof` 跳过返回 `nil`(沿用 migration_mac_history.go 的 SQLite 跳过模式)。
       - PG 分支:执行 `CREATE INDEX IF NOT EXISTS idx_mac_history_device_mac_first_seen ON sys_device_mac_history (device_id, mac_address, first_seen);`。
       - 用 `db.Exec(sql).Error`,失败返回 `fmt.Errorf("创建复合索引失败: %w", err)`。
       - 成功 `applogger.Infof("[迁移] 复合索引 idx_mac_history_device_mac_first_seen 已创建")`。
    4. 不动 BRIN(沿用 Phase 12 D-05),不加额外 B-tree。
    5. 纯 SQL DDL,不需要 GORM Model 配合。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go build ./internal/core/db/migrations/... 2>&1</automated>
  </verify>
  <done>
    - migration_151 文件存在,导出 `Migrate151MACPerfIndexes`
    - `go build ./internal/core/db/migrations/...` 退出码 0
    - 包含字符串 `CREATE INDEX IF NOT EXISTS idx_mac_history_device_mac_first_seen ON sys_device_mac_history (device_id, mac_address, first_seen)`
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 4 个物化视图迁移</name>
  <files>internal/core/db/migrations/migration_152_mac_matview.go</files>
  <read_first>
    - internal/core/db/migrations/migration_mac_history.go (表结构与字段)
    - internal/core/db/database.go (isPostgreSQL 工具函数)
    - .planning/phases/15-performance-optimization/15-CONTEXT.md (D-09/D-10 锁定)
  </read_first>
  <action>
    新建 `internal/core/db/migrations/migration_152_mac_matview.go`:

    1. 包名 `migrations`,导出 `Migrate152MACMatView(db *gorm.DB) error`。
    2. 4 个物化视图,均用 `db.Exec`:
       - MV-01 `mv_mac_port_latest`: 取每 (device_id, interface_name) 最新 MAC 状态
       - MV-02 `mv_mac_device_summary`: 按 device_id 聚合 mac_count / active_count / last_update
       - MV-03 `mv_mac_long_occupancy_top`: 停留 > 24h 的 MAC 按 total_duration 降序 LIMIT 50
       - MV-04 `mv_mac_port_daily_count`: 按 (device_id, interface_name, DATE(first_seen)) 聚合 change_count
    3. 每个视图必须建 UNIQUE 索引(CONCURRENTLY 前置条件):
       - idx_mv_port_latest_pk ON (device_id, interface_name)
       - idx_mv_device_summary_pk ON (device_id)
       - idx_mv_long_occupancy_pk ON (mac_address, last_port)
       - idx_mv_port_daily_pk ON (device_id, interface_name, date)
    4. 非 PG 数据库跳过整函数返回 nil。
    5. 失败返回 `fmt.Errorf("创建物化视图 %s 失败: %w", viewName, err)`,逐个视图错误用 `viewName` 变量区分。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go build ./internal/core/db/migrations/... 2>&1</automated>
  </verify>
  <done>
    - migration_152 文件存在,导出 `Migrate152MACMatView`
    - `go build ./internal/core/db/migrations/...` 退出码 0
    - 包含 4 个 `CREATE MATERIALIZED VIEW IF NOT EXISTS mv_mac_` 字符串
    - 包含 4 个 `CREATE UNIQUE INDEX IF NOT EXISTS idx_mv_` 字符串
  </done>
</task>

<task type="auto">
  <name>Task 3: 注册到 AutoMigrate 流程</name>
  <files>internal/core/db/database.go</files>
  <read_first>
    - internal/core/db/database.go (line 349 `Migrate150AddWorkstationDeviceIPAddress` 调用点)
  </read_first>
  <action>
    修改 `internal/core/db/database.go`:

    1. 在 `Migrate150AddWorkstationDeviceIPAddress` 块(约 line 349)之后,追加 2 个相同风格调用:
       - `if err := migrations.Migrate151MACPerfIndexes(d.DB); err != nil { return fmt.Errorf("Migrate151 失败: %w", err) }`
       - `if err := migrations.Migrate152MACMatView(d.DB); err != nil { return fmt.Errorf("Migrate152 失败: %w", err) }`
    2. 错误处理沿用 Migrate150 之后 `if err != nil { return fmt.Errorf(...) }` 写法。
    3. 在 2 行新调用前加注释 `// Phase 15: 性能优化 (PERF-01 索引 + PERF-02 物化视图 DDL)`。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/xingran-go-backend && go build ./... 2>&1 | head -20</automated>
  </verify>
  <done>
    - database.go 包含 `Migrate151MACPerfIndexes` 调用
    - 2 行新调用前含注释 `// Phase 15: 性能优化`
    - `go build ./...` 退出码 0
  </done>
</task>

</tasks>

<verification>
- `go build ./...` 退出码 0
- `internal/core/db/migrations/migration_151_mac_perf_indexes.go` 存在,导出 `Migrate151MACPerfIndexes`
- `internal/core/db/migrations/migration_152_mac_matview.go` 存在,导出 `Migrate152MACMatView`
- `internal/core/db/database.go` 包含 2 个新迁移调用
- 文件包含预期 SQL 字符串(复合索引 + 4 个物化视图 + 4 个 UNIQUE 索引)
</verification>

<success_criteria>
- 复合 B-tree 索引 `(device_id, mac_address, first_seen)` 在 PG 中已建
- 4 个物化视图 + 各自 UNIQUE 索引在 PG 中已建
- `go build ./...` 退出码 0
</success_criteria>

<output>
Create `.planning/phases/15-performance-optimization/15-01-SUMMARY.md` when done
</output>
