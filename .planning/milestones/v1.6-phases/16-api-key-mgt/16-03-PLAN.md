---
phase: 16-api-key-mgt
plan: 03
type: execute
wave: 3
depends_on: [16-02]
files_modified:
  - internal/api/v1/system/apikey_handler.go
  - internal/api/v1/system/apikey_router.go
  - internal/api/router.go
autonomous: true
requirements: ["INDEPENDENT"]
must_haves:
  truths:
    - APIKeyHandler 处理器实现完整，包含所有端点
    - APIKeyRouter 路由配置正确，注册到主路由
    - 请求参数绑定和验证正确
    - 响应格式符合项目规范（response.Success/Error）
    - 密钥脱敏显示（列表仅显示前12位）
    - 创建密钥时完整显示一次密钥
  artifacts:
    - path: internal/api/v1/system/apikey_handler.go
      provides: APIKey 处理器
      min_lines: 200
      contains:
        - type APIKeyHandler struct
        - func NewAPIKeyHandler
        - func.*Create
        - func.*List
        - func.*GetByID
        - func.*Update
        - func.*Delete
        - func.*ToggleStatus
    - path: internal/api/v1/system/apikey_router.go
      provides: APIKey 路由
      min_lines: 50
      contains:
        - func SetupAPIKeyRouter
        - r.POST.*list
        - r.POST.*:id
  key_links:
    - from: internal/api/v1/system/apikey_handler.go
      to: internal/services/system/apikey_service.go
      via: 依赖注入
      pattern: apiKeyService.*APIKeyService
    - from: internal/api/router.go
      to: internal/api/v1/system/apikey_router.go
      via: 路由注册
      pattern: SetupAPIKeyRouter.*apikeys
---

<objective>
创建 API 密钥管理的 HTTP 处理器和路由

目的：实现 API 密钥管理的 HTTP 接口，支持 CRUD 操作和使用日志查询
输出：APIKeyHandler 处理器，APIKeyRouter 路由配置，注册到主路由

**说明：** 这是独立功能模块，不依赖 REQUIREMENTS.md 中的具体需求 ID。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-api-key-mgt/16-CONTEXT.md
@.planning/phases/16-api-key-mgt/16-PATTERNS.md
@internal/api/v1/system/user_handler.go
@internal/api/v1/system/user_router.go
@internal/api/router.go
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 创建 APIKeyHandler 处理器</name>
  <files>internal/api/v1/system/apikey_handler.go</files>
  <read_first>
    - internal/api/v1/system/user_handler.go
    - internal/services/system/apikey_service.go
  </read_first>
  <action>
创建 internal/api/v1/system/apikey_handler.go 文件，定义处理器：

1. 定义 APIKeyHandler 结构体：
   - apiKeyService systemServices.APIKeyService

2. 实现 NewAPIKeyHandler 构造函数：
   - 参数：apiKeyService systemServices.APIKeyService
   - 返回：*APIKeyHandler

3. 实现 Create 处理器：
   - 绑定 CreateAPIKeyRequest
   - 从 context 获取 userID（c.GetString("user_id")）
   - 调用 service.CreateAPIKey
   - 返回完整密钥（仅此一次）
   - 成功响应：{"code":0, "data":{"key":"rec_..."}}

4. 实现 List 处理器：
   - 绑定 ListAPIKeysParams（默认值处理）
   - 应用分页限制
   - 调用 service.ListAPIKeys
   - 脱敏处理：key 字段仅保留前12位
   - 返回分页数据

5. 实现 GetByID 处理器：
   - 提取 ID 参数
   - 调用 service.GetAPIKey
   - 脱敏处理：key 字段仅保留前12位
   - 返回密钥详情

6. 实现 Update 处理器：
   - 提取 ID 参数
   - 绑定 UpdateAPIKeyRequest
   - 调用 service.UpdateAPIKey
   - 返回成功消息

7. 实现 Delete 处理器：
   - 提取 ID 参数
   - 调用 service.DeleteAPIKey
   - 返回成功消息

8. 实现 ToggleStatus 处理器：
   - 提取 ID 参数
   - 调用 service.ToggleAPIKeyStatus
   - 返回成功消息

9. 实现 ListUsageLogs 处理器：
   - 提取 keyID 参数
   - 绑定 ListUsageLogsParams
   - 调用 service.ListUsageLogs
   - 返回分页数据

10. 实现 GetUsageSummary 处理器：
    - 提取 keyID 参数
    - 调用 service.GetUsageLogSummary
    - 返回统计数据

使用 response.Success() 和 response.Error() 包装响应
使用 c.Request.Context() 传递 context
  </action>
  <verify>
    <automated>grep -E "func.*Create|func.*List|func.*GetByID|func.*Update|func.*Delete|func.*ToggleStatus|func.*ListUsageLogs|func.*GetUsageSummary" internal/api/v1/system/apikey_handler.go</automated>
  </verify>
  <done>
    - 所有处理器方法实现正确
    - 请求参数绑定和验证完整
    - 响应格式符合规范
    - 密钥脱敏处理正确
    - Context 传递正确
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建 APIKeyRouter 路由配置</name>
  <files>internal/api/v1/system/apikey_router.go</files>
  <read_first>
    - internal/api/v1/system/user_router.go
    - internal/core/core.go
  </read_first>
  <action>
创建 internal/api/v1/system/apikey_router.go 文件，定义路由配置：

1. 定义 SetupAPIKeyRouter 函数：
   - 参数：r *gin.RouterGroup, core *core.Core
   - 返回值：无

2. 创建服务实例：
   - apiKeyService := system.NewAPIKeyService(core.GetDB())

3. 创建处理器实例：
   - apikeyHandler := system.NewAPIKeyHandler(apiKeyService)

4. 注册路由：
   - POST /list: apikeyHandler.List
   - POST /: apikeyHandler.Create
   - POST /:id: apikeyHandler.GetByID
   - POST /:id/update: apikeyHandler.Update
   - POST /:id/delete: apikeyHandler.Delete
   - POST /:id/toggle: apikeyHandler.ToggleStatus
   - POST /:id/logs: apikeyHandler.ListUsageLogs
   - GET /:id/summary: apikeyHandler.GetUsageSummary

5. 添加 Swagger 注释：
   - @Summary, @Description, @Tags
   - @Accept, @Produce
   - @Param, @Success, @Failure, @Router

参考 user_router.go 的模式
  </action>
  <verify>
    <automated>grep -E "func SetupAPIKeyRouter|POST.*list|POST.*:id/update|POST.*:id/delete" internal/api/v1/system/apikey_router.go</automated>
  </verify>
  <done>
    - 路由配置正确
    - 所有端点注册成功
    - Swagger 注释完整
    - 服务和处理器依赖注入正确
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 在主路由中注册 APIKey 路由</name>
  <files>internal/api/router.go</files>
  <read_first>
    - internal/api/router.go
    - internal/api/v1/system/apikey_router.go
  </read_first>
  <action>
在 internal/api/router.go 中注册 API 密钥管理路由：

1. 找到 systemV1 路由组定义位置
2. 添加 API 密钥管理路由组：
   ```go
   // API密钥管理
   apikeys := authorized.Group("/apikeys")
   apikeys.Use(middleware.RequirePermissions([]string{
       "system:apikey:list",
       "system:apikey:add",
       "system:apikey:edit",
       "system:apikey:delete",
   }, core))
   {
       systemV1.SetupAPIKeyRouter(apikeys, core)
   }
   ```

3. 确保在 authorized 路由组下（需要认证）
4. 添加适当的权限检查中间件
5. 保持与现有路由的一致性

参考用户管理路由的注册方式
  </action>
  <verify>
    <automated>grep -E "apikeys.*Group|SetupAPIKeyRouter" internal/api/router.go</automated>
  </verify>
  <done>
    - API 密钥路由注册成功
    - 路由路径正确（/api/v1/apikeys）
    - 权限中间件配置正确
    - 与现有路由风格一致
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP 请求 | 客户端发送的 HTTP 请求需要验证和清理 |
| 响应数据 | 返回给客户端的数据需要脱敏处理 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-12 | Information Disclosure | 密钥返回 | mitigate | 列表和详情查询仅返回前12位，仅创建时返回完整密钥 |
| T-16-13 | Tampering | 参数验证 | mitigate | 使用 binding 标签验证输入参数，防止注入攻击 |
| T-16-14 | Elevation of Privilege | 权限检查 | mitigate | 使用 RequirePermissions 中间件检查用户权限 |
| T-16-15 | Denial of Service | 资源限制 | mitigate | 分页限制最大每页100条，防止大量数据返回 |
</threat_model>

<verification>
1. 检查处理器文件是否存在且语法正确
2. 检查路由配置是否正确
3. 运行 go build ./... 验证编译通过
4. 验证主路由注册成功
5. 测试端点是否可访问
</verification>

<success_criteria>
1. APIKeyHandler 所有方法实现正确
2. APIKeyRouter 路由配置完整
3. 主路由注册成功
4. 密钥脱敏处理正确
5. 权限中间件配置正确
6. Swagger 文档注释完整
</success_criteria>

<output>
执行完成后，创建 .planning/phases/16-api-key-mgt/16-03-SUMMARY.md
</output>
