---
phase: 41-debug-session-gap-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/debug/request-encryption-token-refresh-400.md
  - .planning/debug/login-wrong-pwd-no-prompt.md
  - .planning/debug/file-upload-401-refresh.md
  - .planning/debug/ad-admin-lockout-recurrence.md
  - .planning/debug/ad-update-attr-no-such-object.md
  - .planning/debug/ad-user-page-checkbox-not-click.md
  - .planning/debug/vdi-sync-invalid-uri.md
  - .planning/debug/vdi-server-test-500-input-warnings.md
  - .planning/debug/user-deptid-uuid-cast-recurring.md
  - .planning/debug/interface-to-any-conversion-hint.md
  - internal/services/addomain/
  - internal/services/vdi/
  - internal/api/v1/
  - internal/services/system/
autonomous: false
requirements: [TECH-06, TECH-05]
tags: [debug-cleanup, cohort-b, backend, ad-domain, vdi, auth, mixed-fix-wontfix]

must_haves:
  truths:
    - "Cohort B 后端/认证集群 10 个 session 逐个处理:能修则修+build 通过翻 resolved;陈年/重复/外部依赖/跨 phase 覆盖者写 won't_fix_reason 翻 resolved(+ skip_audit 移出 audit)"
    - "won't-fix 决策每个都有书面理由(D-02),无理由不关单"
    - "实修的 session 经 go build ./... 验证通过(D-03/D-06)"
    - "AD 类 session 决策参考项目 memory(ad-admin-lockout 是观察期目标 / ad-update-no-such-object 根因是 ad_dn 残留)"
    - "validator 仍 100% pass"
  artifacts:
    - path: ".planning/debug/ad-admin-lockout-recurrence.md"
      provides: "status: resolved(wontfix 观察期未复发 或 已被 Phase 38 账号池覆盖)+ Phase 41 Closure"
      contains: "Phase 41 Closure"
    - path: ".planning/debug/user-deptid-uuid-cast-recurring.md"
      provides: "status: resolved(实修 或 wontfix 根因已诊断)+ Phase 41 Closure"
      contains: "Phase 41 Closure"
    - path: ".planning/debug/request-encryption-token-refresh-400.md"
      provides: "status: resolved(实修 或 wontfix)+ Phase 41 Closure"
      contains: "Phase 41 Closure"
  key_links:
    - from: "实修型 session .md Resolution"
      to: "go build ./... 退出码 0"
      via: "修复后跑 go build ./..."
      pattern: "verification: go build 退出 0"
    - from: "AD 类 wontfix .md"
      to: "audit-open 计数"
      via: "skip_audit: true 顶层字段让 audit-open 不计"
      pattern: "^skip_audit: true"
---

<objective>
**Cohort B 后端/认证集群 10 个 session 按 D-02 混合策略闭环**,分两组:
- **认证/Token/登录组(3)**:request-encryption-token-refresh-400 / login-wrong-pwd-no-prompt / file-upload-401-refresh
- **AD/VDI/UUID/类型组(7)**:ad-admin-lockout-recurrence / ad-update-attr-no-such-object / ad-user-page-checkbox-not-click / vdi-sync-invalid-uri / vdi-server-test-500-input-warnings / user-deptid-uuid-cast-recurring / interface-to-any-conversion-hint

Purpose: 这 10 个是在途调查,D-02 混合:能修则修(读根因→实修→`go build ./...` 通过→翻 resolved),陈年/重复/外部依赖/已被其他 phase 覆盖者标 won't-fix。

**关键项目 memory 参考**(影响 won't-fix 判定):
- ad-admin-lockout-recurrence:项目 memory 多条笔记(ad-ldap-error49 / ad-modify-fail-double-counts-breaker / ad-admin-bind-sm4-decrypt)显示 Phase 38 已落 AccountPool/FailoverClient/MarkFailure 区分 operation:/dial:,本 session 是"观察复发"性质,可能已被 Phase 38 覆盖
- ad-update-attr-no-such-object:项目 memory(ad-update-no-such-object-vs-lockout)根因是 sys_user.ad_dn 残留 AD 端已删用户 DN,需 DNExists 预检
- user-deptid-uuid-cast-recurring:.md 根因已深度诊断(sys_user.dept_id 是 uuid 列,CASE WHEN guard 多余),修法已知
- interface-to-any-conversion-hint:.md 已诊断(1261 处合法 interface{},非 bug,是风格)

Output: 10 个原子 commit(`fix(41): <slug> — <主题>` 实修 / `docs(41): <slug> — won't-fix <理由>`),audit-open debug_sessions 继续下降。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-debug-session-gap-closure/41-CONTEXT.md
@.planning/phases/40-tech-debt-cleanup/40-01-SUMMARY.md
@.planning/debug/resolved/asset-export-404-error.md
@scripts/validate_debug_frontmatter.sh
@scripts/fix_debug_frontmatter.py
@CLAUDE.md

<interfaces>
<!-- D-02 决策树(每个 session 必走其一): -->
<!--   A) 实修:读 .md 根因 → 实修代码 → go build ./... 退出 0 → 翻 resolved + Phase 41 Closure(real-fix) -->
<!--   B) won't-fix:写 won't_fix_reason → 翻 resolved(+ skip_audit: true 若移出 audit)+ Phase 41 Closure(wontfix) -->

<!-- Phase 41 Closure 证据块格式(D-04): -->
<!--   实修型: -->
<!--     ## Phase 41 Closure (2026-06-26) -->
<!--     fix: <一句话主题> -->
<!--     verification: go build ./... 退出 0 -->
<!--     files_changed: <修改的代码文件> -->
<!--     action: real-fix (D-02) -->
<!--   won't-fix型: -->
<!--     ## Phase 41 Closure (2026-06-26) -->
<!--     won't_fix_reason: <具体理由:已被 Phase X 覆盖 / 观察期未复发 / 外部依赖 / 风格非 bug> -->
<!--     action: wontfix (D-02) -->
<!--     (顶层加 skip_audit: true 若应移出 audit) -->

<!-- commit 格式(D-06): -->
<!--   fix(41): <slug> — <主题>          (实修型) -->
<!--   docs(41): <slug> — won't-fix <理由>  (won't-fix型) -->

<!-- 项目 memory 关键事实(影响 AD/UUID 决策): -->
<!--   ad-admin-lockout: Phase 38 已落 AccountPool + FailoverClient.MarkFailure 区分前缀,熔断可恢复 -->
<!--   ad-update-no-such-object: 根因 sys_user.ad_dn 残留,修法是 syncUserAttributes 加 DNExists 预检 -->
<!--   user-deptid-uuid-cast: 根因 sys_user.dept_id 是 uuid 列,CASE WHEN guard 多余,简化为 sys_dept.id = sys_user.dept_id -->
<!--   interface-to-any: 1261 处合法 interface{},staticcheck SA1029 命中 0,非 bug 是风格 -->

<!-- operlog 约定(CLAUDE.md):若实修触及 handler/business-write 代码,必须加 operlog.Record(...) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 认证/Token/登录 3 个 session(D-02 混合)</name>
  <files>
    .planning/debug/request-encryption-token-refresh-400.md
    .planning/debug/login-wrong-pwd-no-prompt.md
    .planning/debug/file-upload-401-refresh.md
    xingran-react-frontend/src/lib/api.ts
    xingran-react-frontend/src/utils/token/TokenManager.ts
    xingran-react-frontend/src/utils/errorHandler.ts
  </files>
  <read_first>
    - .planning/debug/request-encryption-token-refresh-400.md(status: investigating,关闭请求加密后 /system/auth/refresh 返回 400)
    - .planning/debug/login-wrong-pwd-no-prompt.md(status: investigating,输错密码无提示)
    - .planning/debug/file-upload-401-refresh.md(status: checkpoint_reached,文件上传成功后刷新列表 401,FileUpload 组件用原生 XMLHttpRequest 绕过 axios 拦截器)
    - .planning/debug/page-refresh-token-refresh-loop-failure.md(Plan 41-01 处理的相邻 token 循环问题,参考其修法)
    - xingran-react-frontend/src/lib/api.ts(401 拦截器逻辑,项目 memory:api-401-interceptor-needs-auth-endpoint-exclusion)
    - xingran-react-frontend/src/utils/token/TokenManager.ts
    - xingran-react-frontend/src/utils/errorHandler.ts
    - xingran-react-frontend/src/components/FileUpload.tsx 或类似(确认是否用原生 XHR)
    - .planning/debug/resolved/asset-export-404-error.md
    - scripts/validate_debug_frontmatter.sh
  </read_first>
  <action>
对下列 3 个 session 逐个走 D-02 决策树。**每个 session 一个独立 commit**(D-06)。

**通用流程:** Read .md 根因 → 判定 A(实修)或 B(won't-fix)→ 执行 → Phase 41 Closure 块 → 翻 resolved → 一个 commit。若实修触代码,commit 后跑 `go build ./...` 或 `cd xingran-react-frontend && npm run build` 退出 0。

**Cohort A 升级 pickup 条款(W3 防 orphan):** 本 task 开始时先跑 `gsd-sdk query audit-open | jq '.items.debug_sessions[].slug'`,若 Plan 41-01 Task 1/2 登记为 `action: escalate-to-D-02` 的 12 个 Cohort A slug(go-textfsm-dollar-anchor-bug / quick-create-fields-not-saving / vdi-cpu-display-issue / vdi-menu-routing-redirect / vm-list-page-blank-404 / infopoint-fields-not-saving-at-all / vdi-vm-operations-fail / vendor-commons-createcontext / captcha-custom-background-not-found / page-refresh-token-refresh-loop-failure / ops-perms-alignment / operlog-loginlog-nickname-not-showing)中任一仍未 resolved 且判定为后端 bug,在本 task D-02 实修路径下接手处理(每个一个 commit);若判定为前端 bug,路由到 Plan 41-02。

---

**request-encryption-token-refresh-400**(investigating):关闭请求加密后 /system/auth/refresh 返回 400 "请求参数错误"。
- **决策依据**:Read .md + grep `internal/api/v1/auth.go` 或 `internal/core/security/` 的 refresh 端点。**项目 memory 提示**(api-401-interceptor / slider-captcha 等):关闭加密后端可能仍按加密体解析 refresh 请求 → 400。也可能与 Plan 41-01 的 page-refresh-token-refresh-loop-failure 相邻(都是 token 刷新路径)。
- **分支 A(实修)**:若根因清晰(如 refresh 端点未在 encryption exclude_paths,或前端 TokenManager 切换加密开关时未同步)→ 实修:把 /system/auth/refresh 加入 `security.request_encryption.exclude_paths`(configs/config.yaml + config.prod.yaml)或修前端 refresh 请求构造。修完 `go build ./...` 退出 0 → 翻 resolved + Phase 41 Closure(real-fix) + commit `fix(41): request-encryption-token-refresh-400 — refresh 端点加入 encryption exclude_paths`。
- **分支 B(won't-fix)**:若根因是配置使用错误(用户手动改 sys_config 加密开关但未重启/未同步前后端配置)→ won't-fix 理由"配置开关切换需前后端同步重启,非代码 bug,属运维操作问题"。翻 resolved + Phase 41 Closure(wontfix) + commit `docs(41): request-encryption-token-refresh-400 — won't-fix 配置切换运维问题`。

**login-wrong-pwd-no-prompt**(investigating):输错密码无提示。
- **决策依据**:Read .md 根因 → grep `xingran-react-frontend/src/pages/login/` 或 `xingran-react-frontend/src/utils/errorHandler.ts` 看错误提示逻辑。可能根因:登录失败响应未走 message.error,或 SM4 加密失败导致响应未到前端。
- **分支 A(实修)**:若根因是前端未捕获错误码并 message.error → 实修:在 login handler/errorHandler 加错误码分支提示。`cd xingran-react-frontend && npm run build` 退出 0 → 翻 resolved + Phase 41 Closure(real-fix) + commit `fix(41): login-wrong-pwd-no-prompt — 补登录失败错误提示`。
- **分支 B(won't-fix)**:若根因与 request-encryption 同源(加密开关配置问题)或外部依赖 → won't-fix 具体理由。

**file-upload-401-refresh**(checkpoint_reached):文件上传成功后刷新列表 401。.md 已诊断:FileUpload 组件用原生 XMLHttpRequest 绕过 axios 拦截器的 token 自动刷新。
- **决策依据**:这是**明确的实修型**(根因清晰)。
- **实修步骤**:
  1. grep 定位 FileUpload 组件(`grep -rn "XMLHttpRequest" xingran-react-frontend/src/components/ xingran-react-frontend/src/pages/`)。
  2. 改造:用 axios 实例(走拦截器)替换原生 XHR,**或**在 XHR 上手动加 token 刷新逻辑(从 authStore 读 token,401 时触发 refresh 后重试)。
  3. `cd xingran-react-frontend && npm run build` 退出 0。
  4. 翻 resolved + Phase 41 Closure(real-fix 型) + commit `fix(41): file-upload-401-refresh — FileUpload 接入 axios 拦截器 token 刷新`。
- **分支 B(won't-fix)**:若 FileUpload 改造工作量超 context budget(原生 XHR 上传流式进度条等复杂场景)→ won't-fix 理由"FileUpload XHR 改造涉及上传进度 UX 重构,属新功能范畴,推后续 phase;当前缓解:用户手动刷新页面"。翻 resolved + Phase 41 Closure(wontfix) + commit `docs(41): file-upload-401-refresh — won't-fix XHR 改造属新功能推后续`。

**完成后** 跑 `bash scripts/validate_debug_frontmatter.sh` 确认 100% pass。**不 push**。

**禁止:** 不批量 commit。won't-fix 必须有具体理由。实修必须 build 通过才翻 resolved。
  </action>
  <verify>
    <automated>cd "D:\CODE\ClaudeCode\xingran-go-backend" && go build ./... 2>&1 | tail -5 && echo "---FRONTEND---" && cd xingran-react-frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - request-encryption-token-refresh-400.md 含 `status: resolved` AND Phase 41 Closure(real-fix 含 build 退出 0 或 wontfix 含具体理由)
    - login-wrong-pwd-no-prompt.md 含 `status: resolved` AND Phase 41 Closure(real-fix 或 wontfix 理由)
    - file-upload-401-refresh.md 含 `status: resolved` AND Phase 41 Closure(real-fix:files_changed 含 FileUpload 组件 + build 退出 0;或 wontfix 含"XHR 改造属新功能")
    - 实修型 commit:`go build ./...` 和/或 `npm run build` 退出 0
    - `bash scripts/validate_debug_frontmatter.sh` pass rate `100.0%`
    - git log 显示本 task 新增 3 个 commit
  </acceptance_criteria>
  <done>
    3 个认证/Token/登录 session 按 D-02 闭环;validator 100% pass;3 个独立 commit;不 push。
  </done>
</task>

<task type="auto">
  <name>Task 2: AD/VDI/UUID/类型 7 个 session(D-02 混合,项目 memory 驱动)</name>
  <files>
    .planning/debug/ad-admin-lockout-recurrence.md
    .planning/debug/ad-update-attr-no-such-object.md
    .planning/debug/ad-user-page-checkbox-not-click.md
    .planning/debug/vdi-sync-invalid-uri.md
    .planning/debug/vdi-server-test-500-input-warnings.md
    .planning/debug/user-deptid-uuid-cast-recurring.md
    .planning/debug/interface-to-any-conversion-hint.md
    internal/services/addomain/sync.go
    internal/services/system/user_service.go
    internal/services/vdi/
  </files>
  <read_first>
    - .planning/debug/ad-admin-lockout-recurrence.md(status: investigating,观察期目标 goal: observe_recurrence)
    - .planning/debug/ad-update-attr-no-such-object.md(status: verifying)
    - .planning/debug/ad-user-page-checkbox-not-click.md(status: verifying)
    - .planning/debug/vdi-sync-invalid-uri.md(status: investigating,/api/v1/resources_group 400 COMMON_INVALID_URI)
    - .planning/debug/vdi-server-test-500-input-warnings.md(status: investigation_in_progress)
    - .planning/debug/user-deptid-uuid-cast-recurring.md(status: diagnosed,根因深度诊断完毕:dept_id 是 uuid 列)
    - .planning/debug/interface-to-any-conversion-hint.md(status: diagnosed,1261 处合法 interface{},staticcheck 0 命中)
    - CLAUDE.md § "AD Service Account Pool (Phase 36)" + § "Module-Specific Notes / AD Domain Management"(项目 memory 支持 AD 类决策)
    - internal/services/addomain/sync.go(syncUserAttributes,DNExists 预检位置)
    - internal/services/addomain/failover_client.go(确认 MarkFailure 区分 operation:/dial: 前缀)
    - internal/services/vdi/(vdi_client_extended.go,ListResourceServers 文档化克隆 VM 限制)
    - internal/services/system/user_service.go(userJoinClause 位置,user-deptid-uuid 修法点)
    - .planning/debug/resolved/asset-export-404-error.md
    - scripts/validate_debug_frontmatter.sh
  </read_first>
  <action>
对下列 7 个 session 逐个走 D-02 决策树。**每个 session 一个独立 commit**(D-06)。项目 memory 驱动 AD/UUID 决策(避免重复历史 phase 已修的工作)。

**通用流程:** Read .md 根因 → 判定 A(实修)或 B(won't-fix)→ 执行 → Phase 41 Closure 块 → 翻 resolved → 一个 commit。实修后 `go build ./...` 退出 0。

---

**ad-admin-lockout-recurrence**(investigating,goal: observe_recurrence 观察):
- **项目 memory 关键事实**:Phase 36/38 已落 AccountPool + FailoverClient.MarkFailure 区分 operation:/dial: 前缀(ad-modify-fail-double-counts-breaker 笔记);Phase 40 40-01 已复测 FailoverClient 解密/熔断逻辑就位。
- **决策**:这是**观察期目标 session**(非修代码目标)。Read .md 确认观察期是否已过 + 是否复发。**走 won't-fix**:理由"Phase 36 AccountPool + Phase 38 FailoverClient.MarkFailure 前缀区分已落地(项目 memory ad-modify-fail-double-counts-breaker),复发观察期未报告新锁定事件,代码层无进一步可修;若再发现锁定走 Phase 38 熔断恢复机制"。翻 resolved + `skip_audit: true` + Phase 41 Closure(wontfix,理由含"已被 Phase 36/38 覆盖 + 观察期未复发") + commit `docs(41): ad-admin-lockout-recurrence — won't-fix 已被 Phase 36/38 账号池覆盖,观察期未复发`。

**ad-update-attr-no-such-object**(verifying):
- **项目 memory 关键事实**(ad-update-no-such-object-vs-lockout):根因是 sys_user.ad_dn 残留 AD 端已删用户 DN;修法是 syncUserAttributes 加 DNExists 预检,code 32 短路而非 3 次重试 admin bind。
- **分支判定**:grep `internal/services/addomain/sync.go` 的 syncUserAttributes,看是否已有 DNExists 预检。
- **分支 A(实修)**:若无 DNExists 预检 → 实修:在 syncUserAttributes 入口加 DNExists 检查,code 32 短路。`go build ./...` 退出 0 → 翻 resolved + Phase 41 Closure(real-fix) + commit `fix(41): ad-update-attr-no-such-object — syncUserAttributes 加 DNExists 预检短路 code 32`。**注意**:若实修触及 handler 写操作,加 operlog.Record(CLAUDE.md 约定)。
- **分支 B(won't-fix)**:若 DNExists 预检已在(Phase 40 或历史 phase 已修)→ won't-fix 理由"复测 syncUserAttributes 已有 DNExists 预检(项目 memory ad-update-no-such-object-vs-lockout),代码层修复已落地"。翻 resolved + Phase 41 Closure(wontfix) + commit `docs(41): ad-update-attr-no-such-object — won't-fix DNExists 预检已落地`。

**ad-user-page-checkbox-not-click**(verifying):AD 用户页面 checkbox 不可点击。
- **决策**:Read .md 根因 → grep `xingran-react-frontend/src/pages/ad-domain/` 或相关 AD 用户页 checkbox 渲染逻辑。
- **分支 A(实修)**:若根因清晰(disabled prop 错误 / 权限判断写反 / onClick 缺失)→ 实修前端组件。`npm run build` 退出 0 → 翻 resolved + Phase 41 Closure(real-fix) + commit。
- **分支 B(won't-fix)**:若根因是权限设计(故意 disabled)或跨 phase 范围 → won't-fix 理由。

**vdi-sync-invalid-uri**(investigating):VDI 同步调用 /api/v1/resources_group 400 COMMON_INVALID_URI。
- **决策**:Read .md 根因 → grep `internal/services/vdi/` 看 VDI API base URL 构造。可能根因:base URL 拼接错误(多了 /api/v1 或端口错)或 VDI 服务器版本 API 路径变更。
- **分支 A(实修)**:若根因是 URL 拼接错误 → 实修 vdi_client。`go build ./...` 退出 0 → 翻 resolved + Phase 41 Closure(real-fix) + commit `fix(41): vdi-sync-invalid-uri — 修正 VDI resources_group URL 拼接`。
- **分支 B(won't-fix)**:若根因是 VDI 服务器侧 API 版本问题(外部依赖) → won't-fix 理由"VDI 服务器 API 路径变更属外部依赖,需 VDI 侧确认正确端点,非本项目代码 bug"。翻 resolved + skip_audit: true + Phase 41 Closure(wontfix) + commit。

**vdi-server-test-500-input-warnings**(investigation_in_progress):VDI server test 500 + input warnings。
- **决策**:Read .md → grep VDI server test 端点(`internal/api/v1/` 或 `internal/services/vdi/`)。
- **分支 A(实修)**:根因清晰 → 实修 + build → 翻 resolved。
- **分支 B(won't-fix)**:若 .md 根因未明确(只是 input warnings 不影响功能)→ won't-fix 理由"input warnings 非功能性,VDI server test 500 需 VDI 环境复现,推后续"。翻 resolved + Phase 41 Closure(wontfix) + commit。

**user-deptid-uuid-cast-recurring**(diagnosed,根因深度诊断完毕):
- **关键**:.md 已诊断 sys_user.dept_id 是 uuid 列(非 varchar),CASE WHEN guard 多余,修法是简化为 `sys_dept.id = sys_user.dept_id`。
- **分支判定**:grep `internal/services/system/user_service.go` 的 userJoinClause,看是否还是冗长 CASE WHEN。
- **分支 A(实修)**:若仍含 CASE WHEN regex guard → 实修:简化为 `sys_dept.id = sys_user.dept_id`。`go build ./...` 退出 0 → 翻 resolved + Phase 41 Closure(real-fix) + commit `fix(41): user-deptid-uuid-cast-recurring — 简化 userJoinClause 移除冗余 CASE WHEN`。
- **分支 B(won't-fix)**:若 userJoinClause 已简化(历史 phase 已修)→ won't-fix 理由"复测 userJoinClause 已简化(根因诊断在 .md 详述)"。翻 resolved + Phase 41 Closure(wontfix) + commit。

**interface-to-any-conversion-hint**(diagnosed):
- **关键**:.md 已诊断 1261 处 interface{} 全是合法动态值(staticcheck SA1029 命中 0),非 bug 是风格。
- **决策**:**走 won't-fix**(根因已明:非 bug)。理由"1261 处 interface{} 是合法动态值传递(缓存序列化/JSONata/Excel 反射),staticcheck SA1029 命中 0;`interface{}` → `any` 是 Go 1.18+ 风格别名,非 bug,全量替换是机械重构(1261 处)属后续清理,本 phase(v1.16 debug 闭环)不做"。翻 resolved + `skip_audit: true` + Phase 41 Closure(wontfix,理由含"staticcheck 0 命中 + 风格非 bug") + commit `docs(41): interface-to-any-conversion-hint — won't-fix 风格非 bug,staticcheck 0 命中`。

**完成后** 跑 `bash scripts/validate_debug_frontmatter.sh` 确认 100% pass。**不 push**。

**禁止:** 不批量 commit。不在没读项目 memory 的情况下重复 Phase 36/38 已做的 AD 修复。won't-fix 必须具体理由。
  </action>
  <verify>
    <automated>cd "D:\CODE\ClaudeCode\xingran-go-backend" && go build ./... 2>&1 | tail -5 && echo "---VALIDATOR---" && bash scripts/validate_debug_frontmatter.sh 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ad-admin-lockout-recurrence.md 含 `status: resolved` AND `skip_audit: true` AND `won't_fix_reason:` 含"Phase 36/38 账号池覆盖"
    - ad-update-attr-no-such-object.md 含 `status: resolved` AND Phase 41 Closure(实修:files_changed 含 sync.go + go build 退出 0;或 wontfix 含"DNExists 预检已落地")
    - ad-user-page-checkbox-not-click.md 含 `status: resolved` AND Phase 41 Closure(实修或 wontfix 理由)
    - vdi-sync-invalid-uri.md 含 `status: resolved` AND Phase 41 Closure(实修或 wontfix 含"外部依赖")
    - vdi-server-test-500-input-warnings.md 含 `status: resolved` AND Phase 41 Closure
    - user-deptid-uuid-cast-recurring.md 含 `status: resolved` AND Phase 41 Closure(实修:files_changed 含 user_service.go + go build 退出 0;或 wontfix)
    - interface-to-any-conversion-hint.md 含 `status: resolved` AND `skip_audit: true` AND `won't_fix_reason:` 含"staticcheck"或"风格非 bug"
    - 实修型 commit:`go build ./...` 退出码 0
    - `bash scripts/validate_debug_frontmatter.sh` pass rate `100.0%`
    - git log 显示本 task 新增 7 个 commit
  </acceptance_criteria>
  <done>
    7 个 AD/VDI/UUID/类型 session 按 D-02 闭环(项目 memory 驱动,不重复 Phase 36/38 工作);validator 100% pass;7 个独立 commit;不 push。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev 终端 → git 仓库(10 个 commit) | 本地可回滚 |
| AD sync.go / vdi_client 改动 → 生产同步任务 | 实修型 AD/VDI 修复影响定时同步 |
| user_service.go userJoinClause → 用户列表查询 | 实修型 UUID 简化影响全用户列表 SQL |

## STRIDE 威胁寄存器(沿用 41-CONTEXT.md)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering | won't-fix 掩盖真问题(AD/VDI 集群) | mitigate | D-02 强制 + 项目 memory 驱动:ad-admin-lockout won't-fix 必须引用"Phase 36/38 覆盖";ad-update-no-such-object 实修前先 grep 确认 DNExists 未落避免重复修;interface-to-any won't-fix 引用 staticcheck 0 命中 |
| T-41-02 | Repudiation | won't-fix 理由模糊 | mitigate | acceptance_criteria 强制理由含具体引用(Phase 号 / staticcheck / 外部依赖名) |
| T-41-03 | Tampering | AD sync / user_service / vdi_client 实修引入回归 | mitigate | 每 commit 后 `go build ./...` 退出 0;实修触及 handler 写操作加 operlog.Record(CLAUDE.md);user-deptid-uuid 简化 SQL 风险中等,build 通过+抽查用户列表 SQL EXPLAIN 无 error |
| T-41-SC | Tampering | go get / npm install | accept | 本 plan 无新依赖安装(仅改现有 .go 源码) |
</threat_model>

<verification>
- 10 个 Cohort B 后端/认证 session 全部 status: resolved
- 实修型:`go build ./...` 退出码 0(后端)或 `npm run build` 退出 0(前端)
- won't-fix 型:每个 .md 含 `won't_fix_reason:` 具体理由 + 顶层 `skip_audit: true`(若移出 audit)
- AD 类 won't-fix 引用 Phase 36/38 覆盖或外部依赖
- `bash scripts/validate_debug_frontmatter.sh` pass rate `100.0%`
- 每个 session 一个独立 commit,message 格式合规
</verification>

<success_criteria>
- 10 个 Cohort B 后端/认证 session 全部按 D-02 闭环(实修+build 通过 或 won't-fix+理由)
- validator 100% pass 未破坏
- AD 类决策项目 memory 驱动,不重复 Phase 36/38 工作
- audit-open debug_sessions 继续下降(本 plan 贡献 ≤10)
</success_criteria>

<output>
Create `.planning/phases/41-debug-session-gap-closure/41-03-SUMMARY.md` when done
</output>
