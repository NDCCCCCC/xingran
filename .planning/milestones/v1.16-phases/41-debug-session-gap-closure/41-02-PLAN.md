---
phase: 41-debug-session-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/debug/css-tree-vendor-chunk-tdz.md
  - .planning/debug/login-md-vendor-crash.md
  - .planning/debug/react-vendor-activity-tdz.md
  - .planning/debug/frontend-build-66-ts-errors.md
  - .planning/debug/hardcoded-blue-theme-bypass.md
  - .planning/debug/device-modal-serial-auto-match.md
  - .planning/debug/sidebar-apikey-menu-no-response.md
  - xingran-react-frontend/vite.config.ts
  - xingran-react-frontend/src/App.tsx
  - xingran-react-frontend/src/hooks/usePersistedState.test.ts
  - xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx
autonomous: false
requirements: [TECH-06, TECH-05]
tags: [debug-cleanup, cohort-b, frontend, vendor-chunk, mixed-fix-wontfix]

must_haves:
  truths:
    - "Cohort B 前端集群 7 个 session 逐个处理:能修则修+build 通过翻 resolved;陈年/重复/外部依赖者写 won't_fix_reason 翻 resolved(+ skip_audit 移出 audit)"
    - "won't-fix 决策每个都有书面理由(D-02),无理由不关单"
    - "实修的 session 经 npm run build 验证通过(D-03/D-06)"
    - "validator 仍 100% pass"
  artifacts:
    - path: ".planning/debug/css-tree-vendor-chunk-tdz.md"
      provides: "status: resolved(fix 或 wontfix)+ Phase 41 Closure"
      contains: "Phase 41 Closure"
    - path: ".planning/debug/frontend-build-66-ts-errors.md"
      provides: "status: resolved(fix 或 wontfix)+ Phase 41 Closure"
      contains: "Phase 41 Closure"
    - path: ".planning/debug/hardcoded-blue-theme-bypass.md"
      provides: "status: resolved(fix 或 wontfix)+ Phase 41 Closure"
      contains: "Phase 41 Closure"
  key_links:
    - from: "实修型 session .md Resolution"
      to: "npm run build 退出码 0"
      via: "修复后跑 cd xingran-react-frontend && npm run build"
      pattern: "verification: npm run build 退出 0"
    - from: "won't-fix session .md"
      to: "audit-open 计数"
      via: "skip_audit: true 顶层字段让 audit-open 不计"
      pattern: "^skip_audit: true"
---

<objective>
**Cohort B 前端集群 7 个 session 按 D-02 混合策略闭环**:Vendor chunk TDZ 三连(css-tree/login-md/react-vendor-activity)+ 前端 build/主题/组件/sidebar 四个。

Purpose: 这 7 个 session 是真正在途(investigating/root_cause_identified),不能信任状态直接翻。D-02 混合:能修则修(读根因→实修代码→`npm run build` 通过→翻 resolved+Phase 41 Closure 证据),陈年/重复/外部依赖者标 won't-fix(写明理由→翻 resolved+`skip_audit: true` 若移出 audit)。

**关键集群洞察**:Vendor chunk TDZ 三连(css-tree-vendor-chunk-tdz / login-md-vendor-crash / react-vendor-activity-tdz)是**同一根因**(Vite manualChunks 跨 chunk 求值顺序)的三个观察点 — vendor-commons-createcontext(Plan 41-01 已修)是第四个。需要判断:这三个是否已被 vendor-commons-createcontext 的 `@tanstack/react-query → vendor-react` 规则覆盖?若是 → won't-fix 理由"已被 Plan 41-01 / vendor-commons-createcontext 修复覆盖"。若另有独立包(如 css-tree / react Activity)需补 manualChunks 规则 → 实修 vite.config.ts。

Output: 7 个原子 commit(`fix(41): <slug> — <主题>` 实修型 / `docs(41): <slug> — won't-fix <理由>` 文档型),audit-open debug_sessions 继续下降。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-debug-session-gap-closure/41-CONTEXT.md
@.planning/phases/40-tech-debt-cleanup/40-01-SUMMARY.md
@.planning/debug/resolved/asset-export-404-error.md
@scripts/validate_debug_frontmatter.sh
@scripts/fix_debug_frontmatter.py
@xingran-react-frontend/vite.config.ts

<interfaces>
<!-- D-02 混合策略分支判定(每个 session 必走其一): -->
<!--   A) 能修则修:读 .md 根因 → 实修代码 → npm run build 通过 → 翻 resolved + Phase 41 Closure(fix 型) -->
<!--   B) won't-fix:写 won't_fix_reason → 翻 resolved(+ skip_audit: true 若移出 audit) + Phase 41 Closure(won't_fix 型) -->

<!-- Phase 41 Closure 证据块格式(D-04): -->
<!--   实修型: -->
<!--     ## Phase 41 Closure (2026-06-26) -->
<!--     fix: <一句话主题> -->
<!--     verification: cd xingran-react-frontend && npm run build 退出 0 -->
<!--     files_changed: <修改的代码文件> -->
<!--     action: real-fix (D-02) -->
<!--   won't-fix型: -->
<!--     ## Phase 41 Closure (2026-06-26) -->
<!--     won't_fix_reason: <具体理由,如"已被 vendor-commons-createcontext 修复覆盖"> -->
<!--     action: wontfix (D-02) -->
<!--     (顶层加 skip_audit: true 若应移出 audit 计数) -->

<!-- commit 格式(D-06): -->
<!--   fix(41): <slug> — <主题>          (实修型) -->
<!--   docs(41): <slug> — won't-fix <理由>  (won't-fix型) -->

<!-- vite.config.ts manualChunks 当前规则(vendor-commons-createcontext 已落 @tanstack/react-query 规则): -->
<!--   读 xingran-react-frontend/vite.config.ts 确认现有 vendor 切分规则,本 plan 决定是否需新增规则 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor chunk TDZ 三连 + 前端 build 错误 4 个 session(D-02 混合)</name>
  <files>
    .planning/debug/css-tree-vendor-chunk-tdz.md
    .planning/debug/login-md-vendor-crash.md
    .planning/debug/react-vendor-activity-tdz.md
    .planning/debug/frontend-build-66-ts-errors.md
    xingran-react-frontend/vite.config.ts
    xingran-react-frontend/src/hooks/usePersistedState.test.ts
    xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx
  </files>
  <read_first>
    - .planning/debug/css-tree-vendor-chunk-tdz.md(status: root_cause_identified,根因:css-tree 在 vendor 兜底 chunk 跨 chunk 求值 TDZ)
    - .planning/debug/login-md-vendor-crash.md(status: investigating,与 css-tree 同一报错栈 selectors-4)
    - .planning/debug/react-vendor-activity-tdz.md(status: investigating,react-vendor ↔ vendor 循环,Activity 注册 TDZ)
    - .planning/debug/frontend-build-66-ts-errors.md(status: investigating,66 TS 错误,usePersistedState 重构 + VirtualMachineList 缺 vmApi/vdiServerApi import)
    - .planning/debug/vendor-commons-createcontext.md(Plan 41-01 处理,已修 @tanstack/react-query → vendor-react,**关键**:本 task 判断 TDZ 三连是否已被该修复覆盖)
    - xingran-react-frontend/vite.config.ts(确认现有 manualChunks 规则)
    - xingran-react-frontend/src/hooks/usePersistedState.ts(确认新 API:usePersistedStateController 导出)
    - xingran-react-frontend/src/lib/vdiApi.ts(确认 vmApi/vdiServerApi 在 line 34/147 导出)
    - scripts/validate_debug_frontmatter.sh
  </read_first>
  <action>
对下列 4 个 session 逐个走 D-02 决策树(实修 vs won't-fix)。**每个 session 一个独立 commit**(D-06)。

**通用流程:** Read .md 根因 → 判定 A(实修)或 B(won't-fix)→ 执行 → Phase 41 Closure 块 → 翻 resolved → 一个 commit。

---

**css-tree-vendor-chunk-tdz**(root_cause_identified):
- 决策依据:Read .md 根因(css-tree `selectors-4` 在 vendor 兜底 chunk,跨 chunk 求值时 `Mn["selectors-4"]` 即 `To` 未初始化)。**先验证**:跑 `cd xingran-react-frontend && npm run build` 看是否仍报错。再 grep `xingran-react-frontend/vite.config.ts` 看 css-tree / md-editor 相关包是否已显式分 chunk。
- **分支 A(实修)**:若 build 仍报错 AND vite.config.ts 未对 css-tree/md-editor/react-markdown 显式分 chunk → 实修:在 manualChunks 加规则把这些依赖 React/css-tree 的包归入 vendor-react 或独立 markdown-vendor(参考 vendor-commons-createcontext 的修法)。修完跑 `npm run build` 退出 0 → 翻 resolved + Phase 41 Closure(real-fix 型) + commit `fix(41): css-tree-vendor-chunk-tdz — vite manualChunks 显式分 chunk 破 TDZ`。
- **分支 B(won't-fix)**:若 build 已不报错(已被 vendor-commons-createcontext 修复连带覆盖,或 npm 依赖升级已消解)→ won't-fix,理由"复测 build 已不报错,根因被 vendor-commons-createcontext 修复连带覆盖/依赖升级消解"。翻 resolved + skip_audit: true + Phase 41 Closure(won't_fix 型) + commit `docs(41): css-tree-vendor-chunk-tdz — won't-fix 已被 vendor-commons 覆盖`。

**login-md-vendor-crash**(investigating):与 css-tree 同一报错栈(`selectors-4`)。
- **决策**:几乎确定与 css-tree 同根同源(都是 vendor chunk 的 css-tree 模块 TDZ)。**先复测 build**:`cd xingran-react-frontend && npm run build` 是否报错。
- **若 build 通过** → won't-fix,理由"与 css-tree-vendor-chunk-tdz 同根(selectors-4 报错栈完全一致),已被同一修复覆盖"。翻 resolved + skip_audit: true + Phase 41 Closure + commit `docs(41): login-md-vendor-crash — won't-fix 与 css-tree 同根已被覆盖`。
- **若 build 失败** → 与 css-tree 一并实修(同一 vite.config.ts 改动覆盖两者)。commit 仍一话一个。

**react-vendor-activity-tdz**(investigating):不同报错栈(react-vendor ↔ vendor 循环,Activity 注册 TDZ)。
- **决策**:这是独立的 react 19 `Activity` 注册路径问题,可能与 css-tree 不同根。Read .md 根因 → grep vite.config.ts 看 Activity 相关包(可能是 react-19 实验性 API 或第三方)是否被错误归入 vendor 兜底。
- **分支 A(实修)**:若定位到具体包 → 加 manualChunks 规则归入 react-vendor → build 通过 → 翻 resolved + Phase 41 Closure(real-fix) + commit `fix(41): react-vendor-activity-tdz — manualChunks 归 react 生态包破循环`。
- **分支 B(won't-fix)**:若 (a) build 已不报错 或 (b) 根因是 react 19 实验性 Activity API 本身缺陷(外部依赖问题) → won't-fix,理由具体写明。翻 resolved + skip_audit: true(若移出 audit)或仅 resolved + Phase 41 Closure(won't_fix) + commit `docs(41): react-vendor-activity-tdz — won't-fix <具体理由>`。

**frontend-build-66-ts-errors**(investigating):66 TS 错误,根因已在 .md 量化(usePersistedState 重构 + VirtualMachineList 缺 vmApi/vdiServerApi import)。
- **决策**:这是**明确的实修型**(根因清晰,修法已知)。
- **实修步骤**:
  1. 改 `xingran-react-frontend/src/hooks/usePersistedState.test.ts`:用新 `usePersistedStateController` 元组 API [value, setValue, reset] 重写测试断言(Read usePersistedState.ts 确认导出名)。
  2. 改 `xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx`:加 `import { vmApi, vdiServerApi } from "@/lib/vdiApi"`(line 7-26 区段),把 line 53 附近的 `usePersistedState` 调用改为 `usePersistedStateController` 元组解构。
  3. 跑 `cd xingran-react-frontend && npm run build` 退出 0(tsc -b 0 错误 + vite build 成功)。
  4. 翻 resolved + Phase 41 Closure(real-fix 型,含 `verification: npm run build 退出 0` + files_changed)。
  5. commit `fix(41): frontend-build-66-ts-errors — 迁移 usePersistedStateController + 补 vdiApi import`。
- **若 build 仍失败** → 不谎报,反复修到通过(D-06 沿用 Phase 40 D-06);若发现是更深层问题(如 usePersistedState 本身设计缺陷)→ 在 .md Phase 41 Closure 注明 `action: partial-fix-needs-followup`,但 build 必须 0 错误才算 resolved(不降级)。

**完成后** 跑 `bash scripts/validate_debug_frontmatter.sh` 确认 100% pass。**不 push**。

**禁止:** 不批量 commit。不在没复测 build 的情况下判定 TDZ 三连 won't-fix(必须先跑 build 验证)。won't-fix 必须有具体理由(D-02)。
  </action>
  <verify>
    <automated>cd "D:\CODE\ClaudeCode\xingran-go-backend\xingran-react-frontend" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - css-tree-vendor-chunk-tdz.md 含 `status: resolved` AND(`Phase 41 Closure` 含 `real-fix` 或 `wontfix` + 具体理由)
    - login-md-vendor-crash.md 含 `status: resolved` AND Phase 41 Closure(won't-fix 理由必须明确指向"与 css-tree 同根")
    - react-vendor-activity-tdz.md 含 `status: resolved` AND Phase 41 Closure(实修或 won't-fix 理由)
    - frontend-build-66-ts-errors.md 含 `status: resolved` AND `verification: npm run build 退出 0` AND files_changed 列出 usePersistedState.test.ts + VirtualMachineList/index.tsx
    - 若 frontend-build-66-ts-errors 实修:`cd xingran-react-frontend && npm run build` 退出码 0(tsc -b 0 错误)
    - won't-fix 且加 skip_audit 的 .md 含顶层 `skip_audit: true`
    - `bash scripts/validate_debug_frontmatter.sh` pass rate `100.0%`
    - git log 显示本 task 新增 4 个 commit,每个 message 含对应 slug
  </acceptance_criteria>
  <done>
    4 个 session 全部按 D-02 处理完毕(TDZ 三连或实修或 won't-fix,frontend-build 实修+build 通过);validator 100% pass;4 个独立 commit;不 push。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 主题色 + 设备模态框 + API密钥侧栏 3 个 session(D-02 混合,需 dev 浏览器验证)</name>
  <files>
    .planning/debug/hardcoded-blue-theme-bypass.md
    .planning/debug/device-modal-serial-auto-match.md
    .planning/debug/sidebar-apikey-menu-no-response.md
    xingran-react-frontend/src/App.tsx
  </files>
  <read_first>
    - .planning/debug/hardcoded-blue-theme-bypass.md(status: investigating,根因已确认:App.tsx AntConfigProvider 缺 theme prop)
    - .planning/debug/device-modal-serial-auto-match.md(status: investigating,前端 UI 设计问题,后端已实现序列号自动匹配)
    - .planning/debug/sidebar-apikey-menu-no-response.md(status: investigating)
    - xingran-react-frontend/src/App.tsx(确认 AntConfigProvider 当前是否传 theme prop,本 task 判断 hardcoded-blue-theme 是否需补 theme)
    - xingran-react-frontend/src/store/settingsStore.ts(theme.primaryColor 路径)
    - xingran-react-frontend/src/pages/operations/ 或 workstation 的设备添加 Modal 组件
    - .planning/debug/resolved/asset-export-404-error.md
    - scripts/validate_debug_frontmatter.sh
  </read_first>
  <action>
    Claude 对 3 个 session 逐个走 D-02 决策:
    - **hardcoded-blue-theme-bypass**:Read .md 根因(App.tsx:41 AntConfigProvider 缺 theme prop)→ grep App.tsx 确认现状。若仍缺 theme → 实修:从 settingsStore 读 primaryColor 传入 `<ConfigProvider theme={{ token: { colorPrimary: primaryColor } }}>`。修完跑 `npm run build` 退出 0。这是**实修型**(根因清晰)。
    - **device-modal-serial-auto-match**:Read .md(根因:前端 UI 显示多字段,后端 AddDeviceManual 已实现序列号自动匹配,前端未体现)→ 判断。这是**设计/产品决策型**(不是 bug,是 UX 改进)。**走 won't-fix**:理由"前端 UI 多字段是当前产品设计选择,后端自动匹配逻辑已实现但前端 UX 重构属新功能(v1.16 是纯清理 milestone,不做新功能),推后续功能 phase"。翻 resolved + skip_audit: true + Phase 41 Closure(won't_fix) + commit `docs(41): device-modal-serial-auto-match — won't-fix UI 重构属新功能推后续 phase`。
    - **sidebar-apikey-menu-no-response**:Read .md 根因 → grep sidebar/apikey 菜单响应逻辑判断。若根因清晰可修 → 实修;若是权限/路由深层问题且无法在 context 内闭环 → won't-fix 具体理由。

    完成后 Claude 跑 `bash scripts/validate_debug_frontmatter.sh` 确认 100% pass,并把 3 个 commit 准备好。
  </action>
  <verify>
    本 task 是 checkpoint:human-verify,因为 hardcoded-blue-theme-bypass 的修复(AntConfigProvider theme prop)涉及主题色实时切换,build 通过 ≠ 运行时正确,需 dev 浏览器验证。

    用户验证步骤:
    1. 启动 dev server: `cd xingran-react-frontend && npm run dev`(用户在另一终端跑,Claude 不启动)
    2. 浏览器访问 http://127.0.0.1:4000
    3. **验证 hardcoded-blue-theme**(若实修): 登录 → 进入"工位管理"页面 → 观察"表格/卡片/平面图"三选一切换按钮颜色 → 进入系统设置切换主题主色 → 回到工位管理页 → **预期**:按钮颜色随主题变化(不再是硬编码 #1677ff 蓝)
    4. **验证 sidebar-apikey-menu**(若实修): 登录有 API密钥权限的账号 → 点击侧栏 API密钥菜单 → **预期**:菜单响应,进入 API密钥页面
    5. device-modal-serial-auto-match 是 won't-fix,无需运行时验证(仅文档 commit)

    用户在终端确认后,Type "approved" 让 Claude 继续 commit。若主题色不跟随,Describe issues 让 Claude 回到 D-02 实修分支反复修(CLAUDE.md D-06:反复修到通过)。
  </verify>
  <resume-signal>Type "approved" 确认 dev 浏览器主题色/sidebar 验证通过,或 describe issues 让 Claude 继续修</resume-signal>
  <acceptance_criteria>
    - hardcoded-blue-theme-bypass.md 含 `status: resolved` AND Phase 41 Closure(实修:含 `theme={{ token: { colorPrimary } }}` 注入证据 + npm run build 退出 0;或 won't-fix 具体理由)
    - device-modal-serial-auto-match.md 含 `status: resolved` AND `skip_audit: true` AND `won't_fix_reason:` 含"UI 重构属新功能"
    - sidebar-apikey-menu-no-response.md 含 `status: resolved` AND Phase 41 Closure(实修或 won't-fix 理由)
    - 用户 approved(本 task 是 checkpoint)
    - `bash scripts/validate_debug_frontmatter.sh` pass rate `100.0%`
    - git log 显示本 task 新增 3 个 commit
  </acceptance_criteria>
  <done>
    3 个 session 按 D-02 处理完毕(hardcoded-blue-theme 实修+dev 验证 approved;device-modal won't-fix;sidebar 实修或 won't-fix);validator 100% pass;3 个独立 commit;不 push。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev 终端 → git 仓库(7 个 commit) | 本地可回滚 |
| vite.config.ts manualChunks 改动 → 生产构建产物 | 实修型 TDZ 修复改 chunk 划分,影响 bundle |
| AntConfigProvider theme prop → 全局主题渲染 | 实修型主题修复影响所有 AntD 组件渲染 |

## STRIDE 威胁寄存器(沿用 41-CONTEXT.md)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering | won't-fix 掩盖真问题(本 plan 前端集群) | mitigate | D-02 强制:won't-fix 必须书面理由(具体到"已被 X 覆盖"/"外部依赖"/"新功能推后续");device-modal won't-fix 理由明确指向 v1.16 不做新功能 |
| T-41-02 | Repudiation | won't-fix 理由模糊 | mitigate | acceptance_criteria 强制 won't_fix_reason 含具体引用(如同根 slug / 外部依赖名 / 后续 phase 号) |
| T-41-03 | Tampering | vite.config.ts / App.tsx 实修引入回归 | mitigate | 每 commit 后 `cd xingran-react-frontend && npm run build` 退出 0;hardcoded-blue-theme 加 checkpoint:human-verify dev 浏览器验证主题色实时切换 |
| T-41-SC | Tampering | npm install | accept | 本 plan 无 npm 新安装(仅改 vite.config.ts / App.tsx / .tsx 源码) |
</threat_model>

<verification>
- 7 个 Cohort B 前端 session 全部 status: resolved
- 实修型(TDZ / frontend-build / hardcoded-blue-theme / sidebar 若实修):`cd xingran-react-frontend && npm run build` 退出 0
- won't-fix 型:每个 .md 含 `won't_fix_reason:` 具体理由 + 顶层 `skip_audit: true`(若移出 audit)
- `bash scripts/validate_debug_frontmatter.sh` pass rate `100.0%`
- hardcoded-blue-theme(若实修)经 checkpoint:human-verify 用户 approved
- 每个 session 一个独立 commit,message 格式合规
</verification>

<success_criteria>
- 7 个 Cohort B 前端 session 全部按 D-02 闭环(实修+build 通过 或 won't-fix+理由)
- validator 100% pass 未破坏
- 实修型 commit 后 npm run build 退出 0
- checkpoint:human-verify(hardcoded-blue-theme)用户 approved
- audit-open debug_sessions 继续下降(本 plan 贡献 ≤7)
</success_criteria>

<output>
Create `.planning/phases/41-debug-session-gap-closure/41-02-SUMMARY.md` when done
</output>
