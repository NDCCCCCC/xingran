---
phase: 41-debug-session-gap-closure
plan: 04
type: execute
wave: 2
depends_on: [41-01, 41-02, 41-03]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/41-debug-session-gap-closure/41-VERIFICATION.md
autonomous: false
requirements: [TECH-06, TECH-05]
tags: [acceptance-gate, audit-open, verify-phase40, milestone-close]

must_haves:
  truths:
    - "gsd-sdk query audit-open 输出 debug_sessions < 5(目标硬度 D-03,严格不放宽)"
    - "bash scripts/verify_phase40.sh 两条标准全 PASS exit 0(Standard 1 validator 100% + Standard 2 audit-open < 5)"
    - "bash scripts/validate_debug_frontmatter.sh pass rate 100.0%"
    - "本 phase 全部 commit 经 go build ./... 或 npm run build 验证(若触代码)"
    - "STATE.md 更新为 Phase 41 完成"
    - "ROADMAP.md Phase 41 Plans 列表与 progress 表更新"
    - "沿用 Phase 40 工具链(D-05),不新建脚本:验收用 validate_debug_frontmatter.sh / verify_phase40.sh / fix_debug_frontmatter.py"
  artifacts:
    - path: ".planning/phases/41-debug-session-gap-closure/41-VERIFICATION.md"
      provides: "Phase 41 验收证据汇总(audit-open 截图 + verify_phase40.sh 输出 + 29 session 处置矩阵)"
      contains: "Standard 1 PASS"
    - path: ".planning/STATE.md"
      provides: "Phase 41 完成状态"
      contains: "Phase 41"
  key_links:
    - from: "Plan 41-01/02/03 翻 resolved 的 29 个 .md"
      to: "gsd-sdk audit-open debug_sessions count"
      via: "frontmatter status: resolved(+ skip_audit: true)让 audit-open 不计入 open"
      pattern: '"debug_sessions"\\s*:\\s*[0-4]$'
    - from: "scripts/verify_phase40.sh"
      to: "Phase 41 验收 + v1.16 TECH-05 达成"
      via: "Standard 1 + Standard 2 双 PASS"
      pattern: "\\[ALL PASS\\]"
---

<objective>
**Phase 41 验收关门**:确认 Plan 41-01/02/03 闭环后 `audit-open debug_sessions < 5`(D-03 严格目标),`verify_phase40.sh` 双标准 PASS,validator 100% pass。汇总 29 个 session 处置矩阵(复测翻 / 实修翻 / won't-fix 翻)到 41-VERIFICATION.md,更新 STATE.md / ROADMAP.md,达成 v1.16 TECH-05 验收。

Purpose: 这是 v1.16 milestone 的最终验收关口。Phase 40 已完成 22 个 deferred session + 6 audit 文件但留 29 个 scope 外 open session,Phase 41 三个 closure plan 把这 29 个降到 < 5。本 plan 不动 debug session(那是前三 plan 职责),只做验收+文档收尾。

Output: 41-VERIFICATION.md 验收证据 + STATE.md 更新 + ROADMAP.md 更新 + 1-2 个 docs(41) commit。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-debug-session-gap-closure/41-CONTEXT.md
@.planning/phases/40-tech-debt-cleanup/40-03-SUMMARY.md
@scripts/verify_phase40.sh
@scripts/validate_debug_frontmatter.sh

<interfaces>
<!-- Phase 41 验收三件套(D-03/D-04/D-05): -->
<!--   1. gsd-sdk query audit-open | jq '.counts.debug_sessions' → 必须 < 5 -->
<!--   2. bash scripts/verify_phase40.sh → 必须 [ALL PASS] exit 0 -->
<!--   3. bash scripts/validate_debug_frontmatter.sh → 必须 pass rate 100.0% -->

<!-- Phase 40 verify_phase40.sh 双标准(本 phase 必须双 PASS): -->
<!--   Standard 1: validator 100% pass(Phase 40 已达成,本 phase 不破坏) -->
<!--   Standard 2: audit-open debug_sessions < 5(Phase 40 FAIL=29,本 phase 目标 PASS) -->

<!-- v1.16 milestone 验收:Phase 40 + Phase 41 双 phase 完成 → TECH-05 达成 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 跑三件套验收 + 汇总 29 session 处置矩阵(D-03/D-04/D-05)</name>
  <files>
    .planning/phases/41-debug-session-gap-closure/41-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/41-debug-session-gap-closure/41-01-SUMMARY.md(Plan 41-01 完成总结)
    - .planning/phases/41-debug-session-gap-closure/41-02-SUMMARY.md(Plan 41-02 完成总结)
    - .planning/phases/41-debug-session-gap-closure/41-03-SUMMARY.md(Plan 41-03 完成总结)
    - .planning/phases/40-tech-debt-cleanup/40-03-SUMMARY.md(Phase 40 验收模式范本,Standard 1 PASS / Standard 2 FAIL 记录格式)
    - scripts/verify_phase40.sh
    - scripts/validate_debug_frontmatter.sh
    - .planning/REQUIREMENTS.md(TECH-05 / TECH-06 验收定义)
  </read_first>
  <action>
**前置检查**:确认 Plan 41-01/02/03 三个 SUMMARY.md 都已生成(依赖已完成)。若任一未完成,本 task 阻塞等待。

**Step 1: 跑三件套验收**(逐个跑,捕获完整输出):
1. `gsd-sdk query audit-open | jq '.counts'` — 记录 debug_sessions 数(目标 < 5)。同时记录 quick_tasks / uat_gaps / verification_gaps 等其他计数(参考用,本 phase 不强求)。
2. `bash scripts/verify_phase40.sh` — 记录 Standard 1 (validator 100% pass) + Standard 2 (debug_sessions < 5) 结果。**必须 [ALL PASS] exit 0**(D-03)。
3. `bash scripts/validate_debug_frontmatter.sh` — 记录 pass rate(必须 100.0%)。

**Step 2: 若 Standard 2 未达 < 5**(D-03 不放宽):
- 跑 `gsd-sdk query audit-open | jq '.items.debug_sessions[] | {slug, status}'` 列出剩余 open session。
- 逐一核对:这些是否是 Plan 41-01/02/03 漏处理的(漏翻 resolved / 漏加 skip_audit)→ 回对应 plan 补处理。
- 若是 Plan 41-02/41-03 实修分支未完成 build 的 session(卡在中间)→ 回对应 plan 反复修到通过(D-06)。
- **禁止**:不放宽 verify_phase40.sh 阈值(D-03)。不谎报 audit-open 数字。

**Step 3: 汇总 29 session 处置矩阵** 到 `.planning/phases/41-debug-session-gap-closure/41-VERIFICATION.md`:

矩阵格式(严格 per-slug:每个 slug 一行,共 29 行;即使按 plan 视觉分组也必须 29 行,不允许 per-plan 小计替代 — 此 gate 捕获 W3 的 orphan 风险):
| slug | 原 status | Plan | 处置类型(re-verify-flip / real-fix / wontfix) | Phase 41 Closure 锚点 | 现 status |

参考 Phase 40 40-03-SUMMARY.md 的 Verification 表格风格。

在 VERIFICATION.md 顶部记录:
- 三件套输出(audit-open debug_sessions=X / verify_phase40.sh [ALL PASS] exit 0 / validator 100.0%)
- 29 session 处置统计:复测翻 X / 实修翻 Y / won't-fix 翻 Z(应 = 29)
- TECH-06 达成声明 + TECH-05(< 5 部分)达成声明

**Step 4: commit** `docs(41): verification — 29 session 处置矩阵 + 三件套验收 PASS`。

**不 push**(D-06)。
  </action>
  <verify>
    <automated>cd "D:\CODE\ClaudeCode\xingran-go-backend" && bash scripts/verify_phase40.sh 2>&1 | tail -15 && echo "---AUDIT---" && gsd-sdk query audit-open 2>/dev/null | grep -E '"debug_sessions"'</automated>
  </verify>
  <acceptance_criteria>
    - `gsd-sdk query audit-open | jq '.counts.debug_sessions'` 返回值 < 5(即 0-4)
    - `bash scripts/verify_phase40.sh` 输出含 `[ALL PASS]` AND 退出码 0
    - `bash scripts/validate_debug_frontmatter.sh` 输出 pass rate `100.0%`
    - `.planning/phases/41-debug-session-gap-closure/41-VERIFICATION.md` 存在 AND 含"Standard 1 PASS" AND 含"Standard 2 PASS" AND 含严格 per-slug 29 行矩阵(每个 slug 一行,即使按 plan 视觉分组也必须 29 行,不允许用 per-plan 小计替代)
    - VERIFICATION.md 含 TECH-06 + TECH-05 达成声明
    - git log 显示本 task 新增 1 个 commit `docs(41): verification — ...`
  </acceptance_criteria>
  <done>
    三件套全 PASS(audit-open < 5 + verify_phase40.sh exit 0 + validator 100%);41-VERIFICATION.md 生成含 29 session 矩阵;1 个 docs(41) commit;不 push。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 更新 STATE.md / ROADMAP.md + 用户最终确认 Phase 41 完成</name>
  <files>
    .planning/STATE.md
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/STATE.md(当前 Phase 40 executing 状态)
    - .planning/ROADMAP.md(Phase 41 Plans 列表 + Progress Table)
    - .planning/phases/41-debug-session-gap-closure/41-VERIFICATION.md(Task 1 生成)
    - .planning/REQUIREMENTS.md(TECH-05/TECH-06 状态)
  </read_first>
  <action>
    Claude 更新两份文档:

    **STATE.md 更新**:
    - `status: executing` → `status: milestone_complete`(或 `phase_41_complete`,看 milestone 是否还有其他 phase — v1.16 只有 Phase 40 + 41,所以 milestone complete)
    - `stopped_at: Phase 40 context gathered` → `stopped_at: Phase 41 complete — v1.16 milestone closed`
    - `last_updated` 时间戳更新
    - `progress.completed_phases: 0` → `1`(Phase 41 完成计数,Phase 40 在历史)
    - `progress.completed_plans` 更新为 Phase 41 的 4 个 plan 完成数
    - "Current Position" 段:Phase 41 EXECUTING → COMPLETE
    - "Phase Status (v1.16)" 表:Phase 41 行 0/TBD → 4/4 Complete
    - "Completed Milestones" 段加一行:v1.16 技术债清理(2026-06-26)— 2 phases (40+41), 7 plans
    - "Session History" 表加:2026-06-26 | Phase 41 完成 — debug_sessions 29→<5, v1.16 闭环
    - "Next Steps" 段更新:v1.16 milestone 闭环,可 push;建议下一 milestone 规划

    **ROADMAP.md 更新**:
    - Phase 41 的 `**Plans**: TBD` → `**Plans**: 4 plans`(列出 41-01/02/03/04 一行摘要)
    - Phase 41 Progress Table 行:`0/TBD | Planning | -` → `4/4 | Complete | 2026-06-26`
    - 顶部 milestone 行:v1.16 状态从"in progress"更新为"complete"(若适用)

    **REQUIREMENTS.md 更新**(可选,若 TECH-05/06 状态需勾选):
    - `[ ] TECH-05` → `[x] TECH-05`
    - `[ ] TECH-06` → `[x] TECH-06`
    - Traceability 表对应行 Status: Planned → Completed

    commit `docs(41): close phase 41 + v1.16 milestone — STATE/ROADMAP/REQUIREMENTS 更新`。

    完成后 Claude 把"v1.16 可统一 push"作为 checkpoint 信号交给用户。
  </action>
  <verify>
    本 task 是 checkpoint:human-verify,因为 (a) 文档更新需用户确认无遗漏,(b) v1.16 milestone 闭环是重大节点,(c) 是否现在 `git push` 需用户决策(D-06 说本地完成,结束统一 push)。

    用户验证步骤:
    1. 打开 `.planning/STATE.md` 确认状态更新正确(phase_41_complete / milestone_complete)
    2. 打开 `.planning/ROADMAP.md` 确认 Phase 41 列 4/4 Complete + Plans 列表正确
    3. (可选)打开 `.planning/REQUIREMENTS.md` 确认 TECH-05/06 勾选
    4. 跑一次 `bash scripts/verify_phase40.sh` 最终复核 [ALL PASS]
    5. 决策:是否现在 `git push origin main`(D-06 说本地完成,结束统一 push;用户决定时机)

    Type "approved" 确认文档更新无误。若需补漏,Describe issues。
  </verify>
  <resume-signal>Type "approved" 确认 STATE/ROADMAP 更新,并指示是否现在 git push</resume-signal>
  <acceptance_criteria>
    - `.planning/STATE.md` 含 `phase_41_complete` 或 `milestone_complete` AND Phase 41 行 4/4 Complete
    - `.planning/ROADMAP.md` Phase 41 含 `4 plans` 列表 AND Progress Table 行 `4/4 | Complete | 2026-06-26`
    - 用户 approved(本 task 是 checkpoint)
    - `bash scripts/verify_phase40.sh` 最终复核 [ALL PASS] exit 0
    - git log 显示本 task 新增 1 个 commit `docs(41): close phase 41 ...`
  </acceptance_criteria>
  <done>
    STATE.md / ROADMAP.md / REQUIREMENTS.md 更新完成,用户 approved;Phase 41 完成;v1.16 milestone 闭环;用户决定 push 时机。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev 终端 → git 仓库(2 个 docs(41) commit) | 本地可回滚 |
| Phase 41 验收声明 → v1.16 milestone 关门 | 误报 PASS 会掩盖未达 < 5 |

## STRIDE 威胁寄存器(沿用 41-CONTEXT.md)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering | 验收谎报(写 PASS 但实际 debug_sessions ≥ 5) | mitigate | acceptance_criteria 强制 `gsd-sdk query audit-open | jq '.counts.debug_sessions'` 返回 < 5 AND verify_phase40.sh [ALL PASS] exit 0;三件套输出原样记录到 41-VERIFICATION.md |
| T-41-02 | Repudiation | 29 session 处置矩阵遗漏(某 session 未处置却未发现) | mitigate | VERIFICATION.md 矩阵强制严格 per-slug 29 行(每个 slug 一行,不允许 per-plan 小计替代);每行含 slug + Plan 引用 + 处置类型 + Closure 锚点,可追溯(此 gate 捕获 W3 orphan) |
| T-41-03 | Tampering | 实修型回归(本 plan 不动代码) | accept | 本 plan 纯文档+验收,无代码修改,无回归风险;回归风险在前三 plan 已通过 build 缓解 |
| T-41-SC | Tampering | npm/pip/cargo install | accept | 本 plan 无包安装 |
</threat_model>

<verification>
- `gsd-sdk query audit-open | jq '.counts.debug_sessions'` 返回 < 5
- `bash scripts/verify_phase40.sh` [ALL PASS] exit 0
- `bash scripts/validate_debug_frontmatter.sh` pass rate 100.0%
- `.planning/phases/41-debug-session-gap-closure/41-VERIFICATION.md` 含完整 29 session 矩阵 + 三件套输出
- STATE.md / ROADMAP.md 更新 Phase 41 完成状态
- 用户 checkpoint:human-verify approved
</verification>

<success_criteria>
- audit-open debug_sessions < 5(D-03 严格目标达成)
- verify_phase40.sh 双标准全 PASS(Standard 1 + Standard 2)
- validator 100% pass 未破坏
- 29 session 全部处置(复测翻 / 实修翻 / won't-fix 翻)且矩阵可追溯
- STATE.md / ROADMAP.md / REQUIREMENTS.md 更新完成
- 用户 approved Phase 41 完成 + v1.16 milestone 闭环
- 本 plan 2 个 commit(VERIFICATION + STATE/ROADMAP)
</success_criteria>

<output>
Create `.planning/phases/41-debug-session-gap-closure/41-04-SUMMARY.md` when done
</output>
