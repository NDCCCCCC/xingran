---
phase: 41-debug-session-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/debug/go-textfsm-dollar-anchor-bug.md
  - .planning/debug/quick-create-fields-not-saving.md
  - .planning/debug/vdi-cpu-display-issue.md
  - .planning/debug/vdi-menu-routing-redirect.md
  - .planning/debug/vm-list-page-blank-404.md
  - .planning/debug/infopoint-fields-not-saving-at-all.md
  - .planning/debug/vdi-vm-operations-fail.md
  - .planning/debug/vendor-commons-createcontext.md
  - .planning/debug/captcha-custom-background-not-found.md
  - .planning/debug/page-refresh-token-refresh-loop-failure.md
  - .planning/debug/ops-perms-alignment.md
  - .planning/debug/operlog-loginlog-nickname-not-showing.md
autonomous: true
requirements: [TECH-06, TECH-05]
tags: [debug-cleanup, frontmatter-flip, re-verify, cohort-a]

must_haves:
  truths:
    - "12 个 Cohort A session 按 D-01 复测-后-翻模式:经复测确认修复落地后 status 翻为 resolved(不信任状态直接翻,锚点找不到登记 escalate-to-D-02)"
    - "每个被翻的 session .md Resolution 章节追加 Phase 41 Closure 复测证据块"
    - "复测发现未真落地的 session 不谎报 resolved(转入 Plan 41-02/41-03 实修路径)"
    - "validator 仍 100% pass(本 plan 不破坏 Phase 40 Standard 1)"
  artifacts:
    - path: ".planning/debug/go-textfsm-dollar-anchor-bug.md"
      provides: "status: resolved + Phase 41 Closure 复测证据"
      contains: "Phase 41 Closure"
    - path: ".planning/debug/vendor-commons-createcontext.md"
      provides: "status: resolved + Phase 41 Closure 复测证据"
      contains: "Phase 41 Closure"
    - path: ".planning/debug/operlog-loginlog-nickname-not-showing.md"
      provides: "status: resolved(待重启 → 复测确认) + Phase 41 Closure"
      contains: "Phase 41 Closure"
  key_links:
    - from: ".planning/debug/<slug>.md status field"
      to: "gsd-sdk audit-open debug_sessions count"
      via: "frontmatter status: resolved 让 audit-open 不再计入 open"
      pattern: "^status: resolved"
---

<objective>
**Cohort A 12 个"实质已完成态"session 按 D-01 复测-后-翻模式归零**(debug_complete 5 / fixed 3 / fix_applied 2 / applied 1 / fixed_pending_restart 1)。

Purpose: 这 12 个 session 的 frontmatter 卡在中间状态但根因修复可能已在历史 phase(35-39)落地,本 plan 走 Phase 40 40-01 已验证的"复测发现已落地"模式 — 读 .md Resolution → grep 代码确认所述修复确实在当前代码树 → .md 追加 Phase 41 Closure 复测证据 → 翻 `status: resolved`。**不信任状态直接翻**(D-01):复测若发现修复未真落地,该 session **不谎报 resolved**,转入 Plan 41-02 或 41-03 的 D-02 实修路径(在本 plan Task 的 `<action>` 里明确登记"转入实修"分支)。

Output: 12 个原子 commit(`fix(41): <slug> — <主题>` 或 `docs(41): <slug> — 复测确认 resolved`),audit-open debug_sessions 从 29 下降 ~12(若全部复测通过)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-debug-session-gap-closure/41-CONTEXT.md
@.planning/phases/40-tech-debt-cleanup/40-01-SUMMARY.md
@.planning/debug/resolved/asset-export-404-error.md
@scripts/validate_debug_frontmatter.sh

<interfaces>
<!-- Phase 40 已建立的"复测发现已落地"模式(40-01 SUMMARY),本 plan 直接复用: -->
<!-- 1. Read .md Resolution/Fix 章节 -->
<!-- 2. grep/read 代码确认所述修复确实在当前代码树(具体 file:line) -->
<!-- 3. 在 .md Resolution 章节追加 Phase 41 Closure 复测证据块 -->
<!-- 4. 翻 frontmatter status: <中间态> → resolved -->
<!-- 5. 一个 session 一个 commit -->

<!-- resolved 风格 frontmatter 范式(asset-export-404-error.md): -->
<!--   status: resolved -->
<!--   Resolution 章节含: root_cause / fix / verification / files_changed -->

<!-- Phase 41 Closure 复测证据块格式(D-04): -->
<!--   verification: 2026-06-26 复测 <file>:<line> 确认修复落地 -->
<!--   files_changed: <grep 验证的代码路径> -->

<!-- commit 格式(D-06): -->
<!--   fix(41): <slug> — <主题>          (若复测通过直接翻) -->
<!--   docs(41): <slug> — 复测确认 resolved  (等价,文档型) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cohort A 前端/Vendor 4 个 session 复测-后-翻(D-01)</name>
  <files>
    .planning/debug/vendor-commons-createcontext.md
    .planning/debug/page-refresh-token-refresh-loop-failure.md
    .planning/debug/captcha-custom-background-not-found.md
    .planning/debug/quick-create-fields-not-saving.md
  </files>
  <read_first>
    - .planning/debug/vendor-commons-createcontext.md(当前 status: fixed,Resolution 述 vite.config.ts manualChunks 加 @tanstack/react-query 规则)
    - .planning/debug/page-refresh-token-refresh-loop-failure.md(当前 status: fix_applied,Resolution 述 TokenManager.ts doRefresh 移除 getCachedEncryptionConfig)
    - .planning/debug/captcha-custom-background-not-found.md(当前 status: fix_applied)
    - .planning/debug/quick-create-fields-not-saving.md(当前 status: debug_complete)
    - xingran-react-frontend/vite.config.ts(确认 manualChunks 含 @tanstack/react-query 规则)
    - xingran-react-frontend/src/utils/token/TokenManager.ts(确认 doRefresh 不再调 getCachedEncryptionConfig)
    - .planning/debug/resolved/asset-export-404-error.md(resolved 风格 + Phase 41 Closure 证据块范本)
    - scripts/validate_debug_frontmatter.sh(翻完后跑,确认 100% pass)
    - .planning/phases/40-tech-debt-cleanup/40-01-SUMMARY.md("复测发现已落地"模式范例)
  </read_first>
  <action>
对下列 4 个 session 逐个执行 D-01 "复测-后-翻" 流程。**每个 session 一个独立 commit**(D-06),commit 格式 `fix(41): <slug> — <主题>` 或 `docs(41): <slug> — 复测确认 resolved`。

**通用流程(每个 session):**
1. Read 其 .md 的 Resolution/Fix 章节,提取所述修复的**代码位置**(file:line + 关键符号)
2. 用 Grep/Read 在当前代码树**确认所述修复确实存在**(具体到 file:line)
3. 在 .md Resolution 章节末尾追加 Phase 41 Closure 复测证据块:
   ```
   ## Phase 41 Closure (2026-06-26)
   verification: 2026-06-26 复测 <file>:<line> 确认修复落地
   files_changed: <实际 grep 验证的代码路径>
   action: re-verify-then-flip (D-01)
   ```
4. 翻 frontmatter `status: <当前>` → `status: resolved`
5. 该 session 一个 commit

**逐 session 复测锚点**(若复测不通过,**不谎报**,登记"转入 Plan 41-02 实修"并在 .md Phase 41 Closure 块注明 `action: escalate-to-D-02`,status 保持原值,跳过 commit):

- **vendor-commons-createcontext**(当前 fixed):grep `xingran-react-frontend/vite.config.ts` 含 `@tanstack/react-query` 且 manualChunks 规则 return `'vendor-react'`。验证命令:`grep -n "@tanstack/react-query" xingran-react-frontend/vite.config.ts` 应有 ≥1 行命中,且上下文 return 'vendor-react'。若命中 → 翻 resolved + commit `fix(41): vendor-commons-createcontext — 复测 vite manualChunks 规则已落地`。
- **page-refresh-token-refresh-loop-failure**(当前 fix_applied):grep `xingran-react-frontend/src/utils/token/TokenManager.ts` 的 doRefresh 方法**不含** `getCachedEncryptionConfig` 调用。验证:`grep -n "getCachedEncryptionConfig" xingran-react-frontend/src/utils/token/TokenManager.ts` 应为空(或仅在非 doRefresh 上下文)。若确认 doRefresh 已移除 → 翻 resolved + commit `fix(41): page-refresh-token-refresh-loop-failure — 复测 doRefresh 循环依赖已移除`。
- **captcha-custom-background-not-found**(当前 fix_applied):Read .md Resolution 提取所述文件路径 → grep 确认自定义背景图加载逻辑/资源路径存在。若 .md 未给明确代码锚点 → 用 Grep 搜 `captcha` + `background` 在 `internal/api/v1/` 和 `internal/core/` 的命中,确认所述修复在;若找不到明确代码锚点,**转入 D-02 实修路径**(不翻,在 .md Phase 41 Closure 块写 `action: escalate-to-D-02`,留待 Plan 41-02)。
- **quick-create-fields-not-saving**(当前 debug_complete):Read .md Resolution 提取所述修复 → grep 确认 quick-create 表单字段保存逻辑在(可能在 `xingran-react-frontend/src/pages/operations/` 或 opsApi.ts)。若复测通过 → 翻 resolved;若找不到明确锚点 → 转入 D-02。

**完成后**(4 个 session 处理完):跑 `bash scripts/validate_debug_frontmatter.sh` 确认仍 100% pass(本 task 只动 .md frontmatter,不动代码,不应破坏 validator)。**不 push**(D-06,本地)。

**禁止:** 不批量多 session 一个 commit。不信任状态直接翻(每个必须先 grep 代码确认)。不在本 task 实修代码(那是 Plan 41-02/41-03 的职责)。
  </action>
  <verify>
    <automated>cd "D:\CODE\ClaudeCode\xingran-go-backend" && bash scripts/validate_debug_frontmatter.sh 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/debug/vendor-commons-createcontext.md` 含 `status: resolved` AND grep `@tanstack/react-query` 在 `xingran-react-frontend/vite.config.ts` 至少 1 行命中
    - `.planning/debug/page-refresh-token-refresh-loop-failure.md` 含 `status: resolved` AND `grep -c "getCachedEncryptionConfig" xingran-react-frontend/src/utils/token/TokenManager.ts` 的命中行不在 doRefresh 方法体内(Read 验证)
    - 每个被翻的 .md 含字符串 `Phase 41 Closure` AND `verification: 2026-06-26`
    - 转入 D-02 的 session(若有).md 含 `action: escalate-to-D-02` AND status 保持原值(非 resolved)
    - `bash scripts/validate_debug_frontmatter.sh` 末行 pass rate 为 `100.0%`
    - git log 显示本 task 新增 1-4 个 commit(每个 slug 一个),message 形如 `fix(41): <slug> — ...` 或 `docs(41): <slug> — ...`
  </acceptance_criteria>
  <done>
    4 个 Cohort A 前端/Vendor session 全部按 D-01 复测-后-翻完成(或诚实登记转入 D-02);validator 仍 100% pass;每个 session 一个独立 commit;不 push。
  </done>
</task>

<task type="auto">
  <name>Task 2: Cohort A Go 后端/VDI/operlog/perms 8 个 session 复测-后-翻(D-01)</name>
  <files>
    .planning/debug/go-textfsm-dollar-anchor-bug.md
    .planning/debug/vdi-cpu-display-issue.md
    .planning/debug/vdi-menu-routing-redirect.md
    .planning/debug/vm-list-page-blank-404.md
    .planning/debug/infopoint-fields-not-saving-at-all.md
    .planning/debug/vdi-vm-operations-fail.md
    .planning/debug/ops-perms-alignment.md
    .planning/debug/operlog-loginlog-nickname-not-showing.md
  </files>
  <read_first>
    - 上述 8 个 .planning/debug/<slug>.md(逐个读 Resolution 章节)
    - .planning/debug/resolved/asset-export-404-error.md(resolved 范本)
    - .planning/phases/40-tech-debt-cleanup/40-01-SUMMARY.md(Phase 40 复测归零模式,12/17 复测型范例)
    - scripts/validate_debug_frontmatter.sh
    - 对应代码文件:.md Resolution 述及的(internal/templates/textfsm.go, internal/services/vdi/*, internal/models/log.go, internal/api/v1/auth.go recordLoginLog, xingran-react-frontend/src/pages/vdi/*, internal/core/db/migrations/migration_161_login_log_add_nickname.go)
  </read_first>
  <action>
对下列 8 个 session 逐个执行 D-01 "复测-后-翻" 流程。**每个 session 一个独立 commit**(D-06),格式 `fix(41): <slug> — <主题>` 或 `docs(41): <slug> — 复测确认 resolved`。

**通用流程同 Task 1**(Read .md Resolution → grep 代码确认 → 追加 Phase 41 Closure 复测证据块 → 翻 resolved → 一个 commit)。Phase 41 Closure 块格式:
```
## Phase 41 Closure (2026-06-26)
verification: 2026-06-26 复测 <file>:<line> 确认修复落地
files_changed: <grep 验证的代码路径>
action: re-verify-then-flip (D-01)
```

**逐 session 复测锚点**(锚点找不到 → 不谎报,登记 `action: escalate-to-D-02`,status 保持原值,跳过 commit,留待 Plan 41-02/41-03):

- **go-textfsm-dollar-anchor-bug**(debug_complete):grep `internal/templates/textfsm.go` 的 parseRule 函数含 `hasStartAnchor` / `hasEndAnchor` 变量,且在 buildRegexWithVariables 后接 `^` + `$` 拼回。验证:`grep -n "hasStartAnchor\|hasEndAnchor" internal/templates/textfsm.go` ≥2 行命中。
- **vdi-cpu-display-issue**(debug_complete):Read .md Resolution 提取所述文件(可能 xingran-react-frontend/src/pages/vdi/ 的 CPU 显示列 或 internal/services/vdi/)→ grep 确认。
- **vdi-menu-routing-redirect**(debug_complete):grep `xingran-react-frontend/src/` 的 VDI 菜单路由配置,确认 redirect 已修正。可能涉及 src/App.tsx 或 menu seed。
- **vm-list-page-blank-404**(debug_complete):grep 虚拟机列表页路由,确认 404 已修(可能 vdi_router.go 或前端路由)。
- **infopoint-fields-not-saving-at-all**(fixed):Read .md Resolution → grep `internal/services/operations/` 或 excel_service.go 确认 infopoint 字段保存逻辑。
- **vdi-vm-operations-fail**(fixed):Read .md Resolution → grep VDI VM 操作(启动/停止/重启)handler/service 确认修复。
- **ops-perms-alignment**(applied):Read .md Resolution → grep 权限注册点(`internal/api/v1/operations/` 或 network_router.go)确认权限对齐。
- **operlog-loginlog-nickname-not-showing**(fixed_pending_restart):**特殊**:Read .md 显示修复已落代码(LoginLog 加 Nickname 字段 + migration_161 + recordLoginLog 传值)但"待用户重启验证"。复测锚点:`grep -n "Nickname" internal/models/log.go` 命中 LoginLog struct 字段 AND `ls internal/core/db/migrations/migration_161_login_log_add_nickname.go` 存在 AND `grep -n "migration_161\|Migrate161" internal/core/db/database.go` 命中注册。若三处全部确认修复已落代码 → 在 Phase 41 Closure 块注明 `verification: 2026-06-26 复测代码已落地(待用户重启后端触发 migration_161 运行时验证,但代码层修复完整)`,翻 resolved。这是 D-01 允许的:复测确认修复落地(代码层),运行时验证由后续 dev 启动补足。

**每个 session 处理完后立即 commit**(不累积)。**不 push**。

**禁止:** 不批量多 session 一个 commit。不在本 task 实修代码(转入 D-02 的 session 由 Plan 41-02/41-03 处理)。不谎报 resolved。
  </action>
  <verify>
    <automated>cd "D:\CODE\ClaudeCode\xingran-go-backend" && bash scripts/validate_debug_frontmatter.sh 2>&1 | tail -5 && echo "---" && grep -L "status: resolved" .planning/debug/go-textfsm-dollar-anchor-bug.md .planning/debug/vdi-cpu-display-issue.md .planning/debug/vdi-menu-routing-redirect.md .planning/debug/vm-list-page-blank-404.md .planning/debug/infopoint-fields-not-saving-at-all.md .planning/debug/vdi-vm-operations-fail.md .planning/debug/ops-perms-alignment.md .planning/debug/operlog-loginlog-nickname-not-showing.md 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 8 个 .md 中,被翻 resolved 的每个含 `status: resolved` AND `Phase 41 Closure` AND `verification: 2026-06-26`
    - go-textfsm: `grep -c "hasStartAnchor\|hasEndAnchor" internal/templates/textfsm.go` ≥ 2(若翻 resolved)
    - operlog-loginlog: `grep -c "Nickname" internal/models/log.go` ≥ 1 AND migration_161 文件存在 AND database.go 注册命中(若翻 resolved)
    - 转入 D-02 的 session(若有).md 含 `action: escalate-to-D-02` AND status 非 resolved
    - `bash scripts/validate_debug_frontmatter.sh` pass rate `100.0%`
    - git log 显示本 task 新增 1-8 个 commit,每个 message 含 `fix(41): <对应-slug>` 或 `docs(41): <对应-slug>`
  </acceptance_criteria>
  <done>
    8 个 Cohort A Go/VDI/operlog/perms session 全部按 D-01 复测-后-翻完成(或诚实登记转入 D-02);validator 仍 100% pass;每个 session 一个独立 commit;不 push。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev 终端 → git 仓库(12 个 fix(41)/docs(41) commit) | 本地可回滚 |
| .md status 字段 → audit-open 计数 | 误翻 resolved 掩盖未真修 |

## STRIDE 威胁寄存器(沿用 41-CONTEXT.md)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering | 批量翻 resolved 掩盖未真修(本 plan 12 个 cohort A session) | mitigate | D-01 强制:每个 session 翻前必须 grep 代码确认所述修复在当前代码树;锚点找不到则登记 `action: escalate-to-D-02` 不翻;acceptance_criteria 含具体 grep 命中数 |
| T-41-02 | Repudiation | 复测证据伪造(写"已复测"但未真 grep) | mitigate | Phase 41 Closure 块强制含 `verification: 2026-06-26 复测 <file>:<line>` 具体锚点 + acceptance_criteria 用 grep -c 验证代码符号在 |
| T-41-03 | Tampering | 实修引入回归 | accept | 本 plan **不动代码**(纯复测+翻 frontmatter),无回归风险;转入 D-02 的 session 由 Plan 41-02/41-03 实修并跑 build |
| T-41-SC | Tampering | npm/pip/cargo install | accept | 本 plan 无包安装 |
</threat_model>

<verification>
- 12 个 Cohort A session 中,被翻 resolved 的每个 .md:`grep -c "status: resolved" .planning/debug/<slug>.md` = 1 AND `grep -c "Phase 41 Closure" .planning/debug/<slug>.md` ≥ 1
- `bash scripts/validate_debug_frontmatter.sh` pass rate = `100.0%`(不破坏 Phase 40 Standard 1)
- `gsd-sdk query audit-open | jq '.counts.debug_sessions'` 较本 plan 起始值(29)下降等于本 plan 实翻 resolved 的 session 数(理想 -12,实际取决于多少被转入 D-02)
- 每个 session 一个独立 commit,git log 可追溯
</verification>

<success_criteria>
- 12 个 Cohort A session 全部按 D-01 处理完毕(翻 resolved 或诚实登记转入 D-02)
- validator 100% pass 未破坏
- 12 个(或更少,若有转入 D-02)独立 commit,message 格式合规
- 无代码修改(本 plan 纯文档+frontmatter),无 build 需求
- .md Phase 41 Closure 证据块格式合规(D-04)
</success_criteria>

<output>
Create `.planning/phases/41-debug-session-gap-closure/41-01-SUMMARY.md` when done
</output>
