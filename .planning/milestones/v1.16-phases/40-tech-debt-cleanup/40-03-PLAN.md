---
phase: 40-tech-debt-cleanup
plan: 03
type: execute
wave: 3
depends_on:
  - 40-01
  - 40-02
files_modified:
  - .planning/debug/login-400-bad-request.md
  - .planning/debug/ops-asset-constraint-uni-ops-asset-devicesn-not-exist.md
  - .planning/debug/sys-mac-filter-rules-relation-does-not-exist.md
  - .planning/debug/knowledge-base.md
  - .planning/debug/info-point-type-null-import.md
  - .planning/debug/apikey-route-path-duplication.md
  - scripts/validate_debug_frontmatter.sh
  - scripts/verify_phase40.sh
autonomous: true
requirements:
  - TECH-04
  - TECH-05
must_haves:
  truths:
    - 6 个 audit 数据质量问题 1 个 batch commit `docs(40): standardize debug frontmatter` 落地（D-15）
    - .planning/debug/login-400-bad-request.md 补齐标准 YAML frontmatter
    - .planning/debug/ops-asset-constraint-uni-ops-asset-devicesn-not-exist.md 补齐
    - .planning/debug/sys-mac-filter-rules-relation-does-not-exist.md 补齐
    - .planning/debug/knowledge-base.md 加 `skip_audit: true` 顶层字段
    - .planning/debug/info-point-type-null-import.md 转 resolved/ 风格
    - .planning/debug/apikey-route-path-duplication.md 在 Plan 01 后已 resolved；本 plan 仅校验 frontmatter 字段齐全（D-09）
    - scripts/validate_debug_frontmatter.sh 存在 + 可执行 + 双模式（默认 warn-only / --strict exit 1）
    - scripts/verify_phase40.sh 存在 + 可执行 + 通过两条独立标准（validator 100% + audit-open < 5）
    - `bash scripts/verify_phase40.sh` 退出码 0（Phase 40 验收）
    - 遵守 CONTEXT.md 脚本设计契约：D-10 validate_debug_frontmatter.sh 双模式（默认 warn-only / --strict exit 1）/ D-11 validator 全量覆盖（状态枚举 + 必填字段 + 日期 + slug + skip_audit）/ D-12 仅手动运行不引入 pre-commit/CI / D-16 verify_phase40.sh 验收脚本覆盖 validator 100% + audit-open < 5 两条独立标准
  artifacts:
    - path: scripts/validate_debug_frontmatter.sh
      provides: "双模式 frontmatter 校验脚本"
      contains: "validate_debug_frontmatter"
    - path: scripts/verify_phase40.sh
      provides: "Phase 40 验收脚本"
      contains: "verify_phase40"
    - path: .planning/debug/login-400-bad-request.md
      provides: "标准 YAML frontmatter"
      contains: "^slug:"
    - path: .planning/debug/knowledge-base.md
      provides: "skip_audit 顶层字段"
      contains: "^skip_audit: true"
    - path: .planning/debug/info-point-type-null-import.md
      provides: "resolved/ 风格 frontmatter"
      contains: "^slug:"
  key_links:
    - from: scripts/verify_phase40.sh
      to: scripts/validate_debug_frontmatter.sh
      via: "调用 validator 100% pass 验证"
      pattern: "validate_debug_frontmatter"
    - from: scripts/verify_phase40.sh
      to: gsd-sdk query audit-open
      via: "检查 debug_sessions 计数 < 5"
      pattern: "audit-open"
---

<objective>
按 D-09..D-16 把 6 个 audit 数据质量问题一次性 batch commit 规范化（4 个无 frontmatter 补齐 + 1 个 metadata.* 转 resolved/ + 1 个 apikey-route-path-duplication 状态归一 + knowledge-base 加 skip_audit），并新增 `scripts/validate_debug_frontmatter.sh`（双模式 validator）+ `scripts/verify_phase40.sh`（Phase 40 验收脚本）让审计指标可持续。验收时 `bash scripts/verify_phase40.sh` 必须退出码 0。

Purpose: TECH-04 + TECH-05 — 6 个数据质量缺口清理 + 审计基础设施就位 + 验收闭环。
Output: 1 个 batch docs commit（D-15，涵盖 6 文件 frontmatter 变更）+ 2 个脚本 commit（validator + verify）= 3 commit 落地。`gsd-sdk query audit-open` 中 debug_sessions 计数 < 5。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-tech-debt-cleanup/40-CONTEXT.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

# 锁定决策（D-09..D-16 必须遵守）
- D-09: 统一到 resolved/ 风格（slug/status/trigger/created/updated/session_type）
- D-10: 双模式 validator（默认 warn-only + --strict exit 1）
- D-11: validator 全量覆盖（状态枚举 + 必填字段 + 日期格式 + slug 格式 + skip_audit 识别 + frontmatter 解析错误检测）
- D-12: 仅手动运行（不接 pre-commit / CI）
- D-13: knowledge-base.md 加 skip_audit: true 顶层字段（不嵌套 metadata）
- D-14: apikey-route-path-duplication 改 root_cause_found（Plan 01 已处理）
- D-15: 6 个 frontmatter 规范化用 1 个 batch commit
- D-16: 新增 scripts/verify_phase40.sh 验收脚本

# 6 个 frontmatter 待规范文件
- .planning/debug/login-400-bad-request.md（无 frontmatter）
- .planning/debug/ops-asset-constraint-uni-ops-asset-devicesn-not-exist.md（无 frontmatter）
- .planning/debug/sys-mac-filter-rules-relation-does-not-exist.md（无 frontmatter）
- .planning/debug/knowledge-base.md（无 frontmatter）
- .planning/debug/info-point-type-null-import.md（metadata.* 风格）
- .planning/debug/apikey-route-path-duplication.md（Plan 01 已处理，本 plan 仅校验）

# 现有脚本风格参照
@scripts/check-bundle.sh（bash + set -euo pipefail + exit code 模式）
@scripts/operlog_e2e_verify.sh（双 section + 清晰日志）

# resolved/ 风格 frontmatter 范本
@.planning/debug/resolved/asset-export-404-error.md

# gsd-sdk audit 工具
gsd-sdk query audit-open  # 返回 debug_sessions 计数
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 6 个 frontmatter 规范化（1 个 batch commit，D-15）</name>
  <files>
    - .planning/debug/login-400-bad-request.md
    - .planning/debug/ops-asset-constraint-uni-ops-asset-devicesn-not-exist.md
    - .planning/debug/sys-mac-filter-rules-relation-does-not-exist.md
    - .planning/debug/knowledge-base.md
    - .planning/debug/info-point-type-null-import.md
    - .planning/debug/apikey-route-path-duplication.md
  </files>
  <read_first>
    - .planning/debug/resolved/asset-export-404-error.md（标准 frontmatter 范本）
    - .planning/debug/login-400-bad-request.md（当前无 frontmatter；看症状描述）
    - .planning/debug/ops-asset-constraint-uni-ops-asset-devicesn-not-exist.md（当前无 frontmatter）
    - .planning/debug/sys-mac-filter-rules-relation-does-not-exist.md（当前无 frontmatter；状态已 ✅ Resolved）
    - .planning/debug/knowledge-base.md（当前无 frontmatter；GSD 知识库文件）
    - .planning/debug/info-point-type-null-import.md（当前 metadata.* 风格；状态已 resolved）
    - .planning/debug/apikey-route-path-duplication.md（Plan 01 已切 resolved；本 task 仅校验 6 字段齐全）
  </read_first>
  <action>
按 D-09 + D-13 + D-14 + D-15 一次性处理 6 文件 frontmatter，1 个 batch commit：

**文件 1: .planning/debug/login-400-bad-request.md**
- 在 # 标题前插入标准 YAML frontmatter：
  ```yaml
  ---
  slug: login-400-bad-request
  status: resolved
  trigger: POST /api/v1/system/auth/login 返回 400 错误
  created: 2026-06-16
  updated: 2026-06-25
  session_type: bug
  ---
  ```
- status 字段：阅读正文，若已 Fix Applied 切 resolved；若仍 investigating 保留 investigating 并更新 trigger 描述（参考正文确认）

**文件 2: .planning/debug/ops-asset-constraint-uni-ops-asset-devicesn-not-exist.md**
- 插入标准 frontmatter：
  ```yaml
  ---
  slug: ops-asset-constraint-uni-ops-asset-devicesn-not-exist
  status: resolved
  trigger: GORM AutoMigrate DROP uni_ops_asset_devicesn 不存在导致约束 DROP 失败
  created: 2026-06-15
  updated: 2026-06-25
  session_type: bug
  related:
    - sys-mac-filter-rules-relation-does-not-exist
  ---
  ```

**文件 3: .planning/debug/sys-mac-filter-rules-relation-does-not-exist.md**
- 状态已 ✅ Resolved，插入 frontmatter：
  ```yaml
  ---
  slug: sys-mac-filter-rules-relation-does-not-exist
  status: resolved
  trigger: sys_mac_filter_rules 表不存在 SQLSTATE 42P01
  created: 2026-06-14
  updated: 2026-06-25
  session_type: bug
  ---
  ```

**文件 4: .planning/debug/knowledge-base.md**（D-13：加 skip_audit 顶层字段）
- 在 # 标题前插入：
  ```yaml
  ---
  slug: knowledge-base
  status: resolved
  skip_audit: true
  trigger: GSD Debug Knowledge Base 索引文件（非 debug session）
  created: 2026-06-15
  updated: 2026-06-25
  session_type: meta
  ---
  ```
- skip_audit: true 在顶层（不嵌套 metadata），与 .planning/debug/resolved/* 同级

**文件 5: .planning/debug/info-point-type-null-import.md**（D-09：从 metadata.* 转 resolved/ 风格）
- 移除原 metadata: 嵌套结构
- 在 # 标题前插入：
  ```yaml
  ---
  slug: info-point-type-null-import
  status: resolved
  trigger: 工位信息点导入时 info_point_type 字段为 NULL 导致数据库约束错误
  created: 2026-05-28
  updated: 2026-05-28
  session_type: bug
  ---
  ```

**文件 6: .planning/debug/apikey-route-path-duplication.md**
- Plan 01 已切 status: resolved
- 本 task 仅校验 frontmatter 6 字段齐全（slug/status/trigger/created/updated/session_type），缺则补

**commit**（D-15: 1 个 batch commit）
- 提交 `docs(40): standardize debug frontmatter`
- body 列出 6 个文件路径 + 各文件变更摘要：
  ```
  - login-400-bad-request.md: 补标准 frontmatter
  - ops-asset-constraint-uni-ops-asset-devicesn-not-exist.md: 补标准 frontmatter
  - sys-mac-filter-rules-relation-does-not-exist.md: 补标准 frontmatter
  - knowledge-base.md: 加 skip_audit: true 顶层字段（D-13）
  - info-point-type-null-import.md: metadata.* 转 resolved/ 风格（D-09）
  - apikey-route-path-duplication.md: 校验 6 字段齐全
  ```
  </action>
  <verify>
    <automated>for f in .planning/debug/login-400-bad-request.md .planning/debug/ops-asset-constraint-uni-ops-asset-devicesn-not-exist.md .planning/debug/sys-mac-filter-rules-relation-does-not-exist.md .planning/debug/knowledge-base.md .planning/debug/info-point-type-null-import.md; do grep -c "^slug:" "$f"; done | sort -u</automated>
  </verify>
  <acceptance_criteria>
    - 5 个文件均含 `^slug:` 行（grep -c 输出 1）
    - knowledge-base.md 含 `^skip_audit: true` 顶层
    - info-point-type-null-import.md 不含 `^metadata:` 嵌套
    - 1 个 `docs(40): standardize debug frontmatter` commit 落地
    - body 列出 6 文件路径
  </acceptance_criteria>
  <done>6 个 audit 数据质量问题 1 个 batch commit 落地；4 个文件补 frontmatter + 1 个 skip_audit + 1 个 metadata.* 转 resolved/；审计工具能正确识别。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 新增 scripts/validate_debug_frontmatter.sh（双模式 validator，D-10/D-11）</name>
  <files>
    - scripts/validate_debug_frontmatter.sh
  </files>
  <read_first>
    - scripts/check-bundle.sh（bash 风格：set -euo pipefail + 数组 + exit code）
    - scripts/operlog_e2e_verify.sh（清晰日志 section 划分）
    - .planning/debug/resolved/asset-export-404-error.md（标准 frontmatter 模板）
    - D-11 校验项清单：状态枚举 + 必填字段 + 日期格式 + slug 格式 + skip_audit 识别 + frontmatter 解析错误
  </read_first>
  <action>
按 D-10 + D-11 创建双模式 frontmatter 校验脚本（仅手动运行，D-12）：

**脚本结构**：
```bash
#!/usr/bin/env bash
# scripts/validate_debug_frontmatter.sh
# Phase 40 frontmatter validator (D-10/D-11)
# 模式：
#   默认 (warn-only): 输出 pass rate，扫描所有 .planning/debug/*.md + .planning/debug/resolved/*.md
#   --strict:        任何不合规 exit 1（适配 pre-commit / CI，未来）
# 用法：
#   bash scripts/validate_debug_frontmatter.sh           # warn-only
#   bash scripts/validate_debug_frontmatter.sh --strict  # exit 1 on fail
# 锁定决策：
#   - 状态枚举（D-11）: resolved / root_cause_found / awaiting_human_verify / investigating / verifying / fixed / diagnosed / debug_complete / root_cause_identified / fix_applied
#   - 必填字段: slug / status / trigger / created / updated
#   - skip_audit: true 顶层识别后跳过校验
#   - frontmatter 必须以 --- 开头 + 闭合

set -euo pipefail

STRICT=0
[[ "${1:-}" == "--strict" ]] && STRICT=1

# 状态枚举白名单
VALID_STATUSES='^(resolved|root_cause_found|awaiting_human_verify|investigating|verifying|fixed|diagnosed|debug_complete|root_cause_identified|fix_applied)$'
# slug 格式：小写 + 连字符
SLUG_PATTERN='^[a-z0-9]+(-[a-z0-9]+)*$'
# 日期格式：YYYY-MM-DD
DATE_PATTERN='^[0-9]{4}-[0-9]{2}-[0-9]{2}$'
# 必填字段
REQUIRED_FIELDS=(slug status trigger created updated)

TOTAL=0
PASS=0
WARN=0
SKIP=0
FAIL=0
FAIL_FILES=()

scan_file() {
  local file="$1"
  TOTAL=$((TOTAL + 1))

  # skip_audit 顶层识别后跳过
  if grep -qE "^skip_audit: true" "$file"; then
    SKIP=$((SKIP + 1))
    echo "  [SKIP] $file (skip_audit: true)"
    return
  fi

  # 解析 frontmatter
  local in_fm=0
  local fm_content=""
  local line
  while IFS= read -r line; do
    if [[ "$line" == "---" ]]; then
      if [[ $in_fm -eq 0 ]]; then
        in_fm=1
        continue
      else
        break  # 闭合
      fi
    fi
    if [[ $in_fm -eq 1 ]]; then
      fm_content+="$line"$'\n'
    fi
  done < "$file"

  if [[ -z "$fm_content" ]]; then
    FAIL=$((FAIL + 1))
    FAIL_FILES+=("$file (no frontmatter)")
    echo "  [FAIL] $file (no frontmatter)"
    return
  fi

  # 必填字段检查
  local missing=()
  for field in "${REQUIRED_FIELDS[@]}"; do
    if ! echo "$fm_content" | grep -qE "^${field}:"; then
      missing+=("$field")
    fi
  done
  if [[ ${#missing[@]} -gt 0 ]]; then
    FAIL=$((FAIL + 1))
    FAIL_FILES+=("$file (missing: ${missing[*]})")
    echo "  [FAIL] $file (missing: ${missing[*]})"
    return
  fi

  # 状态枚举
  local status_val
  status_val=$(echo "$fm_content" | grep -E "^status:" | head -1 | sed -E 's/^status:[[:space:]]*//')
  if ! [[ "$status_val" =~ $VALID_STATUSES ]]; then
    FAIL=$((FAIL + 1))
    FAIL_FILES+=("$file (invalid status: $status_val)")
    echo "  [FAIL] $file (invalid status: $status_val)"
    return
  fi

  # slug 格式
  local slug_val
  slug_val=$(echo "$fm_content" | grep -E "^slug:" | head -1 | sed -E 's/^slug:[[:space:]]*//')
  if ! [[ "$slug_val" =~ $SLUG_PATTERN ]]; then
    FAIL=$((FAIL + 1))
    FAIL_FILES+=("$file (invalid slug: $slug_val)")
    echo "  [FAIL] $file (invalid slug: $slug_val)"
    return
  fi

  # 日期格式
  for date_field in created updated; do
    local date_val
    date_val=$(echo "$fm_content" | grep -E "^${date_field}:" | head -1 | sed -E "s/^${date_field}:[[:space:]]*//")
    if ! [[ "$date_val" =~ $DATE_PATTERN ]]; then
      WARN=$((WARN + 1))
      echo "  [WARN] $file (${date_field} not YYYY-MM-DD: $date_val)"
      # date 格式 warn-only（D-11 说"格式验证"，strict 模式才 fail）
      if [[ $STRICT -eq 1 ]]; then
        FAIL=$((FAIL + 1))
        FAIL_FILES+=("$file (${date_field} invalid format)")
      fi
    fi
  done

  PASS=$((PASS + 1))
  echo "  [PASS] $file"
}

echo "== Debug frontmatter scan =="
echo "  mode: $([[ $STRICT -eq 1 ]] && echo 'strict' || echo 'warn-only')"
echo

for f in .planning/debug/*.md .planning/debug/resolved/*.md; do
  [[ -f "$f" ]] || continue
  scan_file "$f"
done

echo
echo "== Summary =="
echo "  total: $TOTAL"
echo "  pass:  $PASS"
echo "  warn:  $WARN"
echo "  skip:  $SKIP"
echo "  fail:  $FAIL"
echo "  pass rate: $(awk "BEGIN{printf \"%.1f\", ($PASS * 100.0) / $TOTAL}")%"

if [[ $FAIL -gt 0 ]]; then
  echo
  echo "FAIL files:"
  for f in "${FAIL_FILES[@]}"; do
    echo "  - $f"
  done
  exit 1
fi

exit 0
```

执行：
1. `chmod +x scripts/validate_debug_frontmatter.sh`
2. 跑 `bash scripts/validate_debug_frontmatter.sh` 验证默认 warn-only 模式输出 pass rate
3. 跑 `bash scripts/validate_debug_frontmatter.sh --strict` 验证 strict 模式对 WARN 也会 fail
4. 确认 pass rate 100%（所有 6 个新规范 + 130/143 resolved 都 PASS）

commit 1：
- 提交 `chore(40): add validate_debug_frontmatter.sh validator`
  </action>
  <verify>
    <automated>bash scripts/validate_debug_frontmatter.sh 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - scripts/validate_debug_frontmatter.sh 存在 + 可执行
    - 默认模式退出码 0 + pass rate 100%
    - --strict 模式遇 WARN 时退出码 1
    - skip_audit 字段被识别（knowledge-base.md 输出 [SKIP]）
  </acceptance_criteria>
  <done>双模式 validator 就位；默认 warn-only 适配 dev 验证；--strict 适配未来 pre-commit / CI（D-12 当前不接）</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 新增 scripts/verify_phase40.sh（Phase 40 验收脚本，D-16）</name>
  <files>
    - scripts/verify_phase40.sh
  </files>
  <read_first>
    - scripts/operlog_e2e_verify.sh（exit code 模式 + 清晰 banner 划分 section）
    - D-16: 验收 2 条独立标准（validator 100% pass + audit-open debug_sessions < 5）
    - Phase 40 success criteria 7 + 8（见 ROADMAP.md）
  </read_first>
  <action>
按 D-16 创建 Phase 40 验收脚本（独立于 validator 本体）：

**脚本结构**：
```bash
#!/usr/bin/env bash
# scripts/verify_phase40.sh
# Phase 40 验收脚本（D-16）
# 两条独立标准：
#   1. scripts/validate_debug_frontmatter.sh 全量扫描通过率 = 100%
#   2. gsd-sdk query audit-open 输出 debug_sessions < 5
# 退出码：
#   0  两条都通过
#   1  任意一条失败
# 用法：bash scripts/verify_phase40.sh

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

cd "$PROJECT_ROOT"

echo "=========================================="
echo "Phase 40 Verification (Tech-Debt Cleanup)"
echo "=========================================="
echo

# ----- 标准 1: validator 100% pass -----
echo "== Standard 1: frontmatter validator 100% pass =="
if bash scripts/validate_debug_frontmatter.sh 2>&1 | tee /tmp/phase40_validator.log; then
  pass_line=$(grep "pass rate:" /tmp/phase40_validator.log | head -1)
  if [[ "$pass_line" == *"100.0%"* ]]; then
    echo "[OK] validator 100% pass"
    STD1=0
  else
    echo "[FAIL] validator pass rate not 100%: $pass_line"
    STD1=1
  fi
else
  echo "[FAIL] validator exited non-zero"
  STD1=1
fi
echo

# ----- 标准 2: audit-open debug_sessions < 5 -----
echo "== Standard 2: audit-open debug_sessions < 5 =="
if ! command -v gsd-sdk >/dev/null 2>&1; then
  echo "[FAIL] gsd-sdk not in PATH"
  STD2=1
else
  audit_json=$(gsd-sdk query audit-open 2>&1)
  debug_count=$(echo "$audit_json" | grep -oE '"debug_sessions"[[:space:]]*:[[:space:]]*[0-9]+' | grep -oE '[0-9]+' | head -1)
  if [[ -z "$debug_count" ]]; then
    echo "[FAIL] cannot parse debug_sessions from audit-open output"
    STD2=1
  elif [[ "$debug_count" -lt 5 ]]; then
    echo "[OK] debug_sessions=$debug_count < 5"
    STD2=0
  else
    echo "[FAIL] debug_sessions=$debug_count >= 5"
    STD2=1
  fi
fi
echo

# ----- 总结 -----
echo "=========================================="
echo "Summary"
echo "=========================================="
if [[ $STD1 -eq 0 ]]; then
  echo "  Standard 1 (validator 100% pass): PASS"
else
  echo "  Standard 1 (validator 100% pass): FAIL"
fi
if [[ $STD2 -eq 0 ]]; then
  echo "  Standard 2 (audit-open < 5):        PASS"
else
  echo "  Standard 2 (audit-open < 5):        FAIL"
fi

if [[ $STD1 -eq 0 && $STD2 -eq 0 ]]; then
  echo
  echo "[ALL PASS] Phase 40 verification SUCCESS"
  exit 0
else
  echo
  echo "[FAILED] Phase 40 verification FAILED"
  exit 1
fi
```

执行：
1. `chmod +x scripts/verify_phase40.sh`
2. 跑 `bash scripts/verify_phase40.sh` 确认两条都通过（exit 0）
3. 临时破坏一条标准（如手工改 knowledge-base.md frontmatter 删 skip_audit），跑 `bash scripts/verify_phase40.sh` 确认 exit 1
4. 恢复 frontmatter 重新跑确认 exit 0

commit 2：
- 提交 `chore(40): add verify_phase40.sh verification script`
  </action>
  <verify>
    <automated>bash scripts/verify_phase40.sh 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify_phase40.sh 存在 + 可执行
    - 跑 `bash scripts/verify_phase40.sh` 退出码 0
    - 输出包含 "Phase 40 verification SUCCESS"
  </acceptance_criteria>
  <done>Phase 40 验收脚本就位；D-16 闭环；可作为 phase 40 收尾的 sanity check</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 终端 → git 仓库 | 3 commit（1 batch docs + 2 scripts）本地顺序完成 |
| 终端 → .planning/debug/*.md | 1 batch commit 触及 6 个 frontmatter 文件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-05 | Tampering | validator 自身被改宽松 | mitigate | validator 落到 scripts/ 独立文件 + commit message 标注 Phase 40 决策来源；strict 模式可被未来 CI 调用 |
| T-40-06 | Repudiation | verify_phase40.sh 被绕过 | mitigate | D-12 强制"仅手动运行"；当前 phase 40 验收必须显式跑此脚本；commit body 注明 D-16 来源 |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | 本 plan 不引入新依赖（bash + grep + sed + awk 全是 POSIX 内建） |
</threat_model>

<verification>
- 1 个 `docs(40): standardize debug frontmatter` commit + 2 个 `chore(40): add *.sh` commit = 3 commit 落地
- 6 个 frontmatter 文件全部合规（slug/status/trigger/created/updated/session_type 齐全）
- knowledge-base.md 含 `skip_audit: true` 顶层
- info-point-type-null-import.md 已转 resolved/ 风格
- scripts/validate_debug_frontmatter.sh 双模式可用
- scripts/verify_phase40.sh 退出码 0
- `gsd-sdk query audit-open` debug_sessions 计数 < 5
</verification>

<success_criteria>
- TECH-04: 6 个 audit 数据质量问题全部规范化（SC #4, #5, #6）
- TECH-05: audit-open debug_sessions < 5（SC #7）+ validator 100% pass（SC #8）
- 3 commit 全部本地完成（D-04）
- 3 commit + 17 commit (Plan 01) + 5 commit (Plan 02) = 25 commit 落地（与 CONTEXT.md D-15 "29 commit" 计数对齐的 25 = code + frontmatter + scripts；剩余 4 = Plan 02 之后可能的反复修复 commit）
</success_criteria>

<output>
Create `.planning/phases/40-tech-debt-cleanup/40-03-SUMMARY.md` when done
</output>
