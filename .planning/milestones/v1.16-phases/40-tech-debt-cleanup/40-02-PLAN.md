---
phase: 40-tech-debt-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 40-01
files_modified:
  - .planning/debug/workstation-device-build-errors.md
  - .planning/debug/workstation-expand-device-load-empty.md
  - .planning/debug/reset-theme-hardcoded-colors.md
  - .planning/debug/user-filter-lost-after-edit.md
  - .planning/debug/vite-hmr-token-blank-page.md
  - xingran-react-frontend/src/components/operations/WorkstationDeviceTable/index.tsx
  - xingran-react-frontend/src/pages/system/user/index.tsx
  - xingran-react-frontend/src/pages/settings/index.tsx
  - xingran-react-frontend/src/lib/api.ts
autonomous: false
requirements:
  - TECH-02
must_haves:
  truths:
    - 5 个 awaiting_human_verify session 在 dev 环境复现 → 验证修复有效 → 标 resolved
    - .planning/debug/<slug>.md frontmatter 全部切为 `status: resolved`，Resolution 章节追加 verification 证据
    - `npm run build` 在所有前端相关 fix 后通过（workstation-device-build-errors 必然通过；其余 4 个为运行时行为，build 通过≠行为正确，需 dev 浏览器手动点击）
    - 遵守 CONTEXT.md 验证深度契约：D-05 build + 手动浏览器验证 / D-06 验证 fail 反复修复到通过不推迟 / D-07 证据追加到 .planning/debug/<slug>.md Resolution 章节 / D-08 dev 环境用现有顺序验证不构建最小化环境
  artifacts:
    - path: .planning/debug/workstation-device-build-errors.md
      provides: "TS 编译错误已修，前端可 build"
      contains: "status: resolved"
    - path: .planning/debug/workstation-expand-device-load-empty.md
      provides: "工位展开子表加载设备成功"
      contains: "status: resolved"
    - path: .planning/debug/reset-theme-hardcoded-colors.md
      provides: "重置主题恢复 admin 默认"
      contains: "status: resolved"
    - path: .planning/debug/user-filter-lost-after-edit.md
      provides: "用户管理编辑后筛选保留"
      contains: "status: resolved"
    - path: .planning/debug/vite-hmr-token-blank-page.md
      provides: "Vite HMR 不再触发 TokenRefreshError"
      contains: "status: resolved"
  key_links:
    - from: xingran-react-frontend/src/components/operations/WorkstationDeviceTable/index.tsx
      to: .planning/debug/workstation-device-build-errors.md
      via: "TS 类型补齐"
      pattern: "useEffect|useState"
    - from: xingran-react-frontend/src/lib/api.ts
      to: .planning/debug/vite-hmr-token-blank-page.md
      via: "HMR 时清空 stale token"
      pattern: "import.meta.hot"
---

<objective>
按 D-05..D-08 把 5 个 `awaiting_human_verify` session 在 dev 环境复现 → 验证修复有效 → 切 frontmatter `status: resolved` → 在 Resolution 章节追加 verification 证据。本 plan 不能仅 commit "标 resolved" 而不验证（CLAUDE.md "Build check before commit" + D-06 反复修复到通过约束）。

Purpose: TECH-02 — 闭环 5 个 dev 验证缺口，让 audit-open 中 awaiting_human_verify 桶清零。
Output: 5 个 fix(40) commit（每 session 一个），每个 commit 之后切 frontmatter；如某 session 验证失败，反复修复到通过才 commit（不推迟到后续 phase）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-tech-debt-cleanup/40-CONTEXT.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

# 锁定决策（D-05..D-08 必须遵守）
- D-05: build + 手动浏览器验证（workstation-device-build-errors 用 npm run build，其余 4 个用 dev 浏览器）
- D-06: 验证 fail → 反复修复到通过（不推迟）
- D-07: 验证证据追加到 .planning/debug/<slug>.md Resolution 章节
- D-08: dev 环境用现有 dev 顺序验证（不构建最小化环境，不跳验证）

# 5 个 awaiting_human_verify session 源文件
- .planning/debug/workstation-device-build-errors.md
- .planning/debug/workstation-expand-device-load-empty.md
- .planning/debug/reset-theme-hardcoded-colors.md
- .planning/debug/user-filter-lost-after-edit.md
- .planning/debug/vite-hmr-token-blank-page.md

# 项目 CLAUDE.md 关键约束
# - "Build check before commit": 每次 commit 前跑 `go build ./...` / `npm run build`
# - "Always ask user before committing"
# - operlog 约定：本 plan 不引入新写 handler，但若 fix 触及写路径需追加 operlog

# 前端 dev 启动顺序
# - 后端：`.\xingran-backend.exe`（参考 CLAUDE.md）
# - 前端：`cd xingran-react-frontend && npm run dev`（http://localhost:4000）
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: workstation-device-build-errors — TypeScript 编译错误修复（build-only 验证）</name>
  <files>
    - xingran-react-frontend/src/components/operations/WorkstationDeviceTable/index.tsx
    - .planning/debug/workstation-device-build-errors.md
  </files>
  <read_first>
    - .planning/debug/workstation-device-build-errors.md（看 trigger + 3 个 TS 错误位置）
    - xingran-react-frontend/src/components/operations/WorkstationDeviceTable/index.tsx（看当前未通过编译的代码）
    - 跑 `cd xingran-react-frontend && npx tsc --noEmit 2>&1 | head -40` 确认 3 个错误点
  </read_first>
  <action>
按 D-05 + D-08 修复该 session：

1. **先复现 build 失败**
   - 跑 `cd xingran-react-frontend && npx tsc --noEmit 2>&1 | tee /tmp/tsc.log`
   - 看到 3 个 TS 错误（如：缺少 import / 类型不匹配 / props 缺失），记录到 plan task log。

2. **逐个修**
   - 按 tsc 错误位置，在 `src/components/operations/WorkstationDeviceTable/index.tsx` 中：
     - 补 import（如缺 `useState` / `useEffect` / `Modal` 等）
     - 修类型注解
     - 补 props 默认值
   - 每改一次跑 `npx tsc --noEmit` 看到错误数下降，最终 0 错误。

3. **跑 build 验证**（D-05 build 验证）
   - 跑 `cd xingran-react-frontend && npm run build` 确认退出码 0。
   - 检查 dist/ 目录有产物。

4. **更新 frontmatter + Resolution 证据**（D-07）
   - 修改 .planning/debug/workstation-device-build-errors.md：`status: awaiting_human_verify` → `status: resolved`。
   - 在 Resolution 章节追加：
     ```
     verification: 2026-06-25 npx tsc --noEmit 退出码 0; npm run build 退出码 0; dist/ 有产物
     files_changed: xingran-react-frontend/src/components/operations/WorkstationDeviceTable/index.tsx
     ```

5. **commit**（D-02 + D-03）
   - 提交 `fix(40): workstation-device-build-errors — WorkstationDeviceTable 3 个 TS 错误修复`（body 引用本 plan）。

按 D-06：若 npm run build 仍 fail，回到步骤 2 反复修直到通过。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - .planning/debug/workstation-device-build-errors.md frontmatter `status: resolved`
    - `npm run build` 退出码 0
    - dist/ 目录有 *.html + assets/ 产物
    - 1 个 fix(40) commit 落地
  </acceptance_criteria>
  <done>TS 编译错误全部修复；`npm run build` 通过；workstation-device-build-errors 转 resolved。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: workstation-expand-device-load-empty — dev 浏览器复现 + 验证 + commit</name>
  <files>
    - .planning/debug/workstation-expand-device-load-empty.md
  </files>
  <read_first>
    - .planning/debug/workstation-expand-device-load-empty.md（看 root cause 2 层：前端缺方法 + 后端 c.Param("id") 漏改）
  </read_first>
  <action>
本 plan 已自动化完成：前序 commit 已修前端 + 后端 + 路由器（参照 commit `8ad0f4a` + `16372f5`）。Task 1 的 npm run build 已通过。等用户在 dev 浏览器复现验证：

1. **启动 dev 环境**（D-08）
   - 终端 A：`.\xingran-backend.exe`（等待 "Server listening on :9000"）
   - 终端 B：`cd xingran-react-frontend && npm run dev`（等待 http://localhost:4000）

2. **用户浏览器操作**（verification steps）
   - 登录系统（admin / admin123）
   - 进入"工位管理"页面
   - 找一个有设备的工位行，点击展开箭头
   - 预期：子表格成功加载设备列表（不再提示"加载设备失败"）
   - 控制台无 TypeError 或 "参数缺失: 工位ID" 400 错误</action>
  <how-to-verify>
    1. dev 环境已起（后端 :9000 + 前端 :4000）
    2. 浏览器操作：登录 → 工位管理 → 展开任一工位行 → 看到设备子表
    3. DevTools Network 标签看到 `GET /api/v1/ops/workstation/{id}/devices` 返回 200
    4. DevTools Console 无 TypeError
    5. 通过验证后，executor 修改 .planning/debug/workstation-expand-device-load-empty.md：
       - frontmatter `status: awaiting_human_verify` → `status: resolved`
       - Resolution 追加 `verification: 2026-06-25 dev 浏览器验证通过，展开工位加载设备成功，无控制台错误`
       - 提交 `fix(40): workstation-expand-device-load-empty — dev 验证通过`
  </how-to-verify>
  <resume-signal>Type "verified" or describe the failure</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: reset-theme-hardcoded-colors — dev 浏览器复现 + 验证 + commit</name>
  <files>
    - .planning/debug/reset-theme-hardcoded-colors.md
  </files>
  <read_first>
    - .planning/debug/reset-theme-hardcoded-colors.md（看 root cause：handleReset 用硬编码 #4F46E5 / #1E293B 而非调 getDefaultThemeConfig）
  </read_first>
  <action>
前序 commit 已改 pages/settings/index.tsx 调 getDefaultThemeConfig。等用户 dev 验证：

1. **启动 dev 环境**（同 Task 2）

2. **用户浏览器操作**
   - 登录系统
   - 进入"个人中心 / 设置"
   - 找到"重置主题"按钮，点击
   - 预期：主题颜色/布局恢复为 admin 在"系统设置 → 默认主题"中配置的值（不再是硬编码 #4F46E5 / #1E293B）
   - 若 admin 未配置过默认值，应回退 light/minimal（与项目 memory 一致）</action>
  <how-to-verify>
    1. admin 在"系统设置 → 默认主题"先配一组 customColors（如 #FF0000 / #00FF00）
    2. 用户进入"个人中心 → 设置"修改主题颜色为别的值
    3. 点击"重置主题"
    4. 预期：颜色恢复为 #FF0000 / #00FF00（admin 配置），非硬编码 #4F46E5
    5. 通过后，executor 改 frontmatter `status: resolved` + Resolution 追加 verification + commit `fix(40): reset-theme-hardcoded-colors — dev 验证通过`
  </how-to-verify>
  <resume-signal>Type "verified" or describe the failure</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: user-filter-lost-after-edit — dev 浏览器复现 + 验证 + commit</name>
  <files>
    - .planning/debug/user-filter-lost-after-edit.md
  </files>
  <read_first>
    - .planning/debug/user-filter-lost-after-edit.md（看 root cause：编辑后 store 未保留 searchParams / status 筛选）
  </read_first>
  <action>
前序 commit 已修 user list 页面在编辑后保留 searchParams。等用户 dev 验证：

1. **启动 dev 环境**

2. **用户浏览器操作**
   - 登录系统
   - 进入"系统管理 → 用户管理"
   - 搜索框输入"test"，状态下拉选"启用"
   - 点击某用户的"编辑"按钮，修改昵称，保存
   - 回到列表页（注意：不应刷新整页）
   - 预期：搜索框仍是"test"，状态仍是"启用"，列表数据仍是筛选后结果
   - 反复操作多次，每次筛选条件持续生效</action>
  <how-to-verify>
    1. dev 环境已起
    2. 按上述操作步骤执行
    3. 列表筛选在编辑后保留
    4. 通过后，executor 改 frontmatter `status: resolved` + Resolution 追加 verification + commit `fix(40): user-filter-lost-after-edit — dev 验证通过`
  </how-to-verify>
  <resume-signal>Type "verified" or describe the failure</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: vite-hmr-token-blank-page — dev 浏览器复现 + 验证 + commit</name>
  <files>
    - .planning/debug/vite-hmr-token-blank-page.md
  </files>
  <read_first>
    - .planning/debug/vite-hmr-token-blank-page.md（看 root cause：HMR 时 store token 丢失 + Refresh 轮询触发死循环）
  </read_first>
  <action>
前序 commit 已修 api.ts TokenManager 在 HMR 时清空 stale token + 避免 RefreshError。等用户 dev 验证：

1. **启动 dev 环境**

2. **用户浏览器操作**
   - 登录系统（获取 accessToken + refreshToken 写入 localStorage）
   - 在 IDE 中修改一个前端组件文件并保存（触发 Vite HMR）
   - 预期：浏览器自动热加载，**不**弹出 "TokenRefreshError: No access token available"
   - 预期：删除 localStorage 的 rt（refreshToken），刷新页面
   - 预期：第一次刷新显示加载中（不卡死），第二次刷新跳登录页（而非一直加载）</action>
  <how-to-verify>
    1. dev 环境已起
    2. 按上述操作步骤执行
    3. HMR 不再触发 TokenRefreshError
    4. 删除 rt 后行为符合预期
    5. 通过后，executor 改 frontmatter `status: resolved` + Resolution 追加 verification + commit `fix(40): vite-hmr-token-blank-page — dev 验证通过`
  </how-to-verify>
  <resume-signal>Type "verified" or describe the failure</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev 浏览器 → 后端 | 用户手动点击验证，所有验证在本地 dev 流程 |
| dev 终端 → git 仓库 | 5 个 fix(40) commit，本地可回滚 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-03 | Repudiation | 用户口头说"通过"但实际未测 | mitigate | 5 个 session 全部用 dev 浏览器手动操作（不可自动化）；D-07 要求把 evidence 写进 .md Resolution |
| T-40-04 | Tampering | 验证失败 commit frontmatter 谎报 resolved | mitigate | D-06 反复修复到通过；本 plan 的 4 个 human-verify checkpoint 阻塞（gate=blocking） |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | 本 plan 不引入新依赖 |
</threat_model>

<verification>
- 5 个 fix(40) commit 存在，subject 包含对应 slug
- 5 个 .planning/debug/<slug>.md frontmatter 均为 `status: resolved`
- 每个 .md 的 Resolution 章节有 verification 时间戳 + 证据描述
- `npm run build` 退出码 0
- `gsd-sdk query audit-open` 中 awaiting_human_verify 桶从 5 降到 0
</verification>

<success_criteria>
- TECH-02: 5 个 awaiting_human_verify session 全部 dev 验证通过 + resolved（SC #2）
- 5 commit 全部本地完成（D-04），尚未 push
</success_criteria>

<output>
Create `.planning/phases/40-tech-debt-cleanup/40-02-SUMMARY.md` when done
</output>
