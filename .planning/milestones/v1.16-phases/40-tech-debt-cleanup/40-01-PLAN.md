---
phase: 40-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/services/addomain/dept_sync_service.go
  - internal/services/addomain/batch_sync_service.go
  - internal/services/addomain/ldap_client.go
  - internal/services/addomain/login_sync.go
  - internal/services/addomain/sync.go
  - internal/services/addomain/user_sync.go
  - internal/services/portcollection/trunk_filter.go
  - internal/services/vdi/vm_sync.go
  - internal/services/vdi/vm_placeholder.go
  - internal/services/operations/infopoint_device_port_service.go
  - internal/services/operations/captcha_rate_limit.go
  - internal/services/system/api_key_service.go
  - internal/services/system/operlog_service.go
  - pkg/middleware/permission.go
  - internal/services/vdi/vm_list_datasope.go
  - internal/core/db/migrations/migration_166_apikey_route_path_fix.go
  - .planning/debug/apikey-route-path-duplication.md
autonomous: false
requirements:
  - TECH-01
  - TECH-03
must_haves:
  truths:
    - 16 root_cause_found debug sessions resolved in code; each .planning/debug/<slug>.md updated to status: resolved
    - apikey-route-path-duplication.md has status: root_cause_found (D-14 normalization) and a new migration 166 fixing menu path
    - "API密钥管理 > 密钥列表" 菜单点击后 URL 为 /system/apikeys (不再重复)
    - 17 atomic fix(40) commits land on local main (one per session slug)
    - 遵守 CONTEXT.md commit 契约：D-01 逐个原子 commit / D-02 commit 格式 `fix(40): <slug> — <主题>` body 引用本 plan / D-03 每 commit 后 `go build ./...` 退出 0 / D-04 本地顺序完成不 push
  artifacts:
    - path: internal/services/addomain/dept_sync_service.go
      provides: "密码解密 + LDAP 绑定"
      contains: "decryptPassword"
    - path: internal/services/addomain/batch_sync_service.go
      provides: "批量同步 context cancel 优化"
      contains: "errgroup"
    - path: internal/services/portcollection/trunk_filter.go
      provides: "trunk port 过滤"
      contains: "trunk"
    - path: internal/services/vdi/vm_sync.go
      provides: "9 台 VM 同步"
      contains: "Processing VM"
    - path: internal/core/db/migrations/migration_166_apikey_route_path_fix.go
      provides: "sys_menu.path 修正"
      contains: "Migrate166ApikeyRoutePathFix"
      exports: ["Migrate166ApikeyRoutePathFix"]
    - path: internal/core/db/database.go
      provides: "注册 migration 166"
      contains: "Migrate166ApikeyRoutePathFix"
    - path: .planning/debug/apikey-route-path-duplication.md
      provides: "frontmatter 状态值归一"
      contains: "status: root_cause_found"
  key_links:
    - from: internal/services/addomain/dept_sync_service.go
      to: .planning/debug/ad-connection-ldap-49-invalid-credentials.md
      via: "fix 落 commit fix(40): ad-connection-ldap-49-invalid-credentials"
      pattern: "decryptPassword"
    - from: internal/core/db/database.go
      to: internal/core/db/migrations/migration_166_apikey_route_path_fix.go
      via: "migrations.Migrate166ApikeyRoutePathFix(d.DB)"
      pattern: "Migrate166ApikeyRoutePathFix"
---

<objective>
把 16 个 `root_cause_found` debug 会话按"一话一 commit"原子提交落地（fix(40): <slug> — <主题>，body 引用本 plan），并把 1 个 `root-cause-found` 异类 `apikey-route-path-duplication` 同步处理为下划线规范值 `root_cause_found` + 新增 migration 166 修正 `sys_menu.path`。本 plan 不写新测试，仅按各 session 的 Resolution/fix 段落执行既有修复方案。

Purpose: TECH-01 + TECH-03 — 让 audit-open 中 16 个 root_cause_found 状态归零，apikey-route-path-duplication 进入"root_cause_found (待 commit)"等价状态。
Output: 17 个原子 fix(40) commit（16 root_cause_found + 1 apikey），每个 commit 后 `go build ./...` / `npm run build` 通过；frontmatter status 已就位（apikey 这一条由本 plan 末尾切换为 resolved）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-tech-debt-cleanup/40-CONTEXT.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

# 锁定决策（D-01..D-04 必须遵守）
- D-01: 逐个原子 commit，每个 debug session 一个 fix(40) commit
- D-02: commit 格式 `fix(40): <slug> — <主题>`，body 引用本 plan
- D-03: 每个 fix commit 后立即跑 `go build ./...` / `npm run build`（CLAUDE.md "Build check before commit"）
- D-04: 本地 17 commit 顺序完成，结束后一次 `git push origin main`

# 16 个 root_cause_found session 源文件（每个 task 的 read_first）
- .planning/debug/ad-batch-sync-performance.md
- .planning/debug/ad-connection-ldap-49-invalid-credentials.md
- .planning/debug/ad-login-deleted-dept-reuse.md
- .planning/debug/ad-sync-500-nul-byte-in-error-msg.md
- .planning/debug/ad-sync-500-on-conflict-duplicate-row.md
- .planning/debug/ad-sync-duplicate-username-softdeleted.md
- .planning/debug/api-key-data-not-displaying.md
- .planning/debug/captcha-rate-limit-not-expiring.md
- .planning/debug/infopoint-device-port-description-fail.md
- .planning/debug/mac-collection-trunk-filter.md
- .planning/debug/operlog-dept-name-null-scan.md
- .planning/debug/permission-control-bypass-network-devices.md
- .planning/debug/role-test-vm-list-403-forbidden.md
- .planning/debug/vdi-vm-sync-missing-vm.md
- .planning/debug/vdi-vm-sync-placeholder.md
- .planning/debug/vm-datascope-userid-not-uuid.md

# 1 个 root-cause-found session 源文件
- .planning/debug/apikey-route-path-duplication.md

# 现有 migration 模式（migration 166 模板）
@internal/core/db/migrations/migration_165_sys_dept_location_alias.go

# 现有 APikey 相关文件
@internal/api/v1/system/api_key_handler.go
@internal/core/db/migrations/migration_085_api_keys.go

# operlog 操作日志规约（如果某个 fix 涉及写 handler，按 CLAUDE.md 操作日志约定追加）
# CLAUDE.md § "操作日志记录约定 (operlog convention) — 强制"
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: AD 域控 6 个 root_cause_found 修复 + commit（5 fix）</name>
  <files>
    - internal/services/addomain/dept_sync_service.go
    - internal/services/addomain/batch_sync_service.go
    - internal/services/addomain/login_sync.go
    - internal/services/addomain/user_sync.go
    - internal/services/addomain/sync.go
    - internal/services/addomain/ldap_client.go
    - .planning/debug/ad-connection-ldap-49-invalid-credentials.md
    - .planning/debug/ad-batch-sync-performance.md
    - .planning/debug/ad-login-deleted-dept-reuse.md
    - .planning/debug/ad-sync-500-nul-byte-in-error-msg.md
    - .planning/debug/ad-sync-500-on-conflict-duplicate-row.md
    - .planning/debug/ad-sync-duplicate-username-softdeleted.md
  </files>
  <read_first>
    - .planning/debug/ad-connection-ldap-49-invalid-credentials.md（看 Resolution 章节："在创建LDAP客户端前添加密码解密"）
    - .planning/debug/ad-batch-sync-performance.md（看 Resolution 章节：errgroup / context cancel 优化）
    - .planning/debug/ad-login-deleted-dept-reuse.md（看 Resolution 章节：检测部门 deleted_at 走新建分支）
    - .planning/debug/ad-sync-500-nul-byte-in-error-msg.md（看 Resolution：NUL 字节过滤）
    - .planning/debug/ad-sync-500-on-conflict-duplicate-row.md（看 Resolution：on conflict 改写或预检）
    - .planning/debug/ad-sync-duplicate-username-softdeleted.md（看 Resolution：软删除恢复去重）
    - internal/services/addomain/dept_sync_service.go（看现状，识别 decryptPassword 缺失点）
    - internal/services/addomain/batch_sync_service.go（看现有 errgroup 用法）
  </read_first>
  <action>
按"一话一 commit"顺序执行 6 个 AD 修复，每改完一个 session 的代码立即 commit（不批量）。每步流程：

**Session 1: ad-connection-ldap-49-invalid-credentials**
- 修改 `internal/services/addomain/dept_sync_service.go` line 44 附近，添加 `adConfig.AdminPassword = decryptPassword(adConfig.AdminPassword)` 后再 `NewLDAPClient(&adConfig)`。
- 跑 `go build ./...`，确认退出码 0。
- 修改 `.planning/debug/ad-connection-ldap-49-invalid-credentials.md`：把 frontmatter `status: root_cause_found` 改为 `status: resolved`，`updated: 2026-06-25` 保留。
- 提交 `fix(40): ad-connection-ldap-49-invalid-credentials — dept_sync_service 添加密码解密`（body 引用本 plan 文件路径 + 该 session slug）。

**Session 2: ad-batch-sync-performance**
- 修改 `internal/services/addomain/batch_sync_service.go`，按 session Resolution 的具体方案（context cancel 传递 / 减少重复查询 / 预取复用）实施。
- 跑 `go build ./...` + `go test ./internal/services/addomain/...`。
- 改 frontmatter `status: resolved`。
- 提交 `fix(40): ad-batch-sync-performance — <主题>`。

**Session 3: ad-login-deleted-dept-reuse**
- 修改 `internal/services/addomain/login_sync.go`（或 Resolution 指明文件），在 `HandleUserLoginAD` 找到 OU 映射后增加 `if dept.DeletedAt != nil { 走新建分支 }` 判定。
- 跑 `go build ./...`。
- 改 frontmatter `status: resolved`。
- 提交 `fix(40): ad-login-deleted-dept-reuse — 登录时检测部门软删除走新建分支`。

**Session 4: ad-sync-500-nul-byte-in-error-msg**
- 修改 `internal/services/addomain/sync.go`（或 Resolution 指明文件），按 session 方案过滤 NUL 字节或加 strip。
- 跑 `go build ./...`。
- 改 frontmatter `status: resolved`。
- 提交 `fix(40): ad-sync-500-nul-byte-in-error-msg — <主题>`。

**Session 5: ad-sync-500-on-conflict-duplicate-row**
- 修改 `internal/services/addomain/sync.go`（按 session Resolution 指示具体函数），用 `ON CONFLICT DO NOTHING` 或 `Where(...).FirstOrCreate` 替代 INSERT。
- 跑 `go build ./...`。
- 改 frontmatter `status: resolved`。
- 提交 `fix(40): ad-sync-500-on-conflict-duplicate-row — <主题>`。

**Session 6: ad-sync-duplicate-username-softdeleted**
- 修改 `internal/services/addomain/user_sync.go`（或 Resolution 指明文件），按方案在恢复软删除用户时按 `username + deleted_at IS NOT NULL` 走 `Unscoped().Updates` 路径避免主键冲突。
- 跑 `go build ./...`。
- 改 frontmatter `status: resolved`。
- 提交 `fix(40): ad-sync-duplicate-username-softdeleted — <主题>`。

按 D-03 严格：每个 commit 之后都跑 `go build ./...`；任何 fix 引入新 handler 写路径，按 CLAUDE.md "操作日志记录约定" 追加 `operlog.Record(...)`。
  </action>
  <verify>
    <automated>git log --oneline -n 6 | grep -cE "^fix\(40\): ad-" </automated>
  </verify>
  <acceptance_criteria>
    - 6 个 fix(40) commit 存在且按上述顺序排列
    - 6 个 .planning/debug/ad-*.md 文件 frontmatter 均为 `status: resolved`
    - `go build ./...` 在每个 commit 后退出码 0
    - `go test ./internal/services/addomain/...` 全部通过
  </acceptance_criteria>
  <done>6 个 AD 域控 session 全部转 resolved，6 个 fix(40) commit 落地；user-visible bug 修复代码就位；frontmatter 与代码同步。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: VDI / 采集 / 前端 / 中间件 / operlog / 系统 9 个 root_cause_found 修复 + commit（9 fix）</name>
  <files>
    - internal/services/vdi/vm_sync.go
    - internal/services/vdi/vm_placeholder.go
    - internal/services/vdi/vm_list_datasope.go
    - internal/services/portcollection/trunk_filter.go
    - internal/services/operations/infopoint_device_port_service.go
    - internal/services/operations/captcha_rate_limit.go
    - internal/services/system/api_key_service.go
    - internal/services/system/operlog_service.go
    - pkg/middleware/permission.go
    - .planning/debug/vdi-vm-sync-missing-vm.md
    - .planning/debug/vdi-vm-sync-placeholder.md
    - .planning/debug/vm-datascope-userid-not-uuid.md
    - .planning/debug/mac-collection-trunk-filter.md
    - .planning/debug/infopoint-device-port-description-fail.md
    - .planning/debug/captcha-rate-limit-not-expiring.md
    - .planning/debug/api-key-data-not-displaying.md
    - .planning/debug/operlog-dept-name-null-scan.md
    - .planning/debug/permission-control-bypass-network-devices.md
    - .planning/debug/role-test-vm-list-403-forbidden.md
  </files>
  <read_first>
    - .planning/debug/vdi-vm-sync-missing-vm.md（Resolution：补回 VM 列表解析循环）
    - .planning/debug/vdi-vm-sync-placeholder.md（Resolution：占位 VM 状态校正）
    - .planning/debug/vm-datascope-userid-not-uuid.md（Resolution：datascope 强转 UUID 校验）
    - .planning/debug/mac-collection-trunk-filter.md（Resolution：trunk port 在过滤白名单）
    - .planning/debug/infopoint-device-port-description-fail.md（Resolution：port.description 字段映射）
    - .planning/debug/captcha-rate-limit-not-expiring.md（Resolution：Redis TTL 改用 SetNX/Expire）
    - .planning/debug/api-key-data-not-displaying.md（Resolution：响应加密中间件放行 apikeys 路径）
    - .planning/debug/operlog-dept-name-null-scan.md（Resolution：sys_oper_log dept_name 字段空指针兜底）
    - .planning/debug/permission-control-bypass-network-devices.md（Resolution：pkg/middleware/permission.go 缺失 case）
    - .planning/debug/role-test-vm-list-403-forbidden.md（Resolution：datascope SQL 中 user_id 类型转换）
  </read_first>
  <action>
按"一话一 commit"顺序执行 9 个非 AD 修复，每改完一个 session 的代码立即 commit：

**Session 7: vdi-vm-sync-missing-vm** — 修改 `internal/services/vdi/vm_sync.go`，在 `Processing resource:` 后补回 `for _, vm := range apiResp.Data { ... }` 解析循环（被删的 add 功能恢复）。
**Session 8: vdi-vm-sync-placeholder** — 修改 `internal/services/vdi/vm_placeholder.go`，按 Resolution 把 placeholder VM 状态字段校正。
**Session 9: vm-datascope-userid-not-uuid** — 修改 `internal/services/vdi/vm_list_datasope.go`，按 Resolution 加 UUID 强转校验或预过滤。
**Session 10: mac-collection-trunk-filter** — 修改 `internal/services/portcollection/trunk_filter.go`，按 Resolution 把 trunk port 从采集过滤中排除。
**Session 11: infopoint-device-port-description-fail** — 修改 `internal/services/operations/infopoint_device_port_service.go`，按 Resolution 把 port.description 字段写入映射。
**Session 12: captcha-rate-limit-not-expiring** — 修改 `internal/services/operations/captcha_rate_limit.go`，按 Resolution 改用 `cache.Set(ctx, key, v, ttl)` 显式 TTL。
**Session 13: api-key-data-not-displaying** — 修改 `internal/services/system/api_key_service.go`（或中间件白名单），按 Resolution 把 `/system/apikeys/*` 加到响应加密 exclude_paths。
**Session 14: operlog-dept-name-null-scan** — 修改 `internal/services/system/operlog_service.go`，按 Resolution 加 `if deptName == nil { deptName = "" }` 兜底。
**Session 15: permission-control-bypass-network-devices** — 修改 `pkg/middleware/permission.go`，按 Resolution 加缺失的 `network:device:*` 权限 case 匹配。

每步流程：
1. 修改源文件
2. `go build ./...` 退出码 0
3. 修改 `.planning/debug/<slug>.md` frontmatter `status: root_cause_found` → `status: resolved`
4. 提交 `fix(40): <slug> — <主题>`（body 引用本 plan）
5. 若新加 handler 写路径，按 CLAUDE.md operlog 约定追加 `operlog.Record(...)`

**Session 16: role-test-vm-list-403-forbidden** — 修改 datascope SQL，按 Resolution 把 user_id 强转 UUID 或前置校验。
- 跑 `go build ./...`。
- 改 frontmatter `status: resolved`。
- 提交 `fix(40): role-test-vm-list-403-forbidden — <主题>`。
  </action>
  <verify>
    <automated>git log --oneline -n 9 | grep -cE "^fix\(40\): (vdi-|vm-|mac-|infopoint-|captcha-|api-key-|operlog-|permission-|role-)"</automated>
  </verify>
  <acceptance_criteria>
    - 9 个 fix(40) commit 存在（VDI 2 + vm-datascope + mac + infopoint + captcha + apikey-data + operlog + permission + role-test-vm）
    - 9 个 .planning/debug/<slug>.md frontmatter 均为 `status: resolved`
    - `go build ./...` 退出码 0
  </acceptance_criteria>
  <done>9 个非 AD root_cause_found session 全部转 resolved，9 个 fix(40) commit 落地；VDI 同步、MAC 采集、验证码、API 密钥、operlog、网络设备权限、角色数据范围均按各自 Resolution 修复。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: apikey-route-path-duplication 异类归一 + migration 166 + commit（TECH-03 第 17 个 fix）</name>
  <files>
    - internal/core/db/migrations/migration_166_apikey_route_path_fix.go
    - internal/core/db/database.go
    - .planning/debug/apikey-route-path-duplication.md
  </files>
  <read_first>
    - .planning/debug/apikey-route-path-duplication.md（看 Resolution：`sys_menu.path` 修正为相对路径）
    - internal/core/db/migrations/migration_165_sys_dept_location_alias.go（migration 模板风格）
    - internal/core/db/database.go（line 388-410 找到 migration 注册块追加新行）
    - internal/api/router.go 路由前缀确认（"API密钥管理"组挂载位置）
  </read_first>
  <action>
按 D-14 归一状态值 + Resolution 落 migration 166 修复菜单 path：

1. **修改 frontmatter**（D-14：连字符变下划线）
   - 在 .planning/debug/apikey-route-path-duplication.md 把 `status: root-cause-found` 改为 `status: root_cause_found`。
   - 注意：这步会立即让 audit-open 把该条从 `root-cause-found` 计数移到 `root_cause_found` 桶（debug_sessions 总数不变，但状态分布正确）。

2. **新增 migration 166**
   - 创建 `internal/core/db/migrations/migration_166_apikey_route_path_fix.go`，模仿 migration_165 风格。
   - 函数签名：`func Migrate166ApikeyRoutePathFix(db *gorm.DB) error`。
   - SQL 内容：UPDATE `sys_menu` SET `path` = 'apikeys' WHERE `menu_name` = 'API密钥管理' AND `path` IS NULL OR `path` = 'system/apikeys'; 然后 UPDATE 子菜单 `密钥列表` `path` = 'list' WHERE `parent_id` = (上述菜单 id) AND `path` = 'system/apikeys'。
   - 注意：避免一次性 UPDATE 覆盖其他菜单的合法 path；用 `menu_name` 锁定。
   - 加 `log.Println("Running migration 166: apikey route path fix")` 与完成日志。

3. **注册 migration**
   - 打开 `internal/core/db/database.go` line 388-410 区域，找到 `Migrate165SysDeptLocationAlias(d.DB)` 之后追加 `if err := migrations.Migrate166ApikeyRoutePathFix(d.DB); err != nil { log.Printf(...) }`。
   - 跑 `go build ./...` 确认编译通过。

4. **本地干跑（无 AD/DB 依赖）**
   - 跑 `go vet ./...`。
   - 跑 `go test ./internal/core/db/migrations/... -run TestMigration166`（如果存在）；不存在则跳过。

5. **完成 frontmatter 闭环**
   - 跑完 build 通过后，把 `.planning/debug/apikey-route-path-duplication.md` 的 `status` 改为 `status: resolved`，`updated: 2026-06-25`。
   - 在 Resolution 章节追加一行 `verification: migration 166 准备就绪，启动后端时自动执行` + `files_changed: internal/core/db/migrations/migration_166_apikey_route_path_fix.go, internal/core/db/database.go, .planning/debug/apikey-route-path-duplication.md`。

6. **commit**
   - 提交 `fix(40): apikey-route-path-duplication — migration 166 修正 sys_menu.path 重复拼接`（body 引用本 plan）。
   - 跑 `go build ./...` 确认最终退出码 0。
  </action>
  <verify>
    <automated>git log --oneline -n 1 | grep -E "apikey-route-path-duplication" && grep -c "Migrate166ApikeyRoutePathFix" internal/core/db/database.go</automated>
  </verify>
  <acceptance_criteria>
    - 1 个 fix(40) commit 存在，subject 包含 `apikey-route-path-duplication`
    - .planning/debug/apikey-route-path-duplication.md frontmatter `status: resolved`
    - internal/core/db/migrations/migration_166_apikey_route_path_fix.go 存在，函数 `Migrate166ApikeyRoutePathFix` 可导出
    - internal/core/db/database.go 包含 `Migrate166ApikeyRoutePathFix` 调用 1 次
    - `go build ./...` 退出码 0
  </acceptance_criteria>
  <done>TECH-03 完成：apikey-route-path-duplication 既归一了状态值（root-cause-found → root_cause_found → resolved），又落了 migration 166 实修；本 plan 累计 17 个 fix(40) commit 全部落地。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 17 commit 落点 + 状态归零 human-verify</name>
  <files>
    - .planning/debug/ad-batch-sync-performance.md
    - .planning/debug/ad-connection-ldap-49-invalid-credentials.md
    - .planning/debug/ad-login-deleted-dept-reuse.md
    - .planning/debug/ad-sync-500-nul-byte-in-error-msg.md
    - .planning/debug/ad-sync-500-on-conflict-duplicate-row.md
    - .planning/debug/ad-sync-duplicate-username-softdeleted.md
    - .planning/debug/api-key-data-not-displaying.md
    - .planning/debug/captcha-rate-limit-not-expiring.md
    - .planning/debug/infopoint-device-port-description-fail.md
    - .planning/debug/mac-collection-trunk-filter.md
    - .planning/debug/operlog-dept-name-null-scan.md
    - .planning/debug/permission-control-bypass-network-devices.md
    - .planning/debug/role-test-vm-list-403-forbidden.md
    - .planning/debug/vdi-vm-sync-missing-vm.md
    - .planning/debug/vdi-vm-sync-placeholder.md
    - .planning/debug/vm-datascope-userid-not-uuid.md
    - .planning/debug/apikey-route-path-duplication.md
  </files>
  <action>本 plan 已自动化完成所有代码修改 + commit，frontmatter 状态切换 + 17 个 fix(40) commit。等用户做最终 sanity check：</action>
  <how-to-verify>
    1. 跑 `git log --oneline -n 17` 看到 17 个 fix(40) commit 按顺序排列。
    2. 跑 `gsd-sdk query audit-open | grep '"debug_sessions"'` 看 debug_sessions 计数下降（应从 57 减 17 = 40；其中 root_cause_found / root-cause-found 桶已清空）。
    3. 跑 `for f in .planning/debug/ad-batch-sync-performance.md .planning/debug/ad-connection-ldap-49-invalid-credentials.md .planning/debug/ad-login-deleted-dept-reuse.md .planning/debug/ad-sync-500-nul-byte-in-error-msg.md .planning/debug/ad-sync-500-on-conflict-duplicate-row.md .planning/debug/ad-sync-duplicate-username-softdeleted.md .planning/debug/api-key-data-not-displaying.md .planning/debug/captcha-rate-limit-not-expiring.md .planning/debug/infopoint-device-port-description-fail.md .planning/debug/mac-collection-trunk-filter.md .planning/debug/operlog-dept-name-null-scan.md .planning/debug/permission-control-bypass-network-devices.md .planning/debug/role-test-vm-list-403-forbidden.md .planning/debug/vdi-vm-sync-missing-vm.md .planning/debug/vdi-vm-sync-placeholder.md .planning/debug/vm-datascope-userid-not-uuid.md .planning/debug/apikey-route-path-duplication.md; do grep -E "^status:" "$f"; done` 输出全部为 `status: resolved`。
    4. 跑 `go build ./...` 退出码 0。
  </how-to-verify>
  <resume-signal>Type "approved" or describe any commit/状态不一致问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev 终端 → git 仓库 | 用户手动执行 17 次 commit，所有变更本地可回滚（D-04） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering | 本地 commit 历史被破坏 | mitigate | 每 commit 独立原子；用户可 `git reset` 撤回单个 fix；本 plan 不 force push |
| T-40-SC | Tampering | 包管理 / 第三方依赖 | accept | 本 plan 不引入新依赖；所有 fix 仅修改既有文件 |
| T-40-02 | Elevation of Privilege | 修复过程中 operlog 漏记录 | mitigate | CLAUDE.md operlog 约定：本 plan 引入的新写 handler 全部追加 `operlog.Record(...)`；既有 handler 由各 session 既有修复决定 |
</threat_model>

<verification>
- 17 个 fix(40) commit 按顺序存在于本地 git log
- 16 个 root_cause_found + 1 个 apikey-route-path-duplication 共 17 个 .planning/debug/<slug>.md frontmatter 均为 `status: resolved`
- `go build ./...` 退出码 0
- `gsd-sdk query audit-open` 中 root_cause_found 桶（连字符 + 下划线）合计计数从 17 降到 0
- migration_166 文件存在 + database.go 注册就位
</verification>

<success_criteria>
- TECH-01: 16 个 root_cause_found session 全部转 resolved 并 commit（SC #1, #3 部分）
- TECH-03: apikey-route-path-duplication 修复 + status 归一为 resolved（SC #3）
- 17 commit 全部本地完成（D-04），尚未 push
</success_criteria>

<output>
Create `.planning/phases/40-tech-debt-cleanup/40-01-SUMMARY.md` when done
</output>
