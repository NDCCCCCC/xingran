---
phase: 39-workstation-dept-location-alias
plan: 04
type: execute
wave: 1
depends_on: ["39-02"]
files_modified:
  - internal/api/router.go
autonomous: true
requirements:
  - REQ-39-03
  - REQ-39-09
  - REQ-39-10
  - REQ-39-12
must_haves:
  truths:
    - 4 个 alias 端点 (POST /list/create/:id/update/:id/delete) 注册到 /api/v1/ops/location-alias 路由组
    - 4 个端点受独立权限 middleware 保护 (ops:location:alias:list/add/edit/delete)
    - 写操作成功后调 dept cache invalidation (D-03 决策)
    - operlog 模块名 "工位管理" + 25 OperType 常量集严格对齐
  artifacts:
    - path: internal/api/router.go
      provides: "SetupLocationAliasRouter 注册块 + 4 路由"
      contains: "/location-alias"
  key_links:
    - from: internal/api/router.go
      to: internal/api/v1/operations/location_alias_handler.go
      via: "SetupLocationAliasRouter(opsGroup, core)"
      pattern: "SetupLocationAliasRouter"
    - from: internal/api/router.go
      to: pkg/middleware/permission.go
      via: "RequirePermissionsWithQuery 中间件"
      pattern: "RequirePermissions"
    - from: internal/api/router.go
      to: internal/services/system/department_cache_impl.go
      via: "core.DeptService.InvalidateDeptCache(ctx) 调用"
      pattern: "core.DeptService.InvalidateDeptCache"
---

<objective>
注册 alias 4 端点到 `internal/api/router.go`, 加权限中间件, 写操作触发 dept cache invalidation。完成 Phase 39 后端 API 层的最后一步。

Purpose: 让 alias CRUD 在路由层可被前端调用, 权限粒度与 4 条 perms 对齐, 写操作副作用触发 dept 缓存失效使前端 useDeptTree 自动刷新。
Output: router.go 中新增 location-alias 路由组 + 4 路由 + 权限 middleware + 缓存失效调用(在 handler 层)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-workstation-dept-location-alias/39-SPEC.md
@.planning/phases/39-workstation-dept-location-alias/39-CONTEXT.md
@.planning/phases/39-workstation-dept-location-alias/39-02-PLAN.md (alias handler 输出)

# Backend canonical references
@internal/api/router.go:579-604 (现有 workstation 路由组样板)
@internal/api/router.go:235-245 (apikey 路由组样板 — 用 4 perms 模式)
@internal/api/router.go:582-587 (RequirePermissionsWithQuery 用法样板)
@internal/services/system/department_cache_impl.go:104-111 (InvalidateDeptCache 实现)
@internal/core/core.go:52, 662 (core.DeptService 字段 + 初始化)
@internal/api/v1/operations/location_alias_handler.go (Plan 39-02 产出)
@internal/utils/operlog/operlog.go (OperType 常量 + Record 调用样板)
</context>

<tasks>

<task type="auto">
  <name>Task 1: SetupLocationAliasRouter 注册块</name>
  <files>internal/api/router.go</files>
  <read_first>
    - internal/api/router.go:579-604 (workstations 路由组 — 4 perms + 路由排列样板)
    - internal/api/router.go:235-245 (apikey 路由组 — 4 perms 模式最简样板)
    - internal/api/router.go:580-604 (workstations 用 RequirePermissionsWithQuery + opsGroup 风格)
    - internal/api/v1/operations/location_alias_handler.go (Plan 39-02 产出的 handler 方法)
  </read_first>
  <action>
在 `internal/api/router.go` 中, 紧接 workstations 路由组 (line 600-604) 之后、workstationDevices 路由组 (line 606-...) 之前, 插入:

```go
// 工位部门物理位置映射 (Phase 39)
locationAlias := ops.Group("/location-alias")
locationAlias.Use(middleware.RequirePermissions([]string{
    "ops:location:alias:list",
    "ops:location:alias:add",
    "ops:location:alias:edit",
    "ops:location:alias:delete",
}, core))
{
    locationAliasService := opsServices.NewLocationAliasService(core.DB.GetDB())
    locationAliasHandler := operations.NewLocationAliasHandler(locationAliasService).WithCore(core)

    // 注意:更具体的路由放在前面,避免被通用路由匹配
    locationAlias.POST("/list", locationAliasHandler.List)
    locationAlias.POST("/:id/update", locationAliasHandler.Update)
    locationAlias.POST("/:id/delete", locationAliasHandler.Delete)
    locationAlias.POST("", locationAliasHandler.Create) // 放在最后,作为兜底路由
}
```

**关键约束**:
- **不** 使用 `RequirePermissionsWithQuery` (alias 没有"只读路径放宽"的诉求, 4 perms 严格)
- 路由顺序严格: `/list` `/update` `/delete` 在前, `""` 在最后(与 workstation 路由排列风格一致,避免 `:id` 兜底匹配)
- 路径: `/ops/location-alias/...` (沿用 ops group 已有的 base prefix, 不要重复 `/ops/`)
- 权限字符串字面量与 Plan 39-01 migration_165 seed 一致: `ops:location:alias:list/add/edit/delete`
- handler 注入 `WithCore(core)` 与 workstation 路由组一致(operlog 需要)
- service 注入 `core.DB.GetDB()` 与 workstation 路由组一致
- 不修改 ops group 外任何代码
- 不修改其他路由组
- 不引入新 middleware
  </action>
  <verify>
    <automated>cd D:\CODE\ClaudeCode\xingran-go-backend && go build ./... && grep -n "location-alias\|locationAlias" internal/api/router.go | head -20</automated>
  </verify>
  <done>
    - `internal/api/router.go` 包含 `locationAlias := ops.Group("/location-alias")` 行
    - 4 条路由注册 (`/list` `/update` `/delete` `""`)
    - `middleware.RequirePermissions([]string{"ops:location:alias:list", "ops:location:alias:add", "ops:location:alias:edit", "ops:location:alias:delete"}, core)` middleware 挂载
    - `go build ./...` 退出码 0
    - 现有路由组( workstations / workstationDevices / buildings / floors 等) 0 修改
  </done>
</task>

<task type="auto">
  <name>Task 2: Cache invalidation — alias 写操作后调 DeptService.InvalidateDeptCache</name>
  <files>internal/api/v1/operations/location_alias_handler.go</files>
  <read_first>
    - internal/services/system/department_cache_impl.go:104-111 (InvalidateDeptCache 实现 — 单参 ctx)
    - internal/api/v1/operations/workstation_handler.go:55-67 (Create handler 样板 — success path 末尾 operlog.Record 之前)
    - internal/api/v1/operations/location_alias_handler.go (Plan 39-02 产出的 4 handler 方法, 当前缺 cache 失效)
    - internal/core/core.go:52, 662 (core.DeptService 字段)
  </read_first>
  <action>
修改 `internal/api/v1/operations/location_alias_handler.go` 的 3 个写方法 (Create/Update/Delete), 在 service 调用成功之后、operlog.Record 之前, 注入:

```go
// 触发 dept 缓存失效 (D-03 决策 — alias 变更影响工位部门下拉 union 结果)
if h.core != nil && h.core.DeptService != nil {
    if cacheErr := h.core.DeptService.InvalidateDeptCache(c.Request.Context()); cacheErr != nil {
        applogger.Warnf("location alias 写操作后失效 dept 缓存失败: %v", cacheErr)
        // 不阻断响应 — 缓存失效失败不影响业务数据落地
    }
}
```

**插入位置** (以 Create 为例):
```go
func (h *LocationAliasHandler) Create(c *gin.Context) {
    var req opsServices.LocationAliasCreateRequest
    if !handleJSONBinding(c, &req) {
        return
    }
    alias, err := h.service.Create(c.Request.Context(), &req)
    if !handleServiceError(c, err, "创建") {
        return
    }
    // NEW: cache invalidation (D-03)
    if h.core != nil && h.core.DeptService != nil {
        if cacheErr := h.core.DeptService.InvalidateDeptCache(c.Request.Context()); cacheErr != nil {
            applogger.Warnf("location alias 写操作后失效 dept 缓存失败: %v", cacheErr)
        }
    }
    operlog.Record(c, h.core.OperLogService, h.core.GetDB(), "工位管理", operlog.OperTypeCreate)
    response.Success(c, alias)
}
```

Update/Delete 同款模式, 在各自 service 调用成功后插入。

**import 调整**:
- 在文件顶部新增 `"github.com/xingran-next/xingran-go-backend/pkg/logger"` import(或 `applogger "github.com/xingran-next/xingran-go-backend/pkg/logger"` 沿用项目别名)
- 校验 h.core.DeptService 非 nil (用 `if h.core != nil && h.core.DeptService != nil` 守护)
- 缓存失效失败只 log warn 不阻断 (与 Phase 28 cache_invalidator.go 风格一致)
- 不修改 List (读操作无需失效缓存)
- 不引入新 import (logger 包项目已用)

**REQ-39-12 验证**:
- operlog.Record 第 5 参数为 `operlog.OperTypeCreate/Update/Delete` 三个常量(值 1/2/3), 已被 regression_test.go 守护
- 不引用 OperTypeOther (0) / OperTypeStatus (10) 等其他常量(保持语义清晰)
  </action>
  <verify>
    <automated>cd D:\CODE\ClaudeCode\xingran-go-backend && go build ./... && go test ./internal/utils/operlog/ -run "TestOperTypeCountEquals25|TestOperTypeConstantStability" -v -count=1</automated>
  </verify>
  <done>
    - `internal/api/v1/operations/location_alias_handler.go` 3 个写方法 (Create/Update/Delete) 内含 `h.core.DeptService.InvalidateDeptCache(...)` 调用
    - 失效调用位于 service 操作成功后、operlog.Record 之前
    - `applogger.Warnf` 用于日志(无需 error 级别)
    - `go build ./...` 退出码 0
    - operlog regression test 全绿 (25 OperType 常量值稳定)
    - List 读方法 0 修改(不触发 cache 失效)
  </done>
</task>

</tasks>

<verification>
- `go build ./...` 退出码 0
- `go vet ./...` 0 警告
- `go test ./internal/utils/operlog/...` 全绿(含 regression_test.go)
- `grep -n "ops:location:alias" internal/api/router.go` 显示 4 条权限字符串字面量
- `grep -n "OperTypeCreate\|OperTypeUpdate\|OperTypeDelete" internal/api/v1/operations/location_alias_handler.go` 显示 3 处引用
</verification>

<success_criteria>
- 4 个 alias 端点注册完成, 受权限中间件保护
- 写操作副作用触发 dept cache invalidation
- operlog 模块名严格 "工位管理", 业务类型用 OperTypeCreate/Update/Delete (25 OperType 常量值不变)
- 不修改任何现有路由(只插入新块)
- 不引入新三方依赖
</success_criteria>

<output>
Create `.planning/phases/39-workstation-dept-location-alias/39-04-SUMMARY.md` when done
</output>