---
phase: 39-workstation-dept-location-alias
plan: 07
type: execute
wave: 3
depends_on: ["39-06"]
files_modified:
  - xingran-react-frontend/src/pages/operations/workstations/LocationAliasDrawer.tsx
  - xingran-react-frontend/src/pages/operations/workstations/index.tsx
autonomous: true
requirements:
  - REQ-39-06
  - REQ-39-08
must_haves:
  truths:
    - 工位列表页工具栏渲染 [⚙ 映射] 按钮, 仅 ops:location:alias:list 权限下可见
    - 点击按钮打开 Drawer, 展示 alias 列表(dept 名 / location 名 / scope / 创建时间 / 操作列)
    - Drawer 内新增表单含 2 个 TreeSelect (dept 全量 + location 仅外部机构) + scope 隐藏字段 + remark 文本框
    - 无 ops:location:alias:add 权限时新增按钮 disabled
    - 无 ops:location:alias:delete 权限时删除按钮 hidden
    - 写操作成功后调 useInvalidateDept + invalidate locationAlias (双失效)
  artifacts:
    - path: xingran-react-frontend/src/pages/operations/workstations/LocationAliasDrawer.tsx
      provides: "LocationAliasDrawer 组件: 列表 + 新增表单 + 删除"
      exports: ["LocationAliasDrawer"]
    - path: xingran-react-frontend/src/pages/operations/workstations/index.tsx
      provides: "[⚙ 映射] 按钮 + Drawer state + 失效联动"
      contains: "LocationAliasDrawer"
  key_links:
    - from: xingran-react-frontend/src/pages/operations/workstations/index.tsx
      to: xingran-react-frontend/src/pages/operations/workstations/LocationAliasDrawer.tsx
      via: "component import + render"
      pattern: "LocationAliasDrawer"
    - from: xingran-react-frontend/src/pages/operations/workstations/LocationAliasDrawer.tsx
      to: xingran-react-frontend/src/lib/opsApi.ts
      via: "locationAliasApi.list/create/delete"
      pattern: "locationAliasApi"
    - from: xingran-react-frontend/src/pages/operations/workstations/LocationAliasDrawer.tsx
      to: xingran-react-frontend/src/hooks/useDeptTree.ts
      via: "useDeptTree 全量部门树 + filterExternalOrgDepts 仅外部机构"
      pattern: "useDeptTree"
    - from: xingran-react-frontend/src/pages/operations/workstations/LocationAliasDrawer.tsx
      to: xingran-react-frontend/src/utils/authHelpers.ts
      via: "hasPermission 权限 gating"
      pattern: "hasPermission"
---

<objective>
为工位管理页 list 工具栏添加 `[⚙ 映射]` 按钮 + Drawer 管理 UI, 承载 alias 完整 CRUD (D-02/D-08 决策)。

Purpose: 用户在工位页就能直接管理 alias 映射, 不必切换到独立菜单(本 phase 不新增菜单项)。Drawer 内表单采用 2 个独立 TreeSelect + 三级校验前后端双重落地。权限 gating 严格对齐 D-08 决策。
Output: LocationAliasDrawer 组件 + index.tsx 按钮注入 + 失效联动。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-workstation-dept-location-alias/39-SPEC.md
@.planning/phases/39-workstation-dept-location-alias/39-CONTEXT.md

# Frontend canonical references
@xingran-react-frontend/src/pages/operations/workstations/index.tsx (工具栏 + 列表区 — 找工具栏按钮注入点)
@xingran-react-frontend/src/components/modal/BaseEditModal.tsx (Drawer/Modal 样式参考)
@xingran-react-frontend/src/utils/authHelpers.ts (hasPermission 用法样板)
@xingran-react-frontend/src/hooks/useDeptTree.ts (deptTreeData + useInvalidateDept)
@xingran-react-frontend/src/utils/deptUtils.ts (filterExternalOrgDepts + trimTitleToLastSegment)
@xingran-react-frontend/src/lib/opsApi.ts (locationAliasApi — Plan 39-05 产出)
</context>

<tasks>

<task type="auto">
  <name>Task 1: LocationAliasDrawer 组件实现</name>
  <files>xingran-react-frontend/src/pages/operations/workstations/LocationAliasDrawer.tsx</files>
  <read_first>
    - xingran-react-frontend/src/pages/operations/workstations/index.tsx (工具栏位置 + state 管理风格)
    - xingran-react-frontend/src/components/modal/BaseEditModal.tsx (Drawer 模式)
    - xingran-react-frontend/src/utils/authHelpers.ts (hasPermission 函数签名)
    - xingran-react-frontend/src/hooks/useDeptTree.ts:27-39 (useDeptTree 签名)
    - xingran-react-frontend/src/utils/deptUtils.ts (filterExternalOrgDepts + trimTitleToLastSegment 签名)
    - xingran-react-frontend/src/lib/opsApi.ts:locationAliasApi (Plan 39-05 产出)
  </read_first>
  <action>
新建 `xingran-react-frontend/src/pages/operations/workstations/LocationAliasDrawer.tsx`:

**1. imports 区域** (React Query + antd + 已有 utils):
- antd: `Drawer, Form, TreeSelect, Button, Table, Space, Popconfirm, Input, App, Tooltip`
- icons: `PlusOutlined, DeleteOutlined, SettingOutlined`
- react: `useState`
- tanstack: `useQuery, useQueryClient`
- opsApi: `locationAliasApi, type LocationAlias`
- hooks: `useDeptTree, useInvalidateDept`
- deptUtils: `filterExternalOrgDepts, trimTitleToLastSegment`
- authHelpers: `hasPermission`
- queryKeys: `queryKeys`
- errorHandler: `handleApiError, handleSuccess`

**2. props 接口**:
```typescript
export interface LocationAliasDrawerProps {
  open: boolean;
  onClose: () => void;
}
```

**3. 内部 state**:
- `const [form] = Form.useForm();`
- `const [pageNum, setPageNum] = useState(1);`
- `const [pageSize, setPageSize] = useState(10);`
- `const [showAddForm, setShowAddForm] = useState(false);`

**4. 数据获取**:
- `const { data: deptTreeData = [] } = useDeptTree();`
- `const { data: aliasPage, isLoading, refetch } = useQuery({ queryKey: queryKeys.locationAlias.list({ pageNum, pageSize }), queryFn: async () => { const res = await locationAliasApi.list({ pageNum, pageSize }); return res.data ?? { list: [], total: 0, current: 1, pageSize: 10 }; } });`
- `const fullDeptTreeData = trimTitleToLastSegment(deptTreeData);`
- `const locationTreeData = filterExternalOrgDepts(fullDeptTreeData as never);`

**5. 失效工具**:
- `const invalidateDept = useInvalidateDept();`
- `const queryClient = useQueryClient();`
- `const invalidateAliasAll = () => queryClient.invalidateQueries({ queryKey: queryKeys.locationAlias.all });`

**6. 提交新增 handleCreate**:
```typescript
const handleCreate = async () => {
  try {
    const values = await form.validateFields();
    await locationAliasApi.create({
      deptId: values.deptId,
      locationId: values.locationId,
      scope: values.scope ?? "workstation",
      remark: values.remark,
    });
    handleSuccess("映射创建成功");
    form.resetFields();
    setShowAddForm(false);
    await refetch();
    invalidateDept();
    invalidateAliasAll();
  } catch (err) {
    handleApiError(err, "创建映射失败", false);
  }
};
```

**7. 删除 handleDelete**:
```typescript
const handleDelete = async (id: string) => {
  try {
    await locationAliasApi.delete(id);
    handleSuccess("映射删除成功");
    await refetch();
    invalidateDept();
    invalidateAliasAll();
  } catch (err) {
    handleApiError(err, "删除映射失败", false);
  }
};
```

**8. 权限 gating**:
- `const canList = hasPermission("ops:location:alias:list");`
- `const canAdd = hasPermission("ops:location:alias:add");`
- `const canDelete = hasPermission("ops:location:alias:delete");`
- canAdd=false → "新增映射"按钮 disabled + Tooltip "无新增权限"
- canDelete=false → 操作列删除按钮 hidden (不渲染)

**9. JSX 结构** (antd `Drawer` width=600):
- 顶部 title `"工位部门物理位置映射管理"`
- 主体上半: 折叠式新增表单(`showAddForm` 控制)
  - 字段: deptId TreeSelect (fullDeptTreeData) + locationId TreeSelect (locationTreeData) + scope hidden Input (defaultValue "workstation") + remark TextArea
  - "提交" + "取消" 按钮
- 主体下半: alias 列表 Table
  - 列: dept_name / location_name / scope / created_at / 操作 (删除)
  - 分页: pageSize=10
  - 删除按钮: Popconfirm 二次确认

**关键约束**:
- Drawer width = 600 (D-02 Claude's discretion)
- 不修改 BaseEditModal.tsx
- 不新增菜单项 (D-02)
- 不引入新三方依赖
- scope 字段在 UI 隐藏(后端默认 "workstation", 前端兜底)
- locationId TreeSelect 数据来源是 `filterExternalOrgDepts(fullDeptTreeData)`
- 删除用 Popconfirm
- 列表为空显示空状态
  </action>
  <verify>
    <automated>cd D:\CODE\ClaudeCode\xingran-react-frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - `LocationAliasDrawer.tsx` 文件存在, 导出 `LocationAliasDrawer` 组件
    - 组件接受 `open: boolean / onClose: () => void` props
    - 包含 2 个 TreeSelect (dept 全量 + location 仅外部机构)
    - 包含 alias 列表 + 删除按钮
    - 权限 gating 使用 hasPermission
    - `npx tsc --noEmit` 退出码 0
  </done>
</task>

<task type="auto">
  <name>Task 2: index.tsx 注入 [⚙ 映射] 按钮 + Drawer state</name>
  <files>xingran-react-frontend/src/pages/operations/workstations/index.tsx</files>
  <read_first>
    - xingran-react-frontend/src/pages/operations/workstations/index.tsx (工具栏位置 — 现有按钮如 PlusOutlined/ImportOutlined/ExportOutlined 排列)
    - xingran-react-frontend/src/pages/operations/workstations/index.tsx (state 声明区域 — 找 useState 块)
    - xingran-react-frontend/src/utils/authHelpers.ts (hasPermission 签名)
  </read_first>
  <action>
修改 `xingran-react-frontend/src/pages/operations/workstations/index.tsx`:

**1. imports 区域追加**:
```typescript
import { LocationAliasDrawer } from "./LocationAliasDrawer";
import { hasPermission } from "@/utils/authHelpers";
import { SettingOutlined } from "@ant-design/icons";
```

**2. 在 WorkstationManagement 函数体内, 工具栏 state 块追加**:
```typescript
// Phase 39: 工位部门物理位置映射 Drawer 状态
const [aliasDrawerOpen, setAliasDrawerOpen] = useState(false);
const canListAlias = hasPermission("ops:location:alias:list");
```

**3. 在工具栏按钮区域(查找现有 `ImportOutlined` / `ExportOutlined` 按钮附近)插入**:
```tsx
{canListAlias && (
  <Button
    icon={<SettingOutlined />}
    onClick={() => setAliasDrawerOpen(true)}
  >
    映射
  </Button>
)}
```

**4. 在 WorkstationEditModal 之后, 渲染 LocationAliasDrawer**:
```tsx
<LocationAliasDrawer
  open={aliasDrawerOpen}
  onClose={() => setAliasDrawerOpen(false)}
/>
```

**关键约束**:
- 无 list 权限时按钮不渲染(D-08 锁定)
- 不修改 Table / StatisticsCards / DeptSidebar / 现有工具栏按钮
- 不引入新三方依赖
- onClose 触发 setAliasDrawerOpen(false), 关闭 Drawer
- 按钮位置: 列表页右上角工具栏, 与 Import/Export 按钮并列(具体位置由 executor 决定, 但要在工具栏区)
  </action>
  <verify>
    <automated>cd D:\CODE\ClaudeCode\xingran-react-frontend && npm run type-check 2>&1 | tail -5 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `index.tsx` 包含 `LocationAliasDrawer` 组件引用
    - `aliasDrawerOpen` state + `setAliasDrawerOpen` setter 存在
    - 工具栏按钮在 `canListAlias` 为 true 时渲染
    - `npm run type-check` 退出码 0
    - `npm run lint` 无 error 级问题
    - 现有列表 / 子表格 / 统计卡片 0 修改
  </done>
</task>

</tasks>

<verification>
- `npm run type-check` 退出码 0
- `npm run lint` 0 error
- 手动验证: 开发环境打开工位列表页 → 工具栏看到 [⚙ 映射] 按钮 → 点击打开 Drawer → 列表加载 + 新增/删除生效 → 关闭 Drawer
- 权限 gating: 用无 list 权限的账号登录, 按钮不渲染
</verification>

<success_criteria>
- LocationAliasDrawer 完整实现 (列表 + 新增 + 删除 + 权限 gating)
- index.tsx 工具栏按钮 + Drawer 状态注入完成
- 写操作触发 useInvalidateDept + invalidate locationAlias (双失效)
- 不修改 BaseEditModal 或其他共享组件
- 不引入新三方依赖
</success_criteria>

<output>
Create `.planning/phases/39-workstation-dept-location-alias/39-07-SUMMARY.md` when done
</output>