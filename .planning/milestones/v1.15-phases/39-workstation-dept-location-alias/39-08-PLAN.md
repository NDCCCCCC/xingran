---
phase: 39-workstation-dept-location-alias
plan: 08
type: execute
wave: 3
depends_on: ["39-04", "39-07"]
files_modified:
  - .planning/phases/39-workstation-dept-location-alias/39-VERIFICATION.md
autonomous: true
requirements:
  - REQ-39-08
  - REQ-39-11
  - REQ-39-12
must_haves:
  truths:
    - 全部 18 项 acceptance criteria 通过 (SPEC.md 验收清单)
    - sys_workstation 表数据零迁移 (migration_165 不含 UPDATE sys_workstation)
    - operlog 25 OperType 常量值稳定 (regression_test.go 全绿)
    - 后端编译 + 测试通过
    - 前端 type-check + lint 通过
  artifacts:
    - path: .planning/phases/39-workstation-dept-location-alias/39-VERIFICATION.md
      provides: "18 项 acceptance criteria 逐项 pass/fail 验收报告"
      contains: "## Verification Checklist"
  key_links:
    - from: .planning/phases/39-workstation-dept-location-alias/39-VERIFICATION.md
      to: internal/core/db/migrations/migration_165_sys_dept_location_alias.go
      via: "grep 验证不含 UPDATE sys_workstation"
      pattern: "UPDATE sys_workstation"
    - from: .planning/phases/39-workstation-dept-location-alias/39-VERIFICATION.md
      to: internal/utils/operlog/regression_test.go
      via: "go test ./internal/utils/operlog/ 验证常量稳定性"
      pattern: "TestOperTypeCountEquals25"
---

<objective>
执行 Phase 39 整体 UAT 验收, 逐项核对 SPEC.md 18 项 acceptance criteria (含后端编译 / 前端类型 / operlog 回归 / 数据零迁移 / 权限 gating / 端到端 UAT)。

Purpose: 验证 Phase 39 全功能落地, 暴露并修复任何回归 / 漏掉的需求 / 隐藏的边界 bug, 为 phase close 提供 pass/fail 证据。
Output: 39-VERIFICATION.md 验收报告 + 任何发现问题的修复 commits。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-workstation-dept-location-alias/39-SPEC.md (12 需求 + 18 acceptance criteria)
@.planning/phases/39-workstation-dept-location-alias/39-CONTEXT.md

# Codebase canonical references
@internal/utils/operlog/regression_test.go (25 OperType 常量守护)
@internal/core/db/migrations/migration_165_sys_dept_location_alias.go (Plan 39-01 产出 — 验证零迁移)
@internal/services/operations/location_alias_service_test.go (Plan 39-02 产出 — 验证三级校验)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 全量自动化验收 — 后端编译 / 测试 / operlog 回归</name>
  <files>.planning/phases/39-workstation-dept-location-alias/39-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/39-workstation-dept-location-alias/39-SPEC.md:127-147 (18 项 acceptance criteria 清单)
    - internal/utils/operlog/regression_test.go:42-71 (expectedOperTypeValues 常量值期望表)
  </read_first>
  <action>
执行全量自动化验证命令, 任何失败都需修复并重跑(本 task 输出 VERIFICATION.md 草稿):

**Step 1 — 后端编译**:
```
cd D:\CODE\ClaudeCode\xingran-go-backend
go build ./...
```
期望: 退出码 0

**Step 2 — 后端测试**:
```
go test ./internal/services/operations/ -run "TestValidateAlias" -v -count=1
go test ./internal/utils/operlog/ -v -count=1
```
期望: TestValidateAlias 3 失败 + 1 pass 用例全绿; operlog 25 OperType 常量守护全绿

**Step 3 — 前端类型检查 + lint**:
```
cd xingran-react-frontend
npm run type-check
npm run lint
```
期望: 退出码 0; lint 无 error 级问题

**Step 4 — 数据零迁移验证 (REQ-39-11)**:
```
grep -n "UPDATE sys_workstation\|UPDATE.*workstation" D:\CODE\ClaudeCode\xingran-go-backend\internal\core\db\migrations\migration_165_sys_dept_location_alias.go
```
期望: 0 命中(migration_165 不含 UPDATE sys_workstation)

**Step 5 — operlog 常量集守护 (REQ-39-12)**:
```
go test ./internal/utils/operlog/ -run "TestOperTypeCountEquals25|TestOperTypeConstantStability" -v -count=1
```
期望: 2 个测试通过(常量数=25, 25 个值稳定)

**Step 6 — 权限字符串对齐** (D-04 决策, 4 权限 seed 一致性):
```
grep -rn "ops:location:alias:" D:\CODE\ClaudeCode\xingran-go-backend\internal\core\db\migrations\migration_165_sys_dept_location_alias.go
grep -rn "ops:location:alias:" D:\CODE\ClaudeCode\xingran-go-backend\internal\api\router.go
```
期望: 4 个权限字符串在两处完全一致(list/add/edit/delete)

**Step 7 — 前端 [映射] 后缀 + Drawer 入口**:
```
grep -n "\[映射\]" D:\CODE\ClaudeCode\xingran-go-backend\xingran-react-frontend\src\pages\operations\workstations\modals\EditModal.tsx
grep -n "LocationAliasDrawer" D:\CODE\ClaudeCode\xingran-go-backend\xingran-react-frontend\src\pages\operations\workstations\index.tsx
```
期望: EditModal 包含 `[映射]` 字面量; index.tsx 包含 `LocationAliasDrawer` 引用

**记录到 VERIFICATION.md**:

```markdown
## Verification Checklist

### Automated Verification
- [ ] AC-01: `go build ./...` 退出码 0
- [ ] AC-02: `go test ./internal/services/operations/ -run "TestValidateAlias"` 全绿(3 fail + 1 pass)
- [ ] AC-03: `go test ./internal/utils/operlog/` 全绿(25 OperType 常量守护)
- [ ] AC-04: `npm run type-check` 退出码 0
- [ ] AC-05: `npm run lint` 无 error 级问题
- [ ] AC-06: migration_165 grep "UPDATE sys_workstation" = 0 命中(零迁移)
- [ ] AC-07: 4 个 ops:location:alias:* 权限字符串在 migration_165 + router.go 严格对齐
- [ ] AC-08: EditModal 包含 [映射] 字面量, index.tsx 包含 LocationAliasDrawer 引用

### Functional Verification (待用户 UAT)
- [ ] AC-09: migration_165 创建 sys_dept_location_alias (7 列 + 1 partial unique + 1 INDEX on location_id)
- [ ] AC-10: POST /api/v1/ops/location-alias/list 返回 alias 列表, 需 list 权限
- [ ] AC-11: 三级校验全部生效(自映射/非外部机构/非祖先均返回 HTTP 400 + 中文错误)
- [ ] AC-12: POST /api/v1/ops/location-alias/create 新增 alias, 需 add 权限
- [ ] AC-13: POST /api/v1/ops/location-alias/:id/update 更新 alias, 需 edit 权限
- [ ] AC-14: POST /api/v1/ops/location-alias/:id/delete 删除 alias (软删除), 需 delete 权限
- [ ] AC-15: 4 个权限 seed 到 sys_menu, sys_role_menu 不含
- [ ] AC-16: 工位编辑下拉选择机构后, subDeptTree 包含该机构子树 + alias 映射部门(带 [映射])
- [ ] AC-17: UAT 场景 — 创建 alias(子部门A → 中心支公司B, scope=workstation) 后, 工位 dept_id = 子部门A 可用, user picker 显示子部门A 用户
- [ ] AC-18: 4 个 alias 写操作在 sys_oper_log 留痕(模块名"工位管理")
```

将自动化 8 项 (AC-01..08) 标记 [x] 或 [ ] 反映实际结果, 功能 10 项 (AC-09..18) 留给后续 manual UAT(Phase 39 整体 verification 阶段由 orchestrator 触发)。
  </action>
  <verify>
    <automated>cd D:\CODE\ClaudeCode\xingran-go-backend && go build ./... && cd xingran-react-frontend && npm run type-check 2>&1 | tail -5 && grep -c "UPDATE sys_workstation" ../internal/core/db/migrations/migration_165_sys_dept_location_alias.go && go test ./internal/utils/operlog/ -run "TestOperTypeCountEquals25" -count=1 -v 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `39-VERIFICATION.md` 文件存在, 包含 "## Verification Checklist" 节
    - 18 项 acceptance criteria 全部列出
    - AC-01..08 自动化验收完成并标注 pass/fail
    - AC-09..18 留作 manual UAT 占位
    - 自动化验收全部 [x] 后, Phase 39 准予 close
  </done>
</task>

</tasks>

<verification>
- `go build ./...` 退出码 0
- `npm run type-check` 退出码 0
- `go test ./internal/utils/operlog/...` 全绿
- `go test ./internal/services/operations/ -run "TestValidateAlias"` 全绿
- `grep -c "UPDATE sys_workstation" internal/core/db/migrations/migration_165_sys_dept_location_alias.go` = 0
- `grep -c "\[映射\]" xingran-react-frontend/src/pages/operations/workstations/modals/EditModal.tsx` >= 1
- `grep -c "LocationAliasDrawer" xingran-react-frontend/src/pages/operations/workstations/index.tsx` >= 1
</verification>

<success_criteria>
- 全部 18 项 acceptance criteria 自动化部分 (AC-01..08) 通过
- operlog 25 OperType 常量值不变
- sys_workstation 数据零迁移
- 后端 / 前端编译 + 测试均绿
- VERIFICATION.md 输出留待 Phase close 阶段 manual UAT 补全 AC-09..18
</success_criteria>

<output>
Create `.planning/phases/39-workstation-dept-location-alias/39-08-SUMMARY.md` when done
</output>