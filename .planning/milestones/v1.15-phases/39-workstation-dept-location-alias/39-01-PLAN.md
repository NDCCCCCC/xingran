---
phase: 39-workstation-dept-location-alias
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/models/sys_dept_location_alias.go
  - internal/core/db/migrations/migration_165_sys_dept_location_alias.go
  - internal/core/db/database.go
autonomous: true
requirements:
  - REQ-39-01
  - REQ-39-04
must_haves:
  truths:
    - sys_dept_location_alias 表存在 (migration 165 创建, 7 列 + 1 partial unique + 1 INDEX on location_id)
    - 4 个 ops:location:alias:* 权限记录存在于 sys_menu (menu_type='F')
    - sys_role_menu 不含这 4 条新权限 (角色需手动授权)
  artifacts:
    - path: internal/models/sys_dept_location_alias.go
      provides: "SysDeptLocationAlias GORM 模型"
      contains: "type SysDeptLocationAlias struct"
      exports: ["SysDeptLocationAlias"]
    - path: internal/core/db/migrations/migration_165_sys_dept_location_alias.go
      provides: "CREATE TABLE IF NOT EXISTS + INSERT 4 sys_menu 权限"
      contains: "Migrate165SysDeptLocationAlias"
    - path: internal/core/db/database.go
      provides: "Migrate165SysDeptLocationAlias(d.DB) 注册"
      contains: "Migrate165SysDeptLocationAlias"
  key_links:
    - from: internal/core/db/database.go
      to: internal/core/db/migrations/migration_165_sys_dept_location_alias.go
      via: "migrations.Migrate165SysDeptLocationAlias(d.DB)"
      pattern: "Migrate165SysDeptLocationAlias"
---

<objective>
为 Phase 39 创建 `sys_dept_location_alias` 表（migration 165），承载"逻辑部门 → 物理部门"映射；同时 seed 4 个按钮权限（`ops:location:alias:list/add/edit/delete`），但 **不** 授权任何角色（D-04/D-05 决策）。

Purpose: 为后续 alias CRUD API（Plan 39-02）和工位部门下拉 union 注入（Plan 39-03）提供数据层基础与权限隔离。
Output: 1 个 GORM 模型 + 1 个 migration 文件 + database.go 中 1 行注册 + DB schema 验证。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-workstation-dept-location-alias/39-SPEC.md
@.planning/phases/39-workstation-dept-location-alias/39-CONTEXT.md
@.planning/REQUIREMENTS.md

# Backend canonical references
@internal/core/db/migrations/migration_164_phase38_verify_admin_migrated.go
@internal/core/db/migrations/migration_163_ad_account_pool_menu.go
@internal/models/dept.go
@internal/models/menu.go
@internal/core/db/database.go (line 388-410 migration registration block)
@internal/utils/operlog/regression_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: SysDeptLocationAlias GORM 模型</name>
  <files>internal/models/sys_dept_location_alias.go</files>
  <read_first>
    - internal/models/dept.go (现有 sys_dept 模型定义 + TableName 模式)
    - internal/models/base.go (BaseModel: ID/created_at/updated_at/deleted_at 字段)
    - internal/models/workstation.go (DeptID *string 列对齐参考)
  </read_first>
  <action>
新建文件 `internal/models/sys_dept_location_alias.go`,定义 GORM 模型:

```go
package models

// BaseModel 嵌入: 提供 ID (UUID PK) / CreatedAt / UpdatedAt / DeletedAt (软删除)
type SysDeptLocationAlias struct {
    BaseModel
    DeptID     string `gorm:"type:varchar(64);not null;index:idx_location_id,priority:2" json:"deptId"`     // 原组织部门ID(逻辑部门)
    LocationID string `gorm:"type:varchar(64);not null;index:idx_location_id,priority:1" json:"locationId"` // 物理位置部门ID(外部机构)
    Scope      string `gorm:"type:varchar(32);not null;default:'workstation'" json:"scope"`                // scope: workstation/floor/building/...
    Remark     string `gorm:"type:varchar(255)" json:"remark,omitempty"`
}

func (SysDeptLocationAlias) TableName() string { return "sys_dept_location_alias" }
```

字段说明:
- `BaseModel` 嵌入 (与项目 sys_* 表一致: UUID PK + 软删除)
- `DeptID/LocationID` 用 `varchar(64)`,与 `sys_workstation.dept_id` 字段类型对齐 (CLAUDE.md UUID Foreign Keys 约定)
- `Scope` 默认 `'workstation'`,预留 future scope 扩展 (D-04 决策)
- `Index idx_location_id` 用于 alias 反向查询 "哪些 dept 映射到该 location" (D-05 决策)
- **partial unique constraint** `(dept_id, scope) WHERE deleted_at IS NULL` 在 migration 内手写 SQL 实现,GORM tag 不支持 partial index 语法

`omitempty` 与 `json:"deptId"` 保持 camelCase JSON 标签与项目其他表一致。
  </action>
  <verify>
    <automated>cd D:\CODE\ClaudeCode\xingran-go-backend && go build ./internal/models/...</automated>
  </verify>
  <done>
    - 文件 `internal/models/sys_dept_location_alias.go` 存在
    - `go build ./internal/models/...` 退出码 0
    - 模型包含 `TableName()` 方法返回 `"sys_dept_location_alias"`
    - `DeptID/LocationID` 字段类型 `varchar(64)`
    - `Scope` 默认值 `'workstation'`
  </done>
</task>

<task type="auto">
  <name>Task 2: Migration 165 — 建表 + 4 条 sys_menu 权限 seed</name>
  <files>internal/core/db/migrations/migration_165_sys_dept_location_alias.go</files>
  <read_first>
    - internal/core/db/migrations/migration_164_phase38_verify_admin_migrated.go (最新 migration 风格样板)
    - internal/core/db/migrations/migration_163_ad_account_pool_menu.go (按钮权限 seed 样板: menu_type='F', perms 字段, sys_role_menu 不授权)
    - internal/models/menu.go (Menu 模型: MenuName / ParentID / MenuType / Perms / Status / Visible 字段)
  </read_first>
  <action>
新建文件 `internal/core/db/migrations/migration_165_sys_dept_location_alias.go`,严格对齐 migration_164 风格:

1. **文件头注释**: 描述 Phase 39 (D-04/D-05): 建表 + 4 条 sys_menu 按钮权限 seed, 不授权任何角色。

2. **CREATE TABLE (幂等, PG/SQLite 兼容)**:
```sql
CREATE TABLE IF NOT EXISTS sys_dept_location_alias (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    dept_id     VARCHAR(64)  NOT NULL,
    location_id VARCHAR(64)  NOT NULL,
    scope       VARCHAR(32)  NOT NULL DEFAULT 'workstation',
    remark      VARCHAR(255),
    created_at  TIMESTAMPTZ  DEFAULT NOW(),
    updated_at  TIMESTAMPTZ  DEFAULT NOW(),
    deleted_at  TIMESTAMPTZ
);
```
注: SQLite 不支持 `gen_random_uuid()` 与 `UUID` 类型; 用 GORM `db.Migrator().CreateTable(&models.SysDeptLocationAlias{})` 替代原始 SQL 以兼容双 DB (参考 migration_162 的 GORM AutoMigrate 模式)。

3. **Indexes (幂等)**:
   - 普通 INDEX on `location_id`: `CREATE INDEX IF NOT EXISTS idx_sys_dept_location_alias_location_id ON sys_dept_location_alias(location_id);`
   - Partial UNIQUE on `(dept_id, scope) WHERE deleted_at IS NULL`: 用 `db.Exec(...)` 包裹 PG 语法 `CREATE UNIQUE INDEX IF NOT EXISTS ... ON sys_dept_location_alias(dept_id, scope) WHERE deleted_at IS NULL;`; SQLite 跳过(用 GORM 兜底 `uniqueIndex` 已有覆盖)

4. **4 条 sys_menu 权限 seed** (D-04 决策: 不授权 sys_role_menu):
   - menu_name: "工位部门映射查询" / "工位部门映射新增" / "工位部门映射编辑" / "工位部门映射删除"
   - perms: `ops:location:alias:list` / `ops:location:alias:add` / `ops:location:alias:edit` / `ops:location:alias:delete`
   - menu_type: `'F'` (按钮)
   - parent_id: 工位管理菜单 (`db.Where("menu_name = ? AND menu_type = ?", "工位管理", "C").First(&parentMenu)`, 失败则 log 警告 + continue 不阻断)
   - visible: 0 (隐藏 — 不在菜单树显示), status: 0 (正常)
   - **不** INSERT sys_role_menu (D-04 锁定: "谁也不给")
   - 幂等: `db.Table("sys_menu").Where("perms = ?", perms).Count(&existingCount); existingCount > 0` 则 skip

5. **日志**: 起始 `log.Println("Running migration 165: ...")`, 收尾 `log.Println("Migration 165 completed: ...")`.

6. **不修改 sys_workstation 表** (D-05/REQ-39-11): 严格不包含 UPDATE sys_workstation 语句。
  </action>
  <verify>
    <automated>cd D:\CODE\ClaudeCode\xingran-go-backend && go build ./internal/core/db/migrations/...</automated>
  </verify>
  <done>
    - 文件存在,函数签名 `func Migrate165SysDeptLocationAlias(db *gorm.DB) error`
    - `go build ./internal/core/db/migrations/...` 退出码 0
    - CREATE TABLE 使用 GORM AutoMigrate (兼容 SQLite)
    - 4 条 sys_menu INSERT 包含 perms 字符串 `ops:location:alias:{list,add,edit,delete}` (可 grep 验证)
    - 不含 `INSERT INTO sys_role_menu` 语句 (D-04 验证)
    - 不含 `UPDATE sys_workstation` 语句 (REQ-39-11 验证)
  </done>
</task>

<task type="auto">
  <name>Task 3: database.go 注册 migration 165</name>
  <files>internal/core/db/database.go</files>
  <read_first>
    - internal/core/db/database.go:393-405 (migration 162/163/164 注册位置, Phase 36/38)
    - internal/core/db/database.go:388-410 (整体 migration 调用顺序)
  </read_first>
  <action>
在 `internal/core/db/database.go` 的 migration 调用序列中 (Phase 164 之后, MAC migration 117 之前), 追加 1 行:

```go
// Phase 39: 工位部门物理位置映射表 (sys_dept_location_alias) + 4 条按钮权限 seed
if err := migrations.Migrate165SysDeptLocationAlias(d.DB); err != nil {
    applogger.Errorf("Phase 39 工位部门映射迁移失败: %v", err)
}
```

位置: 紧接 `Migrate164Phase38VerifyAdminMigrated(d.DB)` 之后 (Phase 38 → Phase 39 顺序), 在 `Migrate117CreateMacFilterRules(d.DB)` 之前 (MAC 规则是历史 Phase 117,保持其原位置)。
  </action>
  <verify>
    <automated>cd D:\CODE\ClaudeCode\xingran-go-backend && go build ./... && grep -n "Migrate165SysDeptLocationAlias" internal/core/db/database.go</automated>
  </verify>
  <done>
    - `internal/core/db/database.go` 包含字符串 `Migrate165SysDeptLocationAlias`
    - `go build ./...` 退出码 0
    - 注册位置在 Migrate164 之后
    - 日志格式与相邻 migration 一致(`applogger.Errorf("Phase 39 工位部门映射迁移失败: %v", err)`)
  </done>
</task>

</tasks>

<verification>
- `go build ./...` 退出码 0
- `go vet ./...` 0 警告
- 文件存在性 + grep 关键内容确认
</verification>

<success_criteria>
- 迁移文件就绪, 启动应用后 `sys_dept_location_alias` 表被 GORM AutoMigrate 创建
- 4 条 sys_menu 按钮权限记录 seed 成功
- sys_role_menu 中不含新增的 4 个权限
- 不修改 sys_workstation 表
</success_criteria>

<output>
Create `.planning/phases/39-workstation-dept-location-alias/39-01-SUMMARY.md` when done
</output>