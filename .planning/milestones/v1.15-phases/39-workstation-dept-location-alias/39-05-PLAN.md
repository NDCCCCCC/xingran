---
phase: 39-workstation-dept-location-alias
plan: 05
type: execute
wave: 2
depends_on: ["39-03"]
files_modified:
  - xingran-react-frontend/src/lib/opsApi.ts
  - xingran-react-frontend/src/lib/queryKeys.ts
  - xingran-react-frontend/src/hooks/useAliasByLocation.ts
autonomous: true
requirements:
  - REQ-39-05
  - REQ-39-07
must_haves:
  truths:
    - locationAliasApi 暴露 list/create/update/delete 4 个方法
    - queryKeys.locationAlias.byLocation(locationId) 工厂函数存在
    - useAliasByLocation(locationId) hook 在 locationId 非空时调用后端 dept-options 接口, 空时 enabled=false
    - 工位编辑模态框可消费 union 结果 (跨 plan 验证, 此 plan 仅落基础设施)
  artifacts:
    - path: xingran-react-frontend/src/lib/opsApi.ts
      provides: "locationAliasApi 对象 + byLocation 工具方法"
      exports: ["locationAliasApi"]
    - path: xingran-react-frontend/src/lib/queryKeys.ts
      provides: "locationAlias 段 byLocation 工厂"
      contains: "locationAlias:"
    - path: xingran-react-frontend/src/hooks/useAliasByLocation.ts
      provides: "useAliasByLocation React Query hook"
      exports: ["useAliasByLocation"]
  key_links:
    - from: xingran-react-frontend/src/hooks/useAliasByLocation.ts
      to: xingran-react-frontend/src/lib/opsApi.ts
      via: "workstationApi.deptOptions or new aliasApi.list call"
      pattern: "workstationApi|deptOptions"
    - from: xingran-react-frontend/src/hooks/useAliasByLocation.ts
      to: xingran-react-frontend/src/lib/queryKeys.ts
      via: "queryKeys.locationAlias.byLocation"
      pattern: "queryKeys.locationAlias"
    - from: xingran-react-frontend/src/lib/opsApi.ts
      to: xingran-react-frontend/src/lib/queryKeys.ts
      via: "queryKey 工厂在两个文件中保持同名一致"
      pattern: "byLocation"
---

<objective>
为前端工位部门下拉 union 渲染提供 API 客户端 + queryKey + React Query hook 三件套 (D-06/D-07 决策)。

Purpose: Plan 39-06 (EditModal subDeptTree 注入) 与 Plan 39-07 (Drawer UI) 都需要 alias 数据获取能力, 此 plan 集中提供基础设施。
Output: locationAliasApi 对象、queryKeys.locationAlias 段、useAliasByLocation hook。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-workstation-dept-location-alias/39-SPEC.md
@.planning/phases/39-workstation-dept-location-alias/39-CONTEXT.md

# Frontend canonical references
@xingran-react-frontend/src/lib/opsApi.ts (existing CRUD api 工厂样板 + workstationApi 创建方式)
@xingran-react-frontend/src/lib/queryKeys.ts (queryKey 工厂样板 + dept.all 模式)
@xingran-react-frontend/src/hooks/useDeptTree.ts (canonical React Query hook 样板)
@xingran-react-frontend/src/hooks/useTableQuery.ts (alternative hook 样板参考)
</context>

<tasks>

<task type="auto">
  <name>Task 1: locationAliasApi 客户端 + workstationApi.deptOptions 扩展</name>
  <files>xingran-react-frontend/src/lib/opsApi.ts</files>
  <read_first>
    - xingran-react-frontend/src/lib/opsApi.ts:100-118 (workstationApi 现有结构样板 — spread 基础 CRUD + 自定义方法)
    - xingran-react-frontend/src/lib/opsApi.ts:33-67 (createCrudApi 工厂 — list/get/create/update/delete/batch 标准签名)
    - xingran-react-frontend/src/lib/opsApi.ts:9-26 (imports 区域 — 类型 + post helper)
  </read_first>
  <action>
修改 `xingran-react-frontend/src/lib/opsApi.ts`:

**1. 新增 DeptOption 类型** (在文件顶部 type 区域, 与 WorkstationListParams 等并列):

```typescript
// Phase 39: 工位部门下拉选项(后端 union: orgId 子孙 + alias 映射)
export interface DeptOption {
  deptId: string;
  deptName: string;
  isAlias: boolean;
}
```

**2. 扩展 workstationApi** (在现有 `workstationApi` 对象中添加 deptOptions 方法):

找到现有 `export const workstationApi = { ...workstationCrudApi, updatePositions: ... }` (约 line 112-118), 改为:

```typescript
export const workstationApi = {
  ...workstationCrudApi,

  updatePositions: async (items: { id: string; positionX: number; positionY: number }[]) => {
    return await post("/ops/workstation/positions", { items });
  },

  // Phase 39: 工位编辑"所属部门"下拉数据源(orgId 子孙 + alias union)
  // 返回 DeptOption[], isAlias=true 表示该条目是 alias 映射而来,前端应追加 [映射] 后缀
  deptOptions: async (orgId: string): Promise<DeptOption[]> => {
    const res = await post<{ data?: DeptOption[] }>("/ops/workstation/dept-options", { orgId });
    return res.data ?? [];
  },
};
```

**3. 新增 locationAliasApi** (alias CRUD API, 用于 Drawer 管理 UI, Plan 39-07 消费):

```typescript
// Phase 39: 工位部门物理位置映射管理 (Drawer UI 使用)
export interface LocationAlias {
  id: string;
  deptId: string;
  locationId: string;
  scope: string;
  remark?: string;
  createdAt?: string;
  updatedAt?: string;
}

export const locationAliasApi = {
  list: async (params: { pageNum?: number; pageSize?: number } = {}) => {
    return await post<{ data: { list: LocationAlias[]; total: number; current: number; pageSize: number } }>(
      "/ops/location-alias/list",
      { pageNum: params.pageNum ?? 1, pageSize: params.pageSize ?? 10 }
    );
  },

  create: async (data: { deptId: string; locationId: string; scope?: string; remark?: string }) => {
    return await post<LocationAlias>("/ops/location-alias", {
      ...data,
      scope: data.scope ?? "workstation",
    });
  },

  update: async (id: string, data: { deptId?: string; locationId?: string; remark?: string }) => {
    return await post(`/ops/location-alias/${id}/update`, data);
  },

  delete: async (id: string) => {
    return await post(`/ops/location-alias/${id}/delete`, {});
  },
};
```

**关键约束**:
- `deptOptions(orgId)` 接受 string, 返回 `DeptOption[]`(而非分页, 列表大小由 orgId 子孙+ alias 命中数决定)
- `locationAliasApi.list` 返回 PageData 风格响应(与项目其他列表端点一致)
- `locationAliasApi.create` 默认 `scope="workstation"`(前端兜底, 后端也会兜底)
- 不引入新依赖 (沿用 `post` helper)
- 不修改其他 api 对象
- 类型字面量与后端 Plan 39-03 的 DeptOption JSON tag 完全对齐(deptId / deptName / isAlias)
  </action>
  <verify>
    <automated>cd D:\CODE\ClaudeCode\xingran-react-frontend && npx tsc -p tsconfig.json --noEmit 2>&1 | head -30 || (cd D:\CODE\ClaudeCode\xingran-go-backend && cd xingran-react-frontend && npx tsc --noEmit 2>&1 | head -30)</automated>
  </verify>
  <done>
    - `xingran-react-frontend/src/lib/opsApi.ts` 导出 `DeptOption` 接口 + `LocationAlias` 接口 + `locationAliasApi` 对象
    - `workstationApi.deptOptions` 方法存在
    - `cd xingran-react-frontend && npm run type-check` 退出码 0(或 npx tsc --noEmit 退出码 0)
    - 不修改其他 api 对象(buildingApi / floorApi / serverRoomApi 等)
  </done>
</task>

<task type="auto">
  <name>Task 2: queryKeys.locationAlias + useAliasByLocation hook</name>
  <files>xingran-react-frontend/src/lib/queryKeys.ts, xingran-react-frontend/src/hooks/useAliasByLocation.ts</files>
  <read_first>
    - xingran-react-frontend/src/lib/queryKeys.ts (现有 queryKey 工厂样板 — dict / list / dept / duty / role)
    - xingran-react-frontend/src/hooks/useDeptTree.ts (canonical React Query hook 样板 + UseQueryResult + staleTime/gcTime)
    - xingran-react-frontend/src/lib/opsApi.ts (Plan Task 1 产出的 workstationApi.deptOptions)
  </read_first>
  <action>
**1. 扩展 queryKeys** (修改 `xingran-react-frontend/src/lib/queryKeys.ts`):

在 `queryKeys.dept` 块之后插入新段:

```typescript
// Phase 39: 工位部门物理位置映射 — 按 location 缓存,用于工位编辑下拉 union 注入
locationAlias: {
  all: ["location-alias"] as const,
  byLocation: (locationId: string) => ["location-alias", "by-location", locationId] as const,
  list: (params?: Record<string, unknown>) => ["location-alias", "list", params ?? {}] as const,
},
```

**2. 新建 useAliasByLocation hook** (新建 `xingran-react-frontend/src/hooks/useAliasByLocation.ts`):

```typescript
/**
 * useAliasByLocation — 按 location(机构)Id 拉取该机构下的 alias 映射部门列表,
 * 用于工位编辑模态框"所属部门"下拉的 union 注入(D-06/D-07 决策)。
 *
 * 行为契约:
 * - locationId 为空时 `enabled: false`,不发起请求(避免无效网络请求)
 * - 调用 workstationApi.deptOptions(orgId), 返回 DeptOption[]
 * - 前端消费时按 isAlias=true 追加 [映射] 后缀(D-01 决策)
 * - 与 useDeptTree 不耦合 — alias 数据来自后端 union SQL,不需要客户端合并
 *
 * 失效策略:
 * - 写操作(Plan 39-07 Drawer CRUD)成功后,调用方应同时:
 *   1) invalidateQueries(queryKeys.dept.all) → 触发 useDeptTree 重新拉取
 *   2) invalidateQueries(queryKeys.locationAlias.byLocation(orgId)) → 触发本 hook 重新拉取
 * - 或简单地 invalidateQueries(queryKeys.locationAlias.all) 一把全清
 */

import { useQuery, type UseQueryResult } from "@tanstack/react-query";
import { workstationApi, type DeptOption } from "@/lib/opsApi";
import { queryKeys } from "@/lib/queryKeys";

export function useAliasByLocation(locationId: string | undefined | null): UseQueryResult<DeptOption[]> {
  return useQuery({
    queryKey: queryKeys.locationAlias.byLocation(locationId ?? ""),
    queryFn: async () => {
      if (!locationId) return [];
      return await workstationApi.deptOptions(locationId);
    },
    enabled: !!locationId,
    staleTime: 5 * 60 * 1000,   // 5 min,与 useDeptTree 对齐
    gcTime: 30 * 60 * 1000,
    refetchOnWindowFocus: false,
  });
}
```

**关键约束**:
- 不修改 useDeptTree.ts(它管的是 sys_dept 主数据,与 alias 无关)
- 不修改 queryKeys 现有段(只在末尾 append)
- useAliasByLocation 接受 `string | undefined | null`, 空时 `enabled=false`(与 useDict / useTableQuery 的 `enabled` 模式一致)
- staleTime / gcTime 与 useDeptTree 对齐(5min / 30min)
- 不引入新依赖 (React Query 已装)
- hook 命名风格与 useDeptTree 一致(前缀 `use`, camelCase)
  </action>
  <verify>
    <automated>cd D:\CODE\ClaudeCode\xingran-react-frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - `xingran-react-frontend/src/lib/queryKeys.ts` 包含 `locationAlias: { all, byLocation, list }` 段
    - `xingran-react-frontend/src/hooks/useAliasByLocation.ts` 文件存在, 导出 `useAliasByLocation`
    - `npx tsc --noEmit` 退出码 0
    - 现有 queryKeys 段 0 修改(只在末尾 append)
    - 不修改其他 hooks
  </done>
</task>

</tasks>

<verification>
- `cd xingran-react-frontend && npm run type-check` 退出码 0
- `cd xingran-react-frontend && npm run lint` 无 error 级问题
- 工位编辑模态框暂未集成(由 Plan 39-06 完成), 但 API 路径已注册, 可通过手动调 workstationApi.deptOptions("xxx") 验证后端连通
</verification>

<success_criteria>
- locationAliasApi 暴露完整 CRUD 接口
- queryKeys.locationAlias 工厂可被消费
- useAliasByLocation 在 locationId 非空时自动拉取, 空时不发请求
- 不修改既有 api / queryKey / hook 文件的现有 API 表面
- 不引入新三方依赖
</success_criteria>

<output>
Create `.planning/phases/39-workstation-dept-location-alias/39-05-SUMMARY.md` when done
</output>