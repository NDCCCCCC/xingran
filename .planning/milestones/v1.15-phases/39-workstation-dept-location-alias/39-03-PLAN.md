---
phase: 39-workstation-dept-location-alias
plan: 03
type: execute
wave: 1
depends_on: ["39-01"]
files_modified:
  - internal/services/operations/workstation_service.go
  - internal/api/v1/operations/workstation_handler.go
  - internal/api/router.go
autonomous: true
requirements:
  - REQ-39-05
must_haves:
  truths:
    - WorkstationService 接口新增 GetWorkstationDeptOptions(orgId) 方法
    - 后端单 query 完成 union: orgId 子孙节点 + alias 映射节点 (含 is_alias 标记)
    - 新端点 POST /ops/workstation/dept-options 接受 orgId, 返回 DeptOption[] 列表
    - 现有 workstationJoinClause 不被修改 (D-06 锁定)
  artifacts:
    - path: internal/services/operations/workstation_service.go
      provides: "WorkstationService.GetWorkstationDeptOptions + DeptOption struct"
      contains: "GetWorkstationDeptOptions"
    - path: internal/api/v1/operations/workstation_handler.go
      provides: "WorkstationHandler.GetWorkstationDeptOptions handler"
      contains: "GetWorkstationDeptOptions"
    - path: internal/api/router.go
      provides: "POST /ops/workstation/dept-options 路由注册"
      contains: "/dept-options"
  key_links:
    - from: internal/services/operations/workstation_service.go
      to: internal/models/sys_dept_location_alias.go
      via: "GORM union query JOIN sys_dept_location_alias"
      pattern: "sys_dept_location_alias"
    - from: internal/api/router.go
      to: internal/api/v1/operations/workstation_handler.go
      via: "POST /dept-options 路由 → handler.GetWorkstationDeptOptions"
      pattern: "DeptOptions"
---

<objective>
为工位编辑模态框的"所属部门"下拉提供 union 数据源 (D-06 决策)。后端 `workstationService.GetWorkstationDeptOptions(orgId)` 单 query 完成 union: orgId 子孙节点 + alias 命中节点 (含 `is_alias` 标记)。

Purpose: 前端 EditModal.subDeptTree (Plan 39-06) 接收 union 结果 + 渲染 `[映射]` 后缀。零代码改动用户下拉 (REQ-39-08) 通过后端递归天然支持。
Output: WorkstationService 新方法 + handler 新端点 + router 1 行注册。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-workstation-dept-location-alias/39-SPEC.md
@.planning/phases/39-workstation-dept-location-alias/39-CONTEXT.md

# Backend canonical references
@internal/services/operations/workstation_service.go:14-18 (workstationJoinClause 常量 — 不修改)
@internal/services/operations/workstation_service.go:32-42 (WorkstationService interface 样板)
@internal/services/operations/workstation_service.go:73-75 (workstationService struct 样板)
@internal/api/v1/operations/workstation_handler.go (handler 样板)
@internal/models/dept.go (sys_dept + Ancestors 字段 + IsExternalOrg 字段)
@internal/models/sys_dept_location_alias.go (Plan 39-01 产出)
@internal/api/router.go:580-604 (workstations 路由组注册块)
</context>

<tasks>

<task type="auto">
  <name>Task 1: WorkstationService.GetWorkstationDeptOptions + 单 query union SQL</name>
  <files>internal/services/operations/workstation_service.go</files>
  <read_first>
    - internal/services/operations/workstation_service.go:14-18 (workstationJoinClause — 不修改)
    - internal/services/operations/workstation_service.go:32-42 (WorkstationService interface 当前定义)
    - internal/services/operations/workstation_service.go:73-110 (workstationService struct + 构造器样板)
    - internal/models/dept.go (sys_dept 表名 + Ancestors 字段类型)
    - internal/models/sys_dept_location_alias.go (alias 表结构 + Scope 字段)
  </read_first>
  <action>
**1. 新增 DeptOption 类型** (与 service 文件同包):

```go
// DeptOption 工位编辑"所属部门"下拉选项(union: orgId 子孙 + alias 映射)
type DeptOption struct {
    DeptID   string `json:"deptId"`   // 部门 ID (UUID text)
    DeptName string `json:"deptName"` // 部门名称 (短名,由前端 trimTitleToLastSegment 进一步收窄)
    IsAlias  bool   `json:"isAlias"`  // 是否为 alias 映射条目; true → 前端追加 [映射] 后缀
}
```

**2. 扩展 WorkstationService 接口** (在现有 Statistics 后追加):

```go
// GetWorkstationDeptOptions 工位编辑"所属部门"下拉数据源(orgId 子孙 + alias union)
GetWorkstationDeptOptions(ctx context.Context, orgId string) ([]DeptOption, error)
```

**3. 实现 GetWorkstationDeptOptions (D-06 SQL, 单 query union)**:

```go
func (s *workstationService) GetWorkstationDeptOptions(ctx context.Context, orgId string) ([]DeptOption, error) {
    if orgId == "" {
        return []DeptOption{}, nil
    }
    var result []DeptOption
    // 单 query union: sys_dept 子孙节点 (is_alias=false) + alias 命中节点 (is_alias=true)
    // 1) 子孙节点: ancestors LIKE '%,<orgId>,%' OR id = orgId, 排除外部机构本身
    // 2) alias 节点: scope='workstation' AND location_id=orgId, JOIN sys_dept 取 dept_name
    err := s.db.WithContext(ctx).Raw(`
        SELECT id::text AS dept_id, dept_name AS dept_name, false AS is_alias
        FROM sys_dept
        WHERE deleted_at IS NULL
          AND is_external_org = 0
          AND ((',' || ancestors || ',') LIKE ('%,' || ? || ',%') OR id::text = ?)
        UNION ALL
        SELECT a.dept_id, d.dept_name, true AS is_alias
        FROM sys_dept_location_alias a
        JOIN sys_dept d ON d.id::text = a.dept_id
        WHERE a.deleted_at IS NULL
          AND a.scope = 'workstation'
          AND a.location_id = ?
    `, orgId, orgId, orgId).Scan(&result).Error
    return result, err
}
```

**SQL 关键点**:
- `id::text = ?` 对齐 alias.dept_id (varchar) 与 sys_dept.id (UUID)
- `',' || ancestors || ','` 包围匹配避免子串误匹配 (与 validateAlias 逻辑一致)
- UNION ALL 而非 UNION 保留顺序(去重交给前端 — alias 节点本就唯一)
- 第一个 query 加 `is_external_org = 0` 排除外部机构本身(避免 location 节点出现在其子树里)
- 第二个 query JOIN sys_dept 是因为 alias 表只存 dept_id 不存 dept_name
- 不修改 workstationJoinClause (D-06 锁定)
- 不修改 List / Statistics 等其他方法
- 不引入新三方依赖
  </action>
  <verify>
    <automated>cd D:\CODE\ClaudeCode\xingran-go-backend && go build ./internal/services/operations/...</automated>
  </verify>
  <done>
    - `internal/services/operations/workstation_service.go` 包含字符串 `GetWorkstationDeptOptions`
    - `DeptOption` struct 存在且字段 `(DeptID string / DeptName string / IsAlias bool)`
    - 实现使用 `s.db.Raw(...).Scan(&result)` 单 query union
    - SQL 包含两个 SELECT (一个 FROM sys_dept, 一个 FROM sys_dept_location_alias JOIN sys_dept)
    - `workstationJoinClause` 字面量在文件中未变更 (可 grep 验证)
    - `go build ./internal/services/operations/...` 退出码 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Handler 端点 + Router 注册</name>
  <files>internal/api/v1/operations/workstation_handler.go, internal/api/router.go</files>
  <read_first>
    - internal/api/v1/operations/workstation_handler.go:32-42 (Statistics handler 样板 — 类似的 orgId 参数读取)
    - internal/api/v1/operations/workstation_handler.go:15-30 (WorkstationHandler struct + WithCore 链式注入)
    - internal/api/router.go:579-604 (workstations 路由组 — POST /list /statistics /batch /positions /:id /:id/update /:id/delete / "" + SetupExcelRouter)
  </read_first>
  <action>
**1. Handler 方法** (在 workstation_handler.go 的 Statistics 后追加):

```go
// GetWorkstationDeptOptions 工位编辑"所属部门"下拉数据源(orgId 子孙 + alias union)
// @Summary 工位部门下拉选项
// @Tags 运维管理
// @Param request body object{orgId=string} true "orgId"
// @Success 200 {object} response.Response{data=[]opsServices.DeptOption}
// @Router /ops/workstation/dept-options [post]
func (h *WorkstationHandler) GetWorkstationDeptOptions(c *gin.Context) {
    var req struct {
        OrgID string `json:"orgId"`
    }
    if err := c.ShouldBindJSON(&req); err != nil {
        req.OrgID = ""
    }
    result, err := h.service.GetWorkstationDeptOptions(c.Request.Context(), req.OrgID)
    if err != nil {
        response.Error(c, http.StatusInternalServerError, err.Error())
        return
    }
    response.Success(c, result)
}
```

**2. Router 注册** (在 internal/api/router.go:579-604 的 workstations 路由组内, `/list` 之后插入, 因 `/dept-options` 是更具体路径):

```go
workstations.POST("/dept-options", workstationHandler.GetWorkstationDeptOptions)
```

插入位置: `workstations.POST("/list", workstationHandler.List)` 之后, `workstations.POST("/statistics", workstationHandler.Statistics)` 之前(具体路径优先匹配, 与现有路由排列风格一致)。

**重要约束**:
- 不引入新权限中间件 (`ops:workstation:list` 已有覆盖, 因该端点是工位列表的下拉数据源; 与现有 `SetupWorkstationRouter` 块共用 middleware)
- 不引入新权限字符串
- 不新增 swagger 注解以外的注释(可选 swagger 注解,与 Statistics 对齐即可)
- 不修改现有任何路由
  </action>
  <verify>
    <automated>cd D:\CODE\ClaudeCode\xingran-go-backend && go build ./... && grep -n "/dept-options" internal/api/router.go</automated>
  </verify>
  <done>
    - `internal/api/v1/operations/workstation_handler.go` 包含 `GetWorkstationDeptOptions` 方法
    - `internal/api/router.go` 包含 `workstations.POST("/dept-options", ...)` 行
    - `go build ./...` 退出码 0
    - router.go 中现有路由 (除新增 1 行) 0 修改
    - handler.go 中现有方法 0 修改
  </done>
</task>

</tasks>

<verification>
- `go build ./...` 退出码 0
- `grep -n "workstationJoinClause" internal/services/operations/workstation_service.go` 行内容与 Plan 39 启动前完全一致
- 现有 workstation handler / router 不被破坏 (build 通过即证明)
</verification>

<success_criteria>
- 单 query union SQL 实现, 含 is_alias 标记
- 新端点注册到 router, 通过 ops:workstation:list 既有权限保护
- workstationJoinClause / 现有 List / Statistics 方法零修改
- 不引入新三方依赖
</success_criteria>

<output>
Create `.planning/phases/39-workstation-dept-location-alias/39-03-SUMMARY.md` when done
</output>