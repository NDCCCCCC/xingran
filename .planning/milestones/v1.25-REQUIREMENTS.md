---
last_updated: 2026-08-20
milestone: v1.25
update_trigger: v1.25 launch-block D-01..D-12 all checked, 7 plans complete
type: launch-block
---

# Requirements: XingRan-Next — Milestone v1.25 (Launch Block)

**Defined:** 2026-08-19
**Milestone:** v1.25 系统设置页面布局重构 (Settings Page Layout Redesign)
**Core Value:** 系统设置页与 v1.22 品牌化后的其余页面视觉语汇一致 — 双层纸感、按钮纪律 D-03、清理多主题时代遗留布局残留(含 default-theme 入口)。

> **性质:** 本启动块是**纯前端布局重构**,以 `70-CONTEXT.md` 锁定决策 D-01~D-12 为需求真相源(12/12 已映射到 plans),不改业务逻辑与 API 契约。

## v1 Requirements (D-01..D-12)

### 布局结构

- [x] **D-01**: 系统设置页 `/system/settings-page` 为左分类 + 右内容 SettingsShell 布局
- [x] **D-02**: 表格/网格类分类(邮箱/API/验证码)撑满,用户设置三类表单限宽 760px
- [x] **D-03**: 激活分类由 URL `?cat=` 参数驱动,非法值回退默认 `email`,切换 replace:true 不产生新 RouteTab
- [x] **D-04**: `<lg` 断点降级为顶部 Segmented block 控件
- [x] **D-05**: 用户设置 `/user/settings` 与系统设置共用同一 `SettingsShell.tsx`(界面/布局/数据 3 分类),不合并菜单入口

### 交互模式

- [x] **D-06**: 用户设置每行 label+描述+右对齐控件(Switch/Select),明暗模式为分段卡片选择器(role=radio 非 Radio.Button),即改即存
- [x] **D-07 v16 范式**: 邮箱/API 配置页为统计卡行 + 品牌工具栏 + `.xr-table-zebra` 双层纸感表格三段式
- [x] **D-08 网格墙**: 验证码背景图 `.xr-captcha-grid` 图片网格墙(4 统计卡 + 紧凑筛选 + 缩略图卡片 + Image 预览)
- [x] **D-09 5 个 Modal 容器**: email 700 / api 800 / captcha 现状,结构与宽度不变;api Modal 内 headers/template/auth Tabs 保留

### 工程治理

- [x] **D-10 首提交**: 工作区 default-theme 清理 13 文件 (+42/-717) 作为 phase 首个原子提交入库
- [x] **D-11 目录合并**: 三个 settings 目录合并为 `src/pages/system/settings/`,`settings-page/` 删除;sys_menu component 经 Migrate208 迁移
- [x] **D-12 残留清零**: settings 范围内死目录 `system/captcha-background/` + settingsStore 死 actions + preset Tag + persisted activeTab 键 + fallback 硬编码色全部清零

### 回归收口

- [x] **回归门**: `npm run build/type-check/lint/test` + `npm run deadcode` + `go build ./...` 全绿
- [x] **截图矩阵**: ≥lg × light/dark 代表页 + <lg Segmented 降级,对照 v16 基准归档并经 checkpoint 确认
