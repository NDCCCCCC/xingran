---
task: 添加 VDI 菜单到 sys_menu 表
status: complete
completed: 2026-05-25
commit: 8625031
---

# Quick Task: 添加 VDI 菜单 - 完成

## 执行摘要

✅ **任务完成**: 已创建 VDI 菜单迁移脚本并提交

## 完成内容

### 1. 创建迁移脚本

**文件**: `internal/core/db/migrations/129_add_vdi_menus.sql`

**菜单结构**:
```
虚拟机管理 (一级菜单, M)
├── 虚拟机列表 (二级菜单, C) - /vdi/vm
│   ├── 查询、新增、修改、删除、操作、配置IP、重命名、绑定用户、同步状态 (F)
├── 虚拟机详情 (隐藏菜单, C) - /vdi/vm/:id
│   ├── 账号查询、创建账号、重置密码、删除账号、打开终端 (F)
└── VDI服务器配置 (二级菜单, C) - /vdi/servers
    └── 查询、新增、修改、删除、测试连接 (F)
```

### 2. 菜单记录统计

- **总记录数**: 23 条
- **一级菜单**: 1 条（虚拟机管理）
- **二级菜单**: 3 条（虚拟机列表、虚拟机详情、VDI服务器配置）
- **按钮权限**: 19 条

### 3. 权限标识符

使用 Phase 22 CONTEXT.md 中定义的权限：
- `vdi:visit` — 访问虚拟机管理模块
- `vdi:vm:*` — 虚拟机相关权限
- `vdi:account:*` — 账号管理权限
- `vdi:admin` — VDI 系统管理

## 后续步骤

**运行迁移**: 应用迁移脚本到数据库
```bash
# 迁移将在应用启动时自动执行
# 或手动执行 SQL 文件
psql -U xingran -d xingran_next -f internal/core/db/migrations/129_add_vdi_menus.sql
```

**验证菜单**:
1. 启动应用
2. 登录系统
3. 查看左侧菜单是否显示"虚拟机管理"
4. 验证权限控制是否正确

## 备注

- 菜单 ID 使用 `770e8400-e29b-41d4-a716-44665544****` 格式
- 与运维管理模块（550e8400）区分开
- 虚拟机详情菜单设置为隐藏（visible=0），通过详情页访问
