# 快速任务：默认主题管理功能

**任务 ID**: 260615-default-theme-config
**创建日期**: 2026-06-15
**预计时间**: 2-4 小时

## 需求描述

在系统管理 → 系统设置页面添加默认主题管理功能，允许管理员：
1. 查看当前默认主题配置
2. 修改默认主题设置
3. 从当前用户（chenchao-076）的配置同步到默认主题

## 技术范围

### 后端
- [ ] API 端点：获取默认主题配置
- [ ] API 端点：设置默认主题配置
- [ ] API 端点：从用户配置同步到默认主题
- [ ] 存储位置：`sys_config` 表（key: `sys.theme.default`）

### 前端
- [ ] 系统设置页面添加"默认主题" tab
- [ ] 主题配置表单（主题模式、主题颜色等）
- [ ] "同步当前配置"按钮（读取当前用户配置并应用到默认）
- [ ] 保存按钮（更新默认主题配置）

### 数据库
- [ ] 添加 `sys.theme.default` 配置项到 `sys_config` 表
- [ ] 初始值：从用户 chenchao-076 的配置读取

## 执行步骤

1. **调研当前实现**：
   - 查看用户主题配置存储位置
   - 查看系统设置页面实现
   - 了解主题配置结构

2. **后端实现**：
   - 添加配置项到 sys_config
   - 实现 3 个 API 端点
   - 单元测试

3. **前端实现**：
   - 添加默认主题 tab
   - 实现表单和交互
   - 集成 API 调用

4. **测试验证**：
   - 功能测试
   - UI 交互测试
   - 边界情况测试

## 技术细节

### 主题配置结构（参考 settingsStore）
```typescript
interface ThemeConfig {
  mode: 'light' | 'dark' | 'auto';
  // 其他主题相关配置
}
```

### API 端点设计
```
GET  /system/config/theme/default     // 获取默认主题
POST /system/config/theme/default     // 设置默认主题
POST /system/config/theme/sync        // 从当前用户同步
```

## 注意事项

- 需要先读取用户 chenchao-076 的当前主题配置
- 同步功能需要管理员权限
- 前端需要实时预览主题变化
- 考虑缓存失效机制

## 完成标准

- [x] 所有功能点实现完成
- [x] 代码通过编译
- [x] 功能测试通过
- [x] 代码已提交
