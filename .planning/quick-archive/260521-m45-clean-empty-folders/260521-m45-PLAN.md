---
phase: quick
plan: 260521-m45
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Empty directories removed from project"
    - "Project structure cleaned up"
    - "Build artifacts and temporary directories cleared"
  artifacts:
    - path: "."
      provides: "Clean project directory structure"
      min_lines: 0
  key_links: []
---

<objective>
Clean up all empty folders in the project to maintain a clean directory structure and remove unused directories that may cause confusion or clutter.

Purpose: Empty folders can accumulate during development, refactoring, or build processes. Removing them improves project hygiene and prevents potential confusion about directory purpose.

Output: Clean project structure with no empty directories (except those that should remain like .git internal folders).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Empty Directory Analysis
Current empty directories found (24 total):

**Planning/GSD Directories (keep):**
- `.planning/phases/11-mac-address-filter` - Completed phase, should be kept
- `.planning/quick/260521-m45-clean-empty-folders` - Current plan directory

**Git/Version Control (exclude from cleanup):**
- `.git/refs/remotes/go_react` - Git internal directory
- `.claude/worktrees` - Git worktree metadata

**Worktree Directories (exclude - separate workspace):**
- `.worktrees/operations-management/*` - Separate workspace, not main project

**Build/Node Modules (exclude - auto-generated):**
- `*/node_modules/.vite-temp` - Vite build cache, auto-generated

**Application Directories (safe to remove):**
- `cmd/migrate`, `cmd/testapi`, `cmd/verify` - Empty command directories
- `internal/api/v1/system/requests` - Empty requests directory  
- `internal/core/middleware`, `internal/core/monitoring`, `internal/core/services`, `internal/core/utils` - Empty core directories
- `internal/services/duty_new` - Empty old service directory
- `scripts/debug` - Empty debug scripts directory
- `uploads/rpa/downloads` - Empty uploads directory

**Frontend RPA Worker (safe to remove):**
- `xingran-react-frontend/rpa-worker/cmd`, `configs`, `deployments` - Empty RPA worker directories
- `xingran-react-frontend/src/pages/operations/building-spaces/hooks` - Empty hooks directory

</context>

<tasks>

<task type="auto">
  <name>Task 1: Identify and categorize empty directories</name>
  <files>[]</files>
  <action>Run a comprehensive scan to identify all empty directories in the project. Categorize them into:
1. Safe to remove (application directories, old code, unused structures)
2. Should keep (planning artifacts, current working directories)
3. Exclude from cleanup (git internals, worktrees, node_modules)

Use this command to identify empty dirs:
```bash
find . -type d -empty 2>/dev/null | grep -v -E '(node_modules|\.git|\.worktrees|\.claude)' > /tmp/empty_dirs.txt && cat /tmp/empty_dirs.txt
```

Create a summary report showing the categorization.</action>
  <verify>Check that empty_dirs.txt contains the list of empty directories to clean</verify>
  <done>All empty directories identified and categorized</done>
</task>

<task type="auto">
  <name>Task 2: Remove empty application directories</name>
  <files>cmd/migrate, cmd/testapi, cmd/verify, internal/api/v1/system/requests, internal/core/middleware, internal/core/monitoring, internal/core/services, internal/core/utils, internal/services/duty_new, scripts/debug, uploads/rpa/downloads, xingran-react-frontend/rpa-worker/cmd, xingran-react-frontend/rpa-worker/configs, xingran-react-frontend/rpa-worker/deployments, xingran-react-frontend/src/pages/operations/building-spaces/hooks</files>
  <action>Remove empty directories that are safe to delete. Use rmdir commands which will only succeed if the directory is empty (providing an extra safety check):

```bash
# Remove empty cmd directories
rmdir cmd/migrate 2>/dev/null || echo "cmd/migrate not empty or doesn't exist"
rmdir cmd/testapi 2>/dev/null || echo "cmd/testapi not empty or doesn't exist"  
rmdir cmd/verify 2>/dev/null || echo "cmd/verify not empty or doesn't exist"

# Remove empty internal directories
rmdir internal/api/v1/system/requests 2>/dev/null || echo "requests not empty or doesn't exist"
rmdir internal/core/middleware 2>/dev/null || echo "middleware not empty or doesn't exist"
rmdir internal/core/monitoring 2>/dev/null || echo "monitoring not empty or doesn't exist"
rmdir internal/core/services 2>/dev/null || echo "services not empty or doesn't exist"
rmdir internal/core/utils 2>/dev/null || echo "utils not empty or doesn't exist"
rmdir internal/services/duty_new 2>/dev/null || echo "duty_new not empty or doesn't exist"

# Remove empty scripts directory
rmdir scripts/debug 2>/dev/null || echo "debug not empty or doesn't exist"

# Remove empty uploads directory
rmdir uploads/rpa/downloads 2>/dev/null || echo "downloads not empty or doesn't exist"
rmdir uploads/rpa 2>/dev/null || echo "rpa not empty or doesn't exist"
rmdir uploads 2>/dev/null || echo "uploads not empty or doesn't exist"

# Remove empty frontend RPA worker directories
rmdir xingran-react-frontend/rpa-worker/cmd 2>/dev/null || echo "rpa-worker/cmd not empty or doesn't exist"
rmdir xingran-react-frontend/rpa-worker/configs 2>/dev/null || echo "rpa-worker/configs not empty or doesn't exist"
rmdir xingran-react-frontend/rpa-worker/deployments 2>/dev/null || echo "rpa-worker/deployments not empty or doesn't exist"

# Remove empty hooks directory
rmdir xingran-react-frontend/src/pages/operations/building-spaces/hooks 2>/dev/null || echo "hooks not empty or doesn't exist"
```

This approach is safe because rmdir will fail if the directory is not empty, preventing accidental deletion of files.</action>
  <verify>
    <automated>find . -type d -empty 2>/dev/null | grep -v -E '(node_modules|\.git|\.worktrees|\.claude|\.planning)' | wc -l</automated>
  </verify>
  <done>All empty application directories removed, leaving only planning and git directories</done>
</task>

<task type="auto">
  <name>Task 3: Verify cleanup and report results</name>
  <files>[]</files>
  <action>Run a final verification to ensure cleanup was successful and generate a summary report:

```bash
echo "=== Empty Directory Cleanup Report ==="
echo ""
echo "Remaining empty directories (excluding git/node_modules/worktrees):"
find . -type d -empty 2>/dev/null | grep -v -E '(node_modules|\.git|\.worktrees|\.claude)' || echo "None"
echo ""
echo "Planning/GSD directories (expected to remain):"
find .planning -type d -empty 2>/dev/null || echo "No empty planning directories"
echo ""
echo "Total empty directories found:"
find . -type d -empty 2>/dev/null | wc -l
```

This provides a clear summary of what was cleaned and what remains.</action>
  <verify>
    <automated>find . -type d -empty 2>/dev/null | grep -v -E '(node_modules|\.git|\.worktrees|\.claude)' | wc -l</automated>
  </verify>
  <done>Cleanup verified, project structure is clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system operations | Deleting directories from the project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-m45-01 | Spoofing | Directory deletion | accept | Low risk - using rmdir which only works on empty directories |
| T-quick-m45-02 | Tampering | Project structure | mitigate | Using rmdir (not rm -rf) ensures only empty dirs are removed |
| T-quick-m45-03 | Denial of Service | Accidental deletion | mitigate | Excluding .git, node_modules, .worktrees from cleanup operations |
</threat_model>

<verification>
1. Confirm all empty application directories have been removed
2. Verify git and node_modules directories were not affected
3. Ensure no files were accidentally deleted (rmdir safety)
4. Confirm project can still build and run normally
</verification>

<success_criteria>
1. All non-essential empty directories removed from project
2. Zero empty directories in application code (cmd/, internal/, scripts/, xingran-react-frontend/)
3. Git and build directories untouched
4. Planning directories preserved as expected
5. Project builds and runs successfully after cleanup
</success_criteria>

<output>
After completion, create `.planning/quick/260521-m45-clean-empty-folders/260521-m45-SUMMARY.md` with cleanup report including:
- Number of directories removed
- List of removed directories
- Remaining empty directories (if any)
- Verification results
</output>
