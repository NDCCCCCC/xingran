---
phase: quick
plan: 260602-k1b
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/services/vdi/vdi_client_extended.go
  - internal/services/vdi/vm_service.go
  - internal/services/vdi/vm_service_impl.go
  - internal/api/v1/vdi/vm_router.go
  - internal/api/v1/vdi/vm_handler.go
  - xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx
  - xingran-react-frontend/src/lib/vdiApi.ts
  - xingran-react-frontend/src/types/vdi.ts
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "User cannot rename virtual machines through the UI"
    - "VM creation form does not include name input or auto-generation logic"
    - "VDI server automatically assigns names to created VMs"
    - "Rename API endpoints are removed from backend and frontend"
    - "Name field is optional in VM creation request"
  artifacts:
    - path: internal/api/v1/vdi/vm_handler.go
      provides: VM HTTP handlers without rename endpoint
      min_lines: 0
    - path: internal/services/vdi/vdi_client_extended.go
      provides: VDI client without RenameVM method
      min_lines: 0
    - path: internal/services/vdi/vm_service.go
      provides: VM service interface without RenameVM method
      min_lines: 0
    - path: xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx
      provides: VM list UI without rename modal and button
      min_lines: 0
    - path: xingran-react-frontend/src/lib/vdiApi.ts
      provides: VDI API client without rename method
      min_lines: 0
  key_links:
    - from: xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx
      to: xingran-react-frontend/src/lib/vdiApi.ts
      via: direct function call
      pattern: vmApi.rename
    - from: xingran-react-frontend/src/lib/vdiApi.ts
      to: internal/api/v1/vdi/vm_handler.go
      via: HTTP POST request
      pattern: /vdi/vm/.*?/rename
    - from: internal/api/v1/vdi/vm_handler.go
      to: internal/services/vdi/vm_service_impl.go
      via: service method call
      pattern: RenameVM
    - from: internal/services/vdi/vm_service_impl.go
      to: internal/services/vdi/vdi_client_extended.go
      via: client method call
      pattern: RenameVM
---

<objective>
Remove virtual machine renaming functionality and name components from VM creation to let VDI server handle naming automatically.

Purpose: Simplify VM management by removing manual naming control and allowing VDI server's automatic naming convention.
Output: Cleaned codebase without rename functionality and name auto-generation logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@D:\CODE\ClaudeCode\xingran-go-backend\CLAUDE.md

# Current VDI Naming Implementation

## Backend

**VDI Client (vdi_client_extended.go):**
- Line 289-312: `RenameVM()` method calls VDI API `PUT /v1/servers/update_name`
- Uses `name_mode: 2` (admin-specified name) with custom name

**VM Service (vm_service.go):**
- Line 122-124: `RenameVMServiceRequest` struct with `NewName` field
- Line 167: `RenameVM()` interface method

**VM Service Implementation (vm_service_impl.go):**
- Line 766+: `RenameVM()` implementation calling VDI client

**VM Handler (vm_handler.go):**
- Line 188-217: `Rename()` HTTP handler endpoint `POST /vdi/vm/:id/rename`

**VM Router (vm_router.go):**
- Registers rename route: `r.POST("/:id/rename", vmHandler.Rename)`

## Frontend

**VM List Component (VirtualMachineList/index.tsx):**
- Line 48: `renameModalVisible` state
- Line 569-584: `handleRename()` function calls `vmApi.rename()`
- Line 769-778: Rename button in table action column
- Line 1060-1073: Rename modal with Form.Item for `new_name`

**VDI API Client (vdiApi.ts):**
- Contains `rename()` method calling backend endpoint

**VDI Types (vdi.ts):**
- Contains rename-related type definitions

## VM Creation Name Logic

**Frontend (VirtualMachineList/index.tsx):**
- Line 971-994: VM name input field with auto-generation logic
- Line 214-230: Auto-generates name when resource and VTP selected
- Pattern: `VDI-{资源组}-{资源}-{后缀}` (e.g., `VDI-研发部-资源池1-zhangsan-001`)
- Lines 971-973: Name input field with required validation
- Lines 975-994: Name suffix input with auto-update logic

**Backend (vm_service.go):**
- Line 14: `Name` field in `CreateVMServiceRequest` with `validate:"required"`

</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove rename functionality from VDI client</name>
  <files>internal/services/vdi/vdi_client_extended.go</files>
  <action>Remove the RenameVM method from VDIClientExtended interface and its implementation:
1. Delete line 31: RenameVM method from interface definition
2. Delete lines 289-312: RenameVM implementation
3. Keep all other methods intact</action>
  <verify>grep -n "RenameVM" internal/services/vdi/vdi_client_extended.go returns no results</verify>
  <done>RenameVM method completely removed from VDI client</done>
</task>

<task type="auto">
  <name>Task 2: Remove rename functionality from VM service layer</name>
  <files>internal/services/vdi/vm_service.go, internal/services/vdi/vm_service_impl.go</files>
  <action>Remove rename-related code from VM service:
1. In vm_service.go:
   - Delete RenameVMServiceRequest struct (lines 122-124)
   - Remove RenameVM method from VMService interface (line 167)
2. In vm_service_impl.go:
   - Remove entire RenameVM implementation (search for "func (s *vmServiceImpl) RenameVM")
   - Remove any import related to rename functionality</action>
  <verify>grep -n "RenameVM" internal/services/vdi/vm_service.go internal/services/vdi/vm_service_impl.go returns no results</verify>
  <done>RenameVM completely removed from service layer</done>
</task>

<task type="auto">
  <name>Task 3: Remove rename endpoint from HTTP handler and router</name>
  <files>internal/api/v1/vdi/vm_handler.go, internal/api/v1/vdi/vm_router.go</files>
  <action>Remove rename HTTP endpoint:
1. In vm_handler.go:
   - Delete Rename handler method (lines 188-217)
2. In vm_router.go:
   - Find and remove the rename route registration (pattern: r.POST("/:id/rename", ...))
   - Keep all other routes intact</action>
  <verify>grep -n "rename\|Rename" internal/api/v1/vdi/vm_handler.go internal/api/v1/vdi/vm_router.go returns no results (excluding comments)</verify>
  <done>Rename HTTP endpoint completely removed</done>
</task>

<task type="auto">
  <name>Task 4: Remove rename functionality from frontend UI and API client</name>
  <files>xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx, xingran-react-frontend/src/lib/vdiApi.ts, xingran-react-frontend/src/types/vdi.ts</files>
  <action>Remove rename UI and API calls:
1. In VirtualMachineList/index.tsx:
   - Delete line 48: renameModalVisible state
   - Delete lines 569-584: handleRename function
   - Delete lines 769-778: Rename button from table action column
   - Delete lines 1060-1073: Rename modal JSX
   - Delete line 48: remove renameModalVisible from state declarations
2. In vdiApi.ts:
   - Remove rename() method from vmApi object
3. In vdi.ts:
   - Remove rename-related type definitions if present</action>
  <verify>grep -n "rename\|Rename" xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx xingran-react-frontend/src/lib/vdiApi.ts returns no results (excluding comments and variable names like 'handleRename')</verify>
  <done>Frontend rename UI and API completely removed</done>
</task>

<task type="auto">
  <name>Task 5: Remove name field and auto-generation from VM creation</name>
  <files>xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx, internal/services/vdi/vm_service.go</files>
  <action>Remove name input and auto-generation logic from VM creation:
1. In VirtualMachineList/index.tsx:
   - Delete lines 971-973: Name input Form.Item (required validation)
   - Delete lines 975-994: Name suffix input Form.Item
   - Delete lines 214-230: Auto-name generation useEffect
   - Remove 'name' from form.setFieldsValue calls in handleCreate
2. In vm_service.go:
   - Change line 14: Remove validate:"required" from Name field, make it optional with json:"name,omitempty"
   - Keep Name field in CreateVMServiceRequest but make optional (VDI server will auto-generate)</action>
  <verify>
    grep -n "name.*required\|name_suffix\|VDI-" xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx returns no results (excluding comments)
    grep -n 'Name.*json:"name"' internal/services/vdi/vm_service.go shows 'omitempty' tag
  </verify>
  <done>Name input and auto-generation removed from VM creation, name field now optional</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → Backend API | HTTP requests with JWT authentication |
| Backend → VDI Server | HTTPS calls with Auth-Token header |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Spoofing | VDI API authentication | accept | Existing VDI authentication mechanism (auth manager) remains unchanged |
| T-quick-02 | Tampering | VM creation requests | accept | Removing name field reduces attack surface, VDI server handles validation |
| T-quick-03 | Information Disclosure | Error messages | accept | Existing error handling remains, no sensitive data exposed |
</threat_model>

<verification>
## Verification Steps

1. **Backend Compilation Check**
   ```bash
   go build ./...
   ```
   Expected: No compilation errors

2. **Frontend Type Check**
   ```bash
   cd xingran-react-frontend && npm run type-check
   ```
   Expected: No type errors

3. **Functionality Verification**
   - Start backend server
   - Navigate to VM list page in browser
   - Verify: No rename button visible in action column
   - Click "Create VM" button
   - Verify: No name input field visible in creation form
   - Submit VM creation (with required fields only)
   - Expected: VM created successfully, VDI server assigns name automatically

4. **API Endpoint Check**
   - Try accessing POST /vdi/vm/{id}/rename endpoint
   - Expected: 404 Not Found
</verification>

<success_criteria>
1. All rename-related code removed from backend (client, service, handler, router)
2. All rename-related code removed from frontend (UI, API client, types)
3. Name input and auto-generation removed from VM creation form
4. Name field in CreateVMServiceRequest made optional (omitempty)
5. Code compiles successfully without errors
6. Frontend type-check passes
7. VM creation works without name field (VDI server auto-generates)
</success_criteria>

<output>
After completion, create `.planning/quick/260602-k1b-vdi/260602-k1b-SUMMARY.md`
</output>
