---
phase: quick
plan: 260529-j3y
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/services/vdi/vdi_types.go
  - internal/services/vdi/vdi_client_service.go
  - internal/api/v1/vdi/vm_handler.go
  - xingran-react-frontend/src/types/vdi.ts
  - xingran-react-frontend/src/lib/vdiApi.ts
  - xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - User can select VTP platform from dropdown when creating VM
    - User can select host position (host.id) from dropdown
    - User can select run position (run_position.id) from dropdown  
    - User can select storage location from dropdown
    - User can select network interface from dropdown
    - Host/run position logic follows father_id rules from vdi_test_standalone.go
    - Backend API accepts and validates all VDI creation fields
    - Frontend form cascades VTP → positions/storage/network selection
  artifacts:
    - path: internal/services/vdi/vdi_types.go
      provides: "Extended CreateVMServiceRequest with VDI creation fields"
      contains: "VTPID, HostID, RunPositionID, DiskID, StorageID, NetworkID"
    - path: internal/services/vdi/vdi_client_service.go
      provides: "VDI API client methods for positions/storage/network/VTP"
      exports: ["GetRunPositions", "GetStorages", "GetNetworks", "GetVTPPlatforms"]
    - path: internal/api/v1/vdi/vm_handler.go
      provides: "Extended VM creation endpoint handler"
      accepts: "CreateVMServiceRequest with VDI fields"
    - path: xingran-react-frontend/src/types/vdi.ts
      provides: "TypeScript types for VDI creation fields"
      contains: "VDIPlatform, RunPosition, Storage, Network interfaces"
    - path: xingran-react-frontend/src/lib/vdiApi.ts
      provides: "Frontend API methods for VDI dropdown data"
      exports: ["listRunPositions", "listStorages", "listNetworks", "listVTPPlatforms"]
    - path: xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx
      provides: "Extended VM creation form with cascading dropdowns"
      contains: "VTP → host/run/storage/network selectors"
  key_links:
    - from: "xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx"
      to: "/vdi/vm"
      via: "vmApi.create with extended CreateVMRequest"
      pattern: "handleCreate.*vmApi.create"
    - from: "internal/api/v1/vdi/vm_handler.go"
      to: "internal/services/vdi/vdi_client_service.go"
      via: "vmService.CreateVM calls VDI client CreateServer"
      pattern: "CreateVM.*CreateServer"
    - from: "xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx"
      to: "/vdi/vm/vtp-platforms"
      via: "vmApi.listVTPPlatforms on form mount"
      pattern: "useEffect.*listVTPPlatforms"
    - from: "scripts/vdi_test_standalone.go"
      to: "internal/services/vdi/vdi_client_service.go"
      via: "Port CreateServer logic to production service"
      pattern: "host.*run_position.*father_id"
---

<objective>
Complete VDI virtual machine creation feature with full VDI API integration

Add host position (host.id/run_position.id logic), storage location, network interface, and VTP platform ID selector to the VM creation form, following the CreateServer method implementation from vdi_test_standalone.go.

Purpose: Enable users to create VDI virtual machines with complete infrastructure configuration including placement, storage, and network settings, matching the full VDI API capabilities.

Output: Working VM creation form with cascading dropdowns (VTP → positions/storage/network) and backend integration that calls VDI API CreateServer endpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@scripts/vdi_test_standalone.go
@internal/services/vdi/vdi_types.go
@internal/models/vdi.go
@internal/api/v1/vdi/vm_handler.go
@xingran-react-frontend/src/types/vdi.ts
@xingran-react-frontend/src/lib/vdiApi.ts
@xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend backend types and service for VDI creation fields</name>
  <files>
    internal/services/vdi/vdi_types.go
    internal/services/vdi/vdi_client_service.go
  </files>
  <action>
    1. Extend CreateVMServiceRequest in vdi_types.go with new required fields:
       - vtp_id int (VTP platform ID)
       - host_id string (host position ID)
       - run_position_id string (run position ID)
       - disk_id string (personal disk ID)
       - storage_id string (storage location ID)
       - network_id string (network interface ID)
       - count int (number of VMs to create, default 1)

    2. Add VDI platform and position types to vdi_types.go:
       - VDIPlatform { id, name, tenant_id }
       - RunPosition { id, name, father_id }
       - VDIStorage { id, name, type, total, avail }
       - VDINetwork { id, name, mode }

    3. Extend VDIClientService interface (or create vdi_client_service.go if missing):
       - GetVTPPlatforms(ctx, vdiServerID) -> []VDIPlatform
       - GetRunPositions(ctx, vtpID) -> []RunPosition
       - GetStorages(ctx, vtpID) -> []VDIStorage
       - GetNetworks(ctx, vtpID) -> []VDINetwork
       - CreateServer(ctx, req) -> CreateServerResponse

    4. Implement host/run position logic from vdi_test_standalone.go CreateServer:
       - host.id = selectedPosition.father_id
       - run_position.id = selectedPosition.id if id != father_id else ""
       - Reference lines 543-554 in vdi_test_standalone.go

    Follow existing service patterns in vdi_service.go and vdi_types.go. Use the existing CreateServerRequest type structure from vdi_types.go (lines 296-305).
  </action>
  <verify>
    <automated>go build ./internal/services/vdi/...</automated>
  </verify>
  <done>
    - CreateVMServiceRequest includes all VDI API fields
    - VDIClientService has methods for VTP/positions/storage/network
    - host/run position logic implements father_id rules correctly
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend backend API handlers and frontend types</name>
  <files>
    internal/api/v1/vdi/vm_handler.go
    xingran-react-frontend/src/types/vdi.ts
    xingran-react-frontend/src/lib/vdiApi.ts
  </files>
  <action>
    1. Update vm_handler.go Create method to accept extended CreateVMServiceRequest
       - No handler logic changes needed (binds to struct automatically)
       - Ensure validation tags are present for new fields

    2. Add TypeScript types to vdi.ts:
       - VDIPlatform interface matching backend
       - RunPosition interface with father_id field
       - VDIStorage interface
       - VDINetwork interface
       - Extend CreateVMRequest with new fields

    3. Extend vdiApi.ts vmApi with new methods:
       - listVTPPlatforms(vdiServerId: string)
       - listRunPositions(vtpId: number, vdiServerId: string)
       - listStorages(vtpId: number, vdiServerId: string)
       - listNetworks(vtpId: number, vdiServerId: string)

    Follow existing patterns in vdiApi.ts (lines 80-93 for resource groups/resources). Use POST /vdi/vm/{endpoint} pattern.
  </action>
  <verify>
    <automated>go build ./internal/api/v1/vdi/... && npm run type-check</automated>
  </verify>
  <done>
    - Backend handler accepts extended request
    - TypeScript types match backend Go structs
    - Frontend API methods follow existing vdiApi patterns
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement frontend form with cascading dropdowns</name>
  <files>
    xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx
  </files>
  <action>
    1. Add new state variables for VDI creation data:
       - vtpPlatforms (VDIPlatform[])
       - runPositions (RunPosition[])
       - storages (VDIStorage[])
       - networks (VDINetwork[])

    2. Add form fields to the create modal (after existing resource_id field):
       - VTP Platform: Select dropdown (cascades from vdi_server_id)
       - Host Position: Select dropdown (cascades from vtp_id, uses father_id logic)
       - Run Position: Select dropdown (cascades from vtp_id, same data source as host)
       - Storage: Select dropdown (cascades from vtp_id)
       - Network: Select dropdown (cascades from vtp_id)
       - Count: InputNumber (default 1, min 1, max 10)

    3. Implement cascading dropdown logic:
       - Load VTP platforms when vdi_server_id changes
       - Load positions/storage/network when vtp_id changes
       - Auto-select first option for required fields
       - Clear dependent fields when parent changes

    4. Implement host/run position selection logic:
       - Show single dropdown for "运行位置"
       - Extract both host_id (father_id) and run_position_id (id) from selection
       - Handle case where id == father_id (run_position.id should be empty)
       - Reference vdi_test_standalone.go lines 543-554

    5. Update handleCreate to include new fields in CreateVMRequest

    Follow existing cascading patterns in the component (lines 76-124 for resource groups/resources). Add form fields after line 125 in the existing create modal form structure.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>
    - Form has all required VDI creation fields
    - Cascading logic works: VTP → positions/storage/network
    - Host/run position selection follows father_id rules
    - Form submission includes all new fields
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → Backend API | User-provided VDI configuration parameters |
| Backend → VDI API | Internal service calls to VDI management endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Injection | VM creation endpoint | mitigate | Validate all VDI API parameters (vtp_id, host_id, storage_id, network_id) against VDI server responses before calling CreateServer |
| T-quick-02 | Tampering | Cascading dropdown state | mitigate | Validate that selected resource/storage/network IDs belong to the same VTP platform on backend |
| T-quick-03 | DoS | VDI API calls | mitigate | Add timeout (30s) and retry limits for VDI API calls, rate limit VM creation endpoint |
| T-quick-04 | Elevation | Resource allocation | mitigate | Validate count parameter (1-10 max), enforce per-user VM quotas |
| T-quick-05 | Spoofing | VDI server credentials | accept | VDI server credentials already encrypted in DB (existing security) |
</threat_model>

<verification>
## Overall Phase Checks

1. **Compilation**: Backend `go build ./...` and frontend `npm run type-check` pass
2. **Form validation**: All required fields show validation errors when empty
3. **Cascading logic**: Changing VTP platform clears dependent selections
4. **VDI API integration**: Backend can call GetRunPositions/GetStorages/GetNetworks successfully
5. **Host/Run position logic**: father_id rules implemented correctly per vdi_test_standalone.go
6. **End-to-end**: Can create VM with full configuration (VTP + host/run + storage + network + count)
</verification>

<success_criteria>
## Measurable Completion

- [ ] Backend accepts extended CreateVMServiceRequest with all VDI fields
- [ ] VDIClientService implements GetVTPPlatforms/GetRunPositions/GetStorages/GetNetworks
- [ ] Host/run position logic follows vdi_test_standalone.go father_id rules
- [ ] Frontend form has 5 new dropdowns: VTP Platform, Host Position, Run Position, Storage, Network
- [ ] Cascading logic works: VTP selection loads positions/storage/network options
- [ ] TypeScript types match backend Go structs
- [ ] Form submission includes all new fields in API call
- [ ] Existing VM creation functionality (name, resource, CPU/memory/disk) still works
- [ ] No compilation errors in Go or TypeScript
</success_criteria>

<output>
After completion, create `.planning/quick/260529-j3y-vdi-host-id-run-position-id-vtp-id-vdi-t/260529-j3y-SUMMARY.md`
</output>
