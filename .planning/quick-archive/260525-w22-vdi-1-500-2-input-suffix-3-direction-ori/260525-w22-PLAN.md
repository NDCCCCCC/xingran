---
phase: quick
plan: 260525-w22
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/v1/vdi/vdi_server_handler.go
  - xingran-react-frontend/src/pages/vdi/VDIServerConfig/index.tsx
  - xingran-react-frontend/src/pages/vdi/VirtualMachineDetail/index.tsx
autonomous: true
requirements: []
user_setup: []
---

<objective>
修复 VDI 服务器配置页面的三个关键问题，确保前端和后端正常工作。

**后端问题**: 分页参数不匹配导致 500 错误 — 前端发送 `current`/`pageSize`，后端期望 `page`/`pageSize`

**前端问题 1**: Input 组件动态 suffix 警告 — 需要检查 Input.Password 组件的动态属性使用

**前端问题 2**: Space 组件使用已弃用的 `direction` 属性 — Ant Design 6.x 改为 `orientation`

Purpose: 修复 VDI 服务器配置页面的三个阻塞性问题，确保列表加载、表单输入和布局渲染正常
Output: 可用的 VDI 服务器配置页面，无 500 错误、无控制台警告、无弃用警告
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@internal/api/v1/vdi/vdi_server_handler.go
@internal/services/vdi/vdi_server_service_impl.go
@xingran-react-frontend/src/pages/vdi/VDIServerConfig/index.tsx
@xingran-react-frontend/src/lib/vdiApi.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 VDI 服务器列表后端 500 错误</name>
  <files>internal/api/v1/vdi/vdi_server_handler.go</files>
  <action>
修复 VDI 服务器列表接口的分页参数不匹配问题：

1. 修改 `List` 方法中的参数结构体，将 `Page` 改为 `Current` 以匹配前端发送的 `current` 字段：
   ```go
   var params struct {
       Current  int `json:"current"`
       PageSize int `json:"pageSize"`
   }
   ```

2. 更新后续使用 `params.Page` 的地方改为 `params.Current`

3. 保持默认值逻辑不变（当 current <= 0 时设置为 1）

这修复了前端发送 `{current: 1, pageSize: 10}` 但后端期望 `{page: 1, pageSize: 10}` 导致的 500 错误。
  </action>
  <verify>
访问 VDI 服务器配置页面，列表应该正常加载，无 500 错误。浏览器控制台 Network 面板显示 `/vdi/servers/list` 返回 200 状态码。
  </verify>
  <done>VDI 服务器列表成功加载，显示所有已配置的服务器，分页功能正常</done>
</task>

<task type="auto">
  <name>Task 2: 修复前端 Space 组件弃用警告</name>
  <files>xingran-react-frontend/src/pages/vdi/VDIServerConfig/index.tsx, xingran-react-frontend/src/pages/vdi/VirtualMachineDetail/index.tsx</files>
  <action>
修复 Ant Design 6.x 中 Space 组件的弃用警告：

1. 在 VDIServerConfig/index.tsx 中，将所有 `direction="vertical"` 改为 `orientation="vertical"`
2. 在 VirtualMachineDetail/index.tsx 中，将所有 `direction="vertical"` 改为 `orientation="vertical"`

具体位置：
- VDIServerConfig/index.tsx:234 — `<Space direction="vertical"` → `<Space orientation="vertical"`
- VirtualMachineDetail/index.tsx:215 和 :262 — 同样修改

注意：保持其他属性（size、style等）不变，只修改 direction 为 orientation。
  </action>
  <verify>
刷新 VDI 服务器配置页面和虚拟机详情页面，浏览器控制台无 "Warning: [antd: Space] 'direction' is deprecated" 相关警告。
  </verify>
  <done>VDI 相关页面无 Space 组件弃用警告，布局显示正常</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: 验证 Input 组件警告并修复</name>
  <files>xingran-react-frontend/src/pages/vdi/VDIServerConfig/index.tsx</files>
  <action>
1. 检查 VDIServerConfig 页面中 Input.Password 组件的使用（行 306）
2. 查看是否有动态 suffix 或其他可能导致警告的属性
3. 如果存在警告，修复为静态值或使用正确的方式处理动态属性
4. 如果实际无警告，确认代码已正确实现

重点检查 Input.Password 的 placeholder 属性是否使用了动态值导致 React key 警告。
  </action>
  <verify>
打开 VDI 服务器配置页面，点击"添加服务器"按钮，查看密码输入框。浏览器控制台应无 Input 组件相关的警告信息。
  </verify>
  <done>VDI 服务器表单无 Input 组件警告，密码输入框正常显示和工作</done>
</task>

</tasks>

<verification>
- 后端 VDI 服务器列表接口正常返回数据（200 状态码）
- 前端页面成功加载并显示 VDI 服务器列表
- 浏览器控制台无 Space direction 弃用警告
- 浏览器控制台无 Input 相关警告
- 创建/编辑 VDI 服务器功能正常
- 测试连接功能正常
</verification>

<success_criteria>
1. VDI 服务器列表页面成功加载，显示所有服务器（无 500 错误）
2. 浏览器开发者工具 Console 面板无 Ant Design 弃用警告
3. 浏览器开发者工具 Console 面板无 React Input 警告
4. 分页功能正常（切换页码、修改每页条数）
5. 创建/编辑/删除/测试连接功能全部正常
</success_criteria>

<output>
After completion, create `.planning/quick/260525-w22-vdi-1-500-2-input-suffix-3-direction-ori/260525-w22-SUMMARY.md`
</output>
