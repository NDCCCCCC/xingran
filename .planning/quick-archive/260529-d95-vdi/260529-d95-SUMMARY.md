---
phase: quick
plan: 260529-d95
subsystem: vdi
tags: [vdi, resource-dropdown, cascade, frontend, backend]
dependency_graph:
  requires: [vdi-client-extended, vm-service]
  provides: [list-resources-endpoint, resource-dropdown]
  affects: [vdi/vm_handler, vdi/vm_router, vdi/vm_service, vdi/vm_service_impl, vdi-types, vdiApi, VirtualMachineList]
tech_stack:
  added: []
  patterns: [cascading-dropdown, proxy-endpoint]
key_files:
  created: []
  modified:
    - internal/services/vdi/vm_service.go
    - internal/services/vdi/vm_service_impl.go
    - internal/api/v1/vdi/vm_handler.go
    - internal/api/v1/vdi/vm_router.go
    - xingran-react-frontend/src/types/vdi.ts
    - xingran-react-frontend/src/lib/vdiApi.ts
    - xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx
decisions:
  - "Renamed form field resource_id (for group) to resource_group_id, so resource_id now holds the individual resource ID"
  - "Auto-select first resource when resource list loads for better UX"
metrics:
  duration: 329s
  completed: "2026-05-29"
---

# Quick Task 260529-d95: VDI Resource Dropdown Summary

Cascading resource dropdown in VDI create VM form, backed by new ListResources endpoint proxying VDI API.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add backend ListResources endpoint | 29b80f8 | vm_service.go, vm_service_impl.go, vm_handler.go, vm_router.go |
| 2 | Add frontend resource dropdown with auto-select | a539277 | vdi.ts, vdiApi.ts, VirtualMachineList/index.tsx |

## Changes Made

### Task 1: Backend ListResources Endpoint

- Added `VDIResourceDTO` type to `vm_service.go` with fields: ID, Name, Note, GrpID
- Added `ListResources(ctx, vdiServerID, groupID)` to `VMService` interface
- Implemented `ListResources` in `vmServiceImpl` -- calls `getClient()` then `client.ListResources(ctx, groupID)` and converts to DTOs
- Added `ListResources` handler in `vm_handler.go` accepting `{vdi_server_id, group_id}` JSON body
- Registered `POST /vdi/vm/resources` route in `vm_router.go`

### Task 2: Frontend Resource Dropdown

- Added `VDIResource` TypeScript type in `vdi.ts`
- Added `listResources(vdiServerId, groupId)` method to `vmApi` in `vdiApi.ts`
- In `VirtualMachineList/index.tsx`:
  - Added `resources` state and `VDIResource` import
  - Renamed `selectedResourceId` to `selectedResourceGroupId`, added `selectedResourceFieldId`
  - Renamed resource group form field from `resource_id` to `resource_group_id`
  - Added `useEffect` to load resources when resource group is selected, with auto-select of first item
  - Added resource `<Select>` dropdown between resource group and VM name fields
  - Clear resources when VDI server changes or modal reopens

## Deviations from Plan

None - plan executed exactly as written.

## Self-Check

- All modified files exist on disk
- Commit 29b80f8 exists in git log
- Commit a539277 exists in git log
- Backend `go build ./...` passes
- Frontend `npx tsc --noEmit` passes

## Self-Check: PASSED
