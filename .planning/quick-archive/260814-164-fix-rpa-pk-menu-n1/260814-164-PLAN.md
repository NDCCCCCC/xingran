---
phase: quick-260814-164
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/services/rpa/worker_service.go
  - internal/core/db/migrations/migration_205_rpa_worker_id_default.go
  - internal/core/db/database.go
  - internal/services/system/menu_service.go
autonomous: true
requirements:
  - BUGFIX-RPA-PK
  - BUGFIX-MENU-N1

must_haves:
  truths:
    - "RPA Worker 注册接口（Register）对全新/已存在的 worker_id 均成功写入并返回记录，不再触发 SQLSTATE 23502（id NOT NULL）"
    - "sys_rpa_workers.id 列具备 DEFAULT gen_random_uuid()，与全库 UUID 主键惯例一致"
    - "my-menus / my-menus/all / my-menus/permissions 三个接口的祖先菜单补全（appendAncestorMenuIDs）只发 1 条 SQL，不再 N+1 逐条查询导致累计超时 / context canceled / 500"
  artifacts:
    - path: "internal/services/rpa/worker_service.go"
      provides: "Register 原生 SQL INSERT 显式写入 id 列（gen_random_uuid()）"
      contains: "gen_random_uuid()"
    - path: "internal/core/db/migrations/migration_205_rpa_worker_id_default.go"
      provides: "幂等迁移：sys_rpa_workers.id SET DEFAULT gen_random_uuid()"
      exports: ["Migrate205RpaWorkerIdDefault"]
    - path: "internal/core/db/database.go"
      provides: "AutoMigrate post-block 注册 Migrate205 调用"
      contains: "Migrate205RpaWorkerIdDefault"
    - path: "internal/services/system/menu_service.go"
      provides: "appendAncestorMenuIDs 批量化（一次 Find 加载 id+parent_id，内存上溯）"
      contains: "appendAncestorMenuIDs"
  key_links:
    - from: "internal/services/rpa/worker_service.go (Register)"
      to: "sys_rpa_workers.id (gen_random_uuid())"
      via: "原生 SQL INSERT 首列 id = gen_random_uuid()"
      pattern: "gen_random_uuid\\(\\)"
    - from: "internal/core/db/database.go (AutoMigrate)"
      to: "migrations.Migrate205RpaWorkerIdDefault"
      via: "post-AutoMigrate 调用块（与 Migrate202/203/204 同段）"
      pattern: "Migrate205RpaWorkerIdDefault\\(d\\.DB\\)"
    - from: "internal/services/system/menu_service.go (appendAncestorMenuIDs)"
      to: "sys_menu (单次 SELECT id, parent_id)"
      via: "Model(&models.Menu{}).Select(...).Find(&rows) 一次性批量加载"
      pattern: "Select\\(\"id, parent_id\"\\)"
---

<objective>
修复后端运行日志暴露的两个独立缺陷：

- **问题 A（RPA Worker 注册 400，SQLSTATE 23502）**：`worker_service.Register` 用原生 SQL INSERT 但不带 `id` 列，而 `sys_rpa_workers.id` 当前无可用 DEFAULT → NULL → 违反 NOT NULL。A1 在 SQL 中显式写 `gen_random_uuid()`；A2 补列 DEFAULT 根治并与全库惯例对齐。
- **问题 B（my-menus / my-menus/all / my-menus/permissions 500，context canceled）**：`menu_service.appendAncestorMenuIDs` 经 `collectAncestors` 对每个 menuID 逐条 `SELECT id, parent_id FROM sys_menu WHERE id=?`（N+1），叠加 dev 库单主键查询慢，累计超请求超时。改为一次性批量加载后在内存上溯。

Purpose: 恢复 RPA Worker 自动注册链路 + 消除菜单接口 N+1 超时，两个问题完全独立、互不影响。
Output: 4 个文件（1 新建 + 3 修改），零接口变更，向后兼容。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/code/ClaudeCode/guoguo/CLAUDE.md
@D:/code/ClaudeCode/guoguo/.planning/STATE.md

# 问题 A 现场（已诊断，无需重新调查）
@D:/code/ClaudeCode/guoguo/internal/services/rpa/worker_service.go
@D:/code/ClaudeCode/guoguo/internal/models/rpa/worker.go
@D:/code/ClaudeCode/guoguo/internal/models/base.go

# 问题 A 迁移机制（已确认可靠：Go 注册的 MigrateNNN，post-AutoMigrate 调用块）
@D:/code/ClaudeCode/guoguo/internal/core/db/database.go
@D:/code/ClaudeCode/guoguo/internal/core/db/migrations/migration_204_add_dot1x_user_limit.go

# 问题 B 现场（已诊断，无需重新调查）
@D:/code/ClaudeCode/guoguo/internal/services/system/menu_service.go

<interfaces>
<!-- 执行者直接使用以下契约，无需再探索代码库 -->

## 迁移函数契约（仿 migration_204 模式）
- 包：`migrations`（import path: `github.com/xingran-next/xingran-go-backend/internal/core/db/migrations`）
- 签名：`func MigrateNNNName(db *gorm.DB) error`
- 必用守卫：`isPostgreSQL(db)`（包内既有 helper，非 PG 方言直接 return nil）
- 幂等 SQL：`ALTER TABLE ... ALTER COLUMN id SET DEFAULT gen_random_uuid()`（SET DEFAULT 本身幂等，无 IF NOT EXISTS 语法）
- 错误：return fmt.Errorf 包装；调用方（database.go）记为非阻断日志

## database.go 注册点（AutoMigrate post-block，约 449-468 行）
现有调用形态（与 Migrate202/203/204 同段，错误非阻断）：
  if err := migrations.Migrate204AddDot1xUserLimit(d.DB); err != nil {
      applogger.Errorf("dot1x_user_limit 列迁移失败 (非阻断,留待下次启动): %v", err)
  }
新增 Migrate205 调用照此模式追加。

## menu_service 现有契约（改动范围）
- `appendAncestorMenuIDs(ctx context.Context, menuIDs []string) []string` — 行 266，被 GetUserMenus:398 / GetAllUserMenus:448 / GetUserPermissions:499 三处调用
- `collectAncestors(ctx context.Context, menuID string, result map[string]bool)` — 行 286，N+1 源头，仅被 appendAncestorMenuIDs:274 调用（executor 改前需 grep 确认无其他调用方再删除）
- `models.Menu.ParentID` 类型为 `*string`（nil = 顶级；空串视为 nil，见 normalizeParentID）
- Menu 嵌入 `models.BaseModel`（含 `DeletedAt gorm.DeletedAt`）→ GORM `Find`/`First` 自动带 `deleted_at IS NULL` 软删除过滤，与原 collectAncestors 语义一致
- `buildMenuTree`（行 302）是纯内存递归，不是 N+1 源头，本计划不动
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 修复 RPA Worker 注册主键 NULL（A1 原生 SQL + A2 列 DEFAULT 迁移）</name>
  <files>internal/services/rpa/worker_service.go, internal/core/db/migrations/migration_205_rpa_worker_id_default.go, internal/core/db/database.go</files>
  <behavior>
    - Register 对全新 worker_id：INSERT 成功，id 由 gen_random_uuid() 生成，返回的 worker.ID 非空
    - Register 对已存在 worker_id：ON CONFLICT DO UPDATE 路径命中，返回更新后的记录（worker_id 唯一约束不变）
    - sys_rpa_workers.id 列 DEFAULT = gen_random_uuid()（迁移幂等，重复启动无副作用）
    - GORM 常规 Create 路径（Worker.BeforeCreate → BaseModel.BeforeCreate 显式赋 UUID）行为不变，不受列 DEFAULT 影响
  </behavior>
  <action>
    **A1（worker_service.go，约 116-132 行的 query 字符串）**：在原生 SQL INSERT 的列清单首位增加 `id`，并在 VALUES 占位符首位对应增加 `gen_random_uuid()`，使语句形态为 `INSERT INTO sys_rpa_workers (id, worker_name, worker_id, ip_address, port, status, capabilities, max_concurrency, current_tasks, last_heartbeat, created_at, updated_at) VALUES (gen_random_uuid(), ?, ?, ?, ?, ?, ?, ?, ?, ?, NOW(), NOW()) ON CONFLICT (worker_id) DO UPDATE SET ... RETURNING *`。ON CONFLICT / DO UPDATE / RETURNING * 部分保持不变；`.Raw(query, ...).Scan(worker)` 的占位实参顺序与数量不变（gen_random_uuid() 是 SQL 内嵌函数、不占 Go 占位符）。更新上方注释说明：原生 SQL 不触发 BeforeCreate，故 id 必须由 SQL 的 gen_random_uuid() 提供。不要改 Register 的入参、返回签名、错误包装。

    **A2（新建 migration_205_rpa_worker_id_default.go）**：仿 migration_204 模式创建。包 `migrations`；函数 `Migrate205RpaWorkerIdDefault(db *gorm.DB) error`；开头 `isPostgreSQL(db)` 守卫（非 PG 跳过 return nil）；幂等执行 `ALTER TABLE sys_rpa_workers ALTER COLUMN id SET DEFAULT gen_random_uuid()`（用 `db.Exec`，错误用 `fmt.Errorf` 包装返回）；带与 204 一致的背景注释（说明：原生 SQL 注册路径绕过 BeforeCreate，列需自带 DEFAULT 与全库 UUID 惯例对齐，SET DEFAULT 幂等）。不要用 ADD COLUMN（列已存在）。

    **A2 注册（database.go，AutoMigrate post-block 约 465 行 Migrate204 调用之后）**：在 `if d.Type == "postgres"` 块内、Migrate204 调用之后追加 Migrate205 调用，形态与相邻调用一致：`if err := migrations.Migrate205RpaWorkerIdDefault(d.DB); err != nil { applogger.Errorf("sys_rpa_workers.id DEFAULT 迁移失败 (非阻断,留待下次启动): %v", err) }`。不要把错误升级为阻断（与 175/176/202/203/204 一致）。

    **不要扩大范围**：不改 BaseModel 全局 tag、不把 rpamodels.Worker 加入 MigrateModelList（该表由 legacy SQL 建表，加入 AutoMigrate 会触发未知 DDL 副作用）、不清理 RPA 双模型遗留、不动 Heartbeat/Progress 等其他方法。
  </action>
  <verify>
    <automated>cd D:/code/ClaudeCode/guoguo && go build ./... && go test ./internal/services/rpa/... ./internal/core/db/migrations/...</automated>
  </verify>
  <done>
    - `go build ./...` exit 0
    - worker_service.go 的 INSERT 列清单首列为 `id`，VALUES 首值为 `gen_random_uuid()`（grep gen_random_uuid 命中）
    - migration_205_rpa_worker_id_default.go 存在并导出 Migrate205RpaWorkerIdDefault，含 isPostgreSQL 守卫与 ALTER COLUMN id SET DEFAULT gen_random_uuid() 语句
    - database.go 在 Migrate204 调用之后、同一 postgres 块内调用 Migrate205（非阻断）
    - 既有 rpa / migrations 包测试不回归
    - （运行时确认，若 dev server 在跑）RPA Worker 注册 POST 返回 200 且响应体 id 非空
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 消除菜单祖先补全 N+1（appendAncestorMenuIDs 批量化）</name>
  <files>internal/services/system/menu_service.go</files>
  <behavior>
    - appendAncestorMenuIDs 行为与原实现等价：对入参每个 menuID 向上收集所有祖先 id，并入结果集（含入参自身）
    - 终止条件与原 collectAncestors 一致：parent_id 为 nil 或空串则停止；节点不存在（已被软删）则停止该链
    - 全过程对 sys_menu 只发 1 条 SELECT（取 id, parent_id），不再逐 menuID 逐层查询
    - GetUserMenus / GetAllUserMenus / GetUserPermissions 三个调用点的返回结果（菜单集 / 权限集）与修复前语义一致
    - 加 visited 防御潜在环路（菜单树理论无环，防御无害）
  </behavior>
  <action>
    改写 `appendAncestorMenuIDs`（约 266 行）为批量化实现：用 `s.db.WithContext(ctx).Model(&models.Menu{}).Select("id, parent_id").Find(&rows)` 一次性加载全表 id 与 parent_id 到一个本地切片（定义本地 struct，字段 ID string gorm:"column:id" 与 ParentID *string gorm:"column:parent_id"；GORM 自动带 deleted_at IS NULL，与原 `.First()` 软删除语义一致）。把切片归一为 `map[string]*string`（id → parentID）。然后对入参每个 menuID，在 map 上循环上溯：取出 current 的 parentID，遇 nil / 空串 / key 不存在即终止；用每条链独立的 visited map 防环，命中 visited 即终止；否则把 parentID 加入结果集并把 current 推进到 parentID 继续上溯。最后把结果集 map 收集为切片返回（入参为空时直接返回空切片）。查询失败时降级返回原始 menuIDs（与原 collectAncestors “查询出错即 return 停止”的容错语义一致，不阻断主流程）。

    删除 `collectAncestors`（约 286 行）。删除前先 grep 确认其唯一调用方是 appendAncestorMenuIDs（本任务预期如此）；若发现其他调用方则保留 collectAncestors 不删，仅改 appendAncestorMenuIDs 不再调用它，并在 SUMMARY 注明。

    **不要动**：buildMenuTree（纯内存递归，非 N+1 源头）、GetUserMenus/GetAllUserMenus/GetUserPermissions 的其余逻辑、deleteChildrenRecursive（已是 WITH RECURSIVE 好榜样）。不要引入新依赖、不要改 MenuService 接口签名。
  </action>
  <verify>
    <automated>cd D:/code/ClaudeCode/guoguo && go build ./... && go test ./internal/services/system/...</automated>
  </verify>
  <done>
    - `go build ./...` exit 0
    - appendAncestorMenuIDs 内对 sys_menu 的查询缩减为单次 `Select("id, parent_id").Find(...)`（grep "id, parent_id" 命中且位于 appendAncestorMenuIDs 路径）
    - 不再存在 collectAncestors 的逐条 `.First()` 上溯（若 grep 确认 collectAncestors 唯一调用方为本函数，则该函数已被删除）
    - 祖先收集带 visited 防环与 nil/空串/key-missing 终止条件
    - 既有 system 包测试不回归
    - （运行时确认，若 dev server 在跑）my-menus / my-menus/all / my-menus/permissions 三接口返回 200 且不再出现 context canceled
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API（RPA Worker 注册） | RPA 节点自动注册，跨网络写入 sys_rpa_workers；本任务前的鉴权/权限中间件链不变 |
| client → API（my-menus 系列） | 已认证用户请求菜单/权限；本任务不改鉴权，仅改服务层查询形态 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Tampering | RPA Worker 注册（sys_rpa_workers.id） | mitigate | A1 使 id 由服务端 gen_random_uuid() 生成（SQL 内嵌函数），非客户端可控；A2 让列自带 DEFAULT，进一步杜绝任何省略 id 的插入路径写入 NULL。完整性提升，无新攻击面 |
| T-quick-02 | Denial of Service | my-menus / my-menus/all / my-menus/permissions | mitigate | appendAncestorMenuIDs 批量化将 N 次主键查询降为 1 次全表轻量列扫描（菜单表通常几十~几百行），消除累计超时导致的 500 / context canceled |
| T-quick-03 | Information Disclosure | 错误信息 | accept | 沿用既有 fmt.Errorf + response 包装，错误消息不新增敏感字段泄露；与现状一致 |

注：本计划无 npm/pip/cargo 包安装，T-{phase}-SC（供应链）不适用。
</threat_model>

<verification>
- `go build ./...` 全量编译通过（两个 task 的强制门）
- `go test ./internal/services/rpa/... ./internal/core/db/migrations/... ./internal/services/system/...` 既有测试不回归
- 启动后端（若 dev 环境可用）确认：RPA Worker 注册返回 200 且 id 非空；三个 my-menus 接口返回 200 且日志无 context canceled
- 迁移幂等性：Migrate205 重复执行（多次重启）无副作用（SET DEFAULT 本身幂等 + isPostgreSQL 守卫）
</verification>

<success_criteria>
- 两个独立后端缺陷均已修复且向后兼容（无接口签名变更、无破坏性 schema 改动）
- RPA Worker 注册不再 400（SQLSTATE 23502 消失），sys_rpa_workers.id 列对齐全库 UUID DEFAULT 惯例
- 菜单祖先补全从 N+1 降为 1 次查询，my-menus 系列接口不再因累计超时返回 500
- `go build ./...` 与受影响包测试全绿
- 未触碰范围外代码（BaseModel 全局 tag、MigrateModelList、buildMenuTree、菜单鉴权链、RPA 其他方法均未改）
</success_criteria>

<output>
Create `.planning/quick/260814-164-fix-rpa-pk-menu-n1/260814-164-SUMMARY.md` when done
</output>
