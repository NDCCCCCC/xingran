---
phase: quick
plan: 260602-jk9
type: execute
wave: 1
depends_on: []
files_modified:
  - xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "For VMs with status 'in_use' (正在使用), only shutdown and restart buttons are enabled"
    - "For VMs with status 'stopped' (关机), only startup button is enabled"
    - "For VMs with other statuses (pending/suspended), all power operation buttons follow the same logic"
    - "The power control buttons are disabled appropriately based on VM power state"
  artifacts:
    - path: xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx
      provides: "Fixed VM power control button logic"
      min_lines: 30
  key_links:
    - from: "VM power state display"
      to: "Power control button disabled state"
      via: "State-based conditional logic"
      pattern: "power_state.*disabled"
---

<objective>
Fix virtual machine list page power control button logic to match user requirements:
- 正在使用 (in_use): Can execute shutdown/restart, CANNOT execute startup
- 关机 (stopped): Can execute startup, CANNOT execute shutdown/restart

Purpose: Prevent invalid VM power operations that would fail at the API level
Output: Correctly enabled/disabled power control buttons based on VM state
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx
@xingran-react-frontend/src/types/vdi.ts

# Current Issue (lines 888-910)
The button disabled logic checks for `'running'` state which doesn't exist in the actual power states:
- `power_state === 'running'` (line 893) - Invalid state, should check for NOT stopped
- `power_state === 'stopped'` (line 901) - Wrong logic, should check for NOT in_use/running  
- `power_state !== 'running'` (line 909) - Invalid state check

# Actual Power States (from display logic, lines 793-796)
- `pending` - 等待Agent
- `stopped` - 关机  
- `suspended` - 挂起
- `in_use` - 正常使用

# User Requirements
- `in_use` (正在使用): Enable shutdown/restart, Disable startup
- `stopped` (关机): Enable startup, Disable shutdown/restart
- Other states: Follow same pattern (startup if stopped, power ops if running)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix VM power control button disabled logic</name>
  <files>xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx</files>
  <action>
    Update the power control button disabled logic (lines 888-910) to correctly handle VM power states:

    1. **Startup button** (line 888-895):
       - Change disabled condition from `record.power_state === 'running'`
       - To: `record.power_state !== 'stopped'` (only enable when VM is stopped)

    2. **Shutdown button** (line 896-903):
       - Change disabled condition from `record.power_state === 'stopped'`
       - To: `record.power_state === 'stopped' || record.power_state === 'suspended' || record.power_state === 'pending'`
       - Alternative: `record.power_state === 'stopped'` (keep simple, only enable when VM is in_use)

    3. **Restart button** (line 904-911):
       - Change disabled condition from `record.power_state !== 'running'`
       - To: `record.power_state !== 'in_use' && record.power_state !== 'suspended'`
       - Alternative: `record.power_state === 'stopped' || record.power_state === 'pending'`

    The key insight: Use actual power states from the API (`stopped`, `in_use`, `suspended`, `pending`) instead of non-existent `'running'` state.
  </action>
  <verify>
    <automated>
      # Visual verification - check the UI logic
      # Open the VM list page and verify button states match:
      # - in_use VMs: Startup disabled, Shutdown/Restart enabled
      # - stopped VMs: Startup enabled, Shutdown/Restart disabled
    </automated>
  </verify>
  <done>
    Power control buttons correctly enable/disable based on VM power state:
    - Startup: Only enabled when power_state === 'stopped'
    - Shutdown: Enabled when power_state === 'in_use' (or similar running states)
    - Restart: Enabled when power_state === 'in_use' (VM is running)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend UI state | Button disabled state based on API data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Tampering | Button disabled logic | accept | UI validation only - API will reject invalid operations anyway |
| T-quick-02 | Information disclosure | Power state visibility | accept | Standard VM status display, no sensitive data |
</threat_model>

<verification>
After implementation, verify:
1. Open VM list page
2. Check VMs with different power states
3. Confirm button disabled states match requirements
4. Test actual button clicks to ensure API operations work
</verification>

<success_criteria>
- [ ] Startup button only enabled for stopped VMs
- [ ] Shutdown button only enabled for in_use/running VMs  
- [ ] Restart button only enabled for in_use/running VMs
- [ ] No references to non-existent 'running' state in button logic
- [ ] UI behavior matches user requirements
</success_criteria>

<output>
After completion, create `.planning/quick/260602-jk9-vm-list-controls/260602-jk9-SUMMARY.md`
</output>
