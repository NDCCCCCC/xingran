# v1.33 Follow-up Fix Report

Fixed: 2026-09-14
Mode: Autonomous / yolo
Source review: `.planning/debug/v1.33-followup-diagnosis.md`

---

## Summary

- Findings fixed: 5 / 5
- Skipped: 0

All issues resolved. No behavioral regressions introduced.

---

## Fixed Issues

### CR-01: InfoWindow XSS — escapeHtml helper + applied to both HubeiMap files

**Files modified:**
- `xingran-react-frontend/src/utils/security.ts` (new)
- `xingran-react-frontend/src/utils/security.test.ts` (new regression test)
- `xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMap.tsx`
- `xingran-react-frontend/src/pages/operations/building-spaces-3d/components/HubeiMapGL.tsx`

**Fix applied:**
- Created `escapeHtml()` in `src/utils/security.ts` — replaces `& < > " '` with their named HTML entities
- Applied `escapeHtml()` to `building.name` and `building.address` in both `HubeiMap.tsx` and `HubeiMapGL.tsx` InfoWindow template literal interpolations
- Regression test in `security.test.ts` asserts all 5 entity escapes + script injection vector neutralised

**Verification:**
- `npm run type-check` — green
- `npm run lint` — green (no errors in affected files)
- `security.test.ts` — 8 tests pass

---

### WR-01: my-notices permanent Spin — setLoading(false) on empty data

**Files modified:**
- `xingran-react-frontend/src/pages/my-notices/detail.tsx`
- `xingran-react-frontend/src/pages/my-notices/__tests__/detail.test.tsx`

**Fix applied:**
- Moved `setLoading(false)` out of the `if (noticeData)` block into a `finally` block, so it runs unconditionally on success, empty-data, and error paths

**Verification:**
- `npm run type-check` — green
- `npm run lint` — green
- `detail.test.tsx` — 6 tests pass (including new regression test: `data=null → no Spin`)

---

### WR-02: my-duty stale closure — explicit page/pageSize args to loadSchedules

**Files modified:**
- `xingran-react-frontend/src/pages/duty/my-duty/index.tsx`
- `xingran-react-frontend/src/pages/duty/my-duty/__tests__/index.test.tsx`

**Fix applied:**
- `handleSearch`: now calls `loadSchedules(1, paginationProps.pageSize)` with explicit args instead of relying on captured `paginationProps`
- `handleTableChange`: now calls `loadSchedules(newCurrent, newPageSize)` with explicit args

**Verification:**
- `npm run type-check` — green
- `npm run lint` — green
- `my-duty/index.test.tsx` — 3 tests pass (including new regression test: `Select onChange → loadSchedules page=1`)

---

### WR-03: building-spaces-3d level hardcoded — derive from viewLevel

**Files modified:**
- `xingran-react-frontend/src/pages/operations/building-spaces-3d/index.tsx`
- `xingran-react-frontend/src/pages/operations/building-spaces-3d/__tests__/levelDerivation.test.ts` (new)

**Fix applied:**
- Replaced `level: 2 as const` with `level: viewLevel === "map" ? 1 : 2`
- Added `viewLevel` to `loadBuildings` useCallback deps array (required after fix — ESLint caught this)

**Verification:**
- `npm run type-check` — green
- `npm run lint` — green (0 errors)
- `levelDerivation.test.ts` — 4 tests pass (pure logic: map→1, building/floor/workstation→2)

---

### WR-04: DashboardScopeSelector bidirectional invariant — reset scope when disabling isSystem

**Files modified:**
- `xingran-react-frontend/src/components/dashboard/settings/DashboardScopeSelector.tsx`
- `xingran-react-frontend/src/components/dashboard/settings/__tests__/DashboardScopeSelector.test.tsx`

**Fix applied:**
- Added symmetric `else if (newValue.scope === "global") { newValue.scope = "private"; }` branch in `handleIsSystemChange`
- Maintains invariant: `isSystem=true → scope=global` AND `scope=global → isSystem=true`

**Verification:**
- `npm run type-check` — green
- `npm run lint` — green
- `DashboardScopeSelector.test.tsx` — 8 tests pass (including new regression test: disabling isSystem from global resets scope to private)

---

## Regression Test Results

```
Test Files  5 passed (5)
Tests      31 passed (31)
```

All new regression tests pass alongside existing tests in the same files.

---

_Fixed: 2026-09-14_
_Fixer: Claude (gsd-code-fixer)_
