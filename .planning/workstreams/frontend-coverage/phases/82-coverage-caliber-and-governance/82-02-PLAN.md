---
phase: 82-coverage-caliber-and-governance
plan: 02
type: execute
wave: 2
depends_on: [82-01]
files_modified:
  - .github/scripts/check-frontend-coverage.sh
  - .coverage-fe-floors
autonomous: true
requirements: [GOV-03, GOV-05]
must_haves:
  truths:
    - "check-frontend-coverage.sh 只以 statements 维度聚合判定（D-13），全局加权 3.85% ≥ GLOBAL 3.8%（D-14）时 exit 0，不足 exit 1——失败即阻断"
    - "per-dir floor 以 src 一级目录 + pages 二级子目录为粒度（D-05），阈值表从数据文件 .coverage-fe-floors 读取（D-07：ratchet bump 是纯数据变更不动脚本），floor 违例 exit 4、白名单漂移 exit 6（D-10）——两者 exit code 独立可区分（GOV-05）"
    - "--init 模式（D-08）从第一次全量口径 coverage json 自动生成 floor 初值，ratchet 初值 = 当期实测 −0.5 个百分点、下限 0（D-06：测量噪声余量，禁止照抄实测值）"
    - ".coverage-fe-floors 含 GLOBAL 行 + 28 个目录条目——含 (src root) 与 api 两个显式条目（消除 ROADMAP 遗漏的 21 stmts 无主面积），且 json 出现 floors 未登记的目录同样判违例（新目录不得免检）"
    - "gate 防御性收尾：输出无 ^PASS: 行按解析失败 exit 1，gate 不静默通过"
  artifacts:
    - path: ".github/scripts/check-frontend-coverage.sh"
      provides: "全局阈值 gate + per-dir floor gate + 白名单漂移检测 + --init 生成器"
      min_lines: 150
    - path: ".coverage-fe-floors"
      provides: "全局阈值行 + 28 目录 floor 表（ratchet 状态数据）"
      contains: "GLOBAL 3.8"
  key_links:
    - from: ".github/scripts/check-frontend-coverage.sh"
      to: "xingran-react-frontend/coverage/coverage-final.json"
      via: "内联 node -e 扁平化为 TSV（路径规范化三要件）"
      pattern: "xingran-react-frontend/src/"
    - from: ".github/scripts/check-frontend-coverage.sh"
      to: ".coverage-fe-floors"
      via: "第二位置参数读取（GLOBAL 行 + 目录表）"
      pattern: "GLOBAL"
    - from: ".coverage-fe-floors"
      to: ".github/workflows/ci.yml frontend job Coverage gate 步骤"
      via: "82-04 接线时作为脚本第二参数"
      pattern: "check-frontend-coverage\\.sh"
---

<objective>
实现前端主 gate 脚本 check-frontend-coverage.sh（全局阈值 + per-directory floor + 白名单漂移检测三合一，对称后端 check-coverage.sh 的 bash+awk 零依赖范式），并用其 --init 模式从 82-01 产出的 coverage-final.json 生成阈值数据文件 .coverage-fe-floors，完成四个失败分支的本地干跑实证。

Purpose: 这是 4 层防倒退 gate 的第 1、2 层（GOV-03 全局阈值 + GOV-05 per-dir floor）——从 82-04 接线 CI 起任何覆盖率倒退都会让 CI 变红。
Output: 两个新文件（脚本 + 数据文件），exit code 语义 0/1/2/4/6 全部经干跑矩阵实证。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-CONTEXT.md
@.planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-RESEARCH.md
@.planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-PATTERNS.md
@.planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-VALIDATION.md
@.planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-01-SUMMARY.md

<interfaces>
后端结构模板（照抄骨架，逐段行号见 82-PATTERNS.md「check-frontend-coverage.sh」段）：
- .github/scripts/check-coverage.sh：参数检查 exit 2 → profile 缺失软跳过 exit 0 → awk 聚合 + PASS:/FAIL: 判定 → 防御性 no-PASS exit 1 → per-package floor 循环
- 关键语义：awk 数值比较一律 `a + 0 >= b + 0`（反字符串序）；聚合表格式 `%-50s %8d %8d %6.2f%%`；无 ^PASS: 行视为解析失败

CLI 契约（本 plan 定义，82-04/82-05 消费）：
- 用法 1：`bash .github/scripts/check-frontend-coverage.sh <coverage-final.json> <floors-file>` → exit 0/1/2/4/6
- 用法 2：`bash .github/scripts/check-frontend-coverage.sh --init <coverage-final.json>` → stdout 输出 floors 初值内容
- exit codes：0 过 / 1 全局未达或解析失败 / 2 用法错误 / 4 per-dir floor 违例 / 6 白名单漂移

扁平化 TSV 契约（两 gate 脚本共用，P-node 模式）：
- 每文件一行：`relpath<TAB>stmts<TAB>covered<TAB>start-end:hit,start-end:hit,...`
- relpath 形如 `xingran-react-frontend/src/pages/login/index.tsx`（路径规范化：反斜杠→正斜杠、toLowerCase 匹配锚点 `xingran-react-frontend/src/` 后截取——Windows/CI 双兼容）

floors 文件契约（D-07）：
- `GLOBAL 3.8` 一行 + 28 行 `目录<TAB>floor`（TAB 分隔），# 注释行与空行允许
- 目录键 28 个 = pages 16 个二级（operations/system/network/duty/ad-domain/monitor/vdi/workorder/asset/knowledge/dashboard-system/my-notices/login/profile/settings/ad）+ components（整体一个 floor，含全部子目录）+ hooks/lib/utils/store/router/services/design-system/constants/types + `(src root)`（App.tsx/main.tsx）+ api
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 实现 check-frontend-coverage.sh（扁平化 + 全局 gate + per-dir floor + 漂移检测 + --init）</name>
  <files>.github/scripts/check-frontend-coverage.sh</files>
  <read_first>
  - .github/scripts/check-coverage.sh（结构模板：头注释体例、set -euo pipefail + ROOT 锚定、L46-85 参数检查与软跳过、L110-130 awk 聚合与数值比较、L149-154 防御收尾、L188-205 per-package 循环、L227-254 反例——硬编码 ratchet，勿复制）
  - .planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-PATTERNS.md（「check-frontend-coverage.sh」段的行级映射表 + P-gate-skeleton/P-numeric/P-node 三个共享模式）
  - .planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-RESEARCH.md（Pattern 3 扁平化骨架 + Pitfall 3 路径形态 + Pitfall 9 软跳过语义）
  </read_first>
  <action>
  新建 .github/scripts/check-frontend-coverage.sh（#!/usr/bin/env bash），结构自上而下：
  1. 头注释照抄后端体例：purpose（Phase 82 / GOV-03 全局阈值 + GOV-05 per-dir floor + D-10 漂移检测）、Usage 两种用法、Exit codes 0/1/2/4/6、CI hookup 示例（含 working-directory: . 的 Pitfall 4 提示）、Ratchet workflow 说明——摘录后端 L230-241 测量噪声方法论（patch 版本 instrumentation 漂移/CI 本地分支差异/异步时序方差），解释 D-06 的 −0.5pp 来由。
  2. `set -euo pipefail` + `ROOT` 以 BASH_SOURCE 锚定（照抄后端 L46-48）。
  3. 参数解析：首个参数为 `--init` 时进入 init 分支（只需 json 一个参数）；否则两参数（json + floors 文件），缺失或不可读 exit 2（照抄后端 case 锚定相对路径的模式）。
  4. profile 缺失软跳过：json 文件不存在时 stderr 提示 + exit 0（逐字迁移后端 L77-85——测试失败时配合上游 if: always() 的 Upload 步骤）。
  5. node -e 扁平化（P-node）：require json → 对每个键做路径规范化三要件（replace 反斜杠、toLowerCase().indexOf("xingran-react-frontend/src/")、slice 截取；indexOf <0 跳过）→ 每文件输出一行 TSV `relpath\tstmts\tcovered\tranges`（ranges = statementMap 每项 `start.line-end.line:hit` 逗号连接，hit = s[k]>0 ? 1 : 0；s 缺失按空表处理）。结果存变量供后续段共用。
  6. 白名单漂移检测（在聚合判定之前 fail-fast）：扁平化输出的路径列含 `cad-editor` 或 `cad-elements` → stderr 报漂移 + exit 6（D-10：exclude 被删/失效即失败，污染数据不得进入聚合判定）。
  7. 全局 gate：awk 对 TSV 求和 stmts/covered，pct = covered*100.0/stmts，与 floors 文件 GLOBAL 行（`awk '$1=="GLOBAL"{print $2}'` 提取）做 `+0` 数值比较；输出 `PASS: weighted avg X% >= threshold Y%` 或 `FAIL: ...`；打印聚合表行格式 `%-50s %8d %8d %6.2f%%`（本地/CI diff-friendly）。
  8. 防御性收尾：全局 gate 输出无 ^PASS: 行 → exit 1（照抄后端 L149-154，拒绝静默通过）。
  9. per-dir gate：先从 TSV 派生目录键——awk `split($1, seg, "/")`，取 `xingran-react-frontend/src/` 之后的段：首段为 `pages` 则键 = `pages/` + 第二段，否则键 = 首段；文件位于 src 根（无目录段）则键 = `(src root)`。再从 floors 文件读目录表（`grep -v '^#'` 后 `awk '$1!="GLOBAL"'`，cut -f1/2 以 TAB 分列）做双向比对：
     - floors 表中某目录在 json 派生键中不存在 → `FAIL: <dir> not found in profile`（照抄后端语义，计入违例）
     - json 派生键中某目录未在 floors 表登记 → `FAIL: <dir> 未登记 floor（新增目录必须入表）`（防新目录免检，白名单外无无主面积）
     - 已登记目录：awk 求该目录 stmts/covered 加权百分比（非文件平均），与 floor 值 `+0` 数值比较，逐目录输出 PASS:/FAIL: 行（带 covered/stmts 明细）
     - 违例计数 >0 → 汇总后 exit 4；全部通过输出汇总 PASS 行
  10. --init 分支：复用第 5 步扁平化与第 9 步目录派生，聚合每目录 stmts/covered；floor 初值 = max(0, 一位小数舍入(实测% − 0.5))（D-06）；GLOBAL 行 = min(实测全局%, 3.8)（D-14）；stdout 按 floors 契约格式输出（`GLOBAL <v>` + 28 行 `目录<TAB>floor` + 注释头）。跳过一切 gate 判定。
  全程 shell 展开加双引号；不引入任何第三方工具（node/awk/grep/sed 均为 runner 标配）。json 只读——本 plan 不得写入 xingran-react-frontend/coverage/ 下任何文件（与并行 plan 82-03 共享该目录的只读纪律）。
  </action>
  <verify>
  <automated>
bash -n .github/scripts/check-frontend-coverage.sh && bash .github/scripts/check-frontend-coverage.sh --init xingran-react-frontend/coverage/coverage-final.json > /tmp/fe-floors-init.txt && test "$(grep -c '^GLOBAL ' /tmp/fe-floors-init.txt)" = 1 && test "$(grep -vc -e '^#' -e '^$' /tmp/fe-floors-init.txt)" = 29 && grep -qF '(src root)' /tmp/fe-floors-init.txt && grep -qF 'api' /tmp/fe-floors-init.txt && grep -qF 'pages/operations' /tmp/fe-floors-init.txt
  </automated>
  </verify>
  <acceptance_criteria>
  - `bash -n` 语法检查通过
  - `--init` 输出含恰好 1 行 `GLOBAL ` 开头（值为 3.8——实测 3.85 与 3.8 取 min）、非注释非空行恰 29 行（1 GLOBAL + 28 目录）
  - 输出含字面 `(src root)`、`api`、`pages/operations` 三个关键条目
  - 脚本内出现字面 `cad-editor` 漂移检测与 `exit 6`、per-dir 违例 `exit 4`、no-PASS 防御 `exit 1`、用法错误 `exit 2`
  - awk 阈值比较全部使用 `+ 0` 数值强制（脚本内无裸字符串百分比比较）
  </acceptance_criteria>
  <done>脚本双模式（gate / --init）可用：结构对称后端 check-coverage.sh，floor 表外置数据文件（D-07），四个失败分支路径齐备待 Task 2 实证。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: --init 生成 .coverage-fe-floors + 四失败分支干跑矩阵</name>
  <files>.coverage-fe-floors</files>
  <read_first>
  - .github/scripts/check-frontend-coverage.sh（Task 1 产出，核对 --init 输出格式与 exit 路径）
  - .coverage-threshold（后端单值数据文件 analog——同为 git 追踪的仓库根文件）
  - .planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-RESEARCH.md（「实测 per-dir 全表」的 D-06 ratchet 初值列——人工核对面板）
  - .planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-VALIDATION.md（GOV-03/GOV-05 干跑约定）
  </read_first>
  <action>
  1. 生成数据文件：`bash .github/scripts/check-frontend-coverage.sh --init xingran-react-frontend/coverage/coverage-final.json > .coverage-fe-floors`，文件头保留 --init 输出的注释（GLOBAL 行 D-14 出处 + ratchet 语义 + 「bump 本文件即可、不改脚本」提示）。文件落在仓库根（与 .coverage-threshold 同级），**不得**加入 .gitignore。
  2. 人工核对（对照 RESEARCH 实测全表的「D-06 ratchet 初值」列）：28 个目录条目齐全——重点确认 `(src root)`（初值 0.0）与 `api`（初值 0.0）在列、pages/login 61.7、pages/settings 82.6、components 4.9、lib 10.4、constants 38.8。若个别值与 RESEARCH 表有 0.1 级差异，以脚本聚合输出为准（Open Question 3：gate 可复算的实测表是真相源）并在 SUMMARY 记录差异。
  3. 幂等性检查：再次运行 --init 与已存文件 diff 为空（证明数据文件可由 json 完全复算）。
  4. 四失败分支 + 正常分支干跑矩阵，全部在 mktemp 临时目录的副本上操作，严禁改坏正式文件：
     - 正常：对真实 json + 正式 .coverage-fe-floors → exit 0，输出含 `PASS: weighted avg`（3.85 ≥ 3.8）
     - exit 1：副本 floors 的 GLOBAL 行 sed 改 5.0 → gate exit 1 且输出 FAIL 行
     - exit 4：副本 floors 的 `components<TAB>` 行 floor 改 90.0 → gate exit 4 且输出该目录 FAIL 行
     - exit 6：node -e 向 json 副本注入假键 `D:/synthetic/xingran-react-frontend/src/components/cad-editor/Fake.tsx`（带最小 s/statementMap）→ gate exit 6
     - exit 2：无参调用 → exit 2
  </action>
  <verify>
  <automated>
S=.github/scripts/check-frontend-coverage.sh; J=xingran-react-frontend/coverage/coverage-final.json; T=$(mktemp -d)
bash "$S" "$J" .coverage-fe-floors; test $? -eq 0
bash "$S" >/dev/null 2>&1; test $? -eq 2
cp .coverage-fe-floors "$T/f5"; sed -i 's/^GLOBAL .*/GLOBAL 5.0/' "$T/f5"; bash "$S" "$J" "$T/f5" >/dev/null 2>&1; test $? -eq 1
cp .coverage-fe-floors "$T/f90"; sed -i $'s/^components\t.*/components\t90.0/' "$T/f90"; bash "$S" "$J" "$T/f90" >/dev/null 2>&1; test $? -eq 4
node -e "const fs=require('fs');const d=JSON.parse(fs.readFileSync(process.argv[1],'utf8'));d['D:/synthetic/xingran-react-frontend/src/components/cad-editor/Fake.tsx']={s:{'0':0},statementMap:{'0':{start:{line:1},end:{line:1}}}};fs.writeFileSync(process.argv[2],JSON.stringify(d))" "$J" "$T/drift.json"; bash "$S" "$T/drift.json" .coverage-fe-floors >/dev/null 2>&1; test $? -eq 6
bash "$S" --init "$J" | diff - .coverage-fe-floors && echo INIT_IDEMPOTENT
  </automated>
  </verify>
  <acceptance_criteria>
  - .coverage-fe-floors 入库于仓库根：`GLOBAL 3.8` 一行 + 28 个 TAB 分隔目录行（`(src root)` 与 `api` 在列）
  - 干跑矩阵五个 exit code 逐一命中：真实数据 0 / 无参 2 / GLOBAL 5.0 → 1 / components 90.0 → 4 / 注入 cad 假键 → 6
  - `--init` 重跑与已存文件 diff 为空（输出 INIT_IDEMPOTENT）
  - exit 4 分支输出含 components 目录的 FAIL 行；exit 6 分支 stderr 报白名单漂移
  - 正式 .coverage-fe-floors 与 coverage/ 目录未被矩阵污染（矩阵只写 mktemp 副本）
  </acceptance_criteria>
  <done>GOV-03/GOV-05 的 gate 逻辑与数据文件双双落地且全分支实证——exit 1/4/6 独立可区分，「floor 违例以独立 exit code 在 CI 可区分」的 SC-5 达成；数据文件就绪供 82-04 接线、82-05 校准。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate 脚本 ← coverage-final.json / .coverage-fe-floors | 输入为工具产物与仓库数据文件——解析失败被当成功 = 绕过质量门 |
| .coverage-fe-floors ← ratchet bump PR | 阈值数据可被人为单向调低（repudiation 面） |
| coverage 口径 ← 白名单 exclude | exclude 被删导致白名单目录重回分母（漂移） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-02-01 | Elevation of Privilege | gate 静默通过（解析失败/空输入当成功） | mitigate | set -euo pipefail + 参数缺失 exit 2 + 软跳过仅限 json 文件不存在场景 + 输出无 ^PASS: 行按失败 exit 1（后端验证过的防御收尾模式） |
| T-82-02-02 | Repudiation | .coverage-fe-floors 阈值被单向调低无记录 | mitigate | ratchet bump 必须与 .planning/frontend-coverage-baseline.md 追加同一 commit（后端 D-04 对称纪律）；任何下调在 PR diff 与基线文档双重可见 |
| T-82-02-03 | Tampering | 白名单漂移（exclude 被删、面积超标无察觉） | mitigate | 扁平化后 fail-fast 检测 cad-editor/cad-elements 路径 → exit 6（D-10）；新增白名单须 milestone 级显式决策 + 面积重核算（D-12） |
| T-82-02-04 | Tampering | 新增 src 目录绕过 floor（未登记目录免检） | mitigate | per-dir 双向比对：json 派生目录未在 floors 表登记即判违例（exit 4），保证白名单外无无主面积 |
| T-82-SC | Tampering | 包安装 | accept | 零新增依赖（bash/awk/node 均为 runner 与前端工具链既有运行时）；出现安装需求必须停下评审 |
</threat_model>

<verification>
- `bash -n` 过；--init 输出 29 行（1 GLOBAL + 28 目录）且幂等
- 五分支 exit code 矩阵全命中（0/2/1/4/6）
- awk 数值比较无字符串序陷阱（+0 强制）；输出格式与后端 diff-friendly 约定一致
</verification>

<success_criteria>
- GOV-03：全局阈值 gate（statements 维度、失败即阻断）本地实证
- GOV-05：per-dir floor gate 独立 exit code 实证，28 目录 ratchet 初值按 D-06 生成
- D-05/D-06/D-07/D-08/D-10/D-13/D-14 全部字面落实在脚本与数据文件中
</success_criteria>

<output>
Create .planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-02-SUMMARY.md when done
</output>
