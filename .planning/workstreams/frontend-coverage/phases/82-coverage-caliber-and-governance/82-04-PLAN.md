---
phase: 82-coverage-caliber-and-governance
plan: 04
type: execute
wave: 3
depends_on: [82-02, 82-03]
files_modified:
  - .github/workflows/ci.yml
  - .planning/frontend-coverage-baseline.md
autonomous: true
requirements: [GOV-02, GOV-03, GOV-04, QUAL-02]
must_haves:
  truths:
    - "ci.yml 的 frontend job：Test 步骤替换为 npm run test:coverage，其后内嵌 Coverage gate 步骤（D-01：全局阈值 + per-dir floor gate 与测试同 job，避免 npm ci + 全量测试重跑），gate exit 1/4/6 任一即 job 红"
    - "新增 frontend-coverage-diff job：needs: frontend + if: github.event_name == 'pull_request' + download-artifact 复用 frontend job 上传的 vitest json——零重复测试（D-02）"
    - "coverage html 目录随 frontend-coverage artifact 上传供本地 debug 下载（D-03：text/json/html 三 reporter 的 CI 侧落地）；frontend job timeout-minutes 保持 15（D-04：先观察全量 transform 增量，不预先上调）"
    - "backend 与 coverage-diff 两个 job 零字节改动（并行 workstream milestone v1.27 共享编辑区——只增不改纪律）"
    - ".planning/frontend-coverage-baseline.md 落盘（D-09：与后端 .planning/coverage-baseline.md 对称并列）：起点 3.85% / 21574 stmts + 28 目录 per-dir 快照 + 与后端同 schema 的 ratchet 记录表（GOV-02）"
    - "白名单登记三项齐全（排除理由 / 面积 / 复审条件），合计 1028/13 文件 = 4.55% ≤ 5% 总语句；复审条件为定量触发（D-11：自身 ≥70% 可启动移除 + milestone 收口强制重审），登记锁死仅 cad-editor 与 cad-elements 两项（D-12）"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "frontend job 内嵌 gate + PR-only diff job 的 CI 编排"
      contains: "check-frontend-coverage.sh"
    - path: ".planning/frontend-coverage-baseline.md"
      provides: "前端覆盖率基线（ratchet 记录 + per-dir 快照 + 白名单登记）"
      min_lines: 80
  key_links:
    - from: ".github/workflows/ci.yml frontend job Coverage gate 步骤"
      to: ".github/scripts/check-frontend-coverage.sh + .coverage-fe-floors"
      via: "working-directory: . 下双参数调用"
      pattern: "check-frontend-coverage\\.sh .* \\.coverage-fe-floors"
    - from: ".github/workflows/ci.yml frontend-coverage-diff job"
      to: ".github/scripts/check-frontend-diff-coverage.sh + frontend-coverage artifact"
      via: "download-artifact@v4 复用 json + origin/base_ref 三参数调用"
      pattern: "check-frontend-diff-coverage\\.sh"
    - from: ".github/workflows/ci.yml Upload coverage artifact 步骤"
      to: "frontend-coverage-diff job"
      via: "artifact 名 frontend-coverage（json + html 目录）"
      pattern: "name: frontend-coverage"
---

<objective>
把 82-02/82-03 产出的两个 gate 脚本接进 CI（D-01 内嵌 frontend job + D-02 PR-only diff job），并落盘前端覆盖率基线文档 .planning/frontend-coverage-baseline.md（GOV-02 起点快照 + ratchet 记录表 + QUAL-02 白名单登记段）。

Purpose: 至此 4 层防线中的 3 层在 CI 上线（全局阈值 / per-dir floor / PR diff coverage；baseline ratchet 是第 4 层的文档载体），覆盖率倒退从此让 CI 变红。基线文档是 ratchet 只升不降的审计载体。
Output: ci.yml 前端侧改造 + 新基线文档；backend/coverage-diff job 保持零改动。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-CONTEXT.md
@.planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-PATTERNS.md
@.planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-02-SUMMARY.md
@.planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-03-SUMMARY.md
@.planning/workstreams/frontend-coverage/STATE.md

<interfaces>
ci.yml 现状锚点（171 行；行号以当前 main 为准）：
- backend job L27-90（含 Test/coverage gate/Upload 三段——D-01 的对称模板，只读参照）
- coverage-diff job L92-119（PR-only diff job——D-02 的逐字段模板，只读参照）
- frontend job L121-170：runs-on ubuntu-latest / timeout-minutes: 15 / defaults.run.working-directory: xingran-react-frontend（Pitfall 4 根源）/ 步骤序 checkout → setup-node → Install → Format check → Lint → Type check → Test → Build
- concurrency（L19-21）与 permissions: contents: read（L23-24）一律不动

被接线资产（82-02/82-03 产出）：
- bash .github/scripts/check-frontend-coverage.sh xingran-react-frontend/coverage/coverage-final.json .coverage-fe-floors（exit 0/1/2/4/6）
- bash .github/scripts/check-frontend-diff-coverage.sh xingran-react-frontend/coverage/coverage-final.json origin/<base_ref> 80（exit 0/1/2）
- .coverage-fe-floors（GLOBAL 3.8 + 28 目录 floor 表）

基线文档 schema（后端 .planning/coverage-baseline.md 对称）：
- ratchet 记录表列名逐字复用：date / phase_label / weighted_avg / total_stmts / total_covered / 0pct_pkg_count / commit / phase_executor / ratchet_from / ratchet_to
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ci.yml 接线——frontend job 四步改造 + 新增 frontend-coverage-diff job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
  - .github/workflows/ci.yml（全文——backend/coverage-diff/frontend 三 job 现状与行锚点）
  - .planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-PATTERNS.md（「ci.yml」段：目标态 YAML 逐字段模板 + Pitfall 4 working-directory 陷阱）
  - .planning/workstreams/frontend-coverage/STATE.md（Blockers：ci.yml 为两 workstream 共享编辑区，严禁触碰 backend job）
  </read_first>
  <action>
  只动 frontend job 与文件末尾追加，backend（L27-90）与 coverage-diff（L92-119）两个 job 一字不改：
  1. frontend job 内替换 Test 步骤（L165-167）为：
     - name: `Test (coverage)`；run: `npm run test:coverage`；注释保留「package.json 已改 vitest run --coverage」说明
  2. 在 Test (coverage) 之后、Build 之前插入两步：
     - name: `Coverage gate`；**必须加 `working-directory: .`**（覆盖 job 级 working-directory——Pitfall 4：否则 .github/scripts 相对 frontend 目录解析不到）；run: `bash .github/scripts/check-frontend-coverage.sh xingran-react-frontend/coverage/coverage-final.json .coverage-fe-floors`；**不加 if: always()**（gate 只在 Test 成功产出 json 后跑；json 缺失软跳过逻辑在脚本内）
     - name: `Upload coverage artifact`；`if: always()`；uses: actions/upload-artifact@v4；with: name `frontend-coverage`（与后端 backend-coverage 命名对齐）、path `xingran-react-frontend/coverage/`（相对 workspace root，非 working-directory——upload 自动 zip 含 json + html，D-03）、retention-days 30
  3. `Build` 步骤保持在 gate 之后（现顺序不动，只在其前插入）。
  4. frontend job 的 timeout-minutes 保持 15 不动（D-04）。
  5. 文件末尾（frontend job 之后）新增 `frontend-coverage-diff` job，逐字段对称 coverage-diff job：runs-on ubuntu-latest / timeout-minutes: 10 / needs: frontend / if: github.event_name == 'pull_request' / 步骤：checkout@v7 且 fetch-depth: 0（三点 merge-base diff 需全历史）→ download-artifact@v4 name frontend-coverage → run: `bash .github/scripts/check-frontend-diff-coverage.sh xingran-react-frontend/coverage/coverage-final.json origin/${{ github.base_ref }} 80`（新 job 无 defaults，从 workspace root 跑；下载的 artifact 还原到 workspace root 下原相对路径）。注释说明 D-02 复用语义与 PR-only 原因（push 无 diff base）。
  6. workflow 顶部 job 注释块（L3-5）补一行 frontend 步骤序说明（lint → type-check → test:coverage → gate → build）。
  收尾自检（verify 执行）：backend + coverage-diff 段与 main 的区间 diff 必须为空。
  </action>
  <verify>
  <automated>
python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml', encoding='utf-8')); print('YAML OK')"
diff <(git show main:.github/workflows/ci.yml | sed -n '/^  backend:/,/^  frontend:/p') <(sed -n '/^  backend:/,/^  frontend:/p' .github/workflows/ci.yml) && echo SHARED_JOBS_UNCHANGED
FE="$(sed -n '/^  frontend:/,/^  frontend-coverage-diff:/p' .github/workflows/ci.yml)"
test -n "$FE" && test "$(echo "$FE" | grep -n 'name: Test (coverage)' | cut -d: -f1)" -lt "$(echo "$FE" | grep -n 'name: Coverage gate' | cut -d: -f1)" && test "$(echo "$FE" | grep -n 'name: Coverage gate' | cut -d: -f1)" -lt "$(echo "$FE" | grep -n 'name: Upload coverage artifact' | cut -d: -f1)" && test "$(echo "$FE" | grep -n 'name: Upload coverage artifact' | cut -d: -f1)" -lt "$(echo "$FE" | grep -n 'name: Build' | cut -d: -f1)" && echo STEP_ORDER_OK
grep -q 'needs: frontend' .github/workflows/ci.yml && grep -q 'name: frontend-coverage' .github/workflows/ci.yml && grep -q 'fetch-depth: 0' .github/workflows/ci.yml && test "$(grep -c 'timeout-minutes: 15' .github/workflows/ci.yml)" = 1
  </automated>
  </verify>
  <acceptance_criteria>
  - pyyaml 解析 ci.yml 通过（输出 YAML OK）
  - backend + coverage-diff 区间与 main 逐字节一致（输出 SHARED_JOBS_UNCHANGED——在 commit 前执行；若已 commit 则对照 HEAD~1）
  - frontend job 步骤序：Test (coverage) → Coverage gate → Upload coverage artifact → Build（输出 STEP_ORDER_OK）
  - frontend-coverage-diff job 含 needs: frontend / if: github.event_name == 'pull_request' / download-artifact name frontend-coverage / fetch-depth: 0 / check-frontend-diff-coverage.sh 调用带 origin/${{ github.base_ref }} 与 80
  - Coverage gate 步骤含 working-directory: . 且无 if: always()；timeout-minutes: 15 在 ci.yml 中恰好出现 1 次（仅 frontend job）
  </acceptance_criteria>
  <done>GOV-03/GOV-04 的 CI 编排落地：D-01 内嵌 gate + D-02 PR-only diff job + D-03 artifact 上传，backend/coverage-diff 零改动——待 82-05 真实 CI 实证。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 基线文档 .planning/frontend-coverage-baseline.md + 白名单登记</name>
  <files>.planning/frontend-coverage-baseline.md</files>
  <read_first>
  - .planning/coverage-baseline.md（对称模板：文档头模式、ratchet 表 schema、per-dir 快照、倒退检查清单、Ratchet note blockquote——列名与结构逐字复用）
  - .planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-RESEARCH.md（「实测 per-dir 全表」28 行 + components 细分参考行）
  - .planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-PATTERNS.md（「.planning/frontend-coverage-baseline.md」段——白名单登记表模板）
  - .coverage-fe-floors（82-02 产出——快照数字以其中 floor 值反推的实测为准）
  </read_first>
  <action>
  新建 .planning/frontend-coverage-baseline.md（D-09），章节自上而下：
  1. 文档头（对称后端）：标题「Coverage Baseline: v1.28 前端测试覆盖率优秀」；起点来源（2026-08-23 全量口径实测，旧口径 24.58% 失真已由 Phase 82 修正）；测量口径（statements，coverage.include 全量 src、白名单排除 cad 两目录，白名单前 3.67%/22602 → 白名单后 3.85%/21574）；生成方式（bash .github/scripts/check-frontend-coverage.sh --init + 同脚本 awk 聚合，本地与 CI 同公式）。
  2. ratchet 记录表：列名逐字复用后端 schema（date / phase_label / weighted_avg / total_stmts / total_covered / 0pct_pkg_count / commit / phase_executor / ratchet_from / ratchet_to）。起点行：date 2026-08-23 / phase_label 起点 / weighted_avg 3.85 / total_stmts 21574 / total_covered 830 / 0pct_pkg_count 填 0% 目录实测数（per-dir 快照中 pct 为 0.00 的目录数）/ commit 先写 `TBD (atomic ratchet)` / phase_executor n/a / ratchet_from→to 均 n/a（Phase 82 只建基线不 bump）。
  3. per-dir 快照：fenced code block 内 28 行（目录 / stmts / covered / pct / 文件数），数据源 = .coverage-fe-floors 的 floor 值 + 0.5 还原的实测（或直接重跑 gate 脚本聚合输出）——RESEARCH 实测全表为交叉参照，两者有出入以脚本输出为准并在行尾标注。必须含 `(src root)` 与 `api` 两行。components 细分（dashboard/shared/layout 等 13 行）作为第二个参考 block 附后，注明「非 gate 粒度，供 Phase 84 用」。
  4. 白名单登记段（QUAL-02，D-11/D-12）：表格三列齐全——cad-editor 行（排除理由：重画布 CAD/3D 交互 UI，vitest jsdom 单测性价比极低，对称后端「辅助包不强制覆盖」先例；面积 804 stmts / 8 文件，占总语句 3.56%；复审条件：自身 statements ≥70% 可启动移除（D-11 定量触发）+ 每个 milestone 收口强制重审）、cad-elements 行（同理由；224 / 5 文件 / 0.99%；同条件）、合计行（1028 / 13 / 4.55% ≤ 5%）。表后两条声明：D-12 锁死（仅此两项，新增须 milestone 级显式决策 + 面积重核算）；D-10 同源（排除真相源是 vitest.config.ts coverage.exclude，gate 脚本 exit 6 漂移检测守护）。
  5. 倒退检查清单（对称后端）：本行勾选项——无新增白名单项 / 白名单面积 ≤5% / 全局与 per-dir 数字可由 gate 脚本复算。
  6. Ratchet note blockquote：commit 列 TBD 由 82-05 在 CI 校准定稿后回填实际短 SHA（后端 D-04 同款纪律：.coverage-fe-floors 变更与本文件追加必须同一 commit）。
  数字纪律：全文档数字必须与本地 json 复算一致（见 verify）；若 82-01 记录过实测偏差，按偏差后实测值书写。
  </action>
  <verify>
  <automated>
S=$(node -e "const d=require('./xingran-react-frontend/coverage/coverage-final.json');let s=0;for(const p in d){const st=d[p].s||{};for(const q in st)s++}console.log(s)")
C=$(node -e "const d=require('./xingran-react-frontend/coverage/coverage-final.json');let c=0;for(const p in d){const st=d[p].s||{};for(const q in st)if(st[q]>0)c++}console.log(c)")
test -f .planning/frontend-coverage-baseline.md && grep -q "$S" .planning/frontend-coverage-baseline.md && grep -q "$C" .planning/frontend-coverage-baseline.md && grep -q 'cad-editor' .planning/frontend-coverage-baseline.md && grep -q 'cad-elements' .planning/frontend-coverage-baseline.md && grep -Eq '4\.5[0-9]%' .planning/frontend-coverage-baseline.md && grep -q '复审条件' .planning/frontend-coverage-baseline.md && grep -q 'ratchet_from' .planning/frontend-coverage-baseline.md && grep -q '(src root)' .planning/frontend-coverage-baseline.md && grep -q 'TBD (atomic ratchet)' .planning/frontend-coverage-baseline.md && echo BASELINE_OK "stmts=$S cov=$C"
  </automated>
  </verify>
  <acceptance_criteria>
  - 文档存在于 .planning/frontend-coverage-baseline.md（仓库 .planning/ 根，与后端对称并列——D-09 字面路径）
  - 文档记载的 total_stmts / total_covered 与 node -e 复算 coverage-final.json 的实际值一致（输出 BASELINE_OK stmts=21574 cov=830，或 82-01 记录的偏差后值）
  - 白名单登记表三列齐全（排除理由/面积/复审条件字面在文），合计 4.5x% ≤ 5%，含 D-12 锁死与 D-10 同源两条声明
  - ratchet 记录表列名与后端 schema 逐字一致（含 ratchet_from / ratchet_to / 0pct_pkg_count）；起点行 commit 为 TBD (atomic ratchet)
  - per-dir 快照含 `(src root)` 与 `api` 行，共 28 行 gate 粒度 + components 细分参考 block
  </acceptance_criteria>
  <done>GOV-02 基线落盘 + QUAL-02 白名单登记完成——ratchet 只升不降自此有可审计的文档载体，起点数字可复算，SHA 回填钩子留给 82-05。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ci.yml ← 两个并行 workstream | 共享编辑区——误改/覆盖 milestone workstream 的 backend job 属跨流破坏 |
| gate 步骤 ↔ job 成败语义 | if: always()/continue-on-error 误用会把阻断 gate 弱化为旁路 |
| artifact ← 仓库外消费者 | coverage artifact（json/html）从 CI 泄出的信息面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-04-01 | Tampering | 共享 ci.yml 误改 backend/coverage-diff job | mitigate | 只增不改纪律写入任务指令 + verify 以 `git show main:` 区间 diff 断言 backend+coverage-diff 段零变更 |
| T-82-04-02 | Elevation of Privilege | gate 步骤被 if: always()/continue-on-error 弱化 | mitigate | Coverage gate 步骤不挂 if: always()（对称 backend L71 语义），失败即步红即 job 红；软跳过仅在脚本内且仅限 json 缺失 |
| T-82-04-03 | Information Disclosure | frontend-coverage artifact 外泄 | accept | permissions: contents: read 保持不变；artifact 仅含 coverage json/html（源码本就在仓库内，无 secrets/PII） |
| T-82-04-04 | Repudiation | 基线文档数字与实际不符（审计失效） | mitigate | verify 以 node -e 复算 json 与文档数字比对；后续每次 ratchet bump 与文档追加同 commit（P-ratchet 纪律） |
| T-82-SC | Tampering | 包安装 | accept | 零新增依赖（actions/* 仅用仓库已在用的 checkout/setup-node/upload/download-artifact 官方 action 版本） |
</threat_model>

<verification>
- ci.yml：YAML 解析通过 + 共享 job 区间 diff 为空 + frontend 步骤序正确 + diff job 四件套（needs/PR-only/fetch-depth/artifact）齐全
- 基线文档：数字复算一致 + 白名单三列齐全 + ratchet 表 schema 对称
- 本地全套 gate 干跑仍全绿（82-02/82-03 产物未被接线改动破坏）：三个 gate 调用 exit 0
</verification>

<success_criteria>
- D-01/D-02/D-03/D-04 的 CI 侧全部字面落地，backend job 零改动
- GOV-02/QUAL-02 文档交付物齐备且可复算
- 4 层防线中 3 层已接线 CI（真实 CI 实证留给 82-05）
</success_criteria>

<output>
Create .planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-04-SUMMARY.md when done
</output>
