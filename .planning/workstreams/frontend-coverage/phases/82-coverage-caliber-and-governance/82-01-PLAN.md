---
phase: 82-coverage-caliber-and-governance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - xingran-react-frontend/vitest.config.ts
  - xingran-react-frontend/package.json
autonomous: true
requirements: [GOV-01, QUAL-02]
must_haves:
  truths:
    - "vitest.config.ts 含 include: [\"src/**/*.{ts,tsx}\"]——全量口径生效（GOV-01），切换瞬间 584 个 src 文件（含 75 个 0 语句文件）全部出现在 coverage-final.json，未测试文件以 0% 计入，statements 报告 ≈3.67%（830/22602）"
    - "coverage.thresholds 整段删除（D-16）——gate 唯一真相源移交外部 bash 脚本，npm run test:coverage 在口径切换瞬间不因旧阈值 24 直接失败"
    - "白名单 cad-editor（804 stmts/8 文件）+ cad-elements（224 stmts/5 文件）写入 coverage.exclude（D-10：白名单排除的单一真相源在此），落地后 per-file 报告 571 文件、cad 条目 0、statements 3.85%（830/21574）"
    - "reporter 保持 text/json/html 三种（D-03）；package.json 的 test:coverage 改为 vitest run --coverage，本地干跑不再进入 watch 模式"
    - "现有 19 文件 159 测试全绿，本 plan 零新增测试"
  artifacts:
    - path: "xingran-react-frontend/vitest.config.ts"
      provides: "全量口径 coverage 配置（include + 白名单 exclude，无 thresholds）"
      contains: "src/**/*.{ts,tsx}"
    - path: "xingran-react-frontend/package.json"
      provides: "test:coverage 单次运行语义"
      contains: "vitest run --coverage"
  key_links:
    - from: "xingran-react-frontend/vitest.config.ts coverage.include"
      to: "coverage/coverage-final.json 文件集合"
      via: "vitest v8 provider 全量计入"
      pattern: "xingran-react-frontend/src/"
    - from: "coverage.exclude 白名单两项"
      to: "coverage-final.json 无 cad 条目"
      via: "exclude 过滤"
      pattern: "cad-editor|cad-elements"
---

<objective>
把前端覆盖率测量切换到全量口径（GOV-01）：Vitest 4 已移除 `coverage.all`，必须显式 `coverage.include` 圈定 `src/**/*.{ts,tsx}` 让 584 个文件全部进入报告；同步删除旧口径 thresholds（D-16）、落地白名单 exclude（D-10/QUAL-02 的配置侧）、修复 test:coverage 的 watch 语义。

Purpose: 旧口径 24.58% 只算被 import 文件的失真值是整个 v1.28 里程碑要修的对象；本 plan 是后续所有 gate（82-02/82-03）与基线（82-04）的数据前提。
Output: 改后的 vitest.config.ts / package.json + 本地产出的 coverage-final.json（571 文件口径，供 Wave 2 两脚本只读复用）。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/frontend-coverage/ROADMAP.md
@.planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-CONTEXT.md
@.planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-RESEARCH.md
@.planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-VALIDATION.md

<interfaces>
现状 xingran-react-frontend/vitest.config.ts coverage 块（L16-37）：
- provider: "v8"; reporter: ["text", "json", "html"]; testTimeout: 15000; setupFiles: ["./src/test/setup.ts"] —— 全部保留不动
- 现 exclude: ["node_modules/", "src/test/", "**/*.d.ts", "**/*.config.*", "**/mockData/**", "dist/"]
- 现 thresholds: { statements: 24, branches: 15, functions: 18, lines: 24 }（含其上 Phase 63 历史注释 L27-30）

xingran-react-frontend/package.json scripts（L17-19）：
- "test": "vitest" / "test:ui": "vitest --ui" / "test:coverage": "vitest --coverage"（仅 test:coverage 改）

coverage 产物契约（Wave 2 依赖）：xingran-react-frontend/coverage/coverage-final.json —— istanbul 格式、单行压缩 ~5.6MB、键为绝对路径（Windows 大写盘符+反斜杠 / CI Linux 正斜杠），每文件对象含 s（语句命中计数表）与 statementMap（语句行区间）。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 全量口径切换——include 落地 + thresholds 删除 + run 语义修复</name>
  <files>xingran-react-frontend/vitest.config.ts, xingran-react-frontend/package.json</files>
  <read_first>
  - xingran-react-frontend/vitest.config.ts（被改文件，看清 L16-37 现状）
  - xingran-react-frontend/package.json（scripts 段）
  - .planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-RESEARCH.md（Code Examples「目标态 vitest.config.ts」段 + Pitfall 5/6）
  - .planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-CONTEXT.md（D-03/D-10/D-16 决策原文）
  </read_first>
  <action>
  vitest.config.ts 的 coverage 块做三处修改，其余（provider/reporter/testTimeout/setupFiles/resolve.alias/plugins）一律不动：
  1. 在 `provider: "v8"` 之后新增 `include: ["src/**/*.{ts,tsx}"]`，行尾注释标明 GOV-01 与 D-10（include 是全量口径唯一开关——Vitest 4 移除 coverage.all 后未配置 include 只报告被 import 文件）。
  2. 重写 exclude 数组为两项：`"src/test/"` 与 `"**/*.d.ts"`（include 圈定 src 后，node_modules/、`**/*.config.*`、dist/ 等 root 级模式已失效可删；`**/mockData/**` 实测 src 内无此目录，删）。**本任务不加白名单**——白名单是 Task 2 的两步验证步骤。
  3. 整段删除 thresholds 对象及其上方 Phase 63 历史注释（L27-36），原位置留一行注释说明 D-16：thresholds 已删除，gate 唯一真相源是外部 bash 脚本 + .coverage-fe-floors（保留会让 test:coverage 在口径切换瞬间因 3.85% < 24 直接红）。注意：新注释不得包含 `thresholds:`（带冒号）字样，避免回归 grep 误报。
  4. package.json 仅改 L19 一行：`"test:coverage": "vitest run --coverage"`（加 run 消除本地 watch 挂住；CI=true 环境行为不变）。"test" 与 "test:ui" 保持原样。
  完成后跑一次全量 coverage 验证 584 口径（见 verify）。若文件数 ≠584：先 `git log --oneline -5 -- xingran-react-frontend/src` 确认 1f3a8f0（研究实测基点）之后 src 是否有新增/删除文件；若有则按实测数记录并在 SUMMARY 注明（下游 82-02 的 --init 会以实测为准自愈，无需回改研究文档），但 584/571/22602/21574 四个断言值必须与实测一致或明确记录偏差原因后才可收尾。
  </action>
  <verify>
  <automated>
cd xingran-react-frontend && npm run test:coverage && node -e "const d=require('./coverage/coverage-final.json');const n=Object.keys(d).length;console.log('files='+n);process.exit(n===584?0:1)" && ! grep -q "thresholds:" vitest.config.ts && grep -qF '"test:coverage": "vitest run --coverage"' package.json && grep -qF 'include: ["src/**/*.{ts,tsx}"]' vitest.config.ts
  </automated>
  </verify>
  <acceptance_criteria>
  - `npm run test:coverage` 退出 0：19 文件 159 测试全绿，text 报告显示 All files statements ≈3.67%（830/22602）
  - node -e 断言输出 `files=584`（含 0 语句文件；vitest 自动排除测试文件，19 个 *.test.* 不在 json 中）
  - `grep -q "thresholds:" vitest.config.ts` 无命中（thresholds 键连同 Phase 63 注释已删除）
  - package.json 含字面 `"test:coverage": "vitest run --coverage"`
  - vitest.config.ts 含字面 `include: ["src/**/*.{ts,tsx}"]`，exclude 恰为 `src/test/` 与 `**/*.d.ts` 两项
  </acceptance_criteria>
  <done>全量口径切换生效：584 文件全部入报告、旧 thresholds 双轨移除、test:coverage 单次运行语义就位，159 测试零回归。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 白名单 exclude 落地——571 文件口径收口（两步断言第二步）</name>
  <files>xingran-react-frontend/vitest.config.ts</files>
  <read_first>
  - xingran-react-frontend/vitest.config.ts（Task 1 完成后的状态）
  - .planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-RESEARCH.md（Pitfall 7「SC-1 两步验证」+「实测 per-dir 全表」合计行 21574/830/3.85）
  - .planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-VALIDATION.md（GOV-01 采样约定）
  </read_first>
  <action>
  在 exclude 数组（Task 1 后为两项）追加白名单两项，配注释标 QUAL-02/D-10 与实测面积：
  - `"src/components/cad-editor/**"` —— 804 stmts / 8 文件
  - `"src/components/cad-elements/**"` —— 224 stmts / 5 文件
  注释注明：白名单排除的单一真相源在此（D-10），gate 脚本（82-02）做漂移检测；合计 1028/22602 ≈ 4.55% ≤ 5% 上限。
  改完重跑全量 coverage，完成两步断言的第二步：571 文件 / cad 条目 0 / stmts 21574 / covered 830 / pct 3.85%。这一步产出的 coverage-final.json 是 Wave 2 两个 gate 脚本（82-02/82-03）只读复用的输入——收尾时确认该文件存在于 xingran-react-frontend/coverage/（目录已被 .gitignore L120 忽略，json 不入库，属正常）。
  </action>
  <verify>
  <automated>
cd xingran-react-frontend && npm run test:coverage && node -e "const d=require('./coverage/coverage-final.json');const k=Object.keys(d);const cad=k.filter(p=>p.includes('cad-editor')||p.includes('cad-elements'));let s=0,c=0;for(const p of k){const st=d[p].s||{};for(const q in st){s++;if(st[q]>0)c++}};const pct=(c*100/s).toFixed(2);console.log('files='+k.length,'cad='+cad.length,'stmts='+s,'cov='+c,'pct='+pct);process.exit(k.length===571&&cad.length===0?0:1)" && test "$(grep -cF 'src/components/cad-editor/**' vitest.config.ts)" = 1 && test "$(grep -cF 'src/components/cad-elements/**' vitest.config.ts)" = 1
  </automated>
  </verify>
  <acceptance_criteria>
  - node -e 断言输出 `files=571 cad=0 stmts=21574 cov=830 pct=3.85`（若 Task 1 已记录实测偏差，按记录后的实测口径断言并在 SUMMARY 说明）
  - cad-editor / cad-elements 的 exclude 模式各恰好出现 1 次（grep -cF 计数 =1）
  - xingran-react-frontend/coverage/coverage-final.json 存在且为 571 文件口径（供 Wave 2 只读复用）
  - 159 测试仍全绿
  </acceptance_criteria>
  <done>SC-1 两步断言闭环：切换瞬间 584（Task 1）→ 白名单落地后 571 + 3.85%（Task 2）；白名单配置侧（QUAL-02 的 exclude 同步 + D-10 真相源）完成，登记文档侧由 82-04 落地。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 开发者/CI ← vitest.config.ts | 覆盖率口径定义在此变更——口径被弱化（去掉 include）会让覆盖率虚高且无痕迹 |
| thresholds 删除窗口 → CI gate 上线 | 82-01 至 82-04 之间存在无本地阈值强制的过渡窗口 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01-01 | Tampering | vitest.config.ts coverage 口径 | mitigate | include 显式圈定并配 GOV-01/D-10 注释；任何口径弱化（删 include / 删白名单）在 PR diff 可见，且 82-02 gate 的漂移检测（exit 6）兜底 |
| T-82-01-02 | Elevation of Privilege | thresholds 删除后的过渡窗口 | accept | 窗口限本 phase plan 序列内（82-04 同 phase 接线、82-05 真实 CI 收口）；里程碑边界外不存在无 gate 状态 |
| T-82-SC | Tampering | npm/pip 安装 | accept | 本 plan 零新增依赖、零安装任务；若执行中出现安装需求必须停下评审（触发 Package Legitimacy Gate） |
</threat_model>

<verification>
- 两步口径断言全部成立（584 → 571 + cad=0 + 3.85%）
- 现有 19 文件 159 测试全绿（qualitative 回归保护，本 phase 不新增测试）
- coverage-final.json（571 口径）留存在工作树供 Wave 2 只读复用
</verification>

<success_criteria>
- GOV-01 的配置侧完成：全量口径生效、白名单同步 exclude、未测试文件 0% 计入
- D-03（三 reporter 保留）/ D-10（exclude 真相源）/ D-16（thresholds 删除）字面落实
- 82-02/82-03 的输入数据就绪
</success_criteria>

<output>
Create .planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-01-SUMMARY.md when done
</output>
