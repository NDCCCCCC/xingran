---
phase: 82-coverage-caliber-and-governance
plan: 05
type: execute
wave: 4
depends_on: [82-04]
files_modified:
  - .coverage-fe-floors
  - .planning/frontend-coverage-baseline.md
autonomous: false
requirements: [GOV-03, GOV-04]
must_haves:
  truths:
    - "真实 PR 上 frontend job 绿（含 Test (coverage) 与 Coverage gate 步骤），frontend-coverage-diff job 出现在 PR checks 并通过——GOV-04 的 PR-only 生效证明"
    - "push main 的 CI 运行不含 frontend-coverage-diff job（PR-only 证明——push 无 diff base），frontend job 的 gate 同样绿——GOV-03 失败即阻断在真实 CI 上闭环"
    - "CI 首跑全局 statements 读数记录在案；若 CI 实测低于 3.8，.coverage-fe-floors 的 GLOBAL 行按 CI 读数校准且与基线文档追加同一 commit（D-14 校准口径：数值属 D-07 数据管辖，「失败即阻断」语义不变；禁止把 GLOBAL 调到高于 3.8 或低于 CI 实测）"
    - "基线文档 ratchet 记录表 commit 列回填实际短 SHA + CI 读数出处（run URL），全程可复算审计"
    - "全量口径 Test (coverage) 步骤实际耗时记录在 SUMMARY（D-04 观察项：仅超时才在 phase 末尾调整 timeout，本 plan 不改）"
  artifacts:
    - path: ".planning/frontend-coverage-baseline.md"
      provides: "commit 列回填后的 ratchet 起点定稿行（+可能的 CI 校准记录行）"
      contains: "TBD (atomic ratchet)"
  key_links:
    - from: "PR checks / gh run view 日志"
      to: ".coverage-fe-floors GLOBAL 行（条件校准）与基线文档记录行"
      via: "CI 实测读数 → 数据文件 + 文档同一 commit 落盘"
      pattern: "PASS: weighted avg"
---

<objective>
用真实 GitHub CI 验证 Phase 82 全部交付物：开验证 PR 观察 frontend job（含 gate）与 frontend-coverage-diff job，merge 后观察 main push run（无 diff job），完成 D-14 全局阈值 3.8% 的 CI 首跑校准（余量仅 0.047pp，本地 3.847% vs CI Linux 可能漂移），回填基线文档 SHA。

Purpose: 这是 VALIDATION.md 的 Phase gate（`/gsd:verify-work` 前置）——所有 gate 干跑都绿不等于真实 CI 绿；D-14 的 CI 校准点是研究员明确要求设为显式检查的（RESEARCH Open Questions Q1）。
Output: 三项 CI 证据（PR job 存在且绿 / main push 无 diff job / gate PASS 读数）+ 条件触发的 GLOBAL 校准 commit + 基线文档 SHA 回填。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-CONTEXT.md
@.planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-RESEARCH.md
@.planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-04-SUMMARY.md
@.github/workflows/ci.yml
@.coverage-fe-floors

<interfaces>
已上线资产（82-01~82-04 产出，本 plan 只观察/条件校准）：
- ci.yml frontend job：Test (coverage) → Coverage gate（check-frontend-coverage.sh，输出含 `PASS:/FAIL: weighted avg X% >= threshold Y%`）→ Upload artifact → Build
- ci.yml frontend-coverage-diff job：PR-only，needs frontend，下载 frontend-coverage artifact 跑 check-frontend-diff-coverage.sh（docs-only PR 走空 diff 软通过 exit 0——FAIL 分支已由 82-03 空树合成基线证毕）
- .coverage-fe-floors：GLOBAL 3.8（本地实测 3.847%，余量 0.047pp）
- 基线文档 ratchet note：commit 列 TBD 待本 plan 回填

gh CLI 契约：gh pr create / gh pr checks / gh run list / gh run view --log / gh run watch（仓库已有 gh 使用先例，push 后盯 ci.yml 运行是既定流程）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 验证 PR 三项证据采集 + D-14 CI 校准 + SHA 回填</name>
  <files>.coverage-fe-floors, .planning/frontend-coverage-baseline.md</files>
  <read_first>
  - .github/workflows/ci.yml（接线后全貌——知道 gate 步骤名与 job 名才能抓日志）
  - .coverage-fe-floors（GLOBAL 行现值）
  - .planning/frontend-coverage-baseline.md（ratchet note 与起点行——回填目标）
  - .planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-RESEARCH.md（Open Questions Q1 + Pitfall 2：D-14 余量与校准预案）
  </read_first>
  <action>
  前置：确认工作树干净、82-01~82-04 全部产物已提交并 push 到 main（`git status --short` 为空、`git log origin/main..HEAD` 为空）。
  Git 确认纪律（CLAUDE.md Git Workflow 约定，贯穿全任务）：本 plan 的每一个 git 写操作——push 分支、merge PR、commit + push main——执行前都必须先向用户展示将提交/合并的变更范围（diff 或 PR 链接 + checks 状态），征得用户显式确认（approved）后才执行；任何一步被拒则停下记录现场，不得擅自继续。
  1. 创建验证分支 `chore/82-ci-gate-verify`（从 main），在其上追加一个 docs-only commit：在 .planning/frontend-coverage-baseline.md 末尾追加一行「CI 验证 PR 记录（占位）」即可——不触 xingran-react-frontend/src（diff gate 走空 diff 软通过分支；FAIL 分支已由 82-03 合成基线证毕，勿为测试 FAIL 而制造红 CI）。向用户展示 commit diff 并确认后 push 分支并 `gh pr create --base main --title "chore(82): CI gate verification" --body "Phase 82 gate 上线验证 PR（docs-only）"`。
  2. 盯运行至完成（`gh pr checks --watch` 或 `gh run watch`）。抓取 frontend job Coverage gate 步骤日志：`gh run view <run-id> --log | grep -E "PASS: weighted avg|FAIL:"`，记录 CI 实测全局 statements 读数；同时记录 Test (coverage) 步骤实际耗时（D-04 观察项，写 SUMMARY；15 分钟内完成则不动 timeout）。
  3. 证据一：`gh pr checks` 输出含 frontend-coverage-diff 且状态为 pass（PR-only job 出现并绿）。
  4. D-14 校准（条件分支）：
     - CI 读数 ≥ 3.8 且 gate 绿：无需校准，读数记录进基线文档即可。
     - gate 红（CI 读数 < 3.8）：在 main 上（或先在验证分支再 merge）把 .coverage-fe-floors 的 GLOBAL 行改为 CI 实测读数向下取一位小数（例：CI 3.79 → GLOBAL 3.7——保最小余量而非贴脸），**同一 commit** 在基线文档追加校准记录行（含 run URL 与前后值），push 后确认 PR 复跑 gate 绿。GLOBAL 禁止高于 3.8、禁止低于 CI 实测（校准只允许 min(CI 实测, 3.8) 向下取一位小数）。校准 commit 同样先经用户确认再 push。
  5. merge 验证 PR（squash/merge 均可）前，向用户展示 PR 的 diff 范围（应仅含基线文档改动）与 checks 双绿状态，征得显式确认后再 merge。等待 main 的 push run 完成。
  6. 证据二：`gh run list --branch main --limit 1` 取 run-id，`gh run view <run-id> --json jobs --jq '[.jobs[].name]'` 断言 job 列表不含 frontend-coverage-diff 且 frontend 结论 success（push 侧 PR-only 证明 + gate 在 main 上同样绿）。
  7. 证据三 + 回填：在 main 上把基线文档 ratchet 记录表起点行 commit 列的 `TBD (atomic ratchet)` 替换为实际短 SHA（携带 .coverage-fe-floors 与基线文档最终态的 commit；发生过校准则为校准 commit，否则为 82-04 落盘 commit），同时改写 ratchet note blockquote 中的 TBD 表述，确保全文再无 `TBD (atomic ratchet)` 字样残留（verify 的 `! grep -q 'TBD (atomic ratchet)'` 是全文断言，note blockquote 与表格单元格一并清理）；同 commit 在文档追加「Phase 82 CI 实测读数」行（weighted_avg 填 CI 读数、commit 填验证 PR merge SHA）；把占位行改写为验证记录（三项证据 + run URL）。向用户展示最终 diff、征得显式确认后 commit + push main，确认 push run 仍绿。
  </action>
  <verify>
  <automated>
gh pr checks 2>/dev/null | grep -E 'frontend|coverage-diff' || echo "PR already merged — using run history"
RUNID=$(gh run list --workflow ci.yml --branch main --limit 1 --json databaseId --jq '.[0].databaseId')
gh run view "$RUNID" --json jobs --jq '[.jobs[].name] | join(",")' > /tmp/82-main-jobs.txt
grep -q frontend /tmp/82-main-jobs.txt && ! grep -q frontend-coverage-diff /tmp/82-main-jobs.txt && echo MAIN_PUSH_NO_DIFF_JOB
gh run view "$RUNID" --json jobs --jq '.jobs[] | select(.name=="frontend") | .conclusion' | grep -qx success && echo FRONTEND_GREEN_ON_MAIN
grep -v '^#' .coverage-fe-floors | grep -q '^GLOBAL ' && ! grep -q 'TBD (atomic ratchet)' .planning/frontend-coverage-baseline.md && echo SHA_BACKFILLED
  </automated>
  </verify>
  <acceptance_criteria>
  - 验证 PR 的 checks 含 frontend-coverage-diff 且 pass；frontend job pass（含 Coverage gate 步骤）——SUMMARY 记录两项证据 + PR URL
  - main 最新 push run：job 列表含 frontend（success）且不含 frontend-coverage-diff（输出 MAIN_PUSH_NO_DIFF_JOB + FRONTEND_GREEN_ON_MAIN）
  - Coverage gate 日志的 `PASS: weighted avg X%` 读数已记录进基线文档；若触发校准，.coverage-fe-floors GLOBAL 行 = min(CI 实测, 3.8) 向下取一位小数，且与基线文档追加同 commit，校准后 CI 复跑绿
  - 基线文档无 `TBD (atomic ratchet)` 残留（输出 SHA_BACKFILLED——含 ratchet 记录表与 note blockquote 全文），起点行 commit 为实际短 SHA
  - Test (coverage) 步骤耗时记录在 SUMMARY（对照 15 分钟 timeout 的余量结论）
  - 全部 git 写操作（push 分支 / merge / push main）均有用户显式确认记录（SUMMARY 列出各确认点）
  </acceptance_criteria>
  <done>三项 CI 证据齐备、D-14 校准闭环（或确认无需校准）、基线文档定稿可审计——Phase 82 的 VALIDATION Phase gate 达成，可进入 /gsd:verify-work。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 人工核验——4 层 gate 上线终检</name>
  <what-built>
  Phase 82 全部交付物：全量口径 coverage（571 文件/3.85%）、白名单登记（4.55% ≤ 5%）、全局阈值 + per-dir floor gate（内嵌 frontend job）、PR diff coverage ≥80% gate（PR-only job）、基线文档 + .coverage-fe-floors ratchet 数据——全部经真实 CI 验证。
  </what-built>
  <how-to-verify>
  1. 打开验证 PR 页面（URL 见 82-05-SUMMARY）：确认 checks 里 frontend 与 frontend-coverage-diff 双绿。
  2. 打开 main 最新 ci run：确认 job 列表只有 backend / coverage-diff / frontend（无 frontend-coverage-diff）。
  3. 本地复跑三件套（应全 exit 0）：`bash .github/scripts/check-frontend-coverage.sh xingran-react-frontend/coverage/coverage-final.json .coverage-fe-floors`；`bash .github/scripts/check-frontend-diff-coverage.sh xingran-react-frontend/coverage/coverage-final.json HEAD 80`；`cd xingran-react-frontend && npm run test`（159 测试全绿）。
  4. 审阅 .planning/frontend-coverage-baseline.md：白名单登记三列齐全、起点行数字与 gate 日志读数一致、commit 列已回填真实 SHA。
  </how-to-verify>
  <files>.github/workflows/ci.yml, .planning/frontend-coverage-baseline.md</files>
  <action>
  checkpoint:human-verify（无自动化实现——本任务是 Task 1 已完成证据的人工终检）。执行者按 how-to-verify 四步引导用户核验后，等待 resume-signal；用户回复 approved 则通过，描述问题则回到对应 plan 修复。
  </action>
  <verify>
  <human-check>用户完成 how-to-verify 四步并回复 approved（PR 双绿 / main job 列表无 diff job / 本地三件套 exit 0 / 基线文档三列与 SHA 齐备）</human-check>
  </verify>
  <done>用户 approved——Phase 82 全部交付物经人工终检，phase 可收口进入 /gsd:verify-work。</done>
  <resume-signal>Type "approved" 或描述问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI 实测读数 → 阈值数据文件 | 校准动作本身是改 GLOBAL 行——必须约束为只降不升且留档 |
| 验证 PR → main | PR 的 merge 是写 main 的入口——docs-only 约束防误触业务代码 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-05-01 | Repudiation | CI 校准读数无记录、事后无法审计 | mitigate | 校准值 + run URL 写入基线文档 ratchet 记录行；起点行 commit 列回填真实短 SHA，全程可复算 |
| T-82-05-02 | Tampering | 以「校准」为名单向调低阈值 | mitigate | 校准仅允许 GLOBAL := min(CI 实测, 3.8) 向下取一位小数；禁止高于 3.8（防倒退）也禁止低于 CI 实测（防贴地白给）；任何后续下调走正常 ratchet 流程（PR review + 基线文档同 commit） |
| T-82-05-03 | Tampering | 验证 PR 混入业务/src 改动 | mitigate | 验证 PR 限定 docs-only 单 commit（只动基线文档），merge 前人工核验 diff 范围（merge 前用户显式确认 + checkpoint Task 2 步骤 1-2） |
| T-82-SC | Tampering | 包安装 | accept | 零新增依赖；出现安装需求必须停下评审 |
</threat_model>

<verification>
- 三项 CI 证据命令化断言（PR job 存在且绿 / main push 无 diff job 且 frontend 绿 / gate 读数留档）
- 校准分支（若触发）前后值与 run URL 在基线文档可追溯
- 本地三件套干跑仍全绿（不因接线/校准破坏）
</verification>

<success_criteria>
- GOV-03/GOV-04 在真实 CI 上闭环：gate 红=CI 红的因果在 PR 与 push 两侧都可观察
- D-14 校准决策有据（CI 读数 vs 3.8）且落盘可审计
- Phase 82 全部 SUCCESS Criteria 的验证证据链完整，可交付 /gsd:verify-work
</success_criteria>

<output>
Create .planning/workstreams/frontend-coverage/phases/82-coverage-caliber-and-governance/82-05-SUMMARY.md when done
</output>
