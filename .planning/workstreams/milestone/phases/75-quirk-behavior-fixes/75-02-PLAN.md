---
phase: 75-quirk-behavior-fixes
plan: 2
type: execute
wave: 1
depends_on: ["75-01"]
files_modified:
  - internal/device/model_extractor.go
  - internal/device/device_74_08_test.go
  - internal/device/snmp_client.go
autonomous: true
requirements: [QUIRK-02]
user_setup: []
must_haves:
  truths:
    - "ModelExtractor 对行首/空格分隔 sysDescr 能提取型号(锚点修复,Q-3):'Huawei Platform\\nS5735-L48T4X-A\\nCopyright' → 'S5735-L48T4X-A'"
    - "USG 型号保留尾字母(Q-8):'USG6000E Security Gateway' → 'USG6000E'(不再截断为 USG6000)"
    - "nextIP(255.255.255.255) 返回 nil,且 ScanIPRange 循环条件同 commit 保持 nil 短路优先(Q-9,RESEARCH 硬约束 3:两处必须同一 commit 改)"
    - "回归测试含双路径样本:新提取器(NewModelExtractor)+ 既有 ExtractModelFromSysDescr 回退 caller(落库 model 值会变,SUMMARY 记录差异)"
    - "三个原子 commit:fix(quirk-3) / fix(quirk-8) / fix(quirk-9)"
  artifacts:
    - path: "internal/device/model_extractor.go"
      provides: "锚点修复 + USG 尾字母正则"
      contains: "USG[0-9]{4,5}[A-Z]{0,2}"
    - path: "internal/device/snmp_client.go"
      provides: "nextIP To4 归一 + 回卷返回 nil"
      contains: "func nextIP"
    - path: "internal/device/device_74_08_test.go"
      provides: "翻转后的型号提取断言 + 双路径回归 + nextIP 回归"
      contains: "TestModelExtractor_Extract"
  key_links:
    - from: "internal/services/device_discovery_service.go:607"
      to: "internal/device/model_extractor.go"
      via: "发现链 NewModelExtractor().Extract() 优先,:611 回退 ExtractModelFromSysDescr"
      pattern: "NewModelExtractor"
---

<objective>
修复 device 家族三项 QUIRK:Q-3 ModelExtractor 锚定(行首/空格分隔 sysDescr 命中)、Q-8 USG6000E 尾字母截断、Q-9 nextIP(255.255.255.255) 回卷返回 nil(与 ScanIPRange 循环条件同 commit)。每项一个原子 commit,含锁定断言翻转与双路径回归样本。

Purpose: Q-3/Q-8 修正好让 Phase 78 device 包新测试从第一天断言正确行为(ROADMAP 排序原则);Q-9 消除扫描到 255.255.255.255 时 nextIP 返回垃圾 IP 导致 ipToUint32 内 To4()=nil 解引用的隐患。
Output: 三个原子 commit(fix(quirk-3) / fix(quirk-8) / fix(quirk-9)),internal/device 包全绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/75-quirk-behavior-fixes/RESEARCH.md

关键坐标(已实证):
- internal/device/model_extractor.go — 5 个提取方法重复同一后处理惯用法:`match := re.FindString(...)` 后 `regexp.MustCompile(`^[A-Z0-9\-]+`).FindString(match)`。锚点 `(?:^|[\s\r\n])` 的前导空白字符进入 match,`^` 锚定导致对含前导空白的 match 提取为空 → 仅串首命中(extractHuaweiModel :45 / extractH3CModel :78 / extractRuijieModel :105 / extractMaipuModel :134(其内层正则为 `^[A-Z0-9\s\-]+`)/ extractGenericModel :162)
- USG 正则:model_extractor.go:53 `(?:^|[\s\r\n])USG[0-9]{4,5}(?:-[A-Z0-9]+)?` — 不含尾字母
- internal/device/device_74_08_test.go:39-45(行首 QUIRK 空用例 want="")、:47(USG-串首 want="USG6000")
- internal/device/snmp_client.go:400 ExtractModelFromSysDescr(旧回退,非锚定正则,行首/空格已可命中 — 双路径回归的另一路径)、:698-717 ScanIPRange(循环条件 :710 `ip != nil && ipToUint32(ip) <= ipToUint32(end)` 已是 nil 短路优先)、:725-742 nextIP(16 字节输入回卷时 Equal(net.IPv4zero) 判不中,返回 byte9=1 的垃圾 IP)
- 联动 caller:internal/services/device_discovery_service.go:607(新提取器)/:611(回退)— 生产代码不改,行为差异记录进 SUMMARY
</context>

<tasks>

<task type="auto">
  <name>Task 1: Q-3 — ModelExtractor 锚定修复(行首/空格分隔 sysDescr 命中)+ 双路径回归</name>
  <files>internal/device/model_extractor.go, internal/device/device_74_08_test.go</files>
  <read_first>
  - internal/device/model_extractor.go(全文,226 行 — 5+1 处后处理惯用法)
  - internal/device/device_74_08_test.go:30-63(TestModelExtractor_Extract 用例表)
  - internal/device/snmp_client.go:398-434(ExtractModelFromSysDescr 回退实现)
  </read_first>
  <action>
  原子 commit fix(quirk-3):
  1. 生产修复:在应用 `^[A-Z0-9\-]+`(Maipu 为 `^[A-Z0-9\s\-]+`)提取之前,先把 FindString 的 match 去掉前导空白(`strings.TrimLeft(match, " \t\r\n\f")`,Go 正则 `\s` = `[\t\n\f\r ]`)。在 6 处(extractHuaweiModel/extractH3CModel/extractRuijieModel/extractMaipuModel/extractGenericModel)落地 — 建议提取一个包内小 helper 统一"去锚点前缀 → 正则取 token"两步,避免 6 份拷贝。补 `strings` import。不改任何正则本身(锚点组保留,只修后处理)。
  2. 翻转断言(device_74_08_test.go):删 :39-42 QUIRK 注释;:45 用例 `{"华为 S 系列-行首(QUIRK 空)", "Huawei Platform\nS5735-L48T4X-A\nCopyright", models.VendorHuawei, ""}` 的 want 改为 `"S5735-L48T4X-A"`(用例名同步去掉 QUIRK 标记)。
  3. 补回归用例(同文件用例表,名字标注修复语义):行首 H3C(`"H3C Comware\nS5120-28P-SI Software"`→`"S5120-28P-SI"`)、空格分隔(`"Huawei Versatile S5735-L48T4X-A Platform"`→`"S5735-L48T4X-A"`)、行首锐捷(`"RGOS\nRG-S5750-28GT-P-S"`→`"RG-S5750-28GT-P-S"`)。
  4. 双路径回归(RESEARCH Q-3 要求,落库 model 值会变):新增或扩展用例,对同一样本断言两条路径结果一致 — `NewModelExtractor(sysDescr, vendor).Extract()` 与 `ExtractModelFromSysDescr(sysDescr, vendor)`(snmp_client.go:400,非锚定回退)对 `"Huawei Platform\nS5735-L48T4X-A\nCopyright"` 均返回 `"S5735-L48T4X-A"`;并在用例注释中记录:修复前新提取器返回 ""、发现链靠 :611 回退落库 — 修复后部分设备落库 model 值将从"回退结果"变"新提取器结果"。
  5. `go test ./internal/device/...` 全绿后 commit:`fix(quirk-3): ModelExtractor 锚定修复,行首/空格分隔 sysDescr 命中 + 双路径回归`。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestModelExtractor|TestExtractModelFromSysDescr" ./internal/device/ && git log -1 --oneline | grep -c "fix(quirk-3)"</automated>
  </verify>
  <done>
  行首/空格分隔样本命中;device_74_08_test.go:39-45 QUIRK 注释与空串期望删除;双路径一致断言存在;fix(quirk-3) 单原子 commit。
  </done>
</task>

<task type="auto">
  <name>Task 2: Q-8 — USG 正则补尾字母(USG6000E 不再截断为 USG6000)</name>
  <files>internal/device/model_extractor.go, internal/device/device_74_08_test.go</files>
  <read_first>
  - internal/device/model_extractor.go:47-60(Huawei patterns)
  - internal/device/device_74_08_test.go:46-48
  </read_first>
  <action>
  原子 commit fix(quirk-8):
  1. model_extractor.go:53 USG 正则改为 `(?:^|[\s\r\n])USG[0-9]{4,5}[A-Z]{0,2}(?:-[A-Z0-9]+)?`(RESEARCH Q-8 行:正则加 `[A-Z]{0,2}` 尾缀)。其余 pattern 不动。
  2. 翻转断言 device_74_08_test.go:47:`{"华为 USG-串首", "USG6000E Security Gateway", models.VendorHuawei, "USG6000"}` want 改为 `"USG6000E"`。
  3. 补回归:带尾字母+后缀 `"USG6680E-AI Firewall"`→`"USG6680E-AI"`;无尾字母不变 `"USG6000 Gateway"`→`"USG6000"`(锁定尾缀量词不吞既有形态);结合 Q-3 的行首样本 `"Huawei USG6000E Security Gateway"`→`"USG6000E"`。
  4. `go test ./internal/device/...` 后 commit:`fix(quirk-8): USG 正则补 [A-Z]{0,2} 尾字母,USG6000E 不再截断 + 回归`。
  </action>
  <verify>
    <automated>go test -count=1 -run TestModelExtractor ./internal/device/ && git log -1 --oneline | grep -c "fix(quirk-8)"</automated>
  </verify>
  <done>USG6000E 提取为 USG6000E;无尾字母形态不回归;fix(quirk-8) 单原子 commit。</done>
</task>

<task type="auto">
  <name>Task 3: Q-9 — nextIP 回卷返回 nil,与 ScanIPRange 循环条件同 commit</name>
  <files>internal/device/snmp_client.go, internal/device/device_74_08_test.go</files>
  <read_first>
  - internal/device/snmp_client.go:698-742(ScanIPRange / ipToUint32 / nextIP)
  </read_first>
  <action>
  原子 commit fix(quirk-9) — RESEARCH 硬约束 3:nextIP 与循环条件**必须同一 commit**,单独改任一侧发散(nil deref / 死循环):
  1. 重写 nextIP(snmp_client.go:726-742):入口先 `v4 := ip.To4()`,v4 == nil 直接返回 nil;在 4 字节副本上自增(从 byte 3 向 byte 0 进位);全部回卷(next == 0.0.0.0,即 Equal(net.IPv4zero))返回 nil;否则返回 4 字节 next。消除 16 字节输入下进位溢出到 v4-in-v6 前缀、Equal 判不中、返回垃圾 IP 的缺陷(垃圾 IP 会在 ipToUint32 里 To4()=nil 后解引用 panic)。
  2. 同 commit 核对 ScanIPRange 循环条件(:710):保持 `ip != nil &&` 在 `ipToUint32(ip) <= ipToUint32(end)` **之前**的短路顺序(现状已正确;若重构则必须保留该顺序),注释标注"Q-9:依赖 nextIP 回卷返回 nil 终止"。
  3. 回归用例(device_74_08_test.go,包内可直接访问小写 nextIP):`nextIP(net.ParseIP("255.255.255.255"))` 返回 nil;`nextIP(net.ParseIP("192.168.1.255"))` 等于 net.ParseIP("192.168.2.0");`nextIP(net.ParseIP("0.0.0.0"))` 返回 "0.0.0.1";`ScanIPRange("255.255.255.255", "255.255.255.255", 10*time.Millisecond)` 在 2 秒内正常返回(不 panic 不挂死 — 用 goroutine+timeout 或直接信任同步路径的短超时均可,选直接调用)。
  4. `go test ./internal/device/...` 后 commit:`fix(quirk-9): nextIP 回卷返回 nil + ScanIPRange nil 短路同 commit 修正 + 回归`。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestNextIP|TestScanIPRange" ./internal/device/ && go test -count=1 ./internal/device/ && git log -1 --oneline | grep -c "fix(quirk-9)"</automated>
  </verify>
  <done>255.255.255.255 的 next 为 nil;ScanIPRange 到达网段末位正常终止;两处改动在 fix(quirk-9) 同一 commit 内(用 `git show --stat` 核对 snmp_client.go 单文件同时含两处)。</done>
</task>

<task type="auto">
  <name>Task 4: 全量验证扫描</name>
  <files></files>
  <read_first>
  - scripts/check-ci-local.sh:1-25
  </read_first>
  <action>
  依次跑:`go build ./...` → `go test -count=1 ./...` → `bash scripts/check-ci-local.sh backend --no-npm-ci`。任一失败回到对应 task 修复。SUMMARY 记录:Q-3 双路径落库值差异说明(发现结果 diff,供 Phase 78 device 测试直接断言新行为)。
  </action>
  <verify>
    <automated>go build ./... && go test -count=1 ./... && bash scripts/check-ci-local.sh backend --no-npm-ci</automated>
  </verify>
  <done>三道全绿;三个 fix(quirk-N) 原子 commit 齐备。</done>
</task>

</tasks>

<verification>
- `go build ./...` + `go test -count=1 ./...` + `bash scripts/check-ci-local.sh backend --no-npm-ci` 全绿
- `git log --oneline` 含 fix(quirk-3)/fix(quirk-8)/fix(quirk-9) 三个原子 commit
- device_74_08_test.go:39-45/:47 锁定断言已翻转;双路径回归用例存在
</verification>

<success_criteria>
1. Q-3/Q-8/Q-9 关闭且行为可观察(ROADMAP SC-3 前三项 device 条目达成)
2. 双路径样本回归落位,落库 model 值变化有记录
3. gate 无倒退
</success_criteria>

<output>
执行完成后创建 `.planning/workstreams/milestone/phases/75-quirk-behavior-fixes/75-02-SUMMARY.md`(执行器写;记录三个 commit hash、双路径落库差异、发现链影响面)。
</output>
