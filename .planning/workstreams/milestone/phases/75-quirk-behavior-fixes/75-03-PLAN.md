---
phase: 75-quirk-behavior-fixes
plan: 3
type: execute
wave: 1
depends_on: ["75-01"]
files_modified:
  - pkg/crypto/sm2_jwt.go
  - pkg/crypto/crypto_74_08_test.go
  - internal/core/captcha_background.go
  - internal/core/core_74_08_test.go
  - internal/pkg/cache/manager.go
  - internal/pkg/cache/manager_stop_75_03_test.go
autonomous: true
requirements: [QUIRK-02]
user_setup: []
must_haves:
  truths:
    - "DecryptWithSM2 对合法 base64 但非 SM2 密文的输入返回 error(HTTP 层变 4xx),不再 panic 500(Q-4)"
    - "validateFile 对无扩展名文件名返回'不支持的文件格式'错误,不再 ext[1:] 越界 panic(Q-5)"
    - "GetRandomEnabled 在 sqlite 下走'没有找到可用的背景图'业务分支,不再报 PG-only SQL error(Q-6)"
    - "MetricsCacheService/MetricsCacheManager.Stop 双调幂等,双 close 不再 panic(Q-7)"
    - "四个原子 commit:fix(quirk-4) / fix(quirk-5) / fix(quirk-6) / fix(quirk-7)"
    - "go test -race ./internal/pkg/cache/... ./internal/core/... 全绿(RESEARCH 硬约束 6:Q-7 并发路径)"
  artifacts:
    - path: "pkg/crypto/sm2_jwt.go"
      provides: "DecryptWithSM2 长度预检"
      contains: "func DecryptWithSM2"
    - path: "internal/core/captcha_background.go"
      provides: "validateFile 空扩展名分支 + GetRandomEnabled 方言分支"
      contains: "不支持的文件格式"
    - path: "internal/pkg/cache/manager.go"
      provides: "MetricsCacheManager.Stop sync.Once 幂等"
      contains: "stopOnce"
    - path: "internal/pkg/cache/manager_stop_75_03_test.go"
      provides: "Stop 双调回归"
      contains: "TestMetricsCacheManager_Stop_Idempotent"
    - path: "internal/core/core_74_08_test.go"
      provides: "恢复被移除的 validateFile 无扩展名子用例"
      contains: "不支持的文件格式"
  key_links:
    - from: "pkg/middleware + internal/api/v1/auth.go:559 + internal/core/security/jwt.go:302"
      to: "pkg/crypto/sm2_jwt.go DecryptWithSM2"
      via: "4 个 caller 全部已判 err — 修复后垃圾密文从 Recovery 500 变正常 4xx"
      pattern: "DecryptWithSM2"
    - from: "internal/core/core.go:747"
      to: "internal/core/metrics_cache.go Stop → internal/pkg/cache/manager.go Stop"
      via: "core Close 关停链"
      pattern: "MetricsCacheService\\.Stop"
---

<objective>
修复 core 防御家族四项 QUIRK:Q-4 sm2.Decrypt 长度预检(垃圾密文 error 而非 panic)、Q-5 validateFile 无扩展名、Q-6 GetRandomEnabled sqlite 空结果分支、Q-7 MetricsCacheService.Stop 幂等。每项一个原子 commit。

Purpose: Q-7 是 Phase 78 core Init 链 Close 收尾的前置(ROADMAP 排序原则);Q-4 把登录加密链的 panic 500 变为可测的 4xx;Q-5/Q-6 消除 dev/sqlite 路径 panic 与 SQL error。
Output: 四个原子 commit(fix(quirk-4..7)),pkg/crypto + internal/core + internal/pkg/cache 全绿且 -race 通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/75-quirk-behavior-fixes/RESEARCH.md

关键坐标(已实证):
- pkg/crypto/sm2_jwt.go:382-408+ DecryptWithSM2 — base64 解码后无条件调 gmsm sm2.Decrypt(v1.4.1 sm2.go:321 无长度预检,短输入 makeslice panic);:400-408 已有 0x04 前缀重试梯子(函数尾部还有后续分支,以实际读到的为准)
- pkg/crypto/crypto_74_08_test.go:320-327 — 非法 base64 已测;:324-326 QUIRK 注释记录"合法 base64 非密文 panic,子用例移除"(RESEARCH:修复=防御性预检,**不翻既有断言、只加新用例**)
- internal/core/captcha_background.go:358-379 validateFile(:365-366 `ext := filepath.Ext(fileName); ext = ext[1:]` — ext=="" 越界);:142-191 GetRandomEnabled(:166-179 PG-only `@>`/jsonb_array_length 回退查询;:181-183 空结果业务分支"没有找到可用的背景图")。service 持 *coredb.Database,Type 字段取值 "sqlite"/"postgres"(database.go:73/:80 实证)
- internal/core/core_74_08_test.go:402-414 — validateFile 测试,:413 QUIRK 注释"无扩展名子用例移除"
- internal/pkg/cache/manager.go:28-34 MetricsCacheManager struct(stopChan/wg);:400-405 Stop 裸 `close(m.stopChan)` — 双调 panic "close of closed channel"
- internal/core/metrics_cache.go:61-67 MetricsCacheService.Stop 委托 manager.Stop;internal/core/core.go:747 Close 链调用;core_74_08_test.go:359 已有 defer svc.Stop()
- caller 面(RESEARCH Q-4):request_encryption.go:184/:249、security/jwt.go:302、api/v1/auth.go:559 — 全部已判 err
</context>

<tasks>

<task type="auto">
  <name>Task 1: Q-4 — DecryptWithSM2 前置长度校验(垃圾密文 error 而非 panic)</name>
  <files>pkg/crypto/sm2_jwt.go, pkg/crypto/crypto_74_08_test.go</files>
  <read_first>
  - pkg/crypto/sm2_jwt.go:382-430(DecryptWithSM2 全函数,含尾部分支)
  - pkg/crypto/crypto_74_08_test.go:280-345(现有加解密用例)
  </read_first>
  <action>
  原子 commit fix(quirk-4):
  1. sm2_jwt.go DecryptWithSM2:base64 解码成功后加长度预检 — SM2 mode-1(C1C3C2 未压缩)密文 = 0x04(1) + X||Y(64) + SM3 hash(32) + 密文(≥0),最小 97 字节。规则:(a) `len(data) < 96` → 直接 `return "", fmt.Errorf("sm2 密文长度非法: %d", len(data))`(96 也凑不出合法前缀形态);(b) 首次裸调 sm2.Decrypt 仅在 `len(data) >= 97` 时执行(96 字节输入跳过裸调,直接走既有 0x04 前缀重试分支,前缀后恰 97);(c) 保留既有重试梯子与尾部分支不动。≥97 的垃圾字节不再 panic(gmsm 长度运算合法,返回 curve 校验错误)。
  2. 不翻既有断言(RESEARCH 明确):crypto_74_08_test.go:280-322 全部保留。恢复被移除的子用例(在 :324-326 QUIRK 注释处):`_, err := DecryptWithSM2(base64.StdEncoding.EncodeToString([]byte("garbage-not-a-ciphertext")), priv)` → `require.Error` 且不 panic(用例名标注"垃圾密文返回错误不 panic");补一个 96 字节边界样本同样 require.Error。
  3. `grep -rn "500" --glob "*_test.go" internal/api/v1/ pkg/middleware/` 检查是否有登录加密 e2e 断言 500/panic 于垃圾密文 — 如有,同 commit 改为 4xx 期望(RESEARCH Q-4 联动效应)。
  4. `go test ./pkg/crypto/... ./internal/api/v1/... ./pkg/middleware/...` 后 commit:`fix(quirk-4): sm2.Decrypt 长度预检,垃圾密文返回 error 而非 panic + 回归`。
  </action>
  <verify>
    <automated>go test -count=1 ./pkg/crypto/... && git log -1 --oneline | grep -c "fix(quirk-4)"</automated>
  </verify>
  <done>合法 base64 垃圾输入得到 error 无 panic;既有断言未动;4 个 caller 联动包全绿;fix(quirk-4) 单原子 commit。</done>
</task>

<task type="auto">
  <name>Task 2: Q-5 — validateFile 无扩展名返回"不支持的文件格式"</name>
  <files>internal/core/captcha_background.go, internal/core/core_74_08_test.go</files>
  <read_first>
  - internal/core/captcha_background.go:355-379
  - internal/core/core_74_08_test.go:402-414
  </read_first>
  <action>
  原子 commit fix(quirk-5):
  1. captcha_background.go:365-366:`ext := filepath.Ext(fileName)` 后先判空 — `if ext == "" { return fmt.Errorf("不支持的文件格式: 文件名缺少扩展名") }`,再 `ext = ext[1:]`。其余逻辑(大小、AllowedFormats 循环)不动。
  2. 恢复被移除的子用例(core_74_08_test.go:413,删 QUIRK 注释):`assert.ErrorContains(t, svc.validateFile("noext", 100), "不支持的文件格式")`;补 `svc.validateFile("bg.", 100)` 同样 ErrorContains(点结尾 Ext 为 ".")— 以实际 filepath.Ext 语义为准取舍。
  3. `go test ./internal/core/...` 后 commit:`fix(quirk-5): validateFile 无扩展名返回不支持的文件格式 + 恢复子用例`。
  </action>
  <verify>
    <automated>go test -count=1 -run TestCaptchaBackgroundService_ValidateFile ./internal/core/ && git log -1 --oneline | grep -c "fix(quirk-5)"</automated>
  </verify>
  <done>"noext" 不再 panic,返回含"不支持的文件格式"的 error;:413 QUIRK 注释删除;fix(quirk-5) 单原子 commit。</done>
</task>

<task type="auto">
  <name>Task 3: Q-6 — GetRandomEnabled sqlite 走空结果分支(不再 PG-only SQL error)</name>
  <files>internal/core/captcha_background.go, internal/core/core_74_08_test.go</files>
  <read_first>
  - internal/core/captcha_background.go:142-191(GetRandomEnabled 全函数)
  - internal/core/db/database.go:54-90(Type 取值 "sqlite"/"postgres" 实证)
  </read_first>
  <action>
  原子 commit fix(quirk-6):
  1. captcha_background.go:166-179:把 PG-only 回退查询(`allowed_shapes @> ... jsonb_array_length`)包进方言 guard — 仅 `s.db.Type == "postgres"` 时执行该回退;非 PG(sqlite dev)跳过,自然落到 :181-183 的既有业务分支 `("没有找到可用的背景图 (shape=..., difficulty=...)")`。生产 PG 行为零变化。
  2. 回归用例(core_74_08_test.go,newBgService 即 sqlite):空表调用 `svc.GetRandomEnabled(ctx, models.PieceShape(...) 或现有用例同款 shape, 1)` 断言 `assert.ErrorContains(err, "没有找到可用的背景图")` 且**不含** "unrecognized token"/"syntax error"(负向断言用 NotContains);精确匹配有数据的正路径(种一条 CaptchaBackground 记录)返回该记录,证明主查询未受 guard 影响。
  3. `go test ./internal/core/...` 后 commit:`fix(quirk-6): GetRandomEnabled PG-only 回退按方言分支,sqlite 走空结果分支`。
  </action>
  <verify>
    <automated>go test -count=1 -run TestCaptchaBackgroundService_GetRandomEnabled ./internal/core/ && git log -1 --oneline | grep -c "fix(quirk-6)"</automated>
  </verify>
  <done>sqlite 下空表返回业务错误而非 SQL error;PG 主路径不回归;fix(quirk-6) 单原子 commit。</done>
</task>

<task type="auto">
  <name>Task 4: Q-7 — MetricsCacheManager.Stop sync.Once 幂等 + 全量验证扫描</name>
  <files>internal/pkg/cache/manager.go, internal/pkg/cache/manager_stop_75_03_test.go</files>
  <read_first>
  - internal/pkg/cache/manager.go:26-70(struct + 构造器)、:394-405(cleanupLoop 退出 + Stop)
  - internal/core/metrics_cache.go:55-67(service 层 Stop)
  </read_first>
  <action>
  原子 commit fix(quirk-7),随后全量验证:
  1. manager.go:28-34 struct 加字段 `stopOnce sync.Once`;:400-405 Stop 改为 `m.stopOnce.Do(func() { close(m.stopChan); m.wg.Wait(); applogger.Infof(...) })`(日志移入 Do,重复调用静默)。
  2. 新建 internal/pkg/cache/manager_stop_75_03_test.go:`TestMetricsCacheManager_Stop_Idempotent` — `m := NewMetricsCacheManager(nil); m.Stop(); m.Stop(); m.Stop()` 不 panic(三次覆盖 close 已关/已 Wait 场景);可加并发双 Stop(goroutine×2 + WaitGroup)断言仅一次生效(用 race 模式佐证)。
  3. core 侧联动:internal/core/core_74_08_test.go:359 已有 defer svc.Stop() — 补一个 double-Stop 冒烟(同一 svc 调两次 Stop)可选;至少跑 `go test ./internal/core/...` 确认 Close 链无回归。
  4. commit:`fix(quirk-7): MetricsCacheManager.Stop sync.Once 幂等 + 双调回归`。
  5. 全量验证(RESEARCH 硬约束 6,Q-7 并发路径必须 race):`go build ./...` → `go test -count=1 ./...` → `go test -race ./internal/pkg/cache/... ./internal/core/...` → `bash scripts/check-ci-local.sh backend --no-npm-ci`。任一失败回对应 task 修复。
  </action>
  <verify>
    <automated>go test -count=1 -run TestMetricsCacheManager_Stop ./internal/pkg/cache/ && go test -race ./internal/pkg/cache/... ./internal/core/... && go build ./... && go test -count=1 ./... && bash scripts/check-ci-local.sh backend --no-npm-ci && git log -1 --oneline | grep -c "fix(quirk-7)"</automated>
  </verify>
  <done>Stop 双调/并发双调无 panic;core Close 链全绿;-race 与本地 CI 镜像全绿;fix(quirk-7) 单原子 commit;本 plan 四个 commit 齐备。</done>
</task>

</tasks>

<verification>
- `go build ./...` + `go test -count=1 ./...` + `bash scripts/check-ci-local.sh backend --no-npm-ci` 全绿
- `go test -race ./internal/pkg/cache/... ./internal/core/...` 全绿
- `git log --oneline` 含 fix(quirk-4)/fix(quirk-5)/fix(quirk-6)/fix(quirk-7) 四个原子 commit
- crypto_74_08_test.go:324 与 core_74_08_test.go:413 的"子用例移除"QUIRK 注释被真实用例取代
</verification>

<success_criteria>
1. Q-4/Q-5/Q-6/Q-7 关闭且行为可观察(ROADMAP SC-3 对应四条达成)
2. Q-7 为 Phase 78 core Init 链 Close 收尾扫清前置
3. gate 无倒退
</success_criteria>

<output>
执行完成后创建 `.planning/workstreams/milestone/phases/75-quirk-behavior-fixes/75-03-SUMMARY.md`(执行器写;记录四个 commit hash、登录加密 e2e 是否有 500 期望需联动的结论、-race 实测)。
</output>
