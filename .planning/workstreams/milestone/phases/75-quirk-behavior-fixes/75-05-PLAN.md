---
phase: 75-quirk-behavior-fixes
plan: 5
type: execute
wave: 1
depends_on: ["75-01"]
files_modified:
  - internal/services/system/menu_service.go
  - internal/services/system/menu_service_test.go
  - internal/core/db/migrations/migration_210_normalize_menu_parent_id.go
  - internal/core/db/migrations/migration_210_normalize_menu_parent_id_test.go
  - internal/core/db/database.go
autonomous: true
requirements: [QUIRK-03]
user_setup: []
must_haves:
  truths:
    - "normalizeParentID 双实现统一为 requests 语义:nil/''/'0' 全塌缩为 nil(Q-11)"
    - "修复前完成存量 parent_id='0' 行数实测普查(census-before-migration,RESEARCH 硬约束 4),数字记录进 SUMMARY"
    - "数据迁移 210 把存量 parent_id='0' 行归一为 NULL,幂等可重放,sqlite/postgres 双分支注册"
    - "迁移后菜单树查询无孤儿节点:所有非 NULL parent_id 均指向存在的菜单行"
    - "代码统一 + 数据迁移同一交付面(同一原子 commit fix(quirk-11))"
  artifacts:
    - path: "internal/services/system/menu_service.go"
      provides: "normalizeParentID 统一 requests 语义"
      contains: '"0"'
    - path: "internal/core/db/migrations/migration_210_normalize_menu_parent_id.go"
      provides: "parent_id='0' → NULL 归一迁移"
      contains: "Migrate210"
    - path: "internal/core/db/migrations/migration_210_normalize_menu_parent_id_test.go"
      provides: "迁移幂等 + 孤儿检查回归"
      contains: "TestMigrate210"
    - path: "internal/core/db/database.go"
      provides: "双方言分支注册"
      contains: "Migrate210"
  key_links:
    - from: "internal/services/system/menu_service.go:80"
      to: "sys_menu.parent_id 落库"
      via: "Update 带 ParentID='0' 曾落库字面 '0' → 修复后归 NULL"
      pattern: "menu\\.ParentID = req\\.ParentID"
    - from: "internal/core/db/database.go(sqlite ~:854 / postgres ~:870 Migrate209 之后)"
      to: "migrations.Migrate210NormalizeMenuParentID"
      via: "启动自动迁移链"
      pattern: "Migrate210"
---

<objective>
修复 Q-11:normalizeParentID 双实现分歧(menu_service.go 只塌缩 "",requests 版塌缩 nil/""/"0")统一为 requests 语义;先实测存量 parent_id='0' 行数,再以迁移 210 归一为 NULL;迁移后菜单树无孤儿节点。代码与数据同一交付面(单一原子 commit)。

Purpose: Update 路径落库字面 "0" 造成菜单树孤儿(该节点既非根也找不到父,buildMenuTree 丢失);RESEARCH Q-11 风险评级"中:含数据修正,不只改代码"。
Output: 一个原子 commit fix(quirk-11)(含代码统一 + 迁移 210 + 注册 + 三层测试)+ census 记录。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/75-quirk-behavior-fixes/RESEARCH.md

关键坐标(已实证):
- internal/services/system/menu_service.go:388-394 normalizeParentID(只塌缩 "")— caller :50(Create)/:80(Update);:91 `menu.ParentID = req.ParentID` 落库点;:345 buildMenuTree(孤儿丢失处)
- internal/models/system/requests/menu_requests.go:61-67 normalizeParentID(塌缩 nil/""/"0",目标语义)— :32 ToModel(Create 模型转换)使用
- 迁移先例:internal/core/db/migrations/migration_209_update_settings_menu_component.go(GORM Update + 双条件幂等 + changed 返回值风格);注册点 internal/core/db/database.go:849-870(sqlite 分支 ~:854 / postgres 分支 ~:870,Migrate209 之后并列挂载);迁移测试先例 migration_208_dict_seed_test.go / migration_209_update_settings_menu_component_test.go
- internal/services/system/menu_service_test.go(既有测试文件,追加用例)
- Database.Type 方言值:"sqlite"/"postgres"(database.go:73/:80)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 存量普查 — parent_id='0' 行数实测(修复前前置,census-before-migration)</name>
  <files></files>
  <read_first>
  - configs/config.yaml(database.type 与 sqlite 文件路径 / PG 连接参数)
  - scripts/sql/ 与 internal/core/db/migrations/(grep 种子源)
  </read_first>
  <action>
  RESEARCH 硬约束 4:**修复前**先实测存量行数,普查结果决定迁移叙述并记录进 SUMMARY(迁移本身幂等,census 缺失不阻塞,但必须留痕):
  1. 种子面:`grep -rn "parent_id" scripts/sql internal/core/db/migrations --include="*.sql" --include="*.go" | grep -v "_test.go"` 找出种子/历史迁移里 parent_id 落 '0' 字面值的来源(尤其 menu_catalog_seed 系)。
  2. 运行库面:读 configs/config.yaml — sqlite 则对本地库文件执行 `SELECT COUNT(*) FROM sys_menu WHERE parent_id = '0';`(sqlite3 CLI 或临时 go run 查询均可,禁改库);postgres 可达则同款 SQL,不可达则记录"PG 环境待部署时由幂等迁移兜底"。
  3. 把两面数字(或 0/不可达结论)写入工作笔记,供 Task 2 的 commit message 与 SUMMARY 引用。本 task 不产生 commit、不改任何文件。
  </action>
  <verify>
    <automated>grep -rn "parent_id" scripts/sql --include="*.sql" | grep -c "0" || true</automated>
  </verify>
  <done>种子面 grep 结论 + 运行库 count(或不可达说明)已取得并记录;无代码改动。</done>
</task>

<task type="auto">
  <name>Task 2: Q-11 — normalizeParentID 统一 + 迁移 210 + 注册 + 三层测试(单一原子 commit)</name>
  <files>internal/services/system/menu_service.go, internal/services/system/menu_service_test.go, internal/core/db/migrations/migration_210_normalize_menu_parent_id.go, internal/core/db/migrations/migration_210_normalize_menu_parent_id_test.go, internal/core/db/database.go</files>
  <read_first>
  - internal/services/system/menu_service.go:45-107(Create/Update 全链)、:388-394
  - internal/models/system/requests/menu_requests.go:28-67(目标语义)
  - internal/core/db/migrations/migration_209_update_settings_menu_component.go(迁移风格范本)
  - internal/core/db/database.go:840-875(双分支注册点)
  - internal/services/system/menu_service_test.go(既有夹具:sqlite 建表/构造 service 的方式)
  </read_first>
  <action>
  全部改动进**单一原子 commit** fix(quirk-11)(RESEARCH:迁移与代码统一同一交付面):
  1. 代码统一 menu_service.go:388-394:normalizeParentID 改为 requests 语义 — `if parentID == nil || *parentID == "" || *parentID == "0" { return nil }; return parentID`。caller :50/:80 与 :91 落库点零改动(自动受益)。
  2. 新建 migration_210_normalize_menu_parent_id.go:`Migrate210NormalizeMenuParentID(db *gorm.DB) (bool, error)` — `db.Model(&models.Menu{}).Where("parent_id = ?", "0").Update("parent_id", nil)`;镜像 209 风格:幂等(重复执行 RowsAffected=0)、changed 返回、日志行;纯 GORM 无 PG 专有语法(UPDATE NULL 双方言通用),注释写明 Q-11 背景 + census 数字引用位。
  3. database.go 注册:sqlite 与 postgres 两分支各在 Migrate209 调用后并列挂载(参照 :854/:870 既有挂法);返回值若 core.go 需要失效菜单缓存,对照 209 的 changed 消费方式处理(209 有 SettingsMenuComponentChanged 先例;若 210 的 changed 也需失效 menu 缓存,同款接线;无需则仅在迁移内打日志,不强制扩 core.go)。
  4. 迁移测试 migration_210_..._test.go(镜像 208/209 测试,gorm sqlite 内存库 + 建 sys_menu 表):种 3 行 — 根 A(parent NULL)、子 B(parent=A.id)、脏行 C(parent='0');跑 Migrate210 → C.parent_id 为 NULL、A/B 不动;二跑 → RowsAffected=0 幂等。
  5. 服务层测试(menu_service_test.go 追加):normalizeParentID 单元用例(nil/""/"0"/合法 UUID 四态);Create+Update 集成 — Update 携带 ParentID 指针 "0" 后落库字段为 NULL(sqlite 断言 `menu.ParentID == nil`)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestMigrate210|TestMenu.*Parent|TestNormalizeParentID" ./internal/core/db/migrations/... ./internal/services/system/ && git log -1 --oneline | grep -c "fix(quirk-11)"</automated>
  </verify>
  <done>Update 带 "0" 落库 NULL;迁移幂等且双方言注册;代码+迁移+测试在 fix(quirk-11) 同一 commit。</done>
</task>

<task type="auto">
  <name>Task 3: 迁移后孤儿检查 + 全量验证扫描</name>
  <files>internal/core/db/migrations/migration_210_normalize_menu_parent_id_test.go</files>
  <read_first>
  - internal/services/system/menu_service.go:345-378(buildMenuTree)
  </read_first>
  <action>
  1. 孤儿检查用例(追加到 migration_210 测试):迁移后在同一测试库执行反连接 — `SELECT COUNT(*) FROM sys_menu m WHERE m.parent_id IS NOT NULL AND NOT EXISTS (SELECT 1 FROM sys_menu p WHERE p.id = m.parent_id)` 断言为 0(含原 '0' 脏行已归 NULL 的验证);再调 buildMenuTree 等价的层级断言:C 节点作为根出现在结果中(不再丢失)。
  2. 全量验证:`go build ./...` → `go test -count=1 ./...` → `bash scripts/check-ci-local.sh backend --no-npm-ci`。任一失败回 Task 2 修复。
  3. commit(孤儿用例若与 Task 2 同文件同批完成可并入 fix(quirk-11);若后补则 `test(quirk-11): 迁移后孤儿检查回归`)。SUMMARY 记录:census 数字、commit hash、菜单树行为变化说明(原 '0' 父节点菜单从树中丢失 → 修复后作为根节点出现)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestMigrate210" ./internal/core/db/migrations/ && go build ./... && go test -count=1 ./... && bash scripts/check-ci-local.sh backend --no-npm-ci</automated>
  </verify>
  <done>孤儿计数 0 断言存在且通过;build 全绿 + 全测全绿 + 本地 CI 镜像 PASS;fix(quirk-11) 交付面完整。</done>
</task>

</tasks>

<verification>
- `go build ./...` + `go test -count=1 ./...` + `bash scripts/check-ci-local.sh backend --no-npm-ci` 全绿
- `git log --oneline` 含 fix(quirk-11)(+ 可选 test(quirk-11))原子 commit,代码与迁移同交付面
- 迁移后反连接孤儿计数 = 0;menu_service 与 requests 的 normalizeParentID 语义一致(逐行对照 nil/""/"0")
</verification>

<success_criteria>
1. QUIRK-03 关闭:normalizeParentID 统一 + 存量 '0' 行归 NULL + 无孤儿节点(ROADMAP SC-4 达成)
2. census-before-migration 留痕(SUMMARY 有实测数字或不可达说明)
3. gate 无倒退
</success_criteria>

<output>
执行完成后创建 `.planning/workstreams/milestone/phases/75-quirk-behavior-fixes/75-05-SUMMARY.md`(执行器写;记录 census 数字、commit hash、菜单树行为变化、PG 环境幂等兜底说明)。
</output>
