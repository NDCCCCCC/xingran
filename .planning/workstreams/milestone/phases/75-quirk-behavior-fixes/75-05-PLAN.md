---
phase: 75-quirk-behavior-fixes
plan: 5
type: execute
wave: 1
depends_on: ["75-01"]
files_modified:
  - internal/utils/config_diff.go
  - internal/utils/config_diff_test.go
  - internal/pkg/system/disk_info.go
  - internal/pkg/system/disk_info_windows.go
  - internal/pkg/system/disk_info_unix.go
  - internal/pkg/system/system_test.go
  - internal/pkg/system/cpu_linux.go
autonomous: true
requirements: [QUIRK-02]
user_setup: []
must_haves:
  truths:
    - "GetUnifiedDiff 相同配置返回带 headers 的空 diff('--- old\\n+++ new\\n'),不同配置输出不变(Q-14,零生产调用方)"
    - "GetDiskInfoDetailed 不再递归栈溢出:平台实现落地 Windows GetDiskFreeSpaceExW / Unix syscall.Statfs(Q-15,M-3 行为面最小化)"
    - "cpu_linux GetCPUUsage mutex 保护 + 去递归,行为不变(M-2)"
    - "三个原子 commit:fix(quirk-14)/fix(quirk-15)+ refactor M-2"
  artifacts:
    - path: "internal/utils/config_diff.go"
      provides: "空 diff 带 headers"
      contains: "--- old"
    - path: "internal/pkg/system/disk_info_windows.go"
      provides: "Windows getDiskInfoByPlatform"
      contains: "procGetDiskFreeSpaceExW"
    - path: "internal/pkg/system/disk_info_unix.go"
      provides: "Unix getDiskInfoByPlatform"
      contains: "syscall.Statfs"
    - path: "internal/pkg/system/cpu_linux.go"
      provides: "mutex + 去递归"
      contains: "sync.Mutex"
  key_links:
    - from: "internal/monitor 详细磁盘端点"
      to: "internal/pkg/system GetDiskInfoDetailed"
      via: "此前必栈溢出,修复后返回真实容量"
      pattern: "GetDiskInfoDetailed"
---

<objective>
修复 utils/system 家族两项 QUIRK + M-2:Q-14 GetUnifiedDiff 空 diff 带 headers、Q-15 GetDiskInfoDetailed 去递归(M-3 最小行为面),外加 M-2 cpu_linux mutex 去递归(行为不变)。每项一个原子 commit。

Purpose: Q-14 零生产调用方,风险全在测试翻转;Q-15 消除 monitor 详细磁盘端点的必炸路径;M-2 消除 linux 上并发采样的数据竞争与自递归。
Output: 三个原子 commit,utils/system 包全绿(含 GOOS=linux 交叉编译验证 M-2/Q-15 unix 文件)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/75-quirk-behavior-fixes/RESEARCH.md

关键坐标(已实证):
- internal/utils/config_diff.go:104-111 GetUnifiedDiff、:249-275 formatUnifiedDiff(:250-252 Hunks 空 → return "");零生产 caller(仅 config_diff_test.go:35/:40)
- internal/utils/config_diff_test.go:33-37 — TestGetUnifiedDiff_Same 锁定 assert.Equal(t, "", d)
- internal/pkg/system/disk_info.go:19-30 — GetDiskInfoDetailed → getDiskInfoByPlatform → GetDiskInfoDetailed 死递归;disk_windows.go:12-15 已有 procGetDiskFreeSpaceExW lazy proc 可复用;system_test.go:42-51(GetAllDiskInfo 冒烟 + :50-51 QUIRK 注释)
- internal/pkg/system/cpu_linux.go:26-29(包级全局 lastCPUStats/firstCall 无锁)、:38-44(firstCall 分支自递归)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Q-14 — GetUnifiedDiff 相同配置返回带 headers 的空 diff</name>
  <files>internal/utils/config_diff.go, internal/utils/config_diff_test.go</files>
  <read_first>
  - internal/utils/config_diff.go:104-111(GetUnifiedDiff)、:246-275(formatUnifiedDiff)
  - internal/utils/config_diff_test.go:33-44
  </read_first>
  <action>
  原子 commit fix(quirk-14):
  1. config_diff.go formatUnifiedDiff :250-252:Hunks 为空时返回 `"--- old\n+++ new\n"`(带 headers 的空 diff,可区分"未计算"与"配置相同");有 hunks 的输出路径**零改动**(现有 @@ 头与行前缀不变)。
  2. 翻转 config_diff_test.go:33-37:删 QUIRK 注释,`assert.Equal(t, "", d)` 改为 `assert.Equal(t, "--- old\n+++ new\n", d)`;TestGetUnifiedDiff_Different(:39-44)不动(负向确认无回归)。
  3. 执行时复核零生产 caller:`grep -rn "GetUnifiedDiff(" --glob "*.go" | grep -v _test.go` 应为空(RESEARCH 已实证),SUMMARY 记录结论。
  4. `go test ./internal/utils/...` 后 commit:`fix(quirk-14): GetUnifiedDiff 相同配置返回带 headers 的空 diff + 断言翻转`。
  </action>
  <verify>
    <automated>go test -count=1 -run TestGetUnifiedDiff ./internal/utils/ && git log -1 --oneline | grep -c "fix(quirk-14)"</automated>
  </verify>
  <done>相同配置返回 headers 空 diff;不同配置输出不变;fix(quirk-14) 单原子 commit。</done>
</task>

<task type="auto">
  <name>Task 2: Q-15 — GetDiskInfoDetailed 去递归(平台实现,M-3 最小行为面)</name>
  <files>internal/pkg/system/disk_info.go, internal/pkg/system/disk_info_windows.go, internal/pkg/system/disk_info_unix.go, internal/pkg/system/system_test.go</files>
  <read_first>
  - internal/pkg/system/disk_info.go(全文 31 行)
  - internal/pkg/system/disk_windows.go:1-55(procGetDiskFreeSpaceExW lazy proc 可复用)
  - internal/pkg/system/system_test.go:1-60
  </read_first>
  <action>
  原子 commit fix(quirk-15) — M-3:签名、错误语义、其余调用点零变化,只消灭死递归:
  1. disk_info.go:删除 :27-30 递归版 getDiskInfoByPlatform;GetAllDiskInfo(:14-16)与 GetDiskInfoDetailed(:19-25)保持原样。
  2. 新建 disk_info_windows.go(`//go:build windows`):实现 `getDiskInfoByPlatform(path string) (uint64, uint64, error)` — 复用 disk_windows.go 的 procGetDiskFreeSpaceExW(同包),返回 (totalBytes, freeBytesAvailable, nil),失败按该文件既有错误风格返回 error。路径转换用 syscall.UTF16PtrFromString(参照 disk_windows.go:20-33)。
  3. 新建 disk_info_unix.go(`//go:build !windows`):同签名 — `var st syscall.Statfs_t; if err := syscall.Statfs(path, &st); err != nil { return 0,0,err }; return st.Blocks*uint64(st.Bsize), st.Bavail*uint64(st.Bsize), nil`。
  4. 翻转 system_test.go:50-51:删 QUIRK 注释,恢复真实用例 `TestGetDiskInfoDetailed`:`total, avail, err := GetDiskInfoDetailed(t.TempDir())` → NoError、total>0、avail>0、avail<=total(Windows 走临时目录所在盘,CI ubuntu 走 /tmp — 双环境成立)。
  5. `go test ./internal/pkg/system/...`(Windows 本地)+ `GOOS=linux go build ./internal/pkg/system/`(Unix 文件交叉编译)后 commit:`fix(quirk-15): getDiskInfoByPlatform 平台实现去递归(GetDiskFreeSpaceExW/Statfs)+ 恢复用例`。
  </action>
  <verify>
    <automated>go test -count=1 ./internal/pkg/system/... && GOOS=linux go build ./internal/pkg/system/ && git log -1 --oneline | grep -c "fix(quirk-15)"</automated>
  </verify>
  <done>GetDiskInfoDetailed(tempdir) 返回真实容量无栈溢出;两平台编译通过;fix(quirk-15) 单原子 commit。</done>
</task>

<task type="auto">
  <name>Task 3: M-2 — cpu_linux mutex 去递归(行为不变)+ 全量验证扫描</name>
  <files>internal/pkg/system/cpu_linux.go</files>
  <read_first>
  - internal/pkg/system/cpu_linux.go:26-77(lastCPUStats/firstCall 全局 + firstCall 自递归)
  </read_first>
  <action>
  commit `refactor(75-05): cpu_linux mutex 去递归 (M-2, 行为不变)`,随后全量验证:
  1. cpu_linux.go:26-29 加 `cpuStatsMu sync.Mutex`(补 sync import);GetCPUUsage 全程持锁(入口 Lock/defer Unlock)。
  2. :38-44 firstCall 分支的 `return GetCPUUsage()` 自递归改为迭代:seed lastCPUStats、sleep 100ms 后**就地重读** stats 再走同一差值计算(合并为一次循环或顺序代码,行为等价:首次调用同样采样两次间隔 100ms)。
  3. Windows 本地无法运行 linux 测试 — 验证 = `GOOS=linux go build ./internal/pkg/system/`(编译面)+ CI ubuntu 全量测试(行为面,本 plan 收尾扫描覆盖)。
  4. 全量验证:`go build ./...` → `go test -count=1 ./...` → `bash scripts/check-ci-local.sh backend --no-npm-ci`。任一失败回对应 task 修复。SUMMARY 记录三个 commit hash 与"Q-14 零生产 caller 复核结论"。
  </action>
  <verify>
    <automated>GOOS=linux go build ./internal/pkg/system/ && go build ./... && go test -count=1 ./... && bash scripts/check-ci-local.sh backend --no-npm-ci && git log -1 --oneline | grep -c "refactor(75-05)"</automated>
  </verify>
  <done>cpu_linux 持锁且无自递归;交叉编译 + 全测 + 本地 CI 镜像全绿;本 plan 三个 commit 齐备(fix(quirk-14/15) + refactor M-2)。</done>
</task>

</tasks>

<verification>
- `go build ./...` + `go test -count=1 ./...` + `bash scripts/check-ci-local.sh backend --no-npm-ci` 全绿
- `GOOS=linux go build ./internal/pkg/system/` 通过(M-2/Q-15 的 linux 文件编译面)
- `git log --oneline` 含 fix(quirk-14)/fix(quirk-15) 与 refactor(75-05) M-2
- config_diff_test.go:34-36 锁定断言翻转;system_test.go:50-51 QUIRK skip 删除、真实用例恢复
</verification>

<success_criteria>
1. Q-14/Q-15 关闭且行为可观察;M-2 行为不变落地(ROADMAP SC-3 utils/system 段 + Notes M-2 达成)
2. Q-15 双平台编译通过(monitor 详细磁盘端点必炸路径消除)
3. gate 无倒退
</success_criteria>

<output>
执行完成后创建 `.planning/workstreams/milestone/phases/75-quirk-behavior-fixes/75-05-SUMMARY.md`(执行器写;记录三个 commit hash、Q-14 零 caller 复核结论、GOOS=linux 编译验证)。
</output>
