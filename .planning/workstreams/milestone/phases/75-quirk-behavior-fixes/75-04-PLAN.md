---
phase: 75-quirk-behavior-fixes
plan: 4
type: execute
wave: 1
depends_on: ["75-01"]
files_modified:
  - internal/agent/pkg/retry/retry.go
  - internal/agent/pkg/retry/retry_74_12_test.go
  - internal/agent/server/logger.go
  - internal/agent/server/jwt_auth.go
  - internal/agent/server/agent_smoke_test.go
  - cmd/agent/main.go
autonomous: true
requirements: [QUIRK-02]
user_setup: []
must_haves:
  truths:
    - "retry.containsIgnoreCase 精确匹配:strings.Contains+ToLower;IsNetworkError('some business error') 为 false(Q-10,零生产调用方,风险全在测试翻转)"
    - "agent InitLogger 非法 level 返回 error(logger 仍完成初始化保持可用,Q-12)"
    - "agent NewTLSConfigFromConfig 全空参数返回 error;cmd/agent/main.go 同 commit 加 TLS 禁用 guard(空参传 nil 走 NewJWTAuthenticator 安全默认分支,Q-13)"
    - "三个原子 commit:fix(quirk-10)/fix(quirk-12)/fix(quirk-13)"
  artifacts:
    - path: "internal/agent/pkg/retry/retry.go"
      provides: "containsIgnoreCase 精确匹配"
      contains: "strings.Contains(strings.ToLower(s), strings.ToLower(substr))"
    - path: "internal/agent/server/logger.go"
      provides: "InitLogger 非法 level 报错"
      contains: "return fmt.Errorf"
    - path: "internal/agent/server/jwt_auth.go"
      provides: "TLS 空参校验"
      contains: "TLS"
    - path: "cmd/agent/main.go"
      provides: "TLS 禁用路径 guard"
      contains: "TLSCertFile"
  key_links:
    - from: "cmd/agent/main.go:51"
      to: "internal/agent/server/jwt_auth.go NewTLSConfigFromConfig"
      via: "启动链 — error 即 Fatal,空参 guard 必须同 commit 否则 TLS 禁用配置炸启动"
      pattern: "NewTLSConfigFromConfig"
---

<objective>
修复 agent 家族三项 QUIRK:Q-10 retry.containsIgnoreCase 精确匹配、Q-12 InitLogger 非法 level 报错、Q-13 TLS 空参校验(含 main.go caller guard)。每项一个原子 commit。

Purpose: Q-12/Q-13 修好后 Phase 77 agent config 测试直接断言新行为;Q-10 零生产调用方,风险全在测试翻转。
Output: 三个原子 commit,agent/retry 包全绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/75-quirk-behavior-fixes/RESEARCH.md

关键坐标(已实证):
- internal/agent/pkg/retry/retry.go:119-129 — contains(:120 前缀直配 + containsIgnoreCase)、containsIgnoreCase(:126-128 `return true` 恒真);IsNetworkError(:36)遍历 pattern 用 contains
- internal/agent/pkg/retry/retry_74_12_test.go:42-47(TestIsNetworkError 三条锁定断言)、:104-117(NonRetryableFailsFast 用自定义 exact Retryable 绕开 QUIRK)、:143-149(TestContainsHelpers)
- internal/agent/server/logger.go:14-45 — InitLogger ParseLevel 失败降级 InfoLevel 返回 nil;caller cmd/agent/main.go:46-48 已 `if err != nil` 打日志(改后安全)
- internal/agent/server/jwt_auth.go:87-125 — NewTLSConfigFromConfig(certFile,keyFile,caFile,verify);全空 → 空 config + nil error;NewJWTAuthenticator :61-68 有 tlsConfig==nil 安全默认分支;caller cmd/agent/main.go:51-59 error 即 Fatal(**空参必须同 commit guard,否则 TLS 禁用配置无法启动**)
- internal/agent/server/agent_smoke_test.go:179-188 — TestNewTLSConfigFromConfig_Errors 锁定断言(:181 全空 NoError+NotNil 要翻)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Q-10 — retry.containsIgnoreCase 精确匹配(三处锁定断言翻转)</name>
  <files>internal/agent/pkg/retry/retry.go, internal/agent/pkg/retry/retry_74_12_test.go</files>
  <read_first>
  - internal/agent/pkg/retry/retry.go:30-60(IsNetworkError pattern 遍历)、:115-129
  - internal/agent/pkg/retry/retry_74_12_test.go:25-50、:93-117、:142-149
  </read_first>
  <action>
  原子 commit fix(quirk-10):
  1. retry.go:126-129:containsIgnoreCase 改为 `return strings.Contains(strings.ToLower(s), strings.ToLower(substr))`(补 strings import)。contains(:120-124)保持"前缀直配 OR 忽略大小写包含"结构 — 修复后语义自然正确。
  2. 翻转 retry_74_12_test.go 三处:
     - :42-47 TestIsNetworkError:删 QUIRK 注释;`assert.True(IsNetworkError("some business error"))` 改 `assert.False`(业务串不再恒可重试);保留 `"Connection Refused"` → True(大小写不敏感命中)与短串 `"x"` → False;补 `assert.False(IsNetworkError("validation failed with long message"))`。
     - :104-117 TestDoWithRetry_NonRetryableFailsFast:删自定义 exact Retryable workaround,直接用 `fastConfig(3, IsNetworkError)` + bizErr "validation failed",断言 `ErrorIs(bizErr)` 且 calls==1(真 fails-fast)。
     - :143-149 TestContainsHelpers:`contains("xyz", "abc")` 改 `assert.False`;补 `assert.True(contains("Connection REFUSED", "refused"))` 与 `assert.False(contains("some business error", "connection refused"))`。
  3. 先 `grep -rn "IsNetworkError\|retry\." --glob "*.go" | grep -v "_test.go" | grep -v "internal/agent/pkg/retry"` 复核零生产调用方(RESEARCH 实证,执行时再确认一次),SUMMARY 记录结论。
  4. `go test ./internal/agent/pkg/retry/...` 后 commit:`fix(quirk-10): retry.containsIgnoreCase 精确匹配,长串不再恒可重试 + 三处断言翻转`。
  </action>
  <verify>
    <automated>go test -count=1 ./internal/agent/pkg/retry/... && git log -1 --oneline | grep -c "fix(quirk-10)"</automated>
  </verify>
  <done>"some business error" 判 false;NonRetryableFailsFast 用 IsNetworkError 真 fails-fast;fix(quirk-10) 单原子 commit。</done>
</task>

<task type="auto">
  <name>Task 2: Q-12 — InitLogger 非法 level 返回 error(logger 保持可用)</name>
  <files>internal/agent/server/logger.go, internal/agent/server/agent_smoke_test.go</files>
  <read_first>
  - internal/agent/server/logger.go:1-50
  - cmd/agent/main.go:45-48(caller 已判 err — 无需改)
  </read_first>
  <action>
  原子 commit fix(quirk-12):
  1. logger.go:18-21:ParseLevel 失败时保留 `level = logrus.InfoLevel` 降级(包级 logger 必须完成初始化,否则 WithContext/Debug 等 nil 解引用),但记下 parseErr;函数尾部 `if parseErr != nil { return fmt.Errorf("无效的日志级别 %q: %w", logLevel, parseErr) }; return nil`。logger 全量初始化(JSON formatter、文件输出)流程不动。
  2. caller 佐证:cmd/agent/main.go:46-48 已 `log.Printf` 继续 — 无需改动,确认即可。
  3. 测试(agent_smoke_test.go 追加):`InitLogger("bogus-level", "")` → require.Error 且随后的 Info/Debug 调用不 panic(logger 已初始化);`InitLogger("debug", "")` → NoError。
  4. `go test ./internal/agent/server/...` 后 commit:`fix(quirk-12): InitLogger 非法 level 返回 error(降级 info 保可用)+ 回归`。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestInitLogger" ./internal/agent/server/ && git log -1 --oneline | grep -c "fix(quirk-12)"</automated>
  </verify>
  <done>非法 level 返回 error 且 logger 可用;合法 level NoError;fix(quirk-12) 单原子 commit。</done>
</task>

<task type="auto">
  <name>Task 3: Q-13 — NewTLSConfigFromConfig 空参校验 + main.go TLS 禁用 guard(同 commit)+ 全量验证</name>
  <files>internal/agent/server/jwt_auth.go, cmd/agent/main.go, internal/agent/server/agent_smoke_test.go</files>
  <read_first>
  - internal/agent/server/jwt_auth.go:60-125(NewJWTAuthenticator nil 分支 + NewTLSConfigFromConfig 全函数)
  - cmd/agent/main.go:45-68(TLS 构造与 authenticator 装配)
  - internal/agent/server/agent_smoke_test.go:179-188
  </read_first>
  <action>
  原子 commit fix(quirk-13) — 校验与 caller guard **必须同 commit**,否则 TLS 禁用(三文件全空)的 agent 启动直接 Fatal:
  1. jwt_auth.go:88 函数入口:`if certFile == "" && keyFile == "" && caFile == "" { return nil, errors.New("TLS 配置不能全空: 至少提供 certFile/keyFile/caFile 之一(未启用 TLS 请传 nil)") }`。其余逻辑不动。
  2. cmd/agent/main.go:51-59 改为:仅当 `config.TLSCertFile != "" || config.TLSKeyFile != "" || config.CAFile != ""` 时调用 NewTLSConfigFromConfig;否则 `tlsConfig = nil`(NewJWTAuthenticator :63-68 的 nil 分支提供 TLS1.3 安全默认),并 `server.Info("TLS 未配置,使用默认传输")`。
  3. 翻转 agent_smoke_test.go:179-188:`NewTLSConfigFromConfig("", "", "", true)` → `require.Error` + `assert.Nil(cfg)`;保留无效 CA 文件 error 用例;补一个有效 mTLS 形态仅当测试夹具可行(可选,不强求)。
  4. `go build ./...`(覆盖 cmd/agent)+ `go test ./internal/agent/server/...` 后 commit:`fix(quirk-13): TLS 空参校验 + 启动链 TLS 禁用 guard + 断言翻转`。
  5. 全量验证:`go build ./...` → `go test -count=1 ./...` → `bash scripts/check-ci-local.sh backend --no-npm-ci`。任一失败回对应 task 修复。SUMMARY 记录三个 commit hash 与"Q-10 零生产 caller 复核结论"。
  </action>
  <verify>
    <automated>go build ./... && go test -count=1 -run "TestNewTLSConfigFromConfig" ./internal/agent/server/ && git log -1 --oneline | grep -c "fix(quirk-13)" && go test -count=1 ./... && bash scripts/check-ci-local.sh backend --no-npm-ci</automated>
  </verify>
  <done>全空参数返回 error;TLS 禁用配置下 agent 启动路径走 nil 默认分支不 Fatal;fix(quirk-13) 单原子 commit 含 main.go guard;build + 全测 + 本地 CI 镜像全绿。</done>
</task>

</tasks>

<verification>
- `go build ./...` + `go test -count=1 ./...` + `bash scripts/check-ci-local.sh backend --no-npm-ci` 全绿
- `git log --oneline` 含 fix(quirk-10)/fix(quirk-12)/fix(quirk-13)
- retry_74_12_test.go:42-47/:105/:144-148 与 agent_smoke_test.go:180-183 锁定断言全部翻转
</verification>

<success_criteria>
1. Q-10/Q-12/Q-13 关闭且行为可观察(ROADMAP SC-3 agent 段达成)
2. Q-13 的 caller guard 与校验同 commit,TLS 禁用启动路径不回归
3. gate 无倒退
</success_criteria>

<output>
执行完成后创建 `.planning/workstreams/milestone/phases/75-quirk-behavior-fixes/75-04-SUMMARY.md`(执行器写;记录三个 commit hash、Q-10 零 caller 复核结论)。
</output>
