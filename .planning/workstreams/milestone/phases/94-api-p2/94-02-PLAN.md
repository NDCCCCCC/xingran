---
phase: 94-api-p2
plan: 02
type: execute
wave: 2
depends_on: ["94-01"]
files_modified:
  - xingran-react-frontend/src/lib/opsApi.ts
  - xingran-react-frontend/src/lib/opsApi.test.ts
  - xingran-react-frontend/src/lib/rpaApi.ts
  - xingran-react-frontend/src/lib/vdiApi.ts
  - xingran-react-frontend/src/lib/download.test.ts
autonomous: true
requirements: [API-FACTORY-03, API-FACTORY-04]
user_setup: []

must_haves:
  truths:
    - "D-03: opsApi.ts 私有 createCrudApi 定义删除，11 资源实例统一 import src/lib/apiFactory.ts 的 createResourceApi（Phase 92 base 包单一权威哲学前端同构）"
    - "D-04: opsApi 的 blob 四件套迁出至 download.ts 消费，excelApi/deptApi/assetApi.excel 导出位置不变、内部委托 downloadFilePost；rpaApi.downloadReport 裸 fetch 重复消灭（白得 5min 超时防护）"
    - "D-07: wall/door/floorPlanText 三处 batch(action, ids: string[]) spread 覆盖层原样保持，不与工厂 batch(action, data) 统一"
    - "D-08: rpaApi 第二份私有工厂（5 方法版 + CrudApiConfig<_T> 死泛型）删除合并（以 opsApi 8 方法版为权威），scriptApi 以 spread 接入，taskApi/workerApi/executionApi 等纯异构主体保持；vdiApi 走 floorApi spread 模式（vmApi list/create SPREAD+OVERRIDE、vdiServerApi 纯 SPREAD）"
    - "D-14: 13 个配套 .test.ts 基本不动——仅 opsApi.test.ts 小幅适配（h.created[0] 位置假设改直取 download 导出），rpaApi.test.ts / vdiApi.test.ts 预期零改动"
    - "全部导出签名零变化：opsApi 27 个生产消费文件、rpaApi/vdiApi 消费文件零改动（type-check 全绿即证明）"
    - "API-FACTORY-03 达成：opsApi.ts（buildingApi/floorApi/workstationApi/assetApi 等 11 资源）迁移到工厂模式且保留同名导出"
  artifacts:
    - path: "xingran-react-frontend/src/lib/opsApi.ts"
      provides: "11 资源工厂实例改 import apiFactory；blob 基建迁出；DropdownOption re-export 兜底"
      contains: "import { createResourceApi } from \"./apiFactory\""
    - path: "xingran-react-frontend/src/lib/rpaApi.ts"
      provides: "7 spread 实例换共享工厂 + scriptApi spread 接入 + downloadReport 委托 downloadFilePost"
      contains: "downloadFilePost"
    - path: "xingran-react-frontend/src/lib/vdiApi.ts"
      provides: "vmApi SPREAD+OVERRIDE + vdiServerApi SPREAD 接入工厂"
      contains: "createResourceApi"
  key_links:
    - from: "xingran-react-frontend/src/lib/opsApi.ts"
      to: "xingran-react-frontend/src/lib/apiFactory.ts"
      via: "11 资源实例工厂调用来源切换（D-03 单一权威）"
      pattern: "createResourceApi<"
    - from: "xingran-react-frontend/src/lib/opsApi.ts"
      to: "xingran-react-frontend/src/lib/download.ts"
      via: "blob 四件套 import + excelApi.export/assetApi.excel.export 委托 downloadFilePost（D-04）"
      pattern: "from \"./download\""
    - from: "xingran-react-frontend/src/lib/rpaApi.ts"
      to: "xingran-react-frontend/src/lib/download.ts"
      via: "downloadReport 裸 fetch 换 downloadFilePost（+5min 超时防护）"
      pattern: "downloadFilePost"
    - from: "xingran-react-frontend/src/lib/vdiApi.ts"
      to: "xingran-react-frontend/src/lib/apiFactory.ts"
      via: "vmApi/vdiServerApi spread 接入（vmApi list/create 覆盖保留原签名）"
      pattern: "\\.\\.\\.vmCrud|\\.\\.\\.vdiServerCrud"
---

<objective>
Phase 94 Wave 2：三个对象形态 API 文件（opsApi / rpaApi / vdiApi）迁移到 94-01 的共享工厂——全部为「删 + 换 import + spread」等价替换，零签名变化。

Purpose: opsApi 是工厂提升源本体（删私有定义即达成 D-03 单一权威）+ 27 个生产消费文件的兼容性主验证对象；rpaApi 消灭仓库第二份平行工厂与裸 fetch 下载链；vdiApi 补齐对象形态最后一块。完成后 D-10「模板清零」在对象形态域成立，为 94-03 扁平迁移与 D-12 硬档扫描铺路。

Output: opsApi.ts（D-03/D-04）+ opsApi.test.ts 适配（Pitfall 6）+ rpaApi.ts（双工厂合并 + scriptApi + downloadReport）+ vdiApi.ts（spread 模式），三件套 gate 全绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/94-api-p2/94-CONTEXT.md
@.planning/workstreams/milestone/phases/94-api-p2/94-RESEARCH.md
@.planning/workstreams/milestone/phases/94-api-p2/94-PATTERNS.md

# 本 plan 修改的对象（迁移目标现状）
@xingran-react-frontend/src/lib/opsApi.ts
@xingran-react-frontend/src/lib/rpaApi.ts
@xingran-react-frontend/src/lib/vdiApi.ts
@xingran-react-frontend/src/lib/opsApi.test.ts

# 94-01 产物（消费契约）
@xingran-react-frontend/src/lib/apiFactory.ts
@xingran-react-frontend/src/lib/download.ts
</context>

<interfaces>
<!-- 94-01 产物消费契约（executor 直接使用，无需探索） -->

来自 src/lib/apiFactory.ts:
- export function createResourceApi<T>(config: CrudApiConfig) — 8 方法 list/get/create/update/delete/batch/statistics/searchOptions
- export interface DropdownOption { value: string; label: string }
- export interface CrudApiConfig { basePath: string; dropdownPath?: string }

来自 src/lib/download.ts:
- export const blobAxios: AxiosInstance（实例本身，opsApi.test.ts 直取依赖此导出）
- export function extractFilenameFromBlobResponse(response: AxiosResponse<Blob>, defaultFilename: string): string
- export function triggerBrowserDownload(blob: Blob, filename: string): void
- export function downloadFile(url: string, filename: string): Promise<void>
- export function downloadFilePost(url: string, body: unknown, defaultFilename: string): Promise<void>
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: opsApi.ts 迁移（D-03 删私有工厂 + D-04 blob 迁出）+ opsApi.test.ts 适配</name>
  <files>xingran-react-frontend/src/lib/opsApi.ts, xingran-react-frontend/src/lib/opsApi.test.ts</files>
  <read_first>
  - xingran-react-frontend/src/lib/opsApi.ts 全文（792 行：私有工厂 :43-97、11 资源实例与 spread 先例 :108/:120-128、locationAliasApi :183-205、roomPhotoApi :220-272、blob 四件套 :310-369、excelApi :371-404、deptApi :414-418、wall/door/floorPlanText batch 覆盖层 :442-448 等、geocode 函数族 :515-632、assetApi :636-700、componentApi :707-716、workstationDeviceApi :718-792）
  - xingran-react-frontend/src/lib/apiFactory.ts 与 src/lib/download.ts（94-01 消费契约）
  - xingran-react-frontend/src/lib/opsApi.test.ts 全文（:99 h.created[0] 位置假设、:335-423 excel/blob 断言链）
  </read_first>
  <action>
  opsApi.ts 六处动作（全部为删 + 换 import，无新写逻辑）：

  1. 删私有 interface CrudApiConfig（:43-47）与 function createCrudApi&lt;T&gt;（:49-97）整段（per D-03），顶部加 import { createResourceApi } from "./apiFactory"。文件内全部工厂调用点（buildingApi :108、floorCrudApi :120、workstationCrudApi :140、serverRoomApi :216、roomDevice、dedicatedLine、infoPoint :306、wall、door、floorPlanText、asset 及其余私有 Crud const）改名 createResourceApi&lt;X&gt;，实参零改动。
  2. DropdownOption 定义（:29-39 含 JSDoc）删除，改为 export type { DropdownOption } from "./apiFactory"（verbatimModuleSyntax 下 interface re-export 必须用 export type；外部 import 路径兼容兜底，RESEARCH 矩阵登记当前零外部消费方）。文件内部若有对 DropdownOption 的剩余类型引用改为自 apiFactory 导入。
  3. blob 四件套删除（:310-369：blobAxios + 拦截器 + extractFilenameFromBlobResponse + triggerBrowserDownload + downloadFile），改 import { blobAxios, downloadFile, extractFilenameFromBlobResponse, triggerBrowserDownload } from "./download"。随之清理仅被四件套使用的 import：axios 值导入、AxiosInstance/AxiosResponse/InternalAxiosRequestConfig 类型、getAccessToken——逐个核对文件内无其他使用点后才删（post/postFormData/get 仍被使用必须保留；lint unused 报错即漏删）。
  4. excelApi.export（:385-396）内联 POST-blob 链改为一行委托 downloadFilePost(`/ops/${entityType}/export`, params, `${entityType}_${Date.now()}.xlsx`)——导出位置与调用签名不变（per D-04）。assetApi.excel.export（:685-698）同构委托 downloadFilePost，defaultFilename 传其现状固定默认名（归一后额外获得 content-disposition 提取能力，属 D-04 白得改善）。excelApi.downloadTemplate / deptApi.exportMapping 已走 downloadFile，仅 import 来源切换零逻辑改动。
  5. KEEP 不动区逐条核对未被顺手改动：locationAliasApi（:183-205，pageNum/pageSize 参数名 + create 注入 scope:"workstation" 两处行为差异，RESEARCH 矩阵明确 KEEP）、roomPhotoApi（:220-272）、wall/door/floorPlanText 的 batch(action, ids) spread 覆盖层（per D-07）、assetApi.statistics 覆盖（:662-668，返回 {total,normal,stopped,nbf} 与工厂 Record&lt;string,number&gt; 不同构）、componentApi、workstationDeviceApi（:718-792）、geocode 函数族。
  6. 验收红线：全部导出成员与方法签名零变化（对象形态 API 的方法集与类型不变），27 个生产消费文件零改动。

  opsApi.test.ts 适配（per D-14，全部幅度仅此）：:99 附近对 h.created[0] 的位置假设与 blobRequestInterceptor 位置猜测，改为顶部 import { blobAxios } from "./download" 直取实例（download.ts 已导出实例本身），删除基于模块加载顺序的推断；excel/blob 断言链（:335-423）的 URL/参数断言原样保留，若存在对「导出失败」错误信息文案的断言，按 downloadFilePost 语义（`下载失败: <默认文件名>`）最小修正；其余约 550 行零改动（工厂行为已由 94-01 apiFactory.test.ts 另锁）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npx vitest run src/lib/opsApi.test.ts && npm run type-check && npm run lint && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
  - opsApi.ts 中 grep "function createCrudApi" == 0 且 grep "interface CrudApiConfig" == 0
  - opsApi.ts 含 import { createResourceApi } from "./apiFactory" 与 from "./download" 的 import
  - opsApi.ts 中 grep "axios.create" == 0（blobAxios 定义已迁出）
  - opsApi.ts 含 export type { DropdownOption } from "./apiFactory"
  - excelApi.export 与 assetApi.excel.export 函数体含 downloadFilePost 调用
  - git diff 中 locationAliasApi / roomPhotoApi / workstationDeviceApi / geocode 区块无改动（KEEP 红线）
  - npx vitest run src/lib/opsApi.test.ts exit 0；npx vitest run 全量 exit 0；type-check 与 lint 双 exit 0
  </acceptance_criteria>
  <done>opsApi 成为 apiFactory + download 的纯消费方（私有工厂与 blob 基建定义清零），全部导出签名不变，既有测试适配后全绿</done>
</task>

<task type="auto">
  <name>Task 2: rpaApi.ts 迁移（第二份工厂合并 + scriptApi 接入 + downloadReport 归一）</name>
  <files>xingran-react-frontend/src/lib/rpaApi.ts</files>
  <read_first>
  - xingran-react-frontend/src/lib/rpaApi.ts 全文（753 行：第二份私有工厂 :49-79、7 个 spread 实例 task :95 / worker :228 / execution :315 / schedule :400 / variable :466 / template :515 / notification :618、scriptApi :160-215、downloadReport 裸 fetch :355-379、aiApi :564-606、statisticsApi :660-735、getAuthHeaders import :6）
  - xingran-react-frontend/src/lib/rpaApi.test.ts 第 180-215 行（核实 :192-208 只断言聚合对象 10 个子 API 名、无子 API 形状断言）
  - xingran-react-frontend/src/lib/__tests__/rpaApi.batch56.unit.test.ts（batch 相关回归面）
  - xingran-react-frontend/src/lib/apiFactory.ts 与 src/lib/download.ts（消费契约）
  </read_first>
  <action>
  1. 删第二份私有工厂（:49-79 整段）：5 方法版 createCrudApi 与 CrudApiConfig&lt;_T&gt; 无用泛型死代码一并消灭（RESEARCH 双工厂对照表：合并以 opsApi 8 方法版为权威），改 import { createResourceApi } from "./apiFactory"（per D-03 同款逻辑，per D-08）。
  2. 7 个 spread 实例的工厂调用换名 createResourceApi，spread 层零改动。已知 additive 副作用（RESEARCH Pitfall 4）无需处理：共享工厂 8 方法使各导出对象新增 batch/statistics/searchOptions 三方法，行为中性；rpaApi.test.ts 无子 API 形状断言（Task 内 read_first 复核确认）；knip 若报新增未调用方法属预期噪音，不处理不修。
  3. scriptApi（:160-215）手写五方法改 spread 接入（per D-08 点名对象）：建 const scriptCrud = createResourceApi&lt;Script&gt;({ basePath: "/rpa/scripts" })（basePath 以文件内现有 URL 前缀实际值为准核对），导出对象改为 { ...scriptCrud, testAction(原样 :199-207), format(原样 :212-214) }。五方法签名与工厂完全同构（list 参数 PageParams 字面兼容，RESEARCH 已验证）；若 type-check 对个别方法报不兼容，按 RESEARCH A3 方向安全原则将该方法改为原样覆盖（编译器为仲裁），不 cast 迁就。
  4. executionApi.downloadReport（:355-379）裸 fetch 全文删除，改一行委托 download.ts 的 downloadFilePost：URL 拼接保持现状语义（format 参数进 URL query），defaultFilename 用其现状默认名 execution_report_&lt;id&gt;.&lt;format&gt; 形态。:6 的 getAuthHeaders import 若为该文件唯一使用点则随之删除（lint unused 仲裁）；token 注入改经 blobAxios 拦截器（语义等价 + 白得 5min 超时防护）。
  5. aiApi（:564-606）/ statisticsApi（:660-735）/ taskApi/workerApi/executionApi 主体方法 KEEP 不动。
  6. 在 src/lib/download.test.ts 追加 RESEARCH D-11 组 6 用例：downloadReport 经新链路——断言 downloadFilePost 收到含 format query 的 URL 与 execution_report 默认文件名（可在 download.test.ts 新 describe 以 downloadFilePost 层或 executionApi.downloadReport 层断言，二选一取改动最小者）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npx vitest run src/lib/rpaApi.test.ts src/lib/__tests__/rpaApi.batch56.unit.test.ts src/lib/download.test.ts && npm run type-check && npm run lint && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
  - rpaApi.ts 中 grep "function createCrudApi" == 0 且 grep "CrudApiConfig" == 0
  - rpaApi.ts 的 downloadReport 区间 grep "fetch(" == 0（裸 fetch 消灭）；函数体含 downloadFilePost
  - rpaApi.ts 含 import { createResourceApi } from "./apiFactory"；scriptApi 导出对象含 ...scriptCrud spread
  - 若 getAuthHeaders 在 rpaApi.ts 无剩余使用点则 grep "getAuthHeaders" == 0
  - rpaApi.test.ts 与 rpaApi.batch56.unit.test.ts 零改动仍全绿（D-14）；download.test.ts 新增用例绿
  - npx vitest run 全量 exit 0；type-check 与 lint 双 exit 0
  </acceptance_criteria>
  <done>仓库第二份平行工厂消灭（单 CreatePayload 化权威唯一），scriptApi 接入工厂，downloadReport 归一 download.ts，rpa 域测试零回归</done>
</task>

<task type="auto">
  <name>Task 3: vdiApi.ts 迁移（vmApi SPREAD+OVERRIDE + vdiServerApi SPREAD）</name>
  <files>xingran-react-frontend/src/lib/vdiApi.ts</files>
  <read_first>
  - xingran-react-frontend/src/lib/vdiApi.ts 全文（190 行：vmApi :31-140、vdiServerApi :144-168、类型 re-export 块 :172-190）
  - xingran-react-frontend/src/types/vdi.ts（snake_case 字段核实：VirtualMachine/VDIServer/VMAccount 的 created_at/updated_at——94-01 排除集已覆盖）
  - xingran-react-frontend/src/pages/vdi/VirtualMachineList/index.tsx 第 180-200 行（interface 变量直传消费点 :186-191——vmApi.list 覆盖的实证依据）
  - xingran-react-frontend/src/lib/vdiApi.test.ts（D-14 预期零改动核实）
  - xingran-react-frontend/src/lib/apiFactory.ts（消费契约）
  </read_first>
  <action>
  1. vmApi（:31-140）SPREAD+OVERRIDE（RESEARCH Pitfall 1 实证决定）：建 const vmCrud = createResourceApi&lt;VirtualMachine&gt;({ basePath: "/vdi/vms" })；导出对象改为 { ...vmCrud, list(原样), create(原样), 其余 14 个异构方法原样 }。list 与 create 必须原样覆盖、覆盖体自 vdiApi.ts:33-43 一字不改照搬——依据：list 参数 VMListParams 是 interface，工厂 list 签名 PageParams &amp; Record&lt;string, unknown&gt; 会拒绝 interface 变量直传（VirtualMachineList/index.tsx:186-191 实锤 TS2345）；create 的 CreateVMRequest 含 vtp_id/count 等实体外字段且缺 vm_id 必选字段（双向不可赋值）。get/update/delete 来自 spread（RESEARCH 已验证类型兼容）；若 type-check 对 update 报 UpdateVMRequest 不兼容，按 A3 方向安全原则把 update 也改为原样覆盖（编译器为仲裁），禁止 cast 迁就工厂。
  2. vdiServerApi（:144-168）纯 SPREAD：建 const vdiServerCrud = createResourceApi&lt;VDIServer&gt;({ basePath: 按文件内现有 URL 前缀 })；导出对象改为 { ...vdiServerCrud, testConnection(原样 :165-167) }。前提（94-01 已满足）：CreatePayload 排除集含 snake_case created_at/updated_at/deleted_at——VDIServerConfig 对 Partial&lt;CreatePayload&lt;VDIServer&gt;&gt; 可赋值（RESEARCH tsc 实测）。若 type-check 仍报 vdiServerApi.create 不兼容，将 create 改原样覆盖并记录到 SUMMARY。
  3. 类型 re-export 块（:172-190）零改动。GET vs POST 端点细节保持现状，不改后端契约。
  4. vdiApi.test.ts 预期零改动（per D-14）；仅当存在子 API 方法集全等断言（Pitfall 4 同类）时做最小适配并在 SUMMARY 记录——新增 batch/statistics/searchOptions 三方法属 additive 行为中性。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npx vitest run src/lib/vdiApi.test.ts && npm run type-check && npm run lint && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
  - vdiApi.ts 含 import { createResourceApi } from "./apiFactory"；vmApi 与 vdiServerApi 导出对象均含 spread 接入
  - vdiApi.ts 中 vmApi.list 参数类型仍为 VMListParams（grep "VMListParams" 命中）；create 参数类型仍为 CreateVMRequest
  - npm run type-check exit 0（VirtualMachineList/index.tsx 消费点不炸——Pitfall 1 红线）
  - vdiApi.test.ts 零改动全绿；npx vitest run 全量 exit 0；lint exit 0
  </acceptance_criteria>
  <done>对象形态三文件（opsApi/rpaApi/vdiApi）全部接入共享工厂，vmApi 高危消费点类型契约原样保留，三件套 gate 全绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 → 后端 blob 端点 | rpaApi.downloadReport 由裸 fetch 切换到 blobAxios 拦截器链（token 注入实现切换） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-04 | Info Disclosure | rpaApi.downloadReport token 注入 | mitigate | 手写 fetch header（getAuthHeaders）换 blobAxios 异步拦截器——语义等价（同为 Bearer token），且获得 5min 超时防护（原裸 fetch 无超时）；download.test.ts 用例锁定 |
| T-94-05 | DoS | downloadReport 无超时请求悬挂 | mitigate | 归一后继承 blobAxios 300000ms 超时，消除无超时裸 fetch（本 phase 唯一安全正向改善点） |
| T-94-06 | Tampering | 工厂 URL 拼接 | accept | 全部为等价替换（路径模板字符串原样迁移），不改后端契约、不改动词；type-check + 既有测试锁定零行为变化 |

**总体评估结论：** 对象形态迁移为等价替换，无新增攻击面；downloadReport 迁移是本 phase 唯一带安全正向收益的动作（超时防护 + 统一 token 注入路径）。
</threat_model>

<verification>
- npm run type-check + npm run lint 双 exit 0
- npx vitest run 全量绿（opsApi.test.ts 适配后 + rpa/vdi 零改动绿 + 94-01 契约测试绿）
- grep 证明：opsApi/rpaApi 无 createCrudApi/CrudApiConfig 定义残留，rpaApi 无裸 fetch，opsApi 无 axios.create
- git diff 确认 KEEP 区（locationAliasApi/roomPhotoApi/workstationDeviceApi/geocode/aiApi/statisticsApi/类型 re-export）零改动
</verification>

<success_criteria>
- API-FACTORY-03 达成：opsApi 11 资源全部走共享工厂，同名导出保留，27 个生产消费文件零改动
- 仓库双份私有工厂清零（D-03 + D-08），blob 下载链三处重复收敛为 download.ts 单一权威（D-04）
- D-07 batch 覆盖层与全部异构方法原样保持；导出签名零变化承诺兑现
- 三件套 gate 全绿，D-14 承诺兑现（仅 opsApi.test.ts 小幅适配）
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/94-api-p2/94-02-SUMMARY.md` when done
</output>
