---
phase: 94-api-p2
plan: 03
type: execute
wave: 3
depends_on: ["94-02"]
files_modified:
  - xingran-react-frontend/src/lib/workorderApi.ts
  - xingran-react-frontend/src/lib/knowledgeApi.ts
  - xingran-react-frontend/src/lib/dutyApi.ts
  - xingran-react-frontend/src/lib/noticeApi.ts
  - xingran-react-frontend/src/lib/adDomainApi.ts
  - xingran-react-frontend/src/lib/apiFactory.invariants.test.ts
  - CLAUDE.md
  - .planning/workstreams/milestone/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [API-FACTORY-04, API-FACTORY-05]
user_setup: []

must_haves:
  truths:
    - "D-05: 5 个扁平文件（workorderApi/knowledgeApi/dutyApi/noticeApi/adDomainApi）中能对上工厂方法的函数内部改一行委托工厂实例，导出签名（含显式返回类型注解）零变化，100+ 消费文件零改动；异构函数（publishNotice/generateSchedule/swapDuty 等）原样保持"
    - "D-06: menuApi/profileApi/columnConfigApi 三个非 CRUD 小文件保持现状不套工厂（硬套只造无意义包装）"
    - "D-08: adDomainApi/workorderApi 只把标准 CRUD 形状函数接上工厂（能对上才委托）；adDomain 全部 list 委托保留 withDefaultPagination 前置调用（:224-228 helper 留原文件，Pitfall 5）"
    - "adDomainApi deleteMapping :501 潜伏 URL bug（模板串末尾多余右括号必然 404）随委托修复，独立 atomic commit 显式登记（v1.29 D-05 例外条款纪律，Phase 91 附带修复 2 现存 bug 先例）"
    - "D-10: 验收锚点达成——src/lib/*Api.ts 不再有与工厂方法同构的手写 CRUD 五件套直调 post 函数体（模板清零定性，不设 LOC 硬数字）"
    - "D-12: apiFactory.invariants.test.ts 双档扫描防线落地——硬档（opsApi/rpaApi/vdiApi 残留 >0 即 fail）+ warning 档 whitelist（期望计数写死，实际计数 != 期望即 fail 防白名单腐烂）"
    - "D-13: CLAUDE.md 新增「前端 API 工厂 Convention」段（单一权威路径 + 新资源必须走工厂 + 模板清零约定）+ 修订 § Frontend API Calling 段"
    - "D-14: 5 个扁平文件的配套 .test.ts 零改动全绿"
    - "API-FACTORY-05: npm run type-check + npm run lint + npm run test 全过；npm run test:coverage + check-frontend-coverage.sh gate exit 0（覆盖率 ≥45.13% 不降）"
    - "REQUIREMENTS/ROADMAP 措辞按 D-01 校准（7 方法→8 方法提升语义、~15→13），独立 docs commit（Phase 91/92 先例）"
  artifacts:
    - path: "xingran-react-frontend/src/lib/workorderApi.ts"
      provides: "categories/periodic/orders-list-get-delete cluster 委托"
      contains: "createResourceApi"
    - path: "xingran-react-frontend/src/lib/knowledgeApi.ts"
      provides: "articles-list-get-delete/categories/tags-delete cluster 委托"
      contains: "createResourceApi"
    - path: "xingran-react-frontend/src/lib/dutyApi.ts"
      provides: "duty pools list/get/delete cluster 委托"
      contains: "createResourceApi"
    - path: "xingran-react-frontend/src/lib/noticeApi.ts"
      provides: "admin notices list/get/delete cluster 委托"
      contains: "createResourceApi"
    - path: "xingran-react-frontend/src/lib/adDomainApi.ts"
      provides: "ou-group-mappings/mappings/configs cluster 委托 + withDefaultPagination 保留 + :501 bug 修复"
      contains: "withDefaultPagination"
    - path: "xingran-react-frontend/src/lib/apiFactory.invariants.test.ts"
      provides: "D-12 双档 AST 扫描测试（硬档 + warning whitelist）"
      contains: "createSourceFile"
    - path: "CLAUDE.md"
      provides: "D-13 Frontend API Factory Convention 段"
      contains: "Frontend API Factory Convention"
  key_links:
    - from: "扁平 5 文件（workorder/knowledge/duty/notice/adDomain）"
      to: "xingran-react-frontend/src/lib/apiFactory.ts"
      via: "文件内私有工厂实例 + 同构函数一行委托（D-05）"
      pattern: "createResourceApi<"
    - from: "xingran-react-frontend/src/lib/apiFactory.invariants.test.ts"
      to: "xingran-react-frontend/src/lib/*Api.ts（13 文件）"
      via: "ts.createSourceFile AST 扫描，范围仅 src/lib，豁免 api/apiFactory/download"
      pattern: "createSourceFile"
    - from: "CLAUDE.md"
      to: "xingran-react-frontend/src/lib/apiFactory.ts"
      via: "D-13 Convention 段锁定单一权威路径"
      pattern: "apiFactory"
---

<objective>
Phase 94 Wave 3：扁平文件 5 件 cluster 级委托迁移（低风险优先）+ D-12 扫描防线 + D-13 文档收口 + 措辞校准 + 覆盖率 gate——phase 全部收尾工作。

Purpose: 完成 API-FACTORY-04（其余文件按迁移矩阵逐个处置）与 API-FACTORY-05（三件套 + 覆盖率 gate）；D-12 扫描测试让 D-10「模板清零」从一次性定性变为可回归防线；adDomainApi :501 潜伏 bug 按登记纪律顺带修复。

Output: 5 个扁平文件委托化 + apiFactory.invariants.test.ts + CLAUDE.md Convention 段 + ROADMAP/REQUIREMENTS 措辞校准 + 覆盖率 gate 通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/94-api-p2/94-CONTEXT.md
@.planning/workstreams/milestone/phases/94-api-p2/94-RESEARCH.md
@.planning/workstreams/milestone/phases/94-api-p2/94-PATTERNS.md

# 本 plan 修改的扁平文件（迁移目标现状——用 PATTERNS 行号定向读取，避免全文件通读）
@xingran-react-frontend/src/lib/workorderApi.ts
@xingran-react-frontend/src/lib/knowledgeApi.ts
@xingran-react-frontend/src/lib/dutyApi.ts
@xingran-react-frontend/src/lib/noticeApi.ts
@xingran-react-frontend/src/lib/adDomainApi.ts

# 94-01 产物（消费契约）与扫描先例
@xingran-react-frontend/src/lib/apiFactory.ts
@internal/services/system/cache_invariants_92_test.go
@xingran-react-frontend/src/design-system/tokens/colors.test.ts

# 收口文档
@CLAUDE.md
@.planning/workstreams/milestone/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<interfaces>
<!-- 94-01 产物消费契约 + 扁平委托目标形态 -->

来自 src/lib/apiFactory.ts:
- export function createResourceApi<T>(config: CrudApiConfig) — 8 方法；create/update 参数为 Partial<CreatePayload<T>>
- 消费形态（扁平文件唯一手法，PATTERNS § 扁平 wrapper 委托）：文件顶部建私有实例（命名 <resource>Crud，如 const categoryCrud = createResourceApi<WorkOrderCategory>({ basePath: "/workorder/categories" })），同构导出函数体内一行委托，导出函数签名（含显式返回类型注解）零变化。

cast 纪律：wrapper 体内类型不兼容处用 as unknown as 双跳（直接 as 在互不可赋时非法，Pitfall 1）；get/delete 返回类型的下转（如 Promise<BaseResponse<{ message: string }>>）用单 as 合法。
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: workorderApi + knowledgeApi + dutyApi 扁平委托（D-05/D-08）</name>
  <files>xingran-react-frontend/src/lib/workorderApi.ts, xingran-react-frontend/src/lib/knowledgeApi.ts, xingran-react-frontend/src/lib/dutyApi.ts</files>
  <read_first>
  - xingran-react-frontend/src/lib/workorderApi.ts 第 380-600 行（orders :385-436、categories :494-521、periodic :531-571）与类型定义区（WorkOrder/WorkOrderCategory :93-96 createdBy 字段）
  - xingran-react-frontend/src/lib/knowledgeApi.ts 第 130-243 行（articles :133-174、categories :186-213、tags :232-234、search/like/convert :176-184/:238-243）
  - xingran-react-frontend/src/lib/dutyApi.ts 第 150-335 行（pools :155-193、holidays :255-284 含 createHoliday 本地 Omit idiom :259-263、config :288-296、getUserList 默认注入 :319-331）
  - xingran-react-frontend/src/lib/apiFactory.ts（消费契约）
  - 三个文件的 .test.ts（D-14 预期零改动——迁移前先跑一遍确认基线绿）
  </read_first>
  <action>
  按 RESEARCH 迁移矩阵 + PATTERNS 委托清单逐 cluster 处置，原则：能对上才委托，导出签名零变化，禁止为迁就工厂改 URL/动词/参数名。

  workorderApi.ts：建 orderCategoryCrud/periodicCrud/orderCrud 私有实例（basePath 按文件内现有 URL 前缀）。DELEGATE——categories 五方法（:494-521，全 POST 最干净）；periodic templates 的 list/get/delete（:531-571；其 create/update 仅当请求类型对 Partial&lt;CreatePayload&lt;T&gt;&gt; 天然可赋值才委托——type-check 为仲裁，默认 KEEP）；orders 的 list/get/delete（:385-429）。KEEP——createWorkOrder/updateWorkOrder（:417-426 解耦请求类型 WorkOrderCreateRequest）、batchDeleteWorkOrders（:432-436 /batch-delete 非 batch 形状）、assign/comments/ratings/config/user/dept 全异构。

  knowledgeApi.ts：建 articleCrud/categoryCrud/tagCrud。DELEGATE——articles 的 list/get/delete（:133-174）；categories 五方法（:186-213）；tags 的 delete（:232-234）。KEEP——articles 的 create/update（:159-170，KnowledgeArticleCreateRequest 含 tagIds 实体外字段；此二函数登记为 D-12 warning 档 whitelist 项）、search/like/convert、getAllKnowledgeTags。

  dutyApi.ts：建 dutyPoolCrud。DELEGATE——duty pools 的 list/get/delete（:155-193；getDutyPool 返回 BaseResponse&lt;DutyPool&gt; 与工厂 T 完全一致零 cast）。KEEP——pools 的 create/update（memberIds 请求类型）、schedules 全族（generate/swap/manual/monthly 全异构）、holidays 全族（list 参数 year: number 非 PageParams；createHoliday 的本地 Omit idiom :259-263 原样保留不换 CreatePayload——scope 纪律）、config 单例、getUserList/getDeptList/getDeptTree（getUserList 有 current:1/pageSize:1000 默认注入 :319-331，Shared Pattern 5 禁止委托）。

  每文件收尾自查：导出成员清单迁移前后一致（无增无减无改名）；所有被委托函数保留原显式返回类型注解。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npx vitest run src/lib/workorderApi.test.ts src/lib/knowledgeApi.test.ts src/lib/dutyApi.test.ts && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
  - 三文件均含 import { createResourceApi } from "./apiFactory"
  - git diff 证明 batchDeleteWorkOrders / getUserList / createHoliday / publishNotice 同族异构函数体零改动（KEEP 红线）
  - workorderApi.ts 中 getWorkOrderList 函数体为 orderCrud（或同名实例）.list 委托；getWorkOrder 为 .get(id) 直委托零 cast
  - knowledgeApi.ts 的 createKnowledgeArticle/updateKnowledgeArticle 函数体保持直调 post（未硬套工厂）
  - 三个 .test.ts 零改动（git diff == 空）且 vitest run 全绿（D-14）
  - npm run type-check exit 0；npm run lint exit 0
  </acceptance_criteria>
  <done>workorder/knowledge/duty 三文件的矩阵 DELEGATE cluster 全部委托化，KEEP 区零触碰，测试零回归</done>
</task>

<task type="auto">
  <name>Task 2: noticeApi + adDomainApi 扁平委托 + :501 潜伏 bug 修复登记（D-05/D-08）</name>
  <files>xingran-react-frontend/src/lib/noticeApi.ts, xingran-react-frontend/src/lib/adDomainApi.ts, xingran-react-frontend/src/lib/adDomainApi.test.ts</files>
  <read_first>
  - xingran-react-frontend/src/lib/noticeApi.ts 第 1-190 行（admin notices :23-76、batchDelete :81-83、statistics GET :88-90、publish/withdraw :95-104、my-notices :111-169、buildWebSocketUrl :176-188）
  - xingran-react-frontend/src/lib/adDomainApi.ts 第 220-260 行（withDefaultPagination helper :224-228 + configs cluster :232-255）与第 480-610 行（mappings :482-502 含 deleteMapping :500-502 的 `/delete}` bug、ou-group-mappings :580-605）
  - xingran-react-frontend/src/lib/adDomainApi.test.ts（核对是否存在断言带右括号错误 URL 的用例）
  - xingran-react-frontend/src/lib/apiFactory.ts（消费契约）
  </read_first>
  <action>
  noticeApi.ts：建 adminNoticeCrud（basePath "/notice/admin/notices" 形态按文件内现有 URL）。DELEGATE——admin notices 的 list/get/delete（:23-76）；create 仅当 CreateNoticeRequest（types/notice.ts :142 附近）对 Partial&lt;CreatePayload&lt;T&gt;&gt; 天然可赋值才委托（type-check 仲裁，默认 KEEP）。KEEP——batchDeleteNotices（:81-83 /batch-delete）、全部 GET 动词端点（getNoticeStatistics :88-90 等——工厂 get 是 POST，动词不可改）、publish/withdraw、用户端 my-notices 全族（:111-169）、buildWebSocketUrl（:176-188）。

  adDomainApi.ts：建 mappingCrud/ouGroupMappingCrud/configCrud（basePath 按现有 URL）。DELEGATE——ou-group-mappings 五方法（:580-605，全文件最干净）；mappings 的 list/create/update（:482-498）；configs 的 list/create/update/delete（:232-255）。全部 list 委托必须保留 withDefaultPagination(params) 前置调用——helper :224-228 留在原文件，委托写法为 crud.list(withDefaultPagination(params) as ...)（Pitfall 5：直接 crud.list(params) = 丢默认分页 = 行为变更）。KEEP——getADConfig/getMapping（GET 动词）、groups/users/computers/logs 全族（id-in-body 非 REST 路径）、accounts 池（:674-731）、sync/test/enable/disable/unlock。

  :501 潜伏 bug 修复（独立 atomic commit，v1.29 D-05 例外条款登记纪律）：deleteMapping（:500-502）现状 URL 模板串 `/ad-domain/mappings/${id}/delete}` 末尾多余右括号，必然 404。随工厂委托后 URL 自然正确化为 `/ad-domain/mappings/${id}/delete`——这是行为变更，必须显式登记：(1) 该函数的委托改动单独成一个 commit，commit message 注明 fix 语义（deleteMapping URL 多余右括号潜伏 404，随工厂化委托修复）；(2) 核对 adDomainApi.test.ts——若存在断言带 `}` 错误 URL 的用例，同 commit 修正断言为正确 URL。其余 adDomain 迁移与 notice 迁移各为独立 commit。

  commit 划分（本 task 共 3 个）：noticeApi 委托 1 个；adDomainApi 委托（不含 deleteMapping）1 个；deleteMapping bugfix 委托 + 测试断言修正 1 个。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npx vitest run src/lib/noticeApi.test.ts src/lib/adDomainApi.test.ts && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
  - 两文件均含 import { createResourceApi } from "./apiFactory"；导出成员清单迁移前后一致
  - adDomainApi.ts 中全部 list 委托行含 withDefaultPagination 调用（grep "withDefaultPagination" 命中数 ≥ 委托 list 数）；helper 定义仍在原文件
  - adDomainApi.ts 中 grep "/delete}" == 0（bug 消灭）
  - adDomainApi.test.ts 无断言带右括号错误 URL 的残留用例
  - git log 显示 adDomain 迁移与 deleteMapping 修复分属不同 commit，fix commit message 含 bug 登记说明
  - noticeApi.ts 的 getNoticeStatistics / batchDeleteNotices / buildWebSocketUrl 函数体零改动（KEEP 红线）
  - 两 .test.ts 基线零改动（除 :501 断言修正）全绿；type-check 与 lint 双 exit 0
  </acceptance_criteria>
  <done>notice/adDomain cluster 委托完成，withDefaultPagination 前置语义保留，:501 潜伏 bug 按登记纪律修复并独立 commit</done>
</task>

<task type="auto">
  <name>Task 3: 收口——D-12 扫描防线 + D-13 Convention + 措辞校准 + 覆盖率 gate</name>
  <files>xingran-react-frontend/src/lib/apiFactory.invariants.test.ts, CLAUDE.md, .planning/workstreams/milestone/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
  - internal/services/system/cache_invariants_92_test.go 第 1-60 行（双档分级先例：allowedResidues map + 硬档 fail / warning 计数注释原文）
  - xingran-react-frontend/src/design-system/tokens/colors.test.ts 第 233-252 行（node:fs 读源码先例 + ESLint typed-lint 定向豁免注释模式）
  - CLAUDE.md 的「### Cache Service Convention」段与「### Frontend API Calling」段（D-13 新增/修订落点）
  - .planning/workstreams/milestone/ROADMAP.md § Phase 94（SC-1/SC-2 措辞校准落点）
  - .planning/REQUIREMENTS.md § API-FACTORY :88-96（API-FACTORY-01/04 措辞校准落点）
  - RESEARCH § D-12 设计与 § 迁移矩阵（whitelist 期望计数的依据）
  </read_first>
  <action>
  一、新建 src/lib/apiFactory.invariants.test.ts（D-12，双档分级镜像 cache_invariants_92_test.go）：
  - 检测实现：import ts from "typescript"（已在 devDependencies，零新装）+ ts.createSourceFile 解析；文件遍历用 node:fs 的 readdirSync/readFileSync（照抄 colors.test.ts 模式，文件定位用 import.meta.url 相对解析到 src/lib，含同款 ESLint typed-lint 定向豁免注释）。扫描范围仅 src/lib/*Api.ts 的 13 个文件，豁免 api.ts / apiFactory.ts / download.ts；禁止扩大到全仓 src（会误伤页面内联 post，RESEARCH Anti-Patterns）。
  - 命中规则：函数体（FunctionDeclaration 或对象字面量 PropertyAssignment）为单条 return 语句且调用表达式标识符属于 post/get/put/del 集合，且 URL 参数为模板字符串、其后缀匹配 CRUD 形状（/list 结尾、/update 结尾、/delete 结尾、/batch-delete 结尾等）。
  - 硬 fail 档：opsApi.ts / rpaApi.ts / vdiApi.ts（对象形态已接工厂，残留即回归）——期望命中 0，实测 >0 则断言失败。
  - warning 档 whitelist：期望计数逐文件写死在 whitelist map（结构对齐 allowedResidues：文件名 → 允许残留数），每项附理由注释——knowledgeApi（articles create/update 2 处，KnowledgeArticleCreateRequest 解耦）、notificationConfigApi（整文件 KEEP：GET list / PUT /{id} / DELETE 动词 + page 参数，四处传输语义全部与工厂不同构）、assetApi.ts（整文件 KEEP：reconciliation/fixSuggestion 解包 res.data 裸数据契约）、dutyApi/noticeApi/adDomainApi/workorderApi 的异构 create/update 及执行 Task 1/2 后实际残留的其他项（以实际扫描结果定数）、menuApi/profileApi/columnConfigApi 登记 0（D-06 非 CRUD，登记文件存在性）。断言：硬档命中 == 0 且 warning 档各文件实际计数 == whitelist 期望计数（实际 != 期望即 fail，防白名单腐烂——新增模板与删除模板都触发）。
  - 红绿演练（Phase 92 惯例）：临时在任一硬档文件加一个同构模板函数，确认扫描测试转红；删除后恢复绿。演练过程不留痕。
  - 若 whitelist 实际计数与矩阵预期不符（RESEARCH A3：个别类型意外可赋值导致函数从 KEEP 升级为 DELEGATE），以执行时实际残留为准修正 whitelist 数值并在 SUMMARY 记录。

  二、CLAUDE.md（D-13，独立 docs commit）：在「### Cache Service Convention」段之后新增「### Frontend API Factory Convention」段，结构照 Cache 段三段式（单一权威 + 规则列表 + Migration status）：锁 src/lib/apiFactory.ts 的 createResourceApi 为唯一工厂权威（8 方法清单）+ src/types/apiFactory.ts 的 CreatePayload 派生类型 + src/lib/download.ts 下载链权威 + 新资源必须走工厂（不再手写五件套直调 post）+ 模板清零约定（D-10）+ D-12 扫描测试回归守护说明 + KEEP 例外登记（notificationConfigApi/assetApi/menuApi/profileApi/columnConfigApi 与异构函数不套工厂的理由）。同步修订「### Frontend API Calling」段：补一句新 ops 域资源用 createResourceApi 创建、不手写 CRUD 封装。

  三、ROADMAP/REQUIREMENTS 措辞校准（D-01，与 CLAUDE.md 同一个 docs commit，Phase 91/92 先例）：
  - ROADMAP § Phase 94 SC-1：「list/getByID/create/update/delete/import/export 7 方法 + 类型推导」→「list/get/create/update/delete/batch/statistics/searchOptions 8 方法（提升自 opsApi 既有工厂；import/export 不进工厂核心、getByID 不采用，per D-01 行业对齐）+ CreatePayload 派生类型」。
  - ROADMAP § Phase 94 Goal 与 SC-2：「~15 个」→「13 个」。
  - REQUIREMENTS API-FACTORY-01：「设计 createResourceApi&lt;T&gt;(basePath, resourceName) 工厂函数（封装 …7 个 CRUD 方法）」→「提升 opsApi 既有 8 方法工厂为共享 createResourceApi&lt;T&gt;(config)（list/get/create/update/delete/batch/statistics/searchOptions；import/export 不进工厂核心，per D-01 行业对齐）」。
  - REQUIREMENTS API-FACTORY-04：「其他 ~10 个」→「其余 12 个（逐文件迁移矩阵判定：DELEGATE / SPREAD+OVERRIDE / KEEP）」。

  四、覆盖率 gate（API-FACTORY-05）：npm run test:coverage 生成 coverage-final.json 后跑 bash .github/scripts/check-frontend-coverage.sh xingran-react-frontend/coverage/coverage-final.json .coverage-fe-floors（GLOBAL 3.8 + lib 87.2 floors；新增两文件自带契约测试接近全覆盖、模板删除使 lib 分母变小，预期只升不降，RESEARCH 覆盖率注意事项）。

  commit 划分：扫描测试 1 个 commit；CLAUDE.md + ROADMAP + REQUIREMENTS docs 1 个 commit。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npx vitest run src/lib/apiFactory.invariants.test.ts && npm run test:coverage && bash .github/scripts/check-frontend-coverage.sh xingran-react-frontend/coverage/coverage-final.json .coverage-fe-floors && npm run type-check && npm run lint && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
  - apiFactory.invariants.test.ts 存在且绿；文件内含 createSourceFile 调用与 whitelist 期望计数 map；扫描范围不含 src/lib 之外的路径（grep 断言文件列表构造逻辑）
  - 硬档三文件（opsApi/rpaApi/vdiApi）期望命中 0；notificationConfigApi/assetApi/knowledgeApi 在 whitelist 中有非零期望计数及理由注释
  - 红绿演练已执行（SUMMARY 记录演练结果）
  - CLAUDE.md 含 "### Frontend API Factory Convention" 段（grep 命中）；§ Frontend API Calling 段含 createResourceApi 用法
  - ROADMAP § Phase 94 中 grep "getByID" == 0 且 grep "~15" == 0；REQUIREMENTS API-FACTORY-01 行含 "8 方法" 且 API-FACTORY-04 行含 "12 个"
  - check-frontend-coverage.sh exit 0（覆盖率 ≥45.13% 不降由 floors gate 证明）
  - npm run type-check / lint / npx vitest run 三件套全 exit 0
  - git log 显示扫描测试与 docs 分属两个 commit
  </acceptance_criteria>
  <done>D-10 模板清零锚点 + D-12 可回归防线 + D-13 文档权威 + 措辞校准 + 覆盖率 gate 全部达成，Phase 94 收口</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新增 | 本 plan 为既有请求封装的内部委托改写 + 开发期测试/文档，无新边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-07 | Repudiation | adDomain deleteMapping URL bug 修复 | mitigate（登记） | 行为变更（失配 URL → 正确 URL）按 v1.29 D-05 例外条款独立 atomic commit 登记，git 历史可审计 |
| T-94-08 | Tampering | 扁平委托的 URL/动词等价性 | mitigate | 全部委托为同 URL 同动词等价替换（withDefaultPagination 前置保留）；type-check + 13 个既有 .test.ts 回归锁定；禁止为迁就工厂改契约写入 plan 纪律 |
| T-94-SC | Tampering | npm 安装 | accept | 零新依赖（typescript/node:fs 均既有），无供应链新面 |

**总体评估结论：** 扁平迁移与收口均为零新增攻击面动作；唯一行为变更（:501 bug 修复）已按登记纪律可审计化。
</threat_model>

<verification>
- npm run type-check + npm run lint + npx vitest run 三件套全绿（API-FACTORY-05 前两件半）
- npm run test:coverage + check-frontend-coverage.sh exit 0（覆盖率 ≥45.13% 不降）
- apiFactory.invariants.test.ts 绿：硬档 0 残留 + warning 计数 == whitelist
- grep "/delete}" 在 adDomainApi.ts == 0；grep "getByID"/"~15" 在 ROADMAP § Phase 94 == 0
- 5 个扁平文件 + notificationConfigApi/assetApi/menuApi/profileApi/columnConfigApi 的 KEEP 判定经 git diff 复核零误伤
</verification>

<success_criteria>
- API-FACTORY-04 达成：13 个 *Api.ts 全部按迁移矩阵处置完毕（5 委托 + 3 对象迁移[94-02] + 5 KEEP），导出签名零变化
- D-10 模板清零定性达成并由 D-12 双档扫描测试可回归守护
- D-13 Convention 段落地；REQUIREMENTS/ROADMAP 措辞按 D-01 校准（独立 docs commit）
- 覆盖率 ≥45.13% 不降（floors gate exit 0）——Phase 94 全部 SC 满足
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/94-api-p2/94-03-SUMMARY.md` when done
</output>
