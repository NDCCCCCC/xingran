---
phase: 93
plan: 05
type: execute
wave: 4
depends_on: ["93-03"]
files_modified:
  - xingran-react-frontend/src/pages/network/backups/types.ts
  - xingran-react-frontend/src/pages/network/backups/hooks/useRestoreTask.ts
  - xingran-react-frontend/src/pages/network/backups/hooks/useRestoreTask.test.tsx
  - xingran-react-frontend/src/pages/network/backups/hooks/useBackupModals.ts
  - xingran-react-frontend/src/pages/network/backups/index.tsx
autonomous: true
requirements: [BACKUP-CLOSED-03]

must_haves:
  truths:
    - "D-16: 任务进度经前端轮询任务状态到达（useRestoreTask 3s 间隔轮询，success/failed 终态停止），不引入 WS 推送"
    - "D-17（前端契约）: 恢复发起读取响应 data.taskId 并以 GET /network/backups/restore-tasks/:id 轮询详情（与 93-04 端点契约一致）"
    - "D-19: 后端 + 最小前端——备份页恢复按钮改异步交互（发起 → taskId → 轮询 → Modal 内进度/结果展示），不做独立任务管理页"
    - "D-04（前端侧）: 恢复请求固定携带所选备份自身的 deviceId（修复现状 handleRestore 发空 body 缺 deviceId 必 400 的缺陷），不提供跨设备选择"
  artifacts:
    - path: xingran-react-frontend/src/pages/network/backups/types.ts
      provides: "ConfigRestoreTask 类型（id/deviceId/backupId/status/totalLines/sentLines/failedLine/result/errorMessage/startedAt/completedAt/createdBy）"
      contains: "ConfigRestoreTask"
    - path: xingran-react-frontend/src/pages/network/backups/hooks/useRestoreTask.ts
      provides: "轮询 hook：taskId 输入、3s 间隔、终态停止、unmount 清理"
      contains: "useRestoreTask"
    - path: xingran-react-frontend/src/pages/network/backups/hooks/useRestoreTask.test.tsx
      provides: "vitest 用例：running 轮询→success 终态停止 + failed 展示"
      contains: "useRestoreTask"
    - path: xingran-react-frontend/src/pages/network/backups/hooks/useBackupModals.ts
      provides: "handleRestore 异步化（携带 deviceId + 捕获 taskId + Modal 切换进度态）"
      contains: "taskId"
    - path: xingran-react-frontend/src/pages/network/backups/index.tsx
      provides: "恢复 Modal 进度/结果渲染（四态文案映射 + SentLines/ErrorMessage 展示）"
      contains: "RestoreTask"
  key_links:
    - from: xingran-react-frontend/src/pages/network/backups/hooks/useBackupModals.ts handleRestore
      to: POST /network/backups/:id/restore
      via: "post 携带 { deviceId }，读取 data.taskId"
      pattern: "taskId"
    - from: xingran-react-frontend/src/pages/network/backups/hooks/useRestoreTask.ts
      to: GET /network/backups/restore-tasks/:id
      via: "轮询任务详情直至终态"
      pattern: "restore-tasks/"
---

# Plan 93-05: 前端最小异步交互（恢复按钮 + 轮询 Modal）

<objective>
备份页恢复交互异步化（D-19 最小改动）：`useBackupModals.handleRestore` 携带所选备份自身 deviceId 发起恢复（顺带修复现状发空 body 缺 deviceId 必 400 的缺陷）、捕获 taskId；新建 `useRestoreTask` 轮询 hook（3s 间隔、终态停止、unmount 清理，照 useDiscoveryPolling 模式）；恢复 Modal 内嵌四态进度/结果展示（pending 排队 / running 下发中 / success 含下发行数 / failed 含失败行与错误信息）。不做独立任务管理页、不引入 WS（D-16/D-19）。API 调用沿用本页既有 `post`/`get` from `@/lib/api` 模式（不新建 wrapper 文件；wrapper 工厂化归 Phase 94）。
</objective>

<decisions_covered>
D-04（前端侧同设备语义——携带备份自身 deviceId）· D-16（前端轮询）· D-17（taskId 契约消费）· D-19（后端 + 最小前端，无独立任务页）
</decisions_covered>

<tasks>

<task id="93-05-01" type="tdd">
<name>Task 1: useRestoreTask 轮询 hook + 类型 + 测试</name>
<files>xingran-react-frontend/src/pages/network/backups/types.ts, xingran-react-frontend/src/pages/network/backups/hooks/useRestoreTask.ts, xingran-react-frontend/src/pages/network/backups/hooks/useRestoreTask.test.tsx</files>
<read_first>
- xingran-react-frontend/src/pages/network/discoveries/hooks/useDiscoveryPolling.ts（D-16 轮询模板：3s interval + running 态启停 + unmount 清理 + useCallback 依赖纪律）
- xingran-react-frontend/src/pages/network/backups/hooks/useBackupData.ts（本页 API 调用模式：post/get from "@/lib/api" + PageResponse 解包）
- xingran-react-frontend/src/pages/network/backups/hooks/useBackupData.test.tsx（vitest mock 模式参照）
- xingran-react-frontend/src/pages/network/backups/types.ts（现有类型组织方式）
- CLAUDE.md § Frontend/React Best Practices（useEffect 依赖 CRITICAL 条款）
</read_first>
<action>
1. types.ts 新增 `ConfigRestoreTask` interface：id/deviceId/backupId/status("pending"|"running"|"success"|"failed")/totalLines?/sentLines?/failedLine?/result?（{ sentLines, totalLines, failedLine, hashMatched, restoredHash } 可选）/errorMessage?/startedAt?/completedAt?/createdBy?/createdAt?/updatedAt?（snake-case 兼容：字段名与 93-03 model json tag 对齐，执行时以 model json tag 为准）。
2. 新建 useRestoreTask.ts：`useRestoreTask(taskId: string | null)` → { task: ConfigRestoreTask | null, polling: boolean }；内部 `get<...>(`/network/backups/restore-tasks/${taskId}`)` 轮询；taskId 非空且 status 为 pending/running 时每 3s 轮询一次，success/failed 终态停止；taskId 变 null 时清空并停止；unmount 清理 timer；全部回调 useCallback/useEffect 依赖数组遵守 memoize 纪律（CLAUDE.md CRITICAL 条款，禁止对象字面量依赖）。
3. useRestoreTask.test.tsx：mock "@/lib/api" 的 get——①发起后 pending 态至少轮询 2 次随后返回 success → 断言停止且 task.status=="success"；②返回 failed → 断言 task.errorMessage 可读且停止；③taskId 置 null → 断言清理（timer 清空）。
</action>
<acceptance_criteria>
- types.ts 含 `ConfigRestoreTask` interface
- useRestoreTask.ts 含 3s 轮询与终态停止逻辑（grep 3000/setInterval 命中）且 useEffect 依赖数组无裸对象字面量
- `npm run type-check` exit 0
- `npm run test -- --run src/pages/network/backups/hooks/useRestoreTask` exit 0
</acceptance_criteria>
<verify>npm run type-check && npm run test -- --run src/pages/network/backups/hooks/useRestoreTask</verify>
<done>轮询 hook 按契约就位：3s 间隔、终态停止、unmount 清理，vitest 双路径全绿</done>
</task>

<task id="93-05-02" type="execute">
<name>Task 2: handleRestore 异步化 + 恢复 Modal 进度/结果渲染</name>
<files>xingran-react-frontend/src/pages/network/backups/hooks/useBackupModals.ts, xingran-react-frontend/src/pages/network/backups/index.tsx</files>
<read_first>
- xingran-react-frontend/src/pages/network/backups/hooks/useBackupModals.ts（:128-139 handleRestore 现状——post 空 body 缺 deviceId 的缺陷点）
- xingran-react-frontend/src/pages/network/backups/index.tsx（恢复 Modal 渲染位置与表单结构）
- xingran-react-frontend/src/pages/network/backups/hooks/useRestoreTask.ts（Task 1 产物）
- .planning/workstreams/milestone/phases/93-config-backup-todo-p2/93-CONTEXT.md（D-19 最小前端边界 + D-04 同设备语义）
</read_first>
<action>
1. useBackupModals.ts：handleRestore 改为——`post(`/network/backups/${selectedRestoreBackup.id}/restore`, { deviceId: selectedRestoreBackup.deviceId })`（修复缺 deviceId 缺陷，D-04 前端侧）；成功后从 `result.data.taskId` 捕获并存入新 state `activeRestoreTaskId`（不立即 closeRestoreModal，Modal 切换为进度态）；失败走既有 message.error 路径。返回值接口增加 `activeRestoreTaskId` 与 `closeRestoreModal` 复位逻辑（关闭时清 taskId）。
2. index.tsx：恢复 Modal 内嵌进度/结果展示——Task 1 的 useRestoreTask(activeRestoreTaskId) 驱动；四态文案映射 pending="排队等待中" / running="配置下发中" / success="恢复成功" / failed="恢复失败"；success 追加展示下发行数（sentLines/totalLines）与 hashMatched==false 时的黄色提示"回读配置与备份存在差异，已记录警告"；failed 展示 failedLine 与 errorMessage；running 态 Spin。保持既有 Modal 结构最小改动（D-19，不新建页面/路由）。
3. useBackupModals.test.tsx 同步：handleRestore 用例断言 post body 含 deviceId 且成功路径不再立即 closeRestoreModal（改为 taskId 态）——若既有用例断言旧同步行为，更新为新契约。
</action>
<acceptance_criteria>
- useBackupModals.ts 的 handleRestore 调用 post 时第二参含 `deviceId: selectedRestoreBackup.deviceId`（grep 命中）且读取 `taskId`
- index.tsx 恢复 Modal 含 pending/running/success/failed 四态文案与 errorMessage 展示（grep "恢复失败" 或等价命中）
- `npm run type-check` exit 0
- `npm run lint` exit 0
- `npm run test -- --run src/pages/network/backups` exit 0（本页全部测试不回归）
</acceptance_criteria>
<verify>npm run type-check && npm run lint && npm run test -- --run src/pages/network/backups</verify>
<done>恢复按钮异步交互闭环：发起携带 deviceId → taskId → Modal 内轮询进度/结果；既有页面测试全绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 任务接口响应 ↔ 前端渲染 | errorMessage/failedLine 等设备侧文本回流到 DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-12 | Tampering/XSS | errorMessage/failedLine 渲染注入脚本 | mitigate | React JSX 默认转义，禁用 dangerouslySetInnerHTML；任务查询端点受组权限保护（93-04） |
| T-93-13 | Information Disclosure | 任务详情暴露给无权用户 | mitigate | 前端不新增权限面——查询端点后端组权限已兜底（D-31），前端仅消费 |
| T-93-SC | Tampering | 依赖引入 | accept | 零新 npm 依赖（复用 antd/hooks 既有），无包合法性风险 |
</threat_model>

<verification>
1. `npm run type-check` + `npm run lint` exit 0
2. `npm run test -- --run src/pages/network/backups` exit 0
3. grep：handleRestore body 含 deviceId；useRestoreTask 轮询间隔 3s
</verification>

<success_criteria>
- [ ] 恢复发起 → taskId → 轮询 → Modal 进度/结果 全交互闭环
- [ ] 现状缺 deviceId 必 400 的缺陷修复
- [ ] type-check + lint + 页面测试三绿
</success_criteria>

<commit_plan>
1. feat(93): add useRestoreTask polling hook with types and tests (D-16) — types.ts + useRestoreTask.ts + useRestoreTask.test.tsx
2. feat(93): async restore interaction in backup modal (D-04/17/19) — useBackupModals.ts + index.tsx + 同步既有测试
</commit_plan>
