---
phase: 93
plan: 04
type: execute
wave: 4
depends_on: ["01", "02", "03"]
files_modified:
  - internal/services/config_backup_service_93_02_test.go
  - .planning/REQUIREMENTS.md
  - .planning/workstreams/milestone/ROADMAP.md
  - CLAUDE.md
autonomous: true
requirements: [BACKUP-CLOSED-04, BACKUP-CLOSED-05]
---

# Plan 93-04: 回归测试全量 + 端到端断言链 + 措辞校准 + 收口文档

<objective>
补齐 BACKUP-CLOSED-04/05：restore 全链路 FileTransport e2e（发起→任务执行→下发→回读 hash→终态）+ 失败 4 场景测试（写失败/损坏 gzip/下发中断留痕/DB 回滚）+ 端到端自动化断言链（CreateBackup→GetBackupContent→restore 任务→hash 一致）；同步修订 REQUIREMENTS/ROADMAP 措辞（D-01 校准 + D-33① _78_NN 笔误，同 commit）；`configBackupAllowedSortFields` 删除不存在的 status 行（D-33②，独立 commit）；CLAUDE.md 新增配置备份恢复约定 Convention 段（D-32，Phase 90/92 收口惯例）。
</objective>

<decisions_covered>
D-27（93_NN 新文件系列）· D-28（锁 4 失败场景，手法对照 79_06/portwrite 现有）· D-29（FileTransport e2e 零 SSH）· D-30（端到端=自动化集成测试）· D-32（CLAUDE.md Convention 段）· D-33（两处顺手修各自独立原子 commit；①与 D-01 措辞修订同 commit）
</decisions_covered>

<tasks>

<task id="93-04-01" type="tdd">
<files>internal/services/config_backup_service_93_02_test.go</files>
<read_first>
- internal/services/config_restore_task_service.go（93-03 产物：StartRestore/runRestore/RecoverStaleRunningTasks）
- internal/services/config_backup_service_79_06_test.go（newCbk7906/newExecutor7906/writeFixture7906 装配与 fixture 字节格式）
- internal/services/portwrite/testdata/huawei_shutdown_success.fixture（fixture 逐字节模板，含 sysname 行格式）
- .planning/workstreams/milestone/phases/93-config-backup-todo-p2/93-RESEARCH.md（§失败场景模拟手法表 + §SendConfigs e2e fixture 字节格式 + P12 sysname 韧性）
</read_first>
<action>
新建 config_backup_service_93_02_test.go（package services），覆盖：
1. `TestCbk93RestoreE2E_Happy`（D-29/D-30 主断言链）：newExecutor7906 装配（fixture：banner → GetConfig 周期 ×2〔创建备份读 + 恢复前备份读 + 恢复后回读按需扩展 cycles〕→ SendConfigs 段〔system-view 上升 + sysname 行 + 配置行 + quit + spare prompts，逐字节按 portwrite fixture 模板〕）；sqlite AutoMigrate ConfigBackup+NetworkDevice+Config+ConfigRestoreTask；流程 = CreateBackup（大文件 CompressLarge=true 验证压缩闭环）→ GetBackupContent 还原 → taskSvc.StartRestore → 轮询等待任务终态（Eventually，上限 10s）→ 断言任务 success、ResultJSON 反序列化 SentLines>0、版本链新增"恢复自版本 N"记录、GetBackupStatistics 不报错。
2. `TestCbk93RestoreE2E_HashMismatchWarns`（D-11 警告分支）：fixture 让恢复后回读输出与备份内容不同（第二轮 GetConfig 周期输出不同 config）→ 断言任务仍 success（警告不失败）+ ResultJSON hashMatched==false。
3. `TestCbk93RestoreE2E_SendInterrupted`（D-28③）：fixture 在 SendConfigs 段第 N 行后 EOF（缺 prompt）→ 断言任务 failed + ResultJSON 含 SentLines=N/FailedLine 非空（进度留痕）。
4. `TestCbk93RestorePreBackupFailureAborts`（D-28①+D-14）：executor 连接 fixture 缺 GetConfig 周期（恢复前备份 GetConfig 即败）→ 断言任务 failed + ErrorMessage 含备份相关 + **无下发命令消耗**（fixture 剩余未读或无 SendConfigs 段）+ 版本链无新记录。
5. `TestCbk93RestoreCrossDeviceRejected`（D-04）：StartRestore 传入不匹配 deviceID → error contains "备份不属于目标设备"；任务表无记录。
6. `TestCbk93RestoreMutualExclusion`（D-08）：同设备存在 pending 任务行（手工插入）→ 第二次 StartRestore error contains "进行中的恢复任务"。
7. `TestCbk93RecoverStaleRunning`（A5/P2）：插入 status=running 任务行 → 调 RecoverStaleRunningTasks → 断言该行 status=failed 且 ErrorMessage 含"服务重启"；再断言同设备 StartRestore 可发起（锁已释放）。
8. `TestCbk93SortWhitelistNoStatus`（D-33② 回归守护）：GetBackupList 传 orderByColumn="status" → 断言不 panic 且 SQL 不报错（status 已从白名单删除，ApplySort 忽略非法列后走默认排序）——本用例在 D-33② 修复后编写，先红后绿（TDD）。
9. `TestCbk93RestoreDBFailureRollback`（D-28④）：对任务表注入写入失败（关闭 sqlDB 或超长列值）→ 断言无半写任务记录/备份记录（事务或先写后查语义验证）。
</action>
<acceptance_criteria>
- config_backup_service_93_02_test.go 含 ≥9 个 TestCbk93* 函数（上述清单）
- e2e fixture 含 sysname 行（P12）
- `go test ./internal/services/ -run TestCbk93 -count=1` exit 0 全绿
- `go test ./internal/services/ -count=1` exit 0
- BACKUP-CLOSED-05 断言链（用例 1）覆盖 备份→恢复→hash 一致 全程且通过
</acceptance_criteria>
<verify>go test ./internal/services/ -run "TestCbk93" -v -count=1</verify>
</task>

<task id="93-04-02" type="execute">
<files>.planning/REQUIREMENTS.md, .planning/workstreams/milestone/ROADMAP.md</files>
<read_first>
- .planning/REQUIREMENTS.md（§ BACKUP-CLOSED :78-86）
- .planning/workstreams/milestone/ROADMAP.md（§ Phase 93 的 Plans 段）
- .planning/workstreams/milestone/phases/93-config-backup-todo-p2/93-CONTEXT.md（D-01 措辞校准 + D-33① 原文）
</read_first>
<action>
同一次提交内（D-33① 与 D-01 措辞修订同 commit）：
1. REQUIREMENTS.md § BACKUP-CLOSED：BACKUP-CLOSED-03 的"事务化：读备份 → 校验 schema → 批量 upsert → 失效相关缓存"改为"异步任务化设备配置恢复：读备份 → 基础清洗 → RestoreConfig 下发 → 回读 hash 校验（恢复=把备份配置下发到网络设备，D-01 校准）"；BACKUP-CLOSED-04 的 `config_backup_service_78_NN_test.go` 改为 `config_backup_service_93_NN_test.go`（笔误修正）。
2. ROADMAP.md § Phase 93：93-02 plan 描述"实现恢复(line 543)：事务化（读备份 → 校验 schema → 批量 upsert → 失效缓存）"改为"实现恢复(line 543)：异步任务化（同设备校验 → 恢复前备份 → 清洗 → RestoreConfig 下发 → 回读校验 → 任务终态）"；Plans 段补记实际 4-plan 拆分（93-01..93-04，wave 1/2/3/4）。
3. 不改 Success Criteria 语义（端到端/0 回归表述已兼容）。
</action>
<acceptance_criteria>
- REQUIREMENTS.md § BACKUP-CLOSED 不再含 "校验 schema" 与 "_78_NN"（grep 0 命中）
- ROADMAP.md § Phase 93 不再含 "批量 upsert"（grep 0 命中）且含 "93-04"
- git diff 仅涉及上述两文件的本 phase 段落
</acceptance_criteria>
<verify>grep -c "校验 schema\|_78_NN" .planning/REQUIREMENTS.md（期望 0）</verify>
</task>

<task id="93-04-03" type="execute">
<files>internal/services/config_backup_service.go</files>
<read_first>
- internal/services/config_backup_service.go（:463-469 configBackupAllowedSortFields——含不存在的 status 列）
- internal/services/base/（ApplySort 白名单语义）
</read_first>
<action>
从 `configBackupAllowedSortFields` map 删除 `"status": "status",` 行（sys_config_backup 表无 status 列，用户传 orderByColumn=status 会 ORDER BY 不存在列 → SQL 500；D-33② 独立原子 commit——与本 plan 其他 commit 分开）。93-04-01 用例 8 为其回归守护（先写测试后修的 TDD 顺序：先跑用例 8 确认红，再删行确认绿）。
</action>
<acceptance_criteria>
- configBackupAllowedSortFields 不含 status 键（grep 0 命中）
- `go test ./internal/services/ -run TestCbk93SortWhitelistNoStatus -count=1` exit 0
- 该修复位于独立 commit（git log 该文件最近一次 commit 仅含此改动）
</acceptance_criteria>
<verify>go test ./internal/services/ -run TestCbk93SortWhitelistNoStatus -v -count=1</verify>
</task>

<task id="93-04-04" type="execute">
<files>CLAUDE.md</files>
<read_first>
- CLAUDE.md（§ Cache Service Convention / § Timeout Constants Convention 的段落格式——Phase 90/92 收口惯例）
- .planning/workstreams/milestone/phases/93-config-backup-todo-p2/93-CONTEXT.md（D-32 三条锁定点）
</read_first>
<action>
在 CLAUDE.md 的 Critical Development Rules 区域（Cache Service Convention 段之后）新增 "### Config Backup Restore Convention" 段，锁定三条路径（D-32）：
1. 压缩/解压唯一走 `gzipCompress`/`gzipDecompress` helper（config_backup_service.go），`.conf.gz` 后缀 + Compressed 标志双检查；
2. 设备配置下发唯一入口 `DeviceExecutor.RestoreConfig`（device 包，内部 SendConfigs + 清洗 + vendor 退出命令）——禁止绕过直接调 wrapper 或 ExecuteMultipleOnDevice 下发配置；
3. 恢复必须走异步任务模式（ConfigRestoreTaskService.StartRestore，detached context + 四态状态机）——禁止同步下发实现；附恢复流程一行链（发起校验/互斥 → 恢复前备份 → 清洗下发 fail-fast → 回读 hash 警告 → 版本链记录 → 任务终态）。
</action>
<acceptance_criteria>
- CLAUDE.md 含 "### Config Backup Restore Convention" 段（grep 命中）且含 gzipCompress/RestoreConfig/StartRestore 三个标识符
- 段落格式与既有 Convention 段一致（Rules 列表式）
</acceptance_criteria>
<verify>grep -A 5 "Config Backup Restore Convention" CLAUDE.md</verify>
</task>

</tasks>

<threat_model>
| 威胁 | STRIDE | 严重度 | Mitigation | 落点 |
|------|--------|--------|-----------|------|
| 回归破坏既有功能（三处 TODO 实现波及存量链路） | (quality) | medium | BACKUP-CLOSED-05 验收线：`go test ./internal/services/...` 全量 0 回归 + 93_02 ≥9 用例 + 93_01 既有 7 用例 | 93-04-01 |
| 测试遗漏 prompt 漂移（真机恢复卡死类缺陷逃逸到生产） | (coverage) | medium | e2e fixture 刻意包含 sysname 行（P12/A3）；fail-fast 与 hash-mismatch 双负路径覆盖 | 93-04-01 用例 1/2/3 |
</threat_model>

<verification>
1. `go build ./...` + `go test ./internal/services/... -count=1` 全绿（BACKUP-CLOSED-05）
2. `go test ./... -count=1` 全绿（v1.29 D-03 零失败底线，收口前跑一次全量）
3. `npm run type-check && npm run lint && npm run test` 全绿（前端 93-03 产物回归确认）
4. grep：REQUIREMENTS 无 _78_NN / "校验 schema"；ROADMAP 无 "批量 upsert"；CLAUDE.md 含新 Convention 段
5. git log：D-33② 修复为独立 commit
</verification>

<must_haves>
- BACKUP-CLOSED-04: 93_NN 测试系列（93_01 7 用例 + 93_02 ≥9 用例）覆盖 compress/decompress/restore 三路径 + 失败 4 场景 + 端到端断言链
- BACKUP-CLOSED-05: `go test ./internal/services/...` 0 回归 + 端到端 备份→恢复→配置一致 断言链通过（D-30 自动化落地）
- D-27: 测试落 93_01/93_02 新文件（无 79_06 追加）
- D-28: 4 失败场景各有具名用例（①TestCbk93RestorePreBackupFailureAborts 内含写失败语义 / ②93_01 用例 2-3 损坏流 / ③TestCbk93RestoreE2E_SendInterrupted / ④TestCbk93RestoreDBFailureRollback）
- D-29: restore e2e 全部 FileTransport 零 SSH、可进 CI
- D-30: 无手动冒烟依赖，验收 = go test
- D-32: CLAUDE.md Convention 段锁定三条路径
- D-33: ① 措辞+笔误同 commit 修订 REQUIREMENTS/ROADMAP；② status 白名单独立 commit 修复
</must_haves>

<success_criteria>
- [ ] `go test ./... -count=1` 全绿（0 失败，v1.29 D-03）
- [ ] 93_02 e2e 断言链（happy/hashMismatch/interrupted/abort）4 场景全过
- [ ] 文档三处（REQUIREMENTS/ROADMAP/CLAUDE.md）修订到位且 grep 可验证
- [ ] D-33② 独立 commit 确认
</success_criteria>

<commit_plan>
1. test(93): restore e2e and failure-scenario regression suite (D-27..D-30, BACKUP-CLOSED-04/05) — 93_02_test.go
2. docs(93): calibrate restore semantics wording and fix test-file-name typo (D-01/D-33a) — REQUIREMENTS.md + ROADMAP.md
3. fix(93): remove nonexistent status column from backup sort whitelist (D-33b) — config_backup_service.go
4. docs(93): add Config Backup Restore Convention to CLAUDE.md (D-32) — CLAUDE.md
</commit_plan>
