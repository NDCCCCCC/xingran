---
phase: 93
plan: 04
type: execute
wave: 4
depends_on: ["93-03"]
files_modified:
  - internal/api/v1/network/backup_handler.go
  - internal/api/v1/network/network_router.go
  - internal/api/v1/network/backup_handler_test.go
  - internal/services/config_backup_service.go
autonomous: true
requirements: [BACKUP-CLOSED-03]

must_haves:
  truths:
    - "D-01: POST /network/backups/:id/restore 的语义落地为异步设备配置下发任务发起——handler 调 restoreTaskSvc.StartRestore（非同步下发）"
    - "D-07: Restore 成功响应扩展为 gin.H{taskId, status, message}（前端展示'发了多少行、是否全部成功'的数据入口由 taskId → 任务查询完成）"
    - "D-13: operlog 复用 OperTypeUpdate（module='配置备份'），25 常量锁值防线零改动（operlog regression_test 不回归）"
    - "D-17: 触发返回 taskId——handler 校验后调 StartRestore 并 response.Success 返回 taskId；新增任务查询双端点 POST /backups/restore-tasks/list + POST /backups/restore-tasks/:id"
    - "D-18: operlog.Record 在 handler success path 末尾、response.Success 之前（发起时记录一次，任务结果只在任务表不双记）"
    - "D-31: 任务查询端点挂 /backups 路由组内，继承组级 4 权限点（list/add/restore/diff 任一），零新权限点、零菜单/角色迁移"
    - "BACKUP-CLOSED-03（收口）: config_backup_service.go 的 RestoreBackup stub 方法与 '配置恢复功能待实现' 注释删除（与 handler 切换同 commit，编译器当 checklist）"
  artifacts:
    - path: internal/api/v1/network/backup_handler.go
      provides: "BackupHandler.restoreTaskSvc 依赖 + Restore 异步语义 + GetRestoreTask/ListRestoreTasks handler 方法"
      contains: "restoreTaskSvc"
    - path: internal/api/v1/network/network_router.go
      provides: "restoreTaskSvc 装配（backupService 之后）+ RecoverStaleRunningTasks 启动调用 + /restore-tasks 双路由（backups 组内）"
      contains: "NewConfigRestoreTaskService"
    - path: internal/api/v1/network/backup_handler_test.go
      provides: "TestBackupHandler_Restore 重写（taskId 契约/跨设备拒绝/互斥拒绝），restore_is_a_documented_stub 子测试删除"
      contains: "taskId"
    - path: internal/services/config_backup_service.go
      provides: "RestoreBackup stub 方法删除（TODO 注释清零）"
      not_contains: "配置恢复功能待实现"
  key_links:
    - from: internal/api/v1/network/backup_handler.go Restore
      to: internal/services/config_restore_task_service.go StartRestore
      via: "handler → task service 发起"
      pattern: "StartRestore\\("
    - from: internal/api/v1/network/network_router.go
      to: middleware.RequirePermissions
      via: "/restore-tasks/* 路由位于 backups 组 Use 之后继承组权限（D-31）"
      pattern: "restore-tasks/list"
---

# Plan 93-04: Restore handler 异步语义 + 任务查询端点 + 装配接线

<objective>
把 `POST /network/backups/:id/restore` 从同步 stub 错误切换为异步任务语义：handler 调 `ConfigRestoreTaskService.StartRestore` 后立即返回 `{taskId, status, message}`（operlog 发起时记录，D-13/D-18）；新增任务查询双端点（list/detail）挂 /backups 组内继承组权限（D-31）；network_router.go 装配 restoreTaskSvc 并启动时调 `RecoverStaleRunningTasks` 一次性收敛（A5）；同 commit 删除 config_backup_service.go 的 RestoreBackup stub（编译器当 checklist 证明零遗漏）；backup_handler_test.go 重写 Restore 用例（stub 锁测试已失真）。
</objective>

<decisions_covered>
D-01（恢复=设备下发，端点语义落地）· D-07（handler 响应扩展 taskId）· D-13（复用 OperTypeUpdate）· D-17（返回 taskId + 列表/详情双端点）· D-18（handler 发起时 operlog）· D-31（组权限复用）
</decisions_covered>

<tasks>

<task id="93-04-01" type="execute">
<name>Task 1: handler 异步切换 + 双端点 + 装配 + stub 删除（原子 commit）</name>
<files>internal/api/v1/network/backup_handler.go, internal/api/v1/network/network_router.go, internal/services/config_backup_service.go</files>
<read_first>
- internal/api/v1/network/backup_handler.go（Restore :257-288 现状 + BackupHandler struct/NewBackupHandler/WithCore 模式 :25-33）
- internal/api/v1/network/network_router.go（:40 backupService 构造 + :46 backupHandler 构造 + :150-174 backups 组）
- internal/services/config_restore_task_service.go（93-03 产物：StartRestore/GetRestoreTask/ListRestoreTasks/RecoverStaleRunningTasks 签名）
- internal/services/config_backup_service.go（RestoreBackup :536-546 stub——本 task 删除）
- CLAUDE.md § operlog convention（D-18 记录位置）
</read_first>
<action>
1. BackupHandler struct 增加 `restoreTaskSvc *services.ConfigRestoreTaskService` 字段；NewBackupHandler 签名追加该参数（现签名 `NewBackupHandler(backupService *services.ConfigBackupService, db *gorm.DB)`，改为三参；WithCore 链保持）。
2. network_router.go 装配（:40 backupService 构造之后、backupHandler 构造之前）：`restoreTaskSvc := services.NewConfigRestoreTaskService(db, backupService, core.DeviceExecutor)`；紧跟非阻断调用一次 `restoreTaskSvc.RecoverStaleRunningTasks(context.Background())`（启动收敛，失败仅 applogger 记录不阻断路由装配）；NewBackupHandler 调用点补第三参。注意：ConfigBackupService 的装配点在 network_router.go:40 而非 core.go（batch_export_helper.go:306 与 network_export_handler.go:356 是导出场景的临时实例，不接线、不改动）。
3. Restore handler 改造：`task, err := h.restoreTaskSvc.StartRestore(c.Request.Context(), id, req.DeviceID, 当前用户)`（userID 经 `c.Get("user_id")` 取，BatchBackup 同款）；错误仍走 HandleServiceError；成功路径 `operlog.Record(c, h.core.OperLogService, h.core.GetDB(), "配置备份", operlog.OperTypeUpdate)` 保持原位（success path 末尾、response.Success 之前，D-13/D-18）；响应改为 `response.Success(c, gin.H{"taskId": task.ID, "status": task.Status, "message": "恢复任务已创建"})`（D-07/D-17）。
4. 新增任务查询双端点 handler 方法（同文件）：`GetRestoreTask`（POST :id 语义——`restoreTaskSvc.GetRestoreTask`）与 `ListRestoreTasks`（POST list 语义——body {deviceId?, current, pageSize, orderByColumn?, isAsc?}，透传 service 分页排序）；查询端点不记 operlog。
5. network_router.go backups 组内追加两条路由：`backups.POST("/restore-tasks/list", backupHandler.ListRestoreTasks)` 与 `backups.POST("/restore-tasks/:id", backupHandler.GetRestoreTask)`——必须位于 `backups.Use(middleware.RequirePermissions(...))` 之后（D-31 继承组级 4 权限点，零新权限点）。
6. 删除 config_backup_service.go 的 `RestoreBackup` 方法整体（:536-546，含 "TODO: 实现配置恢复逻辑" 注释与 "配置恢复功能待实现" 错误）——与 handler 切换同 commit，`go build` 充当删除完整性 checklist（全仓唯一调用方是本 task 改造的 handler）。
</action>
<acceptance_criteria>
- backup_handler.go Restore 成功响应包含 `"taskId"`（grep 命中）且 operlog.Record 调用仍在且使用 operlog.OperTypeUpdate
- backup_handler.go 含 GetRestoreTask 与 ListRestoreTasks 两个方法
- network_router.go 含 `NewConfigRestoreTaskService` 装配、`RecoverStaleRunningTasks` 调用、`/restore-tasks/list` 与 `/restore-tasks/:id` 两条路由且位于 backups 组 Use(RequirePermissions) 之后
- config_backup_service.go 不再包含 "配置恢复功能待实现" 与 "TODO: 实现配置恢复逻辑"（grep 0 命中）；RestoreBackup 方法已删除
- `go build ./...` exit 0（编译器确认无残留调用方）
- `go test ./internal/utils/operlog/ -count=1` exit 0（D-13 锁值防线零改动）
</acceptance_criteria>
<verify>go build ./... && go test ./internal/utils/operlog/ -count=1</verify>
<done>restore 端点异步语义切换完成：taskId 响应 + 双查询端点 + 装配收敛 + stub 原子删除，build/锁值防线全绿</done>
</task>

<task id="93-04-02" type="tdd">
<name>Task 2: handler 测试重写（taskId 契约）</name>
<files>internal/api/v1/network/backup_handler_test.go</files>
<read_first>
- internal/api/v1/network/backup_handler_test.go（:220-246 TestBackupHandler_Restore 及 :233 restore_is_a_documented_stub 子测试；newBackupTestEnv/newBackupHandler/seedBackup/netServe/decodeNetResp helper）
- internal/api/v1/network/backup_handler.go（Task 1 产物：Restore/GetRestoreTask/ListRestoreTasks）
- internal/services/config_restore_task_service.go（StartRestore 错误分支语义）
</read_first>
<action>
重写 TestBackupHandler_Restore：删除 `restore_is_a_documented_stub` 子测试（断言"配置恢复功能待实现"已失真）。newBackupTestEnv 装配真实 ConfigRestoreTaskService（sqlite + db + backupService + nil executor——StartRestore 在校验/互斥/建任务层即返回不触达下发；异步 goroutine 经 93-03 的 nil-executor 防护安全置 failed，不影响 HTTP 断言）。用例：
1. `restore_returns_task_id`：seed 备份（deviceId 匹配）→ POST restore → 断言 HTTP 200、resp.Code==0、resp.data.taskId 非空且 status=="pending"（D-07/D-17 契约）。
2. `cross_device_rejected`：deviceId 与备份不匹配 → 断言错误且 message contains "备份不属于目标设备"（D-04 透传）。
3. `mutual_exclusion_rejected`：预插 pending 任务行 → POST restore → 断言错误 contains "进行中的恢复任务"（D-08 透传）。
4. `binding_requires_deviceId` 保留。
5. 补 GetRestoreTask/ListRestoreTasks 冒烟：seed 任务行 → 各一用例断言 200 + 返回字段（status/deviceId）。
</action>
<acceptance_criteria>
- backup_handler_test.go 无 "restore_is_a_documented_stub"（grep 0 命中）且含 "restore_returns_task_id"
- `go test ./internal/api/v1/network/ -count=1` exit 0（全包不回归）
</acceptance_criteria>
<verify>go test ./internal/api/v1/network/ -count=1</verify>
<done>handler 测试与新契约一致：taskId 契约 + D-04/D-08 错误透传 + 查询端点冒烟全绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP 客户端 ↔ restore/restore-tasks 端点 | 未授权/越权调用高危恢复端点与任务查询端点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-10 | Elevation of Privilege | 未授权触发恢复（横向越权调 POST /backups/:id/restore） | mitigate | /backups 组级 RequirePermissions（network:backup:restore 在 4 权限点集内，D-31 继承）+ operlog 审计留痕（D-13/D-18）；T-93-01 跨设备误推由 93-03 service 层校验兜底 |
| T-93-09 | Information Disclosure | taskId 枚举探测他人任务 | mitigate | 任务 UUID 不可枚举 + 查询端点同处组权限保护（Task 1.5） |
| T-93-11 | Repudiation | 恢复发起无审计 | mitigate | operlog.Record 于 success path 末尾（D-18），module="配置备份" + OperTypeUpdate，25 常量锁值防线零改动（D-13） |
| T-93-SC | Tampering | 依赖引入 | accept | 零新外部依赖，无包合法性风险 |
</threat_model>

<verification>
1. `go build ./...` exit 0
2. `go test ./internal/api/v1/network/ -count=1` 全绿
3. `go test ./internal/utils/operlog/ ./internal/models/ -count=1` 全绿（锁值防线零改动承诺）
4. grep："配置恢复功能待实现" 全仓 0 命中；/restore-tasks 双路由存在
</verification>

<success_criteria>
- [ ] restore 端点返回 taskId；/restore-tasks/list 与 /restore-tasks/:id 上线且继承组权限
- [ ] RestoreBackup stub 删除（BACKUP-CLOSED-03 三处 TODO 全部清零）
- [ ] handler 测试全绿（stub 锁测试移除 + 新契约用例）
- [ ] operlog 锁值防线零回归
</success_criteria>

<commit_plan>
1. feat(93): switch restore endpoint to async taskId semantics and remove stub (D-01/07/13/17/18/31) — backup_handler.go + network_router.go + config_backup_service.go（stub 删除同 commit）
2. test(93): rewrite restore handler tests for taskId contract — backup_handler_test.go
</commit_plan>
