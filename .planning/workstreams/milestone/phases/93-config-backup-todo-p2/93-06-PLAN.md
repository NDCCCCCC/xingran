---
phase: 93
plan: 06
type: execute
wave: 5
depends_on: ["93-03", "93-04", "93-05"]
files_modified:
  - internal/services/config_backup_service_93_02_test.go
  - internal/services/config_backup_service.go
  - .planning/REQUIREMENTS.md
  - .planning/workstreams/milestone/ROADMAP.md
  - CLAUDE.md
autonomous: true
requirements: [BACKUP-CLOSED-04, BACKUP-CLOSED-05]

must_haves:
  truths:
    - "BACKUP-CLOSED-04: 93_NN 测试系列（93_01 ≥10 用例 + 93_02 ≥9 用例）覆盖 compress/decompress/restore 三路径 + 失败 4 场景（写失败/损坏 gzip/下发中断留痕/DB 写入失败）"
    - "BACKUP-CLOSED-05: `go test ./internal/services/...` 0 回归 + 端到端 备份→恢复→配置一致 自动化断言链通过（D-30：验收=go test，无手动冒烟）"
    - "D-27: 测试落 config_backup_service_93_NN_test.go 新文件系列（93_01/93_02），不追加 79_06 文件（职责分离；REQUIREMENTS 原 _78_NN 是笔误）"
    - "D-28: 4 失败场景各有具名用例——①TestCbk93CreateBackupWriteFailure（93_01）②TestCbk93DecompressCorruptHeader/Truncated（93_01）③TestCbk93RestoreE2E_SendInterrupted ④TestCbk93RestoreDBFailureRollback（本 plan）"
    - "D-29: restore e2e 全链路（发起→任务执行→清洗→SendConfigs→回读 hash→状态流转）FileTransport 零 SSH、SeedConnectionForTesting 注入、可进 CI"
    - "D-30: 端到端验收 = 自动化集成测试（CreateBackup → GetBackupContent → StartRestore → 任务 success + hash 一致断言），Phase 92 D-09 先例"
    - "D-32: CLAUDE.md 新增 'Config Backup Restore Convention' 段锁定三条——压缩/解压统一 gzip helper、下发唯一入口 DeviceExecutor.RestoreConfig、恢复必须异步任务模式（禁止同步下发）"
    - "D-33: ① REQUIREMENTS _78_NN→93_NN 笔误 + 恢复措辞按 D-01 校准（同 commit）② configBackupAllowedSortFields 删除不存在的 status 行（独立原子 commit，修 SQL 500）"
  artifacts:
    - path: internal/services/config_backup_service_93_02_test.go
      provides: "restore e2e ≥9 用例（happy/hashMismatch/interrupted/preBackupAbort/crossDevice/mutex/recoverStale/dbFailure/sortWhitelist）"
      contains: "TestCbk93RestoreE2E_Happy"
    - path: .planning/REQUIREMENTS.md
      provides: "BACKUP-CLOSED 措辞校准（异步任务化设备配置下发）+ 93_NN 笔误修正"
      not_contains: "_78_NN"
    - path: .planning/workstreams/milestone/ROADMAP.md
      provides: "Phase 93 段与 D-01 措辞一致（planning 阶段已校准 plan 列表，本 plan 核对无残留）"
      not_contains: "批量 upsert"
    - path: CLAUDE.md
      provides: "Config Backup Restore Convention 段（三条锁定）"
      contains: "Config Backup Restore Convention"
  key_links:
    - from: internal/services/config_backup_service_93_02_test.go TestCbk93RestoreE2E_Happy
      to: internal/services/config_restore_task_service.go StartRestore
      via: "e2e 断言链调用真实编排（D-29/D-30）"
      pattern: "StartRestore\\("
    - from: CLAUDE.md
      to: internal/device/restore_config.go
      via: "Convention 段锁定 RestoreConfig 唯一下发入口（D-32）"
      pattern: "RestoreConfig"
---

# Plan 93-06: restore e2e + 失败场景全量 + 措辞校准 + Convention 收口

<objective>
补齐 BACKUP-CLOSED-04/05：restore 全链路 FileTransport e2e（发起→任务执行→下发→回读 hash→终态）+ 失败 4 场景测试 + 端到端自动化断言链（CreateBackup→GetBackupContent→StartRestore→任务 success→hash 一致）；REQUIREMENTS 恢复措辞按 D-01 校准 + `_78_NN` 笔误修正（同 commit，ROADMAP 的 plan 列表已在 planning 阶段校准、本 plan 核对无残留）；`configBackupAllowedSortFields` 删除不存在的 status 行（D-33②，独立 commit，先红后绿）；CLAUDE.md 新增 Config Backup Restore Convention 段（D-32）；全量回归 gate（go test ./... + 前端三件套）。
</objective>

<decisions_covered>
D-27（93_NN 新文件系列）· D-28（锁 4 失败场景，手法对照 79_06/portwrite 现有）· D-29（FileTransport e2e 零 SSH）· D-30（端到端=自动化集成测试）· D-32（CLAUDE.md Convention 段）· D-33（两处顺手修各自独立原子 commit；①与 D-01 措辞修订同 commit）
</decisions_covered>

<tasks>

<task id="93-06-01" type="tdd">
<name>Task 1: restore e2e + 失败场景回归套件</name>
<files>internal/services/config_backup_service_93_02_test.go</files>
<read_first>
- internal/services/config_restore_task_service.go（93-03 产物：StartRestore/runRestore/RecoverStaleRunningTasks）
- internal/services/config_backup_service_79_06_test.go（newCbk7906/newExecutor7906/writeFixture7906/writeFixtureBytes7906 装配与 fixture 字节格式）
- internal/services/portwrite/testdata/huawei_shutdown_success.fixture（fixture 逐字节模板，含 sysname 行格式，只读参照）
- .planning/workstreams/milestone/phases/93-config-backup-todo-p2/93-RESEARCH.md（失败场景模拟手法表 + SendConfigs e2e fixture 字节格式 + P12 sysname 韧性）
</read_first>
<action>
新建 config_backup_service_93_02_test.go（package services），覆盖：
1. `TestCbk93RestoreE2E_Happy`（D-29/D-30 主断言链）：newExecutor7906 装配（fixture：banner → GetConfig 周期 ×N〔创建备份读 + 恢复前备份读 + 恢复后回读按需扩展 cycles〕→ SendConfigs 段〔配置行 + quit 回显 + spare prompts，逐字节按 portwrite fixture 模板〕）；sqlite AutoMigrate ConfigBackup+NetworkDevice+Config+ConfigRestoreTask；流程 = CreateBackup（大文件 CompressLarge=true 验证压缩闭环）→ GetBackupContent 还原 → taskSvc.StartRestore → 轮询等待任务终态（Eventually，上限 10s）→ 断言任务 success、ResultJSON 反序列化 SentLines>0、版本链新增 "恢复自版本 N" 记录、GetBackupStatistics 不报错。
2. `TestCbk93RestoreE2E_HashMismatchWarns`（D-11 警告分支）：fixture 让恢复后回读输出与备份内容不同 → 断言任务仍 success（警告不失败）+ ResultJSON hashMatched==false。
3. `TestCbk93RestoreE2E_SendInterrupted`（D-28③）：fixture 在 SendConfigs 段第 N 行后 EOF（缺 prompt）→ 断言任务 failed + ResultJSON 含 SentLines==N/FailedLine 非空（进度留痕）。
4. `TestCbk93RestorePreBackupFailureAborts`（D-28①+D-14）：executor 连接 fixture 缺 GetConfig 周期（恢复前备份 GetConfig 即败）→ 断言任务 failed + ErrorMessage 含备份相关 + 无下发命令消耗（fixture 剩余未读或无 SendConfigs 段）+ 版本链无新记录。
5. `TestCbk93RestoreCrossDeviceRejected`（D-04）：StartRestore 传入不匹配 deviceID → error contains "备份不属于目标设备"；任务表无记录。
6. `TestCbk93RestoreMutualExclusion`（D-08）：同设备存在 pending 任务行（手工插入）→ 第二次 StartRestore error contains "进行中的恢复任务"。
7. `TestCbk93RecoverStaleRunning`（A5/P2）：插入 status=running 任务行 → 调 RecoverStaleRunningTasks → 断言该行 status=failed 且 ErrorMessage 含 "服务重启"；再断言同设备 StartRestore 可发起（锁已释放）。
8. `TestCbk93RestoreDBFailureRollback`（D-28④）：对任务表注入写入失败（关闭 sqlDB 或超长列值）→ 断言无半写任务记录/备份记录（事务或先写后查语义验证）。
9. `TestCbk93SortWhitelistNoStatus` 于 93-06-03 落地后归入本文件（先红后绿与其修复同 task，见 93-06-03）。
</action>
<acceptance_criteria>
- config_backup_service_93_02_test.go 含 ≥8 个 TestCbk93* 函数（清单 1-8；93-06-03 完成后含 sortWhitelist 共 ≥9）
- e2e fixture 含 sysname 行（P12）
- `go test ./internal/services/ -run TestCbk93 -count=1` exit 0 全绿
- `go test ./internal/services/ -count=1` exit 0
- BACKUP-CLOSED-05 断言链（用例 1）覆盖 备份→恢复→hash 一致 全程且通过
</acceptance_criteria>
<verify>go test ./internal/services/ -run "TestCbk93" -v -count=1</verify>
<done>restore e2e（happy/hashMismatch/interrupted/abort）+ 失败 4 场景 + 互斥/收敛用例全绿，BACKUP-CLOSED-04 落地</done>
</task>

<task id="93-06-02" type="execute">
<name>Task 2: REQUIREMENTS 措辞校准 + _78_NN 笔误修正（同 commit）</name>
<files>.planning/REQUIREMENTS.md, .planning/workstreams/milestone/ROADMAP.md</files>
<read_first>
- .planning/REQUIREMENTS.md（§ BACKUP-CLOSED :78-86）
- .planning/workstreams/milestone/ROADMAP.md（§ Phase 93——plan 列表已于 planning 阶段按 D-01 校准，本 task 核对一致性）
- .planning/workstreams/milestone/phases/93-config-backup-todo-p2/93-CONTEXT.md（D-01 措辞校准 + D-33① 原文）
</read_first>
<action>
同一次提交内（D-33① 与 D-01 措辞修订同 commit）：
1. REQUIREMENTS.md § BACKUP-CLOSED：BACKUP-CLOSED-03 的 "事务化：读备份 → 校验 schema → 批量 upsert → 失效相关缓存" 改为 "异步任务化设备配置恢复：同设备校验 → 恢复前自动备份 → 基础清洗 → RestoreConfig 下发（fail-fast）→ 回读 hash 校验 → 版本链恢复记录（恢复=把备份配置下发到网络设备，D-01 校准）"；BACKUP-CLOSED-04 的 `config_backup_service_78_NN_test.go` 改为 `config_backup_service_93_NN_test.go`（笔误修正）。
2. ROADMAP.md § Phase 93：核对 Success Criteria 与 plan 列表无 "事务化/批量 upsert" 残留（planning 阶段已校准）；如有残留同步修正。不改 Success Criteria 语义（端到端/0 回归表述已兼容）。
</action>
<acceptance_criteria>
- REQUIREMENTS.md § BACKUP-CLOSED 不再含 "校验 schema" 与 "_78_NN"（grep 0 命中）
- ROADMAP.md § Phase 93 不再含 "批量 upsert"（grep 0 命中）
- git diff 仅涉及上述两文件的本 phase 段落
</acceptance_criteria>
<verify>grep -c "校验 schema\|_78_NN" .planning/REQUIREMENTS.md（期望 0）</verify>
<done>两处规划文档措辞与 D-01/D-33① 对齐，独立 commit 可追溯</done>
</task>

<task id="93-06-03" type="execute">
<name>Task 3: D-33② 排序白名单 status 修复（先红后绿，独立 commit）</name>
<files>internal/services/config_backup_service.go, internal/services/config_backup_service_93_02_test.go</files>
<read_first>
- internal/services/config_backup_service.go（:463-469 configBackupAllowedSortFields——含不存在的 status 列）
- internal/services/base/（ApplySort 白名单语义）
- internal/services/config_backup_service_93_02_test.go（Task 1 产物——本 task 追加用例）
</read_first>
<action>
先写守护测试再修复（TDD，同 task 内闭环）：第一步在 config_backup_service_93_02_test.go 追加 `TestCbk93SortWhitelistNoStatus`（GetBackupList 传 orderByColumn="status" → 断言不 panic 且 SQL 不报错，status 应被白名单忽略后走默认排序），运行确认红（当前白名单含 status → ORDER BY status 报错）；随后从 `configBackupAllowedSortFields` map 删除 `"status": "status",` 行（sys_config_backup 表无 status 列，用户传该值会 SQL 500；D-33② 独立原子 commit——与本 plan 其他 commit 分开），重跑确认绿。
</action>
<acceptance_criteria>
- config_backup_service_93_02_test.go 含 TestCbk93SortWhitelistNoStatus
- configBackupAllowedSortFields 不含 status 键（grep 0 命中）
- `go test ./internal/services/ -run TestCbk93SortWhitelistNoStatus -count=1` exit 0
- 该修复位于独立 commit（git log 该文件最近一次 commit 仅含此改动）
</acceptance_criteria>
<verify>go test ./internal/services/ -run TestCbk93SortWhitelistNoStatus -v -count=1</verify>
<done>现存排序 500 bug 修复（D-33②），守护测试锁定，独立 commit</done>
</task>

<task id="93-06-04" type="execute">
<name>Task 4: CLAUDE.md Convention 段 + 全量回归 gate</name>
<files>CLAUDE.md</files>
<read_first>
- CLAUDE.md（§ Cache Service Convention / § Timeout Constants Convention 的段落格式——Phase 90/92 收口惯例）
- .planning/workstreams/milestone/phases/93-config-backup-todo-p2/93-CONTEXT.md（D-32 三条锁定点）
</read_first>
<action>
在 CLAUDE.md 的 Critical Development Rules 区域（Cache Service Convention 段之后）新增 "### Config Backup Restore Convention" 段，锁定三条路径（D-32）：
1. 压缩/解压唯一走 `gzipCompress`/`gzipDecompress` helper（config_backup_service.go），`.conf.gz` 后缀 + Compressed 标志双检查，解压含 64MB 上限防线；
2. 设备配置下发唯一入口 `DeviceExecutor.RestoreConfig`（device 包，内部 SendConfigs + 清洗 + vendor 退出命令）——禁止绕过直接调 wrapper 或 ExecuteMultipleOnDevice 下发配置；
3. 恢复必须走异步任务模式（ConfigRestoreTaskService.StartRestore，detached context + 四态状态机）——禁止同步下发实现；附恢复流程一行链（发起校验/互斥 → 恢复前备份 → 清洗下发 fail-fast → 回读 hash 警告 → 版本链记录 → 任务终态）。
随后跑全量回归 gate：`go build ./...`、`go test ./... -count=1`、`cd xingran-react-frontend && npm run type-check && npm run lint && npm run test`——全绿为 phase 收口条件（v1.29 D-03 零失败底线）。
</action>
<acceptance_criteria>
- CLAUDE.md 含 "### Config Backup Restore Convention" 段（grep 命中）且含 gzipCompress/RestoreConfig/StartRestore 三个标识符
- 段落格式与既有 Convention 段一致（Rules 列表式）
- `go build ./...` exit 0 且 `go test ./... -count=1` exit 0
- 前端 `npm run type-check` + `npm run lint` + `npm run test` 全 exit 0
</acceptance_criteria>
<verify>grep -A 5 "Config Backup Restore Convention" CLAUDE.md && go test ./... -count=1</verify>
<done>Convention 三条锁定入 CLAUDE.md，phase 全量 gate（go + npm）全绿，BACKUP-CLOSED-05 达成</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试守护 ↔ 生产行为 | 回归套件是三处 TODO 实现波及面的最后防线 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-14 | (quality/DoS) | 回归破坏既有功能（三处 TODO 实现波及存量链路） | mitigate | BACKUP-CLOSED-05 验收线：`go test ./...` 全量 0 回归 + 93_01/93_02 ≥19 用例双文件守护 |
| T-93-15 | (coverage) | 测试遗漏 prompt 漂移（真机恢复卡死类缺陷逃逸到生产） | mitigate | e2e fixture 刻意包含 sysname 行（P12/A3）；fail-fast 与 hash-mismatch 双负路径覆盖 |
| T-93-SC | Tampering | 依赖引入 | accept | 零新外部依赖，无包合法性风险 |
</threat_model>

<verification>
1. `go build ./...` + `go test ./internal/services/... -count=1` 全绿（BACKUP-CLOSED-05）
2. `go test ./... -count=1` 全绿（v1.29 D-03 零失败底线，收口前跑一次全量）
3. `npm run type-check && npm run lint && npm run test` 全绿（前端 93-05 产物回归确认）
4. grep：REQUIREMENTS 无 _78_NN / "校验 schema"；ROADMAP 无 "批量 upsert"；CLAUDE.md 含新 Convention 段
5. git log：D-33② 修复为独立 commit
</verification>

<success_criteria>
- [ ] `go test ./... -count=1` 全绿（0 失败，v1.29 D-03）
- [ ] 93_02 e2e 断言链（happy/hashMismatch/interrupted/abort）4 场景全过
- [ ] 文档三处（REQUIREMENTS/ROADMAP/CLAUDE.md）修订到位且 grep 可验证
- [ ] D-33② 独立 commit 确认
</success_criteria>

<commit_plan>
1. test(93): restore e2e and failure-scenario regression suite (D-27..D-30, BACKUP-CLOSED-04/05) — 93_02_test.go
2. docs(93): calibrate restore semantics wording and fix test-file-name typo (D-01/D-33①) — REQUIREMENTS.md + ROADMAP.md
3. fix(93): remove nonexistent status column from backup sort whitelist (D-33②) — config_backup_service.go + 守护测试
4. docs(93): add Config Backup Restore Convention to CLAUDE.md (D-32) — CLAUDE.md + 全量 gate 记录
</commit_plan>
