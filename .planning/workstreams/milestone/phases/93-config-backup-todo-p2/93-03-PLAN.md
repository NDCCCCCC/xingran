---
phase: 93
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - internal/models/config_restore_task.go
  - internal/core/db/database.go
  - internal/core/db/migrations/migration_211_create_config_restore_task.go
  - internal/services/config_restore_task_service.go
  - internal/services/config_backup_service.go
  - internal/api/v1/network/backup_handler.go
  - internal/api/v1/network/backup_handler_test.go
  - internal/api/v1/network/network_router.go
  - internal/core/core.go
  - xingran-react-frontend/src/lib/networkApi.ts
  - xingran-react-frontend/src/pages/network/backups/hooks/useRestoreTask.ts
  - xingran-react-frontend/src/pages/network/backups/hooks/useRestoreTask.test.ts
  - xingran-react-frontend/src/pages/network/backups/index.tsx
  - xingran-react-frontend/src/pages/network/backups/hooks/useBackupModals.ts
autonomous: true
requirements: [BACKUP-CLOSED-03, BACKUP-CLOSED-05]
---

# Plan 93-03: 恢复异步任务化（model/migration/service/handler/前端）

<objective>
把 RestoreBackup 从同步 stub（:543 "配置恢复功能待实现"）改造为异步任务化全链路：新增 `ConfigRestoreTask` model（string 四态状态机 + BeforeCreate UUID）+ Migrate211（双方言注册 + AutoMigrate 双线齐全）；`ConfigRestoreTaskService` 承载发起（同设备校验 + 互斥 + 建任务 + detached goroutine）与执行编排（恢复前自动备份→失败即中止→读内容→RestoreConfig→回读 hash 警告不失败→版本链恢复记录→任务 success/failed）；handler 改异步语义（返回 taskId + operlog 发起时记录）+ 任务查询双端点挂 /backups 组；前端最小改动（恢复按钮异步交互 + useRestoreTask 轮询 hook + Modal 进度/结果）。
</objective>

<decisions_covered>
D-01（恢复=设备下发，语义落地）· D-04（同设备校验）· D-07（handler 响应扩展 taskId）· D-08（同设备互斥，Enqueue 去重模式）· D-09（版本链恢复记录，复用 BackupTypeManual + ChangeReason="恢复自版本 N"）· D-10/D-14（恢复前自动备份，失败即中止）· D-11（hash 警告不失败）· D-13（复用 OperTypeUpdate）· D-15（任务表，Migrate211）· D-16（前端轮询）· D-17（返回 taskId + 列表/详情双端点）· D-18（handler 发起时 operlog）· D-19（后端+最小前端）· D-20（不做清理）· D-21（不设全局上限）· D-22（不设重试）· D-31（组权限复用）· D-34（四态状态机无取消）· [informational] A5/P2 discretion：启动时 running→failed 一次性收敛（状态机自洽，非清理 cron）
</decisions_covered>

<tasks>

<task id="93-03-01" type="execute">
<files>internal/models/config_restore_task.go, internal/core/db/database.go, internal/core/db/migrations/migration_211_create_config_restore_task.go</files>
<read_first>
- internal/models/config_backup.go（三段式模板：struct + BeforeCreate UUID + TableName）
- internal/core/db/database.go（:511 附近 MigrateModelList 中 &models.ConfigBackup{} 行；:808-890 迁移注册双方言分支布局）
- internal/core/db/migrations/migration_210_normalize_menu_parent_id.go（MigrateNNN 骨架与注册方式）
- .planning/workstreams/milestone/phases/93-config-backup-todo-p2/93-RESEARCH.md（Pitfall 9 三件套 + §DeviceInfoCollectionService 状态字段集）
</read_first>
<action>
1. 新建 internal/models/config_restore_task.go：`ConfigRestoreTask` struct——ID(uuid pk)/DeviceID(uuid not null, index)/BackupID(uuid not null)/Status(string size:20 not null, index)/TotalLines(int)/SentLines(int)/FailedLine(text)/ResultJSON(text, 存 RestoreResult 序列化)/ErrorMessage(text)/StartedAt(*time.Time)/CompletedAt(*time.Time)/CreatedAt/UpdatedAt/CreatedBy(string size:64)；string 状态常量 `RestoreTaskStatusPending/Running/Success/Failed = "pending"/"running"/"success"/"failed"`；BeforeCreate UUID+CreatedAt hook（config_backup.go :51-64 模板）；`TableName() = "sys_config_restore_task"`。
2. database.go：MigrateModelList 中 `&models.ConfigBackup{}` 行后追加 `&models.ConfigRestoreTask{},`（AutoMigrate 线）。
3. 新建 migration_211_create_config_restore_task.go：按 migration_210 骨架——`func Migrate211(db *gorm.DB) error`，PG 分支 `CREATE TABLE IF NOT EXISTS sys_config_restore_task (...)`（uuid 主键 + 各列 + device_id/status 索引），sqlite 分支空操作（AutoMigrate 已建）；在 database.go 迁移注册序列追加 Migrate211（紧跟 Migrate210 之后）。
</action>
<acceptance_criteria>
- internal/models/config_restore_task.go 存在，含 4 个 RestoreTaskStatus* 常量（值为 "pending"/"running"/"success"/"failed"）、BeforeCreate hook、TableName 返回 "sys_config_restore_task"
- database.go 同时包含 `&models.ConfigRestoreTask{}` 与 Migrate211 注册（grep 双命中）
- migration_211 文件含 PG `CREATE TABLE IF NOT EXISTS sys_config_restore_task` 分支
- `go build ./...` exit 0
- `go test ./internal/core/db/ -count=1` exit 0（database_test.go:142 全量模型 AutoMigrate 测试捕获漏注册——必须含新 model 后仍绿）
</acceptance_criteria>
<verify>go build ./... && go test ./internal/core/db/ -count=1</verify>
</task>

<task id="93-03-02" type="tdd">
<files>internal/services/config_restore_task_service.go, internal/services/config_backup_service.go</files>
<read_first>
- internal/services/device_info_collection_service.go（:133-163 Enqueue 同设备 pending/running IN 查询去重 = D-08 模板；:217 recoverPendingTasks 启动收敛参照；worker goroutine 模式）
- internal/services/config_backup_service.go（RestoreBackup :536-546 现状；CreateBackup/CreateBackup 供恢复前备份复用；GetBackupContent/GetBackupByID）
- internal/device/restore_config.go（93-02 产物 RestoreConfig/RestoreResult）
- .planning/workstreams/milestone/phases/93-config-backup-todo-p2/93-RESEARCH.md（Pattern 3 异步任务 + Architecture Diagram 编排序列 + P1 detached context + P2 启动收敛）
</read_first>
<action>
1. 新建 internal/services/config_restore_task_service.go：
   - `ConfigRestoreTaskService` struct{db *gorm.DB, backupSvc *ConfigBackupService, executor *device.DeviceExecutor} + `NewConfigRestoreTaskService(...)` 构造器（Handler-Service 模式）。
   - `StartRestore(ctx, backupID, deviceID, createdBy string) (*models.ConfigRestoreTask, error)`：①查 backup（GetBackupByID），校验 `backup.DeviceID == deviceID` 不一致返回 error "备份不属于目标设备，仅限恢复到备份源设备"（D-04）；②互斥查询 `device_id = ? AND status IN (pending, running)`（D-08，Enqueue:133 模板）命中返回 error "该设备存在进行中的恢复任务"；③创建任务 Status=pending/CreatedBy → 返回；④`go s.runRestore(context.WithTimeout(context.Background(), constants.RestoreConfigTimeout), task.ID)`（P1 detached context——严禁 ctx 透传 HTTP ctx）。
   - 私有 `runRestore(ctx, taskID)`：状态→running（记 StartedAt）；⑤恢复前备份：`backupSvc.CreateBackup(ctx, &BackupRequest{DeviceID, DeviceName, BackupType: models.BackupTypeAuto, ChangeReason: "恢复前自动备份", CreatedBy: "restore"})` 失败→任务 failed（ErrorMessage）并 return（D-10/D-14）；⑥`backupSvc.GetBackupContent(ctx, backupID)` 取内容（走 93-01 解压路径）；⑦`executor.RestoreConfig(ctx, deviceID, config)` 失败→任务 failed + ResultJSON 存部分 RestoreResult（D-12 留痕）；⑧回读 `executor.GetConfig(ctx, deviceID)` → calculateHash 与备份 ConfigHash 比对，不一致仅 applogger.Warnf + hashMatched=false 不算失败（D-11）；⑨`backupSvc.CreateBackup`…不——版本链记录直接构造 `models.ConfigBackup{...BackupType: models.BackupTypeManual, ChangeReason: fmt.Sprintf("恢复自版本 %d", backup.Version), ConfigHash: 回读hash, CreatedBy: createdBy}` Create 插入（D-09，统计链路零改动——research Open Question 2 采纳）；⑩任务 success + ResultJSON（RestoreResult+hashMatched 序列化）+ CompletedAt。
   - `GetRestoreTask(ctx, id)` / `ListRestoreTasks(ctx, current, pageSize, deviceID, orderByColumn, isAsc)`（base.ApplySort + 白名单 map `restoreTaskAllowedSortFields`：deviceId/version 无——列有 deviceId/status/createdAt）。
   - `RecoverStaleRunningTasks(ctx)`：启动收敛（A5 discretion 方案①）——`UPDATE sys_config_restore_task SET status='failed', error_message='服务重启，任务中断' WHERE status='running'`，由 core 装配时调用一次。
2. config_backup_service.go：:536-546 `RestoreBackup` 删除 stub（返回 error 的旧实现整体移除，`// TODO: 实现配置恢复逻辑` 注释删除）——保留方法签名改为薄委托 `s.taskSvc.StartRestore`？——不：为避免循环依赖，直接**删除 RestoreBackup 方法**，调用方 handler 改调 taskSvc（93-03-03）；BackupResult 等其余不动。
3. core 接线在 93-03-03 handler 任务内一并做（构造顺序：executor → backupSvc → taskSvc）。
</action>
<acceptance_criteria>
- config_restore_task_service.go 包含 `func (s *ConfigRestoreTaskService) StartRestore(`、`runRestore(`、`RecoverStaleRunningTasks(`
- StartRestore 含 "备份不属于目标设备" 与 "进行中的恢复任务" 两个错误分支（D-04/D-08）
- runRestore 首行为 detached context（`context.Background()`）且含 "恢复前自动备份" ChangeReason 分支（D-10）
- 失败即中止逻辑存在：CreateBackup 错误 → 任务 failed → return（D-14）
- hash 不一致分支调用 applogger.Warnf 且不返回 error（D-11）
- 含 ChangeReason "恢复自版本" 的版本链插入（D-09，BackupTypeManual）
- config_backup_service.go 不再包含 "配置恢复功能待实现"（grep 0 命中）
- `go build ./...` exit 0
</acceptance_criteria>
<verify>go build ./... && go vet ./internal/services/</verify>
</task>

<task id="93-03-03" type="execute">
<files>internal/api/v1/network/backup_handler.go, internal/api/v1/network/network_router.go, internal/core/core.go</files>
<read_first>
- internal/api/v1/network/backup_handler.go（Restore :257-288 现状 + BackupHandler struct 依赖注入模式）
- internal/api/v1/network/network_router.go（:150-170 backups 组；handler 构造装配段）
- internal/core/core.go（ConfigBackupService 装配点 grep "NewConfigBackupService"）
- CLAUDE.md § operlog convention（D-18 记录位置）
</read_first>
<action>
1. BackupHandler struct 增加 `restoreTaskSvc *services.ConfigRestoreTaskService` 字段；NewBackupHandler 签名追加该参数（调用点 network_router.go 同步）。
2. Restore handler 改造：`task, err := h.restoreTaskSvc.StartRestore(c.Request.Context(), id, req.DeviceID, 当前用户)`；错误仍走 HandleServiceError；成功路径 operlog.Record 保持在 response.Success 之前（D-13 OperTypeUpdate / D-18 发起时记录）；响应改为 `response.Success(c, gin.H{"taskId": task.ID, "status": task.Status, "message": "恢复任务已创建"})`（D-07/D-17）。
3. 新增任务查询双端点（同文件或新 restore_task_handler 方法）：`GetRestoreTask`（POST /restore-tasks/:id 语义，项目 POST 风格）与 `ListRestoreTasks`（POST /restore-tasks/list，分页参数走既有 rawReq 模式 + base.ApplySort）；查询端点不记 operlog。
4. network_router.go：backups 组内追加 `backups.POST("/restore-tasks/list", ...)` 与 `backups.POST("/restore-tasks/:id", ...)`（D-31 继承组权限，零新权限点）；NewBackupHandler 调用点补 taskSvc 参数。
5. core.go：ConfigBackupService 构造之后追加 `NewConfigRestoreTaskService(core.GetDB(), backupSvc, core.DeviceExecutor)` 装配（实际字段名以 core 现状 grep 为准）+ 启动时调用 `taskSvc.RecoverStaleRunningTasks(context.Background())`（A5 discretion 方案①——状态机自洽收敛，非清理 cron，不违 D-20）。
</action>
<acceptance_criteria>
- backup_handler.go Restore 成功响应包含 `"taskId"`（grep 命中）且 operlog.Record 调用仍在（grep 命中）
- network_router.go 含 `/restore-tasks/list` 与 `/restore-tasks/:id` 两条路由且位于 backups 组 Use(RequirePermissions) 之后
- core.go 含 NewConfigRestoreTaskService 装配与 RecoverStaleRunningTasks 启动调用
- `go build ./...` exit 0
- `go test ./internal/api/v1/network/ -run TestBackupHandler -count=1` 结果记录（93-03-04 将重写红掉的用例）
</acceptance_criteria>
<verify>go build ./...</verify>
</task>

<task id="93-03-04" type="execute">
<files>internal/api/v1/network/backup_handler_test.go</files>
<read_first>
- internal/api/v1/network/backup_handler_test.go（:220-246 TestBackupHandler_Restore 及 :233 restore_is_a_documented_stub 子测试）
- .planning/workstreams/milestone/phases/93-config-backup-todo-p2/93-RESEARCH.md（Pitfall 10 stub 锁测试必红）
</read_first>
<action>
重写 TestBackupHandler_Restore：删除 `restore_is_a_documented_stub` 子测试（断言"配置恢复功能待实现"已失真）；新语义用例——mock/装配 restoreTaskSvc 后断言：①成功返回含 taskId 字段且 HTTP 200；②backupID 与 deviceID 不匹配时返回错误（D-04 透传）；③进行中任务时返回错误（D-08 透传）。若 handler 测试基建无 taskSvc mock 先例，用 sqlite 内存 DB + 真实 ConfigRestoreTaskService（无 executor 时 StartRestore 在互斥/校验层即返回，不触达下发——错误路径可测，成功路径留给 93-04 e2e）。
</action>
<acceptance_criteria>
- backup_handler_test.go 无 "restore_is_a_documented_stub"（grep 0 命中）
- `go test ./internal/api/v1/network/ -count=1` exit 0
</acceptance_criteria>
<verify>go test ./internal/api/v1/network/ -count=1</verify>
</task>

<task id="93-03-05" type="execute">
<files>xingran-react-frontend/src/lib/networkApi.ts, xingran-react-frontend/src/pages/network/backups/hooks/useRestoreTask.ts, xingran-react-frontend/src/pages/network/backups/hooks/useRestoreTask.test.ts, xingran-react-frontend/src/pages/network/backups/index.tsx, xingran-react-frontend/src/pages/network/backups/hooks/useBackupModals.ts</files>
<read_first>
- xingran-react-frontend/src/pages/network/backups/index.tsx（:224 恢复调用点现状）
- xingran-react-frontend/src/pages/network/backups/hooks/useBackupModals.ts（:133 restore 直接 post 调用点）
- xingran-react-frontend/src/pages/network/discoveries/hooks/useDiscoveryPolling.ts（D-16 轮询模板：3s interval + running 态启停 + unmount 清理 + useCallback 依赖纪律）
- CLAUDE.md § Frontend/React Best Practices（useEffect 依赖 CRITICAL 条款）
</read_first>
<action>
1. networkApi.ts：新增 `restoreTaskApi = { get: (id) => post(...), list: (params) => post(...) }`（或按文件既有导出风格）；恢复调用响应类型补 `{ taskId: string }`。
2. 新建 useRestoreTask.ts：照抄 useDiscoveryPolling 结构——输入 taskId，running 态时 3s 轮询 restoreTaskApi.get，success/failed 终态停止；返回 {task, polling}；依赖数组遵守 memoize 纪律（P11）。
3. useBackupModals.ts：恢复确认后改存 returned taskId 到 state（替换原同步 success message 逻辑）。
4. index.tsx：恢复 Modal 内嵌进度/结果展示（task.Status 文案映射 pending 执行中排队/running 恢复下发中/success 恢复成功+SentLines/failed 恢复失败+ErrorMessage）——D-19 最小改动，不建独立页面。
5. useRestoreTask.test.ts：vitest 用例覆盖 running→轮询→success 终态停止 + failed 终态展示（参照 useDiscoveryPolling.test 或 useBackupData.test 模式）。
</action>
<acceptance_criteria>
- useRestoreTask.ts 存在且包含 3s 轮询与终态停止逻辑（grep interval/3000 或等价）
- index.tsx 恢复 Modal 展示 task 状态（grep "taskId" 或 RestoreTask 命中）
- `npm run type-check` exit 0
- `npm run test -- --run src/pages/network/backups` exit 0
- `npm run lint` exit 0
</acceptance_criteria>
<verify>npm run type-check && npm run test -- --run src/pages/network/backups</verify>
</task>

</tasks>

<threat_model>
| 威胁 | STRIDE | 严重度 | Mitigation | 落点 |
|------|--------|--------|-----------|------|
| 未授权触发恢复（横向越权调 restore 端点） | Elevation | high | /backups 组级 RequirePermissions（network:backup:restore 在 4 权限点集内）+ operlog 审计留痕（D-13/D-18） | 93-03-03 路由继承 |
| 跨设备误推配置（backup 属 A 推给 B） | Tampering | high | D-04 service 层硬校验 backup.DeviceID == deviceID，不一致拒绝 | 93-03-02 StartRestore |
| 并发恢复交叉下发（两用户同时恢复同一设备） | Tampering | high | D-08 任务表 pending/running IN 查询互斥（Enqueue 先例）+ P2 启动收敛防崩溃残留锁死 | 93-03-02 |
| 崩溃残留 running 永久锁死设备恢复入口 | DoS | medium | RecoverStaleRunningTasks 启动一次性收敛 running→failed（A5 discretion 方案①） | 93-03-02/03 |
| taskId 枚举探测他人任务 | Information Disclosure | low | 任务查询端点同处 /backups 组权限保护；任务 ID 为 uuid 不可枚举 | 93-03-03 |
</threat_model>

<verification>
1. `go build ./...` + `go test ./internal/services/ ./internal/api/v1/network/ ./internal/core/... -count=1` 全绿
2. `go test ./internal/utils/operlog/ ./internal/models/ -count=1` 全绿（锁值防线零改动承诺 D-13）
3. `npm run type-check && npm run lint && npm run test -- --run src/pages/network/backups` 全绿
4. sqlite 分支验证：`go test ./internal/core/db/ -count=1`（AutoMigrate 端到端含新表）
5. grep 验证：无 "配置恢复功能待实现" 残留；RestoreBackup 旧方法已移除
</verification>

<must_haves>
- BACKUP-CLOSED-03: 恢复全链路可用——发起（校验/互斥）→ 异步执行（备份→下发→回读→记录）→ 任务终态可查询
- BACKUP-CLOSED-05（链路部分）: 端到端流程代码闭环（自动化断言在 93-04 落地）
- D-01: 恢复 = 设备配置下发（RestoreConfig 被任务编排调用）
- D-04: 同设备校验错误分支存在
- D-07/D-17: handler 返回 taskId；任务列表+详情双端点在 /restore-tasks/*
- D-08: 同设备互斥（IN pending,running 查询）
- D-09: 版本链恢复记录（BackupTypeManual + ChangeReason "恢复自版本 N"）
- D-10/D-14: 恢复前自动备份，失败即中止不下发
- D-11: hash 不一致 Warnf 不失败
- D-13/D-18: operlog 在 handler 发起时以 OperTypeUpdate 记录，25 常量防线零改动
- D-15/D-34: sys_config_restore_task 表（Migrate211 + AutoMigrate 双注册）+ string 四态状态机
- D-16/D-19: useRestoreTask 轮询 + Modal 进度/结果，无独立任务页
- D-20/D-21/D-22: 无清理 cron、无全局上限、无任务级重试
- D-31: 任务端点挂 /backups 组零新权限点
- A5 discretion: RecoverStaleRunningTasks 启动收敛（P2 防锁死，已注明状态机自洽定性）
</must_haves>

<success_criteria>
- [ ] 后端测试全绿（services/api/core 三域）
- [ ] 前端 type-check + lint + backups 测试全绿
- [ ] operlog/status 锁值防线不回归
- [ ] 5 威胁 mitigations 全部落地并有对应代码位置
</success_criteria>

<commit_plan>
1. feat(93): add ConfigRestoreTask model with Migrate211 dual registration — model + database.go + migration_211
2. feat(93): implement ConfigRestoreTaskService async restore orchestration (D-04/08..11/14/15) — service + RestoreBackup stub removal
3. feat(93): wire restore task handler and endpoints with taskId semantics (D-07/17/18/31) — handler + router + core + handler_test 重写
4. feat(93): frontend async restore interaction with polling hook (D-16/19) — networkApi + useRestoreTask + index/useBackupModals
</commit_plan>
