---
phase: 93
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - pkg/constants/timeouts.go
  - internal/device/executor.go
  - internal/device/restore_config.go
  - internal/device/restore_config_test.go
autonomous: true
requirements: [BACKUP-CLOSED-03]
---

# Plan 93-02: DeviceExecutor.RestoreConfig 下发内核

<objective>
device 包新增厂商感知的 `RestoreConfig` 方法（与 GetConfig 对称，D-02 唯一下发入口）：内部用 `ExecuteCustom` 骨架 + `wrapper.SendConfigs` 配置模式批量下发（scrapligo AcquirePriv 自动处理进入配置模式）；下发前基础清洗（D-03：TrimSpace 后空行/`#`/`!` 前缀行过滤）；末尾按 vendor map（D-06）追加"退一级"命令（华为/H3C `quit`、锐捷/迈普/默认 `exit`——portwrite V7 教训禁用 `end`）；逐行 fail-fast（D-12）并以 `RestoreResult` 结构化返回（D-07 字段：TotalLines/SentLines/FailedLine/Duration）。`pkg/constants/timeouts.go` 新增 `RestoreConfigTimeout`（D-05，值定 10 分钟）。
</objective>

<decisions_covered>
D-02（RestoreConfig 唯一入口，ExecuteCustom 注入）· D-03（基础清洗在 device 包内）· D-05（独立超时常量）· D-06（硬编码 vendor map——退出命令 + platform 名）· D-07（RestoreResult 结构化——本 plan 定义 device 层字段）· D-12（fail-fast 即停 + 进度计数）
</decisions_covered>

<tasks>

<task id="93-02-01" type="execute">
<files>pkg/constants/timeouts.go</files>
<read_first>
- pkg/constants/timeouts.go（既有常量段与注释风格）
- CLAUDE.md § Timeout/Port/Protocol Constants Convention
</read_first>
<action>
在 timeouts.go 新增常量：`RestoreConfigTimeout = 10 * time.Minute`，注释注明用途（"全量 running-config 下发超时（配置备份恢复，93）；千行级配置远超单命令 CommandExecTimeout"）。放在既有常量列表末尾、保持组注释风格。
</action>
<acceptance_criteria>
- pkg/constants/timeouts.go 包含 `RestoreConfigTimeout = 10 * time.Minute`
- `go build ./...` exit 0
- `go test ./pkg/constants/ -count=1` exit 0
</acceptance_criteria>
<verify>go build ./... && go test ./pkg/constants/ -count=1</verify>
</task>

<task id="93-02-02" type="tdd">
<files>internal/device/restore_config.go, internal/device/executor.go</files>
<read_first>
- internal/device/executor.go（ExecuteCustom :161-193 done-channel 骨架；GetConfig :267-300 的 pool.GetDevice + vendor switch 模式）
- internal/device/scrapli_wrapper.go（SendConfigs :614-637 的 per-line SendConfig 循环与部分返回语义；Response.Failed 字段）
- .planning/workstreams/milestone/phases/93-config-backup-todo-p2/93-RESEARCH.md（Pattern 1 scrapligo SendConfigs 源码验证 / Pattern 2 RestoreConfig 集成形态 / Pitfall 6 退出命令 V7 教训 / Pitfall 7 精简失败检测）
</read_first>
<action>
1. 新建 internal/device/restore_config.go：
   - `RestoreResult` struct：`TotalLines int`、`SentLines int`、`FailedLine string`、`Duration time.Duration`（D-07 device 层核心字段；hash 字段由 service 层补，不在 device 层）。
   - `cleanConfigLines(config string) []string`：按行 split，TrimSpace 后过滤空行、前缀 `#`、前缀 `!` 的行（D-03 最小集，不过滤中段含 #/! 的行）。
   - `exitConfigCommand(vendor string) string`：`huawei`/`h3c` → "quit"；`ruijie`/`maipu` 及 default → "exit"（D-06 vendor map 主体，V7 只退一级教训）。
   - `func (e *DeviceExecutor) RestoreConfig(ctx context.Context, deviceID, config string) (*RestoreResult, error)`：`pool.GetDevice(deviceID)` 取 vendor（GetConfig :278 同款）→ cleanConfigLines 清洗（空结果返回 error "备份内容无有效配置行"）→ fullCmds = 清洗行 + exitConfigCommand → 记 startTime → `e.ExecuteCustom(ctx, deviceID, closure, constants.RestoreConfigTimeout)`；closure 内 `pc.GetWrapper().SendConfigs(fullCmds)`：sendErr 非空时 SentLines=len(responses)、FailedLine=fullCmds[len(responses)]（若未发完）、返回 sendErr；逐条检查 `responses[i].Failed`（精简失败检测，Pitfall 7——device 包不 import portwrite），命中即 SentLines=i、FailedLine=fullCmds[i]、返回 error。成功后组装 RestoreResult{TotalLines: len(fullCmds), SentLines: len(fullCmds), Duration: time.Since(startTime)} 返回。
2. executor.go 不改动（RestoreConfig 落独立文件，ExecuteCustom 已是导出方法）。
3. 注意 import：device 包只引 constants/stdlib/scrapligo 既有依赖，禁止 import services/portwrite（Pitfall 7 import cycle）。
</action>
<acceptance_criteria>
- internal/device/restore_config.go 存在且包含 `func (e *DeviceExecutor) RestoreConfig(`、`func cleanConfigLines(`、`func exitConfigCommand(`
- exitConfigCommand 对 "huawei" 返回 "quit"、对 "ruijie" 返回 "exit"、对 "" 返回 "exit"
- cleanConfigLines 过滤空行/`#` 前缀/`!` 前缀，保留中段含 # 的行
- restore_config.go 无 `portwrite` import（grep 0 命中）
- `go build ./...` exit 0
</acceptance_criteria>
<verify>go build ./... && go vet ./internal/device/</verify>
</task>

<task id="93-02-03" type="tdd">
<files>internal/device/restore_config_test.go</files>
<read_first>
- internal/device/restore_config.go（93-02-02 产物）
- internal/services/portwrite/port_write_e2e_test.go（FileTransport fixture 回放 SendConfigs 的 harness 模式，只读参照）
- internal/services/config_backup_service_79_06_test.go（newExecutor7906 :142 完整 executor 装配——跨包不可用，本包测试自建同款装配）
- .planning/workstreams/milestone/phases/93-config-backup-todo-p2/93-RESEARCH.md（§SendConfigs e2e fixture 字节格式——banner/prompt/回显逐字节规则；huawei_shutdown_success.fixture 模板）
</read_first>
<action>
新建 restore_config_test.go（package device）：
1. 纯函数用例：TestCleanConfigLines（空行/前后空白/#/! 前缀过滤 + 中段 # 保留 + 顺序不变）、TestExitConfigCommand（vendor 四分支 + default）。
2. e2e 用例 TestRestoreConfig_E2E_Success：参照 newExecutor7906 模式在 device 包内装配 pool→scheduler→executor（用 SeedConnectionForTesting/NewPooledConnectionForTesting 注入 FileTransport 连接），fixture 按 RESEARCH 字节格式构造——含 `sysname Restored-Switch` 行（P12 prompt 漂移韧性验证）+ 多条配置行 + `quit` 回显 + spare prompts；调用 RestoreConfig 断言 RestoreResult.SentLines==TotalLines、err==nil、Duration>0。
3. e2e 用例 TestRestoreConfig_E2E_FailFast：fixture 在第 N 条配置行回显错误 marker（参照 portwrite huawei_device_rejected.fixture 的错误行格式）或提前 EOF；断言 err!=nil、SentLines==N（部分计数留痕）、FailedLine 非空（D-12）。
4. fixture 文件放 internal/device/testdata/（若 portwrite harness 的装配函数跨包不可复用，按 79_06:142-162 newExecutor7906 逐段复制装配代码并注明来源）；driver 不调用 Close（78-03 S-2 教训，fixture 耗尽后 close 阻塞）。
</action>
<acceptance_criteria>
- internal/device/restore_config_test.go 含 ≥4 个 Test 函数（2 纯函数 + 2 e2e）
- e2e fixture 含 `sysname` 行（P12 验证点）
- `go test ./internal/device/ -run "TestCleanConfigLines|TestExitConfigCommand|TestRestoreConfig" -count=1` exit 0
- `go test ./internal/device/ -count=1` exit 0（device 包整体不回归）
</acceptance_criteria>
<verify>go test ./internal/device/ -run "TestRestoreConfig|TestCleanConfig|TestExitConfig" -v -count=1</verify>
</task>

</tasks>

<threat_model>
| 威胁 | STRIDE | 严重度 | Mitigation | 落点 |
|------|--------|--------|-----------|------|
| 配置内容注入非命令文本（备份文件被篡改后夹带任意命令） | Tampering | high→medium | 清洗过滤非命令行（D-03）+ 备份文件来源受控（系统自身落盘）+ 下发属 auth 后高危操作且 operlog 审计（93-03 落地）；下发目标设备已通过 RBAC network:backup:restore 权限约束 | 93-02-02 cleanConfigLines |
| 超时缺失导致连接/任务悬挂 | DoS | medium | ExecuteCustom 强制 timeout 参数（RestoreConfigTimeout 10min）+ scheduler 任务超时兜底 | 93-02-01 常量 |
</threat_model>

<verification>
1. `go build ./...` + `go vet ./internal/device/` exit 0
2. `go test ./internal/device/ -count=1` 全绿（新用例 + 存量不回归）
3. `go test ./pkg/constants/ -count=1` exit 0
4. RestoreConfig 是 device 包唯一新导出的下发方法（grep SendConfigs 调用点确认未绕过 wrapper）
</verification>

<must_haves>
- BACKUP-CLOSED-03（下发内核）: RestoreConfig 经 scrapligo SendConfigs 真实下发配置行，e2e 全链路（含成功与 fail-fast）可自动化验证
- D-02: RestoreConfig 为唯一下发入口（与 GetConfig 对称、ExecuteCustom 骨架、无 portwrite import）
- D-03: 清洗规则（空行/#/! 前缀）在 device 包 restore_config.go 内实现
- D-05: `pkg/constants/timeouts.go` 含 `RestoreConfigTimeout = 10 * time.Minute`
- D-06: exitConfigCommand vendor map（quit/exit 只退一级，无 `end`）
- D-07: RestoreResult 结构化（TotalLines/SentLines/FailedLine/Duration）
- D-12: fail-fast——中途失败即停、SentLines 部分计数、FailedLine 定位
</must_haves>

<success_criteria>
- [ ] device 包测试全绿（含 2 条 FileTransport e2e）
- [ ] build + vet 零错误
- [ ] vendor map 无 "end" 退出命令
</success_criteria>

<commit_plan>
1. feat(93): add RestoreConfigTimeout constant (D-05) — timeouts.go
2. feat(93): implement DeviceExecutor.RestoreConfig with vendor map and fail-fast (D-02/03/06/07/12) — restore_config.go + executor.go（如动）+ restore_config_test.go
</commit_plan>
