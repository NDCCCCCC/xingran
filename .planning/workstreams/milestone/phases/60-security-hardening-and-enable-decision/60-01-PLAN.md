---
phase: 60-security-hardening-and-enable-decision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/router.go
  - internal/middleware/apikey.go
  - internal/middleware/apikey_test.go
  - internal/middleware/apikey_integration_test.go
  - .planning/notes/260813-auth03-enable-decision.md
autonomous: true
requirements:
  - AUTH-03
  - QUAL-01

must_haves:
  truths:
    - "POST /system/apikeys/* 请求在携带 X-API-Key 头时被 MultiAuth 中间件成功认证,缺失 X-API-Key 时由 JWT 中间件链接管(均不 panic)"
    - "携带不在白名单中的客户端 IP 的 X-API-Key 请求被拒,响应码 403"
    - "携带有效 X-API-Key 的请求被限流后,响应头 X-RateLimit-Limit / X-RateLimit-Remaining 的值是数字字面量字符串且可被 strconv.Atoi 反解析为正整数(D-11 修复 string(rune(int)) → strconv.Itoa,D-12 单测+集成测试覆盖,D-13 严格不修 getScopeFromContext 多 scope 选择逻辑留 Phase 61)"
    - ".planning/notes/260813-auth03-enable-decision.md 文档存在并含「挂载范围 / 认证优先级 / IP 白名单 / JWT 回退 / 作用域继承 (InheritPerms) 行为」5 维度段落,每段引用 D-01..D-04 决策 ID + 最后一段显式说明 Phase 60 InheritPerms scope-boundary"
  artifacts:
    - path: "internal/api/router.go"
      provides: "apikeys 路由组中间件链装配(RequirePermissions → MultiAuth → RateLimitByScope)"
      contains: "internalmw.MultiAuth"
    - path: "internal/middleware/apikey.go"
      provides: "RateLimitByScope 限流响应头用 strconv.Itoa 序列化"
      contains: "strconv.Itoa(result.Limit)"
    - path: "internal/middleware/apikey_test.go"
      provides: "TestRateLimitHeaderEncoding 单元测试(strconv.Itoa 数字字符串化断言)"
      contains: "TestRateLimitHeaderEncoding"
    - path: "internal/middleware/apikey_integration_test.go"
      provides: "TestRateLimitHeadersInResponse 集成测试(跨 gin.Engine + 中间件链路断言响应头可解析)"
      contains: "TestRateLimitHeadersInResponse"
    - path: ".planning/notes/260813-auth03-enable-decision.md"
      provides: "AUTH-03 决策记录(5 维度:D-01 启用决策 / D-02 挂载范围 / D-03 认证优先级 / D-04 IP 白名单 / InheritPerms scope-boundary)"
      contains: "## 挂载范围"
  key_links:
    - from: "internal/api/router.go:apikeys group"
      to: "internal/middleware/apikey.go:MultiAuth"
      via: "internalmw.MultiAuth(apiKeyService, usageLogger) 注册调用"
      pattern: "internalmw\\.MultiAuth"
    - from: "internal/api/router.go:apikeys group"
      to: "internal/middleware/apikey.go:RateLimitByScope"
      via: "internalmw.RateLimitByScope(rateLimiter) 注册调用"
      pattern: "internalmw\\.RateLimitByScope"
    - from: "internal/middleware/apikey.go:RateLimitByScope function"
      to: "internal/middleware/apikey_test.go:TestRateLimitHeaderEncoding"
      via: "strconv.Itoa 替换 string(rune(int)) 后,测试断言 header 可被 strconv.Atoi 反解析"
      pattern: "strconv\\.Itoa\\(result\\.Limit\\)"
---

<objective>
将 Phase 57 已就绪但未挂载的 MultiAuth + RateLimitByScope 中间件真实挂载到 /system/apikeys/* 管理面路由组,并修复 RateLimitByScope 限流响应头的编码错误(P2-a 缺陷),使 API Key 认证链在生产路径具备可观测、可限流的启用条件,产出 AUTH-03 决策记录文档。

Purpose: AUTH-03 是 Phase 60 启用的根本决策;D-01 启用决策=启用时直接触发 Phase 61 执行(不再 conditional)。QUAL-01 修复 string(rune(int)) 让限流响应头变成 RFC 6585 合规数字字符串,前端 / 第三方工具可被标准解析。两条决策点同时落地避免 Phase 61 启用时遇到限流头损坏的次生缺陷。

Output: 1 个 router.go 中间件链装配 + 1 个 apikey.go strconv.Itoa 修复 + 2 个测试新增(单元+集成) + 1 个 AUTH-03 决策记录 notes。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-security-hardening-and-enable-decision/60-CONTEXT.md
@.planning/phases/60-security-hardening-and-enable-decision/60-PATTERNS.md
@.planning/phases/60-security-hardening-and-enable-decision/60-VALIDATION.md
@.planning/phases/60-security-hardening-and-enable-decision/60-DISCUSSION-LOG.md
@.planning/notes/260616-rpa-heartbeat-log-flooding.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (AUTH-03): 挂载 MultiAuth + RateLimitByScope 到 /system/apikeys/* 路由组 + 5 维度决策记录 notes(含 InheritPerms scope-boundary)</name>
  <files>internal/api/router.go, .planning/notes/260813-auth03-enable-decision.md</files>
  <read_first>
- internal/api/router.go(全文件;重点 line 1-27 import 块、line 21 systemServices 别名、line 25 middleware 别名 → pkg/middleware、line 155-164 depts 路由组的 RequirePermissions+DataScopePermission 串联范式、line 238-248 apikeys 路由组当前结构)
- internal/api/v1/system/apikey_router.go(全文件;SetupAPIKeyRouter 注册的 8 路由不被本 task 改动,但需确认 DI 装配兼容性)
- internal/middleware/apikey.go(line 23-87 MultiAuth 函数签名;line 49-56 isIPAllowed 行为;line 247-281 RateLimitByScope 函数签名)
- internal/services/usage_logger.go(line 1-30 用法: NewUsageLogger 构造函数 + services.UsageLogger 接口)
- internal/services/rate_limiter.go(line 1-50 用法: NewRateLimiter 零参构造函数)
- .planning/phases/60-security-hardening-and-enable-decision/60-CONTEXT.md(读 decisions 段 D-01/D-02/D-03/D-04 锁定决策)
- .planning/phases/57-auth-chain-core-fix-regression-test/57-CONTEXT.md(读 D-04 7 个 context 键约束)
- .planning/phases/59-observability-usage-log-fix/59-CONTEXT.md(读 D-02 detached context 约束,影响 usageLogger 注入位置)
  </read_first>
  <action>
按 D-01/D-02/D-03/D-04 锁定决策将 MultiAuth + RateLimitByScope 中间件挂载到 /system/apikeys/* 路由组。

**router.go 改动(per D-01,顺序严格)**:
在现有 import 块(line 3-27)新增一条 import,**别名为 internalmw**(因为 line 25 已用别名 `middleware` 指向 `pkg/middleware`,`MultiAuth`/`RateLimitByScope` 在 `internal/middleware` 包,同名 package 不可重复 import):
```go
internalmw "github.com/xingran-next/xingran-go-backend/internal/middleware"
```
**顺序约束**: 必须新增 `internal/services` import 别名 `services`(PATTERNS.md:65-67 沿用,用于 `services.NewUsageLogger` + `services.NewRateLimiter`)。现有 line 19-22 有 systemServices / asset / operations 但没有最顶层 `services` 包,需新增:
```go
"github.com/xingran-next/xingran-go-backend/internal/services"
```

定位 line 238-248 apikeys 路由组(注释 `// API密钥管理`),在 `apikeys.Use(middleware.RequirePermissions(...))` 之后、`systemV1.SetupAPIKeyRouter(apikeys, core)` 之前,**新增**两个 `apikeys.Use(...)` 调用,顺序严格:
```go
apikeys.Use(internalmw.MultiAuth(
    systemServices.NewAPIKeyService(core.GetDB()),
    services.NewUsageLogger(core.GetDB()),
))
apikeys.Use(internalmw.RateLimitByScope(services.NewRateLimiter()))
```
- D-03 X-API-Key 优先 + JWT 回退 沿用 `apikey.go:23-87` MultiAuth 既有逻辑(line 27-31 无 X-API-Key 头 → c.Next() 跳过 → JWT 接管);**不**在 router 层加任何 fallback 分支
- D-04 IP 白名单严格拒绝 沿用 `apikey.go:49-56` 既有 `isIPAllowed`;**不**改动中间件代码
- IPWhitelist 空白的 API key 仍然放行所有 IP(D-04 配置默认)
- **不动** `apikeys.Use(middleware.RequirePermissions(...))`(既有 line 239-244),不动 `systemV1.SetupAPIKeyRouter(apikeys, core)`(既有 line 247)

**AUTH-03 决策记录 notes**:
新建 `.planning/notes/260813-auth03-enable-decision.md`,**5 维度段落** = D-01/D-02/D-03/D-04 一一对应 + InheritPerms scope-boundary(满足 ROADMAP SC#1 第①项「作用域继承行为」要求):
- `## 1. 挂载范围`(D-01/D-02):仅 `/system/apikeys/*` 管理面 8 路由(`Create/List/GetByID/Update/Delete/ToggleStatus/ListUsageLogs/GetUsageSummary`);不冲击其他 JWT 模块
- `## 2. 认证优先级`(D-03):X-API-Key 头存在 → MultiAuth 完成认证+写 context+记日志(`auth_type="api_key"`);缺失 → c.Next() 跳过 → JWT 中间件接管(`auth_type="jwt"`)。优先级表 + 与 apikey.go:27-31 既有逻辑的引用
- `## 3. IP 白名单`(D-04):严格拒绝 = `IPWhitelist` 非空 + 客户端 IP 不在白名单(含 CIDR)→ `403 "客户端IP不在白名单中"`;空白名单 = 放行所有 IP。引用 `isIPAllowed` (apikey.go:118-152) 实现位置
- `## 4. JWT 回退 + 安全评估`(D-01 衍生):MultiAuth 在 RequirePermissions 之后 → 权限校验先于认证 → 即使 JWT 路径通过 RequirePermissions,handler 内仍按 auth_type 区分上下文;Production 启用 = 立即生效,Phase 61 资源级权限矩阵 + 限流调优同步触发
- `## 5. 作用域继承 (InheritPerms) 行为 — Phase 60 scope-boundary`(满足 ROADMAP SC#1 第①项):**Phase 60 范围内不修改 InheritPerms 行为**——保持 `internal/models/api_key.go` `InheritPerms` 字段既有 GORM 持久化 + MultiAuth (apikey.go:23-87) 既有 context 写入(`inherit_perms`);InheritPerms → resource 参数的细粒度资源校验(AUTH-04 / Phase 61)的 `RequireAPIKeyResourcePermission(resource, action)` 真实生效**明确留 Phase 61**;Phase 60 启用 = context 键完整保留但 resource 维度不强制校验,与 Phase 57 D-04 7 键约束一致。

格式参考: `.planning/notes/260616-rpa-heartbeat-log-flooding.md`(段落 `## N. 标题` + 表格 + 代码引用),文件名前缀 `260813-` 沿用 `260615-` / `260616-` 既有 notes 命名。
  </action>
  <verify>
<automated>cd D:/code/ClaudeCode/guoguo && go build ./...</automated>
<automated>cd D:/code/ClaudeCode/guoguo && go vet ./internal/api/... ./internal/middleware/...</automated>
<automated>cd D:/code/ClaudeCode/guoguo && grep -n "internalmw.MultiAuth\|internalmw.RateLimitByScope" internal/api/router.go</automated>
</verify>
  <acceptance_criteria>
- `internal/api/router.go` 含 `internalmw "github.com/xingran-next/xingran-go-backend/internal/middleware"` import 行(精确别名 `internalmw`)
- `internal/api/router.go` 含 `"github.com/xingran-next/xingran-go-backend/internal/services"` import 行(无别名,用于 `services.NewUsageLogger` + `services.NewRateLimiter`)
- `internal/api/router.go` 第 238-248 行所在 apikeys 路由组按顺序包含 `middleware.RequirePermissions` → `internalmw.MultiAuth(...)` → `internalmw.RateLimitByScope(...)` 三个 `Use` 调用(MultiAuth 的两个参数是 `systemServices.NewAPIKeyService(core.GetDB())` 与 `services.NewUsageLogger(core.GetDB())`)
- `go build ./...` 退出码 0
- `.planning/notes/260813-auth03-enable-decision.md` 文件存在,含 **5 个 H2 段落标题**分别为 `挂载范围` / `认证优先级` / `IP 白名单` / `JWT 回退` / `作用域继承 (InheritPerms) 行为`(最后一段显式说明 Phase 60 不修改 InheritPerms + 引用 Phase 61/AUTH-04)
- notes 文档引用决策 ID(D-01 / D-02 / D-03 / D-04)与对应源码文件:行号
  </acceptance_criteria>
  <done>router.go 中间件链顺序为 RequirePermissions → MultiAuth → RateLimitByScope;NOTES 5 维度齐全(含 InheritPerms scope-boundary);go build 通过</done>
</task>

<task type="auto">
  <name>Task 2 (QUAL-01): 修复 RateLimitByScope 限流响应头 string(rune(int)) 编码错误 + 单测 + 集成测试</name>
  <files>internal/middleware/apikey.go, internal/middleware/apikey_test.go, internal/middleware/apikey_integration_test.go</files>
  <read_first>
- internal/middleware/apikey.go(全文件;重点 line 3-13 import 块、line 247-281 RateLimitByScope 函数,line 267-269 三条 c.Header 调用是 P2-a bug 修复点)
- internal/middleware/apikey_test.go(全文件;重点 line 1-7 import 块、line 10-40 TestIsValidKeyFormat t.Run 子测试模式作为新测试的 format analog)
- internal/middleware/apikey_integration_test.go(line 1-25 import 块、line 26-100 fakeAPIKeyService + fakeUsageLogger 复用、line 102-145 setupUsageLoggerTestDB + hex64 复用、line 151-233 TestMultiAuthIntegration gin.Engine 装配模式作为新集成测试 analog)
- .planning/phases/60-security-hardening-and-enable-decision/60-CONTEXT.md(D-11 修复方式 + D-12 测试覆盖 + D-13 范围限定 不修 getScopeFromContext)
- .planning/phases/60-security-hardening-and-enable-decision/60-PATTERNS.md(## strconv.Itoa 实施片段 line 84-110;## TestRateLimitHeaderEncoding 目标 line 354-373;## TestRateLimitHeadersInResponse 目标 line 414-431)
  </read_first>
  <action>
按 D-11 修复 P2-a `string(rune(int))` 编码错误,按 D-12 补充单元测试 + 集成测试,严格不修 D-13 划定的 getScopeFromContext 多 scope 选择逻辑。

**apikey.go 改动(per D-11)**:
1. import 块(line 3-13)在 stdlib 分组内**新增** `"strconv"`(per PATTERNS.md line 100-103 目标形态)
2. line 267-268 两条 `c.Header(...)` 调用:
   - `string(rune(result.Limit))` → `strconv.Itoa(result.Limit)`
   - `string(rune(result.Remaining))` → `strconv.Itoa(result.Remaining)`
3. line 269 `c.Header("X-RateLimit-Reset", result.ResetAt.Format(time.RFC3339))` **不动**(`time.RFC3339` 已经是字符串)
4. **不**修改 getScopeFromContext(line 283-304, QUAL-03 范畴严格 Phase 61)
5. **不**修改 line 244-281 中其他行(middleware 主体逻辑不变)

**apikey_test.go 扩展(单测 per D-12)**:
1. 文件顶 import 块(line 1-7)新增 `"strconv"`(stdlib)
2. **新增**函数 `TestRateLimitHeaderEncoding`(per PATTERNS.md line 354-373 目标形态):
   - `t.Run("strconv.Itoa 数字字符串化"...)` 子测试:断言 `strconv.Itoa(100) == "100"`、`strconv.Itoa(99) == "99"`、`strconv.Itoa(0) == "0"`、`strconv.Itoa(100) != "d"`(防御性断言,锁定 P2-a 永不复现)
   - `t.Run("header 可被 strconv.Atoi 反解析"...)` 子测试:`n, err := strconv.Atoi(strconv.Itoa(100))` → 断言 `err == nil` + `n == 100`
3. **不**修改既有 TestIsValidKeyFormat / TestIsIPAllowed / TestGetRequiredScope / TestExtractAPIKey / TestRateLimitResult

**apikey_integration_test.go 扩展(集成测 per D-12)**:
1. 文件顶 import 块(line 1-24)新增 `"strconv"`(stdlib)
2. **新增**函数 `TestRateLimitHeadersInResponse`(per PATTERNS.md line 384-431 + CONTEXT.md D-12 集成测模式):
   - `gin.SetMode(gin.TestMode)`
   - 复用现有 `fakeAPIKeyService`(line 33-68) + `fakeUsageLogger`(line 76-89) + `setupUsageLoggerTestDB`(line 111-137) + `hex64`(line 143-145)
   - `rl := services.NewRateLimiter()`(Pitfall 4 防御: 不能传 nil — line 244 RATELIMITER_INIT_PITFALLS 验证)
   - `router.Use(MultiAuth(fakeSvc, fakeLogger))` + `router.Use(RateLimitByScope(rl))`
   - `router.GET("/ping", func(c) { c.JSON(200, gin.H{"ok": true}) })`
   - `httptest.NewRequest("GET", "/ping", nil)` + `req.Header.Set("X-API-Key", "rec_"+hex64())`
   - `router.ServeHTTP(w, req)` 触发限流前导请求
   - **核心断言** `w.Header().Get("X-RateLimit-Limit")` 可被 `strconv.Atoi` 反解析为正整数;`X-RateLimit-Remaining` 可被反解析为非负整数
   - **防御性断言** `w.Header().Get("X-RateLimit-Limit") != "d"`(确保 P2-a 的 `"d"` 不再出现)
3. **不**修改既有 TestMultiAuthIntegration(line 151-233)三路径测试
  </action>
  <verify>
<automated>cd D:/code/ClaudeCode/guoguo && go test -v -run "TestRateLimit|TestMultiAuth" ./internal/middleware/...</automated>
<automated>cd D:/code/ClaudeCode/guoguo && go test -v -run "TestRateLimitHeaderEncoding" ./internal/middleware/...</automated>
<automated>cd D:/code/ClaudeCode/guoguo && grep -n "strconv.Itoa\|string(rune(result" internal/middleware/apikey.go</automated>
<automated>cd D:/code/ClaudeCode/guoguo && go test ./internal/middleware/...</automated>
</verify>
  <acceptance_criteria>
- `internal/middleware/apikey.go` 含 `strconv` import(在 stdlib 块内)
- `internal/middleware/apikey.go` line 267-268 不再出现 `string(rune(result.Limit))` 或 `string(rune(result.Remaining))` 字面量(grep 无匹配)
- `internal/middleware/apikey.go` line 267-268 现在是 `strconv.Itoa(result.Limit)` + `strconv.Itoa(result.Remaining)`
- `internal/middleware/apikey_test.go` 含 `TestRateLimitHeaderEncoding` 函数与两个 t.Run 子测试("strconv.Itoa 数字字符串化" 与 "header 可被 strconv.Atoi 反解析")
- `internal/middleware/apikey_integration_test.go` 含 `TestRateLimitHeadersInResponse` 函数,断言 `strconv.Atoi(w.Header().Get("X-RateLimit-Limit"))` 返回整数无错,且该字符串 `!= "d"`
- `go test -v -run TestRateLimitHeaderEncoding ./internal/middleware/...` 退出码 0
- `go test -v -run TestRateLimitHeadersInResponse ./internal/middleware/...` 退出码 0
- `go test -v -run TestMultiAuth ./internal/middleware/...` 既有测试不回归(退出码 0)
- `go build ./...` 退出码 0
  </acceptance_criteria>
  <done>strconv.Itoa 替换完成;TestRateLimitHeaderEncoding 与 TestRateLimitHeadersInResponse 全绿;既有 MultiAuth 集成测试无回归;build 通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /system/apikeys/* | Untrusted X-API-Key header / JWT bearer token crosses here |
| MultiAuth → APIKeyService | Authenticated API Key crosses via gin.Context (7 keys per Phase 57 D-04) |
| RateLimitByScope → HTTP headers | Result.Limit / Remaining numeric values serialized to client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-A1 | Spoofing | MultiAuth (apikey.go:23-87) | mitigate | X-API-Key 验证走 ValidateAPIKey;失败 → 401;无 X-API-Key → JWT 接管(D-03 锁定) |
| T-60-A2 | Tampering | apikey.go:267-268 rate-limit headers | mitigate | `string(rune(result.Limit))` → `strconv.Itoa(result.Limit)`;header 现在是 `"100"` 数字字面量,可被标准工具解析(D-11 锁定) |
| T-60-A3 | Repudiation | usageLogger.LogUsage (apikey.go:76-85) | mitigate | Phase 59 已固化 detached context + 真实 StatusCode/Duration/Success;Phase 60 沿用 |
| T-60-A4 | Info Disclosure | APIKey struct Key field 已移除 | mitigate | Phase 60 schema 改造(SEC-01 plan 60-02);Task 1 当前不暴露但禁止 `Key: apiKeyStr` log 写 |
| T-60-A5 | DoS | RateLimitByScope | accept | 单 scope 限流(QUAL-03 多 scope 选择留 Phase 61);scope=default 一律兜底 |
| T-60-A6 | EoP | isIPAllowed (apikey.go:118-152) | mitigate | `IPWhitelist` 非空 + 不匹配 → 403;空白名单放行(D-04 锁定) |
| T-60-A7 | Spoofing | /system/apikeys/* 路由组 | mitigate | RequirePermissions 在 MultiAuth 之前 → 权限校验先于认证,防止无权限用户拿到 handler 层逻辑 |
| T-60-SC | Tampering | go install / go build | mitigate | `go build ./...` 退出码 0 验证,无 dep 安装(plan 60 仅用 stdlib + 既有库) |
</threat_model>

<verification>
[Overall phase-60-01 checks]
- go build ./... exit code 0
- go test ./internal/middleware/... exit code 0(Task 2 全套)
- go test -v -run "TestMultiAuth|TestRateLimitHeader" ./internal/middleware/... 既有 + 新增全绿
- grep -n "internalmw.MultiAuth\|internalmw.RateLimitByScope" internal/api/router.go 匹配 2 行
- grep -n "strconv.Itoa(result" internal/middleware/apikey.go 匹配 2 行
- grep -n "string(rune(result" internal/middleware/apikey.go 匹配 0 行
- ls .planning/notes/260813-auth03-enable-decision.md exit code 0
- grep -c "^## " .planning/notes/260813-auth03-enable-decision.md == 5
</verification>

<success_criteria>
[Phase 60 / 60-01 完成判据]

1. SC#1 / AUTH-03 决策记录: `.planning/notes/260813-auth03-enable-decision.md` 存在 + **5 维度段落齐全**(挂载范围 / 认证优先级 / IP 白名单 / JWT 回退 / **作用域继承 (InheritPerms) 行为 — Phase 60 scope-boundary**),每段引用 D-01..D-04 锁定决策 + InheritPerms 段显式说明 Phase 60 不修改 + Phase 61 落地
2. SC#4 / QUAL-01 限流头: `TestRateLimitHeaderEncoding` + `TestRateLimitHeadersInResponse` 全绿,断言 `w.Header().Get("X-RateLimit-Limit")` 经 `strconv.Atoi` 反解析为正整数且 `!= "d"`
3. AUTH-03 挂载生效: `internal/api/router.go:apikeys 路由组` 含 3 段 middleware 链(顺序 `RequirePermissions` → `MultiAuth` → `RateLimitByScope`);`go build ./...` 通过
4. 既有 MultiAuth 集成测试无回归: `go test -v -run TestMultiAuth ./internal/middleware/...` 退出码 0
</success_criteria>

<output>
Create `.planning/phases/60-security-hardening-and-enable-decision/60-01-SUMMARY.md` when done
</output>
