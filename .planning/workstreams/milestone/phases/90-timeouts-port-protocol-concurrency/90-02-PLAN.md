---
phase: "90"
plan: "02"
type: "execute"
wave: 2
depends_on: ["90-01"]
files_modified:
  - "internal/api/v1/network/command_handler.go"
  - "internal/api/v1/network/execution_handler.go"
  - "internal/api/v1/network/discovery_handler.go"
autonomous: true
requirements:
  - "TIMEOUTS-03"
  - "TIMEOUTS-04"
  - "TIMEOUTS-07"
  - "TIMEOUTS-08"

must_haves:
  truths:
    - "command_handler.go:58 replaces req.Concurrency=10 with constants.CommandConcurrency"
    - "command_handler.go:61 replaces req.Timeout=300 with int(constants.CommandExecTimeout.Seconds())"
    - "command_handler.go:98 replaces req.Timeout=60 with int(constants.CommandReadTimeout.Seconds())"
    - "execution_handler.go:99 replaces req.Concurrency=10 with constants.CommandConcurrency"
    - "execution_handler.go:104 replaces req.Timeout=300 with int(constants.CommandExecTimeout.Seconds())"
    - "discovery_handler.go:126 replaces req.SNMPPort=161 with constants.SNMPPort"
    - "go build ./internal/api/v1/network/... 0 errors"
    - "go test ./internal/api/v1/network/... 0 failures"
  artifacts:
    - path: "internal/api/v1/network/command_handler.go"
      provides: "command execution with migrated timeout/concurrency constants"
      line: "57-62 (Concurrency+Timeout), 97-99 (QuickCommand Timeout)"
    - path: "internal/api/v1/network/execution_handler.go"
      provides: "template execution with migrated timeout/concurrency constants"
      line: "97-105 (Concurrency+Timeout)"
    - path: "internal/api/v1/network/discovery_handler.go"
      provides: "discovery with migrated SNMPPort constant"
      line: "124-127 (SNMPPort)"
  key_links:
    - from: "command_handler.go"
      to: "pkg/constants/concurrency.go"
      via: "import + constants.CommandConcurrency"
      pattern: "constants\\.CommandConcurrency"
    - from: "command_handler.go"
      to: "pkg/constants/timeouts.go"
      via: "import + constants.CommandExecTimeout.Seconds()"
      pattern: "constants\\.CommandExecTimeout\\.Seconds"
    - from: "execution_handler.go"
      to: "pkg/constants/timeouts.go"
      via: "import + int(constants.CommandExecTimeout.Seconds())"
      pattern: "constants\\.CommandExecTimeout\\.Seconds"
    - from: "discovery_handler.go"
      to: "pkg/constants/ports.go"
      via: "import + constants.SNMPPort"
      pattern: "constants\\.SNMPPort"
---

<objective>
Migrate network handlers (command_handler, execution_handler, discovery_handler) to reference the new constants package constants. Replace 3 concurrency literals + 3 timeout literals + 1 SNMP port literal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@internal/api/v1/network/command_handler.go (lines 50-108)
@internal/api/v1/network/execution_handler.go (lines 88-115)
@internal/api/v1/network/discovery_handler.go (lines 115-140)
@pkg/constants/timeouts.go
@pkg/constants/ports.go
@pkg/constants/concurrency.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate command_handler.go (3 literal replacements)</name>
  <files>internal/api/v1/network/command_handler.go</files>
  <action>
Read internal/api/v1/network/command_handler.go before editing.

**Changes needed:**

1. Add import for constants package: `"github.com/xingran-next/xingran-go-backend/pkg/constants"`

2. Line 57-58: Replace `req.Concurrency = 10` with:
```go
req.Concurrency = constants.CommandConcurrency
```

3. Line 60-61: Replace `req.Timeout = 300` with:
```go
req.Timeout = int(constants.CommandExecTimeout.Seconds())
```

4. Line 97-98 (QuickCommand): Replace `req.Timeout = 60` with:
```go
req.Timeout = int(constants.CommandReadTimeout.Seconds())
```

After all 3 changes, verify: `go build ./internal/api/v1/network/command_handler.go`
  </action>
  <verify>
    <automated>go build ./internal/api/v1/network/... && echo "command_handler builds"</automated>
  </verify>
  <done>command_handler.go:58 uses constants.CommandConcurrency; line 61 uses int(constants.CommandExecTimeout.Seconds()); line 98 uses int(constants.CommandReadTimeout.Seconds()); no inline 10/300/60 literals remain in these lines</done>
</task>

<task type="auto">
  <name>Task 2: Migrate execution_handler.go (2 literal replacements)</name>
  <files>internal/api/v1/network/execution_handler.go</files>
  <action>
Read internal/api/v1/network/execution_handler.go before editing.

**Changes needed:**

1. Add import for constants package if not already present.

2. Line 98-99: Replace `req.Concurrency = 10` with:
```go
req.Concurrency = constants.CommandConcurrency
```

3. Line 103-104: Replace `req.Timeout = 300` with:
```go
req.Timeout = int(constants.CommandExecTimeout.Seconds())
```

After both changes, verify: `go build ./internal/api/v1/network/...`
  </action>
  <verify>
    <automated>go build ./internal/api/v1/network/... && echo "execution_handler builds"</automated>
  </verify>
  <done>execution_handler.go:99 uses constants.CommandConcurrency; line 104 uses int(constants.CommandExecTimeout.Seconds()); no inline 10/300 literals remain in these lines</done>
</task>

<task type="auto">
  <name>Task 3: Migrate discovery_handler.go (SNMPPort=161 replacement)</name>
  <files>internal/api/v1/network/discovery_handler.go</files>
  <action>
Read internal/api/v1/network/discovery_handler.go before editing.

**Changes needed:**

1. Add import for constants package if not already present.

2. Line 125-126: Replace `req.SNMPPort = 161` with:
```go
req.SNMPPort = constants.SNMPPort
```

After the change, verify: `go build ./internal/api/v1/network/...`
  </action>
  <verify>
    <automated>go build ./internal/api/v1/network/... && echo "discovery_handler builds"</automated>
  </verify>
  <done>discovery_handler.go:126 uses constants.SNMPPort; no inline 161 literal remains in this line</done>
</task>

<task type="auto">
  <name>Task 4: Verify network package builds and tests pass</name>
  <files></files>
  <action>
Run full build and test for the network package:

```bash
go build ./internal/api/v1/network/...
go test ./internal/api/v1/network/... -v
```

Confirm zero errors and zero failures.
  </action>
  <verify>
    <automated>go build ./internal/api/v1/network/... && go test ./internal/api/v1/network/... 2>&1 | tail -5 && echo "network package build+tests pass"</automated>
  </verify>
  <done>go build ./internal/api/v1/network/... 0 errors, go test ./internal/api/v1/network/... 0 failures</done>
</task>

</tasks>

<verification>
go build ./internal/api/v1/network/...
go test ./internal/api/v1/network/... -v
</verification>

<success_criteria>
1. command_handler.go:58 uses constants.CommandConcurrency
2. command_handler.go:61 uses int(constants.CommandExecTimeout.Seconds())
3. command_handler.go:98 uses int(constants.CommandReadTimeout.Seconds())
4. execution_handler.go:99 uses constants.CommandConcurrency
5. execution_handler.go:104 uses int(constants.CommandExecTimeout.Seconds())
6. discovery_handler.go:126 uses constants.SNMPPort
7. go build ./internal/api/v1/network/... 0 errors
8. go test ./internal/api/v1/network/... 0 failures
</success_criteria>

<output>
Create .planning/milestones/v1.29-phases/90-timeouts-port-protocol-concurrency/90-02-SUMMARY.md when done
</output>
