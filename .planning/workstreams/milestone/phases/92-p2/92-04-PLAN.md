---
phase: 92-p2
plan: 04
type: execute
wave: 4
depends_on: ["92-01", "92-02", "92-03"]
files_modified:
  - internal/services/monitor/cache_service.go
  - internal/api/v1/monitor/cache_router.go
  - internal/services/system/cache_invariants_92_test.go
  - CLAUDE.md
  - .planning/REQUIREMENTS.md
  - .planning/workstreams/milestone/ROADMAP.md
autonomous: true
requirements: [CACHE-UNIFY-04, CACHE-UNIFY-05]

must_haves:
  truths:
    - "monitor 包同名接口消歧完成：CacheProvider rename 为 CacheOperator（纯包内 + 1 处跨包返回类型，语义零变更），base.CacheProvider 成为仓内唯一权威 CacheProvider 名（D-08）"
    - "invariants 扫描测试存在：system/ + operations/ 的 *_cache_impl.go 无 interface{} 闭包式 GetOrSet 残留（硬断言 0 + 白名单机制）；duty/knowledge/network/workorder 的同构残留仅 warning 计数不 fail（D-10②，A5 范围外站点）"
    - "CLAUDE.md 过时缓存描述全部修订（Dual Cache Architecture root 6 件套失实段 / Cache System Legacy 提法 / Legacy Services 列表 / Working with Cache / CacheProvider Interface 路径）+ 新增 Cache Service Convention 段（D-10①）"
    - "REQUIREMENTS § CACHE-UNIFY-01..04 措辞与实际达成形态同步（04 删'迁移到 system/ 下'改'原地定性 + 消除平行 TTL 逻辑'，D-06，Phase 90 commit 3a2efe5 先例）+ ROADMAP Phase 92 SC 措辞同步"
    - "LOC 双口径审计完成：样板调用段毛减口径（D-05 锚点 ≥200 判定）+ repo 全口径净减（诚实报告，OVR-91-01 先例）"
    - "phase gate 全绿：go build ./... + go test ./... + operlog/status AST 锁值防线 + invariants 扫描输出"
  artifacts:
    - path: internal/services/monitor/cache_service.go
      provides: "type CacheOperator interface（8 方法 Get/Set/Delete/Exists/Expire/TTL/Keys/FlushDB 签名零变更）"
      contains: "type CacheOperator interface"
    - path: internal/api/v1/monitor/cache_router.go
      provides: "NewCacheProviderAdapter 返回类型 monitorServices.CacheOperator（adapter struct/构造名不变）"
      contains: "monitorServices.CacheOperator"
    - path: internal/services/system/cache_invariants_92_test.go
      provides: "TestNoInterfaceGetOrSetResidue：system+operations 硬断言 0 残留 + 外围 warning 计数"
      contains: "TestNoInterfaceGetOrSetResidue"
    - path: CLAUDE.md
      provides: "Cache Service Convention 段 + 5 处过时段修订"
      contains: "Cache Service Convention"
    - path: .planning/REQUIREMENTS.md
      provides: "CACHE-UNIFY-01..04 措辞同步（含 [x] 勾选准备）"
      contains: "原地定性"
  key_links:
    - from: internal/services/monitor/cache_service.go
      to: internal/api/v1/monitor/cache_router.go
      via: "monitorServices.CacheOperator 返回类型引用（rename 的唯一跨包点）"
      pattern: "monitorServices\\.CacheOperator"
    - from: internal/services/system/cache_invariants_92_test.go
      to: system/operations *_cache_impl.go
      via: "parser.ParseFile 扫描 .GetOrSet(ctx + func() (interface{}, error) 闭包签名"
      pattern: "parser\\.ParseFile"
---

<objective>
消歧 + 守护 + 收口：monitor 同名接口 rename（D-08）、invariants 扫描锁（D-10②）、CLAUDE.md / REQUIREMENTS / ROADMAP 三文档同步（D-10①/D-06）、LOC 双口径审计与 phase gate 全量验证。

Purpose: D-02 定名 base.CacheProvider 唯一权威后消除同名异形认知混乱；invariants + Convention 段使统一成果不可无声倒退（Phase 89/90 收口惯例）；措辞同步使规划文档与实际达成形态一致（92-CONTEXT canonical_refs 锁定）。

Output: rename 完成 + invariants 测试 + 三文档修订 + LOC 审计报告 + 92-04-SUMMARY。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/92-p2/92-CONTEXT.md
@.planning/workstreams/milestone/phases/92-p2/92-PATTERNS.md
@.planning/workstreams/milestone/phases/92-p2/92-03-SUMMARY.md

# D-08 rename 对象
@internal/services/monitor/cache_service.go
@internal/api/v1/monitor/cache_router.go

# D-10② 扫描模式先例（AST 锁值 + 白名单机制）
@pkg/constants/pagination_test.go

# D-10① 修订对象（Cache System / Dual Cache Architecture / Legacy Services 段）
@CLAUDE.md

# D-06 措辞同步对象
@.planning/REQUIREMENTS.md

<interfaces>
<!-- rename 精确面（2026-09-05 planner 实测 grep——研究文档的"~58 处"高估：
     其余命中是 CacheProviderAdapter/NewCacheProviderAdapter 子串匹配，**adapter struct 与构造函数名不变**） -->

internal/services/monitor/cache_service.go:49    type CacheProvider interface { ... }   → type CacheOperator interface（8 方法签名逐字保留）
internal/services/monitor/cache_service.go:155   cacheProvider   CacheProvider          → cacheProvider   CacheOperator
internal/services/monitor/cache_service.go:165   cacheProvider CacheProvider,           → cacheProvider CacheOperator,
internal/api/v1/monitor/cache_router.go:42       func NewCacheProviderAdapter(core *core.Core) monitorServices.CacheProvider → monitorServices.CacheOperator

<!-- 不撞名不动清单（cache_service.go:60-82） -->
CacheConfigProvider / MultiLevelCacheProvider / DirectRedisProvider / StatsProvider — 全部保持原名
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: monitor.CacheProvider → CacheOperator rename 消歧（D-08）</name>
  <files>internal/services/monitor/cache_service.go, internal/api/v1/monitor/cache_router.go</files>
  <read_first>
    - internal/services/monitor/cache_service.go（:48-58 接口定义；:155 字段；:165 参数；:60-82 不撞名清单）
    - internal/api/v1/monitor/cache_router.go（:42 返回类型；CacheProviderAdapter struct 与其方法——**全部不动**）
    - internal/api/v1/monitor/cache_router_test.go（确认无接口类型直接引用——planner 实测其命中均为 CacheProviderAdapter/NewCacheProviderAdapter 子串）
  </read_first>
  <action>
    纯 rename 四处（见 interfaces 块精确面）：接口定义 CacheProvider → CacheOperator（含 :49 的 doc 注释同步，注明"原 CacheProvider，Phase 92 D-08 rename 消歧——业务缓存抽象唯一权威为 base.CacheProvider（internal/services/base），本接口是监控原始操作型（Get/Set/Keys/FlushDB）"）；同文件 :155/:165 两处类型引用；cache_router.go:42 返回类型。**CacheProviderAdapter struct、NewCacheProviderAdapter 构造函数、其全部方法、CacheConfigProvider/MultiLevelCacheProvider/DirectRedisProvider/StatsProvider 一律不动**。8 个方法签名（Get/Set/Delete/Exists/Expire/TTL/Keys/FlushDB）逐字保留——语义零变更。
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/services/monitor/ ./internal/api/v1/monitor/</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "type CacheOperator interface" internal/services/monitor/cache_service.go == 1
    - grep -c "type CacheProvider interface" internal/services/monitor/cache_service.go == 0
    - grep -rn "monitorServices.CacheProvider\b" internal/ --include="*.go" == 空
    - grep -c "type CacheProviderAdapter struct\|func NewCacheProviderAdapter" internal/api/v1/monitor/cache_router.go == 2（adapter 命名不动）
    - go test ./internal/services/monitor/ ./internal/api/v1/monitor/ 全绿
  </acceptance_criteria>
  <done>仓内 CacheProvider 名唯一指向 base.CacheProvider；monitor 监控接口为 CacheOperator；语义零变更</done>
</task>

<task type="auto">
  <name>Task 2: invariants 扫描测试 — interface{} 闭包式 GetOrSet 残留锁（D-10②）</name>
  <files>internal/services/system/cache_invariants_92_test.go</files>
  <read_first>
    - pkg/constants/pagination_test.go（Phase 89 AST 锁值先例：parser.ParseFile + Stability/Count 双锁结构）
    - internal/services/system/cache_utils.go（92-03 后现状——确认 InvalidateCache* 已删）
    - .planning/workstreams/milestone/phases/92-p2/92-RESEARCH.md（AST invariants 扫描先例节：Phase 92 变体定义 + 白名单机制）
  </read_first>
  <action>
    新建 internal/services/system/cache_invariants_92_test.go，package system（in-package 合法——本测试只做文件解析不 import 环包）。实现 TestNoInterfaceGetOrSetResidue：
    (1) 遍历 internal/services/system/ 与 internal/services/operations/ 目录下匹配 *_cache_impl.go 的文件（测试文件内以 runtime.Caller 或相对路径定位仓库 internal/services 根，禁本地绝对路径断言——v1.27 测试纪律）。
    (2) 对每个文件 go/parser.ParseFile 后遍历 ast，检测 CallExpr：selector 为 .GetOrSet 且任一实参含 FuncLit、其签名为 (interface{}, error) 返回（即 interface{} 闭包式 GetOrSet 调用）。实现可从简：优先 AST 精确匹配；若 AST 实现成本过高，允许对源码文本用正则（\.GetOrSet\(ctx[^\n]*func\(\) \(interface\{\}, error\)）但需注释说明正则口径。
    (3) 白名单机制：包级 var allowedResidues = map[string]int{}（初始全空 = 期望 0）；每文件残留计数 > allowedResidues[文件名] 时收集差异。
    (4) 断言双档（D-10②"warning 不 fail"与 Phase 89/90 硬锁的调和，planner 解释：硬档保护本期已清零的 system/operations 迁移面，warning 档覆盖 A5 范围外站点）：
      - 硬档：system + operations 两目录残留必须 == 白名单（初始 0），超出则 t.Errorf 列出 file:line；
      - warning 档：internal/services/{duty,knowledge,network,workorder} 的 *_cache_impl.go / cache_impl.go 同构残留仅 t.Logf("[WARNING] %s: %d 处 interface{} 闭包式 GetOrSet（v1.30+ 候选，A5 范围外）")，不 fail。
    (5) 文件头注释块：Phase 92-04 D-10② 纪律说明 + 与 pagination_test.go 的模式关系。
  </action>
  <verify>
    <automated>go test ./internal/services/system/ -run TestNoInterfaceGetOrSetResidue -v</automated>
  </verify>
  <acceptance_criteria>
    - go test ./internal/services/system/ -run TestNoInterfaceGetOrSetResidue -v PASS
    - 测试输出含 warning 档计数行（t.Logf 的 v1.30+ 候选提示，验证扫描真实执行而非空转）
    - 人为回归演练（临时把任一 system impl 加回一处 interface{} 闭包 GetOrSet 再撤销）：测试转红后再转绿（executor 可用 stash 或手工两步验证，最终工作区保持干净）
    - grep -c "allowedResidues" internal/services/system/cache_invariants_92_test.go >= 1（白名单机制在位）
  </acceptance_criteria>
  <done>invariants 扫描进 CI：system/operations 迁移面残留不可无声倒退，外围 12 处延迟站点可见可追踪</done>
</task>

<task type="auto">
  <name>Task 3: 文档三件套同步 — CLAUDE.md + REQUIREMENTS + ROADMAP（D-10①/D-06）</name>
  <files>CLAUDE.md, .planning/REQUIREMENTS.md, .planning/workstreams/milestone/ROADMAP.md</files>
  <read_first>
    - CLAUDE.md（5 个修订落点：Key Architecture Patterns 的 "Dual Cache Architecture" 段；Module-Specific Notes 的 "### Cache System" 段；Key File Locations 的 "Legacy Services (still used by core)" 列表；"### Working with Cache" 段；Architecture reference 的 "### CacheProvider Interface" 段——以内容锚点定位，行号以当前文件为准）
    - .planning/REQUIREMENTS.md § CACHE-UNIFY（:68-76 原文）
    - .planning/workstreams/milestone/ROADMAP.md § Phase 92（SC 1-5 与 Plans 列表）
    - .planning/workstreams/milestone/phases/92-p2/92-PATTERNS.md（CLAUDE.md 修订落点节：新增 Convention 段内容锚点）
  </read_first>
  <action>
    (1) CLAUDE.md D-10①：
      - "Dual Cache Architecture" 段重写：删除 root 6 件套 Legacy/New 两分描述（dept/role/dict/menu/user/post root 文件已在 v1.27 Phase 79 迁入 system/，描述失实），改述为单一权威路径——缓存抽象唯一权威在 internal/services/base（TTLResolver + CacheServiceBase + CacheProvider + GetOrSetJSON 函数族），system/cache_provider.go 仅余 type alias。
      - "### Cache System" 段：Legacy/New 两分法改为 base 路径单权威 + DataCacheService 定位说明（pkg/cache 封装层 + Adaptee，D-06/D-07 定性）。
      - "Legacy Services (still used by core)" 列表：删除已不存在的 6 个 root cache service 条目，保留真实条目（dept_service.go 等——以 ls internal/services/*.go 实况为准）。
      - "### Working with Cache"：改锁 "新代码一律 base.GetOrSetJSON + base.CacheProvider"；键构造唯一真相源 system/cache_keys.go；失效唯一底层 base.Invalidate/InvalidatePattern。
      - "### CacheProvider Interface"（Architecture reference）：接口定义出处改为 internal/services/base/cache_provider.go（方法清单同步 9 方法）。
      - 新增 "### Cache Service Convention" 段（照 Phase 90 D-11 / Pagination Convention 段格式）：锁 base.GetOrSetJSON/SetJSON/Invalidate/InvalidatePattern + base.CacheProvider 唯一权威；cache_impl 禁止 interface{} 闭包式 GetOrSet（invariants 扫描守护）；monitor.CacheOperator 是监控原始操作接口非业务缓存；新代码禁止直接新增 DataCacheService 调用点（D-07）。
    (2) REQUIREMENTS.md § CACHE-UNIFY 措辞同步（逐条改写原文，保留 REQ-ID）：
      - CACHE-UNIFY-01：补注实际形态"泛型包级函数族 + TTLResolver 薄基类（Go method 不能有类型参数，D-03；SC-1 措辞随之修订）"
      - CACHE-UNIFY-02：补注"29 处实测（含 notice 逃兵归队），嵌入源迁 base + 方法体换泛型函数"
      - CACHE-UNIFY-03：补注"3 处实测（:168 为注释行假阳性）+ CacheInvalidator 保留分发器底层委托（D-04）"
      - CACHE-UNIFY-04：删"标注 @Deprecated 并迁移到 system/ 下，core.Core 引用路径同步更新"，改为"原地定性为基础设施（root↔system import cycle 硬约束）：消除平行 GetExpiration（委托 base TTL 逻辑）+ D-07 定位注释；12+ API 文件引用零改动"（D-06 锁定措辞）
    (3) ROADMAP.md § Phase 92：SC-1 改"base/cache_service_base.go 提供泛型包级函数族（GetOrSetJSON/SetJSON/Invalidate/InvalidatePattern）+ TTLResolver 薄基类"；SC-3 改"DataCacheService 原地定性 + 平行 TTL 逻辑消除（D-06）"；SC-5 改"miniredis 自动化集成验证（D-09）"；Plans 列表按 92-01..04 实际四计划勾选。
    文档措辞以"实际达成形态"为准，不新增范围（Phase 90 commit 3a2efe5 先例：措辞同步非需求变更）。
  </action>
  <verify>
    <automated>grep -c "Cache Service Convention" CLAUDE.md && grep -c "原地定性" .planning/REQUIREMENTS.md && grep -c "GetOrSetJSON" .planning/workstreams/milestone/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md：grep -c "Dual Cache Architecture" == 0（失实段已重写）；grep -c "Cache Service Convention" >= 1；grep -c "Legacy Services" == 0 或列表内容与 internal/services/*.go 实况一致
    - CLAUDE.md 无 "xingran-react-frontend" 以外的失实路径（base/cache_provider.go 出现于 CacheProvider Interface 段）
    - REQUIREMENTS.md § CACHE-UNIFY-04 含"原地定性"且不含"标注 @Deprecated 并迁移到 system/ 下"
    - ROADMAP.md § Phase 92 SC-1 含 GetOrSetJSON、SC-3 含原地定性、SC-5 含 miniredis
    - 三文档不新增 v1.29 范围外语义（仅措辞校准）
  </acceptance_criteria>
  <done>三文档与代码实况一致：新会话/新 agent 读到的缓存架构描述不再误导（D-10①），规划措辞与达成形态对齐（D-06，REQ_SYNC_OK）</done>
</task>

<task type="auto">
  <name>Task 4: LOC 双口径审计 + phase gate 全量验证 + SUMMARY</name>
  <files>.planning/workstreams/milestone/phases/92-p2/92-04-SUMMARY.md</files>
  <read_first>
    - .planning/workstreams/milestone/phases/92-p2/92-01-SUMMARY.md, 92-02-SUMMARY.md, 92-03-SUMMARY.md（前置三 plan 的记录——SUMMARY 汇总与 LOC 分摊口径来源）
    - .planning/workstreams/milestone/phases/91-crud-base-repository-t-p1/91-04-SUMMARY.md（OVR-91-01 双口径报告先例格式）
    - .planning/workstreams/milestone/phases/92-p2/92-VALIDATION.md（Phase Gate 清单逐项对照）
  </read_first>
  <action>
    (1) LOC 双口径审计（D-05 + RESEARCH Open Question 1，OVR-91-01 先例）：
      - 确定审计基点 commit：Phase 92 首个 commit 的父 commit（git log 定位 92-01 首 commit）
      - 口径 A（D-05 锚点口径）：样板调用段毛减——git diff --numstat <base>..HEAD 限定 12 个迁移/改造生产文件（system 9 impl + floor_cache_impl + cache_invalidator + data_cache_service），合计 deleted - added；判定毛减 ≥ 200 达成与否
      - 口径 B（repo 全口径）：git diff --numstat <base>..HEAD -- internal/services/ internal/core/ 全量净额（base 新增生产文件 ~150-200 行计入），诚实记录（可能低于 200，预期 shortfall 不算失败——planning 锁定双基）
      - 结果写入 SUMMARY（双数字 + 判定 + 口径定义）
    (2) Phase gate 全量：go build ./... 0 错误；go test ./... 全绿（全量口径）；go test ./internal/utils/operlog/ ./internal/models/ 防线绿；invariants 测试绿（Task 2 产物）
    (3) SUMMARY 内容：四 task 完成记录；LOC 双口径表；Pitfall 5 单向改善记录（user List 站点 NoOp 路径修正 + SetJSON 组合语义说明）；A6 死代码登记（internal/api/v1/system/cache_adapter.go NewDataCacheAdapter 生产 0 调用者，v1.30 清理候选，本期未动）；日志格式统一说明（含 pattern/key 字段的新格式 + CacheInvalidator 成功 Debugf 消失）；D-01..D-10 决策落地对照表。
    (4) REQUIREMENTS.md CACHE-UNIFY-01..05 若核验达成可勾选 [x]（Task 3 已完成措辞同步，此处按 verify-work 流程惯例由后续 gate 勾选亦可——以 91 先例为准）。
  </action>
  <verify>
    <automated>go build ./... && go test ./... && go test ./internal/services/system/ -run TestNoInterfaceGetOrSetResidue -v</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... 0 错误；go test ./... 0 失败（全量 gate）
    - go test ./internal/utils/operlog/ ./internal/models/ 全绿（AST 锁值防线）
    - 92-04-SUMMARY.md 存在且含：LOC 口径 A 数字、口径 B 数字、双判定、D-01..D-10 对照表、A6 登记、Pitfall 5 记录
    - git log 显示本 phase 四 plan 各自原子 commit 存在（v1.29 D-04 纪律）
  </acceptance_criteria>
  <done>Phase 92 收口：全 gate 绿、双口径 LOC 报告落档、SUMMARY 完整——可进入 /gsd:verify-work</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新信任边界 | 本 plan 是 rename + 文档 + 测试守护 + 审计，无运行时行为面变更 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01 | Tampering | invariants 扫描自身的健壮性 | mitigate | 硬档+白名单机制使扫描不可被静默绕过（新增豁免必须显式登记 allowedResidues，diff 可见）；acceptance 含人为回归演练验证测试真实可红 |
| T-92-02 | Tampering | 文档失实误导后续开发（回退到旧模式） | mitigate | CLAUDE.md Convention 段锁唯一权威路径 + invariants CI 双防线；措辞同步消除规划文档与实现的漂移 |
| T-92-SC | Tampering | 依赖安装 | accept | 零新依赖 |
</threat_model>

<verification>
- go build ./... + go test ./... 全绿（Phase gate）
- monitor rename 后 internal/api/v1/monitor/ 测试全绿（cache_router_test 回归）
- invariants 测试 PASS 且 warning 档输出可见
- 三文档 diff 仅措辞校准无范围变更
- operlog / status AST 锁值防线保持绿
</verification>

<success_criteria>
1. monitor.CacheOperator rename 完成，仓内 CacheProvider 名唯一指向 base
2. TestNoInterfaceGetOrSetResidue 进 CI：system/operations 硬锁 0 残留 + 外围 warning 可见
3. CLAUDE.md 5 处修订 + Cache Service Convention 新段；REQUIREMENTS/ROADMAP 措辞同步完成
4. LOC 双口径审计落档（毛减 ≥200 判定 + 全口径诚实报告）
5. Phase gate 全绿，92-04-SUMMARY 完整
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/92-p2/92-04-SUMMARY.md` when done
</output>
