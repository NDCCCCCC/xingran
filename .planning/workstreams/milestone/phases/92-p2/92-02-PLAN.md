---
phase: 92-p2
plan: 02
type: execute
wave: 2
depends_on: ["92-01"]
files_modified:
  - internal/services/system/user_cache_impl.go
  - internal/services/system/menu_cache_impl.go
  - internal/services/system/role_cache_impl.go
  - internal/services/system/config_cache_impl.go
  - internal/services/system/department_cache_impl.go
  - internal/services/system/dict_cache_impl.go
  - internal/services/system/post_cache_impl.go
  - internal/services/system/settings_cache_impl.go
  - internal/services/system/notice_cache_impl.go
autonomous: true
requirements: [CACHE-UNIFY-02, CACHE-UNIFY-05]

must_haves:
  truths:
    - "system 9 个 *_cache_impl.go 的 29 处 interface{} 闭包式 GetOrSet 样板全部替换为 base.GetOrSetJSON 单 return（D-03/CACHE-UNIFY-02），每处 GetOrSet 调用段 ≤6 行"
    - "system 包内 21 处 InvalidateCacheByKey/ByPattern 调用改写为 base.Invalidate/base.InvalidatePattern（D-04 涟漪的 in-system 部分，与同文件迁移同 commit）"
    - "notice_cache_impl.go 逃兵归队：嵌入 CacheServiceBase + 构造函数接线 + 删除私有 getExpiration，与基类契约一致"
    - "缓存键构造逐字不变（system/cache_keys.go 常量与内联 fmt.Sprintf 键均不改，Redis 现存键迁移后继续命中）"
    - "缓存值 T 形态逐点保持 JSON 兼容：匿名 struct 站点（notice）用具名局部类型，字段名不变"
    - "既有 system 缓存测试（config/department/dict/menu/post/role/user cache_impl_test + gapfill 系列 + cache_infra_test）0 回归"
  artifacts:
    - path: internal/services/system/user_cache_impl.go
      provides: "pilot 样本：5 处 GetOrSetJSON + 7 处 base.Invalidate*（本 plan 的批量复制模板）"
      contains: "base.GetOrSetJSON"
    - path: internal/services/system/menu_cache_impl.go
      provides: "6 处 GetOrSet（最多站点）迁移"
      contains: "base.GetOrSetJSON"
    - path: internal/services/system/notice_cache_impl.go
      provides: "逃兵归队：CacheServiceBase 嵌入 + noticeListPage 具名局部类型 + buildMyNoticesKey helper"
      contains: "CacheServiceBase"
    - path: internal/services/system/role_cache_impl.go
      provides: "6 处 GetOrSet 迁移（含 :174 双 pattern 站点）"
      contains: "base.GetOrSetJSON"
  key_links:
    - from: system/*_cache_impl.go (29 处调用点)
      to: base.GetOrSetJSON
      via: "单 return 泛型调用：return base.GetOrSetJSON(ctx, s.cache, key, s.GetExpiration(cfg, d), query)"
      pattern: "base\\.GetOrSetJSON\\("
    - from: system/*_cache_impl.go (21 处失效调用)
      to: base.Invalidate / base.InvalidatePattern
      via: "机械改名：InvalidateCacheByKey(ctx, s.cache, keys, M) → base.Invalidate(ctx, s.cache, keys, M)"
      pattern: "base\\.(Invalidate|InvalidatePattern)\\("
    - from: notice_cache_impl.go (struct)
      to: CacheServiceBase (= base.CacheServiceBase alias)
      via: "嵌入 + 构造函数 CacheServiceBase: CacheServiceBase{Config: config}"
      pattern: "CacheServiceBase:\\s*CacheServiceBase\\{Config: config\\}"
---

<objective>
把 system 包 9 个 cache_impl 文件的 29 处 GetOrSet 五段式样板全部迁移到 `base.GetOrSetJSON` 泛型函数族（每处 12-15 行 → ~5 行单 return），同步把包内 21 处失效调用改写到 base 唯一底层，notice 逃兵归队嵌入基类。行为零变更，键构造逐字保留。

Purpose: CACHE-UNIFY-02 主体；92-01 产出的泛型函数族的第一个批量消费者，pilot → 批量 → 复杂站点三段节奏（Phase 91 先例）。

Output: 9 个文件迁移完成 + system 缓存测试 0 回归 + interface{} 闭包式 GetOrSet 在 system 包清零。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/92-p2/92-CONTEXT.md
@.planning/workstreams/milestone/phases/92-p2/92-PATTERNS.md
@.planning/workstreams/milestone/phases/92-p2/92-01-SUMMARY.md

# pilot 迁移对象（5 处样板 + 7 处失效的范本文件）
@internal/services/system/user_cache_impl.go

# 逃兵归队对象
@internal/services/system/notice_cache_impl.go

<interfaces>
<!-- 92-01 产出的迁移靶点签名（executor 直接使用，勿重读 base 源码推导） -->

base.GetOrSetJSON[T any](ctx context.Context, p CacheProvider, key string, ttl time.Duration, query func() (T, error)) (T, error)
base.Invalidate(ctx context.Context, p CacheProvider, keys []string, module string)
base.InvalidatePattern(ctx context.Context, p CacheProvider, patterns []string, module string)

<!-- 迁移后单 return 形态（92-CONTEXT D-03 预览，用户已确认；ttl 用预解析 Duration 形态——
     s.GetExpiration 来自嵌入基类，该行零改动，结果作为参数传入） -->

func (s *userCacheService) GetByIDWithCache(ctx context.Context, id string) (*models.User, error) {
    return base.GetOrSetJSON(ctx, s.cache,
        GetUserByIDKey(id),
        s.GetExpiration(services.CacheConfigUserByID, 30*time.Minute),
        func() (*models.User, error) { return s.userService.GetByID(ctx, id) })
}

<!-- 失效调用机械改写（参数列表逐字不变，仅函数名前缀更换） -->

InvalidateCacheByKey(ctx, s.cache, keys, "USER")      → base.Invalidate(ctx, s.cache, keys, "USER")
InvalidateCacheByPattern(ctx, s.cache, pats, "USER")  → base.InvalidatePattern(ctx, s.cache, pats, "USER")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pilot — user_cache_impl.go 迁移（5 处 GetOrSet + 7 处失效调用）</name>
  <files>internal/services/system/user_cache_impl.go</files>
  <read_first>
    - internal/services/system/user_cache_impl.go（全文：5 处 GetOrSet 位于 :44/:61/:91/:129/:194；7 处失效调用位于 :264/:270/:280/:293/:305/:315/:327；struct 嵌入与构造函数 :15-35）
    - internal/services/base/cache_functions.go（92-01 产出的函数族签名）
    - .planning/workstreams/milestone/phases/92-p2/92-PATTERNS.md（"被消灭的样板——五段式现状" :38-52 与目标形态；复杂站点 1——List :185-199）
  </read_first>
  <action>
    逐处替换 5 个 GetOrSet 样板为 base.GetOrSetJSON 单 return（import 增加 base 包；T 从 dest 变量类型与闭包返回类型联合推导）：
    - :44 GetByIDWithCache → GetOrSetJSON[*models.User]，闭包返回 s.userService.GetByID(ctx, id)
    - :61/:91/:129 三个同类站点 → T 按各自 dest 类型（[]models.Role / 角色列表 / 其他），闭包逐字保留原 query 逻辑
    - :194 List → GetOrSetJSON[*PageResult]（**关键**：现状 dest 是 PageResult 值 + 闭包返回 *PageResult——NoOp 反射路径静默丢结果返回零值，即 Pitfall 5 缺陷；迁移后 dest=&result 即 **PageResult，NoOp 路径反而修正为返回真实数据。这是缺陷修复方向的单向改善，**禁止"修回"旧形态**；SUMMARY 需记录此站点）。方法尾部 return &result, err 形态随单 return 消失。

    替换 7 处失效调用为 base.Invalidate / base.InvalidatePattern（参数列表与 module 常量 "USER" 逐字保留，仅前缀改名）。CacheKeyUserByID/CacheKeyUserList 等键常量引用逐字不动。

    struct 定义（*userService + cache + CacheServiceBase 嵌入）与 NewUserServiceWithCache 构造函数零改动（alias 已保证兼容）。每处迁移后确认 GetOrSet 调用段（键构造行 + return base.GetOrSetJSON(...) 整体）≤6 行——键构造如 buildListCacheKey 已抽 helper 的保持现状（Pitfall 3 的口径：调用段 ≤6 行，非整方法体）。
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/services/system/ -run "TestUserCache|TestUser" -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "GetOrSet(ctx" internal/services/system/user_cache_impl.go == 0
    - grep -c "base.GetOrSetJSON" internal/services/system/user_cache_impl.go == 5
    - grep -c "InvalidateCacheByKey\|InvalidateCacheByPattern" internal/services/system/user_cache_impl.go == 0
    - grep -c "base.Invalidate" internal/services/system/user_cache_impl.go == 7
    - git diff internal/services/system/user_cache_impl.go 中 GetUserByIDKey/CacheKeyUserByID/CacheKeyUserList 等键构造零 diff
    - go test ./internal/services/system/ 全绿（user_cache_impl_test.go + gapfill 回归）
  </acceptance_criteria>
  <done>user_cache_impl.go 5+7 处全部收敛到 base 函数族单 return，测试 0 回归——批量复制模板成立</done>
</task>

<task type="auto">
  <name>Task 2: 批量迁移 — menu/role/config/department/dict/post/settings（20 处 GetOrSet + 11 处失效调用）</name>
  <files>internal/services/system/menu_cache_impl.go, internal/services/system/role_cache_impl.go, internal/services/system/config_cache_impl.go, internal/services/system/department_cache_impl.go, internal/services/system/dict_cache_impl.go, internal/services/system/post_cache_impl.go, internal/services/system/settings_cache_impl.go</files>
  <read_first>
    - internal/services/system/user_cache_impl.go（Task 1 迁移后的 pilot 形态——机械复制模板）
    - internal/services/system/menu_cache_impl.go（6 处 GetOrSet :41/:55/:85/:115/:130/:150 + 2 处失效 :162/:189；:189 的实参是局部变量 cache + userMenuCacheInvalidationPatterns()，改写保留其实参原样）
    - internal/services/system/role_cache_impl.go（6 处 :44/:90/:107/:121/:135/:174 + 3 处失效 :213/:221/:265）
    - internal/services/system/config_cache_impl.go（3 处 :42/:59/:76 + 2 处失效 :99/:105）
    - internal/services/system/department_cache_impl.go（2 处 :49/:83 + 1 处失效 :105）
    - internal/services/system/dict_cache_impl.go（2 处 :43/:204 + 2 处失效 :97/:271；**含双 struct** dictTypeCacheService + dictDataCacheService，确认各自嵌入 CacheServiceBase 的现状后再迁）
    - internal/services/system/post_cache_impl.go（2 处 :41/:55 + 1 处失效 :97）
    - internal/services/system/settings_cache_impl.go（1 处 :44，无失效调用）
  </read_first>
  <action>
    按 pilot 模板逐文件机械迁移（每个文件：import 增加 base → 自上而下替换 GetOrSet 样板 → 替换失效调用 → 下一文件）：
    - 五段式（cacheKey 构造 + var result + expiration + GetOrSet 闭包 + err 分支/结果搬运）→ 单 return base.GetOrSetJSON
    - 闭包内逻辑逐字保留（不做任何"顺手重构"；仅当闭包原本就单行委托时可内联为方法值）
    - 失效调用 InvalidateCacheByKey/ByPattern → base.Invalidate/InvalidatePattern，参数逐字不变
    - 缓存键构造（cache_keys.go 常量 + GetXxxKey helper + 内联 fmt.Sprintf）逐字不动
    - T 推导注意：dest 为值类型（models.Config / []models.Xxx）→ T 取值类型；dest 为指针 → T 取指针类型；以闭包返回类型为准对齐（编译器会报 mismatch，按报错修正 T 即可，不改闭包逻辑）
    - menu :150（若为多返回值站点）/ role :174 等复杂站点：result 拆包行保留在 return 处（调用段 ≤6 行口径）

    每完成一个文件立即 go build ./... （CLAUDE.md 单文件修复纪律，禁止跨文件批量盲改）。
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/services/system/ -run "Cache" -v && go test ./internal/services/system/</automated>
  </verify>
  <acceptance_criteria>
    - 7 个文件各自 grep -c "GetOrSet(ctx" == 0
    - 7 个文件 grep -c "base.GetOrSetJSON" 合计 == 20（menu 6 + role 6 + config 3 + department 2 + dict 2 + post 2 + settings 1）
    - 7 个文件 grep -c "InvalidateCacheByKey\|InvalidateCacheByPattern" 合计 == 0 且 grep -c "base.Invalidate" 合计 == 11
    - go test ./internal/services/system/ 全绿（config/department/dict/menu/post/role cache_impl_test + gapfill 系列 + settings/notice 相关测试）
    - git diff 无任何键构造字符串变更
  </acceptance_criteria>
  <done>system 包 27/29 处样板收敛（menu+role+config+department+dict+post+settings），11 处失效调用走 base 底层，测试 0 回归</done>
</task>

<task type="auto">
  <name>Task 3: notice_cache_impl.go 逃兵归队（嵌入基类 + 具名局部类型 + 键 helper）</name>
  <files>internal/services/system/notice_cache_impl.go</files>
  <read_first>
    - internal/services/system/notice_cache_impl.go（全文：struct :42-48 无基类嵌入；构造函数 :50-62；私有 getExpiration :64-70；2 处 GetOrSet :215/:242（调用点 getExpiration :213/:240）；3 处失效 :297/:303/:309）
    - .planning/workstreams/milestone/phases/92-p2/92-PATTERNS.md（"逃逸样本"节——归队四步动作 + 复杂站点 2 匿名 struct :202-233 + Pitfall 6）
    - .planning/workstreams/milestone/phases/92-p2/92-RESEARCH.md（Pitfall 6：匿名 struct 缓存值的 T 形态；Pitfall 3：调用段 ≤6 行口径）
  </read_first>
  <action>
    归队四步（PATTERNS 锁定）：
    (1) noticeCacheService struct 增加嵌入字段 CacheServiceBase（保留 db/base/cache 字段；config 字段若仅剩 getExpiration 一个消费者则删除该字段，若有其他消费者保留）。
    (2) NewNoticeServiceWithCache 构造函数增加 CacheServiceBase: CacheServiceBase{Config: config} 赋值。
    (3) 删除私有 getExpiration 方法（:64-70，与基类 GetExpiration 完全同构——"消除平行 TTL 逻辑"的本体）；:213/:240 调用点改为 s.GetExpiration（configKey 字符串 "cache.notice.my_notices"/"cache.notice.unread_count" 与 default 值 1*time.Minute/30*time.Second 逐字不动）。
    (4) :215 GetUserNotices 迁移——**Pitfall 6**：就地声明具名局部类型 type noticeListPage struct { List []models.Notice; Total int64 }（方法内或包级私有），T = noticeListPage，闭包组装 noticeListPage{List: list, Total: total}；JSON 字段名 List/Total 不变（缓存数据兼容）。cacheKey 的 if/else 双分支（status nil / 非 nil 两种 fmt.Sprintf 内联键）抽为包级私有 helper buildMyNoticesKey(userID string, page, pageSize int, status *string) string——**键构造结果逐字节等价**（两种 fmt.Sprintf 格式串与参数顺序原样搬入）。:242 GetUnreadCount 按 pilot 模板常规迁移。
    (5) 3 处失效调用 :297/:303/:309 → base.Invalidate / base.InvalidatePattern（:303 的双 pattern fmt.Sprintf 构造逐字保留——它是 pattern 实参不是键 helper 对象）。
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/services/system/ -run "Notice" -v && go test ./internal/services/system/</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "getExpiration" internal/services/system/notice_cache_impl.go == 0（平行 TTL 逻辑消灭）
    - grep -c "CacheServiceBase" internal/services/system/notice_cache_impl.go >= 2（嵌入 + 构造赋值）
    - grep -c "GetOrSet(ctx" internal/services/system/notice_cache_impl.go == 0 且 grep -c "base.GetOrSetJSON" == 2
    - grep -c "base.Invalidate" internal/services/system/notice_cache_impl.go == 3
    - notice:my_notices 与 notice:unread_count 键格式串在 git diff 中仅位置移动零字符变更（buildMyNoticesKey 搬迁）
    - grep -rn "GetOrSet(ctx" internal/services/system/*.go | grep -v _test | wc -l == 0（system 包生产代码清零）
    - go test ./internal/services/system/ 全绿
  </acceptance_criteria>
  <done>system 9 文件 29 处样板全部走 base.GetOrSetJSON，notice 逃兵归队嵌入基类，私有 getExpiration 删除，CACHE-UNIFY-02 达成</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 应用 → Redis | 29 处缓存读写字径流经迁移后的 base.GetOrSetJSON（键构造与值序列化不变） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01 | Tampering | 缓存键构造（29 处迁移站点） | mitigate | 键构造逐字不变为 acceptance_criteria 硬项：git diff 校验键常量/格式串零字符变更；Redis 现存键迁移后继续命中（acceptance：键 diff 零变更） |
| T-92-02 | Tampering | 失效路径（21 处 in-system 改写） | mitigate | base.Invalidate/InvalidatePattern 行为与旧函数逐字对齐（92-01 测试锁定）；本 plan 完成后旧函数仍有 floor+外部调用者继续工作，无失效真空；92-03 删除时编译器兜底 |
| T-92-03 | Informational | 闭包逻辑搬运引入行为漂移 | mitigate | 闭包 query 逻辑逐字保留（action 明令禁止顺手重构）；既有 cache_impl_test 全量回归 + 92-01 miniredis 行为锁双保险 |
| T-92-SC | Tampering | 依赖安装 | accept | 零新依赖 |
</threat_model>

<verification>
- go build ./... 0 错误（每文件迁移后即跑）
- go test ./internal/services/system/ 全绿（quick 口径 <60s）
- grep -rn "GetOrSet(ctx" internal/services/system/ --include="*.go" | grep -v _test == 空（29 处清零）
- grep -rn "InvalidateCacheByKey\|InvalidateCacheByPattern" internal/services/system/*_cache_impl.go == 空（21 处清零）
- go test ./internal/services/operations/ 仍绿（floor 跨包嵌入点未被本 plan 触碰，回归确认）
</verification>

<success_criteria>
1. system 9 文件 29 处 GetOrSet 样板全部迁移 base.GetOrSetJSON，每处调用段 ≤6 行
2. notice 逃兵归队：CacheServiceBase 嵌入 + 私有 getExpiration 删除
3. 21 处失效调用改写 base.Invalidate/InvalidatePattern
4. 缓存键构造 diff 级零变更；notice 缓存值 JSON 形状不变
5. go test ./internal/services/system/ 0 失败；既有测试网全绿
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/92-p2/92-02-SUMMARY.md` when done
</output>
