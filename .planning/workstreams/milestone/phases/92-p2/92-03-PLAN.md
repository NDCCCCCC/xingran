---
phase: 92-p2
plan: 03
type: execute
wave: 3
depends_on: ["92-01", "92-02"]
files_modified:
  - internal/services/operations/floor_cache_impl.go
  - internal/services/operations/cache_invalidator.go
  - internal/core/core.go
  - internal/services/duty/duty_cache_impl.go
  - internal/services/knowledge/knowledge_cache_impl.go
  - internal/services/network/cache_impl.go
  - internal/services/workorder/workorder_cache_impl.go
  - internal/services/system/cache_utils.go
  - internal/services/data_cache_service.go
autonomous: true
requirements: [CACHE-UNIFY-03, CACHE-UNIFY-04]

must_haves:
  truths:
    - "operations 3 处真实 GetOrSet 样板（floor_cache_impl.go :43/:60/:179；:168 是注释行 grep 假阳性）全部迁移 base.GetOrSetJSON（CACHE-UNIFY-03）"
    - "operations/CacheInvalidator struct 保留（Excel 管道 entityType 分发语义），InvalidateByEntityType/InvalidateByPatterns 底层循环改为委托 base.InvalidatePattern（D-04）"
    - "D-04 涟漪收尾：floor 2 处 + 外围 19 处（core.go 1 / duty 5 / knowledge 4 / network 5 / workorder 4）失效调用改写 base.* 后，system.InvalidateCacheByPattern/ByKey 从 cache_utils.go 删除——go build ./... 编译器证明零遗漏调用者"
    - "DataCacheService 原地定性：永久留 services root（root↔system import cycle 硬约束），GetExpiration 保留签名、内部委托 base TTL 逻辑，2 个锁定测试（Dcs7901/Ccs7901）保持绿（D-06/A3）"
    - "data_cache_service.go 文件头新增 D-07 双定位注释（pkg/cache.Cache 业务封装层 + base.CacheProvider 实现底座），不标 @Deprecated"
    - "失效日志统一为含 pattern/key 字段的信息量大的格式（纯日志文本变更，业务行为零变更）"
  artifacts:
    - path: internal/services/operations/floor_cache_impl.go
      provides: "3 处 base.GetOrSetJSON + 2 处 base.Invalidate*（跨包嵌入 alias 兼容的实证站点）"
      contains: "base.GetOrSetJSON"
    - path: internal/services/operations/cache_invalidator.go
      provides: "InvalidateByEntityType/InvalidateByPatterns 委托 base.InvalidatePattern（struct/构造函数零改动）"
      contains: "base.InvalidatePattern"
    - path: internal/services/system/cache_utils.go
      provides: "InvalidateCacheByPattern/InvalidateCacheByKey 已删除；仅剩 filterSlice/contains/paginate + CacheServiceBase alias（alias 落点按 92-01 实际）"
      contains: "filterSlice"
    - path: internal/services/data_cache_service.go
      provides: "GetExpiration 委托 base + D-07 定位注释块"
      contains: "base.CacheServiceBase"
  key_links:
    - from: operations/cache_invalidator.go
      to: base.InvalidatePattern
      via: "for 循环替换为单行委托（nil 防护内聚于 base）"
      pattern: "base\\.InvalidatePattern\\(ctx, c\\.cache, patterns, entityType\\)"
    - from: internal/core/core.go:366
      to: base.InvalidatePattern
      via: "system.InvalidateCacheByPattern(...) → base.InvalidatePattern(...)，provider 实参 system.NewCacheProvider(c.DataCacheService) 保留"
      pattern: "base\\.InvalidatePattern\\(context\\.Background"
    - from: duty/knowledge/network/workorder cache_impl (19 处)
      to: base.Invalidate / base.InvalidatePattern
      via: "systemServices.InvalidateCacheByXxx(ctx, s.cache, ...) → base.InvalidateXxx(ctx, s.cache, ...) + import base"
      pattern: "base\\.Invalidate"
    - from: data_cache_service.go GetExpiration
      to: base.CacheServiceBase.GetExpiration
      via: "组合字面量委托 base.CacheServiceBase{Config: s.cacheConfig}.GetExpiration(...)（签名不变，nil-cacheConfig 路径经 nil-receiver 防护语义等价）"
      pattern: "base\\.CacheServiceBase\\{Config: s\\.cacheConfig\\}"
---

<objective>
完成缓存统一的 operations 侧与收尾侧：floor 3 处样板迁移、CacheInvalidator 底层委托改造、D-04 涟漪 21 处外围改写并删除 system.InvalidateCacheByPattern/ByKey 实现唯一失效底层、DataCacheService 原地定性（GetExpiration 委托 + D-07 定位注释）。

Purpose: CACHE-UNIFY-03 + CACHE-UNIFY-04 代码面达成；删除动作与最后一个调用点改写同 commit（D-04 原子性，编译器当 checklist）。

Output: operations 迁移完成 + 失效底层归一（旧函数删除）+ root 定性注释落地 + 全 services 测试 0 回归。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/92-p2/92-CONTEXT.md
@.planning/workstreams/milestone/phases/92-p2/92-PATTERNS.md
@.planning/workstreams/milestone/phases/92-p2/92-02-SUMMARY.md

# 迁移与改造对象
@internal/services/operations/floor_cache_impl.go
@internal/services/operations/cache_invalidator.go
@internal/services/data_cache_service.go

# D-04 涟漪外围（19 处机械改写）
@internal/services/duty/duty_cache_impl.go
@internal/services/network/cache_impl.go

<interfaces>
<!-- floor 跨包嵌入现状（alias 后零改动的关键验证点——struct/构造函数本 plan 零改动） -->

type floorCacheService struct {
    *floorService
    cache system.CacheProvider      // = base.CacheProvider（92-01 alias）
    system.CacheServiceBase              // = base.CacheServiceBase（92-01 alias）
}

<!-- 迁移靶点（92-01 产出，同 92-02） -->

base.GetOrSetJSON[T any](ctx, p CacheProvider, key string, ttl time.Duration, query func() (T, error)) (T, error)
base.Invalidate(ctx, p CacheProvider, keys []string, module string)
base.InvalidatePattern(ctx, p CacheProvider, patterns []string, module string)

<!-- CacheInvalidator 目标形态（D-04，分发语义保留 + 底层委托；成功 Debugf 随循环消失） -->

func (c *CacheInvalidator) InvalidateByEntityType(ctx context.Context, entityType string, patterns []string) error {
    if len(patterns) == 0 {
        logger.Debugf("[%s] 没有配置缓存清理模式", entityType)
        return nil
    }
    base.InvalidatePattern(ctx, c.cache, patterns, entityType) // nil 防护已内聚
    return nil
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: floor_cache_impl 迁移 + CacheInvalidator 底层委托（CACHE-UNIFY-03 / D-04）</name>
  <files>internal/services/operations/floor_cache_impl.go, internal/services/operations/cache_invalidator.go</files>
  <read_first>
    - internal/services/operations/floor_cache_impl.go（全文：跨包嵌入 struct :14-33；3 处 GetOrSet :43/:60/:179；2 处失效 :86/:92；:167-168 是注释行——grep 假阳性锚点，勿当调用点）
    - internal/services/operations/cache_invalidator.go（全文 68 行：struct/构造 :10-19 零改动；InvalidateByEntityType :21-48；InvalidateByPatterns :50-68）
    - .planning/workstreams/milestone/phases/92-p2/92-PATTERNS.md（floor 跨包嵌入样本节 + cache_invalidator 现状完整逻辑与目标形态节）
  </read_first>
  <action>
    floor_cache_impl.go：
    - :43 GetTree → base.GetOrSetJSON[[]FloorTreeNode]（内联键 "floor:tree" 逐字保留）
    - :60 GetFloorsByBuildingID → base.GetOrSetJSON[[]operations.OpsFloor]；**闭包内直查 db 的查询体逐字搬入 query 闭包原样保留**（非委托 floorService 的特殊站点，PATTERNS 已给完整现状）；内联键 fmt.Sprintf("floor:building:%s", buildingID) 逐字不动（Redis 数据兼容）
    - :179 SearchFloorOptions → 按其现状 dest/闭包形态常规迁移（注意 name=="" 走缓存、keyword 绕过缓存的分支结构保持）
    - :86/:92 失效调用 → base.Invalidate / base.InvalidatePattern（module 常量 "FLOOR" 与 pattern "floor:*" 逐字保留）；import 增加 base，import system 仍需保留（struct 嵌入字段类型引用）
    - struct 定义与 NewFloorServiceWithCache 构造函数零改动（跨包 alias 兼容实证点）

    cache_invalidator.go：
    - InvalidateByEntityType（:21-48）：保留 len(patterns)==0 的 Debugf 早退分支；删除 c.cache == nil 分支与 for 循环（nil 防护与循环已内聚于 base.InvalidatePattern）；调用 base.InvalidatePattern(ctx, c.cache, patterns, entityType) 后 return nil。**struct 定义与构造函数（:10-19）零改动**（excel_service 消费其 entityType 分发，D-04 保留分发器）
    - InvalidateByPatterns（:50-68）同构委托
    - import 增加 base；循环内的成功 Debugf（"清除缓存成功"）随循环消失（D-04 锁定目标形态的一部分，SUMMARY 记录日志面变化）
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/services/operations/ -run "Cache|Floor" -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "GetOrSet(ctx" internal/services/operations/floor_cache_impl.go == 0 且 grep -c "base.GetOrSetJSON" == 3
    - grep -c "InvalidateCacheByKey\|InvalidateCacheByPattern" internal/services/operations/floor_cache_impl.go == 0 且 grep -c "base.Invalidate" == 2
    - floor:tree 与 floor:building:%s 键构造 git diff 零字符变更
    - grep -c "DeleteByPattern" internal/services/operations/cache_invalidator.go == 0（循环已委托）且 grep -c "base.InvalidatePattern" == 2
    - grep -c "type CacheInvalidator struct" internal/services/operations/cache_invalidator.go == 1（struct 保留）
    - go test ./internal/services/operations/ 全绿（cache_invalidator_test.go + geocoding_photo_floor_test.go 回归）
  </acceptance_criteria>
  <done>CACHE-UNIFY-03 代码面达成：operations 3 处样板 + 失效分发器委托完成，Excel 管道语义保留</done>
</task>

<task type="auto">
  <name>Task 2: D-04 涟漪外围 19 处改写 + 删除 InvalidateCacheByPattern/ByKey（编译器驱动收尾）</name>
  <files>internal/core/core.go, internal/services/duty/duty_cache_impl.go, internal/services/knowledge/knowledge_cache_impl.go, internal/services/network/cache_impl.go, internal/services/workorder/workorder_cache_impl.go, internal/services/system/cache_utils.go</files>
  <read_first>
    - internal/core/core.go :356-372（唯一 core 侧调用：system.InvalidateCacheByPattern(context.Background(), system.NewCacheProvider(c.DataCacheService), []string{...menu keys...}, ...)）
    - internal/services/duty/duty_cache_impl.go :295-330（5 处 :300/:307/:314/:321/:328）
    - internal/services/knowledge/knowledge_cache_impl.go :245-275（4 处 :252/:259/:266/:273）
    - internal/services/network/cache_impl.go :315-350（5 处 :321/:328/:335/:342/:348）
    - internal/services/workorder/workorder_cache_impl.go :255-282（4 处 :259/:266/:273/:279）
    - internal/services/system/cache_utils.go（92-01 后现状：InvalidateCacheByPattern/ByKey 带"92-03 待删除"注释）
    - .planning/workstreams/milestone/phases/92-p2/92-PATTERNS.md（"跨包失效调用涟漪"19 处 file:line 表 + 改写模式）
  </read_first>
  <action>
    第一步——19 处机械改写（每处 1 行 + 各文件 import 增加 base）：
    - duty/knowledge/network/workorder：systemServices.InvalidateCacheByKey(ctx, s.cache, X, "MODULE") → base.Invalidate(ctx, s.cache, X, "MODULE")；systemServices.InvalidateCacheByPattern(...) → base.InvalidatePattern(...)。实参、module 常量（"DUTY"/"KNOWLEDGE"/"NETWORK_DEVICE"/"WORKORDER"）、pattern 字符串（如 "workorder:*"/"network_device:*"）逐字保留。这 4 个文件的 GetOrSet 样板**不迁**（A5：范围外 12 处留 v1.30+，scope constrainment 纪律——只改编译器强制的失效调用）
    - core.go :366：system.InvalidateCacheByPattern(context.Background(), system.NewCacheProvider(c.DataCacheService), patterns, module) → base.InvalidatePattern(同实参原样)。core.go 的 system import 是否可删视同文件其他 system.* 引用而定（CacheKeyMenuTree 等 → 保留）
    第二步——删除：system/cache_utils.go 删除 InvalidateCacheByPattern 与 InvalidateCacheByKey 两函数（含"待删除"注释）。
    第三步——编译器证明：go build ./... 必须零错误。若报 undefined: xxx.InvalidateCacheByXxx 说明还有漏网调用点，按报错继续改写（这就是 D-04"编译器当 checklist"的原子性机制——删除与最后调用点改写必须落在同一 commit）。
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/services/duty/ ./internal/services/knowledge/ ./internal/services/network/ ./internal/services/workorder/ && go test ./internal/services/system/</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "InvalidateCacheByPattern\|InvalidateCacheByKey" internal/ --include="*.go" | grep -v _test | grep -v "\.planning" 仅允许命中：无（生产代码 0 处；测试文件若引用旧函数属既有测试锁定面，如 duty_cache_impl_test.go 注释行——注释/文档行不算调用，代码引用需为 0）
    - grep -c "func InvalidateCacheByPattern\|func InvalidateCacheByKey" internal/services/system/cache_utils.go == 0
    - 5 个外围文件 grep -c "base.Invalidate" 合计 == 19（duty 5 + knowledge 4 + network 5 + workorder 4 + core 1）
    - go build ./... 0 错误；duty/knowledge/network/workorder/system 五包测试全绿
  </acceptance_criteria>
  <done>D-04 完全达成：base 是唯一失效底层，旧函数已删，19 处外围调用编译器验证零遗漏</done>
</task>

<task type="auto">
  <name>Task 3: DataCacheService 原地定性（D-06 GetExpiration 委托 + D-07 定位注释）</name>
  <files>internal/services/data_cache_service.go</files>
  <read_first>
    - internal/services/data_cache_service.go（文件头 P2-A2 注释块 :16-36——D-07 注释落点；GetExpiration :54-60——委托改造对象；P0 #9 锚点 :99-107 不动）
    - internal/services/data_cache_service_79_01_test.go :351-359 与 internal/services/cache_config_service_79_01_test.go :225-237（锁定 GetExpiration 行为的 2 个既有测试——委托实现必须保持其绿）
    - .planning/workstreams/milestone/phases/92-p2/92-PATTERNS.md（data_cache_service 节：D-06 处置 A3 + 文件头定位注释先例格式）
  </read_first>
  <action>
    (1) GetExpiration（:54-60）方法体改为委托 base：return base.CacheServiceBase{Config: s.cacheConfig}.GetExpiration(configKey, defaultExpiration)。**方法签名保留不删**（A3：2 个测试锁定现行为；委托实现天然保持绿——s.cacheConfig 为 nil 时组合字面量装入 typed-nil，由 92-01 的 nil-receiver 防护返回 default，与现状 nil 判空语义完全等价）。import 增加 base（root→base 方向，实证无环）。
    (2) 文件头（紧邻既有 P2-A2 缓存键迁移说明块之后）新增 D-07 双定位注释块，格式照抄 P2-A2 块的 ==== 分隔风格，内容锚点：① 本文件是 pkg/cache.Cache 的业务封装层（GetOrSet/JSON 序列化/统计）；② 是 base.CacheProvider 接口的实现底座（经 system.NewCacheProvider 适配，Adaptee）；③ **新增业务缓存代码应走 base.GetOrSetJSON + base.CacheProvider，不应直接新增 DataCacheService 调用点**（D-07：不标 @Deprecated，以定位注释代替——12+ API 文件 import 面零改动承诺）。
    (3) 除上述两处外本文件零改动（P0 #9 同步写、Get/Set/GetOrSet 主体、统计方法全部不动——D-06"原地定性"边界）。
  </action>
  <verify>
    <automated>go test ./internal/services/ -run "Dcs7901|Ccs7901" -v && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - grep -A3 "func (s \*DataCacheService) GetExpiration" internal/services/data_cache_service.go 包含 "base.CacheServiceBase{Config: s.cacheConfig}"
    - grep -c "base.CacheServiceBase" internal/services/data_cache_service.go == 1
    - 文件头含 D-07 定位块：grep -c "base.CacheProvider" internal/services/data_cache_service.go >= 1（注释内）
    - go test ./internal/services/ -run "Dcs7901|Ccs7901" 全绿（行为锁定测试零回归）
    - git diff internal/services/data_cache_service.go 仅含 :54-60 方法体 + 文件头注释块（P0 #9 段零 diff）
  </acceptance_criteria>
  <done>CACHE-UNIFY-04 代码面达成：DataCacheService 原地定性（平行 GetExpiration 逻辑消除、定位注释落地、装配链零改动）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Excel 导入管道 → 缓存失效 | CacheInvalidator 分发 entityType patterns 后经 base 底层清 Redis（失效覆盖面不得收窄） |
| core 启动序列 → Redis | core.go:366 菜单缓存失效（迁移配套逻辑，Phase 70 D-11 遗产） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01 | Tampering | 缓存键构造（floor 内联键 :55 等） | mitigate | floor:building:%s / floor:tree 键 git diff 级零变更（Task 1 acceptance 硬项）；Redis 现存键继续命中 |
| T-92-02 | Tampering | 失效遗漏 → 陈旧数据窗口 | mitigate | 19+2 处改写为机械改名（实参逐字保留）+ 删除同 commit 编译器兜底（Task 2 三步法）；duty/knowledge/network/workorder/operations 既有失效测试全量回归（cache_invalidator_test.go 等）；base 失效行为 92-01 miniredis 锁定 |
| T-92-03 | DoS | core.go 启动期菜单失效（:366）语义弱化 | mitigate | 实参原样搬移不改 patterns 列表；go test ./internal/core/ 回归 |
| T-92-SC | Tampering | 依赖安装 | accept | 零新依赖 |
</threat_model>

<verification>
- go build ./... 0 错误（Task 2 的编译器证明是该 task 的核心 verify）
- go test ./internal/services/... 全绿（SC-4 口径，本 plan 收尾跑一次全量）
- grep -rn "InvalidateCacheByPattern\|InvalidateCacheByKey" internal/ --include="*.go" 生产代码 0 处
- grep -rn "GetOrSet(ctx" internal/services/system/ internal/services/operations/ 生产代码 0 处
- operlog / status AST 锁值防线保持绿（go test ./internal/utils/operlog/ ./internal/models/）
</verification>

<success_criteria>
1. operations 3 处真实样板迁移完成（:168 注释假阳性未被误迁）
2. CacheInvalidator 保留分发语义、底层委托 base，excel_service 消费链零改动
3. system.InvalidateCacheByPattern/ByKey 删除，21 处本 plan 改写 + 21 处 92-02 改写 = 42 处全部收敛 base
4. DataCacheService 原地定性：GetExpiration 委托 + D-07 定位注释，2 个行为锁定测试绿
5. go test ./internal/services/... 0 失败
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/92-p2/92-03-SUMMARY.md` when done
</output>
