---
phase: 76-test-doubles
plan: 04
type: execute
wave: 2
depends_on: ["76-01"]
files_modified:
  - internal/agent/server/subprocess_stub_test.go
  - internal/agent/server/subprocess_pgroup_test.go
autonomous: true
requirements: [INFRA-04]
must_haves:
  truths:
    - "TestHelperProcess 守卫在函数第一行（GO_WANT_HELPER_PROCESS != \"1\" 即 return），正常 go test 跑到时静默通过，每个形态分支必达 os.Exit"
    - "subprocess_pgroup_test.go 中 grep 不到 \"echo\" 实参（Windows/CI 平台分歧根源清零）"
    - "runCommand/runCommandOutput 的生产覆盖保留（经父进程环境继承驱动 re-exec，非绕开生产函数）"
    - "internal/agent/server 包测试 Windows 本地绿（re-exec 跨平台：os.Args[0] 即测试 exe）"
  artifacts:
    - path: "internal/agent/server/subprocess_stub_test.go"
      provides: "TestHelperProcess 集中式 helper（多形态分支）+ 形态自验测试"
      min_lines: 80
    - path: "internal/agent/server/subprocess_pgroup_test.go"
      provides: "5 处 echo 全部替换为 re-exec（两组改法）"
  key_links:
    - from: "subprocess_pgroup_test.go 测试侧构造"
      to: "TestHelperProcess"
      via: "exec 测试二进制自身（os.Args[0] + -test.run=^TestHelperProcess$ + -- 形态参数）+ GO_WANT_HELPER_PROCESS=1"
      pattern: "GO_WANT_HELPER_PROCESS"
---

<objective>
INFRA-04 落地：以测试二进制自身重执行（TestHelperProcess re-exec，Go stdlib 官方模式，GOROOT src/os/os_test.go:2475-2516 先例）替换 subprocess_pgroup_test.go 的 5 处 exec.Command("echo")，根除 Windows/CI 平台分歧根源（echo 是 cmd.exe 内建，Windows 上依赖 PATH 里恰有 Git Bash 的 echo.exe 才碰巧能跑）。

Purpose: Phase 77 BLOCK-02（agent/server ≥70%，77-04/77-05）与 Phase 78 BLOCK-03（core reaper）直接沿用本 helper 模式；本 plan 交付集中式 helper + 全部既有 echo 替换。
Output: 1 个新 helper 测试文件 + 1 个既有测试文件改写。生产代码（subprocess.go / sysproc_*.go）零改动。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/76-test-doubles/76-RESEARCH.md
@.planning/workstreams/milestone/phases/76-test-doubles/76-PATTERNS.md
@.planning/workstreams/milestone/phases/76-test-doubles/76-01-SUMMARY.md

<interfaces>
<!-- 执行者需要的既有契约（已本地复核全文） -->

From internal/agent/server/subprocess.go（生产代码，本 plan 零改动）:
```go
func runCommand(ctx context.Context, name string, args ...string) error      // :13-17，exec.CommandContext + setProcessGroup + cmd.Run
func runCommandOutput(ctx context.Context, name string, args ...string) ([]byte, error)  // :20-24，同上 + cmd.Output
func newCommand(ctx context.Context, name string, args ...string) *exec.Cmd  // :28-32，返回 Cmd 由调用方控制
// 关键实证：三个函数均不设置 cmd.Env —— exec.Cmd 零值 Env 继承父进程环境
// → 组 B（:29/:40/:58）可用 t.Setenv("GO_WANT_HELPER_PROCESS", "1") 经父环境继承驱动 helper，保住生产函数覆盖
```

From internal/agent/server/subprocess_pgroup_test.go（5 处替换点，两组改法——pattern-mapper 纠偏 #3）:
```go
:13  cmd := newCommand(ctx, "echo", "hello")                 // 组 A：仅构造不执行（检查 SysProcAttr）
:29  err := runCommand(ctx, "echo", "test")                  // 组 B：执行，测 runCommand 成功路径
:40  output, err := runCommandOutput(ctx, "echo", "hello")   // 组 B：执行，测输出捕获
:58  err = runCommand(ctx, "echo", "should not run")         // 组 B：预取消 ctx，容错断言
:69  cmd := newCommand(ctx, "echo", "test")                  // 组 A：仅构造（幂等检查）
```

From internal/agent/server/sysproc_windows.go / sysproc_linux.go（平台拆分哲学参照，零改动）:
```go
// windows: syscall.SysProcAttr{CreationFlags: syscall.CREATE_NEW_PROCESS_GROUP}
// linux:   syscall.SysProcAttr{Setpgid: true}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: subprocess_stub_test.go — TestHelperProcess helper + 形态自验</name>
  <files>internal/agent/server/subprocess_stub_test.go</files>
  <read_first>internal/agent/server/subprocess.go（全文）、internal/agent/server/subprocess_pgroup_test.go（全文）、internal/agent/server/sysproc_windows.go 与 sysproc_linux.go（平台拆分哲学）</read_first>
  <behavior>
  - 守卫：GO_WANT_HELPER_PROCESS != "1" 时 TestHelperProcess 立即 return，正常 `go test ./internal/agent/server/` 不挂起不污染输出
  - default 形态：子进程打印 "hello" 并 exit 0
  - stdout-flood 形态（"--stdout-flood"）：打印 1000 行后 exit 0
  - sleep-until-stdin-close 形态：io.Copy(io.Discard, os.Stdin)，stdin 关闭后 exit 0
  - ignore-sigterm 形态：仅 linux 有实义（syscall.SIGTERM 在 Windows 无实义），收到信号后打印 "still-alive"
  - 每个形态分支必达 os.Exit（防 testing 框架继续解析后续参数）
  </behavior>
  <action>
  新建 internal/agent/server/subprocess_stub_test.go（package server）。集中式 helper TestHelperProcess(t *testing.T)：第一行守卫 `if os.Getenv("GO_WANT_HELPER_PROCESS") != "1" { return }`；形态参数取 `os.Args[len(os.Args)-1:]`（"--" 之后不经 flag 解析）；switch 分支按 behavior 块四种形态（sleep-until-stdin-close / ignore-sigterm / stdout-flood / default 秒退打印 "hello"）；ignore-sigterm 分支用 runtime.GOOS == "linux" 守卫（非 linux 走 default），signal.Notify(SIGTERM) 后 <-sig 再 fmt.Println("still-alive")；所有分支收尾 os.Exit(0)。同文件写形态自验测试：TestSubprocessStub_Default（newCommand(ctx, os.Args[0], "-test.run=^TestHelperProcess$", "--", "hello") + cmd.Env = append(os.Environ(), "GO_WANT_HELPER_PROCESS=1") + cmd.Output() → 断言 Contains "hello"）；TestSubprocessStub_StdoutFlood（同上形态 stdout-flood → 断言输出行数 ≥1000 或 Contains 多行）；TestSubprocessStub_StdinClose（newCommand + StdinPipe() + Start + pipe.Close() → Wait err==nil，子进程随 stdin 关闭退出）；TestSubprocessStub_IgnoreSigterm 仅 runtime.GOOS == "linux" 执行、其余平台 t.Skip。测试侧构造纪律：`-test.run` 用 ^...$ 精确锚定（防前缀误匹配其它测试）；newCommand 已挂 setProcessGroup（sysproc 拆分保持不动）。全包禁 t.Parallel()（t.Setenv 与 helper 进程语义）。
  </action>
  <verify>
    <automated>go test -count=1 -run 'TestSubprocessStub' ./internal/agent/server/ -v && go test -count=1 ./internal/agent/server/</automated>
  </verify>
  <acceptance_criteria>
  - subprocess_stub_test.go 含 TestHelperProcess 函数且第一行为 GO_WANT_HELPER_PROCESS 守卫
  - `go test -count=1 ./internal/agent/server/` 不挂起（守卫有效的实证）且全绿
  - 四种形态分支各可达 os.Exit；ignore-sigterm 分支有 runtime.GOOS 守卫
  - 形态自验测试断言 default 输出含 "hello"、stdin 关闭后 Wait 返回 nil
  </acceptance_criteria>
  <done>集中式 re-exec helper 落地并经形态自验，正常 go test 静默通过</done>
</task>

<task type="auto">
  <name>Task 2: subprocess_pgroup_test.go 5 处 echo 替换（组 A/组 B 两组改法）</name>
  <files>internal/agent/server/subprocess_pgroup_test.go</files>
  <read_first>internal/agent/server/subprocess_pgroup_test.go（Task 1 后的同包状态）、internal/agent/server/subprocess_stub_test.go（helper 形态）、internal/agent/server/subprocess.go（runCommand/runCommandOutput/newCommand 的 Env 语义）</read_first>
  <action>
  按两组分别改法（pattern-mapper 纠偏 #3，依据：newCommand 返回 *exec.Cmd 可直接设 cmd.Env；runCommand/runCommandOutput 内部自建 cmd 无法设 Env，但生产函数不设 cmd.Env → 零值继承父进程环境）。**组 A（:13 与 :69，仅构造不执行）**：`newCommand(ctx, "echo", ...)` 改为 `newCommand(ctx, os.Args[0], "-test.run=^TestHelperProcess$", "--", "hello")`（":69" 处形态参数用 "test"）——不启动进程、无需 env，断言语义不变（SysProcAttr 检查）。**组 B（:29 / :40 / :58，经生产函数执行）**：每个测试开头 `t.Setenv("GO_WANT_HELPER_PROCESS", "1")`（父进程环境，t 自动 Cleanup 恢复；全包禁 t.Parallel()），再改调用：:29 `runCommand(ctx, os.Args[0], "-test.run=^TestHelperProcess$", "--", "hello")` 断言 err==nil（helper default 分支 exit 0）；:40 同参数走 runCommandOutput，断言 string(output) Contains "hello"（强于原 non-empty 断言）；:58 预取消 ctx 形态改为 runCommand(ctx, os.Args[0], "-test.run=^TestHelperProcess$", "--", "should-not-run")，保留容错断言（不 panic 不挂起，err 两种取值均可，t.Log 说明）。全部替换后 `grep -n '"echo"' subprocess_pgroup_test.go` 必须零命中。生产文件 subprocess.go / sysproc_windows.go / sysproc_linux.go 零改动。Windows 细节提醒：os.Args[0] 即测试 exe；`-test.run` 锚定防误匹配；t.Setenv 期间框架不会并发跑 TestHelperProcess 本体（无 Parallel）。
  </action>
  <verify>
    <automated>! grep -n '"echo"' internal/agent/server/subprocess_pgroup_test.go && go test -count=1 ./internal/agent/server/ -v && git diff --quiet HEAD -- internal/agent/server/subprocess.go</automated>
  </verify>
  <acceptance_criteria>
  - `grep -n '"echo"' internal/agent/server/subprocess_pgroup_test.go` 零命中
  - `git diff internal/agent/server/subprocess.go` 为空（生产零改动）
  - 5 处替换点：组 A 两处经 newCommand + cmd 构造、组 B 三处经 t.Setenv + runCommand/runCommandOutput（保留生产函数覆盖）
  - `go test -count=1 ./internal/agent/server/` 全绿（含既有 TestSubprocess_* 断言语义保持）
  </acceptance_criteria>
  <done>echo 分歧根源清零：5 处 stub 全部 re-exec 化，runCommand/runCommandOutput 生产覆盖保留，Windows 本地绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试父进程 ↔ re-exec 子进程 | 子进程 = 测试二进制自身，经 GO_WANT_HELPER_PROCESS env + "-test.run" 参数区分形态 |
| 环境变量泄漏面 | GO_WANT_HELPER_PROCESS 仅测试进程内约定，不进 .env / CI secrets / 生产配置 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-04-01 | DoS（正常 go test 挂起） | TestHelperProcess 守卫缺失 | mitigate | 守卫第一行 + 每分支必达 os.Exit 的双纪律，Task 1 验收含"不挂起"实证 |
| T-76-04-02 | Tampering（生产代码波及） | subprocess.go | mitigate | 验收强制 git diff subprocess.go 为空；组 B 走父环境继承而非改生产签名 |
| T-76-04-03 | Elevation（env 变量串扰） | t.Setenv 作用域 | mitigate | t.Setenv 自动 Cleanup 恢复 + 全包禁 t.Parallel()（串扰唯一入口）；变量无安全语义 |
| T-76-04-04 | Information Disclosure | 子进程输出 | accept | 输出仅 "hello"/"line"/"still-alive" 字面量，无敏感数据 |
</threat_model>

<verification>
- `grep -n '"echo"' internal/agent/server/subprocess_pgroup_test.go` 零命中
- `git diff --quiet HEAD -- internal/agent/server/subprocess.go internal/agent/server/sysproc_windows.go internal/agent/server/sysproc_linux.go` exit 0（生产三文件零改动）
- `go test -count=1 ./internal/agent/server/` 全绿（Windows 本地）
- `go test -race -count=1 ./internal/agent/server/`（可选抽查 helper 进程并发安全）
- gate 不倒退：`bash scripts/check-ci-local.sh backend`
</verification>

<success_criteria>
- INFRA-04 完成：helper 落地 + echo 清零 + 生产覆盖保留 + Windows/CI 同构（stub = 测试二进制自身，任何环境语义一致）
- Phase 77（77-04/77-05 agent 测试）与 Phase 78（core reaper）可直接复用本 helper 四形态
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/76-test-doubles/76-04-SUMMARY.md` when done
</output>
