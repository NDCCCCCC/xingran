---
phase: 76-test-doubles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - pkg/cache/redis_miniredis_76_01_test.go
  - pkg/cache/cache_74_08_test.go
  - internal/services/operations/geocoding_httpmock_76_01_test.go
autonomous: true
requirements: [INFRA-01]
must_haves:
  truths:
    - "go.mod 含 miniredis/v2 v2.38.0 与 httpmock v1.4.2 两行 direct require，行尾均带 `// test-only (v1.27 D-02)` 注释，生产 require 行零变更"
    - "pkg/cache 的 Redis 命令面经 NewRedisCache→miniredis 真链路冒烟全绿，R-1（FastForward）与 R-2（INFO 降级断言）各有具名用例"
    - "geocoding httpmock PoC 经 NewGeocodingService 构造（Transport==nil 走 DefaultTransport）+ httpmock.Activate(t) 拦截并断言坐标"
    - "go mod tidy 之后 go.mod 仍含 httpmock 行（tidy 保活）"
    - "go test ./... 全绿且全程零 Docker"
  artifacts:
    - path: "pkg/cache/redis_miniredis_76_01_test.go"
      provides: "miniredis 冒烟 + 三坑防护用例（R-1/R-2/R-3）"
      min_lines: 80
    - path: "internal/services/operations/geocoding_httpmock_76_01_test.go"
      provides: "httpmock PoC + tidy 保活 + 使用纪律活样板"
      min_lines: 40
  key_links:
    - from: "pkg/cache/redis_miniredis_76_01_test.go newMiniredisCache(t)"
      to: "pkg/cache/redis.go NewRedisCache"
      via: "miniredis.RunT(t).Addr() 拆 Host/Port（构造器内 PING 即 R-3 握手实证）"
      pattern: "RunT.*NewRedisCache"
    - from: "internal/services/operations/geocoding_httpmock_76_01_test.go"
      to: "internal/services/operations/geocoding_service.go baiduGeocodingAPIURL"
      via: "httpmock.Activate(t) 拦截 DefaultTransport + RegisterResponder"
      pattern: "httpmock\\.Activate\\(t\\)"
---

<objective>
INFRA-01 落地：引入 miniredis/v2 v2.38.0 与 httpmock v1.4.2 两个 test-only 依赖（v1.27 D-02 用户锁定决策），并以 pkg/cache miniredis 冒烟（含三坑防护用例）与 geocoding httpmock PoC 完成真实使用锚定（防 tidy 丢弃）。

Purpose: 这是整个 v1.27 milestone 基建解锁的第一块——Phase 78（core Init 链 / pkg/cache Redis 路径）与 Phase 80（pkg/cache ≥70%）全部依赖本 plan 交付的 miniredis 模式；httpmock 专治 geocoding 类 const-URL 无注入点场景。
Output: go.mod/go.sum 变更 + 2 个新测试文件 + 1 处 doc-only 注释更新。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/phases/76-test-doubles/76-RESEARCH.md
@.planning/workstreams/milestone/phases/76-test-doubles/76-PATTERNS.md

<interfaces>
<!-- 执行者需要的既有契约（已本地复核） -->

From pkg/cache/redis.go:
```go
func NewRedisCache(config *CacheConfig, keyPrefix string) (*RedisCache, error)   // :30，构造器内 rdb.Ping（R-3 冒烟点）
func (r *RedisCache) Get(ctx context.Context, key string) (string, error)        // :76，redis.Nil → ErrNotFound
func (r *RedisCache) Set(ctx context.Context, key string, value interface{}, expiration time.Duration) error  // :85
func (r *RedisCache) Increment(ctx context.Context, key string) (int64, error)   // :165
func (r *RedisCache) IncrementBy(ctx context.Context, key string, value int64) (int64, error)  // :170
func (r *RedisCache) IncrementWithExpire(ctx context.Context, key string, expire time.Duration) (int64, error)  // :186（EVAL/Lua）
func (r *RedisCache) Expire(ctx context.Context, key string, expiration time.Duration) error  // :207
func (r *RedisCache) TTL(ctx context.Context, key string) (time.Duration, error)  // :212
func (r *RedisCache) Keys(ctx context.Context, pattern string) ([]string, error)  // :238（内部 SCAN）
func (r *RedisCache) HSet(ctx context.Context, key, field string, value interface{}) error  // :393
func (r *RedisCache) HGetAll(ctx context.Context, key string) (map[string]string, error)  // :398
func (r *RedisCache) GetStats(ctx context.Context) (map[string]interface{}, error)  // :427（INFO×4 段）
```

From pkg/cache/cache.go（:81-88）:
```go
type CacheConfig struct {  // 字段含 Type/Host/Port/Password/DB/PoolSize/TLS —— 冒烟只需 Host+Port
```

From pkg/cache/errors.go:
```go
var ErrNotFound = errors.New("缓存项不存在")   // :7；过期删除断言用 errors.Is(err, ErrNotFound)
```

From internal/services/operations/geocoding_service.go:
```go
const baiduGeocodingAPIURL = "https://api.map.baidu.com/geocoding/v3/"   // :24（const，无注入点 → httpmock 的用武之地）
func NewGeocodingService(apiKey string) *GeocodingService   // :73 —— httpClient = &http.Client{Timeout:...}，Transport==nil → DefaultTransport
func (s *GeocodingService) Geocode(ctx context.Context, address string) (lng, lat float64, err error)  // :98
```

From internal/services/operations/geocoding_photo_floor_test.go（analog）:
```go
const geocodeOKBody = `{"status":0,"result":{"location":{"lng":116.404,"lat":39.915},"formatted_address":"北京市东城区","level":"城市"}}`
// 断言风格：require.NoError + assert.InDelta(lng/lat, 期望, 容差)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: go.mod 落地两个 test-only 依赖（带注释 + diff 形态核验）</name>
  <files>go.mod, go.sum</files>
  <read_first>go.mod, .planning/workstreams/milestone/phases/76-test-doubles/76-RESEARCH.md（§Standard Stack + §Package Legitimacy Audit）</read_first>
  <action>
  执行精确版本安装（禁止 @latest 浮动）：先 `go get github.com/alicebob/miniredis/v2@v2.38.0`，再 `go get github.com/jarcoal/httpmock@v1.4.2`。然后手工编辑 go.mod，在两个新 direct require 行尾追加注释 `// test-only (v1.27 D-02)`（go get 不会自动带注释，必须手工追加；注释以两空格分隔版本号）。预期 diff 形态（SUMMARY 首段必须预写，防评审误判违反 D-02）：direct +2（miniredis/v2、httpmock，均带注释）；indirect +2（github.com/yuin/gopher-lua、github.com/maxatome/go-testdeep，模块解析必然产物）；既有生产 require 行零变更。明令禁止引入 redismock（锁死 go-redis v8）、testcontainers（强制 Docker）、gock（休眠）。
  </action>
  <verify>
    <automated>grep -c "// test-only (v1.27 D-02)" go.mod && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - go.mod 包含行 `github.com/alicebob/miniredis/v2 v2.38.0 // test-only (v1.27 D-02)`
  - go.mod 包含行 `github.com/jarcoal/httpmock v1.4.2 // test-only (v1.27 D-02)`
  - `git diff go.mod` 中除上述 2 个 direct 行与 gopher-lua/go-testdeep 两个 indirect 行外，零其他变更
  - `go build ./...` exit 0
  </acceptance_criteria>
  <done>两个 pinned test-only 依赖落地，diff 形态与预告完全一致，构建通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pkg/cache miniredis 冒烟（三坑防护）+ cache_74_08 过期注释联动更新</name>
  <files>pkg/cache/redis_miniredis_76_01_test.go, pkg/cache/cache_74_08_test.go</files>
  <read_first>pkg/cache/redis.go（重点 :30-66 构造器 / :68-73 buildKey / :76-231 命令面 / :388-486 Hash+GetStats）、pkg/cache/cache.go（:81-88 CacheConfig）、pkg/cache/errors.go、pkg/cache/cache_74_08_test.go（同包测试风格 + :15 过期注释）、.planning/workstreams/milestone/phases/76-test-doubles/76-RESEARCH.md §Pattern 1</read_first>
  <behavior>
  - R-1（TTL）：Set(key) + Expire(ctx, key, 10*time.Second) → TTL(ctx, key) > 0 → mr.FastForward(11*time.Second) → Get 返回 ErrNotFound
  - R-2（INFO 降级）：GetStats(ctx) 只断言 err==nil 且 stats["key_count"] 非 nil 且 stats["keyspace_info"] 非空；不断言 redis_version/used_memory/hit_rate 具体值（miniredis INFO 仅含 connected_clients 段）
  - R-3（SETINFO 兼容）：不写专门用例——newMiniredisCache 内 NewRedisCache 的 PING 已走完 go-redis v9.7.0 完整握手（HELLO→CLIENT SETINFO×2），helper 本身即回归哨兵
  - EVAL：IncrementWithExpire(ctx, key, 30*time.Second) 返回 1 且 TTL > 0（gopher-lua 驱动）
  </behavior>
  <action>
  新建 pkg/cache/redis_miniredis_76_01_test.go（package cache，同包白盒）。Helper newMiniredisCache(t *testing.T) *RedisCache：t.Helper() + miniredis.RunT(t) + net.SplitHostPort(mr.Addr()) + strconv.Atoi(port) + NewRedisCache(&CacheConfig{Host: host, Port: p}, "xingran")，err 非 nil 时 t.Fatalf。命令面冒烟（测试函数统一 TestRedis 前缀，便于 -run 过滤）：Set/Get/Exists/Delete、Increment/IncrementBy、MSet/MGet、HSet/HGet/HGetAll/HDel/HKeys、Keys（SCAN 路径）。三坑防护按 behavior 块落地，R-1 与 R-2 为两个独立具名测试 TestRedisTTLFastForward / TestRedisGetStatsDegraded。纪律红线：禁止 time.Sleep 等 TTL 过期（一律 mr.FastForward）；禁止出现 DisableIdentity 或 DisableIndentity 字样（v9.7.0 上游 typo 且根本不需要）；断言注意 buildKey 已自动加 xingran: 前缀（经 cache 方法断言无感，直查 mr 时需带前缀）；不要写 SCAN 分批大小断言（miniredis 游标一次归零）。同 commit 顺带 doc-only 更新：cache_74_08_test.go:15 的注释「Redis 实现依赖真实 Redis,不在单测范围(D-12 禁加 miniredis 依赖)」已因 v1.27 D-02 解禁而失实，改写为指向 redis_miniredis_76_01_test.go 的一句话（pattern-mapper 纠偏 #2，防评审困惑）。
  </action>
  <verify>
    <automated>go test -count=1 ./pkg/cache/ && grep -n "redis_miniredis_76_01_test.go" pkg/cache/cache_74_08_test.go</automated>
  </verify>
  <acceptance_criteria>
  - redis_miniredis_76_01_test.go 含 TestRedisTTLFastForward 与 TestRedisGetStatsDegraded 两个具名测试函数
  - 该文件 grep 不到 "time.Sleep" 与 "DisableIdent"
  - cache_74_08_test.go 第 15 行区域注释包含 "redis_miniredis_76_01_test.go" 字样
  - `go test -count=1 ./pkg/cache/` 全绿（含既有 15 个测试，零回归）
  </acceptance_criteria>
  <done>miniredis 冒烟全绿覆盖 pkg/cache 命令面，三坑防护用例落地，过期注释已联动更新</done>
</task>

<task type="auto">
  <name>Task 3: geocoding httpmock PoC（tidy 保活）+ 全量绿</name>
  <files>internal/services/operations/geocoding_httpmock_76_01_test.go</files>
  <read_first>internal/services/operations/geocoding_service.go（:24 const / :73-82 NewGeocodingService / :98 Geocode / :303 callGeocodingAPI）、internal/services/operations/geocoding_photo_floor_test.go（analog：geocodeOKBody 常量 + InDelta 断言风格）、.planning/workstreams/milestone/phases/76-test-doubles/76-PATTERNS.md §76-01b</read_first>
  <action>
  新建 internal/services/operations/geocoding_httpmock_76_01_test.go（package operations，同包）。**纪律核心（pattern-mapper 纠偏 #4，违反则 PoC 无意义）**：用 NewGeocodingService("ak-test") 构造被测服务——其内部 httpClient 为 &http.Client{Timeout:...} 且 Transport==nil → 走 DefaultTransport → httpmock.Activate(t) 才能拦截；**禁止**白盒替换 svc.httpClient（geocoding_photo_floor_test.go 的 fakeGeocodeTransport 换法在本 PoC 是反模式）。流程：httpmock.Activate(t)（testing.TB 自动清理，不用 defer DeactivateAndRestore 旧形态）→ httpmock.RegisterResponder("GET", baiduGeocodingAPIURL, httpmock.NewStringResponder(200, geocodeOKBody))（注册 URL 不带 query 即可匹配任意 query）→ Geocode(ctx, "测试地址") → require.NoError + assert.InDelta(lng, 116.404, 0.001) + assert.InDelta(lat, 39.915, 0.001)。geocodeOKBody 常量从 analog 复制。单次 Geocode 调用无需动共享 BaiduAPIRateLimiter；若确需多次调用则同包白盒 svc.rateLimiter = NewRateLimiter(100, time.Hour)。文件头注释写明使用纪律：优先 Activate(t) 自动清理 + 凡 httpClient/baseURL 可注入场景一律优先 httptest（零依赖），httpmock 专治 const-URL 无注入点场景。收尾三连：`go mod tidy` → grep 验证 httpmock 行仍在 → 全量 `go test ./...` exit 0（零 Docker）。
  </action>
  <verify>
    <automated>go test -count=1 -run 'TestGeocoding' ./internal/services/operations/ && go mod tidy && grep -c "jarcoal/httpmock v1.4.2" go.mod && go test ./... </automated>
  </verify>
  <acceptance_criteria>
  - 测试文件含 httpmock.Activate(t) 与 RegisterResponder 且 URL 引用 baiduGeocodingAPIURL 常量
  - 测试文件 grep 不到 "svc.httpClient = "（白盒换 Transport 反模式）
  - go mod tidy 之后 go.mod 仍含 `github.com/jarcoal/httpmock v1.4.2 // test-only (v1.27 D-02)`
  - `go test ./...` exit 0，全程无 Docker 进程
  </acceptance_criteria>
  <done>httpmock 有真实 import 锚定（tidy 不丢），PoC 兼作全仓使用纪律活样板，全仓测试绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 供应链（go.mod 新增外部模块） | miniredis/httpmock 及其 indirect 依赖进入构建图（仅测试侧） |
| 测试进程内（miniredis TCP / httpmock Transport） | 无外部输入；fixture 全 dummy 值 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01-SC | Tampering（供应链投毒/typo squatting） | go.mod 依赖引入 | mitigate | 包名由 REQUIREMENTS INFRA-01 用户锁定（等价 CONTEXT Decisions 效力）+ 精确版本 pin v2.38.0/v1.4.2（禁 @latest）+ Package Legitimacy Audit 三重核实（module proxy / 官方 README / 本地 pinned 源码逐文件）；slopcheck 不可用已按协议降级标注 [ASSUMED-slopcheck]，幻觉风险实质消除 |
| T-76-01-02 | Information Disclosure（测试数据泄漏） | 冒烟/PoC 测试文件 | accept | 全部 dummy 值（"ak-test"/"测试地址"/示例坐标），无真实凭据/IP/密钥 |
| T-76-01-03 | Tampering（生产依赖漂移） | go.mod 生产 require 块 | mitigate | Task 1 验收强制 git diff go.mod 生产 require 行零变更；indirect 行为模块机制必然产物且已在 SUMMARY 预告 |
</threat_model>

<verification>
- `grep -c "// test-only (v1.27 D-02)" go.mod` 输出 2
- `go test -count=1 -run 'TestRedis' ./pkg/cache/` 全绿（含 TestRedisTTLFastForward / TestRedisGetStatsDegraded）
- `go test -count=1 -run 'TestGeocoding' ./internal/services/operations/` 全绿
- `go mod tidy` 后 httpmock 仍在 go.mod
- `go test ./...` exit 0（零 Docker）
- gate 不倒退：`bash scripts/check-ci-local.sh backend`（lint + 全量 + coverage gate 55.5 floor）
</verification>

<success_criteria>
- INFRA-01 的 go.mod 侧与冒烟侧全部落地：direct +2 带 test-only 注释、三坑防护具名用例、httpmock tidy 保活
- SUMMARY 首段预写 go.mod diff 形态（direct +2 / indirect +2 / 生产零变更），verifier 按此口径验收（RESEARCH Open Question 1 裁决）
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/76-test-doubles/76-01-SUMMARY.md` when done
</output>
