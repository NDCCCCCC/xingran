---
phase: 76-test-doubles
plan: 05
type: execute
wave: 3
depends_on: ["76-02", "76-03", "76-04"]
files_modified:
  - internal/device/for_testing_guard_test.go
autonomous: true
requirements: [INFRA-05]
must_haves:
  truths:
    - "生产 .go 文件（非 _test.go）中出现 *ForTesting 后缀符号的 CallExpr.Fun 或 SelectorExpr.Sel 引用时，守护测试 FAIL 并报 file:line"
    - "当前全仓扫描零违规（绿），且扫描跳过点前缀目录（.claude/worktrees 下有 6 份完整仓库拷贝，不跳过会误报）"
    - "扫描到 0 个 .go 文件时防呆 FAIL（防 cwd 漂移假绿）"
    - "注毒实验实证：临时在生产文件加 ForTesting 调用 → 守护 FAIL → 还原 → PASS（守护自证，毒株不进 commit）"
    - "三层隔离契约（编译器 _test.go 物理隔离 / ForTesting 命名 / AST 守护）在守护测试头注释成文"
  artifacts:
    - path: "internal/device/for_testing_guard_test.go"
      provides: "全仓 ForTesting 引用 AST 守护 + 三层契约成文"
      min_lines: 60
  key_links:
    - from: "for_testing_guard_test.go"
      to: "全仓生产 .go 文件"
      via: "filepath.WalkDir(\"../..\") + 目录 SkipDir + go/parser.ParseFile + ast.Inspect"
      pattern: "WalkDir"
    - from: "白名单"
      to: "internal/device/e2e_helpers.go"
      via: "filepath.ToSlash(path) == \"internal/device/e2e_helpers.go\" 精确匹配（定义文件自身合法）"
      pattern: "e2e_helpers\\.go"
---

<objective>
INFRA-05 落地：仿 status_constants_test.go 的 go/parser 范式，新建全仓 AST 守护测试——禁止生产 .go 文件引用 *ForTesting 后缀符号，把测试隔离契约从「编译器 + 命名」两层补齐为三层（+AST 兜底）。

Purpose: e2e_helpers.go 的 ForTesting 符号会跳过连接池簿记/设备锁/可达性检查/凭据解密——生产代码误用即静默绕过安全步骤，AST 守护是防呆底线。wave 3 排序使其在 76-02/03/04 的生产改动全部落地后验证全仓最终态。
Output: 1 个守护测试文件（internal/device 包共置，与 ForTesting 符号同包）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/76-test-doubles/76-RESEARCH.md
@.planning/workstreams/milestone/phases/76-test-doubles/76-PATTERNS.md
@.planning/workstreams/milestone/phases/76-test-doubles/76-02-SUMMARY.md

<interfaces>
<!-- 执行者需要的既有契约（已本地复核） -->

From internal/models/status_constants_test.go（AST 范式 analog）:
```go
// :289-348 readStatusConsts：
fset := token.NewFileSet()
f, err := parser.ParseFile(fset, file, nil, 0)
if err != nil { return nil, fmt.Errorf("parse %s: %w", file, err) }   // fail 而非 skip 的语义直接照搬
// :297-299 零匹配防呆：len(files) == 0 → 报错（防 cwd 漂移假绿）—— WalkDir 版必须同设"扫描 0 个 .go 文件即 Fail"
// 与 analog 的两处必须偏离：
//   1) 遍历：filepath.Glob（仅本包目录）→ filepath.WalkDir("../..") + 目录过滤
//   2) 检测：const 值锁定 → *ForTesting 后缀的 CallExpr.Fun / SelectorExpr.Sel 引用
```

From internal/device/e2e_helpers.go（白名单文件 + 契约文案范本）:
```go
// :10-31 头注释原文（三层契约成文范本）：
// "Production code MUST NOT call this function. The naming suffix `ForTesting` is
//  the physical isolation contract... Calling this in production silently skips
//  connection pool bookkeeping, device-level locking, reachability checks, and credential decryption"
func NewPooledConnectionForTesting(d *network.Driver) *PooledConnection  // :32
func newScrapliWrapperForTesting(d *network.Driver) *ScrapliWrapper      // :52
// 全仓 ForTesting 符号现仅此一文件（grep 实证）
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: for_testing_guard_test.go — 全仓 AST 守护实现</name>
  <files>internal/device/for_testing_guard_test.go</files>
  <read_first>internal/models/status_constants_test.go（:289-363 AST 范式与防呆语义）、internal/device/e2e_helpers.go（全文：白名单目标 + 契约文案范本）、internal/device/scrapli_wrapper.go（76-02 后状态，确认零 ForTesting 引用）</read_first>
  <action>
  新建 internal/device/for_testing_guard_test.go（package device）。测试 TestNoProductionForTestingReferences：扫描根用 "../.."（go test 的 cwd = 包目录 → 仓库根）；filepath.WalkDir 回调中目录名命中点前缀（strings.HasPrefix(name, ".")，顺带跳过 .git 与 .claude/worktrees 的 6 份仓库拷贝）、vendor、node_modules、xingran-react-frontend、testdata、tests 时返回 fs.SkipDir；文件过滤：仅 .go 后缀且排除 _test.go 结尾；白名单：filepath.ToSlash(path) == "internal/device/e2e_helpers.go" 跳过（定义文件自身内部引用合法）。解析：token.NewFileSet + parser.ParseFile(fset, path, nil, 0)，parse 失败 return fmt.Errorf（fail 而非 skip，照 analog 语义）。检测：ast.Inspect 内两个分支——*ast.CallExpr 的 Fun 为 *ast.Ident 且 strings.HasSuffix(id.Name, "ForTesting")（直接调用形态）、*ast.SelectorExpr 的 Sel.Name HasSuffix "ForTesting"（限定引用形态）——命中即向 violations 追加 "path: call to / reference to 符号名"（用 fset.Position(n.Pos()) 取 file:line）。FuncDecl 函数名是声明不是引用，天然不进两分支、不误报。收尾两道防线：violations 非空时逐条打印并 t.Fail；另设 scannedFiles 计数，==0 时 t.Fatal（防 cwd 漂移假绿，照 analog :297-299 防呆）。文件头注释按 e2e_helpers.go 契约文案成文三层隔离：(1) 编译器——_test.go 内符号生产物理不可 import；(2) 命名——跨包需触达私有字段时用 ForTesting 后缀生产符号；(3) AST——生产代码引用 ForTesting 即本测试失败。
  </action>
  <verify>
    <automated>go test -count=1 -run 'TestNoProductionForTestingReferences' ./internal/device/ -v && go test -count=1 ./internal/device/</automated>
  </verify>
  <acceptance_criteria>
  - for_testing_guard_test.go 含 TestNoProductionForTestingReferences，WalkDir 根为 "../.." 且目录过滤含点前缀 SkipDir
  - 白名单精确匹配 internal/device/e2e_helpers.go
  - 当前全仓扫描 PASS（零 violations），且 scannedFiles > 0
  - 头注释含三层契约说明（编译器/命名/AST）
  </acceptance_criteria>
  <done>AST 守护上线：全仓生产文件零 ForTesting 引用（绿），worktrees 拷贝被正确跳过</done>
</task>

<task type="auto">
  <name>Task 2: 守护自证注毒实验（FAIL→还原→PASS）</name>
  <files>internal/device/for_testing_guard_test.go</files>
  <read_first>internal/device/for_testing_guard_test.go（Task 1 后状态）、internal/device/e2e_helpers.go（:32/:52 两个 ForTesting 符号）</read_first>
  <action>
  注毒自证三步（毒株绝不进 commit）：(1) 临时在任一生产文件（建议 internal/device/connection_pool.go 尾部）追加两行注毒——`var _ = poisonCallForTesting()` 与 `func poisonCallForTesting() int { return 0 }`（CallExpr.Fun=Ident 后缀命中检测分支）；(2) 运行 `go test -count=1 -run TestNoProductionForTestingReferences ./internal/device/` → **必须 FAIL** 且错误输出含 connection_pool.go 的 file:line 与符号名 poisonCallForTesting（若 PASS 说明守护失效，回 Task 1 修检测逻辑）；(3) 还原注毒两行（git diff 确认零残留）→ 重跑守护测试 PASS。把三步实验结果（FAIL 输出摘录 + 还原后 PASS）记入 SUMMARY 作为守护自证证据。可选加一道限定引用形态自证：临时写 `var _ = device.NewPooledConnectionForTesting` 类 SelectorExpr 毒株重复三步（同文件内用本包符号即可：`var _ = NewPooledConnectionForTesting` 不行——裸 Ident 非 SelectorExpr，需包限定或调用形态；直接复用步骤 1 的调用形态已覆盖主分支即可）。
  </action>
  <verify>
    <automated>git diff --stat internal/device/connection_pool.go && go test -count=1 -run 'TestNoProductionForTestingReferences' ./internal/device/ && go test -count=1 ./internal/device/</automated>
  </verify>
  <acceptance_criteria>
  - 注毒实验记录：守护测试对注毒生产文件 FAIL（输出含 file:line 与 ForTesting 符号名）→ 还原后 PASS
  - `git diff internal/device/connection_pool.go` 为空（毒株零残留）
  - `go test -count=1 ./internal/device/` 全绿
  </acceptance_criteria>
  <done>守护自证完成：测试确能抓到生产 ForTesting 引用，且当前全仓干净</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 生产代码 ↔ 测试基建符号 | ForTesting 后缀符号（跳过连接池簿记/设备锁/可达性检查/凭据解密）禁止被生产代码引用 |
| 仓库 ↔ worktrees 副本 | .claude/worktrees 下 6 份完整仓库拷贝是陈旧副本，必须从扫描中隔离 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-05-01 | Elevation（测试替身泄漏进生产） | ForTesting 符号误用 | mitigate | 本 plan 交付物即防线：三层契约（编译器+命名+AST），注毒自证实证守护有效 |
| T-76-05-02 | Tampering（守护假绿） | 扫描器缺陷 | mitigate | 双防呆：0 文件扫描即 Fail（防 cwd 漂移）+ parse 失败即报错（防静默跳过）+ Task 2 注毒自证 |
| T-76-05-03 | DoS（守护随机红） | worktrees 副本误报 | mitigate | 点前缀目录一律 SkipDir（.claude/worktrees 实测 6 份拷贝）；violation 路径含 worktrees 即为过滤失效信号 |
</threat_model>

<verification>
- `go test -count=1 -run 'TestNoProductionForTestingReferences' ./internal/device/` PASS
- 注毒实验 FAIL→还原→PASS 记录在 SUMMARY
- `go test -count=1 ./internal/device/` 全绿
- phase 收尾全量：`go test ./...` exit 0 + `bash scripts/check-ci-local.sh backend`（gate 55.5 floor 不倒退）
</verification>

<success_criteria>
- INFRA-05 完成：AST 守护上线且自证有效，三层隔离契约成文，全仓零违规
- 本 plan 是 phase 最后一个（wave 3）：完成后 Phase 76 全部 5 个 INFRA 需求闭环
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/76-test-doubles/76-05-SUMMARY.md` when done
</output>
