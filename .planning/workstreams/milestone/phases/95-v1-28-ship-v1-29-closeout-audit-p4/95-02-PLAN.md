---
phase: 95-v1-28-ship-v1-29-closeout-audit-p4
plan: 02
type: execute
wave: 2
depends_on: ["95-01"]
files_modified:
  - xingran-react-frontend/package.json
  - xingran-react-frontend/src/pages/network/backups/hooks/useRestoreTask.ts
  - internal/api/v1/network/handlers_test_helpers_test.go
  - internal/api/v1/api_v1_tail_80_03_test.go
  - .planning/milestones/v1.29-MILESTONE-AUDIT.md
  - .planning/MILESTONES.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/workstreams/milestone/phases/94-api-p2/94-HUMAN-UAT.md
autonomous: true
requirements: [CLOSEOUT-03]
user_setup: []

must_haves:
  truths:
    - "D-03: npm run type-check 为真检查（tsc --noEmit -p tsconfig.app.json，3701 文件面）exit 0；useRestoreTask.ts:47 setTask(result.data ?? null) 修复唯一存量错误；npm run build（tsc -b 链）同根因救活 exit 0；实测 1 处错误 < 5 文件降级线，修复线成立"
    - "D-06: 七 gate 跑批逐项 exit 0——① go build ② go test（CI 三包口径主记录 + 字面 ./... 一次留证）③ type-check 真检查（非 0.09s 瞬通）④ lint 0 errors（warnings 存量口径）⑤ 前端全量测试 ⑥ 后端 coverage ≥77.5 ⑦ 前端 coverage floors 45/45"
    - "gate ② 前置: TestBackupHandler_Restore 并发窗口消除（newNetworkTestEnv SetMaxOpenConns(1)，死锁则备选 shared-cache DSN）+ TestJbu8003 种子 CreatedAt 正午锚定，均 -count=10 复验全绿——go test 0 失败诚实达成；job_utils.go 生产代码零改动"
    - "D-07: .planning/milestones/v1.29-MILESTONE-AUDIT.md 落盘——v1.27 六段模板 + type-check 空转缺陷全程章节 + Requirements 追溯 45/45 + V130-CANDIDATES 转记确认 + 本地口径/CI 滞后 168 commits 声明"
    - "D-08: 7 项行动逐项确认表（PAGINATION/TIMEOUTS/CRUD/缓存/config_backup/API 工厂/v1.28 收口；89/90 无 VERIFICATION.md 用 SUMMARY+关键 commit 兜底）"
    - "D-09: MILESTONES.md v1.29 段与 PROJECT.md v1.29 段 ✅ SHIPPED 2026-09-06 文档标记（完整 milestone archive 留 /gsd-complete-milestone，不在本相）"
    - "D-05: 94-HUMAN-UAT.md frontmatter status partial → resolved，两项 result 落档（type-check 修复线 + 外观级变化裁决），仿 92-HUMAN-UAT resolved 先例格式"
    - "D-04: 两处外观级变化（asset excel 文件名英文化 + 下载错误文案归一）裁决 = 接受现状，零代码回退，audit 报告记录"
    - "D-11: 代码修复线（D-03 + flaky）与文档线（audit/UAT/V130 与 SHIPPED 标记）分 atomic commit；gate 跑批自身零代码变更（除 D-03 修复线）"
  artifacts:
    - path: "xingran-react-frontend/package.json"
      provides: "type-check script 真实化（-p tsconfig.app.json）"
      contains: "tsconfig.app.json"
    - path: "xingran-react-frontend/src/pages/network/backups/hooks/useRestoreTask.ts"
      provides: ":47 空值合并修复（?? null）"
      contains: "result.data ?? null"
    - path: "internal/api/v1/network/handlers_test_helpers_test.go"
      provides: "newNetworkTestEnv 单连接串行化（SetMaxOpenConns(1)）"
      contains: "SetMaxOpenConns"
    - path: "internal/api/v1/api_v1_tail_80_03_test.go"
      provides: "TestJbu8003 种子 JobLog CreatedAt 正午锚定"
      contains: "time.Date"
    - path: ".planning/milestones/v1.29-MILESTONE-AUDIT.md"
      provides: "v1.29 milestone audit 报告（六段 + 2 新增章节，45/45 追溯）"
      min_lines: 100
    - path: ".planning/workstreams/milestone/phases/94-api-p2/94-HUMAN-UAT.md"
      provides: "status: resolved + 两项裁决 result（D-05）"
      contains: "resolved"
  key_links:
    - from: "xingran-react-frontend/package.json scripts.type-check"
      to: "xingran-react-frontend/tsconfig.app.json"
      via: "tsc --noEmit -p 显式指向真实检查面（3701 文件）"
      pattern: "tsconfig\\.app\\.json"
    - from: ".github/scripts/check-coverage.sh"
      to: "coverage.out + .coverage-threshold"
      via: "gate ⑥ CI :77 同款命令（threshold 77.5）"
      pattern: "check-coverage\\.sh"
    - from: ".github/scripts/check-frontend-coverage.sh"
      to: "xingran-react-frontend/coverage/coverage-final.json + .coverage-fe-floors"
      via: "gate ⑦ 仓库根 cwd 基准（CI :183 同款，Pitfall 8）"
      pattern: "check-frontend-coverage\\.sh"
    - from: ".planning/milestones/v1.29-MILESTONE-AUDIT.md"
      to: "Task 3 七 gate 实测数字 + D-08 七项行动证据索引"
      via: "audit 报告数据汇编（45/45 追溯表与 REQUIREMENTS 行数自洽）"
      pattern: "45/45"
---

<objective>
v1.29 closeout：D-03 type-check gate 修复 + gate ② flaky 双修复 + 七 gate 跑批 + v1.29-MILESTONE-AUDIT.md 生成 + SHIPPED 标记（CLOSEOUT-03）。

Purpose: CLOSEOUT-03 要求「go test 0 失败」与「type-check 0 错误」诚实达成——当前 type-check 是恒真 gate（solution-style tsconfig 0 文件空转）且 build 链已断（tsc -b exit 2），go test 全量存在 2 个已定性的 flaky 失败。本 plan 先落两处前置修复（唯一代码变更线），再跑 D-06 七 gate 全批，最后汇编 v1.27 模板式 audit 报告并设置 SHIPPED 状态。

Output: 前端 2 文件修复 + 后端 2 测试文件修复（各 atomic commit）+ 七 gate 实测记录 + v1.29-MILESTONE-AUDIT.md + 94-HUMAN-UAT resolved + MILESTONES/PROJECT SHIPPED 标记。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/STATE.md
@.planning/workstreams/milestone/phases/95-v1-28-ship-v1-29-closeout-audit-p4/95-CONTEXT.md
@.planning/workstreams/milestone/phases/95-v1-28-ship-v1-29-closeout-audit-p4/95-RESEARCH.md
@.planning/workstreams/milestone/phases/95-v1-28-ship-v1-29-closeout-audit-p4/95-01-SUMMARY.md

<interfaces>
<!-- D-03 修复面（95-RESEARCH § Code Examples 探针实证，执行以实际文件为准）： -->

xingran-react-frontend/package.json scripts 段（现状 → 目标）：
  "type-check": "tsc --noEmit"                          ← 空转根因（根 tsconfig files: []，0.09s/Files:0 恒 exit 0）
  "type-check": "tsc --noEmit -p tsconfig.app.json"     ← 目标（3701 文件 / ~14s；tsc -b 备选因 build-mode 语义混淆否决）
  "type-check:strict" 不动（CI 未引用无消费方，audit 注记登记）

xingran-react-frontend/src/pages/network/backups/hooks/useRestoreTask.ts:47（现状 → 目标）：
  setTask(result.data);        ← TS2345：src/types/base.ts:11 data?: T → ConfigRestoreTask | undefined 不可赋 null 联合
  setTask(result.data ?? null); ← 目标（:48 isTerminal(result.data?.status) 已容忍 undefined，零涟漪）

gate ② flaky 修复面（95-RESEARCH § Gate ② 源码级实证）：
  internal/api/v1/network/handlers_test_helpers_test.go — newNetworkTestEnv（:48 附近）裸 sqlite.Open(":memory:")，
    需对 gormDB 底层 *sql.DB 设 SetMaxOpenConns(1)（glebarez 每连接 = 独立空库；Phase 93 异步恢复 goroutine
    并发取第二连接见空库 → no such table sys_config_restore_task）；备选 DSN file:network_test?mode=memory&cache=shared
  internal/api/v1/api_v1_tail_80_03_test.go — TestJbu8003_GetJobStatistics 种子 JobLog CreatedAt
    改 time.Date(y, m, d, 12, 0, 0, 0, time.Local) 正午锚定（job_utils.go:52 本地日界 + glebarez 带偏移写格式
    + sqlite DATE() 换算 UTC 取日 → 本地 00:00-08:00 窗口 UTC 日期为前一天永不匹配；正午全天候稳定）

七 gate 命令链（D-06 / 95-RESEARCH § Code Examples，实测时长）：
  ① go build ./...                                                    （~1min）
  ② go test -timeout 15m -count=1 -coverprofile=coverage.out -covermode=atomic ./internal/... ./pkg/... ./cmd/...   （CI 三包口径，~7-9min）
  ⑥ bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold                        （threshold 77.5；exit 0/1/2/4/5）
  ③ cd xingran-react-frontend && npm run type-check                   （修复后 ~14s）
  ④ npm run lint                                                      （0 errors / ~1389 warnings 存量）
  ⑤⑦ npm run test:coverage                                            （vitest run --coverage，~17min）
  ⑦ cd .. && bash .github/scripts/check-frontend-coverage.sh xingran-react-frontend/coverage/coverage-final.json .coverage-fe-floors   （仓库根 cwd；exit 0/1/4/6）
  补充留证：go test -timeout 15m -count=1 ./...                       （D-06 字面口径一次，~8-10min）

audit 模板：.planning/milestones/v1.27-MILESTONE-AUDIT.md 六段结构（SC 验证 / Requirements 追溯 / SUMMARY 索引 / 最终 gate / 裁决 / 结论）
HUMAN-UAT resolved 先例：.planning/workstreams/milestone/phases/92-*/92-HUMAN-UAT.md（frontmatter status + Tests 段 expected/result + Summary 计数块）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-03 type-check gate 空转修复（修复线 + 降级线检查）</name>
  <files>xingran-react-frontend/package.json, xingran-react-frontend/src/pages/network/backups/hooks/useRestoreTask.ts</files>
  <read_first>
    - xingran-react-frontend/package.json（scripts 段）
    - xingran-react-frontend/src/pages/network/backups/hooks/useRestoreTask.ts（:40-60 上下文）
    - xingran-react-frontend/tsconfig.json + tsconfig.app.json（solution-style 缺陷现场）
    - xingran-react-frontend/src/types/base.ts（:11 data?: T）
    - .planning/workstreams/milestone/phases/94-api-p2/94-VERIFICATION.md（tsc 探针数据）
    - 95-RESEARCH.md § D-03 修复方案实证 + § Common Pitfalls 2
  </read_first>
  <action>
    1. package.json scripts 段：`"type-check": "tsc --noEmit"` 改为 `"type-check": "tsc --noEmit -p tsconfig.app.json"`（D-03 修复线；探针实证选型——3701 文件/12.97s，与 94 verifier 探针同款；「根 config 补 references」方案已证伪不可行，`tsc -b` 备选因 build-mode 语义混淆否决）。`type-check:strict` 保持不动（裁定：CI 未引用无消费方，Task 4 audit 注记登记）。
    2. useRestoreTask.ts :47 `setTask(result.data);` 改为 `setTask(result.data ?? null);`（单 token 级；TS2345 根因 = BaseResponse data 可选属性不可赋 null 联合 state；:48 `isTerminal(result.data?.status)` 已容忍 undefined，无连锁）。
    3. 降级线检查（D-03 授权边界）：执行步骤 4 验证链时若暴露 >5 文件新错误、或需拆分/重构 tsconfig config 体系才能达成 → 立即停止代码改动，`git checkout -- <两文件>` 回滚本 task 全部变更，转登记线（audit 报告登记恒真缺陷 + REQUIREMENTS V130-CANDIDATES 候选），并在 SUMMARY 记录降级裁定。实测预期：探针全仓恰 1 处错误，修复线成立，不触发降级。
    4. 验证链（cwd xingran-react-frontend）：`npx tsc --noEmit -p tsconfig.app.json` exit 0（~14s）；`npx tsc -b` exit 0（build 链同根因救活）；`npm run type-check` exit 0 且耗时明显 >1s（非 0.09s 瞬通——恒真断言证伪的判定点）；`npm run build` exit 0 完整确认（tsc -b && vite build 全链）。
    5. atomic commit（D-11 唯一代码变更豁免线）：`fix(95): D-03 type-check gate 真实化（-p tsconfig.app.json）+ useRestoreTask:47 空值合并修复（build 链同根因救活）`。
  </action>
  <acceptance_criteria>
    - source: xingran-react-frontend/package.json scripts.type-check 值为 "tsc --noEmit -p tsconfig.app.json"
    - source: useRestoreTask.ts 含 "setTask(result.data ?? null)" 且不再含裸 "setTask(result.data);"
    - behavior: type-check 在真实检查面（3701 文件）下 0 错误 exit 0
    - behavior: npx tsc -b exit 0（build 链修复）
    - test-command: `cd xingran-react-frontend && npm run type-check && npx tsc -b`
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/CODE/ClaudeCode/guoguo/xingran-react-frontend" && npm run type-check && npx tsc -b && grep -c "tsconfig.app.json" package.json && grep -c "result.data ?? null" src/pages/network/backups/hooks/useRestoreTask.ts</automated>
  </verify>
  <done>type-check 真检查（非瞬通）exit 0；tsc -b exit 0；npm run build exit 0；两文件单行改动落地并独立 atomic commit。</done>
</task>

<task type="auto">
  <name>Task 2: gate ② flaky 双修复（test-infra，零生产行为变更）</name>
  <files>internal/api/v1/network/handlers_test_helpers_test.go, internal/api/v1/api_v1_tail_80_03_test.go</files>
  <read_first>
    - internal/api/v1/network/handlers_test_helpers_test.go（newNetworkTestEnv :48 附近）
    - internal/api/v1/network/backup_handler_test.go（:281 mutual_exclusion_rejected 失败现场）
    - internal/api/v1/api_v1_tail_80_03_test.go（TestJbu8003_GetJobStatistics 种子段）
    - internal/api/v1/job_utils.go（:52 today 日界根因——只读不修改）
    - 95-RESEARCH.md § Gate ② 现状警示（根因链每一环）
  </read_first>
  <action>
    1. 修复 1（并发 flake）：newNetworkTestEnv（handlers_test_helpers_test.go）中 sqlite `:memory:` 打开并 AutoMigrate 后，对 gormDB 底层 `*sql.DB`（gormDB.DB() 或 sqlDB 变量）调 `SetMaxOpenConns(1)`，附注释说明机理（glebarez 每连接 = 独立空库，Phase 93 异步恢复 goroutine 与测试主线程并发取第二连接见空库 → no such table sys_config_restore_task；单连接串行化消除窗口；全仓新 pattern）。
    2. 备选路径（仅当步骤 3 复验出现死锁）：改用 DSN `file:network_test?mode=memory&cache=shared` 替代 SetMaxOpenConns(1)，重验。
    3. 修复 2（时区日界 flake）：api_v1_tail_80_03_test.go TestJbu8003_GetJobStatistics 种子 JobLog 的 CreatedAt 改为显式 `time.Date(y, m, d, 12, 0, 0, 0, time.Local)` 正午锚定（本地正午 +08 = UTC 同日，全天候稳定）。**internal/api/v1/job_utils.go 生产代码零改动**——GetJobStatistics 生产看板同窗口缺陷属行为变更（D-04 红线），由 Task 4 登记 V130-CANDIDATES。
    4. 复验链（Pitfall 1 防管道掩蔽）：`go test -count=10 -run "TestBackupHandler_Restore" ./internal/api/v1/network/` 全绿；`go test -count=1 ./internal/api/v1/network/` 同包无死锁抽验；`go test -count=1 -run "TestJbu8003" ./internal/api/v1/` 绿。任何 FAIL → 启用备选 DSN 重验或记录分析。
    5. atomic commit（test-infra 线）：`test(95): gate ② flaky 双修复——newNetworkTestEnv 单连接串行化 + Jbu8003 种子正午锚定（test-infra，零生产行为变更）`。
  </action>
  <acceptance_criteria>
    - source: git diff 中本 task 触碰文件仅 internal/api/v1/network/handlers_test_helpers_test.go 与 internal/api/v1/api_v1_tail_80_03_test.go（均 _test.go 结尾，零生产文件）
    - behavior: TestBackupHandler_Restore 在 -count=10 下 0 FAIL（原 flake 两轮全量必现）
    - behavior: TestJbu8003_GetJobStatistics 计数断言在任意本地时段稳定通过
    - test-command: `cd "D:/CODE/ClaudeCode/guoguo" && go test -count=10 -run "TestBackupHandler_Restore" ./internal/api/v1/network/ && go test -count=1 ./internal/api/v1/network/ && go test -count=1 -run "TestJbu8003" ./internal/api/v1/`
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/CODE/ClaudeCode/guoguo" && go test -count=10 -run "TestBackupHandler_Restore" ./internal/api/v1/network/ && go test -count=1 ./internal/api/v1/network/ && go test -count=1 -run "TestJbu8003" ./internal/api/v1/</automated>
  </verify>
  <done>-count=10 全绿 + 同包无死锁；两处改动均为 *_test.go（零生产文件）；job_utils.go 未触碰；独立 atomic commit。</done>
</task>

<task type="auto">
  <name>Task 3: D-06 七 gate 跑批 + 工作树快照记录</name>
  <files>（无源码文件；coverage.out 与 xingran-react-frontend/coverage/ 为 gitignored 临时产物，不提交）</files>
  <read_first>
    - .github/workflows/ci.yml（:62-77 后端 gate、:145-200 前端 gate 口径）
    - .github/scripts/check-coverage.sh（exit 0/1/2/4/5 语义，头注）
    - .github/scripts/check-frontend-coverage.sh（exit 0/1/4/6 语义）
    - .coverage-threshold（77.5）+ .coverage-fe-floors（GLOBAL 3.8 + 45 dirs）
    - 95-RESEARCH.md § 七 gate 跑批命令清单 + § Common Pitfalls 1/3/4/5/8
  </read_first>
  <action>
    1. 跑批前快照（Pitfall 4，裁定 = 记录放行）：记录 `git rev-parse HEAD` 与 `git status --short` 输出——audit 报告须含 commit SHA + 未跟踪文件清单（4 个未跟踪测试文件 rpa_model_methods_test.go / manager_coverage_test.go / sysmetrics_common_test.go / sysmetrics_windows_test.go 单列标注「随跑批执行且全绿」；另 cov_full_research.out / node_modules/ / xingran-frontend/ 杂物登记）。
    2. 后端三 gate（仓库根 cwd）：① `go build ./...`；② `go test -timeout 15m -count=1 -coverprofile=coverage.out -covermode=atomic ./internal/... ./pkg/... ./cmd/...`（CI 三包口径 = audit 主记录）；⑥ `bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold`。防 Pitfall 1：重定向输出到文件后单独检查 `$?`，禁止 `cmd | tail; echo $?` 管道取码。
    3. 补充留证：`go test -timeout 15m -count=1 ./...` 字面全量一次（D-06 字面口径；根 tests/ 集成测试自跳过预期——SKIP_E2E 守卫 + v1.26 全仓绿先例）；audit 报告写明双口径（三包为主 / `./...` 补充）。
    4. 前端四 gate（xingran-react-frontend cwd）：③ `npm run type-check`（Task 1 修复后真检查，记录耗时证明非瞬通）；④ `npm run lint`（0 errors；~1389 warnings 存量口径不变）；⑤⑦ 合并 `npm run test:coverage`（= vitest run --coverage 天然 run-once，防 Pitfall 5 watch 挂起；~17min）；随后 `cd ..` 切仓库根跑 ⑦ `bash .github/scripts/check-frontend-coverage.sh xingran-react-frontend/coverage/coverage-final.json .coverage-fe-floors`（Pitfall 8 cwd 基准，CI :183 同款）。
    5. 七 gate 结果逐项记录（供 Task 4 audit「最终 gate 实测」节直接引用）：每项的命令全文 + exit code + 关键数字（后端 coverage 百分比 / 前端 GLOBAL + 45/45 dirs / 测试计数 / lint errors 与 warnings 数）。
    6. 跑批产物处置：coverage.out 与 xingran-react-frontend/coverage/ 已被 .gitignore 覆盖（:58 / :120），跑批后 `git status --short` 复核无新增未忽略产物即可；不提交不删除。
    7. 禁止 push（裁定：push 决策留用户，73-05 先例；audit 如实记录本地口径 + CI 滞后事实即可）。
  </action>
  <acceptance_criteria>
    - behavior: 七 gate 逐项 exit 0（①②③④⑤⑥⑦），其中 ② 含三包口径与 `./...` 补充跑两次记录、③ 记录真实耗时（非 0.09s 瞬通）
    - source: 执行记录含 git rev-parse HEAD 的 commit SHA 与 git status --short 快照
    - source: `git diff --name-only HEAD` 除 Task 1/2 已 commit 内容外无新增代码变更（gate 跑批零代码变更，D-11）
    - test-command: `cd "D:/CODE/ClaudeCode/guoguo" && bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold && bash .github/scripts/check-frontend-coverage.sh xingran-react-frontend/coverage/coverage-final.json .coverage-fe-floors`
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/CODE/ClaudeCode/guoguo" && bash .github/scripts/check-coverage.sh coverage.out .coverage-threshold && bash .github/scripts/check-frontend-coverage.sh xingran-react-frontend/coverage/coverage-final.json .coverage-fe-floors && cd xingran-react-frontend && npm run type-check && echo "GATES_3_6_7_RECONFIRM=0"</automated>
  </verify>
  <done>七 gate 逐项 exit 0 记录在案（含 ./... 补充留证与 type-check 耗时证明）；工作树快照已存档；coverage 双 gate 与 type-check 可复确认 exit 0。</done>
</task>

<task type="auto">
  <name>Task 4: v1.29-MILESTONE-AUDIT.md 生成 + SHIPPED 标记 + HUMAN-UAT 流转 + V130 登记（D-04/D-05/D-07/D-08/D-09/D-11）</name>
  <files>.planning/milestones/v1.29-MILESTONE-AUDIT.md, .planning/MILESTONES.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/workstreams/milestone/phases/94-api-p2/94-HUMAN-UAT.md</files>
  <read_first>
    - .planning/milestones/v1.27-MILESTONE-AUDIT.md（D-07 六段模板，结构对照）
    - .planning/PROJECT.md（v1.29 Current Milestone 段——锁定决策 D-01..D-06 + Progress 七项 = D-08 证据基础）
    - .planning/REQUIREMENTS.md（全量 45 项复选框现状 + V130-CANDIDATES 段）
    - .planning/workstreams/milestone/phases/94-api-p2/94-HUMAN-UAT.md（流转对象）
    - .planning/workstreams/milestone/phases/ 下 92-* 的 92-HUMAN-UAT.md（resolved 先例格式）
    - 95-RESEARCH.md § 七项行动证据索引 + § Pattern 2 audit 模板映射
    - .planning/workstreams/milestone/phases/95-v1-28-ship-v1-29-closeout-audit-p4/95-01-SUMMARY.md（校准后口径 + 记账核销记录）
    - Task 3 七 gate 实测记录
  </read_first>
  <action>
    1. 新建 `.planning/milestones/v1.29-MILESTONE-AUDIT.md`（D-07，仿 v1.27 六段模板，落盘后作为 SHIPPED 判定依据）：
       a) 头部块——Milestone v1.29 / Phases 89-95（7）/ Plans（24 已执行 + 95 的 2）/ gate 配置链（.coverage-threshold 77.5 + .coverage-fe-floors GLOBAL 3.8 + 45 dirs）；
       b) Milestone SC 验证——PROJECT.md 锁定决策 D-01..D-06 践行逐项 + 7 项行动 100% 确认表（D-08：PAGINATION/TIMEOUTS/CRUD 复用/缓存统一/config_backup 闭环/API 工厂化/v1.28 收口；证据口径——89 用 89-0M-SUMMARY.md + commits 238283c..3559626、90 用 90-0M-SUMMARY.md + commits b51f44c..3a2efe5（两相无 VERIFICATION.md，D-08「/关键 commit」分支兜底）；91 用 91-VERIFICATION.md passed 8/9+OVR-91-01；92 用 92-VERIFICATION.md passed 10/10；93 用 93-VERIFICATION.md 结论行「✅ PHASE GOAL ACHIEVED (5/5)」（无 frontmatter）；94 用 94-VERIFICATION.md human_needed 4/4 truths + 本相 D-03/D-04/D-05 裁决闭环；95 用 95-01 核对清单 + 本 audit）；
       c) Requirements 追溯——**45/45**（逐项表格 Req/内容/状态/证据；实数 45 非 41，与追溯表行数自洽；引用 95-01 校准后口径）；
       d) Phase 链 SUMMARY 索引——89-95 七行（以 RESEARCH § 七项行动证据索引表为底稿补实 commit）；
       e) 最终 gate 配置与实测——Task 3 七 gate 逐项命令 + exit code + 关键数字；显式声明本地口径（v1.29 全程未推送、本地领先 origin/main 168 commits、CI 末次绿跑 2026-09-03 = Phase 88 内容、CI 未见证 v1.29 代码）+ 跑批 commit SHA 与工作树快照（Pitfall 4 记录放行裁定）+ gate ② 双口径声明（CI 三包为主 / `./...` 补充留证）；
       f) 预存缺陷裁决——V130-CANDIDATES（CACHEDEF-01..05）转记确认；
       g) 新增章节 1「type-check gate 空转缺陷全程」（D-07 指定）——发现（94-VERIFICATION 探针）→ 定性（solution-style files:[] 恒真 gate，0.09s/Files:0）→ 处置（D-03 修复线：script -p tsconfig.app.json + useRestoreTask:47 ?? null；实测 1 处 < 5 降级线，修复线成立）→ 附带收益（npm run build 的 tsc -b 链同根因救活）→ type-check:strict 注记登记不修（CI 未引用无消费方）→ 教训（「gate 语义 ≠ gate 结果」）；
       h) 新增章节 2「gate ② flaky 双发现」——TestBackupHandler_Restore 并发机理（:memory: 每连接独立空库）+ test-infra 修复；TestJbu8003 时区日界根因链（job_utils.go:52 本地日界 + glebarez 带偏移写格式 + sqlite DATE() UTC 取日）+ 种子正午锚定 + 生产看板缺陷登记 JOBSTAT-01（V130，本相不修）；
       i) 结论——SHIPPED 判定 + known gaps（D-04 接受的 2 项外观级变化 / CI 滞后 / operlog todo deferred / V130 候选清单）+ 文档债核销表（41→45 三处 / 两 ROADMAP Progress stale / BACKUP-CLOSED-01/02 补勾，注明 95-01 已核销）。
    2. REQUIREMENTS.md V130-CANDIDATES 段新增条目 `JOBSTAT-01`（照 CACHEDEF 系列格式）：`internal/api/v1/job_utils.go:52` GetJobStatistics 时区日界生产看板缺陷——本地日界 + glebarez 驱动默认写时间格式带 +08:00 偏移 + sqlite DATE() 换算 UTC 取日 → 本地 00:00-08:00 窗口当天前 8 小时 JobLog 计入「昨日」，今日成功/失败看板少计；修复属行为变更须附回归测试（v1.29 D-05 例外条款同款纪律），v1.30+ 候选；测试侧已正午锚定隔离（api_v1_tail_80_03_test.go，Phase 95）；段标题注记「2026-09-06 Phase 95 增补 JOBSTAT-01」。
    3. 94-HUMAN-UAT.md 状态流转（D-05，仿 92-HUMAN-UAT resolved 先例）：frontmatter `status: partial` → `resolved`，`updated` 刷新为当前时间戳；「Current Test」行更新为「全部判定完成 2026-09-06（Phase 95 closeout 裁决，用户预授权自主运行）」；Test 1 result → 「✅ Phase 95 D-03 修复线落地：type-check script 改 tsc --noEmit -p tsconfig.app.json + useRestoreTask.ts:47 setTask(result.data ?? null)；真实配置（3701 文件）0 错误，build 链（tsc -b）一并修复」；Test 2 result → 「✅ Phase 95 D-04 裁决 = 接受现状（94-03-SUMMARY deviation 6 登记 + 测试锁定；ignoreContentDisposition 选项留 deferred）」；Summary 计数块 passed: 2 / pending: 0。
    4. SHIPPED 标记（D-09）：MILESTONES.md v1.29 段标题「🚧 STARTED 2026-09-04」→「✅ SHIPPED 2026-09-06」，Status 行 planning → shipped，Plans TBD → 实数（26），Delivered 段按 PROJECT.md Progress 七项行动补实；PROJECT.md v1.29 段 Progress 末尾「Phase 95 待推进…」→ 补「Phase 95 ✅ 2026-09-06（v1.28 收口核对 + gate 全绿 + audit）」+「v1.29 ✅ SHIPPED 2026-09-06」。**完整 milestone archive（.planning/milestones/v1.29-phases/ 迁移、workstream 归位）不做**——/gsd-complete-milestone 独立工作流（deferred）。
    5. atomic commits（D-11）：commit A = audit 报告 + 94-HUMAN-UAT 流转 + REQUIREMENTS JOBSTAT-01（`docs(95): v1.29-MILESTONE-AUDIT 报告 + 94-HUMAN-UAT resolved 流转（D-05/D-07）+ JOBSTAT-01 V130 登记`）；commit B = MILESTONES + PROJECT SHIPPED 标记（`docs(95): v1.29 milestone SHIPPED 标记（D-09；完整 archive 留 /gsd-complete-milestone）`）。
  </action>
  <acceptance_criteria>
    - source: .planning/milestones/v1.29-MILESTONE-AUDIT.md 存在，`grep -c "^## "` ≥8（六段 + 2 新增章节），含「45/45」「type-check」「168 commits」关键词
    - source: .planning/REQUIREMENTS.md V130-CANDIDATES 段含 JOBSTAT-01 条目
    - source: 94-HUMAN-UAT.md frontmatter 含 "status: resolved" 且 Summary 计数 passed: 2 / pending: 0
    - source: .planning/MILESTONES.md v1.29 段标题含「✅ SHIPPED 2026-09-06」；.planning/PROJECT.md v1.29 段含 SHIPPED 标记
    - test-command: `test -f .planning/milestones/v1.29-MILESTONE-AUDIT.md && head -3 .planning/workstreams/milestone/phases/94-api-p2/94-HUMAN-UAT.md | grep -c resolved`
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/CODE/ClaudeCode/guoguo" && test -f .planning/milestones/v1.29-MILESTONE-AUDIT.md && grep -c "^## " .planning/milestones/v1.29-MILESTONE-AUDIT.md && grep -c "45/45" .planning/milestones/v1.29-MILESTONE-AUDIT.md && grep -c "JOBSTAT-01" .planning/REQUIREMENTS.md .planning/milestones/v1.29-MILESTONE-AUDIT.md && head -3 .planning/workstreams/milestone/phases/94-api-p2/94-HUMAN-UAT.md | grep -c "resolved" && grep -c "✅ SHIPPED 2026-09-06" .planning/MILESTONES.md && git log --oneline -2</automated>
  </verify>
  <done>audit 报告落盘（六段 + 2 新增章节 + 45/45 追溯 + 本地口径声明）；JOBSTAT-01 登记；94-HUMAN-UAT resolved（2/2）；MILESTONES/PROJECT v1.29 SHIPPED 双标记；两个 atomic commit。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新增攻击面 | 本相唯一代码动作 = type-check script 一行（工具链配置）+ useRestoreTask.ts:47 一行空值合并（类型收紧）；其余为测试基建与文档 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-03 | Tampering/Falsification | 恒真 gate 虚假保证（type-check 空转 = 验证防线失效） | mitigate | D-03 正是修复此模式——script 显式指向 tsconfig.app.json 真实检查面；verify 要求耗时证明非瞬通；audit 记录「gate 语义 ≠ gate 结果」教训 |
| T-95-04 | Information Disclosure | audit 报告误录敏感值 | mitigate | 报告仅引用 gate exit code / 覆盖率数字 / commit SHA / 测试计数，不粘贴 config/env/密钥内容（项目 operlog 脱敏纪律同源） |
| T-95-05 | Tampering（审计可信度） | gate 跑批混入未提交代码 | mitigate | 裁定记录放行：跑批前快照 commit SHA + git status --short，4 个未跟踪测试文件单列标注「随跑批执行且全绿」；audit 写明工作树状态保证可复现性 |
| T-95-06 | Repudiation | audit 证据引用不存在的文件（89/90 无 VERIFICATION.md） | mitigate | 证据口径兜底规则写入 action（SUMMARY + 关键 commit）；93-VERIFICATION 无 frontmatter 用结论行引用 |
| T-95-SC | Tampering | npm/pip/cargo installs | accept | 本 plan 零包安装（唯一 script 变更使用已装 TypeScript 5.9.3），Package Legitimacy Gate 不触发 |
</threat_model>

<verification>
- D-03：`cd xingran-react-frontend && npm run type-check` exit 0（非瞬通）+ `npx tsc -b` exit 0 + `npm run build` exit 0
- gate ②：`go test -count=10 -run "TestBackupHandler_Restore" ./internal/api/v1/network/` 全绿 + TestJbu8003 绿
- D-06：七 gate 逐项 exit 0（①②⑥ 后端 / ③④⑤⑦ 前端 + 仓库根 fe gate）+ `./...` 补充留证
- D-07：`.planning/milestones/v1.29-MILESTONE-AUDIT.md` 存在，`grep -c "^## "` ≥8，「45/45」命中
- D-08：audit 含 7 项行动确认表（89/90 用 SUMMARY+commit 兜底口径）
- D-09：MILESTONES.md + PROJECT.md v1.29 段「✅ SHIPPED 2026-09-06」
- D-05：94-HUMAN-UAT.md `status: resolved`，passed 2 / pending 0
- D-04：零代码回退（git log 无 useRestoreTask 之外的 download/文案相关 revert）
- D-11：三个代码/文档 atomic commit（fix / test / docs×2）分线落地
</verification>

<success_criteria>
1. type-check gate 从恒真变真 gate：真实检查面 0 错误，build 链同根因救活（D-03，1 处错误 < 5 降级线）
2. go test 0 失败诚实达成：2 个 flaky 测试 test-infra 修复 + count=10 复验（零生产文件改动）
3. 七 gate 跑批全绿并逐项留证（命令 + exit code + 关键数字 + 工作树快照 + 本地口径声明）（D-06）
4. v1.29-MILESTONE-AUDIT.md 落盘：六段模板 + type-check 缺陷章节 + 45/45 追溯 + V130 转记确认（D-07/D-08）
5. JOBSTAT-01 生产看板缺陷登记 V130-CANDIDATES（TestJbu8003 连带发现，本相不修）
6. 94-HUMAN-UAT.md resolved 流转（D-03 修复线 + D-04 接受裁决两项 result）
7. MILESTONES.md + PROJECT.md v1.29 ✅ SHIPPED 2026-09-06 标记（D-09；完整 archive 不在本相）
8. 全部变更按 D-11 atomic commit 纪律分线落地
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/95-v1-28-ship-v1-29-closeout-audit-p4/95-02-SUMMARY.md` when done
</output>
