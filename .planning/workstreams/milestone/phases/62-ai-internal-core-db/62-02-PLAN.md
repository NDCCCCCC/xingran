---
phase: 62-ai-internal-core-db
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/core/db/filter_logger.go
  - internal/core/db/filter_logger_test.go
  - internal/core/db/migrations/menu_grant_helpers.go
  - internal/core/db/migrations/menu_grant_helpers_test.go
autonomous: true
requirements: ["C4", "C6"]
must_haves:
  truths:
    - "C4: 耗时达到 SlowThreshold 的成功 SQL 查询被记录为 WARN(慢查询日志真实生效)"
    - "C4: FilterLogger.Info/Warn 尊重 config.MinLevel,DefaultLogFilterConfig(MinLevel=Silent)下保持完全静默(默认行为不变)"
    - "C4: LogMode(level) 设置的级别真实影响后续输出(不再是写入后无人读取的死字段)"
    - "C6: GrantNewMenuToRolesHavingParent 的 SQL 不再含 fmt.Sprintf 字符串插值,newMenuID/parentMenuName 走 $1/$2 参数化"
    - "C6: 既有源码断言测试的 4 个 SQL 片段(INSERT INTO sys_role_menu / JOIN sys_menu m / WHERE m.menu_name = / ON CONFLICT DO NOTHING)在参数化后仍全部保留"
  artifacts:
    - path: "internal/core/db/filter_logger.go"
      provides: "慢查询日志 + MinLevel 生效的 Info/Warn/Trace"
      contains: "SlowThreshold"
    - path: "internal/core/db/filter_logger_test.go"
      provides: "FilterLogger 行为单元测试"
      contains: "TestTrace"
    - path: "internal/core/db/migrations/menu_grant_helpers.go"
      provides: "参数化授权 SQL"
      contains: "$1::uuid"
  key_links:
    - from: "filter_logger.go Trace"
      to: "applogger.Warnf 慢查询输出"
      via: "time.Since(begin) >= SlowThreshold 判定"
      pattern: "SlowThreshold"
    - from: "menu_grant_helpers.go"
      to: "db.Exec 参数绑定"
      via: "$1::uuid / $2 占位符"
      pattern: "db\\.Exec\\(`?[^%]"
---

<objective>
修复评审共识 C4(FilterLogger 的 SlowThreshold/LogMode/MinLevel 是死配置——广告的慢查询功能从未实现)与 C6(GrantNewMenuToRolesHavingParent 导出函数 SQL 字符串插值,对未来调用方是注入隐患)。

Purpose: C4 用户锁定方向为"实现或删除"——本计划选择**实现**(慢查询日志对慢库 dev 环境有真实价值,STATE.md 记录过 dev DB 性能问题);C6 参数化零成本消除注入面。
Output: 可用的慢查询 WARN 日志、MinLevel 语义真实的 FilterLogger、参数化的授权 helper。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/62-ai-internal-core-db/62-REVIEWS.md
@internal/core/db/filter_logger.go
@internal/core/db/migrations/menu_grant_helpers.go
@internal/core/db/migrations/menu_grant_helpers_test.go

<interfaces>
<!-- 既有契约,executor 直接使用 -->

filter_logger.go 当前结构(全部字段):
```go
type LogFilterConfig struct {
    MinLevel      logger.LogLevel  // gorm.io/gorm/logger 的 LogLevel
    FilterTypes   map[LogType]bool // LogTypeSQL / LogTypeError,true=过滤掉
    SlowThreshold int              // 毫秒,0=不启用
}
var DefaultLogFilterConfig = LogFilterConfig{
    MinLevel: logger.Silent,
    FilterTypes: map[LogType]bool{LogTypeSQL: true, LogTypeError: false},
    SlowThreshold: 1000,
}
// FilterLogger 实现 gorm logger.Interface: LogMode/Info/Warn/Error/Trace
```

唯一实例化点:internal/core/db/database.go createFilteredLogger() → NewFilterLogger(DefaultLogFilterConfig)。
结构体改名是 deferred 项,本计划不改名(用户锁定"实现或删除",选择实现)。

menu_grant_helpers.go 当前签名(保持不变):
```go
func GrantNewMenuToRolesHavingParent(db *gorm.DB, parentMenuName string, newMenuID string) error
```
既有源码断言测试(menu_grant_helpers_test.go:104-127)要求保留片段:
"INSERT INTO sys_role_menu" / "JOIN sys_menu m ON rm.menu_id = m.id" / "WHERE m.menu_name =" / "ON CONFLICT DO NOTHING" / "isPostgreSQL(db)"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FilterLogger 实现慢查询日志 + MinLevel 语义 (C4)</name>
  <files>internal/core/db/filter_logger.go, internal/core/db/filter_logger_test.go</files>
  <read_first>
    - internal/core/db/filter_logger.go (当前实现全文 87 行)
    - internal/core/db/database.go:79-81 (createFilteredLogger 唯一实例化点)
    - .planning/phases/62-ai-internal-core-db/62-REVIEWS.md §C4 与 codex "FilterLogger has a broken LogMode and three dead config knobs"
  </read_first>
  <behavior>
    - Test 1: Trace 在 err==nil 且 time.Since(begin) >= SlowThreshold(毫秒)时产生慢查询 WARN 输出;fc() 返回的 SQL 与行数出现在消息中
    - Test 2: Trace 在 err==nil 且耗时 < SlowThreshold 时完全静默(默认 SlowThreshold=1000 下普通查询不输出)
    - Test 3: Trace 在 err!=nil(非 ErrRecordNotFound)时保持现有 ERROR 输出行为不回归
    - Test 4: SlowThreshold=0 时慢查询判定不启用(任何耗时都不输出)
    - Test 5: Info/Warn 在 MinLevel=Silent(默认)时静默;MinLevel<=logger.Info 时 Info 产生输出;LogMode(logger.Warn) 返回的 logger 使 Warn 可输出而 Info 静默
  </behavior>
  <action>
    重写 filter_logger.go(保持 LogType / LogFilterConfig / DefaultLogFilterConfig / NewFilterLogger 导出签名不变,database.go 零改动):

    1) Trace:err==nil(或 ErrRecordNotFound)分支不再直接 return——先判定慢查询:SlowThreshold > 0 且 time.Since(begin) >= time.Duration(SlowThreshold)*time.Millisecond 时,调用 fc() 取 SQL 与行数,applogger.Warnf("[GORM慢查询] 耗时: %v | 行数: %d | %s", elapsed, rows, sql)。慢查询判定不受 FilterTypes[LogTypeSQL] 影响(慢查询是运维信号,不属于"普通 SQL"过滤范畴)。err 分支保持现有行为(ErrRecordNotFound 静默,其余 Errorf)。

    2) Info/Warn 实现 MinLevel:Info 在 config.MinLevel <= logger.Info 时 applogger.Debugf 输出;Warn 在 config.MinLevel <= logger.Warn 时 applogger.Warnf 输出;否则静默。默认配置 MinLevel=Silent 下两者仍全静默(行为不变)。

    3) LogMode 保持现有"拷贝并设置 MinLevel"实现即可——MinLevel 被 Info/Warn/Trace 真实读取后,该方法自然生效,无需改代码;更新其注释去掉"死字段"歧义。

    4) 为可测性,把慢查询消息构造提取为包内私有方法(如 func (l *FilterLogger) slowQueryLog(begin time.Time, fc func() (string, int64)) (msg string, emit bool)),返回是否应输出与消息内容;Trace 调用它决定 applogger.Warnf。测试直接断言该私有方法的返回值,避免捕获日志输出。

    5) 修正 DefaultLogFilterConfig 的注释("慢查询阈值 1 秒"现在是真实行为而非广告)。

    新建 filter_logger_test.go 覆盖 behavior 五条。
  </action>
  <verify>
    <automated>cd D:/code/ClaudeCode/guoguo && go build ./... && go test ./internal/core/db/ -run "TestTrace|TestInfoWarn|TestSlowQuery" -v</automated>
  </verify>
  <done>
    慢查询 WARN 生效、MinLevel 语义真实、默认配置下行为不变;新测试全部 PASS;go build ./... 退出码 0
  </done>
  <acceptance_criteria>
    - filter_logger.go 中 Trace 函数不再以 err==nil 直接 return 结束(源码含 SlowThreshold 耗时判定):grep -q "SlowThreshold" internal/core/db/filter_logger.go 且 grep -q "time.Since(begin)" internal/core/db/filter_logger.go
    - Info/Warn 读取 MinLevel:grep -c "config.MinLevel" internal/core/db/filter_logger.go 输出 >= 3(LogMode + Info + Warn)
    - go test ./internal/core/db/ -v 中 behavior 五条测试全部 PASS
    - go build ./... 退出码 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GrantNewMenuToRolesHavingParent SQL 参数化 (C6)</name>
  <files>internal/core/db/migrations/menu_grant_helpers.go, internal/core/db/migrations/menu_grant_helpers_test.go</files>
  <read_first>
    - internal/core/db/migrations/menu_grant_helpers.go (当前 fmt.Sprintf 实现在 line 43-52)
    - internal/core/db/migrations/menu_grant_helpers_test.go:104-127 (既有源码断言,参数化后必须仍 PASS)
    - .planning/phases/62-ai-internal-core-db/62-REVIEWS.md §C6
  </read_first>
  <behavior>
    - Test 1 (既有,不回归): 4 个 SQL 片段断言 + isPostgreSQL 守卫断言全部保留
    - Test 2 (新增源码断言): menu_grant_helpers.go 不含 "fmt.Sprintf"(SQL 插值消除);含 "$1::uuid" 与 "$2" 占位符
    - Test 3 (既有,sqlite): 非 PG 方言返回 nil 不 panic
  </behavior>
  <action>
    修改 menu_grant_helpers.go:把 fmt.Sprintf 拼接 SQL 改为常量 SQL + 参数绑定——db.Exec 的 SQL 字符串字面量为 INSERT INTO sys_role_menu (role_id, menu_id) SELECT rm.role_id, $1::uuid FROM sys_role_menu rm JOIN sys_menu m ON rm.menu_id = m.id WHERE m.menu_name = $2 ON CONFLICT DO NOTHING,参数依次为 newMenuID, parentMenuName。保持函数签名、isPostgreSQL 守卫、返回 .Error 的行为不变。

    移除 "fmt" import(参数化后文件内不再有 fmt 使用——先确认;若 docstring 以外无其他 fmt 调用则删 import)。

    更新 docstring:删掉"newMenuID / parentMenuName 均为 migration 内部受控值(非 HTTP 输入),无 SQL 注入面"这段(参数化后该假设不再是安全依据),改为"SQL 已参数化($1/$2),对任意输入安全"。

    在 menu_grant_helpers_test.go 的 TestGrantNewMenuToRolesHavingParent_ParameterizedOrControlled 中追加断言:源码不含 "fmt.Sprintf"、含 "$1::uuid"、含 "$2"。
  </action>
  <verify>
    <automated>cd D:/code/ClaudeCode/guoguo && go build ./... && go test ./internal/core/db/migrations/ -run "TestGrantNewMenuToRolesHavingParent" -v</automated>
  </verify>
  <done>
    SQL 参数化完成;既有 4 片段断言与新增无 Sprintf 断言同时 PASS;go build ./... 退出码 0
  </done>
  <acceptance_criteria>
    - grep -c "fmt.Sprintf" internal/core/db/migrations/menu_grant_helpers.go 输出 0
    - grep -q '\$1::uuid' internal/core/db/migrations/menu_grant_helpers.go
    - grep -q 'menu_name = \$2' internal/core/db/migrations/menu_grant_helpers.go
    - go test ./internal/core/db/migrations/ -run TestGrantNewMenuToRolesHavingParent -v 全部 PASS(含既有片段断言,证明 SQL 结构未漂移)
    - go build ./... 退出码 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 导出 helper → DB | GrantNewMenuToRolesHavingParent 是导出函数,未来调用方可能传入非受控值 |
| GORM 日志 → 应用日志 | FilterLogger 决定什么 DB 行为可见,死配置造成"有慢查询监控"的虚假安全感 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-04 | Tampering | GrantNewMenuToRolesHavingParent SQL 插值 | mitigate | $1::uuid / $2 参数化,消除任意输入的注入面;源码断言 fmt.Sprintf 出现次数为 0 |
| T-62-05 | Repudiation | 慢查询不可见(死 SlowThreshold) | mitigate | Trace 实现耗时判定 + applogger.Warnf,慢库问题可观测 |
| T-62-SC | Tampering | 依赖安装 | accept | 本 plan 不引入任何新依赖,无包安装面 |
</threat_model>

<verification>
- go build ./... 退出码 0
- go test ./internal/core/db/ ./internal/core/db/migrations/ -v 全部 PASS(含既有 menu_grant_helpers 4 测试不回归)
- 默认配置下 FilterLogger 输出行为与修复前一致(全静默普通 SQL + 错误保留),仅新增慢查询 WARN
</verification>

<success_criteria>
1. 慢查询日志真实生效(SlowThreshold=1000ms 默认),不再是有配置无实现
2. MinLevel/LogMode 语义真实,默认行为不回归
3. grant helper 注入面消除,SQL 结构与授权语义零变化
</success_criteria>

<output>
创建 .planning/phases/62-ai-internal-core-db/62-02-SUMMARY.md,记录:C4 选择"实现"而非"删除"的理由、C6 参数化前后 SQL 对照、测试覆盖清单。
</output>
