---
phase: 62-ai-internal-core-db
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/core/db/migrations/migration_176_reconciliation_physical_mv.go
  - internal/core/db/migrations/migration_175_reconciliation_physical_link.go
  - internal/core/db/migrations/migration_176_reconciliation_physical_mv_test.go
autonomous: true
requirements: ["C1", "CDX-M-IDX"]
must_haves:
  truths:
    - "C1: 当 reconciliation_normalized MV 存在但缺少 R5 标记列(asset_username / physical_user_id / last_resolved_at / mv_refreshed_at 任一)时,启动迁移走 DROP+CREATE 慢路径而非 REFRESH 旧结构"
    - "C1: ops_asset_physical 回填在快路径与慢路径均执行(不再仅慢路径)"
    - "C1: 历史 Type E 批量标记 resolved 仅在 ops_asset_physical 存在数据时执行,且每次执行以 applogger.Warnf 记录 RowsAffected"
    - "CDX-M-IDX: reconciliation_user_lookup 的 sys_user.nickname 标量子查询有 idx_sys_user_nickname 部分索引支撑"
    - "CDX-M-IDX: MV last_resolved LATERAL 子查询有 idx_recon_resolved_asset_time 部分索引支撑"
    - "幂等: 所有新增 DDL 使用 IF NOT EXISTS / CREATE OR REPLACE,迁移函数在 sqlite 方言下连续调用两次返回 nil"
  artifacts:
    - path: "internal/core/db/migrations/migration_176_reconciliation_physical_mv.go"
      provides: "快路径 schema 版本校验 + 双路径回填 + 门控 Type E 清理 + 支撑索引"
      contains: "information_schema.columns"
    - path: "internal/core/db/migrations/migration_175_reconciliation_physical_link.go"
      provides: "idx_sys_user_nickname 支撑索引"
      contains: "idx_sys_user_nickname"
    - path: "internal/core/db/migrations/migration_176_reconciliation_physical_mv_test.go"
      provides: "源码断言 + sqlite 方言守卫双调幂等测试"
      contains: "TestMigrate176"
  key_links:
    - from: "migration_176 快路径"
      to: "慢路径 DROP+CREATE"
      via: "R5 标记列集合校验失败时回退"
      pattern: "information_schema\\.columns"
    - from: "migration_176 Type E 清理"
      to: "ops_asset_physical 前置条件"
      via: "EXISTS 门控"
      pattern: "ops_asset_physical"
---

<objective>
加固 Migrate176 升级路径(评审共识 C1,两个评审者共同标记的最高优先级问题),并为 175/176 的标量子查询补支撑索引(codex MEDIUM,用户锁定清单内)。

Purpose: 当前快路径只检查 MV 存在性——R1/R2 旧结构 MV 会被 REFRESH 且"验证通过",读取新列的对账服务运行时才失败;破坏性 UPDATE 会把真实 Type E 告警静默标记为已解决;回填只在慢路径跑。
Output: 快路径带 schema 校验可自愈升级、双路径回填、门控 + WARN 的 Type E 清理、两个部分索引。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/62-ai-internal-core-db/62-REVIEWS.md
@internal/core/db/migrations/migration_176_reconciliation_physical_mv.go
@internal/core/db/migrations/migration_175_reconciliation_physical_link.go
@internal/core/db/migrations/migration_helpers.go
@internal/core/db/migrations/menu_grant_helpers_test.go

<interfaces>
<!-- 既有契约,executor 直接使用,无需再探索 -->

migration_helpers.go:
```go
func isPostgreSQL(db *gorm.DB) bool // db.Config.Dialector.Name() == "postgres"
```

menu_grant_helpers_test.go 既有测试模式(必须沿用):
- sqlite 内存库: gorm.Open(sqlite.Open("file::memory:?cache=shared&_busy_timeout=5000"), &gorm.Config{})
- PG 路径测试: if !postgresAvailable() { t.Skip(...) },postgresAvailable() 检查 XINGRAN_PG_TEST_DSN
- 源码断言测试: os.ReadFile + strings.Contains 断言 SQL 片段

migration_176 当前 MV 的 18 列(慢路径 createMV 的 SELECT 列表,schema 校验的参照):
asset_id, asset_code, asset_ip, mac1, mac2, mac_join, asset_user_id, asset_username,
asset_deleted_at, physical_user_id, physical_username, ad_id, ad_username, ad_is_enabled,
mv_refreshed_at, last_resolved_at, last_resolved_by, last_conflict_type

R5 标记列(用于区分 R1/R2 旧结构,校验这 4 列即可):asset_username, physical_user_id, last_resolved_at, mv_refreshed_at
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate176 快路径 schema 校验 + 双路径回填 + Type E 清理门控 (C1)</name>
  <files>internal/core/db/migrations/migration_176_reconciliation_physical_mv.go, internal/core/db/migrations/migration_176_reconciliation_physical_mv_test.go</files>
  <read_first>
    - internal/core/db/migrations/migration_176_reconciliation_physical_mv.go (当前实现,快路径在 line 83-110,慢路径 line 112-293)
    - internal/core/db/migrations/migration_helpers.go (isPostgreSQL 守卫)
    - internal/core/db/migrations/menu_grant_helpers_test.go (既有测试模式:sqlite skip + 源码断言)
    - .planning/phases/62-ai-internal-core-db/62-REVIEWS.md §C1 (codex HIGH "Destructive data rewrite" + opencode HIGH "MV fast path does not verify schema version")
  </read_first>
  <behavior>
    - Test 1 (源码断言): migration_176 源码含 "information_schema.columns"(schema 校验)、"backfillOpsAssetPhysical"(回填提取为函数)、"applogger.Warnf" 在 Type E 清理路径(WARN 每次记录)
    - Test 2 (源码断言): Type E 清理 SQL 前置条件含 "ops_asset_physical" 的 EXISTS 探测
    - Test 3 (sqlite 幂等): sqlite 内存库上连续调用 Migrate176ReconciliationPhysicalMV 两次,均返回 nil(方言守卫路径)
  </behavior>
  <action>
    对 migration_176_reconciliation_physical_mv.go 做三处修改(保持全部既有日志风格 applogger + log.Println 不动,不在本任务统一日志——属 deferred LOW 项):

    1) 快路径 schema 版本校验:在 mvExists.Exists 探测之后,新增查询 information_schema.columns 取 reconciliation_normalized 的 column_name 集合,校验 4 个 R5 标记列 asset_username / physical_user_id / last_resolved_at / mv_refreshed_at 全部存在。重构控制流为:若 mvExists.Exists 且 schemaOK → 走现有快路径(REFRESH CONCURRENTLY)后 return nil;若 mvExists.Exists 且 !schemaOK → applogger.Warnf("[迁移 176] 检测到旧版本 MV schema(缺 R5 标记列),走 DROP+CREATE 升级路径"),继续落入慢路径;若 !mvExists.Exists → 慢路径(现有行为)。

    2) 回填双路径执行:把现有慢路径第 5 步的 backfillSQL(INSERT INTO ops_asset_physical ... ON CONFLICT (asset_id) DO UPDATE)提取为包内私有函数 backfillOpsAssetPhysical(db *gorm.DB),失败时 applogger.Warnf 非致命(与现有一致)。在快路径 REFRESH CONCURRENTLY 成功之后、轻量验证之前调用一次;慢路径保留原调用点改为调用该函数。

    3) Type E 清理门控 + WARN:在慢路径第 7 步 cleanupSQL 执行前,先执行 SELECT EXISTS (SELECT 1 FROM ops_asset_physical LIMIT 1);为 false 时 applogger.Infof 跳过清理并 return nil(说明:R5 物理链路从未回填,Type E 可能是真实告警);为 true 时执行现有 UPDATE,成功分支的日志从 applogger.Infof 改为 applogger.Warnf("[迁移 176] 历史 Type E 已批量标记为 resolved,受影响行数: %d", res.RowsAffected)(每次执行都 WARN,不只首次)。

    4) 修正文件顶部 docstring 漂移:当前注释写"历史 Type E 处理:不主动 UPDATE",与实际行为相反——改为"历史 Type E 处理:门控 UPDATE,仅在 ops_asset_physical 有数据时批量 resolved 并 WARN 记录行数"。

    新建 migration_176_reconciliation_physical_mv_test.go,按 behavior 三条编写;PG 功能路径沿用 postgresAvailable()/XINGRAN_PG_TEST_DSN skip 模式,不新建 PG 依赖。
  </action>
  <verify>
    <automated>cd D:/code/ClaudeCode/guoguo && go build ./... && go test ./internal/core/db/migrations/ -run "TestMigrate176" -v</automated>
  </verify>
  <done>
    快路径带 schema 校验 + 回填,慢路径门控清理;go build ./... 退出码 0;新增测试全部 PASS
  </done>
  <acceptance_criteria>
    - grep -q "information_schema.columns" internal/core/db/migrations/migration_176_reconciliation_physical_mv.go
    - grep -q "backfillOpsAssetPhysical" internal/core/db/migrations/migration_176_reconciliation_physical_mv.go 且该函数在源码中出现 >= 3 次(定义 1 + 快慢路径调用 2):grep -c "backfillOpsAssetPhysical" 输出 >= 3
    - grep -q "SELECT 1 FROM ops_asset_physical LIMIT 1" internal/core/db/migrations/migration_176_reconciliation_physical_mv.go
    - Type E 成功日志为 Warnf:grep "RowsAffected" internal/core/db/migrations/migration_176_reconciliation_physical_mv.go | grep -q "Warnf"
    - 顶部 docstring 不再含"不主动 UPDATE":grep -c "不主动 UPDATE" internal/core/db/migrations/migration_176_reconciliation_physical_mv.go 输出 0
    - 幂等性证明:go test ./internal/core/db/migrations/ -run TestMigrate176_SqliteDoubleInvocation 连续执行两次迁移函数均返回 nil 的测试 PASS;所有 DDL 语句源码断言含 IF NOT EXISTS / CREATE OR REPLACE / ON CONFLICT
    - go build ./... 退出码 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 支撑索引 — 175 idx_sys_user_nickname + 176 idx_recon_resolved_asset_time (CDX-M-IDX)</name>
  <files>internal/core/db/migrations/migration_175_reconciliation_physical_link.go, internal/core/db/migrations/migration_176_reconciliation_physical_mv.go, internal/core/db/migrations/migration_176_reconciliation_physical_mv_test.go</files>
  <read_first>
    - internal/core/db/migrations/migration_175_reconciliation_physical_link.go (reconciliation_user_lookup 视图的 nickname 标量子查询在 line 121-148)
    - internal/core/db/migrations/migration_176_reconciliation_physical_mv.go (last_resolved LATERAL 子查询在 line 188-196)
    - internal/core/db/migrations/migration_202_port_write_audit.go:44-53 (防御索引既有模式:CREATE INDEX IF NOT EXISTS 列表循环)
    - Task 1 完成后的 migration_176_reconciliation_physical_mv_test.go
  </read_first>
  <behavior>
    - Test 1 (源码断言): 175 源码含 "idx_sys_user_nickname" 与 "ON sys_user(nickname)" 与 "WHERE deleted_at IS NULL"
    - Test 2 (源码断言): 176 源码含 "idx_recon_resolved_asset_time" 与 "sys_data_reconciliation(asset_id, resolved_at DESC)"
    - Test 3 (sqlite 幂等): 175 在 sqlite 上连续两次调用返回 nil(方言守卫)
  </behavior>
  <action>
    1) migration_175:在创建 reconciliation_user_lookup 视图成功之后、轻量验证之前,新增执行 CREATE INDEX IF NOT EXISTS idx_sys_user_nickname ON sys_user(nickname) WHERE deleted_at IS NULL。该索引支撑视图内 su.nickname = a.nowuser_name 的逐行标量子查询(6688 资产 × sys_user 全表扫描 → 索引查找)。失败处理:return fmt.Errorf 包装(与 175 既有 DDL 失败风格一致;database.go 调用方对 175 失败本就是 Errorf 非阻断)。

    2) migration_176:在 isPostgreSQL 守卫之后、MV 存在性探测之前(快慢路径都受益的位置),新增执行 CREATE INDEX IF NOT EXISTS idx_recon_resolved_asset_time ON sys_data_reconciliation(asset_id, resolved_at DESC) WHERE deleted_at IS NULL。该索引支撑 MV 定义里 last_resolved LATERAL 子查询的 ORDER BY resolved_at DESC LIMIT 1。失败处理:applogger.Warnf 非阻断(sys_data_reconciliation 在全新库可能尚未由上游建表,下次启动 IF NOT EXISTS 自愈)。

    3) 在 Task 1 新建的测试文件中追加 behavior 三条断言。
  </action>
  <verify>
    <automated>cd D:/code/ClaudeCode/guoguo && go build ./... && go test ./internal/core/db/migrations/ -v</automated>
  </verify>
  <done>
    两个部分索引就位;迁移包全部测试(含既有 menu_grant_helpers_test.go / migration_202 测试)PASS;go build ./... 退出码 0
  </done>
  <acceptance_criteria>
    - grep -q "idx_sys_user_nickname ON sys_user(nickname)" internal/core/db/migrations/migration_175_reconciliation_physical_link.go
    - grep -q "WHERE deleted_at IS NULL" internal/core/db/migrations/migration_175_reconciliation_physical_link.go(部分索引谓词)
    - grep -q "idx_recon_resolved_asset_time ON sys_data_reconciliation(asset_id, resolved_at DESC)" internal/core/db/migrations/migration_176_reconciliation_physical_mv.go
    - 两条索引语句均为 CREATE INDEX IF NOT EXISTS(幂等):grep -c "CREATE INDEX IF NOT EXISTS idx_sys_user_nickname\|CREATE INDEX IF NOT EXISTS idx_recon_resolved_asset_time" 两个文件合计输出 2
    - 幂等性证明:TestMigrate175_SqliteDoubleInvocation(或等名)测试 PASS,sqlite 上两次调用均 nil
    - go build ./... 退出码 0;go test ./internal/core/db/migrations/ 全部 PASS
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 启动迁移 → 生产 DB | 迁移在每次启动以 DB owner 权限执行 DDL/DML,错误逻辑直接改写告警与审计数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01 | Tampering | Migrate176 Type E 批量 UPDATE | mitigate | EXISTS (SELECT 1 FROM ops_asset_physical LIMIT 1) 前置门控 + 每次 applogger.Warnf RowsAffected(审计可见) |
| T-62-02 | Tampering | Migrate176 快路径刷新旧结构 MV | mitigate | R5 标记列(asset_username/physical_user_id/last_resolved_at/mv_refreshed_at)information_schema 校验,缺失即回退 DROP+CREATE |
| T-62-03 | Denial of Service | 逐行标量子查询扫全表 | mitigate | idx_sys_user_nickname + idx_recon_resolved_asset_time 两个部分索引 |
| T-62-SC | Tampering | 依赖安装 | accept | 本 plan 不引入任何新依赖(纯 Go 标准库 + 既有 gorm/applogger),无包安装面 |
</threat_model>

<verification>
- go build ./... 退出码 0
- go test ./internal/core/db/migrations/ -v 全部 PASS(含既有测试不回归)
- 幂等:迁移函数 sqlite 路径双调 nil;全部新 DDL 为 IF NOT EXISTS / CREATE OR REPLACE / ON CONFLICT
- PG 功能路径(REFRESH/校验/门控的真实行为)由下次 dev PG 启动覆盖,与本项目既有 "PG functional 由 UAT/启动覆盖" 惯例一致
</verification>

<success_criteria>
1. 旧结构 MV 存在时启动自动走 DROP+CREATE 升级,不再静默 REFRESH 旧 schema
2. Type E 清理有前置门控且每次 WARN 可审计
3. 回填不再因快路径而永久缺失
4. 两个支撑索引源码就位且幂等
</success_criteria>

<output>
创建 .planning/phases/62-ai-internal-core-db/62-01-SUMMARY.md,记录:C1 三个子问题的修复点、索引选择理由、PG 功能验证的延期说明。
</output>
