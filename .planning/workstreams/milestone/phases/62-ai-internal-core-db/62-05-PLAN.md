---
phase: 62-ai-internal-core-db
plan: 05
type: execute
wave: 2
depends_on: ["62-04"]
files_modified:
  - internal/core/db/database.go
  - internal/core/core.go
autonomous: true
requirements: ["C7", "CDX-H2"]
must_haves:
  truths:
    - "C7: BootstrapMissingTables 不再含硬编码 CREATE TABLE DDL(APIKey schema 的第三份拷贝消除),表结构由 models.APIKey / models.APIKeyUsageLog 经 gorm.Migrator().CreateTable 派生"
    - "C7: 六条 CREATE INDEX IF NOT EXISTS 显式索引语句保留(幂等,覆盖 model tag 之外的索引面)"
    - "C7: 硬编码 public. schema 前缀消除(跟随 search_path)"
    - "CDX-H2: SKIP_AUTOMIGRATE=true 且 server.mode=release 时 initDBAndData 返回错误,启动终止;debug 模式下 WARN 旁路保留(dev 应急能力不丢)"
  artifacts:
    - path: "internal/core/db/database.go"
      provides: "CreateTable 派生的 BootstrapMissingTables"
      contains: "Migrator().CreateTable"
    - path: "internal/core/core.go"
      provides: "SKIP_AUTOMIGRATE 生产模式 fatal 守卫"
      contains: "SKIP_AUTOMIGRATE"
  key_links:
    - from: "BootstrapMissingTables"
      to: "models.APIKey / models.APIKeyUsageLog"
      via: "gorm.Migrator().CreateTable 单一事实源"
      pattern: "Migrator\\(\\)\\.CreateTable"
    - from: "core.go initDBAndData"
      to: "c.Config.Server.Mode"
      via: "release 模式下 SKIP_AUTOMIGRATE 返回错误"
      pattern: "Server\\.Mode"
---

<objective>
修复共识 C7(BootstrapMissingTables 硬编码 DDL 是 APIKey schema 的第三份拷贝,与 model/MigrateModelList 漂移风险——正是 MigrateModelList 抽取时要防的那类 bug)与 codex HIGH(SKIP_AUTOMIGRATE=true 跳过范围超预期,新库得到半初始化系统,生产误用应 fatal)。

Purpose: SKIP_AUTOMIGRATE 是 Supabase pooler 的 dev 应急旁路;当前它在生产模式也静默生效,且旁路补建的表结构是手写第三份拷贝,model 加列即漂移。
Output: model 派生的 bootstrap 建表 + 生产模式 fatal 守卫。

**依赖说明(为何 Wave 2):** 本 plan 与 Plan 04 共享 database.go,必须串行。Plan 04 落地后 database.go 已含 advisory lock / UTC / 回退告警,本 plan 在其之上改 BootstrapMissingTables 函数体(line 548-601 区域)即可,两处修改不相邻,冲突风险低。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/62-ai-internal-core-db/62-REVIEWS.md
@internal/core/db/database.go
@internal/core/core.go
@internal/models/api_key.go

<interfaces>
<!-- 既有契约,executor 直接使用 -->

database.go BootstrapMissingTables 当前结构(line 538-601):
- ddl 切片含 2 个 CREATE TABLE IF NOT EXISTS public.sys_api_keys / public.sys_api_key_usage_logs(硬编码列清单,第三份拷贝)
- 6 个 CREATE INDEX IF NOT EXISTS:idx_api_keys_user_id / idx_api_keys_key_prefix / idx_api_keys_deleted_at / idx_api_key_logs_api_key_id / idx_api_key_logs_created_at / idx_api_key_logs_user_id
- 逐条 d.DB.Exec,失败 return fmt.Errorf("DDL[%d] failed: %w", i+1, err)

调用点(唯一):internal/core/core.go:277-284
```go
if os.Getenv("SKIP_AUTOMIGRATE") == "true" {
    applogger.Warnf("[SKIP_AUTOMIGRATE=true] 跳过 AutoMigrate,改用 inline DDL 补建")
    if err := c.DB.BootstrapMissingTables(); err != nil {
        return fmt.Errorf("BootstrapMissingTables 失败: %w", err)
    }
} else if err := c.DB.AutoMigrate(); err != nil { ... }
```

config:c.Config.Server.Mode(string,"debug" / "release",configs/config.prod.yaml 为 release)。

models.APIKey / models.APIKeyUsageLog 已在 MigrateModelList 注册(database.go:322-323),CreateTable 与 AutoMigrate 同源,天然防漂移。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BootstrapMissingTables 改用 gorm.Migrator().CreateTable (C7)</name>
  <files>internal/core/db/database.go</files>
  <read_first>
    - internal/core/db/database.go:538-601 (BootstrapMissingTables 当前硬编码 DDL)
    - internal/models/api_key.go (APIKey/APIKeyUsageLog 的 gorm tag——CreateTable 的输入)
    - internal/core/db/database.go:281-385 (MigrateModelList 的单一事实源注释,line 321-323 APIKey 注册说明)
    - .planning/phases/62-ai-internal-core-db/62-REVIEWS.md §C7 与 codex suggestion #3
    - Plan 04 落地后的 database.go 全文(advisory lock 等修改已就位,避免覆盖)
  </read_first>
  <behavior>
    - Test 1(源码断言): BootstrapMissingTables 函数体含 Migrator().CreateTable 且含 models.APIKey 与 models.APIKeyUsageLog 两个模型引用
    - Test 2(源码断言): database.go 不再含 "CREATE TABLE IF NOT EXISTS public.sys_api_keys" 硬编码 DDL
    - Test 3(源码断言): 六条 idx_api_keys_* / idx_api_key_logs_* 的 CREATE INDEX IF NOT EXISTS 语句全部保留
  </behavior>
  <action>
    重写 BootstrapMissingTables 函数体(database.go,函数签名与导出名不变,core.go 调用点零改动):

    1) 删除 ddl 切片中的两个硬编码 CREATE TABLE 块(含 public. 前缀的整段列清单)。

    2) 替换为:先 d.DB.Migrator().HasTable(&models.APIKey{}) 判定,缺失则 d.DB.Migrator().CreateTable(&models.APIKey{}),失败 return fmt.Errorf("创建 sys_api_keys 失败: %w", err);sys_api_key_usage_logs 同理(CreateTable(&models.APIKeyUsageLog{}))。注释说明:CreateTable 在 PrepareStmt:false 连接上走 simple protocol,无 AutoMigrate 80+ DDL 批量路径的 pooler 死锁(原硬编码 DDL 的存在理由),且 schema 与 model 同源,消除第三份拷贝。

    3) 保留全部 6 条 CREATE INDEX IF NOT EXISTS 显式语句(从原 ddl 切片中摘出继续逐条 Exec,IF NOT EXISTS 幂等;model tag 未覆盖的索引面由它们兜底),失败处理保持 return fmt.Errorf("DDL[%d] failed: %w") 风格。

    4) 更新函数 docstring:删除"直接用 raw SQL 补建缺失表"描述,改为"经 gorm.Migrator().CreateTable 从 model 派生建表 + 显式索引兜底";保留 Supabase pooler 背景说明。

    测试:database.go 的源码断言追加到 Plan 04 创建的 database_test.go(若该文件存在则追加用例,否则新建;os.ReadFile + strings.Contains 模式)。
  </behavior>
  <verify>
    <automated>cd D:/code/ClaudeCode/guoguo && go build ./... && go test ./internal/core/db/ -v</automated>
  </verify>
  <done>
    硬编码 CREATE TABLE 消除、CreateTable 派生就位、索引兜底保留;源码断言 PASS;go build ./... 退出码 0
  </done>
  <acceptance_criteria>
    - grep -c "CREATE TABLE IF NOT EXISTS public.sys_api_keys" internal/core/db/database.go 输出 0
    - grep -q "Migrator().CreateTable(&models.APIKey{})" internal/core/db/database.go
    - grep -q "Migrator().CreateTable(&models.APIKeyUsageLog{})" internal/core/db/database.go
    - grep -c "CREATE INDEX IF NOT EXISTS idx_api_keys_\|CREATE INDEX IF NOT EXISTS idx_api_key_logs_" internal/core/db/database.go 输出 6
    - go build ./... 退出码 0;go test ./internal/core/db/ -v 全部 PASS(含 Plan 04 测试不回归)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SKIP_AUTOMIGRATE 生产模式 fatal 守卫 (CDX-H2)</name>
  <files>internal/core/core.go</files>
  <read_first>
    - internal/core/core.go:258-285 (initDBAndData 的 SKIP_AUTOMIGRATE 分支)
    - internal/config/config.go:47-66 (ServerConfig.Mode 字段,mapstructure:"mode")
    - configs/config.prod.yaml (确认生产配置 mode: release)与 configs/config.dev.yaml (mode: debug)
    - .planning/phases/62-ai-internal-core-db/62-REVIEWS.md codex "SKIP_AUTOMIGRATE=true + BootstrapMissingTables is incomplete" 与 suggestion #8
  </read_first>
  <behavior>
    - Test 1(源码断言): core.go 的 SKIP_AUTOMIGRATE 分支前存在 server.mode 判定,release 时 return fmt.Errorf(启动终止)
    - Test 2(源码断言): debug/非 release 模式的 WARN 旁路日志保留(dev 应急能力不丢)
  </behavior>
  <action>
    修改 core.go initDBAndData(line 277 分支之前):

    在 if os.Getenv("SKIP_AUTOMIGRATE") == "true" 分支体的最前面(Warnf 之前)插入生产守卫:if c.Config.Server.Mode == "release" 时 return fmt.Errorf("SKIP_AUTOMIGRATE=true 禁止在生产模式(server.mode=release)使用:旁路补建仅覆盖部分表,会产生半初始化系统;请移除该环境变量后重启")。非 release 模式保持现有 Warnf + BootstrapMissingTables 旁路不变。

    同步更新该分支上方注释块(line 274-276):补充"生产模式(mode=release)下此开关 fatal;BootstrapMissingTables 仅保证 api key 两表,不跑 175/176/202-205 迁移,属 dev 应急"。

    测试:在 internal/core/ 下新建或追加源码断言测试文件(若 core 包已有 *_test.go 则选合适文件追加,否则新建 core_skipautomigrate_test.go;os.ReadFile("core.go") + strings.Contains 断言 behavior 两条)。注意 core 包测试编译成本——若 core 包既有测试存在则跟随其模式;若 core 包无任何测试文件,新建最小测试文件并确认 go test ./internal/core/ 可编译运行。
  </behavior>
  <verify>
    <automated>cd D:/code/ClaudeCode/guoguo && go build ./... && go test ./internal/core/ -v</automated>
  </verify>
  <done>
    生产模式 SKIP_AUTOMIGRATE 启动即 fatal 且错误信息指明原因与处置;debug 旁路保留;测试 PASS;go build ./... 退出码 0
  </done>
  <acceptance_criteria>
    - grep -q 'SKIP_AUTOMIGRATE' internal/core/core.go 且 grep -q 'Server.Mode == "release"' internal/core/core.go
    - 守卫 return 错误文案含 "半初始化":grep -q "半初始化" internal/core/core.go
    - debug 旁路保留:grep -q "BootstrapMissingTables()" internal/core/core.go
    - go build ./... 退出码 0;go test ./internal/core/ -v PASS(含新增源码断言)
    - go test ./... 全量无回归(本 phase 收尾门)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 环境变量 → 启动迁移路径 | SKIP_AUTOMIGRATE 由部署环境注入,误设直接改变 schema 初始化路径 |
| model 定义 → bootstrap DDL | 硬编码第三份拷贝与 model 漂移时,旁路路径建出错误表结构 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-12 | Tampering | 生产误设 SKIP_AUTOMIGRATE → 半初始化系统 | mitigate | server.mode=release 时 initDBAndData 返回错误,启动 fail-fast |
| T-62-13 | Tampering | bootstrap DDL 与 model 漂移 → 错误表结构 | mitigate | gorm.Migrator().CreateTable 从 models.APIKey/APIKeyUsageLog 派生,单一事实源;显式索引 IF NOT EXISTS 兜底 |
| T-62-SC | Tampering | 依赖安装 | accept | 本 plan 不引入任何新依赖,无包安装面 |
</threat_model>

<verification>
- go build ./... 退出码 0
- go test ./... 全量 PASS(本 phase 收尾门,含 Plan 01-04 全部新测试)
- SKIP_AUTOMIGRATE dev 旁路真实行为(pooler 环境)由 dev 部署惯例覆盖
</verification>

<success_criteria>
1. APIKey schema 只剩 model 一份事实源,bootstrap 不再可能漂移
2. 生产模式下半初始化旁路被硬性封死,dev 应急能力保留
3. Phase 62 全部 5 plan 完成,C1-C7 + 锁定单方项清零
</success_criteria>

<output>
创建 .planning/phases/62-ai-internal-core-db/62-05-SUMMARY.md,记录:C7 重构前后对照、fatal 守卫的触发条件与文案、全 phase 的 deferred 清单汇总(强制首登改密、LOW 项、schema_migrations 版本表建议)。
</output>
