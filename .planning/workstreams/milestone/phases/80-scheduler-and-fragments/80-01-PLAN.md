---
phase: 80-scheduler-and-fragments
plan: 01
status: pending
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/scheduler/cron_80_01_test.go
autonomous: true
requirements: [TAIL-02]

must_haves:
  truths:
    - "按 D-80-02 裁决:引擎测试零 cron 触发时序断言 —— 直调 JobExecutor.Execute / Scheduler.ExecuteJob,Start/Stop 生命周期用例不 sleep、不等实时时钟;SC-1 的『引擎并发』由既有 cron_test.go 并发范式 + 新增 GetJobCount/GetJobStatus 并发读覆盖"
    - "cron.go(388 stmts @1.5%)≥70%(≥272 covered,力争 ≥85%):JobExecutor.Execute/executeTask 分发(注册 handler 计数器验证)、parseInvokeTarget/calculateNextRunTime 纯表驱动、defaultLogger 三法直调 + SetLogger 注入、Scheduler 10 个公开方法(Start/Stop/AddJob/UpdateJob/RemoveJob/StartJob/StopJob/ExecuteJob/GetJobStatus/GetJobCount)各有用例"
    - "StartJob/StopJob 的 status 断言引用 models.JobStatusNormal / models.JobStatusPause 常量(禁裸 0/1 字面量,CLAUDE.md 状态值惯例);JobLog 断言引用 models.JobLogStatusSuccess / JobLogStatusFailure"
    - "goroutine 纪律(R2):凡 Start 必须 t.Cleanup(s.Stop);不测 Start 的用例一律不起 cron(AddJob 在 !running 分支只落库,cron.go:294-297);任务 handler 内绝不调 Scheduler 方法(handler 用闭包计数器纯函数)"
    - "var seams(SetNoticeHub/GetNoticeHub、SetDB/GetDB、SetGlobalScheduler/GetGlobalScheduler,cron.go:494-568)按 77-05 纪律 save → t.Cleanup restore,全文件禁 t.Parallel"
  artifacts:
    - path: "internal/scheduler/cron_80_01_test.go"
      provides: "引擎 fixture(sys_job/sys_job_log)+ 注册表 + 执行器 + 公开方法 + 生命周期 + var-seam 全量测试(前缀 TestReg8001_/TestExec8001_/TestJob8001_/TestLife8001_/TestSeam8001_)"
      min_lines: 420
      contains: "NewScheduler"
  key_links:
    - from: "internal/scheduler/cron_80_01_test.go"
      to: "internal/scheduler/cron.go:177 NewScheduler(db *gorm.DB)"
      via: "sqlite(t.TempDir 文件库)+ models.Job/models.JobLog AutoMigrate → NewScheduler 装配"
      pattern: "NewScheduler|AutoMigrate"
    - from: "TestJob8001_StartStopJob 系"
      to: "internal/models/log.go:71 JobStatusNormal/JobStatusPause"
      via: "StartJob/StopJob 后重读 models.Job 行断言 Status 常量"
      pattern: "JobStatusNormal|JobStatusPause"
    - from: "TestSeam8001_ 系"
      to: "internal/scheduler/cron.go:494-568 全局 var seams"
      via: "save → Set → Get 断言 → t.Cleanup restore"
      pattern: "SetNoticeHub|SetDB|SetGlobalScheduler"
---

# Plan 1: scheduler 注册表 + job 执行器 (Phase: 80-scheduler-and-fragments)

<objective>
交付 internal/scheduler 引擎主体 cron.go(388 stmts @1.5%,包内最大单文件)的全量测试:JobExecutor(Execute/executeTask/parseInvokeTarget/calculateNextRunTime)、Scheduler 10 个公开方法、defaultLogger 与 Logger 接口注入、三组全局 var seams。目标 cron.go ≥70%(≥272 covered,力争 ≥85%)。

Purpose: TAIL-02(SC-1)的第一段。cron.go 是 80-02 任务族的 handler 注册底座 —— 本 plan 沉淀的 sqlite 引擎 fixture(newSchedDB8001/newScheduler8001/registerCountingTask8001)与 var-seam 纪律直接被 80-02 六个 *_tasks.go 测试复用;cron.go 的实测落点决定 80-02 的任务族覆盖压力(两 plan 逐文件 ≥70% ⇒ 包 ≥70%)。
Output: 新增 1 个测试文件(零生产代码改动,D-80 escape hatch 除外);80-02 可复用的引擎 fixture。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-RESEARCH.md

<interfaces>
<!-- 关键契约(planner 已逐一读源码复核,2026-08-28)。executor 直接使用,无需重新探索。 -->

From internal/scheduler/cron.go:
```go
const defaultShutdownTimeout = 5 * time.Second      // :24 — Stop 等待上限

type JobTask interface{ Execute(ctx context.Context) error }          // :27
type JobExecutor struct { job *models.Job; db *gorm.DB; logger Logger; scheduler *Scheduler } // :31
func (e *JobExecutor) Execute(ctx context.Context) error              // :34 — 建 JobLog(Status=0 起点)→ executeTask → 回写 prev/next_run_time + 存日志
func (e *JobExecutor) executeTask(ctx, jobLog) error                  // :80 — parseInvokeTarget → registry 查 handler → 调用;未注册 → 失败日志
func (e *JobExecutor) parseInvokeTarget(invokeTarget string) (string, map[string]interface{}) // :109 — 纯:"type" 或 `type:{"k":v}`;坏 JSON → params nil
func (e *JobExecutor) calculateNextRunTime() *time.Time               // :124 — 临时 cron Start/Stop(有 Stop 不泄漏;勿循环上千次)
type Logger interface { Infof/Warnf/Errorf(format string, args ...interface{}) } // :143
type defaultLogger struct{}  // :152-163 三法直调可测

type Scheduler struct { /* :165 — mu / cron / executors / taskRegistry / db / logger / running */ }
func NewScheduler(db *gorm.DB) *Scheduler                          // :177
func (s *Scheduler) SetLogger(logger Logger)                        // :197
func (s *Scheduler) RegisterTask(taskType string, handler func(ctx context.Context, params map[string]interface{}) error) // :202
func (s *Scheduler) GetTaskHandler(taskType string) func(...) error // :209 — RLock;Stop 持锁期间调用会阻塞到超时(R2 禁在任务体内调)
func (s *Scheduler) IsTaskRegistered(taskType string) bool          // :217
func (s *Scheduler) Start(ctx context.Context) error                // :225 — 起 robfig/cron goroutine(:246)
func (s *Scheduler) Stop()                                          // :254 — 先 s.mu.Lock() 再等 cron.Stop() ctx,上限 5s(:254-277)
func (s *Scheduler) AddJob(job *models.Job) error                   // :280 — !running 分支只落库(:294-297)
func (s *Scheduler) addJob(job *models.Job) error                   // :301 — running 内部路径
func (s *Scheduler) UpdateJob(job *models.Job) error                // :337
func (s *Scheduler) RemoveJob(jobID string) error                   // :361 / removeJob :368
func (s *Scheduler) StartJob(jobID string) error                    // :386 — Status → JobStatusNormal
func (s *Scheduler) StopJob(jobID string) error                     // :417 — Status → JobStatusPause
func (s *Scheduler) ExecuteJob(jobID string) error              // :445 — 载入 job → JobExecutor.Execute
func (s *Scheduler) GetJobStatus(jobID string) (bool, error)        // :471
func (s *Scheduler) GetJobCount() (total, running int)              // :480
```

Var seams(cron.go:494-568,同包测试直写合法):
```go
type NoticeHub interface { BroadcastToUsers(userIDs []string, message interface{}) } // :496
var (GlobalNoticeHub NoticeHub; GlobalNoticeHubMu sync.RWMutex)     // :501
func SetNoticeHub(hub NoticeHub) / func GetNoticeHub() NoticeHub    // :507/:514
type DBGetter interface { GetDB() *gorm.DB }                        // :520
var (GlobalDB DBGetter; GlobalDBMu sync.RWMutex)                    // :525
func SetDB(db DBGetter) / func GetDB() DBGetter                     // :531/:538
var (GlobalScheduler *Scheduler; GlobalSchedulerMu sync.RWMutex)    // :545
func SetGlobalScheduler(s *Scheduler) / func GetGlobalScheduler() *Scheduler // :552/:559
```

From internal/models/log.go:
```go
type JobStatus int
const (JobStatusNormal JobStatus = 0; JobStatusPause JobStatus = 1)  // :71-72 — StartJob/StopJob 断言唯一合法引用
type Job struct { BaseModel; JobName/JobGroup/InvokeTarget/CronExpression string; MisfirePolicy; Concurrent bool; Status JobStatus; NextRunTime/PrevRunTime *time.Time; Remark *string } // :76
type JobLogStatus int
const (JobLogStatusSuccess JobLogStatus = 0; JobLogStatusFailure JobLogStatus = 1) // :94-95
type JobLog struct { BaseTimeLine; JobName/JobGroup/InvokeTarget/JobMessage string; Status int; ExceptionInfo *string; StartTime/EndTime *time.Time; Duration } // :99
```

既有 analog(必读;helper 名已被占用,新 helper 必须 8001 后缀):
- internal/scheduler/cron_test.go:11-47 — var-seam save/Cleanup/restore + 并发范式(D-80-02 的并发 SC 直接引用)
- internal/scheduler/reconciliation_tasks_test.go:29 `setupReconExceptionTestDB` — sqlite 内存库 + 手写 DDL 形态(注意:本 plan 新 fixture 一律 t.TempDir 文件库,防跨用例串数据)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 引擎 fixture + 注册表(RegisterTask/GetTaskHandler/IsTaskRegistered + Register* 函数群)</name>
  <files>internal/scheduler/cron_80_01_test.go</files>
  <read_first>
  - internal/scheduler/cron.go — :165-225(Scheduler struct/NewScheduler/RegisterTask/GetTaskHandler/IsTaskRegistered)+ :570-990(RegisterNoticeTasks 等注册函数群,逐一列名)
  - internal/scheduler/cron_test.go — 既有测试与 helper 名(勿重名/勿重写)
  - internal/models/log.go — :67-115 Job/JobLog/状态常量
  </read_first>
  <action>
  创建 internal/scheduler/cron_80_01_test.go(package scheduler,同包白盒)。三个文件级 helper:

  - `newSchedDB8001(t *testing.T) *gorm.DB` — sqlite(`filepath.Join(t.TempDir(), "sched8001.db")`)+ `AutoMigrate(&models.Job{}, &models.JobLog{})`;若 glebarez 对 uuid 主键/默认值建表报错则改手写 DDL(列集照 models.Job/JobLog 的 gorm tag,reconciliation_tasks_test.go:29 形态),`t.Cleanup` 关连接。
  - `newScheduler8001(t *testing.T) (*Scheduler, *gorm.DB)` — newSchedDB8001 + `NewScheduler(db)`,SetLogger 注入一个可观察 stub Logger(收集 Infof/Warnf/Errorf 调用)。
  - `registerCountingTask8001(t *testing.T, s *Scheduler, taskType string) *int32` — RegisterTask 闭包 `atomic.AddInt32`,返回计数指针;handler 体只做计数,绝不调 Scheduler 方法(R2)。

  测试前缀 TestReg8001_:
  - `TestReg8001_RegisterLookup`:RegisterTask → IsTaskRegistered true / GetTaskHandler 非 nil;未注册类型 → IsTaskRegistered false / GetTaskHandler nil。
  - `TestReg8001_ReregisterOverwrites`:同 taskType 二次 RegisterTask → GetTaskHandler 返回新 handler(用两个计数器区分,调一次只累加新计数器)。
  - `TestReg8001_RegisterNoticeTasks`:调 RegisterNoticeTasks(s)(及 cron.go 内同构的其它 Register*Tasks/RegisterDeviceTasks 群,以 :570-990 实际函数名为准逐一调)→ 对每个注册名 IsTaskRegistered true 且 GetTaskHandler 非 nil(不执行 handler 体,避开 notice/duty/device 的 wire 依赖 —— 那是 80-02 的范围)。
  - `TestReg8001_GetJobCount_ConcurrentRead`:预置 3 个 job 行(1 个 Status=Normal),10 goroutine 并发 GetJobCount + GetJobStatus(DQ2-a 的"引擎并发"口径,引用既有 cron_test.go 并发范式),断言 total==3 且无 race。

  纪律:禁 t.Parallel();helper 名带 8001 后缀(R5 同包重名防护);status 断言只引用 models 常量。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestReg8001_" ./internal/scheduler/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestReg8001_" internal/scheduler/cron_80_01_test.go` ≥ 4
  - `grep -c "newSchedDB8001\|registerCountingTask8001" internal/scheduler/cron_80_01_test.go` ≥ 4
  - RegisterNoticeTasks 等注册函数群全部被调用(cron.go:570-990 的 Register* 前缀函数逐一出现在测试中)
  - `go test -count=1 -run "TestReg8001_" ./internal/scheduler/` exit 0
  </acceptance_criteria>
  <commit>test(80-01): scheduler 引擎 fixture + 注册表测试(Register*/并发读)</commit>
  <done>三个引擎 helper 就位供 80-02 复用;注册表三方法 + Register* 函数群 + GetJobCount/GetJobStatus 并发读全覆盖。</done>
</task>

<task type="auto">
  <name>Task 2: JobExecutor 全链(Execute/executeTask 分发 + parseInvokeTarget/calculateNextRunTime 纯 + defaultLogger)</name>
  <files>internal/scheduler/cron_80_01_test.go</files>
  <read_first>
  - internal/scheduler/cron.go — :31-163(JobExecutor 全文:Execute/executeTask/parseInvokeTarget/calculateNextRunTime + Logger 接口/defaultLogger 三法)
  </read_first>
  <action>
  同文件追加,前缀 TestExec8001_:

  - `TestExec8001_Execute_SuccessPath`:预置 job(invokeTarget="counter") + registerCountingTask8001 → 构造 JobExecutor(job/db/logger/scheduler)直调 Execute → 计数器==1;sys_job_log 新增 1 行 Status==int(models.JobLogStatusSuccess)、StartTime/EndTime/Duration 非零;models.Job 行 prev_run_time/next_run_time 被回写。
  - `TestExec8001_Execute_HandlerError`:handler 返回 sentinel error → Execute 返回该 error;jobLog 落 Status==int(models.JobLogStatusFailure) 且 ExceptionInfo 含 sentinel 文案(以 :34-78 实际回写逻辑为准)。
  - `TestExec8001_ExecuteTask_UnregisteredTarget`:invokeTarget 指向未注册 taskType → executeTask 返回错误且 jobLog 失败(注册表 miss 分支)。
  - `TestExec8001_ParseInvokeTarget_Table`:表驱动 — `"typeA"` → ("typeA", 空 params);`typeB:{"k":1}` → ("typeB", params["k"]==1.0);`typeC:{bad json` → ("typeC", params 为 nil/空,按 :109-123 实际行为断言并在注释记录);空串 → 空值。
  - `TestExec8001_CalculateNextRunTime`:合法 cron("*/1 * * * * *")→ 返回非 nil 且在未来;calculateNextRunTime 对非法表达式返回 nil(以 :124-140 实现为准);调用次数控制在个位数(临时 cron 有 Stop 不泄漏,勿循环上千次)。
  - `TestExec8001_DefaultLogger_Direct`:defaultLogger 三法直调(不 panic 即过,冒烟)+ stub Logger 经 SetLogger 注入后 ExecuteJob 路径产生 Infof/Errorf 记录(stub 收集器断言被调用,证明 logger 注入缝生效)。
  - `TestExec8001_ExecuteJob_ViaScheduler`:走 Scheduler.ExecuteJob(jobID) 公开入口(:445)全链一次 —— job 存在 → 计数器累加;job 不存在 → 返回错误(不 panic)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestExec8001_" ./internal/scheduler/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestExec8001_" internal/scheduler/cron_80_01_test.go` ≥ 7
  - 存在断言 jobLog Status 成功/失败两态的用例(引用 models.JobLogStatus* 常量,无裸 0/1)
  - `grep -c "JobLogStatusSuccess\|JobLogStatusFailure" internal/scheduler/cron_80_01_test.go` ≥ 2
  - `go test -count=1 -run "TestExec8001_" ./internal/scheduler/` exit 0
  </acceptance_criteria>
  <commit>test(80-01): JobExecutor 全链测试(分发计数器/纯函数表驱动/logger 注入)</commit>
  <done>Execute 成功/失败/未注册三态、parseInvokeTarget 四分支、calculateNextRunTime、defaultLogger 与 SetLogger 缝全覆盖。</done>
</task>

<task type="auto">
  <name>Task 3: Scheduler 公开方法 sqlite 驱动(AddJob/UpdateJob/RemoveJob/StartJob/StopJob/GetJobStatus)</name>
  <files>internal/scheduler/cron_80_01_test.go</files>
  <read_first>
  - internal/scheduler/cron.go — :280-478(AddJob/addJob/UpdateJob/RemoveJob/removeJob/StartJob/StopJob/ExecuteJob/GetJobStatus/GetJobCount)
  - internal/models/log.go — JobStatusNormal/JobStatusPause 常量
  </read_first>
  <action>
  同文件追加,前缀 TestJob8001_。全部用例不起 cron(!running 路径优先,DQ2-a):

  - `TestJob8001_AddJob_NotRunning`:未 Start 时 AddJob → job 落库(models.Job 行存在,Status 默认),不 panic(:294-297 分支);重复 AddJob 同名 job → 断言 :301 addJob 的冲突分支行为(以实现为准,注释记录)。
  - `TestJob8001_UpdateJob`:预置 job → 改 CronExpression/Status 后 UpdateJob → 重读行反映新值;UpdateJob 不存在的 jobID → 错误或不 panic(按实现断言)。
  - `TestJob8001_RemoveJob`:预置 2 job → RemoveJob 其一 → GetJobCount total 减一;Remove 不存在 ID → 断言 :368 removeJob 行为。
  - `TestJob8001_StartStopJob_Status`:StartJob(暂停态 job)→ 重读 Status==models.JobStatusNormal;StopJob → Status==models.JobStatusPause;对不存在 ID 各一遍错误分支。
  - `TestJob8001_GetJobStatus`:存在 job → (true, nil);不存在 → (false, nil 或 error,按 :471 实现断言)。
  - `TestJob8001_GetJobCount_TotalRunning`:预置 3 job(2 Normal 1 Pause)→ total==3;running 计数分支在 !running 调度器下为 0(:480 返回值语义以实现为准)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestJob8001_" ./internal/scheduler/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestJob8001_" internal/scheduler/cron_80_01_test.go` ≥ 6
  - `grep -c "JobStatusNormal\|JobStatusPause" internal/scheduler/cron_80_01_test.go` ≥ 4(禁裸 0/1 status 断言)
  - 全部用例未调用 s.Start(后台 cron 未启动,R2 纪律)
  - `go test -count=1 -run "TestJob8001_" ./internal/scheduler/` exit 0
  </acceptance_criteria>
  <commit>test(80-01): Scheduler 公开方法 sqlite 驱动测试(增删改/启停/计数)</commit>
  <done>AddJob(!running)/UpdateJob/RemoveJob/StartJob/StopJob/GetJobStatus/GetJobCount 全覆盖,status 断言全走 models 常量。</done>
</task>

<task type="auto">
  <name>Task 4: Start/Stop 生命周期 + 三组 var seams + -race 抽样</name>
  <files>internal/scheduler/cron_80_01_test.go</files>
  <read_first>
  - internal/scheduler/cron.go — :225-278(Start/Stop,5s 超时语义)+ :494-568(三组全局 var + Set/Get)
  - internal/scheduler/cron_test.go:11-47 — var-seam save/Cleanup/restore 现例(照抄纪律)
  </read_first>
  <action>
  同文件追加:

  TestLife8001_ 前缀(生命周期,DQ2-a:无 sleep、无实时等待):
  - `TestLife8001_StartAddStop`:newScheduler8001 → Start(ctx) + `t.Cleanup(s.Stop)` → Start 后 AddJob 进 cron(不 panic,job 落库)→ Stop() 正常返回(有 job 在册,验证等待路径不超时挂起);Stop 二次调用幂等不 panic。
  - `TestLife8001_StopWithoutStart`:未 Start 直接 Stop() → 不 panic(边界)。
  - `TestLife8001_StopTimeoutBounded`:Start + 注册一个长任务 handler(不真执行 —— 不触发 cron,DQ2-a)→ Stop() 返回耗时受 defaultShutdownTimeout 上限约束(仅断言返回,不测墙钟时长)。

  TestSeam8001_ 前缀(var seams,save → t.Cleanup restore,禁 t.Parallel):
  - `TestSeam8001_NoticeHub`:save 原值 → SetNoticeHub(stub NoticeHub)→ GetNoticeHub 返回同一实例 → Cleanup restore。
  - `TestSeam8001_DBGetter`:save → SetDB(stub DBGetter,GetDB 返回 fixture gormDB)→ GetDB() 取回 → Cleanup restore。
  - `TestSeam8001_GlobalScheduler`:save → SetGlobalScheduler(s)→ GetGlobalScheduler()==s → Cleanup restore。

  收尾:`go test -race -count=1 -run "TestReg8001_|TestExec8001_|TestJob8001_|TestLife8001_|TestSeam8001_" ./internal/scheduler/`(本 plan 新增用例 race 抽样;scheduler/websocket 是 ROADMAP Notes 点名的 goroutine 纪律包)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestLife8001_|TestSeam8001_" ./internal/scheduler/ && go test -race -count=1 -run "TestReg8001_|TestExec8001_|TestJob8001_|TestLife8001_|TestSeam8001_" ./internal/scheduler/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestLife8001_\|^func TestSeam8001_" internal/scheduler/cron_80_01_test.go` ≥ 6
  - 每个 Start 调用点配对 t.Cleanup(s.Stop)(grep "t.Cleanup" ≥ Start 调用数)
  - `grep -c "t.Parallel" internal/scheduler/cron_80_01_test.go` == 0
  - -race 抽样 exit 0
  </acceptance_criteria>
  <commit>test(80-01): Start/Stop 生命周期 + var seams(save/restore)+ race 抽样</commit>
  <done>Start/Stop/幂等/超时上限覆盖;NoticeHub/DB/GlobalScheduler 三缝 save-restore 纪律落地;-race 绿。</done>
</task>

<task type="auto">
  <name>Task 5: plan 收口 — cron.go per-file 覆盖率实测 + gate + SUMMARY</name>
  <files>.planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-01-SUMMARY.md</files>
  <read_first>
  - .planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-RESEARCH.md §1.2 — cron.go 基线(388/6/1.5%)与 §7 的 80-01 行(对照)
  </read_first>
  <action>
  收口三步:

  1. 全包实测(只跑一次):`go test -count=1 -coverprofile=cov80_01.out ./internal/scheduler/` → `go tool cover -func=cov80_01.out | grep "cron.go"`。
  2. 达标判定:cron.go ≥70%(≥272/388);未达标 → 回对应 task 补 unc 分支(先 `go tool cover -func=cov80_01.out | grep "cron.go"` 找 0% 函数,只补缺口不重写)。力争 ≥85%(为 80-02 的包级 ≥70% 留余量 —— 两 plan 逐文件 ≥70% ⇒ 包 ≥70%,cron.go 落点直接决定任务族压力)。实测后删除 cov80_01.out(临时文件纪律)。
  3. 稳定性与 gate:`go build ./...` + `go test ./...`(全仓 gate)+ 既有 cron_test.go/ad_sync_tasks_test.go/reconciliation_tasks_test.go 不回归(全包跑已含)。

  SUMMARY 落:cron.go 基线 → 实测覆盖率、三个引擎 helper 名(newSchedDB8001/newScheduler8001/registerCountingTask8001)供 80-02 复用、D-80-02 口径声明(零 cron 触发时序断言)、R2 纪律执行情况(Start/Stop 配对、handler 纯净)。
  </action>
  <verify>
    <automated>go test -count=1 -coverprofile=cov80_01.out ./internal/scheduler/ && go tool cover -func=cov80_01.out | grep "cron.go" && rm -f cov80_01.out && go build ./... && go test ./... 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
  - cron.go per-file ≥70%(实测数字落 SUMMARY)
  - `go build ./...` exit 0;`go test ./...` exit 0
  - cov80_01.out 已删除(`ls cov80_01.out` 不存在)
  - `git status --porcelain | grep _test.go` 无未提交测试文件(每 task 一原子 commit 已收)
  </acceptance_criteria>
  <commit>docs(80-01): 收口 cron.go 覆盖率实测 + 引擎 fixture 沉淀落 SUMMARY</commit>
  <done>cron.go 1.5% → ≥70%(目标 ≥85%);引擎底座(fixture/纪律/SC 口径)交付 80-02。</done>
</task>

</tasks>

<notes>
- 依赖:无(全部现成 —— sqlite/testify/robfig/cron 已在 go.mod)。本 plan 零新增依赖。
- 被依赖:80-02 六个 *_tasks.go 测试复用 newSchedDB8001/newScheduler8001/registerCountingTask8001 与 var-seam 纪律;同包(internal/scheduler)必须 wave 串行(D-78-11 同构先例,整包编译互踩规避)。
- race 安全:凡 Start 必 t.Cleanup(s.Stop);handler 内不调 Scheduler 方法(Stop 持锁等 GetTaskHandler RLock 会阻塞 5s);禁 t.Parallel;每 task -run 抽样、收口 -race。
- calculateNextRunTime 每次起临时 cron(:124-140),调用控制在个位数/用例。
- quirk 纪律:行为与源码注释/docs 不符 → 按 D-78-10 同构口径就地记录,零生产代码改动(D-80 escape hatch 仅限源码-vs-文档错配,executor 判断)。
</notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 ↔ 数据库 | glebarez sqlite(t.TempDir 文件库),不触碰生产 PG |
| 测试 ↔ 全局状态 | 三组包级 var(GlobalNoticeHub/GlobalDB/GlobalScheduler)只在同包测试内 save/restore |
| 测试 ↔ 生产代码 | 仅新增 *_80_01_test.go;生产 .go 改动 = 0(D-80 escape hatch 除外) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01-01 | DoS | cron goroutine 泄漏(-race 告警/套件变慢) | mitigate | 凡 Start 必 t.Cleanup(s.Stop);不 Start 的用例不起 cron;收口 -race 抽样 |
| T-80-01-02 | Tampering | 全局 var 污染跨用例/跨 plan 泄漏 | mitigate | save → t.Cleanup restore(77-05 纪律);禁 t.Parallel;同包串行 wave |
| T-80-01-03 | DoS | Stop 持锁 5s 等待 × 多用例拖慢套件 | mitigate | !running 路径优先;handler 纯计数;Start 类用例只 3 个 |
| T-80-01-SC | Tampering | 依赖安装 | accept | 零新增依赖(sqlite/testify/robfig cron 既有) |
</threat_model>

<verification>
- `go test -count=1 ./internal/scheduler/` 全绿(既有 3 个测试文件不回归);新增用例 -race 抽样全绿
- `go tool cover -func`:cron.go ≥70%(基线 1.5%)
- `go build ./...` + `go test ./...` exit 0
- 生产 .go 改动 = 0 文件;status 断言零裸 0/1 字面量
</verification>

<success_criteria>
1. internal/scheduler/cron_80_01_test.go 存在且全绿:TestReg8001_ ≥4、TestExec8001_ ≥7、TestJob8001_ ≥6、TestLife8001_/TestSeam8001_ 合计 ≥6
2. cron.go per-file ≥70%(≥272/388,基线 1.5%),实测落 SUMMARY,力争 ≥85%
3. 引擎 fixture 三 helper(newSchedDB8001/newScheduler8001/registerCountingTask8001)可被 80-02 直接复用
4. D-80-02 口径执行:零 cron 触发时序断言、零 sleep;goroutine 全部 t.Cleanup 收口
5. `go build ./...` + `go test ./...` exit 0;每 task 一原子 commit
</success_criteria>

<decision_audit>
本 plan 涉及的 phase-80 决策(80-RESEARCH.md DQ2/DQ6 命名与纪律在此定案,executor 必须遵守):

| ID | 决策 | 依据 / 理由 |
|----|------|------------|
| D-80-02 | scheduler 引擎断言口径 = research DQ2-(a):直调 JobExecutor.Execute / Scheduler 公开方法 + Start/Stop 生命周期用例;零 cron 触发时序断言(无 sleep、无实时等待、无 Eventually 轮询时钟);不附加真实 cron 触发用例(80-01 收口后缺口 >50 stmts 才重议);"引擎并发"SC 由 cron_test.go 既有并发范式 + 新增 GetJobCount/GetJobStatus 并发读覆盖 | 符合用户记忆 local-vs-ci 的"测试禁时序 flake"纪律;cron 触发时序是 R2 flake 源;ROADMAP 的"并发/取消"由直调路径 + ctx/生命周期用例承载 |
| D-80-07 | 测试文件命名 `<source-or-topic>_80_NN_test.go`(本 plan:cron_80_01_test.go);helper 一律 `newXxx80NN` 后缀;NN = plan 号 | 沿袭 D-78-08 / D-79-06;同包既有 helper(cron_test.go/reconciliation_tasks_test.go)名不可撞,R5 防护 |
| (沿用) | var-seam 纪律:save → t.Cleanup restore,禁 t.Parallel | 77-05 BLOCK-02 惯例 + cron_test.go:11-47 现例 |
| (沿用) | goroutine 纪律:凡 Start 必 t.Cleanup(Stop);handler 内不调 Scheduler 方法 | ROADMAP Phase 80 Notes L276 + research R2 |
| (沿用) | status 断言引用 models.* 常量(JobStatusNormal/JobStatusPause/JobLogStatus*) | CLAUDE.md Status Value Convention |
| (沿用) | quirk 纪律 = 77 D-01..03 / 78 D-78-10 同构:零生产代码改动,行为差异就地记录 | ROADMAP Phase 80 零业务变更纪律;D-80 escape hatch 仅限源码-vs-文档错配 |

基线数字来源:80-RESEARCH.md §1.2(2026-08-28 实测 `go test -count=1 -coverprofile ./internal/scheduler/` → 1103 stmts / 36 covered / 3.3%;cron.go 388/6/1.5%)。
</decision_audit>

<output>
Create `.planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-01-SUMMARY.md` when done
</output>
