---
phase: 80-scheduler-and-fragments
plan: 02
status: pending
type: execute
wave: 2
depends_on: ["80-01"]
files_modified:
  - internal/scheduler/workorder_tasks_80_02_test.go
  - internal/scheduler/ad_sync_tasks_80_02_test.go
  - internal/scheduler/dept_sync_tasks_80_02_test.go
  - internal/scheduler/mac_tasks_80_02_test.go
  - internal/scheduler/vdi_tasks_80_02_test.go
  - internal/scheduler/reconciliation_tasks_80_02_test.go
autonomous: true
requirements: [TAIL-02]

must_haves:
  truths:
    - "任务族 8 文件(workorder 166 / ad_sync 162 / dept_sync 122 / reconciliation 130 / mac_history 49 / vdi_sync 48 / mac_matview 23 / fix_suggestion 15 = 715 stmts)逐文件 ≥70%(豁免段按 D-80-06 剔除分母后仍 ≥70%);与 80-01 的 cron.go ≥70% 合起来 ⇒ internal/scheduler 包 ≥70%(SC-1 达成,加权平均不变式)"
    - "按 D-80-06 裁决:syncADConfig 内层 addomain LDAP wire 段(~26 stmts)按 e 类豁免文档化;checkAndSyncADConfigs 的 3 个调度分支(nil-LastSync / 超间隔 / 未超间隔,:195-255)+ executeADAccountPoolRecoverBreakersTask(sqlite)+ getNextRunTime 纯函数必须收满"
    - "全部接口依赖(PartitionService/MACHistoryPurgeService/VDIVMService/MACHistoryMatViewService/NoticeHub/AccountPool)用 per-interface stub struct 直注(73-04 D-02 范式);dept_sync 经同包特权直写 unexported `ADSyncScheduler.pool` 字段"
    - "var seams(GlobalNoticeHub/globalADSyncScheduler/GlobalDeviceMonitorService/SetADSM4Cipher)save → t.Cleanup restore,禁 t.Parallel;复用 80-01 的 newSchedDB8001/newScheduler8001/registerCountingTask8001(80-02 新 helper 另起 8002 后缀)"
    - "按 D-80-02 口径:零 cron 触发时序断言、零 sleep;workorder 4 纯函数(generateWorkOrderNo/replaceVariables/getWeekdayName/calculateNextRunTimeCron)表驱动,断言禁裸 0/1 status 字面量"
  artifacts:
    - path: "internal/scheduler/workorder_tasks_80_02_test.go"
      provides: "workorder 4 纯函数 + sqlite 路径(assignWorkOrderHandler 4 分支/syncWorkOrderJob/create·update·Disable·Enable)+ GlobalNoticeHub nil-guard(前缀 TestWot8002_)"
      min_lines: 220
      contains: "assignWorkOrderHandler"
    - path: "internal/scheduler/ad_sync_tasks_80_02_test.go"
      provides: "checkAndSyncADConfigs 3 分支 + recoverBreakers + getNextRunTime + ScheduleADSyncForConfig ctx cancel + SM4 cipher seam(前缀 TestAds8002_)"
      min_lines: 200
      contains: "checkAndSyncADConfigs"
    - path: "internal/scheduler/dept_sync_tasks_80_02_test.go"
      provides: "dept_sync 注册体 + pool stub 直注 + 两个 execute 分支(前缀 TestDst8002_)"
      min_lines: 140
      contains: "executeDeptToADSyncTask"
    - path: "internal/scheduler/mac_tasks_80_02_test.go"
      provides: "mac_history + mac_matview 双 seam stub + upsert/cleanup/purge 分发(前缀 TestMht8002_/TestMmv8002_)"
      min_lines: 110
      contains: "RegisterMACHistoryMatViewTasks"
    - path: "internal/scheduler/vdi_tasks_80_02_test.go"
      provides: "vdi seam + executeVDIVMSyncTask 分发 + SyncVDIVMsManually + fix_suggestion 注册(前缀 TestVdi8002_/TestFsm8002_)"
      min_lines: 110
      contains: "executeVDIVMSyncTask"
    - path: "internal/scheduler/reconciliation_tasks_80_02_test.go"
      provides: "RegisterReconciliationTasks 注册体 + createWorkorderBySeverity + checkPortStatusDrift + baseline 读写(前缀 TestRct8002_)"
      min_lines: 140
      contains: "createWorkorderBySeverity"
  key_links:
    - from: "internal/scheduler/ad_sync_tasks_80_02_test.go"
      to: "internal/scheduler/ad_sync_tasks.go:195-255 checkAndSyncADConfigs"
      via: "sqlite sys_ad_config 行驱动三态(nil-LastSync/超间隔/未超间隔),断言 syncADConfig 被调度与否由可观察副作用承载"
      pattern: "checkAndSyncADConfigs|sys_ad_config"
    - from: "internal/scheduler/dept_sync_tasks_80_02_test.go"
      to: "ADSyncScheduler.pool unexported 字段(ad_sync_tasks.go 同包)"
      via: "同包直写 stub pool → executeDeptToADSyncTask 分发"
      pattern: "\\.pool ="
    - from: "internal/scheduler/workorder_tasks_80_02_test.go"
      to: "internal/scheduler/workorder_tasks.go:242 sendWorkOrderNotification"
      via: "GlobalNoticeHub nil → 跳过(:242-247 nil-guard)+ 注入 stub → BroadcastToUsers 被调"
      pattern: "GlobalNoticeHub"
---

# Plan 2: scheduler 任务族——调度分支/取消/恢复 + wire 豁免 (Phase: 80-scheduler-and-fragments)

<objective>
交付 internal/scheduler 任务族 7 个 *_tasks.go 文件(715 stmts,含 ROADMAP 标题所指的调度/取消/恢复分支):workorder(166)/ ad_sync(162,已 27 cov)/ dept_sync(122)/ reconciliation(130,已 3 cov)/ mac_history(49)/ vdi_sync(48)/ mac_matview(23)+ reconciliation_fix_suggestion_monitor(15)。逐文件 ≥70% ⇒ 包级 internal/scheduler ≥70%(SC-1 关门);DQ6 的 addomain wire 段按 e 类豁免文档化。

Purpose: TAIL-02(SC-1)收口段。80-01 交付的引擎 fixture 在此复用;本 plan 的分支矩阵(ADSync 调度三态/工单派单四分支/reconciliation 转单)是 SC-1『注册表/执行器/引擎并发与取消分支』里"取消/恢复"的主体载体(ScheduleADSyncForConfig ctx cancel / executeADAccountPoolRecoverBreakersTask / Disable·EnablePeriodicWorkOrderJob)。
Output: 新增 6 个测试文件(零生产代码改动,D-80 escape hatch 除外)+ D-80-06 豁免条目落 SUMMARY。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-RESEARCH.md
@.planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-01-SUMMARY.md

<interfaces>
<!-- 关键契约(planner 已逐一读源码复核,2026-08-28)。executor 直接使用,无需重新探索。 -->

From internal/scheduler/workorder_tasks.go(166 stmts):
```go
// 纯函数(~45 stmts,表驱动)
generateWorkOrderNo(...)            // 工单号生成(读源码确认签名/格式)
replaceVariables(...)               // 变量替换
getWeekdayName(...)                 // 星期名
calculateNextRunTimeCron(...)       // cron 数学(纯)
// sqlite 路径
getTodayDutyPerson(...)             // 值班人查询
assignWorkOrderHandler(...)         // 派单 4 分支(:139-262 段内)
syncWorkOrderJob / createWorkOrderJob / updateExistingWorkOrderJob
DisablePeriodicWorkOrderJob / EnablePeriodicWorkOrderJob
// var seam
var GlobalNoticeHub NoticeHub       // cron.go:501;:242-247 sendWorkOrderNotification nil-guard
```

From internal/scheduler/ad_sync_tasks.go(162 stmts,27 cov):
```go
var (globalADSyncScheduler *ADSyncScheduler; globalADSyncSchedulerOnce sync.Once)  // :35-36
func SetADSM4Cipher(cipher addomain.PasswordCipher)   // :45 — seam
func getADSM4Cipher() addomain.PasswordCipher         // :54
func RegisterADSyncTasks(scheduler *Scheduler)        // :75(既有测试已覆盖部分,勿重写)
func StartADSyncScheduler(db *gorm.DB)                // :121(既有 Start/Stop/Parallel 测试已覆盖,勿重写)
func (s *ADSyncScheduler) checkAndSyncADConfigs()     // :195 — 3 分支:nil-LastSync / 超间隔 / 未超间隔(sqlite sys_ad_config 行驱动)
func (s *ADSyncScheduler) syncADConfig(ctx, configID) // :256 — addomain wire 内层 = D-80-06 e 类豁免(~26 stmts)
executeADAccountPoolRecoverBreakersTask(...)          // :283 起 — 需 globalADSyncScheduler 非零 + sqlite sys_ad_service_accounts
ScheduleADSyncForConfig / GetADSyncStatus / getNextRunTime(纯 cron 数学)/ getDefaultADConfigID(sqlite)
```

From internal/scheduler/dept_sync_tasks.go(122 stmts):
```go
getGlobalADAccountPool(...)            // :22 var seam(经 globalADSyncScheduler)
RegisterDeptSyncTasks(s *Scheduler)    // 注册体
getDefaultADConfigIDForDept(...)       // sqlite
executeDeptToADSyncTask / executeDeptMemberToADGroupSyncTask   // pool interface → 同包直写 `ADSyncScheduler.pool`(unexported,同包可写)
```

From internal/scheduler/mac_history_tasks.go(49 stmts)+ mac_history_matview_tasks.go(23 stmts):
```go
SetPartitionService(...) / SetMACHistoryPurgeService(...)   // :42/:51 Set* seams → stub 直注
upsertMACHistoryJob(sqlite) / executeMACHistoryCleanup / executeMACHistoryPurge   // 分发
RegisterMACHistoryMatViewTasks(s, db, matViewSvc services.MACHistoryMatViewService)  // 单函数,stub + sqlite
```

From internal/scheduler/vdi_sync_tasks.go(48 stmts)+ reconciliation_fix_suggestion_monitor.go(15 stmts):
```go
SetVDIVMService(...)                    // cron.go:940 seam
executeVDIVMSyncTask / syncAllEnabledVDIServers / syncSingleVDIServer(sqlite VDIServer 行)
SyncVDIVMsManually
RegisterReconciliationFixSuggestionMonitor(...)   // 单注册函数,reconciliation 同模式
```

From internal/scheduler/reconciliation_tasks.go(130 stmts,3 cov,既有测试文件已备 sqlite fixture):
```go
RegisterReconciliationTasks(s, db, wsSvc, noticeSvc)   // wsSvc/noticeSvc 形参 nil-safe(注册路径不广播)
createWorkorderBySeverity / checkPortStatusDrift / readDriftBaseline / upsertDriftBaseline   // 全 sqlite
```

既有 analog(必读;80-02 新 helper 一律 8002 后缀防撞):
- 80-01 的 internal/scheduler/cron_80_01_test.go — newSchedDB8001/newScheduler8001/registerCountingTask8001(直接复用,不复制)
- internal/scheduler/ad_sync_tasks_test.go — 既有 Start/Stop/Parallel 测试(勿重写,只补未覆盖分支)
- internal/scheduler/reconciliation_tasks_test.go:29 — sqlite 内存库 + sys_reconciliation_exception DDL(本 plan 复用其表结构;t.TempDir 文件库为准)
- 73-04 D-02 范本(STATE.md Decisions)— per-interface stub struct(无 testify/mock)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: workorder_tasks——4 纯函数表驱动 + GlobalNoticeHub nil-guard/stub 两态</name>
  <files>internal/scheduler/workorder_tasks_80_02_test.go</files>
  <read_first>
  - internal/scheduler/workorder_tasks.go — 全文(重点 :139-262 的 assignWorkOrderHandler 分支与 :242-247 sendWorkOrderNotification nil-guard、4 个纯函数实现)
  - internal/scheduler/cron_80_01_test.go — 复用的 3 个 helper
  </read_first>
  <action>
  创建 internal/scheduler/workorder_tasks_80_02_test.go(package scheduler)。前缀 TestWot8002_:

  - `TestWot8002_PureFuncs_Table`:generateWorkOrderNo(格式断言:前缀+时间戳/序号结构,以实现为准)/ replaceVariables(含变量命中/未命中/空串)/ getWeekdayName(7 天全表 + 越界分支)/ calculateNextRunTimeCron(合法/非法表达式)表驱动,期望值手算写入用例注释。
  - `TestWot8002_NoticeHub_NilGuard`:不注入 GlobalNoticeHub → sendWorkOrderNotification(经 assignWorkOrderHandler 或直调)走 :242-247 分支 → 不 panic、无广播(可观察:无副作用即可断言)。
  - `TestWot8002_NoticeHub_Stub`:save GlobalNoticeHub → 注入 stub NoticeHub(收集 BroadcastToUsers 调用)→ 触发通知路径 → 断言 stub 被调且参数含 workOrderId/title → t.Cleanup restore。
  - `TestWot8002_AssignWorkOrderHandler_Branches`:sqlite(t.TempDir,复用 newSchedDB8001 或本文件 8002 版)建 duty/workorder 相关表(以 handler 实际引用表为准)→ 表驱动覆盖 assignWorkOrderHandler 的 4 分支(值班人有/无人/异常/边界,以 :139-262 实际分支为准逐一对号)。
  - `TestWot8002_SyncCreateUpdateJob`:syncWorkOrderJob 三态(job 不存在→create / 已存在→update / 表达式变更)→ models.Job 行断言(Status/CronExpression,引用 models 常量);DisablePeriodicWorkOrderJob → Status==models.JobStatusPause;EnablePeriodicWorkOrderJob → Status==models.JobStatusNormal。
  - `TestWot8002_GetTodayDutyPerson`:sqlite 种子值班行 → 命中/未命中两分支。

  纪律:禁 t.Parallel;禁 sleep;全部 helper 8002 后缀。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestWot8002_" ./internal/scheduler/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestWot8002_" internal/scheduler/workorder_tasks_80_02_test.go` ≥ 6
  - `grep -c "JobStatusNormal\|JobStatusPause" internal/scheduler/workorder_tasks_80_02_test.go` ≥ 2
  - 存在 GlobalNoticeHub nil-guard 与 stub 注入两态用例
  - `go test -count=1 -run "TestWot8002_" ./internal/scheduler/` exit 0
  </acceptance_criteria>
  <commit>test(80-02): workorder 任务族测试(纯函数表驱动 + 派单分支 + NoticeHub 两态)</commit>
  <done>workorder_tasks.go 0% → ≥70%;4 纯函数、派单 4 分支、job 同步/启停、通知 nil-guard 全覆盖。</done>
</task>

<task type="auto">
  <name>Task 2: ad_sync_tasks——checkAndSyncADConfigs 三分支 + 恢复任务 + ctx 取消 + SM4 seam(D-80-06 豁免段标注)</name>
  <files>internal/scheduler/ad_sync_tasks_80_02_test.go</files>
  <read_first>
  - internal/scheduler/ad_sync_tasks.go — :20-140(seams/Start/Stop 既有)+ :195-330(checkAndSyncADConfigs 三分支/executeADAccountPoolRecoverBreakersTask/getDefaultADConfigID/ScheduleADSyncForConfig/GetADSyncStatus/getNextRunTime)
  - internal/scheduler/ad_sync_tasks_test.go — 既有测试覆盖清单(勿重写)
  </read_first>
  <action>
  创建 internal/scheduler/ad_sync_tasks_80_02_test.go。前缀 TestAds8002_:

  - `TestAds8002_CheckAndSync_NilLastSync`:sqlite 建 sys_ad_config(models.AutoMigrate 或按列 DDL)种子 LastSyncTime==nil 行 → checkAndSyncADConfigs → 该 config 进入同步调度(可观察副作用:globalADSyncScheduler 的任务入队/计数 stub,或以 :195-255 实际写回字段断言;不触发 addomain wire)。
  - `TestAds8002_CheckAndSync_Overdue`:种子 LastSyncTime = now-间隔-1 → 判定为需同步(超间隔分支)。
  - `TestAds8002_CheckAndSync_Recent`:种子 LastSyncTime = now-1min(未超间隔)→ 跳过(断言不调度/无写回)。
  - `TestAds8002_RecoverBreakersTask`:save globalADSyncScheduler → 注入 `&ADSyncScheduler{db: sqlite}`(同包直写,含 sys_ad_service_accounts 表与熔断行)→ executeADAccountPoolRecoverBreakersTask → 过期熔断行被恢复(状态字段按 models/addomain 常量断言)→ t.Cleanup restore。
  - `TestAds8002_ScheduleForConfig_Cancel`:ctx 可取消 —— ScheduleADSyncForConfig(ctx) → cancel() → 断言取消路径(以 :338-356 的 ctx.Done() 分支为锚,用 goroutine + select 断言返回,禁止 sleep 轮询,可用 chan 同步);GetADSyncStatus 返回结构断言。
  - `TestAds8002_GetNextRunTime_Pure`:getNextRunTime 纯 cron 数学表驱动(合法/非法/空)。
  - `TestAds8002_SM4CipherSeam`:save cipher → SetADSM4Cipher(stub addomain.PasswordCipher)→ getADSM4Cipher 返回同实例 → restore。
  - `TestAds8002_GetDefaultADConfigID`:sqlite 空 config 表 / 有 config 行两分支。

  D-80-06 标注:文件头注释写明 `// D-80-06 豁免:syncADConfig 内层 addomain LDAP wire(~26 stmts)按 e 类豁免,见 80-02 SUMMARY`;测试绝不调用 syncADConfig。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestAds8002_" ./internal/scheduler/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestAds8002_" internal/scheduler/ad_sync_tasks_80_02_test.go` ≥ 8
  - 文件含字面注释 `D-80-06 豁免`
  - 测试无 syncADConfig 直调(`grep -c "syncADConfig(" ad_sync_tasks_80_02_test.go` 中无独立调用行)
  - `go test -count=1 -run "TestAds8002_" ./internal/scheduler/` exit 0
  </acceptance_criteria>
  <commit>test(80-02): ad_sync 调度三分支/恢复/取消/SM4 seam + D-80-06 wire 豁免标注</commit>
  <done>ad_sync_tasks.go 16.7% → ≥70%(wire 段豁免剔除分母);调度三态、熔断恢复、ctx 取消、cipher seam 全覆盖。</done>
</task>

<task type="auto">
  <name>Task 3: dept_sync_tasks——pool stub 同包直注 + 双 execute 分支</name>
  <files>internal/scheduler/dept_sync_tasks_80_02_test.go</files>
  <read_first>
  - internal/scheduler/dept_sync_tasks.go — 全文(:22 getGlobalADAccountPool seam、RegisterDeptSyncTasks、getDefaultADConfigIDForDept、两个 execute 函数的 pool 消费点)
  - internal/services/addomain — AccountPool 的接口面(确认 stub 需要实现的方法集;以 dept_sync_tasks.go 实际引用为准)
  </read_first>
  <action>
  创建 internal/scheduler/dept_sync_tasks_80_02_test.go。前缀 TestDst8002_:

  - `TestDst8002_RegisterDeptSyncTasks`:调 RegisterDeptSyncTasks(newScheduler8001(t))→ 注册名 IsTaskRegistered true(handler 不执行,避开 wire)。
  - `TestDst8002_PoolSeam`:save globalADSyncScheduler → 注入 `&ADSyncScheduler{}` 并同包直写 `.pool = stubPool`(stub 满足 dept_sync 引用的方法集)→ getGlobalADAccountPool()/execute 路径取到 stub → 断言 stub 被调 → t.Cleanup restore。
  - `TestDst8002_ExecuteDeptToADSync`:pool stub + sqlite(默认 config 行)→ executeDeptToADSyncTask → 分发逻辑覆盖(pool 返回成功/错误两态,断言任务日志/错误上抛路径,以实现为准)。
  - `TestDst8002_ExecuteDeptMemberToADGroup`:同上,覆盖成员同步分支(stub 方法逐个命中;wire 层内嵌 addomain 调用若不可绕则该子分支按 D-80-06 同口径在 SUMMARY 记豁免)。
  - `TestDst8002_GetDefaultADConfigIDForDept`:sqlite 空/有 config 两分支。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestDst8002_" ./internal/scheduler/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestDst8002_" internal/scheduler/dept_sync_tasks_80_02_test.go` ≥ 5
  - 存在同包直写 pool 字段的用例(`\.pool =` 出现 ≥1)
  - `go test -count=1 -run "TestDst8002_" ./internal/scheduler/` exit 0
  </acceptance_criteria>
  <commit>test(80-02): dept_sync 任务族测试(pool stub 直注 + 双 execute 分支)</commit>
  <done>dept_sync_tasks.go 0% → ≥70%(不可绕 wire 子分支按 D-80-06 同口径豁免)。</done>
</task>

<task type="auto">
  <name>Task 4: mac_history + mac_matview——双 Set* seam stub + upsert/cleanup/purge 分发</name>
  <files>internal/scheduler/mac_tasks_80_02_test.go</files>
  <read_first>
  - internal/scheduler/mac_history_tasks.go — 全文(:42/:51 Set* seams、upsertMACHistoryJob、executeMACHistoryCleanup/executeMACHistoryPurge 分发)
  - internal/scheduler/mac_history_matview_tasks.go — 全文(单注册函数)
  </read_first>
  <action>
  创建 internal/scheduler/mac_tasks_80_02_test.go。前缀 TestMht8002_(mac_history)/ TestMmv8002_(matview):

  - `TestMht8002_Seams`:save 原值 → SetPartitionService(stub)/ SetMACHistoryPurgeService(stub)(per-interface stub struct,方法集照源码)→ 分发路径取到 stub → restore。
  - `TestMht8002_UpsertJob`:sqlite(t.TempDir)→ upsertMACHistoryJob 三态(首次创建/重复更新/表达式变更),models.Job 行断言引用 models 常量。
  - `TestMht8002_ExecuteCleanup_Purge`:executeMACHistoryCleanup / executeMACHistoryPurge 分发 —— stub 返回成功/错误两态,断言错误上抛与日志路径(实现为准)。
  - `TestMmv8002_Register`:RegisterMACHistoryMatViewTasks(s, db, stubMACHistoryMatViewService)→ 注册名命中;stub 方法在 execute 路径被调(执行一次分发,不依赖真实 matview)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestMht8002_|TestMmv8002_" ./internal/scheduler/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestMht8002_\|^func TestMmv8002_" internal/scheduler/mac_tasks_80_02_test.go` ≥ 4
  - 两个 Set* seam 都有 save/restore 用例
  - `go test -count=1 -run "TestMht8002_|TestMmv8002_" ./internal/scheduler/` exit 0
  </acceptance_criteria>
  <commit>test(80-02): mac_history/matview 任务族测试(双 seam stub + 分发)</commit>
  <done>mac_history_tasks.go 与 mac_history_matview_tasks.go 双双 0% → ≥70%。</done>
</task>

<task type="auto">
  <name>Task 5: vdi_sync + fix_suggestion_monitor——VDI seam + sqlite 行驱动 + 注册体</name>
  <files>internal/scheduler/vdi_tasks_80_02_test.go</files>
  <read_first>
  - internal/scheduler/vdi_sync_tasks.go — 全文(SetVDIVMService seam、executeVDIVMSyncTask、syncAllEnabledVDIServers/syncSingleVDIServer、SyncVDIVMsManually)
  - internal/scheduler/reconciliation_fix_suggestion_monitor.go — 全文(单注册函数)
  - internal/scheduler/cron.go:940 — SetVDIVMService 定义处
  </read_first>
  <action>
  创建 internal/scheduler/vdi_tasks_80_02_test.go。前缀 TestVdi8002_ / TestFsm8002_:

  - `TestVdi8002_Seams`:save → SetVDIVMService(stub VDIVMService,方法集照 vdi_sync_tasks.go 引用)→ 分发取到 stub → restore。
  - `TestVdi8002_ExecuteDispatch`:executeVDIVMSyncTask → stub 方法命中(成功/错误两态)。
  - `TestVdi8002_SyncEnabled_Single`:sqlite 建 VDIServer 表(models.AutoMigrate)种子 enabled/disabled 行 → syncAllEnabledVDIServers 只处理 enabled;syncSingleVDIServer 命中/不存在两分支(断言以 stub 交互记录为准,不走真实 VDI wire)。
  - `TestVdi8002_SyncManually`:SyncVDIVMsManually 公开入口一次(成功路径)。
  - `TestFsm8002_RegisterMonitor`:注册函数 + 一次分发(stub,模式同 TestMmv8002_Register)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestVdi8002_|TestFsm8002_" ./internal/scheduler/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestVdi8002_\|^func TestFsm8002_" internal/scheduler/vdi_tasks_80_02_test.go` ≥ 5
  - SetVDIVMService seam 有 save/restore
  - `go test -count=1 -run "TestVdi8002_|TestFsm8002_" ./internal/scheduler/` exit 0
  </acceptance_criteria>
  <commit>test(80-02): vdi_sync + fix_suggestion 任务族测试(seam + sqlite 行驱动)</commit>
  <done>vdi_sync_tasks.go 与 reconciliation_fix_suggestion_monitor.go 双双 0% → ≥70%。</done>
</task>

<task type="auto">
  <name>Task 6: reconciliation_tasks——注册体 + 转单/漂移/baseline sqlite 分支</name>
  <files>internal/scheduler/reconciliation_tasks_80_02_test.go</files>
  <read_first>
  - internal/scheduler/reconciliation_tasks.go — 全文(RegisterReconciliationTasks、createWorkorderBySeverity、checkPortStatusDrift、readDriftBaseline/upsertDriftBaseline)
  - internal/scheduler/reconciliation_tasks_test.go — 既有 fixture(表结构)与 9 个既有测试(勿重写,新文件另起名)
  </read_first>
  <action>
  创建 internal/scheduler/reconciliation_tasks_80_02_test.go。前缀 TestRct8002_:

  - `TestRct8002_RegisterTasks`:RegisterReconciliationTasks(newScheduler8001(t), db, nil, nil)→ 注册名全部 IsTaskRegistered true;wsSvc/noticeSvc 传 nil 验证注册路径 nil-safe(research §1.2 行 4)。
  - `TestRct8002_CreateWorkorderBySeverity`:sqlite 建工单相关表 → 严重度分级表驱动(每级断言生成工单的字段映射;severity 值若为枚举则引用常量,禁裸字面量)。
  - `TestRct8002_CheckPortStatusDrift`:种子 sys_reconciliation_exception / 端口状态行(复用既有 DDL 形态)→ 漂移检出/无漂移两分支。
  - `TestRct8002_Baseline_RoundTrip`:readDriftBaseline(无基线/有基线)+ upsertDriftBaseline(首写/覆盖)→ sqlite 行断言。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestRct8002_" ./internal/scheduler/ && go test -count=1 -run "TestRecon|TestReconciliation" ./internal/scheduler/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestRct8002_" internal/scheduler/reconciliation_tasks_80_02_test.go` ≥ 4
  - 既有 9 个 reconciliation 测试不回归(全包跑含)
  - `go test -count=1 -run "TestRct8002_" ./internal/scheduler/` exit 0
  </acceptance_criteria>
  <commit>test(80-02): reconciliation 任务族测试(注册体 + 转单/漂移/baseline)</commit>
  <done>reconciliation_tasks.go 2.3% → ≥70%;reconciliation_crons.go 归属的注册 stmt 一并命中。</done>
</task>

<task type="auto">
  <name>Task 7: plan 收口 — 包级 ≥70%(SC-1 达成)+ D-80-06 豁免汇总 + race + gate</name>
  <files>.planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-02-SUMMARY.md</files>
  <read_first>
  - .planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-RESEARCH.md §1.2 — 9 文件基线表(对照)
  - .planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-01-SUMMARY.md — cron.go 实测落点(本 plan 的余量计算输入)
  </read_first>
  <action>
  收口四步:

  1. 全包实测(只跑一次):`go test -count=1 -coverprofile=cov80_02.out ./internal/scheduler/` → `go tool cover -func=cov80_02.out` 逐文件表(9 文件 tot/cov/%);实测后删除 cov80_02.out。
  2. SC-1 判定:包级 `internal/scheduler` ≥70%(1103 stmts ≥772 covered);逐文件 ≥70%(豁免段剔除分母口径:ad_sync 的 syncADConfig ~26 stmts / dept_sync 不可绕 wire 子分支)。未达标文件 → 回对应 task 补 unc 分支;若 80-01 cron.go 落点 <85% 且包级差 <50 stmts,可按 D-80-02 重议一个真实 cron 触发用例(默认不做)。
  3. 稳定性:`go test -race -count=1 ./internal/scheduler/`(全包 —— scheduler 是 ROADMAP 点名 race 纪律包)+ `go build ./...` + `go test ./...`。
  4. SUMMARY 落:9 文件基线 → 实测表、包级百分比、D-80-06 豁免条目清单(syncADConfig 体 + 任何执行中发现的 wire 不可达子分支,逐条 stmt 数)、复用的 80-01 helper 清单。
  </action>
  <verify>
    <automated>go test -count=1 -coverprofile=cov80_02.out ./internal/scheduler/ && go tool cover -func=cov80_02.out | tail -1 && rm -f cov80_02.out && go test -race -count=1 ./internal/scheduler/ && go build ./... && go test ./... 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
  - `internal/scheduler` 包级 ≥70%(SC-1 达成,数字落 SUMMARY)
  - 任务族 8 文件逐文件 ≥70%(豁免剔除口径),无一留在 <50%
  - `go test -race -count=1 ./internal/scheduler/` exit 0;`go build ./...` + `go test ./...` exit 0
  - D-80-06 豁免条目已落 SUMMARY;cov80_02.out 已删除
  </acceptance_criteria>
  <commit>docs(80-02): 收口 scheduler 包级 ≥70%(SC-1)+ D-80-06 豁免汇总落 SUMMARY</commit>
  <done>internal/scheduler 3.3% → ≥70%(SC-1 关门);任务族 715 stmts 清欠完成;豁免全文档化。</done>
</task>

</tasks>

<notes>
- 依赖:80-01 的引擎 fixture(newSchedDB8001/newScheduler8001/registerCountingTask8001)与 cron.go 覆盖落点;同包(internal/scheduler)必须 wave 串行(D-78-11 同构)。
- 被依赖:无(Phase 80 内 80-03/80-04/80-05 与本 plan 无文件交集)。
- 同包特权:ADSyncScheduler.pool/sem/ctx/cancel 与包级 var 可直写 —— Phase 79 的跨包 seeding 死角在此不存在(research §1.2 structural insight)。
- wire 豁免:D-80-06 只豁免 syncADConfig 内层;任何执行中新发现的不可绕 wire 子分支同样按 e 类豁免并逐条落 SUMMARY(stmt 数 + 原因),禁止静默跳过。
- race 安全:save/restore 全局 var;禁 t.Parallel;禁 sleep;ctx 取消用 chan 同步非轮询。
- quirk 纪律:行为与源码注释不符就地记录,零生产代码改动(D-80 escape hatch 仅限源码-vs-文档错配)。
</notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 ↔ 数据库 | glebarez sqlite(t.TempDir 文件库),不触碰生产 PG |
| 测试 ↔ LDAP/addomain wire | 一切 stub 注入,测试进程零 LDAP 出网(D-80-06 豁免段不执行) |
| 测试 ↔ 全局状态 | globalADSyncScheduler/GlobalNoticeHub/GlobalDeviceMonitorService/cipher 全部 save/restore |
| 测试 ↔ 生产代码 | 仅新增 *_80_02_test.go;生产 .go 改动 = 0(D-80 escape hatch 除外) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-02-01 | Information Disclosure | 测试误连真实 LDAP/AD | mitigate | per-interface stub 直注;grep 新测试文件无 ldap:// 字面量;syncADConfig 禁调 |
| T-80-02-02 | DoS | globalADSyncScheduler 泄漏(Start 后未 Stop) | mitigate | save/restore + 既有 Start/Stop 测试不动;新用例不起真实 cron(D-80-02) |
| T-80-02-03 | Tampering | 全局 var 污染跨用例 | mitigate | save → t.Cleanup restore;禁 t.Parallel;同包串行 |
| T-80-02-SC | Tampering | 依赖安装 | accept | 零新增依赖 |
</threat_model>

<verification>
- `go test -count=1 ./internal/scheduler/` 全绿(既有 3 个测试文件不回归);全包 -race 绿
- `go tool cover -func`:internal/scheduler 包级 ≥70%(SC-1);9 文件逐文件 ≥70%(豁免剔除口径)
- `go build ./...` + `go test ./...` exit 0
- 生产 .go 改动 = 0 文件;D-80-06 豁免条目全部落 SUMMARY
</verification>

<success_criteria>
1. 6 个新测试文件存在且全绿:TestWot8002_ ≥6、TestAds8002_ ≥8、TestDst8002_ ≥5、TestMht8002_/TestMmv8002_ ≥4、TestVdi8002_/TestFsm8002_ ≥5、TestRct8002_ ≥4
2. internal/scheduler 包级 ≥70%(基线 3.3%,SC-1 达成);workorder/dept_sync/mac/vdi/fix_suggestion 5 文件 0% → 各 ≥70%;ad_sync 16.7% → ≥70%(wire 剔除);reconciliation 2.3% → ≥70%
3. D-80-06 豁免条目(syncADConfig ~26 stmts + 新发现项)逐条落 SUMMARY
4. D-80-02 口径执行:零 cron 触发时序断言、零 sleep
5. `go build ./...` + `go test ./...` exit 0;每 task 一原子 commit
</success_criteria>

<decision_audit>
本 plan 涉及的 phase-80 决策(executor 必须遵守):

| ID | 决策 | 依据 / 理由 |
|----|------|------------|
| D-80-06 | ADSyncScheduler wire 内层豁免口径 = research DQ6:checkAndSyncADConfigs 3 调度分支 + executeADAccountPoolRecoverBreakersTask(sqlite)+ 纯函数收满;syncADConfig 体(~26 stmts,addomain LDAP wire)按 e 类豁免文档化;执行中新发现的不可绕 wire 子分支同样豁免并逐条落 SUMMARY | 与 79-DQ4 口径一致;78 DQ4 已 deferred vjeantet/ldapserver(无 LDAP 仿真依赖);零生产代码改动纪律下 wire 内层不可达 |
| D-80-02(沿用) | 零 cron 触发时序断言、零 sleep;ctx 取消用 chan 同步 | 80-01 定案;本 plan 的"取消/恢复"由 ScheduleADSyncForConfig cancel 分支 + recoverBreakers sqlite 路径承载 |
| D-80-07(沿用) | 命名 `<source>_80_02_test.go`;多源合并用 topic 前缀(mac_tasks/vdi_tasks);helper 8002 后缀 | D-78-08 / D-79-06 同构 |
| (沿用) | per-interface stub struct(无 testify/mock) | 73-04 D-02 范本(STATE.md) |
| (沿用) | 同包 unexported 字段直写(pool/sem/ctx/cancel)合法 | research §1.2 structural insight(R2-killer 不存在) |
| (沿用) | status/severity 断言引用 models.* 常量 | CLAUDE.md Status Value Convention |

基线数字来源:80-RESEARCH.md §1.2(2026-08-28 实测;任务族 8 文件合计 715 stmts / 30 covered)。
</decision_audit>

<output>
Create `.planning/workstreams/milestone/phases/80-scheduler-and-fragments/80-02-SUMMARY.md` when done
</output>
