---
phase: 70-settings-page-redesign
plan: 07
type: execute
wave: 4
depends_on: ["70-03", "70-04", "70-05", "70-06"]
files_modified:
  - xingran-react-frontend/src/store/settingsStore.ts
  - xingran-react-frontend/src/pages/system/settings/__tests__/categories.test.ts
  - xingran-react-frontend/src/pages/system/captcha-background/   # 整目录删除（7 文件）
autonomous: false
requirements: [D-12, D-02]
must_haves:
  truths:
    - "D-12 落地：settings 范围残留清理完成——死目录 src/pages/system/captcha-background/（7 文件，删除前经运行库防御查询确认 sys_menu 无指向记录）删除、settingsStore 死 actions exportPreferences/importPreferences 删除、settings 页内旧 Tag/持久化 tab 键/fallback 硬编码色清零（不做全仓扫描，QA-02 CI 门已覆盖全仓）"
    - "D-02 落地（注册表断言）：两页分类注册表被 categories.test.ts 纯数据断言锁定——系统设置 3 分类（email/api/captcha，无 maxWidth 撑满）、用户设置 3 分类（appearance/layout/data，均 maxWidth 760）"
    - "Phase 70 收口门全绿：npm run build / type-check / lint / test 四门 + go build ./... + go test migrations + npm run deadcode（knip），且截图矩阵（10 张：email/captcha/appearance × light/dark ≥lg + 两页 <lg Segmented）经用户 checkpoint 确认与 v16 基准语汇一致"
  artifacts:
    - path: xingran-react-frontend/src/pages/system/settings/__tests__/categories.test.ts
      provides: "两页分类注册表完整性单测（D-07/D-08/D-02）"
      min_lines: 40
    - path: xingran-react-frontend/src/store/settingsStore.ts
      provides: "settingsStore（死 actions 已删，保留段 :94-134 单字段 actions 完好）"
  key_links:
    - from: xingran-react-frontend/src/pages/system/settings/__tests__/categories.test.ts
      to: 两页注册表导出
      via: "import systemSettingsCategories + userSettingsCategories 纯数据断言"
      pattern: "systemSettingsCategories|userSettingsCategories"
    - from: 残留清理 grep 门
      to: settings 范围文件
      via: "settings 页内 preset Tag / usePersistedStateController / fallback 色零命中"
      pattern: "Tag color=|usePersistedStateController|#3f8600|#cf1322"
---

<objective>
Phase 70 收口：(1) 完成 D-12 剩余残留清理（死目录 + 死 store actions）；(2) 补齐分类注册表完整性测试；(3) 跑全量回归门（前端四门 + Go 双门 + knip）；(4) 截图矩阵人工确认。

Purpose: 分布在各 plan 的 D-12 分量（Tag 迁移/fallback 色/死状态/persisted 键）在此统一 grep 验收；视觉语汇无像素级自动化基线，按 QA-04 惯例人工确认。
Output: 清理后的 settingsStore、categories.test.ts、全绿门、截图归档。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-settings-page-redesign/70-RESEARCH.md
@.planning/phases/70-settings-page-redesign/70-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-12 残留清理（死目录 + 死 store actions + 范围 grep 门）</name>
  <files>xingran-react-frontend/src/pages/system/captcha-background/（删除）, xingran-react-frontend/src/store/settingsStore.ts</files>
  <read_first>
  - .planning/phases/70-settings-page-redesign/70-RESEARCH.md「死目录裁定」节与「残留清理清单（D-12 汇总）」表
  - xingran-react-frontend/src/store/settingsStore.ts（删除段 :45-46 接口声明 + :181-197 实现；保留段 :94-134 单字段 actions、:209-215 类型选择器）
  - xingran-react-frontend/src/pages/system/captcha-background/（确认 7 文件仅内部相对导入）
  - configs/config.yaml（database.sqlite 路径——防御查询用）
  </read_first>
  <action>
  1. **死目录防御查询后删除**：对开发库执行 `SELECT count(*) FROM sys_menu WHERE component LIKE 'system/captcha-background%'`（sqlite 库文件路径读 configs/config.yaml 的 database 配置；CLI `sqlite3 <dbfile> "..."`）。结果 0 → 删除 `src/pages/system/captcha-background/` 整目录（7 文件）。若环境无 sqlite3 CLI：以 `grep -c "captcha-background" internal/core/db/migrations/menu_catalog_seed.sql == 0`（规范种子无记录，RESEARCH 已验证）为依据放行，并在 SUMMARY 记录 [ASSUMED]（A1 假设：存量生产库无手工菜单记录）。
  2. **settingsStore 死 actions**：删除 `exportPreferences` / `importPreferences` 两处——接口声明（:45-46）与实现（:181-197），其余段零改动（单字段 actions 与类型选择器保留，70-05 依赖它们）。
  3. **范围 grep 门（settings 范围 = src/pages/system/settings/ + src/pages/settings/ + src/store/settingsStore.ts）**：确认 `Tag color="blue|green|orange|cyan|purple"` 零命中、`#3f8600|#cf1322` 零命中、`usePersistedStateController` 零命中（hook 本体在 src/hooks/ 不算）。任何残留回补到对应文件。
  </action>
  <verify>
    <automated>test ! -d xingran-react-frontend/src/pages/system/captcha-background && grep -c "exportPreferences\|importPreferences" xingran-react-frontend/src/store/settingsStore.ts == 0 && cd xingran-react-frontend && grep -rn "Tag color=\"blue\"\|Tag color=\"green\"\|Tag color=\"orange\"\|Tag color=\"cyan\"\|Tag color=\"purple\"\|usePersistedStateController\|#3f8600\|#cf1322" src/pages/system/settings src/pages/settings | wc -l == 0 && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
  - `src/pages/system/captcha-background/` 目录不存在（删除前防御查询结果记录在 SUMMARY）
  - settingsStore 中 exportPreferences/importPreferences 零命中，updateTheme/updateLayout/updateDataPageSize 完好
  - settings 范围 7 项 grep 门全部零命中
  - `npm run type-check` 通过
  </acceptance_criteria>
  <done>D-12 清理清单全部闭环（1 persisted 键随 70-06 / 4 死状态随 70-04 / 5 fallback 色随 70-04 / 6 Tag 随 70-03/04 已前置完成，本 task 收 2/3 两项并统一验收）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 分类注册表完整性测试（categories.test.ts）</name>
  <files>xingran-react-frontend/src/pages/system/settings/__tests__/categories.test.ts</files>
  <read_first>
  - xingran-react-frontend/src/pages/system/settings/index.tsx（70-06 产物：systemSettingsCategories）
  - xingran-react-frontend/src/pages/settings/index.tsx（70-05 产物：userSettingsCategories）
  - xingran-react-frontend/src/design-system/tokens/colors.test.ts（纯常量测试同目录惯例）
  - .planning/phases/70-settings-page-redesign/70-VALIDATION.md（Per-Task Map 的 D-07/D-08 行）
  </read_first>
  <behavior>
  - systemSettingsCategories：长度 3；key 集合 = ["email","api","captcha"] 且唯一；每项 label 非空字符串、icon 为有效 ReactNode；全部无 maxWidth（表格/网格撑满，D-02）
  - userSettingsCategories：长度 3；key 集合 = ["appearance","layout","data"]；每项 maxWidth === 760（表单限宽，D-02）
  </behavior>
  <action>
  新建 `src/pages/system/settings/__tests__/categories.test.ts`：import 两页注册表导出，按 behavior 做纯数据断言（无渲染，不需要 Router/antd Wrapper；icon 断言用 `isValidElement(c.icon)`）。含默认分类存在性断言（keys 含 "email" / "appearance"，供 SettingsShell defaultCat 消费）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npx vitest run src/pages/system/settings/__tests__/categories.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
  - vitest 该文件全绿（两页注册表结构 + maxWidth 策略断言）
  - 测试为纯数据断言，无 DOM 渲染依赖
  - type-check 通过
  </acceptance_criteria>
  <done>D-02/D-07/D-08 的分类注册表契约被测试锁定</done>
</task>

<task type="auto">
  <name>Task 3: Phase 收口回归门（前端四门 + Go 双门 + knip）</name>
  <files>（无文件修改——验证门）</files>
  <read_first>
  - .planning/phases/70-settings-page-redesign/70-VALIDATION.md（Sampling Rate 的 phase gate 定义）
  </read_first>
  <action>
  依次执行并记录输出摘要到 SUMMARY：`cd xingran-react-frontend && npm run build`、`npm run type-check`、`npm run lint`、`npm run test -- run`（禁止裸 `npm run test` 进 watch 模式）、`npm run deadcode`（knip，死代码门）；`go build ./...`、`go test ./internal/core/db/migrations/`。任何一门红：修复后重跑（修复范围限本 phase 触及文件，遵守 CLAUDE.md 编译验证纪律——逐文件修复，勿批量改动）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npm run build && npm run type-check && npm run lint && npm run test -- run && npm run deadcode && cd .. && go build ./... && go test ./internal/core/db/migrations/</automated>
  </verify>
  <acceptance_criteria>
  - 七门全部 exit 0：build / type-check / lint / test / deadcode / go build / go test migrations
  - 无根级 temp_*.go / test_*.go 临时文件产生
  - 全量测试无 watch 模式残留进程
  </acceptance_criteria>
  <done>Phase 70 自动化回归门全绿</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 截图矩阵与迁移运行时确认（QA-04 风格）</name>
  <what-built>Phase 70 全部视觉交付：系统设置 SettingsShell（email 表格页 / captcha 网格墙）+ 用户设置（appearance 分段卡页），明暗双模式 + 窄屏 Segmented 降级；sys_menu 迁移运行时链路。</what-built>
  <how-to-verify>
  1. 启动后端（sqlite 开发库）确认启动日志含 Migrate208 执行行与「菜单缓存已失效」行（首启 changed=true；二启 changed=false 不失效）
  2. 启动前端 dev server，登录后依次截图并归档到 `.planning/phases/70-settings-page-redesign/screenshots/`（≥lg 桌面断点 × light/dark）：email 分类（表格页代表）、captcha 分类（网格墙代表）、用户设置 appearance 分类（分段卡代表）——共 6 张
  3. 窄屏（<lg，DevTools 响应式模拟）× light：系统设置 + 用户设置各 1 张（Segmented 降级形态）——共 2 张
  4. 侧边栏点「系统设置」不白屏（迁移后 component 解析正常）；URL `/system/settings-page?cat=captcha` 刷新后停在 captcha 分类（D-03 还原验证）；浏览器后退离开页面而非逐分类回退
  5. 对照基准 `.planning/phases/64-brand-token-layer-and-contrast-verification/test1-system-user-page.png` 与 `.planning/sketch/real-final-v16.webp`：统计卡行 / 工具栏 / 双层纸感 / 深绿主按钮语汇一致；dark 模式下导航卡/统计卡/分段预览块前景背景可读（WCAG AA 抽查）
  </how-to-verify>
  <resume-signal>回复 "approved" 收口，或列出需修正的视觉/行为问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新增边界 | 清理死代码 + 测试 + 验证门；删除的死目录本就无静态引用与菜单记录（RESEARCH 裁定） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-70-0701 | DoS | 死目录删除引发白屏 | mitigate | 删除前运行库防御查询（sys_menu component LIKE 'system/captcha-background%' = 0）；规范种子 grep 双保险；[ASSUMED] 情形在 SUMMARY 留痕 |
| T-70-0702 | Tampering | settingsStore 删除越界 | mitigate | 仅删 exportPreferences/importPreferences 两处；单字段 actions 完好性由 70-05 单测 + 本 plan 四门共同守护 |
| T-70-SC | Tampering | npm installs | accept | 零新增依赖，legitimacy gate N/A |
</threat_model>

<verification>
- Task 1/2/3 的 automated 命令全部 exit 0
- 截图矩阵 ≥8 张归档（QA-04 风格，chrome-devtools 辅助）
- 迁移运行时双启验证（首启 changed / 二启 no-op）
</verification>

<success_criteria>
- D-12 清理清单 7 项全部闭环并有 grep 门证据
- 两页注册表被纯数据断言锁定
- 七门回归全绿 + 截图 checkpoint 通过
- Phase 70 可进入 /gsd:verify-work
</success_criteria>

<output>
Create `.planning/phases/70-settings-page-redesign/70-07-SUMMARY.md` when done
</output>
