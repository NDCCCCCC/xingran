---
phase: 66-component-styles-and-hardcoded-color-scan
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - xingran-react-frontend/src/design-system/components/AntdThemeBridge.tsx
  - xingran-react-frontend/src/design-system/tokens/colors.test.ts
  - xingran-react-frontend/src/index.css
  - xingran-react-frontend/src/components/layout/sidebar.tsx
  - xingran-react-frontend/src/design-system/tokens/echartsTheme.ts (新建)
  - xingran-react-frontend/src/components/dashboard/widgets/types/ChartWidget.tsx
  - xingran-react-frontend/src/components/network/MACHeatmapChart.tsx
  - xingran-react-frontend/src/components/charts/EChartsWrapper.tsx
  - xingran-react-frontend/src/pages/workorder/statistics/index.tsx
  - xingran-react-frontend/scripts/check-hardcoded-colors.mjs (新建)
  - xingran-react-frontend/package.json
  - xingran-react-frontend/src/ 下 ~95 个文件的批量色值替换（T4 sed/fix 扫描，只改色值字面量）
autonomous: false
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, QA-02]

must_haves:
  truths:
    - "D-01: 全站单一品牌上下文不回归 —— 本 phase 只改色值与主题令牌（D-05），layoutStore 布局/密度/折叠宽度逻辑零改动"
    - "D-02: 所有新增/替换色值唯一来源为 brand-spec.md 与 tokens/colors.ts xingranBrand 常量；推导值（focus 环、hover 光晕、zebra）复用已验证对比度的品牌基色"
    - "D-03: 主按钮 #156031 绿底 #FFFFFF 白字（7.64:1）、hover #2E7444；Button.primaryColor 在 theme 层锁定白字；全站无铜金实心主按钮"
    - "COMP-01: light 模式侧边栏底色 #14532D 深绿，hover/active #156031 底 + active 文字 #E0E0B0（5.62:1），折叠/展开两态正确；顶栏保持白底 64px 不被 --sidebar-bg 染绿"
    - "COMP-02: 表头底 #E9EFEB、分割线 #DBD7CE、斑马纹接令牌、白卡 #FFFFFF 衬 #F0ECE3 双层纸感；排序/筛选/选中态接品牌令牌"
    - "COMP-04: Input focus 环品牌绿；默认 Tag #FEF3C7 底 + #B88850 字（语义 Tag 保留功能色）；ECharts 系列色为绿金梯度 #156031/#3B784C/#C09058/#598E5E/#C89868，无默认蓝紫"
    - "QA-02: scripts/check-hardcoded-colors.mjs 对 src/ 扫描 indigo/slate/Antd 旧色板/rgba(79,70,229) 零命中，非零退出；已链接进 npm run lint；既有残留全部替换为品牌 token 清零"
  artifacts:
    - path: "xingran-react-frontend/src/design-system/components/AntdThemeBridge.tsx"
      provides: "theme.components 补齐 Phase 64 UAT 四 Gap（Table/Input/Tag/Button）+ COMP-02 排序筛选选中态令牌"
      contains: "paleYellow"
    - path: "xingran-react-frontend/src/index.css"
      provides: "light 侧栏深绿变量 + --header-bg 解耦 + modern-tag 品牌化 + indigo rgba 清零"
      contains: "--header-bg"
    - path: "xingran-react-frontend/src/design-system/tokens/echartsTheme.ts"
      provides: "ECharts 品牌系列色/热力梯度/轴线色常量（canvas 安全，不走 CSS var）"
      contains: "brandSeriesColors"
    - path: "xingran-react-frontend/scripts/check-hardcoded-colors.mjs"
      provides: "QA-02 硬编码色扫描 + --fix 确定性映射"
      contains: "check-hardcoded-colors"
  key_links:
    - from: "src/components/layout/sidebar.tsx"
      to: "src/index.css --sidebar-* 变量 + AntdThemeBridge Menu dark 令牌"
      via: "移除内联 background #ffffff + Menu theme=dark + .ant-menu-dark CSS"
      pattern: "theme=\"dark\""
    - from: "src/index.css .ant-layout-header"
      to: ":root/[data-color-mode=dark] --header-bg"
      via: "header 背景与 sidebar 变量解耦（否则 light 深绿侧栏会把顶栏染绿）"
      pattern: "var\\(--header-bg\\)"
    - from: "src/design-system/tokens/echartsTheme.ts"
      to: "ChartWidget.tsx / MACHeatmapChart.tsx / workorder statistics"
      via: "import { brandSeriesColors, brandHeatRamp } 注入 option.color / visualMap.inRange"
      pattern: "brandSeriesColors"
    - from: "package.json lint script"
      to: "scripts/check-hardcoded-colors.mjs"
      via: "eslint . && node scripts/check-hardcoded-colors.mjs（CI frontend-build.yml 沿 npm run lint 自动覆盖）"
      pattern: "check-hardcoded-colors"
---

# Phase 66 Plan 01: 通用组件样式 + 硬编码色扫描

**Status:** READY FOR EXECUTION
**Created:** 2026-08-18
**Source:** ROADMAP Phase 66（COMP-01/02/03/04 + QA-02）+ Phase 64 64-UAT.md 四 Gap 诊断 + Phase 65 65-01-SUMMARY Risks（light sidebar 白底 override / rgba(79,70,229) 残留 / modern-tag 旧色段）+ v1.22 锁定决策 D-01/D-02/D-03/D-05

## Goal

侧边栏 / 表格 / 卡片 / 按钮 / 表单 / 标签 / 图表全量接品牌令牌，落地 D-03 按钮纪律；补齐 Phase 64 UAT 四 Gap；全仓清除 indigo/slate/Antd 旧色板硬编码并以扫描脚本 + lint 链阻止新增，确保 v1.22 品牌效果不被后续代码破坏。纯视觉层重构（D-05）：只改色值与令牌映射，不改业务逻辑、不改布局结构。

## Gap Closure Inventory（Phase 64 UAT 四 Gap + Phase 65 遗留）

| # | Gap | 根因（已核实） | 闭环任务 |
|---|-----|---------------|---------|
| G1 | Table 表头底 `#E9EFEB` | 代码层 `Table.headerBg` 已在（5c716f1 落地），但 UAT 时点（11:48）运行中构建早于改动（11:21）未复测；且排序/筛选/选中态令牌缺失 | T1（补齐 + T6 运行时复测） |
| G2 | Input focus 边框 `#156031` | 同上：`Input.activeBorderColor/hoverBorderColor` 已在，缺 `activeShadow` 焦点环 | T1（补齐 + T6 复测） |
| G3 | Tag 默认 `#FEF3C7` 底 + `#B88850` 字 | 真实缺失：当前 `Tag.defaultBg = cream.headerBg (#E9EFEB)`，非品牌锚点配方 | T1 |
| G4 | 主按钮文字 `#FFFFFF`（7.64:1 最优） | 真实缺失：`Button.primaryColor` 未在 theme 层锁定（antd 6.1.1 该 token 默认 colorTextLightSolid；index.css `.ant-btn-primary` 的 `!important` 白字是唯一保险，非持久机制） | T1 |
| G5 | light 模式侧边栏白底 | 双根因：(a) index.css:146-154 `[data-color-mode="light"]` 把 `--sidebar-*` 覆写回白色；(b) sidebar.tsx:277 Sider 内联 `background: "#ffffff"` 直接压过变量层 | T2 |
| G6 | `rgba(79,70,229,*)` indigo 光晕 36 处 + modern-tag-* Antd 旧色板 | Phase 64/65 遗留 | T4 |

## Context（关键证据，executor 免探索）

### 1. 侧边栏染色链（COMP-01 的真实堵点）

```
sidebar.tsx:276-278  <Sider style={{ background: "#ffffff" }}>   ← 内联白底，压过一切
index.css:146-154    [data-color-mode="light"] { --sidebar-bg:#ffffff; ... }  ← 变量层覆写回白
index.css:62-69      :root { --sidebar-bg:#14532d; ... }          ← 深绿早已定义，被 light 块遮蔽
index.css:416-423    .ant-layout-sider { background: var(--sidebar-bg) !important }
index.css:398-411    .ant-layout-header { background: var(--sidebar-bg) !important }  ← 坑：顶栏也消费此变量
index.css:425-465    .ant-menu-dark 系列规则已就绪（selected 用 --sidebar-bg-active + --sidebar-text-active）
```

**必须先解耦再改色**：新增 `--header-bg`（light `#ffffff` / dark 沿用 `#0a2418`），`.ant-layout-header` 两条规则改指 `--header-bg`；否则 light 侧栏改深绿会把 64px 顶栏一起染绿，直接违反 COMP-01「顶栏保持白底」。

sidebar.tsx 其余清理点：Menu（342-355 行）当前无 `theme` prop 且 `color: var(--theme-text-primary)`（深绿底上会不可读）；折叠按钮（290-299 行）`hover:bg-gray-100 dark:hover:bg-gray-800`；logo 渐变 fallback `#3b82f6`（321-334 行）。

### 2. AntdThemeBridge 现状（antd 6.1.1，token 名已对 node_modules 核实）

已有：`theme.token` 16 项品牌映射；`Table.headerBg/headerColor/borderColor/rowHoverBg`、`Input.activeBorderColor/hoverBorderColor`、`Select` 同款、`Menu.dark*`（134-137 行为字符串字面量 `"#14532D"` 等，应换 token 引用）、`Tabs` 四项、`Tag.defaultBg=cream.headerBg`、`Card`。
缺失：`Button.primaryColor`（antd 6.1.1 `node_modules/antd/es/button/style/token.d.ts:34` 确认存在，默认 `colorTextLightSolid`）；`Tag.defaultBg/defaultColor` 品牌锚点配方；`Input/Select.activeShadow`；Table 排序/筛选/选中态令牌。

### 3. xingranBrand 可用常量（tokens/colors.ts，勿新建重复色值）

`greenPrimary #156031` / `greenPrimaryHover #2E7444` / `greenPrimaryActive #14542E` / `greenPrimaryLight #1A6839`；`green[900] #14532D`、`green[200] #3B784C`、`green[100] #598E5E`、`green[50] #E9EFEB`；`copper[500] #B88850`、`copperAccent #C09058`、`copper[700] #C89868`；`cream.canvas #F0ECE3 / surface #FFFFFF / fg #101010 / muted #707068 / border #DBD7CE / borderStrong #C2BDB2 / headerBg #E9EFEB / zebraBg #F7F5EE`；`onDark.white #FFFFFF / lightYellow #E0E0B0 / paleYellow #FEF3C7`；`functional.success #2D8949 / warning #B07A20 / warningText #905D00 / danger #BA3630 / info #337AB0`。

### 4. ECharts 消费方清单（canvas 不解析 CSS var —— 现有 `var(--xxx, #fallback)` 写法在 canvas 上不生效，属潜在渲染缺陷）

- `src/components/dashboard/widgets/types/ChartWidget.tsx`：78/83/110/115/188/193 行轴线与分割线用 `var(--theme-border-*, #d9d9d9/#f0f0f0)`；91/122/208 行系列色 `display.colors?.[0] ?? "var(--theme-info, #1890ff)"`；209 行渐变端 `rgba(24,144,255,0.1)`；229 行 textStyle。`display.colors`（用户配置）优先级保留，仅替换 fallback 与轴线。
- `src/components/network/MACHeatmapChart.tsx`：25-30 行 `getHeatColor` 阈值色 `#bfbfbf/#50a3ba/#fad252/#eac736/#d94e5d`；86 行 `visualMap.inRange.color` 同款三元组。
- `src/pages/workorder/statistics/index.tsx`：echarts option 需注入 `color: brandSeriesColors`；180-212 行 Statistic 的 `var(--xxx, #faad14/#1890ff/#52c41a/#8c8c8c)` fallback 由 T4 批量清。
- `src/components/charts/EChartsWrapper.tsx`：41 行 loading 文字 `rgba(0,0,0,0.45)`（DOM 元素，可走 var）。
- `src/lib/echarts.ts`：MAC 专用按需注册，不动。

### 5. QA-02 残留面（2026-08-18 实测计数）

| 家族 | 命中 | 处置 |
|------|------|------|
| `rgba(79,70,229,*)` indigo 光晕 | 36（全在 index.css） | → `rgba(21,96,49,*)` 品牌绿光晕 |
| `#1890ff` Antd 旧蓝 | 105（多为 var() fallback，93 文件） | → `#337ab0`（--theme-info 品牌信息色） |
| `#ff4d4f` | 91 | → `#ba3630`（--theme-error） |
| `#52c41a` / `#faad14` | 40 / 27 | → `#2d8949` / `#b07a20` |
| `#8c8c8c` `#bfbfbf` | 26 / 5 | → `#707068`（muted） |
| `#d9d9d9` / `#f0f0f0` | 28 / 29 | → `#dbd7ce` / `#e9efeb` |
| `#3b82f6` / `#2563eb` 蓝色 fallback | 8 / 3 | → `#156031` / `#14542e` |
| `rgba(24,144,255 / 82,196,26 / 250,173,20 / 255,77,79 / 140,140,140,*)` | 20 | → 对应品牌功能色同透明度 |
| modern-tag-* 五组 + dark 五组 | index.css 4448-4536 | light 组手写替换为 CSS 变量；dark 组 bg/border 走映射，文字保留浅化功能色并加扫描器 allowlist |
| `#4F46E5` / `#F1F5F9` / slate 十进制族 | 0 | 扫描器 denylist 防新增 |
| 冻结例外 | `src/utils/three/colors.ts`（65-01 决策：legacy 6 值冻结，VIS-01 v1.23+） | 扫描器路径 allowlist |

### 6. 工具链事实

- Phase 63 husky/lint-staged 尚未落地（package.json 无 lint-staged 字段、无 .husky/）→ QA-02 集成点为 `npm run lint` 链 + CI `frontend-build.yml`（沿 lint 步骤自动覆盖，executor 需确认 CI 含 lint 步骤，无则在 plan SUMMARY 记录跟进项，不擅自改 CI 属 Phase 63 范围的 hook）。
- **65-01 教训：`npm run type-check` 是 solution-style 空检查，有效类型门是 `npm run build` 内的 `tsc -b`。** T1/T4 的 build 门不可省。
- `npm test -- --run src/design-system/tokens/colors.test.ts` 当前 31/31 绿，新断言加此文件。
- scripts/ 目录已有 `sync-columns-schema.mjs`（.mjs 先例，node 内置模块，零新依赖）。

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: AntdThemeBridge theme.components 补齐 4 Gap + COMP-02 令牌扩展 + 对比度断言</name>
  <files>xingran-react-frontend/src/design-system/components/AntdThemeBridge.tsx, xingran-react-frontend/src/design-system/tokens/colors.test.ts</files>
  <behavior>
    - colors.test.ts 新增断言：`#101010 (cream.fg) on #E9EFEB (cream.headerBg) ≥ 12:1`（表头文字对）
    - colors.test.ts 新增断言：`brandSeriesColors` 由 xingranBrand 品牌常量组成（与 T3 联动，此 task 先落 Table/Input 相关，echarts 断言允许 T3 一并补）
    - 存量 31 断言零回归（含 D-03 反向断言 `#FFFFFF on #C09058 < 3.5:1`）
  </behavior>
  <action>
    按 COMP-02/COMP-03/COMP-04 扩展 `theme.components`（全部从 xingranBrand 读取，禁止新增字面量）：
    1. **G4/CMP-03**：`Button` 增加 `primaryColor: xingranBrand.onDark.white`（antd 6.1.1 ComponentToken，主按钮文字色；theme 层锁定 D-03 白字 7.64:1，摆脱对 index.css `!important` 的单点依赖）。
    2. **G3/COMP-04**：`Tag.defaultBg → xingranBrand.onDark.paleYellow (#FEF3C7)`、`defaultColor → xingranBrand.copper[500] (#B88850)`（登录页 SM2/SM3/SM4 品牌锚点配方）；语义 Tag（success/error/warning 及 Tag color="xx" 预设色）走 functional 色不受影响，勿动 preset 色逻辑。已知对比度风险见 Constraints #3。
    3. **G2/COMP-04**：`Input` 与 `Select` 各增加 `activeShadow: "0 0 0 2px rgba(21, 96, 49, 0.15)"`（品牌绿 2px 焦点环，基色 #156031 为已验证 7.64:1 的实测量）；确认 `activeBorderColor/hoverBorderColor = greenPrimary` 仍在。
    4. **G1/COMP-02**：`Table` 补 `headerSortActiveBg: xingranBrand.cream.zebraBg`、`headerFilterHoverBg: xingranBrand.cream.zebraBg`、`fixedHeaderSortActiveBg: xingranBrand.cream.zebraBg`、`rowSelectedBg: xingranBrand.cream.headerBg`、`rowSelectedHoverBg: xingranBrand.cream.zebraBg`、`emptyColor: xingranBrand.cream.muted`；确认 `headerBg/headerColor/borderColor/rowHoverBg` 仍在（borderColor=border #DBD7CE 即分割线令牌）。
    5. `Menu.dark*` 四项（134-137 行）字符串 `"#14532D"/"#156031"/"#1A6839"` 换成 `xingranBrand.green[900]/greenPrimary/greenPrimaryLight/onDark.lightYellow` 引用（QA-02 卫生）。
    6. colors.test.ts 按 behavior 补断言；不为 `#B88850 on #FEF3C7` 添加 AA 断言（见 Constraints #3，不加必失败断言）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npx vitest run src/design-system/tokens/colors.test.ts && npm run build</automated>
  </verify>
  <done>四 Gap 的 theme 层令牌全部就位且从 xingranBrand 读取；对比度测试全绿；tsc -b 零错误。独立 commit：feat(66-01): close phase-64 UAT gaps in AntdThemeBridge components (Table/Input/Tag/Button)</done>
</task>

<task type="auto">
  <name>Task 2: 侧边栏深绿化（COMP-01）+ header/sidebar 变量解耦</name>
  <files>xingran-react-frontend/src/index.css, xingran-react-frontend/src/components/layout/sidebar.tsx</files>
  <action>
    顺序执行（先解耦后改色，防止顶栏被染绿）：
    1. index.css `:root`（141 行前）新增 `--header-bg: #ffffff;`；`[data-color-mode="dark"]`（侧边栏系统段）新增 `--header-bg: #0a2418;`（保持暗色顶栏现状视觉）。
    2. `.ant-layout-header`（398 行）与 `[data-color-mode="dark"] .ant-layout-header`（407 行）的 `background: var(--sidebar-bg)` 改为 `var(--header-bg)`。
    3. `[data-color-mode="light"]` 侧边栏段（146-154 行）整段替换为深绿配方（D-02 实测值）：`--sidebar-bg: #14532d; --sidebar-bg-hover: #156031; --sidebar-bg-active: #156031; --sidebar-border: #1a6839; --sidebar-text: #f0ece3; --sidebar-text-hover: #ffffff; --sidebar-text-active: #e0e0b0; --sidebar-accent: #c09058;`（active 文字 #E0E0B0 深绿底 5.62:1，hover 文字白 7.64:1，均达 SC#1 对比度要求）。
    4. sidebar.tsx：删除 Sider 的 `style={{ background: "#ffffff" }}`（276-278 行，交由 `.ant-layout-sider` 的 var 层）；Menu（342 行起）加 `theme="dark"` 激活 bridge 的 dark Menu 令牌与 `.ant-menu-dark` CSS，style 的 `color: var(--theme-text-primary)` 改 `var(--sidebar-text)`；折叠按钮 className `hover:bg-gray-100 dark:hover:bg-gray-800` 改为品牌 hover（如 `hover:bg-[#1a6839]`），`color: var(--theme-text-secondary)` 改 `var(--sidebar-text)`；logo 渐变 fallback `#3b82f6` 改 `#156031`。
    5. 布局宽度/折叠宽度（layoutStore 64/56/80、DEFAULT_SIDEBAR_WIDTH 280）一律不动 —— THEME-03 边界，本 phase 只换颜色（D-05）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npm run build && sed -n '/\[data-color-mode="light"\]/,/^}/p' src/index.css | grep -c '\-\-sidebar-bg: #14532d' && ! sed -n '/\[data-color-mode="light"\]/,/^}/p' src/index.css | grep -q '\-\-sidebar-bg: #ffffff' && ! grep -n 'background: "#ffffff"' src/components/layout/sidebar.tsx && grep -n 'var(--header-bg)' src/index.css | head -3</automated>
  </verify>
  <done>light 块侧栏变量为深绿配方（定向断言 light 块内 `--sidebar-bg` 不再为 #ffffff，避免 :root 既有 #14532d 恒真误报）；`.ant-layout-header` 消费 `--header-bg`；sidebar.tsx 无内联白底、Menu 为 dark 主题；`npm run build`（tsc -b 有效类型门）过。独立 commit：feat(66-01): deep-green light-mode sidebar with header var decoupled (COMP-01)</done>
</task>

<task type="auto">
  <name>Task 3: ECharts 品牌系列色集中定义 + 消费方应用（COMP-04）</name>
  <files>xingran-react-frontend/src/design-system/tokens/echartsTheme.ts (新建), xingran-react-frontend/src/components/dashboard/widgets/types/ChartWidget.tsx, xingran-react-frontend/src/components/network/MACHeatmapChart.tsx, xingran-react-frontend/src/components/charts/EChartsWrapper.tsx, xingran-react-frontend/src/pages/workorder/statistics/index.tsx</files>
  <action>
    1. 新建 `src/design-system/tokens/echartsTheme.ts`：从 `xingranBrand` 组装导出（D-02 单一来源，canvas 安全的字面量）：`brandSeriesColors = [greenPrimary #156031, green[200] #3B784C, copperAccent #C09058, green[100] #598E5E, copper[700] #C89868]`（ROADMAP SC#4 顺序）；`brandHeatRamp = [green[50] #E9EFEB, green[100] #598E5E, greenPrimary #156031, copperAccent #C09058, copper[500] #B88850]`（低→高热力）；`brandAxisLine = cream.border`、`brandSplitLine = green[50]`、`brandTextStyle = cream.muted`、`brandAreaFade = "rgba(21, 96, 49, 0.1)"`。
    2. ChartWidget.tsx：轴线 `var(--theme-border-primary, #d9d9d9)` → `brandAxisLine`、分割线 `var(--theme-border-secondary, #f0f0f0)` → `brandSplitLine`、系列色 fallback `var(--theme-info, #1890ff)` → `brandSeriesColors[0]`（保留 `display.colors?.[0] ??` 用户配置优先链）、面积渐变端 `rgba(24,144,255,0.1)` → `brandAreaFade`、 textStyle `var(--theme-text-tertiary, #bfbfbf)` → `brandTextStyle`。
    3. MACHeatmapChart.tsx：`getHeatColor` 阈值返回值与 `visualMap.inRange.color` 改用 `brandHeatRamp` 对应档位（ratio 0 → #E9EFEB，<0.25 → #598E5E，<0.5 → #156031，<0.75 → #C09058，else → #B88850；max<=0 零态 #C2BDB2）；只改色值不改阈值逻辑（D-05）。
    4. workorder/statistics/index.tsx：echarts option 顶层注入 `color: brandSeriesColors`（若多处 option 则统一注入）。
    5. EChartsWrapper.tsx：loading 文字色 `rgba(0,0,0,0.45)` → `var(--theme-text-secondary)`。
    6. colors.test.ts 补断言：`brandSeriesColors` 逐项等于对应 xingranBrand 常量且长度 5、无蓝紫（D-02 推导值锁定）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npm run build && npx vitest run src/design-system/tokens/colors.test.ts && ! grep -rn "1890ff\|50a3ba\|eac736\|d94e5d\|fad252" src/components/dashboard src/components/network src/pages/workorder</automated>
  </verify>
  <done>图表消费方零旧色板字面量；系列色/热力梯度/轴线全部出自 echartsTheme；断言绿。独立 commit：feat(66-01): brand echarts palette via tokens/echartsTheme (COMP-04)</done>
</task>

<task type="auto">
  <name>Task 4: QA-02 硬编码色全仓清除 + 扫描器/lint 防回归门</name>
  <files>xingran-react-frontend/scripts/check-hardcoded-colors.mjs (新建), xingran-react-frontend/package.json, xingran-react-frontend/src/index.css, xingran-react-frontend/src/ 下批量替换的 ~95 文件</files>
  <action>
    1. 新建 `scripts/check-hardcoded-colors.mjs`（node 内置 fs/path，零依赖）：递归扫 `src/**/*.{ts,tsx,css}`，正则大小写不敏感匹配 denylist 家族；报告模式输出 `file:line: match` 并 exit 1；`--fix` 模式应用确定性映射后重扫。**路径 allowlist**：`src/utils/three/colors.ts`（65-01 冻结决策，VIS-01 v1.23+）、`src/design-system/tokens/`（真相源本体）、`scripts/`。**值 allowlist**（附注释原因）：`#95de64/#ff7875/#ffc53d/#69c0ff`（modern-tag dark 文字浅化功能色，深底 bg-elevated #1f3524 上 ≈7-9:1）。denylist 与 fix 映射表：`#4F46E5/#F1F5F9/slate 十进制族(#0f172a #1e293b #334155 #475569 #64748b #94a3b8 #cbd5e1 #e2e8f0 #f8fafc)/#1677ff/#3b82f6/#2563eb/#50a3ba/#fad252/#eac736/#d94e5d/#6366f1/#8b5cf6/#22c55e/#f59e0b/#ef4444` 一律报告（映射：蓝紫 indigo/violet `#6366f1/#8b5cf6` → `#156031`/`#c09058`；tailwind 功能色 `#22c55e→#2d8949`、`#f59e0b→#b07a20`、`#ef4444→#ba3630`（即 xingranBrand.functional 对应值）；蓝→`#156031`/`#14542e`，热力→`#ba3630` 系由 echartsTheme 常量替代后自然零命中）；`rgba(79,70,229,α)→rgba(21,96,49,α)`；`rgba(37,42,63,α)→rgba(15,46,27,α)`（冷 slate 深色斑马纹/底 → 深绿深底推导）；`#1890ff→#337ab0`；`#ff4d4f→#ba3630`；`#52c41a→#2d8949`；`#faad14→#b07a20`；`#8c8c8c/#bfbfbf→#707068`；`#d9d9d9→#dbd7ce`；`#f0f0f0→#e9efeb`；`rgba(24,144,255,α)→rgba(51,122,176,α)`；`rgba(82,196,26,α)→rgba(45,137,73,α)`；`rgba(250,173,20,α)→rgba(176,122,32,α)`；`rgba(255,77,79,α)→rgba(186,54,48,α)`；`rgba(140,140,140,α)→rgba(112,112,104,α)`。**注意**：InnovativeLayout.tsx / QuickNav.tsx / HealthBadge.tsx / HealthCard.tsx 含上述 tailwind/indigo 族活渲染色（约 14 处），属本 task 替换范围（只改色值不改布局逻辑，THEME-03 边界内）；`var(--theme-purple, #8b5cf6)` 的 fallback 一并替换（若 `--theme-purple` 未定义则该 var 恒取 fallback，是活渲染色）。所有替换均为语义等价品牌色（D-05 只改色值）。
    2. index.css modern-tag light 五组（4448-4505 行）手写替换为 CSS 变量：success → `var(--theme-success-bg)` 底 + `var(--theme-success)` 字 + `rgba(45,137,73,0.35)` 边；error → `var(--theme-error-bg)` + `var(--theme-error)` + `rgba(186,54,48,0.35)`；warning → `var(--theme-warning-bg)`(#fef3c7) + `var(--theme-warning-text)`(#905d00，brand-spec 实测淡黄底 5.03:1) + `rgba(144,93,0,0.35)`；default → `var(--theme-neutral-50)` + `var(--theme-text-secondary)` + `rgba(112,112,104,0.35)`；processing → `rgba(51,122,176,0.12)` + `var(--theme-info)` + `rgba(51,122,176,0.35)`。dark 五组 bg/border 按映射表替换，文字保留浅化功能色（值 allowlist 覆盖）。类名不变，消费方零感知。
    3. 运行 `node scripts/check-hardcoded-colors.mjs --fix` 清残余，再跑报告模式确认零命中；`src/utils/three/colors.ts` 与 `src/pages/login/login.css` 不在 denylist 命中范围（登录页 FEF3C7/D4A574 为品牌锚点保留）。
    4. package.json：新增 `"check:colors": "node scripts/check-hardcoded-colors.mjs"`；`"lint": "eslint ."` 改为 `"lint": "eslint . && node scripts/check-hardcoded-colors.mjs"`。
    5. 确认 `.github/workflows/frontend-build.yml` 含 lint 步骤（沿 npm run lint 即自动覆盖）；若不含，SUMMARY 记录跟进项交 Phase 63，不擅自改 CI hook。
    6. SC#3 铜金纪律核查：`grep -rE "#C09058|#B88850" src/ --include="*.tsx"` 中确认无 `Button type="primary"` 实心场景（铜金仅描边/图标/图表系列/Tag 背景）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && node scripts/check-hardcoded-colors.mjs && npm run lint && npm run build && npx vitest run</automated>
  </verify>
  <done>扫描器 exit 0（含 allowlist 生效）；npm run lint（含色扫描链）与 build 全绿；全量 vitest 零回归（colors.test.ts 的 index.css 文本断言在 rgba 替换后仍绿）；铜金实心主按钮零命中。独立 commit：feat(66-01): purge hardcoded colors and add scanner lint gate (QA-02)</done>
</task>

<task type="auto">
  <name>Task 5: 全量回归四门 + Gap 证据归档 + Phase 67 视觉确认准备</name>
  <files>xingran-react-frontend/（无源码改动，仅验证与记录）</files>
  <action>
    1. 依次跑四门并记录输出摘要：`npm run type-check`（空检查，记录而已）、`npm run build`（tsc -b 真类型门 + vite build）、`npm run lint`（含 check:colors 链）、`npx vitest run`（全量，Phase 65 后基线 14 files / 111 tests）。
    2. 四 Gap 结构证据归档（写进 SUMMARY）：grep AntdThemeBridge 确认 `Table.headerBg`、`Input.activeBorderColor`、`Tag.defaultBg.*paleYellow`、`Button.*primaryColor.*white` 四项在；index.css light 块 `#14532d` 在、`.ant-layout-header` 用 `--header-bg`。
    3. 记录 build 输出的 vendor-react gzip 体积（ informational，QA-03 硬门在 Phase 67）。
    4. 产出 Phase 67 QA-04 六屏核对清单（仪表盘 / 系统用户 / 工位管理 / 监控仪表盘 / 资产对账看板 / 登录页）+ 每屏预期品牌值速查（侧栏 #14532D、表头 #E9EFEB、主按钮 #156031 白字、Tag #FEF3C7/#B88850、图表绿金梯度），写入 SUMMARY 供 Phase 67 直接取用。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npm run build && npm run lint && npx vitest run && grep -c "paleYellow" src/design-system/components/AntdThemeBridge.tsx</automated>
  </verify>
  <done>四门全绿；四 Gap 结构证据与六屏清单落入 SUMMARY；vendor-react gzip 数值已记录。独立 commit：chore(66-01): full regression gates and phase-67 visual checklist</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Phase 64 UAT 四 Gap + 组件品牌化目检（冷启动）</name>
  <what-built>COMP-01..04 组件品牌化 + Phase 64 UAT 四 Gap 的 theme 层闭环（T1-T5 全部自动化完成）</what-built>
  <how-to-verify>
    背景：Phase 64 UAT 曾在代码已改的情况下测得旧值（运行中构建过期），因此本 checkpoint 必须全新启动验证：
    1. `cd xingran-react-frontend && npm run dev`（冷启动，勿复用旧进程），登录进入后台
    2. 侧边栏：底色 #14532D 深绿；hover 菜单项 #156031 底；选中项 #156031 底 + #E0E0B0 浅黄文字；折叠/展开两态均正确（展开 280px）
    3. 顶栏：白底 64px 不变，面包屑 / ⌘K 全局搜索 / 通知铃铛 / 用户菜单视觉正常
    4. 任一列表页（如 /system/user）：表头 #E9EFEB 绿灰淡彩、分割线 #DBD7CE、行 hover 正常
    5. 任一 Input 聚焦：边框 #156031 品牌绿 + 绿色焦点环（非黑色）
    6. 默认 Tag（如 ⌘K 提示 Tag）：#FEF3C7 淡黄底 + #B88850 铜金字；语义状态 Tag（启用/停用）仍为功能色
    7. 任一主按钮：#156031 绿底 #FFFFFF 白字，hover #2E7444；按钮 hover/focus 光晕为绿色（无任何紫色/indigo 残留）
    8. 仪表盘图表：系列色为绿金梯度（无默认蓝紫）
    9. 切 dark 模式（设置页 → 深色模式 → 保存）：侧栏/表头/Tag/图表正常渲染；modern-tag dark 文字可读（#95de64/#ff7875/#ffc53d/#69c0ff 浅化字在深底 ≈7-9:1）；InnovativeLayout / QuickNav 的替换色（绿/铜）在深底同样可读 —— 保护 THEME-02 不被 T4 全局替换破坏
  </how-to-verify>
  <resume-signal>Type "approved" 或描述视觉问题（将路由回 planner 修订）</resume-signal>
</task>

## Constraints

1. **D-05 范围纪律**：只改色值与令牌映射；不改业务逻辑、不改 API、不改数据模型、不改布局结构（layoutStore 宽度/密度/折叠逻辑零改动）；ECharts 只改系列色/热力梯度/轴线色数组。
2. **D-02 色值来源**：新增色值一律出自 brand-spec.md 实测值或 xingranBrand 常量；推导值（焦点环 rgba、光晕 rgba、zebra 边框）必须基于已验证品牌基色同 hue 派生。登录页 login.css 的 #FEF3C7/#D4A574 为品牌锚点，保留不动。
3. **已知对比度风险（FLAG）**：G3 Tag 默认配方 `#B88850 on #FEF3C7` 实测约 2.8:1，低于 AA 4.5:1 —— 该配方为 orchestrator 锁定决策（登录页 SM2/SM3/SM4 品牌锚点同款、装饰性品牌 Tag），本 plan 照办；语义状态 Tag 保留功能色（warningText #905D00 on #FEF3C7 = 5.03:1 已有断言覆盖）。不为该对添加 AA 断言（必失败）；若 Phase 67 QA-04 目检认为默认 Tag 可读性不足，候选收紧值为 #905D00（brand-spec 已验证）。
4. **T4 批量替换安全网**：sed/fix 后必须重跑全量 vitest（colors.test.ts 有 index.css 文本断言）+ build；`src/utils/three/colors.ts` 冻结不替换；`.test.` 文件如命中 denylist 一并替换后重测。
5. **每 task 独立 commit 前置门**：所有触碰源码的 task（T1-T4）commit 前必须 `npm run build` 全绿（65-01 教训：`npm run type-check` 是 solution-style 空检查，有效类型门是 build 内的 `tsc -b`；T2 改 TSX / T3 新建 TS 模块均为真实编译面）；QA 断言门 `npx vitest run src/design-system/tokens/colors.test.ts`；type-check 仅作记录性输出。
6. **不引入新 npm 依赖**（扫描器为 node 内置 .mjs）—— Package Legitimacy Gate 不适用；不修改 CI workflow 文件（Phase 63 范围），lint 链改造仅在 package.json。

## Verification（phase 级）

- T1-T5 每个 task 的 `<automated>` 门全绿 + 独立 commit（T1-T4 每个边界 `npm run build` 有效门全绿）
- T6 checkpoint 9 步目检全过（含冷启动要求 + dark 模式步骤）
- 结构终扫：`node scripts/check-hardcoded-colors.mjs` exit 0；`grep -rn "rgba(79, ?70, ?229" src/` 零命中；`grep -n 'background: "#ffffff"' src/components/layout/sidebar.tsx` 零命中

## Success Criteria（ROADMAP Phase 66 verbatim）

1. 登录后台后侧边栏底色为 `#14532D` 深绿、折叠态 64px / 展开态 280px 均正确,hover / active 强调用 `#156031` 底 + `#E0E0B0` 浅黄文字(对比度 ≥ 5.6:1);顶栏保持白底 64px,面包屑 / 全局搜索 ⌘K / 通知铃铛 / 用户菜单视觉不破;与品牌深绿(`#14532D` → `#1A6839` 渐变)同源
2. 任一业务页面(如工位管理)的表头底色为 `#E9EFEB` 绿灰淡彩、斑马纹与分割线用 `#DBD7CE`、白卡 `#FFFFFF` 衬 `#F0ECE3` 奶油画布形成双层纸感;表格排序 / 筛选 / 选中态 / 空状态 / 分页器全部接品牌令牌;Card 浮起用 1px 暖灰描边而非重阴影
3. 按钮体系满足 D-03 纪律 —— 主按钮 `#156031` 绿底白字、hover `#2E7444`(白字对比度 ≥ 7.6:1 / 5.68:1);次级(描边绿)、危险(红实心)、禁用、链接、图标按钮全套规范;`#FEF3C7` 从按钮前景移除,回归为 SM2 / SM3 / SM4 淡黄标签底;全站 `grep -rE "#C09058|#B88850" src/ --include="*.tsx" --include="*.css"` 中实心 `background-color: solid` / `Button type="primary"` 零命中,铜金仅出现在描边 / 图标 / 图表系列 / Tag 背景等点缀场景
4. 表单控件 focus 环用品牌绿(`#156031` 2px 焦点环)、校验错误态色阶统一(`#BA3630` + 行内错误文案);Tag / Badge 含 SM2 / SM3 / SM4 淡黄标签 `#FEF3C7` 规范化;Tabs 多页签与面包屑接品牌;ECharts 图表系列色为绿金梯度(`#156031` / `#3B784C` / `#598E5E` / `#C09058` / `#C89868`),无默认蓝紫
5. 全仓扫描脚本(`scripts/check-hardcoded-colors.mjs` 或 `eslint-plugin-no-hardcoded-colors` 自定义规则)对 `src/` 下 `.tsx` / `.ts` / `.css` 文件检查硬编码 `#4F46E5` / `#F1F5F9` / slate 系(`#1e293b` / `#334155` / `#475569` 等)及非品牌裸 hex,命中即非零退出;脚本集成进 `npm run lint` 与 Phase 63 的 CI(`frontend-build.yml`);既有命中通过替换为品牌 token 清零,新文件如有违规即 fail(防回归)

## Threat Model

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-66-01 | Tampering | scripts/check-hardcoded-colors.mjs | mitigate | 零依赖纯 node 内置；denylist/allowlist 常量内联可 review；CI 沿 lint 步骤执行 |
| T-66-02 | Repudiation | 色值批量替换（~95 文件） | mitigate | 每 task 独立 commit + vitest index.css 文本断言 + build 门，替换可追溯可回滚 |
| T-66-SC | Tampering | npm 依赖 | accept | 本 phase 零新增依赖（无 install 步骤），Package Legitimacy Gate 不适用 |

## Output

完成后创建 `.planning/phases/66-component-styles-and-hardcoded-color-scan/66-01-SUMMARY.md`（含四 Gap 证据、四门输出摘要、vendor-react gzip 数值、六屏核对清单、Deviations）
