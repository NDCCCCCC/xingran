---
phase: 61-resource-permission-matrix-and-rate-limit-tuning
plan: 02
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - internal/middleware/apikey.go
  - internal/middleware/select_scope.go
  - internal/middleware/select_scope_test.go
  - internal/middleware/apikey_test.go
  - internal/middleware/apikey_integration_test.go
  - internal/middleware/apikey_rate_limit_test.go
  - internal/services/rate_limiter.go
  - internal/services/rate_limiter_test.go
  - internal/services/cache_config_service.go
  - internal/services/cache_config_service_test.go
  - internal/api/router.go
autonomous: true
requirements_addressed:
  - QUAL-03

must_haves:
  truths:
    - "RateLimitByScope(rateLimiter, action string) 接收 action 参数,注册期算 requiredScope 闭包捕获"
    - "getScopeFromContext 改为 action-aware:从 action 推 required scope,scopes 不含则拒绝 403(无 fallback, fail-closed)"
    - "getRequiredScope 扩展 list→read(view/create/edit/delete→read/write 不变)"
    - "InheritPerms=true 走 default 限额,跳过 action-based scope check(D-13 inherit_perms 短路保留)"
    - "12 个 rate_limit.* config 键(rate_limit.{read|write|admin|default}.{per_minute|per_hour|per_day})注册到 CacheConfigService"
    - "RateLimiter.Check 从 CacheConfigService 读限额(scope 不存在时降级到 default 配置),不再硬编码"
    - "RateLimitByScope 与 RequireScope 职责分工清晰:RequireScope 做硬鉴权,RateLimitByScope 做精细限流(D-14)"
    - "Reload 后新阈值仅对 reload 之后的新请求生效(D-19 在途请求保留旧阈值)"
    - "X-RateLimit-Limit/Remaining 仍是 strconv.Itoa 数字字面量(Phase 60 QUAL-01 回归)"
    - "SelectScope(scopes, inheritPerms, action) 是纯函数,直接可单测,无需 context key 中转"
  artifacts:
    - path: "internal/middleware/apikey.go"
      provides: "D-11/D-12/D-13/D-14 RateLimitByScope + getRequiredScope + getScopeFromContext(薄壳) 改造"
      contains: "RateLimitByScope.*action string"
    - path: "internal/middleware/select_scope.go"
      provides: "D-12 纯函数 SelectScope(scopes, inheritPerms, action) → (scope, allowed)"
      contains: "func SelectScope"
    - path: "internal/middleware/select_scope_test.go"
      provides: "D-20 SelectScope 纯函数单测(直接断言返回值,无 context 中转)"
      min_lines: 60
    - path: "internal/middleware/apikey_test.go"
      provides: "D-20 getRequiredScope list→read 新增 + 既有测试保留"
      contains: "list.*read"
    - path: "internal/middleware/apikey_integration_test.go"
      provides: "D-20 RateLimitByScope 多 scope 选择测试 + 既有 TestRateLimitHeadersInResponse 签名更新"
      contains: "RateLimitByScope(rl,"
    - path: "internal/middleware/apikey_rate_limit_test.go"
      provides: "D-20 中间件单元测试:RateLimitByScope 各 action 选不同 scope"
      min_lines: 80
    - path: "internal/services/rate_limiter.go"
      provides: "D-18 RateLimiter 配置化改造"
      contains: "RateLimitProvider"
    - path: "internal/services/rate_limiter_test.go"
      provides: "D-20 RateLimiter 配置驱动单测(modify existing 449 行测试文件)"
      min_lines: 80
    - path: "internal/services/cache_config_service.go"
      provides: "D-15/D-16/D-17 12 个 rate_limit.* 配置键"
      contains: "rate_limit.read.per_minute"
    - path: "internal/services/cache_config_service_test.go"
      provides: "D-20 rate_limit.* 12 键单元测试"
      min_lines: 80
    - path: "internal/api/router.go"
      provides: "D-11 RateLimitByScope action 参数 + D-18 NewRateLimiter 接收 core.CacheConfigService"
      contains: "RateLimitByScope.*action"
  key_links:
    - from: "internal/middleware/apikey.go"
      to: "internal/middleware/select_scope.go"
      via: "getScopeFromContext 调用 SelectScope"
      pattern: "SelectScope\\("
    - from: "internal/middleware/apikey.go"
      to: "internal/services/rate_limiter.go"
      via: "rateLimiter.Check(key, scope) 调用"
      pattern: "rateLimiter\\.Check"
    - from: "internal/services/rate_limiter.go"
      to: "internal/services/cache_config_service.go"
      via: "RateLimitProvider.GetLimit(scope) 接口"
      pattern: "GetLimit\\(scope\\)"
    - from: "internal/api/router.go"
      to: "internal/middleware/apikey.go"
      via: "RateLimitByScope(rl, action) 调用"
      pattern: "RateLimitByScope\\(.*,\\s*\""
---

<objective>
让 `RateLimitByScope` 的多 scope 选择逻辑从「任意取 scopes[0]」改造为「按 action 推 required scope、scopes 不含则拒绝」的严格语义(D-11/D-12);同时让限流阈值走 `sys_config` 参数表通过 CacheConfigService 实现运维可调(D-15/D-16/D-17/D-18/D-19)。将 scope 选择逻辑提取为纯函数 `SelectScope`,避免 context key 中转,使单元测试可直接断言返回值。

Purpose: 把限流从「硬编码 + 任意 scope」推到「action-aware 严格语义 + 运维可调」,达成 QUAL-03 (ex-FUTURE-APIKEY-02) 的生产调优。

Output: `RateLimitByScope` 接收 action 参数 + 纯函数 `SelectScope` + `getRequiredScope` 扩展 + 12 个 rate_limit.* config 键 + `RateLimiter` 配置化 + 四层测试覆盖。

Wave 2 排序说明:本 Plan 02 在 Wave 1 (Plan 01) 完成后执行,因为 Plan 02 修改 `internal/middleware/apikey.go` 的 RateLimitByScope/getRequiredScope/getScopeFromContext 函数,Plan 01 修改同文件的 MultiAuth/setUserContextForAPIKey/RequireAPIKeyResourcePermission 函数。同一文件的并发修改会引发合并冲突,且 Plan 01 末尾明确「编译失败由 Plan 02 修复」,故按 Plan 01 → Plan 02 顺序执行(消除 parallel write race)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/61-resource-permission-matrix-and-rate-limit-tuning/61-CONTEXT.md
@.planning/phases/61-resource-permission-matrix-and-rate-limit-tuning/61-01-PLAN.md
@.planning/phases/60-security-hardening-and-enable-decision/60-CONTEXT.md
@internal/middleware/apikey.go
@internal/services/rate_limiter.go
@internal/services/cache_config_service.go
@internal/api/router.go
@internal/middleware/apikey_integration_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 改造 RateLimitByScope + getRequiredScope + getScopeFromContext(D-11/D-12/D-13/D-14)+ 提取纯函数 SelectScope + 既有测试签名更新 + 新增中间件单元测试</name>
<files>internal/middleware/apikey.go, internal/middleware/select_scope.go, internal/middleware/select_scope_test.go, internal/middleware/apikey_test.go, internal/middleware/apikey_integration_test.go, internal/middleware/apikey_rate_limit_test.go</files>
  <read_first>
    - internal/middleware/apikey.go(RateLimitByScope 行 248-285,getScopeFromContext 行 289-308,getRequiredScope 行 229-243)
    - .planning/phases/61-resource-permission-matrix-and-rate-limit-tuning/61-CONTEXT.md(D-11/D-12/D-13/D-14 完整语义)
    - internal/middleware/apikey_integration_test.go(TestRateLimitHeadersInResponse 行 238-281 签名调用点 + TestConstructorsCallable_D02 行 310-312 RateLimitByScope(rl) 调用)
    - internal/middleware/apikey_test.go(TestGetRequiredScope 行 104-135 既有用例)
  </read_first>
  <action>
    **第 0 步(重构前置):提取纯函数 SelectScope**(D-12 fail-closed + 测试可观测性):

    新建文件 `internal/middleware/select_scope.go`:

    ```go
    package middleware

    // SelectScope 是从 scopes 集合 + 继承权限标志 + 操作类型推导限流 scope 的纯函数。
    //
    // 语义(D-11/D-12/D-13):
    //   1. inheritPerms == true → 短路返回 ("default", true),不做 action-based 检查
    //   2. 否则计算 requiredScope := scopeMap[action](默认 read)
    //   3. scopes 包含 requiredScope → 精确匹配返回 (requiredScope, true)
    //   4. scopes 包含 "admin" → 返回 ("admin", true)(admin 最高限额)
    //   5. 都不含 → 返回 ("", false)(fail-closed,无 fallback)
    //
    // Phase 61 QUAL-03 引入,替代原 getScopeFromContext 直接读 context 的实现,
    // 使单元测试无需 context key 中转,直接断言纯函数返回值。
    func SelectScope(scopes []string, inheritPerms bool, action string) (scope string, allowed bool) {
        if inheritPerms {
            return "default", true
        }
        required := getRequiredScope(action)
        for _, s := range scopes {
            if s == required {
                return required, true
            }
        }
        for _, s := range scopes {
            if s == "admin" {
                return "admin", true
            }
        }
        return "", false
    }
    ```

    新建文件 `internal/middleware/select_scope_test.go`(D-20 纯函数单测):

    1. `TestSelectScope_InheritPermsShortCircuit`:`SelectScope([]string{"system:user:list"}, true, "list")` 返回 `("default", true)`(D-13 短路)
    2. `TestSelectScope_ExactMatch`:`SelectScope([]string{"read"}, false, "list")` 返回 `("read", true)`(list → read)
    3. `TestSelectScope_ExactMatchWrite`:`SelectScope([]string{"write"}, false, "edit")` 返回 `("write", true)`
    4. `TestSelectScope_AdminOverride`:`SelectScope([]string{"read", "admin"}, false, "edit")` 返回 `("admin", true)`(edit 需要 write,read 不含,但 admin 覆盖)
    5. `TestSelectScope_FailClosed`:`SelectScope([]string{"read"}, false, "edit")` 返回 `("", false)`(D-12 无 fallback)
    6. `TestSelectScope_EmptyScopes`:`SelectScope([]string{}, false, "list")` 返回 `("", false)`
    7. `TestSelectScope_DefaultReadFallback`:`SelectScope([]string{"read"}, false, "unknown_action")` 返回 `("read", true)`(默认 read 兜底)
    8. `TestSelectScope_MultiScopeNotFirst`:`SelectScope([]string{"admin", "write", "read"}, false, "list")` 返回 `("read", true)`(list → read,选 read 而非 admin)
    9. `TestSelectScope_MultiScopeWrite`:`SelectScope([]string{"admin", "write", "read"}, false, "edit")` 返回 `("write", true)`(edit → write,选 write)

    修改 `internal/middleware/apikey.go`,**仅限以下三个函数**,不修改 Plan 01 拥有的 MultiAuth / setUserContextForAPIKey / RequireAPIKeyResourcePermission:

    1. **`getRequiredScope` 扩展 `list→read`**(D-11,行 229-243):
       - 在既有 `scopeMap` 中新增一行 `"list": "read"`(放第一行,语义清晰)
       - 既有 `view/create/edit/delete → read/write` 不变
       - 默认 read 兜底不变
       - 注释更新:在原 "view/create/edit/delete → read/write" 后追加 "list → read(Phase 61 QUAL-03 D-11)"

    2. **`getScopeFromContext` 签名扩展 + 改为薄壳包装 SelectScope**(D-12/D-13,行 289-308):
       - 原:`func getScopeFromContext(c *gin.Context) string`
       - 新:`func getScopeFromContext(c *gin.Context, action string) (string, bool)`
       - 函数体变为**薄壳**:
         ```go
         func getScopeFromContext(c *gin.Context, action string) (string, bool) {
             inheritRaw, exists := c.Get("inherit_perms")
             inheritPerms, _ := inheritRaw.(bool)
             scopesRaw, _ := c.Get("scopes")
             scopes, _ := scopesRaw.([]string)
             return SelectScope(scopes, inheritPerms && exists, action)
         }
         ```
       - **关键变化**:不再返回单一 string,**不再**依赖任何新 context key;从既有 `inherit_perms` 与 `scopes` 读(Phase 57 D-04 已存在的 7 个 context keys 之一)

    3. **`RateLimitByScope` 签名扩展 + 调用更新**(D-11,行 248-285):
       - 原:`func RateLimitByScope(rateLimiter *services.RateLimiter) gin.HandlerFunc`
       - 新:`func RateLimitByScope(rateLimiter *services.RateLimiter, action string) gin.HandlerFunc`
       - 注册期立即计算 `requiredScope := getRequiredScope(action)`,作为闭包捕获(避免每请求重复算)
       - 函数体内 `scope, allowed := getScopeFromContext(c, action)` 替代原 `scope := getScopeFromContext(c)`
       - 若 `!allowed`:调用 `response.Error(c, response.ErrForbidden, "权限作用域不足")` + `c.Abort()` + return(**不**进入限流检查)
       - 若 `allowed`:继续既有 `rateLimiter.Check(identifier, scope)` + 限流响应头 + 429 路径
       - 既有 strconv.Itoa 限流响应头逻辑保留(Phase 60 QUAL-01 不回归)

    4. **`internal/middleware/apikey_test.go` 扩展 `TestGetRequiredScope`**(D-20):
       - 在既有测试用例后追加:`t.Run("list操作映射到read", ...)`:断言 `getRequiredScope("list") == "read"`(D-11)
       - 其他既有用例(view/create/edit/delete/unknown/空)不变
       - **不**修改其他测试函数

    5. **`internal/middleware/apikey_integration_test.go` 既有测试签名更新**:
       - **`TestRateLimitHeadersInResponse`**(行 238-281):`router.Use(RateLimitByScope(rl))` 改为 `router.Use(RateLimitByScope(rl, "list"))`(D-11 新签名),既有断言保留
       - **`TestConstructorsCallable_D02`**(行 301-313):`_ = RateLimitByScope(rl)` 改为 `_ = RateLimitByScope(rl, "list")`
       - **不**删除其他测试函数,不修改 Phase 57/59/60 测试基线

    6. **新增 `internal/middleware/apikey_rate_limit_test.go`**(D-20 中间件单元测试,**直接断言通过 SelectScope 中转的返回值**,但**不**依赖任何 `_internal_scope` 之类的新 context key):

       a. **`TestRateLimitByScope_ActionListSelectsRead`**:
          - 构造 `c.Set("scopes", []string{"read"})` + `c.Set("auth_type", "api_key")`
          - 调用 `RateLimitByScope(realRateLimiter, "list")`
          - 路由 handler 内部直接调用 `SelectScope(scopes, inheritPerms, action)` 验证返回 `("read", true)` — handler 只需 `c.Get("scopes")` 后调用 `SelectScope(...)`,**不**写 `_internal_scope`

       b. **`TestRateLimitByScope_ActionEditSelectsWrite`**:
          - 构造 `c.Set("scopes", []string{"write"})`
          - 调用 `RateLimitByScope(realRateLimiter, "edit")`
          - 断言:`("write", true)`

       c. **`TestRateLimitByScope_ActionMissingScope_403`**:
          - 构造 `c.Set("scopes", []string{"read"})`
          - 调用 `RateLimitByScope(realRateLimiter, "edit")`(edit 需要 write,read 不含)
          - 断言:响应 403 + `code != 0`(D-12 fail-closed)

       d. **`TestRateLimitByScope_AdminOverriddes`**:
          - 构造 `c.Set("scopes", []string{"read", "admin"})`
          - 调用 `RateLimitByScope(realRateLimiter, "edit")`
          - 断言:返回 `("admin", true)`(admin 最高限额覆盖 read)

       e. **`TestRateLimitByScope_InheritPermsDefaultLimit`**:
          - 构造 `c.Set("scopes", []string{"system:user:list"})`(细粒度 permission code,D-13)
          - `c.Set("inherit_perms", true)`
          - 调用 `RateLimitByScope(realRateLimiter, "list")`
          - 断言:返回 `("default", true)`(D-13 inherit_perms 短路)

       f. **`TestRateLimitByScope_MultiScope_SelectsMatchingNotFirst`**:
          - 构造 `c.Set("scopes", []string{"admin", "write", "read"})`(多个 scope,既有代码错误取 scopes[0]=admin)
          - 调用 `RateLimitByScope(realRateLimiter, "list")`(list → read)
          - 断言:返回 `("read", true)`(D-12 action-aware 选 read,而非任意 scopes[0]=admin)

       g. **`TestRateLimitByScope_NotAPIKeyAuth_Skip`**:
          - 构造 `c.Set("auth_type", "jwt")`(非 API Key)
          - 调用 `RateLimitByScope(realRateLimiter, "list")`
          - 断言:handler 继续 `c.Next()`(既有非 API Key 跳过限流逻辑保留)
  </action>
  <verify>
    <automated>cd D:\code\ClaudeCode\guoguo && go build ./... && go test -v -run "TestGetRequiredScope|TestSelectScope|TestRateLimitByScope|TestRateLimitHeadersInResponse|TestConstructorsCallable" ./internal/middleware/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` 退出码 0
    - `go test -v -run "TestGetRequiredScope" ./internal/middleware/...` PASS,新增 "list操作映射到read" 用例
    - `go test -v -run "TestSelectScope" ./internal/middleware/...` 全部 PASS(9 个新用例,纯函数)
    - `go test -v -run "TestRateLimitByScope" ./internal/middleware/...` 全部 PASS(7 个新用例,中间件集成)
    - `grep -n "func getScopeFromContext" internal/middleware/apikey.go` 签名包含 `, action string` 参数 + 返回 `(string, bool)`
    - `grep -n "func RateLimitByScope" internal/middleware/apikey.go` 签名包含 `, action string` 参数
    - `grep -n "func getRequiredScope" internal/middleware/apikey.go` 函数体含 `"list": "read"` entry
    - `grep -n "RateLimitByScope(rl," internal/middleware/apikey_integration_test.go` 至少 2 处(既有 2 个测试签名更新)
    - `grep -n "_internal_scope" internal/middleware/` 返回零匹配(已删除所有 _internal_scope 引用)
    - `grep -n "extractScope" internal/middleware/` 返回零匹配(已删除 extractScope helper)
    - Phase 60 既有 `TestRateLimitHeaderEncoding` 仍 PASS(strconv.Itoa 不回归)
    - Phase 57/59 既有 `TestMultiAuthIntegration` / `TestMultiAuthUsageLogTiming` 仍 PASS(本 Plan 02 不修改 MultiAuth)
  </acceptance_criteria>
  <done>D-11/D-12/D-13/D-14 全部落地,RateLimitByScope 多 scope 选择逻辑改为 action-aware 严格语义,scope 选择逻辑提取为纯函数 SelectScope(直接可单测),既有测试签名更新,9 个 SelectScope 纯函数单测 + 7 个新增中间件单元测试覆盖。</done>
</task>

<task type="auto">
  <name>Task 2: cache_config_service.go 新增 12 个 rate_limit.* 配置键(D-15/D-16/D-17)+ 单元测试</name>
<files>internal/services/cache_config_service.go, internal/services/cache_config_service_test.go</files>
  <read_first>
    - internal/services/cache_config_service.go(CacheConfigService 模式 + LoadConfigs 行 107-185 + setDefaultsIfNeeded 行 188-258 + GetConfigInfo 行 305-641 + ConfigInfo 结构 行 644-652)
    - .planning/phases/61-resource-permission-matrix-and-rate-limit-tuning/61-CONTEXT.md(D-16 12 键清单 + D-17 默认值与 rate_limiter.go:48-50/64 硬编码一致)
    - internal/services/rate_limiter.go(行 47-51 read/write/admin 硬编码 + 行 64 default fallback)
  </read_first>
  <action>
    修改 `internal/services/cache_config_service.go`:

    1. **新增 12 个配置键常量**(行 16-81 末尾追加,与既有 `CacheConfig*` 常量并列):
       ```go
       // 限流阈值配置
       const (
           RateLimitReadPerMinute    = "rate_limit.read.per_minute"      // 默认 30,  Min 1, Max 10000
           RateLimitReadPerHour      = "rate_limit.read.per_hour"        // 默认 500, Min 1, Max 100000
           RateLimitReadPerDay       = "rate_limit.read.per_day"         // 默认 5000,Min 1, Max 1000000
           RateLimitWritePerMinute   = "rate_limit.write.per_minute"     // 默认 100, Min 1, Max 10000
           RateLimitWritePerHour     = "rate_limit.write.per_hour"       // 默认 1500,Min 1, Max 100000
           RateLimitWritePerDay      = "rate_limit.write.per_day"        // 默认 15000,Min 1, Max 1000000
           RateLimitAdminPerMinute   = "rate_limit.admin.per_minute"     // 默认 200, Min 1, Max 10000
           RateLimitAdminPerHour     = "rate_limit.admin.per_hour"       // 默认 5000, Min 1, Max 100000
           RateLimitAdminPerDay      = "rate_limit.admin.per_day"        // 默认 50000, Min 1, Max 1000000
           RateLimitDefaultPerMinute = "rate_limit.default.per_minute"   // 默认 120, Min 1, Max 10000
           RateLimitDefaultPerHour   = "rate_limit.default.per_hour"     // 默认 2000, Min 1, Max 100000
           RateLimitDefaultPerDay    = "rate_limit.default.per_day"      // 默认 20000, Min 1, Max 1000000
       )
       ```

    2. **修改 `LoadConfigs`**(行 107-185):
       - 第 112 行 SQL `Where("config_key LIKE ?", "cache.%")` 拆分为**两次查询**(沿用既有单占位符模式,与项目其他 sys_config 查询保持一致,避免双占位符方言差异):
         ```go
         var cacheConfigs []models.Config
         if err := s.db.Where("config_key LIKE ?", "cache.%").Find(&cacheConfigs).Error; err != nil {
             return fmt.Errorf("查询缓存配置失败: %w", err)
         }
         var rateLimitConfigs []models.Config
         if err := s.db.Where("config_key LIKE ?", "rate_limit.%").Find(&rateLimitConfigs).Error; err != nil {
             return fmt.Errorf("查询限流配置失败: %w", err)
         }
         configs := append(cacheConfigs, rateLimitConfigs...)
         ```
       - **理由**:既有 `cache_config_service.go:112` 使用单占位符 `LIKE 'cache.%'` 模式,沿用同样模式单独查询 `rate_limit.%`,语义清晰,失败错误信息分别报告。
       - 第 116 行 `s.configs = make(map[string]time.Duration)` 之后追加 `s.rateLimits = make(map[string]int)`
       - 第 120 行 `configInfo := s.GetConfigInfo()` 不变,GetConfigInfo 内部同时返回 cache.* 与 rate_limit.*
       - 第 123-178 行解析逻辑:rate_limit.* 值是「次数」整数,无需 `* time.Minute` 转换;新增分支:
         ```go
         for _, config := range configs {
             if strings.HasPrefix(config.ConfigKey, "rate_limit.") {
                 count, err := strconv.Atoi(config.ConfigValue)
                 if err != nil {
                     logger.Warnf("[CACHE_CONFIG] 解析限流配置失败 [%s=%s]: %v，将使用默认值", config.ConfigKey, config.ConfigValue, err)
                     if info, ok := configInfo[config.ConfigKey]; ok {
                         s.rateLimits[config.ConfigKey] = info.Default
                         s.db.Model(&models.Config{}).Where("config_key = ?", config.ConfigKey).
                             Update("config_value", strconv.Itoa(info.Default))
                         logger.Infof("[CACHE_CONFIG] 修复限流配置: %s = %d 次", config.ConfigKey, info.Default)
                     }
                     continue
                 }
                 if info, ok := configInfo[config.ConfigKey]; ok {
                     if count < info.Min || count > info.Max {
                         logger.Warnf("[CACHE_CONFIG] 限流配置值超出范围 [%s=%d]，范围：%d-%d，将重置为默认值",
                             config.ConfigKey, count, info.Min, info.Max)
                         s.rateLimits[config.ConfigKey] = info.Default
                         s.db.Model(&models.Config{}).Where("config_key = ?", config.ConfigKey).
                             Update("config_value", strconv.Itoa(info.Default))
                         continue
                     }
                 }
                 s.rateLimits[config.ConfigKey] = count
                 logger.Infof("[CACHE_CONFIG] 加载限流配置: %s = %d 次", config.ConfigKey, count)
             } else {
                 // 既有 cache.* duration 转换逻辑(行 123-178 保留)
             }
         }
         ```
       - **存储选择**:采纳 Plan A(独立字段,避免破坏既有 cache.* Duration 接口):
         ```go
         type CacheConfigService struct {
             db         *gorm.DB
             configs    map[string]time.Duration  // 既有 cache.*
             rateLimits map[string]int            // 新增 rate_limit.* (count 语义)
             mu         sync.RWMutex
         }
         ```
       - `GetDuration` 既有逻辑不变,新增 `GetRateLimit(key string, defaultValue int) int` 方法:从 `rateLimits` 读,缺省返回 defaultValue。
       - `setDefaultsIfNeeded` 第 189-227 行 `defaults` map 末尾追加 12 个 rate_limit.* entry(默认值与 D-17 一致):
         ```go
         RateLimitReadPerMinute:    30,
         RateLimitReadPerHour:      500,
         RateLimitReadPerDay:       5000,
         RateLimitWritePerMinute:   100,
         RateLimitWritePerHour:     1500,
         RateLimitWritePerDay:      15000,
         RateLimitAdminPerMinute:   200,
         RateLimitAdminPerHour:     5000,
         RateLimitAdminPerDay:      50000,
         RateLimitDefaultPerMinute: 120,
         RateLimitDefaultPerHour:   2000,
         RateLimitDefaultPerDay:    20000,
         ```
       - 第 240-256 行 DB 写入逻辑保留(create models.Config),无需新增类型(全部 IsSystem=1, ConfigType="Y")
       - 第 183 行末尾 `logger.Infof("[CACHE_CONFIG] 配置加载完成,共 %d 项", len(s.configs))` 改为 `logger.Infof("[CACHE_CONFIG] 配置加载完成,共 %d 项 cache + %d 项 rate_limit", len(s.configs), len(s.rateLimits))`

    3. **修改 `GetConfigInfo`**(行 305-641):在末尾追加 12 个 `RateLimit*` ConfigInfo entry:
       - `Key`, `Name` (e.g. "读作用域每分钟限流"), `Description` (e.g. "API Key 读作用域每分钟请求数限制"), `Category: "限流配置"`
       - `Min` / `Max` / `Default` 与 D-16/D-17 一致
       - 注意:ConfigInfo.Default 语义此处表示 count(非 minute),RateLimiter 通过 `GetRateLimit(key, info.Default)` 读取

    4. **新增 `ReloadConfig` 既有逻辑保留**(行 285-289)— reload rate_limit.* 与 cache.* 一同刷新(D-19 在途请求保留旧值)

    5. **新增 `GetRateLimit(key string, defaultValue int) int` 导出方法**:
       ```go
       func (s *CacheConfigService) GetRateLimit(key string, defaultValue int) int {
           s.mu.RLock()
           defer s.mu.RUnlock()
           if v, ok := s.rateLimits[key]; ok { return v }
           return defaultValue
       }
       ```

    6. **`RateLimitProvider` 接口**(D-18 抽象,Plan 02 Task 3 使用):
       ```go
       // RateLimitProvider 限流配置提供者接口(D-18 解耦 RateLimiter)
       type RateLimitProvider interface {
           GetRateLimit(key string, defaultValue int) int
       }
       ```
       `*CacheConfigService` 自动实现该接口(Go duck typing)。

    7. **package 注释**追加:"Phase 61 QUAL-03 新增 rate_limit.* 配置项,与 cache.* 共存于同一 service;遵循既有 LoadConfigs → setDefaultsIfNeeded → ReloadConfig 启动加载 + 手动刷新模式"。

    新建文件 `internal/services/cache_config_service_test.go`(D-20 单元测试):

    1. **`TestCacheConfigService_RateLimitDefaults`**:
       - 构造 sqlite in-memory DB(沿用 Phase 59 模式)
       - 调用 `NewCacheConfigService(db)`
       - 断言:`s.GetRateLimit(RateLimitReadPerMinute, 999) == 30`(默认值)
       - 断言:`s.GetRateLimit(RateLimitWritePerHour, 999) == 1500`
       - 断言:`s.GetRateLimit(RateLimitDefaultPerDay, 999) == 20000`
       - 断言 DB 中存在 12 条 `config_key LIKE 'rate_limit.%'` 记录(由 setDefaultsIfNeeded 自动 INSERT)

    2. **`TestCacheConfigService_ReloadRateLimit`**:
       - 构造 DB,加载 defaults
       - 直接 UPDATE sys_config 表 `RateLimitReadPerMinute = 5`
       - 调用 `s.ReloadConfig(ctx)`
       - 断言:`s.GetRateLimit(RateLimitReadPerMinute, 999) == 5`(D-19 reload 后生效)

    3. **`TestCacheConfigService_RangeValidation`**:
       - INSERT 一条 `RateLimitReadPerMinute = 99999`(超出 Max=10000)
       - 调用 `LoadConfigs`
       - 断言:`s.GetRateLimit(RateLimitReadPerMinute, 999) == 30`(默认值,DB 自动修复回 30)
       - 验证 Phase 59 既有 range 校验逻辑适用于 rate_limit.*

    4. **`TestCacheConfigService_CacheUnaffected`**:
       - 验证新增 rate_limit.* 加载**不**破坏既有 cache.* 加载
       - `s.GetDuration(CacheConfigDeptTree) == 30 * time.Minute`(既有 default 仍正确)

    5. **`TestRateLimitProviderInterface`**:
       - `var _ RateLimitProvider = (*CacheConfigService)(nil)` 编译通过即证明接口实现
  </action>
  <verify>
    <automated>cd D:\code\ClaudeCode\guoguo && go build ./internal/services/... && go test -v -run "TestCacheConfigService_RateLimit|TestCacheConfigService_Reload|TestCacheConfigService_RangeValidation|TestCacheConfigService_CacheUnaffected|TestRateLimitProviderInterface" ./internal/services/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/services/...` 退出码 0
    - `go test -v -run "TestCacheConfigService_RateLimit|TestReload|TestRangeValidation" ./internal/services/...` 全部 PASS(至少 5 个新用例)
    - `grep -c "rate_limit\." internal/services/cache_config_service.go` >= 25(12 常量 + 12 默认值 + 2 SQL LIKE pattern + GetConfigInfo entry)
    - `grep -n "RateLimitProvider" internal/services/cache_config_service.go` 至少 1 处(接口定义)
    - `grep -n "GetRateLimit" internal/services/cache_config_service.go` 至少 3 处(方法定义 + 既有 cache_config_service_test.go 测试调用 + RateLimiter 调用)
    - Phase 59 既有 cache.* 测试不回归(`go test -v -run TestCacheConfig ./internal/services/...` PASS)
  </acceptance_criteria>
  <done>D-15/D-16/D-17 全部落地,12 个 rate_limit.* 配置键注册到 CacheConfigService,默认值与既有硬编码一致,Range 校验沿用既有模式。</done>
</task>

<task type="auto">
  <name>Task 3: rate_limiter.go 配置化改造(D-18/D-19)+ modify existing rate_limiter_test.go(449 行)+ router.go 调用形态</name>
<files>internal/services/rate_limiter.go, internal/services/rate_limiter_test.go, internal/api/router.go</files>
  <read_first>
    - internal/services/rate_limiter.go(原 RateLimiter 结构 行 31-34,NewRateLimiter 行 45-53 硬编码,Check 行 59-131,default fallback 行 64)
    - internal/services/cache_config_service.go(Task 2 新增的 RateLimitProvider 接口 + GetRateLimit 方法 + 12 个常量)
    - internal/api/router.go(行 258 `apikeys.Use(internalmw.RateLimitByScope(services.NewRateLimiter()))`)
    - .planning/phases/61-resource-permission-matrix-and-rate-limit-tuning/61-CONTEXT.md(D-18 RateLimiter 接收 RateLimitProvider,D-19 reload race 语义)
    - internal/services/rate_limiter_test.go(449 行既有测试,需要迁移到新签名 + 移除 limiter.limits 字段断言)
  </read_first>
  <action>
    修改 `internal/services/rate_limiter.go`:

    1. **`RateLimiter` 结构扩展**(行 31-34):
       - 原:`type RateLimiter struct { windows sync.Map; limits map[string]RateLimit }`
       - 新:`type RateLimiter struct { windows sync.Map; config RateLimitProvider }`
       - 移除硬编码 `limits map[string]RateLimit` 字段(D-18)
       - 保留 `windows sync.Map`(滑动窗口状态不变)

    2. **`NewRateLimiter` 签名扩展**(行 45-53):
       - 原:`func NewRateLimiter() *RateLimiter`
       - 新:`func NewRateLimiter(config RateLimitProvider) *RateLimiter`
       - 函数体:`return &RateLimiter{ config: config }`
       - **不**在构造函数中初始化 limits map(D-18:运行时读 config)
       - **Nil guard**:若 `config == nil`,构造一个 fallback `staticRateLimitProvider` 提供默认值(120/2000/20000),保证旧调用路径不 panic,但日志记录 "[RATE_LIMITER] warning: NewRateLimiter(nil), fallback to static defaults"

    3. **`Check` 改造**(行 59-131):
       - 内部将 `rl.limits[scope]` 替换为 `rl.getLimit(scope)`:
         ```go
         func (rl *RateLimiter) getLimit(scope string) RateLimit {
             if rl.config == nil {
                 return RateLimit{PerMinute: 120, PerHour: 2000, PerDay: 20000}  // default fallback
             }
             perMin := rl.config.GetRateLimit("rate_limit."+scope+".per_minute", 120)
             perHour := rl.config.GetRateLimit("rate_limit."+scope+".per_hour", 2000)
             perDay := rl.config.GetRateLimit("rate_limit."+scope+".per_day", 20000)
             return RateLimit{PerMinute: perMin, PerHour: perHour, PerDay: perDay}
         }
         ```
       - 行 64 既有 `default fallback` 删除(由 `getLimit("default")` 自然处理)
       - 行 82-89 / 93-99 / 104-112 三个窗口检查逻辑不变

    4. **`calculateReset` / `calculateRemaining` / `cleanOlderThan` / `getOrCreateWindow`** 全部保留(D-19 reload race 语义:这些函数不引用 `rl.limits`,reload 不影响在途请求的滑动窗口时间戳)

    5. **新增 `staticRateLimitProvider` 私有 fallback**(应对 config==nil):
       ```go
       type staticRateLimitProvider struct {
           limits map[string]RateLimit
       }
       func (p *staticRateLimitProvider) GetRateLimit(key string, defaultValue int) int {
           // 解析 "rate_limit.<scope>.<window>" 取值
           parts := strings.Split(key, ".")
           if len(parts) != 3 { return defaultValue }
           if l, ok := p.limits[parts[1]]; ok {
               switch parts[2] {
               case "per_minute": return l.PerMinute
               case "per_hour": return l.PerHour
               case "per_day": return l.PerDay
               }
           }
           return defaultValue
       }
       ```
       预填 `limits: { read: 30/500/5000, write: 100/1500/15000, admin: 200/5000/50000, default: 120/2000/20000 }`(与既有硬编码一致,D-17)

    6. **package 注释**追加:"Phase 61 QUAL-03: RateLimiter 改为配置驱动,从 RateLimitProvider 读取阈值;移除硬编码 limits map"

    **修改既有 `internal/services/rate_limiter_test.go`(449 行)**,而非新建文件:

    1. **`TestNewRateLimiter` (~L12)** — 必须修改,因为字段 `limiter.limits` 已被移除:
       - 移除所有 `assert.Contains(t, limiter.limits, "read")` / `limiter.limits["read"]` 断言
       - 替换为基于 `mockRateLimitProvider`(新增 helper,见下)的断言:
         ```go
         func TestNewRateLimiter(t *testing.T) {
             provider := newMockRateLimitProvider()
             limiter := NewRateLimiter(provider)
             assert.NotNil(t, limiter)
             _, loaded := limiter.windows.Load("never-set-key")
             assert.False(t, loaded, "未设置的 key 应返回 loaded=false")
             // 验证 limiter.config 已设置
             assert.NotNil(t, limiter.config)
         }
         ```

    2. **`TestRateLimiter_Check` (~L44)** — 修改 `NewRateLimiter()` 调用:
       - 第一行 `limiter := NewRateLimiter()` 改为 `limiter := NewRateLimiter(newMockRateLimitProvider())`
       - 其余测试逻辑保留(都通过 `limiter.Check(key, scope)` 间接验证限额;底层通过 `provider.GetRateLimit("rate_limit.read.per_minute", 120)` 读 30)

    3. **`TestRateLimiter_SlidingWindow` (~L142)** — 修改:
       - `limiter := NewRateLimiter()` 改为 `limiter := NewRateLimiter(newMockRateLimitProvider())`

    4. **`TestRateLimiter_Cleanup` (~L249)** — 修改:
       - `limiter := NewRateLimiter()` 改为 `limiter := NewRateLimiter(newMockRateLimitProvider())`

    5. **`TestRateLimiter_MultipleKeys` (~L309)** — 修改:
       - `limiter := NewRateLimiter()` 改为 `limiter := NewRateLimiter(newMockRateLimitProvider())`

    6. **`TestRateLimiter_Reset` (~L359)** — 修改:
       - `limiter := NewRateLimiter()` 改为 `limiter := NewRateLimiter(newMockRateLimitProvider())`

    7. **`TestRateLimiter_EdgeCases` (~L408)** — 修改:
       - `limiter := NewRateLimiter()` 改为 `limiter := NewRateLimiter(newMockRateLimitProvider())`

    8. **新增 `mockRateLimitProvider` 测试 helper**(文件末尾追加):
       ```go
       type mockRateLimitProvider struct {
           limits map[string]RateLimit
       }

       func newMockRateLimitProvider() *mockRateLimitProvider {
           return &mockRateLimitProvider{
               limits: map[string]RateLimit{
                   "read":    {PerMinute: 30, PerHour: 500, PerDay: 5000},
                   "write":   {PerMinute: 100, PerHour: 1500, PerDay: 15000},
                   "admin":   {PerMinute: 200, PerHour: 5000, PerDay: 50000},
                   "default": {PerMinute: 120, PerHour: 2000, PerDay: 20000},
               },
           }
       }

       func (m *mockRateLimitProvider) GetRateLimit(key string, defaultValue int) int {
           parts := strings.Split(key, ".")
           if len(parts) != 3 { return defaultValue }
           if l, ok := m.limits[parts[1]]; ok {
               switch parts[2] {
               case "per_minute": return l.PerMinute
               case "per_hour": return l.PerHour
               case "per_day": return l.PerDay
               }
           }
           return defaultValue
       }
       ```

    9. **新增 import**:`"strings"`(已存在 sync/time/testing/stretchr;新增 strings)

    10. **新增 `TestRateLimiter_NilProviderFallback`** 测试函数(验证 D-18 nil 兜底):
        ```go
        func TestRateLimiter_NilProviderFallback(t *testing.T) {
            limiter := NewRateLimiter(nil)
            assert.NotNil(t, limiter)
            // 不 panic,使用 staticRateLimitProvider fallback
            for i := 0; i < 30; i++ {
                allowed, _ := limiter.Check("key", "read")
                assert.True(t, allowed, "Request %d should be allowed (static fallback)", i+1)
            }
            allowed, _ := limiter.Check("key", "read")
            assert.False(t, allowed, "31st request should be denied (static fallback 30/min)")
        }
        ```

    11. **新增 `TestRateLimiter_ConfigDrivenRead`** 测试函数(验证 D-18 配置驱动):
        ```go
        func TestRateLimiter_ConfigDrivenRead(t *testing.T) {
            provider := &mockRateLimitProvider{
                limits: map[string]RateLimit{
                    "read": {PerMinute: 5, PerHour: 100, PerDay: 1000},
                },
            }
            limiter := NewRateLimiter(provider)
            for i := 0; i < 5; i++ {
                allowed, result := limiter.Check("key", "read")
                assert.True(t, allowed)
                assert.Equal(t, 5, result.Limit)
            }
            allowed, _ := limiter.Check("key", "read")
            assert.False(t, allowed)
        }
        ```

    修改 `internal/api/router.go`(行 258,**仅修改 RateLimitByScope 调用行**,MultiAuth 行由 Plan 01 修改):
    - 原:`apikeys.Use(internalmw.RateLimitByScope(services.NewRateLimiter()))`
    - 新:`apikeys.Use(internalmw.RateLimitByScope(services.NewRateLimiter(core.CacheConfigService), "list"))`(D-11 action 参数 + D-18 传 core.CacheConfigService 字段,**非** getter;沿用既有 `core.DataCacheService` / `core.UserService` 等字段访问模式,如 `internal/core/core.go:341` `c.CacheConfigService = services.NewCacheConfigService(c.GetDB())`)
    - **关键**:必须是 `core.CacheConfigService`(**字段**访问),不是 `core.GetCacheConfigService()`(**方法**调用)— Core struct 没有 `GetCacheConfigService()` 方法,只有 `GetDB()` / `GetAuthFactory()` 等
    - `core` 变量名沿用 router.go 既有用法
  </action>
  <verify>
    <automated>cd D:\code\ClaudeCode\guoguo && go build ./... && go test -v -run "TestRateLimiter_" ./internal/services/... && go test -v -run "TestRateLimitByScope|TestRateLimitHeadersInResponse" ./internal/middleware/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` 退出码 0
    - `go test -v -run "TestRateLimiter_" ./internal/services/...` 全部 PASS(7 个既有用例 + 2 个新增)
    - `grep -n "func NewRateLimiter" internal/services/rate_limiter.go` 签名包含 `config RateLimitProvider` 参数
    - `grep -n "func (rl \*RateLimiter) Check" internal/services/rate_limiter.go` 函数体不含 `rl.limits`(已替换为 `getLimit`)
    - `grep -n "RateLimitProvider" internal/services/rate_limiter.go` 至少 2 处(结构体字段 + NewRateLimiter 参数)
    - `grep -n "NewRateLimiter()" internal/services/rate_limiter_test.go` 返回零匹配(全部已迁移到 `NewRateLimiter(provider)`)
    - `grep -n "limiter.limits" internal/services/rate_limiter_test.go` 返回零匹配(字段已移除,所有引用已清理)
    - `grep -n "RateLimitByScope(services.NewRateLimiter" internal/api/router.go` 含 `core.CacheConfigService`(**字段**,非 getter)与 `, "list"` action 参数
    - `grep -n "GetCacheConfigService" internal/api/router.go` 返回零匹配(已用字段访问替代 getter)
    - Phase 60 QUAL-01 既有 `TestRateLimitHeaderEncoding` 仍 PASS(strconv.Itoa 不回归)
    - Phase 60 既有 `TestRateLimitHeadersInResponse` 仍 PASS(签名更新 + 真实 CacheConfigService 通过 Core 字段访问)
    - 全量 `go test ./...` 不引入新失败
  </acceptance_criteria>
  <done>D-18/D-19 全部落地,RateLimiter 改为配置驱动,reload race 语义明确,router.go 调用形态变更(用 core.CacheConfigService 字段访问),既有 rate_limiter_test.go 449 行测试全部迁移到新签名,Plan 02 三任务完整覆盖 QUAL-03 success criteria。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | 客户端携带 `X-API-Key` 头进入 MultiAuth → RateLimitByScope |
| middleware→services | RateLimitByScope 调用 rateLimiter.Check(scope),Check 通过 RateLimitProvider 读 sys_config 表 |
| middleware→pkg/permission | RateLimitByScope 调用 getRequiredScope(action) 静态查表(无外部 IO) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|----------------|
| T-61-08 | Spoofing | getScopeFromContext 多 scope 选择 | mitigate | D-12 action-aware 严格匹配 + D-13 inherit_perms 短路保留 + admin 最高限额覆盖;SelectScope 纯函数 5 路径(短路/精确/admin/fail-closed/默认 read)逐路径单测 |
| T-61-09 | Tampering | rate_limit.* sys_config 运维配置 | accept | 配置表 IsSystem=1 防普通用户篡改;默认值与硬编码一致(D-17);Range 校验(Min/Max)防恶意值(D-16) |
| T-61-10 | Repudiation | RateLimitByScope 限流响应头 | mitigate | Phase 60 QUAL-01 已用 strconv.Itoa(不依赖 string(rune(int)));D-12 拒绝路径走 response.Error 而非裸 c.Abort,产生可审计日志 |
| T-61-11 | Information Disclosure | RateLimitProvider.GetRateLimit key 解析 | accept | 仅暴露 count 阈值,不含敏感信息;key pattern "rate_limit.<scope>.<window>" 不含 PII |
| T-61-12 | Denial of Service | admin scope 限额被运维调高导致 DoS | mitigate | D-16 Range 校验 Max=10000/分钟,运维误调不能无上限;reload 后立即生效(D-19),但需要管理员主动触发 reload |
| T-61-13 | Denial of Service | rate_limit.* 配置被删除导致降级到 default 限额 | accept | D-18:scope miss 时 `GetRateLimit(key, defaultValue=120/2000/20000)` 兜底,不会因配置缺失导致"无限制";admin 监控配置缺失告警(超出本 phase scope) |
| T-61-14 | Race Condition | ReloadConfig 在途请求的限额不一致 | mitigate | D-19:reload 后新阈值仅对 reload 之后的新请求生效;RateLimiter 持有 RateLimitProvider 引用,reload 不重建 RateLimiter 实例;新旧 provider 在不同 RateLimiter 实例共存(`go test -v -run TestRateLimiter_ReloadDoesNotAffectInflightWindows` 验证) |
| T-61-15 | Spoofing | getRequiredScope action 未识别 | mitigate | 未知 action → 默认 read(D-11 兜底);若 admin 创建非法 action 的 API Key,Plan 01 D-03 fail-closed 在 RequireAPIKeyResourcePermission 层再拒一次 |
| T-61-16 | Spoofing | SelectScope 纯函数被恶意调用 | accept | SelectScope 是纯函数,无外部 IO;恶意 caller 仍受 `getScopeFromContext` 调用路径保护(必须先有 MultiAuth 上下文) |
| T-61-SC | Tampering | 修改既有 rate_limiter_test.go 449 行 | mitigate | Task 3 显式枚举 7 个既有测试函数逐一迁移(`NewRateLimiter()` → `NewRateLimiter(newMockRateLimitProvider())`);移除 `limiter.limits[...]` 断言(字段已删);新增 mockRateLimitProvider helper;acceptance_criteria 通过 grep 验证 `NewRateLimiter()` / `limiter.limits` 引用零匹配 |

**Package Legitimacy:** 本 Plan 02 不安装任何新包,全部复用既有 import(strings.Split 在 rate_limiter.go 与 rate_limiter_test.go 既有 import 区追加)。
</threat_model>

<verification>
- [ ] Phase 60 QUAL-01 回归(限流响应头 strconv.Itoa):`go test -v -run TestRateLimitHeaderEncoding ./internal/middleware/...` PASS
- [ ] Phase 60 QUAL-01 集成测:`go test -v -run TestRateLimitHeadersInResponse ./internal/middleware/...` PASS(签名更新后)
- [ ] Phase 57 AUTH-01 回归:`go test -v -run TestMultiAuthIntegration ./internal/middleware/...` PASS(Plan 02 不修改 MultiAuth)
- [ ] Phase 59 OBSERV-01 回归:`go test -v -run TestMultiAuthUsageLogTiming ./internal/middleware/...` PASS
- [ ] 本 Plan 02 新增:TestGetRequiredScope list→read 用例 + 9 个 SelectScope 纯函数单测 + 7 个 RateLimitByScope 中间件单测 + 7 个 RateLimiter 配置驱动单测(既有 449 行迁移) + 5 个 CacheConfigService rate_limit 单测 PASS
- [ ] 既有 cache.* 测试不回归:`go test -v -run TestCacheConfig ./internal/services/...` PASS
- [ ] 全量 `go build ./...` 退出码 0,全量 `go test ./...` 不引入新失败
- [ ] 12 个 rate_limit.* config 键全部在 DB sys_config 表存在(测试 INSERT + SELECT 验证)
- [ ] Wave 2 排序验证:Plan 01 SUMMARY 已生成,Plan 02 开始执行(消除 parallel write race)
</verification>

<success_criteria>
1. `RateLimitByScope(rateLimiter, action string)` 接收 action 参数,注册期算 requiredScope 闭包捕获(`grep` 函数签名 + `requiredScope := getRequiredScope(action)`)。
2. `getScopeFromContext` 改为薄壳包装 `SelectScope`,无 fallback 拒绝(`grep` 函数体含 `SelectScope(...)` 调用 + 3 分支语义在 SelectScope 内)。
3. `SelectScope(scopes, inheritPerms, action)` 纯函数导出(`grep -n "func SelectScope" internal/middleware/select_scope.go`)。
4. `getRequiredScope` 扩展 `list → read`(`grep` 函数体含 `"list": "read"`)。
5. 12 个 rate_limit.* config 键(rate_limit.{read|write|admin|default}.{per_minute|per_hour|per_day})全部注册到 CacheConfigService(`grep -c "rate_limit\." internal/services/cache_config_service.go` >= 25)。
6. `RateLimiter.Check` 从 `RateLimitProvider.GetRateLimit()` 读限额,不再硬编码(`grep` 函数体含 `rl.config.GetRateLimit`)。
7. router.go 调用形态含 `RateLimitByScope(services.NewRateLimiter(core.CacheConfigService), "list")`(`grep` 含 `core.CacheConfigService` 字段访问 + `, "list"`)。
8. X-RateLimit-Limit/Remaining 仍是 strconv.Itoa 数字字面量(Phase 60 QUAL-01 回归,既有 TestRateLimitHeaderEncoding PASS)。
9. 既有 Phase 57/59/60 测试不回归(`go test ./...` 全绿)。
10. 既有 rate_limiter_test.go 449 行全部迁移到新签名(`grep -n "NewRateLimiter()" internal/services/rate_limiter_test.go` 零匹配)。
</success_criteria>

<output>
Create `.planning/phases/61-resource-permission-matrix-and-rate-limit-tuning/61-02-SUMMARY.md`
</output>