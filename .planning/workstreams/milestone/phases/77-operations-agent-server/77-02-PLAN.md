---
phase: 77-operations-agent-server
plan: 02
type: execute
wave: 2
depends_on: ["77-01"]
files_modified:
  - internal/services/operations/excel_export_chain_77_02_test.go
autonomous: true
requirements: [BLOCK-01]

must_haves:
  truths:
    - "按 D-08 切分,本 plan 交付 excel_service 导出链测试:ExportData legacy 路径(user/asset/department 等不在 export config 的 8 类)+ queryData/writeDataRows/formatCellValue/writeInstructions/getExampleValue + appendWorkstationDeviceSheets(~115 stmts)"
    - "D-07 导出链结构断言落实:断言 sheet 名 / 表头行 / 数据行数 + 抽查关键单元格(工位设备 sheet 序列号列锚点),不做全量逐单元格快照比对(列顺序调整即碎)"
    - "D-06 全内存生成落实:常规输入 excelize.NewFile()/buildTestXLSX + 既有 xlsxFileHeader 包装,零 testdata/ 二进制进 git"
    - "appendWorkstationDeviceSheets 三 sheet(AD设备/资产设备/物理链路设备)在 sqlite 降级分支(physErr != nil)下仍全部生成,物理链路 sheet 0 数据行但表头在"
    - "新 quirk 一律按 D-01 发现即修 / D-02 plan 内就地修 / D-03 有据判定(同 Phase 75 五步法)"
    - "BLOCK-01 累积推进:本 plan 后 `go test -count=1 -cover ./internal/services/operations/` 数字继续抬升(目标 ≥68%),`go build ./...` + `go test ./...` 全绿"
    - "按 D-09 wave 编排:本 plan 位于 wave 2 与 77-05(不同包)并行,依赖同包 77-01 先行收尾(同包整包编译互踩规避)"
  artifacts:
    - path: "internal/services/operations/excel_export_chain_77_02_test.go"
      provides: "excel_service 导出链结构断言测试(测试函数前缀 TestExp77_)"
      min_lines: 250
  key_links:
    - from: "internal/services/operations/excel_export_chain_77_02_test.go"
      to: "internal/services/operations/excel_service.go ExportData"
      via: "NewExcelService(db, nil, nil, nil).ExportData(ctx, entityType, filters) + f.GetRows(config.SheetName)"
      pattern: "GetRows\\(config\\.SheetName\\)"
    - from: "workstation 追加链"
      to: "appendWorkstationDeviceSheets"
      via: "NewExcelService(db, nil, nil, nil).WithDeviceService(NewWorkstationDeviceService(db))"
      pattern: "WithDeviceService"
---

<objective>
交付 excel_service 导出链测试(ExportData legacy 8 类 + queryData/writeDataRows/formatCellValue/writeInstructions + appendWorkstationDeviceSheets 三 sheet 追加链,~115 stmts),按 D-07 做结构断言、按 D-06 全内存生成。

Purpose: BLOCK-01 的第二大贡献块;appendWorkstationDeviceSheets(60 unc)与 ExportData legacy 路径(~20 stmts)是 operations 包 61.1% → 70% 的关键增量。
Output: 新增 internal/services/operations/excel_export_chain_77_02_test.go(零生产代码改动,除非触发 D-01 quirk 修复)。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/77-operations-agent-server/77-CONTEXT.md
@.planning/workstreams/milestone/phases/77-operations-agent-server/77-RESEARCH.md
@.planning/workstreams/milestone/phases/77-operations-agent-server/77-PATTERNS.md

<interfaces>
<!-- 关键契约(PATTERNS 复核)。同包既有 helper 直接复用,勿重写。 -->

From internal/services/operations/excel_service.go(RESEARCH §77-02):
```go
svc := NewExcelService(db, nil, nil, nil)                     // legacy 路径(8 类: user/asset/department/serverRoom/roomDevice/dedicatedLine/infoPoint 等)
f, err := svc.ExportData(ctx, "asset", map[string]any{"name": "SN", "status": 0})
svc = NewExcelService(db, nil, nil, nil).WithDeviceService(NewWorkstationDeviceService(db))
f, _ := svc.ExportData(ctx, "workstation", map[string]any{})  // deviceService != nil → 追加三 sheet
```

复用 helper(同包 test 文件内,excel_import_export_test.go:26-59):
- buildTestXLSX(t, sheetName, rows) — 内存生成单 sheet xlsx 字节(rows[0] 为表头)
- xlsxFileHeader(t, data, name) — 包装为 *multipart.FileHeader
- newCRUDTestDB(t) / setupEnrichmentTestDB(t) — sqlite fixture 先例(crud_services_test.go:25-54 / excel_export_devices_test.go:107-142)

断言锚(D-07): config.SheetName 存在 + 表头行 = config.Columns[].Header + 数据行数 = 种子命中数 + 工位设备 sheet 序列号列单元格抽查。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExportData legacy 路径 + queryData/writeDataRows/formatCellValue/writeInstructions</name>
  <files>internal/services/operations/excel_export_chain_77_02_test.go</files>
  <read_first>
  - internal/services/operations/excel_service.go — ExportData legacy 分支(:1460-1509 一带)/ queryData / writeDataRows / formatCellValue / writeInstructions / getExampleValue
  - internal/services/operations/excel_import_export_test.go — buildTestXLSX/xlsxFileHeader(:26-59)+ GenerateTemplates 的 GetRows + config 锚定手法(:81-105)
  - internal/services/operations/excel_export_devices_test.go — writeDeviceSheet 结构断言(:220-264)与 &ExcelService{db} 直构(:152)
  - .planning/workstreams/milestone/phases/77-operations-agent-server/77-PATTERNS.md — §77-02(exact analog 摘录)
  </read_first>
  <action>
  创建 excel_export_chain_77_02_test.go(package operations)。fixture:newCRUDTestDB(t) 风格 sqlite + AutoMigrate ops_asset / sys_user / sys_dept(按 legacy 类型实际查询的表),种子行裸 INSERT。测试函数前缀 TestExp77_:

  - TestExp77_ExportData_LegacyTypes:对 legacy 8 类中至少 user / asset / department 三类调用 NewExcelService(db, nil, nil, nil).ExportData(ctx, type, filters):断言 f.GetRows(config.SheetName) 无错、表头行等于 config.Columns[].Header 序列、数据行数等于种子命中数(D-07 结构断言)。
  - TestExp77_ExportData_QueryFilters:asset 类型带 name / status filter(覆盖 queryData 过滤分支 + sys_dept 字段名特判分支),断言过滤后行数。
  - TestExp77_FormatCellValue:Options 映射反查 / createdAt 时间格式化 / nil / 默认 Sprintf 四分支(直调私有函数或经导出数据行单元格抽查)。
  - TestExp77_WriteInstructions:选模板带 Instructions 的类型(department 等)断言说明行存在于 sheet;空串 Instructions 跳过分支。
  - TestExp77_GetExampleValue:reference / options 各列型补尾部分支(当前 63.6%)。

  D-06:全部输入经 excelize.NewFile()/既有 helper 内存生成,本 plan 不新增任何 testdata/ 文件;D-07:断言仅 sheet 名/表头/行数/抽查单元格,禁全量快照比对。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestExp77_" ./internal/services/operations/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestExp77_" internal/services/operations/excel_export_chain_77_02_test.go` ≥ 5
  - 至少 3 个 legacy 类型(user/asset/department)的用例断言 config.SheetName 的 GetRows 结果
  - `git status --porcelain` 无新增 testdata/ 或 *.xlsx 二进制文件
  - `go test -count=1 -run "TestExp77_" ./internal/services/operations/` exit 0;`go build ./...` exit 0
  </acceptance_criteria>
  <done>ExportData legacy 路径与四个写行/格式化辅助函数全部有结构断言测试且全绿。</done>
</task>

<task type="auto">
  <name>Task 2: appendWorkstationDeviceSheets 三 sheet 追加链 + coverage checkpoint</name>
  <files>internal/services/operations/excel_export_chain_77_02_test.go</files>
  <read_first>
  - internal/services/operations/excel_service.go — appendWorkstationDeviceSheets 实现(60 unc)与 physErr 降级分支;queryWorkstationIDsForExport(:2207 注释,直接 Pluck 避开 JOIN 副作用)
  - internal/services/operations/excel_export_devices_test.go — setupEnrichmentTestDB 的 sys_ad_computer/ops_asset 种子形态与空列表写表头行为(:234 锁定)
  - internal/services/operations/workstation_device_77_01_test.go — 77-01 已建的 7 表 fixture(同包可复用 helper 或按需轻量重建)
  </read_first>
  <action>
  同文件追加:

  - TestExp77_ExportData_WorkstationAppendSheets:fixture 含 sys_workstation 种子行 + sys_ad_computer / ops_asset 命中行 → NewExcelService(db, nil, nil, nil).WithDeviceService(NewWorkstationDeviceService(db)).ExportData(ctx, "workstation", map[string]any{}) → 断言 "AD设备" / "资产设备" / "物理链路设备" 三 sheet 存在、各表头行非空、AD sheet 数据行数 = 种子 ad_computer 命中数,并抽查序列号列单元格(D-07 锚点:mergeBySerial 的合并主键)。
  - 物理链路 sheet 在 sqlite 下经 physErr != nil 降级分支(P-77-10):断言该 sheet 仍生成、0 数据行但表头在(writeDeviceSheet 空列表写表头行为已被 analog :234 锁定);若实测物理 sheet 缺席(A1 假设不成立),断言降级为「AD/资产两 sheet 必在」并在 SUMMARY 记录。
  - TestExp77_ExportData_NoDeviceService:deviceService 为 nil → 不追加 sheet 分支(ExportData workstation 主表仍在)。

  收尾 checkpoint:`go test -count=1 -cover ./internal/services/operations/`,数字落 SUMMARY(目标 ≥68%,为 77-03 收口 70% 留 ≤2pp)。quirk 纪律同 D-01/D-02/D-03。
  </action>
  <verify>
    <automated>go test -count=1 ./internal/services/operations/ && go test -count=1 -cover ./internal/services/operations/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestExp77_" internal/services/operations/excel_export_chain_77_02_test.go` ≥ 7
  - `grep -c "物理链路设备" internal/services/operations/excel_export_chain_77_02_test.go` ≥ 1 且 "AD设备" / "资产设备" 均出现
  - `go test -count=1 -cover ./internal/services/operations/` 输出 coverage ≥68%(或 SUMMARY 说明差距与 77-03 余量论证)
  - `go build ./...` exit 0;生产 .go 改动 = 0 文件(除非 D-01 quirk 修复有 SUMMARY deviation)
  </acceptance_criteria>
  <done>导出链全部函数(ExportData legacy + queryData/writeDataRows/formatCellValue/writeInstructions/appendWorkstationDeviceSheets)有测试;per-package coverage ≥68% 落 SUMMARY。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 ↔ 数据库 | sqlite :memory: only |
| 测试 ↔ 文件系统 | 全内存 xlsx 字节流(D-06),不落盘不进 git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-02-01 | Tampering | 二进制 fixture 混入 git | mitigate | D-06 全内存生成;验收 grep git status 无 testdata/*.xlsx |
| T-77-02-02 | Tampering | quirk 修复扩大化 | mitigate | D-03 有据判定 + 原子 commit + SUMMARY deviation |
| T-77-02-SC | Tampering | 依赖安装 | accept | 零新增依赖(excelize/testify 已在 go.mod) |
</threat_model>

<verification>
- `go test -count=1 ./internal/services/operations/` 全绿(含 77-01 测试无回归)
- `go test -count=1 -cover ./internal/services/operations/` ≥68% 落 SUMMARY
- `go build ./...` exit 0;无 testdata 二进制新增
</verification>

<success_criteria>
- excel_export_chain_77_02_test.go 存在,TestExp77_ 前缀测试 ≥7 个全绿
- D-07 结构断言 + 序列号列抽查落实;D-06 零二进制落实
- BLOCK-01 累积覆盖率 ≥68%
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/77-operations-agent-server/77-02-SUMMARY.md` when done
</output>
