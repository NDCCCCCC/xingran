---
phase: 77-operations-agent-server
plan: 03
type: execute
wave: 3
depends_on: ["77-02"]
files_modified:
  - internal/services/operations/excel_import_rest_77_03_test.go
  - internal/services/operations/excel_raw_rows_77_03_test.go
  - internal/services/operations/reference_resolver_77_03_test.go
  - internal/services/operations/workstation_floor_code_77_03_test.go
  - internal/services/operations/excel_raw_rows.go
autonomous: true
requirements: [BLOCK-01]

must_haves:
  truths:
    - "按 D-08 切分,本 plan 交付 excel_service 导入剩余(ImportData 剩余分支/resolveDependentReferencesBatch 二阶段/populateNewUserPasswords)+ reference_resolver 尾部 + workstation/floor/code_generator/excel_raw_rows 卫星测试(~230 stmts)并收口 BLOCK-01"
    - "Q-77-C(normalizeHeaderTrim 注释称转小写但实现仅 TrimSpace)按 D-01 发现即修、D-02 本 plan 内就地修:doc-only 修注释原子 commit,零行为变化(不补 ToLower——会改变 \"Name\" 表头匹配行为);D-03 据源=注释-行为不符本身"
    - "Q-77-D(code_generator 的 ErrRecordNotFound 分支死代码)按 D-03 无据不臆断:按现行为断言 + SUMMARY 记录待人工裁决,不删代码"
    - "D-06 全内存生成落实:常规输入 buildTestXLSX + xlsxFileHeader 包装;畸形输入手工字节构造([]byte(\"not a zip\") / 空流),零 testdata 二进制"
    - "新 quirk 一律按 D-01 发现即修 / D-02 plan 内就地修 / D-03 有据判定(Phase 75 五步法:原子 commit + 同 commit 回归用例)"
    - "BLOCK-01 收口达成:`go test -count=1 -cover ./internal/services/operations/` 输出 ≥70.0%,且 `go test ./...` 全绿、4 层 gate 不倒退"
    - "按 D-09 wave 编排:本 plan 位于 wave 3(operations 包最后一个 plan,依赖同包 77-02 先行),收口 BLOCK-01"
  artifacts:
    - path: "internal/services/operations/excel_import_rest_77_03_test.go"
      provides: "ImportData 剩余分支 + 依赖引用二阶段 + user 导入密码填充测试(前缀 TestImp77_)"
      min_lines: 300
    - path: "internal/services/operations/excel_raw_rows_77_03_test.go"
      provides: "ReadRawRowsByName + normalizeHeaderTrim 测试 + Q-77-C 修复回归"
      min_lines: 100
    - path: "internal/services/operations/reference_resolver_77_03_test.go"
      provides: "ResolveSingleWithCondition + ResolveBatchWithDependencies 尾部测试"
      min_lines: 80
    - path: "internal/services/operations/workstation_floor_code_77_03_test.go"
      provides: "workstation CRUD/List/BatchUpdatePositions + floor Create + code_generator 测试"
      min_lines: 250
    - path: "internal/services/operations/excel_raw_rows.go"
      provides: "Q-77-C 注释修正(doc-only,零行为变化)"
      contains: "TrimSpace"
  key_links:
    - from: "excel_import_rest_77_03_test.go"
      to: "buildTestXLSX / xlsxFileHeader(excel_import_export_test.go:26-59)"
      via: "内存 xlsx 字节流 → *multipart.FileHeader → ImportData"
      pattern: "xlsxFileHeader\\(t, buildTestXLSX"
    - from: "workstation 导入用例"
      to: "sqlite 唯一索引"
      via: "createImportIndexes 先例(P-77-7:ON CONFLICT 需手建唯一索引)"
      pattern: "CREATE UNIQUE INDEX"
    - from: "user 导入用例"
      to: "security.NewPasswordManager(nil)"
      via: "populateNewUserPasswords(HashPassword 纯 pbkdf2SM3,无需 SM4_KEY)"
      pattern: "NewPasswordManager"
---

<objective>
交付 excel_service 导入剩余分支 + reference_resolver 尾部 + workstation/floor/code_generator/excel_raw_rows 卫星测试(~230 stmts),并收口 BLOCK-01(operations ≥70%);就地处置 Q-77-C(doc-only)与 Q-77-D(按现行为断言)。

Purpose: BLOCK-01 的收口 plan——77-01/02 后余 ~2pp 缺口由本 plan 的 ~230 可达 stmts 补齐(余量 2.2×,RESEARCH 数学)。
Output: 4 个新测试文件 + excel_raw_rows.go 注释修正;收口时 operations per-package coverage ≥70.0%。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/77-operations-agent-server/77-CONTEXT.md
@.planning/workstreams/milestone/phases/77-operations-agent-server/77-RESEARCH.md
@.planning/workstreams/milestone/phases/77-operations-agent-server/77-PATTERNS.md

<interfaces>
<!-- 关键契约(PATTERNS 复核)。 -->

复用 helper(同包,excel_import_export_test.go):
- buildTestXLSX(t, sheetName string, rows [][]string) []byte — 内存生成,rows[0] 表头
- xlsxFileHeader(t, data []byte, name string) *multipart.FileHeader — 与 multipart 上传入口同构
- createImportIndexes(t, db)(:68-79)— ON CONFLICT 唯一索引补建先例(P-77-7)

被测关键(77-RESEARCH §77-03):
- ImportData 剩余:sheet 名模糊匹配(大小写/包含/回退首个 sheet)、依赖引用二阶段(:390-403)、building+geocoding 分支(:410-412,需 geocoding 非 nil)、department deptCode 去重
- resolveDependentReferencesBatch / applyDependentReferenceResults / groupRecordsByDependencyID / extractDependentValues / getTargetFieldForReferenceByName / getColumnByName — 私有方法同包直调或 workstation 导入端到端
- NewExcelService(db, security.NewPasswordManager(nil), nil, nil) — populateNewUserPasswords 链(HashPassword 纯 pbkdf2SM3)
- workstation List 需 6 表:sys_workstation/ops_floors/ops_buildings/sys_dept/sys_user/ops_workstation_device
- floor Create 软删恢复分支 UPDATE 含 NOW() → sqlite 报 "no such function: NOW"(P-77-2,canary 实证),只覆盖到恢复失败错误路径
- code_generator:GenerateCode/GenerateCodeWithCustomPrefix;canary 实证 $1 占位符 glebarez sqlite 可用
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ImportData 剩余分支 + ReadRawRowsByName + Q-77-C 注释修正</name>
  <files>internal/services/operations/excel_import_rest_77_03_test.go, internal/services/operations/excel_raw_rows_77_03_test.go, internal/services/operations/excel_raw_rows.go</files>
  <read_first>
  - internal/services/operations/excel_service.go — ImportData 剩余分支(sheet 模糊匹配/依赖引用二阶段 :390-403/geocoding 分支 :410-412/deptCode 去重)
  - internal/services/operations/excel_raw_rows.go — ReadRawRowsByName 全文 + :114-120 normalizeHeaderTrim 的注释与实现(Q-77-C 判修点)
  - internal/services/operations/excel_import_export_test.go — buildTestXLSX/xlsxFileHeader(:26-59)+ createImportIndexes(:68-79)+ 行级错误断言与 sheet 回退形态(:143-181)
  - internal/services/operations/geocoding_photo_floor_test.go — fakeGeocodeTransport(RoundTripper)先例(geocoding 分支注入形态)
  - .planning/workstreams/milestone/phases/77-operations-agent-server/77-PATTERNS.md — §77-03
  </read_first>
  <action>
  创建 excel_import_rest_77_03_test.go 与 excel_raw_rows_77_03_test.go(package operations),测试函数前缀 TestImp77_:

  - TestImp77_ImportData_SheetFuzzyMatch:buildTestXLSX 造 sheet 名大小写不一致/包含目标名/完全不匹配三形态,断言前两者命中、后者回退首个 sheet。
  - TestImp77_ImportData_DependentRefs:workstation 导入端到端——「工位名称/所属楼宇/所属楼层名称/部门代码/主设备序列号」列,xlsx 中 floorName 列 DependsOn buildingName:覆盖 resolveDependentReferencesBatch + applyDependentReferenceResults + groupRecordsByDependencyID + extractDependentValues + getTargetFieldForReferenceByName/getColumnByName;含「楼层名在错误楼宇下」的 validateReferenceFields 失败分支;sqlite 唯一索引按 createImportIndexes 先例对齐 sys_workstation 的 config.UniqueKeys 补建(先核对 DDL)。
  - TestImp77_ImportData_GeocodingBranch:building 导入 + geocoding 非 nil(最小可注入实例,参照 fakeGeocodeTransport 先例构造 GeocodingService)覆盖 :410-412 分支。
  - TestImp77_PopulateNewUserPasswords:NewExcelService(db, security.NewPasswordManager(nil), nil, nil) + user 导入行——新用户默认密码哈希落库 + init_flag=true + 已存在用户跳过;含 assignDefaultRolesToNewUsers(需 sys_role/sys_user_role 表)。
  - TestImp77_ReadRawRowsByName(放 excel_raw_rows_77_03_test.go):正常 map[name]row / 同名覆盖 / 空 name 跳过 / 回退首个 sheet / 缺 name 列 / xlsxFileHeader(t, []byte("not a zip"), "x.xlsx") 畸形分支 / 空 Excel——D-06:畸形输入全部手工字节构造。

  Q-77-C quirk 修复(D-01/D-02/D-03,doc-only):excel_raw_rows.go:114-120 normalizeHeaderTrim 注释「转小写用于匹配」与实现(仅 TrimSpace)不符 → 只修注释对齐现行为,零代码行为变化;独立原子 commit fix(quirk-77-c);不加 ToLower(会改变 "Name" 表头匹配行为,无据)。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestImp77_" ./internal/services/operations/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestImp77_" internal/services/operations/excel_import_rest_77_03_test.go internal/services/operations/excel_raw_rows_77_03_test.go` 合计 ≥ 6
  - 测试文件包含 `[]byte("not a zip")` 字面量(D-06 畸形输入)与 buildTestXLSX 复用
  - Q-77-C:excel_raw_rows.go 的 git diff 仅注释行(无代码行变更),独立原子 commit fix(quirk-77-c)
  - `go test -count=1 -run "TestImp77_" ./internal/services/operations/` exit 0;`go build ./...` exit 0
  </acceptance_criteria>
  <done>ImportData 剩余分支 + ReadRawRowsByName 全分支有测试;Q-77-C doc-only 修复以独立原子 commit 落地。</done>
</task>

<task type="auto">
  <name>Task 2: reference_resolver 尾部 + workstation_service 全 CRUD/List 过滤/BatchUpdatePositions</name>
  <files>internal/services/operations/reference_resolver_77_03_test.go, internal/services/operations/workstation_floor_code_77_03_test.go</files>
  <read_first>
  - internal/services/operations/reference_resolver_test.go 与 reference_resolver_depends_test.go — 同包既有 resolver 测试形态(直接追加同构用例)
  - internal/services/operations/reference_resolver.go — ResolveSingleWithCondition(21 unc)与 ResolveBatchWithDependencies 尾部
  - internal/services/operations/workstation_service.go — List(6 表)/Create/Delete/GetByID/BatchDelete/BatchUpdatePositions(CASE WHEN + CAST JOIN)/GetWorkstationDeptOptions/SearchWorkstationOptions
  - internal/services/operations/crud_services_test.go — newCRUDTestDB(:25-54)+ service CRUD 测试骨架(:56-112)
  </read_first>
  <action>
  创建 reference_resolver_77_03_test.go 与 workstation_floor_code_77_03_test.go(测试函数前缀 TestImp77_,同包):

  - TestImp77_ResolveSingleWithCondition:sqlite fixture + conditions map,覆盖条件命中/未命中两态(21 unc)。
  - TestImp77_ResolveBatchWithDependencies_Tail:依赖批量解析尾部分支(~10 unc),沿用 reference_resolver_depends_test.go 的 mock/fixture 形态。
  - TestImp77_WorkstationCRUD:6 表 fixture(sys_workstation/ops_floors/ops_buildings/sys_dept/sys_user/ops_workstation_device,AutoMigrate 或手动 DDL)→ Create(缺楼层依赖报错 + 正常)/ GetByID / Delete / BatchDelete;List 覆盖 name/floorId/floorCode/status/type/orgId 各过滤分支(orgId EXISTS 子查询 canary 实证 sqlite 可用)+ 分页排序断言(page.Total 语义照 crud_services_test.go:56-112)。
  - TestImp77_WorkstationOptions:GetWorkstationDeptOptions / SearchWorkstationOptions。
  - TestImp77_BatchUpdatePositions:CASE WHEN + CAST JOIN(canary 实证 sqlite 可用)批量改楼层/工位;optional 字段 nil 与非 nil 两分支断言落库。

  状态断言引用 models.WorkstationStatus* 具名常量(三态),禁裸 0/1。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestImp77_" ./internal/services/operations/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - workstation_floor_code_77_03_test.go 存在且含 List 过滤分支用例(至少覆盖 name/status/floorId 三个 filter)
  - reference_resolver_77_03_test.go 含 ResolveSingleWithCondition 用例
  - `grep -c "^func TestImp77_"` 四个新文件合计 ≥ 12
  - `go test -count=1 -run "TestImp77_" ./internal/services/operations/` exit 0;`go build ./...` exit 0
  </acceptance_criteria>
  <done>resolver 尾部与 workstation_service 全函数有测试;6 表 fixture 的 List 各过滤分支覆盖。</done>
</task>

<task type="auto">
  <name>Task 3: floor_service + code_generator + BLOCK-01 收口 checkpoint</name>
  <files>internal/services/operations/workstation_floor_code_77_03_test.go</files>
  <read_first>
  - internal/services/operations/floor_service.go — Create 正常/唯一约束错误/软删恢复分支(恢复 UPDATE 含 NOW(),P-77-2)+ syncWorkstationBuildingID + NewFloorService
  - internal/services/operations/code_generator.go — GenerateCode / GenerateCodeWithCustomPrefix + :48-54/:86-91 的 ErrRecordNotFound 死分支(Q-77-D)
  - .planning/workstreams/milestone/phases/77-operations-agent-server/77-RESEARCH.md — §excel_service 导入剩余实施细节(77-03 测试形态)
  </read_first>
  <action>
  同文件追加:

  - TestImp77_FloorCreate:正常创建路径全覆盖 + 唯一约束错误分支 + 软删恢复分支——sqlite 下恢复 UPDATE 报 "no such function: NOW" → 断言错误含「恢复楼层失败」即止;勿为盖 happy tail 改 NOW()(PG 行为正确,D-03 无据);syncWorkstationBuildingID 联动断言。
  - TestImp77_GenerateCode:空表 → 前缀-001;预置 BLD-202608-007 行 → -008(递增);GenerateCodeWithCustomPrefix;serial 非数字(Sscanf 失败)→ 回 1。Q-77-D:ErrRecordNotFound 分支为死代码(Raw+Scan 不返回该错误)——按现行为断言,不删代码,SUMMARY 记录待人工裁决(D-03 无据不臆断)。

  BLOCK-01 收口 checkpoint:运行 `go test -count=1 -cover ./internal/services/operations/`,必须 ≥70.0%;若 <70,从余量清单回补(queryData 更多 filter 列型 / writeDataRows 更多类型 / GetAssetDevicesByWorkstations 剩余分支)直到达线,并在 SUMMARY 记录最终数字与回补项。随后 `go test ./...` 全绿 + `.github/scripts/check-coverage.sh` 不倒退(gate 需 coverage.out:`go test ./... -coverprofile=coverage.out -covermode=atomic` 后运行脚本)。
  </action>
  <verify>
    <automated>go test -count=1 -cover ./internal/services/operations/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `go test -count=1 -cover ./internal/services/operations/` 输出行 coverage: **≥70.0%**(BLOCK-01 达成判据,数字落 SUMMARY)
  - floor 恢复分支断言含「恢复楼层失败」错误信息,且 floor_service.go 无 NOW() 改动(git diff 为 0)
  - code_generator 测试覆盖空表/递增/非数字三态;Q-77-D 在 SUMMARY 有待人工裁决记录
  - `go test -count=1 ./internal/services/operations/` exit 0;`go build ./...` exit 0
  - `git diff --stat` 生产 .go 改动仅 excel_raw_rows.go(Task 1 的 Q-77-C 注释)
  </acceptance_criteria>
  <done>BLOCK-01 收口:operations per-package coverage ≥70.0%,全仓测试全绿,gate 不倒退;quirk 处置(Q-77-C 修/Q-77-D 记录)完整落 SUMMARY。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 ↔ 数据库 | sqlite :memory: only(含 NOW() 方言差异仅影响测试可达性,不改生产 SQL) |
| 测试 ↔ 文件系统 | 全内存 xlsx 字节流(D-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-03-01 | Tampering | 为覆盖率改 PG-only SQL(NOW()/方言) | mitigate | D-03 无据不臆断:P-77-1/P-77-2 明令只测可达段;验收 git diff 生产文件仅 excel_raw_rows.go 注释 |
| T-77-03-02 | Tampering | 二进制 fixture 混入 git | mitigate | D-06;验收 git status 无 testdata/*.xlsx |
| T-77-03-03 | Tampering | Q-77-C 修复越界改行为 | mitigate | doc-only 修正注释,验收 diff 仅注释行;不加 ToLower |
| T-77-03-SC | Tampering | 依赖安装 | accept | 零新增依赖 |
</threat_model>

<verification>
- `go test -count=1 -cover ./internal/services/operations/` ≥70.0%(BLOCK-01 收口判据)
- `go test ./...` 全绿;check-coverage.sh 4 层 gate 不倒退(weighted ≥55.5 + P1 floor + P2 ratchet)
- 生产 .go 改动仅 excel_raw_rows.go 注释(Q-77-C doc-only)
</verification>

<success_criteria>
- 4 个新测试文件全绿,TestImp77_ 前缀 ≥12 个
- BLOCK-01 达成:operations ≥70.0% 落 SUMMARY
- Q-77-C 原子 commit 修复;Q-77-D 按现行为断言 + SUMMARY 待裁决记录
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/77-operations-agent-server/77-03-SUMMARY.md` when done
</output>
