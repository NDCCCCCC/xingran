---
phase: 77-operations-agent-server
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/agent/server/jwt_conn_77_04_test.go
autonomous: true
requirements: [BLOCK-02]

must_haves:
  truths:
    - "按 D-08 切分,本 plan 交付 agent jwt_auth(107 unc)+ connection_manager(80 unc)= 191 stmts 测试,全部经 httptest.NewServer 假后端(backendURL 明文参数注入 NewJWTAuthenticator)驱动,本地回环零真实网络外呼"
    - "connection_manager 状态机全覆盖:Connect 成功(Connecting→Connected + 回调序列 [Connecting, Connected])/heartbeat 失败([Connecting, Disconnected])/Reconnect 三分支(reconnectDelay=1ms 白盒 + maxReconnects 超限 + ctx cancel + stopCh)/StartHealthMonitor channel 同步断言禁裸 sleep + ctx cancel + Disconnect 收尾防 goroutine 泄漏(P-77-4)"
    - "registration 失败分支(:93-101)在 Register 为本地实现的现状下不可达——接受不覆盖,按 D-03 无据不改, SUMMARY 记录"
    - "新 quirk 一律按 D-01 发现即修 / D-02 plan 内就地修 / D-03 有据判定(原子 commit + 同 commit 翻转断言 + 回归用例)"
    - "BLOCK-02 主力贡献:本 plan 后 `go test -count=1 -cover ./internal/agent/server/` 从 21.9% 显著抬升(预期 ≥52%),`go build ./...` + `go test ./...` 全绿"
    - "按 D-09 wave 编排:本 plan 位于 wave 1 与 77-01(不同包)并行;同包 77-05 排 wave 2(同包整包编译互踩规避),其 handlers 测试直接复用本 plan 落地的假后端形态"
  artifacts:
    - path: "internal/agent/server/jwt_conn_77_04_test.go"
      provides: "jwt_auth 全函数 + connection_manager 状态机测试(前缀 TestJWT77_ / TestCM77_)"
      min_lines: 450
      contains: "httptest.NewServer"
  key_links:
    - from: "internal/agent/server/jwt_conn_77_04_test.go"
      to: "NewJWTAuthenticator(jwt_auth.go:61)"
      via: "backendURL 明文参数 = httptest 假后端 srv.URL"
      pattern: "srv\\.URL"
    - from: "假后端 handler"
      to: "APIPath* 常量(jwt_auth.go:23-28)"
      via: "switch r.URL.Path 分发 heartbeat/status/register"
      pattern: "APIPath(Heartbeat|Status|Register)"
    - from: "白盒时钟/延时注入"
      to: "tokenExpiryAt(jwt_auth.go:50) / reconnectDelay(connection_manager.go:48) / stopCh(:51)"
      via: "同包私有字段直写,先注册 t.Cleanup 恢复"
      pattern: "t\\.Cleanup"
---

<objective>
交付 agent/server 的 jwt_auth + connection_manager 测试(191 stmts,BLOCK-02 缺口 299 的主力):httptest.NewServer 假后端经 NewJWTAuthenticator 的 backendURL 明文参数注入,覆盖 agent 出站 HTTP 全链 + 连接状态机。

Purpose: BLOCK-02 首个 plan,建立出站假后端形态(仓内首例)供 77-05 handlers 测试复用。
Output: 新增 internal/agent/server/jwt_conn_77_04_test.go(零生产代码改动,除非触发 D-01 quirk 修复)。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/77-operations-agent-server/77-CONTEXT.md
@.planning/workstreams/milestone/phases/77-operations-agent-server/77-RESEARCH.md
@.planning/workstreams/milestone/phases/77-operations-agent-server/77-PATTERNS.md

<interfaces>
<!-- 关键契约(PATTERNS 逐一复核行号)。 -->

From internal/agent/server/jwt_auth.go:
```go
// :61-73 — backendURL 明文构造参数;nil tlsConfig 走 TLS1.3 安全默认但不阻 http://(A2 已核实)
auth := NewJWTAuthenticator("test-secret", srv.URL, "agent-1", "vm-1", nil)

// :23-28 常量(假后端 switch 用)
const (
    APIPathHeartbeat = "/api/v1/agent/heartbeat"
    APIPathStatus    = "/api/v1/agent/status"
    APIPathRegister  = "/api/v1/vdi/agent/register"
    APIPathTokenRefresh = "/api/v1/agent/refresh"
)
// :50 白盒字段 tokenExpiryAt(RefreshToken 有效跳过分支预置 now+2h)
// Register(:152)=纯本地 generateToken 零 HTTP;RegisterToBackend(:347)=打假后端提取 token
```

From internal/agent/server/connection_manager.go:
```go
// :48 reconnectDelay / :51 stopCh — 同包白盒字段
// Connect = Register(本地必成) + SendHeartbeat(HTTP);registration 失败分支 :93-101 现状不可达
```

假后端响应壳(pkg/response):{"code":0,"message":"...","data":{...}} 即可被 CallBackend 的 json.Decode 吃进。
InitLogger 前置(P-77-5):StartHealthMonitor 的 WithFields(...).Warn 依赖全局 logger,agent_smoke_test.go:133 有 InitLogger("info","") 先例。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: jwt_auth 全函数测试(httptest 假后端 + 白盒时钟 + x509 自签)</name>
  <files>internal/agent/server/jwt_conn_77_04_test.go</files>
  <read_first>
  - internal/agent/server/jwt_auth.go — 全文精读:NewJWTAuthenticator(:61-73)/CallBackend/RegisterToBackend(:347)/Register(:152)/RefreshToken/GetCurrentToken/generateToken/ValidateToken/ParseTokenClaims/NewTLSConfigFromConfig/APIPath 常量(:23-28)/tokenExpiryAt(:50)
  - internal/agent/server/agent_smoke_test.go — InitLogger 前置先例(:133)+ NewTLSConfigFromConfig_Errors 既有错误分支断言(:187-197,直接扩展)
  - .planning/workstreams/milestone/phases/77-operations-agent-server/77-PATTERNS.md — §77-04(role-match analog + 假后端形态)
  </read_first>
  <action>
  创建 jwt_conn_77_04_test.go(package server,同包白盒)。文件级 helper newAgentBackend77(t) 用 httptest.NewServer 建"假后端":handler 内 switch r.URL.Path 分发 APIPathHeartbeat(断言 Authorization: Bearer 头 + body 含 agent_id/vm_id/status/timestamp,回 {"code":0,"message":"success"})/ APIPathStatus(回 {"code":0})/ APIPathRegister(回 {"code":0,"data":{"token":"..."}});t.Cleanup(srv.Close);全程本地回环。被测对象 NewJWTAuthenticator("test-secret", srv.URL, "agent-1", "vm-1", nil)。测试函数前缀 TestJWT77_:

  - TestJWT77_CallBackend:heartbeat/status 成功链;假后端返回非 JSON → decode error 分支;srv.Close() 后调用 → backend request failed 分支。
  - TestJWT77_RegisterToBackend:code=0 + data.token 提取成功;code!=0 注册失败 / Data 为 nil / token 空三分支。
  - TestJWT77_Register:纯本地 generateToken,零 HTTP(断言 token 非空且不依赖后端)。
  - TestJWT77_RefreshToken:白盒预置 tokenExpiryAt = now+2h → 有效提前返回;过期 → 重新生成;GetCurrentToken 读锁快路径与降级刷新路径。白盒字段改动前注册 t.Cleanup 恢复。
  - TestJWT77_ValidateToken / TestJWT77_ParseTokenClaims:good token / bad secret / 非 HMAC 签名 / 垃圾串。
  - TestJWT77_SendHeartbeat / TestJWT77_ReportSystemStatus / Error×2:经 CallBackend 假后端。
  - TestJWT77_NewTLSConfigFromConfig:错误分支扩展 agent_smoke_test.go:187-197 既有函数(全空/坏文件);happy path 用 t.TempDir + crypto/x509 + crypto/ecdsa 自签写 PEM(stdlib ~25 行 helper,零新依赖)覆盖 caFile / cert+key 两形态 + InsecureSkipVerify=!verify 分支;若自签 helper 实现受阻允许降级只覆盖错误分支 + InsecureSkipVerify 分支并在 SUMMARY 记录(35%→~85%)。

  全部出站请求目标只能是 srv.URL(本地回环),测试文件不得出现任何生产后端地址字面量。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestJWT77_" ./internal/agent/server/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestJWT77_" internal/agent/server/jwt_conn_77_04_test.go` ≥ 9
  - `grep -c "httptest.NewServer" internal/agent/server/jwt_conn_77_04_test.go` ≥ 1 且 NewJWTAuthenticator 调用中 backendURL 参数为 srv.URL 类变量(grep 不出现硬编码生产域名)
  - RegisterToBackend 三分支与 RefreshToken 两分支各有独立断言用例
  - `go test -count=1 -run "TestJWT77_" ./internal/agent/server/` exit 0;`go build ./...` exit 0
  </acceptance_criteria>
  <done>jwt_auth 全函数(107 unc)有测试;假后端 helper 就位(77-05 复用);零真实外呼。</done>
</task>

<task type="auto">
  <name>Task 2: connection_manager 状态机 + per-package coverage checkpoint</name>
  <files>internal/agent/server/jwt_conn_77_04_test.go</files>
  <read_first>
  - internal/agent/server/connection_manager.go — 全文:Connect/Reconnect/StartHealthMonitor/Disconnect/String/GetState/IsConnected/GetStats/SetStateChangeCallback/notifyStateChange;:48 reconnectDelay / :51 stopCh;:93-101 registration 失败分支
  - internal/agent/server/jwt_conn_77_04_test.go — Task 1 的 newAgentBackend77 helper 与 InitLogger 前置,本任务复用
  </read_first>
  <action>
  同文件追加测试函数前缀 TestCM77_(文件开头已有 InitLogger("info","") 前置,P-77-5):

  - TestCM77_Connect_Success:正常假后端 → Connecting → Register(本地) → SendHeartbeat(HTTP) → Connected;SetStateChangeCallback 记录回调序列,断言 [Connecting, Connected]。
  - TestCM77_Connect_HeartbeatFail:已 Close 的假后端(或返回 500 的 handler)→ 断言 [Connecting, Disconnected] + "heartbeat failed" 错误。registration 失败分支(:93-101)在 Register 本地实现下不可达——接受不覆盖,D-03 无据不改,SUMMARY 记录。
  - TestCM77_Reconnect:白盒 reconnectDelay = time.Millisecond(先注册 t.Cleanup 恢复)/ maxReconnects 超限(白盒设 reconnectCount=maxReconnects)/ ctx cancel / stopCh(直写 cm.stopCh <- struct{}{})三分支。
  - TestCM77_StartHealthMonitor:interval=5ms + 断连状态 + ctx.WithTimeout(200ms) → goroutine 触发 Reconnect;用 channel 同步等待回调再断言(禁裸 sleep,P-77-4);收尾必须 cancel ctx + cm.Disconnect() 防 goroutine 泄漏。
  - TestCM77_Misc:Disconnect / String 四态 + unknown / GetState / IsConnected / GetStats / SetStateChangeCallback / notifyStateChange 直调。

  收尾 checkpoint:`go test -count=1 -cover ./internal/agent/server/` 数字落 SUMMARY(基线 21.9% Windows;本 plan 191 stmts 主力,预期 ≥52%——余量由 77-05 收口)。
  </action>
  <verify>
    <automated>go test -count=1 ./internal/agent/server/ && go test -count=1 -cover ./internal/agent/server/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -cE "^func TestCM77_" internal/agent/server/jwt_conn_77_04_test.go` ≥ 5
  - StartHealthMonitor 用例含 channel 同步等待(测试文件不含 time.Sleep 裸等待主断言路径)且收尾有 Disconnect/cancel
  - 白盒字段改动(reconnectDelay/stopCh/tokenExpiryAt)均有 t.Cleanup 恢复
  - `go test -count=1 -cover ./internal/agent/server/` 输出 coverage ≥52%(数字落 SUMMARY;若未达,SUMMARY 说明 77-05 余量论证)
  - `go build ./...` exit 0;生产 .go 改动 = 0 文件(除非 D-01 quirk 修复有 SUMMARY deviation)
  </acceptance_criteria>
  <done>connection_manager 状态机全覆盖(80 unc);per-package coverage 数字落 SUMMARY;假后端形态沉淀为 77-05 复用基础。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent 测试 ↔ 网络 | 出站 HTTP 仅允许 httptest.NewServer 本地回环(srv.URL),禁止任何真实后端/外网地址 |
| 测试 ↔ 凭据 | token/secret 只用字面量 "test-secret"/fakeToken,非真实凭据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-04-01 | Spoofing/信息外泄 | 测试误呼真实后端 | mitigate | 假后端仅 srv.URL;验收 grep 测试文件无生产域名/https 外网字面量 |
| T-77-04-02 | DoS | goroutine 泄漏(StartHealthMonitor/Reconnect)导致 -race 噪声/CI 卡死 | mitigate | ctx cancel + cm.Disconnect() 收尾 + channel 同步断言禁裸 sleep(P-77-4) |
| T-77-04-03 | Information Disclosure | 测试 token 误用真实值 | accept | 仅 "test-secret"/fakeToken 字面量,无真实凭据入仓 |
| T-77-04-SC | Tampering | 依赖安装 | accept | 零新增依赖(x509/ecdsa/httptest 均 stdlib) |
</threat_model>

<verification>
- `go test -count=1 ./internal/agent/server/` 全绿
- `go test -count=1 -cover ./internal/agent/server/` ≥52% 落 SUMMARY
- `go build ./...` exit 0;零真实网络外呼;生产 .go 改动 0 文件
</verification>

<success_criteria>
- jwt_conn_77_04_test.go 存在,TestJWT77_ ≥9 + TestCM77_ ≥5 全绿
- 假后端形态(backendURL 注入)落地,77-05 可直接复用
- BLOCK-02 累积覆盖率 ≥52%
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/77-operations-agent-server/77-04-SUMMARY.md` when done
</output>
