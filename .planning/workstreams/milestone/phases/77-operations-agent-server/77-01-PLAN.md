---
phase: 77-operations-agent-server
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/services/operations/workstation_device_77_01_test.go
autonomous: true
requirements: [BLOCK-01]

must_haves:
  truths:
    - "按 D-08 切分,本 plan 交付 workstation_device_service 全部 sqlite 可达函数测试(* 前段)——RESEARCH 实证该 service 零 LDAP/AD 依赖,7 表 sqlite fixture 直测,无任何 stub"
    - "mergeBySerial 合并优先级三态断言(AD+Asset 双命中 / 仅 Asset / req fallback)以 workstation_device_service.go:53-61 接口注释为据源——D-03 有据判定"
    - "新 quirk 一律按 D-01 发现即修(原子 commit + 同 commit 翻转断言 + 回归用例)、按 D-02 在本 plan 内就地修(SUMMARY 记 deviation + 根因 + 证据)、按 D-03 有据判定(models 常量/字段注释/docs/standards 为据;无据则按现行为断言 + SUMMARY 记录待人工裁决)"
    - "BLOCK-01 主力贡献达成:本 plan 覆盖 ~375 unc stmts,收尾 `go test -count=1 -cover ./internal/services/operations/` 较 61.1% 基线 measurable 提升,`go build ./...` 与 `go test ./...` 全绿,4 层 gate 不倒退"
    - "按 D-09 wave 编排:本 plan 位于 wave 1 与 77-04(不同包)并行;同包 77-02/77-03 隔 wave 串行(同包并行执行会互踩 per-task 的整包 `go test` 编译,Phase 76 先例)"
  artifacts:
    - path: "internal/services/operations/workstation_device_77_01_test.go"
      provides: "workstation_device_service 的 sqlite 7 表 fixture + 全函数测试(测试函数前缀 TestWSD77_)"
      min_lines: 400
      contains: "func setupWSD77DB"
  key_links:
    - from: "internal/services/operations/workstation_device_77_01_test.go"
      to: "internal/services/operations/workstation_device_service.go"
      via: "NewWorkstationDeviceService(db) 直调(唯一依赖 *gorm.DB)"
      pattern: "NewWorkstationDeviceService\\(db\\)"
    - from: "mergeBySerial 三态断言"
      to: "ops_workstation_device 落库行"
      via: "同包 db 直查落库结果断言合并字段"
      pattern: "WorkstationDevice"
---

<objective>
交付 workstation_device_service 的全量 sqlite 直测(operations 包 61.1% → 70% 的主力,RESEARCH 实测该文件 445 unc,sqlite 可达 ~375)。

Purpose: BLOCK-01 缺口 331 stmts 的 ~375 由本 plan 单独覆盖,是达线主力(RESEARCH 建议"77-01 列为达线主力和首个 wave")。
Output: 新增 internal/services/operations/workstation_device_77_01_test.go(零生产代码改动,除非触发 D-01 quirk 修复)。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/77-operations-agent-server/77-CONTEXT.md
@.planning/workstreams/milestone/phases/77-operations-agent-server/77-RESEARCH.md
@.planning/workstreams/milestone/phases/77-operations-agent-server/77-PATTERNS.md

<interfaces>
<!-- 关键契约(PATTERNS 已逐一复核行号)。executor 直接使用,无需探索代码库。 -->

From internal/services/operations/workstation_device_service.go:
```go
// :156 — 唯一依赖 *gorm.DB,所谓 "AD 设备" 全部读表(sys_ad_computer/sys_ad_user 由同步调度器预落库),零 LDAP 调用
func NewWorkstationDeviceService(db *gorm.DB) WorkstationDeviceService

// 接口注释 :53-61 = mergeBySerial 合并优先级据源(D-03 有据):
//   deviceName: AD > req; deviceModel/deviceType/responsibleUser: Asset > req;
//   mac: AD > Asset > req; ip: AD > req; assetID/adComputerID: 命中填
```

7 表 sqlite fixture(RESEARCH §77-01,推荐手动 CREATE TABLE,physical_test 先例):
sys_workstation / sys_user / sys_ad_user / sys_ad_computer(managed_by, original_description, serial_number)/ ops_asset(devicesn, nowuser_name, deptname, mac1, machine_ip)/ sys_dept / ops_workstation_device

状态常量真相源: internal/models 具名常量(如 models.DeviceSourceAD/Asset/Manual、models.WorkstationDeviceStatusNormal),断言禁裸 0/1(status_constants_test.go 锁值)。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sqlite 7 表 fixture + 查询链测试(GetADDevices 家族 + GetPhysicalDevices 前段)</name>
  <files>internal/services/operations/workstation_device_77_01_test.go</files>
  <read_first>
  - internal/services/operations/workstation_device_service.go — 被测主体(1948 行;重点 :53-61 接口注释 / :156 构造器 / 各查询函数的表与列引用)
  - internal/services/operations/workstation_device_physical_test.go — 同包 analog:手动 CREATE TABLE fixture(:86-103)与「报错即证明越过早退」断言手法(:249-284)
  - internal/services/operations/excel_export_devices_test.go — setupEnrichmentTestDB(:107-142)的 sys_ad_computer/ops_asset 列定义是 fixture 现成起点
  - .planning/workstreams/milestone/phases/77-operations-agent-server/77-PATTERNS.md — §77-01(exact analog 摘录)
  </read_first>
  <action>
  创建 workstation_device_77_01_test.go(package operations,同包白盒)。fixture:sqlite.Open(":memory:")(glebarez 驱动,cru_services_test.go 同款)+ 手动 CREATE TABLE 建 7 表(列集精简到被测函数实际引用列;sys_ad_computer 必含 managed_by/original_description/serial_number/mac_address/ip_address,ops_asset 必含 devicesn/nowuser_name/deptname/mac1/machine_ip),helper 命名 setupWSD77DB(t) 返回 *gorm.DB,种子行照 analog 裸 INSERT INTO 风格。测试函数前缀 TestWSD77_:

  - TestWSD77_GetADDevices:造数走通 工位→sys_user(username)→sys_ad_user(user_dn)→sys_ad_computer(managed_by = UserDN 命中 + original_description LIKE '%|username|%' 命中)四表链,断言返回设备数与 serial_number/mac/ip;含工位无 user_id 的空结果分支。
  - TestWSD77_GetAssetDevices:工位→sys_user→ops_asset(join sys_dept)链,断言 devicesn/nowuser_name/deptname 透传。
  - TestWSD77_GetADDevicesByUser:managed_by 与 original_description LIKE 双命中策略各造一台 → 返回 2 台;用户不存在(:608)与无 AD 记录(:622)两处 warn-and-empty 分支返回空切片不报错。
  - TestWSD77_GetAssetDevicesByUser:两个同名 nickname 的 asset + 用户带 dept → 精确匹配过滤生效;dept 不匹配 → 保留策略 1 全量。
  - TestWSD77_GetADDevicesByWorkstations / TestWSD77_GetAssetDevicesByWorkstations:补错误分支 + original_description 匹配分支(当前 84.1%/89.3% 的剩余 unc)。
  - TestWSD77_GetPhysicalDevices_FrontSegment / TestWSD77_GetPhysicalDevicesByWorkstations_FrontSegment:ParamMissing(空 ID)/ParamInvalid(非 UUID)/工位不存在三分支正常断言;越过后照 physical_test.go:249-253 手法断言 require.Error + require.Empty 即停(P-77-1:DISTINCT ON/REGEXP_REPLACE/::text 是 PG-only SQL,sqlite 必报错属预期,勿为覆盖率改 SQL——D-03 无据)。
  - TestWSD77_macIDFragment / TestWSD77_stringFromMap:纯函数边角直调。

  状态与来源断言一律引用 internal/models 具名常量,禁裸 0/1。
  </action>
  <verify>
    <automated>go test -count=1 -run "TestWSD77_" ./internal/services/operations/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - 文件存在且以 `package operations` 声明
  - `grep -c "^func TestWSD77_" internal/services/operations/workstation_device_77_01_test.go` ≥ 8
  - `grep -o 'CREATE TABLE' internal/services/operations/workstation_device_77_01_test.go | wc -l` ≥ 7(7 表 fixture)
  - GetPhysicalDevices* 测试含 require.Error 断言(越过早退即断 SQL 错误),且测试文件不含对 DISTINCT ON 查询结果行的断言
  - `go test -count=1 -run "TestWSD77_" ./internal/services/operations/` exit 0;`go build ./...` exit 0
  </acceptance_criteria>
  <done>查询链全部函数有测试且全绿;7 表 fixture 就位;GetPhysicalDevices* 仅覆盖 sqlite 可达前段。</done>
</task>

<task type="auto">
  <name>Task 2: 写链 + mergeBySerial 三态 + per-package coverage checkpoint</name>
  <files>internal/services/operations/workstation_device_77_01_test.go</files>
  <read_first>
  - internal/services/operations/workstation_device_service.go — SyncFromAD/SyncFromAsset/AddDeviceManual/UpdateDevice/DeleteDevice/SetPrimaryDevice/SetPrimaryAndSave/SetPrimaryAndSaveBySerial/mergeBySerial 实现 + :53-61 优先级注释
  - internal/services/operations/workstation_device_77_01_test.go — Task 1 已落盘的 fixture,本任务直接复用 setupWSD77DB
  </read_first>
  <action>
  同文件追加 TestWSD77_ 前缀测试:

  - TestWSD77_SyncFromAD / TestWSD77_SyncFromAsset:预置来源表种子(sys_ad_computer / ops_asset)与同 workstation_id+device_source 旧行 → 调用后断言旧行被删、新行按 models.DeviceSourceAD / models.DeviceSourceAD/Asset 正确插入(「读表→删旧→插新」纯 DB 语义,RESEARCH 实证零 LDAP)。
  - TestWSD77_AddDeviceManual:资产命中 / 未命中 / 错误三分支。
  - TestWSD77_UpdateDevice:11 个可选字段的 updates map——请求只带 2 个字段时断言其余字段不被覆盖。
  - TestWSD77_DeleteDevice:删除 + 不存在分支。
  - TestWSD77_SetPrimaryDevice:事务两步——预置既有主设备 → 调用后旧主 is_primary=false、新行 is_primary=true。
  - TestWSD77_SetPrimaryAndSave / TestWSD77_SetPrimaryAndSaveBySerial:预置 ad/asset 来源旧行 + 既有主设备 → 调用后断言 ad/asset 行被清、旧主 false、新 manual 行 true;BySerial 变体含 req nil 归一化分支。
  - TestWSD77_mergeBySerial_ThreeStates:造三条 SN 数据——SN 同时存在于 sys_ad_computer 与 ops_asset(双命中)/ 仅 ops_asset / 都没有(req fallback),断言落库 ops_workstation_device 行字段严格按 :53-61 注释优先级:deviceName AD>req、model/type/responsibleUser Asset>req、mac AD>Asset>req、ip AD>req、assetID/adComputerID 命中填(D-03 有据锚点)。

  quirk 纪律(D-01/D-02/D-03):测试过程中发现任何行为与据源(接口注释/models 常量/docs/standards)不符 → 当场判修:原子 commit fix(quirk-77-x) + 同 commit 回归用例 + SUMMARY 记 deviation;无据可查 → 按现行为断言 + SUMMARY 记录待人工裁决,不臆断。

  收尾 checkpoint:运行 `go test -count=1 -cover ./internal/services/operations/`,把 per-package 覆盖率数字记入 SUMMARY(基线 61.1%;本 plan 目标贡献 ~375 stmts,若实测提升 <4pp 需在 SUMMARY 说明缺口语义并确认 77-02/03 余量仍足)。
  </action>
  <verify>
    <automated>go test -count=1 ./internal/services/operations/ && go test -count=1 -cover ./internal/services/operations/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "^func TestWSD77_" internal/services/operations/workstation_device_77_01_test.go` ≥ 15(Task 1+2 合计)
  - 测试文件包含 mergeBySerial 三态断言:至少一个用例同时预置 sys_ad_computer 与 ops_asset 同 SN 种子行,并断言 mac/ip/model 的来源优先级
  - `go test -count=1 -cover ./internal/services/operations/` 输出 coverage 较 61.1% 提升 ≥4pp(数字落 SUMMARY)
  - `go build ./...` exit 0;`git diff --stat` 生产 .go 改动为 0 文件(除非 D-01 quirk 修复,SUMMARY 有对应 deviation 记录)
  </acceptance_criteria>
  <done>workstation_device_service 全部 sqlite 可达函数有测试;per-package coverage 数字记录进 SUMMARY;quirk 处理遵循 D-01/D-02/D-03。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 ↔ 数据库 | 全部走 sqlite :memory:,不触碰任何生产 PG/Redis |
| 测试 ↔ 生产代码 | 本 plan 仅新增 *_test.go;生产 .go 改动仅 D-01 quirk 修复豁免 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01-01 | Tampering | fixture 误连生产库 | mitigate | 全部连接串为 sqlite.Open(":memory:");测试文件 grep 不得出现 postgres DSN |
| T-77-01-02 | Tampering | quirk 修复扩大化改生产代码 | mitigate | D-03 有据判定 + D-02 plan 内就地修 + 原子 commit + SUMMARY deviation(git diff --stat 审计) |
| T-77-01-SC | Tampering | 依赖安装 | accept | 零新增外部依赖(RESEARCH Package Legitimacy Audit),slopcheck 不适用 |
</threat_model>

<verification>
- `go test -count=1 ./internal/services/operations/` 全绿
- `go test -count=1 -cover ./internal/services/operations/` 数字较 61.1% 基线提升 ≥4pp 并落 SUMMARY
- `go build ./...` exit 0
- 生产 .go 改动 = 0 文件(或仅有 D-01 quirk 修复 + SUMMARY deviation 记录)
</verification>

<success_criteria>
- workstation_device_77_01_test.go 存在,TestWSD77_ 前缀测试 ≥15 个全绿
- mergeBySerial 三态断言以 :53-61 注释为据(D-03)
- per-package coverage 提升落 SUMMARY,为 77-02/03 收口 70% 建立主力基础(BLOCK-01)
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/77-operations-agent-server/77-01-SUMMARY.md` when done
</output>
