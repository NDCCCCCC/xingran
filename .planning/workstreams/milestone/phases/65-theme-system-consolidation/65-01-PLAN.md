---
phase: 65-theme-system-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - xingran-react-frontend/src/index.css
  - xingran-react-frontend/src/main.tsx
  - xingran-react-frontend/src/App.tsx
  - xingran-react-frontend/src/components/ConfigProvider.tsx
  - xingran-react-frontend/src/components/layout/header.tsx
  - xingran-react-frontend/src/components/layout/sidebar.tsx
  - xingran-react-frontend/src/components/layout/shared/TabBar.tsx
  - xingran-react-frontend/src/components/layout/InnovativeLayout.tsx
  - xingran-react-frontend/src/design-system/components/ThemeProvider.tsx
  - xingran-react-frontend/src/design-system/components/AntdThemeBridge.tsx
  - xingran-react-frontend/src/design-system/components/ThemeSwitcher.tsx
  - xingran-react-frontend/src/design-system/components/ColorSwitcher.tsx
  - xingran-react-frontend/src/design-system/themes/ (20 files, deleted)
  - xingran-react-frontend/src/design-system/tokens/colors.ts
  - xingran-react-frontend/src/design-system/tokens/shadows.ts
  - xingran-react-frontend/src/design-system/tokens/spacing.ts
  - xingran-react-frontend/src/design-system/tokens/colors.test.ts
  - xingran-react-frontend/src/design-system/utils/color.ts
  - xingran-react-frontend/src/types/theme.ts
  - xingran-react-frontend/src/types/config.ts
  - xingran-react-frontend/src/store/themeStore.ts
  - xingran-react-frontend/src/store/settingsStore.ts
  - xingran-react-frontend/src/services/configService.ts
  - xingran-react-frontend/src/lib/defaultThemeApi.ts
  - xingran-react-frontend/src/pages/settings/index.tsx
  - xingran-react-frontend/src/pages/system/settings/default-theme.tsx
  - xingran-react-frontend/src/utils/three/colors.ts
autonomous: false
requirements: [THEME-01, THEME-02, THEME-03]

must_haves:
  truths:
    - "D-01: design-system/themes/ 目录整体删除（6 套主题目录 + index.ts + theme-styles.css 共 20 个文件），ThemeSwitcher.tsx / ColorSwitcher.tsx 组件文件不存在"
    - "D-01: settings 页主题风格 Select 与颜色自定义（主色/侧边栏 ColorPicker）入口不再存在；管理员默认主题页 default-theme.tsx 同样只剩明暗模式"
    - "D-01/D-05: 布局切换（ClassicLayout/HybridLayout/InnovativeLayout）与密度切换（compact/comfortable/spacious）入口完整保留，layoutStore.ts 本 phase 零改动"
    - "D-02: 所有保留色值唯一来源为 brand-spec.md —— index.css 静态变量层 + tokens/colors.ts xingranBrand 常量，无任何 6 套旧主题色值残留"
    - "D-03: AntdThemeBridge 中 Button colorPrimary 永远为 xingranBrand.greenPrimary #156031，三层优先级链（customColors→antdPrimary→DEFAULT）已删除"
    - "THEME-02: settings 页明暗模式 Radio 可切换 light/dark，data-color-mode 属性正确写入，暗色模式深绿底 #0A2418/#0F2E1B 等推导值生效且 QA-01 对比度测试全绿"
    - "THEME-01: grep 验证 ThemeSwitcher|ColorSwitcher|getTheme|ThemeType|ThemeStyle|themePresets|applyPrimaryColor|applySidebarBackgroundColor 在 src/ 下零命中"
  artifacts:
    - path: "xingran-react-frontend/src/index.css"
      provides: "品牌变量唯一静态来源（含新增 --theme-brand* 与 --theme-primary-500/600）+ 合并后的 theme-styles 规则"
      contains: "--theme-brand:"
    - path: "xingran-react-frontend/src/store/themeStore.ts"
      provides: "单 mode 字段的主题 store"
      contains: "ColorMode"
    - path: "xingran-react-frontend/src/design-system/components/AntdThemeBridge.tsx"
      provides: "直连 xingranBrand 的 AntD 主题桥"
      contains: "xingranBrand.greenPrimary"
    - path: "xingran-react-frontend/src/pages/settings/index.tsx"
      provides: "仅含明暗/布局/密度/数据设置的设置页"
      contains: "明暗模式"
  key_links:
    - from: "src/components/layout/header.tsx, sidebar.tsx, shared/TabBar.tsx"
      to: "src/index.css"
      via: "theme-styles.css 规则合并后由 main.tsx 的 index.css 单入口提供"
      pattern: "ant-menu-item-selected"
    - from: "src/design-system/components/AntdThemeBridge.tsx"
      to: "src/design-system/tokens/colors.ts"
      via: "import { xingranBrand }"
      pattern: "xingranBrand\\.greenPrimary"
    - from: "src/store/themeStore.ts"
      to: "document.documentElement[data-color-mode]"
      via: "applyToDOM 写入"
      pattern: "data-color-mode"
---

# Phase 65 Plan 01: 主题系统收敛

**Status:** READY FOR EXECUTION
**Created:** 2026-08-18
**Source:** ROADMAP Phase 65（THEME-01/02/03）+ Phase 64 64-01-SUMMARY cleanup_notes + v1.22 锁定决策 D-01/D-02/D-03/D-05

<objective>

移除 6 套主题（minimal / glassmorphism / neumorphism / flat2.0 / luxury-quiet / ink-amber）与主题切换能力，整站视觉归一到品牌单主题；保留 light/dark 双模式（品牌色相的深底推导）与 layoutStore 的布局/密度切换。

Purpose: 落地 D-01 不可逆决策，消除多主题死代码与维护成本，为 Phase 66 组件样式落地提供单一品牌上下文。
Output: themes/ 目录整体删除、13+1 个消费方零残留、themeStore 单 mode 化、bundle 体积下降、全量自动化门绿。

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-brand-token-layer-and-contrast-verification/64-01-SUMMARY.md
@655aa291-9bfe-4e94-ad5d-b3c8b2d24984/brand-spec.md

<interfaces>
<!-- 关键现状契约（已从代码库提取，executor 无需再探索） -->

themeStore 现状（src/store/themeStore.ts）：
- 字段: configuration: ThemeConfiguration{mode,style,customColors?}, appliedTheme: ThemeType, appliedMode: ColorMode
- 动作: syncFromSettings / previewTheme / previewMode / applyToDOM / savePreview / resetPreview
- 导入 getTheme/applyThemeVariables/applyEffectVariables/applyPrimaryColor/applySidebarBackgroundColor from "@/design-system/themes"（全部待删）
- 导出 useTheme() hook（仅 ThemeSwitcher 消费，随之删除）
- 监听 window "settings-changed" 事件 → syncFromSettings(preferences.theme)

index.css 现状（Phase 64 已落地品牌变量）：
- :root 定义 --theme-primary/--theme-neutral-*/--sidebar-* 等 253 变量
- [data-color-mode="light"] 块（line ~134）与 [data-color-mode="dark"] 块（line ~178）
- 缺失（仅靠 themes/index.ts 运行时注入，本 phase 必须补静态定义）:
  --theme-brand / --theme-brand-dark / --theme-brand-alpha-10（theme-styles.css 消费）
  --theme-primary-50/100/500/600/700（InnovativeLayout.tsx + GlobalSearch.tsx 消费，当前回退蓝色 #3b82f6 系）
- --theme-purple 无任何定义，消费方（7 处，含 3D 业务页）全部走 fallback hex —— 本 phase 不动（VIS-01 3D 接品牌转 v1.23+）

theme-styles.css（97 行，将被合并进 index.css 后删除）：
- .ant-menu-item-selected / .ant-menu-submenu-selected 系列 → var(--theme-brand) + var(--theme-brand-alpha-10)
- .ant-tabs-tab-active / .ant-tabs-ink-bar → var(--theme-brand)
- .layout-sidebar / .layout-header / .layout-tab-bar / .tab-scroll-button / .tabs-scroll-container
- 当前被 main.tsx / header.tsx / sidebar.tsx / TabBar.tsx 4 处 import

colors.ts 旧导出（Phase 64 保留待清）：
- baseColors（蓝/紫/indigo/灰等色阶）、semanticColors、gradients、brandColors
- 唯一外部消费方：src/utils/three/colors.ts 用 brandColors.{success,error,warning,primary,secondary,info} 6 值
  → 内联当前 hex 值到 three/colors.ts（值不变，3D 视觉零变化；接品牌属 VIS-01 v1.23+）

shadows.ts / spacing.ts：唯一消费方是 themes/*/light|dark.ts，themes/ 删除后零消费 → 整文件删除
typography.ts：AntdThemeBridge 消费 fontFamily → 保留
utils/color.ts：仅 ColorSwitcher + themes/index.ts 消费 → 删除
types/theme.ts：仅 themes/* + themeStore 消费 → 删除（ColorMode 以 types/config.ts 为准）

隐藏消费方（13 列表之外，grep 实测发现）：
- src/pages/system/settings/default-theme.tsx —— 管理员默认主题页（style Select + customColors ColorPicker + setDefaultThemeConfig）
- src/lib/defaultThemeApi.ts —— ThemeConfiguration{mode,style,customColors} + getDefaultThemeConfig/setDefaultThemeConfig/syncUserThemeToDefault
- src/services/configService.ts —— migratePreferences / backendToFrontend / toBackend 处理 themeStyle 与 customPrimaryColor/customSidebarColor
</interfaces>
</context>

## Cleanup Inventory

| 待清理项 | 位置 | 处置 | 任务 |
|---------|------|------|------|
| 6 套主题目录（minimal/glassmorphism/neumorphism/flat2.0/luxury-quiet/ink-amber，各 3 文件） | `src/design-system/themes/` | 整目录删除（20 文件） | T6 |
| themes/index.ts（getTheme/themePresets/applyThemeVariables/applyEffectVariables/applyPrimaryColor/applySidebarBackgroundColor） | `src/design-system/themes/index.ts` | 删除；运行时注入变量先补入 index.css | T1+T6 |
| themes/theme-styles.css（97 行 antd menu/tab 覆盖） | `src/design-system/themes/theme-styles.css` | 规则合并进 index.css，文件删除，4 处 import 移除 | T1+T6 |
| ThemeSwitcher.tsx（65 行）/ ColorSwitcher.tsx（477 行） | `src/design-system/components/` | 删除 + InnovativeLayout 引用移除 | T2 |
| themeStore 多主题字段（appliedTheme/previewTheme/savePreview/resetPreview/useTheme hook） | `src/store/themeStore.ts` | 重写为单 mode store | T4 |
| ThemeProvider appliedTheme 订阅与 data-theme 写入 | `src/design-system/components/ThemeProvider.tsx` | 简化为 mode-only（CSS 无 [data-theme] 选择器，可安全丢弃 data-theme 属性） | T4 |
| ConfigProvider save-theme-settings 监听（preview 保存通道） | `src/components/ConfigProvider.tsx` | 移除该监听，保留 save-layout-settings | T4 |
| AntdThemeBridge 三层优先级链 customColors.primary→antdPrimary→DEFAULT_ANTD_PRIMARY | `src/design-system/components/AntdThemeBridge.tsx` | 直连 xingranBrand.greenPrimary；移除 themeStore/getTheme 依赖 | T3 |
| settings 页主题风格 Select + 颜色自定义区（2 ColorPicker + 清除按钮 + 4 handler） | `src/pages/settings/index.tsx` | 移除；保留明暗模式 Radio + 布局/密度/数据区 | T5 |
| 管理员默认主题页 style/customColors 字段 | `src/pages/system/settings/default-theme.tsx` + `src/lib/defaultThemeApi.ts` | 收窄为仅 mode | T5 |
| types/config.ts ThemeStyle/ThemeConfiguration.style/customColors/isValidThemeStyle | `src/types/config.ts` | 删除；ThemeConfiguration → { mode } | T6 |
| configService themeStyle/customColors 迁移与映射 | `src/services/configService.ts` | 移除 style/customColors 处理；BackendUserPreferences 类型字段保留 optional（后端契约不动） | T6 |
| colors.ts 旧导出 baseColors/semanticColors/gradients/brandColors | `src/design-system/tokens/colors.ts` | 删除；three/colors.ts 内联 6 值 | T6 |
| shadows.ts（neumorphicShadows/glassShadows/directionalShadows 等全量）/ spacing.ts | `src/design-system/tokens/` | 零消费整文件删除（先 grep 复核） | T6 |
| types/theme.ts / utils/color.ts | `src/types/`、`src/design-system/utils/` | 删除 | T6 |

## 执行顺序设计（每任务独立 commit + 编译常绿）

依赖分析决定顺序：先解除消费方对 themes/ 与 themeStore 旧 API 的引用（T1-T5），最后物理删除（T6）。每个 commit 边界构建验证分级：全部任务跑 `npm run type-check`；T2 / T4 / T6 / T8（涉及 import 图或物理删除的任务）附加 `npm run build`（与 `<verification>` 段一致）。Phase 66 可在任意边界接续。

<tasks>

<task type="auto">
  <name>T1: CSS 地基 —— index.css 补齐运行时变量 + 合并 theme-styles.css + 记录 bundle 基线</name>
  <files>xingran-react-frontend/src/index.css, xingran-react-frontend/src/main.tsx, xingran-react-frontend/src/components/layout/header.tsx, xingran-react-frontend/src/components/layout/sidebar.tsx, xingran-react-frontend/src/components/layout/shared/TabBar.tsx</files>
  <action>
    1. 先运行 npm run build 记录 bundle 基线（vendor-react gzip kB，历史参考 774.96 kB），把数值写入 commit message，供 T8 对比。
    2. index.css :root 变量段新增（brand-spec 实测值，per D-02）：
       --theme-brand: #156031; --theme-brand-dark: #14542E; --theme-brand-alpha-10: rgba(21, 96, 49, 0.1);
       --theme-primary-50: #E9EFEB; --theme-primary-100: #598E5E; --theme-primary-500: #156031; --theme-primary-600: #14532D; --theme-primary-700: #14542E;
    3. index.css [data-color-mode="dark"] 块新增暗色推导（深底提亮，per brand-spec 深底浅字规则）：
       --theme-brand: #598E5E; --theme-brand-dark: #3B784C; --theme-brand-alpha-10: rgba(89, 142, 94, 0.15);
       --theme-primary-500: #3B784C; --theme-primary-600: #598E5E;（50/100/700 沿用 :root 值即可，不覆盖）
    4. 把 design-system/themes/theme-styles.css 的 97 行规则原样合并进 index.css 末尾（新建「Theme Component Overrides (merged from theme-styles.css)」注释分区），不改任何选择器与值。
    5. 删除 4 处 import "@/design-system/themes/theme-styles.css"（main.tsx 第 3 行、header.tsx 第 12 行、sidebar.tsx 第 24 行、TabBar.tsx 第 24 行）。main.tsx 的 "./index.css" import 保留为唯一样式入口。
    注意：本任务不删 themes/ 目录任何文件；[data-color-mode="light"] 块的 sidebar 白底 override 保持不动（侧边栏深绿化属 Phase 66 COMP-01）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && grep -c "theme-brand-alpha-10" src/index.css && grep -rn "theme-styles.css" src/ --include="*.tsx" --include="*.ts" | wc -l && npm run type-check</automated>
  </verify>
  <done>index.css 含 :root + dark 两组 --theme-brand* 与 --theme-primary-500/600 静态定义；theme-styles.css 规则在 index.css 可检索（如 ".ant-menu-item-selected"）；src/ 下 theme-styles.css import 计数为 0；type-check 退出码 0；bundle 基线数值已记录。Commit: refactor(brand): merge theme-styles.css into index.css and add static brand vars per THEME-01</done>
</task>

<task type="auto">
  <name>T2: 删除 ThemeSwitcher / ColorSwitcher + InnovativeLayout 引用清理</name>
  <files>xingran-react-frontend/src/design-system/components/ThemeSwitcher.tsx, xingran-react-frontend/src/design-system/components/ColorSwitcher.tsx, xingran-react-frontend/src/components/layout/InnovativeLayout.tsx</files>
  <action>
    1. 物理删除 ThemeSwitcher.tsx 与 ColorSwitcher.tsx（per D-01）。
    2. InnovativeLayout.tsx：删除第 9/12 行 import 与第 244/247 行渲染（&lt;ThemeSwitcher /&gt; 与 &lt;ColorSwitcher /&gt;）；保留 &lt;LayoutSwitcher /&gt; 与 &lt;DensitySwitcher /&gt;（第 245/246 行，THEME-03 边界）；删除分隔 Space 中因此产生的多余 &lt;Divider&gt; 时注意保持其余项间距结构不变。
    3. InnovativeLayout 其余 var(--theme-*) 引用不动（--theme-primary-500/600 已由 T1 静态定义，--theme-purple 走既有 fallback，属 VIS-01 范围）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && test ! -f src/design-system/components/ThemeSwitcher.tsx && test ! -f src/design-system/components/ColorSwitcher.tsx && grep -rn "ThemeSwitcher\|ColorSwitcher" src/ --include="*.tsx" --include="*.ts" | wc -l && npm run type-check && npm run build</automated>
  </verify>
  <done>两个组件文件不存在；src/ 下 ThemeSwitcher|ColorSwitcher 引用 0 命中；InnovativeLayout 仍渲染 LayoutSwitcher/DensitySwitcher；type-check + build 退出码 0。Commit: feat(brand): remove ThemeSwitcher and ColorSwitcher components per D-01/THEME-01</done>
</task>

<task type="auto">
  <name>T3: AntdThemeBridge 简化 —— 直连 xingranBrand，解除 themeStore/getTheme 依赖</name>
  <files>xingran-react-frontend/src/design-system/components/AntdThemeBridge.tsx</files>
  <action>
    1. 删除 import { useThemeStore } 与 import { getTheme }；删除 appliedThemeStyle/appliedThemeMode 两个订阅与 themeAntdPrimary 计算。
    2. 主色优先级链 customColors.primary → theme.antdPrimary → DEFAULT_ANTD_PRIMARY 整体删除（per D-01/D-03）；token.colorPrimary/colorInfo/colorLink 直接使用 xingranBrand.greenPrimary；删除 DEFAULT_ANTD_PRIMARY 常量与 customColors 订阅（useSettingsStore 仅保留 mode 与 density 两个选择器）。
    3. 保留：algorithm 切换（darkAlgorithm/defaultAlgorithm + density compactAlgorithm 叠加）、theme.components 8 组件覆盖、hashed: true、MessageContextInjector、locale zhCN。
    4. useMemo 依赖数组同步收窄为 [themeMode, density]；更新文件头 JSDoc（v1.22 Phase 65：单一品牌主题，优先级链已移除）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && grep -c "customColors\|getTheme\|useThemeStore" src/design-system/components/AntdThemeBridge.tsx; npm run type-check</automated>
  </verify>
  <done>AntdThemeBridge 中 customColors|getTheme|useThemeStore 计数为 0；colorPrimary === xingranBrand.greenPrimary；type-check 退出码 0。Commit: refactor(brand): simplify AntdThemeBridge to direct xingranBrand mapping per THEME-01/D-03</done>
</task>

<task type="auto">
  <name>T4: themeStore 重写为单 mode + ThemeProvider/ConfigProvider 同步简化</name>
  <files>xingran-react-frontend/src/store/themeStore.ts, xingran-react-frontend/src/design-system/components/ThemeProvider.tsx, xingran-react-frontend/src/components/ConfigProvider.tsx</files>
  <action>
    1. themeStore.ts 重写（per D-01）：state 仅 { mode: ColorMode }；actions 仅 syncFromSettings(config: { mode: ColorMode }) / setMode(mode) / applyToDOM()；applyToDOM 只做一件事 document.documentElement.setAttribute("data-color-mode", mode)。删除 appliedTheme/appliedMode/previewTheme/previewMode/savePreview/resetPreview/useTheme hook/ThemeType 与 themes 导入。保留 window "settings-changed" 监听（命名函数 + 先 remove 再 add 的 HMR 防护注释保留），handler 改为 syncFromSettings({ mode: preferences.theme.mode })。
    2. ThemeProvider.tsx 简化：订阅 themeStore 的 mode；effect 在 mode 变化时写 data-color-mode + 加 .theme-switching 动画类（300ms 后移除）；ThemeContext value 收窄为 { mode }（删除 theme 字段）；不再写 data-theme 属性（已验证全仓 CSS 无 [data-theme] 选择器）。
    3. ConfigProvider.tsx：删除 "save-theme-settings" 事件监听与 handleSaveTheme（preview 保存通道随 themeStore.preview 能力一并移除）；保留 "save-layout-settings" 监听与 syncTheme(preferences.theme) 调用（structural typing：ThemeConfiguration 在 T6 前仍带 style 字段，传给 { mode } 形参兼容）。
    4. useThemeContext 的消费方若读取 .theme 字段需同步改 .mode（grep useThemeContext 复核；预期仅 ThemeProvider 自身）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && grep -c "previewTheme\|appliedTheme\|ThemeType\|design-system/themes" src/store/themeStore.ts; grep -rn "save-theme-settings" src/ --include="*.tsx" --include="*.ts" | wc -l && npm run type-check && npm run build</automated>
  </verify>
  <done>themeStore 无 previewTheme/appliedTheme/ThemeType/themes 引用（计数 0）；save-theme-settings 全仓 0 命中；ThemeProvider 仅写 data-color-mode；type-check + build 绿。Commit: refactor(brand): rewrite themeStore to single color-mode store per THEME-01/THEME-02</done>
</task>

<task type="auto">
  <name>T5: settings 页主题入口移除 + 管理员默认主题页收窄 + defaultThemeApi 类型收窄</name>
  <files>xingran-react-frontend/src/pages/settings/index.tsx, xingran-react-frontend/src/pages/system/settings/default-theme.tsx, xingran-react-frontend/src/lib/defaultThemeApi.ts</files>
  <action>
    1. settings/index.tsx（per D-01，SC#5 验收清单）：删除 themePresets 与 applyPrimaryColor/applySidebarBackgroundColor import；删除「主题风格」Form.Item（name=["theme","style"] 的 Select）；删除「颜色自定义（可选）」Divider 及其下两个 ColorPicker Form.Item + 清除自定义颜色 Button；删除 primaryColor/sidebarColor state、handleClearCustomColors、handlePrimaryColorChange/Clear、handleSidebarColorChange/Clear 共 5 个 handler 与相关同步逻辑；handleSave 的 processedValues 不再拼 customColors；handleReset 改为仅从 getDefaultThemeConfig() 取 mode 覆盖表单 theme.mode（不再读 style/customColors、不再调 applyPrimaryColor/applySidebarBackgroundColor）。保留：明暗模式 Radio（THEME-02 入口）、布局类型/密度模式/默认折叠侧边栏/默认分页大小（THEME-03 边界）、ColorPicker 相关 antd import 清理（ColorPicker/Row/Col 若无其他使用则移除 import）。
    2. default-theme.tsx（隐藏消费方，per D-01）：删除 style 表单项（约 line 181 name="style" 的 Select）与 primaryColor/sidebarColor ColorPicker 字段；保存 payload 收窄为 { mode: values.mode }；「从当前用户配置同步」逻辑同样只取 preferences.theme.mode。页面与路由保留（菜单不破）。
    3. defaultThemeApi.ts：ThemeConfiguration 接口收窄为 { mode: "light" | "dark" }（删除 style/customColors/"auto"）；getDefaultThemeConfig/setDefaultThemeConfig 签名随类型收窄；grep syncUserThemeToDefault 消费方，若无消费则删除该函数，有消费则保留并标注 deprecated。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && grep -c "themePresets\|applyPrimaryColor\|applySidebarBackgroundColor\|customColors" src/pages/settings/index.tsx src/pages/system/settings/default-theme.tsx; grep -n "明暗模式" src/pages/settings/index.tsx && npm run type-check</automated>
  </verify>
  <done>两个 settings 页 themePresets/applyPrimaryColor/applySidebarBackgroundColor/customColors 计数为 0；明暗模式 Radio 保留；布局/密度 Select 保留；type-check 绿。Commit: feat(brand): remove theme style and custom color entries from settings pages per D-01/THEME-01</done>
</task>

<task type="auto">
  <name>T6: 物理清除 —— themes/ 目录 + types/theme.ts + utils/color.ts + 旧导出与类型收窄</name>
  <files>xingran-react-frontend/src/design-system/themes/ (20 files), xingran-react-frontend/src/types/theme.ts, xingran-react-frontend/src/types/config.ts, xingran-react-frontend/src/design-system/utils/color.ts, xingran-react-frontend/src/design-system/tokens/colors.ts, xingran-react-frontend/src/design-system/tokens/shadows.ts, xingran-react-frontend/src/design-system/tokens/spacing.ts, xingran-react-frontend/src/utils/three/colors.ts, xingran-react-frontend/src/services/configService.ts, xingran-react-frontend/src/store/settingsStore.ts</files>
  <action>
    1. 删除整个 src/design-system/themes/ 目录（6 主题子目录 × 3 文件 + index.ts + theme-styles.css 共 20 文件，per D-01/SC#1）。
    2. 删除 src/types/theme.ts（全仓仅 themes/* 与旧 themeStore 消费，均已清除）；删除 src/design-system/utils/color.ts（仅 ColorSwitcher/themes 消费）。
    3. types/config.ts：删除 ThemeStyle 类型与 isValidThemeStyle 函数；ThemeConfiguration 收窄为 { mode: ColorMode }（删除 style 与 customColors）；defaultThemeConfiguration → { mode: "light" }。BackendUserPreferences 的 themeStyle/customPrimaryColor/customSidebarColor 字段保留并标注 @Deprecated（后端契约不动，D-05）。
    4. configService.ts：migratePreferences 与 backendToFrontend 删除 themeStyle 校验/映射（theme 仅映射 mode，旧数据 style 字段静默丢弃）；toBackend 不再发送 themeStyle/customPrimaryColor/customSidebarColor。
    5. settingsStore.ts：确认无 customColors 引用残留（defaultUserPreferences 随 types/config.ts 自动收窄）；persist 旧 localStorage 数据含 style/customColors 多余键，运行时静默忽略（zustand persist 浅合并，无需版本迁移 —— 在 SUMMARY 记录此行为）。
    6. colors.ts：删除 baseColors/semanticColors/gradients/brandColors 四个旧导出（Phase 64 cleanup note，per D-02 单一真相源）；xingranBrand 保留为唯一导出常量。
    7. utils/three/colors.ts：删除 brandColors import，把当前 6 个值内联为本地常量（success/error/warning/info 值从删除前的 baseColors 读取原样 hex，primary=#2563eb、secondary=#a855f7），加注释「值冻结自 legacy brandColors，3D 接品牌令牌属 VIS-01 (v1.23+)」——3D 视觉零变化（D-05 业务页面零改动的等价落地）。
    8. shadows.ts / spacing.ts：先 grep -rn "tokens/shadows\|tokens/spacing" src/ 复核零命中后整文件删除（含 neumorphicShadows/glassShadows/directionalShadows 清理，覆盖 Phase 64 cleanup note 第 2/3 项）；typography.ts 保留（AntdThemeBridge 消费 fontFamily）。
    9. 全仓终扫：grep -rn "getTheme\|getMinimalTheme\|getGlassmorphismTheme\|getNeumorphismTheme\|getFlat2Theme\|getLuxuryQuietTheme\|getInkAmberTheme\|themePresets\|ThemeType\|ThemeStyle\|applyThemeVariables\|applyEffectVariables\|applyPrimaryColor\|applySidebarBackgroundColor\|design-system/themes" src/ 必须零命中。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && test ! -d src/design-system/themes && test ! -f src/types/theme.ts && grep -rn "getTheme\|ThemeType\|ThemeStyle\|themePresets\|applyPrimaryColor\|applySidebarBackgroundColor\|design-system/themes\|baseColors\|semanticColors\|brandColors" src/ --include="*.ts" --include="*.tsx" | grep -v "colors.test.ts" | wc -l && npm run type-check && npm run lint && npm run build</automated>
  </verify>
  <done>themes/ 目录不存在；types/theme.ts / utils/color.ts / shadows.ts / spacing.ts 不存在；旧主题 API grep 零命中；type-check + lint + build 三门绿。Commit: feat(brand): purge multi-theme system directories, types and legacy exports per THEME-01</done>
</task>

<task type="auto" tdd="true">
  <name>T7: THEME-02 验证 —— light/dark 双模式 + 暗色品牌深底推导 + WCAG AA 断言</name>
  <files>xingran-react-frontend/src/design-system/tokens/colors.test.ts</files>
  <behavior>
    - 暗色深底推导值存在性：index.css [data-color-mode="dark"] 段含 --sidebar-bg: #0a2418、--theme-bg-primary: #0f2e1b（Phase 64 已落地，本测试防回归）
    - 暗色关键对比度 ≥ 4.5:1（WCAG AA）：#F0ECE3(奶油文字) on #0A2418(深绿底)、#E0E0B0(浅黄强调) on #0A2418、#FFFFFF on #156031（dark 下主按钮不变）
    - 铜金提亮受控断言：dark 段 --theme-brand (#598E5E) 与 :root (#156031) 不同且均为品牌绿梯度成员
    - light 模式基线不断言变化：:root --theme-primary 仍为 #156031
  </behavior>
  <action>
    1. 在 colors.test.ts 追加 dark-mode describe 块：用 fs.readFileSync 读取 src/index.css 文本，切出 [data-color-mode="dark"] 段，断言上述变量值存在（统一小写比较）。
    2. 用文件内既有 contrastRatio 工具断言 dark 关键前景/背景对 ≥ 4.5（per THEME-02 + QA-01 既有断言保持全绿）。
    3. 先写测试运行确认红/绿状态符合预期（若 Phase 64 落地值已满足则直接绿，属验证型测试）；npm test 全量运行确认 20+ 旧断言无回归。
    4. 暗色推导值若与 brand-spec 有出入需要微调时，per D-02 必须同步更新断言并重跑对比度验证。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npx vitest run src/design-system/tokens/colors.test.ts</automated>
  </verify>
  <done>colors.test.ts 含 dark-mode 断言组且全量绿（含 Phase 64 既有 20 断言）；dark 段品牌推导值被测试锁定。Commit: test(brand): lock dark-mode brand derivation and WCAG AA pairs per THEME-02</done>
</task>

<task type="auto">
  <name>T8: THEME-03 边界验证 + 全量 QA 门 + bundle 体积对比</name>
  <files>xingran-react-frontend/src/design-system/tokens/colors.test.ts（仅如需修正）, .planning/phases/65-theme-system-consolidation/65-01-SUMMARY.md（由 output 步骤产出）</files>
  <action>
    1. THEME-03 边界证据：git diff 本 phase 范围确认 layoutStore.ts / DensitySwitcher.tsx / LayoutSwitcher.tsx 零改动（git log --oneline 本 phase 7 个 commit 均不触碰）；grep 确认 InnovativeLayout 仍渲染 LayoutSwitcher/DensitySwitcher、settings/index.tsx 仍含布局类型与密度模式 Select、types/config.ts 的 LayoutConfiguration/DensityMode 完整（compact/comfortable/spacious）。
    2. 全量门：npm run type-check、npm run lint、npx vitest run、npm run build 四门全绿。
    3. bundle 对比：记录 build 输出 vendor-react gzip kB，与 T1 基线及历史 774.96 kB 对比，预期下降（删除 6 主题 + ColorSwitcher 477 行 + themes 20 文件）；前后数值写入 SUMMARY。
    4. 13+1 消费方终扫清单逐一 grep（ConfigProvider/header/InnovativeLayout/TabBar/sidebar/AntdThemeBridge/ThemeProvider/main.tsx/settings/settingsStore/themeStore/settingsStore 的 theme 语义/default-theme.tsx/configService）确认无多主题残留引用；knip（npm run deadcode）可选运行，记录结果不阻断。
    5. ESLint 无新增 warning（重点 react/no-unused-prop-types 与 unused imports）。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npm run type-check && npm run lint && npx vitest run && npm run build && git diff --stat $(git log --oneline -8 | tail -1 | cut -d' ' -f1)~1 HEAD -- src/store/layoutStore.ts src/design-system/components/DensitySwitcher.tsx src/design-system/components/LayoutSwitcher.tsx | wc -l</automated>
  </verify>
  <done>四门全绿；layoutStore/DensitySwitcher/LayoutSwitcher 本 phase diff 计数为 0；bundle gzip 不高于 T1 基线（预期下降）；13+1 消费方 grep 终扫零残留。Commit: chore(brand): verify layout/density boundary and full QA gates per THEME-03</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>T9: 视觉冒烟确认 —— 设置页 + 明暗切换 + 三布局</name>
  <files>（无文件改动 —— 人工验证检查点）</files>
  <action>启动 dev server 并按 how-to-verify 四步引导用户完成视觉冒烟；用户确认后记录结果到 SUMMARY。</action>
  <what-built>多主题能力已整体移除；settings 页只剩明暗/布局/密度/数据设置；明暗切换走品牌单色相深底推导；3 套布局与密度切换保留。</what-built>
  <how-to-verify>
    1. cd xingran-react-frontend && npm run dev，登录后访问设置页：确认「主题风格」下拉与「颜色自定义」区块不存在；「明暗模式」Radio、「布局类型」「密度模式」Select 存在。
    2. 切换深色模式：页面整体转深绿底（#0A2418 系），侧边栏/卡片/表格无不可读文本、无 indigo/slate 冲突色；切回浅色恢复正常。
    3. 切换 3 套布局（classic/hybrid/innovative）与密度（compact/comfortable/spacious）：侧边栏 280px↔64px（innovative 80px）、表格行高随密度变化、InnovativeLayout 顶栏只剩 LayoutSwitcher/DensitySwitcher 两个切换器。
    4. 任意业务页（如 /system/user）DevTools :root 检查 --theme-brand: #156031、--theme-primary-500: #156031 存在。
  </how-to-verify>
  <verify><human-check>用户按 4 步清单完成设置页 / 明暗切换 / 三布局冒烟</human-check></verify>
  <done>用户输入 approved；或问题记录后回流修复</done>
  <resume-signal>输入 "approved" 或描述问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → settingsStore rehydrate | 旧持久化偏好含已删除的 style/customColors 字段，反序列化后进入 store |
| 后端 /system/settings/config/theme/default → defaultThemeApi | 后端返回的 JSON 可能含旧 style/customColors 字段 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Tampering | settingsStore persist rehydrate | mitigate | T6: ThemeConfiguration 收窄后多余键运行时静默忽略；configService.migratePreferences 对服务器数据只做 mode 白名单映射（light/dark 之外回退 "light"） |
| T-65-02 | Tampering | defaultThemeApi.getDefaultThemeConfig | mitigate | T5: 类型收窄为 { mode }，仅读取 mode 字段，style/customColors 不再进入前端状态（攻击面随 ColorPicker 移除而消除） |
| T-65-03 | Info Disclosure | — | accept | 纯视觉层重构，无 PII/凭据处理，无可披露面 |
| T-65-SC | Tampering | npm installs | accept | 本 phase 不新增任何依赖（纯删除 + 静态 CSS），无包安装任务，Package Legitimacy Gate 不适用 |
</threat_model>

<verification>

**每任务边界（常绿保证）：** 每个 commit 前 npm run type-check 退出码 0；T2/T4/T6/T8 附加 npm run build 退出码 0。

**终态验证（T8 执行）：**
1. `test ! -d src/design-system/themes` —— themes 目录不存在
2. `grep -rn "ThemeSwitcher|ColorSwitcher|getTheme\(|getMinimalTheme\(|getGlassmorphismTheme\(|getNeumorphismTheme\(|getFlat2Theme\(|getLuxuryQuietTheme\(|getInkAmberTheme\(|themePresets|ThemeType|ThemeStyle" src/` 零命中
3. `npx vitest run` 全绿（含 colors.test.ts dark-mode 断言组）
4. `npm run build` 退出码 0，vendor-react gzip ≤ T1 基线
5. `git diff` 确认 layoutStore.ts / DensitySwitcher.tsx / LayoutSwitcher.tsx 本 phase 零改动

</verification>

<success_criteria>

（ROADMAP Phase 65 SC#1-5 逐字映射）

1. `design-system/themes/` 下 6 套主题目录（`minimal` / `glassmorphism` / `neumorphism` / `flat2.0` / `luxury-quiet` / `ink-amber`）全部删除，`themes/index.ts` 与 `themes/theme-styles.css` 清理；`ThemeSwitcher.tsx` / `ColorSwitcher.tsx` 组件删除；`themeStore` 主题类型字段（`configuration.style` 等）与 `themes/` 引用清除，`settings/index.tsx` 主题入口移除；13 个消费方（`ConfigProvider` / `header` / `InnovativeLayout` / `TabBar` / `sidebar` / `ThemeProvider` / `main.tsx` / `settings/index.tsx` / `settingsStore` 等）`grep -rn "ThemeSwitcher|ColorSwitcher|getTheme\(|getMinimalTheme\(|getGlassmorphismTheme\(|getInkAmberTheme\("` 在 `src/` 下零命中，无死代码 / TS 错误 / unused import 警告 —— 由 T1-T6 交付
2. light / dark 双模式切换仍可在 settings 页操作 —— 暗色模式下 `--theme-bg-primary` / `--theme-text-primary` / `--sidebar-bg` 等关键变量有对应的深底推导（深绿底加深、铜金提亮受控、奶油转深灰纸感），`#FFFFFF` on 深绿底 / `#E0E0B0` on 深底等关键前景/背景对均满足 WCAG AA（对比度校验在 dark fixture 下全绿） —— 由 T5（入口）+ T7（断言）交付
3. `layoutStore` 的布局切换（`ClassicLayout` / `HybridLayout` / `InnovativeLayout`）与密度切换（`classic` / `comfortable`）完整保留 —— 切换后侧边栏宽 280px ↔ 64px、列表行高密度变化正确，工具栏 / 表单 / 表格不受影响；`settings/layout` 入口与 density switcher 控件正常工作 —— 由 T8（边界证据）+ T9（人工冒烟）交付
4. `npm run type-check` / `npm run build` / `npm run lint` / `npm run test` 全绿，移除 6 套主题后 vendor-react 打包体积较 Phase 64 baseline 不增（预期下降）；无新增 lint warning —— 由 T8 交付
5. 验收清单：在 settings 页内，**主题切换器 UI 不再存在**；**颜色自定义（主色 / 侧边栏色）入口不再存在**（D-01 不可逆）；**布局 / 密度 / 浅色 / 深色切换入口完整可用**；主页面视觉与登录页品牌一致（深绿 × 铜金 × 奶油） —— 由 T5 + T9 交付

</success_criteria>

<output>
Create `.planning/phases/65-theme-system-consolidation/65-01-SUMMARY.md` when done，必须包含：
- 9 个任务 × commit hash 对照表
- bundle 体积前后对比数值（T1 基线 vs T8 终值 vs 历史 774.96 kB）
- 13+1 消费方 grep 终扫结果
- THEME-03 边界证据（layoutStore 等 3 文件零改动 diff）
- settingsStore persist 旧数据兼容性说明（多余键静默忽略）
</output>
