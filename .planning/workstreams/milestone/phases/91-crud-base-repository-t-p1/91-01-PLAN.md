---
phase: 91-crud-base-repository-t-p1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/services/base/service.go
  - internal/services/base/service_test.go
  - internal/services/base/base_80_05_test.go
  - internal/services/operations/pagination_helper.go
autonomous: true
requirements: [CRUD-REUSE-01, CRUD-REUSE-07]
must_haves:
  truths:
    - "GORMRepository[T] 以 gorm scope 函数式提供六方法（Create/Update/Delete/GetByID/List/BatchDelete），service 直接组合 struct，无 Repository[T] interface"
    - "BatchDelete(ctx, nil) 与 BatchDelete(ctx, []string{}) 返回 nil（与 11 个 operations service 现状一致，P1 语义反转）"
    - "List 返回的 PageResult.List 是值切片 []T（crud_services_test.go:179 类型断言依赖，P4）"
    - "GetByID 支持变长 scopes：空 scope → WHERE id = ?；非空 scope → <TableName()>.id = ?（Tabler 断言，A4）"
    - "base.SortScope（B 型排他默认）与 base.SortScopeWithTail（A 型复合尾随）内部复用 ApplySort/ResolveSort 白名单机制，不重写"
    - "operations.PageResult 成为 base.PageResult 的 type alias，operations 包 76 处测试引用零改动编译通过（F4）"
  artifacts:
    - path: internal/services/base/service.go
      provides: "Scope/PageParams/PageResult/GORMRepository[T] scope 版六方法 + SortScope/SortScopeWithTail"
      contains: "func (r *GORMRepository[T]) List(ctx context.Context, page PageParams, scopes ...Scope)"
    - path: internal/services/base/service_test.go
      provides: "泛型契约锁值 TestBase91_* 系列（六契约点）"
      contains: "TestBase91"
    - path: internal/services/base/base_80_05_test.go
      provides: "Query DSL 用例改写为 scope 形态 + BatchDelete 空切片期望反转"
    - path: internal/services/operations/pagination_helper.go
      provides: "type PageResult = base.PageResult alias（D-03 单一定义的本质达成）"
      contains: "type PageResult = base.PageResult"
  key_links:
    - from: internal/services/base/service.go (SortScope/SortScopeWithTail)
      to: internal/services/base/list_request.go (ResolveSort/ApplySort)
      via: 内部复用白名单解析，不重写排序逻辑
      pattern: "ApplySort\\(db, req, allowed\\)"
    - from: internal/services/operations/pagination_helper.go
      to: internal/services/base/service.go
      via: type alias（两种写法同一类型）
      pattern: "type PageResult = base\\.PageResult"
---

<objective>
把 `internal/services/base` 的 `GORMRepository[T]` 从"已测试的 dead code"改造为本 phase 全部 11 个 operations CRUD service 可复用的泛型仓储：List 改为 gorm scope 函数式（D-01）、删除 Repository[T] interface 与 Query/WhereCondition DSL（D-02/D-01）、BatchDelete 空 ids 语义反转为 nil（P1）、新增 A/B 双型排序 scope helper、GetByID 支持带表限定的变长 scopes，并用 `base/service_test.go` 泛型契约测试把 RESEARCH spike 已验证的 GORM 链语义（Count 与 Joins+Select 共存、ORDER BY 剥离恢复、值切片、软删过滤）锁死。同时把 `operations.PageResult` 合并为 `base.PageResult` 的 type alias（D-03/F4）。

Purpose: 91-02/03/04 全部 11 个 service 迁移的公共底座；契约测试是后续三个 plan "行为零变更" 的基准防 screwed。
Output: 改造后的 base 包（零 DSL、scope 化六方法、双型排序 helper）+ 契约测试 + PageResult 单一定义。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/91-crud-base-repository-t-p1/91-CONTEXT.md
@.planning/workstreams/milestone/phases/91-crud-base-repository-t-p1/91-RESEARCH.md
@.planning/workstreams/milestone/phases/91-crud-base-repository-t-p1/91-PATTERNS.md
@CLAUDE.md

@internal/services/base/service.go
@internal/services/base/list_request.go
@internal/services/base/base_80_05_test.go
@internal/services/operations/pagination_helper.go
@internal/services/operations/door_service.go
</context>

<interfaces>
<!-- 本 plan 创建的契约，91-02/03/04 三个下游 plan 直接消费。签名经 RESEARCH spike 实证定稿（D-01/D-02 授权 planner 定稿）。 -->

改造后 internal/services/base/service.go 必须导出：

```go
// Scope 与 gorm 官方 scope 函数签名一致
type Scope = func(*gorm.DB) *gorm.DB

// PageParams 已归一化的分页参数（service 层负责 clamp，repo 直信入参 — P5 三种 clamp 语义并存）
type PageParams struct {
    Current  int
    PageSize int
}

// PageResult 保持现有定义逐字不动（base/service.go:38-43），成为全项目唯一定义
type PageResult struct {
    List     interface{} `json:"list"`
    Total    int64       `json:"total"`
    Current  int         `json:"current"`
    PageSize int         `json:"pageSize"`
}

func NewGORMRepository[T any](db *gorm.DB) *GORMRepository[T]
func (r *GORMRepository[T]) Create(ctx context.Context, entity *T) error
func (r *GORMRepository[T]) Update(ctx context.Context, entity *T) error
func (r *GORMRepository[T]) Delete(ctx context.Context, id string) error
func (r *GORMRepository[T]) GetByID(ctx context.Context, id string, scopes ...Scope) (*T, error)
func (r *GORMRepository[T]) List(ctx context.Context, page PageParams, scopes ...Scope) (*PageResult, error)
func (r *GORMRepository[T]) BatchDelete(ctx context.Context, ids []string) error // 空 ids → nil

// B 型（排他默认）：白名单命中 → ORDER BY <col> <dir>；OrderByColumn 为空 → ORDER BY defaultOrder；
// OrderByColumn 非法（不在白名单）→ warn 日志 + 无显式排序（与现有 ApplySort + if OrderByColumn == "" 组合语义逐字一致）
func SortScope(req BaseListRequest, allowed map[string]string, defaultOrder string) Scope

// A 型（复合尾随，door/wall 专用）：白名单命中 → ORDER BY <col> <dir>, <tail>；否则仅 ORDER BY <tail>
func SortScopeWithTail(req BaseListRequest, allowed map[string]string, tail string) Scope
```

删除（D-01/D-02，零生产消费者）：`Repository[T]` interface、`Query` struct、`WhereCondition` struct。
原样保留：`WrapError`/`IsNotFound`/`IsDuplicate`（base_80_05_test.go:181-208 锁定）。
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 重写 base/service.go — scope 函数式六方法 + 双型排序 helper（D-01/D-02）</name>
  <files>internal/services/base/service.go</files>
  <read_first>
    - internal/services/base/service.go（改造对象：现有 Repository[T] interface :11-19、Query :21-28、WhereCondition :30-35、List 的 operator switch 管道 :81-135、BatchDelete :138-143、PageResult :38-43、WrapError/IsNotFound/IsDuplicate :145-161）
    - internal/services/base/list_request.go（ResolveSort/ApplySort — SortScope helper 的底座，101 处引用的真资产，本期零改动）
    - internal/services/operations/door_service.go:69-148（A 型排序语义的现状参照：ApplySort + 尾随 created_at DESC；B 型语义参照 workstation_service.go:289-293）
    - 91-RESEARCH.md「GORMRepository 目标设计（spike 实证版）」节（List 内部管道与 7 项已验证行为前提）
  </read_first>
  <action>
    删除 Repository[T] interface、Query struct、WhereCondition struct（三者零生产消费者，D-01/D-02）。保留 PageResult struct、Create/Update/Delete、NewGORMRepository、WrapError/IsNotFound/IsDuplicate 逐字不动。

    新增 type Scope（= func(*gorm.DB) *gorm.DB 的 type alias）与 PageParams struct（字段 Current/PageSize int）。

    改写 GetByID 签名为变长 scopes：内部以 r.db.WithContext(ctx).Model(new(T)) 起链（注意：现状是 Where+First 无 Model 起链，改后经 Model 起链软删除语义不变且更完备），依序应用非 nil scopes；id 过滤规则——scopes 为空时 Where("id = ?", id)，scopes 非空时先对 new(T) 做 Tabler 断言（interface{ TableName() string }），断言成功用 Where(fmt.Sprintf("%s.id = ?", tableName), id) 表限定（避免 join 场景 id 列 ambiguous），断言失败退回 Where("id = ?", id)；最后 First 到 *T 并保持现有错误透传。

    重写 List 为 (ctx, page PageParams, scopes ...Scope)：Model(new(T)) 起链 → 依序应用非 nil scopes → var total int64 + Count（GORM 在 Count 时自动临时替换非 count Select 为 count(*) 并恢复，剥离并恢复 ORDER BY——spike 已验证）→ var list []T（值切片，P4）→ Offset((page.Current-1)*page.PageSize).Limit(page.PageSize).Find(&list) → 返回 &PageResult{List: list, Total: total, Current: page.Current, PageSize: page.PageSize}。设计红线：repo 内不 clamp 分页、不加默认排序、不用 fmt.Sprintf 拼用户输入进条件（ORDER BY 只能来自白名单 map value）。

    BatchDelete：把空 ids 分支从 return apperrors.BadRequest("ids不能为空") 改为 return nil（P1 语义反转：11 个 operations service 全部返回 nil，workstation_floor_code_77_03_test.go:173 锁定 BatchDelete(ctx, nil) 为 NoError）；非空分支逐字保留。若 apperrors import 因此不再被 IsNotFound/IsDuplicate 之外的地方使用，保留 import（IsNotFound/IsDuplicate 仍在用）。

    新增 SortScope(req BaseListRequest, allowed map[string]string, defaultOrder string) Scope：返回闭包内部先 db = ApplySort(db, req, allowed)（复用白名单解析 + 非法 key warn 日志），然后 if req.OrderByColumn == "" { db = db.Order(defaultOrder) }，返回 db——三态语义（合法 → 用户排序；空 → 默认序；非法 → warn + 无显式排序）与 operations 现有 9 个 B 型服务的 "ApplySort + if OrderByColumn == \"\"" 组合逐字一致。
    新增 SortScopeWithTail(req BaseListRequest, allowed map[string]string, tail string) Scope：闭包内部 db = ApplySort(db, req, allowed) 后无条件 db = db.Order(tail)——与 door_service.go 现状（ApplySort 后 fetchRecords 恒追加 created_at DESC）一致。两个 helper 的 ORDER BY 字符串拼接用 fmt.Sprintf("%s %s", col, direction) 形式（col/direction 来自 ResolveSort，均为白名单值，非外部输入）。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/guoguo && go build ./... && grep -c "type Repository\[" internal/services/base/service.go; grep -c "type Query struct\|type WhereCondition struct" internal/services/base/service.go; grep -c "func (r \*GORMRepository\[T\]) List(ctx context.Context, page PageParams, scopes ...Scope)" internal/services/base/service.go</automated>
  </verify>
  <acceptance_criteria>
    - internal/services/base/service.go 中 `type Repository[` 计数为 0（D-02 interface 删除）
    - internal/services/base/service.go 中 `type Query struct` 与 `type WhereCondition struct` 计数为 0（D-01 DSL 删除）
    - internal/services/base/service.go 包含签名 `func (r *GORMRepository[T]) List(ctx context.Context, page PageParams, scopes ...Scope) (*PageResult, error)`
    - internal/services/base/service.go 包含签名 `func (r *GORMRepository[T]) GetByID(ctx context.Context, id string, scopes ...Scope) (*T, error)`
    - internal/services/base/service.go 包含 `func SortScope(req BaseListRequest, allowed map[string]string, defaultOrder string) Scope` 与 `func SortScopeWithTail(req BaseListRequest, allowed map[string]string, tail string) Scope`
    - BatchDelete 中不再出现 apperrors.BadRequest（空 ids 返回 nil）
    - go build ./... 退出码 0
  </acceptance_criteria>
  <done>base 包只剩 scope 函数式 GORMRepository[T] struct（无 interface、无 DSL），六方法 + 双型排序 helper 全部就位，全库编译通过</done>
</task>

<task type="auto">
  <name>Task 2: operations.PageResult 合并为 base.PageResult 的 type alias（D-03/F4）</name>
  <files>internal/services/operations/pagination_helper.go</files>
  <read_first>
    - internal/services/operations/pagination_helper.go（现有 PageResult struct :16-22 与 base 定义字段/JSON tag 完全一致；本地 PaginationParams struct :11-14 是另一个独立类型，勿与 requests.PaginationParams 混淆）
    - internal/services/base/service.go（Task 1 改造后的 PageResult 定义）
  </read_first>
  <action>
    把 pagination_helper.go 中的 PageResult struct 定义（:16-22，含 List/Total/Current/PageSize 四字段）替换为 type alias：type PageResult = base.PageResult。alias 使 opsServices.PageResult 与 base.PageResult 成为同一类型：operations 包 12 个测试文件共 76 处 PageResult 引用、以及两个非迁移目标生产文件 floor_cache_impl.go 与 location_alias_service.go 全部零改动编译（F4——不做硬删除，硬删除会强制触碰 Phase 92 文件并与 phase boundary 冲突）。本地 PaginationParams struct（无 JSON tag 的纯内部分页载体）原样保留；extractPagination/extractSortRequest/extractIntParam/extractStringParam/clampPageSize/calculateOffset 本 task 全部不动（F1：3 个 map 服务与 SearchXxxOptions/Statistics 仍需要；修剪属 91-04）。文件头 import 已含 base 包，无需新增。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/guoguo && go build ./... && go vet ./internal/services/operations/ && grep -c "type PageResult = base.PageResult" internal/services/operations/pagination_helper.go</automated>
  </verify>
  <acceptance_criteria>
    - internal/services/operations/pagination_helper.go 恰好包含 1 处 `type PageResult = base.PageResult`
    - internal/services/operations/pagination_helper.go 不再包含 PageResult 的 struct 字段定义（List interface{} / Total int64 等字段声明行）
    - extractPagination/extractSortRequest/extractIntParam/extractStringParam/clampPageSize/calculateOffset 六个 helper 函数在本 task 后全部仍存在
    - go build ./... 退出码 0；go vet ./internal/services/operations/ 退出码 0
  </acceptance_criteria>
  <done>PageResult 唯一 struct 定义在 base 包，operations 侧为 alias，全库（含 76 处测试引用）零改动编译通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 新建 base/service_test.go 泛型契约锁值 + 适配 base_80_05_test.go（CRUD-REUSE-07）</name>
  <files>internal/services/base/service_test.go, internal/services/base/base_80_05_test.go</files>
  <read_first>
    - internal/services/base/base_80_05_test.go（现有 sqlite 脚手架 newBasRepo8005/seedBasRow8005 :27-55 保留复用；TestBas8005_List_Paged :88-155 需从 Query DSL 改写；TestBas8005_BatchDelete :158-178 空切片期望 :174-177 需反转；:181-208 错误 helper 测试不动）
    - pkg/constants/pagination_test.go:60-97（AST Stability + Count 双锁结构参考，本 task 的命名/组织风格模板）
    - 91-PATTERNS.md「internal/services/base/service_test.go（新建 test）」节（必须覆盖的 6 个契约点）
  </read_first>
  <behavior>
    契约 1（P4）: 空 scopes List 返回全表分页，page.List 经类型断言为 []basRepoRow8005 值切片，Total/Current/PageSize 回填正确
    契约 2: Joins+Select scope 下 Count 正确（自关联 join 不增殖行）且 Find 保留 Select 列——用 seed 两行 + LEFT JOIN 自表聚合列断言
    契约 3: 顺序两个 Order 的 scope 产生复合排序（同列值两行按第二排序键稳定有序）
    契约 4（P1）: repo.BatchDelete(ctx, nil) 与 repo.BatchDelete(ctx, []string{}) 均返回 NoError；非空 ids 软删后 List Total 减少、软删行不再出现在 Find 结果
    契约 5（A4）: GetByID 无 scope 命中行；带 join scope 时 id 过滤为表限定——行为证明：join 一张同含 id 列的表，无表限定会因列名 ambiguous 失败、表限定成功（若 sqlite 对该场景不报 ambiguous，改用 DryRun Session 捕获生成 SQL 断言含 "<table>.id = ?"）
    契约 6: SortScope 三态（白名单命中 → ORDER BY <col> <dir>；OrderByColumn 空 → defaultOrder；非法 key → 无显式排序）与 SortScopeWithTail 尾随语义（命中 → 复合、未命中 → 仅 tail），经 DryRun 或行为断言验证
    适配契约 7: base_80_05_test.go TestBas8005_BatchDelete 空切片期望由 require.Error + CodeParamError 反转为 require.NoError
  </behavior>
  <action>
    新建 internal/services/base/service_test.go（package base，与 base_80_05_test.go 同包），复用同文件包内的 newBasRepo8005 脚手架（勿重复定义行模型；如需额外表做 join/排序契约，新定义独立行模型 + TableName + 独立 sqlite 库脚手架，遵循 v1.27 规约：t.TempDir 文件库 + AutoMigrate + t.Cleanup 关闭 + 零 sleep + 零 t.Parallel）。按 <behavior> 七个契约写 TestBase91_ 前缀测试函数（如 TestBase91_List_ValueSlice、TestBase91_List_JoinsSelectCount、TestBase91_CompositeSort、TestBase91_BatchDelete_NilIDs、TestBase91_GetByID_TableQualified、TestBase91_SortScope_TwoModes）。排序契约的 SQL 断言优先用 gorm DryRun Session（db.Session(&gorm.Session{DryRun: true}) 构造 repo 后调用方法，经 stmt.SQL.String() 或回调捕获断言 ORDER BY / WHERE 内容）；DryRun 不适用于 Count 计数路径时用真库行为断言。

    适配 internal/services/base/base_80_05_test.go：TestBas8005_List_Paged 中所有 &Query{...} / WhereCondition 构造改写为等价 scope + PageParams 调用（= 操作符用例改 Where scope、OrderBy 改 Order scope、Offset/Limit 改 PageParams、Preloads 用例若依赖关联模型则删除并代之以 scope 组合用例）；TestBas8005_BatchDelete 的空切片分支期望反转为 require.NoError（r.repo.BatchDelete(ctx, []string{})）；其余（newBasRepo8005/seedBasRow8005 脚手架、WrapError/IsNotFound/IsDuplicate 测试）逐字保留。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/guoguo && go test ./internal/services/base/ -run "TestBase91|TestBas8005" -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - internal/services/base/service_test.go 存在且含至少 6 个 TestBase91_ 前缀测试函数
    - go test ./internal/services/base/ -run "TestBase91" 全部 PASS
    - base_80_05_test.go 中不再出现 &Query{ 或 WhereCondition{（DSL 引用清零）
    - TestBas8005_BatchDelete 对空切片输入断言 require.NoError（旧期望 require.Error 已移除）
    - go test ./internal/services/base/ 全部 PASS（含既有 list_request_test.go）
  </acceptance_criteria>
  <done>RESEARCH spike 验证的全部 GORM 链语义固化为可重跑的契约测试；旧 DSL 测试完成 scope 形态适配且 BatchDelete 语义反转锁值</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service 层 → base.GORMRepository | 查询条件（scope）从此处进入 SQL 构造；scope 内容若被动态拼接用户输入即成注入面 |
| handler JSON → (后续 plan) → repo 分页参数 | PageParams 直信入参，越界值由 service 层既有 clamp 语义约束（repo 有意不 clamp，P5） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-01-01 | Tampering | BatchDelete 空 ids 语义反转 | mitigate | 反转方向与全部 11 个消费者现状一致（返回 nil），且有 workstation_floor_code_77_03_test.go:173 既有锁定 + 新契约 4 双向锁值；无调用方依赖旧 BadRequest 行为（grep 零生产消费者） |
| T-91-01-02 | Tampering | SortScope/SortScopeWithTail 的 ORDER BY 构造 | mitigate | 内部复用 ResolveSort/ApplySort：col 只取白名单 map value、direction 只取字面量 ASC/DESC，用户输入只能匹配 map key；契约 6 锁定三态语义 |
| T-91-01-03 | Information Disclosure | GetByID 表限定（Tabler 断言） | mitigate | 非 join 场景保持 WHERE id = ? 逐字等价；join 场景表限定防止跨表 id 碰撞（契约 5 行为锁）；软删过滤经 Model(new(T)) 起链对 Count/Find 一致生效（较现状更完备） |
| T-91-01-04 | DoS | repo 不 clamp 分页参数 | accept | 有意设计（P5）：三种既有 clamp 语义（10..100 / 10..10000 / 无 clamp）并存于 service 层，repo 侧统一 clamp 会破坏零行为变更底线；每个 service 迁移时保留原 clamp 路径 |
| T-91-01-SC | Tampering | npm/pip/cargo installs | N/A | 本 phase 零外部包安装（RESEARCH Package Legitimacy Audit：N/A），无供应链面 |
</threat_model>

<verification>
- go build ./... 退出码 0（CLAUDE.md 强制，每 task 收尾执行）
- go test ./internal/services/base/... 全绿（契约 + 适配 + 既有 list_request_test）
- go test ./internal/services/operations/... 全绿（pagination_helper alias 改动零回归）
- grep 确认 base 包无 Repository[T]/Query/WhereCondition 残留
- 既有 AST 锁值防线保持绿：go test ./internal/models/ ./internal/utils/operlog/ ./pkg/constants/（status 常量 / operlog / pagination 锁值）
</verification>

<success_criteria>
1. base.GORMRepository[T] 为 scope 函数式纯 struct（D-01/D-02 达成），可供 91-02/03/04 组合消费
2. BatchDelete 空 ids → nil（P1 语义反转，测试锁定）
3. PageResult 全项目单一定义在 base（D-03 达成，operations alias 兼容 76 处引用）
4. 泛型契约测试 TestBase91_* 六契约全绿，防后续 plan 语义漂移
5. go build ./... + go test ./internal/services/... 0 失败
</success_criteria>

<output>
完成本 plan 后无需单独 SUMMARY（phase SUMMARY 由 91-04 统一产出），但按 execute-plan 工作流做原子 commit：refactor(91-01): rebuild base.GORMRepository as scope-based generic repository
</output>
