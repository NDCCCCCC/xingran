---
phase: 91-crud-base-repository-t-p1
plan: 04
type: execute
wave: 4
depends_on: ["91-01", "91-02", "91-03"]
files_modified:
  - internal/services/operations/door_service.go
  - internal/services/operations/wall_service.go
  - internal/services/operations/server_room_service.go
  - internal/services/operations/dedicated_line_service.go
  - internal/services/operations/floor_plan_text_service.go
  - internal/services/operations/room_device_service.go
  - internal/services/operations/infopoint_service.go
  - internal/services/operations/floor_plan_text_crud_test.go
  - internal/services/operations/building_service_typesafe.go
  - internal/api/v1/operations/building_handler_typesafe.go
  - internal/api/v1/operations/building_handler_typesafe_test.go
  - internal/services/operations/asset_building_roomdevice_test.go
  - internal/services/operations/pagination_helper.go
  - internal/services/operations/pagination_helper_test.go
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CRUD-REUSE-06, CRUD-REUSE-07, CRUD-REUSE-08]
user_setup: []
must_haves:
  truths:
    - "11 个 services 全部复用 base.GORMRepository[T]：任何迁移目标文件中不再存在直调 GORM 的 CRUD 六方法体与 countRecords/fetchRecords/buildListQueryFromRequest 同类私有管道（D-07 定性底线）"
    - "door/wall 使用 base.SortScopeWithTail 保持复合尾随排序（ORDER BY <user>, created_at DESC），P3 tiebreaker 语义有测试锁定"
    - "room_device 的 scope 顺序保持 Joins 先于引用 joined 表的 filter（P8 顺序敏感）"
    - "building_service_typesafe.go + building_handler_typesafe.go + 对应测试块全部删除（零生产消费者死代码，LOC ≥800 达标来源；本项已经 orchestrator 拍板纳入，无需 checkpoint）"
    - "pagination_helper.go 仅 calculateOffset 达到零消费者并删除；extractXxxParam/clampPageSize/extractPagination/extractSortRequest 全部保留（F1 口径：3 个 map 服务 + SearchXxxOptions + Statistics 仍需）"
    - "LOC 净减 ≥800（git numstat 审计写入 SUMMARY，D-07）"
    - "go build ./... 0 错误 + go test ./... 0 失败 + handler 层测试全绿（e2e smoke 测试口径）"
  artifacts:
    - path: internal/services/operations/door_service.go
      provides: "A 型排序 repo 化样本（filterScope + SortScopeWithTail）"
      contains: "base.SortScopeWithTail"
    - path: internal/services/operations/wall_service.go
      provides: "A 型 + Order(\"\") 死代码删除"
    - path: internal/services/operations/server_room_service.go
      provides: "B 型 + Select/Joins 前置 + orgId 早退保留 service"
      contains: "base.GORMRepository"
    - path: internal/services/operations/dedicated_line_service.go
      provides: "B 型 + 11 filter 平移 + populateRoomNames 保留"
    - path: internal/services/operations/floor_plan_text_service.go
      provides: "B 型 + Table→repo 等价委托"
    - path: internal/services/operations/room_device_service.go
      provides: "JOIN typed + scope 顺序敏感样本"
    - path: internal/services/operations/infopoint_service.go
      provides: "JOIN typed + WorkID/PointType 兼容逻辑保留"
    - path: internal/services/operations/floor_plan_text_crud_test.go
      provides: "fpt 六方法 CRUD 冒烟（Wave 0 缺口补齐）"
      contains: "func TestFloorPlanText91"
    - path: .planning/workstreams/milestone/phases/91-crud-base-repository-t-p1/91-04-SUMMARY.md
      provides: "LOC 审计表（before/after 逐文件）+ 11 服务迁移清单 + 决策记录"
    - path: .planning/REQUIREMENTS.md
      provides: "CRUD-REUSE-01/06/08 措辞按 D-04/D-06/D-07 同步（scope 化口径 / 剩 7 个 / 净减 ≥800 混合标准）"
      contains: "净减 ≥800"
  key_links:
    - from: internal/services/operations/{7 个 service}
      to: internal/services/base/service.go
      via: "s.repo.List(ctx, base.PageParams{...}, scopes...)"
      pattern: "s\\.repo\\.(List|Create|Update|Delete|GetByID|BatchDelete)"
    - from: 91-04-SUMMARY.md
      to: git history
      via: "git diff --numstat <phase-start>..HEAD 审计"
      pattern: "numstat"
---

<objective>
收尾 plan：迁移剩余 7 个同构服务（door / wall / server_room / dedicated_line / floor_plan_text / room_device / infopoint，其中 door+wall 为 A 型复合排序样本）；删除零生产消费者死代码 building_service_typesafe.go 一族（LOC ≥800 达标来源，orchestrator 已拍板纳入）；修剪 pagination_helper 中达到零消费者的 calculateOffset；执行 LOC 审计并产出 phase SUMMARY；全量回归收口。

Purpose: 完成 D-06 的 11 服务全覆盖与 D-07 的混合验收标准（模板清零 + LOC ≥800），phase 收口。
Output: 7 服务 repo 化 + typesafe 死文件清理 + REQUIREMENTS.md § CRUD-REUSE 措辞同步（D-04/D-06/D-07，沿 Phase 90 commit 3a2efe5 先例）+ 91-04-SUMMARY.md。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/91-crud-base-repository-t-p1/91-CONTEXT.md
@.planning/workstreams/milestone/phases/91-crud-base-repository-t-p1/91-RESEARCH.md
@.planning/workstreams/milestone/phases/91-crud-base-repository-t-p1/91-PATTERNS.md
@internal/services/base/service.go
@internal/services/operations/door_service.go
@internal/services/operations/wall_service.go
@internal/services/operations/server_room_service.go
@internal/services/operations/dedicated_line_service.go
@internal/services/operations/floor_plan_text_service.go
@internal/services/operations/room_device_service.go
@internal/services/operations/infopoint_service.go
@internal/services/operations/crud_services_test.go
@internal/services/operations/building_service_typesafe.go
@internal/api/v1/operations/building_handler_typesafe.go
@internal/services/operations/pagination_helper.go
</context>

<interfaces>
<!-- 91-01 产出的 base 契约（消费方视角，完整签名见 91-01-PLAN.md） -->
base.NewGORMRepository[T](db) / Create / Update / Delete / GetByID(ctx, id, scopes ...) / List(ctx, page PageParams, scopes ...) / BatchDelete(ctx, ids)（空 ids → nil）
base.SortScope(req, allowed, defaultOrder) — B 型排他默认（本 plan 6 个服务用）
base.SortScopeWithTail(req, allowed, tail) — A 型复合尾随（door/wall 用）
base.Scope = func(*gorm.DB) *gorm.DB; base.PageParams{Current, PageSize}

<!-- 7 服务迁移速查（91-PATTERNS 迁移分型表） -->
door/wall:            typed, plain GetByID, A 型（尾随 created_at DESC），wall 的 Order("") 死代码删除
floor_plan_text:      typed, plain, B 型（默认序 read_first 确认）, Delete/BatchDelete 现用 .Table(...) 形态——repo 委托行为等价
dedicated_line:       typed, plain, B 型, 11 个 filter 平移, populateRoomNames 回填留 service
server_room:          typed, plain, B 型, Select+Joins 现状就在 Count 前, orgId 空命中早退空 PageResult 留 service
room_device:          typed, JOIN(server_rooms), B 型, P8——Joins 必须先于 orgId filter 入 scopes, validateRoom 留 service
infopoint:            typed, JOIN(workstation+floors+buildings), B 型默认 ops_info_points.created_at DESC, WorkID/PointType 旧字段兼容 + populateRedundantFields 留 service
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: door + wall 迁移（A 型复合尾随首个实战）</name>
  <files>internal/services/operations/door_service.go, internal/services/operations/wall_service.go</files>
  <read_first>
    - internal/services/operations/door_service.go（全文 148 行——本 phase 的最小同构模板：六方法 :42-103、validateDoorRelations :106-116、buildListQueryFromRequest :119-130、countRecords :133-137、fetchRecords :140-148 含尾随 Order、doorAllowedSortFields :36-40）
    - internal/services/operations/wall_service.go（全文 136 行——同构；确认其 buildListQueryFromRequest 条件集与 wallAllowedSortFields）
    - internal/services/operations/crud_services_test.go（TestWall/TestDoor 用例 = 行为零变更验证器）
    - 91-PATTERNS.md「11 个迁移 service」节（door 迁移前后对照示例）
  </read_first>
  <action>
    door_service.go（模板落地）：doorService struct 加 repo *base.GORMRepository[operationsmodels.Door]，NewDoorService 初始化；db 保留（validateDoorRelations 的 Validator 构造仍需）。Create/Update：validateDoorRelations 前置逐字保留，落库委托 s.repo.Create/Update。Delete/GetByID/BatchDelete：直接委托 repo（GetByID 零 scope——plain First 等价；BatchDelete 的 len(ids)==0 早退可删，repo 已 nil 语义）。List：current, pageSize := req.GetPagination()；return s.repo.List(ctx, base.PageParams{Current: current, PageSize: pageSize}, s.filterScope(req), base.SortScopeWithTail(req.BaseListRequest, doorAllowedSortFields, "created_at DESC"))。filterScope 把 :122-127 两条件逐字平移进闭包（floor_id = ? / type = ?）。删除 buildListQueryFromRequest、countRecords、fetchRecords 三个私有 helper；删除 List 中 if req.OrderByColumn != "" { query = query.Order("") } 死代码块（Order("") 是 GORM no-op，RESEARCH spike 实证 #4——尾随排序由 SortScopeWithTail 无条件承接，语义严格等价）。A 型语义注释：用户排序 → ORDER BY <col> <dir>, created_at DESC；无/非法排序 → 仅 created_at DESC（与现状 fetchRecords 恒追加逐字等价）。

    wall_service.go：同 door 完全同型（repo 泛型参数 operationsmodels.Wall、wallAllowedSortFields、filterScope 平移其自身条件集、SortScopeWithTail 尾随 "created_at DESC"）；确认并删除其 List 中同型 Order("") 死代码块。两文件的接口签名（DoorService/WallService）零变更。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/guoguo && go build ./... && go test ./internal/services/operations/ -run "TestDoor|TestWall" -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - door_service.go / wall_service.go 均含 s.repo. 委托与 base.SortScopeWithTail 调用（尾随参数 "created_at DESC"）
    - 两文件中 buildListQueryFromRequest、countRecords、fetchRecords 函数定义计数为 0
    - 两文件中 Order("") 死代码计数为 0（grep 'Order("")'）
    - 两服务 interface 声明块零变更
    - go test ./internal/services/operations/ -run "TestDoor|TestWall" 全部 PASS
  </acceptance_criteria>
  <done>A 型复合排序在 repo 形态下落地，最小同构样本完成迁移，模板可供 Task 2/3 复制</done>
</task>

<task type="auto">
  <name>Task 2: server_room + dedicated_line + floor_plan_text 迁移 + fpt 冒烟测试</name>
  <files>internal/services/operations/server_room_service.go, internal/services/operations/dedicated_line_service.go, internal/services/operations/floor_plan_text_service.go, internal/services/operations/floor_plan_text_crud_test.go</files>
  <read_first>
    - internal/services/operations/server_room_service.go（List 现状：Select+Joins 在 Count 之前、orgId 空命中早退、validateFloor 前置；serverRoomAllowedSortFields 与默认序）
    - internal/services/operations/dedicated_line_service.go（List 六方法管道 :33-40 接口、11 个 filter 的实际位置——注意 :208-223 的 extractStringParam 调用属非 List 方法（Statistics/导出路径）则不动；populateRoomNames；dedicatedLineAllowedSortFields 与默认序）
    - internal/services/operations/floor_plan_text_service.go（全文 116 行：六方法、Delete/BatchDelete 的 .Table(...) 形态、B 型默认序、validator.ValidateFloor 前置）
    - internal/services/operations/crud_services_test.go（TestServerRoom/TestDedicatedLine 用例）
    - internal/api/v1/operations/requests/ 下三个服务的 XxxListRequest（filter 字段名）
  </read_first>
  <action>
    三文件同型迁移（套 Task 1 已定型的模板，全部 B 型 base.SortScope）：
    struct 各加对应泛型 repo 字段并初始化；db 保留给回填/校验 helper。

    server_room：List 的 scopes 按原代码顺序组合——joinSelectScope（Select+Joins 常量逐字平移；现状本就在 Count 前，全 scope 前置即严格等价）→ filterScope（orgId 空命中早退分支保留在 List 函数体内、在构造 scopes 之前，早退返回空 PageResult{List: []operations.OpsServerRoom{}, Total: 0, ...} 的现状值逐字）→ base.SortScope(sortReq, allowed, 默认序以 read_first 为准)。Create/Update 的 validateFloor 前置保留后委托 repo；Delete/GetByID/BatchDelete 委托 repo。

    dedicated_line：filterScope 平移 List 管道内的全部 11 个 filter（逐字，read_first 确认当前 List 实现内的条件全集）；populateRoomNames（2 次回查回填）保留在 service——Create/Update 中调用顺序不变，落库委托 repo；:208-223 区域若属 Statistics/导出等非六方法管道则零改动（以 read_first 判定归属，勿越界）；Statistics 等其余方法零改动。

    floor_plan_text：六方法全部委托 repo（Create/Update 的 validator.ValidateFloor 前置保留后委托）；其 Delete/BatchDelete 现用 .Table(...) 形态与 repo 的 Model 形态行为等价（软删 + WHERE 语义一致，RESEARCH 盘点 #11）；filterScope 平移其条件集；base.SortScope 默认序以 read_first 为准。
    新建 floor_plan_text_crud_test.go（TestFloorPlanText91_ 前缀）：CRUD 冒烟六方法 happy path（Create→GetByID 含字段→Update→List filter 命中→Delete 软删后 GetByID NotFound→BatchDelete(nil) NoError）+ 一条 List 过滤用例，脚手架照 crud_services_test.go 模式（sqlite + AutoMigrate FloorPlanText + 关联 Floor 模型）。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/guoguo && go build ./... && go test ./internal/services/operations/ -run "TestServerRoom|TestDedicatedLine|TestFloorPlanText91" -v 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - 三文件均含 s.repo. 委托与 base.SortScope；三服务 interface 声明块零变更
    - server_room_service.go 的 orgId 空早退分支保留（grep 早退返回的零值 PageResult 构造仍存在）
    - dedicated_line_service.go 的 populateRoomNames 函数仍在且被 Create/Update 调用；六方法管道之外的方法（Statistics 等）git diff 零变更
    - 三文件 countRecords/fetchRecords/buildListQueryFromRequest 同类私有管道定义计数为 0
    - floor_plan_text_crud_test.go 存在且 TestFloorPlanText91 用例全 PASS
    - go test ./internal/services/operations/ -run "TestServerRoom|TestDedicatedLine" 全部 PASS
  </acceptance_criteria>
  <done>3 个 B 型 plain 服务迁移完成，fpt 补齐 service 级冒烟测试，仅剩 2 个 JOIN 服务</done>
</task>

<task type="auto">
  <name>Task 3: room_device + infopoint 迁移（JOIN typed，P8 顺序敏感）</name>
  <files>internal/services/operations/room_device_service.go, internal/services/operations/infopoint_service.go</files>
  <read_first>
    - internal/services/operations/room_device_service.go（List 现状：Joins 在 filter 之前的顺序、validateRoom、Create 的后续逻辑、GetByID 的 server_rooms JOIN、roomDeviceAllowedSortFields 与默认序）
    - internal/services/operations/infopoint_service.go（List 现状：WorkID/PointType 旧字段兼容分支 :250 附近、GetByID 的 workstation+floors+buildings JOIN、populateRedundantFields 3 次回查、默认序 ops_info_points.created_at DESC、infopointAllowedSortFields）
    - internal/services/operations/crud_services_test.go（TestInfoPoint 用例）与 asset_building_roomdevice_test.go（room_device 行为用例）
  </read_first>
  <action>
    room_device_service.go：struct 加 repo *base.GORMRepository[operations.OpsRoomDevice]；joinScope 闭包平移 server_rooms JOIN 的 Select/Joins 逐字；filterScope 平移 name/roomId/deviceType/status 条件逐字；List 的 scopes 顺序严格按原代码：joinScope() 在前、filterScope(req) 在后（P8：orgId 等 filter 引用 joined 表列，顺序颠倒会产生 SQL 错误或语义漂移）——repo 按变参顺序应用，把 joinScope 放第一个实参；base.SortScope(sortReq, allowed, 默认序 read_first 确认)；GetByID 委托 s.repo.GetByID(ctx, id, s.joinScope())；Create 的 validateRoom 前置保留 + Create 后续逻辑保留（read_first 确认边界），落库委托 repo；Update/Delete/BatchDelete 委托 repo。

    infopoint_service.go：struct 加 repo *base.GORMRepository[operations.OpsInfoPoint]；joinScope 平移 workstation+floors+buildings 的 Select/Joins 逐字（含 CAST 写法）；filterScope 平移 name/workstationId（含 workId 旧字段兼容 else-if 分支——分支结构与条件字符串逐字）/infoPointType/status；base.SortScope(sortReq, allowed, "ops_info_points.created_at DESC")；GetByID 委托 repo + joinScope；populateRedundantFields（3 次回查回填）保留在 Create/Update 中调用顺序不变；其余方法（Statistics/GetDeviceTypes 等）零改动。
    两文件删除各自的 countRecords/fetchRecords/buildListQueryFromRequest 同类私有管道（若有）。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/guoguo && go build ./... && go test ./internal/services/operations/ -run "TestRoomDevice|TestInfoPoint|TestAsset" -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - room_device_service.go 的 repo.List 调用中 joinScope 实参位于 filterScope 之前（source 断言：s.repo.List 参数顺序）
    - infopoint_service.go 的 workId 旧字段兼容分支保留在 filterScope 内（grep workId 分支）
    - 两文件 populateRedundantFields / validateRoom / Create 后续逻辑全部保留且调用顺序不变
    - 两文件无 countRecords/fetchRecords/buildListQueryFromRequest 定义残留
    - go test ./internal/services/operations/ -run "TestRoomDevice|TestInfoPoint|TestAsset" 全部 PASS
    - go test ./internal/services/operations/... 全包 0 失败（11 服务迁移全部完成的包级证明）
  </acceptance_criteria>
  <done>11/11 服务全部迁移完成——每服务 CRUD 管道清零，operations 包测试全绿</done>
</task>

<task type="auto">
  <name>Task 4: typesafe 死文件清理 + pagination_helper 修剪 + LOC 审计 + SUMMARY + REQUIREMENTS 同步 + 全量 gate</name>
  <files>internal/services/operations/building_service_typesafe.go, internal/api/v1/operations/building_handler_typesafe.go, internal/api/v1/operations/building_handler_typesafe_test.go, internal/services/operations/asset_building_roomdevice_test.go, internal/services/operations/pagination_helper.go, internal/services/operations/pagination_helper_test.go, .planning/REQUIREMENTS.md</files>
  <read_first>
    - internal/services/operations/building_service_typesafe.go（BuildingServiceTypeSafe interface :16、buildingServiceTypeSafe struct :26、NewBuildingServiceTypeSafe :32——整文件 171 行删除对象）
    - internal/api/v1/operations/building_handler_typesafe.go（BuildingHandlerTypeSafe 全家 :15-124——整文件 169 行删除对象）与 building_handler_typesafe_test.go（224 行整文件删除对象）
    - internal/services/operations/asset_building_roomdevice_test.go:200 起（TestBuildingService_TypeSafe 测试函数块——唯一引用 NewBuildingServiceTypeSafe 的测试块，整函数删除）
    - internal/services/operations/pagination_helper.go（calculateOffset :80-82）与 pagination_helper_test.go（calculateOffset 用例 :101 附近）
    - internal/services/operations/location_alias_service.go:102（clampPageSize 的剩余合法消费——保留 clampPageSize 的证据）
    - 91-RESEARCH.md「LOC 净减估算」节 + F5/A1
    - .planning/REQUIREMENTS.md § CRUD-REUSE :55-66（D-04/D-06/D-07 措辞同步对象：:59 / :64 / :66 三行，先例 commit 3a2efe5）
  </read_first>
  <action>
    体量说明（checker scope_sanity 缓解，不拆 4a/4b 的依据）：本 task 触及 7 个文件，但其中 3 个 typesafe 文件为零内容产出的整文件删除、TestBuildingService_TypeSafe 与 calculateOffset 用例为机械删除、REQUIREMENTS.md 为 3 行措辞修订、LOC 审计与 SUMMARY 为机械化 git 统计 + 文档写作——无一处需要设计决策的实施工作，单 task 责任密度可控，故保持单 task 不拆分。

    死文件清理（已拍板项，无需 checkpoint；安全性由编译期保证 A1）：删除 internal/services/operations/building_service_typesafe.go（171 行）、internal/api/v1/operations/building_handler_typesafe.go（169 行）、internal/api/v1/operations/building_handler_typesafe_test.go（224 行）三个整文件；删除 asset_building_roomdevice_test.go 中 TestBuildingService_TypeSafe 整个函数块（:200 起至函数收尾）。依据：router.go:546 只用 NewBuildingService；BuildingServiceTypeSafe/BuildingHandlerTypeSafe 全库仅上述 4 处引用（已 grep 验证），零生产消费者。若 go build 暴露任何遗漏引用（预期不会），就地修复并记录进 SUMMARY。

    pagination_helper 修剪：grep 确认 calculateOffset 在 operations 包内的消费者——building/asset List 已于 91-03 迁移、workstation 已于 91-02 迁移，预期仅剩 pagination_helper_test.go 自身；确认零生产消费者后删除 calculateOffset 函数（:80-82）及 pagination_helper_test.go 中对应的 calculateOffset 测试用例（TestXxx_calculateOffset 整个 test 函数或 subtest 块）。红线：extractPagination、extractSortRequest、extractIntParam、extractStringParam、clampPageSize、本地 PaginationParams struct 全部保留（F1——building/floor/asset 的 List map 路径 + 全部 SearchXxxOptions + Statistics + location_alias 的 clampPageSize 仍消费）；MaxOptionsPageSize 上限语义不动（CONTEXT deferred）。

    REQUIREMENTS.md 措辞同步（D-04 锁定要求，沿 Phase 90 commit 3a2efe5 先例；随本 task 收口 commit 一并提交）：修订 .planning/REQUIREMENTS.md § CRUD-REUSE 三行——:59（CRUD-REUSE-01）把「阅读 internal/services/base/service.go 现有 Repository[T] 接口，补全缺失方法（Statistics、SearchOptions、BatchDelete）到完整 CRUD 抽象」改为「改造 internal/services/base/service.go 为 gorm scope 函数式 GORMRepository[T] 完整 CRUD 抽象（List scope 化 + 删除 Repository[T] interface 与 Query/WhereCondition DSL，BatchDelete 已存在仅反转空 ids 语义；Statistics/SearchOptions 为异构业务查询，按 D-04 留 service 层不进 Repository）」；:64（CRUD-REUSE-06）服务清单追加 + wall_service.go + floor_plan_text_service.go，末尾「（剩 5 个）」改「（D-06 扩容后剩 7 个）」；:66（CRUD-REUSE-08）把「统计 LOC 减少（target ~2000 行）并写入 SUMMARY」改「LOC 净减审计（D-07 混合标准：11 services 全部复用 GORMRepository + 每服务 CRUD 模板清零 + LOC 净减 ≥800 行，git numstat 证据写入 SUMMARY）」。只改这三行措辞，不动复选框勾选状态与其余行。

    LOC 审计：定位 phase 起点 commit（git log --oneline 找 91-01 首个 commit，取其父 SHA 记为 BASE；若 message 无前缀则用 git log --oneline -- internal/services/base/service.go 定位本 phase 最早改写 commit 再取父）。执行 git diff --numstat BASE..HEAD -- internal/services/ internal/api/v1/operations/ 汇总（新增行合计 add、删除行合计 del），净减 = del - add。基线参考（main @ 917428a 实测 wc -l）：11 服务合计 2972（workstation 456/building 300/floor 436/asset 411/server_room 251/infopoint 264/dedicated_line 231/room_device 223/door 148/wall 136/floor_plan_text 116）+ base/service.go 161 + base_80_05_test.go 207 + pagination_helper.go 82 + pagination_helper_test.go 207 + typesafe 三文件 564。若净减 <800：如实记录差距与原因（不得虚报），并在 SUMMARY 列出可选补充来源（如 floor typed 接线扩展，RESEARCH F5 组合 2）留待用户决策——禁止为凑数做无意义删除。

    SUMMARY：写 .planning/workstreams/milestone/phases/91-crud-base-repository-t-p1/91-04-SUMMARY.md——含：11 服务迁移清单（每服务：迁移形状/List 参数形态/排序语义型/特殊保留项）；LOC 审计表（逐文件 before → after + numstat 汇总 + ≥800 达成结论）；关键决策记录（BatchDelete 语义反转、F3 软删 bugfix 经 checkpoint、D-05 分页收紧经 checkpoint、typesafe 清理已拍板、extractXxxParam 保留口径 = workstation List/SearchOptions 消费点清零而 helper 本体保留）；测试证据（go build/go test ./.../operations 包/AST 防线全绿的命令输出摘要）。

    全量 gate：go build ./... 0 错误；go test ./... 0 失败（1688+ 既有测试 + AST 锁值防线 status_constants_test/operlog regression/pagination 锁值全绿）；go vet ./internal/services/... ./internal/api/v1/operations/... 无新增告警。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/guoguo && go build ./... && go test ./... 2>&1 | tail -5 && ls internal/services/operations/building_service_typesafe.go internal/api/v1/operations/building_handler_typesafe.go 2>&1; grep -c "calculateOffset" internal/services/operations/pagination_helper.go; test -f .planning/workstreams/milestone/phases/91-crud-base-repository-t-p1/91-04-SUMMARY.md && echo SUMMARY_OK && grep -q "D-06 扩容后剩 7 个" .planning/REQUIREMENTS.md && grep -q "净减 ≥800" .planning/REQUIREMENTS.md && ! grep -q "补全缺失方法" .planning/REQUIREMENTS.md && echo REQ_SYNC_OK</automated>
  </verify>
  <acceptance_criteria>
    - building_service_typesafe.go / building_handler_typesafe.go / building_handler_typesafe_test.go 三文件不存在（ls 报 No such file）
    - asset_building_roomdevice_test.go 中 TestBuildingService_TypeSafe 函数计数为 0
    - pagination_helper.go 中 calculateOffset 计数为 0；extractPagination/extractSortRequest/extractIntParam/extractStringParam/clampPageSize 五个 helper 计数各 ≥1（保留验证）
    - go build ./... 退出码 0；go test ./... 0 失败
    - 91-04-SUMMARY.md 存在，含 LOC 审计表（逐文件 before/after 数字 + numstat 净减值）且净减 ≥800，或如实记录未达标差距
    - 11 个迁移目标 service 文件中 grep "countRecords\|fetchRecords" 计数为 0
    - .planning/REQUIREMENTS.md § CRUD-REUSE 同步到位：:59 不再含「补全缺失方法（Statistics、SearchOptions、BatchDelete）」、:64 服务清单含 wall_service.go 与 floor_plan_text_service.go 且末尾为「（D-06 扩容后剩 7 个）」、:66 含「净减 ≥800」且不再含「target ~2000 行」（grep 验证，D-04 锁定要求）
  </acceptance_criteria>
  <done>phase 收口：11 服务全部 repo 化、死代码清理、LOC 审计落 SUMMARY、REQUIREMENTS § CRUD-REUSE 措辞同步（D-04/D-06/D-07）、全量 gate 全绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| typed request → filterScope | 7 个服务的请求字段进 SQL 条件——逐字平移与 ? 占位符是注入防线 |
| scope 顺序 → SQL 正确性 | room_device 的 Joins/filter 顺序错误会把 filter 打在未 join 的表列上 |
| 死代码删除 → 构建完整性 | typesafe 三文件删除若有遗漏引用即编译失败 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-04-01 | Tampering | door/wall 复合排序 tiebreaker 丢失（P3） | mitigate | base.SortScopeWithTail 无条件尾随（91-01 契约 3/6 锁定）；crud_services_test 的排序用例 + 多行同列值场景守护；验收含 Order("") 死代码清零 |
| T-91-04-02 | Tampering | room_device scope 顺序颠倒（P8） | mitigate | scopes 按原代码顺序传入（joinScope 先于 filterScope），验收为 source 断言；asset_building_roomdevice_test 的 room_device 用例守护 |
| T-91-04-03 | DoS | typesafe 删除遗漏引用致断编译 | mitigate | 编译期风险（A1）：go build ./... 立即暴露，四处引用已 grep 全集验证；失败即可见可修，无静默面 |
| T-91-04-04 | Repudiation | LOC 审计虚报 | mitigate | SUMMARY 强制 git numstat 命令级证据 + 逐文件 before/after 表；未达标须如实记录（acceptance 明确禁止虚报） |
| T-91-04-05 | Information Disclosure | infopoint WorkID 旧字段兼容分支在平移中丢失 | mitigate | 分支结构逐字平移 + 验收 grep workId；crud_services_test 的 infopoint 用例守护 |
| T-91-04-SC | Tampering | npm/pip/cargo installs | N/A | 零外部包安装（纯删除与内部重构） |
</threat_model>

<verification>
- go build ./... 0 错误（CLAUDE.md 强制）
- go test ./... 0 失败——phase 总 gate：1688+ 既有测试 + TestBase91 契约 + TestFloor91/TestFloorPlanText91 新基线 + 全部既有行为锁（TestImp77/TestDoor/TestWall/TestServerRoom/TestDedicatedLine/TestInfoPoint/TestAsset/TestBuilding/TestRoomDevice）全绿
- handler 层 go test ./internal/api/v1/operations/... 全绿 = handler e2e smoke 测试口径（RESEARCH Validation Architecture：无独立 e2e harness，handler 测试覆盖 bind/response 路径 + service 级 sqlite 测试打真库，两层拼合即完整采样）
- AST 锁值防线全程绿：internal/models/status_constants_test.go、internal/utils/operlog/regression_test.go、pkg/constants/*_test.go
- grep 证明：11 个服务文件 countRecords/fetchRecords 零残留；typesafe 三文件已删除
</verification>

<success_criteria>
1. 11 个 operations services 全部复用 base.GORMRepository[T]，每服务 CRUD 模板清零（D-06 + D-07 定性底线 + CRUD-REUSE-06）
2. LOC 净减 ≥800（D-07，numstat 审计入 SUMMARY）
3. typesafe 死代码 564 行 + 测试块清理完成
4. floor_plan_text 冒烟测试补齐（Wave 0 缺口清零）
5. 全量 gate：go build + go test ./... + handler 包测试 0 失败（CRUD-REUSE-08）
6. phase SUMMARY 产出，含迁移清单/LOC 审计/决策记录/测试证据
</success_criteria>

<output>
Create .planning/workstreams/milestone/phases/91-crud-base-repository-t-p1/91-04-SUMMARY.md when done；按 execute-plan 工作流原子 commit：feat(91-04): migrate remaining 7 services, remove typesafe dead code, LOC audit + REQUIREMENTS CRUD-REUSE wording sync (closes phase 91)
</output>
