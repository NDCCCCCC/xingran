---
phase: 91-crud-base-repository-t-p1
plan: 03
type: execute
wave: 3
depends_on: ["91-01", "91-02"]
files_modified:
  - internal/services/operations/floor_service_crud_test.go
  - internal/services/operations/building_service.go
  - internal/services/operations/asset_service.go
  - internal/services/operations/floor_service.go
autonomous: false
requirements: [CRUD-REUSE-03, CRUD-REUSE-04, CRUD-REUSE-05, CRUD-REUSE-07]
user_setup: []
must_haves:
  truths:
    - "building/floor/asset 六方法管道经各自 base.GORMRepository[T]，List 对外 map[string]interface{} 签名不变（F1：三服务保持 map，D-05 不涉及）"
    - "floor 全部公开方法签名不变（P7：floorCacheService 装饰器实现同一 FloorService 接口，floor_cache_impl.go 是 Phase 92 文件零触碰）"
    - "building/asset List 的 Total 不再计入软删除行（F3 latent bugfix：Model(new(T)) 统一起链，经 checkpoint 确认）"
    - "asset 的 component_type IS NULL 恒定过滤进入 scope（Phase 48 D-07 语义，asset_listfilter_test.go 守护）"
    - "floor 的唯一约束软删恢复 / 异步同步工位 building_id / updateBuildingFloorCount 编排整体保留 service 层，仅 GetByID/List 管道进 repo"
    - "floor 迁移前有 service 级行为基线测试（91-RESEARCH Wave 0 缺口补齐）"
  artifacts:
    - path: internal/services/operations/floor_service_crud_test.go
      provides: "floor 六方法 + 软删恢复 + 楼层计数 + List 过滤的行为基线（迁移前先绿）"
      contains: "func TestFloor91"
    - path: internal/services/operations/building_service.go
      provides: "repo 组合 + applyFilters→filterScope + workstation_count 子查询 selectScope"
      contains: "base.GORMRepository[operations.OpsBuilding]"
    - path: internal/services/operations/asset_service.go
      provides: "repo 组合 + component_type IS NULL 恒定 scope + 9 filter scope 化"
      contains: "base.GORMRepository[models.Asset]"
    - path: internal/services/operations/floor_service.go
      provides: "仅 GetByID/List 管道 repo 化，其余方法逐字不动"
      contains: "base.GORMRepository[operations.OpsFloor]"
  key_links:
    - from: internal/services/operations/building_service.go
      to: internal/services/base/service.go
      via: "s.repo.List(ctx, base.PageParams{...}, scopes...)"
      pattern: "s\\.repo\\.List"
    - from: internal/services/operations/floor_service.go
      to: internal/services/operations/floor_cache_impl.go
      via: "FloorService 接口签名不变 → 装饰器零影响"
      pattern: "func NewFloorService"
---

<objective>
批量迁移第二波：building / floor / asset 三个 map 参数服务（共 1147 行）。三服务 List 保持 map 签名（F1——D-05 只锁 workstation），service 内部 map→scope 转换；floor 因 floorCacheService 装饰器锁定接口签名（P7），只把 GetByID/List 管道进 repo，Create/Update/Delete/BatchDelete 的业务编排整体保留。迁移前置：先补 floor service 级行为基线测试（Wave 0 缺口）。building/asset 的 Table→Model 起链统一会修复"Count 含软删、Find 不含"的现存 Total 虚高分歧（F3），经 checkpoint 向用户确认。

Purpose: 覆盖 CRUD-REUSE-03/04/05；三服务是"map 签名不变"迁移形状的唯一样本（91-04 无此形状）。
Output: 三服务 repo 化 + floor 行为基线测试 + F3 语义变更用户确认。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/91-crud-base-repository-t-p1/91-CONTEXT.md
@.planning/workstreams/milestone/phases/91-crud-base-repository-t-p1/91-RESEARCH.md
@.planning/workstreams/milestone/phases/91-crud-base-repository-t-p1/91-PATTERNS.md
@internal/services/base/service.go
@internal/services/operations/building_service.go
@internal/services/operations/floor_service.go
@internal/services/operations/asset_service.go
@internal/services/operations/asset_building_roomdevice_test.go
@internal/services/operations/asset_listfilter_test.go
@internal/services/operations/geocoding_photo_floor_test.go
@internal/services/operations/floor_cache_impl.go
</context>

<interfaces>
<!-- 91-01 产出的 base 契约（消费方视角，完整签名见 91-01-PLAN.md） -->
base.NewGORMRepository[T](db) / Create / Update / Delete / GetByID(ctx, id, scopes ...) / List(ctx, page PageParams, scopes ...) / BatchDelete(ctx, ids)（空 ids → nil）
base.SortScope(req, allowed, defaultOrder)（B 型）/ base.SortScopeWithTail（A 型，本 plan 不用）
type Scope = func(*gorm.DB) *gorm.DB; type PageParams struct{ Current, PageSize int }

<!-- 三服务的迁移形状（RESEARCH Pattern 3：map → scope，接口签名不变） -->
buildingService: List(ctx, params map[string]interface{}) (*base.PageResult, error) 签名不变；内部 extractPagination + filterScope + selectScope(workstation_count) + base.SortScope(sortReq, buildingAllowedSortFields, "order_num ASC")
assetService: List map 签名不变；恒定 scope db.Where("component_type IS NULL") 必须在最前 + filterScope + base.SortScope(..., "created_at DESC")
floorService: 全部公开方法签名不变（P7）；List map 签名不变；分页为无 clamp 内联断言（current=1/pageSize=10 默认，任意值直传 repo）
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 补 floor service 级行为基线测试（Wave 0 缺口，迁移前先绿）</name>
  <files>internal/services/operations/floor_service_crud_test.go</files>
  <read_first>
    - internal/services/operations/asset_building_roomdevice_test.go（sqlite 建库 + AutoMigrate 多模型 + service 构造的脚手架模式）
    - internal/services/operations/floor_service.go（全文：Create :87-125 唯一约束软删恢复、Update :126-155 异步 syncWorkstationBuildingID、Delete :156-166 + BatchDelete :333-367 的 updateBuildingFloorCount、GetByID :167-180、List :181-244、validateBuilding :368、updateBuildingFloorCount :383、syncWorkstationBuildingID :393）
    - internal/services/operations/geocoding_photo_floor_test.go:420-440（floor List map 调用的既有间接覆盖形态）
    - internal/models/operations 中 OpsFloor/OpsBuilding 与 internal/models 中 Workstation/SysFile 的表名（AutoMigrate 注册清单）
    - 91-RESEARCH.md「Wave 0 Gaps」节
  </read_first>
  <behavior>
    TestFloor91_CRUD: Create（validateBuilding 通过后入库，含 BaseModel UUID 回填）→ GetByID 返回含 building_name 与 plan_image_url join 字段 → Update 改名生效 → Delete 软删后 GetByID 返回 ErrRecordNotFound → List 不含软删行
    TestFloor91_Create_SoftDeleteRestore: 同 (building_id, floor_no) 唯一键——先 Create、再 Delete（软删）、再 Create 同键 → 恢复软删行而非唯一冲突（按 :87-125 实现的实际语义断言；若实现为"物理删除旧行后插入"则以实现为准，测试锁当前行为）
    TestFloor91_Update_SyncsWorkstation: Update 修改 floor 的 building_id → 异步 goroutine 同步挂靠工位的 building_id——用 require.Eventually 轮询工位行（项目规约：异步用 Eventually，禁 sleep）
    TestFloor91_Delete_UpdatesBuildingCount: Create floor → Delete → 楼宇的 floor_count 字段（按 updateBuildingFloorCount 实际写的列名）归零/递减；BatchDelete(nil) 返回 NoError、BatchDelete 非空后计数同步
    TestFloor91_List_Filters: map 过滤 name（LIKE）/buildingId/orgId（EXISTS 子查询）/status 各命中与不命中分支 + 默认排序 ops_floors.order_num ASC（两行不同 order_num 断言顺序）+ 分页 total/current/pageSize 回填
    TestFloor91_List_NoClamp: pageSize=1 与 pageSize=5000 均按原值生效（无 clamp 内联断言语契约，P5 floor 行）
  </behavior>
  <action>
    新建 internal/services/operations/floor_service_crud_test.go（package operations）。脚手架照 asset_building_roomdevice_test.go 模式：glebarez sqlite 临时库 + AutoMigrate（OpsFloor/OpsBuilding/SysFile/Workstation 及 updateBuildingFloorCount 写入的目标模型——以 read_first 确认的实际列为准）+ NewFloorService(db)。全部用例先对未迁移的当前实现跑到全绿（这是基线：91-03 Task 3 迁移后必须逐字节同结果）。注意 floor Update 的异步同步在测试库中依赖 Workstation 表存在且 floor 记录有挂靠工位。断言禁本地绝对路径/目录名（项目 CI 规约）。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/guoguo && go test ./internal/services/operations/ -run TestFloor91 -v 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - internal/services/operations/floor_service_crud_test.go 存在且含 TestFloor91_ 前缀的 6 个测试函数
    - go test ./internal/services/operations/ -run TestFloor91 全部 PASS（对当前未迁移实现）
    - TestFloor91_Update_SyncsWorkstation 使用 require.Eventually 而非 time.Sleep
    - 本 task 零生产代码改动（git diff 仅新增测试文件）
  </acceptance_criteria>
  <done>floor 获得迁移前行为基线：CRUD/软删恢复/异步同步/楼层计数/过滤/无 clamp 全部有锁定</done>
</task>

<task type="auto">
  <name>Task 2: building_service + asset_service repo 化（map 签名不变）</name>
  <files>internal/services/operations/building_service.go, internal/services/operations/asset_service.go</files>
  <read_first>
    - internal/services/operations/building_service.go（List :130-167 含 workstation_count 子查询 Select :155、applyFilters :170-187、applyDeptFilter :190-205 的 Pluck 子部门查询、Create/Update :83-113 的 validateOrg/validateNameUnique、Delete :115、GetByID :120-127、BatchDelete :207-210、buildingAllowedSortFields）
    - internal/services/operations/asset_service.go（List :183-224 含恒定过滤 :191、applyFilters :232-268 的 9 个 filter、Update :123-146 的 Omit("CreatedAt") 变体、Delete :148、GetByID :153、BatchDelete :227-229 无空守卫、assetAllowedSortFields）
    - internal/services/operations/asset_listfilter_test.go 与 asset_building_roomdevice_test.go（现有 map 调用测试 = 行为验证器，迁移后必须全绿且零改动）
    - 91-RESEARCH.md「11 服务形状盘点」#2/#4 与 Pitfall P2/F3
  </read_first>
  <action>
    两文件同型迁移（RESEARCH Pattern 3）：
    struct 各加 repo 字段（building 用 base.GORMRepository[operations.OpsBuilding]，asset 用 base.GORMRepository[models.Asset]）并在构造函数初始化；db 字段保留（validateOrg/validateNameUnique/applyDeptFilter 的 Pluck/Statistics/SearchBuildingOptions/GetDeviceTypes 仍直用 db）。

    List（两服务均保持 params map[string]interface{} 签名不变）：
    - building：pagination := extractPagination(params)；sortReq := extractSortRequest(params)；filterScope 把 applyFilters 的三个条件逐字平移进闭包（name LIKE / orgId 走 applyDeptFilter——该函数保留为独立 helper 并在闭包内调用，因其内含 db Pluck 预查询；status >= 0）；selectScope 闭包携带 :155 的 workstation_count 子查询 Select 逐字；排序 base.SortScope(sortReq, buildingAllowedSortFields, "order_num ASC")；return s.repo.List(ctx, base.PageParams{Current: pagination.Current, PageSize: pagination.PageSize}, s.filterScope(params), s.selectScope(), s.sortScope(params))。语义说明写入注释：现状 Select 在 Count 后（:155），repo 化后前置——Count 时 Select 被替换为 count(*)（spike 实证）所以 Total 不变；现状 Table("ops_buildings") 起链导致 Count 不过滤软删（F3），repo 的 Model(new(T)) 统一后 Total 收紧为不含软删行——checkpoint 任务处理。
    - asset：恒定过滤 scope 放 scopes 首位（db.Where("component_type IS NULL")，Phase 48 D-07 注释逐字保留）；filterScope 平移 applyFilters 全部 9 个 filter（devicesn/deviceModelName/deptId→applyDeptFilter 同型 helper/等——逐字，实现读 read_first）；排序 base.SortScope(sortReq, assetAllowedSortFields, "created_at DESC")；其余同 building。
    - calculateOffset 的调用随之消失（offset 计算移入 repo.List）。

    Create：validateOrg/validateNameUnique（building）与 validateDept/validateUser/validateDeviceSNUnique（asset）前置校验逐字保留在 repo 调用之前，落库委托 s.repo.Create。
    Update：保留各自的现状实现——若当前为 Omit("CreatedAt") 变体（building :111 区段 / asset :144 区段，以 read_first 实际代码为准）则整段保留不动（业务注释：零值覆盖 created_at bug 的修复，不隐入通用 repo），不委托 repo.Update；若为纯 Save 则委托。
    Delete：委托 s.repo.Delete(ctx, id)（与现状 Where("id = ?").Delete 等价）。
    GetByID：委托 s.repo.GetByID(ctx, id)（零 scope——两服务现状均为 plain First，repo 空 scope 时 WHERE id = ? 逐字等价）。
    BatchDelete：直接委托 s.repo.BatchDelete（asset 现状无空守卫、building 按实际——repo 空 ids nil 语义两态兼容）。
    清理：两文件的 countRecords/fetchRecords/buildListQueryFromRequest 同类私有管道 helper（若有）删除；calculateOffset 引用清零。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/guoguo && go build ./... && go test ./internal/services/operations/ -run "TestAsset|TestBuilding" -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - building_service.go 含 base.GORMRepository[operations.OpsBuilding]、asset_service.go 含 base.GORMRepository[models.Asset]，两者 List 的 map[string]interface{} 签名保持不变
    - asset_service.go 的 List scopes 中 component_type IS NULL 过滤存在于 repo.List 调用的 scope 列表（source 断言 grep "component_type IS NULL"）
    - 两文件 calculateOffset 引用计数为 0
    - go test ./internal/services/operations/ -run "TestAsset|TestBuilding" 全部 PASS（asset_listfilter_test + asset_building_roomdevice_test 零改动全绿）
    - go test ./internal/services/operations/... 全包 0 失败（含 TestFloor91 基线不受影响）
  </acceptance_criteria>
  <done>building/asset repo 化完成，map 签名与全部 filter 语义逐字保留，Total 软删语义变更点就绪待 checkpoint</done>
</task>

<task type="auto">
  <name>Task 3: floor_service repo 化（P7 签名锁定，仅 GetByID/List 管道）</name>
  <files>internal/services/operations/floor_service.go</files>
  <read_first>
    - internal/services/operations/floor_service.go（GetByID :167-180 的 join Select/Joins 形状、List :181-244 的内联分页断言 :203-214 与 join :228-234、Create :87-125、Update :126-155、Delete :156-166、BatchDelete :333-367、floorAllowedSortFields）
    - internal/services/operations/floor_cache_impl.go（floorCacheService 实现同一 FloorService 接口——只读确认签名兼容，禁止修改该文件）
    - internal/services/operations/floor_service_crud_test.go（Task 1 基线——迁移后必须全绿）
  </read_first>
  <action>
    struct 加 repo *base.GORMRepository[operations.OpsFloor] 并在 NewFloorService 初始化；db 保留（Create 软删恢复 / Update 异步同步 / updateBuildingFloorCount / SearchFloorOptions / GetTree / Statistics 仍直用 db）。

    接口红线（P7）：FloorService 的全部公开方法签名（含 List(ctx, params map[string]interface{})）一字不改——floorCacheService 装饰器实现同一接口，任何签名变更都会强制触碰 Phase 92 文件。

    新私有 joinScope() base.Scope：闭包返回 db.Select("ops_floors.*, ops_buildings.name as building_name, '/uploads/' || sys_files.storage_path as plan_image_url").Joins("LEFT JOIN ops_buildings ON CAST(ops_buildings.id AS TEXT) = ops_floors.building_id").Joins("LEFT JOIN sys_files ON CAST(sys_files.id AS TEXT) = ops_floors.plan_image_id")（:229-231 三段逐字平移）。
    新私有 filterScope(params map[string]interface{}) base.Scope：name LIKE / buildingId / orgId EXISTS 子查询 / status >= 0 四条件逐字平移（:187-201，含 CAST 写法）。

    List：保留 :203-214 的内联分页断言逐字（current 默认 1、pageSize 默认 10、int/float64 双型断言、无任何 clamp）——分页值直传 base.PageParams（repo 直信入参，floor 的无 clamp 语义由 service 内联逻辑保持，P5）；排序 base.SortScope(extractSortRequest(params), floorAllowedSortFields, "ops_floors.order_num ASC")；return s.repo.List(ctx, base.PageParams{Current: current, PageSize: pageSize}, s.filterScope(params), s.joinScope(), s.sortScope(params))。现状 Select+Joins 在 Count 后，repo 化前置于 Count——两 join 均 N:1 LEFT JOIN 无行增殖 + Select 被替换为 count(*)，Total 不变（spike + TestFloor91_List_Filters 守护）。floor 起链本就是 Model 形态（:185），无 F3 分歧。

    GetByID：s.repo.GetByID(ctx, id, s.joinScope())——repo 表限定生成 ops_floors.id = ?（现状 :167-180 实现以 read_first 为准对齐；确认 OpsFloor TableName() 返回 ops_floors）。

    Create/Update/Delete/BatchDelete/validateBuilding/updateBuildingFloorCount/syncWorkstationBuildingID/SearchFloorOptions/GetTree/Statistics：全部逐字不动（业务编排留 service——CONTEXT 已锁定）。Delete/BatchDelete 不委托 repo（因需编排 updateBuildingFloorCount），但 BatchDelete 的空 ids 早退 return nil 保留。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/guoguo && go build ./... && go test ./internal/services/operations/ -run "TestFloor91|TestImp77" -v 2>&1 | tail -12 && git diff --stat internal/services/operations/floor_cache_impl.go</automated>
  </verify>
  <acceptance_criteria>
    - floor_service.go 的 FloorService interface 全部方法签名与迁移前逐字一致（git diff 中 interface 块零变更）
    - git diff --stat 显示 floor_cache_impl.go 零改动（P7 红线）
    - floor_service.go 含 base.GORMRepository[operations.OpsFloor] 且仅 GetByID/List 两方法委托 repo
    - List 的内联分页断言（current/pageSize int/float64 断言、无 clamp）逐字保留
    - go test ./internal/services/operations/ -run TestFloor91 全部 PASS（Task 1 基线零回归）
    - go test ./internal/services/operations/... 全包 0 失败
  </acceptance_criteria>
  <done>floor repo 化完成：接口签名零变更、装饰器零影响、业务编排原位保留、基线测试全绿</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: building/asset Total 软删语义修复确认（F3/A3）</name>
  <files>internal/services/operations/building_service.go, internal/services/operations/asset_service.go（本 checkpoint 校验对象为 Task 2 产物，无新增文件）</files>
  <action>无实现动作——阻塞等待用户确认 building/asset List Total 软删语义修复（F3）后方可继续。</action>
  <done>用户回复 approved（或提出的问题已在后续处理），F3 软删 Total 语义变更获得显式确认。</done>
  <verify>
    <human-check>用户确认 building/asset List Total 不再计入软删除行（Model(new(T)) 统一起链的 latent bugfix）可接受。验证步骤见 how-to-verify。</human-check>
  </verify>
  <what-built>
    building 与 asset 的 List 统一到 base.GORMRepository 的 Model(new(T)) 起链后，修复了一个此前无人知晓的现存行为分歧（RESEARCH F3，GORM 源码 + sqlite spike 双重实证）：两服务旧代码用 .Table("ops_buildings")/.Table("ops_asset") 起链，导致 Count 不过滤软删除行、Find 过滤——即列表 Total 比实际行数虚高（删过楼的部门会看到"总条数 > 实际页内行数 × 页数"）。统一后 Total 与列表行数严格一致。方向是修 bug（Total 虚高 → 准确）；现有测试不软化任何行，零回归。
  </what-built>
  <how-to-verify>
    1. cd D:/CODE/ClaudeCode/guoguo && go test ./internal/services/operations/ -run "TestAsset|TestBuilding|TestFloor91" -v —— 全绿
    2. 行为差异说明：仅当存在软删除的楼宇/资产行时，列表接口返回的 total 会比旧版小（不再计入已删行）；无软删数据时逐字节一致
    3. 确认业务侧没有依赖"Total 含软删行"的调用方（该行为是 GORM 链式副作用的意外产物，非设计意图）
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handler map → service filterScope | map 值（字符串）直接进 scope 条件参数——? 占位符与 EXISTS 子查询的逐字平移是注入防线 |
| soft-delete 语义 → API 消费方 | building/asset Total 口径变化直接改变前端分页展示值 |
| FloorService 接口 → floorCacheService 装饰器 | 签名漂移会涟漪至 Phase 92 文件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-03-01 | Information Disclosure | building/asset Total 软删语义变更（F3） | mitigate | 方向为更严格过滤（Total 不再虚高），checkpoint:human-verify 显式确认（RESEARCH A3）；现有测试零软列数据，零回归 |
| T-91-03-02 | Tampering | floor 接口签名漂移强制触碰 Phase 92 文件 | mitigate | 验收红线：git diff floor_cache_impl.go 为空 + interface 块零变更（Task 3 acceptance）；P7 全程约束 |
| T-91-03-03 | Tampering | asset component_type IS NULL 恒定过滤在平移中丢失 | mitigate | 过滤进 scopes 首位 + asset_listfilter_test.go（Phase 48 D-07 行为锁）零改动全绿作守护 |
| T-91-03-04 | Tampering | applyDeptFilter 的子部门 Pluck 在 scope 化中被改写 | mitigate | 保留为独立 helper 在闭包内调用，orgId LIKE 模式与 ancestors 匹配字符串逐字平移；TestBuilding/TestAsset 既有 orgId 用例守护 |
| T-91-03-05 | DoS | floor 无 clamp 分页语义被顺手"修复" | mitigate | Task 3 明确保留内联断言逐字（P5 三语义并存原则）；TestFloor91_List_NoClamp 锁定 |
| T-91-03-SC | Tampering | npm/pip/cargo installs | N/A | 零外部包安装 |
</threat_model>

<verification>
- go build ./... 退出码 0
- go test ./internal/services/operations/... 0 失败（TestFloor91 基线 + TestAsset/TestBuilding/TestImp77 既有行为锁全绿）
- git diff floor_cache_impl.go 为空（P7 红线）
- 三服务 List map 签名 grep 确认不变：grep -c "List(ctx context.Context, params map\[string\]interface{})" 对三文件各为 1
- 全量：go test ./... 0 失败
</verification>

<success_criteria>
1. building/floor/asset 全部经 base.GORMRepository[T]，CRUD 管道清零（CRUD-REUSE-03/04/05）
2. floor 接口签名零变更、floor_cache_impl.go 零触碰（CRUD-REUSE-04 + P7）
3. floor 行为基线测试先行并保持全绿（CRUD-REUSE-07 Wave 0 缺口补齐）
4. F3 Total 语义修复经用户确认
5. map 签名不变样本形状定型（asset 恒定过滤 scope 化模式可供后续复用）
</success_criteria>

<output>
按 execute-plan 工作流原子 commit：feat(91-03): migrate building/floor/asset to base.GORMRepository (map signatures preserved)
</output>
