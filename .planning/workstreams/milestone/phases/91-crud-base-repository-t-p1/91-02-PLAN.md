---
phase: 91-crud-base-repository-t-p1
plan: 02
type: execute
wave: 2
depends_on: ["91-01"]
files_modified:
  - internal/api/v1/operations/requests/workstation_requests.go
  - internal/services/operations/workstation_service.go
  - internal/api/v1/operations/workstation_handler.go
  - internal/services/operations/workstation_floor_code_77_03_test.go
  - internal/api/v1/operations/workstation_handler_test.go
  - internal/api/v1/operations/workstation_handler_full_test.go
autonomous: false
requirements: [CRUD-REUSE-02, CRUD-REUSE-07]
user_setup: []
must_haves:
  truths:
    - "WorkstationService.List 与 SearchWorkstationOptions 的参数从 map[string]interface{} 改为 typed request（D-05），其余 5 个接口方法签名不变"
    - "workstation 六方法 CRUD 管道（Create/Update/Delete/GetByID/List/BatchDelete）经 base.GORMRepository[models.Workstation] 执行，不再直调 GORM 拼查询管道"
    - "status/type 的 -1（及 type 为 nil）跳过过滤语义保留（P6，workstation_floor_code_77_03_test.go:145 锁定）"
    - "handler bind 失败降级为零值请求继续查询（与现状空 map 语义一致，不返回 400）"
    - "Statistics/GetWorkstationDeptOptions/BatchUpdatePositions 保持原签名与原实现（D-04 留 service）"
    - "前端 JSON 契约零变更：仅加性新增 floorCode/type/orgId 三个可选字段（F2），buildingId/code 仍被静默忽略（:415 行为锁保留）"
  artifacts:
    - path: internal/api/v1/operations/requests/workstation_requests.go
      provides: "WorkstationListRequest +3 字段（FloorCode/Type *int/OrgID），BuildingID/Code 保留声明并注释锁定不消费"
      contains: "FloorCode"
    - path: internal/services/operations/workstation_service.go
      provides: "repo 组合 + filterScope/joinScope scope 化 + typed List/SearchWorkstationOptions"
      contains: "base.GORMRepository[models.Workstation]"
    - path: internal/api/v1/operations/workstation_handler.go
      provides: "List 与 SearchWorkstationOptions 2 处 typed bind（降级语义）"
    - path: internal/services/operations/workstation_floor_code_77_03_test.go
      provides: "~14 处 List + 7 处 SearchWorkstationOptions 的 map 字面量改写为 typed 字面量，断言逐字不变"
  key_links:
    - from: internal/services/operations/workstation_service.go
      to: internal/services/base/service.go
      via: "s.repo.List(ctx, base.PageParams{...}, scopes...)"
      pattern: "s\\.repo\\.(List|Create|Update|Delete|GetByID|BatchDelete)"
    - from: internal/api/v1/operations/workstation_handler.go
      to: internal/api/v1/operations/requests/workstation_requests.go
      via: "ShouldBindJSON(&req) typed bind"
      pattern: "requests\\.WorkstationListRequest\\{\\}"
---

<objective>
Pilot 迁移：把最复杂、最异类的 workstation_service（456 行）迁到 91-01 改造后的 base.GORMRepository[T] 上，同步完成 D-05 typed request 全套接线（接口 typed 化 + struct 扩 3 字段 + handler 2 处 bind + 3 个测试文件改写）。pilot 跑通 = 模式可复制，为 91-03/91-04 的 10 个服务批量迁移定型。

Purpose: 验证 scope 函数式 repo 在最重场景（6 表 LEFT JOIN、EXISTS 子查询、-1 跳过语义、异步回填）下的行为零变更。
Output: workstation 服务/handler/requests/测试全链 typed 化 + repo 化，包级测试全绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/91-crud-base-repository-t-p1/91-CONTEXT.md
@.planning/workstreams/milestone/phases/91-crud-base-repository-t-p1/91-RESEARCH.md
@.planning/workstreams/milestone/phases/91-crud-base-repository-t-p1/91-PATTERNS.md
@internal/services/base/service.go
@internal/services/operations/workstation_service.go
@internal/api/v1/operations/requests/workstation_requests.go
@internal/api/v1/operations/requests/common.go
@internal/api/v1/operations/workstation_handler.go
@internal/services/operations/workstation_floor_code_77_03_test.go
@internal/api/v1/operations/workstation_handler_test.go
@internal/api/v1/operations/workstation_handler_full_test.go
</context>

<interfaces>
<!-- 91-01 产出的 base 契约（本 plan 消费）；完整签名见 91-01-PLAN.md <interfaces> -->
type Scope = func(*gorm.DB) *gorm.DB
type PageParams struct { Current int; PageSize int }
func NewGORMRepository[T any](db *gorm.DB) *GORMRepository[T]
func (r *GORMRepository[T]) Create/Update/Delete/GetByID(ctx, id, scopes ...)/List(ctx, page, scopes ...)/BatchDelete(ctx, ids) — BatchDelete 空 ids 返回 nil
func base.SortScope(req BaseListRequest, allowed map[string]string, defaultOrder string) Scope        // B 型
func base.SortScopeWithTail(req BaseListRequest, allowed map[string]string, tail string) Scope        // A 型（本 plan 不用）

<!-- 本 plan 改造后的 workstation 关键形状 -->
interface: List(ctx context.Context, req requests.WorkstationListRequest) (*base.PageResult, error)
interface: SearchWorkstationOptions(ctx context.Context, req requests.WorkstationListRequest) ([]DropdownOption, error)  // 复用同一 request struct（planner 决定，D-05 只要求去 map）
interface: Statistics/GetWorkstationDeptOptions/BatchUpdatePositions/BatchDelete/Create/Update/Delete/GetByID 签名全部不变
struct: workstationService{ repo *base.GORMRepository[models.Workstation], db *gorm.DB }  // db 保留给 Statistics/SearchOptions/BatchUpdatePositions/First 回填
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: WorkstationListRequest 扩 3 字段 + workstation_service repo 化 + D-05 typed 化</name>
  <files>internal/api/v1/operations/requests/workstation_requests.go, internal/services/operations/workstation_service.go</files>
  <read_first>
    - internal/api/v1/operations/requests/workstation_requests.go（现状 4 字段 + 2 嵌入；扩字段对象）
    - internal/api/v1/operations/requests/common.go（PaginationParams.GetPagination :16-29 clamp 语义；StatusRequest.GetStatus(-1) :54-59 默认值技巧）
    - internal/services/operations/workstation_service.go（全文：join 常量 :13-21、validateTableName :24-32、接口 :35-50、Statistics :68-86 保持 map、struct :122-124、排序白名单 :133-145、Create/Update :171-198、applyWorkstationOccupancyLink :209-219、Delete/GetByID :221-237、List :240-309、BatchDelete :311-316）
    - internal/services/operations/workstation_floor_code_77_03_test.go:100-160（各 map key 的行为锁：name/floorId/status/type -1 跳过/floorCode/orgId；:415 buildingId 被忽略的锁定）
    - 91-RESEARCH.md「D-05 workstation 接线清单」节（6 点改动全集）
  </read_first>
  <action>
    第一步（requests/workstation_requests.go，F2）：新增三个字段——FloorCode string 带 tag json:"floorCode"、Type *int 带 tag json:"type"、OrgID string 带 tag json:"orgId"；给现有 BuildingID 与 Code 字段补注释"保留声明；服务从不消费（workstation_floor_code_77_03_test.go:415 锁定忽略行为），typed 化后不得开始消费"。加性 JSON 变更，前端契约零变化。

    第二步（workstation_service.go 接口，D-05）：List 签名改为 (ctx, req requests.WorkstationListRequest) (*base.PageResult, error)；SearchWorkstationOptions 签名改为 (ctx, req requests.WorkstationListRequest) ([]DropdownOption, error)（复用 ListRequest，避免重复 struct）；其余六个方法签名逐字不动。接口注释标注 D-05。

    第三步（struct 组合）：workstationService 增加 repo *base.GORMRepository[models.Workstation] 字段，NewWorkstationService 中初始化为 base.NewGORMRepository[models.Workstation](db)；db 字段保留（Statistics/SearchOptions 内部/BatchUpdatePositions/Update 的 First 回填仍需直用 db）。

    第四步（六方法 repo 化）：
    - Create：applyWorkstationOccupancyLink 前置联动逐字保留，落库改 s.repo.Create(ctx, workstation)。
    - Update：First 查出现有记录回填 CreatedAt/CreatedBy 的整段业务逻辑逐字保留（0001-01-01 零值覆盖 bug 的修复，勿动），最后落库改 s.repo.Update(ctx, workstation)（repo.Update 即 Save 全量，与现状等价）。
    - Delete/BatchDelete：直接委托 s.repo.Delete(ctx, id) / s.repo.BatchDelete(ctx, ids)（现状 .Table(workstationTable) 形式与 repo 的 Model 形式对软删除行为等价；BatchDelete 的空 ids 早退可删——repo 已反转为 nil 语义）。
    - GetByID：改 s.repo.GetByID(ctx, id, s.joinScope())——repo 在 scope 非空时经 Tabler 断言生成 sys_workstation.id = ?（确认 internal/models 中 Workstation 模型的 TableName() 返回 sys_workstation，与现状 :231 的表限定逐字一致）。

    第五步（List scope 化）：新私有方法 joinScope() base.Scope 返回闭包：db.Select(workstationJoinSelect).Joins(workstationJoinClause)（两个常量逐字平移）。新私有方法 filterScope(req requests.WorkstationListRequest) base.Scope 把 :247-277 的六个条件逐字平移为闭包内 Where（条件字符串与 ? 占位符不许改写）：name → sys_workstation.workstation_name LIKE ?；floorId → sys_workstation.floor_id = ?；floorCode → 保留 validateTableName(floorTable) 白名单守卫与 EXISTS 子查询逐字（含 CAST 写法）；status → if st := req.GetStatus(-1); st >= 0（P6：nil/缺失 → -1 → 跳过，与 extractIntParam(params,"status",-1) 语义等价）；type → if req.Type != nil && *req.Type >= 0（nil/缺失 → 跳过，-1 → 跳过）；orgId → 保留 EXISTS 子查询逐字（含 ancestors LIKE 与 b.deleted_at IS NULL）。List 函数体：current, pageSize := req.GetPagination()，然后 return s.repo.List(ctx, base.PageParams{Current: current, PageSize: pageSize}, s.filterScope(req), s.joinScope(), base.SortScope(req.BaseListRequest, workstationAllowedSortFields, "sys_workstation.created_at DESC"))。

    行为等价说明（写入 List 的注释）：现状 Select+Joins 仅在 Find 前追加（:294-298），repo 化后 joinScope 前置于 Count——workstationJoinClause 全部为 N:1 LEFT JOIN 无行增殖，Count 结果不变（spike 实证 + 测试 Total 断言守护）；Count 时 Select 被临时替换为 count(*) 并恢复（spike 实证）。分页切换语义（GetPagination：上限 10000→100、current<1→1）为有意收紧，由 Task 3 checkpoint 向用户确认。

    第六步（SearchWorkstationOptions 内部）：查询形状逐字保留（Select 两列 + DropdownMaxRows LIMIT + Order），仅把 extractStringParam/extractIntParam 的 map 读取替换为 req 字段读取（name/floorId/floorCode/status GetStatus(-1)/type 同 List 规则/orgId），实现读 read_first 的当前方法体后平移。

    第七步（D-04 保留项）：Statistics（保持 map 参数与 extractStringParam(params,"orgId")）、GetWorkstationDeptOptions、BatchUpdatePositions 三方法零改动；workstationAllowedSortFields 白名单、validateTableName、applyWorkstationOccupancyLink、join 常量全部原位保留。

    第八步（清理）：workstation_service.go 内 List/SearchWorkstationOptions 不再出现 extractPagination/calculateOffset/extractSortRequest 调用（extractStringParam 仅剩 Statistics 一处合法保留）；若 fmt/strings import 因此不再使用则同步修剪。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/guoguo && go build ./... && go test ./internal/services/operations/ -run TestImp77 -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - workstation_service.go 的 WorkstationService interface 中 List 与 SearchWorkstationOptions 的参数类型为 requests.WorkstationListRequest，且不再出现 map[string]interface{} 于这两个方法
    - workstation_service.go 包含 base.GORMRepository[models.Workstation] 组合字段与 s.repo. 委托（Create/Update/Delete/GetByID/List/BatchDelete 六处）
    - workstation_service.go 不再包含 buildListQueryFromRequest / countRecords / fetchRecords 同类私有管道 helper（若有）
    - workstation_service.go 中 extractPagination、calculateOffset、extractSortRequest 的引用计数为 0；extractStringParam 仅剩 Statistics 内 1 处
    - requests/workstation_requests.go 含 FloorCode/Type/OrgID 三字段且 Type 为 *int
    - List 的 status 过滤通过 req.GetStatus(-1) 且判断 st >= 0；type 过滤判断 req.Type != nil && *req.Type >= 0
    - 注意：本 task 后 3 个测试文件尚编译失败（List 签名变更），属预期中间态，Task 2 修复；以 Task 2 的 verify 为准
  </acceptance_criteria>
  <done>service 层 typed 化 + repo 化完成，Statistics/SearchOptions/GetWorkstationDeptOptions/BatchUpdatePositions 与排序白名单零改动</done>
</task>

<task type="auto">
  <name>Task 2: handler 2 处 typed bind（降级语义）+ 3 个测试文件改写</name>
  <files>internal/api/v1/operations/workstation_handler.go, internal/services/operations/workstation_floor_code_77_03_test.go, internal/api/v1/operations/workstation_handler_test.go, internal/api/v1/operations/workstation_handler_full_test.go</files>
  <read_first>
    - internal/api/v1/operations/workstation_handler.go（List :139-152 与 SearchWorkstationOptions :90-101 的现状 map bind + 降级逻辑；其余方法零改动）
    - internal/api/v1/operations/workstation_handler_test.go（stubWorkstationService 全部 10 个方法 :30-66，其中 List :34 与 SearchWorkstationOptions :66 需改签名）
    - internal/api/v1/operations/workstation_handler_full_test.go（mockWorkstationService :34-88，其中 List :58 与 SearchWorkstationOptions :88 需改签名；ListFunc/SearchWorkstationOptionsFunc 字段类型与内联实现）
    - internal/services/operations/workstation_floor_code_77_03_test.go（~14 处 svc.List + 7 处 svc.SearchWorkstationOptions 的 map 字面量；测试意图与断言全部保持）
    - 91-PATTERNS.md「workstation_handler.go」节（typed bind 目标形状：不用 handleJSONBinding 的 400 路径）
  </read_first>
  <action>
    workstation_handler.go：List 与 SearchWorkstationOptions 两处改 typed bind——var req requests.WorkstationListRequest 后 ShouldBindJSON(&req)，err != nil 时 req = requests.WorkstationListRequest{} 后继续执行（零值 request = 空过滤全列表，与现状 bind 失败降级空 map 语义逐字对齐；不用 base_handler.handleJSONBinding，因其返回 400 与现状不符）。service 调用改传 req。其余 handler 方法（Create/Update/Delete/GetByID/BatchDelete/BatchUpdatePositions/Statistics/GetWorkstationDeptOptions）零改动。

    workstation_floor_code_77_03_test.go：全部 map 字面量按 key 机械改写为 requests.WorkstationListRequest 复合字面量——"name" → Name 字段、"floorId" → FloorID、"floorCode" → FloorCode、"orgId" → OrgID、"buildingId" → BuildingID（:415 的断言是"传了也不生效"，保持传入 + 断言不变）、"status"/"type" 数值 → 构造 *int（文件内已有或新增局部小 helper 如 intPtr(v int) *int），status:-1 用例（:145）与 type:-1 用例保持"显式跳过"语义；分页 key（current/pageSize）改写为 PaginationParams 嵌入字段 req.Current/req.PageSize（BaseListRequest 提升字段）。每个用例的期望值（Total/条数/排序）逐字不变——这些断言是行为零变更的证明器。

    workstation_handler_test.go：stubWorkstationService 的 List 签名改为 List(_ context.Context, _ requests.WorkstationListRequest) 与 SearchWorkstationOptions 同理（requests 包 import 路径与既有 opsServices import 并列补齐）；返回值类型 opsServices.PageResult 因 91-01 alias 与 base.PageResult 同一类型，无需改动。

    workstation_handler_full_test.go：mockWorkstationService 的 List/SearchWorkstationOptions 方法签名同步；struct 上的 ListFunc/SearchWorkstationOptionsFunc 函数字段类型同步为 typed 签名；测试体内对 ListFunc 的内联实现（若有按 params key 断言的）改读 req 字段；Handler 层用例的请求 JSON body 不变（加性字段零影响）。
  </action>
  <verify>
    <automated>cd D:/CODE/ClaudeCode/guoguo && go build ./... && go test ./internal/services/operations/... ./internal/api/v1/operations/... 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... 退出码 0（中间态编译错误全部消除）
    - workstation_handler.go 中 List 与 SearchWorkstationOptions 不再含 map[string]interface{} bind；两处均含 requests.WorkstationListRequest{} 降级赋值
    - workstation_floor_code_77_03_test.go 中 svc.List(ctx, map 与 svc.SearchWorkstationOptions(ctx, map 调用计数为 0（grep 验证）
    - go test ./internal/services/operations/ -run TestImp77 全部 PASS（含 :145 status:-1 跳过、:415 buildingId 忽略两个行为锁）
    - go test ./internal/api/v1/operations/ -run Workstation 全部 PASS
    - go test ./internal/services/operations/... 全包 0 失败
  </acceptance_criteria>
  <done>pilot 全链路（requests → service → handler → 3 测试文件）typed 化完成，既有行为断言全绿，模式定型可供 91-03/91-04 复制</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: workstation 分页语义收紧确认（A2）</name>
  <files>internal/api/v1/operations/workstation_handler.go, internal/services/operations/workstation_service.go（本 checkpoint 校验对象为 Task 1/2 产物，无新增文件）</files>
  <action>无实现动作——阻塞等待用户确认分页语义收紧（pageSize 上限 10000→100、current<1 回退 1）后方可视为 pilot 收口。</action>
  <done>用户回复 approved（或提出的问题已在后续处理），workstation pilot 语义收紧获得显式确认。</done>
  <verify>
    <human-check>用户确认两项运行时收紧可接受：workstation List pageSize 上限 10000→100；current<1 回退 1（负 offset 缺陷修复）。验证步骤见 how-to-verify。</human-check>
  </verify>
  <what-built>
    workstation List/SearchOptions 的分页归一化从 map 路径（extractPagination + clampPageSize：下限 10、上限 MaxOptionsPageSize=10000、current 不守卫）切换到 typed 路径（PaginationParams.GetPagination：下限 10、上限 MaxListPageSize=100、current<1 回退 1）。这是 D-05 typed 化的两处有意运行时语义收紧：(1) pageSize 上限 10000→100——前端常规列表页发送 ≤100，实际无感，但依赖大 pageSize 拉全量的下游脚本会被截断到 100；(2) current=0 或负数现在回退 1——修复现状会产生负 offset 的潜在缺陷（latent bug，从未触发过 SQL 错误）。另有一处微小变化：畸形 status/type 值（如非整数字符串）从静默强转变为绑定失败 → 降级零值请求（过滤全跳过）。
  </what-built>
  <how-to-verify>
    1. cd D:/CODE/ClaudeCode/guoguo && go test ./internal/services/operations/ -run TestImp77 -v —— 14 个行为锁全绿
    2. 确认业务侧没有依赖 pageSize > 100 拉取 workstation 全量的调用方（前端分页组件常规发送 ≤100；若有导出/同步脚本依赖大 pageSize 请指出）
    3. 接受两项收紧：pageSize 上限 100、current<1 回退 1（负 offset 缺陷修复）
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端 JSON → workstation_handler List/SearchOptions | typed bind 引入新的反序列化路径；bind 失败走降级而非 400 |
| service scope → SQL | filter 平移涉及 EXISTS 子查询与 CAST 写法，逐字平移是注入防线 |
| 分页参数 → repo（直信入参） | GetPagination clamp 是唯一守卫，上限收紧 100 是本次 checkpoint 主体 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-02-01 | Tampering | 分页 clamp 语义收紧（10000→100）+ current 守卫 | mitigate | 方向为收紧与修复（负 offset latent bug），checkpoint:human-verify 显式确认（RESEARCH A2）；测试锁定 ≤100 常规路径行为不变 |
| T-91-02-02 | Tampering | filter→scope 平移中的 SQL 注入面 | mitigate | 六个条件字符串与 ? 占位符逐字平移，禁止改写；floorCode 分支保留 validateTableName 白名单守卫；orgId EXISTS 子查询逐字含 deleted_at 过滤；排序经 base.SortScope 白名单（91-01 契约 6） |
| T-91-02-03 | Repudiation / 行为漂移 | handler bind 失败降级语义 | mitigate | 保留零值请求继续查询（与现状空 map 一致），不引入 400 行为变更；stub/mock 测试覆盖 bind 路径 |
| T-91-02-04 | Information Disclosure | type 字段 typed 化后误开始消费 buildingId/code | mitigate | 两字段注释锁定"服务从不消费"，:415 测试用例（传 buildingId 断言不生效）作为回归守卫保留 |
| T-91-02-SC | Tampering | npm/pip/cargo installs | N/A | 零外部包安装 |
</threat_model>

<verification>
- go build ./... 退出码 0
- go test ./internal/services/operations/... 0 失败（TestImp77 工位行为锁 14 用例为 pilot 行为零变更的直接证据）
- go test ./internal/api/v1/operations/... 0 失败（handler bind/响应路径 = 端到端 smoke 的测试口径，RESEARCH Validation Architecture）
- grep 验证 workstation_service.go 无 extractPagination/calculateOffset/extractSortRequest 残留
- 全量防线：go test ./... （AST 锁值 + operlog 回归 + 1688+ tests 零回归）
</verification>

<success_criteria>
1. WorkstationService 六方法管道全部经 base.GORMRepository[models.Workstation]，CRUD 模板清零（D-05 + CRUD-REUSE-02）
2. status/type -1 跳过、buildingId 忽略、bind 降级三个语义锁全绿
3. 前端 JSON 契约零变更（仅 +3 可选字段）
4. 分页语义收紧经用户 checkpoint 确认
5. workstation 消费点清零达成（extractPagination/calculateOffset/extractSortRequest 在该文件零引用）
</success_criteria>

<output>
按 execute-plan 工作流原子 commit：feat(91-02): migrate workstation_service to base.GORMRepository with typed requests (D-05)
</output>
