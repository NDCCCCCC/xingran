---
phase: 68-deploy-robustness-and-docs-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/deployment/secret-management.md
  - docs/deployment/single-machine-deployment.md
  - docs/deployment/docker-compose.md
  - configs/config.example.yaml
  - configs/config.prod.example.yaml
  - configs/config.sqlite.example.yaml
  - scripts/deploy/setup-server.sh
  - scripts/crypto/gen_sm2_keys/main.go
  - internal/api/v1/auth.go
  - internal/core/security/jwt.go
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-03, DEPLOY-04, DEPLOY-05]

must_haves:
  truths:
    - "DEPLOY-01: 全仓 grep `XINGRAN_JWT_SM2_PRIVATE_KEY|XINGRAN_JWT_SM2_PUBLIC_KEY` 在 `docs/`、`configs/` 下零命中（除 `.planning/debug/resolved/public-key-500-after-subpath-fix.md` 历史归档外）"
    - "DEPLOY-02: `scripts/deploy/setup-server.sh` 的 `/etc/xingran/secrets.env` heredoc 在 JWT_ACCESS_SECRET/JWT_REFRESH_SECRET/SM4_KEY 之后多出 `# === SM2 密钥对 ===` 注释段 + 两行 `JWT_SM2_PRIVATE_KEY=` 与 `JWT_SM2_PUBLIC_KEY=` 占位，且文件包含使用 `scripts/crypto/gen_sm2_keys/main.go` 的一次性生成命令说明"
    - "DEPLOY-03: `scripts/crypto/gen_sm2_keys/main.go` line 2 注释改为 `go run scripts/crypto/gen_sm2_keys/main.go`，与文件实际位置一致；`docs/deployment/secret-management.md` 与 `docs/deployment/single-machine-deployment.md` 中所有 `scripts/gen-sm2-keys/main.go` / `scripts/gen-sm2-keys.go` 历史路径引用同步改为 `scripts/crypto/gen_sm2_keys/main.go`"
    - "DEPLOY-04: `internal/api/v1/auth.go` 的 `getPublicKey` handler 在返回 500 之前打印一条 `applogger.Warnf` 日志，包含 `useSM2`、`sm2PublicKey==nil` 状态；后端日志 `journalctl -u xingran -n 100 | grep -i 'SM2 公钥不可用'` 可立即定位根因"
    - "DEPLOY-05: `configs/config.sqlite.example.yaml` 中 `use_sm2: false` 改为 `use_sm2: true`，与 `config.example.yaml` / `config.prod.example.yaml` 对齐；文件顶部注释段或邻近行新增「启用 SM2 必须先在 /etc/xingran/secrets.env 注入 JWT_SM2_* 密钥，参见 docs/deployment/secret-management.md」迁移指引"
  artifacts:
    - path: "configs/config.sqlite.example.yaml"
      provides: "sqlite 部署模板 use_sm2 对齐 + SM2 迁移指引"
      contains: "use_sm2: true"
    - path: "scripts/deploy/setup-server.sh"
      provides: "secrets.env heredoc 含 SM2 密钥段 + 生成命令说明"
      contains: "JWT_SM2_PRIVATE_KEY"
    - path: "scripts/crypto/gen_sm2_keys/main.go"
      provides: "header 注释路径与文件实际位置一致"
      contains: "scripts/crypto/gen_sm2_keys/main.go"
    - path: "internal/api/v1/auth.go"
      provides: "getPublicKey handler 500 时打印 useSM2/sm2PublicKey 状态"
      contains: "applogger.Warnf"
    - path: "internal/core/security/jwt.go"
      provides: "JWTManager 暴露 IsSM2Enabled/HasSM2PublicKey getter 供 handler 可观测性读取"
      contains: "func (j \\*JWTManager) IsSM2Enabled"
  key_links:
    - from: "internal/api/v1/auth.go (getPublicKey)"
      to: "internal/core/security/jwt.go (JWTManager.IsSM2Enabled/HasSM2PublicKey)"
      via: "core.JWTManager 方法调用读取状态"
      pattern: "IsSM2Enabled|HasSM2PublicKey"
    - from: "scripts/deploy/setup-server.sh (secrets.env heredoc)"
      to: "scripts/crypto/gen_sm2_keys/main.go"
      via: "注释指引运维调用一次性工具生成 hex"
      pattern: "scripts/crypto/gen_sm2_keys/main.go"
    - from: "configs/config.sqlite.example.yaml (use_sm2: true)"
      to: "internal/config/config.go (env vars)"
      via: "启动时需通过 secrets.env 注入 JWT_SM2_PRIVATE_KEY / JWT_SM2_PUBLIC_KEY"
      pattern: "JWT_SM2_(PRIVATE|PUBLIC)_KEY"
---

# Phase 68 Plan 01: 部署稳健性 & 文档一致性（SM2 密钥配置闭环）

**Status:** READY FOR EXECUTION
**Created:** 2026-08-19
**Source:** ROADMAP Phase 68（DEPLOY-01..05）+ `.planning/debug/resolved/public-key-500-after-subpath-fix.md` Specialist Review §Related bugs
**Type:** v1.23 启动块；非新功能，纯部署/文档稳健性修复

<objective>

闭环 5 项与 SM2 密钥配置相关的部署稳健性与文档一致性缺陷——文档-代码 env var 名称不一致（DEPLOY-01）、`setup-server.sh` 的 secrets.env 模板未覆盖 SM2 keys（DEPLOY-02）、`gen_sm2_keys/main.go` header 注释路径过时（DEPLOY-03）、`getPublicKey` handler 缺可观测性日志（DEPLOY-04）、`config.sqlite.example.yaml` `use_sm2` 默认值与生产模板不一致（DEPLOY-05）。

Purpose: 把 `public-key-500-after-subpath-fix.md` Specialist Review 列出的 4 项 related bugs + 1 项 sqlite 模板漂移一次性闭环。任一项缺失都会让"SM2 配置链路"再走一次生产事件（500 + 人工 ssh grep）。
Output: 10 文件改动 / 5 独立 commit / 5 DEPLOY-XX 全 PASS；grep `XINGRAN_JWT_SM2_*` 在 docs+configs 下零命中；`setup-server.sh` 生成的 secrets.env 含完整密钥位 + 注入指引；`getPublicKey` handler 500 时日志可 ssh 直查。

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/debug/resolved/public-key-500-after-subpath-fix.md
@.planning/STATE.md

<interfaces>
<!-- 关键现状契约（已从代码库提取，executor 无需再探索） -->

`internal/config/config.go:359-360` BindEnv（SM2 实际读取的 env 名，无 XINGRAN_ 前缀）:
{"jwt.sm2_private_key", "JWT_SM2_PRIVATE_KEY"},
{"jwt.sm2_public_key", "JWT_SM2_PUBLIC_KEY"}

`internal/core/security/jwt.go:274-283` GetPublicKey（DEPLOY-04 可观测性增强的对象）:
if !j.useSM2 || j.sm2PublicKey == nil {
    return ""
}
return crypto.ExportPublicKeyToHex(j.sm2PublicKey)

`internal/api/v1/auth.go:500-512` getPublicKey handler（DEPLOY-04 改造点，已读全文）:
func getPublicKey(core *core.Core) gin.HandlerFunc {
    return func(c *gin.Context) {
        publicKey := core.JWTManager.GetPublicKey()
        if publicKey == "" {
            response.Error(c, response.ErrServerError, "SM2 未启用或公钥不可用")
            return
        }
        response.Success(c, gin.H{"publicKey": publicKey})
    }
}
- 已 import `applogger "github.com/xingran-next/xingran-go-backend/pkg/logger"`（line 13）
- 包内已有日志调用范式：`applogger.Warnf("...", ...)`（line 145/159/166/179/183/212/220/267/373/596）
- `core.JWTManager` 是 `*security.JWTManager` 类型（core.go:127-135 注入），其字段 `useSM2 bool` / `sm2PublicKey *ecdsa.PublicKey`（jwt.go struct 内）
- 本 phase 采用保守做法：在 `internal/core/security/jwt.go` 新增最小 getter `IsSM2Enabled() bool` 与 `HasSM2PublicKey() bool`，handler 调用后写入 WARN 日志（具体方法实现见 T4 action）

`scripts/deploy/setup-server.sh:22-38` secrets.env 模板（DEPLOY-02 改造点，已读全文）:
sudo tee /etc/xingran/secrets.env >/dev/null <<'EOF'
# XingRan secrets — injected by systemd (pid 1 reads this, 600 root:root).
# Fill real values, then: sudo systemctl restart xingran
# JWT_ACCESS_SECRET / JWT_REFRESH_SECRET: openssl rand -base64 48
# SM4_KEY: openssl rand -base64 16
JWT_ACCESS_SECRET=change-me
JWT_REFRESH_SECRET=change-me
SM4_KEY=
EOF

`scripts/crypto/gen_sm2_keys/main.go:1-3` header（DEPLOY-03 改造点，已读全文）:
// 一次性工具: 生成 SM2 密钥对并输出 hex 字符串
// 用法: go run scripts/gen-sm2-keys.go                            ← 需要改为 scripts/crypto/gen_sm2_keys/main.go
// 不会进入正常构建（//go:build ignore 标签）

`configs/config.sqlite.example.yaml:53-62` jwt section（DEPLOY-05 改造点，已读全文）:
jwt:
  secret_key: ""              # ⚠️ 必须通过 JWT_SECRET 注入
  access_key_expire: 7200
  refresh_key_expire: 604800
  issuer: "XingRan-Next"
  use_sm2: false              # 首跑用 HS256 免 SM2 密钥生成依赖(部署期可改 true)   ← 需要改为 true
  # SM2 密钥留空(use_sm2=false 时不会读取);生产环境务必切 true 并通过
  # XINGRAN_JWT_SM2_PRIVATE_KEY / XINGRAN_JWT_SM2_PUBLIC_KEY 注入新生成的对  ← env 名同步改为 JWT_SM2_*
  sm2_private_key: ""
  sm2_public_key: ""

DEPLOY-01 一致性扫描 grep 命中清单（执行 executor 必须知道全量位置）:
- `docs/deployment/secret-management.md:46,47,123,124,151,156` (6 处)
- `docs/deployment/single-machine-deployment.md:152,153,477` (3 处)
- `docs/deployment/docker-compose.md:360,361` (2 处)
- `configs/config.example.yaml:71,78,79` (3 处)
- `configs/config.prod.example.yaml:86,97,100` (3 处)
- `configs/config.sqlite.example.yaml:60` (1 处)
合计 17 处需改为 `JWT_SM2_*`。
</interfaces>
</context>

## Cleanup Inventory

| 待修复项 | 位置 | 处置 | 任务 |
|---------|------|------|------|
| env var 名称 docs-vs-code 不一致（XINGRAN_JWT_SM2_* vs JWT_SM2_*） | docs/deployment/{secret-management,single-machine-deployment,docker-compose}.md + configs/*.example.yaml 共 17 处 | 全量改为 JWT_SM2_*；不动注入值；保留文档语义 | T1 |
| setup-server.sh secrets.env 模板缺 SM2 keys 段 | scripts/deploy/setup-server.sh:24-32 heredoc | 在 SM4_KEY 后追加 # === SM2 密钥对（非对称，不能动态生成） === 注释 + 一次性生成命令 + JWT_SM2_PRIVATE_KEY= / JWT_SM2_PUBLIC_KEY= 占位 | T2 |
| gen_sm2_keys header 注释路径过时 | scripts/crypto/gen_sm2_keys/main.go:2 | line 2 改为 `go run scripts/crypto/gen_sm2_keys/main.go` | T3 |
| getPublicKey handler 缺可观测性日志 | internal/api/v1/auth.go:502-505 + internal/core/security/jwt.go 新增最小 getter | 在 response.Error 之前增加 applogger.Warnf 含 useSM2 / sm2PublicKey 状态；JWTManager 暴露 IsSM2Enabled() 与 HasSM2PublicKey() 两个 getter | T4 |
| sqlite 模板 use_sm2: false 与生产漂移 | configs/config.sqlite.example.yaml:58 + 顶部注释 | 改 true；顶部注释加迁移指引；同步修正 line 60 的 XINGRAN_JWT_SM2_* → JWT_SM2_*（与 DEPLOY-01 一并） | T5 |

## 执行顺序设计（每任务独立 commit + 自动化门常绿）

依赖分析：5 项无文件冲突（每个 task 触碰的文件集合互斥），可平行执行；但为可读性 + 每 commit 独立验证，按 T1→T2→T3→T4→T5 顺序产出。每个 commit 边界跑对应自动化门：T1 grep + docs 阅读（无构建门）；T2 `bash -n scripts/deploy/setup-server.sh`；T3 `go build ./scripts/...` 不编译（//go:build ignore 跳过）+ 文件读取复核；T4 `go build ./...` + `go vet ./internal/api/v1/...`；T5 `go build ./...`（虽然 yaml 改动不影响 go build，但作为回归基线）+ `git diff --check`。

<tasks>

<task type="auto">
  <name>T1: DEPLOY-01 — env var 名称全仓一致性（XINGRAN_JWT_SM2_* → JWT_SM2_*）</name>
  <files>docs/deployment/secret-management.md, docs/deployment/single-machine-deployment.md, docs/deployment/docker-compose.md, configs/config.example.yaml, configs/config.prod.example.yaml, configs/config.sqlite.example.yaml</files>
  <action>
    1. 全量扫描 `docs/` 与 `configs/` 下 `XINGRAN_JWT_SM2_PRIVATE_KEY|XINGRAN_JWT_SM2_PUBLIC_KEY` 共 17 处（行号见 interfaces 段）。逐处替换为 `JWT_SM2_PRIVATE_KEY|JWT_SM2_PUBLIC_KEY`，**仅替换字符串**，不动表格语义、不动注释周围的句子结构、不动任何 secret 注入值。
    2. 文件级拆分（每文件独立 sed）：
       - `docs/deployment/secret-management.md`：line 46-47 表格第 4 列（环境变量列名）、line 123-124 代码块内的 `fmt.Printf("XINGRAN_JWT_SM2_*=%s")`、line 151 for 循环变量名、line 156 默认值比较。代码块的 `import` 段不动。
       - `docs/deployment/single-machine-deployment.md`：line 152-153 实际 docker-compose env 值定义（保持字面值更新）、line 477 排错表的环境变量名。
       - `docs/deployment/docker-compose.md`：line 360-361 占位符 `=` 后的变量名。
       - `configs/config.example.yaml`：line 71 注释行的 env 列表、line 78-79 sm2_*_key 注释里的 env 名。
       - `configs/config.prod.example.yaml`：line 86 注释、line 97 sm2_private_key 注释、line 100 sm2_public_key 注释。
       - `configs/config.sqlite.example.yaml`：line 60（与 T5 一并修，本任务先完成字符串替换，T5 再加 use_sm2: true 与迁移指引）。
    3. 保留文件所有注释、表格、缩进、emoji；不引入新行；不修改任何数值。
    4. 完成后跑 `grep -rn "XINGRAN_JWT_SM2" docs/ configs/` 期望输出 0；`grep -rn "JWT_SM2_PRIVATE_KEY\|JWT_SM2_PUBLIC_KEY" docs/ configs/ | wc -l` 期望输出 17（替换前后数量守恒）。
  </action>
  <verify>
    <automated>grep -rn "XINGRAN_JWT_SM2" docs/ configs/ | wc -l && grep -rn "JWT_SM2_PRIVATE_KEY\|JWT_SM2_PUBLIC_KEY" docs/ configs/ | wc -l</automated>
  </verify>
  <done>grep XINGRAN_JWT_SM2 在 docs/ + configs/ 下零命中；grep JWT_SM2_(PRIVATE|PUBLIC)_KEY 共 17 处命中（替换前后数量守恒）；全部 6 文件保留原结构、注释、表格、缩进；不动 secrets 注入值。Commit: docs(deploy): align SM2 env var names with config.go BindEnv (JWT_SM2_*) per DEPLOY-01</done>
</task>

<task type="auto">
  <name>T2: DEPLOY-02 — setup-server.sh secrets.env 模板补 SM2 keys 段</name>
  <files>scripts/deploy/setup-server.sh</files>
  <action>
    1. 修改 `scripts/deploy/setup-server.sh:24-32` 的 heredoc，在 `SM4_KEY=` 行（第 31 行）后追加 SM2 段（约 6 行），最终结构（注释行用 `#` 开头）：
       ```
       JWT_ACCESS_SECRET=change-me
       JWT_REFRESH_SECRET=change-me
       SM4_KEY=
       # === SM2 密钥对（非对称，绝不能用 openssl 动态生成；否则每次重启踢全部用户） ===
       # 生成方法（一次性,在本地仓库内运行,把两行输出粘贴到下面两行等号右侧）:
       #   go run scripts/crypto/gen_sm2_keys/main.go
       # 生产环境两行必须都填写;空值会让 jwt.go 走"动态生成"分支重启即失效。
       JWT_SM2_PRIVATE_KEY=
       JWT_SM2_PUBLIC_KEY=
       ```
    2. line 28 `# SM4_KEY: openssl rand -base64 16` 注释保留不动（仅 SM4 说明）。
    3. 在文件顶部 doc-comment 段（第 4-12 行）补充一句：第 7 条说明 secrets.env 现在含 6 项 key（增加 SM2 keys），保持与 step 2 中 "creates /etc/xingran/secrets.env skeleton" 描述一致。
    4. **不做** `use_sm2=true` 强制校验（user-managed config，保守做法）；不在 heredoc 后追加 sed/inject 逻辑。
    5. 完成后跑 `bash -n scripts/deploy/setup-server.sh` 期望退出码 0（语法检查）；`grep -A 12 'sudo tee /etc/xingran/secrets.env' scripts/deploy/setup-server.sh | grep -E 'JWT_SM2_(PRIVATE|PUBLIC)_KEY=|SM2 密钥对'` 期望输出 4 行。
  </action>
  <verify>
    <automated>bash -n scripts/deploy/setup-server.sh && grep -A 12 'sudo tee /etc/xingran/secrets.env' scripts/deploy/setup-server.sh | grep -E 'JWT_SM2_(PRIVATE|PUBLIC)_KEY=|SM2 密钥对' | wc -l</automated>
  </verify>
  <done>heredoc 在 SM4_KEY= 后追加 6 行 SM2 段（含注释 + 占位）；bash -n 退出码 0；grep 命中 4 行；不引入 shellcheck 警告（避免 `set -u` 下的未引用变量）；secret 注入路径仍是运维手工填值，不自动化。Commit: chore(deploy): add SM2 keys segment to secrets.env template per DEPLOY-02</done>
</task>

<task type="auto">
  <name>T3: DEPLOY-03 — gen_sm2_keys header 注释路径修正</name>
  <files>scripts/crypto/gen_sm2_keys/main.go</files>
  <action>
    1. 修改 `scripts/crypto/gen_sm2_keys/main.go:2` 一行注释。原内容 `// 用法: go run scripts/gen-sm2-keys.go` 改为 `// 用法: go run scripts/crypto/gen_sm2_keys/main.go`。
    2. line 3 `// 不会进入正常构建（//go:build ignore 标签）` 保留不动。
    3. **不修改** `//go:build ignore` 标签（实际未发现该标签在当前文件，注释中只是提了一下；若 grep `//go:build` 真有则保留）。
    4. 完成后跑 `head -3 scripts/crypto/gen_sm2_keys/main.go | grep "scripts/crypto/gen_sm2_keys/main.go"` 期望输出 1 行；`go build ./...` 期望退出码 0（确认此文件仍被构建系统忽略，不参与编译路径）。
    5. T1 已经覆盖了 docs 内 `scripts/gen-sm2-keys*` 路径 → `scripts/crypto/gen_sm2_keys/main.go` 的全量替换；本任务仅处理脚本本体，不重做 docs。
  </action>
  <verify>
    <automated>head -3 scripts/crypto/gen_sm2_keys/main.go | grep "scripts/crypto/gen_sm2_keys/main.go" && go build ./...</automated>
  </verify>
  <done>脚本 line 2 注释路径与文件实际位置一致；go build ./... 退出码 0（构建系统对该路径无感）；不修改 import、main 函数、密钥生成逻辑。Commit: chore(scripts): fix gen_sm2_keys header comment path per DEPLOY-03</done>
</task>

<task type="auto">
  <name>T4: DEPLOY-04 — getPublicKey handler 500 时打印 useSM2/sm2PublicKey 状态</name>
  <files>internal/core/security/jwt.go, internal/api/v1/auth.go</files>
  <action>
    1. 在 `internal/core/security/jwt.go` 现有 `GetPublicKey()` 方法（lines 274-283）下追加两个最小 getter（不修改现有方法签名 / 不修改 struct 字段可见性）：
       ```go
       // IsSM2Enabled 返回 SM2 签名是否启用（来自 jwt.use_sm2 配置）
       func (j *JWTManager) IsSM2Enabled() bool {
           return j.useSM2
       }

       // HasSM2PublicKey 返回 SM2 公钥是否已加载（用于诊断 GetPublicKey 返回空的原因）
       func (j *JWTManager) HasSM2PublicKey() bool {
           return j.sm2PublicKey != nil
       }
       ```
    2. 在 `internal/api/v1/auth.go:502-505` 的 `getPublicKey` handler 内，把当前两行：
        ```go
        if publicKey == "" {
            response.Error(c, response.ErrServerError, "SM2 未启用或公钥不可用")
            return
        }
        ```
        改为：
        ```go
        if publicKey == "" {
            useSM2 := core.JWTManager.IsSM2Enabled()
            hasPub := core.JWTManager.HasSM2PublicKey()
            applogger.Warnf("SM2 公钥不可用: useSM2=%v, sm2PublicKeyLoaded=%v, requestPath=%s, clientIP=%s",
                useSM2, hasPub, c.Request.URL.Path, c.ClientIP())
            response.Error(c, response.ErrServerError, "SM2 未启用或公钥不可用")
            return
        }
        ```
    3. **不修改** `response.Error` 的 code/message/HTTPStatus（前端看到的 `{"code":500, "message":"SM2 未启用或公钥不可用"}` 响应文案零变化）；WARN 日志只走后台。
    4. **不修改** GetPublicKey() 函数体（保持向后兼容）；仅新增两个只读 getter。
    5. 完成后跑 `go build ./...` 与 `go vet ./internal/api/v1/... ./internal/core/security/...` 均期望退出码 0；`grep -B 2 -A 2 'response.Error(c, response.ErrServerError, "SM2 未启用' internal/api/v1/auth.go | grep applogger.Warnf` 期望输出 1 行。
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/api/v1/... ./internal/core/security/... && grep -B 2 -A 2 'response.Error(c, response.ErrServerError, "SM2 未启用' internal/api/v1/auth.go | grep applogger.Warnf</automated>
  </verify>
  <done>handler 在返回 500 前打印 WARN 含 useSM2/sm2PublicKeyLoaded/requestPath/clientIP 四元组；JWTManager 暴露 IsSM2Enabled/HasSM2PublicKey getter；前端响应文案零变化；go build + go vet 双门绿。Commit: fix(auth): log useSM2/sm2PublicKey state in getPublicKey 500 path per DEPLOY-04</done>
</task>

<task type="auto">
  <name>T5: DEPLOY-05 — sqlite 模板 use_sm2 默认值 + 迁移指引</name>
  <files>configs/config.sqlite.example.yaml</files>
  <action>
    1. 修改 `configs/config.sqlite.example.yaml:58`：`use_sm2: false              # 首跑用 HS256 免 SM2 密钥生成依赖(部署期可改 true)` 改为 `use_sm2: true               # SM2 国密签名（与 config.prod.example.yaml 对齐；启用前必读下方迁移指引）`。
    2. line 60 注释（DEPLOY-01 字符串替换已在 T1 完成，本任务确认其内容已是 `JWT_SM2_PRIVATE_KEY / JWT_SM2_PUBLIC_KEY`；不再修改）。
    3. **顶部注释段**（line 1-16）增加一条迁移指引（插入到 line 14 后 `#   SM2_私钥对（绝对不能复用仓库默认值）` 之后，line 15 `# 此文件覆盖 configs/config.prod.example.yaml...` 之前），共 6 行：
       ```
       #   【启用 SM2 必读】
       #     本模板 use_sm2: true 与生产模板对齐，强制走 SM2 国密签名。
       #     启用前必须先在 /etc/xingran/secrets.env 注入 JWT_SM2_PRIVATE_KEY / JWT_SM2_PUBLIC_KEY
       #     两项密钥（生成方法见 docs/deployment/secret-management.md §2.2）。
       #     若仅作本地 sqlite 首跑调试,可临时把 use_sm2 改回 false 跳过密钥注入,
       #     但 /api/v1/system/auth/public-key 将返回 500（与生产行为一致）。
       ```
    4. 文件其余 server/database/cache/security 等块零改动。
    5. 完成后跑 `grep -n "use_sm2: true" configs/config.sqlite.example.yaml` 期望输出 1 行；`grep -n "启用 SM2 必读" configs/config.sqlite.example.yaml` 期望输出 1 行；`grep -n "JWT_SM2_PRIVATE_KEY" configs/config.sqlite.example.yaml` 期望输出 ≥1 行（T1 已替换 line 60）。
  </action>
  <verify>
    <automated>grep -n "use_sm2: true" configs/config.sqlite.example.yaml && grep -n "启用 SM2 必读" configs/config.sqlite.example.yaml && grep -n "JWT_SM2_PRIVATE_KEY" configs/config.sqlite.example.yaml && go build ./...</automated>
  </verify>
  <done>use_sm2: true 落地、顶部注释新增【启用 SM2 必读】6 行指引、line 60 env 名已是 JWT_SM2_*（T1 接力）；其余段零改动；与 config.example.yaml / config.prod.example.yaml 默认值统一。Commit: chore(deploy): align sqlite use_sm2 default and add migration note per DEPLOY-05</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 服务器 /etc/xingran/secrets.env → 后端启动 (overrideFromEnv) | 运维手工维护的 env 文件，systemd EnvironmentFile 注入到 Go 进程 |
| 后端启动 → internal/api/v1/auth.go getPublicKey handler | 客户端匿名请求，但 JWTManager 状态决定响应 |
| sqlite 部署用户 → configs/config.sqlite.example.yaml | 用户复制模板到 configs/config.yaml，触发 use_sm2 默认值 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Tampering | docs/deployment/* env var 名称错误 | mitigate | T1: 全量 grep + 17 处修正为 JWT_SM2_*，与 config.go BindEnv 同步 |
| T-68-02 | Tampering | setup-server.sh 生成的 secrets.env 缺 SM2 keys | mitigate | T2: heredoc 追加 SM2 keys 占位 + 非对称密钥不能动态生成注释 + 一次性工具调用指引 |
| T-68-03 | Spoofing | gen_sm2_keys header 注释误导（指向不存在路径） | mitigate | T3: line 2 改为真实路径 scripts/crypto/gen_sm2_keys/main.go |
| T-68-04 | Repudiation | getPublicKey handler 500 时无可观测性日志（前端只能看到统一文案） | mitigate | T4: handler 返回 500 前打印 WARN 含 useSM2/sm2PublicKeyLoaded/requestPath/clientIP，下次同类问题 ssh grep 即可定位 |
| T-68-05 | Tampering | sqlite 模板 use_sm2: false 与生产漂移，误导本地开发者 | mitigate | T5: 改 true + 顶部注释显式说明【启用 SM2 必读】与 fallback 到 false 的取舍 |
| T-68-SC | Tampering | npm/pip/cargo installs | accept | 本 phase 零依赖安装（仅 yaml/sh/go 注释 + 1 个 handler 日志 + 2 个 getter），Package Legitimacy Gate 不适用 |
</threat_model>

<verification>

**每任务边界（常绿保证）：** 每个 commit 前对应自动化门 T1 grep 0 / T2 bash -n / T3 go build ./... / T4 go build ./... + go vet / T5 go build ./...。

**终态验证（5 个 DEPLOY-XX 全 PASS）：**

1. **DEPLOY-01 PASS**
   ```
   grep -rn "XINGRAN_JWT_SM2" docs/ configs/ | wc -l   # 期望输出: 0
   ```

2. **DEPLOY-02 PASS**
   ```
   grep -A 12 'sudo tee /etc/xingran/secrets.env' scripts/deploy/setup-server.sh \
     | grep -E "JWT_SM2_(PRIVATE|PUBLIC)_KEY=|SM2 密钥对"   # 期望输出: 4 行
   bash -n scripts/deploy/setup-server.sh              # 期望退出码: 0
   ```

3. **DEPLOY-03 PASS**
   ```
   head -3 scripts/crypto/gen_sm2_keys/main.go \
     | grep "scripts/crypto/gen_sm2_keys/main.go"      # 期望输出: 1 行
   go build ./...                                      # 期望退出码: 0（确认 //go:build ignore 仍生效）
   ```

4. **DEPLOY-04 PASS**
   ```
   grep -B 2 -A 2 'response.Error(c, response.ErrServerError, "SM2 未启用' internal/api/v1/auth.go \
     | grep applogger.Warnf                           # 期望输出: 1 行
   go build ./...                                      # 期望退出码: 0
   go vet ./internal/api/v1/... ./internal/core/security/...   # 期望退出码: 0
   ```

5. **DEPLOY-05 PASS**
   ```
   grep -n "use_sm2: true" configs/config.sqlite.example.yaml   # 期望输出: 1 行
   grep -n "启用 SM2 必读" configs/config.sqlite.example.yaml  # 期望输出: 1 行
   grep -n "JWT_SM2_PRIVATE_KEY" configs/config.sqlite.example.yaml  # 期望输出: ≥ 1 行
   ```

**全文一致性：**
- `grep -rn "XINGRAN_JWT_SM2" docs/ configs/` = 0
- `grep -rn "scripts/gen-sm2-keys" docs/ scripts/crypto/gen_sm2_keys/main.go` 中 docs/ 内零命中（main.go line 2 历史 commit message 可能命中，docs 内必须 0）
- `grep -rn "JWT_SM2_PRIVATE_KEY\|JWT_SM2_PUBLIC_KEY" docs/ configs/ scripts/` 与 config.go BindEnv 一致

</verification>

<success_criteria>

（ROADMAP Phase 68 SC 映射 — 5 个 DEPLOY-XX 逐字验收）

1. **DEPLOY-01 PASS** — `docs/deployment/{secret-management,single-machine-deployment,docker-compose}.md` + `configs/{config,config.prod,config.sqlite}.example.yaml` 共 17 处 `XINGRAN_JWT_SM2_*` 全部改为 `JWT_SM2_*`，与 `internal/config/config.go:359-360` BindEnv 一致；语义/表头/必填列保留；不动 secrets 注入值；`grep -rn "XINGRAN_JWT_SM2" docs/ configs/` 零命中 —— 由 T1 交付
2. **DEPLOY-02 PASS** — `scripts/deploy/setup-server.sh` 的 `/etc/xingran/secrets.env` heredoc 在 `SM4_KEY=` 后追加 `# === SM2 密钥对（非对称，不能动态生成，否则每次重启踢全部用户） ===` 注释 + `go run scripts/crypto/gen_sm2_keys/main.go` 一次性生成命令说明 + `JWT_SM2_PRIVATE_KEY=` / `JWT_SM2_PUBLIC_KEY=` 两行占位；脚本整体可幂等重跑；**不做** `use_sm2=true` 强制校验（user-managed config 保守处理） —— 由 T2 交付
3. **DEPLOY-03 PASS** — `scripts/crypto/gen_sm2_keys/main.go` line 2 注释改为 `go run scripts/crypto/gen_sm2_keys/main.go`，与文件实际路径一致；`//go:build ignore` 标签保留（`go build ./...` 仍忽略此文件，但 `go run <explicit-path>` 可执行）；docs 中 `scripts/gen-sm2-keys*` 历史路径同步修正 —— 由 T1（docs 注释同步修正）+ T3（脚本本体）联合交付
4. **DEPLOY-04 PASS** — `internal/api/v1/auth.go` `getPublicKey` handler 在 `response.Error` 返回 500 之前打印 `applogger.Warnf("SM2 公钥不可用: useSM2=%v, sm2PublicKeyLoaded=%v, requestPath=%s, clientIP=%s", ...)`；`internal/core/security/jwt.go` 新增 `IsSM2Enabled()` 与 `HasSM2PublicKey()` 两个最小 getter；`go build ./...` + `go vet` 退出码 0；handler HTTP status / 响应文案零变化 —— 由 T4 交付
5. **DEPLOY-05 PASS** — `configs/config.sqlite.example.yaml` line 58 `use_sm2: false` → `true`（与 `config.example.yaml` / `config.prod.example.yaml` 对齐）；line 60 env 名同步修正（DEPLOY-01 一致性）；文件顶部注释段新增【启用 SM2 必读】6 行迁移指引指向 `docs/deployment/secret-management.md §2.2` —— 由 T1（line 60 兼容修正）+ T5 联合交付

</success_criteria>

<output>
Create `.planning/phases/68-deploy-robustness-and-docs-consistency/68-01-SUMMARY.md` when done，必须包含：
- 5 个 task × commit hash 对照表（T1-T5 顺序）
- 每个 DEPLOY-XX 的最终验证命令输出（grep 0 命中 / bash -n 通过 / go build 通过 / go vet 通过）
- docs/deployment/ 单文件 diff stat（行数变化）—— 17 处替换的归一化计数
- getPublicKey handler diff 摘要（新增的 WARN 日志行 + 2 个 getter 的 minimal code diff）
- setup-server.sh heredoc 完整 diff 段（新增 6 行）
- DEPLOY-05 sqlite 模板顶部注释 diff 段
- T1/T3 联合交付的 docs path 修正清单（scripts/gen-sm2-keys* → scripts/crypto/gen_sm2_keys/main.go）
- ROADMAP Phase 68 状态更新建议（5 SC 全部 PASS，phase 可 SHIPPED）
</output>