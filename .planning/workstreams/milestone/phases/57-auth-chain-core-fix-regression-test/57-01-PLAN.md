---
phase: 57-auth-chain-core-fix-regression-test
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/middleware/apikey.go
  - internal/middleware/apikey_integration_test.go
autonomous: true
requirements: [AUTH-01, AUTH-02, QUAL-02]

must_haves:
  truths:
    - "API Key 认证成功后 gin context 中 user_id / api_key_id / scopes / auth_type=\"api_key\" 四键被下游 handler 成功读取且值非空（由集成测试断言，非手工打印）"
    - "D-05 落地：P0-2 恒 false 分支消除——setUserContextForAPIKey 签名改为 (c, apiKey *models.APIKey, scopes) + 直接 import internal/models，apikey.go 不再含 `apiKey interface{}` 参数、局部 `apiKeyType` 值类型、或 `interface{} 避免循环导入` 注释"
    - "D-04 落地：零行为变更——保留全部 7 个 context 键（user_id/username/nickname/api_key_id/scopes/auth_type/inherit_perms）+ username := apiKey.Name 语义原样保留（密钥名而非登录名；username 语义修正延后 Phase 61 资源权限领域）"
    - "D-03 落地：RequireAPIKeyResourcePermission 改注册期委托 return RequireScope(getRequiredScope(action))，不再含 `RequireScope(requiredScope)(c)` 内联反模式（靠 c.Next() 副作用推进）；resource 参数被忽略 + getScopeFromContext 取 scopes[0] 两项延后 Phase 61 AUTH-04/QUAL-03，本 phase 不动"
    - "4 中间件（MultiAuth/RequireScope/RequireAPIKeyResourcePermission/RateLimitByScope）类型签名经审查自洽，services.NewUsageLogger(db) 与 services.NewRateLimiter() 在测试文件内有真实实例化调用点（D-02 证据，fake ≠ NewUsageLogger）"
    - "D-01 落地：三路径集成测试命中（有效 key+正确 scope→200 / 有效 key+缺失 scope→403 / 无效 key→401），用手写 fake/stub（无 gomock 框架）+ 真实 gin.Engine + httptest 驱动 MultiAuth 中间件，符合 TESTING.md 既有 testify 风格"
    - "既有 apikey_test.go 3 个纯函数测试（TestIsValidKeyFormat / TestIsIPAllowed / TestGetRequiredScope）不回归"
    - "go build ./... 退出码 0"
  artifacts:
    - path: "internal/middleware/apikey.go"
      provides: "P0-2 类型断言修复 (setUserContextForAPIKey 签名 interface{}→*models.APIKey) + P0-1 反模式重写 (RequireAPIKeyResourcePermission)"
      contains: "func setUserContextForAPIKey(c *gin.Context, apiKey *models.APIKey"
    - path: "internal/middleware/apikey_integration_test.go"
      provides: "三路径集成测试 (TestMultiAuthIntegration) + D-02 构造函数证据 (TestConstructorsCallable_D02) + 手写 fake 类型"
      contains: "func TestMultiAuthIntegration(t *testing.T)"
  key_links:
    - from: "internal/middleware/apikey.go:40 (apiKey, err := apiKeyService.ValidateAPIKey)"
      to: "internal/middleware/apikey.go:58 (setUserContextForAPIKey(c, apiKey, apiKey.Scopes))"
      via: "*models.APIKey 指针传入须匹配修复后的签名"
      pattern: "setUserContextForAPIKey\\(c, apiKey, apiKey\\.Scopes\\)"
    - from: "internal/middleware/apikey_integration_test.go (router.Use(MultiAuth(fakeSvc, logger)))"
      to: "internal/middleware/apikey.go:22 (func MultiAuth(apiKeyService system.APIKeyService, usageLogger services.UsageLogger))"
      via: "fake 实现 system.APIKeyService 接口 9 方法 + fake/services.UsageLogger 接口 1 方法"
      pattern: "router\\.Use\\(MultiAuth\\("
    - from: "internal/middleware/apikey_integration_test.go (_ = RateLimitByScope(services.NewRateLimiter()))"
      to: "internal/middleware/apikey.go:254 (func RateLimitByScope(rateLimiter *services.RateLimiter))"
      via: "D-02 证据：真实构造函数返回值类型兼容"
      pattern: "RateLimitByScope\\(services\\.NewRateLimiter\\(\\)\\)"
---

<objective>
修复 API Key 认证链两个 P0 确定性缺陷并用集成测试锁住完整链路防回归。

Purpose: Phase 57 是 v1.21 milestone 的第一个 phase，恢复 v1.6「API 密钥管理系统」(Phase 16) 中已失效的认证链代码。两个根因均已逐行验证（57-RESEARCH.md + STATE.md §根因调查结论）：① P0-2 `setUserContextForAPIKey` 把 `*models.APIKey` 指针断言为局部值类型 `apiKeyType` 恒 false，导致 7 个 `c.Set` 从未执行（AUTH-01）；② P0-1 `RequireAPIKeyResourcePermission` 内联 `RequireScope(requiredScope)(c)` 反模式（靠 `c.Next()` 副作用推进，AUTH-02）+ 4 中间件零挂载。本 phase 修代码让其"正确且可挂载"（不挂载——挂载属 Phase 60 AUTH-03 决策），并用三路径集成测试证明链路工作。

Output:
- `internal/middleware/apikey.go` — 2 处修复（AUTH-01 签名 + AUTH-02 反模式重写）
- `internal/middleware/apikey_integration_test.go` — 集成测试 + D-02 证据 + 手写 fake
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-auth-chain-core-fix-regression-test/57-CONTEXT.md
@.planning/phases/57-auth-chain-core-fix-regression-test/57-RESEARCH.md
@.planning/phases/57-auth-chain-core-fix-regression-test/57-PATTERNS.md
@.planning/phases/57-auth-chain-core-fix-regression-test/57-VALIDATION.md

<interfaces>
<!-- 关键接口契约，executor 直接消费，无需 codebase 探索 -->

From internal/services/system/apikey_service.go:29-39 (APIKeyService 接口 9 方法，fake 须全实现):
```go
type APIKeyService interface {
    CreateAPIKey(ctx context.Context, userID string, req *requests.CreateAPIKeyRequest) (*string, error)
    ListAPIKeys(ctx context.Context, userID string, params requests.ListAPIKeysParams) (*PageResult, error)
    GetAPIKey(ctx context.Context, id string) (*models.APIKey, error)
    UpdateAPIKey(ctx context.Context, id string, req *requests.UpdateAPIKeyRequest) error
    DeleteAPIKey(ctx context.Context, id string) error
    ToggleAPIKeyStatus(ctx context.Context, id string) error
    ValidateAPIKey(ctx context.Context, keyStr string) (*models.APIKey, error)
    ListUsageLogs(ctx context.Context, params ListUsageLogsParams) (*UsageLogsPageResult, error)
    GetUsageLogSummary(ctx context.Context, apiKeyID string) (*UsageSummary, error)
}
```
注意：`PageResult` / `ListUsageLogsParams` / `UsageLogsPageResult` / `UsageSummary` 都在 `system` 包内；`requests.CreateAPIKeyRequest` 等在 `internal/models/system/requests` 包内。fake 在 `package middleware` 内定义时用全限定类型引用（`*requests.CreateAPIKeyRequest`、`system.PageResult` 等）。

From internal/services/usage_logger.go:12-17 (UsageLogger 接口 1 方法):
```go
type UsageLogger interface {
    LogUsage(ctx context.Context, req *LogUsageRequest) error
}
```

From internal/services/usage_logger.go:38 (构造函数签名，D-02 证据):
```go
func NewUsageLogger(db *gorm.DB) UsageLogger
```

From internal/services/rate_limiter.go:45 (构造函数签名，D-02 证据；勿与 operations/rate_limiter.go:28 同名带参构造混淆):
```go
func NewRateLimiter() *RateLimiter
```

From internal/middleware/apikey.go:22,184,223,254 (4 中间件签名，SC#2 审查项，须保持不变):
```go
func MultiAuth(apiKeyService system.APIKeyService, usageLogger services.UsageLogger) gin.HandlerFunc
func RequireScope(requiredScope string) gin.HandlerFunc
func RequireAPIKeyResourcePermission(resource string, action string) gin.HandlerFunc
func RateLimitByScope(rateLimiter *services.RateLimiter) gin.HandlerFunc
```

From internal/models/api_key.go + base.go (APIKey struct 形状):
```go
type APIKey struct {
    BaseModel                            // 内嵌 → apiKey.ID 来自 BaseModel.ID (UUID string)
    Name         string                  // apiKey.Name (D-04: username 语义保留, 是密钥名而非登录名)
    UserID       *string                 // apiKey.UserID (指针, 须 nil-check)
    IsActive     bool
    Scopes       []string                // GORM 已反序列化为 []string
    IPWhitelist  []string
    InheritPerms bool
    User         *User                   // apiKey.User (指针)
}
```

From pkg/response/response.go:40-41,87-102 (测试状态码断言基准):
```go
ErrUnauthorized = &AppError{Code: 401, HTTPStatus: 401}  // → HTTP 401
ErrForbidden    = &AppError{Code: 403, HTTPStatus: 403}  // → HTTP 403
// response.Error(c, err) 用 appErr.HTTPStatus 作 HTTP 状态码
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 修复 setUserContextForAPIKey 类型断言 (AUTH-01 / P0-2) + 重写 RequireAPIKeyResourcePermission 反模式 (AUTH-02 / P0-1)</name>
  <files>internal/middleware/apikey.go</files>

  <read_first>
    - internal/middleware/apikey.go (待修改文件，当前完整状态——重点 line 1-12 import 块、line 22-77 MultiAuth、line 144-179 setUserContextForAPIKey、line 184-219 RequireScope、line 221-231 RequireAPIKeyResourcePermission、line 233-249 getRequiredScope)
    - internal/services/system/apikey_service.go:29-39 (APIKeyService 接口——确认 ValidateAPIKey 返回 *models.APIKey)
    - internal/models/api_key.go (APIKey struct 形状——确认字段访问路径 BaseModel.ID/Name/UserID/Scopes/InheritPerms/User)
    - internal/models/base.go (BaseModel——确认 ID 是 UUID string)
    - .planning/phases/57-auth-chain-core-fix-regression-test/57-PATTERNS.md (§MODIFY apikey.go 段——含修复前后完整代码 + getRequiredScope 纯函数证据 + 4 中间件签名表)
    - .planning/codebase/CONVENTIONS.md (Go 代码规范——import 分组、response.Error 调用约定)
  </read_first>

  <behavior>
    - 修复后 setUserContextForAPIKey 被调用时 7 个 c.Set 真实执行（不再恒 false 跳过）
    - c.Set("user_id", userID) 中 userID 来自 *apiKey.UserID 解引用（nil 时为 ""）
    - c.Set("username", apiKey.Name) 保留 D-04 语义（密钥名而非用户名，零行为变更）
    - c.Set("nickname", "")、c.Set("api_key_id", apiKey.ID)、c.Set("scopes", scopes)、c.Set("auth_type", "api_key")
    - c.Set("inherit_perms", true) 仅当 apiKey.InheritPerms && apiKey.User != nil 时执行
    - RequireAPIKeyResourcePermission 注册期计算 scope 并直接 return RequireScope(...)，不再在返回的 func 内调 RequireScope(...)(c)
  </behavior>

  <action>
    **AUTH-01 修复 (D-05，P0-2 根因)：** 在 import 块新增 `"github.com/xingran-next/xingran-go-backend/internal/models"`（保持既有 import 分组——stdlib 在前，项目内包在后，按字母序）。然后改 `setUserContextForAPIKey` 签名：把第 146 行 `func setUserContextForAPIKey(c *gin.Context, apiKey interface{}, scopes []string)` 改为 `func setUserContextForAPIKey(c *gin.Context, apiKey *models.APIKey, scopes []string)`。删除第 145 行注释 `// apiKey 参数的类型是 *models.APIKey，使用 interface{} 避免循环导入`（误判，循环依赖已由 57-RESEARCH.md §循环依赖验证双向 grep 排除——middleware 已通过 services/system 传递依赖 models，直接 import 不引入新环）。删除第 147-148 行 `// 通过类型断言访问 apiKey 的字段` 与 `// models.APIKey 结构: ...` 注释。删除第 149-155 行整个局部 `type apiKeyType struct { ... }` 定义。删除第 157 行 `if ak, ok := apiKey.(apiKeyType); ok {` 类型断言分支头与第 178 行对应的 `}`——把内部 7 个 c.Set 提升到函数主体（去掉一层缩进）。把第 159 行 `username := ak.Name` 改为直接 `c.Set("username", apiKey.Name)`（消除中间变量）。把第 161-163 行 `if ak.UserID != nil { userID = *ak.UserID }` 改为 `if apiKey.UserID != nil { userID = *apiKey.UserID }`（保留 `userID := ""` 在前）。把第 169 行 `c.Set("api_key_id", ak.ID)` 改为 `c.Set("api_key_id", apiKey.ID)`。把第 174 行 `if ak.InheritPerms && ak.User != nil` 改为 `if apiKey.InheritPerms && apiKey.User != nil`。D-04 锁定：**不得修改 username 语义**（不得改为查关联 User 真名），**不得删除/重命名任何 7 个 context 键**，**不得改既有的 `c.Set("nickname", "")` 空字面量**。

    **AUTH-02 修复 (D-03，P0-1 反模式)：** 采用 PATTERNS.md §MODIFY apikey.go 的"最小委托写法"（非"显式内联"备选）。把第 223-231 行整个 `RequireAPIKeyResourcePermission` 函数体替换为：在函数主体第一行调用 `requiredScope := getRequiredScope(action)`（纯函数，不依赖 *gin.Context，apikey.go:235-249 已验证），然后 `return RequireScope(requiredScope)`。删除中间件返回的 `func(c *gin.Context) { ... }` 闭包包装。重写后的函数体只有 2 行（算 scope + return），签名 `func RequireAPIKeyResourcePermission(resource string, action string) gin.HandlerFunc` 保持不变。**D-03 scope fence：`resource` 参数仍忽略，不得添加 resource→permission 映射逻辑（延后 Phase 61 AUTH-04）；`getScopeFromContext` line 292-311 的 `scopes[0]` 取法不得改动（延后 Phase 61 QUAL-03）；RateLimitByScope line 274-275 的 `string(rune(result.Limit))` 编码 bug 不得修（P2-a，Phase 60 QUAL-01）；MultiAuth line 60-73 的 usage logger goroutine 不得改（P1-2 / P2-b，Phase 59）。**

    **不得做的事 (scope fence 全集，diff 触及任一项即视为越界)：**
    - 不动 MultiAuth 函数体（line 22-77）
    - 不动 extractAPIKey / isValidKeyFormat / isIPAllowed（line 79-142）
    - 不动 RequireScope（line 184-219）
    - 不动 getRequiredScope（line 233-249）
    - 不动 RateLimitByScope / getScopeFromContext / getIdentifier（line 251-332）
    - 不动 apikey_test.go（既有 3 纯函数测试）
    - 不引入任何新外部依赖（零新增包，go.mod 不动）
    - 不在生产路由挂载 MultiAuth（挂载属 Phase 60 AUTH-03）
  </action>

  <verify>
    <automated>cd D:/code/ClaudeCode/guoguo && go build ./... && go vet ./internal/middleware/</automated>
  </verify>

  <acceptance_criteria>
    1. `grep -c "func setUserContextForAPIKey(c \*gin.Context, apiKey \*models.APIKey" internal/middleware/apikey.go` 返回 `1`（签名修复，AUTH-01）。
    2. `grep -c "apiKey interface{}" internal/middleware/apikey.go` 返回 `0`（interface{} 参数消除）。
    3. `grep -c "apiKeyType" internal/middleware/apikey.go` 返回 `0`（局部值类型消除）。
    4. `grep -c "避免循环导入" internal/middleware/apikey.go` 返回 `0`（误判注释消除）。
    5. `grep -c "github.com/xingran-next/xingran-go-backend/internal/models" internal/middleware/apikey.go` 返回 `≥1`（新增 import）。
    6. `grep -c "RequireScope(requiredScope)(c)" internal/middleware/apikey.go` 返回 `0`（P0-1 内联调用反模式消除，AUTH-02）。
    7. `grep -A2 "func RequireAPIKeyResourcePermission" internal/middleware/apikey.go` 输出含 `requiredScope := getRequiredScope(action)` 与 `return RequireScope(requiredScope)`（最小委托重写）。
    8. `grep -c "c.Set(\"username\", apiKey.Name)" internal/middleware/apikey.go` 返回 `1`（D-04 username 语义原样保留）。
    9. `grep -c "scopes\[0\]" internal/middleware/apikey.go` 返回 `1`（scope fence：getScopeFromContext 未动，仍延后 Phase 61）。
    10. `grep -c "string(rune(result.Limit))" internal/middleware/apikey.go` 返回 `1`（scope fence：P2-a 限流头 bug 未动，延后 Phase 60）。
    11. `cd D:/code/ClaudeCode/guoguo && go build ./...` 退出码 0（编译通过——SC#4 前置）。
    12. `cd D:/code/ClaudeCode/guoguo && go vet ./internal/middleware/` 退出码 0（无 vet warning）。
  </acceptance_criteria>

  <done>
    setUserContextForAPIKey 签名改为 `(c *gin.Context, apiKey *models.APIKey, scopes []string)`，局部 apiKeyType 与 interface{} workaround 全部移除，7 个 c.Set 提升到函数主体直接执行；RequireAPIKeyResourcePermission 用最小委托写法（注册期算 scope + return RequireScope(...)）消除内联调用反模式；D-04 七个 context 键与 username=ak.Name 语义保留；scope fence 项（resource 参数、scopes[0]、P2-a 限流头、usage logger goroutine）全部未动；go build ./... 与 go vet ./internal/middleware/ 退出 0。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建集成测试（QUAL-02 三路径 + AUTH-02 D-02 构造函数证据 + 手写 fake）</name>
  <files>internal/middleware/apikey_integration_test.go</files>

  <read_first>
    - internal/middleware/apikey.go (Task 1 修复后的状态——确认 setUserContextForAPIKey 新签名、4 中间件签名、isValidKeyFormat 的 rec_+64hex=68 字符校验、RequireScope 的 scopes/admin 逻辑)
    - internal/middleware/apikey_test.go (既有同包测试——确认 package 名为 `middleware`、import 风格、testify 子测试中文命名约定，本任务新文件须与之并存不冲突)
    - internal/api/v1/auth_integration_test.go:20-46,154-186,419-454 (gin+httptest 装配 analog——gin.SetMode(gin.TestMode) + gin.New + httptest.NewRequest/NewRecorder + assert.Equal 状态码断言三段式)
    - internal/services/usage_logger_test.go:11-12,30-57 (testify import + setupUsageLoggerTestDB sqlite helper analog——含 busy_timeout=5000 + 独立文件 DB + 手动 CREATE TABLE sys_api_key_usage_logs + 不用 t.TempDir 的原因注释)
    - internal/services/system/apikey_service_test.go:402-405 (testify + 真实构造函数风格 analog——db := setupTestDB(t); service := NewAPIKeyService(db) 模式)
    - internal/services/system/apikey_service.go:29-39 (APIKeyService 接口 9 方法签名——fake 须全实现)
    - internal/services/usage_logger.go:12-17,38 (UsageLogger 接口 1 方法 + NewUsageLogger 签名)
    - internal/services/rate_limiter.go:45 (NewRateLimiter 无参签名——D-02 证据用，勿与 operations/rate_limiter.go:28 混淆)
    - pkg/response/response.go:40-41,87-102 (错误码 HTTPStatus 映射——401/403 断言基准)
    - .planning/phases/57-auth-chain-core-fix-regression-test/57-PATTERNS.md (§CREATE apikey_integration_test.go 段——含 fake 形状、装配骨架、setupUsageLoggerTestDB 完整代码、D-02 测试代码、X-API-Key 格式 helper、SC#1 context 键断言清单)
    - .planning/codebase/TESTING.md (无 gomock、需 DB 用真实连接约定——D-01 fake 策略依据)
  </read_first>

  <behavior>
    - fakeAPIKeyService 实现 system.APIKeyService 全 9 方法；ValidateAPIKey 按 validKey/validateErr 预置返回，其余 8 方法 panic("not used in MultiAuth path")
    - fakeUsageLogger 实现 services.UsageLogger 1 方法 LogUsage；被调用时置 logged=true 并返回 nil
    - TestMultiAuthIntegration 三子测试：有效 key+正确 scope→200 且 handler 内 4 个 context 键非空且 fakeLogger.logged=true；有效 key+缺失 scope→403；无效 key→401
    - TestConstructorsCallable_D02：services.NewUsageLogger(sqliteDB) 与 services.NewRateLimiter() 真实调用，返回值能赋给 MultiAuth 第 2 参 / RateLimitByScope 第 1 参（编译通过即签名兼容）
    - 请求头 X-API-Key 用 "rec_" + 64 位 hex（68 字符，过 isValidKeyFormat 校验）
    - 既有 apikey_test.go 3 个纯函数测试不回归
  </behavior>

  <action>
    **文件头：** `package middleware`（与 apikey_test.go 同包，才能访问私有 setUserContextForAPIKey 与直接调用 MultiAuth/RequireScope/RateLimitByScope）。import 块按 PATTERNS.md §CREATE 段给出的清单：`context` / `errors` / `fmt` / `net/http` / `net/http/httptest` / `os` / `path/filepath` / `strings` / `testing` / `time`（stdlib 分组）+ `gin` / `testify/assert` / `testify/require`（第三方分组）+ `internal/models` / `internal/services` / `internal/services/system` / `internal/models/system/requests` / `gorm.io/driver/sqlite` / `gorm.io/gorm`（项目内 + gorm 分组）。

    **fake 类型定义（D-01）：** 定义 `type fakeAPIKeyService struct { validKey *models.APIKey; validateErr error }`。`ValidateAPIKey` 方法：`if f.validateErr != nil { return nil, f.validateErr }; return f.validKey, nil`。其余 8 方法签名严格对齐 apikey_service.go:29-39（用全限定类型 `*requests.CreateAPIKeyRequest` / `requests.ListAPIKeysParams` / `*system.PageResult` / `*requests.UpdateAPIKeyRequest` / `system.ListUsageLogsParams` / `*system.UsageLogsPageResult` / `*system.UsageSummary`），方法体 `panic("not used in MultiAuth path")`。定义 `type fakeUsageLogger struct { logged bool }`，`LogUsage` 方法体 `f.logged = true; return nil`。

    **helper 函数：** 复制 PATTERNS.md §CREATE 段给出的 `setupUsageLoggerTestDB(t *testing.T) *gorm.DB`（基于 usage_logger_test.go:30-57 verbatim——独立文件 DB 路径 + `busy_timeout=5000` + 手动 `CREATE TABLE IF NOT EXISTS sys_api_key_usage_logs (...)` + `require.NoError`）。**保留原 helper 的设计注释**（独立文件防 SQLite 写锁 / busy_timeout 让写锁排队 / 不用 t.TempDir 的原因——fire-and-forget goroutine 测试结束后仍写文件）。再加一个 `hex64() string { return strings.Repeat("0123456789abcdef", 4) }` 返回 64 位 hex（满足 isValidKeyFormat 的 rec_+64hex=68 字符要求，Pitfall 3 防御）。

    **TestMultiAuthIntegration (SC#1 + SC#3)：** 函数开头 `gin.SetMode(gin.TestMode)`。三个 `t.Run` 子测试，子测试名用中文（与既有 apikey_test.go:11 / usage_logger_test.go:74 / apikey_service_test.go:408 风格一致）：
    - 子测试 1 `"有效key+正确scope_通过并写入context"`：构造 `uid := "11111111-2222-3333-4444-555555555555"`，`fakeSvc := &fakeAPIKeyService{validKey: &models.APIKey{Name: "test-key", UserID: &uid, Scopes: []string{"read"}, IsActive: true}}`（APIKey 内嵌 BaseModel，设 `BaseModel: models.BaseModel{ID: "ak-test-id"}`），`fakeLogger := &fakeUsageLogger{}`。`router := gin.New(); router.Use(MultiAuth(fakeSvc, fakeLogger)); router.GET("/ping", RequireScope("read"), func(c *gin.Context) { /* SC#1 断言 */ c.JSON(200, gin.H{"ok": true}) })`。SC#1 在 handler 内断言 4 键：`assert.NotEmpty(t, c.GetString("user_id"))`、`assert.Equal(t, "ak-test-id", c.GetString("api_key_id"))`、`assert.Equal(t, []string{"read"}, c.MustGet("scopes"))`、`assert.Equal(t, "api_key", c.GetString("auth_type"))`。`req := httptest.NewRequest("GET", "/ping", nil); req.Header.Set("X-API-Key", "rec_"+hex64()); w := httptest.NewRecorder(); router.ServeHTTP(w, req)`。`assert.Equal(t, 200, w.Code)` 与 `assert.True(t, fakeLogger.logged)`。
    - 子测试 2 `"有效key+缺失scope_403"`：fakeSvc 返回 `Scopes: []string{"read"}`，路由挂 `RequireScope("write")`。请求同上。`assert.Equal(t, 403, w.Code)`（response.ErrForbidden.HTTPStatus）。
    - 子测试 3 `"无效key_401"`：`fakeSvc := &fakeAPIKeyService{validateErr: errors.New("密钥不存在或已禁用")}`。路由挂一个简单 handler。请求同上。`assert.Equal(t, 401, w.Code)`（response.ErrUnauthorized.HTTPStatus）。

    **TestConstructorsCallable_D02 (SC#2 / AUTH-02 构造函数证据)：** `db := setupUsageLoggerTestDB(t); logger := services.NewUsageLogger(db); assert.NotNil(t, logger); _ = MultiAuth(&fakeAPIKeyService{}, logger)`（编译通过即证明 MultiAuth 第 2 参接受 services.UsageLogger）。`rl := services.NewRateLimiter(); assert.NotNil(t, rl); _ = RateLimitByScope(rl)`（编译通过即证明签名兼容）。**用 `services.NewRateLimiter()`（无参），不得用 `operations.NewRateLimiter(...)`（Pitfall 4）。** `_ = MultiAuth(...)` / `_ = RateLimitByScope(...)` 赋值给 `_` 是类型兼容性证明，不引入生产死代码。

    **文件命名与既有测试并存：** 文件名严格用 `apikey_integration_test.go`（与既有 `apikey_test.go` 同包并存，不冲突——Go 测试文件同包多文件允许）。**不修改 apikey_test.go**（既有 3 纯函数测试 TestIsValidKeyFormat / TestIsIPAllowed / TestGetRequiredScope 不回归）。

    **Pitfall 防御（PATTERNS.md §Common Pitfalls）：**
    - Pitfall 2：fake 必须实现全部 9 方法（接口完整满足），否则 `MultiAuth(fakeSvc, ...)` 编译报 `cannot use fakeSvc as type system.APIKeyService`。
    - Pitfall 3：请求头用 `"rec_" + hex64()`（68 字符），否则被 isValidKeyFormat 在 ValidateAPIKey 前拦截走 401，导致路径①/② 实际返回 401 而非 200/403。
    - Pitfall 4：D-02 用 `services.NewRateLimiter()`（无参，internal/services/rate_limiter.go:45），不得用 `operations.NewRateLimiter(maxTokens, interval)`。
  </action>

  <verify>
    <automated>cd D:/code/ClaudeCode/guoguo && go test ./internal/middleware/ -run "TestMultiAuthIntegration|TestConstructorsCallable_D02|TestIsValidKeyFormat|TestIsIPAllowed|TestGetRequiredScope" -v</automated>
  </verify>

  <acceptance_criteria>
    1. 文件 `internal/middleware/apikey_integration_test.go` 存在且 `head -1` 输出 `package middleware`（同包，访问私有函数）。
    2. `grep -c "func TestMultiAuthIntegration" internal/middleware/apikey_integration_test.go` 返回 `1`。
    3. `grep -c "func TestConstructorsCallable_D02" internal/middleware/apikey_integration_test.go` 返回 `1`。
    4. `grep -c "type fakeAPIKeyService struct" internal/middleware/apikey_integration_test.go` 返回 `1`。
    5. `grep -c "type fakeUsageLogger struct" internal/middleware/apikey_integration_test.go` 返回 `1`。
    6. `grep -c "func (f \*fakeAPIKeyService) ValidateAPIKey" internal/middleware/apikey_integration_test.go` 返回 `1`。
    7. fakeAPIKeyService 含全部 9 方法：`grep -c "func (f \*fakeAPIKeyService)" internal/middleware/apikey_integration_test.go` 返回 `≥9`（Pitfall 2 防御）。
    8. `grep -c "services.NewUsageLogger" internal/middleware/apikey_integration_test.go` 返回 `≥1`（D-02 真实调用点）。
    9. `grep -c "services.NewRateLimiter()" internal/middleware/apikey_integration_test.go` 返回 `≥1`（D-02 真实调用点，无参版）。
    10. `grep -c "operations.NewRateLimiter" internal/middleware/apikey_integration_test.go` 返回 `0`（Pitfall 4——未误用令牌桶版）。
    11. `grep -c '"rec_" + hex64()' internal/middleware/apikey_integration_test.go` 返回 `≥3`（三路径均用合法 68 字符格式，Pitfall 3 防御）。
    12. `grep -c "rec_\" + hex64()" internal/middleware/apikey_integration_test.go` 与 `grep -c 'X-API-Key.*rec_' internal/middleware/apikey_integration_test.go` 至少一个返回 `≥3`（X-API-Key 头设置）。
    13. SC#1 4 键断言：`grep -c 'c.GetString("user_id")' internal/middleware/apikey_integration_test.go` 返回 `≥1`，`grep -c 'c.GetString("api_key_id")' internal/middleware/apikey_integration_test.go` 返回 `≥1`，`grep -c 'c.MustGet("scopes")' internal/middleware/apikey_integration_test.go` 返回 `≥1`，`grep -c 'c.GetString("auth_type")' internal/middleware/apikey_integration_test.go` 返回 `≥1`。
    14. `cd D:/code/ClaudeCode/guoguo && go test ./internal/middleware/ -run "TestMultiAuthIntegration|TestConstructorsCallable_D02|TestIsValidKeyFormat|TestIsIPAllowed|TestGetRequiredScope" -v` 退出码 0，输出含 `PASS` 且 TestMultiAuthIntegration 的 3 个子测试全部通过（SC#3）。
  </acceptance_criteria>

  <done>
    apikey_integration_test.go 创建完成：TestMultiAuthIntegration 三子测试覆盖 200/403/401 三路径（SC#3）+ SC#1 四个 context 键断言；TestConstructorsCallable_D02 真实实例化 services.NewUsageLogger(db) 与 services.NewRateLimiter()（SC#2 D-02 证据）；fakeAPIKeyService 实现全 9 方法（仅 ValidateAPIKey 真逻辑）+ fakeUsageLogger 实现单方法；setupUsageLoggerTestDB helper 按 usage_logger_test.go 既有 sqlite 模式复制；请求头格式用 rec_+64hex（Pitfall 3 防御）；既有 apikey_test.go 3 个纯函数测试不回归；go test 退出 0。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → MultiAuth middleware | HTTP 请求（含 X-API-Key 头）从此边界进入 API Key 认证链；本 phase 修复此边界内的功能正确性，不改变安全策略 |
| MultiAuth → handler (gin context) | 认证成功后 context 键（user_id/api_key_id/scopes/auth_type）被下游 handler 读取；P0-2 病灶即此边界"上下文丢失" |
| middleware 代码 → 生产路由挂载 | **本 phase 不跨越此边界**——挂载属 Phase 60 AUTH-03 discuss 决策点（含安全评估）。本 phase 只让代码"正确且可挂载"+ 用测试证明链路工作 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Elevation of Privilege (认证后上下文丢失) | `setUserContextForAPIKey` (apikey.go:144-179) | mitigate | AUTH-01 修复（Task 1）：签名 `interface{}` → `*models.APIKey`，消除恒 false 类型断言；7 个 c.Set 真实执行；集成测试（Task 2 子测试 1）用 handler 内断言 4 个 context 键非空锁住防回归。此威胁已被 P0-2 病灶实证（57-STATE.md §根因调查结论 line 56）。 |
| T-57-02 | Authentication Bypass (死代码中间件，若错误挂载) | `MultiAuth` / `RequireScope` / `RequireAPIKeyResourcePermission` / `RateLimitByScope` 4 中间件 (apikey.go) | accept (本 phase) + mitigate (Phase 60) | 本 phase 让代码自洽（修 RequireAPIKeyResourcePermission 内联调用反模式 + D-02 证据证明构造函数可装配），但**不挂载生产路由**。死代码"若错误挂载"的风险（信息泄露/认证绕过）仅在 Phase 60 决策=启用后才会物化——届时由 Phase 60 AUTH-03 discuss 决策含挂载点清单 + 权限校验矩阵 + 安全影响评估。本 phase 接受此威胁为"未激活状态"。 |
| T-57-03 | Tampering (RequireAPIKeyResourcePermission 靠 c.Next() 副作用推进) | `RequireAPIKeyResourcePermission` (apikey.go:221-231) | mitigate | AUTH-02 修复（Task 1）：改用注册期委托 `return RequireScope(getRequiredScope(action))`，由 gin 正常挂载——内部 c.Next() 处于正确链位置，消除"下游 handler 重复执行或链索引错乱"风险。 |
| T-57-SC | Tampering (npm/pip/cargo 包) | N/A — Go 项目零新增依赖 | accept (不适用) | 本 phase 不安装任何外部包（57-RESEARCH.md §Package Legitimacy Audit：纯 Go stdlib + 既有 go.mod 依赖 gin/testify/gorm/sqlite driver）。无新包需要 slopcheck；gate 不触发。 |

**安全影响范围说明：** 本 phase 修复认证链**功能正确性**，**不改变安全策略**——不启用挂载（Phase 60 AUTH-03 决策）、不改存储（Phase 60 SEC-01 决策）、不改加密（与 SM2/SM4 请求加密层无关）。启用决策与完整威胁建模（含 IP 白名单绕过 Spoofing、作用域继承的权限放大）归 Phase 60 AUTH-03 discuss 决策点。本 phase 不存在高严重性威胁需要阻塞交付——T-57-01 与 T-57-03 是被修复对象（修复即消除），T-57-02 在未挂载状态下不激活。

**Scope fence 延后项的安全处置：**
- `RequireAPIKeyResourcePermission` 的 `resource` 参数被忽略 → Phase 61 AUTH-04（仅在 Phase 60 AUTH-03=启用后执行）。本 phase 不修，已在 T-57-02 记录"挂载决策含资源权限矩阵评估"。
- `getScopeFromContext` 只取 `scopes[0]`（多 scope key 限流作用域选择错误）→ Phase 61 QUAL-03。本 phase 不修。
- 上述两项"延后"非"忽略"——其安全影响在 Phase 60 AUTH-03 启用决策时一并评估，并由 Phase 61 落地修复。
</threat_model>

<verification>
**Phase gate（进入 `/gsd:verify-work` 前必须全绿）：**

1. **SC#4 编译 + 残留 grep**（每任务提交后跑）：
   ```bash
   cd D:/code/ClaudeCode/guoguo && go build ./...
   # 退出码 0
   grep -rn "避免循环导入\|apiKeyType\|apiKey interface{}" internal/middleware/apikey.go
   # 无输出（workaround 全部消除）
   ```

2. **SC#3 + SC#1 三路径集成测试 + context 键断言**：
   ```bash
   cd D:/code/ClaudeCode/guoguo && go test ./internal/middleware/ -run "TestMultiAuthIntegration" -v
   # 3 个子测试（有效key+正确scope_通过并写入context / 有效key+缺失scope_403 / 无效key_401）全部 PASS
   # 子测试 1 输出含 user_id/api_key_id/scopes/auth_type 四键断言通过
   ```

3. **SC#2 构造函数证据 + grep 真实调用点**：
   ```bash
   cd D:/code/ClaudeCode/guoguo && go test ./internal/middleware/ -run "TestConstructorsCallable_D02" -v
   # PASS
   grep -rn "NewUsageLogger\|NewRateLimiter()" internal/middleware/
   # 输出含 apikey_integration_test.go 内的真实调用（D-02 证据落地）
   ```

4. **既有纯函数测试不回归**：
   ```bash
   cd D:/code/ClaudeCode/guoguo && go test ./internal/middleware/ -run "TestIsValidKeyFormat|TestIsIPAllowed|TestGetRequiredScope" -v
   # 3 个既有测试 PASS（未修改 apikey_test.go）
   ```

5. **全量回归（wave merge 前）**：
   ```bash
   cd D:/code/ClaudeCode/guoguo && go test ./...
   # 全绿，含 apikey_service_test.go / usage_logger_test.go / rate_limiter_test.go 不回归
   ```

6. **Scope fence 审查（人工，VALIDATION.md Manual-Only Verifications）**：
   - 读 apikey.go diff，确认 RequireAPIKeyResourcePermission 不再有 `RequireScope(...)(c)` 内联调用
   - 读 apikey.go diff，确认 4 中间件签名不变
   - 读 apikey.go diff，确认 `resource` 参数仍忽略 + `getScopeFromContext` 仍取 `scopes[0]` + `string(rune(result.Limit))` 仍未改 + MultiAuth usage logger goroutine 未动（本 phase 不越界到 Phase 59/60/61）

**反馈延迟估计：** < 15 秒（go build + go test 单包）。
</verification>

<success_criteria>
1. **SC#1 (AUTH-01)：** 集成测试子测试 1（有效key+正确scope）的 handler 内 4 个 context 键（user_id / api_key_id / scopes / auth_type="api_key"）断言全部通过；apikey.go 不再含 `apiKey interface{}` / 局部 `apiKeyType` / `避免循环导入` workaround。
2. **SC#2 (AUTH-02)：** apikey.go 不再含 `RequireScope(requiredScope)(c)` 内联调用反模式；4 中间件签名（MultiAuth / RequireScope / RequireAPIKeyResourcePermission / RateLimitByScope）保持不变；TestConstructorsCallable_D02 通过 + grep 证据显示 services.NewUsageLogger(db) / services.NewRateLimiter() 在测试文件内有真实调用点。
3. **SC#3 (QUAL-02)：** TestMultiAuthIntegration 三子测试 200/403/401 全部命中；既有 apikey_test.go 3 个纯函数测试不回归；全量 `go test ./...` 全绿。
4. **SC#4：** `go build ./...` 退出码 0；`grep -rn "避免循环导入\|interface{}" internal/middleware/apikey.go` 无 workaround 残留（注：`interface{}` 在 type assertion `scopes.([]string)` 等位置可能仍以非 workaround 形式存在——本判据只针对 `apiKey interface{}` 参数 + `避免循环导入` 注释，由 Task 1 acceptance_criteria #2/#4 精确锁定）。
5. **Scope fence：** `resource` 参数仍忽略、`getScopeFromContext` 仍取 `scopes[0]`、`string(rune(result.Limit))` P2-a bug 仍未改、MultiAuth usage logger goroutine 未动——本 phase 未越界到 Phase 59/60/61 领域。
</success_criteria>

<output>
Create `.planning/phases/57-auth-chain-core-fix-regression-test/57-01-SUMMARY.md` when done.
</output>
