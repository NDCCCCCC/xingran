---
phase: 69-dict-and-status-governance
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - xingran-react-frontend/src/constants/status.ts
  - xingran-react-frontend/src/constants/status.test.ts
  - xingran-react-frontend/src/pages/system/user/constants.ts
  - xingran-react-frontend/src/pages/system/role/constants.ts
  - xingran-react-frontend/src/pages/system/dict/constants.tsx
  - xingran-react-frontend/src/pages/system/dept/constants.tsx
  - xingran-react-frontend/src/pages/system/menu/constants.tsx
  - xingran-react-frontend/src/pages/operations/workstations/constants.tsx
  - xingran-react-frontend/src/pages/operations/floors/constants.ts
autonomous: true
requirements: [DICT-03]

must_haves:
  truths:
    - "DICT-03: `src/constants/status.ts` 落地——ENABLE_DISABLE（0=启用/1=禁用，对齐 models.UserStatus）与 NORMAL_STOP（0=正常/1=停用，对齐 models.RoleStatus/DeptStatus/PostStatus）两组 options + tag 配置单一拷贝，status.test.ts 断言值/label/键一致性全绿"
    - "DICT-03: 7 个 A 簇 status 定义文件改为引用共享模块（本地导出名不变、页面文件零改动），user/role/dict 三份「启用/禁用 vs 正常/停用」漂移拷贝收敛为单一模块；menu 的 VISIBLE（1=可见反转）保留独立定义不被误统一"
    - "DICT-03: status 0/1 下拉不进字典（安全决策：status 是代码分支语义不宜运营可配，走共享常量而非 sys_dict）"
  artifacts:
    - path: "xingran-react-frontend/src/constants/status.ts"
      provides: "通用启停 options/tag 共享常量（两套 label 语义组）"
      contains: "ENABLE_DISABLE_OPTIONS"
    - path: "xingran-react-frontend/src/constants/status.test.ts"
      provides: "共享常量值/label/键一致性断言（vitest）"
      contains: "NORMAL_STOP_OPTIONS"
  key_links:
    - from: "src/pages/system/user/constants.ts 等 7 文件"
      to: "src/constants/status.ts"
      via: "本地 STATUS_OPTIONS 导出改为共享模块别名引用（页面 import 不变）"
      pattern: "from \"@/constants/status\""
    - from: "src/constants/status.ts 常量注释"
      to: "internal/models/base.go UserStatus/RoleStatus/DeptStatus/PostStatus"
      via: "注释对齐后端常量名（前端镜像不引代码，注释即对齐凭证）"
      pattern: "UserStatusEnabled|RoleStatusEnabled"
---

# Phase 69 Plan 06: DICT-03 前端 status 线 — 共享常量模块落地 + 7 文件漂移收敛

**Status:** READY FOR EXECUTION
**Created:** 2026-08-19（2026-08-19 revision：由原 69-04（前端双线 + DICT-04 合体）拆出 status 共享常量线为独立 plan——与后端线（69-01）文件集互斥，Wave 1 并行）
**Source:** ROADMAP Phase 69（DICT-03）+ 69-RESEARCH.md A5 + 69-PATTERNS.md
**决策依据:** orchestrator 规划要点 3/4 + Q2 —— status 类走共享常量模块（不进字典）；本线与 useDict 迁移线（69-07）分离，69-07 硬依赖本 plan（user/workstations/holidays 的 constants 文件先收敛 STATUS 再被 69-07 处理 type 类导出）

<objective>

前端 status 0/1 下拉统一到 `src/constants/status.ts` 共享模块（消除 user「启用/禁用」/ role「正常/停用」/ dict「启用/禁用」三份漂移拷贝），7 个 A 簇 status 定义文件改为共享模块别名引用（导出名不变、页面零改动），menu 的 VISIBLE 反转例外完好保留。

Purpose: 前端 OPTIONS 硬编码是三处真相源拷贝中离用户最近的一处；status 类语义不进字典（Q2 安全决策），共享常量是唯一收敛路径。本 plan 只动 constants 定义文件，不触及页面组件与 useDict（那是 69-07 的工作面）。
Output: 2 个新文件（status.ts / status.test.ts）+ 7 个 constants 文件改造；type-check / lint / vitest 全绿；1 个 commit。

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/69-dict-and-status-governance/69-RESEARCH.md
@.planning/phases/69-dict-and-status-governance/69-PATTERNS.md

<interfaces>
<!-- 关键现状契约（已从代码库提取，executor 无需再探索） -->

status.ts 内容范本（迁移源头的单一拷贝，user/constants.ts:11-27 现状）:
```typescript
export const STATUS_OPTIONS: SelectOption[] = [
  { label: "启用", value: 0 },
  { label: "禁用", value: 1 },
];
export const STATUS_TAG_CONFIG: Record<number, { text: string; color: string }> = {
  0: { text: "启用", color: "success" },
  1: { text: "禁用", color: "error" },
};
```
status.ts 设计（对齐后端 base.go 常量注释，两套 label 语义组、零 UX 变化）:
- ENABLE_DISABLE_OPTIONS（0=启用/1=禁用）+ ENABLE_DISABLE_TAG_CONFIG —— 注释 `// 对齐 models.UserStatusEnabled=0(启用) / UserStatusDisabled=1(禁用), internal/models/base.go`，消费方 user/dict
- NORMAL_STOP_OPTIONS（0=正常/1=停用）+ NORMAL_STOP_TAG_CONFIG —— 注释 `// 对齐 models.RoleStatus/DeptStatus/PostStatus: 0=正常, 1=停用, internal/models/base.go`，消费方 role/dept/menu(status)/workstations/floors
- 自带导出类型 StatusOption（label: string; value: number），不反向 import 页面类型（避免循环依赖）

7 个 status 迁移文件与语义组映射:
| 文件 | 现状 label | 改为 |
|------|-----------|------|
| src/pages/system/user/constants.ts | 启用/禁用 | ENABLE_DISABLE 组别名 |
| src/pages/system/dict/constants.tsx | 启用/禁用 | ENABLE_DISABLE 组别名 |
| src/pages/system/role/constants.ts | 正常/停用 | NORMAL_STOP 组别名 |
| src/pages/system/dept/constants.tsx | 正常/停用 | NORMAL_STOP 组别名 |
| src/pages/system/menu/constants.tsx | status=正常/停用；VISIBLE=1显示/0隐藏(反转) | 仅 STATUS 改 NORMAL_STOP；**VISIBLE 相关导出原样保留独立定义**（VisibleShow=1 反转例外，严禁统一） |
| src/pages/operations/workstations/constants.tsx | 以文件实际为准（models.WorkstationStatus 注释为准选组） | NORMAL_STOP 组别名（若现状 label 与组不符，以模型注释对齐并在 SUMMARY 记录 UX 文案变化） |
| src/pages/operations/floors/constants.ts | 正常/停用（FloorStatusNormal/Stopped） | NORMAL_STOP 组别名 |

本地文件改造形态（导出名不变 → 页面 index.tsx/modals 零改动）: 文件内 STATUS_OPTIONS 定义替换为 `export const STATUS_OPTIONS = ENABLE_DISABLE_OPTIONS;` 一行 + import；STATUS_TAG_CONFIG 同理。（menu 的 VISIBLE_OPTIONS 等保持原定义不动。）

测试范本: src/components/network/port-write/__tests__/constants.test.ts（vitest 纯常量自洽测试：describe/it + expect，断言 label 非空/value 唯一/数值锁定）。

工作区护栏: `src/pages/settings/index.tsx` 与 `src/pages/system/settings-page/index.tsx` 存在与本 phase 无关的未提交改动（default_theme 删除遗留）——本 plan 任何任务不得触碰这两个文件。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>T1: src/constants/status.ts + status.test.ts + 7 个 A 簇 status 定义文件改共享引用</name>
  <files>xingran-react-frontend/src/constants/status.ts, xingran-react-frontend/src/constants/status.test.ts, xingran-react-frontend/src/pages/system/user/constants.ts, xingran-react-frontend/src/pages/system/role/constants.ts, xingran-react-frontend/src/pages/system/dict/constants.tsx, xingran-react-frontend/src/pages/system/dept/constants.tsx, xingran-react-frontend/src/pages/system/menu/constants.tsx, xingran-react-frontend/src/pages/operations/workstations/constants.tsx, xingran-react-frontend/src/pages/operations/floors/constants.ts</files>
  <read_first>
  - xingran-react-frontend/src/pages/system/user/constants.ts:11-27（内容范本 + 迁移源）
  - xingran-react-frontend/src/pages/system/role/constants.ts:17-20、src/pages/system/dict/constants.tsx:10-20（漂移实证三份拷贝）
  - xingran-react-frontend/src/pages/system/menu/constants.tsx 全文（识别 STATUS 与 VISIBLE 两组导出——VISIBLE 是反转例外必须保留）
  - xingran-react-frontend/src/pages/operations/workstations/constants.tsx、src/pages/operations/floors/constants.ts（现状 label 与导出名）
  - internal/models/base.go UserStatus/RoleStatus/DeptStatus/PostStatus 注释（label 对齐基准——只读）
  - xingran-react-frontend/src/components/network/port-write/__tests__/constants.test.ts（测试范本）
  - xingran-react-frontend/src/constants/ 目录既有文件（buttonStyles.tsx 等——位置与导出风格先例）
  </read_first>
  <action>
  1. 新建 src/constants/status.ts：按 interfaces 设计导出 StatusOption 类型、ENABLE_DISABLE_OPTIONS、ENABLE_DISABLE_TAG_CONFIG、NORMAL_STOP_OPTIONS、NORMAL_STOP_TAG_CONFIG；每组常量上方注释对齐后端 models 常量名（internal/models/base.go）；tag 配置的 color 沿用现状（success/error）。
  2. 新建 src/constants/status.test.ts（port-write constants.test.ts 风格）：断言 两组 options 各覆盖 value 0/1 且唯一、label 与 base.go 注释一致（"启用"/"禁用" 与 "正常"/"停用" 字面锁定）、两组 tag 配置键集合与对应 options 的 value 集合一致、tag text 与 options label 一致——锁死后端常量注释对齐关系。
  3. 7 个文件逐一改造：删除本地 STATUS_OPTIONS/STATUS_TAG_CONFIG 字面定义，改为 import 共享模块对应组并按 interfaces 映射表 re-export（导出名与类型签名不变）；menu/constants.tsx 只动 STATUS 相关导出，VISIBLE 相关原样保留；workstations 若现状 label 与 NORMAL_STOP 不符，以 models.WorkstationStatus 注释为准对齐并在 SUMMARY 记录。
  4. 各文件其余导出（GENDER_OPTIONS/TYPE_OPTIONS 等）本任务不动（69-07 处理）；页面 index.tsx/modals 零改动（导出名不变）。
  5. 门禁：npm run type-check && npx vitest run src/constants/status.test.ts && npm run lint。
  </action>
  <verify>
    <automated>cd xingran-react-frontend && npm run type-check && npx vitest run src/constants/status.test.ts && grep -c "VISIBLE" src/pages/system/menu/constants.tsx</automated>
  </verify>
  <acceptance_criteria>
  - `npm run type-check` 退出码 0；`npx vitest run src/constants/status.test.ts` 全绿
  - `grep -rl "from \"@/constants/status\"" src/pages/` 命中 7 个文件（user/role/dict/dept/menu/workstations/floors constants）
  - `grep -c "VISIBLE" src/pages/system/menu/constants.tsx` ≥ 1（VISIBLE 导出未被误统一/删除）
  - status.ts 内不存在 "sys_user_status" 等字典化 status 痕迹（status 不进字典，Q2 决策的源码断言）
  - user/role/dict 三文件的 STATUS_OPTIONS label 分别为 启用/禁用、正常/停用、启用/禁用（与迁移前一致，零 UX 变化）
  </acceptance_criteria>
  <done>共享 status 常量模块与测试落地，7 文件漂移拷贝收敛，VISIBLE 反转例外完好，type-check/lint/vitest 全绿。Commit: refactor(frontend): unify status options into shared constants module per DICT-03</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sys_dict 字典值（管理员可配）→ 前端下拉渲染 | status 类不进此通道——共享常量是代码资产，管理员不可配（Q2 安全边界） |
| CLAUDE.md/后端常量 → 前端镜像常量 | 前端不引 Go 代码，注释对齐 + 测试字面锁定是对齐凭证；后端值由 69-01 锁值测试守卫 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-13 | Tampering | 字典值注入业务逻辑（status 若进字典，管理员可配出代码未识别值） | mitigate | Q2 决策落地：status 走 src/constants/status.ts 共享常量不进字典（T1 acceptance 源码断言无 sys_user_status 痕迹） |
| T-69-15 | Tampering | Menu VISIBLE 反转语义被 status 统一误伤（1=可见 ≠ 1=停用） | mitigate | T1: menu/constants.tsx 仅改 STATUS 导出，VISIBLE 原样保留；acceptance grep VISIBLE 命中 |
| T-69-21 | Tampering | 前端镜像常量与后端 models 常量值漂移（无编译期约束） | mitigate | status.ts 注释对齐 + status.test.ts 字面锁定（"启用"/"禁用"/"正常"/"停用" + value 0/1 数值断言）；后端侧由 69-01 锁值测试双向守卫 |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | 零依赖安装（vitest 仓内既有），Package Legitimacy Gate 不适用 |
</threat_model>

<verification>

**每任务边界：** T1 `npm run type-check` + `npx vitest run src/constants/status.test.ts` + lint。

**Plan 终态：**
1. `npm run type-check && npm run lint` 全绿
2. `npx vitest run src/constants/status.test.ts` 全绿
3. grep: 7 文件 import @/constants/status；menu VISIBLE 导出完好

</verification>

<success_criteria>

（ROADMAP Phase 69 SC#3 的 status 共享常量部分）

1. status 0/1 走共享常量模块（7 文件收敛、三份漂移消除、VISIBLE 例外完好）—— 由 T1 交付
2. status 不进字典的安全设计落地（Q2 决策）—— 由 T1 源码断言 + threat model T-69-13 背书

</success_criteria>

<output>
Create `.planning/phases/69-dict-and-status-governance/69-06-SUMMARY.md` when done，必须包含：
- 1 个 task × commit hash 对照表
- status.ts 两组常量与后端 models 常量对齐表
- 7 文件 status 收敛映射表（含 workstations label 对齐决策）
- VISIBLE 反转例外完好的证据（grep 输出）
</output>
