---
phase: quick-260814-211
plan: 01
subsystem: backend services (operations/system)
tags: [bugfix, sql, uuid, postgres, workstation, dept]
requires: []
provides:
  - workstation list 子查询 uuid=uuid 等值比较（消除 SQLSTATE 42883）
  - fillLeaderInfo 对 leader 值 UUID 过滤（消除 SQLSTATE 22P02 日志噪音）
affects:
  - POST /ops/workstation/list
  - POST /system/dept/tree
tech-stack:
  added: []
  patterns:
    - constants.UUIDPattern 复用（与 building validateOrg 同标准）
key-files:
  modified:
    - internal/services/operations/workstation_service.go
    - internal/services/system/department_service.go
decisions:
  - 仅去掉 workstationJoinSelect 子查询的 ::text；workstationJoinClause 中 sys_dept.id::text / sys_user.id::text 保留（dept_id/user_id 为 text 列，cast 必要）
  - fillLeaderInfo 保持静默容错语义（非 UUID leader 跳过、查询失败 return），不改函数签名
  - leader 治本项（种子/存量清洗/字段约束）延期至 deferred-items.md
metrics:
  duration: ~10min
  completed: 2026-08-14
  tasks: 2
  files: 2
---

# Quick 260814-211: 修复工位列表 uuid=text 42883 + 部门 leader 非 UUID 22P02 Summary

修复后端运行日志暴露的两个独立 SQL 错误：工位列表子查询 uuid=text 类型不匹配（42883，500）单行去 cast；部门树 fillLeaderInfo 对非 UUID leader 值查询前过滤（22P02 日志噪音）。

## Tasks

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | 去掉 workstationJoinSelect 中 sys_workstation.id 的多余 ::text | c9ab875 | internal/services/operations/workstation_service.go |
| 2 | fillLeaderInfo 用 constants.UUIDPattern 过滤 leaderIDs 再查 sys_user | 08d97ed | internal/services/system/department_service.go |

## Verification

- `go build ./...` exit 0（两个 task 均通过）
- `go test ./internal/services/system/...` PASS（无回归）
- Task 1 grep 门：`workstation_id = sys_workstation.id` 命中且无 `::text`；join clause 中 sys_dept.id::text / sys_user.id::text 未动
- Task 2 grep 门：`constants.UUIDPattern.MatchString` 命中；`Where("id IN ?", validLeaderIDs)`；空集合提前 return
- 运行态端到端验证（PG 实际 200 / 日志无 42883/22P02）未在本环境执行，属部署后确认项

## Deviations from Plan

None — plan executed exactly as written.

## Deferred

- 错误①治本项（统一 sys_dept.leader 为 user uuid：init_data.go 种子 + 存量清洗 + dept.go 字段语义）→ 见 [deferred-items.md](./deferred-items.md)

## Threat Flags

None — 无新增网络端点 / 鉴权路径 / schema 变更。
