# 部署和运维文档

> 更新日期：2025-12-23
> 项目版本：v1.0.0

## 目录

1. [技术栈版本](#技术栈版本)
2. [部署架构](#部署架构)
3. [环境准备](#环境准备)
4. [应用部署](#应用部署)
5. [监控模块部署](#监控模块部署)
6. [CI/CD流水线](#cicd流水线)
7. [监控和告警](#监控和告警)
8. [备份和恢复](#备份和恢复)
9. [运维脚本](#运维脚本)
10. [故障排查](#故障排查)

---

## 技术栈版本

### 后端技术栈

| 技术类别 | 技术名称 | 版本号 | 说明 |
|---------|---------|--------|------|
| 编程语言 | Go | 1.23.2+ | 最新稳定版本 |
| Web框架 | Gin | 1.10+ | 轻量级、高性能 |
| ORM框架 | GORM | 1.25+ | 功能完善的ORM |
| 数据库 | PostgreSQL | 18.1+ | I/O性能提升3倍 |
| 缓存 | Redis | 7.4+ | 性能提升35% |
| 消息队列 | NATS | 2.10+ | 云原生消息系统 |
| 加密算法 | SM2/SM3/SM4 | - | 国密算法 |

### 前端技术栈

| 技术类别 | 技术名称 | 版本号 | 说明 |
|---------|---------|--------|------|
| 构建环境 | Node.js | 24.12+ | 当前LTS版本 |
| UI框架 | React | 19.2+ | 最新稳定版 |
| 类型系统 | TypeScript | 5.9+ | 最新稳定版 |
| 构建工具 | Vite | 6.0+ | 快速构建工具 |
| CSS框架 | Tailwind CSS | 3.4+ | 原子化CSS |
| 状态管理 | Zustand | 5.0+ | 轻量级状态管理 |
| UI组件库 | Ant Design | 5.21+ | 企业级组件 |
| HTTP客户端 | Axios | 1.7+ | API请求 |

### 开发工具链

| 工具类别 | 工具名称 | 版本号 |
|---------|---------|--------|
| 代码编辑器 | VS Code | 1.96+ |
| 包管理器 | npm | 10.9+ |
| 版本控制 | Git | 2.48+ |
| API测试 | Postman | 11.5+ |
| 数据库管理 | pgAdmin | 8.12+ |
| Redis管理 | RedisInsight | 2.60+ |

### 基础设施

| 组件 | 技术名称 | 版本号 |
|------|---------|--------|
| 操作系统 | Rocky Linux | 9+ |
| 反向代理 | Nginx | 1.27+ |
| 证书 | Let's Encrypt | - |
| 监控可视化 | Grafana | 11.4+ |
| 日志收集 | ELK Stack | 8.17+ |

---

## 部署架构

### 1.1 部署架构图

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   前端服务      │    │   后端服务      │    │   数据库服务    │
│   (React)       │    │   (Go/Gin)      │    │   (PostgreSQL)  │
│   Port: 4000    │◄──►│   Port: 9000    │◄──►│   Port: 5432    │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                                │
                                ▼
                       ┌─────────────────┐
                       │   缓存服务      │
                       │   (Redis)       │
                       │   Port: 6379    │
                       └─────────────────┘
```

### 1.2 环境规划

| 环境 | 用途 | 配置 | 数据 |
|------|------|------|------|
| **开发环境** | 功能开发和调试 | 2核4G，单机部署 | 测试数据 |
| **测试环境** | 功能测试和集成测试 | 4核8G，单机部署 | 测试数据 |
| **预生产环境** | 上线前验证 | 8核16G，2节点 | 生产数据备份 |
| **生产环境** | 正式运行环境 | 16核32G，3+节点 | 生产数据 |

### 1.3 部署方式

- **容器化部署**：Docker + Docker Compose（开发/测试）
- **容器编排**：Kubernetes（生产）
- **传统部署**：Systemd服务（特定场景）

---

## 环境准备

### 2.1 系统要求

#### 最低配置

```yaml
# 开发环境
development:
  cpu: 2 cores
  memory: 4 GB RAM
  disk: 50 GB SSD
  os: Linux (Ubuntu 20.04+ / CentOS 8+) / Windows 10+

# 生产环境（每节点）
production:
  cpu: 8 cores minimum (16 recommended)
  memory: 16 GB RAM minimum (32 GB recommended)
  disk: 100 GB SSD minimum (500 GB recommended)
  os: Linux (CentOS 8+ / RHEL 8+ / Ubuntu 20.04+)
  network: Gigabit Ethernet minimum
```

#### 软件依赖

```bash
# 基础软件
- Docker 20.10+
- Docker Compose 2.0+
- Kubernetes 1.24+ (生产)

# 数据库
- PostgreSQL 15+
- Redis 7.0+
- NATS 2.9+ (可选)

# 开发工具
- Git 2.30+
- Make 4.2+
- OpenSSL 1.1.1+
```

### 2.2 环境验证

#### 2.2.1 验证Go环境

```bash
go version
# 应显示: go version go1.23.2 windows/amd64 (或平台对应版本)
```

#### 2.2.2 验证Node.js环境

```bash
node --version
# 应显示: v24.x.x 或更高

npm --version
# 应显示: 10.x.x 或更高
```

#### 2.2.3 验证PostgreSQL服务

```bash
# Windows
net start postgresql-x64-18

# Linux
sudo systemctl start postgresql

# 验证连接
psql -h localhost -U postgres
```

#### 2.2.4 验证Redis服务

```bash
# Windows
redis-server

# Linux
sudo systemctl start redis

# 验证连接
redis-cli ping
# 应返回: PONG
```

---

## 应用部署

### 3.1 数据库部署

#### 3.1.1 创建数据库

```sql
CREATE DATABASE xingran_go;
CREATE USER xingran_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE xingran_go TO xingran_user;
```

#### 3.1.2 创建数据库表结构

**方法一：自动迁移（推荐）**

表结构由 `internal/core/db/migrations/` 目录下的 SQL 脚本在应用启动时自动执行，
无需手动干预。相关配置位于 `configs/config.yaml` 的 `database.auto_migrate` 字段。

**方法二：直接执行SQL**

```bash
psql -h localhost -U postgres -d xingran_go -f internal/core/db/migrations/<migration_file>.sql
```

#### 3.1.3 验证表创建

```sql
\dt
# 应显示以下表:
# sys_user, sys_role, sys_menu, sys_dept
# sys_server_info, sys_system_metrics
# sys_oper_log, sys_logininfor
# sys_job, sys_job_log
# sys_cache_info, sys_cache_stats
```

### 3.2 后端服务部署

#### 3.2.1 安装依赖

```bash
go mod download
go mod tidy
```

#### 3.2.2 配置文件修改

编辑 `configs/config.yaml`：

```yaml
server:
  port: 9000
  mode: debug

database:
  host: localhost
  port: 5432
  user: postgres
  password: your_password
  name: xingran_go
  sslmode: disable

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0
```

#### 3.2.3 编译后端

```bash
# 编译为可执行文件
go build -o xingran-server ./cmd/main.go

# 或者直接运行
go run ./cmd/main.go
```

#### 3.2.4 验证后端服务

```bash
# 检查端口监听
netstat -an | findstr :9000

# 测试API
curl -X GET http://localhost:9000/health
# 应返回: {"code":0,"msg":"success"}
```

### 3.3 前端服务部署

#### 3.3.1 进入前端目录

```bash
cd xingran-react-frontend
```

#### 3.3.2 安装依赖

```bash
npm install
```

#### 3.3.3 环境配置

创建 `.env` 文件：

```env
VITE_API_BASE_URL=http://localhost:9000/api/v1
```

#### 3.3.4 编译前端

```bash
# 开发模式运行
npm run dev

# 生产模式编译
npm run build
```

#### 3.3.5 验证前端服务

- 访问: http://localhost:4000
- 应能看到登录页面

### 3.4 Docker Compose部署（推荐开发/测试）

> 注意：本仓库根目录及 `xingran-react-frontend/` 目录当前未提供 Dockerfile 与
> `docker-compose.yml`（仅在 `rpa-worker/deployments/` 存在 `docker-compose.yml`）。
> 下方为推荐的目标部署配置样例，用于本地或测试环境的容器化部署参考。
> 实际使用前请根据目标环境补全相应的 `Dockerfile` 与编排文件。

#### 3.4.1 docker-compose.yml（参考样例）

```yaml
version: '3.8'

services:
  # PostgreSQL数据库
  postgres:
    image: postgres:15-alpine
    container_name: xingran-postgres
    environment:
      POSTGRES_DB: xingran_go
      POSTGRES_USER: xingran
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: unless-stopped

  # Redis缓存
  redis:
    image: redis:7-alpine
    container_name: xingran-redis
    command: redis-server --requirepass ${REDIS_PASSWORD}
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    restart: unless-stopped

  # 后端服务
  xingran-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: xingran-backend
    environment:
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=xingran_go
      - DB_USER=xingran
      - DB_PASSWORD=${POSTGRES_PASSWORD}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=${REDIS_PASSWORD}
    ports:
      - "9000:9000"
    depends_on:
      - postgres
      - redis
    restart: unless-stopped

  # 前端服务
  xingran-frontend:
    build:
      context: ./xingran-react-frontend
      dockerfile: Dockerfile
    container_name: xingran-frontend
    ports:
      - "4000:80"
    depends_on:
      - xingran-backend
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
```

#### 3.4.2 .env环境变量

```bash
# .env
POSTGRES_PASSWORD=xingran_2024
REDIS_PASSWORD=xingran_redis_2024
```

#### 3.4.3 启动服务

```bash
docker-compose up -d
```

---

## 监控模块部署

### 4.1 数据库表

监控模块包含以下核心数据表：

| 表名 | 说明 |
|------|------|
| sys_server_info | 服务器信息表 |
| sys_system_metrics | 系统性能指标表 |
| sys_oper_log | 操作日志表 |
| sys_logininfor | 登录日志表 |
| sys_job | 定时任务表 |
| sys_job_log | 定时任务执行日志表 |
| sys_cache_info | 缓存信息表 |
| sys_cache_stats | 缓存统计表 |

### 4.2 部署步骤

#### 步骤1：创建数据库表

监控模块相关表由 `internal/core/db/migrations/` 下的 SQL 迁移脚本在应用启动时
自动执行；如需手动执行，可直接运行对应迁移文件。

#### 步骤2：初始化定时任务

系统会自动创建以下定时任务：

| 任务名称 | Cron表达式 | 说明 |
|---------|-----------|------|
| 系统监控任务 | 0 */5 * * * * | 每5分钟收集系统指标 |
| 缓存清理任务 | 0 0 2 * * * | 每天凌晨2点清理过期缓存 |
| 日志归档任务 | 0 0 3 * * 0 | 每周日凌晨3点归档旧日志 |
| 数据备份任务 | 0 0 1 * * * | 每天凌晨1点备份数据库 |

#### 步骤3：验证部署

```sql
-- 查看所有表是否创建成功
SELECT tablename
FROM pg_tables
WHERE schemaname = 'public'
AND tablename IN (
    'sys_server_info', 'sys_system_metrics',
    'sys_oper_log', 'sys_logininfor',
    'sys_job', 'sys_job_log',
    'sys_cache_info', 'sys_cache_stats'
)
ORDER BY tablename;
```

### 4.3 表命名规范

- 所有表使用 `sys_` 前缀
- 登录日志表名为 `sys_logininfor`（不是info）
- 遵循PostgreSQL命名规范（小写+下划线）

### 4.4 字段类型规范

- 主键使用 `varchar(36)` 存储UUID
- 时间戳使用 `timestamp` 类型
- 大文本使用 `text` 类型
- 布尔值使用 `boolean` 类型

---

## CI/CD流水线

### 5.1 简化部署流程

#### 开发环境

```bash
# 1. 拉取代码
git clone <repository-url>
cd xingran-go-backend

# 2. 启动后端
go run ./cmd/main.go

# 3. 启动前端（新终端）
cd xingran-react-frontend
npm run dev
```

#### 生产环境

```bash
# 1. 编译后端
go build -o xingran-server ./cmd/main.go

# 2. 编译前端
cd xingran-react-frontend
npm run build

# 3. 部署
systemctl start xingran-backend
systemctl start xingran-frontend
```

---

## 监控和告警

### 6.1 应用健康检查

```bash
# 检查后端服务
curl http://localhost:9000/health

# 检查数据库连接
psql -h localhost -U postgres -d xingran_go -c "SELECT 1"

# 检查Redis连接
redis-cli ping
```

### 6.2 日志查看

```bash
# 查看后端日志
tail -f logs/xingran-go.log

# 查看数据库日志
tail -f /var/log/postgresql/postgresql-*.log

# 查看Nginx日志
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log
```

### 6.3 性能监控

#### 使用Prometheus + Grafana

```yaml
# prometheus.yml
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'xingran-backend'
    static_configs:
      - targets: ['localhost:9000']
```

---

## 备份和恢复

### 7.1 数据库备份

```bash
#!/bin/bash
# backup_database.sh

DB_HOST="localhost"
DB_PORT="5432"
DB_NAME="xingran_go"
DB_USER="postgres"
BACKUP_DIR="/backup/postgresql"
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_FILE="${BACKUP_DIR}/${DB_NAME}_backup_${DATE}.sql"

# 创建备份目录
mkdir -p ${BACKUP_DIR}

# 执行备份
pg_dump -h ${DB_HOST} -p ${DB_PORT} -U ${DB_USER} -d ${DB_NAME} \
    --verbose --clean --if-exists \
    --format=custom --file=${BACKUP_FILE}

# 压缩备份文件
gzip ${BACKUP_FILE}

echo "Database backup completed: ${BACKUP_FILE}.gz"
```

### 7.2 数据库恢复

```bash
#!/bin/bash
# restore_database.sh

BACKUP_FILE=$1
DB_HOST="localhost"
DB_PORT="5432"
DB_NAME="xingran_go"
DB_USER="postgres"

# 恢复数据库
pg_restore -h ${DB_HOST} -p ${DB_PORT} -U ${DB_USER} -d ${DB_NAME} \
    --verbose --clean --if-exists ${BACKUP_FILE}

echo "Database restore completed!"
```

---

## 运维脚本

### 8.1 健康检查脚本

```bash
#!/bin/bash
# health_check.sh

echo "Checking services health..."

# 检查后端服务
echo -n "Checking backend service... "
if curl -sf http://localhost:9000/health > /dev/null; then
    echo "OK"
else
    echo "FAILED"
fi

# 检查数据库
echo -n "Checking database... "
if psql -h localhost -U postgres -d xingran_go -c "SELECT 1" > /dev/null 2>&1; then
    echo "OK"
else
    echo "FAILED"
fi

# 检查Redis
echo -n "Checking Redis... "
if redis-cli ping > /dev/null 2>&1; then
    echo "OK"
else
    echo "FAILED"
fi
```

### 8.2 日志查看脚本

```bash
#!/bin/bash
# logs.sh

SERVICE=$1
LINES=${2:-100}

case $SERVICE in
    backend)
        tail -n $LINES logs/xingran-go.log
        ;;
    database)
        tail -n $LINES /var/log/postgresql/postgresql-*.log
        ;;
    redis)
        tail -n $LINES /var/log/redis/redis-server.log
        ;;
    *)
        echo "Usage: $0 {backend|database|redis} [lines]"
        exit 1
        ;;
esac
```

---

## 故障排查

### 9.1 常见问题及解决方案

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| **后端启动失败** | 端口9000被占用 | 检查端口占用，终止占用进程 |
| **数据库连接失败** | PostgreSQL服务未启动 | 启动PostgreSQL服务 |
| **前端API请求失败** | CORS配置问题 | 检查后端CORS中间件配置 |
| **Redis连接失败** | Redis服务未启动 | 启动Redis服务 |
| **表不存在错误** | 数据库表未创建 | 执行数据库迁移脚本 |
| **JWT验证失败** | Token过期或密钥错误 | 重新登录或检查JWT配置 |

### 9.2 排查命令

```bash
# 查看端口占用
netstat -ano | findstr :9000

# 查看进程
ps aux | grep xingran-server

# 测试数据库连接
psql -h localhost -U postgres -d xingran_go

# 测试Redis连接
redis-cli -a password ping

# 查看系统资源
top
htop
df -h
```

### 9.3 故障排查流程

1. **检查服务状态**
   ```bash
   systemctl status xingran-backend
   systemctl status postgresql
   systemctl status redis
   ```

2. **查看日志**
   ```bash
   journalctl -u xingran-backend -f
   tail -f logs/xingran-go.log
   ```

3. **测试连接**
   ```bash
   curl http://localhost:9000/health
   ```

4. **检查配置**
   ```bash
   cat configs/config.yaml
   ```

---

## 安全建议

1. **更改默认密码**
   - 数据库密码
   - Redis密码
   - 管理员账号密码

2. **网络安全**
   - 使用HTTPS
   - 配置防火墙规则
   - 限制数据库访问IP

3. **应用安全**
   - 启用CSRF保护
   - 配置XSS防护
   - 定期更新依赖包

---

## 版本更新记录

| 日期 | 版本 | 更新内容 |
|------|------|----------|
| 2025-12-23 | 1.0.0 | 整合技术栈版本、部署架构、监控模块部署 |
| 2025-12-16 | 0.2.0 | 添加监控模块部署说明 |
| 2025-12-16 | 0.1.0 | 初始版本 |

---

## 参考链接

- [Go 1.23 Release Notes](https://go.dev/doc/devel/release#go1.23)
- [React 19 Announcement](https://react.dev/blog/2024/12/05/react-19)
- [PostgreSQL 18 Release](https://www.postgresql.org/about/news/postgresql-18-released-3142/)
- [Redis 7.4 Release Notes](https://redis.io/topics/release-notes)
